import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'scala-fp-l4-001',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is a Free Monad and what problem does it solve in functional architecture?',
    options: [
      'A monad that can be used without import statements',
      'A data structure that separates the description of a program (as an AST) from its interpretation, enabling multiple interpreters for the same program',
      'A monad that automatically frees memory',
      'A monad that has no computational cost',
    ],
    correctAnswer: 1,
    explanation: 'A Free Monad lifts an algebra (ADT of operations) into a monadic structure, creating an AST (Abstract Syntax Tree) representing the program. The program is then executed by an interpreter (natural transformation) that maps each operation to an effect. This separation allows swapping interpreters (production vs test, sync vs async) without changing the program logic.',
    codeSnippet: 'sealed trait KVStoreA[A]\ncase class Put[T](key: String, value: T) extends KVStoreA[Unit]\ncase class Get[T](key: String) extends KVStoreA[Option[T]]\ncase class Delete(key: String) extends KVStoreA[Unit]\n\ntype KVStore[A] = Free[KVStoreA, A]\n\ndef put[T](key: String, value: T): KVStore[Unit] =\n  Free.liftF(Put(key, value))\ndef get[T](key: String): KVStore[Option[T]] =\n  Free.liftF(Get(key))',
    tags: ['free-monad', 'interpreter', 'architecture'],
  },
  {
    id: 'scala-fp-l4-002',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is a natural transformation (~>) in the context of Free Monads?',
    options: [
      'A type conversion between two unrelated types',
      'A built-in Scala compiler optimization',
      'A polymorphic function from F[A] to G[A] for all A, used as the interpreter that maps each algebra operation to a target monad',
      'A transformation that preserves the original data structure',
    ],
    correctAnswer: 2,
    explanation: 'A natural transformation (FunctionK or ~>) is a function from F[A] to G[A] that works for any type A. In Free Monad programs, the interpreter is a natural transformation from the algebra functor to the target monad (e.g., KVStoreA ~> IO). It maps each case of the algebra ADT to an effectful computation in the target monad.',
    codeSnippet: 'import cats.~>\n\nval interpreter: KVStoreA ~> Id = new (KVStoreA ~> Id) {\n  val store = scala.collection.mutable.Map.empty[String, Any]\n  def apply[A](fa: KVStoreA[A]): Id[A] = fa match {\n    case Put(key, value) => store(key) = value\n    case Get(key) => store.get(key).asInstanceOf[A]\n    case Delete(key) => store.remove(key); ()\n  }\n}\n\n// Run the program with the interpreter\nprogram.foldMap(interpreter)',
    tags: ['free-monad', 'natural-transformation', 'interpreter'],
  },
  {
    id: 'scala-fp-l4-003',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the main performance concern with Free Monads and how can it be mitigated?',
    options: [
      'Each flatMap builds a nested data structure that can cause stack overflow on deep programs; this is mitigated by using trampolining (built into Cats Free) which converts recursion to heap-allocated thunks',
      'Free Monads are slower than mutable code due to garbage collection',
      'Free Monads cannot be parallelized',
      'Free Monads use too much disk space',
    ],
    correctAnswer: 0,
    explanation: 'Free Monads build an AST where each flatMap adds a layer of nesting. Interpreting this naively is recursive and can overflow the stack for deep programs. Cats\' Free implementation uses trampolining internally: instead of recursive calls, it creates heap-allocated continuation objects that are evaluated in a loop. This makes Free stack-safe but allocates more objects than direct-style code.',
    tags: ['free-monad', 'performance', 'trampolining'],
  },
  {
    id: 'scala-fp-l4-004',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'How do you combine multiple Free Monad algebras in a single program?',
    options: [
      'Simply use both algebras without any special handling',
      'Use Coproduct (EitherK) to combine algebra types and InjectK to lift operations into the combined algebra, allowing programs to use operations from multiple algebras',
      'Merge all operations into a single large algebra',
      'You cannot combine multiple algebras',
    ],
    correctAnswer: 1,
    explanation: 'To combine algebras A and B, you use EitherK[A, B, ?] as the combined functor. InjectK[A, F] provides automatic lifting of algebra A into the combined type F. Each algebra\'s smart constructors use InjectK to inject into the coproduct. The interpreter handles both sides of the EitherK. This pattern enables modular DSL composition.',
    codeSnippet: 'type App[A] = EitherK[KVStoreA, LoggingA, A]\n\ndef put[F[_]](key: String, value: String)\n  (implicit I: InjectK[KVStoreA, F]): Free[F, Unit] =\n  Free.inject[KVStoreA, F](Put(key, value))\n\ndef log[F[_]](msg: String)\n  (implicit I: InjectK[LoggingA, F]): Free[F, Unit] =\n  Free.inject[LoggingA, F](Log(msg))\n\ndef program: Free[App, Unit] = for {\n  _ <- log[App]("Starting")\n  _ <- put[App]("key", "value")\n} yield ()',
    tags: ['free-monad', 'coproduct', 'composition'],
  },
  {
    id: 'scala-fp-l4-005',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the Tagless Final encoding and how does it differ from Free Monads?',
    options: [
      'Tagless Final is a newer version of Free Monads with the same implementation',
      'Tagless Final encodes programs as method calls on a typeclass (algebra trait parameterized by F[_]), with no intermediate AST; interpretation happens by providing a typeclass instance, which is more performant than Free but less inspectable',
      'Tagless Final removes all type parameters from the code',
      'Tagless Final uses a final class instead of a sealed trait',
    ],
    correctAnswer: 1,
    explanation: 'Tagless Final defines the algebra as a trait with an abstract type constructor F[_]: trait KVStore[F[_]] { def get(key: String): F[Option[String]] }. Programs are written as def program[F[_]: KVStore: Monad]. Interpretation is providing an instance: implicit val ioKVStore: KVStore[IO] = ???. Unlike Free Monads, there is no intermediate data structure, making it more performant but impossible to inspect or optimize the program before running it.',
    codeSnippet: 'trait KVStore[F[_]] {\n  def get(key: String): F[Option[String]]\n  def put(key: String, value: String): F[Unit]\n}\n\ndef program[F[_]: KVStore: Monad]: F[Option[String]] = for {\n  _ <- KVStore[F].put("key", "value")\n  result <- KVStore[F].get("key")\n} yield result\n\n// Interpretation: provide instance for IO\nimplicit val ioStore: KVStore[IO] = new KVStore[IO] {\n  def get(key: String) = IO(db.get(key))\n  def put(key: String, value: String) = IO(db.put(key, value))\n}',
    tags: ['tagless-final', 'typeclass', 'encoding'],
  },
  {
    id: 'scala-fp-l4-006',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'How do you compose multiple Tagless Final algebras in a single program?',
    options: [
      'Simply add multiple context bounds (implicit parameters) to the program function; the caller provides instances for all required algebras',
      'Create a super-algebra that extends all other algebras',
      'You can only use one algebra at a time',
      'Use coproducts like with Free Monads',
    ],
    correctAnswer: 0,
    explanation: 'In Tagless Final, composing algebras is trivial: add multiple type class constraints. def program[F[_]: Monad: KVStore: Logger: Auth] can use operations from all three algebras. At the call site, you provide instances for each. This is simpler than Free Monad coproducts but means you cannot easily introspect which algebras a program uses at the type level.',
    codeSnippet: 'trait Logger[F[_]] {\n  def info(msg: String): F[Unit]\n}\n\ntrait Auth[F[_]] {\n  def authenticate(token: String): F[Boolean]\n}\n\ndef secureProgram[F[_]: Monad: KVStore: Logger: Auth](\n  token: String, key: String\n): F[Option[String]] = for {\n  valid <- Auth[F].authenticate(token)\n  _ <- Logger[F].info(s"Auth result: $valid")\n  result <- if (valid) KVStore[F].get(key)\n            else Monad[F].pure(None)\n} yield result',
    tags: ['tagless-final', 'composition', 'algebras'],
  },
  {
    id: 'scala-fp-l4-007',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "companion object apply trick" used in Tagless Final algebras?',
    options: [
      'It is a macro that generates boilerplate code',
      'It applies default values to the algebra methods',
      'It defines an apply method on the companion object that summons the implicit instance, enabling KVStore[F] syntax instead of implicitly[KVStore[F]]',
      'It creates new instances of the algebra automatically',
    ],
    correctAnswer: 2,
    explanation: 'The companion object defines def apply[F[_]](implicit ev: KVStore[F]): KVStore[F] = ev. This allows writing KVStore[F].get(key) instead of the verbose implicitly[KVStore[F]].get(key). This is a standard Scala pattern for typeclasses, sometimes called the "summoner" pattern.',
    codeSnippet: 'trait KVStore[F[_]] {\n  def get(key: String): F[Option[String]]\n  def put(key: String, value: String): F[Unit]\n}\n\nobject KVStore {\n  def apply[F[_]](implicit ev: KVStore[F]): KVStore[F] = ev\n}\n\n// Usage: clean syntax\ndef program[F[_]: KVStore: Monad]: F[Unit] =\n  KVStore[F].put("hello", "world")',
    tags: ['tagless-final', 'summoner', 'syntax'],
  },
  {
    id: 'scala-fp-l4-008',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'How do you test Tagless Final programs?',
    options: [
      'Tagless Final programs cannot be unit tested',
      'Provide a test interpreter instance of the algebra, typically using a State monad or Id monad, that records operations and returns predetermined values',
      'Test against the production interpreter',
      'Use mocking frameworks like Mockito',
    ],
    correctAnswer: 1,
    explanation: 'For testing, create instances of your algebras targeting a test monad. Using State[TestState, *] lets you record all operations and assert on them. Using Id lets you run synchronous tests. Using IO with in-memory implementations provides integration-level tests. This is the main benefit of Tagless Final: programs are decoupled from their effects.',
    codeSnippet: 'case class TestState(store: Map[String, String], logs: List[String])\ntype Test[A] = State[TestState, A]\n\nimplicit val testKVStore: KVStore[Test] = new KVStore[Test] {\n  def get(key: String) = State.inspect(_.store.get(key))\n  def put(key: String, value: String) =\n    State.modify(s => s.copy(store = s.store + (key -> value)))\n}\n\n// Test\nval (finalState, result) = program[Test]\n  .run(TestState(Map.empty, Nil)).value\nassert(finalState.store == Map("key" -> "value"))',
    tags: ['tagless-final', 'testing', 'state-monad'],
  },
  {
    id: 'scala-fp-l4-009',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is referential transparency and why is it fundamental to effect systems like Cats Effect and ZIO?',
    options: [
      'It means all references are transparent (public)',
      'It is a garbage collection optimization for reference types',
      'It means references are resolved at compile time',
      'An expression is referentially transparent if it can be replaced with its value without changing program behavior; effect systems wrap side effects in a referentially transparent data type (IO) so programs can be reasoned about equationally',
    ],
    correctAnswer: 3,
    explanation: 'Referential transparency means substituting an expression with its value produces the same result. println("hello") is not RT because calling it twice has different effects than evaluating it once and reusing the result. IO(println("hello")) IS RT because it just describes the effect. val x = IO(...); (x, x) is the same as (IO(...), IO(...)). This property enables local reasoning and safe refactoring.',
    codeSnippet: '// Not referentially transparent\nval a = println("hello")  // prints once\n(a, a)  // prints once total\n// vs\n(println("hello"), println("hello"))  // prints twice\n\n// Referentially transparent with IO\nval b = IO(println("hello"))  // describes, does not execute\n(b, b)  // two descriptions, same as:\n(IO(println("hello")), IO(println("hello")))  // equivalent!',
    tags: ['effect-systems', 'referential-transparency', 'reasoning'],
  },
  {
    id: 'scala-fp-l4-010',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the difference between Cats Effect IO and ZIO in terms of their core type signatures?',
    options: [
      'IO[A] has one type parameter (the result type), while ZIO[R, E, A] has three: R (environment/dependencies), E (error type), A (success type), providing compile-time tracking of dependencies and error types',
      'ZIO uses untyped errors while IO uses typed errors',
      'They are identical in type signatures',
      'IO supports concurrency while ZIO does not',
    ],
    correctAnswer: 0,
    explanation: 'Cats Effect IO[A] wraps a computation that may fail with Throwable. ZIO[R, E, A] additionally tracks: R (required environment, enabling dependency injection), E (typed error channel, enabling exhaustive error handling). ZIO\'s richer type signature provides more compile-time safety. Cats Effect achieves similar DI via Tagless Final patterns and error tracking via MonadError.',
    codeSnippet: '// Cats Effect IO\ndef fetchUser(id: UserId): IO[User] // may throw anything\n\n// ZIO\ndef fetchUser(id: UserId): ZIO[Database, DbError, User]\n// Requires Database service, may fail with DbError, succeeds with User\n\n// Type aliases in ZIO\ntype Task[A] = ZIO[Any, Throwable, A]  // like IO\ntype UIO[A] = ZIO[Any, Nothing, A]    // cannot fail\ntype URIO[R, A] = ZIO[R, Nothing, A]  // requires R, cannot fail',
    tags: ['effect-systems', 'io', 'zio'],
  },
  {
    id: 'scala-fp-l4-011',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the Resource type in Cats Effect and why is it important?',
    options: [
      'It tracks memory usage of allocated objects',
      'It is a wrapper around Java NIO resources',
      'Resource[F, A] encapsulates safe acquisition and guaranteed release of resources (files, connections, etc.), composable via flatMap so that nested resources are released in reverse order even on failure',
      'It is a data type for managing REST resources',
    ],
    correctAnswer: 2,
    explanation: 'Resource[F, A] pairs acquire: F[A] with release: A => F[Unit]. The release is guaranteed to run even if the use block fails. Resources compose with flatMap: Resource.make(openDb)(_.close).flatMap(db => Resource.make(openPool(db))(_.close)) releases the pool before the db. This is the bracket pattern generalized to be composable.',
    codeSnippet: 'def dbPool: Resource[IO, ConnectionPool] =\n  Resource.make(\n    IO(ConnectionPool.create(config))  // acquire\n  )(pool => IO(pool.close()))          // release\n\ndef httpClient: Resource[IO, HttpClient] =\n  Resource.make(IO(HttpClient.create()))(c => IO(c.close()))\n\n// Compose: both released on completion/failure\nval app: Resource[IO, (ConnectionPool, HttpClient)] = for {\n  pool <- dbPool\n  client <- httpClient\n} yield (pool, client)\n\napp.use { case (pool, client) => myApp(pool, client) }',
    tags: ['effect-systems', 'resource', 'safety'],
  },
  {
    id: 'scala-fp-l4-012',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is a Monad Transformer and why is it needed?',
    options: [
      'It allows composing two monadic effects into one, solving the problem that monads do not compose generically; e.g., OptionT[IO, A] combines IO and Option so you can flatMap over the combined IO[Option[A]] as a single monad',
      'It optimizes monad performance',
      'It transforms data from one type to another',
      'It transforms monads into functors',
    ],
    correctAnswer: 0,
    explanation: 'Unlike functors and applicatives, monads do not compose in general. If you have IO[Option[A]], you cannot flatMap over A directly. OptionT[IO, A] wraps IO[Option[A]] and provides a Monad instance for the combined stack: flatMap chains through both IO and Option. Similarly, EitherT[IO, E, A] wraps IO[Either[E, A]]. This avoids nested pattern matching on inner values.',
    codeSnippet: 'import cats.data.OptionT\n\n// Without OptionT: nested flatMap + pattern match\ndef findUser(id: Int): IO[Option[User]] = ???\ndef findAddress(user: User): IO[Option[Address]] = ???\n\n// Painful:\nfindUser(1).flatMap {\n  case Some(user) => findAddress(user)\n  case None => IO.pure(None)\n}\n\n// With OptionT: clean flatMap\nval result: OptionT[IO, Address] = for {\n  user <- OptionT(findUser(1))\n  addr <- OptionT(findAddress(user))\n} yield addr',
    tags: ['monad-transformers', 'composition', 'optionT'],
  },
  {
    id: 'scala-fp-l4-013',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is the performance overhead of stacking multiple Monad Transformers?',
    options: [
      'Each layer adds allocation overhead: a stack of n transformers wraps/unwraps n layers per flatMap, creating O(n) allocations per operation, which can be significant in tight loops',
      'Monad Transformers have no performance overhead',
      'Monad Transformers improve performance through fusion',
      'The overhead is only at compile time, not runtime',
    ],
    correctAnswer: 0,
    explanation: 'Each Monad Transformer wraps the underlying monad. With EitherT[OptionT[IO, *], Error, A], every flatMap must allocate and pattern-match through both transformer layers. For a stack of 3+ transformers, this creates significant allocation pressure. Alternatives include: using a custom monad (like ZIO which has built-in error and environment channels), or MTL-style typeclasses which avoid the wrapping overhead.',
    codeSnippet: '// Deep stack: 3 layers of wrapping per flatMap\ntype Stack[A] = EitherT[StateT[IO, AppState, *], AppError, A]\n\n// Each flatMap on Stack[A] performs:\n// 1. Unwrap EitherT -> StateT[IO, AppState, Either[AppError, A]]\n// 2. Unwrap StateT -> IO[AppState => (AppState, Either[AppError, A])]\n// 3. Run IO\n// 4. Re-wrap all layers\n\n// Alternative: ZIO has all three built-in\ntype App[A] = ZIO[AppState, AppError, A]  // no wrapping overhead',
    tags: ['monad-transformers', 'performance', 'allocation'],
  },
  {
    id: 'scala-fp-l4-014',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is the MTL (Monad Transformer Library) style and how does it differ from concrete Monad Transformer stacks?',
    options: [
      'MTL is a specific monad transformer implementation',
      'MTL uses macros to generate transformer code',
      'MTL style uses typeclasses like MonadError[F, E] and MonadState[F, S] to abstract over the monad capabilities, avoiding commitment to a specific transformer stack and its associated performance overhead',
      'MTL eliminates the need for monads entirely',
    ],
    correctAnswer: 2,
    explanation: 'Instead of writing programs against concrete stacks like EitherT[StateT[IO, S, *], E, A], MTL style uses typeclass constraints: def program[F[_]: Monad: MonadError[*, E]: MonadState[*, S]]. The program works with any F that provides these capabilities. At the edge, you instantiate F to a concrete type. This decouples programs from specific stack orderings and can eliminate transformer overhead if F natively supports the capabilities.',
    codeSnippet: 'import cats.mtl.{Ask, Raise, Stateful}\n\ndef program[F[_]: Monad](\n  implicit\n  state: Stateful[F, AppState],\n  errors: Raise[F, AppError],\n  config: Ask[F, Config]\n): F[Result] = for {\n  cfg <- config.ask\n  s <- state.get\n  _ <- if (s.isValid) state.set(s.updated)\n       else errors.raise(InvalidState)\n} yield Result(s)',
    tags: ['mtl', 'typeclasses', 'abstraction'],
  },
  {
    id: 'scala-fp-l4-015',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Church Encoding',
    question: 'What is Church encoding in the context of functional programming?',
    options: [
      'A method of encoding binary data as text',
      'A way to encode data in a church database',
      'Representing data types as functions (their elimination/fold function) rather than as algebraic data types, enabling encoding of data in any language that supports higher-order functions',
      'An encryption algorithm invented by Alonzo Church',
    ],
    correctAnswer: 2,
    explanation: 'Church encoding represents data as its fold function. For example, a Boolean is encoded as a function that takes two arguments and returns one: true = (a, b) => a, false = (a, b) => b. A natural number n is encoded as a function that applies f n times: 0 = f => x => x, 1 = f => x => f(x), 2 = f => x => f(f(x)). In Scala, this often appears as CPS (continuation-passing style) or the Bohm-Berarducci encoding.',
    codeSnippet: '// Church-encoded Boolean\ntype ChurchBool = [A] => (A, A) => A\nval cTrue: ChurchBool = [A] => (a: A, b: A) => a\nval cFalse: ChurchBool = [A] => (a: A, b: A) => b\n\n// Church-encoded Option (Maybe)\ntrait ChurchOption[A] {\n  def fold[B](none: => B)(some: A => B): B\n}\nval none: ChurchOption[Nothing] = new ChurchOption[Nothing] {\n  def fold[B](none: => B)(some: Nothing => B): B = none\n}\ndef some[A](a: A): ChurchOption[A] = new ChurchOption[A] {\n  def fold[B](none: => B)(some: A => B): B = some(a)\n}',
    tags: ['church-encoding', 'lambda-calculus', 'data-as-functions'],
  },
  {
    id: 'scala-fp-l4-016',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Church Encoding',
    question: 'What is the Bohm-Berarducci encoding and how does it relate to Church encoding in Scala?',
    options: [
      'It is a method for encoding algebraic effects',
      'It is the typed version of Church encoding for GADTs, where data types are represented as universally quantified fold functions, enabling encoding of complex recursive data types in System F',
      'It is a compression algorithm',
      'It is an alternative to UTF-8 encoding',
    ],
    correctAnswer: 1,
    explanation: 'Bohm-Berarducci encoding is the typed generalization of Church encoding. For a data type like List[A], the encoding is: type List[A] = [B] => (B, (A, B) => B) => B (the fold function). In Scala, this is often seen as trait List[A] { def fold[B](nil: B)(cons: (A, B) => B): B }. This is the same as a catamorphism/fold and connects Church encoding to recursion schemes.',
    codeSnippet: '// ADT encoding\nsealed trait MyList[+A]\ncase object Nil extends MyList[Nothing]\ncase class Cons[A](head: A, tail: MyList[A]) extends MyList[A]\n\n// Bohm-Berarducci encoding (Church-encoded)\ntrait ChurchList[A] {\n  def fold[B](nil: B)(cons: (A, B) => B): B\n}\n\nval empty: ChurchList[Nothing] = new ChurchList[Nothing] {\n  def fold[B](nil: B)(cons: (Nothing, B) => B): B = nil\n}\ndef cons[A](h: A, t: ChurchList[A]): ChurchList[A] =\n  new ChurchList[A] {\n    def fold[B](nil: B)(cons: (A, B) => B): B =\n      cons(h, t.fold(nil)(cons))\n  }',
    tags: ['church-encoding', 'bohm-berarducci', 'recursion'],
  },
  {
    id: 'scala-fp-l4-017',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Yoneda',
    question: 'What is the Yoneda lemma in the context of Scala functional programming?',
    options: [
      'A lemma about Japanese programming styles',
      'It is a sorting algorithm for functors',
      'It proves that all monads are functors',
      'Yoneda[F, A] is isomorphic to F[A] and represents the idea that a functor value F[A] is equivalent to a natural transformation from Hom(A, -) to F; practically, it fuses consecutive map operations into a single map',
    ],
    correctAnswer: 3,
    explanation: 'The Yoneda lemma states that for any functor F, the type forall B. (A => B) => F[B] is isomorphic to F[A]. In Scala, Yoneda[F, A] wraps a function [B] => (A => B) => F[B]. Lifting an F[A] into Yoneda and mapping N times fuses all the maps: instead of N traversals, only one traversal happens when you lower back to F[A]. This is a "free functor" optimization.',
    codeSnippet: 'trait Yoneda[F[_], A] {\n  def apply[B](f: A => B): F[B]\n}\n\ndef lift[F[_]: Functor, A](fa: F[A]): Yoneda[F, A] =\n  new Yoneda[F, A] {\n    def apply[B](f: A => B): F[B] = fa.map(f)\n  }\n\ndef lower[F[_], A](ya: Yoneda[F, A]): F[A] =\n  ya.apply(identity)\n\n// Map fusion: 3 maps become 1\nval result = lift(list)\n  .map(f).map(g).map(h)  // no intermediate lists!\nlower(result)              // single traversal: list.map(f andThen g andThen h)',
    tags: ['yoneda', 'map-fusion', 'optimization'],
  },
  {
    id: 'scala-fp-l4-018',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Yoneda',
    question: 'What is Coyoneda and why is it useful when you do not have a Functor instance?',
    options: [
      'It is the dual of Yoneda that provides a Cofunctor instance',
      'Coyoneda[F, A] wraps an F[X] and a function X => A, providing a free Functor for any type constructor F, even without a Functor instance for F',
      'It is an optimized version of Yoneda for contravariants',
      'It converts any monad into a comonad',
    ],
    correctAnswer: 1,
    explanation: 'Coyoneda[F, A] = exists X. (F[X], X => A). It stores an original F[X] value and accumulated mapped functions. map on Coyoneda just composes functions without needing a Functor for F. When you finally need the F[A] result, you provide a Functor instance to "lower" it. This is useful for building up maps lazily on types that are not functors, or for fusing maps like Yoneda.',
    codeSnippet: 'trait Coyoneda[F[_], A] {\n  type Pivot\n  val fi: F[Pivot]\n  val k: Pivot => A\n\n  def map[B](f: A => B): Coyoneda[F, B] =\n    Coyoneda.apply(fi)(k andThen f)  // just compose!\n\n  def lower(implicit F: Functor[F]): F[A] =\n    F.map(fi)(k)  // Functor needed only here\n}\n\n// Works even without Functor[Set]!\nval cy: Coyoneda[Set, String] =\n  Coyoneda.lift(Set(1, 2, 3)).map(_.toString).map(_ + "!")',
    tags: ['coyoneda', 'free-functor', 'map-fusion'],
  },
  {
    id: 'scala-fp-l4-019',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Profunctors',
    question: 'What is a Profunctor and how does it relate to regular Functors?',
    options: [
      'A Profunctor is a professional-grade Functor for production code',
      'A Profunctor is a functor that works on product types only',
      'A Profunctor is a monad that can transform between two types',
      'A Profunctor P[A, B] is contravariant in A and covariant in B, generalizing functions; it has a dimap method that maps both input and output types simultaneously',
    ],
    correctAnswer: 3,
    explanation: 'Profunctor[P[_, _]] has dimap[A, B, C, D](pab: P[A, B])(f: C => A)(g: B => D): P[C, D]. The most basic example is Function1: dimap maps the input contravariantly and the output covariantly. Profunctors generalize the concept of "things that consume A and produce B". They are fundamental to optics (lenses, prisms) via the profunctor optics formulation.',
    codeSnippet: 'trait Profunctor[P[_, _]] {\n  def dimap[A, B, C, D](pab: P[A, B])(f: C => A)(g: B => D): P[C, D]\n}\n\n// Function1 is a Profunctor\nimplicit val function1Profunctor: Profunctor[Function1] =\n  new Profunctor[Function1] {\n    def dimap[A, B, C, D](fab: A => B)(f: C => A)(g: B => D): C => D =\n      f andThen fab andThen g\n  }\n\n// Example: adapt a String => Int function to work with User => String\nval strlen: String => Int = _.length\nval nameLen: User => String = Profunctor[Function1]\n  .dimap(strlen)((_: User).name)(_.toString)',
    tags: ['profunctor', 'dimap', 'optics'],
  },
  {
    id: 'scala-fp-l4-020',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is a Lens in functional optics and what problem does it solve?',
    options: [
      'A Lens[S, A] provides a composable getter and setter for a field A within an immutable data structure S, solving the nested update problem without mutation',
      'A Lens converts between two isomorphic types',
      'A Lens is a way to zoom into UI components',
      'A Lens is a logging utility that focuses on specific events',
    ],
    correctAnswer: 0,
    explanation: 'A Lens[S, A] has get: S => A and set: (S, A) => S (or modify: (A => A) => S => S). For deeply nested immutable case classes, updating a field requires copying all intermediate structures. Lenses compose: if you have Lens[Company, Address] and Lens[Address, Street], you can compose them to get Lens[Company, Street]. This eliminates the boilerplate of nested copy() calls.',
    codeSnippet: 'import monocle.Lens\nimport monocle.macros.GenLens\n\ncase class Address(street: String, city: String)\ncase class Person(name: String, address: Address)\n\nval addressLens: Lens[Person, Address] = GenLens[Person](_.address)\nval streetLens: Lens[Address, String] = GenLens[Address](_.street)\n\n// Compose lenses\nval personStreet: Lens[Person, String] = addressLens.andThen(streetLens)\n\n// Use: no nested copy!\nval updated = personStreet.modify(_.toUpperCase)(person)\n// Instead of: person.copy(address = person.address.copy(street = ...))',
    tags: ['optics', 'lens', 'immutable-updates'],
  },
  {
    id: 'scala-fp-l4-021',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is a Prism and how does it differ from a Lens?',
    options: [
      'A Prism provides read-only access to a field',
      'A Prism and a Lens are the same thing',
      'A Prism[S, A] focuses on one case of a sum type (sealed trait), providing a partial getter (getOption: S => Option[A]) and a constructor (reverseGet: A => S), while a Lens focuses on a field of a product type',
      'A Prism splits a type into multiple types',
    ],
    correctAnswer: 2,
    explanation: 'Lens is for product types (case classes): every S has an A. Prism is for sum types (sealed traits): an S might be an A. Prism[S, A] has getOption: S => Option[A] (pattern match) and reverseGet: A => S (constructor). For example, Prism[Shape, Circle] extracts a Circle from a Shape. Prisms compose with Lenses: Prism[Shape, Circle] andThen Lens[Circle, Double] focuses on the radius of circles.',
    codeSnippet: 'import monocle.Prism\n\nsealed trait Shape\ncase class Circle(radius: Double) extends Shape\ncase class Rect(w: Double, h: Double) extends Shape\n\nval circlePrism: Prism[Shape, Circle] = Prism.partial[Shape, Circle] {\n  case c: Circle => c\n}(identity)\n\nval radiusLens: Lens[Circle, Double] = GenLens[Circle](_.radius)\n\n// Compose: focus on radius of circles only\nval shapeRadius = circlePrism.andThen(radiusLens)\n\nshapeRadius.getOption(Circle(5.0))  // Some(5.0)\nshapeRadius.getOption(Rect(3, 4))   // None\nshapeRadius.modify(_ * 2)(Circle(5.0))  // Circle(10.0)',
    tags: ['optics', 'prism', 'sum-types'],
  },
  {
    id: 'scala-fp-l4-022',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is a Traversal in functional optics?',
    options: [
      'It converts between two collection types',
      'It traverses a tree data structure',
      'It is a type of Iterator in functional programming',
      'A Traversal[S, A] focuses on zero or more targets of type A within S, generalizing Lens (exactly one) and Prism (zero or one) to multiple targets, enabling batch modifications',
    ],
    correctAnswer: 3,
    explanation: 'Traversal[S, A] provides getAll: S => List[A] and modify: (A => A) => S => S over multiple focuses. For example, a Traversal that focuses on all elements of a List, or all Strings in a nested data structure. Traversals compose with Lenses and Prisms. The optics hierarchy: Iso > Lens > Prism > Optional > Traversal (each generalizes the previous).',
    codeSnippet: 'import monocle.Traversal\nimport monocle.macros.GenLens\n\ncase class Department(name: String, employees: List[Employee])\ncase class Employee(name: String, salary: Double)\n\nval employees = GenLens[Department](_.employees)\nval salary = GenLens[Employee](_.salary)\n\n// Traversal: focus on all employee salaries\nval allSalaries: Traversal[Department, Double] =\n  employees.andThen(Traversal.fromTraverse[List, Employee]).andThen(salary)\n\n// Give everyone a 10% raise\nval updated = allSalaries.modify(_ * 1.1)(department)',
    tags: ['optics', 'traversal', 'batch-update'],
  },
  {
    id: 'scala-fp-l4-023',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is the profunctor optics formulation and why is it considered more elegant than the van Laarhoven encoding?',
    options: [
      'It stores optics as binary data for efficient serialization',
      'It uses GPU acceleration for optical computations',
      'It replaces optics with direct field access',
      'It represents optics as polymorphic functions over profunctors: type Optic[P[_, _], S, T, A, B] = P[A, B] => P[S, T], unifying all optics under a single type by constraining the profunctor P differently for each optic kind',
    ],
    correctAnswer: 3,
    explanation: 'In profunctor optics, all optics are functions P[A, B] => P[S, T] where P is constrained to different profunctor subclasses. A Lens requires P: Strong, a Prism requires P: Choice, a Traversal requires P: Wander. This gives a uniform representation where composition is just function composition. It avoids the complexity of existential types in the concrete encoding.',
    codeSnippet: '// All optics as profunctor transformations\ntype Optic[P[_, _], S, T, A, B] = P[A, B] => P[S, T]\n\n// Lens = Optic constrained to Strong profunctors\ntype Lens[S, T, A, B] = [P[_, _]] => Strong[P] ?=> P[A, B] => P[S, T]\n\n// Prism = Optic constrained to Choice profunctors\ntype Prism[S, T, A, B] = [P[_, _]] => Choice[P] ?=> P[A, B] => P[S, T]\n\n// Composition is just function composition!\nval composed = lens andThen prism  // (.) in Haskell',
    tags: ['optics', 'profunctor', 'theory'],
  },
  {
    id: 'scala-fp-l4-024',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is a catamorphism and how does it relate to fold?',
    options: [
      'A catamorphism constructs data structures',
      'A catamorphism is a category theory concept with no programming applications',
      'A catamorphism is a type of encryption',
      'A catamorphism is a generalized fold that tears down a recursive data structure layer by layer using an algebra F[A] => A, abstracting the recursion pattern away from the business logic',
    ],
    correctAnswer: 3,
    explanation: 'A catamorphism (cata) generalizes foldRight. Given a Functor F and an algebra (F[A] => A), cata recursively unrolls the fixed-point structure Fix[F] and applies the algebra bottom-up. For a list, F[A] = ListF[E, A] = NilF | ConsF(head: E, tail: A), and the algebra handles each case. This separates the recursion from the logic, enabling reuse of the recursion pattern across different data types.',
    codeSnippet: '// Base functor for a list\nsealed trait ListF[E, +A]\ncase class NilF[E]() extends ListF[E, Nothing]\ncase class ConsF[E, A](head: E, tail: A) extends ListF[E, A]\n\n// Fix point: ties the recursive knot\ncase class Fix[F[_]](unfix: F[Fix[F]])\n\n// Catamorphism\ndef cata[F[_]: Functor, A](algebra: F[A] => A)(fix: Fix[F]): A =\n  algebra(fix.unfix.map(cata(algebra)))\n\n// Example: sum a list\nval sumAlgebra: ListF[Int, Int] => Int = {\n  case NilF() => 0\n  case ConsF(head, tailSum) => head + tailSum\n}',
    tags: ['recursion-schemes', 'catamorphism', 'fold'],
  },
  {
    id: 'scala-fp-l4-025',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is an anamorphism and how does it relate to unfold?',
    options: [
      'An anamorphism is the opposite of an isomorphism',
      'An anamorphism analyzes morphological patterns in code',
      'An anamorphism is a generalized unfold that builds up a recursive data structure from a seed value using a coalgebra A => F[A], dual to catamorphism',
      'An anamorphism converts between two recursive types',
    ],
    correctAnswer: 2,
    explanation: 'An anamorphism (ana) is the dual of catamorphism. Given a coalgebra (A => F[A]), ana builds a Fix[F] by repeatedly applying the coalgebra to expand the structure. For a list, the coalgebra takes a seed and returns either NilF (stop) or ConsF(element, newSeed). Combining cata and ana gives a hylomorphism: unfold then fold, with the intermediate structure often optimized away.',
    codeSnippet: '// Anamorphism: unfold\ndef ana[F[_]: Functor, A](coalgebra: A => F[A])(seed: A): Fix[F] =\n  Fix(coalgebra(seed).map(ana(coalgebra)))\n\n// Example: generate list from range\nval rangeCoalgebra: (Int, Int) => ListF[Int, (Int, Int)] = {\n  case (lo, hi) if lo >= hi => NilF()\n  case (lo, hi) => ConsF(lo, (lo + 1, hi))\n}\n\n// Hylomorphism: unfold then fold (ana then cata)\ndef hylo[F[_]: Functor, A, B](\n  algebra: F[B] => B)(coalgebra: A => F[A])(seed: A): B =\n  algebra(coalgebra(seed).map(hylo(algebra)(coalgebra)))',
    tags: ['recursion-schemes', 'anamorphism', 'unfold'],
  },
  {
    id: 'scala-fp-l4-026',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is a paramorphism and when would you use it instead of a catamorphism?',
    options: [
      'A paramorphism is a catamorphism that runs in constant space',
      'A paramorphism is like a catamorphism but the algebra also receives the original sub-structure alongside the recursive result, enabling access to both the processed result and the original data at each step',
      'A paramorphism handles parameters of functions',
      'A paramorphism is a parallel catamorphism',
    ],
    correctAnswer: 1,
    explanation: 'A paramorphism (para) has an algebra F[(Fix[F], A)] => A. At each step, you receive both the recursive result (A) and the original sub-structure (Fix[F]). This is useful when the computation needs to inspect the remaining structure, not just the already-folded results. Example: computing factorials where you need both n and (n-1)!, or finding the first element that satisfies a predicate while keeping the rest of the list.',
    codeSnippet: '// Paramorphism: algebra gets both original and result\ndef para[F[_]: Functor, A](\n  algebra: F[(Fix[F], A)] => A\n)(fix: Fix[F]): A =\n  algebra(fix.unfix.map(f => (f, para(algebra)(f))))\n\n// Example: tails of a list\n// At each ConsF, we get (original_tail, processed_result)\nval tailsAlgebra: ListF[Int, (Fix[ListF[Int, *]], List[List[Int]])] =>\n  List[List[Int]] = {\n  case NilF() => List(Nil)\n  case ConsF(h, (originalTail, processedTails)) =>\n    toList(Fix(ConsF(h, originalTail))) :: processedTails\n}',
    tags: ['recursion-schemes', 'paramorphism', 'structural-recursion'],
  },
  {
    id: 'scala-fp-l4-027',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the Free Applicative and when should you prefer it over Free Monad?',
    options: [
      'Free Applicative is only for parallel operations',
      'Free Applicative does not require a Functor instance',
      'Free Applicative allows static analysis of the program structure (introspection of all operations without executing them) because it does not support sequential dependencies between operations, unlike Free Monad where later operations can depend on earlier results',
      'Free Applicative is a faster version of Free Monad',
    ],
    correctAnswer: 2,
    explanation: 'Free Monad\'s flatMap allows each step to depend on the previous result, making the structure unknowable without execution. Free Applicative\'s ap combines independent computations, so the entire structure can be inspected before execution. This enables optimizations like batching independent database queries, or generating documentation from the program structure. Use Free Applicative when operations are independent.',
    codeSnippet: 'import cats.free.FreeApplicative\n\nsealed trait Fetch[A]\ncase class FetchUser(id: Int) extends Fetch[User]\ncase class FetchOrder(id: Int) extends Fetch[Order]\n\ntype FetchApp[A] = FreeApplicative[Fetch, A]\n\n// Independent fetches - can be batched!\nval program: FetchApp[(User, Order)] =\n  (FreeApplicative.lift(FetchUser(1)),\n   FreeApplicative.lift(FetchOrder(42))).mapN((_, _))\n\n// Interpreter can see both fetches and batch them\n// unlike Free Monad where the second fetch might depend on the first',
    tags: ['free-applicative', 'static-analysis', 'batching'],
  },
  {
    id: 'scala-fp-l4-028',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Algebraic Effects',
    question: 'What are algebraic effects and how do they relate to monads in functional programming?',
    options: [
      'Algebraic effects are effects that use algebra for computation',
      'Algebraic effects separate effect declaration from handling, similar to checked exceptions but more powerful; they offer an alternative to monads for effect management where effects compose without the transformer overhead problem',
      'Algebraic effects are a way to perform algebraic operations on data',
      'Algebraic effects are mathematical equations in code',
    ],
    correctAnswer: 1,
    explanation: 'Algebraic effects define operations (like "throw", "read state", "fork") that can be "performed" and are intercepted by handlers higher in the call stack. Unlike monads, effects compose without transformers: a function can use State + Error + IO without specifying a monad stack. The handler determines the semantics. While Scala does not natively support algebraic effects, libraries like Kyo and ZIO approximate them.',
    tags: ['algebraic-effects', 'effects', 'composition'],
  },
  {
    id: 'scala-fp-l4-029',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the Sync typeclass in Cats Effect and how does it relate to the effect hierarchy?',
    options: [
      'It synchronizes data between threads',
      'It ensures all effects execute in order',
      'It provides synchronization primitives like locks',
      'Sync[F] extends MonadCancel[F, Throwable] and adds the ability to suspend synchronous side effects via delay/blocking, sitting between pure computation and full async capability in the typeclass hierarchy',
    ],
    correctAnswer: 3,
    explanation: 'The Cats Effect typeclass hierarchy is: Functor < Applicative < Monad < MonadCancel < Sync < Async. Sync[F] adds F.delay(sideEffect) for suspending synchronous side effects and F.blocking(blockingOp) for blocking operations. Async[F] adds callback-based asynchronous effects. This hierarchy lets you constrain functions to only the capabilities they need.',
    codeSnippet: 'import cats.effect.Sync\n\ndef readFile[F[_]: Sync](path: String): F[String] =\n  Sync[F].blocking {  // blocking I/O on a blocking thread pool\n    scala.io.Source.fromFile(path).mkString\n  }\n\ndef currentTime[F[_]: Sync]: F[Long] =\n  Sync[F].delay {  // non-blocking side effect\n    System.currentTimeMillis()\n  }\n\n// Hierarchy: Sync < Async < Concurrent < Temporal < Spawn',
    tags: ['effect-systems', 'sync', 'typeclass-hierarchy'],
  },
  {
    id: 'scala-fp-l4-030',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is fiber-based concurrency in Cats Effect 3 and how does it differ from thread-based concurrency?',
    options: [
      'Fibers are just a different name for threads',
      'Fibers are slower than threads but use less memory',
      'Fibers are lightweight, cooperatively-scheduled virtual threads managed by the IO runtime; millions can exist concurrently with minimal memory overhead, unlike OS threads which are heavyweight and limited',
      'Fibers only work on single-core processors',
    ],
    correctAnswer: 2,
    explanation: 'A Cats Effect Fiber (IO.start) creates a lightweight green thread that is scheduled on a fixed-size thread pool (typically matching CPU cores). Fibers yield at async boundaries, allowing cooperative multitasking. Unlike OS threads (which cost ~1MB stack each and require kernel scheduling), fibers cost a few hundred bytes and are scheduled in user space. This enables massive concurrency for I/O-bound workloads.',
    codeSnippet: 'import cats.effect.{IO, Fiber}\n\nval task: IO[String] = IO.sleep(1.second) *> IO.pure("done")\n\n// Start a fiber (lightweight concurrent computation)\nval program: IO[String] = for {\n  fiber <- task.start  // returns Fiber[IO, Throwable, String]\n  // Do other work concurrently\n  _ <- otherWork\n  result <- fiber.joinWithNever  // await result\n} yield result\n\n// Race: run two fibers, cancel the loser\nval raced: IO[Either[A, B]] = taskA.race(taskB)',
    tags: ['effect-systems', 'fibers', 'concurrency'],
  },
  {
    id: 'scala-fp-l4-031',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "higher-kinded type constraint" pattern in Tagless Final and why do you need F[_]: Monad?',
    options: [
      'F[_]: Monad converts F to a monad transformer',
      'It is a performance optimization hint',
      'The Monad constraint on F ensures you can sequence computations with flatMap; without it, you could only lift pure values into F but not chain effectful operations that depend on previous results',
      'F[_]: Monad creates a new monad instance',
    ],
    correctAnswer: 2,
    explanation: 'In Tagless Final, F[_] is the abstract effect type. Without any constraint, you cannot do anything with F values. Adding Functor lets you map, Applicative lets you combine independent effects, and Monad lets you flatMap (sequence dependent effects). The minimum constraint should be used: if your program only maps, require Functor. If it uses for-comprehensions, require Monad.',
    codeSnippet: '// Only needs Functor: no sequencing\ndef transform[F[_]: Functor: KVStore](key: String): F[String] =\n  KVStore[F].get(key).map(_.getOrElse("default"))\n\n// Needs Monad: result of get determines next action\ndef conditional[F[_]: Monad: KVStore: Logger](\n  key: String\n): F[Unit] = for {\n  value <- KVStore[F].get(key)\n  _ <- value match {\n    case Some(v) => Logger[F].info(s"Found: $v\")\n    case None => KVStore[F].put(key, "initialized\")\n  }\n} yield ()',
    tags: ['tagless-final', 'constraints', 'monad'],
  },
  {
    id: 'scala-fp-l4-032',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the difference between Free[F, A] and FreeT[F, G, A] (Free Monad Transformer)?',
    options: [
      'FreeT works with transformers while Free works with monads',
      'There is no practical difference',
      'FreeT is a compiled version of Free for better performance',
      'FreeT[F, G, A] interleaves the free algebra F with effects in monad G at each step, allowing the program to perform G effects between algebra operations; Free[F, A] is equivalent to FreeT[F, Id, A]',
    ],
    correctAnswer: 3,
    explanation: 'Free[F, A] builds a pure AST with no effects. FreeT[F, G, A] allows interleaving effects from monad G between algebra steps. For example, FreeT[MyAlgebra, IO, A] can perform IO actions (like logging) between interpreted algebra operations. This is useful when the program needs ambient effects during construction, not just during interpretation.',
    codeSnippet: '// Free: pure AST, effects only at interpretation\nval pure: Free[KVStoreA, String] = for {\n  _ <- put("key", "val\")\n  v <- get("key\")\n} yield v.getOrElse("\")\n\n// FreeT: interleave IO effects during construction\nval withEffects: FreeT[KVStoreA, IO, String] = for {\n  _ <- FreeT.liftF[KVStoreA, IO, Unit](Put("key", "val\"))\n  _ <- FreeT.liftT[KVStoreA, IO, Unit](IO(println("between ops\")))\n  v <- FreeT.liftF[KVStoreA, IO, Option[String]](Get("key\"))\n} yield v.getOrElse("\")',
    tags: ['free-monad', 'freeT', 'effects'],
  },
  {
    id: 'scala-fp-l4-033',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is the "lifting problem" in Monad Transformer stacks?',
    options: [
      'The problem of lifting heavy objects in code',
      'When working with a transformer stack, you need to lift operations from inner monads to the full stack type; deeper layers require more lift calls, making code verbose and fragile when the stack changes',
      'The problem of lifting Futures into IOs',
      'The problem of converting between different monad types',
    ],
    correctAnswer: 1,
    explanation: 'In a stack like EitherT[StateT[IO, S, *], E, A], using IO directly requires two lifts: EitherT.liftF(StateT.liftF(ioAction)). Adding or removing a layer changes all lift calls throughout the codebase. MTL style solves this: MonadError[F, E].raiseError(e) works regardless of whether F is EitherT or a native error monad. The lifting is handled by typeclass instances.',
    codeSnippet: 'type Stack[A] = EitherT[StateT[IO, AppState, *], AppError, A]\n\n// Verbose: manual lifting\ndef withLifts: Stack[Unit] = {\n  val io: IO[Unit] = IO(println("hello\"))\n  // Must lift through both layers\n  EitherT.liftF(StateT.liftF[IO, AppState, Unit](io))\n}\n\n// MTL style: no lifting needed\ndef withMTL[F[_]: Sync: Stateful[*, AppState]: Raise[*, AppError]]: F[Unit] =\n  Sync[F].delay(println("hello\"))  // just works for any F',
    tags: ['monad-transformers', 'lifting', 'mtl'],
  },
  {
    id: 'scala-fp-l4-034',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is an Iso (Isomorphism) in functional optics?',
    options: [
      'An Iso is a read-only optic',
      'An Iso[S, A] represents a lossless bidirectional conversion between types S and A, where get: S => A and reverseGet: A => S are total and inverse of each other',
      'An Iso isolates a value from the rest of the program',
      'An Iso is an ISO standard for optics',
    ],
    correctAnswer: 1,
    explanation: 'Iso[S, A] proves that S and A are isomorphic: they contain exactly the same information. get and reverseGet are inverses: reverseGet(get(s)) == s and get(reverseGet(a)) == a. Examples include case class Celsius(value: Double) <=> Double, or String <=> List[Char]. An Iso is the most powerful optic: it can be used as a Lens, Prism, or any other optic.',
    codeSnippet: 'import monocle.Iso\n\ncase class Celsius(value: Double)\n\n// Celsius is isomorphic to Double\nval celsiusIso: Iso[Celsius, Double] =\n  Iso[Celsius, Double](_.value)(Celsius.apply)\n\n// Compose Iso with other optics\nval tempLens: Lens[WeatherReport, Celsius] = GenLens[WeatherReport](_.temp)\nval tempDouble: Lens[WeatherReport, Double] =\n  tempLens.andThen(celsiusIso)\n\n// String <=> List[Char]\nval stringToChars: Iso[String, List[Char]] =\n  Iso[String, List[Char]](_.toList)(_.mkString)',
    tags: ['optics', 'iso', 'isomorphism'],
  },
  {
    id: 'scala-fp-l4-035',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "final tagless vs initial encoding" terminology?',
    options: [
      'Initial is for beginners, final is for experts',
      'Final means compiled, initial means interpreted',
      'Final means the last version, initial means the first version',
      'Initial encoding represents programs as data (ADT/AST, like Free Monads); final encoding represents programs as functions (method calls on typeclasses, like Tagless Final). "Tagless" means no type tags are needed for pattern matching.',
    ],
    correctAnswer: 3,
    explanation: 'In initial encoding (Free Monads), a program is an AST (data structure) that is later interpreted. In final encoding (Tagless Final), a program is direct method calls on an abstract interface, interpreted by the choice of implementation. "Tagless" refers to not needing runtime type tags (unlike initial encoding which pattern matches on constructors). Final encoding gives better performance; initial gives better introspection.',
    tags: ['tagless-final', 'free-monad', 'encoding-comparison'],
  },
  {
    id: 'scala-fp-l4-036',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the Ref type in Cats Effect and how does it provide safe shared mutable state?',
    options: [
      'Ref[F, A] is a concurrent mutable reference that provides atomic updates via compare-and-swap, wrapped in the effect type F, ensuring all access is referentially transparent and thread-safe',
      'Ref provides garbage-collected references',
      'Ref is a database reference type',
      'Ref is a reference to an external service',
    ],
    correctAnswer: 0,
    explanation: 'Ref[IO, A] wraps an AtomicReference and provides get: IO[A], set: IO[Unit], and update: (A => A) => IO[Unit] (atomic CAS loop). Unlike a raw var, all operations return IO, maintaining referential transparency. Concurrent modification is safe because update retries on CAS failure. Ref is the building block for lock-free concurrent data structures in Cats Effect.',
    codeSnippet: 'import cats.effect.{IO, Ref}\n\ndef counter: IO[Unit] = for {\n  ref <- Ref[IO].of(0)  // create with initial value 0\n  _ <- (1 to 1000).toList.parTraverse_ { _ =>\n    ref.update(_ + 1)  // atomic increment\n  }\n  final <- ref.get\n  _ <- IO.println(s"Count: $final\")  // always 1000\n} yield ()',
    tags: ['effect-systems', 'ref', 'concurrency'],
  },
  {
    id: 'scala-fp-l4-037',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is Deferred in Cats Effect and how is it used for synchronization?',
    options: [
      'It delays error handling until the end of the program',
      'Deferred[F, A] is a one-shot concurrent primitive: it starts empty, can be completed exactly once with a value, and get blocks (semantically) until the value is available, acting like a functional Promise',
      'It is a data type for deferred database writes',
      'It defers computation to a later time like lazy evaluation',
    ],
    correctAnswer: 1,
    explanation: 'Deferred[IO, A] is like a Promise that can only be completed once. Calling get on an incomplete Deferred semantically blocks the fiber (not the thread) until complete(a) is called from another fiber. Attempting to complete twice has no effect (tryGet/complete return Boolean). This is the primitive for one-shot fiber synchronization and is used to build higher-level concurrency tools.',
    codeSnippet: 'import cats.effect.{Deferred, IO}\n\ndef example: IO[String] = for {\n  deferred <- Deferred[IO, String]\n  // Consumer fiber: waits for the value\n  consumer <- deferred.get.flatMap(v => IO.println(s"Got: $v\")).start\n  // Producer fiber: provides the value\n  _ <- IO.sleep(1.second)\n  _ <- deferred.complete("hello\")\n  _ <- consumer.join\n} yield "done\"',
    tags: ['effect-systems', 'deferred', 'synchronization'],
  },
  {
    id: 'scala-fp-l4-038',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Profunctors',
    question: 'What is a Strong profunctor and why is it important for Lens encoding?',
    options: [
      'A Strong profunctor is a profunctor with better performance',
      'A Strong profunctor has first: P[A, B] => P[(A, C), (B, C)], allowing it to pass through extra context; Lenses are exactly the optics that work with Strong profunctors because they focus on one component of a product while preserving the other',
      'A Strong profunctor enforces type safety',
      'A Strong profunctor combines multiple profunctors',
    ],
    correctAnswer: 1,
    explanation: 'Strong[P] extends Profunctor[P] with first[A, B, C](pab: P[A, B]): P[(A, C), (B, C)]. This lets the profunctor operate on one component of a tuple while passing the other through unchanged. A Lens focuses on one field of a product type (case class), which is isomorphic to a tuple. In profunctor optics, type Lens s t a b = forall p. Strong p => p a b -> p s t.',
    codeSnippet: 'trait Strong[P[_, _]] extends Profunctor[P] {\n  def first[A, B, C](pab: P[A, B]): P[(A, C), (B, C)]\n  def second[A, B, C](pab: P[A, B]): P[(C, A), (C, B)]\n}\n\n// A Lens using profunctor encoding\ndef _1[A, B, C]: Lens[(A, C), (B, C), A, B] =\n  new Lens[(A, C), (B, C), A, B] {\n    def apply[P[_, _]: Strong](pab: P[A, B]): P[(A, C), (B, C)] =\n      Strong[P].first(pab)\n  }',
    tags: ['profunctor', 'strong', 'lens'],
  },
  {
    id: 'scala-fp-l4-039',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Profunctors',
    question: 'What is a Choice profunctor and how does it relate to Prism?',
    options: [
      'A Choice profunctor has left: P[A, B] => P[Either[A, C], Either[B, C]], allowing it to handle one branch of a sum type; Prisms are exactly the optics that work with Choice profunctors',
      'A Choice profunctor chooses the most efficient implementation',
      'A Choice profunctor makes random selections',
      'A Choice profunctor selects between two profunctors',
    ],
    correctAnswer: 0,
    explanation: 'Choice[P] extends Profunctor[P] with left[A, B, C](pab: P[A, B]): P[Either[A, C], Either[B, C]]. This lets the profunctor operate on one branch of a sum type (Either/sealed trait). A Prism focuses on one case of a sum type, which maps to one branch of an Either. In profunctor optics, type Prism s t a b = forall p. Choice p => p a b -> p s t.',
    codeSnippet: 'trait Choice[P[_, _]] extends Profunctor[P] {\n  def left[A, B, C](pab: P[A, B]): P[Either[A, C], Either[B, C]]\n  def right[A, B, C](pab: P[A, B]): P[Either[C, A], Either[C, B]]\n}\n\n// A Prism using profunctor encoding\ndef _Left[A, B, C]: Prism[Either[A, C], Either[B, C], A, B] =\n  new Prism[Either[A, C], Either[B, C], A, B] {\n    def apply[P[_, _]: Choice](pab: P[A, B]): P[Either[A, C], Either[B, C]] =\n      Choice[P].left(pab)\n  }',
    tags: ['profunctor', 'choice', 'prism'],
  },
  {
    id: 'scala-fp-l4-040',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is a histomorphism and when would you use it?',
    options: [
      'A histomorphism converts between historical data formats',
      'A recursion scheme that tracks the history of changes',
      'A histomorphism records metrics about recursive operations',
      'A histomorphism is like a catamorphism but the algebra receives the full history of previously computed results (via a cofree comonad), enabling access to all intermediate folded values, not just the immediate children',
    ],
    correctAnswer: 3,
    explanation: 'A histomorphism (histo) gives the algebra access to the results of all recursive sub-computations through a Cofree comonad. While cata only sees the immediate children\'s results, histo can look at arbitrary depth. This is useful for dynamic programming problems where the result depends on multiple previous results, like Fibonacci (needs n-1 and n-2) or coin change problems.',
    codeSnippet: '// Histomorphism: access full history of results\ndef histo[F[_]: Functor, A](\n  algebra: F[Cofree[F, A]] => A\n)(fix: Fix[F]): A = {\n  def go(f: Fix[F]): Cofree[F, A] = {\n    val annotated = f.unfix.map(go)\n    Cofree(algebra(annotated), annotated)\n  }\n  go(fix).head\n}\n\n// Example: Fibonacci using histomorphism\n// The algebra can look back 2 steps via Cofree',
    tags: ['recursion-schemes', 'histomorphism', 'dynamic-programming'],
  },
  {
    id: 'scala-fp-l4-041',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the "freer monad" (or "operational monad") and how does it simplify Free Monads?',
    options: [
      'The freer monad removes the Functor requirement on the algebra F by using Coyoneda internally, allowing any type constructor to be used as an algebra without manually implementing Functor',
      'It is a commercial version of the Free Monad',
      'It is a monad with no constraints or requirements',
      'It is a monad that runs without memory allocation',
    ],
    correctAnswer: 0,
    explanation: 'Free[F, A] requires F to be a Functor. The freer monad (Eff monad) uses Coyoneda internally to provide a free Functor for any F, removing this requirement. Your algebra can be a simple GADT without a Functor instance. This simplifies the boilerplate: you only need to define the algebra ADT and the interpreter, with no manual Functor instance needed.',
    codeSnippet: '// With standard Free: need Functor[KVStoreA]\nsealed trait KVStoreA[A]\n// Must implement Functor[KVStoreA] manually\n\n// With Freer: no Functor needed!\n// The Freer monad wraps operations in Coyoneda internally\ntype Freer[F[_], A] = Free[Coyoneda[F, *], A]\n\ndef liftFreer[F[_], A](fa: F[A]): Freer[F, A] =\n  Free.liftF(Coyoneda.lift(fa))',
    tags: ['free-monad', 'freer', 'coyoneda'],
  },
  {
    id: 'scala-fp-l4-042',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is IOApp in Cats Effect and why should you use it instead of def main?',
    options: [
      'IOApp is faster than main because it uses native compilation',
      'IOApp provides a structured entry point that properly initializes the IO runtime, handles fiber cancellation on shutdown, installs signal handlers, and ensures resources are cleaned up, unlike a raw main method that would require manual runtime management',
      'IOApp and main are interchangeable',
      'IOApp is a UI application framework',
    ],
    correctAnswer: 1,
    explanation: 'IOApp extends App and requires you to implement def run(args: List[String]): IO[ExitCode]. It sets up the Cats Effect runtime (thread pools, scheduler), handles SIGINT/SIGTERM by cancelling the IO fiber (triggering Resource cleanup), and properly shuts down the runtime. Using unsafeRunSync in a raw main method bypasses all of this safety, risking resource leaks on interruption.',
    codeSnippet: 'import cats.effect.{IOApp, IO, ExitCode}\n\nobject MyApp extends IOApp {\n  def run(args: List[String]): IO[ExitCode] = {\n    val program = for {\n      _ <- IO.println("Starting...")\n      _ <- myResource.use { r =>\n        processForever(r)  // properly cancelled on SIGTERM\n      }\n    } yield ExitCode.Success\n\n    program.handleErrorWith { error =>\n      IO.println(s"Fatal: $error\").as(ExitCode.Error)\n    }\n  }\n}',
    tags: ['effect-systems', 'ioapp', 'lifecycle'],
  },
  {
    id: 'scala-fp-l4-043',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is StateT and how does it compose state management with other effects?',
    options: [
      'StateT provides immutable state only',
      'StateT is a state management library like Redux',
      'StateT[F, S, A] wraps S => F[(S, A)], composing a state threading computation with an underlying monad F, enabling stateful computations within effectful contexts like IO',
      'StateT represents a database state transaction',
    ],
    correctAnswer: 2,
    explanation: 'StateT[F, S, A] represents a computation that takes a state S, runs an effect in monad F, and produces a new state S and a result A. It enables functional state management within effectful contexts. StateT[IO, GameState, A] threads game state through IO computations. However, StateT is not suitable for concurrent state (use Ref instead) because each fiber would get its own copy of the state.',
    codeSnippet: 'import cats.data.StateT\n\ntype GameIO[A] = StateT[IO, GameState, A]\n\ndef movePlayer(dir: Direction): GameIO[Position] =\n  StateT { state =>\n    val newPos = state.player.move(dir)\n    val newState = state.copy(player = state.player.copy(pos = newPos))\n    IO.pure((newState, newPos))\n  }\n\ndef gameLoop: GameIO[Unit] = for {\n  input <- StateT.liftF(readInput)\n  pos <- movePlayer(input)\n  _ <- StateT.liftF(IO.println(s"Moved to $pos\"))\n  _ <- gameLoop\n} yield ()',
    tags: ['monad-transformers', 'stateT', 'state-management'],
  },
  {
    id: 'scala-fp-l4-044',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is an Optional in functional optics and how does it relate to Lens and Prism?',
    options: [
      'Optional is the same as scala.Option wrapped in an optic',
      'Optional[S, A] focuses on zero or one target A within S, combining Lens (always present) and Prism (may be absent) capabilities; it has getOption: S => Option[A] and set: A => S => S',
      'Optional makes any optic nullable',
      'Optional is a deprecated optic replaced by Traversal',
    ],
    correctAnswer: 1,
    explanation: 'Optional[S, A] is at the intersection of Lens and Prism in the optics hierarchy. It focuses on a value that may or may not exist. Example: the head of a list (exists only if non-empty), or a field in a JSON object (may be missing). An Optional composes naturally with Lenses and Prisms. Composing a Lens with a Prism produces an Optional.',
    codeSnippet: 'import monocle.Optional\n\n// Head of a list: may not exist\nval headOption: Optional[List[Int], Int] =\n  Optional[List[Int], Int](_.headOption) {\n    newHead => {\n      case Nil => Nil\n      case _ :: tail => newHead :: tail\n    }\n  }\n\n// Lens + Prism = Optional\nval personStreet: Lens[Person, Address] = ...\nval addressZip: Prism[Address, USAddress] = ...\nval usZip: Lens[USAddress, String] = ...\n\n// Composing: Optional[Person, String]\nval personZip = personStreet andThen addressZip andThen usZip',
    tags: ['optics', 'optional', 'composition'],
  },
  {
    id: 'scala-fp-l4-045',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the difference between IO.parMapN and IO.flatMap for combining effects?',
    options: [
      'parMapN is always faster than flatMap',
      'There is no semantic difference; the compiler chooses the optimal strategy',
      'parMapN runs effects concurrently on separate fibers and combines results when all complete; flatMap runs effects sequentially where each depends on the previous result',
      'parMapN runs on multiple CPUs while flatMap runs on one CPU',
    ],
    correctAnswer: 2,
    explanation: 'IO.flatMap sequences effects: the second starts only after the first completes. (io1, io2).parMapN((a, b) => ...) starts both io1 and io2 as concurrent fibers and combines their results when both finish. If either fails, the other is cancelled. Use flatMap when there is a data dependency; use parMapN when effects are independent. parTupled and parTraverse are related combinators.',
    codeSnippet: '// Sequential: takes 2 seconds total\nval sequential = for {\n  a <- IO.sleep(1.second).as("a\")\n  b <- IO.sleep(1.second).as("b\")\n} yield (a, b)\n\n// Parallel: takes 1 second total\nval parallel = (\n  IO.sleep(1.second).as("a\"),\n  IO.sleep(1.second).as("b\")\n).parMapN((a, b) => (a, b))\n\n// Parallel traverse a list\nval results: IO[List[Response]] =\n  urls.parTraverse(url => httpClient.get(url))',
    tags: ['effect-systems', 'parallelism', 'concurrency'],
  },
  {
    id: 'scala-fp-l4-046',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'How do you handle errors in Tagless Final programs?',
    options: [
      'Use try-catch blocks',
      'Errors are not possible in Tagless Final',
      'Use ApplicativeError[F, E] or MonadError[F, E] constraints to raise and handle typed errors within the abstract F effect, keeping error handling referentially transparent',
      'Throw exceptions and let the caller handle them',
    ],
    correctAnswer: 2,
    explanation: 'MonadError[F, E] (or its supertype ApplicativeError) provides raiseError[A](e: E): F[A] and handleErrorWith[A](fa: F[A])(f: E => F[A]): F[A]. By constraining your program with MonadError[F, AppError], you can raise and recover from typed errors without committing to a specific error handling monad (EitherT, IO with typed errors, ZIO, etc.).',
    codeSnippet: 'sealed trait AppError\ncase class NotFound(id: String) extends AppError\ncase class Unauthorized(msg: String) extends AppError\n\ndef fetchUser[F[_]: Monad](\n  id: String\n)(implicit\n  store: UserStore[F],\n  E: MonadError[F, AppError]\n): F[User] = for {\n  maybeUser <- store.find(id)\n  user <- maybeUser match {\n    case Some(u) => u.pure[F]\n    case None => E.raiseError[User](NotFound(id))\n  }\n} yield user\n\n// Can be interpreted with IO, EitherT, ZIO, etc.',
    tags: ['tagless-final', 'error-handling', 'monad-error'],
  },
  {
    id: 'scala-fp-l4-047',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is Fix[F] (the fixed-point type) and why is it central to recursion schemes?',
    options: [
      'Fix is a bug-fixing utility type',
      'Fix[F] ties the recursive knot for a base functor F, where Fix[F] = F[Fix[F]]; it separates the recursion from the shape of the data, enabling generic recursion patterns (cata, ana, etc.) to work uniformly across different recursive data types',
      'Fix converts mutable types to immutable types',
      'Fix prevents changes to a data type',
    ],
    correctAnswer: 1,
    explanation: 'Fix[F] = F[Fix[F]] is the type-level fixed point. Instead of defining List[A] directly as a recursive type, you define a non-recursive "base functor" ListF[A, X] where X replaces the recursive position. Then Fix[ListF[A, *]] is the actual recursive list. This abstraction allows recursion schemes (cata, ana, hylo, etc.) to be defined once and work for any recursive data type.',
    codeSnippet: '// Non-recursive base functor\nsealed trait ExprF[+A]\ncase class LitF(value: Int) extends ExprF[Nothing]\ncase class AddF[A](left: A, right: A) extends ExprF[A]\ncase class MulF[A](left: A, right: A) extends ExprF[A]\n\n// Fixed point: recursive expression type\ntype Expr = Fix[ExprF]\n\n// Build: 1 + (2 * 3)\nval expr: Expr = Fix(AddF(\n  Fix(LitF(1)),\n  Fix(MulF(Fix(LitF(2)), Fix(LitF(3))))\n))\n\n// Evaluate with catamorphism\nval eval: ExprF[Int] => Int = {\n  case LitF(v) => v\n  case AddF(l, r) => l + r\n  case MulF(l, r) => l * r\n}\ncata(eval)(expr)  // 7',
    tags: ['recursion-schemes', 'fix', 'base-functor'],
  },
  {
    id: 'scala-fp-l4-048',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'Why does the order of Monad Transformer layers matter?',
    options: [
      'The outermost transformer runs first',
      'The order determines error handling and state semantics: EitherT[StateT[IO, S, *], E, A] rolls back state on error (state is local to the Either), while StateT[EitherT[IO, E, *], S, A] preserves state on error (state survives Either)',
      'Order only matters for performance, not semantics',
      'The order does not matter; all orderings are equivalent',
    ],
    correctAnswer: 1,
    explanation: 'Layer order changes semantics. Consider State + Either: StateT[Either[E, *], S, A] = S => Either[E, (S, A)] — on error, no state update is returned. EitherT[State[S, *], E, A] = S => (S, Either[E, A]) — state is always updated, even on error. This distinction matters for transaction-like behavior (rollback vs commit) and must be chosen deliberately based on requirements.',
    codeSnippet: '// State inside Either: state lost on error\ntype RollbackStack[A] = StateT[Either[String, *], Int, A]\nval rollback: RollbackStack[Unit] = for {\n  _ <- StateT.modify[Either[String, *], Int](_ + 1)\n  _ <- StateT.liftF[Either[String, *], Int, Unit](Left("error\"))\n} yield ()\nrollback.run(0)  // Left("error\") - state 1 is lost\n\n// Either inside State: state preserved on error\ntype CommitStack[A] = EitherT[State[Int, *], String, A]\nval commit: CommitStack[Unit] = for {\n  _ <- EitherT.liftF[State[Int, *], String, Unit](State.modify(_ + 1))\n  _ <- EitherT.leftT[State[Int, *], Unit]("error\")\n} yield ()\ncommit.value.run(0).value  // (1, Left("error\")) - state preserved',
    tags: ['monad-transformers', 'ordering', 'semantics'],
  },
  {
    id: 'scala-fp-l4-049',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is the "at" combinator in Monocle and when do you use it?',
    options: [
      'at(key) creates a Lens that focuses on the value at a key in a Map, returning Option[V]; it allows getting, setting, and removing entries in immutable Maps through optics composition',
      'It creates a new optic at compile time',
      'It adds elements to a collection',
      'It positions an element at a specific index',
    ],
    correctAnswer: 0,
    explanation: 'at(key): Lens[Map[K, V], Option[V]] focuses on the entry at a given key. Setting Some(v) inserts/updates the entry; setting None removes it. This integrates Map operations into optics composition. For indexed access (like List index), use index(n): Optional[List[A], A] which may return None for out-of-bounds.',
    codeSnippet: 'import monocle.function.At.at\nimport monocle.function.Index.index\n\ncase class Config(settings: Map[String, String])\nval settingsLens = GenLens[Config](_.settings)\n\n// Lens into a specific map key\nval debugSetting = settingsLens.andThen(at("debug\"))\n\nval config = Config(Map("debug\" -> "true\"))\ndebugSetting.get(config)  // Some("true\")\ndebugSetting.set(None)(config)  // Config(Map()) - removed!\n\n// Index for lists\nval secondElement = index[List[Int], Int, Int](1)\nsecondElement.getOption(List(1, 2, 3))  // Some(2)',
    tags: ['optics', 'at', 'map-access'],
  },
  {
    id: 'scala-fp-l4-050',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the mapK pattern for changing the effect type in Tagless Final?',
    options: [
      'mapK is a Kafka mapping function',
      'mapK applies a natural transformation (FunctionK/~>) to change the effect type of a program or data structure from F to G, enabling effect polymorphism and runtime interpreter swapping',
      'mapK converts between different collection types',
      'mapK maps keys in a map data structure',
    ],
    correctAnswer: 1,
    explanation: 'mapK (or FunctionK, ~>) transforms F[A] => G[A] for all A. This is used to change the effect type of a program. For example, you might write your program targeting a test monad and then mapK to IO for production. In Cats, many data types support mapK: Resource[F, A].mapK(f2g) => Resource[G, A], Stream[F, A].translate(f2g) => Stream[G, A].',
    codeSnippet: 'import cats.~>\n\nval optionToList: Option ~> List = new (Option ~> List) {\n  def apply[A](opt: Option[A]): List[A] = opt.toList\n}\n\n// Transform a Resource from IO to Kleisli[IO, Config, *]\nval configTransform: IO ~> Kleisli[IO, Config, *] =\n  new (IO ~> Kleisli[IO, Config, *]) {\n    def apply[A](io: IO[A]): Kleisli[IO, Config, A] =\n      Kleisli.liftF(io)\n  }\n\nval ioResource: Resource[IO, Database] = ...\nval kleisliResource: Resource[Kleisli[IO, Config, *], Database] =\n  ioResource.mapK(configTransform)',
    tags: ['tagless-final', 'mapK', 'natural-transformation'],
  },
  {
    id: 'scala-fp-l4-051',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Church Encoding',
    question: 'What is the Scott encoding and how does it differ from Church encoding?',
    options: [
      'Scott encoding is a newer version of Church encoding',
      'Scott encoding is specific to the Scala programming language',
      'Scott encoding represents data by its pattern match (case analysis) rather than its fold, preserving laziness and enabling constant-time destructuring of the outermost constructor',
      'Scott encoding is used for string processing',
    ],
    correctAnswer: 2,
    explanation: 'Church encoding represents data as its fold (catamorphism): processing the entire structure. Scott encoding represents data as its pattern match: you get access to the outermost constructor\'s fields and the unevaluated tail. For lists, Church-encoded head traverses the list; Scott-encoded head is O(1). Scott encoding preserves laziness better but requires recursive types in the host language.',
    codeSnippet: '// Church-encoded list: fold over entire structure\ntrait ChurchList[A] {\n  def fold[B](nil: B)(cons: (A, B) => B): B\n}\n\n// Scott-encoded list: pattern match on head\ntrait ScottList[A] {\n  def match_[B](nil: => B)(cons: (A, ScottList[A]) => B): B\n}\n\n// Scott head is O(1)!\ndef head[A](list: ScottList[A]): Option[A] =\n  list.match_(None)((a, _) => Some(a))\n\n// Church head traverses the entire list!\ndef churchHead[A](list: ChurchList[A]): Option[A] =\n  list.fold[Option[A]](None)((a, _) => Some(a)) // still O(n)',
    tags: ['church-encoding', 'scott-encoding', 'data-representation'],
  },
  {
    id: 'scala-fp-l4-052',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the Bracket pattern and how does it relate to Resource in Cats Effect?',
    options: [
      'Bracket is a JavaScript pattern not available in Scala',
      'Bracket ensures resource cleanup by providing acquire-use-release semantics: bracket(acquire)(use)(release) guarantees release runs even if use fails; Resource is the composable generalization of bracket',
      'Bracket is a syntax for array access',
      'Bracket is for creating balanced parentheses in strings',
    ],
    correctAnswer: 1,
    explanation: 'IO.bracket(acquire)(use)(release) acquires a resource, uses it, and guarantees release runs on success, error, or cancellation. Resource[F, A] generalizes this: it stores the acquire and release internally and exposes a composable API. Multiple Resources compose with flatMap (releases run in reverse order). Resource is preferred because bracket does not compose well for multiple resources.',
    codeSnippet: '// Bracket: manual, does not compose well\nIO(openFile(path)).bracket { file =>\n  IO(processFile(file))\n} { file =>\n  IO(file.close())\n}\n\n// Resource: composable\nval fileResource = Resource.make(\n  IO(openFile(path))\n)(file => IO(file.close()))\n\n// Compose multiple resources\nval program = (fileResource, dbResource).tupled.use {\n  case (file, db) => processData(file, db)\n}  // both cleaned up in reverse order',
    tags: ['effect-systems', 'bracket', 'resource-safety'],
  },
  {
    id: 'scala-fp-l4-053',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the relationship between Free Monads and dependency injection?',
    options: [
      'Free Monads have no relationship to dependency injection',
      'Free Monads use runtime reflection for dependency injection',
      'Free Monads replace dependency injection frameworks',
      'Free Monads provide compile-time dependency injection: the algebra defines the interface (what operations are needed), and the interpreter provides the implementation (how they are executed), similar to constructor injection but with deferred binding',
    ],
    correctAnswer: 3,
    explanation: 'A Free Monad program declares its dependencies through the algebra ADT (the "what"). The interpreter (natural transformation) provides the implementations (the "how"). Different interpreters can be used in different contexts: production interpreter uses real services, test interpreter uses mocks. This is structural dependency injection at the type level, without any DI framework or runtime reflection.',
    tags: ['free-monad', 'dependency-injection', 'architecture'],
  },
  {
    id: 'scala-fp-l4-054',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is WriterT and what is it commonly used for?',
    options: [
      'WriterT[F, W, A] wraps F[(W, A)] where W is a monoidal log, allowing computations to accumulate a log alongside their results; commonly used for logging, metrics accumulation, or collecting warnings during computation',
      'WriterT is a template for writing boilerplate code',
      'WriterT writes data to files',
      'WriterT writes to standard output',
    ],
    correctAnswer: 0,
    explanation: 'WriterT[F, W, A] pairs a monoidal value W with the result A inside effect F. flatMap combines the W values using Monoid[W].combine (|+|). This is useful for accumulating logs, collecting warnings, or tracking computation traces in a pure way. W must be a Monoid so logs can be combined. Common W types: List[String] for log lines, Map[String, Int] for metrics, Chain[Event] for events.',
    codeSnippet: 'import cats.data.WriterT\n\ntype Logged[A] = WriterT[IO, Chain[String], A]\n\ndef processItem(item: Item): Logged[Result] = for {\n  _ <- WriterT.tell[IO, Chain[String]](Chain.one(s"Processing ${item.id}\"))\n  result <- WriterT.liftF[IO, Chain[String], Result](computeResult(item))\n  _ <- WriterT.tell[IO, Chain[String]](Chain.one(s"Result: $result\"))\n} yield result\n\n// Run and get both result and logs\nval (logs, result) = processItem(myItem).run.unsafeRunSync()\nlogs.toList.foreach(println)',
    tags: ['monad-transformers', 'writerT', 'logging'],
  },
  {
    id: 'scala-fp-l4-055',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'How do you derive optics automatically for case classes in Scala?',
    options: [
      'Use Monocle macros (@Lenses annotation or GenLens) for Scala 2, or Monocle\'s Focus macro (focus syntax) for Scala 3, to automatically generate Lens instances for case class fields at compile time',
      'Use runtime reflection to create optics',
      'Optics must always be written manually',
      'Optics are automatically available for all case classes without any library',
    ],
    correctAnswer: 0,
    explanation: 'Monocle provides multiple derivation strategies. In Scala 2: @Lenses annotation on case classes generates companion object lenses, or GenLens[T](_.field) macro generates individual lenses. In Scala 3: the Focus macro provides focus(_.field1.field2) syntax for inline lens creation. These eliminate the boilerplate of manually writing get/set for each field.',
    codeSnippet: '// Scala 2: @Lenses macro\n@Lenses case class Address(street: String, city: String)\n// Generates: Address.street: Lens[Address, String]\n\n// Scala 2: GenLens\nval street = GenLens[Address](_.street)\n\n// Scala 3: Focus macro\nimport monocle.syntax.all._\nval updated = person\n  .focus(_.address.street)\n  .modify(_.toUpperCase)\n\n// Scala 3: derivation\ncase class Person(name: String, address: Address) derives Lens',
    tags: ['optics', 'derivation', 'macros'],
  },
  {
    id: 'scala-fp-l4-056',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is a zygomorphism and how does it generalize paramorphism?',
    options: [
      'A zygomorphism is a combination of catamorphism and anamorphism',
      'A zygomorphism operates on pairs of data structures',
      'A zygomorphism runs two mutually dependent folds simultaneously: an auxiliary fold produces helper values that the primary fold can access at each step, generalizing paramorphism which only provides the original sub-structure',
      'A zygomorphism processes data in zigzag order',
    ],
    correctAnswer: 2,
    explanation: 'A zygomorphism (zygo) pairs two algebras: a helper algebra F[B] => B and a primary algebra F[(B, A)] => A. The helper runs first at each level, and its result B is available to the primary. A paramorphism is a special case where the helper is the "unfold back to Fix" (cata with Fix constructor as algebra). Zygomorphism is useful when you need an auxiliary computation to guide the main fold.',
    codeSnippet: '// Zygomorphism: two cooperating folds\ndef zygo[F[_]: Functor, A, B](\n  helper: F[B] => B,\n  algebra: F[(B, A)] => A\n)(fix: Fix[F]): A = {\n  def go(f: Fix[F]): (B, A) = {\n    val fb = f.unfix.map(go)\n    val b = helper(fb.map(_._1))\n    val a = algebra(fb.map { case (bi, ai) => (bi, ai) })\n    (b, a)\n  }\n  go(fix)._2\n}\n\n// Example: check if expression is valid AND evaluate it',
    tags: ['recursion-schemes', 'zygomorphism', 'mutual-recursion'],
  },
  {
    id: 'scala-fp-l4-057',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is cancellation in Cats Effect and why does it matter for resource safety?',
    options: [
      'Cancellation is only relevant for network operations',
      'Cancellation removes data from the database',
      'Cancellation is a cooperative mechanism where a fiber can be cancelled at async boundaries; it matters because cancelled computations must still clean up resources, which is handled by bracket/Resource release guarantees',
      'Cancellation stops all running fibers',
    ],
    correctAnswer: 2,
    explanation: 'When an IO fiber is cancelled (e.g., via race where the loser is cancelled, or explicit fiber.cancel), execution stops at the next cancelable point (async boundary). However, bracket/Resource release actions run as uncancelable, ensuring resources are properly cleaned up. IO.uncancelable marks regions where cancellation is deferred until the end of the region. This is critical for maintaining invariants during cleanup.',
    codeSnippet: 'import cats.effect.IO\n\n// Race: loser gets cancelled\nval raced: IO[String] = IO.race(\n  IO.sleep(1.second).as("slow\"),\n  IO.sleep(100.millis).as("fast\")\n).map(_.merge)  // "fast\"; slow fiber is cancelled\n\n// Resource cleanup runs even on cancellation\nval safe = Resource.make(\n  IO(openConnection())\n)(conn => IO(conn.close()))  // ALWAYS runs\n  .use(conn => longRunningWork(conn))\n\n// Uncancelable region\nIO.uncancelable { poll =>\n  // This region cannot be cancelled\n  criticalSection *>\n  poll(cancelableStep) *>  // opt back into cancellation briefly\n  finalCriticalSection\n}',
    tags: ['effect-systems', 'cancellation', 'resource-safety'],
  },
  {
    id: 'scala-fp-l4-058',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "higher-kinded type lambda" trick used with Tagless Final when working with multi-parameter type constructors?',
    options: [
      'It is a way to create anonymous function types',
      'It is a lambda function that operates on types',
      'Type lambdas like [A] =>> Either[Error, A] or ({type L[A] = Either[Error, A]})#L partially apply type constructors to fit the F[_] shape required by Tagless Final algebras',
      'It is used to define lambda expressions in type annotations',
    ],
    correctAnswer: 2,
    explanation: 'Tagless Final requires F[_] (a type constructor with one parameter). Types like Either[E, A] have two parameters. Type lambdas partially apply one parameter: [A] =>> Either[Error, A] (Scala 3) or ({type L[A] = Either[Error, A]})#L (Scala 2). This creates a type constructor F[_] = Either[Error, _] that can be used as the effect type in Tagless Final programs.',
    codeSnippet: '// Scala 3: type lambda\ntype ErrorOr = [A] =>> Either[AppError, A]\n\n// Scala 2: type lambda (structural type trick)\ntype ErrorOr2[A] = Either[AppError, A]\n// or with kind-projector plugin: Either[AppError, *]\n\n// Usage with Tagless Final\ndef program[F[_]: Monad: KVStore]: F[String] = ???\n\n// Instantiate with Either[AppError, *]\nval result: Either[AppError, String] =\n  program[ErrorOr]  // Scala 3\n  // program[Either[AppError, *]]  // with kind-projector',
    tags: ['tagless-final', 'type-lambda', 'higher-kinded'],
  },
  {
    id: 'scala-fp-l4-059',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the Extensible Effects (Eff) pattern and how does it relate to Free Monads?',
    options: [
      'Eff is the same as algebraic effects',
      'Eff is an efficient version of Free with no overhead',
      'Eff uses an open union of effects (type-level list) with a freer monad, allowing modular effect composition without the coproduct boilerplate of Free Monads while supporting effect handlers that can be composed and reordered',
      'Eff is a deprecated pattern replaced by Tagless Final',
    ],
    correctAnswer: 2,
    explanation: 'The Eff monad (from Freer Monads, More Extensible Effects) represents a computation with an open union of effects R. type Eff[R, A] where R is a type-level set of effect types. Effects can be added/removed from R by handlers. Unlike Free with Coproduct where you must manually combine algebras, Eff uses an extensible effect row that effects are automatically injected into. Libraries like atnos-eff implement this in Scala.',
    codeSnippet: 'import org.atnos.eff._\nimport org.atnos.eff.all._\n\ntype MyStack = Fx.fx3[State[Int, *], Either[String, *], IO]\n\ndef program: Eff[MyStack, Int] = for {\n  s <- get[MyStack, Int]\n  _ <- if (s < 0) left[MyStack, String]("negative\")\n       else right[MyStack, String]("ok\")\n  _ <- put[MyStack, Int](s + 1)\n} yield s + 1\n\n// Run effects one at a time in any order\nval result = program\n  .runState(0)\n  .runEither\n  .unsafePerformIO()',
    tags: ['free-monad', 'extensible-effects', 'eff'],
  },
  {
    id: 'scala-fp-l4-060',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is the "Each" typeclass in Monocle and how does it create Traversals?',
    options: [
      'Each[S, A] provides a Traversal[S, A] that focuses on each element A within a collection-like structure S, enabling optics-based batch operations on collections',
      'Each applies a function to one element at a time',
      'Each counts the number of elements in a structure',
      'Each iterates over elements imperatively',
    ],
    correctAnswer: 0,
    explanation: 'Each[S, A] provides each: Traversal[S, A] for collection-like types. For List[A], it focuses on all elements. For Map[K, V], it focuses on all values. Combined with other optics, it enables powerful batch updates on nested structures. For example: company lens to departments, then Each to get all departments, then each department\'s employees, then each employee\'s salary.',
    codeSnippet: 'import monocle.function.Each._\n\ncase class Company(departments: List[Department])\ncase class Department(employees: List[Employee])\ncase class Employee(name: String, salary: Double)\n\n// Traversal: all employees in a company\nval allEmployees: Traversal[Company, Employee] =\n  GenLens[Company](_.departments)\n    .andThen(each[List[Department], Department])\n    .andThen(GenLens[Department](_.employees))\n    .andThen(each[List[Employee], Employee])\n\n// Give everyone a raise\nval allSalaries = allEmployees.andThen(GenLens[Employee](_.salary))\nval updated = allSalaries.modify(_ * 1.1)(company)',
    tags: ['optics', 'each', 'traversal'],
  },
  {
    id: 'scala-fp-l4-061',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the difference between IO.delay, IO.blocking, and IO.interruptible in Cats Effect 3?',
    options: [
      'delay is for async, blocking is for sync, interruptible is for testing',
      'IO.delay suspends a side effect on the compute pool; IO.blocking moves execution to a blocking thread pool for blocking operations; IO.interruptible additionally allows the blocking thread to be interrupted on cancellation',
      'They are all the same; they suspend side effects',
      'delay wraps pure values, blocking wraps side effects, interruptible wraps Futures',
    ],
    correctAnswer: 1,
    explanation: 'IO.delay(expr) suspends a side-effecting expression on the default compute pool (meant for non-blocking code). IO.blocking(expr) shifts execution to a dedicated blocking thread pool (for JDBC calls, file I/O, etc.) to avoid starving the compute pool. IO.interruptible(expr) is like blocking but also sets up thread interruption so the blocking operation can be cancelled via Thread.interrupt().',
    codeSnippet: '// Non-blocking side effect: runs on compute pool\nIO.delay(System.currentTimeMillis())\n\n// Blocking I/O: runs on blocking pool\nIO.blocking {\n  val conn = DriverManager.getConnection(url)\n  conn.prepareStatement(sql).executeQuery()\n}\n\n// Interruptible blocking: can be cancelled\nIO.interruptible {\n  // Thread.interrupt() will be called on cancellation\n  Thread.sleep(Long.MaxValue)\n}',
    tags: ['effect-systems', 'blocking', 'thread-pools'],
  },
  {
    id: 'scala-fp-l4-062',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Profunctors',
    question: 'What is the Wander typeclass for profunctors and how does it enable Traversals?',
    options: [
      'Wander extends Strong and Choice with wander, enabling a profunctor to traverse over structures with multiple focuses, which is exactly what Traversals need in the profunctor optics formulation',
      'Wander is a debug utility for profunctors',
      'Wander allows profunctors to iterate randomly',
      'Wander converts profunctors to functors',
    ],
    correctAnswer: 0,
    explanation: 'In profunctor optics, a Traversal is Optic[P, S, T, A, B] where P: Wander. Wander[P] provides wander: (forall F. Applicative[F] => (A => F[B]) => S => F[T]) => P[A, B] => P[S, T]. This captures the essence of Traverse: iterating over multiple focuses within a structure. Lens requires Strong, Prism requires Choice, Traversal requires Wander.',
    tags: ['profunctor', 'wander', 'traversal'],
  },
  {
    id: 'scala-fp-l4-063',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is a futumorphism and what is its relationship to anamorphism?',
    options: [
      'A futumorphism is like an anamorphism but the coalgebra can produce multiple layers of structure at once using Free, enabling generation of multiple levels ahead instead of just one',
      'A futumorphism predicts future values',
      'A futumorphism is a futuristic recursion scheme for async operations',
      'A futumorphism reverses the effects of a catamorphism',
    ],
    correctAnswer: 0,
    explanation: 'Anamorphism produces one layer at a time with A => F[A]. Futumorphism (futu) uses A => F[Free[F, A]], where the Free can embed multiple layers of F before reaching the next seed A. This means the coalgebra can "look ahead" and produce multiple levels of the output structure in one step. Futumorphism is dual to histomorphism (which looks back multiple levels).',
    codeSnippet: '// Anamorphism: produce one layer at a time\n// coalgebra: A => F[A]\n\n// Futumorphism: produce multiple layers at once\n// coalgebra: A => F[Free[F, A]]\ndef futu[F[_]: Functor, A](\n  coalgebra: A => F[Free[F, A]]\n)(seed: A): Fix[F] = {\n  Fix(coalgebra(seed).map {\n    case Pure(a) => futu(coalgebra)(a)\n    case Free(ff) => Fix(ff.map(free => futu(coalgebra)(???)))\n  })\n}',
    tags: ['recursion-schemes', 'futumorphism', 'unfold'],
  },
  {
    id: 'scala-fp-l4-064',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is Supervisor in Cats Effect and how does it manage fiber lifecycle?',
    options: [
      'Supervisor is a testing utility for mocking fibers',
      'Supervisor monitors system resources',
      'Supervisor is an actor supervision strategy',
      'Supervisor provides structured concurrency: fibers started within a Supervisor are automatically cancelled when the supervisor scope exits, preventing fiber leaks and ensuring cleanup',
    ],
    correctAnswer: 3,
    explanation: 'Supervisor[IO] creates a scope for managing fibers. Fibers started via supervisor.supervise(io) are tracked, and when the supervisor\'s use block completes (or fails), all supervised fibers are automatically cancelled and awaited. This prevents "fire and forget" fiber leaks. It provides structured concurrency where the lifetime of child fibers is bounded by the parent scope.',
    codeSnippet: 'import cats.effect.std.Supervisor\n\nSupervisor[IO].use { supervisor =>\n  for {\n    // These fibers are tracked by the supervisor\n    _ <- supervisor.supervise(backgroundTask1)\n    _ <- supervisor.supervise(backgroundTask2)\n    // When this block exits, both fibers are cancelled\n    result <- mainTask\n  } yield result\n}\n// All supervised fibers guaranteed to be cleaned up here',
    tags: ['effect-systems', 'supervisor', 'structured-concurrency'],
  },
  {
    id: 'scala-fp-l4-065',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is Kleisli and how does it relate to ReaderT?',
    options: [
      'Kleisli is a debugging tool',
      'Kleisli[F, A, B] wraps A => F[B] and is identical to ReaderT[F, A, B]; it represents a function that takes input A and produces effectful output F[B], enabling dependency injection by threading configuration/environment through computations',
      'Kleisli is a serialization format',
      'Kleisli is a concurrency primitive',
    ],
    correctAnswer: 1,
    explanation: 'Kleisli[F, A, B] = ReaderT[F, A, B] = A => F[B]. It is a function from A to F[B] that composes via flatMap (Kleisli composition). This is the foundation of reader-based dependency injection: A is the "environment" (config, services), F is the effect, B is the result. Kleisli.ask provides the environment. In practice, ZIO\'s R parameter and Cats Effect\'s IOLocal serve similar purposes.',
    codeSnippet: 'import cats.data.Kleisli\n\ncase class AppConfig(dbUrl: String, apiKey: String)\n\ntype AppIO[A] = Kleisli[IO, AppConfig, A]\n\ndef connectDb: AppIO[Database] = Kleisli { config =>\n  IO(Database.connect(config.dbUrl))\n}\n\ndef callApi: AppIO[ApiResponse] = Kleisli { config =>\n  IO(httpClient.get(config.apiKey))\n}\n\nval program: AppIO[Result] = for {\n  db <- connectDb\n  api <- callApi\n} yield combine(db, api)\n\n// Provide environment at the edge\nprogram.run(AppConfig("jdbc:...\", \"key123\"))',
    tags: ['monad-transformers', 'kleisli', 'dependency-injection'],
  },
  {
    id: 'scala-fp-l4-066',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "mid" pattern in Tagless Final for cross-cutting concerns?',
    options: [
      'A Mid[F, A] wraps F[A] => F[A] and acts as middleware for a Tagless Final algebra, enabling cross-cutting concerns like logging, metrics, tracing, or retry logic to be added to any algebra implementation without modifying it',
      'Mid computes the median value of a collection',
      'Mid is middleware for HTTP servers',
      'Mid is the middle layer of a three-tier architecture',
    ],
    correctAnswer: 0,
    explanation: 'The Mid pattern defines a natural transformation-like wrapper that intercepts each operation of an algebra. Given an algebra instance, you can wrap it with a Mid that adds behavior before/after each call. Multiple Mids compose. This separates cross-cutting concerns from business logic. Libraries like tofu provide Mid support, or you can implement it manually.',
    codeSnippet: 'trait UserService[F[_]] {\n  def getUser(id: UserId): F[Option[User]]\n  def saveUser(user: User): F[Unit]\n}\n\n// Mid: logging wrapper\nclass LoggingMid[F[_]: FlatMap: Logger] extends UserService[Mid[F, *]] {\n  def getUser(id: UserId) = Mid { fa =>\n    Logger[F].info(s"Getting user $id\") *> fa\n  }\n  def saveUser(user: User) = Mid { fa =>\n    Logger[F].info(s"Saving user ${user.id}\") *> fa <*\n    Logger[F].info("User saved\")\n  }\n}\n\n// Apply mid to real implementation\nval logged: UserService[IO] = loggingMid.attach(realService)',
    tags: ['tagless-final', 'mid', 'cross-cutting'],
  },
  {
    id: 'scala-fp-l4-067',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is a "Fold" optic in Monocle?',
    options: [
      'It folds paper-like data structures',
      'A Fold[S, A] is a read-only optic that can focus on multiple targets, providing getAll: S => List[A] and fold operations but no modification capability; it sits at the bottom of the optics hierarchy',
      'It reduces a collection to a single value',
      'It creates a fold in the type system',
    ],
    correctAnswer: 1,
    explanation: 'Fold[S, A] is the most general read-only optic. It generalizes Getter (one target) and Traversal (read-write, multiple targets). A Fold can focus on any number of elements and provides foldMap, getAll, find, exists, etc. Any optic (Lens, Prism, Traversal, Getter) can be used as a Fold. Use Fold when you only need to read multiple values without modification.',
    codeSnippet: 'import monocle.Fold\n\n// Fold from a Traversal (read-only view)\nval allSalaries: Fold[Company, Double] =\n  allEmployeesTraversal.andThen(salaryLens).asFold\n\n// Use Fold operations\nallSalaries.getAll(company)       // List[Double]\nallSalaries.find(_ > 100000)(company)  // Option[Double]\nallSalaries.exists(_ > 100000)(company) // Boolean\nallSalaries.foldMap(identity)(company)  // Double (sum via Monoid)\n\n// Custom Fold\nval evenIndices: Fold[List[Int], Int] =\n  Fold.fromFoldable[List, Int].andThen(\n    Fold.select[Int](_ % 2 == 0)\n  )',
    tags: ['optics', 'fold', 'read-only'],
  },
  {
    id: 'scala-fp-l4-068',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is IOLocal in Cats Effect and how does it compare to ThreadLocal?',
    options: [
      'IOLocal stores data on the local filesystem',
      'IOLocal is a local network IO implementation',
      'IOLocal[A] provides fiber-local storage: each fiber has its own copy of the value, and child fibers inherit the parent\'s value at fork time, unlike ThreadLocal which is bound to OS threads and breaks with fiber-based concurrency',
      'IOLocal is a thread-local variable for IO',
    ],
    correctAnswer: 2,
    explanation: 'ThreadLocal binds values to OS threads, but fibers multiplex across threads, so ThreadLocal values are inconsistent. IOLocal[A] binds values to fibers: each fiber sees its own value, and forked fibers inherit the parent\'s current value. Modifications in a child fiber do not affect the parent. This is essential for tracing (trace IDs), MDC logging, and per-request context in fiber-based applications.',
    codeSnippet: 'import cats.effect.IOLocal\n\ndef withTraceId: IO[Unit] = for {\n  traceId <- IOLocal(\"\")\n  _ <- traceId.set("trace-123\")\n  // Child fiber inherits "trace-123\"\n  fiber <- (for {\n    id <- traceId.get\n    _ <- IO.println(s"Child sees: $id\")  // "trace-123\"\n    _ <- traceId.set("child-456\")\n  } yield ()).start\n  _ <- fiber.join\n  // Parent still sees original value\n  id <- traceId.get\n  _ <- IO.println(s"Parent sees: $id\")  // "trace-123\"\n} yield ()',
    tags: ['effect-systems', 'io-local', 'fiber-local'],
  },
  {
    id: 'scala-fp-l4-069',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Yoneda',
    question: 'How does the Codensity monad relate to performance optimization?',
    options: [
      'Codensity[F, A] = forall B. (A => F[B]) => F[B] is the CPS-transformed version of F[A]; for Free Monads, using Codensity converts left-associated flatMap chains (O(n^2)) to right-associated ones (O(n)), dramatically improving performance',
      'Codensity increases code density for better cache performance',
      'Codensity compresses code for smaller binaries',
      'Codensity is unrelated to performance',
    ],
    correctAnswer: 0,
    explanation: 'Left-associated flatMap chains on Free Monads have O(n^2) complexity because each step must traverse the entire left spine. Codensity wraps the computation in CPS (continuation-passing style), which naturally right-associates the binds. This is the same optimization as difference lists for list concatenation. Using Codensity then lowering back to Free converts the O(n^2) to O(n).',
    codeSnippet: '// Left-associated: O(n^2) for Free\nval slow = ((m >>= f) >>= g) >>= h  // each >>= traverses left spine\n\n// Codensity: right-associated internally\ntrait Codensity[F[_], A] {\n  def run[B](f: A => F[B]): F[B]\n}\n\n// Lift into Codensity, do work, then lower\ndef improve[F[_]: Monad, A](fa: Free[F, A]): Free[F, A] =\n  toCodensity(fa).lower  // O(n) instead of O(n^2)',
    tags: ['yoneda', 'codensity', 'performance'],
  },
  {
    id: 'scala-fp-l4-070',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is ContT (Continuation monad transformer) and when is it useful?',
    options: [
      'ContT provides content type negotiation for HTTP',
      'ContT[F, R, A] wraps (A => F[R]) => F[R], representing computations in continuation-passing style; it is useful for implementing backtracking, early exit, coroutines, and for converting callback-based APIs to monadic style',
      'ContT is a container type for collections',
      'ContT continues execution after an error',
    ],
    correctAnswer: 1,
    explanation: 'ContT[F, R, A] = (A => F[R]) => F[R]. It captures "the rest of the computation" as a continuation. flatMap passes the current value to the continuation. ContT enables: early exit (don\'t call the continuation), backtracking (call it multiple times), and converting callback-based APIs (wrap the callback in a continuation). It is powerful but can be hard to reason about.',
    codeSnippet: 'import cats.data.ContT\n\n// Early exit pattern\ndef findFirst[F[_]: Monad](items: List[Item]): ContT[F, Result, Item] =\n  ContT { (k: Item => F[Result]) =>\n    items.find(_.isValid) match {\n      case Some(item) => k(item)  // continue with found item\n      case None => Monad[F].pure(NotFound)  // early exit!\n    }\n  }\n\n// Convert callback API to monadic\ndef fromCallback[A]: ContT[IO, A, A] =\n  ContT { (k: A => IO[A]) =>\n    IO.async_[A] { cb =>\n      callbackApi.register(result => cb(Right(result)))\n    }.flatMap(k)\n  }',
    tags: ['monad-transformers', 'contT', 'continuations'],
  },
  {
    id: 'scala-fp-l4-071',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the difference between foldMap and compile in Free Monad interpretation?',
    options: [
      'compile is faster than foldMap',
      'foldMap interprets a Free[F, A] into a target monad G using a natural transformation F ~> G; compile transforms a Free[F, A] into Free[G, A] by translating the algebra without running it, enabling algebra-to-algebra transformations',
      'foldMap works with Futures while compile works with IOs',
      'foldMap is for folding while compile is for compilation',
    ],
    correctAnswer: 1,
    explanation: 'foldMap uses F ~> G (where G is a Monad) to interpret the Free program, running it and producing a G[A] result. compile uses F ~> G (where G is any functor) to translate the algebra without running: the result is Free[G, A], a new program in a different algebra. compile is useful for layering transformations: first compile from one algebra to another, then foldMap to run.',
    codeSnippet: '// foldMap: interpret and run\nval result: IO[String] = program.foldMap(interpreter)\n// interpreter: KVStoreA ~> IO\n\n// compile: translate algebra without running\nval translated: Free[EnhancedKVStoreA, String] =\n  program.compile(algebrasTranslation)\n// algebrasTranslation: KVStoreA ~> EnhancedKVStoreA\n\n// Then foldMap the translated program\nval result2: IO[String] = translated.foldMap(enhancedInterpreter)',
    tags: ['free-monad', 'foldMap', 'compile'],
  },
  {
    id: 'scala-fp-l4-072',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the difference between IO.async and IO.async_ in Cats Effect?',
    options: [
      'IO.async_ provides a simple callback registration without cancellation support; IO.async provides a way to return an optional finalizer for cancellation, enabling integration with cancellable external APIs',
      'async_ is the deprecated version of async',
      'async is synchronous and async_ is asynchronous',
      'async handles errors while async_ ignores errors',
    ],
    correctAnswer: 0,
    explanation: 'IO.async_(cb => register(cb)) is for simple callback-based APIs where there is no way to cancel the registered callback. IO.async(cb => IO(Some(cancelToken))) allows returning an IO[Unit] finalizer that is invoked if the fiber is cancelled while waiting. This is essential for cancellation-aware integrations (e.g., cancelling an HTTP request, unsubscribing from an event).',
    codeSnippet: '// async_: no cancellation support\nval simple: IO[String] = IO.async_[String] { cb =>\n  eventBus.subscribe(event => cb(Right(event)))\n  // No way to unsubscribe on cancellation\n}\n\n// async: with cancellation support\nval cancellable: IO[String] = IO.async[String] { cb =>\n  IO {\n    val subscription = eventBus.subscribe(event => cb(Right(event)))\n    Some(IO(subscription.unsubscribe()))  // cleanup on cancel\n  }\n}',
    tags: ['effect-systems', 'async', 'callbacks'],
  },
  {
    id: 'scala-fp-l4-073',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is a Getter in Monocle optics?',
    options: [
      'A Getter fetches data from a database',
      'A Getter is a Java-style getter method',
      'A Getter retrieves data from an HTTP endpoint',
      'A Getter[S, A] is a read-only optic that focuses on exactly one target A in S, essentially wrapping S => A; it is the read-only counterpart of Lens and composes with other optics to produce Folds',
    ],
    correctAnswer: 3,
    explanation: 'Getter[S, A] wraps a function S => A and provides get: S => A. Unlike Lens, it has no set/modify capability. It is useful when you want to expose a derived or computed value as an optic that composes with others. Composing any optic with a Getter produces a Fold (read-only). Getter is less commonly used directly since Lens already provides get functionality.',
    codeSnippet: 'import monocle.Getter\n\ncase class User(firstName: String, lastName: String)\n\n// Getter for a computed value\nval fullName: Getter[User, String] =\n  Getter[User, String](u => s"${u.firstName} ${u.lastName}\")\n\n// Compose with Lens\nval userLens: Lens[Request, User] = ...\nval requestName: Getter[Request, String] =\n  userLens.andThen(fullName)  // Lens + Getter = Getter\n\nrequestName.get(request)  // "John Doe\"',
    tags: ['optics', 'getter', 'read-only'],
  },
  {
    id: 'scala-fp-l4-074',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "higher-order algebra" pattern in Tagless Final?',
    options: [
      'It uses algebras parameterized by type-level numbers',
      'It uses algebras that have higher priority',
      'It creates algebras that extend other algebras',
      'A higher-order algebra has methods that take F[A] as parameters (not just returning F[A]), enabling expression of control flow patterns like retry, timeout, and transactional boundaries within the algebra',
    ],
    correctAnswer: 3,
    explanation: 'Standard TF algebras have methods returning F[A]. Higher-order algebras take F[A] as input: def retry[A](fa: F[A], attempts: Int): F[A] or def transact[A](fa: F[A]): F[A]. This allows the algebra to express effect-level operations like retrying, timeouts, or running something in a transaction. The interpreter decides how these operations are implemented.',
    codeSnippet: 'trait Transactions[F[_]] {\n  def transact[A](fa: F[A]): F[A]  // higher-order: takes F[A]\n}\n\ntrait Retry[F[_]] {\n  def retry[A](fa: F[A], maxAttempts: Int): F[A]  // higher-order\n}\n\ndef program[F[_]: Monad: UserRepo: Transactions: Retry]: F[User] =\n  Retry[F].retry(\n    Transactions[F].transact(\n      UserRepo[F].save(user)\n    ),\n    maxAttempts = 3\n  )',
    tags: ['tagless-final', 'higher-order', 'control-flow'],
  },
  {
    id: 'scala-fp-l4-075',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is the Matryoshka library in Scala and what does it provide?',
    options: [
      'Matryoshka is a UI component library',
      'Matryoshka is a nesting utility for collections',
      'Matryoshka is a Scala library implementing recursion schemes (catamorphism, anamorphism, hylomorphism, paramorphism, etc.) with fixed-point types and automatic derivation of base functors',
      'Matryoshka is a testing framework for nested test suites',
    ],
    correctAnswer: 2,
    explanation: 'Matryoshka provides Fix, Mu, Nu as fixed-point types, and implements standard recursion schemes (cata, ana, hylo, para, histo, futu, zygo, etc.). It uses the Recursive and Corecursive typeclasses to abstract over different fixed-point representations. The library allows you to define non-recursive base functors and get all recursion schemes for free, promoting separation of recursion from logic.',
    codeSnippet: 'import matryoshka._\nimport matryoshka.implicits._\n\nsealed trait ExprF[A]\ncase class LitF[A](value: Int) extends ExprF[A]\ncase class AddF[A](l: A, r: A) extends ExprF[A]\n\nimplicit val exprFunctor: Functor[ExprF] = ...\n\ntype Expr = Fix[ExprF]\n\n// Use any recursion scheme\nval eval: Algebra[ExprF, Int] = {  // ExprF[Int] => Int\n  case LitF(v) => v\n  case AddF(l, r) => l + r\n}\n\nexpr.cata(eval)  // evaluate\nexpr.para(paraAlgebra)  // with original structure access',
    tags: ['recursion-schemes', 'matryoshka', 'library'],
  },
  {
    id: 'scala-fp-l4-076',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the Dispatcher in Cats Effect and when do you need it?',
    options: [
      'Dispatcher routes messages to actors',
      'Dispatcher[IO] provides a way to unsafely run IO effects from within synchronous callback-based code, enabling interop between Cats Effect IO and legacy APIs that require synchronous results',
      'Dispatcher manages thread pool allocation',
      'Dispatcher is the same as ExecutionContext',
    ],
    correctAnswer: 1,
    explanation: 'Dispatcher[IO] provides unsafeRunSync, unsafeRunAsync, and unsafeToFutureCancelable methods. It is needed at integration boundaries where a callback-based or synchronous API requires a result, but your logic is in IO. For example, Java Servlet filters, JDBC statement interceptors, or legacy callback APIs. Dispatcher is a Resource because it manages a fiber runtime that must be shut down.',
    codeSnippet: 'import cats.effect.std.Dispatcher\n\nDispatcher.parallel[IO].use { dispatcher =>\n  IO {\n    // Legacy Java callback API\n    legacyApi.registerHandler(new Handler {\n      override def onEvent(event: Event): Response = {\n        // Need to run IO synchronously inside callback\n        dispatcher.unsafeRunSync(\n          processEvent(event)  // IO[Response]\n        )\n      }\n    })\n  } *> IO.never\n}',
    tags: ['effect-systems', 'dispatcher', 'interop'],
  },
  {
    id: 'scala-fp-l4-077',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Free Monads',
    question: 'What is the "day convolution" and how does it relate to Free Applicatives?',
    options: [
      'Day convolution Day[F, G, A] combines two functors F and G by pairing their values with a combining function; Free Applicative can be understood as the free construction over Day convolution, enabling independent parallel composition',
      'Day convolution is unrelated to Free Applicatives',
      'Day convolution processes daily data batches',
      'Day convolution converts between day and night time formats',
    ],
    correctAnswer: 0,
    explanation: 'Day[F, G, A] = exists X Y. (F[X], G[Y], (X, Y) => A). It pairs values from two functors and combines them. The Free Applicative over a functor F is equivalent to the free monoidal functor, which uses Day convolution to combine independent computations. This structure preserves independence (unlike Free Monad which introduces sequencing), enabling static analysis and parallel interpretation.',
    tags: ['free-applicative', 'day-convolution', 'category-theory'],
  },
  {
    id: 'scala-fp-l4-078',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'How does the Cats Effect runtime handle work-stealing and what is its impact on performance?',
    options: [
      'Cats Effect does not implement work-stealing',
      'The runtime uses a single thread to avoid concurrency issues',
      'Work-stealing is handled by the operating system',
      'The Cats Effect 3 runtime uses a work-stealing thread pool where idle worker threads steal tasks from busy threads\' local queues, reducing contention and improving CPU utilization compared to traditional fork-join pools',
    ],
    correctAnswer: 3,
    explanation: 'The CE3 runtime uses a custom work-stealing pool with per-thread local queues. When a fiber yields (at an async boundary), it is placed on the current thread\'s local queue. When a thread finishes its work, it steals from other threads\' queues. This minimizes contention (no shared queue) and improves cache locality (fibers tend to run on the same thread). The pool is sized to CPU core count for compute-bound work.',
    tags: ['effect-systems', 'runtime', 'work-stealing'],
  },
  {
    id: 'scala-fp-l4-079',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'How do you use optics with JSON processing in Scala?',
    options: [
      'Libraries like Circe integrate with Monocle to provide optics-based JSON traversal and modification, allowing type-safe navigation of JSON structures using Lenses, Prisms, and Traversals',
      'You must convert JSON to case classes first, then use optics',
      'Optics and JSON are unrelated',
      'JSON optics only work with XML data',
    ],
    correctAnswer: 0,
    explanation: 'Circe-optics provides Monocle-based optics for Json values. You can navigate JSON using field-specific lenses (root.user.name.string) and modify nested values without parsing to case classes. This is useful for JSON transformation pipelines where you need to modify specific fields in large JSON documents without defining a full domain model.',
    codeSnippet: 'import io.circe.optics.JsonPath._\n\nval json = parse(\"\"\"{"user":{"name":"Alice","age":30}}\"\"\").getOrElse(Json.Null)\n\n// Navigate with optics\nval name: Option[String] = root.user.name.string.getOption(json)\n// Some("Alice\")\n\n// Modify nested fields\nval updated = root.user.age.int.modify(_ + 1)(json)\n// {"user":{"name":"Alice","age":31}}\n\n// Traverse arrays\nval items = parse(\"\"\"{"items":[1,2,3]}\"\"\")\nroot.items.each.int.getAll(items)  // List(1, 2, 3)',
    tags: ['optics', 'json', 'circe'],
  },
  {
    id: 'scala-fp-l4-080',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is IorT and when would you use it over EitherT?',
    options: [
      'IorT[F, E, A] wraps F[Ior[E, A]] where Ior can be Left (error only), Right (success only), or Both (success with accumulated warnings); use it when you want to collect warnings while still producing a result, unlike EitherT which short-circuits on error',
      'IorT is a typed version of EitherT',
      'IorT handles input/output/read operations',
      'IorT is used for international operations requiring localization',
    ],
    correctAnswer: 0,
    explanation: 'Ior[E, A] (Inclusive Or) has three cases: Left(e), Right(a), or Both(e, a). IorT[F, E, A] wraps F[Ior[E, A]]. When sequencing with flatMap, Both accumulates E values (requiring Semigroup[E]) while continuing computation. This is perfect for validation with warnings: collect all warnings while still producing a result. EitherT stops at the first error; IorT continues and accumulates.',
    codeSnippet: 'import cats.data.{Ior, IorT}\nimport cats.data.NonEmptyChain\n\ntype Warnings = NonEmptyChain[String]\ntype Validated[A] = IorT[IO, Warnings, A]\n\ndef validateAge(age: Int): Validated[Int] =\n  if (age < 0) IorT.leftT(NonEmptyChain.one("Invalid age\"))\n  else if (age > 150) IorT.bothT(NonEmptyChain.one("Unusual age\"), age)\n  else IorT.rightT(age)\n\ndef validateName(name: String): Validated[String] =\n  if (name.isEmpty) IorT.leftT(NonEmptyChain.one("Empty name\"))\n  else IorT.rightT(name)\n\n// Accumulates warnings via Semigroup\nval result = for {\n  age <- validateAge(200)   // Both(warning, 200)\n  name <- validateName("Alice\") // Right("Alice\")\n} yield User(name, age)  // Both(warning, User(...))',
    tags: ['monad-transformers', 'iorT', 'validation'],
  },
  {
    id: 'scala-fp-l4-081',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Tagless Final',
    question: 'What is the "interpreter pattern" for running Tagless Final programs in tests with mock clocks, mock databases, etc.?',
    options: [
      'Create algebra instances targeting a state monad (StateT or Ref-based) that simulates the behavior and records interactions, enabling deterministic testing without external dependencies',
      'Use Java mocking frameworks to mock the type class instances',
      'Test programs can only be tested with integration tests against real services',
      'Use dependency injection containers for test configuration',
    ],
    correctAnswer: 0,
    explanation: 'For testing TF programs, create algebra instances that use controlled implementations. For time-dependent code, create a Clock[F] instance that returns pre-set times. For databases, use an in-memory Map inside a Ref. For randomness, use a seeded generator. This makes tests deterministic, fast, and isolated. The program under test does not change; only the provided instances differ between production and test.',
    codeSnippet: 'trait Clock[F[_]] {\n  def now: F[Instant]\n}\n\n// Test instance with controllable time\ndef testClock(ref: Ref[IO, Instant]): Clock[IO] = new Clock[IO] {\n  def now: IO[Instant] = ref.get\n}\n\n// Test\nval test = for {\n  timeRef <- Ref[IO].of(Instant.parse("2024-01-01T00:00:00Z\"))\n  implicit0(clock: Clock[IO]) = testClock(timeRef)\n  result <- myProgram[IO]  // uses test clock\n  _ <- timeRef.set(Instant.parse("2024-01-02T00:00:00Z\"))\n  result2 <- myProgram[IO]  // one day later\n} yield assert(result != result2)',
    tags: ['tagless-final', 'testing', 'mocking'],
  },
  {
    id: 'scala-fp-l4-082',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Recursion Schemes',
    question: 'What is the difference between Fix, Mu, and Nu as fixed-point types?',
    options: [
      'They differ only in performance characteristics',
      'Fix is for fixing bugs, Mu is for music data, Nu is for new data',
      'Fix is the standard fixed-point (iso-recursive), Mu is the least fixed-point (corresponds to finite/inductive data, enables catamorphism), and Nu is the greatest fixed-point (corresponds to potentially infinite/coinductive data, enables anamorphism)',
      'They are different names for the same type',
    ],
    correctAnswer: 2,
    explanation: 'Fix[F] = F[Fix[F]] is a simple recursive wrapper. Mu[F] (least fixed-point) is defined as forall A. (F[A] => A) => A (it IS a catamorphism), guaranteeing finite/well-founded data. Nu[F] (greatest fixed-point) is defined as exists A. (A, A => F[A]) (it IS an anamorphism), allowing potentially infinite data. In Scala, the distinction mostly matters for totality: Mu guarantees termination of folds, Nu guarantees productivity of unfolds.',
    codeSnippet: '// Fix: simple recursive wrapping\ncase class Fix[F[_]](unfix: F[Fix[F]])\n\n// Mu: least fixed-point (inductive/finite)\ntrait Mu[F[_]] {\n  def cata[A](algebra: F[A] => A): A  // always terminates\n}\n\n// Nu: greatest fixed-point (coinductive/possibly infinite)\ntrait Nu[F[_]] {\n  type A\n  val seed: A\n  val coalgebra: A => F[A]  // always productive\n}',
    tags: ['recursion-schemes', 'fixed-point', 'theory'],
  },
  {
    id: 'scala-fp-l4-083',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the "tagless final with resources" pattern for application startup?',
    options: [
      'Use Resource[F, A] to compose the application wiring, where each component is a Resource that acquires dependencies and registers cleanup; the final Resource.use runs the application with guaranteed cleanup of all components on shutdown',
      'Resources are allocated lazily on first use',
      'Use a configuration file to define resources',
      'Tag all resources with final tags for tracking',
    ],
    correctAnswer: 0,
    explanation: 'The application is wired as a Resource[IO, App] where App contains all running services. Each component (database pool, HTTP client, server) is a Resource. They compose with flatMap: the database Resource is used to create the repository, which is used to create the service, which is used to create the HTTP server. On shutdown (or error), everything is cleaned up in reverse order.',
    codeSnippet: 'def app: Resource[IO, Unit] = for {\n  config <- Resource.eval(loadConfig)\n  pool <- DatabasePool.resource(config.db)\n  client <- EmberClient.default[IO]\n  repo = new UserRepo[IO](pool)\n  service = new UserService[IO](repo, client)\n  routes = new UserRoutes[IO](service).routes\n  _ <- EmberServer.builder[IO]\n    .withHost(config.host)\n    .withPort(config.port)\n    .withHttpApp(routes.orNotFound)\n    .build\n} yield ()\n\n// Run: all resources cleaned up on SIGTERM\nobject Main extends IOApp.Simple {\n  def run = app.useForever\n}',
    tags: ['effect-systems', 'resource', 'application-wiring'],
  },
  {
    id: 'scala-fp-l4-084',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Monad Transformers',
    question: 'What is the EitherT.cond pattern and why is it useful?',
    options: [
      'It conditionally creates an EitherT from a boolean expression',
      'EitherT.cond[F](test, rightValue, leftValue) lifts a boolean condition into an EitherT, returning Right when true and Left when false, providing a concise way to express validation logic',
      'It converts exceptions into EitherT values',
      'It applies conditional logic to the transformer stack',
    ],
    correctAnswer: 1,
    explanation: 'EitherT.cond[F](predicate, rightValue, leftValue) is a convenient constructor that creates EitherT(F.pure(Right(rightValue))) when the predicate is true, or EitherT(F.pure(Left(leftValue))) when false. This replaces verbose if-else chains in validation logic. Combined with flatMap, it creates clean validation pipelines in for-comprehensions.',
    codeSnippet: 'type Result[A] = EitherT[IO, AppError, A]\n\ndef validateUser(req: CreateUserReq): Result[ValidUser] = for {\n  _ <- EitherT.cond[IO](\n    req.name.nonEmpty,\n    (),\n    ValidationError("Name is required\")\n  )\n  _ <- EitherT.cond[IO](\n    req.age >= 18,\n    (),\n    ValidationError("Must be 18 or older\")\n  )\n  _ <- EitherT.cond[IO](\n    req.email.contains("@\"),\n    (),\n    ValidationError("Invalid email\")\n  )\n} yield ValidUser(req.name, req.age, req.email)',
    tags: ['monad-transformers', 'eitherT', 'validation'],
  },
  {
    id: 'scala-fp-l4-085',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Optics',
    question: 'What is the "Plated" typeclass in optics and what does it enable?',
    options: [
      'Plated[A] provides a Traversal from A to its immediate children of the same type A, enabling generic operations like rewrite rules, transformation, and universal quantification over recursive data structures',
      'Plated converts optics to SQL queries',
      'Plated provides gold-plating for premium optics',
      'Plated is used for flattening nested structures',
    ],
    correctAnswer: 0,
    explanation: 'Plated[A] provides plate: Traversal[A, A] that focuses on the immediate self-typed children. From this, you get: rewrite (bottom-up transformation until fixpoint), transform (one-pass bottom-up), universe (all descendants), cosmos (all descendants including self). This is extremely powerful for AST transformations, query optimization, and code rewriting.',
    codeSnippet: 'import monocle.Plated\n\nsealed trait Expr\ncase class Lit(n: Int) extends Expr\ncase class Add(l: Expr, r: Expr) extends Expr\ncase class Mul(l: Expr, r: Expr) extends Expr\n\nimplicit val exprPlated: Plated[Expr] = Plated[Expr] {\n  case Lit(_) => Traversal.void  // no children\n  case Add(l, r) => Traversal.pair[Expr]  // two children\n  case Mul(l, r) => Traversal.pair[Expr]\n}\n\n// Simplification: rewrite until no more changes\nval simplified = Plated.rewrite[Expr] {\n  case Add(Lit(0), x) => Some(x)  // 0 + x => x\n  case Mul(Lit(1), x) => Some(x)  // 1 * x => x\n  case _ => None\n}(expr)',
    tags: ['optics', 'plated', 'rewriting'],
  },
  {
    id: 'scala-fp-l4-086',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Effect Systems',
    question: 'What is the difference between IO.race and IO.racePair in Cats Effect?',
    options: [
      'IO.race cancels the loser and returns the winner\'s result; IO.racePair returns the winner\'s result AND the loser\'s Fiber so you can decide what to do with the loser (cancel, join, or ignore)',
      'race returns the faster result, racePair returns both',
      'race is faster, racePair is more reliable',
      'race works with two IOs, racePair works with more',
    ],
    correctAnswer: 0,
    explanation: 'IO.race(io1, io2): IO[Either[A, B]] runs both and cancels the loser automatically, returning the winner. IO.racePair(io1, io2): IO[Either[(A, Fiber[B]), (Fiber[A], B)]] returns the winner\'s result and the loser\'s Fiber. This gives you control: you might want to log the loser\'s result, let it complete for side effects, or cancel it conditionally.',
    codeSnippet: '// race: automatic cancellation of loser\nval result: IO[String] =\n  IO.race(primaryService.call(), fallbackService.call())\n    .map(_.merge)\n\n// racePair: manual control over loser\nval result2: IO[String] =\n  IO.racePair(primaryService.call(), fallbackService.call()).flatMap {\n    case Left((primaryResult, fallbackFiber)) =>\n      fallbackFiber.cancel.as(primaryResult)\n    case Right((primaryFiber, fallbackResult)) =>\n      // Primary lost; let it complete for metrics\n      primaryFiber.joinWithNever.attempt.void\n        .start.as(fallbackResult)\n  }',
    tags: ['effect-systems', 'race', 'concurrency'],
  },
  {
    id: 'scala-fp-l4-087',
    language: 'scala',
    level: 'level4',
    category: 'Functional Programming',
    subcategory: 'Algebraic Effects',
    question: 'How does Scala 3\'s context functions feature relate to algebraic effects patterns?',
    options: [
      'Context functions are unrelated to algebraic effects',
      'Context functions are only for dependency injection',
      'Context functions (T ?=> R) provide a lightweight way to thread implicit capabilities through computations, approximating the "effect row" of algebraic effects by making required capabilities explicit in the type without explicit parameter passing',
      'Context functions replace monads entirely',
    ],
    correctAnswer: 2,
    explanation: 'Scala 3 context functions (A ?=> B) are functions that take A as an implicit parameter. Combined with capabilities like CanThrow, they approximate effect systems: a function requiring FileSystem ?=> Result explicitly declares its effects in the type. While not as powerful as true algebraic effects (no handlers/resumptions), they provide a lightweight, zero-overhead way to track required capabilities at the type level.',
    codeSnippet: '// Scala 3 context functions as lightweight effects\nimport language.experimental.saferExceptions\n\nclass FileNotFound extends Exception\nclass ParseError extends Exception\n\n// Effects declared in type via context functions\ndef readConfig(path: String)(using CanThrow[FileNotFound]): Config =\n  val content = readFile(path)  // may throw FileNotFound\n  parseConfig(content)\n\ndef parseConfig(s: String)(using CanThrow[ParseError]): Config = ???\n\n// Caller must handle the effects\ntry\n  readConfig("app.conf\")\ncatch\n  case _: FileNotFound => defaultConfig\n  case _: ParseError => defaultConfig',
    tags: ['algebraic-effects', 'context-functions', 'scala3'],
  },
];

export default questions;
