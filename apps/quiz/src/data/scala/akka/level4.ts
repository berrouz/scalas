import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'scala-akka-l4-001',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'In Akka Typed, what happens when a Behavior returns Behaviors.same but the actor has a stash that was filled before a behavior switch?',
    options: [
      'Stashed messages remain in the stash indefinitely',
      'The actor restarts to clear the stash',
      'Stashed messages are replayed against the current behavior before processing new messages',
      'The stash is automatically cleared',
    ],
    correctAnswer: 2,
    explanation: 'When unstashAll() is called, stashed messages are replayed one by one against the current behavior. If the behavior returns Behaviors.same, subsequent stashed messages continue to be processed against that same behavior. The stash is drained in FIFO order before any new messages from the mailbox are processed.',
    codeSnippet: 'Behaviors.withStash(capacity) { stash =>\n  Behaviors.receiveMessage {\n    case Initialize(data) =>\n      stash.unstashAll(active(data))\n    case other =>\n      stash.stash(other)\n      Behaviors.same\n  }\n}',
    tags: ['typed-actors', 'stash', 'behavior'],
  },
  {
    id: 'scala-akka-l4-002',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the key difference between Behaviors.supervise and wrapping a behavior with try-catch inside receiveMessage?',
    options: [
      'try-catch is more performant than Behaviors.supervise',
      'Behaviors.supervise restarts the actor preserving its ActorRef and mailbox, while try-catch only catches exceptions without actor lifecycle management',
      'Behaviors.supervise handles failures from child actors while try-catch handles local exceptions',
      'There is no difference; both achieve the same result',
    ],
    correctAnswer: 1,
    explanation: 'Behaviors.supervise integrates with the actor lifecycle: on failure, the actor can be restarted (preserving its ActorRef, mailbox, and children), resumed, or stopped. A plain try-catch only catches exceptions at the message processing level without resetting internal state or interacting with supervision trees. The supervisor strategy also affects how the PreRestart signal is handled.',
    codeSnippet: 'Behaviors.supervise(\n  myBehavior\n).onFailure[DbException](\n  SupervisorStrategy.restart\n    .withLimit(3, 1.minute)\n    .withLoggingEnabled(true)\n)',
    tags: ['typed-actors', 'supervision', 'fault-tolerance'],
  },
  {
    id: 'scala-akka-l4-003',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'In Akka Typed, what is the purpose of the narrow method on Behavior[T]?',
    options: [
      'It limits the number of messages the actor can process per second',
      'It safely casts a Behavior[T] to Behavior[U] where U is a subtype of T, for use in typed protocol hierarchies',
      'It reduces memory usage of the behavior',
      'It restricts which actors can send messages to this actor',
    ],
    correctAnswer: 1,
    explanation: 'Behavior.narrow[U] casts a Behavior[T] to Behavior[U] where U <: T. This is needed when composing behaviors in typed protocol hierarchies where a child behavior handles a subset of the parent protocol. It is a safe cast because any handler of T can handle U.',
    codeSnippet: 'sealed trait Command\ncase class Greet(name: String) extends Command\ncase class Stop() extends Command\n\ndef greeter: Behavior[Greet] = ???\n// Use narrow when you need Behavior[Command]\nval behavior: Behavior[Command] = greeter.narrow[Greet]',
    tags: ['typed-actors', 'protocols', 'type-safety'],
  },
  {
    id: 'scala-akka-l4-004',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What happens in Akka Cluster Sharding when a shard region receives a message for an entity that is being rebalanced to another node?',
    options: [
      'The message is dropped silently',
      'The message is buffered in the shard region and delivered after rebalancing completes',
      'An exception is thrown and the sender receives a failure',
      'The message is immediately forwarded to the target node',
    ],
    correctAnswer: 1,
    explanation: 'During shard rebalancing, messages for entities in the shard being moved are buffered by the shard region. Once the handoff is complete and the shard is allocated on the new node, the buffered messages are forwarded. The buffer has a configurable size limit (akka.cluster.sharding.buffer-size), and messages exceeding it are dropped.',
    tags: ['cluster-sharding', 'rebalancing', 'message-buffering'],
  },
  {
    id: 'scala-akka-l4-005',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'In Akka Cluster Sharding, what is the purpose of the remember-entities feature and what is its performance trade-off?',
    options: [
      'It caches entity state in memory for faster access with higher memory usage',
      'It remembers the last message sent to each entity for exactly-once delivery',
      'It stores entity locations in a distributed cache with eventual consistency lag',
      'It ensures entities are automatically restarted after rebalancing or crash by persisting entity IDs, at the cost of additional persistence writes per entity start/stop',
    ],
    correctAnswer: 3,
    explanation: 'Remember-entities persists the set of active entity IDs so that after a shard rebalance or node crash, all previously active entities are restarted on the new node without waiting for a message. The trade-off is that every entity start and stop requires an additional persistence write (to the remember-entities store), which adds latency and storage overhead.',
    codeSnippet: 'ClusterSharding(system).init(\n  Entity(TypeKey)(ctx => MyEntity(ctx.entityId))\n    .withSettings(\n      ClusterShardingSettings(system)\n        .withRememberEntities(true)\n        .withRememberEntitiesStoreMode(RememberEntitiesStoreModeEventSourced)\n    )\n)',
    tags: ['cluster-sharding', 'remember-entities', 'persistence'],
  },
  {
    id: 'scala-akka-l4-006',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What is the ShardCoordinator in Akka Cluster Sharding and what happens if it becomes unavailable?',
    options: [
      'It allocates shards to nodes; if unavailable, already allocated shards continue working but new shard allocations are blocked until a new coordinator is elected',
      'It is a load balancer; if unavailable, messages are sent directly to entities',
      'It routes messages; if unavailable, the entire cluster stops processing',
      'It manages entity lifecycle; if unavailable, all entities are passivated',
    ],
    correctAnswer: 0,
    explanation: 'The ShardCoordinator is a cluster singleton that decides which shard lives on which node. If it becomes unavailable (e.g., the node hosting it crashes), already-allocated shards continue processing messages normally. However, messages for shards not yet allocated will be buffered until a new ShardCoordinator is elected on another node. This is why shard allocation is eventually consistent.',
    tags: ['cluster-sharding', 'coordinator', 'availability'],
  },
  {
    id: 'scala-akka-l4-007',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What allocation strategy should you use in Akka Cluster Sharding when entities have vastly different resource consumption?',
    options: [
      'ConsistentHashingAllocationStrategy for stable mapping',
      'LeastShardAllocationStrategy, as it balances by shard count which works for any workload',
      'ShardCoordinator.ShardAllocationStrategy with a custom implementation that considers entity resource metrics',
      'RoundRobinAllocationStrategy for even distribution',
    ],
    correctAnswer: 2,
    explanation: 'The default LeastShardAllocationStrategy balances by shard count, not resource usage. When entities have heterogeneous resource needs, you should implement a custom ShardAllocationStrategy that factors in metrics like CPU, memory, or message throughput per shard. This lets you route heavy shards to less loaded nodes.',
    codeSnippet: 'class ResourceAwareAllocation extends ShardAllocationStrategy {\n  override def allocateShard(\n    requester: ActorRef,\n    shardId: ShardId,\n    currentShardAllocations: Map[ActorRef, IndexedSeq[ShardId]]\n  ): Future[ActorRef] = {\n    // Pick node with lowest resource usage\n    val leastLoaded = currentShardAllocations.minBy {\n      case (region, shards) => getNodeLoad(region)\n    }\n    Future.successful(leastLoaded._1)\n  }\n}',
    tags: ['cluster-sharding', 'allocation-strategy', 'performance'],
  },
  {
    id: 'scala-akka-l4-008',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'In Akka Persistence, what is the danger of changing the event schema without a proper migration strategy?',
    options: [
      'Old events stored in the journal cannot be deserialized during replay, causing the actor to fail on recovery',
      'New events will fail to serialize',
      'The cluster will split into partitions',
      'The snapshot store will become corrupted',
    ],
    correctAnswer: 0,
    explanation: 'When an EventSourcedBehavior recovers, it replays all events from the journal. If the event schema has changed (e.g., fields removed or renamed) without proper migration, old events will fail deserialization. Solutions include using schema evolution-friendly serializers (like Protobuf with reserved fields), event adapters, or rolling event transformations.',
    codeSnippet: '// Event adapter for schema migration\nclass MyEventAdapter extends EventAdapter[Event, EventWrapper] {\n  override def manifest(event: Event) = event.getClass.getName\n  override def toJournal(event: Event) = EventWrapper(event, schemaVersion = 2)\n  override def fromJournal(wrapper: EventWrapper, manifest: String) =\n    wrapper.schemaVersion match {\n      case 1 => migrateV1toV2(wrapper.event)\n      case 2 => wrapper.event\n    }\n}',
    tags: ['persistence', 'schema-evolution', 'event-sourcing'],
  },
  {
    id: 'scala-akka-l4-009',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'What is the optimal snapshot strategy for an Akka Persistence actor that processes thousands of events per minute?',
    options: [
      'Never snapshot and always replay all events',
      'Snapshot after every single event for maximum safety',
      'Snapshot every N events with retention of the last M snapshots, and delete old events up to the snapshot sequence number',
      'Snapshot only on actor shutdown',
    ],
    correctAnswer: 2,
    explanation: 'Snapshotting every N events (e.g., every 100-1000) balances recovery speed with write overhead. Retaining the last M snapshots provides fallback if the latest is corrupted. Deleting events older than the oldest retained snapshot keeps the journal size bounded. Snapshotting every event wastes I/O; never snapshotting makes recovery increasingly slow.',
    codeSnippet: 'EventSourcedBehavior[Command, Event, State](\n  persistenceId,\n  emptyState,\n  commandHandler,\n  eventHandler\n).withRetention(\n  RetentionCriteria.snapshotEvery(\n    numberOfEvents = 500,\n    keepNSnapshots = 3\n  ).withDeleteEventsOnSnapshot\n)',
    tags: ['persistence', 'snapshots', 'performance'],
  },
  {
    id: 'scala-akka-l4-010',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'What is the role of the eventHandler in Akka Persistence Typed and why must it be a pure function?',
    options: [
      'It serializes events for storage in the journal',
      'It applies events to the current state to produce new state; it must be pure because it is called both during normal processing and during recovery replay',
      'It validates events before they are persisted',
      'It processes commands and may produce side effects',
    ],
    correctAnswer: 1,
    explanation: 'The eventHandler transforms (State, Event) => State. It is called when events are first persisted AND when they are replayed during recovery. If it had side effects (like sending messages or writing to a database), those side effects would be duplicated on every recovery. Purity ensures that replaying the event log always reconstructs the same state.',
    codeSnippet: '// CORRECT: pure event handler\nval eventHandler: (State, Event) => State = { (state, event) =>\n  event match {\n    case ItemAdded(item) => state.copy(items = state.items + item)\n    case ItemRemoved(item) => state.copy(items = state.items - item)\n  }\n}\n// WRONG: side effects in event handler\n// case ItemAdded(item) => db.write(item); state.copy(...)',
    tags: ['persistence', 'event-handler', 'purity'],
  },
  {
    id: 'scala-akka-l4-011',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Event Sourcing',
    question: 'In an event-sourced system using Akka Persistence, how should you handle a command that requires reading the current state of another aggregate before deciding which event to emit?',
    options: [
      'Use distributed transactions across both aggregates',
      'Access the other aggregate\'s internal state through shared mutable state',
      'Directly query the other aggregate\'s database table from the command handler',
      'Use a Saga/Process Manager pattern that orchestrates across aggregates using asynchronous messaging',
    ],
    correctAnswer: 3,
    explanation: 'In event sourcing, aggregates should not directly access each other\'s state. The Saga/Process Manager pattern coordinates multi-aggregate workflows by listening to events from one aggregate and sending commands to another. This preserves aggregate boundaries and avoids coupling, while handling failures through compensating actions.',
    tags: ['event-sourcing', 'saga', 'architecture'],
  },
  {
    id: 'scala-akka-l4-012',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Event Sourcing',
    question: 'What is the key advantage of using event-sourced CQRS projections with Akka Projections over traditional read-model updates?',
    options: [
      'Projections are faster than direct database queries',
      'Projections are always strongly consistent with the write side',
      'Projections allow building multiple independent read models from the same event stream with at-least-once or exactly-once processing guarantees',
      'Projections eliminate the need for a separate database',
    ],
    correctAnswer: 2,
    explanation: 'Akka Projections consume event journals and build read-optimized views. Multiple projections can be created from the same events for different query needs. They provide at-least-once delivery by default (with exactly-once when using transactional offset storage). The read side is eventually consistent but independently scalable and optimized for specific query patterns.',
    codeSnippet: 'val projection = JdbcProjection.exactlyOnce(\n  projectionId = ProjectionId("orders", tag),\n  sourceProvider = EventSourcedProvider\n    .eventsByTag[Order.Event](system, tag),\n  handler = () => new OrderViewHandler(repository),\n  sessionFactory = () => new JdbcSession(dataSource)\n)',
    tags: ['event-sourcing', 'projections', 'cqrs'],
  },
  {
    id: 'scala-akka-l4-013',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'CQRS',
    question: 'In a CQRS architecture with Akka, what is the typical strategy for handling the eventual consistency gap between write and read sides?',
    options: [
      'Use techniques like causal consistency tokens, read-your-writes via direct aggregate queries, or UI optimistic updates',
      'Eliminate the read side and always query the write side',
      'Force synchronous projection updates in the same transaction',
      'Block the write until the read side catches up',
    ],
    correctAnswer: 0,
    explanation: 'Eventual consistency is inherent in CQRS. Common strategies include: returning a consistency token with the write response so the client can wait for it on the read side; reading directly from the aggregate for the user\'s own writes; optimistic UI updates that assume success; or subscription-based notifications when the projection catches up.',
    tags: ['cqrs', 'eventual-consistency', 'architecture'],
  },
  {
    id: 'scala-akka-l4-014',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'CQRS',
    question: 'When implementing CQRS with Akka Persistence and Akka Projections, what determines the parallelism of projection processing?',
    options: [
      'The size of the database connection pool',
      'The number of CPU cores on the machine',
      'The number of persistence IDs in the system',
      'The number of event tags used for tagging persisted events, as each tag creates an independent projection stream',
    ],
    correctAnswer: 3,
    explanation: 'Akka Projections processes events grouped by tags. If events are tagged with N distinct tags (e.g., "order-0" through "order-9"), N projection instances can run in parallel, each consuming one tag. The ShardedDaemonProcess is typically used to distribute these tag-based projections across cluster nodes. More tags = more parallelism but also more offset tracking overhead.',
    codeSnippet: 'ShardedDaemonProcess(system).init(\n  name = "OrderProjection",\n  numberOfInstances = 10, // matches number of tags\n  index => ProjectionBehavior(\n    createProjectionFor(system, index)\n  ),\n  ShardedDaemonProcessSettings(system),\n  Some(ProjectionBehavior.Stop)\n)',
    tags: ['cqrs', 'projections', 'parallelism'],
  },
  {
    id: 'scala-akka-l4-015',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the correct way to create a custom GraphStage with an asynchronous side-channel input in Akka Streams?',
    options: [
      'Use getAsyncCallback to safely inject elements from outside the stage into the stream processing logic',
      'Use ActorRef.tell from within the onPush handler',
      'Use a regular Inlet and connect it in the graph',
      'Use a Future inside the stage handler',
    ],
    correctAnswer: 0,
    explanation: 'getAsyncCallback returns an AsyncCallback[T] that can be safely invoked from any thread. When invoked, it schedules the callback to run inside the stage\'s execution context, preserving the single-threaded illusion of the GraphStage. This is the only safe way to inject external events (timer callbacks, completion of futures, actor messages) into a running stage.',
    codeSnippet: 'class MyStage extends GraphStage[FlowShape[In, Out]] {\n  override def createLogic(attrs: Attributes) = new GraphStageLogic(shape) {\n    val callback = getAsyncCallback[ExternalEvent] { event =>\n      // Safe to access stage state here\n      emit(out, transform(event))\n    }\n    // Pass callback to external system\n    override def preStart(): Unit = externalSystem.register(callback.invoke)\n  }\n}',
    tags: ['streams', 'graph-stage', 'async-callback'],
  },
  {
    id: 'scala-akka-l4-016',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'In Akka Streams, what is the difference between buffer with OverflowStrategy.backpressure and the default backpressure behavior?',
    options: [
      'The buffer stage adds an explicit bounded buffer that absorbs bursts before backpressure propagates upstream, while default backpressure signals demand immediately through each stage',
      'Default backpressure uses TCP flow control while buffer uses in-memory storage',
      'The buffer stage removes backpressure entirely',
      'There is no difference; both behave identically',
    ],
    correctAnswer: 0,
    explanation: 'Without an explicit buffer, demand propagates directly through the stream: a slow downstream immediately slows the upstream. Adding a buffer(N, OverflowStrategy.backpressure) introduces an N-element buffer that absorbs temporary bursts. Upstream is only backpressured when the buffer is full. This smooths out throughput variations but adds memory overhead and latency.',
    codeSnippet: 'source\n  .buffer(1000, OverflowStrategy.backpressure)\n  .async // async boundary increases the effect\n  .via(slowProcessing)\n  .runWith(Sink.ignore)',
    tags: ['streams', 'backpressure', 'buffer'],
  },
  {
    id: 'scala-akka-l4-017',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the purpose of the .async boundary in Akka Streams and what is its impact on performance?',
    options: [
      'It converts synchronous sources to asynchronous ones',
      'It introduces an asynchronous boundary that runs stages on different actors/threads, enabling pipelining but adding overhead from async messaging between stages',
      'It makes all operations asynchronous with no performance impact',
      'It enables parallel execution of all downstream stages',
    ],
    correctAnswer: 1,
    explanation: 'By default, connected stages in Akka Streams execute synchronously (fused) on the same thread. The .async boundary splits the stream into separate async regions, each running on its own actor. This enables pipelining (stage N processes element K while stage N+1 processes element K-1) but adds overhead from mailbox-based messaging between regions. Use it when stages have different processing speeds.',
    codeSnippet: 'source\n  .via(cpuIntensiveFlow).async  // runs on one actor\n  .via(ioFlow).async            // runs on another actor\n  .via(lightTransform)          // fused with ioFlow\n  .runWith(sink)',
    tags: ['streams', 'async-boundary', 'fusion'],
  },
  {
    id: 'scala-akka-l4-018',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the mapAsync vs mapAsyncUnordered trade-off in Akka Streams?',
    options: [
      'mapAsyncUnordered may have higher throughput because it emits results as they complete rather than waiting for in-order completion, but output order is non-deterministic',
      'mapAsyncUnordered uses more memory because it buffers all results',
      'mapAsync is faster because it preserves order',
      'mapAsync works with Futures while mapAsyncUnordered works with callbacks',
    ],
    correctAnswer: 0,
    explanation: 'mapAsync(n)(f) runs up to n Futures concurrently but emits results in the same order as inputs, meaning a slow Future blocks emission of faster subsequent ones. mapAsyncUnordered(n)(f) also runs n Futures concurrently but emits each result as soon as its Future completes, regardless of input order. This yields higher throughput when processing times vary, at the cost of non-deterministic ordering.',
    codeSnippet: '// Ordered: preserves input order\nsource.mapAsync(4)(id => fetchFromDb(id))\n\n// Unordered: faster but output order varies\nsource.mapAsyncUnordered(4)(id => fetchFromDb(id))',
    tags: ['streams', 'mapAsync', 'parallelism'],
  },
  {
    id: 'scala-akka-l4-019',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you implement a rate-limiting stage in Akka Streams that emits at most N elements per time window?',
    options: [
      'Use the throttle operator with a specified number of elements and time duration',
      'Use Thread.sleep inside a map stage',
      'Use a buffer with dropHead strategy',
      'Use mapAsync with a fixed delay Future',
    ],
    correctAnswer: 0,
    explanation: 'The throttle operator limits the stream to at most N elements per duration. It supports different modes: Shaping (delays elements to meet the rate) and Enforcing (fails the stream if upstream is too fast). It also supports a burst parameter for allowing short bursts above the sustained rate.',
    codeSnippet: 'source\n  .throttle(\n    elements = 100,\n    per = 1.second,\n    maximumBurst = 50,\n    mode = ThrottleMode.Shaping\n  )\n  .runWith(sink)',
    tags: ['streams', 'throttle', 'rate-limiting'],
  },
  {
    id: 'scala-akka-l4-020',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is a KillSwitch in Akka Streams and when should you use SharedKillSwitch vs UniqueKillSwitch?',
    options: [
      'SharedKillSwitch is for cluster-wide shutdown while UniqueKillSwitch is for local shutdown',
      'There is no practical difference between them',
      'UniqueKillSwitch controls a single materialized stream, while SharedKillSwitch can shut down multiple streams that share the same switch instance',
      'KillSwitch stops the JVM; SharedKillSwitch stops all streams in the application',
    ],
    correctAnswer: 2,
    explanation: 'A KillSwitch provides an external control to shut down a running stream. UniqueKillSwitch is materialized per-stream and controls only that stream. SharedKillSwitch is created independently and can be attached to multiple streams via viaMat; calling shutdown() on it terminates all streams using that switch. SharedKillSwitch is useful for coordinated shutdown of related streams.',
    codeSnippet: 'val sharedSwitch = KillSwitches.shared("my-switch")\n\nval stream1 = source1.via(sharedSwitch.flow).runWith(sink1)\nval stream2 = source2.via(sharedSwitch.flow).runWith(sink2)\n\n// Shuts down both streams\nsharedSwitch.shutdown()',
    tags: ['streams', 'kill-switch', 'lifecycle'],
  },
  {
    id: 'scala-akka-l4-021',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the Hub pattern in Akka Streams and what are BroadcastHub and MergeHub used for?',
    options: [
      'Hub is a cluster routing pattern unrelated to streams',
      'BroadcastHub and MergeHub are deprecated in favor of Kafka integration',
      'BroadcastHub allows dynamically attaching multiple consumers to a single producer, while MergeHub allows dynamically attaching multiple producers to a single consumer',
      'Hub is a central message broker; BroadcastHub sends messages to a broker, MergeHub receives from it',
    ],
    correctAnswer: 2,
    explanation: 'BroadcastHub.sink materializes a Source that can be consumed by multiple downstream consumers, each getting all elements (dynamic fan-out). MergeHub.source materializes a Sink that multiple upstream producers can write to (dynamic fan-in). Together they create a dynamic pub-sub bus within Akka Streams. New producers/consumers can attach at runtime.',
    codeSnippet: 'val (sink, source) = MergeHub.source[String]\n  .via(processingFlow)\n  .toMat(BroadcastHub.sink[String])(Keep.both)\n  .run()\n\n// Dynamic producers\nproducer1.runWith(sink)\nproducer2.runWith(sink)\n// Dynamic consumers\nsource.runWith(consumer1)\nsource.runWith(consumer2)',
    tags: ['streams', 'hub', 'dynamic-fan'],
  },
  {
    id: 'scala-akka-l4-022',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Backpressure',
    question: 'In Akka Streams, what happens when you connect a fast Source to a slow Sink through an operator with OverflowStrategy.dropHead?',
    options: [
      'Elements are dropped randomly',
      'The stream fails with a BufferOverflowException',
      'The oldest buffered element is dropped when the buffer is full, and upstream is never backpressured',
      'The newest element is dropped when the buffer is full',
    ],
    correctAnswer: 2,
    explanation: 'OverflowStrategy.dropHead drops the oldest element in the buffer when a new element arrives and the buffer is full. This means the upstream never experiences backpressure (it can always push). This is useful for scenarios where only the most recent data matters (e.g., sensor readings). dropTail drops the newest waiting element, dropBuffer clears the entire buffer, and dropNew drops the incoming element.',
    tags: ['streams', 'backpressure', 'overflow-strategy'],
  },
  {
    id: 'scala-akka-l4-023',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Backpressure',
    question: 'How does backpressure work across an Akka HTTP connection between a streaming response producer and a slow client?',
    options: [
      'TCP flow control propagates backpressure from the slow client through the Akka HTTP layer up to the source stream, automatically slowing the producer',
      'The server buffers the entire response before sending',
      'The connection is terminated if the client is too slow',
      'The server drops data that the client cannot consume',
    ],
    correctAnswer: 0,
    explanation: 'Akka HTTP is built on Akka Streams, so backpressure flows end-to-end. When a client reads slowly, the TCP send buffer fills up, which causes the Akka HTTP layer to stop pulling from the response entity Source. This propagates upstream through the stream, eventually slowing or pausing the producer. This prevents the server from overwhelming slow clients or running out of memory.',
    tags: ['backpressure', 'http', 'tcp-flow-control'],
  },
  {
    id: 'scala-akka-l4-024',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka gRPC',
    question: 'What is the key advantage of Akka gRPC\'s streaming support over traditional REST APIs?',
    options: [
      'Akka gRPC does not require serialization',
      'Akka gRPC is always faster than REST',
      'Akka gRPC works without a network connection',
      'Akka gRPC supports bidirectional streaming with backpressure over HTTP/2, allowing efficient real-time communication without polling',
    ],
    correctAnswer: 3,
    explanation: 'Akka gRPC leverages HTTP/2 multiplexing and Akka Streams to provide server streaming, client streaming, and bidirectional streaming RPCs with built-in backpressure. The Akka Streams integration means gRPC streaming responses are modeled as Source[T, NotUsed] with full backpressure semantics. This is more efficient than REST polling or WebSocket-based solutions for real-time data.',
    codeSnippet: '// Service definition (protobuf)\n// rpc StreamUpdates(Request) returns (stream Update) {}\n\n// Implementation with Akka Streams\noverride def streamUpdates(req: Request): Source[Update, NotUsed] =\n  Source\n    .tick(0.seconds, 1.second, ())\n    .map(_ => fetchLatestUpdate(req.id))\n    .takeWhile(_.nonEmpty)',
    tags: ['grpc', 'streaming', 'http2'],
  },
  {
    id: 'scala-akka-l4-025',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka gRPC',
    question: 'How do you handle errors in Akka gRPC server streaming responses?',
    options: [
      'Return null elements to signal errors',
      'Throw exceptions in the stream; gRPC automatically converts them to status codes',
      'Errors are not possible in gRPC streaming',
      'Use GrpcServiceException with a specific Status code, or let the stream fail which maps to INTERNAL status',
    ],
    correctAnswer: 3,
    explanation: 'In Akka gRPC, you can signal errors by failing the Source with a GrpcServiceException containing a gRPC Status code (NOT_FOUND, PERMISSION_DENIED, etc.) and optional metadata. If the stream fails with any other exception, it is mapped to Status.INTERNAL. For per-element errors in a stream, you can use a wrapper message with a oneof for success/error.',
    codeSnippet: 'override def getItems(req: Request): Source[Item, NotUsed] = {\n  if (!authorized(req))\n    Source.failed(\n      new GrpcServiceException(\n        Status.PERMISSION_DENIED.withDescription("Not authorized")\n      )\n    )\n  else\n    itemSource(req.id)\n}',
    tags: ['grpc', 'error-handling', 'status-codes'],
  },
  {
    id: 'scala-akka-l4-026',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Distributed Data',
    question: 'What is Akka Distributed Data (ddata) and when should you use it instead of Akka Persistence?',
    options: [
      'ddata is a persistence layer optimized for writes',
      'ddata provides eventually consistent replicated data using CRDTs, suitable for data that can be merged without conflicts, unlike Persistence which provides strong consistency per entity',
      'ddata is a caching layer built on top of Akka Persistence',
      'ddata replaces Akka Persistence in all cases',
    ],
    correctAnswer: 1,
    explanation: 'Akka Distributed Data replicates data across cluster nodes using Conflict-free Replicated Data Types (CRDTs). Data is eventually consistent and merges automatically without coordination. It is ideal for shared cluster state like configuration, counters, or sets where conflicts can be resolved by the CRDT merge function. Akka Persistence provides strong consistency within a single entity via event sourcing.',
    codeSnippet: 'val replicator = DistributedData(system).replicator\nval key = GCounterKey("visitor-count")\n\n// Update on any node\nreplicator ! Update(key, GCounter.empty, WriteLocal)(\n  _.increment(node, 1)\n)\n\n// Read from any node\nreplicator ! Get(key, ReadMajority(5.seconds))',
    tags: ['distributed-data', 'crdt', 'eventual-consistency'],
  },
  {
    id: 'scala-akka-l4-027',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Distributed Data',
    question: 'What consistency levels are available when reading from Akka Distributed Data and what are their trade-offs?',
    options: [
      'ReadLocal (fast, may be stale), ReadMajority (consistent across majority, slower), ReadAll (fully consistent, slowest, fails if any node is down)',
      'Only eventual consistency is available',
      'Strong consistency and eventual consistency only',
      'ReadLocal and ReadCluster only',
    ],
    correctAnswer: 0,
    explanation: 'ReadLocal reads only from the local replica (fastest, but may return stale data). ReadMajority requires responses from a majority of nodes (provides stronger consistency guarantees). ReadAll requires all nodes to respond (strongest consistency but fails if any replica node is unreachable). Similarly, write consistency levels (WriteLocal, WriteMajority, WriteAll) control how many nodes must acknowledge a write.',
    tags: ['distributed-data', 'consistency', 'read-levels'],
  },
  {
    id: 'scala-akka-l4-028',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Split Brain Resolver',
    question: 'What is the "split brain" problem in Akka Cluster and why is a Split Brain Resolver (SBR) essential for production?',
    options: [
      'Split brain only affects clusters with more than 100 nodes',
      'Split brain occurs when a network partition divides the cluster into subclusters that each believe they are the full cluster; without SBR, both sides continue operating, potentially causing data corruption',
      'Split brain is when a node has too many actors; SBR redistributes them',
      'Split brain is a performance issue where the cluster is slow; SBR improves performance',
    ],
    correctAnswer: 1,
    explanation: 'A network partition can split a cluster into two (or more) groups that cannot communicate. Without SBR, both sides may mark the other as unreachable and continue operating independently, leading to duplicate singletons, duplicate shards, and data inconsistency. SBR resolves this by automatically downing one side based on a configurable strategy (keep-majority, keep-oldest, keep-referee, down-all, or lease-based).',
    tags: ['split-brain', 'cluster', 'network-partition'],
  },
  {
    id: 'scala-akka-l4-029',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Split Brain Resolver',
    question: 'Which Split Brain Resolver strategy is most appropriate for a cluster with an even number of nodes hosting Cluster Sharding?',
    options: [
      'static-quorum with a configured quorum size, because keep-majority cannot break ties in even-numbered clusters',
      'keep-oldest, because the oldest node is most stable',
      'keep-majority, because it always picks the larger partition',
      'down-all, because it is the safest option',
    ],
    correctAnswer: 0,
    explanation: 'With an even number of nodes, a partition can split the cluster exactly in half. keep-majority would down both sides since neither has a majority. static-quorum requires a specific number of nodes (typically N/2+1) to be present, ensuring exactly one partition survives. Alternatively, lease-based SBR using a distributed lock avoids this issue entirely.',
    tags: ['split-brain', 'quorum', 'cluster-strategy'],
  },
  {
    id: 'scala-akka-l4-030',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Projections',
    question: 'What is the difference between at-least-once and exactly-once delivery semantics in Akka Projections?',
    options: [
      'Exactly-once prevents duplicate messages at the network level',
      'At-least-once may deliver duplicate events if the handler crashes after processing but before offset commit; exactly-once stores the offset and handler side-effect in the same transaction',
      'At-least-once is faster; exactly-once is slower but both guarantee the same outcomes',
      'At-least-once requires idempotent handlers; exactly-once does not require any special handler logic',
    ],
    correctAnswer: 1,
    explanation: 'In at-least-once mode, the offset is committed after the handler processes the event. If the handler succeeds but the process crashes before offset commit, the event will be replayed on restart. In exactly-once mode (e.g., JdbcProjection.exactlyOnce), the handler\'s side-effect and the offset update happen in the same database transaction, ensuring they either both succeed or both fail.',
    codeSnippet: '// At-least-once: offset saved after handler\nJdbcProjection.atLeastOnce(\n  projectionId, sourceProvider,\n  handler = () => new MyHandler(),\n  sessionFactory\n)\n\n// Exactly-once: offset saved in same transaction\nJdbcProjection.exactlyOnce(\n  projectionId, sourceProvider,\n  handler = () => new MyTransactionalHandler(),\n  sessionFactory\n)',
    tags: ['projections', 'delivery-semantics', 'offset-management'],
  },
  {
    id: 'scala-akka-l4-031',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Projections',
    question: 'How do you ensure that an Akka Projection can be safely restarted and resumed from where it left off?',
    options: [
      'Projections always start from the beginning of the event stream',
      'The projection framework automatically persists offsets to an offset store (database), and on restart it queries the last committed offset to resume from that position',
      'Store the projection state in an actor\'s memory',
      'You must manually track the offset in application code',
    ],
    correctAnswer: 1,
    explanation: 'Akka Projections automatically manages offsets. Each projection has a ProjectionId, and the framework persists the latest processed offset to a configurable offset store (JDBC, Cassandra, etc.). On restart, it queries the offset store for the last committed offset and resumes reading events from that position. This makes projections crash-resilient by default.',
    tags: ['projections', 'offset-store', 'crash-recovery'],
  },
  {
    id: 'scala-akka-l4-032',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'What is the impact of the akka.actor.default-dispatcher configuration on Akka application performance?',
    options: [
      'It only affects logging performance',
      'It has no performance impact; Akka automatically tunes it',
      'It configures the thread pool used by all actors by default; misconfiguring it (e.g., too few threads with blocking actors) can cause thread starvation and deadlock-like behavior',
      'It only affects remote messaging performance',
    ],
    correctAnswer: 2,
    explanation: 'The default dispatcher is the fork-join-pool used by all actors unless overridden. If actors perform blocking I/O on this dispatcher, they can starve other actors of threads. Best practice is to use a separate dispatcher for blocking operations and keep the default dispatcher for non-blocking message processing. Key settings include parallelism-min, parallelism-max, and parallelism-factor.',
    codeSnippet: '# application.conf\nblocking-dispatcher {\n  type = Dispatcher\n  executor = "thread-pool-executor"\n  thread-pool-executor {\n    fixed-pool-size = 32\n  }\n  throughput = 1\n}\n\n# Usage in code\ncontext.spawn(\n  blockingBehavior,\n  "blocking-actor",\n  DispatcherSelector.fromConfig("blocking-dispatcher")\n)',
    tags: ['performance', 'dispatcher', 'thread-pool'],
  },
  {
    id: 'scala-akka-l4-033',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'What does the throughput setting on an Akka dispatcher control?',
    options: [
      'The number of messages an actor processes from its mailbox before yielding the thread to another actor',
      'The maximum number of messages the system can process per second',
      'The number of concurrent actors that can run simultaneously',
      'The maximum network throughput in bytes per second',
    ],
    correctAnswer: 0,
    explanation: 'The throughput setting (default 5) defines how many messages an actor will process from its mailbox in one batch before the thread is returned to the pool for other actors. Higher values improve throughput for busy actors but increase latency for other actors waiting for a thread. Lower values improve fairness but add scheduling overhead.',
    codeSnippet: '# Higher throughput for batch processing actors\nbatch-dispatcher {\n  type = Dispatcher\n  executor = "fork-join-executor"\n  throughput = 50  # process 50 messages before yielding\n}\n\n# Lower throughput for latency-sensitive actors\nlatency-dispatcher {\n  throughput = 1  # yield after every message\n}',
    tags: ['performance', 'dispatcher', 'throughput'],
  },
  {
    id: 'scala-akka-l4-034',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'How should you handle blocking I/O operations in an Akka application to avoid degrading performance?',
    options: [
      'Increase the default dispatcher thread count to compensate',
      'Wrap blocking calls in a Future running on a dedicated blocking dispatcher, and pipe the result back to the actor',
      'Use Thread.sleep to throttle the blocking operations',
      'Use Await.result in the actor\'s receive handler',
    ],
    correctAnswer: 1,
    explanation: 'Blocking operations on the default dispatcher starve other actors of threads. The solution is to run blocking code on a separate, bounded thread pool (blocking dispatcher) using Future { blockingCall() }(blockingDispatcher) and pipe the result back to the actor. Alternatively, use non-blocking APIs. Never increase the default dispatcher to compensate for blocking, as it just delays the problem.',
    codeSnippet: 'implicit val blockingEc: ExecutionContext =\n  context.system.dispatchers.lookup("blocking-dispatcher")\n\ndef active(state: State): Behavior[Command] =\n  Behaviors.receiveMessage {\n    case FetchData(replyTo) =>\n      val futureResult = Future {\n        // Blocking I/O on dedicated dispatcher\n        database.queryBlocking(state.id)\n      }(blockingEc)\n      context.pipeToSelf(futureResult) {\n        case Success(data) => DataFetched(data, replyTo)\n        case Failure(ex) => FetchFailed(ex, replyTo)\n      }\n      Behaviors.same\n  }',
    tags: ['performance', 'blocking', 'dispatcher'],
  },
  {
    id: 'scala-akka-l4-035',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the difference between context.ask and the ask pattern (?) in Akka Typed?',
    options: [
      'context.ask is deprecated in favor of the ask pattern',
      'The ask pattern works only with untyped actors',
      'They are identical in behavior',
      'context.ask is used inside an actor behavior and adapts the response to the actor\'s protocol type, while the ask pattern (?) returns a Future and is used from outside actors',
    ],
    correctAnswer: 3,
    explanation: 'context.ask[Res](target, createRequest)(mapResponse) sends a message to the target and maps the response to the asking actor\'s message type. It is the correct way to ask from within a behavior. The ask pattern (ActorRef ? message) is used from outside the actor system (e.g., from HTTP routes) and returns a Future[Response]. Using Future-based ask inside an actor is an anti-pattern because it breaks the single-threaded actor model.',
    codeSnippet: '// Inside actor: context.ask\ncontext.ask[StatusResponse](targetRef, ref => GetStatus(ref)) {\n  case Success(status) => WrappedStatus(status)\n  case Failure(ex) => StatusFailed(ex)\n}\n\n// Outside actor: ask pattern\nimplicit val timeout: Timeout = 3.seconds\nval future: Future[StatusResponse] = targetRef.ask(ref => GetStatus(ref))',
    tags: ['typed-actors', 'ask-pattern', 'interaction'],
  },
  {
    id: 'scala-akka-l4-036',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the Receptionist pattern in Akka Typed and when is it needed?',
    options: [
      'It is a service discovery mechanism that allows actors to register themselves under a ServiceKey and other actors to discover them, essential in clusters where ActorRefs cannot be directly shared',
      'It is a load balancing pattern for distributing messages',
      'It is used only for testing actor interactions',
      'It is a UI pattern for building actor-based web applications',
    ],
    correctAnswer: 0,
    explanation: 'In Akka Typed, ActorRefs are not serializable by path alone. The Receptionist allows actors to register themselves under a ServiceKey, and other actors can subscribe to or query for registered actors. In a cluster, the Receptionist is cluster-aware and aggregates registrations across nodes. This replaces the classic actorSelection/actorFor pattern.',
    codeSnippet: 'val serviceKey = ServiceKey[MyProtocol]("my-service")\n\n// Register\ncontext.system.receptionist ! Receptionist.Register(serviceKey, context.self)\n\n// Discover\ncontext.system.receptionist ! Receptionist.Subscribe(\n  serviceKey,\n  listingAdapter\n)',
    tags: ['typed-actors', 'receptionist', 'service-discovery'],
  },
  {
    id: 'scala-akka-l4-037',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the purpose of Behaviors.withTimers in Akka Typed?',
    options: [
      'It adds timeout behavior to all incoming messages',
      'It measures the time each message takes to process',
      'It creates a timer service that other actors can use',
      'It provides a TimerScheduler that can schedule messages to be sent to the actor itself at fixed intervals or after delays, integrated with the actor lifecycle',
    ],
    correctAnswer: 3,
    explanation: 'Behaviors.withTimers provides a TimerScheduler[T] scoped to the actor. You can start single or repeating timers that send messages to the actor\'s own mailbox. Timers are automatically cancelled when the actor stops. This is safer than using system.scheduler directly because the timer lifecycle is tied to the actor lifecycle, avoiding messages sent to dead actors.',
    codeSnippet: 'Behaviors.withTimers[Command] { timers =>\n  timers.startTimerWithFixedDelay(\n    key = "refresh",\n    msg = Refresh,\n    delay = 30.seconds\n  )\n  Behaviors.receiveMessage {\n    case Refresh =>\n      refreshData()\n      Behaviors.same\n    case Stop =>\n      timers.cancel("refresh")\n      Behaviors.stopped\n  }\n}',
    tags: ['typed-actors', 'timers', 'scheduling'],
  },
  {
    id: 'scala-akka-l4-038',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What is passivation in Akka Cluster Sharding and how does it affect memory usage?',
    options: [
      'Passivation permanently deletes entities that are no longer needed',
      'Passivation moves entities to disk for persistence',
      'Passivation stops idle entity actors after a configurable timeout to free memory, and they are transparently restarted when a new message arrives for them',
      'Passivation suspends entity processing during rebalancing',
    ],
    correctAnswer: 2,
    explanation: 'Entity passivation stops entity actors that have been idle for a configurable period (akka.cluster.sharding.passivation). When a new message arrives for a passivated entity, the shard transparently restarts it (replaying events if event-sourced). This bounds memory usage proportional to active entities rather than total entities. Strategies include idle-timeout, least-recently-used, and composite policies.',
    codeSnippet: 'Entity(TypeKey)(ctx => MyEntity(ctx.entityId))\n  .withSettings(\n    ClusterShardingSettings(system)\n      .withPassivationStrategy(\n        ClusterShardingSettings.PassivationStrategySettings\n          .defaults\n          .withIdleEntityPassivation(timeout = 2.minutes)\n      )\n  )',
    tags: ['cluster-sharding', 'passivation', 'memory'],
  },
  {
    id: 'scala-akka-l4-039',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'How do you implement multi-region Akka Cluster Sharding where entities should be local to specific data centers?',
    options: [
      'Use Akka Cluster Sharding with a custom router that sends messages to specific IPs',
      'Use Akka Multi-DC Cluster with replicated sharding, where each DC has its own shard region and entities are replicated across DCs using Replicated Event Sourcing',
      'Use a single cluster spanning all data centers',
      'Deploy separate independent clusters and use HTTP for cross-DC communication',
    ],
    correctAnswer: 1,
    explanation: 'Akka Multi-DC Cluster allows running sharding across data centers. With Replicated Event Sourcing, each entity has a replica in each DC. Events are replicated asynchronously via the journal. Each DC can process commands locally against its replica, with conflicts resolved by the event handler\'s CRDT-like merge logic. This provides low-latency local access with eventual cross-DC consistency.',
    codeSnippet: 'ReplicatedEventSourcing.commonJournalConfig(\n  ReplicationId("MyEntity", entityId, selfDc),\n  allReplicas = Set(\n    Replica("dc-east"), Replica("dc-west")\n  ),\n  queryPluginId = "jdbc-read-journal"\n) { replicationContext =>\n  EventSourcedBehavior[Command, Event, State](\n    replicationContext.persistenceId, ...)\n}',
    tags: ['cluster-sharding', 'multi-dc', 'replication'],
  },
  {
    id: 'scala-akka-l4-040',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'What is the difference between deleteMessages and deleteEvents in Akka Persistence and when should you use each?',
    options: [
      'deleteMessages removes events from the journal up to a sequence number, used for compaction after snapshots; there is no separate deleteEvents method—the distinction is between event deletion and snapshot deletion',
      'They are synonymous operations',
      'deleteMessages is for classic actors and deleteEvents is for typed actors',
      'deleteEvents removes persisted events while deleteMessages removes mailbox messages',
    ],
    correctAnswer: 0,
    explanation: 'In Akka Persistence, deleteMessages(toSequenceNr) marks journal events up to the given sequence number as deleted. This is typically used after a successful snapshot to bound journal growth. The events are logically deleted (some journal plugins physically delete them). There is no separate deleteEvents API; snapshot deletion is done via deleteSnapshot/deleteSnapshots. In Typed, this is managed via RetentionCriteria.',
    tags: ['persistence', 'compaction', 'journal'],
  },
  {
    id: 'scala-akka-l4-041',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'How does Akka Persistence handle concurrent writes to the same persistenceId?',
    options: [
      'Concurrent writes are automatically merged using CRDTs',
      'It uses distributed locks to serialize writes',
      'It uses optimistic locking and retries on conflict',
      'Multiple actors with the same persistenceId will corrupt the event log; the framework relies on single-writer principle enforced by Cluster Sharding or application-level uniqueness',
    ],
    correctAnswer: 3,
    explanation: 'Akka Persistence assumes a single-writer per persistenceId. If two actors write events with the same persistenceId concurrently, the journal may contain interleaved events leading to corruption. Cluster Sharding naturally enforces single-writer by ensuring each entity exists on exactly one node. Without Cluster Sharding, the application must ensure uniqueness through other means.',
    tags: ['persistence', 'single-writer', 'consistency'],
  },
  {
    id: 'scala-akka-l4-042',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Event Sourcing',
    question: 'What is the Tagging pattern in Akka Persistence and why is it important for projections?',
    options: [
      'Tags determine which serializer to use for events',
      'Tags are used to prioritize events in the journal',
      'Tags categorize events into streams that projections can consume independently, enabling parallel processing and selective event consumption',
      'Tags are metadata labels for actors used in monitoring',
    ],
    correctAnswer: 2,
    explanation: 'Event tagging assigns one or more string tags to persisted events. Projections use eventsByTag queries to consume events with a specific tag. By distributing events across multiple tags (e.g., using a hash of entityId modulo N), you enable parallel projection processing. Tags also allow building topic-specific projections that only consume relevant events.',
    codeSnippet: 'EventSourcedBehavior[Command, Event, State](\n  persistenceId, emptyState, commandHandler, eventHandler\n).withTagger { event =>\n  val tag = math.abs(event.entityId.hashCode % 10)\n  Set(s"order-tag-$tag\")\n}',
    tags: ['event-sourcing', 'tagging', 'projections'],
  },
  {
    id: 'scala-akka-l4-043',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'CQRS',
    question: 'In a CQRS setup with Akka, what is the recommended approach for building a read model that requires data from multiple aggregate types?',
    options: [
      'Create a projection that consumes tagged events from multiple aggregate types and builds a denormalized read model',
      'Store all aggregates in a single event stream',
      'Query multiple aggregates synchronously from the read side',
      'Join the event journals of different aggregates in SQL',
    ],
    correctAnswer: 0,
    explanation: 'A projection can consume events from multiple event streams (by tag) and combine them into a denormalized read model optimized for specific queries. This is one of the main benefits of CQRS: the read model schema is independent of the write model. The projection handler processes events from different aggregates and updates a joined/denormalized view.',
    tags: ['cqrs', 'read-model', 'denormalization'],
  },
  {
    id: 'scala-akka-l4-044',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you implement exactly-once processing semantics in an Akka Streams pipeline that writes to an external database?',
    options: [
      'Use idempotent writes with a deduplication key (e.g., offset or event ID) stored in the same transaction as the write, combined with at-least-once source delivery',
      'Use a two-phase commit protocol between the stream and database',
      'Use the built-in exactlyOnce operator',
      'Akka Streams guarantees exactly-once by default',
    ],
    correctAnswer: 0,
    explanation: 'Akka Streams itself provides at-most-once or at-least-once delivery. For exactly-once semantics with external systems, you need idempotent writes: store a deduplication key (like the Kafka offset or event sequence number) alongside the data in the same database transaction. On retry, check if the key already exists to skip duplicate processing. This is the transactional outbox pattern.',
    codeSnippet: 'source.mapAsync(1) { event =>\n  db.transactionally {\n    for {\n      exists <- db.checkProcessed(event.id)\n      _ <- if (!exists) {\n        db.writeResult(event.data) *>\n        db.markProcessed(event.id)\n      } else {\n        DBIO.successful(())\n      }\n    } yield Done\n  }\n}',
    tags: ['streams', 'exactly-once', 'idempotency'],
  },
  {
    id: 'scala-akka-l4-045',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the difference between Source.queue and Source.actorRef in Akka Streams?',
    options: [
      'Source.queue is for typed actors and Source.actorRef is for classic actors',
      'Source.queue provides a materialized SourceQueue with offer/complete methods and backpressure support, while Source.actorRef provides an ActorRef to send messages to with overflow strategies but no backpressure signal to the sender',
      'Source.actorRef supports backpressure while Source.queue does not',
      'They are identical in functionality',
    ],
    correctAnswer: 1,
    explanation: 'Source.queue materializes a SourceQueue that returns a Future[QueueOfferResult] on offer(), telling the caller whether the element was enqueued, dropped, or the queue is full (enabling the caller to implement backpressure). Source.actorRef materializes an ActorRef that elements can be sent to via tell (!), but since tell is fire-and-forget, there is no backpressure feedback to the sender.',
    codeSnippet: '// Source.queue with backpressure feedback\nval (queue, done) = Source.queue[Int](bufferSize = 100)\n  .toMat(Sink.foreach(println))(Keep.both).run()\nqueue.offer(42).map {\n  case QueueOfferResult.Enqueued => "ok"\n  case QueueOfferResult.Dropped => "dropped"\n}\n\n// Source.actorRef without backpressure\nval actorRef = Source.actorRef[Int](\n  bufferSize = 100,\n  OverflowStrategy.dropHead\n).to(Sink.foreach(println)).run()',
    tags: ['streams', 'source-queue', 'materialization'],
  },
  {
    id: 'scala-akka-l4-046',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the substream pattern using groupBy in Akka Streams and what are the pitfalls?',
    options: [
      'groupBy creates parallel streams with no limitations',
      'groupBy creates actor-per-group for parallel processing',
      'groupBy is a batch operator that collects elements into groups',
      'groupBy splits a stream into substreams by a key function, with a maximum number of substreams; exceeding maxSubstreams fails the stream, and each substream must be consumed (merged back or sinked) to avoid deadlock',
    ],
    correctAnswer: 3,
    explanation: 'groupBy(maxSubstreams, keyFn) creates a SubFlow where elements with the same key go to the same substream. The maxSubstreams parameter is a hard limit: exceeding it fails the stream. Each substream must eventually be consumed (via mergeSubstreams, mergeSubstreamsWithParallelism, or a Sink). Unclosed substreams backpressure the entire flow. Substreams are closed when the parent completes or via splitWhen/splitAfter.',
    codeSnippet: 'source\n  .groupBy(maxSubstreams = 100, _.category)\n  .map(transform)\n  .reduce(_ merge _)  // aggregate per substream\n  .mergeSubstreamsWithParallelism(10)\n  .runWith(Sink.seq)',
    tags: ['streams', 'substreams', 'groupBy'],
  },
  {
    id: 'scala-akka-l4-047',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the EventSourcedBehavior.withEnforcedReplies pattern and why should you use it?',
    options: [
      'It enforces that replies are sent in order',
      'It is a compile-time check that ensures every command handler returns a ReplyEffect, preventing bugs where the actor forgets to reply to the sender',
      'It forces all events to be replicated before the actor responds',
      'It enables automatic retry of failed replies',
    ],
    correctAnswer: 1,
    explanation: 'EventSourcedBehavior.withEnforcedReplies changes the command handler return type from Effect to ReplyEffect, which forces the developer to include a reply in every code path. Without this, it is easy to accidentally forget to reply to the sender, causing the ask timeout to fire instead of getting a response. This is a compile-time safety mechanism.',
    codeSnippet: 'EventSourcedBehavior.withEnforcedReplies[\n  Command, Event, State\n](\n  persistenceId, emptyState,\n  commandHandler = (state, cmd) => cmd match {\n    case AddItem(item, replyTo) =>\n      Effect.persist(ItemAdded(item))\n        .thenReply(replyTo)(_ => Accepted)\n    case GetItems(replyTo) =>\n      Effect.reply(replyTo)(state.items)\n    // Compiler error if any case lacks a reply!\n  },\n  eventHandler\n)',
    tags: ['typed-actors', 'persistence', 'enforced-replies'],
  },
  {
    id: 'scala-akka-l4-048',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What is the impact of the number-of-shards setting in Akka Cluster Sharding?',
    options: [
      'It determines the number of nodes in the cluster',
      'It limits the number of entities per node',
      'It is automatically adjusted based on cluster size',
      'It sets a fixed maximum number of shards; more shards allow finer-grained rebalancing but increase coordination overhead, and it cannot be changed after deployment without migration',
    ],
    correctAnswer: 3,
    explanation: 'number-of-shards determines how many shard buckets exist. Entity IDs are mapped to shards via hash. More shards (e.g., 10x the max cluster size) allow finer-grained distribution and smoother rebalancing. Too few shards cause uneven distribution; too many increase ShardCoordinator overhead. Critically, this value cannot be changed after deployment without a full data migration because it changes the entity-to-shard mapping.',
    tags: ['cluster-sharding', 'configuration', 'capacity-planning'],
  },
  {
    id: 'scala-akka-l4-049',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you implement a custom GraphStage that acts as a stateful filter, emitting only elements that differ from the previous one?',
    options: [
      'Use a var inside a regular map stage',
      'Use groupBy to group identical elements',
      'Use statefulMapConcat which provides a simpler API for stateful stream transformations',
      'Use scan followed by filter',
    ],
    correctAnswer: 2,
    explanation: 'statefulMapConcat(() => { var prev: Option[T] = None; elem => ... }) provides a closure over mutable state that is safe within the stream execution model. It is simpler than writing a full custom GraphStage. For each element, the function returns an Iterable: empty to filter, or List(elem) to emit. The state is scoped to one materialization.',
    codeSnippet: 'source.statefulMapConcat { () =>\n  var previous: Option[Int] = None\n  element => {\n    val emit = previous match {\n      case Some(prev) if prev == element => Nil\n      case _ => List(element)\n    }\n    previous = Some(element)\n    emit\n  }\n}',
    tags: ['streams', 'stateful', 'deduplication'],
  },
  {
    id: 'scala-akka-l4-050',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the RestartSource/RestartFlow/RestartSink pattern in Akka Streams?',
    options: [
      'It restarts the JVM when a stream fails',
      'It wraps a Source/Flow/Sink with automatic restart logic using exponential backoff, recreating the wrapped stage when it fails or completes',
      'It provides checkpointing for stream recovery',
      'It sends a restart signal to all connected streams',
    ],
    correctAnswer: 1,
    explanation: 'RestartSource.withBackoff wraps a Source factory. If the inner Source fails or completes, it is recreated after a backoff delay with exponential increase. This is useful for resilient connections (e.g., restarting a Kafka consumer or WebSocket connection on failure). The backoff parameters include minBackoff, maxBackoff, and randomFactor to prevent thundering herd.',
    codeSnippet: 'val restartSource = RestartSource.withBackoff(\n  RestartSettings(\n    minBackoff = 1.second,\n    maxBackoff = 30.seconds,\n    randomFactor = 0.2\n  ).withMaxRestarts(20, 5.minutes)\n) { () =>\n  // Factory: creates a new Source on each restart\n  Source.fromPublisher(kafkaConsumer.subscribe("topic"))\n}',
    tags: ['streams', 'restart', 'resilience'],
  },
  {
    id: 'scala-akka-l4-051',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'What is the role of Akka\'s internal-dispatcher and why should you never run user code on it?',
    options: [
      'It is a low-priority dispatcher for background tasks',
      'It is the same as the default-dispatcher',
      'It is used for database operations',
      'It is a dedicated dispatcher for Akka internal tasks like remoting and cluster protocol; running user code on it can starve critical system operations and cause cluster instability',
    ],
    correctAnswer: 3,
    explanation: 'Akka uses internal dispatchers for critical system operations: cluster heartbeats, remote message processing, and system-level actors. If user code (especially blocking) runs on these dispatchers, it can delay heartbeats causing false failure detection, or delay remote message delivery. Always use the default-dispatcher or custom dispatchers for application actors.',
    tags: ['performance', 'internal-dispatcher', 'cluster'],
  },
  {
    id: 'scala-akka-l4-052',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the difference between context.pipeToSelf and context.ask for integrating Futures with Akka Typed actors?',
    options: [
      'pipeToSelf converts a Future result to a message sent to the current actor; ask sends a message to another actor and converts the response to a local message. Both safely bridge async boundaries.',
      'pipeToSelf is for local Futures while ask is for remote Futures',
      'ask is deprecated in favor of pipeToSelf',
      'They are identical in behavior',
    ],
    correctAnswer: 0,
    explanation: 'context.pipeToSelf(future)(resultMapper) takes any Future and maps its Success/Failure to a message that is sent to the actor\'s mailbox. It is used to safely integrate external async operations. context.ask sends a message to another actor (including a replyTo ActorRef) and maps the response. Both prevent the common mistake of closing over mutable actor state in Future callbacks.',
    codeSnippet: '// pipeToSelf: integrate external Future\nval dbResult: Future[Data] = db.query(id)\ncontext.pipeToSelf(dbResult) {\n  case Success(data) => DataRetrieved(data)\n  case Failure(ex) => DataFailed(ex)\n}\n\n// ask: request-response with another actor\ncontext.ask[GetStatus, StatusResponse](otherActor, GetStatus(_)) {\n  case Success(resp) => WrappedResponse(resp)\n  case Failure(ex) => ResponseFailed(ex)\n}',
    tags: ['typed-actors', 'pipeToSelf', 'async-integration'],
  },
  {
    id: 'scala-akka-l4-053',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'What are the advantages of using Protobuf serialization over Java serialization for Akka Persistence events?',
    options: [
      'Protobuf provides schema evolution (add/remove fields), compact binary encoding, cross-language compatibility, and explicit versioning, while Java serialization is fragile, verbose, and breaks on class changes',
      'Protobuf is only needed for remote messaging, not persistence',
      'Java serialization supports more data types than Protobuf',
      'Protobuf is always faster at serialization',
    ],
    correctAnswer: 0,
    explanation: 'For event-sourced systems, events live forever in the journal. Java serialization breaks when classes change (field additions, renames, package moves). Protobuf uses field numbers for stable identification, supports adding/removing fields without breaking deserialization, produces compact binary output (10-30x smaller), and generates code in multiple languages. This makes it the standard choice for persistent events.',
    codeSnippet: '// protobuf schema with evolution\nmessage OrderPlaced {\n  string order_id = 1;\n  string customer_id = 2;\n  double amount = 3;\n  // Added in v2 - old events without this field deserialize fine\n  string currency = 4;\n  // Field 5 was removed - number is reserved\n  reserved 5;\n}',
    tags: ['persistence', 'serialization', 'protobuf'],
  },
  {
    id: 'scala-akka-l4-054',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Event Sourcing',
    question: 'What is the "upcasting" pattern in event-sourced systems?',
    options: [
      'Promoting events from one aggregate to another',
      'Transforming old event versions to newer versions during replay, allowing the event handler to only deal with the latest event schema version',
      'Serializing events to a more efficient format',
      'Converting events to a higher priority level',
    ],
    correctAnswer: 1,
    explanation: 'Upcasting transforms events stored in old formats to the current format during deserialization/replay. Instead of writing migration scripts against the journal, you transform events on-the-fly. The event handler only needs to handle the latest version. This is implemented via event adapters or custom serializers that check the manifest/version and transform as needed.',
    codeSnippet: 'class OrderEventSerializer extends SerializerWithStringManifest {\n  override def fromBinary(bytes: Array[Byte], manifest: String) =\n    manifest match {\n      case "OrderPlacedV1" =>\n        val v1 = OrderPlacedV1.parseFrom(bytes)\n        // Upcast V1 -> V2: add default currency\n        OrderPlaced(v1.orderId, v1.amount, currency = "USD")\n      case "OrderPlacedV2" =>\n        OrderPlaced.parseFrom(bytes)\n    }\n}',
    tags: ['event-sourcing', 'upcasting', 'schema-evolution'],
  },
  {
    id: 'scala-akka-l4-055',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'CQRS',
    question: 'How should you handle a situation where a CQRS projection falls far behind the event journal?',
    options: [
      'Delete the projection and recreate it from scratch',
      'Accept that the projection will always be behind',
      'Slow down the write side to let the projection catch up',
      'Increase the projection parallelism by adding more tags, rebuild the projection from its last known offset with a higher throughput configuration, or use a dedicated catch-up projection that bulk-processes events',
    ],
    correctAnswer: 3,
    explanation: 'When a projection falls behind, strategies include: increasing parallelism (more tag-based partitions), batch processing in the handler (grouping multiple events per database write), rebuilding from a known-good offset, or deploying a temporary high-throughput catch-up projection. Adding an index on the events table for the tag query can also help. Slowing the write side defeats the purpose of CQRS.',
    tags: ['cqrs', 'projections', 'operational'],
  },
  {
    id: 'scala-akka-l4-056',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka gRPC',
    question: 'How does Akka gRPC handle deadline propagation and why is it important?',
    options: [
      'Deadlines are handled by the load balancer, not the application',
      'Deadlines are not supported in Akka gRPC',
      'Deadlines only apply to unary calls, not streaming',
      'Akka gRPC propagates gRPC deadlines as timeouts through the call chain; if a deadline expires, the server-side stream is cancelled, preventing wasted work on requests the client has already given up on',
    ],
    correctAnswer: 3,
    explanation: 'gRPC deadlines represent an absolute point in time by which the RPC must complete. Akka gRPC respects these deadlines: when a client sets a deadline, the remaining time is propagated in the gRPC metadata. If the deadline expires, the server receives a cancellation signal, which in Akka Streams terms completes the stream with a DEADLINE_EXCEEDED error. This prevents servers from processing requests that no client is waiting for.',
    tags: ['grpc', 'deadlines', 'cancellation'],
  },
  {
    id: 'scala-akka-l4-057',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Distributed Data',
    question: 'What is an ORSet in Akka Distributed Data and how does it handle concurrent add/remove conflicts?',
    options: [
      'ORSet randomly resolves conflicts between add and remove',
      'ORSet prevents concurrent modifications by using distributed locks',
      'ORSet is an ordered set that maintains insertion order',
      'ORSet (Observed-Remove Set) is a CRDT where add wins over concurrent remove, because each element is tagged with a unique causal context that tracks which adds have been observed',
    ],
    correctAnswer: 3,
    explanation: 'ORSet (Observed-Remove Set) tracks a unique dot/tag for each add operation. When an element is removed, only the observed dots are removed. If a concurrent add on another node creates a new dot, the element survives because the remove only deletes dots it had observed. This gives "add-wins" semantics: concurrent add + remove results in the element being present.',
    codeSnippet: 'val key = ORSetKey[String]("my-set")\n\n// Node A adds "x"\nreplicator ! Update(key, ORSet.empty[String], WriteLocal)(\n  _ :+ "x"\n)\n\n// Node B concurrently removes "x" (only removes observed version)\nreplicator ! Update(key, ORSet.empty[String], WriteLocal)(\n  _ remove "x"\n)\n\n// After merge: "x" is present (add wins over concurrent remove)',
    tags: ['distributed-data', 'crdt', 'or-set'],
  },
  {
    id: 'scala-akka-l4-058',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'How do you implement the "circuit breaker" pattern with Akka Typed actors?',
    options: [
      'Use Akka\'s CircuitBreaker utility which tracks failures and automatically opens/half-opens/closes, preventing calls to a failing dependency for a cool-down period',
      'Use a watchdog actor that monitors all other actors',
      'Implement a custom supervision strategy that stops the actor on failure',
      'Use Thread.sleep to pause processing',
    ],
    correctAnswer: 0,
    explanation: 'Akka provides a CircuitBreaker that wraps calls to external services. It tracks consecutive failures; after a threshold, it "opens" and immediately fails subsequent calls without executing them. After a reset timeout, it enters "half-open" state and allows one trial call. On success, it closes; on failure, it re-opens. This prevents cascading failures and gives the failing service time to recover.',
    codeSnippet: 'val breaker = CircuitBreaker(\n  scheduler = context.system.scheduler,\n  maxFailures = 5,\n  callTimeout = 10.seconds,\n  resetTimeout = 1.minute\n)\n\ndef callExternalService(): Future[Response] =\n  breaker.withCircuitBreaker(\n    externalService.call()\n  ).recoverWith {\n    case _: CircuitBreakerOpenException =>\n      Future.successful(cachedFallback)\n  }',
    tags: ['typed-actors', 'circuit-breaker', 'resilience'],
  },
  {
    id: 'scala-akka-l4-059',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What is the difference between ClusterSharding.init and ClusterSharding.entityRefFor?',
    options: [
      'init creates entities while entityRefFor destroys them',
      'They are interchangeable methods',
      'init registers the entity type and starts the shard region on this node; entityRefFor returns a typed EntityRef for sending messages to a specific entity without knowing its location',
      'init is for classic actors and entityRefFor is for typed actors',
    ],
    correctAnswer: 2,
    explanation: 'ClusterSharding.init(Entity(...)) must be called once per entity type on each node that will host or proxy for that entity type. It sets up the shard region. entityRefFor(TypeKey, entityId) returns an EntityRef[Command] that transparently routes messages to the correct shard/node. You can call entityRefFor from any node, even one that only runs as a proxy (shard region proxy).',
    codeSnippet: '// On startup: register entity type\nval TypeKey = EntityTypeKey[Command]("Order")\nClusterSharding(system).init(\n  Entity(TypeKey)(ctx => OrderEntity(ctx.entityId))\n)\n\n// Anywhere: get reference to specific entity\nval orderRef: EntityRef[Command] =\n  ClusterSharding(system).entityRefFor(TypeKey, "order-123")\norderRef ! PlaceOrder(items)',
    tags: ['cluster-sharding', 'entity-ref', 'initialization'],
  },
  {
    id: 'scala-akka-l4-060',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'What is the stashing behavior during recovery in Akka Persistence Typed?',
    options: [
      'Messages received during recovery are automatically stashed and processed after recovery completes, without requiring explicit stash configuration',
      'Recovery blocks the actor from receiving any messages',
      'Messages received during recovery are dropped',
      'Messages received during recovery cause the actor to restart',
    ],
    correctAnswer: 0,
    explanation: 'During recovery (event replay and snapshot loading), the EventSourcedBehavior automatically stashes incoming commands. Once recovery completes, all stashed commands are processed in order. This ensures the actor has a consistent state before handling new commands. The stash buffer size during recovery is configurable via akka.persistence.typed.stash-capacity.',
    tags: ['persistence', 'recovery', 'stashing'],
  },
  {
    id: 'scala-akka-l4-061',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you implement a sliding window aggregation in Akka Streams?',
    options: [
      'Use a custom GraphStage with internal state',
      'Use the window operator which is built into Akka Streams',
      'Use the sliding operator to create overlapping windows of elements, then aggregate each window',
      'Use the groupBy operator with a time-based key',
    ],
    correctAnswer: 2,
    explanation: 'The sliding(n, step) operator creates a sliding window of size n that advances by step elements. Each emitted element is a Seq of n elements. You can then map over each window to compute aggregates (average, sum, etc.). For time-based windows, you would combine groupedWithin or use a custom stage.',
    codeSnippet: 'source\n  .sliding(n = 5, step = 1)  // 5-element sliding window\n  .map(window => window.sum / window.size.toDouble)  // moving average\n  .runWith(Sink.foreach(avg => println(s"Moving avg: $avg")))',
    tags: ['streams', 'sliding-window', 'aggregation'],
  },
  {
    id: 'scala-akka-l4-062',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'What is the impact of serialization on Akka Cluster performance and how can you optimize it?',
    options: [
      'Only snapshot serialization matters for performance',
      'Serialization affects every remote message; using efficient binary serializers (Protobuf, Kryo), enabling compression for large messages, and avoiding serialization of unnecessary data significantly reduces network bandwidth and latency',
      'Akka automatically optimizes serialization',
      'Serialization has no impact on cluster performance',
    ],
    correctAnswer: 1,
    explanation: 'Every message sent between cluster nodes is serialized. Java serialization is slow and verbose. Switching to Protobuf or Jackson (for JSON) can improve throughput 5-10x. For large payloads, enabling compression (akka.remote.artery.advanced.compression) reduces bandwidth. You should also avoid serializing unnecessary fields and ensure your serializer bindings cover all message types to avoid fallback to Java serialization.',
    codeSnippet: '# application.conf\nakka.actor.serialization-bindings {\n  "com.example.CborSerializable" = jackson-cbor\n  "com.example.ProtobufSerializable" = myProtobuf\n}\n\nakka.remote.artery.advanced {\n  compression {\n    actor-refs.max = 256\n    manifests.max = 256\n  }\n}',
    tags: ['performance', 'serialization', 'cluster'],
  },
  {
    id: 'scala-akka-l4-063',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the purpose of FlowWithContext and SourceWithContext in Akka Streams?',
    options: [
      'They are used for logging context propagation',
      'They provide execution context for async operations',
      'They propagate a context value (like a commit offset or metadata) alongside each element through the stream, ensuring the context stays paired with its element through transformations',
      'They provide dependency injection context to stream stages',
    ],
    correctAnswer: 2,
    explanation: 'FlowWithContext[In, CtxIn, Out, CtxOut, Mat] and SourceWithContext carry a context value alongside each stream element. This is essential for patterns like Kafka offset committing: the offset (context) must stay paired with its message through all transformations so that after processing, the correct offset is committed. Without this, tracking which offset corresponds to which processed element requires manual bookkeeping.',
    codeSnippet: 'val source: SourceWithContext[ConsumerRecord, CommittableOffset, _] =\n  Consumer.sourceWithOffsetContext(settings, Subscriptions.topics("topic"))\n\nsource\n  .map(record => processRecord(record))  // context (offset) flows through\n  .mapContext(offset => offset)           // transform context if needed\n  .toMat(Committer.sinkWithOffsetContext(committerSettings))(Keep.both)\n  .run()',
    tags: ['streams', 'context', 'kafka'],
  },
  {
    id: 'scala-akka-l4-064',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the danger of closing over an actor\'s mutable state in a Future callback?',
    options: [
      'The actor will deadlock waiting for the Future',
      'Futures cannot access actor state due to visibility restrictions',
      'The Future callback executes on a different thread than the actor, causing data races on the mutable state since actor thread-safety relies on single-threaded message processing',
      'There is no danger; Futures and actors share the same thread',
    ],
    correctAnswer: 2,
    explanation: 'Actor thread-safety depends on processing one message at a time on a single thread. A Future callback runs on the ExecutionContext\'s thread pool, not the actor\'s dispatcher thread. If the callback reads or modifies actor state, it creates a data race. The solution is to use context.pipeToSelf to convert the Future result into a message that is safely processed by the actor.',
    codeSnippet: '// DANGEROUS: data race on mutable state\nvar count = 0\ndef handle(): Behavior[Msg] = Behaviors.receiveMessage {\n  case Process =>\n    Future { expensiveWork() }.foreach { result =>\n      count += 1  // BUG: runs on Future thread!\n    }\n    Behaviors.same\n}\n\n// SAFE: pipe result to self\ncontext.pipeToSelf(Future(expensiveWork())) {\n  case Success(r) => WorkDone(r)\n  case Failure(e) => WorkFailed(e)\n}',
    tags: ['typed-actors', 'thread-safety', 'futures'],
  },
  {
    id: 'scala-akka-l4-065',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'How does Akka Cluster Sharding handle entity ID extraction and what is the role of the ShardRegion.HashCodeMessageExtractor?',
    options: [
      'It hashes the entity ID to determine the shard ID using entityId.hashCode.abs % numberOfShards, distributing entities across shards evenly',
      'It extracts entity IDs from HTTP headers',
      'It extracts entity IDs from database records',
      'It generates unique entity IDs automatically',
    ],
    correctAnswer: 0,
    explanation: 'The message extractor determines two things: the entityId from the message and the shardId from the entityId. HashCodeMessageExtractor uses entityId.hashCode.abs % numberOfShards to map entities to shards. This provides even distribution assuming entity IDs have good hash distribution. You can also implement custom extractors for domain-specific sharding (e.g., geographic or tenant-based).',
    codeSnippet: '// Default: hash-based shard extraction\nEntity(TypeKey)(ctx => MyEntity(ctx.entityId))\n  .withMessageExtractor(\n    new HashCodeNoEnvelopeMessageExtractor[Command](numberOfShards = 100) {\n      override def entityId(message: Command): String =\n        message.entityId\n    }\n  )',
    tags: ['cluster-sharding', 'message-extractor', 'hashing'],
  },
  {
    id: 'scala-akka-l4-066',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Persistence',
    question: 'What is Replicated Event Sourcing in Akka and when should you use it?',
    options: [
      'It duplicates events for audit logging',
      'It replicates the event journal to a backup database',
      'It allows an entity to have active replicas in multiple data centers, each processing commands locally with events replicated asynchronously between replicas for conflict-free multi-master operation',
      'It is a read replica pattern for query optimization',
    ],
    correctAnswer: 2,
    explanation: 'Replicated Event Sourcing allows the same entity (same persistenceId) to exist on multiple nodes/DCs simultaneously. Each replica can accept commands and persist events locally. Events are replicated to other replicas via the shared journal. The event handler must be designed to handle concurrent events (CRDT-style), as different replicas may see events in different orders. This enables active-active multi-DC setups.',
    codeSnippet: 'ReplicatedEventSourcing.commonJournalConfig(\n  ReplicationId("ShoppingCart", cartId, selfReplica),\n  allReplicas = Set(Replica("dc-a"), Replica("dc-b")),\n  queryPluginId\n) { replicationCtx =>\n  EventSourcedBehavior[Cmd, Evt, State](\n    replicationCtx.persistenceId,\n    emptyState,\n    commandHandler,\n    // Event handler must be commutative!\n    eventHandler\n  )\n}',
    tags: ['persistence', 'replicated-event-sourcing', 'multi-dc'],
  },
  {
    id: 'scala-akka-l4-067',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is stream materialization in Akka Streams and why can a stream be materialized multiple times?',
    options: [
      'Materialization converts the stream to a different format',
      'Materialization means persisting stream data to disk',
      'Materialization is a compile-time operation for type checking',
      'Materialization is the process of allocating resources and starting the stream execution; a stream blueprint (RunnableGraph) can be materialized multiple times, each creating independent running instances',
    ],
    correctAnswer: 3,
    explanation: 'An Akka Streams graph (Source, Flow, Sink, RunnableGraph) is a blueprint describing the stream topology. Materialization (calling .run()) allocates actual resources (actors, buffers) and starts execution. Each materialization creates an independent running instance. This is why Source.single(1).runWith(Sink.head) returns a different Future each time it is called.',
    codeSnippet: 'val blueprint: RunnableGraph[Future[Int]] =\n  Source(1 to 10).toMat(Sink.fold(0)(_ + _))(Keep.right)\n\n// Two independent materializations\nval sum1: Future[Int] = blueprint.run()  // independent instance\nval sum2: Future[Int] = blueprint.run()  // another independent instance\n// sum1 and sum2 are separate Futures from separate stream runs',
    tags: ['streams', 'materialization', 'blueprints'],
  },
  {
    id: 'scala-akka-l4-068',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Backpressure',
    question: 'What is the conflate operator in Akka Streams and when is it appropriate?',
    options: [
      'It removes duplicate elements from the stream',
      'It aggregates/combines elements when downstream is slow, always keeping the latest aggregated value ready without backpressuring upstream',
      'It merges multiple streams into one',
      'It compresses elements for network transfer',
    ],
    correctAnswer: 1,
    explanation: 'conflate(seed)(aggregate) combines elements when downstream cannot keep up. It never backpressures upstream. When downstream pulls, it gets the latest aggregated value. This is useful when you want to keep the most recent state (e.g., sensor readings) or accumulate a summary (e.g., counting dropped elements). Its counterpart extrapolate repeats/generates elements when upstream is slow.',
    codeSnippet: '// Keep latest value when downstream is slow\nsource.conflateWithSeed(identity)((_, newest) => newest)\n\n// Aggregate when downstream is slow: count elements\nsource.conflateWithSeed(_ => 1)((count, _) => count + 1)\n  .map(count => s"Processed batch of $count elements")',
    tags: ['streams', 'conflate', 'backpressure'],
  },
  {
    id: 'scala-akka-l4-069',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'What is Artery in Akka and how does it improve cluster communication performance?',
    options: [
      'Artery is a monitoring tool for Akka clusters',
      'Artery is a serialization framework',
      'Artery is Akka\'s high-performance remote transport layer based on Aeron (UDP) or TCP, replacing the old Netty-based remoting with lower latency, higher throughput, and better resource efficiency',
      'Artery is a database driver for Akka Persistence',
    ],
    correctAnswer: 2,
    explanation: 'Artery replaced classic Akka Remoting. It offers two transports: aeron-udp (using the Aeron library for reliable UDP with very low latency) and tcp (using Akka Streams TCP). Artery features include: multiplexed streams for large messages, flight-recorder for debugging, built-in compression of actor refs and manifests, and better resource utilization. It is the default since Akka 2.6.',
    tags: ['performance', 'artery', 'remoting'],
  },
  {
    id: 'scala-akka-l4-070',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you implement backpressure-aware batching in Akka Streams for efficient database writes?',
    options: [
      'Use groupedWithin(maxBatch, maxDuration) to batch elements by count or time, then write the batch to the database',
      'Write each element individually and let the database handle batching',
      'Use a custom actor to accumulate elements',
      'Use Thread.sleep to accumulate elements',
    ],
    correctAnswer: 0,
    explanation: 'groupedWithin(n, d) collects up to n elements or waits up to duration d (whichever comes first) and emits them as a Seq. This naturally integrates with backpressure: if downstream is slow, upstream is backpressured, and the batch fills up to maxBatch before being emitted. Combined with mapAsync for the database write, this provides efficient batched writes with flow control.',
    codeSnippet: 'source\n  .groupedWithin(maxBatch = 500, within = 1.second)\n  .mapAsync(parallelism = 4) { batch =>\n    db.batchInsert(batch)  // Insert up to 500 records at once\n  }\n  .runWith(Sink.ignore)',
    tags: ['streams', 'batching', 'database'],
  },
  {
    id: 'scala-akka-l4-071',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the Behavior.transformMessages pattern in Akka Typed?',
    options: [
      'It applies a partial function to incoming messages before they reach the behavior, allowing message adaptation and protocol translation',
      'It logs all messages automatically',
      'It encrypts messages before processing',
      'It changes the serialization format of messages',
    ],
    correctAnswer: 0,
    explanation: 'transformMessages applies a PartialFunction[Outer, Inner] to adapt messages from an outer protocol to an inner protocol. Messages not matched by the partial function are unhandled. This is useful for wrapping a reusable behavior with a specific protocol adapter without modifying the original behavior.',
    codeSnippet: 'sealed trait OuterProtocol\ncase class Wrapped(inner: InnerCommand) extends OuterProtocol\ncase class ExtraCommand(data: String) extends OuterProtocol\n\nval adapted: Behavior[OuterProtocol] =\n  innerBehavior.transformMessages[OuterProtocol] {\n    case Wrapped(inner) => inner\n    // ExtraCommand is unhandled\n  }',
    tags: ['typed-actors', 'message-adaptation', 'protocols'],
  },
  {
    id: 'scala-akka-l4-072',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What is the Cluster Sharding proxy pattern and when do you need it?',
    options: [
      'A shard region proxy allows nodes that do not host entities to send messages to sharded entities; the proxy forwards messages to the correct shard region on nodes that do host entities',
      'A proxy caches entity state for faster reads',
      'A proxy is needed for cross-cluster communication',
      'A proxy is used to load balance between shard regions',
    ],
    correctAnswer: 0,
    explanation: 'In a heterogeneous cluster, some nodes may only need to communicate with sharded entities but not host them (e.g., API gateway nodes). ClusterSharding.init with Entity.withRole ensures entities only run on nodes with a specific role. Nodes without that role automatically act as proxies: they know the shard allocation and forward messages to the hosting nodes.',
    codeSnippet: '// Entity nodes: host entities\nClusterSharding(system).init(\n  Entity(TypeKey)(ctx => MyEntity(ctx.entityId))\n    .withRole("entity-node")\n)\n\n// API nodes (different role): automatically proxy\n// Same init call, but since this node doesn\'t have\n// "entity-node" role, it runs as proxy only\nval entityRef = ClusterSharding(system)\n  .entityRefFor(TypeKey, "entity-123")',
    tags: ['cluster-sharding', 'proxy', 'heterogeneous-cluster'],
  },
  {
    id: 'scala-akka-l4-073',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Event Sourcing',
    question: 'What is the tombstone event pattern in event-sourced systems?',
    options: [
      'An event that marks the beginning of an entity\'s lifecycle',
      'An event that moves an entity to archive storage',
      'An event that triggers garbage collection',
      'A special event that marks an entity as logically deleted, after which the entity rejects all commands and can eventually be physically cleaned up',
    ],
    correctAnswer: 3,
    explanation: 'In event sourcing, you cannot simply delete an entity because its event history must be preserved. A tombstone event (e.g., EntityDeleted) transitions the entity to a "deleted" state where it rejects all commands except possibly queries about its deletion. Combined with passivation and eventually journal cleanup, this implements logical deletion while maintaining the audit trail.',
    codeSnippet: 'def commandHandler(state: State, cmd: Command): Effect[Event, State] =\n  state match {\n    case Deleted =>\n      cmd match {\n        case _ => Effect.unhandled // reject all commands\n      }\n    case Active(data) =>\n      cmd match {\n        case Delete(replyTo) =>\n          Effect.persist(EntityDeleted)\n            .thenReply(replyTo)(_ => Confirmed)\n        // ... other commands\n      }\n  }',
    tags: ['event-sourcing', 'tombstone', 'deletion'],
  },
  {
    id: 'scala-akka-l4-074',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Split Brain Resolver',
    question: 'What is the lease-based Split Brain Resolver strategy and why is it considered the safest?',
    options: [
      'It requires a node to hold an external distributed lease (e.g., from a database or coordination service) to remain in the cluster; only the partition that can reach the lease provider survives',
      'It is a proprietary strategy only available in Akka commercial edition',
      'It uses a timer-based lease that expires after a fixed duration',
      'It leases shard ownership to prevent split brain in sharding',
    ],
    correctAnswer: 0,
    explanation: 'The lease-based SBR strategy requires each side of a partition to acquire a lease from an external coordination service (e.g., a database with atomic compare-and-swap, or Kubernetes lease API). Only one side can hold the lease, so only that side survives. This avoids the edge cases of quorum-based strategies (e.g., simultaneous network partitions) at the cost of depending on an external service.',
    tags: ['split-brain', 'lease', 'safety'],
  },
  {
    id: 'scala-akka-l4-075',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Projections',
    question: 'How do you implement a projection that needs to call an external service (e.g., sending an email) exactly once per event?',
    options: [
      'Use at-least-once delivery combined with an idempotency key stored in a database; the projection handler checks for the key before calling the external service',
      'Use exactly-once projection delivery which guarantees it automatically',
      'Use a two-phase commit between the projection and the external service',
      'Call the service synchronously in the event handler',
    ],
    correctAnswer: 0,
    explanation: 'Exactly-once delivery to external services is impossible in distributed systems. The practical approach is at-least-once delivery (ensuring no events are skipped) combined with idempotency: store a unique event ID in a database before/after calling the external service. On retry, check if the ID exists to skip the duplicate. This provides effectively-once semantics for side effects.',
    codeSnippet: 'class EmailProjectionHandler extends Handler[EventEnvelope[Event]] {\n  override def process(envelope: EventEnvelope[Event]) = {\n    val eventId = envelope.persistenceId + "-" + envelope.sequenceNr\n    for {\n      alreadySent <- db.exists(eventId)\n      _ <- if (!alreadySent) {\n        emailService.send(envelope.event) *>\n        db.markProcessed(eventId)\n      } else Future.successful(())\n    } yield Done\n  }\n}',
    tags: ['projections', 'idempotency', 'side-effects'],
  },
  {
    id: 'scala-akka-l4-076',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the difference between Keep.left, Keep.right, and Keep.both when combining stream stages?',
    options: [
      'They determine error handling: left retries, right fails, both does both',
      'They control which materialized value is kept when composing stages: Keep.left retains the upstream materialized value, Keep.right retains the downstream, and Keep.both creates a tuple of both',
      'They determine which elements pass through the stream',
      'They control flow direction in bidirectional streams',
    ],
    correctAnswer: 1,
    explanation: 'Every stream stage has a materialized value (the value produced when the stream is run). When connecting stages with viaMat/toMat, you choose which materialized value to keep. Keep.left keeps the upstream\'s, Keep.right keeps the downstream\'s, and Keep.both creates a tuple. The shorthand via/to methods default to Keep.left. This is crucial for accessing things like completion Futures or queue references.',
    codeSnippet: '// Keep.right: get the Sink\'s Future[Done]\nval done: Future[Done] =\n  Source(1 to 10).toMat(Sink.foreach(println))(Keep.right).run()\n\n// Keep.both: get both materialized values\nval (queue, done2): (SourceQueueWithComplete[Int], Future[Done]) =\n  Source.queue[Int](10)\n    .toMat(Sink.foreach(println))(Keep.both).run()',
    tags: ['streams', 'materialization', 'composition'],
  },
  {
    id: 'scala-akka-l4-077',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'How do you monitor and diagnose a slow Akka Streams pipeline?',
    options: [
      'Add println statements between each stage',
      'Use the log operator for element-level logging, Akka\'s built-in stream attributes for naming stages, and integrate with metrics libraries (Kamon, Lightbend Telemetry) to measure throughput and demand at each stage boundary',
      'There is no way to monitor Akka Streams',
      'Use Thread.sleep to measure timing',
    ],
    correctAnswer: 1,
    explanation: 'The log("name") operator logs elements, completions, and errors. Adding .addAttributes(Attributes.name("stage-name")) helps identify stages in logs and metrics. For production monitoring, Lightbend Telemetry (Cinnamon) or Kamon provide per-stage throughput, demand wait time, and processing time metrics. You can also use .wireTap to send elements to a monitoring side-channel without affecting the main flow.',
    codeSnippet: 'source\n  .log("after-source").addAttributes(\n    Attributes.logLevels(onElement = Logging.DebugLevel)\n  )\n  .via(processingFlow)\n  .wireTap(element => metrics.recordProcessed(element))\n  .log("before-sink")\n  .runWith(Sink.ignore)',
    tags: ['performance', 'monitoring', 'streams'],
  },
  {
    id: 'scala-akka-l4-078',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'How do you implement the "worker pool" pattern in Akka Typed?',
    options: [
      'Use Akka Streams for worker pools instead of actors',
      'Use the pool router (Routers.pool) which creates a specified number of child actor routees and routes messages using a configurable strategy (round-robin or random)',
      'Use a single actor with parallel message processing',
      'Create a pool of actors manually and round-robin messages',
    ],
    correctAnswer: 1,
    explanation: 'Routers.pool(poolSize)(behaviorFactory) creates a router that spawns poolSize child actors and distributes messages among them. The default strategy is round-robin. You can use Routers.pool with .withRoundRobinRouting() or .withRandomRouting(). The pool router also supports broadcasting to all routees. For resizable pools, you can combine with Cluster-aware routers.',
    codeSnippet: 'val pool: Behavior[WorkerCommand] = Routers.pool(poolSize = 8) {\n  Behaviors.supervise(Worker())\n    .onFailure[Exception](SupervisorStrategy.restart)\n}.withRoundRobinRouting()\n\nval poolRef = context.spawn(pool, "worker-pool")\npoolRef ! DoWork(data)  // routed to one of 8 workers',
    tags: ['typed-actors', 'router', 'worker-pool'],
  },
  {
    id: 'scala-akka-l4-079',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Cluster Sharding',
    question: 'What is the impact of entity passivation timeout on Akka Cluster Sharding performance during high-traffic scenarios?',
    options: [
      'A longer timeout reduces memory usage',
      'The timeout has no performance impact',
      'A shorter timeout always improves performance',
      'A very short timeout causes frequent passivation/reactivation (thrashing), increasing latency from repeated event replay; a very long timeout wastes memory on idle entities. The optimal timeout depends on entity access patterns.',
    ],
    correctAnswer: 3,
    explanation: 'If the passivation timeout is too short relative to the inter-arrival time of messages, entities will repeatedly stop and restart, each restart requiring event replay from the journal (expensive for entities with many events). Too long, and idle entities consume memory. The right balance depends on traffic patterns, entity state size, and event count. Monitoring passivation/activation rates helps tune this.',
    tags: ['cluster-sharding', 'passivation', 'performance-tuning'],
  },
  {
    id: 'scala-akka-l4-080',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you handle stream failures gracefully without stopping the entire stream?',
    options: [
      'Use try-catch around the run() call',
      'Ignore failures and they will be handled automatically',
      'Use a dead letter queue actor to collect failures',
      'Use the recover, recoverWithRetries, or supervision strategy attributes to handle failures per-element and continue processing',
    ],
    correctAnswer: 3,
    explanation: 'recover maps a stream failure to a final element. recoverWithRetries switches to an alternative Source on failure with a retry count. ActorAttributes.supervisionStrategy allows per-stage strategies: Resume (drop failed element and continue), Restart (reset stage state and continue), or Stop (default, terminates stream). The divertTo operator can also route failures to a side channel.',
    codeSnippet: '// Supervision: skip elements that cause exceptions\nval decider: Supervision.Decider = {\n  case _: NumberFormatException => Supervision.Resume\n  case _ => Supervision.Stop\n}\n\nsource\n  .map(s => s.toInt)  // may throw\n  .withAttributes(ActorAttributes.supervisionStrategy(decider))\n  .runWith(Sink.seq)\n\n// Alternative: recoverWithRetries\nsource.recoverWithRetries(3, {\n  case _: IOException => Source(fallbackData)\n})',
    tags: ['streams', 'error-handling', 'supervision'],
  },
  {
    id: 'scala-akka-l4-081',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Distributed Data',
    question: 'What is the LWWMap (Last-Writer-Wins Map) in Akka Distributed Data and what are its consistency trade-offs?',
    options: [
      'It is a map that prevents concurrent writes entirely',
      'LWWMap stores the full history of all writes',
      'It is a map that uses distributed locks for consistency',
      'LWWMap resolves concurrent writes to the same key by using timestamps, where the write with the latest timestamp wins; this requires reasonably synchronized clocks across nodes',
    ],
    correctAnswer: 3,
    explanation: 'LWWMap uses LWWRegister for each key, resolving concurrent updates by timestamp (last write wins). This requires that node clocks are reasonably synchronized (NTP). Clock skew can cause unexpected results: a write with a higher timestamp wins even if it was logically earlier. For cases where clock synchronization cannot be guaranteed, use ORMap or PNCounterMap instead.',
    codeSnippet: 'val key = LWWMapKey[String, String]("config")\n\n// Node A at time T1\nreplicator ! Update(key, LWWMap.empty, WriteLocal)(\n  _ :+ (node, "theme" -> "dark")\n)\n\n// Node B at time T2 > T1 (concurrent)\nreplicator ! Update(key, LWWMap.empty, WriteLocal)(\n  _ :+ (node, "theme" -> "light")\n)\n// After merge: "theme" -> "light" wins (higher timestamp)',
    tags: ['distributed-data', 'lww-map', 'clock-skew'],
  },
  {
    id: 'scala-akka-l4-082',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Performance Tuning',
    question: 'What is the effect of setting akka.cluster.sharding.updating-state-timeout too low?',
    options: [
      'It reduces memory usage of the coordinator',
      'It can cause shard allocation failures during high cluster churn, as the ShardCoordinator may time out while persisting state changes, leading to retries and temporary unavailability of affected shards',
      'It has no effect on cluster behavior',
      'It improves shard allocation speed',
    ],
    correctAnswer: 1,
    explanation: 'The ShardCoordinator persists shard allocations using Distributed Data or Akka Persistence. updating-state-timeout controls how long it waits for persistence to confirm. If set too low during periods of high cluster activity (nodes joining/leaving, rebalancing), the timeout triggers, causing the coordinator to retry. This delays shard allocation and can cause message buffering for affected shards.',
    tags: ['performance', 'cluster-sharding', 'timeout'],
  },
  {
    id: 'scala-akka-l4-083',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'What is the difference between flatMapConcat and flatMapMerge in Akka Streams?',
    options: [
      'flatMapConcat is for concatenation, flatMapMerge is for merging strings',
      'flatMapConcat produces a single output, flatMapMerge produces multiple outputs',
      'flatMapConcat processes inner sources sequentially (waits for each to complete before starting the next), while flatMapMerge runs up to N inner sources concurrently and merges their outputs',
      'They produce the same output but flatMapMerge is always faster',
    ],
    correctAnswer: 2,
    explanation: 'flatMapConcat(f) maps each element to a Source and concatenates them sequentially: inner Source 1 must complete before inner Source 2 starts. flatMapMerge(breadth, f) maps each element to a Source and merges up to breadth inner Sources concurrently, interleaving their outputs. flatMapMerge is faster when inner Sources have latency, but output order is non-deterministic.',
    codeSnippet: '// Sequential: one API call at a time\nsource.flatMapConcat(id => Source.future(fetchFromApi(id)))\n\n// Concurrent: up to 4 API calls in parallel\nsource.flatMapMerge(\n  breadth = 4,\n  id => Source.future(fetchFromApi(id))\n)',
    tags: ['streams', 'flatMap', 'concurrency'],
  },
  {
    id: 'scala-akka-l4-084',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Event Sourcing',
    question: 'What is the "process manager" (saga) implementation strategy in Akka Persistence?',
    options: [
      'A persistent actor that listens to events from multiple aggregates and orchestrates a multi-step workflow by maintaining its own state and sending commands, with compensating actions for failure handling',
      'A single EventSourcedBehavior that manages the entire application state',
      'A REST endpoint that calls multiple services in sequence',
      'A database stored procedure that coordinates transactions',
    ],
    correctAnswer: 0,
    explanation: 'A process manager is itself an event-sourced actor. It subscribes to events from other aggregates (via projections or event bus), maintains a state machine tracking the workflow progress, and sends commands to advance the workflow. If a step fails, it executes compensating commands to undo previous steps. Being event-sourced itself, the process manager recovers its state after restarts.',
    codeSnippet: 'sealed trait SagaState\ncase object WaitingForPayment extends SagaState\ncase object WaitingForShipping extends SagaState\ncase object Completed extends SagaState\ncase object Compensating extends SagaState\n\ndef commandHandler(state: SagaState, cmd: SagaCommand) =\n  (state, cmd) match {\n    case (WaitingForPayment, PaymentConfirmed(orderId)) =>\n      Effect.persist(PaymentReceived(orderId))\n        .thenRun(_ => shippingRef ! ShipOrder(orderId))\n    case (WaitingForShipping, ShippingFailed(orderId)) =>\n      Effect.persist(ShippingFailedEvt(orderId))\n        .thenRun(_ => paymentRef ! RefundPayment(orderId))\n  }',
    tags: ['event-sourcing', 'saga', 'process-manager'],
  },
  {
    id: 'scala-akka-l4-085',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka gRPC',
    question: 'How do you implement server-side load balancing for Akka gRPC services in a Kubernetes environment?',
    options: [
      'Use headless Kubernetes services with client-side load balancing, or deploy an L7 proxy (like Envoy/Linkerd) that understands HTTP/2, because standard L4 load balancers do not properly distribute HTTP/2 streams across connections',
      'Use gRPC\'s built-in load balancing which works automatically',
      'Use round-robin DNS which works perfectly with gRPC',
      'gRPC does not support load balancing',
    ],
    correctAnswer: 0,
    explanation: 'HTTP/2 multiplexes many RPCs over a single TCP connection. A standard L4 load balancer (like a Kubernetes Service of type ClusterIP) assigns the connection to one backend, sending all RPCs to the same pod. For proper load balancing, you need either: a headless service with client-side discovery, or an L7 proxy (Envoy, Linkerd, Istio) that understands HTTP/2 framing and distributes individual RPCs across backends.',
    tags: ['grpc', 'load-balancing', 'kubernetes'],
  },
  {
    id: 'scala-akka-l4-086',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Typed Actors',
    question: 'What is the difference between Behaviors.stopped and returning Behaviors.empty from a message handler?',
    options: [
      'Behaviors.empty stops the actor without cleanup',
      'Behaviors.stopped pauses the actor while Behaviors.empty terminates it',
      'They are identical in behavior',
      'Behaviors.stopped terminates the actor and triggers PostStop signal; Behaviors.empty keeps the actor alive but makes it ignore all messages (useful for actors that only handle signals)',
    ],
    correctAnswer: 3,
    explanation: 'Behaviors.stopped initiates actor termination: PostStop signal is sent, watchers are notified with Terminated, and resources are cleaned up. Behaviors.empty keeps the actor running but any message received is treated as unhandled. This is useful for actors that only need to exist (e.g., for lifecycle watching) or only respond to signals, not messages.',
    codeSnippet: '// Stopped: actor terminates\nBehaviors.receiveMessage[Command] {\n  case Shutdown =>\n    context.log.info("Shutting down")\n    Behaviors.stopped  // triggers PostStop\n}\n\n// Empty: actor stays alive but ignores messages\nBehaviors.receiveSignal[Nothing] {\n  case (_, PostStop) =>\n    cleanup()\n    Behaviors.same\n}\n// Mix: Behaviors.empty handles no messages but can receive signals',
    tags: ['typed-actors', 'lifecycle', 'stopped-vs-empty'],
  },
  {
    id: 'scala-akka-l4-087',
    language: 'scala',
    level: 'level4',
    category: 'Akka',
    subcategory: 'Akka Streams',
    question: 'How do you implement a reactive Kafka consumer with Akka Streams that supports cooperative partition rebalancing and exactly-once semantics?',
    options: [
      'Use Alpakka Kafka\'s Consumer.committableSource with manual offset committing, combined with a transactional sink or idempotent handler, and configure cooperative-sticky assignor for smooth partition rebalancing',
      'Use Akka HTTP to poll a Kafka REST proxy',
      'Use a plain KafkaConsumer and poll in a loop',
      'Use Akka Persistence to store Kafka messages',
    ],
    correctAnswer: 0,
    explanation: 'Alpakka Kafka (akka-stream-kafka) provides Consumer.committableSource which materializes a backpressure-aware Kafka consumer. Each element carries a committable offset. For exactly-once, use Transactional.source with Transactional.sink (Kafka transactions) or implement idempotent writes. Cooperative-sticky partition assignment avoids stop-the-world rebalancing, allowing unaffected partitions to continue processing during rebalance.',
    codeSnippet: 'val consumerSettings = ConsumerSettings(system, keyDeserializer, valueDeserializer)\n  .withGroupId("my-group")\n  .withProperty(ConsumerConfig.PARTITION_ASSIGNMENT_STRATEGY_CONFIG,\n    classOf[CooperativeStickyAssignor].getName)\n\nConsumer.committableSource(consumerSettings, Subscriptions.topics("topic"))\n  .mapAsync(4)(msg => processAndStore(msg.record).map(_ => msg.committableOffset))\n  .via(Committer.flow(CommitterSettings(system)))\n  .runWith(Sink.ignore)',
    tags: ['streams', 'kafka', 'exactly-once'],
  },
];

export default questions;
