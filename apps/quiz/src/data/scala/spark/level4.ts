import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'scala-spark-l4-001',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the difference between `map` and `mapPartitions` in terms of function call overhead?',
    options: [
      '`map` calls the function once per partition; `mapPartitions` calls it once per element',
      '`mapPartitions` calls the function once per partition, reducing object creation overhead',
      'Both call the function once per element with identical overhead',
      '`mapPartitions` is always slower due to iterator materialization',
    ],
    correctAnswer: 1,
    explanation: '`mapPartitions` invokes the function once per partition, allowing expensive setup (e.g., DB connections) to be amortized across all elements in that partition.',
    tags: ['rdd', 'performance', 'mapPartitions'],
  },
  {
    id: 'scala-spark-l4-002',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'Which RDD operation causes a shuffle and why?',
    options: [
      '`map` — it changes element types',
      '`filter` — it reduces the number of elements',
      '`groupByKey` — data with the same key must be co-located',
      '`flatMap` — it may expand partitions',
    ],
    correctAnswer: 2,
    explanation: '`groupByKey` requires all values for a key to reside on the same partition, forcing a network shuffle to redistribute data by key.',
    tags: ['rdd', 'shuffle', 'groupByKey'],
  },
  {
    id: 'scala-spark-l4-003',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'Why is `reduceByKey` preferred over `groupByKey` followed by `map`?',
    options: [
      '`reduceByKey` skips the shuffle entirely',
      '`reduceByKey` performs partial aggregation on each partition before the shuffle',
      '`groupByKey` is deprecated in Spark 3.x',
      '`reduceByKey` preserves key ordering',
    ],
    correctAnswer: 1,
    explanation: '`reduceByKey` applies the combiner function locally on each partition (map-side combine), drastically reducing the amount of data sent over the network.',
    tags: ['rdd', 'shuffle', 'reduceByKey', 'performance'],
  },
  {
    id: 'scala-spark-l4-004',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does `rdd.persist(StorageLevel.MEMORY_AND_DISK_SER)` do differently from `MEMORY_AND_DISK`?',
    options: [
      'It stores data in serialized form, reducing memory footprint at the cost of CPU',
      'It replicates each partition to disk regardless of memory availability',
      'It pins the RDD in off-heap memory',
      'It behaves identically; the suffix `_SER` is ignored',
    ],
    correctAnswer: 0,
    explanation: 'The `_SER` suffix serializes Java objects before storing them, which uses less RAM but requires deserialization CPU cost on every read.',
    tags: ['rdd', 'caching', 'storage-level', 'serialization'],
  },
  {
    id: 'scala-spark-l4-005',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is a narrow dependency vs. a wide dependency in Spark lineage?',
    options: [
      'Narrow: each child partition depends on multiple parent partitions; Wide: one-to-one',
      'Narrow: each child partition depends on at most one parent partition; Wide: multiple parent partitions',
      'Narrow and wide refer to the number of columns, not partitions',
      'Wide dependencies never cause a stage boundary',
    ],
    correctAnswer: 1,
    explanation: 'Narrow dependencies (e.g., `map`, `filter`) allow pipelined execution within one stage. Wide dependencies (e.g., `groupByKey`) require a shuffle and create a new stage boundary.',
    tags: ['rdd', 'lineage', 'dependency', 'DAG'],
  },
  {
    id: 'scala-spark-l4-006',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What happens when you call `rdd.checkpoint()` without first calling `sc.setCheckpointDir(...)`?',
    options: [
      'Spark silently falls back to in-memory caching',
      'An exception is thrown at job execution time',
      'The RDD is checkpointed to /tmp automatically',
      'Checkpointing is skipped without error',
    ],
    correctAnswer: 1,
    explanation: 'Spark requires a checkpoint directory to be set before materializing a checkpoint; omitting it results in a `SparkException` at action execution time.',
    tags: ['rdd', 'checkpoint', 'fault-tolerance'],
  },
  {
    id: 'scala-spark-l4-007',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the effect of calling `rdd.coalesce(1, shuffle = false)` on a 200-partition RDD?',
    options: [
      'Creates 1 partition by shuffling all data to a single node',
      'Creates 1 partition by merging partitions on each executor without a shuffle, potentially causing severe data skew on one task',
      'Raises an error because shuffle must be true when reducing partitions',
      'Triggers a full sort before merging',
    ],
    correctAnswer: 1,
    explanation: 'With `shuffle=false`, `coalesce` avoids a shuffle by combining existing partitions, but all data flows through a single task, creating a bottleneck.',
    tags: ['rdd', 'partitioning', 'coalesce'],
  },
  {
    id: 'scala-spark-l4-008',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does the Catalyst optimizer\'s "predicate pushdown" optimization do?',
    options: [
      'Moves `filter` operations as close to the data source as possible to minimize data read',
      'Pushes UDFs into the physical plan for early evaluation',
      'Reorders JOIN operations to reduce shuffle size',
      'Converts row-based filters to column-based projections',
    ],
    correctAnswer: 0,
    explanation: 'Predicate pushdown translates DataFrame filters into data-source-level predicates (e.g., Parquet row group pruning), reducing the volume of data read from storage.',
    tags: ['catalyst', 'optimization', 'predicate-pushdown', 'dataframe'],
  },
  {
    id: 'scala-spark-l4-009',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'Which method prints the physical and logical query plans for a DataFrame?',
    options: [
      '`df.schema`',
      '`df.explain(true)`',
      '`df.printSchema()`',
      '`df.queryExecution.toString`',
    ],
    correctAnswer: 1,
    explanation: '`df.explain(true)` prints the parsed, analyzed, optimized logical plans and the physical plan, making it the standard way to inspect Catalyst output.',
    tags: ['catalyst', 'debugging', 'explain', 'dataframe'],
  },
  {
    id: 'scala-spark-l4-010',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is the risk of using `df.collect()` in production pipelines?',
    options: [
      'It invalidates the DataFrame cache',
      'It materializes all data into driver memory, risking OOM errors on large datasets',
      'It triggers an extra shuffle before collection',
      'It converts Rows to RDD elements, losing schema information',
    ],
    correctAnswer: 1,
    explanation: '`collect()` brings all partition data to the driver JVM heap; for large DataFrames this causes OutOfMemoryError on the driver.',
    tags: ['dataframe', 'driver', 'memory', 'production'],
  },
  {
    id: 'scala-spark-l4-011',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How does `repartition(n, col)` differ from `repartition(n)`?',
    options: [
      '`repartition(n, col)` uses range partitioning; `repartition(n)` uses hash partitioning',
      '`repartition(n, col)` hash-partitions by `col`, collocating same-key rows; `repartition(n)` round-robins',
      'Both are identical — the column argument is ignored',
      '`repartition(n, col)` avoids a shuffle; `repartition(n)` always shuffles',
    ],
    correctAnswer: 1,
    explanation: 'Specifying a column causes Spark to hash-partition by that column, ensuring rows with the same key end up in the same partition — useful before joins or aggregations on that key.',
    tags: ['dataframe', 'partitioning', 'repartition'],
  },
  {
    id: 'scala-spark-l4-012',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does `spark.sql.shuffle.partitions` control and what is its default value?',
    options: [
      'Number of partitions after a shuffle; default is 200',
      'Maximum partition size in MB; default is 128',
      'Number of parallel shuffle readers; default is 50',
      'Number of partitions read from source; default is 200',
    ],
    correctAnswer: 0,
    explanation: 'This config sets the number of output partitions created after a shuffle (e.g., after a join or aggregation). The default of 200 is often suboptimal for very large or very small datasets.',
    tags: ['configuration', 'shuffle', 'partitioning', 'dataframe'],
  },
  {
    id: 'scala-spark-l4-013',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is column pruning in Spark and how does it improve performance?',
    options: [
      'Dropping null columns before a join to reduce schema complexity',
      'Reading only the columns needed by the query from columnar formats like Parquet, reducing I/O',
      'Removing duplicate column names after a self-join',
      'Eliminating constant columns detected by the Catalyst analyzer',
    ],
    correctAnswer: 1,
    explanation: 'Column pruning pushes the projection down to the data source, so Parquet/ORC readers skip column chunks not referenced by the query, dramatically reducing I/O and deserialization cost.',
    tags: ['catalyst', 'optimization', 'column-pruning', 'parquet'],
  },
  {
    id: 'scala-spark-l4-014',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What is the key advantage of `Dataset[T]` over `DataFrame` (i.e., `Dataset[Row]`)?',
    options: [
      'Datasets bypass the Catalyst optimizer for faster execution',
      'Datasets provide compile-time type safety, catching schema errors before runtime',
      'Datasets always use off-heap memory, avoiding GC',
      'Datasets support more SQL functions than DataFrames',
    ],
    correctAnswer: 1,
    explanation: 'Dataset[T] encodes the schema in the Scala type system, so type mismatches are caught at compile time rather than at runtime as with DataFrame.',
    tags: ['dataset', 'type-safety', 'encoder'],
  },
  {
    id: 'scala-spark-l4-015',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What is an `Encoder[T]` in Spark and why is it critical for Datasets?',
    options: [
      'A Parquet codec that compresses Dataset columns',
      'A bidirectional converter between JVM objects and Spark\'s internal binary (Tungsten) format',
      'A broadcast variable that caches the schema on all executors',
      'An interface for custom partitioners on typed Datasets',
    ],
    correctAnswer: 1,
    explanation: 'Encoders serialize/deserialize JVM objects to/from Spark\'s off-heap Tungsten binary format efficiently without Java serialization, enabling Project Tungsten optimizations.',
    tags: ['dataset', 'encoder', 'tungsten', 'serialization'],
  },
  {
    id: 'scala-spark-l4-016',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'When you call `ds.map(f)` with a lambda on a `Dataset[T]`, what happens to Catalyst optimization?',
    options: [
      'Catalyst fully optimizes the lambda the same way it optimizes SQL expressions',
      'The lambda is treated as a black box; Catalyst cannot optimize inside it, losing some optimizations',
      'Spark converts the lambda to a SQL expression automatically',
      'The map operation is pushed down to the data source',
    ],
    correctAnswer: 1,
    explanation: 'Opaque JVM lambdas are black boxes to Catalyst; optimizations like predicate pushdown and column pruning cannot be applied inside them, unlike DataFrame column expressions.',
    tags: ['dataset', 'catalyst', 'lambda', 'optimization'],
  },
  {
    id: 'scala-spark-l4-017',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What is the `ExpressionEncoder` and how does Spark derive it for case classes?',
    options: [
      'A hand-written encoder that must be provided by the user for each type',
      'A macro-generated encoder derived via `implicit` resolution using `Encoders.product` or `implicitly[Encoder[T]]`',
      'An encoder stored in the Hive metastore for structured types',
      'A runtime-reflection encoder that requires `ClassTag`',
    ],
    correctAnswer: 1,
    explanation: 'Spark uses Scala macro-based implicit derivation to generate `ExpressionEncoder[T]` for case classes at compile time, mapping each field to an internal Spark expression.',
    tags: ['dataset', 'encoder', 'implicit', 'case-class'],
  },
  {
    id: 'scala-spark-l4-018',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'How does Spark\'s Adaptive Query Execution (AQE) improve shuffle partition count?',
    options: [
      'AQE sets partition count before the job using table statistics',
      'AQE coalesces small shuffle partitions at runtime based on actual data size collected after each stage',
      'AQE increases partition count if tasks are slow',
      'AQE requires manual configuration and does not adjust partitions automatically',
    ],
    correctAnswer: 1,
    explanation: 'AQE inspects runtime shuffle map output statistics after each stage and merges small consecutive partitions into fewer, larger ones, avoiding thousands of tiny tasks.',
    tags: ['aqe', 'adaptive', 'shuffle', 'partitioning', 'spark3'],
  },
  {
    id: 'scala-spark-l4-019',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What is the purpose of `spark.sql.autoBroadcastJoinThreshold`?',
    options: [
      'Sets the maximum row count before converting a sort-merge join to a hash join',
      'Sets the maximum size (bytes) of a relation that Spark will automatically broadcast in a join',
      'Limits the number of broadcast variables per executor',
      'Controls how long broadcast data is retained in executor memory',
    ],
    correctAnswer: 1,
    explanation: 'Relations smaller than this threshold (default 10 MB) are automatically broadcast to all executors, replacing an expensive shuffle-based join with a local hash lookup.',
    tags: ['join', 'broadcast', 'configuration', 'sparksql'],
  },
  {
    id: 'scala-spark-l4-020',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What are Spark\'s runtime statistics used for in the Cost-Based Optimizer (CBO)?',
    options: [
      'They determine executor memory allocation at startup',
      'They guide join reordering and join strategy selection based on estimated row counts and column statistics',
      'They are only used for display in `EXPLAIN` output',
      'They replace Catalyst logical plan rules',
    ],
    correctAnswer: 1,
    explanation: 'With `spark.sql.cbo.enabled=true`, Spark uses table/column statistics (collected via `ANALYZE TABLE`) to pick optimal join orders and strategies by minimizing estimated cost.',
    tags: ['cbo', 'statistics', 'catalyst', 'join', 'optimization'],
  },
  {
    id: 'scala-spark-l4-021',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What does `LATERAL VIEW explode(col)` do in Spark SQL?',
    options: [
      'Pivots multiple columns into rows',
      'Generates a virtual table by exploding an array column and joining it back to the outer table row',
      'Samples a fraction of each partition for testing',
      'Creates a window over a nested struct column',
    ],
    correctAnswer: 1,
    explanation: '`LATERAL VIEW explode` is a table-generating function that unpacks each array element into a separate row while preserving the parent row\'s other columns.',
    tags: ['sparksql', 'explode', 'lateral-view', 'arrays'],
  },
  {
    id: 'scala-spark-l4-022',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'How does `spark.sql.files.maxPartitionBytes` affect reading Parquet files?',
    options: [
      'It sets the maximum number of Parquet files per partition',
      'It controls the maximum byte size of each input partition, governing parallelism when reading files',
      'It limits the Parquet row group size written by Spark',
      'It is only applicable to CSV and JSON sources',
    ],
    correctAnswer: 1,
    explanation: 'Spark splits file blocks into input partitions not exceeding `maxPartitionBytes` (default 128 MB), directly controlling the initial task parallelism for file-based reads.',
    tags: ['configuration', 'parquet', 'partitioning', 'dataframe'],
  },
  {
    id: 'scala-spark-l4-023',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'In what order does Catalyst process a query plan?',
    options: [
      'Physical Plan → Logical Plan → Analysis → Optimization',
      'Parsing → Analysis → Logical Optimization → Physical Planning → Code Generation',
      'Code Generation → Physical Planning → Logical Optimization → Analysis → Parsing',
      'Analysis → Parsing → Code Generation → Physical Planning',
    ],
    correctAnswer: 1,
    explanation: 'Catalyst follows: parse SQL to unresolved logical plan → resolve references (analysis) → apply logical optimizations → generate physical plans (strategies) → select best plan → whole-stage code gen.',
    tags: ['catalyst', 'query-planning', 'internals'],
  },
  {
    id: 'scala-spark-l4-024',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'What is whole-stage code generation (WSCG) in Spark?',
    options: [
      'Compiling SQL strings to JVM bytecode at cluster startup',
      'Fusing multiple operator pipelines into a single optimized JVM method to reduce virtual dispatch and object creation',
      'Generating Python UDF wrappers at runtime',
      'Pre-compiling shuffle write/read paths into native code',
    ],
    correctAnswer: 1,
    explanation: 'WSCG (Project Tungsten) generates a single tight Java method for a chain of operators, eliminating per-row virtual function calls and intermediate object allocation.',
    tags: ['catalyst', 'tungsten', 'codegen', 'performance'],
  },
  {
    id: 'scala-spark-l4-025',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'Which Catalyst rule eliminates `WHERE 1=1` from a query plan?',
    options: [
      'ColumnPruning',
      'ConstantFolding',
      'PushPredicateThroughJoin',
      'EliminateSubqueryAliases',
    ],
    correctAnswer: 1,
    explanation: '`ConstantFolding` evaluates constant expressions at plan time; `1=1` becomes `true` and the filter node is then removed by `BooleanSimplification`.',
    tags: ['catalyst', 'constant-folding', 'optimization'],
  },
  {
    id: 'scala-spark-l4-026',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'What is the role of `TreeNode` in Catalyst\'s internal representation?',
    options: [
      'It is a distributed data structure for storing partition metadata',
      'It is the base class for all logical/physical plan nodes, enabling tree transformations via pattern matching',
      'It maps Spark SQL types to JVM types during encoding',
      'It manages task scheduling within a stage',
    ],
    correctAnswer: 1,
    explanation: 'Catalyst represents query plans as trees of `TreeNode` subclasses; optimizer rules are functions `Tree => Tree` applied via `transform` with Scala pattern matching.',
    tags: ['catalyst', 'internals', 'treenode', 'optimizer'],
  },
  {
    id: 'scala-spark-l4-027',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'When does Spark choose a Sort-Merge Join (SMJ) over a Broadcast Hash Join?',
    options: [
      'Always when both tables have more than 1 million rows',
      'When neither side fits within `autoBroadcastJoinThreshold` and both sides can be sorted on the join key',
      'When the join is on a string column',
      'When AQE is disabled',
    ],
    correctAnswer: 1,
    explanation: 'SMJ is the default for large-large joins: both sides are shuffled/sorted by join key, then merged. It requires both sides to be sortable and is more memory-efficient than hash join for large data.',
    tags: ['join', 'sort-merge', 'physical-plan'],
  },
  {
    id: 'scala-spark-l4-028',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'What is a skewed join in Spark and how does AQE address it?',
    options: [
      'A join where key counts are uneven; AQE splits oversized partitions and replicates the other side',
      'A join on non-equi predicates; AQE converts it to a broadcast join',
      'A join with null keys; AQE filters nulls before shuffling',
      'A join across different clusters; AQE reroutes data between clusters',
    ],
    correctAnswer: 0,
    explanation: 'AQE\'s skew join optimization detects partitions larger than `spark.sql.adaptive.skewJoin.skewedPartitionThresholdInBytes`, splits them, and replicates the corresponding partition from the other side.',
    tags: ['join', 'skew', 'aqe', 'spark3'],
  },
  {
    id: 'scala-spark-l4-029',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'How does a `BroadcastHashJoin` work internally?',
    options: [
      'The larger table is broadcast; the smaller table is probed with a hash lookup',
      'The smaller table is broadcast to all executors and built into a hash map; each partition of the larger table probes it locally',
      'Both tables are broadcast and merged on each executor',
      'A Bloom filter is broadcast instead of actual data',
    ],
    correctAnswer: 1,
    explanation: 'The smaller relation is fully serialized and sent to all executor JVMs. Each executor builds a local hash table from it and probes it with rows from its partition of the larger table — no shuffle required.',
    tags: ['join', 'broadcast', 'hash-join', 'internals'],
  },
  {
    id: 'scala-spark-l4-030',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'What hint forces a broadcast join in a DataFrame operation?',
    options: [
      '`df.hint("SHUFFLE_HASH")`',
      '`df.hint("BROADCAST")`',
      '`df.hint("SKEW_JOIN")`',
      '`df.hint("COALESCE")`',
    ],
    correctAnswer: 1,
    explanation: '`df.hint("BROADCAST")` (or `broadcast(df)` function) instructs Catalyst to plan a `BroadcastHashJoin` for that relation, overriding the size threshold heuristic.',
    tags: ['join', 'broadcast', 'hint', 'dataframe'],
  },
  {
    id: 'scala-spark-l4-031',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'What is a Shuffle Hash Join and when is it preferred over Sort-Merge Join?',
    options: [
      'SHJ is preferred when both sides are very large and sorted',
      'SHJ is preferred when one post-shuffle partition fits in memory; it builds a hash table avoiding the sort step',
      'SHJ is always chosen when AQE is enabled',
      'SHJ is used for non-equi joins only',
    ],
    correctAnswer: 1,
    explanation: 'Shuffle Hash Join shuffles both sides by key, then builds a hash table from the smaller partition. It avoids the sort step of SMJ but requires the build-side partition to fit in memory.',
    tags: ['join', 'shuffle-hash', 'physical-plan'],
  },
  {
    id: 'scala-spark-l4-032',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What is the purpose of `spark.sql.adaptive.coalescePartitions.minPartitionNum`?',
    options: [
      'Sets the minimum number of partitions AQE will coalesce down to, preventing over-reduction',
      'Sets the minimum number of partitions before AQE activates',
      'Limits the number of partitions created by `repartition`',
      'Controls the minimum parallelism for broadcast joins',
    ],
    correctAnswer: 0,
    explanation: 'AQE coalesces shuffle partitions but will not reduce below this minimum, ensuring enough parallelism remains even when individual partitions are tiny.',
    tags: ['aqe', 'partitioning', 'configuration', 'shuffle'],
  },
  {
    id: 'scala-spark-l4-033',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What is a custom `Partitioner` in RDD API used for?',
    options: [
      'Controlling which columns are written to each output file',
      'Defining a custom hash or range scheme to determine which partition a key goes to, enabling downstream co-partitioning',
      'Splitting large partitions into smaller ones during a shuffle',
      'Assigning executor affinity to specific partitions',
    ],
    correctAnswer: 1,
    explanation: 'Subclassing `Partitioner` and overriding `numPartitions` / `getPartition` lets you co-partition two RDDs identically, enabling `join` and `cogroup` without a shuffle.',
    tags: ['rdd', 'partitioner', 'shuffle', 'co-partitioning'],
  },
  {
    id: 'scala-spark-l4-034',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What is shuffle spill and how does it impact job performance?',
    options: [
      'Shuffle spill occurs when sort buffers overflow to disk, increasing I/O and slowing shuffle write/read',
      'Shuffle spill is when data is lost due to executor failure during shuffle',
      'Shuffle spill happens when broadcast variables exceed memory limits',
      'Shuffle spill refers to data recomputed from lineage after a node failure',
    ],
    correctAnswer: 0,
    explanation: 'When the in-memory sort buffer fills during shuffle write, Spark spills sorted chunks to disk and merges them later, adding significant disk I/O overhead.',
    tags: ['shuffle', 'spill', 'memory', 'performance'],
  },
  {
    id: 'scala-spark-l4-035',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What does `spark.locality.wait` control and why does it matter?',
    options: [
      'How long Spark waits before reducing task locality level to schedule a task on a less-preferred node',
      'How long an executor waits for a shuffle file before failing',
      'The heartbeat interval between driver and executors',
      'The timeout for broadcast variable delivery',
    ],
    correctAnswer: 0,
    explanation: 'Spark prefers data-local task scheduling; `locality.wait` is the delay before relaxing from PROCESS_LOCAL → NODE_LOCAL → RACK_LOCAL → ANY, trading latency for data locality.',
    tags: ['scheduling', 'locality', 'configuration'],
  },
  {
    id: 'scala-spark-l4-036',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'What is the difference between `cache()` and `persist(StorageLevel.MEMORY_ONLY)`?',
    options: [
      '`cache()` uses disk fallback; `persist(MEMORY_ONLY)` does not',
      '`cache()` is shorthand for `persist(MEMORY_ONLY)`; they are identical',
      '`cache()` stores data serialized; `persist(MEMORY_ONLY)` stores it deserialized',
      '`cache()` works only on DataFrames; `persist` works only on RDDs',
    ],
    correctAnswer: 1,
    explanation: '`cache()` simply calls `persist(StorageLevel.MEMORY_ONLY)` — both store deserialized Java objects in executor heap memory with no disk fallback.',
    tags: ['caching', 'persist', 'storage-level'],
  },
  {
    id: 'scala-spark-l4-037',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'When is it counter-productive to cache a DataFrame?',
    options: [
      'When the DataFrame is used more than twice in the same job',
      'When the DataFrame is used only once or the computation is trivial relative to serialization/deserialization cost',
      'When the DataFrame has more than 100 columns',
      'When the DataFrame was read from Parquet',
    ],
    correctAnswer: 1,
    explanation: 'Caching consumes memory and incurs serialization overhead on first materialization; if the DataFrame is scanned only once, caching wastes resources without benefit.',
    tags: ['caching', 'performance', 'memory'],
  },
  {
    id: 'scala-spark-l4-038',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'What happens to cached RDD/DataFrame partitions when executor memory is full?',
    options: [
      'Spark throws an OOM exception and aborts the job',
      'Spark evicts partitions using an LRU policy; evicted partitions are recomputed from lineage on next access',
      'Spark checkpoints evicted partitions to HDFS automatically',
      'Spark moves evicted partitions to driver memory',
    ],
    correctAnswer: 1,
    explanation: 'The Spark storage memory uses LRU eviction. Evicted partitions are transparently recomputed by replaying their lineage, adding latency but avoiding failure.',
    tags: ['caching', 'memory', 'eviction', 'lru'],
  },
  {
    id: 'scala-spark-l4-039',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'What is the difference between `DISK_ONLY` and `MEMORY_AND_DISK` storage levels?',
    options: [
      '`DISK_ONLY` stores on disk exclusively; `MEMORY_AND_DISK` tries memory first and spills to disk',
      '`MEMORY_AND_DISK` is always faster because disk is never used',
      '`DISK_ONLY` replicates to two disks; `MEMORY_AND_DISK` does not replicate',
      'Both are identical in Spark 3.x',
    ],
    correctAnswer: 0,
    explanation: '`MEMORY_AND_DISK` keeps partitions in heap memory and spills overflow to disk, while `DISK_ONLY` skips memory entirely and serializes all partitions directly to executor local disk.',
    tags: ['caching', 'storage-level', 'disk'],
  },
  {
    id: 'scala-spark-l4-040',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is the role of a checkpoint location in Structured Streaming?',
    options: [
      'It stores the final output of each micro-batch for auditing',
      'It persists query progress, offsets, and state to enable fault-tolerant exactly-once processing and query restarts',
      'It caches streaming DataFrames to avoid recomputation',
      'It limits the number of active streaming queries',
    ],
    correctAnswer: 1,
    explanation: 'The checkpoint directory stores committed offsets and operator state to HDFS/S3, allowing a failed or restarted streaming query to resume from exactly where it left off.',
    tags: ['streaming', 'checkpoint', 'fault-tolerance', 'exactly-once'],
  },
  {
    id: 'scala-spark-l4-041',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is the difference between `Complete` and `Append` output modes in Structured Streaming?',
    options: [
      '`Append` writes the entire result table each trigger; `Complete` writes only new rows',
      '`Complete` writes the full result table each trigger; `Append` writes only rows added since last trigger',
      'Both modes write the same data; the difference is in sink format',
      '`Complete` mode requires a watermark; `Append` does not',
    ],
    correctAnswer: 1,
    explanation: '`Complete` mode is for aggregations that need the full updated result emitted each trigger. `Append` mode emits only finalized rows that will never be updated, requiring a watermark for stateful aggregations.',
    tags: ['streaming', 'output-mode', 'append', 'complete'],
  },
  {
    id: 'scala-spark-l4-042',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is a watermark in Structured Streaming and what problem does it solve?',
    options: [
      'A threshold that limits how many records per batch are processed',
      'A maximum allowed delay for late-arriving data; Spark drops data older than the watermark and clears state',
      'A minimum batch interval to ensure trigger stability',
      'A memory limit that triggers state eviction in stream-stream joins',
    ],
    correctAnswer: 1,
    explanation: '`withWatermark` tells Spark the maximum expected event-time lateness. State for windows older than `(maxEventTime - watermark delay)` is evicted, bounding state store size.',
    tags: ['streaming', 'watermark', 'late-data', 'state'],
  },
  {
    id: 'scala-spark-l4-043',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'How does `foreachBatch` differ from `foreach` in Structured Streaming sinks?',
    options: [
      '`foreachBatch` provides the micro-batch DataFrame to a user function enabling arbitrary batch operations; `foreach` processes one row at a time',
      '`foreach` is faster because it avoids DataFrame overhead',
      '`foreachBatch` requires the sink to support transactions',
      'Both are identical; `foreachBatch` is just an alias',
    ],
    correctAnswer: 0,
    explanation: '`foreachBatch` gives access to the full micro-batch as a DataFrame, allowing reuse of batch DataFrame/Dataset APIs (e.g., writing to multiple sinks, deduplication with Delta Lake).',
    tags: ['streaming', 'sink', 'foreachBatch', 'micro-batch'],
  },
  {
    id: 'scala-spark-l4-044',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What guarantees does Structured Streaming provide with Kafka source and a transactional sink?',
    options: [
      'At-most-once delivery',
      'Exactly-once end-to-end semantics when the sink is idempotent or transactional and checkpointing is enabled',
      'At-least-once delivery only, regardless of sink',
      'No delivery guarantees without custom code',
    ],
    correctAnswer: 1,
    explanation: 'With idempotent writes or transactional sinks + WAL checkpointing of Kafka offsets, Structured Streaming achieves exactly-once semantics end-to-end.',
    tags: ['streaming', 'kafka', 'exactly-once', 'fault-tolerance'],
  },
  {
    id: 'scala-spark-l4-045',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is `mapGroupsWithState` used for in Structured Streaming?',
    options: [
      'Applying a stateless transformation grouped by key',
      'Maintaining arbitrary per-key user-defined state across micro-batches with custom timeout logic',
      'Joining two streaming DataFrames on a group key',
      'Writing grouped aggregations to a custom sink',
    ],
    correctAnswer: 1,
    explanation: '`mapGroupsWithState` lets you define custom stateful logic (update/read arbitrary state per key) and set per-key timeouts, giving full control beyond built-in window aggregations.',
    tags: ['streaming', 'state', 'mapGroupsWithState', 'stateful'],
  },
  {
    id: 'scala-spark-l4-046',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'Why can Spark not optimize inside a Scala UDF?',
    options: [
      'Scala UDFs are compiled to Python bytecode, which Catalyst cannot parse',
      'Spark treats UDFs as opaque black boxes — the function body is invisible to Catalyst for predicate pushdown, constant folding, etc.',
      'UDFs are executed on the driver, not executors',
      'Spark can fully optimize UDFs using whole-stage code generation',
    ],
    correctAnswer: 1,
    explanation: 'Catalyst only optimizes its own expression tree. A Scala closure registered as a UDF is an opaque `(Any*) => Any`; Catalyst cannot inspect or rewrite it.',
    tags: ['udf', 'catalyst', 'optimization', 'black-box'],
  },
  {
    id: 'scala-spark-l4-047',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'How do you register a typed UDF for use in DataFrame column expressions?',
    options: [
      '`spark.udf.register("name", udf((x: Int) => x * 2))`',
      '`val myUdf = udf((x: Int) => x * 2)` then `df.withColumn("c", myUdf(col("a")))`',
      '`df.mapPartitions(_.map(r => r.getInt(0) * 2))`',
      '`spark.catalog.createFunction("name", (x: Int) => x * 2)`',
    ],
    correctAnswer: 1,
    explanation: 'The canonical pattern is wrapping the lambda with `udf(...)` to produce a `UserDefinedFunction`, then applying it as a column expression via `myUdf(col("a"))`.',
    codeSnippet: `val double = udf((x: Int) => x * 2)
df.withColumn("doubled", double(col("value")))`,
    tags: ['udf', 'dataframe', 'column-expression'],
  },
  {
    id: 'scala-spark-l4-048',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'What is the performance penalty of Python UDFs (PySpark) compared to native Spark SQL functions?',
    options: [
      'No penalty — Spark JIT-compiles Python UDFs',
      'Row-by-row serialization between JVM and Python process, causing high IPC overhead',
      'Python UDFs run only on the driver, adding network round-trips',
      'Python UDFs are 10% slower due to GIL contention',
    ],
    correctAnswer: 1,
    explanation: 'Each row must be serialized from JVM to Python (via Py4J/pickle) and the result serialized back, creating severe per-row overhead versus in-JVM Catalyst expressions.',
    tags: ['udf', 'python', 'performance', 'serialization'],
  },
  {
    id: 'scala-spark-l4-049',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'How can you create a UDF that handles null values without throwing a NullPointerException?',
    options: [
      'Use `udf.asNondeterministic()` to skip null rows',
      'Declare the UDF input type as `Option[T]` or call `.asNullSafe` on it',
      'Use `nullSafeUdf` wrapper from spark-functions',
      'Spark automatically wraps all UDFs in null checks',
    ],
    correctAnswer: 1,
    explanation: 'Declaring UDF parameters as `Option[T]` causes Spark to pass `None` for null inputs instead of raw nulls that would cause NPE in the JVM function body.',
    codeSnippet: `val safeLen = udf((s: Option[String]) => s.map(_.length).getOrElse(0))`,
    tags: ['udf', 'null-handling', 'option', 'safety'],
  },
  {
    id: 'scala-spark-l4-050',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'What does marking a UDF as `nondeterministic` do?',
    options: [
      'It caches the UDF result per unique input value',
      'It prevents Catalyst from caching or reordering calls to the UDF, ensuring it is called once per row per invocation',
      'It makes the UDF run on the driver instead of executors',
      'It disables null checking for the UDF inputs',
    ],
    correctAnswer: 1,
    explanation: 'By default Catalyst may eliminate duplicate UDF calls with the same inputs. `nondeterministic` (e.g., `udf(...).asNondeterministic()`) prevents this, necessary for functions like random or timestamp-based logic.',
    tags: ['udf', 'nondeterministic', 'catalyst', 'optimization'],
  },
  {
    id: 'scala-spark-l4-051',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'Why does Spark prefer Kryo serialization over Java serialization for RDDs?',
    options: [
      'Kryo supports more Java types than the standard serializer',
      'Kryo is significantly faster and produces smaller serialized output, reducing shuffle and cache overhead',
      'Kryo is required for Dataset encoders',
      'Java serialization is deprecated in Spark 3.x',
    ],
    correctAnswer: 1,
    explanation: 'Kryo uses a compact binary format and avoids Java reflection overhead, typically 10x faster and 3-5x smaller than Java serialization for RDD shuffle and persist operations.',
    tags: ['serialization', 'kryo', 'performance', 'rdd'],
  },
  {
    id: 'scala-spark-l4-052',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'How do you register a custom class for Kryo serialization in Spark?',
    options: [
      '`spark.conf.set("spark.kryo.registrator", classOf[MyRegistrator].getName)`',
      '`spark.sparkContext.addJar(classOf[MyClass].getName)`',
      '`SparkKryo.register(classOf[MyClass])`',
      '`spark.conf.set("spark.serializer.classes", "com.MyClass")`',
    ],
    correctAnswer: 0,
    explanation: 'Implement `KryoRegistrator`, call `kryo.register(classOf[MyClass])` inside it, then set `spark.kryo.registrator` to point to your registrator class.',
    tags: ['serialization', 'kryo', 'configuration', 'registrator'],
  },
  {
    id: 'scala-spark-l4-053',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'What causes a `NotSerializableException` when using Spark with Scala closures?',
    options: [
      'Using `val` instead of `var` inside a closure',
      'The closure captures a non-serializable outer object (e.g., a class instance), which Spark must serialize to send to executors',
      'Passing a DataFrame as a parameter to an RDD `map`',
      'Using implicits inside a closure',
    ],
    correctAnswer: 1,
    explanation: 'Spark serializes closures and all captured references. If a closure captures `this` (an outer class instance) and that class is not `Serializable`, the job fails with `NotSerializableException`.',
    tags: ['serialization', 'closure', 'notserializable', 'rdd'],
  },
  {
    id: 'scala-spark-l4-054',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'What is Project Tungsten\'s off-heap memory management and why does it help GC?',
    options: [
      'Tungsten allocates JVM objects on the young generation to speed up allocation',
      'Tungsten manages binary data in native (off-heap) memory using sun.misc.Unsafe, bypassing JVM GC entirely',
      'Tungsten uses G1 GC exclusively to minimize pause times',
      'Tungsten moves all shuffle data to the driver heap to avoid executor GC',
    ],
    correctAnswer: 1,
    explanation: 'By storing data in raw binary off-heap memory, Tungsten avoids creating millions of JVM objects, eliminating GC pressure and long stop-the-world pauses that degrade Spark performance.',
    tags: ['tungsten', 'memory', 'gc', 'off-heap', 'performance'],
  },
  {
    id: 'scala-spark-l4-055',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'What is the purpose of `spark.sql.execution.arrow.pyspark.enabled`?',
    options: [
      'Enables vectorized reading of Arrow IPC files from S3',
      'Uses Apache Arrow columnar format for zero-copy data transfer between JVM and Python, greatly speeding up Pandas UDFs',
      'Activates Arrow-based shuffle for sort-merge joins',
      'Converts Parquet files to Arrow format at query time',
    ],
    correctAnswer: 1,
    explanation: 'With Arrow enabled, PySpark transfers columnar batches via shared memory instead of row-by-row pickling, dramatically reducing serialization overhead for Pandas UDFs and `toPandas()`.',
    tags: ['arrow', 'pyspark', 'pandas-udf', 'serialization', 'performance'],
  },
  {
    id: 'scala-spark-l4-056',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the effect of calling `rdd.unpersist()` before the job that reads the cached data has finished?',
    options: [
      'Spark waits for all jobs using the cache to complete before removing it',
      'Cached partitions are removed from storage immediately; subsequent tasks recompute from lineage',
      'The call is ignored if any job is currently using the RDD',
      'Spark raises a `ConcurrentModificationException`',
    ],
    correctAnswer: 1,
    explanation: '`unpersist()` (by default with `blocking=false`) asynchronously removes partitions from the block manager; tasks that needed them will recompute via lineage.',
    tags: ['rdd', 'caching', 'unpersist', 'lineage'],
  },
  {
    id: 'scala-spark-l4-057',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the `aggregate` action on an RDD and how does it differ from `fold`?',
    options: [
      '`aggregate` allows a different zero value and return type from the element type; `fold` requires the same type',
      '`fold` supports parallel reduce; `aggregate` is sequential',
      '`aggregate` runs on the driver only; `fold` runs on executors',
      'Both are identical; `aggregate` is the deprecated name for `fold`',
    ],
    correctAnswer: 0,
    explanation: '`aggregate(zero)(seqOp, combOp)` lets the accumulator type differ from element type (e.g., summing integers into a `(sum, count)` tuple), while `fold` requires the same type for elements and accumulator.',
    tags: ['rdd', 'aggregate', 'fold', 'action'],
  },
  {
    id: 'scala-spark-l4-058',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'How does `zipPartitions` differ from `zip` on RDDs?',
    options: [
      '`zip` combines element-wise requiring equal partition counts and sizes; `zipPartitions` combines iterators per partition allowing different element counts',
      '`zipPartitions` always triggers a shuffle; `zip` does not',
      '`zip` works on DataFrames; `zipPartitions` is RDD-only',
      'Both are identical but `zipPartitions` is the newer API',
    ],
    correctAnswer: 0,
    explanation: '`zipPartitions` passes full iterators from each RDD partition to a user function, allowing flexible element-count-independent merging, unlike `zip` which requires strict element parity.',
    tags: ['rdd', 'zipPartitions', 'partitions'],
  },
  {
    id: 'scala-spark-l4-059',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does `df.createOrReplaceTempView` vs `df.createOrReplaceGlobalTempView` differ in scope?',
    options: [
      'Both are session-scoped; the "Global" version just persists after restart',
      'Temp view is session-scoped and dropped when the session ends; global temp view is application-scoped, accessible across sessions via `global_temp` database',
      'Global temp view is persisted to disk; temp view is in-memory only',
      'There is no difference in Spark 3.x',
    ],
    correctAnswer: 1,
    explanation: 'Temp views are tied to the `SparkSession` that created them. Global temp views persist for the `SparkContext` lifetime and are accessible from any session as `global_temp.viewname`.',
    tags: ['dataframe', 'tempview', 'sparksql', 'scope'],
  },
  {
    id: 'scala-spark-l4-060',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is the risk of calling `df.schema` on a DataFrame derived from a wide transformation?',
    options: [
      'It triggers full query execution to infer the schema',
      'Schema resolution is lazy metadata only for most transformations, but some sources (e.g., JSON inference) trigger a data scan',
      'It always causes a shuffle to validate column types',
      'It raises `AnalysisException` if columns are ambiguous',
    ],
    correctAnswer: 1,
    explanation: 'For schema-inferred sources like JSON without a user-provided schema, Spark samples or fully scans data to infer types — calling `.schema` too early triggers this scan.',
    tags: ['dataframe', 'schema', 'inference', 'lazy'],
  },
  {
    id: 'scala-spark-l4-061',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How does `bucketBy` in DataFrameWriter improve subsequent join performance?',
    options: [
      'It writes data sorted by the bucket column, enabling merge-sort joins without reading the full dataset',
      'It pre-partitions data on disk by the bucket column, eliminating the shuffle step for joins on the same column with the same bucket count',
      'It creates a Hive partition directory structure for the column',
      'It creates a bitmap index on the column for fast point lookups',
    ],
    correctAnswer: 1,
    explanation: 'Tables bucketed on the same column with the same bucket count are already hash-partitioned on disk; Spark can join them without any shuffle, dramatically reducing I/O for repeated joins.',
    tags: ['dataframe', 'bucketBy', 'join', 'optimization'],
  },
  {
    id: 'scala-spark-l4-062',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is `df.na.fill` and what are its limitations?',
    options: [
      'Fills null values with a specified value per column; it cannot fill different values for different columns in one call',
      'Fills null values; limitation is it works only on String columns',
      'Fills nulls by interpolating adjacent row values',
      'Fills nulls with a broadcast variable value, limited to 100 MB',
    ],
    correctAnswer: 0,
    explanation: '`na.fill(Map("col1" -> 0, "col2" -> "N/A"))` supports per-column values, but it cannot perform conditional fills or use row-context values — for those you need `when/otherwise`.',
    tags: ['dataframe', 'null-handling', 'na-fill'],
  },
  {
    id: 'scala-spark-l4-063',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'What is the purpose of `ANALYZE TABLE ... COMPUTE STATISTICS FOR COLUMNS` in SparkSQL?',
    options: [
      'Computes partition counts for each Hive partition',
      'Collects column-level statistics (min, max, distinct count, nulls) used by the Cost-Based Optimizer for join reordering',
      'Rebuilds Parquet row group indexes',
      'Validates data types against the registered schema',
    ],
    correctAnswer: 1,
    explanation: 'CBO uses per-column statistics to estimate cardinality and choose optimal join order/strategy. Without running `ANALYZE TABLE ... FOR COLUMNS`, the CBO relies on rough estimates.',
    tags: ['cbo', 'statistics', 'analyze', 'catalyst'],
  },
  {
    id: 'scala-spark-l4-064',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'What Catalyst optimization does `df.select("a").where($"a" > 5)` benefit from that a raw RDD would not?',
    options: [
      'Automatic caching of the filtered result',
      'Predicate pushdown and column pruning combined: only relevant rows and the single column are read from source',
      'Automatic partitioning by column "a"',
      'Compile-time type verification of the filter predicate',
    ],
    correctAnswer: 1,
    explanation: 'Catalyst rewrites the plan to push the filter and projection to the scan operator, so the data source reads only column "a" and only rows where `a > 5` — eliminating unnecessary I/O.',
    tags: ['catalyst', 'predicate-pushdown', 'column-pruning', 'optimization'],
  },
  {
    id: 'scala-spark-l4-065',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'What happens during the "analysis" phase of Catalyst query planning?',
    options: [
      'Logical rules are applied to minimize data shuffled',
      'Unresolved attribute and relation references are resolved against the catalog, and types are checked',
      'Physical operators are selected and cost-estimated',
      'Whole-stage code generation templates are filled in',
    ],
    correctAnswer: 1,
    explanation: 'The Analyzer uses the Catalog to resolve table/column names, check types, and replace unresolved references with concrete `AttributeReference` nodes, producing a fully resolved logical plan.',
    tags: ['catalyst', 'analysis', 'analyzer', 'internals'],
  },
  {
    id: 'scala-spark-l4-066',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'What is a cross join (Cartesian product) and when does Spark require explicit opt-in?',
    options: [
      'A join on non-equi predicates; always requires `spark.sql.crossJoin.enabled=true`',
      'A join with no condition producing M×N rows; requires `spark.sql.crossJoin.enabled=true` (Spark < 3.x) or an explicit `crossJoin()` call',
      'A join between two streaming DataFrames',
      'A join that shuffles both sides without hashing',
    ],
    correctAnswer: 1,
    explanation: 'Cartesian products are expensive and accidental; Spark guards against implicit cross joins by requiring explicit `crossJoin()` or the config flag, preventing unintentional quadratic blowup.',
    tags: ['join', 'cross-join', 'cartesian', 'configuration'],
  },
  {
    id: 'scala-spark-l4-067',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'How does a stream-stream join in Structured Streaming maintain bounded state?',
    options: [
      'Stream-stream joins have unbounded state and always require manual state cleanup',
      'Using watermarks on both sides; Spark evicts join state older than the watermark delay, bounding memory',
      'Spark uses a TTL cache that evicts entries after a fixed wall-clock time',
      'Stream-stream joins are not supported in Spark 3.x',
    ],
    correctAnswer: 1,
    explanation: 'Both input streams need watermarks; Spark evicts buffered rows from each side that are older than the watermark, ensuring join state does not grow unboundedly.',
    tags: ['streaming', 'join', 'watermark', 'state', 'stream-stream'],
  },
  {
    id: 'scala-spark-l4-068',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What does `spark.sql.adaptive.advisoryPartitionSizeInBytes` control in AQE?',
    options: [
      'The target size for coalesced shuffle partitions after AQE merging',
      'The maximum size of a broadcast variable',
      'The initial partition size before any shuffle',
      'The minimum size below which Spark skips writing a partition file',
    ],
    correctAnswer: 0,
    explanation: 'AQE uses this advisory size (default 64 MB) as the target when coalescing small shuffle partitions, aiming to produce output partitions close to this size.',
    tags: ['aqe', 'partitioning', 'configuration', 'shuffle'],
  },
  {
    id: 'scala-spark-l4-069',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'Why can broadcasting a large table cause executor OOM errors?',
    options: [
      'The broadcast serializes data using slow Java serialization, consuming extra memory during transfer',
      'Each executor holds a full copy of the broadcast table in JVM heap; if too large, it causes heap OOM',
      'Broadcasting triggers an extra sort on every executor',
      'Broadcast data is stored in off-heap memory, which has a 2 GB limit',
    ],
    correctAnswer: 1,
    explanation: 'Each executor materializes the entire broadcast dataset into its heap memory. Broadcasting a multi-GB table multiplied across all executors can exhaust executor heap on memory-constrained clusters.',
    tags: ['broadcast', 'memory', 'oom', 'join'],
  },
  {
    id: 'scala-spark-l4-070',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What is the "fetch failed" error in Spark shuffle and what causes it?',
    options: [
      'An executor failed to read its own local shuffle files due to disk corruption',
      'A reducer task could not fetch shuffle map output from an executor that died or had its shuffle files cleaned up, causing the stage to retry',
      'The driver ran out of memory fetching shuffle statistics',
      'A shuffle fetch exceeded the `spark.network.timeout` due to slow network',
    ],
    correctAnswer: 1,
    explanation: 'If a map-stage executor fails after writing shuffle files, reducer tasks fail with `FetchFailedException`. Spark retries the map stage and then the reduce stage, adding significant latency.',
    tags: ['shuffle', 'fault-tolerance', 'fetch-failed', 'internals'],
  },
  {
    id: 'scala-spark-l4-071',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What is Spark\'s `EXPLAIN FORMATTED` and how does it differ from `EXPLAIN`?',
    options: [
      '`EXPLAIN FORMATTED` shows a JSON representation; `EXPLAIN` shows plain text',
      '`EXPLAIN FORMATTED` is only available in Python; `EXPLAIN` works in all languages',
      '`EXPLAIN FORMATTED` provides structured output with node IDs, metrics placeholders, and plan details suitable for programmatic parsing',
      'Both are identical — `FORMATTED` is an alias in Spark 3.x',
    ],
    correctAnswer: 2,
    explanation: '`EXPLAIN FORMATTED` (Spark 3.0+) produces a structured, human-readable plan with explicit node IDs and formatting that makes it easier to correlate with Spark UI metrics.',
    tags: ['sparksql', 'explain', 'debugging', 'catalyst'],
  },
  {
    id: 'scala-spark-l4-072',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What does a WINDOW function with `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` compute?',
    options: [
      'A rolling window of fixed size preceding the current row',
      'A cumulative aggregate from the first row of the partition up to and including the current row',
      'The entire partition result applied to the current row',
      'The average over all rows following the current row',
    ],
    correctAnswer: 1,
    explanation: 'This frame specification defines a cumulative (running total) window: from partition start to current row, used for running sums, running counts, etc.',
    tags: ['sparksql', 'window-function', 'frame', 'cumulative'],
  },
  {
    id: 'scala-spark-l4-073',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What is the Spark `catalog` API and what operations does it expose?',
    options: [
      'An API to list and manage databases, tables, columns, and functions registered in the metastore',
      'A low-level API for reading Parquet footer metadata',
      'A plugin interface for custom data source connectors',
      'A REST API for submitting Spark jobs to a cluster',
    ],
    correctAnswer: 0,
    explanation: '`spark.catalog` provides methods like `listTables()`, `listColumns()`, `tableExists()`, `refreshTable()` to introspect and manage metadata in the Spark/Hive metastore.',
    tags: ['sparksql', 'catalog', 'metastore', 'api'],
  },
  {
    id: 'scala-spark-l4-074',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is the `Update` output mode and when can it be used?',
    options: [
      'It writes only the rows whose aggregated values changed since the last trigger; requires aggregation and is not supported by all sinks',
      'It overwrites all previously written output with the new result',
      'It is identical to `Append` but allows late-data updates',
      'It is used for streaming deduplication operations only',
    ],
    correctAnswer: 0,
    explanation: '`Update` mode emits only changed aggregated rows each trigger (not the full result table), making it efficient for key-value sinks like Cassandra, but not supported by file-based sinks.',
    tags: ['streaming', 'output-mode', 'update', 'aggregation'],
  },
  {
    id: 'scala-spark-l4-075',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is the difference between `processingTime` and `once` trigger modes in Structured Streaming?',
    options: [
      '`processingTime` processes one micro-batch per clock tick; `once` processes all available data in a single batch and stops',
      '`once` is for Kafka sources only; `processingTime` works with all sources',
      '`processingTime` is deprecated; `once` replaces it in Spark 3.3+',
      'Both are identical; the trigger name is user-defined',
    ],
    correctAnswer: 0,
    explanation: '`Trigger.Once()` is ideal for scheduled batch-like runs: process all pending data in one micro-batch then stop, useful for cost-optimized periodic ETL via streaming APIs.',
    tags: ['streaming', 'trigger', 'once', 'batch'],
  },
  {
    id: 'scala-spark-l4-076',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'How does `flatMapGroupsWithState` differ from `mapGroupsWithState`?',
    options: [
      '`flatMapGroupsWithState` can emit zero or more output rows per group per trigger; `mapGroupsWithState` emits exactly one',
      '`flatMapGroupsWithState` does not support timeouts; `mapGroupsWithState` does',
      '`flatMapGroupsWithState` works on DataFrames; `mapGroupsWithState` works on RDDs',
      'Both are identical; `flatMapGroupsWithState` is the older name',
    ],
    correctAnswer: 0,
    explanation: '`flatMapGroupsWithState` returns an `Iterator[U]`, allowing variable output cardinality per group, while `mapGroupsWithState` must return exactly one output row per group per trigger.',
    tags: ['streaming', 'state', 'flatMapGroupsWithState', 'stateful'],
  },
  {
    id: 'scala-spark-l4-077',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the `toDebugString` method on an RDD useful for?',
    options: [
      'Printing the first 20 rows of the RDD for debugging',
      'Displaying the full RDD lineage as a human-readable DAG string for debugging dependency chains',
      'Printing executor allocation per partition',
      'Generating a serialized snapshot of the RDD for checkpointing',
    ],
    correctAnswer: 1,
    explanation: '`rdd.toDebugString` returns a multi-line string showing the RDD lineage graph with indentation indicating dependency depth, helping diagnose long chains or missing caches.',
    tags: ['rdd', 'debugging', 'lineage', 'toDebugString'],
  },
  {
    id: 'scala-spark-l4-078',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the purpose of `sc.broadcast` and what are the memory implications?',
    options: [
      'Broadcasts an RDD to all executors for faster joins; each executor stores a full copy',
      'Efficiently distributes a read-only variable to all executors via a BitTorrent-like protocol; each executor node gets one copy in memory',
      'Sends an accumulator value to all executors for distributed aggregation',
      'Publishes a streaming schema to downstream consumers',
    ],
    correctAnswer: 1,
    explanation: 'Broadcast variables use a tree-based distribution protocol (not driver-to-each-executor) and are cached on each executor *node* (not per task), making lookups efficient but consuming heap per node.',
    tags: ['rdd', 'broadcast', 'memory', 'distributed'],
  },
  {
    id: 'scala-spark-l4-079',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does `spark.sql.parquet.filterPushdown` enable?',
    options: [
      'Pushes column projections to reduce Parquet I/O',
      'Passes DataFrame filter predicates to the Parquet reader so it can skip row groups using min/max statistics',
      'Enables dictionary encoding for string columns in Parquet output',
      'Converts Parquet timestamp columns to Spark TimestampType automatically',
    ],
    correctAnswer: 1,
    explanation: 'With filter pushdown enabled, Spark passes predicates (e.g., `age > 30`) to the Parquet reader, which uses column chunk statistics and bloom filters to skip irrelevant row groups without reading them.',
    tags: ['parquet', 'filter-pushdown', 'optimization', 'configuration'],
  },
  {
    id: 'scala-spark-l4-080',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is the issue with using `df.show()` inside a `foreachPartition` or `mapPartitions`?',
    options: [
      'It triggers a nested Spark job from within an executor, which is not supported and causes an exception',
      'It prints output to executor logs instead of driver stdout',
      'It is valid but causes a full data collect on each partition',
      'It only works if the DataFrame was created from the same SparkSession',
    ],
    correctAnswer: 0,
    explanation: 'Spark actions like `show()` or `collect()` cannot be called from within executor-side code — they attempt to schedule jobs via the driver, causing a deadlock or exception.',
    tags: ['dataframe', 'executor', 'antipattern', 'nested-action'],
  },
  {
    id: 'scala-spark-l4-081',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'How can you verify that a DataFrame is actually cached in Spark UI?',
    options: [
      'Check the "SQL" tab and look for a `Cache` node in the plan',
      'Check the "Storage" tab in Spark UI, which lists all cached RDDs/DataFrames with memory usage per partition',
      'Run `df.isStreaming` and check the result',
      'Call `spark.catalog.isCached(df)` which returns true when cached',
    ],
    correctAnswer: 1,
    explanation: 'The Spark UI "Storage" tab shows all persisted/cached datasets, their storage level, memory footprint, and fraction of partitions cached — the authoritative source for cache status.',
    tags: ['caching', 'spark-ui', 'monitoring', 'debugging'],
  },
  {
    id: 'scala-spark-l4-082',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What is the performance trade-off of using `Dataset[T]` with `.map` vs. DataFrame `.select` with column expressions?',
    options: [
      'Dataset map is always faster due to Tungsten binary encoding',
      'DataFrame column expressions are generally faster because they go through Catalyst and whole-stage codegen; Dataset map uses JVM serialization overhead for deserialization',
      'Both have identical performance when Kryo is enabled',
      'Dataset map avoids GC while DataFrame select does not',
    ],
    correctAnswer: 1,
    explanation: 'Dataset operations with lambdas require deserializing Tungsten binary rows into JVM objects (via Encoder), running the function, then re-encoding — a round trip that DataFrame column expressions avoid.',
    tags: ['dataset', 'dataframe', 'performance', 'encoder', 'codegen'],
  },
  {
    id: 'scala-spark-l4-083',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What happens when you `joinWith` two Datasets in Spark?',
    options: [
      'Returns a `Dataset[(T, U)]` of paired objects, preserving type safety unlike a plain join that returns `Row`',
      'Returns a `DataFrame` with flattened columns from both sides',
      'Triggers a full broadcast join regardless of size',
      'Raises a compile error if the join key types differ',
    ],
    correctAnswer: 0,
    explanation: '`ds1.joinWith(ds2, condition)` returns `Dataset[(T, U)]` — a tuple of typed objects — enabling type-safe access to both sides without casting through `Row`.',
    tags: ['dataset', 'joinWith', 'type-safety', 'join'],
  },
  {
    id: 'scala-spark-l4-084',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What is the impact of setting `spark.sql.codegen.wholeStage=false`?',
    options: [
      'Disables all SQL execution; only RDD API works',
      'Disables whole-stage code generation; each operator runs as a separate interpreted function call, typically causing 2-10x slowdown for CPU-bound queries',
      'Forces all queries to use interpreted mode, enabling better error messages',
      'Switches execution from Tungsten to classic Volcano iterator model only for UDFs',
    ],
    correctAnswer: 1,
    explanation: 'Disabling WSCG reverts to the Volcano iterator model where each operator calls `next()` on its child per row — virtual dispatch overhead makes it significantly slower for compute-intensive pipelines.',
    tags: ['codegen', 'wscg', 'configuration', 'performance'],
  },
  {
    id: 'scala-spark-l4-085',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'partitioning/shuffling',
    question: 'What is the `External Shuffle Service` in Spark and why is it important for dynamic allocation?',
    options: [
      'A separate process on each node that serves shuffle files independently of executor lifecycle, allowing executors to be removed without losing shuffle data',
      'A cloud storage service that replaces local shuffle disk with S3/GCS',
      'A shuffle manager that encrypts data in transit between executors',
      'A Spark driver component that coordinates shuffle file locations',
    ],
    correctAnswer: 0,
    explanation: 'With dynamic allocation, idle executors are reclaimed. The External Shuffle Service (running as a node daemon) retains and serves shuffle map outputs so reducers can still fetch them after the writing executor is removed.',
    tags: ['shuffle', 'dynamic-allocation', 'external-shuffle-service', 'yarn'],
  },
  {
    id: 'scala-spark-l4-086',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the difference between `sc.parallelize` and `sc.makeRDD`?',
    options: [
      '`makeRDD` accepts preferred locations (host hints) per partition element in addition to data; `parallelize` does not',
      '`parallelize` creates an RDD from a sequence; `makeRDD` reads from a file',
      'Both are identical; `makeRDD` is the internal implementation of `parallelize`',
      '`makeRDD` supports lazy evaluation; `parallelize` is eager',
    ],
    correctAnswer: 0,
    explanation: '`sc.makeRDD(seq: Seq[(T, Seq[String])])` lets you specify preferred node locations per element, enabling data-locality hints that `parallelize` does not support.',
    tags: ['rdd', 'parallelize', 'makeRDD', 'locality'],
  },
  {
    id: 'scala-spark-l4-087',
    language: 'scala',
    level: 'level4',
    category: 'Apache Spark',
    subcategory: 'Catalyst optimizer',
    question: 'What is a "Rule" in Catalyst optimizer terminology and how is it applied?',
    options: [
      'A runtime metric threshold that triggers query plan rewriting',
      'A `Rule[TreeType]` is a function that pattern-matches plan nodes and transforms them; the optimizer applies batches of rules to fixed-point convergence',
      'A configuration key that activates a specific optimizer feature',
      'A type-class constraint enforced during the analysis phase',
    ],
    correctAnswer: 1,
    explanation: 'Catalyst optimizer rules are `TreeNode => TreeNode` functions grouped into `Batch`es. Spark applies each batch repeatedly until the plan stops changing (fixed-point) or a max iteration limit is hit.',
    tags: ['catalyst', 'rule', 'optimizer', 'internals', 'fixed-point'],
  },
];

export default questions;
