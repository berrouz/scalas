import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'scala-spark-l1-001',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does RDD stand for in Apache Spark?',
    options: [
      'Resilient Distributed Dataset',
      'Remote Distributed Data',
      'Replicated Distributed Dataset',
      'Resilient Dynamic Data',
    ],
    correctAnswer: 0,
    explanation: 'RDD stands for Resilient Distributed Dataset. It is the fundamental data structure of Apache Spark — a fault-tolerant collection of elements that can be operated on in parallel.',
    tags: ['rdd', 'basics', 'terminology'],
  },
  {
    id: 'scala-spark-l1-002',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'How do you create an RDD from a local Scala collection?',
    options: [
      'sc.parallelize(Seq(1, 2, 3))',
      'sc.createRDD(Seq(1, 2, 3))',
      'RDD.from(Seq(1, 2, 3))',
      'sc.makeRDD(Seq(1, 2, 3), distribute = true)',
    ],
    correctAnswer: 0,
    explanation: 'SparkContext.parallelize() is the standard method to create an RDD from a local Scala collection, distributing it across the cluster.',
    codeSnippet: 'val rdd = sc.parallelize(Seq(1, 2, 3, 4, 5))',
    tags: ['rdd', 'creation', 'sparkcontext'],
  },
  {
    id: 'scala-spark-l1-003',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'Which of the following is a transformation in Spark RDD?',
    options: [
      'collect()',
      'count()',
      'map()',
      'save()',
    ],
    correctAnswer: 2,
    explanation: 'map() is a transformation — it returns a new RDD. collect(), count(), and save() are actions that trigger computation and return results or write data.',
    codeSnippet: 'val doubled = rdd.map(x => x * 2) // transformation, lazy',
    tags: ['rdd', 'transformations', 'actions'],
  },
  {
    id: 'scala-spark-l1-004',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the difference between a transformation and an action in Spark?',
    options: [
      'Transformations run on the driver; actions run on executors',
      'Transformations are lazy and return a new RDD; actions trigger computation and return a result',
      'Transformations are eager; actions are lazy',
      'There is no difference — both return RDDs',
    ],
    correctAnswer: 1,
    explanation: 'Transformations (like map, filter) are lazy — they define a new RDD but do not execute immediately. Actions (like collect, count) trigger the actual computation and return a result to the driver or write data.',
    tags: ['rdd', 'lazy-evaluation', 'transformations', 'actions'],
  },
  {
    id: 'scala-spark-l1-005',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does the filter transformation do?',
    options: [
      'Returns only the first element of the RDD',
      'Returns a new RDD containing only elements that satisfy the predicate',
      'Removes duplicate elements from the RDD',
      'Converts the RDD to a DataFrame',
    ],
    correctAnswer: 1,
    explanation: 'filter() returns a new RDD containing only the elements for which the provided function returns true.',
    codeSnippet: 'val evens = rdd.filter(x => x % 2 == 0)',
    tags: ['rdd', 'filter', 'transformations'],
  },
  {
    id: 'scala-spark-l1-006',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does the flatMap transformation do differently from map?',
    options: [
      'flatMap applies a function that returns an Iterable and then flattens the results into a single RDD',
      'flatMap runs faster than map because it skips null values',
      'flatMap only works on RDDs of strings',
      'flatMap and map are identical in behavior',
    ],
    correctAnswer: 0,
    explanation: 'flatMap applies a function that returns an Iterable (e.g., a list) for each element, then flattens all those iterables into a single RDD. map returns exactly one element per input element.',
    codeSnippet: 'val words = lines.flatMap(line => line.split(" "))',
    tags: ['rdd', 'flatmap', 'transformations'],
  },
  {
    id: 'scala-spark-l1-007',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does collect() do in Spark?',
    options: [
      'Saves the RDD to HDFS',
      'Returns all elements of the RDD to the driver as a Scala Array',
      'Groups elements by key',
      'Collects statistics about the RDD',
    ],
    correctAnswer: 1,
    explanation: 'collect() is an action that retrieves all elements of the RDD and returns them as a Scala Array to the driver program. Be careful using this on large datasets as it can cause out-of-memory errors on the driver.',
    codeSnippet: 'val arr: Array[Int] = rdd.collect()',
    tags: ['rdd', 'actions', 'collect'],
  },
  {
    id: 'scala-spark-l1-008',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'How do you count the number of elements in an RDD?',
    options: [
      'rdd.size()',
      'rdd.length()',
      'rdd.count()',
      'rdd.total()',
    ],
    correctAnswer: 2,
    explanation: 'count() is an action that returns the number of elements in the RDD as a Long.',
    codeSnippet: 'val n: Long = rdd.count()',
    tags: ['rdd', 'actions', 'count'],
  },
  {
    id: 'scala-spark-l1-009',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does reduceByKey do?',
    options: [
      'Sorts elements by key in ascending order',
      'Merges values for each key using an associative and commutative reduce function',
      'Removes duplicate keys from a Pair RDD',
      'Converts keys to integers',
    ],
    correctAnswer: 1,
    explanation: 'reduceByKey merges values for each key using an associative and commutative function. It performs partial aggregation on each partition before shuffling, making it more efficient than groupByKey.',
    codeSnippet: 'val wordCounts = pairs.reduceByKey(_ + _)',
    tags: ['rdd', 'pair-rdd', 'reducebykey'],
  },
  {
    id: 'scala-spark-l1-010',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is a DataFrame in Apache Spark?',
    options: [
      'An RDD of Array[Any] with named columns',
      'A distributed collection of data organized into named columns, similar to a table in a relational database',
      'A Scala case class that wraps an RDD',
      'A streaming data source',
    ],
    correctAnswer: 1,
    explanation: 'A DataFrame is a distributed collection of data organized into named columns. It is conceptually equivalent to a table in a relational database or a data frame in R/Python pandas.',
    tags: ['dataframe', 'basics'],
  },
  {
    id: 'scala-spark-l1-011',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you create a SparkSession in Scala?',
    options: [
      'new SparkSession(appName = "MyApp")',
      'SparkSession.builder().appName("MyApp").getOrCreate()',
      'SparkContext.session("MyApp")',
      'Session.spark("MyApp").build()',
    ],
    correctAnswer: 1,
    explanation: 'SparkSession is the entry point to Spark SQL and DataFrame API. It is created using the builder pattern: SparkSession.builder().appName(...).getOrCreate().',
    codeSnippet: `val spark = SparkSession.builder()
  .appName("MyApp")
  .master("local[*]")
  .getOrCreate()`,
    tags: ['sparksession', 'setup', 'dataframe'],
  },
  {
    id: 'scala-spark-l1-012',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you read a CSV file into a DataFrame?',
    options: [
      'spark.read.csv("path/to/file.csv")',
      'spark.load.csv("path/to/file.csv")',
      'DataFrame.readCSV("path/to/file.csv")',
      'spark.file.csv("path/to/file.csv")',
    ],
    correctAnswer: 0,
    explanation: 'spark.read.csv() reads a CSV file into a DataFrame. You can also chain options like .option("header", "true") and .option("inferSchema", "true").',
    codeSnippet: `val df = spark.read
  .option("header", "true")
  .option("inferSchema", "true")
  .csv("path/to/file.csv")`,
    tags: ['dataframe', 'read', 'csv'],
  },
  {
    id: 'scala-spark-l1-013',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'Which method shows the schema of a DataFrame?',
    options: [
      'df.schema()',
      'df.printSchema()',
      'df.showSchema()',
      'df.describe()',
    ],
    correctAnswer: 1,
    explanation: 'df.printSchema() prints the schema of the DataFrame in a tree format, showing column names and data types. df.schema returns the StructType object programmatically.',
    codeSnippet: 'df.printSchema()\n// root\n//  |-- name: string (nullable = true)\n//  |-- age: integer (nullable = true)',
    tags: ['dataframe', 'schema'],
  },
  {
    id: 'scala-spark-l1-014',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you select specific columns from a DataFrame?',
    options: [
      'df.get("name", "age")',
      'df.select("name", "age")',
      'df.pick("name", "age")',
      'df.columns("name", "age")',
    ],
    correctAnswer: 1,
    explanation: 'df.select() returns a new DataFrame with only the specified columns. You can pass column names as strings or Column objects.',
    codeSnippet: 'val result = df.select("name", "age")\n// or using Column objects\nval result2 = df.select(df("name"), df("age"))',
    tags: ['dataframe', 'select'],
  },
  {
    id: 'scala-spark-l1-015',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you filter rows in a DataFrame where age > 18?',
    options: [
      'df.where(df("age") > 18)',
      'df.filter(df("age") > 18)',
      'Both A and B are correct',
      'df.query("age > 18")',
    ],
    correctAnswer: 2,
    explanation: 'Both filter() and where() are equivalent in Spark DataFrames. They both accept a Column expression or a SQL string expression.',
    codeSnippet: 'val adults = df.filter(df("age") > 18)\n// equivalent:\nval adults2 = df.where("age > 18")',
    tags: ['dataframe', 'filter', 'where'],
  },
  {
    id: 'scala-spark-l1-016',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you display the first 20 rows of a DataFrame?',
    options: [
      'df.print()',
      'df.show()',
      'df.display()',
      'df.head(20)',
    ],
    correctAnswer: 1,
    explanation: 'df.show() displays the first 20 rows of the DataFrame in a tabular format. You can pass a number to show a different number of rows, e.g., df.show(5).',
    codeSnippet: 'df.show()       // shows first 20 rows\ndf.show(5)      // shows first 5 rows\ndf.show(false)  // does not truncate wide columns',
    tags: ['dataframe', 'show'],
  },
  {
    id: 'scala-spark-l1-017',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you add a new column to a DataFrame?',
    options: [
      'df.addColumn("newCol", expr)',
      'df.withColumn("newCol", expr)',
      'df.append("newCol", expr)',
      'df.createColumn("newCol", expr)',
    ],
    correctAnswer: 1,
    explanation: 'withColumn() returns a new DataFrame with the specified column added or replaced. The first argument is the column name; the second is a Column expression.',
    codeSnippet: 'val df2 = df.withColumn("ageInMonths", df("age") * 12)',
    tags: ['dataframe', 'withcolumn'],
  },
  {
    id: 'scala-spark-l1-018',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you rename a column in a DataFrame?',
    options: [
      'df.renameColumn("oldName", "newName")',
      'df.withColumnRenamed("oldName", "newName")',
      'df.rename("oldName", "newName")',
      'df.alter("oldName", "newName")',
    ],
    correctAnswer: 1,
    explanation: 'withColumnRenamed() returns a new DataFrame with the specified column renamed.',
    codeSnippet: 'val df2 = df.withColumnRenamed("age", "years")',
    tags: ['dataframe', 'withcolumnrenamed'],
  },
  {
    id: 'scala-spark-l1-019',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you drop a column from a DataFrame?',
    options: [
      'df.remove("age")',
      'df.delete("age")',
      'df.drop("age")',
      'df.exclude("age")',
    ],
    correctAnswer: 2,
    explanation: 'drop() returns a new DataFrame with the specified column(s) removed.',
    codeSnippet: 'val df2 = df.drop("age")\nval df3 = df.drop("age", "salary") // drop multiple columns',
    tags: ['dataframe', 'drop'],
  },
  {
    id: 'scala-spark-l1-020',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'How do you register a DataFrame as a temporary SQL view?',
    options: [
      'df.registerAsTable("myTable")',
      'df.createTempView("myTable")',
      'spark.registerTable(df, "myTable")',
      'df.asSQLTable("myTable")',
    ],
    correctAnswer: 1,
    explanation: 'createTempView() registers the DataFrame as a temporary view with the given name, which can then be queried using SQL. The view exists only for the duration of the SparkSession.',
    codeSnippet: 'df.createTempView("people")\nval result = spark.sql("SELECT * FROM people WHERE age > 18")',
    tags: ['sparksql', 'tempview'],
  },
  {
    id: 'scala-spark-l1-021',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'How do you run a SQL query on a registered view in Spark?',
    options: [
      'spark.query("SELECT * FROM people")',
      'spark.sql("SELECT * FROM people")',
      'spark.execute("SELECT * FROM people")',
      'SQL.run(spark, "SELECT * FROM people")',
    ],
    correctAnswer: 1,
    explanation: 'spark.sql() executes a SQL query and returns the result as a DataFrame.',
    codeSnippet: 'val result: DataFrame = spark.sql("SELECT name, age FROM people WHERE age > 21")',
    tags: ['sparksql', 'query'],
  },
  {
    id: 'scala-spark-l1-022',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What is the difference between createTempView and createOrReplaceTempView?',
    options: [
      'createTempView is faster; createOrReplaceTempView is slower',
      'createTempView throws an exception if a view with the same name already exists; createOrReplaceTempView replaces it',
      'createTempView creates a global view; createOrReplaceTempView creates a local view',
      'There is no difference between them',
    ],
    correctAnswer: 1,
    explanation: 'createTempView() raises an AnalysisException if a view with that name already exists. createOrReplaceTempView() silently replaces any existing view with the same name.',
    codeSnippet: 'df.createOrReplaceTempView("people") // safe to call multiple times',
    tags: ['sparksql', 'tempview'],
  },
  {
    id: 'scala-spark-l1-023',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What is a Dataset in Apache Spark?',
    options: [
      'A distributed collection of data that provides the benefits of both RDDs (type safety) and DataFrames (optimizations)',
      'A special type of RDD that only works with integers',
      'A cached DataFrame stored in memory',
      'An external data source connector',
    ],
    correctAnswer: 0,
    explanation: 'Dataset is a strongly-typed distributed collection of data. It combines the benefits of RDDs (compile-time type safety) and DataFrames (Catalyst optimizer, Tungsten execution). DataFrame is an alias for Dataset[Row].',
    tags: ['dataset', 'basics'],
  },
  {
    id: 'scala-spark-l1-024',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'How do you convert a DataFrame to a typed Dataset in Scala?',
    options: [
      'df.toDataset[Person]',
      'df.as[Person]',
      'Dataset.from[Person](df)',
      'df.cast[Person]',
    ],
    correctAnswer: 1,
    explanation: 'df.as[T] converts a DataFrame to a Dataset[T]. You need an implicit Encoder[T] in scope, which is automatically provided for case classes.',
    codeSnippet: `case class Person(name: String, age: Int)
val ds: Dataset[Person] = df.as[Person]`,
    tags: ['dataset', 'encoder', 'as'],
  },
  {
    id: 'scala-spark-l1-025',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'What is an Encoder in Spark Datasets?',
    options: [
      'A compression algorithm used for storing data on disk',
      'A mechanism that converts between JVM objects and Spark\'s internal binary format',
      'A class that converts DataFrames to RDDs',
      'A serialization library for network transmission only',
    ],
    correctAnswer: 1,
    explanation: 'An Encoder converts JVM objects (like case classes) to and from Spark\'s internal binary (Tungsten) format. Encoders are used in Datasets and enable compact storage and efficient execution without Java serialization.',
    tags: ['dataset', 'encoder', 'tungsten'],
  },
  {
    id: 'scala-spark-l1-026',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Datasets',
    question: 'How do you convert a Dataset back to a DataFrame?',
    options: [
      'ds.toDataFrame()',
      'ds.asDataFrame()',
      'ds.toDF()',
      'DataFrame.from(ds)',
    ],
    correctAnswer: 2,
    explanation: 'toDF() converts a Dataset[T] to a DataFrame (Dataset[Row]). You can optionally pass new column names as arguments.',
    codeSnippet: `val ds: Dataset[Person] = df.as[Person]
val df2: DataFrame = ds.toDF()
val df3: DataFrame = ds.toDF("fullName", "years") // rename columns`,
    tags: ['dataset', 'todf', 'dataframe'],
  },
  {
    id: 'scala-spark-l1-027',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'What does cache() do in Spark?',
    options: [
      'Writes the RDD or DataFrame to disk permanently',
      'Stores the RDD or DataFrame in memory (and optionally on disk) for faster future access',
      'Compresses the data to reduce network traffic',
      'Replicates data across all nodes',
    ],
    correctAnswer: 1,
    explanation: 'cache() persists an RDD or DataFrame in memory. Subsequent actions on the cached data will be much faster because Spark does not need to recompute the lineage. cache() is equivalent to persist(StorageLevel.MEMORY_AND_DISK).',
    codeSnippet: 'val cached = df.cache()\ncached.count() // triggers caching\ncached.show()  // reads from cache',
    tags: ['caching', 'persist', 'performance'],
  },
  {
    id: 'scala-spark-l1-028',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'How do you remove a cached RDD or DataFrame from memory?',
    options: [
      'rdd.delete()',
      'rdd.uncache()',
      'rdd.unpersist()',
      'spark.clearCache(rdd)',
    ],
    correctAnswer: 2,
    explanation: 'unpersist() removes the RDD or DataFrame from the Spark cache, freeing the memory.',
    codeSnippet: 'val cached = df.cache()\n// ... do work ...\ncached.unpersist() // free the memory',
    tags: ['caching', 'unpersist'],
  },
  {
    id: 'scala-spark-l1-029',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'caching',
    question: 'What storage level does cache() use by default?',
    options: [
      'MEMORY_ONLY',
      'DISK_ONLY',
      'MEMORY_AND_DISK',
      'OFF_HEAP',
    ],
    correctAnswer: 2,
    explanation: 'For DataFrames and Datasets, cache() uses MEMORY_AND_DISK storage level by default. For RDDs, cache() uses MEMORY_ONLY. MEMORY_AND_DISK spills to disk if data does not fit in memory.',
    tags: ['caching', 'storage-level', 'persist'],
  },
  {
    id: 'scala-spark-l1-030',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'partitioning',
    question: 'What is a partition in Spark?',
    options: [
      'A logical division of data stored on a single node that can be processed in parallel',
      'A physical server in the Spark cluster',
      'A checkpoint saved to disk',
      'A named section of a Hive table',
    ],
    correctAnswer: 0,
    explanation: 'A partition is a logical chunk of data that resides on a node in the cluster. Spark processes one partition per task, so the number of partitions determines the level of parallelism.',
    tags: ['partitioning', 'basics', 'parallelism'],
  },
  {
    id: 'scala-spark-l1-031',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'partitioning',
    question: 'How do you check the number of partitions in an RDD?',
    options: [
      'rdd.partitions()',
      'rdd.numPartitions',
      'rdd.getNumPartitions()',
      'rdd.partitionCount',
    ],
    correctAnswer: 1,
    explanation: 'getNumPartitions() is the method to call on an RDD. However, for DataFrames/Datasets, rdd.getNumPartitions() or df.rdd.getNumPartitions() is used. rdd.getNumPartitions returns an Int.',
    codeSnippet: 'val partCount: Int = rdd.getNumPartitions\nprintln(s"Partitions: $partCount")',
    tags: ['partitioning', 'rdd'],
  },
  {
    id: 'scala-spark-l1-032',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'partitioning',
    question: 'How do you repartition a DataFrame to 10 partitions?',
    options: [
      'df.partition(10)',
      'df.setPartitions(10)',
      'df.repartition(10)',
      'df.split(10)',
    ],
    correctAnswer: 2,
    explanation: 'repartition(n) returns a new DataFrame with exactly n partitions using a full shuffle. Use it to increase or decrease partitions. For only decreasing, coalesce() is more efficient.',
    codeSnippet: 'val df10 = df.repartition(10)',
    tags: ['partitioning', 'repartition', 'dataframe'],
  },
  {
    id: 'scala-spark-l1-033',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'partitioning',
    question: 'What is the difference between repartition() and coalesce()?',
    options: [
      'repartition can increase or decrease partitions using a full shuffle; coalesce only decreases partitions without a full shuffle',
      'repartition only decreases partitions; coalesce only increases partitions',
      'They are identical in behavior',
      'coalesce performs a full shuffle; repartition avoids shuffling',
    ],
    correctAnswer: 0,
    explanation: 'repartition() can increase or decrease partition count and always performs a full shuffle. coalesce() can only decrease partitions and avoids a full shuffle by combining existing partitions, making it more efficient when reducing.',
    tags: ['partitioning', 'repartition', 'coalesce'],
  },
  {
    id: 'scala-spark-l1-034',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'How do you perform an inner join between two DataFrames in Spark?',
    options: [
      'df1.merge(df2, "id")',
      'df1.join(df2, "id")',
      'df1.innerJoin(df2, "id")',
      'DataFrame.join(df1, df2, "id")',
    ],
    correctAnswer: 1,
    explanation: 'df.join() is the method for joining DataFrames. When you pass just a column name string, Spark performs an inner join by default. You can specify the join type with a third argument.',
    codeSnippet: 'val result = df1.join(df2, "id")              // inner join\nval left   = df1.join(df2, "id", "left")      // left outer join',
    tags: ['joins', 'dataframe', 'inner-join'],
  },
  {
    id: 'scala-spark-l1-035',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'joins',
    question: 'Which join type returns all rows from the left DataFrame and matching rows from the right DataFrame?',
    options: [
      'inner',
      'right',
      'left',
      'cross',
    ],
    correctAnswer: 2,
    explanation: 'A left outer join returns all rows from the left DataFrame and matching rows from the right. Non-matching rows from the right are filled with nulls.',
    codeSnippet: 'val result = df1.join(df2, Seq("id"), "left")',
    tags: ['joins', 'left-join'],
  },
  {
    id: 'scala-spark-l1-036',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What import is commonly needed to use DataFrame column expressions like $"name"?',
    options: [
      'import org.apache.spark.sql.functions._',
      'import spark.implicits._',
      'import org.apache.spark.rdd._',
      'import scala.spark.sql._',
    ],
    correctAnswer: 1,
    explanation: 'import spark.implicits._ enables the $"columnName" syntax for column references and also provides implicit encoders for converting Scala objects to Datasets.',
    codeSnippet: 'import spark.implicits._\nval result = df.select($"name", $"age" + 1)',
    tags: ['sparksql', 'implicits', 'column'],
  },
  {
    id: 'scala-spark-l1-037',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you sort a DataFrame by a column in ascending order?',
    options: [
      'df.sort("age")',
      'df.orderBy("age")',
      'Both A and B are correct',
      'df.sortBy("age")',
    ],
    correctAnswer: 2,
    explanation: 'Both sort() and orderBy() are aliases for sorting a DataFrame. Both default to ascending order. You can use .desc to sort in descending order.',
    codeSnippet: 'df.sort("age")             // ascending\ndf.sort(df("age").desc)    // descending\ndf.orderBy("age")          // same as sort',
    tags: ['dataframe', 'sort', 'orderby'],
  },
  {
    id: 'scala-spark-l1-038',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you perform a GROUP BY and count in a DataFrame?',
    options: [
      'df.group("dept").count()',
      'df.groupBy("dept").count()',
      'df.aggregate("dept", count())',
      'df.pivot("dept").count()',
    ],
    correctAnswer: 1,
    explanation: 'groupBy() groups the DataFrame by the specified columns, returning a RelationalGroupedDataset. You can then call aggregation functions like count(), sum(), avg(), etc.',
    codeSnippet: 'val counts = df.groupBy("department").count()\ncounts.show()',
    tags: ['dataframe', 'groupby', 'aggregation'],
  },
  {
    id: 'scala-spark-l1-039',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you write a DataFrame to Parquet format?',
    options: [
      'df.save("path", "parquet")',
      'df.write.parquet("path")',
      'df.export.parquet("path")',
      'df.toParquet("path")',
    ],
    correctAnswer: 1,
    explanation: 'df.write.parquet("path") writes the DataFrame to the specified path in Parquet format. Parquet is a columnar storage format that is efficient for analytical queries.',
    codeSnippet: 'df.write.parquet("/output/path")\n// with save mode:\ndf.write.mode("overwrite").parquet("/output/path")',
    tags: ['dataframe', 'write', 'parquet'],
  },
  {
    id: 'scala-spark-l1-040',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does the union transformation do on two RDDs?',
    options: [
      'Returns only elements present in both RDDs (intersection)',
      'Returns a new RDD containing all elements from both RDDs (with duplicates)',
      'Returns elements in the first RDD but not in the second',
      'Merges the two RDDs and removes duplicates',
    ],
    correctAnswer: 1,
    explanation: 'union() returns a new RDD containing all elements from both RDDs, including duplicates. It does not perform deduplication. Use distinct() afterwards if you need unique elements.',
    codeSnippet: 'val rdd3 = rdd1.union(rdd2) // includes duplicates',
    tags: ['rdd', 'union', 'transformations'],
  },
  {
    id: 'scala-spark-l1-041',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'How do you remove duplicate elements from an RDD?',
    options: [
      'rdd.unique()',
      'rdd.deduplicate()',
      'rdd.distinct()',
      'rdd.removeDuplicates()',
    ],
    correctAnswer: 2,
    explanation: 'distinct() returns a new RDD containing only the unique elements. It triggers a shuffle to compare elements across partitions.',
    codeSnippet: 'val unique = rdd.distinct()',
    tags: ['rdd', 'distinct'],
  },
  {
    id: 'scala-spark-l1-042',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does take(n) do in Spark?',
    options: [
      'Returns the first n elements of the RDD as an Array',
      'Removes the first n elements from the RDD',
      'Partitions the RDD into groups of n',
      'Takes a random sample of n elements',
    ],
    correctAnswer: 0,
    explanation: 'take(n) is an action that returns the first n elements of the RDD as a Scala Array to the driver. It tries to minimize the number of partitions accessed.',
    codeSnippet: 'val first5: Array[Int] = rdd.take(5)',
    tags: ['rdd', 'actions', 'take'],
  },
  {
    id: 'scala-spark-l1-043',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does the reduce action do?',
    options: [
      'Reduces the number of partitions in the RDD',
      'Applies a function to aggregate all elements of the RDD into a single value',
      'Filters elements below a threshold',
      'Returns a new RDD with fewer elements',
    ],
    correctAnswer: 1,
    explanation: 'reduce() applies a commutative and associative binary function to aggregate all elements in the RDD into a single value, which is returned to the driver.',
    codeSnippet: 'val sum = rdd.reduce(_ + _)\nval max = rdd.reduce((a, b) => if (a > b) a else b)',
    tags: ['rdd', 'actions', 'reduce'],
  },
  {
    id: 'scala-spark-l1-044',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does groupByKey() do?',
    options: [
      'Sorts elements by key',
      'Groups values with the same key into an Iterable',
      'Aggregates values by key using addition',
      'Removes all duplicate keys',
    ],
    correctAnswer: 1,
    explanation: 'groupByKey() groups all values with the same key into an Iterable. Note: it is generally less efficient than reduceByKey because it shuffles all values across the network before combining them.',
    codeSnippet: `val pairs = sc.parallelize(Seq(("a", 1), ("b", 2), ("a", 3)))
val grouped = pairs.groupByKey()
// Result: ("a", Iterable(1, 3)), ("b", Iterable(2))`,
    tags: ['rdd', 'pair-rdd', 'groupbykey'],
  },
  {
    id: 'scala-spark-l1-045',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Spark Streaming',
    question: 'What is Spark Streaming?',
    options: [
      'A library for processing static batch datasets',
      'An extension of Spark for processing real-time data streams using micro-batches',
      'A tool for streaming data to HDFS',
      'A module for reading streaming files only',
    ],
    correctAnswer: 1,
    explanation: 'Spark Streaming is an extension of core Spark that enables scalable, high-throughput, fault-tolerant processing of live data streams using micro-batch processing.',
    tags: ['streaming', 'basics'],
  },
  {
    id: 'scala-spark-l1-046',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'What is Structured Streaming in Spark?',
    options: [
      'A legacy streaming API based on DStreams',
      'A scalable streaming engine built on the Spark SQL engine, treating streams as unbounded tables',
      'A file-based batch processing module',
      'A Kafka-specific integration for Spark',
    ],
    correctAnswer: 1,
    explanation: 'Structured Streaming is a scalable and fault-tolerant stream processing engine built on Spark SQL. It models a stream as an infinite/unbounded table and uses the DataFrame/Dataset API.',
    tags: ['structured-streaming', 'basics'],
  },
  {
    id: 'scala-spark-l1-047',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Structured Streaming',
    question: 'How do you start a Structured Streaming query to write to the console?',
    options: [
      'df.stream.start()',
      'df.writeStream.format("console").start()',
      'df.startStream("console")',
      'StreamingQuery.run(df, "console")',
    ],
    correctAnswer: 1,
    explanation: 'writeStream.format("console").start() starts a streaming query that writes output to the console. You should call awaitTermination() on the returned StreamingQuery object to keep the application running.',
    codeSnippet: `val query = df.writeStream
  .outputMode("append")
  .format("console")
  .start()
query.awaitTermination()`,
    tags: ['structured-streaming', 'writestream'],
  },
  {
    id: 'scala-spark-l1-048',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'What is a UDF in Spark?',
    options: [
      'Unified Data Format — a file format for Spark',
      'User-Defined Function — a custom function registered to extend Spark SQL',
      'Universal Distribution Framework — Spark\'s scheduling module',
      'Unique Data Filter — used for deduplication',
    ],
    correctAnswer: 1,
    explanation: 'A UDF (User-Defined Function) is a custom function written in Scala (or another language) that can be used in Spark SQL queries and DataFrame operations.',
    tags: ['udf', 'basics'],
  },
  {
    id: 'scala-spark-l1-049',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'UDFs',
    question: 'How do you define and register a UDF in Spark?',
    options: [
      'spark.registerFunction("double", x: Int => x * 2)',
      'val doubleUDF = udf((x: Int) => x * 2)',
      'UDF.create("double", x => x * 2)',
      'spark.sql.udf.register((x: Int) => x * 2)',
    ],
    correctAnswer: 1,
    explanation: 'UDFs are created using the udf() function from org.apache.spark.sql.functions. You can also register them with spark.udf.register() to use them in SQL strings.',
    codeSnippet: `import org.apache.spark.sql.functions.udf

val doubleUDF = udf((x: Int) => x * 2)
val result = df.withColumn("doubled", doubleUDF(df("value")))

// For SQL usage:
spark.udf.register("double", (x: Int) => x * 2)`,
    tags: ['udf', 'registration'],
  },
  {
    id: 'scala-spark-l1-050',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'Which function from spark.sql.functions calculates the average of a column?',
    options: [
      'mean()',
      'average()',
      'avg()',
      'sum() / count()',
    ],
    correctAnswer: 2,
    explanation: 'avg() from org.apache.spark.sql.functions computes the average value of a column. mean() is also an alias for avg() in Spark.',
    codeSnippet: `import org.apache.spark.sql.functions._
val result = df.agg(avg("salary"), max("salary"), min("salary"))`,
    tags: ['dataframe', 'functions', 'aggregation'],
  },
  {
    id: 'scala-spark-l1-051',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you handle null values in a DataFrame by dropping rows with any null?',
    options: [
      'df.filter(row => !row.anyNull)',
      'df.na.drop()',
      'df.dropNull()',
      'df.removeNulls()',
    ],
    correctAnswer: 1,
    explanation: 'df.na.drop() drops rows that contain any null values. df.na is a DataFrameNaFunctions object that provides methods for handling missing data.',
    codeSnippet: 'val clean = df.na.drop()          // drop any null\nval clean2 = df.na.drop("all")   // drop only if ALL columns are null\nval clean3 = df.na.drop(Seq("age")) // drop if specific column is null',
    tags: ['dataframe', 'null', 'na'],
  },
  {
    id: 'scala-spark-l1-052',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you fill null values with a default value in a DataFrame?',
    options: [
      'df.na.fill(0)',
      'df.fillNull(0)',
      'df.replaceNull(0)',
      'df.na.replace(null, 0)',
    ],
    correctAnswer: 0,
    explanation: 'df.na.fill() replaces null values with the specified value. You can pass a scalar, a Map of column-name to fill-value, or a sequence of column names.',
    codeSnippet: 'val filled = df.na.fill(0)                      // fill numeric nulls with 0\nval filled2 = df.na.fill("unknown")             // fill string nulls\nval filled3 = df.na.fill(Map("age" -> 0, "name" -> "unknown"))',
    tags: ['dataframe', 'null', 'na', 'fill'],
  },
  {
    id: 'scala-spark-l1-053',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is RDD lineage in Spark?',
    options: [
      'The list of all partitions in an RDD',
      'The sequence of transformations that were applied to produce an RDD, used for fault recovery',
      'The order in which RDDs are cached in memory',
      'The dependency graph of Spark executors',
    ],
    correctAnswer: 1,
    explanation: 'RDD lineage (also called the DAG of transformations) records the sequence of transformations that produced the RDD. Spark uses this lineage to recompute lost partitions on failure without needing full replication.',
    tags: ['rdd', 'lineage', 'fault-tolerance'],
  },
  {
    id: 'scala-spark-l1-054',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What are the two types of dependencies in Spark RDDs?',
    options: [
      'Strong and Weak dependencies',
      'Narrow and Wide dependencies',
      'Local and Distributed dependencies',
      'Synchronous and Asynchronous dependencies',
    ],
    correctAnswer: 1,
    explanation: 'Narrow dependencies (e.g., map, filter) mean each parent partition is used by at most one child partition — no shuffle needed. Wide dependencies (e.g., groupByKey, reduceByKey) require data from multiple parent partitions, causing a shuffle.',
    tags: ['rdd', 'dependencies', 'shuffle', 'narrow', 'wide'],
  },
  {
    id: 'scala-spark-l1-055',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'partitioning',
    question: 'What is a shuffle in Spark?',
    options: [
      'Randomly reordering elements within a partition',
      'Redistributing data across partitions across the network, typically caused by wide transformations',
      'Sorting data within each partition',
      'Compressing data before writing to disk',
    ],
    correctAnswer: 1,
    explanation: 'A shuffle is the process of redistributing data across partitions, which requires data to be sent across the network. It is triggered by wide transformations like groupByKey, reduceByKey, and joins. Shuffles are expensive.',
    tags: ['shuffle', 'partitioning', 'performance'],
  },
  {
    id: 'scala-spark-l1-056',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does limit() do in a DataFrame?',
    options: [
      'Sets the maximum number of partitions',
      'Returns a new DataFrame with the first n rows',
      'Limits the execution time of a query',
      'Restricts the number of concurrent tasks',
    ],
    correctAnswer: 1,
    explanation: 'limit(n) returns a new DataFrame with the first n rows. It is similar to LIMIT in SQL.',
    codeSnippet: 'val top10 = df.limit(10)',
    tags: ['dataframe', 'limit'],
  },
  {
    id: 'scala-spark-l1-057',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you remove duplicate rows from a DataFrame?',
    options: [
      'df.unique()',
      'df.removeDuplicates()',
      'df.distinct()',
      'df.dedupe()',
    ],
    correctAnswer: 2,
    explanation: 'distinct() returns a new DataFrame with duplicate rows removed. dropDuplicates() is an alias that also lets you specify specific columns to consider for deduplication.',
    codeSnippet: 'val unique = df.distinct()\nval unique2 = df.dropDuplicates() // same as distinct()\nval unique3 = df.dropDuplicates("name", "dept") // based on specific columns',
    tags: ['dataframe', 'distinct', 'dropduplicates'],
  },
  {
    id: 'scala-spark-l1-058',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'What is the purpose of the explain() method on a DataFrame?',
    options: [
      'Prints the results of the DataFrame computation',
      'Displays the logical and physical execution plan for the query',
      'Shows the schema of the DataFrame',
      'Explains the purpose of each column',
    ],
    correctAnswer: 1,
    explanation: 'explain() prints the logical plan, analyzed plan, optimized plan, and physical plan for the DataFrame query. It is very useful for understanding how Spark will execute your query and for performance tuning.',
    codeSnippet: 'df.filter("age > 18").explain()\ndf.filter("age > 18").explain(true) // extended output',
    tags: ['sparksql', 'explain', 'query-plan'],
  },
  {
    id: 'scala-spark-l1-059',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'How do you read a text file into an RDD?',
    options: [
      'sc.readFile("path/to/file.txt")',
      'sc.textFile("path/to/file.txt")',
      'RDD.fromFile("path/to/file.txt")',
      'sc.loadText("path/to/file.txt")',
    ],
    correctAnswer: 1,
    explanation: 'sc.textFile() reads a text file and returns an RDD of Strings, where each String is one line of the file.',
    codeSnippet: 'val lines: RDD[String] = sc.textFile("hdfs:///data/myfile.txt")',
    tags: ['rdd', 'io', 'textfile'],
  },
  {
    id: 'scala-spark-l1-060',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you compute descriptive statistics for numeric columns in a DataFrame?',
    options: [
      'df.stats()',
      'df.describe()',
      'df.summary()',
      'df.statistics()',
    ],
    correctAnswer: 1,
    explanation: 'describe() returns a DataFrame with summary statistics (count, mean, stddev, min, max) for numeric columns. summary() provides additional percentile statistics.',
    codeSnippet: 'df.describe("age", "salary").show()',
    tags: ['dataframe', 'describe', 'statistics'],
  },
  {
    id: 'scala-spark-l1-061',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does withColumn do when the column name already exists?',
    options: [
      'Throws an exception',
      'Creates a second column with the same name',
      'Replaces the existing column with the new expression',
      'Creates a new column with a suffix "_new"',
    ],
    correctAnswer: 2,
    explanation: 'If you call withColumn() with a column name that already exists in the DataFrame, it replaces that column with the new expression.',
    codeSnippet: '// Replace existing "age" column\nval df2 = df.withColumn("age", df("age") + 1)',
    tags: ['dataframe', 'withcolumn'],
  },
  {
    id: 'scala-spark-l1-062',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you read a JSON file into a DataFrame?',
    options: [
      'spark.read.json("path/to/file.json")',
      'spark.load.json("path/to/file.json")',
      'DataFrame.readJson("path/to/file.json")',
      'spark.json("path/to/file.json")',
    ],
    correctAnswer: 0,
    explanation: 'spark.read.json() reads a JSON file (one JSON object per line) into a DataFrame, automatically inferring the schema.',
    codeSnippet: 'val df = spark.read.json("path/to/data.json")',
    tags: ['dataframe', 'read', 'json'],
  },
  {
    id: 'scala-spark-l1-063',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is the col() function used for in Spark?',
    options: [
      'Creates a new column with a constant value',
      'Returns a Column object representing the named column',
      'Counts non-null values in a column',
      'Converts a column to a collection',
    ],
    correctAnswer: 1,
    explanation: 'col("columnName") from org.apache.spark.sql.functions returns a Column object. It is equivalent to $"columnName" when spark.implicits._ is imported.',
    codeSnippet: `import org.apache.spark.sql.functions.col
val result = df.select(col("name"), col("age"))
// equivalent to: df.select($"name", $"age")`,
    tags: ['dataframe', 'col', 'functions'],
  },
  {
    id: 'scala-spark-l1-064',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does the lit() function do in Spark?',
    options: [
      'Converts a Column to a literal string for SQL',
      'Creates a Column with a constant literal value',
      'Loads a file with literal content',
      'Returns the number of literals in a column',
    ],
    correctAnswer: 1,
    explanation: 'lit() creates a Column from a constant/literal value. It is used when you need to add a constant column or compare a column to a constant in DataFrame operations.',
    codeSnippet: `import org.apache.spark.sql.functions.lit
val df2 = df.withColumn("country", lit("Germany"))
val filtered = df.filter(col("status") === lit("active"))`,
    tags: ['dataframe', 'lit', 'functions'],
  },
  {
    id: 'scala-spark-l1-065',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you union two DataFrames with the same schema?',
    options: [
      'df1.merge(df2)',
      'df1.union(df2)',
      'df1.concat(df2)',
      'df1.append(df2)',
    ],
    correctAnswer: 1,
    explanation: 'union() returns a new DataFrame with all rows from both DataFrames combined. Both DataFrames must have the same schema (same column names and types in the same order).',
    codeSnippet: 'val combined = df1.union(df2)',
    tags: ['dataframe', 'union'],
  },
  {
    id: 'scala-spark-l1-066',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does agg() do in a DataFrame?',
    options: [
      'Aggregates the DataFrame using a single function',
      'Computes multiple aggregate expressions without grouping',
      'Automatically groups by all columns then aggregates',
      'Writes aggregate results to a file',
    ],
    correctAnswer: 1,
    explanation: 'agg() computes one or more aggregate expressions over the whole DataFrame (or grouped DataFrame). It allows multiple aggregations in a single operation.',
    codeSnippet: `import org.apache.spark.sql.functions._
val stats = df.agg(
  count("*").as("total"),
  avg("salary").as("avgSalary"),
  max("age").as("maxAge")
)`,
    tags: ['dataframe', 'agg', 'aggregation'],
  },
  {
    id: 'scala-spark-l1-067',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you create a DataFrame from a Scala Seq of case class instances?',
    options: [
      'spark.createDataFrame(Seq(...))',
      'spark.read.seq(Seq(...))',
      'Seq(...).toDF()(spark)',
      'Both A and C are correct (with implicits)',
    ],
    correctAnswer: 3,
    explanation: 'Both approaches work: spark.createDataFrame(Seq(...)) and Seq(...).toDF() (the latter requires import spark.implicits._). Both produce a DataFrame from a local Scala sequence.',
    codeSnippet: `case class Person(name: String, age: Int)
import spark.implicits._

val df1 = Seq(Person("Alice", 30), Person("Bob", 25)).toDF()
val df2 = spark.createDataFrame(Seq(Person("Alice", 30), Person("Bob", 25)))`,
    tags: ['dataframe', 'creation', 'case-class'],
  },
  {
    id: 'scala-spark-l1-068',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the purpose of the foreach action?',
    options: [
      'Returns a new RDD by applying a function to each element',
      'Applies a function to each element of the RDD for side effects, without returning a value',
      'Groups elements and applies a function to each group',
      'Iterates over elements and collects results to the driver',
    ],
    correctAnswer: 1,
    explanation: 'foreach() applies a function to each element of the RDD. It is used for side effects (e.g., writing to a database). It runs on the executors and does not return a value to the driver.',
    codeSnippet: 'rdd.foreach(x => println(x)) // runs on executors\n// Note: output won\'t appear on driver console in cluster mode',
    tags: ['rdd', 'actions', 'foreach'],
  },
  {
    id: 'scala-spark-l1-069',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What does saveAsTextFile() do?',
    options: [
      'Saves a single text file to the local filesystem',
      'Writes each partition of the RDD as a text file to the specified directory',
      'Saves the first 100 elements as a text file',
      'Converts the RDD to a text-based format in memory',
    ],
    correctAnswer: 1,
    explanation: 'saveAsTextFile() writes the RDD to a directory, creating one file per partition. Each element is written as its toString representation.',
    codeSnippet: 'rdd.saveAsTextFile("hdfs:///output/mydata")',
    tags: ['rdd', 'actions', 'io'],
  },
  {
    id: 'scala-spark-l1-070',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'Which save mode overwrites the destination if it already exists?',
    options: [
      '"append"',
      '"ignore"',
      '"overwrite"',
      '"error"',
    ],
    correctAnswer: 2,
    explanation: 'The "overwrite" save mode deletes existing data at the destination and writes new data. Other modes: "append" adds to existing data, "ignore" does nothing if data exists, "error" (default) throws an exception.',
    codeSnippet: 'df.write.mode("overwrite").parquet("/output/path")',
    tags: ['dataframe', 'write', 'save-mode'],
  },
  {
    id: 'scala-spark-l1-071',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'SparkSQL',
    question: 'How do you use a global temporary view in Spark SQL?',
    options: [
      'df.createGlobalTempView("myView")\nspark.sql("SELECT * FROM myView")',
      'df.createGlobalTempView("myView")\nspark.sql("SELECT * FROM global_temp.myView")',
      'df.createTempView("myView", global = true)\nspark.sql("SELECT * FROM myView")',
      'df.registerGlobal("myView")\nspark.sql("SELECT * FROM myView")',
    ],
    correctAnswer: 1,
    explanation: 'Global temporary views are accessible across multiple SparkSessions and are stored in the global_temp database. They are accessed via global_temp.viewName.',
    codeSnippet: 'df.createGlobalTempView("people")\nval result = spark.sql("SELECT * FROM global_temp.people")',
    tags: ['sparksql', 'global-temp-view'],
  },
  {
    id: 'scala-spark-l1-072',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does crossJoin() do?',
    options: [
      'Joins two DataFrames on a column that exists in both',
      'Returns the Cartesian product of two DataFrames (all combinations of rows)',
      'Joins on all common columns automatically',
      'Performs a cross-cluster join between two Spark applications',
    ],
    correctAnswer: 1,
    explanation: 'crossJoin() returns the Cartesian product — every row in the left DataFrame is combined with every row in the right DataFrame. This can produce very large results.',
    codeSnippet: 'val cartesian = df1.crossJoin(df2) // nRows = df1.count * df2.count',
    tags: ['joins', 'crossjoin', 'cartesian'],
  },
  {
    id: 'scala-spark-l1-073',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you cast a column to a different data type in a DataFrame?',
    options: [
      'df.withColumn("age", df("age").as("integer"))',
      'df.withColumn("age", df("age").cast("integer"))',
      'df.withColumn("age", df("age").convert("integer"))',
      'df.withColumn("age", df("age").to("integer"))',
    ],
    correctAnswer: 1,
    explanation: 'The cast() method on a Column converts it to a different DataType. You can pass a string type name or a DataType object.',
    codeSnippet: `import org.apache.spark.sql.types._
val df2 = df.withColumn("age", df("age").cast(IntegerType))
// or using string:
val df3 = df.withColumn("age", df("age").cast("int"))`,
    tags: ['dataframe', 'cast', 'types'],
  },
  {
    id: 'scala-spark-l1-074',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does the when() function do in Spark?',
    options: [
      'Adds a time-based filter to the query',
      'Creates a conditional Column expression similar to SQL CASE WHEN',
      'Waits for a streaming query to finish',
      'Triggers execution only when a condition is met',
    ],
    correctAnswer: 1,
    explanation: 'when() creates a conditional expression like SQL CASE WHEN. You chain it with .otherwise() for the default case.',
    codeSnippet: `import org.apache.spark.sql.functions._
val df2 = df.withColumn("category",
  when(col("age") < 18, "minor")
   .when(col("age") < 65, "adult")
   .otherwise("senior")
)`,
    tags: ['dataframe', 'when', 'conditional', 'functions'],
  },
  {
    id: 'scala-spark-l1-075',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you count distinct values in a column?',
    options: [
      'df.countDistinct("name")',
      'df.select(countDistinct("name"))',
      'df.groupBy("name").distinct().count()',
      'df.distinct("name").count()',
    ],
    correctAnswer: 1,
    explanation: 'countDistinct() from org.apache.spark.sql.functions counts the number of distinct values in a column. You use it inside select() or agg().',
    codeSnippet: `import org.apache.spark.sql.functions._
val result = df.select(countDistinct("name").as("uniqueNames"))`,
    tags: ['dataframe', 'countdistinct', 'aggregation'],
  },
  {
    id: 'scala-spark-l1-076',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the difference between first() and take(1) in Spark?',
    options: [
      'first() returns an Array; take(1) returns a single element',
      'first() returns the first element directly; take(1) returns an Array with one element',
      'first() triggers a shuffle; take(1) does not',
      'They are completely identical',
    ],
    correctAnswer: 1,
    explanation: 'first() returns the first element of the RDD as a value of type T. take(1) returns an Array[T] with one element. Both are actions that return data to the driver.',
    codeSnippet: 'val elem: Int = rdd.first()      // returns Int\nval arr: Array[Int] = rdd.take(1) // returns Array[Int]',
    tags: ['rdd', 'actions', 'first', 'take'],
  },
  {
    id: 'scala-spark-l1-077',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you write a DataFrame to a CSV file?',
    options: [
      'df.write.csv("path")',
      'df.export.csv("path")',
      'df.save.csv("path")',
      'df.toCSV("path")',
    ],
    correctAnswer: 0,
    explanation: 'df.write.csv("path") writes the DataFrame to a CSV file. You can add options like header and delimiter.',
    codeSnippet: `df.write
  .option("header", "true")
  .option("delimiter", ",")
  .mode("overwrite")
  .csv("/output/path")`,
    tags: ['dataframe', 'write', 'csv'],
  },
  {
    id: 'scala-spark-l1-078',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What is the purpose of the pivot() function in DataFrame API?',
    options: [
      'Rotates data by transposing rows and columns based on a column\'s values',
      'Splits a DataFrame into multiple files',
      'Pivots the sort order of a DataFrame',
      'Converts a wide DataFrame to a narrow one',
    ],
    correctAnswer: 0,
    explanation: 'pivot() rotates a column into multiple columns — one per distinct value. It is used with groupBy() to create a cross-tabulation or pivot table.',
    codeSnippet: `val pivoted = df.groupBy("year")
  .pivot("quarter")
  .agg(sum("revenue"))`,
    tags: ['dataframe', 'pivot'],
  },
  {
    id: 'scala-spark-l1-079',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does the sample() method do?',
    options: [
      'Takes the first n rows from a DataFrame',
      'Returns a random sample of rows from the DataFrame',
      'Creates a DataFrame with randomly generated data',
      'Measures the time to process a sample query',
    ],
    correctAnswer: 1,
    explanation: 'sample(withReplacement, fraction) returns a random subset of rows. The fraction is between 0 and 1 and specifies the approximate fraction of rows to include.',
    codeSnippet: 'val sampled = df.sample(withReplacement = false, fraction = 0.1)',
    tags: ['dataframe', 'sample'],
  },
  {
    id: 'scala-spark-l1-080',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you get all column names of a DataFrame?',
    options: [
      'df.columnNames',
      'df.columns',
      'df.schema.names',
      'Both B and C are correct',
    ],
    correctAnswer: 3,
    explanation: 'df.columns returns an Array[String] of column names. df.schema.fieldNames also returns the column names. Both work correctly.',
    codeSnippet: 'val cols: Array[String] = df.columns\nval cols2: Array[String] = df.schema.fieldNames',
    tags: ['dataframe', 'columns', 'schema'],
  },
  {
    id: 'scala-spark-l1-081',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'RDDs',
    question: 'What is the purpose of mapPartitions() compared to map()?',
    options: [
      'mapPartitions applies a function to individual elements like map, but runs in parallel',
      'mapPartitions applies a function to an entire partition (Iterator), allowing initialization of resources once per partition',
      'mapPartitions is identical to map but with a different name',
      'mapPartitions only works with key-value pair RDDs',
    ],
    correctAnswer: 1,
    explanation: 'mapPartitions() processes an entire partition at once (receives an Iterator[T]). It is useful when you want to initialize an expensive resource (like a DB connection) once per partition rather than once per element.',
    codeSnippet: `val result = rdd.mapPartitions { iter =>
  val conn = createDatabaseConnection()
  val output = iter.map(x => processWithConn(conn, x))
  conn.close()
  output
}`,
    tags: ['rdd', 'mappartitions', 'performance'],
  },
  {
    id: 'scala-spark-l1-082',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'How do you find the number of rows in a DataFrame?',
    options: [
      'df.rows()',
      'df.size()',
      'df.count()',
      'df.length()',
    ],
    correctAnswer: 2,
    explanation: 'count() is an action that returns the number of rows in the DataFrame as a Long.',
    codeSnippet: 'val rowCount: Long = df.count()',
    tags: ['dataframe', 'count', 'actions'],
  },
  {
    id: 'scala-spark-l1-083',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does df.head() return?',
    options: [
      'A DataFrame with only the first row',
      'The first Row object of the DataFrame',
      'An Array of the first 5 rows',
      'The column names of the DataFrame',
    ],
    correctAnswer: 1,
    explanation: 'head() returns the first Row of the DataFrame. head(n) returns the first n rows as an Array[Row]. These are actions that return data to the driver.',
    codeSnippet: 'val firstRow: Row = df.head()\nval first5: Array[Row] = df.head(5)',
    tags: ['dataframe', 'head', 'actions'],
  },
  {
    id: 'scala-spark-l1-084',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does inferSchema do when reading data?',
    options: [
      'Reads the schema from an external schema registry',
      'Automatically determines column data types by sampling the data',
      'Infers the schema from the column names only',
      'Creates a schema from the first row of data',
    ],
    correctAnswer: 1,
    explanation: 'When inferSchema is enabled, Spark reads a portion of the data and infers the appropriate data types for each column. Without it, all columns default to String type.',
    codeSnippet: `val df = spark.read
  .option("header", "true")
  .option("inferSchema", "true")
  .csv("data.csv")`,
    tags: ['dataframe', 'schema', 'inferschema'],
  },
  {
    id: 'scala-spark-l1-085',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'Why is Java serialization generally not recommended for Spark?',
    options: [
      'Java serialization is not supported in Spark',
      'Java serialization is slow and produces large byte arrays compared to Kryo or Tungsten',
      'Java serialization cannot handle case classes',
      'Java serialization requires special Spark licenses',
    ],
    correctAnswer: 1,
    explanation: 'Java serialization (ObjectOutputStream) is flexible but slow and verbose. Kryo is typically 10x faster and produces smaller serialized objects. Spark\'s Tungsten uses its own binary format for Dataset operations, which is even more efficient.',
    tags: ['serialization', 'performance', 'kryo'],
  },
  {
    id: 'scala-spark-l1-086',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'serialization',
    question: 'How do you configure Spark to use Kryo serialization?',
    options: [
      'spark.conf.set("serialization", "kryo")',
      'spark.conf.set("spark.serializer", "org.apache.spark.serializer.KryoSerializer")',
      'SparkConf().set("kryo.enabled", "true")',
      'spark.enableKryo()',
    ],
    correctAnswer: 1,
    explanation: 'Kryo serialization is configured by setting spark.serializer to KryoSerializer. You should also register your custom classes with Kryo for maximum efficiency.',
    codeSnippet: `val conf = new SparkConf()
  .set("spark.serializer", "org.apache.spark.serializer.KryoSerializer")
  .registerKryoClasses(Array(classOf[MyClass]))`,
    tags: ['serialization', 'kryo', 'configuration'],
  },
  {
    id: 'scala-spark-l1-087',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'Spark Streaming',
    question: 'What is a DStream in Spark Streaming?',
    options: [
      'A Distributed Stream — Spark\'s RDD equivalent for streaming data',
      'A Data Stream object in Structured Streaming',
      'A database stream connection',
      'A sequence of continuous DataFrames',
    ],
    correctAnswer: 0,
    explanation: 'DStream (Discretized Stream) is the fundamental abstraction in Spark Streaming. It represents a continuous stream of data internally represented as a sequence of RDDs, one per micro-batch interval.',
    tags: ['streaming', 'dstream', 'basics'],
  },
  {
    id: 'scala-spark-l1-088',
    language: 'scala',
    level: 'level1',
    category: 'Apache Spark',
    subcategory: 'DataFrames',
    question: 'What does the concat_ws() function do in Spark?',
    options: [
      'Concatenates two DataFrames horizontally',
      'Concatenates multiple string columns with a separator',
      'Counts words in a string column',
      'Creates a websocket connection',
    ],
    correctAnswer: 1,
    explanation: 'concat_ws(separator, col1, col2, ...) concatenates multiple string columns with the specified separator, ignoring null values.',
    codeSnippet: `import org.apache.spark.sql.functions._
val df2 = df.withColumn("fullName", concat_ws(" ", col("firstName"), col("lastName")))`,
    tags: ['dataframe', 'string-functions', 'concat_ws'],
  },
];

export default questions;
