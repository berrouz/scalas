import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'scala-http4s-l4-001',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'HttpRoutes',
    question: 'How does the Kleisli-based representation of HttpRoutes enable middleware composition that preserves referential transparency?',
    options: ['Kleisli has no effect on middleware', 'Kleisli forces sequential execution', 'Kleisli wraps A => F[B], so composing middleware via andThen/compose produces new functions without running effects, preserving pure composition', 'Kleisli eliminates the need for middleware'],
    correctAnswer: 2,
    explanation: 'Kleisli[F, A, B] wraps A => F[B] and provides andThen/compose for pure function composition. Middleware transforms Kleisli values without executing effects, maintaining referential transparency.',
    tags: ['http4s', 'Kleisli', 'referential-transparency', 'composition'],
  },
  {
    id: 'scala-http4s-l4-002',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'HttpRoutes',
    question: 'What is the relationship between HttpRoutes, ContextRoutes, and AuthedRoutes in http4s?',
    options: ['AuthedRoutes extends HttpRoutes', 'They are unrelated types', 'AuthedRoutes[T, F] is an alias for ContextRoutes[T, F] which is Kleisli[OptionT[F, *], ContextRequest[F, T], Response[F]]', 'ContextRoutes is deprecated'],
    correctAnswer: 2,
    explanation: 'AuthedRoutes[T, F] is a type alias for ContextRoutes[T, F], which generalizes HttpRoutes by carrying a context T in the request. This enables type-safe middleware that threads context.',
    tags: ['http4s', 'AuthedRoutes', 'ContextRoutes', 'type-aliases'],
  },
  {
    id: 'scala-http4s-l4-003',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Middleware',
    question: 'How do you implement a distributed tracing middleware that propagates trace context through http4s using Vault?',
    options: ['Using global variables', 'Store trace IDs in Request Vault attributes, extract/inject in middleware, and read in routes via req.attributes', 'Tracing requires a separate process', 'Using thread-local storage'],
    correctAnswer: 1,
    explanation: 'http4s Request has a Vault (type-safe heterogeneous map) for storing request-scoped attributes. Middleware can inject trace context into the Vault, and routes/downstream middleware can read it.',
    codeSnippet: 'val TraceId = Vault.Key.newKey[IO, String]\n\ndef tracing(http: HttpApp[IO]): HttpApp[IO] = Kleisli { req =>\n  val traceId = req.headers.get(ci"X-Trace-Id")\n    .map(_.head.value).getOrElse(UUID.randomUUID().toString)\n  http(req.withAttribute(TraceId, traceId)).map(\n    _.putHeaders(Header.Raw(ci"X-Trace-Id", traceId))\n  )\n}',
    tags: ['http4s', 'middleware', 'tracing', 'Vault'],
  },
  {
    id: 'scala-http4s-l4-004',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Middleware',
    question: 'How do you implement middleware that modifies the response body stream in http4s?',
    options: ['By transforming the response body fs2 Stream using .withBodyStream or mapping over the entity', 'By buffering the entire response', 'By replacing the response entirely', 'Response body modification is not possible'],
    correctAnswer: 0,
    explanation: 'Since response bodies are fs2 Streams, middleware can transform them lazily without buffering using .withBodyStream or by piping through stream transformations.',
    codeSnippet: 'def addTrailer(http: HttpApp[IO]): HttpApp[IO] = Kleisli { req =>\n  http(req).map { resp =>\n    val newBody = resp.body ++ Stream.emit("\\n<!-- generated -->").through(text.utf8.encode)\n    resp.withBodyStream(newBody)\n  }\n}',
    tags: ['http4s', 'middleware', 'streaming', 'response-body'],
  },
  {
    id: 'scala-http4s-l4-005',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Middleware',
    question: 'What is the correct order of middleware application if you want logging to see the final response (after CORS and GZip)?',
    options: ['Apply logging outermost: Logging(GZip(CORS(routes)))', 'Apply logging innermost', 'Order does not matter', 'Apply CORS last'],
    correctAnswer: 0,
    explanation: 'Middleware wraps in layers. The outermost middleware sees the final request/response. So Logging(GZip(CORS(routes))) means logging sees the gzipped CORS response.',
    tags: ['http4s', 'middleware', 'ordering', 'composition'],
  },
  {
    id: 'scala-http4s-l4-006',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Authentication Middleware',
    question: 'How do you implement role-based access control (RBAC) with http4s AuthMiddleware?',
    options: ['Using route-level guards', 'Chain AuthMiddleware for authentication with an additional middleware that checks user roles, or use ContextRoutes with a role-enriched context', 'Using annotations on routes', 'RBAC requires a separate authorization server'],
    correctAnswer: 1,
    explanation: 'After authentication with AuthMiddleware, apply an additional middleware layer that inspects the authenticated user\'s roles and returns 403 Forbidden for unauthorized access.',
    codeSnippet: 'def requireRole[F[_]: Monad](role: String)(\n  routes: AuthedRoutes[User, F]\n): AuthedRoutes[User, F] =\n  Kleisli { authedReq =>\n    if (authedReq.context.roles.contains(role)) routes(authedReq)\n    else OptionT.liftF(Forbidden("Insufficient permissions").widen)\n  }',
    tags: ['http4s', 'authentication', 'rbac', 'authorization'],
  },
  {
    id: 'scala-http4s-l4-007',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Authentication Middleware',
    question: 'How do you implement token refresh flow in http4s client middleware?',
    options: ['Intercept 401 responses, refresh the token atomically using a Ref, and retry the request with the new token', 'Token refresh is server-side only', 'Manual refresh only', 'Using a separate refresh endpoint'],
    correctAnswer: 0,
    explanation: 'Client middleware catches 401 responses, uses Deferred or Ref for atomic token refresh (avoiding thundering herd), then retries the original request with the new token.',
    codeSnippet: 'def withTokenRefresh(tokenRef: Ref[IO, Token], refreshToken: IO[Token])\n  (client: Client[IO]): Client[IO] = Client { req =>\n  Resource.eval(tokenRef.get).flatMap { token =>\n    client.run(addAuth(req, token)).evalMap {\n      case resp if resp.status == Status.Unauthorized =>\n        refreshToken.flatMap(tokenRef.set) >> tokenRef.get.flatMap { newToken =>\n          client.run(addAuth(req, newToken)).use(IO.pure)\n        }\n      case resp => IO.pure(resp)\n    }\n  }\n}',
    tags: ['http4s', 'client', 'token-refresh', 'authentication'],
  },
  {
    id: 'scala-http4s-l4-008',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Streaming',
    question: 'How do you implement backpressure-aware streaming JSON encoding for large collections in http4s?',
    options: ['Use pagination instead', 'Stream individual JSON elements with fs2 and encode them incrementally with proper array framing', 'JSON streaming is not possible', 'Serialize everything to a String'],
    correctAnswer: 1,
    explanation: 'Stream each element, encode to JSON individually, and frame as a JSON array with commas. This enables constant memory usage and backpressure through fs2.',
    codeSnippet: 'def streamJsonArray[F[_], A: Encoder](stream: Stream[F, A]): Stream[F, Byte] = {\n  val items = stream.map(_.asJson.noSpaces)\n  val framed = Stream.emit("[") ++ items.intersperse(",") ++ Stream.emit("]")\n  framed.through(text.utf8.encode)\n}',
    tags: ['http4s', 'streaming', 'json', 'backpressure'],
  },
  {
    id: 'scala-http4s-l4-009',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Streaming',
    question: 'How do you handle chunked transfer encoding in http4s for real-time data?',
    options: ['Set a chunked header manually', 'Use WebSockets instead', 'Chunked encoding requires special server config', 'http4s automatically uses chunked transfer encoding when the response body is an fs2 Stream without a known Content-Length'],
    correctAnswer: 3,
    explanation: 'When the response body is a stream and no Content-Length is set, http4s automatically uses chunked Transfer-Encoding. Each stream chunk becomes an HTTP chunk.',
    tags: ['http4s', 'streaming', 'chunked-encoding', 'transfer-encoding'],
  },
  {
    id: 'scala-http4s-l4-010',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Streaming',
    question: 'How do you implement a streaming CSV export endpoint in http4s that works with millions of rows?',
    options: ['Use a background job', 'Stream rows from the database through Doobie, transform to CSV lines with fs2 Pipe, and return as a streaming response', 'Load all rows and convert', 'CSV export requires buffering'],
    correctAnswer: 1,
    explanation: 'Doobie streams query results as fs2 Stream. Pipe each row through CSV formatting and return directly as the response body, achieving constant memory usage.',
    codeSnippet: 'val csvPipe: Pipe[IO, Row, String] = _.map(r => s"${r.id},${r.name}\\n")\nval rows = sql"SELECT * FROM data".query[Row].stream.transact(xa)\nval csvStream = (Stream.emit("id,name\\n") ++ rows.through(csvPipe))\n  .through(text.utf8.encode)\nOk(csvStream).map(_.withContentType(`Content-Type`(MediaType.text.csv)))',
    tags: ['http4s', 'streaming', 'csv', 'doobie'],
  },
  {
    id: 'scala-http4s-l4-011',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'WebSockets',
    question: 'How do you implement heartbeat/ping-pong for WebSocket connections in http4s to detect dead connections?',
    options: ['It is automatic', 'Merge a periodic ping stream with the send stream and filter pong from the receive stream', 'WebSocket heartbeat is not possible', 'Using TCP keepalive only'],
    correctAnswer: 1,
    explanation: 'Merge a periodic Ping frame stream with your application send stream. Filter incoming Pong frames from the receive stream. Timeout if no Pong received.',
    codeSnippet: 'val pingStream = Stream.awakeEvery[IO](30.seconds)\n  .map(_ => WebSocketFrame.Ping())\nval sendWithPing = dataStream.merge(pingStream)',
    tags: ['http4s', 'websocket', 'heartbeat', 'ping-pong'],
  },
  {
    id: 'scala-http4s-l4-012',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'WebSockets',
    question: 'How do you implement a WebSocket chat room with multiple rooms in http4s?',
    options: ['Multiple rooms are not supported', 'Using one global Topic', 'Using database polling', 'Use a Ref[IO, Map[RoomId, Topic[IO, WebSocketFrame]]] to manage per-room topics dynamically'],
    correctAnswer: 3,
    explanation: 'Maintain a map of room IDs to Topics in a Ref. Create new Topics on demand and clean up when rooms are empty. Each client subscribes to their room\'s Topic.',
    codeSnippet: 'def getOrCreateRoom(rooms: Ref[IO, Map[String, Topic[IO, WSFrame]]], roomId: String): IO[Topic[IO, WSFrame]] =\n  rooms.modify { map =>\n    map.get(roomId) match {\n      case Some(topic) => (map, IO.pure(topic))\n      case None => ???  // Create new topic and update map\n    }\n  }.flatten',
    tags: ['http4s', 'websocket', 'chat-room', 'Topic', 'Ref'],
  },
  {
    id: 'scala-http4s-l4-013',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'WebSockets',
    question: 'How do you implement WebSocket authentication in http4s since browsers cannot set custom headers on WebSocket connections?',
    options: ['Use cookies only', 'WebSocket auth is impossible', 'Accept token as a query parameter during handshake, or use a ticket-based auth where a short-lived token is obtained via REST first', 'Use HTTP Basic auth'],
    correctAnswer: 2,
    explanation: 'Since browsers cannot set custom headers for WebSocket handshakes, use query parameters (ws://host/ws?token=xxx) or a two-step flow: get a short-lived ticket via REST, then pass it during WS handshake.',
    tags: ['http4s', 'websocket', 'authentication', 'security'],
  },
  {
    id: 'scala-http4s-l4-014',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'JSON with Circe',
    question: 'How do you implement incremental JSON parsing for very large JSON documents in http4s with Circe?',
    options: ['Large JSON is not supported', 'Use circe-fs2 to parse a streaming JSON array element by element from the request body stream', 'Use a SAX-like parser', 'Parse the entire document at once'],
    correctAnswer: 1,
    explanation: 'circe-fs2 provides stringStreamParser and byteStreamParser that parse JSON tokens incrementally from an fs2 Stream, producing a Stream of decoded values.',
    codeSnippet: 'import io.circe.fs2._\nval decoded: Stream[IO, User] =\n  req.body\n    .through(byteStreamParser)\n    .through(decoder[IO, User])',
    tags: ['circe', 'fs2', 'streaming', 'json-parsing'],
  },
  {
    id: 'scala-http4s-l4-015',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'JSON with Circe',
    question: 'How do you implement a custom Circe Codec that uses different field names for encoding vs decoding?',
    options: ['Create separate Encoder and Decoder instances with different field mappings using cursor navigation for decoding and custom object building for encoding', 'Using a bidirectional mapping', 'Using annotations only', 'It is not possible'],
    correctAnswer: 0,
    explanation: 'Create separate Encoder and Decoder instances. The Encoder builds a Json object with desired output keys, while the Decoder uses cursor.downField with input keys.',
    codeSnippet: 'implicit val decoder: Decoder[ApiUser] = (c: HCursor) => for {\n  name <- c.downField("user_name").as[String]\n  email <- c.downField("email_address").as[String]\n} yield ApiUser(name, email)\n\nimplicit val encoder: Encoder[ApiUser] = (u: ApiUser) =>\n  Json.obj("userName" -> u.name.asJson, "emailAddress" -> u.email.asJson)',
    tags: ['circe', 'encoder', 'decoder', 'asymmetric'],
  },
  {
    id: 'scala-http4s-l4-016',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'JSON with Circe',
    question: 'How do you handle versioned JSON APIs with Circe where different versions have different schemas?',
    options: ['Versioning is not possible with JSON', 'Using URL-based versioning only', 'Use a version-aware Decoder that inspects a version field and delegates to version-specific decoders', 'Maintain separate services'],
    correctAnswer: 2,
    explanation: 'Create a dispatcher Decoder that reads a version field and delegates to version-specific decoders. Each version can have its own field names, types, and transformations.',
    codeSnippet: 'implicit val decoder: Decoder[Event] = (c: HCursor) =>\n  c.downField("version").as[Int].flatMap {\n    case 1 => c.as[EventV1].map(_.toEvent)\n    case 2 => c.as[EventV2].map(_.toEvent)\n    case v => Left(DecodingFailure(s"Unknown version $v", c.history))\n  }',
    tags: ['circe', 'versioning', 'decoder', 'api-evolution'],
  },
  {
    id: 'scala-http4s-l4-017',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Entity Codecs',
    question: 'How do you implement a content-type-aware EntityDecoder that handles both JSON and XML?',
    options: ['Using two separate routes', 'Mixed content types are not supported', 'Create an EntityDecoder that checks the Content-Type header and delegates to JSON or XML decoder accordingly', 'Using content negotiation library'],
    correctAnswer: 2,
    explanation: 'Build a composite EntityDecoder that inspects the Content-Type header and dispatches to the appropriate decoder (Circe for JSON, scala-xml for XML).',
    codeSnippet: 'implicit def flexibleDecoder[F[_]: Concurrent]: EntityDecoder[F, User] =\n  jsonOf[F, User].orElse(\n    EntityDecoder.text[F].flatMapR { xml =>\n      // Parse XML to User\n      DecodeResult.successT(parseXml(xml))\n    }.withContentType(MediaType.application.xml)\n  )',
    tags: ['http4s', 'entity', 'decoder', 'content-type', 'polymorphic'],
  },
  {
    id: 'scala-http4s-l4-018',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Entity Codecs',
    question: 'What is the relationship between EntityEncoder, EntityBody, and Entity in http4s?',
    options: ['EntityEncoder[F, A] produces Entity[F] (body: EntityBody[F] + headers), where EntityBody[F] = Stream[F, Byte]', 'Entity is deprecated', 'EntityBody is not related to Entity', 'They are the same type'],
    correctAnswer: 0,
    explanation: 'EntityEncoder[F, A] converts A to Entity[F], which contains an EntityBody[F] (Stream[F, Byte]) and Headers. This separation allows attaching content-type and other headers.',
    tags: ['http4s', 'entity', 'types', 'architecture'],
  },
  {
    id: 'scala-http4s-l4-019',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Client',
    question: 'How do you implement a connection-aware http4s client that routes requests to different backends based on the target host?',
    options: ['Using a reverse proxy', 'Using DNS only', 'Client routing is not possible', 'Create a Client wrapper that inspects the request URI and dispatches to different underlying clients based on routing rules'],
    correctAnswer: 3,
    explanation: 'Build a Client[F] that inspects the request URI host/path and routes to different backend clients, enabling service mesh-like behavior in the application layer.',
    codeSnippet: 'def routingClient(clients: Map[String, Client[IO]], default: Client[IO]): Client[IO] =\n  Client { req =>\n    val host = req.uri.host.map(_.renderString).getOrElse("")\n    clients.getOrElse(host, default).run(req)\n  }',
    tags: ['http4s', 'client', 'routing', 'service-mesh'],
  },
  {
    id: 'scala-http4s-l4-020',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Client',
    question: 'How do you implement request deduplication in http4s client middleware to prevent duplicate concurrent requests?',
    options: ['Using a MapRef to track in-flight requests by key, and have duplicate requests await the result of the first one via Deferred', 'Using a semaphore per URL', 'Deduplication is server-side only', 'Caching responses'],
    correctAnswer: 0,
    explanation: 'Maintain a Map of request keys to Deferred values in a Ref. The first request starts the actual call and completes the Deferred; duplicates await it.',
    codeSnippet: 'def dedup(client: Client[IO], flights: Ref[IO, Map[Uri, Deferred[IO, Response[IO]]]]): Client[IO] =\n  Client { req =>\n    Resource.eval(Deferred[IO, Response[IO]]).flatMap { d =>\n      Resource.eval(flights.modify { map =>\n        map.get(req.uri) match {\n          case Some(existing) => (map, existing.get)  // Await existing\n          case None => (map + (req.uri -> d), client.run(req).use(r => d.complete(r) >> IO.pure(r)))\n        }\n      }.flatten).flatMap(resp => Resource.pure(resp))\n    }\n  }',
    tags: ['http4s', 'client', 'deduplication', 'Deferred', 'concurrency'],
  },
  {
    id: 'scala-http4s-l4-021',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Server',
    question: 'How do you implement graceful shutdown with a drain period in http4s?',
    options: ['Using System.exit', 'Use Deferred or SignallingRef to signal shutdown, stop accepting new requests, and wait for in-flight requests to complete within a timeout', 'Graceful shutdown is automatic', 'Kill the process'],
    correctAnswer: 1,
    explanation: 'Use a SignallingRef to signal shutdown intention. Stop accepting new connections, let in-flight requests finish (with timeout), and then release the server Resource.',
    tags: ['http4s', 'server', 'graceful-shutdown', 'drain'],
  },
  {
    id: 'scala-http4s-l4-022',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Server',
    question: 'How do you implement HTTP/2 server push in http4s?',
    options: ['Using Link headers for preload hints', 'HTTP/2 server push is not directly supported in http4s; use a reverse proxy like nginx for push, or handle it at the framework level', 'Using push headers', 'Automatically enabled with HTTP/2'],
    correctAnswer: 0,
    explanation: 'http4s does not directly implement HTTP/2 server push. However, you can use Link headers with rel=preload to hint resources, and rely on a reverse proxy or CDN for actual server push.',
    tags: ['http4s', 'http2', 'server-push', 'performance'],
  },
  {
    id: 'scala-http4s-l4-023',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Server',
    question: 'What considerations are important when deploying http4s with Ember in a Kubernetes environment?',
    options: ['Configure health/readiness probes, handle SIGTERM for graceful shutdown, set appropriate resource limits, and configure shutdown timeout less than terminationGracePeriod', 'Only JVM flags matter', 'Kubernetes does not support Scala applications', 'No special considerations'],
    correctAnswer: 0,
    explanation: 'Key considerations: health/readiness endpoints for k8s probes, handling SIGTERM (Cats Effect does this), shutdown timeout < pod terminationGracePeriodSeconds, connection draining, and JVM memory settings.',
    tags: ['http4s', 'kubernetes', 'deployment', 'production'],
  },
  {
    id: 'scala-http4s-l4-024',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you implement optimistic locking with Doobie?',
    options: ['Using database locks', 'Optimistic locking is database-specific', 'Include a version column, read it before update, and use WHERE version = readVersion in the UPDATE; check update count to detect conflicts', 'Using synchronized blocks'],
    correctAnswer: 2,
    explanation: 'Read the version column with the entity. On update, include WHERE version = expectedVersion and increment the version. If update count is 0, a concurrent modification occurred.',
    codeSnippet: 'def update(user: User): ConnectionIO[Either[ConcurrencyError, User]] =\n  sql"""UPDATE users SET name = ${user.name}, version = ${user.version + 1}\n        WHERE id = ${user.id} AND version = ${user.version}"""\n    .update.run.map {\n      case 0 => Left(ConcurrencyError("Concurrent modification"))\n      case _ => Right(user.copy(version = user.version + 1))\n    }',
    tags: ['doobie', 'optimistic-locking', 'concurrency', 'versioning'],
  },
  {
    id: 'scala-http4s-l4-025',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you implement batch inserts efficiently with Doobie?',
    options: ['Using raw JDBC batch', 'Use Update[A].updateMany(List[A]) which generates a single multi-row INSERT', 'Batch inserts are not supported', 'Insert one by one in a loop'],
    correctAnswer: 1,
    explanation: 'Update[A].updateMany generates efficient batch inserts. For very large batches, chunk the data and use .stream.chunkN with batch processing.',
    codeSnippet: 'val insert = Update[User](\n  "INSERT INTO users (name, email) VALUES (?, ?)"\n)\nval users = List(User("Alice", "a@b.com"), User("Bob", "b@b.com"))\ninsert.updateMany(users).transact(xa)',
    tags: ['doobie', 'batch-insert', 'performance'],
  },
  {
    id: 'scala-http4s-l4-026',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you implement a repository pattern with Doobie that is testable?',
    options: ['Repository pattern does not work with Doobie', 'Using abstract classes only', 'Define a trait with ConnectionIO return types, implement with Doobie, and test using a test Transactor with an in-memory database', 'Using mocks only'],
    correctAnswer: 2,
    explanation: 'Define repository trait returning ConnectionIO[A]. Test implementations with a Transactor backed by H2 or testcontainers PostgreSQL, ensuring real SQL is tested.',
    codeSnippet: 'trait UserRepo[F[_]] {\n  def findById(id: Long): F[Option[User]]\n  def create(user: User): F[Long]\n}\n\nclass DoobieUserRepo(xa: Transactor[IO]) extends UserRepo[IO] {\n  def findById(id: Long) =\n    sql"SELECT id, name FROM users WHERE id = $id"\n      .query[User].option.transact(xa)\n}',
    tags: ['doobie', 'repository', 'testing', 'architecture'],
  },
  {
    id: 'scala-http4s-l4-027',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you handle database migrations in a Doobie/http4s application?',
    options: ['Manual SQL scripts', 'Doobie handles migrations automatically', 'Use Flyway or Liquibase executed as a Resource before the server starts, ensuring migrations complete before accepting requests', 'Using Doobie\'s built-in migration tool'],
    correctAnswer: 2,
    explanation: 'Integrate Flyway or Liquibase as a Resource that runs before server start. This ensures the database schema is up-to-date before the application begins handling requests.',
    codeSnippet: 'val migrateDb: IO[Unit] = IO {\n  Flyway.configure()\n    .dataSource(url, user, pass)\n    .load()\n    .migrate()\n}\n\nval app = migrateDb >> serverResource.useForever',
    tags: ['doobie', 'migrations', 'flyway', 'production'],
  },
  {
    id: 'scala-http4s-l4-028',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you implement a generic pagination helper with Doobie that works with any query?',
    options: ['Create a reusable function that takes a Fragment, page/size parameters, and returns both data and total count in a single transaction', 'Copy-paste LIMIT/OFFSET everywhere', 'Generic pagination is not possible', 'Use a pagination library'],
    correctAnswer: 0,
    explanation: 'Build a reusable pagination function that composes the base query Fragment with LIMIT/OFFSET and a COUNT query, running both in a single transaction.',
    codeSnippet: 'def paginate[A: Read](base: Fragment, page: Int, size: Int): ConnectionIO[Page[A]] = {\n  val offset = (page - 1) * size\n  val data = (base ++ fr"LIMIT $size OFFSET $offset").query[A].to[List]\n  val count = (fr"SELECT count(*) FROM (" ++ base ++ fr") AS t").query[Long].unique\n  (data, count).mapN(Page(_, _, page, size))\n}',
    tags: ['doobie', 'pagination', 'reusable', 'generic'],
  },
  {
    id: 'scala-http4s-l4-029',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Tapir',
    question: 'How do you implement a Tapir endpoint with custom error handling that maps different exceptions to different HTTP status codes?',
    options: ['Using a global exception handler', 'Define a oneOfVariant error output with multiple status codes and map exceptions in server logic to specific error variants', 'Using try-catch blocks', 'Tapir does not support multiple error types'],
    correctAnswer: 1,
    explanation: 'Tapir\'s oneOf allows defining multiple error output variants with different status codes. Server logic returns the appropriate error variant based on the failure type.',
    codeSnippet: 'val myEndpoint = endpoint.get\n  .errorOut(oneOf[AppError](\n    oneOfVariant(StatusCode.NotFound, jsonBody[NotFoundError]),\n    oneOfVariant(StatusCode.BadRequest, jsonBody[ValidationError]),\n    oneOfVariant(StatusCode.InternalServerError, jsonBody[InternalError])\n  ))\n  .out(jsonBody[User])',
    tags: ['tapir', 'error-handling', 'oneOf', 'status-codes'],
  },
  {
    id: 'scala-http4s-l4-030',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Tapir',
    question: 'How do you share common endpoint definition elements (base path, auth, error handling) across multiple Tapir endpoints?',
    options: ['Create a base endpoint val with common inputs/outputs and extend it for specific endpoints', 'Copy-paste the definitions', 'Using macros', 'Using inheritance'],
    correctAnswer: 0,
    explanation: 'Define a base endpoint with common elements (path prefix, auth, error output) and extend it with .in/.out for specific endpoints.',
    codeSnippet: 'val baseEndpoint = endpoint\n  .in("api" / "v1")\n  .securityIn(auth.bearer[String]())\n  .errorOut(jsonBody[ApiError])\n\nval getUserEndpoint = baseEndpoint.get\n  .in("users" / path[Long]("id"))\n  .out(jsonBody[User])\n\nval listUsersEndpoint = baseEndpoint.get\n  .in("users")\n  .in(query[Int]("page"))\n  .out(jsonBody[List[User]])',
    tags: ['tapir', 'endpoint', 'reuse', 'base-endpoint'],
  },
  {
    id: 'scala-http4s-l4-031',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Tapir',
    question: 'How do you implement streaming file download in Tapir with http4s?',
    options: ['Return the file as a byte array', 'Use a separate download endpoint', 'Use streamBody(Fs2Streams[IO])(Schema.binary, CodecFormat.OctetStream) as the output', 'Streaming is not supported in Tapir'],
    correctAnswer: 2,
    explanation: 'Tapir supports streaming bodies via streamBody. Combined with the http4s server interpreter, this enables streaming file downloads with fs2.',
    codeSnippet: 'val downloadEndpoint = endpoint.get\n  .in("download" / path[String]("filename"))\n  .out(streamBody(Fs2Streams[IO])(Schema.binary, CodecFormat.OctetStream))\n  .out(header[String]("Content-Disposition"))',
    tags: ['tapir', 'streaming', 'download', 'fs2'],
  },
  {
    id: 'scala-http4s-l4-032',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Tapir',
    question: 'How do you implement request validation in Tapir that produces detailed error messages?',
    options: ['Using schema validation only', 'Use Tapir Validator instances on input types to validate at the endpoint definition level', 'Validation is not possible in Tapir', 'Validate in server logic only'],
    correctAnswer: 1,
    explanation: 'Tapir provides Validator[T] that can be attached to inputs. Validators run before server logic and produce detailed validation errors.',
    codeSnippet: 'val ageInput = query[Int]("age")\n  .validate(Validator.min(0))\n  .validate(Validator.max(150))\n\nval nameInput = query[String]("name")\n  .validate(Validator.minLength(1))\n  .validate(Validator.maxLength(100))',
    tags: ['tapir', 'validation', 'input', 'error-messages'],
  },
  {
    id: 'scala-http4s-l4-033',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'sttp',
    question: 'How do you implement resilient API calls with sttp using retries, circuit breaker, and timeout?',
    options: ['Using a single retry flag', 'Compose sttp backend wrappers: retrying backend wrapping circuit breaker backend wrapping timeout-configured backend', 'Resilience is not supported in sttp', 'Adding all logic manually'],
    correctAnswer: 1,
    explanation: 'sttp provides backend wrappers that can be composed. Apply timeout configuration, wrap with circuit breaker logic, then wrap with retry logic for layered resilience.',
    codeSnippet: 'val backend = HttpClientCatsBackend.resource[IO]().map { base =>\n  val withTimeout = base  // Configure via .readTimeout\n  val withRetry = new RetryingBackend(withTimeout, maxRetries = 3)\n  withRetry\n}',
    tags: ['sttp', 'resilience', 'retry', 'circuit-breaker'],
  },
  {
    id: 'scala-http4s-l4-034',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'sttp',
    question: 'How do you use Tapir-generated client from endpoint descriptions with sttp?',
    options: ['Clients cannot be generated', 'Use SttpClientInterpreter to generate type-safe client functions from Tapir endpoint definitions', 'Using code generation tools', 'Write the client manually'],
    correctAnswer: 1,
    explanation: 'Tapir\'s sttp client interpreter generates type-safe client functions from endpoint definitions, ensuring client and server always agree on the API contract.',
    codeSnippet: 'val getUserClient: Long => Request[Either[ApiError, User], Any] =\n  SttpClientInterpreter()\n    .toClient(getUserEndpoint, Some(uri"http://localhost:8080"), backend)',
    tags: ['tapir', 'sttp', 'client-generation', 'type-safe'],
  },
  {
    id: 'scala-http4s-l4-035',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Routing',
    question: 'How do you implement SIRD (String Interpolation Routing DSL) in Play for programmatic routing?',
    options: ['SIRD is deprecated', 'It is the default routing', 'Using annotations instead', 'Use PartialFunction[RequestHeader, Handler] with string interpolation extractors for type-safe programmatic routing'],
    correctAnswer: 3,
    explanation: 'SIRD provides string interpolation-based routing as an alternative to the routes file. It enables compile-time checked routing with pattern matching.',
    codeSnippet: 'import play.api.routing.sird._\nval router = Router.from {\n  case GET(p"/users/$id<[0-9]+>") =>\n    Action { Ok(s"User $id") }\n  case GET(p"/users" ? q"name=$name") =>\n    Action { Ok(s"Search: $name") }\n}',
    tags: ['play', 'sird', 'routing', 'dsl'],
  },
  {
    id: 'scala-http4s-l4-036',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How do you implement a custom error handler in Play that returns JSON errors for API routes and HTML for web routes?',
    options: ['Implement HttpErrorHandler that inspects the Accept header or path prefix to determine the response format', 'Error handlers cannot be customized', 'Using separate applications', 'Using a single error page'],
    correctAnswer: 0,
    explanation: 'Create a custom HttpErrorHandler that checks if the request path starts with /api or if Accept prefers JSON, then returns the appropriate error format.',
    codeSnippet: 'class ApiErrorHandler extends HttpErrorHandler {\n  def onClientError(req: RequestHeader, status: Int, msg: String) =\n    if (req.path.startsWith("/api"))\n      Future.successful(Status(status)(Json.obj("error" -> msg)))\n    else\n      Future.successful(Status(status)(views.html.error(status, msg)))\n}',
    tags: ['play', 'error-handler', 'content-negotiation'],
  },
  {
    id: 'scala-http4s-l4-037',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How do you implement request-scoped dependency injection in Play?',
    options: ['Request scoping is not possible', 'Use a custom RequestHandler or ActionBuilder that creates a child injector per request with request-scoped bindings', 'Using thread-local variables', 'Using session variables'],
    correctAnswer: 1,
    explanation: 'Create a child Guice injector per request with request-scoped bindings, or use custom ActionBuilders that thread request context through the request attribute.',
    tags: ['play', 'di', 'request-scoped', 'guice'],
  },
  {
    id: 'scala-http4s-l4-038',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How does Play handle thread pool management and why is it important for non-blocking performance?',
    options: ['Thread management is automatic', 'Play uses unlimited threads', 'Play uses separate dispatchers: a small default pool for non-blocking work and custom pools for blocking I/O, preventing thread starvation', 'Play uses a single thread'],
    correctAnswer: 2,
    explanation: 'Play\'s default execution context has a small thread pool optimized for non-blocking async work. Blocking I/O should use a separate CustomExecutionContext to prevent starving the default pool.',
    tags: ['play', 'thread-pools', 'performance', 'non-blocking'],
  },
  {
    id: 'scala-http4s-l4-039',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Controllers',
    question: 'How do you implement content streaming with chunked responses in Play?',
    options: ['Using Ok.chunked(source) or Ok.sendEntity with streaming body from Akka Streams Source', 'Using Future only', 'Chunked responses are not supported', 'Using Ok(data)'],
    correctAnswer: 0,
    explanation: 'Ok.chunked takes an Akka Streams Source and streams it as a chunked HTTP response, enabling constant memory usage for large responses.',
    codeSnippet: 'def streamData = Action {\n  val source = Source.fromIterator(() => (1 to 1000000).iterator)\n    .map(i => ByteString(s"$i\\n"))\n  Ok.chunked(source).as("text/plain")\n}',
    tags: ['play', 'streaming', 'chunked', 'akka-streams'],
  },
  {
    id: 'scala-http4s-l4-040',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Controllers',
    question: 'How do you implement a custom BodyParser in Play for handling protobuf or other binary formats?',
    options: ['Custom body parsers are not possible', 'Extend BodyParser or use parse.when/parse.using to create a parser that accumulates and decodes the request body', 'Using parse.raw always', 'Using a filter instead'],
    correctAnswer: 1,
    explanation: 'Create a custom BodyParser by defining how to accumulate the raw bytes and decode them into the target type, with proper error handling and size limits.',
    codeSnippet: 'def protobuf[A](parser: Array[Byte] => A)(implicit ec: ExecutionContext): BodyParser[A] =\n  BodyParser("protobuf") { _ =>\n    Accumulator.source[ByteString].mapFuture { source =>\n      source.runWith(Sink.fold(ByteString.empty)(_ ++ _)).map { bytes =>\n        Right(parser(bytes.toArray))\n      }\n    }\n  }',
    tags: ['play', 'body-parser', 'custom', 'protobuf'],
  },
  {
    id: 'scala-http4s-l4-041',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play JSON',
    question: 'How do you implement JSON schema evolution in Play JSON where old clients may send incomplete data?',
    options: ['Reject old schemas', 'Use readWithDefault, readNullable, and orElse combinators to provide backward-compatible Reads', 'Schema evolution is not supported', 'Version the entire API'],
    correctAnswer: 1,
    explanation: 'Combine readWithDefault for new fields with defaults, readNullable for optional fields, and orElse for reading fields that may have been renamed.',
    codeSnippet: 'implicit val reads: Reads[User] = (\n  (__ \\ "name").read[String] ~\n  (__ \\ "email").read[String] ~\n  (__ \\ "role").readWithDefault[String]("user") ~\n  ((__ \\ "displayName").readNullable[String] orElse\n   (__ \\ "display_name").readNullable[String])  // Handle renamed field\n)(User.apply _)',
    tags: ['play', 'json', 'schema-evolution', 'backward-compatibility'],
  },
  {
    id: 'scala-http4s-l4-042',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play JSON',
    question: 'How do you implement JSON Writes that conditionally includes fields based on the user\'s permissions?',
    options: ['Create a Writes that takes a context parameter (or use OWrites with transformation) to include/exclude fields dynamically', 'Using view models only', 'Field filtering is not possible', 'Using separate Writes per role'],
    correctAnswer: 0,
    explanation: 'Create a function that returns Writes based on the user role, or write to JsObject and then filter keys. View-model pattern is often cleaner for this.',
    codeSnippet: 'def userWrites(role: Role): Writes[User] = Writes { user =>\n  val base = Json.obj("name" -> user.name, "email" -> user.email)\n  if (role.isAdmin) base ++ Json.obj("salary" -> user.salary, "ssn" -> user.ssn)\n  else base\n}',
    tags: ['play', 'json', 'writes', 'permissions', 'dynamic'],
  },
  {
    id: 'scala-http4s-l4-043',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Filters',
    question: 'How do you implement a Play filter that modifies the response body (e.g., injecting analytics scripts)?',
    options: ['Use an EssentialFilter that processes the response body Accumulator by transforming the ByteString stream', 'Filters cannot modify response bodies', 'Using a post-processing library', 'Using JavaScript injection only'],
    correctAnswer: 0,
    explanation: 'EssentialFilter can modify response bodies by transforming the Accumulator flow. Map the Result body source to inject or transform content.',
    codeSnippet: 'class InjectAnalytics @Inject()(implicit ec: ExecutionContext, mat: Materializer)\n  extends EssentialFilter {\n  def apply(next: EssentialAction) = EssentialAction { req =>\n    next(req).map { result =>\n      if (result.body.contentType.exists(_.contains("text/html"))) {\n        val script = "<script>analytics()</script>"\n        result.copy(body = result.body.map(b => b ++ ByteString(script)))\n      } else result\n    }\n  }\n}',
    tags: ['play', 'filters', 'response-body', 'transformation'],
  },
  {
    id: 'scala-http4s-l4-044',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Filters',
    question: 'How do you implement request correlation ID propagation across all Play requests and responses?',
    options: ['Using session cookies', 'Using thread-local storage', 'Create a filter that extracts or generates a correlation ID, adds it to the request, MDC, and response headers', 'Using a database table'],
    correctAnswer: 2,
    explanation: 'A filter extracts X-Correlation-Id from the request (or generates one), puts it in the MDC for logging, passes it through, and adds it to the response headers.',
    codeSnippet: 'class CorrelationIdFilter @Inject()(implicit ec: ExecutionContext) extends EssentialFilter {\n  def apply(next: EssentialAction) = EssentialAction { req =>\n    val corrId = req.headers.get("X-Correlation-Id").getOrElse(UUID.randomUUID().toString)\n    MDC.put("correlationId", corrId)\n    next(req.withHeaders(req.headers.add("X-Correlation-Id" -> corrId))).map {\n      _.withHeaders("X-Correlation-Id" -> corrId)\n    }\n  }\n}',
    tags: ['play', 'filters', 'correlation-id', 'observability'],
  },
  {
    id: 'scala-http4s-l4-045',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Slick',
    question: 'How do you implement multi-tenancy with Slick using separate schemas or row-level security?',
    options: ['Using separate table classes per tenant', 'Multi-tenancy requires NoSQL', 'Using separate databases', 'Use a custom session with schema setting per tenant, or add tenant_id column with automatic filtering via Slick query extensions'],
    correctAnswer: 3,
    explanation: 'Row-level: add tenant_id to tables and create base queries that always filter by tenant. Schema-level: set the schema search path per connection using custom before/after hooks.',
    codeSnippet: 'trait TenantScope {\n  def tenantId: Long\n  def tenantQuery[T <: TenantTable](query: TableQuery[T]) =\n    query.filter(_.tenantId === tenantId)\n}\n\ntrait TenantTable { self: Table[_] =>\n  def tenantId: Rep[Long]\n}',
    tags: ['slick', 'multi-tenancy', 'security', 'architecture'],
  },
  {
    id: 'scala-http4s-l4-046',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Slick',
    question: 'How do you optimize Slick queries for N+1 problem avoidance?',
    options: ['Using lazy loading', 'N+1 is not a problem with Slick', 'Using database caching', 'Use join queries or batch loading with .in filter instead of iterating and querying per entity'],
    correctAnswer: 3,
    explanation: 'Avoid N+1 by using explicit joins or batch queries (WHERE id IN (...)) instead of loading related entities one by one. Slick makes this explicit since it has no lazy loading.',
    codeSnippet: '// Instead of N+1:\n// users.result.flatMap(us => DBIO.sequence(us.map(u => orders.filter(_.userId === u.id).result)))\n// Use a join:\nval query = users.join(orders).on(_.id === _.userId)\n  .groupBy(_._1)\n  .map { case (user, group) => (user, group.map(_._2.amount).sum) }',
    tags: ['slick', 'n-plus-one', 'optimization', 'queries'],
  },
  {
    id: 'scala-http4s-l4-047',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Slick',
    question: 'How do you implement soft deletes with Slick using a reusable pattern?',
    options: ['Using database triggers', 'Create a trait with deletedAt column and override default queries to filter out soft-deleted rows', 'Using a separate archive table', 'Soft deletes require raw SQL'],
    correctAnswer: 1,
    explanation: 'Define a SoftDeletable trait adding a deletedAt column. Create base queries that filter WHERE deleted_at IS NULL and a softDelete method that sets deletedAt.',
    codeSnippet: 'trait SoftDeletable { self: Table[_] =>\n  def deletedAt: Rep[Option[Timestamp]]\n}\n\ndef active[T <: SoftDeletable](query: TableQuery[T]) =\n  query.filter(_.deletedAt.isEmpty)\n\ndef softDelete[T <: SoftDeletable](query: TableQuery[T], id: Long) =\n  query.filter(_.id === id).map(_.deletedAt).update(Some(now))',
    tags: ['slick', 'soft-delete', 'pattern', 'reusable'],
  },
  {
    id: 'scala-http4s-l4-048',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'How do you implement the tagless final pattern in an http4s application for maximum testability?',
    options: ['Abstract over the effect type F[_] with type class constraints, define services as traits parameterized by F, and only bind to IO at the edge', 'Using Free monads', 'Using mock frameworks', 'Using concrete IO everywhere'],
    correctAnswer: 0,
    explanation: 'Define all services/repos as traits parameterized by F[_] with Monad/Concurrent/etc. constraints. Wire everything together with IO only in the main entry point, enabling testing with different effect types.',
    codeSnippet: 'trait UserService[F[_]] {\n  def getUser(id: Long): F[Option[User]]\n}\n\nclass UserRoutes[F[_]: Concurrent](service: UserService[F]) {\n  val routes: HttpRoutes[F] = HttpRoutes.of {\n    case GET -> Root / "user" / LongVar(id) =>\n      service.getUser(id).flatMap {\n        case Some(u) => Ok(u.asJson)\n        case None    => NotFound()\n      }\n  }\n}',
    tags: ['http4s', 'tagless-final', 'architecture', 'testability'],
  },
  {
    id: 'scala-http4s-l4-049',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'How do you structure a large http4s application using the "modules" pattern?',
    options: ['One big file', 'Define modules as classes that take dependencies and expose HttpRoutes, compose them in a central wiring point using Resource', 'Using reflection-based DI', 'Using separate microservices'],
    correctAnswer: 1,
    explanation: 'Each module encapsulates related functionality (routes, services, repos) as a class with explicit dependencies. A central wiring point composes modules using Resource for lifecycle management.',
    codeSnippet: 'class UserModule[F[_]: Async](xa: Transactor[F]) {\n  private val repo = new DoobieUserRepo(xa)\n  private val service = new UserServiceImpl(repo)\n  val routes: HttpRoutes[F] = new UserRoutes(service).routes\n}\n\nclass App[F[_]: Async](xa: Transactor[F]) {\n  val userModule = new UserModule(xa)\n  val productModule = new ProductModule(xa)\n  val httpApp = (userModule.routes <+> productModule.routes).orNotFound\n}',
    tags: ['http4s', 'architecture', 'modules', 'organization'],
  },
  {
    id: 'scala-http4s-l4-050',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'How do you implement structured logging with context propagation in http4s using IOLocal?',
    options: ['Using global variables', 'Structured logging is not supported with IO', 'Using thread-local MDC', 'Use IOLocal (or Vault attributes) to propagate context, and a log wrapper that reads from IOLocal before logging'],
    correctAnswer: 3,
    explanation: 'IOLocal provides fiber-local storage in Cats Effect 3. Middleware sets context in IOLocal, and a logging wrapper reads it, ensuring proper context even with concurrent requests.',
    codeSnippet: 'def contextMiddleware(local: IOLocal[Map[String, String]])(http: HttpApp[IO]): HttpApp[IO] =\n  Kleisli { req =>\n    val ctx = Map("requestId" -> UUID.randomUUID().toString, "path" -> req.uri.path.renderString)\n    local.set(ctx) >> http(req)\n  }',
    tags: ['http4s', 'logging', 'IOLocal', 'context-propagation'],
  },
  {
    id: 'scala-http4s-l4-051',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'What are the implications of using Cats Effect 3 fiber-based concurrency for http4s server performance?',
    options: ['No impact on performance', 'Fibers are lightweight (no 1:1 thread mapping), enabling high concurrency with small thread pools; blocking operations must use Sync[F].blocking to avoid starving compute threads', 'Fibers are slower than threads', 'Each request gets a separate OS thread'],
    correctAnswer: 1,
    explanation: 'CE3 fibers multiplex onto a small work-stealing thread pool. This enables thousands of concurrent requests with minimal threads. But blocking ops must be marked with Sync[F].blocking to shift to a blocking pool.',
    tags: ['http4s', 'cats-effect', 'fibers', 'concurrency', 'performance'],
  },
  {
    id: 'scala-http4s-l4-052',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Testing',
    question: 'How do you implement integration testing for http4s routes with a real database using testcontainers?',
    options: ['Using H2 only', 'Integration tests are not valuable', 'Mock the database', 'Use testcontainers-scala to spin up a PostgreSQL container, create a Transactor pointing to it, and test routes with real SQL'],
    correctAnswer: 3,
    explanation: 'testcontainers-scala provides ephemeral PostgreSQL containers for tests. Create a Transactor for the container, run migrations, and test routes against real database behavior.',
    codeSnippet: 'class RoutesSpec extends AnyFunSuite with ForAllTestContainer {\n  override val container = PostgreSQLContainer()\n  lazy val xa = Transactor.fromDriverManager[IO](\n    "org.postgresql.Driver", container.jdbcUrl, container.username, container.password\n  )\n  test("GET /users returns users") {\n    val routes = new UserRoutes(new DoobieUserRepo(xa)).routes\n    val resp = routes.orNotFound.run(Request[IO](GET, uri"/users")).unsafeRunSync()\n    assert(resp.status == Ok)\n  }\n}',
    tags: ['http4s', 'testing', 'testcontainers', 'integration'],
  },
  {
    id: 'scala-http4s-l4-053',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Testing',
    question: 'How do you test http4s middleware in isolation?',
    options: ['Apply the middleware to a minimal test HttpApp and verify the transformation', 'Test with full application', 'Using integration tests only', 'Middleware cannot be tested'],
    correctAnswer: 0,
    explanation: 'Create a minimal HttpApp that returns a known response, apply the middleware, and verify the transformation (added headers, modified body, etc.).',
    codeSnippet: 'test("timing middleware adds X-Response-Time header") {\n  val inner = HttpApp[IO](_ => IO.pure(Response[IO](Status.Ok)))\n  val timed = timingMiddleware(inner)\n  val resp = timed.run(Request[IO]()).unsafeRunSync()\n  assert(resp.headers.get(ci"X-Response-Time").isDefined)\n}',
    tags: ['http4s', 'testing', 'middleware', 'isolation'],
  },
  {
    id: 'scala-http4s-l4-054',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Testing',
    question: 'How do you implement property-based testing for http4s JSON codecs?',
    options: ['Property testing does not apply to JSON', 'Using example-based tests only', 'Using mutation testing', 'Use ScalaCheck generators for your types and test that encoding then decoding yields the original value (roundtrip property)'],
    correctAnswer: 3,
    explanation: 'Define ScalaCheck generators for your domain types, then test the property that for all instances, decode(encode(a)) == a. This catches edge cases in codecs.',
    codeSnippet: 'implicit val arbUser: Arbitrary[User] = Arbitrary(\n  for {\n    name <- Gen.alphaStr.suchThat(_.nonEmpty)\n    age <- Gen.choose(0, 150)\n  } yield User(name, age)\n)\nproperty("User JSON roundtrip") = forAll { (user: User) =>\n  decode[User](user.asJson.noSpaces) == Right(user)\n}',
    tags: ['http4s', 'testing', 'property-based', 'scalacheck', 'json'],
  },
  {
    id: 'scala-http4s-l4-055',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Circe',
    question: 'How do you implement optics-based JSON transformations with Circe?',
    options: ['Using XPath', 'Using string manipulation', 'Using circe-optics with monocle to create lenses and traversals for deeply nested JSON transformations', 'Optics are not related to JSON'],
    correctAnswer: 2,
    explanation: 'circe-optics provides monocle integration for traversing and transforming JSON structures. Use JsonPath for navigating nested structures and modifying values.',
    codeSnippet: 'import io.circe.optics.JsonPath._\n\nval json = parse("""{"user":{"name":"Alice","scores":[1,2,3]}}""").getOrElse(Json.Null)\n\n// Modify nested field\nval updated = root.user.name.string.modify(_.toUpperCase)(json)\n\n// Traverse array\nval doubled = root.user.scores.each.int.modify(_ * 2)(json)',
    tags: ['circe', 'optics', 'monocle', 'json-transformation'],
  },
  {
    id: 'scala-http4s-l4-056',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Circe',
    question: 'How do you handle circular references in Circe encoding?',
    options: ['Circe handles them automatically', 'Circular references cause stack overflow', 'Using null references', 'Break the cycle by using lazy implicits, @JsonCodec with explicit excludes, or transforming to a DTO without cycles before encoding'],
    correctAnswer: 3,
    explanation: 'Circular references cause infinite recursion. Solutions: use lazy val for implicit codecs, exclude back-references with custom Encoder, or map to cycle-free DTOs before encoding.',
    codeSnippet: 'case class Parent(name: String, children: List[Child])\ncase class Child(name: String, parent: Option[Parent])\n\n// Break cycle in encoding\nimplicit val childEncoder: Encoder[Child] = Encoder.instance { c =>\n  Json.obj("name" -> c.name.asJson)  // Omit parent to break cycle\n}',
    tags: ['circe', 'circular-references', 'encoding', 'patterns'],
  },
  {
    id: 'scala-http4s-l4-057',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Circe',
    question: 'How do you implement a Circe Decoder that accumulates all errors instead of failing on the first one?',
    options: ['Decoders always fail-fast', 'Use Decoder.decodeAccumulating or AccumulatingDecoder which returns ValidatedNel instead of Either', 'Error accumulation is not possible', 'Using a custom parser'],
    correctAnswer: 1,
    explanation: 'Circe provides AccumulatingDecoder that accumulates all decoding errors as a NonEmptyList, useful for form validation where you want to report all errors at once.',
    codeSnippet: 'val result: ValidatedNel[DecodingFailure, User] =\n  userDecoder.decodeAccumulating(json.hcursor)\n// Returns all errors, not just the first one',
    tags: ['circe', 'decoder', 'error-accumulation', 'validation'],
  },
  {
    id: 'scala-http4s-l4-058',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Middleware',
    question: 'How do you implement a caching middleware for http4s that supports conditional GET (ETag/If-None-Match)?',
    options: ['Compute ETag from response body, cache responses keyed by URI+ETag, and return 304 Not Modified when client sends matching If-None-Match', 'Caching must be done at proxy level', 'Using HTTP cache headers only', 'Using Redis only'],
    correctAnswer: 0,
    explanation: 'Middleware computes ETags (e.g., hash of body), stores responses in a cache (MapRef or Redis), and checks If-None-Match headers to return 304 when content is unchanged.',
    codeSnippet: 'def caching(cache: MapRef[IO, Uri, Option[(String, Response[IO])]])(http: HttpApp[IO]): HttpApp[IO] =\n  Kleisli { req =>\n    req.headers.get[`If-None-Match`].flatMap { inm =>\n      // Check cache for matching ETag\n    }.getOrElse {\n      http(req).flatMap { resp =>\n        // Compute ETag, cache, and return with ETag header\n        ???\n      }\n    }\n  }',
    tags: ['http4s', 'middleware', 'caching', 'etag', 'conditional-get'],
  },
  {
    id: 'scala-http4s-l4-059',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Middleware',
    question: 'How do you implement request body validation middleware that rejects requests with bodies exceeding a size limit?',
    options: ['Both check Content-Length header and limit the body stream using .take to enforce actual body size', 'Check Content-Length header only', 'Using request.body.size', 'Size limits are server configuration only'],
    correctAnswer: 0,
    explanation: 'Check Content-Length for early rejection, but also limit the actual body stream since Content-Length can be spoofed or absent for chunked transfers.',
    codeSnippet: 'def maxBodySize(maxBytes: Long)(http: HttpApp[IO]): HttpApp[IO] = Kleisli { req =>\n  req.headers.get[`Content-Length`] match {\n    case Some(cl) if cl.length > maxBytes =>\n      PayloadTooLarge(s"Body exceeds $maxBytes bytes")\n    case _ =>\n      val limited = req.withBodyStream(req.body.take(maxBytes + 1))\n      http(limited)\n  }\n}',
    tags: ['http4s', 'middleware', 'validation', 'body-size'],
  },
  {
    id: 'scala-http4s-l4-060',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'CORS',
    question: 'What security considerations are important when configuring CORS in production?',
    options: ['Only the server port matters', 'CORS has no security implications', 'Never use wildcard origins with credentials, validate allowed origins against a whitelist, limit exposed headers, and set appropriate max-age', 'Allow all origins for convenience'],
    correctAnswer: 2,
    explanation: 'Wildcard origins (*) cannot be used with credentials. Origins should be whitelisted. Limit allowed methods/headers. Set max-age to reduce preflight requests. Never reflect arbitrary origins.',
    tags: ['cors', 'security', 'production', 'best-practices'],
  },
  {
    id: 'scala-http4s-l4-061',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How do you implement graceful degradation in a Play application when downstream services are unavailable?',
    options: ['Use circuit breaker pattern with Akka, provide fallback responses, and implement bulkhead pattern to isolate failures', 'Restart the server', 'Return 500 for all failures', 'Queue all requests'],
    correctAnswer: 0,
    explanation: 'Combine circuit breakers (prevent cascading failures), fallback responses (cached or default data), and bulkhead pattern (separate thread pools per downstream service).',
    codeSnippet: 'class ResilientService @Inject()(ws: WSClient, breaker: CircuitBreaker) {\n  def getData: Future[JsValue] =\n    breaker.withCircuitBreaker(\n      ws.url("http://downstream/api").get().map(_.json)\n    ).recover {\n      case _: CircuitBreakerOpenException => cachedFallback\n    }\n}',
    tags: ['play', 'resilience', 'circuit-breaker', 'graceful-degradation'],
  },
  {
    id: 'scala-http4s-l4-062',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How do you implement database connection pool monitoring in a Play/Slick application?',
    options: ['Log messages only', 'Connection pools do not need monitoring', 'Expose HikariCP metrics via JMX or Prometheus, monitor active/idle/waiting connections, and set alerts for pool exhaustion', 'Using database-side monitoring only'],
    correctAnswer: 2,
    explanation: 'HikariCP (used by Slick/Play-Slick) exposes metrics via JMX or Prometheus MeterRegistry. Monitor active connections, wait time, pool size, and connection creation rate.',
    tags: ['play', 'slick', 'monitoring', 'hikaricp', 'connection-pool'],
  },
  {
    id: 'scala-http4s-l4-063',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play WS',
    question: 'How do you implement mutual TLS (mTLS) authentication with Play WS?',
    options: ['Using HTTP Basic auth instead', 'TLS is automatic', 'mTLS is not supported in Play WS', 'Configure a custom SSLContext with client certificate keystore and trust store in ahc.ssl configuration'],
    correctAnswer: 3,
    explanation: 'Configure Play WS underlying AHC with a custom SSLContext that includes the client certificate (keystore) and trusted CA certificates (truststore) for mutual authentication.',
    codeSnippet: '// In application.conf:\n// play.ws.ssl {\n//   keyManager {\n//     stores = [{ type = "JKS", path = "client.jks", password = "changeit" }]\n//   }\n//   trustManager {\n//     stores = [{ type = "JKS", path = "truststore.jks", password = "changeit" }]\n//   }\n// }',
    tags: ['play', 'ws', 'mtls', 'security'],
  },
  {
    id: 'scala-http4s-l4-064',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you implement event sourcing with Doobie for an append-only event store?',
    options: ['Event sourcing requires specialized databases', 'Using Doobie normally', 'Using Slick instead', 'Create an append-only events table, write events as ConnectionIO, and reconstruct state by folding over the event stream'],
    correctAnswer: 3,
    explanation: 'Store events in an append-only table with aggregate ID and sequence number. Replay events using Doobie streaming query and fold them to reconstruct current state.',
    codeSnippet: 'def appendEvent(aggId: UUID, event: Event): ConnectionIO[Unit] =\n  sql"""INSERT INTO events (aggregate_id, sequence_nr, event_type, payload)\n        VALUES ($aggId, nextval(\'event_seq\'), ${event.eventType}, ${event.asJson})"""\n    .update.run.void\n\ndef loadState(aggId: UUID): ConnectionIO[AggregateState] =\n  sql"SELECT payload FROM events WHERE aggregate_id = $aggId ORDER BY sequence_nr"\n    .query[Json].stream.fold(AggregateState.empty)(applyEvent)',
    tags: ['doobie', 'event-sourcing', 'cqrs', 'architecture'],
  },
  {
    id: 'scala-http4s-l4-065',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Doobie',
    question: 'How do you handle long-running database operations in Doobie without blocking the compute thread pool?',
    options: ['Using connection timeouts', 'JDBC is inherently blocking; Doobie uses Cats Effect Sync[F].blocking to shift JDBC calls to the blocking thread pool', 'Using async JDBC drivers', 'They are automatically non-blocking'],
    correctAnswer: 1,
    explanation: 'JDBC is blocking by design. Cats Effect 3 Transactors shift JDBC operations to the blocking thread pool via Sync[F].blocking, keeping the compute pool free for non-blocking work.',
    tags: ['doobie', 'blocking', 'cats-effect', 'thread-pools'],
  },
  {
    id: 'scala-http4s-l4-066',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Tapir',
    question: 'How do you implement API versioning in Tapir with multiple versions coexisting?',
    options: ['Using separate applications', 'Using content negotiation only', 'Define base endpoints per version with path prefix, share common types via ADTs, and serve all versions from the same server', 'Versioning is not supported in Tapir'],
    correctAnswer: 2,
    explanation: 'Create version-specific base endpoints (in("v1") vs in("v2")), share common domain types, and compose all versioned routes into a single http4s application.',
    codeSnippet: 'val v1Base = endpoint.in("api" / "v1")\nval v2Base = endpoint.in("api" / "v2")\n\nval getUserV1 = v1Base.get.in("users" / path[Long]).out(jsonBody[UserV1])\nval getUserV2 = v2Base.get.in("users" / path[Long]).out(jsonBody[UserV2])\n\nval allRoutes = Http4sServerInterpreter[IO]().toRoutes(\n  List(getUserV1Impl, getUserV2Impl)\n)',
    tags: ['tapir', 'versioning', 'api-design', 'coexistence'],
  },
  {
    id: 'scala-http4s-l4-067',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Tapir',
    question: 'How do you implement file upload with progress tracking using Tapir and http4s?',
    options: ['Use Tapir multipartBody with streaming, and wrap the receive stream with a progress-tracking Pipe that updates a Ref', 'Upload the entire file first', 'Progress tracking requires WebSockets', 'Using a polling endpoint'],
    correctAnswer: 0,
    explanation: 'Define a multipart Tapir endpoint. In the server logic, wrap the incoming file stream with an fs2 Pipe that counts bytes and updates a progress Ref, queryable via a separate endpoint.',
    tags: ['tapir', 'upload', 'progress', 'streaming'],
  },
  {
    id: 'scala-http4s-l4-068',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'How do you implement OpenTelemetry integration with http4s for distributed tracing?',
    options: ['Using log-based tracing only', 'OpenTelemetry is not compatible with Scala', 'Use natchez or otel4s to create spans in middleware, propagate trace context via headers, and instrument routes and client calls', 'Using println for tracing'],
    correctAnswer: 2,
    explanation: 'Use otel4s (or natchez) for Cats Effect-integrated tracing. Middleware creates spans for incoming requests, propagates W3C trace context, and instruments outgoing client calls.',
    codeSnippet: 'def tracingMiddleware(tracer: Tracer[IO])(http: HttpApp[IO]): HttpApp[IO] =\n  Kleisli { req =>\n    val spanName = s"${req.method.name} ${req.uri.path.renderString}"\n    tracer.spanBuilder(spanName)\n      .withParent(extractContext(req.headers))\n      .build.use { span =>\n        http(req).flatTap(resp => span.addAttribute(Attribute("http.status_code", resp.status.code.toLong)))\n      }\n  }',
    tags: ['http4s', 'opentelemetry', 'tracing', 'observability'],
  },
  {
    id: 'scala-http4s-l4-069',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'How do you implement a service mesh sidecar proxy pattern purely in http4s?',
    options: ['Create an http4s proxy that intercepts requests, applies cross-cutting concerns (auth, retry, circuit-breaking), and forwards to the target service via Client', 'Using envoy only', 'http4s cannot act as a proxy', 'Service mesh requires Kubernetes'],
    correctAnswer: 0,
    explanation: 'An http4s server can act as a sidecar proxy: receive requests, apply middleware (auth, logging, metrics, retry), then forward via Client to the actual service, returning the response.',
    codeSnippet: 'def proxy(client: Client[IO], target: Uri): HttpApp[IO] = Kleisli { req =>\n  val proxied = req.withUri(target.resolve(req.uri))\n    .removeHeader(ci"Host")\n  client.run(proxied).use(IO.pure)\n}',
    tags: ['http4s', 'proxy', 'service-mesh', 'sidecar'],
  },
  {
    id: 'scala-http4s-l4-070',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'http4s Basics',
    question: 'What is the difference between Concurrent, Temporal, and Async constraints on the effect type in http4s?',
    options: ['They are the same', 'They are unrelated type classes', 'Only Async matters for http4s', 'Concurrent adds fibers/races, Temporal adds sleep/timeout, Async adds callback interop; each builds on the previous with more capabilities'],
    correctAnswer: 3,
    explanation: 'Monad < Concurrent (fibers, race, cancelation) < Temporal (sleep, timeout) < Async (callback interop, blocking). http4s requires different levels depending on features used.',
    tags: ['http4s', 'cats-effect', 'type-classes', 'constraints'],
  },
  {
    id: 'scala-http4s-l4-071',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Performance',
    question: 'What JVM tuning parameters are important for http4s/Cats Effect 3 applications?',
    options: ['JVM tuning does not affect Scala apps', 'Only heap size matters', 'Use -XX:+UseG1GC or ZGC, set appropriate heap size, tune GC pause targets, and consider -XX:ActiveProcessorCount for containerized environments', 'Default JVM settings are optimal'],
    correctAnswer: 2,
    explanation: 'Key settings: G1GC/ZGC for low-latency, appropriate -Xmx, -XX:ActiveProcessorCount for containers (affects CE3 thread pool size), and GC pause targets matching SLA requirements.',
    tags: ['http4s', 'jvm', 'performance', 'tuning'],
  },
  {
    id: 'scala-http4s-l4-072',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Performance',
    question: 'How do you identify and resolve memory leaks in an http4s application related to stream processing?',
    options: ['Memory leaks do not occur with fs2', 'Using System.gc()', 'Use heap dumps, check for unclosed Resources, verify streams are consumed/drained, and ensure proper error handling does not prevent cleanup', 'Restart the server periodically'],
    correctAnswer: 2,
    explanation: 'Common causes: unfinished streams accumulate in memory, Resources not properly released on error, response bodies not consumed by clients. Use heap dumps and stream lifecycle tracking.',
    tags: ['http4s', 'memory-leak', 'streaming', 'debugging'],
  },
  {
    id: 'scala-http4s-l4-073',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Performance',
    question: 'How do you benchmark http4s endpoints accurately?',
    options: ['Using System.currentTimeMillis', 'Count requests manually', 'Use wrk/gatling/k6 for load testing, JMH for micro-benchmarks, and ensure warm-up periods account for JIT compilation', 'Using unit test timing'],
    correctAnswer: 2,
    explanation: 'Use proper load testing tools (wrk, gatling, k6) with sufficient warm-up for JIT compilation. JMH for micro-benchmarks of specific components. Monitor GC pauses and thread pool utilization.',
    tags: ['http4s', 'benchmarking', 'performance', 'load-testing'],
  },
  {
    id: 'scala-http4s-l4-074',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Security',
    question: 'How do you prevent SSRF (Server-Side Request Forgery) in http4s when accepting user-provided URLs for the client to fetch?',
    options: ['Trust all URLs', 'SSRF is only a browser concern', 'Using CORS', 'Validate and sanitize URLs, block private/internal IP ranges, use DNS resolution checks, and apply URL allowlists'],
    correctAnswer: 3,
    explanation: 'Validate URLs against allowlists, resolve DNS and check against private IP ranges (10.x, 172.16.x, 192.168.x, 127.x), block file:// and other dangerous schemes, and use network policies.',
    tags: ['http4s', 'security', 'ssrf', 'validation'],
  },
  {
    id: 'scala-http4s-l4-075',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Security',
    question: 'How do you implement request signing verification for webhook endpoints in http4s?',
    options: ['Read the raw body as bytes, compute HMAC-SHA256 with the shared secret, and compare with the signature header', 'Using IP allowlisting only', 'Skip verification', 'Using TLS only'],
    correctAnswer: 0,
    explanation: 'For webhook security, read the raw request body, compute HMAC-SHA256 (or other agreed algorithm) using the shared secret, and compare with the signature sent in the request header.',
    codeSnippet: 'def verifySignature(secret: String)(req: Request[IO]): IO[Boolean] =\n  req.as[Array[Byte]].map { body =>\n    val expected = req.headers.get(ci"X-Hub-Signature-256").map(_.head.value)\n    val mac = Mac.getInstance("HmacSHA256")\n    mac.init(new SecretKeySpec(secret.getBytes, "HmacSHA256"))\n    val computed = "sha256=" + mac.doFinal(body).map("%02x".format(_)).mkString\n    expected.contains(computed)\n  }',
    tags: ['http4s', 'security', 'webhooks', 'hmac'],
  },
  {
    id: 'scala-http4s-l4-076',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Security',
    question: 'How do you implement Content Security Policy (CSP) headers in an http4s application?',
    options: ['CSP is handled by the CDN', 'Using a JavaScript library', 'CSP is browser-side only', 'Add CSP headers via middleware that constructs the policy string from a typed configuration'],
    correctAnswer: 3,
    explanation: 'Implement middleware that adds Content-Security-Policy headers. Build the policy from a typed configuration to avoid typos and ensure consistent policy across responses.',
    codeSnippet: 'def cspMiddleware(policy: String)(http: HttpApp[IO]): HttpApp[IO] = Kleisli { req =>\n  http(req).map(_.putHeaders(\n    Header.Raw(ci"Content-Security-Policy", policy)\n  ))\n}\nval policy = "default-src \'self\'; script-src \'self\' \'nonce-abc123\'; style-src \'self\'"',
    tags: ['http4s', 'security', 'csp', 'headers'],
  },
  {
    id: 'scala-http4s-l4-077',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How do you implement custom session management in Play without using the default cookie-based session?',
    options: ['Custom sessions are not possible', 'Using thread-local sessions', 'Using JWT only', 'Implement a custom session store (Redis/DB) with a session ID cookie, and use an ActionBuilder that loads/saves session data per request'],
    correctAnswer: 3,
    explanation: 'Replace default cookie session with server-side storage: generate session ID, store session data in Redis/DB, send only the session ID as a cookie, and manage lifecycle in custom ActionBuilder.',
    codeSnippet: 'class SessionAction(sessionStore: SessionStore, parser: BodyParsers.Default)\n  (implicit ec: ExecutionContext) extends ActionBuilderImpl(parser) {\n  override def invokeBlock[A](request: Request[A], block: Request[A] => Future[Result]) = {\n    val sessionId = request.cookies.get("sid").map(_.value).getOrElse(UUID.randomUUID().toString)\n    sessionStore.load(sessionId).flatMap { session =>\n      block(new SessionRequest(session, request))\n    }\n  }\n}',
    tags: ['play', 'sessions', 'custom', 'server-side'],
  },
  {
    id: 'scala-http4s-l4-078',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Play Framework Basics',
    question: 'How do you implement a blue-green deployment strategy with Play?',
    options: ['Restart and hope for the best', 'Play does not support zero-downtime deployment', 'Using hot-code swapping', 'Use the application lifecycle hooks (ApplicationLifecycle) for startup/shutdown coordination, and implement health checks that integrate with load balancer readiness probes'],
    correctAnswer: 3,
    explanation: 'Register startup tasks and graceful shutdown hooks via ApplicationLifecycle. Health check endpoints tell the load balancer when instances are ready/draining. Rolling deployment ensures no downtime.',
    tags: ['play', 'deployment', 'blue-green', 'lifecycle'],
  },
  {
    id: 'scala-http4s-l4-079',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Streaming',
    question: 'How do you implement backpressure between http4s response streaming and a slow client?',
    options: ['Disconnect slow clients', 'Drop data when client is slow', 'fs2 Stream naturally provides backpressure; when the client TCP buffer fills, the stream pauses automatically without extra work', 'Buffer everything in memory'],
    correctAnswer: 2,
    explanation: 'fs2 Streams are pull-based. When the client TCP receive buffer fills, the server network write blocks, which propagates backpressure through the fs2 stream, pausing upstream production.',
    tags: ['http4s', 'streaming', 'backpressure', 'fs2'],
  },
  {
    id: 'scala-http4s-l4-080',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Streaming',
    question: 'How do you implement a streaming aggregation pipeline in http4s that processes data in windows?',
    options: ['Use fs2 Stream groupWithin or sliding window combinators to aggregate data in time-based or count-based windows', 'Windowing requires Kafka', 'Collect all data first', 'Using external stream processors only'],
    correctAnswer: 0,
    explanation: 'fs2 provides groupWithin(n, d) for time/count based windowing, sliding for sliding windows, and scan for running aggregations. These work within http4s streaming pipelines.',
    codeSnippet: 'val aggregated: Stream[IO, Summary] =\n  eventStream\n    .groupWithin(1000, 5.seconds)  // Chunk by count or time\n    .map(chunk => Summary.aggregate(chunk.toList))',
    tags: ['http4s', 'streaming', 'windowing', 'aggregation', 'fs2'],
  },
  {
    id: 'scala-http4s-l4-081',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Client',
    question: 'How do you implement HTTP caching in http4s client middleware following RFC 7234?',
    options: ['Client-side caching is not useful', 'Store all responses', 'Using browser cache only', 'Implement middleware that checks Cache-Control directives, stores cacheable responses with their validators, and serves from cache when fresh or revalidates with conditional requests'],
    correctAnswer: 3,
    explanation: 'Implement RFC 7234-compliant caching: check response freshness (max-age, Expires), store cacheable responses, serve from cache when fresh, and send conditional requests (If-None-Match) when stale.',
    tags: ['http4s', 'client', 'caching', 'rfc7234'],
  },
  {
    id: 'scala-http4s-l4-082',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Client',
    question: 'How do you implement request prioritization in http4s client middleware?',
    options: ['Using Thread.priority', 'Priority requires OS support', 'Use a priority queue backed by Cats Effect Queue with a custom ordering, and dequeue in priority order for sending', 'All requests have equal priority'],
    correctAnswer: 2,
    explanation: 'Implement a priority-aware queue using CE3 Queue or PQueue. Client middleware enqueues requests with priorities, and a consumer dequeues in priority order, rate-limiting lower priorities.',
    tags: ['http4s', 'client', 'prioritization', 'queue'],
  },
  {
    id: 'scala-http4s-l4-083',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Testing',
    question: 'How do you implement chaos testing for http4s services to verify resilience?',
    options: ['Manually causing failures', 'Create middleware that randomly injects failures (latency, errors, connection resets) based on configurable probability', 'Using Chaos Monkey only', 'Chaos testing requires production traffic'],
    correctAnswer: 1,
    explanation: 'Implement chaos middleware that probabilistically adds latency (Temporal[F].sleep), returns error responses, or raises exceptions to test how the system handles failures.',
    codeSnippet: 'def chaosMiddleware(failRate: Double)(http: HttpApp[IO]): HttpApp[IO] = Kleisli { req =>\n  IO(Random.nextDouble()).flatMap {\n    case r if r < failRate * 0.5 => IO.sleep(5.seconds) >> http(req)  // Latency\n    case r if r < failRate => InternalServerError("Chaos!")           // Error\n    case _ => http(req)                                                // Normal\n  }\n}',
    tags: ['http4s', 'testing', 'chaos', 'resilience'],
  },
  {
    id: 'scala-http4s-l4-084',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Testing',
    question: 'How do you implement contract testing between http4s services using Tapir?',
    options: ['Using Pact only', 'Generate both server and client from the same Tapir endpoint definitions, ensuring API contracts are enforced at compile time', 'Contract testing is not possible with Scala', 'Manual testing only'],
    correctAnswer: 1,
    explanation: 'Tapir endpoint descriptions serve as the contract. Both server and client are generated from the same endpoints. Changes to the contract require updating both, caught at compile time.',
    tags: ['tapir', 'contract-testing', 'compile-time', 'api-contract'],
  },
  {
    id: 'scala-http4s-l4-085',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Architecture',
    question: 'How do you implement the CQRS pattern with http4s, separating read and write models?',
    options: ['Using separate microservices only', 'Separate command (write) and query (read) routes with different data stores and models, connected via events or eventual consistency', 'Use one route for everything', 'CQRS requires event sourcing'],
    correctAnswer: 1,
    explanation: 'Define separate routes for commands (POST/PUT/DELETE) and queries (GET). Commands write to the write model, emit events, and read projections consume events to update the read model.',
    codeSnippet: 'val commandRoutes: HttpRoutes[IO] = HttpRoutes.of {\n  case req @ POST -> Root / "orders" =>\n    req.as[CreateOrder].flatMap(cmd =>\n      commandService.execute(cmd) >> Created()\n    )\n}\nval queryRoutes: HttpRoutes[IO] = HttpRoutes.of {\n  case GET -> Root / "orders" =>\n    queryService.listOrders.flatMap(Ok(_))\n}',
    tags: ['http4s', 'cqrs', 'architecture', 'patterns'],
  },
  {
    id: 'scala-http4s-l4-086',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Architecture',
    question: 'How do you implement feature flags in an http4s application that can be toggled without redeployment?',
    options: ['Recompile for each flag change', 'Using build flags', 'Store flags in a Ref updated from an external source (config service, database), and check flags in middleware or route handlers', 'Using environment variables only'],
    correctAnswer: 2,
    explanation: 'Store feature flags in a Ref[IO, FeatureFlags] updated periodically from an external source. Middleware or service-level checks can gate features based on the current flag state.',
    codeSnippet: 'class FeatureFlagService(flags: Ref[IO, Map[String, Boolean]]) {\n  def isEnabled(flag: String): IO[Boolean] = flags.get.map(_.getOrElse(flag, false))\n}\n\n// In routes:\nfeatureFlags.isEnabled("new-search").flatMap {\n  case true  => newSearchHandler(req)\n  case false => legacySearchHandler(req)\n}',
    tags: ['http4s', 'feature-flags', 'architecture', 'runtime-config'],
  },
  {
    id: 'scala-http4s-l4-087',
    language: 'scala',
    level: 'level4',
    category: 'http4s & Play',
    subcategory: 'Architecture',
    question: 'How do you implement a saga pattern for distributed transactions across multiple http4s microservices?',
    options: ['Implement a saga orchestrator that sequences service calls with compensating actions, using Cats Effect for error handling and rollback coordination', 'Two-phase commit across HTTP services', 'Distributed transactions are not possible', 'Use distributed database transactions'],
    correctAnswer: 0,
    explanation: 'A saga orchestrator sequences operations across services. Each step has a compensating action. On failure, the orchestrator runs compensations in reverse. Use Resource or MonadCancel for cleanup.',
    codeSnippet: 'def saga: IO[OrderResult] = {\n  val reserveStock = client.expect[StockReservation](reserveReq)\n  val processPayment = client.expect[Payment](paymentReq)\n  \n  reserveStock.flatMap { stock =>\n    processPayment.handleErrorWith { err =>\n      // Compensate: release stock reservation\n      client.expect[Unit](releaseStockReq(stock.id)) >>\n        IO.raiseError(err)\n    }\n  }\n}',
    tags: ['http4s', 'saga', 'distributed-transactions', 'microservices'],
  },
];

export default questions;
