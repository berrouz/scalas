import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-consumers-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'What is the role of the "group leader" consumer during a rebalance?',
    options: [
      'The group leader is the consumer with the lowest consumer ID',
      'The first consumer to join the group becomes leader; it receives the full member list and is responsible for computing and returning the partition assignment to the coordinator',
      'The group leader handles all offset commits on behalf of the group',
      'The group leader is responsible for sending heartbeats for all group members',
    ],
    correctAnswer: 1,
    explanation: 'During rebalance, the coordinator sends all member subscriptions to the group leader, which runs the assignment strategy and sends the result back via SyncGroup.',
    tags: ['kafka', 'group-leader', 'rebalancing'],
  },
  {
    id: 'kafka-consumers-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'seek/assign',
    question: 'How do you implement a "rewind" pattern to re-process messages from a specific point in time?',
    options: [
      'Set auto.offset.reset=earliest and restart the consumer',
      'Call consumer.offsetsForTimes() to get the offset at the target timestamp, then consumer.seek() to that offset',
      'Delete and recreate the consumer group to reset all offsets',
      'Use kafka-consumer-groups.sh --reset-offsets with --by-duration flag',
    ],
    correctAnswer: 1,
    explanation: 'offsetsForTimes() queries the time index to map a timestamp to an offset; seek() then positions the consumer at that offset without restarting or modifying committed group offsets.',
    codeSnippet: `Map<TopicPartition, Long> query = Map.of(tp, targetEpochMs);\nMap<TopicPartition, OffsetAndTimestamp> result = consumer.offsetsForTimes(query);\nconsumer.seek(tp, result.get(tp).offset());`,
    tags: ['kafka', 'seek', 'time-based'],
  },
  {
    id: 'kafka-consumers-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'auto/manual commit',
    question: 'What is the "at-least-once" processing guarantee pattern with Kafka consumers?',
    options: [
      'Commit the offset before processing each message',
      'Use auto.commit=true with the default interval',
      'Process the message first, then commit the offset; on failure, re-process from the last committed offset',
      'Use transactions to atomically commit both the processing result and the offset',
    ],
    correctAnswer: 2,
    explanation: 'Committing after processing ensures no message is skipped on failure, but a crash between processing and commit causes re-processing — the defining characteristic of at-least-once semantics.',
    tags: ['kafka', 'at-least-once', 'semantics'],
  },
  {
    id: 'kafka-consumers-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'What is the `onPartitionsLost()` callback in `ConsumerRebalanceListener` (added in Kafka 2.4)?',
    options: [
      'Called when all consumer group members lose their partitions due to a cluster failure',
      'Called when partitions are revoked without the consumer having a chance to commit offsets, e.g., during a forced rebalance or fencing',
      'Called when the consumer subscription pattern no longer matches any existing topics',
      'Called when a partition\'s messages are expired and no longer available',
    ],
    correctAnswer: 1,
    explanation: 'onPartitionsLost() is called instead of onPartitionsRevoked() when the consumer loses assignments unexpectedly (e.g., epoch mismatch), signaling that offsets may not be committable.',
    tags: ['kafka', 'rebalance-listener', 'partitions-lost'],
  },
  {
    id: 'kafka-consumers-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'heartbeat',
    question: 'Since Kafka 3.x (KIP-848), how is the group management protocol changing for consumers?',
    options: [
      'Consumers will send heartbeats to ZooKeeper instead of the group coordinator',
      'The new consumer group protocol uses server-side assignment, removing the need for group leader-based client-side assignment and the JoinGroup/SyncGroup round trip',
      'Heartbeats will be eliminated and replaced with lease-based membership',
      'Consumers will use the Raft protocol for group coordination',
    ],
    correctAnswer: 1,
    explanation: 'KIP-848 (new group coordinator) moves partition assignment logic to the server, allowing incremental assignment and removing the stop-the-world rebalance dance entirely.',
    tags: ['kafka', 'kip-848', 'group-protocol'],
  },
  {
    id: 'kafka-consumers-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'lag monitoring',
    question: 'What is the difference between committed offset lag and end-to-end latency as consumer health metrics?',
    options: [
      'They are the same metric measured in different units',
      'Committed offset lag counts unprocessed messages; end-to-end latency measures the wall-clock time from message production to processing completion',
      'End-to-end latency only applies to real-time consumers; lag applies to batch consumers',
      'Committed lag is a broker metric; end-to-end latency is a consumer metric',
    ],
    correctAnswer: 1,
    explanation: 'High lag can mean slow consumer or high producer rate; end-to-end latency reveals the user-facing impact. A consumer with low lag but slow processing may have acceptable lag but unacceptable latency.',
    tags: ['kafka', 'lag', 'latency'],
  },
  {
    id: 'kafka-consumers-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'poll loop',
    question: 'What is `consumer.pause(partitions)` used for?',
    options: [
      'It permanently removes those partitions from the consumer\'s assignment',
      'It temporarily stops the consumer from fetching data for specified partitions in poll(), while keeping the partitions assigned and heartbeating',
      'It pauses the entire consumer group until all members call resume()',
      'It pauses offset commits for the specified partitions',
    ],
    correctAnswer: 1,
    explanation: 'pause() lets you apply backpressure to specific partitions (e.g., when a downstream sink is slow) while still calling poll() to maintain group liveness.',
    codeSnippet: `consumer.pause(Set.of(slowPartition));\n// ... backpressure resolved ...\nconsumer.resume(Set.of(slowPartition));`,
    tags: ['kafka', 'pause-resume', 'backpressure'],
  },
  {
    id: 'kafka-consumers-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'What is the `isolation.level` consumer setting and when would you set it to `read_committed`?',
    options: [
      'It controls whether the consumer can read from follower replicas',
      'It determines whether the consumer reads messages from open (uncommitted) Kafka transactions; read_committed skips transactional messages until their transaction commits or aborts',
      'It controls TLS encryption level for the consumer connection',
      'It determines whether the consumer can read compacted messages',
    ],
    correctAnswer: 1,
    explanation: 'read_committed ensures consumers see only finalized (committed or aborted marker seen) transactional messages, enabling end-to-end exactly-once semantics in consume-transform-produce pipelines.',
    codeSnippet: `props.put("isolation.level", "read_committed");`,
    tags: ['kafka', 'isolation-level', 'transactions'],
  },
  {
    id: 'kafka-consumers-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'What is the "generation ID" in Kafka consumer group protocol?',
    options: [
      'The number of times the consumer group has been created and destroyed',
      'A monotonically increasing counter incremented with each rebalance, used to fence stale group members from committing offsets after a rebalance',
      'The version of the partition assignment algorithm in use',
      'The ZooKeeper epoch for the group coordinator broker',
    ],
    correctAnswer: 1,
    explanation: 'Each rebalance increments the generation ID. Offset commits include the generation ID; the coordinator rejects commits from members with an outdated generation, preventing stale commits.',
    tags: ['kafka', 'generation-id', 'fencing'],
  },
  {
    id: 'kafka-consumers-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'static membership',
    question: 'What happens if two consumers start with the same `group.instance.id` in the same consumer group?',
    options: [
      'Both consumers receive the same partition assignment',
      'The second consumer triggers a fencing of the first, which receives an exception and is disconnected',
      'Both consumers are removed from the group and a rebalance occurs',
      'The group coordinator ignores the second consumer until the first one leaves',
    ],
    correctAnswer: 1,
    explanation: 'group.instance.id must be unique within the group. When a new member presents an existing static ID, the coordinator fences the old member with a FencedInstanceIdException.',
    tags: ['kafka', 'static-membership', 'fencing'],
  },
  {
    id: 'kafka-consumers-l3-011',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'deserializers',
    question: 'What is the purpose of `ErrorHandlingDeserializer` in Spring Kafka?',
    options: [
      'It retries deserialization with exponential backoff',
      'It wraps a delegate deserializer and, on failure, sets the deserialized value to null and adds error information to record headers, preventing consumer loop death',
      'It routes failed deserialization attempts to a dead letter topic automatically',
      'It validates deserialized objects against a schema before returning them',
    ],
    correctAnswer: 1,
    explanation: 'ErrorHandlingDeserializer catches DeserializationException, wraps the bad bytes in the record headers, and allows the application error handler to route or log the poison pill without crashing.',
    codeSnippet: `props.put("value.deserializer", ErrorHandlingDeserializer.class);\nprops.put(ErrorHandlingDeserializer.VALUE_DESERIALIZER_CLASS, AvroDeserializer.class);`,
    tags: ['kafka', 'error-handling-deserializer', 'spring-kafka'],
  },
  {
    id: 'kafka-consumers-l3-012',
    language: 'kafka',
    level: 'level3',
    category: 'Consumers',
    subcategory: 'poll loop',
    question: 'What is the recommended way to handle a long-running message processor without violating `max.poll.interval.ms`?',
    options: [
      'Increase max.poll.interval.ms to a very large value like Integer.MAX_VALUE',
      'Process messages asynchronously in a separate thread pool, call consumer.pause() on the partition, continue polling for liveness, then resume when processing completes',
      'Reduce max.poll.records to 1 so each poll processes exactly one message',
      'Use separate consumer instances per partition for parallel processing',
    ],
    correctAnswer: 1,
    explanation: 'Offloading work to a thread pool + pause/resume is the correct pattern: poll() keeps the consumer alive via heartbeats, while processing happens independently.',
    tags: ['kafka', 'async-processing', 'poll-loop'],
  },
];

export default questions;
