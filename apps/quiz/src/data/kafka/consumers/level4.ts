import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-consumers-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'Describe the exact protocol sequence during an eager consumer group rebalance.',
    options: [
      'SyncGroup → JoinGroup → partition assignment → LeaveGroup',
      'LeaveGroup → JoinGroup → SyncGroup → partition assignment',
      'All members send JoinGroup (triggering revocation on ACK); group leader computes assignment; all members send SyncGroup; coordinator distributes assignment',
      'Coordinator directly pushes new assignments to each consumer without member requests',
    ],
    correctAnswer: 2,
    explanation: 'In eager rebalancing: (1) All members send JoinGroup — this triggers partition revocation. (2) Group leader receives full member list, computes assignment. (3) All members send SyncGroup. (4) Coordinator distributes assignments from leader\'s SyncGroup response.',
    tags: ['kafka', 'join-group', 'sync-group'],
  },
  {
    id: 'kafka-consumers-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'auto/manual commit',
    question: 'How do you implement exactly-once consume-process-produce semantics using Kafka transactions?',
    options: [
      'Use enable.auto.commit=false on the consumer and acks=all on the producer',
      'Use consumer.commitSync() inside producer.commitTransaction()',
      'Use producer.sendOffsetsToTransaction(offsets, consumerGroupMetadata) to atomically include the consumed offset in the producer transaction, then commitTransaction()',
      'Set isolation.level=read_committed on both producer and consumer',
    ],
    correctAnswer: 2,
    explanation: 'sendOffsetsToTransaction() writes the consumer offsets as part of the producer transaction; both the output messages and the offset commit are atomically committed or aborted together.',
    codeSnippet: `producer.beginTransaction();\nproducer.send(new ProducerRecord<>("output", result));\nproducer.sendOffsetsToTransaction(offsets, consumer.groupMetadata());\nproducer.commitTransaction();`,
    tags: ['kafka', 'exactly-once', 'transactions'],
  },
  {
    id: 'kafka-consumers-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'What is the "consumer group protocol" generation mismatch and how does it prevent split-brain processing?',
    options: [
      'Generation mismatch causes the broker to reject all fetches from outdated consumers',
      'Offset commits with a stale generation ID are rejected by the coordinator; the stale member gets CommitFailedException and knows to stop processing',
      'Stale members automatically detect the mismatch via ZooKeeper watches and stop',
      'The group coordinator sends a kill signal to stale consumer JVM processes',
    ],
    correctAnswer: 1,
    explanation: 'If a rebalance occurs while a slow consumer is processing, the consumer will fail on its next commitSync/Async with CommitFailedException (stale generation), preventing it from committing offsets for partitions it no longer owns.',
    tags: ['kafka', 'generation-id', 'commit-failed-exception'],
  },
  {
    id: 'kafka-consumers-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'In incremental cooperative rebalancing, how many rebalance rounds are typically needed to move a partition?',
    options: [
      '1 round — identical to eager rebalancing',
      '2 rounds — first round revokes the partition from the old owner; second round assigns it to the new owner',
      '3 rounds — revoke, negotiate, assign',
      'Variable — depends on the number of partitions being moved',
    ],
    correctAnswer: 1,
    explanation: 'Cooperative rebalancing requires 2 rounds per moved partition: round 1 revokes partitions that need to move (owner stops consuming them); round 2 assigns them to new owners. Partitions that stay put are unaffected.',
    tags: ['kafka', 'cooperative-rebalancing', 'protocol'],
  },
  {
    id: 'kafka-consumers-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'seek/assign',
    question: 'What is the risk of using `consumer.assign()` with `consumer.seek()` for exactly-once semantics in a stateful application?',
    options: [
      'assign() does not support seek(); you must use subscribe()',
      'The application must handle crash recovery by reading checkpointed offsets from an external store and seeking to them, since there is no group coordinator managing offsets',
      'seek() resets the committed offset in __consumer_offsets, affecting other consumers in the group',
      'Exactly-once is not possible with assign(); it requires the group protocol',
    ],
    correctAnswer: 1,
    explanation: 'Without the group protocol, offset management is entirely the application\'s responsibility. Storing processed offsets atomically with application state (e.g., in a database transaction) is necessary for exactly-once guarantees.',
    tags: ['kafka', 'assign', 'exactly-once'],
  },
  {
    id: 'kafka-consumers-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'poll loop',
    question: 'What is the `fetch.min.bytes` and `fetch.max.wait.ms` interaction and how do they affect consumer latency?',
    options: [
      'fetch.min.bytes triggers immediate fetch regardless of fetch.max.wait.ms',
      'The broker waits until fetch.min.bytes of data is available OR fetch.max.wait.ms elapses before responding; setting high fetch.min.bytes increases throughput at the cost of higher latency',
      'fetch.max.wait.ms overrides fetch.min.bytes when the consumer is lagging',
      'fetch.min.bytes applies per partition; fetch.max.wait.ms applies per request',
    ],
    correctAnswer: 1,
    explanation: 'The broker holds the fetch request until either min bytes are available or the wait timer expires, enabling throughput/latency tradeoff tuning on the broker side.',
    codeSnippet: `props.put("fetch.min.bytes", 50000);      // wait for 50KB\nprops.put("fetch.max.wait.ms", 500);      // or up to 500ms`,
    tags: ['kafka', 'fetch-min-bytes', 'fetch-max-wait'],
  },
  {
    id: 'kafka-consumers-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'lag monitoring',
    question: 'What is the "consumer group offset reset" operation and what are the available reset strategies in `kafka-consumer-groups.sh`?',
    options: [
      'Only --to-earliest and --to-latest are available',
      '--to-earliest, --to-latest, --to-offset, --to-datetime, --by-duration, --from-file, --shift-by',
      '--to-earliest, --to-latest, --shift-by, --to-timestamp',
      '--reset-to-beginning and --reset-to-end only',
    ],
    correctAnswer: 1,
    explanation: 'The tool provides flexible reset strategies: absolute (earliest/latest/specific offset), relative (shift-by N), time-based (to-datetime/by-duration), and file-based (from-file with custom per-partition offsets).',
    codeSnippet: `kafka-consumer-groups.sh --bootstrap-server :9092 \\\n  --group my-group --topic orders \\\n  --reset-offsets --to-datetime 2026-01-01T00:00:00.000 --execute`,
    tags: ['kafka', 'offset-reset', 'consumer-groups'],
  },
  {
    id: 'kafka-consumers-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'static membership',
    question: 'How does static membership interact with session.timeout.ms when a consumer restarts?',
    options: [
      'Static members bypass session.timeout.ms and always trigger immediate rebalance on disconnect',
      'If a static member reconnects within session.timeout.ms, the coordinator restores its assignment without a rebalance; if it exceeds session.timeout.ms, a rebalance occurs',
      'Static members have an infinite session timeout; they can disconnect indefinitely without triggering rebalance',
      'Static membership sets session.timeout.ms automatically based on the restart frequency',
    ],
    correctAnswer: 1,
    explanation: 'session.timeout.ms is the grace period for static members: brief restarts (e.g., rolling deployments) within this window avoid rebalances; longer outages trigger a rebalance as expected.',
    tags: ['kafka', 'static-membership', 'session-timeout'],
  },
  {
    id: 'kafka-consumers-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'deserializers',
    question: 'What is the "poison pill" problem in Kafka consumption and what are the main strategies to handle it?',
    options: [
      'A message that exceeds max.message.bytes; handled by increasing that config',
      'A message that cannot be deserialized or processed, blocking the consumer; strategies include dead letter topics, skip-and-log, error headers with ErrorHandlingDeserializer, and custom error handlers',
      'A message from an expired producer PID; the consumer should re-request it from the producer',
      'A compaction tombstone that appears unexpectedly; handled by filtering null values',
    ],
    correctAnswer: 1,
    explanation: 'Poison pills block consumer progress if unhandled. Best practices include routing bad messages to a dead letter queue for investigation while continuing to process valid messages.',
    tags: ['kafka', 'poison-pill', 'error-handling'],
  },
  {
    id: 'kafka-consumers-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'heartbeat',
    question: 'What is the relationship between `heartbeat.interval.ms`, `session.timeout.ms`, and `max.poll.interval.ms` for a reliable consumer configuration?',
    options: [
      'All three should be set to the same value for consistency',
      'heartbeat.interval.ms should be ~1/3 of session.timeout.ms; session.timeout.ms < max.poll.interval.ms to ensure the coordinator detects slow polling before declaring the consumer dead',
      'max.poll.interval.ms should equal session.timeout.ms; heartbeat.interval.ms is independent',
      'session.timeout.ms should be greater than max.poll.interval.ms to allow heartbeats to compensate for slow polling',
    ],
    correctAnswer: 1,
    explanation: 'heartbeat.interval.ms << session.timeout.ms provides liveness detection tolerance. max.poll.interval.ms > session.timeout.ms is recommended so that slow-poll detection triggers the right error (rebalance vs session expiry).',
    tags: ['kafka', 'heartbeat', 'tuning'],
  },
  {
    id: 'kafka-consumers-l4-011',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'What happens to in-flight messages being processed when a consumer receives `CommitFailedException` due to a rebalance?',
    options: [
      'Kafka automatically rolls back the processing and re-delivers the messages to the new owner',
      'The messages have already been processed by the application; the new partition owner will re-process them from the last committed offset, resulting in at-least-once delivery',
      'The consumer can still commit by using the next generation ID',
      'The messages are lost because the old consumer cannot commit and the new owner skips them',
    ],
    correctAnswer: 1,
    explanation: 'CommitFailedException means the consumer processed messages it can no longer commit. The new owner reads from the last committed offset, causing duplicate processing — the inherent cost of at-least-once semantics during rebalances.',
    tags: ['kafka', 'commit-failed-exception', 'at-least-once'],
  },
  {
    id: 'kafka-consumers-l4-012',
    language: 'kafka',
    level: 'level4',
    category: 'Consumers',
    subcategory: 'lag monitoring',
    question: 'What is the difference between monitoring consumer lag via `__consumer_offsets` versus querying the broker\'s LEO directly?',
    options: [
      'They are equivalent; both give the same lag value',
      'Committed offset lag (from __consumer_offsets) reflects only durably committed progress; real-time position lag (from broker LEO minus in-memory position) reflects actual current progress but is not durable',
      'LEO-based lag is more accurate for batch consumers; committed-offset lag is better for streaming consumers',
      '__consumer_offsets lag includes only partitions being actively consumed; LEO lag includes all partitions',
    ],
    correctAnswer: 1,
    explanation: 'Committed offset lag is conservative (can be ahead of actual processing with auto-commit); in-memory position lag from consumer.position() reflects actual fetch progress but is lost on crash.',
    tags: ['kafka', 'consumer-lag', 'observability'],
  },
];

export default questions;
