import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-consumers-l1-001',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'How does a Kafka consumer join a consumer group?',
    options: [
      'By connecting directly to ZooKeeper and registering its ID',
      'By setting the `group.id` property and sending a JoinGroup request to the group coordinator',
      'By subscribing to a special internal Kafka topic',
      'By calling broker.registerConsumer() on the bootstrap server',
    ],
    correctAnswer: 1,
    explanation: 'A consumer with a configured `group.id` sends a JoinGroup request to the group coordinator broker, which manages membership and triggers partition assignment.',
    codeSnippet: `props.put("group.id", "order-processors");\nKafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(List.of("orders"));`,
    tags: ['kafka', 'consumer-groups', 'join-group'],
  },
  {
    id: 'kafka-consumers-l1-002',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'poll loop',
    question: 'What does `consumer.poll(Duration timeout)` return?',
    options: [
      'The number of messages available in all subscribed partitions',
      'A ConsumerRecords object containing all fetched records from the subscribed partitions',
      'A single ConsumerRecord from the next available partition',
      'The current consumer lag across all subscribed partitions',
    ],
    correctAnswer: 1,
    explanation: 'poll() returns a ConsumerRecords<K,V> batch containing records from all assigned partitions fetched in this call; it also triggers heartbeat sending and rebalance handling.',
    codeSnippet: `while (true) {\n  ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n  for (ConsumerRecord<String, String> record : records) { ... }\n}`,
    tags: ['kafka', 'poll-loop', 'consumer'],
  },
  {
    id: 'kafka-consumers-l1-003',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'auto/manual commit',
    question: 'What does `enable.auto.commit=true` do in a Kafka consumer?',
    options: [
      'Automatically creates the topic if it does not exist',
      'Periodically commits the current offset to Kafka in the background without explicit application calls',
      'Automatically resets the offset to earliest on consumer restart',
      'Enables automatic schema validation during consumption',
    ],
    correctAnswer: 1,
    explanation: 'Auto-commit periodically (every `auto.commit.interval.ms`) commits the latest polled offset, simplifying development at the cost of possible duplicate processing on restart.',
    tags: ['kafka', 'auto-commit', 'offsets'],
  },
  {
    id: 'kafka-consumers-l1-004',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'deserializers',
    question: 'What is the role of a deserializer in a Kafka consumer?',
    options: [
      'It decompresses the message payload',
      'It converts raw bytes received from Kafka into Java objects',
      'It validates that the message schema matches the expected version',
      'It decrypts encrypted message payloads',
    ],
    correctAnswer: 1,
    explanation: 'The deserializer converts the raw byte arrays stored in Kafka back into typed Java objects (e.g., String, Avro, JSON) for use by the application.',
    codeSnippet: `props.put("key.deserializer", "org.apache.kafka.common.serialization.StringDeserializer");\nprops.put("value.deserializer", "org.apache.kafka.common.serialization.StringDeserializer");`,
    tags: ['kafka', 'deserializer', 'consumer'],
  },
  {
    id: 'kafka-consumers-l1-005',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'What triggers a Kafka consumer group rebalance?',
    options: [
      'A new message being published to a subscribed topic',
      'A consumer joining, leaving, or crashing, or a topic partition count changing',
      'A broker restarting in the cluster',
      'A producer changing its acks configuration',
    ],
    correctAnswer: 1,
    explanation: 'Rebalances are triggered by membership changes (join/leave/crash) or partition topology changes; during a rebalance, partition assignments are redistributed among active consumers.',
    tags: ['kafka', 'rebalance', 'consumer-groups'],
  },
  {
    id: 'kafka-consumers-l1-006',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'seek/assign',
    question: 'What is the difference between `consumer.subscribe()` and `consumer.assign()`?',
    options: [
      'subscribe() is for a single partition; assign() is for multiple partitions',
      'subscribe() uses automatic partition assignment via the group; assign() manually specifies exact partitions, bypassing the group protocol',
      'assign() is the newer API that replaces subscribe()',
      'subscribe() is for compacted topics; assign() is for regular topics',
    ],
    correctAnswer: 1,
    explanation: 'subscribe() opts into dynamic partition assignment with group coordination; assign() lets you manually control which specific TopicPartitions to consume, without any group rebalancing.',
    codeSnippet: `// Manual assignment - no group coordinator\nconsumer.assign(List.of(new TopicPartition("orders", 0), new TopicPartition("orders", 1)));`,
    tags: ['kafka', 'subscribe', 'assign'],
  },
  {
    id: 'kafka-consumers-l1-007',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'max.poll.records',
    question: 'What does `max.poll.records` control?',
    options: [
      'The maximum number of partitions a consumer can be assigned',
      'The maximum number of records returned in a single poll() call',
      'The maximum time a consumer polls before timing out',
      'The maximum message size the consumer will accept',
    ],
    correctAnswer: 1,
    explanation: 'max.poll.records limits the number of records returned per poll() call, helping you control the processing batch size and avoid max.poll.interval.ms violations.',
    tags: ['kafka', 'max-poll-records', 'consumer'],
  },
  {
    id: 'kafka-consumers-l1-008',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'session.timeout',
    question: 'What happens when a Kafka consumer does not send a heartbeat within `session.timeout.ms`?',
    options: [
      'The consumer is paused but remains in the group',
      'The consumer is removed from the group and a rebalance is triggered',
      'The consumer is marked as lagging and receives lower priority',
      'The broker closes the consumer\'s socket connection',
    ],
    correctAnswer: 1,
    explanation: 'The group coordinator considers the consumer dead if it misses heartbeats beyond session.timeout.ms, removes it from the group, and triggers a rebalance to reassign its partitions.',
    tags: ['kafka', 'session-timeout', 'heartbeat'],
  },
  {
    id: 'kafka-consumers-l1-009',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'seek/assign',
    question: 'What does `consumer.seekToBeginning(partitions)` do?',
    options: [
      'Resets the consumer group offset to 0 in the __consumer_offsets topic',
      'Sets the consumer\'s fetch position to the earliest available offset in the given partitions',
      'Deletes all messages and resets the partition to empty',
      'Assigns the consumer to the first partition of each topic',
    ],
    correctAnswer: 1,
    explanation: 'seekToBeginning() sets the local fetch position (not the committed offset) to the earliest available message, causing the consumer to re-read from the start of the partition.',
    tags: ['kafka', 'seek', 'offsets'],
  },
  {
    id: 'kafka-consumers-l1-010',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'auto/manual commit',
    question: 'How do you manually commit offsets synchronously in Kafka?',
    options: [
      'consumer.commitOffset()',
      'consumer.flush()',
      'consumer.commitSync()',
      'consumer.ackOffset()',
    ],
    correctAnswer: 2,
    explanation: 'commitSync() blocks until the broker confirms the offset commit; it retries on retriable failures, ensuring the offset is durably stored before continuing.',
    codeSnippet: `consumer.poll(Duration.ofMillis(100));\nfor (ConsumerRecord<String, String> r : records) { process(r); }\nconsumer.commitSync();`,
    tags: ['kafka', 'commit-sync', 'manual-commit'],
  },
  {
    id: 'kafka-consumers-l1-011',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'heartbeat',
    question: 'What is the purpose of the `heartbeat.interval.ms` setting?',
    options: [
      'It controls how often the consumer fetches data from brokers',
      'It sets the frequency at which the consumer sends heartbeat signals to the group coordinator',
      'It determines how often consumer offsets are committed',
      'It controls the interval between reconnection attempts after a broker failure',
    ],
    correctAnswer: 1,
    explanation: 'The consumer sends periodic heartbeats at heartbeat.interval.ms intervals (recommended ≤ 1/3 of session.timeout.ms) to signal liveness to the group coordinator.',
    tags: ['kafka', 'heartbeat', 'consumer-groups'],
  },
  {
    id: 'kafka-consumers-l1-012',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'What is `auto.offset.reset` and when does it apply?',
    options: [
      'It resets all consumer group offsets when a rebalance occurs',
      'It determines where the consumer starts reading when no committed offset exists for a partition',
      'It automatically resets offsets after the retention period expires',
      'It controls whether offsets are stored in ZooKeeper or Kafka',
    ],
    correctAnswer: 1,
    explanation: 'auto.offset.reset (earliest/latest/none) kicks in only when there is no committed offset for the partition, e.g., when a new consumer group first subscribes.',
    codeSnippet: `props.put("auto.offset.reset", "earliest"); // read from start if no committed offset`,
    tags: ['kafka', 'auto-offset-reset', 'consumer'],
  },
  {
    id: 'kafka-consumers-l1-013',
    language: 'kafka',
    level: 'level1',
    category: 'Consumers',
    subcategory: 'lag monitoring',
    question: 'What is "consumer lag" in Kafka?',
    options: [
      'The time it takes the consumer to process a single message',
      'The difference between the partition\'s log end offset and the consumer\'s committed offset',
      'The number of rebalances the consumer has experienced',
      'The network latency between the consumer and the broker',
    ],
    correctAnswer: 1,
    explanation: 'Consumer lag is the number of messages the consumer is behind the tail of the partition; high lag indicates the consumer is not keeping up with the producer\'s write rate.',
    tags: ['kafka', 'consumer-lag', 'monitoring'],
  },
];

export default questions;
