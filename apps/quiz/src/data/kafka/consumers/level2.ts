import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-consumers-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'What is the difference between "eager" and "cooperative" rebalancing in Kafka consumers?',
    options: [
      'Eager rebalancing is faster; cooperative is more reliable',
      'Eager revokes all partitions from all consumers before reassignment; cooperative transfers only partitions that need to move',
      'Cooperative rebalancing only works with static membership; eager works with dynamic membership',
      'Eager rebalancing uses ZooKeeper; cooperative uses the internal group protocol',
    ],
    correctAnswer: 1,
    explanation: 'Eager (stop-the-world) rebalancing creates a processing gap while all partitions are unassigned and reassigned; cooperative (incremental) rebalancing minimizes this gap by only moving necessary partitions.',
    codeSnippet: `props.put("partition.assignment.strategy",\n  "org.apache.kafka.clients.consumer.CooperativeStickyAssignor");`,
    tags: ['kafka', 'rebalancing', 'cooperative'],
  },
  {
    id: 'kafka-consumers-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'auto/manual commit',
    question: 'What is the risk of using `commitAsync()` without an error callback?',
    options: [
      'Async commits are not retried on failure, so transient errors may result in missed offset commits without the application knowing',
      'commitAsync() blocks the poll loop, causing session timeouts',
      'Async commits write to ZooKeeper instead of Kafka, which is deprecated',
      'commitAsync() commits offsets for all consumer groups, not just the current one',
    ],
    correctAnswer: 0,
    explanation: 'commitAsync() does not retry on failure to avoid committing a stale offset over a newer one; without a callback, failures are silently ignored, risking re-processing on restart.',
    codeSnippet: `consumer.commitAsync((offsets, exception) -> {\n  if (exception != null) log.error("Commit failed for {}", offsets, exception);\n});`,
    tags: ['kafka', 'commit-async', 'error-handling'],
  },
  {
    id: 'kafka-consumers-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'max.poll.records',
    question: 'Why might you reduce `max.poll.records` when processing is slow?',
    options: [
      'To reduce the amount of data fetched from the broker per request',
      'To avoid violating `max.poll.interval.ms` by keeping per-poll processing time short enough',
      'To reduce consumer memory usage',
      'To ensure more even distribution of messages across consumer group members',
    ],
    correctAnswer: 1,
    explanation: 'If processing takes too long per poll, the consumer may not call poll() within max.poll.interval.ms, causing the group coordinator to assume it is dead and triggering a rebalance.',
    tags: ['kafka', 'max-poll-records', 'max-poll-interval'],
  },
  {
    id: 'kafka-consumers-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'static membership',
    question: 'What is static group membership in Kafka (`group.instance.id`)?',
    options: [
      'A mode where partition assignments never change regardless of member changes',
      'A mechanism that assigns a stable identity to a consumer, avoiding rebalances when it briefly disconnects and reconnects within session.timeout.ms',
      'A feature that persists group membership to disk for crash recovery',
      'A way to pin a consumer to specific partitions permanently',
    ],
    correctAnswer: 1,
    explanation: 'Static membership uses a persistent group.instance.id; if the consumer reconnects within session.timeout.ms, the coordinator restores its previous assignment without triggering a rebalance.',
    codeSnippet: `props.put("group.instance.id", "consumer-node-1");`,
    tags: ['kafka', 'static-membership', 'rebalancing'],
  },
  {
    id: 'kafka-consumers-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'seek/assign',
    question: 'How do you seek to a specific offset in a Kafka consumer?',
    options: [
      'consumer.setOffset(partition, offset)',
      'consumer.seek(TopicPartition tp, long offset)',
      'consumer.poll(offset)',
      'consumer.rewind(partition, offset)',
    ],
    correctAnswer: 1,
    explanation: 'consumer.seek(TopicPartition, long) sets the fetch position for the specified partition to the given offset; the next poll() will start reading from that offset.',
    codeSnippet: `TopicPartition tp = new TopicPartition("orders", 0);\nconsumer.seek(tp, 1000L); // start reading from offset 1000`,
    tags: ['kafka', 'seek', 'offsets'],
  },
  {
    id: 'kafka-consumers-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'lag monitoring',
    question: 'Which tool or method can you use to monitor consumer group lag from the command line?',
    options: [
      'kafka-topics.sh --describe',
      'kafka-consumer-groups.sh --describe --group <group-id>',
      'kafka-log-dirs.sh --describe',
      'kafka-configs.sh --describe --entity-type groups',
    ],
    correctAnswer: 1,
    explanation: 'kafka-consumer-groups.sh --describe shows each partition\'s LOG-END-OFFSET, CURRENT-OFFSET, and LAG for the specified consumer group.',
    codeSnippet: `kafka-consumer-groups.sh --bootstrap-server localhost:9092 \\\n  --describe --group my-group`,
    tags: ['kafka', 'consumer-lag', 'monitoring'],
  },
  {
    id: 'kafka-consumers-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'session.timeout',
    question: 'What is `max.poll.interval.ms` and how does it differ from `session.timeout.ms`?',
    options: [
      'They are identical; one is for older API versions',
      'session.timeout.ms covers heartbeat-based liveness; max.poll.interval.ms covers the maximum time between poll() calls — useful when processing is slow',
      'max.poll.interval.ms applies to producers; session.timeout.ms applies only to consumers',
      'session.timeout.ms is set on the broker; max.poll.interval.ms is a client-only setting',
    ],
    correctAnswer: 1,
    explanation: 'Heartbeats prove network liveness but do not prove processing progress; max.poll.interval.ms detects a consumer stuck in long processing without calling poll().',
    tags: ['kafka', 'max-poll-interval', 'session-timeout'],
  },
  {
    id: 'kafka-consumers-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'rebalancing',
    question: 'What is a `ConsumerRebalanceListener` used for?',
    options: [
      'Listening for changes in consumer group size metrics',
      'Running custom logic on partition assignment (onPartitionsAssigned) and revocation (onPartitionsRevoked) during rebalances',
      'Detecting when a rebalance is about to cause message loss',
      'Blocking rebalances until the application has finished processing current records',
    ],
    correctAnswer: 1,
    explanation: 'ConsumerRebalanceListener hooks let you commit offsets or save state in onPartitionsRevoked before partitions are taken away, and initialize state in onPartitionsAssigned.',
    codeSnippet: `consumer.subscribe(topics, new ConsumerRebalanceListener() {\n  public void onPartitionsRevoked(Collection<TopicPartition> partitions) { consumer.commitSync(); }\n  public void onPartitionsAssigned(Collection<TopicPartition> partitions) { /* init state */ }\n});`,
    tags: ['kafka', 'rebalance-listener', 'consumer'],
  },
  {
    id: 'kafka-consumers-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'auto/manual commit',
    question: 'What does committing a specific offset map mean in `commitSync(Map<TopicPartition, OffsetAndMetadata>)`?',
    options: [
      'It commits only those specific partitions\' offsets, not all currently assigned partitions',
      'It commits the offset of the last message already processed rather than the latest polled',
      'It commits offset + 1 for each partition (the next offset to be fetched)',
      'Both A and C — it commits specified partitions only, and the value should be the next offset to fetch (last processed + 1)',
    ],
    correctAnswer: 3,
    explanation: 'The committed offset should be lastProcessedOffset + 1 (the next offset to fetch); you specify this per-partition map to commit only the partitions you have processed.',
    codeSnippet: `Map<TopicPartition, OffsetAndMetadata> offsets = new HashMap<>();\noffsets.put(tp, new OffsetAndMetadata(record.offset() + 1));\nconsumer.commitSync(offsets);`,
    tags: ['kafka', 'manual-commit', 'offsets'],
  },
  {
    id: 'kafka-consumers-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'consumer groups',
    question: 'What partition assignment strategy assigns partitions to minimize the number of partition movements during rebalance?',
    options: [
      'RoundRobinAssignor',
      'RangeAssignor',
      'StickyAssignor',
      'UniformStickyAssignor',
    ],
    correctAnswer: 2,
    explanation: 'StickyAssignor tries to maintain existing assignments as much as possible and only moves partitions that need to be redistributed, minimizing rebalance churn.',
    tags: ['kafka', 'sticky-assignor', 'rebalancing'],
  },
  {
    id: 'kafka-consumers-l2-011',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'deserializers',
    question: 'What happens if a Kafka consumer receives a message it cannot deserialize?',
    options: [
      'The message is silently skipped and the offset is committed',
      'A DeserializationException is thrown, which may cause the consumer to stop if not handled',
      'The consumer automatically retries with a different deserializer',
      'The message is moved to a dead letter topic automatically',
    ],
    correctAnswer: 1,
    explanation: 'Deserialization failures throw an exception in poll(); if not caught, the consumer loop dies. This is a common use case for dead letter queue patterns or error handlers in frameworks like Spring Kafka.',
    tags: ['kafka', 'deserialization-error', 'consumer'],
  },
  {
    id: 'kafka-consumers-l2-012',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'poll loop',
    question: 'Why should you avoid calling `consumer.poll()` from multiple threads simultaneously?',
    options: [
      'poll() is not thread-safe; a KafkaConsumer instance must be used from one thread only',
      'Multiple threads would duplicate offset commits',
      'Kafka brokers throttle connections from the same client.id on multiple threads',
      'Each thread would receive the same messages, causing duplicate processing',
    ],
    correctAnswer: 0,
    explanation: 'KafkaConsumer is not thread-safe. All operations including poll(), seek(), commit(), and close() must be called from the same thread that owns the consumer instance.',
    tags: ['kafka', 'thread-safety', 'consumer'],
  },
  {
    id: 'kafka-consumers-l2-013',
    language: 'kafka',
    level: 'level2',
    category: 'Consumers',
    subcategory: 'lag monitoring',
    question: 'How can you programmatically get consumer lag for a partition in Java?',
    options: [
      'consumer.getLag(topicPartition)',
      'consumer.endOffsets(partitions) minus consumer.position(partition)',
      'consumer.metrics().get("consumer-lag")',
      'consumer.committed(partitions) compared with consumer.beginningOffsets(partitions)',
    ],
    correctAnswer: 1,
    explanation: 'Lag = endOffset(partition) - currentPosition(partition). Use consumer.endOffsets() to get broker\'s LEO and consumer.position() for the current fetch position.',
    codeSnippet: `Map<TopicPartition, Long> endOffsets = consumer.endOffsets(assignedPartitions);\nlong lag = endOffsets.get(tp) - consumer.position(tp);`,
    tags: ['kafka', 'consumer-lag', 'programmatic'],
  },
];

export default questions;
