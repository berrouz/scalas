import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-ops-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Replication',
    question: 'What is "Fetch from Follower" (KIP-392) and what operational benefit does it provide?',
    options: [
      'Consumers can fetch messages directly from follower replicas to reduce leader load and cross-AZ traffic',
      'Follower replicas can serve Fetch requests from other followers, eliminating the leader from the replication path',
      'Producers can write to followers instead of the leader for lower-latency acknowledgment',
      'The log cleaner fetches compacted segments from a follower to avoid impacting the leader',
    ],
    correctAnswer: 0,
    explanation: 'KIP-392 (Kafka 2.4+) allows consumers to fetch from the geographically nearest replica rather than always going to the leader, reducing cross-AZ bandwidth costs and latency when consumers are co-located with a follower.',
    codeSnippet: `# Consumer config for follower fetching
client.rack=us-east-1a
# Broker config
replica.selector.class=org.apache.kafka.common.replica.RackAwareReplicaSelector`,
    tags: ['kafka', 'follower-fetch', 'kip-392', 'rack-awareness'],
  },
  {
    id: 'kafka-ops-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Log Compaction',
    question: 'A compacted topic has `min.compaction.lag.ms=3600000`. What does this guarantee?',
    options: [
      'Messages will be compacted within one hour of being written',
      'Messages will not be compacted (i.e., remain visible in the head) for at least one hour after being written',
      'Tombstone records are retained for at least one hour after the key\'s latest record',
      'The log cleaner runs at most once per hour for this topic',
    ],
    correctAnswer: 1,
    explanation: '`min.compaction.lag.ms` ensures messages in the head log are preserved for at least that duration before the cleaner can compact them, useful for consumers that are temporarily offline.',
    tags: ['kafka', 'log-compaction', 'min-compaction-lag', 'configuration'],
  },
  {
    id: 'kafka-ops-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'KRaft',
    question: 'In a KRaft deployment, what is the significance of the "metadata log" and how does it affect broker startup time?',
    options: [
      'The metadata log stores all topic data; brokers must replay it completely, making startup proportional to total data size',
      'The metadata log records cluster state changes; brokers must replay it from the last snapshot, so large metadata logs without recent snapshots slow startup',
      'The metadata log is kept in memory only and does not affect startup time',
      'Brokers only read the last 100 entries of the metadata log on startup regardless of its total size',
    ],
    correctAnswer: 1,
    explanation: 'KRaft brokers replay the `__cluster_metadata` log from the last persisted snapshot forward. Without periodic snapshots, the log grows and slows startup. KIP-630 introduced automatic snapshotting to bound replay time.',
    tags: ['kafka', 'kraft', 'metadata-log', 'snapshot', 'startup'],
  },
  {
    id: 'kafka-ops-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'ISR',
    question: 'What is the "ISR shrink/expand" storm and how can it be mitigated?',
    options: [
      'A situation where all partitions lose their leaders simultaneously; mitigated by increasing `min.insync.replicas`',
      'Frequent oscillation of replicas in/out of the ISR due to GC pauses or I/O spikes; mitigated by tuning `replica.lag.time.max.ms` and JVM GC settings',
      'A replication storm caused by too many partitions on a single broker; mitigated by partition reassignment',
      'Metadata updates flooding ZooKeeper when ISR membership changes; mitigated by batching ZooKeeper writes',
    ],
    correctAnswer: 1,
    explanation: 'JVM stop-the-world GC pauses or I/O saturation can cause followers to miss their fetch window, triggering repeated ISR changes. Tuning GC (G1GC), reducing JVM heap, or increasing `replica.lag.time.max.ms` reduces churn.',
    tags: ['kafka', 'isr', 'gc-pause', 'replication', 'tuning'],
  },
  {
    id: 'kafka-ops-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'What is the difference between `BytesInPerSec` and `ReplicationBytesInPerSec` on a broker?',
    options: [
      'They are identical metrics with different JMX paths',
      '`BytesInPerSec` counts bytes from producers; `ReplicationBytesInPerSec` counts bytes received from other brokers as follower fetch responses',
      '`BytesInPerSec` includes all traffic; `ReplicationBytesInPerSec` counts only SSL-encrypted replication bytes',
      '`BytesInPerSec` is per-topic; `ReplicationBytesInPerSec` is cluster-wide',
    ],
    correctAnswer: 1,
    explanation: 'Distinguishing producer ingress (`BytesInPerSec`) from replication ingress (`ReplicationBytesInPerSec`) is essential for capacity planning: a broker with high replication load but low producer load may be a follower-heavy node.',
    tags: ['kafka', 'jmx', 'bytes-in', 'replication', 'monitoring'],
  },
  {
    id: 'kafka-ops-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Segment Files',
    question: 'How does Kafka use the OS page cache and what operational implication does this have for broker sizing?',
    options: [
      'Kafka manages its own in-process cache independent of the OS page cache',
      'Kafka relies heavily on the OS page cache for read performance; brokers should have enough RAM to hold the active read working set in page cache, and JVM heap should be kept small',
      'Kafka pins log segments into page cache using `mlock`, requiring elevated OS permissions',
      'The page cache is used only for index files; log data bypasses it via O_DIRECT',
    ],
    correctAnswer: 1,
    explanation: 'Kafka intentionally writes to the OS page cache (not JVM heap) and relies on the OS to serve reads from cache. Brokers should have modest JVM heaps (6–8 GB) and large available RAM for page cache to maximize throughput.',
    tags: ['kafka', 'page-cache', 'broker-sizing', 'performance', 'ops'],
  },
  {
    id: 'kafka-ops-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Leader Election',
    question: 'What happens to in-flight produce requests with `acks=all` when a partition undergoes leader election?',
    options: [
      'They are immediately returned with a success acknowledgment by the new leader',
      'The producer receives a `NotLeaderOrFollowerException` and retries; the new leader must have all in-sync data, but requests that were only partially replicated before the old leader died are lost if `unclean.leader.election.enable=true`',
      'All in-flight requests are buffered by ZooKeeper and replayed to the new leader automatically',
      'In-flight requests are committed by the follower before it accepts the leader role',
    ],
    correctAnswer: 1,
    explanation: 'A leader failover causes `NotLeaderOrFollowerException` on in-flight requests. With clean election (from ISR), committed data is safe. With unclean election, records acknowledged only by the dead leader may be lost.',
    tags: ['kafka', 'leader-election', 'acks-all', 'data-loss', 'producer'],
  },
  {
    id: 'kafka-ops-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'What is the `ActiveControllerCount` metric and what does a value of 0 mean for a cluster?',
    options: [
      'The number of active consumers; 0 means no consumers are reading',
      'The number of active controller nodes; 0 means the cluster has no elected controller and cannot perform metadata operations (topic creation, leader election)',
      'The number of active partition reassignments; 0 means no reassignments are running',
      'The number of ZooKeeper sessions; 0 means all brokers are disconnected from ZooKeeper',
    ],
    correctAnswer: 1,
    explanation: '`ActiveControllerCount` should always be exactly 1 across the cluster. If it reaches 0, the cluster cannot elect leaders or manage topic changes. If it exceeds 1 ("split brain"), it indicates a serious metadata consistency issue.',
    tags: ['kafka', 'controller', 'monitoring', 'jmx', 'cluster-health'],
  },
  {
    id: 'kafka-ops-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Cruise Control',
    question: 'How does Cruise Control\'s self-healing feature work in response to broker failures?',
    options: [
      'It automatically replaces the failed broker with a new VM from a cloud provider',
      'It detects under-replicated partitions caused by broker failure and generates a reassignment plan to move affected replicas to healthy brokers, then executes it automatically if self-healing is enabled',
      'It increases `replication.factor` on affected topics to compensate for the lost replica',
      'It promotes follower replicas to leaders to restore serving capacity without moving data',
    ],
    correctAnswer: 1,
    explanation: 'When Cruise Control detects a broker failure goal violation (under-replicated partitions), its self-healing executor generates and applies a partition reassignment plan to restore the desired replication factor on remaining brokers.',
    tags: ['kafka', 'cruise-control', 'self-healing', 'broker-failure', 'ops'],
  },
  {
    id: 'kafka-ops-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Log Compaction',
    question: 'What is `max.compaction.lag.ms` and how does it interact with `min.cleanable.dirty.ratio`?',
    options: [
      'It is an alias for `log.retention.ms` in compacted topics',
      '`max.compaction.lag.ms` ensures messages are compacted within that time even if the dirty ratio has not been reached, overriding `min.cleanable.dirty.ratio`',
      'It sets the maximum time a compaction job can run before being aborted',
      '`max.compaction.lag.ms` and `min.cleanable.dirty.ratio` are mutually exclusive; only one can be set per topic',
    ],
    correctAnswer: 1,
    explanation: '`max.compaction.lag.ms` is an upper bound: even if the dirty ratio has not been reached, any message older than this value must be compacted. It guarantees freshness of the compacted view in low-write topics.',
    tags: ['kafka', 'log-compaction', 'max-compaction-lag', 'configuration'],
  },
  {
    id: 'kafka-ops-l4-011',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Replication',
    question: 'What is "epoch fencing" in Kafka\'s leader epoch mechanism and what problem does it solve?',
    options: [
      'Rate-limiting of follower fetch requests to prevent leader overload',
      'Each leader is assigned a monotonically increasing epoch; followers reject writes from old leaders using stale epochs, preventing split-brain data corruption after network partitions',
      'Fencing ZooKeeper sessions of failed leaders to prevent them from writing after re-connection',
      'Epoch counters used to invalidate consumer group metadata when members rejoin',
    ],
    correctAnswer: 1,
    explanation: 'Leader epochs (introduced in KIP-101) give each leader a unique, incrementing epoch number. Followers use epochs to detect divergence after failover and truncate their logs to the correct point, preventing data loss and inconsistency.',
    tags: ['kafka', 'leader-epoch', 'fencing', 'split-brain', 'replication'],
  },
  {
    id: 'kafka-ops-l4-012',
    language: 'kafka',
    level: 'level4',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'What does `NetworkProcessorAvgIdlePercent` close to 0 indicate and what should you adjust?',
    options: [
      'All network processor threads are saturated; increase `num.network.threads` to handle more concurrent connections',
      'The broker is receiving no traffic; check the load balancer configuration',
      'SSL handshake overhead is causing packet drops; disable TLS compression',
      'Consumer fetch threads are exhausted; increase `num.replica.fetchers`',
    ],
    correctAnswer: 0,
    explanation: 'Network processor threads handle socket reads and writes. When `NetworkProcessorAvgIdlePercent` approaches 0, increasing `num.network.threads` (default 3) distributes the connection load across more threads.',
    codeSnippet: `# server.properties
num.network.threads=6
num.io.threads=16`,
    tags: ['kafka', 'jmx', 'network-threads', 'monitoring', 'tuning'],
  },
];

export default questions;
