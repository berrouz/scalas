import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-ops-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'ISR',
    question: 'A replica is removed from the ISR. Under what condition is it allowed back in?',
    options: [
      'When its broker restarts successfully',
      'When it has fetched all messages up to the leader\'s log-end-offset and maintained that for one replica.lag.time.max.ms interval',
      'When the controller acknowledges its re-join request via ZooKeeper',
      'When min.insync.replicas is temporarily reduced',
    ],
    correctAnswer: 1,
    explanation: 'A lagging replica re-enters the ISR once it has fully caught up to the leader\'s log-end-offset and has been fetching continuously within `replica.lag.time.max.ms`.',
    tags: ['kafka', 'isr', 'replica-recovery', 'replication'],
  },
  {
    id: 'kafka-ops-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Log Compaction',
    question: 'What is the "head" vs "tail" distinction in a Kafka compacted log?',
    options: [
      'Head is the newest uncompacted segment; tail is the set of already-compacted older segments',
      'Head contains compacted data; tail contains raw messages awaiting compaction',
      'Head is the leader replica; tail is a set of follower replicas',
      'Head refers to message headers; tail refers to message payloads',
    ],
    correctAnswer: 0,
    explanation: 'In a compacted log, the tail consists of compacted segments where only one value per key exists. The head is the active/dirty portion with potential duplicates that has not yet been compacted.',
    tags: ['kafka', 'log-compaction', 'log-head', 'log-tail'],
  },
  {
    id: 'kafka-ops-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'What does the `RequestHandlerAvgIdlePercent` JMX metric reveal about broker health?',
    options: [
      'The fraction of request handler threads (I/O threads) that are idle; low values indicate CPU saturation',
      'The average time requests spend waiting in the network buffer',
      'The percentage of fetch requests served from page cache',
      'The fraction of time brokers spend on log compaction',
    ],
    correctAnswer: 0,
    explanation: '`RequestHandlerAvgIdlePercent` close to 0% means request handler threads are fully saturated. This is a key indicator of broker CPU bottleneck and the need to increase `num.io.threads`.',
    tags: ['kafka', 'jmx', 'request-handler', 'monitoring', 'cpu'],
  },
  {
    id: 'kafka-ops-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Segment Files',
    question: 'How does Kafka\'s log retention by time work at the segment level?',
    options: [
      'Individual messages are deleted as soon as their timestamp exceeds `log.retention.ms`',
      'An entire segment is deleted only when the timestamp of its last message exceeds `log.retention.ms`',
      'Segments are deleted when the oldest message timestamp exceeds `log.retention.ms`',
      'Log retention by time requires `cleanup.policy=delete` and scans all messages on each cycle',
    ],
    correctAnswer: 1,
    explanation: 'Retention works at segment granularity: a segment is eligible for deletion when the timestamp of its most recent (last) message is older than `log.retention.ms`. This means the active segment is never deleted.',
    tags: ['kafka', 'log-retention', 'segment', 'time-retention'],
  },
  {
    id: 'kafka-ops-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Partition Reassignment',
    question: 'What is the difference between partition reassignment and `kafka-preferred-replica-election.sh`?',
    options: [
      'Reassignment moves replicas to different brokers; preferred replica election reassigns leadership within the existing replica set without moving data',
      'Preferred replica election physically moves log data; reassignment only updates metadata',
      'They are identical operations with different CLI interfaces',
      'Reassignment is automatic; preferred replica election requires manual JSON input',
    ],
    correctAnswer: 0,
    explanation: 'Partition reassignment copies data to new brokers. Preferred replica election is a lightweight metadata operation that simply promotes the first-listed replica to leader, balancing leadership without data movement.',
    tags: ['kafka', 'partition-reassignment', 'preferred-replica', 'ops'],
  },
  {
    id: 'kafka-ops-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Replication',
    question: 'What is the "Log End Offset" (LEO) and how does it differ from the High Watermark?',
    options: [
      'LEO is the next offset to be written on the leader; HW is the offset up to which all ISR replicas have replicated',
      'LEO is the consumer\'s current position; HW is the maximum offset ever written',
      'LEO is only tracked on followers; HW is a leader-only concept',
      'They are synonymous terms for the same offset value',
    ],
    correctAnswer: 0,
    explanation: 'LEO is the offset of the next message to be appended, representing how far each replica has written. The HW is the minimum LEO across all ISR replicas, marking the safe read boundary for consumers.',
    tags: ['kafka', 'leo', 'high-watermark', 'replication'],
  },
  {
    id: 'kafka-ops-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'How does Burrow\'s sliding-window consumer evaluation work?',
    options: [
      'It computes a moving average of `records-lag` over a fixed time window and alerts if the trend is increasing',
      'It records committed offsets over a configurable window; if offsets are not advancing relative to the end offset, the consumer is flagged as ERR',
      'It reads `__consumer_offsets` every second and compares with the current LEO to compute lag percentage',
      'It sends synthetic test messages and measures end-to-end latency',
    ],
    correctAnswer: 1,
    explanation: 'Burrow tracks committed offsets over a sliding window (N intervals). If offsets increase but the lag is growing, it is WARN; if offsets stop changing but the partition continues to receive messages, it is ERR.',
    tags: ['kafka', 'burrow', 'consumer-lag', 'sliding-window'],
  },
  {
    id: 'kafka-ops-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Leader Election',
    question: 'In KRaft mode, what replaces ZooKeeper for controller election and metadata storage?',
    options: [
      'An embedded etcd cluster managed by the broker processes',
      'A Raft-based quorum of KRaft controller nodes using the `__cluster_metadata` topic',
      'A dedicated metadata broker that acts as a single point of truth',
      'The Schema Registry, which stores metadata alongside schema definitions',
    ],
    correctAnswer: 1,
    explanation: 'KRaft uses the Raft consensus protocol implemented within Kafka itself. Controller nodes replicate cluster metadata through the `__cluster_metadata` internal topic, forming a quorum without ZooKeeper.',
    tags: ['kafka', 'kraft', 'raft', 'controller'],
  },
  {
    id: 'kafka-ops-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Log Compaction',
    question: 'What is `log.cleaner.backoff.ms` and when does it apply?',
    options: [
      'The time the log cleaner waits between checking if any log needs cleaning',
      'The delay after which dirty segments are force-compacted regardless of dirty ratio',
      'The cooldown period before a tombstone record is permanently deleted',
      'The interval at which the log cleaner reports progress to the controller',
    ],
    correctAnswer: 0,
    explanation: '`log.cleaner.backoff.ms` controls how long the log cleaner thread sleeps when it finds no dirty logs that meet the compaction threshold, reducing CPU usage when topics are clean.',
    tags: ['kafka', 'log-cleaner', 'log-compaction', 'configuration'],
  },
  {
    id: 'kafka-ops-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Replication',
    question: 'What is "replica fetch throttling" and why would you enable it?',
    options: [
      'Throttling consumer fetch rates to prevent overloading leader replicas',
      'Limiting the bandwidth followers use to replicate from the leader, typically during partition reassignment to avoid saturating the network',
      'Reducing the number of fetch threads on followers when CPU is high',
      'Capping the fetch response size to prevent JVM GC pauses on followers',
    ],
    correctAnswer: 1,
    explanation: 'During partition reassignment, new replicas fetch large amounts of historical data. Throttling via `quota.replication.throttled.rate` limits this bandwidth to protect client-facing throughput.',
    codeSnippet: `kafka-configs.sh --bootstrap-server host:9092 \\
  --alter --entity-type brokers --entity-default \\
  --add-config 'follower.replication.throttled.rate=10000000'`,
    tags: ['kafka', 'replication-throttle', 'partition-reassignment', 'ops'],
  },
  {
    id: 'kafka-ops-l3-011',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'What does the `OfflinePartitionsCount` metric indicate and why is it critical?',
    options: [
      'The number of partitions whose consumers have gone offline',
      'The number of partitions with no available leader, making them completely unavailable for reads and writes',
      'The count of partitions excluded from preferred replica election',
      'The number of partitions currently undergoing log compaction',
    ],
    correctAnswer: 1,
    explanation: '`OfflinePartitionsCount > 0` is a cluster emergency: those partitions have no elected leader, meaning all produce and consume operations for them fail. It typically means all ISR members are down.',
    tags: ['kafka', 'offline-partition', 'monitoring', 'availability'],
  },
  {
    id: 'kafka-ops-l3-012',
    language: 'kafka',
    level: 'level3',
    category: 'Operations & Internals',
    subcategory: 'Segment Files',
    question: 'What is the purpose of the `.snapshot` file in a Kafka partition directory?',
    options: [
      'A periodic backup of the segment created by the log cleaner',
      'A producer epoch snapshot used to support idempotent and transactional producers by recording the latest sequence numbers',
      'A compressed snapshot of the compacted log tail for faster startup',
      'A ZooKeeper-synchronized state file for the partition controller',
    ],
    correctAnswer: 1,
    explanation: '`.snapshot` files store the producer state (epoch, sequence numbers, last offset) for idempotent and transactional producers, enabling recovery after a broker restart without data duplication.',
    tags: ['kafka', 'snapshot', 'idempotent', 'transactional', 'storage'],
  },
];

export default questions;
