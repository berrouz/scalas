import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-ops-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'ISR',
    question: 'What configuration determines how long a replica can fall behind before being removed from the ISR?',
    options: [
      'replica.fetch.max.bytes',
      'replica.lag.time.max.ms',
      'replica.socket.timeout.ms',
      'min.insync.replicas',
    ],
    correctAnswer: 1,
    explanation: '`replica.lag.time.max.ms` (default 30000 ms) is the maximum time a follower replica can go without sending a fetch request to the leader before being kicked out of the ISR.',
    tags: ['kafka', 'isr', 'replica-lag', 'configuration'],
  },
  {
    id: 'kafka-ops-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Unclean Leader Election',
    question: 'What does `unclean.leader.election.enable=true` allow?',
    options: [
      'Electing a leader from outside the ISR when no ISR replicas are available, potentially losing data',
      'Promoting a replica to leader without waiting for ISR acknowledgment',
      'Electing any broker as controller without a ZooKeeper quorum',
      'Skipping preferred replica election during rolling restarts',
    ],
    correctAnswer: 0,
    explanation: 'Unclean leader election allows an out-of-sync replica to become leader when all ISR replicas are unavailable. This avoids downtime but can result in data loss and is disabled by default.',
    tags: ['kafka', 'unclean-leader-election', 'availability', 'data-loss'],
  },
  {
    id: 'kafka-ops-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Log Compaction',
    question: 'What happens to a message with a null value (`tombstone`) in a compacted Kafka topic?',
    options: [
      'It is rejected by the broker with a validation error',
      'It marks the key for deletion; after compaction, both the tombstone and all earlier records for that key are removed',
      'It resets the offset for that key to zero',
      'It is stored permanently and never compacted away',
    ],
    correctAnswer: 1,
    explanation: 'A tombstone (null-value record) signals that the key should be deleted. The compaction process retains the tombstone for a configurable period (`delete.retention.ms`) before removing it along with older key records.',
    tags: ['kafka', 'log-compaction', 'tombstone', 'deletion'],
  },
  {
    id: 'kafka-ops-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Segment Files',
    question: 'What triggers the creation of a new log segment file?',
    options: [
      'A new consumer joining a group',
      'Either `log.segment.bytes` size is reached or `log.roll.ms` time elapses',
      'The broker\'s heap usage exceeding 75%',
      'A partition reassignment completing',
    ],
    correctAnswer: 1,
    explanation: 'A new segment is rolled when the active segment reaches `log.segment.bytes` (default 1 GB) or when `log.roll.ms` elapses, whichever comes first.',
    tags: ['kafka', 'log-segment', 'log-roll', 'storage'],
  },
  {
    id: 'kafka-ops-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Monitoring',
    question: 'What does the `kafka.consumer:type=consumer-fetch-manager-metrics,client-id=X,attribute=records-lag-max` JMX metric represent?',
    options: [
      'The maximum number of records fetched in a single poll',
      'The maximum consumer lag (in number of records) across all partitions assigned to the consumer',
      'The number of failed fetch requests per second',
      'The maximum time a fetch request waited before being fulfilled',
    ],
    correctAnswer: 1,
    explanation: '`records-lag-max` is the key consumer-side lag metric: it reports the maximum number of records the consumer is behind the log-end-offset across all assigned partitions.',
    tags: ['kafka', 'consumer-lag', 'monitoring', 'jmx'],
  },
  {
    id: 'kafka-ops-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Cruise Control',
    question: 'What is the primary purpose of LinkedIn\'s Cruise Control for Kafka?',
    options: [
      'SSL certificate rotation across all brokers',
      'Automated partition rebalancing to optimize resource utilization across brokers',
      'Managing consumer group offsets when brokers fail',
      'Providing a REST API for topic creation',
    ],
    correctAnswer: 1,
    explanation: 'Cruise Control continuously monitors broker and partition metrics and automatically (or on demand) generates and executes partition reassignment plans to balance CPU, network, and disk load.',
    tags: ['kafka', 'cruise-control', 'rebalancing', 'ops'],
  },
  {
    id: 'kafka-ops-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Preferred Replica Election',
    question: 'Why is preferred replica election important for cluster balance?',
    options: [
      'It forces all partitions to migrate to newly added brokers',
      'After broker restarts, it reassigns partition leadership back to the intended leaders to rebalance request load',
      'It triggers log compaction on under-utilized brokers',
      'It removes dead replicas from the ISR list',
    ],
    correctAnswer: 1,
    explanation: 'When a broker restarts after a failure, its partitions rejoin as followers. Preferred replica election promotes them back to leader (if they are the preferred replica), preventing load from concentrating on a few brokers.',
    tags: ['kafka', 'preferred-replica', 'load-balancing', 'ops'],
  },
  {
    id: 'kafka-ops-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Replication',
    question: 'What does a follower replica do to stay in sync with the leader?',
    options: [
      'It pushes new messages to the leader from its local cache',
      'It continuously sends Fetch requests to the leader and appends received messages to its local log',
      'It subscribes to a replication topic created by the controller',
      'It polls ZooKeeper for metadata updates and applies them locally',
    ],
    correctAnswer: 1,
    explanation: 'Follower replicas act like ordinary consumers: they send Fetch requests to the partition leader and replicate messages to their local log. If they fall too far behind, they are removed from the ISR.',
    tags: ['kafka', 'follower', 'replication', 'fetch'],
  },
  {
    id: 'kafka-ops-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Index Files',
    question: 'What is the `.timeindex` file used for in a Kafka partition?',
    options: [
      'Tracking when each broker last wrote to the partition',
      'Mapping timestamps to offsets for time-based message retrieval',
      'Recording compaction timestamps for each key',
      'Storing the time-to-live for each message',
    ],
    correctAnswer: 1,
    explanation: 'The time index (`.timeindex`) maps timestamps to offsets, enabling consumers to seek to a position by time (e.g., `seekToTimestamp`) without scanning the entire log.',
    tags: ['kafka', 'timeindex', 'timestamp', 'storage'],
  },
  {
    id: 'kafka-ops-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Burrow',
    question: 'What does LinkedIn\'s Burrow provide that standard Kafka tooling does not?',
    options: [
      'Automated topic creation based on schema registry entries',
      'Consumer lag evaluation with a sliding-window algorithm that distinguishes lagging from stalled consumers',
      'Encryption of consumer group offsets stored in `__consumer_offsets`',
      'Real-time dashboard for broker JMX metrics',
    ],
    correctAnswer: 1,
    explanation: 'Burrow evaluates consumer lag using a sliding window of committed offsets and classifies consumers as OK, WARNING, or ERR based on whether they are making progress, which is more useful than a raw lag number.',
    tags: ['kafka', 'burrow', 'consumer-lag', 'monitoring'],
  },
  {
    id: 'kafka-ops-l2-011',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Log Compaction',
    question: 'What is `min.cleanable.dirty.ratio` in Kafka log compaction?',
    options: [
      'The ratio of dirty (uncompacted) bytes to total log bytes at which the log cleaner begins compacting',
      'The minimum fraction of partitions that must be compacted before a broker accepts new writes',
      'The proportion of disk space reserved for the log cleaner threads',
      'The maximum ratio of null-value records allowed before tombstone cleanup runs',
    ],
    correctAnswer: 0,
    explanation: '`min.cleanable.dirty.ratio` (default 0.5) means the log cleaner starts compacting a partition once more than 50% of the log is "dirty" (written since the last clean).',
    tags: ['kafka', 'log-compaction', 'log-cleaner', 'configuration'],
  },
  {
    id: 'kafka-ops-l2-012',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Partition Reassignment',
    question: 'When generating a partition reassignment plan, which factor should you consider to avoid throttling issues?',
    options: [
      'The number of partitions must equal the number of brokers',
      'Setting `--replica-alter-log-dirs-throttle` to limit replication bandwidth during the move',
      'Reassignments can only be done during off-peak hours when the cluster is idle',
      'Partition count must be reduced by half before reassignment',
    ],
    correctAnswer: 1,
    explanation: 'Partition reassignment triggers inter-broker data movement. Using `--throttle` (or `--replica-alter-log-dirs-throttle`) limits replication bandwidth to avoid impacting producer and consumer performance.',
    codeSnippet: `kafka-reassign-partitions.sh \\
  --bootstrap-server host:9092 \\
  --reassignment-json-file plan.json \\
  --throttle 50000000 --execute`,
    tags: ['kafka', 'partition-reassignment', 'throttle', 'ops'],
  },
  {
    id: 'kafka-ops-l2-013',
    language: 'kafka',
    level: 'level2',
    category: 'Operations & Internals',
    subcategory: 'Replication',
    question: 'What is the High Watermark (HW) in Kafka replication?',
    options: [
      'The maximum offset a producer can write to',
      'The offset up to which all ISR replicas have confirmed receipt; consumers can only read up to this point',
      'The total number of bytes written to a partition since creation',
      'The retention limit in bytes for a partition',
    ],
    correctAnswer: 1,
    explanation: 'The High Watermark is the last committed offset — the point up to which all ISR replicas have replicated. Consumers receive only messages below the HW to ensure they do not read uncommitted data.',
    tags: ['kafka', 'high-watermark', 'replication', 'consumer'],
  },
];

export default questions;
