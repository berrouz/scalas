import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-perf-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Replication Fetch',
    question: 'What does `replica.fetch.max.bytes` control on a broker?',
    options: [
      'The maximum message size a producer can send',
      'The maximum number of bytes fetched per partition by follower replicas in a single request',
      'The maximum total bytes sent per fetch response to consumers',
      'The maximum bytes per partition stored in the OS page cache',
    ],
    correctAnswer: 1,
    explanation: '`replica.fetch.max.bytes` limits how much data a follower requests per partition per fetch. Increasing it helps when large messages cause replication bottlenecks, but it must be at least as large as `message.max.bytes`.',
    codeSnippet: `# server.properties
replica.fetch.max.bytes=2097152  # 2 MB
message.max.bytes=1500000`,
    tags: ['kafka', 'replication', 'replica-fetch', 'configuration'],
  },
  {
    id: 'kafka-perf-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Producer Batching',
    question: 'What is `buffer.memory` on a Kafka producer and what happens when it is exhausted?',
    options: [
      'The JVM heap allocated to the producer; when full, the JVM throws OutOfMemoryError',
      'The total bytes of producer record buffer; when full, `send()` blocks for `max.block.ms` then throws `BufferExhaustedException`',
      'The OS socket buffer size; when full, packets are dropped silently',
      'The in-memory cache for key serialization; when full, older keys are evicted',
    ],
    correctAnswer: 1,
    explanation: '`buffer.memory` (default 32 MB) is the total memory pool for record batches awaiting send. When exhausted, producers block up to `max.block.ms` before throwing `BufferExhaustedException`.',
    tags: ['kafka', 'producer', 'buffer-memory', 'backpressure'],
  },
  {
    id: 'kafka-perf-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Consumer Fetch',
    question: 'What is `fetch.max.wait.ms` and how does it interact with `fetch.min.bytes`?',
    options: [
      'It is the consumer timeout for waiting for broker acknowledgment; it replaces `fetch.min.bytes` when set',
      'It is the maximum time the broker waits for `fetch.min.bytes` to be available; the broker responds after either condition is met',
      'It limits the total time spent fetching across all partitions in a single poll loop',
      'It is the maximum age of a cached fetch response the consumer will accept',
    ],
    correctAnswer: 1,
    explanation: 'The broker waits up to `fetch.max.wait.ms` (default 500 ms) for enough data to satisfy `fetch.min.bytes`. Whichever threshold is hit first triggers the response, balancing latency and throughput.',
    tags: ['kafka', 'consumer', 'fetch-max-wait', 'fetch-min-bytes', 'latency'],
  },
  {
    id: 'kafka-perf-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'What is the benefit of using end-to-end compression (producer compresses, broker stores as-is, consumer decompresses)?',
    options: [
      'The broker re-compresses messages with a stronger codec before storing them to disk',
      'Data is transmitted and stored in compressed form, reducing network I/O and disk usage without broker CPU overhead for re-compression',
      'The broker validates message integrity more efficiently on compressed batches',
      'Consumers receive pre-decompressed messages to reduce client CPU usage',
    ],
    correctAnswer: 1,
    explanation: 'When the producer\'s compression codec matches the topic\'s configured codec, the broker avoids decompressing and re-compressing, reducing broker CPU load significantly on high-throughput topics.',
    tags: ['kafka', 'compression', 'end-to-end', 'broker-cpu'],
  },
  {
    id: 'kafka-perf-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'OS Page Cache',
    question: 'Why should Kafka brokers avoid using large JVM heaps (e.g., > 8 GB)?',
    options: [
      'Kafka\'s producer API does not support large heap allocations',
      'Large heaps increase stop-the-world GC pause duration, which can cause replicas to fall out of ISR',
      'The Kafka protocol limits per-broker memory to 8 GB',
      'Large heaps prevent the broker from using zero-copy transfers',
    ],
    correctAnswer: 1,
    explanation: 'Large JVM heaps (especially with CMS or old G1GC configurations) can trigger multi-second GC pauses, causing replication lag and ISR shrinkage. Keeping the heap small (4–8 GB) leaves more RAM for page cache.',
    tags: ['kafka', 'jvm', 'gc', 'heap', 'page-cache', 'performance'],
  },
  {
    id: 'kafka-perf-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Broker Threads',
    question: 'What is the difference between `num.network.threads` and `num.io.threads` on a Kafka broker?',
    options: [
      'Network threads handle TLS handshakes; I/O threads handle plaintext connections',
      'Network threads read/write socket data and queue requests; I/O threads dequeue and process those requests (including disk operations)',
      'They are aliases for the same thread pool used for all broker operations',
      'Network threads handle producer requests; I/O threads handle consumer fetch requests',
    ],
    correctAnswer: 1,
    explanation: 'Network threads (acceptor/network layer) deal with socket I/O and hand off parsed requests to the request queue. I/O threads pick up requests from the queue and perform disk reads, writes, and other processing.',
    tags: ['kafka', 'network-threads', 'io-threads', 'broker', 'architecture'],
  },
  {
    id: 'kafka-perf-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Log Flush',
    question: 'What is the risk of setting `log.flush.interval.ms` to a very low value (e.g., 1000 ms)?',
    options: [
      'It can cause log compaction to run too frequently',
      'Frequent fsyncs reduce write throughput significantly since each fsync blocks until the OS flushes dirty pages',
      'It conflicts with `min.insync.replicas` settings and causes producer errors',
      'Low flush intervals prevent the broker from using zero-copy transfers',
    ],
    correctAnswer: 1,
    explanation: 'Frequent fsyncs are expensive: each call blocks until the OS flushes dirty data to disk. This adds latency to write operations and reduces overall throughput compared to relying on replication for durability.',
    tags: ['kafka', 'log-flush', 'fsync', 'write-throughput'],
  },
  {
    id: 'kafka-perf-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Throughput vs Latency',
    question: 'A Kafka producer is optimized for low latency. Which settings should you prioritize?',
    options: [
      'High `batch.size`, high `linger.ms`, `compression.type=gzip`',
      'Low `linger.ms` (0–5 ms), moderate `batch.size`, `acks=1`',
      'High `buffer.memory`, low `max.block.ms`, `acks=all`',
      'High `max.in.flight.requests.per.connection`, `retries=0`, `batch.size=1`',
    ],
    correctAnswer: 1,
    explanation: 'For low latency, minimize batching delay (`linger.ms=0`) and reduce acknowledgment overhead (`acks=1`). Trade-off is lower throughput and reduced durability compared to `acks=all`.',
    codeSnippet: `props.put("linger.ms", "0");
props.put("batch.size", "16384");
props.put("acks", "1");`,
    tags: ['kafka', 'producer', 'low-latency', 'configuration'],
  },
  {
    id: 'kafka-perf-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Consumer Fetch',
    question: 'What does `max.partition.fetch.bytes` control and how does it differ from `fetch.max.bytes`?',
    options: [
      'They are synonymous; `max.partition.fetch.bytes` is the deprecated name',
      '`max.partition.fetch.bytes` limits bytes per partition per fetch; `fetch.max.bytes` limits the total bytes across all partitions in a single fetch response',
      '`max.partition.fetch.bytes` applies to producers; `fetch.max.bytes` applies to consumers',
      '`max.partition.fetch.bytes` is a broker setting; `fetch.max.bytes` is a consumer setting',
    ],
    correctAnswer: 1,
    explanation: 'Both are consumer settings. `max.partition.fetch.bytes` (default 1 MB) caps per-partition data, while `fetch.max.bytes` (default 50 MB) caps the total response size, preventing large fetch responses from causing OOM.',
    tags: ['kafka', 'consumer', 'fetch-bytes', 'configuration'],
  },
  {
    id: 'kafka-perf-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Performance Tuning',
    subcategory: 'Zero-Copy',
    question: 'Under what condition does Kafka\'s zero-copy transfer fail to apply?',
    options: [
      'When the broker has fewer than 4 CPU cores',
      'When the message batch is compressed or when SSL/TLS is enabled, requiring data to pass through the JVM for encryption',
      'When the consumer uses `fetch.min.bytes > 1`',
      'When the topic has a replication factor greater than 1',
    ],
    correctAnswer: 1,
    explanation: 'Zero-copy (`sendfile`) bypasses the JVM but cannot be used when SSL is enabled (data must be encrypted by the JVM) or when the broker needs to decompress and re-compress batches. In those cases, data passes through the JVM.',
    tags: ['kafka', 'zero-copy', 'ssl', 'compression', 'performance'],
  },
];

export default questions;
