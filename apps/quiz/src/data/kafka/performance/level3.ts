import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-perf-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Producer Batching',
    question: 'What is `max.in.flight.requests.per.connection` and what is its effect on ordering guarantees?',
    options: [
      'The maximum number of partitions a producer can write to simultaneously; higher values improve parallelism but break key ordering',
      'The maximum number of unacknowledged requests per broker connection; setting it above 1 with retries enabled can cause message reordering on retry',
      'The maximum batch pipeline depth per network thread; higher values reduce CPU but increase memory',
      'The number of simultaneous metadata fetches; does not affect ordering',
    ],
    correctAnswer: 1,
    explanation: 'With `max.in.flight.requests.per.connection > 1` and `retries > 0`, a failed batch may be retried after a later batch succeeds, causing reordering. Idempotent producers solve this by allowing up to 5 in-flight requests safely.',
    codeSnippet: `# Safe high-throughput with ordering
props.put("enable.idempotence", "true");
props.put("max.in.flight.requests.per.connection", "5");`,
    tags: ['kafka', 'producer', 'in-flight-requests', 'ordering', 'idempotent'],
  },
  {
    id: 'kafka-perf-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'What happens when a broker must "recompress" a producer batch?',
    options: [
      'The broker applies a stronger codec to further reduce disk usage',
      'The broker decompresses the producer batch and re-compresses it with the topic\'s configured codec, burning CPU and increasing produce latency',
      'The broker reorders records within the batch to improve compressibility',
      'Recompression is a consumer-side operation triggered by codec mismatch',
    ],
    correctAnswer: 1,
    explanation: 'If the producer uses codec A but the topic is configured with `compression.type=B`, the broker must decompress and re-compress every batch, adding significant CPU overhead. Always align producer and topic codecs.',
    tags: ['kafka', 'compression', 'recompression', 'broker-cpu'],
  },
  {
    id: 'kafka-perf-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'OS Page Cache',
    question: 'Why is it beneficial to have consumers that are "caught up" (near the head of the log) from a performance perspective?',
    options: [
      'Caught-up consumers receive smaller fetch responses, reducing network usage',
      'Recent data is likely still in the OS page cache, so caught-up consumers are served from RAM via zero-copy without triggering disk reads',
      'The broker uses a separate faster code path for consumers reading recent offsets',
      'Caught-up consumers skip the CRC validation step in the fetch path',
    ],
    correctAnswer: 1,
    explanation: 'Recently produced data stays in the OS page cache. Consumers reading near the end of the log hit the page cache instead of disk, keeping latency low and avoiding I/O pressure on the broker.',
    tags: ['kafka', 'page-cache', 'consumer', 'performance', 'zero-copy'],
  },
  {
    id: 'kafka-perf-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Broker Threads',
    question: 'When would you increase `num.replica.fetchers` on a Kafka broker?',
    options: [
      'When consumer fetch latency is high due to too few threads serving fetch requests',
      'When follower replicas are consistently lagging behind leaders, and the replication throughput per follower thread is the bottleneck',
      'When log compaction is slow and not keeping up with write volume',
      'When the broker has high network latency to ZooKeeper',
    ],
    correctAnswer: 1,
    explanation: '`num.replica.fetchers` (default 1) sets the number of threads per broker fetching from leaders. Increasing it improves replication throughput when many partitions need catching up and a single fetch thread cannot keep pace.',
    tags: ['kafka', 'replica-fetchers', 'replication', 'configuration'],
  },
  {
    id: 'kafka-perf-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Throughput vs Latency',
    question: 'What is "pipelining" in Kafka producer terms and how does it affect throughput?',
    options: [
      'Sending compressed messages in a pipeline through multiple brokers before acknowledgment',
      'Having multiple in-flight batches per broker connection so new batches can be sent without waiting for previous acknowledgments, increasing throughput',
      'Using a dedicated network thread per partition to overlap I/O operations',
      'Pre-fetching topic metadata in the background while sending records',
    ],
    correctAnswer: 1,
    explanation: 'Producer pipelining (`max.in.flight.requests.per.connection > 1`) allows multiple unacknowledged request windows per connection, keeping the network pipe full and dramatically increasing throughput over high-latency links.',
    tags: ['kafka', 'producer', 'pipelining', 'in-flight', 'throughput'],
  },
  {
    id: 'kafka-perf-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Consumer Fetch',
    question: 'What is the effect of setting a very large `max.poll.records` on consumer performance?',
    options: [
      'It reduces broker load by fetching all available messages in one request',
      'It can increase processing latency per poll loop and risk session timeouts (`session.timeout.ms`) if processing takes too long',
      'It always improves throughput because larger batches are more efficient',
      'It increases memory usage on the broker side for buffering the response',
    ],
    correctAnswer: 1,
    explanation: 'A large `max.poll.records` means each `poll()` returns more records to process. If processing is slow, the consumer may not call `poll()` within `max.poll.interval.ms`, triggering a group rebalance.',
    codeSnippet: `props.put("max.poll.records", "500");
props.put("max.poll.interval.ms", "300000");`,
    tags: ['kafka', 'consumer', 'max-poll-records', 'session-timeout'],
  },
  {
    id: 'kafka-perf-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Log Flush',
    question: 'For maximum write throughput, what is the recommended `log.flush.interval.messages` and `log.flush.interval.ms` setting?',
    options: [
      '`log.flush.interval.messages=1` to guarantee every message is fsynced',
      'Leave both at their defaults (effectively never fsync via Kafka) and rely on OS flushing and replication for durability',
      '`log.flush.interval.ms=100` to flush every 100 ms as a balanced compromise',
      '`log.flush.interval.messages=10000` as an absolute minimum for any production topic',
    ],
    correctAnswer: 1,
    explanation: 'For highest write throughput, Kafka\'s defaults (no explicit Kafka-level fsync) are recommended. Replication across multiple ISR replicas provides durability, and the OS flushes dirty pages in the background.',
    tags: ['kafka', 'log-flush', 'write-throughput', 'durability'],
  },
  {
    id: 'kafka-perf-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'OS Tuning',
    question: 'Which Linux kernel parameter is critical to tune for Kafka broker disk write performance?',
    options: [
      '`net.core.somaxconn` — the socket listen backlog size',
      '`vm.dirty_ratio` and `vm.dirty_background_ratio` — controlling when the kernel flushes dirty pages',
      '`kernel.shmmax` — the maximum shared memory segment size',
      '`fs.file-max` — the maximum number of open file handles',
    ],
    correctAnswer: 1,
    explanation: '`vm.dirty_ratio` (e.g., 60%) and `vm.dirty_background_ratio` (e.g., 10%) control OS write-back behavior. Tuning them prevents the OS from blocking writes when dirty memory exceeds the threshold, smoothing Kafka\'s write throughput.',
    codeSnippet: `# /etc/sysctl.conf
vm.dirty_ratio=60
vm.dirty_background_ratio=10
vm.swappiness=1`,
    tags: ['kafka', 'os-tuning', 'dirty-ratio', 'page-cache', 'linux'],
  },
  {
    id: 'kafka-perf-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Throughput vs Latency',
    question: 'What is the effect of increasing the number of partitions on producer throughput and consumer parallelism?',
    options: [
      'More partitions always improve both throughput and latency linearly',
      'More partitions allow more parallel producers and consumers but increase controller load, file descriptor usage, and end-to-end latency due to larger metadata',
      'More partitions reduce replication overhead because each replica handles fewer messages',
      'More partitions improve compaction efficiency by distributing cleaner work across threads',
    ],
    correctAnswer: 1,
    explanation: 'Partitions are the unit of parallelism: more partitions allow more concurrent producers/consumers. However, each partition adds overhead: file handles, ZooKeeper/KRaft nodes, and end-to-end latency from leader election. There are diminishing returns beyond a certain count.',
    tags: ['kafka', 'partitions', 'parallelism', 'throughput', 'tradeoff'],
  },
  {
    id: 'kafka-perf-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'In which scenario would `snappy` be preferred over `gzip` for Kafka compression?',
    options: [
      'When the messages contain binary data that does not compress well',
      'When CPU resources are constrained and moderate compression ratio is acceptable, since snappy is faster to compress/decompress than gzip',
      'When compatibility with older Kafka clients (< 0.8) is required',
      'When messages are very small (< 100 bytes) and gzip overhead is too large',
    ],
    correctAnswer: 1,
    explanation: 'Snappy provides moderate compression with much lower CPU overhead than gzip. It is a good middle ground when CPU is a bottleneck but some space savings are still desired.',
    tags: ['kafka', 'compression', 'snappy', 'gzip', 'cpu'],
  },
];

export default questions;
