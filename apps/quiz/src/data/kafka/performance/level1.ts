import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-perf-l1-001',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Producer Batching',
    question: 'What does `batch.size` control in a Kafka producer?',
    options: [
      'The maximum number of partitions a producer can write to simultaneously',
      'The maximum number of bytes the producer will batch together per partition before sending',
      'The number of network connections the producer maintains to the broker',
      'The maximum number of retries for a failed send',
    ],
    correctAnswer: 1,
    explanation: '`batch.size` (default 16384 bytes) sets the upper bound on the size of a batch of records sent together to a single partition leader. Larger batches improve throughput but increase latency.',
    tags: ['kafka', 'producer', 'batch-size', 'throughput'],
  },
  {
    id: 'kafka-perf-l1-002',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Producer Latency',
    question: 'What does `linger.ms` do in a Kafka producer?',
    options: [
      'Sets the timeout for waiting for broker acknowledgment',
      'Adds an artificial delay before sending a batch to allow more records to accumulate',
      'Controls how long the producer waits before retrying a failed send',
      'Defines the maximum idle time before a broker connection is closed',
    ],
    correctAnswer: 1,
    explanation: '`linger.ms` (default 0) tells the producer to wait up to that many milliseconds for additional records before sending a batch. Setting it to a small value (e.g., 5–20 ms) increases batch fill rate and throughput.',
    tags: ['kafka', 'producer', 'linger-ms', 'latency', 'batching'],
  },
  {
    id: 'kafka-perf-l1-003',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'Which compression codec generally offers the best compression ratio for Kafka messages?',
    options: [
      'snappy',
      'lz4',
      'gzip',
      'zstd',
    ],
    correctAnswer: 3,
    explanation: 'zstd (Zstandard) was added in Kafka 2.1 and typically achieves the best compression ratio among the supported codecs while also offering competitive decompression speed.',
    tags: ['kafka', 'compression', 'zstd', 'throughput'],
  },
  {
    id: 'kafka-perf-l1-004',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Consumer Fetch',
    question: 'What does `fetch.min.bytes` control on the consumer?',
    options: [
      'The minimum number of messages the consumer will process per poll',
      'The minimum amount of data the broker must have before responding to a fetch request',
      'The minimum batch size for offset commits',
      'The minimum bytes per partition the consumer requests',
    ],
    correctAnswer: 1,
    explanation: '`fetch.min.bytes` (default 1) makes the broker wait until at least this many bytes are available before responding. Increasing it reduces fetch request rate and improves throughput at the cost of higher latency.',
    tags: ['kafka', 'consumer', 'fetch-min-bytes', 'throughput'],
  },
  {
    id: 'kafka-perf-l1-005',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Throughput vs Latency',
    question: 'What is the general tradeoff when increasing `batch.size` and `linger.ms`?',
    options: [
      'Higher throughput with lower per-message latency',
      'Higher throughput but increased per-message latency',
      'Lower throughput but improved durability',
      'Lower throughput and higher CPU usage',
    ],
    correctAnswer: 1,
    explanation: 'Larger batches mean fewer network requests and better throughput, but individual messages wait longer in the buffer before being sent, increasing end-to-end latency.',
    tags: ['kafka', 'throughput', 'latency', 'tradeoff', 'producer'],
  },
  {
    id: 'kafka-perf-l1-006',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'At which layer is Kafka message compression applied?',
    options: [
      'Per-message on the broker before writing to disk',
      'Per-batch on the producer before sending to the broker',
      'Per-partition on the broker during log compaction',
      'Per-fetch response on the broker before sending to consumers',
    ],
    correctAnswer: 1,
    explanation: 'Kafka compression is applied by the producer at the batch level: the entire RecordBatch is compressed before transmission. The broker stores the compressed batch as-is and the consumer decompresses it.',
    tags: ['kafka', 'compression', 'producer', 'batch'],
  },
  {
    id: 'kafka-perf-l1-007',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Zero-Copy',
    question: 'What operating system feature does Kafka use to efficiently transfer data from disk to the network?',
    options: [
      'Memory-mapped files (mmap) with explicit flush calls',
      'The `sendfile` system call (zero-copy), avoiding unnecessary data copies through user space',
      'Direct I/O (O_DIRECT) to bypass the page cache',
      'Asynchronous I/O (io_uring) for non-blocking disk reads',
    ],
    correctAnswer: 1,
    explanation: 'Kafka uses `sendfile` (via Java\'s `FileChannel.transferTo`) to move data from the page cache directly to the network socket without copying through JVM heap, dramatically reducing CPU usage for consumers.',
    tags: ['kafka', 'zero-copy', 'sendfile', 'performance'],
  },
  {
    id: 'kafka-perf-l1-008',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Broker Threads',
    question: 'What does `num.io.threads` control on a Kafka broker?',
    options: [
      'The number of threads used to handle network connections',
      'The number of threads processing disk I/O requests from the request queue',
      'The number of threads performing log compaction',
      'The number of concurrent partition reassignments',
    ],
    correctAnswer: 1,
    explanation: '`num.io.threads` (default 8) sets the thread pool that processes requests taken from the network layer, including disk reads and writes. Increasing it helps when I/O operations are the bottleneck.',
    tags: ['kafka', 'io-threads', 'broker', 'configuration'],
  },
  {
    id: 'kafka-perf-l1-009',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'Which Kafka compression codec prioritizes CPU efficiency and speed over compression ratio?',
    options: [
      'gzip',
      'zstd',
      'lz4',
      'bzip2',
    ],
    correctAnswer: 2,
    explanation: 'lz4 is the fastest Kafka compression codec, offering very low CPU overhead for both compression and decompression, making it ideal for high-throughput, latency-sensitive pipelines.',
    tags: ['kafka', 'compression', 'lz4', 'cpu', 'performance'],
  },
  {
    id: 'kafka-perf-l1-010',
    language: 'kafka',
    level: 'level1',
    category: 'Performance Tuning',
    subcategory: 'Log Flush',
    question: 'By default, does Kafka call `fsync` after every message write?',
    options: [
      'Yes, Kafka fsyncs after every record to guarantee durability',
      'No, Kafka relies on OS page cache flushing and replication for durability, avoiding per-message fsync',
      'Yes, but only when `acks=all` is configured by the producer',
      'No, fsync is called only during log compaction',
    ],
    correctAnswer: 1,
    explanation: 'Kafka avoids per-message `fsync` by default for performance, relying on replication (multiple ISR copies) and OS background flushing to provide durability. `log.flush.interval.messages` and `log.flush.interval.ms` can be set to force periodic fsyncs.',
    tags: ['kafka', 'fsync', 'log-flush', 'durability', 'performance'],
  },
];

export default questions;
