import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-perf-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Producer Batching',
    question: 'Why does enabling idempotent producers (`enable.idempotence=true`) not degrade throughput despite adding sequence number tracking?',
    options: [
      'Idempotent producers disable compression to reduce overhead',
      'The sequence number is a small addition to the batch header, and the broker deduplication is O(1) per batch; the throughput benefit from up to 5 in-flight requests outweighs the overhead',
      'Idempotent mode uses a faster code path on the broker that bypasses ISR checks',
      'The JVM JIT optimizes away the sequence number bookkeeping after warmup',
    ],
    correctAnswer: 1,
    explanation: 'Idempotent producers add a 4-byte sequence number per batch header. Broker dedup is O(1) via producer state tracking. Allowing 5 in-flight requests safely increases pipeline depth, often improving throughput over the default 1.',
    tags: ['kafka', 'idempotent', 'producer', 'throughput', 'in-flight'],
  },
  {
    id: 'kafka-perf-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'OS Page Cache',
    question: 'What is "double buffering" in the context of Kafka and why is it problematic?',
    options: [
      'When a producer and consumer both cache the same message, consuming twice the memory',
      'When Kafka data is both in the OS page cache and copied into the JVM heap, wasting memory and potentially evicting useful page cache data',
      'When two log segment files are opened simultaneously during a segment roll',
      'When the producer batches records in both the accumulator and the in-flight queue simultaneously',
    ],
    correctAnswer: 1,
    explanation: 'If Kafka were to read disk data into JVM heap for processing, the same data would exist in both the page cache and the JVM heap ("double buffering"). Kafka avoids this via zero-copy, keeping data only in the page cache for consumer reads.',
    tags: ['kafka', 'double-buffering', 'page-cache', 'jvm', 'zero-copy'],
  },
  {
    id: 'kafka-perf-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Throughput vs Latency',
    question: 'What is the impact of increasing the number of partitions on end-to-end latency in a replication-heavy cluster?',
    options: [
      'More partitions always reduce latency because the load is spread across more leaders',
      'More partitions can increase end-to-end latency because the controller must manage more state, leader elections take longer, and `fetch.purgatory` queues grow',
      'Partition count has no effect on latency; latency depends only on `linger.ms`',
      'More partitions reduce latency only if `acks=0` is used',
    ],
    correctAnswer: 1,
    explanation: 'Each partition requires controller state and contributes to fetch purgatory queue length. At high partition counts (100k+), controller operations slow down, replication fetch delays grow, and end-to-end latency increases even at modest load.',
    tags: ['kafka', 'partitions', 'latency', 'controller', 'scaling'],
  },
  {
    id: 'kafka-perf-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Broker Threads',
    question: 'What is the "purgatory" mechanism in Kafka and how does it affect performance?',
    options: [
      'A background thread that purges expired log segments; misconfiguration causes disk space buildup',
      'A delayed operation queue where produce/fetch requests wait for conditions (e.g., acks from followers or min.bytes from producers); too many parked requests increase memory pressure',
      'The ZooKeeper session expiry handler that purges stale broker registrations',
      'A compaction purgatory that queues partitions awaiting the log cleaner',
    ],
    correctAnswer: 1,
    explanation: 'Kafka\'s `DelayedOperation` purgatory holds produce requests waiting for ISR acks and fetch requests waiting for `fetch.min.bytes`. High purgatory sizes indicate ack or data availability delays and increase broker heap pressure.',
    tags: ['kafka', 'purgatory', 'delayed-operation', 'broker', 'memory'],
  },
  {
    id: 'kafka-perf-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Compression',
    question: 'When does enabling `compression.type=producer` on a topic (broker-side setting) provide maximum performance benefit?',
    options: [
      'When all producers use the same compression codec, allowing the broker to store and forward batches without decompression',
      'When producers use no compression but the broker should apply gzip before storing to disk',
      'When the topic has a compaction policy and compression reduces the log cleaner\'s memory footprint',
      'It always degrades performance by forcing the broker to validate compression codecs',
    ],
    correctAnswer: 0,
    explanation: '`compression.type=producer` tells the broker to preserve whatever codec the producer used. This avoids broker-side recompression and is the recommended setting when producers consistently compress their data.',
    codeSnippet: `kafka-topics.sh --alter --topic events \\
  --config compression.type=producer`,
    tags: ['kafka', 'compression', 'broker', 'producer', 'performance'],
  },
  {
    id: 'kafka-perf-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Consumer Fetch',
    question: 'How does consumer fetch session caching (KIP-227, incremental fetch) reduce broker CPU usage?',
    options: [
      'It caches decompressed message payloads in consumer memory to avoid re-fetching',
      'It establishes a stateful fetch session on the broker that tracks which partitions have new data, allowing the broker to respond incrementally without scanning all partitions on every fetch',
      'It pre-fetches the next segment into the page cache before the consumer requests it',
      'It batches offset commit requests to reduce the number of write operations on `__consumer_offsets`',
    ],
    correctAnswer: 1,
    explanation: 'KIP-227 incremental fetch sessions maintain per-consumer partition state on the broker. Subsequent fetches only need to communicate partitions with changes, dramatically reducing CPU for high-partition-count consumers.',
    tags: ['kafka', 'incremental-fetch', 'kip-227', 'consumer', 'broker-cpu'],
  },
  {
    id: 'kafka-perf-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'OS Tuning',
    question: 'What is the benefit of using XFS over ext4 for Kafka log directories, and what mounting option improves write performance?',
    options: [
      'XFS has better small-file performance; mount with `sync` for guaranteed durability',
      'XFS handles Kafka\'s many small index files more efficiently; mounting with `noatime` eliminates atime update overhead on every read',
      'XFS natively supports Kafka\'s log segment rotation; ext4 requires manual inode allocation',
      'XFS enables O_DIRECT access automatically; ext4 requires explicit fsync calls',
    ],
    correctAnswer: 1,
    explanation: 'XFS performs better for Kafka\'s workload (many concurrent large sequential files). Mounting with `noatime` avoids writing atime metadata on every read, reducing unnecessary I/O on Kafka\'s heavily-read log files.',
    codeSnippet: `# /etc/fstab entry for Kafka data volume
/dev/sdb1  /kafka/data  xfs  defaults,noatime  0  2`,
    tags: ['kafka', 'filesystem', 'xfs', 'noatime', 'os-tuning'],
  },
  {
    id: 'kafka-perf-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Throughput vs Latency',
    question: 'What is "record accumulator" contention and how does it manifest in high-throughput Kafka producers?',
    options: [
      'Multiple producer threads compete for locks on the per-partition deque in the record accumulator, causing CPU spinning and reduced throughput',
      'The record accumulator runs out of buffer memory causing `BufferExhaustedException` storms under high load',
      'The record accumulator\'s timer thread and I/O thread compete for the same network connection pool',
      'Contention occurs when the serializer threads cannot keep up with record accumulation speed',
    ],
    correctAnswer: 0,
    explanation: 'The Kafka producer\'s RecordAccumulator maintains a deque per partition protected by a lock. Under high concurrency, many threads contending for the same partition\'s lock causes CPU overhead and throughput degradation. Using partition-aware routing or reducing partition contention helps.',
    tags: ['kafka', 'record-accumulator', 'contention', 'producer', 'concurrency'],
  },
  {
    id: 'kafka-perf-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Broker Threads',
    question: 'What is the `request.timeout.ms` interaction with `delivery.timeout.ms` and why does misconfiguration cause silent data loss?',
    options: [
      'They are independent; `request.timeout.ms` covers network-level timeouts, `delivery.timeout.ms` covers application-level callbacks',
      '`delivery.timeout.ms` is the outer bound for total delivery including retries; if `request.timeout.ms * (retries + 1)` exceeds `delivery.timeout.ms`, retries are cut short and the record is expired with failure, which applications may not handle',
      '`request.timeout.ms` must equal `delivery.timeout.ms` to avoid race conditions in the I/O thread',
      'Kafka automatically adjusts `delivery.timeout.ms` to always be at least `request.timeout.ms * retries`',
    ],
    correctAnswer: 1,
    explanation: '`delivery.timeout.ms` (default 120s) is the total time budget for a record including retries. If the application ignores the `Future` or callback result, expired records fail silently. Monitoring `record-error-rate` is essential to detect this.',
    tags: ['kafka', 'delivery-timeout', 'request-timeout', 'producer', 'data-loss'],
  },
  {
    id: 'kafka-perf-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Performance Tuning',
    subcategory: 'Zero-Copy',
    question: 'How does Kafka\'s tiered storage (KIP-405) affect zero-copy performance for consumers reading old data?',
    options: [
      'Tiered storage enhances zero-copy by pre-loading cold segments from object storage into the page cache',
      'Consumers reading from tiered (remote) storage cannot use zero-copy because data must be fetched from object storage through the broker\'s JVM heap before being forwarded',
      'Tiered storage adds a parallel zero-copy path using `splice()` for object store data',
      'Zero-copy is unaffected; tiered storage operates at a layer below the file system abstraction',
    ],
    correctAnswer: 1,
    explanation: 'When consumers read data that has been offloaded to object storage (S3, GCS), the broker fetches it through its JVM, bypassing the page cache and zero-copy path. This makes catch-up reads from tiered storage more CPU and memory intensive.',
    tags: ['kafka', 'tiered-storage', 'zero-copy', 'kip-405', 'performance'],
  },
];

export default questions;
