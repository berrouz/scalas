import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-producers-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'How does the idempotent producer detect and discard duplicate messages on the broker side?',
    options: [
      'By comparing message content hashes',
      'By checking the producer ID (PID) and per-partition sequence number against the last accepted sequence',
      'By storing message checksums in ZooKeeper',
      'By comparing timestamps and discarding messages within a deduplication window',
    ],
    correctAnswer: 1,
    explanation: 'Each idempotent producer has a unique PID; it assigns monotonically increasing sequence numbers per partition. The broker rejects any batch whose sequence is not exactly lastSeq+1.',
    tags: ['kafka', 'idempotent', 'deduplication'],
  },
  {
    id: 'kafka-producers-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'interceptors',
    question: 'In what order are producer interceptors, the serializer, and the partitioner called in the producer send pipeline?',
    options: [
      'Serializer → Partitioner → Interceptor',
      'Interceptor → Serializer → Partitioner',
      'Partitioner → Interceptor → Serializer',
      'Serializer → Interceptor → Partitioner',
    ],
    correctAnswer: 1,
    explanation: 'The interceptor\'s onSend() fires first on the raw object, then the key/value are serialized, then the partitioner determines the target partition.',
    codeSnippet: `// Pipeline order:\n// onSend(record) -> serialize key/value -> partition(record, ...) -> accumulate in batch`,
    tags: ['kafka', 'interceptors', 'send-pipeline'],
  },
  {
    id: 'kafka-producers-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'acks',
    question: 'A producer is configured with `acks=all` and `min.insync.replicas=2`, but only 1 replica is currently in ISR. What happens on send()?',
    options: [
      'The message is sent successfully since acks=all just means all current ISR members',
      'The producer retries indefinitely until a second ISR member comes back',
      'The broker returns NotEnoughReplicasException and the producer fails (or retries)',
      'The message is accepted with a warning logged on the broker',
    ],
    correctAnswer: 2,
    explanation: 'With min.insync.replicas=2 and only 1 ISR replica, the broker cannot satisfy the quorum requirement and returns LEADER_NOT_AVAILABLE or NOT_ENOUGH_REPLICAS error.',
    tags: ['kafka', 'acks', 'min-insync-replicas'],
  },
  {
    id: 'kafka-producers-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'custom partitioner',
    question: 'What method must a custom Kafka partitioner implement to receive topic metadata for dynamic routing?',
    options: [
      'initialize(Cluster cluster)',
      'onNewBatch(String topic, Cluster cluster, int prevPartition)',
      'configure(Map<String, ?> configs)',
      'Both configure() and partition(String topic, Object key, byte[] keyBytes, Object value, byte[] valueBytes, Cluster cluster)',
    ],
    correctAnswer: 3,
    explanation: 'The partition() method receives the full Cluster object with topic metadata (partition count, leaders, etc.), which is essential for dynamic routing decisions.',
    codeSnippet: `public int partition(String topic, Object key, byte[] keyBytes,\n    Object value, byte[] valueBytes, Cluster cluster) {\n  int numParts = cluster.partitionCountForTopic(topic);\n  return Math.abs(key.hashCode()) % numParts;\n}`,
    tags: ['kafka', 'custom-partitioner', 'cluster-metadata'],
  },
  {
    id: 'kafka-producers-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'retries',
    question: 'What is the difference between `retries` and `delivery.timeout.ms` in controlling retry behavior?',
    options: [
      'retries controls the number of attempts; delivery.timeout.ms has no effect if retries is set',
      'delivery.timeout.ms is the wall-clock bound on the entire send lifecycle; retries controls individual attempt count but is bounded by delivery.timeout.ms',
      'They are equivalent; delivery.timeout.ms = retries × request.timeout.ms',
      'retries applies only to network errors; delivery.timeout.ms applies only to broker errors',
    ],
    correctAnswer: 1,
    explanation: 'Both act as guards: delivery.timeout.ms caps total elapsed time including all retries; individual retries stop when either retries count or delivery.timeout.ms is exhausted.',
    tags: ['kafka', 'retries', 'delivery-timeout'],
  },
  {
    id: 'kafka-producers-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'batching',
    question: 'Why does increasing `linger.ms` generally improve throughput but increase latency?',
    options: [
      'Larger linger values enable stronger compression algorithms',
      'More messages accumulate per batch, amortizing per-request overhead and enabling better compression, but each message waits longer before being sent',
      'linger.ms increases the network socket buffer size, allowing more data per TCP packet',
      'Higher linger values trigger more aggressive garbage collection',
    ],
    correctAnswer: 1,
    explanation: 'Batching amortizes Kafka\'s per-request overhead (TCP, framing, I/O) across more records and yields better compression ratios, at the cost of additional queuing latency.',
    tags: ['kafka', 'linger-ms', 'throughput-latency'],
  },
  {
    id: 'kafka-producers-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'What happens to the producer ID (PID) when a Kafka producer is restarted?',
    options: [
      'The same PID is reused if the client.id is the same',
      'A new PID is assigned from the broker on each producer initialization',
      'The PID is stored in ZooKeeper and restored on restart',
      'The producer reuses the last PID stored in a local checkpoint file',
    ],
    correctAnswer: 1,
    explanation: 'A new PID is assigned on every producer initialization. This means that after a producer restart, the broker cannot correlate new messages with the previous session, so cross-session idempotence is NOT guaranteed — that requires transactional producers.',
    tags: ['kafka', 'idempotent', 'pid'],
  },
  {
    id: 'kafka-producers-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'max.in.flight.requests',
    question: 'With idempotence enabled, why is max.in.flight.requests.per.connection limited to 5?',
    options: [
      'The broker can only buffer 5 in-flight batches per producer connection',
      'The broker maintains a window of 5 sequence numbers per PID/partition to detect duplicates; exceeding this would break deduplication',
      'The Kafka protocol header can only encode up to 5 sequence IDs per request',
      'TCP flow control restricts simultaneous in-flight requests to 5',
    ],
    correctAnswer: 1,
    explanation: 'The broker tracks the last 5 sequence numbers per PID+partition. Allowing more than 5 in-flight requests could cause a valid retry to fall outside the deduplication window.',
    tags: ['kafka', 'max-in-flight', 'idempotent'],
  },
  {
    id: 'kafka-producers-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'serializers',
    question: 'What is a `Serde` in Kafka and how does it relate to serializers/deserializers?',
    options: [
      'A Serde is a combined serializer+deserializer interface used primarily by Kafka Streams',
      'A Serde is a schema-aware serializer that integrates with Schema Registry',
      'A Serde is a compressed serializer that automatically applies gzip',
      'A Serde is an asynchronous serializer interface',
    ],
    correctAnswer: 0,
    explanation: 'A Serde<T> bundles a Serializer<T> and Deserializer<T> together; Kafka Streams uses Serdes extensively for configuring key/value types of streams and state stores.',
    codeSnippet: `Serde<String> stringSerde = Serdes.String();\nSerde<Order> orderSerde = Serdes.serdeFrom(new OrderSerializer(), new OrderDeserializer());`,
    tags: ['kafka', 'serde', 'kafka-streams'],
  },
  {
    id: 'kafka-producers-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'acks',
    question: 'How does `request.timeout.ms` interact with `acks=all`?',
    options: [
      'It is the time the producer waits between retry attempts',
      'It is the maximum time the broker waits for ISR acknowledgments before returning a timeout error to the producer',
      'It limits how long the producer waits for ZooKeeper to confirm leader election',
      'It is the total wall-clock time for the entire send including retries',
    ],
    correctAnswer: 1,
    explanation: 'request.timeout.ms is the time the broker waits for ISR follower acknowledgments (when acks=all); if ISR replicas are slow, the request times out and the error is sent back to the producer.',
    tags: ['kafka', 'request-timeout', 'acks'],
  },
  {
    id: 'kafka-producers-l3-011',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'interceptors',
    question: 'Which use case is NOT appropriate for a producer interceptor?',
    options: [
      'Adding distributed tracing headers to every message',
      'Logging metrics about send success/failure rates',
      'Preventing messages from being sent to unauthorized topics',
      'Transforming message values after broker acknowledgment',
    ],
    correctAnswer: 3,
    explanation: 'Interceptors cannot transform data post-acknowledgment in a meaningful way for routing; the message has already been committed. Access control should use Kafka ACLs on the broker, not client-side interceptors.',
    tags: ['kafka', 'interceptors', 'use-cases'],
  },
  {
    id: 'kafka-producers-l3-012',
    language: 'kafka',
    level: 'level3',
    category: 'Producers',
    subcategory: 'buffer.memory',
    question: 'What happens if you set `buffer.memory` smaller than `batch.size`?',
    options: [
      'Kafka automatically adjusts batch.size to fit within buffer.memory',
      'An IllegalArgumentException is thrown at producer initialization',
      'The producer creates batches smaller than batch.size, reducing throughput',
      'Messages accumulate in heap memory outside the producer buffer',
    ],
    correctAnswer: 1,
    explanation: 'Kafka validates at startup that buffer.memory >= batch.size; if buffer.memory is too small to hold even one batch, an IllegalArgumentException is thrown.',
    tags: ['kafka', 'buffer-memory', 'batch-size'],
  },
];

export default questions;
