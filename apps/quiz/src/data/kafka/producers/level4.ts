import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-producers-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'What is the semantic difference between an idempotent producer and a transactional producer?',
    options: [
      'Idempotent producers support cross-topic atomicity; transactional producers do not',
      'Idempotent producers guarantee exactly-once within a single producer session per partition; transactional producers guarantee atomicity across multiple partitions and topics',
      'They are equivalent; transactional is just the Java API for idempotent',
      'Transactional producers offer higher throughput due to batching across topics',
    ],
    correctAnswer: 1,
    explanation: 'Idempotent deduplicates retries per partition/session. Transactions extend this to atomic multi-partition, multi-topic writes and atomic read-process-write (with consumers using isolation.level=read_committed).',
    codeSnippet: `producer.initTransactions();\nproducer.beginTransaction();\nproducer.send(new ProducerRecord<>("topic-a", key, val));\nproducer.send(new ProducerRecord<>("topic-b", key, val));\nproducer.commitTransaction();`,
    tags: ['kafka', 'transactions', 'idempotent'],
  },
  {
    id: 'kafka-producers-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'acks',
    question: 'With `acks=all`, `min.insync.replicas=2`, and `replication.factor=3`, what is the maximum number of broker failures the cluster can sustain while still accepting writes?',
    options: [
      '0 — any failure stops writes',
      '1 — one broker can fail',
      '2 — two brokers can fail',
      '3 — all three can fail and writes still succeed',
    ],
    correctAnswer: 1,
    explanation: 'With RF=3 and min.insync.replicas=2, one broker can fail (leaving 2 ISR) and writes continue. A second failure drops ISR below 2 and writes fail with NOT_ENOUGH_REPLICAS.',
    tags: ['kafka', 'acks', 'fault-tolerance'],
  },
  {
    id: 'kafka-producers-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'What is "ProducerFencedException" and when is it thrown?',
    options: [
      'When the producer\'s buffer memory is exceeded',
      'When a new transactional producer with the same transactional.id initializes, fencing out the previous instance',
      'When the producer attempts to write to a topic it does not have ACL permission for',
      'When the producer\'s PID is recycled by the broker after expiry',
    ],
    correctAnswer: 1,
    explanation: 'When initTransactions() is called with an existing transactional.id, the broker increments the producer epoch. The old producer receives ProducerFencedException on its next operation, preventing zombie writes.',
    codeSnippet: `// New instance fences the old one:\nprops.put("transactional.id", "order-processor-1");\nproducer.initTransactions(); // increments epoch, fences old producer`,
    tags: ['kafka', 'producer-fenced', 'transactions'],
  },
  {
    id: 'kafka-producers-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'max.in.flight.requests',
    question: 'What is the precise mechanism by which the broker detects out-of-order sequence numbers from an idempotent producer?',
    options: [
      'The broker timestamps all messages and reorders them before writing',
      'The broker checks if the incoming sequence equals lastSequence+1; if sequence > lastSequence+1 it returns an OutOfOrderSequenceException',
      'ZooKeeper tracks the global message order and rejects out-of-order writes',
      'The broker uses a Bloom filter to detect sequence gaps',
    ],
    correctAnswer: 1,
    explanation: 'The broker maintains lastAckedSequence per PID+partition. If received seq != lastAcked+1, an OutOfOrderSequenceException is returned, signaling a logic error that retries cannot fix.',
    tags: ['kafka', 'idempotent', 'sequence-numbers'],
  },
  {
    id: 'kafka-producers-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'interceptors',
    question: 'If a producer interceptor\'s `onSend()` throws an unchecked exception, what happens?',
    options: [
      'The exception propagates to the application and the message is not sent',
      'The exception is caught by Kafka, logged as a warning, and the message is sent without interceptor modifications',
      'All subsequent interceptors in the chain are skipped but the message is sent unmodified',
      'The producer enters an error state and must be closed and recreated',
    ],
    correctAnswer: 1,
    explanation: 'Kafka wraps interceptor invocations in a try-catch; exceptions are logged at WARN level and the original record is passed through unmodified, ensuring interceptor bugs do not block production.',
    tags: ['kafka', 'interceptors', 'error-handling'],
  },
  {
    id: 'kafka-producers-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'batching',
    question: 'How does Kafka\'s record accumulator manage memory for batches of different partitions?',
    options: [
      'A single shared pool allocates byte buffers; returning a batch releases its memory back to the pool',
      'Each partition gets a fixed dedicated memory region from JVM heap',
      'Batches are stored outside JVM heap using direct ByteBuffer allocations',
      'Memory is allocated per broker connection, not per partition',
    ],
    correctAnswer: 0,
    explanation: 'The RecordAccumulator uses a BufferPool (a free list of fixed-size ByteBuffers); finished batches return buffers to the pool for reuse, reducing GC pressure.',
    tags: ['kafka', 'record-accumulator', 'memory'],
  },
  {
    id: 'kafka-producers-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'custom partitioner',
    question: 'What is `onNewBatch()` callback in the Partitioner interface (added in Kafka 2.4) and when is it useful?',
    options: [
      'It is called after a batch is sent successfully, allowing the partitioner to update routing tables',
      'It is called when a new batch is started for a partition, useful for sticky partitioning implementations that want to change partition only on batch boundaries',
      'It is called when a new topic partition is added to the cluster',
      'It notifies the partitioner about changes in consumer group membership',
    ],
    correctAnswer: 1,
    explanation: 'onNewBatch() lets a partitioner switch the sticky partition exactly when a batch boundary occurs, enabling cleaner sticky behavior than time-based switching.',
    codeSnippet: `public void onNewBatch(String topic, Cluster cluster, int prevPartition) {\n  // switch sticky target partition here\n  stickyPartition.remove(topic);\n}`,
    tags: ['kafka', 'custom-partitioner', 'sticky'],
  },
  {
    id: 'kafka-producers-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'retries',
    question: 'What is "transaction timeout" (`transaction.timeout.ms`) and what happens when it expires?',
    options: [
      'It limits how long a producer can remain idle between beginTransaction() and commitTransaction(); expiry aborts the transaction',
      'It is the maximum time to wait for a broker to confirm a transaction commit',
      'It sets the retention time for messages in the __transaction_state topic',
      'It is the timeout for the producer to reconnect after a broker failure during a transaction',
    ],
    correctAnswer: 0,
    explanation: 'The transaction coordinator enforces transaction.timeout.ms; if the producer does not commit or abort within this window, the coordinator proactively aborts the transaction to unblock consumers.',
    codeSnippet: `props.put("transaction.timeout.ms", 60000); // abort if no commit within 60s`,
    tags: ['kafka', 'transactions', 'timeout'],
  },
  {
    id: 'kafka-producers-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'acks',
    question: 'Under what circumstances can a Kafka producer receive a successful acknowledgment (no exception) but still have the message effectively "lost" from consumer perspective?',
    options: [
      'This is impossible; a successful ack always means the message is durable',
      'When acks=1, the leader acks after local write but crashes before followers replicate; the new leader elected from ISR may not have the message',
      'When acks=all and all ISR replicas fail simultaneously after acknowledging',
      'When compression is enabled and the broker decompresses before writing',
    ],
    correctAnswer: 1,
    explanation: 'With acks=1, the leader acks after its local log write. If it crashes before any follower replicates, and a follower (with lower LEO) is elected, that message is effectively lost despite the producer receiving success.',
    tags: ['kafka', 'acks', 'data-loss'],
  },
  {
    id: 'kafka-producers-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'What happens if the broker\'s PID expiry (`transactional.id.expiration.ms`) removes a producer\'s state while the producer is still active?',
    options: [
      'The producer seamlessly re-registers with the same PID',
      'The broker reissues the same PID to a different producer, causing silent data corruption',
      'The broker assigns a new PID to the producer on its next request, breaking the idempotency guarantee for in-flight messages',
      'The producer is fenced out permanently and must be recreated',
    ],
    correctAnswer: 2,
    explanation: 'PID expiry (controlled by transactional.id.expiration.ms for transactional producers) can cause the broker to lose the sequence number window; a new PID assignment resets the counter, potentially allowing duplicate delivery.',
    tags: ['kafka', 'pid-expiry', 'idempotent'],
  },
  {
    id: 'kafka-producers-l4-011',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'serializers',
    question: 'What is "schema-on-write" vs "schema-on-read" in the context of Kafka serialization, and which does Avro with Schema Registry implement?',
    options: [
      'Schema Registry implements schema-on-read; Avro files store schema inline',
      'Avro with Schema Registry implements schema-on-write; producers must have a valid registered schema before writing',
      'Both approaches are equivalent when using Schema Registry',
      'Kafka implements schema-on-read by default; schema-on-write requires custom serializers',
    ],
    correctAnswer: 1,
    explanation: 'Schema-on-write enforces schema validity at produce time; Schema Registry rejects producers writing data that violates registered compatibility rules before data reaches the topic.',
    tags: ['kafka', 'schema-registry', 'avro'],
  },
  {
    id: 'kafka-producers-l4-012',
    language: 'kafka',
    level: 'level4',
    category: 'Producers',
    subcategory: 'batching',
    question: 'How does the Kafka producer handle a batch that is partially filled when `delivery.timeout.ms` expires?',
    options: [
      'The partial batch is discarded and all its records fail with TimeoutException',
      'The partial batch is sent immediately and all records in it are failed with TimeoutException',
      'Only records that have individually exceeded their deadline are failed; others continue waiting',
      'The batch is retried indefinitely until all records are acknowledged or individually expire',
    ],
    correctAnswer: 0,
    explanation: 'When delivery.timeout.ms expires for records in a batch, those records fail with TimeoutException and are removed from the accumulator — they will not be sent.',
    tags: ['kafka', 'delivery-timeout', 'batching'],
  },
];

export default questions;
