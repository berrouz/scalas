import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-producers-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'What does enabling `enable.idempotence=true` guarantee for the Kafka producer?',
    options: [
      'Messages are delivered in strict timestamp order',
      'Each message is written to the partition exactly once, even on retries',
      'Messages are encrypted end-to-end',
      'The producer can write to multiple partitions atomically',
    ],
    correctAnswer: 1,
    explanation: 'Idempotent producers use a producer ID (PID) and sequence numbers so brokers can detect and deduplicate retried messages, preventing duplicates.',
    codeSnippet: `props.put("enable.idempotence", true);\n// Automatically sets: acks=all, retries=MAX_INT, max.in.flight.requests.per.connection=5`,
    tags: ['kafka', 'idempotent', 'exactly-once'],
  },
  {
    id: 'kafka-producers-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'max.in.flight.requests',
    question: 'What risk does setting `max.in.flight.requests.per.connection > 1` introduce when retries are enabled without idempotence?',
    options: [
      'The producer may exhaust buffer memory faster',
      'Out-of-order message delivery within a partition due to retried batches',
      'The broker may reject messages with duplicate offsets',
      'Consumers will receive messages in reverse order',
    ],
    correctAnswer: 1,
    explanation: 'If batch N fails and is retried while batch N+1 already succeeded, N could be written after N+1, causing out-of-order delivery. Idempotence + max.in.flight=5 avoids this.',
    codeSnippet: `// Safe setting without idempotence if ordering matters:\nprops.put("max.in.flight.requests.per.connection", 1);`,
    tags: ['kafka', 'max-in-flight', 'ordering'],
  },
  {
    id: 'kafka-producers-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'custom partitioner',
    question: 'How do you implement a custom partitioner in Kafka?',
    options: [
      'By subclassing KafkaProducer and overriding the send() method',
      'By implementing the Partitioner interface and setting partitioner.class in producer config',
      'By writing a custom serializer that encodes the target partition in the message',
      'By configuring partition.strategy in server.properties',
    ],
    correctAnswer: 1,
    explanation: 'You implement the `Partitioner` interface (partition(), close(), configure() methods) and register it via `partitioner.class` producer property.',
    codeSnippet: `public class PriorityPartitioner implements Partitioner {\n  public int partition(String topic, Object key, ...) {\n    return "HIGH".equals(key) ? 0 : (numPartitions - 1);\n  }\n}`,
    tags: ['kafka', 'custom-partitioner', 'producer'],
  },
  {
    id: 'kafka-producers-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'interceptors',
    question: 'What is a Kafka producer interceptor used for?',
    options: [
      'Filtering out messages that fail schema validation before sending',
      'Intercepting and modifying or observing records before they are sent and after acknowledgment',
      'Encrypting message payloads transparently',
      'Re-routing messages to alternative topics when the primary topic is unavailable',
    ],
    correctAnswer: 1,
    explanation: 'Producer interceptors implement ProducerInterceptor to hook into onSend() (before serialization) and onAcknowledgement() (after ack), enabling tracing, auditing, or header injection.',
    codeSnippet: `props.put("interceptor.classes", "com.example.TracingInterceptor");`,
    tags: ['kafka', 'interceptors', 'producer'],
  },
  {
    id: 'kafka-producers-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'batching',
    question: 'What compression codec can be set on a Kafka producer and where does compression happen?',
    options: [
      'Compression is set on the broker; the producer sends uncompressed data',
      'The producer compresses the entire batch before sending; decompression happens on the consumer',
      'Compression is applied per-message by the serializer',
      'Only the broker can decompress; producers cannot compress',
    ],
    correctAnswer: 1,
    explanation: 'Setting `compression.type` (none/gzip/snappy/lz4/zstd) on the producer compresses the whole batch, reducing network and storage usage; consumers decompress transparently.',
    codeSnippet: `props.put("compression.type", "lz4");`,
    tags: ['kafka', 'compression', 'batching'],
  },
  {
    id: 'kafka-producers-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'retries',
    question: 'What is `delivery.timeout.ms` and how does it relate to retries?',
    options: [
      'It is the timeout for a single broker acknowledgment attempt',
      'It is the upper bound on the total time from send() to final acknowledgment, including retries',
      'It is the maximum time a message can remain in the producer buffer',
      'It controls the time between retry backoff intervals',
    ],
    correctAnswer: 1,
    explanation: 'delivery.timeout.ms is the total allowed time for send + retries + acknowledgment; it supersedes older retries+request.timeout.ms combinations as the primary timeout knob.',
    codeSnippet: `props.put("delivery.timeout.ms", 120000); // 2 minutes total`,
    tags: ['kafka', 'delivery-timeout', 'retries'],
  },
  {
    id: 'kafka-producers-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'key-based partitioning',
    question: 'Which hashing algorithm does the default Kafka partitioner use to assign a key to a partition?',
    options: [
      'SHA-256',
      'MD5',
      'Murmur2',
      'CRC32',
    ],
    correctAnswer: 2,
    explanation: 'The DefaultPartitioner uses a Murmur2 hash of the serialized key bytes modulo the number of partitions to determine the target partition.',
    tags: ['kafka', 'partitioning', 'murmur2'],
  },
  {
    id: 'kafka-producers-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'acks',
    question: 'What exception is thrown when a producer write is rejected because fewer than `min.insync.replicas` are available?',
    options: [
      'ProducerFencedException',
      'NotEnoughReplicasException',
      'LeaderNotAvailableException',
      'TopicAuthorizationException',
    ],
    correctAnswer: 1,
    explanation: 'When acks=all and the ISR is smaller than min.insync.replicas, the broker returns a NotEnoughReplicasException to the producer.',
    tags: ['kafka', 'min-insync-replicas', 'exceptions'],
  },
  {
    id: 'kafka-producers-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'buffer.memory',
    question: 'What happens when the producer\'s `buffer.memory` is exhausted?',
    options: [
      'Messages are dropped silently',
      'The producer immediately throws a BufferExhaustedException',
      'The producer blocks for up to `max.block.ms` then throws a TimeoutException',
      'The producer switches to synchronous sending automatically',
    ],
    correctAnswer: 2,
    explanation: 'When the buffer is full, the producer blocks the calling thread for max.block.ms; if space does not free up by then, a TimeoutException is thrown.',
    tags: ['kafka', 'buffer-memory', 'backpressure'],
  },
  {
    id: 'kafka-producers-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'idempotent producer',
    question: 'What configuration constraints are automatically enforced when `enable.idempotence=true`?',
    options: [
      'acks=1, retries=0, max.in.flight.requests=1',
      'acks=all, retries=Integer.MAX_VALUE, max.in.flight.requests.per.connection<=5',
      'acks=all, retries=3, max.in.flight.requests=1',
      'acks=0, retries=Integer.MAX_VALUE, linger.ms=0',
    ],
    correctAnswer: 1,
    explanation: 'Idempotent mode requires acks=all for durability and allows up to 5 in-flight requests per connection (Kafka guarantees ordering up to 5 with idempotence).',
    tags: ['kafka', 'idempotent', 'configuration'],
  },
  {
    id: 'kafka-producers-l2-011',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'serializers',
    question: 'What must a custom Kafka serializer implement?',
    options: [
      'java.io.Serializable',
      'org.apache.kafka.common.serialization.Serializer<T>',
      'org.apache.kafka.common.serialization.Serde<T>',
      'com.fasterxml.jackson.databind.JsonSerializer<T>',
    ],
    correctAnswer: 1,
    explanation: 'Custom serializers implement Kafka\'s Serializer<T> interface, providing serialize(String topic, T data) and optionally configure() and close() methods.',
    codeSnippet: `public class OrderSerializer implements Serializer<Order> {\n  public byte[] serialize(String topic, Order data) { ... }\n}`,
    tags: ['kafka', 'custom-serializer', 'producer'],
  },
  {
    id: 'kafka-producers-l2-012',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'retries',
    question: 'Which errors are considered "retriable" by the Kafka producer?',
    options: [
      'All errors are retriable',
      'Only authentication errors are retriable',
      'Transient errors like leader election in progress or network timeouts',
      'Only serialization errors are retriable',
    ],
    correctAnswer: 2,
    explanation: 'Retriable errors (e.g., LEADER_NOT_AVAILABLE, REQUEST_TIMED_OUT) are temporary; non-retriable errors (e.g., INVALID_TOPIC, MESSAGE_TOO_LARGE) require application-level handling.',
    tags: ['kafka', 'retriable-errors', 'producer'],
  },
  {
    id: 'kafka-producers-l2-013',
    language: 'kafka',
    level: 'level2',
    category: 'Producers',
    subcategory: 'batching',
    question: 'What is the "sticky partitioner" introduced in Kafka 2.4?',
    options: [
      'A partitioner that always sends to the same partition until it is full',
      'A partitioner that accumulates records for the same target partition into a single batch before switching to another partition, reducing small batches',
      'A partitioner that "sticks" to the partition with the lowest consumer lag',
      'A partitioner that routes messages based on sticky sessions in HTTP load balancers',
    ],
    correctAnswer: 1,
    explanation: 'The StickyPartitioner fills a batch for one partition before switching to the next, reducing the overhead of many tiny batches that the pure round-robin approach created.',
    tags: ['kafka', 'sticky-partitioner', 'batching'],
  },
];

export default questions;
