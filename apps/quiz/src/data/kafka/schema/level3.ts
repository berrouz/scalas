import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-schema-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'What are "schema references" in Confluent Schema Registry and what problem do they solve?',
    options: [
      'References link a schema to its Git repository for version control',
      'Schema references allow one schema to embed and reuse another registered schema by reference (like imports), enabling shared types without duplication',
      'References are used to link Avro schemas to their corresponding Protobuf equivalent',
      'They reference the previous schema version for migration purposes',
    ],
    correctAnswer: 1,
    explanation: 'Schema references allow schemas to reference other registered schemas (e.g., a shared Address type), promoting DRY principles and enabling independent evolution of shared types.',
    codeSnippet: `# Registering a schema with a reference\ncurl -X POST http://schema-registry:8081/subjects/order-value/versions \\\n  -d '{"schema": "...", "references": [{"name": "address.avsc", "subject": "address", "version": 1}]}'`,
    tags: ['kafka', 'schema-references', 'schema-registry'],
  },
  {
    id: 'kafka-schema-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'Avro',
    question: 'What is "schema fingerprint" in Avro and how does Schema Registry use it?',
    options: [
      'A fingerprint is a digital signature authenticating the schema author',
      'A hash (e.g., MD5, SHA-256) of the canonical form of the schema used to detect duplicate schemas; Schema Registry uses fingerprints to avoid registering identical schemas under new IDs',
      'A compact binary representation of the Avro schema for network transmission',
      'A checksum appended to each Avro record for data integrity verification',
    ],
    correctAnswer: 1,
    explanation: 'Schema Registry hashes schemas to detect duplicates; if the same schema is submitted again, the existing ID is returned rather than creating a new version, preventing schema ID proliferation.',
    tags: ['kafka', 'avro', 'schema-fingerprint'],
  },
  {
    id: 'kafka-schema-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'What does `use.latest.version=true` do in the Avro serializer and what risk does it introduce?',
    options: [
      'It always fetches the schema from Schema Registry instead of the cache',
      'The serializer uses the latest registered schema version instead of the one passed to the send() call; risk: if a new incompatible schema is registered, existing producers silently start using it',
      'It auto-upgrades the schema to the latest version during deserialization',
      'It disables schema caching and always validates against the registry',
    ],
    correctAnswer: 1,
    explanation: 'use.latest.version can cause silent breaking changes if a newer, incompatible schema is registered — the producer starts serializing with the new schema without any code change.',
    tags: ['kafka', 'use-latest-version', 'serializer'],
  },
  {
    id: 'kafka-schema-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'Protobuf',
    question: 'What are oneof fields in Protobuf and how do they map to Avro unions conceptually?',
    options: [
      'oneof defines a field that can hold exactly one of several specified types at a time; semantically similar to Avro unions but with type-specific field names',
      'oneof is Protobuf syntax for required fields; Avro has no equivalent',
      'oneof is used for repeated (array) fields with multiple types',
      'oneof has no Avro equivalent; it is unique to Protobuf',
    ],
    correctAnswer: 0,
    explanation: 'Both oneof and Avro unions represent mutually exclusive alternatives, but oneof uses named sub-fields while Avro unions use type-in-array notation with different resolution semantics.',
    codeSnippet: `message Payment {\n  oneof payment_type {\n    CreditCard credit_card = 1;\n    BankTransfer bank_transfer = 2;\n  }\n}`,
    tags: ['kafka', 'protobuf', 'oneof'],
  },
  {
    id: 'kafka-schema-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'How does Schema Registry enforce compatibility when a new schema version is registered via the REST API?',
    options: [
      'It runs compatibility checks asynchronously and alerts via email if incompatible',
      'Before persisting the new version, the registry retrieves the relevant previous version(s), runs the compatibility algorithm for the configured mode, and returns a 409 Conflict error if the check fails',
      'Compatibility is checked only when a consumer first reads with the new schema',
      'The registry checks compatibility only if explicitly requested via a separate validate endpoint',
    ],
    correctAnswer: 1,
    explanation: 'Schema compatibility is enforced synchronously at registration time; a 409 HTTP response is returned for incompatible schemas, blocking the registration.',
    codeSnippet: `# Check compatibility before registering\ncurl -X POST http://schema-registry:8081/compatibility/subjects/orders-value/versions/latest \\\n  -d '{"schema": "<new-schema-json>"}'`,
    tags: ['kafka', 'compatibility-enforcement', 'schema-registry'],
  },
  {
    id: 'kafka-schema-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'subject naming strategy',
    question: 'What is the challenge of using `RecordNameStrategy` with Schema Registry compatibility checks across topics?',
    options: [
      'RecordNameStrategy does not support Protobuf; only Avro is supported',
      'Since subjects are per record type (not per topic), a schema change compatible in one use case may break another topic\'s consumers; all consumers of that record type must be able to handle the new schema',
      'RecordNameStrategy requires every record to have a unique namespace',
      'Compatibility checks are disabled for RecordNameStrategy by default',
    ],
    correctAnswer: 1,
    explanation: 'With RecordNameStrategy, one subject covers all topics using a record type. Schema evolution must be compatible for every consumer of that record across all topics, increasing coordination complexity.',
    tags: ['kafka', 'record-name-strategy', 'compatibility'],
  },
  {
    id: 'kafka-schema-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'What is "schema normalization" in Schema Registry and why does it matter?',
    options: [
      'Converting all schemas to normal form (3NF) for relational data modeling',
      'Canonicalizing schema representation (removing whitespace, ordering fields) before fingerprinting, so semantically equivalent schemas with different formatting are treated as identical',
      'Converting Avro schemas to Protobuf format for cross-format compatibility',
      'Normalizing field names to snake_case for consistency across teams',
    ],
    correctAnswer: 1,
    explanation: 'Without normalization, the same logical schema with different whitespace or field ordering would generate different fingerprints and be registered as distinct versions. Normalization prevents this.',
    tags: ['kafka', 'schema-normalization', 'fingerprint'],
  },
  {
    id: 'kafka-schema-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'Avro',
    question: 'How does Avro schema resolution handle type promotion (e.g., int to long)?',
    options: [
      'Type promotion is not supported; mismatched types always cause an error',
      'Avro allows specific type promotions during schema resolution: int can be promoted to long, float, or double; long can be promoted to float or double',
      'All numeric types are interchangeable in Avro',
      'Type promotion only works for fields with explicit "promote": true annotation',
    ],
    correctAnswer: 1,
    explanation: 'Avro schema resolution defines a promotion table for numeric widening; int→long/float/double, long→float/double, float→double. This allows safe field type evolution within these boundaries.',
    codeSnippet: `// Writer schema: {"name": "count", "type": "int"}\n// Reader schema: {"name": "count", "type": "long"}  // valid promotion`,
    tags: ['kafka', 'avro', 'type-promotion'],
  },
  {
    id: 'kafka-schema-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'What is `schema.registry.basic.auth.credentials.source` used for?',
    options: [
      'Configuring the local schema cache size',
      'Specifying how the serializer/deserializer authenticates to a secured Schema Registry (e.g., USER_INFO for username:password, URL for credentials in the URL)',
      'Enabling TLS certificate-based authentication to the registry',
      'Setting the source topic for schema bootstrap',
    ],
    correctAnswer: 1,
    explanation: 'When Schema Registry requires authentication, this property tells the client where to find credentials: USER_INFO (separate user/password config), URL (embedded in the URL), or SASL_INHERIT.',
    codeSnippet: `props.put("schema.registry.basic.auth.credentials.source", "USER_INFO");\nprops.put("schema.registry.basic.auth.user.info", "user:password");`,
    tags: ['kafka', 'schema-registry', 'authentication'],
  },
  {
    id: 'kafka-schema-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Schema Registry',
    subcategory: 'JSON Schema',
    question: 'What is the "$id" field in JSON Schema and how does it interact with Schema Registry subject management?',
    options: [
      '$id is the Schema Registry ID assigned automatically; it cannot be set manually',
      '$id is a URI that identifies the schema document; Schema Registry uses it for schema references resolution but it does not replace the registry-assigned integer schema ID',
      '$id is used as the Kafka record key for JSON Schema messages',
      '$id specifies the version of the JSON Schema specification being used',
    ],
    correctAnswer: 1,
    explanation: 'The $id in JSON Schema is a logical identifier for the schema document used for reference resolution within Schema Registry\'s JSON Schema reference feature; the registry still assigns its own integer ID.',
    tags: ['kafka', 'json-schema', 'schema-id'],
  },
];

export default questions;
