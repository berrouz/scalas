import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-schema-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'How do you safely migrate a topic from one schema type to another (e.g., Avro to Protobuf) without downtime?',
    options: [
      'Delete the subject from Schema Registry and re-register with the new type; existing consumers will auto-detect the change',
      'Use RecordNameStrategy to register both schemas under separate subjects, dual-publish in both formats during a migration window, upgrade consumers to handle both, then cut over producers and retire the old format',
      'Simply register the Protobuf schema under the same subject; Schema Registry handles cross-format compatibility',
      'Schema types cannot be changed; you must create a new topic',
    ],
    correctAnswer: 1,
    explanation: 'Cross-format migration requires a careful dual-write strategy: consumers must handle both formats simultaneously during the transition period to avoid data loss or downtime.',
    tags: ['kafka', 'schema-migration', 'avro-protobuf'],
  },
  {
    id: 'kafka-schema-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'What is the "schema mode" in Schema Registry (READWRITE, READONLY, IMPORT) and when is IMPORT used?',
    options: [
      'These modes control which clients can register schemas',
      'READWRITE is normal operation; READONLY prevents new registrations; IMPORT allows registering schemas with specific IDs (for migrating schemas between registries without ID conflicts)',
      'IMPORT mode is used to load schemas from Avro IDL files automatically',
      'READONLY mode is activated during broker maintenance windows',
    ],
    correctAnswer: 1,
    explanation: 'IMPORT mode is critical for disaster recovery or registry migration: it allows you to restore schemas with their original IDs, ensuring serialized wire format schema IDs remain valid.',
    codeSnippet: `# Set registry to IMPORT mode\ncurl -X PUT http://schema-registry:8081/mode \\\n  -d '{"mode": "IMPORT"}'`,
    tags: ['kafka', 'schema-registry-mode', 'import'],
  },
  {
    id: 'kafka-schema-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'Avro',
    question: 'What is the Avro "default" value semantics during schema resolution and what is the "first type in union" rule?',
    options: [
      'The default value is used when a field is missing during read; it must be a valid instance of the field\'s type, and for unions, it must be a valid instance of the FIRST type in the union array',
      'Default values are only used when writing new records; they have no effect during reading',
      'The default can be any valid JSON value regardless of the union order',
      'Defaults are not supported for union types in Avro',
    ],
    correctAnswer: 0,
    explanation: 'This is a common Avro pitfall: ["null","string"] with "default":null works, but ["string","null"] with "default":null fails because the default must match the first type (string, not null).',
    codeSnippet: `// CORRECT: null first, default null\n{"type": ["null", "string"], "default": null}\n\n// INCORRECT: string first, default null (schema parse error)\n{"type": ["string", "null"], "default": null}`,
    tags: ['kafka', 'avro', 'union-default'],
  },
  {
    id: 'kafka-schema-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'What is "schema caching" in the Kafka Avro serializer/deserializer and what is the trade-off of `max.schemas.per.subject`?',
    options: [
      'The cache stores compiled Avro parsers; larger cache speeds up serialization but increases GC pressure',
      'The client caches schema ID ↔ schema mappings locally; larger cache reduces Registry HTTP round trips but increases heap usage; on cache miss, it fetches from Registry',
      'The cache is stored on disk in the client\'s working directory',
      'Caching is disabled by default to ensure schemas are always current',
    ],
    correctAnswer: 1,
    explanation: 'The in-memory schema cache prevents repeated Registry HTTP calls. max.schemas.per.subject limits cached versions per subject. In high-churn environments (many schema versions), tuning this prevents excessive heap use.',
    codeSnippet: `props.put("max.schemas.per.subject", 1000); // cache up to 1000 schema versions`,
    tags: ['kafka', 'schema-cache', 'serializer'],
  },
  {
    id: 'kafka-schema-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'Protobuf',
    question: 'What is the significance of field number reuse in Protobuf schema evolution and how should it be handled?',
    options: [
      'Field numbers can be freely reused once a field is removed; there are no issues',
      'Reusing a removed field\'s number causes old messages to be misinterpreted: the old binary data for the deleted field would be decoded as the new field, causing data corruption',
      'Protobuf automatically invalidates reused field numbers via schema validation',
      'Field number reuse is only safe for fields of the same type',
    ],
    correctAnswer: 1,
    explanation: 'Reserved field numbers (reserved 1;) prevent accidental reuse. Old binary data still contains the old field\'s tag number; if that number is reused with a different meaning, deserialization of historical data will be corrupted.',
    codeSnippet: `message Order {\n  reserved 3; // prevent reuse of deleted "discount" field\n  reserved "discount";\n  string id = 1;\n  double amount = 2;\n}`,
    tags: ['kafka', 'protobuf', 'field-number'],
  },
  {
    id: 'kafka-schema-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'subject naming strategy',
    question: 'What is a "context" in Confluent Schema Registry 7.x and how does it differ from a subject naming strategy?',
    options: [
      'Contexts are logical namespaces that partition schemas into isolated groups (like separate registries within one instance), each with independent subjects and compatibility settings',
      'A context is the namespace prefix used by RecordNameStrategy for subject naming',
      'Contexts are per-environment configurations (dev/prod) for the same Schema Registry instance',
      'A context is a deprecated feature replaced by subject naming strategies in newer versions',
    ],
    correctAnswer: 0,
    explanation: 'Contexts (prefixed with :.context.) allow true multi-tenancy in a single Schema Registry, with isolated subject namespaces, independent configurations, and separate compatibility settings per tenant.',
    codeSnippet: `# Register in a specific context\ncurl -X POST http://schema-registry:8081/subjects/:.ctx.team-a:orders-value/versions \\\n  -d '{"schema": "..."}'`,
    tags: ['kafka', 'schema-registry-context', 'multi-tenancy'],
  },
  {
    id: 'kafka-schema-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'What is the "data contract" pattern using Schema Registry, and what governance capabilities does it enable?',
    options: [
      'A data contract is a legal agreement between teams about who owns a Kafka topic',
      'A data contract defines agreed-upon schemas, compatibility rules, metadata (owner, SLA, tags), and optionally data quality rules, enforced via Schema Registry to provide self-serve governance',
      'A data contract is a Kafka ACL that restricts which producers can write to a topic',
      'A data contract is the JSON configuration file that defines a Kafka Connect connector',
    ],
    correctAnswer: 1,
    explanation: 'Confluent\'s Data Contracts (Schema Registry + metadata rules) allow teams to attach business metadata, data quality rules, and migration rules to schemas, enabling automated governance beyond just compatibility checking.',
    tags: ['kafka', 'data-contracts', 'governance'],
  },
  {
    id: 'kafka-schema-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'Why can FULL_TRANSITIVE compatibility be too restrictive in practice, and what alternative strategy helps?',
    options: [
      'FULL_TRANSITIVE is rarely too restrictive; it is the recommended mode for all production topics',
      'FULL_TRANSITIVE requires every schema version to be compatible with every other version ever registered, making it impossible to ever remove any field; using FULL (non-transitive) or strategically archiving old schema versions enables gradual field removal',
      'FULL_TRANSITIVE breaks when using Protobuf; it only works with Avro',
      'FULL_TRANSITIVE prevents adding new required fields, which is needed for schema migrations',
    ],
    correctAnswer: 1,
    explanation: 'FULL_TRANSITIVE accumulates compatibility constraints over time. Strategies include: using FULL (check only against latest), retiring old schemas via soft delete, or batching breaking changes behind a version increment.',
    codeSnippet: `# Soft delete a schema version to remove from transitive chain\ncurl -X DELETE \\\n  "http://schema-registry:8081/subjects/orders-value/versions/2?permanent=false"`,
    tags: ['kafka', 'full-transitive', 'schema-governance'],
  },
  {
    id: 'kafka-schema-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'Avro',
    question: 'How does Avro handle schema resolution when the writer\'s schema has a field that is absent in the reader\'s schema?',
    options: [
      'An exception is thrown; all writer fields must appear in the reader schema',
      'The extra writer field is silently ignored during deserialization; the reader only populates its own fields',
      'The extra field is stored in a generic "extras" map field if present in the reader schema',
      'The reader schema is automatically updated with the extra field from the writer',
    ],
    correctAnswer: 1,
    explanation: 'Avro schema resolution skips fields in the writer\'s schema that don\'t appear in the reader\'s schema. This is the basis for forward compatibility: old readers ignore new fields added by the writer.',
    tags: ['kafka', 'avro', 'schema-resolution'],
  },
  {
    id: 'kafka-schema-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'What is "schema auto-evolution" using `latest.compatibility.strict=false` and what risk does it introduce?',
    options: [
      'It allows producers to use any schema version, not just the latest',
      'It allows the deserializer to fall back to the latest registered schema when the schema ID in the message cannot be found in the registry, potentially misinterpreting the data',
      'It auto-applies schema migrations on the consumer side when receiving data with an older schema',
      'It enables automatic schema registration for any new message type encountered',
    ],
    correctAnswer: 1,
    explanation: 'Allowing fallback to latest schema when ID is missing can cause data corruption if the latest schema is incompatible with the actual data format. The missing schema ID likely indicates a registry availability or migration issue that should be explicitly addressed.',
    tags: ['kafka', 'schema-strict', 'deserialization-risk'],
  },
];

export default questions;
