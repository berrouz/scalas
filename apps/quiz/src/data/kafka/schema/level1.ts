import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-schema-l1-001',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'Avro',
    question: 'What is Apache Avro and why is it commonly used with Kafka?',
    options: [
      'A query language for Kafka topics, similar to SQL',
      'A binary serialization format with rich data types and schema evolution support, offering compact payloads and fast serialization',
      'A monitoring agent for Kafka brokers',
      'A compression algorithm optimized for time-series data',
    ],
    correctAnswer: 1,
    explanation: 'Avro provides compact binary encoding, supports complex types, and allows schema evolution with backward/forward compatibility — making it ideal for Kafka\'s high-throughput, schema-evolving data pipelines.',
    tags: ['kafka', 'avro', 'serialization'],
  },
  {
    id: 'kafka-schema-l1-002',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'What is the Schema Registry in the Kafka ecosystem?',
    options: [
      'A Kafka broker plugin that validates message schemas before writing to topics',
      'A standalone service that stores and retrieves Avro/Protobuf/JSON schemas, assigns schema IDs, and enforces compatibility rules',
      'A ZooKeeper node that stores schema definitions',
      'A Kafka topic that stores schema definitions for internal use',
    ],
    correctAnswer: 1,
    explanation: 'Schema Registry (commonly Confluent\'s) provides a REST API for schema management, enforces compatibility checks on schema evolution, and allows serializers/deserializers to reference schemas by ID.',
    tags: ['kafka', 'schema-registry', 'basics'],
  },
  {
    id: 'kafka-schema-l1-003',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'How does the Confluent Avro serializer embed schema information in a Kafka message?',
    options: [
      'The full Avro schema is embedded in each message as a JSON header',
      'A magic byte (0x00) followed by the 4-byte schema ID is prepended to the binary Avro payload',
      'The schema is stored as a separate Kafka record preceding the data record',
      'The schema is stored in ZooKeeper and looked up by message timestamp',
    ],
    correctAnswer: 1,
    explanation: 'The Confluent wire format is: 0x00 (magic byte) + 4-byte schema ID + Avro binary data. Consumers use the schema ID to fetch the correct schema from Schema Registry for deserialization.',
    codeSnippet: `// Wire format: [0x00][schema-id 4 bytes][avro payload]\n// Example: 0x00 0x00 0x00 0x00 0x03 <avro bytes>`,
    tags: ['kafka', 'avro', 'wire-format'],
  },
  {
    id: 'kafka-schema-l1-004',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'What does "backward compatibility" mean in Schema Registry?',
    options: [
      'New schemas can read data written with older schemas',
      'Older schemas can read data written with newer schemas',
      'Old and new schemas are fully interchangeable',
      'The schema can be reverted to a previous version without data loss',
    ],
    correctAnswer: 0,
    explanation: 'Backward compatibility means a newer schema version can read data serialized with an older version — allowing consumers to be upgraded before producers without breaking the pipeline.',
    tags: ['kafka', 'backward-compatibility', 'schema-evolution'],
  },
  {
    id: 'kafka-schema-l1-005',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'Protobuf',
    question: 'What is Protocol Buffers (Protobuf) and how does it differ from Avro for Kafka use?',
    options: [
      'Protobuf is a Kafka-specific format; Avro is a general-purpose format',
      'Protobuf uses field numbers for encoding (enabling forward/backward compatibility without schema files at deserialization); Avro requires the schema for decoding',
      'Protobuf is a JSON-based format; Avro is binary only',
      'They are functionally identical; the choice is purely preferential',
    ],
    correctAnswer: 1,
    explanation: 'Protobuf encodes fields by number, so readers can skip unknown fields; Avro uses positional encoding and requires the writer\'s schema to decode. Both are supported by Schema Registry.',
    codeSnippet: `syntax = "proto3";\nmessage Order {\n  string id = 1;\n  double amount = 2;\n  string customer_id = 3;\n}`,
    tags: ['kafka', 'protobuf', 'avro'],
  },
  {
    id: 'kafka-schema-l1-006',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'subject naming strategy',
    question: 'What is the default subject naming strategy in Schema Registry?',
    options: [
      'SchemaIdNamingStrategy — subjects are named by schema ID',
      'TopicNameStrategy — subject is named <topic-name>-key or <topic-name>-value',
      'RecordNameStrategy — subject is named by the fully qualified record name',
      'TopicRecordNameStrategy — subject combines topic and record name',
    ],
    correctAnswer: 1,
    explanation: 'TopicNameStrategy creates one subject per topic per key/value (e.g., orders-value), meaning all records in a topic must use the same schema — the most common default.',
    tags: ['kafka', 'subject-naming-strategy', 'schema-registry'],
  },
  {
    id: 'kafka-schema-l1-007',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'JSON Schema',
    question: 'What is JSON Schema support in Schema Registry?',
    options: [
      'Schema Registry stores JSON schemas to validate JSON messages against, providing compatibility checking for JSON-formatted Kafka records',
      'JSON Schema converts Avro schemas to JSON format for readability',
      'JSON Schema is only used for Kafka Connect converter configuration',
      'It is a legacy format replaced by Avro in modern deployments',
    ],
    correctAnswer: 0,
    explanation: 'Schema Registry supports JSON Schema (in addition to Avro and Protobuf), enabling compatibility enforcement and schema evolution for applications that use JSON serialization.',
    tags: ['kafka', 'json-schema', 'schema-registry'],
  },
  {
    id: 'kafka-schema-l1-008',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'What does "forward compatibility" mean in Schema Registry?',
    options: [
      'New schemas can read data written with older schemas',
      'Older schemas can read data written with newer schemas',
      'Both old and new schema versions can read each other\'s data',
      'The schema can be evolved forward by adding required fields',
    ],
    correctAnswer: 1,
    explanation: 'Forward compatibility means that data written with a newer schema can be read by consumers using an older schema — allowing producers to be upgraded before consumers.',
    tags: ['kafka', 'forward-compatibility', 'schema-evolution'],
  },
  {
    id: 'kafka-schema-l1-009',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'What configuration is required in a Kafka producer to use the Avro serializer with Schema Registry?',
    options: [
      'Only avro.schema.path pointing to a local schema file',
      'The KafkaAvroSerializer class and the schema.registry.url property',
      'The AvroConverter class and the connect.schema.registry.url property',
      'Only the enable.auto.schema setting on the producer',
    ],
    correctAnswer: 1,
    explanation: 'The producer needs value.serializer=KafkaAvroSerializer and schema.registry.url pointing to the registry; the serializer automatically registers and fetches schemas.',
    codeSnippet: `props.put("value.serializer", "io.confluent.kafka.serializers.KafkaAvroSerializer");\nprops.put("schema.registry.url", "http://schema-registry:8081");`,
    tags: ['kafka', 'avro-serializer', 'schema-registry'],
  },
  {
    id: 'kafka-schema-l1-010',
    language: 'kafka',
    level: 'level1',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'What is the "FULL" compatibility mode in Schema Registry?',
    options: [
      'All existing data can be re-read with the new schema',
      'Both backward and forward compatibility — new schema can read old data AND old schema can read new data',
      'The schema supports all Avro data types without restrictions',
      'The schema is stored in both Avro and JSON formats',
    ],
    correctAnswer: 1,
    explanation: 'FULL compatibility is the intersection of backward and forward — the safest mode, but most restrictive: only additions with defaults and deletions of optional fields are allowed.',
    tags: ['kafka', 'full-compatibility', 'schema-registry'],
  },
];

export default questions;
