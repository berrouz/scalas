import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-schema-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'In Avro, what makes adding a new field backward compatible?',
    options: [
      'The field must have a required type with no default',
      'The field must have a default value, so older readers that don\'t have the field can use the default',
      'The field must be placed at the beginning of the schema',
      'New fields are always backward compatible regardless of defaults',
    ],
    correctAnswer: 1,
    explanation: 'For backward compatibility, new fields must have defaults so that a newer schema can deserialize data from older writers that didn\'t include the field.',
    codeSnippet: `{\n  "name": "email",\n  "type": ["null", "string"],\n  "default": null  // required for backward compatibility\n}`,
    tags: ['kafka', 'avro', 'backward-compatibility'],
  },
  {
    id: 'kafka-schema-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'subject naming strategy',
    question: 'When would you choose `RecordNameStrategy` over the default `TopicNameStrategy`?',
    options: [
      'When you want to enforce that all records in a topic use the same schema',
      'When multiple different record types are published to the same topic (e.g., event sourcing) and each type should have its own schema subject',
      'When using Avro with Schema Registry and Kafka Streams simultaneously',
      'When you need schemas to be shared across topics of the same name in different environments',
    ],
    correctAnswer: 1,
    explanation: 'RecordNameStrategy subjects schemas by fully qualified Avro record name rather than topic, allowing a single topic to carry heterogeneous record types each with independent schema evolution.',
    codeSnippet: `props.put("value.subject.name.strategy",\n  "io.confluent.kafka.serializers.subject.RecordNameStrategy");`,
    tags: ['kafka', 'record-name-strategy', 'subject'],
  },
  {
    id: 'kafka-schema-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'What is "NONE" compatibility mode in Schema Registry and when might you use it?',
    options: [
      'It means no schemas are accepted — the registry is effectively disabled',
      'No compatibility check is performed; any schema can be registered regardless of changes, useful for development or when you fully control all producers and consumers',
      'Only schemas with no required fields are accepted',
      'It means schemas are compared but errors are only logged, not enforced',
    ],
    correctAnswer: 1,
    explanation: 'NONE disables compatibility enforcement, allowing arbitrary schema changes. This is useful in development but dangerous in production because it can break existing consumers.',
    tags: ['kafka', 'none-compatibility', 'schema-registry'],
  },
  {
    id: 'kafka-schema-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'Avro',
    question: 'What is an Avro "union" type and how is it commonly used for nullable fields?',
    options: [
      'A union merges two Avro schemas into one combined schema',
      'A union allows a field to hold a value of one of several types; ["null", "string"] is the idiomatic pattern for nullable fields with null as the default',
      'A union is used to join two Kafka topics in Avro format',
      'A union is an Avro type that represents a set of key-value pairs',
    ],
    correctAnswer: 1,
    explanation: 'Avro unions are arrays of type names; ["null", "string"] means the field can be null or a string. Placing null first allows "default": null for backward-compatible optional fields.',
    codeSnippet: `{"name": "phone", "type": ["null", "string"], "default": null}`,
    tags: ['kafka', 'avro', 'union'],
  },
  {
    id: 'kafka-schema-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'serializer/deserializer config',
    question: 'What does `auto.register.schemas=false` do when set on the Avro serializer?',
    options: [
      'Disables schema validation entirely for faster serialization',
      'Prevents the producer from automatically registering new schemas with Schema Registry; the schema must be pre-registered, and unregistered schemas cause a serialization error',
      'Forces the producer to use only schemas from a local file cache',
      'Disables the schema cache so every message fetches the schema fresh',
    ],
    correctAnswer: 1,
    explanation: 'Disabling auto-registration enforces a schema governance workflow where schemas must be manually reviewed and registered (e.g., via CI/CD) before producers can use them.',
    codeSnippet: `props.put("auto.register.schemas", false);\nprops.put("use.latest.version", true);`,
    tags: ['kafka', 'auto-register-schemas', 'governance'],
  },
  {
    id: 'kafka-schema-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'schema evolution',
    question: 'Is removing a field with a default value backward compatible in Avro?',
    options: [
      'Yes — removing any field is always backward compatible',
      'No — removing fields always breaks backward compatibility',
      'Yes — removing a field with a default is backward compatible (new readers use the default for missing fields) but NOT forward compatible (old readers see an unknown field)',
      'It depends on whether the field is at the beginning or end of the schema',
    ],
    correctAnswer: 2,
    explanation: 'Removing a field with a default is backward compatible (new schema can read old data by substituting the default) but breaks forward compatibility (old schema sees the field in new data and must be able to ignore it).',
    tags: ['kafka', 'avro', 'field-removal'],
  },
  {
    id: 'kafka-schema-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'Protobuf',
    question: 'What makes Protobuf inherently more tolerant of schema changes than Avro?',
    options: [
      'Protobuf has stricter validation rules preventing breaking changes',
      'Protobuf encodes fields by tag number; unknown field numbers are skipped during deserialization, so adding new fields never breaks existing consumers even without a default',
      'Protobuf uses JSON encoding which is self-describing',
      'Protobuf schemas are immutable and cannot be changed',
    ],
    correctAnswer: 1,
    explanation: 'The Protobuf wire format tags each field with its number; readers skip unknown tags, making adding new fields always safe. Avro uses positional encoding requiring schema resolution.',
    tags: ['kafka', 'protobuf', 'schema-evolution'],
  },
  {
    id: 'kafka-schema-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'subject naming strategy',
    question: 'What is `TopicRecordNameStrategy` and when would you use it?',
    options: [
      'A strategy that names subjects by combining the topic name and the Avro record\'s fully qualified name',
      'A strategy that names subjects by record name only, ignoring the topic',
      'A strategy that assigns schemas to topics based on the record count',
      'A deprecated strategy replaced by RecordNameStrategy',
    ],
    correctAnswer: 0,
    explanation: 'TopicRecordNameStrategy combines topic name + record name for subjects, allowing the same record type to have different schema evolution histories when used in different topics.',
    codeSnippet: `props.put("value.subject.name.strategy",\n  "io.confluent.kafka.serializers.subject.TopicRecordNameStrategy");`,
    tags: ['kafka', 'topic-record-name-strategy', 'subject'],
  },
  {
    id: 'kafka-schema-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'JSON Schema',
    question: 'What is a key limitation of JSON Schema compatibility in Schema Registry compared to Avro?',
    options: [
      'JSON Schema does not support complex types like arrays and maps',
      'JSON Schema compatibility checking is less mature and precise; some schema changes that are semantically compatible may be flagged as incompatible, or vice versa',
      'JSON Schema cannot be used with Kafka Connect',
      'JSON Schema requires more storage in Schema Registry than Avro',
    ],
    correctAnswer: 1,
    explanation: 'JSON Schema\'s structural composition operators (anyOf, allOf, oneOf) make compatibility analysis significantly more complex than Avro\'s explicit evolution rules, leading to less reliable compatibility checks.',
    tags: ['kafka', 'json-schema', 'limitations'],
  },
  {
    id: 'kafka-schema-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Schema Registry',
    subcategory: 'compatibility modes',
    question: 'What is the difference between BACKWARD and BACKWARD_TRANSITIVE compatibility modes?',
    options: [
      'BACKWARD checks compatibility with the immediately previous version only; BACKWARD_TRANSITIVE checks against all previously registered versions',
      'They are identical in Confluent Schema Registry',
      'BACKWARD_TRANSITIVE only applies to Protobuf; BACKWARD applies to Avro',
      'BACKWARD checks key schemas; BACKWARD_TRANSITIVE checks value schemas',
    ],
    correctAnswer: 0,
    explanation: 'BACKWARD_TRANSITIVE is stricter: the new schema must be backward compatible with all historical schema versions, not just the latest, preventing accumulated incompatibilities.',
    codeSnippet: `# Set compatibility to BACKWARD_TRANSITIVE\ncurl -X PUT http://schema-registry:8081/config/orders-value \\\n  -d '{"compatibility": "BACKWARD_TRANSITIVE"}'`,
    tags: ['kafka', 'backward-transitive', 'compatibility'],
  },
];

export default questions;
