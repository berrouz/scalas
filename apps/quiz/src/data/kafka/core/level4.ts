import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-core-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'replication',
    question: 'Under what exact conditions can a committed message be lost in Kafka?',
    options: [
      'When acks=1 and the leader crashes before followers replicate',
      'When acks=all and min.insync.replicas=1 and the single ISR broker fails',
      'When both acks=all, min.insync.replicas equals the replication factor, and all replicas fail simultaneously',
      'Message loss is impossible with acks=all regardless of other settings',
    ],
    correctAnswer: 2,
    explanation: 'With acks=all and min.insync.replicas equal to the full replication factor, simultaneous failure of ALL replicas is the only way to lose a committed message — a highly unlikely but theoretically possible scenario.',
    tags: ['kafka', 'durability', 'message-loss'],
  },
  {
    id: 'kafka-core-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'ZooKeeper vs KRaft',
    question: 'In KRaft, what happens to cluster availability during a metadata log catch-up after a controller failover?',
    options: [
      'The cluster continues fully normally without interruption',
      'The cluster enters read-only mode until the new active controller is fully caught up',
      'All partition leadership is lost until the new controller loads metadata from followers',
      'Producers experience increased latency while the new controller replays the metadata log, but the cluster remains available',
    ],
    correctAnswer: 3,
    explanation: 'The newly active KRaft controller replays the metadata log to rebuild its state; during this time it cannot process metadata changes, causing some latency, but the cluster continues serving data.',
    codeSnippet: `# Monitor KRaft controller state\nkafka-metadata-quorum.sh --bootstrap-server :9092 describe --status`,
    tags: ['kafka', 'kraft', 'controller-failover'],
  },
  {
    id: 'kafka-core-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'log segments',
    question: 'What is the purpose of the `.timeindex` file in a Kafka partition log?',
    options: [
      'It stores the wall-clock timestamp of each log segment for retention decisions',
      'It maps timestamps to offsets enabling time-based offset lookups',
      'It records when each consumer last read from the segment',
      'It tracks the producer timestamp for ordering validation',
    ],
    correctAnswer: 1,
    explanation: 'The time index maps message timestamps to offsets, allowing consumers to seek to a specific timestamp using `offsetsForTimes()` without scanning the log.',
    codeSnippet: `// Java: seek consumer to a specific timestamp\nMap<TopicPartition, Long> tsMap = Map.of(tp, Instant.now().minusHours(1).toEpochMilli());\nMap<TopicPartition, OffsetAndTimestamp> result = consumer.offsetsForTimes(tsMap);`,
    tags: ['kafka', 'timeindex', 'timestamp'],
  },
  {
    id: 'kafka-core-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'cluster architecture',
    question: 'What is "follower fetching" optimization (KIP-392) and what problem does it solve?',
    options: [
      'It allows followers to serve read requests, reducing leader load',
      'It allows consumers to fetch from the nearest replica (same rack/AZ) to reduce cross-AZ bandwidth costs',
      'It allows followers to push data to lagging replicas',
      'It enables consumers to fetch from multiple partitions in a single request',
    ],
    correctAnswer: 1,
    explanation: 'KIP-392 (fetch from closest replica) lets consumers fetch from the nearest replica in their rack/AZ, minimizing cross-AZ data transfer costs without sacrificing consistency.',
    codeSnippet: `# Consumer config\nclient.rack=us-east-1a\n# Broker config\nreplica.selector.class=org.apache.kafka.common.replica.RackAwareReplicaSelector`,
    tags: ['kafka', 'follower-fetching', 'rack-awareness'],
  },
  {
    id: 'kafka-core-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'retention',
    question: 'During log compaction, what guarantees does Kafka provide about ordering of retained records?',
    options: [
      'Records are re-sorted by key after compaction',
      'The relative order of records with different keys may change after compaction',
      'The relative order of all retained records is preserved — compaction never reorders messages',
      'Only the last record per key is guaranteed; ordering across keys is undefined',
    ],
    correctAnswer: 2,
    explanation: 'Compaction removes older duplicates per key but never reorders the surviving records; the relative offset ordering of retained records is preserved.',
    tags: ['kafka', 'log-compaction', 'ordering'],
  },
  {
    id: 'kafka-core-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'partitions',
    question: 'What is the impact of increasing partition count on an existing topic in Kafka?',
    options: [
      'Existing messages are redistributed across all new partitions automatically',
      'Key-based routing for existing keys may change, breaking ordering guarantees for those keys',
      'The topic temporarily becomes unavailable during partition addition',
      'Replication factor is automatically increased to match the new partition count',
    ],
    correctAnswer: 1,
    explanation: 'Adding partitions changes the key-to-partition mapping (hash % numPartitions), so previously co-located messages may land in different partitions, breaking per-key ordering.',
    tags: ['kafka', 'partitions', 'key-ordering'],
  },
  {
    id: 'kafka-core-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'consumer groups',
    question: 'What is the "incremental cooperative rebalancing" protocol and how does it differ from eager rebalancing?',
    options: [
      'Cooperative rebalancing pauses all consumers during the rebalance; eager rebalancing only pauses affected consumers',
      'Eager rebalancing revokes all partitions before reassignment causing a full stop; cooperative rebalancing transfers only partitions that need to move, minimizing downtime',
      'Cooperative rebalancing is only available with static membership; eager rebalancing works with dynamic membership',
      'Incremental rebalancing adds partitions one at a time; eager adds all at once',
    ],
    correctAnswer: 1,
    explanation: 'Eager (stop-the-world) rebalancing revokes all partitions from all consumers before reassigning; cooperative rebalancing moves only the partitions that need to change, reducing processing gaps.',
    codeSnippet: `# Consumer config\npartition.assignment.strategy=org.apache.kafka.clients.consumer.CooperativeStickyAssignor`,
    tags: ['kafka', 'cooperative-rebalancing', 'consumer-groups'],
  },
  {
    id: 'kafka-core-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'brokers',
    question: 'What is "tiered storage" in Kafka (KIP-405) and what operational problem does it address?',
    options: [
      'Storing hot partitions on SSD and cold partitions on HDD on the same broker',
      'Offloading older log segments to object storage (S3/GCS/ADLS) to decouple retention from broker disk capacity',
      'Using a two-tier replication strategy with local and remote replicas',
      'Separating the controller tier from the broker tier in KRaft mode',
    ],
    correctAnswer: 1,
    explanation: 'Tiered storage allows Kafka to move older log segments to cheap object storage, enabling very long retention without scaling broker disk capacity.',
    tags: ['kafka', 'tiered-storage', 'retention'],
  },
  {
    id: 'kafka-core-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'offsets',
    question: 'What is "epoch fencing" (leader epoch) and what problem does it solve?',
    options: [
      'A mechanism to limit how many epochs a leader can serve before rotation',
      'A mechanism to prevent a stale leader from accepting writes after a newer leader has been elected, preventing data divergence',
      'A ZooKeeper session timeout mechanism that fences out disconnected brokers',
      'A client-side guard that discards messages from unknown epoch producers',
    ],
    correctAnswer: 1,
    explanation: 'Each leader epoch is a monotonically increasing number; followers and producers include the epoch in requests, allowing the cluster to reject stale writes from a previously elected leader that has been superseded.',
    tags: ['kafka', 'leader-epoch', 'fencing'],
  },
  {
    id: 'kafka-core-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'cluster architecture',
    question: 'What is the "delegation token" feature in Kafka used for?',
    options: [
      'Allowing brokers to authenticate to ZooKeeper without a password',
      'Generating short-lived credentials from a long-lived principal for efficient authentication in large clusters',
      'Delegating partition leadership to a secondary broker',
      'Authorizing Kafka Connect workers to access schema registry',
    ],
    correctAnswer: 1,
    explanation: 'Delegation tokens allow an authenticated principal to generate lightweight tokens that workers/tasks can use for authentication, avoiding the overhead of distributing Kerberos keytabs to many processes.',
    tags: ['kafka', 'security', 'delegation-token'],
  },
  {
    id: 'kafka-core-l4-011',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'replication',
    question: 'What is the "observer" replica type introduced in Confluent Platform and its open-source equivalent concept?',
    options: [
      'A replica that votes in ISR quorum but does not serve reads',
      'A non-voting replica that replicates data but is never eligible for leader election, used to serve geo-distributed reads',
      'A replica dedicated to log compaction tasks',
      'A replica that only stores index files, not the actual log data',
    ],
    correctAnswer: 1,
    explanation: 'Observer replicas (Confluent) or "learner" nodes replicate data without participating in the ISR write quorum, enabling read-scaling or cross-region data locality without affecting write latency.',
    tags: ['kafka', 'observer-replica', 'replication'],
  },
  {
    id: 'kafka-core-l4-012',
    language: 'kafka',
    level: 'level4',
    category: 'Core Concepts',
    subcategory: 'topics',
    question: 'What is `message.timestamp.type` and how does "LogAppendTime" differ from "CreateTime"?',
    options: [
      'LogAppendTime uses the producer clock; CreateTime uses the broker clock',
      'CreateTime is set by the producer when the record is created; LogAppendTime is overwritten by the broker with the local time when appended to the log',
      'They are identical; the setting only affects how timestamps are displayed in tooling',
      'LogAppendTime is used for compacted topics; CreateTime for delete-retention topics',
    ],
    correctAnswer: 1,
    explanation: 'CreateTime preserves the producer\'s timestamp for event-time semantics; LogAppendTime makes timestamps monotonically increasing per broker but loses the original event time.',
    codeSnippet: `kafka-configs.sh --alter --entity-type topics --entity-name orders \\\n  --add-config message.timestamp.type=LogAppendTime`,
    tags: ['kafka', 'timestamp', 'log-append-time'],
  },
];

export default questions;
