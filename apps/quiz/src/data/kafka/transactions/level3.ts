import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-tx-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'Transaction Coordinator',
    question: 'What is `transaction.timeout.ms` and what happens when it expires?',
    options: [
      'The maximum time a consumer waits for a commit marker before re-reading from the last committed offset',
      'The maximum time a transaction can stay open before the Transaction Coordinator automatically aborts it and bumps the producer epoch',
      'The timeout for the `initTransactions()` call when connecting to the coordinator',
      'The retention time of completed transaction state in `__transaction_state`',
    ],
    correctAnswer: 1,
    explanation: '`transaction.timeout.ms` (default 60s) is the maximum lifetime of an open transaction. If not committed or aborted within this period, the coordinator aborts it and increments the producer epoch to fence the slow producer.',
    tags: ['kafka', 'transaction-timeout', 'coordinator', 'abort'],
  },
  {
    id: 'kafka-tx-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'Isolation Level',
    question: 'What is the "Last Stable Offset" (LSO) and how does it affect `read_committed` consumers?',
    options: [
      'The last offset where the log was fsynced to disk',
      'The highest offset below which all transactions have been resolved (committed or aborted); `read_committed` consumers cannot advance beyond the LSO',
      'The offset of the most recent compacted record in a compacted topic',
      'The offset of the last transaction marker written to a partition',
    ],
    correctAnswer: 1,
    explanation: 'The LSO is the boundary below which every record is either non-transactional or part of a resolved transaction. `read_committed` consumers are blocked from reading past an open (in-progress) transaction, causing lag if that transaction stalls.',
    tags: ['kafka', 'lso', 'last-stable-offset', 'read-committed', 'consumer-lag'],
  },
  {
    id: 'kafka-tx-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'EOS Basics',
    question: 'Why is exactly-once delivery within Kafka not the same as exactly-once processing in external systems?',
    options: [
      'Kafka EOS only applies to topics with `cleanup.policy=compact`',
      'Kafka guarantees atomic writes within the Kafka log, but side effects on external systems (databases, APIs) are not part of the Kafka transaction and may be re-executed on retry',
      'Kafka EOS requires the external system to support two-phase commit with Kafka',
      'External system writes are automatically rolled back when a Kafka transaction aborts',
    ],
    correctAnswer: 1,
    explanation: 'Kafka EOS guarantees atomic visibility within Kafka. If your consumer writes to a database or calls an external API, those operations happen outside the Kafka transaction and are not atomically coordinated with it.',
    tags: ['kafka', 'eos', 'external-systems', 'idempotency'],
  },
  {
    id: 'kafka-tx-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'Consume-Transform-Produce',
    question: 'How does Kafka Streams achieve exactly-once semantics internally?',
    options: [
      'By using a separate ZooKeeper-backed two-phase commit protocol for each task',
      'By wrapping each task\'s consume-transform-produce cycle in a Kafka transaction using `sendOffsetsToTransaction()`, with a unique `transactional.id` per task',
      'By deduplicating records in the state store using a Bloom filter',
      'By using `isolation.level=serializable` on internal repartition topic consumers',
    ],
    correctAnswer: 1,
    explanation: 'Kafka Streams assigns each stream task a unique `transactional.id` (based on application ID + partition). Each task wraps its processing loop in a transaction, atomically committing output records and input offsets.',
    tags: ['kafka', 'kafka-streams', 'eos', 'transactional-id', 'task'],
  },
  {
    id: 'kafka-tx-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'Idempotent Producer',
    question: 'How many unique (PID, epoch, sequence) combinations can a broker track per partition for idempotency?',
    options: [
      'Unlimited; the broker stores all historical producer state on disk',
      'Up to 5 in-flight batches per (PID, epoch) pair; the broker uses a sliding window of sequence numbers per producer',
      'Exactly 1; only the last sequence number per producer is remembered',
      'Up to `transactional.id.expiration.ms / 1000` entries per partition',
    ],
    correctAnswer: 1,
    explanation: 'The broker maintains a sliding window of the last 5 sequence numbers per (PID, epoch) per partition. This supports up to 5 in-flight batches safely. Sequences outside the window are rejected as duplicates or out-of-order.',
    tags: ['kafka', 'idempotent', 'sequence-number', 'pid', 'sliding-window'],
  },
  {
    id: 'kafka-tx-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'Transaction Coordinator',
    question: 'What happens to a producer\'s transactional state if the Transaction Coordinator broker fails during a commit?',
    options: [
      'The transaction is immediately aborted and the producer must restart from scratch',
      'The coordinator\'s state is persisted in `__transaction_state` (replicated); when a new leader takes over, it reads the state and completes or aborts the in-flight transaction on the producer\'s next request',
      'The transaction enters a permanent "pending" state and requires manual intervention',
      'All partitions written in the transaction are rolled back automatically via a two-phase protocol',
    ],
    correctAnswer: 1,
    explanation: '`__transaction_state` is a replicated topic. The new coordinator leader loads the persisted transaction state and can complete the protocol. On the next `commitTransaction()` or `initTransactions()` call, the producer interacts with the new coordinator to resolve the state.',
    tags: ['kafka', 'transaction-coordinator', 'fault-tolerance', 'recovery'],
  },
  {
    id: 'kafka-tx-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Transactions & EOS',
    subcategory: 'Isolation Level',
    question: 'Can `read_committed` consumers experience increased lag compared to `read_uncommitted` consumers? Why?',
    options: [
      'No — transaction markers are written before any records so committed data is always available immediately',
      'Yes — a long-running or stalled transaction blocks the LSO from advancing, causing `read_committed` consumers to stop reading even if non-transactional records are available past the LSO',
      'No — brokers physically separate transactional and non-transactional records in separate partitions',
      'Yes — but only when `isolation.level=read_committed` is combined with `acks=all`',
    ],
    correctAnswer: 1,
    explanation: 'The LSO is pinned at the start offset of the oldest open transaction. Even if new, non-transactional records arrive after it, `read_committed` consumers cannot read past the LSO until that transaction resolves. Monitoring the LSO lag is critical for transactional pipelines.',
    tags: ['kafka', 'read-committed', 'lso', 'consumer-lag', 'transactions'],
  },
];

export default questions;
