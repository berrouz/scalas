import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-streams-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'windowing',
    question: 'What is "sliding window" in Kafka Streams and how does it differ from hopping windows?',
    options: [
      'They are identical; "sliding" is an older name for hopping',
      'A sliding window in Kafka Streams creates a new window for every unique pair of events within the time difference; it does not advance by a fixed step',
      'Sliding windows use event time; hopping windows use processing time',
      'Sliding windows are only used with session aggregations',
    ],
    correctAnswer: 1,
    explanation: 'SlidingWindows.ofTimeDifferenceWithNoGrace() creates a window that spans exactly the time difference between two events; a new window starts with every new record, unlike hopping which advances by a fixed step.',
    codeSnippet: `stream.groupByKey()\n  .windowedBy(SlidingWindows.ofTimeDifferenceWithNoGrace(Duration.ofMinutes(5)))\n  .count();`,
    tags: ['kafka-streams', 'sliding-window', 'windowing'],
  },
  {
    id: 'kafka-streams-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'interactive queries',
    question: 'How do interactive queries work across multiple Kafka Streams instances in a distributed deployment?',
    options: [
      'All state is replicated to instance 0 which serves all queries',
      'Each instance serves only the keys it owns locally; the application must route queries to the correct instance using metadata from streams.queryMetadataForKey()',
      'Kafka Streams provides a built-in REST server that aggregates state from all instances',
      'Interactive queries are only available when all instances run on the same machine',
    ],
    correctAnswer: 1,
    explanation: 'queryMetadataForKey() returns the host:port of the instance owning a specific key\'s partition; the caller must forward the query to that instance via its own RPC mechanism.',
    codeSnippet: `KeyQueryMetadata metadata = streams.queryMetadataForKey("my-store", key, stringSerde.serializer());\n// If metadata.activeHost() != localHost, forward query via HTTP`,
    tags: ['kafka-streams', 'interactive-queries', 'distributed'],
  },
  {
    id: 'kafka-streams-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'exactly-once',
    question: 'Why does enabling exactly-once semantics in Kafka Streams impact throughput?',
    options: [
      'EOS forces the application to use a single-threaded processing model',
      'Each processing cycle involves a Kafka transaction (begin, write, sendOffsets, commit) with synchronous coordination, adding overhead per commit interval',
      'EOS requires double the number of partitions to maintain transactional state',
      'EOS disables RocksDB caching to ensure consistent state',
    ],
    correctAnswer: 1,
    explanation: 'Each transaction commit requires coordination between the producer, transaction coordinator, and consumer group coordinator, adding latency. commit.interval.ms controls the trade-off between overhead and latency.',
    codeSnippet: `props.put(StreamsConfig.COMMIT_INTERVAL_MS_CONFIG, 100); // commit every 100ms`,
    tags: ['kafka-streams', 'exactly-once', 'performance'],
  },
  {
    id: 'kafka-streams-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'state stores',
    question: 'What is the `punctuate()` method in the Kafka Streams Processor API used for?',
    options: [
      'Adding punctuation to string values for formatting',
      'Scheduling periodic callbacks (by stream time or wall-clock time) to trigger actions like emitting results or cleaning state',
      'Marking the boundary between two sub-topologies',
      'Flushing the state store to the changelog topic',
    ],
    correctAnswer: 1,
    explanation: 'Punctuation schedules a recurring callback at a specified interval using either STREAM_TIME (advances with events) or WALL_CLOCK_TIME (real-time), useful for windowed result emission or periodic cleanup.',
    codeSnippet: `context.schedule(Duration.ofSeconds(10), PunctuationType.STREAM_TIME, timestamp -> {\n  // emit aggregated results or clean up expired state\n});`,
    tags: ['kafka-streams', 'punctuate', 'processor-api'],
  },
  {
    id: 'kafka-streams-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'topology',
    question: 'What is "sub-topology" in Kafka Streams and why does it matter?',
    options: [
      'A subset of the topology that runs on a dedicated broker',
      'A connected component of the topology that processes data independently; sub-topologies are separated by repartition or changelog topics',
      'A topology fragment that handles exactly-once logic',
      'A topology test utility for unit testing individual processors',
    ],
    correctAnswer: 1,
    explanation: 'Kafka Streams breaks a topology into sub-topologies at repartition/changelog topic boundaries; each sub-topology can be scaled independently and represents a distinct stream processing task.',
    tags: ['kafka-streams', 'sub-topology', 'topology'],
  },
  {
    id: 'kafka-streams-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'joins',
    question: 'What happens in a KStream-KTable join when the KTable does not have an entry for the stream record\'s key?',
    options: [
      'The stream record is buffered until a matching KTable entry arrives',
      'An inner join drops the stream record; a left join emits the stream record with a null table value',
      'The stream record is sent to a dead letter topic',
      'An exception is thrown and the application pauses',
    ],
    correctAnswer: 1,
    explanation: 'In an inner KStream-KTable join, stream records without a matching table key are dropped. In a left join, they are emitted with null as the table-side value.',
    codeSnippet: `// Left join: emit even if table has no entry for the key\nstream.leftJoin(table, (streamVal, tableVal) ->\n  tableVal == null ? streamVal + ":unknown" : streamVal + ":" + tableVal);`,
    tags: ['kafka-streams', 'stream-table-join', 'null-handling'],
  },
  {
    id: 'kafka-streams-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'KTable',
    question: 'What is the `suppress()` operator in Kafka Streams and when do you use it?',
    options: [
      'It suppresses error messages in the streams log',
      'It buffers intermediate KTable/windowed aggregation updates and emits only the final result per window/key, reducing downstream update chatter',
      'It prevents a KTable from emitting updates until a minimum number of records have been processed',
      'It suppresses reprocessing of already-processed records after a restart',
    ],
    correctAnswer: 1,
    explanation: 'suppress() with Suppressed.untilWindowCloses() buffers all window updates and emits only one final result per window after it closes and the grace period expires, useful for exactly-once windowed output.',
    codeSnippet: `table.suppress(Suppressed.untilWindowCloses(Suppressed.BufferConfig.unbounded()))\n  .toStream().to("window-results");`,
    tags: ['kafka-streams', 'suppress', 'windowing'],
  },
  {
    id: 'kafka-streams-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'state stores',
    question: 'What is the difference between a persistent and an in-memory state store in Kafka Streams?',
    options: [
      'Persistent stores are backed by a changelog topic; in-memory stores are not backed by any Kafka topic',
      'Persistent stores (RocksDB) survive application restarts by replaying the changelog; in-memory stores lose state on restart and must fully replay the changelog',
      'In-memory stores are faster but have no fault tolerance at all, even with changelog topics',
      'There is no functional difference; the choice is purely a performance optimization',
    ],
    correctAnswer: 1,
    explanation: 'Both persistent (RocksDB) and in-memory stores can be changelog-backed. However, RocksDB stores only need to replay the changelog delta from the last checkpoint; in-memory stores replay from scratch on every restart.',
    tags: ['kafka-streams', 'in-memory-store', 'rocksdb'],
  },
  {
    id: 'kafka-streams-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'topology',
    question: 'What is "stream task" parallelism in Kafka Streams?',
    options: [
      'The number of Java threads allocated per KafkaStreams instance',
      'Each task processes one input partition; Kafka Streams creates as many tasks as the maximum partition count across input topics, distributing them across available threads/instances',
      'Parallelism is determined by the number of KafkaStreams instances running',
      'Tasks are dynamically scaled based on consumer lag',
    ],
    correctAnswer: 1,
    explanation: 'Streams tasks are the unit of parallelism: one task per partition. num.stream.threads controls thread count per instance; tasks are distributed across threads and instances for parallel execution.',
    codeSnippet: `props.put(StreamsConfig.NUM_STREAM_THREADS_CONFIG, 4); // 4 threads per instance`,
    tags: ['kafka-streams', 'parallelism', 'tasks'],
  },
  {
    id: 'kafka-streams-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'GlobalKTable',
    question: 'What are the consistency implications of using GlobalKTable in Kafka Streams?',
    options: [
      'GlobalKTable provides stronger consistency than KTable because all data is local',
      'GlobalKTable may lag behind the source topic; a stream record joining against GlobalKTable may see stale data if the table has not consumed all updates yet',
      'GlobalKTable guarantees read-your-writes consistency for all joins',
      'GlobalKTable is always fully consistent because it uses exactly-once semantics',
    ],
    correctAnswer: 1,
    explanation: 'GlobalKTable is populated asynchronously; if updates to the source topic have not been consumed yet by the GlobalKTable consumer, joins may see outdated values.',
    tags: ['kafka-streams', 'globalktable', 'consistency'],
  },
  {
    id: 'kafka-streams-l3-011',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'Processor API',
    question: 'What is `context.forward()` in the Kafka Streams Processor API?',
    options: [
      'It sends a record to a specified downstream processor node or all child nodes',
      'It forwards the record to the next Kafka topic without processing',
      'It schedules the record for processing in the next poll cycle',
      'It marks the record as processed and commits the offset',
    ],
    correctAnswer: 0,
    explanation: 'context.forward() routes the record to one or all downstream processor nodes in the topology, optionally specifying a child node name.',
    codeSnippet: `context.forward(record); // to all children\ncontext.forward(record, To.child("downstream-processor")); // to specific child`,
    tags: ['kafka-streams', 'processor-api', 'forward'],
  },
  {
    id: 'kafka-streams-l3-012',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Streams',
    subcategory: 'exactly-once',
    question: 'What is the role of `commit.interval.ms` in Kafka Streams exactly-once processing?',
    options: [
      'It controls how often the state store is flushed to disk',
      'It controls how frequently Kafka Streams commits the transaction (offsets + state store + output records), balancing latency and throughput',
      'It is the maximum time allowed for a single transaction to complete',
      'It sets the offset commit interval for the underlying Kafka consumer',
    ],
    correctAnswer: 1,
    explanation: 'A smaller commit.interval.ms reduces reprocessing on failure (shorter transactions) but increases transaction overhead; larger values batch more work per transaction for higher throughput.',
    tags: ['kafka-streams', 'commit-interval', 'exactly-once'],
  },
];

export default questions;
