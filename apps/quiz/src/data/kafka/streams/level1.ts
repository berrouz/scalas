import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-streams-l1-001',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'KStream',
    question: 'What is a KStream in Kafka Streams?',
    options: [
      'A stream of key-value pairs where each record is treated as an independent event (insert semantics)',
      'A changelog stream where each record updates or replaces the previous value for a key',
      'A global table replicated to all stream processor instances',
      'A windowed aggregation of events grouped by key',
    ],
    correctAnswer: 0,
    explanation: 'KStream represents an unbounded, append-only stream of records where every record is an independent event — similar to a log.',
    codeSnippet: `KStream<String, String> stream = builder.stream("input-topic");\nstream.filter((k, v) -> v != null).to("output-topic");`,
    tags: ['kafka-streams', 'kstream', 'basics'],
  },
  {
    id: 'kafka-streams-l1-002',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'KTable',
    question: 'What is a KTable in Kafka Streams?',
    options: [
      'A table of fixed schema stored in RocksDB',
      'A changelog stream where each record is an update to the value for a given key (upsert semantics)',
      'A SQL table exposed via KSQL',
      'A partitioned in-memory cache of recent messages',
    ],
    correctAnswer: 1,
    explanation: 'KTable represents a materialized view of the latest value for each key; a new record with an existing key replaces the previous value, and a null value is treated as a delete.',
    codeSnippet: `KTable<String, String> table = builder.table("users-topic");\ntable.toStream().to("users-stream");`,
    tags: ['kafka-streams', 'ktable', 'basics'],
  },
  {
    id: 'kafka-streams-l1-003',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'topology',
    question: 'What is a "topology" in Kafka Streams?',
    options: [
      'The network layout of Kafka brokers',
      'A directed acyclic graph of stream processing nodes (sources, processors, sinks)',
      'The physical partition assignment of stream tasks',
      'A configuration object that defines consumer group settings',
    ],
    correctAnswer: 1,
    explanation: 'A topology is a DAG defining how records flow from source nodes (reading topics), through processor nodes (transformations), to sink nodes (writing topics).',
    tags: ['kafka-streams', 'topology', 'basics'],
  },
  {
    id: 'kafka-streams-l1-004',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'state stores',
    question: 'What is a state store in Kafka Streams?',
    options: [
      'A remote database used to persist Kafka messages',
      'A local storage (typically RocksDB) that allows stream processors to store and query state, backed by a changelog Kafka topic',
      'An in-memory cache shared across all stream processor instances',
      'A Kafka topic used to store topology configuration',
    ],
    correctAnswer: 1,
    explanation: 'State stores are local key-value stores (default RocksDB) used by stateful operations like aggregations; they are backed by changelog topics for fault tolerance.',
    tags: ['kafka-streams', 'state-stores', 'rocksdb'],
  },
  {
    id: 'kafka-streams-l1-005',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'windowing',
    question: 'What is a tumbling window in Kafka Streams?',
    options: [
      'A window that slides forward by a fixed interval, potentially overlapping',
      'A fixed-size, non-overlapping time window that moves forward by its own size',
      'A window that grows dynamically based on event activity',
      'A window defined by gaps in event arrival time',
    ],
    correctAnswer: 1,
    explanation: 'Tumbling windows are fixed-size, non-overlapping buckets; e.g., a 5-minute tumbling window creates buckets 00:00-00:05, 00:05-00:10, etc.',
    codeSnippet: `stream.groupByKey()\n  .windowedBy(TimeWindows.ofSizeWithNoGrace(Duration.ofMinutes(5)))\n  .count();`,
    tags: ['kafka-streams', 'tumbling-window', 'windowing'],
  },
  {
    id: 'kafka-streams-l1-006',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'joins',
    question: 'What type of join can be performed between a KStream and a KTable?',
    options: [
      'Only outer join is supported',
      'Inner join and left join are supported; the KTable provides the "right" side lookup',
      'Full outer join only',
      'Only stream-to-stream joins are supported in Kafka Streams',
    ],
    correctAnswer: 1,
    explanation: 'KStream-KTable joins are lookup-based: each incoming stream record looks up the current KTable value for the same key. Inner join emits only when the KTable has a value; left join emits even without a KTable match.',
    tags: ['kafka-streams', 'stream-table-join', 'joins'],
  },
  {
    id: 'kafka-streams-l1-007',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'topology',
    question: 'How do you start a Kafka Streams application?',
    options: [
      'By deploying it to a Kafka broker as a plugin',
      'By calling kafkaStreams.start() on a KafkaStreams instance built from a topology and config',
      'By submitting the topology to the Kafka Connect framework',
      'By publishing the topology definition to a special Kafka topic',
    ],
    correctAnswer: 1,
    explanation: 'A KafkaStreams object wraps the topology and properties; calling start() launches the processing threads and begins consuming from source topics.',
    codeSnippet: `KafkaStreams streams = new KafkaStreams(builder.build(), props);\nstreams.start();\nRuntime.getRuntime().addShutdownHook(new Thread(streams::close));`,
    tags: ['kafka-streams', 'startup', 'kafkastreams'],
  },
  {
    id: 'kafka-streams-l1-008',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'GlobalKTable',
    question: 'What is the key difference between a KTable and a GlobalKTable?',
    options: [
      'GlobalKTable stores data in a remote database; KTable uses local RocksDB',
      'KTable is partitioned — each instance holds a subset of keys; GlobalKTable is fully replicated to every stream instance',
      'GlobalKTable supports streaming joins; KTable supports only batch joins',
      'GlobalKTable is backed by a compacted topic; KTable is not',
    ],
    correctAnswer: 1,
    explanation: 'GlobalKTable populates itself from all partitions on every instance, enabling key-based joins without co-partitioning requirements, at the cost of more memory and storage per instance.',
    tags: ['kafka-streams', 'globalktable', 'ktable'],
  },
  {
    id: 'kafka-streams-l1-009',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'exactly-once',
    question: 'How do you enable exactly-once semantics in Kafka Streams?',
    options: [
      'Set enable.auto.commit=false in the streams properties',
      'Set processing.guarantee=exactly_once_v2 in the streams properties',
      'Use GlobalKTable instead of KTable for all joins',
      'Call stream.enableExactlyOnce() before calling start()',
    ],
    correctAnswer: 1,
    explanation: 'Setting processing.guarantee to exactly_once_v2 (Kafka 2.5+) configures Streams to use Kafka transactions under the hood for atomic read-process-write cycles.',
    codeSnippet: `props.put(StreamsConfig.PROCESSING_GUARANTEE_CONFIG, StreamsConfig.EXACTLY_ONCE_V2);`,
    tags: ['kafka-streams', 'exactly-once', 'processing-guarantee'],
  },
  {
    id: 'kafka-streams-l1-010',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'KStream',
    question: 'What does the `map()` operation do in Kafka Streams?',
    options: [
      'Filters records based on a predicate',
      'Transforms each input record into a new key-value pair',
      'Joins two streams based on matching keys',
      'Groups records by key for aggregation',
    ],
    correctAnswer: 1,
    explanation: 'map() applies a KeyValueMapper to each record and emits a new record with potentially different key and value types; it can trigger repartitioning if the key changes.',
    codeSnippet: `stream.map((key, value) -> KeyValue.pair(key.toUpperCase(), value.trim()));`,
    tags: ['kafka-streams', 'map', 'transformation'],
  },
  {
    id: 'kafka-streams-l1-011',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'state stores',
    question: 'What Kafka topic backs a Kafka Streams state store for fault tolerance?',
    options: [
      '__state_store_changelog',
      'A changelog topic named <application.id>-<store-name>-changelog',
      'The same source topic the stream reads from',
      '__consumer_offsets',
    ],
    correctAnswer: 1,
    explanation: 'Kafka Streams automatically creates a compacted changelog topic per state store. On restart or failover, the state store is rebuilt by replaying this changelog.',
    tags: ['kafka-streams', 'changelog-topic', 'fault-tolerance'],
  },
  {
    id: 'kafka-streams-l1-012',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'windowing',
    question: 'What is a hopping window in Kafka Streams?',
    options: [
      'A window that moves forward by its own size (non-overlapping)',
      'A fixed-size window that advances by a smaller step, so windows can overlap',
      'A window that spans an entire Kafka topic from start to end',
      'A window defined by the gap between consecutive events',
    ],
    correctAnswer: 1,
    explanation: 'Hopping windows have a fixed size and a smaller advance interval; e.g., a 10-minute window that hops every 5 minutes means each event appears in two windows.',
    codeSnippet: `TimeWindows.ofSizeAndGrace(Duration.ofMinutes(10), Duration.ofSeconds(30))\n  .advanceBy(Duration.ofMinutes(5))`,
    tags: ['kafka-streams', 'hopping-window', 'windowing'],
  },
  {
    id: 'kafka-streams-l1-013',
    language: 'kafka',
    level: 'level1',
    category: 'Kafka Streams',
    subcategory: 'topology',
    question: 'What is the `application.id` property in Kafka Streams used for?',
    options: [
      'A human-readable label displayed in monitoring dashboards only',
      'The consumer group ID for the streams application, prefix for internal topics, and identifier for application state stores',
      'The Kafka broker identifier that the streams app connects to',
      'The schema registry namespace for this application\'s schemas',
    ],
    correctAnswer: 1,
    explanation: 'application.id serves as the consumer group ID (enabling offset tracking), prefix for auto-created repartition and changelog topics, and uniquely identifies the application.',
    codeSnippet: `props.put(StreamsConfig.APPLICATION_ID_CONFIG, "order-processor");`,
    tags: ['kafka-streams', 'application-id', 'configuration'],
  },
];

export default questions;
