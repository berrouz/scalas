import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-streams-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'joins',
    question: 'What is a requirement for a KStream-KStream join to work correctly?',
    options: [
      'Both streams must have the same number of partitions and be co-partitioned',
      'One stream must be a GlobalKTable',
      'Joins are based on value content, not key',
      'Both streams must be read from the same topic',
    ],
    correctAnswer: 0,
    explanation: 'Stream-stream joins require co-partitioning: same number of partitions and same partitioning strategy, so matching keys land on the same stream task.',
    codeSnippet: `// Both topics must have the same partition count and key type\nKStream<String, Order> orders = builder.stream("orders");\nKStream<String, Payment> payments = builder.stream("payments");\norders.join(payments, (o, p) -> combine(o, p), JoinWindows.ofTimeDifferenceWithNoGrace(Duration.ofMinutes(5)));`,
    tags: ['kafka-streams', 'stream-stream-join', 'co-partitioning'],
  },
  {
    id: 'kafka-streams-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'windowing',
    question: 'What is a session window in Kafka Streams?',
    options: [
      'A window tied to a user login session in a web application',
      'A dynamically sized window that groups events by key within a period of inactivity (gap); if no event arrives within the gap, the session closes',
      'A fixed-size window aligned to clock boundaries (hour, day)',
      'A window that spans the entire stream from the beginning',
    ],
    correctAnswer: 1,
    explanation: 'Session windows group per-key events that arrive within an inactivity gap; each new event either extends the current session or starts a new one, making window size dynamic.',
    codeSnippet: `stream.groupByKey()\n  .windowedBy(SessionWindows.ofInactivityGapWithNoGrace(Duration.ofMinutes(30)))\n  .count();`,
    tags: ['kafka-streams', 'session-window', 'windowing'],
  },
  {
    id: 'kafka-streams-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'state stores',
    question: 'What is the default local storage engine used by Kafka Streams state stores?',
    options: [
      'LevelDB',
      'H2 in-memory database',
      'RocksDB',
      'MapDB',
    ],
    correctAnswer: 2,
    explanation: 'Kafka Streams uses RocksDB by default for persistent state stores, providing efficient on-disk key-value storage with good write throughput.',
    tags: ['kafka-streams', 'rocksdb', 'state-stores'],
  },
  {
    id: 'kafka-streams-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'interactive queries',
    question: 'What are "interactive queries" in Kafka Streams?',
    options: [
      'SQL queries executed against Kafka topics via KSQL',
      'The ability to query the local state stores of a running Streams application directly, without going through Kafka',
      'A REST API for submitting new topologies at runtime',
      'Consumer group queries for monitoring lag',
    ],
    correctAnswer: 1,
    explanation: 'Interactive queries allow external services to read the materialized state (e.g., aggregation results) from the local RocksDB state stores of stream instances via the Streams QueryableStoreTypes API.',
    codeSnippet: `ReadOnlyKeyValueStore<String, Long> store =\n  streams.store(StoreQueryParameters.fromNameAndType("counts", QueryableStoreTypes.keyValueStore()));\nlong count = store.get("my-key");`,
    tags: ['kafka-streams', 'interactive-queries', 'state-stores'],
  },
  {
    id: 'kafka-streams-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'KTable',
    question: 'What does a null value in a KTable record represent?',
    options: [
      'An uninitialized value that will be set on the next update',
      'A delete tombstone — the key is removed from the materialized table',
      'An error in the source data',
      'A placeholder for schema evolution',
    ],
    correctAnswer: 1,
    explanation: 'In KTable semantics, a null value acts as a delete marker (tombstone): the entry for that key is removed from the local state store and downstream KTable subscribers.',
    tags: ['kafka-streams', 'ktable', 'tombstone'],
  },
  {
    id: 'kafka-streams-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'Processor API',
    question: 'What is the Processor API in Kafka Streams?',
    options: [
      'A high-level DSL for defining stream transformations declaratively',
      'A low-level API allowing custom processing logic with direct access to context, state stores, and punctuation (scheduled callbacks)',
      'An API for deploying Streams applications to Kubernetes',
      'A REST API for managing running Streams topologies',
    ],
    correctAnswer: 1,
    explanation: 'The Processor API provides fine-grained control: implement Processor<K,V> to access ProcessorContext for forward(), state store access, and scheduling punctuators for time-based callbacks.',
    codeSnippet: `public class MyProcessor implements Processor<String, String> {\n  public void process(Record<String, String> record) {\n    context.forward(record.withValue(record.value().toUpperCase()));\n  }\n}`,
    tags: ['kafka-streams', 'processor-api', 'low-level'],
  },
  {
    id: 'kafka-streams-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'joins',
    question: 'What is the advantage of using GlobalKTable over KTable for a join when data is not co-partitioned?',
    options: [
      'GlobalKTable joins are faster because data is local to every instance',
      'GlobalKTable does not require co-partitioning; every instance has a full copy so any stream record can join regardless of its partition',
      'GlobalKTable supports outer joins while KTable only supports inner joins',
      'GlobalKTable automatically repartitions the stream to match the table',
    ],
    correctAnswer: 1,
    explanation: 'Since GlobalKTable is fully replicated on every instance, join lookups always succeed locally without requiring the stream to be repartitioned to match table keys.',
    tags: ['kafka-streams', 'globalktable', 'co-partitioning'],
  },
  {
    id: 'kafka-streams-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'topology',
    question: 'What is a "repartition topic" in Kafka Streams and when is it created?',
    options: [
      'A topic created manually to increase partition count of existing topics',
      'An internal topic automatically created when a key-changing operation (e.g., map, selectKey) precedes a stateful operation that requires co-partitioning',
      'A topic used to store serialization errors during stream processing',
      'The changelog topic for a state store',
    ],
    correctAnswer: 1,
    explanation: 'When a key-changing operation occurs before a groupBy or join, Kafka Streams inserts an internal repartition topic to ensure correct key-based routing before the stateful step.',
    tags: ['kafka-streams', 'repartition-topic', 'internal-topics'],
  },
  {
    id: 'kafka-streams-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'windowing',
    question: 'What is "grace period" in Kafka Streams windowing?',
    options: [
      'A delay before the first window is evaluated after startup',
      'Extra time after a window\'s end time during which late-arriving records are still accepted into that window',
      'The time Kafka Streams waits before emitting a final window result',
      'The minimum gap between consecutive session windows',
    ],
    correctAnswer: 1,
    explanation: 'Grace period extends the window\'s acceptance time beyond its end timestamp, allowing late records to be included in the correct window before it is closed and results are finalized.',
    codeSnippet: `TimeWindows.ofSizeAndGrace(Duration.ofMinutes(5), Duration.ofSeconds(30))`,
    tags: ['kafka-streams', 'grace-period', 'late-data'],
  },
  {
    id: 'kafka-streams-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'exactly-once',
    question: 'What is the difference between `exactly_once` and `exactly_once_v2` in Kafka Streams?',
    options: [
      'They are identical; v2 is just an alias for backward compatibility',
      'exactly_once creates one transactional producer per partition; exactly_once_v2 creates one per task, reducing broker load significantly',
      'exactly_once_v2 requires a newer broker version but has identical semantics',
      'exactly_once is for stateless operations; exactly_once_v2 is for stateful operations',
    ],
    correctAnswer: 1,
    explanation: 'exactly_once (EOS v1) creates a transactional producer per partition; EOS v2 uses one producer per thread, drastically reducing the number of transactions and broker-side PID state.',
    tags: ['kafka-streams', 'exactly-once', 'eos-v2'],
  },
  {
    id: 'kafka-streams-l2-011',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'KStream',
    question: 'What is the difference between `flatMap()` and `map()` in Kafka Streams?',
    options: [
      'flatMap() changes the key type; map() changes the value type',
      'flatMap() can emit zero or more output records per input record; map() always emits exactly one',
      'map() supports stateful operations; flatMap() is stateless only',
      'They are equivalent; flatMap() is the newer API version',
    ],
    correctAnswer: 1,
    explanation: 'flatMap() allows an input record to produce a variable number of output records (including zero), useful for splitting or expanding events.',
    codeSnippet: `stream.flatMap((k, v) -> {\n  List<KeyValue<String, String>> list = new ArrayList<>();\n  for (String word : v.split(" ")) list.add(KeyValue.pair(k, word));\n  return list;\n});`,
    tags: ['kafka-streams', 'flatmap', 'transformation'],
  },
  {
    id: 'kafka-streams-l2-012',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'state stores',
    question: 'What is a "standby replica" in Kafka Streams?',
    options: [
      'A backup Kafka broker holding copies of state store data',
      'A stream task on a secondary instance that keeps a warm copy of the state store, enabling faster failover',
      'A read-only replica of a state store for interactive queries',
      'A consumer group member that is on standby waiting for a partition assignment',
    ],
    correctAnswer: 1,
    explanation: 'Configuring `num.standby.replicas` causes Kafka Streams to maintain shadow copies of state stores on other instances, minimizing restoration time on failure by replaying fewer changelog records.',
    codeSnippet: `props.put(StreamsConfig.NUM_STANDBY_REPLICAS_CONFIG, 1);`,
    tags: ['kafka-streams', 'standby-replicas', 'fault-tolerance'],
  },
  {
    id: 'kafka-streams-l2-013',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Streams',
    subcategory: 'GlobalKTable',
    question: 'When should you prefer GlobalKTable over KTable for a stream-table join?',
    options: [
      'When the table is very large (>10 GB) and cannot fit on a single instance',
      'When the stream and table are not co-partitioned and repartitioning would be too expensive or impossible',
      'When the join must be performed with an outer join semantic',
      'When the table needs to be updated more frequently than once per minute',
    ],
    correctAnswer: 1,
    explanation: 'GlobalKTable is ideal for reference/lookup data that is not co-partitioned with the stream (e.g., joining events with a user profile table keyed differently).',
    tags: ['kafka-streams', 'globalktable', 'use-cases'],
  },
];

export default questions;
