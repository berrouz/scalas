import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-connect-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What is Debezium and how does it differ from a JDBC Source Connector?',
    options: [
      'Debezium is a sink connector for writing to relational databases; JDBC Source is for reading',
      'Debezium captures row-level changes using database transaction logs (CDC), streaming every insert/update/delete as an event; JDBC Source polls the database periodically and cannot detect deletes',
      'They are functionally identical; Debezium is just the open-source version of JDBC Source',
      'Debezium requires Kafka Streams; JDBC Source is standalone',
    ],
    correctAnswer: 1,
    explanation: 'Debezium implements CDC (Change Data Capture) via log mining (Binlog for MySQL, WAL for PostgreSQL), enabling sub-second latency and capturing deletes — capabilities the poll-based JDBC connector lacks.',
    codeSnippet: `# Debezium MySQL connector config excerpt\nconnector.class=io.debezium.connector.mysql.MySqlConnector\ndatabase.server.name=mydb\ndatabase.include.list=inventory`,
    tags: ['kafka-connect', 'debezium', 'cdc'],
  },
  {
    id: 'kafka-connect-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'SMTs',
    question: 'What is the `ExtractField` SMT and how is it different from `ValueToKey`?',
    options: [
      'ExtractField extracts a single field from a struct to use as the entire key or value; ValueToKey copies the entire value struct to become the key',
      'They are identical SMTs with different names for historical reasons',
      'ExtractField works on keys only; ValueToKey works on values only',
      'ValueToKey extracts a nested field; ExtractField works only on top-level fields',
    ],
    correctAnswer: 0,
    explanation: 'ExtractField replaces the whole key/value with a single extracted field value. ValueToKey copies specified value fields into the key struct. Both enable key-based routing but in different ways.',
    codeSnippet: `transforms=extractId,setKey\ntransforms.extractId.type=org.apache.kafka.connect.transforms.ExtractField\$Key\ntransforms.extractId.field=id`,
    tags: ['kafka-connect', 'extract-field', 'value-to-key'],
  },
  {
    id: 'kafka-connect-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'offset management',
    question: 'How does a sink connector track which Kafka records it has already processed?',
    options: [
      'It uses its own internal database to store processed record IDs',
      'Kafka Connect manages sink offsets using the standard consumer group mechanism via __consumer_offsets, committing offsets after the task\'s put() method succeeds',
      'Sink connectors do not track offsets; they rely on idempotent writes to the target system',
      'Offsets are stored in the connect-offsets topic alongside source connector offsets',
    ],
    correctAnswer: 1,
    explanation: 'Sink connectors use the Kafka consumer protocol under the hood; Kafka Connect commits consumer offsets after SinkTask.put() returns successfully, providing at-least-once delivery semantics.',
    tags: ['kafka-connect', 'sink-offsets', 'consumer-offsets'],
  },
  {
    id: 'kafka-connect-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'converters',
    question: 'What happens when a connector uses `AvroConverter` but the Schema Registry is unavailable?',
    options: [
      'The converter falls back to JSON serialization automatically',
      'The connector task fails with a serialization exception; records cannot be produced/consumed until Schema Registry is restored',
      'The connector caches the last known schema and continues temporarily',
      'The converter writes raw bytes without schema information',
    ],
    correctAnswer: 1,
    explanation: 'AvroConverter must contact Schema Registry to register or retrieve schemas; if unavailable, schema lookup fails and the task will fail. Resilience requires Schema Registry high availability.',
    tags: ['kafka-connect', 'avro-converter', 'schema-registry'],
  },
  {
    id: 'kafka-connect-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'distributed vs standalone mode',
    question: 'What is "exactly-once" support in Kafka Connect sink connectors (KIP-618) and what does it require?',
    options: [
      'It requires the sink target to support two-phase commit protocol',
      'Kafka Connect 3.3+ supports exactly-once for sink connectors by using the consumer\'s isolation.level=read_committed and committing consumer offsets atomically with the sink write transaction',
      'Exactly-once in Connect is achieved via deduplication in the SMT layer',
      'Exactly-once sink is only available with Confluent Platform, not open-source Kafka',
    ],
    correctAnswer: 1,
    explanation: 'KIP-618 adds exactly-once sink support: Connect reads committed transactions only (read_committed) and uses a two-phase approach — write to sink, then commit offset — preventing duplicates on task restart.',
    codeSnippet: `exactly.once.source.support=enabled  # in worker config\nexactly.once.support=required         # in connector config`,
    tags: ['kafka-connect', 'exactly-once', 'kip-618'],
  },
  {
    id: 'kafka-connect-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What is the `tasks.max` configuration in Kafka Connect and how does it relate to actual task count?',
    options: [
      'tasks.max is the exact number of tasks that will always be created',
      'tasks.max is the upper bound; the connector\'s taskConfigs() method determines actual tasks, which may be less if the source has fewer parallelizable units',
      'tasks.max sets the number of worker threads per task',
      'tasks.max controls the number of Kafka partitions the connector creates',
    ],
    correctAnswer: 1,
    explanation: 'The connector\'s taskConfigs() returns a list of per-task configs; the framework creates min(taskConfigs.size(), tasks.max) tasks. A JDBC connector with 3 tables and tasks.max=10 creates only 3 tasks.',
    codeSnippet: `tasks.max=4  # up to 4 tasks; actual count depends on connector implementation`,
    tags: ['kafka-connect', 'tasks-max', 'parallelism'],
  },
  {
    id: 'kafka-connect-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'SMTs',
    question: 'What are the limitations of Single Message Transforms in Kafka Connect?',
    options: [
      'SMTs cannot modify field names or types',
      'SMTs are stateless and operate on single records; they cannot join data, aggregate, or access state from other records or external systems',
      'SMTs can only be applied to sink connectors, not source connectors',
      'At most one SMT can be chained per connector',
    ],
    correctAnswer: 1,
    explanation: 'SMTs are intentionally simple and stateless. For stateful transformations, joins, aggregations, or lookups against external systems, Kafka Streams or ksqlDB should be used instead.',
    tags: ['kafka-connect', 'smt', 'limitations'],
  },
  {
    id: 'kafka-connect-l3-008',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'dead letter queue',
    question: 'What is the difference between `errors.tolerance=none` and `errors.tolerance=all`?',
    options: [
      'none retries forever; all fails immediately on first error',
      'none causes the task to fail immediately on any error; all tolerates errors and routes failed records to the DLQ (if configured) or drops them',
      'all enables DLQ automatically; none disables DLQ',
      'They control the number of retry attempts: none=0, all=unlimited',
    ],
    correctAnswer: 1,
    explanation: 'With errors.tolerance=none (default), any processing error fails the task immediately. With all, errors are tolerated (logged or sent to DLQ) and the connector continues processing.',
    tags: ['kafka-connect', 'errors-tolerance', 'dlq'],
  },
  {
    id: 'kafka-connect-l3-009',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'converters',
    question: 'What is the `ByteArrayConverter` used for in Kafka Connect?',
    options: [
      'It converts all record values to byte arrays for generic storage',
      'It passes through raw bytes without any conversion, useful when the data is already in the desired serialization format',
      'It converts Java byte arrays to Base64 strings',
      'It is used internally by Kafka Connect for offset storage',
    ],
    correctAnswer: 1,
    explanation: 'ByteArrayConverter is a no-op converter: it writes bytes as-is to Kafka and reads them back without modification, useful for connectors where data is pre-serialized (e.g., binary protocols).',
    tags: ['kafka-connect', 'byte-array-converter', 'passthrough'],
  },
  {
    id: 'kafka-connect-l3-010',
    language: 'kafka',
    level: 'level3',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What is the "exactly-once source" support (KIP-618) for Kafka Connect source connectors?',
    options: [
      'Source connectors use transactions to atomically write records to Kafka and commit their offsets to the connect-offsets topic, ensuring no duplicates on restart',
      'Source connectors implement a two-phase commit with the source database',
      'Exactly-once for sources is achieved via idempotent writes and record deduplication',
      'Source connectors checkpoint state to S3 after every batch',
    ],
    correctAnswer: 0,
    explanation: 'KIP-618 wraps source connector record production and offset commit in a Kafka transaction: both are committed atomically, so on restart the connector resumes exactly from where it left off with no duplicate records.',
    tags: ['kafka-connect', 'exactly-once-source', 'kip-618'],
  },
];

export default questions;
