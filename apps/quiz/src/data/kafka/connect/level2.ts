import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-connect-l2-001',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'dead letter queue',
    question: 'What is a "dead letter queue" (DLQ) in Kafka Connect and when is it used?',
    options: [
      'A Kafka topic where connector configurations that fail validation are stored',
      'A Kafka topic where records that fail to be processed (e.g., deserialization errors) are routed instead of blocking the pipeline',
      'A queue for retrying failed connector REST API requests',
      'A ZooKeeper node storing failed connector offsets',
    ],
    correctAnswer: 1,
    explanation: 'DLQ allows Kafka Connect to continue processing by routing unprocessable records (e.g., schema mismatch, converter errors) to a separate topic for investigation rather than halting the task.',
    codeSnippet: `errors.tolerance=all\nerrors.deadletterqueue.topic.name=dlq-my-connector\nerrors.deadletterqueue.context.headers.enable=true`,
    tags: ['kafka-connect', 'dlq', 'error-handling'],
  },
  {
    id: 'kafka-connect-l2-002',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'SMTs',
    question: 'What is the `MaskField` SMT used for?',
    options: [
      'Encrypting sensitive fields before they are written to Kafka',
      'Replacing the value of specified fields with null or a type-appropriate zero value, useful for PII masking',
      'Removing fields from the schema definition only, keeping values intact',
      'Converting field values to a masked string like "***"',
    ],
    correctAnswer: 1,
    explanation: 'MaskField replaces field values with type-appropriate zeros (0 for numbers, empty string, null) without altering the schema, providing a simple way to redact sensitive data in the pipeline.',
    codeSnippet: `transforms=maskSensitive\ntransforms.maskSensitive.type=org.apache.kafka.connect.transforms.MaskField\$Value\ntransforms.maskSensitive.fields=password,token`,
    tags: ['kafka-connect', 'mask-field', 'smt'],
  },
  {
    id: 'kafka-connect-l2-003',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'distributed vs standalone mode',
    question: 'How does Kafka Connect distributed mode handle connector task rebalancing when a worker fails?',
    options: [
      'All connectors and tasks are restarted from scratch on the remaining workers',
      'The Connect group coordinator triggers a rebalance; surviving workers redistribute connector tasks among themselves using the Connect rebalancing protocol',
      'Tasks are moved to a hot-standby worker that was pre-assigned',
      'The failed worker\'s tasks are permanently stopped until it recovers',
    ],
    correctAnswer: 1,
    explanation: 'Connect workers form a group (like consumer groups); when a worker fails, the coordinator triggers rebalancing and tasks are redistributed to the remaining workers automatically.',
    tags: ['kafka-connect', 'worker-rebalance', 'fault-tolerance'],
  },
  {
    id: 'kafka-connect-l2-004',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'converters',
    question: 'What is the difference between `JsonConverter` with `schemas.enable=true` vs `schemas.enable=false`?',
    options: [
      'schemas.enable=true produces Avro; schemas.enable=false produces plain JSON',
      'schemas.enable=true embeds the Connect schema as a JSON object alongside the payload; schemas.enable=false produces a flat JSON payload without schema metadata',
      'schemas.enable controls whether Schema Registry is queried for validation',
      'schemas.enable=false disables all converters for that connector',
    ],
    correctAnswer: 1,
    explanation: 'With schemas.enable=true, the JSON message includes a "schema" and "payload" wrapper. With false, only the raw payload is serialized, which is simpler but loses schema information for downstream consumers.',
    codeSnippet: `# With schemas.enable=true:\n# {"schema": {"type": "struct", ...}, "payload": {"id": 1, "name": "Alice"}}\n# With schemas.enable=false:\n# {"id": 1, "name": "Alice"}`,
    tags: ['kafka-connect', 'json-converter', 'schema'],
  },
  {
    id: 'kafka-connect-l2-005',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What are the query modes available in the JDBC Source Connector?',
    options: [
      'full-scan and incremental',
      'bulk, incrementing, timestamp, and timestamp+incrementing',
      'poll, push, and cdc',
      'snapshot and streaming',
    ],
    correctAnswer: 1,
    explanation: 'Bulk re-reads the full table each poll; incrementing uses an auto-increment column; timestamp uses a timestamp column; timestamp+incrementing combines both for reliable deduplication.',
    codeSnippet: `mode=timestamp+incrementing\ntimestamp.column.name=updated_at\nincrementing.column.name=id`,
    tags: ['kafka-connect', 'jdbc-source', 'query-modes'],
  },
  {
    id: 'kafka-connect-l2-006',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'offset management',
    question: 'How does a source connector track its position in the external source system?',
    options: [
      'It uses the same __consumer_offsets topic as regular Kafka consumers',
      'The connector implementation calls context.offsetStorageReader() and context.offsetStorageWriter() to persist custom source offsets to the connect-offsets topic',
      'Offsets are stored in ZooKeeper using the connector\'s name as the path',
      'Kafka Connect does not track source offsets; connectors handle this themselves externally',
    ],
    correctAnswer: 1,
    explanation: 'Source connectors use the Connect framework\'s OffsetStorageWriter to persist custom offset maps (e.g., last read timestamp, file position, Binlog position) to the connect-offsets Kafka topic.',
    tags: ['kafka-connect', 'source-offsets', 'offset-storage'],
  },
  {
    id: 'kafka-connect-l2-007',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'SMTs',
    question: 'What does the `TimestampConverter` SMT do?',
    options: [
      'Inserts the current wall-clock timestamp into each record',
      'Converts timestamp fields between different formats (Unix milliseconds, ISO 8601 string, Date) within a record',
      'Adds a timestamp to the Kafka record headers',
      'Sets the Kafka record\'s event-time timestamp from a field value',
    ],
    correctAnswer: 1,
    explanation: 'TimestampConverter transforms timestamp fields between representations (string, epoch ms, Date), resolving incompatibilities between source system timestamp formats and target system expectations.',
    codeSnippet: `transforms=ts\ntransforms.ts.type=org.apache.kafka.connect.transforms.TimestampConverter\$Value\ntransforms.ts.field=created_at\ntransforms.ts.target.type=string\ntransforms.ts.format=yyyy-MM-dd HH:mm:ss`,
    tags: ['kafka-connect', 'timestamp-converter', 'smt'],
  },
  {
    id: 'kafka-connect-l2-008',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What does the Elasticsearch Sink Connector use as the Elasticsearch document ID by default?',
    options: [
      'A UUID generated by the connector for each record',
      'The Kafka record offset',
      'The Kafka record key (if non-null), otherwise the topic+partition+offset',
      'A hash of the record value',
    ],
    correctAnswer: 2,
    explanation: 'Using the record key as the document ID enables upsert behavior: multiple records with the same key update the same document in Elasticsearch, which is ideal for CDC or KTable-like use cases.',
    tags: ['kafka-connect', 'elasticsearch-sink', 'document-id'],
  },
  {
    id: 'kafka-connect-l2-009',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'distributed vs standalone mode',
    question: 'What is the `connect-configs` topic in Kafka Connect distributed mode?',
    options: [
      'A topic that stores worker configuration (bootstrap.servers, group.id)',
      'A compacted topic that stores connector and task configurations, enabling all workers to share and restore config after restart',
      'A topic for connector-to-connector communication',
      'A topic where schema definitions are stored for converters',
    ],
    correctAnswer: 1,
    explanation: 'The connect-configs topic (compacted) stores all connector configurations; when a new worker joins, it reads this topic to learn about all active connectors and tasks.',
    tags: ['kafka-connect', 'connect-configs', 'distributed'],
  },
  {
    id: 'kafka-connect-l2-010',
    language: 'kafka',
    level: 'level2',
    category: 'Kafka Connect',
    subcategory: 'dead letter queue',
    question: 'What information is added to DLQ record headers when `errors.deadletterqueue.context.headers.enable=true`?',
    options: [
      'Only the original topic and partition where the error occurred',
      'Error class, error message, stack trace, original topic/partition/offset, and the connector/task that produced the error',
      'Only the timestamp of when the error occurred',
      'The schema ID that caused the deserialization failure',
    ],
    correctAnswer: 1,
    explanation: 'Enriched DLQ headers include the full error context (exception class, message, stack trace) alongside source coordinates, making it much easier to diagnose why a specific record failed.',
    tags: ['kafka-connect', 'dlq', 'error-headers'],
  },
];

export default questions;
