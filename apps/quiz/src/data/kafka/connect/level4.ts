import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-connect-l4-001',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What is the "Outbox Pattern" in event-driven architecture and how does Debezium implement it?',
    options: [
      'It uses an outbox Kafka topic as a buffer between services; Debezium reads from this topic',
      'Debezium monitors a dedicated outbox table in the database; application code writes events atomically with business data; Debezium captures inserts and routes them to Kafka, ensuring no lost events even without distributed transactions',
      'The outbox pattern requires Kafka Streams to transform Debezium events before delivery',
      'It is a Kafka Connect SMT that routes records to different topics based on an outbox header',
    ],
    correctAnswer: 1,
    explanation: 'The Outbox Pattern avoids dual writes: the application writes to the outbox table in the same DB transaction as the business operation; Debezium captures changes via CDC, providing reliable event delivery without a distributed transaction.',
    codeSnippet: `# Debezium Outbox SMT config\ntransforms=outbox\ntransforms.outbox.type=io.debezium.transforms.outbox.EventRouter\ntransforms.outbox.table.field.event.key=aggregateid`,
    tags: ['kafka-connect', 'outbox-pattern', 'debezium'],
  },
  {
    id: 'kafka-connect-l4-002',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'SMTs',
    question: 'How would you implement a custom SMT in Kafka Connect?',
    options: [
      'By extending the AbstractTransformation class and overriding the transform() method',
      'By implementing the Transformation<R extends ConnectRecord<R>> interface, providing apply(R record) and config() methods',
      'By writing a Kafka Streams topology that processes records from the connector\'s output topic',
      'By implementing the ConnectorTransform interface and registering it in the connector config',
    ],
    correctAnswer: 1,
    explanation: 'Custom SMTs implement Transformation<R> where R is SourceRecord or SinkRecord; apply() transforms the record, config() defines the ConfigDef, and the class is registered as a plugin on the classpath.',
    codeSnippet: `public class RedactEmail<R extends ConnectRecord<R>> implements Transformation<R> {\n  public R apply(R record) {\n    // transform record here\n    return record.newRecord(...);\n  }\n  public ConfigDef config() { return new ConfigDef(); }\n  public void close() {}\n}`,
    tags: ['kafka-connect', 'custom-smt', 'transformation'],
  },
  {
    id: 'kafka-connect-l4-003',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'distributed vs standalone mode',
    question: 'What is the `connector.client.config.override.policy` setting and why is it important in multi-tenant Connect deployments?',
    options: [
      'It controls whether connectors can override their own task count',
      'It restricts which producer/consumer configs individual connector configurations are allowed to override (e.g., preventing security downgrade), protecting the cluster in shared environments',
      'It determines how connector configs are synchronized across workers',
      'It controls whether connectors can override the converter settings set at the worker level',
    ],
    correctAnswer: 1,
    explanation: 'In multi-tenant environments, operators use this policy to prevent connectors from overriding security-sensitive configs (e.g., ssl.truststore, sasl settings), enforcing cluster-wide security standards.',
    codeSnippet: `# worker config\nconnector.client.config.override.policy=All    # allow all overrides\nconnector.client.config.override.policy=None   # block all overrides (most secure)`,
    tags: ['kafka-connect', 'security', 'multi-tenant'],
  },
  {
    id: 'kafka-connect-l4-004',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'offset management',
    question: 'What is the implication of using `offset.flush.interval.ms` in Kafka Connect and how does it affect delivery semantics?',
    options: [
      'Longer intervals reduce at-least-once redelivery window; shorter intervals increase broker load',
      'Longer flush intervals mean more records may be reprocessed on task restart (larger redelivery window), while shorter intervals increase the frequency of offset commits (more broker overhead)',
      'offset.flush.interval.ms controls source connector polling rate, not offset commits',
      'It only applies to sink connectors; source offsets are committed per-record',
    ],
    correctAnswer: 1,
    explanation: 'Kafka Connect flushes (commits) sink and source offsets at this interval. A longer interval means more records since the last commit must be reprocessed on restart — the trade-off between overhead and redelivery risk.',
    codeSnippet: `# worker config\noffset.flush.interval.ms=10000  # commit every 10 seconds`,
    tags: ['kafka-connect', 'offset-flush', 'delivery-semantics'],
  },
  {
    id: 'kafka-connect-l4-005',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'source/sink connectors',
    question: 'What is the "schema change topic" in Debezium and what type of events does it contain?',
    options: [
      'A topic containing Schema Registry registration events',
      'A topic where Debezium publishes DDL changes (ALTER TABLE, CREATE TABLE) captured from the database transaction log, used to track schema evolution',
      'A topic where Debezium stores its internal connector state',
      'A topic containing record schema validation failures',
    ],
    correctAnswer: 1,
    explanation: 'Debezium publishes DDL statements to a schema history topic (e.g., database schema history), enabling full table schema reconstruction and tracking structural changes over time.',
    codeSnippet: `schema.history.internal.kafka.topic=dbhistory.mydb\nschema.history.internal.kafka.bootstrap.servers=kafka:9092`,
    tags: ['kafka-connect', 'debezium', 'schema-history'],
  },
  {
    id: 'kafka-connect-l4-006',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'converters',
    question: 'What is the difference between a Connect schema and an Avro schema, and how does AvroConverter bridge them?',
    options: [
      'They are identical; AvroConverter is just a wrapper',
      'Connect schema is Kafka Connect\'s internal type system (Struct, Map, Array); AvroConverter translates between Connect\'s schema representation and Avro\'s schema format, registering with Schema Registry as needed',
      'Connect schemas are for keys; Avro schemas are for values only',
      'AvroConverter ignores the Connect schema and uses Avro schema exclusively',
    ],
    correctAnswer: 1,
    explanation: 'Connect has its own Schema object model. AvroConverter maps Connect types to Avro types bidirectionally, registers the resulting Avro schema with Schema Registry, and embeds the schema ID in the message wire format.',
    tags: ['kafka-connect', 'avro-converter', 'connect-schema'],
  },
  {
    id: 'kafka-connect-l4-007',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'dead letter queue',
    question: 'What is the risk of a high-throughput pipeline with a DLQ and `errors.tolerance=all`, and how do you mitigate it?',
    options: [
      'The DLQ topic fills up, causing out-of-memory on the broker',
      'Silent data loss: if the DLQ itself is unavailable or full, failed records are dropped without alerting; monitor DLQ lag, set DLQ topic retention, and alert on DLQ record production rate',
      'errors.tolerance=all disables retry logic, causing unnecessary data loss',
      'There is no risk; DLQ provides complete fault isolation',
    ],
    correctAnswer: 1,
    explanation: 'DLQ routing masks errors in metrics. You must monitor the DLQ\'s offset growth, set up alerting on DLQ message rates, and regularly audit/reprocess DLQ records to prevent silent ongoing data loss.',
    tags: ['kafka-connect', 'dlq', 'observability'],
  },
  {
    id: 'kafka-connect-l4-008',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'distributed vs standalone mode',
    question: 'How does Kafka Connect handle connector configuration updates (e.g., changing a topic name) in distributed mode without downtime?',
    options: [
      'Configuration changes require stopping and restarting the entire Connect cluster',
      'A PUT request to the connector\'s REST endpoint triggers a graceful task restart: the coordinator publishes the new config to connect-configs, triggers a rebalance, and tasks restart with the new config',
      'Configuration is read from file; changing the file and sending SIGHUP reloads it',
      'Only additive changes (adding new connectors) are supported without downtime',
    ],
    correctAnswer: 1,
    explanation: 'Updating a connector via the REST API atomically publishes the new config to connect-configs, causing all workers to detect the change and restart the affected tasks with the updated configuration.',
    codeSnippet: `curl -X PUT http://connect:8083/connectors/my-connector/config \\\n  -H "Content-Type: application/json" \\\n  -d '{"connector.class": "...", "topics": "new-topic", ...}'`,
    tags: ['kafka-connect', 'config-update', 'zero-downtime'],
  },
  {
    id: 'kafka-connect-l4-009',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'SMTs',
    question: 'What is the `Filter` SMT and what is an important limitation to be aware of?',
    options: [
      'Filter drops records whose key matches a regex; it cannot filter on value fields',
      'Filter conditionally drops records based on a predicate; it requires pairing with a Predicate implementation, but it cannot be used to selectively route records to different topics — only to drop them',
      'Filter buffers records and emits them in sorted order',
      'Filter is a sink SMT only; it cannot be used on source connectors',
    ],
    correctAnswer: 1,
    explanation: 'The Filter SMT drops records matching a predicate. To route to different topics you need multiple connectors or a topic.creation config. Filter is all-or-nothing per matched record.',
    codeSnippet: `transforms=filterNulls\ntransforms.filterNulls.type=org.apache.kafka.connect.transforms.Filter\ntransforms.filterNulls.predicate=isNull\npredicates=isNull\npredicates.isNull.type=org.apache.kafka.connect.transforms.predicates.IsNull\$Value`,
    tags: ['kafka-connect', 'filter-smt', 'predicate'],
  },
  {
    id: 'kafka-connect-l4-010',
    language: 'kafka',
    level: 'level4',
    category: 'Kafka Connect',
    subcategory: 'offset management',
    question: 'What happens when a Debezium MySQL connector loses its position in the MySQL Binlog (e.g., the Binlog was purged)?',
    options: [
      'The connector automatically falls back to a full table snapshot',
      'The connector enters a FAILED state; recovery requires deciding between a new snapshot (snapshot.mode=initial) to re-read all data, or setting a known safe binlog position manually',
      'Debezium automatically retrieves the missing binlog events from a backup',
      'The connector continues from the beginning of the earliest available binlog',
    ],
    correctAnswer: 1,
    explanation: 'Binlog position loss is a critical failure. The connector must be reset and a new snapshot taken, or binlog_expire_logs_seconds must be tuned to retain logs longer than the maximum allowable connector downtime.',
    tags: ['kafka-connect', 'debezium', 'binlog-position'],
  },
];

export default questions;
