import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kafka-security-l3-001',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'SASL/GSSAPI',
    question: 'In a Kerberos-secured Kafka cluster, what file maps Kerberos principals to Kafka principals?',
    options: [
      'krb5.conf',
      'kafka_server_jaas.conf combined with sasl.kerberos.principal.to.local.rules',
      'ssl-mapping.properties',
      'principal-mapping.yaml',
    ],
    correctAnswer: 1,
    explanation: 'The JAAS config provides the service principal and keytab, while `sasl.kerberos.principal.to.local.rules` (a Hadoop-style rule set) maps full Kerberos principals like `alice/host@REALM` to short names.',
    codeSnippet: `sasl.kerberos.principal.to.local.rules=\\
  RULE:[1:\$1@\$0](.*@EXAMPLE\\.COM)s/@.*//,DEFAULT`,
    tags: ['kafka', 'kerberos', 'gssapi', 'principal-mapping'],
  },
  {
    id: 'kafka-security-l3-002',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'Authorization',
    question: 'What is the effect of setting `allow.everyone.if.no.acl.found=true` on a broker?',
    options: [
      'All operations are denied when no ACL exists for a resource',
      'All authenticated users are allowed access to resources that have no ACL defined',
      'Only super users can access resources without ACLs',
      'Consumers skip offset commits when ACLs are missing',
    ],
    correctAnswer: 1,
    explanation: 'When `allow.everyone.if.no.acl.found=true`, Kafka grants access by default if no matching ACL is found — useful during migration but dangerous in production.',
    tags: ['kafka', 'acl', 'authorization', 'security-config'],
  },
  {
    id: 'kafka-security-l3-003',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'SSL/TLS',
    question: 'How can you restrict Kafka to accept only TLS 1.2 and 1.3?',
    options: [
      'Set `ssl.protocol=TLSv1.2` only',
      'Set `ssl.enabled.protocols=TLSv1.2,TLSv1.3` in broker config',
      'Set `tls.min.version=1.2` in server.properties',
      'Disable SASL which forces TLS upgrade',
    ],
    correctAnswer: 1,
    explanation: '`ssl.enabled.protocols` lists the acceptable TLS protocol versions; removing TLSv1 and TLSv1.1 from this list prevents older, insecure connections.',
    codeSnippet: `ssl.enabled.protocols=TLSv1.2,TLSv1.3
ssl.protocol=TLSv1.3`,
    tags: ['kafka', 'ssl', 'tls-version', 'hardening'],
  },
  {
    id: 'kafka-security-l3-004',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'Encryption at Rest',
    question: 'Kafka does not natively encrypt data at rest. What is the recommended approach for encrypting Kafka log data on disk?',
    options: [
      'Enable `log.encryption=true` in broker configuration',
      'Use OS-level or filesystem-level encryption such as dm-crypt/LUKS',
      'Configure `compression.type=encrypted` on topics',
      'Use KRaft mode, which adds native at-rest encryption',
    ],
    correctAnswer: 1,
    explanation: 'Kafka has no built-in at-rest encryption; operators rely on OS-level disk encryption (dm-crypt/LUKS, AWS EBS encryption, etc.) to protect log segment files.',
    tags: ['kafka', 'encryption-at-rest', 'security', 'disk'],
  },
  {
    id: 'kafka-security-l3-005',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'SASL/OAUTHBEARER',
    question: 'What is the role of `unsecuredLoginStringClaim_sub` in the default SASL/OAUTHBEARER implementation?',
    options: [
      'It defines the OAuth scopes required for producing to a topic',
      'It sets the `sub` claim in the generated unsecured JWT used for testing',
      'It specifies the OAuth token endpoint URL',
      'It names the consumer group in the JWT payload',
    ],
    correctAnswer: 1,
    explanation: 'The built-in `OAuthBearerUnsecuredLoginCallbackHandler` is for testing only; `unsecuredLoginStringClaim_sub` sets the subject claim in the self-signed JWT it generates.',
    codeSnippet: `sasl.jaas.config=org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginModule required \\
  unsecuredLoginStringClaim_sub="alice";`,
    tags: ['kafka', 'oauth', 'oauthbearer', 'jwt'],
  },
  {
    id: 'kafka-security-l3-006',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'ACLs',
    question: 'What is a Kafka "super user" and how is it configured?',
    options: [
      'A user that bypasses all ACL checks; configured via `super.users=User:admin` in broker config',
      'A user with Read+Write ACLs on all topics',
      'A ZooKeeper admin account used during cluster initialization',
      'A user authenticated via Kerberos that gets elevated privileges automatically',
    ],
    correctAnswer: 0,
    explanation: 'Super users are listed in `super.users` on the broker and are exempt from all ACL enforcement, making them useful for administrative clients.',
    codeSnippet: `# server.properties
super.users=User:admin;User:kafka-broker`,
    tags: ['kafka', 'acl', 'super-user', 'authorization'],
  },
  {
    id: 'kafka-security-l3-007',
    language: 'kafka',
    level: 'level3',
    category: 'Security',
    subcategory: 'Inter-broker Security',
    question: 'When using `SASL_SSL` for inter-broker communication, what two security layers are combined?',
    options: [
      'SASL authentication and ACL authorization',
      'SSL/TLS encryption and SASL authentication',
      'Kerberos and OAuth token validation',
      'Mutual TLS and digest authentication',
    ],
    correctAnswer: 1,
    explanation: '`SASL_SSL` means the connection is first encrypted with TLS, then the client authenticates using a SASL mechanism (e.g., GSSAPI or SCRAM) over that encrypted channel.',
    tags: ['kafka', 'sasl-ssl', 'inter-broker', 'security'],
  },
];

export default questions;
