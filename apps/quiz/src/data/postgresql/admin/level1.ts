import { Question } from '@/types/question';

const questions: Question[] = [
  { id: 'pg-admin-l1-001', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you connect to a PostgreSQL database using psql?', options: ['psql -d database_name -U username -h hostname', 'mysql -u username database_name', 'sqlplus username/password@database', 'mongo database_name'], correctAnswer: 0, explanation: 'psql is the PostgreSQL interactive terminal. The -d flag specifies the database, -U the username, and -h the host. By default, psql connects to localhost on port 5432 as the current OS user.', codeSnippet: "# Connect to a local database:\npsql -d mydb -U postgres\n\n# Connect to a remote database:\npsql -d mydb -U admin -h db.example.com -p 5432\n\n# Connect using a connection string:\npsql 'postgresql://admin:secret@db.example.com:5432/mydb'", tags: ['admin', 'psql', 'connection'] },
  { id: 'pg-admin-l1-002', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does the \\l meta-command do in psql?', options: ['Lists all tables', 'Lists all users', 'Lists all databases on the PostgreSQL server', 'Lists all schemas'], correctAnswer: 2, explanation: '\\l (or \\list) displays all databases on the server, including their owners, encoding, collation, and access privileges. It is equivalent to querying the pg_database system catalog.', codeSnippet: "-- In psql:\n\\l\n\n-- Equivalent SQL:\nSELECT datname, datdba::regrole, encoding,\n       datcollate, datacl\nFROM pg_database\nORDER BY datname;", tags: ['admin', 'psql', 'meta-commands'] },
  { id: 'pg-admin-l1-003', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you list all tables in the current database using psql?', options: ['SHOW TABLES', '\\dt', 'LIST TABLES', 'SELECT TABLES'], correctAnswer: 1, explanation: '\\dt lists all tables in the current schema (usually public). Use \\dt *.* to see tables in all schemas. \\dt+ shows additional information like size and description.', codeSnippet: "-- List tables in current schema:\n\\dt\n\n-- List tables in all schemas:\n\\dt *.*\n\n-- List tables with extra info (size, description):\n\\dt+\n\n-- Filter by pattern:\n\\dt user*", tags: ['admin', 'psql', 'tables'] },
  { id: 'pg-admin-l1-004', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you create a new database in PostgreSQL?', options: ['NEW DATABASE mydb', 'CREATE DATABASE mydb;', 'ADD DATABASE mydb', 'MAKE DATABASE mydb'], correctAnswer: 1, explanation: 'CREATE DATABASE creates a new database. By default, it copies the template1 database. You can specify owner, encoding, locale, and template with additional options.', codeSnippet: "-- Basic creation:\nCREATE DATABASE myapp;\n\n-- With options:\nCREATE DATABASE myapp\n  OWNER = appuser\n  ENCODING = 'UTF8'\n  LC_COLLATE = 'en_US.UTF-8'\n  LC_CTYPE = 'en_US.UTF-8'\n  TEMPLATE = template0;", tags: ['admin', 'create-database'] },
  { id: 'pg-admin-l1-005', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you drop (delete) a database?', options: ['DELETE DATABASE mydb', 'REMOVE DATABASE mydb', 'DROP DATABASE mydb;', 'DESTROY DATABASE mydb'], correctAnswer: 2, explanation: 'DROP DATABASE permanently removes the database and all its objects. It cannot be undone. No one can be connected to the target database when you drop it. Use IF EXISTS to avoid errors if the database does not exist.', codeSnippet: "-- Drop a database:\nDROP DATABASE mydb;\n\n-- Drop only if it exists:\nDROP DATABASE IF EXISTS mydb;\n\n-- Force disconnect all users first (PG 13+):\nDROP DATABASE mydb WITH (FORCE);", tags: ['admin', 'drop-database'] },
  { id: 'pg-admin-l1-006', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you switch to a different database in psql?', options: ['USE mydb;', '\\c mydb', 'SWITCH TO mydb', 'CONNECT mydb'], correctAnswer: 1, explanation: '\\c (or \\connect) switches to a different database. You can also specify a different user and host. This closes the current connection and opens a new one.', codeSnippet: "-- Switch to another database:\n\\c myotherdb\n\n-- Switch database and user:\n\\c myotherdb admin\n\n-- Switch to a remote database:\n\\c myotherdb admin db.example.com 5432", tags: ['admin', 'psql', 'connect'] },
  { id: 'pg-admin-l1-007', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What is the difference between a user and a role in PostgreSQL?', options: ['Users can only read data, roles can write', 'There is no difference — CREATE USER is an alias for CREATE ROLE WITH LOGIN; both create roles, but CREATE USER adds the LOGIN privilege by default', 'Roles are groups, users are individuals', 'Users are deprecated in favor of roles'], correctAnswer: 1, explanation: 'In PostgreSQL, users and roles are the same thing. The only difference is that CREATE USER adds the LOGIN attribute by default, while CREATE ROLE does not. All access control is role-based.', codeSnippet: "-- These are equivalent:\nCREATE USER alice WITH PASSWORD 'secret';\nCREATE ROLE alice WITH LOGIN PASSWORD 'secret';\n\n-- Role without login (for grouping):\nCREATE ROLE readonly;\n\n-- List all roles:\n\\du", tags: ['admin', 'roles', 'users'] },
  { id: 'pg-admin-l1-008', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'How do you create a new user (role with login) in PostgreSQL?', options: ['ADD USER alice', 'INSERT INTO users VALUES (alice)', 'CREATE ROLE alice WITH LOGIN PASSWORD \'secret\';', 'NEW USER alice'], correctAnswer: 2, explanation: 'CREATE ROLE with LOGIN creates a role that can connect to the database. The PASSWORD attribute sets the authentication password. You can also use CREATE USER which adds LOGIN by default.', codeSnippet: "-- Create a login role:\nCREATE ROLE alice WITH LOGIN PASSWORD 'secret';\n\n-- Or using CREATE USER shorthand:\nCREATE USER bob WITH PASSWORD 'secret';\n\n-- With additional attributes:\nCREATE ROLE admin WITH\n  LOGIN PASSWORD 'admin_pass'\n  CREATEDB CREATEROLE;", tags: ['admin', 'roles', 'create-user'] },
  { id: 'pg-admin-l1-009', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'How do you drop a role in PostgreSQL?', options: ['DELETE ROLE alice', 'DROP ROLE alice;', 'REMOVE ROLE alice', 'REVOKE ROLE alice'], correctAnswer: 1, explanation: 'DROP ROLE removes a role from the database cluster. The role must not own any database objects and must not have any granted privileges, or the DROP will fail. Use REASSIGN OWNED and DROP OWNED first to clean up.', codeSnippet: "-- Drop a role:\nDROP ROLE alice;\n\n-- Drop only if exists:\nDROP ROLE IF EXISTS alice;\n\n-- If role owns objects, first:\nREASSIGN OWNED BY alice TO postgres;\nDROP OWNED BY alice;\nDROP ROLE alice;", tags: ['admin', 'roles', 'drop-role'] },
  { id: 'pg-admin-l1-010', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What does the GRANT command do?', options: ['Creates a new user', 'Gives specific privileges (SELECT, INSERT, UPDATE, DELETE, etc.) on database objects to a role', 'Backs up the database', 'Locks a table'], correctAnswer: 1, explanation: 'GRANT assigns privileges on database objects (tables, schemas, sequences, functions, etc.) to roles. Privileges include SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, CREATE, CONNECT, USAGE, EXECUTE, and ALL PRIVILEGES.', codeSnippet: "-- Grant SELECT on a table:\nGRANT SELECT ON employees TO alice;\n\n-- Grant multiple privileges:\nGRANT SELECT, INSERT, UPDATE ON orders TO app_user;\n\n-- Grant on all tables in schema:\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly;\n\n-- Grant all privileges:\nGRANT ALL PRIVILEGES ON employees TO admin;", tags: ['admin', 'privileges', 'grant'] },
  { id: 'pg-admin-l1-011', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'How do you remove privileges from a role?', options: ['REMOVE privileges', 'DELETE privileges', 'REVOKE SELECT ON table FROM role;', 'UNGRANT privileges'], correctAnswer: 2, explanation: 'REVOKE removes previously granted privileges. The syntax mirrors GRANT but uses FROM instead of TO. You can revoke specific privileges or ALL PRIVILEGES.', codeSnippet: "-- Revoke SELECT:\nREVOKE SELECT ON employees FROM alice;\n\n-- Revoke multiple privileges:\nREVOKE INSERT, UPDATE ON orders FROM app_user;\n\n-- Revoke all privileges:\nREVOKE ALL PRIVILEGES ON employees FROM temp_user;\n\n-- Revoke from all tables in schema:\nREVOKE ALL ON ALL TABLES IN SCHEMA public FROM readonly;", tags: ['admin', 'privileges', 'revoke'] },
  { id: 'pg-admin-l1-012', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'What is pg_dump used for?', options: ['Monitoring database performance', 'Creating a backup (dump) of a single PostgreSQL database in SQL or custom format', 'Replicating databases', 'Compressing database files'], correctAnswer: 1, explanation: 'pg_dump extracts a PostgreSQL database into a script file or archive file. It creates consistent backups even while the database is in use. It can output in plain SQL, custom, directory, or tar formats.', codeSnippet: "# Plain SQL dump:\npg_dump mydb > backup.sql\n\n# Custom format (compressed, supports parallel restore):\npg_dump -Fc mydb > backup.dump\n\n# Directory format (parallel dump):\npg_dump -Fd -j 4 mydb -f backup_dir/\n\n# Dump a specific table:\npg_dump -t orders mydb > orders.sql", tags: ['admin', 'backup', 'pg-dump'] },
  { id: 'pg-admin-l1-013', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you restore a database from a plain SQL dump?', options: ['pg_restore backup.sql', 'RESTORE DATABASE FROM backup.sql', 'psql mydb < backup.sql', 'LOAD backup.sql'], correctAnswer: 2, explanation: 'Plain SQL dumps produced by pg_dump are restored using psql, which reads and executes the SQL statements. For custom/directory/tar format dumps, use pg_restore instead.', codeSnippet: "# Restore plain SQL dump:\npsql mydb < backup.sql\n\n# Or using -f flag:\npsql -d mydb -f backup.sql\n\n# For custom format dumps, use pg_restore:\npg_restore -d mydb backup.dump\n\n# Create database and restore:\ncreatedb mydb_restored\npsql mydb_restored < backup.sql", tags: ['admin', 'backup', 'restore'] },
  { id: 'pg-admin-l1-014', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does \\d table_name do in psql?', options: ['Drops the table', 'Deletes all data from the table', 'Describes the table structure — shows columns, their types, constraints, and indexes', 'Duplicates the table'], correctAnswer: 2, explanation: '\\d tablename displays the table definition including column names, data types, collation, nullable status, and default values. It also shows indexes, foreign keys, check constraints, and triggers. Use \\d+ for additional storage and statistics information.', codeSnippet: "-- Describe a table:\n\\d employees\n\n-- With extended information (size, storage):\n\\d+ employees\n\n-- Describe an index:\n\\d idx_employees_name\n\n-- Describe a view:\n\\d active_employees_view", tags: ['admin', 'psql', 'describe'] },
  { id: 'pg-admin-l1-015', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'How do you list all roles in the PostgreSQL cluster?', options: ['SHOW ROLES', 'SELECT * FROM roles', '\\du in psql or SELECT rolname FROM pg_roles;', 'LIST ROLES'], correctAnswer: 2, explanation: '\\du (describe users) in psql lists all roles with their attributes. Alternatively, query pg_roles or pg_authid (superuser only) for programmatic access. pg_roles is a view of pg_authid that hides passwords.', codeSnippet: "-- In psql:\n\\du\n\n-- SQL query:\nSELECT rolname, rolsuper, rolcreatedb,\n       rolcreaterole, rolcanlogin\nFROM pg_roles\nORDER BY rolname;", tags: ['admin', 'roles', 'list'] },
  { id: 'pg-admin-l1-016', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What does GRANT USAGE ON SCHEMA public TO alice mean?', options: ['Gives alice ownership of the schema', 'Allows alice to access objects within the public schema — without USAGE, she cannot see or use any objects in the schema even if she has table-level privileges', 'Gives alice all privileges on the schema', 'Allows alice to drop the schema'], correctAnswer: 1, explanation: 'USAGE on a schema allows a role to look up objects within the schema. It does not grant any privileges on the objects themselves — those must be granted separately. Without USAGE on the schema, table-level grants are ineffective because the role cannot find the tables.', codeSnippet: "-- Allow access to the schema:\nGRANT USAGE ON SCHEMA public TO alice;\n\n-- Then grant table access:\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO alice;\n\n-- For new tables created in the future:\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT SELECT ON TABLES TO alice;", tags: ['admin', 'privileges', 'schema', 'usage'] },
  { id: 'pg-admin-l1-017', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'What is the template1 database?', options: ['A sample database with example data', 'A read-only archive', 'The default template used when creating new databases — new databases are copies of template1', 'A backup of all databases'], correctAnswer: 2, explanation: 'When you CREATE DATABASE, PostgreSQL copies template1 to create the new database. Any objects or extensions installed in template1 will be present in all new databases. template0 is a pristine, unmodified template that should not be changed.', codeSnippet: "-- Create a database (copies template1):\nCREATE DATABASE myapp;\n\n-- Use template0 for a clean database:\nCREATE DATABASE clean_db TEMPLATE template0;\n\n-- List templates:\nSELECT datname, datistemplate\nFROM pg_database\nWHERE datistemplate;", tags: ['admin', 'database', 'template'] },
  { id: 'pg-admin-l1-018', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you execute a SQL file from within psql?', options: ['RUN file.sql', 'EXEC file.sql', '\\i file.sql', 'SOURCE file.sql'], correctAnswer: 2, explanation: '\\i (include) reads and executes SQL commands from a file. The file path can be absolute or relative to where psql was started. Use \\ir for relative to the current file (useful for nested includes).', codeSnippet: "-- Execute a SQL file:\n\\i /path/to/script.sql\n\n-- Relative path:\n\\i ./migrations/001_create_tables.sql\n\n-- Include relative to current script:\n\\ir ./helpers.sql\n\n-- From command line:\npsql -d mydb -f script.sql", tags: ['admin', 'psql', 'execute-file'] },
  { id: 'pg-admin-l1-019', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What does the SUPERUSER attribute mean for a role?', options: ['The role can only read data', 'The role bypasses all permission checks and has unrestricted access to the entire database cluster', 'The role can create databases only', 'The role can only manage other users'], correctAnswer: 1, explanation: 'A superuser role bypasses all access permission checks except the right to log in. It can do anything in the database cluster: create/drop databases, modify system catalogs, bypass RLS, etc. Use superuser access sparingly for security reasons.', codeSnippet: "-- Create a superuser:\nCREATE ROLE admin WITH SUPERUSER LOGIN PASSWORD 'strong_pass';\n\n-- Check if current user is superuser:\nSELECT current_user, usesuper\nFROM pg_user\nWHERE usename = current_user;\n\n-- Remove superuser privilege:\nALTER ROLE admin NOSUPERUSER;", tags: ['admin', 'roles', 'superuser'] },
  { id: 'pg-admin-l1-020', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'How do you grant a role to another role (role membership)?', options: ['GRANT readonly TO alice;', 'ADD alice TO readonly', 'ASSIGN ROLE readonly alice', 'SET ROLE alice readonly'], correctAnswer: 0, explanation: 'GRANT role TO role adds a role as a member of another role, giving it all the privileges of the granted role. This is the mechanism for implementing role-based access control with group roles.', codeSnippet: "-- Create a group role:\nCREATE ROLE readonly;\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly;\n\n-- Add users to the group:\nGRANT readonly TO alice;\nGRANT readonly TO bob;\n\n-- Alice and bob now inherit readonly privileges\n-- Remove membership:\nREVOKE readonly FROM alice;", tags: ['admin', 'privileges', 'role-membership'] },
  { id: 'pg-admin-l1-021', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you quit psql?', options: ['EXIT', '\\q', 'QUIT', 'Both \\q and typing quit/exit will exit psql — \\q is the standard meta-command'], correctAnswer: 3, explanation: 'The standard way to exit psql is \\q. Recent versions of psql also accept quit and exit as convenience aliases. Ctrl+D (EOF) also exits psql.', tags: ['admin', 'psql', 'quit'] },
  { id: 'pg-admin-l1-022', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you rename a database?', options: ['RENAME DATABASE old_name TO new_name', 'ALTER DATABASE old_name RENAME TO new_name;', 'UPDATE pg_database SET datname = new_name', 'You cannot rename a database'], correctAnswer: 1, explanation: 'ALTER DATABASE ... RENAME TO renames a database. No one can be connected to the database being renamed (except the connection performing the rename). You must have CREATEDB privilege or be a superuser.', codeSnippet: "-- Rename a database:\nALTER DATABASE myapp RENAME TO myapp_v2;\n\n-- Must disconnect all other sessions first:\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE datname = 'myapp' AND pid <> pg_backend_pid();", tags: ['admin', 'database', 'rename'] },
  { id: 'pg-admin-l1-023', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What does the CREATEDB attribute allow a role to do?', options: ['Create tables', 'Create schemas', 'Create new databases', 'Create indexes'], correctAnswer: 2, explanation: 'The CREATEDB attribute allows a role to create new databases. Without it, only superusers can create databases. This is useful for application administrators who need to manage databases without full superuser access.', codeSnippet: "-- Create a role that can create databases:\nCREATE ROLE dbadmin WITH LOGIN CREATEDB PASSWORD 'pass';\n\n-- Grant CREATEDB to existing role:\nALTER ROLE alice CREATEDB;\n\n-- Remove CREATEDB:\nALTER ROLE alice NOCREATEDB;", tags: ['admin', 'roles', 'createdb'] },
  { id: 'pg-admin-l1-024', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'What is the difference between pg_dump and pg_dumpall?', options: ['pg_dump is faster', 'pg_dump backs up a single database; pg_dumpall backs up all databases in the cluster plus global objects (roles, tablespaces)', 'pg_dumpall is deprecated', 'They produce different file formats'], correctAnswer: 1, explanation: 'pg_dump exports one database at a time. pg_dumpall exports all databases plus cluster-wide objects like roles, tablespaces, and role memberships. pg_dumpall only produces plain SQL format, while pg_dump supports multiple formats.', codeSnippet: "# Dump a single database:\npg_dump mydb > mydb_backup.sql\n\n# Dump all databases and global objects:\npg_dumpall > full_cluster_backup.sql\n\n# Dump only global objects (roles, tablespaces):\npg_dumpall --globals-only > globals.sql\n\n# Dump only roles:\npg_dumpall --roles-only > roles.sql", tags: ['admin', 'backup', 'pg-dump', 'pg-dumpall'] },
  { id: 'pg-admin-l1-025', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you show the current database and user in psql?', options: ['SHOW DATABASE', '\\conninfo displays the current connection information including database, user, host, and port', 'SELECT database()', 'SHOW CONNECTION'], correctAnswer: 1, explanation: '\\conninfo shows the current connection details. You can also use SELECT current_database() for the database name and SELECT current_user for the user. The psql prompt typically shows the database name.', codeSnippet: "-- Connection info:\n\\conninfo\n-- You are connected to database \"mydb\" as user \"postgres\"\n-- via socket in \"/var/run/postgresql\" at port \"5432\".\n\n-- SQL alternatives:\nSELECT current_database();  -- mydb\nSELECT current_user;         -- postgres\nSELECT inet_server_addr();   -- server IP\nSELECT inet_server_port();   -- 5432", tags: ['admin', 'psql', 'connection-info'] },
  { id: 'pg-admin-l1-026', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What is the PUBLIC pseudo-role?', options: ['The superuser account', 'The default admin account', 'A special role that represents all current and future roles — privileges granted to PUBLIC apply to everyone', 'The database owner'], correctAnswer: 2, explanation: 'PUBLIC is a special role that includes all roles. When you GRANT ... TO PUBLIC, the privilege applies to every role, including roles created in the future. By default, all roles have CONNECT on databases and USAGE on the public schema via PUBLIC grants.', codeSnippet: "-- Grant SELECT to everyone:\nGRANT SELECT ON lookup_table TO PUBLIC;\n\n-- Revoke default public schema access (security hardening):\nREVOKE ALL ON SCHEMA public FROM PUBLIC;\n\n-- Revoke default database connect:\nREVOKE CONNECT ON DATABASE mydb FROM PUBLIC;", tags: ['admin', 'privileges', 'public'] },
  { id: 'pg-admin-l1-027', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you change the owner of a database?', options: ['UPDATE pg_database SET owner = new_owner', 'ALTER DATABASE mydb OWNER TO new_owner;', 'CHANGE OWNER mydb new_owner', 'GRANT OWNERSHIP ON mydb TO new_owner'], correctAnswer: 1, explanation: 'ALTER DATABASE ... OWNER TO changes the database owner. The new owner must exist and the current user must be a superuser or the current owner (with CREATEDB privilege). The new owner is granted all privileges on the database.', codeSnippet: "-- Change database owner:\nALTER DATABASE myapp OWNER TO app_admin;\n\n-- Verify:\nSELECT datname, datdba::regrole AS owner\nFROM pg_database\nWHERE datname = 'myapp';", tags: ['admin', 'database', 'owner'] },
  { id: 'pg-admin-l1-028', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does \\dn do in psql?', options: ['Describe network settings', 'Drop a namespace', 'List all schemas (namespaces) in the current database', 'Describe NULL handling'], correctAnswer: 2, explanation: '\\dn lists all schemas in the current database. Schemas are namespaces that contain tables, views, functions, and other database objects. The default schema is public. Use \\dn+ for access privileges.', codeSnippet: "-- List schemas:\n\\dn\n\n-- With access privileges:\n\\dn+\n\n-- SQL equivalent:\nSELECT schema_name, schema_owner\nFROM information_schema.schemata;", tags: ['admin', 'psql', 'schemas'] },
  { id: 'pg-admin-l1-029', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'How do you change a role password?', options: ['UPDATE pg_roles SET password = new_pass', 'CHANGE PASSWORD role_name', 'ALTER ROLE alice WITH PASSWORD \'new_password\';', 'SET PASSWORD FOR alice = new_pass'], correctAnswer: 2, explanation: 'ALTER ROLE ... WITH PASSWORD changes a role\'s password. Passwords are stored as MD5 or SCRAM-SHA-256 hashes (depending on password_encryption setting). Use \\password in psql for interactive password changes that avoid exposing the password in the query log.', codeSnippet: "-- Change password:\nALTER ROLE alice WITH PASSWORD 'new_secret';\n\n-- In psql (interactive, hides password):\n\\password alice\n\n-- Set password expiration:\nALTER ROLE alice VALID UNTIL '2025-12-31';", tags: ['admin', 'roles', 'password'] },
  { id: 'pg-admin-l1-030', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you show the definition of a view in psql?', options: ['\\d+ view_name shows the view definition along with column information', 'DESCRIBE VIEW view_name', 'SHOW VIEW view_name', 'SELECT definition FROM views'], correctAnswer: 0, explanation: '\\d+ viewname shows the view definition (the SELECT query), column information, and any rules or triggers. You can also use \\sv to see just the SQL definition, or pg_get_viewdef() in SQL.', codeSnippet: "-- View definition with column info:\n\\d+ my_view\n\n-- Just the SQL definition:\n\\sv my_view\n\n-- SQL alternative:\nSELECT pg_get_viewdef('my_view'::regclass, true);", tags: ['admin', 'psql', 'views'] },
  { id: 'pg-admin-l1-031', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What is the CONNECT privilege on a database?', options: ['It allows creating connections to other databases', 'It allows a role to connect to the specified database — without it, the role cannot establish a session', 'It enables network connections', 'It allows connecting to remote servers'], correctAnswer: 1, explanation: 'CONNECT privilege on a database controls whether a role can connect to it. By default, CONNECT is granted to PUBLIC (everyone). Revoking CONNECT from PUBLIC and granting it selectively is a common security measure.', codeSnippet: "-- Revoke public connect (security hardening):\nREVOKE CONNECT ON DATABASE mydb FROM PUBLIC;\n\n-- Grant connect to specific role:\nGRANT CONNECT ON DATABASE mydb TO app_user;\n\n-- Check connect privileges:\n\\l mydb", tags: ['admin', 'privileges', 'connect'] },
  { id: 'pg-admin-l1-032', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'What does the -Fc flag mean in pg_dump?', options: ['Force compression', 'Format custom — produces a compressed, custom-format archive suitable for pg_restore with support for selective restore and parallel restore', 'Fast copy', 'File checksum'], correctAnswer: 1, explanation: 'The -Fc flag tells pg_dump to output in custom format. This format is compressed by default, supports selective restore (individual tables, schemas), parallel restore (-j), and reordering of data during restore. It can only be restored with pg_restore, not psql.', codeSnippet: "# Create custom format dump:\npg_dump -Fc mydb > backup.dump\n\n# Restore with pg_restore:\npg_restore -d mydb backup.dump\n\n# Selective restore (specific table):\npg_restore -d mydb -t orders backup.dump\n\n# Parallel restore:\npg_restore -d mydb -j 4 backup.dump", tags: ['admin', 'backup', 'pg-dump', 'custom-format'] },
  { id: 'pg-admin-l1-033', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you check the current PostgreSQL version?', options: ['VERSION()', 'SHOW VERSION', 'SELECT version(); or use psql --version from the command line', 'pg_config --version only'], correctAnswer: 2, explanation: 'SELECT version() returns the full version string including the platform. SHOW server_version gives just the version number. From the command line, psql --version or postgres --version show the client/server version respectively.', codeSnippet: "-- Full version info:\nSELECT version();\n-- PostgreSQL 16.2 on x86_64-pc-linux-gnu...\n\n-- Just version number:\nSHOW server_version;  -- 16.2\n\n-- Numeric version for comparison:\nSHOW server_version_num;  -- 160002\n\n-- From command line:\n-- psql --version\n-- postgres --version", tags: ['admin', 'database', 'version'] },
  { id: 'pg-admin-l1-034', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you enable expanded (vertical) output in psql?', options: ['SET VERTICAL ON', 'EXPAND ON', '\\x toggles expanded display mode, showing each column on a separate line — useful for wide tables', 'FORMAT VERTICAL'], correctAnswer: 2, explanation: '\\x toggles expanded display where each column is shown on its own line instead of in a tabular layout. This is useful when tables have many columns. \\x auto lets psql choose based on output width.', codeSnippet: "-- Toggle expanded display:\n\\x\n\n-- Auto mode (use expanded when output is wide):\n\\x auto\n\n-- Output format:\n-- -[ RECORD 1 ]----\n-- id       | 1\n-- name     | Alice\n-- email    | alice@example.com\n-- created  | 2024-01-15", tags: ['admin', 'psql', 'display'] },
  { id: 'pg-admin-l1-035', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What does the INHERIT attribute mean for a role?', options: ['The role inherits table structures', 'The role automatically inherits privileges of roles it is a member of — without INHERIT, the role must use SET ROLE to activate the privileges', 'The role inherits data from other tables', 'The role inherits the superuser status'], correctAnswer: 1, explanation: 'INHERIT (the default) means the role automatically has all privileges of roles it is a member of. With NOINHERIT, the role must explicitly SET ROLE to activate the privileges of the granted role. This provides a safety mechanism for elevated privileges.', codeSnippet: "-- Default: inherits privileges automatically\nCREATE ROLE alice WITH LOGIN INHERIT;\nGRANT readonly TO alice;\n-- alice automatically has readonly privileges\n\n-- NOINHERIT: must SET ROLE explicitly\nCREATE ROLE bob WITH LOGIN NOINHERIT;\nGRANT admin TO bob;\n-- bob must do: SET ROLE admin;\n-- to activate admin privileges", tags: ['admin', 'roles', 'inherit'] },
  { id: 'pg-admin-l1-036', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you dump only the schema (structure) without data?', options: ['pg_dump --no-data mydb', 'pg_dump --schema-only mydb or pg_dump -s mydb', 'pg_dump --structure mydb', 'pg_dump --ddl mydb'], correctAnswer: 1, explanation: 'pg_dump --schema-only (or -s) dumps only the DDL statements (CREATE TABLE, CREATE INDEX, etc.) without any data. The opposite is --data-only (-a) which dumps only the data.', codeSnippet: "# Schema only (DDL):\npg_dump -s mydb > schema.sql\n\n# Data only:\npg_dump -a mydb > data.sql\n\n# Schema for specific table:\npg_dump -s -t employees mydb > employees_schema.sql\n\n# Full dump (schema + data, default):\npg_dump mydb > full_backup.sql", tags: ['admin', 'backup', 'schema-only'] },
  { id: 'pg-admin-l1-037', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you display the execution time of queries in psql?', options: ['SET TIMER ON', 'SHOW TIMING', '\\timing toggles the display of query execution time in milliseconds', 'ENABLE TIMING'], correctAnswer: 2, explanation: '\\timing toggles the display of how long each query takes. When enabled, psql shows the execution time in milliseconds after each query result. This is useful for quick performance checking.', codeSnippet: "-- Enable timing:\n\\timing\n-- Timing is on.\n\nSELECT count(*) FROM large_table;\n--  count\n-- -------\n--  50000\n-- (1 row)\n-- Time: 45.123 ms\n\n-- Disable timing:\n\\timing\n-- Timing is off.", tags: ['admin', 'psql', 'timing'] },
  { id: 'pg-admin-l1-038', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What does ALTER DEFAULT PRIVILEGES do?', options: ['Changes the default password', 'Sets privileges that will be automatically applied to objects created in the future by the specified role — does not affect existing objects', 'Alters the default database', 'Changes the default schema'], correctAnswer: 1, explanation: 'ALTER DEFAULT PRIVILEGES pre-defines privileges for objects that will be created in the future. This is essential because GRANT ... ON ALL TABLES only affects existing tables. Without ALTER DEFAULT PRIVILEGES, you must re-grant after every new table creation.', codeSnippet: "-- Auto-grant SELECT on future tables created by app_admin:\nALTER DEFAULT PRIVILEGES FOR ROLE app_admin\n  IN SCHEMA public\n  GRANT SELECT ON TABLES TO readonly;\n\n-- Now any table created by app_admin automatically\n-- grants SELECT to readonly\n\n-- Auto-grant on future sequences:\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT USAGE ON SEQUENCES TO app_user;", tags: ['admin', 'privileges', 'default-privileges'] },
  { id: 'pg-admin-l1-039', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'What is the postgres database?', options: ['A test database', 'The system catalog database', 'A default database created during PostgreSQL installation — it serves as the default connection target and is useful for administrative connections', 'A backup database'], correctAnswer: 2, explanation: 'The postgres database is created by default during initdb. It is used as the default database when connecting without specifying one. It is typically used for administrative tasks like creating new databases, managing roles, and checking cluster-wide status.', tags: ['admin', 'database', 'postgres'] },
  { id: 'pg-admin-l1-040', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you list all indexes in psql?', options: ['SHOW INDEXES', '\\di lists all indexes in the current database', 'LIST INDEXES', 'SELECT * FROM indexes'], correctAnswer: 1, explanation: '\\di lists all indexes. \\di+ shows additional information like size. You can filter by pattern: \\di employees* shows indexes on tables starting with "employees".', codeSnippet: "-- List all indexes:\n\\di\n\n-- With sizes:\n\\di+\n\n-- Filter by table pattern:\n\\di employees*\n\n-- SQL alternative:\nSELECT indexname, tablename, indexdef\nFROM pg_indexes\nWHERE schemaname = 'public';", tags: ['admin', 'psql', 'indexes'] },
  { id: 'pg-admin-l1-041', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What does CREATEROLE allow a role to do?', options: ['Create new tables', 'Create new databases', 'Create, alter, and drop other non-superuser roles', 'Create new schemas'], correctAnswer: 2, explanation: 'CREATEROLE allows a role to create new roles and manage non-superuser roles (alter, drop, grant membership). A role with CREATEROLE cannot create superuser roles or alter superuser attributes. This is useful for delegated user management.', codeSnippet: "-- Create a role manager:\nCREATE ROLE user_manager WITH LOGIN CREATEROLE PASSWORD 'pass';\n\n-- user_manager can now:\nCREATE ROLE new_user WITH LOGIN;\nALTER ROLE new_user WITH PASSWORD 'new_pass';\nDROP ROLE new_user;\n\n-- But cannot:\n-- CREATE ROLE super WITH SUPERUSER;  -- ERROR", tags: ['admin', 'roles', 'createrole'] },
  { id: 'pg-admin-l1-042', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you dump a specific table only?', options: ['pg_dump --table orders mydb', 'pg_dump -t orders mydb or pg_dump --table=orders mydb', 'pg_dump mydb orders', 'pg_dump --select orders mydb'], correctAnswer: 1, explanation: 'pg_dump -t table_name (or --table=table_name) dumps only the specified table(s). You can use -t multiple times for multiple tables and wildcards (e.g., -t \'user*\') for pattern matching.', codeSnippet: "# Dump one table:\npg_dump -t orders mydb > orders.sql\n\n# Dump multiple tables:\npg_dump -t orders -t customers mydb > subset.sql\n\n# Dump tables matching a pattern:\npg_dump -t 'user*' mydb > user_tables.sql\n\n# Dump table in specific schema:\npg_dump -t 'sales.orders' mydb > sales_orders.sql", tags: ['admin', 'backup', 'pg-dump', 'specific-table'] },
  { id: 'pg-admin-l1-043', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does \\? do in psql?', options: ['Shows SQL syntax help', 'Shows help for psql meta-commands (backslash commands)', 'Exits psql', 'Shows the current query'], correctAnswer: 1, explanation: '\\? displays help for psql meta-commands (\\l, \\dt, \\d, etc.). For SQL syntax help, use \\h or \\help followed by a SQL command name (e.g., \\h SELECT).', codeSnippet: "-- Help for psql meta-commands:\n\\?\n\n-- Help for SQL commands:\n\\h\n\n-- Help for specific SQL command:\n\\h SELECT\n\\h CREATE TABLE\n\\h ALTER ROLE", tags: ['admin', 'psql', 'help'] },
  { id: 'pg-admin-l1-044', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you see the size of a database?', options: ['SHOW DATABASE SIZE', 'SELECT pg_size_pretty(pg_database_size(\'mydb\'));', 'du -h /var/lib/postgresql', 'CHECK SIZE mydb'], correctAnswer: 1, explanation: 'pg_database_size() returns the database size in bytes. Wrap it with pg_size_pretty() for human-readable format. You can also use \\l+ in psql to see all database sizes.', codeSnippet: "-- Human-readable database size:\nSELECT pg_size_pretty(pg_database_size('mydb'));\n-- 256 MB\n\n-- All database sizes:\nSELECT datname,\n  pg_size_pretty(pg_database_size(datname)) AS size\nFROM pg_database\nORDER BY pg_database_size(datname) DESC;\n\n-- In psql:\n\\l+", tags: ['admin', 'database', 'size'] },
  { id: 'pg-admin-l1-045', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'How do you grant SELECT on a specific column?', options: ['Column-level privileges are not supported', 'GRANT SELECT(column_name) ON table TO role; — you can grant privileges on specific columns', 'GRANT SELECT ON table.column TO role', 'GRANT COLUMN SELECT ON table TO role'], correctAnswer: 1, explanation: 'PostgreSQL supports column-level privileges. You can grant SELECT, INSERT, UPDATE, and REFERENCES on specific columns. This allows fine-grained access control where a role can see some columns but not others (e.g., hiding salary data).', codeSnippet: "-- Grant SELECT on specific columns:\nGRANT SELECT(name, email) ON employees TO intern;\n\n-- Grant UPDATE on specific column:\nGRANT UPDATE(email) ON employees TO self_service;\n\n-- intern can:\nSELECT name, email FROM employees;  -- OK\nSELECT salary FROM employees;        -- ERROR", tags: ['admin', 'privileges', 'column-level'] },
  { id: 'pg-admin-l1-046', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you set the output format to CSV in psql?', options: ['SET FORMAT CSV', 'OUTPUT CSV', '\\pset format csv sets the output format to CSV for query results', 'EXPORT AS CSV'], correctAnswer: 2, explanation: '\\pset format csv changes the output format to comma-separated values. Other formats include aligned (default), unaligned, wrapped, html, latex, and asciidoc. You can also use COPY or \\copy to export query results to CSV files.', codeSnippet: "-- Set CSV format:\n\\pset format csv\n\n-- Run query:\nSELECT * FROM employees;\n-- id,name,email\n-- 1,Alice,alice@example.com\n\n-- Alternative: export to file\n\\copy (SELECT * FROM employees) TO '/tmp/employees.csv' CSV HEADER\n\n-- Reset to default:\n\\pset format aligned", tags: ['admin', 'psql', 'csv', 'output-format'] },
  { id: 'pg-admin-l1-047', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What is the VALID UNTIL attribute for roles?', options: ['Specifies when the role was created', 'Sets a password expiration date — after this timestamp, the password no longer works for authentication but the role still exists', 'Sets when the role is deleted', 'Specifies the maximum session length'], correctAnswer: 1, explanation: 'VALID UNTIL sets a timestamp after which the role\'s password is no longer valid for authentication. The role itself is not dropped. This is useful for temporary access or enforcing password rotation policies.', codeSnippet: "-- Set password expiration:\nCREATE ROLE temp_user WITH LOGIN\n  PASSWORD 'temp_pass'\n  VALID UNTIL '2025-03-31';\n\n-- Update expiration:\nALTER ROLE temp_user VALID UNTIL '2025-06-30';\n\n-- Remove expiration (never expires):\nALTER ROLE temp_user VALID UNTIL 'infinity';", tags: ['admin', 'roles', 'password-expiration'] },
  { id: 'pg-admin-l1-048', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you restore a custom-format dump?', options: ['psql < backup.dump', 'RESTORE backup.dump', 'pg_restore -d mydb backup.dump', 'LOAD backup.dump'], correctAnswer: 2, explanation: 'Custom format (-Fc) and directory format (-Fd) dumps must be restored with pg_restore. The -d flag specifies the target database. pg_restore supports parallel restore (-j), selective restore (-t, -n), and verbose output (-v).', codeSnippet: "# Restore custom format dump:\npg_restore -d mydb backup.dump\n\n# Parallel restore (4 workers):\npg_restore -d mydb -j 4 backup.dump\n\n# Restore specific table:\npg_restore -d mydb -t orders backup.dump\n\n# List contents of dump:\npg_restore -l backup.dump", tags: ['admin', 'backup', 'pg-restore'] },
  { id: 'pg-admin-l1-049', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you see the current connections to a database?', options: ['SHOW CONNECTIONS', 'SELECT * FROM pg_stat_activity; — shows all current sessions with database, user, query, and state', 'LIST SESSIONS', 'SHOW SESSIONS'], correctAnswer: 1, explanation: 'pg_stat_activity is a system view that shows one row per server process, including the database, user, client address, current query, state (active, idle, etc.), and timestamps. It is essential for monitoring and troubleshooting.', codeSnippet: "-- All active connections:\nSELECT pid, usename, datname, state, query\nFROM pg_stat_activity;\n\n-- Count connections per database:\nSELECT datname, count(*)\nFROM pg_stat_activity\nGROUP BY datname;\n\n-- Find long-running queries:\nSELECT pid, now() - query_start AS duration, query\nFROM pg_stat_activity\nWHERE state = 'active'\nORDER BY duration DESC;", tags: ['admin', 'monitoring', 'pg-stat-activity'] },
  { id: 'pg-admin-l1-050', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you set a psql variable?', options: ['SET var = value', 'DEFINE var value', '\\set var value assigns a value to a psql variable that can be referenced as :var in queries', 'LET var = value'], correctAnswer: 2, explanation: '\\set assigns a psql variable that can be used in queries with :var syntax. This is different from SET which changes PostgreSQL server settings. psql variables are client-side string substitution.', codeSnippet: "-- Set a variable:\n\\set my_table 'employees'\n\n-- Use in a query (substituted before sending to server):\nSELECT * FROM :my_table WHERE id = 1;\n\n-- Set from query result:\nSELECT count(*) AS cnt FROM employees \\gset\n\\echo :cnt\n\n-- Unset a variable:\n\\unset my_table", tags: ['admin', 'psql', 'variables'] },
  { id: 'pg-admin-l1-051', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What does GRANT ALL PRIVILEGES ON DATABASE mydb TO alice mean?', options: ['Alice can do anything in the database', 'Grants CREATE, CONNECT, and TEMPORARY privileges on the database — this does NOT include access to tables or schemas inside the database', 'Alice becomes the database owner', 'Alice gets superuser access'], correctAnswer: 1, explanation: 'ALL PRIVILEGES on a database grants only CREATE (create schemas), CONNECT, and TEMPORARY (create temp tables). It does NOT grant access to any tables, views, or other objects within the database. Those must be granted separately on schemas and tables.', codeSnippet: "-- Database-level privileges:\nGRANT ALL ON DATABASE mydb TO alice;\n-- Gives: CREATE, CONNECT, TEMPORARY\n\n-- This does NOT give table access!\n-- Alice still needs:\nGRANT USAGE ON SCHEMA public TO alice;\nGRANT SELECT ON ALL TABLES IN SCHEMA public TO alice;", tags: ['admin', 'privileges', 'database-level'] },
  { id: 'pg-admin-l1-052', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'How do you prevent a role from logging in?', options: ['DROP ROLE role_name', 'ALTER ROLE role_name WITH NOLOGIN; — removes the LOGIN attribute while keeping the role and its privileges', 'DISABLE ROLE role_name', 'LOCK ROLE role_name'], correctAnswer: 1, explanation: 'NOLOGIN prevents a role from connecting to the database. The role still exists and retains all its privileges, which can be inherited by member roles. This is the standard way to create group roles that should not be used for direct connections.', codeSnippet: "-- Disable login:\nALTER ROLE alice NOLOGIN;\n\n-- Re-enable login:\nALTER ROLE alice LOGIN;\n\n-- Create a group role (no login by default):\nCREATE ROLE developers;  -- NOLOGIN is default for CREATE ROLE\n\n-- Verify:\nSELECT rolname, rolcanlogin FROM pg_roles;", tags: ['admin', 'roles', 'nologin'] },
  { id: 'pg-admin-l1-053', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you see the SQL behind psql meta-commands?', options: ['\\debug on', 'Start psql with the -E flag (or use \\set ECHO_HIDDEN on) to see the SQL queries that psql generates for meta-commands', 'Use EXPLAIN before meta-commands', 'Use \\sql mode'], correctAnswer: 1, explanation: 'The -E flag (or \\set ECHO_HIDDEN on) makes psql display the actual SQL queries it sends to the server for meta-commands like \\dt, \\du, etc. This is educational and useful for building custom monitoring queries.', codeSnippet: "# Start psql with -E:\npsql -E mydb\n\n-- Or enable inside psql:\n\\set ECHO_HIDDEN on\n\n-- Now meta-commands show their SQL:\n\\dt\n-- Shows: SELECT ... FROM pg_catalog.pg_class ...\n-- Then shows the normal output", tags: ['admin', 'psql', 'echo-hidden'] },
  { id: 'pg-admin-l1-054', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'What is the search_path setting?', options: ['The path to search for SQL files', 'The order in which schemas are searched when a table is referenced without a schema qualifier — default is "$user", public', 'The path to the PostgreSQL data directory', 'The path for full-text search dictionaries'], correctAnswer: 1, explanation: 'search_path defines the schema search order for unqualified object names. By default it is "$user", public — first looking in a schema matching the current user name, then in public. You can change it per-role, per-database, or per-session.', codeSnippet: "-- Show current search_path:\nSHOW search_path;\n-- \"$user\", public\n\n-- Set for current session:\nSET search_path TO myschema, public;\n\n-- Set for a specific role:\nALTER ROLE alice SET search_path = 'app, public';\n\n-- Set for a database:\nALTER DATABASE mydb SET search_path = 'app, public';", tags: ['admin', 'database', 'search-path', 'schemas'] },
  { id: 'pg-admin-l1-055', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'What is the difference between \\copy and COPY in PostgreSQL?', options: ['They are identical', '\\copy runs on the client side (psql) reading/writing local files; COPY runs on the server side reading/writing files on the server filesystem', 'COPY is faster', '\\copy is deprecated'], correctAnswer: 1, explanation: '\\copy is a psql meta-command that reads/writes files on the client machine and sends/receives data over the connection. COPY is a SQL command that reads/writes files on the server filesystem (requires superuser or pg_read_server_files/pg_write_server_files role).', codeSnippet: "-- \\copy: client-side file (run in psql)\n\\copy employees TO '/tmp/employees.csv' CSV HEADER\n\\copy employees FROM '/tmp/employees.csv' CSV HEADER\n\n-- COPY: server-side file (requires privileges)\nCOPY employees TO '/var/lib/postgresql/employees.csv' CSV HEADER;\nCOPY employees FROM '/var/lib/postgresql/employees.csv' CSV HEADER;", tags: ['admin', 'backup', 'copy', 'data-export'] },
  { id: 'pg-admin-l1-056', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you list all functions in psql?', options: ['SHOW FUNCTIONS', '\\df lists all functions in the current database', 'LIST FUNCTIONS', 'SELECT * FROM functions'], correctAnswer: 1, explanation: '\\df lists functions with their schemas, return types, and argument types. Use \\df+ for additional details like the function body. Filter by name pattern: \\df *search*.', codeSnippet: "-- List all functions:\n\\df\n\n-- With details (body, language):\n\\df+\n\n-- Filter by name:\n\\df *update*\n\n-- SQL alternative:\nSELECT routine_name, routine_type, data_type\nFROM information_schema.routines\nWHERE routine_schema = 'public';", tags: ['admin', 'psql', 'functions'] },
  { id: 'pg-admin-l1-057', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What is the TRUNCATE privilege?', options: ['The right to shorten text columns', 'The right to execute TRUNCATE TABLE, which removes all rows from a table — separate from DELETE privilege', 'The right to truncate log files', 'The right to limit query results'], correctAnswer: 1, explanation: 'TRUNCATE privilege controls who can execute TRUNCATE TABLE. TRUNCATE is much faster than DELETE for removing all rows because it deallocates pages without scanning rows. It is a separate privilege from DELETE because it bypasses row-level triggers and cannot have a WHERE clause.', codeSnippet: "-- Grant TRUNCATE:\nGRANT TRUNCATE ON staging_table TO etl_user;\n\n-- ETL user can now:\nTRUNCATE staging_table;\n\n-- Revoke:\nREVOKE TRUNCATE ON staging_table FROM etl_user;", tags: ['admin', 'privileges', 'truncate'] },
  { id: 'pg-admin-l1-058', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you terminate a specific database connection?', options: ['KILL pid', 'DISCONNECT pid', 'SELECT pg_terminate_backend(pid); — terminates the server process with the given PID', 'STOP SESSION pid'], correctAnswer: 2, explanation: 'pg_terminate_backend(pid) terminates a specific backend process. Use pg_cancel_backend(pid) to just cancel the current query without terminating the connection. Find PIDs using pg_stat_activity.', codeSnippet: "-- Find the PID of the session to terminate:\nSELECT pid, usename, query, state\nFROM pg_stat_activity\nWHERE datname = 'mydb';\n\n-- Cancel current query (gentle):\nSELECT pg_cancel_backend(12345);\n\n-- Terminate connection (forceful):\nSELECT pg_terminate_backend(12345);\n\n-- Terminate all connections to a database:\nSELECT pg_terminate_backend(pid)\nFROM pg_stat_activity\nWHERE datname = 'mydb' AND pid <> pg_backend_pid();", tags: ['admin', 'database', 'terminate-backend'] },
  { id: 'pg-admin-l1-059', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What does the CONNECTION LIMIT attribute do?', options: ['Limits query execution time', 'Limits the number of concurrent connections a role can have — useful for preventing a single user from consuming all connection slots', 'Limits the number of queries per connection', 'Limits the data transfer rate'], correctAnswer: 1, explanation: 'CONNECTION LIMIT sets the maximum number of simultaneous connections for a role. A value of -1 (default) means no limit. This prevents a single role from exhausting the max_connections pool.', codeSnippet: "-- Limit alice to 5 concurrent connections:\nALTER ROLE alice CONNECTION LIMIT 5;\n\n-- Create role with connection limit:\nCREATE ROLE app_user WITH LOGIN\n  CONNECTION LIMIT 10\n  PASSWORD 'pass';\n\n-- Remove limit:\nALTER ROLE app_user CONNECTION LIMIT -1;\n\n-- Check current limits:\nSELECT rolname, rolconnlimit FROM pg_roles;", tags: ['admin', 'roles', 'connection-limit'] },
  { id: 'pg-admin-l1-060', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you edit the last query in an external editor from psql?', options: ['EDIT', '\\e opens the last query in the default system editor ($EDITOR) — after saving and closing, the query is loaded back into psql', 'F2', 'OPEN EDITOR'], correctAnswer: 1, explanation: '\\e opens the query buffer in your $EDITOR (or vi/nano by default). You can edit the SQL, save, and close the editor — psql then loads the modified query. Use \\e filename to edit a file directly.', codeSnippet: "-- Edit last query in $EDITOR:\n\\e\n\n-- Edit a file:\n\\e /path/to/query.sql\n\n-- Set preferred editor:\n\\setenv EDITOR vim\n-- Or set in shell: export EDITOR=vim\n\n-- After editing, the query is loaded but not executed.\n-- Press Enter or \\g to execute.", tags: ['admin', 'psql', 'editor'] },
  { id: 'pg-admin-l1-061', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'How do you check what privileges a role has on a table?', options: ['SHOW PRIVILEGES', 'Use \\dp table_name in psql or query information_schema.table_privileges', 'SELECT * FROM permissions', 'DESCRIBE PRIVILEGES table_name'], correctAnswer: 1, explanation: '\\dp (display privileges) shows the access privileges on tables, views, and sequences. You can also query information_schema.table_privileges for a structured result. The privilege display uses a compact format: r=SELECT, w=UPDATE, a=INSERT, d=DELETE, etc.', codeSnippet: "-- Show privileges in psql:\n\\dp employees\n\n-- SQL query:\nSELECT grantee, privilege_type\nFROM information_schema.table_privileges\nWHERE table_name = 'employees';\n\n-- Privilege abbreviations:\n-- r = SELECT, a = INSERT, w = UPDATE\n-- d = DELETE, D = TRUNCATE, x = REFERENCES\n-- t = TRIGGER", tags: ['admin', 'privileges', 'check-privileges'] },
  { id: 'pg-admin-l1-062', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'What is the pg_hba.conf file?', options: ['PostgreSQL configuration file for performance tuning', 'The host-based authentication configuration file that controls which users can connect from which hosts using which authentication methods', 'A log file for database errors', 'A file containing SQL schema definitions'], correctAnswer: 1, explanation: 'pg_hba.conf (Host-Based Authentication) controls client authentication. Each line specifies a connection type (local/host), database, user, client address, and authentication method (trust, md5, scram-sha-256, peer, reject, etc.). Lines are evaluated top to bottom; first match wins.', codeSnippet: "# pg_hba.conf examples:\n\n# Local connections: peer auth (OS username = PG role)\nlocal  all  all  peer\n\n# Local network: password auth\nhost  all  all  192.168.1.0/24  scram-sha-256\n\n# Specific user/db: allow from anywhere\nhost  mydb  app_user  0.0.0.0/0  scram-sha-256\n\n# Reject all other connections:\nhost  all  all  0.0.0.0/0  reject", tags: ['admin', 'authentication', 'pg-hba'] },
  { id: 'pg-admin-l1-063', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you create a compressed backup?', options: ['pg_dump --compress mydb', 'pg_dump -Z 9 mydb > backup.sql.gz or use custom format (-Fc) which compresses by default', 'pg_dump --zip mydb', 'gzip mydb.sql'], correctAnswer: 1, explanation: 'pg_dump -Z sets the compression level (0-9) for plain format dumps. Custom format (-Fc) compresses by default using zlib. For directory format (-Fd), each table is compressed individually. You can also pipe to gzip/lz4.', codeSnippet: "# Custom format (compressed by default):\npg_dump -Fc mydb > backup.dump\n\n# Plain format with compression:\npg_dump -Z 9 mydb > backup.sql.gz\n\n# Pipe to gzip:\npg_dump mydb | gzip > backup.sql.gz\n\n# Using lz4 (PG 16+):\npg_dump -Fc --compress=lz4 mydb > backup.lz4", tags: ['admin', 'backup', 'compression'] },
  { id: 'pg-admin-l1-064', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does \\g do in psql?', options: ['Goes to the next record', 'Generates a report', 'Executes the current query buffer — equivalent to pressing Enter after a semicolon', 'Groups results'], correctAnswer: 2, explanation: '\\g executes the current query buffer. It is equivalent to a semicolon at the end of a query. \\g can also redirect output to a file: \\g /tmp/output.txt. \\gx executes and displays in expanded format.', codeSnippet: "-- Execute query:\nSELECT * FROM employees\n\\g\n\n-- Execute and save to file:\nSELECT * FROM employees\n\\g /tmp/employees.txt\n\n-- Execute in expanded format:\nSELECT * FROM employees\n\\gx\n\n-- Execute and store result in variable:\nSELECT count(*) AS total FROM employees\n\\gset\n\\echo :total", tags: ['admin', 'psql', 'execute'] },
  { id: 'pg-admin-l1-065', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What is the REFERENCES privilege used for?', options: ['Creating foreign keys that reference the table — needed because a FK effectively gives the referencing table some control over the referenced table', 'Referencing other databases', 'Creating views that reference the table', 'Cross-referencing schemas'], correctAnswer: 0, explanation: 'The REFERENCES privilege on a table (or specific columns) allows another user to create a foreign key referencing that table. This is needed because a foreign key can prevent DELETE/UPDATE operations on the referenced table, effectively giving the FK creator some control over it.', codeSnippet: "-- Allow alice to create FKs referencing customers:\nGRANT REFERENCES ON customers TO alice;\n\n-- Column-specific:\nGRANT REFERENCES(id) ON customers TO alice;\n\n-- Now alice can:\nCREATE TABLE orders (\n  customer_id INT REFERENCES customers(id)\n);", tags: ['admin', 'privileges', 'references', 'foreign-keys'] },
  { id: 'pg-admin-l1-066', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'What is postgresql.conf?', options: ['The authentication configuration file', 'The main server configuration file that controls database settings like memory allocation, connections, logging, WAL, and performance parameters', 'The replication configuration file', 'The backup configuration file'], correctAnswer: 1, explanation: 'postgresql.conf is the main configuration file for PostgreSQL server settings. It contains parameters for memory (shared_buffers, work_mem), connections (max_connections), logging, WAL, replication, query planning, and many other settings. Changes may require a reload or restart.', codeSnippet: "-- Key settings in postgresql.conf:\n-- max_connections = 100\n-- shared_buffers = 256MB\n-- work_mem = 4MB\n-- maintenance_work_mem = 64MB\n-- effective_cache_size = 4GB\n\n-- Check a setting:\nSHOW shared_buffers;\n\n-- Show all settings:\nSHOW ALL;\n\n-- Check if change requires restart:\nSELECT name, setting, context\nFROM pg_settings\nWHERE name = 'shared_buffers';", tags: ['admin', 'configuration', 'postgresql-conf'] },
  { id: 'pg-admin-l1-067', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'How do you grant all privileges on all tables in a schema?', options: ['GRANT ALL ON SCHEMA public TO alice', 'GRANT ALL ON ALL TABLES IN SCHEMA public TO alice;', 'GRANT EVERYTHING ON public TO alice', 'GRANT * ON *.* TO alice'], correctAnswer: 1, explanation: 'GRANT ALL ON ALL TABLES IN SCHEMA grants all table-level privileges (SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER) on all existing tables in the specified schema. Use ALTER DEFAULT PRIVILEGES for future tables.', codeSnippet: "-- All privileges on all current tables:\nGRANT ALL ON ALL TABLES IN SCHEMA public TO app_admin;\n\n-- Also grant on sequences (for SERIAL columns):\nGRANT ALL ON ALL SEQUENCES IN SCHEMA public TO app_admin;\n\n-- And for future objects:\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT ALL ON TABLES TO app_admin;\nALTER DEFAULT PRIVILEGES IN SCHEMA public\n  GRANT ALL ON SEQUENCES TO app_admin;", tags: ['admin', 'privileges', 'grant-all'] },
  { id: 'pg-admin-l1-068', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you display null values differently in psql output?', options: ['SET NULL display', 'NULL FORMAT value', '\\pset null \'[NULL]\' sets the string displayed for NULL values', 'SHOW NULL AS value'], correctAnswer: 2, explanation: 'By default, psql shows empty strings for NULL values, making them indistinguishable from empty strings. \\pset null sets a custom string (like [NULL] or NULL) to clearly identify NULL values in output.', codeSnippet: "-- Set NULL display:\n\\pset null '[NULL]'\n\n-- Now NULLs are visible:\nSELECT NULL AS empty, '' AS blank;\n--  empty  | blank\n-- --------+-------\n--  [NULL] |\n\n-- Reset:\n\\pset null ''", tags: ['admin', 'psql', 'null-display'] },
  { id: 'pg-admin-l1-069', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you list the contents of a custom-format dump without restoring?', options: ['pg_dump --list backup.dump', 'pg_restore -l backup.dump or pg_restore --list backup.dump', 'tar -tf backup.dump', 'cat backup.dump'], correctAnswer: 1, explanation: 'pg_restore -l (or --list) shows the table of contents of a custom or directory format dump file. It lists all objects (tables, indexes, functions, etc.) with their sizes and dependencies. This output can be edited and used with -L to selectively restore.', codeSnippet: "# List dump contents:\npg_restore -l backup.dump\n\n# Output example:\n# 1; 1262 16384 DATABASE - mydb postgres\n# 200; 1259 16385 TABLE public orders postgres\n# 201; 1259 16386 TABLE public customers postgres\n\n# Save list, edit it, then restore selectively:\npg_restore -l backup.dump > toc.txt\n# Edit toc.txt to comment out unwanted items\npg_restore -d mydb -L toc.txt backup.dump", tags: ['admin', 'backup', 'pg-restore', 'list'] },
  { id: 'pg-admin-l1-070', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What does GRANT CREATE ON SCHEMA public TO alice allow?', options: ['Create databases', 'Allows alice to create new objects (tables, views, functions, etc.) in the public schema', 'Create users', 'Create backups'], correctAnswer: 1, explanation: 'CREATE privilege on a schema allows creating new objects within it. This is separate from USAGE (accessing existing objects) and ownership (modifying/dropping objects). In PostgreSQL 15+, CREATE on the public schema is no longer granted to PUBLIC by default.', codeSnippet: "-- Grant CREATE on schema:\nGRANT CREATE ON SCHEMA public TO alice;\n\n-- Alice can now:\nCREATE TABLE public.my_table (id INT);\nCREATE VIEW public.my_view AS SELECT 1;\n\n-- PG 15+ security: revoke public CREATE\nREVOKE CREATE ON SCHEMA public FROM PUBLIC;", tags: ['admin', 'privileges', 'create', 'schema'] },
  { id: 'pg-admin-l1-071', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you see the table sizes in a database?', options: ['SHOW TABLE SIZES', 'Use pg_relation_size() or pg_total_relation_size() for individual tables; \\dt+ in psql shows all tables with sizes', 'SELECT SIZE FROM tables', 'du -h /var/lib/postgresql/data'], correctAnswer: 1, explanation: 'pg_relation_size() returns the main heap size. pg_total_relation_size() includes indexes, TOAST, and free space map. pg_table_size() includes TOAST but not indexes. \\dt+ in psql shows the size column for all tables.', codeSnippet: "-- Table sizes with indexes:\nSELECT relname,\n  pg_size_pretty(pg_relation_size(oid)) AS data,\n  pg_size_pretty(pg_total_relation_size(oid)) AS total\nFROM pg_class\nWHERE relkind = 'r' AND relnamespace = 'public'::regnamespace\nORDER BY pg_total_relation_size(oid) DESC;\n\n-- In psql:\n\\dt+\n\n-- Specific table:\nSELECT pg_size_pretty(pg_total_relation_size('orders'));", tags: ['admin', 'database', 'table-size'] },
  { id: 'pg-admin-l1-072', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What is the pg_read_all_data role?', options: ['A superuser role', 'A built-in predefined role (PostgreSQL 14+) that grants SELECT on all tables and USAGE on all schemas — useful for read-only monitoring without granting individual privileges', 'A replication role', 'The default public role'], correctAnswer: 1, explanation: 'pg_read_all_data is a predefined role introduced in PostgreSQL 14 that provides read access to all data in all schemas. Grant it to monitoring or analytics roles instead of manually granting SELECT on every table. pg_write_all_data is its write counterpart.', codeSnippet: "-- Grant read-only access to all data:\nGRANT pg_read_all_data TO analytics_user;\n\n-- analytics_user can now SELECT from any table\n-- in any schema without individual grants\n\n-- Write access to all data:\nGRANT pg_write_all_data TO etl_user;\n\n-- Other predefined roles:\n-- pg_monitor, pg_read_all_settings,\n-- pg_read_all_stats, pg_signal_backend", tags: ['admin', 'roles', 'predefined-roles'] },
  { id: 'pg-admin-l1-073', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you save query output to a file in psql?', options: ['SAVE TO file.txt', 'EXPORT file.txt', '\\o file.txt redirects all subsequent query output to the specified file; \\o with no argument stops redirection', 'OUTPUT file.txt'], correctAnswer: 2, explanation: '\\o (output) redirects all query output to a file. Subsequent queries write their results to the file instead of the terminal. Use \\o without arguments to stop redirection and return output to the screen.', codeSnippet: "-- Start redirecting to file:\n\\o /tmp/report.txt\n\n-- All output goes to the file:\nSELECT * FROM employees;\nSELECT * FROM departments;\n\n-- Stop redirecting:\n\\o\n\n-- Alternative: single query to file\n\\g /tmp/single_query.txt", tags: ['admin', 'psql', 'output-file'] },
  { id: 'pg-admin-l1-074', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'What does the --clean flag do in pg_dump?', options: ['Cleans up temporary files', 'Removes the dump file after restore', 'Includes DROP statements before CREATE statements — so restoring the dump will drop existing objects before recreating them', 'Cleans the database before dumping'], correctAnswer: 2, explanation: 'The --clean (or -c) flag adds DROP commands (DROP TABLE, DROP INDEX, etc.) before the CREATE commands in the dump output. This is useful for restoring into a database that already has the objects, ensuring a clean replacement.', codeSnippet: "# Dump with DROP statements:\npg_dump --clean mydb > backup_clean.sql\n\n# Content includes:\n# DROP TABLE IF EXISTS employees;\n# CREATE TABLE employees (...);\n# COPY employees FROM stdin;\n\n# Combined with --if-exists to avoid errors:\npg_dump --clean --if-exists mydb > backup.sql", tags: ['admin', 'backup', 'pg-dump', 'clean'] },
  { id: 'pg-admin-l1-075', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What is the EXECUTE privilege?', options: ['The privilege to execute SQL queries', 'The privilege to run or call a function or procedure — without it, a role cannot invoke the function', 'The privilege to run psql', 'The privilege to execute backup commands'], correctAnswer: 1, explanation: 'EXECUTE privilege controls who can call a function or procedure. By default, EXECUTE is granted to PUBLIC for all functions. This is often revoked for security-sensitive functions and granted selectively.', codeSnippet: "-- Revoke public execution (security best practice):\nREVOKE EXECUTE ON FUNCTION sensitive_func() FROM PUBLIC;\n\n-- Grant to specific role:\nGRANT EXECUTE ON FUNCTION sensitive_func() TO app_user;\n\n-- For all functions in schema:\nREVOKE EXECUTE ON ALL FUNCTIONS IN SCHEMA public FROM PUBLIC;\nGRANT EXECUTE ON ALL FUNCTIONS IN SCHEMA public TO app_user;", tags: ['admin', 'privileges', 'execute', 'functions'] },
  { id: 'pg-admin-l1-076', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you reload the PostgreSQL configuration without restarting?', options: ['RESTART postgresql', 'SELECT pg_reload_conf(); — reloads postgresql.conf and pg_hba.conf without restarting the server', 'RELOAD CONFIG', 'ALTER SYSTEM RELOAD'], correctAnswer: 1, explanation: 'pg_reload_conf() sends a SIGHUP signal to the postmaster, causing it to re-read configuration files. Most settings take effect immediately, but some (like shared_buffers, max_connections) require a full restart. You can also use pg_ctl reload from the command line.', codeSnippet: "-- Reload configuration:\nSELECT pg_reload_conf();\n\n-- From command line:\n-- pg_ctl reload -D /var/lib/postgresql/data\n-- systemctl reload postgresql\n\n-- Check which settings need restart:\nSELECT name, setting, pending_restart\nFROM pg_settings\nWHERE pending_restart = true;", tags: ['admin', 'configuration', 'reload'] },
  { id: 'pg-admin-l1-077', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does \\dv do in psql?', options: ['Describe variables', 'Delete views', 'Lists all views in the current database', 'Display version'], correctAnswer: 2, explanation: '\\dv lists all views. \\dv+ shows additional information. \\dm lists materialized views. These meta-commands query the pg_class catalog filtering by relation kind (v for view, m for materialized view).', codeSnippet: "-- List all views:\n\\dv\n\n-- With details:\n\\dv+\n\n-- List materialized views:\n\\dm\n\n-- SQL equivalent:\nSELECT table_name, view_definition\nFROM information_schema.views\nWHERE table_schema = 'public';", tags: ['admin', 'psql', 'views', 'list'] },
  { id: 'pg-admin-l1-078', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Roles', question: 'What does REASSIGN OWNED BY do?', options: ['Reassigns table data', 'Transfers ownership of all objects owned by a role to another role — essential before dropping a role that owns objects', 'Reassigns privileges', 'Reassigns database connections'], correctAnswer: 1, explanation: 'REASSIGN OWNED BY old_role TO new_role changes the ownership of all database objects (tables, sequences, functions, schemas, etc.) owned by old_role to new_role. This must be run in each database where the role owns objects.', codeSnippet: "-- Transfer all objects from alice to postgres:\nREASSIGN OWNED BY alice TO postgres;\n\n-- Then drop alice's remaining privileges:\nDROP OWNED BY alice;\n\n-- Now safe to drop the role:\nDROP ROLE alice;\n\n-- Note: REASSIGN must be run in each database\n-- where alice owns objects", tags: ['admin', 'roles', 'reassign-owned'] },
  { id: 'pg-admin-l1-079', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'How do you dump only data without schema?', options: ['pg_dump --no-schema mydb', 'pg_dump --data-only mydb or pg_dump -a mydb', 'pg_dump --rows-only mydb', 'pg_dump --content mydb'], correctAnswer: 1, explanation: 'pg_dump --data-only (or -a) outputs only the data (INSERT or COPY statements) without any DDL. The target database must already have the tables created. This is useful for refreshing data while keeping the existing schema.', codeSnippet: "# Data only dump:\npg_dump --data-only mydb > data.sql\n\n# Short form:\npg_dump -a mydb > data.sql\n\n# Data only for specific table:\npg_dump -a -t orders mydb > orders_data.sql\n\n# Use COPY (default, fast) or INSERT:\npg_dump -a --inserts mydb > data_inserts.sql", tags: ['admin', 'backup', 'data-only'] },
  { id: 'pg-admin-l1-080', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'What is the max_connections setting?', options: ['Maximum number of tables', 'Maximum number of concurrent database connections — the default is typically 100 and can be changed in postgresql.conf', 'Maximum query length', 'Maximum number of databases'], correctAnswer: 1, explanation: 'max_connections determines the maximum number of concurrent connections to the PostgreSQL server. Each connection uses memory (~5-10MB). Setting it too high wastes memory; too low causes connection failures. Consider using a connection pooler (pgBouncer) for high-concurrency applications.', codeSnippet: "-- Check current setting:\nSHOW max_connections;  -- 100\n\n-- Check current usage:\nSELECT count(*) FROM pg_stat_activity;\n\n-- Remaining connections:\nSELECT max_conn - current_conn AS available\nFROM\n  (SELECT setting::int AS max_conn FROM pg_settings\n   WHERE name = 'max_connections') mc,\n  (SELECT count(*) AS current_conn\n   FROM pg_stat_activity) cc;", tags: ['admin', 'configuration', 'max-connections'] },
  { id: 'pg-admin-l1-081', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you see the query plan without executing the query?', options: ['PLAN SELECT ...', 'EXPLAIN SELECT ...; shows the query execution plan including estimated costs and row counts', 'SHOW PLAN FOR SELECT ...', 'DESCRIBE QUERY SELECT ...'], correctAnswer: 1, explanation: 'EXPLAIN shows the query plan without executing the query. EXPLAIN ANALYZE actually executes the query and shows both estimated and actual row counts and timing. EXPLAIN (ANALYZE, BUFFERS) also shows buffer usage. EXPLAIN (FORMAT JSON) outputs in JSON format.', codeSnippet: "-- Show plan without execution:\nEXPLAIN SELECT * FROM employees WHERE department = 'Engineering';\n\n-- Show plan with actual execution:\nEXPLAIN ANALYZE SELECT * FROM employees WHERE department = 'Engineering';\n\n-- With buffer info:\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM employees WHERE department = 'Engineering';", tags: ['admin', 'psql', 'explain', 'query-plan'] },
  { id: 'pg-admin-l1-082', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What is the difference between table owner and a user with ALL PRIVILEGES?', options: ['They are identical', 'The owner can DROP, ALTER, and GRANT on the table, while a user with ALL PRIVILEGES can only perform DML operations (SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER) — they cannot drop or alter the table', 'ALL PRIVILEGES includes ownership', 'Only the owner can SELECT'], correctAnswer: 1, explanation: 'Table ownership provides DROP, ALTER, and the ability to GRANT privileges to others. ALL PRIVILEGES provides all grantable privileges (SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER) but not ownership rights. Only the owner (or superuser) can DROP or ALTER the table.', codeSnippet: "-- alice owns the table:\nCREATE TABLE employees (...);  -- alice is owner\n\n-- Grant ALL to bob:\nGRANT ALL ON employees TO bob;\n\n-- bob can: SELECT, INSERT, UPDATE, DELETE, TRUNCATE\n-- bob cannot: DROP TABLE, ALTER TABLE, GRANT to others\n\n-- Only alice (or superuser) can:\nDROP TABLE employees;\nALTER TABLE employees ADD COLUMN ...;\nGRANT SELECT ON employees TO charlie;", tags: ['admin', 'privileges', 'ownership'] },
  { id: 'pg-admin-l1-083', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you create a schema?', options: ['CREATE SCHEMA sales;', 'ADD SCHEMA sales', 'NEW SCHEMA sales', 'MAKE SCHEMA sales'], correctAnswer: 0, explanation: 'CREATE SCHEMA creates a new namespace within the database. Schemas help organize objects and provide an additional layer of access control. You can create a schema with authorization for a specific role.', codeSnippet: "-- Create a schema:\nCREATE SCHEMA sales;\n\n-- Create with owner:\nCREATE SCHEMA sales AUTHORIZATION sales_admin;\n\n-- Create and add objects in one command:\nCREATE SCHEMA sales\n  CREATE TABLE orders (id SERIAL PRIMARY KEY)\n  CREATE VIEW active_orders AS\n    SELECT * FROM sales.orders;\n\n-- Access objects in schema:\nSELECT * FROM sales.orders;", tags: ['admin', 'database', 'schema'] },
  { id: 'pg-admin-l1-084', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Backup', question: 'What does pg_dump --inserts do?', options: ['Uses INSERT INTO statements instead of COPY for data output — useful for compatibility with non-PostgreSQL databases but slower to restore', 'Inserts data into the dump file', 'Only dumps INSERT triggers', 'Compresses the output'], correctAnswer: 0, explanation: 'By default, pg_dump uses COPY statements for data which is much faster. The --inserts flag generates INSERT INTO statements instead, which are portable across database systems but significantly slower to restore. Use --inserts when you need to load the dump into a different RDBMS.', codeSnippet: "# Default: COPY (fast, PostgreSQL-specific)\npg_dump mydb > backup.sql\n# COPY employees FROM stdin;\n# 1\tAlice\talice@ex.com\n# \\.\n\n# With --inserts (slower, portable)\npg_dump --inserts mydb > backup.sql\n# INSERT INTO employees VALUES (1, 'Alice', 'alice@ex.com');\n\n# With column names:\npg_dump --inserts --column-inserts mydb > backup.sql\n# INSERT INTO employees (id, name, email) VALUES (...);", tags: ['admin', 'backup', 'pg-dump', 'inserts'] },
  { id: 'pg-admin-l1-085', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'How do you show the definition of a function in psql?', options: ['DESCRIBE FUNCTION func_name', '\\sf func_name displays the full CREATE FUNCTION statement including the function body', 'SHOW FUNCTION func_name', 'SELECT body FROM functions WHERE name = func_name'], correctAnswer: 1, explanation: '\\sf (show function) displays the complete CREATE OR REPLACE FUNCTION statement. \\sf+ also includes line numbers, which is helpful for debugging. You can also use pg_get_functiondef() in SQL.', codeSnippet: "-- Show function definition:\n\\sf my_function\n\n-- With line numbers:\n\\sf+ my_function\n\n-- SQL alternative:\nSELECT pg_get_functiondef('my_function'::regproc);", tags: ['admin', 'psql', 'functions'] },
  { id: 'pg-admin-l1-086', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Privileges', question: 'What does GRANT TEMPORARY ON DATABASE mydb TO alice allow?', options: ['Allows creating temporary tables in the database — temporary tables exist only for the duration of the session', 'Allows temporary access to the database', 'Allows temporary role changes', 'Grants time-limited privileges'], correctAnswer: 0, explanation: 'TEMPORARY (or TEMP) privilege on a database allows creating temporary tables. Temporary tables are visible only to the current session and are automatically dropped when the session ends. By default, this is granted to PUBLIC.', codeSnippet: "-- Grant temporary table creation:\nGRANT TEMPORARY ON DATABASE mydb TO alice;\n\n-- Alice can now create temp tables:\nCREATE TEMPORARY TABLE temp_results (\n  id INT, value TEXT\n);\n\n-- Temp table is session-scoped:\n-- Automatically dropped when session ends\n-- Not visible to other sessions", tags: ['admin', 'privileges', 'temporary'] },
  { id: 'pg-admin-l1-087', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'Database Management', question: 'How do you see the data directory location?', options: ['SHOW datadir', 'SHOW data_directory; shows the file system path where PostgreSQL stores its data files', 'SELECT datadir()', 'pg_config --datadir'], correctAnswer: 1, explanation: 'SHOW data_directory returns the path to the PostgreSQL data directory (often /var/lib/postgresql/data or similar). This directory contains the configuration files, WAL files, and all database data files.', codeSnippet: "-- Show data directory:\nSHOW data_directory;\n-- /var/lib/postgresql/16/main\n\n-- Show config file location:\nSHOW config_file;\n-- /etc/postgresql/16/main/postgresql.conf\n\n-- Show HBA file location:\nSHOW hba_file;\n-- /etc/postgresql/16/main/pg_hba.conf", tags: ['admin', 'database', 'data-directory'] },
  { id: 'pg-admin-l1-088', language: 'postgresql', level: 'level1', category: 'Administration', subcategory: 'psql Basics', question: 'What does \\watch do in psql?', options: ['Watches for changes in a table', 'Monitors disk usage', 'Repeatedly executes the previous query at a specified interval in seconds — useful for monitoring', 'Watches for new connections'], correctAnswer: 2, explanation: '\\watch [seconds] re-executes the previous query at the specified interval (default 2 seconds). Press Ctrl+C to stop. This is useful for monitoring queries like checking active connections or replication lag.', codeSnippet: "-- Monitor active queries every 5 seconds:\nSELECT pid, state, query FROM pg_stat_activity WHERE state = 'active';\n\\watch 5\n\n-- Monitor replication lag:\nSELECT now() - pg_last_xact_replay_timestamp() AS lag;\n\\watch 1\n\n-- Press Ctrl+C to stop", tags: ['admin', 'psql', 'watch', 'monitoring'] },
];

export default questions;
