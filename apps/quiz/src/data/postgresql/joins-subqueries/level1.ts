import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'pg-joins-l1-001',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What does an INNER JOIN return?',
    options: ['All rows from both tables', 'Only rows that have matching values in both tables', 'All rows from the left table', 'All rows from the right table'],
    correctAnswer: 1,
    explanation: 'INNER JOIN returns only the rows where there is a match in both tables based on the join condition.',
    tags: ['inner-join', 'basics', 'matching'],
  },
  {
    id: 'pg-joins-l1-002',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What is the correct syntax for an INNER JOIN?',
    options: [
      'SELECT * FROM a INNER JOIN b WHERE a.id = b.id;',
      'SELECT * FROM a INNER JOIN b ON a.id = b.a_id;',
      'SELECT * FROM a JOIN TO b ON a.id = b.a_id;',
      'SELECT * FROM a LINK b ON a.id = b.a_id;'
    ],
    correctAnswer: 1,
    explanation: 'INNER JOIN uses the ON clause to specify the join condition. The INNER keyword is optional — JOIN alone defaults to INNER JOIN.',
    codeSnippet: 'SELECT o.id, u.name\nFROM orders o\nINNER JOIN users u ON o.user_id = u.id;',
    tags: ['inner-join', 'syntax', 'on-clause'],
  },
  {
    id: 'pg-joins-l1-003',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'Is the INNER keyword required when writing an INNER JOIN?',
    options: ['Yes, it is always required', 'No, JOIN alone defaults to INNER JOIN', 'Only in PostgreSQL', 'Only when joining more than 2 tables'],
    correctAnswer: 1,
    explanation: 'The INNER keyword is optional. Writing just JOIN is equivalent to INNER JOIN in SQL.',
    tags: ['inner-join', 'syntax', 'shorthand'],
  },
  {
    id: 'pg-joins-l1-004',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'What does a LEFT JOIN (LEFT OUTER JOIN) return?',
    options: [
      'Only matching rows from both tables',
      'All rows from the left table, plus matching rows from the right table (NULLs where no match)',
      'All rows from the right table',
      'Only non-matching rows'
    ],
    correctAnswer: 1,
    explanation: 'LEFT JOIN returns all rows from the left table. For rows with no match in the right table, the right-side columns are filled with NULL.',
    tags: ['left-join', 'outer-join', 'null'],
  },
  {
    id: 'pg-joins-l1-005',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'What values appear for right-table columns when there is no match in a LEFT JOIN?',
    options: ['Empty strings', 'Zero', 'NULL', 'The column default value'],
    correctAnswer: 2,
    explanation: 'When a LEFT JOIN finds no matching row in the right table, all columns from the right table are set to NULL.',
    tags: ['left-join', 'null', 'no-match'],
  },
  {
    id: 'pg-joins-l1-006',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'What does the following query return for a user with no orders?\n\nSELECT u.name, o.id FROM users u LEFT JOIN orders o ON u.id = o.user_id;',
    options: ['The user is excluded', 'The user appears with o.id as NULL', 'An error occurs', 'The user appears with o.id as 0'],
    correctAnswer: 1,
    explanation: 'LEFT JOIN preserves all rows from the left table (users). A user with no orders will appear with NULL for all order columns.',
    codeSnippet: 'SELECT u.name, o.id\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id;',
    tags: ['left-join', 'null', 'no-orders'],
  },
  {
    id: 'pg-joins-l1-007',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'RIGHT JOIN',
    question: 'What does a RIGHT JOIN return?',
    options: [
      'Only matching rows',
      'All rows from the left table plus matches from the right',
      'All rows from the right table, plus matching rows from the left table (NULLs where no match)',
      'The same as INNER JOIN'
    ],
    correctAnswer: 2,
    explanation: 'RIGHT JOIN is the mirror of LEFT JOIN. It returns all rows from the right table and fills left-table columns with NULL where there is no match.',
    tags: ['right-join', 'outer-join', 'basics'],
  },
  {
    id: 'pg-joins-l1-008',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'RIGHT JOIN',
    question: 'Can a RIGHT JOIN always be rewritten as a LEFT JOIN?',
    options: ['No, they are fundamentally different', 'Yes, by swapping the table order', 'Only for two tables', 'Only in PostgreSQL'],
    correctAnswer: 1,
    explanation: 'A RIGHT JOIN b ON condition is equivalent to B LEFT JOIN a ON condition. Most developers prefer LEFT JOIN for readability.',
    tags: ['right-join', 'left-join', 'equivalence'],
  },
  {
    id: 'pg-joins-l1-009',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'FULL JOIN',
    question: 'What does a FULL OUTER JOIN return?',
    options: [
      'Only matching rows',
      'All rows from both tables, with NULLs where there is no match on either side',
      'Only non-matching rows',
      'The Cartesian product'
    ],
    correctAnswer: 1,
    explanation: 'FULL OUTER JOIN returns all rows from both tables. Matching rows are combined; non-matching rows from either table appear with NULLs on the other side.',
    tags: ['full-join', 'outer-join', 'all-rows'],
  },
  {
    id: 'pg-joins-l1-010',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CROSS JOIN',
    question: 'What does a CROSS JOIN produce?',
    options: [
      'Only matching rows',
      'The Cartesian product — every row from the first table paired with every row from the second',
      'Only rows where columns are equal',
      'An error — CROSS JOIN is not valid SQL'
    ],
    correctAnswer: 1,
    explanation: 'CROSS JOIN produces the Cartesian product. If table A has 10 rows and B has 5 rows, the result has 50 rows. No ON clause is used.',
    codeSnippet: 'SELECT * FROM colors CROSS JOIN sizes;\n-- 3 colors × 4 sizes = 12 rows',
    tags: ['cross-join', 'cartesian-product', 'basics'],
  },
  {
    id: 'pg-joins-l1-011',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CROSS JOIN',
    question: 'Which syntax also produces a CROSS JOIN?',
    options: [
      'SELECT * FROM a, b;',
      'SELECT * FROM a INNER JOIN b;',
      'SELECT * FROM a MERGE b;',
      'SELECT * FROM a CONNECT b;'
    ],
    correctAnswer: 0,
    explanation: 'The comma syntax FROM a, b produces a Cartesian product (CROSS JOIN). Adding a WHERE clause effectively makes it an INNER JOIN.',
    codeSnippet: 'SELECT * FROM a, b;\n-- equivalent to: SELECT * FROM a CROSS JOIN b;',
    tags: ['cross-join', 'comma-syntax', 'implicit-join'],
  },
  {
    id: 'pg-joins-l1-012',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Condition',
    question: 'What is the ON clause used for in a JOIN?',
    options: ['To name the result table', 'To specify the condition that determines how rows are matched between tables', 'To limit the number of results', 'To order the results'],
    correctAnswer: 1,
    explanation: 'The ON clause specifies the join condition — typically matching a foreign key in one table to a primary key in another.',
    tags: ['on-clause', 'join-condition', 'basics'],
  },
  {
    id: 'pg-joins-l1-013',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'USING',
    question: 'What does the USING clause do in a JOIN?',
    options: [
      'Specifies which table to use',
      'Shorthand for ON when both tables have a column with the same name',
      'Specifies the join type',
      'Specifies the result columns'
    ],
    correctAnswer: 1,
    explanation: 'USING(column_name) is shorthand for ON a.column_name = b.column_name. The column appears only once in the result.',
    codeSnippet: 'SELECT * FROM orders\nJOIN users USING (user_id);\n-- equivalent to: ON orders.user_id = users.user_id',
    tags: ['using', 'join-shorthand', 'syntax'],
  },
  {
    id: 'pg-joins-l1-014',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'NATURAL JOIN',
    question: 'What does a NATURAL JOIN do?',
    options: [
      'Joins on a user-specified column',
      'Automatically joins on all columns that share the same name in both tables',
      'Creates a natural sort order',
      'Only joins on primary keys'
    ],
    correctAnswer: 1,
    explanation: 'NATURAL JOIN automatically matches all columns with the same name in both tables. It is convenient but risky if column names change.',
    codeSnippet: 'SELECT * FROM orders NATURAL JOIN customers;\n-- Joins on all columns with matching names',
    tags: ['natural-join', 'automatic', 'implicit'],
  },
  {
    id: 'pg-joins-l1-015',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Self Join',
    question: 'What is a self join?',
    options: ['A join with itself using reflection', 'A table joined to itself using aliases', 'An automatic join', 'A join that returns one row'],
    correctAnswer: 1,
    explanation: 'A self join joins a table to itself. You must use table aliases to distinguish the two instances of the same table.',
    codeSnippet: 'SELECT e.name AS employee, m.name AS manager\nFROM employees e\nJOIN employees m ON e.manager_id = m.id;',
    tags: ['self-join', 'alias', 'hierarchy'],
  },
  {
    id: 'pg-joins-l1-016',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Self Join',
    question: 'Why are aliases required in a self join?',
    options: ['For performance optimization', 'To distinguish between the two references to the same table', 'Aliases are optional', 'To rename the result columns'],
    correctAnswer: 1,
    explanation: 'Without aliases, PostgreSQL cannot tell which instance of the table a column reference belongs to, making the query ambiguous.',
    tags: ['self-join', 'alias', 'disambiguation'],
  },
  {
    id: 'pg-joins-l1-017',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Multiple Joins',
    question: 'Can you join more than two tables in a single query?',
    options: ['No, only two tables at a time', 'Yes, by chaining multiple JOIN clauses', 'Only with subqueries', 'Only in stored procedures'],
    correctAnswer: 1,
    explanation: 'You can chain as many JOINs as needed. Each JOIN adds another table to the query.',
    codeSnippet: 'SELECT o.id, u.name, p.title\nFROM orders o\nJOIN users u ON o.user_id = u.id\nJOIN products p ON o.product_id = p.id;',
    tags: ['multiple-joins', 'chaining', 'three-tables'],
  },
  {
    id: 'pg-joins-l1-018',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Table Aliases',
    question: 'What is the purpose of a table alias in a JOIN query?',
    options: ['To permanently rename the table', 'To provide a shorter name for the table in the query', 'To create a copy of the table', 'To filter the table'],
    correctAnswer: 1,
    explanation: 'Table aliases provide shorter, more readable references to tables within a query. They are especially useful when joining multiple tables.',
    codeSnippet: 'SELECT e.name, d.name\nFROM employees e\nJOIN departments d ON e.dept_id = d.id;',
    tags: ['alias', 'readability', 'joins'],
  },
  {
    id: 'pg-joins-l1-019',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Where can a subquery be placed in a SQL statement?',
    options: ['Only in WHERE', 'In WHERE, FROM, SELECT, or HAVING clauses', 'Only in FROM', 'Only in SELECT'],
    correctAnswer: 1,
    explanation: 'Subqueries can appear in multiple places: WHERE (filter), FROM (derived table), SELECT (scalar subquery), and HAVING (group filter).',
    tags: ['subquery', 'placement', 'basics'],
  },
  {
    id: 'pg-joins-l1-020',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is a subquery in the WHERE clause commonly used for?',
    options: ['Joining tables', 'Filtering rows based on the result of another query', 'Sorting results', 'Creating indexes'],
    correctAnswer: 1,
    explanation: 'WHERE subqueries filter rows based on values from another query. For example: WHERE id IN (SELECT id FROM ...).',
    codeSnippet: 'SELECT * FROM products\nWHERE category_id IN (\n  SELECT id FROM categories\n  WHERE name = \'Electronics\'\n);',
    tags: ['subquery', 'where', 'filtering'],
  },
  {
    id: 'pg-joins-l1-021',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is a derived table (subquery in FROM)?',
    options: ['A permanent table', 'A subquery used in the FROM clause that acts as a temporary table', 'A table with derived columns', 'A materialized view'],
    correctAnswer: 1,
    explanation: 'A derived table is a subquery in the FROM clause. It must have an alias and can be joined with other tables.',
    codeSnippet: 'SELECT t.department, t.avg_salary\nFROM (\n  SELECT department, AVG(salary) AS avg_salary\n  FROM employees\n  GROUP BY department\n) t\nWHERE t.avg_salary > 50000;',
    tags: ['derived-table', 'from-subquery', 'alias'],
  },
  {
    id: 'pg-joins-l1-022',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'IN',
    question: 'What does WHERE id IN (SELECT ...) do?',
    options: ['Joins two queries', 'Returns rows where id matches any value from the subquery', 'Returns only the first match', 'Creates a new table'],
    correctAnswer: 1,
    explanation: 'IN with a subquery checks if a value matches any value in the subquery result set. It is equivalent to multiple OR conditions.',
    codeSnippet: 'SELECT * FROM users\nWHERE id IN (\n  SELECT user_id FROM orders\n  WHERE total > 100\n);',
    tags: ['in', 'subquery', 'set-membership'],
  },
  {
    id: 'pg-joins-l1-023',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'NOT IN',
    question: 'What does WHERE id NOT IN (SELECT ...) return?',
    options: ['All rows', 'Rows where id does not match any value in the subquery', 'Only NULL values', 'An error'],
    correctAnswer: 1,
    explanation: 'NOT IN returns rows where the value does not match any value in the subquery. Warning: if the subquery returns any NULL, NOT IN returns no rows at all.',
    tags: ['not-in', 'subquery', 'exclusion'],
  },
  {
    id: 'pg-joins-l1-024',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'EXISTS',
    question: 'What does WHERE EXISTS (subquery) check?',
    options: ['If the subquery has correct syntax', 'If the subquery returns at least one row', 'If the table exists', 'If the column exists'],
    correctAnswer: 1,
    explanation: 'EXISTS returns TRUE if the subquery returns at least one row. It is often more efficient than IN for correlated subqueries.',
    codeSnippet: 'SELECT * FROM users u\nWHERE EXISTS (\n  SELECT 1 FROM orders o\n  WHERE o.user_id = u.id\n);',
    tags: ['exists', 'subquery', 'boolean'],
  },
  {
    id: 'pg-joins-l1-025',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'EXISTS',
    question: 'What does NOT EXISTS check?',
    options: ['If the table does not exist', 'If the subquery returns no rows', 'If a column is NULL', 'If the database is empty'],
    correctAnswer: 1,
    explanation: 'NOT EXISTS returns TRUE when the subquery returns zero rows. It is useful for finding rows without related records.',
    codeSnippet: 'SELECT * FROM users u\nWHERE NOT EXISTS (\n  SELECT 1 FROM orders o\n  WHERE o.user_id = u.id\n);',
    tags: ['not-exists', 'anti-join', 'subquery'],
  },
  {
    id: 'pg-joins-l1-026',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'If table A has 5 rows and table B has 3 matching rows, how many rows does INNER JOIN return?',
    options: ['5', '3', '8', 'It depends on how many rows match the join condition'],
    correctAnswer: 3,
    explanation: 'INNER JOIN returns one row for each matching pair. The count depends on the join condition — a one-to-many relationship produces more rows than either table alone.',
    tags: ['inner-join', 'row-count', 'cardinality'],
  },
  {
    id: 'pg-joins-l1-027',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What happens to rows with no match in an INNER JOIN?',
    options: ['They appear with NULLs', 'They are excluded from the result', 'They cause an error', 'They appear at the end'],
    correctAnswer: 1,
    explanation: 'INNER JOIN only returns rows where the join condition is satisfied. Rows without a match in the other table are excluded entirely.',
    tags: ['inner-join', 'no-match', 'exclusion'],
  },
  {
    id: 'pg-joins-l1-028',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'How can you find users who have never placed an order using a LEFT JOIN?',
    options: [
      'LEFT JOIN with WHERE order_id IS NOT NULL',
      'LEFT JOIN with WHERE order_id IS NULL',
      'Using INNER JOIN',
      'Using CROSS JOIN'
    ],
    correctAnswer: 1,
    explanation: 'LEFT JOIN keeps all users, putting NULL for order columns when no order exists. Filtering WHERE order_id IS NULL finds users without orders.',
    codeSnippet: 'SELECT u.name\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nWHERE o.id IS NULL;',
    tags: ['left-join', 'anti-join', 'is-null'],
  },
  {
    id: 'pg-joins-l1-029',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is a scalar subquery?',
    options: ['A subquery returning multiple rows', 'A subquery that returns exactly one value (one row, one column)', 'A subquery in a JOIN', 'A subquery that returns boolean'],
    correctAnswer: 1,
    explanation: 'A scalar subquery returns a single value and can be used in SELECT lists, WHERE conditions, or anywhere a single value is expected.',
    codeSnippet: 'SELECT name,\n  (SELECT MAX(price) FROM products) AS max_price\nFROM categories;',
    tags: ['scalar-subquery', 'single-value', 'select-list'],
  },
  {
    id: 'pg-joins-l1-030',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'JOIN Order',
    question: 'Does the order of tables in a JOIN matter for the result of an INNER JOIN?',
    options: ['Yes, it changes the results', 'No, INNER JOIN is commutative — the results are the same regardless of order', 'Only for self joins', 'Only for more than 2 tables'],
    correctAnswer: 1,
    explanation: 'For INNER JOIN, the table order does not affect the result set (though column order may differ). The planner may reorder joins for performance.',
    tags: ['join-order', 'commutative', 'inner-join'],
  },
  {
    id: 'pg-joins-l1-031',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'JOIN Order',
    question: 'Does the order matter for LEFT JOIN?',
    options: ['No, order never matters', 'Yes, swapping tables changes which table preserves all rows', 'Only for readability', 'Only with WHERE clause'],
    correctAnswer: 1,
    explanation: 'In LEFT JOIN, the left table preserves all rows. Swapping tables changes the semantics — A LEFT JOIN B is different from B LEFT JOIN A.',
    tags: ['left-join', 'order', 'semantics'],
  },
  {
    id: 'pg-joins-l1-032',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'What does WITH (Common Table Expression) do?',
    options: ['Creates a permanent table', 'Defines a temporary named result set that can be referenced in the main query', 'Starts a transaction', 'Creates a view'],
    correctAnswer: 1,
    explanation: 'A CTE (WITH clause) defines a temporary, named result set. It improves readability and allows the same subquery to be referenced multiple times.',
    codeSnippet: 'WITH active_users AS (\n  SELECT * FROM users WHERE active = true\n)\nSELECT * FROM active_users\nJOIN orders ON active_users.id = orders.user_id;',
    tags: ['cte', 'with', 'named-query'],
  },
  {
    id: 'pg-joins-l1-033',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Can you define multiple CTEs in a single WITH clause?',
    options: ['No, only one CTE per query', 'Yes, separated by commas', 'Only with UNION', 'Only in stored procedures'],
    correctAnswer: 1,
    explanation: 'Multiple CTEs are separated by commas. Later CTEs can reference earlier ones in the same WITH clause.',
    codeSnippet: 'WITH\n  dept_totals AS (\n    SELECT dept_id, SUM(salary) AS total\n    FROM employees GROUP BY dept_id\n  ),\n  high_salary_depts AS (\n    SELECT * FROM dept_totals WHERE total > 500000\n  )\nSELECT * FROM high_salary_depts;',
    tags: ['cte', 'multiple', 'chaining'],
  },
  {
    id: 'pg-joins-l1-034',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What does this query do?\n\nSELECT u.name, COUNT(o.id) FROM users u JOIN orders o ON u.id = o.user_id GROUP BY u.name;',
    options: ['Lists all users', 'Lists users who have orders with the count of their orders', 'Lists all orders', 'Counts all users'],
    correctAnswer: 1,
    explanation: 'INNER JOIN excludes users without orders. GROUP BY and COUNT show how many orders each remaining user has.',
    codeSnippet: 'SELECT u.name, COUNT(o.id)\nFROM users u\nJOIN orders o ON u.id = o.user_id\nGROUP BY u.name;',
    tags: ['inner-join', 'count', 'group-by'],
  },
  {
    id: 'pg-joins-l1-035',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'What does this query show for users with no orders?\n\nSELECT u.name, COUNT(o.id) FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.name;',
    options: ['They are excluded', 'They appear with count = 0', 'They appear with count = NULL', 'An error occurs'],
    correctAnswer: 1,
    explanation: 'LEFT JOIN preserves all users. For users with no orders, o.id is NULL. COUNT(o.id) counts non-NULL values, so it returns 0 for users without orders.',
    codeSnippet: 'SELECT u.name, COUNT(o.id)\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nGROUP BY u.name;',
    tags: ['left-join', 'count', 'zero-count'],
  },
  {
    id: 'pg-joins-l1-036',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is the difference between a subquery and a JOIN for combining data?',
    options: [
      'Subqueries are always faster',
      'JOINs combine columns from multiple tables side-by-side; subqueries typically filter or compute values',
      'They are always identical in function',
      'JOINs only work with two tables'
    ],
    correctAnswer: 1,
    explanation: 'JOINs combine rows from tables into wider rows. Subqueries can filter rows (WHERE), compute values (SELECT list), or create derived tables (FROM).',
    tags: ['subquery-vs-join', 'comparison', 'basics'],
  },
  {
    id: 'pg-joins-l1-037',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'ANY',
    question: 'What does WHERE salary > ANY (subquery) return?',
    options: ['Rows where salary is greater than all subquery values', 'Rows where salary is greater than at least one subquery value', 'The any value', 'An error'],
    correctAnswer: 1,
    explanation: 'ANY (or SOME) returns TRUE if the comparison is true for at least one value from the subquery. Salary > ANY means greater than the minimum.',
    tags: ['any', 'comparison', 'subquery'],
  },
  {
    id: 'pg-joins-l1-038',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'ALL',
    question: 'What does WHERE salary > ALL (subquery) return?',
    options: ['Rows where salary is greater than at least one value', 'Rows where salary is greater than every value from the subquery', 'All rows', 'No rows'],
    correctAnswer: 1,
    explanation: 'ALL returns TRUE only if the comparison is true for every value from the subquery. Salary > ALL means greater than the maximum.',
    tags: ['all', 'comparison', 'subquery'],
  },
  {
    id: 'pg-joins-l1-039',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can a subquery reference a table from the outer query?',
    options: ['No, subqueries are independent', 'Yes, this is called a correlated subquery', 'Only with EXISTS', 'Only in PostgreSQL'],
    correctAnswer: 1,
    explanation: 'A correlated subquery references columns from the outer query. It is re-evaluated for each row of the outer query.',
    tags: ['correlated', 'subquery', 'outer-reference'],
  },
  {
    id: 'pg-joins-l1-040',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What is the result when joining two tables where a foreign key column in one table is NULL?',
    options: ['The row is included with NULLs', 'The row is excluded from INNER JOIN result', 'An error occurs', 'NULL matches everything'],
    correctAnswer: 1,
    explanation: 'NULL never equals anything (not even NULL). In an INNER JOIN, rows with NULL in the join column are excluded because the ON condition evaluates to NULL (not TRUE).',
    tags: ['inner-join', 'null', 'join-condition'],
  },
  {
    id: 'pg-joins-l1-041',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What happens if a scalar subquery returns more than one row?',
    options: ['Only the first row is used', 'An error occurs', 'All rows are returned', 'The last row is used'],
    correctAnswer: 1,
    explanation: 'A scalar subquery must return at most one row. If it returns multiple rows, PostgreSQL raises an error: "more than one row returned by a subquery used as an expression".',
    tags: ['scalar-subquery', 'error', 'multiple-rows'],
  },
  {
    id: 'pg-joins-l1-042',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What happens if a scalar subquery returns no rows?',
    options: ['An error occurs', 'It returns NULL', 'It returns 0', 'It returns an empty string'],
    correctAnswer: 1,
    explanation: 'When a scalar subquery returns no rows, the result is NULL, not an error.',
    tags: ['scalar-subquery', 'null', 'empty-result'],
  },
  {
    id: 'pg-joins-l1-043',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'FULL JOIN',
    question: 'When would you use a FULL OUTER JOIN?',
    options: [
      'Always — it is the best join type',
      'When you need all rows from both tables, including unmatched rows from either side',
      'Only for self joins',
      'Never — it is not useful'
    ],
    correctAnswer: 1,
    explanation: 'FULL OUTER JOIN is useful for reconciliation tasks, finding mismatches between two data sets, or when you need a complete picture of both tables.',
    tags: ['full-join', 'use-case', 'reconciliation'],
  },
  {
    id: 'pg-joins-l1-044',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'Can you use multiple conditions in a JOIN ON clause?',
    options: ['No, only one condition is allowed', 'Yes, using AND and OR', 'Only with WHERE', 'Only with USING'],
    correctAnswer: 1,
    explanation: 'The ON clause supports complex conditions with AND, OR, and other operators, just like WHERE.',
    codeSnippet: 'SELECT * FROM orders o\nJOIN products p\n  ON o.product_id = p.id\n  AND o.region = p.available_region;',
    tags: ['on-clause', 'multiple-conditions', 'and'],
  },
  {
    id: 'pg-joins-l1-045',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'What is the difference between putting a filter in ON versus WHERE with a LEFT JOIN?',
    options: [
      'No difference',
      'ON filters before the join (preserving left rows); WHERE filters after the join (potentially removing left rows)',
      'WHERE is evaluated first',
      'ON only works with equalities'
    ],
    correctAnswer: 1,
    explanation: 'With LEFT JOIN, conditions in ON affect which right rows match but preserve all left rows. A condition in WHERE filters the final result, potentially eliminating left rows that had no match.',
    codeSnippet: '-- Shows all users, only recent orders:\nSELECT * FROM users u\nLEFT JOIN orders o\n  ON u.id = o.user_id\n  AND o.created_at > \'2024-01-01\';\n\n-- Excludes users without recent orders:\nSELECT * FROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nWHERE o.created_at > \'2024-01-01\';',
    tags: ['left-join', 'on-vs-where', 'filter-placement'],
  },
  {
    id: 'pg-joins-l1-046',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is the advantage of using EXISTS over IN with subqueries?',
    options: [
      'EXISTS is always faster',
      'EXISTS can stop as soon as it finds one match (short-circuits) and handles NULLs correctly',
      'IN is always better',
      'EXISTS returns actual data; IN only returns boolean'
    ],
    correctAnswer: 1,
    explanation: 'EXISTS stops evaluating as soon as the first matching row is found. IN with NULL values in the subquery can behave unexpectedly with NOT IN.',
    tags: ['exists-vs-in', 'performance', 'null-safety'],
  },
  {
    id: 'pg-joins-l1-047',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What happens if you omit the ON clause in a JOIN (not CROSS JOIN)?',
    options: ['It becomes a CROSS JOIN', 'An error occurs', 'It matches on primary keys automatically', 'It joins on all common column names'],
    correctAnswer: 1,
    explanation: 'An INNER JOIN or OUTER JOIN requires an ON or USING clause. Omitting it causes a syntax error. Only CROSS JOIN and NATURAL JOIN do not need ON.',
    tags: ['join', 'on-clause', 'syntax-error'],
  },
  {
    id: 'pg-joins-l1-048',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'What keyword starts a Common Table Expression?',
    options: ['CTE', 'WITH', 'DEFINE', 'DECLARE'],
    correctAnswer: 1,
    explanation: 'CTEs begin with the WITH keyword followed by the CTE name and the defining query.',
    tags: ['cte', 'with', 'keyword'],
  },
  {
    id: 'pg-joins-l1-049',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Joining Different Types',
    question: 'Can you JOIN a table with a view?',
    options: ['No, views cannot be joined', 'Yes, views can be used in JOINs just like tables', 'Only with subqueries', 'Only with materialized views'],
    correctAnswer: 1,
    explanation: 'Views are virtual tables. They can be used in JOINs, WHERE clauses, and everywhere a regular table can be used.',
    tags: ['view', 'join', 'virtual-table'],
  },
  {
    id: 'pg-joins-l1-050',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What is an equi-join?',
    options: ['A join that uses the EQUAL function', 'A join where the condition uses the equality (=) operator', 'A join that returns equal numbers of rows', 'A balanced join'],
    correctAnswer: 1,
    explanation: 'An equi-join uses the = operator in the join condition. Most JOINs are equi-joins (e.g., ON a.id = b.a_id). Non-equi joins use <, >, !=, etc.',
    tags: ['equi-join', 'equality', 'join-types'],
  },
  {
    id: 'pg-joins-l1-051',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Non-equi Join',
    question: 'What is a non-equi join?',
    options: ['A join without a condition', 'A join using operators other than = (like >, <, !=, BETWEEN)', 'An incorrect join', 'A join that returns no rows'],
    correctAnswer: 1,
    explanation: 'Non-equi joins use comparison operators other than =. For example, finding employees who earn more than their managers.',
    codeSnippet: 'SELECT e.name, e.salary, m.salary AS mgr_salary\nFROM employees e\nJOIN employees m ON e.manager_id = m.id\nWHERE e.salary > m.salary;',
    tags: ['non-equi-join', 'comparison', 'inequality'],
  },
  {
    id: 'pg-joins-l1-052',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What does the following return?\n\nSELECT * FROM users WHERE salary > (SELECT AVG(salary) FROM users);',
    options: ['All users', 'Users earning above the average salary', 'Only the highest earner', 'The average salary'],
    correctAnswer: 1,
    explanation: 'The subquery calculates the average salary. The outer query returns all users whose salary exceeds that average.',
    codeSnippet: 'SELECT * FROM users\nWHERE salary > (\n  SELECT AVG(salary) FROM users\n);',
    tags: ['subquery', 'comparison', 'avg'],
  },
  {
    id: 'pg-joins-l1-053',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CROSS JOIN',
    question: 'If table A has 100 rows and table B has 200 rows, how many rows does CROSS JOIN produce?',
    options: ['100', '200', '300', '20,000'],
    correctAnswer: 3,
    explanation: 'CROSS JOIN produces the Cartesian product: 100 × 200 = 20,000 rows. Be cautious with large tables.',
    tags: ['cross-join', 'cartesian', 'row-count'],
  },
  {
    id: 'pg-joins-l1-054',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Can a CTE reference itself?',
    options: ['No, CTEs cannot be self-referencing', 'Yes, using the RECURSIVE keyword', 'Only in stored procedures', 'Only with UNION'],
    correctAnswer: 1,
    explanation: 'A recursive CTE (WITH RECURSIVE) can reference itself. It has an anchor member and a recursive member joined by UNION ALL.',
    tags: ['recursive-cte', 'self-referencing', 'with-recursive'],
  },
  {
    id: 'pg-joins-l1-055',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is the advantage of a CTE over a subquery?',
    options: ['CTEs are always faster', 'CTEs improve readability and allow reuse of the same query multiple times', 'Subqueries are always better', 'CTEs use less memory'],
    correctAnswer: 1,
    explanation: 'CTEs make complex queries more readable by naming intermediate results. They can also be referenced multiple times, unlike a subquery which must be repeated.',
    tags: ['cte', 'readability', 'reuse'],
  },
  {
    id: 'pg-joins-l1-056',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'OUTER JOIN',
    question: 'Is LEFT OUTER JOIN the same as LEFT JOIN?',
    options: ['No, they are different', 'Yes, the OUTER keyword is optional', 'Only in PostgreSQL', 'LEFT OUTER JOIN is deprecated'],
    correctAnswer: 1,
    explanation: 'The OUTER keyword is optional in LEFT OUTER JOIN, RIGHT OUTER JOIN, and FULL OUTER JOIN. Most developers omit it for brevity.',
    tags: ['outer-join', 'optional-keyword', 'syntax'],
  },
  {
    id: 'pg-joins-l1-057',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Must a derived table (subquery in FROM) have an alias?',
    options: ['No, aliases are optional', 'Yes, PostgreSQL requires a derived table to have an alias', 'Only if it has joins', 'Only in newer PostgreSQL versions'],
    correctAnswer: 1,
    explanation: 'PostgreSQL requires every derived table (subquery in FROM) to have an alias. Without it, a syntax error occurs.',
    codeSnippet: '-- Correct:\nSELECT * FROM (SELECT 1 AS n) AS subq;\n\n-- Error: subquery in FROM must have an alias\nSELECT * FROM (SELECT 1 AS n);',
    tags: ['derived-table', 'alias', 'requirement'],
  },
  {
    id: 'pg-joins-l1-058',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'Which query finds customers who have placed orders?',
    options: [
      'SELECT c.name FROM customers c LEFT JOIN orders o ON c.id = o.customer_id WHERE o.id IS NULL;',
      'SELECT DISTINCT c.name FROM customers c JOIN orders o ON c.id = o.customer_id;',
      'SELECT c.name FROM customers c CROSS JOIN orders o;',
      'SELECT c.name FROM customers c WHERE c.id IS NULL;'
    ],
    correctAnswer: 1,
    explanation: 'INNER JOIN returns only customers who have matching orders. DISTINCT removes duplicates if a customer has multiple orders.',
    tags: ['inner-join', 'distinct', 'matching-rows'],
  },
  {
    id: 'pg-joins-l1-059',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'What is an "anti-join" pattern?',
    options: ['A join that reverses the result', 'Using LEFT JOIN + WHERE IS NULL to find rows with no match in the other table', 'A join that excludes all rows', 'A join that uses NOT'],
    correctAnswer: 1,
    explanation: 'An anti-join finds rows in one table that have no corresponding rows in another. LEFT JOIN with WHERE right_table.column IS NULL is a common pattern.',
    tags: ['anti-join', 'left-join', 'no-match'],
  },
  {
    id: 'pg-joins-l1-060',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can you use ORDER BY inside a subquery?',
    options: ['No, ORDER BY is only for the outer query', 'Yes, but it only affects the subquery result (useful with LIMIT)', 'It causes an error', 'Only in correlated subqueries'],
    correctAnswer: 1,
    explanation: 'ORDER BY in a subquery is meaningful when combined with LIMIT to get the top-N rows, or in certain contexts like ARRAY_AGG.',
    codeSnippet: 'SELECT * FROM users\nWHERE id IN (\n  SELECT user_id FROM orders\n  ORDER BY total DESC LIMIT 10\n);',
    tags: ['order-by', 'subquery', 'limit'],
  },
  {
    id: 'pg-joins-l1-061',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What is the implicit join syntax using a comma?',
    options: [
      'It does not exist',
      'SELECT * FROM a, b WHERE a.id = b.a_id — equivalent to INNER JOIN',
      'SELECT * FROM a + b',
      'SELECT * FROM a & b'
    ],
    correctAnswer: 1,
    explanation: 'The older comma-separated syntax puts join conditions in WHERE. It is equivalent to INNER JOIN but harder to read and error-prone for outer joins.',
    codeSnippet: '-- Old style (implicit join):\nSELECT * FROM a, b WHERE a.id = b.a_id;\n\n-- Modern style (explicit join):\nSELECT * FROM a JOIN b ON a.id = b.a_id;',
    tags: ['implicit-join', 'comma-syntax', 'old-style'],
  },
  {
    id: 'pg-joins-l1-062',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What does the following pattern find?\n\nSELECT * FROM products WHERE price = (SELECT MAX(price) FROM products);',
    options: ['The cheapest product', 'The most expensive product(s)', 'All products', 'The average priced products'],
    correctAnswer: 1,
    explanation: 'The subquery finds the maximum price. The outer query returns all products with that price (there could be ties).',
    codeSnippet: 'SELECT * FROM products\nWHERE price = (\n  SELECT MAX(price) FROM products\n);',
    tags: ['subquery', 'max', 'scalar'],
  },
  {
    id: 'pg-joins-l1-063',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'In a LEFT JOIN, which table\'s rows are always included in the result?',
    options: ['The right table', 'The left table (the one before LEFT JOIN)', 'Both tables', 'Neither — it depends on the data'],
    correctAnswer: 1,
    explanation: 'In a LEFT JOIN, all rows from the left table (written before the LEFT JOIN keywords) are always included in the result.',
    tags: ['left-join', 'preserved-table', 'basics'],
  },
  {
    id: 'pg-joins-l1-064',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Does a CTE persist after the query finishes?',
    options: ['Yes, like a temporary table', 'No, it only exists during the execution of the query', 'Yes, until the session ends', 'Yes, permanently'],
    correctAnswer: 1,
    explanation: 'A CTE exists only for the duration of the single SQL statement. After the query completes, the CTE is gone.',
    tags: ['cte', 'lifetime', 'temporary'],
  },
  {
    id: 'pg-joins-l1-065',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Conditions',
    question: 'Can you join tables on columns with different names?',
    options: ['No, column names must match', 'Yes, the ON clause can match any columns', 'Only with NATURAL JOIN', 'Only with USING'],
    correctAnswer: 1,
    explanation: 'The ON clause allows joining on any columns regardless of their names. For example: ON orders.customer_id = users.id.',
    tags: ['on-clause', 'different-names', 'join-condition'],
  },
  {
    id: 'pg-joins-l1-066',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'FULL JOIN',
    question: 'What does a FULL JOIN between table A (rows 1,2,3) and table B (rows 2,3,4) on their shared column produce?',
    options: [
      'Rows 2,3 only',
      'Rows 1(A only), 2(matched), 3(matched), 4(B only) — with NULLs for unmatched sides',
      'Rows 1,2,3,4 without NULLs',
      'An error'
    ],
    correctAnswer: 1,
    explanation: 'FULL JOIN includes all matched rows plus unmatched rows from both tables. Row 1 has no match in B (NULLs for B columns), row 4 has no match in A (NULLs for A columns).',
    tags: ['full-join', 'example', 'null-fill'],
  },
  {
    id: 'pg-joins-l1-067',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'Why is explicit JOIN syntax preferred over the comma syntax?',
    options: ['It is faster', 'It clearly separates join conditions (ON) from filter conditions (WHERE), reducing errors', 'There is no preference', 'Comma syntax is preferred'],
    correctAnswer: 1,
    explanation: 'Explicit JOIN syntax makes it clear which conditions are for joining and which are for filtering. With commas, accidentally omitting a join condition creates a Cartesian product.',
    tags: ['explicit-join', 'best-practice', 'readability'],
  },
  {
    id: 'pg-joins-l1-068',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can a subquery return multiple columns?',
    options: ['No, only one column', 'Yes, when used in FROM or with EXISTS', 'Only with special syntax', 'Only in stored procedures'],
    correctAnswer: 1,
    explanation: 'Subqueries in FROM (derived tables) can return multiple columns. EXISTS only checks for row existence. IN and comparisons require a single column.',
    tags: ['subquery', 'multiple-columns', 'derived-table'],
  },
  {
    id: 'pg-joins-l1-069',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Self Join',
    question: 'What common scenario requires a self join?',
    options: ['Joining two databases', 'Finding relationships within the same table, like employees and their managers', 'Duplicating data', 'Creating indexes'],
    correctAnswer: 1,
    explanation: 'Self joins are common for hierarchical data (employee-manager), finding pairs within a table, or comparing rows in the same table.',
    tags: ['self-join', 'hierarchy', 'use-case'],
  },
  {
    id: 'pg-joins-l1-070',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'USING',
    question: 'How does the result differ when using USING vs ON for a join?',
    options: [
      'No difference in results',
      'USING produces one copy of the shared column; ON produces both copies',
      'ON is faster',
      'USING allows multiple columns; ON does not'
    ],
    correctAnswer: 1,
    explanation: 'With USING(col), the column appears once in the result. With ON a.col = b.col, both a.col and b.col appear (though they have the same value for matched rows).',
    tags: ['using', 'on', 'result-difference'],
  },
  {
    id: 'pg-joins-l1-071',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Multiple Joins',
    question: 'In a query with multiple JOINs, in what order are they evaluated?',
    options: ['Right to left', 'Left to right as written, but the planner may reorder them', 'Alphabetically by table name', 'Randomly'],
    correctAnswer: 1,
    explanation: 'Logically, JOINs are evaluated left to right. However, the query planner may reorder INNER JOINs for efficiency (not outer joins).',
    tags: ['join-order', 'evaluation', 'planner'],
  },
  {
    id: 'pg-joins-l1-072',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What is a many-to-many relationship and how is it typically joined?',
    options: [
      'Direct join between two tables',
      'Uses a junction (bridge) table with foreign keys to both tables, requiring two JOINs',
      'It is not possible in SQL',
      'Uses CROSS JOIN'
    ],
    correctAnswer: 1,
    explanation: 'Many-to-many relationships use a junction table (e.g., student_courses) that has foreign keys to both related tables. Two JOINs connect through it.',
    codeSnippet: 'SELECT s.name, c.title\nFROM students s\nJOIN student_courses sc ON s.id = sc.student_id\nJOIN courses c ON sc.course_id = c.id;',
    tags: ['many-to-many', 'junction-table', 'two-joins'],
  },
  {
    id: 'pg-joins-l1-073',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CROSS JOIN',
    question: 'When is CROSS JOIN useful in practice?',
    options: ['Never — avoid it', 'For generating all combinations, such as all products × all regions, or for date calendars', 'Only for testing', 'For data migration'],
    correctAnswer: 1,
    explanation: 'CROSS JOIN is useful for generating combinations (product × color × size), creating calendar/time series data, or producing test data.',
    tags: ['cross-join', 'use-case', 'combinations'],
  },
  {
    id: 'pg-joins-l1-074',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is a correlated subquery in the SELECT list used for?',
    options: ['Joining tables', 'Computing a value for each row based on related data', 'Filtering rows', 'Sorting results'],
    correctAnswer: 1,
    explanation: 'A correlated subquery in SELECT computes a value per outer row, like counting related records or getting a max value per group.',
    codeSnippet: 'SELECT u.name,\n  (SELECT COUNT(*) FROM orders o WHERE o.user_id = u.id) AS order_count\nFROM users u;',
    tags: ['correlated', 'select-list', 'per-row'],
  },
  {
    id: 'pg-joins-l1-075',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'If you add a WHERE condition on a column from the right table of a LEFT JOIN (other than IS NULL), what happens?',
    options: [
      'Nothing changes',
      'The LEFT JOIN effectively becomes an INNER JOIN because non-matching rows have NULL and are filtered out',
      'An error occurs',
      'Only the right table is filtered'
    ],
    correctAnswer: 1,
    explanation: 'A WHERE condition on a right-table column (e.g., WHERE o.status = \'active\') filters out NULLs, effectively turning the LEFT JOIN into an INNER JOIN for those rows.',
    tags: ['left-join', 'where-filter', 'gotcha'],
  },
  {
    id: 'pg-joins-l1-076',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'NATURAL JOIN',
    question: 'Why is NATURAL JOIN generally discouraged in production code?',
    options: [
      'It is slower than explicit joins',
      'Adding or renaming columns can silently change the join behavior',
      'It does not work in PostgreSQL',
      'It always produces a CROSS JOIN'
    ],
    correctAnswer: 1,
    explanation: 'NATURAL JOIN implicitly matches column names. If columns are added or renamed, the join condition changes silently, potentially causing subtle bugs.',
    tags: ['natural-join', 'risk', 'best-practice'],
  },
  {
    id: 'pg-joins-l1-077',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can subqueries be nested inside other subqueries?',
    options: ['No, only one level is allowed', 'Yes, subqueries can be nested to any depth', 'Only two levels deep', 'Only in PostgreSQL 14+'],
    correctAnswer: 1,
    explanation: 'Subqueries can be nested multiple levels deep. However, deeply nested subqueries become hard to read — CTEs are often better.',
    tags: ['nested-subquery', 'depth', 'readability'],
  },
  {
    id: 'pg-joins-l1-078',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'What is the main advantage of recursive CTEs?',
    options: ['They are faster than regular queries', 'They can traverse hierarchical or graph data (trees, paths)', 'They use less memory', 'They are easier to write'],
    correctAnswer: 1,
    explanation: 'Recursive CTEs excel at traversing hierarchical data like org charts, category trees, bill of materials, or graph paths.',
    tags: ['recursive-cte', 'hierarchy', 'graph'],
  },
  {
    id: 'pg-joins-l1-079',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What is a "semi-join" conceptually?',
    options: ['Half of a join', 'A join that returns only rows from the left table where at least one match exists in the right table', 'A join with only half the columns', 'A partial CROSS JOIN'],
    correctAnswer: 1,
    explanation: 'A semi-join returns rows from one table where a match exists in another, without duplicating rows for multiple matches. EXISTS and IN implement semi-joins.',
    tags: ['semi-join', 'exists', 'concept'],
  },
  {
    id: 'pg-joins-l1-080',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is the difference between an inline subquery and a CTE?',
    options: [
      'CTEs are faster',
      'Both define reusable query blocks, but CTEs are named and defined before the main query for readability',
      'Subqueries cannot be correlated',
      'CTEs persist across queries'
    ],
    correctAnswer: 1,
    explanation: 'CTEs (WITH clauses) name and define query blocks before the main query. Inline subqueries are embedded directly. CTEs are often more readable, especially when reused.',
    tags: ['cte', 'subquery', 'comparison'],
  },
  {
    id: 'pg-joins-l1-081',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What does the following query find?\n\nSELECT * FROM a JOIN b ON a.x = b.y WHERE a.status = \'active\';',
    options: ['All rows from a and b', 'Active rows from a that have matching rows in b', 'All active rows', 'Rows where x equals y in the same table'],
    correctAnswer: 1,
    explanation: 'The INNER JOIN returns only matching rows. The WHERE clause further filters to only rows where a.status is \'active\'.',
    codeSnippet: 'SELECT * FROM a\nJOIN b ON a.x = b.y\nWHERE a.status = \'active\';',
    tags: ['inner-join', 'where', 'combined-filter'],
  },
  {
    id: 'pg-joins-l1-082',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'LEFT JOIN',
    question: 'How many rows can a LEFT JOIN return compared to the left table?',
    options: ['Always the same number', 'At least as many rows as the left table, possibly more if there are multiple matches', 'Always more rows', 'Always fewer rows'],
    correctAnswer: 1,
    explanation: 'LEFT JOIN preserves all left rows. If a left row matches multiple right rows, it produces multiple result rows. If no match, one row with NULLs.',
    tags: ['left-join', 'row-count', 'one-to-many'],
  },
  {
    id: 'pg-joins-l1-083',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Is a CTE the same as a temporary table?',
    options: ['Yes, they are identical', 'No, a CTE exists only for one query; a temp table persists for the session', 'CTEs are faster than temp tables', 'Temp tables are for CTEs only'],
    correctAnswer: 1,
    explanation: 'A CTE is query-scoped and disappears after the statement. A temporary table persists for the duration of the database session.',
    tags: ['cte', 'temp-table', 'comparison'],
  },
  {
    id: 'pg-joins-l1-084',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What does WHERE NOT IN (subquery) return if the subquery result includes NULL?',
    options: ['Normal results excluding subquery values', 'No rows at all', 'Only NULL rows', 'An error'],
    correctAnswer: 1,
    explanation: 'NOT IN returns no rows if the subquery contains NULL because value != NULL evaluates to NULL (unknown), making the entire NOT IN condition false. Use NOT EXISTS instead.',
    tags: ['not-in', 'null', 'gotcha'],
  },
  {
    id: 'pg-joins-l1-085',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'INNER JOIN',
    question: 'What are the three most common JOIN types used in everyday queries?',
    options: ['CROSS, NATURAL, SELF', 'INNER JOIN, LEFT JOIN, and LEFT JOIN with WHERE IS NULL (anti-join)', 'RIGHT, FULL, CROSS', 'MERGE, HASH, NESTED LOOP'],
    correctAnswer: 1,
    explanation: 'INNER JOIN (matching rows), LEFT JOIN (all from left + matches), and LEFT JOIN + WHERE IS NULL (anti-join) cover the vast majority of join use cases.',
    tags: ['join-types', 'common', 'best-practice'],
  },
  {
    id: 'pg-joins-l1-086',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can you use LIMIT inside a subquery?',
    options: ['No, LIMIT is only for the outer query', 'Yes, to restrict the subquery results', 'Only in PostgreSQL', 'Only with ORDER BY'],
    correctAnswer: 1,
    explanation: 'LIMIT can be used inside subqueries to restrict results. Combined with ORDER BY, it selects top-N rows from the subquery.',
    tags: ['limit', 'subquery', 'top-n'],
  },
  {
    id: 'pg-joins-l1-087',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'USING',
    question: 'Can USING reference multiple columns?',
    options: ['No, only one column', 'Yes, USING (col1, col2) joins on both columns', 'Only with NATURAL JOIN', 'Only for composite keys'],
    correctAnswer: 1,
    explanation: 'USING can specify multiple columns: USING (col1, col2) is equivalent to ON a.col1 = b.col1 AND a.col2 = b.col2.',
    codeSnippet: 'SELECT * FROM orders\nJOIN shipments USING (order_id, warehouse_id);',
    tags: ['using', 'multiple-columns', 'composite'],
  },
  {
    id: 'pg-joins-l1-088',
    language: 'postgresql',
    level: 'level1',
    category: 'JOINs & Subqueries',
    subcategory: 'EXISTS',
    question: 'Why do people often write SELECT 1 inside an EXISTS subquery?',
    options: ['To return the number 1', 'Because EXISTS only checks for row existence — the SELECT list does not matter', 'It is required syntax', 'For performance'],
    correctAnswer: 1,
    explanation: 'EXISTS only checks if the subquery returns any rows. The actual values in the SELECT list are irrelevant. SELECT 1, SELECT *, or any expression all work the same.',
    codeSnippet: 'WHERE EXISTS (SELECT 1 FROM orders WHERE ...)\n-- Same as:\nWHERE EXISTS (SELECT * FROM orders WHERE ...)',
    tags: ['exists', 'select-1', 'convention'],
  },
];

export default questions;
