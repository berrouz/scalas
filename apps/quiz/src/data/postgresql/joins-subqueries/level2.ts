import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'pg-joins-l2-001',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'What does the MATERIALIZED hint do on a CTE in PostgreSQL 12+?',
    options: ['Creates a materialized view', 'Forces the CTE to be evaluated once and stored in a temp buffer, preventing the optimizer from inlining it', 'Makes the CTE persistent', 'Caches the CTE across sessions'],
    correctAnswer: 1,
    explanation: 'WITH cte AS MATERIALIZED (...) forces the CTE result to be computed once and stored. This prevents the optimizer from pushing filters into the CTE, which can be useful or harmful.',
    codeSnippet: 'WITH active AS MATERIALIZED (\n  SELECT * FROM users WHERE active = true\n)\nSELECT * FROM active WHERE name LIKE \'A%\';',
    tags: ['cte', 'materialized', 'optimization-fence'],
  },
  {
    id: 'pg-joins-l2-002',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'What does the NOT MATERIALIZED hint do on a CTE?',
    options: ['Prevents the CTE from being materialized', 'Allows the optimizer to inline the CTE, pushing predicates down into it', 'Deletes the CTE result', 'Makes the CTE read-only'],
    correctAnswer: 1,
    explanation: 'NOT MATERIALIZED tells PostgreSQL to treat the CTE like a subquery, allowing predicate pushdown and other optimizations. This is the default for non-recursive CTEs in PG 12+.',
    codeSnippet: 'WITH active AS NOT MATERIALIZED (\n  SELECT * FROM users WHERE active = true\n)\nSELECT * FROM active WHERE age > 25;\n-- Optimizer can push age > 25 into the CTE scan',
    tags: ['cte', 'not-materialized', 'inlining'],
  },
  {
    id: 'pg-joins-l2-003',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'What terminates the recursion in a recursive CTE?',
    options: ['A STOP keyword', 'When the recursive member returns no rows', 'After a fixed number of iterations', 'When LIMIT is reached'],
    correctAnswer: 1,
    explanation: 'Recursion stops when the recursive term returns zero rows. If it never returns zero rows, it runs forever (or until canceled). Use LIMIT or a depth counter as a safety measure.',
    tags: ['recursive-cte', 'termination', 'base-case'],
  },
  {
    id: 'pg-joins-l2-004',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'What is the difference between using UNION and UNION ALL in a recursive CTE?',
    options: ['No difference', 'UNION removes duplicates per iteration (preventing cycles); UNION ALL does not', 'UNION ALL is not allowed in recursive CTEs', 'UNION is faster'],
    correctAnswer: 1,
    explanation: 'UNION in a recursive CTE removes duplicate rows at each iteration, which can prevent infinite loops in graph traversals. UNION ALL keeps all rows and is more common.',
    tags: ['recursive-cte', 'union', 'cycle-prevention'],
  },
  {
    id: 'pg-joins-l2-005',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'How can you detect and prevent infinite recursion in a recursive CTE for graph data?',
    options: [
      'PostgreSQL handles it automatically',
      'Track visited nodes in an array and check before recursing, or use CYCLE detection (PG 14+)',
      'Set a global recursion limit',
      'Use UNION instead of UNION ALL'
    ],
    correctAnswer: 1,
    explanation: 'Accumulate visited node IDs in an array column and check if the next node was already visited. PostgreSQL 14 added built-in CYCLE detection clause.',
    codeSnippet: 'WITH RECURSIVE traverse AS (\n  SELECT id, ARRAY[id] AS path, false AS is_cycle\n  FROM nodes WHERE id = 1\n  UNION ALL\n  SELECT n.id, t.path || n.id, n.id = ANY(t.path)\n  FROM nodes n JOIN traverse t ON n.parent_id = t.id\n  WHERE NOT t.is_cycle\n)\nSELECT * FROM traverse;',
    tags: ['recursive-cte', 'cycle-detection', 'graph'],
  },
  {
    id: 'pg-joins-l2-006',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'What does LATERAL JOIN allow that a regular subquery in FROM does not?',
    options: ['Parallel execution', 'The subquery can reference columns from preceding tables in the FROM clause', 'Joining more than two tables', 'Using ORDER BY in the subquery'],
    correctAnswer: 1,
    explanation: 'A LATERAL subquery can reference columns from tables that appear before it in FROM. Without LATERAL, a subquery in FROM is evaluated independently.',
    codeSnippet: 'SELECT u.name, latest.title\nFROM users u\nLATERAL (\n  SELECT title FROM posts p\n  WHERE p.user_id = u.id\n  ORDER BY p.created_at DESC\n  LIMIT 3\n) latest;',
    tags: ['lateral', 'correlated-from', 'cross-reference'],
  },
  {
    id: 'pg-joins-l2-007',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'What is the default join type when using LATERAL with a comma?',
    options: ['LEFT JOIN', 'INNER JOIN (CROSS JOIN LATERAL)', 'FULL JOIN', 'No join — it is a separate query'],
    correctAnswer: 1,
    explanation: 'FROM a, LATERAL (...) is equivalent to FROM a CROSS JOIN LATERAL (...), which acts like an INNER JOIN. Use LEFT JOIN LATERAL for preserving rows with no match.',
    tags: ['lateral', 'cross-join', 'implicit'],
  },
  {
    id: 'pg-joins-l2-008',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'When would you use LEFT JOIN LATERAL instead of CROSS JOIN LATERAL?',
    options: ['They are the same', 'When you want to keep outer rows even if the lateral subquery returns no rows', 'When the subquery returns multiple rows', 'For better performance'],
    correctAnswer: 1,
    explanation: 'LEFT JOIN LATERAL ON TRUE preserves outer rows even when the lateral subquery returns no rows (NULLs for lateral columns). CROSS JOIN LATERAL excludes those outer rows.',
    codeSnippet: 'SELECT u.name, latest.title\nFROM users u\nLEFT JOIN LATERAL (\n  SELECT title FROM posts p\n  WHERE p.user_id = u.id\n  ORDER BY created_at DESC LIMIT 1\n) latest ON TRUE;',
    tags: ['lateral', 'left-join', 'preserve-rows'],
  },
  {
    id: 'pg-joins-l2-009',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Semi-join',
    question: 'How does PostgreSQL internally execute an EXISTS subquery?',
    options: ['As a full subquery scan', 'Often as a semi-join, which stops after finding the first match', 'As a CROSS JOIN', 'As a separate query'],
    correctAnswer: 1,
    explanation: 'PostgreSQL can transform EXISTS into a semi-join internally. A semi-join returns the outer row once a match is found, without scanning all matching inner rows.',
    tags: ['semi-join', 'exists', 'optimization'],
  },
  {
    id: 'pg-joins-l2-010',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Anti-join',
    question: 'Which three methods can implement an anti-join (rows with no match)?',
    options: [
      'INNER JOIN, CROSS JOIN, NATURAL JOIN',
      'LEFT JOIN WHERE IS NULL, NOT EXISTS, NOT IN',
      'FULL JOIN, UNION, INTERSECT',
      'DELETE, UPDATE, INSERT'
    ],
    correctAnswer: 1,
    explanation: 'Anti-joins find rows without matches. LEFT JOIN + WHERE IS NULL, NOT EXISTS, and NOT IN all work, but NOT IN has NULL gotchas. NOT EXISTS is generally safest.',
    tags: ['anti-join', 'methods', 'comparison'],
  },
  {
    id: 'pg-joins-l2-011',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Anti-join',
    question: 'Why is NOT EXISTS generally preferred over NOT IN for anti-joins?',
    options: ['NOT EXISTS is faster', 'NOT EXISTS handles NULLs correctly; NOT IN returns no rows if the subquery contains any NULL', 'NOT EXISTS uses less memory', 'No difference'],
    correctAnswer: 1,
    explanation: 'NOT IN with NULLs in the subquery returns empty results. NOT EXISTS handles NULLs correctly because it checks for row existence, not value equality.',
    codeSnippet: '-- Safe: NOT EXISTS\nSELECT * FROM a\nWHERE NOT EXISTS (SELECT 1 FROM b WHERE b.id = a.id);\n\n-- Risky if b.id has NULLs:\nSELECT * FROM a WHERE a.id NOT IN (SELECT id FROM b);',
    tags: ['not-exists', 'not-in', 'null-safety'],
  },
  {
    id: 'pg-joins-l2-012',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Correlated Subquery',
    question: 'How does a correlated subquery differ from a regular subquery in terms of execution?',
    options: ['No difference', 'A correlated subquery is conceptually re-evaluated for each outer row; a regular subquery runs once', 'Correlated subqueries run in parallel', 'Regular subqueries run per row too'],
    correctAnswer: 1,
    explanation: 'Conceptually, a correlated subquery runs once per outer row. The optimizer may transform it into a join for efficiency, but the semantics are per-row evaluation.',
    tags: ['correlated', 'execution', 'per-row'],
  },
  {
    id: 'pg-joins-l2-013',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Correlated Subquery',
    question: 'How do you find employees who earn more than the average salary in their department?',
    options: [
      'Using a simple WHERE > AVG(salary)',
      'Using a correlated subquery: WHERE salary > (SELECT AVG(salary) FROM employees e2 WHERE e2.dept_id = e1.dept_id)',
      'Using HAVING',
      'Using DISTINCT'
    ],
    correctAnswer: 1,
    explanation: 'The correlated subquery computes the department average for each employee\'s department and compares their salary against it.',
    codeSnippet: 'SELECT e1.name, e1.salary, e1.dept_id\nFROM employees e1\nWHERE e1.salary > (\n  SELECT AVG(e2.salary)\n  FROM employees e2\n  WHERE e2.dept_id = e1.dept_id\n);',
    tags: ['correlated', 'avg', 'department'],
  },
  {
    id: 'pg-joins-l2-014',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Derived Tables',
    question: 'Can you join a derived table with another table?',
    options: ['No, derived tables cannot be joined', 'Yes, derived tables can be joined like regular tables', 'Only with CROSS JOIN', 'Only in CTEs'],
    correctAnswer: 1,
    explanation: 'Derived tables (subqueries in FROM) can be joined with other tables or derived tables using any join type.',
    codeSnippet: 'SELECT u.name, stats.order_count\nFROM users u\nJOIN (\n  SELECT user_id, COUNT(*) AS order_count\n  FROM orders GROUP BY user_id\n) stats ON u.id = stats.user_id;',
    tags: ['derived-table', 'join', 'aggregation'],
  },
  {
    id: 'pg-joins-l2-015',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Multiple CTEs',
    question: 'Can a later CTE reference an earlier CTE in the same WITH clause?',
    options: ['No, CTEs are independent', 'Yes, later CTEs can reference earlier ones', 'Only the last CTE can reference others', 'Only with RECURSIVE'],
    correctAnswer: 1,
    explanation: 'CTEs are evaluated in order. A later CTE can reference any CTE defined before it in the same WITH clause.',
    codeSnippet: 'WITH\n  a AS (SELECT * FROM users WHERE active),\n  b AS (SELECT * FROM a WHERE age > 25)  -- references CTE a\nSELECT * FROM b;',
    tags: ['cte', 'chaining', 'dependencies'],
  },
  {
    id: 'pg-joins-l2-016',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Writable CTE',
    question: 'What is a writable (data-modifying) CTE?',
    options: ['A CTE that creates tables', 'A CTE containing INSERT, UPDATE, or DELETE that can chain with RETURNING', 'A CTE with write-ahead logging', 'A CTE stored permanently'],
    correctAnswer: 1,
    explanation: 'PostgreSQL allows INSERT/UPDATE/DELETE in CTEs, using RETURNING to pass affected rows to the main query or other CTEs.',
    codeSnippet: 'WITH moved AS (\n  DELETE FROM old_orders\n  WHERE created_at < \'2020-01-01\'\n  RETURNING *\n)\nINSERT INTO archive_orders\nSELECT * FROM moved;',
    tags: ['writable-cte', 'data-modifying', 'returning'],
  },
  {
    id: 'pg-joins-l2-017',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Self Join',
    question: 'How do you find all pairs of employees in the same department?',
    options: [
      'Using GROUP BY department',
      'Self join with a condition to avoid pairing an employee with themselves',
      'Using DISTINCT',
      'Using UNION'
    ],
    correctAnswer: 1,
    explanation: 'Self join the employees table twice and match on department. Use e1.id < e2.id to avoid duplicate pairs and self-pairs.',
    codeSnippet: 'SELECT e1.name, e2.name, e1.department\nFROM employees e1\nJOIN employees e2\n  ON e1.department = e2.department\n  AND e1.id < e2.id;',
    tags: ['self-join', 'pairs', 'department'],
  },
  {
    id: 'pg-joins-l2-018',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'How do you find the second highest salary using a subquery?',
    options: [
      'SELECT MAX(salary) FROM employees',
      'SELECT MAX(salary) FROM employees WHERE salary < (SELECT MAX(salary) FROM employees)',
      'SELECT salary FROM employees ORDER BY salary LIMIT 2',
      'SELECT DISTINCT salary FROM employees ORDER BY salary OFFSET 1'
    ],
    correctAnswer: 1,
    explanation: 'The inner subquery finds the maximum salary. The outer query finds the maximum salary that is less than the overall maximum — the second highest.',
    codeSnippet: 'SELECT MAX(salary)\nFROM employees\nWHERE salary < (\n  SELECT MAX(salary) FROM employees\n);',
    tags: ['subquery', 'second-highest', 'nested'],
  },
  {
    id: 'pg-joins-l2-019',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'How do you generate a series of dates for the last 30 days using a recursive CTE?',
    options: [
      'Using a loop',
      'With an anchor of CURRENT_DATE and recursing backwards subtracting 1 day each step',
      'Using GENERATE_SERIES only',
      'With a stored procedure'
    ],
    correctAnswer: 1,
    explanation: 'The anchor starts at the current date. The recursive part subtracts 1 day until reaching 30 days back. (generate_series is simpler for this, but recursive CTE also works.)',
    codeSnippet: 'WITH RECURSIVE dates AS (\n  SELECT CURRENT_DATE AS d\n  UNION ALL\n  SELECT d - 1 FROM dates\n  WHERE d > CURRENT_DATE - 29\n)\nSELECT d FROM dates ORDER BY d;',
    tags: ['recursive-cte', 'date-series', 'generate'],
  },
  {
    id: 'pg-joins-l2-020',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CROSS JOIN',
    question: 'How would you generate all possible color-size combinations for products?',
    options: [
      'Using INNER JOIN',
      'Using CROSS JOIN between colors and sizes tables',
      'Using UNION',
      'Using GROUP BY'
    ],
    correctAnswer: 1,
    explanation: 'CROSS JOIN produces every combination. If you have 5 colors and 4 sizes, you get 20 rows representing all possible combinations.',
    codeSnippet: 'SELECT c.name AS color, s.name AS size\nFROM colors c\nCROSS JOIN sizes s\nORDER BY c.name, s.name;',
    tags: ['cross-join', 'combinations', 'product'],
  },
  {
    id: 'pg-joins-l2-021',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subquery Optimization',
    question: 'Can PostgreSQL transform an IN subquery into a join?',
    options: ['No, they always execute differently', 'Yes, the optimizer can transform IN subqueries into semi-joins', 'Only with EXPLAIN ANALYZE', 'Only for small subqueries'],
    correctAnswer: 1,
    explanation: 'PostgreSQL\'s optimizer can transform IN (SELECT ...) into a semi-join, which is typically more efficient. The transformation happens automatically.',
    tags: ['in', 'semi-join', 'optimization'],
  },
  {
    id: 'pg-joins-l2-022',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Multiple Joins',
    question: 'What is the maximum number of tables you can JOIN in a single PostgreSQL query?',
    options: ['10', '64', 'There is no hard limit, but join_collapse_limit affects planning', '256'],
    correctAnswer: 2,
    explanation: 'There is no strict limit on JOIN count. However, join_collapse_limit (default 8) controls how many tables the planner tries to reorder. Beyond that, planning time may increase.',
    tags: ['join-limit', 'join-collapse-limit', 'planning'],
  },
  {
    id: 'pg-joins-l2-023',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join with Aggregation',
    question: 'How do you count orders per user while keeping all users visible, including those with zero orders?',
    options: [
      'INNER JOIN with COUNT',
      'LEFT JOIN with COUNT on the right table\'s column (not *)',
      'CROSS JOIN with COUNT',
      'RIGHT JOIN with COUNT'
    ],
    correctAnswer: 1,
    explanation: 'LEFT JOIN preserves all users. COUNT(o.id) counts non-NULL values, returning 0 for users without orders. COUNT(*) would return 1 instead of 0.',
    codeSnippet: 'SELECT u.name, COUNT(o.id) AS order_count\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nGROUP BY u.name;',
    tags: ['left-join', 'count', 'zero-counts'],
  },
  {
    id: 'pg-joins-l2-024',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join with Aggregation',
    question: 'What is the problem with COUNT(*) in a LEFT JOIN when you want to count related rows?',
    options: ['It is always correct', 'COUNT(*) returns 1 for rows with no match instead of 0', 'COUNT(*) causes an error', 'COUNT(*) returns NULL'],
    correctAnswer: 1,
    explanation: 'LEFT JOIN produces one row with NULLs for non-matching rows. COUNT(*) counts this NULL row as 1. Use COUNT(right_table.column) to get 0 for unmatched rows.',
    tags: ['count-star', 'left-join', 'gotcha'],
  },
  {
    id: 'pg-joins-l2-025',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries in SELECT',
    question: 'What is the performance concern with correlated subqueries in the SELECT list?',
    options: ['They use too much memory', 'They are conceptually executed once per outer row, which can be slow for large result sets', 'They cause locking issues', 'No performance concern'],
    correctAnswer: 1,
    explanation: 'A correlated subquery in SELECT is re-evaluated for each outer row. For millions of rows, this can be much slower than a single JOIN with GROUP BY.',
    tags: ['correlated', 'performance', 'select-list'],
  },
  {
    id: 'pg-joins-l2-026',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries in SELECT',
    question: 'How can you rewrite a correlated subquery in SELECT as a JOIN?',
    options: [
      'It is not possible',
      'Replace the subquery with a LEFT JOIN to a derived table that computes the same aggregation',
      'Using UNION',
      'Using CROSS JOIN'
    ],
    correctAnswer: 1,
    explanation: 'A correlated subquery like (SELECT COUNT(*) FROM orders WHERE user_id = u.id) can be rewritten as LEFT JOIN (SELECT user_id, COUNT(*) ... GROUP BY user_id) for better performance.',
    codeSnippet: '-- Correlated (slow for many users):\nSELECT u.name,\n  (SELECT COUNT(*) FROM orders o WHERE o.user_id = u.id)\nFROM users u;\n\n-- JOIN (faster):\nSELECT u.name, COALESCE(c.cnt, 0)\nFROM users u\nLEFT JOIN (\n  SELECT user_id, COUNT(*) AS cnt\n  FROM orders GROUP BY user_id\n) c ON u.id = c.user_id;',
    tags: ['rewrite', 'correlated-to-join', 'performance'],
  },
  {
    id: 'pg-joins-l2-027',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'How do you get the top-N items per group using LATERAL?',
    options: [
      'Using GROUP BY with LIMIT',
      'Using LEFT JOIN LATERAL with ORDER BY and LIMIT inside the lateral subquery',
      'Using window functions only',
      'Using DISTINCT ON'
    ],
    correctAnswer: 1,
    explanation: 'LATERAL allows a per-row subquery with ORDER BY and LIMIT, effectively getting top-N per group. This is often the most efficient approach.',
    codeSnippet: 'SELECT c.name, p.title, p.created_at\nFROM categories c\nLEFT JOIN LATERAL (\n  SELECT title, created_at\n  FROM posts\n  WHERE category_id = c.id\n  ORDER BY created_at DESC\n  LIMIT 5\n) p ON TRUE;',
    tags: ['lateral', 'top-n-per-group', 'limit'],
  },
  {
    id: 'pg-joins-l2-028',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'How do you compute the path from a node to the root in a tree using a recursive CTE?',
    options: [
      'Using repeated self-joins',
      'Starting from the node and recursing up through parent_id, accumulating the path',
      'Using an array column only',
      'It requires a stored procedure'
    ],
    correctAnswer: 1,
    explanation: 'Start from the target node as the anchor. The recursive term joins on parent_id, climbing up the tree. Accumulate node names/ids in an array or string.',
    codeSnippet: 'WITH RECURSIVE ancestors AS (\n  SELECT id, name, parent_id, ARRAY[name] AS path\n  FROM categories WHERE id = 42\n  UNION ALL\n  SELECT c.id, c.name, c.parent_id, c.name || a.path\n  FROM categories c\n  JOIN ancestors a ON c.id = a.parent_id\n)\nSELECT * FROM ancestors WHERE parent_id IS NULL;',
    tags: ['recursive-cte', 'path', 'tree-traversal'],
  },
  {
    id: 'pg-joins-l2-029',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'What does the SEARCH clause do in a recursive CTE (PostgreSQL 14+)?',
    options: ['Performs a text search', 'Defines the traversal order: BREADTH FIRST or DEPTH FIRST', 'Searches for cycles', 'Limits the search depth'],
    correctAnswer: 1,
    explanation: 'The SEARCH clause controls traversal order. SEARCH BREADTH FIRST BY col processes all nodes at each level before going deeper. DEPTH FIRST explores one branch fully before the next.',
    codeSnippet: 'WITH RECURSIVE tree AS (\n  SELECT id, parent_id, name FROM nodes WHERE parent_id IS NULL\n  UNION ALL\n  SELECT n.id, n.parent_id, n.name\n  FROM nodes n JOIN tree t ON n.parent_id = t.id\n)\nSEARCH DEPTH FIRST BY id SET ordercol\nSELECT * FROM tree ORDER BY ordercol;',
    tags: ['search', 'breadth-first', 'depth-first'],
  },
  {
    id: 'pg-joins-l2-030',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'What does the CYCLE clause do in a recursive CTE (PostgreSQL 14+)?',
    options: ['Creates a loop', 'Automatically detects and marks cycles in the recursion', 'Limits iterations', 'Cycles through data types'],
    correctAnswer: 1,
    explanation: 'CYCLE col SET is_cycle USING path_column automatically tracks visited values and stops recursion when a cycle is detected.',
    codeSnippet: 'WITH RECURSIVE traverse AS (\n  SELECT id, parent_id FROM graph WHERE id = 1\n  UNION ALL\n  SELECT g.id, g.parent_id\n  FROM graph g JOIN traverse t ON g.parent_id = t.id\n)\nCYCLE id SET is_cycle USING path\nSELECT * FROM traverse WHERE NOT is_cycle;',
    tags: ['cycle', 'recursive-cte', 'postgresql-14'],
  },
  {
    id: 'pg-joins-l2-031',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you find rows in table A that have no match in table B using a FULL OUTER JOIN?',
    options: [
      'WHERE a.id IS NULL OR b.id IS NULL',
      'WHERE a.id IS NULL (B-only rows) or WHERE b.id IS NULL (A-only rows)',
      'WHERE a.id = b.id',
      'Using HAVING'
    ],
    correctAnswer: 1,
    explanation: 'In a FULL JOIN, WHERE b.id IS NULL gives rows only in A, WHERE a.id IS NULL gives rows only in B. Combining with OR gives all unmatched rows.',
    codeSnippet: 'SELECT *\nFROM table_a a\nFULL OUTER JOIN table_b b ON a.id = b.id\nWHERE a.id IS NULL OR b.id IS NULL;',
    tags: ['full-join', 'unmatched', 'difference'],
  },
  {
    id: 'pg-joins-l2-032',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subquery vs JOIN',
    question: 'In general, when is a JOIN more efficient than a correlated subquery?',
    options: [
      'Never — subqueries are always better',
      'When you need to retrieve or aggregate data from the related table for every row; JOINs allow set-based processing',
      'JOINs are only better for small tables',
      'When the subquery is simple'
    ],
    correctAnswer: 1,
    explanation: 'JOINs allow the optimizer to use hash/merge joins for set-based processing, while correlated subqueries conceptually execute per-row. For aggregating related data, JOINs are typically faster.',
    tags: ['join-vs-subquery', 'performance', 'optimization'],
  },
  {
    id: 'pg-joins-l2-033',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'DISTINCT ON + JOIN',
    question: 'How can you get the most recent order for each user using DISTINCT ON?',
    options: [
      'DISTINCT with GROUP BY',
      'SELECT DISTINCT ON (user_id) from orders joined with users, ordered by user_id and created_at DESC',
      'Using MAX(created_at)',
      'Using LIMIT 1'
    ],
    correctAnswer: 1,
    explanation: 'DISTINCT ON (user_id) combined with ORDER BY user_id, created_at DESC returns the first (most recent) row per user_id.',
    codeSnippet: 'SELECT DISTINCT ON (o.user_id)\n  u.name, o.*\nFROM orders o\nJOIN users u ON o.user_id = u.id\nORDER BY o.user_id, o.created_at DESC;',
    tags: ['distinct-on', 'latest-per-group', 'join'],
  },
  {
    id: 'pg-joins-l2-034',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you find customers with exactly 3 orders?',
    options: [
      'WHERE COUNT(orders) = 3',
      'JOIN orders, GROUP BY customer, HAVING COUNT(*) = 3',
      'WHERE orders = 3',
      'Using LIMIT 3'
    ],
    correctAnswer: 1,
    explanation: 'Join the tables, group by customer, and use HAVING to filter groups by their count.',
    codeSnippet: 'SELECT c.name, COUNT(*) AS order_count\nFROM customers c\nJOIN orders o ON c.id = o.customer_id\nGROUP BY c.name\nHAVING COUNT(*) = 3;',
    tags: ['having', 'count', 'exact-match'],
  },
  {
    id: 'pg-joins-l2-035',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Multiple Joins',
    question: 'When joining 4+ tables, what determines the join order?',
    options: [
      'Always left to right as written',
      'The PostgreSQL query planner chooses the optimal order for INNER JOINs based on statistics',
      'Alphabetical by table name',
      'The DBA specifies the order'
    ],
    correctAnswer: 1,
    explanation: 'The planner evaluates different join orderings (up to join_collapse_limit tables) and picks the cheapest plan based on statistics. Outer joins preserve their written order.',
    tags: ['join-order', 'planner', 'optimization'],
  },
  {
    id: 'pg-joins-l2-036',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subquery with UPDATE',
    question: 'Can you use a subquery in an UPDATE statement?',
    options: ['No, UPDATE does not support subqueries', 'Yes, both in SET and WHERE clauses', 'Only in WHERE', 'Only in SET'],
    correctAnswer: 1,
    explanation: 'Subqueries can be used in UPDATE\'s SET clause (to compute new values) and WHERE clause (to filter which rows to update).',
    codeSnippet: 'UPDATE products\nSET price = (\n  SELECT AVG(price) FROM products\n)\nWHERE category_id IN (\n  SELECT id FROM categories WHERE name = \'Clearance\'\n);',
    tags: ['subquery', 'update', 'set-where'],
  },
  {
    id: 'pg-joins-l2-037',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subquery with DELETE',
    question: 'How do you delete rows based on a condition in another table?',
    options: [
      'DELETE cannot reference other tables',
      'Using DELETE ... WHERE EXISTS (subquery) or DELETE ... USING other_table',
      'Using DROP TABLE',
      'Using TRUNCATE'
    ],
    correctAnswer: 1,
    explanation: 'Both WHERE EXISTS and PostgreSQL\'s USING clause allow deleting rows based on conditions in other tables.',
    codeSnippet: 'DELETE FROM orders o\nWHERE EXISTS (\n  SELECT 1 FROM customers c\n  WHERE c.id = o.customer_id AND c.status = \'deleted\'\n);',
    tags: ['delete', 'subquery', 'exists'],
  },
  {
    id: 'pg-joins-l2-038',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subquery in FROM',
    question: 'What is a common use case for a subquery in FROM (derived table)?',
    options: ['Sorting data', 'Pre-aggregating data before joining it with another table', 'Creating indexes', 'Defining permissions'],
    correctAnswer: 1,
    explanation: 'A derived table that pre-aggregates (GROUP BY) can be joined with other tables. This avoids GROUP BY issues when joining with a table that duplicates rows.',
    codeSnippet: 'SELECT u.name, order_stats.total_spent\nFROM users u\nJOIN (\n  SELECT user_id, SUM(amount) AS total_spent\n  FROM orders GROUP BY user_id\n) order_stats ON u.id = order_stats.user_id;',
    tags: ['derived-table', 'pre-aggregation', 'join'],
  },
  {
    id: 'pg-joins-l2-039',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you find the department with the highest average salary?',
    options: [
      'SELECT MAX(AVG(salary))',
      'Using a subquery or CTE that computes averages per department, then ORDER BY DESC LIMIT 1',
      'Using HAVING MAX',
      'Using DISTINCT'
    ],
    correctAnswer: 1,
    explanation: 'First compute average salary per department, then order by average descending and limit to 1. Nested aggregate functions are not allowed directly.',
    codeSnippet: 'SELECT department, AVG(salary) AS avg_sal\nFROM employees\nGROUP BY department\nORDER BY avg_sal DESC\nLIMIT 1;',
    tags: ['top-group', 'avg', 'order-by'],
  },
  {
    id: 'pg-joins-l2-040',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'Can LATERAL be used with set-returning functions?',
    options: ['No, only with subqueries', 'Yes, LATERAL is implicit for set-returning functions in FROM', 'Only with generate_series', 'Only with UNNEST'],
    correctAnswer: 1,
    explanation: 'Set-returning functions in FROM can reference preceding tables even without the LATERAL keyword. PostgreSQL implicitly treats them as LATERAL.',
    codeSnippet: 'SELECT u.name, g.n\nFROM users u,\ngenerate_series(1, u.order_count) AS g(n);',
    tags: ['lateral', 'set-returning', 'implicit'],
  },
  {
    id: 'pg-joins-l2-041',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Duplicates',
    question: 'If a user has 3 orders and you INNER JOIN users with orders, how many rows appear for that user?',
    options: ['1', '3', 'It depends on GROUP BY', '0'],
    correctAnswer: 1,
    explanation: 'INNER JOIN produces one result row for each matching combination. A user with 3 matching orders produces 3 rows (the user data is repeated).',
    tags: ['inner-join', 'one-to-many', 'row-multiplication'],
  },
  {
    id: 'pg-joins-l2-042',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Duplicates',
    question: 'What is the "row multiplication" problem when joining multiple one-to-many tables?',
    options: [
      'Tables get physically larger',
      'Joining table A to B (1:N) and C (1:M) produces N×M rows per A row, inflating aggregates',
      'JOIN produces NULL rows',
      'It only affects CROSS JOIN'
    ],
    correctAnswer: 1,
    explanation: 'When joining a parent to two child tables independently, a Cartesian product forms between the children. SUM/COUNT aggregates get inflated. Solve by pre-aggregating.',
    codeSnippet: '-- WRONG: COUNT is inflated\nSELECT u.name, COUNT(o.id), COUNT(r.id)\nFROM users u\nJOIN orders o ON u.id = o.user_id\nJOIN reviews r ON u.id = r.user_id\nGROUP BY u.name;\n\n-- CORRECT: pre-aggregate\nSELECT u.name, COALESCE(oc.cnt, 0), COALESCE(rc.cnt, 0)\nFROM users u\nLEFT JOIN (SELECT user_id, COUNT(*) cnt FROM orders GROUP BY user_id) oc ON u.id = oc.user_id\nLEFT JOIN (SELECT user_id, COUNT(*) cnt FROM reviews GROUP BY user_id) rc ON u.id = rc.user_id;',
    tags: ['row-multiplication', 'fan-out', 'pre-aggregate'],
  },
  {
    id: 'pg-joins-l2-043',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Conditional Join',
    question: 'Can you use a CASE expression inside a JOIN ON clause?',
    options: ['No, ON only supports simple conditions', 'Yes, any expression that evaluates to boolean can be used', 'Only in LEFT JOIN', 'Only with PostgreSQL extensions'],
    correctAnswer: 1,
    explanation: 'The ON clause accepts any boolean expression, including CASE, function calls, and complex logic.',
    codeSnippet: 'SELECT *\nFROM orders o\nJOIN addresses a ON a.id = CASE\n  WHEN o.type = \'shipping\' THEN o.shipping_addr_id\n  ELSE o.billing_addr_id\nEND;',
    tags: ['case', 'on-clause', 'conditional-join'],
  },
  {
    id: 'pg-joins-l2-044',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE Performance',
    question: 'Before PostgreSQL 12, how did CTEs affect query optimization?',
    options: ['CTEs were always inlined', 'CTEs acted as optimization fences — the optimizer could not push predicates into them', 'CTEs were materialized to disk', 'No effect on optimization'],
    correctAnswer: 1,
    explanation: 'Before PG 12, CTEs were always materialized (optimization fence). This prevented predicate pushdown. Since PG 12, non-recursive CTEs are inlined by default.',
    tags: ['cte', 'optimization-fence', 'postgresql-12'],
  },
  {
    id: 'pg-joins-l2-045',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subquery Correlation',
    question: 'Can a subquery in the FROM clause be correlated?',
    options: ['Yes, with regular subqueries', 'No, only LATERAL subqueries in FROM can reference outer tables', 'Always correlated', 'Only with CTEs'],
    correctAnswer: 1,
    explanation: 'A regular subquery in FROM cannot reference other tables in the FROM clause. The LATERAL keyword is required for a correlated subquery in FROM.',
    tags: ['lateral', 'correlated', 'from-clause'],
  },
  {
    id: 'pg-joins-l2-046',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'How do you use a subquery with BETWEEN?',
    options: [
      'BETWEEN does not support subqueries',
      'WHERE col BETWEEN (SELECT min_val ...) AND (SELECT max_val ...)',
      'Only with constants',
      'Using two separate WHERE conditions'
    ],
    correctAnswer: 1,
    explanation: 'Each boundary in BETWEEN can be a scalar subquery, allowing dynamic range filtering.',
    codeSnippet: 'SELECT * FROM sales\nWHERE amount BETWEEN\n  (SELECT AVG(amount) - STDDEV(amount) FROM sales)\n  AND\n  (SELECT AVG(amount) + STDDEV(amount) FROM sales);',
    tags: ['between', 'subquery', 'dynamic-range'],
  },
  {
    id: 'pg-joins-l2-047',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Types',
    question: 'What is a theta join?',
    options: ['A join using the theta symbol', 'Any join that uses a comparison operator other than = in its condition', 'A special PostgreSQL join type', 'A join with three tables'],
    correctAnswer: 1,
    explanation: 'A theta join uses any comparison operator (<, >, <=, >=, !=) in the join condition, not just equality. Range joins and inequality joins are theta joins.',
    codeSnippet: 'SELECT * FROM employees e\nJOIN salary_bands b\n  ON e.salary BETWEEN b.min_salary AND b.max_salary;',
    tags: ['theta-join', 'range-join', 'inequality'],
  },
  {
    id: 'pg-joins-l2-048',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Can you use a CTE in a CREATE VIEW statement?',
    options: ['No, views cannot use CTEs', 'Yes, the CTE becomes part of the view definition', 'Only materialized views', 'Only with RECURSIVE'],
    correctAnswer: 1,
    explanation: 'CTEs can be used in the defining query of a view. Each time the view is queried, the CTE is evaluated.',
    codeSnippet: 'CREATE VIEW dept_summary AS\nWITH dept_stats AS (\n  SELECT dept_id, COUNT(*) AS emp_count, AVG(salary) AS avg_sal\n  FROM employees GROUP BY dept_id\n)\nSELECT d.name, ds.emp_count, ds.avg_sal\nFROM departments d\nJOIN dept_stats ds ON d.id = ds.dept_id;',
    tags: ['cte', 'view', 'definition'],
  },
  {
    id: 'pg-joins-l2-049',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'How do you compute the total depth of each node in a tree?',
    options: [
      'Using COUNT with GROUP BY',
      'Start from root nodes (depth 0) and increment depth in each recursive step',
      'Using a self-join',
      'Using LEVEL keyword'
    ],
    correctAnswer: 1,
    explanation: 'The anchor selects root nodes with depth = 0. The recursive term increments depth for each child level.',
    codeSnippet: 'WITH RECURSIVE tree AS (\n  SELECT id, name, parent_id, 0 AS depth\n  FROM categories WHERE parent_id IS NULL\n  UNION ALL\n  SELECT c.id, c.name, c.parent_id, t.depth + 1\n  FROM categories c JOIN tree t ON c.parent_id = t.id\n)\nSELECT * FROM tree ORDER BY depth;',
    tags: ['recursive-cte', 'depth', 'tree'],
  },
  {
    id: 'pg-joins-l2-050',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you join a table to itself to find consecutive rows (e.g., consecutive log entries)?',
    options: [
      'Using UNION',
      'Self-join on row number or sequential ID where the second row\'s ID = first row\'s ID + 1',
      'Using CROSS JOIN',
      'Using GROUP BY'
    ],
    correctAnswer: 1,
    explanation: 'Self-join where one instance is offset by 1 from the other. Window functions (LAG/LEAD) are often a better alternative.',
    codeSnippet: 'SELECT a.id, a.value AS val1, b.value AS val2\nFROM events a\nJOIN events b ON b.id = a.id + 1;',
    tags: ['self-join', 'consecutive', 'lag-lead'],
  },
  {
    id: 'pg-joins-l2-051',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'USING vs ON',
    question: 'What happens to the join column when using USING vs ON with SELECT *?',
    options: [
      'No difference',
      'USING produces one copy of the join column; ON with SELECT * produces both copies',
      'USING excludes the column',
      'ON merges the columns'
    ],
    correctAnswer: 1,
    explanation: 'With USING(id), SELECT * returns the id column once. With ON a.id = b.id, SELECT * returns both a.id and b.id (potentially with different table prefixes).',
    tags: ['using', 'on', 'select-star'],
  },
  {
    id: 'pg-joins-l2-052',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is the difference between a scalar subquery returning NULL and returning no rows?',
    options: ['They are the same', 'Both return NULL; no rows = NULL scalar', 'No rows causes an error', 'NULL returns an error'],
    correctAnswer: 1,
    explanation: 'A scalar subquery that returns no rows evaluates to NULL. A subquery that returns a row with NULL value also evaluates to NULL. Both produce the same result.',
    tags: ['scalar', 'null', 'no-rows'],
  },
  {
    id: 'pg-joins-l2-053',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'How does LATERAL compare to a correlated subquery in the SELECT list?',
    options: [
      'They are identical',
      'LATERAL can return multiple rows and columns; a scalar subquery in SELECT returns only one value',
      'LATERAL is slower',
      'SELECT subqueries return multiple columns'
    ],
    correctAnswer: 1,
    explanation: 'LATERAL subqueries in FROM can return multiple rows and columns. Scalar subqueries in SELECT must return exactly one value.',
    tags: ['lateral', 'scalar-subquery', 'comparison'],
  },
  {
    id: 'pg-joins-l2-054',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you find duplicates in a table using a self-join?',
    options: [
      'SELECT DISTINCT *',
      'Self-join where columns match but IDs are different',
      'Using COUNT',
      'Using EXCEPT'
    ],
    correctAnswer: 1,
    explanation: 'Self-join where the data columns match but the unique identifier differs reveals duplicates.',
    codeSnippet: 'SELECT a.*\nFROM users a\nJOIN users b\n  ON a.email = b.email AND a.id < b.id;',
    tags: ['self-join', 'duplicates', 'detection'],
  },
  {
    id: 'pg-joins-l2-055',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What does ROW(a, b) IN (SELECT x, y FROM ...) do?',
    options: ['Compares arrays', 'Checks if the composite value (a, b) matches any (x, y) pair from the subquery', 'Compares rows as strings', 'Returns the row position'],
    correctAnswer: 1,
    explanation: 'Row-value comparison IN checks if the tuple matches any tuple from the subquery, comparing element by element.',
    codeSnippet: 'SELECT * FROM orders\nWHERE (customer_id, product_id) IN (\n  SELECT customer_id, product_id\n  FROM returns\n);',
    tags: ['row-comparison', 'in', 'composite'],
  },
  {
    id: 'pg-joins-l2-056',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'How do you limit the depth of recursion in a recursive CTE?',
    options: [
      'Using MAX_DEPTH keyword',
      'Add a depth counter column and include WHERE depth < max_depth in the recursive term',
      'Using LIMIT on the outer query',
      'It is not possible'
    ],
    correctAnswer: 1,
    explanation: 'Add a depth counter starting at 0 in the anchor and increment in the recursive term. Filter WHERE depth < N to limit recursion depth.',
    codeSnippet: 'WITH RECURSIVE tree AS (\n  SELECT id, 0 AS depth FROM categories WHERE parent_id IS NULL\n  UNION ALL\n  SELECT c.id, t.depth + 1\n  FROM categories c JOIN tree t ON c.parent_id = t.id\n  WHERE t.depth < 5  -- max 5 levels deep\n)\nSELECT * FROM tree;',
    tags: ['recursive-cte', 'depth-limit', 'safety'],
  },
  {
    id: 'pg-joins-l2-057',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'What is an interval join (range join)?',
    options: ['A join using INTERVAL type', 'A join where rows match based on overlapping ranges rather than exact equality', 'A join between partitions', 'A time-based join'],
    correctAnswer: 1,
    explanation: 'Interval/range joins match rows where a value falls within a range. Common for date ranges, salary bands, or time intervals.',
    codeSnippet: 'SELECT e.name, b.band_name\nFROM employees e\nJOIN salary_bands b\n  ON e.salary >= b.min_sal AND e.salary < b.max_sal;',
    tags: ['range-join', 'interval', 'between'],
  },
  {
    id: 'pg-joins-l2-058',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'How do you use a subquery to compute a running total?',
    options: [
      'Using SUM directly',
      'A correlated subquery that sums all rows up to the current row',
      'Using RUNNING_TOTAL function',
      'Window functions are required'
    ],
    correctAnswer: 1,
    explanation: 'A correlated subquery can sum rows where id <= current_id. However, window functions (SUM() OVER) are the modern and efficient approach.',
    codeSnippet: '-- Using correlated subquery (less efficient):\nSELECT id, amount,\n  (SELECT SUM(amount) FROM orders o2\n   WHERE o2.id <= o1.id) AS running_total\nFROM orders o1\nORDER BY id;\n\n-- Better: window function\nSELECT id, amount,\n  SUM(amount) OVER (ORDER BY id) AS running_total\nFROM orders;',
    tags: ['running-total', 'correlated', 'window-function'],
  },
  {
    id: 'pg-joins-l2-059',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Can a single CTE be used in multiple places in the main query?',
    options: ['No, each CTE can only be referenced once', 'Yes, a CTE can be referenced multiple times', 'Only with MATERIALIZED', 'Only in JOINs'],
    correctAnswer: 1,
    explanation: 'A CTE can be referenced multiple times in the main query. With NOT MATERIALIZED (default since PG 12), it may be computed multiple times. With MATERIALIZED, it is computed once.',
    tags: ['cte', 'reuse', 'multiple-references'],
  },
  {
    id: 'pg-joins-l2-060',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you perform a "division" operation (find users who have ordered ALL products)?',
    options: [
      'Using INNER JOIN only',
      'Compare the count of distinct products per user to the total product count using HAVING',
      'Using FULL JOIN',
      'Using CROSS JOIN'
    ],
    correctAnswer: 1,
    explanation: 'Relational division finds rows related to ALL items in another set. Count distinct matching products per user and compare to total product count.',
    codeSnippet: 'SELECT user_id\nFROM orders\nGROUP BY user_id\nHAVING COUNT(DISTINCT product_id) = (\n  SELECT COUNT(*) FROM products\n);',
    tags: ['relational-division', 'having', 'all-items'],
  },
  {
    id: 'pg-joins-l2-061',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join + Window',
    question: 'Can you use window functions in a query that also has JOINs?',
    options: ['No, they are mutually exclusive', 'Yes, window functions apply after JOINs and GROUP BY', 'Only with CTEs', 'Only with subqueries'],
    correctAnswer: 1,
    explanation: 'Window functions evaluate after FROM, WHERE, GROUP BY, and HAVING. They can use columns from joined tables.',
    codeSnippet: 'SELECT u.name, o.amount,\n  SUM(o.amount) OVER (PARTITION BY u.id ORDER BY o.created_at) AS running_total\nFROM users u\nJOIN orders o ON u.id = o.user_id;',
    tags: ['window-function', 'join', 'combined'],
  },
  {
    id: 'pg-joins-l2-062',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'FULL JOIN',
    question: 'How can you use FULL JOIN to compare two datasets for differences?',
    options: [
      'FULL JOIN cannot compare datasets',
      'FULL JOIN both datasets and filter WHERE one side IS NULL to find rows only in one dataset',
      'Using EXCEPT only',
      'Using INTERSECT'
    ],
    correctAnswer: 1,
    explanation: 'FULL JOIN shows matching rows, plus A-only rows (b.col IS NULL) and B-only rows (a.col IS NULL). This is useful for data reconciliation.',
    codeSnippet: 'SELECT\n  COALESCE(a.id, b.id) AS id,\n  CASE\n    WHEN a.id IS NULL THEN \'Only in B\'\n    WHEN b.id IS NULL THEN \'Only in A\'\n    ELSE \'Both\'\n  END AS status\nFROM table_a a\nFULL JOIN table_b b ON a.id = b.id;',
    tags: ['full-join', 'reconciliation', 'comparison'],
  },
  {
    id: 'pg-joins-l2-063',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can you use GROUP BY and HAVING inside a subquery?',
    options: ['No, only in the outer query', 'Yes, subqueries can contain any valid SQL clauses', 'Only GROUP BY', 'Only HAVING'],
    correctAnswer: 1,
    explanation: 'A subquery is a complete SELECT statement and can use all SQL clauses including GROUP BY, HAVING, ORDER BY, and LIMIT.',
    tags: ['subquery', 'group-by', 'having'],
  },
  {
    id: 'pg-joins-l2-064',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join with UNNEST',
    question: 'How do you join a table with an array column using UNNEST?',
    options: [
      'Arrays cannot be joined',
      'Use UNNEST to expand the array into rows, then join with the resulting set',
      'Using CROSS JOIN only',
      'Using a special ARRAY JOIN'
    ],
    correctAnswer: 1,
    explanation: 'UNNEST expands an array into a set of rows. Combined with LATERAL (implicit), it allows joining array elements with other tables.',
    codeSnippet: 'SELECT u.name, t.tag\nFROM users u,\nUNNEST(u.tags) AS t(tag);',
    tags: ['unnest', 'array', 'lateral'],
  },
  {
    id: 'pg-joins-l2-065',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What is a "top-1-per-group" query and how is it best solved in PostgreSQL?',
    options: [
      'Using GROUP BY with LIMIT',
      'Using DISTINCT ON, window functions with ROW_NUMBER, or LATERAL with ORDER BY LIMIT 1',
      'Using CROSS JOIN',
      'It requires a cursor'
    ],
    correctAnswer: 1,
    explanation: 'PostgreSQL offers multiple approaches: DISTINCT ON (simplest), ROW_NUMBER() OVER with a filter, or LATERAL for complex cases. DISTINCT ON is PostgreSQL-specific and very efficient.',
    tags: ['top-1-per-group', 'distinct-on', 'row-number'],
  },
  {
    id: 'pg-joins-l2-066',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Multiple JOINs',
    question: 'When mixing INNER JOIN and LEFT JOIN in a query, what is an important ordering consideration?',
    options: [
      'Order does not matter',
      'Placing an INNER JOIN after a LEFT JOIN on the same chain can eliminate LEFT JOIN rows, effectively converting it to INNER JOIN',
      'LEFT JOIN must always come first',
      'INNER JOIN must always come first'
    ],
    correctAnswer: 1,
    explanation: 'If A LEFT JOIN B INNER JOIN C ON B.col = C.col, the INNER JOIN requires B columns to be non-NULL, effectively converting the LEFT to INNER for that chain. Be careful with join ordering.',
    tags: ['mixed-joins', 'ordering', 'gotcha'],
  },
  {
    id: 'pg-joins-l2-067',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'How do you use a CTE to simplify a complex query with multiple levels of aggregation?',
    options: [
      'CTEs cannot do multi-level aggregation',
      'Define each aggregation level as a separate CTE, with later CTEs referencing earlier ones',
      'Using nested GROUP BY',
      'Using HAVING only'
    ],
    correctAnswer: 1,
    explanation: 'Multiple CTEs can build on each other: first CTE aggregates at the detail level, second aggregates those results, creating clear, readable multi-level aggregation.',
    codeSnippet: 'WITH\n  daily AS (\n    SELECT date, product_id, SUM(amount) AS daily_total\n    FROM sales GROUP BY date, product_id\n  ),\n  monthly AS (\n    SELECT DATE_TRUNC(\'month\', date) AS month, product_id,\n      SUM(daily_total) AS monthly_total\n    FROM daily GROUP BY 1, 2\n  )\nSELECT * FROM monthly ORDER BY month, product_id;',
    tags: ['cte', 'multi-level', 'aggregation'],
  },
  {
    id: 'pg-joins-l2-068',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'How does PostgreSQL handle IN with a large list of values?',
    options: [
      'It always uses a sequential scan',
      'It may convert the list to a temporary hash table or use an array and use ANY(ARRAY[...]) for better performance',
      'It fails with more than 1000 values',
      'It uses binary search'
    ],
    correctAnswer: 1,
    explanation: 'For large IN lists, PostgreSQL builds a hash table internally. Using = ANY(ARRAY[...]) with a parameter is preferred for very large lists as it avoids query parsing overhead.',
    codeSnippet: '-- For many values, prefer:\nSELECT * FROM users WHERE id = ANY($1::INT[]);\n-- Over:\nSELECT * FROM users WHERE id IN (1, 2, 3, ...10000);',
    tags: ['in', 'performance', 'array-any'],
  },
  {
    id: 'pg-joins-l2-069',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Self Join',
    question: 'How do you find employees who share the same manager?',
    options: [
      'Using GROUP BY manager_id',
      'Self-join on manager_id where the two employee IDs are different',
      'Using DISTINCT',
      'Using HAVING'
    ],
    correctAnswer: 1,
    explanation: 'Self-join employees twice on the same manager_id, ensuring different employee IDs. Use e1.id < e2.id to avoid duplicate pairs.',
    codeSnippet: 'SELECT e1.name AS emp1, e2.name AS emp2, e1.manager_id\nFROM employees e1\nJOIN employees e2\n  ON e1.manager_id = e2.manager_id\n  AND e1.id < e2.id;',
    tags: ['self-join', 'shared-manager', 'pairs'],
  },
  {
    id: 'pg-joins-l2-070',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join with generate_series',
    question: 'How do you create a report showing every day in a range, even days with no data?',
    options: [
      'Using GROUP BY date',
      'LEFT JOIN the data table onto a generate_series date range',
      'Using UNION with empty rows',
      'Using COALESCE only'
    ],
    correctAnswer: 1,
    explanation: 'generate_series creates all dates in the range. LEFT JOIN with the data table fills in values where they exist and shows NULLs (or 0 with COALESCE) for missing dates.',
    codeSnippet: 'SELECT d::DATE, COALESCE(SUM(o.amount), 0) AS total\nFROM generate_series(\n  \'2024-01-01\'::DATE,\n  \'2024-01-31\'::DATE,\n  \'1 day\'::INTERVAL\n) AS d\nLEFT JOIN orders o ON o.order_date = d::DATE\nGROUP BY d\nORDER BY d;',
    tags: ['generate-series', 'left-join', 'gap-filling'],
  },
  {
    id: 'pg-joins-l2-071',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'What is the difference between LATERAL and a correlated subquery in WHERE?',
    options: [
      'No difference',
      'LATERAL produces a set of rows that can be joined; a WHERE subquery produces a boolean condition',
      'LATERAL is slower',
      'WHERE subqueries return sets too'
    ],
    correctAnswer: 1,
    explanation: 'A LATERAL subquery in FROM produces rows that become part of the result. A correlated subquery in WHERE filters rows based on a boolean result.',
    tags: ['lateral', 'where-subquery', 'semantics'],
  },
  {
    id: 'pg-joins-l2-072',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What does SELECT * FROM t1 WHERE (col1, col2) IN (SELECT col1, col2 FROM t2) do?',
    options: ['Syntax error', 'Checks if the composite (col1, col2) pair exists in t2', 'Compares only col1', 'Returns Cartesian product'],
    correctAnswer: 1,
    explanation: 'Row-value IN compares tuples. A row from t1 is included if the exact (col1, col2) combination exists in t2.',
    tags: ['row-value', 'in', 'composite-comparison'],
  },
  {
    id: 'pg-joins-l2-073',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'Can recursive CTEs be used with UNION instead of UNION ALL?',
    options: ['No, only UNION ALL is allowed', 'Yes, UNION can be used to eliminate duplicates during recursion', 'UNION causes an error in recursive CTEs', 'They behave identically'],
    correctAnswer: 1,
    explanation: 'PostgreSQL allows UNION (without ALL) in recursive CTEs. This removes duplicates at each step, which can prevent infinite loops in graph traversals but is less efficient.',
    tags: ['recursive-cte', 'union', 'deduplication'],
  },
  {
    id: 'pg-joins-l2-074',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you pivot rows to columns using JOINs or subqueries?',
    options: [
      'Using a PIVOT keyword',
      'Using conditional aggregation with CASE inside SUM/COUNT or using the crosstab function',
      'Using CROSS JOIN',
      'Using UNION'
    ],
    correctAnswer: 1,
    explanation: 'PostgreSQL does not have a PIVOT keyword. Use conditional aggregation (SUM(CASE WHEN ...)) or the tablefunc extension\'s crosstab() function.',
    codeSnippet: 'SELECT product,\n  SUM(CASE WHEN month = \'Jan\' THEN amount ELSE 0 END) AS jan,\n  SUM(CASE WHEN month = \'Feb\' THEN amount ELSE 0 END) AS feb\nFROM sales\nGROUP BY product;',
    tags: ['pivot', 'conditional-aggregation', 'case'],
  },
  {
    id: 'pg-joins-l2-075',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'How do you use a subquery to replace NULLs with values from a related table?',
    options: [
      'Using IFNULL',
      'Using COALESCE with a correlated subquery or LEFT JOIN with COALESCE',
      'It is not possible',
      'Using DEFAULT keyword'
    ],
    correctAnswer: 1,
    explanation: 'COALESCE with a LEFT JOIN or subquery can provide fallback values from related tables when the primary value is NULL.',
    codeSnippet: 'SELECT o.id,\n  COALESCE(o.shipping_name, c.name) AS ship_to\nFROM orders o\nLEFT JOIN customers c ON o.customer_id = c.id;',
    tags: ['coalesce', 'null-replacement', 'left-join'],
  },
  {
    id: 'pg-joins-l2-076',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'What is a "last non-null" problem and how can it be solved?',
    options: [
      'It cannot be solved in SQL',
      'Finding the most recent non-NULL value; solve with LATERAL, window functions, or correlated subqueries',
      'Using MAX function',
      'Using DISTINCT'
    ],
    correctAnswer: 1,
    explanation: 'The "last non-null" problem finds the most recent non-NULL value for a series. LATERAL with ORDER BY and LIMIT is efficient when there is an index.',
    tags: ['last-non-null', 'lateral', 'time-series'],
  },
  {
    id: 'pg-joins-l2-077',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'How do you compute a bill of materials (total cost) using a recursive CTE?',
    options: [
      'Using a simple SUM',
      'Recursively traverse the assembly tree, multiplying quantities at each level and summing costs',
      'Using CROSS JOIN',
      'Using a stored procedure'
    ],
    correctAnswer: 1,
    explanation: 'A BOM query starts at the top assembly, recursively expands sub-components, carries forward quantity multipliers, and sums up individual part costs.',
    codeSnippet: 'WITH RECURSIVE bom AS (\n  SELECT part_id, quantity, cost\n  FROM assemblies WHERE parent_id = \'BIKE-001\'\n  UNION ALL\n  SELECT a.part_id, a.quantity * b.quantity, a.cost\n  FROM assemblies a JOIN bom b ON a.parent_id = b.part_id\n)\nSELECT SUM(quantity * cost) AS total_cost FROM bom;',
    tags: ['recursive-cte', 'bill-of-materials', 'hierarchical'],
  },
  {
    id: 'pg-joins-l2-078',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you find gaps in a sequence (missing IDs) using a join?',
    options: [
      'Using COUNT',
      'LEFT JOIN the table against a complete sequence (generate_series) and filter WHERE the table column IS NULL',
      'Using DISTINCT',
      'Using EXCEPT only'
    ],
    correctAnswer: 1,
    explanation: 'Generate a complete sequence of expected IDs, LEFT JOIN with the actual table, and find where the actual ID IS NULL — those are the gaps.',
    codeSnippet: 'SELECT s.id AS missing_id\nFROM generate_series(1, (SELECT MAX(id) FROM orders)) s(id)\nLEFT JOIN orders o ON o.id = s.id\nWHERE o.id IS NULL;',
    tags: ['gaps', 'generate-series', 'left-join'],
  },
  {
    id: 'pg-joins-l2-079',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'Can LATERAL be combined with a set-returning function like UNNEST?',
    options: ['No, only with subqueries', 'Yes, and it is the standard way to unnest arrays alongside their parent rows', 'Only in FROM clause', 'Only with CROSS JOIN'],
    correctAnswer: 1,
    explanation: 'LATERAL with UNNEST expands array elements into rows while keeping the association with the parent row. This is implicit for set-returning functions.',
    codeSnippet: 'SELECT u.id, u.name, t.tag\nFROM users u\nLEFT JOIN LATERAL UNNEST(u.tags) AS t(tag) ON TRUE;',
    tags: ['lateral', 'unnest', 'arrays'],
  },
  {
    id: 'pg-joins-l2-080',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'CTE',
    question: 'Can a CTE be used inside INSERT, UPDATE, or DELETE statements?',
    options: ['No, only in SELECT', 'Yes, CTEs can precede INSERT/UPDATE/DELETE statements', 'Only with RETURNING', 'Only in PL/pgSQL'],
    correctAnswer: 1,
    explanation: 'WITH clauses can precede any DML statement, not just SELECT. This allows complex data preparation before modifications.',
    codeSnippet: 'WITH new_data AS (\n  SELECT id, new_price\n  FROM price_updates WHERE approved = true\n)\nUPDATE products p\nSET price = nd.new_price\nFROM new_data nd\nWHERE p.id = nd.id;',
    tags: ['cte', 'dml', 'update'],
  },
  {
    id: 'pg-joins-l2-081',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'How do you efficiently find the "latest record per entity" in a table?',
    options: [
      'GROUP BY entity_id with MAX(date)',
      'Multiple approaches: DISTINCT ON, ROW_NUMBER() window function, or JOIN with MAX subquery',
      'Using LIMIT 1',
      'Only with cursors'
    ],
    correctAnswer: 1,
    explanation: 'Three main approaches: 1) DISTINCT ON (entity_id) ORDER BY date DESC, 2) ROW_NUMBER() OVER (PARTITION BY entity_id ORDER BY date DESC) = 1, 3) JOIN (SELECT entity_id, MAX(date)...).',
    tags: ['latest-per-entity', 'distinct-on', 'row-number'],
  },
  {
    id: 'pg-joins-l2-082',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Techniques',
    question: 'What is a "star join" or "star schema join"?',
    options: [
      'A join shaped like a star',
      'A fact table joined to multiple dimension tables, common in data warehousing',
      'A special PostgreSQL join type',
      'A join with 5 tables'
    ],
    correctAnswer: 1,
    explanation: 'In a star schema, a central fact table (e.g., sales) is joined to multiple dimension tables (e.g., products, customers, dates). The fact table has foreign keys to each dimension.',
    tags: ['star-join', 'data-warehouse', 'fact-dimension'],
  },
  {
    id: 'pg-joins-l2-083',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'What does SELECT ... WHERE col = (SELECT col FROM t LIMIT 1) risk?',
    options: [
      'Nothing, it is safe',
      'Without ORDER BY, the LIMIT 1 subquery returns an arbitrary row, leading to non-deterministic results',
      'It always returns the first row',
      'It returns NULL'
    ],
    correctAnswer: 1,
    explanation: 'LIMIT without ORDER BY returns an arbitrary row. The result may change between executions depending on the physical storage order.',
    tags: ['limit', 'non-deterministic', 'order-by'],
  },
  {
    id: 'pg-joins-l2-084',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'LATERAL',
    question: 'What is the ON TRUE clause in LEFT JOIN LATERAL?',
    options: [
      'A boolean filter',
      'It means always join (no additional condition needed since LATERAL handles the correlation internally)',
      'It enables the LATERAL',
      'It is optional'
    ],
    correctAnswer: 1,
    explanation: 'LEFT JOIN requires an ON clause. Since LATERAL already contains the correlated condition, ON TRUE is used as a placeholder that always passes.',
    tags: ['on-true', 'lateral', 'left-join'],
  },
  {
    id: 'pg-joins-l2-085',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Subqueries',
    question: 'Can you nest CTEs (a CTE within a CTE)?',
    options: ['Yes, CTEs can be nested', 'No, but you can chain multiple CTEs where later ones reference earlier ones', 'Only with RECURSIVE', 'Only in PL/pgSQL'],
    correctAnswer: 1,
    explanation: 'CTEs cannot be nested (a CTE definition cannot contain its own WITH clause). Instead, define multiple CTEs in sequence, with later ones referencing earlier ones.',
    tags: ['cte', 'nesting', 'chaining'],
  },
  {
    id: 'pg-joins-l2-086',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Join Optimization',
    question: 'How does having an index on the join column affect JOIN performance?',
    options: [
      'Indexes don\'t affect JOINs',
      'An index allows the planner to use nested loop or merge joins efficiently instead of hash joins',
      'Indexes only affect WHERE clauses',
      'Indexes slow down JOINs'
    ],
    correctAnswer: 1,
    explanation: 'Indexes on join columns enable index scans in nested loop joins and sorted input for merge joins. Without indexes, the planner relies on hash joins or sequential scans.',
    tags: ['index', 'join-performance', 'nested-loop'],
  },
  {
    id: 'pg-joins-l2-087',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Writable CTE',
    question: 'What is the execution order guarantee for writable CTEs?',
    options: [
      'They execute in written order',
      'All CTEs (including writable ones) logically execute at the same time; the order of effects is undefined between CTEs',
      'The main query executes first',
      'RETURNING controls the order'
    ],
    correctAnswer: 1,
    explanation: 'Writable CTEs all see the same snapshot. The order of side effects between CTEs is undefined. Each CTE cannot see changes made by other CTEs in the same statement.',
    tags: ['writable-cte', 'execution-order', 'snapshot'],
  },
  {
    id: 'pg-joins-l2-088',
    language: 'postgresql',
    level: 'level2',
    category: 'JOINs & Subqueries',
    subcategory: 'Recursive CTE',
    question: 'What is the working table in a recursive CTE?',
    options: [
      'The final result table',
      'An internal intermediate table that holds the rows from the current recursion iteration, used as input for the next iteration',
      'A temporary table on disk',
      'The anchor result'
    ],
    correctAnswer: 1,
    explanation: 'The working table holds the latest iteration\'s rows. Each recursive step reads from the working table and produces new rows. When the working table is empty, recursion stops.',
    tags: ['working-table', 'recursive-cte', 'iteration'],
  },
];

export default questions;
