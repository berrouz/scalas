import { Question } from '@/types/question';

const questions: Question[] = [
  { id: 'pg-joins-l4-001', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the "skew problem" in parallel hash joins?', options: ['Data is too uniform', 'Uneven distribution of hash values causes one worker to process most of the data, negating parallelism benefits', 'The hash table is too small', 'Workers process data in wrong order'], correctAnswer: 1, explanation: 'If many rows hash to the same bucket, one worker gets disproportionate work. PostgreSQL mitigates this with shared hash tables, but severely skewed data can still cause imbalance.', tags: ['parallel-hash-join', 'skew', 'data-distribution'] },
  { id: 'pg-joins-l4-002', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does PostgreSQL handle hash join when the hash table size is severely underestimated?', options: ['It aborts the query', 'It dynamically increases the number of batches (re-batching), writing excess data to disk', 'It switches to nested loop', 'It increases work_mem automatically'], correctAnswer: 1, explanation: 'If the initial batch count is too low, PostgreSQL increases batches dynamically. This causes some already-processed tuples to need reprocessing, degrading performance.', tags: ['hash-join', 're-batching', 'underestimate'] },
  { id: 'pg-joins-l4-003', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is the computational complexity concern with recursive CTEs traversing graphs?', options: ['Always O(n)', 'Without cycle detection, paths can grow exponentially; with UNION (dedup), each node is visited once but the working table still grows', 'O(n log n) always', 'No complexity concerns'], correctAnswer: 1, explanation: 'In a graph with many paths between nodes, UNION ALL recursive CTEs can produce exponentially many rows. UNION prevents revisits but may still be expensive. Path tracking arrays also grow.', tags: ['recursive-cte', 'complexity', 'exponential'] },
  { id: 'pg-joins-l4-004', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is a "Gather Merge" node in EXPLAIN and how does it relate to joins?', options: ['Merging two tables', 'Collects pre-sorted results from parallel workers, maintaining sort order for a subsequent merge join or ORDER BY', 'A special merge join', 'Gathering query statistics'], correctAnswer: 1, explanation: 'Gather Merge preserves the sort order of parallel worker results. This enables subsequent merge joins or ORDER BY without an additional sort step, crucial for efficient parallel plans.', tags: ['gather-merge', 'parallel', 'sort-order'] },
  { id: 'pg-joins-l4-005', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "EXISTS to semi-join" transformation and when does it fail?', options: ['It always works', 'The optimizer converts EXISTS into a semi-join unless the subquery contains volatile functions, LIMIT, or certain aggregates that change semantics', 'It fails for correlated subqueries', 'It only works for INNER JOIN'], correctAnswer: 1, explanation: 'The transformation may fail when the subquery has side effects (volatile), LIMIT (which changes cardinality semantics), or when GROUP BY/aggregate in the subquery complicates flattening.', tags: ['exists', 'semi-join', 'transformation-failure'] },
  { id: 'pg-joins-l4-006', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does PostgreSQL implement "right semi join" and "right anti join" (PostgreSQL 15)?', options: ['They are not supported', 'PostgreSQL 15 added right semi/anti join nodes, allowing the hash table to be built from the outer (left) table when it is smaller', 'Using right outer join', 'Using subqueries only'], correctAnswer: 1, explanation: 'Before PG 15, the hash table was always built from the inner (right) side. Right semi/anti joins allow building from the left side when it is smaller, improving performance.', tags: ['right-semi-join', 'postgresql-15', 'hash-table'] },
  { id: 'pg-joins-l4-007', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What is the interaction between writable CTEs and triggers?', options: ['Triggers are disabled in CTEs', 'Triggers fire normally for DML in writable CTEs, but the trigger can see different data than the CTE due to snapshot isolation', 'Triggers fire after all CTEs complete', 'Only AFTER triggers fire'], correctAnswer: 1, explanation: 'Triggers fire normally per-row/per-statement for CTE DML. However, AFTER triggers see the changes from their CTE but not from other CTEs in the same statement due to snapshot isolation.', tags: ['writable-cte', 'triggers', 'snapshot'] },
  { id: 'pg-joins-l4-008', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is "inner unique" optimization for joins?', options: ['Deduplicating the inner table', 'When the optimizer knows the inner side has a unique constraint on the join key, it can optimize nested loop to stop after first match', 'Making all values unique', 'A constraint check'], correctAnswer: 1, explanation: 'If the inner side has a unique constraint on the join columns, the planner knows each outer row matches at most one inner row. This enables optimizations like stopping the inner scan after the first match.', tags: ['inner-unique', 'optimization', 'unique-constraint'] },
  { id: 'pg-joins-l4-009', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is "breadth-first search with level-limited expansion" in a recursive CTE?', options: ['Expanding one level at a time', 'Processing all nodes at depth N before any at depth N+1, with the ability to stop at a specific depth for pagination', 'Random exploration', 'A binary search in trees'], correctAnswer: 0, explanation: 'BFS with level limits processes the tree layer by layer. Useful for "friends of friends" queries where you want to expand only to a certain social distance.', tags: ['bfs', 'level-limited', 'recursive-cte'] },
  { id: 'pg-joins-l4-010', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does the planner decide between a parameterized and non-parameterized path for a nested loop join?', options: ['Always chooses parameterized', 'It compares costs: parameterized uses index lookup per outer row, non-parameterized scans the inner table once but may use more memory', 'Random choice', 'Based on table size only'], correctAnswer: 1, explanation: 'Parameterized path: N * (index scan cost). Non-parameterized: full inner scan + join in memory. The planner compares total costs considering table sizes, selectivity, and available indexes.', tags: ['parameterized-path', 'cost-comparison', 'nested-loop'] },
  { id: 'pg-joins-l4-011', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "subquery scan" plan node?', options: ['A sequential scan of a subquery', 'A plan node that wraps a subquery result, adding a layer of indirection; the optimizer tries to eliminate it when possible', 'A scan for finding subqueries', 'An index scan inside a subquery'], correctAnswer: 1, explanation: 'SubqueryScan wraps a subquery result set. The optimizer eliminates it when possible (subquery flattening). Seeing SubqueryScan in EXPLAIN may indicate a missed optimization opportunity.', tags: ['subquery-scan', 'plan-node', 'indirection'] },
  { id: 'pg-joins-l4-012', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'How do you implement a "delta join" for incremental view maintenance?', options: ['Using materialized views', 'Join new/changed rows (delta) with existing data and merge the results, avoiding full recomputation', 'Using FULL JOIN', 'Using triggers only'], correctAnswer: 1, explanation: 'Delta joins process only changed rows. For example, when new orders arrive, join only new orders with existing products instead of recomputing the entire order-product join.', tags: ['delta-join', 'incremental', 'view-maintenance'] },
  { id: 'pg-joins-l4-013', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the cost model difference between LATERAL and a regular join?', options: ['No difference', 'LATERAL forces nested loop execution (one inner scan per outer row); a regular join can use hash or merge strategies', 'LATERAL is always cheaper', 'Regular joins cannot use indexes'], correctAnswer: 1, explanation: 'LATERAL constrains the planner to nested loop (the inner query depends on the outer). Regular joins can choose hash or merge join strategies, which may be faster for large-scale joins.', tags: ['lateral', 'cost-model', 'nested-loop'] },
  { id: 'pg-joins-l4-014', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the interaction between join order and parallel query execution?', options: ['No interaction', 'The chosen join order affects which joins can be parallelized; some orderings enable partial paths while others do not', 'Parallel always reorders joins', 'Join order is irrelevant for parallel'], correctAnswer: 1, explanation: 'Not all join plan shapes support parallel execution. The planner considers parallelizability when choosing join order. Some orderings enable Parallel Hash Join while others force serial execution.', tags: ['join-order', 'parallel', 'partial-path'] },
  { id: 'pg-joins-l4-015', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "equivalence class" mechanism and how does it optimize joins?', options: ['A class of equal operators', 'The planner groups columns known to be equal (via join conditions) into equivalence classes, enabling filter pushdown and join elimination', 'A type system concept', 'A parallel execution grouping'], correctAnswer: 1, explanation: 'If a.x = b.y AND b.y = c.z, the planner infers a.x = c.z. This equivalence class enables pushing WHERE a.x = 5 to also filter b.y = 5 and c.z = 5, enabling index scans on all tables.', tags: ['equivalence-class', 'transitivity', 'optimization'] },
  { id: 'pg-joins-l4-016', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What happens if a writable CTE and the main query both modify the same table?', options: ['It is safe', 'The behavior is unpredictable; modifying the same row in multiple sub-statements of WITH leads to undefined results', 'An error occurs', 'The last modification wins'], correctAnswer: 1, explanation: 'Modifying the same row in multiple CTEs or between a CTE and the main query leads to unpredictable results because the execution order of modifications is not guaranteed.', tags: ['writable-cte', 'same-table', 'undefined'] },
  { id: 'pg-joins-l4-017', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is "partitionwise join" and what conditions must be met for it to work?', options: ['Any join with partitioned tables', 'Both tables must be partitioned on the join key with compatible partition bounds; each partition pair is joined independently', 'Only for hash partitions', 'Requires the same number of partitions'], correctAnswer: 1, explanation: 'Partitionwise join requires both tables to be partitioned by the join key with matching bounds. The planner joins corresponding partitions independently, enabling better parallelism and pruning.', codeSnippet: 'SET enable_partitionwise_join = on;\n-- Both orders and items partitioned by date\nSELECT * FROM orders o\nJOIN order_items i ON o.id = i.order_id AND o.order_date = i.order_date;', tags: ['partitionwise-join', 'partition-matching', 'optimization'] },
  { id: 'pg-joins-l4-018', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is the "working table" vs "intermediate table" in recursive CTE execution?', options: ['They are the same', 'The working table holds new rows from the current iteration; the intermediate table accumulates all rows from all iterations (the final result)', 'Working table is temporary', 'Intermediate table is on disk'], correctAnswer: 1, explanation: 'At each iteration: 1) The recursive term reads the working table, 2) New rows go to an intermediate table, 3) The intermediate table becomes the new working table. The full result is all intermediate rows.', tags: ['working-table', 'intermediate-table', 'execution-model'] },
  { id: 'pg-joins-l4-019', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does "Incremental Sort" (PG 13) interact with merge joins?', options: ['No interaction', 'Incremental Sort can provide partially sorted input to a Merge Join when an index provides a prefix of the required sort order', 'It replaces merge joins', 'It only helps hash joins'], correctAnswer: 1, explanation: 'If an index sorts by column A but the merge join needs (A, B), Incremental Sort leverages the A-ordering and sorts only by B within each A group, feeding into the merge join.', tags: ['incremental-sort', 'merge-join', 'postgresql-13'] },
  { id: 'pg-joins-l4-020', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is "sublink" in PostgreSQL\'s internal planner terminology?', options: ['A hyperlink', 'An internal representation of a subquery that appears in an expression (WHERE, SELECT list, HAVING) rather than in FROM', 'A type of join', 'A foreign key link'], correctAnswer: 1, explanation: 'A SubLink is PostgreSQL\'s internal AST node for subqueries in expressions (EXISTS, IN, ANY, ALL, scalar subquery). SubLinks may be converted to SubPlans or flattened into joins during planning.', tags: ['sublink', 'internal', 'planner-terminology'] },
  { id: 'pg-joins-l4-021', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does PostgreSQL estimate join selectivity?', options: ['It always assumes 10%', 'Using column statistics (MCV, histogram, n_distinct) to estimate the fraction of rows matching the join condition', 'By running the join first', 'Using fixed formulas only'], correctAnswer: 1, explanation: 'The planner uses pg_statistic data: MCV lists estimate matches between common values, histograms estimate range overlaps, and n_distinct estimates the reduction ratio. Poor statistics lead to bad plans.', tags: ['selectivity', 'estimation', 'statistics'] },
  { id: 'pg-joins-l4-022', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is a "bushy" join tree vs a "left-deep" join tree?', options: ['Tree shape terminology', 'Left-deep processes tables one by one (pipeline-friendly); bushy joins intermediate results from subtrees (can parallelize better)', 'Bushy trees are always better', 'Left-deep is deprecated'], correctAnswer: 1, explanation: 'Left-deep trees join tables linearly (A⋈B⋈C⋈D). Bushy trees allow intermediate results to be joined ((A⋈B)⋈(C⋈D)). PostgreSQL considers both shapes during planning.', tags: ['bushy', 'left-deep', 'join-tree'] },
  { id: 'pg-joins-l4-023', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the "LATERAL deoptimization" problem?', options: ['LATERAL is always optimal', 'LATERAL forces nested loop execution, preventing the planner from using hash or merge join even when they would be faster for the overall query', 'LATERAL uses too much memory', 'It causes index bloat'], correctAnswer: 1, explanation: 'LATERAL creates a dependency between outer and inner, requiring nested loop. For large tables without selective indexes, a hash join on a flattened query might be much faster.', tags: ['lateral', 'deoptimization', 'nested-loop-forced'] },
  { id: 'pg-joins-l4-024', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'How can recursive CTEs be optimized for performance?', options: ['They cannot be optimized', 'Use indexes on join columns, limit recursion depth, minimize columns in the CTE, and consider replacing with iterative PL/pgSQL for complex cases', 'Only by increasing work_mem', 'By using UNION ALL always'], correctAnswer: 1, explanation: 'Performance tips: 1) Index the join column (parent_id), 2) SELECT only needed columns, 3) Add depth limits, 4) For complex processing, PL/pgSQL loops may outperform CTE.', tags: ['recursive-cte', 'performance', 'optimization-tips'] },
  { id: 'pg-joins-l4-025', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the impact of correlation between join columns on plan quality?', options: ['No impact', 'Correlated columns cause the planner to over/underestimate join output, leading to suboptimal join strategies; CREATE STATISTICS helps', 'Correlated columns always help', 'The planner handles correlation automatically'], correctAnswer: 1, explanation: 'When two columns are correlated (e.g., city and state), the planner may assume independence and severely misestimate. CREATE STATISTICS with dependencies or ndistinct can help.', codeSnippet: 'CREATE STATISTICS city_state_corr (dependencies)\n  ON city, state FROM addresses;\nANALYZE addresses;', tags: ['correlation', 'statistics', 'misestimation'] },
  { id: 'pg-joins-l4-026', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "InitPlan" vs "SubPlan" distinction in EXPLAIN output?', options: ['They are the same', 'InitPlan is evaluated once (uncorrelated); SubPlan is evaluated per outer row (correlated)', 'InitPlan is faster', 'SubPlan is for CTEs'], correctAnswer: 1, explanation: 'InitPlan appears for uncorrelated subqueries evaluated once before the main plan. SubPlan appears for correlated subqueries evaluated for each outer row. InitPlan is generally much cheaper.', tags: ['initplan', 'subplan', 'explain'] },
  { id: 'pg-joins-l4-027', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'How do you implement "temporal bi-temporal joins" for auditing?', options: ['Using triggers', 'Join on entity ID and valid_time && transaction_time ranges, using range types and GiST indexes for efficient overlap checking', 'Using versioned tables', 'Not possible in PostgreSQL'], correctAnswer: 1, explanation: 'Bi-temporal data has valid_time (when true in reality) and transaction_time (when recorded). Join on overlapping ranges for point-in-time queries. GiST indexes on ranges are critical.', tags: ['bi-temporal', 'range-join', 'auditing'] },
  { id: 'pg-joins-l4-028', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What is the performance impact of recursive CTEs when the recursion produces millions of rows?', options: ['No impact', 'All intermediate rows are materialized in memory/disk, potentially causing OOM or excessive temp file I/O', 'It is always efficient', 'Only the final result is stored'], correctAnswer: 1, explanation: 'Recursive CTEs materialize ALL generated rows. If recursion produces millions of rows, this consumes significant memory. Consider PL/pgSQL loops with cursors for very large result sets.', tags: ['recursive-cte', 'memory', 'materialization'] },
  { id: 'pg-joins-l4-029', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the "outer join strength reduction" optimization?', options: ['Weakening outer joins', 'When a WHERE clause on the nullable side of an outer join rejects NULLs, the planner converts it to an inner join', 'Reducing join columns', 'A parallel optimization'], correctAnswer: 1, explanation: 'If WHERE b.col = 5 (which rejects NULL b rows), a LEFT JOIN can be safely converted to INNER JOIN. This gives the planner more freedom to reorder joins.', tags: ['strength-reduction', 'outer-to-inner', 'optimization'] },
  { id: 'pg-joins-l4-030', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does PostgreSQL handle the "join ordering problem" complexity?', options: ['Brute force', 'Dynamic programming for small numbers of tables (≤join_collapse_limit), GEQO for larger numbers', 'Always uses left-deep trees', 'Random search'], correctAnswer: 1, explanation: 'The join ordering problem is NP-hard. PostgreSQL uses bottom-up dynamic programming (optimal for N≤8-12) and switches to GEQO genetic algorithm for more tables to keep planning time reasonable.', tags: ['join-ordering', 'dynamic-programming', 'geqo'] },
  { id: 'pg-joins-l4-031', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is "hashed SubPlan" in EXPLAIN output?', options: ['A hash index on a subquery', 'The planner builds a hash table from an IN subquery result for O(1) lookups instead of scanning per outer row', 'A hashed CTE', 'A hash join subplan'], correctAnswer: 1, explanation: 'For IN (subquery) where the subquery is uncorrelated, the planner can hash the subquery result and probe the hash table per outer row, converting O(N*M) to O(N+M).', tags: ['hashed-subplan', 'in-subquery', 'hash-table'] },
  { id: 'pg-joins-l4-032', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is the "broadcast join" pattern for foreign data wrappers?', options: ['Broadcasting results to all servers', 'Sending the smaller table to the foreign server so the join executes remotely, avoiding transferring the larger table', 'A network join protocol', 'A parallel hash join variant'], correctAnswer: 1, explanation: 'With postgres_fdw, if one table is local and one is remote, pushing the smaller local result to the remote server for the join can be faster than pulling the entire remote table locally.', tags: ['broadcast-join', 'fdw', 'distributed'] },
  { id: 'pg-joins-l4-033', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'How does LATERAL interact with partitioned tables?', options: ['LATERAL cannot use partitioned tables', 'LATERAL works with partitioned tables; the planner may enable partition pruning inside the lateral subquery based on outer row values', 'Only with runtime pruning', 'Partitioned tables prevent LATERAL'], correctAnswer: 1, explanation: 'When the LATERAL subquery filters on the partition key using an outer row value, PostgreSQL can prune irrelevant partitions at runtime, making LATERAL very efficient with partitioned data.', tags: ['lateral', 'partitioning', 'runtime-pruning'] },
  { id: 'pg-joins-l4-034', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is the difference between "linear" and "branching" recursion in CTEs?', options: ['No difference', 'Linear recursion produces one output row per input row; branching produces multiple, potentially causing exponential growth', 'Linear is faster', 'Branching is not supported'], correctAnswer: 1, explanation: 'Linear recursion (like linked list traversal) adds one row per step. Branching (like tree traversal) adds multiple rows per step. Branching can produce exponentially many rows without limits.', tags: ['linear-recursion', 'branching', 'growth-rate'] },
  { id: 'pg-joins-l4-035', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What does the "disabled_nodes" column in pg_stat_statements tell you about join planning?', options: ['Nodes that crashed', 'In PostgreSQL 17+, it tracks how many plan nodes the planner disabled, indicating how well the plan matches the query', 'Disabled indexes', 'Failed parallel workers'], correctAnswer: 1, explanation: 'PostgreSQL 17 tracks disabled plan nodes to indicate suboptimal planning. A high count suggests the planner struggled, possibly due to poor statistics or unusual query patterns.', tags: ['disabled-nodes', 'pg-stat-statements', 'planning-quality'] },
  { id: 'pg-joins-l4-036', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'How does the planner handle ALL (subquery) compared to ANY (subquery)?', options: ['Identically', 'ALL is transformed into NOT (ANY with negated operator); this affects which semi-join or anti-join strategy is used', 'ALL is always slower', 'ANY uses hash; ALL uses nested loop'], correctAnswer: 1, explanation: 'x > ALL (subquery) is equivalent to NOT (x <= ANY (subquery)). The planner may transform ALL into a negated ANY semi-join, choosing hash or merge anti-join strategies.', tags: ['all', 'any', 'transformation'] },
  { id: 'pg-joins-l4-037', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What are the trade-offs of using writable CTEs for batch operations vs. multiple statements?', options: ['Writable CTEs are always better', 'Writable CTEs execute atomically in one statement but share a snapshot; multiple statements allow seeing intermediate changes and simpler error handling', 'Multiple statements are always better', 'No difference'], correctAnswer: 1, explanation: 'Writable CTEs: atomic execution, no intermediate snapshots, complex error handling. Multiple statements: see intermediate results, simpler debugging, can checkpoint between operations.', tags: ['writable-cte', 'trade-offs', 'batch-operations'] },
  { id: 'pg-joins-l4-038', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is "join filter" vs "index cond" vs "filter" in EXPLAIN output for a nested loop join?', options: ['They are the same', 'Index Cond: checked during index scan; Join Filter: checked during join; Filter: applied after the join', 'Join filter is for outer joins', 'Filter is always on the outer table'], correctAnswer: 1, explanation: 'Index Cond is pushed into the index scan (efficient). Join Filter is checked per joined row pair (less efficient). Filter is a general post-join predicate. Moving conditions to Index Cond improves performance.', tags: ['explain', 'join-filter', 'index-cond'] },
  { id: 'pg-joins-l4-039', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'Can you use window functions in the final SELECT of a recursive CTE (the outer query)?', options: ['No', 'Yes, window functions can be applied to the materialized result of the recursive CTE in the outer query', 'Only ROW_NUMBER', 'Only with ORDER BY'], correctAnswer: 1, explanation: 'The recursive CTE materializes its result. The outer query can apply any operation including window functions, GROUP BY, JOINs, etc. on the CTE result.', tags: ['recursive-cte', 'window-function', 'outer-query'] },
  { id: 'pg-joins-l4-040', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is the "bloom filter" optimization for hash joins (PostgreSQL 17+)?', options: ['A flower-based algorithm', 'Building a compact probabilistic data structure that quickly eliminates non-matching rows before the actual hash table probe', 'A full-text search filter', 'A color-based filter'], correctAnswer: 1, explanation: 'A Bloom filter can quickly determine that a row definitely does not match (with small probability of false positives). This reduces the number of expensive hash table probes.', tags: ['bloom-filter', 'hash-join', 'postgresql-17'] },
  { id: 'pg-joins-l4-041', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'How does PostgreSQL handle multi-row subqueries used as arrays?', options: ['It cannot', 'Using ARRAY(subquery) syntax to convert a multi-row subquery result into an array value', 'Using ARRAY_AGG only', 'Using UNNEST'], correctAnswer: 1, explanation: 'ARRAY(SELECT ...) collects all rows from a subquery into an array. This is useful for comparing or storing sets of related values.', codeSnippet: 'SELECT u.name,\n  ARRAY(SELECT p.title FROM posts p WHERE p.user_id = u.id) AS post_titles\nFROM users u;', tags: ['array-subquery', 'array-constructor', 'postgresql'] },
  { id: 'pg-joins-l4-042', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the "startup cost" vs "total cost" trade-off in join planning?', options: ['They are the same', 'Startup cost matters for LIMIT queries (return first rows fast); total cost matters for full scans. The planner considers both for different contexts', 'Startup cost is always more important', 'Total cost is always more important'], correctAnswer: 1, explanation: 'For cursor-like queries with LIMIT, the planner prefers plans with low startup cost (nested loop + index). For full result sets, total cost matters more (hash join may be better overall).', tags: ['startup-cost', 'total-cost', 'limit'] },
  { id: 'pg-joins-l4-043', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the interaction between LATERAL and the Memoize node?', options: ['They are incompatible', 'Memoize can cache LATERAL subquery results for repeated outer row values, dramatically improving performance when there are many duplicate join keys', 'Memoize replaces LATERAL', 'They are identical'], correctAnswer: 1, explanation: 'When LATERAL produces the same result for many outer rows (duplicate join key values), Memoize caches and reuses results. The cache hit rate determines the benefit.', tags: ['lateral', 'memoize', 'caching'] },
  { id: 'pg-joins-l4-044', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does asynchronous append interact with foreign table joins?', options: ['No interaction', 'PostgreSQL 14+ can send queries to multiple foreign servers simultaneously and combine results as they arrive, parallelizing distributed joins', 'It serializes foreign queries', 'Only for local tables'], correctAnswer: 1, explanation: 'Async Append allows PostgreSQL to initiate scans on multiple foreign servers in parallel. When combined with join pushdown, this dramatically speeds up federated queries.', tags: ['async-append', 'fdw', 'parallel-foreign'] },
  { id: 'pg-joins-l4-045', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is the "non-linear" recursive CTE restriction?', options: ['Recursion must be linear', 'The recursive term cannot: 1) reference the CTE inside a subquery, 2) use aggregate/window functions on the CTE, 3) use OUTER JOIN on the CTE reference', 'Only LEFT JOIN is restricted', 'There are no restrictions'], correctAnswer: 1, explanation: 'PostgreSQL restricts recursive terms to prevent non-linear (non-monotonic) recursion: no aggregate on the recursive ref, no subquery containing the ref, no OUTER JOIN on the ref.', tags: ['recursive-cte', 'non-linear', 'restrictions'] },
  { id: 'pg-joins-l4-046', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'How do you implement optimistic locking using a writable CTE?', options: ['Using FOR UPDATE', 'CTE with UPDATE ... WHERE version = expected_version RETURNING * to atomically check and update', 'Using locks', 'Using SERIALIZABLE isolation'], correctAnswer: 1, explanation: 'A writable CTE can attempt the UPDATE with a version check and use RETURNING to determine if it succeeded. If zero rows return, the version has changed.', codeSnippet: 'WITH updated AS (\n  UPDATE products\n  SET price = 29.99, version = version + 1\n  WHERE id = 42 AND version = 5\n  RETURNING *\n)\nSELECT CASE WHEN EXISTS (SELECT 1 FROM updated)\n  THEN \'success\' ELSE \'conflict\' END;', tags: ['optimistic-locking', 'writable-cte', 'version'] },
  { id: 'pg-joins-l4-047', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the effect of effective_cache_size on join planning?', options: ['It allocates cache memory', 'It tells the planner how much OS/PostgreSQL cache to assume is available, affecting the estimated cost of random I/O in index scans used for joins', 'It caches join results', 'It sizes the shared buffer pool'], correctAnswer: 1, explanation: 'effective_cache_size (typically 75% of RAM) reduces the estimated cost of random I/O. Higher values make index scans (and thus nested loop joins with index) more attractive to the planner.', tags: ['effective-cache-size', 'planner', 'io-cost'] },
  { id: 'pg-joins-l4-048', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'How does the planner handle a LIMIT inside a subquery used with IN?', options: ['It removes the LIMIT', 'The LIMIT prevents subquery flattening into a semi-join, potentially forcing a less efficient plan', 'LIMIT is ignored in subqueries', 'It optimizes the LIMIT away'], correctAnswer: 1, explanation: 'LIMIT changes the semantics of the subquery (it constrains which rows are returned). The planner cannot safely flatten it into a join because LIMIT would need to be preserved.', tags: ['limit', 'subquery-flattening', 'prevention'] },
  { id: 'pg-joins-l4-049', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is a "late materialization" optimization for joins?', options: ['Delaying table creation', 'Carrying only join keys through join nodes and fetching full rows later, reducing memory bandwidth for wide tables', 'Creating materialized views after joins', 'Lazy evaluation of subqueries'], correctAnswer: 1, explanation: 'Late materialization passes only the TID and join key through join operations, fetching the full tuple only when needed for the final result. This reduces memory bandwidth for wide rows.', tags: ['late-materialization', 'memory-bandwidth', 'wide-tables'] },
  { id: 'pg-joins-l4-050', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'How can you implement a recursive CTE that processes data in "waves" or "generations"?', options: ['Using LIMIT per wave', 'Track a generation counter and process each generation in the recursive term, enabling level-by-level processing', 'Using PARTITION BY', 'It is not possible'], correctAnswer: 1, explanation: 'Add a generation column initialized to 0. Increment in the recursive term. The working table naturally contains one generation at a time. You can limit by WHERE generation < max.', tags: ['recursive-cte', 'waves', 'generation'] },
  { id: 'pg-joins-l4-051', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the interaction between the Memoize node and hash join batching?', options: ['They are unrelated', 'Memoize caches parameterized nested loop results per key; hash join batching handles hash tables exceeding work_mem. Both manage memory differently', 'Memoize replaces hash batching', 'Hash batching replaces Memoize'], correctAnswer: 1, explanation: 'Memoize works with nested loop joins caching per-key results. Hash join batching works within hash joins to handle large inner tables. They solve different memory pressure scenarios.', tags: ['memoize', 'hash-batching', 'memory-management'] },
  { id: 'pg-joins-l4-052', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'How does the planner decide between hashing and sorting for IN subquery evaluation?', options: ['Always hashes', 'For small result sets, it may sort and binary search. For larger sets, it hashes. The crossover depends on work_mem and set size', 'Always sorts', 'Random choice'], correctAnswer: 1, explanation: 'The planner estimates the subquery result size and compares hashing cost (O(N) build + O(1) probes) against sorting cost (O(N log N) sort + O(log N) binary search probes).', tags: ['hash-vs-sort', 'in-subquery', 'cost-comparison'] },
  { id: 'pg-joins-l4-053', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the "lateral anti-join" pattern?', options: ['An anti-join with LATERAL', 'Using LEFT JOIN LATERAL with WHERE lateral_result IS NULL to find outer rows where the lateral subquery returns no results', 'A negated lateral join', 'LATERAL with NOT EXISTS'], correctAnswer: 1, explanation: 'LEFT JOIN LATERAL (subquery) ON TRUE WHERE subquery.col IS NULL finds outer rows for which the lateral subquery returns nothing. This is the LATERAL equivalent of NOT EXISTS.', codeSnippet: 'SELECT u.*\nFROM users u\nLEFT JOIN LATERAL (\n  SELECT 1 AS found FROM orders\n  WHERE user_id = u.id AND status = \'completed\'\n  LIMIT 1\n) o ON TRUE\nWHERE o.found IS NULL;', tags: ['lateral', 'anti-join', 'pattern'] },
  { id: 'pg-joins-l4-054', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the "parameterized path" concept in the planner?', options: ['A path with URL parameters', 'A plan path that requires specific parameter values from an outer plan node, enabling parameterized nested loop joins and index scans', 'A stored procedure path', 'A file system path'], correctAnswer: 1, explanation: 'Parameterized paths represent plans that need outer row values to execute (e.g., an index scan needing the join key from the outer table). The planner generates these for nested loop consideration.', tags: ['parameterized-path', 'planner-internals', 'nested-loop'] },
  { id: 'pg-joins-l4-055', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'How do writable CTEs interact with Row-Level Security (RLS)?', options: ['RLS is ignored in CTEs', 'RLS policies are enforced on each DML operation in writable CTEs, just as for regular statements', 'Only the main query checks RLS', 'RLS prevents writable CTEs'], correctAnswer: 1, explanation: 'RLS policies apply to writable CTE DML operations. The USING clause filters visible rows and WITH CHECK validates new/updated rows, ensuring security is maintained in complex CTE chains.', tags: ['writable-cte', 'rls', 'security'] },
  { id: 'pg-joins-l4-056', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is the "zigzag join" concept for multi-column indexes?', options: ['A join that alternates directions', 'Using multiple index scans on the same composite index with different leading columns to efficiently intersect results', 'A special merge join variant', 'A self-join pattern'], correctAnswer: 1, explanation: 'Zigzag join exploits composite indexes by alternating lookups on different prefix columns. While PostgreSQL does not have a native zigzag join node, bitmap index scans achieve similar results.', tags: ['zigzag-join', 'composite-index', 'bitmap'] },
  { id: 'pg-joins-l4-057', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is the maximum number of columns you should track in a recursive CTE for performance?', options: ['No limit', 'Minimize columns; each additional column in the working table increases memory usage and comparison cost per iteration', 'At most 10', 'At most 100'], correctAnswer: 1, explanation: 'The recursive CTE materializes all columns of all intermediate rows. Carrying unnecessary columns wastes memory and slows processing. Select only what is needed.', tags: ['recursive-cte', 'performance', 'column-minimization'] },
  { id: 'pg-joins-l4-058', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does the "enable_material" setting affect joins?', options: ['It materializes all tables', 'When off, it discourages the planner from materializing inner sides of nested loops, which can change join strategy choices', 'It enables materialized views', 'It enables materialized CTEs'], correctAnswer: 1, explanation: 'The planner sometimes materializes the inner side of a nested loop to avoid rescanning. Disabling materialization forces alternative strategies, useful for debugging plan choices.', tags: ['enable-material', 'nested-loop', 'planner'] },
  { id: 'pg-joins-l4-059', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "hashed subplan" vs "unhashed subplan" trade-off?', options: ['Hashed is always better', 'Hashed SubPlan uses memory (work_mem) for O(1) lookups; unhashed does O(N) scan per outer row. Hashed wins for large subquery results with many outer rows', 'Unhashed is always better', 'They perform identically'], correctAnswer: 1, explanation: 'Hashed SubPlan: O(M) memory + O(N) probes. Unhashed SubPlan: O(1) memory + O(N*M) scans. The crossover depends on work_mem availability and the ratio of N to M.', tags: ['hashed-subplan', 'unhashed', 'trade-off'] },
  { id: 'pg-joins-l4-060', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'How do you diagnose and fix slow LATERAL queries?', options: ['LATERAL is always slow', 'Check EXPLAIN ANALYZE for the number of loops, ensure inner scans use indexes, consider replacing with hash join if many loops are inefficient', 'Use CROSS JOIN instead', 'Increase work_mem'], correctAnswer: 1, explanation: 'Key diagnostics: 1) Check "loops" count — high loops with slow inner scans indicate issues. 2) Ensure the inner scan uses an index. 3) If the Memoize hit rate is low, consider a JOIN rewrite.', tags: ['lateral', 'diagnosis', 'explain-analyze'] },
  { id: 'pg-joins-l4-061', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is "predicate locking" and how does it interact with joins in SERIALIZABLE isolation?', options: ['Locking predicates', 'PostgreSQL tracks which data was read by join operations in serializable transactions to detect read-write conflicts between concurrent transactions', 'It prevents joins', 'It locks join conditions'], correctAnswer: 1, explanation: 'In SERIALIZABLE, predicate locks (SIRead locks) track the ranges of data read by joins. If a concurrent transaction writes to those ranges, it may cause a serialization failure.', tags: ['predicate-locking', 'serializable', 'joins'] },
  { id: 'pg-joins-l4-062', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'How do CTEs interact with the PostgreSQL rule system?', options: ['Rules are disabled for CTEs', 'Rules are applied to the DML operations within writable CTEs, which can cause unexpected behavior if rules rewrite the underlying queries', 'Rules prevent CTE usage', 'Rules and CTEs are unrelated'], correctAnswer: 1, explanation: 'The rule system rewrites queries before execution, including those in writable CTEs. This can lead to unexpected results if rules (like INSERT rules on views) transform the CTE\'s DML.', tags: ['cte', 'rules', 'rewriting'] },
  { id: 'pg-joins-l4-063', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How do you force a specific join order in PostgreSQL without SET join_collapse_limit?', options: ['Using ORDER BY', 'Using explicit parentheses in the FROM clause or using a set-returning function wrapper', 'Using hints', 'It is not possible'], correctAnswer: 1, explanation: 'Explicit JOIN syntax with parentheses preserves ordering. Also, wrapping in a function or subquery with OFFSET 0 prevents reordering. pg_hint_plan extension provides explicit hints.', codeSnippet: '-- Force A join B first, then join C:\nSELECT * FROM (a JOIN b ON ...) JOIN c ON ...;\n\n-- Or prevent flattening:\nSELECT * FROM\n  (SELECT * FROM a WHERE ... OFFSET 0) sub\nJOIN b ON ...;', tags: ['join-order', 'forcing', 'parentheses'] },
  { id: 'pg-joins-l4-064', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'How can you make recursive CTEs work with partitioned tables efficiently?', options: ['Recursion and partitioning are incompatible', 'Ensure the recursive join column matches the partition key so partition pruning works, and create indexes on the partition key', 'Use inheritance instead', 'Convert to non-recursive'], correctAnswer: 1, explanation: 'When the recursive CTE joins on the partition key, PostgreSQL can prune partitions at each recursion step. This is crucial for performance with large partitioned hierarchical data.', tags: ['recursive-cte', 'partitioning', 'pruning'] },
  { id: 'pg-joins-l4-065', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "provably empty" optimization for subqueries?', options: ['Removing empty results', 'The planner detects when a subquery can never return rows (e.g., contradictory WHERE conditions) and short-circuits the evaluation', 'Emptying the subquery cache', 'Removing NULL rows'], correctAnswer: 1, explanation: 'If the planner can prove a subquery returns no rows (e.g., WHERE 1 = 0), it can eliminate the entire subquery and potentially surrounding logic, simplifying the plan significantly.', tags: ['provably-empty', 'optimization', 'short-circuit'] },
  { id: 'pg-joins-l4-066', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the impact of the "from_collapse_limit" setting?', options: ['Limits FROM clause items', 'Controls how many FROM items the planner will consider when flattening subqueries in FROM into the main query for join ordering', 'Limits table aliases', 'Controls CTE flattening'], correctAnswer: 1, explanation: 'from_collapse_limit (default 8) controls subquery flattening. If a subquery in FROM would bring total FROM items above this limit, it remains as a SubqueryScan, preventing join reordering.', tags: ['from-collapse-limit', 'subquery-flattening', 'planning'] },
  { id: 'pg-joins-l4-067', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the "correlated subquery vs LATERAL" performance comparison for aggregate lookups?', options: ['Always the same', 'For aggregate lookups (e.g., getting MAX per row), LATERAL usually equals correlated subquery performance; the planner often transforms one into the other', 'LATERAL is always faster', 'Correlated is always faster'], correctAnswer: 1, explanation: 'For simple aggregate lookups, the planner often generates the same plan for both. For complex lookups (multiple columns, LIMIT), LATERAL gives the planner more optimization options.', tags: ['lateral', 'correlated', 'aggregate-lookup'] },
  { id: 'pg-joins-l4-068', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is "speculative insertion" in the context of INSERT ... ON CONFLICT with joins?', options: ['Guessing which rows to insert', 'PostgreSQL speculatively inserts the row and then checks for conflicts, using special locking to handle concurrent inserts', 'Inserting future data', 'A batch insert optimization'], correctAnswer: 1, explanation: 'For ON CONFLICT, PostgreSQL inserts speculatively, obtaining a special lock. If a conflict is detected, it rolls back the speculative insert and executes the DO UPDATE/NOTHING path.', tags: ['speculative-insertion', 'on-conflict', 'concurrency'] },
  { id: 'pg-joins-l4-069', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What is the performance impact of deeply chained CTEs (10+)?', options: ['No impact', 'Each CTE may be separately planned and potentially materialized; deep chains can prevent global optimization across the entire query', 'Exponential slowdown', 'Linear slowdown only'], correctAnswer: 1, explanation: 'Multiple CTEs prevent global join reordering. MATERIALIZED CTEs act as optimization fences. The planner optimizes each CTE independently, potentially missing cross-CTE optimizations.', tags: ['cte', 'deep-chains', 'optimization-fence'] },
  { id: 'pg-joins-l4-070', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the "cost_limit" concept for autovacuum and how does it affect join performance indirectly?', options: ['No relation', 'Insufficient autovacuum leads to table bloat; bloated tables have more pages to scan, making joins slower', 'It limits join cost', 'It controls join parallelism'], correctAnswer: 1, explanation: 'When autovacuum is throttled too aggressively (low cost_limit), tables accumulate dead tuples. Sequential scans in joins read more pages (dead rows), and indexes become bloated and less efficient.', tags: ['autovacuum', 'bloat', 'indirect-impact'] },
  { id: 'pg-joins-l4-071', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'How do you implement a recursive "running total" or "inventory balance" calculation?', options: ['Using SUM() in the recursive term', 'Accumulate the running total in a carry column: each step adds the current row value to the previous running total', 'Using window functions only', 'Using aggregate functions'], correctAnswer: 1, explanation: 'The anchor starts with the first value. The recursive term joins the next chronological record and adds its value to the accumulated total. ORDER BY in the join ensures chronological processing.', codeSnippet: 'WITH RECURSIVE running AS (\n  SELECT date, amount, amount AS balance\n  FROM transactions WHERE date = (SELECT MIN(date) FROM transactions)\n  UNION ALL\n  SELECT t.date, t.amount, r.balance + t.amount\n  FROM transactions t\n  JOIN running r ON t.date = (\n    SELECT MIN(date) FROM transactions WHERE date > r.date\n  )\n)\nSELECT * FROM running;', tags: ['recursive-cte', 'running-total', 'accumulation'] },
  { id: 'pg-joins-l4-072', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'What is the "pull-up" vs "push-down" optimization direction for subqueries?', options: ['Pull up is faster', 'Pull-up converts subqueries into joins (widening the search space); push-down moves predicates into subqueries (narrowing early)', 'They are the same', 'Push-down is for CTEs only'], correctAnswer: 1, explanation: 'Pull-up: converting FROM subqueries into joins for better join ordering. Push-down: moving WHERE conditions into subqueries/CTEs to filter early. Both are crucial optimization strategies.', tags: ['pull-up', 'push-down', 'optimization-direction'] },
  { id: 'pg-joins-l4-073', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does the planner handle "clause selectivity" for multi-condition join filters?', options: ['It assumes independence', 'It assumes independence by default (multiplying selectivities), which can severely misestimate when conditions are correlated', 'It considers all correlations', 'It uses machine learning'], correctAnswer: 0, explanation: 'The planner multiplies individual selectivities assuming independence. For correlated conditions (city+state), this can produce wildly wrong estimates. Extended statistics help but are not always used.', tags: ['selectivity', 'independence-assumption', 'misestimation'] },
  { id: 'pg-joins-l4-074', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the relationship between LATERAL and "apply" operations in other databases?', options: ['No relationship', 'PostgreSQL\'s LATERAL JOIN is equivalent to CROSS APPLY (LATERAL) and OUTER APPLY (LEFT JOIN LATERAL) in SQL Server', 'APPLY is a PostgreSQL extension', 'They are completely different'], correctAnswer: 1, explanation: 'SQL Server\'s CROSS APPLY = CROSS JOIN LATERAL, OUTER APPLY = LEFT JOIN LATERAL ON TRUE. The semantics are identical: a correlated subquery in FROM evaluated per outer row.', tags: ['lateral', 'cross-apply', 'outer-apply'] },
  { id: 'pg-joins-l4-075', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What are the consequences of setting join_collapse_limit = 1?', options: ['Optimal join order', 'Joins execute in exactly the written order with no reordering, which can either fix or hurt performance depending on whether the written order is good', 'Fastest possible joins', 'All joins become CROSS JOIN'], correctAnswer: 1, explanation: 'join_collapse_limit = 1 preserves written join order completely. This gives developers control but removes the planner\'s ability to find a better order. Useful for debugging or when you know the best order.', tags: ['join-collapse-limit', 'fixed-order', 'manual-control'] },
  { id: 'pg-joins-l4-076', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'What is the "fixpoint" computation model of recursive CTEs?', options: ['A mathematical term', 'Recursion continues until a fixed point is reached: when the recursive term adds no new rows, the result is the complete fixpoint of the query', 'It fixes broken queries', 'A debugging model'], correctAnswer: 1, explanation: 'Recursive CTEs compute the least fixpoint of the recursive definition. Iteration continues until no new rows are produced (the working table is empty). This is grounded in relational algebra theory.', tags: ['fixpoint', 'recursive-cte', 'theory'] },
  { id: 'pg-joins-l4-077', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'How does the planner handle EXISTS (SELECT ... HAVING ...) differently from EXISTS (SELECT ... WHERE ...)?', options: ['Identically', 'HAVING prevents semi-join transformation because the aggregate changes the semantics; the subquery must be evaluated as-is', 'WHERE prevents optimization', 'HAVING enables better optimization'], correctAnswer: 1, explanation: 'HAVING requires computing the aggregate first, which changes whether the subquery returns rows. This prevents the planner from transforming EXISTS into a simple semi-join on the base table.', tags: ['exists', 'having', 'semi-join-prevention'] },
  { id: 'pg-joins-l4-078', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Techniques', question: 'What is "join clause elision" in PostgreSQL?', options: ['Removing unnecessary join clauses', 'When the planner determines a join condition is always true (e.g., after constraint exclusion), it can simplify the join', 'A syntax shortcut', 'Merging duplicate conditions'], correctAnswer: 0, explanation: 'If the planner can prove a join condition is always satisfied (e.g., CHECK constraints guarantee it), it may simplify the join. This is related to constraint exclusion and proof-based optimization.', tags: ['clause-elision', 'constraint-exclusion', 'proof-based'] },
  { id: 'pg-joins-l4-079', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What is the impact of CTE materialization on memory usage in PostgreSQL?', options: ['No memory impact', 'MATERIALIZED CTEs store the complete result set in memory (or temp files), consuming tuplestore memory proportional to the result size', 'Fixed memory usage', 'Memory is freed immediately after CTE'], correctAnswer: 1, explanation: 'Materialized CTEs use a tuplestore. Small results stay in memory; large results spill to disk. The memory threshold is work_mem. Multiple references to the CTE read from the tuplestore.', tags: ['cte', 'materialized', 'memory-usage'] },
  { id: 'pg-joins-l4-080', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'How does enable_presorted_aggregate (PG 16) affect GROUP BY after joins?', options: ['No effect on joins', 'When input is already sorted from a merge join or index scan, aggregation can use this order without a separate sort, improving post-join GROUP BY', 'It pre-sorts the aggregate function', 'It enables parallel aggregation'], correctAnswer: 1, explanation: 'If a merge join produces sorted output that matches the GROUP BY, the planner can skip an explicit sort step before aggregation. This makes merge join + aggregate pipelines more efficient.', tags: ['presorted-aggregate', 'merge-join', 'postgresql-16'] },
  { id: 'pg-joins-l4-081', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Recursive CTE', question: 'How do you implement "recursive partitioning" (finding connected components) using a recursive CTE?', options: ['Using PARTITION BY', 'Start from each unvisited node and recursively expand to all connected nodes, assigning the same component ID', 'Using window functions', 'It requires graph extensions'], correctAnswer: 1, explanation: 'For each component, start from a node and recursively follow edges to find all connected nodes. Use a sequential approach or union-find algorithm for efficiency with many components.', tags: ['connected-components', 'recursive-cte', 'graph'] },
  { id: 'pg-joins-l4-082', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is the role of "restrict_selectivity" and "join_selectivity" functions in custom operator classes?', options: ['User-facing SQL functions', 'C functions that tell the planner how selective an operator is for WHERE (restrict) and JOIN (join) conditions, enabling accurate cost estimation', 'Functions that restrict joins', 'Functions that join restrictions'], correctAnswer: 1, explanation: 'Custom operators need selectivity estimator functions so the planner can accurately estimate how many rows match. Without them, the planner uses default estimates that may be wildly inaccurate.', tags: ['selectivity-estimator', 'custom-operator', 'planner'] },
  { id: 'pg-joins-l4-083', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'LATERAL', question: 'What is the "LATERAL degeneration" — when LATERAL produces the same plan as a regular join?', options: ['LATERAL always differs', 'When the LATERAL subquery is simple enough, the planner may generate the same nested loop + index scan plan as an equivalent regular join', 'LATERAL prevents optimization', 'Only for CROSS JOIN LATERAL'], correctAnswer: 1, explanation: 'For simple LATERAL subqueries that are equivalent to a regular join (no LIMIT, no ORDER BY), the planner may generate an identical plan. LATERAL only constrains to nested loop when the subquery requires it.', tags: ['lateral', 'degeneration', 'equivalent-plan'] },
  { id: 'pg-joins-l4-084', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Subquery', question: 'How does PostgreSQL handle correlated subqueries in CHECK constraints of foreign tables (FDW)?', options: ['Check constraints work normally', 'CHECK constraints on foreign tables are only informational (not enforced) and subqueries are not allowed regardless', 'FDW enforces all checks', 'Subqueries are pushed to the foreign server'], correctAnswer: 1, explanation: 'CHECK constraints on foreign tables are assumed true but not enforced by PostgreSQL (the remote server is responsible). Subqueries cannot appear in CHECK constraints at all.', tags: ['fdw', 'check-constraint', 'limitation'] },
  { id: 'pg-joins-l4-085', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What is "outer join decorrelation" in the query optimizer?', options: ['Removing correlations from outer joins', 'Transforming a correlated subquery with LEFT/ANTI join semantics into an explicit outer join, enabling hash/merge join strategies', 'Decorating outer joins', 'A display optimization'], correctAnswer: 1, explanation: 'The optimizer converts WHERE NOT EXISTS (correlated subquery) into a LEFT ANTI JOIN, and similar transformations for other correlatedpatterns. This enables hash/merge anti-join execution.', tags: ['decorrelation', 'outer-join', 'optimizer'] },
  { id: 'pg-joins-l4-086', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'CTE', question: 'What is the interaction between recursive CTEs and the statement_timeout setting?', options: ['No interaction', 'A runaway recursive CTE (infinite loop) will be terminated by statement_timeout if set, providing a safety net', 'Recursive CTEs ignore timeouts', 'statement_timeout does not apply to CTEs'], correctAnswer: 1, explanation: 'statement_timeout applies to the entire SQL statement including recursive CTEs. An infinite recursion will be cancelled when the timeout expires. Always set this as a safety net.', tags: ['recursive-cte', 'statement-timeout', 'safety'] },
  { id: 'pg-joins-l4-087', language: 'postgresql', level: 'level4', category: 'JOINs & Subqueries', subcategory: 'Join Planning', question: 'What does the "Groups" information in EXPLAIN ANALYZE HashAggregate tell you about post-join aggregation?', options: ['Number of GROUP BY values', 'The number of hash groups created, and comparing it to the planner\'s estimate reveals potential misestimation that affected the join plan choice', 'Number of parallel groups', 'Number of sort groups'], correctAnswer: 1, explanation: 'If the actual number of groups differs significantly from the estimate, the planner may have chosen a suboptimal join strategy (e.g., hash vs. sort) due to the misestimated output size.', tags: ['explain-analyze', 'groups', 'misestimation'] },
];

export default questions;
