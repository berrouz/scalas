import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'algorithms-binary-search-l2-001',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you search for a target in a rotated sorted array without duplicates?',
    options: [
      'Find the pivot first, then binary search the correct half',
      'At each step, determine which half is sorted by comparing arr[lo] and arr[mid], then check if target is in the sorted half',
      'Both approaches work and have the same time complexity',
      'You must un-rotate the array first',
    ],
    correctAnswer: 2,
    explanation: 'Both approaches work in O(log n). The single-pass approach checks if the left half [lo, mid] or right half [mid, hi] is sorted, then determines if the target falls within the sorted range. The two-pass approach first finds the pivot (minimum), then searches the correct sorted half.',
    codeSnippet: 'function search(arr, target):\n  lo, hi = 0, len(arr) - 1\n  while lo <= hi:\n    mid = lo + (hi - lo) / 2\n    if arr[mid] == target: return mid\n    if arr[lo] <= arr[mid]:  // left half sorted\n      if arr[lo] <= target < arr[mid]:\n        hi = mid - 1\n      else: lo = mid + 1\n    else:                    // right half sorted\n      if arr[mid] < target <= arr[hi]:\n        lo = mid + 1\n      else: hi = mid - 1\n  return -1',
    tags: ['rotated-array', 'search', 'single-pass'],
  },
  {
    id: 'algorithms-binary-search-l2-002',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the minimum eating speed so that Koko can eat all bananas within h hours?',
    options: [
      'Sort bananas and eat the smallest piles first',
      'Binary search on speed k from 1 to max(piles): check if sum of ceil(pile/k) for all piles <= h',
      'Try every possible speed from 1 to max(piles)',
      'Use dynamic programming on pile sizes',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the answer (speed k). For each candidate k, compute total hours = sum(ceil(pile_i / k)). If total <= h, k is feasible (try smaller). If total > h, k is too slow (try larger). Time O(n * log(max_pile)) where n is the number of piles.',
    tags: ['koko-bananas', 'search-on-answer', 'feasibility'],
  },
  {
    id: 'algorithms-binary-search-l2-003',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How do you find the first and last position of a target value in a sorted array?',
    options: [
      'Find any occurrence and scan left and right',
      'Run lower_bound to find the first position and upper_bound - 1 to find the last position',
      'Binary search twice with different comparison operators',
      'Both the second and third approaches are correct and equivalent',
    ],
    correctAnswer: 3,
    explanation: 'Both approaches work in O(log n). Lower bound finds the first position >= target. Upper bound finds the first position > target, so upper_bound - 1 is the last occurrence. Equivalently, modify binary search with "find first" and "find last" variants using different conditions when arr[mid] == target.',
    tags: ['first-last-position', 'lower-upper-bound', 'duplicates'],
  },
  {
    id: 'algorithms-binary-search-l2-004',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you find the peak element in an array (an element greater than its neighbors)?',
    options: [
      'Linear scan comparing adjacent elements',
      'Binary search: if arr[mid] < arr[mid+1], the peak is to the right; otherwise it is to the left',
      'Sort the array and return the maximum',
      'The peak is always at the midpoint',
    ],
    correctAnswer: 1,
    explanation: 'A peak element exists in any array (by boundary conditions). Binary search: if arr[mid] < arr[mid+1], a peak must exist in [mid+1, hi] (values are still increasing). Otherwise, a peak exists in [lo, mid]. This finds A peak in O(log n). Note there may be multiple peaks.',
    codeSnippet: 'function findPeak(arr):\n  lo, hi = 0, len(arr) - 1\n  while lo < hi:\n    mid = lo + (hi - lo) / 2\n    if arr[mid] < arr[mid + 1]:\n      lo = mid + 1\n    else:\n      hi = mid\n  return lo',
    tags: ['peak-element', 'binary-search', 'local-maximum'],
  },
  {
    id: 'algorithms-binary-search-l2-005',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'What is the time complexity of finding the kth smallest element in a sorted matrix using binary search?',
    options: [
      'O(k)',
      'O(n * log(max - min)) where n is the matrix dimension',
      'O(n^2)',
      'O(n * log n)',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the value range [min element, max element]. For each candidate value, count how many elements are <= it using O(n) staircase counting (since rows and columns are sorted). If count >= k, search smaller; else search larger. Time O(n * log(max - min)).',
    tags: ['kth-smallest', 'matrix', 'value-binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-006',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you handle duplicates when searching in a rotated sorted array?',
    options: [
      'Duplicates have no effect on the algorithm',
      'When arr[lo] == arr[mid] == arr[hi], you cannot determine which half is sorted, so skip lo++ and hi--',
      'Duplicates make binary search impossible',
      'Always compare with the middle element only',
    ],
    correctAnswer: 1,
    explanation: 'With duplicates, when arr[lo] == arr[mid] == arr[hi], both halves could contain the pivot. The solution is to shrink the bounds (lo++, hi--). This degrades worst case to O(n) (e.g., all elements the same except one), but average case remains O(log n).',
    tags: ['rotated-array', 'duplicates', 'worst-case'],
  },
  {
    id: 'algorithms-binary-search-l2-007',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the minimum maximum distance when placing m balls in n positions?',
    options: [
      'Sort positions and place balls evenly',
      'Binary search on the minimum distance d: check if you can place m balls such that any two are at least d apart (greedy check)',
      'Dynamic programming on positions and balls',
      'Try all combinations of positions',
    ],
    correctAnswer: 1,
    explanation: 'Sort positions. Binary search on distance d from 1 to (max - min). For each d, greedily place balls: start at the first position, then place the next ball at the first position >= current + d. If you can place m balls, d is feasible (try larger). Time O(n * log(max - min)).',
    tags: ['magnetic-balls', 'search-on-answer', 'greedy-check'],
  },
  {
    id: 'algorithms-binary-search-l2-008',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you find the square root of a number using binary search (integer part)?',
    options: [
      'Guess and check with random numbers',
      'Binary search on [0, n]: if mid*mid <= n, search right for a larger root; if mid*mid > n, search left',
      'Use Newton\'s method only',
      'Divide n by 2 repeatedly',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the answer: lo=0, hi=n. If mid*mid <= n, the answer could be mid or larger (lo = mid + 1). If mid*mid > n, the answer is smaller (hi = mid - 1). Return hi after the loop. Time O(log n). For floating point, use epsilon-based termination.',
    tags: ['square-root', 'search-on-answer', 'integer'],
  },
  {
    id: 'algorithms-binary-search-l2-009',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How can you use binary search to find the smallest element greater than a given value (successor) in a sorted array?',
    options: [
      'Standard binary search for the value plus one',
      'Upper bound: find the first element strictly greater than the target',
      'Lower bound and add one to the result',
      'Linear scan from the target position',
    ],
    correctAnswer: 1,
    explanation: 'The upper bound returns the index of the first element strictly greater than the target. This is the successor. Implementation: when arr[mid] <= target, move right (lo = mid + 1); when arr[mid] > target, record and move left (hi = mid - 1). Time O(log n).',
    tags: ['successor', 'upper-bound', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-010',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you find the median of a row-sorted matrix in O(n * log(max-min) * log(m)) time?',
    options: [
      'Flatten the matrix, sort, and find the middle element',
      'Binary search on the value: for each candidate, count elements less than it using binary search on each row',
      'Use a heap to merge all rows',
      'Find the median of each row and take the median of medians',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the value range [min, max]. For a candidate value v, count elements <= v by running binary search (upper bound) on each row — O(n * log m). The median is at position (n*m+1)/2. If count >= position, search smaller; else search larger. Total: O(n * log(m) * log(max-min)).',
    tags: ['matrix-median', 'value-binary-search', 'row-sorted'],
  },
  {
    id: 'algorithms-binary-search-l2-011',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you find the rotation count (number of positions the array was rotated) in a rotated sorted array?',
    options: [
      'Count elements from the front until a decrease is found',
      'Find the index of the minimum element — that is the rotation count',
      'Subtract the first element from the last element',
      'Binary search for the original first element',
    ],
    correctAnswer: 1,
    explanation: 'The rotation count equals the index of the minimum element. Use the binary search approach for finding the minimum in a rotated array. If arr[mid] > arr[hi], the minimum is in [mid+1, hi]. Otherwise it is in [lo, mid]. The index of the minimum gives the rotation count. Time O(log n).',
    tags: ['rotation-count', 'minimum-index', 'rotated-array'],
  },
  {
    id: 'algorithms-binary-search-l2-012',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you split an array into m subarrays to minimize the maximum subarray sum?',
    options: [
      'Dynamic programming with O(n * m) states',
      'Binary search on the maximum sum: check if you can split into at most m parts where each part sums to at most the candidate',
      'Greedy: split whenever the current sum exceeds average',
      'Both DP and binary search approaches work, but binary search is simpler and often preferred',
    ],
    correctAnswer: 3,
    explanation: 'Binary search on the answer (max sum) from max(arr) to sum(arr). For each candidate, greedily form subarrays without exceeding the candidate sum. If you need more than m parts, the candidate is too small. Time O(n * log(sum)). DP takes O(n^2 * m) but also works.',
    tags: ['split-array', 'search-on-answer', 'minimize-maximum'],
  },
  {
    id: 'algorithms-binary-search-l2-013',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you find the first bad version in a sequence of versions 1 to n, where all versions after the first bad one are also bad?',
    options: [
      'Check every version from 1 to n',
      'Binary search: if mid is bad, the first bad version is at or before mid; if good, it is after mid',
      'Start from the end and scan backwards',
      'Check version n/2 and decide',
    ],
    correctAnswer: 1,
    explanation: 'This is a direct binary search problem. The "badness" property is monotonic: all versions >= first_bad are bad, all before are good. Binary search finds the boundary in O(log n) API calls. This is a classic "binary search on a predicate" pattern.',
    tags: ['first-bad-version', 'predicate', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-014',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'What is the bisect_left vs bisect_right distinction in Python?',
    options: [
      'bisect_left is for left-aligned data; bisect_right is for right-aligned data',
      'bisect_left returns the leftmost insertion point (lower bound); bisect_right returns the rightmost (upper bound)',
      'bisect_left searches the left half; bisect_right searches the right half',
      'They are identical functions',
    ],
    correctAnswer: 1,
    explanation: 'bisect_left (lower bound) returns the leftmost position where the target can be inserted while maintaining sort order — it returns the index of the first element >= target. bisect_right (upper bound) returns the rightmost insertion point — the first element > target. For distinct elements, they differ by 1 when the target exists.',
    tags: ['bisect', 'python', 'lower-upper-bound'],
  },
  {
    id: 'algorithms-binary-search-l2-015',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you count negative numbers in a sorted matrix (rows and columns sorted in non-decreasing order)?',
    options: [
      'Count all elements using a nested loop',
      'Use the staircase approach: start from bottom-left, move right past negatives and up past non-negatives',
      'Binary search on each row independently',
      'Both staircase O(m+n) and per-row binary search O(m*log n) work',
    ],
    correctAnswer: 3,
    explanation: 'The staircase method starts at the bottom-left. If the element is negative, count the remaining elements in the column and move right. If non-negative, move up. Time O(m + n). Alternatively, binary search on each row for the first non-negative element in O(m * log n). Both are valid.',
    tags: ['count-negatives', 'sorted-matrix', 'staircase'],
  },
  {
    id: 'algorithms-binary-search-l2-016',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'What is the time complexity of finding the minimum in a rotated sorted array with duplicates?',
    options: [
      'O(log n) always',
      'O(log n) average, O(n) worst case',
      'O(n) always',
      'O(n log n)',
    ],
    correctAnswer: 1,
    explanation: 'With duplicates, when arr[lo] == arr[mid] == arr[hi], we cannot determine which half contains the minimum. We must shrink both bounds (lo++, hi--). In the worst case (e.g., [1,1,1,...,0,1,...,1]), this degrades to O(n). Average case remains O(log n).',
    tags: ['rotated-array', 'duplicates', 'complexity'],
  },
  {
    id: 'algorithms-binary-search-l2-017',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the smallest divisor such that the sum of ceil(arr[i]/divisor) <= threshold?',
    options: [
      'Try all divisors from 1 to max(arr)',
      'Binary search on the divisor from 1 to max(arr): compute the sum for each candidate',
      'Sort the array and divide by the average',
      'Use the median as the divisor',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the divisor d from 1 to max(arr). For each d, compute sum of ceil(arr[i]/d). If sum <= threshold, d is feasible (try smaller d). If sum > threshold, d is too small (try larger). Monotonic property: larger divisor means smaller or equal sum. Time O(n * log(max)).',
    tags: ['smallest-divisor', 'search-on-answer', 'ceiling-division'],
  },
  {
    id: 'algorithms-binary-search-l2-018',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you find the single non-duplicate in a sorted array where every other element appears exactly twice?',
    options: [
      'XOR all elements',
      'Binary search: check if the pair pattern (same element at even-odd index pairs) breaks at or before mid',
      'Linear scan comparing pairs',
      'Both XOR O(n) and binary search O(log n) work, but binary search is more efficient',
    ],
    correctAnswer: 3,
    explanation: 'Before the single element, pairs are at (even, odd) indices. After it, pairs shift to (odd, even). Binary search checks which pattern holds at mid to determine which half the single element is in. Time O(log n). XOR also works in O(n) but does not use the sorted property.',
    tags: ['single-element', 'pair-pattern', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-019',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How can binary search be used with a custom comparator to search objects?',
    options: [
      'Binary search only works with integers',
      'Define a comparison function that returns whether element a should come before element b, and use it in place of < during binary search',
      'Convert objects to integers first',
      'Use a hash map instead',
    ],
    correctAnswer: 1,
    explanation: 'Binary search works on any totally ordered set. Provide a comparator function that defines the ordering. Languages like C++ (std::lower_bound with comparator), Java (Arrays.binarySearch with Comparator), and Python (using key function with bisect) all support this.',
    tags: ['custom-comparator', 'generalized', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-020',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you find the kth smallest pair sum from two sorted arrays?',
    options: [
      'Generate all pair sums, sort, and return the kth',
      'Binary search on the value of the pair sum, counting pairs with sum <= candidate using two pointers',
      'Use a min-heap to extract pairs in order',
      'Both binary search and min-heap approaches work',
    ],
    correctAnswer: 3,
    explanation: 'Binary search on sum value: for a candidate sum, count pairs (a[i] + b[j]) <= candidate using two pointers in O(n) time. Total: O(n * log(range)). Min-heap: start with (a[0]+b[0]), expand neighbors, extract k times in O(k * log(min(k, n))). Both are valid with different tradeoffs.',
    tags: ['pair-sum', 'kth-smallest', 'two-approaches'],
  },
  {
    id: 'algorithms-binary-search-l2-021',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you find the peak element in a mountain array (strictly increasing then strictly decreasing)?',
    options: [
      'Check every element against its neighbors',
      'Binary search: if arr[mid] < arr[mid+1], the peak is to the right; otherwise it is to the left or at mid',
      'The peak is always at the midpoint',
      'Sort and return the maximum',
    ],
    correctAnswer: 1,
    explanation: 'A mountain array has a unique peak. Binary search: if arr[mid] < arr[mid+1], we are on the ascending side (peak is right: lo = mid + 1). If arr[mid] > arr[mid+1], we are on the descending side (peak is at mid or left: hi = mid). Time O(log n).',
    tags: ['mountain-array', 'peak', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-022',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the maximum value at which you can cut ribbons to get at least k pieces?',
    options: [
      'Sort ribbons and cut from the longest',
      'Binary search on the cut length from 1 to max(ribbons): count total pieces at each candidate length',
      'Use greedy: always cut the longest ribbon first',
      'Dynamic programming on ribbon lengths',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on cut length L from 1 to max(ribbons). For each L, count total pieces = sum(ribbon_i / L). If pieces >= k, L is feasible (try larger). If pieces < k, L is too large (try smaller). Time O(n * log(max_ribbon)). Monotonic: larger L means fewer pieces.',
    tags: ['cut-ribbons', 'search-on-answer', 'maximize'],
  },
  {
    id: 'algorithms-binary-search-l2-023',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How do you find the closest element to a target in a sorted array?',
    options: [
      'Binary search for the target and check neighbors',
      'Find the lower bound position and compare the element at that position with the one before it',
      'Linear scan comparing differences',
      'Both the first and second approaches work in O(log n)',
    ],
    correctAnswer: 3,
    explanation: 'Find the lower bound (first element >= target). The closest element is either at the lower bound position or the position before it (if it exists). Compare their distances to the target. Time O(log n) for the binary search plus O(1) for the comparison.',
    tags: ['closest-element', 'lower-bound', 'comparison'],
  },
  {
    id: 'algorithms-binary-search-l2-024',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How does binary search apply to the "H-index" problem?',
    options: [
      'Sort citations in descending order and find the first i where citations[i] < i+1',
      'Binary search on h from 0 to n: count papers with >= h citations',
      'Both approaches work',
      'H-index cannot be computed with binary search',
    ],
    correctAnswer: 2,
    explanation: 'H-index is the largest h such that at least h papers have >= h citations. Linear approach: sort descending, scan for threshold, O(n log n). Binary search approach: binary search on h from 0 to n, counting papers with >= h citations. With a sorted array, the count check is O(log n), giving O(log^2 n) or O(n log n) total.',
    tags: ['h-index', 'counting', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l2-025',
    language: 'algorithms',
    level: 'level2',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you count pairs with sum less than a target in a sorted array using binary search?',
    options: [
      'Check all pairs in O(n^2)',
      'For each element arr[i], binary search for target - arr[i] in the remaining array, adding the count of valid pairs',
      'Sort and use two pointers',
      'Both binary search O(n log n) and two pointers O(n) approaches work',
    ],
    correctAnswer: 3,
    explanation: 'Two pointers: lo=0, hi=n-1. If arr[lo]+arr[hi] < target, all pairs (lo, lo+1..hi) work (add hi-lo), then lo++. Else hi--. Time O(n). Binary search: for each arr[i], binary search for target-arr[i], count elements. Time O(n log n). Both are valid with different complexities.',
    tags: ['pair-count', 'two-pointers', 'binary-search'],
  },
];

export default questions;
