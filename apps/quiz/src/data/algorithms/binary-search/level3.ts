import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'algorithms-binary-search-l3-001',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the median of two sorted arrays of sizes m and n in O(log(min(m,n))) time?',
    options: [
      'Merge both arrays and find the middle element',
      'Binary search on the partition point of the smaller array, ensuring left halves of both arrays contain exactly (m+n+1)/2 elements total',
      'Use two pointers advancing in both arrays',
      'Binary search on the value range',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the smaller array\'s partition index i (0 to m). Set j = (m+n+1)/2 - i. Check if maxLeft1 <= minRight2 and maxLeft2 <= minRight1. If yes, the median is found. If maxLeft1 > minRight2, move i left. Else move i right. Time O(log(min(m,n))).',
    codeSnippet: 'function findMedian(A, B):  // len(A) <= len(B)\n  lo, hi = 0, len(A)\n  while lo <= hi:\n    i = (lo + hi) / 2\n    j = (len(A) + len(B) + 1) / 2 - i\n    if i > 0 and A[i-1] > B[j]:\n      hi = i - 1\n    elif i < len(A) and B[j-1] > A[i]:\n      lo = i + 1\n    else:\n      // found correct partition\n      // median from max(A[i-1], B[j-1]) and min(A[i], B[j])',
    tags: ['median-two-arrays', 'partition', 'optimal'],
  },
  {
    id: 'algorithms-binary-search-l3-002',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you find the longest increasing subsequence in O(n log n) time using binary search?',
    options: [
      'Sort the array and find the longest sorted prefix',
      'Maintain a "tails" array and use binary search to find the correct position for each element',
      'Binary search for each element in the remaining array',
      'Use a balanced BST to track elements',
    ],
    correctAnswer: 1,
    explanation: 'Maintain tails[i] = smallest possible tail of an increasing subsequence of length i+1. For each element x, binary search (lower bound) in tails: if x > all tails, append (extend LIS). Otherwise, replace the first tail >= x. The length of tails is the LIS length. Time O(n log n).',
    tags: ['lis', 'binary-search', 'patience-sorting'],
  },
  {
    id: 'algorithms-binary-search-l3-003',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How do you find the smallest window in a sorted array that contains all unique elements using binary search?',
    options: [
      'Sliding window with two pointers',
      'For each starting position, binary search for the ending position where the element at the end differs from the start',
      'Binary search on the window size: check if any window of that size contains all unique elements',
      'Both sliding window and binary search on window size work',
    ],
    correctAnswer: 3,
    explanation: 'Sliding window O(n) is typically optimal. Binary search on window size: for each candidate size w, check if any window of size w contains all k unique elements in O(n) time. Total O(n * log n). Both approaches solve the problem but sliding window is usually preferred for this particular problem.',
    tags: ['window-search', 'binary-search-on-answer', 'sliding-window'],
  },
  {
    id: 'algorithms-binary-search-l3-004',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you find the kth element in the union of two sorted arrays without merging them?',
    options: [
      'Merge the first k elements',
      'Binary search: eliminate k/2 elements from one array at a time by comparing arr1[k/2] with arr2[k/2]',
      'Run two pointers advancing k times',
      'Both binary search O(log k) and two pointers O(k) work',
    ],
    correctAnswer: 3,
    explanation: 'Binary search approach: compare arr1[k/2-1] with arr2[k/2-1]. If arr1[k/2-1] < arr2[k/2-1], the first k/2 elements of arr1 cannot contain the kth element — discard them. Recurse with k = k - k/2. Time O(log k) which is O(log(m+n)). Two pointers takes O(k).',
    tags: ['kth-element', 'two-arrays', 'elimination'],
  },
  {
    id: 'algorithms-binary-search-l3-005',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you solve the "aggressive cows" / "magnetic balls" problem optimally?',
    options: [
      'Place cows at regular intervals',
      'Binary search on the minimum distance d, with a greedy check placing cows at the first valid position',
      'Dynamic programming on positions',
      'Sort positions and use interval scheduling',
    ],
    correctAnswer: 1,
    explanation: 'Sort stall positions. Binary search on distance d from 1 to (max-min)/(c-1). Greedy check: place first cow at position[0], then each subsequent cow at the first position >= last_placed + d. If all c cows are placed, d is feasible. Find the maximum feasible d. Time O(n * log(max-min)).',
    tags: ['aggressive-cows', 'search-on-answer', 'greedy-verification'],
  },
  {
    id: 'algorithms-binary-search-l3-006',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you find the kth smallest element in a multiplication table (m x n matrix where matrix[i][j] = i * j)?',
    options: [
      'Generate all products, sort, and pick the kth',
      'Binary search on the value: count how many products <= candidate using min(candidate/i, n) for each row i',
      'Use a heap to generate products in order',
      'The answer is always at position (k/n, k%n)',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on value v from 1 to m*n. For each v, count products <= v: for row i, there are min(v/i, n) valid columns. Sum over all rows gives the count in O(m) time. If count >= k, search smaller. Total: O(m * log(m*n)).',
    tags: ['multiplication-table', 'kth-smallest', 'counting'],
  },
  {
    id: 'algorithms-binary-search-l3-007',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How do you find the longest duplicate substring in a string using binary search?',
    options: [
      'Check all substrings of all lengths',
      'Binary search on the length of the duplicate substring, using rolling hash (Rabin-Karp) to check if a duplicate of that length exists',
      'Use a trie to find duplicates',
      'Sort all suffixes and check adjacent ones',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on length L from 1 to n-1. For each L, use Rabin-Karp rolling hash to check if any substring of length L appears twice in O(n) expected time. If yes, try longer; if no, try shorter. Total: O(n * log n) expected. Suffix array approach also works in O(n) with LCP.',
    codeSnippet: 'function longestDupSubstring(s):\n  lo, hi = 1, len(s) - 1\n  result = \'\'\n  while lo <= hi:\n    mid = (lo + hi) / 2\n    dup = checkDuplicate(s, mid)  // Rabin-Karp\n    if dup:\n      result = dup\n      lo = mid + 1\n    else:\n      hi = mid - 1\n  return result',
    tags: ['longest-duplicate', 'rabin-karp', 'binary-search-on-length'],
  },
  {
    id: 'algorithms-binary-search-l3-008',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you perform binary search on a circularly sorted array (conceptually infinite, wrapping around)?',
    options: [
      'Use modular arithmetic for index calculations',
      'Find the start of the sorted order, then do standard binary search with offset index mapping: actual_index = (start + relative_index) % n',
      'Convert to a regular array first',
      'Binary search does not work on circular arrays',
    ],
    correctAnswer: 1,
    explanation: 'First find the rotation index (start of sorted order) using binary search in O(log n). Then perform standard binary search with index mapping: for any logical index i, the actual index is (rotation + i) % n. Total time: O(log n) for both steps.',
    tags: ['circular-array', 'index-mapping', 'rotated-search'],
  },
  {
    id: 'algorithms-binary-search-l3-009',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the minimum number of days to make m bouquets from n flowers, where flower i blooms on day bloomDay[i]?',
    options: [
      'Simulate day by day',
      'Binary search on the number of days: check if enough adjacent groups of k consecutive bloomed flowers exist',
      'Sort flowers by bloom day and pick greedily',
      'Use a priority queue to process flowers in bloom order',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on day d from min(bloomDay) to max(bloomDay). For each d, scan the array counting consecutive bloomed flowers (bloomDay[i] <= d). Each group of k consecutive bloomed flowers makes one bouquet. If total bouquets >= m, d is feasible. Time O(n * log(max_day)).',
    tags: ['bouquets', 'search-on-answer', 'consecutive-groups'],
  },
  {
    id: 'algorithms-binary-search-l3-010',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'What is the "book allocation" problem and how is it solved?',
    options: [
      'Binary search on the maximum pages assigned to any student, with greedy verification',
      'Sort books and assign greedily',
      'Dynamic programming on students and books',
      'Both binary search and DP work, but binary search is more efficient',
    ],
    correctAnswer: 3,
    explanation: 'Allocate n books to m students (each gets consecutive books) minimizing the maximum total pages any student reads. Binary search on max pages from max(pages) to sum(pages). Greedy check: assign books sequentially until the limit is reached, count students needed. Also solvable with DP in O(n * m). Binary search: O(n * log(sum)).',
    tags: ['book-allocation', 'search-on-answer', 'minimize-maximum'],
  },
  {
    id: 'algorithms-binary-search-l3-011',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you count the number of pairs (i, j) with i < j such that arr[i] + arr[j] <= target in a sorted array?',
    options: [
      'Binary search for each element: count = sum over i of upper_bound(target - arr[i]) - i - 1',
      'Two pointers: lo=0, hi=n-1, count pairs when sum <= target',
      'Both approaches are correct',
      'Neither approach is correct',
    ],
    correctAnswer: 2,
    explanation: 'Two pointers: if arr[lo] + arr[hi] <= target, all pairs (lo, lo+1..hi) work, add (hi - lo), then lo++. Else hi--. O(n). Binary search: for each i, find the largest j > i with arr[i] + arr[j] <= target using binary search. Count = sum of (j - i). O(n log n). Both are correct.',
    tags: ['pair-counting', 'two-pointers', 'binary-search'],
  },
  {
    id: 'algorithms-binary-search-l3-012',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How does the "Russian Doll Envelopes" problem use binary search?',
    options: [
      'Binary search on envelope sizes',
      'Sort by width ascending, then by height descending for same width; compute LIS on heights using O(n log n) binary search',
      'Sort by area and greedily nest',
      'Use 2D binary search on both dimensions',
    ],
    correctAnswer: 1,
    explanation: 'Sort envelopes by width ascending; for equal widths, sort by height descending (to prevent using two envelopes of the same width). Then find the LIS on heights using patience sorting with binary search. The descending height for same width ensures we do not pick both. Time O(n log n).',
    tags: ['russian-dolls', 'lis', 'sorting-trick'],
  },
  {
    id: 'algorithms-binary-search-l3-013',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you find the number of times a sorted array has been rotated if duplicates are allowed?',
    options: [
      'Always O(log n) by finding the minimum index',
      'O(n) worst case due to duplicates, but O(log n) average by finding the minimum index with duplicate handling',
      'O(n log n) using sorting',
      'It is impossible with duplicates',
    ],
    correctAnswer: 1,
    explanation: 'The rotation count equals the index of the minimum element. With duplicates, the worst case is O(n) (e.g., [2,2,2,2,0,2,2] requires checking most elements). On average, binary search with the skip-duplicates optimization (lo++, hi--) gives O(log n).',
    tags: ['rotation-count', 'duplicates', 'worst-case'],
  },
  {
    id: 'algorithms-binary-search-l3-014',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you solve "nth magical number" (find the nth positive integer divisible by a or b)?',
    options: [
      'Generate multiples of a and b, merge, and find the nth',
      'Binary search on the answer: count numbers <= x divisible by a or b using inclusion-exclusion: x/a + x/b - x/lcm(a,b)',
      'Use a priority queue to generate numbers in order',
      'Mathematical formula using GCD',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on value x from 1 to n * min(a,b). Count magical numbers <= x using inclusion-exclusion: floor(x/a) + floor(x/b) - floor(x/lcm(a,b)). If count >= n, search smaller. Time O(log(n * min(a,b))). The counting formula uses LCM to avoid double-counting multiples of both a and b.',
    tags: ['magical-number', 'inclusion-exclusion', 'lcm'],
  },
  {
    id: 'algorithms-binary-search-l3-015',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you find the longest subarray with sum <= k in an array of positive integers?',
    options: [
      'Check all subarrays',
      'Sliding window with two pointers is optimal at O(n)',
      'Binary search on prefix sums: for each prefix[i], binary search for the largest j such that prefix[i] - prefix[j] <= k',
      'Both sliding window O(n) and binary search on prefix sums O(n log n) work',
    ],
    correctAnswer: 3,
    explanation: 'For positive integers, prefix sums are monotonically increasing. Use sliding window O(n): expand right, shrink left when sum > k. Or binary search on prefix sums: for each i, binary search for the leftmost j where prefix[i] - prefix[j] <= k. Both work; sliding window is more efficient.',
    tags: ['subarray-sum', 'prefix-sums', 'sliding-window'],
  },
  {
    id: 'algorithms-binary-search-l3-016',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'What is the "split array largest sum" problem equivalent to?',
    options: [
      'Maximum subarray problem',
      'Minimizing the maximum subarray sum when splitting into k parts — equivalent to "painters partition" and "book allocation"',
      'Finding the kth largest element',
      'Matrix chain multiplication',
    ],
    correctAnswer: 1,
    explanation: 'All three problems — split array largest sum, painters partition, and book allocation — are structurally identical: partition a linear array into k contiguous parts to minimize the maximum part sum. Binary search on the answer with greedy verification solves all in O(n * log(sum)). DP also works in O(n^2 * k).',
    tags: ['split-array', 'painters-partition', 'equivalence'],
  },
  {
    id: 'algorithms-binary-search-l3-017',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How do you find the number of subarrays with sum in range [lo, hi] in an array of positive integers?',
    options: [
      'Check all subarrays',
      'Compute count(sum <= hi) - count(sum <= lo - 1), where each count uses prefix sums with binary search or two pointers',
      'Sort the array and use binary search',
      'Use a hash map to count prefix sums',
    ],
    correctAnswer: 1,
    explanation: 'For positive integers, prefix sums are sorted. Count subarrays with sum <= x: for each prefix[i], binary search for the earliest prefix[j] where prefix[i] - prefix[j] <= x. Total: O(n log n). Alternatively, two pointers gives O(n). Answer = count(<=hi) - count(<=lo-1).',
    tags: ['range-sum', 'prefix-sums', 'counting'],
  },
  {
    id: 'algorithms-binary-search-l3-018',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you find the median of a sorted array that has been rotated?',
    options: [
      'Find the minimum (rotation point), then compute the median index using modular arithmetic',
      'Sort the array and find the median',
      'Binary search directly for the median value',
      'The median is always at the rotation point',
    ],
    correctAnswer: 0,
    explanation: 'First find the rotation index r (minimum element) using binary search in O(log n). The median in a sorted array is at index n/2. In the rotated array, the actual index is (r + n/2) % n. Total time: O(log n) for finding the rotation point, O(1) for the median.',
    tags: ['rotated-median', 'index-mapping', 'modular'],
  },
  {
    id: 'algorithms-binary-search-l3-019',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you find the maximum distance to the closest person in a row of seats (some occupied, some empty)?',
    options: [
      'Binary search on the distance, checking if a seat at that distance from all occupied seats exists',
      'Linear scan tracking gaps between occupied seats',
      'Both approaches work',
      'Use BFS from occupied seats',
    ],
    correctAnswer: 2,
    explanation: 'Linear approach: track the size of each gap between occupied seats. The maximum distance is max(gap/2) for interior gaps, or the gap length for edge gaps. O(n). Binary search approach: binary search on distance d, check if any empty seat is at least d from all occupied seats. O(n log n). Both correct.',
    tags: ['maximize-distance', 'seats', 'greedy'],
  },
  {
    id: 'algorithms-binary-search-l3-020',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Sorted Array Search',
    question: 'How do you use binary search to solve "find the duplicate number" in an array of n+1 integers in range [1, n]?',
    options: [
      'Sort the array and check adjacent elements',
      'Binary search on value: count numbers <= mid; if count > mid, the duplicate is in [lo, mid]',
      'Use XOR to find the duplicate',
      'Use Floyd\'s tortoise and hare cycle detection',
    ],
    correctAnswer: 1,
    explanation: 'Binary search on the value range [1, n]. For mid, count elements in the array <= mid. By pigeonhole: if count > mid, at least one number in [1, mid] appears more than once (search left). Otherwise search right. Time O(n log n), space O(1). Floyd\'s cycle detection also works in O(n).',
    tags: ['find-duplicate', 'pigeonhole', 'value-binary-search'],
  },
  {
    id: 'algorithms-binary-search-l3-021',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you find the kth smallest distance among all pairs in an array?',
    options: [
      'Compute all pairwise distances, sort, and return the kth',
      'Sort the array, binary search on the distance d, count pairs with distance <= d using two pointers in O(n)',
      'Use a heap to generate distances in order',
      'Binary search on pair indices',
    ],
    correctAnswer: 1,
    explanation: 'Sort the array. Binary search on distance d from 0 to max-min. For each d, count pairs with |arr[i]-arr[j]| <= d using a sliding window or two pointers (for each i, find the rightmost j with arr[j]-arr[i] <= d). If count >= k, search smaller. Time O(n * log(max-min) + n log n).',
    tags: ['kth-distance', 'pair-counting', 'two-pointers'],
  },
  {
    id: 'algorithms-binary-search-l3-022',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Lower/Upper Bound',
    question: 'How do you implement an order-statistic data structure that supports insert, delete, and kth-smallest in O(log n)?',
    options: [
      'Sorted array with binary search',
      'Augmented balanced BST (e.g., AVL or Red-Black tree) storing subtree sizes',
      'Hash map with counting',
      'Binary Indexed Tree (Fenwick tree) on the value range',
    ],
    correctAnswer: 1,
    explanation: 'An augmented balanced BST stores the subtree size at each node. Kth-smallest: compare k with left subtree size to decide direction. Insert/delete maintain subtree sizes in O(log n). Fenwick tree on value range also works for O(log n) operations if values are bounded.',
    tags: ['order-statistics', 'augmented-bst', 'kth-smallest'],
  },
  {
    id: 'algorithms-binary-search-l3-023',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Search on Answer',
    question: 'How do you solve "minimize the maximum difference between adjacent elements after removing k elements"?',
    options: [
      'Sort and remove the k elements with the largest gaps',
      'Binary search on the answer d: check if you can keep n-k elements with all adjacent differences <= d',
      'Dynamic programming on element positions',
      'Greedy: remove elements adjacent to the largest gap',
    ],
    correctAnswer: 1,
    explanation: 'Sort the array. Binary search on maximum difference d. Check: can we select n-k elements from the sorted array such that consecutive selected elements differ by at most d? Greedily select: include the first element, then include the next element with gap <= d. If enough elements are selected, d is feasible.',
    tags: ['minimize-max-difference', 'search-on-answer', 'selection'],
  },
  {
    id: 'algorithms-binary-search-l3-024',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Rotated Arrays',
    question: 'How do you find the kth missing positive number from a sorted array?',
    options: [
      'Iterate through positive integers and check each',
      'Binary search: at index i, there are arr[i] - (i + 1) missing numbers before arr[i]; find the index where this count reaches k',
      'Use a hash set and check numbers 1 to n+k',
      'Both linear O(n) iteration and binary search O(log n) work',
    ],
    correctAnswer: 3,
    explanation: 'At index i, the number of missing positive integers before arr[i] is arr[i] - (i + 1). Binary search for the smallest i where arr[i] - (i + 1) >= k. The answer is k + the index. Time O(log n). Linear scan also works: track expected vs actual, time O(n). Both are correct.',
    tags: ['kth-missing', 'binary-search', 'counting-formula'],
  },
  {
    id: 'algorithms-binary-search-l3-025',
    language: 'algorithms',
    level: 'level3',
    category: 'Binary Search',
    subcategory: 'Matrix Search',
    question: 'How do you find the nth ugly number (numbers whose prime factors only include 2, 3, and 5)?',
    options: [
      'Check each integer for ugly-ness until finding n of them',
      'Use three pointers tracking the next multiple of 2, 3, and 5 in the ugly sequence',
      'Binary search on the value, counting ugly numbers <= x using inclusion-exclusion',
      'Both three-pointer O(n) and binary search O(log^2(answer)) approaches work',
    ],
    correctAnswer: 3,
    explanation: 'Three pointers: maintain pointers i2, i3, i5 into the ugly sequence. Next ugly = min(ugly[i2]*2, ugly[i3]*3, ugly[i5]*5). O(n). Binary search: binary search on value x, count ugly numbers <= x. For three primes this uses inclusion-exclusion with GCD/LCM. O(log(answer) * log(answer)).',
    tags: ['ugly-number', 'three-pointers', 'inclusion-exclusion'],
  },
];

export default questions;
