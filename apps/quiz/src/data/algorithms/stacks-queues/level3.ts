import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'algorithms-stacks-queues-l3-001',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Monotonic Stack',
    question: 'The "trapping rain water" problem can be solved with a monotonic stack. How does the stack-based approach calculate water?',
    options: [
      'It sums the difference between each bar and the maximum bar',
      'It processes horizontal layers: when a bar higher than the stack top is found, water is trapped between the current bar and the second element in the stack',
      'It computes vertical water columns for each position',
      'It uses two separate stacks for left and right boundaries',
    ],
    correctAnswer: 1,
    explanation: 'The stack stores indices in decreasing height order. When a taller bar is found, pop the top (this is the bottom of the water layer). The water width is between the current bar and the new stack top. Water height is min(current, new_top) - popped_height. This processes water in horizontal layers.',
    tags: ['trapping-rain-water', 'horizontal-layers', 'monotonic-stack'],
  },
  {
    id: 'algorithms-stacks-queues-l3-002',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'BFS with Queue',
    question: 'The "shortest path in a binary matrix" (from top-left to bottom-right, moving in 8 directions through 0-cells) uses BFS. What is the time complexity?',
    options: [
      'O(n)',
      'O(n^2) where n is the matrix side length',
      'O(n * m) for n rows and m columns',
      'O(8 * n^2)',
    ],
    correctAnswer: 1,
    explanation: 'BFS visits each cell at most once. For an n x n matrix, there are n^2 cells, each with at most 8 neighbors to check. Total: O(n^2) time and O(n^2) space for the visited matrix and queue.',
    tags: ['binary-matrix', 'BFS', '8-directional'],
  },
  {
    id: 'algorithms-stacks-queues-l3-003',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Valid Parentheses',
    question: 'The "minimum remove to make valid parentheses" problem can be solved in O(n) time and O(1) extra space (not counting output) by:',
    options: [
      'Using a stack to find all invalid indices',
      'Two passes: left-to-right to remove excess ")", then right-to-left to remove excess "("',
      'Sorting the parentheses',
      'Using regex replacement',
    ],
    correctAnswer: 1,
    explanation: 'First pass (left to right): track open count, skip ")" when open == 0. Second pass (right to left): track close count, skip "(" when close == 0. This removes the minimum brackets. Building the result string uses O(n) for the output, but the logic uses O(1) extra space.',
    tags: ['minimum-remove', 'two-pass', 'O1-space'],
  },
  {
    id: 'algorithms-stacks-queues-l3-004',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Min Stack',
    question: 'The "online stock span" problem efficiently computes the stock span (consecutive days with price <= today) using a stack of (price, span) pairs. What is the amortized cost per call?',
    options: [
      'O(n)',
      'O(log n)',
      'O(1)',
      'O(sqrt(n))',
    ],
    correctAnswer: 2,
    explanation: 'Each price is pushed once and popped at most once across all calls. The while loop pops and accumulates spans. Over n calls, total pushes = n and total pops <= n. By the aggregate method, amortized cost per call is O(2n/n) = O(1).',
    tags: ['stock-span', 'amortized-O1', 'aggregate-method'],
  },
  {
    id: 'algorithms-stacks-queues-l3-005',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Deque',
    question: 'The "constrained subsequence sum" problem (max sum subsequence where consecutive chosen indices differ by at most k) combines DP with a deque. Why?',
    options: [
      'The deque stores all possible subsequences',
      'The deque maintains a sliding window maximum of the last k DP values, providing the optimal previous choice in O(1)',
      'The deque sorts the DP values',
      'The deque is used for BFS',
    ],
    correctAnswer: 1,
    explanation: 'dp[i] = arr[i] + max(0, max(dp[j] for i-k <= j < i)). The inner max over a sliding window of size k is computed with a monotonic deque in O(1) amortized. Total: O(n) time instead of O(n*k) brute force.',
    tags: ['constrained-subsequence', 'deque-dp', 'sliding-window-max'],
  },
  {
    id: 'algorithms-stacks-queues-l3-006',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Monotonic Stack',
    question: 'The "sum of subarray minimums" problem computes the sum of min(subarray) over all subarrays. The monotonic stack approach runs in:',
    options: [
      'O(n^2)',
      'O(n log n)',
      'O(n)',
      'O(n * sqrt(n))',
    ],
    correctAnswer: 2,
    explanation: 'For each element, find how many subarrays have it as the minimum. Using a monotonic increasing stack, compute the distance to the previous smaller element (left) and next smaller element (right). Contribution of arr[i] = arr[i] * left[i] * right[i]. Sum all contributions in O(n).',
    codeSnippet: 'stack = []\nleft = [0] * n  # distance to previous smaller\nfor i in range(n):\n  while stack and arr[stack[-1]] >= arr[i]:\n    stack.pop()\n  left[i] = i - stack[-1] if stack else i + 1\n  stack.append(i)\n# Similarly compute right[], then sum arr[i] * left[i] * right[i]',
    tags: ['subarray-minimums', 'contribution-technique', 'monotonic-stack'],
  },
  {
    id: 'algorithms-stacks-queues-l3-007',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'BFS with Queue',
    question: '0-1 BFS (graph with edges of weight 0 or 1) uses a deque instead of a priority queue. How?',
    options: [
      'Push 0-weight edges to the back and 1-weight edges to the front',
      'Push 0-weight neighbors to the front and 1-weight neighbors to the back of the deque',
      'Use two separate queues',
      'Sort the deque after each insertion',
    ],
    correctAnswer: 1,
    explanation: '0-weight edges do not increase distance, so add those neighbors to the front (process them first, like the current level). 1-weight edges increase distance by 1, so add to the back (process later). This gives shortest paths in O(V + E), avoiding O(E log V) for Dijkstra.',
    tags: ['0-1-BFS', 'deque', 'weighted-graph'],
  },
  {
    id: 'algorithms-stacks-queues-l3-008',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Valid Parentheses',
    question: 'The "minimum insertions to balance a parentheses string" where each "(" needs two consecutive "))" uses:',
    options: [
      'A simple counter',
      'Tracking open count and needed insertions, handling the case where a single ")" appears (requires inserting another ")")',
      'A stack of parentheses',
      'Dynamic programming',
    ],
    correctAnswer: 1,
    explanation: 'Track open count (unmatched "(") and insertions needed. For ")": if next char is also ")", consume both and match with an open (or add 1 insertion for "("). If single ")", add 1 insertion for the missing ")". Count remaining open * 2 for missing "))" at the end.',
    tags: ['minimum-insertions', 'balance', 'greedy'],
  },
  {
    id: 'algorithms-stacks-queues-l3-009',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Min Stack',
    question: 'The "number of visible people in a queue" problem (person i can see person j if all people between i and j are shorter than both) uses a monotonic stack. What is stored?',
    options: [
      'Heights in increasing order',
      'Heights in decreasing order; when a taller person is found, all shorter people it "passes" are visible to previous people',
      'Indices only',
      'Pairs of (height, visible count)',
    ],
    correctAnswer: 1,
    explanation: 'Process right to left with a monotonic decreasing stack. For each person, pop all shorter people (these are visible). If the stack is non-empty, the remaining top person is also visible (the first taller person seen). Count popped elements + 1 (if stack non-empty). Time O(n).',
    tags: ['visible-people', 'monotonic-decreasing', 'stack'],
  },
  {
    id: 'algorithms-stacks-queues-l3-010',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Deque',
    question: 'The "jump game VI" problem (reach the last index with max score, can jump 1 to k steps) uses DP with a deque for:',
    options: [
      'BFS to find the shortest path',
      'Maintaining the maximum DP value in a sliding window of size k',
      'Sorting possible jumps',
      'Storing all possible paths',
    ],
    correctAnswer: 1,
    explanation: 'dp[i] = nums[i] + max(dp[j] for max(0, i-k) <= j < i). The max over a sliding window is maintained with a monotonic decreasing deque. For each i, remove out-of-window indices from front, dp[i] = nums[i] + dp[deque_front]. Remove smaller values from back before adding i. Total O(n).',
    tags: ['jump-game', 'deque-dp', 'sliding-window-max'],
  },
  {
    id: 'algorithms-stacks-queues-l3-011',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Monotonic Stack',
    question: 'The "132 pattern" problem (find i < j < k where arr[i] < arr[k] < arr[j]) uses a stack traversing right to left. What does the stack maintain?',
    options: [
      'Potential "3" candidates (arr[j])',
      'Potential "2" candidates (arr[k]) in the stack, while tracking the largest popped value as the "2" candidate',
      'All three elements of the pattern',
      'Indices in sorted order',
    ],
    correctAnswer: 1,
    explanation: 'Traverse right to left. Maintain a stack of "3" candidates (decreasing). When encountering a larger value, pop smaller values; the largest popped becomes the "2" candidate. If any value < "2" candidate is found, a 132 pattern exists. Time O(n), space O(n).',
    tags: ['132-pattern', 'stack', 'candidate-tracking'],
  },
  {
    id: 'algorithms-stacks-queues-l3-012',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'BFS with Queue',
    question: 'The "minimum knight moves" on an infinite chessboard from (0,0) to (x,y) uses BFS with what optimization?',
    options: [
      'No optimization needed; standard BFS works',
      'Pruning the search space by restricting to a bounded region and using symmetry (only consider |x|, |y|)',
      'Using DFS instead',
      'Sorting moves by distance',
    ],
    correctAnswer: 1,
    explanation: 'Exploit symmetry: work with |x|, |y| (all quadrants are symmetric). Bound the search: the knight never needs to go more than a few steps beyond the target. Bidirectional BFS or A* can further optimize. Without bounds, BFS on an infinite board would not terminate.',
    tags: ['knight-moves', 'BFS', 'pruning-symmetry'],
  },
  {
    id: 'algorithms-stacks-queues-l3-013',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Valid Parentheses',
    question: 'The "check if a parentheses string can be valid" problem (some positions are locked, others can be changed to either bracket) uses:',
    options: [
      'A stack-based simulation',
      'Two greedy passes: left-to-right checking min/max possible open count, right-to-left checking min/max close count',
      'Dynamic programming on all possible strings',
      'Brute force trying all combinations',
    ],
    correctAnswer: 1,
    explanation: 'Left-to-right: track the range [minOpen, maxOpen] of possible open bracket counts. Locked chars force a specific change; unlocked can go either way. If maxOpen < 0, invalid. If minOpen < 0, reset to 0. At the end, minOpen must be 0. A right-to-left pass confirms. Time O(n).',
    tags: ['flexible-parentheses', 'range-tracking', 'greedy'],
  },
  {
    id: 'algorithms-stacks-queues-l3-014',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Min Stack',
    question: 'The "basic calculator III" (supports +, -, *, /, and parentheses) can be implemented with:',
    options: [
      'A single stack',
      'Two stacks (one for numbers, one for operators) with operator precedence handling',
      'Shunting-yard algorithm to convert to RPN, then evaluate',
      'Both B and C work correctly',
    ],
    correctAnswer: 3,
    explanation: 'Dijkstra\'s shunting-yard converts infix to postfix (RPN) respecting precedence, then evaluates with a stack. Alternatively, two stacks handle operators by comparing precedence before pushing. Both correctly handle +, -, *, /, and nested parentheses in O(n) time.',
    tags: ['calculator', 'shunting-yard', 'operator-precedence'],
  },
  {
    id: 'algorithms-stacks-queues-l3-015',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Deque',
    question: 'The "design a front-middle-back queue" (O(1) push/pop at front, middle, and back) can be implemented using:',
    options: [
      'A single deque',
      'Two deques where the first holds the first half and the second holds the second half, rebalanced to maintain size invariant',
      'A balanced BST',
      'Three stacks',
    ],
    correctAnswer: 1,
    explanation: 'Use two deques: front_half and back_half. Maintain invariant: front_half.size() == back_half.size() or front_half.size() == back_half.size() - 1. The middle element is at the junction. Rebalance after each operation by moving one element. All operations O(1).',
    tags: ['front-middle-back', 'two-deques', 'rebalancing'],
  },
  {
    id: 'algorithms-stacks-queues-l3-016',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Monotonic Stack',
    question: 'For the "sum of subarray ranges" problem (sum of max(subarray) - min(subarray) over all subarrays), the O(n) approach computes:',
    options: [
      'Max and min for each subarray independently',
      'Sum of all subarray maximums minus sum of all subarray minimums, each computed with a monotonic stack',
      'A running max and min',
      'The range of the entire array times the number of subarrays',
    ],
    correctAnswer: 1,
    explanation: 'Sum of ranges = Sum of maxs - Sum of mins. Each sum is computed independently using the contribution technique: for each element, count how many subarrays have it as max (or min) using previous/next greater (or smaller) elements from a monotonic stack. Total: O(n).',
    tags: ['subarray-ranges', 'contribution', 'decomposition'],
  },
  {
    id: 'algorithms-stacks-queues-l3-017',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'BFS with Queue',
    question: 'The "open the lock" problem (BFS from "0000" to target, avoiding deadends) has a state space of size:',
    options: [
      '10^4 = 10000 possible combinations',
      '4^10 combinations',
      'Infinite',
      '2^4 = 16 combinations',
    ],
    correctAnswer: 0,
    explanation: 'Each of the 4 wheels has 10 positions (0-9), giving 10^4 = 10000 possible states. BFS explores at most 10000 states. Each state has 8 neighbors (4 wheels x 2 directions). Time: O(10000 * 8) = O(80000) = O(1) since the state space is bounded.',
    tags: ['open-lock', 'BFS', 'state-space'],
  },
  {
    id: 'algorithms-stacks-queues-l3-018',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Valid Parentheses',
    question: 'The "decode string" problem (e.g., "3[a2[c]]" -> "accaccacc") uses a stack to handle:',
    options: [
      'Character frequency counting',
      'Nested repetitions by pushing the current string and multiplier before each "[" and restoring on "]"',
      'Sorting characters',
      'Pattern matching',
    ],
    correctAnswer: 1,
    explanation: 'On "[": push current string and current number onto the stack, reset both. On "]": pop the number k and previous string, set current = previous + current * k. This naturally handles nesting because inner brackets resolve first. Time O(output_length).',
    tags: ['decode-string', 'nested-repetition', 'stack'],
  },
  {
    id: 'algorithms-stacks-queues-l3-019',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Min Stack',
    question: 'A "stack with increment operation" supports push, pop, and increment(k, val) which adds val to the bottom k elements. The O(1) lazy approach uses:',
    options: [
      'Incrementing all k elements immediately',
      'An auxiliary array where increment[i] stores pending increments, applied lazily on pop',
      'A segment tree',
      'A hash map of increments',
    ],
    correctAnswer: 1,
    explanation: 'Maintain an inc[] array. increment(k, val): inc[min(k, stack_size) - 1] += val. On pop: add inc[top] to the popped value, carry inc[top] to inc[top-1] (propagate downward), reset inc[top] = 0. This defers the work, keeping each operation O(1).',
    codeSnippet: 'def increment(k, val):\n  idx = min(k, len(stack)) - 1\n  if idx >= 0:\n    inc[idx] += val\n\ndef pop():\n  idx = len(stack) - 1\n  result = stack.pop() + inc[idx]\n  if idx > 0:\n    inc[idx - 1] += inc[idx]\n  inc[idx] = 0\n  return result',
    tags: ['lazy-increment', 'stack-with-increment', 'O1-trick'],
  },
  {
    id: 'algorithms-stacks-queues-l3-020',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Deque',
    question: 'The "maximum value of equation" problem (max yi + yj + |xi - xj| where xi < xj and xj - xi <= k) uses a deque to:',
    options: [
      'Sort the points by x-coordinate',
      'Maintain a monotonic deque of (yi - xi) values as candidates, giving the best left point for each right point',
      'Store all possible pairs',
      'Implement a priority queue',
    ],
    correctAnswer: 1,
    explanation: 'Rewrite: yi + yj + xj - xi = (yi - xi) + (yj + xj). For each point j, maximize (yi - xi) for valid i (within distance k). Use a monotonic decreasing deque of (yi - xi, xi) pairs. Remove expired entries from front, query front for max, remove smaller from back before adding. O(n) total.',
    tags: ['max-equation', 'deque', 'algebraic-transformation'],
  },
  {
    id: 'algorithms-stacks-queues-l3-021',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Monotonic Stack',
    question: 'The "remove k digits" problem (remove k digits from a number string to minimize the result) uses a monotonic stack to:',
    options: [
      'Sort the digits',
      'Maintain non-decreasing digits from left to right, removing larger preceding digits when a smaller digit is found',
      'Find the k largest digits',
      'Reverse the number',
    ],
    correctAnswer: 1,
    explanation: 'Greedily maintain a non-decreasing stack. For each digit, while the stack top is larger than the current digit and removals remain (k > 0), pop (remove that digit). Push current digit. After processing, if k > 0, remove from the end. Strip leading zeros. Time O(n).',
    codeSnippet: 'stack = []\nfor digit in num:\n  while k > 0 and stack and stack[-1] > digit:\n    stack.pop()\n    k -= 1\n  stack.append(digit)\n# Remove remaining k from end\nstack = stack[:len(stack) - k]\nreturn "".join(stack).lstrip("0") or "0"',
    tags: ['remove-k-digits', 'greedy', 'monotonic-stack'],
  },
  {
    id: 'algorithms-stacks-queues-l3-022',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'BFS with Queue',
    question: 'The "sliding puzzle" problem (solve a 2x3 sliding puzzle using BFS) has a state space of:',
    options: [
      '720 (6! permutations)',
      '6^6 states',
      '2^6 states',
      '360 reachable states',
    ],
    correctAnswer: 3,
    explanation: 'The board has 6 tiles (including the blank), giving 6! = 720 permutations. However, only half are reachable from any starting position (determined by inversion count parity). So at most 360 states are explored. BFS finds the shortest solution in O(1) since the state space is bounded.',
    tags: ['sliding-puzzle', 'BFS', 'state-space-size'],
  },
  {
    id: 'algorithms-stacks-queues-l3-023',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Valid Parentheses',
    question: 'The "number of ways to remove invalid parentheses" to get the longest valid string is closely related to the Catalan number. For a string of n opening and n closing brackets with k mismatched, the count of valid removals involves:',
    options: [
      'Simply C(2n, k)',
      'Finding all distinct valid strings after removing exactly k brackets, which requires careful deduplication',
      'The answer is always 1',
      'Factorial of the string length',
    ],
    correctAnswer: 1,
    explanation: 'The key challenge is deduplication. Multiple removal sequences can produce the same valid string. BFS with string hashing or careful pruning (e.g., only removing the first occurrence in a run of identical brackets, and only removing at or after the last removal index) avoids generating duplicates.',
    tags: ['removal-counting', 'deduplication', 'catalan-related'],
  },
  {
    id: 'algorithms-stacks-queues-l3-024',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Min Stack',
    question: 'The "asteroid collision" problem (positive asteroids move right, negative move left, collisions destroy smaller one) uses a stack. What is the tricky edge case?',
    options: [
      'All asteroids moving in the same direction',
      'When a left-moving asteroid destroys multiple right-moving ones in sequence, and also the case of equal-size collision (both destroyed)',
      'Empty input',
      'Single asteroid',
    ],
    correctAnswer: 1,
    explanation: 'A left-moving asteroid may destroy multiple right-moving ones (loop while stack top is positive and smaller). Equal size: both destroyed. The left-mover survives if it destroys all smaller right-movers. Care needed to handle the loop termination and the equal-size case correctly.',
    tags: ['asteroid-collision', 'stack', 'edge-cases'],
  },
  {
    id: 'algorithms-stacks-queues-l3-025',
    language: 'algorithms',
    level: 'level3',
    category: 'Stacks & Queues',
    subcategory: 'Deque',
    question: 'The "longest continuous subarray with absolute diff at most limit" problem uses:',
    options: [
      'A single monotonic deque',
      'Two monotonic deques (one for max, one for min) with a sliding window',
      'A balanced BST',
      'A heap',
    ],
    correctAnswer: 1,
    explanation: 'Maintain a decreasing deque for the window max and an increasing deque for the window min. When max - min > limit, shrink from the left. The window is valid when max_deque.front - min_deque.front <= limit. Each element enters and leaves each deque once. Total: O(n).',
    tags: ['absolute-diff', 'two-deques', 'sliding-window'],
  },
];

export default questions;
