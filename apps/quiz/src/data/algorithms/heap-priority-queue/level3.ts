import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'algorithms-heap-priority-queue-l3-001',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Median from Stream',
    question: 'You need to support insert, find-median, and delete any element from a stream. What combination of data structures achieves O(log n) for all three operations?',
    options: [
      'Two heaps with lazy deletion and a hash map of deleted elements',
      'A single balanced BST (like AVL or Red-Black tree)',
      'Two heaps with immediate deletion using index tracking',
      'Both B and C work equally well',
    ],
    correctAnswer: 3,
    explanation: 'Both a balanced BST with order statistics and two heaps with index-tracked deletion can achieve O(log n) for all three operations. The BST approach uses the rank to find the median; the two-heap approach uses a position map for O(log n) arbitrary deletion.',
    tags: ['heap', 'median', 'advanced-deletion'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-002',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Scheduling',
    question: 'In the task scheduler problem (LeetCode 621), tasks have cooldown period n. What is the minimum intervals to finish all tasks?',
    options: [
      'Total tasks + idle slots, where idle slots depend on the most frequent task',
      'Total tasks * (n + 1)',
      'Number of distinct tasks * n',
      'Always equal to total number of tasks',
    ],
    correctAnswer: 0,
    explanation: 'The answer is max(total tasks, (maxFreq - 1) * (n + 1) + countOfMaxFreqTasks). The most frequent task determines the frame size. Idle slots appear when there are not enough distinct tasks to fill the gaps between repetitions of the most frequent task.',
    codeSnippet: 'maxFreq = max frequency of any task\ncountMax = number of tasks with maxFreq\nresult = max(len(tasks), (maxFreq - 1) * (n + 1) + countMax)',
    tags: ['priority-queue', 'scheduling', 'cooldown'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-003',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'What is the amortized time complexity of insert in a Fibonacci heap, and why?',
    options: [
      'O(log n) because it merges trees',
      'O(1) because it simply adds a new tree to the root list',
      'O(n) because it consolidates',
      'O(1) but only for the first n/2 inserts',
    ],
    correctAnswer: 1,
    explanation: 'Fibonacci heap insert is O(1) amortized because it simply creates a new single-node tree and adds it to the root list. No consolidation happens during insert. Consolidation is deferred to extract-min, which is O(log n) amortized.',
    tags: ['fibonacci-heap', 'amortized', 'insert'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-004',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Top-K Elements',
    question: 'You have a stream of elements and need to maintain the top-K at all times, but elements can also expire (sliding window of size W). What is the best approach?',
    options: [
      'Rebuild the heap of size K every W elements',
      'Use an indexed priority queue with a deque for expiration tracking',
      'Sort the window each time',
      'Use a simple min-heap of size K and ignore expirations',
    ],
    correctAnswer: 1,
    explanation: 'An indexed priority queue allows O(log n) insertion and deletion by key, combined with a deque to track element order for expiration. When the oldest element falls outside the window, delete it from the priority queue by its index in O(log n).',
    tags: ['heap', 'sliding-window', 'indexed-pq'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-005',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Merge K Sorted',
    question: 'You need to find the Kth smallest pair sum from two sorted arrays A and B of sizes M and N. What is the time complexity of the heap-based approach?',
    options: [
      'O(M * N)',
      'O(K log K)',
      'O(K log(min(K, M, N)))',
      'O((M + N) log(M + N))',
    ],
    correctAnswer: 2,
    explanation: 'Start with (A[0]+B[0]) in the heap. Each extraction adds at most 2 new pairs. The heap size is bounded by min(K, M, N). Extract K times with O(log(min(K, M, N))) per extraction. Use a visited set to avoid duplicates.',
    tags: ['heap', 'kth-smallest', 'pair-sum'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-006',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'In a min-max heap, elements at even levels satisfy the min-heap property and elements at odd levels satisfy the max-heap property. What is the time complexity of finding the maximum?',
    options: [
      'O(n)',
      'O(log n)',
      'O(1)',
      'O(n log n)',
    ],
    correctAnswer: 2,
    explanation: 'In a min-max heap, the maximum is among the children of the root (at level 1, which is a max level). Since the root has at most 2 children, finding the max requires checking at most 2 elements, which is O(1).',
    tags: ['min-max-heap', 'find-max', 'time-complexity'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-007',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Scheduling',
    question: 'You have N weighted jobs with start times, end times, and profits. Maximize profit without overlapping jobs. Which approach combines heap usage with DP?',
    options: [
      'Sort by start time, use a max-heap of profits',
      'Sort by end time, DP with binary search for last non-overlapping job',
      'Use a priority queue to greedily pick highest-profit jobs',
      'Sort by profit, use a min-heap to track conflicts',
    ],
    correctAnswer: 1,
    explanation: 'Sort jobs by end time. For each job i, use binary search to find the latest non-overlapping job j. DP: dp[i] = max(dp[i-1], profit[i] + dp[j]). This is O(N log N) for sorting and binary search. A heap is not the primary structure here; this is weighted interval scheduling.',
    tags: ['scheduling', 'dynamic-programming', 'weighted-intervals'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-008',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Top-K Elements',
    question: 'The quickselect algorithm finds the Kth largest element. What is its expected time complexity and worst case?',
    options: [
      'Expected O(n), worst O(n^2)',
      'Expected O(n log n), worst O(n log n)',
      'Expected O(n log K), worst O(n^2)',
      'Expected O(n), worst O(n log n)',
    ],
    correctAnswer: 0,
    explanation: 'Quickselect has expected O(n) time because on average the partition halves the search space. Worst case is O(n^2) when the pivot is always the minimum or maximum. Using median-of-medians as pivot gives guaranteed O(n) worst case.',
    tags: ['quickselect', 'order-statistics', 'randomized'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-009',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Median from Stream',
    question: 'You need the running median in a sliding window of size K. What is the per-element time complexity using two multisets (balanced BSTs)?',
    options: [
      'O(K)',
      'O(log K)',
      'O(K log K)',
      'O(1)',
    ],
    correctAnswer: 1,
    explanation: 'Using two balanced BSTs (multisets), insertion and deletion each take O(log K). For each new element in the sliding window, insert the new element and delete the expired element, rebalancing between the two halves. Total per-element cost is O(log K).',
    tags: ['median', 'sliding-window', 'balanced-bst'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-010',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'What is the time complexity of merging two binary heaps of sizes M and N?',
    options: [
      'O(M + N)',
      'O(M log N)',
      'O(N log M)',
      'O(min(M, N) * log(M + N))',
    ],
    correctAnswer: 0,
    explanation: 'Concatenate both arrays and run bottom-up heapify on the combined array. Heapify takes O(M + N) regardless of the initial order. This is faster than inserting elements one by one, which would take O(M log(M + N)) or O(N log(M + N)).',
    tags: ['heap', 'merge', 'heapify'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-011',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Merge K Sorted',
    question: 'You need to find the smallest range that includes at least one number from each of K sorted lists. What is the approach?',
    options: [
      'Sort all elements and use two pointers',
      'Use a min-heap of size K, track the current max, minimize (max - min_heap.peek())',
      'Use binary search on each list',
      'Merge all lists and find the smallest window with K distinct sources',
    ],
    correctAnswer: 1,
    explanation: 'Initialize the heap with the first element from each list and track the current maximum. The range is [heap.peek(), currentMax]. Extract min, advance that list, insert the next element, update max. Track the minimum range seen. O(N log K) time.',
    tags: ['heap', 'merge-k-sorted', 'smallest-range'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-012',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Top-K Elements',
    question: 'Reorganize a string so that no two adjacent characters are the same. Which approach uses a heap?',
    options: [
      'Sort characters and interleave',
      'Use a max-heap of (frequency, char), greedily place the most frequent char, then re-insert with decremented count',
      'Use a min-heap and always place the least frequent char',
      'Use a hash map and swap adjacent duplicates',
    ],
    correctAnswer: 1,
    explanation: 'Use a max-heap ordered by character frequency. Pop the most frequent character, place it, then push it back with decremented count. To avoid adjacent duplicates, hold back the previously placed character for one round before re-inserting.',
    codeSnippet: 'heap = MaxHeap of (freq, char)\nprev = null\nresult = []\nwhile heap is not empty:\n  curr = heap.pop()\n  result.append(curr.char)\n  if prev and prev.freq > 0:\n    heap.push(prev)\n  curr.freq -= 1\n  prev = curr',
    tags: ['heap', 'greedy', 'reorganize-string'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-013',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'A leftist heap supports merge in O(log n). What property does it maintain?',
    options: [
      'The left subtree is always taller than the right subtree',
      'The rank (shortest path to a null node) of the left child is >= the rank of the right child',
      'The left child is always smaller than the right child',
      'Both subtrees have equal height',
    ],
    correctAnswer: 1,
    explanation: 'A leftist heap maintains the leftist property: the rank (length of the rightmost path to null) of the left child is always >= that of the right child. This ensures the rightmost path is O(log n) long, allowing merge along this path in O(log n).',
    tags: ['leftist-heap', 'merge', 'rank-property'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-014',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Scheduling',
    question: 'In Dijkstra\'s algorithm with a binary heap, the bottleneck is decrease-key. How does using a d-ary heap with d = E/V optimize the algorithm for dense graphs?',
    options: [
      'It makes extract-min O(1)',
      'It reduces the total time to O(E log_{E/V} V), which is O(E) when E = V^2',
      'It eliminates the need for decrease-key',
      'It increases parallelism',
    ],
    correctAnswer: 1,
    explanation: 'With d = E/V, decrease-key is O(log_d V) = O(log_{E/V} V), and extract-min is O(d * log_d V) = O((E/V) * log_{E/V} V). Total: V * extract-min + E * decrease-key = O(E * log_{E/V} V). For E = V^2, log_{V} V = 1, so total is O(E).',
    tags: ['d-ary-heap', 'dijkstra', 'optimization'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-015',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Median from Stream',
    question: 'You have a stream where elements can be both added and removed. How do you maintain the median efficiently using two heaps with lazy deletion?',
    options: [
      'Delete immediately from the heap by scanning for the element',
      'Mark elements as deleted in a hash map, only physically remove when they appear at a heap root during extract',
      'Rebuild both heaps after each deletion',
      'Use a separate stack to track deletions',
    ],
    correctAnswer: 1,
    explanation: 'Lazy deletion marks elements as deleted in a hash map. During extract-min/max, skip deleted elements at the root. Track effective sizes of both heaps (total - deleted in each). This gives amortized O(log n) per operation.',
    tags: ['heap', 'lazy-deletion', 'median'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-016',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Top-K Elements',
    question: 'To find the K largest elements in a min-heap of N elements, what is the optimal time complexity?',
    options: [
      'O(N)',
      'O(N log K)',
      'O(K log N)',
      'O(N + K log K)',
    ],
    correctAnswer: 0,
    explanation: 'Since a min-heap does not help locate large elements efficiently, the best approach is to scan all N elements and use quickselect to find the Kth largest in O(N) expected time. The heap structure provides no advantage for finding large elements in a min-heap.',
    tags: ['heap', 'top-k', 'quickselect'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-017',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Merge K Sorted',
    question: 'An external sort processes data too large for memory. If you have B buffer pages and a file of N pages, what is the number of passes needed in a K-way merge sort?',
    options: [
      'ceil(log_2(N/B)) + 1',
      'ceil(log_{B-1}(N/B)) + 1',
      'N / B',
      'log_2(N)',
    ],
    correctAnswer: 1,
    explanation: 'Pass 0 creates ceil(N/B) sorted runs. Each subsequent pass merges B-1 runs at a time (1 buffer for output). The number of merge passes is ceil(log_{B-1}(N/B)). Total passes = 1 + ceil(log_{B-1}(ceil(N/B))). A heap is used for the K-way merge.',
    tags: ['external-sort', 'merge-k-sorted', 'I/O-complexity'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-018',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'What is the time complexity of the cascading cut operation in a Fibonacci heap, and why does it maintain O(1) amortized decrease-key?',
    options: [
      'O(n) worst case, but amortized O(1) using potential function analysis',
      'O(log n) worst case and amortized',
      'O(1) worst case',
      'O(n) both worst case and amortized',
    ],
    correctAnswer: 0,
    explanation: 'A cascading cut may propagate up through O(n) ancestors in the worst case. However, using the potential function Phi = trees + 2*marked_nodes, each cut reduces the potential enough to pay for the work. The amortized cost of decrease-key is O(1).',
    tags: ['fibonacci-heap', 'cascading-cut', 'amortized'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-019',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Scheduling',
    question: 'You have N CPUs and tasks with dependencies (DAG). Minimize the total completion time. What is this problem called, and what role does the priority queue play?',
    options: [
      'Knapsack problem; heap sorts by value/weight ratio',
      'Parallel task scheduling; heap selects the next available CPU (earliest finish time)',
      'Traveling salesman; heap provides nearest neighbor',
      'Maximum flow; heap implements augmenting paths',
    ],
    correctAnswer: 1,
    explanation: 'This is the parallel machine scheduling problem with precedence constraints. A min-heap tracks CPU availability (earliest finish time). Tasks are scheduled in topological order; each ready task is assigned to the CPU that becomes free earliest.',
    tags: ['scheduling', 'DAG', 'parallel'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-020',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Top-K Elements',
    question: 'Given an N x N matrix where each row and column is sorted, find the Kth smallest using a heap. How do you avoid adding duplicate entries?',
    options: [
      'Use a visited boolean matrix of size N x N',
      'Only expand rightward from the diagonal',
      'Track visited cells with a hash set of (row, col) pairs',
      'Both A and C work; A uses O(N^2) space, C uses O(K) space',
    ],
    correctAnswer: 3,
    explanation: 'Both approaches prevent duplicates. A visited matrix uses O(N^2) space but O(1) lookup. A hash set of coordinates uses O(K) space since at most K cells are visited. For large N but small K, the hash set is more space-efficient.',
    tags: ['heap', 'sorted-matrix', 'deduplication'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-021',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'A skew heap is a self-adjusting variant of a leftist heap. What is its key operation and amortized merge complexity?',
    options: [
      'Rotate after every insertion; O(log n) amortized merge',
      'Unconditionally swap left and right children during merge; O(log n) amortized merge',
      'Double the heap capacity on overflow; O(1) amortized merge',
      'Splay the accessed node; O(log n) amortized merge',
    ],
    correctAnswer: 1,
    explanation: 'A skew heap always swaps left and right children at every node along the merge path. Unlike leftist heaps, it does not track rank. The amortized merge complexity is O(log n), proven using a potential function based on the number of heavy nodes.',
    tags: ['skew-heap', 'merge', 'amortized'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-022',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Median from Stream',
    question: 'If you need to find the P-th percentile (not just median) from a data stream, how would you adapt the two-heap approach?',
    options: [
      'Keep both heaps equal in size',
      'Maintain the size ratio so that maxHeap has P% of elements and minHeap has (100-P)% of elements',
      'Use three heaps instead of two',
      'The two-heap approach cannot be adapted for arbitrary percentiles',
    ],
    correctAnswer: 1,
    explanation: 'Instead of keeping heaps balanced (50/50), maintain the lower max-heap at ceil(P/100 * n) elements and the upper min-heap at the rest. The P-th percentile is at the max-heap root. Rebalance after each insertion to maintain the ratio.',
    tags: ['heap', 'percentile', 'streaming'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-023',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Scheduling',
    question: 'In the IPO problem (LeetCode 502), you have capital W and can do at most K projects, each with a capital requirement and profit. Maximize final capital. What is the approach?',
    options: [
      'Dynamic programming on capital values',
      'Sort by capital, use a min-heap of available projects',
      'Sort by capital, use a max-heap of profits for affordable projects',
      'Greedy: always pick the project with highest profit regardless of capital',
    ],
    correctAnswer: 2,
    explanation: 'Sort projects by capital requirement. Use a max-heap of profits for projects whose capital requirement <= current capital. Greedily pick the most profitable available project, add its profit to capital, unlock more projects. Repeat K times. O(N log N) time.',
    tags: ['heap', 'greedy', 'IPO'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-024',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Merge K Sorted',
    question: 'You need to merge K sorted iterators (lazy streams) without loading all data into memory. What is the space complexity of the heap-based approach?',
    options: [
      'O(N) where N is total elements across all iterators',
      'O(K) plus one buffered element per iterator',
      'O(K * N/K) = O(N)',
      'O(K^2)',
    ],
    correctAnswer: 1,
    explanation: 'The heap holds K entries, one per iterator, each containing the current element and a reference to its iterator. The space is O(K) for the heap plus O(1) per iterator for the current buffered element. No need to load entire streams into memory.',
    tags: ['heap', 'merge-k-sorted', 'lazy-evaluation'],
  },
  {
    id: 'algorithms-heap-priority-queue-l3-025',
    language: 'algorithms',
    level: 'level3',
    category: 'Heap & Priority Queue',
    subcategory: 'Heap Operations',
    question: 'What is the tight bound on the number of comparisons needed to build a binary heap from n elements using the bottom-up approach?',
    options: [
      'Exactly n comparisons',
      'At most 2n - 2 comparisons',
      'At most n log n comparisons',
      'Exactly n/2 comparisons',
    ],
    correctAnswer: 1,
    explanation: 'The tight bound for bottom-up heap construction is at most 2n - 2 comparisons. This is derived from the sum of heights of all nodes: sum from i=1 to floor(log n) of floor(n / 2^i) * i, which sums to at most n - 1 sift operations, each requiring at most 2 comparisons.',
    tags: ['heap', 'heapify', 'comparison-count'],
  },
];

export default questions;
