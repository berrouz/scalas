import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'java-testing-l4-001',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'What is the mutation score and how is it calculated?',
    options: ['Ratio of code coverage to test count', 'Number of tests that pass', 'Ratio of killed mutants to total mutants, expressed as percentage', 'Number of mutants generated'],
    correctAnswer: 2,
    explanation: 'Mutation score = (killed mutants / total mutants) * 100%. A higher mutation score means the test suite is more effective at detecting code changes. A score of 100% means all mutants were killed.',
    tags: ['mutation-testing', 'mutation-score'],
  },
  {
    id: 'java-testing-l4-002',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'What is an "equivalent mutant" in mutation testing?',
    options: ['A mutant that kills all tests', 'A mutant that crashes the program', 'A mutant that is identical to another mutant', 'A mutant that produces the same output as the original code for all inputs'],
    correctAnswer: 3,
    explanation: 'An equivalent mutant is a mutation that does not change the observable behavior of the program for any input. These cannot be killed by any test and inflate the number of survived mutants. Detecting equivalent mutants is undecidable in general.',
    tags: ['mutation-testing', 'equivalent-mutant'],
  },
  {
    id: 'java-testing-l4-003',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you implement a custom Testcontainers module?',
    options: ['Extend GenericContainer and add domain-specific configuration', 'Implement the Container interface from scratch', 'Create a custom Docker image only', 'Use Docker SDK directly'],
    correctAnswer: 0,
    explanation: 'Custom Testcontainers modules extend GenericContainer (or another specialized container). Add configuration methods, wait strategies, and convenience accessors for your specific service.',
    codeSnippet: 'public class CustomServiceContainer\n    extends GenericContainer<\n        CustomServiceContainer> {\n\n    private static final int PORT = 9090;\n\n    public CustomServiceContainer() {\n        super("my-service:latest");\n        withExposedPorts(PORT);\n        waitingFor(Wait.forHttp("/health")\n            .forStatusCode(200));\n    }\n\n    public String getServiceUrl() {\n        return String.format("http://%s:%d",\n            getHost(), getMappedPort(PORT));\n    }\n}',
    tags: ['testcontainers', 'custom-module'],
  },
  {
    id: 'java-testing-l4-004',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How does Mockito create mocks internally?',
    options: ['Using Java reflection only', 'Using source code generation', 'Using Java Proxy for all types', 'Using ByteBuddy to generate subclasses at runtime'],
    correctAnswer: 3,
    explanation: 'Mockito uses ByteBuddy (previously CGLIB) to generate subclasses of the mocked type at runtime. For interfaces, Java dynamic proxies can also be used. The inline mock maker instruments classes directly.',
    tags: ['mockito', 'internals', 'bytebuddy'],
  },
  {
    id: 'java-testing-l4-005',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test database migrations with Flyway or Liquibase?',
    options: ['Test SQL syntax only', 'Run migrations against a real database in Testcontainers and verify schema', 'Skip migration testing', 'Use in-memory databases exclusively'],
    correctAnswer: 1,
    explanation: 'Test migrations by running them against a real database (same vendor as production) using Testcontainers. Verify schema structure, data transformations, and backward compatibility.',
    codeSnippet: '@Testcontainers\n@SpringBootTest\nclass MigrationTest {\n    @Container\n    @ServiceConnection\n    static PostgreSQLContainer<?> pg =\n        new PostgreSQLContainer<>("postgres:15");\n\n    @Autowired JdbcTemplate jdbc;\n\n    @Test\n    void shouldApplyAllMigrations() {\n        // Flyway runs automatically\n        Integer count = jdbc.queryForObject(\n            "SELECT count(*) FROM " +\n            "flyway_schema_history",\n            Integer.class);\n        assertTrue(count > 0);\n    }\n\n    @Test\n    void shouldHaveExpectedColumns() {\n        jdbc.execute(\n            "SELECT id, name, email, " +\n            "created_at FROM users LIMIT 1");\n    }\n}',
    tags: ['integration-testing', 'flyway', 'migrations'],
  },
  {
    id: 'java-testing-l4-006',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'Why is 100% code coverage not a sufficient quality metric?',
    options: ['Code coverage tools are inaccurate', 'It is too expensive to achieve', 'It is always sufficient', 'Coverage does not verify correctness - a test can execute code without meaningful assertions'],
    correctAnswer: 3,
    explanation: '100% code coverage means all lines were executed, but does not guarantee tests verify correct behavior. Tests without meaningful assertions, missing edge cases, and logical gaps can exist at 100% coverage. Mutation testing is a better quality indicator.',
    tags: ['coverage', 'quality', 'best-practices'],
  },
  {
    id: 'java-testing-l4-007',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'What is the inline mock maker and how does it differ from the subclass mock maker?',
    options: ['Inline mock maker instruments classes directly via Java agent; subclass maker creates proxy subclasses', 'Inline is for interfaces; subclass is for classes', 'Inline is faster; subclass supports more types', 'They are identical'],
    correctAnswer: 0,
    explanation: 'The inline mock maker uses a Java agent to instrument classes directly in memory, allowing mocking of final classes and methods. The subclass mock maker creates runtime subclasses via ByteBuddy, which cannot override final methods.',
    tags: ['mockito', 'inline-mock-maker', 'internals'],
  },
  {
    id: 'java-testing-l4-008',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you implement contract testing between microservices?',
    options: ['Share DTOs between services', 'End-to-end tests only', 'Use Spring Cloud Contract or Pact to define and verify API contracts', 'Manual API testing'],
    correctAnswer: 2,
    explanation: 'Contract testing tools like Spring Cloud Contract (producer-driven) and Pact (consumer-driven) generate tests from API contracts. The producer verifies it meets the contract; the consumer verifies it can handle the responses.',
    codeSnippet: '// Spring Cloud Contract DSL (Groovy)\nContract.make {\n    description "should return user"\n    request {\n        method GET()\n        url "/users/1"\n    }\n    response {\n        status OK()\n        body([\n            id: 1,\n            name: "John"\n        ])\n        headers {\n            contentType(applicationJson())\n        }\n    }\n}',
    tags: ['contract-testing', 'spring-cloud-contract', 'pact'],
  },
  {
    id: 'java-testing-l4-009',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you optimize Testcontainers startup time in CI/CD?',
    options: ['Only use H2 database in CI', 'Use container reuse, parallel starts, singleton pattern, and pre-pulled images', 'Disable Testcontainers in CI', 'Run fewer tests in CI'],
    correctAnswer: 1,
    explanation: 'Optimize by: reusing containers (.withReuse(true)), using singleton pattern for shared containers, pulling images in CI setup step, running containers in parallel, and using lighter image variants (alpine).',
    codeSnippet: '// Singleton pattern for fast reuse\npublic abstract class AbstractIT {\n    static final PostgreSQLContainer<?> PG;\n    static final KafkaContainer KAFKA;\n\n    static {\n        PG = new PostgreSQLContainer<>(\n            "postgres:15-alpine")\n            .withReuse(true);\n        KAFKA = new KafkaContainer(\n            DockerImageName.parse(\n                "confluentinc/cp-kafka:7.5.0"))\n            .withReuse(true);\n        // Parallel start\n        Stream.of(PG, KAFKA)\n            .parallel()\n            .forEach(GenericContainer::start);\n    }\n}',
    tags: ['testcontainers', 'optimization', 'ci-cd'],
  },
  {
    id: 'java-testing-l4-010',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TDD',
    question: 'What is the "Transformation Priority Premise" in TDD?',
    options: ['A rule for mock creation', 'A rule for ordering test priorities', 'A guideline for making code transformations from simple to complex to avoid over-engineering', 'A test execution order strategy'],
    correctAnswer: 2,
    explanation: 'Robert Martin\'s Transformation Priority Premise suggests an ordered list of code transformations (from simple like constant->variable to complex like statement->recursion). Choosing simpler transformations first leads to better-designed code in TDD.',
    tags: ['tdd', 'transformation-priority', 'design'],
  },
  {
    id: 'java-testing-l4-011',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you create a custom Mockito extension?',
    options: ['Create a custom Answer implementation', 'Implement AfterEachCallback and BeforeEachCallback', 'Implement MockitoExtension interface', 'Extend MockitoJUnitRunner'],
    correctAnswer: 1,
    explanation: 'Custom JUnit 5 extensions can integrate with Mockito by implementing lifecycle callbacks. You can create extensions that set up mocks, validate interactions, or inject test doubles.',
    codeSnippet: 'public class AutoMockExtension\n    implements BeforeEachCallback,\n               AfterEachCallback {\n\n    private AutoCloseable closeable;\n\n    @Override\n    public void beforeEach(\n            ExtensionContext ctx) {\n        Object testInstance =\n            ctx.getRequiredTestInstance();\n        closeable = MockitoAnnotations\n            .openMocks(testInstance);\n    }\n\n    @Override\n    public void afterEach(\n            ExtensionContext ctx)\n            throws Exception {\n        closeable.close();\n    }\n}',
    tags: ['mockito', 'junit5', 'extensions'],
  },
  {
    id: 'java-testing-l4-012',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test event-driven architectures?',
    options: ['Only test synchronous parts', 'Mock all events', 'Use only unit tests', 'Combine Testcontainers for brokers, Awaitility for async verification, and ArgumentCaptor for event content'],
    correctAnswer: 3,
    explanation: 'Event-driven testing requires: real brokers (Testcontainers), async verification (Awaitility), event content validation (ArgumentCaptor or direct DB checks), and proper test ordering with eventual consistency.',
    codeSnippet: '@Testcontainers\n@SpringBootTest\nclass EventDrivenTest {\n    @Container\n    @ServiceConnection\n    static KafkaContainer kafka = ...;\n\n    @SpyBean EventHandler handler;\n\n    @Test\n    void shouldProcessOrderEvent() {\n        kafkaTemplate.send("orders",\n            new OrderEvent("ORD-1"));\n\n        await().atMost(Duration.ofSeconds(10))\n            .untilAsserted(() -> {\n                verify(handler)\n                    .handle(argThat(e ->\n                        e.getOrderId()\n                            .equals("ORD-1")));\n                assertThat(orderRepo\n                    .findByOrderId("ORD-1"))\n                    .isPresent();\n            });\n    }\n}',
    tags: ['event-driven', 'kafka', 'async-testing'],
  },
  {
    id: 'java-testing-l4-013',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'What is path coverage and how does it differ from branch coverage?',
    options: ['Path coverage covers all possible execution paths; branch coverage only covers each branch direction', 'They are the same', 'Branch coverage is more thorough', 'Path coverage only applies to loops'],
    correctAnswer: 0,
    explanation: 'Path coverage requires testing every possible unique path through code (all combinations of branches). Branch coverage only requires each branch direction (true/false) to be taken at least once. Path coverage is exponentially harder to achieve.',
    tags: ['coverage', 'path-coverage', 'branch-coverage'],
  },
  {
    id: 'java-testing-l4-014',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you handle thread-safety issues when using Mockito with concurrent tests?',
    options: ['Synchronize all mock access', 'Use volatile mocks', 'Use separate mock instances per thread and avoid shared mutable state', 'Mockito is always thread-safe'],
    correctAnswer: 2,
    explanation: 'Mockito mocks are not thread-safe by default. In concurrent tests, use separate mock instances per thread, or use thread-local patterns. Shared mocks can cause intermittent failures.',
    codeSnippet: '// Thread-safe approach: create mocks\n// in each thread\n@Test\nvoid testConcurrent() throws Exception {\n    ExecutorService exec =\n        Executors.newFixedThreadPool(4);\n\n    List<Future<?>> futures =\n        IntStream.range(0, 4)\n            .mapToObj(i -> exec.submit(() -> {\n                // Each thread gets own mock\n                var repo = mock(\n                    UserRepository.class);\n                when(repo.findById(anyLong()))\n                    .thenReturn(Optional.of(\n                        new User("User" + i)));\n                var service =\n                    new UserService(repo);\n                service.process();\n            }))\n            .collect(Collectors.toList());\n\n    futures.forEach(f -> {\n        try { f.get(); }\n        catch (Exception e) { fail(e); }\n    });\n}',
    tags: ['mockito', 'concurrency', 'thread-safety'],
  },
  {
    id: 'java-testing-l4-015',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you implement test data management for integration tests at scale?',
    options: ['Combine test data builders, database seeding scripts, and cleanup strategies', 'Share data between all tests', 'Use random data always', 'Use production database copies'],
    correctAnswer: 0,
    explanation: 'At scale, use: test data builders (builder pattern for entities), @Sql scripts for database seeding, @Transactional for automatic rollback, or database-per-test with Testcontainers for isolation.',
    codeSnippet: '// Test data builder pattern\npublic class UserBuilder {\n    private String name = "Default";\n    private int age = 25;\n    private Role role = Role.USER;\n\n    public static UserBuilder aUser() {\n        return new UserBuilder();\n    }\n\n    public UserBuilder withName(String n) {\n        this.name = n; return this;\n    }\n    public UserBuilder withRole(Role r) {\n        this.role = r; return this;\n    }\n    public UserBuilder asAdmin() {\n        return withRole(Role.ADMIN);\n    }\n    public User build() {\n        return new User(name, age, role);\n    }\n    public User buildAndPersist(\n            EntityManager em) {\n        User u = build();\n        em.persist(u);\n        return u;\n    }\n}',
    tags: ['test-data', 'builders', 'patterns'],
  },
  {
    id: 'java-testing-l4-016',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'MockMvc',
    question: 'How do you test OAuth2/JWT secured endpoints with MockMvc?',
    options: ['Disable security', 'Use basic auth instead', 'Use spring-security-test with jwt() RequestPostProcessor or @WithMockUser', 'Generate real JWT tokens'],
    correctAnswer: 2,
    explanation: 'Spring Security Test provides jwt() RequestPostProcessor for configuring JWT-based authentication in MockMvc tests without real token generation.',
    codeSnippet: '@WebMvcTest(ApiController.class)\nclass SecuredApiTest {\n    @Autowired MockMvc mockMvc;\n\n    @Test\n    void shouldAccessWithJwt() throws Exception {\n        mockMvc.perform(\n            get("/api/data")\n                .with(jwt()\n                    .jwt(j -> j\n                        .subject("user-123")\n                        .claim("scope", "read"))\n                    .authorities(\n                        new SimpleGrantedAuthority(\n                            "SCOPE_read"))))\n            .andExpect(status().isOk());\n    }\n\n    @Test\n    void shouldRejectWithoutScope()\n            throws Exception {\n        mockMvc.perform(\n            get("/api/admin")\n                .with(jwt()\n                    .jwt(j -> j.subject("user"))))\n            .andExpect(status().isForbidden());\n    }\n}',
    tags: ['mockmvc', 'security', 'jwt', 'oauth2'],
  },
  {
    id: 'java-testing-l4-017',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'What are the common mutation operators in PIT?',
    options: ['Only arithmetic mutations', 'Conditional boundary, negate conditional, math mutator, void method call, return values, increments', 'Only exception mutations', 'Only string mutations'],
    correctAnswer: 1,
    explanation: 'PIT includes mutators like: CONDITIONALS_BOUNDARY (< to <=), NEGATE_CONDITIONALS (== to !=), MATH (+ to -), VOID_METHOD_CALLS (remove calls), RETURN_VALS (change returns), INCREMENTS (++ to --), and more.',
    tags: ['mutation-testing', 'pitest', 'operators'],
  },
  {
    id: 'java-testing-l4-018',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test with multiple interconnected containers that depend on each other?',
    options: ['Use Docker Compose with Testcontainers and define startup dependencies', 'Use a single container for everything', 'Test each container independently', 'Start them sequentially'],
    correctAnswer: 0,
    explanation: 'Use Docker Compose support or manually create a Network, define containers with withNetworkAliases(), and use proper WaitStrategy to handle startup ordering and dependencies.',
    codeSnippet: 'static Network network =\n    Network.newNetwork();\n\n@Container\nstatic PostgreSQLContainer<?> db =\n    new PostgreSQLContainer<>("postgres:15")\n        .withNetwork(network)\n        .withNetworkAliases("db");\n\n@Container\nstatic GenericContainer<?> cache =\n    new GenericContainer<>("redis:7")\n        .withNetwork(network)\n        .withNetworkAliases("cache");\n\n@Container\nstatic GenericContainer<?> app =\n    new GenericContainer<>("my-app")\n        .withNetwork(network)\n        .withEnv("DB_URL",\n            "jdbc:postgresql://db:5432/test")\n        .withEnv("REDIS_HOST", "cache")\n        .dependsOn(db, cache)\n        .waitingFor(\n            Wait.forHttp("/health"));',
    tags: ['testcontainers', 'multi-container', 'networking'],
  },
  {
    id: 'java-testing-l4-019',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TDD',
    question: 'What is the "London School" vs "Chicago School" of TDD?',
    options: ['Different programming languages', 'London is newer; Chicago is legacy', 'London is for web; Chicago is for desktop', 'London uses mocks heavily (outside-in); Chicago prefers real objects (inside-out)'],
    correctAnswer: 3,
    explanation: 'London School (mockist TDD) drives design outside-in using mocks for all dependencies. Chicago School (classicist TDD) prefers real objects and only mocks external dependencies. Each has trade-offs in coupling, design feedback, and refactoring ease.',
    tags: ['tdd', 'london-school', 'chicago-school'],
  },
  {
    id: 'java-testing-l4-020',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'What are the implications of using deep stubs in production code testing?',
    options: ['Deep stubs are required for complex objects', 'No implications', 'Deep stubs improve test quality', 'Deep stubs may hide Law of Demeter violations and lead to brittle tests'],
    correctAnswer: 3,
    explanation: 'RETURNS_DEEP_STUBS can mask design problems like Law of Demeter violations (train wreck pattern: a.getB().getC().getValue()). Tests become tightly coupled to the internal structure, breaking easily on refactoring.',
    tags: ['mockito', 'deep-stubs', 'code-smells'],
  },
  {
    id: 'java-testing-l4-021',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test distributed transactions across microservices?',
    options: ['Use mocks for all services', 'Distributed transactions cannot be tested', 'Combine Testcontainers for all services, test saga/choreography patterns, verify compensating transactions', 'Use a single database'],
    correctAnswer: 2,
    explanation: 'Test distributed transactions by running all required services in Testcontainers, triggering the flow, and verifying both success paths and compensating transactions on failure. Test saga orchestration and choreography patterns explicitly.',
    tags: ['distributed-transactions', 'saga', 'microservices'],
  },
  {
    id: 'java-testing-l4-022',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'AssertJ',
    question: 'How do you implement recursive comparison with custom comparators in AssertJ?',
    options: ['Use usingRecursiveComparison().withComparatorForType()', 'Override equals() on all objects', 'Use custom assertion classes only', 'Implement Comparable interface'],
    correctAnswer: 0,
    explanation: 'AssertJ recursive comparison supports custom comparators per type or per field name, allowing precise control over how specific types are compared during deep comparison.',
    codeSnippet: 'assertThat(actual)\n    .usingRecursiveComparison()\n    .withComparatorForType(\n        (a, b) -> {\n            // Compare BigDecimal by value\n            return a.compareTo(b);\n        },\n        BigDecimal.class)\n    .withComparatorForFields(\n        (a, b) -> 0, // ignore\n        "createdAt", "updatedAt")\n    .withEqualsForType(\n        (a, b) -> a.getId()\n            .equals(b.getId()),\n        Entity.class)\n    .isEqualTo(expected);',
    tags: ['assertj', 'recursive-comparison', 'comparators'],
  },
  {
    id: 'java-testing-l4-023',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'How do you configure PIT mutation testing in a Maven project?',
    options: ['Add pitest-maven plugin and configure mutators, target classes, and target tests', 'PIT does not support Maven', 'Install a separate tool', 'Use JaCoCo with mutation flag'],
    correctAnswer: 0,
    explanation: 'PIT integrates via pitest-maven plugin. Configure target classes to mutate, target tests to run, mutation operators, thresholds, and output formats. Run with mvn pitest:mutationCoverage.',
    codeSnippet: '<plugin>\n  <groupId>org.pitest</groupId>\n  <artifactId>pitest-maven</artifactId>\n  <version>1.15.0</version>\n  <configuration>\n    <targetClasses>\n      <param>com.example.service.*</param>\n    </targetClasses>\n    <targetTests>\n      <param>com.example.service.*Test</param>\n    </targetTests>\n    <mutators>\n      <mutator>STRONGER</mutator>\n    </mutators>\n    <mutationThreshold>80</mutationThreshold>\n    <outputFormats>\n      <format>HTML</format>\n    </outputFormats>\n  </configuration>\n  <dependencies>\n    <dependency>\n      <groupId>org.pitest</groupId>\n      <artifactId>pitest-junit5-plugin</artifactId>\n      <version>1.2.1</version>\n    </dependency>\n  </dependencies>\n</plugin>',
    tags: ['mutation-testing', 'pitest', 'maven'],
  },
  {
    id: 'java-testing-l4-024',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you test code that uses Mockito.mockConstruction with parameterized constructors?',
    options: ['Only no-arg constructors can be mocked', 'Use the withSettings() and provide an initialization callback', 'Pass parameters to mockConstruction', 'Use PowerMock instead'],
    correctAnswer: 1,
    explanation: 'mockConstruction() accepts a MockInitializer callback that configures each mock when its constructor is called. You can access constructor arguments through the Context parameter.',
    codeSnippet: '@Test\nvoid testParameterizedConstructor() {\n    try (MockedConstruction<HttpClient> mock =\n        mockConstruction(HttpClient.class,\n            (mockObj, context) -> {\n                // context.arguments() has\n                // constructor args\n                String url = (String)\n                    context.arguments().get(0);\n                when(mockObj.getBaseUrl())\n                    .thenReturn(url);\n            })) {\n\n        HttpClient client =\n            new HttpClient("http://api.com");\n        assertEquals("http://api.com",\n            client.getBaseUrl());\n        assertEquals(1,\n            mock.constructed().size());\n    }\n}',
    tags: ['mockito', 'mockConstruction', 'constructors'],
  },
  {
    id: 'java-testing-l4-025',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test circuit breaker patterns (e.g., Resilience4j)?',
    options: ['Only test the happy path', 'Circuit breakers do not need testing', 'Use mocks to simulate the circuit breaker', 'Configure short thresholds, simulate failures, and verify state transitions and fallback behavior'],
    correctAnswer: 3,
    explanation: 'Test circuit breakers by configuring small failure thresholds, using mock/stub external services to simulate failures, and verifying the circuit opens, uses fallback, and eventually closes after the wait period.',
    codeSnippet: '@SpringBootTest\nclass CircuitBreakerTest {\n    @MockBean ExternalService external;\n    @Autowired ResilientService service;\n\n    @Test\n    void shouldOpenCircuitOnFailures() {\n        when(external.call())\n            .thenThrow(\n                new RuntimeException("Down"));\n\n        // Trigger failures to open circuit\n        for (int i = 0; i < 5; i++) {\n            assertThrows(\n                RuntimeException.class,\n                () -> service.callExternal());\n        }\n\n        // Verify fallback is used\n        String result =\n            service.callExternal();\n        assertEquals("fallback", result);\n\n        // Verify external not called\n        // (circuit is open)\n        verify(external, times(5)).call();\n    }\n}',
    tags: ['circuit-breaker', 'resilience4j', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-026',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test database connection pooling behavior with Testcontainers?',
    options: ['Use a single connection', 'Connection pooling does not need testing', 'Configure pool settings, simulate load, verify pool metrics, and test exhaustion scenarios', 'Mock the connection pool'],
    correctAnswer: 2,
    explanation: 'Test connection pooling by configuring HikariCP settings, running concurrent queries against Testcontainers database, monitoring pool metrics (active, idle, waiting), and testing pool exhaustion and recovery.',
    codeSnippet: '@SpringBootTest\n@Testcontainers\n@TestPropertySource(properties = {\n    "spring.datasource.hikari.maximum-pool-size=5",\n    "spring.datasource.hikari.connection-timeout=1000"\n})\nclass ConnectionPoolTest {\n    @Container @ServiceConnection\n    static PostgreSQLContainer<?> pg = ...;\n\n    @Autowired HikariDataSource dataSource;\n\n    @Test\n    void shouldExhaustPool() {\n        List<Connection> conns = new ArrayList<>();\n        for (int i = 0; i < 5; i++) {\n            conns.add(\n                dataSource.getConnection());\n        }\n        // Pool is exhausted\n        assertThrows(\n            SQLTransientConnectionException.class,\n            () -> dataSource.getConnection());\n        conns.forEach(c -> {\n            try { c.close(); }\n            catch (Exception e) { }\n        });\n    }\n}',
    tags: ['testcontainers', 'connection-pool', 'hikari'],
  },
  {
    id: 'java-testing-l4-027',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you implement a custom Answer in Mockito for complex stubbing?',
    options: ['Implement the Answer<T> interface', 'Create a custom mock class', 'Use lambda with thenReturn', 'Extend AbstractAnswer'],
    correctAnswer: 0,
    explanation: 'Implement Answer<T> for complex stubbing logic that depends on invocation context (method name, arguments, call count). Answers provide full access to the InvocationOnMock.',
    codeSnippet: 'public class SequentialAnswer<T>\n    implements Answer<T> {\n    private final Queue<T> values;\n    private final T defaultValue;\n\n    public SequentialAnswer(\n            T defaultValue, T... values) {\n        this.values =\n            new LinkedList<>(Arrays.asList(values));\n        this.defaultValue = defaultValue;\n    }\n\n    @Override\n    public T answer(\n            InvocationOnMock invocation) {\n        if (values.isEmpty()) {\n            return defaultValue;\n        }\n        return values.poll();\n    }\n}\n\n// Usage:\nwhen(mock.getData()).thenAnswer(\n    new SequentialAnswer<>("default",\n        "first", "second"));',
    tags: ['mockito', 'Answer', 'custom-stubbing'],
  },
  {
    id: 'java-testing-l4-028',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test idempotency of API endpoints?',
    options: ['Idempotency does not need testing', 'Send the same request multiple times and verify the result is identical each time', 'Call the endpoint once', 'Use random request IDs'],
    correctAnswer: 1,
    explanation: 'Test idempotency by sending identical requests (same body, same idempotency key) multiple times and verifying: same response, same state in database, no duplicate side effects (emails, events).',
    codeSnippet: '@Test\nvoid shouldBeIdempotent() throws Exception {\n    String idempotencyKey = UUID.randomUUID()\n        .toString();\n    String body = """  \n        {"amount": 100, "to": "user-2"}\n        """;\n\n    // First call - creates payment\n    mockMvc.perform(post("/payments")\n        .header("Idempotency-Key",\n            idempotencyKey)\n        .contentType(APPLICATION_JSON)\n        .content(body))\n        .andExpect(status().isCreated());\n\n    // Second call - same result\n    mockMvc.perform(post("/payments")\n        .header("Idempotency-Key",\n            idempotencyKey)\n        .contentType(APPLICATION_JSON)\n        .content(body))\n        .andExpect(status().isCreated());\n\n    // Only one payment created\n    assertEquals(1,\n        paymentRepo.count());\n}',
    tags: ['idempotency', 'api-testing', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-029',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'How does JaCoCo handle coverage in multi-module Maven projects?',
    options: ['It only works with single modules', 'Each module has separate coverage only', 'Use a different tool for multi-module', 'Use jacoco:report-aggregate goal to merge coverage from all modules'],
    correctAnswer: 3,
    explanation: 'JaCoCo provides jacoco:report-aggregate goal that collects and merges coverage data from all modules into a single report. Create a dedicated reporting module that depends on all other modules.',
    codeSnippet: '<!-- In reporting module pom.xml -->\n<plugin>\n    <groupId>org.jacoco</groupId>\n    <artifactId>jacoco-maven-plugin</artifactId>\n    <executions>\n        <execution>\n            <id>report-aggregate</id>\n            <phase>verify</phase>\n            <goals>\n                <goal>report-aggregate</goal>\n            </goals>\n        </execution>\n    </executions>\n</plugin>\n\n<!-- Depend on all modules -->\n<dependencies>\n    <dependency>\n        <groupId>${project.groupId}</groupId>\n        <artifactId>module-a</artifactId>\n    </dependency>\n    <dependency>\n        <groupId>${project.groupId}</groupId>\n        <artifactId>module-b</artifactId>\n    </dependency>\n</dependencies>',
    tags: ['jacoco', 'multi-module', 'maven'],
  },
  {
    id: 'java-testing-l4-030',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test database schema compatibility during rolling deployments?',
    options: ['Run both old and new application versions against the migrated schema in Testcontainers', 'Test schema changes manually', 'Use feature flags only', 'Do not test schema changes'],
    correctAnswer: 0,
    explanation: 'Test backward-compatible migrations by: running migrations in Testcontainers, testing both old and new application code against the new schema. This verifies expand-contract migration patterns work.',
    tags: ['testcontainers', 'schema-migration', 'rolling-deployment'],
  },
  {
    id: 'java-testing-l4-031',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you verify that no static methods were called on a class?',
    options: ['Use verifyNoStaticInteractions()', 'Open a MockedStatic scope and use verifyNoInteractions()', 'Static calls cannot be verified', 'Use verifyStatic(Class, never())'],
    correctAnswer: 1,
    explanation: 'Within a try-with-resources MockedStatic block, use mockedStatic.verifyNoInteractions() or verify with never() to assert no static methods were called.',
    codeSnippet: '@Test\nvoid shouldNotCallStatic() {\n    try (MockedStatic<AuditLogger> mock =\n            mockStatic(AuditLogger.class)) {\n\n        service.processWithoutAudit(data);\n\n        mock.verifyNoInteractions();\n        // Or verify specific:\n        mock.verify(\n            () -> AuditLogger.log(any()),\n            never());\n    }\n}',
    tags: ['mockito', 'mockStatic', 'verify'],
  },
  {
    id: 'java-testing-l4-032',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you implement chaos testing in Java applications?',
    options: ['Use libraries like Chaos Monkey for Spring Boot to inject failures, latency, and exceptions', 'Only test in production', 'Use random test data', 'Randomly delete code'],
    correctAnswer: 0,
    explanation: 'Chaos Monkey for Spring Boot injects failures (exceptions, latency, process termination) into Spring components. Combined with integration tests, it verifies system resilience.',
    codeSnippet: '# application-chaos.properties\nchaos.monkey.enabled=true\nchaos.monkey.watcher.service=true\nchaos.monkey.assaults.level=5\nchaos.monkey.assaults.\n    latencyActive=true\nchaos.monkey.assaults.\n    latencyRangeStart=1000\nchaos.monkey.assaults.\n    latencyRangeEnd=3000\nchaos.monkey.assaults.\n    exceptionsActive=true',
    tags: ['chaos-testing', 'chaos-monkey', 'resilience'],
  },
  {
    id: 'java-testing-l4-033',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'AssertJ',
    question: 'How do you generate custom AssertJ assertions for your domain classes?',
    options: ['Write them all manually', 'Use annotation processing', 'Import them from a library', 'Use the AssertJ assertions generator Maven/Gradle plugin'],
    correctAnswer: 3,
    explanation: 'The assertj-assertions-generator plugin scans domain classes and generates custom assertion classes with type-safe assertion methods for each property. This automates creating custom assertions.',
    codeSnippet: '<!-- Maven plugin -->\n<plugin>\n    <groupId>org.assertj</groupId>\n    <artifactId>\n        assertj-assertions-generator-\n        maven-plugin</artifactId>\n    <version>2.2.0</version>\n    <configuration>\n        <classes>\n            <param>\n                com.example.domain.User\n            </param>\n            <param>\n                com.example.domain.Order\n            </param>\n        </classes>\n    </configuration>\n</plugin>\n\n// Generated assertion usage:\nassertThat(user)\n    .hasName("John")  // generated\n    .hasAge(30)        // generated\n    .hasRole(ADMIN);   // generated',
    tags: ['assertj', 'code-generation', 'custom-assertions'],
  },
  {
    id: 'java-testing-l4-034',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'What is the difference between weak and strong mutation testing?',
    options: ['Weak checks state immediately after mutation point; strong checks final output', 'Weak is faster; strong is slower', 'They produce identical results', 'Weak uses fewer mutants'],
    correctAnswer: 0,
    explanation: 'Weak mutation (or first-order mutation) checks if the internal state differs immediately after the mutated statement. Strong mutation requires the difference to propagate to observable output. Strong is more thorough but harder to achieve.',
    tags: ['mutation-testing', 'weak-mutation', 'strong-mutation'],
  },
  {
    id: 'java-testing-l4-035',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test with TLS/SSL certificates in Testcontainers?',
    options: ['Use self-signed certificates only', 'Disable TLS in tests', 'TLS cannot be tested with Testcontainers', 'Mount certificates into containers and configure clients to trust them'],
    correctAnswer: 3,
    explanation: 'Mount TLS certificates into containers using withCopyFileToContainer(). Configure the application to trust test certificates via custom TrustStore or disabling verification for test environments.',
    codeSnippet: 'GenericContainer<?> server =\n    new GenericContainer<>("nginx:alpine")\n        .withCopyFileToContainer(\n            MountableFile.forClasspathResource(\n                "certs/server.crt"),\n            "/etc/ssl/certs/server.crt")\n        .withCopyFileToContainer(\n            MountableFile.forClasspathResource(\n                "certs/server.key"),\n            "/etc/ssl/private/server.key")\n        .withCopyFileToContainer(\n            MountableFile.forClasspathResource(\n                "nginx-ssl.conf"),\n            "/etc/nginx/conf.d/default.conf")\n        .withExposedPorts(443)\n        .waitingFor(\n            Wait.forHttp("/health")\n                .usingTls());',
    tags: ['testcontainers', 'tls', 'ssl', 'certificates'],
  },
  {
    id: 'java-testing-l4-036',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'What are the risks of over-mocking in tests?',
    options: ['Over-mocking makes tests faster', 'Tests become tightly coupled to implementation, fragile to refactoring, and may pass while production fails', 'Over-mocking is always better than under-mocking', 'No risks'],
    correctAnswer: 1,
    explanation: 'Over-mocking leads to: tests coupled to implementation details (break on refactoring), false confidence (mocks hide real integration issues), reduced readability, and tests that verify mocking setup rather than business logic.',
    tags: ['mockito', 'over-mocking', 'best-practices'],
  },
  {
    id: 'java-testing-l4-037',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test rate limiting in APIs?',
    options: ['Test only in production', 'Do not test rate limiting', 'Use mocks for rate limiter', 'Send requests exceeding the limit and verify 429 responses and headers'],
    correctAnswer: 3,
    explanation: 'Test rate limiting by configuring low limits in test config, sending requests exceeding the limit, and verifying 429 Too Many Requests responses with Retry-After headers.',
    codeSnippet: '@SpringBootTest(\n    webEnvironment = RANDOM_PORT)\n@TestPropertySource(properties = {\n    "rate.limit.requests=3",\n    "rate.limit.period=60"\n})\nclass RateLimitTest {\n    @Autowired\n    TestRestTemplate rest;\n\n    @Test\n    void shouldEnforceRateLimit() {\n        // 3 requests should succeed\n        for (int i = 0; i < 3; i++) {\n            ResponseEntity<String> resp =\n                rest.getForEntity(\n                    "/api/data", String.class);\n            assertEquals(200,\n                resp.getStatusCode().value());\n        }\n\n        // 4th should be rate limited\n        ResponseEntity<String> resp =\n            rest.getForEntity(\n                "/api/data", String.class);\n        assertEquals(429,\n            resp.getStatusCode().value());\n        assertNotNull(\n            resp.getHeaders()\n                .get("Retry-After"));\n    }\n}',
    tags: ['rate-limiting', 'api-testing', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-038',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'What is condition/decision coverage (MC/DC)?',
    options: ['Covering all method calls', 'Same as line coverage', 'Each condition in a decision independently affects the decision outcome', 'Testing all data conditions'],
    correctAnswer: 2,
    explanation: 'Modified Condition/Decision Coverage (MC/DC) requires that each condition in a boolean expression independently affects the overall decision. It is used in safety-critical software (DO-178C). It is stricter than branch coverage.',
    tags: ['coverage', 'mc-dc', 'safety-critical'],
  },
  {
    id: 'java-testing-l4-039',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you implement verification in custom order with Mockito?',
    options: ['Use @Order annotation', 'Use custom Verification class', 'Use InOrder with after() for timed ordering', 'Order verification is limited to strict sequence'],
    correctAnswer: 2,
    explanation: 'InOrder supports flexible ordering verification. Combined with after() for async scenarios, you can verify complex interaction patterns including partial ordering.',
    codeSnippet: 'InOrder order = inOrder(\n    auditLog, repo, notifier);\n\n// Strict order for first two\norder.verify(auditLog).log("started");\norder.verify(repo).save(any());\n\n// Must happen after save, with timeout\norder.verify(notifier,\n    after(500).times(1))\n    .notify(any());\n\norder.verify(auditLog).log("completed");\n\n// Verify nothing else happened\norder.verifyNoMoreInteractions();',
    tags: ['mockito', 'InOrder', 'complex-verification'],
  },
  {
    id: 'java-testing-l4-040',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test email sending functionality?',
    options: ['Skip email testing', 'Mock the mail sender only', 'Use GreenMail or MailHog in Testcontainers as a fake SMTP server', 'Send real emails'],
    correctAnswer: 2,
    explanation: 'Use GreenMail (embedded) or MailHog/Mailpit (Testcontainers) as fake SMTP servers. They capture sent emails for assertion without actually delivering them.',
    codeSnippet: '@Testcontainers\n@SpringBootTest\nclass EmailTest {\n    @Container\n    static GenericContainer<?> mailhog =\n        new GenericContainer<>(\n            "mailhog/mailhog:latest")\n            .withExposedPorts(1025, 8025);\n\n    @DynamicPropertySource\n    static void props(\n            DynamicPropertyRegistry r) {\n        r.add("spring.mail.host",\n            mailhog::getHost);\n        r.add("spring.mail.port",\n            () -> mailhog.getMappedPort(1025));\n    }\n\n    @Test\n    void shouldSendWelcomeEmail() {\n        userService.register(newUser);\n\n        // Check via MailHog API\n        String apiUrl = String.format(\n            "http://%s:%d/api/v2/messages",\n            mailhog.getHost(),\n            mailhog.getMappedPort(8025));\n        // Verify email was sent\n    }\n}',
    tags: ['email-testing', 'greenmail', 'mailhog'],
  },
  {
    id: 'java-testing-l4-041',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TDD',
    question: 'What is Property-Based Testing and how does it complement TDD?',
    options: ['Testing getter/setter methods', 'Generating random inputs to verify universal properties of code', 'Testing with property injection', 'Testing Java properties files'],
    correctAnswer: 1,
    explanation: 'Property-Based Testing (PBT) generates random inputs to verify that certain properties always hold (e.g., sorting is idempotent, encoding/decoding roundtrips). Libraries like jqwik integrate with JUnit 5.',
    codeSnippet: '// Using jqwik library\n@Property\nvoid sortIsIdempotent(\n        @ForAll List<Integer> list) {\n    List<Integer> sorted =\n        sort(list);\n    List<Integer> sortedAgain =\n        sort(sorted);\n    assertThat(sorted)\n        .isEqualTo(sortedAgain);\n}\n\n@Property\nvoid encodeDecode(\n        @ForAll @StringLength(max = 1000)\n        String original) {\n    String encoded = encode(original);\n    String decoded = decode(encoded);\n    assertThat(decoded)\n        .isEqualTo(original);\n}',
    tags: ['property-based-testing', 'jqwik', 'tdd'],
  },
  {
    id: 'java-testing-l4-042',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test with Localstack (AWS services) using Testcontainers?',
    options: ['Use real AWS services', 'Mock all AWS SDK calls', 'Use AWS test accounts only', 'Use LocalStackContainer to emulate AWS services locally'],
    correctAnswer: 3,
    explanation: 'Testcontainers provides a LocalStackContainer that emulates AWS services (S3, SQS, DynamoDB, etc.) locally. Configure AWS SDK clients to point to the LocalStack endpoint.',
    codeSnippet: '@Testcontainers\n@SpringBootTest\nclass S3Test {\n    @Container\n    static LocalStackContainer localstack =\n        new LocalStackContainer(\n            DockerImageName.parse(\n                "localstack/localstack:3.0"))\n            .withServices(\n                LocalStackContainer.Service.S3);\n\n    @DynamicPropertySource\n    static void props(\n            DynamicPropertyRegistry r) {\n        r.add("aws.s3.endpoint",\n            () -> localstack\n                .getEndpointOverride(S3)\n                .toString());\n        r.add("aws.s3.region",\n            localstack::getRegion);\n        r.add("aws.s3.access-key",\n            localstack::getAccessKey);\n        r.add("aws.s3.secret-key",\n            localstack::getSecretKey);\n    }\n\n    @Test\n    void shouldUploadToS3() {\n        // Test real S3 operations\n    }\n}',
    tags: ['testcontainers', 'localstack', 'aws'],
  },
  {
    id: 'java-testing-l4-043',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'What is the "test smell" of testing implementation details rather than behavior?',
    options: ['A performance issue', 'Using too many assertions', 'A naming convention issue', 'Tests that verify internal method calls instead of observable outcomes, breaking on refactoring'],
    correctAnswer: 3,
    explanation: 'Testing implementation details means verifying internal method calls, field values, or execution order rather than observable behavior. These tests break during refactoring even when behavior is preserved, providing no confidence in correctness.',
    codeSnippet: '// BAD: Tests implementation detail\n@Test\nvoid badTest() {\n    service.processOrder(order);\n    verify(repo).findById(order.getId());\n    verify(validator).validate(order);\n    verify(repo).save(order);\n    verify(notifier).send(any());\n    // Breaks if internal flow changes\n}\n\n// GOOD: Tests behavior/outcome\n@Test\nvoid goodTest() {\n    Order result =\n        service.processOrder(order);\n    assertThat(result.getStatus())\n        .isEqualTo(PROCESSED);\n    // Verify important side effect\n    assertThat(orderRepo.findById(\n        result.getId())).isPresent();\n}',
    tags: ['test-smells', 'behavior-testing', 'best-practices'],
  },
  {
    id: 'java-testing-l4-044',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test WebSocket endpoints?',
    options: ['Use WebSocketStompClient or a testing WebSocket client with a real server', 'WebSockets cannot be tested', 'Use REST calls to simulate WebSocket', 'Use MockMvc'],
    correctAnswer: 0,
    explanation: 'Test WebSocket endpoints by starting a real server (RANDOM_PORT) and using WebSocketStompClient to connect, subscribe, send messages, and verify received messages.',
    codeSnippet: '@SpringBootTest(\n    webEnvironment = RANDOM_PORT)\nclass WebSocketTest {\n    @LocalServerPort int port;\n\n    @Test\n    void shouldReceiveMessages()\n            throws Exception {\n        WebSocketStompClient client =\n            new WebSocketStompClient(\n                new StandardWebSocketClient());\n        client.setMessageConverter(\n            new MappingJackson2MessageConverter());\n\n        BlockingQueue<String> messages =\n            new LinkedBlockingQueue<>();\n\n        StompSession session = client.connect(\n            "ws://localhost:" + port + "/ws",\n            new StompSessionHandlerAdapter() {})\n            .get(5, TimeUnit.SECONDS);\n\n        session.subscribe(\n            "/topic/messages",\n            new StompFrameHandler() {\n                @Override\n                public Type getPayloadType(\n                    StompHeaders h) {\n                    return String.class;\n                }\n                @Override\n                public void handleFrame(\n                    StompHeaders h, Object p) {\n                    messages.add((String) p);\n                }\n            });\n\n        session.send(\n            "/app/chat", "Hello");\n        String received =\n            messages.poll(5, TimeUnit.SECONDS);\n        assertThat(received)\n            .contains("Hello");\n    }\n}',
    tags: ['websocket', 'stomp', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-045',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'How do you integrate mutation testing into a CI/CD pipeline?',
    options: ['Run manually only', 'Run it only in production', 'Mutation testing is too slow for CI', 'Add PIT as a CI step with mutation thresholds that fail the build'],
    correctAnswer: 3,
    explanation: 'Configure PIT with mutationThreshold and coverageThreshold in Maven plugin. Run as a CI step (mvn pitest:mutationCoverage). Use incremental analysis to speed up by only mutating changed code.',
    codeSnippet: '<!-- PIT with CI configuration -->\n<configuration>\n    <mutationThreshold>80</mutationThreshold>\n    <coverageThreshold>90</coverageThreshold>\n    <failWhenNoMutations>false</failWhenNoMutations>\n    <!-- Incremental analysis -->\n    <historyInputFile>\n        target/pit-history.bin\n    </historyInputFile>\n    <historyOutputFile>\n        target/pit-history.bin\n    </historyOutputFile>\n    <!-- Speed: only strong mutators -->\n    <mutators>\n        <mutator>STRONGER</mutator>\n    </mutators>\n    <timestampedReports>false</timestampedReports>\n</configuration>',
    tags: ['mutation-testing', 'pitest', 'ci-cd'],
  },
  {
    id: 'java-testing-l4-046',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you test code that uses the Service Locator pattern with Mockito?',
    options: ['Use mockStatic or dependency injection refactoring to inject mocks', 'Use reflection to set fields', 'Mock the locator class', 'Service Locator cannot be tested'],
    correctAnswer: 0,
    explanation: 'Service Locator pattern can be tested by: mocking the static locator method with mockStatic(), or better yet, refactoring to dependency injection. The need to mock statics often signals a design improvement opportunity.',
    codeSnippet: '// If refactoring is not possible:\n@Test\nvoid testServiceLocator() {\n    UserService mockService =\n        mock(UserService.class);\n\n    try (MockedStatic<ServiceLocator> mock =\n        mockStatic(ServiceLocator.class)) {\n\n        mock.when(() ->\n            ServiceLocator.get(\n                UserService.class))\n            .thenReturn(mockService);\n\n        when(mockService.findById(1L))\n            .thenReturn(new User("John"));\n\n        // Code under test uses\n        // ServiceLocator.get()\n        controller.handleRequest(1L);\n\n        verify(mockService).findById(1L);\n    }\n}',
    tags: ['mockito', 'service-locator', 'mockStatic'],
  },
  {
    id: 'java-testing-l4-047',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test caching behavior in Spring applications?',
    options: ['Caching does not need testing', 'Disable caching in tests', 'Use a separate caching test framework', 'Verify cache hits/misses by checking call counts on underlying services'],
    correctAnswer: 3,
    explanation: 'Test caching by calling a method multiple times and verifying the underlying service/repository is called only once (cache hit). Also test cache eviction, TTL, and conditional caching.',
    codeSnippet: '@SpringBootTest\nclass CachingTest {\n    @SpyBean\n    UserRepository userRepo;\n\n    @Autowired\n    UserService userService;\n\n    @Autowired\n    CacheManager cacheManager;\n\n    @BeforeEach\n    void clearCache() {\n        cacheManager.getCache("users")\n            .clear();\n    }\n\n    @Test\n    void shouldCacheUser() {\n        userService.findById(1L); // miss\n        userService.findById(1L); // hit\n        userService.findById(1L); // hit\n\n        // Only 1 actual DB call\n        verify(userRepo, times(1))\n            .findById(1L);\n    }\n\n    @Test\n    void shouldEvictOnUpdate() {\n        userService.findById(1L);\n        userService.update(1L, newData);\n        userService.findById(1L); // miss\n\n        verify(userRepo, times(2))\n            .findById(1L);\n    }\n}',
    tags: ['caching', 'spring-cache', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-048',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test with Testcontainers in environments without Docker (e.g., some CI)?',
    options: ['Use Docker-in-Docker always', 'Only use embedded alternatives', 'Use Testcontainers Cloud or configure remote Docker hosts', 'Skip all integration tests'],
    correctAnswer: 2,
    explanation: 'Testcontainers Cloud provides a cloud-based Docker runtime that works without local Docker. Alternatively, configure DOCKER_HOST to point to a remote Docker daemon. TC also supports Podman.',
    tags: ['testcontainers', 'testcontainers-cloud', 'ci-cd'],
  },
  {
    id: 'java-testing-l4-049',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'AssertJ',
    question: 'How do you implement condition-based assertions in AssertJ?',
    options: ['Use if-else in tests', 'Use Predicate<T> directly', 'Create Condition objects and use has(), is(), doesNotHave()', 'Use when() method'],
    correctAnswer: 2,
    explanation: 'AssertJ Condition wraps a predicate with a description for readable assertions. Use has(), is(), doesNotHave() to apply conditions.',
    codeSnippet: 'Condition<User> admin = new Condition<>(\n    u -> u.getRole() == Role.ADMIN,\n    "admin user");\n\nCondition<User> active = new Condition<>(\n    User::isActive, "active user");\n\nassertThat(user).is(admin);\nassertThat(user).has(active);\n\n// On collections\nassertThat(users)\n    .haveAtLeast(1, admin)\n    .haveExactly(3, active)\n    .doNotHave(new Condition<>(\n        u -> u.getAge() < 0,\n        "negative age"));',
    tags: ['assertj', 'conditions', 'custom-assertions'],
  },
  {
    id: 'java-testing-l4-050',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test transactional outbox pattern?',
    options: ['Mock the outbox', 'Verify that domain event and outbox entry are saved in the same transaction, then the relay publishes', 'Do not test the outbox pattern', 'Test inbox and outbox separately'],
    correctAnswer: 1,
    explanation: 'Test the outbox pattern by: verifying the domain operation and outbox entry are in the same transaction, testing the relay/poller publishes messages, and verifying idempotent message processing.',
    codeSnippet: '@Test\n@Transactional\nvoid shouldSaveOrderAndOutbox() {\n    orderService.createOrder(orderDTO);\n\n    // Both in same transaction\n    assertThat(orderRepo.findAll())\n        .hasSize(1);\n    assertThat(outboxRepo.findAll())\n        .hasSize(1)\n        .first()\n        .satisfies(entry -> {\n            assertThat(entry.getType())\n                .isEqualTo("ORDER_CREATED");\n            assertThat(entry.isPublished())\n                .isFalse();\n        });\n}\n\n@Test\nvoid shouldRelayOutboxMessages() {\n    orderService.createOrder(orderDTO);\n    outboxRelay.processOutbox();\n\n    // Verify message published to Kafka\n    await().atMost(Duration.ofSeconds(5))\n        .until(() -> outboxRepo.findAll()\n            .stream()\n            .allMatch(OutboxEntry::isPublished));\n}',
    tags: ['outbox-pattern', 'transactional', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-051',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you handle Mockito limitations with records and sealed classes in Java 17+?',
    options: ['Use the inline mock maker (default in Mockito 5+) or create test doubles manually', 'Use PowerMock', 'Records and sealed classes cannot be tested', 'Avoid records in testable code'],
    correctAnswer: 0,
    explanation: 'Java records are final and Mockito inline mock maker (default in Mockito 5) can handle them. For sealed classes, use permitted subtypes for test doubles or the inline mock maker. Manual test doubles (fakes) are also a clean option.',
    tags: ['mockito', 'records', 'sealed-classes', 'java17'],
  },
  {
    id: 'java-testing-l4-052',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you implement end-to-end API testing with real services?',
    options: ['Combine Testcontainers, TestRestTemplate/WebTestClient, and database verification', 'Use Selenium for API testing', 'End-to-end tests should only be manual', 'Use MockMvc only'],
    correctAnswer: 0,
    explanation: 'Full E2E API tests use real infrastructure (Testcontainers), real HTTP calls (TestRestTemplate/WebTestClient), and verify across all layers including database state and external service interactions.',
    codeSnippet: '@SpringBootTest(\n    webEnvironment = RANDOM_PORT)\n@Testcontainers\nclass E2EOrderTest {\n    @Container @ServiceConnection\n    static PostgreSQLContainer<?> pg = ...;\n    @Container @ServiceConnection\n    static KafkaContainer kafka = ...;\n\n    @Autowired TestRestTemplate rest;\n    @Autowired OrderRepository orderRepo;\n\n    @Test\n    void shouldCreateAndProcessOrder() {\n        // Create order via API\n        ResponseEntity<Order> response =\n            rest.postForEntity(\n                "/api/orders",\n                new CreateOrderDTO(...),\n                Order.class);\n        assertEquals(201,\n            response.getStatusCode().value());\n\n        Long orderId =\n            response.getBody().getId();\n\n        // Verify in database\n        await().atMost(Duration.ofSeconds(10))\n            .until(() -> orderRepo\n                .findById(orderId)\n                .map(Order::getStatus)\n                .orElse(null)\n                == OrderStatus.CONFIRMED);\n    }\n}',
    tags: ['e2e-testing', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-053',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'How do you enforce coverage thresholds in a build pipeline?',
    options: ['Review coverage reports manually', 'Use SonarQube only', 'Configure JaCoCo check goal with minimum thresholds that fail the build', 'Coverage thresholds should not be enforced'],
    correctAnswer: 2,
    explanation: 'JaCoCo check goal verifies coverage against configured thresholds and fails the build if not met. Configure at bundle, package, or class level with instruction, branch, line, and complexity counters.',
    codeSnippet: '<plugin>\n    <groupId>org.jacoco</groupId>\n    <artifactId>jacoco-maven-plugin</artifactId>\n    <executions>\n        <execution>\n            <id>check</id>\n            <goals><goal>check</goal></goals>\n            <configuration>\n                <rules>\n                    <rule>\n                        <element>BUNDLE</element>\n                        <limits>\n                            <limit>\n                                <counter>\n                                    LINE\n                                </counter>\n                                <value>\n                                    COVEREDRATIO\n                                </value>\n                                <minimum>\n                                    0.80\n                                </minimum>\n                            </limit>\n                            <limit>\n                                <counter>\n                                    BRANCH\n                                </counter>\n                                <value>\n                                    COVEREDRATIO\n                                </value>\n                                <minimum>\n                                    0.70\n                                </minimum>\n                            </limit>\n                        </limits>\n                    </rule>\n                </rules>\n            </configuration>\n        </execution>\n    </executions>\n</plugin>',
    tags: ['jacoco', 'coverage-thresholds', 'ci-cd'],
  },
  {
    id: 'java-testing-l4-054',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you use Mockito with Kotlin coroutines?',
    options: ['Use MockK instead (but Mockito works with mockito-kotlin)', 'Mockito does not support Kotlin', 'Convert coroutines to Java first', 'Use mockito-kotlin library with runTest and coEvery/coVerify'],
    correctAnswer: 3,
    explanation: 'mockito-kotlin provides Kotlin-friendly extensions. For coroutines, use kotlinx-coroutines-test runTest block and stub suspend functions with whenBlocking or use MockK which has native coroutine support.',
    codeSnippet: '// Using mockito-kotlin\n@Test\nfun `should fetch user`() = runTest {\n    val repo = mock<UserRepository>()\n    whenever(repo.findById(1L))\n        .thenReturn(User("John"))\n\n    val service = UserService(repo)\n    val result = service.getUser(1L)\n\n    assertEquals("John", result.name)\n    verify(repo).findById(1L)\n}',
    tags: ['mockito', 'kotlin', 'coroutines'],
  },
  {
    id: 'java-testing-l4-055',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test database optimistic locking?',
    options: ['Do not test locking', 'Mock the version field', 'Test locking with single-threaded tests', 'Simulate concurrent modifications and verify OptimisticLockException is thrown'],
    correctAnswer: 3,
    explanation: 'Test optimistic locking by reading an entity in two "users," modifying and saving with the first, then attempting to save with the stale second version. Verify OptimisticLockException.',
    codeSnippet: '@SpringBootTest\n@Testcontainers\nclass OptimisticLockTest {\n    @Container @ServiceConnection\n    static PostgreSQLContainer<?> pg = ...;\n\n    @Autowired UserRepository repo;\n    @Autowired EntityManager em;\n\n    @Test\n    void shouldDetectConcurrentModification() {\n        User user = repo.save(\n            new User("John"));\n\n        // Simulate first user reads\n        User user1 = repo.findById(\n            user.getId()).get();\n        // Simulate second user reads\n        User user2 = repo.findById(\n            user.getId()).get();\n\n        // First user saves\n        user1.setName("Jane");\n        repo.saveAndFlush(user1);\n\n        // Second user tries with stale version\n        user2.setName("Bob");\n        assertThrows(\n            OptimisticLockingFailureException\n                .class,\n            () -> repo.saveAndFlush(user2));\n    }\n}',
    tags: ['optimistic-locking', 'concurrency', 'jpa'],
  },
  {
    id: 'java-testing-l4-056',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you handle Testcontainers container logs for debugging failing tests?',
    options: ['Ignore container logs', 'Container logs are not accessible', 'Use container.getLogs() or withLogConsumer() for real-time log streaming', 'Check Docker logs manually'],
    correctAnswer: 2,
    explanation: 'Testcontainers provides getLogs() for retrieving logs and withLogConsumer() for streaming logs in real-time. Use Slf4jLogConsumer for integration with test logging frameworks.',
    codeSnippet: '// Real-time log streaming\n@Container\nstatic GenericContainer<?> app =\n    new GenericContainer<>("my-app:latest")\n        .withLogConsumer(\n            new Slf4jLogConsumer(\n                LoggerFactory.getLogger(\n                    "testcontainer")))\n        .withExposedPorts(8080);\n\n// Get logs in test\n@Test\nvoid debugFailure() {\n    try {\n        callService();\n    } catch (Exception e) {\n        String logs = app.getLogs();\n        System.err.println(\n            "Container logs:\\n" + logs);\n        throw e;\n    }\n}\n\n// Get specific log types\nString stdout = app.getLogs(\n    OutputFrame.OutputType.STDOUT);\nString stderr = app.getLogs(\n    OutputFrame.OutputType.STDERR);',
    tags: ['testcontainers', 'logging', 'debugging'],
  },
  {
    id: 'java-testing-l4-057',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'How do you handle false positives from survived mutants?',
    options: ['Ignore all survived mutants', 'Lower the mutation score threshold', 'Kill every mutant regardless of value', 'Analyze each: some are equivalent mutants, some indicate missing tests, some are in non-critical code'],
    correctAnswer: 3,
    explanation: 'Not all survived mutants indicate test weakness. Analyze: equivalent mutants (behavior unchanged), mutants in logging/toString (low value), mutants revealing actual test gaps. Focus on business-critical code mutants.',
    tags: ['mutation-testing', 'false-positives', 'analysis'],
  },
  {
    id: 'java-testing-l4-058',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'What is the impact of Mockito on GraalVM native image compilation?',
    options: ['Mockito relies on runtime bytecode generation which conflicts with GraalVM AOT compilation', 'No impact', 'GraalVM makes mocks faster', 'Mockito works perfectly with GraalVM'],
    correctAnswer: 0,
    explanation: 'Mockito generates proxy classes at runtime using ByteBuddy/Java agent, which conflicts with GraalVM native image AOT compilation. Tests using Mockito must run on the JVM, not as native images. Spring Native test support addresses some of these issues.',
    tags: ['mockito', 'graalvm', 'native-image'],
  },
  {
    id: 'java-testing-l4-059',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test pagination endpoints thoroughly?',
    options: ['Test first page, middle page, last page, empty results, invalid parameters, and sorting', 'Pagination does not need testing', 'Test only the first page', 'Use one test for all pages'],
    correctAnswer: 0,
    explanation: 'Thorough pagination testing covers: first/middle/last pages, page beyond data range, empty results, different page sizes, sorting combinations, total count accuracy, and link headers.',
    codeSnippet: '@Test\nvoid paginationTests() throws Exception {\n    // Setup: 25 users in DB\n\n    // First page\n    mockMvc.perform(get("/users")\n        .param("page", "0")\n        .param("size", "10"))\n        .andExpect(jsonPath("$.content",\n            hasSize(10)))\n        .andExpect(jsonPath("$.totalElements")\n            .value(25))\n        .andExpect(jsonPath("$.totalPages")\n            .value(3));\n\n    // Last page (partial)\n    mockMvc.perform(get("/users")\n        .param("page", "2")\n        .param("size", "10"))\n        .andExpect(jsonPath("$.content",\n            hasSize(5)));\n\n    // Beyond range\n    mockMvc.perform(get("/users")\n        .param("page", "99"))\n        .andExpect(jsonPath("$.content",\n            hasSize(0)));\n\n    // Sorted\n    mockMvc.perform(get("/users")\n        .param("sort", "name,desc"))\n        .andExpect(jsonPath(\n            "$.content[0].name")\n            .value("Zara"));\n}',
    tags: ['pagination', 'api-testing', 'thorough-testing'],
  },
  {
    id: 'java-testing-l4-060',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you implement container image build and test in the same test lifecycle?',
    options: ['Use pre-built images only', 'Build images separately', 'Build in CI, test locally', 'Use ImageFromDockerfile to build the image from a Dockerfile as part of the test'],
    correctAnswer: 3,
    explanation: 'ImageFromDockerfile builds a Docker image from a Dockerfile or programmatic definition as part of the test setup, ensuring the latest code is tested.',
    codeSnippet: '@Container\nstatic GenericContainer<?> app =\n    new GenericContainer<>(\n        new ImageFromDockerfile()\n            .withDockerfile(\n                Path.of("Dockerfile"))\n            .withBuildArg("VERSION", "1.0"))\n        .withExposedPorts(8080)\n        .waitingFor(\n            Wait.forHttp("/health")\n                .forStatusCode(200));\n\n// Or programmatic image:\n@Container\nstatic GenericContainer<?> custom =\n    new GenericContainer<>(\n        new ImageFromDockerfile()\n            .withDockerfileFromBuilder(b -> b\n                .from("openjdk:17-slim")\n                .copy("app.jar", "/app.jar")\n                .cmd("java", "-jar",\n                    "/app.jar")\n                .build())\n            .withFileFromPath(\n                "app.jar",\n                Path.of("target/app.jar")));',
    tags: ['testcontainers', 'ImageFromDockerfile', 'build'],
  },
  {
    id: 'java-testing-l4-061',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TDD',
    question: 'What is the FIRST principle for unit tests?',
    options: ['First test wins', 'A coding convention', 'Focus, Iterate, Review, Submit, Track', 'Fast, Isolated, Repeatable, Self-validating, Timely'],
    correctAnswer: 3,
    explanation: 'FIRST stands for: Fast (milliseconds), Isolated (no inter-test dependencies), Repeatable (same result every time), Self-validating (pass/fail without manual inspection), Timely (written before or alongside production code).',
    tags: ['tdd', 'first-principle', 'best-practices'],
  },
  {
    id: 'java-testing-l4-062',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test GraphQL APIs in Spring?',
    options: ['Use MockMvc with JSON body', 'Use REST testing tools', 'GraphQL cannot be tested in Spring', 'Use HttpGraphQlTester or GraphQlTester provided by Spring GraphQL test'],
    correctAnswer: 3,
    explanation: 'Spring GraphQL provides HttpGraphQlTester (for full integration) and GraphQlTester (for service-layer testing). They support query/mutation/subscription testing with fluent assertions.',
    codeSnippet: '@SpringBootTest(\n    webEnvironment = RANDOM_PORT)\n@AutoConfigureHttpGraphQlTester\nclass GraphQLTest {\n    @Autowired\n    HttpGraphQlTester tester;\n\n    @Test\n    void shouldQueryUser() {\n        tester.document("""\n            query {\n                user(id: 1) {\n                    name\n                    email\n                }\n            }\n            """)\n            .execute()\n            .path("user.name")\n            .entity(String.class)\n            .isEqualTo("John");\n    }\n\n    @Test\n    void shouldHandleErrors() {\n        tester.document("""\n            query { user(id: 999) { name } }\n            """)\n            .execute()\n            .errors()\n            .satisfy(errors ->\n                assertThat(errors).hasSize(1));\n    }\n}',
    tags: ['graphql', 'spring-graphql', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-063',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you handle testing legacy code with many static dependencies?',
    options: ['Rewrite everything first', 'Use mockStatic for seams, gradually introduce dependency injection, use the Sprout Method pattern', 'Legacy code cannot be tested', 'Use PowerMock for everything'],
    correctAnswer: 1,
    explanation: 'For legacy code: use mockStatic as a seam to inject test behavior, apply the Sprout Method pattern (create testable methods alongside legacy), gradually extract dependencies. Follow Michael Feathers\' "Working Effectively with Legacy Code" patterns.',
    tags: ['legacy-code', 'refactoring', 'testing-strategies'],
  },
  {
    id: 'java-testing-l4-064',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'What is the relationship between mutation testing and code coverage?',
    options: ['Mutation testing measures test effectiveness; coverage measures test breadth; they are complementary', 'Mutation testing replaces coverage', 'They measure the same thing', 'Coverage is always better than mutation testing'],
    correctAnswer: 0,
    explanation: 'Code coverage tells you which code is EXECUTED by tests. Mutation testing tells you if tests actually DETECT changes. High coverage with low mutation score means tests execute code without meaningful assertions. They are complementary metrics.',
    tags: ['mutation-testing', 'coverage', 'metrics'],
  },
  {
    id: 'java-testing-l4-065',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test Spring Security method-level security (@PreAuthorize, @Secured)?',
    options: ['Mock the security context', 'Disable security in tests', 'Use @WithMockUser with specific roles and verify access control decisions', 'Test security only at controller level'],
    correctAnswer: 2,
    explanation: 'Test method-level security by calling methods with different user contexts (@WithMockUser, @WithUserDetails) and verifying AccessDeniedException for unauthorized users.',
    codeSnippet: '@SpringBootTest\nclass SecurityTest {\n    @Autowired AdminService adminService;\n\n    @Test\n    @WithMockUser(roles = "ADMIN")\n    void adminCanDeleteUser() {\n        assertDoesNotThrow(\n            () -> adminService.deleteUser(1L));\n    }\n\n    @Test\n    @WithMockUser(roles = "USER")\n    void regularUserCannotDeleteUser() {\n        assertThrows(\n            AccessDeniedException.class,\n            () -> adminService.deleteUser(1L));\n    }\n\n    @Test\n    void unauthenticatedCannotAccess() {\n        assertThrows(\n            AuthenticationException.class,\n            () -> adminService.deleteUser(1L));\n    }\n}',
    tags: ['spring-security', 'method-security', 'authorization'],
  },
  {
    id: 'java-testing-l4-066',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you handle flaky tests caused by Testcontainers container startup?',
    options: ['Increase timeouts indefinitely', 'Use proper WaitStrategy, health checks, retry mechanisms, and container readiness probes', 'Run tests multiple times', 'Remove Testcontainers'],
    correctAnswer: 1,
    explanation: 'Address flakiness by: using appropriate WaitStrategy (HTTP health check, log message), increasing startup timeout for slow images, implementing retry logic, and using container readiness probes instead of port-only checks.',
    codeSnippet: '// Robust wait strategy\nGenericContainer<?> app =\n    new GenericContainer<>("my-app")\n        .withExposedPorts(8080)\n        .waitingFor(\n            Wait.forHttp("/actuator/health")\n                .forStatusCode(200)\n                .forResponsePredicate(\n                    resp -> resp.contains(\n                        "\\\"status\\\":\\\"UP\\\""))\n                .withStartupTimeout(\n                    Duration.ofMinutes(2))\n                .withReadTimeout(\n                    Duration.ofSeconds(10)))\n        .withStartupAttempts(3)\n        .withStartupCheckStrategy(\n            new MinimumDurationRunningStartup\n                Strategy(\n                    Duration.ofSeconds(5)));',
    tags: ['testcontainers', 'flaky-tests', 'reliability'],
  },
  {
    id: 'java-testing-l4-067',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you test reactive streams (Project Reactor) with Mockito?',
    options: ['Use Thread.sleep for async', 'Return Mono/Flux from mocks and use StepVerifier for assertions', 'Use blocking calls', 'Reactive code cannot use Mockito'],
    correctAnswer: 1,
    explanation: 'Stub reactive methods to return Mono/Flux and verify with StepVerifier. Mockito works well for stubbing reactive return types; StepVerifier handles async assertion.',
    codeSnippet: '@ExtendWith(MockitoExtension.class)\nclass ReactiveServiceTest {\n    @Mock ReactiveUserRepo repo;\n    @InjectMocks ReactiveUserService service;\n\n    @Test\n    void shouldFindUser() {\n        when(repo.findById(1L))\n            .thenReturn(\n                Mono.just(new User("John")));\n\n        StepVerifier.create(\n            service.getUser(1L))\n            .assertNext(user ->\n                assertEquals("John",\n                    user.getName()))\n            .verifyComplete();\n    }\n\n    @Test\n    void shouldHandleError() {\n        when(repo.findById(999L))\n            .thenReturn(Mono.error(\n                new NotFoundException()));\n\n        StepVerifier.create(\n            service.getUser(999L))\n            .expectError(\n                NotFoundException.class)\n            .verify();\n    }\n}',
    tags: ['reactive', 'project-reactor', 'StepVerifier'],
  },
  {
    id: 'java-testing-l4-068',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test multi-tenant applications?',
    options: ['Set tenant context before each test and verify data isolation between tenants', 'Use separate databases per test', 'Multi-tenancy does not need testing', 'Test one tenant only'],
    correctAnswer: 0,
    explanation: 'Test multi-tenancy by: setting different tenant contexts, verifying data isolation (tenant A cannot see tenant B data), testing tenant switching, and verifying shared vs tenant-specific resources.',
    codeSnippet: '@SpringBootTest\nclass MultiTenantTest {\n    @Autowired TenantContext ctx;\n    @Autowired UserRepository repo;\n\n    @Test\n    void shouldIsolateData() {\n        // Create data for tenant A\n        ctx.setTenantId("tenant-a");\n        repo.save(new User("Alice"));\n\n        // Create data for tenant B\n        ctx.setTenantId("tenant-b");\n        repo.save(new User("Bob"));\n\n        // Verify isolation\n        ctx.setTenantId("tenant-a");\n        List<User> usersA = repo.findAll();\n        assertThat(usersA)\n            .extracting(User::getName)\n            .containsExactly("Alice");\n\n        ctx.setTenantId("tenant-b");\n        List<User> usersB = repo.findAll();\n        assertThat(usersB)\n            .extracting(User::getName)\n            .containsExactly("Bob");\n    }\n}',
    tags: ['multi-tenancy', 'data-isolation', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-069',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'AssertJ',
    question: 'How do you test file system operations with AssertJ?',
    options: ['Use AssertJ file/path assertions with @TempDir', 'Read files and compare strings', 'File assertions are not in AssertJ', 'Use a mocked file system'],
    correctAnswer: 0,
    explanation: 'AssertJ provides rich file and path assertions: exists(), hasContent(), hasExtension(), isDirectory(), isReadable(), hasSameTextualContentAs(), etc. Combine with @TempDir for test isolation.',
    codeSnippet: '@Test\nvoid testFileOperations(\n        @TempDir Path tempDir) throws Exception {\n    Path output = service.generateReport(\n        tempDir, data);\n\n    assertThat(output)\n        .exists()\n        .isRegularFile()\n        .hasExtension("csv")\n        .hasParent(tempDir)\n        .content()\n        .contains("Name,Age")\n        .contains("John,30")\n        .hasLineCount(4);\n\n    assertThat(tempDir)\n        .isDirectoryContaining(\n            "glob:**.csv")\n        .isDirectoryNotContaining(\n            "glob:**.tmp");\n}',
    tags: ['assertj', 'file-assertions', 'filesystem'],
  },
  {
    id: 'java-testing-l4-070',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test eventually consistent systems?',
    options: ['Use Awaitility with appropriate polling intervals, timeouts, and condition checks', 'Only test strongly consistent parts', 'Add Thread.sleep() everywhere', 'Assume consistency in tests'],
    correctAnswer: 0,
    explanation: 'Use Awaitility to poll until the expected state is reached. Configure appropriate timeouts, poll intervals, and use untilAsserted() for complex condition checking. Avoid fixed sleeps.',
    codeSnippet: '@Test\nvoid shouldEventuallySync() {\n    // Trigger async operation\n    service.updateUserAsync(\n        1L, new UserDTO("Jane"));\n\n    // Wait for eventual consistency\n    await()\n        .atMost(Duration.ofSeconds(30))\n        .pollInterval(Duration.ofMillis(500))\n        .pollDelay(Duration.ofMillis(100))\n        .ignoreExceptions()\n        .untilAsserted(() -> {\n            User user =\n                readReplica.findById(1L);\n            assertThat(user.getName())\n                .isEqualTo("Jane");\n            assertThat(\n                searchIndex.findByName("Jane"))\n                .isNotEmpty();\n            assertThat(\n                cacheService.get("user:1"))\n                .isNotNull();\n        });\n}',
    tags: ['eventual-consistency', 'awaitility', 'async-testing'],
  },
  {
    id: 'java-testing-l4-071',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test with Elasticsearch using Testcontainers?',
    options: ['Mock all Elasticsearch operations', 'Use ElasticsearchContainer from Testcontainers', 'Use an embedded Elasticsearch', 'Use a shared Elasticsearch cluster'],
    correctAnswer: 1,
    explanation: 'Testcontainers provides ElasticsearchContainer for running a real Elasticsearch instance. This ensures tests use the same query behavior, mappings, and analyzers as production.',
    codeSnippet: '@Testcontainers\n@SpringBootTest\nclass SearchTest {\n    @Container\n    static ElasticsearchContainer elastic =\n        new ElasticsearchContainer(\n            DockerImageName.parse(\n                "elasticsearch:8.11.0"))\n            .withEnv("xpack.security.enabled",\n                "false")\n            .withEnv(\n                "discovery.type",\n                "single-node");\n\n    @DynamicPropertySource\n    static void props(\n            DynamicPropertyRegistry r) {\n        r.add("spring.elasticsearch.uris",\n            elastic::getHttpHostAddress);\n    }\n\n    @Test\n    void shouldSearchProducts() {\n        productRepo.save(\n            new Product("Java Testing Guide"));\n\n        // Wait for index refresh\n        await().atMost(\n            Duration.ofSeconds(5))\n            .until(() ->\n                searchService.search("Java")\n                    .size() > 0);\n\n        List<Product> results =\n            searchService.search("Java");\n        assertThat(results)\n            .extracting(Product::getName)\n            .contains(\n                "Java Testing Guide");\n    }\n}',
    tags: ['testcontainers', 'elasticsearch', 'search'],
  },
  {
    id: 'java-testing-l4-072',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you test interceptors and filters in isolation?',
    options: ['Interceptors cannot be tested', 'Use @WebMvcTest only', 'Create standalone MockMvc with the specific filter/interceptor, or unit test with mock chains', 'Only test through controllers'],
    correctAnswer: 2,
    explanation: 'Test interceptors/filters in isolation by: creating standalone MockMvc (MockMvcBuilders.standaloneSetup().addFilter()), or unit testing with mock FilterChain/HandlerInterceptor parameters.',
    codeSnippet: '// Standalone MockMvc with specific filter\n@Test\nvoid testLoggingFilter() throws Exception {\n    MockMvc mvc = MockMvcBuilders\n        .standaloneSetup(new TestController())\n        .addFilter(new LoggingFilter())\n        .build();\n\n    mvc.perform(get("/test")\n        .header("X-Request-Id", "abc-123"))\n        .andExpect(status().isOk())\n        .andExpect(\n            header().exists("X-Request-Id"));\n}\n\n// Unit test interceptor\n@Test\nvoid testAuthInterceptor() {\n    AuthInterceptor interceptor =\n        new AuthInterceptor(tokenService);\n    MockHttpServletRequest request =\n        new MockHttpServletRequest();\n    MockHttpServletResponse response =\n        new MockHttpServletResponse();\n\n    request.addHeader("Authorization",\n        "Bearer valid-token");\n    boolean result =\n        interceptor.preHandle(\n            request, response, null);\n    assertTrue(result);\n}',
    tags: ['filters', 'interceptors', 'isolation-testing'],
  },
  {
    id: 'java-testing-l4-073',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test Spring Boot actuator health checks?',
    options: ['Do not test health checks', 'Call the actuator endpoint and verify status, components, and details', 'Use mocks for health indicators', 'Health checks are tested by Spring'],
    correctAnswer: 1,
    explanation: 'Test health checks by calling /actuator/health and verifying: overall status, individual component statuses (db, disk, custom), and response structure. Test both UP and DOWN scenarios.',
    codeSnippet: '@SpringBootTest(\n    webEnvironment = RANDOM_PORT)\n@Testcontainers\nclass HealthCheckTest {\n    @Container @ServiceConnection\n    static PostgreSQLContainer<?> pg = ...;\n\n    @Autowired TestRestTemplate rest;\n\n    @Test\n    void shouldReportHealthy() {\n        ResponseEntity<Map> response =\n            rest.getForEntity(\n                "/actuator/health\",\n                Map.class);\n        assertEquals(200,\n            response.getStatusCode().value());\n        assertEquals("UP",\n            response.getBody().get("status"));\n    }\n\n    @Test\n    void shouldReportUnhealthy() {\n        pg.stop(); // Simulate DB down\n\n        ResponseEntity<Map> response =\n            rest.getForEntity(\n                "/actuator/health",\n                Map.class);\n        assertEquals(503,\n            response.getStatusCode().value());\n    }\n}',
    tags: ['actuator', 'health-checks', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-074',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Test Coverage',
    question: 'How do you exclude generated code from coverage reports?',
    options: ['Generated code cannot be excluded', 'Configure JaCoCo excludes by class pattern or use @Generated annotation', 'Delete generated code before measuring', 'Manually subtract from totals'],
    correctAnswer: 1,
    explanation: 'JaCoCo supports excludes by class pattern (e.g., **/generated/**) and recognizes @Generated annotation (javax.annotation.Generated or jakarta.annotation.Generated) to automatically skip generated classes.',
    codeSnippet: '<plugin>\n    <groupId>org.jacoco</groupId>\n    <artifactId>jacoco-maven-plugin</artifactId>\n    <configuration>\n        <excludes>\n            <!-- Lombok generated -->\n            <exclude>**/*Builder*</exclude>\n            <!-- MapStruct -->\n            <exclude>**/*MapperImpl*</exclude>\n            <!-- Generated DTOs -->\n            <exclude>**/generated/**</exclude>\n            <!-- JPA metamodel -->\n            <exclude>**/*_*</exclude>\n        </excludes>\n    </configuration>\n</plugin>\n\n// Or use annotation\n@Generated("lombok")\npublic class UserBuilder { }',
    tags: ['jacoco', 'exclusions', 'generated-code'],
  },
  {
    id: 'java-testing-l4-075',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'How do you test code that uses ThreadLocal values with Mockito?',
    options: ['ThreadLocal values are not testable', 'ThreadLocal is automatically handled by Mockito', 'Use @MockThreadLocal annotation', 'Set and clear ThreadLocal values in @BeforeEach/@AfterEach, or use try-finally blocks'],
    correctAnswer: 3,
    explanation: 'Manage ThreadLocal values manually in tests. Set values before the test, clean up after using @AfterEach or try-finally. For production code, consider using Spring RequestContext or scoped beans instead.',
    codeSnippet: 'class TenantContextTest {\n    @AfterEach\n    void cleanup() {\n        TenantContext.clear(); // IMPORTANT!\n    }\n\n    @Test\n    void shouldUseTenantContext() {\n        TenantContext.setTenantId("test-tenant");\n\n        service.processForCurrentTenant();\n\n        verify(repo).findByTenantId(\n            "test-tenant");\n    }\n\n    @Test\n    void shouldHandleMissingTenant() {\n        // No tenant set\n        assertThrows(\n            TenantNotFoundException.class,\n            () -> service\n                .processForCurrentTenant());\n    }\n}',
    tags: ['mockito', 'ThreadLocal', 'testing-patterns'],
  },
  {
    id: 'java-testing-l4-076',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test Spring Cloud stream bindings?',
    options: ['Stream bindings cannot be tested', 'Use real message brokers only', 'Mock all channels', 'Use TestChannelBinder for in-memory testing or Testcontainers for integration'],
    correctAnswer: 3,
    explanation: 'Spring Cloud Stream provides TestChannelBinder for in-memory unit testing of stream processors. For integration tests, use Testcontainers with real brokers (Kafka, RabbitMQ).',
    codeSnippet: '@SpringBootTest\nclass StreamProcessorTest {\n    @Autowired InputDestination input;\n    @Autowired OutputDestination output;\n\n    @Test\n    void shouldProcessMessage() {\n        // Send to input binding\n        input.send(\n            new GenericMessage<>(\n                new OrderEvent("ORD-1")),\n            "orders-in");\n\n        // Receive from output binding\n        Message<byte[]> result =\n            output.receive(\n                1000, "notifications-out");\n\n        assertNotNull(result);\n        String payload = new String(\n            result.getPayload());\n        assertThat(payload)\n            .contains("ORD-1");\n    }\n}',
    tags: ['spring-cloud-stream', 'messaging', 'integration-testing'],
  },
  {
    id: 'java-testing-l4-077',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you performance test with Testcontainers?',
    options: ['Only benchmark unit tests', 'Use Testcontainers for realistic infrastructure then apply load with tools like Gatling or JMH', 'Testcontainers is not for performance testing', 'Use production for performance tests'],
    correctAnswer: 1,
    explanation: 'Testcontainers provides realistic infrastructure for performance tests. Combine with Gatling (HTTP load testing), JMH (microbenchmarks), or custom load generators to test under realistic conditions.',
    tags: ['testcontainers', 'performance-testing', 'gatling'],
  },
  {
    id: 'java-testing-l4-078',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'What is incremental mutation testing?',
    options: ['Testing with increasing numbers of mutants', 'Running mutations in order', 'Gradually increasing mutation operators', 'Only mutating code that changed since the last run'],
    correctAnswer: 3,
    explanation: 'Incremental mutation testing only analyzes code that has changed since the last run, dramatically reducing execution time. PIT supports this via historyInputFile/historyOutputFile configuration.',
    tags: ['mutation-testing', 'pitest', 'incremental'],
  },
  {
    id: 'java-testing-l4-079',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test data migration correctness?',
    options: ['Compare row counts only', 'Trust the migration script', 'Test in production', 'Verify data integrity, transformations, default values, and backward compatibility with specific test data'],
    correctAnswer: 3,
    explanation: 'Test migrations by: inserting known test data, running migration, verifying transformations are correct, checking default values, ensuring no data loss, and testing rollback capability.',
    codeSnippet: '@Test\nvoid shouldMigrateUserData() {\n    // Insert pre-migration data\n    jdbc.execute("""\n        INSERT INTO users_v1\n        (id, full_name, email)\n        VALUES (1, \'John Doe\', \'j@test.com\')\n        """);\n\n    // Run migration\n    flyway.migrate();\n\n    // Verify post-migration data\n    Map<String, Object> user =\n        jdbc.queryForMap(\n            "SELECT * FROM users WHERE id = 1");\n\n    // Verify name was split\n    assertEquals("John",\n        user.get("first_name"));\n    assertEquals("Doe",\n        user.get("last_name"));\n    // Verify email preserved\n    assertEquals("j@test.com",\n        user.get("email"));\n    // Verify default values\n    assertNotNull(\n        user.get("created_at"));\n    assertEquals("ACTIVE",\n        user.get("status"));\n}',
    tags: ['data-migration', 'flyway', 'verification'],
  },
  {
    id: 'java-testing-l4-080',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mockito',
    question: 'What are the key differences between Mockito and MockK (Kotlin)?',
    options: ['MockK has first-class Kotlin support (coroutines, extension functions, object mocking); Mockito is Java-first', 'They are identical APIs', 'Mockito supports Kotlin better', 'MockK is deprecated'],
    correctAnswer: 0,
    explanation: 'MockK is designed for Kotlin with native support for: coroutines (coEvery/coVerify), extension functions, object declarations, companion objects, and DSL syntax. Mockito works in Kotlin via mockito-kotlin but lacks native coroutine support.',
    tags: ['mockito', 'mockk', 'kotlin'],
  },
  {
    id: 'java-testing-l4-081',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test retry logic with exponential backoff?',
    options: ['Wait for all retries in real time', 'Test only the first attempt', 'Disable retries in tests', 'Use a clock abstraction, mock time progression, and verify retry attempts'],
    correctAnswer: 3,
    explanation: 'Test retry logic by: injecting a Clock or time abstraction, configuring short retry intervals for tests, verifying the number of attempts, and checking exponential delay progression without waiting real time.',
    codeSnippet: '@SpringBootTest\n@TestPropertySource(properties = {\n    "retry.maxAttempts=3",\n    "retry.initialDelay=100",\n    "retry.multiplier=2"\n})\nclass RetryTest {\n    @MockBean ExternalApi api;\n    @Autowired RetryableService service;\n\n    @Test\n    void shouldRetryOnFailure() {\n        when(api.call())\n            .thenThrow(new IOException())\n            .thenThrow(new IOException())\n            .thenReturn("success");\n\n        String result = service.callWithRetry();\n\n        assertEquals("success", result);\n        verify(api, times(3)).call();\n    }\n\n    @Test\n    void shouldFailAfterMaxRetries() {\n        when(api.call())\n            .thenThrow(new IOException());\n\n        assertThrows(\n            MaxRetriesExceededException.class,\n            () -> service.callWithRetry());\n        verify(api, times(3)).call();\n    }\n}',
    tags: ['retry', 'exponential-backoff', 'resilience'],
  },
  {
    id: 'java-testing-l4-082',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you test with Redis using Testcontainers for session management?',
    options: ['Mock Redis operations', 'Use in-memory Redis alternative', 'Use GenericContainer with Redis image and configure Spring Session to use it', 'Use embedded Redis'],
    correctAnswer: 2,
    explanation: 'Run a real Redis container with Testcontainers, configure Spring Session to connect to it, and test session creation, retrieval, expiration, and invalidation with real Redis behavior.',
    codeSnippet: '@Testcontainers\n@SpringBootTest(\n    webEnvironment = RANDOM_PORT)\nclass SessionTest {\n    @Container\n    static GenericContainer<?> redis =\n        new GenericContainer<>(\n            "redis:7-alpine")\n            .withExposedPorts(6379);\n\n    @DynamicPropertySource\n    static void props(\n            DynamicPropertyRegistry r) {\n        r.add("spring.data.redis.host",\n            redis::getHost);\n        r.add("spring.data.redis.port",\n            () -> redis.getMappedPort(6379));\n    }\n\n    @Autowired TestRestTemplate rest;\n\n    @Test\n    void shouldMaintainSession() {\n        // Login\n        ResponseEntity<String> login =\n            rest.postForEntity("/login",\n                creds, String.class);\n        String sessionCookie =\n            login.getHeaders()\n                .getFirst("Set-Cookie");\n\n        // Access with session\n        HttpHeaders headers = new HttpHeaders();\n        headers.add("Cookie", sessionCookie);\n        ResponseEntity<String> response =\n            rest.exchange("/profile\",\n                HttpMethod.GET,\n                new HttpEntity<>(headers),\n                String.class);\n        assertEquals(200,\n            response.getStatusCode().value());\n    }\n}',
    tags: ['testcontainers', 'redis', 'session'],
  },
  {
    id: 'java-testing-l4-083',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'AssertJ',
    question: 'How do you test concurrent data structures with AssertJ?',
    options: ['Use multiple threads, collect results in concurrent collections, then assert with AssertJ', 'Use synchronized assertions', 'AssertJ does not support concurrency testing', 'Test sequentially only'],
    correctAnswer: 0,
    explanation: 'Run concurrent operations, collect results in thread-safe collections (ConcurrentLinkedQueue, etc.), then use AssertJ to assert on the collected results after all threads complete.',
    codeSnippet: '@Test\nvoid testConcurrentAccess()\n        throws Exception {\n    ConcurrentMap<Long, User> cache =\n        new ConcurrentHashMap<>();\n    ConcurrentLinkedQueue<Exception> errors =\n        new ConcurrentLinkedQueue<>();\n    int threadCount = 50;\n    CountDownLatch latch =\n        new CountDownLatch(threadCount);\n\n    for (int i = 0; i < threadCount; i++) {\n        final long id = i;\n        new Thread(() -> {\n            try {\n                User user = service.getOrCreate(id);\n                cache.put(id, user);\n            } catch (Exception e) {\n                errors.add(e);\n            } finally {\n                latch.countDown();\n            }\n        }).start();\n    }\n\n    latch.await(10, TimeUnit.SECONDS);\n\n    assertThat(errors).isEmpty();\n    assertThat(cache)\n        .hasSize(threadCount)\n        .allSatisfy((id, user) ->\n            assertThat(user.getId())\n                .isEqualTo(id));\n}',
    tags: ['concurrency', 'assertj', 'thread-safety'],
  },
  {
    id: 'java-testing-l4-084',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'How do you test Spring Boot configuration properties binding?',
    options: ['Configuration properties do not need testing', 'Trust Spring auto-configuration', 'Use reflection to check fields', 'Use @ConfigurationPropertiesTest or test with @EnableConfigurationProperties and verify binding'],
    correctAnswer: 3,
    explanation: 'Test configuration property binding by loading specific properties and verifying the @ConfigurationProperties class binds correctly. Test validation constraints, default values, and type conversion.',
    codeSnippet: '@SpringBootTest(properties = {\n    "app.retry.max-attempts=5",\n    "app.retry.delay=PT2S",\n    "app.retry.enabled=true",\n    "app.features[0]=FEATURE_A",\n    "app.features[1]=FEATURE_B"\n})\nclass ConfigPropertiesTest {\n    @Autowired AppProperties props;\n\n    @Test\n    void shouldBindProperties() {\n        assertThat(props.getRetry())\n            .satisfies(retry -> {\n                assertThat(retry.getMaxAttempts())\n                    .isEqualTo(5);\n                assertThat(retry.getDelay())\n                    .isEqualTo(\n                        Duration.ofSeconds(2));\n                assertThat(retry.isEnabled())\n                    .isTrue();\n            });\n        assertThat(props.getFeatures())\n            .containsExactly(\n                \"FEATURE_A\", \"FEATURE_B\");\n    }\n\n    @Test\n    void shouldValidateProperties() {\n        // Test with invalid values\n        assertThatThrownBy(() ->\n            new AnnotationConfigApplicationContext(\n                register -> register.register(\n                    InvalidConfig.class)))\n            .hasCauseInstanceOf(\n                BindValidationException.class);\n    }\n}',
    tags: ['configuration', 'properties', 'spring-boot'],
  },
  {
    id: 'java-testing-l4-085',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'TestContainers',
    question: 'How do you implement blue-green deployment testing with Testcontainers?',
    options: ['Test deployments manually', 'Deploy to production', 'Run two versions of the application in parallel containers and verify zero-downtime switching', 'Blue-green deployment cannot be tested locally'],
    correctAnswer: 2,
    explanation: 'Test blue-green deployments by running both old and new application versions in separate containers, verifying both handle requests correctly, and testing the switch-over process including database compatibility.',
    tags: ['testcontainers', 'blue-green', 'deployment-testing'],
  },
  {
    id: 'java-testing-l4-086',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Mutation Testing',
    question: 'How do you use PIT with Testcontainers integration tests?',
    options: ['Configure PIT targetTests to include integration tests, but be aware of significantly longer execution times', 'PIT cannot work with external dependencies', 'PIT only works with unit tests', 'Run PIT in Docker'],
    correctAnswer: 0,
    explanation: 'PIT can mutate code tested by integration tests. Configure targetTests to include integration test classes. However, execution time grows significantly because each mutant requires starting containers. Use incremental analysis and limit mutator scope.',
    tags: ['mutation-testing', 'pitest', 'integration-tests'],
  },
  {
    id: 'java-testing-l4-087',
    language: 'java',
    level: 'level4',
    category: 'Testing',
    subcategory: 'Integration Testing',
    question: 'What is ArchUnit and how does it complement traditional testing?',
    options: ['An architecture documentation tool', 'A build architecture tool', 'A container architecture framework', 'A library for testing architectural rules and coding conventions as unit tests'],
    correctAnswer: 3,
    explanation: 'ArchUnit is a library for checking architecture rules (package dependencies, layer access, naming conventions) as unit tests. It prevents architectural drift without manual code reviews.',
    codeSnippet: '@AnalyzeClasses(\n    packages = "com.example")\nclass ArchitectureTest {\n    @ArchTest\n    static final ArchRule serviceShouldNotAccess\n        Controllers = noClasses()\n            .that().resideInAPackage(\n                \"..service..\")\n            .should().accessClassesThat()\n            .resideInAPackage(\n                \"..controller..\");\n\n    @ArchTest\n    static final ArchRule repoShouldNotAccess\n        Service = noClasses()\n            .that().resideInAPackage(\n                \"..repository..\")\n            .should().accessClassesThat()\n            .resideInAPackage(\n                \"..service..\");\n\n    @ArchTest\n    static final ArchRule layeredArchitecture =\n        layeredArchitecture()\n            .consideringAllDependencies()\n            .layer("Controller")\n                .definedBy("..controller..")\n            .layer("Service")\n                .definedBy("..service..")\n            .layer("Repository")\n                .definedBy("..repository..")\n            .whereLayer("Controller")\n                .mayNotBeAccessedByAnyLayer()\n            .whereLayer("Service")\n                .mayOnlyBeAccessedByLayers(\n                    "Controller")\n            .whereLayer("Repository")\n                .mayOnlyBeAccessedByLayers(\n                    "Service");',
    tags: ['archunit', 'architecture-testing', 'coding-conventions'],
  },
];

export default questions;
