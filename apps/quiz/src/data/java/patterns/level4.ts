import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'java-patterns-l4-001',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'How can you implement a Singleton that survives class unloading and reloading in an OSGi or module-based environment?',
    options: [
      'Use volatile and synchronized',
      'Use the standard static field approach',
      'Store the instance in a shared class loader scope, use a service registry (OSGi ServiceRegistry), or persist state externally',
      'Use an enum — it handles class reloading automatically',
    ],
    correctAnswer: 2,
    explanation: 'In OSGi/modular environments, classes can be unloaded and reloaded. A traditional Singleton\'s static field is tied to the class loader. Solutions: use a service registry visible across bundles, store state externally (database/file), or use a shared parent class loader.',
    tags: ['singleton', 'osgi', 'classloader'],
  },
  {
    id: 'java-patterns-l4-002',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'What is the Phantom Singleton problem and how do you mitigate it?\n\n// Phantom: GC can collect the Singleton if the class\n// is not referenced, then re-initialization occurs\n// on next access',
    options: [
      'This only happens with WeakReferences',
      'Phantom Singleton is a myth',
      'If no strong reference to the class or its ClassLoader exists, the class (and its static fields) can be garbage collected; mitigation includes maintaining a strong reference chain or using a shared class loader',
      'The JVM never garbage collects Singletons',
    ],
    correctAnswer: 2,
    explanation: 'In some environments (especially application servers), if a class loader is unreachable, the class and its static fields (including Singleton instances) can be garbage collected. On next access, the class is reloaded, creating a new instance. Maintain strong reference chains to prevent this.',
    tags: ['singleton', 'phantom', 'garbage-collection'],
  },
  {
    id: 'java-patterns-l4-003',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Abstract Factory',
    question: 'How can you design an extensible Abstract Factory that supports adding new product types without modifying the factory interface?\n\npublic interface ExtensibleFactory {\n  <T> T create(Class<T> type);\n}',
    options: [
      'This is impossible with Abstract Factory',
      'Use reflection to scan classpath',
      'Use a generic create method that accepts a type token (Class<T>) and delegates to a registry of type-specific creators',
      'Add a new method for each product type',
    ],
    correctAnswer: 2,
    explanation: 'A generic create(Class<T>) method allows the factory to support any product type without interface changes. Internally, it maps Class tokens to creators (via a registry). New product types are added by registering creators, not modifying the interface.',
    codeSnippet: 'public interface ExtensibleFactory {\n  <T> T create(Class<T> type);\n}',
    tags: ['abstract-factory', 'extensible', 'type-token'],
  },
  {
    id: 'java-patterns-l4-004',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Factory Method',
    question: 'How can you implement a self-registering factory where product classes register themselves automatically?\n\npublic abstract class Shape {\n  private static Map<String, Supplier<Shape>> registry = new HashMap<>();\n  protected static void register(String name, Supplier<Shape> creator) {\n    registry.put(name, creator);\n  }\n  public static Shape create(String name) {\n    return registry.get(name).get();\n  }\n}\npublic class Circle extends Shape {\n  static { register("circle", Circle::new); }\n}',
    options: [
      'The factory must manually register all products',
      'Each product class registers itself in a static initializer block, which runs when the class is first loaded',
      'Only annotation processors can register products',
      'Self-registration is not possible in Java',
    ],
    correctAnswer: 1,
    explanation: 'Self-registering factories use static initializer blocks in each product class to register a creator function. The caveat: the class must be loaded first (e.g., via Class.forName() or classpath scanning) for the static block to execute.',
    codeSnippet: 'public class Circle extends Shape {\n  static { register("circle", Circle::new); }\n}',
    tags: ['factory-method', 'self-registration', 'static-initializer'],
  },
  {
    id: 'java-patterns-l4-005',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How can you implement a generic Builder that works with inheritance (the "Curiously Recurring Template Pattern" in Java)?\n\npublic abstract class PersonBuilder<T extends PersonBuilder<T>> {\n  protected String name;\n  @SuppressWarnings("unchecked")\n  protected T self() { return (T) this; }\n  public T name(String name) { this.name = name; return self(); }\n}\npublic class EmployeeBuilder extends PersonBuilder<EmployeeBuilder> {\n  private String department;\n  public EmployeeBuilder department(String dept) {\n    this.department = dept; return self();\n  }\n}',
    options: [
      'Use raw types to avoid the generic complexity',
      'Create completely separate builders for each subclass',
      'Builders cannot support inheritance',
      'Use self-referential generics (CRTP) so that base builder methods return the subtype, preserving the fluent API chain in subclass builders',
    ],
    correctAnswer: 3,
    explanation: 'The recursive generic pattern (PersonBuilder<T extends PersonBuilder<T>>) and the self() method ensure that base class methods return the subtype T, not the base type. This preserves fluent chaining when using a subclass builder like EmployeeBuilder.',
    codeSnippet: 'public abstract class PersonBuilder<T extends PersonBuilder<T>> {\n  protected T self() { return (T) this; }\n  public T name(String name) { this.name = name; return self(); }\n}',
    tags: ['builder', 'crtp', 'self-referential-generics'],
  },
  {
    id: 'java-patterns-l4-006',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How can you implement a Phantom Type Builder that uses type parameters to track builder state at compile time?\n\npublic class Builder<HasName, HasAge> {\n  public static class TRUE {} public static class FALSE {}\n  public Builder<TRUE, HasAge> name(String n) { ... return new Builder<>(); }\n  public Builder<HasName, TRUE> age(int a) { ... return new Builder<>(); }\n}\n// Only builds when all required fields are set:\npublic static Person build(Builder<TRUE, TRUE> b) { ... }',
    options: [
      'This is the same as the step builder',
      'Phantom type parameters encode builder state in the type system, so only fully-configured builders can be passed to build()',
      'Phantom types only work in Haskell',
      'Phantom types cannot be used in Java',
    ],
    correctAnswer: 1,
    explanation: 'Phantom types are type parameters that do not correspond to any runtime value but carry information at compile time. By changing the type parameter from FALSE to TRUE when a field is set, the compiler ensures build() only accepts fully configured builders.',
    codeSnippet: 'public class Builder<HasName, HasAge> {\n  public Builder<TRUE, HasAge> name(String n) { ... }\n  public Builder<HasName, TRUE> age(int a) { ... }\n}\npublic static Person build(Builder<TRUE, TRUE> b) { ... }',
    tags: ['builder', 'phantom-types', 'compile-time-safety'],
  },
  {
    id: 'java-patterns-l4-007',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Prototype',
    question: 'How can you implement a versioned prototype registry that supports creating objects at specific version snapshots?',
    options: [
      'Versioning is unrelated to Prototype',
      'Maintain a map of version numbers to prototype snapshots; clone from the version-specific prototype to create objects consistent with that version\'s state',
      'Create a new registry for each version',
      'Use git for versioning prototypes',
    ],
    correctAnswer: 1,
    explanation: 'A versioned prototype registry stores snapshots of prototypes at different versions (Map<Version, Map<String, Prototype>>). Clients request objects at a specific version, and the registry clones the appropriate prototype snapshot, ensuring version consistency.',
    tags: ['prototype', 'versioning', 'registry'],
  },
  {
    id: 'java-patterns-l4-008',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Adapter',
    question: 'How can you implement a runtime-adaptive adapter that dynamically adapts its behavior based on the capabilities of the adaptee?\n\npublic class SmartAdapter implements TargetInterface {\n  private Object adaptee;\n  public void execute() {\n    if (adaptee instanceof NewAPI) {\n      ((NewAPI) adaptee).newExecute();\n    } else if (adaptee instanceof OldAPI) {\n      ((OldAPI) adaptee).legacyRun();\n    } else {\n      Method m = adaptee.getClass().getMethod("run");\n      m.invoke(adaptee);\n    }\n  }\n}',
    options: [
      'The adapter inspects the adaptee\'s capabilities at runtime (via instanceof, reflection, or feature detection) and adjusts its delegation strategy accordingly',
      'This violates the Adapter pattern',
      'Runtime adaptation requires CGLIB',
      'Adapters must be statically typed',
    ],
    correctAnswer: 0,
    explanation: 'A smart adapter probes the adaptee\'s capabilities at runtime. It may use instanceof for known types, reflection for unknown types, or capability interfaces. This enables a single adapter to work with multiple incompatible adaptees discovered at runtime.',
    codeSnippet: 'if (adaptee instanceof NewAPI) ((NewAPI) adaptee).newExecute();\nelse if (adaptee instanceof OldAPI) ((OldAPI) adaptee).legacyRun();\nelse adaptee.getClass().getMethod("run").invoke(adaptee);',
    tags: ['adapter', 'runtime-adaptive', 'reflection'],
  },
  {
    id: 'java-patterns-l4-009',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Bridge',
    question: 'How does the Bridge pattern enable multi-dimensional extensibility in a notification system?\n\n// Dimension 1: Notification type (Email, SMS, Push)\n// Dimension 2: Urgency (Normal, Urgent, Critical)\n// Dimension 3: Template (Plain, HTML, Markdown)\n// Without Bridge: 3 * 3 * 3 = 27 classes\n// With Bridge: 3 + 3 + 3 = 9 classes',
    options: [
      'Bridge only handles two dimensions',
      'Three dimensions require the Mediator pattern',
      'You need multiple Bridge patterns',
      'Each dimension becomes an independent interface hierarchy connected via composition, reducing N*M*P classes to N+M+P',
    ],
    correctAnswer: 3,
    explanation: 'Bridge can separate any number of dimensions. Each dimension has its own hierarchy (NotificationType, UrgencyLevel, TemplateFormat), connected via composition. This reduces the multiplicative explosion (N*M*P) to an additive combination (N+M+P).',
    codeSnippet: '// Without Bridge: 3 * 3 * 3 = 27 classes\n// With Bridge: 3 + 3 + 3 = 9 classes',
    tags: ['bridge', 'multi-dimensional', 'composition'],
  },
  {
    id: 'java-patterns-l4-010',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How can you implement a visitor-enabled Composite with type-safe dispatch?\n\nsealed interface Component permits Leaf, Container {\n  <R> R accept(ComponentVisitor<R> visitor);\n}\nrecord Leaf(String name) implements Component {\n  public <R> R accept(ComponentVisitor<R> v) { return v.visitLeaf(this); }\n}\nrecord Container(String name, List<Component> children) implements Component {\n  public <R> R accept(ComponentVisitor<R> v) { return v.visitContainer(this); }\n}',
    options: [
      'This is only the Visitor pattern',
      'Composite and Visitor cannot be combined with sealed classes',
      'Sealed interfaces ensure exhaustive handling in visitors; records provide immutability; generics allow type-safe return values',
      'Sealed classes prevent adding new component types',
    ],
    correctAnswer: 2,
    explanation: 'Combining sealed interfaces (exhaustiveness), records (immutability), generics (type-safe return), and the Visitor pattern creates a robust type-safe Composite. The compiler verifies all component types are handled in every visitor.',
    codeSnippet: 'sealed interface Component permits Leaf, Container {\n  <R> R accept(ComponentVisitor<R> visitor);\n}',
    tags: ['composite', 'visitor', 'sealed-classes'],
  },
  {
    id: 'java-patterns-l4-011',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you implement a self-describing decorator chain that provides introspection?\n\npublic interface Describable {\n  List<String> getAppliedDecorations();\n}\npublic abstract class BaseDecorator implements Component, Describable {\n  protected Component wrapped;\n  public List<String> getAppliedDecorations() {\n    List<String> decorations = wrapped instanceof Describable d\n      ? new ArrayList<>(d.getAppliedDecorations())\n      : new ArrayList<>();\n    decorations.add(getDecorationName());\n    return decorations;\n  }\n  protected abstract String getDecorationName();\n}',
    options: [
      'Use reflection to scan the chain',
      'Decorator chains cannot be introspected',
      'Each decorator implements a Describable interface that collects decoration names from the chain, enabling runtime inspection of the full decoration stack',
      'Only the outermost decorator is visible',
    ],
    correctAnswer: 2,
    explanation: 'A self-describing decorator chain uses an additional interface (Describable) to collect information about applied decorations. Each decorator adds its name to the chain, allowing clients to inspect what decorations have been applied at runtime.',
    codeSnippet: 'public List<String> getAppliedDecorations() {\n  List<String> decorations = wrapped instanceof Describable d\n    ? new ArrayList<>(d.getAppliedDecorations())\n    : new ArrayList<>();\n  decorations.add(getDecorationName());\n  return decorations;\n}',
    tags: ['decorator', 'introspection', 'self-describing'],
  },
  {
    id: 'java-patterns-l4-012',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Facade',
    question: 'How should you design a Facade for a microservices architecture that handles distributed failures?\n\npublic class OrderFacade {\n  @CircuitBreaker(name = "inventory")\n  @Retry(maxAttempts = 3)\n  @TimeLimiter(timeoutDuration = "2s")\n  public OrderResult placeOrder(OrderRequest request) {\n    InventoryResult inv = inventoryService.check(request);\n    PaymentResult pay = paymentService.charge(request);\n    ShippingResult ship = shippingService.schedule(request);\n    return new OrderResult(inv, pay, ship);\n  }\n}',
    options: [
      'The Facade orchestrates multiple services and incorporates resilience patterns (circuit breaker, retry, timeout) to handle distributed failures gracefully',
      'Each service should handle its own failures',
      'Facades should not handle failures',
      'Use a global try-catch block',
    ],
    correctAnswer: 0,
    explanation: 'A distributed Facade must incorporate resilience patterns: circuit breakers (prevent cascading failures), retries (handle transient errors), timeouts (prevent hanging), and fallbacks. This adds essential operational concerns to the simplification role of the Facade.',
    codeSnippet: '@CircuitBreaker(name = "inventory")\n@Retry(maxAttempts = 3)\n@TimeLimiter(timeoutDuration = "2s")\npublic OrderResult placeOrder(OrderRequest request) { ... }',
    tags: ['facade', 'microservices', 'resilience'],
  },
  {
    id: 'java-patterns-l4-013',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How can you implement a Flyweight with automatic garbage collection of unused flyweights?\n\npublic class FlyweightPool<K, V> {\n  private final Map<K, WeakReference<V>> pool = new ConcurrentHashMap<>();\n  private final ReferenceQueue<V> refQueue = new ReferenceQueue<>();\n  private final Function<K, V> factory;\n  \n  public V get(K key) {\n    cleanup();\n    WeakReference<V> ref = pool.get(key);\n    V value = (ref != null) ? ref.get() : null;\n    if (value == null) {\n      value = factory.apply(key);\n      pool.put(key, new WeakReference<>(value, refQueue));\n    }\n    return value;\n  }\n}',
    options: [
      'This requires a finalizer',
      'WeakReferences allow the GC to reclaim unused flyweights; a ReferenceQueue detects when entries are collected for cleanup',
      'WeakReferences break the Flyweight pattern',
      'Flyweights should never be garbage collected',
    ],
    correctAnswer: 1,
    explanation: 'Using WeakReferences in the flyweight pool allows the GC to reclaim flyweights when no strong references remain. The ReferenceQueue enables proactive cleanup of stale entries. This prevents unbounded pool growth while maintaining the sharing benefits.',
    codeSnippet: 'private final Map<K, WeakReference<V>> pool = new ConcurrentHashMap<>();\nprivate final ReferenceQueue<V> refQueue = new ReferenceQueue<>();',
    tags: ['flyweight', 'weak-reference', 'garbage-collection'],
  },
  {
    id: 'java-patterns-l4-014',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How does ByteBuddy create proxy classes compared to CGLIB and JDK dynamic proxies?\n\nClass<?> dynamicType = new ByteBuddy()\n  .subclass(UserService.class)\n  .method(ElementMatchers.any())\n  .intercept(MethodDelegation.to(LogInterceptor.class))\n  .make()\n  .load(getClass().getClassLoader())\n  .getLoaded();',
    options: [
      'ByteBuddy works the same as JDK proxies',
      'ByteBuddy generates bytecode at runtime like CGLIB but with a more flexible API, supporting class subclassing, interface implementation, and method delegation with type-safe interceptors',
      'ByteBuddy modifies the original class bytecode in place',
      'ByteBuddy only works with interfaces',
    ],
    correctAnswer: 1,
    explanation: 'ByteBuddy generates bytecode at runtime (like CGLIB) but with a more fluent, type-safe API. It can subclass concrete classes, implement interfaces, and intercept methods with flexible delegation strategies. Spring Boot 3+ uses ByteBuddy as its default bytecode generation library.',
    codeSnippet: 'new ByteBuddy()\n  .subclass(UserService.class)\n  .method(ElementMatchers.any())\n  .intercept(MethodDelegation.to(LogInterceptor.class))\n  .make().load(getClass().getClassLoader()).getLoaded();',
    tags: ['proxy', 'bytebuddy', 'bytecode-generation'],
  },
  {
    id: 'java-patterns-l4-015',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Chain of Responsibility',
    question: 'How can you implement a non-blocking asynchronous Chain of Responsibility using CompletableFuture?\n\npublic interface AsyncHandler {\n  CompletableFuture<Response> handle(Request req, AsyncHandler next);\n}\n\npublic class AuthHandler implements AsyncHandler {\n  public CompletableFuture<Response> handle(Request req, AsyncHandler next) {\n    return authenticateAsync(req)\n      .thenCompose(authResult -> {\n        if (authResult.isValid()) return next.handle(req, null);\n        return CompletableFuture.completedFuture(Response.unauthorized());\n      });\n  }\n}',
    options: [
      'Use Thread.sleep() between handlers',
      'Async chains require the Observer pattern',
      'Chain of Responsibility must be synchronous',
      'Each handler returns a CompletableFuture that chains to the next handler via thenCompose(), enabling non-blocking async processing throughout the chain',
    ],
    correctAnswer: 3,
    explanation: 'Each handler returns a CompletableFuture. On successful processing, thenCompose() invokes the next handler. This creates a non-blocking chain where each handler can perform async I/O (database, HTTP calls) without blocking threads.',
    codeSnippet: 'return authenticateAsync(req)\n  .thenCompose(authResult -> {\n    if (authResult.isValid()) return next.handle(req, null);\n    return CompletableFuture.completedFuture(Response.unauthorized());\n  });',
    tags: ['chain-of-responsibility', 'async', 'completable-future'],
  },
  {
    id: 'java-patterns-l4-016',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can you implement a persistent Command pattern for event sourcing with snapshotting?\n\npublic class EventStore {\n  private List<Event> events = new ArrayList<>();\n  private Map<Long, Snapshot> snapshots = new TreeMap<>();\n  \n  public void append(Event event) { events.add(event); }\n  \n  public <T extends AggregateRoot> T reconstruct(Class<T> type, long id) {\n    Snapshot snapshot = getLatestSnapshot(id);\n    T aggregate = snapshot != null ? snapshot.restore(type) : type.newInstance();\n    getEventsSince(snapshot).forEach(aggregate::apply);\n    return aggregate;\n  }\n}',
    options: [
      'Snapshots replace event sourcing',
      'Snapshots periodically capture the full aggregate state to avoid replaying all events from the beginning during reconstruction',
      'Event sourcing does not need snapshots',
      'Every event should be a snapshot',
    ],
    correctAnswer: 1,
    explanation: 'In event sourcing, replaying all events from the start becomes expensive over time. Snapshots periodically capture the aggregate\'s full state. Reconstruction starts from the latest snapshot and replays only subsequent events, dramatically improving performance.',
    codeSnippet: 'public <T> T reconstruct(Class<T> type, long id) {\n  Snapshot snapshot = getLatestSnapshot(id);\n  T aggregate = snapshot != null ? snapshot.restore(type) : type.newInstance();\n  getEventsSince(snapshot).forEach(aggregate::apply);\n  return aggregate;\n}',
    tags: ['command', 'event-sourcing', 'snapshots'],
  },
  {
    id: 'java-patterns-l4-017',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can CQRS (Command Query Responsibility Segregation) be implemented using the Command pattern?',
    options: [
      'CQRS means using a separate database for each query',
      'Commands modify state (write model) via command handlers; Queries read state (read model) from optimized views; the two models are separated and can scale independently',
      'CQRS and Command pattern are unrelated',
      'CQRS requires microservices',
    ],
    correctAnswer: 1,
    explanation: 'CQRS separates the write model (Command side) from the read model (Query side). Commands are processed by command handlers that validate and apply changes. Queries are served from optimized read models (materialized views). This enables independent scaling and optimization.',
    tags: ['command', 'cqrs', 'architecture'],
  },
  {
    id: 'java-patterns-l4-018',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Iterator',
    question: 'How would you implement a cursor-based database iterator that fetches pages lazily?\n\npublic class CursorIterator<T> implements Iterator<T> {\n  private final Function<String, Page<T>> fetcher;\n  private String cursor;\n  private Iterator<T> pageIterator;\n  \n  public boolean hasNext() {\n    if (pageIterator != null && pageIterator.hasNext()) return true;\n    if (cursor == null && pageIterator != null) return false;\n    Page<T> page = fetcher.apply(cursor);\n    cursor = page.getNextCursor();\n    pageIterator = page.getItems().iterator();\n    return pageIterator.hasNext();\n  }\n  public T next() { return pageIterator.next(); }\n}',
    options: [
      'All data must be loaded into memory first',
      'The iterator fetches pages on demand using a cursor token, transparently exposing paginated data as a seamless stream of elements',
      'Cursor-based iteration is incompatible with Iterator',
      'Cursor-based iteration requires a custom collection',
    ],
    correctAnswer: 1,
    explanation: 'A cursor-based iterator fetches pages lazily using cursor tokens. It hides pagination from the consumer, who sees a simple Iterator. Pages are fetched on demand in hasNext(), maintaining constant memory regardless of total dataset size.',
    codeSnippet: 'public boolean hasNext() {\n  if (pageIterator != null && pageIterator.hasNext()) return true;\n  if (cursor == null && pageIterator != null) return false;\n  Page<T> page = fetcher.apply(cursor);\n  cursor = page.getNextCursor();\n  pageIterator = page.getItems().iterator();\n  return pageIterator.hasNext();\n}',
    tags: ['iterator', 'cursor', 'lazy-pagination'],
  },
  {
    id: 'java-patterns-l4-019',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'How can you implement a saga pattern using the Mediator as the orchestrator?',
    options: [
      'The Mediator orchestrates a sequence of distributed transactions, invoking compensating actions if any step fails, maintaining eventual consistency',
      'Sagas do not need a Mediator',
      'Sagas only work with choreography, not orchestration',
      'The Mediator handles only local transactions',
    ],
    correctAnswer: 0,
    explanation: 'An orchestrating Saga Mediator manages a sequence of steps across distributed services. If step N fails, the mediator invokes compensating transactions for steps N-1 through 1 in reverse, achieving eventual consistency without distributed locks.',
    tags: ['mediator', 'saga', 'distributed-transactions'],
  },
  {
    id: 'java-patterns-l4-020',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Memento',
    question: 'How can you implement a space-efficient Memento using structural sharing (persistent data structures)?',
    options: [
      'Use compression algorithms',
      'Instead of copying the entire state, share unchanged parts between mementos; only the changed portions are new objects, dramatically reducing memory',
      'Structural sharing is unrelated to Memento',
      'Delete old mementos immediately',
    ],
    correctAnswer: 1,
    explanation: 'Persistent data structures share unchanged portions between versions. When creating a memento, only the changed paths in the data structure are copied; the rest is shared. This provides O(log n) space per memento instead of O(n), similar to how Git stores snapshots.',
    tags: ['memento', 'structural-sharing', 'persistent-data-structures'],
  },
  {
    id: 'java-patterns-l4-021',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you implement exactly-once event delivery in a distributed Observer system?',
    options: [
      'Send each event only once and hope it arrives',
      'Exactly-once delivery is impossible',
      'Use idempotent event handlers combined with message deduplication (event IDs) and at-least-once delivery with acknowledgment',
      'Use synchronous calls instead of events',
    ],
    correctAnswer: 2,
    explanation: 'True exactly-once delivery is theoretically impossible in distributed systems. The practical approach is: at-least-once delivery (retry until acknowledged) + idempotent handlers (processing the same event multiple times produces the same result) + deduplication via unique event IDs.',
    tags: ['observer', 'exactly-once', 'distributed'],
  },
  {
    id: 'java-patterns-l4-022',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How does Project Reactor implement the Observer pattern with backpressure for reactive systems?\n\nFlux.create(sink -> {\n  // producer\n  sink.next("item1");\n  sink.next("item2");\n  sink.complete();\n})\n.onBackpressureBuffer(100)\n.publishOn(Schedulers.parallel())\n.subscribe(\n  item -> process(item),\n  error -> handleError(error),\n  () -> onComplete()\n);',
    options: [
      'Reactor replaces the Observer pattern entirely',
      'Reactor does not relate to Observer',
      'Flux is the subject that emits items; subscribe() registers observers; backpressure mechanisms control emission rate; schedulers control threading',
      'Backpressure is handled by the subscriber only',
    ],
    correctAnswer: 2,
    explanation: 'Project Reactor\'s Flux is a reactive Observer with backpressure. It implements the Reactive Streams spec: the publisher (Flux) emits items, subscribers receive them, and backpressure (buffer, drop, latest) controls flow. Schedulers control execution threads.',
    codeSnippet: 'Flux.create(sink -> { sink.next("item"); sink.complete(); })\n  .onBackpressureBuffer(100)\n  .publishOn(Schedulers.parallel())\n  .subscribe(item -> process(item));',
    tags: ['observer', 'reactor', 'backpressure'],
  },
  {
    id: 'java-patterns-l4-023',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you implement a hierarchical state machine (HSM) using the State pattern?',
    options: [
      'Use if-else chains for hierarchies',
      'Every state must be flat (no nesting)',
      'Hierarchical states are not possible with the State pattern',
      'States can contain sub-states, forming a tree; if a sub-state cannot handle an event, it delegates to its parent state, enabling behavior inheritance',
    ],
    correctAnswer: 3,
    explanation: 'Hierarchical state machines nest states within parent states. If a child state does not handle an event, it delegates to its parent (behavioral inheritance). This reduces duplication — common behavior lives in parent states, and children only override specifics.',
    tags: ['state', 'hierarchical', 'hsm'],
  },
  {
    id: 'java-patterns-l4-024',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'How can you implement an adaptive strategy that automatically selects the best algorithm based on input characteristics?\n\npublic class AdaptiveSorter<T extends Comparable<T>> {\n  public void sort(List<T> list) {\n    if (list.size() < 10) insertionSort(list);\n    else if (isNearlySorted(list)) timSort(list);\n    else if (hasLimitedRange(list)) countingSort(list);\n    else quickSort(list);\n  }\n}',
    options: [
      'This is not a pattern — it is just an if-else chain',
      'Adaptive strategies violate the Strategy pattern',
      'Adaptive selection requires machine learning',
      'The context analyzes input characteristics and selects the optimal strategy dynamically, combining Strategy with a meta-strategy selector',
    ],
    correctAnswer: 3,
    explanation: 'An adaptive strategy analyzes the input at runtime and selects the best algorithm. This is a meta-strategy: a strategy that selects strategies. The analysis logic acts as a factory for strategies, optimizing for the specific input characteristics.',
    codeSnippet: 'if (list.size() < 10) insertionSort(list);\nelse if (isNearlySorted(list)) timSort(list);\nelse if (hasLimitedRange(list)) countingSort(list);\nelse quickSort(list);',
    tags: ['strategy', 'adaptive', 'meta-strategy'],
  },
  {
    id: 'java-patterns-l4-025',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Template Method',
    question: 'How can you modernize the Template Method pattern to use functional composition instead of inheritance?\n\npublic class DataProcessor {\n  public void process(\n    Supplier<Data> reader,\n    UnaryOperator<Data> transformer,\n    Consumer<Data> writer\n  ) {\n    Data data = reader.get();\n    Data transformed = transformer.apply(data);\n    writer.accept(transformed);\n  }\n}',
    options: [
      'Functional composition is slower than inheritance',
      'Template Method requires inheritance',
      'This is the Strategy pattern, not Template Method',
      'Pass the variable steps as functional interfaces instead of using abstract methods, achieving the same algorithm structure without inheritance',
    ],
    correctAnswer: 3,
    explanation: 'The functional Template Method replaces inheritance with composition: variable steps are passed as lambdas/function references. The fixed algorithm structure remains in the method, but steps are injected rather than overridden. This avoids the fragile base class problem.',
    codeSnippet: 'public void process(\n  Supplier<Data> reader,\n  UnaryOperator<Data> transformer,\n  Consumer<Data> writer\n) {\n  Data data = reader.get();\n  Data transformed = transformer.apply(data);\n  writer.accept(transformed);\n}',
    tags: ['template-method', 'functional', 'composition'],
  },
  {
    id: 'java-patterns-l4-026',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'How can you implement an extensible Visitor using the "Extension Object" pattern to avoid modifying existing visitors when adding new element types?',
    options: [
      'Visitor can never be extended without modification',
      'Use reflection to find visit methods',
      'Delete old visitors and create new ones',
      'Use a default method in the visitor interface and a visitor extension registry that allows new visit methods to be registered dynamically',
    ],
    correctAnswer: 3,
    explanation: 'An extensible visitor uses: (1) a default visitUnknown() method for new types, (2) a registry of extension visitors, or (3) pattern matching with sealed classes. Default methods in Java interfaces enable adding new visit methods without breaking existing visitors.',
    tags: ['visitor', 'extensible', 'default-methods'],
  },
  {
    id: 'java-patterns-l4-027',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How do the SOLID principles sometimes conflict with each other, and how should you balance them?',
    options: [
      'Apply all principles to every class equally',
      'SRP may create many classes that complicate ISP compliance; strict OCP may violate KISS; the balance depends on context, team size, and expected change axes',
      'SOLID principles never conflict',
      'Always prioritize DIP over other principles',
    ],
    correctAnswer: 1,
    explanation: 'SOLID principles can conflict: SRP may create many small interfaces (ISP overhead). Strict OCP may add unnecessary abstractions (YAGNI). LSP may prevent useful specialization. The art is balancing them based on context — identify the most likely axes of change and apply principles selectively.',
    tags: ['solid', 'conflicts', 'trade-offs'],
  },
  {
    id: 'java-patterns-l4-028',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does the Stable Abstractions Principle (SAP) relate to the Dependency Inversion Principle?',
    options: [
      'SAP replaces DIP',
      'SAP applies only to concrete classes',
      'SAP states that a package should be as abstract as it is stable; DIP ensures dependencies point toward these stable abstractions, together forming a stable dependency architecture',
      'They are unrelated',
    ],
    correctAnswer: 2,
    explanation: 'SAP says stable packages (hard to change) should be abstract (flexible). DIP says depend on abstractions. Together: dependencies point toward stable, abstract packages. Unstable, concrete packages can change freely. This creates a robust architectural foundation.',
    tags: ['solid', 'sap', 'stable-abstractions'],
  },
  {
    id: 'java-patterns-l4-029',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Dependency Inversion',
    question: 'How can you implement a plugin architecture using DIP with Java modules (JPMS)?\n\nmodule core {\n  exports com.app.api;\n  uses com.app.api.Plugin;\n}\nmodule plugin.a {\n  requires core;\n  provides com.app.api.Plugin with com.plugin.a.PluginAImpl;\n}',
    options: [
      'The core module defines and uses abstractions (Plugin interface); plugin modules depend on core and provide implementations, inverting the traditional dependency direction',
      'JPMS replaces the need for DIP',
      'All plugins must be in the core module',
      'JPMS does not support DIP',
    ],
    correctAnswer: 0,
    explanation: 'JPMS provides module-level DIP: the core module defines the Plugin interface and declares it uses implementations. Plugin modules provide implementations. Dependencies point from plugins to core (toward abstractions), not from core to plugins.',
    codeSnippet: 'module core {\n  exports com.app.api;\n  uses com.app.api.Plugin;\n}\nmodule plugin.a {\n  provides com.app.api.Plugin with com.plugin.a.PluginAImpl;\n}',
    tags: ['dependency-inversion', 'jpms', 'plugin-architecture'],
  },
  {
    id: 'java-patterns-l4-030',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Repository Pattern',
    question: 'How can you implement a CQRS-based Repository with separate read and write models?\n\npublic interface WriteRepository<T> {\n  void save(T entity);\n  void delete(T entity);\n}\npublic interface ReadRepository<T, P> {\n  Optional<P> findById(long id);\n  List<P> findByFilter(Filter filter);\n}\n// T = domain entity (write model)\n// P = projection (read model, possibly denormalized)',
    options: [
      'Separate read and write repository interfaces operate on different models; the write model is normalized for consistency; the read model is optimized for queries',
      'CQRS requires NoSQL databases',
      'Repositories must have a single model',
      'Read and write models must be the same class',
    ],
    correctAnswer: 0,
    explanation: 'CQRS repositories separate concerns: WriteRepository handles domain entities (normalized, validated). ReadRepository serves projections (denormalized, query-optimized). Events synchronize the models. This enables independent optimization of reads and writes.',
    codeSnippet: 'public interface WriteRepository<T> {\n  void save(T entity);\n}\npublic interface ReadRepository<T, P> {\n  Optional<P> findById(long id);\n  List<P> findByFilter(Filter filter);\n}',
    tags: ['repository', 'cqrs', 'read-write-separation'],
  },
  {
    id: 'java-patterns-l4-031',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'DAO',
    question: 'How can you implement a generic DAO with type-safe criteria queries using JPA Criteria API?\n\npublic abstract class GenericDao<T> {\n  private final Class<T> entityClass;\n  protected GenericDao(Class<T> entityClass) {\n    this.entityClass = entityClass;\n  }\n  public List<T> findByCriteria(Consumer<CriteriaBuilder> criteriaConfig) {\n    CriteriaBuilder cb = em.getCriteriaBuilder();\n    CriteriaQuery<T> cq = cb.createQuery(entityClass);\n    Root<T> root = cq.from(entityClass);\n    criteriaConfig.accept(cb);\n    return em.createQuery(cq).getResultList();\n  }\n}',
    options: [
      'All DAOs must be entity-specific with no generics',
      'Generic DAOs are an anti-pattern',
      'A generic DAO uses the entity class type token and accepts query criteria as a functional parameter, providing type-safe, reusable data access',
      'JPA Criteria API replaces the DAO pattern',
    ],
    correctAnswer: 2,
    explanation: 'A generic DAO combines Java generics (for type safety) with the Criteria API (for type-safe queries). The entity class is captured as a type token. Query customization is injected via functional parameters, creating reusable yet flexible data access.',
    codeSnippet: 'public abstract class GenericDao<T> {\n  private final Class<T> entityClass;\n  public List<T> findByCriteria(Consumer<CriteriaBuilder> criteriaConfig) {\n    CriteriaQuery<T> cq = cb.createQuery(entityClass);\n    // ...\n  }\n}',
    tags: ['dao', 'generic', 'criteria-api'],
  },
  {
    id: 'java-patterns-l4-032',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How can you implement a smart reference proxy that performs reference counting and automatic resource cleanup?\n\npublic class ResourceProxy implements Resource {\n  private RealResource resource;\n  private AtomicInteger refCount = new AtomicInteger(0);\n  \n  public ResourceProxy acquire() {\n    if (refCount.getAndIncrement() == 0) {\n      resource = new RealResource();\n    }\n    return this;\n  }\n  public void release() {\n    if (refCount.decrementAndGet() == 0) {\n      resource.close();\n      resource = null;\n    }\n  }\n}',
    options: [
      'The proxy tracks active references; it creates the resource when the first reference is acquired and releases it when the last reference is released',
      'This is the Flyweight pattern',
      'Reference counting is not a proxy concern',
      'Java\'s GC handles this automatically',
    ],
    correctAnswer: 0,
    explanation: 'A smart reference proxy uses reference counting to manage resource lifecycle. The resource is lazily created on first acquire() and automatically cleaned up when the last reference calls release(). AtomicInteger ensures thread safety.',
    codeSnippet: 'public ResourceProxy acquire() {\n  if (refCount.getAndIncrement() == 0) resource = new RealResource();\n  return this;\n}\npublic void release() {\n  if (refCount.decrementAndGet() == 0) { resource.close(); resource = null; }\n}',
    tags: ['proxy', 'reference-counting', 'resource-management'],
  },
  {
    id: 'java-patterns-l4-033',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you implement a thread-safe decorator that adds synchronization transparently?\n\npublic class SynchronizedListDecorator<E> implements List<E> {\n  private final List<E> delegate;\n  private final ReadWriteLock lock = new ReentrantReadWriteLock();\n  \n  public boolean add(E e) {\n    lock.writeLock().lock();\n    try { return delegate.add(e); }\n    finally { lock.writeLock().unlock(); }\n  }\n  public E get(int index) {\n    lock.readLock().lock();\n    try { return delegate.get(index); }\n    finally { lock.readLock().unlock(); }\n  }\n}',
    options: [
      'Synchronization decorators are not useful',
      'A ReadWriteLock decorator allows concurrent reads while ensuring exclusive writes, transparently adding thread safety to any List implementation',
      'Use Collections.synchronizedList() which synchronizes on every operation with a single lock',
      'Thread safety requires modifying the original class',
    ],
    correctAnswer: 1,
    explanation: 'A ReadWriteLock decorator is superior to Collections.synchronizedList() (which uses a single mutex). It allows concurrent read access while ensuring exclusive write access, significantly improving throughput for read-heavy workloads.',
    codeSnippet: 'public boolean add(E e) {\n  lock.writeLock().lock();\n  try { return delegate.add(e); } finally { lock.writeLock().unlock(); }\n}\npublic E get(int index) {\n  lock.readLock().lock();\n  try { return delegate.get(index); } finally { lock.readLock().unlock(); }\n}',
    tags: ['decorator', 'thread-safety', 'read-write-lock'],
  },
  {
    id: 'java-patterns-l4-034',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Bridge',
    question: 'How can the Bridge pattern support runtime switching of implementations (hot-swapping)?',
    options: [
      'Bridge implementations cannot be changed at runtime',
      'Only the Strategy pattern supports hot-swapping',
      'Hot-swapping requires class reloading',
      'The abstraction holds a reference to the implementation interface; replacing this reference at runtime switches the implementation without changing the abstraction or client code',
    ],
    correctAnswer: 3,
    explanation: 'Since the Bridge abstraction holds a reference to the implementation via its interface, the implementation can be swapped at runtime by changing this reference. This enables hot-swapping (e.g., switching from MySQL to PostgreSQL without restart).',
    tags: ['bridge', 'hot-swapping', 'runtime'],
  },
  {
    id: 'java-patterns-l4-035',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How can you implement a reactive Composite that propagates changes asynchronously?\n\npublic class ReactiveComposite implements Component {\n  private Flux<Integer> valueFlux;\n  private List<Component> children;\n  \n  public Flux<Integer> getValue() {\n    return Flux.merge(\n      children.stream()\n        .map(Component::getValue)\n        .collect(Collectors.toList())\n    ).scan(0, Integer::sum);\n  }\n}',
    options: [
      'Composite cannot be reactive',
      'Reactive Composite requires the Observer pattern',
      'Use polling to detect changes',
      'Each component emits a Flux of values; the composite merges child fluxes and computes aggregates reactively, automatically propagating changes',
    ],
    correctAnswer: 3,
    explanation: 'A reactive Composite uses reactive streams (Flux) to propagate changes. When any child\'s value changes, the merged stream automatically recomputes the aggregate. This eliminates polling and manual cache invalidation.',
    codeSnippet: 'public Flux<Integer> getValue() {\n  return Flux.merge(\n    children.stream().map(Component::getValue).toList()\n  ).scan(0, Integer::sum);\n}',
    tags: ['composite', 'reactive', 'flux'],
  },
  {
    id: 'java-patterns-l4-036',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Facade',
    question: 'How can you implement a versioned API Facade that supports multiple API versions simultaneously?\n\n@RestController\npublic class UserFacadeV2 {\n  @GetMapping("/api/v2/users/{id}")\n  public UserDtoV2 getUser(@PathVariable long id) {\n    User user = userService.findById(id);\n    return UserMapper.toV2Dto(user);\n  }\n}\n// V1 facade still active for backward compatibility',
    options: [
      'A Facade should only support one version',
      'Deprecate old versions immediately',
      'Maintain separate Facade versions that adapt the same underlying services to different API contracts, enabling backward compatibility',
      'Use content negotiation instead of versioning',
    ],
    correctAnswer: 2,
    explanation: 'A versioned Facade maintains multiple versions simultaneously. Each version adapts the same underlying services to its specific API contract (different DTOs, different field names, etc.). This supports backward compatibility during API evolution.',
    codeSnippet: '@GetMapping("/api/v2/users/{id}")\npublic UserDtoV2 getUser(@PathVariable long id) {\n  User user = userService.findById(id);\n  return UserMapper.toV2Dto(user);\n}',
    tags: ['facade', 'api-versioning', 'backward-compatibility'],
  },
  {
    id: 'java-patterns-l4-037',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How can you implement a compile-time Flyweight using Java\'s annotation processing?\n\n@Interned\npublic class Color {\n  private final int r, g, b;\n  // Annotation processor generates a factory method\n  // that returns cached instances\n}',
    options: [
      'This requires runtime reflection',
      'Annotation processing only generates documentation',
      'An annotation processor generates Flyweight factory code at compile time, creating a cache and interning method automatically without runtime overhead',
      'Annotation processing cannot create Flyweights',
    ],
    correctAnswer: 2,
    explanation: 'Annotation processing can generate Flyweight infrastructure at compile time: a cache, a static factory method (of/valueOf), and proper equals/hashCode. This eliminates the boilerplate of manual Flyweight implementation with zero runtime overhead.',
    codeSnippet: '@Interned\npublic class Color {\n  private final int r, g, b;\n  // Generated: static factory with cache\n}',
    tags: ['flyweight', 'annotation-processing', 'compile-time'],
  },
  {
    id: 'java-patterns-l4-038',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'How can you implement a distributed Mediator using a message broker (e.g., Kafka)?',
    options: [
      'Mediator pattern does not work in distributed systems',
      'The message broker acts as a distributed mediator; components publish messages to topics and subscribe to topics, achieving decoupled communication across services',
      'Use direct HTTP calls between services',
      'Each service must know about all other services',
    ],
    correctAnswer: 1,
    explanation: 'A message broker (Kafka, RabbitMQ) is a distributed Mediator. Services publish events to topics and subscribe to relevant topics. No service knows about other services — the broker mediates all communication, enabling loose coupling at the distributed level.',
    tags: ['mediator', 'kafka', 'distributed-mediator'],
  },
  {
    id: 'java-patterns-l4-039',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Memento',
    question: 'How can you implement a time-travel debugging Memento that captures application state at every method call?',
    options: [
      'Save state to files manually',
      'Use aspect-oriented programming to intercept method calls and automatically capture mementos, enabling forward and backward stepping through execution history',
      'Use System.currentTimeMillis() to track time',
      'Time-travel debugging is only for IDEs',
    ],
    correctAnswer: 1,
    explanation: 'AOP aspects intercept method calls before/after execution and capture the relevant state as mementos. These mementos are stored in a timeline structure. A debugger UI can navigate forward/backward through the timeline, restoring state at any point.',
    tags: ['memento', 'time-travel', 'aop'],
  },
  {
    id: 'java-patterns-l4-040',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you implement a persistent state machine that survives application restarts?\n\n@Entity\npublic class Order {\n  @Id\n  private Long id;\n  \n  @Enumerated(EnumType.STRING)\n  private OrderState state;\n  \n  @Version\n  private Long version;\n  \n  public void transition(OrderEvent event) {\n    this.state = state.handle(event, this);\n    // JPA automatically persists the new state\n  }\n}',
    options: [
      'Save the entire object graph to a file',
      'Use only in-memory state machines',
      'State machines cannot be persisted',
      'Store the current state in a database (e.g., via JPA @Enumerated); on restart, load the entity and resume the state machine from the persisted state',
    ],
    correctAnswer: 3,
    explanation: 'A persistent state machine stores the current state in a database column (using @Enumerated for type safety). On restart, the entity is loaded with its persisted state. The @Version field ensures optimistic locking for concurrent state transitions.',
    codeSnippet: '@Enumerated(EnumType.STRING)\nprivate OrderState state;\n\npublic void transition(OrderEvent event) {\n  this.state = state.handle(event, this);\n}',
    tags: ['state', 'persistence', 'jpa'],
  },
  {
    id: 'java-patterns-l4-041',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'How can you implement a machine-learning-based strategy selector that optimizes strategy selection over time?',
    options: [
      'Always use the fastest strategy',
      'ML cannot be used with the Strategy pattern',
      'Randomly select strategies',
      'Collect performance metrics for each strategy per input type; use a multi-armed bandit or classification model to predict the best strategy for new inputs',
    ],
    correctAnswer: 3,
    explanation: 'A ML-based strategy selector treats strategy selection as a classification/optimization problem. It collects performance metrics (latency, accuracy) per strategy per input characteristics, and uses algorithms like multi-armed bandit or decision trees to select the optimal strategy.',
    tags: ['strategy', 'machine-learning', 'optimization'],
  },
  {
    id: 'java-patterns-l4-042',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Template Method',
    question: 'How can you implement a Template Method that supports aspect-oriented cross-cutting concerns?\n\n@Around("execution(* AbstractProcessor.process(..))")\npublic Object aroundProcess(ProceedingJoinPoint pjp) throws Throwable {\n  logStart(pjp);\n  Object result;\n  try {\n    result = pjp.proceed();\n    logSuccess(pjp, result);\n  } catch (Exception e) {\n    logFailure(pjp, e);\n    throw e;\n  }\n  recordMetrics(pjp);\n  return result;\n}',
    options: [
      'Cross-cutting concerns must be in the template method',
      'AOP replaces the Template Method pattern',
      'AOP aspects wrap the template method with cross-cutting concerns (logging, metrics, error handling) without modifying the template or its steps',
      'Template Method and AOP are incompatible',
    ],
    correctAnswer: 2,
    explanation: 'AOP complements Template Method by adding cross-cutting concerns (logging, metrics, security) around the template execution without modifying the template or any of its steps. This keeps the template focused on its algorithm while aspects handle orthogonal concerns.',
    codeSnippet: '@Around("execution(* AbstractProcessor.process(..))")\npublic Object aroundProcess(ProceedingJoinPoint pjp) throws Throwable {\n  logStart(pjp);\n  Object result = pjp.proceed();\n  recordMetrics(pjp);\n  return result;\n}',
    tags: ['template-method', 'aop', 'cross-cutting'],
  },
  {
    id: 'java-patterns-l4-043',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'How does the "Object Algebra" pattern solve the Expression Problem that the Visitor cannot?\n\npublic interface ExpAlg<E> {\n  E lit(int value);\n  E add(E left, E right);\n}\n// Add new operation:\npublic class EvalAlg implements ExpAlg<Integer> {\n  public Integer lit(int v) { return v; }\n  public Integer add(Integer l, Integer r) { return l + r; }\n}\n// Add new type:\npublic interface ExpAlgExt<E> extends ExpAlg<E> {\n  E mul(E left, E right);\n}',
    options: [
      'This only works in functional languages',
      'Object Algebras require code generation',
      'Object Algebras use generic factory interfaces to represent data types, allowing both new types and new operations to be added without modifying existing code',
      'Object Algebras are unrelated to Visitor',
    ],
    correctAnswer: 2,
    explanation: 'Object Algebras solve the Expression Problem by encoding data types as generic factory interfaces. New operations = new implementations of the algebra. New types = extending the algebra interface. Both extensions require no modification of existing code.',
    codeSnippet: 'public interface ExpAlg<E> {\n  E lit(int value);\n  E add(E left, E right);\n}\npublic interface ExpAlgExt<E> extends ExpAlg<E> {\n  E mul(E left, E right); // new type\n}',
    tags: ['visitor', 'object-algebra', 'expression-problem'],
  },
  {
    id: 'java-patterns-l4-044',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'How does Java\'s module system (JPMS) affect the Singleton pattern in terms of reflection attacks?\n\nmodule my.app {\n  exports com.myapp.api;\n  // Does NOT open packages for reflection\n}',
    options: [
      'JPMS makes all Singletons thread-safe',
      'JPMS has no effect on Singleton',
      'JPMS can prevent reflection-based Singleton attacks by not opening packages, making setAccessible() fail on private constructors at runtime',
      'JPMS requires enum-based Singletons',
    ],
    correctAnswer: 2,
    explanation: 'JPMS enforces strong encapsulation. If a module does not open its packages (via "opens" directive), reflection cannot access private members. This means setAccessible(true) on a private constructor fails at runtime, protecting the Singleton from reflection attacks.',
    codeSnippet: 'module my.app {\n  exports com.myapp.api;\n  // No "opens" = reflection blocked\n}',
    tags: ['singleton', 'jpms', 'reflection-protection'],
  },
  {
    id: 'java-patterns-l4-045',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Abstract Factory',
    question: 'How can you implement an Abstract Factory that uses compile-time dependency injection via Dagger 2?\n\n@Module\npublic class DatabaseModule {\n  @Provides @Singleton\n  DatabaseFactory provideDatabaseFactory() {\n    return new PostgresDatabaseFactory();\n  }\n}\n@Component(modules = DatabaseModule.class)\npublic interface AppComponent {\n  DatabaseFactory databaseFactory();\n}',
    options: [
      'This is not the Abstract Factory pattern',
      'Dagger uses runtime reflection like Spring',
      'Dagger generates the factory wiring at compile time, ensuring type-safe dependency injection without runtime reflection overhead',
      'Dagger cannot implement Abstract Factory',
    ],
    correctAnswer: 2,
    explanation: 'Dagger 2 generates all dependency injection code at compile time via annotation processing. Combined with Abstract Factory, it provides type-safe, zero-reflection factory wiring. Compile-time verification catches missing bindings before runtime.',
    codeSnippet: '@Module\npublic class DatabaseModule {\n  @Provides @Singleton\n  DatabaseFactory provideDatabaseFactory() {\n    return new PostgresDatabaseFactory();\n  }\n}',
    tags: ['abstract-factory', 'dagger', 'compile-time-di'],
  },
  {
    id: 'java-patterns-l4-046',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Adapter',
    question: 'How can you implement a bi-directional streaming adapter between a blocking InputStream and a reactive Flux?\n\npublic class StreamAdapter {\n  public static Flux<byte[]> toFlux(InputStream is, int bufferSize) {\n    return Flux.generate(\n      () -> is,\n      (stream, sink) -> {\n        byte[] buf = new byte[bufferSize];\n        int read = stream.read(buf);\n        if (read == -1) sink.complete();\n        else sink.next(Arrays.copyOf(buf, read));\n        return stream;\n      },\n      InputStream::close\n    );\n  }\n}',
    options: [
      'This requires a dedicated thread pool per stream',
      'Use Flux.generate() to lazily pull data from the blocking stream, adapting a pull-based blocking API to a push-based reactive API',
      'Blocking and reactive streams cannot be adapted',
      'Read all data into memory first, then create a Flux',
    ],
    correctAnswer: 1,
    explanation: 'Flux.generate() creates a demand-driven adapter. It reads from the InputStream only when downstream requests data (backpressure). The blocking read is called lazily per element, and the stream is closed on completion or error via the cleanup callback.',
    codeSnippet: 'return Flux.generate(\n  () -> is,\n  (stream, sink) -> {\n    byte[] buf = new byte[bufferSize];\n    int read = stream.read(buf);\n    if (read == -1) sink.complete();\n    else sink.next(Arrays.copyOf(buf, read));\n    return stream;\n  },\n  InputStream::close\n);',
    tags: ['adapter', 'reactive', 'flux-generate'],
  },
  {
    id: 'java-patterns-l4-047',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you implement a decorator that uses ThreadLocal to add context propagation?\n\npublic class ContextDecorator implements Service {\n  private static final ThreadLocal<Map<String, String>> context = \n    ThreadLocal.withInitial(HashMap::new);\n  private final Service delegate;\n  \n  public Result execute(Request req) {\n    context.get().put("requestId", req.getId());\n    context.get().put("userId", req.getUserId());\n    try {\n      return delegate.execute(req);\n    } finally {\n      context.get().clear();\n    }\n  }\n  public static String getContextValue(String key) {\n    return context.get().get(key);\n  }\n}',
    options: [
      'The decorator sets ThreadLocal context before delegation and clears it after, making context available throughout the call chain without passing parameters explicitly',
      'ThreadLocal should not be used with Decorator',
      'Context propagation requires MDC only',
      'ThreadLocal replaces the Decorator pattern',
    ],
    correctAnswer: 0,
    explanation: 'A ThreadLocal decorator sets contextual data (request ID, user ID) before delegation. Any code in the call chain can access this context statically. The finally block ensures cleanup, preventing ThreadLocal leaks in thread pools.',
    codeSnippet: 'public Result execute(Request req) {\n  context.get().put("requestId", req.getId());\n  try { return delegate.execute(req); }\n  finally { context.get().clear(); }\n}',
    tags: ['decorator', 'thread-local', 'context-propagation'],
  },
  {
    id: 'java-patterns-l4-048',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Chain of Responsibility',
    question: 'How can you implement a type-safe pipeline using the Chain of Responsibility pattern with generic type transformation?\n\npublic interface Stage<I, O> {\n  O process(I input);\n}\npublic class Pipeline<I, O> {\n  private final Function<I, O> chain;\n  public <N> Pipeline<I, N> addStage(Stage<O, N> stage) {\n    return new Pipeline<>(chain.andThen(stage::process));\n  }\n  public O execute(I input) { return chain.apply(input); }\n}',
    options: [
      'Pipelines cannot change types between stages',
      'Type-safe pipelines require reflection',
      'Each stage transforms input of type I to output of type O; addStage chains a new transformation, and generics ensure compile-time type safety across the entire pipeline',
      'This is the Decorator pattern',
    ],
    correctAnswer: 2,
    explanation: 'A generic pipeline uses parameterized stages where each stage\'s output type matches the next stage\'s input type. The compiler verifies type compatibility at each stage addition. This creates a type-safe, composable data processing pipeline.',
    codeSnippet: 'public class Pipeline<I, O> {\n  public <N> Pipeline<I, N> addStage(Stage<O, N> stage) {\n    return new Pipeline<>(chain.andThen(stage::process));\n  }\n}',
    tags: ['chain-of-responsibility', 'generic-pipeline', 'type-safe'],
  },
  {
    id: 'java-patterns-l4-049',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can you implement compensating transactions in a distributed system using the Command pattern?\n\npublic interface CompensableCommand {\n  CompletableFuture<Result> execute();\n  CompletableFuture<Void> compensate();\n}\npublic class SagaExecutor {\n  public CompletableFuture<Void> executeSaga(List<CompensableCommand> commands) {\n    List<CompensableCommand> executed = new ArrayList<>();\n    return commands.stream()\n      .reduce(CompletableFuture.completedFuture(null),\n        (future, cmd) -> future.thenCompose(v -> \n          cmd.execute().thenAccept(r -> executed.add(cmd))),\n        (a, b) -> a)\n      .exceptionallyCompose(ex -> compensateAll(executed));\n  }\n}',
    options: [
      'Use database rollback instead',
      'Each command has an execute() and a compensate() method; on failure, all previously executed commands are compensated in reverse order',
      'Compensating transactions are a database concept only',
      'Distributed compensation requires two-phase commit',
    ],
    correctAnswer: 1,
    explanation: 'Compensable commands pair each action with its reverse (compensate). The saga executor tracks executed commands. On failure, it compensates all executed commands in reverse order, achieving eventual consistency without distributed locks or two-phase commit.',
    codeSnippet: 'public interface CompensableCommand {\n  CompletableFuture<Result> execute();\n  CompletableFuture<Void> compensate();\n}',
    tags: ['command', 'compensating-transaction', 'saga'],
  },
  {
    id: 'java-patterns-l4-050',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How can Java agents and bytecode instrumentation implement transparent proxying without modifying source code?\n\npublic class ProfilingAgent {\n  public static void premain(String args, Instrumentation inst) {\n    inst.addTransformer((loader, name, cls, domain, bytecode) -> {\n      if (name.startsWith("com/myapp/")) {\n        return addProfilingBytecode(bytecode);\n      }\n      return bytecode;\n    });\n  }\n}',
    options: [
      'Java agents intercept class loading and modify bytecode before classes are loaded, injecting proxy behavior (profiling, logging) transparently into all specified classes',
      'This requires recompiling the application',
      'Java agents only work with interfaces',
      'Java agents cannot create proxies',
    ],
    correctAnswer: 0,
    explanation: 'Java agents use the Instrumentation API to intercept class loading. A ClassFileTransformer modifies bytecode before the JVM defines the class. This enables transparent, application-wide proxying (APM, profiling) without source code changes.',
    codeSnippet: 'public static void premain(String args, Instrumentation inst) {\n  inst.addTransformer((loader, name, cls, domain, bytecode) -> {\n    if (name.startsWith("com/myapp/")) {\n      return addProfilingBytecode(bytecode);\n    }\n    return bytecode;\n  });\n}',
    tags: ['proxy', 'java-agent', 'bytecode-instrumentation'],
  },
  {
    id: 'java-patterns-l4-051',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Iterator',
    question: 'How would you implement a tree iterator that supports depth-first and breadth-first traversal using the Strategy pattern?\n\npublic class TreeIterator<T> implements Iterator<T> {\n  private final Deque<TreeNode<T>> frontier;\n  private final TraversalStrategy strategy;\n  \n  public enum TraversalStrategy {\n    DFS { void add(Deque<TreeNode<T>> d, TreeNode<T> n) { d.push(n); } },\n    BFS { void add(Deque<TreeNode<T>> d, TreeNode<T> n) { d.offer(n); } };\n    abstract <T> void add(Deque<TreeNode<T>> d, TreeNode<T> node);\n  }\n}',
    options: [
      'DFS and BFS differ only in frontier ordering: DFS uses the deque as a stack (push/pop); BFS uses it as a queue (offer/poll); this single abstraction unifies both',
      'Use recursion for DFS and iteration for BFS',
      'Tree iterators cannot support multiple traversal strategies',
      'DFS and BFS require completely different implementations',
    ],
    correctAnswer: 0,
    explanation: 'DFS and BFS share the same algorithm structure — the only difference is how the frontier is managed. DFS adds to the front (stack: push/pop); BFS adds to the back (queue: offer/poll). A single Deque with a strategy for add() unifies both traversals.',
    codeSnippet: 'DFS { void add(Deque d, TreeNode n) { d.push(n); } },\nBFS { void add(Deque d, TreeNode n) { d.offer(n); } };',
    tags: ['iterator', 'tree-traversal', 'strategy'],
  },
  {
    id: 'java-patterns-l4-052',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you implement eventually consistent event-driven architecture using the Outbox pattern with the Observer?',
    options: [
      'Events are persisted to an "outbox" table in the same transaction as business changes; a separate process reads and publishes them, ensuring at-least-once delivery with eventual consistency',
      'Use two-phase commit for consistency',
      'The Outbox pattern is unrelated to Observer',
      'Store events in memory only',
    ],
    correctAnswer: 0,
    explanation: 'The Outbox pattern ensures reliable event publishing: business state changes and outbox event writes happen in one local transaction. A separate polling or CDC (Change Data Capture) process reads the outbox and publishes events, guaranteeing eventual delivery.',
    tags: ['observer', 'outbox-pattern', 'eventual-consistency'],
  },
  {
    id: 'java-patterns-l4-053',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you implement a concurrent state machine that handles events from multiple threads safely?\n\npublic class ConcurrentStateMachine {\n  private final AtomicReference<State> currentState;\n  \n  public boolean processEvent(Event event) {\n    State current;\n    State next;\n    do {\n      current = currentState.get();\n      next = current.handle(event);\n      if (next == null) return false;\n    } while (!currentState.compareAndSet(current, next));\n    next.onEntry();\n    return true;\n  }\n}',
    options: [
      'Use volatile for the state field',
      'State machines must be single-threaded',
      'Use synchronized on every method',
      'Use AtomicReference with compareAndSet (CAS) for lock-free state transitions, ensuring only one thread successfully transitions the state',
    ],
    correctAnswer: 3,
    explanation: 'AtomicReference with CAS provides lock-free state transitions. The loop retries if another thread changed the state concurrently. This ensures exactly one thread wins each transition, maintaining state machine integrity without locks.',
    codeSnippet: 'do {\n  current = currentState.get();\n  next = current.handle(event);\n} while (!currentState.compareAndSet(current, next));\nnext.onEntry();',
    tags: ['state', 'concurrent', 'lock-free'],
  },
  {
    id: 'java-patterns-l4-054',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'How can you implement the Strategy pattern with compile-time strategy selection using Java generics?\n\npublic class Processor<S extends Strategy> {\n  private final S strategy;\n  public Processor(S strategy) { this.strategy = strategy; }\n  public Result process(Data data) {\n    return strategy.execute(data);\n  }\n}\n// Compile-time bound:\nProcessor<FastStrategy> fast = new Processor<>(new FastStrategy());\n// Cannot accidentally use SlowStrategy methods',
    options: [
      'Generics cannot constrain strategy selection',
      'Generics add unnecessary complexity here',
      'This is identical to regular Strategy',
      'Parameterizing the context with the strategy type provides compile-time guarantees about which strategy is used, enabling type-specific optimizations',
    ],
    correctAnswer: 3,
    explanation: 'Parameterizing the context with the strategy type binds the strategy at compile time. This enables the compiler to verify strategy compatibility and allows the context to expose strategy-specific methods when needed, creating stronger type guarantees.',
    codeSnippet: 'public class Processor<S extends Strategy> {\n  private final S strategy;\n  public Result process(Data data) {\n    return strategy.execute(data);\n  }\n}',
    tags: ['strategy', 'generics', 'compile-time'],
  },
  {
    id: 'java-patterns-l4-055',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'How can you implement a "tagless final" encoding in Java as an alternative to the Visitor pattern?\n\npublic interface ExprSym<R> {\n  R lit(int value);\n  R add(R a, R b);\n  R neg(R a);\n}\n// Evaluation interpreter:\npublic class Eval implements ExprSym<Integer> {\n  public Integer lit(int v) { return v; }\n  public Integer add(Integer a, Integer b) { return a + b; }\n  public Integer neg(Integer a) { return -a; }\n}\n// Pretty printer:\npublic class PrettyPrint implements ExprSym<String> {\n  public String lit(int v) { return String.valueOf(v); }\n  public String add(String a, String b) { return "(" + a + " + " + b + ")"; }\n  public String neg(String a) { return "(-" + a + ")"; }\n}',
    options: [
      'Tagless final cannot add new operations',
      'Tagless final encodes the syntax as an interface parameterized by the result type; different implementations are different interpreters, solving the Expression Problem',
      'This is the same as the Visitor pattern',
      'Tagless final is only for Haskell',
    ],
    correctAnswer: 1,
    explanation: 'Tagless final encoding represents expressions as interface methods parameterized by the result type R. Each implementation (Eval, PrettyPrint) is a different "interpreter." New operations = new implementations. New syntax = extending the interface. This fully solves the Expression Problem.',
    codeSnippet: 'public interface ExprSym<R> {\n  R lit(int value);\n  R add(R a, R b);\n}\npublic class Eval implements ExprSym<Integer> { ... }\npublic class PrettyPrint implements ExprSym<String> { ... }',
    tags: ['visitor', 'tagless-final', 'expression-problem'],
  },
  {
    id: 'java-patterns-l4-056',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How do the Package Cohesion Principles (REP, CRP, CCP) extend SOLID to the package/module level?',
    options: [
      'REP (Reuse-Release Equivalence), CRP (Common Reuse), and CCP (Common Closure) apply SRP and OCP at the package level, determining which classes belong together',
      'These principles only apply to Java modules',
      'Package principles are unrelated to SOLID',
      'Package principles replace SOLID',
    ],
    correctAnswer: 0,
    explanation: 'REP: classes in a package should be released together. CRP: classes used together should be packaged together (ISP at package level). CCP: classes changed for the same reason belong together (SRP at package level). These extend SOLID thinking to architectural organization.',
    tags: ['solid', 'package-principles', 'architecture'],
  },
  {
    id: 'java-patterns-l4-057',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Dependency Inversion',
    question: 'How does the Hexagonal Architecture (Ports and Adapters) implement the Dependency Inversion Principle at the architectural level?',
    options: [
      'The domain depends on specific technologies',
      'Hexagonal Architecture does not use DIP',
      'The domain core defines ports (interfaces); adapters implement ports for specific technologies; all dependencies point inward toward the domain, inverting the traditional dependency direction',
      'Hexagonal Architecture requires Spring',
    ],
    correctAnswer: 2,
    explanation: 'Hexagonal Architecture applies DIP at the architectural level: the domain core defines ports (inbound and outbound interfaces). Adapters (REST controllers, database repositories) implement these ports. Dependencies point inward — adapters depend on the domain, never the reverse.',
    tags: ['dependency-inversion', 'hexagonal', 'ports-adapters'],
  },
  {
    id: 'java-patterns-l4-058',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Repository Pattern',
    question: 'How can you implement a multi-tenant Repository that automatically scopes queries to the current tenant?',
    options: [
      'Create a separate repository per tenant',
      'Add tenantId parameter to every method',
      'Multi-tenancy is not a Repository concern',
      'The repository automatically applies tenant filters using ThreadLocal tenant context, Hibernate @FilterDef, or schema-per-tenant database isolation',
    ],
    correctAnswer: 3,
    explanation: 'Multi-tenant repositories use: (1) discriminator column with automatic Hibernate @Filter based on ThreadLocal tenant context, (2) schema-per-tenant with dynamic DataSource routing, or (3) database-per-tenant. The repository automatically scopes without changing its API.',
    tags: ['repository', 'multi-tenant', 'hibernate-filter'],
  },
  {
    id: 'java-patterns-l4-059',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'DTO',
    question: 'How can you implement a GraphQL-style projection DTO that only serializes requested fields?\n\npublic class ProjectionDTO {\n  private Map<String, Object> fields = new LinkedHashMap<>();\n  private Set<String> requestedFields;\n  \n  @JsonAnyGetter\n  public Map<String, Object> getFields() {\n    return fields.entrySet().stream()\n      .filter(e -> requestedFields.contains(e.getKey()))\n      .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue));\n  }\n}',
    options: [
      'DTOs must always include all fields',
      'Use a dynamic Map-based DTO with @JsonAnyGetter that serializes only the fields requested by the client, eliminating over-fetching',
      'Use separate DTO classes for each field combination',
      'GraphQL replaces DTOs entirely',
    ],
    correctAnswer: 1,
    explanation: 'A projection DTO stores all available fields but serializes only those requested by the client (field selection). @JsonAnyGetter enables dynamic JSON serialization. This approach supports fine-grained queries (like GraphQL) with a single DTO class.',
    codeSnippet: '@JsonAnyGetter\npublic Map<String, Object> getFields() {\n  return fields.entrySet().stream()\n    .filter(e -> requestedFields.contains(e.getKey()))\n    .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue));\n}',
    tags: ['dto', 'projection', 'graphql-style'],
  },
  {
    id: 'java-patterns-l4-060',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How can you implement a Builder that produces immutable objects using Java records and a wither pattern?\n\npublic record Config(String host, int port, boolean ssl, Duration timeout) {\n  public Config withHost(String host) {\n    return new Config(host, this.port, this.ssl, this.timeout);\n  }\n  public Config withPort(int port) {\n    return new Config(this.host, port, this.ssl, this.timeout);\n  }\n  // ... more withers\n}',
    options: [
      'Withers are mutable setters',
      'Records cannot have builder-like APIs',
      'This creates too many objects',
      'The wither pattern creates new immutable record instances for each change, providing a builder-like fluent API while maintaining full immutability',
    ],
    correctAnswer: 3,
    explanation: 'Wither methods on records create new instances with one field changed, preserving immutability. Combined with a defaults-providing factory method, this creates a builder-like experience: Config.defaults().withHost("x").withPort(443). Each step produces a valid, immutable config.',
    codeSnippet: 'public record Config(String host, int port, boolean ssl, Duration timeout) {\n  public Config withHost(String host) {\n    return new Config(host, this.port, this.ssl, this.timeout);\n  }\n}',
    tags: ['builder', 'records', 'wither-pattern'],
  },
  {
    id: 'java-patterns-l4-061',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How can you implement a permission system using the Composite pattern with boolean algebra?\n\nsealed interface Permission permits SimplePermission, AllOf, AnyOf, Not {\n  boolean check(User user, Resource resource);\n}\nrecord AllOf(List<Permission> perms) implements Permission {\n  public boolean check(User u, Resource r) {\n    return perms.stream().allMatch(p -> p.check(u, r));\n  }\n}\nrecord AnyOf(List<Permission> perms) implements Permission {\n  public boolean check(User u, Resource r) {\n    return perms.stream().anyMatch(p -> p.check(u, r));\n  }\n}\nrecord Not(Permission perm) implements Permission {\n  public boolean check(User u, Resource r) {\n    return !perm.check(u, r);\n  }\n}',
    options: [
      'Permission rules are composed into boolean expression trees (AllOf=AND, AnyOf=OR, Not=NOT), evaluated recursively via Composite\'s uniform interface',
      'This is the Specification pattern, not Composite',
      'Use if-else chains for permissions',
      'Permissions should not use Composite',
    ],
    correctAnswer: 0,
    explanation: 'This is a Composite pattern forming boolean expression trees. AllOf (AND), AnyOf (OR), and Not (NOT) compose simple permissions into complex rules. The check() method evaluates recursively. Sealed interfaces ensure exhaustive handling.',
    codeSnippet: 'record AllOf(List<Permission> perms) implements Permission {\n  public boolean check(User u, Resource r) {\n    return perms.stream().allMatch(p -> p.check(u, r));\n  }\n}',
    tags: ['composite', 'permissions', 'boolean-algebra'],
  },
  {
    id: 'java-patterns-l4-062',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How can you use Project Valhalla\'s value types (preview feature) to achieve Flyweight-like memory optimization without the pattern?\n\n// Future Java value types:\nvalue class Point {\n  private final double x;\n  private final double y;\n}',
    options: [
      'Value types require explicit caching',
      'Value types are slower than objects',
      'Value types are stack-allocated, identity-free, and flattened into arrays; the JVM optimizes memory layout automatically, achieving Flyweight-like density without explicit caching',
      'Value types are unrelated to Flyweight',
    ],
    correctAnswer: 2,
    explanation: 'Project Valhalla\'s value types (inline classes) remove object headers and identity, enabling dense, cache-friendly memory layouts. An array of value Points stores raw data without per-element object overhead, achieving Flyweight-like memory efficiency at the JVM level.',
    codeSnippet: 'value class Point {\n  private final double x;\n  private final double y;\n}',
    tags: ['flyweight', 'valhalla', 'value-types'],
  },
  {
    id: 'java-patterns-l4-063',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How does GraalVM native-image handle dynamic proxies at build time?\n\n// reflect-config.json:\n[\n  {\n    "name": "java.lang.reflect.Proxy",\n    "methods": [{\"name\":\"newProxyInstance\", \"parameterTypes\":[...]}]\n  }\n]\n// proxy-config.json:\n[\n  [\"com.myapp.api.DataService\"]\n]',
    options: [
      'GraalVM requires proxy interfaces to be declared at build time in configuration files, so it can generate proxy classes ahead of time instead of at runtime',
      'GraalVM uses CGLIB instead of JDK proxies',
      'GraalVM automatically detects all proxies',
      'GraalVM does not support dynamic proxies',
    ],
    correctAnswer: 0,
    explanation: 'GraalVM native-image performs ahead-of-time compilation. Dynamic proxies require build-time configuration (proxy-config.json) declaring which interfaces will be proxied. GraalVM pre-generates proxy classes at build time since runtime class generation is not available in native images.',
    codeSnippet: '// proxy-config.json:\n[[\"com.myapp.api.DataService\"]]',
    tags: ['proxy', 'graalvm', 'native-image'],
  },
  {
    id: 'java-patterns-l4-064',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Chain of Responsibility',
    question: 'How can you implement a chain that handles different exception types using the Chain of Responsibility pattern?\n\npublic interface ExceptionHandler<T extends Exception> {\n  Class<T> getHandledType();\n  Response handle(T exception);\n}\npublic class ExceptionHandlerChain {\n  private final List<ExceptionHandler<?>> handlers;\n  \n  @SuppressWarnings("unchecked")\n  public Response handle(Exception ex) {\n    return handlers.stream()\n      .filter(h -> h.getHandledType().isAssignableFrom(ex.getClass()))\n      .findFirst()\n      .map(h -> ((ExceptionHandler<Exception>) h).handle(ex))\n      .orElseGet(() -> Response.internalError(ex));\n  }\n}',
    options: [
      'Exception handling should use try-catch, not patterns',
      'This violates the Chain of Responsibility pattern',
      'All exceptions should be handled by one handler',
      'Each handler declares which exception type it handles; the chain finds the most specific handler via isAssignableFrom(), creating an extensible exception handling framework',
    ],
    correctAnswer: 3,
    explanation: 'An exception handler chain uses typed handlers, each declaring its handled exception type. The chain uses isAssignableFrom() for type matching, finding the most specific handler. New exception handlers can be added without modifying the chain infrastructure.',
    codeSnippet: 'return handlers.stream()\n  .filter(h -> h.getHandledType().isAssignableFrom(ex.getClass()))\n  .findFirst()\n  .map(h -> ((ExceptionHandler<Exception>) h).handle(ex))\n  .orElseGet(() -> Response.internalError(ex));',
    tags: ['chain-of-responsibility', 'exception-handling', 'type-matching'],
  },
  {
    id: 'java-patterns-l4-065',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can you implement distributed command execution with exactly-once semantics using a command log?\n\npublic class DistributedCommandExecutor {\n  private final CommandLog log; // persistent, distributed\n  \n  public Result execute(Command cmd) {\n    if (log.isExecuted(cmd.getId())) {\n      return log.getResult(cmd.getId());\n    }\n    Result result = cmd.execute();\n    log.record(cmd.getId(), result); // atomic with execution\n    return result;\n  }\n}',
    options: [
      'Execute every command regardless of duplicates',
      'Exactly-once is impossible in distributed systems',
      'Record command IDs and results in a persistent log; check the log before execution to skip already-executed commands (idempotency key pattern)',
      'Use distributed locks for every command',
    ],
    correctAnswer: 2,
    explanation: 'The idempotency key pattern: each command has a unique ID. Before execution, check if the ID exists in the log (already executed). If yes, return the cached result. If no, execute and atomically record the result. This provides effective exactly-once semantics.',
    codeSnippet: 'if (log.isExecuted(cmd.getId())) {\n  return log.getResult(cmd.getId());\n}\nResult result = cmd.execute();\nlog.record(cmd.getId(), result);',
    tags: ['command', 'exactly-once', 'idempotency'],
  },
  {
    id: 'java-patterns-l4-066',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'How can you implement a conflict resolution Mediator for concurrent edits (like operational transformation)?',
    options: [
      'Use last-write-wins for all conflicts',
      'The Mediator receives edit operations from all clients, transforms conflicting operations against each other, and broadcasts the resolved results to maintain consistency',
      'Conflict resolution is not a Mediator concern',
      'Prevent concurrent editing entirely',
    ],
    correctAnswer: 1,
    explanation: 'A conflict resolution Mediator (like in collaborative editors) receives operations from multiple clients, applies operational transformation (OT) or CRDT rules to resolve conflicts, and broadcasts the transformed operations to all clients, maintaining consistency across all views.',
    tags: ['mediator', 'conflict-resolution', 'operational-transformation'],
  },
  {
    id: 'java-patterns-l4-067',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Memento',
    question: 'How can you combine the Memento pattern with event sourcing for bi-temporal data modeling?',
    options: [
      'Track both transaction time (when the change was recorded) and valid time (when the change was effective); mementos capture state at any (transaction-time, valid-time) coordinate',
      'Bi-temporal modeling does not relate to Memento',
      'Use two separate databases',
      'Bi-temporal requires NoSQL exclusively',
    ],
    correctAnswer: 0,
    explanation: 'Bi-temporal Memento tracks two time dimensions: transaction time (when recorded in the system) and valid time (when true in reality). This enables queries like "what did we believe the state was on date X, as of recording date Y" — essential for auditing and compliance.',
    tags: ['memento', 'bi-temporal', 'event-sourcing'],
  },
  {
    id: 'java-patterns-l4-068',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Template Method',
    question: 'How can sealed interfaces in Java improve the Template Method pattern?\n\nsealed interface ProcessingStep permits Validate, Transform, Persist {\n  void execute(Context ctx);\n}\nrecord Validate(Predicate<Context> rule) implements ProcessingStep {\n  public void execute(Context ctx) { if (!rule.test(ctx)) throw new ValidationException(); }\n}\nrecord Transform(UnaryOperator<Context> fn) implements ProcessingStep {\n  public void execute(Context ctx) { fn.apply(ctx); }\n}\nrecord Persist(Consumer<Context> saver) implements ProcessingStep {\n  public void execute(Context ctx) { saver.accept(ctx); }\n}',
    options: [
      'Sealed interfaces cannot improve Template Method',
      'This replaces Template Method entirely',
      'Sealed interfaces are too restrictive',
      'Sealed interfaces constrain the step types to a known set, enabling exhaustive switch-based processing and compile-time verification that all step types are handled',
    ],
    correctAnswer: 3,
    explanation: 'Sealed interfaces combined with records create type-safe, exhaustive step definitions. The template can use pattern matching switch to handle each step type, and the compiler ensures no step type is missed. Records add immutability and data transparency.',
    codeSnippet: 'sealed interface ProcessingStep permits Validate, Transform, Persist {\n  void execute(Context ctx);\n}',
    tags: ['template-method', 'sealed-interfaces', 'pattern-matching'],
  },
  {
    id: 'java-patterns-l4-069',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you implement a distributed event mesh using the Observer pattern with Apache Kafka Streams?',
    options: [
      'Each Kafka consumer is a Singleton',
      'Kafka Streams creates a distributed event mesh where KStream/KTable are reactive observables; topology operators (map, filter, join) form a distributed DAG of observers',
      'Kafka Streams does not use Observer',
      'Kafka Streams is only for batch processing',
    ],
    correctAnswer: 1,
    explanation: 'Kafka Streams implements distributed Observer: KStream/KTable are observable data streams. Topology operators (map, filter, join, aggregate) create a directed acyclic graph of transformations. State stores provide fault-tolerant materialized views.',
    tags: ['observer', 'kafka-streams', 'event-mesh'],
  },
  {
    id: 'java-patterns-l4-070',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you implement a state machine with guard conditions and actions using a DSL-like builder?\n\nStateMachine<OrderState, OrderEvent> sm = StateMachine.builder()\n  .initial(CREATED)\n  .transition(CREATED, PAY, PAID)\n    .guard(order -> order.getTotal() > 0)\n    .action(order -> paymentService.charge(order))\n  .transition(PAID, SHIP, SHIPPED)\n    .guard(order -> order.getAddress() != null)\n    .action(order -> shippingService.ship(order))\n  .build();',
    options: [
      'Guard conditions require if-else chains',
      'A fluent builder DSL defines states, events, transitions, guard conditions (boolean predicates), and actions (side effects), creating a readable, declarative state machine definition',
      'Actions should be in the state classes',
      'State machines cannot have guards and actions',
    ],
    correctAnswer: 1,
    explanation: 'A DSL builder defines the state machine declaratively: states, events, transitions, guards (predicates that must be true for the transition), and actions (side effects on transition). This creates a clear, readable definition that separates machine structure from behavior.',
    codeSnippet: 'StateMachine.builder()\n  .initial(CREATED)\n  .transition(CREATED, PAY, PAID)\n    .guard(order -> order.getTotal() > 0)\n    .action(order -> paymentService.charge(order))\n  .build();',
    tags: ['state', 'dsl', 'guard-conditions'],
  },
  {
    id: 'java-patterns-l4-071',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Adapter',
    question: 'How can you implement a protocol adapter that translates between REST and gRPC?\n\npublic class RestToGrpcAdapter {\n  private final ManagedChannel channel;\n  \n  @PostMapping("/api/users")\n  public ResponseEntity<UserDto> createUser(@RequestBody CreateUserRequest req) {\n    UserServiceGrpc.UserServiceBlockingStub stub = \n      UserServiceGrpc.newBlockingStub(channel);\n    \n    CreateUserProto grpcReq = CreateUserProto.newBuilder()\n      .setName(req.getName())\n      .setEmail(req.getEmail())\n      .build();\n    \n    UserProto grpcResp = stub.createUser(grpcReq);\n    return ResponseEntity.ok(toDto(grpcResp));\n  }\n}',
    options: [
      'REST and gRPC cannot be bridged',
      'The adapter translates between REST (JSON DTOs) and gRPC (Protocol Buffers), adapting the communication protocol while preserving business semantics',
      'Protocol adapters are not Adapters',
      'Use a message queue between REST and gRPC',
    ],
    correctAnswer: 1,
    explanation: 'A protocol adapter translates between two communication protocols: it receives REST requests (JSON), converts to gRPC calls (Protocol Buffers), and translates the response back. This allows REST clients to access gRPC services seamlessly.',
    codeSnippet: 'CreateUserProto grpcReq = CreateUserProto.newBuilder()\n  .setName(req.getName()).setEmail(req.getEmail()).build();\nUserProto grpcResp = stub.createUser(grpcReq);\nreturn ResponseEntity.ok(toDto(grpcResp));',
    tags: ['adapter', 'rest-grpc', 'protocol-translation'],
  },
  {
    id: 'java-patterns-l4-072',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you implement a resilience decorator stack (Circuit Breaker + Retry + Timeout + Bulkhead)?\n\nSupplier<String> decoratedSupplier = Decorators.ofSupplier(() -> service.call())\n  .withCircuitBreaker(circuitBreaker)\n  .withRetry(retry)\n  .withBulkhead(bulkhead)\n  .withTimeLimiter(timeLimiter)\n  .decorate();',
    options: [
      'Resilience patterns should not be stacked',
      'Use a single try-catch for resilience',
      'Each resilience pattern is a decorator; stacking them creates a resilience pipeline where the order matters: Bulkhead limits concurrency, CircuitBreaker prevents cascading failure, Retry handles transient errors, TimeLimiter prevents hanging',
      'Resilience requires a separate microservice',
    ],
    correctAnswer: 2,
    explanation: 'Resilience4j uses the Decorator pattern to stack resilience mechanisms. Each decorator wraps the previous one. Order matters: Bulkhead (outermost) limits concurrency, CircuitBreaker prevents cascading failure, Retry handles transients, TimeLimiter (innermost) prevents hanging.',
    codeSnippet: 'Decorators.ofSupplier(() -> service.call())\n  .withCircuitBreaker(circuitBreaker)\n  .withRetry(retry)\n  .withBulkhead(bulkhead)\n  .withTimeLimiter(timeLimiter)\n  .decorate();',
    tags: ['decorator', 'resilience4j', 'circuit-breaker'],
  },
  {
    id: 'java-patterns-l4-073',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does the Clean Architecture (Uncle Bob) incorporate all SOLID principles at the architectural level?',
    options: [
      'Clean Architecture replaces SOLID',
      'Entities follow SRP; use cases follow OCP; interface adapters follow ISP and LSP; the dependency rule enforces DIP — all dependencies point inward toward the domain',
      'Clean Architecture only applies to microservices',
      'Clean Architecture uses only DIP',
    ],
    correctAnswer: 1,
    explanation: 'Clean Architecture embodies all SOLID principles: SRP in focused entities/use cases, OCP through the plugin architecture, LSP in adapter substitutability, ISP in boundary interfaces, and DIP via the dependency rule (all dependencies point toward the center/domain).',
    tags: ['solid', 'clean-architecture', 'uncle-bob'],
  },
  {
    id: 'java-patterns-l4-074',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Dependency Inversion',
    question: 'How can you implement anti-corruption layers using DIP to protect your domain from external system changes?',
    options: [
      'Anti-corruption layers violate DIP',
      'Anti-corruption layers are only for legacy systems',
      'Use the external model directly in your domain',
      'Define domain interfaces, implement adapters that translate external models to domain models, shielding the domain from external API changes and data format differences',
    ],
    correctAnswer: 3,
    explanation: 'An anti-corruption layer is an Adapter that translates between the external system\'s model and your domain model. Your domain depends on its own interfaces (DIP). The ACL implements those interfaces using the external API, protecting the domain from external changes.',
    tags: ['dependency-inversion', 'anti-corruption-layer', 'ddd'],
  },
  {
    id: 'java-patterns-l4-075',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Factory Method',
    question: 'How can you implement a zero-reflection factory using Java\'s sealed classes and pattern matching?\n\nsealed interface Shape permits Circle, Rectangle, Triangle {}\n\npublic static Shape create(ShapeType type, Map<String, Double> params) {\n  return switch (type) {\n    case CIRCLE -> new Circle(params.get("radius"));\n    case RECTANGLE -> new Rectangle(params.get("width"), params.get("height"));\n    case TRIANGLE -> new Triangle(params.get("a"), params.get("b"), params.get("c"));\n  };\n}',
    options: [
      'This is a simple if-else, not a factory',
      'Sealed classes cannot be used in factories',
      'Factories always need reflection',
      'Sealed classes with exhaustive switch expressions create compile-time verified factories where the compiler ensures all types are handled, with zero reflection overhead',
    ],
    correctAnswer: 3,
    explanation: 'Sealed classes + exhaustive switch expressions create a factory verified at compile time. The compiler ensures every permitted subtype is handled. If a new subtype is added to the sealed interface, all switch expressions must be updated — preventing runtime errors with zero reflection.',
    codeSnippet: 'return switch (type) {\n  case CIRCLE -> new Circle(params.get("radius"));\n  case RECTANGLE -> new Rectangle(params.get("width"), params.get("height"));\n  case TRIANGLE -> new Triangle(params.get("a"), params.get("b"), params.get("c"));\n};',
    tags: ['factory-method', 'sealed-classes', 'zero-reflection'],
  },
  {
    id: 'java-patterns-l4-076',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Bridge',
    question: 'How can you implement a Bridge pattern that supports feature flags for gradual rollout of new implementations?',
    options: [
      'Feature flags are unrelated to Bridge',
      'Feature flags require the Strategy pattern only',
      'The abstraction holds multiple implementations and a feature flag evaluator that routes calls to the old or new implementation based on user/context, enabling gradual rollout',
      'Replace the implementation all at once',
    ],
    correctAnswer: 2,
    explanation: 'A feature-flagged Bridge holds both old and new implementations. A feature flag evaluator determines which implementation to use per request (based on user percentage, user ID, region). This enables gradual rollout with instant rollback capability.',
    tags: ['bridge', 'feature-flags', 'gradual-rollout'],
  },
  {
    id: 'java-patterns-l4-077',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Facade',
    question: 'How can you implement a Backend-for-Frontend (BFF) as a Facade pattern in microservices?',
    options: [
      'BFF requires GraphQL',
      'Each frontend (web, mobile, IoT) gets its own Facade service that aggregates data from multiple backend microservices and tailors the API to the specific frontend needs',
      'BFF is not a Facade',
      'Use a single API gateway for all frontends',
    ],
    correctAnswer: 1,
    explanation: 'BFF is a Facade per frontend type. Each BFF aggregates calls to multiple backend services, transforms data to the format needed by its specific frontend, and handles frontend-specific concerns (pagination, image sizing). This prevents a one-size-fits-all API.',
    tags: ['facade', 'bff', 'microservices'],
  },
  {
    id: 'java-patterns-l4-078',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you implement the Observer pattern with virtual threads (Project Loom) for massive concurrency?\n\npublic class VirtualThreadEventBus {\n  private final List<EventListener> listeners = new CopyOnWriteArrayList<>();\n  \n  public void publish(Event event) {\n    for (EventListener listener : listeners) {\n      Thread.startVirtualThread(() -> listener.onEvent(event));\n    }\n  }\n}',
    options: [
      'Virtual threads are slower than platform threads',
      'Virtual threads cannot be used with Observer',
      'This requires Project Reactor instead',
      'Each observer notification runs on a virtual thread, enabling millions of concurrent notifications without thread pool exhaustion or platform thread overhead',
    ],
    correctAnswer: 3,
    explanation: 'Virtual threads (Project Loom) enable massive concurrency with minimal overhead. Each observer notification runs on its own virtual thread. Unlike platform threads, millions of virtual threads can exist simultaneously, making per-notification threading practical.',
    codeSnippet: 'public void publish(Event event) {\n  for (EventListener listener : listeners) {\n    Thread.startVirtualThread(() -> listener.onEvent(event));\n  }\n}',
    tags: ['observer', 'virtual-threads', 'project-loom'],
  },
  {
    id: 'java-patterns-l4-079',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How can you implement an immutable Composite using persistent data structures for thread-safe, lock-free operations?',
    options: [
      'Make all fields volatile',
      'Immutable Composites are impractical due to copying overhead',
      'Use persistent data structures that share unchanged subtrees; modifications create new root nodes while reusing unmodified branches, enabling lock-free reads with O(log n) modifications',
      'Use synchronized blocks on every operation',
    ],
    correctAnswer: 2,
    explanation: 'Immutable Composites with structural sharing (persistent data structures) reuse unchanged subtrees. Adding/removing a child creates new nodes only along the path from the changed leaf to the root — O(log n) time and space. Reads require no locks.',
    tags: ['composite', 'immutable', 'persistent-data-structures'],
  },
  {
    id: 'java-patterns-l4-080',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'How can you design a testable alternative to Singleton using the Ambient Context pattern?\n\npublic class SecurityContext {\n  private static final ThreadLocal<SecurityContext> current = \n    ThreadLocal.withInitial(SecurityContext::defaultContext);\n  \n  public static SecurityContext current() { return current.get(); }\n  public static void setCurrent(SecurityContext ctx) { current.set(ctx); }\n  public static AutoCloseable runWith(SecurityContext ctx) {\n    SecurityContext prev = current();\n    setCurrent(ctx);\n    return () -> setCurrent(prev);\n  }\n}',
    options: [
      'ThreadLocal cannot replace Singleton',
      'Ambient Context is the same as Singleton',
      'Ambient Context uses ThreadLocal for per-thread "singleton" behavior with explicit override capability, making it testable by allowing test-specific contexts',
      'This pattern is not used in practice',
    ],
    correctAnswer: 2,
    explanation: 'Ambient Context provides singleton-like behavior per thread via ThreadLocal. Unlike true Singleton, the context can be overridden for testing (set a mock context) and supports per-thread isolation. The runWith() method provides scoped context replacement with automatic cleanup.',
    codeSnippet: 'public static AutoCloseable runWith(SecurityContext ctx) {\n  SecurityContext prev = current();\n  setCurrent(ctx);\n  return () -> setCurrent(prev);\n}',
    tags: ['singleton', 'ambient-context', 'testable'],
  },
  {
    id: 'java-patterns-l4-081',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does Domain-Driven Design (DDD) enforce SOLID principles through bounded contexts and aggregates?',
    options: [
      'DDD is unrelated to SOLID',
      'Bounded contexts enforce SRP at the module level; aggregates enforce LSP within consistency boundaries; anti-corruption layers enforce DIP between contexts',
      'DDD only uses SRP',
      'DDD replaces the need for SOLID',
    ],
    correctAnswer: 1,
    explanation: 'DDD enforces SOLID naturally: Bounded contexts (SRP for modules), aggregates (consistency boundaries with LSP for entities), domain services (SRP for operations), ports/adapters (DIP for infrastructure), and narrow interfaces for domain events (ISP).',
    tags: ['solid', 'ddd', 'bounded-contexts'],
  },
  {
    id: 'java-patterns-l4-082',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How can you implement method-level caching with automatic cache invalidation using a proxy?\n\n@Cacheable("users")\npublic User findById(long id) { ... }\n\n@CacheEvict(value = "users", key = "#user.id")\npublic void update(User user) { ... }\n\n@Caching(evict = {\n  @CacheEvict(value = "users", allEntries = true)\n})\npublic void importBulkUsers(List<User> users) { ... }',
    options: [
      'Cache invalidation must be manual',
      'Spring\'s caching proxy intercepts method calls, checks the cache on reads, and invalidates cache entries on writes, with configurable strategies per method',
      'Use database triggers for invalidation',
      'Cache proxies cannot handle invalidation',
    ],
    correctAnswer: 1,
    explanation: 'Spring\'s caching abstraction uses proxies to intercept method calls. @Cacheable checks/populates the cache. @CacheEvict invalidates specific or all entries. @CachePut always executes and updates the cache. The proxy manages all this transparently.',
    codeSnippet: '@Cacheable("users")\npublic User findById(long id) { ... }\n@CacheEvict(value = "users", key = "#user.id")\npublic void update(User user) { ... }',
    tags: ['proxy', 'caching', 'spring-cache'],
  },
  {
    id: 'java-patterns-l4-083',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Iterator',
    question: 'How can you implement a distributed iterator that transparently iterates over data partitioned across multiple nodes?',
    options: [
      'The iterator maintains partition state and fetches data from different nodes as it progresses, transparently presenting a unified sequential view across the distributed dataset',
      'Distributed iteration requires loading all data locally',
      'Use a MapReduce job instead',
      'Distributed iterators are impossible',
    ],
    correctAnswer: 0,
    explanation: 'A distributed iterator tracks the current partition and position within it. When one partition is exhausted, it transparently moves to the next node. The client sees a single Iterator interface. Implementations handle node failures, rebalancing, and consistency.',
    tags: ['iterator', 'distributed', 'partitioned'],
  },
  {
    id: 'java-patterns-l4-084',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How can you implement compile-time string interning as a Flyweight optimization using Java\'s constant pool?\n\npublic static final String STATUS_ACTIVE = "ACTIVE";\npublic static final String STATUS_INACTIVE = "INACTIVE";\n// Compile-time constants are automatically interned\n\n// vs. runtime:\nString s = new String("ACTIVE"); // NOT interned\nString interned = s.intern(); // manually interned',
    options: [
      'All strings are automatically interned',
      'Compile-time string constants are automatically interned in the constant pool; runtime strings can be manually interned; both achieve Flyweight sharing to reduce memory',
      'String interning is not related to Flyweight',
      'String interning is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Java\'s string constant pool is a JVM-level Flyweight. Compile-time constants and string literals are automatically interned. Runtime strings can be interned via intern(). For status codes, enum values, etc., this provides automatic memory sharing.',
    codeSnippet: 'public static final String STATUS_ACTIVE = "ACTIVE";\n// Automatically in constant pool\nString interned = runtimeString.intern(); // Manual interning',
    tags: ['flyweight', 'string-interning', 'constant-pool'],
  },
  {
    id: 'java-patterns-l4-085',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'How can you implement a choreography-based alternative to the Mediator pattern for distributed systems?',
    options: [
      'Instead of a central mediator, each service reacts to events independently; there is no central coordinator — services subscribe to events and emit their own, forming a decentralized communication mesh',
      'Choreography requires a message broker',
      'Choreography prevents any communication between services',
      'Choreography is the same as Mediator',
    ],
    correctAnswer: 0,
    explanation: 'Choreography is the decentralized alternative to orchestrating Mediator. Each service listens for events it cares about and emits events when its work is done. There is no single point of control or failure. The trade-off is harder to track the overall flow.',
    tags: ['mediator', 'choreography', 'decentralized'],
  },
  {
    id: 'java-patterns-l4-086',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'How can you implement a Visitor-based AST transformer for a compiler using immutable nodes?\n\npublic interface AstVisitor<R> {\n  R visitBinaryOp(BinaryOp node);\n  R visitLiteral(Literal node);\n  R visitVariable(Variable node);\n}\n\npublic class ConstantFolder implements AstVisitor<AstNode> {\n  public AstNode visitBinaryOp(BinaryOp node) {\n    AstNode left = node.left().accept(this);\n    AstNode right = node.right().accept(this);\n    if (left instanceof Literal l && right instanceof Literal r) {\n      return new Literal(evaluate(node.op(), l.value(), r.value()));\n    }\n    return new BinaryOp(node.op(), left, right);\n  }\n}',
    options: [
      'The visitor traverses the immutable AST and returns a new transformed AST, replacing nodes as needed while sharing unchanged subtrees',
      'AST transformation requires mutable nodes',
      'This mutates the original AST',
      'Constant folding is not a visitor concern',
    ],
    correctAnswer: 0,
    explanation: 'An AST transformer Visitor creates new AST nodes where changes are needed and reuses unchanged subtrees (structural sharing). The ConstantFolder visits each node, evaluates constant expressions, and returns simplified nodes — producing a new, optimized AST without mutating the original.',
    codeSnippet: 'if (left instanceof Literal l && right instanceof Literal r) {\n  return new Literal(evaluate(node.op(), l.value(), r.value()));\n}\nreturn new BinaryOp(node.op(), left, right);',
    tags: ['visitor', 'ast', 'constant-folding'],
  },
  {
    id: 'java-patterns-l4-087',
    language: 'java',
    level: 'level4',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How do modern Java features (records, sealed classes, pattern matching) change how we apply design patterns and SOLID principles?',
    options: [
      'Only functional patterns matter in modern Java',
      'Modern Java features make design patterns obsolete',
      'Records reduce Builder/DTO boilerplate; sealed classes enable exhaustive Visitor/State without frameworks; pattern matching simplifies dispatch; but SOLID principles remain essential for architectural decisions',
      'SOLID principles are no longer relevant in modern Java',
    ],
    correctAnswer: 2,
    explanation: 'Modern Java features reduce the ceremony of patterns (records for DTOs/Builders, sealed classes for Visitor/State, pattern matching for type dispatch) but do not eliminate the need for patterns or SOLID. The principles guide architectural decisions; the features reduce implementation boilerplate.',
    tags: ['solid', 'modern-java', 'evolution'],
  },
];

export default questions;
