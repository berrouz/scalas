import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'java-patterns-l3-001',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'What is the problem with this Singleton in a clustered environment?\n\npublic class CacheManager {\n  private static final CacheManager INSTANCE = new CacheManager();\n  private Map<String, Object> cache = new HashMap<>();\n  private CacheManager() {}\n  public static CacheManager getInstance() { return INSTANCE; }\n}',
    options: [
      'The HashMap will throw exceptions',
      'It uses too much memory',
      'Each JVM in the cluster will have its own Singleton instance, so they are not truly single across the cluster',
      'It is not thread-safe',
    ],
    correctAnswer: 2,
    explanation: 'In a clustered or distributed environment, each JVM process has its own class loader and memory space. The Singleton pattern only guarantees a single instance per JVM, not across the cluster. A distributed cache (e.g., Redis, Hazelcast) is needed.',
    codeSnippet: 'public class CacheManager {\n  private static final CacheManager INSTANCE = new CacheManager();\n  private Map<String, Object> cache = new HashMap<>();\n  private CacheManager() {}\n  public static CacheManager getInstance() { return INSTANCE; }\n}',
    tags: ['singleton', 'distributed', 'clustering'],
  },
  {
    id: 'java-patterns-l3-002',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'How can different class loaders break the Singleton pattern?',
    options: [
      'Each class loader loads a separate copy of the class, resulting in separate static fields and thus separate instances',
      'Class loaders make the constructor public',
      'Class loaders force garbage collection of the instance',
      'Class loaders delete the static field',
    ],
    correctAnswer: 0,
    explanation: 'In Java, classes loaded by different class loaders are considered different classes, even if they share the same fully qualified name. Each has its own static fields, so each gets its own Singleton instance.',
    tags: ['singleton', 'classloader', 'advanced'],
  },
  {
    id: 'java-patterns-l3-003',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Abstract Factory',
    question: 'How can you implement Abstract Factory using Java generics to avoid type casting?\n\npublic interface Factory<T> {\n  T create();\n}\npublic class CircleFactory implements Factory<Circle> {\n  public Circle create() { return new Circle(); }\n}',
    options: [
      'You cannot use generics with Abstract Factory',
      'By using Object as the return type',
      'By parameterizing the factory interface with the product type, concrete factories return the exact type without casting',
      'By using raw types',
    ],
    correctAnswer: 2,
    explanation: 'Generic factories return the exact parameterized type, eliminating the need for downcasting at the call site. Factory<Circle> guarantees that create() returns a Circle, providing compile-time type safety.',
    codeSnippet: 'public interface Factory<T> {\n  T create();\n}\npublic class CircleFactory implements Factory<Circle> {\n  public Circle create() { return new Circle(); }\n}',
    tags: ['abstract-factory', 'generics', 'type-safety'],
  },
  {
    id: 'java-patterns-l3-004',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Factory Method',
    question: 'How can the Factory Method pattern be combined with the Registry pattern?\n\npublic class ShapeFactory {\n  private static Map<String, Supplier<Shape>> registry = new HashMap<>();\n  public static void register(String type, Supplier<Shape> creator) {\n    registry.put(type, creator);\n  }\n  public static Shape create(String type) {\n    Supplier<Shape> creator = registry.get(type);\n    if (creator == null) throw new IllegalArgumentException("Unknown: " + type);\n    return creator.get();\n  }\n}',
    options: [
      'By storing pre-built objects in a map',
      'By using reflection exclusively',
      'This is impossible in Java',
      'By maintaining a registry of factory functions that can be dynamically added, avoiding conditionals in the factory',
    ],
    correctAnswer: 3,
    explanation: 'A registry-based factory maps type identifiers to factory functions (Supplier<Shape>). New types can be registered dynamically without modifying the factory class, fully adhering to the Open/Closed Principle.',
    codeSnippet: 'private static Map<String, Supplier<Shape>> registry = new HashMap<>();\npublic static void register(String type, Supplier<Shape> creator) {\n  registry.put(type, creator);\n}\npublic static Shape create(String type) {\n  return registry.get(type).get();\n}',
    tags: ['factory-method', 'registry', 'supplier'],
  },
  {
    id: 'java-patterns-l3-005',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How can you implement a type-safe step builder that enforces the order of method calls at compile time?\n\npublic interface NeedsName { NeedsAge name(String n); }\npublic interface NeedsAge { CanBuild age(int a); }\npublic interface CanBuild { Person build(); }',
    options: [
      'By using runtime validation only',
      'By throwing exceptions for invalid order',
      'By using annotations',
      'By returning different interfaces from each step, so the compiler only allows valid method sequences',
    ],
    correctAnswer: 3,
    explanation: 'A step builder uses a chain of interfaces where each step returns the next interface. This technique leverages the type system to enforce the correct order of method calls at compile time, preventing invalid states.',
    codeSnippet: 'public interface NeedsName { NeedsAge name(String n); }\npublic interface NeedsAge { CanBuild age(int a); }\npublic interface CanBuild { Person build(); }',
    tags: ['builder', 'step-builder', 'type-safety'],
  },
  {
    id: 'java-patterns-l3-006',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How does Lombok\'s @Builder annotation implement the Builder pattern?\n\n@Builder\npublic class Employee {\n  private String name;\n  private int age;\n  private String department;\n}',
    options: [
      'It generates a runtime proxy',
      'It uses reflection at runtime',
      'It creates a factory method',
      'It generates the Builder inner class, fluent setters, and build() method at compile time via annotation processing',
    ],
    correctAnswer: 3,
    explanation: 'Lombok\'s @Builder uses annotation processing at compile time to generate an inner Builder class with fluent setter methods and a build() method. The generated code is identical to a hand-written Builder but requires zero boilerplate.',
    codeSnippet: '@Builder\npublic class Employee {\n  private String name;\n  private int age;\n  private String department;\n}\n// Usage: Employee.builder().name("Alice").age(30).build();',
    tags: ['builder', 'lombok', 'annotation-processing'],
  },
  {
    id: 'java-patterns-l3-007',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Prototype',
    question: 'Why is Java\'s Object.clone() mechanism considered problematic, leading many to prefer copy constructors?',
    options: [
      'clone() is too fast',
      'clone() creates deep copies by default',
      'clone() bypasses constructors, has a confusing contract, requires checked exceptions, and returns Object requiring casting',
      'clone() is deprecated in Java 17',
    ],
    correctAnswer: 2,
    explanation: 'Object.clone() bypasses constructors (can leave objects in invalid states), has a confusing contract (what exactly does "copy" mean?), requires handling CloneNotSupportedException, and returns Object requiring a cast. Copy constructors are clearer and safer.',
    tags: ['prototype', 'clone', 'copy-constructor'],
  },
  {
    id: 'java-patterns-l3-008',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Adapter',
    question: 'How can you use a lambda expression as an adapter in Java?\n\nExecutorService executor = Executors.newSingleThreadExecutor();\nexecutor.submit(() -> legacyService.process());',
    options: [
      'The lambda creates a new thread',
      'This is not an adapter',
      'The lambda acts as an adapter, converting the legacyService.process() call into a Runnable expected by submit()',
      'The lambda is a factory method',
    ],
    correctAnswer: 2,
    explanation: 'The lambda () -> legacyService.process() adapts the legacy service method (which may return a value or throw checked exceptions) into the Runnable interface expected by ExecutorService.submit(). Lambdas can serve as lightweight, inline adapters.',
    codeSnippet: 'executor.submit(() -> legacyService.process());',
    tags: ['adapter', 'lambda', 'functional'],
  },
  {
    id: 'java-patterns-l3-009',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How do you prevent the loss of type identity when using the Decorator pattern?\n\n// Problem:\nCoffee coffee = new MilkDecorator(new SimpleCoffee());\ncoffee instanceof SimpleCoffee; // false!',
    options: [
      'Use instanceof checks everywhere',
      'Decorators should delegate identity checks to the wrapped component, or avoid relying on concrete types',
      'Make all decorators extend SimpleCoffee',
      'Use reflection to check the wrapped type',
    ],
    correctAnswer: 1,
    explanation: 'Decorator changes the apparent type — the decorated object is no longer an instance of the original class. Solutions include: (1) using the component interface instead of concrete types, (2) providing a getWrapped() method, or (3) overriding equals/hashCode to delegate to the wrapped component.',
    codeSnippet: 'Coffee coffee = new MilkDecorator(new SimpleCoffee());\ncoffee instanceof SimpleCoffee; // false!',
    tags: ['decorator', 'type-identity', 'instanceof'],
  },
  {
    id: 'java-patterns-l3-010',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How does Java\'s dynamic proxy work internally?\n\nDataService proxy = (DataService) Proxy.newProxyInstance(\n  DataService.class.getClassLoader(),\n  new Class[]{DataService.class},\n  (proxy1, method, args) -> {\n    System.out.println("Before: " + method.getName());\n    Object result = method.invoke(realService, args);\n    System.out.println("After: " + method.getName());\n    return result;\n  }\n);',
    options: [
      'It modifies the bytecode of the original class',
      'It generates a proxy class at runtime that implements the specified interfaces and delegates all calls to the InvocationHandler',
      'It uses native code to intercept method calls',
      'It creates a source file and compiles it',
    ],
    correctAnswer: 1,
    explanation: 'Proxy.newProxyInstance() dynamically generates a class at runtime that implements the specified interfaces. Every method call on the proxy is intercepted and forwarded to the InvocationHandler\'s invoke() method.',
    codeSnippet: 'DataService proxy = (DataService) Proxy.newProxyInstance(\n  DataService.class.getClassLoader(),\n  new Class[]{DataService.class},\n  (proxy1, method, args) -> {\n    Object result = method.invoke(realService, args);\n    return result;\n  }\n);',
    tags: ['proxy', 'dynamic-proxy', 'invocation-handler'],
  },
  {
    id: 'java-patterns-l3-011',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'What is a limitation of Java\'s built-in dynamic proxy (java.lang.reflect.Proxy)?',
    options: [
      'It cannot handle multiple interfaces',
      'It only works with public methods',
      'It requires the proxied class to be final',
      'It can only proxy interfaces, not concrete classes',
    ],
    correctAnswer: 3,
    explanation: 'Java\'s built-in Proxy can only create proxies for interfaces, not concrete classes. To proxy classes, you need a library like CGLIB or ByteBuddy, which generates subclasses of the target class at runtime.',
    tags: ['proxy', 'dynamic-proxy', 'limitation'],
  },
  {
    id: 'java-patterns-l3-012',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Bridge',
    question: 'How does the Bridge pattern prevent a "class explosion" problem?\n\n// Without Bridge: need N x M classes\n// RedCircle, BlueCircle, GreenCircle, RedSquare, BlueSquare, GreenSquare...\n\n// With Bridge:\n// Shapes: Circle, Square (N=2)\n// Colors: Red, Blue, Green (M=3)\n// Total classes: N + M = 5 instead of N * M = 6',
    options: [
      'By merging all classes into one',
      'By separating two orthogonal dimensions into independent hierarchies connected by composition, reducing N*M classes to N+M',
      'By using generics',
      'By using inheritance for all combinations',
    ],
    correctAnswer: 1,
    explanation: 'Without Bridge, every combination of two dimensions requires its own class (N*M classes). Bridge separates the dimensions and connects them via composition, requiring only N+M classes. The savings grow dramatically with more dimensions.',
    codeSnippet: '// Without Bridge: N * M = 6 classes\n// With Bridge: N + M = 5 classes\n// Shapes: Circle, Square\n// Colors: Red, Blue, Green',
    tags: ['bridge', 'class-explosion', 'composition'],
  },
  {
    id: 'java-patterns-l3-013',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How would you implement thread-safe operations on a Composite tree?',
    options: [
      'Composites are inherently thread-safe',
      'Make all methods synchronized on the root node',
      'Use a CopyOnWriteArrayList for children or implement read-write locks for traversal vs. modification',
      'Use volatile on all fields',
    ],
    correctAnswer: 2,
    explanation: 'Thread-safe Composites can use CopyOnWriteArrayList (safe for read-heavy scenarios), read-write locks (separate locks for traversal and modification), or immutable composite structures where modifications create new trees.',
    tags: ['composite', 'thread-safety', 'concurrent'],
  },
  {
    id: 'java-patterns-l3-014',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How would you implement a Flyweight pool with bounded size and eviction?',
    options: [
      'Use a TreeMap',
      'Use a WeakHashMap or an LRU cache (e.g., LinkedHashMap with removeEldestEntry) as the flyweight pool',
      'Use an unbounded HashMap',
      'Create a new flyweight every time',
    ],
    correctAnswer: 1,
    explanation: 'A bounded flyweight pool can use WeakHashMap (entries are GC\'d when no strong references remain) or a LinkedHashMap with removeEldestEntry() override to implement LRU eviction, preventing unbounded memory growth.',
    tags: ['flyweight', 'eviction', 'lru-cache'],
  },
  {
    id: 'java-patterns-l3-015',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Chain of Responsibility',
    question: 'How would you implement a Chain of Responsibility using Java functional programming?\n\nFunction<Request, Optional<Response>> handler1 = req -> {\n  if (canHandle(req)) return Optional.of(process(req));\n  return Optional.empty();\n};\n\nFunction<Request, Optional<Response>> chain = Stream.of(handler1, handler2, handler3)\n  .reduce((f, g) -> req -> f.apply(req).or(() -> g.apply(req)))\n  .orElse(req -> Optional.empty());',
    options: [
      'By composing functions using reduce(), where each function tries to handle the request and falls back to the next',
      'By creating a linked list of handler objects',
      'By using inheritance only',
      'This approach does not work',
    ],
    correctAnswer: 0,
    explanation: 'Functional Chain of Responsibility composes handler functions. Each function returns Optional — if it handles the request, it returns a result; otherwise empty, and the chain tries the next function. reduce() combines them into a single function.',
    codeSnippet: 'Function<Request, Optional<Response>> chain = Stream.of(handler1, handler2, handler3)\n  .reduce((f, g) -> req -> f.apply(req).or(() -> g.apply(req)))\n  .orElse(req -> Optional.empty());',
    tags: ['chain-of-responsibility', 'functional', 'stream'],
  },
  {
    id: 'java-patterns-l3-016',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can the Command pattern be used to implement a transaction with rollback?\n\npublic class TransactionManager {\n  private Deque<Command> executed = new ArrayDeque<>();\n  public void execute(Command cmd) {\n    cmd.execute();\n    executed.push(cmd);\n  }\n  public void rollback() {\n    while (!executed.isEmpty()) {\n      executed.pop().undo();\n    }\n  }\n}',
    options: [
      'By using database transactions directly',
      'By executing all commands and hoping they succeed',
      'By cloning objects before each command',
      'By maintaining a stack of executed commands and calling undo() on each in reverse order during rollback',
    ],
    correctAnswer: 3,
    explanation: 'A transaction manager stores executed commands on a stack. If rollback is needed, it pops each command and calls undo() in reverse execution order, unwinding the transaction to the original state.',
    codeSnippet: 'public void rollback() {\n  while (!executed.isEmpty()) {\n    executed.pop().undo();\n  }\n}',
    tags: ['command', 'transaction', 'rollback'],
  },
  {
    id: 'java-patterns-l3-017',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Iterator',
    question: 'How would you implement a lazy iterator that generates values on demand?\n\npublic class FibonacciIterator implements Iterator<Long> {\n  private long a = 0, b = 1;\n  public boolean hasNext() { return true; }\n  public Long next() {\n    long result = a;\n    long temp = a + b;\n    a = b;\n    b = temp;\n    return result;\n  }\n}',
    options: [
      'By precomputing all values into a list',
      'By using parallel streams',
      'By computing and returning each value only when next() is called, without storing all values in memory',
      'By caching all results',
    ],
    correctAnswer: 2,
    explanation: 'A lazy iterator computes values on-the-fly in next() without pre-computing or storing all values. The Fibonacci iterator only keeps two state variables and computes the next value when requested, using O(1) memory.',
    codeSnippet: 'public class FibonacciIterator implements Iterator<Long> {\n  private long a = 0, b = 1;\n  public boolean hasNext() { return true; }\n  public Long next() {\n    long result = a;\n    long temp = a + b; a = b; b = temp;\n    return result;\n  }\n}',
    tags: ['iterator', 'lazy', 'fibonacci'],
  },
  {
    id: 'java-patterns-l3-018',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'How does the Mediator pattern relate to the Spring IoC container?',
    options: [
      'Spring does not use the Mediator pattern',
      'The Mediator pattern was deprecated in Spring',
      'The Spring IoC container acts as a Mediator by managing dependencies between beans, so beans communicate through the container rather than directly',
      'Spring only uses the Factory pattern',
    ],
    correctAnswer: 2,
    explanation: 'Spring\'s IoC container acts as a Mediator: it manages the creation, wiring, and lifecycle of beans. Beans don\'t directly instantiate or look up their dependencies — the container mediates all relationships.',
    tags: ['mediator', 'spring', 'ioc'],
  },
  {
    id: 'java-patterns-l3-019',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Memento',
    question: 'How can you implement an efficient Memento for a large object by storing only incremental changes?',
    options: [
      'Store only the diff (changes) between the current and previous state in each memento, and reconstruct full state by applying diffs',
      'Always store the full state',
      'Store nothing and recalculate from scratch',
      'Use compression only',
    ],
    correctAnswer: 0,
    explanation: 'Incremental mementos store only the changes (diffs) rather than the full state. To restore, you start from a baseline and apply/undo diffs. This dramatically reduces memory usage for objects with large state but small changes.',
    tags: ['memento', 'incremental', 'optimization'],
  },
  {
    id: 'java-patterns-l3-020',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you prevent memory leaks in the Observer pattern?',
    options: [
      'Use WeakReferences for observers or ensure observers are explicitly unregistered when no longer needed',
      'Memory leaks cannot occur with Observer',
      'Call System.gc() regularly',
      'Use strong references everywhere',
    ],
    correctAnswer: 0,
    explanation: 'If a subject holds strong references to observers, observers cannot be garbage collected even when they are no longer needed. Using WeakReferences (e.g., WeakHashMap) or explicit deregistration prevents this common memory leak.',
    tags: ['observer', 'memory-leak', 'weak-reference'],
  },
  {
    id: 'java-patterns-l3-021',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How does the reactive streams specification (java.util.concurrent.Flow) improve upon the traditional Observer pattern?',
    options: [
      'It adds backpressure support, allowing subscribers to control the rate of data they receive',
      'It is exactly the same',
      'It removes the need for subjects',
      'It is slower but more readable',
    ],
    correctAnswer: 0,
    explanation: 'Reactive streams (Flow API) add backpressure via the Subscription.request(n) mechanism. Subscribers tell the publisher how many items they can handle, preventing overflow. Traditional Observer has no such flow control.',
    tags: ['observer', 'reactive-streams', 'backpressure'],
  },
  {
    id: 'java-patterns-l3-022',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you combine the State pattern with enums in Java for type-safe state machines?\n\npublic enum DoorState {\n  OPEN {\n    public DoorState close() { return CLOSED; }\n    public DoorState open() { return this; }\n  },\n  CLOSED {\n    public DoorState close() { return this; }\n    public DoorState open() { return OPEN; }\n  };\n  public abstract DoorState close();\n  public abstract DoorState open();\n}',
    options: [
      'By using switch statements',
      'By using abstract methods in the enum that each constant overrides, creating a compile-time-verified state machine',
      'By storing the state as a String',
      'Enums cannot implement state machines',
    ],
    correctAnswer: 1,
    explanation: 'Java enums can have abstract methods that each constant overrides. This creates a type-safe state machine where all states and transitions are verified at compile time. Missing transitions cause compilation errors.',
    codeSnippet: 'public enum DoorState {\n  OPEN {\n    public DoorState close() { return CLOSED; }\n    public DoorState open() { return this; }\n  },\n  CLOSED {\n    public DoorState close() { return this; }\n    public DoorState open() { return OPEN; }\n  };\n  public abstract DoorState close();\n  public abstract DoorState open();\n}',
    tags: ['state', 'enum', 'state-machine'],
  },
  {
    id: 'java-patterns-l3-023',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'How can you implement the Strategy pattern using Java enums with behavior?\n\npublic enum SortStrategy {\n  BUBBLE {\n    public <T extends Comparable<T>> void sort(List<T> list) { /* bubble sort */ }\n  },\n  QUICK {\n    public <T extends Comparable<T>> void sort(List<T> list) { /* quick sort */ }\n  },\n  MERGE {\n    public <T extends Comparable<T>> void sort(List<T> list) { /* merge sort */ }\n  };\n  public abstract <T extends Comparable<T>> void sort(List<T> list);\n}',
    options: [
      'Enums cannot be strategies',
      'This requires reflection',
      'Each enum constant implements the abstract strategy method, creating a fixed set of type-safe strategies',
      'This is actually the Template Method pattern',
    ],
    correctAnswer: 2,
    explanation: 'Java enums with abstract methods can serve as strategies. Each constant provides its own implementation. This approach is type-safe, prevents null strategies, and works well when the set of strategies is fixed and known at compile time.',
    codeSnippet: 'public enum SortStrategy {\n  BUBBLE { public <T extends Comparable<T>> void sort(List<T> list) { } },\n  QUICK { public <T extends Comparable<T>> void sort(List<T> list) { } };\n  public abstract <T extends Comparable<T>> void sort(List<T> list);\n}',
    tags: ['strategy', 'enum', 'type-safe'],
  },
  {
    id: 'java-patterns-l3-024',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Template Method',
    question: 'How does the Template Method pattern interact with the Hollywood Principle?',
    options: [
      'Template Method follows the Hollywood Principle: "Don\'t call us, we\'ll call you" — the base class calls subclass methods, not the other way around',
      'They are unrelated',
      'The Hollywood Principle requires the Strategy pattern instead',
      'The Hollywood Principle prevents Template Method from working',
    ],
    correctAnswer: 0,
    explanation: 'The Hollywood Principle states that high-level components call low-level components, not vice versa. In Template Method, the abstract base class (high-level) controls the algorithm and calls subclass methods (low-level) at the appropriate points.',
    tags: ['template-method', 'hollywood-principle', 'inversion-of-control'],
  },
  {
    id: 'java-patterns-l3-025',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'How can you implement a type-safe Visitor without the accept/visit double dispatch using sealed classes (Java 17+)?\n\nsealed interface Shape permits Circle, Rectangle {}\nrecord Circle(double radius) implements Shape {}\nrecord Rectangle(double w, double h) implements Shape {}\n\ndouble area(Shape s) {\n  return switch (s) {\n    case Circle c -> Math.PI * c.radius() * c.radius();\n    case Rectangle r -> r.w() * r.h();\n  };\n}',
    options: [
      'This is still the Visitor pattern',
      'Sealed classes cannot replace Visitor',
      'This only works with records',
      'Sealed classes with pattern matching provide exhaustive switch expressions, eliminating the need for the accept/visit mechanism',
    ],
    correctAnswer: 3,
    explanation: 'Sealed classes combined with pattern matching in switch expressions provide exhaustive type checking at compile time. The compiler ensures all subtypes are handled, making the traditional Visitor\'s double dispatch unnecessary.',
    codeSnippet: 'sealed interface Shape permits Circle, Rectangle {}\nrecord Circle(double radius) implements Shape {}\nrecord Rectangle(double w, double h) implements Shape {}\n\ndouble area(Shape s) {\n  return switch (s) {\n    case Circle c -> Math.PI * c.radius() * c.radius();\n    case Rectangle r -> r.w() * r.h();\n  };\n}',
    tags: ['visitor', 'sealed-classes', 'pattern-matching'],
  },
  {
    id: 'java-patterns-l3-026',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does the Open/Closed Principle apply to the Decorator pattern?',
    options: [
      'The Decorator violates OCP',
      'OCP requires using only inheritance',
      'Decorators extend behavior (open for extension) without modifying the original component (closed for modification)',
      'Decorators always modify the original class',
    ],
    correctAnswer: 2,
    explanation: 'The Decorator pattern is a direct implementation of OCP. New behavior is added by wrapping objects with decorators (open for extension), and the original component class remains unchanged (closed for modification).',
    tags: ['solid', 'ocp', 'decorator'],
  },
  {
    id: 'java-patterns-l3-027',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does the Interface Segregation Principle relate to the Adapter pattern?',
    options: [
      'Adapters must implement all methods of the adaptee',
      'An Adapter can implement a narrow interface (ISP) even if the adaptee has a wide interface, exposing only what clients need',
      'ISP prevents the use of Adapters',
      'They are unrelated',
    ],
    correctAnswer: 1,
    explanation: 'An Adapter can present a focused, minimal interface (following ISP) while internally delegating to a broader adaptee interface. This shields clients from methods they don\'t need, combining ISP with Adapter effectively.',
    tags: ['solid', 'isp', 'adapter'],
  },
  {
    id: 'java-patterns-l3-028',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Dependency Inversion',
    question: 'What is the difference between Dependency Inversion, Dependency Injection, and Inversion of Control?',
    options: [
      'DI is the principle; DIP is the technique; IoC is the pattern',
      'DIP is the principle; DI is the technique to achieve it; IoC is the broader design principle where framework controls the flow',
      'IoC is a pattern; DIP and DI are frameworks',
      'They are all the same concept',
    ],
    correctAnswer: 1,
    explanation: 'Dependency Inversion Principle (DIP) states that code should depend on abstractions. Dependency Injection (DI) is a technique to achieve DIP by injecting dependencies from outside. Inversion of Control (IoC) is the broader principle where a framework controls program flow.',
    tags: ['dependency-inversion', 'dependency-injection', 'ioc'],
  },
  {
    id: 'java-patterns-l3-029',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Repository Pattern',
    question: 'How does the Specification pattern complement the Repository pattern?\n\npublic interface Specification<T> {\n  boolean isSatisfiedBy(T entity);\n}\npublic class AgeSpecification implements Specification<User> {\n  private int minAge;\n  public boolean isSatisfiedBy(User user) {\n    return user.getAge() >= minAge;\n  }\n}',
    options: [
      'Specifications are only for validation',
      'Specifications replace repositories',
      'They are unrelated',
      'Specifications encapsulate query criteria that can be passed to repositories, enabling flexible and composable queries',
    ],
    correctAnswer: 3,
    explanation: 'The Specification pattern encapsulates business rules as objects that can be combined (AND, OR, NOT). When used with Repository, specifications define query criteria without coupling the repository to specific query logic.',
    codeSnippet: 'public interface Specification<T> {\n  boolean isSatisfiedBy(T entity);\n}\npublic class AgeSpecification implements Specification<User> {\n  public boolean isSatisfiedBy(User user) {\n    return user.getAge() >= minAge;\n  }\n}',
    tags: ['repository', 'specification', 'composable'],
  },
  {
    id: 'java-patterns-l3-030',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'DAO',
    question: 'How should a DAO handle database transactions?',
    options: [
      'Each DAO method should manage its own transaction',
      'Transaction management should be handled at a higher level (service layer) to allow multiple DAO operations within one transaction',
      'Transactions should be managed by the database only',
      'DAOs should never use transactions',
    ],
    correctAnswer: 1,
    explanation: 'Transaction management belongs in the service layer, not individual DAO methods. This allows multiple DAO calls to participate in a single transaction (e.g., transferring money requires debiting one account and crediting another atomically).',
    tags: ['dao', 'transaction', 'service-layer'],
  },
  {
    id: 'java-patterns-l3-031',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'DTO',
    question: 'How should mapping between entities and DTOs be handled in a layered architecture?',
    options: [
      'Use String conversion for all fields',
      'Let the database handle the mapping',
      'Entities and DTOs should be the same class',
      'Use dedicated mapper classes or libraries (MapStruct, ModelMapper) at the boundary between layers',
    ],
    correctAnswer: 3,
    explanation: 'Mapping should be done by dedicated mapper classes or libraries like MapStruct (compile-time code generation) or ModelMapper (reflection-based) at layer boundaries. This keeps each layer clean and the mapping logic centralized.',
    tags: ['dto', 'mapper', 'mapstruct'],
  },
  {
    id: 'java-patterns-l3-032',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'MVC',
    question: 'How does Spring MVC implement the Front Controller pattern alongside MVC?',
    options: [
      'Spring MVC does not use Front Controller',
      'The View acts as the Front Controller',
      'DispatcherServlet acts as the Front Controller that receives all requests and dispatches them to appropriate controllers',
      'Each controller is a Front Controller',
    ],
    correctAnswer: 2,
    explanation: 'Spring MVC\'s DispatcherServlet is a Front Controller — a single entry point that intercepts all HTTP requests. It consults handler mappings to dispatch requests to the appropriate @Controller, which follows the MVC pattern.',
    tags: ['mvc', 'spring', 'front-controller'],
  },
  {
    id: 'java-patterns-l3-033',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you implement the Decorator pattern using Java\'s functional interfaces?\n\nFunction<String, String> trim = String::trim;\nFunction<String, String> toUpper = String::toUpperCase;\nFunction<String, String> addBrackets = s -> "[" + s + "]";\n\nFunction<String, String> pipeline = trim.andThen(toUpper).andThen(addBrackets);',
    options: [
      'This is the Strategy pattern',
      'Function composition via andThen() is a functional form of the Decorator pattern, where each function wraps and extends the previous one',
      'This is not the Decorator pattern',
      'This is the Chain of Responsibility pattern',
    ],
    correctAnswer: 1,
    explanation: 'Function composition using andThen() is a functional Decorator. Each function "decorates" the previous by adding behavior. The pipeline trim -> toUpper -> addBrackets applies transformations in order, equivalent to nested decorators.',
    codeSnippet: 'Function<String, String> pipeline = trim.andThen(toUpper).andThen(addBrackets);\npipeline.apply("  hello  "); // "[HELLO]"',
    tags: ['decorator', 'functional', 'composition'],
  },
  {
    id: 'java-patterns-l3-034',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Facade',
    question: 'How should a Facade handle errors from the subsystem?',
    options: [
      'Log errors and return null',
      'Suppress all exceptions silently',
      'Catch subsystem-specific exceptions and translate them into higher-level, meaningful exceptions for the client',
      'Propagate all subsystem exceptions directly to the client',
    ],
    correctAnswer: 2,
    explanation: 'A well-designed Facade catches low-level subsystem exceptions and translates them into higher-level, domain-meaningful exceptions. This shields clients from subsystem details and provides better error information.',
    tags: ['facade', 'error-handling', 'exception-translation'],
  },
  {
    id: 'java-patterns-l3-035',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you implement an asynchronous Observer pattern?\n\npublic class AsyncEventBus {\n  private ExecutorService executor = Executors.newCachedThreadPool();\n  private List<EventListener> listeners = new CopyOnWriteArrayList<>();\n  \n  public void publish(Event event) {\n    for (EventListener listener : listeners) {\n      executor.submit(() -> listener.onEvent(event));\n    }\n  }\n}',
    options: [
      'By dispatching observer notifications on separate threads using an ExecutorService',
      'By using sleep() between notifications',
      'By sending email notifications',
      'Observer pattern must be synchronous',
    ],
    correctAnswer: 0,
    explanation: 'An async Observer dispatches notifications to separate threads via ExecutorService. CopyOnWriteArrayList ensures thread-safe iteration. Each observer is notified independently and concurrently, preventing slow observers from blocking others.',
    codeSnippet: 'public void publish(Event event) {\n  for (EventListener listener : listeners) {\n    executor.submit(() -> listener.onEvent(event));\n  }\n}',
    tags: ['observer', 'async', 'executor-service'],
  },
  {
    id: 'java-patterns-l3-036',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'How can you implement the Strategy pattern using a Map of functions for plugin-style extensibility?\n\nMap<String, Function<Order, Double>> discountStrategies = new HashMap<>();\ndiscountStrategies.put("VIP", order -> order.getTotal() * 0.2);\ndiscountStrategies.put("STUDENT", order -> order.getTotal() * 0.15);\ndiscountStrategies.put("SENIOR", order -> order.getTotal() * 0.1);\n\ndouble discount = discountStrategies.get(customerType).apply(order);',
    options: [
      'Maps cannot hold strategies',
      'This is the Command pattern',
      'This is the Flyweight pattern',
      'Store strategy functions in a Map keyed by identifiers, allowing dynamic lookup and runtime registration of new strategies',
    ],
    correctAnswer: 3,
    explanation: 'A Map of strategy functions provides lookup-based strategy selection. New strategies can be registered at runtime by adding entries to the map. This combines Strategy with a simple Registry, enabling plugin-style extensibility.',
    codeSnippet: 'Map<String, Function<Order, Double>> discountStrategies = new HashMap<>();\ndiscountStrategies.put("VIP", order -> order.getTotal() * 0.2);\ndouble discount = discountStrategies.get(customerType).apply(order);',
    tags: ['strategy', 'map', 'functional'],
  },
  {
    id: 'java-patterns-l3-037',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can the Command pattern implement event sourcing?',
    options: [
      'By storing only the final state',
      'By persisting every command (event) to a log and reconstructing state by replaying all commands in order',
      'By discarding all commands after execution',
      'By using database snapshots only',
    ],
    correctAnswer: 1,
    explanation: 'Event sourcing stores every state change as an immutable command/event. The current state is derived by replaying all events from the beginning (or from a snapshot). This provides a complete audit trail and enables temporal queries.',
    tags: ['command', 'event-sourcing', 'cqrs'],
  },
  {
    id: 'java-patterns-l3-038',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you prevent invalid state transitions in the State pattern?',
    options: [
      'Make all states transitions public',
      'Allow all transitions and handle errors at runtime',
      'Each state object only exposes valid transitions and throws IllegalStateException for invalid ones, or use a transition table',
      'Use a boolean flag to track validity',
    ],
    correctAnswer: 2,
    explanation: 'Invalid transitions can be prevented by: (1) each state only supporting valid transitions and throwing for invalid ones, (2) using a transition table that defines allowed transitions, or (3) using the type system (step builder approach) for compile-time safety.',
    tags: ['state', 'validation', 'transition-table'],
  },
  {
    id: 'java-patterns-l3-039',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How does CGLIB create proxies for concrete classes?\n\n// CGLIB example\nEnhancer enhancer = new Enhancer();\nenhancer.setSuperclass(UserService.class);\nenhancer.setCallback((MethodInterceptor) (obj, method, args, proxy) -> {\n  System.out.println("Before: " + method.getName());\n  Object result = proxy.invokeSuper(obj, args);\n  System.out.println("After: " + method.getName());\n  return result;\n});\nUserService proxy = (UserService) enhancer.create();',
    options: [
      'CGLIB uses Java\'s built-in Proxy class',
      'CGLIB generates a subclass of the target class at runtime using bytecode manipulation',
      'CGLIB wraps the class using composition',
      'CGLIB modifies the original class bytecode',
    ],
    correctAnswer: 1,
    explanation: 'CGLIB (Code Generation Library) creates a proxy by generating a subclass of the target class at runtime using bytecode generation (ASM library). It overrides methods to add interception logic. This is how Spring AOP proxies non-interface beans.',
    codeSnippet: 'Enhancer enhancer = new Enhancer();\nenhancer.setSuperclass(UserService.class);\nenhancer.setCallback((MethodInterceptor) (obj, method, args, proxy) -> {\n  Object result = proxy.invokeSuper(obj, args);\n  return result;\n});\nUserService proxy = (UserService) enhancer.create();',
    tags: ['proxy', 'cglib', 'bytecode'],
  },
  {
    id: 'java-patterns-l3-040',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Abstract Factory',
    question: 'How can you combine Abstract Factory with dependency injection in Spring?\n\n@Configuration\npublic class DatabaseConfig {\n  @Bean\n  @Profile("mysql")\n  public DatabaseFactory mysqlFactory() { return new MySQLFactory(); }\n  \n  @Bean\n  @Profile("postgres")\n  public DatabaseFactory postgresFactory() { return new PostgresFactory(); }\n}',
    options: [
      'Spring profiles select the concrete factory to inject, combining Abstract Factory with DI for environment-specific configuration',
      'This requires manual factory lookup',
      'Each factory must be a Singleton',
      'Abstract Factory and DI are incompatible',
    ],
    correctAnswer: 0,
    explanation: 'Spring profiles (or conditional annotations) select which concrete factory bean to create. The rest of the application depends on the abstract DatabaseFactory interface. Spring\'s DI wires the correct factory based on the active profile.',
    codeSnippet: '@Bean @Profile("mysql")\npublic DatabaseFactory mysqlFactory() { return new MySQLFactory(); }\n@Bean @Profile("postgres")\npublic DatabaseFactory postgresFactory() { return new PostgresFactory(); }',
    tags: ['abstract-factory', 'spring', 'dependency-injection'],
  },
  {
    id: 'java-patterns-l3-041',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'What is the Multiton pattern and how does it relate to Singleton?\n\npublic class Logger {\n  private static Map<String, Logger> instances = new ConcurrentHashMap<>();\n  private Logger() {}\n  public static Logger getInstance(String name) {\n    return instances.computeIfAbsent(name, k -> new Logger());\n  }\n}',
    options: [
      'Multiton is an anti-pattern that should never be used',
      'Multiton creates unlimited instances',
      'Multiton extends Singleton to manage a fixed set of named instances, one per key, instead of just one global instance',
      'Multiton is the same as Singleton',
    ],
    correctAnswer: 2,
    explanation: 'The Multiton pattern extends Singleton by managing a map of named instances. Instead of one global instance, it provides one instance per key. This is useful for pool-like scenarios where you need a fixed number of distinct instances.',
    codeSnippet: 'private static Map<String, Logger> instances = new ConcurrentHashMap<>();\npublic static Logger getInstance(String name) {\n  return instances.computeIfAbsent(name, k -> new Logger());\n}',
    tags: ['singleton', 'multiton', 'named-instances'],
  },
  {
    id: 'java-patterns-l3-042',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How can you combine the Builder pattern with validation to ensure only valid objects are created?\n\npublic User build() {\n  if (name == null || name.isEmpty()) {\n    throw new IllegalStateException("Name is required");\n  }\n  if (age < 0 || age > 150) {\n    throw new IllegalStateException("Invalid age: " + age);\n  }\n  if (email != null && !email.matches("^[A-Za-z0-9+_.-]+@(.+)$")) {\n    throw new IllegalStateException("Invalid email: " + email);\n  }\n  return new User(this);\n}',
    options: [
      'Validation is not needed with builders',
      'Validation should be in the constructor, not the builder',
      'Each setter should validate individually',
      'The build() method validates all accumulated state before creating the object, ensuring only valid objects are ever instantiated',
    ],
    correctAnswer: 3,
    explanation: 'Placing validation in build() ensures all fields are validated together (cross-field validation is possible) right before object creation. This makes it impossible to create an invalid object — the builder either produces a valid object or throws.',
    codeSnippet: 'public User build() {\n  if (name == null || name.isEmpty())\n    throw new IllegalStateException("Name is required");\n  if (age < 0 || age > 150)\n    throw new IllegalStateException("Invalid age");\n  return new User(this);\n}',
    tags: ['builder', 'validation', 'invariants'],
  },
  {
    id: 'java-patterns-l3-043',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How does the Flyweight pattern interact with immutability?',
    options: [
      'Flyweight objects are always primitive types',
      'Immutability is unrelated to Flyweight',
      'Flyweight objects should be immutable because shared mutable state would cause thread-safety issues and corruption',
      'Flyweight objects must be mutable',
    ],
    correctAnswer: 2,
    explanation: 'Flyweight objects store shared intrinsic state that must be immutable. If shared state were mutable, changes by one client would affect all others, causing bugs and thread-safety issues. Immutability ensures safe sharing.',
    tags: ['flyweight', 'immutability', 'thread-safety'],
  },
  {
    id: 'java-patterns-l3-044',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Chain of Responsibility',
    question: 'How does Spring Security implement the Chain of Responsibility pattern?',
    options: [
      'Spring Security does not use this pattern',
      'Spring Security uses a single monolithic filter',
      'Spring Security uses only the Strategy pattern',
      'Spring Security uses a FilterChain where each security filter (authentication, authorization, CSRF, etc.) processes the request and passes it to the next filter',
    ],
    correctAnswer: 3,
    explanation: 'Spring Security\'s FilterChainProxy contains an ordered list of security filters (UsernamePasswordAuthenticationFilter, CsrfFilter, etc.). Each filter processes the HTTP request and invokes chain.doFilter() to pass to the next, forming a Chain of Responsibility.',
    tags: ['chain-of-responsibility', 'spring-security', 'filter-chain'],
  },
  {
    id: 'java-patterns-l3-045',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Template Method',
    question: 'What are the downsides of the Template Method pattern compared to Strategy?',
    options: [
      'Strategy requires more classes',
      'Template Method cannot have abstract methods',
      'Template Method is always better than Strategy',
      'Template Method uses inheritance (tight coupling, fragile base class problem); Strategy uses composition (looser coupling, easier testing)',
    ],
    correctAnswer: 3,
    explanation: 'Template Method relies on inheritance, creating tight coupling between the base and subclasses (the fragile base class problem). Strategy uses composition, which is more flexible, easier to test (mock strategies), and allows runtime switching.',
    tags: ['template-method', 'strategy', 'inheritance-vs-composition'],
  },
  {
    id: 'java-patterns-l3-046',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'What is the risk of the Mediator pattern becoming a "God Object"?',
    options: [
      'Mediators are always small and simple',
      'As more colleagues and interactions are added, the mediator can grow excessively complex, concentrating too much logic in one class',
      'God Objects only occur with Singleton',
      'Mediators prevent God Objects by definition',
    ],
    correctAnswer: 1,
    explanation: 'The Mediator can become a God Object if it accumulates too much coordination logic. As the number of colleagues and interaction types grows, the mediator becomes a maintenance bottleneck. Mitigation: decompose into sub-mediators or use events.',
    tags: ['mediator', 'god-object', 'anti-pattern'],
  },
  {
    id: 'java-patterns-l3-047',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Memento',
    question: 'How can you implement Memento using serialization for automatic state capture?\n\npublic class Memento implements Serializable {\n  private byte[] state;\n  public Memento(Serializable obj) throws IOException {\n    ByteArrayOutputStream bos = new ByteArrayOutputStream();\n    new ObjectOutputStream(bos).writeObject(obj);\n    this.state = bos.toByteArray();\n  }\n  public Object restore() throws Exception {\n    return new ObjectInputStream(\n      new ByteArrayInputStream(state)).readObject();\n  }\n}',
    options: [
      'Serialize the originator to a byte array for capture and deserialize it for restoration, providing automatic deep-copy memento',
      'This requires the Prototype pattern',
      'Serialization cannot be used for Memento',
      'This only works for primitive fields',
    ],
    correctAnswer: 0,
    explanation: 'Serialization-based Memento automatically captures the entire object graph by serializing to bytes. Restoration deserializes the bytes back to an object. This provides deep-copy semantics without manually tracking individual fields.',
    codeSnippet: 'public Memento(Serializable obj) throws IOException {\n  ByteArrayOutputStream bos = new ByteArrayOutputStream();\n  new ObjectOutputStream(bos).writeObject(obj);\n  this.state = bos.toByteArray();\n}',
    tags: ['memento', 'serialization', 'deep-copy'],
  },
  {
    id: 'java-patterns-l3-048',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'What is the "Expression Problem" and how does it relate to the Visitor pattern?',
    options: [
      'The Expression Problem only applies to functional languages',
      'It is the tension between easily adding new types (hard for Visitor) and easily adding new operations (easy for Visitor)',
      'The Expression Problem is about parsing expressions',
      'Visitor solves the Expression Problem completely',
    ],
    correctAnswer: 1,
    explanation: 'The Expression Problem is the challenge of extending both the set of types and the set of operations without modifying existing code. Visitor makes it easy to add operations but hard to add types. The opposite approach (adding types) makes it hard to add operations.',
    tags: ['visitor', 'expression-problem', 'extensibility'],
  },
  {
    id: 'java-patterns-l3-049',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does the Single Responsibility Principle apply to design patterns themselves?',
    options: [
      'Patterns do not need to follow SRP',
      'All patterns have exactly one responsibility by definition',
      'Each pattern should address one specific concern; combining too many responsibilities into one pattern creates complexity',
      'SRP only applies to classes, not patterns',
    ],
    correctAnswer: 2,
    explanation: 'SRP applies to patterns too. Each pattern addresses a specific concern: Singleton manages instance count, Factory manages creation, Observer manages notification. Combining multiple concerns into one class (even with a pattern) violates SRP.',
    tags: ['solid', 'srp', 'pattern-responsibility'],
  },
  {
    id: 'java-patterns-l3-050',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Adapter',
    question: 'How can you implement a two-way adapter that adapts in both directions?\n\npublic class TwoWayAdapter implements OldSystem, NewSystem {\n  private OldSystem oldSystem;\n  private NewSystem newSystem;\n  \n  public TwoWayAdapter(OldSystem old) { this.oldSystem = old; }\n  public TwoWayAdapter(NewSystem nw) { this.newSystem = nw; }\n  \n  // OldSystem methods delegate to newSystem\n  // NewSystem methods delegate to oldSystem\n}',
    options: [
      'By implementing both interfaces and delegating in both directions based on which underlying object was provided',
      'By creating two separate adapters',
      'Two-way adapters are impossible',
      'By using only inheritance',
    ],
    correctAnswer: 0,
    explanation: 'A two-way adapter implements both interfaces. When used as OldSystem, it delegates to the NewSystem adaptee, and vice versa. This enables legacy and modern code to use each other through a single adapter.',
    codeSnippet: 'public class TwoWayAdapter implements OldSystem, NewSystem {\n  private OldSystem oldSystem;\n  private NewSystem newSystem;\n  // Delegates in both directions\n}',
    tags: ['adapter', 'two-way', 'bidirectional'],
  },
  {
    id: 'java-patterns-l3-051',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'Why does Spring AOP\'s proxy-based approach fail when a bean method calls another method on the same bean?',
    options: [
      'Self-calls bypass the proxy because "this" refers to the actual bean, not the proxy, so aspects are not applied',
      'Spring prevents self-calls at compile time',
      'Self-calls cause a StackOverflowError',
      'Spring AOP works perfectly with self-calls',
    ],
    correctAnswer: 0,
    explanation: 'Spring AOP wraps beans in proxies that intercept external calls. When a method calls another method via "this", it bypasses the proxy entirely, so any aspects (e.g., @Transactional) on the called method are not applied. Solutions include self-injection or AspectJ weaving.',
    tags: ['proxy', 'spring-aop', 'self-invocation'],
  },
  {
    id: 'java-patterns-l3-052',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you use Java annotations and a wrapper to implement a declarative Decorator?\n\n@Logged\n@Cached\n@Retryable(maxAttempts = 3)\npublic String fetchData(String key) { ... }',
    options: [
      'Annotations replace the method body',
      'Annotations cannot be used as decorators',
      'Each annotation creates a new class at compile time',
      'Annotations mark the method, and a framework (like Spring AOP) creates dynamic proxy decorators that add logging, caching, and retry behavior',
    ],
    correctAnswer: 3,
    explanation: 'Annotations serve as declarative markers. A framework (like Spring AOP) reads them at runtime and creates proxy-based decorators that wrap the method with the specified behavior (logging, caching, retry). This is decoration via metadata.',
    codeSnippet: '@Logged\n@Cached\n@Retryable(maxAttempts = 3)\npublic String fetchData(String key) { ... }',
    tags: ['decorator', 'annotations', 'spring-aop'],
  },
  {
    id: 'java-patterns-l3-053',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Factory Method',
    question: 'How does Java\'s ServiceLoader implement a form of the Factory pattern?\n\nServiceLoader<Plugin> loader = ServiceLoader.load(Plugin.class);\nfor (Plugin plugin : loader) {\n  plugin.execute();\n}',
    options: [
      'ServiceLoader discovers and instantiates implementations of an interface declared in META-INF/services, acting as a service factory',
      'ServiceLoader only loads classes, not instances',
      'ServiceLoader requires Spring',
      'ServiceLoader is not related to Factory',
    ],
    correctAnswer: 0,
    explanation: 'ServiceLoader implements a factory that discovers and instantiates service implementations at runtime. Implementations are registered in META-INF/services files. This is a form of the Abstract Factory pattern for plugin architectures.',
    codeSnippet: 'ServiceLoader<Plugin> loader = ServiceLoader.load(Plugin.class);\nfor (Plugin plugin : loader) {\n  plugin.execute();\n}',
    tags: ['factory-method', 'service-loader', 'plugin'],
  },
  {
    id: 'java-patterns-l3-054',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How can you implement the Composite pattern with type safety using generics?\n\npublic interface Expression {\n  int evaluate();\n}\npublic record Number(int value) implements Expression {\n  public int evaluate() { return value; }\n}\npublic record Add(Expression left, Expression right) implements Expression {\n  public int evaluate() { return left.evaluate() + right.evaluate(); }\n}',
    options: [
      'This is the Interpreter pattern only',
      'Expression is the component; Number is the leaf; Add is the composite that holds and operates on child expressions',
      'This is not the Composite pattern',
      'Records cannot be used in Composite',
    ],
    correctAnswer: 1,
    explanation: 'This is a Composite for arithmetic expressions. Expression is the component interface. Number is a leaf (no children). Add is a composite that holds two child expressions. evaluate() is uniformly called on both leaves and composites.',
    codeSnippet: 'public record Number(int value) implements Expression {\n  public int evaluate() { return value; }\n}\npublic record Add(Expression left, Expression right) implements Expression {\n  public int evaluate() { return left.evaluate() + right.evaluate(); }\n}',
    tags: ['composite', 'expression-tree', 'records'],
  },
  {
    id: 'java-patterns-l3-055',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How can the Liskov Substitution Principle be enforced through design-by-contract?',
    options: [
      'Subtypes can weaken postconditions freely',
      'Subtypes can strengthen preconditions freely',
      'Design by contract is unrelated to LSP',
      'Subtypes must maintain preconditions (no stronger), postconditions (no weaker), and invariants of the supertype',
    ],
    correctAnswer: 3,
    explanation: 'LSP via design-by-contract requires that subtypes: (1) do not strengthen preconditions (accept at least what the supertype does), (2) do not weaken postconditions (guarantee at least what the supertype does), and (3) maintain all invariants of the supertype.',
    tags: ['solid', 'lsp', 'design-by-contract'],
  },
  {
    id: 'java-patterns-l3-056',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Singleton',
    question: 'How do dependency injection frameworks like Spring effectively replace the Singleton pattern?',
    options: [
      'Spring beans are singleton-scoped by default, but managed by the container rather than by the class itself, improving testability',
      'Spring does not provide singleton behavior',
      'Spring uses the Multiton pattern exclusively',
      'Spring creates new instances every time',
    ],
    correctAnswer: 0,
    explanation: 'Spring beans are singleton-scoped by default (one instance per application context). Unlike the traditional Singleton pattern, the class itself is not responsible for enforcing single-instance behavior — the container manages it, making the class easier to test and configure.',
    tags: ['singleton', 'spring', 'dependency-injection'],
  },
  {
    id: 'java-patterns-l3-057',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can the Command pattern be combined with the Composite pattern for macro commands?\n\npublic class MacroCommand implements Command {\n  private List<Command> commands;\n  public MacroCommand(List<Command> commands) {\n    this.commands = commands;\n  }\n  public void execute() {\n    for (Command cmd : commands) cmd.execute();\n  }\n  public void undo() {\n    ListIterator<Command> it = commands.listIterator(commands.size());\n    while (it.hasPrevious()) it.previous().undo();\n  }\n}',
    options: [
      'This only works for two commands',
      'The undo must be in the same order as execute',
      'MacroCommand is both a Command (implements execute/undo) and a Composite (contains a list of Commands), enabling batch execution and batch undo in reverse order',
      'Command and Composite cannot be combined',
    ],
    correctAnswer: 2,
    explanation: 'MacroCommand combines Command and Composite patterns. It implements Command (is-a) and contains a list of Commands (has-children). Execute runs all commands in order; undo reverses through them, maintaining correct rollback semantics.',
    codeSnippet: 'public void undo() {\n  ListIterator<Command> it = commands.listIterator(commands.size());\n  while (it.hasPrevious()) it.previous().undo();\n}',
    tags: ['command', 'composite', 'macro-command'],
  },
  {
    id: 'java-patterns-l3-058',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Iterator',
    question: 'How does Java\'s Spliterator improve upon Iterator for parallel processing?',
    options: [
      'Spliterator works only with arrays',
      'Spliterator does not support parallel processing',
      'Spliterator is slower than Iterator',
      'Spliterator can split its elements into two halves (trySplit) for parallel processing, and provides characteristics and size estimation',
    ],
    correctAnswer: 3,
    explanation: 'Spliterator (introduced in Java 8) supports parallel iteration via trySplit(), which divides elements for processing by multiple threads. It also provides characteristics (ORDERED, SIZED, etc.) and estimateSize() for optimization.',
    tags: ['iterator', 'spliterator', 'parallel'],
  },
  {
    id: 'java-patterns-l3-059',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Adapter',
    question: 'How can method references act as adapters in Java functional programming?\n\nlist.forEach(System.out::println);',
    options: [
      'This is the Observer pattern',
      'System.out::println adapts the PrintStream.println(Object) method to the Consumer<T> functional interface expected by forEach',
      'Method references are not adapters',
      'This creates a new PrintStream',
    ],
    correctAnswer: 1,
    explanation: 'System.out::println is an adapter that converts PrintStream\'s println method signature to the Consumer<T> functional interface expected by forEach. Method references are the most concise form of adapter in Java.',
    codeSnippet: 'list.forEach(System.out::println);',
    tags: ['adapter', 'method-reference', 'functional'],
  },
  {
    id: 'java-patterns-l3-060',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Bridge',
    question: 'How does the JDBC API exemplify the Bridge pattern?',
    options: [
      'JDBC does not use the Bridge pattern',
      'JDBC uses the Factory pattern exclusively',
      'JDBC uses only the Adapter pattern',
      'JDBC\'s Driver/Connection/Statement interfaces (abstraction) are separated from vendor-specific implementations (implementation), allowing database switching without code changes',
    ],
    correctAnswer: 3,
    explanation: 'JDBC is a Bridge pattern: the API interfaces (Connection, Statement, ResultSet) form the abstraction, while each database vendor provides its own implementations. Client code uses the abstraction and can switch databases by changing the driver.',
    tags: ['bridge', 'jdbc', 'real-world'],
  },
  {
    id: 'java-patterns-l3-061',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Facade',
    question: 'How can you design a Facade that is backward-compatible when the subsystem evolves?',
    options: [
      'Version the Facade API, provide default implementations for new methods, and deprecate old ones gradually',
      'Create a new Facade for every change',
      'Never change the Facade',
      'Let clients call subsystem classes directly',
    ],
    correctAnswer: 0,
    explanation: 'Backward-compatible Facades can version their API (v1, v2), use Java default methods for backward-compatible interface evolution, and use the deprecation mechanism to gradually migrate clients from old to new methods.',
    tags: ['facade', 'backward-compatible', 'api-evolution'],
  },
  {
    id: 'java-patterns-l3-062',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you implement type-safe events in the Observer pattern using generics?\n\npublic interface EventHandler<T extends Event> {\n  void handle(T event);\n}\npublic class EventBus {\n  private Map<Class<?>, List<EventHandler<?>>> handlers = new HashMap<>();\n  \n  public <T extends Event> void subscribe(Class<T> type, EventHandler<T> handler) {\n    handlers.computeIfAbsent(type, k -> new ArrayList<>()).add(handler);\n  }\n  \n  @SuppressWarnings("unchecked")\n  public <T extends Event> void publish(T event) {\n    List<EventHandler<?>> list = handlers.get(event.getClass());\n    if (list != null) {\n      for (EventHandler<?> h : list) ((EventHandler<T>) h).handle(event);\n    }\n  }\n}',
    options: [
      'By parameterizing handlers with the event type, ensuring handlers only receive events they can process',
      'Generics cannot be used with Observer',
      'This introduces runtime type errors',
      'This requires separate EventBus instances per event type',
    ],
    correctAnswer: 0,
    explanation: 'Generic EventHandler<T> ensures type safety at the subscription point. The EventBus routes events to the correct handlers based on event class. The unchecked cast is safe because subscribe() guarantees type correspondence.',
    codeSnippet: 'public <T extends Event> void subscribe(Class<T> type, EventHandler<T> handler) {\n  handlers.computeIfAbsent(type, k -> new ArrayList<>()).add(handler);\n}',
    tags: ['observer', 'generics', 'type-safe-events'],
  },
  {
    id: 'java-patterns-l3-063',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you share state objects between multiple contexts in the State pattern to save memory?',
    options: [
      'Each context must have its own state objects',
      'State objects must always be unique',
      'Use global variables for states',
      'If state objects have no context-specific data (stateless), they can be shared as singletons or flyweights across contexts',
    ],
    correctAnswer: 3,
    explanation: 'When state objects have no instance-specific data (they are effectively stateless), they can be shared among multiple contexts using the Flyweight pattern. Each state becomes a singleton-like shared object, reducing memory usage.',
    tags: ['state', 'flyweight', 'sharing'],
  },
  {
    id: 'java-patterns-l3-064',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'What is the difference between the Strategy pattern and the Policy pattern?',
    options: [
      'Policy is a structural pattern; Strategy is behavioral',
      'They are essentially the same pattern; "Policy" is an alternative name for Strategy in some contexts (e.g., C++ STL)',
      'They are completely different patterns',
      'Policy applies only to security',
    ],
    correctAnswer: 1,
    explanation: 'Policy and Strategy are essentially the same pattern. "Policy" is the term used in C++ template-based designs (policy-based design), while "Strategy" is the GoF term. Both encapsulate interchangeable algorithms.',
    tags: ['strategy', 'policy', 'naming'],
  },
  {
    id: 'java-patterns-l3-065',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Prototype',
    question: 'How would you implement a copy factory method as an alternative to clone()?\n\npublic class Shape {\n  private int x, y;\n  private String color;\n  \n  // Copy factory method\n  public static Shape copyOf(Shape source) {\n    Shape copy = new Shape();\n    copy.x = source.x;\n    copy.y = source.y;\n    copy.color = source.color;\n    return copy;\n  }\n}',
    options: [
      'Copy factory methods are not valid alternatives',
      'A static factory method that takes an instance and creates a new instance with the same state, without requiring Cloneable',
      'This is a regular constructor, not a factory',
      'This requires serialization',
    ],
    correctAnswer: 1,
    explanation: 'Copy factory methods (or copy constructors) are a cleaner alternative to clone(). They use the normal constructor, allow type-specific copying, handle deep copy explicitly, and don\'t require the Cloneable interface or checked exceptions.',
    codeSnippet: 'public static Shape copyOf(Shape source) {\n  Shape copy = new Shape();\n  copy.x = source.x;\n  copy.y = source.y;\n  copy.color = source.color;\n  return copy;\n}',
    tags: ['prototype', 'copy-factory', 'alternative'],
  },
  {
    id: 'java-patterns-l3-066',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Composite',
    question: 'How can you implement caching of computed values in the Composite pattern?\n\npublic class CachedDirectory implements FileSystemItem {\n  private List<FileSystemItem> children = new ArrayList<>();\n  private Integer cachedSize = null;\n  \n  public int getSize() {\n    if (cachedSize == null) {\n      cachedSize = children.stream().mapToInt(FileSystemItem::getSize).sum();\n    }\n    return cachedSize;\n  }\n  public void add(FileSystemItem item) {\n    children.add(item);\n    invalidateCache();\n  }\n  private void invalidateCache() { cachedSize = null; }\n}',
    options: [
      'Only leaf nodes should cache',
      'Caching is not possible in Composite',
      'Caching violates the Composite pattern',
      'Cache computed values (like total size) in composite nodes and invalidate the cache when children are modified',
    ],
    correctAnswer: 3,
    explanation: 'Composite nodes can cache aggregate computations (like total size). The cache is invalidated when children are added, removed, or modified. This avoids recomputing expensive aggregations on every access, especially for deep trees.',
    codeSnippet: 'public void add(FileSystemItem item) {\n  children.add(item);\n  invalidateCache();\n}',
    tags: ['composite', 'caching', 'invalidation'],
  },
  {
    id: 'java-patterns-l3-067',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How does the Interface Segregation Principle interact with Java\'s default methods?\n\npublic interface DataSource {\n  List<Record> read();\n  default void write(List<Record> records) {\n    throw new UnsupportedOperationException("Read-only source");\n  }\n}',
    options: [
      'Default methods can partially mitigate ISP issues but throwing UnsupportedOperationException still violates ISP — prefer separate ReadableDataSource and WritableDataSource interfaces',
      'This code follows ISP perfectly',
      'Default methods always satisfy ISP',
      'Default methods make ISP unnecessary',
    ],
    correctAnswer: 0,
    explanation: 'Default methods that throw UnsupportedOperationException are a code smell indicating ISP violation. The interface should be split: ReadableDataSource with read() and WritableDataSource with write(). Clients depend only on what they need.',
    codeSnippet: 'default void write(List<Record> records) {\n  throw new UnsupportedOperationException("Read-only source");\n}',
    tags: ['solid', 'isp', 'default-methods'],
  },
  {
    id: 'java-patterns-l3-068',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Dependency Inversion',
    question: 'What are the three forms of Dependency Injection?',
    options: [
      'Static, dynamic, and hybrid injection',
      'Field injection, method injection, and property injection',
      'Class injection, object injection, and module injection',
      'Constructor injection, setter injection, and interface injection',
    ],
    correctAnswer: 3,
    explanation: 'The three forms of DI are: (1) Constructor injection (dependencies provided through constructor), (2) Setter injection (via setter methods), and (3) Interface injection (the dependency provides an injector method). Constructor injection is generally preferred for required dependencies.',
    tags: ['dependency-inversion', 'injection-types', 'constructor'],
  },
  {
    id: 'java-patterns-l3-069',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Repository Pattern',
    question: 'How does Spring Data JPA implement the Repository pattern automatically?\n\npublic interface UserRepository extends JpaRepository<User, Long> {\n  List<User> findByAgeGreaterThan(int age);\n  Optional<User> findByEmail(String email);\n}',
    options: [
      'It uses reflection to call database directly',
      'It uses code generation at compile time',
      'Spring generates a dynamic proxy at runtime that implements the interface by deriving queries from method names',
      'It requires manual implementation of every method',
    ],
    correctAnswer: 2,
    explanation: 'Spring Data JPA creates a dynamic proxy that implements the repository interface at runtime. It parses method names (findByAgeGreaterThan) and generates JPQL queries automatically. Standard CRUD methods come from JpaRepository.',
    codeSnippet: 'public interface UserRepository extends JpaRepository<User, Long> {\n  List<User> findByAgeGreaterThan(int age);\n  Optional<User> findByEmail(String email);\n}',
    tags: ['repository', 'spring-data', 'dynamic-proxy'],
  },
  {
    id: 'java-patterns-l3-070',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Flyweight',
    question: 'How can the Flyweight pattern be applied in game development for rendering a forest with millions of trees?',
    options: [
      'Share tree type objects (mesh, texture, color) as flyweights; store per-tree position and age as extrinsic state',
      'Render only visible trees',
      'Create a unique object for each tree',
      'Use a single tree model for all trees',
    ],
    correctAnswer: 0,
    explanation: 'In a forest renderer, TreeType (mesh, texture, bark color) is intrinsic shared state — there might be 5 types shared among millions of trees. Each tree\'s position, age, and height are extrinsic state stored separately, dramatically reducing memory.',
    tags: ['flyweight', 'game-development', 'forest'],
  },
  {
    id: 'java-patterns-l3-071',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Builder',
    question: 'How does Java\'s Stream API use the Builder pattern internally?\n\nStream.Builder<String> builder = Stream.builder();\nbuilder.add("a").add("b").add("c");\nStream<String> stream = builder.build();',
    options: [
      'This is the Iterator pattern',
      'Stream does not use Builder',
      'Stream.Builder accumulates elements via add() and materializes a Stream via build(), following the Builder pattern',
      'This is the Factory pattern',
    ],
    correctAnswer: 2,
    explanation: 'Stream.Builder follows the Builder pattern: elements are accumulated via add() (fluent API), and build() creates the final immutable Stream. After build(), the builder cannot be reused, ensuring the stream is properly constructed.',
    codeSnippet: 'Stream.Builder<String> builder = Stream.builder();\nbuilder.add("a").add("b").add("c");\nStream<String> stream = builder.build();',
    tags: ['builder', 'stream-api', 'java8'],
  },
  {
    id: 'java-patterns-l3-072',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'How can you test decorated objects effectively?',
    options: [
      'Test each decorator in isolation by wrapping a mock component, verifying that the decorator adds its behavior and delegates correctly',
      'Testing decorators is impossible',
      'Test only the outermost decorator',
      'Skip testing decorators',
    ],
    correctAnswer: 0,
    explanation: 'Each decorator should be tested in isolation: wrap a mock/stub component and verify the decorator adds its specific behavior while correctly delegating to the wrapped component. Integration tests can verify the full decorator chain.',
    tags: ['decorator', 'testing', 'mocking'],
  },
  {
    id: 'java-patterns-l3-073',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Command',
    question: 'How can the Command pattern be used with Java\'s CompletableFuture for asynchronous execution?',
    options: [
      'Command and CompletableFuture are incompatible',
      'Commands can return CompletableFuture from execute(), enabling async processing, chaining, and composition of results',
      'You must use Thread.sleep() instead',
      'CompletableFuture replaces the Command pattern entirely',
    ],
    correctAnswer: 1,
    explanation: 'Commands can return CompletableFuture<Result> from execute(), enabling non-blocking execution. Results can be chained (thenApply), combined (thenCombine), and error-handled (exceptionally). This merges the Command pattern with async programming.',
    tags: ['command', 'completable-future', 'async'],
  },
  {
    id: 'java-patterns-l3-074',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Mediator',
    question: 'How does the Event Bus (e.g., Guava EventBus) implement the Mediator pattern?\n\nEventBus bus = new EventBus();\nbus.register(new OrderEventHandler());\nbus.post(new OrderCreatedEvent(orderId));',
    options: [
      'EventBus acts as a Mediator that decouples event publishers from subscribers; publishers post events without knowing who handles them',
      'EventBus is an Observer only',
      'EventBus requires direct references between components',
      'EventBus is not a Mediator',
    ],
    correctAnswer: 0,
    explanation: 'EventBus is a Mediator that decouples event publishers from subscribers. Components post events to the bus and subscribe via annotations. Neither side knows about the other — the bus mediates all communication.',
    codeSnippet: 'EventBus bus = new EventBus();\nbus.register(new OrderEventHandler());\nbus.post(new OrderCreatedEvent(orderId));',
    tags: ['mediator', 'event-bus', 'guava'],
  },
  {
    id: 'java-patterns-l3-075',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'DTO',
    question: 'What are the trade-offs between using fine-grained DTOs vs. coarse-grained DTOs?',
    options: [
      'Coarse-grained is always better',
      'Fine-grained DTOs (many small ones) allow precise data transfer but increase complexity; coarse-grained (few large ones) are simpler but may over-fetch data',
      'There are no trade-offs',
      'Fine-grained is always better',
    ],
    correctAnswer: 1,
    explanation: 'Fine-grained DTOs transfer only needed data (good for bandwidth) but create many classes. Coarse-grained DTOs are simpler (fewer classes) but may include unnecessary data. The choice depends on use case — API endpoints often need fine-grained; internal layers may use coarse-grained.',
    tags: ['dto', 'granularity', 'trade-offs'],
  },
  {
    id: 'java-patterns-l3-076',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'What is the Acyclic Dependencies Principle and how does it relate to SOLID?',
    options: [
      'It states that the dependency graph of packages/modules should have no cycles; it extends DIP by ensuring clean dependency hierarchies',
      'It is about database dependencies',
      'It is one of the SOLID principles',
      'It only applies to circular references in code',
    ],
    correctAnswer: 0,
    explanation: 'The Acyclic Dependencies Principle (ADP) states that there should be no circular dependencies between packages/modules. It extends the Dependency Inversion Principle by ensuring the overall architecture forms a directed acyclic graph (DAG).',
    tags: ['solid', 'acyclic-dependencies', 'architecture'],
  },
  {
    id: 'java-patterns-l3-077',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Proxy',
    question: 'How can the Proxy pattern implement caching (Caching Proxy)?\n\npublic class CachingProxy implements DataService {\n  private DataService realService;\n  private Map<String, Data> cache = new ConcurrentHashMap<>();\n  private Duration ttl;\n  \n  public Data getData(String key) {\n    return cache.computeIfAbsent(key, k -> realService.getData(k));\n  }\n}',
    options: [
      'Caching should be in the real service, not the proxy',
      'The proxy intercepts calls, checks the cache first, and only delegates to the real service on a cache miss',
      'The proxy always delegates to the real service',
      'Caching Proxy is not a real pattern',
    ],
    correctAnswer: 1,
    explanation: 'A Caching Proxy intercepts method calls and checks a local cache first. On a cache miss, it delegates to the real service and stores the result. This adds caching transparently without modifying the original service.',
    codeSnippet: 'public Data getData(String key) {\n  return cache.computeIfAbsent(key, k -> realService.getData(k));\n}',
    tags: ['proxy', 'caching', 'transparent'],
  },
  {
    id: 'java-patterns-l3-078',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Strategy',
    question: 'How can you implement a fallback strategy chain?\n\npublic class ResilientFetcher {\n  private List<FetchStrategy> strategies;\n  \n  public Data fetch(String key) {\n    for (FetchStrategy strategy : strategies) {\n      try {\n        return strategy.fetch(key);\n      } catch (Exception e) {\n        // try next strategy\n      }\n    }\n    throw new RuntimeException("All strategies failed");\n  }\n}',
    options: [
      'This combines Strategy and Chain of Responsibility: multiple strategies are tried in order until one succeeds',
      'This is the Template Method pattern',
      'This is Chain of Responsibility, not Strategy',
      'Fallback strategies are an anti-pattern',
    ],
    correctAnswer: 0,
    explanation: 'This is a hybrid of Strategy and Chain of Responsibility. Multiple strategies (e.g., cache, database, remote API) are tried in order. If one fails, the next is tried. This provides resilience through fallback mechanisms.',
    codeSnippet: 'for (FetchStrategy strategy : strategies) {\n  try { return strategy.fetch(key); }\n  catch (Exception e) { /* try next */ }\n}\nthrow new RuntimeException("All strategies failed");',
    tags: ['strategy', 'chain-of-responsibility', 'fallback'],
  },
  {
    id: 'java-patterns-l3-079',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Observer',
    question: 'How can you prevent notification ordering issues in the Observer pattern?',
    options: [
      'Always use random order',
      'Notify only one observer',
      'Ordering is never an issue',
      'Use a priority-based notification system, topological sorting of observers, or make observer order explicit in the subscription',
    ],
    correctAnswer: 3,
    explanation: 'When notification order matters, use: (1) priority-based observers (subscribe with a priority number), (2) topological ordering (if observers have dependencies), or (3) explicit ordered registration. The default unordered approach can cause subtle bugs.',
    tags: ['observer', 'ordering', 'priority'],
  },
  {
    id: 'java-patterns-l3-080',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'State',
    question: 'How can you combine the State pattern with the Template Method pattern?\n\npublic abstract class OrderState {\n  public final void process(Order order) {\n    validate(order);\n    execute(order);\n    transition(order);\n  }\n  protected abstract void validate(Order order);\n  protected abstract void execute(Order order);\n  protected abstract void transition(Order order);\n}',
    options: [
      'This is only the Template Method pattern',
      'This is only the State pattern',
      'These patterns cannot be combined',
      'Each state class uses Template Method to define a processing algorithm with validate/execute/transition steps, ensuring consistent state handling',
    ],
    correctAnswer: 3,
    explanation: 'Combining State with Template Method: each state class follows a standard processing algorithm (validate, execute, transition) defined by the template method. Concrete states customize each step while maintaining the overall processing structure.',
    codeSnippet: 'public final void process(Order order) {\n  validate(order);\n  execute(order);\n  transition(order);\n}',
    tags: ['state', 'template-method', 'combination'],
  },
  {
    id: 'java-patterns-l3-081',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Abstract Factory',
    question: 'How can you implement an Abstract Factory using Java\'s Service Provider Interface (SPI)?',
    options: [
      'SPI requires Spring',
      'Define the factory as a service interface, implement it in separate modules, and use ServiceLoader to discover and load the appropriate factory at runtime',
      'SPI cannot be used with Abstract Factory',
      'SPI is only for logging',
    ],
    correctAnswer: 1,
    explanation: 'The factory interface is declared as a service. Each module provides a factory implementation and registers it in META-INF/services. At runtime, ServiceLoader discovers available factories, creating a pluggable Abstract Factory system.',
    tags: ['abstract-factory', 'spi', 'service-loader'],
  },
  {
    id: 'java-patterns-l3-082',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Factory Method',
    question: 'How does the Factory Method pattern interact with Java generics for type-safe creation?\n\npublic interface Converter<I, O> {\n  O convert(I input);\n}\npublic interface ConverterFactory<S, R> {\n  <T extends R> Converter<S, T> getConverter(Class<T> targetType);\n}',
    options: [
      'Generics are only for collections',
      'This is the Strategy pattern',
      'Generics allow the factory to return type-safe converters based on the target type, eliminating runtime casts',
      'Generics make Factory Method unusable',
    ],
    correctAnswer: 2,
    explanation: 'The generic ConverterFactory returns a Converter parameterized with the exact target type. The caller gets Converter<String, Integer> (not Converter<String, Object>), achieving compile-time type safety without casts.',
    codeSnippet: 'public interface ConverterFactory<S, R> {\n  <T extends R> Converter<S, T> getConverter(Class<T> targetType);\n}',
    tags: ['factory-method', 'generics', 'type-safe'],
  },
  {
    id: 'java-patterns-l3-083',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Decorator',
    question: 'What is the "transparent enclosure" principle in the Decorator pattern?',
    options: [
      'Transparent enclosure means using generics',
      'A decorator should be interchangeable with the component it wraps — clients should not need to know whether they are dealing with a decorated or undecorated component',
      'Decorators must be invisible to the client',
      'Decorators must encrypt data',
    ],
    correctAnswer: 1,
    explanation: 'Transparent enclosure means a decorated component should be indistinguishable from an undecorated one. The decorator preserves the component\'s interface contract and only adds behavior, so clients work with both uniformly.',
    tags: ['decorator', 'transparent-enclosure', 'principle'],
  },
  {
    id: 'java-patterns-l3-084',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Chain of Responsibility',
    question: 'How can you implement a middleware pipeline using the Chain of Responsibility pattern?\n\npublic interface Middleware {\n  Response handle(Request req, MiddlewareChain chain);\n}\npublic class MiddlewareChain {\n  private Iterator<Middleware> iterator;\n  public Response next(Request req) {\n    if (iterator.hasNext()) {\n      return iterator.next().handle(req, this);\n    }\n    return finalHandler.handle(req);\n  }\n}',
    options: [
      'Middleware does not use Chain of Responsibility',
      'Each middleware decides whether to process the request, optionally modify it, and call chain.next() to pass to the next middleware',
      'Middleware requires the Decorator pattern',
      'All middleware must be executed in any order',
    ],
    correctAnswer: 1,
    explanation: 'The middleware pipeline is a Chain of Responsibility where each middleware processes the request, can modify it, and calls chain.next() to pass it along. A middleware can short-circuit by returning a response without calling next().',
    codeSnippet: 'public interface Middleware {\n  Response handle(Request req, MiddlewareChain chain);\n}',
    tags: ['chain-of-responsibility', 'middleware', 'pipeline'],
  },
  {
    id: 'java-patterns-l3-085',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Visitor',
    question: 'How can you implement the Visitor pattern using functional programming in Java?\n\npublic interface ShapeVisitor<R> {\n  R visitCircle(Circle c);\n  R visitRectangle(Rectangle r);\n}\n\n// Functional visitor using lambdas:\nShapeVisitor<Double> areaVisitor = new ShapeVisitor<>() {\n  public Double visitCircle(Circle c) { return Math.PI * c.r() * c.r(); }\n  public Double visitRectangle(Rectangle r) { return r.w() * r.h(); }\n};',
    options: [
      'Functional Visitor requires sealed classes',
      'The visitor interface can use generics for the return type, and concrete visitors can be created inline using anonymous classes or lambdas',
      'Visitor cannot be functional',
      'This is the Strategy pattern',
    ],
    correctAnswer: 1,
    explanation: 'A functional Visitor uses a generic return type R and can be instantiated inline. With sealed classes and pattern matching (Java 17+), the Visitor can be further simplified to a switch expression, making the pattern nearly invisible.',
    tags: ['visitor', 'functional', 'generics'],
  },
  {
    id: 'java-patterns-l3-086',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'Memento',
    question: 'How can you implement undo/redo using two stacks of mementos?\n\npublic class History {\n  private Deque<Memento> undoStack = new ArrayDeque<>();\n  private Deque<Memento> redoStack = new ArrayDeque<>();\n  \n  public void save(Memento m) {\n    undoStack.push(m);\n    redoStack.clear();\n  }\n  public Memento undo() {\n    Memento m = undoStack.pop();\n    redoStack.push(m);\n    return undoStack.peek();\n  }\n  public Memento redo() {\n    Memento m = redoStack.pop();\n    undoStack.push(m);\n    return m;\n  }\n}',
    options: [
      'The undo stack holds past states; undo pops from it and pushes to the redo stack; redo reverses the process; new actions clear the redo stack',
      'Two stacks are unnecessary for undo/redo',
      'The redo stack holds future predictions',
      'Both stacks hold the same data',
    ],
    correctAnswer: 0,
    explanation: 'Two stacks implement undo/redo: undo pops the current state from the undo stack and pushes it to the redo stack. Redo does the reverse. New actions (save) clear the redo stack because the redo history is no longer valid.',
    codeSnippet: 'public void save(Memento m) {\n  undoStack.push(m);\n  redoStack.clear();\n}\npublic Memento undo() {\n  Memento m = undoStack.pop();\n  redoStack.push(m);\n  return undoStack.peek();\n}',
    tags: ['memento', 'undo-redo', 'two-stacks'],
  },
  {
    id: 'java-patterns-l3-087',
    language: 'java',
    level: 'level3',
    category: 'Design Patterns',
    subcategory: 'SOLID Principles',
    question: 'How do the SOLID principles collectively contribute to reducing technical debt?',
    options: [
      'SRP keeps classes focused; OCP reduces modification risk; LSP ensures safe polymorphism; ISP prevents bloated interfaces; DIP enables flexible architecture — together they create maintainable, extensible code',
      'Only SRP reduces technical debt',
      'SOLID principles increase technical debt due to more classes',
      'SOLID has no impact on technical debt',
    ],
    correctAnswer: 0,
    explanation: 'SOLID principles synergistically reduce technical debt: SRP creates focused classes, OCP enables safe extensions, LSP ensures reliable inheritance, ISP keeps interfaces lean, and DIP decouples architecture. The result is code that is easier to maintain, test, and extend.',
    tags: ['solid', 'technical-debt', 'summary'],
  },
];

export default questions;
