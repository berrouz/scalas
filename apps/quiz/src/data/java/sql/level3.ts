import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'java-sql-l3-001',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Window Functions',
    question: 'What is the difference between ROWS and RANGE in a window frame specification?',
    options: ['ROWS counts physical rows, RANGE considers logical value ranges', 'ROWS is for numbers, RANGE is for dates', 'They are identical', 'RANGE counts physical rows, ROWS considers logical values'],
    correctAnswer: 0,
    explanation: 'ROWS defines the frame based on the physical number of rows relative to the current row. RANGE defines the frame based on the logical value range, grouping rows with the same ORDER BY value together. RANGE is the default.',
    codeSnippet: '-- ROWS: exactly 2 preceding physical rows\nSUM(salary) OVER (ORDER BY hire_date\n  ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)\n\n-- RANGE: all rows with same or lower hire_date value\nSUM(salary) OVER (ORDER BY hire_date\n  RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)',
    tags: ['sql', 'window-functions', 'rows', 'range', 'frame'],
  },
  {
    id: 'java-sql-l3-002',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Window Functions',
    question: 'What does NTILE(n) do?',
    options: ['Splits the result into n equal-sized pages', 'Tiles data across n tables', 'Distributes rows into n approximately equal buckets', 'Returns every nth row'],
    correctAnswer: 2,
    explanation: 'NTILE(n) distributes rows in an ordered partition into n approximately equal-sized groups (buckets), numbered from 1 to n. Useful for creating percentiles, quartiles, or deciles.',
    codeSnippet: 'SELECT name, salary,\n  NTILE(4) OVER (ORDER BY salary DESC) AS quartile\nFROM employees;',
    tags: ['sql', 'window-functions', 'ntile'],
  },
  {
    id: 'java-sql-l3-003',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Window Functions',
    question: 'What do FIRST_VALUE() and LAST_VALUE() do?',
    options: ['Return the first and last values within the window frame', 'Return the minimum and maximum values', 'Return the first and last row of the table', 'Return the first and last column values'],
    correctAnswer: 0,
    explanation: 'FIRST_VALUE() and LAST_VALUE() return the first and last values in the window frame respectively. Note that LAST_VALUE with default frame (RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) only sees up to the current row.',
    codeSnippet: 'SELECT name, salary,\n  FIRST_VALUE(name) OVER (\n    PARTITION BY dept ORDER BY salary DESC\n    ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n  ) AS highest_paid\nFROM employees;',
    tags: ['sql', 'window-functions', 'first-value', 'last-value'],
  },
  {
    id: 'java-sql-l3-004',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the B-Tree index structure and why is it preferred for databases?',
    options: ['A balanced tree with high branching factor that minimizes disk I/O for lookups', 'A binary tree used for sorting', 'A hash-based tree structure', 'A binary search tree stored on disk'],
    correctAnswer: 0,
    explanation: 'B-Tree (balanced tree) indexes use a high branching factor to keep the tree height small (typically 3-4 levels), minimizing disk I/O. They support range queries, equality lookups, and sorting efficiently.',
    tags: ['sql', 'index', 'b-tree', 'internals'],
  },
  {
    id: 'java-sql-l3-005',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is a hash index and when is it better than a B-Tree index?',
    options: ['A hash index is always better', 'A hash index supports range queries better', 'Hash indexes and B-Tree indexes are identical in performance', 'A hash index provides O(1) equality lookups but does not support range queries'],
    correctAnswer: 3,
    explanation: 'Hash indexes use a hash function for O(1) average-case equality lookups, faster than B-Tree for exact matches. However, they cannot perform range scans, prefix searches, or ordering, making B-Tree more versatile.',
    tags: ['sql', 'index', 'hash', 'comparison'],
  },
  {
    id: 'java-sql-l3-006',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is index selectivity and why does it matter?',
    options: ['How the database selects which index to use', 'The ratio of distinct values to total rows; higher selectivity means more efficient index usage', 'How many indexes a table can have', 'The order of columns in a composite index'],
    correctAnswer: 1,
    explanation: 'Index selectivity = number of distinct values / total rows. A selectivity of 1.0 (all unique) is ideal. Low selectivity (e.g., boolean column) means the index is less useful because it does not significantly reduce the number of rows to scan.',
    tags: ['sql', 'index', 'selectivity', 'performance'],
  },
  {
    id: 'java-sql-l3-007',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the leftmost prefix rule for composite indexes?',
    options: ['A composite index on (a, b, c) can be used for queries on (a), (a, b), or (a, b, c) but not (b) or (c) alone', 'Left columns have higher priority', 'The first column must always be queried first', 'The index is sorted by the leftmost column only'],
    correctAnswer: 0,
    explanation: 'A composite index on (a, b, c) can efficiently serve queries that filter on the leftmost prefix: (a), (a, b), or (a, b, c). It cannot efficiently serve queries on (b) alone or (b, c) because the index is ordered by (a) first.',
    tags: ['sql', 'index', 'composite', 'leftmost-prefix'],
  },
  {
    id: 'java-sql-l3-008',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is an EXPLAIN plan and how is it used?',
    options: ['A tool that shows the query execution plan including index usage, join strategy, and estimated costs', 'A way to explain SQL syntax', 'A debugging tool for stored procedures', 'A documentation generator'],
    correctAnswer: 0,
    explanation: 'EXPLAIN (or EXPLAIN ANALYZE) shows how the database plans to execute a query, including which indexes are used, join algorithms, estimated row counts, and costs. It is essential for query performance tuning.',
    codeSnippet: 'EXPLAIN ANALYZE\nSELECT e.name, d.name\nFROM employees e\nJOIN departments d ON e.dept_id = d.id\nWHERE e.salary > 50000;',
    tags: ['sql', 'explain', 'query-plan', 'performance'],
  },
  {
    id: 'java-sql-l3-009',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Isolation Levels',
    question: 'How does PostgreSQL implement MVCC (Multi-Version Concurrency Control)?',
    options: ['By locking all rows', 'By using optimistic locking exclusively', 'By serializing all transactions', 'By maintaining multiple versions of each row so readers never block writers'],
    correctAnswer: 3,
    explanation: 'MVCC keeps multiple versions of each row. Each transaction sees a snapshot of the data as it was at the start of the transaction. Readers never block writers and writers never block readers, improving concurrency.',
    tags: ['sql', 'mvcc', 'concurrency', 'postgresql'],
  },
  {
    id: 'java-sql-l3-010',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Isolation Levels',
    question: 'What is Snapshot Isolation and how does it differ from Serializable?',
    options: ['Snapshot Isolation is only used in NoSQL databases', 'They are identical', 'Serializable is less strict than Snapshot Isolation', 'Snapshot Isolation uses MVCC to provide a consistent view but may allow write skew anomalies that Serializable prevents'],
    correctAnswer: 3,
    explanation: 'Snapshot Isolation gives each transaction a consistent snapshot of the database at transaction start. It prevents dirty reads, non-repeatable reads, and phantom reads, but allows write skew anomalies. True Serializable prevents all anomalies.',
    tags: ['sql', 'snapshot-isolation', 'serializable', 'isolation-levels'],
  },
  {
    id: 'java-sql-l3-011',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Isolation Levels',
    question: 'What is a write skew anomaly?',
    options: ['Skipping write operations', 'Writing data in the wrong order', 'Writing to the wrong table', 'Two concurrent transactions read overlapping data and make writes based on stale reads, violating a constraint that neither violated individually'],
    correctAnswer: 3,
    explanation: 'Write skew occurs when two transactions read overlapping data, each makes a decision based on what it read, and both commit. Individually each is correct, but together they violate a constraint. Example: two doctors both go off-call when at least one must remain.',
    tags: ['sql', 'write-skew', 'isolation-levels', 'anomaly'],
  },
  {
    id: 'java-sql-l3-012',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is a distributed transaction and how is it managed in Java?',
    options: ['A transaction that runs on multiple threads', 'A long-running transaction', 'A transaction spanning multiple databases/resources, managed by JTA and the two-phase commit protocol', 'A transaction across multiple tables'],
    correctAnswer: 2,
    explanation: 'A distributed transaction spans multiple resources (databases, message queues). Java Transaction API (JTA) manages them using the two-phase commit (2PC) protocol: phase 1 prepares all resources, phase 2 commits or rolls back.',
    tags: ['transactions', 'distributed', 'jta', '2pc'],
  },
  {
    id: 'java-sql-l3-013',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What are the problems with the two-phase commit protocol?',
    options: ['It is a blocking protocol that can lead to resource locking if the coordinator fails', 'It does not guarantee atomicity', 'It is too simple', 'It is not supported by any database'],
    correctAnswer: 0,
    explanation: 'Two-phase commit (2PC) is a blocking protocol: if the coordinator crashes between prepare and commit phases, participants are stuck holding locks. It also has performance overhead due to multiple round trips and synchronous coordination.',
    tags: ['transactions', '2pc', 'distributed', 'problems'],
  },
  {
    id: 'java-sql-l3-014',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the Saga pattern as an alternative to distributed transactions?',
    options: ['A connection pooling strategy', 'A pattern that breaks a distributed transaction into local transactions with compensating actions for rollback', 'A design pattern for database migrations', 'A caching pattern'],
    correctAnswer: 1,
    explanation: 'The Saga pattern replaces a single distributed transaction with a sequence of local transactions. Each step has a compensating transaction that undoes its work if a later step fails. It provides eventual consistency without distributed locking.',
    tags: ['transactions', 'saga', 'distributed', 'pattern'],
  },
  {
    id: 'java-sql-l3-015',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'N+1 Problem',
    question: 'What is the Cartesian product problem with multiple JOIN FETCH?',
    options: ['Fetching multiple collection associations simultaneously creates a Cartesian product explosion', 'It returns no results', 'It causes a syntax error', 'The results are duplicated'],
    correctAnswer: 0,
    explanation: 'If you JOIN FETCH two collection associations in one query (e.g., department.employees and department.projects), the result is a Cartesian product of both collections. Use @BatchSize or separate queries instead.',
    tags: ['jpa', 'n+1', 'cartesian-product', 'join-fetch'],
  },
  {
    id: 'java-sql-l3-016',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'N+1 Problem',
    question: 'How does @BatchSize work in Hibernate to solve N+1?',
    options: ['It sets the JDBC fetch size', 'It limits the number of entities returned', 'It loads associated entities in batches using IN clauses instead of one at a time', 'It enables batch insert operations'],
    correctAnswer: 2,
    explanation: '@BatchSize(size = 25) tells Hibernate to load lazy associations in batches. Instead of N individual queries, it generates queries with IN clauses containing up to 25 IDs each, significantly reducing query count.',
    codeSnippet: '@OneToMany(mappedBy = "department")\n@BatchSize(size = 25)\nprivate List<Employee> employees;\n// Instead of N queries, loads in ceil(N/25) batches:\n// SELECT * FROM employees WHERE dept_id IN (?, ?, ..., ?)',
    tags: ['hibernate', 'batch-size', 'n+1', 'performance'],
  },
  {
    id: 'java-sql-l3-017',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the LazyInitializationException in Hibernate?',
    options: ['An error during entity creation', 'A transaction timeout exception', 'An error in lazy configuration', 'An exception thrown when accessing a lazy-loaded association after the Session is closed'],
    correctAnswer: 3,
    explanation: 'LazyInitializationException is thrown when code tries to access a lazily-loaded association after the Hibernate Session that loaded the parent entity has been closed. Solutions include JOIN FETCH, @EntityGraph, or Open Session in View.',
    tags: ['hibernate', 'lazy-initialization', 'exception', 'session'],
  },
  {
    id: 'java-sql-l3-018',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is Open Session in View (OSIV) and why is it controversial?',
    options: ['Keeping the Hibernate Session open during view rendering to avoid LazyInitializationException', 'A way to open multiple sessions', 'A caching strategy', 'A security feature'],
    correctAnswer: 0,
    explanation: 'OSIV keeps the Session open until the HTTP response is rendered, allowing lazy loading in views. It is controversial because it can hide N+1 problems, trigger unexpected queries during rendering, and hold database connections longer than necessary.',
    tags: ['hibernate', 'osiv', 'anti-pattern', 'session'],
  },
  {
    id: 'java-sql-l3-019',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What are JPA inheritance mapping strategies?',
    options: ['FLAT, HIERARCHICAL, RELATIONAL', 'SINGLE, MULTIPLE, HYBRID', 'SINGLE_TABLE, TABLE_PER_CLASS, JOINED', 'POLYMORPHIC, ABSTRACT, CONCRETE'],
    correctAnswer: 2,
    explanation: 'JPA supports three inheritance strategies: SINGLE_TABLE (all subclasses in one table with a discriminator), TABLE_PER_CLASS (each concrete class has its own table), and JOINED (each class has its own table, joined by PK/FK).',
    codeSnippet: '@Entity\n@Inheritance(strategy = InheritanceType.SINGLE_TABLE)\n@DiscriminatorColumn(name = "vehicle_type")\npublic abstract class Vehicle { ... }\n\n@Entity\n@DiscriminatorValue("CAR")\npublic class Car extends Vehicle { ... }',
    tags: ['jpa', 'inheritance', 'mapping-strategy'],
  },
  {
    id: 'java-sql-l3-020',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What are the tradeoffs of SINGLE_TABLE inheritance in JPA?',
    options: ['Best for queries but worst for storage', 'Best query performance (no joins) but nullable columns and no NOT NULL constraints on subclass fields', 'Worst performance but best data integrity', 'Identical to JOINED strategy'],
    correctAnswer: 1,
    explanation: 'SINGLE_TABLE stores all entities in one table using a discriminator column. Pros: fastest queries (no joins), simple. Cons: subclass-specific columns must be nullable, cannot enforce NOT NULL on subclass fields, table can become wide.',
    tags: ['jpa', 'inheritance', 'single-table', 'tradeoffs'],
  },
  {
    id: 'java-sql-l3-021',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the @Embeddable and @Embedded approach in JPA?',
    options: ['A way to compose value objects into entities without a separate table', 'A way to embed SQL in Java', 'A way to embed views in entities', 'An inheritance strategy'],
    correctAnswer: 0,
    explanation: '@Embeddable marks a class as a value object whose fields will be embedded into the owning entity\'s table. @Embedded is used in the entity to include the embeddable. This avoids creating a separate table for the value object.',
    codeSnippet: '@Embeddable\npublic class Address {\n  private String street;\n  private String city;\n  private String zipCode;\n}\n\n@Entity\npublic class Employee {\n  @Id private Long id;\n  @Embedded\n  private Address address; // columns in employees table\n}',
    tags: ['jpa', 'embeddable', 'embedded', 'value-object'],
  },
  {
    id: 'java-sql-l3-022',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is Hibernate\'s query plan cache?',
    options: ['A cache for database query results', 'A cache that stores parsed and compiled HQL/JPQL query plans to avoid re-parsing', 'A cache for connection plans', 'A cache for native SQL queries'],
    correctAnswer: 1,
    explanation: 'Hibernate caches the parsed AST and compiled SQL for HQL/JPQL queries. The query plan cache avoids re-parsing queries on repeated executions. Its size is configurable via hibernate.query.plan_cache_max_size.',
    tags: ['hibernate', 'query-plan-cache', 'performance'],
  },
  {
    id: 'java-sql-l3-023',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the difference between Hibernate\'s save() and persist()?',
    options: ['persist() returns the ID; save() returns void', 'save() returns the generated ID and works outside a transaction; persist() returns void and requires a transaction', 'They are completely identical', 'save() is for updates; persist() is for inserts'],
    correctAnswer: 1,
    explanation: 'save() is Hibernate-specific, returns the generated Serializable ID, and can execute an INSERT immediately outside a transaction. persist() is JPA standard, returns void, and guarantees the INSERT is deferred within a transaction.',
    tags: ['hibernate', 'save', 'persist', 'comparison'],
  },
  {
    id: 'java-sql-l3-024',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'How does HikariCP achieve high performance compared to other pools?',
    options: ['By using more connections', 'By caching all query results', 'Through ConcurrentBag for lock-free collection management, bytecode-level optimizations, and minimal overhead', 'By using native database protocols'],
    correctAnswer: 2,
    explanation: 'HikariCP uses a custom lock-free ConcurrentBag collection, avoids unnecessary locks, uses bytecode-level engineering (e.g., invokevirtual instead of invokestatic), and generates optimized proxy classes at compile time.',
    tags: ['hikaricp', 'performance', 'internals'],
  },
  {
    id: 'java-sql-l3-025',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'What is the maxLifetime setting in HikariCP?',
    options: ['The maximum time a connection can exist before being retired, to prevent stale connections', 'The maximum time the pool exists', 'The maximum query execution time', 'The maximum number of lifetime connections'],
    correctAnswer: 0,
    explanation: 'maxLifetime controls the maximum lifetime of a connection in the pool. Connections are retired after this time to prevent issues with database-side timeouts or firewall rules that close idle connections. It should be set shorter than the database wait_timeout.',
    tags: ['hikaricp', 'max-lifetime', 'configuration'],
  },
  {
    id: 'java-sql-l3-026',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Injection',
    question: 'How can SQL injection occur through ORDER BY clauses?',
    options: ['Only if using PreparedStatement', 'It cannot occur in ORDER BY', 'ORDER BY cannot use parameterized values, so dynamic column names must be whitelisted', 'ORDER BY automatically prevents injection'],
    correctAnswer: 2,
    explanation: 'PreparedStatement placeholders (?) cannot be used for identifiers like column names in ORDER BY. If sorting column is user-controlled, it must be validated against a whitelist of allowed column names to prevent injection.',
    codeSnippet: '// VULNERABLE - column name from user input\nString sql = "SELECT * FROM users ORDER BY " + userInput;\n\n// SAFE - whitelist approach\nSet<String> allowedCols = Set.of("name", "email", "created_at");\nif (!allowedCols.contains(sortColumn)) {\n  throw new IllegalArgumentException("Invalid sort column");\n}',
    tags: ['sql-injection', 'order-by', 'whitelist', 'security'],
  },
  {
    id: 'java-sql-l3-027',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Injection',
    question: 'What is second-order SQL injection?',
    options: ['A duplicate SQL injection attack', 'Injection through a second database', 'SQL injection using two queries', 'Malicious input stored in the database and later used unsafely in a different query'],
    correctAnswer: 3,
    explanation: 'Second-order SQL injection occurs when malicious input is safely stored in the database (e.g., via PreparedStatement) but is later retrieved and used in a dynamically constructed SQL query without parameterization.',
    tags: ['sql-injection', 'second-order', 'security'],
  },
  {
    id: 'java-sql-l3-028',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Stored Procedures',
    question: 'How do you call a stored procedure with OUT parameters in JDBC?',
    options: ['Use CallableStatement with registerOutParameter()', 'OUT parameters are not supported in JDBC', 'Use PreparedStatement with output markers', 'Use Statement with output capture'],
    correctAnswer: 0,
    explanation: 'CallableStatement.registerOutParameter() registers an OUT parameter with its SQL type. After execution, the value is retrieved using the appropriate getter method.',
    codeSnippet: 'CallableStatement cs = conn.prepareCall(\n  "{call get_employee_count(?, ?)}"\n);\ncs.setInt(1, deptId);            // IN param\ncs.registerOutParameter(2, Types.INTEGER); // OUT param\ncs.execute();\nint count = cs.getInt(2);         // get OUT value',
    tags: ['jdbc', 'callable-statement', 'out-parameter'],
  },
  {
    id: 'java-sql-l3-029',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Stored Procedures',
    question: 'What are the advantages and disadvantages of stored procedures?',
    options: ['Always better than application-level SQL', 'Pros: reduced network traffic, precompiled; Cons: harder to debug, version, test, and may cause vendor lock-in', 'Cons only: slow and outdated', 'No advantages in modern applications'],
    correctAnswer: 1,
    explanation: 'Pros: reduce network round trips, precompiled for performance, enforce business rules at DB level. Cons: harder to debug and test, business logic split between app and DB, database vendor lock-in, harder to version control.',
    tags: ['sql', 'stored-procedure', 'tradeoffs'],
  },
  {
    id: 'java-sql-l3-030',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Views',
    question: 'What is a materialized view?',
    options: ['A view that physically stores the query result and must be refreshed to reflect changes', 'A view with extra formatting', 'A view with parameters', 'A temporary view'],
    correctAnswer: 0,
    explanation: 'A materialized view stores the result of its query physically on disk, unlike regular views which re-execute their query each time. It provides fast reads but must be refreshed (manually or on a schedule) to reflect underlying data changes.',
    codeSnippet: '-- PostgreSQL example\nCREATE MATERIALIZED VIEW monthly_sales AS\nSELECT product_id, SUM(amount) AS total\nFROM orders\nGROUP BY product_id;\n\n-- Must be refreshed to see new data\nREFRESH MATERIALIZED VIEW monthly_sales;',
    tags: ['sql', 'materialized-view', 'performance'],
  },
  {
    id: 'java-sql-l3-031',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is a recursive CTE?',
    options: ['A CTE that references itself to process hierarchical or graph data', 'A CTE that calls other CTEs', 'A CTE with multiple WITH clauses', 'A CTE that runs repeatedly'],
    correctAnswer: 0,
    explanation: 'A recursive CTE references itself in its definition. It has an anchor member (base case) and a recursive member joined with UNION ALL. It is used for hierarchical data like org charts, bill of materials, or graph traversal.',
    codeSnippet: 'WITH RECURSIVE org_chart AS (\n  -- Anchor: top-level managers\n  SELECT id, name, manager_id, 1 AS level\n  FROM employees WHERE manager_id IS NULL\n  UNION ALL\n  -- Recursive: find reports\n  SELECT e.id, e.name, e.manager_id, oc.level + 1\n  FROM employees e\n  JOIN org_chart oc ON e.manager_id = oc.id\n)\nSELECT * FROM org_chart;',
    tags: ['sql', 'cte', 'recursive', 'hierarchical'],
  },
  {
    id: 'java-sql-l3-032',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What are lateral joins (CROSS APPLY / LATERAL)?',
    options: ['A join where the right side can reference columns from the left side for each row', 'A horizontal join between tables', 'Joins between tables in different schemas', 'An alternative name for CROSS JOIN'],
    correctAnswer: 0,
    explanation: 'A LATERAL join allows the subquery on the right side to reference columns from the left side of the join for each row. This enables correlated subqueries in the FROM clause, which is useful for top-N-per-group queries.',
    codeSnippet: 'SELECT d.name, e.name, e.salary\nFROM departments d\nCROSS JOIN LATERAL (\n  SELECT name, salary FROM employees\n  WHERE dept_id = d.id\n  ORDER BY salary DESC LIMIT 3\n) e;',
    tags: ['sql', 'lateral-join', 'cross-apply'],
  },
  {
    id: 'java-sql-l3-033',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between INTERSECT and EXCEPT in SQL?',
    options: ['EXCEPT returns common rows; INTERSECT returns different rows', 'They are the same', 'They are types of joins', 'INTERSECT returns rows common to both queries; EXCEPT returns rows in the first query but not the second'],
    correctAnswer: 3,
    explanation: 'INTERSECT returns only rows that appear in both result sets. EXCEPT (or MINUS in Oracle) returns rows from the first query that do not appear in the second query. Both remove duplicates by default.',
    tags: ['sql', 'set-operations', 'intersect', 'except'],
  },
  {
    id: 'java-sql-l3-034',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Normalization',
    question: 'What is Boyce-Codd Normal Form (BCNF)?',
    options: ['A stricter version of 3NF where every determinant must be a candidate key', 'A form of denormalization', 'The same as 3NF', 'The most relaxed normal form'],
    correctAnswer: 0,
    explanation: 'BCNF is stricter than 3NF. A table is in BCNF if for every functional dependency X -> Y, X is a superkey. 3NF allows non-trivial dependencies where Y is part of a candidate key, but BCNF does not.',
    tags: ['sql', 'normalization', 'bcnf'],
  },
  {
    id: 'java-sql-l3-035',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the difference between EntityManager.find() and EntityManager.getReference()?',
    options: ['find() eagerly loads the entity; getReference() returns a lazy proxy and may throw EntityNotFoundException', 'find() only works with primary keys', 'getReference() is faster because it skips the cache', 'They are identical'],
    correctAnswer: 0,
    explanation: 'find() immediately hits the database (or cache) and returns the entity or null. getReference() returns a lazy proxy without hitting the database; it throws EntityNotFoundException when a property is first accessed if the entity does not exist.',
    tags: ['jpa', 'entity-manager', 'find', 'get-reference'],
  },
  {
    id: 'java-sql-l3-036',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the purpose of @NamedQuery in JPA?',
    options: ['To name database tables', 'To create named stored procedures', 'To alias column names', 'To define static JPQL queries at compile time for validation and potential pre-compilation'],
    correctAnswer: 3,
    explanation: '@NamedQuery defines a static JPQL query on an entity class. The query is validated at deployment time (catching syntax errors early) and may be precompiled for performance. It promotes reuse and centralized query management.',
    codeSnippet: '@Entity\n@NamedQuery(\n  name = "Employee.findByDept",\n  query = "SELECT e FROM Employee e WHERE e.department = :dept"\n)\npublic class Employee { ... }\n\n// Usage\nList<Employee> emps = em.createNamedQuery(\n  "Employee.findByDept", Employee.class\n).setParameter("dept", dept).getResultList();',
    tags: ['jpa', 'named-query', 'jpql'],
  },
  {
    id: 'java-sql-l3-037',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the @Converter annotation used for in JPA?',
    options: ['Converting between a Java type and a database column type', 'Converting entities to DTOs', 'Converting SQL to JPQL', 'Converting between database vendors'],
    correctAnswer: 0,
    explanation: '@Converter with AttributeConverter<X, Y> allows automatic conversion between a Java type and its database representation. Useful for enums, value objects, encrypted fields, or JSON storage.',
    codeSnippet: '@Converter(autoApply = true)\npublic class MoneyConverter\n    implements AttributeConverter<Money, BigDecimal> {\n  @Override\n  public BigDecimal convertToDatabaseColumn(Money m) {\n    return m.getAmount();\n  }\n  @Override\n  public Money convertToEntityAttribute(BigDecimal v) {\n    return new Money(v);\n  }\n}',
    tags: ['jpa', 'converter', 'attribute-converter'],
  },
  {
    id: 'java-sql-l3-038',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is Hibernate\'s StatelessSession?',
    options: ['A session for read-only operations', 'A lightweight session without first-level cache, dirty checking, or cascading; ideal for bulk operations', 'A session without transactions', 'A session that cannot track state'],
    correctAnswer: 1,
    explanation: 'StatelessSession bypasses the first-level cache, dirty checking, and interceptors. It does not manage entity state. This makes it ideal for bulk operations where the overhead of the persistence context is not needed.',
    codeSnippet: 'StatelessSession session = sessionFactory.openStatelessSession();\nTransaction tx = session.beginTransaction();\nScrollableResults results = session.createQuery(\n  "FROM Employee").scroll();\nwhile (results.next()) {\n  Employee e = (Employee) results.get(0);\n  e.setSalary(e.getSalary() * 1.1);\n  session.update(e);\n}\ntx.commit();\nsession.close();',
    tags: ['hibernate', 'stateless-session', 'bulk'],
  },
  {
    id: 'java-sql-l3-039',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate handle the N+1 problem with @Fetch(FetchMode.SUBSELECT)?',
    options: ['It joins all associations', 'It loads all associated collections with a single subselect query when any one of them is accessed', 'It prevents lazy loading entirely', 'It uses batch processing'],
    correctAnswer: 1,
    explanation: 'FetchMode.SUBSELECT loads all uninitialized collections of the same type with a single IN (SELECT ...) subquery when any one collection is first accessed. This avoids N+1 with a maximum of 2 queries total.',
    codeSnippet: '@OneToMany(mappedBy = "department")\n@Fetch(FetchMode.SUBSELECT)\nprivate List<Employee> employees;\n// When any department.employees is accessed:\n// SELECT * FROM employees WHERE dept_id IN\n//   (SELECT id FROM departments WHERE ...)',
    tags: ['hibernate', 'fetch-mode', 'subselect', 'n+1'],
  },
  {
    id: 'java-sql-l3-040',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'PreparedStatement',
    question: 'What is statement pooling and how does it differ from connection pooling?',
    options: ['They are the same thing', 'Statement pooling caches PreparedStatements across uses on the same connection to avoid re-preparation', 'Statement pooling is an application-level cache', 'Statement pooling pools SQL strings'],
    correctAnswer: 1,
    explanation: 'Statement pooling caches compiled PreparedStatement objects on a per-connection basis. When the same SQL is executed again on the same connection, the cached statement is reused without re-parsing. HikariCP does not pool statements internally but many drivers do.',
    tags: ['jdbc', 'statement-pooling', 'performance'],
  },
  {
    id: 'java-sql-l3-041',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'PreparedStatement',
    question: 'What is the JDBC fetch size and how does it affect performance?',
    options: ['The maximum number of rows a query can return', 'The batch size for inserts', 'The number of rows the driver fetches from the server per round trip', 'The size of each column value'],
    correctAnswer: 2,
    explanation: 'Fetch size (Statement.setFetchSize()) controls how many rows the JDBC driver retrieves from the database per network round trip. A larger fetch size reduces round trips for large result sets but uses more memory.',
    codeSnippet: 'Statement stmt = conn.createStatement();\nstmt.setFetchSize(100); // fetch 100 rows per round trip\nResultSet rs = stmt.executeQuery("SELECT * FROM large_table");',
    tags: ['jdbc', 'fetch-size', 'performance', 'tuning'],
  },
  {
    id: 'java-sql-l3-042',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the difference between @Transactional in Spring and JTA @Transactional?',
    options: ['They are identical', 'Only Spring @Transactional supports rollback', 'JTA is for local transactions; Spring is for distributed', 'Spring @Transactional (org.springframework) manages local transactions; JTA @Transactional (javax/jakarta) manages distributed transactions'],
    correctAnswer: 3,
    explanation: 'Spring\'s @Transactional manages transactions via PlatformTransactionManager and supports both local and JTA. JTA\'s @Transactional is a Java EE standard for container-managed transactions. Spring\'s version offers more configuration options.',
    tags: ['spring', 'jta', 'transactional', 'annotation'],
  },
  {
    id: 'java-sql-l3-043',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What does @Transactional(propagation = Propagation.REQUIRES_NEW) do?',
    options: ['Runs without any transaction', 'Suspends the current transaction and creates a new one', 'Reuses the existing transaction', 'Fails if there is an existing transaction'],
    correctAnswer: 1,
    explanation: 'REQUIRES_NEW suspends any existing transaction and starts a brand new transaction. The new transaction commits or rolls back independently. Useful when an operation must be committed regardless of the outer transaction\'s outcome.',
    tags: ['spring', 'transactional', 'propagation', 'requires-new'],
  },
  {
    id: 'java-sql-l3-044',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'Why does @Transactional not work when called from within the same class?',
    options: ['It is a bug in Spring', 'It only works with public methods', 'Transaction annotations are ignored at runtime', 'Spring AOP uses proxy-based interception; internal method calls bypass the proxy'],
    correctAnswer: 3,
    explanation: 'Spring @Transactional uses AOP proxies. When a method calls another method in the same class, it uses "this" (the real object) instead of the proxy, bypassing the transactional interceptor. Solutions include self-injection or AspectJ weaving.',
    tags: ['spring', 'transactional', 'proxy', 'self-invocation'],
  },
  {
    id: 'java-sql-l3-045',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the MERGE statement (UPSERT) in SQL?',
    options: ['A way to merge two databases', 'A statement that inserts or updates rows depending on whether they already exist', 'A type of join', 'A way to merge two result sets'],
    correctAnswer: 1,
    explanation: 'MERGE (or UPSERT in some databases) atomically inserts a row if it does not exist or updates it if it does. Syntax varies: SQL standard uses MERGE, PostgreSQL uses INSERT ... ON CONFLICT, MySQL uses INSERT ... ON DUPLICATE KEY.',
    codeSnippet: '-- PostgreSQL UPSERT\nINSERT INTO products (id, name, price)\nVALUES (1, \'Widget\', 9.99)\nON CONFLICT (id)\nDO UPDATE SET price = EXCLUDED.price;\n\n-- Standard SQL MERGE\nMERGE INTO target USING source\nON target.id = source.id\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT ...;',
    tags: ['sql', 'merge', 'upsert'],
  },
  {
    id: 'java-sql-l3-046',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is a database trigger?',
    options: ['A type of constraint', 'An alert mechanism', 'A scheduled job', 'A procedure that automatically executes in response to INSERT, UPDATE, or DELETE events on a table'],
    correctAnswer: 3,
    explanation: 'A trigger is a database object that automatically fires (executes) when a specified DML event (INSERT, UPDATE, DELETE) occurs on a table. Triggers can execute BEFORE or AFTER the event and can access both old and new row values.',
    codeSnippet: 'CREATE TRIGGER audit_salary\nAFTER UPDATE OF salary ON employees\nFOR EACH ROW\nBEGIN\n  INSERT INTO salary_audit (emp_id, old_salary, new_salary)\n  VALUES (OLD.id, OLD.salary, NEW.salary);\nEND;',
    tags: ['sql', 'trigger', 'automation'],
  },
  {
    id: 'java-sql-l3-047',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is a partial (filtered) index?',
    options: ['An index on only part of a column', 'An index on a subset of columns', 'An incomplete index', 'An index that includes only rows satisfying a WHERE condition'],
    correctAnswer: 3,
    explanation: 'A partial index indexes only rows that meet a specified condition. This makes the index smaller and more efficient for queries matching the condition. Useful when queries frequently filter on a specific subset.',
    codeSnippet: '-- Only index active users\nCREATE INDEX idx_active_users ON users (email)\nWHERE status = \'active\';',
    tags: ['sql', 'index', 'partial', 'filtered'],
  },
  {
    id: 'java-sql-l3-048',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is an expression (functional) index?',
    options: ['An index with complex expressions in its name', 'An index that uses regular expressions', 'An index built on the result of an expression or function rather than a plain column', 'An index that evaluates conditions'],
    correctAnswer: 2,
    explanation: 'An expression index (functional index) is built on the result of a function or expression applied to a column. It enables index usage for queries that apply the same function in WHERE clauses.',
    codeSnippet: '-- Index on lowercase email for case-insensitive search\nCREATE INDEX idx_lower_email ON users (LOWER(email));\n\n-- Query that uses this index\nSELECT * FROM users WHERE LOWER(email) = \'john@example.com\';',
    tags: ['sql', 'index', 'expression', 'functional'],
  },
  {
    id: 'java-sql-l3-049',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is Spring Data JPA\'s Specification interface?',
    options: ['A specification document for JPA', 'An interface for defining entity specifications', 'A type-safe way to build dynamic WHERE clause predicates that can be combined', 'A way to specify database schemas'],
    correctAnswer: 2,
    explanation: 'Specification<T> is a functional interface that wraps a JPA Criteria Predicate. Specifications can be composed with and(), or(), and not() to build complex, reusable, dynamic queries in a type-safe manner.',
    codeSnippet: 'public class EmployeeSpecs {\n  public static Specification<Employee> hasSalaryAbove(int min) {\n    return (root, query, cb) ->\n      cb.greaterThan(root.get("salary"), min);\n  }\n  public static Specification<Employee> inDepartment(String dept) {\n    return (root, query, cb) ->\n      cb.equal(root.get("department"), dept);\n  }\n}\n// Combine specs\nrepo.findAll(hasSalaryAbove(50000).and(inDepartment("IT")));',
    tags: ['spring-data', 'jpa', 'specification', 'dynamic-query'],
  },
  {
    id: 'java-sql-l3-050',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the difference between @OneToOne with shared primary key and a foreign key?',
    options: ['Foreign key is always better', 'Shared PK uses @MapsId to share the primary key value; FK uses a separate foreign key column', 'Shared PK requires a junction table', 'They are identical'],
    correctAnswer: 1,
    explanation: '@MapsId makes the child entity use the same primary key as the parent, eliminating the extra foreign key column. A foreign key approach adds a separate column. Shared PK is more efficient but tightly couples the entities.',
    codeSnippet: '@Entity\npublic class EmployeeDetails {\n  @Id\n  private Long id; // same as Employee.id\n\n  @OneToOne\n  @MapsId\n  @JoinColumn(name = "id")\n  private Employee employee;\n}',
    tags: ['jpa', 'one-to-one', 'maps-id', 'shared-pk'],
  },
  {
    id: 'java-sql-l3-051',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the MultipleBagFetchException in Hibernate?',
    options: ['A batch processing error', 'An error with multiple databases', 'An error when fetching too many rows', 'An error thrown when trying to eagerly fetch multiple bag (List without @OrderColumn) associations simultaneously'],
    correctAnswer: 3,
    explanation: 'Hibernate throws MultipleBagFetchException when you try to JOIN FETCH two or more List associations (bags) in one query, as it cannot differentiate which rows belong to which collection. Solutions: use Set instead of List, or @OrderColumn, or separate queries.',
    tags: ['hibernate', 'multiple-bag-fetch', 'exception'],
  },
  {
    id: 'java-sql-l3-052',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is Hibernate Envers?',
    options: ['A migration tool', 'A performance monitoring tool', 'A versioning tool for Hibernate JAR files', 'An auditing module that automatically tracks and stores entity revision history'],
    correctAnswer: 3,
    explanation: 'Hibernate Envers provides automatic entity auditing. By annotating entities with @Audited, Envers creates audit tables that store every revision of the entity, allowing you to query historical data at any point in time.',
    codeSnippet: '@Entity\n@Audited\npublic class Employee {\n  @Id private Long id;\n  private String name;\n  private double salary;\n}\n// Creates employees_AUD table with revision tracking\n// Query historical data:\n// AuditReader reader = AuditReaderFactory.get(em);\n// Employee old = reader.find(Employee.class, id, revisionNumber);',
    tags: ['hibernate', 'envers', 'auditing', 'history'],
  },
  {
    id: 'java-sql-l3-053',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between TRUNCATE, DELETE, and DROP in terms of transaction behavior?',
    options: ['DELETE is fully transactional; TRUNCATE is DDL (auto-commits in many DBs); DROP removes the table entirely', 'TRUNCATE is fully transactional', 'All are transactional', 'DROP is the only transactional operation'],
    correctAnswer: 0,
    explanation: 'DELETE is DML: fully transactional, can be rolled back, fires triggers. TRUNCATE is DDL in most databases: auto-commits, cannot be rolled back (except in PostgreSQL), does not fire row triggers. DROP removes the table structure entirely.',
    tags: ['sql', 'truncate', 'delete', 'drop', 'transactions'],
  },
  {
    id: 'java-sql-l3-054',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is index-only scan (index-covered query)?',
    options: ['A query execution where all needed data is found in the index without accessing the table', 'Scanning only the first index', 'Scanning all indexes on a table', 'A sequential scan that skips indexes'],
    correctAnswer: 0,
    explanation: 'An index-only scan occurs when all columns referenced by the query (in SELECT, WHERE, etc.) are present in the index. The database reads only the index structure and never accesses the table heap, significantly reducing I/O.',
    tags: ['sql', 'index', 'index-only-scan', 'performance'],
  },
  {
    id: 'java-sql-l3-055',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between a correlated subquery and a derived table?',
    options: ['A correlated subquery references the outer query and runs once per outer row; a derived table is independent and runs once', 'They are the same', 'A derived table is always faster', 'Correlated subqueries are not allowed in standard SQL'],
    correctAnswer: 0,
    explanation: 'A correlated subquery references columns from the outer query and is evaluated for each outer row (O(n*m) potentially). A derived table (subquery in FROM) is independent, evaluated once, and used as a virtual table.',
    tags: ['sql', 'subquery', 'correlated', 'derived-table'],
  },
  {
    id: 'java-sql-l3-056',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What are the different Spring transaction propagation levels?',
    options: ['REQUIRED, REQUIRES_NEW, MANDATORY only', 'REQUIRED, REQUIRES_NEW, SUPPORTS, NOT_SUPPORTED, MANDATORY, NEVER, NESTED', 'LOCAL, DISTRIBUTED, NESTED', 'READ, WRITE, READ_WRITE'],
    correctAnswer: 1,
    explanation: 'Spring defines 7 propagation levels: REQUIRED (use existing or create new), REQUIRES_NEW (always new), SUPPORTS (use if exists), NOT_SUPPORTED (suspend existing), MANDATORY (must exist), NEVER (must not exist), NESTED (nested with savepoint).',
    tags: ['spring', 'transactional', 'propagation'],
  },
  {
    id: 'java-sql-l3-057',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What does Propagation.NESTED do in Spring?',
    options: ['Nests multiple database connections', 'Creates a completely independent transaction', 'Executes within a nested transaction using a savepoint, so it can roll back independently', 'Fails if there is no existing transaction'],
    correctAnswer: 2,
    explanation: 'NESTED executes within the outer transaction but creates a savepoint. If the nested transaction fails, it rolls back to the savepoint without affecting the outer transaction. The outer transaction can then decide whether to continue or roll back entirely.',
    tags: ['spring', 'transactional', 'nested', 'savepoint'],
  },
  {
    id: 'java-sql-l3-058',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the purpose of @EntityGraph in JPA?',
    options: ['To graph entity performance metrics', 'To declaratively specify which associations should be eagerly fetched for a specific query', 'To define the database schema', 'To visualize entity relationships'],
    correctAnswer: 1,
    explanation: '@EntityGraph provides a way to override the default fetch plan for specific queries. It specifies which associations should be loaded eagerly, avoiding N+1 problems without permanently changing the entity\'s FetchType.',
    codeSnippet: '@Entity\n@NamedEntityGraph(\n  name = "Employee.withDeptAndProjects",\n  attributeNodes = {\n    @NamedAttributeNode("department"),\n    @NamedAttributeNode("projects")\n  }\n)\npublic class Employee { ... }\n\n// Usage in repository\n@EntityGraph("Employee.withDeptAndProjects")\nList<Employee> findByDepartmentName(String name);',
    tags: ['jpa', 'entity-graph', 'fetch-plan'],
  },
  {
    id: 'java-sql-l3-059',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is a DTO projection in Spring Data JPA?',
    options: ['Querying only specific columns and mapping them directly to a DTO or interface, avoiding full entity loading', 'Converting entities to DTOs after loading', 'Projecting data onto a DTO using a constructor', 'A way to project database schemas'],
    correctAnswer: 0,
    explanation: 'DTO projections fetch only needed columns from the database, reducing data transfer and avoiding managed entity overhead. Spring Data supports interface-based (closed/open projections) and class-based (constructor expression) projections.',
    codeSnippet: '// Interface projection\npublic interface EmployeeSummary {\n  String getName();\n  Double getSalary();\n}\n\n// In repository\nList<EmployeeSummary> findByDepartment(String dept);\n\n// Class projection with JPQL\n@Query("SELECT new com.dto.EmpDTO(e.name, e.salary) FROM Employee e")\nList<EmpDTO> findAllSummaries();',
    tags: ['spring-data', 'jpa', 'dto-projection', 'performance'],
  },
  {
    id: 'java-sql-l3-060',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the difference between session.clear() and session.evict()?',
    options: ['clear() closes the session; evict() does not', 'They are identical', 'clear() removes all entities from the persistence context; evict() removes a single specific entity', 'evict() clears all entities; clear() removes one'],
    correctAnswer: 2,
    explanation: 'session.clear() detaches all entities from the persistence context, which is useful for batch processing to prevent OutOfMemoryError. session.evict(entity) detaches a single specified entity, leaving others managed.',
    tags: ['hibernate', 'session', 'clear', 'evict'],
  },
  {
    id: 'java-sql-l3-061',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How should you perform batch inserts efficiently in Hibernate?',
    options: ['Use hibernate.jdbc.batch_size, flush and clear periodically, and use SEQUENCE ID generation', 'Use native SQL for all inserts', 'Insert one at a time with persist()', 'Disable transactions for batch operations'],
    correctAnswer: 0,
    explanation: 'For efficient batch inserts: set hibernate.jdbc.batch_size, use SEQUENCE (not IDENTITY) for ID generation (IDENTITY disables batching), periodically flush() and clear() the session to prevent memory exhaustion.',
    codeSnippet: '// hibernate.jdbc.batch_size = 50\nfor (int i = 0; i < 10000; i++) {\n  em.persist(new Employee("emp_" + i));\n  if (i % 50 == 0) {\n    em.flush();\n    em.clear(); // free memory\n  }\n}',
    tags: ['hibernate', 'batch', 'performance', 'bulk-insert'],
  },
  {
    id: 'java-sql-l3-062',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'What is the connectionTestQuery (validation query) in a connection pool?',
    options: ['A lightweight query executed to verify a connection is still valid before returning it from the pool', 'A query that tests all connections simultaneously', 'A query to test database performance', 'A query to validate SQL syntax'],
    correctAnswer: 0,
    explanation: 'The validation query (e.g., SELECT 1) verifies that a connection is alive before it is borrowed from the pool. Modern JDBC4 drivers support Connection.isValid() which is preferred over a test query.',
    tags: ['connection-pooling', 'validation', 'health-check'],
  },
  {
    id: 'java-sql-l3-063',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between UNION and JOIN?',
    options: ['UNION is faster than JOIN', 'They produce the same result', 'UNION combines rows vertically from similar queries; JOIN combines columns horizontally from related tables', 'JOIN combines rows vertically; UNION combines columns horizontally'],
    correctAnswer: 2,
    explanation: 'UNION stacks result sets vertically (adds rows from multiple queries with compatible columns). JOIN combines tables horizontally (adds columns based on a relationship). They serve fundamentally different purposes.',
    tags: ['sql', 'union', 'join', 'comparison'],
  },
  {
    id: 'java-sql-l3-064',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is a generated column (computed column) in SQL?',
    options: ['A column generated by the ORM', 'A column in a view', 'A column whose value is automatically computed from an expression involving other columns', 'A column with an auto-generated ID'],
    correctAnswer: 2,
    explanation: 'A generated column (virtual or stored) has its value computed from an expression. STORED columns are physically saved; VIRTUAL columns are computed on read. Useful for derived values that benefit from indexing.',
    codeSnippet: 'CREATE TABLE products (\n  price DECIMAL(10,2),\n  tax_rate DECIMAL(4,2),\n  total_price DECIMAL(10,2) GENERATED ALWAYS AS\n    (price * (1 + tax_rate)) STORED\n);',
    tags: ['sql', 'generated-column', 'computed'],
  },
  {
    id: 'java-sql-l3-065',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is @Transactional(readOnly = true) and its benefits?',
    options: ['It prevents all database access', 'It disables transactions entirely', 'It hints that no writes will occur, enabling optimizations like skipping dirty checking and potentially using read replicas', 'It makes the query return read-only objects'],
    correctAnswer: 2,
    explanation: 'readOnly = true signals that the transaction will not modify data. Hibernate can skip dirty checking (performance boost). Spring can route to read replicas. The JDBC driver can set the connection as read-only for database-level optimizations.',
    tags: ['spring', 'transactional', 'read-only', 'performance'],
  },
  {
    id: 'java-sql-l3-066',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the difference between Spring Data JPA\'s save() and saveAndFlush()?',
    options: ['save() defers the SQL until flush/commit; saveAndFlush() immediately executes the SQL', 'save() only works for new entities', 'They are identical', 'saveAndFlush() is asynchronous'],
    correctAnswer: 0,
    explanation: 'save() persists or merges an entity but the SQL may be deferred until the persistence context flushes. saveAndFlush() persists and immediately flushes, useful when you need the database to assign values (like generated IDs) right away.',
    tags: ['spring-data', 'jpa', 'save', 'flush'],
  },
  {
    id: 'java-sql-l3-067',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the purpose of @DynamicUpdate in Hibernate?',
    options: ['To generate UPDATE SQL that includes only changed columns instead of all columns', 'To update multiple entities at once', 'To update entities dynamically at runtime', 'To enable dynamic schema updates'],
    correctAnswer: 0,
    explanation: 'By default, Hibernate generates UPDATE statements with all columns. @DynamicUpdate generates SQL that only includes columns that have actually changed. This reduces network traffic for wide tables but prevents statement caching.',
    codeSnippet: '@Entity\n@DynamicUpdate\npublic class Employee {\n  // If only salary changes, generates:\n  // UPDATE employees SET salary = ? WHERE id = ?\n  // Instead of:\n  // UPDATE employees SET name = ?, salary = ?, dept = ? WHERE id = ?\n}',
    tags: ['hibernate', 'dynamic-update', 'performance'],
  },
  {
    id: 'java-sql-l3-068',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What are window frame boundaries in SQL?',
    options: ['The maximum result set size', 'The start and end points of the window for aggregate calculations (UNBOUNDED PRECEDING, CURRENT ROW, etc.)', 'Physical borders of the database', 'Partition boundaries'],
    correctAnswer: 1,
    explanation: 'Window frame boundaries define which rows the window function operates on: UNBOUNDED PRECEDING (first row), N PRECEDING (N rows before), CURRENT ROW, N FOLLOWING (N rows after), UNBOUNDED FOLLOWING (last row).',
    codeSnippet: '-- Moving average of last 3 rows\nAVG(salary) OVER (\n  ORDER BY hire_date\n  ROWS BETWEEN 2 PRECEDING AND CURRENT ROW\n)\n\n-- Running total from start to current\nSUM(amount) OVER (\n  ORDER BY date\n  ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n)',
    tags: ['sql', 'window-functions', 'frame', 'boundaries'],
  },
  {
    id: 'java-sql-l3-069',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is a GIN (Generalized Inverted Index) in PostgreSQL?',
    options: ['A geographic index', 'A standard B-Tree index', 'A general-purpose numeric index', 'An index type optimized for composite values like arrays, JSONB, and full-text search'],
    correctAnswer: 3,
    explanation: 'GIN indexes are optimized for values that contain multiple elements (arrays, JSONB documents, tsvector for full-text). They index individual elements and map them back to rows, enabling efficient containment and overlap queries.',
    codeSnippet: '-- GIN index for JSONB queries\nCREATE INDEX idx_data_gin ON documents USING GIN (data);\nSELECT * FROM documents WHERE data @> \'{"status": "active"}\';\n\n-- GIN for full-text search\nCREATE INDEX idx_search ON articles USING GIN (to_tsvector(\'english\', content));',
    tags: ['postgresql', 'gin', 'index', 'jsonb', 'full-text'],
  },
  {
    id: 'java-sql-l3-070',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What causes index bloat and how can it be addressed?',
    options: ['Too many queries', 'Large column values', 'Frequent updates and deletes leave dead space in the index; addressed by REINDEX or VACUUM', 'Creating too many indexes'],
    correctAnswer: 2,
    explanation: 'In PostgreSQL, updates create new row versions, leaving dead tuples in indexes. Over time, this bloat degrades performance. VACUUM reclaims dead space, REINDEX rebuilds the index entirely, and pg_repack can do it online.',
    tags: ['sql', 'index', 'bloat', 'maintenance'],
  },
  {
    id: 'java-sql-l3-071',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the difference between @ElementCollection and @OneToMany?',
    options: ['@OneToMany is for basic types; @ElementCollection is for entities', 'They are identical', '@ElementCollection is for value types (embeddables, basic) without their own identity; @OneToMany is for entities with their own identity', '@ElementCollection supports cascading; @OneToMany does not'],
    correctAnswer: 2,
    explanation: '@ElementCollection maps a collection of value types (String, Integer, @Embeddable) that have no identity of their own. @OneToMany maps a collection of entities that have their own @Id. Value types are always owned by the parent.',
    codeSnippet: '@Entity\npublic class Employee {\n  @ElementCollection\n  @CollectionTable(name = "phone_numbers")\n  private List<String> phoneNumbers;\n\n  @OneToMany(mappedBy = "employee")\n  private List<Project> projects; // Project is an @Entity\n}',
    tags: ['jpa', 'element-collection', 'one-to-many', 'comparison'],
  },
  {
    id: 'java-sql-l3-072',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate generate SQL for identifier generation strategies?',
    options: ['TABLE is the fastest strategy', 'IDENTITY uses auto-increment (disables batching), SEQUENCE uses DB sequences (supports batching), TABLE uses a separate table (slowest)', 'SEQUENCE disables batching; IDENTITY enables it', 'All strategies generate the same SQL'],
    correctAnswer: 1,
    explanation: 'IDENTITY relies on database auto-increment, requiring individual INSERTs to get the ID (disables JDBC batching). SEQUENCE can pre-allocate IDs via allocationSize, enabling batching. TABLE emulates sequences using a table with row-level locking (slowest).',
    tags: ['hibernate', 'id-generation', 'sequence', 'identity', 'performance'],
  },
  {
    id: 'java-sql-l3-073',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is table partitioning in SQL?',
    options: ['Dividing columns across tables', 'Creating copies of a table', 'Splitting a table across multiple databases', 'Dividing a large table into smaller physical pieces while maintaining a single logical table'],
    correctAnswer: 3,
    explanation: 'Table partitioning splits a large table into smaller physical partitions based on a partition key (range, list, or hash). Queries that filter on the partition key can scan only relevant partitions (partition pruning), improving performance.',
    codeSnippet: '-- Range partitioning in PostgreSQL\nCREATE TABLE orders (\n  id SERIAL, order_date DATE, amount DECIMAL\n) PARTITION BY RANGE (order_date);\n\nCREATE TABLE orders_2024 PARTITION OF orders\n  FOR VALUES FROM (\'2024-01-01\') TO (\'2025-01-01\');',
    tags: ['sql', 'partitioning', 'performance', 'scalability'],
  },
  {
    id: 'java-sql-l3-074',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the @SecondaryTable annotation in JPA?',
    options: ['Maps an entity to a backup table', 'References a table in a secondary database', 'Maps a single entity across two or more database tables', 'Creates a secondary index'],
    correctAnswer: 2,
    explanation: '@SecondaryTable allows mapping an entity\'s fields to columns in multiple tables. Fields can specify their table using @Column(table = "secondary_table"). This avoids entity splitting while maintaining the table structure.',
    codeSnippet: '@Entity\n@Table(name = "employees")\n@SecondaryTable(name = "employee_details",\n  pkJoinColumns = @PrimaryKeyJoinColumn(name = "emp_id"))\npublic class Employee {\n  @Id private Long id;\n  private String name; // in employees table\n  @Column(table = "employee_details")\n  private String bio; // in employee_details table\n}',
    tags: ['jpa', 'secondary-table', 'multi-table'],
  },
  {
    id: 'java-sql-l3-075',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between pessimistic and optimistic concurrency control in terms of performance?',
    options: ['Optimistic is always faster', 'Pessimistic is always faster', 'They have identical performance', 'Optimistic performs better under low contention; pessimistic performs better under high contention'],
    correctAnswer: 3,
    explanation: 'Optimistic locking (version check at commit) avoids lock overhead and is better when conflicts are rare. Pessimistic locking (acquire locks upfront) avoids retry overhead and is better when conflicts are frequent.',
    tags: ['sql', 'concurrency', 'optimistic', 'pessimistic', 'performance'],
  },
  {
    id: 'java-sql-l3-076',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is natural ID in Hibernate?',
    options: ['An auto-generated UUID', 'A unique business key (like email or SSN) that Hibernate can cache and look up efficiently', 'The row number', 'The primary key'],
    correctAnswer: 1,
    explanation: 'A natural ID is a unique business identifier (distinct from the surrogate PK). Hibernate\'s @NaturalId enables efficient lookups and caching by natural IDs. It supports both mutable and immutable natural IDs.',
    codeSnippet: '@Entity\npublic class User {\n  @Id @GeneratedValue\n  private Long id;\n\n  @NaturalId\n  private String email;\n}\n// Lookup by natural id\nUser user = session.byNaturalId(User.class)\n  .using("email", "john@example.com")\n  .load();',
    tags: ['hibernate', 'natural-id', 'business-key'],
  },
  {
    id: 'java-sql-l3-077',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the GROUP_CONCAT (or STRING_AGG) function?',
    options: ['Groups strings by length', 'Creates group-based constraints', 'Concatenates values from multiple rows into a single string within each group', 'Concatenates column names'],
    correctAnswer: 2,
    explanation: 'GROUP_CONCAT (MySQL) or STRING_AGG (PostgreSQL, SQL Server) aggregates string values from multiple rows into a single delimited string. Useful for creating comma-separated lists from grouped data.',
    codeSnippet: '-- MySQL\nSELECT dept, GROUP_CONCAT(name ORDER BY name SEPARATOR \', \')\nFROM employees GROUP BY dept;\n\n-- PostgreSQL\nSELECT dept, STRING_AGG(name, \', \' ORDER BY name)\nFROM employees GROUP BY dept;',
    tags: ['sql', 'aggregate', 'string-agg', 'group-concat'],
  },
  {
    id: 'java-sql-l3-078',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is @QueryHints in JPA and when would you use it?',
    options: ['Hints for writing better queries', 'Vendor-specific hints passed to the JPA provider to optimize query execution', 'Comments added to SQL queries', 'Documentation for queries'],
    correctAnswer: 1,
    explanation: '@QueryHints passes provider-specific hints to the JPA implementation. Common Hibernate hints include read-only mode, fetch size, query timeout, and cache settings. They optimize query execution without changing the query itself.',
    codeSnippet: '@QueryHints({\n  @QueryHint(name = "org.hibernate.readOnly", value = "true"),\n  @QueryHint(name = "org.hibernate.fetchSize", value = "50"),\n  @QueryHint(name = "javax.persistence.query.timeout", value = "5000")\n})\n@Query("SELECT e FROM Employee e")\nList<Employee> findAllReadOnly();',
    tags: ['jpa', 'query-hints', 'optimization'],
  },
  {
    id: 'java-sql-l3-079',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'What is the minimumIdle setting in HikariCP?',
    options: ['The minimum number of idle connections the pool tries to maintain', 'The minimum query idle time', 'The minimum idle time before eviction', 'The minimum pool size at startup'],
    correctAnswer: 0,
    explanation: 'minimumIdle sets the minimum number of idle connections to maintain in the pool. HikariCP recommends setting this equal to maximumPoolSize for best performance, as creating connections on demand adds latency.',
    tags: ['hikaricp', 'minimum-idle', 'configuration'],
  },
  {
    id: 'java-sql-l3-080',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is @Formula in Hibernate?',
    options: ['A formula for calculating batch sizes', 'A math formula validator', 'An annotation that maps a field to a SQL expression instead of a column', 'A query optimization formula'],
    correctAnswer: 2,
    explanation: '@Formula maps a read-only property to a SQL expression that is evaluated at query time. The SQL fragment is included directly in the SELECT clause when the entity is loaded.',
    codeSnippet: '@Entity\npublic class Employee {\n  private double salary;\n  private double bonus;\n\n  @Formula("salary + bonus")\n  private double totalCompensation;\n\n  @Formula("(SELECT COUNT(*) FROM projects p WHERE p.emp_id = id)")\n  private int projectCount;\n}',
    tags: ['hibernate', 'formula', 'computed-field'],
  },
  {
    id: 'java-sql-l3-081',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the PIVOT operation in SQL?',
    options: ['Sorting in reverse order', 'Moving data between tables', 'Transforming rows into columns to create cross-tabulation reports', 'Rotating an index'],
    correctAnswer: 2,
    explanation: 'PIVOT rotates row data into columns, creating a cross-tabulation (crosstab) report. Some databases have PIVOT syntax (SQL Server, Oracle); others use CASE with GROUP BY or the crosstab function (PostgreSQL).',
    codeSnippet: '-- Using CASE with GROUP BY (works in all databases)\nSELECT department,\n  SUM(CASE WHEN quarter = \'Q1\' THEN revenue END) AS q1,\n  SUM(CASE WHEN quarter = \'Q2\' THEN revenue END) AS q2,\n  SUM(CASE WHEN quarter = \'Q3\' THEN revenue END) AS q3,\n  SUM(CASE WHEN quarter = \'Q4\' THEN revenue END) AS q4\nFROM sales GROUP BY department;',
    tags: ['sql', 'pivot', 'cross-tabulation'],
  },
  {
    id: 'java-sql-l3-082',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between a temporary table and a CTE?',
    options: ['They are identical', 'A CTE persists for the session; a temporary table is for one query', 'A temporary table is physically created and persists for the session; a CTE exists only for one query', 'CTEs are faster than temporary tables'],
    correctAnswer: 2,
    explanation: 'Temporary tables are physically created (with indexes possible) and persist until session end. CTEs are virtual, exist only within the single query, and cannot be indexed. Temporary tables are better for reuse within a session; CTEs for query readability.',
    tags: ['sql', 'temporary-table', 'cte', 'comparison'],
  },
  {
    id: 'java-sql-l3-083',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the @Where annotation in Hibernate?',
    options: ['Defines WHERE clauses for JPQL', 'Marks conditional properties', 'A validation annotation', 'Adds a permanent SQL WHERE clause to an entity or collection mapping'],
    correctAnswer: 3,
    explanation: '@Where adds a SQL condition that is always applied when loading the annotated entity or collection. It acts as a permanent filter at the entity level, commonly used for soft-delete patterns.',
    codeSnippet: '@Entity\n@Where(clause = "deleted = false")\npublic class Employee {\n  private boolean deleted;\n  // All queries will automatically add: AND deleted = false\n}\n\n@OneToMany(mappedBy = "department")\n@Where(clause = "status = \'ACTIVE\'")\nprivate List<Employee> activeEmployees;',
    tags: ['hibernate', 'where', 'soft-delete', 'filter'],
  },
  {
    id: 'java-sql-l3-084',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the Criteria API metamodel and why is it useful?',
    options: ['An ORM metadata format', 'Auto-generated type-safe classes (SingularAttribute, etc.) that provide compile-time safety for Criteria queries', 'A way to model criteria', 'A model of the database schema'],
    correctAnswer: 1,
    explanation: 'The JPA metamodel generates static *_ classes with SingularAttribute fields for each entity property. Using these instead of string-based property access provides compile-time type checking for Criteria queries.',
    codeSnippet: '// Generated metamodel class\n@StaticMetamodel(Employee.class)\npublic class Employee_ {\n  public static volatile SingularAttribute<Employee, Long> id;\n  public static volatile SingularAttribute<Employee, String> name;\n  public static volatile SingularAttribute<Employee, Double> salary;\n}\n\n// Type-safe criteria query\ncq.select(root).where(\n  cb.greaterThan(root.get(Employee_.salary), 50000.0)\n);',
    tags: ['jpa', 'criteria-api', 'metamodel', 'type-safety'],
  },
  {
    id: 'java-sql-l3-085',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between a scalar subquery and a table subquery?',
    options: ['A scalar subquery returns a table; a table subquery returns a value', 'They are the same', 'A scalar subquery returns a single value; a table subquery returns a result set (rows and columns)', 'Scalar subqueries are always faster'],
    correctAnswer: 2,
    explanation: 'A scalar subquery returns exactly one value (one row, one column) and can be used anywhere a single value is expected (SELECT list, WHERE comparison). A table subquery returns a result set and is used with IN, EXISTS, or in the FROM clause.',
    tags: ['sql', 'subquery', 'scalar', 'table'],
  },
  {
    id: 'java-sql-l3-086',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the read-write splitting pattern for database scalability?',
    options: ['Splitting reads and writes in code', 'Splitting data into read and write tables', 'Routing write queries to a primary database and read queries to one or more read replicas', 'Using different connection pools for reads and writes'],
    correctAnswer: 2,
    explanation: 'Read-write splitting routes writes to a primary (master) and reads to replicas (slaves). This distributes load across multiple servers. Spring supports this with AbstractRoutingDataSource and @Transactional(readOnly = true).',
    tags: ['scalability', 'read-replica', 'routing', 'pattern'],
  },
  {
    id: 'java-sql-l3-087',
    language: 'java',
    level: 'level3',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate Filters feature?',
    options: ['A security filter for SQL injection', 'A way to filter Hibernate logs', 'Input validation filters', 'Dynamic, parameterized WHERE conditions that can be enabled/disabled at session level'],
    correctAnswer: 3,
    explanation: 'Hibernate Filters allow defining named, parameterized filter conditions on entities or collections that can be dynamically enabled or disabled on a per-session basis. Unlike @Where, they are controllable at runtime.',
    codeSnippet: '@Entity\n@FilterDef(name = "tenantFilter",\n  parameters = @ParamDef(name = "tenantId", type = Long.class))\n@Filter(name = "tenantFilter",\n  condition = "tenant_id = :tenantId")\npublic class Employee { ... }\n\n// Enable filter at runtime\nsession.enableFilter("tenantFilter")\n  .setParameter("tenantId", 42L);',
    tags: ['hibernate', 'filter', 'multi-tenancy', 'dynamic'],
  },
];

export default questions;
