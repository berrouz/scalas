import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'java-sql-l4-001',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Isolation Levels',
    question: 'How does PostgreSQL implement Serializable Snapshot Isolation (SSI)?',
    options: ['By using two-phase locking', 'By running transactions sequentially', 'By tracking read-write dependencies between transactions and aborting those that could produce a non-serializable cycle', 'By acquiring exclusive locks on all rows'],
    correctAnswer: 2,
    explanation: 'PostgreSQL SSI extends snapshot isolation by tracking rw-dependencies (where one transaction reads data another writes). If a dangerous cycle of dependencies is detected (a pivot structure), one transaction is aborted. This provides true serializability without locking.',
    tags: ['postgresql', 'ssi', 'serializable', 'isolation-levels'],
  },
  {
    id: 'java-sql-l4-002',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Isolation Levels',
    question: 'What is the difference between predicate locking and range locking for preventing phantom reads?',
    options: ['Predicate locks lock specific rows; range locks lock entire tables', 'Predicate locks lock on logical conditions; range locks lock on index key ranges as a practical implementation of predicate locks', 'Range locks are more flexible than predicate locks', 'They are identical'],
    correctAnswer: 1,
    explanation: 'Predicate locks conceptually lock all rows matching a condition (even those not yet existing). Range locks (next-key locks in InnoDB) are a practical implementation that locks index ranges to prevent phantom rows from being inserted.',
    tags: ['sql', 'locks', 'predicate', 'range', 'phantom'],
  },
  {
    id: 'java-sql-l4-003',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'How does InnoDB\'s clustered index affect secondary index lookups?',
    options: ['Secondary indexes contain copies of all row data', 'Secondary indexes store the primary key value, requiring a second lookup to the clustered index for non-covered queries', 'Secondary indexes point directly to disk locations', 'Secondary indexes are not affected'],
    correctAnswer: 1,
    explanation: 'In InnoDB, secondary indexes store the primary key value (not a physical row pointer). A secondary index lookup first finds the PK in the secondary index, then looks up the row in the clustered index. This double lookup is avoided by covering indexes.',
    tags: ['mysql', 'innodb', 'clustered-index', 'secondary-index'],
  },
  {
    id: 'java-sql-l4-004',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the Index Condition Pushdown (ICP) optimization?',
    options: ['The storage engine evaluates WHERE conditions that can be checked using the index before reading the full row', 'Pushing queries to the index server', 'Pushing index creation to the background', 'A way to push updates to indexes immediately'],
    correctAnswer: 0,
    explanation: 'ICP allows the storage engine to evaluate parts of the WHERE clause using the index tuple before reading the full row. This reduces the number of full row reads and base table accesses, improving performance for multi-column indexes.',
    tags: ['mysql', 'icp', 'optimization', 'index'],
  },
  {
    id: 'java-sql-l4-005',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is a BRIN (Block Range Index) in PostgreSQL?',
    options: ['A standard B-Tree index', 'A compact index that stores summary info for ranges of physical table blocks, ideal for naturally ordered data', 'A bitmap index for read-only tables', 'A reverse index for text search'],
    correctAnswer: 1,
    explanation: 'BRIN stores min/max values for ranges of physical table pages. It is extremely compact and efficient for columns that are physically correlated with their value ordering (e.g., timestamps in append-only tables). Much smaller than B-Tree but less precise.',
    codeSnippet: '-- Ideal for append-only tables with time-ordered data\nCREATE INDEX idx_created ON events USING BRIN (created_at)\n  WITH (pages_per_range = 128);',
    tags: ['postgresql', 'brin', 'index', 'time-series'],
  },
  {
    id: 'java-sql-l4-006',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the difference between a GiST and a GIN index in PostgreSQL?',
    options: ['GIN is lossy; GiST is exact', 'GiST is lossy and better for read-write workloads; GIN is exact and better for static data with complex searches', 'GiST is for text; GIN is for numbers', 'They are the same'],
    correctAnswer: 1,
    explanation: 'GiST (Generalized Search Tree) is lossy (may produce false positives requiring recheck), faster to update, and uses less memory. GIN is exact (no recheck), faster for lookups, but slower to update and uses more memory. GiST suits range/spatial; GIN suits containment/text.',
    tags: ['postgresql', 'gist', 'gin', 'index', 'comparison'],
  },
  {
    id: 'java-sql-l4-007',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the Outbox Pattern for reliable event publishing in database transactions?',
    options: ['A mailbox for database events', 'Writing events to an outbox table within the same transaction as the business data, then publishing asynchronously', 'An output buffer for SQL queries', 'A pattern for handling transaction timeouts'],
    correctAnswer: 1,
    explanation: 'The Outbox Pattern writes events to an "outbox" table within the same database transaction as the business data change. A separate process (CDC or polling) reads the outbox and publishes events to a message broker, ensuring exactly-once delivery semantics.',
    codeSnippet: '@Transactional\npublic void placeOrder(Order order) {\n  orderRepo.save(order);\n  outboxRepo.save(new OutboxEvent(\n    "OrderPlaced",\n    objectMapper.writeValueAsString(order)\n  ));\n  // Both saved atomically in same transaction\n}\n// Separate process publishes outbox events to Kafka',
    tags: ['pattern', 'outbox', 'event-driven', 'consistency'],
  },
  {
    id: 'java-sql-l4-008',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is Change Data Capture (CDC) and how does Debezium implement it?',
    options: ['A data validation mechanism', 'A backup strategy', 'Capturing user changes in the UI', 'Reading the database transaction log to capture row-level changes and stream them as events'],
    correctAnswer: 3,
    explanation: 'CDC captures row-level changes by reading the database transaction log (WAL in PostgreSQL, binlog in MySQL). Debezium is a CDC platform that reads these logs and publishes change events to Kafka, enabling event-driven architectures without modifying application code.',
    tags: ['cdc', 'debezium', 'event-streaming', 'wal'],
  },
  {
    id: 'java-sql-l4-009',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate implement bytecode enhancement and what are its benefits?',
    options: ['It generates optimized SQL from bytecode', 'It instruments entity bytecode at build or load time to enable lazy field loading, dirty tracking without snapshots, and bi-directional sync', 'It compresses bytecode for storage', 'It modifies Java source code'],
    correctAnswer: 1,
    explanation: 'Hibernate bytecode enhancement instruments entity classes to: (1) enable lazy loading of individual fields (not just associations), (2) replace snapshot-based dirty checking with in-entity change tracking, and (3) auto-manage bidirectional associations.',
    codeSnippet: '<!-- Maven plugin for build-time enhancement -->\n<plugin>\n  <groupId>org.hibernate.orm.tooling</groupId>\n  <artifactId>hibernate-enhance-maven-plugin</artifactId>\n  <configuration>\n    <enableLazyInitialization>true</enableLazyInitialization>\n    <enableDirtyTracking>true</enableDirtyTracking>\n    <enableAssociationManagement>true</enableAssociationManagement>\n  </configuration>\n</plugin>',
    tags: ['hibernate', 'bytecode-enhancement', 'performance', 'lazy'],
  },
  {
    id: 'java-sql-l4-010',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What are the implications of using hashCode() and equals() in JPA entities?',
    options: ['They should always use all fields', 'They should never be overridden', 'They have no impact', 'Incorrect implementations break Set collections, merge operations, and detached entity comparisons; should use business key or ID with care'],
    correctAnswer: 3,
    explanation: 'Using all fields breaks when entities are modified. Using generated ID breaks because it is null before persist. Best practice: use a natural/business key, or use the ID with a fixed hashCode (like constant or class hash) to work correctly across entity states.',
    codeSnippet: '@Entity\npublic class Employee {\n  @Id @GeneratedValue\n  private Long id;\n  @NaturalId\n  private String email;\n\n  @Override\n  public boolean equals(Object o) {\n    if (this == o) return true;\n    if (!(o instanceof Employee)) return false;\n    return email != null && email.equals(((Employee)o).email);\n  }\n\n  @Override\n  public int hashCode() {\n    return getClass().hashCode(); // constant to handle transient state\n  }\n}',
    tags: ['jpa', 'hibernate', 'equals', 'hashcode', 'identity'],
  },
  {
    id: 'java-sql-l4-011',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the @SqlResultSetMapping annotation and when is it needed?',
    options: ['It creates result set types', 'It maps the results of native SQL queries to entities, DTOs, or scalar values', 'It validates SQL results', 'It maps SQL results to tables'],
    correctAnswer: 1,
    explanation: '@SqlResultSetMapping defines how native SQL query results map to entity classes, constructor results (DTOs), or scalar columns. It is needed when native query results do not directly correspond to a single entity.',
    codeSnippet: '@SqlResultSetMapping(\n  name = "EmployeeDeptMapping",\n  classes = @ConstructorResult(\n    targetClass = EmpDeptDTO.class,\n    columns = {\n      @ColumnResult(name = "emp_name", type = String.class),\n      @ColumnResult(name = "dept_name", type = String.class),\n      @ColumnResult(name = "salary", type = Double.class)\n    }\n  )\n)\n@NamedNativeQuery(\n  name = "findEmpDept",\n  query = "SELECT e.name as emp_name, d.name as dept_name, e.salary FROM ...",\n  resultSetMapping = "EmployeeDeptMapping"\n)',
    tags: ['jpa', 'native-query', 'result-mapping', 'dto'],
  },
  {
    id: 'java-sql-l4-012',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate multi-tenancy support and what strategies are available?',
    options: ['Only database-based separation', 'Only schema-based separation', 'DATABASE (separate databases), SCHEMA (separate schemas, shared DB), DISCRIMINATOR (shared table, tenant column)', 'Multi-tenancy is not supported'],
    correctAnswer: 2,
    explanation: 'Hibernate supports three multi-tenancy strategies: DATABASE (complete isolation, separate connections), SCHEMA (shared database, separate schemas), and DISCRIMINATOR (shared tables with a tenant_id column, lowest isolation but easiest scaling).',
    tags: ['hibernate', 'multi-tenancy', 'saas', 'architecture'],
  },
  {
    id: 'java-sql-l4-013',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'How do you use JSON/JSONB columns effectively in PostgreSQL with JPA?',
    options: ['JSON is not supported in JPA', 'Use @Lob annotation for JSON', 'Use a custom Hibernate type or @JdbcTypeCode(SqlTypes.JSON) to map Java objects to JSONB with native query support', 'Store JSON as String columns'],
    correctAnswer: 2,
    explanation: 'Hibernate 6+ supports @JdbcTypeCode(SqlTypes.JSON) to map Java objects to JSONB columns. For earlier versions, use a custom UserType or libraries like hibernate-types. JSONB enables rich querying with GIN indexes.',
    codeSnippet: '@Entity\npublic class Product {\n  @Id private Long id;\n\n  @JdbcTypeCode(SqlTypes.JSON)\n  @Column(columnDefinition = "jsonb")\n  private Map<String, Object> attributes;\n}\n\n// Native query using JSONB operators\n@Query(value = "SELECT * FROM products WHERE attributes @> :filter::jsonb",\n  nativeQuery = true)\nList<Product> findByAttributes(@Param("filter") String jsonFilter);',
    tags: ['postgresql', 'jsonb', 'jpa', 'hibernate'],
  },
  {
    id: 'java-sql-l4-014',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is an Adaptive Hash Index in InnoDB?',
    options: ['An automatically managed in-memory hash index that InnoDB builds for frequently accessed B-Tree index pages', 'An index that combines hash and B-Tree', 'A hash index that adapts to query patterns', 'A manually created hash index'],
    correctAnswer: 0,
    explanation: 'InnoDB automatically monitors index page accesses and builds an in-memory hash index for hot pages. This provides O(1) lookups for frequently accessed data without manual intervention. It can be disabled if it causes contention on busy systems.',
    tags: ['mysql', 'innodb', 'adaptive-hash-index', 'performance'],
  },
  {
    id: 'java-sql-l4-015',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the query optimizer cost model and how does it choose execution plans?',
    options: ['It uses a fixed set of rules', 'It tries all plans and picks the fastest', 'It always chooses the fastest plan', 'It estimates the cost of different plans based on statistics (row counts, data distribution, I/O cost) and chooses the lowest cost'],
    correctAnswer: 3,
    explanation: 'The cost-based optimizer estimates the cost of different execution plans using statistics about data distribution (histograms), table sizes, index selectivity, and I/O costs. It considers join order, join methods, index usage, and chooses the plan with lowest estimated cost.',
    tags: ['sql', 'optimizer', 'cost-model', 'statistics'],
  },
  {
    id: 'java-sql-l4-016',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What are the different join algorithms used by database query engines?',
    options: ['Only Hash Join', 'Only Nested Loop', 'Nested Join and Parallel Join', 'Nested Loop Join, Hash Join, and Merge (Sort-Merge) Join'],
    correctAnswer: 3,
    explanation: 'Nested Loop: iterate outer, probe inner per row (good for small datasets, indexed). Hash Join: build hash table from smaller set, probe with larger (good for equi-joins). Merge Join: sort both sides and merge (good when data is pre-sorted).',
    tags: ['sql', 'join-algorithms', 'nested-loop', 'hash-join', 'merge-join'],
  },
  {
    id: 'java-sql-l4-017',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'When does the optimizer choose a Hash Join over a Nested Loop Join?',
    options: ['When both tables are large, no useful index exists, and the join is an equi-join', 'When there is an index on the join column', 'When tables are small', 'Hash Join is always chosen'],
    correctAnswer: 0,
    explanation: 'Hash Join is preferred for large equi-joins without useful indexes. It builds a hash table from the smaller table (build phase) and probes it with the larger table (probe phase). It requires memory for the hash table and only works with equality conditions.',
    tags: ['sql', 'hash-join', 'optimizer', 'performance'],
  },
  {
    id: 'java-sql-l4-018',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the SELECT FOR UPDATE SKIP LOCKED pattern and when is it used?',
    options: ['Skipping locked tables', 'A locking strategy that skips deadlocks', 'A pattern for implementing job queues where workers skip rows already being processed by other workers', 'Skipping updates on locked rows'],
    correctAnswer: 2,
    explanation: 'SELECT FOR UPDATE SKIP LOCKED acquires row locks and skips rows already locked by other transactions. This enables efficient job queue implementations where multiple workers can dequeue different items concurrently without blocking.',
    codeSnippet: '-- Worker dequeues next available job\nBEGIN;\nSELECT * FROM jobs\nWHERE status = \'pending\'\nORDER BY created_at\nLIMIT 1\nFOR UPDATE SKIP LOCKED;\n\nUPDATE jobs SET status = \'processing\' WHERE id = :id;\nCOMMIT;',
    tags: ['sql', 'for-update', 'skip-locked', 'queue'],
  },
  {
    id: 'java-sql-l4-019',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the difference between SELECT FOR UPDATE and SELECT FOR SHARE?',
    options: ['They are identical', 'FOR SHARE means sharing the result set', 'FOR UPDATE acquires an exclusive lock (blocks reads and writes); FOR SHARE acquires a shared lock (blocks writes but allows reads)', 'FOR SHARE is exclusive; FOR UPDATE is shared'],
    correctAnswer: 2,
    explanation: 'FOR UPDATE acquires an exclusive row lock, preventing other transactions from reading (with FOR UPDATE) or writing the locked rows. FOR SHARE acquires a shared lock, allowing other FOR SHARE reads but blocking FOR UPDATE and modifications.',
    tags: ['sql', 'for-update', 'for-share', 'locking'],
  },
  {
    id: 'java-sql-l4-020',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the difference between Hibernate\'s first-level cache and the persistence context?',
    options: ['They are completely different concepts', 'Persistence context is the second-level cache', 'They are essentially the same thing: the persistence context IS the first-level cache', 'First-level cache is optional; persistence context is mandatory'],
    correctAnswer: 2,
    explanation: 'The persistence context and first-level cache are the same concept viewed from different angles. The persistence context manages entity lifecycle states, identity map, dirty checking, and write-behind. These features collectively function as the first-level cache.',
    tags: ['hibernate', 'persistence-context', 'first-level-cache'],
  },
  {
    id: 'java-sql-l4-021',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What are the pitfalls of CascadeType.REMOVE vs orphanRemoval?',
    options: ['orphanRemoval is safer', 'CASCADE REMOVE only triggers on parent deletion; orphanRemoval also triggers when child is removed from collection. Both can cause unexpected mass deletions.', 'CASCADE REMOVE works on collections; orphanRemoval works on single associations', 'They are identical in behavior'],
    correctAnswer: 1,
    explanation: 'CascadeType.REMOVE deletes children when the parent is deleted. orphanRemoval additionally deletes children removed from the parent collection. Danger: clearing a collection with orphanRemoval deletes ALL children. Using both together requires careful understanding.',
    tags: ['hibernate', 'cascade', 'orphan-removal', 'pitfalls'],
  },
  {
    id: 'java-sql-l4-022',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'How does Hibernate implement @Version for optimistic locking at the SQL level?',
    options: ['By adding the version value to UPDATE/DELETE WHERE clauses and checking affected row count', 'By creating a separate version table', 'By using database locks', 'By using database triggers'],
    correctAnswer: 0,
    explanation: 'Hibernate includes the version column in the WHERE clause of UPDATE and DELETE statements. If the affected row count is 0 (version mismatch), it throws OptimisticLockException. This is a race-condition-free check because the UPDATE is atomic.',
    codeSnippet: '// Entity with @Version\n@Version private int version;\n\n// Generated SQL:\n// UPDATE employee\n// SET name = ?, salary = ?, version = 2\n// WHERE id = 1 AND version = 1\n//\n// If 0 rows affected -> OptimisticLockException',
    tags: ['jpa', 'hibernate', 'optimistic-locking', 'version', 'sql'],
  },
  {
    id: 'java-sql-l4-023',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate Interceptor and when would you use it?',
    options: ['A request interceptor', 'A callback interface for intercepting entity lifecycle events (load, save, delete, flush) for cross-cutting concerns', 'An SQL interceptor', 'A security interceptor'],
    correctAnswer: 1,
    explanation: 'Hibernate Interceptor provides callbacks at key lifecycle points (onLoad, onSave, onDelete, onFlushDirty, etc.). Use cases include audit logging, encryption/decryption, multi-tenancy filtering, and data masking. It operates at a lower level than JPA listeners.',
    codeSnippet: 'public class AuditInterceptor extends EmptyInterceptor {\n  @Override\n  public boolean onFlushDirty(Object entity, Serializable id,\n      Object[] currentState, Object[] previousState,\n      String[] propertyNames, Type[] types) {\n    for (int i = 0; i < propertyNames.length; i++) {\n      if ("modifiedDate".equals(propertyNames[i])) {\n        currentState[i] = Instant.now();\n        return true;\n      }\n    }\n    return false;\n  }\n}',
    tags: ['hibernate', 'interceptor', 'lifecycle', 'auditing'],
  },
  {
    id: 'java-sql-l4-024',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between horizontal and vertical database partitioning (sharding)?',
    options: ['They are the same', 'Horizontal splits columns; vertical splits rows', 'Horizontal splits rows across shards by a key; vertical splits columns/tables across databases by domain', 'Both split by primary key'],
    correctAnswer: 2,
    explanation: 'Horizontal partitioning (sharding) distributes rows across multiple databases based on a shard key. Vertical partitioning splits different tables or column groups into different databases (e.g., user service DB, order service DB).',
    tags: ['sql', 'sharding', 'partitioning', 'scalability'],
  },
  {
    id: 'java-sql-l4-025',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What challenges does database sharding introduce?',
    options: ['Only performance challenges', 'No challenges; it is straightforward', 'Cross-shard queries, distributed transactions, rebalancing, shard key selection, and loss of referential integrity across shards', 'Only connection management issues'],
    correctAnswer: 2,
    explanation: 'Sharding introduces: cross-shard queries (joins become difficult), distributed transactions, data rebalancing when adding shards, choosing the right shard key, loss of cross-shard foreign keys, and increased application complexity.',
    tags: ['sql', 'sharding', 'challenges', 'distributed'],
  },
  {
    id: 'java-sql-l4-026',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the Skip Scan optimization for composite indexes?',
    options: ['Scanning every other index page', 'Skipping NULL values in the index', 'Skipping index scans entirely', 'Allowing use of a composite index even when the leading column is not in the WHERE clause by scanning distinct values of the leading column'],
    correctAnswer: 3,
    explanation: 'Skip Scan (available in Oracle, MySQL 8.0.13+) enables a composite index (a, b) to be used even when "a" is not filtered. The optimizer scans distinct values of "a" and performs range scans on "b" for each. Useful when leading column has few distinct values.',
    tags: ['sql', 'index', 'skip-scan', 'optimization'],
  },
  {
    id: 'java-sql-l4-027',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'How does the database handle index maintenance during bulk data loads?',
    options: ['Bulk loads skip index updates', 'Indexes are automatically disabled during bulk loads', 'Indexes are always maintained row by row', 'Dropping indexes before load and rebuilding after is often faster; some DBs support deferred index maintenance or COPY/LOAD optimizations'],
    correctAnswer: 3,
    explanation: 'Maintaining indexes during bulk inserts is expensive (O(n log n) random I/O). Dropping indexes, loading data, then rebuilding is often faster as the rebuild can sort data first. PostgreSQL COPY and MySQL LOAD DATA have optimizations for this.',
    tags: ['sql', 'index', 'bulk-load', 'performance'],
  },
  {
    id: 'java-sql-l4-028',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'What is the relationship between connection pool size and database throughput according to the Universal Scalability Law?',
    options: ['More connections always means more throughput', 'Throughput decreases linearly with pool size', 'Pool size has no effect on throughput', 'Throughput increases with connections up to a point, then decreases due to context switching and contention'],
    correctAnswer: 3,
    explanation: 'According to USL, throughput increases sub-linearly with connections, hits a maximum, then decreases. Excess connections cause CPU context switching, increased lock contention, and cache thrashing. The optimal pool size is typically much smaller than expected.',
    tags: ['connection-pooling', 'usl', 'throughput', 'performance'],
  },
  {
    id: 'java-sql-l4-029',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate\'s second-level cache handle concurrency?',
    options: ['By using database locks', 'By locking the entire cache', 'Through configurable concurrency strategies: READ_ONLY, NONSTRICT_READ_WRITE, READ_WRITE (soft locks), and TRANSACTIONAL', 'It does not handle concurrency'],
    correctAnswer: 2,
    explanation: 'Hibernate L2 cache supports four concurrency strategies: READ_ONLY (immutable data), NONSTRICT_READ_WRITE (eventual consistency, no locks), READ_WRITE (uses soft locks for consistency), and TRANSACTIONAL (full JTA transaction support).',
    codeSnippet: '@Entity\n@Cache(usage = CacheConcurrencyStrategy.READ_WRITE)\npublic class Product {\n  @Id private Long id;\n  private String name;\n  private BigDecimal price;\n}',
    tags: ['hibernate', 'second-level-cache', 'concurrency', 'strategy'],
  },
  {
    id: 'java-sql-l4-030',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the query cache in Hibernate and what are its pitfalls?',
    options: ['A cache for connection queries', 'A cache for SQL syntax', 'A cache that stores query results keyed by the query and parameters; invalidated whenever any entity in the result region is modified', 'A cache for JPQL parsing'],
    correctAnswer: 2,
    explanation: 'The query cache stores query results (entity IDs or projections) keyed by query string and parameters. The pitfall: it is invalidated whenever ANY entity of the queried type is updated, causing high cache miss rates for frequently changing data.',
    codeSnippet: '// Enable query cache\n// hibernate.cache.use_query_cache = true\n\n@QueryHint(name = "org.hibernate.cacheable", value = "true")\n@Query("SELECT p FROM Product p WHERE p.category = :cat")\nList<Product> findByCategory(@Param("cat") String category);\n\n// WARNING: Any Product update invalidates ALL Product query caches',
    tags: ['hibernate', 'query-cache', 'pitfalls', 'invalidation'],
  },
  {
    id: 'java-sql-l4-031',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the Blaze-Persistence library and how does it improve JPA queries?',
    options: ['A caching library', 'A performance monitoring tool', 'A library providing a fluent query builder with support for CTEs, lateral joins, set operations, and pagination in JPA', 'A database migration tool'],
    correctAnswer: 2,
    explanation: 'Blaze-Persistence extends JPA with a fluent builder API supporting features JPA lacks: CTEs, lateral joins, UNION/INTERSECT/EXCEPT, keyset pagination, entity views (optimized projections), and advanced window functions.',
    codeSnippet: 'CriteriaBuilder<Employee> cb = cbf.create(em, Employee.class)\n  .where("salary").gt(50000)\n  .orderByAsc("name");\n\n// Keyset pagination (more efficient than OFFSET)\nPagedList<Employee> page = cb.page(lastKey, 20).getResultList();',
    tags: ['blaze-persistence', 'jpa', 'query-builder', 'advanced'],
  },
  {
    id: 'java-sql-l4-032',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is keyset pagination and why is it better than OFFSET pagination for large datasets?',
    options: ['They have the same performance', 'OFFSET is always faster', 'Keyset requires an additional table', 'Keyset uses WHERE with the last seen key instead of OFFSET; performance is constant regardless of page number'],
    correctAnswer: 3,
    explanation: 'OFFSET pagination becomes slower on deeper pages because the database must scan and discard all preceding rows. Keyset pagination uses WHERE id > last_id LIMIT n, which uses the index to jump directly to the starting point, giving constant O(log n) performance.',
    codeSnippet: '-- OFFSET: slow for large offsets\nSELECT * FROM products ORDER BY id LIMIT 20 OFFSET 1000000;\n-- Must scan and discard 1,000,000 rows\n\n-- KEYSET: constant performance\nSELECT * FROM products WHERE id > 1000000 ORDER BY id LIMIT 20;\n-- Uses index to seek directly',
    tags: ['sql', 'pagination', 'keyset', 'performance'],
  },
  {
    id: 'java-sql-l4-033',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'How do you implement efficient multi-column keyset pagination?',
    options: ['Use UNION for each sort column', 'Use OFFSET with multiple ORDER BY columns', 'Multiple keyset columns are not supported', 'Use a compound WHERE clause with row value comparison or boolean logic'],
    correctAnswer: 3,
    explanation: 'For multi-column keyset pagination (ORDER BY date DESC, id DESC), use row value comparison WHERE (date, id) < (last_date, last_id) or equivalent boolean logic: WHERE date < last_date OR (date = last_date AND id < last_id).',
    codeSnippet: '-- Row value comparison (PostgreSQL, MySQL 8.0+)\nSELECT * FROM events\nWHERE (created_at, id) < (:last_date, :last_id)\nORDER BY created_at DESC, id DESC\nLIMIT 20;\n\n-- Boolean equivalent (all databases)\nSELECT * FROM events\nWHERE created_at < :last_date\n   OR (created_at = :last_date AND id < :last_id)\nORDER BY created_at DESC, id DESC LIMIT 20;',
    tags: ['sql', 'pagination', 'keyset', 'multi-column'],
  },
  {
    id: 'java-sql-l4-034',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the problem with long-running transactions and how should they be handled?',
    options: ['They are automatically optimized by the database', 'Long transactions have no issues', 'Only a problem with pessimistic locking', 'They hold locks, consume resources, increase MVCC bloat, and block vacuum; break them into smaller units or use batch processing'],
    correctAnswer: 3,
    explanation: 'Long transactions hold locks (blocking others), increase MVCC version retention (bloat), prevent PostgreSQL VACUUM from reclaiming dead rows, risk connection timeouts, and can cause OOM in the persistence context. Solutions: batch processing, pagination, background jobs.',
    tags: ['transactions', 'long-running', 'performance', 'best-practices'],
  },
  {
    id: 'java-sql-l4-035',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate\'s batch processing interact with IDENTITY ID generation?',
    options: ['IDENTITY generation disables JDBC batch inserts because Hibernate needs the generated ID immediately after each insert', 'They work together perfectly', 'IDENTITY allows larger batches', 'Batch processing is not affected by ID strategy'],
    correctAnswer: 0,
    explanation: 'With IDENTITY (auto-increment), the database generates the ID upon INSERT. Hibernate must execute each INSERT individually to get the generated ID back for the persistence context. SEQUENCE strategy supports batching because IDs can be pre-allocated.',
    tags: ['hibernate', 'identity', 'batch', 'sequence', 'performance'],
  },
  {
    id: 'java-sql-l4-036',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the pooled-lo optimizer for Hibernate sequence generation?',
    options: ['An optimizer for LOB handling', 'A way to pool low-priority queries', 'An optimizer that allocates ID ranges from a sequence and assigns them locally, reducing sequence calls', 'A connection pool optimizer'],
    correctAnswer: 2,
    explanation: 'The pooled-lo optimizer pre-allocates a range of IDs by incrementing the database sequence by allocationSize. The application assigns IDs from the low end of the range, reducing database round trips. Safer than pooled optimizer for interoperability.',
    codeSnippet: '@Id\n@GeneratedValue(\n  strategy = GenerationType.SEQUENCE,\n  generator = "emp_seq"\n)\n@SequenceGenerator(\n  name = "emp_seq",\n  sequenceName = "employee_seq",\n  allocationSize = 50 // fetch 50 IDs per sequence call\n)\nprivate Long id;',
    tags: ['hibernate', 'sequence', 'pooled-lo', 'optimization'],
  },
  {
    id: 'java-sql-l4-037',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the WAL (Write-Ahead Logging) and how does it ensure durability?',
    options: ['A logging framework for SQL', 'A wall between database instances', 'A write buffer for queries', 'A technique where all modifications are written to a sequential log before applying to data pages, enabling crash recovery'],
    correctAnswer: 3,
    explanation: 'WAL ensures durability by writing all data modifications to a sequential log file before they are applied to the actual data pages. On crash recovery, the database replays the WAL to restore committed transactions and roll back uncommitted ones.',
    tags: ['sql', 'wal', 'durability', 'crash-recovery'],
  },
  {
    id: 'java-sql-l4-038',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the checkpoint process in database recovery?',
    options: ['A point where transactions are validated', 'A process that flushes dirty pages from memory to disk and records a position in the WAL, reducing recovery time', 'A backup checkpoint', 'A security checkpoint'],
    correctAnswer: 1,
    explanation: 'Checkpoints flush modified (dirty) pages from the buffer pool to disk and record the WAL position. During crash recovery, the database only needs to replay WAL entries after the last checkpoint, significantly reducing recovery time.',
    tags: ['sql', 'checkpoint', 'recovery', 'wal'],
  },
  {
    id: 'java-sql-l4-039',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is an Include index (covering index with INCLUDE clause)?',
    options: ['An index where non-key columns are stored in leaf pages only for covering queries without affecting the index sort order', 'An index that includes computed columns', 'An index that includes all tables', 'An index included in the query cache'],
    correctAnswer: 0,
    explanation: 'The INCLUDE clause (PostgreSQL, SQL Server) adds non-key columns to the index leaf pages without including them in the search key. This enables covering queries without increasing the index tree size or affecting sort order.',
    codeSnippet: '-- The index is sorted by last_name only\n-- But includes email in leaf pages for covering\nCREATE INDEX idx_name_incl_email\n  ON employees (last_name)\n  INCLUDE (email, phone);\n\n-- This query is now index-only:\nSELECT email, phone FROM employees WHERE last_name = \'Smith\';',
    tags: ['sql', 'index', 'include', 'covering'],
  },
  {
    id: 'java-sql-l4-040',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between correlated and uncorrelated DELETE with subqueries in terms of performance?',
    options: ['They have the same performance', 'Correlated DELETE re-evaluates the subquery per row (slow); uncorrelated evaluates once or uses JOIN DELETE (fast)', 'Uncorrelated is always slower', 'DELETE does not support subqueries'],
    correctAnswer: 1,
    explanation: 'Correlated DELETE (WHERE EXISTS with reference to outer) can be slow as the subquery runs per row. JOIN-based DELETE or DELETE with uncorrelated IN subquery evaluates once. For large datasets, batch DELETE with LIMIT is often best.',
    codeSnippet: '-- Slow: correlated subquery\nDELETE FROM orders o\nWHERE EXISTS (\n  SELECT 1 FROM customers c\n  WHERE c.id = o.customer_id AND c.status = \'inactive\'\n);\n\n-- Often faster: JOIN DELETE (MySQL)\nDELETE o FROM orders o\nJOIN customers c ON o.customer_id = c.id\nWHERE c.status = \'inactive\';\n\n-- PostgreSQL using USING\nDELETE FROM orders o\nUSING customers c\nWHERE o.customer_id = c.id AND c.status = \'inactive\';',
    tags: ['sql', 'delete', 'performance', 'subquery'],
  },
  {
    id: 'java-sql-l4-041',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the FlushModeType in JPA and how does it affect performance?',
    options: ['It controls how frequently connections are recycled', 'It controls when the database is backed up', 'It controls when the persistence context is synchronized with the database: AUTO (before queries), COMMIT (only at commit)', 'It has no effect on performance'],
    correctAnswer: 2,
    explanation: 'FlushModeType.AUTO (default) flushes before every query to ensure consistency. FlushModeType.COMMIT only flushes at transaction commit, reducing unnecessary flushes. COMMIT mode improves performance but may return stale data from queries.',
    codeSnippet: 'em.setFlushMode(FlushModeType.COMMIT);\n// Or per-query:\nquery.setFlushMode(FlushModeType.COMMIT);',
    tags: ['jpa', 'flush-mode', 'performance', 'consistency'],
  },
  {
    id: 'java-sql-l4-042',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate Statistics API and how do you use it for performance monitoring?',
    options: ['A tool for generating test data', 'An SQL EXPLAIN wrapper', 'A database statistics viewer', 'An API that provides metrics on cache hits/misses, query execution counts, entity load counts, and session metrics'],
    correctAnswer: 3,
    explanation: 'Hibernate Statistics collects runtime metrics: query execution times, cache hit/miss ratios, entity/collection load/update counts, session open/close counts. Enable with hibernate.generate_statistics=true and access via SessionFactory.getStatistics().',
    codeSnippet: 'Statistics stats = sessionFactory.getStatistics();\nstats.setStatisticsEnabled(true);\n\n// After some operations:\nlog.info("Queries: {}", stats.getQueryExecutionCount());\nlog.info("L2 cache hits: {}", stats.getSecondLevelCacheHitCount());\nlog.info("L2 cache misses: {}", stats.getSecondLevelCacheMissCount());\nlog.info("Entity loads: {}", stats.getEntityLoadCount());\n\n// Slowest query\nlog.info("Slowest query: {} ({}ms)",\n  stats.getQueryStatistics(slowestQuery).getExecutionMaxTime());',
    tags: ['hibernate', 'statistics', 'monitoring', 'performance'],
  },
  {
    id: 'java-sql-l4-043',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the GROUPING SETS extension in SQL?',
    options: ['A grouping of database connections', 'A way to specify multiple GROUP BY groupings in a single query, producing subtotals at different levels', 'Setting up groups', 'A type of set operation'],
    correctAnswer: 1,
    explanation: 'GROUPING SETS allows multiple grouping combinations in a single query. It is equivalent to running multiple GROUP BY queries and UNIONing them. ROLLUP and CUBE are shorthand for common GROUPING SETS patterns.',
    codeSnippet: 'SELECT department, job_title, SUM(salary)\nFROM employees\nGROUP BY GROUPING SETS (\n  (department, job_title),  -- group by both\n  (department),             -- subtotal by dept\n  (job_title),              -- subtotal by title\n  ()                        -- grand total\n);',
    tags: ['sql', 'grouping-sets', 'rollup', 'cube', 'analytics'],
  },
  {
    id: 'java-sql-l4-044',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between ROLLUP and CUBE in SQL?',
    options: ['ROLLUP generates hierarchical subtotals (left to right); CUBE generates all possible subtotal combinations', 'CUBE generates hierarchical subtotals; ROLLUP generates all combinations', 'ROLLUP is for dates; CUBE is for categories', 'They are identical'],
    correctAnswer: 0,
    explanation: 'ROLLUP(a, b, c) generates groupings: (a,b,c), (a,b), (a), (). It is hierarchical. CUBE(a,b,c) generates all 2^n combinations: (a,b,c), (a,b), (a,c), (b,c), (a), (b), (c), (). CUBE is more complete but more expensive.',
    tags: ['sql', 'rollup', 'cube', 'analytics'],
  },
  {
    id: 'java-sql-l4-045',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the idempotency pattern for database operations and why is it important?',
    options: ['Designing operations so they can be safely retried with the same result, crucial for distributed systems and message processing', 'Operations that can only run once', 'A security pattern', 'A pattern for read-only operations'],
    correctAnswer: 0,
    explanation: 'Idempotent operations produce the same result whether executed once or multiple times. This is crucial when retries are needed (network failures, message redelivery). Implemented via idempotency keys, UPSERT, or conditional updates.',
    codeSnippet: '// Idempotent payment processing\n@Transactional\npublic void processPayment(String idempotencyKey, BigDecimal amount) {\n  // Check if already processed\n  if (paymentRepo.existsByIdempotencyKey(idempotencyKey)) {\n    return; // already processed, safe to skip\n  }\n  Payment p = new Payment(idempotencyKey, amount);\n  paymentRepo.save(p);\n  // UNIQUE constraint on idempotency_key prevents duplicates\n}',
    tags: ['pattern', 'idempotency', 'distributed', 'reliability'],
  },
  {
    id: 'java-sql-l4-046',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the N+1 detection approach using datasource-proxy or p6spy?',
    options: ['A proxy for the database', 'Intercepting JDBC calls to count queries per request and failing tests or logging warnings when thresholds are exceeded', 'A network monitoring tool', 'A query profiling tool only'],
    correctAnswer: 1,
    explanation: 'datasource-proxy wraps the DataSource and intercepts all JDBC calls. It can count queries per transaction/request and assert thresholds in tests. This catches N+1 problems automatically in integration tests.',
    codeSnippet: '// In tests: assert query count\n@Test\nvoid shouldNotHaveNPlusOne() {\n  QueryCountHolder.clear();\n  \n  departmentService.findAllWithEmployees();\n  \n  QueryCount queryCount = QueryCountHolder.getGrandTotal();\n  assertThat(queryCount.getSelect())\n    .as("Should use JOIN FETCH, not N+1")\n    .isLessThanOrEqualTo(2);\n}',
    tags: ['testing', 'n+1', 'datasource-proxy', 'performance'],
  },
  {
    id: 'java-sql-l4-047',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the lock escalation problem and how does it affect performance?',
    options: ['Increasing the lock timeout', 'Escalating security locks', 'Escalating from shared to exclusive locks', 'The database promotes many row locks to a table lock, reducing concurrency when many rows are locked'],
    correctAnswer: 3,
    explanation: 'Lock escalation occurs when the database converts many fine-grained row locks to a coarser table lock to reduce memory overhead. This blocks other transactions from accessing any row in the table, even unlocked rows, reducing concurrency.',
    tags: ['sql', 'lock-escalation', 'concurrency', 'performance'],
  },
  {
    id: 'java-sql-l4-048',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'How does Spring Data JPA auditing work with @CreatedDate, @LastModifiedDate?',
    options: ['Through manual service layer code', 'Through stored procedures', 'Through database triggers', 'Through JPA entity listeners that automatically set audit fields when entities are persisted or updated'],
    correctAnswer: 3,
    explanation: 'Spring Data JPA auditing uses @EntityListeners(AuditingEntityListener.class) with @CreatedDate, @LastModifiedDate, @CreatedBy, @LastModifiedBy. The listener intercepts persist/update events and sets audit fields automatically.',
    codeSnippet: '@EntityListeners(AuditingEntityListener.class)\n@MappedSuperclass\npublic abstract class Auditable {\n  @CreatedDate\n  private Instant createdAt;\n\n  @LastModifiedDate\n  private Instant updatedAt;\n\n  @CreatedBy\n  private String createdBy;\n\n  @LastModifiedBy\n  private String updatedBy;\n}\n\n// Enable with @EnableJpaAuditing on config class\n// Implement AuditorAware<String> for createdBy/updatedBy',
    tags: ['spring-data', 'jpa', 'auditing', 'entity-listener'],
  },
  {
    id: 'java-sql-l4-049',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the difference between Hibernate\'s @Immutable and @ReadOnly?',
    options: ['@ReadOnly is a JPA standard; @Immutable is not', '@Immutable is a Hibernate annotation preventing dirty checking and updates; readOnly is a transaction/query hint affecting flush behavior', 'readOnly prevents inserts; @Immutable prevents deletes', 'They are identical'],
    correctAnswer: 1,
    explanation: '@Immutable on an entity means Hibernate will never generate UPDATE statements for it and excludes it from dirty checking. readOnly=true on @Transactional is a hint that disables flushing for the entire transaction. They serve different purposes.',
    codeSnippet: '@Entity\n@Immutable // Entity-level: never updated\npublic class AuditLog {\n  @Id private Long id;\n  private String action;\n  private Instant timestamp;\n}\n\n// Transaction-level: skip all dirty checking\n@Transactional(readOnly = true)\npublic List<Employee> findAll() { ... }',
    tags: ['hibernate', 'immutable', 'read-only', 'optimization'],
  },
  {
    id: 'java-sql-l4-050',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'How does PostgreSQL\'s VACUUM work and why is it necessary?',
    options: ['It optimizes query plans', 'It cleans up log files', 'It reclaims storage from dead tuples left by MVCC updates/deletes and updates visibility map and statistics', 'It defragments disk space'],
    correctAnswer: 2,
    explanation: 'PostgreSQL MVCC creates dead tuples on UPDATE/DELETE. VACUUM marks dead tuples as reusable, updates the visibility map (for index-only scans), and updates planner statistics. VACUUM FULL reclaims disk space but locks the table. Autovacuum handles this automatically.',
    tags: ['postgresql', 'vacuum', 'mvcc', 'maintenance'],
  },
  {
    id: 'java-sql-l4-051',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is transaction ID wraparound in PostgreSQL and why is it critical?',
    options: ['When the 32-bit transaction ID counter wraps around, old tuples could appear to be from the future; prevented by freezing tuples', 'A problem with distributed transactions', 'Transaction IDs exceeding the maximum integer', 'Transaction IDs wrapping around a load balancer'],
    correctAnswer: 0,
    explanation: 'PostgreSQL uses 32-bit transaction IDs with modular arithmetic. After ~2 billion transactions, without VACUUM freezing old tuples (marking them as definitively visible), wraparound would make old data appear to be from future transactions, causing data loss. Aggressive autovacuum prevents this.',
    tags: ['postgresql', 'txid-wraparound', 'vacuum', 'critical'],
  },
  {
    id: 'java-sql-l4-052',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the @TransactionalEventListener in Spring and how does it differ from @EventListener?',
    options: ['@TransactionalEventListener executes in a specific transaction phase (AFTER_COMMIT, etc.), ensuring event handling only occurs when the transaction succeeds', '@EventListener is deprecated', 'They are identical', '@TransactionalEventListener runs before the transaction'],
    correctAnswer: 0,
    explanation: '@TransactionalEventListener binds event handling to transaction phases: BEFORE_COMMIT, AFTER_COMMIT (default), AFTER_ROLLBACK, AFTER_COMPLETION. This ensures side effects (email, notifications) only happen when the transaction commits.',
    codeSnippet: '@TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)\npublic void handleOrderPlaced(OrderPlacedEvent event) {\n  // Only runs after transaction commits successfully\n  emailService.sendOrderConfirmation(event.getOrder());\n}\n\n// WARNING: runs in a new transaction or no transaction\n// Cannot modify the original transaction\'s data',
    tags: ['spring', 'transactional-event', 'event-listener', 'transaction-phase'],
  },
  {
    id: 'java-sql-l4-053',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate handle the persistence context in a Spring @Async method?',
    options: ['Async methods cannot use Hibernate', 'The persistence context is shared across threads', 'Hibernate automatically synchronizes across threads', 'A new persistence context is created; lazy-loaded associations from the calling thread will fail'],
    correctAnswer: 3,
    explanation: 'The Hibernate Session and persistence context are thread-bound. @Async methods run in a different thread with no access to the caller\'s session. Lazy associations fetched in the async method throw LazyInitializationException. Solution: pass IDs and re-fetch in the new thread.',
    tags: ['hibernate', 'async', 'thread-safety', 'persistence-context'],
  },
  {
    id: 'java-sql-l4-054',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'What is the leakDetectionThreshold in HikariCP and how does it work?',
    options: ['Limits the number of leaked connections', 'Detects data leaks', 'Detects memory leaks', 'Logs a warning when a connection is not returned within the threshold time, helping identify connection leak locations'],
    correctAnswer: 3,
    explanation: 'leakDetectionThreshold sets a time (ms) after which HikariCP logs a warning with the stack trace of where the connection was obtained. This helps identify code paths that fail to close connections. Set to 0 to disable, or slightly longer than your longest expected query.',
    codeSnippet: '# application.properties\nspring.datasource.hikari.leak-detection-threshold=30000\n\n# Output when leak detected:\n# WARN HikariPool - Connection leak detection triggered\n# for connection com.mysql.jdbc.JDBC4Connection@abc123\n# on thread http-nio-8080-exec-1,\n# stack trace follows...',
    tags: ['hikaricp', 'leak-detection', 'debugging', 'monitoring'],
  },
  {
    id: 'java-sql-l4-055',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between logical and physical replication in PostgreSQL?',
    options: ['Logical is faster than physical', 'Physical replication only works locally', 'Physical replicates the WAL byte stream (whole cluster); logical decodes changes into row-level events (selective, cross-version)', 'They are identical'],
    correctAnswer: 2,
    explanation: 'Physical replication streams raw WAL bytes, replicating the entire cluster identically (same version required). Logical replication decodes WAL into logical change events, allowing selective table replication, cross-version replication, and different schemas.',
    tags: ['postgresql', 'replication', 'physical', 'logical', 'wal'],
  },
  {
    id: 'java-sql-l4-056',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the deferred index creation strategy for bulk imports?',
    options: ['Creating partial indexes first', 'Creating indexes lazily', 'Dropping indexes before bulk insert, loading data, then recreating indexes with CONCURRENTLY option to avoid downtime', 'Deferring index constraint checking'],
    correctAnswer: 2,
    explanation: 'For bulk imports: drop secondary indexes, load data (faster without index maintenance), recreate indexes. In PostgreSQL, CREATE INDEX CONCURRENTLY builds the index without locking writes. Monitor progress via pg_stat_progress_create_index.',
    codeSnippet: '-- 1. Drop indexes\nDROP INDEX idx_email;\n\n-- 2. Bulk load (much faster without index maintenance)\nCOPY users FROM \'/data/users.csv\' CSV;\n\n-- 3. Recreate without locking the table\nCREATE INDEX CONCURRENTLY idx_email ON users (email);',
    tags: ['sql', 'index', 'bulk-import', 'concurrently'],
  },
  {
    id: 'java-sql-l4-057',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the problem with Spring Data JPA\'s saveAll() for batch inserts?',
    options: ['It is always efficient', 'It only works with IDENTITY generation', 'It does not support transactions', 'It checks isNew() for each entity, potentially doing SELECT before INSERT, and may not enable JDBC batching without proper configuration'],
    correctAnswer: 3,
    explanation: 'saveAll() calls save() per entity. If the entity has an assigned ID (not generated), Spring calls merge() which does a SELECT to check existence. JDBC batching requires: spring.jpa.properties.hibernate.jdbc.batch_size, correct ID strategy, and order_inserts=true.',
    codeSnippet: '# Required configuration for batch inserts\nspring.jpa.properties.hibernate.jdbc.batch_size=50\nspring.jpa.properties.hibernate.order_inserts=true\nspring.jpa.properties.hibernate.order_updates=true\n\n// For assigned IDs, implement Persistable<T>\n@Entity\npublic class Item implements Persistable<UUID> {\n  @Transient\n  private boolean isNew = true;\n  \n  @Override\n  public boolean isNew() { return isNew; }\n  \n  @PostPersist @PostLoad\n  void markNotNew() { isNew = false; }\n}',
    tags: ['spring-data', 'jpa', 'batch', 'saveall', 'performance'],
  },
  {
    id: 'java-sql-l4-058',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'How does Hibernate handle inheritance with @MappedSuperclass vs @Entity @Inheritance?',
    options: ['They are identical', '@Inheritance is for single table only', '@MappedSuperclass provides field inheritance without polymorphic queries; @Inheritance supports polymorphic queries with a table strategy', '@MappedSuperclass creates a table; @Inheritance does not'],
    correctAnswer: 2,
    explanation: '@MappedSuperclass provides field/mapping inheritance but the superclass is not an entity - no table, no polymorphic queries. @Inheritance creates actual entity inheritance with a table strategy, enabling polymorphic queries and relationships.',
    codeSnippet: '// No table, no polymorphic queries\n@MappedSuperclass\npublic abstract class Auditable {\n  @CreatedDate private Instant createdAt;\n}\n\n// Has table(s), supports: SELECT p FROM Payment p\n@Entity\n@Inheritance(strategy = InheritanceType.JOINED)\npublic abstract class Payment {\n  @Id private Long id;\n  private BigDecimal amount;\n}\n@Entity\npublic class CreditCardPayment extends Payment { ... }',
    tags: ['jpa', 'mapped-superclass', 'inheritance', 'polymorphism'],
  },
  {
    id: 'java-sql-l4-059',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is a bloom filter index and how is it used in databases?',
    options: ['A B-Tree variant for text', 'An index for image data', 'A probabilistic data structure that quickly eliminates non-matching rows with no false negatives, used in PostgreSQL with the bloom extension', 'A colorful index visualization'],
    correctAnswer: 2,
    explanation: 'A bloom filter index is a space-efficient probabilistic structure. It can definitively say a value is NOT in a set (no false negatives) but may have false positives. PostgreSQL\'s bloom extension creates multi-column lossy indexes useful for ad-hoc queries on many columns.',
    codeSnippet: 'CREATE EXTENSION bloom;\n\n-- One compact index for ad-hoc queries on any column combination\nCREATE INDEX idx_bloom ON orders USING bloom (customer_id, product_id, status)\n  WITH (length=80, col1=2, col2=2, col3=2);\n\n-- Works for: WHERE customer_id=1 AND status=\'shipped\'\n-- Or: WHERE product_id=42\n-- Or: any combination of indexed columns',
    tags: ['postgresql', 'bloom-filter', 'index', 'probabilistic'],
  },
  {
    id: 'java-sql-l4-060',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the difference between the Saga orchestration and choreography patterns?',
    options: ['Orchestration is event-driven; choreography is command-driven', 'They are identical patterns', 'Choreography requires a coordinator; orchestration does not', 'Orchestration uses a central coordinator; choreography uses events where each service decides its own actions'],
    correctAnswer: 3,
    explanation: 'Orchestration: a central saga orchestrator tells each participant what to do (command-driven, easier to understand, single point of failure). Choreography: each service listens to events and decides its own response (decoupled, harder to trace, no single point of failure).',
    tags: ['saga', 'orchestration', 'choreography', 'distributed'],
  },
  {
    id: 'java-sql-l4-061',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What are the pitfalls of using @OneToOne with FetchType.LAZY?',
    options: ['On the non-owning side (mappedBy), Hibernate cannot proxy the association because it needs to know if it is null, forcing EAGER loading', 'LAZY always works perfectly for @OneToOne', 'LAZY is not supported for @OneToOne', '@OneToOne always uses EAGER'],
    correctAnswer: 0,
    explanation: 'On the mappedBy side of @OneToOne, Hibernate must check if the associated entity exists (to decide between null and proxy). This requires a query, effectively making it eager. Solutions: @MapsId (shared PK), bytecode enhancement, or @LazyToOne(NO_PROXY).',
    codeSnippet: '@Entity\npublic class Employee {\n  @OneToOne(mappedBy = "employee", fetch = FetchType.LAZY)\n  private EmployeeDetails details;\n  // LAZY is IGNORED here! Hibernate fetches eagerly.\n\n  // Solution: use @MapsId on EmployeeDetails\n  // Then Hibernate knows details exists if employee exists\n}',
    tags: ['jpa', 'one-to-one', 'lazy', 'pitfall'],
  },
  {
    id: 'java-sql-l4-062',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the advisory lock mechanism in PostgreSQL?',
    options: ['Application-level locks managed by the database that do not lock any table or row', 'Locks with advisory warnings', 'A lock recommended by the optimizer', 'Temporary locks that expire'],
    correctAnswer: 0,
    explanation: 'Advisory locks are application-level cooperative locks stored in the database but not associated with any table or row. They are useful for coordinating distributed application logic (e.g., ensuring only one instance processes a job).',
    codeSnippet: '// Acquire advisory lock (blocks if already held)\nSELECT pg_advisory_lock(12345);\n\n// Try without blocking\nSELECT pg_try_advisory_lock(12345); -- returns true/false\n\n// Session-level lock (released on disconnect)\nSELECT pg_advisory_lock(hashtext(\'process_daily_report\'));\n\n// Transaction-level lock (released on commit/rollback)\nSELECT pg_advisory_xact_lock(12345);',
    tags: ['postgresql', 'advisory-lock', 'distributed', 'coordination'],
  },
  {
    id: 'java-sql-l4-063',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'How do you implement soft delete with JPA and Hibernate?',
    options: ['By overriding the repository delete method only', 'Using @SQLDelete to override DELETE SQL, @Where to filter deleted records, and a deleted flag column', 'Only with native SQL', 'Soft delete is not possible with JPA'],
    correctAnswer: 1,
    explanation: 'Hibernate\'s @SQLDelete overrides the DELETE statement to set a flag instead. @Where automatically filters deleted records. For global filtering, Hibernate Filters can be used. Spring Data also supports custom base repositories.',
    codeSnippet: '@Entity\n@SQLDelete(sql = "UPDATE employees SET deleted = true WHERE id = ? AND version = ?")\n@Where(clause = "deleted = false")\npublic class Employee {\n  @Id private Long id;\n  @Version private int version;\n  private boolean deleted = false;\n}\n\n// em.remove(employee) executes:\n// UPDATE employees SET deleted = true WHERE id = ? AND version = ?\n// All queries automatically add: AND deleted = false',
    tags: ['jpa', 'hibernate', 'soft-delete', 'pattern'],
  },
  {
    id: 'java-sql-l4-064',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate type system in Hibernate 6 (UserType vs JavaType/JdbcType)?',
    options: ['Unchanged from earlier versions', 'Hibernate 6 removed custom types', 'UserType is still the only way', 'Hibernate 6 replaced UserType with a split model: JavaType handles Java-side conversion, JdbcType handles JDBC-side conversion'],
    correctAnswer: 3,
    explanation: 'Hibernate 6 introduced a new type system: JavaType<T> handles conversion between Java types, JdbcType handles conversion to/from JDBC types, and BasicTypeReference ties them together. The old UserType still works but the new model is cleaner and more composable.',
    codeSnippet: '// Hibernate 6 custom type\npublic class MoneyJavaType extends AbstractClassJavaType<Money> {\n  public MoneyJavaType() { super(Money.class); }\n  \n  @Override\n  public Money fromString(CharSequence s) {\n    return Money.parse(s.toString());\n  }\n  @Override\n  public <X> X unwrap(Money value, Class<X> type, WrapperOptions opt) {\n    return (X) value.getAmount();\n  }\n}\n\n@JavaType(MoneyJavaType.class)\n@JdbcTypeCode(SqlTypes.DECIMAL)\nprivate Money salary;',
    tags: ['hibernate6', 'type-system', 'java-type', 'jdbc-type'],
  },
  {
    id: 'java-sql-l4-065',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the impact of parameter sniffing on query performance?',
    options: ['It is a security concern', 'Parameter sniffing always improves performance', 'Parameters have no effect on performance', 'The optimizer creates a plan based on first-seen parameter values which may be suboptimal for different values'],
    correctAnswer: 3,
    explanation: 'Parameter sniffing causes the optimizer to create a query plan based on the first parameter values seen. If data distribution is skewed, this plan may be terrible for other values. Solutions: OPTION(RECOMPILE) in SQL Server, plan hints, or parameterized statistics.',
    tags: ['sql', 'parameter-sniffing', 'optimizer', 'performance'],
  },
  {
    id: 'java-sql-l4-066',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is a Loose Index Scan and when does MySQL use it?',
    options: ['A scan that loosely matches patterns', 'An index scan with low precision', 'Reading only the first entry for each group in GROUP BY using the index to skip duplicates', 'Scanning every other index entry'],
    correctAnswer: 2,
    explanation: 'A Loose Index Scan reads only the first entry for each group when executing GROUP BY with MIN/MAX. It jumps between groups in the index instead of scanning all entries. It requires the GROUP BY column(s) to be a leftmost prefix of the index.',
    codeSnippet: '-- With index on (department, salary)\n-- Loose Index Scan for:\nSELECT department, MIN(salary)\nFROM employees\nGROUP BY department;\n-- Reads first salary entry per department, skips the rest',
    tags: ['mysql', 'loose-index-scan', 'group-by', 'optimization'],
  },
  {
    id: 'java-sql-l4-067',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the Transactional Outbox with polling vs CDC approach?',
    options: ['Polling periodically queries the outbox table; CDC reads the transaction log for changes - CDC has lower latency and no polling overhead', 'CDC is simpler than polling', 'They are the same approach', 'Polling is always faster'],
    correctAnswer: 0,
    explanation: 'Polling approach: a scheduled job queries the outbox table for unsent events. Simple but adds latency and DB load. CDC approach (Debezium): reads the DB transaction log to detect outbox inserts. Near real-time, no polling overhead, but more infrastructure complexity.',
    tags: ['outbox', 'polling', 'cdc', 'event-driven', 'comparison'],
  },
  {
    id: 'java-sql-l4-068',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the custom base repository pattern in Spring Data JPA?',
    options: ['A naming convention', 'Using abstract entity classes', 'Creating a custom implementation of JpaRepository that all repositories inherit from, adding cross-cutting functionality', 'A pattern for base tables'],
    correctAnswer: 2,
    explanation: 'A custom base repository allows adding common functionality (soft delete, multi-tenancy, auditing) to all repositories. Implement it by extending SimpleJpaRepository and registering it with @EnableJpaRepositories(repositoryBaseClass = ...).',
    codeSnippet: 'public class SoftDeleteRepositoryImpl<T, ID>\n    extends SimpleJpaRepository<T, ID> {\n\n  @Override\n  @Transactional\n  public void delete(T entity) {\n    // Soft delete instead of hard delete\n    if (entity instanceof SoftDeletable sd) {\n      sd.setDeleted(true);\n      save(entity);\n    } else {\n      super.delete(entity);\n    }\n  }\n}\n\n@EnableJpaRepositories(\n  repositoryBaseClass = SoftDeleteRepositoryImpl.class\n)',
    tags: ['spring-data', 'jpa', 'custom-repository', 'pattern'],
  },
  {
    id: 'java-sql-l4-069',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the difference between synchronous and asynchronous replication?',
    options: ['Synchronous waits for replica confirmation (zero data loss, higher latency); asynchronous does not wait (possible data loss, lower latency)', 'They provide the same guarantees', 'Synchronous replication does not exist in PostgreSQL', 'Asynchronous is always better'],
    correctAnswer: 0,
    explanation: 'Synchronous replication waits for at least one replica to confirm WAL receipt before committing, ensuring zero data loss but adding latency. Asynchronous commits immediately, risking data loss on primary failure. PostgreSQL supports both with configurable sync_standby_names.',
    tags: ['sql', 'replication', 'synchronous', 'asynchronous', 'durability'],
  },
  {
    id: 'java-sql-l4-070',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the problem with open-in-view and how should you architecture without it?',
    options: ['OSIV holds connections longer, hides N+1, triggers queries in view layer; without it, use DTOs, projections, and explicit fetch plans in the service layer', 'There is no problem with open-in-view', 'OSIV is required for JPA to work', 'OSIV improves performance'],
    correctAnswer: 0,
    explanation: 'OSIV keeps a database connection for the entire request lifecycle (including view rendering). Problems: connection starvation under load, N+1 in templates, unclear data access boundaries. Without OSIV: explicitly fetch needed data in service layer, return DTOs.',
    codeSnippet: '# Disable OSIV\nspring.jpa.open-in-view=false\n\n// Service layer: explicit fetch\n@Transactional(readOnly = true)\npublic DepartmentDTO getDepartment(Long id) {\n  Department dept = deptRepo.findByIdWithEmployees(id); // JOIN FETCH\n  return DepartmentDTO.from(dept); // map while session is open\n}',
    tags: ['hibernate', 'osiv', 'anti-pattern', 'architecture'],
  },
  {
    id: 'java-sql-l4-071',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the cost of a sequential scan vs index scan and when does the optimizer choose each?',
    options: ['Sequential scan is always faster', 'Sequential scan reads contiguous pages (efficient I/O); index scan does random I/O. For small tables or queries returning >5-10% of rows, sequential scan is faster', 'The choice is random', 'Index scan is always faster'],
    correctAnswer: 1,
    explanation: 'Sequential scan reads pages contiguously (sequential I/O). Index scan reads pages randomly via index pointers (random I/O, ~4x slower on HDD). When a query returns a large fraction of rows (typically >5-10%), the optimizer prefers a sequential scan to avoid random I/O overhead.',
    tags: ['sql', 'seq-scan', 'index-scan', 'optimizer', 'io'],
  },
  {
    id: 'java-sql-l4-072',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'How does Spring Data JPA\'s @Lock annotation work?',
    options: ['It prevents concurrent method calls', 'It locks the entity class', 'It locks the repository', 'It specifies the LockModeType for a query method, causing the generated query to include locking clauses'],
    correctAnswer: 3,
    explanation: '@Lock on a repository method sets the JPA LockModeType. PESSIMISTIC_WRITE generates SELECT ... FOR UPDATE, PESSIMISTIC_READ generates SELECT ... FOR SHARE, and OPTIMISTIC checks the @Version at commit time.',
    codeSnippet: 'public interface ProductRepo extends JpaRepository<Product, Long> {\n  @Lock(LockModeType.PESSIMISTIC_WRITE)\n  @Query("SELECT p FROM Product p WHERE p.id = :id")\n  Optional<Product> findByIdForUpdate(@Param("id") Long id);\n  // Generates: SELECT ... FROM products WHERE id = ? FOR UPDATE\n\n  @Lock(LockModeType.PESSIMISTIC_WRITE)\n  @QueryHints(@QueryHint(\n    name = "javax.persistence.lock.timeout", value = "3000"\n  ))\n  Optional<Product> findWithLockById(Long id);\n}',
    tags: ['spring-data', 'jpa', 'lock', 'pessimistic', 'for-update'],
  },
  {
    id: 'java-sql-l4-073',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is a covering index with INCLUDE columns vs a wide composite index?',
    options: ['INCLUDE is only for PostgreSQL', 'INCLUDE columns are stored only in leaf pages (no sort key impact); composite index includes columns in the sort key (affecting index size and maintenance)', 'They are the same', 'Composite is always better'],
    correctAnswer: 1,
    explanation: 'An INCLUDE index stores extra columns in leaf pages without affecting the B-Tree sort key. A wide composite index includes all columns in the sort key. INCLUDE is preferred when extra columns are only needed for covering, not filtering or sorting.',
    tags: ['sql', 'index', 'include', 'composite', 'comparison'],
  },
  {
    id: 'java-sql-l4-074',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the @Subselect annotation in Hibernate?',
    options: ['A type of fetch strategy', 'A way to define subqueries', 'An annotation for subselect N+1 optimization', 'Maps a read-only entity to a SQL subquery instead of a table, acting like an inline view'],
    correctAnswer: 3,
    explanation: '@Subselect maps an entity to a SQL SELECT statement instead of a table. The entity is read-only and Hibernate wraps the subselect in a FROM clause. Useful for mapping complex queries or views that do not have corresponding database views.',
    codeSnippet: '@Entity\n@Immutable\n@Subselect("SELECT d.id, d.name, COUNT(e.id) as emp_count, " +\n  "AVG(e.salary) as avg_salary " +\n  "FROM departments d LEFT JOIN employees e ON e.dept_id = d.id " +\n  "GROUP BY d.id, d.name")\n@Synchronize({"departments", "employees"}) // for auto-flush\npublic class DepartmentSummary {\n  @Id private Long id;\n  private String name;\n  private long empCount;\n  private Double avgSalary;\n}',
    tags: ['hibernate', 'subselect', 'read-only', 'virtual-entity'],
  },
  {
    id: 'java-sql-l4-075',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'How do modern databases use columnar storage and what are its benefits for analytics?',
    options: ['It is slower for all query types', 'All databases use columnar storage', 'Columnar storage is only for NoSQL', 'Columnar storage stores each column contiguously, enabling better compression, vectorized processing, and reading only needed columns for analytical queries'],
    correctAnswer: 3,
    explanation: 'Columnar storage stores values of each column together on disk. Benefits: better compression (similar values together), read only needed columns (skip irrelevant ones), vectorized CPU operations. PostgreSQL supports this with cstore_fdw/columnar tables.',
    tags: ['sql', 'columnar', 'analytics', 'storage'],
  },
  {
    id: 'java-sql-l4-076',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Transactions',
    question: 'What is the READ COMMITTED isolation implementation difference between PostgreSQL and Oracle?',
    options: ['Oracle does not support READ COMMITTED', 'PostgreSQL re-evaluates the WHERE clause with the committed version after a conflict; Oracle uses undo segments for consistent reads', 'PostgreSQL locks rows; Oracle does not', 'They are identical'],
    correctAnswer: 1,
    explanation: 'PostgreSQL READ COMMITTED: if a row is updated by a concurrent transaction, it waits for commit/rollback, then re-evaluates the WHERE clause against the updated version. Oracle: uses undo segments to construct a read-consistent snapshot at the statement level.',
    tags: ['sql', 'read-committed', 'postgresql', 'oracle', 'implementation'],
  },
  {
    id: 'java-sql-l4-077',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the DTO projection performance impact compared to entity loading in JPA?',
    options: ['No difference', 'DTO projections can be 2-10x faster as they skip entity lifecycle management, dirty checking, first-level cache overhead, and fetch only needed columns', 'DTO projections are slower due to object creation', 'Entities are always faster due to caching'],
    correctAnswer: 1,
    explanation: 'DTO projections avoid: persistence context tracking, dirty checking snapshots, first-level cache management, proxy creation, and fetching unneeded columns. For read-only queries returning many rows, the performance difference can be dramatic.',
    codeSnippet: '// Entity query: loads full entity into persistence context\n// ~50ms for 1000 rows\nList<Employee> employees = repo.findAll();\n\n// DTO projection: only needed columns, no PC overhead\n// ~10ms for 1000 rows\n@Query("SELECT new com.dto.EmpSummary(e.name, e.salary) FROM Employee e")\nList<EmpSummary> summaries = repo.findAllSummaries();\n\n// Interface projection: Spring generates proxy\nList<EmployeeSummary> projections = repo.findAllProjectedBy();',
    tags: ['jpa', 'dto', 'projection', 'performance', 'benchmark'],
  },
  {
    id: 'java-sql-l4-078',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate SQM (Semantic Query Model) in Hibernate 6?',
    options: ['The new query representation model that replaces HQL AST, providing a database-agnostic semantic tree that is translated to SQL AST', 'A SQL query monitor', 'A security query module', 'A stored query mechanism'],
    correctAnswer: 0,
    explanation: 'SQM is Hibernate 6\'s new query model. HQL/JPQL is parsed into an SQM tree (database-agnostic, Java-type-aware), which is then translated to a SQL AST (database-specific). This two-phase approach improves type safety, error messages, and cross-dialect support.',
    tags: ['hibernate6', 'sqm', 'query-model', 'internals'],
  },
  {
    id: 'java-sql-l4-079',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the buffer pool (shared_buffers) and how does it interact with the OS page cache?',
    options: ['It is a database-managed memory area for caching data pages; it works alongside the OS page cache, creating potential double-buffering', 'It stores query results only', 'It is the same as the OS cache', 'It replaces the OS cache'],
    correctAnswer: 0,
    explanation: 'The buffer pool (shared_buffers in PostgreSQL, buffer_pool_size in MySQL) caches frequently accessed data pages in memory. The OS also caches file pages. This can cause double-caching. PostgreSQL typically sets shared_buffers to 25% of RAM, relying on OS cache for the rest.',
    tags: ['sql', 'buffer-pool', 'memory', 'performance', 'tuning'],
  },
  {
    id: 'java-sql-l4-080',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Connection Pooling',
    question: 'How do you configure HikariCP for optimal performance in a Spring Boot microservice?',
    options: ['Set pool size = 2*CPU + disk, maxLifetime < DB wait_timeout, connectionTimeout based on SLA, enable leak detection', 'Minimize pool size to save memory', 'Use default settings', 'Maximize pool size for throughput'],
    correctAnswer: 0,
    explanation: 'Key settings: maximumPoolSize = 2*CPU+spindles (usually 10-20), maxLifetime slightly less than DB wait_timeout, connectionTimeout based on acceptable wait time, leakDetectionThreshold for debugging, and minimumIdle = maximumPoolSize.',
    codeSnippet: '# application.yml\nspring:\n  datasource:\n    hikari:\n      maximum-pool-size: 10\n      minimum-idle: 10\n      max-lifetime: 1800000       # 30 min (< DB wait_timeout)\n      connection-timeout: 30000   # 30 sec\n      idle-timeout: 600000        # 10 min\n      leak-detection-threshold: 60000\n      pool-name: MyApp-HikariPool\n      data-source-properties:\n        cachePrepStmts: true\n        prepStmtCacheSize: 250\n        prepStmtCacheSqlLimit: 2048',
    tags: ['hikaricp', 'spring-boot', 'configuration', 'best-practices'],
  },
  {
    id: 'java-sql-l4-081',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Indexes',
    question: 'What is the Multi-Column Statistics feature in PostgreSQL?',
    options: ['Multi-table join statistics', 'Statistics about multiple databases', 'Statistics about index usage', 'Extended statistics that capture correlations between columns for better optimizer estimates on multi-column conditions'],
    correctAnswer: 3,
    explanation: 'Standard statistics assume column independence. Extended statistics (CREATE STATISTICS) capture correlations between columns, improving cardinality estimates for multi-column WHERE clauses. Available types: ndistinct, dependencies, and MCV lists.',
    codeSnippet: '-- Without extended stats, optimizer assumes independence\n-- and may severely underestimate rows for correlated columns\n\nCREATE STATISTICS stat_city_zip (dependencies)\n  ON city, zip_code FROM addresses;\n\nANALYZE addresses;\n\n-- Now the optimizer knows city and zip_code are correlated\n-- and produces better estimates for:\n-- WHERE city = \'NYC\' AND zip_code = \'10001\'',
    tags: ['postgresql', 'statistics', 'optimizer', 'cardinality'],
  },
  {
    id: 'java-sql-l4-082',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the @PostLoad lifecycle callback pitfall in high-throughput applications?',
    options: ['@PostLoad does not work in JPA', '@PostLoad has no pitfalls', '@PostLoad only runs on explicit find()', '@PostLoad runs for every loaded entity including collections, which can cause performance issues with heavy initialization logic'],
    correctAnswer: 3,
    explanation: '@PostLoad is called every time an entity is loaded from the database (including lazy collection elements, query results, and cache retrieval). Heavy logic in @PostLoad can severely impact performance when loading many entities.',
    codeSnippet: '@Entity\npublic class Document {\n  @Lob\n  private byte[] content;\n  @Transient\n  private String contentHash;\n\n  // BAD: runs for every Document loaded\n  @PostLoad\n  void computeHash() {\n    this.contentHash = sha256(content); // expensive!\n  }\n\n  // BETTER: lazy computation\n  public String getContentHash() {\n    if (contentHash == null) {\n      contentHash = sha256(content);\n    }\n    return contentHash;\n  }\n}',
    tags: ['jpa', 'post-load', 'lifecycle', 'performance', 'pitfall'],
  },
  {
    id: 'java-sql-l4-083',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the LISTEN/NOTIFY mechanism in PostgreSQL?',
    options: ['A replication mechanism', 'A monitoring feature', 'A query notification system', 'A pub/sub messaging system built into PostgreSQL that allows sessions to send and receive asynchronous notifications'],
    correctAnswer: 3,
    explanation: 'LISTEN/NOTIFY is an intra-database pub/sub mechanism. Sessions LISTEN on channels and receive NOTIFY messages sent by other sessions. It is lightweight, transactional (notifications sent on commit), and useful for cache invalidation or real-time updates.',
    codeSnippet: '// Publisher (in a trigger or application)\n// NOTIFY channel_name, \'payload\';\n\n// Java listener using PGConnection\nPGConnection pgConn = conn.unwrap(PGConnection.class);\ntry (Statement stmt = conn.createStatement()) {\n  stmt.execute("LISTEN order_events");\n}\nwhile (true) {\n  PGNotification[] notifications = pgConn.getNotifications(10000);\n  if (notifications != null) {\n    for (PGNotification n : notifications) {\n      processEvent(n.getParameter());\n    }\n  }\n}',
    tags: ['postgresql', 'listen-notify', 'pubsub', 'real-time'],
  },
  {
    id: 'java-sql-l4-084',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the impact of Hibernate\'s auto-flush on query performance and how to optimize it?',
    options: ['Hibernate flushes before every query to ensure consistency, causing unexpected DML execution. Optimize with FlushMode.COMMIT or read-only transactions.', 'Auto-flush improves all query performance', 'Auto-flush has no impact', 'Auto-flush only affects native queries'],
    correctAnswer: 0,
    explanation: 'Auto-flush synchronizes the persistence context before queries, executing pending INSERTs/UPDATEs to ensure query consistency. For read-heavy workloads, this is wasteful. Solutions: FlushMode.COMMIT, @Transactional(readOnly=true), or stateless sessions.',
    codeSnippet: '// Problem: inserting 1000 entities then running queries\n// causes a flush (1000 INSERTs) before EACH query\n\n// Solution 1: Set flush mode\nem.setFlushMode(FlushModeType.COMMIT);\n\n// Solution 2: Read-only transaction (skips flush entirely)\n@Transactional(readOnly = true)\npublic List<Report> generateReports() { ... }\n\n// Solution 3: Separate read and write operations\n// into different transactions',
    tags: ['hibernate', 'auto-flush', 'performance', 'optimization'],
  },
  {
    id: 'java-sql-l4-085',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'SQL Basics',
    question: 'What is the impact of NULL on query optimization and how should NULLs be handled in index design?',
    options: ['All databases handle NULLs identically', 'NULLs are never stored in indexes', 'NULLs can prevent index usage for IS NOT NULL queries, affect aggregate calculations, and partial indexes can exclude NULLs for efficiency', 'NULLs have no impact on indexes'],
    correctAnswer: 2,
    explanation: 'In PostgreSQL, NULLs are indexed; in Oracle, NULLs in all columns of an index entry are not indexed. NULLs affect: IS NULL/IS NOT NULL index usage, aggregate calculations (NULLs are ignored), and sort order. Partial indexes excluding NULLs can be smaller and faster.',
    codeSnippet: '-- Partial index excluding NULLs (smaller, faster)\nCREATE INDEX idx_email_nn ON users (email)\nWHERE email IS NOT NULL;\n\n-- For Oracle: include a constant to index NULLs\nCREATE INDEX idx_nullable ON orders (nullable_col, 0);\n\n-- NULLS FIRST/LAST for sort order control\nSELECT * FROM employees ORDER BY manager_id NULLS LAST;',
    tags: ['sql', 'null', 'index', 'optimization'],
  },
  {
    id: 'java-sql-l4-086',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'JPA',
    question: 'What is the N+1 detection and prevention testing strategy for a JPA application?',
    options: ['Use EXPLAIN on every query', 'Manual code review only', 'Combine datasource-proxy query counting in integration tests, Hibernate Statistics, and CI assertions on maximum query counts', 'N+1 cannot be detected automatically'],
    correctAnswer: 2,
    explanation: 'Comprehensive N+1 prevention: (1) datasource-proxy in tests to assert query counts, (2) Hibernate Statistics for production monitoring, (3) CI/CD pipeline assertions, (4) Spring Boot Actuator metrics for real-time alerting. Test each service method independently.',
    codeSnippet: '// Integration test with query count assertion\n@SpringBootTest\nclass DepartmentServiceTest {\n  @Autowired DepartmentService service;\n\n  @Test\n  void findAllShouldNotHaveNPlusOne() {\n    // Given: 10 departments, each with 5 employees\n    QueryCountHolder.clear();\n\n    // When\n    List<DeptDTO> result = service.findAllWithEmployees();\n\n    // Then: max 2 queries (departments + employees)\n    QueryCount count = QueryCountHolder.getGrandTotal();\n    assertThat(count.getSelect())\n      .as("N+1 detected! Expected <=2 queries")\n      .isLessThanOrEqualTo(2);\n  }\n}',
    tags: ['jpa', 'n+1', 'testing', 'prevention', 'ci-cd'],
  },
  {
    id: 'java-sql-l4-087',
    language: 'java',
    level: 'level4',
    category: 'SQL & JDBC',
    subcategory: 'Hibernate Basics',
    question: 'What is the Hibernate Reactive project and how does it differ from traditional Hibernate?',
    options: ['Hibernate with reactive Spring WebFlux only', 'A non-blocking, reactive API for Hibernate using Vert.x and Mutiny, designed for reactive stacks without JDBC', 'A faster version of Hibernate', 'A real-time event system'],
    correctAnswer: 1,
    explanation: 'Hibernate Reactive replaces JDBC with non-blocking database drivers (Vert.x SQL client). It provides reactive (Mutiny/CompletionStage) APIs for all operations. It supports the same entity mappings but uses a reactive Session with non-blocking I/O throughout the stack.',
    codeSnippet: '// Hibernate Reactive with Mutiny API\n@Inject\nMutiny.SessionFactory sessionFactory;\n\npublic Uni<Employee> findById(Long id) {\n  return sessionFactory.withSession(session ->\n    session.find(Employee.class, id)\n  );\n}\n\npublic Uni<Void> persist(Employee emp) {\n  return sessionFactory.withTransaction(session ->\n    session.persist(emp)\n  );\n}\n// Fully non-blocking from application to database',
    tags: ['hibernate-reactive', 'non-blocking', 'vertx', 'mutiny'],
  },
];

export default questions;
