import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'java-oop-l4-001',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass A {\n  String show(A a) { return "A-A"; }\n  String show(D d) { return "A-D"; }\n}\nclass B extends A {\n  String show(A a) { return "B-A"; }\n  String show(B b) { return "B-B"; }\n}\nclass C extends B {}\nclass D extends B {}\nA ref = new B();\nSystem.out.println(ref.show(new C()));',
    options: ['B-A', 'B-B', 'A-A', 'A-D'],
    correctAnswer: 0,
    explanation: 'ref has compile-time type A. The compiler resolves to show(A) because A only has show(A) and show(D), and C is-a A (but not a D). At runtime, B\'s overridden show(A) is called. show(B) in B is overloaded, not visible through A reference.',
    codeSnippet: 'A ref = new B();\nSystem.out.println(ref.show(new C())); // "B-A"',
    tags: ['polymorphism', 'overloading-overriding', 'method-resolution'],
  },
  {
    id: 'java-oop-l4-002',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What happens with this Singleton when deserialized?\n\npublic class Singleton implements Serializable {\n  private static final Singleton INSTANCE = new Singleton();\n  private Singleton() {}\n  public static Singleton getInstance() { return INSTANCE; }\n}',
    options: [
      'A new instance is created, breaking the Singleton guarantee',
      'The deserialization fails silently',
      'The same instance is returned',
      'A NotSerializableException is thrown',
    ],
    correctAnswer: 0,
    explanation: 'Deserialization creates a new object by bypassing the constructor. Without a readResolve() method, each deserialization creates a new instance, breaking the Singleton pattern. Add readResolve() returning INSTANCE to fix this.',
    codeSnippet: '// Fix:\nprivate Object readResolve() {\n  return INSTANCE;\n}',
    tags: ['singleton', 'serialization', 'deserialization-attack'],
  },
  {
    id: 'java-oop-l4-003',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'Can reflection break a Singleton by calling the private constructor?',
    options: [
      'Yes, reflection can invoke private constructors unless the Singleton uses an enum',
      'Only with a SecurityManager',
      'Only in Java 8 and earlier',
      'No, private constructors are always safe',
    ],
    correctAnswer: 0,
    explanation: 'Reflection can invoke private constructors using setAccessible(true). Enum singletons are protected because the JVM prevents reflective instantiation of enum types, throwing IllegalArgumentException.',
    codeSnippet: '// Reflection attack:\nConstructor<Singleton> c = Singleton.class.getDeclaredConstructor();\nc.setAccessible(true);\nSingleton second = c.newInstance(); // new instance!\n\n// Enum is safe:\npublic enum Singleton { INSTANCE; }',
    tags: ['singleton', 'reflection', 'security'],
  },
  {
    id: 'java-oop-l4-004',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass Base {\n  private void method() { System.out.println("Base"); }\n  void callMethod() { method(); }\n}\nclass Derived extends Base {\n  void method() { System.out.println("Derived"); }\n}\nnew Derived().callMethod();',
    options: ['Derived', 'Base', 'Runtime error', 'Compilation error'],
    correctAnswer: 1,
    explanation: 'Private methods are not subject to dynamic dispatch. Base.callMethod() calls method(), which is resolved at compile time to Base\'s private method(). Derived\'s method() is a completely separate method (not an override). Output: "Base".',
    codeSnippet: 'new Derived().callMethod(); // "Base" - private methods are statically bound',
    tags: ['private-methods', 'static-binding', 'polymorphism-edge-case'],
  },
  {
    id: 'java-oop-l4-005',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the output?\n\nclass A {\n  int x;\n  A(int x) { this.x = x; }\n}\nclass B extends A {\n  int y;\n  B(int x, int y) { super(x); this.y = y; }\n  public boolean equals(Object o) {\n    if (!(o instanceof B)) return false;\n    B b = (B) o;\n    return x == b.x && y == b.y;\n  }\n}\nA a = new B(1, 2);\nB b = new B(1, 2);\nSystem.out.println(a.equals(b) + " " + b.equals(a));',
    options: ['false true', 'true true', 'true false', 'false false'],
    correctAnswer: 1,
    explanation: 'a is actually a B object (runtime type), so a.equals(b) calls B\'s equals(). Both are B instances with same x and y, so true. b.equals(a) also true since a is also a B instance. Both return true.',
    codeSnippet: 'A a = new B(1, 2);\nB b = new B(1, 2);\n// a.equals(b) → true (a is B at runtime)\n// b.equals(a) → true (a is instanceof B)',
    tags: ['equals', 'inheritance', 'symmetric'],
  },
  {
    id: 'java-oop-l4-006',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the problem with this equals implementation using instanceof with inheritance?\n\nclass Point {\n  int x, y;\n  public boolean equals(Object o) {\n    if (!(o instanceof Point p)) return false;\n    return x == p.x && y == p.y;\n  }\n}\nclass ColorPoint extends Point {\n  String color;\n  public boolean equals(Object o) {\n    if (!(o instanceof ColorPoint cp)) return false;\n    return super.equals(cp) && color.equals(cp.color);\n  }\n}',
    options: [
      'Symmetry violation: point.equals(colorPoint) can be true, but colorPoint.equals(point) is false',
      'Reflexivity violation',
      'Transitivity violation only',
      'No problem',
    ],
    correctAnswer: 0,
    explanation: 'A Point can equal a ColorPoint (if x,y match, since ColorPoint instanceof Point is true). But a ColorPoint never equals a plain Point (since Point is not instanceof ColorPoint). This breaks the symmetry requirement of equals().',
    codeSnippet: 'Point p = new Point(1, 2);\nColorPoint cp = new ColorPoint(1, 2, "red");\np.equals(cp);  // true (cp is instanceof Point)\ncp.equals(p);  // false (p is NOT instanceof ColorPoint)',
    tags: ['equals', 'symmetry-violation', 'inheritance-problem'],
  },
  {
    id: 'java-oop-l4-007',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inner Classes',
    question: 'What does the compiler generate for an inner class accessing a private outer field?',
    options: [
      'It inlines the access',
      'It generates a synthetic (package-private) access$xxx method on the outer class',
      'It copies the field to the inner class',
      'It uses reflection',
    ],
    correctAnswer: 1,
    explanation: 'At the bytecode level, inner classes are separate class files. The compiler generates synthetic package-private accessor methods (like access$000) on the outer class so the inner class can access private members. This is a compiler trick.',
    tags: ['inner-classes', 'synthetic-methods', 'bytecode'],
  },
  {
    id: 'java-oop-l4-008',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the vtable (virtual method table) in the JVM?',
    options: [
      'A table for interface constants',
      'A table of static methods',
      'A table of field names',
      'A lookup table containing method pointers for virtual method dispatch, where each class has its own vtable',
    ],
    correctAnswer: 3,
    explanation: 'The vtable is an internal JVM data structure. Each class has a vtable containing pointers to the method implementations. When an overridden method is called, the JVM looks up the method in the vtable of the actual object\'s class, enabling polymorphism.',
    tags: ['vtable', 'JVM-internals', 'virtual-dispatch'],
  },
  {
    id: 'java-oop-l4-009',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'How does the JVM handle interface method dispatch differently from virtual method dispatch?',
    options: [
      'Interface dispatch uses reflection',
      'Interface dispatch uses an itable (interface method table) or inline caching, since a class can implement multiple interfaces at different offsets',
      'They are handled the same way',
      'Interface methods are always slower',
    ],
    correctAnswer: 1,
    explanation: 'Unlike class vtables (fixed offsets), interface methods cannot have fixed offsets because a class can implement multiple interfaces. The JVM uses itables (interface tables) or inline caching/megamorphic dispatch for interface method calls.',
    tags: ['itable', 'interface-dispatch', 'JVM-internals'],
  },
  {
    id: 'java-oop-l4-010',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the output?\n\nclass A {\n  A() { System.out.print(getClass().getSimpleName() + " "); }\n}\nclass B extends A {\n  B() { System.out.print(getClass().getSimpleName() + " "); }\n}\nnew B();',
    options: ['A A', 'A B', 'B B', 'B A'],
    correctAnswer: 2,
    explanation: 'getClass() returns the runtime class, which is always B (the actual instantiated class). In A\'s constructor, getClass().getSimpleName() still returns "B" because the object\'s actual type is B. Output: "B B".',
    codeSnippet: 'new B(); // "B B" - getClass() always returns runtime type',
    tags: ['getClass', 'constructors', 'runtime-type'],
  },
  {
    id: 'java-oop-l4-011',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the problem with double-checked locking without volatile?\n\nprivate static Singleton instance;\npublic static Singleton getInstance() {\n  if (instance == null) {\n    synchronized(Singleton.class) {\n      if (instance == null) {\n        instance = new Singleton();\n      }\n    }\n  }\n  return instance;\n}',
    options: [
      'No problem at all',
      'The synchronized block doesn\'t work',
      'Without volatile, another thread may see a partially constructed object due to instruction reordering',
      'The null check is incorrect',
    ],
    correctAnswer: 2,
    explanation: 'Without volatile, the JVM may reorder instructions: 1) allocate memory, 2) assign reference to instance, 3) call constructor. Another thread could see instance != null before the constructor completes, accessing an uninitialized object.',
    codeSnippet: '// The JVM may reorder:\n// 1. memory = allocate()\n// 2. instance = memory  ← another thread sees non-null!\n// 3. constructor(memory) ← not yet executed',
    tags: ['double-checked-locking', 'volatile', 'memory-model'],
  },
  {
    id: 'java-oop-l4-012',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'How does the Java Module System (JPMS) affect reflection-based access?',
    options: [
      'Reflection still works the same way',
      'Reflection is completely disabled',
      'Strong encapsulation prevents reflective access to non-exported packages unless explicitly opened via "opens" directive',
      'Only public classes can be reflected upon',
    ],
    correctAnswer: 2,
    explanation: 'JPMS introduces strong encapsulation. Reflective access to non-exported packages requires the module to declare "opens <package>" or "opens <package> to <module>". Without this, setAccessible(true) throws InaccessibleObjectException.',
    codeSnippet: '// module-info.java\nmodule my.module {\n  exports com.api;        // compile-time + runtime access\n  opens com.internal;     // runtime reflective access only\n}',
    tags: ['JPMS', 'reflection', 'strong-encapsulation'],
  },
  {
    id: 'java-oop-l4-013',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is monomorphic, bimorphic, and megamorphic dispatch in the JVM?',
    options: [
      'JIT optimization levels based on how many different target types a call site encounters at runtime',
      'Static dispatch optimizations',
      'Different types of inheritance',
      'Different types of method overloading',
    ],
    correctAnswer: 0,
    explanation: 'The JIT compiler optimizes call sites: monomorphic (1 target type, inline cached), bimorphic (2 target types, conditional branch), megamorphic (3+ types, full vtable lookup). More target types = less optimization, slower dispatch.',
    tags: ['JIT', 'dispatch-optimization', 'JVM-internals'],
  },
  {
    id: 'java-oop-l4-014',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the problem with implementing compareTo() in a class hierarchy?\n\nclass Point implements Comparable<Point> {\n  int x, y;\n  public int compareTo(Point p) { ... }\n}\nclass ColorPoint extends Point {\n  String color;\n  public int compareTo(Point p) { ... }\n}',
    options: [
      'It is impossible to extend a Comparable class with a new field and maintain the compareTo contract (antisymmetry, transitivity)',
      'The method signature is wrong',
      'compareTo cannot be overridden',
      'No problems',
    ],
    correctAnswer: 0,
    explanation: 'Like equals(), extending a concrete Comparable class with a value field while maintaining the compareTo contract is problematic. If Point and ColorPoint are compared, either the color is ignored (losing information) or transitivity is violated. Use composition instead.',
    tags: ['compareTo', 'inheritance-problem', 'contract-violation'],
  },
  {
    id: 'java-oop-l4-015',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the Liskov Substitution Principle violation in terms of preconditions and postconditions?',
    options: [
      'Subclasses should not strengthen preconditions or weaken postconditions of the parent class',
      'Subclasses must have the same preconditions and postconditions',
      'Subclasses can strengthen preconditions and weaken postconditions',
      'Preconditions and postconditions don\'t matter for LSP',
    ],
    correctAnswer: 0,
    explanation: 'LSP requires: subclasses should not strengthen preconditions (require more) or weaken postconditions (guarantee less). A subclass can weaken preconditions (accept more) and strengthen postconditions (guarantee more). This is the design-by-contract approach.',
    tags: ['LSP', 'preconditions', 'postconditions', 'design-by-contract'],
  },
  {
    id: 'java-oop-l4-016',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass A {\n  void m(A a) { System.out.print("A.m(A) "); }\n}\nclass B extends A {\n  void m(A a) { System.out.print("B.m(A) "); }\n  void m(B b) { System.out.print("B.m(B) "); }\n}\nB b = new B();\nA a = b;\na.m(b);\nb.m(b);',
    options: [
      'B.m(A) B.m(A)',
      'B.m(B) B.m(B)',
      'B.m(A) B.m(B)',
      'A.m(A) B.m(B)',
    ],
    correctAnswer: 2,
    explanation: 'a.m(b): compile-time type of a is A, which only has m(A). At runtime, B.m(A) is called (override). b.m(b): compile-time type of b is B, which has m(A) and m(B). The most specific match m(B) is chosen. Output: "B.m(A) B.m(B)".',
    codeSnippet: 'a.m(b); // B.m(A) - resolved to m(A) at compile time, B\'s override at runtime\nb.m(b); // B.m(B) - most specific overload',
    tags: ['overloading-overriding', 'method-resolution', 'tricky'],
  },
  {
    id: 'java-oop-l4-017',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the "Curiously Recurring Template Pattern" (CRTP) equivalent in Java?',
    options: [
      'Java does not support anything similar',
      'A class that takes itself as a generic parameter: class Enum<E extends Enum<E>>',
      'A class that extends itself',
      'Circular inheritance',
    ],
    correctAnswer: 1,
    explanation: 'Java\'s Enum class uses this pattern: Enum<E extends Enum<E>>. It allows the base class to reference the derived type. This enables type-safe methods that return the correct type without casting.',
    codeSnippet: '// java.lang.Enum\npublic abstract class Enum<E extends Enum<E>> implements Comparable<E> {\n  public final int compareTo(E o) { ... } // type-safe comparison\n}\n\n// User-defined CRTP\nabstract class Builder<T extends Builder<T>> {\n  abstract T self();\n  T withName(String n) { this.name = n; return self(); }\n}',
    tags: ['CRTP', 'generics', 'self-type'],
  },
  {
    id: 'java-oop-l4-018',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the "Effective Java" approach to handling equals() in a class hierarchy?',
    options: [
      'Always use getClass()',
      'Use instanceof in all classes',
      'Don\'t override equals() in hierarchies',
      'Use composition over inheritance: embed the superclass as a field and provide a view method',
    ],
    correctAnswer: 3,
    explanation: 'Joshua Bloch advises: "There is no way to extend an instantiable class and add a value component while preserving the equals contract." The solution is to use composition: ColorPoint has a Point field rather than extending Point.',
    codeSnippet: '// Instead of: class ColorPoint extends Point\nclass ColorPoint {\n  private final Point point;\n  private final Color color;\n  \n  Point asPoint() { return point; } // view method\n  \n  public boolean equals(Object o) {\n    if (!(o instanceof ColorPoint cp)) return false;\n    return point.equals(cp.point) && color.equals(cp.color);\n  }\n}',
    tags: ['equals', 'composition', 'effective-java'],
  },
  {
    id: 'java-oop-l4-019',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Interfaces',
    question: 'What is the semantic difference between Java interfaces with default methods and multiple inheritance?',
    options: [
      'Default methods prevent multiple implementation',
      'Interfaces provide multiple inheritance of type and behavior, but not state; conflicts must be resolved explicitly',
      'Default methods make interfaces identical to abstract classes',
      'No difference, Java has full multiple inheritance now',
    ],
    correctAnswer: 1,
    explanation: 'Java interfaces provide multiple inheritance of type (implementing multiple interfaces) and behavior (default methods), but NOT state (no instance fields). When conflicts arise from multiple default methods, the implementing class must resolve them explicitly.',
    tags: ['interfaces', 'multiple-inheritance', 'default-methods', 'semantics'],
  },
  {
    id: 'java-oop-l4-020',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the output?\n\npublic class Test {\n  static int x = 10;\n  static { x = 20; }\n  static int y = x;\n  static { x = 30; }\n  public static void main(String[] args) {\n    System.out.println(x + " " + y);\n  }\n}',
    options: ['30 30', '10 10', '30 20', '20 20'],
    correctAnswer: 2,
    explanation: 'Static fields and static initializer blocks execute in declaration order: x=10, then x=20, then y=x (y=20), then x=30. Final values: x=30, y=20.',
    codeSnippet: 'static int x = 10; // x=10\nstatic { x = 20; } // x=20\nstatic int y = x;   // y=20\nstatic { x = 30; }  // x=30',
    tags: ['static-initialization', 'declaration-order', 'tricky'],
  },
  {
    id: 'java-oop-l4-021',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is "invokevirtual" vs "invokeinterface" vs "invokespecial" in JVM bytecode?',
    options: [
      'They are all the same',
      'They differ only in speed',
      'invokevirtual dispatches on class methods, invokeinterface dispatches on interface methods, invokespecial calls constructors/super/private methods (no dispatch)',
      'invokespecial is for static methods',
    ],
    correctAnswer: 2,
    explanation: 'JVM bytecode instructions: invokevirtual - virtual dispatch on class instance methods, invokeinterface - dispatch on interface methods (potentially slower), invokespecial - direct invocation without dispatch (constructors, super calls, private methods), invokestatic - static methods.',
    tags: ['bytecode', 'invokevirtual', 'JVM-internals'],
  },
  {
    id: 'java-oop-l4-022',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the difference between the Flyweight and Singleton patterns?',
    options: [
      'Singleton ensures one instance of a class; Flyweight shares fine-grained objects to minimize memory by separating intrinsic and extrinsic state',
      'Flyweight is behavioral; Singleton is structural',
      'Flyweight creates one instance; Singleton creates many',
      'They are the same pattern',
    ],
    correctAnswer: 0,
    explanation: 'Singleton ensures exactly one instance of a class. Flyweight shares instances to reduce memory: intrinsic state (shared) is stored in the flyweight, extrinsic state (unique) is passed as parameters. Example: Integer.valueOf() caches small integers.',
    tags: ['flyweight', 'singleton', 'comparison', 'memory-optimization'],
  },
  {
    id: 'java-oop-l4-023',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the "Effective Java" guideline for protected members?',
    options: [
      'Use protected freely',
      'Protected members should always be abstract',
      'Each protected member is a commitment to an implementation detail; minimize protected members as they are part of the class\'s contract with subclasses',
      'Protected members should be final',
    ],
    correctAnswer: 2,
    explanation: 'Protected members form a contract with subclasses. They expose implementation details and cannot be easily changed without breaking subclasses. Minimize protected members and document their self-use patterns carefully.',
    tags: ['protected', 'effective-java', 'design-guidelines'],
  },
  {
    id: 'java-oop-l4-024',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'How does serialization interact with inheritance when a subclass is Serializable but the parent is not?',
    options: [
      'Everything is serialized normally',
      'The subclass cannot be serialized',
      'Parent fields are re-initialized via the parent\'s no-arg constructor during deserialization; if no no-arg constructor exists, InvalidClassException is thrown',
      'Parent fields are serialized separately',
    ],
    correctAnswer: 2,
    explanation: 'If a subclass implements Serializable but the parent does not, the parent\'s fields are not serialized. During deserialization, the parent\'s no-arg constructor is called to initialize parent fields. If the parent lacks a no-arg constructor, serialization fails.',
    tags: ['serialization', 'inheritance', 'no-arg-constructor'],
  },
  {
    id: 'java-oop-l4-025',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\ninterface I { default String name() { return "I"; } }\nclass A implements I { public String name() { return "A"; } }\nclass B extends A { }\nclass C extends B implements I { }\nSystem.out.println(new C().name());',
    options: ['I', 'C', 'A', 'Compilation error'],
    correctAnswer: 2,
    explanation: 'C extends B which extends A. A overrides I.name(). C does not override name() and also implements I, but the "class wins" rule means A\'s implementation takes precedence over I\'s default. C inherits A.name(). Output: "A".',
    codeSnippet: 'new C().name(); // "A" - inherited from A, class wins over interface default',
    tags: ['default-methods', 'class-wins', 'inheritance-chain'],
  },
  {
    id: 'java-oop-l4-026',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is the "Serialization Proxy Pattern"?',
    options: [
      'A pattern for proxying serialized data over the network',
      'Using dynamic proxies for serialization',
      'A pattern for converting XML to objects',
      'Using a private static inner class as a serialization substitute to maintain invariants and prevent attacks',
    ],
    correctAnswer: 3,
    explanation: 'The Serialization Proxy Pattern uses writeReplace() to substitute a private static inner class (the proxy) for serialization. On deserialization, readResolve() creates a proper instance using the public API, maintaining invariants and preventing attacks.',
    codeSnippet: 'class Period implements Serializable {\n  private Object writeReplace() {\n    return new SerializationProxy(this);\n  }\n  private static class SerializationProxy implements Serializable {\n    private final Date start, end;\n    SerializationProxy(Period p) { start = p.start; end = p.end; }\n    private Object readResolve() {\n      return new Period(start, end); // uses public constructor - validates!\n    }\n  }\n}',
    tags: ['serialization-proxy', 'design-patterns', 'security'],
  },
  {
    id: 'java-oop-l4-027',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inner Classes',
    question: 'What is the compile-time representation of a lambda expression vs an anonymous inner class?',
    options: [
      'Both generate anonymous class files',
      'Lambdas generate .class files; anonymous classes use invokedynamic',
      'Anonymous inner classes generate a separate .class file; lambdas use invokedynamic and may be implemented as method handles or classes at runtime',
      'Both use invokedynamic',
    ],
    correctAnswer: 2,
    explanation: 'Anonymous inner classes always generate a separate .class file (Outer$1.class). Lambda expressions use invokedynamic bytecode instruction, and the JVM decides at runtime how to implement them (possibly as a class generated at runtime via LambdaMetafactory).',
    tags: ['lambda', 'invokedynamic', 'anonymous-classes', 'bytecode'],
  },
  {
    id: 'java-oop-l4-028',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the "Expression Problem" in OOP?',
    options: [
      'A problem with mathematical expressions in Java',
      'The difficulty of adding both new types and new operations to a type hierarchy without modifying existing code',
      'A problem with regular expressions',
      'A compiler optimization problem',
    ],
    correctAnswer: 1,
    explanation: 'The Expression Problem: OOP makes it easy to add new types (subclasses) but hard to add new operations (must modify all classes). FP (pattern matching) makes it easy to add operations but hard to add types. Sealed classes + pattern matching help address this.',
    tags: ['expression-problem', 'OOP-vs-FP', 'design-theory'],
  },
  {
    id: 'java-oop-l4-029',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the "effective" Java rule about Comparable and equals consistency?',
    options: [
      'They don\'t need to be consistent',
      'compareTo must call equals',
      'It is strongly recommended that compareTo is consistent with equals: (x.compareTo(y)==0) == x.equals(y)',
      'equals must call compareTo',
    ],
    correctAnswer: 2,
    explanation: 'While not strictly required, it is strongly recommended that compareTo be consistent with equals. TreeSet uses compareTo for ordering/equality, while HashSet uses equals/hashCode. Inconsistency can cause surprising behavior when the same objects behave differently in different collections.',
    codeSnippet: '// BigDecimal violates this:\nnew BigDecimal("1.0").equals(new BigDecimal("1.00")) // false\nnew BigDecimal("1.0").compareTo(new BigDecimal("1.00")) // 0',
    tags: ['compareTo', 'equals', 'consistency', 'effective-java'],
  },
  {
    id: 'java-oop-l4-030',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the problem with this enum Singleton in a multi-classloader environment?',
    options: [
      'Each classloader may load its own copy of the enum class, creating multiple "singleton" instances',
      'Enums don\'t work with classloaders',
      'No problem, enums are always safe',
      'Classloaders don\'t affect singletons',
    ],
    correctAnswer: 0,
    explanation: 'In environments with multiple classloaders (application servers, OSGi), each classloader can load its own copy of a class, including an enum singleton. This creates multiple instances that appear to be "the same" class but are actually different.',
    tags: ['singleton', 'classloader', 'multi-classloader', 'edge-case'],
  },
  {
    id: 'java-oop-l4-031',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the Service Locator pattern and how does it compare to Dependency Injection?',
    options: [
      'Service Locator is always better',
      'Service Locator provides a registry to look up dependencies; DI has a container push dependencies in. DI is preferred for better testability and explicit dependencies',
      'They are the same pattern',
      'DI requires a framework; Service Locator does not',
    ],
    correctAnswer: 1,
    explanation: 'Service Locator uses a central registry that components query for dependencies (pull). DI has a container inject dependencies via constructors/setters (push). DI is preferred because dependencies are explicit, testable, and not hidden behind a global lookup.',
    tags: ['service-locator', 'dependency-injection', 'architecture'],
  },
  {
    id: 'java-oop-l4-032',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass A {\n  int x = 1;\n  int getX() { return x; }\n}\nclass B extends A {\n  int x = 2;\n  int getX() { return x; }\n}\nA a = new B();\nSystem.out.println(a.x + " " + a.getX());',
    options: ['2 1', '2 2', '1 1', '1 2'],
    correctAnswer: 3,
    explanation: 'a.x uses the compile-time type A, returning A\'s field x = 1 (fields are not polymorphic). a.getX() uses runtime dispatch, calling B\'s overridden getX() which returns B\'s x = 2. Output: "1 2".',
    codeSnippet: 'A a = new B();\n// a.x = 1 (static field binding)\n// a.getX() = 2 (dynamic method dispatch)',
    tags: ['fields-vs-methods', 'static-vs-dynamic-binding', 'tricky'],
  },
  {
    id: 'java-oop-l4-033',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the canEqual pattern for fixing equals() in class hierarchies?',
    options: [
      'A pattern for comparing arrays',
      'A pattern that prevents equals() from being called',
      'A pattern for null checking',
      'Adding a canEqual(Object) method that subclasses override to indicate type compatibility, preserving symmetry',
    ],
    correctAnswer: 3,
    explanation: 'The canEqual pattern (from Scala, applicable in Java) adds a method: boolean canEqual(Object other). In equals(), you check other.canEqual(this). Subclasses override canEqual to return true only for their own type. This preserves symmetry while allowing meaningful equals in hierarchies.',
    codeSnippet: 'class Point {\n  boolean canEqual(Object o) { return o instanceof Point; }\n  public boolean equals(Object o) {\n    if (!(o instanceof Point p)) return false;\n    if (!p.canEqual(this)) return false;\n    return x == p.x && y == p.y;\n  }\n}\nclass ColorPoint extends Point {\n  boolean canEqual(Object o) { return o instanceof ColorPoint; }\n}',
    tags: ['canEqual', 'equals', 'hierarchy-pattern'],
  },
  {
    id: 'java-oop-l4-034',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the "Fragile Base Class" problem with the following code?\n\nclass InstrumentedHashSet<E> extends HashSet<E> {\n  private int addCount = 0;\n  @Override public boolean add(E e) {\n    addCount++;\n    return super.add(e);\n  }\n  @Override public boolean addAll(Collection<? extends E> c) {\n    addCount += c.size();\n    return super.addAll(c);\n  }\n}',
    options: [
      'The addCount should be static',
      'HashSet cannot be extended',
      'No problem',
      'HashSet.addAll() internally calls add() for each element, so addAll() double-counts (both in addAll override and in add override)',
    ],
    correctAnswer: 3,
    explanation: 'This is a classic fragile base class example from Effective Java. addAll() increments addCount by c.size(), then calls super.addAll() which calls add() for each element, incrementing addCount again. This is a self-use detail of HashSet that creates double-counting.',
    codeSnippet: '// addAll({a, b, c}):\n// addCount += 3 (in addAll override)\n// super.addAll() calls add() 3 times, each adding 1\n// addCount is now 6 instead of expected 3',
    tags: ['fragile-base-class', 'self-use', 'effective-java'],
  },
  {
    id: 'java-oop-l4-035',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'How would you fix the InstrumentedHashSet problem using composition?',
    options: [
      'Make addCount volatile',
      'Override more methods',
      'Use a wrapper class that delegates to a HashSet field instead of extending HashSet',
      'Use a synchronized block',
    ],
    correctAnswer: 2,
    explanation: 'Use composition: create a ForwardingSet that wraps a Set and delegates all calls. Then InstrumentedSet extends ForwardingSet and only overrides methods it needs to instrument. This decouples from HashSet\'s internal implementation.',
    codeSnippet: 'class ForwardingSet<E> implements Set<E> {\n  private final Set<E> s;\n  ForwardingSet(Set<E> s) { this.s = s; }\n  public boolean add(E e) { return s.add(e); }\n  public boolean addAll(Collection<? extends E> c) { return s.addAll(c); }\n  // ... delegate all methods\n}\nclass InstrumentedSet<E> extends ForwardingSet<E> {\n  private int addCount = 0;\n  public boolean add(E e) { addCount++; return super.add(e); }\n  public boolean addAll(Collection<? extends E> c) { addCount += c.size(); return super.addAll(c); }\n}',
    tags: ['composition', 'forwarding', 'effective-java'],
  },
  {
    id: 'java-oop-l4-036',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the relationship between Java\'s type system and the Liskov Substitution Principle regarding arrays?',
    options: [
      'Java arrays are invariant',
      'Java arrays are covariant (String[] is a subtype of Object[]) which can violate type safety at runtime with ArrayStoreException',
      'Arrays fully satisfy LSP',
      'Java arrays are contravariant',
    ],
    correctAnswer: 1,
    explanation: 'Java arrays are covariant: String[] is a subtype of Object[]. This allows Object[] arr = new String[]{"a"}. But arr[0] = 42 compiles but throws ArrayStoreException at runtime. Generics (List<String> is NOT a subtype of List<Object>) fix this with compile-time safety.',
    codeSnippet: 'Object[] arr = new String[1]; // compiles (covariant arrays)\narr[0] = 42; // compiles but throws ArrayStoreException at runtime!',
    tags: ['covariant-arrays', 'type-safety', 'ArrayStoreException'],
  },
  {
    id: 'java-oop-l4-037',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the "Type Object" pattern?',
    options: [
      'Using instanceof for type checking',
      'Creating a Type enum',
      'A pattern where instances of a TypeObject class define different types at runtime, allowing new types without new code',
      'Using Class objects as parameters',
    ],
    correctAnswer: 2,
    explanation: 'The Type Object pattern creates a class whose instances represent different types. Instead of subclassing for each type, you create instances of a "type" class that define behavior. Used in game programming (Monster types) and ECS (entity-component systems).',
    tags: ['type-object', 'design-patterns', 'data-driven'],
  },
  {
    id: 'java-oop-l4-038',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is the Principle of Minimal Authority (POLA) in Java OOP?',
    options: [
      'Always use the private modifier',
      'Only create one instance of each class',
      'Only use the public access modifier',
      'Grant each component the minimum capabilities and access it needs, using the most restrictive access possible',
    ],
    correctAnswer: 3,
    explanation: 'POLA/POLP (Principle of Least Privilege) means each component should have only the access rights it needs to function. In Java: use the most restrictive access modifier possible, expose minimal API, accept interfaces not concrete types.',
    tags: ['POLA', 'minimal-authority', 'encapsulation', 'security'],
  },
  {
    id: 'java-oop-l4-039',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the Acyclic Visitor pattern?',
    options: [
      'A variant of Visitor that uses runtime type checking to avoid cyclic dependencies between visitor and element hierarchies',
      'A visitor that visits nodes in a tree',
      'A visitor for graph traversal',
      'A visitor that prevents infinite loops',
    ],
    correctAnswer: 0,
    explanation: 'The Acyclic Visitor pattern breaks the dependency cycle of the classic Visitor pattern. Instead of the visitor interface declaring visit() for every element type, each element type has its own visitor interface. Type checking and casting determine if the visitor handles a specific element.',
    tags: ['acyclic-visitor', 'design-patterns', 'decoupling'],
  },
  {
    id: 'java-oop-l4-040',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass Base {\n  final void method() { System.out.println("Base"); }\n}\nclass Derived extends Base {\n  void method() { System.out.println("Derived"); }\n}',
    options: [
      'Compiles fine, prints Derived when called on Derived',
      'Compilation error: cannot override final method',
      'Runtime error',
      'Compiles fine, prints Base when called on Derived',
    ],
    correctAnswer: 1,
    explanation: 'A final method cannot be overridden. Attempting to define a method with the same signature in a subclass results in a compilation error. The final keyword on a method prevents any subclass from overriding it.',
    codeSnippet: 'class Derived extends Base {\n  void method() {} // Compilation error: method() in Derived cannot override method() in Base\n}',
    tags: ['final', 'method-overriding', 'compilation-error'],
  },
  {
    id: 'java-oop-l4-041',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the safe publication problem and how does it relate to object construction?',
    options: [
      'Publishing is about Serialization',
      'An object reference may be visible to another thread before the constructor has finished, exposing a partially constructed object',
      'Publishing means making an object visible in the UI',
      'Safe publication only matters for static fields',
    ],
    correctAnswer: 1,
    explanation: 'Safe publication ensures that when an object reference is shared between threads, the other thread sees the fully constructed object. Without proper synchronization, another thread might see default field values. Solutions: volatile, final fields, AtomicReference, or synchronized blocks.',
    tags: ['safe-publication', 'concurrency', 'object-construction'],
  },
  {
    id: 'java-oop-l4-042',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'How do final fields help with safe publication?',
    options: [
      'Final fields are always thread-safe for writes',
      'The JVM guarantees that final fields are properly initialized and visible to all threads once the constructor completes (freeze semantics)',
      'Final fields prevent other threads from reading',
      'Final fields are not related to thread safety',
    ],
    correctAnswer: 1,
    explanation: 'The Java Memory Model guarantees that final fields are properly visible to other threads once the constructor completes, without explicit synchronization. This is called "freeze" semantics. Immutable objects (all final fields) are inherently thread-safe for publication.',
    tags: ['final', 'safe-publication', 'JMM', 'freeze-semantics'],
  },
  {
    id: 'java-oop-l4-043',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the sealed class hierarchy exhaustiveness checking mechanism?',
    options: [
      'A reflection-based check',
      'Runtime checking of all subclasses',
      'The compiler knows all permitted subtypes at compile time and verifies that switch/if covers all cases',
      'Only works with records',
    ],
    correctAnswer: 2,
    explanation: 'Sealed classes provide compile-time exhaustiveness checking. The compiler knows every permitted subclass, so in pattern matching switches, it can verify all cases are covered without needing a default branch. This is critical for algebraic data types.',
    codeSnippet: 'sealed interface Expr permits Literal, Add, Multiply {}\nint eval(Expr e) {\n  return switch (e) {\n    case Literal l -> l.value();\n    case Add a -> eval(a.left()) + eval(a.right());\n    case Multiply m -> eval(m.left()) * eval(m.right());\n    // No default needed - compiler verifies exhaustiveness\n  };\n}',
    tags: ['sealed-classes', 'exhaustiveness', 'pattern-matching'],
  },
  {
    id: 'java-oop-l4-044',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'SOLID Principles',
    question: 'What is the Dependency Inversion Principle\'s relationship with the Factory pattern?',
    options: [
      'Factory pattern violates DIP',
      'Factory methods/abstract factories enable DIP by creating objects through abstractions rather than using "new" with concrete classes directly',
      'They are unrelated',
      'DIP replaces the Factory pattern',
    ],
    correctAnswer: 1,
    explanation: 'DIP says to depend on abstractions. Using "new ConcreteClass()" creates a dependency on a concrete class. Factories abstract the creation, allowing the client to depend on the factory interface while the concrete creation is encapsulated. DI frameworks automate this further.',
    tags: ['DIP', 'factory', 'abstraction', 'architecture'],
  },
  {
    id: 'java-oop-l4-045',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass A {\n  String show(A obj) { return "A and A"; }\n}\nclass B extends A {\n  String show(B obj) { return "B and B"; }\n  String show(A obj) { return "B and A"; }\n}\nA a = new B();\nB b = new B();\nSystem.out.println(a.show(b));',
    options: ['B and B', 'Compilation error', 'A and A', 'B and A'],
    correctAnswer: 3,
    explanation: 'a has compile-time type A. A only has show(A). b is passed and is-a A. Compiler resolves to show(A). At runtime, a is a B object. B\'s overridden show(A) returns "B and A". B\'s show(B) is not considered because the compiler only sees A\'s methods.',
    codeSnippet: 'a.show(b); // "B and A"',
    tags: ['overloading-overriding', 'compile-time-resolution', 'tricky'],
  },
  {
    id: 'java-oop-l4-046',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What are value types (Project Valhalla) and how will they change Java OOP?',
    options: [
      'They already exist as primitives',
      'They are a new kind of interface',
      'Value types are user-defined types that have no identity, are immutable, and can be flattened into memory like primitives, improving performance',
      'They replace all classes',
    ],
    correctAnswer: 2,
    explanation: 'Project Valhalla introduces value types (value classes): user-defined types without identity (no meaningful ==, no synchronization), immutable, and inlinable. They eliminate indirection overhead (no object header, no heap allocation needed), bridging the gap between primitives and objects.',
    tags: ['valhalla', 'value-types', 'future-java'],
  },
  {
    id: 'java-oop-l4-047',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the output?\n\nclass Parent {\n  static String greet() { return "Parent"; }\n  String hello() { return greet(); }\n}\nclass Child extends Parent {\n  static String greet() { return "Child"; }\n}\nSystem.out.println(new Child().hello());',
    options: ['Parent', 'Child', 'Runtime error', 'Compilation error'],
    correctAnswer: 0,
    explanation: 'hello() is an instance method inherited by Child. Inside hello(), greet() is called. Since greet() is static, it is resolved at compile time based on the class where hello() is defined (Parent). So Parent.greet() is called, returning "Parent".',
    codeSnippet: 'new Child().hello(); // "Parent" - static greet() resolved in Parent where hello() is defined',
    tags: ['static-binding', 'method-hiding', 'inheritance-trap'],
  },
  {
    id: 'java-oop-l4-048',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the difference between the Bridge and Adapter patterns?',
    options: [
      'Bridge separates abstraction from implementation at design time; Adapter converts an existing incompatible interface after the fact',
      'They are the same pattern',
      'Bridge is simpler than Adapter',
      'Adapter is used at design time; Bridge after the fact',
    ],
    correctAnswer: 0,
    explanation: 'Bridge is designed upfront: it separates an abstraction from its implementation so both can vary independently. Adapter is applied retroactively: it makes an existing class work with an interface it wasn\'t designed for. Intent and timing differ.',
    tags: ['bridge', 'adapter', 'comparison', 'design-patterns'],
  },
  {
    id: 'java-oop-l4-049',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the "Effective Java" recommendation for clone() vs copy constructors?',
    options: [
      'Use neither; always create new objects manually',
      'Prefer copy constructors or static factory methods over clone() as they don\'t rely on a risk-prone extralinguistic mechanism',
      'Always use clone()',
      'Always use serialization for copying',
    ],
    correctAnswer: 1,
    explanation: 'Josh Bloch recommends copy constructors/static factories over clone() because: 1) No dependence on the broken Cloneable mechanism, 2) No conflict with final fields, 3) No need for type casting, 4) Can accept an interface type for conversion.',
    codeSnippet: '// Prefer:\npublic TreeSet(Collection<? extends E> c) { ... } // copy constructor\n\n// Over:\npublic Object clone() { ... } // problematic',
    tags: ['clone', 'copy-constructor', 'effective-java'],
  },
  {
    id: 'java-oop-l4-050',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the concept of structural typing and how does Java differ from it?',
    options: [
      'Java uses nominal typing (types are compatible based on declared names/hierarchy), not structural typing (types compatible based on structure/shape)',
      'Java uses both',
      'Java uses structural typing',
      'There is no difference',
    ],
    correctAnswer: 0,
    explanation: 'Java uses nominal typing: two types are compatible only if they declare a relationship (extends, implements). In structural typing (Go, TypeScript), types are compatible if they have the same structure. In Java, two classes with identical methods are not interchangeable without an explicit relationship.',
    tags: ['nominal-typing', 'structural-typing', 'type-systems'],
  },
  {
    id: 'java-oop-l4-051',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is the "leaking this" problem during construction?',
    options: [
      'Using "this" in a static context',
      'Calling this() recursively',
      'A memory leak caused by "this"',
      'Passing "this" to external code during construction, allowing other threads or objects to see a partially constructed object',
    ],
    correctAnswer: 3,
    explanation: 'Leaking "this" during construction means making "this" visible to other code before the constructor completes. For example, registering a listener in the constructor that references "this". Other threads may observe the object in an inconsistent, partially constructed state.',
    codeSnippet: 'class EventSource {\n  EventSource(EventListener listener) {\n    listener.register(this); // BAD: leaking this\n    // ... more initialization\n  }\n}',
    tags: ['leaking-this', 'construction', 'thread-safety'],
  },
  {
    id: 'java-oop-l4-052',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'SOLID Principles',
    question: 'How does the ISP (Interface Segregation Principle) relate to Java\'s default methods?',
    options: [
      'ISP doesn\'t apply to interfaces with default methods',
      'Default methods violate ISP',
      'Default methods enforce ISP automatically',
      'Default methods can help implement ISP by providing no-op defaults in interfaces, but fat interfaces with many defaults can violate ISP',
    ],
    correctAnswer: 3,
    explanation: 'Default methods are a double-edged sword for ISP. They can provide sensible defaults so implementors don\'t need to implement every method. But they can also encourage fat interfaces. ISP still recommends small, focused interfaces.',
    tags: ['ISP', 'default-methods', 'design-tension'],
  },
  {
    id: 'java-oop-l4-053',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the Mediator pattern and when is it preferred over direct object communication?',
    options: [
      'A pattern for database mediation',
      'A pattern where objects communicate through a mediator to reduce coupling, preferred when many objects have complex interconnections',
      'A pattern for media file handling',
      'A pattern for resolving inheritance conflicts',
    ],
    correctAnswer: 1,
    explanation: 'The Mediator pattern encapsulates how objects interact. Instead of objects referencing each other directly (N*N connections), they communicate through a mediator (N connections). This reduces coupling and centralizes complex communication logic.',
    tags: ['mediator', 'design-patterns', 'coupling'],
  },
  {
    id: 'java-oop-l4-054',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the output?\n\nclass A {\n  A() { System.out.print("A(" + this.getClass().getSimpleName() + ") "); }\n}\nclass B extends A {\n  int x;\n  B() { this.x = 10; System.out.print("B(x=" + x + ") "); }\n}\nclass C extends B {\n  C() { System.out.print("C(x=" + x + ") "); }\n}\nnew C();',
    options: [
      'C(x=0) B(x=10) A(C)',
      'A(A) B(x=10) C(x=10)',
      'A(C) B(x=10) C(x=10)',
      'A(C) B(x=0) C(x=10)',
    ],
    correctAnswer: 2,
    explanation: 'Order: A constructor runs first, getClass() returns C (actual type). B constructor runs next, sets x=10. C constructor runs last, x is already 10. Output: "A(C) B(x=10) C(x=10)".',
    codeSnippet: 'new C();\n// A(C) B(x=10) C(x=10)',
    tags: ['constructor-order', 'getClass', 'inheritance-chain'],
  },
  {
    id: 'java-oop-l4-055',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the difference between Class.forName() and ClassLoader.loadClass()?',
    options: [
      'ClassLoader.loadClass() is faster',
      'Class.forName() loads and initializes the class (runs static initializers); ClassLoader.loadClass() only loads without initialization',
      'Class.forName() is deprecated',
      'They are identical',
    ],
    correctAnswer: 1,
    explanation: 'Class.forName("MyClass") loads the class AND runs its static initializers (initialization). ClassLoader.loadClass("MyClass") loads the class but defers initialization until the class is first used. This matters for JDBC driver registration, etc.',
    tags: ['Class.forName', 'ClassLoader', 'class-loading'],
  },
  {
    id: 'java-oop-l4-056',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the impact of JIT inlining on polymorphic method calls?',
    options: [
      'The JIT can inline monomorphic/bimorphic calls and devirtualize them for better performance; megamorphic calls fall back to vtable lookup',
      'JIT always inlines all calls',
      'Inlining is only for static methods',
      'JIT cannot inline polymorphic calls',
    ],
    correctAnswer: 0,
    explanation: 'The JIT compiler profiles call sites. For monomorphic sites (always one type), it devirtualizes and inlines the call. For bimorphic (2 types), it uses a conditional branch. Megamorphic (3+ types) falls back to vtable/itable lookup, which is slower.',
    tags: ['JIT', 'inlining', 'devirtualization', 'performance'],
  },
  {
    id: 'java-oop-l4-057',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the difference between writeObject/readObject and writeReplace/readResolve in serialization?',
    options: [
      'writeReplace is for writing; readResolve is for reading',
      'They are the same',
      'One is for XML; the other for binary',
      'writeObject/readObject customize how the current object is serialized; writeReplace/readResolve substitute a different object entirely',
    ],
    correctAnswer: 3,
    explanation: 'writeObject/readObject customize the serialization of the current object (field-level control). writeReplace returns a substitute object to serialize instead of this. readResolve returns a substitute for the deserialized object. They serve different purposes.',
    tags: ['serialization', 'writeObject', 'writeReplace', 'customization'],
  },
  {
    id: 'java-oop-l4-058',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the "Prefer composition over inheritance" principle in the context of the Decorator pattern vs subclassing?',
    options: [
      'Decorators are slower than subclassing',
      'Always use subclassing',
      'Use inheritance when you need to add behavior',
      'Decorators use composition to add behavior dynamically without the exponential class explosion that inheritance would require for all combinations',
    ],
    correctAnswer: 3,
    explanation: 'With N behaviors and inheritance, you need 2^N classes for all combinations. Decorators compose behaviors at runtime: you can stack any combination. Java I/O streams are a classic example: BufferedInputStream(FileInputStream(new File(...))).',
    tags: ['decorator', 'composition', 'class-explosion'],
  },
  {
    id: 'java-oop-l4-059',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the "Self Problem" in inheritance-based frameworks?',
    options: [
      'A problem with the "this" keyword',
      'When a class references itself',
      'When a wrapper/decorator overrides a method, but the wrapped object\'s self-calls bypass the wrapper, calling its own methods instead',
      'A problem with self-referencing fields',
    ],
    correctAnswer: 2,
    explanation: 'In the Self Problem, when object A wraps object B and overrides method m, if B internally calls m on itself, it calls its own m, not A\'s override. This breaks the decorator/wrapper pattern for self-use. Composition suffers from this but inheritance doesn\'t (dynamic dispatch works on this).',
    tags: ['self-problem', 'decorator', 'wrapper'],
  },
  {
    id: 'java-oop-l4-060',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the output?\n\nclass A {\n  { System.out.print("iA "); }\n  static { System.out.print("sA "); }\n  A() { System.out.print("cA "); }\n}\nclass B extends A {\n  { System.out.print("iB "); }\n  static { System.out.print("sB "); }\n  B() { System.out.print("cB "); }\n}\nSystem.out.println("Start");\nB b = new B();',
    options: [
      'Start sA sB iA cA iB cB',
      'sA sB Start iA cA iB cB',
      'sA sB iA cA iB cB Start',
      'Start iA cA iB cB sA sB',
    ],
    correctAnswer: 0,
    explanation: 'The println("Start") comes first since the classes are not loaded yet. When new B() is called, classes are loaded (sA sB), then instance init and constructors run (iA cA iB cB). Classes are loaded on first use.',
    codeSnippet: '// Output: Start sA sB iA cA iB cB',
    tags: ['initialization-order', 'class-loading', 'lazy-loading'],
  },
  {
    id: 'java-oop-l4-061',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'SOLID Principles',
    question: 'How does the Hexagonal Architecture (Ports and Adapters) embody SOLID principles?',
    options: [
      'It only uses SRP',
      'It only uses OCP',
      'It violates SOLID',
      'It uses DIP (domain depends on abstractions/ports), OCP (new adapters without modifying domain), ISP (small port interfaces), and SRP (separate adapters for each concern)',
    ],
    correctAnswer: 3,
    explanation: 'Hexagonal architecture: ports are interfaces (ISP - small interfaces), adapters implement ports (OCP - new adapters don\'t change domain), domain depends on ports not adapters (DIP), and each adapter has one responsibility (SRP).',
    tags: ['hexagonal-architecture', 'SOLID', 'architecture'],
  },
  {
    id: 'java-oop-l4-062',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the "Telescoping Constructor Anti-pattern"?',
    options: [
      'Having multiple constructors where each adds one more parameter, leading to hard-to-read code with many constructor overloads',
      'Constructors that call themselves recursively',
      'A pattern for creating deep inheritance chains',
      'A pattern for constructor chaining',
    ],
    correctAnswer: 0,
    explanation: 'The telescoping constructor anti-pattern creates multiple constructors with increasing parameters: ctor(a), ctor(a,b), ctor(a,b,c), etc. Client code becomes hard to read when many parameters are the same type. The Builder pattern is the solution.',
    codeSnippet: '// Anti-pattern:\nnew Pizza(2, true, false, true, false, true);\n\n// Builder solution:\nnew Pizza.Builder(2).cheese(true).pepperoni(false).bacon(true).build();',
    tags: ['telescoping-constructor', 'anti-pattern', 'builder'],
  },
  {
    id: 'java-oop-l4-063',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is the difference between "exports" and "opens" in Java module declarations?',
    options: [
      '"exports" allows compile-time and runtime access to public types; "opens" additionally allows deep reflective access at runtime',
      '"opens" is for testing only',
      '"exports" allows reflection; "opens" does not',
      'They are the same',
    ],
    correctAnswer: 0,
    explanation: '"exports" makes a package\'s public types accessible at compile time and runtime. "opens" additionally allows deep reflective access (setAccessible) to all types in the package at runtime. Frameworks like Spring/Hibernate need "opens" for reflection-based DI and ORM.',
    codeSnippet: 'module my.app {\n  exports com.api;       // public API, compile + runtime\n  opens com.model;       // runtime reflective access for frameworks\n  opens com.config to spring.core; // qualified opens\n}',
    tags: ['modules', 'exports', 'opens', 'reflection'],
  },
  {
    id: 'java-oop-l4-064',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is "dynamic proxy" in Java and how does it enable runtime polymorphism?',
    options: [
      'A design pattern for proxy servers',
      'java.lang.reflect.Proxy creates proxy instances at runtime that implement specified interfaces, dispatching calls to an InvocationHandler',
      'A way to create abstract classes at runtime',
      'A bytecode manipulation technique',
    ],
    correctAnswer: 1,
    explanation: 'java.lang.reflect.Proxy creates runtime implementations of interfaces. All method calls are dispatched to an InvocationHandler. This enables AOP, lazy loading, access control, and logging without concrete class implementations. Used extensively in Spring and Hibernate.',
    codeSnippet: 'MyInterface proxy = (MyInterface) Proxy.newProxyInstance(\n  MyInterface.class.getClassLoader(),\n  new Class<?>[] { MyInterface.class },\n  (proxy1, method, args) -> {\n    System.out.println("Before: " + method.getName());\n    return method.invoke(target, args);\n  }\n);',
    tags: ['dynamic-proxy', 'Proxy', 'InvocationHandler', 'AOP'],
  },
  {
    id: 'java-oop-l4-065',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the impact of generics type erasure on method overriding?',
    options: [
      'Generics improve overriding',
      'No impact',
      'Type erasure can create bridge methods and prevent expected overrides when generic parameters are erased to the same raw type',
      'Type erasure only affects interfaces',
    ],
    correctAnswer: 2,
    explanation: 'After erasure, List<String> and List<Integer> both become List. This can cause: 1) Two methods that appear overloaded become clashes after erasure, 2) The compiler generates bridge methods to maintain polymorphism with erased types.',
    codeSnippet: '// These appear different but clash after erasure:\nvoid process(List<String> list) { }  // → process(List)\nvoid process(List<Integer> list) { }  // → process(List) CLASH!',
    tags: ['type-erasure', 'bridge-methods', 'overriding'],
  },
  {
    id: 'java-oop-l4-066',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the Command pattern and how does it use OOP principles?',
    options: [
      'A pattern for command-line applications',
      'A pattern for sequential method execution',
      'A pattern for executing system commands',
      'A pattern that encapsulates a request as an object, enabling parameterization, queuing, logging, and undo operations',
    ],
    correctAnswer: 3,
    explanation: 'The Command pattern encapsulates a request as an object (encapsulation), implements a common interface (polymorphism), and allows the invoker to work with any command (DIP). This enables undo/redo, macro recording, queuing, and deferred execution.',
    codeSnippet: 'interface Command {\n  void execute();\n  void undo();\n}\nclass AddTextCommand implements Command {\n  private final Document doc;\n  private final String text;\n  public void execute() { doc.append(text); }\n  public void undo() { doc.removeLast(text.length()); }\n}',
    tags: ['command', 'design-patterns', 'encapsulation'],
  },
  {
    id: 'java-oop-l4-067',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the difference between ObjectOutputStream.writeUnshared() and writeObject()?',
    options: [
      'writeObject() remembers objects and writes references for duplicates; writeUnshared() always writes a new copy, ignoring the shared reference mechanism',
      'They are the same',
      'writeObject() cannot handle graphs',
      'writeUnshared() is faster',
    ],
    correctAnswer: 0,
    explanation: 'writeObject() tracks objects: if the same object is written twice, the second write is a reference to the first (preserving object graph). writeUnshared() always writes a fresh copy, which can be important for security-sensitive objects.',
    tags: ['serialization', 'writeUnshared', 'object-graph'],
  },
  {
    id: 'java-oop-l4-068',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the "Polymorphic Enum" pattern?',
    options: [
      'Using enum constants that each provide different implementations of abstract methods, enabling strategy-like behavior with type safety',
      'An enum with generic types',
      'An enum that extends a class',
      'An enum with polymorphic fields',
    ],
    correctAnswer: 0,
    explanation: 'Each enum constant can have a constant-specific class body with its own method implementations. This combines the safety of enums with polymorphic behavior, making it a type-safe alternative to Strategy pattern when the set of strategies is fixed.',
    codeSnippet: 'enum PaymentMethod {\n  CREDIT_CARD {\n    double fee(double amount) { return amount * 0.03; }\n  },\n  DEBIT_CARD {\n    double fee(double amount) { return amount * 0.01; }\n  },\n  CASH {\n    double fee(double amount) { return 0; }\n  };\n  abstract double fee(double amount);\n}',
    tags: ['polymorphic-enum', 'strategy', 'type-safety'],
  },
  {
    id: 'java-oop-l4-069',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is the Capability-based security pattern in Java OOP?',
    options: [
      'Objects themselves serve as unforgeable tokens of authority; holding a reference to an object grants the capabilities that object exposes',
      'Encrypting object fields',
      'Using SecurityManager for all security',
      'Using access modifiers for security',
    ],
    correctAnswer: 0,
    explanation: 'In capability-based security, object references ARE capabilities. If you have a reference to a FileWriter, you can write. If you don\'t have the reference, you can\'t. Encapsulation ensures references can\'t be forged. This is simpler and more composable than ACL-based security.',
    tags: ['capability-security', 'encapsulation', 'security-pattern'],
  },
  {
    id: 'java-oop-l4-070',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the output?\n\nclass Outer {\n  class Inner extends Outer {\n    void test() { System.out.println("Inner.test"); }\n  }\n}\nOuter o = new Outer();\nOuter.Inner i = o.new Inner();\ni.test();',
    options: ['null', 'Runtime error', 'Compilation error', 'Inner.test'],
    correctAnswer: 3,
    explanation: 'An inner class can extend its outer class. The Inner class extends Outer and adds a test() method. Creating an Inner instance requires an Outer instance (o.new Inner()). Calling i.test() prints "Inner.test". This is valid but unusual.',
    codeSnippet: 'Outer.Inner i = o.new Inner();\ni.test(); // "Inner.test"',
    tags: ['inner-classes', 'inheritance', 'unusual-pattern'],
  },
  {
    id: 'java-oop-l4-071',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'SOLID Principles',
    question: 'What is the "Stable Abstractions Principle" (SAP) and how does it relate to SOLID?',
    options: [
      'A package should be as abstract as it is stable; stable packages (depended upon by many) should be abstract, and unstable packages should be concrete',
      'Only stable code should use abstractions',
      'Always use abstract classes',
      'Abstractions should never change',
    ],
    correctAnswer: 0,
    explanation: 'SAP extends DIP to the package level: packages that many others depend on (stable) should consist of abstractions (interfaces/abstract classes) so they\'re flexible. Packages that depend on others (unstable) should contain concrete implementations that can change freely.',
    tags: ['SAP', 'package-principles', 'architecture'],
  },
  {
    id: 'java-oop-l4-072',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the relationship between sealed classes and algebraic data types (ADTs)?',
    options: [
      'ADTs require pattern matching only',
      'Sealed classes prevent ADTs',
      'Sealed classes enable sum types (tagged unions) in Java, which together with records (product types) form algebraic data types',
      'They are unrelated concepts',
    ],
    correctAnswer: 2,
    explanation: 'Records are product types (combination of fields). Sealed interfaces/classes are sum types (one of several alternatives). Together they form algebraic data types. Combined with pattern matching in switch, they enable a functional programming style in Java.',
    codeSnippet: '// Sum type (sealed) + Product types (records) = ADT\nsealed interface Expr permits Lit, Add, Mul {}\nrecord Lit(int n) implements Expr {}\nrecord Add(Expr a, Expr b) implements Expr {}\nrecord Mul(Expr a, Expr b) implements Expr {}',
    tags: ['ADT', 'sealed-classes', 'records', 'sum-types'],
  },
  {
    id: 'java-oop-l4-073',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the "Type-safe Heterogeneous Container" pattern?',
    options: [
      'A container that holds only one type',
      'A pattern for type conversion',
      'A container that uses Class<T> objects as keys to store and retrieve values of different types with type safety',
      'A generic container like List<Object>',
    ],
    correctAnswer: 2,
    explanation: 'This pattern (from Effective Java) uses Class<T> as a type token key in a Map. When putting a value, the Class<T> key ensures type safety. When getting, the key\'s Class is used to cast. Example: a type-safe Preferences store.',
    codeSnippet: 'class TypeSafeMap {\n  private Map<Class<?>, Object> map = new HashMap<>();\n  \n  <T> void put(Class<T> type, T value) {\n    map.put(type, value);\n  }\n  \n  <T> T get(Class<T> type) {\n    return type.cast(map.get(type));\n  }\n}\n// Usage:\nmap.put(String.class, "hello");\nString s = map.get(String.class); // type-safe!',
    tags: ['type-safe-container', 'type-tokens', 'effective-java'],
  },
  {
    id: 'java-oop-l4-074',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the "Null Object" pattern?',
    options: [
      'Checking for null in every method',
      'Setting all objects to null',
      'A pattern for handling NullPointerException',
      'Providing a do-nothing object that implements the expected interface instead of using null references',
    ],
    correctAnswer: 3,
    explanation: 'The Null Object pattern provides an object with neutral ("do nothing") behavior instead of using null. This eliminates null checks throughout the code. The null object implements the same interface and provides benign default behavior.',
    codeSnippet: 'interface Logger {\n  void log(String msg);\n}\nclass ConsoleLogger implements Logger {\n  public void log(String msg) { System.out.println(msg); }\n}\nclass NullLogger implements Logger {\n  public void log(String msg) { /* do nothing */ }\n}',
    tags: ['null-object', 'design-patterns', 'null-safety'],
  },
  {
    id: 'java-oop-l4-075',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is object identity hazard when using WeakReference/SoftReference?',
    options: [
      'The garbage collector can reclaim the object and re-create it, potentially creating a different object with a different identity',
      'References become null',
      'References are always strong',
      'Weak references don\'t exist in Java',
    ],
    correctAnswer: 0,
    explanation: 'With WeakReference, the GC can reclaim the object. If a cache lazily re-creates the object, the new object has a different identity (different == and possibly different identityHashCode). Code relying on object identity (IdentityHashMap, synchronization) may break.',
    tags: ['WeakReference', 'identity', 'garbage-collection'],
  },
  {
    id: 'java-oop-l4-076',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'What is the "double dispatch" technique and why is it needed in Java?',
    options: [
      'Dispatching to two threads',
      'Using two interfaces',
      'Calling a method twice',
      'A technique where method selection depends on the runtime types of two objects, overcoming Java\'s single-dispatch limitation',
    ],
    correctAnswer: 3,
    explanation: 'Java uses single dispatch: the method is chosen based on the receiver\'s runtime type only. Double dispatch selects the method based on two objects\' runtime types. This is implemented via the Visitor pattern: element.accept(visitor) → visitor.visit(element).',
    codeSnippet: '// Single dispatch: only "this" type matters\nshape.intersect(otherShape); // calls Shape\'s intersect\n\n// Double dispatch via Visitor:\nshape.accept(visitor); // first dispatch on shape type\n// Inside accept: visitor.visit(this); // second dispatch on visitor type',
    tags: ['double-dispatch', 'visitor', 'single-dispatch'],
  },
  {
    id: 'java-oop-l4-077',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is the impact of records on the Java Bean convention?',
    options: [
      'Records extend the Bean convention',
      'Records are Beans',
      'Records follow the Bean convention exactly',
      'Records break the Bean convention: accessors use component names (x()) not getX(), records are immutable (no setters), and records have no no-arg constructor',
    ],
    correctAnswer: 3,
    explanation: 'Records deliberately diverge from Java Beans: accessor names match components (point.x() not point.getX()), no setters (immutable), no no-arg constructor. This creates friction with frameworks expecting Beans but promotes better design with immutability.',
    tags: ['records', 'java-beans', 'convention-mismatch'],
  },
  {
    id: 'java-oop-l4-078',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the "Call Super" anti-pattern?',
    options: [
      'Calling super multiple times',
      'Always calling super in constructors',
      'Not calling super at all',
      'Requiring subclasses to call super.method() in their overrides, creating a fragile and error-prone contract',
    ],
    correctAnswer: 3,
    explanation: 'The "Call Super" anti-pattern occurs when a framework requires overriding methods to call super.method(), and forgetting causes subtle bugs. The Template Method pattern is preferred: make the method final and provide abstract hooks that subclasses implement.',
    codeSnippet: '// Anti-pattern: developer may forget super call\nvoid save() {\n  super.save(); // forgetting this breaks the system\n  // custom logic\n}\n\n// Better: Template Method\nfinal void save() { validate(); doSave(); log(); } // fixed algorithm\nabstract void doSave(); // subclass implements this',
    tags: ['call-super', 'anti-pattern', 'template-method'],
  },
  {
    id: 'java-oop-l4-079',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the difference between Composite and Decorator patterns?',
    options: [
      'Decorator creates trees; Composite adds behavior',
      'Both create tree structures',
      'Composite creates tree structures treating individual objects and compositions uniformly; Decorator adds responsibilities to individual objects without subclassing',
      'They are the same',
    ],
    correctAnswer: 2,
    explanation: 'Composite creates part-whole hierarchies (trees) where leaves and composites share the same interface. Decorator wraps a single object to add behavior. Composite focuses on structure, Decorator on adding responsibilities. Both use recursive composition.',
    tags: ['composite', 'decorator', 'comparison'],
  },
  {
    id: 'java-oop-l4-080',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Classes',
    question: 'What is the impact of records on data-oriented programming in Java?',
    options: [
      'Records replace all classes',
      'Records enable modeling data as data (not objects with behavior), supporting pattern matching, destructuring, and algebraic data types',
      'Records make Java fully functional',
      'Records don\'t affect programming paradigms',
    ],
    correctAnswer: 1,
    explanation: 'Records shift Java toward data-oriented programming: data is modeled as plain, immutable records instead of rich objects. Combined with sealed classes (sum types) and pattern matching, this enables a style where data and operations are separated.',
    tags: ['records', 'data-oriented', 'programming-paradigm'],
  },
  {
    id: 'java-oop-l4-081',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Polymorphism',
    question: 'How does Java\'s method dispatch differ from C++ virtual dispatch?',
    options: [
      'Java requires the "virtual" keyword',
      'They are identical',
      'C++ has dynamic dispatch by default',
      'In Java all non-static, non-private, non-final instance methods are virtual by default; C++ requires the "virtual" keyword explicitly',
    ],
    correctAnswer: 3,
    explanation: 'Java makes all instance methods virtual by default (dynamic dispatch). C++ requires explicit "virtual" keyword. Java uses "final" to opt out of virtual dispatch. This means Java has a higher baseline for polymorphism but may have more dispatch overhead.',
    tags: ['java-vs-cpp', 'virtual-dispatch', 'language-comparison'],
  },
  {
    id: 'java-oop-l4-082',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'SOLID Principles',
    question: 'What is the Acyclic Dependencies Principle (ADP)?',
    options: [
      'Classes should have no dependencies',
      'Only acyclic data structures should be used',
      'The dependency graph of packages/modules must not contain cycles; break cycles using DIP or creating a shared abstraction package',
      'Dependencies should be removed periodically',
    ],
    correctAnswer: 2,
    explanation: 'ADP states there should be no circular dependencies between packages. If A depends on B and B depends on A, changes cascade bidirectionally. Fix by: 1) Using DIP to invert one dependency, 2) Extracting a shared abstraction both depend on.',
    tags: ['ADP', 'package-principles', 'circular-dependencies'],
  },
  {
    id: 'java-oop-l4-083',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Encapsulation',
    question: 'What is "broken encapsulation via serialization" and how to prevent it?',
    options: [
      'Serialization doesn\'t affect encapsulation',
      'Serialization bypasses constructors and access modifiers, allowing attackers to create objects in invalid states. Use the serialization proxy pattern or validate in readObject()',
      'Only public fields are affected',
      'Use transient for all fields',
    ],
    correctAnswer: 1,
    explanation: 'Deserialization creates objects without calling constructors, bypassing invariant checks. An attacker can craft malicious byte streams to create objects in invalid states. Solutions: 1) Validate in readObject(), 2) Use serialization proxy pattern, 3) Use record serialization (safer by design).',
    tags: ['serialization', 'encapsulation-breach', 'security'],
  },
  {
    id: 'java-oop-l4-084',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Composition',
    question: 'What is the difference between the Chain of Responsibility and Decorator patterns?',
    options: [
      'Both chain objects, but Chain of Responsibility passes requests along until one handles it (short-circuits), while Decorator always invokes the wrapped object (augments)',
      'They are the same',
      'Decorator short-circuits; Chain of Responsibility augments',
      'Chain of Responsibility uses inheritance',
    ],
    correctAnswer: 0,
    explanation: 'Chain of Responsibility passes a request along a chain of handlers; each handler decides whether to handle or pass to the next (may short-circuit). Decorator always delegates to the wrapped object and adds behavior. The chain vs augmentation distinction is key.',
    tags: ['chain-of-responsibility', 'decorator', 'comparison'],
  },
  {
    id: 'java-oop-l4-085',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Inheritance',
    question: 'What is the output?\n\nclass A {\n  int hashCode() { return 1; }\n}\nclass B extends A {\n  int hashCode() { return 2; }\n}\nA a = new B();\nSystem.out.println(a.hashCode() + " " + System.identityHashCode(a));',
    options: [
      'Compilation error',
      '1 1',
      '2 <identity-hash>',
      '1 <identity-hash>',
    ],
    correctAnswer: 2,
    explanation: 'a.hashCode() uses dynamic dispatch, calling B\'s hashCode() which returns 2. System.identityHashCode() returns the original Object.hashCode() value regardless of overrides, which is based on the object\'s identity.',
    codeSnippet: 'a.hashCode(); // 2 (dynamic dispatch to B)\nSystem.identityHashCode(a); // original identity hash',
    tags: ['hashCode', 'identityHashCode', 'dynamic-dispatch'],
  },
  {
    id: 'java-oop-l4-086',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'Objects',
    question: 'What is the impact of Valhalla\'s "primitive classes" on the identity model of Java objects?',
    options: [
      'Primitive classes (value types) have no identity: you cannot synchronize on them, == compares values not references, and they cannot be null',
      'All classes become primitive',
      'No impact on identity',
      'Identity is removed from all objects',
    ],
    correctAnswer: 0,
    explanation: 'Valhalla\'s value types have no identity: == compares by value, they cannot be synchronized upon (no monitor), they may be allocated on the stack, flattened into arrays, and potentially cannot be null. This fundamentally changes the relationship between identity and state in Java.',
    tags: ['valhalla', 'value-types', 'identity', 'future-java'],
  },
  {
    id: 'java-oop-l4-087',
    language: 'java',
    level: 'level4',
    category: 'OOP',
    subcategory: 'SOLID Principles',
    question: 'How do sealed classes help enforce the Open/Closed Principle in domain modeling?',
    options: [
      'OCP doesn\'t apply to sealed classes',
      'Sealed classes create a closed set of types that is open to new operations via pattern matching, inverting the traditional OCP direction',
      'Sealed classes violate OCP',
      'Sealed classes are always closed',
    ],
    correctAnswer: 1,
    explanation: 'Traditional OCP: closed for modification, open for new types via subclassing. Sealed classes invert this: the type hierarchy is closed (fixed subtypes), but operations are open (add new switch expressions/methods without modifying the types). This is the functional approach to OCP.',
    codeSnippet: '// Type hierarchy is CLOSED:\nsealed interface Shape permits Circle, Rect {}\n\n// Operations are OPEN - add new functions without modifying Shape:\ndouble area(Shape s) { return switch (s) { ... }; }\ndouble perimeter(Shape s) { return switch (s) { ... }; } // new operation!',
    tags: ['sealed-classes', 'OCP', 'domain-modeling'],
  },
];

export default questions;
