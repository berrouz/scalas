import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-branching-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge strategies',
    question: 'What is the `subtree` merge strategy option used for?',
    options: [
      'Merging only specific subdirectories',
      'Adjusting the subtree for the merge by guessing how to shift the tree of one branch to match the other',
      'Merging a git subtree module',
      'Creating a separate history subtree',
    ],
    correctAnswer: 1,
    explanation: 'The `-s subtree` strategy is a variant of `recursive` that shifts one tree to match the prefix of the other, used when merging a project into a subdirectory of another.',
    codeSnippet: 'git merge -s subtree lib-repo',
    tags: ['git', 'merge', 'subtree', 'strategy'],
  },
  {
    id: 'git-branching-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge conflicts',
    question: 'What is the `zdiff3` conflict style and how does it improve on `diff3`?',
    options: [
      'It is an alias for `diff3` in older Git versions',
      'It collapses identical lines in the ancestor and ours/theirs sections to reduce noise',
      'It shows conflicts in a zip-like interleaved format',
      'It requires an external tool to render',
    ],
    correctAnswer: 1,
    explanation: 'The `zdiff3` (zealous diff3) style, available since Git 2.35, reduces the size of conflict markers by removing lines that are the same in all three versions, showing only the truly conflicting portions.',
    codeSnippet: 'git config --global merge.conflictstyle zdiff3',
    tags: ['git', 'conflict', 'zdiff3', 'diff3', 'style'],
  },
  {
    id: 'git-branching-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge commit',
    question: 'What does `git merge --into-name=<name>` control?',
    options: [
      'The name of the merge commit ref',
      'The name used as the "into" branch in the auto-generated merge commit message',
      'The branch that will receive the merge',
      'The tag created at the merge point',
    ],
    correctAnswer: 1,
    explanation: '`--into-name=<name>` changes the name shown in the auto-generated merge message (e.g., "Merge branch \'feature\' into `<name>`"), useful in scripts where HEAD is detached but you want a clean message.',
    codeSnippet: 'git merge --no-ff --into-name=main feature',
    tags: ['git', 'merge', 'into-name', 'message'],
  },
  {
    id: 'git-branching-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'branch internals',
    question: 'Where is a local branch reference physically stored in the `.git` directory?',
    options: [
      '`.git/branches/<name>`',
      '`.git/refs/heads/<name>` (or packed in `.git/packed-refs`)',
      '`.git/objects/<name>`',
      '`.git/remotes/<name>`',
    ],
    correctAnswer: 1,
    explanation: 'Each local branch is stored as a file under `.git/refs/heads/` containing the commit SHA. After running `git pack-refs`, branches may be consolidated into `.git/packed-refs`.',
    codeSnippet: 'cat .git/refs/heads/main',
    tags: ['git', 'branch', 'refs', 'internals', 'packed-refs'],
  },
  {
    id: 'git-branching-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge strategies',
    question: 'You use `-X ours` vs `-s ours` in a merge. What is the critical difference?',
    options: [
      'They produce identical results',
      '`-s ours` discards all incoming changes at the strategy level; `-X ours` uses the recursive strategy but resolves individual conflicts in our favor',
      '`-X ours` discards all incoming changes; `-s ours` resolves conflicts in our favor',
      '`-s ours` is for two-branch merges only; `-X ours` works with octopus',
    ],
    correctAnswer: 1,
    explanation: '`-s ours` completely ignores the other branch\'s content — the resulting tree is 100% ours. `-X ours` still does a proper three-way merge but auto-resolves conflicting hunks by choosing our side.',
    tags: ['git', 'merge', 'ours', '-s', '-X', 'strategy'],
  },
  {
    id: 'git-branching-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge commit',
    question: 'What does `MERGE_HEAD` file in `.git/` represent?',
    options: [
      'The commit hash of the last successful merge',
      'The tip of the branch being merged in (the "theirs" side) during an in-progress merge',
      'The merge base commit hash',
      'The parent commit before the merge started',
    ],
    correctAnswer: 1,
    explanation: 'During a merge operation, Git writes the SHA of the incoming branch\'s tip to `.git/MERGE_HEAD`. This is used to construct the merge commit\'s second parent when you `git commit`.',
    codeSnippet: 'cat .git/MERGE_HEAD',
    tags: ['git', 'MERGE_HEAD', 'internals', 'merge'],
  },
  {
    id: 'git-branching-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'detached HEAD',
    question: 'How long does Git keep commits made in detached HEAD state before garbage collecting them?',
    options: [
      'Immediately when you switch away',
      'Until the next `git gc` run, which by default is at 30 days for unreachable objects',
      'Forever, they are always retained',
      'For exactly 7 days',
    ],
    correctAnswer: 1,
    explanation: 'Unreachable objects (like detached HEAD commits) are retained by the reflog for `gc.reflogExpireUnreachable` days (default 30) before being eligible for garbage collection.',
    tags: ['git', 'HEAD', 'detached', 'gc', 'reflog', 'expire'],
  },
  {
    id: 'git-branching-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge commit',
    question: 'What does `git merge --verify-signatures feature` do?',
    options: [
      'Verifies that all commit messages in the feature branch follow the configured format',
      'Requires all commits in the feature branch to be GPG-signed before merging',
      'Validates the merge commit after it is created',
      'Checks that the feature branch exists on the remote',
    ],
    correctAnswer: 1,
    explanation: '`--verify-signatures` causes Git to check that all commit tips to be merged are GPG-signed by a trusted key. If any commit is unsigned or has an invalid signature, the merge is aborted.',
    codeSnippet: 'git merge --verify-signatures feature',
    tags: ['git', 'merge', 'gpg', 'signatures', 'security'],
  },
  {
    id: 'git-branching-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge strategies',
    question: 'When does Git choose the `ort` strategy over `recursive`?',
    options: [
      'When merging more than two branches',
      '`ort` is the default since Git 2.34 for all two-way merges, replacing `recursive`',
      'Only when `merge.strategy=ort` is configured',
      '`ort` is only used on Linux platforms',
    ],
    correctAnswer: 1,
    explanation: 'Starting with Git 2.34, `ort` (Ostensibly Recursive\'s Twin) became the default strategy. It\'s faster, more correct in edge cases, and does not require a working tree during the merge.',
    tags: ['git', 'merge', 'ort', 'recursive', 'default'],
  },
  {
    id: 'git-branching-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'branch internals',
    question: 'What happens to a branch reference when you run `git pack-refs --all`?',
    options: [
      'All branch refs are deleted',
      'Loose ref files are consolidated into `.git/packed-refs`, and the individual files are removed',
      'The refs are compressed using zlib',
      'All branch names are cached in `.git/info/refs`',
    ],
    correctAnswer: 1,
    explanation: '`git pack-refs --all` moves all loose ref files from `.git/refs/` into the single `.git/packed-refs` text file, reducing the inode count on large repositories.',
    codeSnippet: 'git pack-refs --all',
    tags: ['git', 'pack-refs', 'refs', 'internals', 'performance'],
  },
  {
    id: 'git-branching-l4-011',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge conflicts',
    question: 'What is the purpose of `git merge-tree` (its newer form in Git 2.38+)?',
    options: [
      'Merges git subtree modules',
      'Performs a merge without touching the working tree or index, reporting conflicts to stdout',
      'Shows the tree object created by a merge',
      'Creates a merge commit with a custom tree',
    ],
    correctAnswer: 1,
    explanation: 'The rewritten `git merge-tree --write-tree` (Git 2.38+) can perform a complete three-way merge entirely in-memory, returning the resulting tree hash and any conflict info without modifying the working directory.',
    codeSnippet: 'git merge-tree --write-tree HEAD feature',
    tags: ['git', 'merge-tree', 'in-memory', 'dry-run', 'internals'],
  },
  {
    id: 'git-branching-l4-012',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'detached HEAD',
    question: 'Which file in `.git/` contains HEAD\'s current value, and what format does it use?',
    options: [
      '`.git/BRANCH` — contains the branch name',
      '`.git/HEAD` — contains a symbolic ref like `ref: refs/heads/main`, or a raw SHA for detached HEAD',
      '`.git/refs/HEAD` — contains the commit SHA',
      '`.git/config` — contains the HEAD section',
    ],
    correctAnswer: 1,
    explanation: '`.git/HEAD` stores either `ref: refs/heads/<branch>` (symbolic ref, attached HEAD) or a bare 40-character SHA (detached HEAD). Git reads this file to know the current position.',
    codeSnippet: 'cat .git/HEAD\n# ref: refs/heads/main  (or a SHA when detached)',
    tags: ['git', 'HEAD', 'internals', 'symbolic-ref', 'detached'],
  },
  {
    id: 'git-branching-l4-013',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge commit',
    question: 'You want to merge a branch but record the merge commit with a future date. Which environment variables override the commit timestamps?',
    options: [
      'GIT_AUTHOR_DATE and GIT_COMMITTER_DATE',
      'GIT_MERGE_DATE and GIT_COMMIT_DATE',
      'GIT_DATE only affects the author date',
      'You cannot override timestamps for merge commits',
    ],
    correctAnswer: 0,
    explanation: 'Setting `GIT_AUTHOR_DATE` and `GIT_COMMITTER_DATE` environment variables before `git commit` (or the auto-commit of a merge) overrides both timestamps in the commit object.',
    codeSnippet: 'GIT_COMMITTER_DATE="2025-01-01" git merge --no-ff feature',
    tags: ['git', 'merge', 'date', 'environment', 'timestamp'],
  },
  {
    id: 'git-branching-l4-014',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge strategies',
    question: 'What does `git merge -X patience feature` do?',
    options: [
      'Makes the merge wait for user confirmation before each hunk',
      'Uses the patience diff algorithm for the diff portion of the merge, producing cleaner conflict markers',
      'Adds a 1-second delay between conflict resolutions',
      'Is invalid syntax — patience is not a valid merge strategy option',
    ],
    correctAnswer: 1,
    explanation: 'The `-X diff-algorithm=patience` (or `-X patience` shorthand) option uses the patience diff algorithm during the three-way merge, which can produce cleaner, more meaningful conflict markers.',
    codeSnippet: 'git merge -X patience feature',
    tags: ['git', 'merge', 'patience', 'diff-algorithm'],
  },
  {
    id: 'git-branching-l4-015',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'branch internals',
    question: 'What does `git symbolic-ref HEAD` return and when would it fail?',
    options: [
      'It always returns the current branch name as a path like `refs/heads/main`',
      'It returns the full symbolic ref path, but fails with an error in detached HEAD state',
      'It returns the short branch name like `main`',
      'It returns the SHA of HEAD',
    ],
    correctAnswer: 1,
    explanation: '`git symbolic-ref HEAD` returns the full ref path (e.g., `refs/heads/main`) when HEAD is attached to a branch. In detached HEAD state, it exits with a non-zero code and an error, since HEAD holds a bare SHA.',
    codeSnippet: 'git symbolic-ref HEAD  # exits non-zero if detached',
    tags: ['git', 'symbolic-ref', 'HEAD', 'internals', 'detached'],
  },
  {
    id: 'git-branching-l4-016',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge commit',
    question: 'What does `MERGE_MSG` file in `.git/` contain?',
    options: [
      'The log of all past merge operations',
      'The pre-populated commit message that Git prepares for an in-progress merge',
      'The list of conflicted files',
      'The merge strategy used for the current merge',
    ],
    correctAnswer: 1,
    explanation: 'When a merge is in progress, Git writes the default merge commit message to `.git/MERGE_MSG`. Your editor opens this file when you run `git commit` to finalize the merge.',
    codeSnippet: 'cat .git/MERGE_MSG',
    tags: ['git', 'MERGE_MSG', 'internals', 'merge', 'message'],
  },
  {
    id: 'git-branching-l4-017',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge conflicts',
    question: 'What does `git checkout -m <file>` do to a conflicted file?',
    options: [
      'Marks the file as merged',
      'Re-introduces the conflict markers if they were previously removed',
      'Performs a three-way merge with the merge tool',
      'Moves the file to a merge-pending state',
    ],
    correctAnswer: 1,
    explanation: '`git checkout -m <file>` re-runs the three-way merge on the file, re-creating conflict markers. Useful if you accidentally overwrote a conflicted file and want to start resolution over.',
    codeSnippet: 'git checkout -m src/config.ts',
    tags: ['git', 'checkout', 'conflict', 'markers', '-m'],
  },
  {
    id: 'git-branching-l4-018',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'branch list',
    question: 'What does `git branch --sort=-version:refname` sort branches by?',
    options: [
      'Branch creation time',
      'Version string semantics (semver-aware), newest version first',
      'Alphabetically in reverse',
      'Number of commits in each branch',
    ],
    correctAnswer: 1,
    explanation: '`--sort=-version:refname` sorts branch names using version comparison semantics, so `v1.10` sorts after `v1.9` — useful for tag or branch names following semantic versioning.',
    codeSnippet: 'git branch --sort=-version:refname',
    tags: ['git', 'branch', 'sort', 'version', 'semver'],
  },
  {
    id: 'git-branching-l4-019',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge strategies',
    question: 'In the `ort` strategy, what is the key architectural difference from `recursive` that makes it safer for concurrent use?',
    options: [
      '`ort` uses multi-threading while `recursive` is single-threaded',
      '`ort` performs the merge entirely in memory without touching the working tree or index during computation',
      '`ort` uses a different hash algorithm',
      '`ort` never produces conflict markers',
    ],
    correctAnswer: 1,
    explanation: 'The `ort` strategy computes the merge result entirely in-memory (using tree objects), writing to the working tree only at the end. This makes it safe to use in concurrent scenarios and enables features like `git merge-tree`.',
    tags: ['git', 'merge', 'ort', 'in-memory', 'strategy', 'internals'],
  },
  {
    id: 'git-branching-l4-020',
    language: 'git',
    level: 'level4',
    category: 'Branching & Merging',
    subcategory: 'merge commit',
    question: 'What does `git merge --edit` do, and when is it the default behavior?',
    options: [
      'Edits the source branch before merging',
      'Forces the editor to open for the merge commit message even when there are no conflicts — the default when using `-m` is NOT set',
      'Opens a visual merge editor',
      'Only opens the editor when `--no-ff` is also set',
    ],
    correctAnswer: 1,
    explanation: '`--edit` (or `-e`) forces Git to open the commit message editor for merge commits. By default, Git opens the editor for merge commits unless `--no-edit` is passed or the merge was automated.',
    codeSnippet: 'git merge --no-ff --edit feature',
    tags: ['git', 'merge', 'edit', 'message', 'editor'],
  },
];

export default questions;
