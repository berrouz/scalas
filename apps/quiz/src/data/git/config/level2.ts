import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-config-l2-001',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'aliases advanced',
    question: 'How do you create a Git alias that runs an external shell command (not a git subcommand)?',
    options: [
      'Prefix the alias value with `shell:`',
      'Prefix the alias value with `!`',
      'Use `git config --global alias.cmd "exec <cmd>"`',
      'External commands cannot be used as aliases',
    ],
    correctAnswer: 1,
    explanation: 'When an alias value starts with `!`, Git passes the rest to the shell. This allows complex pipelines: `git config --global alias.ls "!ls -la"`.',
    codeSnippet: 'git config --global alias.root "!pwd"\ngit config --global alias.la "!git log --oneline -20"',
    tags: ['git', 'alias', 'shell', 'config'],
  },
  {
    id: 'git-config-l2-002',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'core.autocrlf',
    question: 'What does `core.autocrlf = input` do, and on which platform is it typically used?',
    options: [
      'Converts LF to CRLF on commit; used on Windows',
      'Converts CRLF to LF on commit but does not convert on checkout; used on macOS/Linux',
      'Disables all line-ending conversion; used on all platforms',
      'Normalises all files to CRLF; used on Windows',
    ],
    correctAnswer: 1,
    explanation: '`input` normalises CRLF → LF when staging but performs no conversion on checkout, keeping the repository clean while not altering working-directory files on Unix systems.',
    codeSnippet: 'git config --global core.autocrlf input',
    tags: ['git', 'config', 'autocrlf', 'input', 'line-endings'],
  },
  {
    id: 'git-config-l2-003',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'commit-msg hook',
    question: 'What argument does Git pass to the `commit-msg` hook, and what is the hook\'s purpose?',
    options: [
      'The branch name; used to enforce branch naming conventions',
      'The path to a temp file containing the proposed commit message; used to validate or rewrite the message',
      'The SHA of the parent commit; used to check if a linear history is maintained',
      'The staged file list; used to enforce file naming rules',
    ],
    correctAnswer: 1,
    explanation: 'Git passes the path to the file holding the commit message as the first argument. The hook can read, validate, or modify the message — exiting non-zero aborts the commit.',
    codeSnippet: '#!/bin/sh\n# .git/hooks/commit-msg\nmsg=\$(cat "\$1")\necho "\$msg" | grep -qE "^(feat|fix|chore):" || exit 1',
    tags: ['git', 'hooks', 'commit-msg', 'validation'],
  },
  {
    id: 'git-config-l2-004',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'pre-push hook',
    question: 'What information is passed to the `pre-push` hook via stdin?',
    options: [
      'The list of staged files',
      'Lines of `<local-ref> <local-sha> <remote-ref> <remote-sha>` for each ref being pushed',
      'The remote URL only',
      'The commit messages of commits being pushed',
    ],
    correctAnswer: 1,
    explanation: 'For each ref to be pushed, Git writes a line containing the local ref name, local SHA, remote ref name, and remote SHA to the hook\'s stdin, enabling the hook to inspect or block specific pushes.',
    codeSnippet: '#!/bin/sh\n# .git/hooks/pre-push\nwhile read local_ref local_sha remote_ref remote_sha; do\n  echo "Pushing \$local_ref"\ndone',
    tags: ['git', 'hooks', 'pre-push', 'stdin'],
  },
  {
    id: 'git-config-l2-005',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'gitattributes LFS',
    question: 'Which `.gitattributes` attribute marks files for Git LFS tracking?',
    options: [
      '*.psd lfs=true',
      '*.psd filter=lfs diff=lfs merge=lfs -text',
      '*.psd track=lfs',
      '*.psd git-lfs',
    ],
    correctAnswer: 1,
    explanation: 'Git LFS uses the `filter`, `diff`, and `merge` attributes set to `lfs` along with `-text` to handle pointer substitution and prevent text diff/merge on binary files.',
    codeSnippet: '# .gitattributes\n*.psd filter=lfs diff=lfs merge=lfs -text',
    tags: ['git', 'gitattributes', 'LFS', 'filter'],
  },
  {
    id: 'git-config-l2-006',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'credential helpers',
    question: 'Which git config key configures the credential helper, and what does the `store` helper do?',
    options: [
      'auth.helper; `store` caches credentials in memory for 15 minutes',
      'credential.helper; `store` saves credentials as plaintext in `~/.git-credentials`',
      'core.credentialHelper; `store` integrates with the OS keychain',
      'remote.credentialHelper; `store` encrypts and stores credentials in the repo',
    ],
    correctAnswer: 1,
    explanation: '`credential.helper = store` persists credentials permanently in plaintext at `~/.git-credentials`. Use `osxkeychain`, `wincred`, or `libsecret` for secure OS-keychain storage.',
    codeSnippet: 'git config --global credential.helper store',
    tags: ['git', 'credential', 'helper', 'config'],
  },
  {
    id: 'git-config-l2-007',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'post-merge hook',
    question: 'When does the `post-merge` hook fire, and what is a common use case?',
    options: [
      'Before a merge commit is created; used to lint code before merging',
      'After a successful merge completes; commonly used to run `npm install` when package.json changed',
      'After a rebase finishes; used to update submodules',
      'After every fetch; used to sync remote tags',
    ],
    correctAnswer: 1,
    explanation: '`post-merge` fires after a successful `git merge` or `git pull --merge`. Teams use it to automatically install dependencies when lock files change.',
    codeSnippet: '#!/bin/sh\n# .git/hooks/post-merge\ngit diff-tree -r --name-only --no-commit-id ORIG_HEAD HEAD | grep package-lock.json && npm install',
    tags: ['git', 'hooks', 'post-merge', 'npm'],
  },
  {
    id: 'git-config-l2-008',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'sparse checkout',
    question: 'Which command initialises a cone-mode sparse checkout, limiting the working tree to specified directories?',
    options: [
      'git sparse-checkout init --cone',
      'git checkout --sparse --cone',
      'git config core.sparsecheckout true && git sparse-checkout set --cone',
      'git clone --sparse --cone',
    ],
    correctAnswer: 0,
    explanation: '`git sparse-checkout init --cone` enables cone mode, which restricts the working tree to the repo root plus explicitly added subdirectories, and is significantly faster than pattern mode.',
    codeSnippet: 'git sparse-checkout init --cone\ngit sparse-checkout set src tests',
    tags: ['git', 'sparse-checkout', 'cone', 'config'],
  },
  {
    id: 'git-config-l2-009',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'config list',
    question: 'How do you list all effective configuration values and their source files?',
    options: [
      'git config --all',
      'git config --list --show-origin',
      'git config --dump',
      'git config --verbose',
    ],
    correctAnswer: 1,
    explanation: '`git config --list --show-origin` prints every key=value pair with a prefix showing which config file it came from, making it easy to diagnose precedence issues.',
    codeSnippet: 'git config --list --show-origin --show-scope',
    tags: ['git', 'config', 'list', 'show-origin'],
  },
  {
    id: 'git-config-l2-010',
    language: 'git',
    level: 'level2',
    category: 'Advanced Config',
    subcategory: 'gitattributes diff',
    question: 'How do you configure a custom diff driver for `.docx` files in `.gitattributes`?',
    options: [
      '*.docx diff=word',
      '*.docx diff-driver=docx',
      '*.docx custom-diff=word2txt',
      '*.docx -diff +textconv=docx2txt',
    ],
    correctAnswer: 0,
    explanation: 'You assign a named diff driver with `*.docx diff=word` in `.gitattributes`, then configure the driver in git config with `[diff "word"] textconv = catdoc` (or similar converter).',
    codeSnippet: '# .gitattributes\n*.docx diff=word\n\n# .gitconfig\n[diff "word"]\n  textconv = catdoc',
    tags: ['git', 'gitattributes', 'diff', 'textconv', 'custom'],
  },
];

export default questions;
