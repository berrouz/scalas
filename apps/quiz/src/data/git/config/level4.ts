import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-config-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'config internals',
    question: 'Git config files use an INI-like format. What is the exact rule for multi-valued keys (keys that appear multiple times in the same section)?',
    options: [
      'The last occurrence wins and earlier values are ignored',
      'The first occurrence wins; duplicates are ignored',
      'All values are retained and returned as a list; `git config --get-all` retrieves them all',
      'Duplicate keys cause a parse error',
    ],
    correctAnswer: 2,
    explanation: 'Git config supports multi-value keys (e.g. multiple `url` values in `[remote]`). `git config --get` returns the last value; `git config --get-all` returns all. `--add` appends a new value without removing existing ones.',
    codeSnippet: 'git config --get-all remote.origin.url\ngit config --add remote.origin.url https://mirror.example.com/repo.git',
    tags: ['git', 'config', 'multi-value', 'get-all', 'internals'],
  },
  {
    id: 'git-config-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'safe.directory',
    question: 'Why did Git 2.35.2 introduce `safe.directory`, and what does it protect against?',
    options: [
      'It prevents running git commands in world-writable directories to block privilege escalation via hook injection',
      'It whitelists directories where git gc is allowed to prune objects',
      'It restricts which directories can store global config files',
      'It marks directories as safe for sparse-checkout expansion',
    ],
    correctAnswer: 0,
    explanation: 'CVE-2022-24765: a non-privileged user could create a `.git` directory in a shared writable path, tricking a privileged user\'s git into executing attacker-controlled hooks. `safe.directory` requires explicit opt-in for repos owned by a different UID.',
    codeSnippet: 'git config --global safe.directory /opt/shared-repo',
    tags: ['git', 'config', 'safe.directory', 'security', 'CVE'],
  },
  {
    id: 'git-config-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'filter driver internals',
    question: 'How does a Git clean/smudge filter driver interact with `git add` and `git checkout` at the plumbing level?',
    options: [
      'The clean filter is invoked on `git checkout`, the smudge filter on `git add`',
      'The clean filter transforms content as it enters the index (on `git add`); the smudge filter transforms it as it leaves the index (on `git checkout`)',
      'Both filters run at commit time to normalise the tree',
      'The filters are applied only during `git diff` to show canonical content',
    ],
    correctAnswer: 1,
    explanation: 'Clean runs working-tree content through a transform before staging (normalisation, LFS pointer substitution). Smudge runs the reverse transform when populating the working tree from the index.',
    codeSnippet: '# .gitattributes\n*.enc filter=encrypt\n\n# git config\n[filter "encrypt"]\n  clean = encrypt.sh\n  smudge = decrypt.sh',
    tags: ['git', 'filter', 'clean', 'smudge', 'gitattributes', 'internals'],
  },
  {
    id: 'git-config-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'protocol.allow',
    question: 'What security risk does `protocol.allow = always` pose, and what is the safe alternative?',
    options: [
      'It enables unencrypted HTTP, exposing credentials; use `protocol.https.allow = always` instead',
      'It permits any protocol including `ext::` which can execute arbitrary shell commands; use per-protocol allows like `protocol.https.allow = always`',
      'It disables signature verification for all protocols; configure `gpg.minTrustLevel` instead',
      'It allows pushing to any remote without authentication; use `receive.denyNonFastForwards`',
    ],
    correctAnswer: 1,
    explanation: 'The `ext::` protocol lets a URL spawn an arbitrary shell command. Setting `protocol.allow = always` (or forgetting to restrict it) lets a malicious `.gitmodules` or `repo URL` execute code. Allowlist only the protocols you need.',
    codeSnippet: 'git config --global protocol.ext.allow never\ngit config --global protocol.https.allow always',
    tags: ['git', 'config', 'protocol.allow', 'security', 'ext'],
  },
  {
    id: 'git-config-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'hooks reference-transaction',
    question: 'What is the `reference-transaction` hook introduced in Git 2.28, and when does it fire?',
    options: [
      'It fires before and after every ref update (create, delete, update), enabling atomic auditing or enforcement of ref policies without the per-ref overhead of `update` hooks',
      'It fires only during `git fetch` to validate remote refs',
      'It fires when the packed-refs file is rewritten by git gc',
      'It fires during `git clone` to initialise the ref namespace',
    ],
    correctAnswer: 0,
    explanation: 'The `reference-transaction` hook receives all ref updates for a single transaction at once (prepared/committed/aborted states), making it efficient for audit logging and policy enforcement compared to separate `pre-receive`/`update` hooks.',
    codeSnippet: '#!/bin/sh\n# .git/hooks/reference-transaction\nwhile read old new ref; do\n  echo "\$ref: \$old -> \$new"\ndone',
    tags: ['git', 'hooks', 'reference-transaction', 'atomic', 'audit'],
  },
  {
    id: 'git-config-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'sparse checkout partial clone',
    question: 'How does combining `--filter=blob:none` (partial clone) with `--sparse` (sparse checkout) improve performance for monorepos?',
    options: [
      'They are mutually exclusive and cannot be combined',
      'Partial clone delays blob fetching until files are accessed; sparse checkout limits which paths are materialised — together only blobs in the cone are ever downloaded',
      'Sparse checkout re-enables blobs that partial clone filtered out',
      'The combination automatically enables the commit-graph and bitmap index',
    ],
    correctAnswer: 1,
    explanation: 'With partial clone, blobs outside the sparse cone are never downloaded (lazy fetched on demand). With sparse checkout, those paths are not even written to the working tree. Together, checkout time and network usage scale with the cone size, not the monorepo size.',
    codeSnippet: 'git clone --filter=blob:none --sparse <url>\ngit sparse-checkout set src/my-service',
    tags: ['git', 'partial-clone', 'sparse-checkout', 'monorepo', 'performance'],
  },
  {
    id: 'git-config-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'config worktree',
    question: 'When using `git worktree add`, which config scope is specific to each worktree and where is it stored?',
    options: [
      'There is no per-worktree config; all worktrees share the local config',
      'Per-worktree config is stored in `.git/worktrees/<id>/config.worktree` and has higher precedence than local config',
      'Per-worktree config is stored in `.git/worktrees/<id>/.git/config`',
      'Per-worktree config is an environment variable `GIT_CONFIG_WORKTREE`',
    ],
    correctAnswer: 1,
    explanation: 'Git 2.20+ supports a worktree-specific config scope at `.git/worktrees/<id>/config.worktree`. Config keys like `core.worktree` and `core.sparseCheckout` are worktree-specific by design.',
    codeSnippet: 'cat .git/worktrees/my-branch/config.worktree',
    tags: ['git', 'config', 'worktree', 'scope', 'advanced'],
  },
  {
    id: 'git-config-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'gitattributes ident',
    question: 'What does the `ident` attribute in `.gitattributes` do when checking out a file?',
    options: [
      'Inserts the committer identity (name + email) into the file header',
      'Replaces occurrences of `\$Id\$` in a file with `\$Id: <blob-sha> \$` on checkout, and strips them back to `\$Id\$` on commit',
      'Marks the file as an identity (credentials) file, preventing it from being diffed',
      'Sets the file\'s owner to match the committer\'s username',
    ],
    correctAnswer: 1,
    explanation: '`ident` is a CVS/SVN-style keyword substitution: on checkout `\$Id\$` is expanded to `\$Id: <40-char-blob-sha> \$`; on staging it is collapsed back to `\$Id\$` to keep the repo clean.',
    codeSnippet: '# .gitattributes\n*.c ident',
    tags: ['git', 'gitattributes', 'ident', 'keyword-substitution'],
  },
  {
    id: 'git-config-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'maintenance tasks',
    question: 'What does `git maintenance start` configure, and which tasks does it schedule by default?',
    options: [
      'Schedules a cron/launchd job that runs `gc`, `commit-graph`, `prefetch`, `loose-objects`, and `incremental-repack` at various intervals',
      'Starts a background daemon that continuously monitors for new commits',
      'Sets up a CI pipeline that runs `git fsck` nightly',
      'Configures automatic `git push` on a schedule',
    ],
    correctAnswer: 0,
    explanation: '`git maintenance start` registers background tasks (via cron or OS scheduler) for: hourly `prefetch` (fetch remotes without updating refs) and `commit-graph` updates; daily `loose-objects` consolidation; weekly `incremental-repack`.',
    codeSnippet: 'git maintenance start\ngit maintenance run --task=commit-graph',
    tags: ['git', 'maintenance', 'gc', 'commit-graph', 'schedule', 'config'],
  },
  {
    id: 'git-config-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Advanced Config',
    subcategory: 'transfer.credentialsInUrl',
    question: 'What does `transfer.credentialsInUrl = die` do, and why is it a security best practice?',
    options: [
      'Prevents Git from reading credentials from `~/.git-credentials`',
      'Causes Git to abort with an error if a URL contains an embedded username:password, preventing credentials from appearing in logs and process listings',
      'Encrypts credentials before storing them in the config',
      'Requires two-factor authentication for all remote operations',
    ],
    correctAnswer: 1,
    explanation: 'Embedded credentials in URLs (e.g. `https://user:pass@host`) appear in `git remote -v`, shell history, and server logs. Setting `transfer.credentialsInUrl = die` (Git 2.39+) forces teams to use credential helpers instead.',
    codeSnippet: 'git config --global transfer.credentialsInUrl die',
    tags: ['git', 'config', 'security', 'credentials', 'transfer.credentialsInUrl'],
  },
];

export default questions;
