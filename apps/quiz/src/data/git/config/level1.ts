import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-config-l1-001',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'config scopes',
    question: 'What are the three configuration scopes in Git, from lowest to highest precedence?',
    options: [
      'project → user → system',
      'system → global → local',
      'local → global → system',
      'global → system → local',
    ],
    correctAnswer: 1,
    explanation: 'Git reads config from system (`/etc/gitconfig`), then global (`~/.gitconfig`), then local (`.git/config`). Local overrides global, which overrides system.',
    codeSnippet: 'git config --system ...\ngit config --global ...\ngit config --local ...',
    tags: ['git', 'config', 'system', 'global', 'local', 'precedence'],
  },
  {
    id: 'git-config-l1-002',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'config --global',
    question: 'Where is the `--global` git config file stored on Linux/macOS?',
    options: [
      '/etc/gitconfig',
      '~/.gitconfig (or ~/.config/git/config)',
      '.git/config in the current repo',
      '/usr/local/etc/gitconfig',
    ],
    correctAnswer: 1,
    explanation: 'The global config lives in the user\'s home directory at `~/.gitconfig` (or the XDG path `~/.config/git/config` if it exists). It applies to all repositories for that user.',
    codeSnippet: 'git config --global user.name "Alice"',
    tags: ['git', 'config', 'global', 'path'],
  },
  {
    id: 'git-config-l1-003',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'aliases',
    question: 'How do you create a Git alias `st` that runs `git status`?',
    options: [
      'git alias st status',
      'git config --global alias.st status',
      'git config --global st "git status"',
      'git set alias st=status',
    ],
    correctAnswer: 1,
    explanation: 'Aliases are set under the `[alias]` section in git config. `git config --global alias.st status` adds `st = status` to `~/.gitconfig`.',
    codeSnippet: 'git config --global alias.st status\ngit st  # runs git status',
    tags: ['git', 'config', 'alias'],
  },
  {
    id: 'git-config-l1-004',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'core.editor',
    question: 'Which config key sets the text editor Git uses for commit messages?',
    options: ['git.editor', 'core.editor', 'user.editor', 'commit.editor'],
    correctAnswer: 1,
    explanation: '`core.editor` tells Git which program to launch for interactive text input (commit messages, rebase instructions, etc.). It falls back to the `EDITOR` environment variable if unset.',
    codeSnippet: 'git config --global core.editor "code --wait"',
    tags: ['git', 'config', 'core.editor', 'editor'],
  },
  {
    id: 'git-config-l1-005',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'core.autocrlf',
    question: 'What does setting `core.autocrlf = true` do on Windows?',
    options: [
      'Converts LF to CRLF on checkout; CRLF to LF on commit',
      'Converts CRLF to LF on checkout; LF to CRLF on commit',
      'Strips all line endings from files',
      'Disables line ending conversion completely',
    ],
    correctAnswer: 0,
    explanation: 'With `core.autocrlf = true` (Windows default), Git converts LF → CRLF when checking out files and CRLF → LF when staging/committing, keeping the repository in Unix line endings.',
    codeSnippet: 'git config --global core.autocrlf true',
    tags: ['git', 'config', 'autocrlf', 'line-endings', 'windows'],
  },
  {
    id: 'git-config-l1-006',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'hooks',
    question: 'Where must Git hook scripts be placed to be executed automatically?',
    options: [
      '`.git/scripts/`',
      '`.github/hooks/`',
      '`.git/hooks/`',
      '`hooks/` in the repo root',
    ],
    correctAnswer: 2,
    explanation: 'Git looks for hook scripts in `.git/hooks/`. The files must be executable and named exactly after the hook event (e.g. `pre-commit`, `commit-msg`).',
    codeSnippet: 'ls .git/hooks/\n# pre-commit  commit-msg  pre-push ...',
    tags: ['git', 'hooks', 'config', 'path'],
  },
  {
    id: 'git-config-l1-007',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'hooks',
    question: 'When does the `pre-commit` hook run, and how can you bypass it?',
    options: [
      'Before staging files; bypassed with `git add --no-hooks`',
      'Before the commit message is entered; bypassed with `git commit --no-verify`',
      'After the commit is created; it cannot be bypassed',
      'Before a push; bypassed with `git push --force`',
    ],
    correctAnswer: 1,
    explanation: 'The `pre-commit` hook runs after `git commit` is invoked but before the commit message editor opens. A non-zero exit code aborts the commit. `--no-verify` skips it.',
    codeSnippet: 'git commit --no-verify -m "skip hooks"',
    tags: ['git', 'hooks', 'pre-commit', 'no-verify'],
  },
  {
    id: 'git-config-l1-008',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'merge.tool',
    question: 'Which config key sets the default merge conflict resolution tool?',
    options: ['core.mergetool', 'merge.tool', 'diff.mergetool', 'conflict.tool'],
    correctAnswer: 1,
    explanation: '`merge.tool` specifies the program launched by `git mergetool` to resolve conflicts interactively (e.g. `vimdiff`, `kdiff3`, `opendiff`).',
    codeSnippet: 'git config --global merge.tool vimdiff',
    tags: ['git', 'config', 'merge.tool', 'conflict'],
  },
  {
    id: 'git-config-l1-009',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'config read',
    question: 'How do you read a specific config value, e.g. `user.email`, from the command line?',
    options: [
      'git config show user.email',
      'git config get user.email',
      'git config user.email',
      'git show config user.email',
    ],
    correctAnswer: 2,
    explanation: '`git config <key>` with only a key (no value) reads and prints the effective value after merging all scopes.',
    codeSnippet: 'git config user.email\ngit config --global user.email',
    tags: ['git', 'config', 'read', 'user.email'],
  },
  {
    id: 'git-config-l1-010',
    language: 'git',
    level: 'level1',
    category: 'Advanced Config',
    subcategory: 'gitattributes',
    question: 'What file controls per-path Git attributes like line-ending handling and diff drivers?',
    options: ['.gitignore', '.gitconfig', '.gitattributes', '.gitmeta'],
    correctAnswer: 2,
    explanation: '`.gitattributes` assigns attributes to file patterns (e.g. `*.png binary`, `*.sh text eol=lf`), controlling line-ending conversion, diff drivers, merge strategies, and Git LFS tracking.',
    codeSnippet: '# .gitattributes\n*.png binary\n*.sh text eol=lf',
    tags: ['git', 'gitattributes', 'config', 'attributes'],
  },
];

export default questions;
