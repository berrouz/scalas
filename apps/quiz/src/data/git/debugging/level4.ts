import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-debugging-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git bisect',
    question: 'A git bisect session requires testing a regression across 1 million commits. In the worst case, how many checkouts does bisect need?',
    options: ['1,000,000', '500,000', 'About 20 (log₂ 1,000,000)', 'About 100'],
    correctAnswer: 2,
    explanation: 'Binary search halves the candidate range each step. log₂(1,000,000) ≈ 20, so bisect needs at most ~20 checkouts to isolate the culprit regardless of history size.',
    tags: ['git', 'bisect', 'binary-search', 'complexity'],
  },
  {
    id: 'git-debugging-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git blame internals',
    question: 'git blame tracks which commit last modified each line. What algorithm does Git use internally for this?',
    options: [
      'Longest-common-subsequence diff applied commit by commit',
      'A patience diff-based annotation that walks the commit graph backwards, attributing blame as early as possible',
      'A full text search across all blobs',
      'SHA-1 hashing of individual lines',
    ],
    correctAnswer: 1,
    explanation: 'Git\'s blame algorithm walks the commit graph in reverse, comparing diffs using its internal diff engine, and progressively assigns lines to the earliest commit that introduced them.',
    tags: ['git', 'blame', 'algorithm', 'internals'],
  },
  {
    id: 'git-debugging-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git rerere internals',
    question: 'How does git rerere identify that two conflicts are "the same" when deciding to apply a recorded resolution?',
    options: [
      'It matches the full file path and line numbers',
      'It hashes the conflicting hunks (both sides of the conflict without the middle) as a fingerprint',
      'It compares the author and timestamp of the conflicting commits',
      'It uses the branch names involved in the merge',
    ],
    correctAnswer: 1,
    explanation: 'rerere computes a SHA fingerprint of the conflict\'s "ours" and "theirs" content (ignoring line numbers), so the same logical conflict is recognised even in a different file or position.',
    tags: ['git', 'rerere', 'fingerprint', 'internals'],
  },
  {
    id: 'git-debugging-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'commit archaeology',
    question: 'You need to find every commit across all branches and tags that contains changes to a specific line range of a file, even across renames. Which compound command best achieves this?',
    options: [
      'git log --all -L 10,20:src/service.ts',
      'git log --all --follow -p -- src/service.ts | grep -A5 "line 10"',
      'git blame -L 10,20 --all src/service.ts',
      'git log --all -S "" -- src/service.ts',
    ],
    correctAnswer: 0,
    explanation: 'git log -L <start>,<end>:<file> performs "line-level log" — it tracks the evolution of that exact code block across history, including renames when combined with --follow.',
    codeSnippet: 'git log --all -L 10,30:src/service.ts',
    tags: ['git', 'log', '-L', 'line-log', 'forensics'],
  },
  {
    id: 'git-debugging-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git fsck',
    question: 'After a corruption event, git fsck reports "error: object file ... is empty". What is the correct recovery procedure?',
    options: [
      'Run git gc --prune=all to remove the empty object',
      'Delete the empty object file, then fetch or clone to restore it from the remote, or restore from a backup',
      'Run git fsck --repair to auto-fix it',
      'Run git repack -ad which recreates all objects',
    ],
    correctAnswer: 1,
    explanation: 'An empty object file is an unrecoverable corruption locally; the object must be retrieved from a remote that has an intact copy, by fetching or cloning, after deleting the empty file.',
    codeSnippet: 'rm .git/objects/ab/cdef1234...\ngit fetch --all',
    tags: ['git', 'fsck', 'corruption', 'recovery'],
  },
  {
    id: 'git-debugging-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git log -S',
    question: 'Why does git log -S find only commits that change the occurrence count of a string, and what implication does this have for a refactor that moves code between files in the same commit?',
    options: [
      'It has no implication; all moves are detected',
      'A same-commit move nets zero count change for the whole tree, so the commit does not appear in -S results even though the code moved',
      '-S always detects moves because it operates at the object level',
      'Moving code increases the count, so it always appears',
    ],
    correctAnswer: 1,
    explanation: 'If a string is deleted from one file and added to another in the same commit, the net count change is zero; -S will not show that commit. Use -G with a regex or separate the move into two commits.',
    tags: ['git', 'log', '-S', 'pickaxe', 'edge-case'],
  },
  {
    id: 'git-debugging-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'commit archaeology',
    question: 'What does git log --ancestry-path A..B do differently from git log A..B?',
    options: [
      'It limits to commits on the direct ancestry chain between A and B, excluding side branches that were merged in',
      'It shows commits in both A and B that share the same author',
      'It filters commits by the committer date range between A and B',
      'They are identical',
    ],
    correctAnswer: 0,
    explanation: '--ancestry-path removes merge-introduced commits from side branches that happen to be in the A..B range but are not on the direct path from A to B.',
    codeSnippet: 'git log --ancestry-path v1.0..v1.5 --oneline',
    tags: ['git', 'log', '--ancestry-path', 'history-filtering'],
  },
  {
    id: 'git-debugging-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git rerere',
    question: 'How can you share rerere resolutions between team members working on the same long-lived feature branch?',
    options: [
      'rerere resolutions are automatically synced via git fetch',
      'Copy the .git/rr-cache/ directory into a shared location or commit the rr-cache into a special branch and distribute it via a custom script',
      'Enable git config rerere.share true',
      'Use git rerere sync <remote>',
    ],
    correctAnswer: 1,
    explanation: 'rerere data lives only in .git/rr-cache and is not transferred by fetch/push. Teams share it by versioning the cache content in a dedicated branch or using tooling like git-rr-cache.',
    codeSnippet: '# Share rr-cache via a branch:\ngit add .git/rr-cache && git commit -m "chore: shared rerere cache"',
    tags: ['git', 'rerere', 'sharing', 'team'],
  },
  {
    id: 'git-debugging-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git blame',
    question: 'What is the --ignore-rev flag in git blame and when was it introduced?',
    options: [
      'Introduced in Git 2.23; it skips the specified commit when assigning blame, attributing lines to the previous meaningful change instead',
      'Introduced in Git 2.0; it hides commits by a specific author',
      'Introduced in Git 1.8; it ignores binary files',
      'Introduced in Git 2.30; it skips commits authored in a date range',
    ],
    correctAnswer: 0,
    explanation: 'git blame --ignore-rev <sha> (Git 2.23+) skips a specific commit (e.g., a bulk formatting change) so blame shows the original authors rather than the formatter.',
    codeSnippet: 'git blame --ignore-rev abc123 src/api.ts\n# or use .git-blame-ignore-revs file',
    tags: ['git', 'blame', '--ignore-rev', 'formatting-commits'],
  },
  {
    id: 'git-debugging-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Debugging & Forensics',
    subcategory: 'git bisect',
    question: 'How do you use git bisect to find the first "good" commit (regression introduced by reverting a fix) instead of the first "bad" commit?',
    options: [
      'Swap the meaning: mark the broken state as "good" and the working state as "bad", then interpret the result inverted',
      'Use git bisect new / git bisect old to define the semantics explicitly',
      'Use git bisect --invert',
      'Both A and B are correct',
    ],
    correctAnswer: 3,
    explanation: 'You can invert semantics (bad=working, good=broken) or use the new/old terminology introduced in Git 2.7 which lets you name states semantically without confusion.',
    codeSnippet: 'git bisect start --term-new=fixed --term-old=broken\ngit bisect fixed HEAD\ngit bisect broken v2.0',
    tags: ['git', 'bisect', 'invert', 'new-old'],
  },
];

export default questions;
