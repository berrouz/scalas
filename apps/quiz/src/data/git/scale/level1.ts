import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-scale-l1-001',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'shallow clone',
    question: 'What does git clone --depth 1 do?',
    options: [
      'Clones only the first commit in the repository',
      'Clones only the most recent commit and its tree, without the full history',
      'Clones only branches that are one commit ahead of main',
      'Creates a clone with a single remote branch',
    ],
    correctAnswer: 1,
    explanation: 'A shallow clone fetches the tip commit and its tree but truncates the history, drastically reducing download size and time for CI pipelines that do not need full history.',
    codeSnippet: 'git clone --depth 1 https://github.com/org/repo.git',
    tags: ['git', 'shallow-clone', 'depth', 'performance'],
  },
  {
    id: 'git-scale-l1-002',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'git-lfs',
    question: 'What is Git LFS (Large File Storage) designed to solve?',
    options: [
      'Storing encrypted secrets outside the repository',
      'Replacing large binary files (images, videos, models) in the Git history with lightweight pointers, storing the actual content on a separate LFS server',
      'Compressing text files in the object store',
      'Enabling parallel git push operations',
    ],
    correctAnswer: 1,
    explanation: 'Git LFS keeps large binaries out of the main object store; the repository contains only small pointer files, while the actual data lives on an LFS server and is fetched on demand.',
    codeSnippet: 'git lfs track "*.psd"\ngit lfs track "*.mp4"',
    tags: ['git', 'git-lfs', 'large-files'],
  },
  {
    id: 'git-scale-l1-003',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'sparse-checkout',
    question: 'What does git sparse-checkout allow you to do?',
    options: [
      'Clone a repository without its commit history',
      'Check out only a subset of directories or files from a repository, reducing working tree size',
      'Fetch only specific branches from the remote',
      'Split a monorepo into multiple repositories',
    ],
    correctAnswer: 1,
    explanation: 'Sparse checkout lets developers work on only the parts of the repository they need, leaving the rest absent from the working tree while the full history remains intact.',
    codeSnippet: 'git sparse-checkout init --cone\ngit sparse-checkout set src/payments/',
    tags: ['git', 'sparse-checkout', 'performance'],
  },
  {
    id: 'git-scale-l1-004',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'shallow clone',
    question: 'How do you convert a shallow clone to a full clone?',
    options: [
      'git clone --full',
      'git fetch --unshallow',
      'git pull --depth 0',
      'git fetch --all --tags',
    ],
    correctAnswer: 1,
    explanation: 'git fetch --unshallow downloads the missing history from the remote, converting the shallow clone into a complete clone with full ancestry.',
    codeSnippet: 'git fetch --unshallow',
    tags: ['git', 'shallow-clone', 'unshallow'],
  },
  {
    id: 'git-scale-l1-005',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'git-lfs',
    question: 'Which command registers a file pattern for LFS tracking and what file does it update?',
    options: [
      'git lfs add "*.psd" — updates .gitignore',
      'git lfs track "*.psd" — updates .gitattributes',
      'git lfs enable "*.psd" — updates .lfsconfig',
      'git lfs set "*.psd" — updates .gitmodules',
    ],
    correctAnswer: 1,
    explanation: 'git lfs track writes a filter=lfs entry to .gitattributes; you must commit .gitattributes so the LFS configuration is shared with all collaborators.',
    codeSnippet: 'git lfs track "*.psd"\ngit add .gitattributes',
    tags: ['git', 'git-lfs', 'track', 'gitattributes'],
  },
  {
    id: 'git-scale-l1-006',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'partial clone',
    question: 'What does git clone --filter=blob:none do?',
    options: [
      'Clones without any branches',
      'Creates a blobless clone: the history and trees are fetched but file contents (blobs) are downloaded lazily on demand',
      'Skips all binary files during clone',
      'Clones only the default branch without any blobs or tags',
    ],
    correctAnswer: 1,
    explanation: 'A blobless partial clone fetches the commit graph and tree objects upfront but downloads file contents lazily when a checkout actually touches them, greatly reducing initial clone time.',
    codeSnippet: 'git clone --filter=blob:none https://github.com/org/repo.git',
    tags: ['git', 'partial-clone', 'filter', 'blobless'],
  },
  {
    id: 'git-scale-l1-007',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'repo size management',
    question: 'Which command recompresses and prunes unreachable objects to reduce repository size?',
    options: ['git cleanup', 'git pack', 'git gc', 'git prune'],
    correctAnswer: 2,
    explanation: 'git gc (garbage collection) runs git pack-objects, git prune, and other housekeeping tasks to consolidate loose objects into pack files and remove unreachable objects.',
    codeSnippet: 'git gc --aggressive --prune=now',
    tags: ['git', 'gc', 'repo-size', 'performance'],
  },
  {
    id: 'git-scale-l1-008',
    language: 'git',
    level: 'level1',
    category: 'Performance & Scale',
    subcategory: 'commit-graph',
    question: 'What is the Git commit-graph file and why does it improve performance?',
    options: [
      'A file listing all commit SHAs for fast lookup by date',
      'A binary cache of pre-computed commit metadata (generation numbers, tree SHAs, parent info) that speeds up operations like git log and git merge-base',
      'A visual representation of the commit DAG stored in .git/',
      'A configuration file for graph-based merge strategies',
    ],
    correctAnswer: 1,
    explanation: 'The commit-graph file caches expensive traversal data; commands that walk commit ancestors (log, merge-base, reachability) are dramatically faster because they read from this cache instead of parsing raw objects.',
    codeSnippet: 'git commit-graph write --reachable --changed-paths',
    tags: ['git', 'commit-graph', 'performance', 'cache'],
  },
];

export default questions;
