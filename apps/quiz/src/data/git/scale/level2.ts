import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-scale-l2-001',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'partial clone',
    question: 'What is the difference between --filter=blob:none and --filter=tree:0 in a partial clone?',
    options: [
      'blob:none skips file contents lazily; tree:0 skips both tree and blob objects, fetching them only on checkout — making it the most aggressive (treeless) clone',
      'tree:0 is an alias for blob:none',
      'tree:0 fetches only the root tree; blob:none fetches all trees',
      'Both produce identical clones',
    ],
    correctAnswer: 0,
    explanation: 'A blobless clone (blob:none) fetches trees eagerly; a treeless clone (tree:0) skips both trees and blobs, minimising initial download further but requiring more lazy fetches during checkout.',
    codeSnippet: '# Blobless (most common for development):\ngit clone --filter=blob:none <url>\n# Treeless (CI, read-only usage):\ngit clone --filter=tree:0 <url>',
    tags: ['git', 'partial-clone', 'filter', 'treeless', 'blobless'],
  },
  {
    id: 'git-scale-l2-002',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'git-lfs',
    question: 'How do you migrate existing large files already committed to Git history into LFS without rewriting the repository?',
    options: [
      'You cannot migrate files already in history; only new commits are tracked by LFS',
      'Use git lfs migrate import --include="*.psd" to rewrite history and move matching blobs to LFS storage',
      'Add the files to .gitattributes and they are automatically moved',
      'Delete the files, commit, then re-add them with lfs track',
    ],
    correctAnswer: 1,
    explanation: 'git lfs migrate import rewrites commits to replace matching blobs with LFS pointer files, then you force-push the rewritten history to the remote.',
    codeSnippet: 'git lfs migrate import --include="*.psd,*.mp4" --everything',
    tags: ['git', 'git-lfs', 'migrate', 'history-rewrite'],
  },
  {
    id: 'git-scale-l2-003',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'sparse-checkout',
    question: 'What is cone mode in git sparse-checkout and why is it faster than non-cone mode?',
    options: [
      'Cone mode enables recursive sparse-checkout for submodules',
      'Cone mode restricts patterns to directories only (no arbitrary glob patterns), allowing Git to use a faster algorithm that avoids testing every file against every pattern',
      'Cone mode fetches only the root of the repository',
      'Cone mode is a deprecated alias for partial clone',
    ],
    correctAnswer: 1,
    explanation: 'In cone mode, Git only needs to check whether a path is inside or outside a set of directory cones, which is O(depth) per file; arbitrary glob matching is O(patterns × files).',
    codeSnippet: 'git sparse-checkout init --cone\ngit sparse-checkout add services/auth services/payment',
    tags: ['git', 'sparse-checkout', 'cone-mode', 'performance'],
  },
  {
    id: 'git-scale-l2-004',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'filter-branch vs filter-repo',
    question: 'Why is git filter-repo preferred over git filter-branch for rewriting history?',
    options: [
      'filter-repo is built into Git; filter-branch requires a plugin',
      'filter-repo is dramatically faster, safer (protects against common mistakes), and has a richer API; filter-branch is slow on large repos and has known footguns',
      'filter-branch is deprecated and removed from Git 2.30+',
      'filter-repo supports LFS objects; filter-branch does not',
    ],
    correctAnswer: 1,
    explanation: 'git filter-repo is a third-party Python tool (now recommended in Git docs) that rewrites history 10–100× faster than filter-branch and avoids pitfalls like accidentally rewriting remote refs.',
    codeSnippet: 'git filter-repo --path src/ --path-rename src/:lib/',
    tags: ['git', 'filter-repo', 'filter-branch', 'history-rewrite'],
  },
  {
    id: 'git-scale-l2-005',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'shallow clone',
    question: 'How do you deepen a shallow clone to include more history without fully unshallowing?',
    options: [
      'git fetch --depth=<n> which fetches N more commits from the current shallow boundary',
      'git clone --depth=<n> again in the same directory',
      'git unshallow --depth=<n>',
      'git pull --depth=<n>',
    ],
    correctAnswer: 0,
    explanation: 'git fetch --depth=<n> extends the shallow boundary by N more commits from the remote, allowing selective history expansion without fetching the entire graph.',
    codeSnippet: 'git fetch --depth=50 origin main',
    tags: ['git', 'shallow-clone', 'deepen', 'fetch'],
  },
  {
    id: 'git-scale-l2-006',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'multi-pack-index',
    question: 'What is the multi-pack-index (MIDX) and what problem does it solve?',
    options: [
      'An index for multi-branch repositories',
      'A single index file that covers all pack files in the repository, replacing per-pack .idx files and enabling faster object lookup across many packs',
      'A file that merges multiple repositories into one',
      'An optimisation for repositories with multiple remotes',
    ],
    correctAnswer: 1,
    explanation: 'Without MIDX, Git scans each pack\'s individual .idx for every object lookup; a multi-pack-index provides a unified sorted index, significantly reducing object lookup time in repos with many packs.',
    codeSnippet: 'git multi-pack-index write --bitmap',
    tags: ['git', 'multi-pack-index', 'midx', 'performance'],
  },
  {
    id: 'git-scale-l2-007',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'bfg-repo-cleaner',
    question: 'What is BFG Repo-Cleaner and when would you use it over git filter-repo?',
    options: [
      'BFG is a Git GUI for large repositories; filter-repo is a CLI tool',
      'BFG is a fast JVM tool specialised for removing large files or sensitive data from history; it is simpler to use for those two specific tasks but less flexible than filter-repo for complex rewrites',
      'BFG and filter-repo are identical tools by different vendors',
      'BFG works only with GitHub; filter-repo works with any remote',
    ],
    correctAnswer: 1,
    explanation: 'BFG excels at its two primary tasks (remove large blobs, delete secrets) with a very simple command; filter-repo is more general-purpose but requires more configuration for complex transformations.',
    codeSnippet: '# Remove all files larger than 50MB from history:\nbfg --strip-blobs-bigger-than 50M repo.git',
    tags: ['git', 'bfg', 'history-rewrite', 'large-files'],
  },
  {
    id: 'git-scale-l2-008',
    language: 'git',
    level: 'level2',
    category: 'Performance & Scale',
    subcategory: 'git-lfs',
    question: 'How do you fetch LFS objects for branches you have not yet checked out?',
    options: [
      'git lfs pull --all-branches',
      'git lfs fetch --all',
      'git fetch --lfs --all',
      'git lfs sync --remote',
    ],
    correctAnswer: 1,
    explanation: 'git lfs fetch --all downloads all LFS objects referenced by all refs in the repository, ensuring you have all binary content locally even without checking out each branch.',
    codeSnippet: 'git lfs fetch --all origin',
    tags: ['git', 'git-lfs', 'fetch', 'all-branches'],
  },
];

export default questions;
