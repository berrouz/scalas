import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-scale-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'partial clone internals',
    question: 'How does a partial clone server implement lazy blob fetching without the client cloning the full object store first?',
    options: [
      'The server sends a manifest file listing all missing objects',
      'The server uses the "promised objects" mechanism: it sends placeholder commit/tree objects that reference missing blobs; the client fetches those blobs on-demand via a "lazy fetch" protocol extension when they are first accessed',
      'The client sends a list of needed blobs via HTTP headers',
      'The server uses delta compression to represent missing blobs as empty deltas',
    ],
    correctAnswer: 1,
    explanation: 'The partial clone protocol (promisor remotes) allows the server to omit blobs from the initial packfile; the client stores a promisor marker and fetches missing objects individually via a separate pack-objects request.',
    tags: ['git', 'partial-clone', 'promisor', 'protocol', 'internals'],
  },
  {
    id: 'git-scale-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'Git VFS/GVFS',
    question: 'What is the Scalar tool and how does it improve the developer experience on very large repositories?',
    options: [
      'Scalar is a Git GUI client for enterprise users',
      'Scalar is a set of opinionated Git config defaults and automation (background maintenance, partial clone, sparse-checkout, commit-graph, FSMONITOR) that together make large repos perform well without manual tuning',
      'Scalar is a Git server that shards large repositories',
      'Scalar is a replacement for git-lfs in Microsoft Azure DevOps',
    ],
    correctAnswer: 1,
    explanation: 'Scalar (now integrated into Git) applies best-practice configuration in one command: it enables background maintenance, prefetch, commit-graph updates, and optional sparse-checkout, making large repos usable on commodity hardware.',
    codeSnippet: 'scalar clone https://github.com/org/huge-repo.git',
    tags: ['git', 'scalar', 'large-repos', 'performance', 'configuration'],
  },
  {
    id: 'git-scale-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'commit-graph',
    question: 'What are "changed-path Bloom filters" stored in the commit-graph and which operation do they accelerate?',
    options: [
      'Probabilistic structures recording which file paths were changed in each commit, used to speed up git log -- <path> by skipping commits that definitely did not touch the path',
      'Bloom filters for branch name autocomplete',
      'Checksums for detecting corrupt commit objects',
      'Filters used during pack compression to skip unchanged blobs',
    ],
    correctAnswer: 0,
    explanation: 'Bloom filters in the commit-graph allow git log -- <path> to quickly skip commits that certainly did not modify the path, avoiding expensive tree comparisons for the majority of commits in a large history.',
    codeSnippet: 'git commit-graph write --reachable --changed-paths',
    tags: ['git', 'commit-graph', 'bloom-filter', 'changed-paths', 'performance'],
  },
  {
    id: 'git-scale-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'filter-repo',
    question: 'When using git filter-repo to rewrite history across a repository shared with many collaborators, what is the correct process to minimise disruption?',
    options: [
      'Rewrite locally and push with --force; collaborators run git pull --rebase',
      'Coordinate a maintenance window; rewrite and force-push all refs; all collaborators must re-clone or run git fetch --all + git reset to the new SHAs; old clones with the old SHAs must not push',
      'Push the rewrite to a new branch and use GitHub to switch the default branch',
      'Run the rewrite on the server using GitHub\'s repository maintenance API',
    ],
    correctAnswer: 1,
    explanation: 'History rewriting changes all commit SHAs; existing clones have different SHAs and a merge would re-introduce the erased content. All collaborators must re-clone or hard-reset — this cannot be done gracefully without coordination.',
    tags: ['git', 'filter-repo', 'history-rewrite', 'collaboration', 'process'],
  },
  {
    id: 'git-scale-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'git-lfs',
    question: 'What is the LFS "locking" feature and when is it critical?',
    options: [
      'It encrypts LFS files at rest on the server',
      'It allows users to exclusively lock binary files before editing them, preventing concurrent conflicting edits since binaries cannot be merged — critical for game asset workflows',
      'It prevents LFS objects from being garbage collected on the server',
      'It restricts LFS push to CI accounts only',
    ],
    correctAnswer: 1,
    explanation: 'Binary files like 3D models, audio, or Photoshop files cannot be diff-merged; file locking serialises edits so two artists never produce unresolvable conflicts.',
    codeSnippet: 'git lfs lock assets/hero.fbx\n# edit the file...\ngit lfs unlock assets/hero.fbx',
    tags: ['git', 'git-lfs', 'locking', 'binary-files', 'game-dev'],
  },
  {
    id: 'git-scale-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'multi-pack-index',
    question: 'What is "geometric repacking" and how does it relate to the multi-pack-index?',
    options: [
      'A technique that packs commits in geometric (spiral) order to improve compression',
      'A repacking strategy where pack sizes grow geometrically (each pack is ~2× the previous), keeping the number of packs logarithmic and the MIDX lean — avoiding a full repack on every write',
      'A method for distributing pack files across multiple servers geometrically',
      'A Git internal format using geometric hashing for object IDs',
    ],
    correctAnswer: 1,
    explanation: 'Geometric repacking (git repack -d --geometric=2) maintains a balanced set of packs so MIDX lookups stay fast without requiring expensive full repacks; it is the strategy used by Gitaly and GitHub\'s infrastructure.',
    codeSnippet: 'git repack --write-midx --geometric=2 -d',
    tags: ['git', 'geometric-repack', 'multi-pack-index', 'performance', 'server'],
  },
  {
    id: 'git-scale-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Performance & Scale',
    subcategory: 'repo size management',
    question: 'A repository\'s .git directory is 50 GB despite source code being only 500 MB. What are the most likely root causes and the recommended remediation?',
    options: [
      'Too many branches; remove stale branches and run git gc',
      'Large binary files committed without LFS and/or a history full of large blobs that were later deleted but never purged — remediate by migrating binaries to LFS via git lfs migrate import and then running git filter-repo to remove old blobs, followed by a force-push',
      'Too many pack files; run git repack -a',
      'The commit-graph is taking up space; delete .git/objects/info/commit-graph',
    ],
    correctAnswer: 1,
    explanation: 'Deleted large files still exist as objects in history; Git GC does not remove reachable objects. LFS migration + filter-repo removes the blob objects from history, then a force-push and server-side GC shrinks the remote.',
    codeSnippet: 'git lfs migrate import --include="*.zip,*.jar" --everything\ngit filter-repo --strip-blobs-bigger-than 10M\ngit push --force --all',
    tags: ['git', 'repo-size', 'git-lfs', 'filter-repo', 'large-files'],
  },
];

export default questions;
