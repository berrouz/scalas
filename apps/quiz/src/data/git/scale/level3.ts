import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-scale-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'commit-graph',
    question: 'What are "generation numbers" stored in the commit-graph and why do they speed up reachability queries?',
    options: [
      'The total number of commits in the repository at write time',
      'A topological level assigned to each commit (0 for root commits, max(parents)+1 for others) that allows pruning branches of the walk when a commit\'s generation is already below the target',
      'The SHA prefix length used for abbreviated IDs',
      'The number of files changed by each commit',
    ],
    correctAnswer: 1,
    explanation: 'Generation numbers enable early termination: if commit A has generation 10 and we are looking for an ancestor with generation ≥ 50, we can skip entire sub-trees, making operations like git log A..B O(answer) instead of O(graph).',
    tags: ['git', 'commit-graph', 'generation-numbers', 'reachability'],
  },
  {
    id: 'git-scale-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'filter-repo',
    question: 'How do you use git filter-repo to extract a subdirectory into a new standalone repository while preserving its full history?',
    options: [
      'git filter-repo --subdirectory-filter src/payments',
      'git filter-repo --path src/payments/ --path-rename src/payments/:./',
      'git filter-branch --subdirectory-filter src/payments',
      'Both A and B are correct',
    ],
    correctAnswer: 1,
    explanation: '--path src/payments/ keeps only commits touching that directory and --path-rename moves its files to the root, making the result a standalone repository for that service.',
    codeSnippet: 'git filter-repo --path src/payments/ --path-rename src/payments/:/',
    tags: ['git', 'filter-repo', 'subdirectory', 'monorepo-split'],
  },
  {
    id: 'git-scale-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'sparse-checkout',
    question: 'How do you combine a partial clone (blobless) with sparse-checkout for maximum clone speed in a large monorepo?',
    options: [
      'They are mutually exclusive; you can only use one at a time',
      'Clone with --filter=blob:none then configure sparse-checkout so only the needed directories trigger lazy blob fetches — minimising both initial download and subsequent checkout I/O',
      'Use --filter=sparse:path=<dir> in the clone command',
      'Use git sparse-checkout --no-blobs after a regular clone',
    ],
    correctAnswer: 1,
    explanation: 'Partial clone avoids downloading blobs upfront; sparse-checkout restricts which paths are checked out, so Git only lazily fetches blobs for the sparse paths — both together minimise data transferred.',
    codeSnippet: 'git clone --filter=blob:none --sparse https://github.com/org/monorepo.git\ncd monorepo\ngit sparse-checkout set services/auth',
    tags: ['git', 'sparse-checkout', 'partial-clone', 'monorepo', 'performance'],
  },
  {
    id: 'git-scale-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'Git VFS/GVFS',
    question: 'What is GVFS (Git Virtual File System) / Scalar designed to solve?',
    options: [
      'It provides a virtual overlay for LFS files',
      'It virtualises the working directory so Git only materialises files that are actually accessed, enabling Windows-scale repositories with millions of files (like the Windows OS repo)',
      'It is a Git server implementation that supports larger pack files',
      'It is a distributed version of Git for offline-first workflows',
    ],
    correctAnswer: 1,
    explanation: 'GVFS (now largely superseded by Scalar + native Git features) uses a file system filter driver to project the repository on demand; only accessed files are downloaded and hydrated.',
    tags: ['git', 'gvfs', 'scalar', 'virtual-filesystem', 'large-repos'],
  },
  {
    id: 'git-scale-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'bfg-repo-cleaner',
    question: 'After running BFG or git filter-repo to remove a secret from history, what additional steps are required to fully expunge the object from GitHub?',
    options: [
      'git push --force is sufficient',
      'Force-push all affected refs, then contact GitHub support to run server-side garbage collection, or wait for their maintenance cycle',
      'Run git gc --prune=now locally then push',
      'Delete and recreate the repository',
    ],
    correctAnswer: 1,
    explanation: 'The remote caches objects referenced by old refs; you must force-push rewritten refs and ask the hosting platform to expire caches and run GC on their servers to fully purge the secret.',
    codeSnippet: 'git push origin --force --all\ngit push origin --force --tags\n# Then: contact GitHub support to purge caches',
    tags: ['git', 'bfg', 'filter-repo', 'secret-removal', 'github'],
  },
  {
    id: 'git-scale-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'multi-pack-index',
    question: 'How does a reachability bitmap in a Git pack file improve performance for server-side operations like git clone?',
    options: [
      'Bitmaps compress pack files by removing duplicate objects',
      'Bitmaps store pre-computed reachability sets for commits; when packing objects for a clone, the server can use bitwise OR to compute the object set quickly instead of traversing the graph',
      'Bitmaps are used to verify SHA-1 integrity during transfer',
      'Bitmaps accelerate blame operations on the server',
    ],
    correctAnswer: 1,
    explanation: 'Each bitmap entry represents the full set of objects reachable from a commit as a bit vector; a clone can compute the answer with bitwise operations in microseconds rather than a full graph walk.',
    codeSnippet: 'git multi-pack-index write --bitmap',
    tags: ['git', 'bitmap', 'pack', 'performance', 'server'],
  },
  {
    id: 'git-scale-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Performance & Scale',
    subcategory: 'repo size management',
    question: 'What git configuration option enables automatic write of the commit-graph on fetch and gc operations?',
    options: [
      'core.commitGraph = true',
      'fetch.writeCommitGraph = true',
      'gc.commitGraphExpire = 0',
      'pack.writeCommitGraph = true',
    ],
    correctAnswer: 1,
    explanation: 'Setting fetch.writeCommitGraph=true causes Git to update the commit-graph file automatically after every fetch, keeping the performance cache fresh without manual invocations.',
    codeSnippet: 'git config --global fetch.writeCommitGraph true',
    tags: ['git', 'commit-graph', 'configuration', 'fetch'],
  },
];

export default questions;
