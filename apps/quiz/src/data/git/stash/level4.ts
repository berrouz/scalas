import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-stash-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'stash push',
    question: 'A stash entry created with `git stash -u` has three parent commits. What does each parent represent?',
    options: [
      'Parent 1 = previous stash, Parent 2 = branch tip, Parent 3 = index',
      'Parent 1 = HEAD commit, Parent 2 = index (staged state), Parent 3 = untracked files commit',
      'Parent 1 = merge base, Parent 2 = ours, Parent 3 = theirs',
      'Parent 1 = index, Parent 2 = working tree, Parent 3 = HEAD',
    ],
    correctAnswer: 1,
    explanation: 'The stash commit\'s first parent is HEAD (where the branch was), second parent is the index state (as a tree/commit), and third parent (with `-u`) is a commit containing the untracked files. This structure lets `git stash apply` reconstruct all three states.',
    codeSnippet: 'git cat-file -p stash@{0}  # shows 3 parents with -u',
    tags: ['git', 'stash', 'internals', 'parents', 'untracked'],
  },
  {
    id: 'git-stash-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'Where does Git store the administrative data for linked worktrees?',
    options: [
      'In the worktree directory itself under `.git/`',
      'In the main repository under `.git/worktrees/<name>/`',
      'In `~/.config/git/worktrees/`',
      'In `.gitmodules`',
    ],
    correctAnswer: 1,
    explanation: 'Each linked worktree has a subdirectory under `.git/worktrees/<name>/` in the main repository containing `gitdir` (points to the worktree), `HEAD`, `index`, `commondir`, and optionally `locked`.',
    codeSnippet: 'ls .git/worktrees/',
    tags: ['git', 'worktree', 'internals', 'administrative', 'gitdir'],
  },
  {
    id: 'git-stash-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'stash list',
    question: 'What is the actual Git ref that stores all stash entries, and how is the stash list maintained?',
    options: [
      '`refs/stash` holds only the latest entry; older entries are in `refs/stash-list`',
      '`refs/stash` holds the latest entry; the full history is in `logs/refs/stash` (the reflog)',
      'Each stash is stored as `refs/stash@{n}`',
      'Stashes are stored in `refs/hidden/stash`',
    ],
    correctAnswer: 1,
    explanation: '`refs/stash` always points to the most recent stash commit. The entire stash stack is the reflog of this ref, stored in `.git/logs/refs/stash`. Each `stash@{n}` is a reflog entry.',
    codeSnippet: 'cat .git/refs/stash\ncat .git/logs/refs/stash',
    tags: ['git', 'stash', 'refs', 'reflog', 'internals'],
  },
  {
    id: 'git-stash-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'A linked worktree has its own `.git` — what is the content of that file and why?',
    options: [
      'It is a full copy of the `.git` directory',
      'It is a `gitfile` (text file) containing `gitdir: <path-to-main-repo>/.git/worktrees/<name>`, pointing back to the administrative data in the main repo',
      'It is a symlink to the main `.git` directory',
      'It contains only the `HEAD` and `index` files',
    ],
    correctAnswer: 1,
    explanation: 'Each linked worktree has a `.git` gitfile (not a directory) that points to its administrative data under the main repo\'s `.git/worktrees/<name>/`. This keeps the worktree\'s HEAD and index separate while sharing the object database.',
    codeSnippet: 'cat ../hotfix/.git\n# gitdir: /path/to/main/.git/worktrees/hotfix',
    tags: ['git', 'worktree', 'gitfile', 'internals', 'gitdir'],
  },
  {
    id: 'git-stash-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'stash pop vs apply',
    question: 'When `git stash pop` has a conflict, the stash is NOT dropped. What is the exact state of the stash stack and working tree?',
    options: [
      'The stash is reverted and nothing changes',
      'The working tree has conflict markers, the index has conflicting entries at stages 2 and 3, and the stash entry remains at stash@{0}',
      'The working tree has the stash\'s changes applied cleanly and the stash is in a pending state',
      'Only the non-conflicting parts are applied and the stash is partially dropped',
    ],
    correctAnswer: 1,
    explanation: 'On a pop conflict: the working tree shows conflict markers, the index has multiple stage entries (2=ours, 3=theirs) for conflicted files, and the stash entry remains so you can re-apply after fixing.',
    tags: ['git', 'stash', 'pop', 'conflict', 'stages', 'index'],
  },
  {
    id: 'git-stash-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'What is the `commondir` file in `.git/worktrees/<name>/` used for?',
    options: [
      'It specifies the shared configuration file',
      'It points to the main repository\'s `.git/` directory so the worktree can find shared objects, refs, and config',
      'It lists all worktrees that share the object database',
      'It is the lockfile for concurrent operations',
    ],
    correctAnswer: 1,
    explanation: '`commondir` contains the path to the main `.git/` directory. Git uses it to locate the object store, refs, config, and hooks shared across all worktrees of the repository.',
    codeSnippet: 'cat .git/worktrees/hotfix/commondir\n# ../..  (relative path to main .git)',
    tags: ['git', 'worktree', 'commondir', 'internals', 'object-store'],
  },
  {
    id: 'git-stash-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'stash specific files',
    question: 'You stash specific files with `git stash push -- src/a.ts`. The other changed files are left unstaged. After `git stash pop`, what is the state?',
    options: [
      'Only src/a.ts changes are restored; the other files remain as you left them',
      'All files including those you did not stash are also popped back',
      'The other files are overwritten with the stash\'s versions',
      'Git refuses to pop if there are other uncommitted changes',
    ],
    correctAnswer: 0,
    explanation: 'Popping a partial stash restores only the files that were stashed. Files that were not stashed remain in whatever state you left them in the working tree.',
    tags: ['git', 'stash', 'partial', 'specific-files', 'pop'],
  },
  {
    id: 'git-stash-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'worktree remove',
    question: 'What is the difference between `git worktree remove` and manually deleting the worktree directory?',
    options: [
      'They are equivalent',
      '`git worktree remove` also cleans up `.git/worktrees/<name>/` in the main repo; a manual delete leaves stale administrative data',
      '`git worktree remove` also deletes the branch; a manual delete does not',
      'Manual delete is faster; `git worktree remove` also runs `git gc`',
    ],
    correctAnswer: 1,
    explanation: '`git worktree remove` removes the worktree directory AND its administrative files under `.git/worktrees/<name>/`. Deleting manually leaves orphaned admin data requiring `git worktree prune` to clean up.',
    codeSnippet: 'git worktree remove ../hotfix\n# vs rm -rf ../hotfix && git worktree prune',
    tags: ['git', 'worktree', 'remove', 'cleanup', 'admin'],
  },
  {
    id: 'git-stash-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'stash push',
    question: 'You have staged some changes and unstaged others. After `git stash push --keep-index`, what does the working tree and index contain?',
    options: [
      'Nothing — everything is stashed',
      'Index contains the originally staged changes; working tree has ONLY those staged changes (unstaged changes are stashed and removed)',
      'Index is empty; working tree has all original changes',
      'Index has staged changes; working tree retains both staged and unstaged',
    ],
    correctAnswer: 1,
    explanation: '`--keep-index` stashes all changes but then immediately re-applies the staged state. Result: the index is restored to what you had staged, the unstaged changes are removed from the working tree (they are in the stash).',
    tags: ['git', 'stash', 'keep-index', 'staged', 'working-tree'],
  },
  {
    id: 'git-stash-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'Can a bare repository have linked worktrees, and what is the practical use case?',
    options: [
      'No, bare repositories cannot have worktrees',
      'Yes — this is the canonical way to use a bare repository as a central hub with multiple worktrees, avoiding constant branch switching',
      'Only if the bare repo was created with `--worktree` flag',
      'Bare repos support worktrees but they must be on the same filesystem',
    ],
    correctAnswer: 1,
    explanation: 'A bare repo can serve as a hub with linked worktrees for each active branch. This pattern (popularized by ThePrimeagen) replaces branch switching entirely — each branch gets its own directory.',
    codeSnippet: 'git clone --bare <url> repo.git\ncd repo.git\ngit worktree add ../main main\ngit worktree add ../feature feature',
    tags: ['git', 'worktree', 'bare', 'workflow', 'advanced'],
  },
];

export default questions;
