import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-stash-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'stash push',
    question: 'How is the stash implemented internally in Git?',
    options: [
      'As a hidden branch called `stash`',
      'As commit objects attached to `refs/stash`, with a special two- or three-parent merge commit per stash entry',
      'As patch files in `.git/stash/`',
      'As blob objects stored in the pack files',
    ],
    correctAnswer: 1,
    explanation: 'Each stash entry is a commit pointed to by `refs/stash`, with parent 1 being HEAD, parent 2 the index state, and (with `-u`) parent 3 for untracked files. The reflog of `refs/stash` holds all entries.',
    codeSnippet: 'git cat-file -p stash@{0}',
    tags: ['git', 'stash', 'internals', 'commit', 'refs'],
  },
  {
    id: 'git-stash-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'stash pop vs apply',
    question: 'What does `git stash apply --index` do that plain `git stash apply` does not?',
    options: [
      'Applies only the index (staged) changes',
      'Also restores the staged state of files as they were when stashed, in addition to the working tree changes',
      'Applies by index number instead of stash@{n}',
      'Applies all stash entries sequentially',
    ],
    correctAnswer: 1,
    explanation: '`git stash apply --index` attempts to restore both the working directory AND the staging area to their state at stash time. Without `--index`, all changes are applied as unstaged.',
    codeSnippet: 'git stash apply --index stash@{0}',
    tags: ['git', 'stash', 'apply', 'index', 'staged'],
  },
  {
    id: 'git-stash-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'stash specific files',
    question: 'How do you stash only staged (indexed) changes, keeping unstaged changes in the working tree?',
    options: [
      'git stash push --only-staged',
      'git stash push --staged',
      'git stash --index',
      'git stash push --include-staged',
    ],
    correctAnswer: 1,
    explanation: '`git stash push --staged` (Git 2.35+) stashes only the staged changes, leaving unstaged modifications in the working tree — the opposite of `--keep-index`.',
    codeSnippet: 'git stash push --staged',
    tags: ['git', 'stash', 'staged', 'push', 'selective'],
  },
  {
    id: 'git-stash-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'What happens if you try to check out the same branch in two different worktrees?',
    options: [
      'Git allows it without any issue',
      'Git refuses the checkout with an error, as a branch can only be checked out in one worktree at a time',
      'The second checkout creates a detached HEAD',
      'Both worktrees share the same index and can cause corruption',
    ],
    correctAnswer: 1,
    explanation: 'Git enforces a constraint that a branch can only be checked out in one worktree at a time to prevent index corruption. Attempting it fails with "fatal: \'<branch>\' is already checked out at \'<path>\'".',
    codeSnippet: 'git worktree add ../review feature  # fails if feature is already checked out',
    tags: ['git', 'worktree', 'branch', 'constraint', 'exclusive'],
  },
  {
    id: 'git-stash-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'stash list',
    question: 'You dropped a stash accidentally with `git stash drop`. How can you potentially recover it?',
    options: [
      'It is permanently lost',
      'Use `git reflog show stash` or `git fsck --unreachable` to find the dangling commit hash, then `git stash apply <hash>`',
      'Run `git stash recover`',
      'Use `git checkout stash@{0}` to get the previous entry',
    ],
    correctAnswer: 1,
    explanation: 'Dropped stash commits become unreachable but are not immediately deleted. `git fsck --unreachable | grep commit` finds dangling commits, and `git stash apply <hash>` re-applies one.',
    codeSnippet: 'git fsck --unreachable | grep commit\ngit stash apply <dangling-hash>',
    tags: ['git', 'stash', 'recovery', 'fsck', 'reflog'],
  },
  {
    id: 'git-stash-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'How do you create a worktree for a new branch that does not yet exist?',
    options: [
      'git worktree add <path> -b <new-branch>',
      'git worktree add <path> --create <new-branch>',
      'git worktree add --branch <new-branch> <path>',
      'git branch <new-branch> && git worktree add <path> <new-branch>',
    ],
    correctAnswer: 0,
    explanation: '`git worktree add <path> -b <new-branch>` (or `--detach` for detached HEAD) creates the worktree and simultaneously creates and checks out the new branch.',
    codeSnippet: 'git worktree add ../feature-x -b feature/new-ui',
    tags: ['git', 'worktree', 'add', 'new-branch', '-b'],
  },
  {
    id: 'git-stash-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'stash push',
    question: 'What does `git stash push --message "msg" -- path/to/dir` do?',
    options: [
      'Stashes all files in the given directory recursively with a message',
      'Creates a stash entry containing only changes within `path/to/dir` and its subdirectories',
      'Moves the directory to a stash',
      'Stashes and immediately applies to a different directory',
    ],
    correctAnswer: 1,
    explanation: 'The `-- <pathspec>` argument to `git stash push` limits which files are included in the stash. A directory pathspec stashes changes in that directory only.',
    codeSnippet: 'git stash push -m "WIP auth module" -- src/auth/',
    tags: ['git', 'stash', 'push', 'pathspec', 'directory'],
  },
  {
    id: 'git-stash-l3-008',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'worktree remove',
    question: 'What does `git worktree lock <path>` do?',
    options: [
      'Prevents commits from being made in that worktree',
      'Prevents the worktree from being removed by `git worktree prune` or `git worktree remove`',
      'Locks the branch so it cannot be pushed',
      'Creates a read-only worktree',
    ],
    correctAnswer: 1,
    explanation: '`git worktree lock` marks the worktree as locked, preventing it from being pruned automatically (e.g., when the worktree is on removable media). Unlock with `git worktree unlock`.',
    codeSnippet: 'git worktree lock ../hotfix --reason "On USB drive"',
    tags: ['git', 'worktree', 'lock', 'prune'],
  },
  {
    id: 'git-stash-l3-009',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'stash with message',
    question: 'Can you create a stash on a branch with no commits (a new empty repository)?',
    options: [
      'Yes, stash works at any point',
      'No, `git stash` requires at least one commit to exist (for the parent pointer)',
      'Only with the `--initial` flag',
      'Only if there are staged changes',
    ],
    correctAnswer: 1,
    explanation: 'Git stash needs at least one commit to exist because each stash entry is a commit whose parent is the current HEAD. On an empty (initial) repository, `git stash` fails with "You do not have the initial commit yet".',
    tags: ['git', 'stash', 'initial-commit', 'empty-repo', 'limitation'],
  },
  {
    id: 'git-stash-l3-010',
    language: 'git',
    level: 'level3',
    category: 'Stash & Worktree',
    subcategory: 'worktree add',
    question: 'What does `git worktree prune` do?',
    options: [
      'Removes all worktrees',
      'Removes stale worktree administrative files for worktrees whose directories no longer exist',
      'Prunes the object database across all worktrees',
      'Removes worktrees that are behind the main branch',
    ],
    correctAnswer: 1,
    explanation: '`git worktree prune` cleans up the administrative files in `.git/worktrees/` for worktrees that have been deleted manually (directory removed without using `git worktree remove`).',
    codeSnippet: 'git worktree prune',
    tags: ['git', 'worktree', 'prune', 'cleanup'],
  },
];

export default questions;
