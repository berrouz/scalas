import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-rebase-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase --onto',
    question: 'What is the three-argument form `git rebase --onto A B C` equivalent to in terms of cherry-pick?',
    options: [
      'Cherry-picking A onto B, then onto C',
      'Cherry-picking commits that are in C\'s ancestry but not B\'s ancestry, replaying them on top of A',
      'Cherry-picking A onto C excluding B',
      'Cherry-picking C..B onto A',
    ],
    correctAnswer: 1,
    explanation: '`git rebase --onto A B C` is conceptually: find commits reachable from C but not from B (`B..C`), then replay them on top of A. It\'s equivalent to cherry-picking `B..C` onto A.',
    codeSnippet: 'git rebase --onto A B C\n# Equivalent to: git cherry-pick B..C (applied onto A)',
    tags: ['git', 'rebase', '--onto', 'cherry-pick', 'equivalence'],
  },
  {
    id: 'git-rebase-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'interactive rebase',
    question: 'What does the `label` command do in `git rebase --rebase-merges` interactive mode?',
    options: [
      'Adds a tag to the current commit',
      'Creates a named reference to the current position in the rebase, usable by `merge` or `reset` commands',
      'Labels the commit with a category for filtering',
      'Is a deprecated alias for `break`',
    ],
    correctAnswer: 1,
    explanation: 'In `--rebase-merges` mode, `label <name>` saves the current HEAD position under that name. It can then be referenced by `merge -C <label>` or `reset <label>` commands in the todo list.',
    codeSnippet: 'label feature-tip\nreset onto\npick abc Add main change\nmerge -C feature-tip Merge feature',
    tags: ['git', 'rebase', 'interactive', 'label', 'rebase-merges'],
  },
  {
    id: 'git-rebase-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'autosquash',
    question: 'What does `git commit --fixup=amend:<commit>` do (Git 2.32+)?',
    options: [
      'Creates a fixup commit that will amend only the commit message when autosquashed',
      'Creates a fixup commit that replaces the original commit\'s tree and message when autosquashed',
      'Is identical to `--fixup=<commit>`',
      'Amends the specified commit in place without autosquash',
    ],
    correctAnswer: 0,
    explanation: 'The `amend:` prefix creates a fixup commit that, when autosquashed, updates only the commit message of the target commit while keeping its tree — combining `reword` with fixup syntax.',
    codeSnippet: 'git commit --fixup=amend:abc1234',
    tags: ['git', 'commit', 'fixup', 'amend', 'autosquash'],
  },
  {
    id: 'git-rebase-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'reflog',
    question: 'What is stored in `.git/logs/refs/heads/main` and how does it differ from `.git/refs/heads/main`?',
    options: [
      'They are the same file',
      '`.git/refs/heads/main` holds the current SHA; `.git/logs/refs/heads/main` is the reflog file with a line per update, containing old-SHA, new-SHA, identity, and message',
      '`.git/logs/` is a symlink to the git log output',
      '`.git/logs/` contains binary pack objects',
    ],
    correctAnswer: 1,
    explanation: '`.git/refs/heads/main` is a single-line file with the current tip SHA. `.git/logs/refs/heads/main` is the reflog file with one line per ref update: `<old-sha> <new-sha> <author> <timestamp> <message>`.',
    codeSnippet: 'cat .git/logs/refs/heads/main',
    tags: ['git', 'reflog', 'internals', 'refs', 'logs'],
  },
  {
    id: 'git-rebase-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase basics',
    question: 'What happens to commit timestamps during a rebase?',
    options: [
      'All commit timestamps are preserved exactly',
      'The author date is preserved; the committer date is updated to the current time by default',
      'Both timestamps are updated to the current time',
      'The committer date is preserved; the author date is updated',
    ],
    correctAnswer: 1,
    explanation: 'Rebase preserves the original `author date` from the source commit but sets a new `committer date` to the current time. Use `--committer-date-is-author-date` to keep the original committer date.',
    codeSnippet: 'git rebase --committer-date-is-author-date main',
    tags: ['git', 'rebase', 'timestamp', 'author-date', 'committer-date'],
  },
  {
    id: 'git-rebase-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'cherry-pick',
    question: 'What does `git cherry-pick --ff <commit>` do when the cherry-pick can be fast-forwarded?',
    options: [
      'It always creates a new commit',
      'If the cherry-picked commit is a direct child of HEAD, it fast-forwards instead of creating a new commit',
      'It force-applies the commit ignoring conflicts',
      'It is equivalent to `git merge --ff`',
    ],
    correctAnswer: 1,
    explanation: 'With `--ff`, if HEAD is the parent of the commit being cherry-picked, Git simply advances HEAD to that commit (fast-forward) instead of creating a new copy.',
    codeSnippet: 'git cherry-pick --ff abc1234',
    tags: ['git', 'cherry-pick', 'ff', 'fast-forward'],
  },
  {
    id: 'git-rebase-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase conflicts',
    question: 'During a rebase, you encounter the same conflict repeatedly on each commit. What mechanism avoids re-resolving it each time?',
    options: [
      'git rebase --skip-conflicts',
      'Enable `rerere` — Reuse Recorded Resolution — which saves resolutions and applies them automatically',
      'Use `git rebase -X ours` for all conflicts',
      'Create a `.gitconflict` file with default resolutions',
    ],
    correctAnswer: 1,
    explanation: '`git config rerere.enabled true` enables rerere, which records how you resolve a conflict the first time. On subsequent encounters of the same conflict (during rebase, re-merge), it applies the saved resolution automatically.',
    codeSnippet: 'git config --global rerere.enabled true',
    tags: ['git', 'rebase', 'rerere', 'conflict', 'automation'],
  },
  {
    id: 'git-rebase-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase --onto',
    question: 'What does `git rebase --onto <newbase>` (with no upstream or branch arguments) do?',
    options: [
      'Rebases the entire repository history onto newbase',
      'Replays all commits since the last non-rebased point onto newbase — it infers the upstream from the current branch\'s tracking configuration',
      'Is invalid and requires all three arguments',
      'Moves HEAD to newbase',
    ],
    correctAnswer: 1,
    explanation: 'When called with only `--onto <newbase>`, Git infers the upstream from the tracking branch and uses the current branch, rebasing the difference onto the new base.',
    codeSnippet: 'git rebase --onto origin/main',
    tags: ['git', 'rebase', '--onto', 'upstream', 'inference'],
  },
  {
    id: 'git-rebase-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'interactive rebase',
    question: 'What does the `exec` command in the interactive rebase todo list do when the command exits with a non-zero status?',
    options: [
      'The command failure is ignored and rebase continues',
      'The rebase pauses and lets you fix the issue, then continue with `git rebase --continue`',
      'The rebase is automatically aborted',
      'Git retries the command 3 times before pausing',
    ],
    correctAnswer: 1,
    explanation: 'When an `exec` command fails, the interactive rebase halts, dropping you to the shell to investigate and fix the issue. You then run `git rebase --continue` after correcting the problem.',
    codeSnippet: 'exec npm test  # rebase pauses if this fails',
    tags: ['git', 'rebase', 'exec', 'failure', 'interactive'],
  },
  {
    id: 'git-rebase-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'reflog expire',
    question: 'What is the difference between `git gc --prune=now` and `git prune`?',
    options: [
      'They are identical',
      '`git gc` runs a full maintenance pass including repacking; `git prune` only removes unreachable objects without repacking',
      '`git prune` is faster and preferred; `git gc` is deprecated',
      '`git gc` only prunes tags; `git prune` handles all refs',
    ],
    correctAnswer: 1,
    explanation: '`git prune` is a low-level command that only removes unreachable loose objects. `git gc` is a high-level command that runs repacking, pruning, and ref compression as a full maintenance suite.',
    codeSnippet: 'git gc --prune=now --aggressive',
    tags: ['git', 'gc', 'prune', 'maintenance', 'objects'],
  },
  {
    id: 'git-rebase-l4-011',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase basics',
    question: 'What is stored in `.git/rebase-merge/` during an interactive rebase?',
    options: [
      'The merge base commit',
      'State files including the todo list, the commit being applied, the original head, and the onto commit',
      'The diff of all pending commits',
      'A lock file preventing concurrent Git operations',
    ],
    correctAnswer: 1,
    explanation: '`.git/rebase-merge/` contains the rebase state: `git-rebase-todo` (remaining operations), `head-name` (original branch), `onto` (target), `msgnum`/`end` (progress), and `current` (current commit being applied).',
    codeSnippet: 'ls .git/rebase-merge/',
    tags: ['git', 'rebase', 'internals', 'state', 'rebase-merge'],
  },
  {
    id: 'git-rebase-l4-012',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'cherry-pick',
    question: 'When does `git cherry-pick` produce an "empty" commit and how do you handle it?',
    options: [
      'It never produces empty commits',
      'When the changes in the commit already exist in the current branch; use `--skip` to skip it or `--allow-empty` to keep it',
      'When the commit message is blank; use `--edit` to provide a message',
      'When cherry-picking a merge commit without `--mainline`',
    ],
    correctAnswer: 1,
    explanation: 'If the cherry-picked commit\'s changes are already present in the target branch, the result is an empty commit. Use `git cherry-pick --skip` to skip it, or `--allow-empty` to keep the empty commit.',
    codeSnippet: 'git cherry-pick --skip  # skip an already-applied commit',
    tags: ['git', 'cherry-pick', 'empty', 'skip', 'allow-empty'],
  },
  {
    id: 'git-rebase-l4-013',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase vs merge',
    question: 'After running `git rebase`, Git has rewound your branch and replayed commits. At this point, what do the old commit hashes refer to?',
    options: [
      'They no longer exist and are immediately deleted',
      'They still exist as unreachable objects, accessible via reflog, until garbage collected',
      'They are stored in `.git/stash` automatically',
      'They are moved to a special `orig/` branch prefix',
    ],
    correctAnswer: 1,
    explanation: 'The original commits are not deleted immediately — they become unreachable objects. The reflog records ORIG_HEAD and individual commit entries, allowing recovery for the duration of `gc.reflogExpireUnreachable` (default 30 days).',
    codeSnippet: 'git reflog  # see ORIG_HEAD entry pointing to pre-rebase state',
    tags: ['git', 'rebase', 'unreachable', 'reflog', 'gc', 'recovery'],
  },
  {
    id: 'git-rebase-l4-014',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'autosquash',
    question: 'What does `git commit --fixup=reword:<commit>` create (Git 2.32+)?',
    options: [
      'A fixup commit that both updates the content and message of the target',
      'A commit that, when autosquashed, updates only the commit message of the target (like reword) without changing the tree',
      'A squash commit with a reword prefix',
      'Is identical to `--fixup=amend:<commit>`',
    ],
    correctAnswer: 3,
    explanation: 'Both `--fixup=reword:<commit>` and `--fixup=amend:<commit>` update only the commit message without changing the tree when autosquashed. They are functionally identical — `reword:` is an alias for `amend:` in this context.',
    codeSnippet: 'git commit --fixup=reword:abc1234',
    tags: ['git', 'commit', 'fixup', 'reword', 'autosquash'],
  },
  {
    id: 'git-rebase-l4-015',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase basics',
    question: 'What does `git rebase --reset-author-date` do?',
    options: [
      'Resets the author of all commits to the current user',
      'Sets the author date to the current time for each rebased commit, discarding the original author date',
      'Restores the original author dates if they were changed',
      'Is an alias for `--committer-date-is-author-date`',
    ],
    correctAnswer: 1,
    explanation: '`--reset-author-date` (equivalent to `--ignore-date`) discards the original author dates and sets them to the current time for each replayed commit, alongside the new committer date.',
    codeSnippet: 'git rebase --reset-author-date main',
    tags: ['git', 'rebase', 'author-date', 'reset-author-date', 'timestamp'],
  },
  {
    id: 'git-rebase-l4-016',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'cherry-pick',
    question: 'What does `git cherry-pick --keep-redundant-commits` do?',
    options: [
      'Keeps all commits even if they produce empty diffs due to already being in the branch',
      'Keeps commits with duplicate messages',
      'Prevents cherry-pick from deduplicating commits in a range',
      'Is equivalent to `--allow-empty`',
    ],
    correctAnswer: 0,
    explanation: '`--keep-redundant-commits` forces cherry-pick to create an empty commit for each cherry-picked commit whose changes are already present, rather than silently failing or requiring manual `--allow-empty`.',
    codeSnippet: 'git cherry-pick --keep-redundant-commits abc..def',
    tags: ['git', 'cherry-pick', 'redundant', 'empty', 'range'],
  },
  {
    id: 'git-rebase-l4-017',
    language: 'git',
    level: 'level4',
    category: 'Rebase & History',
    subcategory: 'rebase basics',
    question: 'What is the practical difference between `git rebase --apply` and `git rebase --merge` backends?',
    options: [
      'They produce identical results',
      '`--apply` uses the patch+apply pipeline (like `git am`); `--merge` uses the merge machinery. `--merge` is the default and handles renames better',
      '`--apply` is faster for large repositories; `--merge` is more accurate',
      '`--apply` backend is deprecated and removed in Git 2.34+',
    ],
    correctAnswer: 1,
    explanation: 'The `--apply` backend applies patches sequentially (similar to `git am`), while `--merge` uses the three-way merge engine. `--merge` (now the default) handles renamed files better and is more accurate.',
    codeSnippet: 'git rebase --merge main   # default\ngit rebase --apply main   # legacy path',
    tags: ['git', 'rebase', 'apply', 'merge', 'backend'],
  },
];

export default questions;
