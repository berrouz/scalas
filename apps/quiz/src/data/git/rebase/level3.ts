import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-rebase-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'rebase --onto',
    question: 'What does `git rebase --onto HEAD~3 HEAD~1` do?',
    options: [
      'Removes 3 commits from the current branch',
      'Removes the two most recent commits (HEAD~1 and HEAD) by replaying nothing on top of HEAD~3',
      'Moves HEAD~3..HEAD~1 commits onto HEAD',
      'Squashes the last 3 commits',
    ],
    correctAnswer: 1,
    explanation: 'This form of `--onto` replays commits in the range `HEAD~1..HEAD` (current branch) onto `HEAD~3`, effectively deleting HEAD~2 and HEAD~1 (2 commits) from history.',
    codeSnippet: 'git rebase --onto HEAD~3 HEAD~1',
    tags: ['git', 'rebase', '--onto', 'remove-commits'],
  },
  {
    id: 'git-rebase-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'autosquash',
    question: 'What configuration option enables `--autosquash` by default for all interactive rebases?',
    options: [
      'rebase.autosquash=true',
      'git.autosquash=true',
      'interactive.autosquash=true',
      'commit.autosquash=true',
    ],
    correctAnswer: 0,
    explanation: 'Setting `rebase.autosquash=true` in Git config automatically enables `--autosquash` for every `git rebase -i` without needing to pass the flag explicitly.',
    codeSnippet: 'git config --global rebase.autosquash true',
    tags: ['git', 'rebase', 'autosquash', 'config'],
  },
  {
    id: 'git-rebase-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'preserving merge commits',
    question: 'What is the key difference between `--rebase-merges` and the deprecated `--preserve-merges`?',
    options: [
      'They are identical — the new flag is just a rename',
      '`--rebase-merges` correctly rebases merge commits by replaying them, while `--preserve-merges` just cherry-picked the merge and could silently produce wrong results',
      '`--preserve-merges` supports octopus merges; `--rebase-merges` does not',
      '`--rebase-merges` squashes all merge commits',
    ],
    correctAnswer: 1,
    explanation: '`--preserve-merges` worked by cherry-picking commits and had known bugs (wrong merge parent resolution, can silently drop commits). `--rebase-merges` properly reconstructs the merge topology.',
    codeSnippet: 'git rebase --rebase-merges main',
    tags: ['git', 'rebase', 'rebase-merges', 'preserve-merges'],
  },
  {
    id: 'git-rebase-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'reflog expire',
    question: 'What is the difference between `gc.reflogExpire` and `gc.reflogExpireUnreachable`?',
    options: [
      'They are the same setting with different names',
      '`gc.reflogExpire` controls retention for commits reachable from a ref tip (default 90 days); `gc.reflogExpireUnreachable` for unreachable ones (default 30 days)',
      '`gc.reflogExpire` is for tags; `gc.reflogExpireUnreachable` is for branches',
      '`gc.reflogExpireUnreachable` is only used by `git prune`',
    ],
    correctAnswer: 1,
    explanation: 'Reachable reflog entries (e.g., on an active branch tip) expire after 90 days by default. Unreachable entries (like detached HEAD commits) expire sooner at 30 days.',
    tags: ['git', 'reflog', 'gc', 'expire', 'config'],
  },
  {
    id: 'git-rebase-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'cherry-pick',
    question: 'You want to cherry-pick commits A, B, C from a feature branch onto main, but in reverse order. How?',
    options: [
      'git cherry-pick A B C',
      'git cherry-pick C B A',
      'git cherry-pick --reverse A..C',
      'git cherry-pick C..A',
    ],
    correctAnswer: 1,
    explanation: '`git cherry-pick` applies commits in the order specified. Listing them in reverse order `C B A` applies C first, then B, then A.',
    codeSnippet: 'git cherry-pick commitC commitB commitA',
    tags: ['git', 'cherry-pick', 'order', 'range'],
  },
  {
    id: 'git-rebase-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'rebase basics',
    question: 'What does `git rebase --update-refs main` do (Git 2.38+)?',
    options: [
      'Updates all remote tracking refs after rebasing',
      'Automatically updates local branches that point to rebased commits to their new positions',
      'Merges the reflog into main after rebasing',
      'Pushes all rebased refs to origin',
    ],
    correctAnswer: 1,
    explanation: '`--update-refs` (Git 2.38+) automatically updates local branches that point to intermediate commits being rebased, so they follow along rather than being left pointing to the old (unreachable) commits.',
    codeSnippet: 'git rebase --update-refs main',
    tags: ['git', 'rebase', 'update-refs', 'stacked-branches'],
  },
  {
    id: 'git-rebase-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'interactive rebase',
    question: 'In interactive rebase\'s `--rebase-merges` mode, what new commands appear in the todo list?',
    options: [
      '`merge` and `reset`',
      '`squash-merge` and `rebuild`',
      '`join` and `branch`',
      '`remerge` and `fork-point`',
    ],
    correctAnswer: 0,
    explanation: 'With `--rebase-merges`, the todo list gains `merge` (to create a merge commit) and `reset` (to set the current position to a specific commit) commands for reconstructing merge topology.',
    codeSnippet: 'reset refs/rewritten/feature\npick abc Add feature\nmerge -C def Merge feature into main',
    tags: ['git', 'rebase', 'rebase-merges', 'interactive', 'merge', 'reset'],
  },
  {
    id: 'git-rebase-l3-008',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'reflog',
    question: 'What does `git reflog show stash` show?',
    options: [
      'All commits ever stashed',
      'The history of the stash ref — each time stash was pushed, popped, or dropped',
      'Only the most recent stash entry',
      'The reflog of the branch from which stashes were created',
    ],
    correctAnswer: 1,
    explanation: 'The stash is a ref like any other (`refs/stash`). `git reflog show stash` lists its history, showing all stash operations (push, pop, drop) with their hashes.',
    codeSnippet: 'git reflog show stash',
    tags: ['git', 'reflog', 'stash', 'history'],
  },
  {
    id: 'git-rebase-l3-009',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'rebase --onto',
    question: 'How do you use `--onto` to transplant a single commit from one branch to another without including its ancestors?',
    options: [
      'git rebase --onto <target> <commit>^ <commit>',
      'git cherry-pick <commit> onto <target>',
      'git rebase --onto <target> <commit>',
      'git transplant <commit> <target>',
    ],
    correctAnswer: 0,
    explanation: 'By setting `<upstream>` to `<commit>^` and the branch to `<commit>`, only that one commit is replayed onto `<target>`. This is equivalent to `git cherry-pick` but uses the rebase machinery.',
    codeSnippet: 'git rebase --onto main abc1234^ abc1234',
    tags: ['git', 'rebase', '--onto', 'single-commit', 'transplant'],
  },
  {
    id: 'git-rebase-l3-010',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'cherry-pick',
    question: 'What does `git cherry-pick -x <commit>` add to the commit message?',
    options: [
      'A GPG signature',
      'A line saying "(cherry picked from commit <hash>)" for traceability',
      'The original author date',
      'The branch name the commit was picked from',
    ],
    correctAnswer: 1,
    explanation: '`-x` appends a line like `(cherry picked from commit abc1234)` to the new commit message, creating an audit trail back to the original commit.',
    codeSnippet: 'git cherry-pick -x abc1234',
    tags: ['git', 'cherry-pick', '-x', 'traceability', 'message'],
  },
  {
    id: 'git-rebase-l3-011',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'rebase vs merge',
    question: 'When you rebase and then push with `--force`, a colleague who already pulled the old commits runs `git pull`. What is the typical result?',
    options: [
      'Their pull succeeds and history is automatically reconciled',
      'They get a diverged history with duplicate commits and need to run `git pull --rebase` or manually reset',
      'Their local branch is automatically rebased by Git',
      'Git refuses the pull and shows an error',
    ],
    correctAnswer: 1,
    explanation: 'After a force push that rewrites history, colleagues who have the old commits will see a diverged history. They must `git fetch && git reset --hard origin/<branch>` or `git pull --rebase` to reconcile.',
    tags: ['git', 'rebase', 'force-push', 'collaboration', 'risk'],
  },
  {
    id: 'git-rebase-l3-012',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'reflog',
    question: 'How does `git log --walk-reflogs <branch>` differ from `git reflog show <branch>`?',
    options: [
      'They are identical',
      '`git log --walk-reflogs` shows full commit info and diff while `git reflog show` shows only the reflog metadata',
      '`git log --walk-reflogs` is available since Git 2.0 only',
      '`git reflog show` includes remote entries; `git log --walk-reflogs` does not',
    ],
    correctAnswer: 1,
    explanation: '`git log -g` (or `--walk-reflogs`) walks the reflog like a normal log and shows full commit details (diff, author, message) with the standard log format, while `git reflog show` is more compact.',
    codeSnippet: 'git log -g HEAD',
    tags: ['git', 'log', 'reflog', 'walk-reflogs', '-g'],
  },
  {
    id: 'git-rebase-l3-013',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'autosquash',
    question: 'What does `git commit --squash=<commit>` create, and how is it different from `--fixup`?',
    options: [
      'They are identical',
      '`--squash` creates a `squash! <msg>` commit that will open an editor during autosquash; `--fixup` creates `fixup! <msg>` which silently discards the message',
      '`--squash` is for merge commits; `--fixup` is for regular commits',
      '`--fixup` combines multiple commits; `--squash` only renames',
    ],
    correctAnswer: 1,
    explanation: '`git commit --squash` creates a `squash!` commit that, during `--autosquash`, will squash into the target and open an editor to combine the messages. `--fixup` creates a `fixup!` commit that silently discards its message.',
    codeSnippet: 'git commit --squash=abc1234',
    tags: ['git', 'commit', 'squash', 'fixup', 'autosquash'],
  },
  {
    id: 'git-rebase-l3-014',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'rebase conflicts',
    question: 'During an interactive rebase, you are stuck in a conflict. How do you view which commit is currently being applied?',
    options: [
      'git rebase --show-current',
      'cat .git/rebase-merge/msgnum and .git/rebase-merge/head-name',
      'git status',
      'git log --rebase',
    ],
    correctAnswer: 2,
    explanation: '`git status` during a rebase shows "interactive rebase in progress; onto <hash>" and tells you which step out of total you are on. `.git/rebase-merge/` also contains state files.',
    codeSnippet: 'git status',
    tags: ['git', 'rebase', 'status', 'conflict', 'interactive'],
  },
  {
    id: 'git-rebase-l3-015',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'cherry-pick',
    question: 'What does `git cherry-pick --strategy-option=theirs <commit>` do?',
    options: [
      'Picks the commit and overwrites the current branch with the commit\'s tree',
      'Applies the commit and resolves any conflicts using the cherry-picked commit\'s version',
      'Picks the commit only if theirs version is newer',
      'Is identical to `git cherry-pick -n`',
    ],
    correctAnswer: 1,
    explanation: 'Passing `-X theirs` (or `--strategy-option=theirs`) to cherry-pick automatically resolves conflicts by preferring the incoming commit\'s version over the current branch.',
    codeSnippet: 'git cherry-pick -X theirs abc1234',
    tags: ['git', 'cherry-pick', 'theirs', 'strategy', 'conflict'],
  },
  {
    id: 'git-rebase-l3-016',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'rebase basics',
    question: 'What does the `--fork-point` option do in `git rebase`?',
    options: [
      'Creates a fork of the repository',
      'Uses `git merge-base --fork-point` to find a more accurate upstream point that accounts for force pushes to the upstream branch',
      'Only rebases up to the fork point, not beyond',
      'Points the fork of the branch to a specific commit',
    ],
    correctAnswer: 1,
    explanation: '`--fork-point` uses the reflog to find the historical tip of the upstream at the time your branch diverged, which is more accurate than the current merge-base when the upstream has been force-pushed.',
    codeSnippet: 'git rebase --fork-point main',
    tags: ['git', 'rebase', 'fork-point', 'merge-base', 'reflog'],
  },
  {
    id: 'git-rebase-l3-017',
    language: 'git',
    level: 'level3',
    category: 'Rebase & History',
    subcategory: 'reflog expire',
    question: 'How do you delete a specific entry from the reflog?',
    options: [
      'git reflog delete HEAD@{3}',
      'git reflog drop HEAD@{3}',
      'git reflog expire --expire-unreachable=now HEAD@{3}',
      'git log --delete-reflog HEAD@{3}',
    ],
    correctAnswer: 0,
    explanation: '`git reflog delete <ref>@{<n>}` removes a specific reflog entry. This is different from expire, which removes entries older than a given time.',
    codeSnippet: 'git reflog delete HEAD@{3}',
    tags: ['git', 'reflog', 'delete', 'entry'],
  },
];

export default questions;
