import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-github-l3-001',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is a reusable workflow in GitHub Actions and how is it called?',
    options: [
      'A workflow defined in .github/shared/ that runs automatically on all PRs',
      'A workflow with workflow_call trigger that is invoked from another workflow using uses: <owner>/<repo>/.github/workflows/<file>.yml@<ref>',
      'A workflow template stored in the Actions marketplace',
      'A composite action stored in a repository',
    ],
    correctAnswer: 1,
    explanation: 'Reusable workflows expose a workflow_call trigger; callers reference them with the uses: key at the job level, enabling DRY pipeline composition across repositories.',
    codeSnippet: 'jobs:\n  call-deploy:\n    uses: org/repo/.github/workflows/deploy.yml@main\n    with:\n      environment: production\n    secrets: inherit',
    tags: ['git', 'github-actions', 'reusable-workflow', 'workflow_call'],
  },
  {
    id: 'git-github-l3-002',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is an environment in GitHub Actions and what protection does it offer?',
    options: [
      'A set of environment variables injected into every step',
      'A named deployment target (e.g., production) that can require reviewers, wait timers, and branch restrictions before a job deploys to it',
      'A Docker image cache for runners',
      'A group of secrets shared across all workflows',
    ],
    correctAnswer: 1,
    explanation: 'Environments add deployment gates: required reviewers must approve, timers can delay deployment, and branch policies can restrict which branches are allowed to deploy.',
    codeSnippet: 'jobs:\n  deploy:\n    environment:\n      name: production\n      url: https://app.example.com',
    tags: ['git', 'github-actions', 'environments', 'deployment'],
  },
  {
    id: 'git-github-l3-003',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub CLI',
    question: 'How do you use the GitHub CLI to watch the live logs of a running Actions workflow?',
    options: [
      'gh run watch <run-id>',
      'gh workflow logs --live <run-id>',
      'gh actions tail <run-id>',
      'gh run logs --stream <run-id>',
    ],
    correctAnswer: 0,
    explanation: 'gh run watch streams live status updates of a workflow run, and gh run view --log shows the full log output once it completes.',
    codeSnippet: 'gh run watch $(gh run list --limit 1 --json databaseId -q ".[0].databaseId")',
    tags: ['git', 'github-cli', 'gh', 'run', 'watch'],
  },
  {
    id: 'git-github-l3-004',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'security advisories',
    question: 'What is "private vulnerability reporting" on GitHub and how is it different from a regular issue?',
    options: [
      'It is a private issue visible only to CODEOWNERS',
      'It opens a temporary private fork for the fix before public disclosure',
      'It sends a report directly to GitHub\'s security team via a dedicated encrypted channel, keeping the vulnerability hidden from the public while the maintainer works on a fix',
      'It is a Dependabot alert for a known CVE',
    ],
    correctAnswer: 2,
    explanation: 'Private vulnerability reporting lets researchers report directly to maintainers without public exposure; the maintainer can then issue a security advisory and coordinate a coordinated disclosure timeline.',
    tags: ['git', 'github', 'security', 'private-vulnerability-reporting'],
  },
  {
    id: 'git-github-l3-005',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is the difference between github.token and a Personal Access Token (PAT) in GitHub Actions?',
    options: [
      'They are identical; github.token is just a PAT alias',
      'github.token is an automatically provisioned, short-lived token scoped to the repository for the duration of the workflow run; a PAT is a long-lived token created by a user with broader scopes',
      'github.token can only read; PATs can write',
      'PATs expire after 24 hours; github.token never expires',
    ],
    correctAnswer: 1,
    explanation: 'GITHUB_TOKEN is automatically injected by GitHub Actions, scoped to the current repo and run, then revoked when the job ends — much safer than storing a user PAT as a secret.',
    codeSnippet: '- uses: actions/checkout@v4\n  with:\n    token: \${{ github.token }}',
    tags: ['git', 'github-actions', 'github-token', 'pat', 'security'],
  },
  {
    id: 'git-github-l3-006',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'merge queue',
    question: 'How does a GitHub Merge Queue handle PRs that conflict with each other while both are in the queue?',
    options: [
      'Both are merged simultaneously, creating a conflict commit',
      'They are tested sequentially: the second PR is rebased on top of the first after the first passes, then tested again',
      'The conflicting PR is automatically rejected from the queue',
      'A human must resolve the conflict before re-queuing',
    ],
    correctAnswer: 1,
    explanation: 'The merge queue serialises integration: PR #2 is tested against the merge of PR #1 + base, so any interactions between them are caught before either lands in the protected branch.',
    tags: ['git', 'merge-queue', 'conflict', 'github'],
  },
  {
    id: 'git-github-l3-007',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What does the concurrency: key in a GitHub Actions workflow do?',
    options: [
      'Sets the number of parallel matrix jobs',
      'Ensures only one workflow run per concurrency group is active at a time, optionally cancelling in-progress runs when a new one starts',
      'Limits the total number of workflow runs per day',
      'Shares runner resources across multiple repositories',
    ],
    correctAnswer: 1,
    explanation: 'concurrency: groups runs so that a new push to a PR cancels any still-running workflow for that same PR, preventing redundant runs and saving CI minutes.',
    codeSnippet: 'concurrency:\n  group: \${{ github.workflow }}-\${{ github.ref }}\n  cancel-in-progress: true',
    tags: ['git', 'github-actions', 'concurrency', 'cancel'],
  },
  {
    id: 'git-github-l3-008',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'CODEOWNERS',
    question: 'CODEOWNERS entries are evaluated in what order, and which one wins?',
    options: [
      'First match wins',
      'Most specific path wins regardless of order',
      'Last matching entry wins',
      'All matching entries are combined',
    ],
    correctAnswer: 2,
    explanation: 'Like .gitignore, CODEOWNERS uses last-match-wins: a more specific rule placed later in the file overrides earlier broader rules for the same path.',
    codeSnippet: '# .github/CODEOWNERS\n*         @default-team\nsrc/auth/ @auth-team   # wins for src/auth/ files',
    tags: ['git', 'github', 'codeowners', 'order', 'precedence'],
  },
  {
    id: 'git-github-l3-009',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What is the purpose of the permissions: key in a GitHub Actions job?',
    options: [
      'It configures which runners have access to the job',
      'It sets the minimum required review count for the workflow\'s PR',
      'It scopes the GITHUB_TOKEN permissions for that job, following least-privilege principles',
      'It restricts which secrets the job can access',
    ],
    correctAnswer: 2,
    explanation: 'By specifying permissions:, you reduce the GITHUB_TOKEN to only the scopes the job needs (e.g., contents: read, id-token: write), limiting blast radius if the job is compromised.',
    codeSnippet: 'jobs:\n  deploy:\n    permissions:\n      id-token: write\n      contents: read',
    tags: ['git', 'github-actions', 'permissions', 'security', 'least-privilege'],
  },
  {
    id: 'git-github-l3-010',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'pull requests',
    question: 'How do you reference and auto-close a GitHub issue from a PR description?',
    options: [
      'Write #<issue-number> anywhere in the description',
      'Write closes #<issue-number> (or fixes / resolves) in the PR description or a commit message',
      'Add the issue URL to the PR title',
      'Link the issue via the repository\'s Projects board',
    ],
    correctAnswer: 1,
    explanation: 'Using "Closes #42", "Fixes #42", or "Resolves #42" in the PR body or a commit message automatically closes the referenced issue when the PR is merged into the default branch.',
    codeSnippet: '## Summary\nAdds OAuth2 login.\n\nCloses #42',
    tags: ['git', 'github', 'pull-request', 'auto-close', 'issues'],
  },
  {
    id: 'git-github-l3-011',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'GitHub Actions',
    question: 'What does the if: github.event_name == \'push\' && github.ref == \'refs/heads/main\' condition on a job do?',
    options: [
      'Runs the job only on pull request events targeting main',
      'Runs the job only when a commit is pushed directly to the main branch, skipping PRs and other branches',
      'Runs the job whenever the main branch is referenced in any event',
      'Skips the job if there are no changes to main',
    ],
    correctAnswer: 1,
    explanation: 'Combining event type and ref filters ensures the job (e.g., a production deployment) only runs on direct pushes to main, not on PR builds targeting main from other branches.',
    codeSnippet: 'jobs:\n  deploy:\n    if: github.event_name == \'push\' && github.ref == \'refs/heads/main\'',
    tags: ['git', 'github-actions', 'conditional', 'event-filter'],
  },
  {
    id: 'git-github-l3-012',
    language: 'git',
    level: 'level3',
    category: 'GitHub/GitLab Features',
    subcategory: 'Dependabot',
    question: 'How do you prevent Dependabot from opening PRs for major version bumps of a specific package?',
    options: [
      'Add the package to .gitignore',
      'Use the ignore: block in .github/dependabot.yml specifying update-types: ["version-update:semver-major"]',
      'Set max-version: "minor" in dependabot.yml',
      'Dependabot cannot be configured per-package; disable it entirely',
    ],
    correctAnswer: 1,
    explanation: 'The ignore: configuration in dependabot.yml allows fine-grained control: you can ignore specific packages, version ranges, or update types like semver-major.',
    codeSnippet: 'updates:\n  - package-ecosystem: npm\n    ignore:\n      - dependency-name: "lodash"\n        update-types: ["version-update:semver-major"]',
    tags: ['git', 'github', 'dependabot', 'ignore', 'configuration'],
  },
];

export default questions;
