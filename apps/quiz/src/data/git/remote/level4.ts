import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-remote-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'push --force-with-lease',
    question: 'How does `--force-if-includes` (Git 2.30+) strengthen `--force-with-lease`?',
    options: [
      'It checks the remote directly instead of the local remote-tracking ref',
      'It verifies that the remote-tracking commit is actually in the local history of what you are pushing, preventing the CI-fetch bypass',
      'It requires a signed commit before force-pushing',
      'It is identical to `--force-with-lease` with a different name',
    ],
    correctAnswer: 1,
    explanation: '`--force-if-includes` additionally verifies that the remote-tracking ref is reachable from your current branch tip, closing the loophole where a CI `git fetch` updates the ref but you haven\'t integrated the remote\'s new commits.',
    codeSnippet: 'git push --force-with-lease --force-if-includes origin feature',
    tags: ['git', 'push', 'force-if-includes', 'force-with-lease', 'safety'],
  },
  {
    id: 'git-remote-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'upstream tracking',
    question: 'Where is the upstream tracking configuration stored in `.git/config`?',
    options: [
      'Under `[remote "origin"]` as `track = <branch>`',
      'Under `[branch "<name>"]` as `remote = <remote>` and `merge = refs/heads/<branch>`',
      'In `.git/refs/remotes/` as a special file',
      'In `~/.gitconfig` under `[tracking]`',
    ],
    correctAnswer: 1,
    explanation: 'Each tracked branch has a section `[branch "<name>"]` in `.git/config` with `remote` (the remote alias) and `merge` (the full refspec of the remote branch) entries.',
    codeSnippet: '# .git/config\n[branch "main"]\n  remote = origin\n  merge = refs/heads/main',
    tags: ['git', 'branch', 'tracking', 'config', 'internals'],
  },
  {
    id: 'git-remote-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does `git push origin +refs/heads/feature:refs/heads/feature` mean? What does the `+` prefix do?',
    options: [
      'It stages the push for confirmation',
      'The `+` prefix forces the push even if it is not a fast-forward update',
      'It creates the branch if it does not exist',
      'It is a syntax error',
    ],
    correctAnswer: 1,
    explanation: 'A `+` prefix on a refspec forces the update even if it is not a fast-forward (overwriting history). It is the refspec-level equivalent of `--force`, but applies only to the specific ref.',
    codeSnippet: 'git push origin +refs/heads/feature:refs/heads/feature',
    tags: ['git', 'push', 'refspec', 'force', 'plus-prefix'],
  },
  {
    id: 'git-remote-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git fetch',
    question: 'What does `git fetch --negotiate-only` do?',
    options: [
      'Starts a negotiation dialog with the remote about what to fetch',
      'Performs only the negotiation phase of a fetch, outputting what the server would send without actually downloading objects',
      'Resolves a fetch conflict automatically',
      'Syncs only the ref tips without transferring objects',
    ],
    correctAnswer: 1,
    explanation: '`--negotiate-only` (Git 2.29+) runs the negotiation phase (determining what objects the remote has that we don\'t) and exits, printing the negotiated commits. Used for optimization analysis.',
    tags: ['git', 'fetch', 'negotiate-only', 'internals', 'protocol'],
  },
  {
    id: 'git-remote-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'prune',
    question: 'After `git remote prune origin`, the remote-tracking branch `origin/feature` is deleted. Is the local branch `feature` affected?',
    options: [
      'Yes, the local branch is also deleted automatically',
      'No, the local branch remains untouched and retains its commits',
      'The local branch loses its tracking configuration but remains',
      'The local branch is renamed to `orphan/feature`',
    ],
    correctAnswer: 1,
    explanation: '`git remote prune` only removes remote-tracking refs (like `origin/feature`). Local branches are never deleted by pruning — they remain fully intact with their commits.',
    tags: ['git', 'remote', 'prune', 'local-branch', 'safety'],
  },
  {
    id: 'git-remote-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does `push.autoSetupRemote=true` in Git config (2.37+) enable?',
    options: [
      'Automatically creates the remote if it does not exist',
      'Automatically uses `--set-upstream` when pushing a new branch, so tracking is set without -u',
      'Auto-pushes after every commit',
      'Sets the remote URL from the package.json repository field',
    ],
    correctAnswer: 1,
    explanation: '`push.autoSetupRemote=true` makes `git push` behave as if `--set-upstream` was always passed for new branches, eliminating the need to remember `-u` on first push.',
    codeSnippet: 'git config --global push.autoSetupRemote true',
    tags: ['git', 'push', 'autoSetupRemote', 'upstream', 'config'],
  },
  {
    id: 'git-remote-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'remote add',
    question: 'What is the difference between a "fetch refspec" and a "push refspec" in a remote configuration?',
    options: [
      'They are identical and interchangeable',
      'The fetch refspec maps remote refs to local remote-tracking refs; the push refspec defines what is sent when pushing',
      'Fetch refspecs are read-only; push refspecs are write-only',
      'Push refspecs use `+` prefix; fetch refspecs do not',
    ],
    correctAnswer: 1,
    explanation: 'The `fetch` refspec (e.g., `+refs/heads/*:refs/remotes/origin/*`) maps what is downloaded to local tracking refs. The `push` refspec (in `pushurl` / `push =`) controls the mapping for uploads.',
    codeSnippet: '# In .git/config\n[remote "origin"]\n  url = ...\n  fetch = +refs/heads/*:refs/remotes/origin/*\n  push = refs/heads/main:refs/heads/main',
    tags: ['git', 'remote', 'refspec', 'fetch', 'push', 'config'],
  },
  {
    id: 'git-remote-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git fetch',
    question: 'What does `git fetch --depth=1` do on an already fully-cloned repository?',
    options: [
      'Has no effect since the repository is already fully cloned',
      'Converts the existing full clone into a shallow clone with only the latest commit per branch',
      'Fetches only the top 1 commit of each branch from the remote',
      'Errors out because shallow fetches only work on shallow clones',
    ],
    correctAnswer: 1,
    explanation: 'Running `git fetch --depth=1` on a full clone truncates the local history by creating a new shallow boundary at depth 1, effectively making it a shallow clone. This can reduce disk usage but loses history.',
    codeSnippet: 'git fetch --depth=1 origin',
    tags: ['git', 'fetch', 'depth', 'shallow', 'history'],
  },
  {
    id: 'git-remote-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'fork workflow',
    question: 'What does `git request-pull v1.0 https://github.com/you/fork.git main` generate?',
    options: [
      'Automatically opens a pull request on GitHub',
      'Generates a summary message with the list of commits and diffstat, suitable for emailing a maintainer',
      'Creates a patch file for each commit since v1.0',
      'Fetches from the fork URL and creates a local branch',
    ],
    correctAnswer: 1,
    explanation: '`git request-pull` generates a human-readable pull request summary for email-based workflows (like the Linux kernel), listing commits and a diffstat since the given start point.',
    codeSnippet: 'git request-pull v1.0 https://github.com/you/fork.git main',
    tags: ['git', 'request-pull', 'fork', 'email', 'workflow'],
  },
  {
    id: 'git-remote-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does `git push --signed=if-asked origin main` do?',
    options: [
      'Signs the push only if GPG is configured',
      'Sends a GPG-signed push if the server indicates it accepts push certificates, otherwise pushes unsigned',
      'Fails if the commit is not signed',
      'Prompts the user to sign before pushing',
    ],
    correctAnswer: 1,
    explanation: 'Push certificates (RFC 7929) let the server verify push authenticity. `--signed=if-asked` enables signing only when the server requests it, while `--signed=yes` always signs.',
    codeSnippet: 'git push --signed=if-asked origin main',
    tags: ['git', 'push', 'signed', 'gpg', 'push-certificate'],
  },
  {
    id: 'git-remote-l4-011',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git fetch',
    question: 'What does the `FETCH_HEAD` file in `.git/` store?',
    options: [
      'The URL of the remote that was last fetched',
      'The commit hashes of all refs fetched in the last `git fetch`, with annotations about whether they were merged',
      'The current remote-tracking branch',
      'A lock file preventing concurrent fetches',
    ],
    correctAnswer: 1,
    explanation: '`FETCH_HEAD` records what was fetched in the most recent `git fetch`, including the SHA and whether each ref is "not-for-merge" (only fetched) or intended to be merged. `git merge FETCH_HEAD` uses this file.',
    codeSnippet: 'cat .git/FETCH_HEAD',
    tags: ['git', 'FETCH_HEAD', 'internals', 'fetch'],
  },
  {
    id: 'git-remote-l4-012',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'pull --rebase',
    question: 'What is the difference between `rebase.autoStash=true` and using `git stash` manually before `git pull --rebase`?',
    options: [
      'They are functionally identical',
      '`rebase.autoStash=true` automatically stashes dirty working directory state before the rebase and pops it after, including unstaged changes',
      '`rebase.autoStash` only handles staged changes, not unstaged ones',
      '`rebase.autoStash` creates a separate stash branch instead of using the stash stack',
    ],
    correctAnswer: 1,
    explanation: 'With `rebase.autoStash=true`, Git automatically stashes uncommitted changes, runs the rebase, then pops the stash — the same as doing it manually but in one step without needing to remember.',
    codeSnippet: 'git config --global rebase.autoStash true',
    tags: ['git', 'rebase', 'autoStash', 'pull', 'config'],
  },
  {
    id: 'git-remote-l4-013',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'remote add',
    question: 'What does `git ls-remote origin` output?',
    options: [
      'The list of files in the remote repository',
      'All refs (branches, tags, HEAD) on the remote with their SHA hashes, without fetching them',
      'The remote server\'s configuration',
      'The list of commits on the remote not yet fetched',
    ],
    correctAnswer: 1,
    explanation: '`git ls-remote` queries the remote for its ref list over the network, returning SHA-ref pairs without modifying the local repository — useful for scripting or inspecting a remote.',
    codeSnippet: 'git ls-remote origin',
    tags: ['git', 'ls-remote', 'remote', 'refs', 'inspect'],
  },
  {
    id: 'git-remote-l4-014',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git fetch',
    question: 'What does Git Protocol v2 improve over Protocol v1 for `git fetch`?',
    options: [
      'It uses TLS encryption where v1 does not',
      'It allows the server to filter the initial ref advertisement, significantly reducing bandwidth for repositories with many refs',
      'It supports pushing, where v1 only supports fetching',
      'It adds multi-threading on the server side',
    ],
    correctAnswer: 1,
    explanation: 'Git Protocol v2 (enabled by default in modern Git) allows the client to send capability information first, so the server only advertises the refs the client needs — a major improvement for repos with thousands of refs.',
    codeSnippet: 'git config --global protocol.version 2',
    tags: ['git', 'protocol', 'v2', 'fetch', 'performance'],
  },
  {
    id: 'git-remote-l4-015',
    language: 'git',
    level: 'level4',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What is a "push certificate" in Git and what problem does it solve?',
    options: [
      'A TLS certificate required for HTTPS push authentication',
      'A GPG-signed record of a push operation stored on the remote, proving who pushed what and when, even without server-side logging',
      'A certificate authority for validating server SSH keys',
      'A pre-push hook validation artifact',
    ],
    correctAnswer: 1,
    explanation: 'Push certificates (git push --sign) create a signed record of the entire push operation. The remote can store these as nonce-verified proofs of who pushed which refs, useful for audit trails in high-security environments.',
    codeSnippet: 'git push --sign=yes origin main',
    tags: ['git', 'push', 'certificate', 'sign', 'audit', 'security'],
  },
];

export default questions;
