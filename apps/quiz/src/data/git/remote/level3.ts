import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-remote-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'push --force-with-lease',
    question: 'What is the subtle limitation of `--force-with-lease` when using a fetch-cache (e.g., configured in CI with shared remote tracking)?',
    options: [
      'It does not work with SSH remotes',
      'If a `git fetch` was run but the branch was not updated locally, the lease check passes even if someone pushed — the lease is based on your local remote-tracking ref, not the actual remote',
      'It always fails on protected branches',
      'It cannot be combined with `--no-verify`',
    ],
    correctAnswer: 1,
    explanation: '`--force-with-lease` compares your local `origin/<branch>` ref. If CI fetches for you (updating the remote-tracking ref to the new state) before you push, the lease passes even though someone else pushed — a known footgun.',
    codeSnippet: 'git push --force-with-lease --force-if-includes origin feature',
    tags: ['git', 'push', 'force-with-lease', 'limitation', 'ci'],
  },
  {
    id: 'git-remote-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does `git push origin HEAD:refs/for/main` typically indicate?',
    options: [
      'A force push to a protected branch',
      'Pushing to a Gerrit code review system using its special ref namespace',
      'Creating a new remote branch called `refs/for/main`',
      'Pushing HEAD to a detached reference',
    ],
    correctAnswer: 1,
    explanation: 'The `refs/for/<branch>` namespace is used by Gerrit code review. Pushing to it submits the change for review rather than directly pushing to the branch.',
    codeSnippet: 'git push origin HEAD:refs/for/main',
    tags: ['git', 'push', 'gerrit', 'refs', 'review'],
  },
  {
    id: 'git-remote-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'upstream tracking',
    question: 'What is a "refspec" in Git and give an example?',
    options: [
      'A specification for the ref name format',
      'A pattern that maps local refs to remote refs, e.g. `+refs/heads/*:refs/remotes/origin/*`',
      'A special commit message format',
      'A file listing all remote branches',
    ],
    correctAnswer: 1,
    explanation: 'A refspec is a mapping of the form `[+]<src>:<dst>` used in fetch/push configuration. The `+` prefix allows non-fast-forward updates. The default fetch refspec for origin maps all remote heads to local remote-tracking refs.',
    codeSnippet: '# In .git/config\nfetch = +refs/heads/*:refs/remotes/origin/*',
    tags: ['git', 'refspec', 'remote', 'config'],
  },
  {
    id: 'git-remote-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'git fetch',
    question: 'How do you fetch a single remote branch and place it in a local branch named `local-copy`?',
    options: [
      'git fetch origin feature:local-copy',
      'git fetch origin feature --as=local-copy',
      'git fetch origin feature && git branch local-copy FETCH_HEAD',
      'Both A and C work',
    ],
    correctAnswer: 3,
    explanation: '`git fetch origin feature:local-copy` fetches `origin/feature` into a local branch named `local-copy`. Alternatively, fetch sets `FETCH_HEAD` which you can branch from.',
    codeSnippet: 'git fetch origin feature:local-copy',
    tags: ['git', 'fetch', 'refspec', 'local-branch'],
  },
  {
    id: 'git-remote-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'prune',
    question: 'What does `git fetch --prune --prune-tags` do?',
    options: [
      'Fetches all branches and tags and deletes everything locally',
      'Fetches new commits and removes stale remote-tracking branches AND tags that no longer exist on the remote',
      'Only prunes tags, not branches',
      'Prunes the local pack files',
    ],
    correctAnswer: 1,
    explanation: '`--prune-tags` (added in Git 2.17) extends `--prune` to also remove local tags that have been deleted on the remote, keeping your local tag list in sync.',
    codeSnippet: 'git fetch --prune --prune-tags origin',
    tags: ['git', 'fetch', 'prune', 'prune-tags'],
  },
  {
    id: 'git-remote-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'pull --rebase',
    question: 'What does `git pull --rebase=interactive` do?',
    options: [
      'Performs `git pull` and then opens interactive rebase',
      'Is not valid syntax',
      'Fetches then runs `git rebase -i` to let you squash/reorder your local commits before replaying them on top of the remote',
      'Only rebases if there are no conflicts',
    ],
    correctAnswer: 2,
    explanation: '`git pull --rebase=interactive` is equivalent to fetching and then running `git rebase -i` on your local commits, giving you the chance to clean up history before integration.',
    codeSnippet: 'git pull --rebase=interactive origin main',
    tags: ['git', 'pull', 'rebase', 'interactive'],
  },
  {
    id: 'git-remote-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does `git push --mirror origin` do?',
    options: [
      'Pushes only the current branch as a read-only mirror',
      'Pushes all refs (branches, tags, notes) and deletes remote refs that no longer exist locally',
      'Creates a bare clone of the repository on origin',
      'Mirrors the commit messages to the remote',
    ],
    correctAnswer: 1,
    explanation: '`git push --mirror` pushes all refs including `refs/heads`, `refs/tags`, and `refs/remotes`, and also deletes remote refs that have been deleted locally — making the remote an exact mirror.',
    codeSnippet: 'git push --mirror backup-remote',
    tags: ['git', 'push', 'mirror', 'backup'],
  },
  {
    id: 'git-remote-l3-008',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'remote add',
    question: 'How do you set a different push URL for an existing remote?',
    options: [
      'git remote set-url --push origin <url>',
      'git remote add --push origin <url>',
      'git config remote.origin.pushurl <url>',
      'Both A and C work',
    ],
    correctAnswer: 3,
    explanation: '`git remote set-url --push origin <url>` sets the push URL separately from the fetch URL. Alternatively, editing `pushurl` in `.git/config` under `[remote "origin"]` achieves the same effect.',
    codeSnippet: 'git remote set-url --push origin git@github.com:user/repo.git',
    tags: ['git', 'remote', 'set-url', 'push-url'],
  },
  {
    id: 'git-remote-l3-009',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'fork workflow',
    question: 'In a fork workflow, how do you keep a feature branch up to date with upstream/main while preserving clean history?',
    options: [
      'git merge upstream/main into feature',
      'git fetch upstream && git rebase upstream/main while on the feature branch',
      'git pull upstream main && git push origin feature',
      'git cherry-pick upstream/main',
    ],
    correctAnswer: 1,
    explanation: 'Rebasing your feature branch onto `upstream/main` replays your feature commits on top of the latest upstream changes, keeping a linear history and making the eventual PR cleaner.',
    codeSnippet: 'git fetch upstream\ngit rebase upstream/main',
    tags: ['git', 'fork', 'rebase', 'upstream', 'workflow'],
  },
  {
    id: 'git-remote-l3-010',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does `git push --atomic origin main tag-v1.0` guarantee?',
    options: [
      'The push succeeds partially if one ref fails',
      'Either all refs are updated successfully on the remote, or none are — preventing partial updates',
      'The push is done in a single network packet',
      'It requires a GPG signature for the push',
    ],
    correctAnswer: 1,
    explanation: '`--atomic` instructs the remote (if it supports it) to accept or reject all pushed refs as a single transaction, ensuring you don\'t end up with a tag pointing to a commit that wasn\'t pushed.',
    codeSnippet: 'git push --atomic origin main v1.0',
    tags: ['git', 'push', 'atomic', 'transaction'],
  },
  {
    id: 'git-remote-l3-011',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'upstream tracking',
    question: 'How do you unset the upstream tracking for a branch?',
    options: [
      'git branch --unset-upstream',
      'git remote untrack origin feature',
      'git branch -d --upstream',
      'git config --unset branch.feature.remote',
    ],
    correctAnswer: 0,
    explanation: '`git branch --unset-upstream` removes the upstream tracking configuration for the current branch, causing `git push`/`git pull` to require explicit remote/branch arguments.',
    codeSnippet: 'git branch --unset-upstream',
    tags: ['git', 'branch', 'unset-upstream', 'tracking'],
  },
  {
    id: 'git-remote-l3-012',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'git fetch',
    question: 'What does `git fetch --shallow-since=<date>` do?',
    options: [
      'Fetches only commits with messages since the given date',
      'Deepens a shallow clone to include all commits since the given date',
      'Creates a new shallow clone from the given date',
      'Fetches only the latest commit for each branch',
    ],
    correctAnswer: 1,
    explanation: '`git fetch --shallow-since=<date>` deepens a shallow repository to include all commits reachable since the given date, useful for incrementally deepening a shallow clone.',
    codeSnippet: 'git fetch --shallow-since="2024-01-01" origin',
    tags: ['git', 'fetch', 'shallow', 'shallow-since'],
  },
  {
    id: 'git-remote-l3-013',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'git push',
    question: 'What does the `push.default` config option control?',
    options: [
      'The default remote to push to',
      'The behavior of `git push` with no explicit refspec: which branches to push',
      'The default commit message for force pushes',
      'Whether tags are pushed by default',
    ],
    correctAnswer: 1,
    explanation: '`push.default` controls what `git push` does without explicit arguments. Values: `simple` (default, push current branch to upstream), `matching` (push all matching branches), `current`, `upstream`, or `nothing`.',
    codeSnippet: 'git config --global push.default simple',
    tags: ['git', 'push', 'push.default', 'config'],
  },
  {
    id: 'git-remote-l3-014',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'remote add',
    question: 'How do you add an additional push URL to an existing remote (so a single push goes to multiple remotes)?',
    options: [
      'git remote add --extra origin <url2>',
      'git remote set-url --add --push origin <url2>',
      'git config remote.origin.pushurl <url1> <url2>',
      'You cannot push to multiple remotes simultaneously',
    ],
    correctAnswer: 1,
    explanation: '`git remote set-url --add --push origin <url>` adds an additional push URL. With two `pushurl` entries, a single `git push origin` sends to both remotes.',
    codeSnippet: 'git remote set-url --add --push origin https://gitlab.com/user/repo.git',
    tags: ['git', 'remote', 'set-url', 'multiple-push', 'mirror'],
  },
  {
    id: 'git-remote-l3-015',
    language: 'git',
    level: 'level3',
    category: 'Remote Operations',
    subcategory: 'pull --rebase',
    question: 'What does `git pull --rebase=merges` do?',
    options: [
      'Rebases only the merge commits',
      'Preserves local merge commits when rebasing on top of the upstream, re-creating the merge structure',
      'Merges instead of rebasing for conflicting commits',
      'Is equivalent to a regular pull with merge',
    ],
    correctAnswer: 1,
    explanation: '`--rebase=merges` (equivalent to `rebase --rebase-merges`) preserves the local merge topology when rebasing, so merge commits within your feature branch are kept rather than linearized.',
    codeSnippet: 'git pull --rebase=merges origin main',
    tags: ['git', 'pull', 'rebase', 'rebase-merges', 'topology'],
  },
];

export default questions;
