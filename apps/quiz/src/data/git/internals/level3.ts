import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-internals-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'pack v2 index',
    question: 'What improvement does the pack index v2 (`.idx` version 2) offer over version 1?',
    options: [
      'It supports repositories with more than 4 GB of pack data by using 64-bit offsets',
      'It stores objects in sorted order by type',
      'It embeds delta chains to avoid reading the pack file',
      'It replaces zlib with LZ4 compression',
    ],
    correctAnswer: 0,
    explanation: 'Pack index v2 introduces a separate table of 64-bit offsets for large objects, allowing pack files to exceed the 4 GB limit that 32-bit offsets in v1 imposed.',
    tags: ['git', 'packfile', 'idx-v2', 'large-pack', 'internals'],
  },
  {
    id: 'git-internals-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'delta chain depth',
    question: 'What risk does a very deep delta chain pose in a packfile?',
    options: [
      'SHA-1 collisions become more likely',
      'Reconstructing an object requires applying many deltas sequentially, increasing read latency',
      'Delta chains deeper than 50 cause git fsck failures',
      'The index file grows quadratically with chain depth',
    ],
    correctAnswer: 1,
    explanation: 'To reconstruct an object at depth N, Git must apply all N delta instructions from the base. Very deep chains (controlled by `pack.depth`, default 50) increase object access time.',
    codeSnippet: 'git config pack.depth 50',
    tags: ['git', 'delta-chain', 'packfile', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'index v3 extensions',
    question: 'What information does the "resolve undo" extension in the Git index store?',
    options: [
      'The pre-merge versions of conflicted files so they can be un-resolved',
      'The sequence of undo operations for `git reset`',
      'Pointers to the stash entries created during merge',
      'The conflict markers inserted into conflicted files',
    ],
    correctAnswer: 0,
    explanation: 'The "resolve undo" (REUC) extension records the stage 1/2/3 entries that existed before the conflict was marked resolved, enabling tools to re-open a resolved conflict.',
    tags: ['git', 'index', 'resolve-undo', 'merge', 'internals'],
  },
  {
    id: 'git-internals-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'transfer protocol negotiation',
    question: 'During `git fetch`, what problem does the "have/want" negotiation solve?',
    options: [
      'It authenticates the client with the server',
      'It determines the minimal set of objects the client lacks by the client advertising its known commit SHAs',
      'It negotiates compression algorithms for the pack transfer',
      'It resolves merge conflicts between local and remote branches',
    ],
    correctAnswer: 1,
    explanation: 'The client sends "want <sha>" for refs it needs and "have <sha>" for commits it already possesses. The server finds the common ancestors and sends only the missing object chain.',
    tags: ['git', 'fetch', 'protocol', 'have-want', 'negotiation', 'internals'],
  },
  {
    id: 'git-internals-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'SHA-1 collision',
    question: 'Git is transitioning from SHA-1 to SHA-256. What practical problem motivates this change?',
    options: [
      'SHA-1 produces 40-hex-character names that are too long for some filesystems',
      'The SHAttered attack demonstrated that SHA-1 collisions can be crafted, potentially allowing malicious objects to share a legitimate SHA-1',
      'SHA-1 computation is too slow for large repositories',
      'SHA-1 hashes are not compatible with GPG signing',
    ],
    correctAnswer: 1,
    explanation: 'The 2017 SHAttered attack produced two PDFs with the same SHA-1. Although practically hard to exploit in Git, it proved SHA-1 is cryptographically broken, driving the SHA-256 object format (git-hash-sha256).',
    tags: ['git', 'SHA-1', 'SHA-256', 'security', 'collision'],
  },
  {
    id: 'git-internals-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'object model',
    question: 'How does Git represent a symlink in the object model?',
    options: [
      'As a tree entry of type "symlink" with a special mode',
      'As a blob whose content is the symlink target path, with mode 120000 in the tree',
      'As a special "link" object type',
      'Symlinks are not supported in Git\'s object model',
    ],
    correctAnswer: 1,
    explanation: 'A symlink is stored as a blob containing the target path string, and the tree entry has mode `120000` (instead of the usual `100644` for regular files), signalling to Git and checkout that it should be created as a symlink.',
    codeSnippet: 'git ls-tree HEAD | grep ^120000',
    tags: ['git', 'internals', 'symlink', 'blob', 'tree', 'mode'],
  },
  {
    id: 'git-internals-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'commit graph generation numbers',
    question: 'What are "generation numbers" in the commit-graph file, and why are they useful?',
    options: [
      'Auto-incrementing IDs assigned to commits in order of creation',
      'Topological depth values where a commit\'s generation is max(parent generations) + 1, enabling fast ancestry checks without full traversal',
      'The number of files changed in each commit',
      'Timestamps encoded as integers relative to the epoch',
    ],
    correctAnswer: 1,
    explanation: 'Generation number of a commit is max(parent generation numbers) + 1 (root = 1). If commit A has a smaller generation than B, A cannot be an ancestor of B — allowing many graph queries to short-circuit without traversal.',
    tags: ['git', 'commit-graph', 'generation-numbers', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l3-008',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'index stages',
    question: 'During a merge conflict, the Git index holds up to three stages per conflicted path. What do stages 1, 2, and 3 represent?',
    options: [
      'Working directory, HEAD, and remote version',
      'Common ancestor (base), "ours" (current branch), and "theirs" (merging branch)',
      'Original file, first patch, and second patch',
      'Cached, staged, and unstaged versions',
    ],
    correctAnswer: 1,
    explanation: 'Stage 1 = common merge base, stage 2 = the version on the current branch (ours), stage 3 = the version being merged in (theirs). The normal stage 0 is absent for conflicted paths.',
    codeSnippet: 'git ls-files -u   # shows stages 1,2,3 for conflicts',
    tags: ['git', 'index', 'stages', 'merge', 'conflict', 'internals'],
  },
  {
    id: 'git-internals-l3-009',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'git fsck advanced',
    question: 'What does `git fsck --lost-found` write to disk?',
    options: [
      'A repair script for corrupted objects',
      'Unreachable blob and commit objects as files under `.git/lost-found/`',
      'A log of all missing objects',
      'Backup copies of the pack files',
    ],
    correctAnswer: 1,
    explanation: '`git fsck --lost-found` writes unreachable blobs to `.git/lost-found/other/` and unreachable commits to `.git/lost-found/commit/`, named by SHA, letting you inspect or recover lost content.',
    codeSnippet: 'git fsck --lost-found\nls .git/lost-found/commit/',
    tags: ['git', 'fsck', 'lost-found', 'recovery', 'internals'],
  },
  {
    id: 'git-internals-l3-010',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'transfer protocol v2',
    question: 'What key improvement does Git protocol version 2 provide over v1?',
    options: [
      'End-to-end encryption for all object transfers',
      'Server-side filtering of refs so the client only receives refs it needs, reducing overhead on large repos',
      'Binary-encoded pack data instead of ASCII capability lines',
      'Multiplexed simultaneous fetch and push',
    ],
    correctAnswer: 1,
    explanation: 'Protocol v2 allows the client to advertise ref filters (prefix filters, etc.), so the server sends only matching refs rather than the entire ref list — a major performance gain for repos with millions of refs.',
    codeSnippet: 'git -c protocol.version=2 fetch origin',
    tags: ['git', 'protocol-v2', 'transfer', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l3-011',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'tree mode bits',
    question: 'What do the following Git tree entry modes represent: 100644, 100755, 040000?',
    options: [
      'Regular file (non-executable), regular file (executable), directory (subtree)',
      'Blob, commit, tree',
      'Read-only file, read-write file, directory',
      'Text file, binary file, symlink',
    ],
    correctAnswer: 0,
    explanation: '`100644` = regular non-executable file, `100755` = regular executable file, `040000` = directory (subtree). Other modes include `120000` (symlink) and `160000` (gitlink/submodule).',
    codeSnippet: 'git ls-tree HEAD',
    tags: ['git', 'tree', 'mode', 'permissions', 'internals'],
  },
  {
    id: 'git-internals-l3-012',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'refspec',
    question: 'What does the refspec `+refs/heads/*:refs/remotes/origin/*` mean when used in a fetch?',
    options: [
      'Push all local branches to the remote with force',
      'Fetch all remote branches and map them to local remote-tracking refs, with `+` allowing non-fast-forward updates',
      'Fetch only branches starting with "refs"',
      'Sync all refs bidirectionally',
    ],
    correctAnswer: 1,
    explanation: 'The `+` prefix forces the update even if it is not a fast-forward. The pattern maps every branch on the remote (`refs/heads/*`) to a local remote-tracking ref (`refs/remotes/origin/*`).',
    tags: ['git', 'refspec', 'fetch', 'remote-tracking', 'internals'],
  },
  {
    id: 'git-internals-l3-013',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'multi-pack-index',
    question: 'What is the purpose of the multi-pack-index (MIDX) file?',
    options: [
      'It replaces multiple pack files with a single merged pack',
      'It provides a single index spanning multiple pack files, enabling faster object lookup without repacking',
      'It indexes commit messages across all packs for full-text search',
      'It caches delta chain lengths to avoid re-computation',
    ],
    correctAnswer: 1,
    explanation: 'MIDX stores a combined index over all existing pack files, so Git can binary-search all packs in one pass rather than consulting each pack\'s `.idx` separately, improving lookup speed.',
    codeSnippet: 'git multi-pack-index write',
    tags: ['git', 'midx', 'multi-pack-index', 'packfile', 'performance'],
  },
  {
    id: 'git-internals-l3-014',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'object model',
    question: 'What is a "gitlink" (mode 160000 in a tree), and what does it point to?',
    options: [
      'A hard link to a file in another repository',
      'A tree entry that records a submodule: it stores the commit SHA of the pinned submodule state',
      'A symlink stored as a regular blob with a special mode',
      'A reference to a blob in another pack file',
    ],
    correctAnswer: 1,
    explanation: 'When a subdirectory is a Git submodule, its tree entry has mode `160000` and stores the SHA-1 of the commit in the submodule repository that the parent repo is pinned to — not a tree or blob SHA.',
    codeSnippet: 'git ls-tree HEAD submodule-dir\n# 160000 commit <sha> submodule-dir',
    tags: ['git', 'gitlink', 'submodule', 'tree', 'mode', 'internals'],
  },
  {
    id: 'git-internals-l3-015',
    language: 'git',
    level: 'level3',
    category: 'Git Internals',
    subcategory: 'reachability bitmaps',
    question: 'What are reachability bitmaps (`.bitmap` files) in Git, and what do they accelerate?',
    options: [
      'Images embedded in the repository for documentation',
      'Bitsets indexed per commit that record which objects are reachable, dramatically speeding up `git clone` pack generation',
      'Bitmasks for file permissions stored in the index',
      'Compressed bloom filters for fast substring search in commit messages',
    ],
    correctAnswer: 1,
    explanation: 'Bitmap files store a bitset per tip commit indicating object reachability. During clone, the server can union/intersect bitmaps to quickly identify what to include in the pack, reducing CPU time for popular repos.',
    codeSnippet: 'git repack -d --write-bitmap-index',
    tags: ['git', 'bitmap', 'reachability', 'clone', 'performance', 'internals'],
  },
];

export default questions;
