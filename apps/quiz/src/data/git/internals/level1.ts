import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-internals-l1-001',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'object model',
    question: 'What are the four fundamental object types in Git\'s object model?',
    options: [
      'file, directory, commit, branch',
      'blob, tree, commit, tag',
      'blob, index, ref, pack',
      'data, pointer, snapshot, delta',
    ],
    correctAnswer: 1,
    explanation: 'Git stores everything as one of four object types: blob (file content), tree (directory listing), commit (snapshot + metadata), and tag (annotated label). Every object is identified by its SHA-1 hash.',
    tags: ['git', 'internals', 'object-model', 'blob', 'tree', 'commit', 'tag'],
  },
  {
    id: 'git-internals-l1-002',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'blob',
    question: 'What does a Git blob object store?',
    options: [
      'A directory listing with file names and permissions',
      'The raw content of a file, without its name or path',
      'A commit message and author information',
      'A list of parent commits',
    ],
    correctAnswer: 1,
    explanation: 'A blob stores only the raw bytes of a file\'s content. File names and paths are stored in tree objects that reference blobs by their SHA-1 hashes.',
    codeSnippet: 'git cat-file -t HEAD:README.md  # shows "blob"',
    tags: ['git', 'internals', 'blob'],
  },
  {
    id: 'git-internals-l1-003',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'tree',
    question: 'What does a Git tree object represent?',
    options: [
      'The entire history of the repository',
      'A single file with its metadata',
      'A directory: a list of entries (blobs and sub-trees) with names, modes, and SHA-1s',
      'The commit graph',
    ],
    correctAnswer: 2,
    explanation: 'A tree object is analogous to a directory: each entry holds a file-mode, the object type (blob or tree), a SHA-1 hash, and the name, allowing Git to reconstruct any directory snapshot.',
    codeSnippet: 'git cat-file -p HEAD^{tree}',
    tags: ['git', 'internals', 'tree', 'directory'],
  },
  {
    id: 'git-internals-l1-004',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'commit',
    question: 'Which fields are stored inside a Git commit object?',
    options: [
      'tree, parent(s), author, committer, message',
      'blob, branch, author, timestamp',
      'tree, index, tagger, message',
      'parent, diff, author, branch',
    ],
    correctAnswer: 0,
    explanation: 'A commit object records: the root tree SHA, zero or more parent commit SHAs, author identity+timestamp, committer identity+timestamp, and the commit message.',
    codeSnippet: 'git cat-file -p HEAD',
    tags: ['git', 'internals', 'commit', 'tree', 'parent'],
  },
  {
    id: 'git-internals-l1-005',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'SHA-1',
    question: 'How does Git use SHA-1 hashes to identify objects?',
    options: [
      'As sequential IDs assigned at commit time',
      'As content-addressable keys: the hash is computed from the object\'s content so identical content always yields the same SHA',
      'As timestamps encoded in base-16',
      'As random UUIDs assigned per repository',
    ],
    correctAnswer: 1,
    explanation: 'Git is a content-addressable store: each object\'s SHA-1 is deterministically derived from its type, size, and content. Two objects with identical content will always have the same SHA-1.',
    tags: ['git', 'internals', 'SHA-1', 'content-addressable'],
  },
  {
    id: 'git-internals-l1-006',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'HEAD file',
    question: 'What does the file `.git/HEAD` typically contain on a normal branch?',
    options: [
      'The SHA-1 of the latest commit',
      'The name of the current branch as a symbolic ref, e.g. `ref: refs/heads/main`',
      'A list of all recent commits',
      'The SHA-1 of the root tree',
    ],
    correctAnswer: 1,
    explanation: 'In a normal (attached) state, `.git/HEAD` is a symbolic ref — a text file containing `ref: refs/heads/<branchname>`. In detached HEAD state it contains a raw SHA-1.',
    codeSnippet: 'cat .git/HEAD\n# ref: refs/heads/main',
    tags: ['git', 'HEAD', 'symbolic-ref', 'internals'],
  },
  {
    id: 'git-internals-l1-007',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'index',
    question: 'What is the Git index (staging area)?',
    options: [
      'A log of all past commits',
      'A binary file tracking what will go into the next commit',
      'The set of all remote branches',
      'The compressed pack of all objects',
    ],
    correctAnswer: 1,
    explanation: 'The index (`.git/index`) is a binary file that acts as the staging area: it records the file paths, modes, blob SHAs, and stat information that will form the next commit\'s tree.',
    tags: ['git', 'index', 'staging', 'internals'],
  },
  {
    id: 'git-internals-l1-008',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'object storage',
    question: 'Where are loose Git objects stored on disk?',
    options: [
      '`.git/objects/<first-2-chars-of-sha>/<remaining-38-chars>`',
      '`.git/refs/<sha>`',
      '`.git/index/<sha>`',
      '`.git/commits/<sha>`',
    ],
    correctAnswer: 0,
    explanation: 'Git stores loose objects under `.git/objects/` using a two-level directory scheme: the first two hex characters form the subdirectory, and the remaining 38 characters are the filename.',
    codeSnippet: 'ls .git/objects/ab/\n# cdef1234...  (38-char filename)',
    tags: ['git', 'internals', 'loose-objects', 'object-storage'],
  },
  {
    id: 'git-internals-l1-009',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'cat-file',
    question: 'What does `git cat-file -t <sha>` output?',
    options: [
      'The full content of the object',
      'The type of the object: blob, tree, commit, or tag',
      'The size of the object in bytes',
      'The parent SHA of the object',
    ],
    correctAnswer: 1,
    explanation: '`-t` prints the object type. Use `-p` to pretty-print the content and `-s` to show the size in bytes.',
    codeSnippet: 'git cat-file -t HEAD   # outputs: commit',
    tags: ['git', 'cat-file', 'object-type', 'internals'],
  },
  {
    id: 'git-internals-l1-010',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'refs',
    question: 'What are Git refs?',
    options: [
      'Compressed archives of object history',
      'Human-readable names (like branch and tag names) that point to commit SHA-1 hashes',
      'Remote tracking configuration files',
      'Cached diffs between commits',
    ],
    correctAnswer: 1,
    explanation: 'Refs are files under `.git/refs/` (or entries in `packed-refs`) that contain a SHA-1, providing named pointers — branches (`refs/heads/`), tags (`refs/tags/`), and remote-tracking refs (`refs/remotes/`).',
    tags: ['git', 'refs', 'branch', 'tag', 'internals'],
  },
  {
    id: 'git-internals-l1-011',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'packfiles',
    question: 'What is a Git packfile?',
    options: [
      'A zip archive of the entire repository',
      'A binary file that stores many objects together, using delta compression to save space',
      'A list of all commit SHAs in the repository',
      'A configuration file for packing large files with Git LFS',
    ],
    correctAnswer: 1,
    explanation: 'Packfiles (`.git/objects/pack/*.pack`) bundle many loose objects into a single file and use delta compression (storing differences between similar objects) to dramatically reduce storage size.',
    tags: ['git', 'packfile', 'delta-compression', 'internals'],
  },
  {
    id: 'git-internals-l1-012',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'hash-object',
    question: 'What does `git hash-object <file>` do?',
    options: [
      'Adds the file to the staging area',
      'Computes and prints the SHA-1 that Git would use for the file\'s content, without storing it',
      'Stores the file as a blob and returns its SHA-1',
      'Computes an MD5 checksum of the file',
    ],
    correctAnswer: 1,
    explanation: 'Without `-w`, `git hash-object` computes the SHA-1 of the file as Git would store it (with the blob header) but does not write any object. Add `-w` to actually write the blob.',
    codeSnippet: 'git hash-object README.md\ngit hash-object -w README.md  # writes blob',
    tags: ['git', 'hash-object', 'blob', 'SHA-1'],
  },
  {
    id: 'git-internals-l1-013',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'symbolic refs',
    question: 'What is a symbolic ref in Git?',
    options: [
      'A ref stored as a GPG-signed tag',
      'A ref that contains the name of another ref rather than a direct SHA-1',
      'A ref used only for remote branches',
      'A special ref that always points to the initial commit',
    ],
    correctAnswer: 1,
    explanation: 'A symbolic ref is an indirection — it contains the string `ref: refs/heads/<name>` instead of a SHA-1. HEAD is the canonical example; it points to the current branch ref.',
    codeSnippet: 'git symbolic-ref HEAD\n# refs/heads/main',
    tags: ['git', 'symbolic-ref', 'HEAD', 'internals'],
  },
  {
    id: 'git-internals-l1-014',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'git fsck',
    question: 'What is the purpose of `git fsck`?',
    options: [
      'Formats and speeds up the commit graph',
      'Verifies the integrity of the object database, finding corrupted or unreachable objects',
      'Flushes the staging-area cache',
      'Fetches and syncs all remote refs',
    ],
    correctAnswer: 1,
    explanation: '`git fsck` (file system check) walks the entire object graph, verifying that every referenced object exists and is internally consistent, and reports dangling or corrupted entries.',
    tags: ['git', 'fsck', 'integrity', 'internals'],
  },
  {
    id: 'git-internals-l1-015',
    language: 'git',
    level: 'level1',
    category: 'Git Internals',
    subcategory: 'commit graph',
    question: 'What type of data structure does the Git commit history form?',
    options: [
      'A doubly linked list',
      'A directed acyclic graph (DAG)',
      'A balanced binary tree',
      'A circular buffer',
    ],
    correctAnswer: 1,
    explanation: 'Git commits form a DAG: each commit points to one or more parents (never itself or creating cycles), enabling non-linear history through branches and merges.',
    tags: ['git', 'commit', 'DAG', 'history', 'internals'],
  },
];

export default questions;
