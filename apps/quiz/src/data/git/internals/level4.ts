import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-internals-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'object format sha256',
    question: 'In the new SHA-256 object format (git --object-format=sha256), how are SHA-1 repositories bridged for interoperability?',
    options: [
      'Git maintains a compatibility SHA-1 → SHA-256 mapping table in `.git/compat/`',
      'Interoperability objects contain both a SHA-256 hash and a compatibility SHA-1, stored in the object header extensions',
      'SHA-256 repos can push to SHA-1 remotes transparently via server-side translation',
      'There is currently no supported interoperability path; they are entirely separate formats',
    ],
    correctAnswer: 1,
    explanation: 'The SHA-256 transition plan specifies "compatibility SHA-1" values stored as extensions in object headers so SHA-256 repos can map their objects to SHA-1 equivalents when communicating with SHA-1 servers.',
    tags: ['git', 'SHA-256', 'object-format', 'interoperability', 'internals'],
  },
  {
    id: 'git-internals-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'delta compression algorithm',
    question: 'Git\'s xdelta-style pack delta instruction set has three instruction types. Which set is correct?',
    options: [
      'INSERT, COPY, DELETE',
      'ADD, REMOVE, REPLACE',
      'COPY_FROM_BASE, INSERT_LITERAL (no delete instruction)',
      'SEEK, READ, WRITE',
    ],
    correctAnswer: 2,
    explanation: 'Git\'s binary delta format has exactly two instruction types: COPY (copy a range of bytes from the base object) and INSERT (include literal bytes from the delta stream). Deletions are represented by the absence of a COPY for those bytes.',
    tags: ['git', 'delta', 'xdelta', 'COPY', 'INSERT', 'packfile', 'internals'],
  },
  {
    id: 'git-internals-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'index v4',
    question: 'What compression technique does Git index format v4 use to reduce the index file size?',
    options: [
      'zlib compression of the entire index binary',
      'Prefix compression of path names: each entry stores only the suffix that differs from the previous entry\'s path',
      'Delta encoding of blob SHAs between adjacent entries',
      'Run-length encoding of repeated stat fields',
    ],
    correctAnswer: 1,
    explanation: 'Index v4 uses path prefix compression: each entry omits the shared path prefix with the previous entry and encodes only the unique suffix, significantly reducing index size for repos with deep directory trees.',
    tags: ['git', 'index-v4', 'prefix-compression', 'path', 'internals'],
  },
  {
    id: 'git-internals-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'commit graph generation v2',
    question: 'What are "corrected committer dates" (generation number v2) in the commit-graph, and why were they introduced?',
    options: [
      'Timestamps corrected to UTC to handle timezone inconsistencies',
      'Monotonically non-decreasing values that equal max(parent values, committer date), fixing issues where clock skew caused child commits to have earlier dates than parents',
      'Commit sequence numbers assigned by the server during push',
      'Estimated dates for commits whose timestamps were corrupted',
    ],
    correctAnswer: 1,
    explanation: 'Classic generation numbers fail when committer dates are out of order (clock skew). Generation v2 uses `max(parent corrected dates, committer date)`, guaranteeing monotonicity and enabling correct time-based graph queries.',
    tags: ['git', 'commit-graph', 'generation-v2', 'corrected-date', 'internals'],
  },
  {
    id: 'git-internals-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'bitmap v2',
    question: 'How does Git\'s bitmap v2 (BITMAPV2) improve on v1 for partial clone / filter scenarios?',
    options: [
      'It adds per-object type bitmaps for blobs, trees, and commits separately',
      'It supports "pseudo-merge bitmaps" that encode reachability for commit ranges, not just individual commits',
      'It compresses bitmaps with Zstandard instead of EWAH',
      'It stores bitmaps for every commit rather than only selected tip commits',
    ],
    correctAnswer: 1,
    explanation: 'Bitmap v2 introduces pseudo-merge bitmaps that cover large sets of commits (e.g. older history), enabling fast reachability queries for partial clone filters without requiring a bitmap per commit.',
    tags: ['git', 'bitmap-v2', 'pseudo-merge', 'partial-clone', 'internals'],
  },
  {
    id: 'git-internals-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'protocol v2 capabilities',
    question: 'Which capability introduced in Git protocol v2 allows a client to fetch only a filtered subset of objects (e.g. no blobs)?',
    options: [
      'ref-in-want',
      'filter',
      'shallow',
      'object-info',
    ],
    correctAnswer: 1,
    explanation: 'The `filter` capability (combined with `--filter=blob:none` or similar) instructs the server to omit specified object types during pack generation, enabling partial clones that lazily fetch blobs on demand.',
    codeSnippet: 'git clone --filter=blob:none <url>',
    tags: ['git', 'protocol-v2', 'filter', 'partial-clone', 'internals'],
  },
  {
    id: 'git-internals-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'loose object race',
    question: 'Why does Git use a two-step write (write to `.git/objects/tmp_<N>` then rename) when creating loose objects?',
    options: [
      'To allow the garbage collector to skip temp files',
      'To make writes atomic: a rename on POSIX filesystems is atomic, preventing other processes from reading a partially-written object',
      'To avoid triggering inotify events until the object is complete',
      'To support CoW (copy-on-write) filesystems like btrfs',
    ],
    correctAnswer: 1,
    explanation: 'Writing content to a temp file and then renaming it to the final SHA path ensures that any other process either sees the old (absent) or new (complete) object — never a partial write, preserving object integrity.',
    tags: ['git', 'loose-objects', 'atomic-write', 'rename', 'internals'],
  },
  {
    id: 'git-internals-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'ref storage backend',
    question: 'What is the "reftable" format, and what problem does it solve compared to the filesystem-based ref storage?',
    options: [
      'A relational database for refs enabling SQL queries over branch history',
      'A sorted, block-based on-disk format that provides O(log n) ref lookup and atomic multi-ref updates, solving scale and race-condition problems of individual ref files',
      'A memory-mapped ref cache replacing packed-refs for performance',
      'An immutable append-only log of all ref changes since repo creation',
    ],
    correctAnswer: 1,
    explanation: 'Reftable (used by default in new repos in Git 2.45+) stores all refs in a sorted table with indexed blocks, enabling fast lookups, atomic transactions that update many refs at once, and an integrated reflog — solving the per-file overhead and TOCTOU issues of the classic filesystem storage.',
    tags: ['git', 'reftable', 'refs', 'atomic', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'cat-file batch',
    question: 'How does `git cat-file --batch` differ from calling `git cat-file -p <sha>` repeatedly?',
    options: [
      'It is identical; --batch is just an alias',
      'It reads SHA-1s from stdin line by line and streams object contents without forking a new process per object, dramatically reducing overhead for bulk access',
      'It outputs objects in binary rather than text form',
      'It only works with pack files, not loose objects',
    ],
    correctAnswer: 1,
    explanation: '`git cat-file --batch` is a single long-running process that reads object identifiers from stdin and writes header + content for each, avoiding per-object fork/exec cost — critical for tooling that needs to inspect thousands of objects.',
    codeSnippet: 'git rev-list HEAD | git cat-file --batch',
    tags: ['git', 'cat-file', 'batch', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'tree walk optimisation',
    question: 'How does the `core.untrackedCache` feature speed up `git status` on large repositories?',
    options: [
      'It caches the full working tree hash to avoid any filesystem traversal',
      'It records per-directory mtime values; if a directory mtime is unchanged, Git skips re-scanning that directory for untracked files',
      'It stores a bloom filter of untracked file paths in the index',
      'It pre-computes the diff between HEAD and the working tree',
    ],
    correctAnswer: 1,
    explanation: 'The untracked cache stores the mtime of each directory alongside its list of untracked children. On subsequent `git status` calls, directories whose mtime has not changed are skipped entirely, dramatically reducing syscall count.',
    codeSnippet: 'git config core.untrackedCache true',
    tags: ['git', 'untracked-cache', 'status', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l4-011',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'FETCH_HEAD',
    question: 'What does `.git/FETCH_HEAD` record, and how does `git merge FETCH_HEAD` use it?',
    options: [
      'The SHA of the last successfully pushed commit',
      'All fetched branch tips from the last fetch, with their remote names; `git merge FETCH_HEAD` merges the first (non-not-for-merge) entry',
      'The remote URL used in the last fetch',
      'A lock file preventing concurrent fetches',
    ],
    correctAnswer: 1,
    explanation: '`FETCH_HEAD` contains one line per fetched ref: SHA, optional "not-for-merge" flag, and the remote refname + URL. `git pull` internally runs `git merge FETCH_HEAD`, picking the first entry not marked "not-for-merge".',
    codeSnippet: 'cat .git/FETCH_HEAD',
    tags: ['git', 'FETCH_HEAD', 'fetch', 'merge', 'internals'],
  },
  {
    id: 'git-internals-l4-012',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'object existence check',
    question: 'How does `git cat-file -e <sha>` differ from `git cat-file -t <sha>` in performance terms?',
    options: [
      'They are equivalent; both read the full object',
      '`-e` only checks for the object\'s existence (exits 0 or 1) without reading or printing its content, making it faster for existence checks',
      '`-e` reads only the header; `-t` reads the full content',
      '`-e` works only on packed objects; `-t` works on loose objects',
    ],
    correctAnswer: 1,
    explanation: '`-e` (exists) does the minimum work to verify the object is present, returning exit code 0 (exists) or 1 (missing) without outputting anything, making it ideal for scripted existence checks.',
    codeSnippet: 'git cat-file -e HEAD:missing.txt || echo "not found"',
    tags: ['git', 'cat-file', 'exists', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l4-013',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'wire protocol sideband',
    question: 'In Git\'s wire protocol, what is the "sideband" mechanism?',
    options: [
      'An out-of-band TCP connection used for progress reporting',
      'A multiplexing scheme that encodes packfile data (sideband 1) and progress/error messages (sideband 2) in the same stream using pkt-line framing',
      'An SSH channel dedicated to authentication',
      'A secondary HTTP request for metadata while the pack download continues',
    ],
    correctAnswer: 1,
    explanation: 'Git\'s pkt-line stream can carry multiple "channels": sideband 1 carries the actual packfile bytes; sideband 2 carries human-readable progress and error messages, both interleaved in the same network connection.',
    tags: ['git', 'sideband', 'wire-protocol', 'pkt-line', 'internals'],
  },
  {
    id: 'git-internals-l4-014',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'merge base algorithm',
    question: 'What algorithm does Git use by default to find the merge base, and when does it differ from a simple LCA?',
    options: [
      'Dijkstra\'s shortest path; it differs when branches have different commit counts',
      'The "recursive" strategy finds the best common ancestor using a virtual merge of multiple LCA candidates when criss-cross merges create more than one LCA',
      'BFS from both branch tips; it always returns a single unique ancestor',
      'Tarjan\'s SCC algorithm; it differs on repos with cycles',
    ],
    correctAnswer: 1,
    explanation: 'In criss-cross merge scenarios, two branches can have multiple equally valid LCA candidates. The recursive merge strategy creates a virtual merge of those candidates as a synthetic base, avoiding the ambiguity.',
    tags: ['git', 'merge-base', 'recursive', 'LCA', 'criss-cross', 'internals'],
  },
  {
    id: 'git-internals-l4-015',
    language: 'git',
    level: 'level4',
    category: 'Git Internals',
    subcategory: 'sparse index',
    question: 'What is a "sparse index" in Git (introduced for sparse checkout), and how does it differ from a full index?',
    options: [
      'An index that only contains staged but not committed files',
      'An index that replaces entries for entire out-of-cone directories with a single tree-level entry, reducing index size and status time proportionally',
      'An index compressed with Zstandard instead of stored uncompressed',
      'An index that records only the 1000 most recently changed files',
    ],
    correctAnswer: 1,
    explanation: 'A sparse index collapses all entries under directories excluded from the sparse-checkout cone into one tree entry per directory, making the index size proportional to the sparse cone rather than the full repository.',
    codeSnippet: 'git sparse-checkout init --cone\ngit config index.sparse true',
    tags: ['git', 'sparse-index', 'sparse-checkout', 'performance', 'internals'],
  },
];

export default questions;
