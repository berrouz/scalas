import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-internals-l2-001',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'object model',
    question: 'If you rename a file without changing its content, does Git create a new blob object?',
    options: [
      'Yes, because the path changed',
      'No, the same blob SHA is reused; only the tree object changes',
      'Yes, because file metadata is stored in the blob',
      'It depends on the file size',
    ],
    correctAnswer: 1,
    explanation: 'Blobs store only content; names live in trees. Renaming a file produces a new tree object referencing the same blob SHA, so no new blob is created — only the tree changes.',
    tags: ['git', 'internals', 'blob', 'tree', 'rename'],
  },
  {
    id: 'git-internals-l2-002',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'SHA-1 format',
    question: 'How does Git prepend a header to content before computing a blob\'s SHA-1?',
    options: [
      '"blob\\0<content>"',
      '"blob <size>\\0<content>"',
      '"file <size>\\n<content>"',
      '"object blob\\n<content>"',
    ],
    correctAnswer: 1,
    explanation: 'Git computes `SHA1("blob <bytesize>\\0<content>")` — the header includes the object type, a space, the byte length as a decimal string, and a NUL byte, followed by the raw content.',
    codeSnippet: 'printf "blob 5\\0hello" | sha1sum',
    tags: ['git', 'internals', 'SHA-1', 'blob', 'header'],
  },
  {
    id: 'git-internals-l2-003',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'index internals',
    question: 'Besides the blob SHA and path, what performance-critical data does the Git index store for each file?',
    options: [
      'The file\'s gzip checksum',
      'File stat information (mtime, ctime, size, inode) used to detect modifications without hashing',
      'The line count and character encoding',
      'The diff against the previous version',
    ],
    correctAnswer: 1,
    explanation: 'The index caches OS stat data (mtime, ctime, dev, ino, uid, gid, size) alongside each entry\'s SHA. Git compares this cached stat to the current filesystem stat before deciding whether to re-hash a file.',
    tags: ['git', 'index', 'stat', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l2-004',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'packfile index',
    question: 'What is the role of the `.idx` file that accompanies a `.pack` file?',
    options: [
      'It stores the delta bases needed to reconstruct objects',
      'It provides a sorted index from SHA-1 to byte offset within the pack, enabling O(log n) lookups',
      'It records which commits were packed and when',
      'It is a human-readable manifest of the pack contents',
    ],
    correctAnswer: 1,
    explanation: 'The pack index (`.idx`) contains a fan-out table and a sorted list of SHA-1s with their offsets in the `.pack` file, allowing Git to binary-search for any object without scanning the entire pack.',
    tags: ['git', 'packfile', 'idx', 'index', 'internals'],
  },
  {
    id: 'git-internals-l2-005',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'delta compression',
    question: 'How does Git\'s delta compression in packfiles differ from file-level diff tools?',
    options: [
      'Git deltas are line-based like unified diffs',
      'Git deltas are binary copy-and-insert instructions on raw byte data, applied across similar objects regardless of type',
      'Git deltas operate only on text files',
      'Git deltas use the LZ77 algorithm identical to gzip',
    ],
    correctAnswer: 1,
    explanation: 'Git packfile deltas use a binary instruction set: "copy N bytes from base at offset X" or "insert M literal bytes." This works on raw byte content, making it efficient for any file type.',
    tags: ['git', 'delta-compression', 'packfile', 'internals'],
  },
  {
    id: 'git-internals-l2-006',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'cat-file',
    question: 'What command pretty-prints the content of any Git object given its SHA-1?',
    options: [
      'git show <sha>',
      'git cat-file -p <sha>',
      'git log -1 <sha>',
      'git read-tree <sha>',
    ],
    correctAnswer: 1,
    explanation: '`git cat-file -p <sha>` pretty-prints any object type: for blobs it shows raw content, for trees a formatted listing, for commits the header fields, and for tags the tag metadata.',
    codeSnippet: 'git cat-file -p HEAD\ngit cat-file -p HEAD^{tree}',
    tags: ['git', 'cat-file', 'pretty-print', 'internals'],
  },
  {
    id: 'git-internals-l2-007',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'loose vs packed',
    question: 'When does `git gc` convert loose objects into a packfile?',
    options: [
      'Every time you commit',
      'When the number of loose objects exceeds the threshold set by `gc.auto` (default 6700)',
      'Only when you run `git push`',
      'After every `git fetch`',
    ],
    correctAnswer: 1,
    explanation: '`git gc --auto` (called implicitly by many commands) packs loose objects when their count exceeds `gc.auto` (default 6700), keeping the object database efficient.',
    codeSnippet: 'git config gc.auto 256  # pack sooner',
    tags: ['git', 'gc', 'loose-objects', 'packfile', 'auto'],
  },
  {
    id: 'git-internals-l2-008',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'refs',
    question: 'What is the full path of the ref for a branch named `feature/login`?',
    options: [
      '`.git/branches/feature/login`',
      '`.git/refs/heads/feature/login`',
      '`.git/refs/branches/feature/login`',
      '`.git/heads/feature/login`',
    ],
    correctAnswer: 1,
    explanation: 'Local branch refs are stored under `.git/refs/heads/`. Slashes in branch names become subdirectory separators, so `feature/login` lives at `.git/refs/heads/feature/login`.',
    codeSnippet: 'cat .git/refs/heads/feature/login',
    tags: ['git', 'refs', 'branch', 'internals'],
  },
  {
    id: 'git-internals-l2-009',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'transfer protocols',
    question: 'What is the "smart" HTTP protocol in Git?',
    options: [
      'A protocol that compresses all data with HTTPS',
      'A protocol where the server participates in negotiation to send only missing objects, unlike the dumb protocol',
      'A protocol that uses WebSockets for real-time push',
      'HTTP/2-based transfer with multiplexed streams',
    ],
    correctAnswer: 1,
    explanation: 'In the smart HTTP protocol the server runs `git-upload-pack`/`git-receive-pack` over HTTP, enabling capability negotiation so only the minimal set of objects is transferred. The dumb protocol just serves static files.',
    tags: ['git', 'protocol', 'smart-http', 'transfer', 'internals'],
  },
  {
    id: 'git-internals-l2-010',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'hash-object',
    question: 'What flag must you add to `git hash-object` to actually store the object in the repository?',
    options: ['-s (--store)', '-w (--write)', '-a (--add)', '-c (--cache)'],
    correctAnswer: 1,
    explanation: '`git hash-object -w <file>` computes the SHA-1 and writes the resulting blob object to `.git/objects/`. Without `-w`, only the hash is printed.',
    codeSnippet: 'echo "hello" | git hash-object -w --stdin',
    tags: ['git', 'hash-object', '-w', 'blob', 'internals'],
  },
  {
    id: 'git-internals-l2-011',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'commit graph',
    question: 'What is the `.git/objects/info/commit-graph` file?',
    options: [
      'A JSON export of all commit metadata',
      'A binary cache of reachability and generation numbers for commits, speeding up graph traversals',
      'The output of `git log --graph`',
      'A list of merge commits for conflict detection',
    ],
    correctAnswer: 1,
    explanation: 'The commit-graph file (introduced in Git 2.18) stores a serialized representation of the commit DAG including generation numbers, enabling O(1) reachability checks and faster operations like `git log` and `git merge-base`.',
    codeSnippet: 'git commit-graph write --reachable',
    tags: ['git', 'commit-graph', 'performance', 'internals'],
  },
  {
    id: 'git-internals-l2-012',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'fsck',
    question: 'What kind of objects does `git fsck --unreachable` report?',
    options: [
      'Objects that are corrupted',
      'Objects not referenced by any branch, tag, or other reachable ref',
      'Objects stored in packfiles rather than loose',
      'Objects larger than 1 MB',
    ],
    correctAnswer: 1,
    explanation: 'Unreachable objects exist in the object store but are not referenced by any ref or reachable commit. They are candidates for pruning by `git gc --prune`.',
    codeSnippet: 'git fsck --unreachable',
    tags: ['git', 'fsck', 'unreachable', 'gc', 'internals'],
  },
  {
    id: 'git-internals-l2-013',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'object model',
    question: 'Two different commits that represent the exact same file tree will have the same root tree SHA. True or false?',
    options: [
      'True — trees are content-addressed, so identical directory snapshots always share the same SHA',
      'False — tree SHAs include the commit timestamp',
      'False — each commit generates a unique tree SHA',
      'True, but only for lightweight commits without merges',
    ],
    correctAnswer: 0,
    explanation: 'Tree objects are pure content-addressed snapshots. If two commits result in an identical directory structure (same file names, modes, and content), their root tree SHAs are identical and Git only stores one tree object.',
    tags: ['git', 'internals', 'tree', 'content-addressable', 'dedup'],
  },
  {
    id: 'git-internals-l2-014',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'object storage',
    question: 'How are loose Git objects compressed on disk?',
    options: [
      'They are not compressed; only packfiles use compression',
      'They are compressed with zlib (deflate)',
      'They are compressed with bzip2',
      'They are compressed with LZ4',
    ],
    correctAnswer: 1,
    explanation: 'Each loose object file is compressed with zlib deflate. The raw (uncompressed) content is `<type> <size>\\0<content>`; the entire string is deflated before writing to disk.',
    tags: ['git', 'internals', 'zlib', 'loose-objects', 'compression'],
  },
  {
    id: 'git-internals-l2-015',
    language: 'git',
    level: 'level2',
    category: 'Git Internals',
    subcategory: 'refs',
    question: 'What is the purpose of `packed-refs` and under what circumstance does it grow?',
    options: [
      'It caches remote refs; it grows every time you fetch',
      'It consolidates loose ref files into one file; `git gc` or `git pack-refs` writes refs here to reduce filesystem overhead',
      'It stores refs for packed objects only',
      'It is a read-only snapshot of refs taken at clone time',
    ],
    correctAnswer: 1,
    explanation: '`packed-refs` consolidates many small ref files into a single file, reducing inode usage and speeding up ref lookups. `git pack-refs --all` or `git gc` triggers this consolidation.',
    codeSnippet: 'git pack-refs --all\ncat .git/packed-refs',
    tags: ['git', 'packed-refs', 'gc', 'refs', 'internals'],
  },
];

export default questions;
