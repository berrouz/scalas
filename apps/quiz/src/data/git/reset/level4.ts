import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-reset-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'reset internals',
    question: 'Explain exactly what Git writes to disk when you execute `git reset --hard <sha>` on a normal (non-detached) branch.',
    options: [
      'It updates the branch ref file, the ORIG_HEAD file, the index binary, and all working-tree files to match the target tree',
      'It updates only the HEAD symref and the working tree',
      'It rewrites the pack file to remove commits after <sha>',
      'It updates the index and working tree but does not change any ref file',
    ],
    correctAnswer: 0,
    explanation: 'Git writes the new SHA to the branch ref (e.g. `.git/refs/heads/main`), writes the old HEAD to `.git/ORIG_HEAD`, overwrites `.git/index` with the target tree, and updates every tracked file in the working tree.',
    codeSnippet: 'cat .git/refs/heads/main\ncat .git/ORIG_HEAD',
    tags: ['git', 'reset', 'hard', 'internals', 'refs'],
  },
  {
    id: 'git-reset-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'reflog internals',
    question: 'Where exactly does Git store reflog entries, and in what format?',
    options: [
      'In `.git/logs/` as plain-text files, one line per entry: old-SHA new-SHA author timestamp message',
      'In the pack file as special delta objects',
      'In `.git/info/` as a JSON array',
      'In the commit graph file as auxiliary metadata',
    ],
    correctAnswer: 0,
    explanation: 'Each ref has a corresponding file under `.git/logs/` (e.g. `.git/logs/refs/heads/main`) containing space-separated lines of old-sha, new-sha, identity, Unix timestamp+tz, and the operation message.',
    codeSnippet: 'cat .git/logs/HEAD',
    tags: ['git', 'reflog', 'internals', 'logs'],
  },
  {
    id: 'git-reset-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'reset vs checkout tree',
    question: 'What is the key semantic difference between `git reset --hard <commit>` and `git checkout <commit>` (without a path)?',
    options: [
      'reset --hard moves the current branch tip; checkout detaches HEAD',
      'checkout moves the current branch tip; reset --hard detaches HEAD',
      'They are equivalent; only the flags differ',
      'reset --hard only works on the index; checkout works on the working tree',
    ],
    correctAnswer: 0,
    explanation: '`git reset --hard` moves the branch ref that HEAD points to, rewriting branch history. `git checkout <commit>` moves HEAD itself to point directly at the commit (detached HEAD), leaving the branch untouched.',
    tags: ['git', 'reset', 'checkout', 'branch', 'detached-HEAD'],
  },
  {
    id: 'git-reset-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'revert internals',
    question: 'How does `git revert` determine what changes to apply when inverting a commit?',
    options: [
      'It diffs the commit\'s tree against its parent\'s tree and applies the reverse patch',
      'It reads the commit message and undoes the named files',
      'It does a three-way merge between the commit, its parent, and HEAD',
      'It rebuilds the commit from scratch using the reflog',
    ],
    correctAnswer: 2,
    explanation: 'Git revert performs a three-way merge: the commit being reverted is the "ours", its parent is the merge base, and the current HEAD is "theirs". This properly handles context that has changed since the original commit.',
    tags: ['git', 'revert', 'three-way-merge', 'internals'],
  },
  {
    id: 'git-reset-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'restore internals',
    question: 'Under the hood, what does `git restore --staged <file>` write to the index?',
    options: [
      'The blob SHA from the working directory',
      'The blob SHA from the HEAD tree for that path',
      'A zero SHA indicating the file should be removed',
      'The blob SHA from the most recent stash entry',
    ],
    correctAnswer: 1,
    explanation: 'The command reads the blob object SHA recorded in the HEAD commit\'s tree for that path and writes that SHA (along with mode and stat data) into the index entry, effectively unstaging the change.',
    tags: ['git', 'restore', 'staged', 'index', 'blob'],
  },
  {
    id: 'git-reset-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'clean internals',
    question: 'How does Git decide which files `git clean` will remove when run without `-x`?',
    options: [
      'Any file not listed in the pack file index',
      'Any file whose path is not tracked by the index AND is not matched by .gitignore rules',
      'Any file not tracked by the index, including ignored files',
      'Any file modified since the last commit',
    ],
    correctAnswer: 1,
    explanation: 'Without `-x`, `git clean` only removes untracked non-ignored files. It checks the index for tracked files and consults gitignore rules to exclude ignored paths, removing only what falls through both filters.',
    tags: ['git', 'clean', 'untracked', 'gitignore', 'internals'],
  },
  {
    id: 'git-reset-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'reset after push',
    question: 'A colleague force-pushed a `git reset --hard` to a shared branch, discarding your commits. Which git command helps you find the lost commits without access to their machine?',
    options: [
      'git log --all --remotes',
      'git reflog origin/main',
      'git fsck --lost-found',
      'git gc --aggressive',
    ],
    correctAnswer: 2,
    explanation: '`git fsck --lost-found` walks the entire object database and writes unreachable commit and blob objects to `.git/lost-found/`, allowing recovery of commits not referenced by any ref or reflog.',
    codeSnippet: 'git fsck --lost-found\nls .git/lost-found/commit/',
    tags: ['git', 'fsck', 'lost-found', 'recovery'],
  },
  {
    id: 'git-reset-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'revert cherry-pick symmetry',
    question: 'What is the relationship between `git revert <sha>` and `git cherry-pick <sha>`?',
    options: [
      'They are unrelated commands that happen to share similar syntax',
      'revert is the inverse of cherry-pick: cherry-pick applies a diff, revert applies its negation',
      'revert calls cherry-pick internally with a negated patch',
      'cherry-pick is only for single commits; revert works on ranges',
    ],
    correctAnswer: 1,
    explanation: 'Both commands apply a diff derived from a commit, but cherry-pick applies it forward (adding the changes) while revert applies the inverse diff (subtracting the changes), using the same three-way merge machinery.',
    tags: ['git', 'revert', 'cherry-pick', 'diff', 'internals'],
  },
  {
    id: 'git-reset-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'reflog recovery advanced',
    question: 'How would you use the reflog to restore a branch that was accidentally deleted?',
    options: [
      'git checkout -b <branch> HEAD@{1}',
      'git reflog expire --all && git branch <branch>',
      'Find the branch\'s last commit SHA in `git reflog`, then `git branch <branch> <sha>`',
      'git reset --hard refs/heads/<branch>@{0}',
    ],
    correctAnswer: 2,
    explanation: 'After deleting a branch, its commits are still in the reflog under HEAD or the branch\'s own log entry. You find the SHA and recreate the branch pointer with `git branch <name> <sha>` before the reflog expires.',
    codeSnippet: 'git reflog | grep "branch: Created from"\ngit branch recovered-branch <sha>',
    tags: ['git', 'reflog', 'branch-recovery', 'advanced'],
  },
  {
    id: 'git-reset-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'reset impact on upstream',
    question: 'You reset a published branch and then push with `--force-with-lease`. What does `--force-with-lease` check before overwriting?',
    options: [
      'That the remote SHA matches your local ORIG_HEAD',
      'That the remote branch tip matches your local remote-tracking ref (e.g. origin/main)',
      'That no CI pipeline is currently running on the branch',
      'That your local index is clean',
    ],
    correctAnswer: 1,
    explanation: '`--force-with-lease` verifies that the remote ref you last fetched is still the current remote tip; if someone else pushed in the meantime, the push is rejected, preventing overwriting their work.',
    codeSnippet: 'git push origin main --force-with-lease',
    tags: ['git', 'force-with-lease', 'push', 'reset', 'safety'],
  },
  {
    id: 'git-reset-l4-011',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'restore vs checkout vs reset',
    question: 'In Git 2.23+, which trio of commands cleanly replaces the overloaded responsibilities of the old `git checkout`?',
    options: [
      'git switch, git restore, git reset',
      'git branch, git restore, git revert',
      'git switch, git restore, git revert',
      'git checkout, git restore, git reset',
    ],
    correctAnswer: 0,
    explanation: 'Git 2.23 introduced `git switch` (change branches) and `git restore` (restore files), leaving `git reset` for index/HEAD manipulation, together replacing all three modes of the old `git checkout`.',
    tags: ['git', 'switch', 'restore', 'reset', 'checkout', 'git-2.23'],
  },
  {
    id: 'git-reset-l4-012',
    language: 'git',
    level: 'level4',
    category: 'Reset & Revert',
    subcategory: 'gc and lost objects',
    question: 'How long after a `git reset --hard` will `git gc` permanently prune the now-unreachable commits by default?',
    options: [
      'Immediately on the next `git gc` run',
      'After 14 days (the default gc.pruneExpire)',
      'After 30 days (gc.reflogExpireUnreachable)',
      'After 90 days (gc.reflogExpire)',
    ],
    correctAnswer: 2,
    explanation: 'Unreachable objects referenced only by reflog entries are protected for `gc.reflogExpireUnreachable` (default 30 days); once that expires, `git gc --prune` can delete them permanently.',
    codeSnippet: 'git config gc.reflogExpireUnreachable 30',
    tags: ['git', 'gc', 'prune', 'reflog', 'unreachable'],
  },
];

export default questions;
