import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-reset-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reset internals',
    question: 'In the three-tree model of Git, which trees does `git reset --mixed <commit>` modify?',
    options: [
      'HEAD and working directory only',
      'HEAD (repository) and index only',
      'Index and working directory only',
      'All three: HEAD, index, and working directory',
    ],
    correctAnswer: 1,
    explanation: 'Git\'s three trees are the repository (HEAD), the index, and the working directory. `--mixed` updates HEAD and the index but leaves the working directory untouched.',
    tags: ['git', 'reset', 'three-tree', 'internals'],
  },
  {
    id: 'git-reset-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reset internals',
    question: 'When you run `git reset --soft HEAD~1`, which file inside `.git/` is directly updated?',
    options: ['ORIG_HEAD only', 'HEAD and ORIG_HEAD', 'HEAD, ORIG_HEAD, and the index', 'Only the index'],
    correctAnswer: 1,
    explanation: 'Git updates the branch ref that HEAD points to (or HEAD itself in detached mode) and saves the old value in ORIG_HEAD; the index and working tree are not touched.',
    codeSnippet: 'cat .git/HEAD\ncat .git/ORIG_HEAD',
    tags: ['git', 'reset', 'soft', 'HEAD', 'ORIG_HEAD'],
  },
  {
    id: 'git-reset-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reflog recovery',
    question: 'After `git reset --hard HEAD~5`, which sequence correctly recovers the lost commit?',
    options: [
      'git log --all; git checkout <sha>',
      'git reflog; git reset --hard <sha>',
      'git stash list; git stash pop',
      'git fsck --lost-found; git merge FETCH_HEAD',
    ],
    correctAnswer: 1,
    explanation: 'The reflog tracks every HEAD movement; once you find the SHA of the lost commit you can `git reset --hard <sha>` to restore the branch pointer to that commit.',
    codeSnippet: 'git reflog\ngit reset --hard HEAD@{3}',
    tags: ['git', 'reflog', 'recovery', 'reset', 'hard'],
  },
  {
    id: 'git-reset-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reset path internals',
    question: 'What does `git reset <tree-ish> -- <path>` do that differs from the no-path form?',
    options: [
      'It also moves HEAD to <tree-ish>',
      'It only updates the index entry for <path> without moving HEAD',
      'It checks out the file into the working directory',
      'It is identical to the no-path form but scoped to one file',
    ],
    correctAnswer: 1,
    explanation: 'When a path is supplied, `git reset` copies the entry from the specified tree into the index without moving HEAD at all — it is an index-only operation.',
    codeSnippet: 'git reset HEAD~2 -- src/utils.ts',
    tags: ['git', 'reset', 'path', 'index'],
  },
  {
    id: 'git-reset-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'revert conflict',
    question: 'During `git revert`, a conflict arises. Which sequence properly resolves and finalises it?',
    options: [
      'Fix conflicts → git add → git revert --continue',
      'Fix conflicts → git commit → git revert --finish',
      'git revert --skip → git commit',
      'Fix conflicts → git rebase --continue',
    ],
    correctAnswer: 0,
    explanation: 'After resolving conflicts manually, you stage the resolved files with `git add` and then run `git revert --continue` (or `git commit`) to complete the revert.',
    codeSnippet: '# edit conflicted files\ngit add .\ngit revert --continue',
    tags: ['git', 'revert', 'conflict', 'continue'],
  },
  {
    id: 'git-reset-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'restore internals',
    question: 'What is the default source for `git restore <file>` (without `--source`)?',
    options: [
      'The most recent stash entry',
      'HEAD when no `--staged` flag, or HEAD when `--staged` is given',
      'The index when no `--staged` flag; HEAD when `--staged` is given',
      'Always HEAD regardless of flags',
    ],
    correctAnswer: 2,
    explanation: 'Without `--staged`, `git restore` restores the working tree from the index (staging area). With `--staged`, it restores the index from HEAD. The source differs depending on the target.',
    tags: ['git', 'restore', 'source', 'index', 'HEAD'],
  },
  {
    id: 'git-reset-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'git clean interactive',
    question: 'Which `git clean` flag launches an interactive mode letting you confirm each file?',
    options: ['-i', '-p', '-e', '-q'],
    correctAnswer: 0,
    explanation: '`git clean -i` (interactive) presents a menu where you can inspect and selectively remove untracked files rather than deleting everything at once.',
    codeSnippet: 'git clean -id   # interactive, including dirs',
    tags: ['git', 'clean', 'interactive'],
  },
  {
    id: 'git-reset-l3-008',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reset detached HEAD',
    question: 'You are in detached HEAD state and run `git reset --hard HEAD~2`. What happens?',
    options: [
      'Git refuses to reset in detached HEAD state',
      'HEAD moves back two commits; no branch pointer is updated',
      'A new branch is created at HEAD~2',
      'The nearest branch tip is moved back two commits',
    ],
    correctAnswer: 1,
    explanation: 'In detached HEAD state HEAD points directly to a commit SHA, so `git reset` moves that SHA pointer; no branch is affected because there is no branch to update.',
    tags: ['git', 'reset', 'detached-HEAD'],
  },
  {
    id: 'git-reset-l3-009',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reflog expiry',
    question: 'By default, how long does Git retain reflog entries before they expire?',
    options: ['7 days', '30 days', '90 days', '180 days'],
    correctAnswer: 2,
    explanation: 'The default reflog expiration is 90 days (`gc.reflogExpire=90`); unreachable entries expire after 30 days by default (`gc.reflogExpireUnreachable=30`).',
    codeSnippet: 'git config gc.reflogExpire 90',
    tags: ['git', 'reflog', 'expiry', 'gc'],
  },
  {
    id: 'git-reset-l3-010',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reset --keep',
    question: 'What does the rarely used `git reset --keep` mode do?',
    options: [
      'Keeps staged changes and discards working-directory changes',
      'Resets the index and HEAD but aborts if any file differs between HEAD and the working directory',
      'Preserves the stash while resetting',
      'Works like --mixed but never touches tracked files',
    ],
    correctAnswer: 1,
    explanation: '`--keep` updates HEAD and the index (like `--mixed`) but refuses to overwrite any file in the working tree that has local modifications, reducing the risk of data loss.',
    codeSnippet: 'git reset --keep HEAD~1',
    tags: ['git', 'reset', 'keep'],
  },
  {
    id: 'git-reset-l3-011',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'revert chain',
    question: 'You need to revert commits A, B, C (oldest to newest) and want only one resulting commit. Which approach is correct?',
    options: [
      'git revert A B C',
      'git revert --no-commit C B A && git commit',
      'git revert --no-commit A..C && git commit',
      'git revert A..C --squash',
    ],
    correctAnswer: 1,
    explanation: 'Reverts should be applied newest-first to avoid conflicts; using `--no-commit` on each accumulates changes in the index so a single `git commit` creates one revert commit.',
    codeSnippet: 'git revert --no-commit C B A\ngit commit -m "revert A, B, C"',
    tags: ['git', 'revert', 'no-commit', 'squash'],
  },
  {
    id: 'git-reset-l3-012',
    language: 'git',
    level: 'level3',
    category: 'Reset & Revert',
    subcategory: 'reset --merge',
    question: 'What is the purpose of `git reset --merge`?',
    options: [
      'Resets and merges the target branch simultaneously',
      'Resets HEAD and index but preserves changes that are staged and not in the conflicting path',
      'Aborts an in-progress merge and resets to HEAD',
      'Equivalent to --hard but only for merge commits',
    ],
    correctAnswer: 1,
    explanation: '`--merge` is designed for safely aborting a failed merge: it resets HEAD and the index while keeping local modifications that are not part of the merge conflict untouched.',
    codeSnippet: 'git reset --merge ORIG_HEAD',
    tags: ['git', 'reset', 'merge', 'abort'],
  },
];

export default questions;
