import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-tags-l3-001',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'tag object internals',
    question: 'An annotated tag object in Git contains four header fields. Which set is correct?',
    options: [
      'object, type, tag, tagger',
      'parent, tree, author, tag',
      'object, blob, tagger, message',
      'commit, name, tagger, signature',
    ],
    correctAnswer: 0,
    explanation: 'An annotated tag object stores: `object` (the SHA it points to), `type` (usually "commit"), `tag` (the tag name), and `tagger` (identity + timestamp), followed by a blank line and the message body.',
    codeSnippet: 'git cat-file -p v1.0',
    tags: ['git', 'tag', 'object', 'internals', 'cat-file'],
  },
  {
    id: 'git-tags-l3-002',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'lightweight tag internals',
    question: 'Where is a lightweight tag stored on disk, and what does it contain?',
    options: [
      'In `.git/objects/` as a tag object containing the commit SHA',
      'In `.git/refs/tags/<name>` as a plain text file containing the commit SHA',
      'In `.git/packed-refs` only, never as a loose file',
      'In `.git/info/refs` as a key-value pair',
    ],
    correctAnswer: 1,
    explanation: 'A lightweight tag is simply a file at `.git/refs/tags/<name>` whose contents are the 40-character commit SHA followed by a newline — no tag object is created.',
    codeSnippet: 'cat .git/refs/tags/v1.0-light',
    tags: ['git', 'tag', 'lightweight', 'refs', 'internals'],
  },
  {
    id: 'git-tags-l3-003',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'describe advanced',
    question: 'What does `git describe --always --dirty=-modified` produce on a repo with no tags but uncommitted changes?',
    options: [
      'An error because there are no tags',
      'An empty string',
      'The abbreviated HEAD SHA followed by "-modified"',
      'The full HEAD SHA',
    ],
    correctAnswer: 2,
    explanation: '`--always` falls back to the abbreviated commit hash when no tag is reachable, and `--dirty=<suffix>` appends the suffix when the working tree has uncommitted changes.',
    codeSnippet: 'git describe --always --dirty=-modified\n# e.g. a4b8c2d-modified',
    tags: ['git', 'describe', 'always', 'dirty'],
  },
  {
    id: 'git-tags-l3-004',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'signing tags',
    question: 'Which git config key specifies the GPG key ID used when signing tags with `git tag -s`?',
    options: [
      'user.signingkey',
      'gpg.keyid',
      'tag.gpgsign',
      'core.gpgprogram',
    ],
    correctAnswer: 0,
    explanation: '`user.signingkey` sets the default key fingerprint or email for tag (and commit) signing. `tag.gpgsign = true` auto-signs all annotated tags without requiring `-s`.',
    codeSnippet: 'git config --global user.signingkey ABCDEF01\ngit config --global tag.gpgsign true',
    tags: ['git', 'tag', 'gpg', 'signingkey', 'config'],
  },
  {
    id: 'git-tags-l3-005',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'fetching tags',
    question: 'After cloning, a teammate added tags remotely. Which command fetches only the new tags without fetching other updates?',
    options: [
      'git fetch --tags-only origin',
      'git fetch origin --tags',
      'git pull --tags',
      'git fetch origin refs/tags/*:refs/tags/*',
    ],
    correctAnswer: 1,
    explanation: '`git fetch origin --tags` fetches all tags from the remote (and any commits they reference) without merging anything into your working branch.',
    codeSnippet: 'git fetch origin --tags',
    tags: ['git', 'tag', 'fetch', 'remote'],
  },
  {
    id: 'git-tags-l3-006',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'tag sorting',
    question: 'How do you list tags sorted by the date of the commit they point to (newest first)?',
    options: [
      'git tag -l --sort=-version:refname',
      'git tag --sort=-creatordate',
      'git log --tags --simplify-by-decoration',
      'git tag -l | sort -r',
    ],
    correctAnswer: 1,
    explanation: '`git tag --sort=-creatordate` sorts by the creator date descending; `creatordate` uses the tagger date for annotated tags and the committer date for lightweight tags.',
    codeSnippet: 'git tag --sort=-creatordate',
    tags: ['git', 'tag', 'sort', 'creatordate'],
  },
  {
    id: 'git-tags-l3-007',
    language: 'git',
    level: 'level3',
    category: 'Tags & Releases',
    subcategory: 'moving tags',
    question: 'How do you move an existing tag `v1.0` to point to a different commit?',
    options: [
      'git tag -f v1.0 <new-commit>',
      'git tag --move v1.0 <new-commit>',
      'git tag -d v1.0 && git tag v1.0 <new-commit>',
      'Both A and C work, but A is the single command option',
    ],
    correctAnswer: 3,
    explanation: '`git tag -f <name> <commit>` forcibly overwrites an existing tag. Alternatively, deleting and recreating achieves the same result. If the tag was already pushed, you must also force-push the updated tag.',
    codeSnippet: 'git tag -f v1.0 <new-sha>\ngit push origin -f tag v1.0',
    tags: ['git', 'tag', 'force', 'move'],
  },
];

export default questions;
