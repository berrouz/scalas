import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-tags-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'tag object internals',
    question: 'How does Git store a GPG-signed annotated tag differently from an unsigned annotated tag?',
    options: [
      'Signed tags are stored in a separate `.git/signed-tags/` directory',
      'The PGP signature is appended after the message body in the same tag object, separated by a blank line',
      'Git creates two objects: a tag object and a separate signature object linked by SHA',
      'The signature replaces the message in the tag object',
    ],
    correctAnswer: 1,
    explanation: 'Git appends the ASCII-armored PGP signature directly into the tag object after the message, making the entire object self-contained. `git cat-file -p <tag>` reveals the signature block.',
    codeSnippet: 'git cat-file -p v1.0-signed\n# shows: ...message body...\n# -----BEGIN PGP SIGNATURE-----',
    tags: ['git', 'tag', 'gpg', 'signed', 'internals'],
  },
  {
    id: 'git-tags-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'packed-refs',
    question: 'After `git gc`, tag refs are consolidated into `packed-refs`. What is the format of an entry in that file?',
    options: [
      'tagname → SHA mapping, one per line',
      '<SHA> <refname>, one per line; annotated tags have a peeled line starting with `^`',
      'JSON objects with sha, name, and type fields',
      'Binary records sorted by SHA for O(log n) lookup',
    ],
    correctAnswer: 1,
    explanation: 'Each line in `packed-refs` is `<40-char-sha> <full-refname>`. For annotated tags, a following `^<sha>` line ("peeled ref") records the commit the tag ultimately points to, enabling fast listing without dereferencing tag objects.',
    codeSnippet: 'cat .git/packed-refs\n# abc123 refs/tags/v1.0\n# ^def456',
    tags: ['git', 'packed-refs', 'tag', 'internals', 'gc'],
  },
  {
    id: 'git-tags-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'describe internals',
    question: 'How does `git describe` traverse the commit graph to find the nearest tag?',
    options: [
      'It scans all refs alphabetically and picks the lexicographically smallest tag',
      'It performs a breadth-first traversal from HEAD, stopping at the first reachable annotated tag',
      'It reads the commit messages looking for a tag format pattern',
      'It uses the reflog to find the last time a tag was created',
    ],
    correctAnswer: 1,
    explanation: '`git describe` does a BFS walk of the commit ancestry from HEAD; by default it stops at the first reachable annotated tag, returning the tag name plus the number of additional commits traversed.',
    tags: ['git', 'describe', 'BFS', 'commit-graph', 'internals'],
  },
  {
    id: 'git-tags-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'tag transfer protocol',
    question: 'During `git push --follow-tags`, which tags are pushed in addition to the branch refs?',
    options: [
      'All local tags, identical to `--tags`',
      'Only tags that are reachable from the commits being pushed and are not already on the remote',
      'Only annotated tags created in the last 24 hours',
      'Only the tag most recently created locally',
    ],
    correctAnswer: 1,
    explanation: '`--follow-tags` pushes only annotated tags that point to commits reachable from the refs being pushed and that do not already exist on the remote — a safer alternative to `--tags`.',
    codeSnippet: 'git push origin --follow-tags',
    tags: ['git', 'tag', 'push', 'follow-tags', 'protocol'],
  },
  {
    id: 'git-tags-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'tag signing verification',
    question: 'A CI pipeline needs to verify that a tag is signed by a key in a specific keyring before deployment. Which Git mechanism supports this?',
    options: [
      'git tag -v combined with a custom gpg.program that checks a project keyring',
      'git describe --verify-signature',
      'git push --verify-sig',
      'git config tag.requireSignature',
    ],
    correctAnswer: 0,
    explanation: 'By setting `gpg.program` to a script that uses a project-specific keyring, `git tag -v` (or `git verify-tag`) delegates verification to that script, enabling policy-controlled release gates.',
    codeSnippet: 'git config gpg.program /usr/local/bin/project-gpg\ngit verify-tag v2.0.0',
    tags: ['git', 'tag', 'gpg', 'verify', 'ci', 'security'],
  },
  {
    id: 'git-tags-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'semver sorting',
    question: 'Why does `git tag --sort=version:refname` produce more accurate SemVer ordering than `--sort=refname`?',
    options: [
      'It contacts a remote SemVer registry to order versions',
      'version:refname uses a version-aware comparator that treats numeric parts numerically, avoiding lexicographic problems like v1.10 < v1.9',
      'It reads embedded metadata from annotated tag messages',
      'It is identical to refname sorting for tags using the v prefix',
    ],
    correctAnswer: 1,
    explanation: 'Lexicographic sorting puts "v1.10" before "v1.9" because "1" < "9" in ASCII. The `version:refname` sort key uses GNU version comparison rules that handle integers numerically.',
    codeSnippet: 'git tag --sort=version:refname -l "v*"',
    tags: ['git', 'tag', 'sort', 'version', 'semver'],
  },
  {
    id: 'git-tags-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Tags & Releases',
    subcategory: 'tag and shallow clone',
    question: 'In a shallow clone (`git clone --depth=1`), why might tags for older commits be absent?',
    options: [
      'Shallow clones never transfer any tags',
      'Tags referencing commits outside the shallow boundary are not transferred unless `--tags` is also specified',
      'Tags are stored separately from the commit graph and require `git fetch --tags`',
      'Git automatically converts annotated tags to lightweight in shallow clones',
    ],
    correctAnswer: 1,
    explanation: 'A shallow clone fetches only the specified depth of commits; tags whose tagged commit lies beyond that boundary are not included unless you deepen the clone or explicitly fetch tags with `git fetch --tags`.',
    codeSnippet: 'git clone --depth=1 <url>\ngit fetch --tags --unshallow',
    tags: ['git', 'tag', 'shallow-clone', 'fetch'],
  },
];

export default questions;
