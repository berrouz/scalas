import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-tags-l2-001',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'deleting remote tags',
    question: 'Which command deletes the tag `v1.0-beta` from the remote `origin`?',
    options: [
      'git push origin --delete tag v1.0-beta',
      'git push origin :refs/tags/v1.0-beta',
      'git push origin --remove v1.0-beta',
      'git remote tag -d origin v1.0-beta',
    ],
    correctAnswer: 1,
    explanation: 'Pushing an empty refspec with `:refs/tags/<name>` deletes the ref on the remote. Alternatively, `git push origin --delete v1.0-beta` (Git 1.7+) achieves the same result.',
    codeSnippet: 'git push origin :refs/tags/v1.0-beta\n# or\ngit push origin --delete v1.0-beta',
    tags: ['git', 'tag', 'delete', 'remote'],
  },
  {
    id: 'git-tags-l2-002',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'signing tags',
    question: 'Which flag creates a GPG-signed annotated tag?',
    options: ['-g', '-s', '--gpg', '--sign-off'],
    correctAnswer: 1,
    explanation: '`git tag -s <name>` creates a GPG-signed tag using the key associated with your configured `user.email`. You can verify it later with `git tag -v <name>`.',
    codeSnippet: 'git tag -s v3.0 -m "Signed release 3.0"',
    tags: ['git', 'tag', 'gpg', 'signing'],
  },
  {
    id: 'git-tags-l2-003',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'filtering tags',
    question: 'How do you list only tags matching the pattern `v2.*`?',
    options: [
      'git tag --filter "v2.*"',
      'git tag -l "v2.*"',
      'git tag --grep "v2"',
      'git log --tags="v2.*"',
    ],
    correctAnswer: 1,
    explanation: '`git tag -l <pattern>` (or `git tag --list <pattern>`) uses shell glob matching to filter tag names, showing only those that match the provided pattern.',
    codeSnippet: 'git tag -l "v2.*"',
    tags: ['git', 'tag', 'list', 'filter', 'glob'],
  },
  {
    id: 'git-tags-l2-004',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'git describe',
    question: 'What does `git describe --tags` output?',
    options: [
      'A list of all tags in the repo',
      'The most recent tag, the number of commits since it, and the abbreviated HEAD SHA',
      'A tag object with full metadata',
      'The commit message of the most recent tag',
    ],
    correctAnswer: 1,
    explanation: '`git describe` finds the most recent tag reachable from HEAD and formats output as `<tag>-<n>-g<sha>` where `n` is the number of additional commits and `sha` is the abbreviated commit hash.',
    codeSnippet: 'git describe --tags\n# e.g. v1.4-14-g2414721',
    tags: ['git', 'describe', 'tags', 'version'],
  },
  {
    id: 'git-tags-l2-005',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'pushing specific tag',
    question: 'You want to push only the tag `v1.5.0` without pushing any other refs. Which command achieves this?',
    options: [
      'git push origin --only v1.5.0',
      'git push origin v1.5.0',
      'git push origin tag v1.5.0',
      'git push origin refs/tags/v1.5.0',
    ],
    correctAnswer: 2,
    explanation: 'Using the keyword `tag` before the name (`git push origin tag <name>`) explicitly tells Git to push that tag ref. `git push origin refs/tags/v1.5.0` is the fully-qualified equivalent.',
    codeSnippet: 'git push origin tag v1.5.0',
    tags: ['git', 'tag', 'push', 'specific'],
  },
  {
    id: 'git-tags-l2-006',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'annotated tag metadata',
    question: 'Which command shows the full metadata of an annotated tag including tagger, date, and message?',
    options: [
      'git log v1.0',
      'git show v1.0',
      'git cat-file -p v1.0',
      'git tag -v v1.0',
    ],
    correctAnswer: 1,
    explanation: '`git show <tag>` displays the tag object metadata (for annotated tags) followed by the diff of the tagged commit. `git cat-file -p` also works but is lower-level.',
    codeSnippet: 'git show v1.0',
    tags: ['git', 'tag', 'show', 'metadata'],
  },
  {
    id: 'git-tags-l2-007',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'semver prerelease',
    question: 'According to SemVer, which tag correctly represents a pre-release of version 2.0.0?',
    options: ['v2.0.0-beta.1', 'v2.0.0.beta1', 'v2.0.0_beta', 'v2.0.0rc1'],
    correctAnswer: 0,
    explanation: 'SemVer pre-release identifiers are appended with a hyphen and dot-separated identifiers, e.g. `2.0.0-alpha.1`, `2.0.0-beta.1`, `2.0.0-rc.1`.',
    tags: ['git', 'semver', 'prerelease', 'tags'],
  },
  {
    id: 'git-tags-l2-008',
    language: 'git',
    level: 'level2',
    category: 'Tags & Releases',
    subcategory: 'tag verification',
    question: 'How do you verify the GPG signature of an annotated tag?',
    options: [
      'git tag --check v1.0',
      'git tag -v v1.0',
      'git verify-tag v1.0',
      'Both B and C are correct',
    ],
    correctAnswer: 3,
    explanation: 'Both `git tag -v <name>` and `git verify-tag <name>` validate the GPG signature; the latter is slightly more explicit and is used in scripting.',
    codeSnippet: 'git tag -v v1.0\ngit verify-tag v1.0',
    tags: ['git', 'tag', 'gpg', 'verify'],
  },
];

export default questions;
