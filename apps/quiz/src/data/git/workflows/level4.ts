import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'git-workflows-l4-001',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'merge queue',
    question: 'What problem does a GitHub Merge Queue solve that standard branch protection does not?',
    options: [
      'It enforces commit signing',
      'It prevents two approved PRs from passing CI independently but breaking each other when merged together, by testing them in a serialised queue',
      'It blocks force pushes to main',
      'It auto-assigns reviewers based on file ownership',
    ],
    correctAnswer: 1,
    explanation: 'The merge queue serialises PRs, testing each new head (base + previous queued PRs) before merging, catching integration failures that per-PR CI misses.',
    tags: ['git', 'merge-queue', 'github', 'ci-cd'],
  },
  {
    id: 'git-workflows-l4-002',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'CI/CD integration',
    question: 'A monorepo team only wants to run CI for services that changed in a given commit. What technique enables this?',
    options: [
      'Running all pipelines every time and caching aggressively',
      'Path-based filtering in the CI configuration combined with git diff to detect changed directories',
      'Creating a separate repository per service',
      'Using git submodules for each service',
    ],
    correctAnswer: 1,
    explanation: 'Path filters (e.g., on.push.paths in GitHub Actions) combined with changed-file detection allow selective execution, dramatically reducing CI costs in monorepos.',
    codeSnippet: 'on:\n  push:\n    paths:\n      - \'services/payment/**\'',
    tags: ['git', 'monorepo', 'ci-cd', 'path-filter'],
  },
  {
    id: 'git-workflows-l4-003',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'GitFlow',
    question: 'In GitFlow, what is the correct procedure when a hotfix needs to be applied to an older release branch (e.g., v1.x) that diverged significantly from main?',
    options: [
      'Cherry-pick the hotfix commit from main onto the v1.x release branch',
      'Merge main into v1.x to bring it up to date first',
      'Create a new hotfix branch from v1.x, fix, then merge it into v1.x and back-merge to develop',
      'Apply the fix only to main and let v1.x remain vulnerable',
    ],
    correctAnswer: 2,
    explanation: 'Branching a hotfix from the specific release branch, merging it back to that branch, and also merging to develop mirrors standard GitFlow but scoped to the older release line.',
    codeSnippet: 'git checkout -b hotfix/v1.x-critical v1.x\n# fix ...\ngit checkout v1.x && git merge --no-ff hotfix/v1.x-critical',
    tags: ['git', 'gitflow', 'hotfix', 'release-branch'],
  },
  {
    id: 'git-workflows-l4-004',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'feature flags',
    question: 'What is a "canary release" and how does it relate to feature flags?',
    options: [
      'A release branch named after the canary deployment pattern',
      'Gradually rolling out a new feature to a small percentage of users using a flag, then expanding if metrics look healthy',
      'A CI job that monitors pipeline health',
      'A Git tag format used for pre-release versions',
    ],
    correctAnswer: 1,
    explanation: 'Canary releases use feature flags to expose a change to a small traffic slice first, measuring error rates and performance before a full rollout, limiting blast radius.',
    tags: ['git', 'feature-flags', 'canary', 'deployment'],
  },
  {
    id: 'git-workflows-l4-005',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'merge strategies',
    question: 'A team migrating from squash-merge to rebase-merge finds that git blame output changes significantly. Why?',
    options: [
      'Rebase-merge deletes blame metadata',
      'Squash-merge attributes all lines to one commit; rebase-merge preserves individual commits, so blame points to the smaller, original commits',
      'They produce identical blame output',
      'Rebase-merge re-authors all commits under the merge author',
    ],
    correctAnswer: 1,
    explanation: 'With squash-merge, git blame shows a single squash commit for all touched lines; rebase-merge preserves individual commits so blame points to the specific change that introduced each line.',
    tags: ['git', 'squash-merge', 'rebase', 'blame'],
  },
  {
    id: 'git-workflows-l4-006',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'trunk-based development',
    question: 'What role do "branch by abstraction" and "strangler fig" patterns play in Trunk-Based Development?',
    options: [
      'They are branching strategies equivalent to GitFlow',
      'They are code-level techniques for making large-scale refactors incrementally so they can be committed to trunk continuously without breaking the build',
      'They are Git hooks that prevent direct trunk commits',
      'They are CI pipeline patterns for parallel test execution',
    ],
    correctAnswer: 1,
    explanation: 'Branch by abstraction introduces an indirection layer to switch implementations gradually; strangler fig wraps old code with new incrementally — both keep trunk always green during big changes.',
    tags: ['git', 'trunk-based', 'branch-by-abstraction', 'refactoring'],
  },
  {
    id: 'git-workflows-l4-007',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'CI/CD integration',
    question: 'In a GitOps workflow, what is the single source of truth for the desired state of a production environment?',
    options: [
      'The CI runner\'s last successful build artifact',
      'A Git repository containing declarative configuration; the CD agent reconciles live state with what is in the repo',
      'Environment variables stored in the deployment server',
      'The most recent Docker image in the container registry',
    ],
    correctAnswer: 1,
    explanation: 'GitOps treats the Git repo as the sole source of truth: any drift between the live environment and the repo is automatically corrected by an operator (e.g., Argo CD or Flux).',
    tags: ['git', 'gitops', 'ci-cd', 'declarative'],
  },
  {
    id: 'git-workflows-l4-008',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'code review flow',
    question: 'What is "LGTM drift" and how does a team guard against it in the code review process?',
    options: [
      'A phenomenon where LGTM comments become detached from specific lines after force pushes — guarded by anchoring reviews to commit SHAs',
      'Approvals becoming stale after new commits are pushed — guarded by "Dismiss stale reviews" and re-requiring approval on every push',
      'A bug where CODEOWNERS entries stop matching after a rename — guarded by running codeowners-checker in CI',
      'Reviewers approving too quickly — guarded by mandatory wait periods',
    ],
    correctAnswer: 1,
    explanation: 'LGTM drift occurs when a reviewer approves a diff and then additional commits change the code, making the approval no longer valid for the current state — "Dismiss stale reviews" forces re-approval.',
    tags: ['git', 'code-review', 'lgtm', 'branch-protection'],
  },
  {
    id: 'git-workflows-l4-009',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'release branches',
    question: 'A library team uses semantic versioning and wants to automate changelog generation from commit messages. Which commit message convention enables this most directly?',
    options: [
      'Free-form commit messages with a version in the subject',
      'Conventional Commits (feat:, fix:, chore:, BREAKING CHANGE:) parsed by tools like semantic-release',
      'GitHub squash-merge default messages',
      'Branch names containing the version number',
    ],
    correctAnswer: 1,
    explanation: 'Conventional Commits provides a machine-readable structure that tools like semantic-release use to determine the next SemVer bump and generate changelogs automatically.',
    codeSnippet: 'feat(auth): add OAuth2 PKCE support\nfix(api): handle null response from upstream',
    tags: ['git', 'conventional-commits', 'semver', 'changelog'],
  },
  {
    id: 'git-workflows-l4-010',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'merge queue',
    question: 'When using GitHub\'s merge queue with the "Required merge queue" setting, what happens if a PR author bypasses the queue and force-merges directly?',
    options: [
      'The push succeeds because branch protection only blocks non-admins',
      'The push is rejected unless the actor has admin override rights and the "Allow admin bypass" setting is enabled',
      'The queue is reset and all queued PRs are re-tested',
      'Nothing — the merge queue is advisory, not enforced',
    ],
    correctAnswer: 1,
    explanation: 'With merge queue enforced as a required status, direct merges bypass the serialised integration testing and are blocked by default, preserving the correctness guarantee the queue provides.',
    tags: ['git', 'merge-queue', 'branch-protection', 'github'],
  },
  {
    id: 'git-workflows-l4-011',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'GitLab Flow',
    question: 'In GitLab Flow, why is cherry-picking from main to a production branch considered an anti-pattern?',
    options: [
      'Cherry-pick is slower than merge',
      'It creates divergent histories and makes it hard to reason about what is deployed; the intended flow is always downstream merging',
      'GitLab does not support cherry-pick',
      'Cherry-pick requires force-push permission',
    ],
    correctAnswer: 1,
    explanation: 'Cherry-picking breaks the downstream cascade model: if a commit is only cherry-picked but not merged, it can disappear in the next environment merge, leading to accidental regressions.',
    tags: ['git', 'gitlab-flow', 'cherry-pick', 'anti-pattern'],
  },
  {
    id: 'git-workflows-l4-012',
    language: 'git',
    level: 'level4',
    category: 'Git Workflows',
    subcategory: 'CI/CD integration',
    question: 'What is the purpose of a "deployment lock" mechanism in a CD pipeline?',
    options: [
      'It encrypts the deployment artifacts',
      'It prevents concurrent deployments to the same environment, avoiding race conditions where two pipelines overwrite each other\'s state',
      'It blocks rollbacks after a deployment succeeds',
      'It restricts deploys to specific time windows',
    ],
    correctAnswer: 1,
    explanation: 'Without a deployment lock, two pipelines triggered in quick succession can interleave and leave the environment in an inconsistent state; a lock serialises deployments per environment.',
    tags: ['git', 'ci-cd', 'deployment-lock', 'concurrency'],
  },
];

export default questions;
