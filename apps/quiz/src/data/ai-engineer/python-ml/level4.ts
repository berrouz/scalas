import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ai-engineer-python-ml-l4-001',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced NumPy',
    question: 'What happens when you use np.einsum("ij,jk->ik", A, B) versus np.matmul(A, B) for large matrices on a machine with MKL-backed NumPy?',
    options: [
      'einsum is always faster because it avoids temporary allocations',
      'matmul typically delegates to BLAS gemm routines and is faster; einsum may not use optimized BLAS paths for all contraction patterns',
      'einsum cannot perform matrix multiplication at all',
      'They are always identical in performance because einsum internally calls matmul',
    ],
    correctAnswer: 1,
    explanation: 'np.matmul directly maps to optimized BLAS dgemm/sgemm routines. While einsum can express the same operation, not all einsum contraction patterns are recognized and dispatched to BLAS. For simple matmul patterns einsum may optimize, but for complex contractions it falls back to less optimized loops.',
    tags: ['numpy', 'einsum', 'blas', 'performance'],
  },
  {
    id: 'ai-engineer-python-ml-l4-002',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Memory Management',
    question: 'In CPython, what is the primary risk of using multiprocessing.Pool with large NumPy arrays as function arguments?',
    options: [
      'NumPy arrays cannot be used across processes',
      'Pool workers share memory by default, so there is no overhead',
      'The GIL prevents NumPy from running in subprocesses',
      'Each array is pickled and copied into each worker process, causing massive memory overhead and serialization latency',
    ],
    correctAnswer: 3,
    explanation: 'multiprocessing.Pool uses pickle to serialize arguments to worker processes. Large NumPy arrays are fully copied, leading to significant memory duplication and serialization time. Using shared memory (e.g., multiprocessing.shared_memory or memory-mapped files) avoids this overhead.',
    tags: ['python', 'multiprocessing', 'numpy', 'memory'],
  },
  {
    id: 'ai-engineer-python-ml-l4-003',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Pandas Internals',
    question: 'Why does Pandas 2.0+ default to pyarrow-backed string columns and what is the key performance implication?',
    options: [
      'pyarrow-backed strings use contiguous columnar memory with zero-copy slicing, drastically reducing memory usage and avoiding Python object overhead per string',
      'pyarrow backing makes string operations slower but more memory-safe',
      'pyarrow strings are mutable, allowing in-place modifications',
      'pyarrow strings are only useful for serialization, not computation',
    ],
    correctAnswer: 0,
    explanation: 'Traditional Pandas object-dtype strings store each string as a separate Python object, leading to high memory overhead and poor cache locality. pyarrow-backed strings store data in contiguous Arrow buffers with offsets, enabling zero-copy slicing, reduced memory footprint (~50-90% less), and faster vectorized operations.',
    tags: ['pandas', 'pyarrow', 'strings', 'memory'],
  },
  {
    id: 'ai-engineer-python-ml-l4-004',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Python Concurrency',
    question: 'What is the effect of the Python 3.13 free-threaded build (no-GIL) on existing C extension modules like NumPy?',
    options: [
      'C extensions must be recompiled and potentially modified to be thread-safe since they previously relied on the GIL for implicit synchronization',
      'C extensions are forbidden in the no-GIL build',
      'All C extensions automatically become thread-safe',
      'The GIL removal only affects pure Python code, not C extensions',
    ],
    correctAnswer: 0,
    explanation: 'Many C extension modules relied on the GIL for thread-safety guarantees (e.g., reference counting, accessing shared data structures). The free-threaded build requires extensions to explicitly handle thread safety using atomic operations, per-object locks, or other synchronization. Extensions must opt-in and be rebuilt for this mode.',
    tags: ['python', 'gil', 'concurrency', 'c-extensions'],
  },
  {
    id: 'ai-engineer-python-ml-l4-005',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced NumPy',
    question: 'What is the difference between C-contiguous (row-major) and Fortran-contiguous (column-major) memory layout in NumPy, and when does it critically affect performance?',
    options: [
      'C-contiguous stores rows contiguously; operations iterating along the last axis are cache-friendly. Fortran-contiguous stores columns contiguously. Passing a C-contiguous array to a LAPACK routine expecting Fortran order triggers an implicit copy',
      'The layout only matters for display purposes when printing arrays',
      'Fortran-contiguous is always faster because BLAS was written in Fortran',
      'There is no practical difference since modern CPUs handle both layouts equally',
    ],
    correctAnswer: 0,
    explanation: 'Memory layout determines cache access patterns. C-contiguous (row-major) is efficient for row-wise operations, Fortran-contiguous (column-major) for column-wise. Many LAPACK routines expect Fortran order; passing C-order arrays forces a copy, doubling memory usage and adding latency. np.asfortranarray() can pre-convert to avoid hidden copies.',
    tags: ['numpy', 'memory-layout', 'performance', 'cache'],
  },
  {
    id: 'ai-engineer-python-ml-l4-006',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Python Internals',
    question: 'How does Python\'s __slots__ mechanism affect memory usage and attribute access in ML pipeline classes with millions of instances?',
    options: [
      '__slots__ makes instances immutable, preventing any attribute modification',
      '__slots__ has no effect on memory, only on attribute name checking',
      '__slots__ is only supported in Python 2 and was removed in Python 3',
      '__slots__ eliminates the per-instance __dict__ dictionary, storing attributes in a compact fixed-size struct, reducing memory by 40-60% for small objects and slightly speeding up attribute access',
    ],
    correctAnswer: 3,
    explanation: 'Each normal Python object carries a __dict__ (typically 104+ bytes overhead). __slots__ replaces this with a fixed struct of pointers, saving ~40-60% memory per instance. For ML pipelines creating millions of data point objects (e.g., custom sample classes), this can save gigabytes. Attribute access is also faster since it uses direct offset indexing instead of hash table lookup.',
    tags: ['python', 'slots', 'memory', 'optimization'],
  },
  {
    id: 'ai-engineer-python-ml-l4-007',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Numerical Stability',
    question: 'When computing softmax in pure NumPy, why is subtracting the maximum value from inputs critical, and what subtle issue remains with float16?',
    options: [
      'Subtracting max converts softmax into a linear operation',
      'Subtracting max prevents underflow but causes overflow in all dtypes',
      'Subtracting max prevents overflow in exp(); with float16 (max ~65504), even after subtraction, intermediate exp values can still overflow for inputs with large positive values, requiring float32 accumulation',
      'Subtracting max is only for readability; float16 has no issues',
    ],
    correctAnswer: 2,
    explanation: 'Without max subtraction, exp(x) overflows for large x (e.g., x>709 in float64, x>88 in float32, x>11 in float16). Subtracting max ensures the largest exponent is exp(0)=1. However, float16 has only 5 exponent bits, so even "safe" values can still overflow/underflow. Production softmax implementations (e.g., in Flash Attention) perform accumulation in float32.',
    tags: ['numerical-stability', 'softmax', 'float16', 'numpy'],
  },
  {
    id: 'ai-engineer-python-ml-l4-008',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Python Profiling',
    question: 'What is the key difference between cProfile and py-spy for profiling ML training scripts?',
    options: [
      'py-spy is a sampling profiler that attaches externally without modifying or slowing the target process, while cProfile is a deterministic profiler that instruments every function call and adds 2-5x overhead',
      'cProfile can profile C extensions but py-spy cannot',
      'py-spy only works on Windows; cProfile is cross-platform',
      'cProfile provides line-level profiling while py-spy only profiles at the module level',
    ],
    correctAnswer: 0,
    explanation: 'cProfile instruments every call/return, adding significant overhead that distorts timing of fast functions. py-spy periodically samples the call stack of a running process (even one already started) with minimal overhead (<5%), making it suitable for profiling production ML training. py-spy can also profile through native C extensions and generate flame graphs.',
    tags: ['profiling', 'python', 'py-spy', 'cprofile'],
  },
  {
    id: 'ai-engineer-python-ml-l4-009',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced Pandas',
    question: 'Why can chained indexing (df["a"]["b"] = val) silently fail in Pandas, and what is the correct alternative?',
    options: [
      'Chained indexing may operate on a copy instead of the original DataFrame due to how __getitem__ returns views vs copies, making the assignment a no-op. Using df.loc[row, col] ensures single-step indexing on the original',
      'The issue only occurs with integer indices, not with labels',
      'Chained indexing is slower but always works correctly',
      'Chained indexing always raises an error in modern Pandas',
    ],
    correctAnswer: 0,
    explanation: 'In Pandas, df["a"] may return a view or a copy depending on the dtype and operation. If it returns a copy, subsequent assignment df["a"]["b"] = val modifies the copy, not the original DataFrame. This triggers SettingWithCopyWarning. Using .loc/.iloc performs the operation in a single step on the original DataFrame, guaranteeing correctness.',
    tags: ['pandas', 'chained-indexing', 'settingwithcopy', 'best-practices'],
  },
  {
    id: 'ai-engineer-python-ml-l4-010',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Async Python',
    question: 'When building an async ML inference server with asyncio, what happens if a CPU-bound preprocessing step is called directly in an async handler?',
    options: [
      'The CPU-bound work blocks the event loop, preventing all other coroutines (including health checks and other requests) from making progress until it completes',
      'The operation runs in a subprocess automatically',
      'asyncio raises an exception for CPU-bound operations',
      'asyncio automatically offloads it to a thread pool',
    ],
    correctAnswer: 0,
    explanation: 'asyncio uses cooperative multitasking on a single thread. A CPU-bound call (e.g., image resizing, tokenization) blocks the event loop, starving all other coroutines. Solutions include: asyncio.to_thread() or loop.run_in_executor() to offload to a thread/process pool, or using a dedicated worker process for preprocessing.',
    tags: ['asyncio', 'event-loop', 'cpu-bound', 'inference'],
  },
  {
    id: 'ai-engineer-python-ml-l4-011',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'NumPy Broadcasting',
    question: 'Given A with shape (3, 1, 5) and B with shape (4, 1), what is the result shape of A + B and why?',
    options: [
      '(3, 1, 5) — B is ignored because it has fewer dimensions',
      '(3, 4, 5) — broadcasting aligns trailing dimensions and expands size-1 dims to match the other array along each axis',
      'Error — shapes are incompatible for broadcasting',
      '(4, 1, 5) — the first dimension of A is dropped',
    ],
    correctAnswer: 1,
    explanation: 'Broadcasting rules: (1) prepend 1s to the shorter shape → B becomes (1, 4, 1), (2) align: (3,1,5) vs (1,4,1), (3) for each axis, if dimensions differ one must be 1, and it expands to match. Result: (3,4,5). This is a common pattern for computing pairwise operations efficiently.',
    tags: ['numpy', 'broadcasting', 'shapes', 'vectorization'],
  },
  {
    id: 'ai-engineer-python-ml-l4-012',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Memory-Mapped Files',
    question: 'How does np.memmap enable training on datasets larger than available RAM, and what is its main limitation?',
    options: [
      'memmap compresses data to fit in RAM',
      'memmap requires the entire dataset to fit in swap space',
      'memmap stores data in GPU memory instead of RAM',
      'memmap creates a memory-mapped file that lazily loads pages from disk on access via the OS virtual memory system, but random access patterns cause excessive page faults and thrashing, making sequential access essential',
    ],
    correctAnswer: 3,
    explanation: 'np.memmap maps a file into virtual address space. The OS loads pages on demand and evicts them under memory pressure. This allows working with files much larger than RAM with zero upfront load time. However, random access patterns cause frequent page faults (each ~microseconds), making it critical to access data sequentially or in locality-preserving order.',
    tags: ['numpy', 'memmap', 'out-of-core', 'virtual-memory'],
  },
  {
    id: 'ai-engineer-python-ml-l4-013',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Type System',
    question: 'How do Protocol classes (structural subtyping) from typing module benefit ML codebases compared to abstract base classes?',
    options: [
      'Protocols are faster at runtime than ABCs',
      'Protocols are only used for documentation, not actual type checking',
      'Protocols enable type checking against an interface without requiring inheritance, allowing third-party classes (e.g., sklearn estimators) to satisfy a protocol without modification',
      'Protocols replace all need for isinstance checks',
    ],
    correctAnswer: 2,
    explanation: 'typing.Protocol defines structural types: any class with matching method signatures satisfies the protocol without inheriting from it. This is invaluable in ML where you might define a Predictor protocol with .predict() and .fit() methods — sklearn, XGBoost, and custom models all satisfy it without knowing about the protocol. ABCs require explicit inheritance.',
    tags: ['python', 'typing', 'protocol', 'structural-subtyping'],
  },
  {
    id: 'ai-engineer-python-ml-l4-014',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Serialization',
    question: 'Why is pickle considered dangerous for loading ML models from untrusted sources, and what is a safer alternative?',
    options: [
      'Pickle only works with scikit-learn models, not deep learning',
      'Pickle executes arbitrary Python code during deserialization via __reduce__, enabling remote code execution. SafeTensors stores only tensor data without executable code, making it safe to load from any source',
      'Pickle files are too large for production use',
      'Pickle is safe but slow; SafeTensors is faster but less safe',
    ],
    correctAnswer: 1,
    explanation: 'pickle.loads() invokes __reduce__ methods that can execute arbitrary code, including os.system() calls. This makes loading pickled models from untrusted sources equivalent to running arbitrary code. SafeTensors (by Hugging Face) stores tensors in a simple header+data format with no code execution, providing safe, fast, zero-copy loading.',
    tags: ['pickle', 'security', 'safetensors', 'serialization'],
  },
  {
    id: 'ai-engineer-python-ml-l4-015',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced NumPy',
    question: 'What is the purpose of np.errstate and when should it be used in ML code?',
    options: [
      'It fixes numerical errors by automatically switching to higher precision',
      'It is deprecated and replaced by np.seterr in all cases',
      'It catches and logs all NumPy errors to a file',
      'It is a context manager that controls how floating-point errors (division by zero, overflow, underflow, invalid operations) are handled, allowing you to suppress or raise warnings in specific code blocks like log-probability computations',
    ],
    correctAnswer: 3,
    explanation: 'np.errstate is a context manager for locally controlling floating-point error handling. In ML, operations like np.log(0) (in cross-entropy) or division by zero (in normalization) are sometimes expected. np.errstate(divide="ignore", invalid="ignore") suppresses warnings locally without affecting global state, unlike np.seterr which changes behavior globally.',
    tags: ['numpy', 'errstate', 'floating-point', 'error-handling'],
  },
  {
    id: 'ai-engineer-python-ml-l4-016',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Generator Patterns',
    question: 'What is the advantage of using itertools.islice with a generator over list slicing when processing large datasets?',
    options: [
      'islice works only with files, not generators',
      'islice returns a list which is faster to iterate',
      'islice lazily consumes only the needed elements from the generator without materializing the entire sequence in memory, maintaining O(1) memory usage regardless of dataset size',
      'There is no advantage; both approaches use the same memory',
    ],
    correctAnswer: 2,
    explanation: 'Converting a generator to a list to slice it defeats the purpose of lazy evaluation, loading the entire dataset into memory. itertools.islice consumes only the requested elements, discarding earlier ones and never loading the full sequence. For a 100GB dataset processed line-by-line, islice(gen, 1000, 2000) uses constant memory.',
    tags: ['python', 'generators', 'itertools', 'memory-efficiency'],
  },
  {
    id: 'ai-engineer-python-ml-l4-017',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Cython & Numba',
    question: 'When should you use Numba @jit(nopython=True) versus Cython for accelerating ML preprocessing code?',
    options: [
      'Numba excels at JIT-compiling NumPy-heavy loops with zero code changes but has limited support for complex Python objects; Cython requires type annotations and a build step but can wrap C/C++ libraries and handle arbitrary Python types',
      'They are identical in capability and performance',
      'Cython is only for Python 2; Numba is for Python 3',
      'Numba is always better because it requires no compilation step',
    ],
    correctAnswer: 0,
    explanation: 'Numba uses LLVM to JIT-compile Python functions with NumPy operations, requiring minimal code changes but only supporting a subset of Python (numeric types, arrays, basic containers). Cython compiles Python-like code to C with explicit type declarations, offering broader compatibility including wrapping external C libraries, but requires a build step and .pyx files.',
    tags: ['numba', 'cython', 'performance', 'jit'],
  },
  {
    id: 'ai-engineer-python-ml-l4-018',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Data Formats',
    question: 'Why is Parquet preferred over CSV for ML feature stores, and what specific property makes it efficient for column selection?',
    options: [
      'Parquet uses columnar storage with per-column chunks and metadata offsets, allowing reading only the needed columns without scanning the entire file; it also supports predicate pushdown, efficient encoding per column type, and column statistics for query planning',
      'Parquet is preferred only because it supports schemas, unlike CSV',
      'Parquet files are always smaller because they use lossy compression',
      'Parquet is faster because it stores data as plain text with indexes',
    ],
    correctAnswer: 0,
    explanation: 'Parquet stores data column-by-column in row groups. Footer metadata records each column chunk\'s offset and statistics (min/max). Reading 3 columns out of 1000 requires reading only those 3 column chunks. Type-specific encodings (dictionary, run-length, delta) achieve 5-10x compression over CSV. Predicate pushdown skips entire row groups whose statistics exclude the filter range.',
    tags: ['parquet', 'columnar-storage', 'feature-store', 'data-formats'],
  },
  {
    id: 'ai-engineer-python-ml-l4-019',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Python Packaging',
    question: 'What problem does the pyproject.toml build system specification solve for ML packages that include compiled extensions (e.g., CUDA kernels)?',
    options: [
      'It replaces pip entirely with a faster package manager',
      'It eliminates the need for CUDA toolkit installation',
      'It declaratively specifies build-time dependencies and the build backend (e.g., scikit-build-core, meson-python), ensuring the correct compilers and libraries are installed before building, unlike setup.py which required them pre-installed',
      'It only handles pure Python packages, not compiled extensions',
    ],
    correctAnswer: 2,
    explanation: 'PEP 517/518 pyproject.toml allows specifying build-system.requires (e.g., cmake, ninja, scikit-build-core) and the build backend. pip installs these in an isolated environment before building. For ML packages with CUDA/C++ extensions, this ensures the build toolchain is present. setup.py required users to manually install compilers and libraries beforehand.',
    tags: ['python', 'packaging', 'pyproject-toml', 'build-system'],
  },
  {
    id: 'ai-engineer-python-ml-l4-020',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced Pandas',
    question: 'What is the performance impact of using .apply() with a Python function on a Pandas DataFrame versus vectorized operations?',
    options: [
      'There is no performance difference; .apply() is syntactic sugar for vectorized ops',
      '.apply() is faster for small DataFrames but slower for large ones',
      '.apply() with a Python function runs a Python-level loop over each row/element, typically 10-100x slower than equivalent vectorized NumPy/Pandas operations due to function call overhead and inability to use SIMD instructions',
      '.apply() is optimized by Pandas to run at C speed',
    ],
    correctAnswer: 2,
    explanation: 'Vectorized operations in Pandas/NumPy execute pre-compiled C loops with SIMD instructions over contiguous memory. .apply() calls a Python function per row/element, incurring Python function call overhead, type checking, and boxing/unboxing per element. For 1M rows, vectorized df["a"] + df["b"] might take 1ms while df.apply(lambda r: r["a"] + r["b"], axis=1) takes 500ms.',
    tags: ['pandas', 'apply', 'vectorization', 'performance'],
  },
  {
    id: 'ai-engineer-python-ml-l4-021',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Debugging',
    question: 'How can you use sys.settrace or sys.monitoring (Python 3.12+) to build a custom profiler for tracking tensor shapes through an ML pipeline?',
    options: [
      'sys.monitoring replaces all debuggers and profilers in Python 3.12',
      'sys.settrace cannot inspect function arguments or return values',
      'These APIs can only trace pure Python code, not NumPy operations',
      'sys.settrace registers a callback for call/return/line events that can inspect frame locals to log tensor shapes; sys.monitoring (3.12+) provides lower-overhead event hooks with selective activation per code object, reducing overhead from ~100x to ~5x compared to settrace',
    ],
    correctAnswer: 3,
    explanation: 'sys.settrace installs a per-thread trace function called on every call, return, line, and exception event. It can inspect frame.f_locals to see tensor shapes, but the per-event overhead is substantial. Python 3.12\'s sys.monitoring allows registering callbacks for specific events on specific code objects, with the runtime only paying overhead where monitoring is active.',
    tags: ['python', 'debugging', 'sys-monitoring', 'profiling'],
  },
  {
    id: 'ai-engineer-python-ml-l4-022',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Distributed Computing',
    question: 'What is the key limitation of Python\'s multiprocessing.Queue for passing large tensors between data loading workers and a training process?',
    options: [
      'Queue serializes objects through pickle and copies data through pipes/sockets, making it inefficient for large tensors. torch.utils.data.DataLoader uses shared memory (file-backed mmap) to pass tensors between workers with near-zero copy overhead',
      'Queue cannot be used with GPU tensors at all',
      'Queue is limited to 100 items regardless of memory',
      'Queue only supports string messages',
    ],
    correctAnswer: 0,
    explanation: 'multiprocessing.Queue pickles objects and sends bytes through OS pipes. For a 224x224x3 float32 image batch (~600KB), this means serialize + memcpy + deserialize per batch. PyTorch DataLoader instead places tensors in shared memory segments (file_system or file_descriptor strategy), allowing workers and the main process to access the same physical memory pages.',
    tags: ['multiprocessing', 'shared-memory', 'dataloader', 'performance'],
  },
  {
    id: 'ai-engineer-python-ml-l4-023',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced NumPy',
    question: 'What does np.shares_memory(a, b) check and why is it critical for avoiding bugs in feature engineering pipelines?',
    options: [
      'It verifies that two arrays are stored in the same file',
      'It checks if two arrays have the same dtype',
      'It determines whether two arrays reference overlapping memory regions, which matters because modifying one would silently modify the other — a common source of data leakage bugs when slicing DataFrames into train/test splits',
      'It checks if two arrays are equal in value',
    ],
    correctAnswer: 2,
    explanation: 'NumPy slicing creates views sharing the same memory buffer. If train_X = X[:1000] and test_X = X[1000:], they share the underlying buffer. Normalizing train_X in-place (train_X /= std) could corrupt test_X if the memory layout overlaps. np.shares_memory() detects this, and explicit .copy() prevents it.',
    tags: ['numpy', 'memory', 'views', 'data-leakage'],
  },
  {
    id: 'ai-engineer-python-ml-l4-024',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Python Metaprogramming',
    question: 'How can __init_subclass__ be used to build a self-registering model registry pattern in ML frameworks?',
    options: [
      '__init_subclass__ only works with dataclasses',
      '__init_subclass__ was removed in Python 3.10 in favor of metaclasses',
      '__init_subclass__ is called when a class is subclassed, allowing the parent class to automatically register each new model subclass in a dictionary without requiring decorators or explicit registration calls',
      '__init_subclass__ is called when an instance is created, not when a class is defined',
    ],
    correctAnswer: 2,
    explanation: 'Defining class BaseModel with __init_subclass__ that appends cls to a registry dict means every class MyModel(BaseModel) automatically registers itself. This is cleaner than metaclasses or decorators for building plugin-style model registries (e.g., how timm, MMDetection handle model registration). The method receives the subclass as its first argument.',
    tags: ['python', 'metaprogramming', 'init-subclass', 'design-patterns'],
  },
  {
    id: 'ai-engineer-python-ml-l4-025',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'NumPy Advanced Indexing',
    question: 'What is the critical difference between basic and advanced (fancy) indexing in NumPy regarding views vs copies?',
    options: [
      'Both basic and advanced indexing always return copies',
      'The distinction only matters for 1D arrays',
      'Basic indexing (slices, single integers) returns a view sharing memory; advanced indexing (integer arrays, boolean masks) always returns a copy, meaning modifications to the result do not affect the original array',
      'Advanced indexing returns views; basic indexing returns copies',
    ],
    correctAnswer: 2,
    explanation: 'Basic indexing (a[2:5], a[3]) returns views because the result can be described by offset+strides. Advanced indexing (a[[1,3,5]], a[mask]) gathers arbitrary elements that may not be contiguous, requiring a new array (copy). This matters for in-place operations: a[::2] *= 2 modifies a, but modifying a[[0,2,4]] does not modify a.',
    tags: ['numpy', 'indexing', 'views', 'copies'],
  },
  {
    id: 'ai-engineer-python-ml-l4-026',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Environment Management',
    question: 'What problem does uv solve compared to pip + venv for ML projects with complex dependency trees?',
    options: [
      'uv is identical to pip but with a different CLI syntax',
      'uv is a Rust-based pip replacement that resolves dependencies 10-100x faster using a SAT solver with caching and parallel downloads, and manages virtual environments, making it practical to resolve complex ML dependency trees (PyTorch + JAX + TensorFlow) that pip struggles with',
      'uv only supports Rust packages, not Python',
      'uv eliminates the need for virtual environments entirely',
    ],
    correctAnswer: 1,
    explanation: 'ML projects often have deeply nested, conflicting dependency trees (e.g., PyTorch needing specific CUDA versions, multiple packages pinning different NumPy ranges). pip\'s resolver can take minutes or fail. uv uses a PubGrub-based SAT solver implemented in Rust with aggressive caching and parallel metadata fetching, resolving in seconds what pip takes minutes to solve.',
    tags: ['python', 'uv', 'dependency-management', 'packaging'],
  },
  {
    id: 'ai-engineer-python-ml-l4-027',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Decorators',
    question: 'How does functools.lru_cache interact with unhashable arguments (e.g., NumPy arrays), and what is the workaround for caching ML feature computations?',
    options: [
      'lru_cache automatically converts arrays to hashable tuples',
      'lru_cache raises TypeError for unhashable arguments like NumPy arrays. Workarounds include: using array.tobytes() as a hashable key in a custom cache, joblib.Memory for disk-based caching with content hashing, or converting arguments to hashable representations manually',
      'NumPy arrays are hashable by default since NumPy 1.20',
      'lru_cache works with all Python objects regardless of hashability',
    ],
    correctAnswer: 1,
    explanation: 'lru_cache uses arguments as dictionary keys, requiring hashability. NumPy arrays are mutable and unhashable. For caching expensive feature computations, joblib.Memory provides disk-based caching that content-hashes array data. Alternatively, wrapping arrays with their .tobytes() hash as a key enables custom memoization.',
    tags: ['python', 'lru-cache', 'caching', 'hashability'],
  },
  {
    id: 'ai-engineer-python-ml-l4-028',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Context Managers',
    question: 'What is the purpose of torch.inference_mode() versus torch.no_grad(), and when should each be used?',
    options: [
      'inference_mode() only works during model evaluation, not during data preprocessing',
      'They are identical; inference_mode is just an alias',
      'inference_mode() disables both gradient computation AND version counting/autograd graph metadata, making it faster and more memory-efficient than no_grad() which only disables gradient computation but still tracks tensor metadata',
      'no_grad() is faster because it disables more internal tracking',
    ],
    correctAnswer: 2,
    explanation: 'torch.no_grad() disables gradient recording but tensors still have requires_grad tracking, version counters (for in-place op safety), and autograd metadata. torch.inference_mode() additionally disables these, producing InferenceTensors that skip all autograd overhead. This gives ~5-10% speedup and lower memory. However, InferenceTensors cannot be used in any autograd computation later.',
    tags: ['pytorch', 'inference-mode', 'no-grad', 'optimization'],
  },
  {
    id: 'ai-engineer-python-ml-l4-029',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Testing',
    question: 'What is property-based testing (e.g., Hypothesis library) and why is it valuable for testing ML data preprocessing functions?',
    options: [
      'Property-based testing tests class properties and attributes',
      'Property-based testing is slower and less thorough than unit testing',
      'Property-based testing only works for mathematical functions, not data processing',
      'Property-based testing generates random inputs satisfying specified constraints and verifies that output invariants always hold, finding edge cases (NaN, empty arrays, extreme values) that manual test cases miss in preprocessing functions',
    ],
    correctAnswer: 3,
    explanation: 'Hypothesis generates diverse inputs (including edge cases like empty sequences, NaN values, extreme floats, Unicode strings) and checks that specified properties hold. For an ML normalizer, you might assert output always has zero mean and unit variance, that it handles empty input gracefully, and that it\'s idempotent. Hypothesis finds the minimal failing example automatically.',
    tags: ['testing', 'hypothesis', 'property-based', 'preprocessing'],
  },
  {
    id: 'ai-engineer-python-ml-l4-030',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Data Validation',
    question: 'How does Pydantic v2\'s Rust-based validation engine benefit ML API input validation compared to manual checks?',
    options: [
      'Pydantic v2 only validates JSON, not Python objects',
      'Pydantic validation adds too much overhead for real-time ML inference',
      'Pydantic v2 is slower than v1 but has better error messages',
      'Pydantic v2 uses a Rust core (pydantic-core) for 5-50x faster validation than v1, with TypeAdapter enabling validation of complex nested structures like batched inference requests with automatic coercion, detailed error messages, and JSON Schema generation for API documentation',
    ],
    correctAnswer: 3,
    explanation: 'Pydantic v2 rewrote validation in Rust, achieving 5-50x speedup over v1. For ML APIs, this means validating batched requests (e.g., 100 images with metadata) adds microseconds, not milliseconds. TypeAdapter validates arbitrary types without model classes. Automatic JSON Schema generation enables OpenAPI docs for inference endpoints.',
    tags: ['pydantic', 'validation', 'api', 'rust'],
  },
  {
    id: 'ai-engineer-python-ml-l4-031',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Advanced Python',
    question: 'What is the purpose of __array_ufunc__ and __array_function__ protocols in NumPy, and how do ML libraries like JAX and CuPy use them?',
    options: [
      'They are internal APIs not meant for external use',
      'They allow non-NumPy array types to intercept and override NumPy function calls (e.g., np.sum(cupy_array) dispatches to CuPy\'s implementation), enabling seamless interoperability where NumPy functions work on GPU arrays or traced arrays without code changes',
      'They force all arrays to be converted to NumPy before operations',
      'They are used to define custom mathematical functions in NumPy',
    ],
    correctAnswer: 1,
    explanation: '__array_ufunc__ intercepts ufunc calls (np.add, np.sin) and __array_function__ intercepts higher-level functions (np.concatenate, np.stack). When np.sum(cupy_arr) is called, CuPy\'s __array_function__ implementation handles the dispatch to GPU code. This protocol enables libraries like JAX, CuPy, Dask to work with existing NumPy-based code transparently.',
    tags: ['numpy', 'array-protocol', 'dispatch', 'interoperability'],
  },
  {
    id: 'ai-engineer-python-ml-l4-032',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Structured Arrays',
    question: 'When is using NumPy structured arrays with custom dtypes preferable to a Pandas DataFrame for ML pipelines?',
    options: [
      'Structured arrays provide contiguous, fixed-layout memory for heterogeneous records without Python object overhead, making them ideal for memory-mapped binary datasets, zero-copy C/C++ interop, and scenarios where DataFrame column flexibility and label-based indexing are unnecessary',
      'Structured arrays are deprecated in favor of Pandas',
      'Structured arrays are always better than DataFrames',
      'Structured arrays cannot store mixed types like int and float together',
    ],
    correctAnswer: 0,
    explanation: 'A structured array with dtype [(\"feature1\", \"f4\"), (\"label\", \"i4\")] stores records in contiguous fixed-size blocks, enabling mmap, direct C struct mapping, and cache-friendly iteration. DataFrames store each column separately with label indexes and Python overhead. For binary ML dataset files (millions of fixed-format records), structured arrays are significantly more efficient.',
    tags: ['numpy', 'structured-arrays', 'dtypes', 'memory-layout'],
  },
  {
    id: 'ai-engineer-python-ml-l4-033',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Python & ML Fundamentals',
    subcategory: 'Polars',
    question: 'What architectural advantage does Polars have over Pandas for large-scale ML feature engineering?',
    options: [
      'Polars only supports numerical data, not strings or categories',
      'Polars is a wrapper around Pandas that adds multithreading',
      'Polars is faster only for DataFrames with fewer than 1000 rows',
      'Polars uses Apache Arrow columnar memory, a lazy query engine with automatic query optimization (predicate pushdown, projection pruning, join reordering), and Rust-based parallelized execution, avoiding the GIL and enabling multi-threaded operations on all CPU cores',
    ],
    correctAnswer: 3,
    explanation: 'Polars is built from scratch in Rust on Apache Arrow. Its lazy API builds a query plan that is optimized before execution: unnecessary columns are pruned, filters pushed down, and operations parallelized across cores without GIL contention. For feature engineering on 100M+ rows, this can be 5-20x faster than Pandas while using less memory due to Arrow\'s efficient columnar format.',
    tags: ['polars', 'arrow', 'feature-engineering', 'performance'],
  },
];

export default questions;
