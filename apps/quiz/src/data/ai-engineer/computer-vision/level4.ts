import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ai-engineer-computer-vision-l4-001',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Vision Transformers',
    question: 'What is the DINOv2 self-supervised training approach and why do its features transfer so well across vision tasks?',
    options: [
      'DINOv2 uses supervised ImageNet pre-training with label smoothing',
      'DINOv2 requires labeled data for pre-training',
      'DINOv2 combines self-distillation (student-teacher with EMA), masked image modeling, and large-scale curated data. The self-distillation loss on [CLS] tokens learns global features while patch-level MIM learns local features. Training on 142M curated images produces features that match or exceed task-specific supervised models on depth estimation, segmentation, and classification without any fine-tuning',
      'DINOv2 only works for image classification tasks',
    ],
    correctAnswer: 2,
    explanation: 'DINOv2 (Oquab et al. 2023): student sees masked/augmented crops, teacher (EMA of student) sees full/different crops. Loss combines: (1) cross-entropy between student and teacher [CLS] tokens (global self-distillation), (2) masked patch prediction (local MIM). Data curation pipeline: web-crawled images filtered by embedding similarity to curated dataset. Key insight: the combination of self-distillation (which learns good global representations) and MIM (which learns spatial understanding) produces universal visual features.',
    tags: ['dinov2', 'self-supervised', 'self-distillation', 'mim'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-002',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Object Detection',
    question: 'How does DETR (Detection Transformer) eliminate the need for NMS and anchor boxes?',
    options: [
      'DETR uses traditional anchor boxes with a transformer backbone',
      'DETR applies NMS in the transformer attention layers',
      'DETR can only detect objects from a fixed set of 100 categories',
      'DETR uses a set of learned object queries that attend to the image features via cross-attention, producing a fixed set of predictions. Bipartite matching (Hungarian algorithm) uniquely assigns each prediction to a ground truth object during training, with unmatched queries predicting "no object." This end-to-end approach removes the need for hand-designed NMS, anchors, and IoU thresholds',
    ],
    correctAnswer: 3,
    explanation: 'DETR (Carion et al. 2020): (1) CNN backbone extracts features, (2) transformer encoder processes features globally, (3) N learned object queries (e.g., 100) cross-attend to encoder features, (4) each query outputs a class + bounding box prediction, (5) Hungarian matching finds optimal 1-to-1 assignment between predictions and GT objects, minimizing assignment cost. Duplicates are avoided because each GT has exactly one matched prediction. No NMS, no anchors, no IoU thresholds. Limitations: slow convergence (500 epochs vs 12 for Faster R-CNN), addressed by Deformable DETR.',
    tags: ['detr', 'object-detection', 'hungarian-matching', 'end-to-end'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-003',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Segmentation',
    question: 'What is the Segment Anything Model (SAM) architecture and how does it achieve zero-shot segmentation?',
    options: [
      'SAM can only segment objects it was explicitly trained on',
      'SAM requires fine-tuning for each new domain',
      'SAM consists of a heavy image encoder (MAE-pretrained ViT) that runs once per image, a prompt encoder that handles points/boxes/text, and a lightweight mask decoder that produces segmentation masks. Trained on SA-1B (1 billion masks), it generalizes to unseen objects and domains zero-shot because the prompt-based interface decouples what to segment from how to segment',
      'SAM is a traditional U-Net trained on ImageNet',
    ],
    correctAnswer: 2,
    explanation: 'SAM (Kirillov et al. 2023): (1) Image encoder: ViT-H processes the image once → image embeddings, (2) Prompt encoder: converts points, boxes, or masks into prompt embeddings, (3) Mask decoder: lightweight transformer that cross-attends prompt tokens to image embeddings → mask predictions + IoU scores. The SA-1B dataset was created in a data engine loop: SAM assists human annotators → annotations train a better SAM → repeat. This produces a foundation model for segmentation that generalizes to medical images, satellite imagery, etc. without domain-specific training.',
    tags: ['sam', 'segmentation', 'zero-shot', 'foundation-model'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-004',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Diffusion Models',
    question: 'What is latent diffusion (Stable Diffusion) and why is it more efficient than pixel-space diffusion?',
    options: [
      'Latent diffusion requires more GPU memory than pixel-space diffusion',
      'Latent diffusion first compresses images to a lower-dimensional latent space using a pretrained VAE encoder, performs the diffusion process (noising and denoising) in this compact latent space, then decodes back to pixel space. Operating on 64x64 latents instead of 512x512 pixels reduces compute by ~50x while preserving perceptual quality, because the VAE captures imperceptible details that diffusion need not model',
      'Latent diffusion produces lower quality images than pixel-space methods',
      'Latent diffusion operates on full-resolution images',
    ],
    correctAnswer: 1,
    explanation: 'Rombach et al. (2022): a pretrained autoencoder (KL-regularized) compresses 512×512×3 images to 64×64×4 latents (~48x spatial reduction). The diffusion U-Net operates entirely in this latent space. Benefits: (1) 48x fewer pixels to denoise per step, (2) latent space focuses on semantic content (high-frequency imperceptible details handled by the decoder), (3) cross-attention conditions on text embeddings. The VAE is trained once and frozen. This architecture enabled Stable Diffusion to run on consumer GPUs (8GB VRAM).',
    tags: ['latent-diffusion', 'stable-diffusion', 'vae', 'efficiency'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-005',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: '3D Vision',
    question: 'What is NeRF (Neural Radiance Fields) and what is its key computational limitation?',
    options: [
      'NeRF uses point clouds for rendering and is real-time',
      'NeRF is a mesh-based 3D reconstruction method',
      'NeRF can only represent indoor scenes',
      'NeRF represents a scene as a continuous volumetric function F(x,y,z,θ,φ) → (RGB, density) using an MLP. Novel views are rendered by casting rays, sampling points along each ray, querying the MLP at each point, and compositing colors using volume rendering. The limitation: each pixel requires ~100+ MLP forward passes (one per sample point), making rendering slow (~30 seconds per frame). 3D Gaussian Splatting addresses this with explicit Gaussian primitives',
    ],
    correctAnswer: 3,
    explanation: 'NeRF (Mildenhall et al. 2020): encode position with Fourier features → MLP → (color, density). Volume rendering: C(r) = Σ T_i · α_i · c_i where T_i = Π(1-α_j) is transmittance and α_i = 1-exp(-σ_i·δ_i). Requiring ~192 samples per ray × ~640K rays per image = 123M MLP evaluations per frame. Instant-NGP uses hash-grid encoding for 100x speedup. 3D Gaussian Splatting replaces the MLP with millions of explicit 3D Gaussians that are rasterized via differentiable splatting, achieving real-time rendering (>100 FPS).',
    tags: ['nerf', '3d-gaussian-splatting', 'novel-view-synthesis', 'volume-rendering'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-006',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Data Augmentation',
    question: 'What is CutMix and why does it outperform basic augmentations like random erasing for training robust classifiers?',
    options: [
      'CutMix produces worse results than standard random cropping',
      'CutMix randomly erases patches and fills them with zeros',
      'CutMix replaces a rectangular patch of one training image with a patch from another image, and mixes the labels proportionally to the patch area. Unlike random erasing (which adds no information) or Mixup (which creates blurry blends), CutMix retains full pixel information from both images, forcing the model to attend to partial objects and learn more spatially distributed features rather than relying on a single discriminative region',
      'CutMix is only effective for small images (<64x64)',
    ],
    correctAnswer: 2,
    explanation: 'CutMix (Yun et al. 2019): sample λ from Beta(α,α), select random rectangle with area ratio λ, paste image B\'s patch onto image A, label = λ·y_A + (1-λ)·y_B. Benefits: (1) no uninformative pixels (unlike Cutout/random erasing which create zero/noise regions), (2) forces model to recognize objects from partial views (localization regularization), (3) combined with the label mixing, prevents overconfident predictions. Improves top-1 accuracy by 1-2% on ImageNet and significantly improves calibration and OOD detection.',
    tags: ['cutmix', 'data-augmentation', 'regularization', 'training'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-007',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Efficient Networks',
    question: 'What is the neural architecture search (NAS) strategy behind EfficientNet and how does compound scaling work?',
    options: [
      'Compound scaling requires different NAS searches for each scale',
      'EfficientNet uses a manually designed architecture like ResNet',
      'EfficientNet uses NAS to find a base architecture (B0), then applies compound scaling that uniformly scales depth, width, and resolution using fixed ratios (d=α^φ, w=β^φ, r=γ^φ) subject to the constraint α·β²·γ² ≈ 2. This balances all dimensions simultaneously, outperforming models that scale only one dimension, achieving better accuracy per FLOP',
      'EfficientNet scales only the depth of the network',
    ],
    correctAnswer: 2,
    explanation: 'Tan & Le (2019): observation — scaling depth, width, and resolution independently hits diminishing returns. Compound scaling: given a FLOP budget increase of 2^φ, scale all three: depth×α^φ, width×β^φ, resolution×γ^φ where α=1.2, β=1.1, γ=1.15 and α·β²·γ² ≈ 2 (approximately doubles FLOPs). This is because increasing resolution needs more layers (depth) to process larger receptive fields, which need wider layers to capture more patterns. EfficientNet-B7 achieved 84.3% ImageNet top-1 with 66M params vs ResNet-152\'s 78.3% with 60M.',
    tags: ['efficientnet', 'nas', 'compound-scaling', 'efficient-networks'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-008',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Feature Matching',
    question: 'What is LoFTR and how does it handle feature matching differently from traditional detect-then-describe methods?',
    options: [
      'LoFTR can only match features between identical images',
      'LoFTR is slower than SIFT and provides no accuracy improvement',
      'LoFTR uses a detector-free approach: it extracts dense feature maps from both images using a CNN backbone, then uses a transformer with cross-attention to establish correspondences at coarse resolution before refining to sub-pixel accuracy. This enables matching in low-texture and repetitive regions where keypoint detectors fail, since the transformer can reason about global context to resolve ambiguities',
      'LoFTR uses SIFT features with a transformer matcher',
    ],
    correctAnswer: 2,
    explanation: 'Traditional: detect keypoints (SIFT/SuperPoint) → describe each → match descriptions → verify with RANSAC. Failure: low-texture regions have no keypoints. LoFTR (Sun et al. 2021): (1) CNN extracts features at 1/8 and 1/2 resolution for both images, (2) transformer with self-attention (intra-image) and cross-attention (inter-image) establishes coarse matches at 1/8 resolution, (3) fine-level module refines to sub-pixel accuracy. Cross-attention is key: each feature in image A attends to all features in image B, enabling matching in textureless regions via global context. Used in SfM, SLAM, and visual localization.',
    tags: ['loftr', 'feature-matching', 'detector-free', 'cross-attention'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-009',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Video Understanding',
    question: 'What is the key challenge of applying transformers to video and how does TimeSformer address it?',
    options: [
      'Video transformers cannot capture motion information',
      'TimeSformer processes only 2 frames at a time',
      'Transformers process video frames independently with no temporal modeling',
      'Full spatiotemporal attention over all tokens from all frames has O((T·H·W)²) complexity, which is prohibitive. TimeSformer decomposes attention into separate temporal and spatial components: each patch first attends to the same spatial location across all frames (temporal attention), then attends to all spatial locations within the same frame (spatial attention). This reduces complexity from O((THW)²) to O(T²·HW + TH²W²)',
    ],
    correctAnswer: 3,
    explanation: 'For 8 frames of 224×224 with 16×16 patches: 8×196 = 1568 tokens. Full attention: 1568² ≈ 2.5M. TimeSformer (Bertasius et al. 2021) factorizes: (1) Temporal attention: each patch attends to the same position in other frames (8×8 per patch × 196 patches), (2) Spatial attention: each frame\'s 196 patches attend to each other (196² × 8 frames). Total: ~310K. ViViT further explores factorizations. More recent: video MAE pre-training with high masking ratio (90%) learns strong spatiotemporal features efficiently.',
    tags: ['timesformer', 'video-understanding', 'factorized-attention', 'spatiotemporal'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-010',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Open Vocabulary Detection',
    question: 'How does Grounding DINO enable open-vocabulary object detection with text prompts?',
    options: [
      'Grounding DINO fuses image and text features through cross-modality attention in both encoder and decoder. Text tokens attend to image features and vice versa, enabling the model to detect any object described by text. It combines a DINO-like detection head with grounded pre-training on detection, grounding, and caption datasets, achieving strong zero-shot detection for novel categories described in natural language',
      'Grounding DINO requires separate models for each object category',
      'Grounding DINO can only detect objects with single-word descriptions',
      'Grounding DINO is limited to detecting objects in a fixed set of COCO categories',
    ],
    correctAnswer: 0,
    explanation: 'Grounding DINO (Liu et al. 2023): (1) Image encoder (Swin Transformer) + Text encoder (BERT), (2) Feature enhancer with cross-modality fusion (image tokens attend to text, text tokens attend to image), (3) Language-guided query selection initializes object queries from text-relevant image features, (4) Cross-modality decoder refines detections with both image and text awareness. Input: image + text prompt "cat. dog. person." → detects and labels each instance. Combined with SAM: Grounded-SAM detects then segments any described object zero-shot.',
    tags: ['grounding-dino', 'open-vocabulary', 'detection', 'cross-modality'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-011',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Depth Estimation',
    question: 'What is the difference between monocular depth estimation and stereo depth estimation, and how do modern foundation models handle depth?',
    options: [
      'Foundation depth models can only process images at 224x224 resolution',
      'Stereo depth uses triangulation from two calibrated cameras (geometric, accurate). Monocular depth predicts depth from a single image using learned monocular cues (texture gradients, object size, occlusion). Foundation models like Depth Anything and MiDaS produce highly accurate relative depth from single images by training on massive diverse datasets, though they predict relative rather than metric depth without calibration',
      'Monocular depth estimation is always more accurate than stereo',
      'Monocular and stereo depth estimation produce identical results',
    ],
    correctAnswer: 1,
    explanation: 'Stereo: disparity d = baseline × focal_length / depth. Geometric, gives metric depth, but requires calibrated camera pair. Monocular: ML model predicts depth from single image using learned priors. DPT/MiDaS and Depth Anything use ViT-based encoders trained on millions of images with diverse depth annotations (from stereo, LiDAR, SfM). They produce excellent relative depth maps (up/down ordering) but need scale-shift alignment for metric depth. Depth Anything V2 uses DINOv2 features for robust generalization. Metric3D adds camera intrinsics to predict metric depth.',
    tags: ['depth-estimation', 'monocular', 'depth-anything', 'foundation-model'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-012',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Image Generation',
    question: 'What is the difference between DDPM, DDIM, and DPM-Solver sampling schedules for diffusion models?',
    options: [
      'DDIM requires more steps than DDPM for equivalent quality',
      'DPM-Solver can only be used with pixel-space diffusion models',
      'All sampling schedules produce identical results in the same number of steps',
      'DDPM uses stochastic Markov chain sampling requiring ~1000 steps. DDIM reformulates the reverse process as a deterministic ODE, enabling fewer steps (~50) with consistent outputs. DPM-Solver applies higher-order ODE solvers (predictor-corrector) to reduce steps further to 10-25 with quality matching 1000-step DDPM, by exploiting the semi-linear structure of the diffusion ODE',
    ],
    correctAnswer: 3,
    explanation: 'DDPM: x_{t-1} = μ(x_t, t) + σ(t)·z, each step adds noise → stochastic, needs many steps for quality. DDIM (Song et al. 2020): derives a non-Markovian process with deterministic sampling (σ=0) → ODE: dx = f(x,t)dt. Since it\'s an ODE, larger step sizes are possible. DPM-Solver (Lu et al. 2022): exploits the diffusion ODE\'s structure to derive 2nd/3rd order solvers that compute more accurate steps, achieving high quality in 10-20 steps. Further: distilled models (LCM, SDXL Turbo) achieve 1-4 step generation by distilling the multi-step process.',
    tags: ['ddpm', 'ddim', 'dpm-solver', 'sampling-schedule'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-013',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Optical Flow',
    question: 'What is RAFT (Recurrent All-Pairs Field Transforms) and how does it compute optical flow?',
    options: [
      'RAFT uses traditional Lucas-Kanade optical flow',
      'RAFT requires ground truth flow for initialization',
      'RAFT can only estimate flow between consecutive video frames',
      'RAFT builds a 4D correlation volume from all pairs of pixels between two frames, then iteratively refines the flow field using a GRU-based update operator that looks up the correlation volume at current flow-predicted positions. This recurrent refinement with shared weights enables progressive convergence from coarse to fine flow estimates without requiring a coarse-to-fine image pyramid',
    ],
    correctAnswer: 3,
    explanation: 'RAFT (Teed & Deng 2020): (1) Extract features F1, F2 from both frames, (2) Build 4D correlation volume C(i,j,k,l) = F1(i,j)·F2(k,l) for all pixel pairs, (3) Initialize flow = 0, (4) Iteratively: look up correlation at current flow-warped positions → feed correlation + context + current flow into GRU → GRU outputs flow update Δf. 12-32 iterations converge to accurate flow. The correlation volume provides a global matching signal, and the GRU refinement handles occlusions and fine details. State-of-the-art on Sintel and KITTI benchmarks.',
    tags: ['raft', 'optical-flow', 'correlation-volume', 'recurrent-refinement'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-014',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Contrastive Learning',
    question: 'What is the difference between CLIP\'s contrastive pre-training and SigLIP\'s sigmoid-based alternative?',
    options: [
      'CLIP uses softmax-based contrastive loss over the full batch: each image-text pair is a positive, all other pairs are negatives. This requires global communication for the softmax normalization. SigLIP replaces this with independent sigmoid losses per pair (binary: is this pair matched?), eliminating the need for global batch statistics and enabling more efficient distributed training while maintaining equivalent zero-shot performance',
      'SigLIP requires a much larger batch size than CLIP',
      'CLIP works only with English text while SigLIP is multilingual',
      'CLIP and SigLIP use identical training objectives',
    ],
    correctAnswer: 0,
    explanation: 'CLIP: InfoNCE loss requires computing softmax over the entire batch → all-gather of embeddings across GPUs for global normalization. For batch size 32K across 1024 GPUs, this is expensive. SigLIP (Zhai et al. 2023): for each (image_i, text_j) pair, apply sigmoid: loss = -log σ(y_ij · (sim(i,j) - b)) where y_ij = +1 if matched, -1 if not, b is a bias. No global normalization needed → more efficient distributed training. SigLIP also uses a chunked approach to sample negatives within and across devices efficiently. Performance matches CLIP at equivalent scale.',
    tags: ['clip', 'siglip', 'contrastive-learning', 'vision-language'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-015',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Image Restoration',
    question: 'What are the key architectural choices in modern image super-resolution networks like SwinIR?',
    options: [
      'Super-resolution networks cannot handle arbitrary upscaling factors',
      'SwinIR requires separate models for each type of image degradation',
      'Modern SR networks use simple CNN stacking like SRCNN',
      'SwinIR uses Swin Transformer blocks (shifted window attention) to capture both local texture patterns and long-range structural dependencies. Key innovations: residual connections around transformer groups for stable training, sub-pixel convolution (PixelShuffle) for efficient upsampling instead of transposed convolution (which causes checkerboard artifacts), and channel attention for feature recalibration',
    ],
    correctAnswer: 3,
    explanation: 'SwinIR (Liang et al. 2021): (1) shallow feature extraction (Conv), (2) deep feature extraction (Residual Swin Transformer Blocks with shifted window attention for global context), (3) reconstruction (sub-pixel convolution for upsampling). Key: window attention limits compute to O(N·w²) while shifted windows enable cross-window information flow. PixelShuffle: Conv(in, out×r², k) → rearrange → upsampled image, avoiding deconvolution artifacts. Pre-training on large diverse datasets (ImageNet) then fine-tuning on SR data improves generalization. HAT extends this with hybrid attention combining channel and window attention.',
    tags: ['swinir', 'super-resolution', 'swin-transformer', 'pixelshuffle'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-016',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Pose Estimation',
    question: 'What is the difference between top-down and bottom-up approaches for multi-person pose estimation?',
    options: [
      'Both approaches detect keypoints in the same order',
      'Top-down: first detect all persons (object detector), then run single-person pose estimation on each crop. Scales O(N) with number of people but handles occlusion well since each person is processed independently. Bottom-up: detect all keypoints in the image simultaneously, then group them into persons using association techniques. Fixed compute regardless of person count, but grouping keypoints correctly is challenging with heavy occlusion',
      'Bottom-up approaches cannot handle more than 5 people',
      'Top-down approaches are always faster than bottom-up',
    ],
    correctAnswer: 1,
    explanation: 'Top-down (HRNet + detector): detect people → crop → estimate 17 keypoints per crop. Pros: high per-person accuracy, well-studied. Cons: runtime ∝ N_people, detector failures cascade. Bottom-up (OpenPose, HigherHRNet): predict all keypoints + part affinity fields (PAFs) → bipartite matching to group keypoints into people. Pros: fixed compute, real-time for many people. Cons: small/occluded people have weak keypoint detections, grouping errors. Modern: PETR combines both via set prediction (DETR-like), predicting all persons\' poses in one pass.',
    tags: ['pose-estimation', 'top-down', 'bottom-up', 'multi-person'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-017',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Medical Imaging',
    question: 'What are the unique challenges of applying deep learning to medical images and how do models like MedSAM address them?',
    options: [
      'Medical imaging has the same challenges as natural image recognition',
      'Medical imaging challenges: extremely limited labeled data (expert annotation required), 3D volumetric data (CT, MRI), class imbalance (pathologies are rare), distribution shift across scanners/protocols, and strict regulatory requirements. MedSAM adapts SAM to medical images by fine-tuning on 1.5M medical image-mask pairs across 10+ modalities, enabling interactive segmentation of tumors, organs, and anomalies with point/box prompts',
      'Deep learning cannot be applied to medical images due to data limitations',
      'Standard ImageNet-pretrained models work perfectly for medical imaging',
    ],
    correctAnswer: 1,
    explanation: 'Medical imaging specifics: (1) 3D volumes: CT scans are 512×512×N_slices, requiring 3D or slice-by-slice processing, (2) Domain gap: X-rays, MRI, ultrasound have very different characteristics from natural images, (3) Annotation cost: a single CT segmentation takes a radiologist 30+ minutes, (4) Tiny pathologies: lung nodules can be 3mm in a 512×512 image. MedSAM (Ma et al. 2023) fine-tunes SAM on diverse medical data, demonstrating that foundation model adaptation outperforms training from scratch on limited medical data. Still requires prompt (physician provides point/box).',
    tags: ['medical-imaging', 'medsam', 'segmentation', 'domain-adaptation'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-018',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Domain Adaptation',
    question: 'What is the domain adversarial neural network (DANN) approach for unsupervised domain adaptation in vision?',
    options: [
      'DANN trains separate models for source and target domains',
      'DANN can only adapt between visually similar domains',
      'DANN requires labeled data from both domains',
      'DANN adds a domain classifier branch to the feature extractor and trains it adversarially: the feature extractor learns domain-invariant features by maximizing domain classification loss (via a gradient reversal layer) while minimizing task loss on labeled source data. This aligns source and target feature distributions so that a classifier trained on source features generalizes to the target domain without target labels',
    ],
    correctAnswer: 3,
    explanation: 'DANN (Ganin et al. 2016): feature extractor G → two heads: (1) task classifier C minimizes task loss on source labels, (2) domain classifier D classifies source vs target. The gradient reversal layer (GRL) multiplies the gradient from D by -λ before passing to G, so G learns features that maximize domain confusion (fool D) while minimizing task loss. At convergence: features are domain-invariant and task-discriminative. Theory: Ben-David bound shows target error ≤ source error + domain divergence + adaptability constant.',
    tags: ['dann', 'domain-adaptation', 'gradient-reversal', 'unsupervised'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-019',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Image Editing',
    question: 'What is the InstructPix2Pix approach and how does it enable text-based image editing without per-image optimization?',
    options: [
      'InstructPix2Pix requires optimizing the model for each edit',
      'InstructPix2Pix trains a conditional diffusion model on (input image, edit instruction, edited image) triplets generated synthetically using GPT-3 (for instruction generation) and Stable Diffusion (for paired image generation). At inference, it takes an image and a text instruction and directly generates the edited image in a single forward pass without any per-image fine-tuning or inversion',
      'InstructPix2Pix can only apply style transfers, not structural edits',
      'InstructPix2Pix requires the edited image as input during inference',
    ],
    correctAnswer: 1,
    explanation: 'Brooks et al. (2023): training data pipeline: (1) GPT-3 generates (input caption, edit instruction, output caption) triplets, (2) Prompt-to-Prompt generates paired images from input/output captions maintaining structural similarity. The diffusion model conditions on both the input image (via additional channels) and the edit instruction (via cross-attention). Two guidance scales control edit strength and image fidelity. This enables "make it winter," "add glasses," "make them smile" without per-image optimization (unlike DreamBooth or textual inversion).',
    tags: ['instructpix2pix', 'image-editing', 'instruction-following', 'diffusion'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-020',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Tracking',
    question: 'What is the key innovation of ByteTrack and how does it improve multi-object tracking?',
    options: [
      'ByteTrack requires a separate re-identification model',
      'ByteTrack tracks every detected bounding box (every "byte"), not just high-confidence ones. It first associates high-confidence detections with tracks using IoU matching, then associates remaining low-confidence detections with unmatched tracks. This recovers occluded or partially visible objects that are detected with low confidence, significantly reducing ID switches and fragmentation',
      'ByteTrack only tracks objects in the first frame',
      'ByteTrack uses appearance features for re-identification',
    ],
    correctAnswer: 1,
    explanation: 'Traditional MOT (SORT, DeepSORT) filters detections by confidence threshold, losing occluded objects. ByteTrack (Zhang et al. 2022): (1) First association: match high-confidence detections to existing tracks using Kalman filter-predicted IoU, (2) Second association: match remaining low-confidence detections to unmatched tracks from step 1 using IoU. This two-stage association recovers objects that are temporarily occluded or partially visible (low confidence but correct location). No appearance model needed — pure motion-based, yet achieves state-of-the-art on MOT17/MOT20 benchmarks.',
    tags: ['bytetrack', 'mot', 'tracking', 'occlusion-handling'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-021',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'OCR',
    question: 'What is the TrOCR architecture and how does it differ from traditional OCR pipelines?',
    options: [
      'TrOCR requires character-level bounding box annotations for training',
      'TrOCR uses an end-to-end encoder-decoder transformer: a pretrained image transformer (ViT/DeiT) encodes the text image, and a pretrained language model (RoBERTa/GPT-2 initialized) decoder generates text autoregressively. This eliminates the traditional multi-stage pipeline (binarization → segmentation → character recognition → language model post-processing) and leverages language model pretraining for implicit spell-checking',
      'TrOCR uses the same multi-stage pipeline as Tesseract',
      'TrOCR can only process printed text, not handwriting',
    ],
    correctAnswer: 1,
    explanation: 'Traditional OCR: preprocess → text detection → text recognition → language model. Each stage introduces errors that cascade. TrOCR (Li et al. 2023): image encoder pretrained on ImageNet (visual features) + decoder initialized from language model (implicit language prior). End-to-end training on text recognition datasets. Benefits: (1) decoder\'s language model knowledge helps correct visual ambiguities, (2) no separate text detection needed for single-line recognition, (3) handles diverse fonts, languages, and degraded text. Combined with text detection models for document-level OCR.',
    tags: ['trocr', 'ocr', 'encoder-decoder', 'text-recognition'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-022',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Panoptic Segmentation',
    question: 'What is panoptic segmentation and how does Mask2Former unify instance and semantic segmentation?',
    options: [
      'Panoptic segmentation only segments "stuff" classes like sky and road',
      'Panoptic segmentation assigns every pixel both a semantic class and an instance ID (combining semantic segmentation for "stuff" and instance segmentation for "things"). Mask2Former uses a universal architecture: a set of learnable queries (like DETR) produce class predictions and mask embeddings, which are dot-producted with pixel-level features to generate masks. The same architecture handles semantic, instance, and panoptic segmentation by changing only the loss function',
      'Mask2Former can only segment objects from COCO categories',
      'Panoptic segmentation requires separate models for things and stuff',
    ],
    correctAnswer: 1,
    explanation: 'Panoptic = semantic (every pixel gets a class) + instance (each "thing" object gets a unique ID). Mask2Former (Cheng et al. 2022): (1) backbone + pixel decoder → multi-scale features, (2) N learnable queries + transformer decoder with masked cross-attention → N (class, mask_embedding) predictions, (3) mask = sigmoid(mask_embedding · pixel_features). Loss: Hungarian matching between predictions and GT masks + cross-entropy + dice loss. The masked cross-attention restricts each query to attend only to its predicted mask region, improving convergence. Universal: same architecture, different loss for different tasks.',
    tags: ['panoptic-segmentation', 'mask2former', 'universal-segmentation', 'instance'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-023',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Robustness',
    question: 'What is adversarial robustness in vision models and why does adversarial training improve it at the cost of clean accuracy?',
    options: [
      'Adversarial training always improves both clean and robust accuracy',
      'Adversarial examples cannot fool deep learning models',
      'Only CNNs are vulnerable to adversarial examples; ViTs are immune',
      'Adversarial examples are imperceptibly perturbed images that cause misclassification (e.g., adding ε-bounded noise flips "panda" to "gibbon"). Adversarial training minimizes the worst-case loss over ε-perturbations: min_θ max_{δ:||δ||≤ε} L(f_θ(x+δ), y). This improves robustness but reduces clean accuracy by 5-15% because the model must learn smoother decision boundaries that sacrifice discriminability in exchange for stability',
    ],
    correctAnswer: 3,
    explanation: 'Madry et al. (2018): PGD adversarial training solves a min-max optimization. The inner max finds the strongest attack within ε-ball using projected gradient descent. The outer min trains on these worst-case examples. The accuracy trade-off is fundamental: Tsipras et al. (2019) showed robustness and accuracy may be at odds — robust models learn coarse, shape-based features (more aligned with human perception) while sacrificing fine texture features that boost clean accuracy. For ε=8/255 on CIFAR-10, clean accuracy drops from ~95% to ~85%.',
    tags: ['adversarial-robustness', 'pgd-training', 'robustness-accuracy', 'perturbation'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-024',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'GAN Applications',
    question: 'What is the StyleGAN architecture innovation and how does its style-based generator enable controllable image synthesis?',
    options: [
      'StyleGAN uses a standard progressive growing generator',
      'StyleGAN can only generate faces, not other objects',
      'StyleGAN maps random noise through a mapping network to an intermediate latent space W, then injects these style vectors via AdaIN (Adaptive Instance Normalization) at each generator layer. Different layers control different attributes: early layers = pose/shape, middle layers = features, late layers = colors/textures. This disentangled control enables precise attribute editing by modifying style vectors at specific layers',
      'StyleGAN requires paired training data for each attribute',
    ],
    correctAnswer: 2,
    explanation: 'StyleGAN (Karras et al. 2019): z ∈ Z → mapping network (8 FC layers) → w ∈ W. The generator starts from a learned constant, and at each resolution block, w is transformed via affine to produce style parameters (scale, shift) for AdaIN. This "style injection" at each level provides hierarchical control. The W space is much more disentangled than Z: linear interpolation in W produces smooth, meaningful attribute transitions. StyleGAN2 removed AdaIN artifacts with weight demodulation. StyleGAN3 added alias-free operations for equivariance.',
    tags: ['stylegan', 'adain', 'style-injection', 'controllable-generation'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-025',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Self-Supervised',
    question: 'What is Masked Autoencoder (MAE) and why does it require a high masking ratio (75%) for ViTs?',
    options: [
      'MAE masks only 10% of patches like BERT masks 15% of tokens',
      'MAE randomly masks 75% of image patches, encodes only the visible 25% with a ViT encoder, then reconstructs all patches (including masked) with a lightweight decoder. The high masking ratio is needed because images have higher spatial redundancy than text — with low masking, the model can interpolate from nearby patches without learning semantic representations. 75% masking forces learning higher-level features',
      'MAE uses the same masking ratio as BERT (15%)',
      'Higher masking ratios always produce worse representations',
    ],
    correctAnswer: 1,
    explanation: 'MAE (He et al. 2022): key insight — images are more redundant than text. A missing word in a sentence is hard to guess, but a missing patch in an image can be easily interpolated from neighbors. 75% masking creates a challenging pretext task: the model must understand object structure and scene semantics to reconstruct. Practical benefit: encoding only 25% of patches makes pre-training 3x faster and uses 3x less memory. The asymmetric design (heavy encoder on visible, light decoder for reconstruction) further improves efficiency. Fine-tuned MAE matches supervised ViT-Large on ImageNet.',
    tags: ['mae', 'masked-autoencoder', 'self-supervised', 'masking-ratio'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-026',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Point Clouds',
    question: 'How does PointNet process 3D point clouds and what is the key invariance it must maintain?',
    options: [
      'PointNet processes each point independently through shared MLPs, then aggregates using a symmetric function (max pooling) to achieve permutation invariance — the output must be the same regardless of the order of input points. A spatial transformer network provides transformation invariance. This avoids the information loss of voxelization and handles variable-sized point sets directly',
      'PointNet voxelizes the point cloud into a 3D grid first',
      'PointNet uses convolution operations on point neighborhoods',
      'PointNet requires a fixed number of points in each input',
    ],
    correctAnswer: 0,
    explanation: 'Point clouds are unordered sets: {p₁, p₂, ..., pₙ} where pᵢ ∈ R³. The network must be invariant to permutation: f(p₁,...,pₙ) = f(pπ(1),...,pπ(n)). PointNet (Qi et al. 2017): (1) shared MLP per point: h(pᵢ) → feature vector, (2) max pooling across all points → global feature (symmetric function guarantees permutation invariance), (3) classify/segment from global feature. T-Net aligns input points and features for transformation invariance. PointNet++: hierarchical grouping + PointNet on local neighborhoods for local feature learning.',
    tags: ['pointnet', 'point-cloud', 'permutation-invariance', '3d-deep-learning'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-027',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Image Retrieval',
    question: 'What is the difference between global and local feature-based image retrieval, and what is re-ranking?',
    options: [
      'Re-ranking always degrades retrieval precision',
      'Local feature retrieval is always faster than global retrieval',
      'Global and local retrieval produce identical results',
      'Global retrieval: encode the entire image into a single vector and use ANN search (fast, handles appearance changes poorly). Local retrieval: extract and match individual local features/patches for geometric verification (slower, handles viewpoint/occlusion better). Two-stage systems use global retrieval to shortlist candidates, then re-rank using local feature matching or cross-attention models for higher precision',
    ],
    correctAnswer: 3,
    explanation: 'Global: image → GeM pooling → single 2048-d vector → L2/cosine NN search. Fast (milliseconds for millions) but sensitive to viewpoint, occlusion. Local: extract SuperPoint/DISK features → match with LightGlue/LoFTR → count inlier matches after RANSAC geometric verification. Accurate but expensive per pair. Production pipeline: (1) global retrieval → top-100 candidates (fast), (2) re-rank with cross-encoder or local matching → top-10 (accurate). This balances speed and precision. Modern: token-level ViT features enable efficient local matching without separate feature extraction.',
    tags: ['image-retrieval', 'global-features', 'local-features', 'reranking'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-028',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Semantic Understanding',
    question: 'What is CLIP\'s zero-shot classification approach and what are its failure modes?',
    options: [
      'CLIP requires fine-tuning on each classification task',
      'CLIP computes similarity between an image embedding and text embeddings of class descriptions ("a photo of a {class}"). The class with highest similarity wins. Failure modes: (1) distribution shift from web-crawled training data, (2) inability to count objects or understand spatial relationships, (3) sensitivity to prompt template wording, (4) poor fine-grained recognition (bird species, car models) without domain-specific training',
      'CLIP has no failure modes and works perfectly on all vision tasks',
      'Zero-shot CLIP always outperforms supervised models',
    ],
    correctAnswer: 1,
    explanation: 'CLIP zero-shot: encode all class prompts → encode image → cosine similarity → argmax. Remarkable generalization from 400M web image-text pairs. Failure modes: (1) abstract concepts (counting: "3 cats" vs "5 cats"), (2) spatial reasoning ("cat above dog"), (3) prompt sensitivity: "a photo of a {class}" vs "a {class}" can change results by 5%+, (4) systematic biases from web data (gender, racial), (5) adversarial vulnerability (typographic attacks: pasting "iPod" text on an apple makes CLIP classify it as iPod). Prompt ensembling ("a photo of a...", "a painting of a...") helps mitigate template sensitivity.',
    tags: ['clip', 'zero-shot', 'classification', 'failure-modes'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-029',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Autonomous Driving',
    question: 'What is BEV (Bird\'s Eye View) perception and why has it become the dominant paradigm in autonomous driving vision?',
    options: [
      'BEV perception requires exactly 6 cameras positioned symmetrically',
      'BEV transforms multi-camera perspective images into a unified top-down representation using learned depth estimation and geometric projection. This unified representation simplifies downstream tasks (planning, prediction) by operating in a single 3D coordinate frame. BEVFormer uses spatiotemporal transformers with deformable cross-attention from BEV queries to multi-camera features, achieving 3D object detection without LiDAR',
      'BEV perception only uses top-down LiDAR data',
      'BEV perception cannot detect objects beyond 50 meters',
    ],
    correctAnswer: 1,
    explanation: 'Multi-camera 3D perception challenge: each camera has a different viewpoint and FoV. BEV (Li et al. 2022, BEVFormer): (1) define a grid of BEV queries in the ground plane, (2) each BEV query samples features from relevant camera images using deformable attention + known camera intrinsics/extrinsics, (3) temporal self-attention aligns BEV features across frames for velocity estimation. Benefits: all objects in a unified metric space, natural for path planning, fuses information from all cameras. Tesla\'s approach similarly constructs BEV features from 8 cameras for their vision-only autonomous driving.',
    tags: ['bev', 'autonomous-driving', 'multi-camera', '3d-detection'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-030',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Generative Models',
    question: 'What is ControlNet and how does it add spatial conditioning to pretrained diffusion models?',
    options: [
      'ControlNet creates a trainable copy of the encoder blocks of a pretrained diffusion model, connected via zero-initialized convolutions. The conditioning input (edges, depth maps, poses) is processed by this copy, and the outputs are added to the original model\'s features. Zero-initialization ensures the model starts from the pretrained state and gradually learns conditioning, preserving generation quality while adding precise spatial control',
      'ControlNet modifies the original model weights during training',
      'ControlNet can only use edge detection maps as conditioning',
      'ControlNet retrains the diffusion model from scratch with conditioning',
    ],
    correctAnswer: 0,
    explanation: 'ControlNet (Zhang et al. 2023): (1) Lock the pretrained Stable Diffusion weights, (2) Create a trainable copy of the encoder blocks, (3) Connect copy outputs to the locked decoder via "zero convolutions" (1×1 convs initialized to zero weights and biases). At initialization, the zero convolutions output zeros → the locked model behaves exactly as before. During training, the zero convolutions gradually learn non-zero weights, allowing the conditioning signal to influence generation. This preserves pretrained quality while adding controls like Canny edges, depth, OpenPose, segmentation maps.',
    tags: ['controlnet', 'conditional-generation', 'diffusion', 'spatial-conditioning'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-031',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Anomaly Detection',
    question: 'What is PatchCore for visual anomaly detection and why is it effective with limited normal training data?',
    options: [
      'PatchCore extracts patch-level features from a pretrained network (e.g., WideResNet) on normal training images and stores representative patches in a coreset memory bank. At test time, each test patch is compared to its nearest neighbor in the bank; patches with high distances are anomalous. This requires no training on anomaly examples, works with as few as a few dozen normal images, and achieves state-of-the-art on MVTec AD benchmark',
      'PatchCore can only detect anomalies in industrial manufacturing images',
      'PatchCore requires examples of every type of anomaly for training',
      'PatchCore trains a classifier to distinguish normal from anomalous images',
    ],
    correctAnswer: 0,
    explanation: 'PatchCore (Roth et al. 2022): (1) extract mid-level features (layers 2-3 of ImageNet-pretrained WideResNet) for all patches of normal training images, (2) coreset subsampling (greedy furthest-point) reduces memory bank to ~1-10% of patches while preserving coverage, (3) at test time: compute max nearest-neighbor distance across all patches → anomaly score. Why it works: pretrained features capture semantic and structural patterns; anomalies produce features far from the normal distribution. No anomaly examples needed (unsupervised). Achieves ~99% AUROC on MVTec AD.',
    tags: ['patchcore', 'anomaly-detection', 'coreset', 'unsupervised'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-032',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Knowledge Distillation',
    question: 'What is feature-based distillation for vision models and how does it differ from logit-based distillation?',
    options: [
      'Logit-based distillation transfers knowledge through the teacher\'s output probabilities (soft labels). Feature-based distillation additionally aligns intermediate feature maps between teacher and student using MSE or attention transfer losses. This transfers structural knowledge about how features are organized spatially, which is especially important for dense prediction tasks (detection, segmentation) where per-pixel features matter more than global logits',
      'Logit-based distillation always outperforms feature-based methods',
      'Feature-based distillation only works for classification tasks',
      'Feature-based and logit-based distillation are identical',
    ],
    correctAnswer: 0,
    explanation: 'Logit distillation: KL(teacher_logits/T || student_logits/T). Only transfers final prediction distribution. Feature distillation: align intermediate representations — L_feat = ||A(f_student) - f_teacher||² where A is an adaptor (handles dimension mismatch). Attention transfer (Zagoruyko 2017): align attention maps ||A_s - A_t||². FitNets: align specific hidden layers. For detection/segmentation: intermediate spatial features encode object structure that logits collapse. CWD (Channel-wise Distillation): normalize features per channel for better transfer. Feature + logit distillation combined typically outperforms either alone.',
    tags: ['feature-distillation', 'knowledge-distillation', 'attention-transfer', 'dense-prediction'],
  },
  {
    id: 'ai-engineer-computer-vision-l4-033',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Computer Vision',
    subcategory: 'Document Understanding',
    question: 'What is the LayoutLM architecture and how does it incorporate spatial layout information for document AI?',
    options: [
      'LayoutLM extends BERT by adding 2D position embeddings (x, y, width, height of each token\'s bounding box) to the text token embeddings, enabling the model to understand spatial relationships in documents. LayoutLMv3 further adds patch embeddings for the document image, unifying text, layout, and visual information. This allows understanding tables, forms, and invoices where spatial structure is critical for meaning',
      'LayoutLM can only process single-column documents',
      'LayoutLM processes documents as plain text without spatial information',
      'LayoutLM requires manual annotation of document layout',
    ],
    correctAnswer: 0,
    explanation: 'Documents have 2D structure: a table cell\'s meaning depends on its row/column position, not just text content. LayoutLM (Xu et al. 2020): token embedding + 1D position + 2D position (x0, y0, x1, y1 bounding box corners) + segment embedding. LayoutLMv2 adds visual features from a document image encoder. LayoutLMv3 (Huang et al. 2022): unified text-image multimodal pre-training with masked language modeling + masked image modeling + word-patch alignment. Pre-trained on 11M document images. Excels at: form understanding, receipt extraction, table detection, document classification.',
    tags: ['layoutlm', 'document-ai', '2d-position', 'multimodal-document'],
  },
];

export default questions;
