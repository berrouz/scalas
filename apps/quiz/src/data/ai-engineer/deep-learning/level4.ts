import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ai-engineer-deep-learning-l4-001',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Training Stability',
    question: 'What is the "loss of plasticity" problem in continual training of deep networks?',
    options: [
      'The model becomes too flexible and overfits',
      'Loss of plasticity only affects recurrent neural networks',
      'Over long training sequences, neurons become saturated or dead, and the network loses its ability to learn new tasks even with new data. This is caused by growing weight magnitudes, dead ReLUs, and rank collapse of representations, requiring periodic "regeneration" of dormant neurons',
      'The model forgets all previously learned information immediately',
    ],
    correctAnswer: 2,
    explanation: 'Lyle et al. (2023) showed that networks trained on a sequence of tasks progressively lose the ability to fit new tasks. Contributing factors: (1) dead ReLUs accumulate (no gradient flow), (2) weights grow unbounded, saturating activations, (3) effective rank of hidden representations decreases. Mitigations include periodic reinitialization of dormant neurons, continual backpropagation, or using shrink-and-perturb strategies.',
    tags: ['plasticity', 'continual-learning', 'dead-neurons', 'rank-collapse'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-002',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Normalization',
    question: 'Why does Layer Normalization work well in transformers while Batch Normalization fails, and what is RMSNorm\'s advantage?',
    options: [
      'RMSNorm is slower than LayerNorm but more accurate',
      'BatchNorm computes statistics across the batch dimension, which fails with variable-length sequences and small/distributed batches. LayerNorm normalizes across feature dimensions per token. RMSNorm drops the mean-centering of LayerNorm (only divides by RMS), saving ~10% computation with equivalent performance, since the re-centering is redundant when followed by learned affine parameters',
      'Batch Normalization is always superior to Layer Normalization',
      'LayerNorm and BatchNorm are mathematically identical',
    ],
    correctAnswer: 1,
    explanation: 'BatchNorm statistics depend on batch composition, causing issues with variable-length sequences (padding affects statistics) and small batches (noisy statistics). LayerNorm computes μ and σ per token across features, independent of other sequences. RMSNorm: RMS(x) = √(mean(x²)), skipping the mean subtraction step. Zhang & Sennrich (2019) showed the mean-centering in LayerNorm is not critical, and RMSNorm is used in LLaMA, Gemma, and other modern LLMs.',
    tags: ['normalization', 'rmsnorm', 'layernorm', 'batchnorm'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-003',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Quantization',
    question: 'What is GPTQ quantization and how does it achieve 4-bit weights with minimal quality loss?',
    options: [
      'GPTQ uses a layer-wise quantization approach based on optimal brain quantization: it quantizes weights one column at a time while adjusting remaining unquantized weights to compensate for the quantization error using the inverse Hessian of the layer\'s reconstruction loss',
      'GPTQ requires retraining the model after quantization',
      'GPTQ simply rounds weights to the nearest 4-bit value',
      'GPTQ only works with 8-bit quantization, not 4-bit',
    ],
    correctAnswer: 0,
    explanation: 'GPTQ (Frantar et al. 2022) adapts Optimal Brain Quantization to LLMs: for each weight column, it (1) quantizes to the nearest grid point, (2) distributes the quantization error to unprocessed columns using the inverse Hessian (H⁻¹), computed from a small calibration set. Processing columns in order of quantization sensitivity (diagonal of H) minimizes total error. This achieves <1% perplexity increase at 4-bit for most LLMs, taking only a few GPU-hours.',
    tags: ['gptq', 'quantization', 'compression', 'hessian'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-004',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Architecture',
    question: 'What is the Mamba architecture and how does it differ from standard transformer attention?',
    options: [
      'Mamba is a larger version of the transformer with more attention heads',
      'Mamba is a selective state space model (SSM) that replaces attention with input-dependent state transitions. Unlike fixed-parameter SSMs, Mamba makes the state transition matrices dependent on the input, enabling content-aware reasoning with O(N) compute and memory instead of attention\'s O(N²), using a hardware-aware parallel scan algorithm',
      'Mamba uses convolutions instead of attention but with O(N²) complexity',
      'Mamba is identical to RWKV in both design and performance',
    ],
    correctAnswer: 1,
    explanation: 'Mamba (Gu & Dao 2023) extends structured SSMs by making the B, C, and Δ (discretization step) matrices functions of the input token, allowing selective state updates (the model can "choose" what to remember). The parallel scan algorithm processes sequences in O(N) time with efficient GPU memory access patterns. This matches transformer quality on language modeling while being 5x faster at inference for long sequences.',
    tags: ['mamba', 'ssm', 'state-space-model', 'linear-attention'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-005',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Diffusion Models',
    question: 'What is classifier-free guidance in diffusion models and why does it produce higher quality images than classifier guidance?',
    options: [
      'Classifier-free guidance uses a pretrained classifier to steer generation',
      'Classifier-free guidance eliminates the need for text prompts',
      'Classifier-free guidance trains a single diffusion model jointly on conditional and unconditional generation (randomly dropping conditioning), then at inference amplifies the difference between conditional and unconditional score estimates: ε = ε_uncond + w·(ε_cond - ε_uncond). This avoids needing a separate classifier and provides smoother, more coherent guidance',
      'Classifier-free guidance is identical to CLIP guidance',
    ],
    correctAnswer: 2,
    explanation: 'Classifier guidance (Dhariwal & Nichol 2021) adds ∇log p(y|x_t) from a noisy classifier, but requires training a separate classifier on noisy images. Classifier-free guidance (Ho & Salimans 2022) trains the denoising model with conditional dropout: during training, the class/text label is randomly replaced with null. At inference, the guided score ε = ε_∅ + w·(ε_c - ε_∅) amplifies the direction toward the condition. Higher w = more adherent to prompt but less diverse.',
    tags: ['diffusion', 'classifier-free-guidance', 'conditional-generation', 'score-function'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-006',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Optimization',
    question: 'What is the μP (Maximal Update Parametrization) and why does it enable hyperparameter transfer across model scales?',
    options: [
      'μP is a learning rate schedule that decreases as models get larger',
      'μP rescales initialization, learning rates, and layer outputs based on model width such that the optimal hyperparameters remain stable across different model sizes. This allows tuning hyperparameters on a small (cheap) model and directly transferring them to a large model without re-tuning',
      'μP requires training all model sizes simultaneously',
      'μP only works for models with fewer than 1 billion parameters',
    ],
    correctAnswer: 1,
    explanation: 'Standard parametrization (SP) breaks hyperparameter transfer: optimal LR for a 125M model differs from a 13B model. μP (Yang et al. 2022) derives width-dependent scaling factors for init variance (1/width), LR (1/width for embedding, constant for hidden), and output multiplier (1/width) such that all layers have O(1) activations and updates regardless of width. Cerebras showed this enables tuning on a 40M proxy and transferring to 6.7B.',
    tags: ['mup', 'hyperparameter-transfer', 'scaling', 'parametrization'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-007',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Sparse Training',
    question: 'What is the Lottery Ticket Hypothesis and what is its practical limitation?',
    options: [
      'Dense networks contain sparse subnetworks (winning tickets) that when trained in isolation from the same initialization achieve comparable accuracy. The practical limitation is that finding these tickets requires training the full dense model first (often multiple times with iterative magnitude pruning), offering no training compute savings',
      'It states that larger models always contain smaller models that perform better',
      'It only applies to fully connected networks, not CNNs or transformers',
      'It proves that all neural networks can be pruned to 1% of parameters without loss',
    ],
    correctAnswer: 0,
    explanation: 'Frankle & Carlin (2019): train dense → prune smallest-magnitude weights → reset remaining weights to original initialization → retrain. The surviving subnetwork achieves full accuracy. However, identifying the winning ticket requires complete dense training. Later work (SNIP, GraSP) attempts to find tickets at initialization, and "late rewinding" (resetting to epoch k instead of 0) improves stability at higher sparsity.',
    tags: ['lottery-ticket', 'pruning', 'sparse-networks', 'efficient-training'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-008',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Knowledge Distillation',
    question: 'What is the role of the temperature parameter in knowledge distillation and why do "dark knowledge" soft targets improve student learning?',
    options: [
      'Temperature T softens the teacher\'s output distribution (softmax(z/T)), revealing inter-class similarities (dark knowledge) that hard labels destroy. A cat image with soft probabilities [0.7 cat, 0.2 tiger, 0.1 dog] teaches the student that cats resemble tigers more than dogs — information absent from the one-hot label [1, 0, 0]',
      'Temperature makes the teacher model train faster',
      'Higher temperature always produces better students',
      'Temperature is only used during student training, not during teacher inference',
    ],
    correctAnswer: 0,
    explanation: 'Hinton et al. (2015): softmax(z_i/T) with T>1 produces softer distributions that expose relative similarities between classes. The student is trained on KL(soft_teacher || soft_student) · T² + α · CE(hard_label, student). The T² scaling compensates for reduced gradient magnitudes at higher T. Dark knowledge encodes the teacher\'s learned metric structure of the output space, providing more supervisory signal than hard labels.',
    tags: ['distillation', 'temperature', 'dark-knowledge', 'soft-targets'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-009',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Mixed Precision',
    question: 'What is the "loss scaling" technique in mixed-precision training and why is it necessary with FP16?',
    options: [
      'Loss scaling normalizes the loss to be between 0 and 1',
      'Loss scaling is only needed for the first epoch of training',
      'Loss scaling increases the actual learning rate proportionally',
      'FP16 gradients can underflow to zero for small gradient values (FP16 min positive ~6e-8). Loss scaling multiplies the loss by a large factor before backpropagation to shift gradients into FP16 representable range, then divides the weight update by the same factor. Dynamic loss scaling automatically adjusts the factor based on gradient overflow detection',
    ],
    correctAnswer: 3,
    explanation: 'FP16 has limited dynamic range (5.96e-8 to 65504). Many gradients in deep networks have magnitudes below 1e-7, which become zero in FP16. Loss scaling: L_scaled = L × 2^k → backprop in FP16 → divide gradients by 2^k before FP32 weight update. Dynamic loss scaling starts with a large scale, halves it when NaN/Inf gradients are detected, and doubles it every N steps without NaN. This is essential for stable FP16 training.',
    tags: ['mixed-precision', 'loss-scaling', 'fp16', 'dynamic-scaling'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-010',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Attention Mechanisms',
    question: 'What is Multi-Query Attention (MQA) and how does it reduce the KV cache memory bottleneck?',
    options: [
      'MQA shares a single key and value head across all query heads, reducing the KV cache size by a factor equal to the number of heads (e.g., 32x for 32-head model). This dramatically reduces memory bandwidth requirements during autoregressive inference, where KV cache loading is the primary bottleneck, with minimal quality degradation',
      'MQA increases the number of attention heads for better quality',
      'MQA uses multiple queries per attention head',
      'MQA eliminates the key and value matrices entirely',
    ],
    correctAnswer: 0,
    explanation: 'Standard multi-head attention: H query heads, H key heads, H value heads. MQA: H query heads, 1 key head, 1 value head. KV cache per token drops from H·d to 1·d. For a 32-head model with d=128, this is 32x less KV cache memory. Grouped-Query Attention (GQA) is a middle ground: G key/value groups shared among H/G query heads. LLaMA 2 70B uses GQA with 8 KV heads for 32 query heads.',
    tags: ['mqa', 'gqa', 'kv-cache', 'inference-optimization'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-011',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Activation Functions',
    question: 'Why has SwiGLU replaced ReLU/GELU in modern LLM architectures like LLaMA and PaLM?',
    options: [
      'SwiGLU uses fewer parameters than ReLU-based FFN layers',
      'SwiGLU is identical to GELU in practice',
      'SwiGLU is mathematically simpler than ReLU',
      'SwiGLU combines Swish activation with a Gated Linear Unit, using SwiGLU(x) = Swish(xW₁) ⊙ (xV) with a multiplicative gating mechanism. Despite having more parameters per layer (three weight matrices vs two), it achieves better loss per FLOP due to the gating providing input-dependent feature selection',
    ],
    correctAnswer: 3,
    explanation: 'Standard FFN: ReLU(xW₁)W₂ has 2 matrices. SwiGLU(x,W,V,W₂) = (Swish(xW) ⊙ xV)W₂ has 3 matrices, but the hidden dimension is typically reduced to 2/3 to match total parameter count. Shazeer (2020) showed GLU variants consistently outperform standard FFNs. The gating mechanism xV acts as a learned feature selector, and Swish\'s smoothness helps gradient flow. PaLM, LLaMA, Mistral all use SwiGLU.',
    tags: ['swiglu', 'glu', 'activation-functions', 'llm-architecture'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-012',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Distributed Training',
    question: 'What is the key difference between ZeRO Stage 1, 2, and 3 in DeepSpeed, and what are the memory savings at each stage?',
    options: [
      'Stage 1 partitions optimizer states across GPUs (~4x savings). Stage 2 additionally partitions gradients (~8x). Stage 3 partitions parameters too, enabling training models larger than single GPU memory (~Nx for N GPUs). Each stage trades communication volume for memory savings, with Stage 3 requiring all-gather for each forward/backward pass',
      'Each stage adds more data parallelism workers',
      'ZeRO stages only affect inference, not training memory',
      'All three stages provide identical memory savings',
    ],
    correctAnswer: 0,
    explanation: 'For a model with Ψ parameters in FP16: memory = 2Ψ (params) + 2Ψ (gradients) + 12Ψ (Adam optimizer: fp32 params + fp32 momentum + fp32 variance). ZeRO-1: partition 12Ψ across N GPUs → 12Ψ/N per GPU. ZeRO-2: also partition 2Ψ gradients → (12+2)Ψ/N. ZeRO-3: also partition 2Ψ params → 16Ψ/N, but requires all-gather for params before each op. Combined with offloading (ZeRO-Infinity), CPU/NVMe can further extend capacity.',
    tags: ['zero', 'deepspeed', 'distributed-training', 'memory-optimization'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-013',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Architecture',
    question: 'What are Mixture of Experts (MoE) models and what is the load balancing problem?',
    options: [
      'MoE replaces each dense FFN layer with N expert FFN modules and a learned router that selects top-k experts per token. This scales model capacity without proportional compute increase. The load balancing problem: without auxiliary losses, the router can collapse to routing most tokens to a few experts, wasting capacity and causing memory/compute imbalances across GPUs',
      'MoE is only applicable to computer vision models',
      'MoE models use fewer total parameters than dense models',
      'MoE trains multiple separate models and votes on predictions',
    ],
    correctAnswer: 0,
    explanation: 'Switch Transformer/Mixtral: each token is routed to top-k of N experts (e.g., top-2 of 8). A token sees ~2/8 of parameters, keeping FLOPs similar to a 2-expert model while having 8x capacity. Load balancing auxiliary loss: L_bal = α·N·Σ(f_i·P_i) where f_i = fraction of tokens routed to expert i, P_i = mean routing probability to expert i. This penalizes uneven routing. Expert parallelism distributes experts across GPUs.',
    tags: ['moe', 'mixture-of-experts', 'load-balancing', 'routing'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-014',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Positional Encoding',
    question: 'How does Rotary Position Embedding (RoPE) encode position information and why does it enable length generalization?',
    options: [
      'RoPE works identically to learned absolute position embeddings',
      'RoPE adds a fixed sinusoidal vector to each token embedding',
      'RoPE encodes only the first 512 positions; beyond that, positions wrap around',
      'RoPE applies rotation matrices to query and key vectors based on their positions, encoding relative position through the angle between rotated vectors. The dot product q_m · k_n depends only on (m-n), providing relative position encoding. Its frequency-based design allows interpolation (YaRN, NTK-aware) to extend to longer sequences than seen during training',
    ],
    correctAnswer: 3,
    explanation: 'RoPE (Su et al. 2021) rotates pairs of dimensions by angles proportional to position: R_θ,m applied to queries at position m, R_θ,n to keys at position n. The attention score q_m^T R_θ,m^T R_θ,n k_n = q_m^T R_θ,(m-n) k_n depends only on relative position. For length extension, the frequencies can be scaled: NTK-aware scaling adjusts high-frequency components, and YaRN combines frequency scaling with attention scaling for effective 4-8x context extension.',
    tags: ['rope', 'positional-encoding', 'rotary', 'length-generalization'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-015',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Training Techniques',
    question: 'What is gradient checkpointing (activation recomputation) and what is the compute-memory trade-off?',
    options: [
      'Instead of storing all intermediate activations for backpropagation, gradient checkpointing saves activations only at selected layers (checkpoints) and recomputes intermediate activations during the backward pass. This trades ~33% extra compute for ~O(√N) memory reduction for N layers, enabling training much deeper models in limited GPU memory',
      'Gradient checkpointing saves gradients to disk and reloads them',
      'Gradient checkpointing doubles both compute and memory',
      'Gradient checkpointing eliminates the backward pass entirely',
    ],
    correctAnswer: 0,
    explanation: 'Standard backprop stores all N layers\' activations (~O(N) memory). With √N evenly-spaced checkpoints, only √N activations are stored. During backward pass, activations between checkpoints are recomputed from the nearest checkpoint. This adds one extra forward pass per segment (~33% compute overhead) but reduces activation memory from O(N) to O(√N). PyTorch: torch.utils.checkpoint.checkpoint(). This is essential for training large transformers with long sequences.',
    tags: ['gradient-checkpointing', 'activation-recomputation', 'memory', 'training'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-016',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Initialization',
    question: 'Why does the standard Xavier/Kaiming initialization fail for transformers, and what does GPT-style initialization do differently?',
    options: [
      'Xavier initialization is always correct for all architectures',
      'In deep transformers, residual connections accumulate variance across layers (each residual adds signal), causing exploding activations. GPT-style initialization scales the output projection of each residual block by 1/√(2N) where N is the number of layers, keeping the variance of the residual stream approximately constant regardless of depth',
      'Kaiming initialization works perfectly for transformers',
      'GPT-style initialization uses zero initialization for all weights',
    ],
    correctAnswer: 1,
    explanation: 'With N residual layers, the residual stream variance grows as O(N) if each branch adds unit-variance signal: x + f(x) where f has unit variance means var(output) = var(x) + var(f(x)). After N layers, var ∝ N. GPT-2/3 scales the last linear in each residual branch by 1/√(2N) (2N because each block has attention + FFN). This ensures the residual stream variance remains O(1). Some implementations use pre-norm + careful init to achieve similar effects.',
    tags: ['initialization', 'residual-connection', 'transformer', 'variance-scaling'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-017',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Efficient Attention',
    question: 'How does Flash Attention achieve exact attention computation while being 2-4x faster and using O(N) memory instead of O(N²)?',
    options: [
      'Flash Attention only works on NVIDIA A100 GPUs',
      'Flash Attention approximates attention by sampling random key-value pairs',
      'Flash Attention tiles the Q, K, V matrices into blocks that fit in GPU SRAM, computes attention block-by-block with online softmax (maintaining running statistics), and never materializes the full N×N attention matrix in HBM. This exploits the GPU memory hierarchy: SRAM access is ~100x faster than HBM',
      'Flash Attention uses sparse attention patterns to reduce computation',
    ],
    correctAnswer: 2,
    explanation: 'Standard attention materializes S=QK^T (N×N in HBM) → softmax → multiply by V. Flash Attention (Dao et al. 2022) uses tiling: load Q_block, K_block, V_block into SRAM, compute local attention with online softmax correction, accumulate results. The N×N matrix never exists in HBM. The online softmax trick maintains running max and sum-of-exp across blocks for numerically stable computation. Flash Attention 2 further optimizes by reducing non-matmul FLOPs and improving parallelism.',
    tags: ['flash-attention', 'tiling', 'sram', 'memory-hierarchy'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-018',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Contrastive Learning',
    question: 'What causes "dimensional collapse" in self-supervised contrastive learning and how do methods like VICReg prevent it?',
    options: [
      'Dimensional collapse occurs when the representation vectors span only a low-dimensional subspace of the embedding space, wasting capacity. VICReg prevents this with an explicit variance term (keeping each dimension\'s variance above a threshold) and a covariance term (decorrelating different dimensions), without requiring negative pairs or momentum encoders',
      'Dimensional collapse only happens with very small batch sizes',
      'Dimensional collapse means the model runs out of GPU memory',
      'VICReg uses contrastive loss with large batches to prevent collapse',
    ],
    correctAnswer: 0,
    explanation: 'Without explicit prevention, self-supervised methods can converge to representations where all vectors lie on a low-dimensional manifold (e.g., all embeddings become nearly identical — complete collapse, or only a few dimensions have variance — dimensional collapse). VICReg (Bardes et al. 2022) adds three terms: Variance (std of each dimension > threshold), Invariance (MSE between positive pair embeddings), Covariance (off-diagonal covariance → 0). No negatives, no asymmetric architectures needed.',
    tags: ['vicreg', 'contrastive-learning', 'dimensional-collapse', 'self-supervised'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-019',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Neural ODE',
    question: 'What are Neural ODEs and what computational advantage does the adjoint method provide over backpropagation through the ODE solver?',
    options: [
      'The adjoint method is less accurate than standard backpropagation',
      'Neural ODEs define the hidden state dynamics as dh/dt = f(h, t, θ) and solve the ODE to get the final state. The adjoint method computes gradients by solving another ODE backward in time, requiring O(1) memory regardless of the number of solver steps, unlike backpropagation through the solver which stores all intermediate states',
      'Neural ODEs are ordinary differential equation solvers implemented on neural hardware',
      'Neural ODEs always converge faster than discrete neural networks',
    ],
    correctAnswer: 1,
    explanation: 'Standard backprop through an ODE solver (e.g., RK45 with 100 steps) requires storing all 100 intermediate states for the backward pass — O(L) memory. The adjoint method: define adjoint a(t) = ∂L/∂h(t), which satisfies its own ODE da/dt = -a^T ∂f/∂h. Solve this ODE backward from t₁ to t₀ while simultaneously computing ∂L/∂θ. Memory: O(1) — only the current state and adjoint are needed. Trade-off: requires solving another ODE.',
    tags: ['neural-ode', 'adjoint-method', 'continuous-dynamics', 'memory-efficiency'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-020',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Weight Tying',
    question: 'What is weight tying between the input embedding and output projection in language models and what is its impact?',
    options: [
      'Weight tying only works for character-level models',
      'Weight tying shares the same weight matrix between the input token embedding layer and the output softmax projection layer (transposed). This reduces parameters by vocab_size × hidden_dim (often 25-50% of total for small models), acts as regularization, and is theoretically motivated by the observation that semantically similar tokens should map to similar regions in both input and output space',
      'Weight tying prevents the model from learning different input and output representations',
      'Weight tying duplicates the embedding layer for redundancy',
    ],
    correctAnswer: 1,
    explanation: 'If embedding matrix E ∈ R^{V×d}, the output projection W_out ∈ R^{d×V}. Weight tying sets W_out = E^T. For V=50000, d=4096, this saves 200M parameters. Press & Wolf (2017) showed this improves perplexity on smaller models while reducing parameters. Most modern LLMs (GPT-2, LLaMA) use weight tying. The intuition: a word\'s embedding should be close to its output logit direction. Without tying, these are learned independently and may diverge.',
    tags: ['weight-tying', 'embedding', 'parameter-sharing', 'language-model'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-021',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Pruning',
    question: 'What is structured pruning versus unstructured pruning, and why does structured pruning enable actual speedup?',
    options: [
      'Both types of pruning provide identical speedup on all hardware',
      'Structured pruning is always superior with no accuracy trade-off',
      'Unstructured pruning zeros individual weights, creating sparse matrices that require special sparse hardware/libraries for speedup. Structured pruning removes entire neurons, channels, or attention heads, reducing actual tensor dimensions and enabling speedup on standard hardware (GPUs, CPUs) with dense matrix operations, though it typically causes more accuracy loss per parameter removed',
      'Unstructured pruning removes entire layers from the network',
    ],
    correctAnswer: 2,
    explanation: 'Unstructured: W[i,j] = 0 for selected weights → sparse matrix. GPUs are optimized for dense matmul; sparse operations only show speedup at >90% sparsity with specialized kernels (cuSPARSE, NVIDIA 2:4 sparsity). Structured: remove entire rows/columns from W → smaller dense matrix → standard matmul is immediately faster. Channel pruning of a Conv2d from 256→128 channels gives instant 2x speedup. The cost: removing an entire structure is more destructive than removing individual weights.',
    tags: ['pruning', 'structured-pruning', 'sparse', 'efficiency'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-022',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Loss Functions',
    question: 'What is the label smoothing technique and how does it affect the model\'s learned representations?',
    options: [
      'Label smoothing increases the model\'s confidence in its predictions',
      'Label smoothing is only applicable to regression tasks',
      'Label smoothing averages predictions across multiple epochs',
      'Label smoothing replaces hard one-hot targets y=[0,0,1,0] with soft targets y=[ε/K, ε/K, 1-ε+ε/K, ε/K] where ε is typically 0.1. This prevents the model from becoming overconfident, improves calibration, and encourages tighter clustering of same-class representations with larger inter-class margins, but can slightly hurt distillation since soft targets lose information',
    ],
    correctAnswer: 3,
    explanation: 'With hard labels, the optimal logit for the correct class is +∞ and -∞ for others — the model never stops pushing logits apart, leading to overconfidence. Label smoothing caps the optimal logit magnitude, resulting in better calibrated probabilities. Müller et al. (2019) showed it makes penultimate-layer representations form tighter, more separated clusters. The downside: soft targets from a teacher become less distinct when the student already uses label smoothing.',
    tags: ['label-smoothing', 'calibration', 'regularization', 'cross-entropy'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-023',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Flow Models',
    question: 'What is a normalizing flow and what constraint does it impose on the neural network architecture?',
    options: [
      'Normalizing flows require the network to be recurrent',
      'A normalizing flow transforms a simple base distribution (e.g., Gaussian) through a sequence of invertible, differentiable transformations with tractable Jacobian determinants. This constrains the architecture: each layer must be invertible and have efficiently computable log|det(J)|. Coupling layers, autoregressive transforms, and residual flows satisfy this constraint',
      'Normalizing flows only produce discrete probability distributions',
      'Normalizing flows can use any neural network architecture without constraints',
    ],
    correctAnswer: 1,
    explanation: 'Normalizing flows model p(x) = p_z(f⁻¹(x)) · |det(∂f⁻¹/∂x)| where f transforms base distribution z to data x. The key constraint: f must be invertible with tractable Jacobian determinant. Coupling layers (RealNVP): split input, transform one half conditioned on the other — triangular Jacobian with O(d) det computation. Autoregressive flows: O(d) Jacobian but sequential generation. This enables exact likelihood computation unlike VAEs/diffusion.',
    tags: ['normalizing-flows', 'invertible', 'jacobian', 'density-estimation'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-024',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Batch Size',
    question: 'What is the critical batch size phenomenon and how does it relate to the gradient noise scale?',
    options: [
      'Larger batch sizes always converge faster and to better minima',
      'Critical batch size is the minimum batch that fits in GPU memory',
      'Below the critical batch size, doubling the batch size approximately doubles throughput with similar convergence (compute-optimal). Above it, doubling the batch gives diminishing returns. The critical batch size B_crit ≈ B_noise = tr(Σ)/||G||² relates gradient noise to signal, and training beyond B_crit wastes compute as linear scaling of LR breaks down',
      'The critical batch size is always 32 regardless of the model',
    ],
    correctAnswer: 2,
    explanation: 'McCandlish et al. (2018): the gradient noise scale B_noise = tr(Σ)/G² (ratio of gradient variance to gradient norm squared) predicts the critical batch size. Below B_crit: SGD is noise-dominated, larger batches improve signal/noise efficiently. Above B_crit: SGD is gradient-dominated, larger batches provide diminishing returns in convergence per step. For GPT-3 175B, B_crit ≈ 3.2M tokens. This determines the optimal batch size for compute efficiency.',
    tags: ['critical-batch-size', 'gradient-noise', 'scaling-laws', 'compute-efficiency'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-025',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Speculative Decoding',
    question: 'What is speculative decoding and why does it provide lossless speedup for autoregressive generation?',
    options: [
      'Speculative decoding approximates the target model\'s distribution for speed',
      'Speculative decoding generates multiple possible outputs and lets users choose',
      'Speculative decoding requires two GPUs, one for each model',
      'A small draft model generates k tokens cheaply, then the large target model verifies all k tokens in a single parallel forward pass. Tokens where draft and target agree are accepted; the first disagreement triggers resampling from the target model. This is mathematically guaranteed to produce the same distribution as the target model while reducing the number of expensive forward passes',
    ],
    correctAnswer: 3,
    explanation: 'Leviathan et al. (2023): draft model generates x₁...x_k autoregressively (cheap). Target model computes p_target(x_i|x_{<i}) for all i in parallel (one forward pass). Accept x_i if p_target(x_i) ≥ p_draft(x_i), otherwise accept with probability p_target/p_draft and resample. This rejection sampling scheme provably samples from p_target exactly. Speedup ≈ k/(1 + k·(1-α)) where α is acceptance rate. Typically 2-3x speedup with a well-matched draft model.',
    tags: ['speculative-decoding', 'inference', 'autoregressive', 'lossless-speedup'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-026',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Architecture Search',
    question: 'What is the scaling law for neural language models and how does it guide compute-optimal training?',
    options: [
      'Chinchilla scaling laws show loss ∝ a/N^α + b/D^β + c, where N=parameters, D=data tokens. For compute-optimal training, model size and data should scale equally: doubling compute should double both N and D. This revealed that GPT-3 175B was undertrained (too large for its data budget), and the 70B Chinchilla trained on 4x more data matched its performance',
      'Scaling laws predict that 10x compute gives 10x performance improvement',
      'Scaling laws only apply to convolutional neural networks',
      'Scaling laws state that performance always improves linearly with model size',
    ],
    correctAnswer: 0,
    explanation: 'Hoffmann et al. (2022) found that for compute-optimal training, N and D should scale roughly equally: N_opt ∝ C^0.5, D_opt ∝ C^0.5. GPT-3 used 300B tokens for 175B params; Chinchilla used 1.4T tokens for 70B params (same compute). The power law exponents (α≈0.34, β≈0.28) determine diminishing returns. This fundamentally changed LLM training strategy toward using more data with smaller models.',
    tags: ['scaling-laws', 'chinchilla', 'compute-optimal', 'llm-training'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-027',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Compiler Optimization',
    question: 'What does torch.compile do in PyTorch 2.0 and how does it achieve speedup?',
    options: [
      'torch.compile requires rewriting all model code in a special DSL',
      'torch.compile captures the computation graph via TorchDynamo (Python bytecode analysis), applies graph-level optimizations via TorchInductor (operator fusion, memory planning), and generates optimized Triton GPU kernels or C++ CPU code. This reduces kernel launch overhead, eliminates redundant memory reads, and fuses operations like matmul+bias+activation into single kernels',
      'torch.compile only works in eager mode and provides no performance benefit',
      'torch.compile converts PyTorch code to TensorFlow for faster execution',
    ],
    correctAnswer: 1,
    explanation: 'TorchDynamo uses CPython frame evaluation hooks to capture the computation graph from eager Python code with minimal user changes. TorchInductor then: (1) fuses pointwise operations (eliminating intermediate tensor materializations), (2) generates Triton kernels for GPU (automatic tiling, memory coalescing), (3) optimizes memory allocation patterns. Typical speedup: 30-60% for training, 50-100% for inference. Graph breaks (data-dependent control flow) cause fallback to eager for those sections.',
    tags: ['torch-compile', 'torchdynamo', 'torchinductor', 'kernel-fusion'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-028',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'GAN Training',
    question: 'What is the mode collapse problem in GANs and how do Wasserstein GANs (WGANs) mitigate it?',
    options: [
      'Mode collapse means the generator produces too many modes',
      'Mode collapse only occurs with very deep generators',
      'WGANs completely solve mode collapse in all cases',
      'Mode collapse occurs when the generator produces only a few sample types, ignoring the full data distribution. Standard GAN\'s JS divergence provides zero gradient when generator and real distributions don\'t overlap. WGAN uses the Wasserstein-1 (Earth Mover\'s) distance, which provides meaningful gradients even for non-overlapping distributions, with Lipschitz constraint enforced via weight clipping or gradient penalty',
    ],
    correctAnswer: 3,
    explanation: 'JS divergence is maxed out (log 2) when distributions don\'t overlap, providing no gradient signal. The Wasserstein distance W₁(P,Q) = inf E[||x-y||] always provides a continuous, differentiable loss landscape. The critic (discriminator) must be 1-Lipschitz: WGAN-GP adds λ·(||∇D(x̂)||₂ - 1)² where x̂ is interpolated between real and fake. While WGANs reduce mode collapse, they don\'t eliminate it entirely; spectral normalization and progressive growing further help.',
    tags: ['wgan', 'mode-collapse', 'wasserstein', 'gradient-penalty'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-029',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Tensor Parallelism',
    question: 'How does tensor parallelism split a transformer layer across GPUs and what are the communication requirements?',
    options: [
      'Tensor parallelism requires no inter-GPU communication',
      'Tensor parallelism puts different layers on different GPUs',
      'Tensor parallelism splits weight matrices within a layer: for the FFN, W₁ is column-split and W₂ is row-split across GPUs, so each GPU computes a partial result. An all-reduce synchronizes the outputs. For attention, Q/K/V projections are column-split (one head per GPU) and the output projection is row-split. This requires 2 all-reduce operations per transformer layer (one after attention, one after FFN)',
      'Tensor parallelism is identical to data parallelism',
    ],
    correctAnswer: 2,
    explanation: 'Megatron-LM style: FFN has Y = GeLU(XW₁)W₂. Column-split W₁ into [W₁¹, W₁²] across 2 GPUs: each computes GeLU(XW₁ⁱ) locally. Row-split W₂: each computes Y_i = GeLU(XW₁ⁱ)W₂ⁱ locally, then all-reduce to sum Y = ΣY_i. Same pattern for attention: column-split Q/K/V (each GPU handles subset of heads), row-split output projection, all-reduce. 2 all-reduces per layer, each O(batch·seq·hidden) volume.',
    tags: ['tensor-parallelism', 'megatron', 'all-reduce', 'model-parallelism'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-030',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'RLHF',
    question: 'What is Direct Preference Optimization (DPO) and how does it simplify RLHF?',
    options: [
      'DPO is an improved version of PPO for RLHF',
      'DPO requires more compute than RLHF with PPO',
      'DPO eliminates the need for a separate reward model by showing that the optimal policy under the KL-constrained reward maximization objective can be expressed as a closed-form function of the policy itself. This enables training directly on preference pairs (chosen, rejected) with a simple classification-like loss, avoiding the instability of RL training',
      'DPO can only be used for binary feedback, not ranked preferences',
    ],
    correctAnswer: 2,
    explanation: 'Standard RLHF: (1) train reward model on preferences, (2) optimize policy via PPO to maximize reward - KL(π||π_ref). DPO (Rafailov et al. 2023) shows the optimal solution has r*(x,y) = β·log(π*(y|x)/π_ref(y|x)) + f(x). Substituting into the Bradley-Terry preference model gives: L_DPO = -log σ(β·log(π(y_w|x)/π_ref(y_w|x)) - β·log(π(y_l|x)/π_ref(y_l|x))). This is a supervised loss on preference pairs — no reward model, no RL.',
    tags: ['dpo', 'rlhf', 'preference-optimization', 'alignment'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-031',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'VAE',
    question: 'What is the posterior collapse problem in VAEs and how do techniques like KL annealing and free bits address it?',
    options: [
      'Posterior collapse is beneficial as it means the model has converged',
      'Posterior collapse only happens with convolutional decoders',
      'Posterior collapse means the VAE decoder produces blurry images',
      'Posterior collapse occurs when the encoder learns to ignore the input and output the prior q(z|x) ≈ p(z), making the latent code z uninformative. KL annealing gradually increases the KL weight from 0 to 1 during training, allowing the encoder to encode information before the KL penalty pushes it toward the prior. Free bits sets a minimum KL per dimension, ensuring some information is always encoded',
    ],
    correctAnswer: 3,
    explanation: 'ELBO = E[log p(x|z)] - KL(q(z|x)||p(z)). With a powerful autoregressive decoder, the model can achieve low reconstruction loss by ignoring z, while the KL term pushes q→p (KL=0). This is a valid local optimum but useless: z carries no information. KL annealing: weight β on KL goes from 0→1 over training. Free bits: λ = max(KL_j, target) per dimension j ensures each latent dimension carries ≥target nats of information.',
    tags: ['vae', 'posterior-collapse', 'kl-annealing', 'free-bits'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-032',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Efficient Fine-Tuning',
    question: 'What is QLoRA and how does it enable fine-tuning a 65B parameter model on a single 48GB GPU?',
    options: [
      'QLoRA combines 4-bit NormalFloat quantization of the base model with Low-Rank Adapters (LoRA) trained in BF16. The base model is quantized to 4-bit NF4 (using a quantization distribution matched to normal weight distributions), while LoRA adapters (rank 16-64) add <1% trainable parameters in higher precision. Double quantization further reduces quantization constant overhead',
      'QLoRA uses quantization-aware training from scratch',
      'QLoRA requires the model to be pre-quantized before training',
      'QLoRA sacrifices 20%+ accuracy for memory savings',
    ],
    correctAnswer: 0,
    explanation: 'QLoRA (Dettmers et al. 2023): (1) 4-bit NormalFloat quantization uses quantile-based bins matching the empirical weight distribution (approximately normal), (2) double quantization quantizes the quantization constants themselves (saving ~0.5 bits/param), (3) paged optimizers handle memory spikes via CPU offloading. A 65B model: 65B × 0.5 bytes = ~32GB base + small LoRA adapters + optimizer states for LoRA only ≈ 40GB total. Matches full fine-tuning quality on benchmarks.',
    tags: ['qlora', 'lora', 'quantization', 'efficient-finetuning'],
  },
  {
    id: 'ai-engineer-deep-learning-l4-033',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Deep Learning',
    subcategory: 'Architecture',
    question: 'What are the key architectural differences between GPT (decoder-only), BERT (encoder-only), and T5 (encoder-decoder), and when is each optimal?',
    options: [
      'GPT uses causal (left-to-right) attention for autoregressive generation. BERT uses bidirectional attention for understanding/classification via masked language modeling. T5 uses a full encoder-decoder with cross-attention for sequence-to-sequence tasks. Decoder-only has emerged as dominant for scale due to simpler training (next-token prediction) and unified interface for all tasks via prompting',
      'They are all identical architectures with different training data',
      'BERT can generate text as effectively as GPT',
      'Encoder-decoder is always better than decoder-only for all tasks',
    ],
    correctAnswer: 0,
    explanation: 'GPT: triangular causal mask prevents attending to future tokens, enabling generation. BERT: no causal mask, full bidirectional context, trained with MLM (predict [MASK] tokens). T5: encoder sees full input bidirectionally, decoder generates output autoregressively with cross-attention to encoder. At scale, decoder-only dominates because: (1) single unified training objective, (2) natural few-shot capability via prompting, (3) KV cache makes incremental decoding efficient. Encoder-decoder excels at fixed input→output tasks (translation, summarization).',
    tags: ['gpt', 'bert', 't5', 'architecture-comparison'],
  },
];

export default questions;
