import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ai-engineer-nlp-transformers-l3-001',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Tokenization',
    question: 'In the Unigram language model used by SentencePiece, how is the vocabulary pruned during training?',
    options: [
      'Tokens whose removal causes the smallest increase in overall corpus loss are iteratively removed',
      'The least frequent tokens are removed first',
      'Random tokens are removed until the target size is reached',
      'Tokens are removed based on their character length, longest first',
    ],
    correctAnswer: 0,
    explanation:
      'The Unigram model starts with a large initial vocabulary and iteratively prunes it. At each step, it computes how much each token contributes to the total loss; tokens whose removal increases the loss the least are discarded. This continues until the desired vocabulary size is reached.',
    tags: ['tokenization', 'unigram', 'sentencepiece', 'pruning'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-002',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Tokenization',
    question: 'What is byte-level BPE and why is it used in models like GPT-2?',
    options: [
      'BPE applied to raw bytes (0-255) as base units, enabling tokenization of any text without unknown tokens',
      'BPE that compresses tokens into 8-bit representations for memory efficiency',
      'A variant of BPE that only works with ASCII text',
      'BPE that uses byte-pair statistics from a separate binary corpus',
    ],
    correctAnswer: 0,
    explanation:
      'Byte-level BPE uses the 256 byte values as base vocabulary units instead of Unicode characters. This guarantees that any input text can be tokenized without out-of-vocabulary tokens, since any string is a sequence of bytes. GPT-2 popularized this approach.',
    tags: ['tokenization', 'byte-level-bpe', 'gpt-2'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-003',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Attention Mechanisms',
    question: 'What is Grouped Query Attention (GQA) and what problem does it solve?',
    options: [
      'It shares key-value heads across groups of query heads, reducing KV cache memory while retaining most of MHA quality',
      'It groups tokens into clusters and only computes attention within clusters',
      'It replaces multi-head attention with a single grouped convolution',
      'It divides the sequence into groups and applies separate attention to each group',
    ],
    correctAnswer: 0,
    explanation:
      'GQA is an interpolation between multi-head attention (MHA) and multi-query attention (MQA). It uses fewer key-value heads than query heads, with groups of query heads sharing the same KV heads. This reduces KV cache size during inference while maintaining quality closer to full MHA.',
    tags: ['attention', 'gqa', 'kv-cache', 'efficiency'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-004',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Attention Mechanisms',
    question: 'What is Multi-Query Attention (MQA) and how does it differ from standard Multi-Head Attention?',
    options: [
      'MQA uses a single shared key-value head across all query heads, greatly reducing KV cache size',
      'MQA uses multiple queries per token but standard keys and values',
      'MQA queries the input multiple times with different random seeds',
      'MQA replaces the softmax with a linear attention kernel',
    ],
    correctAnswer: 0,
    explanation:
      'In MQA, all attention heads share a single set of key and value projections while maintaining separate query projections. This dramatically reduces the KV cache memory during autoregressive inference (by a factor of the number of heads) at a small quality cost.',
    tags: ['attention', 'mqa', 'kv-cache'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-005',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Positional Encoding',
    question: 'How does RoPE encode relative position information in the attention computation?',
    options: [
      'The dot product of rotated query and key vectors naturally decomposes into a function of relative position (m-n)',
      'It adds absolute position IDs to the attention scores after softmax',
      'It trains a lookup table of relative position embeddings',
      'It concatenates position information to the value vectors only',
    ],
    correctAnswer: 0,
    explanation:
      'RoPE applies rotation matrices R(m) and R(n) to query and key vectors at positions m and n respectively. The dot product q_m^T * k_n = (R(m)q)^T(R(n)k) depends on R(m-n), making the attention score a function of relative position (m-n). This elegantly encodes relative positions without explicit relative position embeddings.',
    tags: ['positional-encoding', 'rope', 'relative-position'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-006',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Positional Encoding',
    question: 'What is NTK-aware interpolation for extending RoPE to longer contexts?',
    options: [
      'It adjusts the base frequency of RoPE to spread out the rotation angles, preserving high-frequency information while interpolating',
      'It adds new sinusoidal dimensions for positions beyond the training length',
      'It trains additional position embedding parameters for extended positions',
      'It truncates sequences to the original training length and processes them in chunks',
    ],
    correctAnswer: 0,
    explanation:
      'NTK-aware interpolation modifies the base (theta) of the RoPE frequencies rather than linearly scaling positions. This preserves the high-frequency components (which capture local position information) while adjusting low-frequency components for longer range, based on Neural Tangent Kernel theory insights.',
    tags: ['positional-encoding', 'rope', 'context-extension', 'ntk'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-007',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Long Context',
    question: 'How does Flash Attention achieve exact attention with sub-quadratic memory?',
    options: [
      'It tiles the Q, K, V matrices into blocks, computes partial softmax in SRAM, and never materializes the full attention matrix in HBM',
      'It approximates attention with a low-rank decomposition',
      'It uses sparse random projections to reduce the matrix dimensions',
      'It computes attention only for the top-k scoring pairs',
    ],
    correctAnswer: 0,
    explanation:
      'Flash Attention partitions Q, K, V into blocks that fit in GPU SRAM. It computes partial attention (with online softmax normalization via the log-sum-exp trick) block by block, accumulating results without ever storing the full n x n attention matrix in HBM. This yields O(n) memory usage while computing mathematically exact attention.',
    tags: ['flash-attention', 'tiling', 'online-softmax', 'memory'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-008',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Long Context',
    question: 'What is sparse attention as used in models like Longformer?',
    options: [
      'A combination of local sliding window attention and global attention on selected tokens',
      'Randomly dropping attention connections with a fixed probability',
      'Using attention only in alternating layers',
      'Computing attention only between tokens that share the same embedding cluster',
    ],
    correctAnswer: 0,
    explanation:
      'Longformer combines local sliding window attention (each token attends to nearby tokens) with global attention on a few designated tokens (e.g., [CLS]) that can attend to and be attended by all tokens. This achieves O(n) complexity while maintaining global information flow through the global tokens.',
    tags: ['long-context', 'sparse-attention', 'longformer'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-009',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Mixture of Experts',
    question: 'What is the "load balancing loss" in MoE models and why is it necessary?',
    options: [
      'An auxiliary loss that encourages uniform distribution of tokens across experts, preventing expert collapse',
      'A loss function that balances the model\'s performance across different tasks',
      'A regularization term that equalizes the magnitude of expert weights',
      'A loss that ensures equal gradient magnitude across all layers',
    ],
    correctAnswer: 0,
    explanation:
      'Without load balancing, the gating network may route most tokens to a few experts while others remain underutilized (expert collapse). The auxiliary load balancing loss penalizes uneven token distribution across experts, ensuring all experts are trained and utilized effectively.',
    tags: ['moe', 'load-balancing', 'expert-collapse'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-010',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Mixture of Experts',
    question: 'In the Switch Transformer MoE architecture, how many experts does each token get routed to?',
    options: [
      'Exactly 1 (top-1 routing)',
      'Exactly 2 (top-2 routing)',
      'All experts with weighted averaging',
      'A variable number depending on token complexity',
    ],
    correctAnswer: 0,
    explanation:
      'The Switch Transformer simplifies MoE by routing each token to exactly one expert (top-1 routing), reducing computation and communication overhead compared to top-2 routing used in earlier MoE designs. Despite using only one expert per token, it achieves strong performance with proper load balancing.',
    tags: ['moe', 'switch-transformer', 'top-1-routing'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-011',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Transformer Architecture',
    question: 'What is Pre-Norm vs Post-Norm in Transformer architectures, and which is more commonly used in modern LLMs?',
    options: [
      'Pre-Norm applies layer norm before attention/FFN sublayers and is preferred in modern LLMs for training stability',
      'Post-Norm applies layer norm before attention/FFN and is preferred for better final performance',
      'Pre-Norm normalizes the input embeddings only, and is rarely used',
      'There is no practical difference between the two approaches',
    ],
    correctAnswer: 0,
    explanation:
      'Pre-Norm places layer normalization before each sublayer (attention and FFN), while Post-Norm places it after. Pre-Norm provides more stable gradients and easier training, especially for deep models, making it the standard choice for modern large language models. Post-Norm can achieve slightly better final performance but is harder to train.',
    tags: ['transformer', 'pre-norm', 'post-norm', 'training-stability'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-012',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Transformer Architecture',
    question: 'What is RMSNorm and why has it replaced LayerNorm in many modern Transformers?',
    options: [
      'It normalizes using only the root mean square of activations (no mean centering), reducing computation while maintaining quality',
      'It applies normalization only to the residual stream, skipping the main path',
      'It uses a running mean and variance like BatchNorm but per-layer',
      'It normalizes gradients rather than activations',
    ],
    correctAnswer: 0,
    explanation:
      'RMSNorm simplifies LayerNorm by removing the mean-centering step and only dividing by the root mean square of activations. This reduces computational overhead (fewer operations) while empirically matching LayerNorm performance. It is used in LLaMA, Mistral, and other modern LLMs.',
    tags: ['transformer', 'rmsnorm', 'normalization', 'efficiency'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-013',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Transformer Architecture',
    question: 'What is the SwiGLU activation function used in modern Transformers like LLaMA?',
    options: [
      'A gated linear unit variant using SiLU (Swish) activation: SwiGLU(x) = (xW1 * SiLU(xV)) * W2',
      'A simple combination of sigmoid and ReLU activations',
      'A softmax-weighted gate that replaces the FFN entirely',
      'A learned activation function with trainable parameters for the nonlinearity shape',
    ],
    correctAnswer: 0,
    explanation:
      'SwiGLU combines the Swish (SiLU) activation with a Gated Linear Unit structure. The FFN computes SwiGLU(x) = (xW1 * SiLU(xV)) * W2, where one linear projection is gated by the SiLU activation of another. It consistently outperforms ReLU and GELU in practice.',
    tags: ['transformer', 'swiglu', 'activation', 'llama'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-014',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Text Generation',
    question: 'What is speculative decoding and how does it speed up autoregressive generation?',
    options: [
      'A smaller draft model generates candidate tokens that the larger model verifies in parallel, accepting correct predictions',
      'The model generates multiple sequences simultaneously using batch processing',
      'Tokens are generated from the end of the sequence backward',
      'The model predicts multiple tokens at once using a separate multi-token head',
    ],
    correctAnswer: 0,
    explanation:
      'Speculative decoding uses a small, fast draft model to generate several candidate tokens ahead. The large target model then verifies all candidates in a single forward pass (which processes them in parallel). Correct predictions are accepted, and generation continues from the first rejected position. This can provide 2-3x speedup without changing output quality.',
    tags: ['text-generation', 'speculative-decoding', 'inference-speed'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-015',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Text Generation',
    question: 'What is the KV cache and why is it essential for efficient autoregressive inference?',
    options: [
      'It stores previously computed key and value tensors so they are not recomputed at each generation step',
      'It caches the vocabulary probabilities from the previous forward pass',
      'It stores the model weights in a compressed format for faster loading',
      'It keeps a cache of previously generated sequences for deduplication',
    ],
    correctAnswer: 0,
    explanation:
      'During autoregressive generation, each new token only needs to compute its own Q, K, V and attend to all previous positions. The KV cache stores the key and value projections from all previous tokens, avoiding redundant recomputation. Without it, generation would require reprocessing the entire sequence at each step, making it O(n^2) instead of O(n).',
    tags: ['text-generation', 'kv-cache', 'inference', 'efficiency'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-016',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Text Generation',
    question: 'What is the relationship between temperature, top-k, and top-p when used together during sampling?',
    options: [
      'Temperature is applied first to scale logits, then top-k and top-p filter the resulting distribution before sampling',
      'Only one of the three can be active at any time',
      'Top-k and top-p are applied before temperature scaling',
      'Temperature replaces both top-k and top-p when set to a non-default value',
    ],
    correctAnswer: 0,
    explanation:
      'The standard pipeline applies temperature scaling to the raw logits first, then applies top-k filtering (keep only k highest), then top-p filtering (keep smallest set with cumulative probability >= p), then normalizes and samples. This ordering ensures temperature affects which tokens pass the filters.',
    tags: ['text-generation', 'sampling', 'temperature', 'top-k', 'top-p'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-017',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Word Embeddings',
    question: 'What is the "analogy task" used to evaluate word embeddings, and what mathematical operation does it rely on?',
    options: [
      'It tests relationships like "king - man + woman = queen" using vector arithmetic in the embedding space',
      'It measures cosine similarity between random word pairs',
      'It checks whether embeddings can reconstruct the original text through decoding',
      'It uses principal component analysis to find the nearest analogous word',
    ],
    correctAnswer: 0,
    explanation:
      'The analogy task evaluates whether word embeddings capture semantic relationships through vector arithmetic. The classic example is: vec("king") - vec("man") + vec("woman") should be closest to vec("queen"). This works because directions in the embedding space encode semantic relationships.',
    tags: ['word-embeddings', 'evaluation', 'analogy', 'vector-arithmetic'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-018',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'BERT',
    question: 'How does RoBERTa improve upon the original BERT pre-training?',
    options: [
      'It removes NSP, uses dynamic masking, trains longer with more data and larger batches',
      'It uses a decoder-only architecture instead of encoder-only',
      'It replaces WordPiece with character-level tokenization',
      'It halves the number of layers and doubles the hidden dimension',
    ],
    correctAnswer: 0,
    explanation:
      'RoBERTa (Robustly Optimized BERT Approach) improves BERT by: removing the NSP objective, using dynamic masking (different mask per epoch instead of static), training with larger mini-batches, on more data, for longer. These changes significantly improve downstream task performance.',
    tags: ['bert', 'roberta', 'pre-training', 'optimization'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-019',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Transformer Architecture',
    question: 'What is the purpose of the "parallel attention and FFN" architecture used in models like PaLM?',
    options: [
      'It computes attention and FFN in parallel (rather than sequentially) and sums their outputs, improving training throughput',
      'It runs attention on one GPU and FFN on another for model parallelism',
      'It applies attention and FFN to different halves of the input',
      'It alternates between attention-only and FFN-only layers',
    ],
    correctAnswer: 0,
    explanation:
      'In the parallel formulation, the attention and FFN sublayers receive the same input and their outputs are summed (y = x + Attention(x) + FFN(x)), rather than the sequential formulation (y = FFN(x + Attention(x))). This enables fusing the two operations for ~15% training speedup with minimal quality impact.',
    tags: ['transformer', 'parallel-attention', 'palm', 'efficiency'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-020',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Long Context',
    question: 'What is Ring Attention and how does it enable training on extremely long sequences?',
    options: [
      'It distributes sequence chunks across devices in a ring topology, each computing local attention while passing KV blocks to neighbors',
      'It applies attention in a circular pattern where the last token attends to the first',
      'It uses a ring buffer to cache attention scores for reuse',
      'It compresses the sequence into a ring-shaped embedding',
    ],
    correctAnswer: 0,
    explanation:
      'Ring Attention distributes the input sequence across multiple devices, each holding a chunk. Devices are arranged in a ring and pass KV blocks to their neighbors while concurrently computing local attention. This overlaps communication with computation and allows processing sequences whose length scales linearly with the number of devices.',
    tags: ['long-context', 'ring-attention', 'distributed', 'scaling'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-021',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Mixture of Experts',
    question: 'What is "expert parallelism" in the context of training MoE models?',
    options: [
      'Placing different experts on different devices and using all-to-all communication to route tokens',
      'Training each expert on a different dataset',
      'Running multiple experts sequentially on the same device',
      'Using expert knowledge to manually parallelize the training code',
    ],
    correctAnswer: 0,
    explanation:
      'Expert parallelism distributes experts across different devices/GPUs. An all-to-all communication primitive is used to route each token to the device hosting its assigned expert(s) and return the results. This is the standard approach for scaling MoE models across many GPUs.',
    tags: ['moe', 'expert-parallelism', 'distributed-training'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-022',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Named Entity Recognition',
    question: 'Why can subword tokenization cause problems for token-level NER, and how is this typically handled?',
    options: [
      'Subword tokens split entity words into pieces; typically only the first subword token gets the entity label, and the rest are ignored during evaluation',
      'Subword tokenization is never used for NER tasks',
      'Each subword token receives the full entity label, and no special handling is needed',
      'NER models use character-level input exclusively to avoid this issue',
    ],
    correctAnswer: 0,
    explanation:
      'When a word is split into multiple subword tokens, we need a strategy for label alignment. The standard approach assigns the entity label only to the first subword token of each word, labeling continuation subtokens with a special ignore index (-100). During evaluation, predictions are mapped back to word-level labels.',
    tags: ['ner', 'subword-tokenization', 'label-alignment'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-023',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Machine Translation',
    question: 'What is the "exposure bias" problem in autoregressive machine translation?',
    options: [
      'During training the model sees ground-truth tokens as input, but at inference it conditions on its own (potentially erroneous) predictions',
      'The model is biased toward translating languages it was most exposed to during pre-training',
      'The attention mechanism is exposed to too many source tokens, causing diluted attention',
      'The decoder is exposed to the encoder output for too many layers',
    ],
    correctAnswer: 0,
    explanation:
      'Exposure bias arises from the mismatch between training (teacher forcing with ground-truth inputs) and inference (the model feeds its own predictions back as input). Errors at inference compound because the model encounters input distributions it never saw during training. Techniques like scheduled sampling and sequence-level training address this.',
    tags: ['machine-translation', 'exposure-bias', 'teacher-forcing'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-024',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Attention Mechanisms',
    question: 'What is linear attention and what computational advantage does it offer?',
    options: [
      'It replaces softmax(QK^T)V with phi(Q)(phi(K)^T V), enabling O(n) complexity by changing the computation order',
      'It uses only linear layers instead of attention, achieving O(1) complexity',
      'It linearizes the transformer by removing all nonlinear activations',
      'It processes the sequence linearly (left to right) without any attention',
    ],
    correctAnswer: 0,
    explanation:
      'Linear attention replaces the softmax with kernel feature maps phi, transforming softmax(QK^T)V into phi(Q)(phi(K)^T V). By computing phi(K)^T V first (an (d x d) matrix), the complexity drops from O(n^2 d) to O(n d^2), making it linear in sequence length. The tradeoff is typically reduced expressiveness.',
    tags: ['attention', 'linear-attention', 'efficiency', 'kernel'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-025',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'GPT',
    question: 'What is the difference between GPT\'s in-context learning and traditional fine-tuning?',
    options: [
      'In-context learning conditions on examples in the prompt at inference time without updating model weights, while fine-tuning updates weights on task data',
      'In-context learning fine-tunes only the last layer, while traditional fine-tuning updates all layers',
      'There is no difference; they are two names for the same technique',
      'In-context learning uses a separate retrieval model, while fine-tuning does not',
    ],
    correctAnswer: 0,
    explanation:
      'In-context learning (few-shot prompting) provides task demonstrations directly in the input prompt. The model adapts its behavior based on these examples at inference time without any gradient updates. Fine-tuning, in contrast, updates the model parameters on task-specific training data. In-context learning emerges in large GPT models without explicit training for it.',
    tags: ['gpt', 'in-context-learning', 'fine-tuning', 'few-shot'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-026',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Long Context',
    question: 'What is PagedAttention as used in vLLM for efficient inference serving?',
    options: [
      'It manages the KV cache using non-contiguous memory blocks (pages), enabling dynamic memory allocation and sharing across requests',
      'It splits the attention computation into pages of fixed token count',
      'It pages model weights to disk when GPU memory is full',
      'It divides the vocabulary into pages for more efficient token sampling',
    ],
    correctAnswer: 0,
    explanation:
      'PagedAttention, inspired by OS virtual memory paging, stores KV cache in non-contiguous memory blocks (pages). This eliminates memory fragmentation from pre-allocated contiguous buffers, enables memory sharing between requests (e.g., shared prefixes), and significantly improves GPU memory utilization in serving systems.',
    tags: ['long-context', 'paged-attention', 'vllm', 'inference-serving'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-027',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Transformer Architecture',
    question: 'What is the "reversal curse" observed in autoregressive language models?',
    options: [
      'Models trained on "A is B" fail to generalize to "B is A" without explicit exposure to the reversed statement',
      'Models perform worse when sequences are presented in reverse order during evaluation',
      'The final layers of the transformer contribute less than the initial layers',
      'Decoder-only models cannot be converted to encoder-decoder architectures',
    ],
    correctAnswer: 0,
    explanation:
      'The reversal curse refers to the finding that autoregressive LLMs trained on statements like "Valentina Tereshkova was the first woman in space" cannot reliably answer "Who was the first woman in space?" because they never saw the information with the answer entity in the completion position. This reveals a fundamental limitation in how these models store and access factual knowledge.',
    tags: ['transformer', 'reversal-curse', 'knowledge', 'limitations'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-028',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Text Classification',
    question: 'What is the advantage of using contrastive learning for text classification with transformers compared to standard fine-tuning?',
    options: [
      'It learns discriminative sentence representations by pulling similar examples together and pushing dissimilar ones apart, improving few-shot performance',
      'It eliminates the need for any labeled data',
      'It always achieves higher accuracy than supervised fine-tuning with full data',
      'It reduces the model size by half',
    ],
    correctAnswer: 0,
    explanation:
      'Contrastive learning (e.g., SimCSE, supervised contrastive fine-tuning) optimizes the embedding space so that semantically similar texts are closer together. This produces more discriminative representations that generalize better, especially in few-shot and data-scarce scenarios, compared to standard cross-entropy fine-tuning.',
    tags: ['text-classification', 'contrastive-learning', 'few-shot'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-029',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Mixture of Experts',
    question: 'What is "token dropping" in MoE training and why might it be used?',
    options: [
      'Tokens exceeding an expert\'s buffer capacity are dropped, enforcing strict load balance but losing some gradient signal',
      'Random tokens are dropped from the input as a regularization technique',
      'Expert networks randomly drop their outputs during training',
      'Tokens below a confidence threshold are removed before routing',
    ],
    correctAnswer: 0,
    explanation:
      'In MoE with fixed expert buffer capacities, tokens that would exceed the capacity of their assigned expert are dropped (not processed by any expert). This enforces hard load balancing and prevents memory overflow, but some tokens lose their expert processing. The capacity factor hyperparameter controls this tradeoff.',
    tags: ['moe', 'token-dropping', 'capacity-factor'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-030',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Long Context',
    question: 'What is the "lost in the middle" phenomenon observed in long-context language models?',
    options: [
      'Models perform well on information at the beginning and end of the context but poorly on information in the middle',
      'Tokens in the middle layers of the transformer lose their representations',
      'The middle segments of generated text are typically of lower quality',
      'Models lose track of conversation context in multi-turn dialogues',
    ],
    correctAnswer: 0,
    explanation:
      'Research has shown that LLMs exhibit a U-shaped performance curve: they reliably use information placed at the very beginning (primacy) or end (recency) of the context, but struggle to utilize information buried in the middle. This has practical implications for RAG systems and prompt engineering.',
    tags: ['long-context', 'lost-in-middle', 'position-bias'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-031',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Sequence Labeling',
    question: 'Why might a CRF (Conditional Random Field) layer be added on top of a Transformer for sequence labeling?',
    options: [
      'It models dependencies between adjacent labels, ensuring globally consistent label sequences (e.g., I-PER cannot follow B-LOC)',
      'It speeds up inference by avoiding the softmax computation',
      'It allows the model to handle longer sequences',
      'It replaces the need for the attention mechanism in the transformer',
    ],
    correctAnswer: 0,
    explanation:
      'A CRF layer on top of a Transformer captures transition probabilities between labels, enforcing structural constraints (e.g., B-PER must precede I-PER). While the Transformer provides powerful per-token representations, independent per-token classification may produce invalid label sequences. The CRF performs global inference via the Viterbi algorithm.',
    tags: ['sequence-labeling', 'crf', 'structured-prediction'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-032',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Text Generation',
    question: 'What is "contrastive decoding" and how does it improve text generation?',
    options: [
      'It subtracts the log-probabilities of a weak (amateur) model from a strong (expert) model to amplify tokens the expert prefers',
      'It contrasts the output with a reference translation to compute BLEU scores during generation',
      'It generates two sequences and selects the one with higher perplexity',
      'It uses contrastive loss during training to improve generation quality',
    ],
    correctAnswer: 0,
    explanation:
      'Contrastive decoding computes the difference between log-probabilities from a large expert model and a smaller amateur model. Tokens that the expert strongly favors over the amateur are boosted, reducing generic and repetitive text. This exploits the assumption that undesirable patterns (repetition, dullness) are shared by both models, while quality differences are highlighted by the contrast.',
    tags: ['text-generation', 'contrastive-decoding', 'inference'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l3-033',
    language: 'ai-engineer',
    level: 'level3',
    category: 'NLP & Transformers',
    subcategory: 'Positional Encoding',
    question: 'What is YaRN (Yet another RoPE extensioN) and what improvement does it offer for context length extension?',
    options: [
      'It combines NTK-aware interpolation with attention scaling and a temperature factor to extend context with minimal fine-tuning',
      'It replaces RoPE entirely with a learned positional embedding scheme',
      'It removes all positional encoding and relies on causal masking alone',
      'It uses a fixed sinusoidal encoding that works at any sequence length without modification',
    ],
    correctAnswer: 0,
    explanation:
      'YaRN extends RoPE-based models to longer contexts by combining NTK-aware interpolation (adjusting rotation frequencies) with attention scaling (a learned or fixed temperature on the attention logits). It achieves strong performance on extended contexts with only a small amount of fine-tuning data, outperforming simpler interpolation methods.',
    tags: ['positional-encoding', 'yarn', 'rope', 'context-extension'],
  },
];

export default questions;
