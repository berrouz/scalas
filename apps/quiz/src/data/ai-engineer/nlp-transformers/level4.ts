import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ai-engineer-nlp-transformers-l4-001',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Tokenization',
    question: 'What is the "fertility" problem in BPE tokenization for multilingual models, and how does it affect cross-lingual performance?',
    options: [
      'BPE tokenization produces identical token counts across all languages',
      'Fertility only affects character-level tokenizers, not BPE',
      'Fertility is about the number of languages a tokenizer supports',
      'Fertility refers to the number of tokens generated per word; high-resource languages get shorter tokenizations (fewer tokens per word) while low-resource languages are over-segmented, consuming more of the context window and receiving less training signal per concept',
    ],
    correctAnswer: 3,
    explanation: 'BPE merges are learned from corpus frequency. English "information" → 1 token, but Swahili equivalent might become 5 tokens. This means: (1) the same concept uses 5x context budget, (2) the model sees 5x fewer Swahili concepts per sequence, (3) positional encodings spread across subwords instead of semantic units. Mitigation: language-balanced tokenizer training data, larger vocabulary, or language-specific tokenizers with adapters.',
    tags: ['bpe', 'tokenization', 'multilingual', 'fertility'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-002',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Attention Analysis',
    question: 'What are "attention sinks" in autoregressive transformers and why do they occur?',
    options: [
      'Attention sinks cause transformers to lose information',
      'Attention sinks only occur in encoder models, not decoders',
      'Attention sinks are attention heads that perform poorly',
      'Attention sinks are specific tokens (typically the first token or special tokens) that receive disproportionately high attention from many heads regardless of content, because softmax attention must sum to 1 and these tokens serve as "no-op" targets when a head has no relevant key to attend to',
    ],
    correctAnswer: 3,
    explanation: 'Xiao et al. (2023) showed that the first token in autoregressive LLMs receives massive attention from subsequent tokens across many heads/layers. This happens because softmax forces attention weights to sum to 1 — when no key is semantically relevant, the model "dumps" attention on a fixed token. StreamingLLM leverages this: keeping the first few "sink" tokens plus a sliding window of recent tokens enables infinite-length inference with bounded KV cache.',
    tags: ['attention-sinks', 'softmax', 'streaming-llm', 'kv-cache'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-003',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Long Context',
    question: 'What is Ring Attention and how does it enable training on sequences of millions of tokens?',
    options: [
      'Ring Attention distributes sequence chunks across GPUs in a ring topology, where each GPU computes attention for its local chunk while receiving KV blocks from the previous GPU and sending to the next. By overlapping computation with communication, the full causal attention is computed without any single GPU holding the entire sequence',
      'Ring Attention uses circular positional encodings for infinite context',
      'Ring Attention approximates attention with random sampling',
      'Ring Attention is limited to sequences of 8K tokens per GPU',
    ],
    correctAnswer: 0,
    explanation: 'Ring Attention (Liu et al. 2023): N GPUs each hold seq_len/N tokens. In each step, each GPU computes attention between its Q block and the current K,V block, then passes KV to the next GPU in the ring. After N steps, each GPU has attended to all KV pairs. The key insight: computation (matmul) overlaps with communication (sending KV blocks), hiding communication latency. Memory per GPU: O(seq_len/N), enabling million-token sequences.',
    tags: ['ring-attention', 'long-context', 'distributed', 'sequence-parallelism'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-004',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Sparse Attention',
    question: 'What are the key sparse attention patterns and their trade-offs in handling long sequences?',
    options: [
      'Local/sliding window (each token attends to k neighbors: O(Nk)), global tokens (selected tokens attend to all: capturing long-range), strided/dilated (attend every k-th token: efficient long-range). Longformer combines local + global; BigBird adds random edges. Trade-off: each pattern sacrifices certain dependency types — local misses long-range, strided misses adjacent context, requiring careful combination',
      'Sparse attention always produces the same results as full attention',
      'All sparse attention methods use the same pattern',
      'Sparse attention is only useful for sequences shorter than 512 tokens',
    ],
    correctAnswer: 0,
    explanation: 'Full attention O(N²) is impractical for long sequences. Sliding window (Mistral): each token sees ±w neighbors, O(Nw). Information propagates log_w(N) layers to cross the sequence. Global tokens (CLS, start tokens) in Longformer: O(Ng) additional. BigBird proves that random + local + global attention is a universal approximator. Trade-offs: local alone fails for tasks needing global reasoning; global tokens bottleneck all long-range info through few tokens.',
    tags: ['sparse-attention', 'longformer', 'bigbird', 'sliding-window'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-005',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Pre-training',
    question: 'What is the difference between causal language modeling (CLM) and prefix language modeling, and why does prefix LM benefit certain tasks?',
    options: [
      'Prefix LM requires separate encoder and decoder models',
      'Prefix LM cannot generate text, only classify',
      'CLM and prefix LM use identical attention masks',
      'CLM uses strictly causal (left-to-right) attention for all tokens. Prefix LM uses bidirectional attention for the input prefix (like an encoder) and causal attention for the continuation (like a decoder). This allows the model to fully understand the input context bidirectionally while generating output autoregressively, benefiting conditional generation tasks',
    ],
    correctAnswer: 3,
    explanation: 'In CLM (GPT), token i can only attend to tokens 1..i, even within the prompt. This means early prompt tokens don\'t benefit from later prompt context. Prefix LM (used in U-PaLM, some T5 variants): tokens within the prefix attend bidirectionally to each other, then generation tokens use causal attention. The attention mask has a dense block for the prefix and a triangular block for generation. This improves conditioning on complex inputs without needing a separate encoder.',
    tags: ['prefix-lm', 'causal-lm', 'attention-mask', 'pre-training'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-006',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Mixture of Experts',
    question: 'What is the expert choice routing strategy and how does it solve the load balancing problem in MoE transformers?',
    options: [
      'Expert choice routing is identical to top-k token-choice routing',
      'Expert choice routing requires more experts than tokens in the batch',
      'Expert choice lets experts choose which tokens to process',
      'Instead of tokens choosing experts (token-choice), expert choice routing has each expert select its top-k tokens from the batch. This guarantees perfect load balance by construction since each expert processes exactly k tokens, eliminates the need for auxiliary balancing losses, and enables variable compute per token (important tokens are selected by more experts)',
    ],
    correctAnswer: 3,
    explanation: 'Token-choice (Switch, Mixtral): each token routes to top-k experts → uneven load, dropped tokens. Expert-choice (Zhou et al. 2022): each expert picks its top-k tokens from the entire batch → each expert processes exactly k tokens (perfect balance). Benefits: no auxiliary loss needed, no dropped tokens, variable expert-per-token (some tokens chosen by 0 experts, others by many). Challenge: requires batch-level routing decisions, less compatible with autoregressive caching.',
    tags: ['expert-choice', 'moe', 'load-balancing', 'routing'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-007',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Emergent Abilities',
    question: 'What is the debate around "emergent abilities" in large language models?',
    options: [
      'Emergent abilities prove that larger models are always better',
      'Some benchmarks show abilities appearing suddenly at certain scales, but Schaeffer et al. (2023) argued this is partly an artifact of using non-linear metrics (exact-match accuracy): smooth underlying improvement appears as sharp transitions when measured with threshold metrics. Using continuous metrics like log-likelihood shows gradual improvement, though some capabilities do qualitatively change with scale',
      'Emergent abilities are proven to appear suddenly at specific model scales',
      'Emergent abilities only appear in models with more than 1 trillion parameters',
    ],
    correctAnswer: 1,
    explanation: 'The debate: Wei et al. (2022) documented abilities that appear to "emerge" suddenly past certain scales. Schaeffer et al. (2023) showed that measuring performance with metrics like exact-match accuracy (0 or 1) on discrete tasks creates apparent phase transitions from gradually improving token-level probabilities. A model improving from 45% to 55% per-token accuracy might jump from 0% to 80% on a multi-token exact-match task. However, some capabilities (multi-step reasoning) do require minimum scale.',
    tags: ['emergent-abilities', 'scaling', 'metrics', 'evaluation'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-008',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Mechanistic Interpretability',
    question: 'What are induction heads in transformers and why are they considered a key mechanism for in-context learning?',
    options: [
      'Induction heads are two-head circuits where one head copies previous token positions to current positions (previous-token head), and a second head uses this to attend to the token following previous occurrences of the current token, enabling the transformer to complete patterns like "A B ... A" → "B". They form during a phase transition in training and are central to in-context learning',
      'Induction heads are attention heads that process the first token',
      'Induction heads are manually programmed into transformer architectures',
      'Induction heads only exist in models with more than 100 layers',
    ],
    correctAnswer: 0,
    explanation: 'Olsson et al. (2022): induction heads are a circuit of 2+ attention heads. Head 1 (previous-token head) shifts position info by one. Head 2 uses the shifted info to implement the pattern: "find where the current token appeared before, then predict what came after it." This emerges during a specific training phase transition correlated with a sharp decrease in in-context learning loss. Ablating induction heads dramatically hurts in-context learning while barely affecting zero-shot performance.',
    tags: ['induction-heads', 'mechanistic-interpretability', 'in-context-learning', 'circuits'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-009',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Efficient Transformers',
    question: 'What is linear attention and what capability does it lose compared to standard softmax attention?',
    options: [
      'Linear attention is identical to softmax attention but faster',
      'Linear attention uses linear activation functions in all layers',
      'Linear attention can only process sequences up to 1024 tokens',
      'Linear attention replaces softmax(QK^T)V with φ(Q)(φ(K)^T V), using the associativity of matrix multiplication to compute in O(N·d²) instead of O(N²·d). However, it loses the sharp, selective attention patterns that softmax creates — softmax can concentrate attention on a few tokens while linear attention spreads attention more uniformly, hurting tasks requiring precise retrieval',
    ],
    correctAnswer: 3,
    explanation: 'Standard: (softmax(QK^T))V ∈ R^{N×d} requires materializing the N×N attention matrix. Linear: φ(Q)(φ(K)^T V) — first compute φ(K)^T V ∈ R^{d×d} (d² cost), then multiply by φ(Q) ∈ R^{N×d} → O(N·d²). The d×d "summary matrix" compresses all key-value information, losing the ability to attend precisely to individual tokens. Softmax\'s exp() creates peaky distributions; linear kernels create flatter attention. Hybrid approaches (local softmax + global linear) balance efficiency and precision.',
    tags: ['linear-attention', 'efficient-transformers', 'kernel-approximation', 'complexity'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-010',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Tokenization',
    question: 'What problem does byte-level tokenization (e.g., ByT5) solve and what is its computational trade-off?',
    options: [
      'Byte-level tokenization produces shorter sequences than BPE',
      'Byte-level tokenization only works for English text',
      'Byte-level tokenization operates on raw UTF-8 bytes, eliminating the need for a learned vocabulary, handling any language/script without OOV tokens, and being robust to typos and character-level noise. The trade-off: sequences are 3-5x longer than BPE, requiring 3-5x more compute for attention, though ByT5 mitigates this with a lightweight encoder that downsamples the byte sequence',
      'Byte-level tokenization requires a larger vocabulary than BPE',
    ],
    correctAnswer: 2,
    explanation: 'BPE/SentencePiece have fixed vocabularies and struggle with unseen scripts, code-switching, typos, and morphologically rich languages. Byte-level: vocabulary of 256 bytes, any UTF-8 text is directly encodable. "café" = 5 bytes vs 1-2 BPE tokens. ByT5 uses a deeper encoder with local attention + downsampling to reduce the sequence length before the full transformer, managing the compute cost. MegaByte uses patches of bytes to amortize the overhead.',
    tags: ['byte-tokenization', 'byt5', 'vocabulary-free', 'multilingual'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-011',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Position Encoding',
    question: 'What is ALiBi (Attention with Linear Biases) and how does it handle extrapolation to longer sequences?',
    options: [
      'ALiBi requires retraining when the sequence length changes',
      'ALiBi adds learnable position embeddings to key vectors',
      'ALiBi can only handle sequences shorter than training length',
      'ALiBi adds a static linear bias to attention scores: score(i,j) = q_i·k_j - m·|i-j|, where m is a head-specific slope. This penalizes distant tokens linearly without any learned position parameters. Because the bias is relative and has no periodicity, ALiBi extrapolates to sequences longer than training length more naturally than sinusoidal or learned absolute position encodings',
    ],
    correctAnswer: 3,
    explanation: 'ALiBi (Press et al. 2022): no position embeddings at all. Instead, attention_score[i][j] -= m * |i - j| where m varies per head geometrically (e.g., 1/2, 1/4, 1/8...). Heads with small m attend broadly; heads with large m focus locally. The linear penalty has no periodicity or maximum position, so it naturally extends to any length. Experiments showed ALiBi trained on 1K tokens can perform well on 2-4K tokens without fine-tuning, though modern RoPE + interpolation has become more prevalent.',
    tags: ['alibi', 'position-encoding', 'extrapolation', 'attention-bias'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-012',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Retrieval-Augmented',
    question: 'What is RETRO (Retrieval-Enhanced Transformer) and how does it integrate retrieval into the transformer architecture?',
    options: [
      'RETRO splits input into chunks, retrieves nearest neighbors from a frozen database for each chunk, and uses cross-attention layers (chunked cross-attention) to attend to retrieved passages. This architecture integrates retrieval at the architecture level rather than input level, reducing the model size needed for a given perplexity by ~10x compared to a retrieval-free model of equal perplexity',
      'RETRO only retrieves single tokens, not passages',
      'RETRO simply concatenates retrieved documents to the input prompt',
      'RETRO requires retraining the retrieval index with each model update',
    ],
    correctAnswer: 0,
    explanation: 'RETRO (Borgeaud et al. 2022): (1) split input into L-token chunks, (2) each chunk queries a BERT-based retrieval index (2T tokens, frozen), (3) retrieved passages are encoded by a separate encoder, (4) specialized CCA (Chunked Cross-Attention) layers in the main transformer attend to the retrieved content. This is more efficient than prepending documents to the context because: retrieved content only enters at CCA layers, not all self-attention layers. A 7.5B RETRO matches a 175B GPT-3.',
    tags: ['retro', 'retrieval-augmented', 'cross-attention', 'knowledge-base'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-013',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Training Dynamics',
    question: 'What is the "grokking" phenomenon in transformer training and what does it reveal about generalization?',
    options: [
      'Grokking only occurs in very large models (>100B parameters)',
      'Grokking means the model cannot learn the task at all',
      'Grokking means the model immediately generalizes after seeing any data',
      'Grokking is the phenomenon where a model first memorizes the training set (zero training loss, random test performance), then after prolonged training far past overfitting, suddenly generalizes (test accuracy jumps to near-perfect). This suggests that generalization circuits are formed slowly through weight regularization even after memorization circuits have converged',
    ],
    correctAnswer: 3,
    explanation: 'Power et al. (2022) showed transformers trained on modular arithmetic first memorize (train acc=100%, test acc~random), then after 10-100x more training steps, suddenly generalize (test acc→100%). Mechanistic analysis reveals: memorization uses lookup-table-like weights, while generalization uses Fourier-based algorithms in the weights. Weight decay is critical — without it, grokking doesn\'t occur. This challenges the assumption that training past overfitting is wasteful.',
    tags: ['grokking', 'generalization', 'memorization', 'training-dynamics'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-014',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Representation',
    question: 'What is the "anisotropy" problem in transformer embeddings and how does it affect similarity search?',
    options: [
      'Anisotropy only occurs in BERT, not in GPT models',
      'Transformer embeddings tend to occupy a narrow cone in the embedding space (high cosine similarity between random pairs), making cosine similarity less discriminative. This happens because the embedding distribution has high mean and low variance in certain directions. Mitigation: whitening transforms, contrastive fine-tuning (SimCSE), or normalizing embeddings to remove the common mean direction',
      'Anisotropy is beneficial for all downstream tasks',
      'Anisotropy means embeddings are uniformly distributed in all directions',
    ],
    correctAnswer: 1,
    explanation: 'Ethayarajh (2019) showed BERT embeddings have high anisotropy: random word pairs have cosine similarity ~0.6-0.9 instead of ~0. This compresses the effective similarity range, making it hard to distinguish similar from dissimilar items. Causes: dominant singular vectors in the embedding matrix, frequency-based token biases. SimCSE (Gao et al. 2021) addresses this by contrastive learning that spreads embeddings uniformly on the hypersphere, dramatically improving semantic similarity tasks.',
    tags: ['anisotropy', 'embeddings', 'cosine-similarity', 'simcse'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-015',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Architecture Variants',
    question: 'What is Hyena and how does it replace attention with convolution-based operators?',
    options: [
      'Hyena replaces attention with a cascade of element-wise gating and long convolutions parameterized implicitly via a small neural network (FFN) that generates convolution filters of arbitrary length. This achieves sub-quadratic O(N log N) complexity via FFT-based convolution while matching attention quality on language tasks up to moderate context lengths',
      'Hyena can only process sequences shorter than 1024 tokens',
      'Hyena requires O(N²) memory like standard attention',
      'Hyena uses standard 1D convolutions with fixed kernels',
    ],
    correctAnswer: 0,
    explanation: 'Hyena (Poli et al. 2023): replaces the attention matrix A(Q,K) with a sequence of operations: element-wise gating (data-dependent) interleaved with long convolutions. The convolution filters are generated by a small MLP applied to positional encodings, enabling arbitrary-length filters without storing them explicitly. Convolution is computed via FFT: O(N log N). Hyena achieves comparable perplexity to attention transformers on sequences up to ~8K tokens, with clear wins on very long sequences.',
    tags: ['hyena', 'long-convolution', 'sub-quadratic', 'implicit-filter'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-016',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Decoding Strategies',
    question: 'What is contrastive decoding and why does it produce higher quality text than standard sampling?',
    options: [
      'Contrastive decoding always produces the same output as greedy decoding',
      'Contrastive decoding compares the logits of a large (expert) and small (amateur) model, preferring tokens where the expert is much more confident than the amateur: score = log p_expert(x) - log p_amateur(x). This amplifies tokens that require the expert\'s knowledge while filtering common/generic tokens that even the amateur predicts well, reducing repetition and improving coherence',
      'Contrastive decoding uses contrastive learning during training',
      'Contrastive decoding requires training a special discriminator model',
    ],
    correctAnswer: 1,
    explanation: 'Li et al. (2023): contrastive decoding selects tokens that maximize log p_L(x|context) - α·log p_S(x|context), where L is the large model and S is a smaller model. Tokens scoring high on this metric are those where model capability matters (coherent reasoning, factual knowledge), not where both models agree (common function words). An adaptive threshold filters out tokens where the expert probability is too low. This reduces degenerate repetition and hallucination without temperature tuning.',
    tags: ['contrastive-decoding', 'sampling', 'text-generation', 'quality'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-017',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Training Efficiency',
    question: 'What is the "curriculum learning" approach for pre-training LLMs and does data ordering matter?',
    options: [
      'Curriculum learning always improves pre-training by at least 20%',
      'Data ordering has no effect on LLM pre-training',
      'Curriculum learning orders training data from easy to hard (e.g., short sentences → complex documents, high-quality → noisy data). Research shows mixed results: simple curricula rarely help for pre-training (random shuffling is strong baseline), but domain-mixing schedules (varying the proportion of code, web, books over training) significantly impact final performance. DoReMi and online data mixing optimize this dynamically',
      'Curriculum learning only works for fine-tuning, never for pre-training',
    ],
    correctAnswer: 2,
    explanation: 'Classic easy-to-hard curriculum often doesn\'t help LLM pre-training because: (1) the model needs diverse data throughout, (2) defining "difficulty" for language is ambiguous. However, the proportion of different data sources (web, books, code, scientific papers) over training significantly matters. DoReMi (Xie et al. 2023) trains a small proxy model to optimize domain weights that minimize worst-case loss, then uses these weights for the large model. D4 and online data mixing similarly optimize data composition dynamically.',
    tags: ['curriculum-learning', 'data-mixing', 'doremi', 'pre-training'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-018',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Knowledge Editing',
    question: 'What is ROME (Rank-One Model Editing) and how does it update factual knowledge in a transformer without retraining?',
    options: [
      'ROME fine-tunes the entire model on corrected facts',
      'ROME identifies the specific MLP layer where a fact is stored (using causal tracing), then performs a rank-one update to the MLP weight matrix that changes the output for the target subject while minimizing disruption to other facts. It treats the MLP as a key-value store where keys are subject representations and values are attribute representations',
      'ROME requires retraining the layer from scratch with corrected data',
      'ROME can only edit facts about named entities',
    ],
    correctAnswer: 1,
    explanation: 'ROME (Meng et al. 2022): (1) Causal tracing identifies that middle MLP layers are critical for factual recall — corrupting subject tokens and restoring at specific layers reveals the "factual hotspot." (2) The MLP\'s W_out matrix is viewed as a linear associative memory: k → v mapping. (3) A rank-one update ΔW = (v_new - v_old)·k^T / (k^T·k) inserts the new fact. MEMIT extends this to editing multiple facts simultaneously across multiple layers.',
    tags: ['rome', 'knowledge-editing', 'model-editing', 'factual-knowledge'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-019',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Structured Prediction',
    question: 'What is the advantage of using CRF (Conditional Random Field) layers on top of transformer encoders for sequence labeling tasks like NER?',
    options: [
      'CRF layers make the transformer encode longer sequences',
      'CRF layers are only useful for non-transformer models like LSTMs',
      'CRF layers replace the transformer\'s attention mechanism',
      'A CRF layer models transition probabilities between adjacent labels, enforcing structural constraints (e.g., I-PER cannot follow B-ORG) that independent per-token classification cannot capture. The transformer provides rich contextual features while the CRF ensures globally consistent label sequences via the Viterbi algorithm for decoding',
    ],
    correctAnswer: 3,
    explanation: 'Independent softmax per token can produce invalid label sequences (e.g., I-PER following O). A CRF models P(y|x) ∝ Π exp(emission_t(y_t) + transition(y_{t-1}, y_t)), where emission scores come from the transformer and transition scores are learned. The partition function is computed via forward algorithm (O(T·C²)), and Viterbi decoding finds the highest-scoring valid sequence. This improves F1 by 1-3% on NER benchmarks over independent classification, with the gap larger for complex label schemes.',
    tags: ['crf', 'sequence-labeling', 'ner', 'structured-prediction'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-020',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Multi-Task',
    question: 'What is instruction tuning and how does it differ from standard supervised fine-tuning?',
    options: [
      'Instruction tuning fine-tunes a pre-trained LM on a diverse collection of tasks formatted as natural language instructions (prompt + completion). Unlike task-specific fine-tuning which trains separate models per task, instruction tuning trains one model to follow arbitrary instructions, enabling zero-shot generalization to unseen task types. The diversity and quality of instruction data is more important than quantity',
      'Instruction tuning only works for classification tasks',
      'Instruction tuning requires millions of examples per task',
      'Instruction tuning and supervised fine-tuning are identical',
    ],
    correctAnswer: 0,
    explanation: 'FLAN (Wei et al. 2022), InstructGPT: fine-tune on thousands of task templates as (instruction, response) pairs. The key insight: seeing enough task diversity teaches the model to follow the instruction format itself, not just solve specific tasks. Quality matters: LIMA showed 1000 carefully curated examples can match models trained on millions. Self-instruct and Alpaca automate instruction generation. Instruction tuning bridges the gap between pre-training (next-token prediction) and user intent following.',
    tags: ['instruction-tuning', 'flan', 'zero-shot', 'generalization'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-021',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Hallucination',
    question: 'What is the difference between intrinsic and extrinsic hallucination in language models, and which is harder to detect?',
    options: [
      'Intrinsic hallucination contradicts the provided source text (e.g., summarization that changes a fact from the input). Extrinsic hallucination generates information not present in any provided source, which may or may not be factually correct. Extrinsic hallucination is harder to detect because it requires world knowledge verification, while intrinsic hallucination can be caught by comparing output to input via entailment models',
      'Intrinsic hallucination only occurs in decoder-only models',
      'Extrinsic hallucination is always factually incorrect',
      'All hallucinations are equally easy to detect',
    ],
    correctAnswer: 0,
    explanation: 'Intrinsic: summary says "born in 1985" when source says "born in 1983" — detectable by NLI model comparing summary to source (entailment check). Extrinsic: model adds "she graduated from MIT" when the source doesn\'t mention education — may be true or false, requires external knowledge to verify. Detection: intrinsic → faithfulness metrics (BERTScore, Q²); extrinsic → knowledge grounding (retrieve + verify). Mitigation: constrained decoding, retrieval augmentation, self-consistency checking.',
    tags: ['hallucination', 'intrinsic', 'extrinsic', 'faithfulness'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-022',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Efficient Pre-training',
    question: 'What is the "UL2" (Unifying Language Learning) pre-training paradigm and why does it outperform pure causal or pure masked LM?',
    options: [
      'UL2 unifies three denoising objectives with different corruption strategies: regular span corruption (like T5), extreme span corruption (long spans for summarization-like learning), and prefix LM (for in-context learning). A sentinel token signals which "mode" is active, allowing one model to learn skills that each individual objective provides, resulting in stronger performance across diverse tasks',
      'UL2 requires 3x more compute than standard pre-training',
      'UL2 only works with encoder-decoder architectures',
      'UL2 simply alternates between BERT and GPT training objectives',
    ],
    correctAnswer: 0,
    explanation: 'UL2 (Tay et al. 2022): three mixture-of-denoisers: (1) R-denoiser: ~15% short spans masked (like T5, good for NLU), (2) S-denoiser: ~50% sequential prefix preserved (like prefix LM, good for generation), (3) X-denoiser: extreme corruption with long spans (good for long-range reasoning). A special token [R], [S], or [X] prepended to each example signals the mode. The model learns to handle all denoising patterns, outperforming single-objective models on both NLU and NLG benchmarks.',
    tags: ['ul2', 'pre-training', 'denoising', 'unified-objective'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-023',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Activation Memory',
    question: 'Why does the KV cache grow linearly with sequence length during autoregressive inference, and what are the approaches to bound it?',
    options: [
      'The KV cache only stores the last 10 tokens',
      'Each generated token requires storing its key and value vectors across all layers and heads (2 × layers × heads × d_head bytes per token). For a 32-layer, 32-head model with d_head=128 in FP16, this is 512KB per token. Approaches to bound it: sliding window attention (Mistral: fixed window), H2O (evict low-attention KV pairs), quantized KV cache (INT8/INT4), and paged KV cache management (vLLM)',
      'KV cache size is constant regardless of sequence length',
      'KV cache is eliminated by using Flash Attention',
    ],
    correctAnswer: 1,
    explanation: 'KV cache per token = 2 (K and V) × L layers × H heads × d_head × dtype_bytes. For LLaMA-70B (80 layers, 8 KV heads with GQA, d=128, FP16): 2 × 80 × 8 × 128 × 2 = 327KB per token. At 100K context: ~32GB just for KV cache. Solutions: (1) GQA/MQA reduce heads, (2) sliding window limits stored tokens, (3) H2O/ScissorHands evict unimportant KV pairs, (4) KV cache quantization to INT4 (4x reduction), (5) vLLM\'s PagedAttention manages KV as virtual memory pages to eliminate fragmentation.',
    tags: ['kv-cache', 'inference-memory', 'paged-attention', 'vllm'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-024',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Reasoning',
    question: 'What is chain-of-thought prompting\'s underlying mechanism and when does it fail?',
    options: [
      'Chain-of-thought always improves performance on all tasks',
      'Chain-of-thought (CoT) prompting elicits intermediate reasoning steps, allowing the model to decompose complex problems. It works by using the serial computation of token-by-token generation to perform iterative computation that a single forward pass cannot. CoT fails when: the model lacks the knowledge needed for reasoning steps, the task requires non-verbal reasoning, or the chain introduces compounding errors in multi-step logic',
      'CoT is only effective for math word problems',
      'CoT works by accessing an external database of reasoning chains',
    ],
    correctAnswer: 1,
    explanation: 'A single transformer forward pass has limited computational depth (number of layers). CoT converts "depth" requirements into "length" — each generated token adds another forward pass of computation. This enables multi-step reasoning beyond the model\'s fixed depth. Failures: (1) knowledge gap — the model generates plausible but incorrect reasoning steps, (2) unfaithful CoT — the stated reasoning doesn\'t match the model\'s actual computation path, (3) compounding errors — each wrong step makes subsequent steps more likely wrong. Verification and self-consistency help.',
    tags: ['chain-of-thought', 'reasoning', 'prompting', 'failure-modes'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-025',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Embeddings',
    question: 'What is Matryoshka Representation Learning (MRL) and why is it useful for production embedding systems?',
    options: [
      'MRL only works with sentence-level embeddings, not token-level',
      'MRL requires separate models for each embedding dimension',
      'MRL trains embeddings such that the first d dimensions (for any d) form a valid lower-dimensional embedding, by applying the loss function at multiple truncation points simultaneously. This allows dynamically choosing embedding dimensionality at inference time based on accuracy/cost trade-offs, using the same model — e.g., 64-dim for fast recall, full 768-dim for re-ranking',
      'MRL creates nested dolls of different model sizes',
    ],
    correctAnswer: 2,
    explanation: 'MRL (Kusupati et al. 2022): during training, apply contrastive/classification loss simultaneously on the first 8, 16, 32, 64, 128, ..., d dimensions of the embedding. This forces the model to encode the most important information in the first dimensions. At serving time, truncating to 64 dims retains ~95% of full-dimension retrieval quality while reducing storage and computation by 12x (for 768-dim base). OpenAI\'s text-embedding-3 uses this approach, exposing a "dimensions" parameter.',
    tags: ['matryoshka', 'embeddings', 'dimensionality', 'retrieval'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-026',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Data Deduplication',
    question: 'Why is training data deduplication critical for LLM pre-training and what methods are used at trillion-token scale?',
    options: [
      'Modern LLMs are robust to duplicate data and don\'t need deduplication',
      'Duplicate data causes the model to memorize specific sequences (increasing verbatim regurgitation risk), wastes training compute on redundant information, and degrades benchmark performance. At scale, MinHash with LSH identifies near-duplicate documents in O(N) time, suffix arrays find duplicate substrings, and semantic deduplication uses embeddings to find paraphrases. Deduplication can improve perplexity by 5-10% with fewer tokens',
      'Deduplication only removes exact copies and has minimal impact',
      'Deduplication is only needed for fine-tuning data, not pre-training',
    ],
    correctAnswer: 1,
    explanation: 'Lee et al. (2022) showed that deduplicating C4 improved LM perplexity by ~10% and reduced memorization (measured by extractable training sequences). Methods: (1) exact dedup: hash-based, O(N) but misses near-duplicates, (2) MinHash + LSH: estimates Jaccard similarity, identifies near-duplicate documents in ~O(N), (3) suffix array: finds all duplicate substrings of length ≥ k, (4) SemDeDup: clusters embeddings, removes near-cluster-center duplicates. The Pile, RefinedWeb, and Dolma all use multi-stage dedup pipelines.',
    tags: ['deduplication', 'pre-training', 'minhash', 'data-quality'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-027',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Alignment',
    question: 'What is Constitutional AI (CAI) and how does it reduce the need for human feedback in alignment?',
    options: [
      'CAI uses legal constitutions to train language models',
      'CAI only works for safety-related behaviors, not helpfulness',
      'CAI provides the model with a set of principles (the "constitution") and uses the model itself to generate, critique, and revise responses according to these principles. This self-improvement loop generates preference pairs for RLHF/DPO training without human labelers judging each output, scaling alignment to cover more scenarios while maintaining consistency with the stated principles',
      'CAI eliminates the need for any training data',
    ],
    correctAnswer: 2,
    explanation: 'CAI (Bai et al. 2022): (1) Red-team: generate harmful prompts, (2) Initial response: model answers, (3) Critique: model critiques its own response against constitutional principles ("Is this response harmful?"), (4) Revision: model rewrites to fix issues, (5) Use (original, revised) as preference pair for RLAIF (RL from AI Feedback). This scales beyond human feedback by automating preference generation while keeping behaviors grounded in explicit, auditable principles.',
    tags: ['constitutional-ai', 'alignment', 'rlaif', 'self-improvement'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-028',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Efficient Inference',
    question: 'What is continuous batching (iteration-level scheduling) and why does it improve LLM serving throughput?',
    options: [
      'Continuous batching requires dedicated hardware accelerators',
      'Instead of waiting for the longest sequence in a batch to finish (static batching), continuous batching inserts new requests into the batch as soon as any request completes a generation. This eliminates padding waste from length-mismatched requests and keeps the GPU saturated. vLLM and TensorRT-LLM implement this, achieving 2-4x higher throughput than static batching',
      'Continuous batching reduces throughput but improves latency',
      'Continuous batching sends all requests to the model at once',
    ],
    correctAnswer: 1,
    explanation: 'Static batching: batch of [10, 50, 200] tokens → all must wait for the 200-token request. GPU is idle for short requests after they complete. Continuous (in-flight) batching: as the 10-token request finishes, immediately schedule a new request in its slot. Each iteration, the batch may contain requests at different generation stages. This requires per-request KV cache management (PagedAttention) but maximizes GPU utilization. Orca (Yu et al. 2022) pioneered this, and it\'s now standard in production serving frameworks.',
    tags: ['continuous-batching', 'inference-serving', 'throughput', 'vllm'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-029',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Multilingual',
    question: 'What is the "curse of multilinguality" and how does language-specific adapter training mitigate it?',
    options: [
      'Adding more languages to a fixed-capacity model degrades per-language performance (capacity dilution) since languages compete for the same parameters. Language-specific adapters add small per-language modules (adapter layers or LoRA) while sharing the multilingual backbone, allowing specialization without interference. This achieves near-monolingual performance for each language with minimal additional parameters',
      'The curse only affects languages with non-Latin scripts',
      'Increasing model size always completely solves the curse of multilinguality',
      'The curse means multilingual models cannot process any language well',
    ],
    correctAnswer: 0,
    explanation: 'Conneau et al. (2020) showed mBERT/XLM-R performance per language decreases as more languages are added to a fixed-size model — positive transfer from related languages is offset by capacity dilution. MAD-X, AdapterFusion: add small adapter modules (~2-5% params) per language into the frozen multilingual model. Each adapter specializes for its language while the backbone provides cross-lingual transfer. This is more parameter-efficient than training separate models per language.',
    tags: ['multilingual', 'curse-of-multilinguality', 'adapters', 'capacity-dilution'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-030',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Architecture',
    question: 'What is the difference between Pre-LayerNorm and Post-LayerNorm transformer blocks, and why has Pre-LN become standard?',
    options: [
      'Pre-LN eliminates the need for residual connections',
      'Pre-LN applies normalization before each sub-layer (attention/FFN), while Post-LN applies it after the residual connection. Pre-LN has better-behaved gradients at initialization (gradient norm is approximately constant across layers), enabling stable training without learning rate warmup. Post-LN can achieve slightly better final performance but requires careful warmup and initialization to avoid training instability',
      'Pre-LN and Post-LN produce identical gradients',
      'Post-LN is always superior and used in all modern architectures',
    ],
    correctAnswer: 1,
    explanation: 'Post-LN (original transformer): x + SubLayer(LayerNorm(x)) — wait, actually Post-LN: LayerNorm(x + SubLayer(x)). Pre-LN: x + SubLayer(LayerNorm(x)). Xiong et al. (2020) showed Post-LN has gradient magnitude that grows with depth at initialization, requiring warmup. Pre-LN keeps gradients well-scaled at all depths, enabling stable training with larger learning rates from step 1. Most modern LLMs (GPT, LLaMA, Mistral) use Pre-LN. Some find Post-LN + careful init gives 1-2% better final quality.',
    tags: ['pre-layernorm', 'post-layernorm', 'training-stability', 'transformer-block'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-031',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Watermarking',
    question: 'How do statistical watermarking schemes for LLM-generated text work without modifying the model?',
    options: [
      'Watermarking requires modifying the model weights',
      'At each generation step, the scheme uses the previous tokens to seed a hash that partitions the vocabulary into "green" and "red" lists, then biases sampling toward green tokens. Detection checks if a text has statistically more green tokens than expected by chance. This is imperceptible to humans but detectable with a statistical z-test, even after moderate paraphrasing',
      'Watermarking adds invisible characters to the generated text',
      'Watermarking only works for texts longer than 10,000 tokens',
    ],
    correctAnswer: 1,
    explanation: 'Kirchenbauer et al. (2023): at position t, hash the previous k tokens → random seed → split vocab into green/red lists. Add δ to green token logits before sampling. Detection: given text and the secret key, compute the green-list fraction. Under null hypothesis (human text), green fraction ≈ 0.5. Under watermarked text, green fraction ≈ 0.75+. z-test detects this with high confidence even for 200+ token texts. Robustness varies: paraphrasing can reduce signal but doesn\'t eliminate it for sufficiently long texts.',
    tags: ['watermarking', 'detection', 'green-list', 'statistical-test'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-032',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Compression',
    question: 'What is AWQ (Activation-Aware Weight Quantization) and how does it improve upon naive round-to-nearest quantization?',
    options: [
      'AWQ quantizes activations instead of weights',
      'AWQ requires calibration data with millions of examples',
      'AWQ can only achieve 8-bit quantization, not 4-bit',
      'AWQ observes that a small fraction of weight channels are critical (corresponding to large activation magnitudes) and should be quantized with higher precision. It protects these salient channels by scaling them up before quantization and scaling activations down equivalently, preserving the mathematical result while keeping the important channels in a more quantization-friendly range',
    ],
    correctAnswer: 3,
    explanation: 'AWQ (Lin et al. 2023): observation — 1% of weight channels handle features with large activation magnitudes and are crucial for model quality. Naive quantization damages these equally. AWQ: for channel i, multiply weight by s_i and divide activation by s_i (no math change). Choose s_i to minimize quantization error: scale up important channels (those with large activations) to occupy more of the quantization grid. This simple per-channel scaling achieves 4-bit quantization with <0.5% accuracy loss on most LLMs.',
    tags: ['awq', 'quantization', 'activation-aware', 'compression'],
  },
  {
    id: 'ai-engineer-nlp-transformers-l4-033',
    language: 'ai-engineer',
    level: 'level4',
    category: 'NLP & Transformers',
    subcategory: 'Training Data',
    question: 'What is the "data contamination" problem in LLM evaluation and how can it be detected?',
    options: [
      'Data contamination only affects small models, not large ones',
      'Data contamination occurs when benchmark test examples are present in the pre-training corpus, inflating evaluation scores. Detection methods include: n-gram overlap analysis between training data and benchmarks, perplexity-based detection (contaminated examples have abnormally low perplexity), and canary-based approaches that insert known strings to test for memorization. Contamination can inflate scores by 5-20% on affected benchmarks',
      'Data contamination means the training data contains errors',
      'Data contamination is impossible to detect once the model is trained',
    ],
    correctAnswer: 1,
    explanation: 'With trillion-token pre-training corpora scraped from the web, benchmark test sets inevitably leak into training data. GPT-4 technical report documented contamination on various benchmarks. Detection: (1) exact/near-duplicate search with MinHash on training data, (2) completion test: can the model complete test examples given a prefix? (3) min-k% probe: examples with very low token probabilities in the tail are likely not memorized. Mitigation: time-based splits, dynamic benchmarks, decontamination during training data processing.',
    tags: ['data-contamination', 'benchmark', 'evaluation', 'memorization'],
  },
];

export default questions;
