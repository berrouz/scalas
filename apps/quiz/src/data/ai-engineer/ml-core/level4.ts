import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ai-engineer-ml-core-l4-001',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Advanced Gradient Boosting',
    question: 'In CatBoost, what is "ordered boosting" and what problem does it solve compared to standard gradient boosting?',
    options: [
      'Ordered boosting is equivalent to sorting the learning rate schedule',
      'Ordered boosting uses different random permutations of the training data to compute residuals, preventing target leakage that occurs when residuals are computed on the same data used for tree construction',
      'Ordered boosting trains trees in a fixed order from simplest to most complex',
      'Ordered boosting sorts features before training for faster splits',
    ],
    correctAnswer: 1,
    explanation: 'Standard gradient boosting computes residuals using the model trained on the same data, creating a subtle target leakage (prediction shift). CatBoost\'s ordered boosting maintains multiple models trained on different prefixes of a random permutation, using model_i (trained on examples 1..i-1) to compute the residual for example i. This eliminates prediction shift at the cost of more computation.',
    tags: ['catboost', 'ordered-boosting', 'prediction-shift', 'gradient-boosting'],
  },
  {
    id: 'ai-engineer-ml-core-l4-002',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Conformal Prediction',
    question: 'What guarantee does split conformal prediction provide, and what is the key assumption?',
    options: [
      'Given exchangeable data, it constructs prediction sets that contain the true value with at least (1-α) probability in finite samples, using calibration residuals from a held-out set to determine the set size',
      'It guarantees convergence of the training loss',
      'It guarantees the model\'s point predictions are within 5% of the true value',
      'It provides Bayesian credible intervals using prior distributions',
    ],
    correctAnswer: 0,
    explanation: 'Split conformal prediction calibrates on a held-out set by computing nonconformity scores (e.g., |y - ŷ|). The (1-α) quantile of these scores becomes the radius of the prediction interval. The coverage guarantee P(Y ∈ C(X)) ≥ 1-α holds for any model, any distribution, in finite samples — only requiring exchangeability (weaker than i.i.d.). No distributional assumptions are needed.',
    tags: ['conformal-prediction', 'uncertainty', 'calibration', 'coverage'],
  },
  {
    id: 'ai-engineer-ml-core-l4-003',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Double Machine Learning',
    question: 'What is Double Machine Learning (DML) and why does it use cross-fitting?',
    options: [
      'DML uses ML models to estimate nuisance parameters (treatment propensity and outcome regression), then estimates causal effects from residuals. Cross-fitting splits data so nuisance models are trained on different folds than where residuals are computed, preventing overfitting bias (Donsker condition violation)',
      'DML refers to training a model twice with different hyperparameters',
      'DML trains two separate models and averages their predictions',
      'DML uses two loss functions simultaneously for better optimization',
    ],
    correctAnswer: 0,
    explanation: 'DML (Chernozhukov et al. 2018) provides √n-consistent causal effect estimates by: (1) predicting Y from controls (get residual Ỹ), (2) predicting treatment T from controls (get residual T̃), (3) regressing Ỹ on T̃. Cross-fitting is essential: using the same data for nuisance estimation and causal estimation introduces bias that doesn\'t vanish at √n rate.',
    tags: ['double-ml', 'causal-inference', 'cross-fitting', 'nuisance-parameters'],
  },
  {
    id: 'ai-engineer-ml-core-l4-004',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Feature Engineering',
    question: 'What is target encoding with Bayesian smoothing, and how does it handle high-cardinality categorical features?',
    options: [
      'It replaces each category with a weighted average of the category\'s mean target and the global mean, where the weight depends on the category count — categories with few samples are shrunk toward the global mean to prevent overfitting on noisy estimates',
      'It replaces categories with their frequency count',
      'It uses the target variable directly as a feature',
      'It creates binary dummy variables for each category',
    ],
    correctAnswer: 0,
    explanation: 'Target encoding with Bayesian smoothing computes: encoded = (n * cat_mean + m * global_mean) / (n + m), where n is category count and m is a smoothing parameter. For rare categories (small n), the encoding is close to global_mean; for common categories, it\'s close to cat_mean. This prevents overfitting on rare categories while preserving signal. Cross-validation schemes further prevent target leakage.',
    tags: ['target-encoding', 'categorical-features', 'bayesian-smoothing', 'feature-engineering'],
  },
  {
    id: 'ai-engineer-ml-core-l4-005',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Advanced Regularization',
    question: 'What is the relationship between dropout and L2 regularization from a Bayesian perspective?',
    options: [
      'L2 regularization implements exact dropout',
      'Dropout and L2 are completely unrelated techniques',
      'Dropout is a replacement for L2 that provides no regularization benefit',
      'Dropout can be interpreted as approximate Bayesian inference where each forward pass samples a different subnetwork; Gal & Ghahramani (2016) showed dropout training approximates a deep Gaussian process, with the dropout rate controlling the prior precision (analogous to L2 strength)',
    ],
    correctAnswer: 3,
    explanation: 'Gal & Ghahramani showed that training with dropout minimizes a variational lower bound on a Gaussian process model. The weight decay (L2) parameter and dropout rate together determine the prior precision. MC Dropout (keeping dropout at test time and averaging predictions) provides a principled uncertainty estimate. This connects two seemingly different regularization approaches through variational inference.',
    tags: ['dropout', 'l2-regularization', 'bayesian', 'variational-inference'],
  },
  {
    id: 'ai-engineer-ml-core-l4-006',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Ensemble Methods',
    question: 'What is negative correlation learning in ensemble methods, and how does it improve upon independent training?',
    options: [
      'It trains models to produce negatively correlated errors by adding a penalty term that encourages diversity among ensemble members, reducing ensemble variance more effectively than independently trained models',
      'It inverts the predictions of half the ensemble members',
      'It trains models on negatively labeled data to improve recall',
      'It uses negative sampling to speed up ensemble training',
    ],
    correctAnswer: 0,
    explanation: 'Negative correlation learning adds a regularization term λ·(f_i - f̄)·Σ_{j≠i}(f_j - f̄) to each model\'s loss, penalizing correlated errors. This explicitly encourages ensemble diversity. The ensemble error decomposes into average individual error minus average pairwise diversity; NCL directly optimizes this decomposition, achieving lower ensemble error than independent training which only minimizes individual errors.',
    tags: ['ensemble', 'negative-correlation', 'diversity', 'variance-reduction'],
  },
  {
    id: 'ai-engineer-ml-core-l4-007',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Causal Inference',
    question: 'What is the Regression Discontinuity Design (RDD) and when is it applicable for causal estimation in ML systems?',
    options: [
      'RDD exploits a threshold-based treatment assignment rule (e.g., users with score > X get a feature) to estimate local causal effects by comparing outcomes just above and below the threshold, where assignment is quasi-random',
      'RDD requires random assignment of treatment',
      'RDD is only applicable to time-series data',
      'RDD is a method for handling discontinuities in regression data',
    ],
    correctAnswer: 0,
    explanation: 'RDD applies when treatment is assigned by a sharp cutoff on a running variable (e.g., credit score > 700 gets approved). Near the threshold, subjects are quasi-randomly assigned. Comparing outcomes in a narrow bandwidth around the cutoff estimates the Local Average Treatment Effect. This is common in ML A/B testing when features are rolled out based on user engagement scores.',
    tags: ['rdd', 'causal-inference', 'quasi-experiment', 'threshold'],
  },
  {
    id: 'ai-engineer-ml-core-l4-008',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Imbalanced Learning',
    question: 'Why does SMOTE fail in high-dimensional spaces and what alternatives work better?',
    options: [
      'SMOTE is designed only for binary classification',
      'In high dimensions, SMOTE interpolates between neighbors in sparse regions where the convex hull of minority points may overlap with majority class territory, generating noisy synthetic samples. Alternatives include ADASYN with careful k-selection, cost-sensitive learning, or focal loss which avoids synthetic data entirely',
      'SMOTE fails because it only works with numerical features',
      'SMOTE works perfectly in any dimensionality',
    ],
    correctAnswer: 1,
    explanation: 'The curse of dimensionality means k-nearest neighbors in high dimensions are not truly "near" — distances become uniform. SMOTE\'s linear interpolation between these pseudo-neighbors generates points in empty regions that may be closer to majority class clusters. For high-dimensional data (>50 features), cost-sensitive learning (adjusting class weights), focal loss, or careful undersampling often outperform oversampling.',
    tags: ['smote', 'imbalanced-learning', 'high-dimensional', 'focal-loss'],
  },
  {
    id: 'ai-engineer-ml-core-l4-009',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Feature Selection',
    question: 'What is the Boruta algorithm and how does it improve upon simple feature importance thresholding?',
    options: [
      'Boruta is a dimensionality reduction algorithm like PCA',
      'Boruta is a wrapper method that tries all feature subsets exhaustively',
      'Boruta creates "shadow" features (shuffled copies of all features), trains a random forest, and iteratively compares each real feature\'s importance against the maximum shadow importance. Features consistently outperforming the best random shadow are confirmed; this provides a statistical test rather than an arbitrary importance cutoff',
      'Boruta selects the top-k features based on correlation with the target',
    ],
    correctAnswer: 2,
    explanation: 'Boruta creates shadow features by randomly permuting each original feature, destroying its relationship with the target. It then compares each real feature\'s importance against the maximum importance of any shadow feature across iterations. Features significantly exceeding this null distribution are "confirmed." This provides a principled all-relevant feature selection, not just minimal optimal subset.',
    tags: ['boruta', 'feature-selection', 'shadow-features', 'random-forest'],
  },
  {
    id: 'ai-engineer-ml-core-l4-010',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Online Learning',
    question: 'What is the Follow-The-Regularized-Leader (FTRL) algorithm and why is it preferred for large-scale online learning with sparse features?',
    options: [
      'FTRL is identical to Adam optimizer',
      'FTRL only works with linear models, not neural networks',
      'FTRL maintains per-coordinate learning rates and produces truly sparse models by using L1 regularization with a proximal update that drives weights to exact zero, unlike SGD with L1 which rarely produces exact zeros due to the gradient update step',
      'FTRL is a batch learning algorithm for dense feature spaces',
    ],
    correctAnswer: 2,
    explanation: 'FTRL-Proximal (McMahan 2013) solves the problem that online gradient descent with L1 penalty rarely produces exact zeros (weights oscillate near zero). FTRL accumulates gradients and applies the L1 proximal operator, which has a closed-form solution that thresholds weights to exactly zero. This produces truly sparse models essential for serving with billions of sparse features (e.g., ad click prediction).',
    tags: ['ftrl', 'online-learning', 'sparse-models', 'l1-regularization'],
  },
  {
    id: 'ai-engineer-ml-core-l4-011',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Calibration',
    question: 'What is the difference between Platt scaling and isotonic regression for probability calibration, and when does each fail?',
    options: [
      'Isotonic regression always outperforms Platt scaling',
      'They are identical methods with different names',
      'Platt scaling fits a sigmoid (2 parameters) assuming a monotonic S-curve relationship; it can undercalibrate multimodal models. Isotonic regression fits a non-parametric monotonic step function with more flexibility but can overfit with small calibration sets (<1000 samples)',
      'Platt scaling works only for binary classification; isotonic regression only for regression',
    ],
    correctAnswer: 2,
    explanation: 'Platt scaling: P(y=1|s) = σ(As + B) — learns 2 parameters, robust but assumes a sigmoidal relationship between model scores and true probabilities. Isotonic regression: fits a piecewise constant non-decreasing function, more flexible but needs sufficient data (~1000+ calibration samples) to avoid overfitting. For neural networks with non-sigmoidal calibration curves, isotonic often wins; for SVMs, Platt is typically sufficient.',
    tags: ['calibration', 'platt-scaling', 'isotonic-regression', 'probability'],
  },
  {
    id: 'ai-engineer-ml-core-l4-012',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Optimization',
    question: 'What is the Polyak-Ruppert averaging scheme and why is it beneficial for SGD convergence?',
    options: [
      'It averages the learning rates across epochs',
      'It averages predictions from multiple models',
      'It computes the average gradient over the entire dataset',
      'It maintains a running average of all parameter iterates during SGD training; the averaged parameters converge at the optimal O(1/n) rate for convex problems, even with a constant learning rate, smoothing out SGD noise without requiring learning rate decay schedules',
    ],
    correctAnswer: 3,
    explanation: 'Polyak-Ruppert averaging: θ̄_n = (1/n)Σθ_t. Even with constant step size (which keeps SGD exploring), the averaged iterates converge at O(1/n) rate, matching the information-theoretic lower bound. This is used in stochastic approximation and has been revived in modern deep learning as Stochastic Weight Averaging (SWA), which averages weights from the last several epochs.',
    tags: ['sgd', 'polyak-averaging', 'swa', 'optimization'],
  },
  {
    id: 'ai-engineer-ml-core-l4-013',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Gaussian Processes',
    question: 'What is the computational bottleneck of Gaussian Processes and how do inducing point methods address it?',
    options: [
      'GPs have no computational bottleneck; they scale linearly',
      'GP inference requires inverting an N×N kernel matrix (O(N³) time, O(N²) memory). Inducing point methods (e.g., SVGP) select M≪N pseudo-inputs and approximate the posterior using only M×M matrices, reducing complexity to O(NM²) while enabling mini-batch training via variational inference',
      'The bottleneck is in kernel computation, which takes O(N⁴) time',
      'GPs are bottlenecked by feature dimensionality',
    ],
    correctAnswer: 1,
    explanation: 'Exact GP inference requires computing K⁻¹ for an N×N kernel matrix — O(N³) time and O(N²) memory, prohibitive for N > ~10,000. Sparse Variational GP (SVGP) introduces M inducing points with variational parameters, fitting an approximate posterior q(f) by optimizing the ELBO. This reduces per-step cost to O(NM²) and is compatible with stochastic optimization (mini-batches).',
    tags: ['gaussian-processes', 'inducing-points', 'svgp', 'scalability'],
  },
  {
    id: 'ai-engineer-ml-core-l4-014',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Meta-Learning',
    question: 'How does MAML (Model-Agnostic Meta-Learning) differ from standard transfer learning?',
    options: [
      'MAML requires millions of examples per task while transfer learning works with few',
      'MAML can only be applied to classification tasks',
      'MAML and transfer learning are identical approaches',
      'MAML optimizes initial parameters such that a few gradient steps on a new task\'s small support set achieves good performance, explicitly training the initialization to be easily adaptable. Transfer learning simply pretrains on source tasks and fine-tunes, without optimizing for fast adaptation',
    ],
    correctAnswer: 3,
    explanation: 'MAML\'s bi-level optimization: inner loop takes k gradient steps on task i\'s support set from θ to get θ\'_i; outer loop updates θ to minimize loss on task i\'s query set evaluated at θ\'_i, summed over tasks. This trains θ to be a good initialization for rapid adaptation. Transfer learning pretrains θ on source data without explicitly optimizing for few-step adaptability.',
    tags: ['maml', 'meta-learning', 'few-shot', 'transfer-learning'],
  },
  {
    id: 'ai-engineer-ml-core-l4-015',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Survival Analysis',
    question: 'What is the Cox Proportional Hazards model assumption and how can it be extended with ML for survival prediction?',
    options: [
      'The Cox model cannot handle censored observations',
      'The Cox model requires the exact time of event for all subjects',
      'The Cox model assumes hazard ratios between individuals are constant over time (proportional hazards). ML extensions like DeepSurv replace the linear predictor with a neural network while maintaining the partial likelihood loss, allowing non-linear covariate effects while preserving the semi-parametric baseline hazard',
      'The Cox model assumes the survival function is always exponential',
    ],
    correctAnswer: 2,
    explanation: 'Cox PH model: h(t|x) = h₀(t)·exp(βx), where h₀(t) is an unspecified baseline hazard. The proportional hazard assumption means the ratio h(t|x₁)/h(t|x₂) is constant over time. DeepSurv replaces βx with a neural network f(x), optimizing the same partial likelihood. This handles non-linear and interaction effects while naturally handling right-censoring without specifying the baseline hazard form.',
    tags: ['survival-analysis', 'cox-model', 'deepsurv', 'censoring'],
  },
  {
    id: 'ai-engineer-ml-core-l4-016',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Interpretability',
    question: 'What is the difference between LIME and SHAP for local model explanations, and what are their respective failure modes?',
    options: [
      'LIME and SHAP always produce identical explanations',
      'SHAP is always faster and more accurate than LIME',
      'LIME works only for tree models; SHAP works only for neural networks',
      'LIME fits a linear model on perturbed samples around a point, but explanations can be unstable (different perturbations → different explanations). SHAP provides theoretically grounded attributions satisfying efficiency, symmetry, and monotonicity axioms, but kernel SHAP can be slow for high-dimensional data and assumes feature independence in perturbation',
    ],
    correctAnswer: 3,
    explanation: 'LIME\'s instability comes from random perturbation sampling and the choice of kernel width. Different random seeds can give contradictory explanations. SHAP\'s Shapley values satisfy uniqueness axioms (only method satisfying local accuracy + missingness + consistency), but Kernel SHAP approximates by treating features as independent during perturbation, which can be wrong for correlated features. TreeSHAP handles feature interactions correctly for tree models.',
    tags: ['lime', 'shap', 'interpretability', 'local-explanations'],
  },
  {
    id: 'ai-engineer-ml-core-l4-017',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Anomaly Detection',
    question: 'How does Isolation Forest achieve O(n log n) anomaly detection without distance computation?',
    options: [
      'It uses a single decision tree trained on labeled anomalies',
      'It builds random binary trees by selecting random features and random split points; anomalies are isolated in fewer splits (shorter path lengths) because they are sparse and far from clusters, making the average path length inversely related to anomaly score',
      'It uses k-means clustering to identify outliers',
      'It computes pairwise distances and selects points with maximum average distance',
    ],
    correctAnswer: 1,
    explanation: 'Isolation Forest exploits that anomalies are "few and different." Random splits isolate anomalies quickly (few splits needed) while normal points in dense regions require many splits. The anomaly score uses the average path length across trees, normalized by the expected path length for a random BST. No distance metrics needed, making it efficient for high-dimensional data. Complexity: O(n·t·log(ψ)) where t=trees, ψ=subsample size.',
    tags: ['isolation-forest', 'anomaly-detection', 'unsupervised', 'random-trees'],
  },
  {
    id: 'ai-engineer-ml-core-l4-018',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Gradient Boosting',
    question: 'What is GPU histogram-based gradient boosting and why does it fundamentally change the computational profile?',
    options: [
      'GPU histogram boosting requires more memory but is slower than CPU for small datasets',
      'GPU boosting only accelerates prediction, not training',
      'GPU histogram-based methods (e.g., XGBoost gpu_hist, LightGBM CUDA) bin continuous features into histograms on GPU with parallel atomic adds, then compute split gains for all features and bins simultaneously. This reduces split finding from O(N·F) sequential scans to O(N/P + bins·F) parallel operations, where P is GPU parallelism',
      'GPU boosting simply runs the same CPU algorithm on GPU cores',
    ],
    correctAnswer: 2,
    explanation: 'CPU gradient boosting scans sorted features sequentially. GPU histogram methods: (1) bin features into 256 buckets using parallel radix sort, (2) use atomic adds to build gradient/hessian histograms in shared memory, (3) evaluate all bin splits in parallel. The histogram subtraction trick (child = parent - sibling) halves computation. For datasets with millions of rows, GPU hist can be 5-20x faster than CPU.',
    tags: ['gpu', 'histogram', 'gradient-boosting', 'parallel-computing'],
  },
  {
    id: 'ai-engineer-ml-core-l4-019',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Distribution Shift',
    question: 'What is the difference between covariate shift, label shift, and concept drift, and how should each be addressed?',
    options: [
      'Covariate shift affects classification; label shift affects regression; concept drift affects clustering',
      'They are three names for the same phenomenon',
      'Covariate shift: P(X) changes but P(Y|X) is stable → importance weighting. Label shift: P(Y) changes but P(X|Y) is stable → adjust class priors using confusion matrix. Concept drift: P(Y|X) changes → retrain or use online learning. Each requires different detection methods and correction strategies',
      'All three are solved by collecting more training data',
    ],
    correctAnswer: 2,
    explanation: 'Covariate shift (common in deployment): test inputs differ from training. Corrected by reweighting training samples by density ratio p_test(x)/p_train(x). Label shift (e.g., disease prevalence change): class proportions change. Corrected by estimating new priors via confusion matrix inversion. Concept drift: the actual input-output relationship changes. Requires retraining, online learning, or windowed models. Detection via statistical tests (KS, MMD, PSI).',
    tags: ['distribution-shift', 'covariate-shift', 'concept-drift', 'monitoring'],
  },
  {
    id: 'ai-engineer-ml-core-l4-020',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Hyperparameter Optimization',
    question: 'How does multi-fidelity optimization (e.g., Successive Halving, Hyperband) accelerate hyperparameter search?',
    options: [
      'It evaluates many configurations with small budgets (few epochs, subsampled data), progressively allocating more resources to promising ones and early-stopping poor performers, achieving the same result as full training with a fraction of the total compute',
      'It uses multiple GPUs to train configurations in parallel',
      'It reduces the number of hyperparameters by fixing less important ones',
      'It tunes hyperparameters on a proxy task and transfers them',
    ],
    correctAnswer: 0,
    explanation: 'Successive Halving: start N configurations with budget B/N each, keep top 1/η, double their budget, repeat. Hyperband wraps this with different initial N values (aggressive vs conservative early stopping). BOHB combines this with Bayesian optimization for configuration selection. The key insight: early performance is correlated with final performance, so poor configurations can be identified cheaply.',
    tags: ['hyperband', 'successive-halving', 'multi-fidelity', 'hyperparameter-optimization'],
  },
  {
    id: 'ai-engineer-ml-core-l4-021',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Causal Inference',
    question: 'What is the Synthetic Control Method and when is it preferred over Difference-in-Differences?',
    options: [
      'Synthetic Control requires more treated units than DiD',
      'Synthetic Control generates artificial training data',
      'DiD always outperforms Synthetic Control in precision',
      'Synthetic Control constructs a weighted combination of untreated units that best matches the treated unit\'s pre-treatment trajectory, providing a counterfactual. Unlike DiD, it does not require the parallel trends assumption and works with a single treated unit (e.g., one region receiving a new recommendation algorithm)',
    ],
    correctAnswer: 3,
    explanation: 'Synthetic Control (Abadie 2003): for a single treated unit, find weights w_j for untreated units (donor pool) such that Σw_j·X_j ≈ X_treated pre-treatment. The post-treatment gap between the treated unit and synthetic control estimates the treatment effect. It excels when you have one treated entity (e.g., one country, one product line) and DiD\'s parallel trends assumption is dubious.',
    tags: ['synthetic-control', 'causal-inference', 'did', 'counterfactual'],
  },
  {
    id: 'ai-engineer-ml-core-l4-022',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Label Noise',
    question: 'How does confident learning (Cleanlab) identify label errors in training data without access to ground truth?',
    options: [
      'It requires at least 50% of labels to be correct to function',
      'It trains two models and flags examples where they disagree',
      'It compares labels against a dictionary of known correct labels',
      'It uses out-of-sample predicted probabilities (via cross-validation) to estimate the joint distribution of noisy and true labels, identifying examples where the model\'s confident prediction disagrees with the given label as likely mislabeled',
    ],
    correctAnswer: 3,
    explanation: 'Confident Learning (Northcutt et al. 2021): (1) get out-of-sample predicted probabilities via cross-validation, (2) compute per-class thresholds, (3) estimate the confident joint matrix Q(ỹ=i, y*=j) counting examples labeled i but confidently predicted as j, (4) identify likely errors as examples where the noisy label has low predicted probability. This finds 50-70% of label errors in benchmarks.',
    tags: ['confident-learning', 'label-noise', 'cleanlab', 'data-quality'],
  },
  {
    id: 'ai-engineer-ml-core-l4-023',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Differential Privacy',
    question: 'How does DP-SGD (Differentially Private Stochastic Gradient Descent) work and what is the privacy-utility trade-off?',
    options: [
      'DP-SGD encrypts gradients before aggregation',
      'DP-SGD eliminates the need for data anonymization',
      'DP-SGD clips per-sample gradients to a maximum norm C and adds calibrated Gaussian noise proportional to C/batch_size. The noise is tracked via the moments accountant to provide (ε,δ)-DP guarantees. Stronger privacy (smaller ε) requires more noise, degrading model accuracy, especially for complex models with many parameters',
      'DP-SGD only protects the model weights, not the training data',
    ],
    correctAnswer: 2,
    explanation: 'DP-SGD (Abadi et al. 2016): (1) compute per-sample gradients, (2) clip each to norm ≤ C (bounds sensitivity), (3) average clipped gradients, (4) add N(0, σ²C²/B²) noise. The Rényi DP accountant tightly tracks cumulative privacy loss across epochs. Key trade-offs: clipping distorts gradients; noise variance scales with dimensionality. Practical ε values (1-10) degrade accuracy by 2-20% depending on dataset size and model complexity.',
    tags: ['differential-privacy', 'dp-sgd', 'privacy', 'gradient-clipping'],
  },
  {
    id: 'ai-engineer-ml-core-l4-024',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Quantile Regression',
    question: 'What is quantile regression and why is it more robust than least-squares regression for skewed distributions?',
    options: [
      'Quantile regression minimizes the asymmetric pinball loss ρ_τ(y-ŷ) = τ·max(y-ŷ, 0) + (1-τ)·max(ŷ-y, 0) to estimate conditional quantiles. Unlike least-squares which estimates E[Y|X] and is sensitive to outliers, quantile regression estimates robust conditional quantiles and naturally provides prediction intervals',
      'Quantile regression bins the target into quantiles and does classification',
      'Quantile regression requires the target to be normally distributed',
      'Quantile regression only works for the median, not other percentiles',
    ],
    correctAnswer: 0,
    explanation: 'The pinball loss asymmetrically penalizes over/under-prediction. For τ=0.5, it\'s equivalent to MAE (median regression). For τ=0.1 and τ=0.9, it estimates the 10th and 90th percentiles, forming a prediction interval. Since it doesn\'t rely on distributional assumptions and doesn\'t square residuals, it\'s robust to outliers and skewed targets. Multiple quantiles can be estimated simultaneously for full conditional distribution coverage.',
    tags: ['quantile-regression', 'pinball-loss', 'robust-regression', 'prediction-intervals'],
  },
  {
    id: 'ai-engineer-ml-core-l4-025',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Semi-Supervised Learning',
    question: 'What is FixMatch and how does it combine consistency regularization with pseudo-labeling?',
    options: [
      'FixMatch is a supervised learning method that requires all labels',
      'FixMatch uses GANs to generate additional labeled training data',
      'FixMatch applies weak augmentation to unlabeled data to generate pseudo-labels (keeping only high-confidence ones above a threshold), then trains the model to predict the same pseudo-label on a strongly augmented version of the same image, combining the benefits of pseudo-labeling and consistency regularization in a simple framework',
      'FixMatch trains only on labeled data with fixed learning rates',
    ],
    correctAnswer: 2,
    explanation: 'FixMatch (Sohn et al. 2020): (1) weakly augment unlabeled image → get model prediction, (2) if max class probability > τ (typically 0.95), create hard pseudo-label, (3) strongly augment same image → train model to match the pseudo-label. This achieves remarkable results: 94.93% on CIFAR-10 with only 250 labels. The high confidence threshold ensures pseudo-label quality; strong augmentation provides diverse training signal.',
    tags: ['fixmatch', 'semi-supervised', 'pseudo-labeling', 'consistency-regularization'],
  },
  {
    id: 'ai-engineer-ml-core-l4-026',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Time Series',
    question: 'What is the key insight behind temporal fusion transformers (TFT) for multi-horizon time series forecasting?',
    options: [
      'TFT only supports single-step-ahead predictions',
      'TFT combines LSTM for local temporal patterns with multi-head attention for long-range dependencies, uses variable selection networks to learn feature importance per time step, and separates static covariates from time-varying inputs through gated residual networks, enabling interpretable multi-horizon forecasts',
      'TFT simply applies a standard transformer to time series data',
      'TFT requires the time series to be stationary',
    ],
    correctAnswer: 1,
    explanation: 'TFT (Lim et al. 2021) has four key components: (1) Variable Selection Networks with GRNs that learn to weight input features, providing interpretability, (2) Static enrichment that processes entity-level features (e.g., store ID) separate from temporal, (3) Temporal processing combining LSTM (local) and interpretable multi-head attention (long-range), (4) Quantile outputs for prediction intervals. This architecture handles heterogeneous inputs and provides feature importance per time step.',
    tags: ['tft', 'time-series', 'multi-horizon', 'attention'],
  },
  {
    id: 'ai-engineer-ml-core-l4-027',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Active Learning',
    question: 'What is batch mode active learning with diversity and why does pure uncertainty sampling fail for batch queries?',
    options: [
      'Uncertainty sampling always selects the most diverse batch',
      'Batch mode active learning labels all uncertain samples simultaneously',
      'Batch mode is only needed when the labeling budget exceeds 1000 samples',
      'Pure uncertainty sampling selects the top-k most uncertain samples, which are often redundant (similar to each other). Batch mode methods like BatchBALD or k-centers additionally enforce diversity among selected samples, maximizing information gain for the entire batch rather than greedily selecting individually uncertain points',
    ],
    correctAnswer: 3,
    explanation: 'If 100 uncertain samples all come from the same ambiguous region, labeling all 100 provides barely more information than labeling 1. BatchBALD computes the joint mutual information between a batch of points and the model parameters, penalizing redundancy. k-centers-greedy selects points that maximize minimum distance to already-selected points. Core-set approaches approximate the full dataset geometry.',
    tags: ['active-learning', 'batch-mode', 'batchbald', 'diversity'],
  },
  {
    id: 'ai-engineer-ml-core-l4-028',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Gradient Boosting',
    question: 'What is the monotonic constraint feature in gradient boosting and when is it essential for deployment?',
    options: [
      'Monotonic constraints enforce that the model\'s output is non-decreasing (or non-increasing) with respect to specified features, even where data is noisy. This is essential in regulated domains (credit scoring: higher income should never decrease predicted creditworthiness) to ensure predictions satisfy known domain relationships',
      'Monotonic constraints prevent overfitting by reducing tree depth',
      'Monotonic constraints force the model to be linear',
      'Monotonic constraints are only available in CatBoost',
    ],
    correctAnswer: 0,
    explanation: 'XGBoost/LightGBM enforce monotonicity during tree construction: when evaluating splits, they reject any split that would violate the constraint (e.g., left child value > right child value when the feature should be monotonically increasing). This may slightly reduce accuracy but ensures regulatory compliance and domain consistency. Essential for credit models (Fair Lending), insurance pricing, and medical risk scores.',
    tags: ['monotonic-constraints', 'gradient-boosting', 'regulatory-compliance', 'credit-scoring'],
  },
  {
    id: 'ai-engineer-ml-core-l4-029',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Optimal Transport',
    question: 'How is optimal transport (Wasserstein distance) used in machine learning beyond distribution comparison?',
    options: [
      'Optimal transport finds the minimum-cost mapping between distributions and is used for domain adaptation (aligning source and target feature distributions), generative modeling (Wasserstein GANs provide smoother gradients than JS divergence), data augmentation (interpolating between datasets), and fairness (equalizing score distributions across groups)',
      'Optimal transport is too computationally expensive for any practical ML use',
      'Optimal transport is only used for comparing probability distributions',
      'Optimal transport only works with 1D distributions',
    ],
    correctAnswer: 0,
    explanation: 'The Wasserstein distance W(P,Q) = inf E[c(X,Y)] over joint distributions with marginals P,Q provides a meaningful metric even for non-overlapping distributions (unlike KL divergence). Applications: WGAN uses W₁ for stable GAN training; OTDA aligns source/target features via optimal transport plans; Sinkhorn divergences enable efficient differentiable OT; barycentric mapping interpolates between distributions for data augmentation.',
    tags: ['optimal-transport', 'wasserstein', 'domain-adaptation', 'wgan'],
  },
  {
    id: 'ai-engineer-ml-core-l4-030',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Neural Architecture Search',
    question: 'What is the weight-sharing paradigm in neural architecture search (NAS) and why did it enable practical NAS?',
    options: [
      'A supernet containing all candidate architectures is trained once with shared weights; individual architectures are evaluated by inheriting weights from the supernet rather than training from scratch. This reduces NAS cost from thousands of GPU-hours per architecture to a single supernet training, though shared weights can rank architectures differently than independently trained ones',
      'Weight sharing means all architectures use the same weights',
      'Weight sharing refers to using pre-trained ImageNet weights for NAS',
      'Weight sharing makes NAS deterministic rather than stochastic',
    ],
    correctAnswer: 0,
    explanation: 'One-shot NAS (e.g., DARTS, OFA) trains a single supernet where each path represents an architecture. Search evaluates architectures by activating their corresponding path and using inherited weights, avoiding retraining. This reduces search cost from ~3000 GPU-hours (NASNet) to ~1 GPU-day (DARTS). The key challenge is that weight-sharing rankings can diverge from true rankings, requiring careful calibration.',
    tags: ['nas', 'weight-sharing', 'supernet', 'darts'],
  },
  {
    id: 'ai-engineer-ml-core-l4-031',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Multi-Task Learning',
    question: 'What is the gradient conflict problem in multi-task learning and how does PCGrad address it?',
    options: [
      'When tasks have conflicting gradients (negative cosine similarity), optimizing one task degrades another. PCGrad projects each task\'s gradient onto the normal plane of other tasks\' gradients when they conflict, removing the conflicting component and preserving the non-conflicting direction',
      'PCGrad simply averages gradients from all tasks equally',
      'Gradient conflict means tasks need different learning rates',
      'Gradient conflict only occurs with more than 10 tasks',
    ],
    correctAnswer: 0,
    explanation: 'In multi-task learning, ∇L_i and ∇L_j may point in opposing directions for shared parameters. Simple averaging cancels useful gradient signal. PCGrad (Yu et al. 2020): for each task pair, if g_i · g_j < 0, replace g_i with g_i - (g_i · g_j / ||g_j||²) · g_j (projection onto the orthogonal complement). This removes the conflicting component while preserving beneficial gradient directions.',
    tags: ['multi-task-learning', 'pcgrad', 'gradient-conflict', 'optimization'],
  },
  {
    id: 'ai-engineer-ml-core-l4-032',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Data Valuation',
    question: 'What is Data Shapley and why is it important for data marketplace and debugging applications?',
    options: [
      'Data Shapley assigns each training datum a value based on its marginal contribution to model performance across all possible subsets of training data. It provides equitable data valuation for data markets and identifies harmful training points (negative value) for data debugging, though exact computation is exponential so Monte Carlo approximation is used',
      'Data Shapley ranks features, not data points',
      'Data Shapley only works for tabular data, not images',
      'Data Shapley measures the size of each data point in bytes',
    ],
    correctAnswer: 0,
    explanation: 'Data Shapley (Ghorbani & Zou 2019) applies Shapley values to data points: φ_i = Σ_{S⊆D\\{i}} [v(S∪{i}) - v(S)] · weight, where v(S) is model performance trained on subset S. This uniquely satisfies fairness axioms (symmetry, null player, additivity). Practical computation uses Monte Carlo permutation sampling or KNN-Shapley for efficiency. Applications: compensating data contributors, removing corrupted training samples.',
    tags: ['data-shapley', 'data-valuation', 'shapley-values', 'data-marketplace'],
  },
  {
    id: 'ai-engineer-ml-core-l4-033',
    language: 'ai-engineer',
    level: 'level4',
    category: 'Machine Learning Core',
    subcategory: 'Federated Learning',
    question: 'What is the non-IID data problem in federated learning and how does FedProx address it?',
    options: [
      'Non-IID data is beneficial for federated learning convergence',
      'In federated learning, each client\'s local data can have very different distributions (non-IID: different class proportions, feature distributions). FedAvg diverges in this setting because local models overfit to their skewed data. FedProx adds a proximal term ||w - w_global||² to each client\'s objective, penalizing local models that drift too far from the global model',
      'Federated learning always assumes data is identically distributed',
      'FedProx requires clients to share raw data samples with the server',
    ],
    correctAnswer: 1,
    explanation: 'With non-IID data, each client\'s local SGD steps optimize for different distributions, causing "client drift" — local models diverge, and averaging them produces a poor global model. FedProx (Li et al. 2020) adds μ/2·||w - w_t||² to each client\'s loss, keeping local updates close to the current global model. This stabilizes convergence with non-IID data and also handles systems heterogeneity (variable local epochs per client).',
    tags: ['federated-learning', 'non-iid', 'fedprox', 'client-drift'],
  },
];

export default questions;
