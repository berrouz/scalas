import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-gradle-l3-001',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Convention Plugins',
    question: 'What is a convention plugin in Gradle?',
    options: [
      'A plugin that validates code conventions',
      'A built-in Gradle plugin',
      'A plugin that follows naming conventions',
      'A reusable plugin that encapsulates common build configuration and is applied to projects that follow a certain convention',
    ],
    correctAnswer: 3,
    explanation: 'Convention plugins encapsulate common build logic (e.g., Kotlin setup, testing, publishing) into reusable plugins. They replace allprojects/subprojects blocks with a more modular approach.',
    codeSnippet: '// build-logic/convention/src/main/kotlin/kotlin-library-conventions.gradle.kts\nplugins {\n    kotlin("jvm")\n    `java-library`\n}\n\nkotlin {\n    jvmToolchain(17)\n}\n\ntasks.test {\n    useJUnitPlatform()\n}',
    tags: ['convention-plugins', 'build-logic', 'reusability'],
  },
  {
    id: 'kotlin-gradle-l3-002',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Convention Plugins',
    question: 'Where is the recommended location for convention plugins?',
    options: [
      'In each subproject',
      'In a separate build-logic included build or in buildSrc',
      'In a separate repository',
      'In the root build.gradle.kts',
    ],
    correctAnswer: 1,
    explanation: 'Convention plugins are best placed in a build-logic included build (composite build) or in buildSrc. The included build approach is preferred as it avoids buildSrc invalidation issues.',
    codeSnippet: '// settings.gradle.kts\npluginManagement {\n    includeBuild("build-logic")\n}\n\n// build-logic/settings.gradle.kts\nrootProject.name = "build-logic"\n\n// build-logic/convention/build.gradle.kts\nplugins {\n    `kotlin-dsl`\n}',
    tags: ['convention-plugins', 'build-logic', 'included-build'],
  },
  {
    id: 'kotlin-gradle-l3-003',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Composite Builds',
    question: 'What is a composite build (included build) in Gradle?',
    options: [
      'A build that uses multiple languages',
      'A build that targets multiple platforms',
      'A build that combines Maven and Gradle',
      'A build that includes other independent Gradle builds, allowing them to work together',
    ],
    correctAnswer: 3,
    explanation: 'A composite build includes one or more independent Gradle builds into another. This enables dependency substitution and is the recommended way to organize build logic.',
    codeSnippet: '// settings.gradle.kts\nincludeBuild("build-logic")\nincludeBuild("../shared-library")',
    tags: ['composite-builds', 'includeBuild', 'modularization'],
  },
  {
    id: 'kotlin-gradle-l3-004',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Composite Builds',
    question: 'How does dependency substitution work in composite builds?',
    options: [
      'Dependencies are never substituted',
      'Gradle automatically substitutes dependencies with matching GAV coordinates from included builds',
      'Only plugins are substituted',
      'You must manually configure substitution rules',
    ],
    correctAnswer: 1,
    explanation: 'When an included build produces a module with the same group and name as an external dependency, Gradle automatically substitutes the external dependency with the local project.',
    tags: ['composite-builds', 'dependency-substitution', 'automatic'],
  },
  {
    id: 'kotlin-gradle-l3-005',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Configuration Cache',
    question: 'What restrictions does the configuration cache impose on build scripts?',
    options: [
      'No restrictions',
      'Only Kotlin DSL is supported',
      'Plugin block is not allowed',
      'Build scripts cannot reference Task objects at configuration time, must not use certain Gradle APIs, and should avoid shared mutable state',
    ],
    correctAnswer: 3,
    explanation: 'The configuration cache requires that build logic is serializable. It restricts accessing Task instances during configuration, using certain Gradle/Project APIs from tasks, and sharing mutable state.',
    tags: ['configuration-cache', 'restrictions', 'serialization'],
  },
  {
    id: 'kotlin-gradle-l3-006',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Configuration Cache',
    question: 'How do you make a custom task compatible with the configuration cache?',
    options: [
      'Disable caching for the task',
      'Use Property and Provider APIs instead of directly referencing Project or other non-serializable objects',
      'Add @ConfigurationCache annotation',
      'Use only string inputs',
    ],
    correctAnswer: 1,
    explanation: 'Configuration cache compatible tasks use Property/Provider APIs for inputs, avoid referencing Project at execution time, and use services like providers.exec() instead of project.exec().',
    codeSnippet: 'abstract class MyTask : DefaultTask() {\n    @get:Input\n    abstract val inputValue: Property<String>\n    \n    // Wrong: val proj = project (captured at config time)\n    // Right: use providers to defer evaluation\n    \n    @TaskAction\n    fun execute() {\n        println(inputValue.get())\n    }\n}',
    tags: ['configuration-cache', 'property-api', 'compatibility'],
  },
  {
    id: 'kotlin-gradle-l3-007',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is the Provider API and why is it important?',
    options: [
      'A dependency injection framework',
      'An API for providing build scripts',
      'A lazy evaluation API that defers value computation and enables configuration cache compatibility',
      'An API for providing dependencies',
    ],
    correctAnswer: 2,
    explanation: 'The Provider API (Property<T>, Provider<T>, ListProperty<T>) defers value computation to when the value is actually needed. This enables lazy configuration, avoids ordering issues, and is required for configuration cache.',
    codeSnippet: 'abstract class GenerateFile : DefaultTask() {\n    @get:Input\n    abstract val message: Property<String>\n    \n    @get:OutputFile\n    abstract val outputFile: RegularFileProperty\n    \n    @TaskAction\n    fun generate() {\n        outputFile.get().asFile.writeText(message.get())\n    }\n}',
    tags: ['provider-api', 'lazy-configuration', 'property'],
  },
  {
    id: 'kotlin-gradle-l3-008',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'How do you connect the output of one task to the input of another using the Provider API?',
    options: [
      'taskB.inputFile.set(taskA.outputFile)',
      'taskB.from(taskA)',
      'taskB.dependsOn(taskA); taskB.input = taskA.outputPath',
      'taskB.input = taskA.output.get()',
    ],
    correctAnswer: 0,
    explanation: 'By setting a task\'s input property to another task\'s output property (without .get()), Gradle establishes both a data flow connection and an implicit task dependency.',
    codeSnippet: 'val generate = tasks.register<GenerateFile>("generate") {\n    outputFile.set(layout.buildDirectory.file("generated.txt"))\n}\n\ntasks.register<ProcessFile>("process") {\n    inputFile.set(generate.flatMap { it.outputFile }) // implicit dependency\n}',
    tags: ['provider-api', 'task-wiring', 'implicit-dependency'],
  },
  {
    id: 'kotlin-gradle-l3-009',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you write a custom Gradle plugin in Kotlin?',
    options: [
      'Extend AbstractPlugin class',
      'Annotate a class with @GradlePlugin',
      'Implement the Plugin<Project> interface',
      'Implement the BuildPlugin interface',
    ],
    correctAnswer: 2,
    explanation: 'A custom Gradle plugin implements the Plugin<Project> interface and overrides the apply() method to configure the project when the plugin is applied.',
    codeSnippet: 'class MyPlugin : Plugin<Project> {\n    override fun apply(project: Project) {\n        project.tasks.register("myTask") {\n            doLast {\n                println("Hello from MyPlugin!")\n            }\n        }\n    }\n}',
    tags: ['plugins', 'custom-plugin', 'implementation'],
  },
  {
    id: 'kotlin-gradle-l3-010',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you create a plugin extension for configuration?',
    options: [
      'Use a configuration file',
      'Create an interface/class and register it with project.extensions.create()',
      'Use @Extension annotation',
      'Add properties to the plugin class',
    ],
    correctAnswer: 1,
    explanation: 'Plugin extensions are custom DSL objects that allow users to configure your plugin. Create an interface or open class and register it as an extension.',
    codeSnippet: 'interface MyExtension {\n    val outputDir: DirectoryProperty\n    val verbose: Property<Boolean>\n}\n\nclass MyPlugin : Plugin<Project> {\n    override fun apply(project: Project) {\n        val ext = project.extensions.create<MyExtension>("myPlugin")\n        ext.verbose.convention(false)\n    }\n}',
    tags: ['plugins', 'extensions', 'dsl'],
  },
  {
    id: 'kotlin-gradle-l3-011',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin Multiplatform',
    question: 'How do you configure hierarchical source sets in Kotlin Multiplatform?',
    options: [
      'Both A and B: default hierarchy exists but can be customized',
      'You must manually create intermediate source sets and configure dependsOn relationships',
      'Use the hierarchy {} block',
      'They are automatically configured based on declared targets',
    ],
    correctAnswer: 0,
    explanation: 'Since Kotlin 1.8, the default target hierarchy template creates intermediate source sets automatically. You can also create custom intermediate source sets with dependsOn for sharing code between specific targets.',
    codeSnippet: 'kotlin {\n    applyDefaultHierarchyTemplate()\n    \n    jvm()\n    iosArm64()\n    iosSimulatorArm64()\n    \n    sourceSets {\n        val commonMain by getting\n        // iosMain is auto-created, shared by iosArm64 and iosSimulatorArm64\n    }\n}',
    tags: ['kotlin-multiplatform', 'source-sets', 'hierarchy'],
  },
  {
    id: 'kotlin-gradle-l3-012',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin Multiplatform',
    question: 'How do you declare expect/actual dependencies in KMP Gradle configuration?',
    options: [
      'Use expect {} and actual {} blocks in build.gradle.kts',
      'Use the @Expect annotation on dependencies',
      'expect/actual is not related to Gradle configuration',
      'Dependencies in commonMain can be expect-based; platform source sets add actual implementations via their own dependencies',
    ],
    correctAnswer: 3,
    explanation: 'In KMP, commonMain declares dependencies on multiplatform libraries. Platform source sets add platform-specific implementations. The expect/actual mechanism is a language feature; Gradle just manages the classpath.',
    codeSnippet: 'kotlin {\n    sourceSets {\n        commonMain.dependencies {\n            implementation("io.ktor:ktor-client-core:2.3.0")\n        }\n        jvmMain.dependencies {\n            implementation("io.ktor:ktor-client-okhttp:2.3.0")\n        }\n        iosMain.dependencies {\n            implementation("io.ktor:ktor-client-darwin:2.3.0")\n        }\n    }\n}',
    tags: ['kotlin-multiplatform', 'expect-actual', 'dependencies'],
  },
  {
    id: 'kotlin-gradle-l3-013',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Build Cache',
    question: 'How do you configure a remote build cache in Gradle?',
    options: [
      'Configure buildCache {} block in settings.gradle.kts with remote {} section',
      'Set remoteCache=true in gradle.properties',
      'Install a Gradle plugin',
      'Use the --remote-cache flag',
    ],
    correctAnswer: 0,
    explanation: 'Remote build cache is configured in the buildCache {} block in settings.gradle.kts, specifying the cache server URL and push/pull settings.',
    codeSnippet: 'buildCache {\n    local {\n        isEnabled = true\n    }\n    remote<HttpBuildCache> {\n        url = uri("https://cache.example.com/cache/")\n        isPush = System.getenv("CI") != null\n        credentials {\n            username = "user"\n            password = "pass"\n        }\n    }\n}',
    tags: ['build-cache', 'remote', 'configuration'],
  },
  {
    id: 'kotlin-gradle-l3-014',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Build Cache',
    question: 'What is @PathSensitive annotation used for in cacheable tasks?',
    options: [
      'To set the working directory',
      'To specify how much of a file path should be considered as part of the cache key',
      'To restrict file access',
      'To validate file paths',
    ],
    correctAnswer: 1,
    explanation: '@PathSensitive controls what part of input file paths affects the cache key. RELATIVE means only the relative path matters (not the absolute path), enabling cache reuse across machines.',
    codeSnippet: '@CacheableTask\nabstract class MyTask : DefaultTask() {\n    @get:InputFiles\n    @get:PathSensitive(PathSensitivity.RELATIVE)\n    abstract val sources: ConfigurableFileCollection\n}',
    tags: ['build-cache', 'path-sensitive', 'annotations'],
  },
  {
    id: 'kotlin-gradle-l3-015',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Incremental Compilation',
    question: 'What is the difference between Kotlin incremental compilation and Gradle incremental builds?',
    options: [
      'Gradle incremental builds are faster',
      'Kotlin incremental compilation only works on Android',
      'Kotlin incremental compilation recompiles only changed Kotlin files; Gradle incremental builds skip entire tasks if inputs/outputs are unchanged',
      'They are the same thing',
    ],
    correctAnswer: 2,
    explanation: 'Gradle incremental builds operate at the task level (skip or reuse entire tasks). Kotlin incremental compilation operates within the compileKotlin task, recompiling only changed files and their dependents.',
    tags: ['incremental-compilation', 'incremental-build', 'performance'],
  },
  {
    id: 'kotlin-gradle-l3-016',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Incremental Compilation',
    question: 'What can break Kotlin incremental compilation, causing a full recompilation?',
    options: [
      'Changes to classpath (dependencies), changes to compiler arguments, or changes to build script plugins',
      'Adding a new file',
      'Modifying test files only',
      'Changing resource files',
    ],
    correctAnswer: 0,
    explanation: 'Incremental compilation falls back to full recompilation when classpath changes are detected, compiler arguments change, or certain annotation processors are used. Build cache can help mitigate this.',
    tags: ['incremental-compilation', 'full-recompilation', 'causes'],
  },
  {
    id: 'kotlin-gradle-l3-017',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you implement a custom component metadata rule?',
    options: [
      'Implement ComponentMetadataRule and register it with dependencies.components',
      'Edit the POM file',
      'Use a Gradle plugin',
      'Use a repository filter',
    ],
    correctAnswer: 0,
    explanation: 'ComponentMetadataRule allows you to modify component metadata during dependency resolution. This is useful for fixing incorrect metadata in published POMs or adding missing capabilities.',
    codeSnippet: 'abstract class FixMetadataRule : ComponentMetadataRule {\n    override fun execute(context: ComponentMetadataContext) {\n        context.details.allVariants {\n            withCapabilities {\n                addCapability("org.slf4j", "slf4j-impl", "0")\n            }\n        }\n    }\n}\n\ndependencies {\n    components {\n        withModule<FixMetadataRule>("ch.qos.logback:logback-classic")\n    }\n}',
    tags: ['dependencies', 'metadata-rule', 'component'],
  },
  {
    id: 'kotlin-gradle-l3-018',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'What are Gradle Module Metadata files (.module)?',
    options: [
      'Rich metadata files that support variants, capabilities, and dependency constraints beyond what POM files can express',
      'Build cache metadata files',
      'Configuration files for modules',
      'Module documentation files',
    ],
    correctAnswer: 0,
    explanation: 'Gradle Module Metadata (.module files) is a JSON format that provides richer dependency information than Maven POM files, including variant-aware resolution, capabilities, and constraints.',
    tags: ['dependencies', 'module-metadata', 'variants'],
  },
  {
    id: 'kotlin-gradle-l3-019',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'What is variant-aware dependency resolution?',
    options: [
      'A/B testing for dependency versions',
      'Resolving platform-specific dependencies',
      'Gradle\'s ability to select the correct variant of a dependency based on attributes like JVM version, Kotlin target, etc.',
      'Resolving dependencies for different build variants',
    ],
    correctAnswer: 2,
    explanation: 'Variant-aware resolution uses attributes (like org.gradle.jvm.version, org.jetbrains.kotlin.platform.type) to select the most appropriate variant of a dependency for the consumer\'s context.',
    tags: ['dependencies', 'variant-aware', 'attributes'],
  },
  {
    id: 'kotlin-gradle-l3-020',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you create a custom dependency configuration that extends another?',
    options: [
      'configurations.create("myConfig").extend("implementation")',
      'configurations { myConfig extends implementation }',
      'configurations.create("myConfig") { extendsFrom(configurations.implementation.get()) }',
      'configurations.register("myConfig", extends = "implementation")',
    ],
    correctAnswer: 2,
    explanation: 'extendsFrom() makes one configuration inherit all dependencies from another. This is commonly used for creating integration test configurations.',
    codeSnippet: 'val integrationTestImplementation by configurations.creating {\n    extendsFrom(configurations.testImplementation.get())\n}\n\nval integrationTestRuntimeOnly by configurations.creating {\n    extendsFrom(configurations.testRuntimeOnly.get())\n}',
    tags: ['configurations', 'extendsFrom', 'custom'],
  },
  {
    id: 'kotlin-gradle-l3-021',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'How do you implement an incremental task in Gradle?',
    options: [
      'Use the IncrementalTask base class',
      'Declare @InputFiles with @Incremental and use InputChanges in the task action',
      'Add @Incremental to the task class',
      'Set incremental = true on the task',
    ],
    correctAnswer: 1,
    explanation: 'Incremental tasks use @Incremental on input file properties and receive InputChanges in the task action, which provides information about which input files changed.',
    codeSnippet: 'abstract class IncrementalReverseTask : DefaultTask() {\n    @get:Incremental\n    @get:InputDirectory\n    @get:PathSensitive(PathSensitivity.RELATIVE)\n    abstract val inputDir: DirectoryProperty\n\n    @get:OutputDirectory\n    abstract val outputDir: DirectoryProperty\n\n    @TaskAction\n    fun execute(inputChanges: InputChanges) {\n        inputChanges.getFileChanges(inputDir).forEach { change ->\n            // Process only changed files\n        }\n    }\n}',
    tags: ['tasks', 'incremental-task', 'InputChanges'],
  },
  {
    id: 'kotlin-gradle-l3-022',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is a worker API in Gradle tasks?',
    options: [
      'An API for managing CI/CD workers',
      'An API for distributing work across machines',
      'An API for executing work in parallel using classloader or process isolation',
      'An API for managing Gradle daemon workers',
    ],
    correctAnswer: 2,
    explanation: 'The Worker API allows tasks to execute work items in parallel with optional classloader or process isolation. This enables tasks to parallelize work and avoid classpath conflicts.',
    codeSnippet: 'abstract class MyWorkAction : WorkAction<MyWorkAction.Params> {\n    interface Params : WorkParameters {\n        val inputFile: RegularFileProperty\n    }\n    override fun execute() {\n        // Process inputFile\n    }\n}\n\nabstract class MyTask : DefaultTask() {\n    @get:Inject\n    abstract val workerExecutor: WorkerExecutor\n\n    @TaskAction\n    fun execute() {\n        workerExecutor.noIsolation().submit(MyWorkAction::class) {\n            inputFile.set(/* ... */)\n        }\n    }\n}',
    tags: ['tasks', 'worker-api', 'parallelism'],
  },
  {
    id: 'kotlin-gradle-l3-023',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you publish a custom Gradle plugin to the Gradle Plugin Portal?',
    options: [
      'Use the publish task from maven-publish',
      'Upload the JAR file manually',
      'Use the com.gradle.plugin-publish plugin and configure publishPlugins task',
      'Push to Maven Central',
    ],
    correctAnswer: 2,
    explanation: 'The com.gradle.plugin-publish plugin provides the publishPlugins task that publishes to the Gradle Plugin Portal, making your plugin available to the community.',
    codeSnippet: 'plugins {\n    `java-gradle-plugin`\n    id("com.gradle.plugin-publish") version "1.2.1"\n}\n\ngradlePlugin {\n    plugins {\n        create("myPlugin") {\n            id = "com.example.my-plugin"\n            implementationClass = "com.example.MyPlugin"\n            displayName = "My Plugin"\n            description = "A custom Gradle plugin"\n        }\n    }\n}',
    tags: ['plugins', 'publishing', 'plugin-portal'],
  },
  {
    id: 'kotlin-gradle-l3-024',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'What does the java-gradle-plugin plugin do?',
    options: [
      'Converts Java builds to Gradle',
      'Compiles Java code',
      'Assists in developing Gradle plugins by generating plugin descriptors and setting up testKit',
      'Provides Java compilation tasks',
    ],
    correctAnswer: 2,
    explanation: 'The java-gradle-plugin plugin helps develop Gradle plugins by auto-generating plugin descriptors, configuring publication metadata, and providing TestKit integration for plugin testing.',
    tags: ['plugins', 'java-gradle-plugin', 'development'],
  },
  {
    id: 'kotlin-gradle-l3-025',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you test a Gradle plugin using TestKit?',
    options: [
      'Use mock objects for the Project interface',
      'Use GradleRunner to execute builds with the plugin applied and verify outcomes',
      'Write unit tests against the plugin class',
      'Run the plugin on a sample project manually',
    ],
    correctAnswer: 1,
    explanation: 'Gradle TestKit provides GradleRunner for functional testing of plugins. It executes real builds in temporary directories and lets you verify task outcomes, output, and file contents.',
    codeSnippet: '@Test\nfun `plugin applies successfully`() {\n    val projectDir = tempDir.newFolder()\n    projectDir.resolve("build.gradle.kts").writeText(\"\"\"\n        plugins { id("com.example.my-plugin") }\n    \"\"\".trimIndent())\n    \n    val result = GradleRunner.create()\n        .withProjectDir(projectDir)\n        .withArguments("myTask")\n        .withPluginClasspath()\n        .build()\n    \n    assertEquals(TaskOutcome.SUCCESS, result.task(":myTask")?.outcome)\n}',
    tags: ['plugins', 'testkit', 'testing'],
  },
  {
    id: 'kotlin-gradle-l3-026',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Publishing',
    question: 'How do you publish a Kotlin library with sources and Javadoc JARs?',
    options: [
      'Use a separate publishing plugin',
      'They are published automatically',
      'Add sources and javadoc as dependencies',
      'Configure the publishing extension with withSourcesJar() and withJavadocJar() from the java extension',
    ],
    correctAnswer: 3,
    explanation: 'The java extension provides withSourcesJar() and withJavadocJar() to automatically create and attach source and documentation JARs to publications.',
    codeSnippet: 'java {\n    withSourcesJar()\n    withJavadocJar()\n}\n\npublishing {\n    publications {\n        create<MavenPublication>("maven") {\n            from(components["java"])\n            pom {\n                name.set("My Library")\n                description.set("A Kotlin library")\n            }\n        }\n    }\n}',
    tags: ['publishing', 'sources', 'javadoc'],
  },
  {
    id: 'kotlin-gradle-l3-027',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Publishing',
    question: 'How do you sign published artifacts with GPG?',
    options: [
      'Artifacts are signed automatically',
      'Use the maven-publish plugin signing option',
      'Apply the "signing" plugin and configure it with GPG keys',
      'Use the gpg command manually',
    ],
    correctAnswer: 2,
    explanation: 'The "signing" plugin provides artifact signing. It can use GPG keys from properties, environment variables, or the local GPG agent. Required for publishing to Maven Central.',
    codeSnippet: 'plugins {\n    signing\n    `maven-publish`\n}\n\nsigning {\n    useInMemoryPgpKeys(\n        findProperty("signingKey") as String?,\n        findProperty("signingPassword") as String?\n    )\n    sign(publishing.publications["maven"])\n}',
    tags: ['publishing', 'signing', 'gpg'],
  },
  {
    id: 'kotlin-gradle-l3-028',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'ProGuard/R8',
    question: 'What is R8 in the context of Android/Kotlin builds?',
    options: [
      'A testing framework',
      'A code shrinker, optimizer, and obfuscator that replaces ProGuard for Android builds',
      'A dependency resolver',
      'A build cache server',
    ],
    correctAnswer: 1,
    explanation: 'R8 is Google\'s replacement for ProGuard in Android builds. It performs code shrinking, optimization, obfuscation, and desugaring in a single step, integrated into the Android Gradle Plugin.',
    tags: ['r8', 'proguard', 'android', 'optimization'],
  },
  {
    id: 'kotlin-gradle-l3-029',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'ProGuard/R8',
    question: 'How do you configure ProGuard/R8 rules in a Gradle build?',
    options: [
      'In settings.gradle.kts',
      'In gradle.properties',
      'In proguard-rules.pro files referenced from build.gradle.kts',
      'In build.gradle.kts directly',
    ],
    correctAnswer: 2,
    explanation: 'ProGuard/R8 rules are typically defined in proguard-rules.pro files. The Android Gradle plugin references these files in the buildTypes configuration.',
    codeSnippet: 'android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = true\n            proguardFiles(\n                getDefaultProguardFile("proguard-android-optimize.txt"),\n                "proguard-rules.pro"\n            )\n        }\n    }\n}',
    tags: ['proguard', 'r8', 'rules', 'android'],
  },
  {
    id: 'kotlin-gradle-l3-030',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Build Variants',
    question: 'What are build types and product flavors in Android Gradle?',
    options: [
      'Types of Gradle tasks',
      'Different Kotlin compilation modes',
      'Build types define build characteristics (debug/release); product flavors define different versions of the app (free/paid)',
      'Repository categories',
    ],
    correctAnswer: 2,
    explanation: 'Build types (like debug, release) define how the app is built and packaged. Product flavors define different versions of the app. Combining them creates build variants (e.g., freeDebug, paidRelease).',
    codeSnippet: 'android {\n    buildTypes {\n        release { isMinifyEnabled = true }\n        debug { isDebuggable = true }\n    }\n    productFlavors {\n        flavorDimensions += "version"\n        create("free") { dimension = "version" }\n        create("paid") { dimension = "version" }\n    }\n}',
    tags: ['build-variants', 'android', 'product-flavors'],
  },
  {
    id: 'kotlin-gradle-l3-031',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin DSL',
    question: 'How do you create a custom DSL extension with nested blocks?',
    options: [
      'Define an extension interface with Action<T> parameters for nested configuration',
      'Use data classes',
      'Nested blocks are not supported',
      'Use maps for nested configuration',
    ],
    correctAnswer: 0,
    explanation: 'Nested DSL blocks are created using Action<T> parameters. Gradle\'s ObjectFactory can create nested domain objects that support the DSL style.',
    codeSnippet: 'interface MyExtension {\n    val server: ServerConfig\n    fun server(action: Action<ServerConfig>) {\n        action.execute(server)\n    }\n}\n\ninterface ServerConfig {\n    val host: Property<String>\n    val port: Property<Int>\n}\n\n// Usage in build.gradle.kts:\nmyPlugin {\n    server {\n        host.set("localhost")\n        port.set(8080)\n    }\n}',
    tags: ['kotlin-dsl', 'extension', 'nested-dsl'],
  },
  {
    id: 'kotlin-gradle-l3-032',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin DSL',
    question: 'What is the kotlin-dsl plugin used for?',
    options: [
      'Running Kotlin REPL',
      'Converting Groovy scripts to Kotlin',
      'Developing Gradle plugins and build logic with full Kotlin DSL support and generated accessors',
      'Writing Kotlin source code',
    ],
    correctAnswer: 2,
    explanation: 'The kotlin-dsl plugin is applied in buildSrc or build-logic projects to enable writing Gradle plugins in Kotlin with full IDE support, type-safe accessors, and Kotlin DSL precompiled scripts.',
    codeSnippet: '// buildSrc/build.gradle.kts\nplugins {\n    `kotlin-dsl`\n}\n\nrepositories {\n    mavenCentral()\n}',
    tags: ['kotlin-dsl', 'plugin', 'buildSrc'],
  },
  {
    id: 'kotlin-gradle-l3-033',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Multi-module Projects',
    question: 'What are the pros and cons of buildSrc vs. composite builds for build logic?',
    options: [
      'buildSrc is simpler but invalidates all configuration on changes; composite builds are more modular and avoid full invalidation',
      'buildSrc supports Kotlin DSL but composite builds do not',
      'They are identical',
      'Composite builds are simpler but slower',
    ],
    correctAnswer: 0,
    explanation: 'buildSrc is easy to set up but any change invalidates all build configurations. A composite build (build-logic) is more modular, changes only invalidate dependent projects, and it can be shared across builds.',
    tags: ['buildSrc', 'composite-builds', 'comparison'],
  },
  {
    id: 'kotlin-gradle-l3-034',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Multi-module Projects',
    question: 'What is the "typesafe project accessors" feature?',
    options: [
      'Type-safe access to project properties',
      'Type-safe access to environment variables',
      'Auto-generated accessor methods for tasks',
      'Type-safe references to subprojects in dependency declarations (e.g., projects.core.model instead of project(":core:model"))',
    ],
    correctAnswer: 3,
    explanation: 'Typesafe project accessors generate type-safe references to subprojects, enabling IDE auto-completion and compile-time checking of project references.',
    codeSnippet: '// settings.gradle.kts\nenableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")\n\n// build.gradle.kts\ndependencies {\n    implementation(projects.core.model) // instead of project(":core:model")\n}',
    tags: ['multi-module', 'type-safe', 'project-accessors'],
  },
  {
    id: 'kotlin-gradle-l3-035',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is the purpose of @get:Internal annotation on a task property?',
    options: [
      'Makes the property internal to the module',
      'Marks the property as not an input or output, so it does not affect up-to-date checks or caching',
      'Makes the property private',
      'Marks the property as deprecated',
    ],
    correctAnswer: 1,
    explanation: '@Internal indicates that a task property is used internally but should not be considered for up-to-date checking or build caching. Useful for injected services or intermediate values.',
    tags: ['tasks', 'annotations', 'internal'],
  },
  {
    id: 'kotlin-gradle-l3-036',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What annotation marks that the order of input files does not matter for caching?',
    options: [
      '@NormalizeLineEndings',
      '@Unordered',
      '@get:InputFiles with @get:IgnoreEmptyDirectories and @get:PathSensitive',
      '@Classpath',
    ],
    correctAnswer: 3,
    explanation: '@Classpath annotation on input files means they are treated as a classpath: order matters, but the specific file names/paths in the classpath do not affect the cache key (only content does).',
    tags: ['tasks', 'annotations', 'classpath', 'caching'],
  },
  {
    id: 'kotlin-gradle-l3-037',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you implement a custom dependency resolution strategy to fail on version conflicts?',
    options: [
      'Add a verification plugin',
      'Set strictMode = true',
      'Use resolutionStrategy { failOnVersionConflict() }',
      'Use --strict flag',
    ],
    correctAnswer: 2,
    explanation: 'failOnVersionConflict() causes the build to fail when two dependencies require different versions of the same module, instead of Gradle silently picking the highest version.',
    codeSnippet: 'configurations.all {\n    resolutionStrategy {\n        failOnVersionConflict()\n    }\n}',
    tags: ['dependencies', 'resolution-strategy', 'conflict'],
  },
  {
    id: 'kotlin-gradle-l3-038',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'What is dependency verification in Gradle?',
    options: [
      'Verifying dependency checksums and signatures to ensure integrity and authenticity',
      'Validating dependency licenses',
      'Testing that dependencies work correctly',
      'Checking if dependencies are up to date',
    ],
    correctAnswer: 0,
    explanation: 'Dependency verification (gradle/verification-metadata.xml) verifies checksums (MD5, SHA-256, SHA-512) and PGP signatures of downloaded dependencies to prevent supply chain attacks.',
    codeSnippet: './gradlew --write-verification-metadata sha256,pgp help\n// Generates gradle/verification-metadata.xml',
    tags: ['dependencies', 'verification', 'security'],
  },
  {
    id: 'kotlin-gradle-l3-039',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin Multiplatform',
    question: 'How do you configure Kotlin/Native binary output in KMP?',
    options: [
      'Add binary dependencies',
      'Configure binaries within the target: iosArm64 { binaries { framework { baseName = "shared" } } }',
      'Use the native-binary plugin',
      'Use the native {} block',
    ],
    correctAnswer: 1,
    explanation: 'Native targets can produce executables, static/dynamic libraries, or Apple frameworks. These are configured in the binaries {} block of each native target.',
    codeSnippet: 'kotlin {\n    iosArm64 {\n        binaries {\n            framework {\n                baseName = "SharedKit"\n                isStatic = true\n            }\n        }\n    }\n    linuxX64 {\n        binaries {\n            executable {\n                entryPoint = "main"\n            }\n        }\n    }\n}',
    tags: ['kotlin-multiplatform', 'native', 'binaries'],
  },
  {
    id: 'kotlin-gradle-l3-040',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin Multiplatform',
    question: 'What is CocoaPods integration in KMP?',
    options: [
      'Integration that allows publishing KMP frameworks as CocoaPods pods for iOS consumption',
      'A build server integration',
      'A caching mechanism',
      'A coffee brewing API',
    ],
    correctAnswer: 0,
    explanation: 'The kotlin-cocoapods plugin integrates KMP with the CocoaPods dependency manager, allowing iOS projects to consume KMP frameworks as CocoaPods dependencies.',
    codeSnippet: 'plugins {\n    kotlin("multiplatform")\n    kotlin("native.cocoapods")\n}\n\nkotlin {\n    cocoapods {\n        summary = "Shared module"\n        homepage = "https://example.com"\n        ios.deploymentTarget = "14.0"\n        framework { baseName = "shared" }\n    }\n}',
    tags: ['kotlin-multiplatform', 'cocoapods', 'ios'],
  },
  {
    id: 'kotlin-gradle-l3-041',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you create a Gradle plugin that applies other plugins?',
    options: [
      'List required plugins in plugin.xml',
      'Call project.plugins.apply() or project.pluginManager.apply() in the plugin\'s apply() method',
      'Use the requires {} block in the plugin class',
      'Use @RequiresPlugin annotation',
    ],
    correctAnswer: 1,
    explanation: 'A plugin can apply other plugins programmatically using project.pluginManager.apply() inside its apply() method, ensuring required plugins are present.',
    codeSnippet: 'class MyLibraryPlugin : Plugin<Project> {\n    override fun apply(project: Project) {\n        with(project.pluginManager) {\n            apply("org.jetbrains.kotlin.jvm")\n            apply("java-library")\n            apply("maven-publish")\n        }\n    }\n}',
    tags: ['plugins', 'custom-plugin', 'applying-plugins'],
  },
  {
    id: 'kotlin-gradle-l3-042',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'What is a settings plugin and how does it differ from a project plugin?',
    options: [
      'There is no difference',
      'A settings plugin implements Plugin<Settings> and is applied in settings.gradle.kts, running before project configuration',
      'Settings plugins are deprecated',
      'A settings plugin configures IDE settings',
    ],
    correctAnswer: 1,
    explanation: 'Settings plugins (Plugin<Settings>) are applied in settings.gradle.kts and can configure builds, repositories, and included builds before any project is evaluated.',
    codeSnippet: 'class MySettingsPlugin : Plugin<Settings> {\n    override fun apply(settings: Settings) {\n        settings.dependencyResolutionManagement {\n            repositories {\n                mavenCentral()\n            }\n        }\n    }\n}',
    tags: ['plugins', 'settings-plugin', 'initialization'],
  },
  {
    id: 'kotlin-gradle-l3-043',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Version Catalogs',
    question: 'How do you share a Version Catalog across multiple independent projects?',
    options: [
      'Copy the TOML file',
      'Publish the catalog as a dependency and import it using from() in the versionCatalogs block',
      'Version Catalogs cannot be shared',
      'Use a shared repository',
    ],
    correctAnswer: 1,
    explanation: 'A Version Catalog can be published as a Gradle dependency and imported in other projects using the from() function, ensuring consistent versions across projects.',
    codeSnippet: '// Publishing project\nplugins { `version-catalog` }\ncatalog {\n    versionCatalog { /* define catalog */ }\n}\n\n// Consuming project settings.gradle.kts\ndependencyResolutionManagement {\n    versionCatalogs {\n        create("libs") {\n            from("com.example:version-catalog:1.0")\n        }\n    }\n}',
    tags: ['version-catalogs', 'sharing', 'publishing'],
  },
  {
    id: 'kotlin-gradle-l3-044',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Version Catalogs',
    question: 'How do you override a version from a published Version Catalog?',
    options: [
      'Versions from published catalogs cannot be overridden',
      'Edit the TOML file',
      'Create a local TOML file that takes precedence',
      'Use version() override in the versionCatalogs block in settings.gradle.kts',
    ],
    correctAnswer: 3,
    explanation: 'When importing a published catalog, you can override specific versions using the version() method in the versionCatalogs configuration block.',
    codeSnippet: 'dependencyResolutionManagement {\n    versionCatalogs {\n        create("libs") {\n            from("com.example:catalog:1.0")\n            version("kotlin", "1.9.20") // override\n        }\n    }\n}',
    tags: ['version-catalogs', 'override', 'versions'],
  },
  {
    id: 'kotlin-gradle-l3-045',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Build Cache',
    question: 'Why might a task not be cacheable even with @CacheableTask?',
    options: [
      'The JVM version is too old',
      'The task is too fast to cache',
      'The build cache is full',
      'Missing @PathSensitive, non-serializable inputs, or volatile inputs like timestamps',
    ],
    correctAnswer: 3,
    explanation: 'Tasks can fail to cache if file inputs lack @PathSensitive (making the cache key machine-specific), inputs contain timestamps or absolute paths, or inputs are not properly serializable.',
    tags: ['build-cache', 'troubleshooting', 'cacheable'],
  },
  {
    id: 'kotlin-gradle-l3-046',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'KSP',
    question: 'How do you pass arguments to a KSP processor?',
    options: [
      'kspArgs = mapOf("key" to "value")',
      'kspConfig { set("key", "value") }',
      'tasks.ksp { arguments["key"] = "value" }',
      'ksp { arg("key", "value") }',
    ],
    correctAnswer: 3,
    explanation: 'KSP processor arguments are passed using the ksp {} extension block with the arg() function. These arguments are available to the processor via SymbolProcessorEnvironment.options.',
    codeSnippet: 'ksp {\n    arg("roomSchemaLocation", "$projectDir/schemas")\n    arg("generateKotlin", "true")\n}',
    tags: ['ksp', 'arguments', 'configuration'],
  },
  {
    id: 'kotlin-gradle-l3-047',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'How do you use Gradle\'s FileCollection and FileTree APIs?',
    options: [
      'FileCollection is for source files; FileTree is for resources',
      'They are just regular Java collections',
      'FileCollection represents a flat set of files; FileTree adds directory hierarchy with pattern matching',
      'They are deprecated in favor of Path API',
    ],
    correctAnswer: 2,
    explanation: 'FileCollection is a lazy, immutable set of files. FileTree extends it with directory hierarchy and glob pattern matching (include/exclude). Both integrate with task inputs/outputs.',
    codeSnippet: 'val sources: FileCollection = files("src/main/kotlin")\nval javaFiles: FileTree = fileTree("src") {\n    include("**/*.java")\n    exclude("**/generated/**")\n}',
    tags: ['tasks', 'file-collection', 'file-tree'],
  },
  {
    id: 'kotlin-gradle-l3-048',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you create a resolvable vs. consumable configuration?',
    options: [
      'Use different base classes',
      'Set isCanBeResolved and isCanBeConsumed flags to control whether a configuration can resolve dependencies or be consumed by other projects',
      'All configurations are both',
      'Use @Resolvable and @Consumable annotations',
    ],
    correctAnswer: 1,
    explanation: 'Configurations have two roles: resolvable (can compute a dependency graph) and consumable (can be consumed by other projects). Setting these flags correctly is important for variant-aware resolution.',
    codeSnippet: 'configurations {\n    create("myResolvable") {\n        isCanBeResolved = true\n        isCanBeConsumed = false\n    }\n    create("myConsumable") {\n        isCanBeResolved = false\n        isCanBeConsumed = true\n    }\n}',
    tags: ['configurations', 'resolvable', 'consumable'],
  },
  {
    id: 'kotlin-gradle-l3-049',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is task avoidance and why is it important?',
    options: [
      'Disabling tasks in specific build types',
      'Using register() and configureEach() to avoid eagerly creating/configuring tasks that may not be needed',
      'Removing unused task classes',
      'Skipping unnecessary tasks in CI',
    ],
    correctAnswer: 1,
    explanation: 'Task avoidance means deferring task creation and configuration until the task is actually needed. Using register() instead of create() and configureEach() instead of all() reduces configuration time.',
    tags: ['tasks', 'task-avoidance', 'performance'],
  },
  {
    id: 'kotlin-gradle-l3-050',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Configuration Cache',
    question: 'How do you detect configuration cache problems in your build?',
    options: [
      'Run with --info flag',
      'Check the .gradle directory',
      'Run with --configuration-cache and Gradle will report any incompatibilities',
      'Use a special analysis plugin',
    ],
    correctAnswer: 2,
    explanation: 'Running with --configuration-cache (or enabling it in gradle.properties) causes Gradle to detect and report configuration cache incompatibilities, including problematic API usage.',
    codeSnippet: './gradlew build --configuration-cache\n# or in gradle.properties:\n# org.gradle.configuration-cache=true\n# org.gradle.configuration-cache.problems=warn',
    tags: ['configuration-cache', 'debugging', 'problems'],
  },
  {
    id: 'kotlin-gradle-l3-051',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'How do you use Gradle\'s layout API to reference build directories?',
    options: [
      'Use System.getProperty("user.dir")',
      'Use project.layout.buildDirectory and project.layout.projectDirectory for configuration cache compatible file references',
      'Use project.buildDir',
      'Use File("build/...")',
    ],
    correctAnswer: 1,
    explanation: 'The layout API provides lazy, configuration cache compatible references to project directories. project.buildDir is deprecated in favor of layout.buildDirectory.',
    codeSnippet: 'tasks.register<MyTask>("generate") {\n    outputFile.set(layout.buildDirectory.file("generated/output.txt"))\n    inputDir.set(layout.projectDirectory.dir("src/templates"))\n}',
    tags: ['tasks', 'layout-api', 'configuration-cache'],
  },
  {
    id: 'kotlin-gradle-l3-052',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'What is a rich version constraint in Gradle?',
    options: [
      'A version that includes release notes',
      'A constraint on the number of versions',
      'A version constraint that can express require, prefer, strictly, and reject constraints',
      'A version with many digits',
    ],
    correctAnswer: 2,
    explanation: 'Rich version constraints allow fine-grained control: require() sets a hard lower bound, prefer() is a soft preference, strictly() forces exact version, and reject() excludes specific versions.',
    codeSnippet: 'dependencies {\n    implementation("com.example:lib") {\n        version {\n            require("1.0")\n            prefer("1.2")\n            reject("1.1.0")\n        }\n    }\n}',
    tags: ['dependencies', 'rich-version', 'constraints'],
  },
  {
    id: 'kotlin-gradle-l3-053',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Multi-module Projects',
    question: 'How do you avoid circular dependencies between modules?',
    options: [
      'Extract shared code into a common module, use dependency inversion, or restructure module boundaries',
      'Gradle handles circular dependencies automatically',
      'Declare both dependencies as compileOnly',
      'Use the circular-dependency plugin',
    ],
    correctAnswer: 0,
    explanation: 'Circular dependencies are not allowed in Gradle. Common solutions include extracting shared interfaces/models into a separate module, applying dependency inversion principle, or using APIs/SPIs.',
    tags: ['multi-module', 'circular-dependency', 'architecture'],
  },
  {
    id: 'kotlin-gradle-l3-054',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you use Gradle\'s service injection in custom tasks?',
    options: [
      'Annotate properties with @get:Inject to have Gradle inject services like FileSystemOperations or ExecOperations',
      'Access services through project.services',
      'Use a DI framework like Dagger',
      'Use constructor injection',
    ],
    correctAnswer: 0,
    explanation: 'Gradle provides service injection via @Inject annotation. Services like FileSystemOperations, ExecOperations, and ArchiveOperations are available without referencing the Project object.',
    codeSnippet: 'abstract class MyTask : DefaultTask() {\n    @get:Inject\n    abstract val fs: FileSystemOperations\n    \n    @get:Inject\n    abstract val exec: ExecOperations\n    \n    @TaskAction\n    fun execute() {\n        fs.copy {\n            from("src")\n            into("dest")\n        }\n    }\n}',
    tags: ['tasks', 'service-injection', 'inject'],
  },
  {
    id: 'kotlin-gradle-l3-055',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is a shared build service in Gradle?',
    options: [
      'A remote build server',
      'A shared dependency cache',
      'A CI/CD integration service',
      'A singleton service that can be shared across tasks with controlled parallelism and lifecycle',
    ],
    correctAnswer: 3,
    explanation: 'BuildService<T> is a managed singleton that tasks can share. It supports controlled parallelism (maxParallelUsages) and has a lifecycle tied to the build. Useful for database connections, HTTP clients, etc.',
    codeSnippet: 'abstract class DatabaseService : BuildService<DatabaseService.Params> {\n    interface Params : BuildServiceParameters {\n        val connectionUrl: Property<String>\n    }\n    // Service implementation\n}\n\nval dbService = gradle.sharedServices.registerIfAbsent("db", DatabaseService::class) {\n    parameters.connectionUrl.set("jdbc:h2:mem:test")\n    maxParallelUsages.set(1)\n}',
    tags: ['tasks', 'build-service', 'shared-state'],
  },
  {
    id: 'kotlin-gradle-l3-056',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin Multiplatform',
    question: 'How do you configure Kotlin/JS target in KMP?',
    options: [
      'kotlin { js() }',
      'plugins { kotlin("js") }',
      'kotlin { javascript() }',
      'kotlin { js { browser(); nodejs() } } to configure both browser and Node.js environments',
    ],
    correctAnswer: 3,
    explanation: 'The js target in KMP can be configured for browser and/or Node.js environments. Each environment has its own testing and packaging configuration.',
    codeSnippet: 'kotlin {\n    js {\n        browser {\n            testTask {\n                useKarma { useChromeHeadless() }\n            }\n            webpackTask {\n                outputFileName = "app.js"\n            }\n        }\n        nodejs()\n        binaries.executable()\n    }\n}',
    tags: ['kotlin-multiplatform', 'js', 'browser', 'nodejs'],
  },
  {
    id: 'kotlin-gradle-l3-057',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Publishing',
    question: 'How do you configure a MavenPublication with custom POM metadata?',
    options: [
      'Use a separate POM plugin',
      'Edit the POM file manually after generation',
      'Use the pom {} block within the publication configuration to set name, description, licenses, developers, and SCM info',
      'Add metadata as gradle.properties',
    ],
    correctAnswer: 2,
    explanation: 'The pom {} block in MavenPublication allows configuring all POM metadata including name, description, URL, licenses, developers, SCM connection, etc.',
    codeSnippet: 'publishing {\n    publications {\n        create<MavenPublication>("maven") {\n            from(components["java"])\n            pom {\n                name.set("My Library")\n                description.set("A great library")\n                url.set("https://github.com/example/lib")\n                licenses {\n                    license {\n                        name.set("Apache-2.0")\n                        url.set("https://www.apache.org/licenses/LICENSE-2.0")\n                    }\n                }\n                scm {\n                    url.set("https://github.com/example/lib")\n                }\n            }\n        }\n    }\n}',
    tags: ['publishing', 'pom', 'metadata'],
  },
  {
    id: 'kotlin-gradle-l3-058',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'What are alignment constraints and why are they useful?',
    options: [
      'Constraints for text alignment in reports',
      'Constraints that align build outputs',
      'Virtual platforms that ensure related modules use the same version (e.g., all Jackson modules)',
      'Constraints that align code formatting',
    ],
    correctAnswer: 2,
    explanation: 'Alignment constraints (via virtual platforms) ensure that a group of related modules (like all Jackson or Kotlin libraries) resolve to the same version, preventing incompatibilities.',
    codeSnippet: 'dependencies {\n    components {\n        withModule<AlignmentRule>("com.fasterxml.jackson.core:jackson-core") {\n            belongsTo("com.fasterxml.jackson:jackson-bom:\${id.version}")\n        }\n    }\n}',
    tags: ['dependencies', 'alignment', 'virtual-platform'],
  },
  {
    id: 'kotlin-gradle-l3-059',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Convention Plugins',
    question: 'How do you apply a convention plugin from build-logic to a subproject?',
    options: [
      'Include it in settings.gradle.kts',
      'apply(plugin = "convention-name")',
      'Use the plugin ID (file name without .gradle.kts extension) in the plugins block',
      'Import the convention class',
    ],
    correctAnswer: 2,
    explanation: 'Precompiled script plugins get an ID based on their file name. A file named kotlin-library-conventions.gradle.kts gets the ID "kotlin-library-conventions".',
    codeSnippet: '// build-logic/src/main/kotlin/kotlin-library-conventions.gradle.kts\n// ... convention logic ...\n\n// app/build.gradle.kts\nplugins {\n    id("kotlin-library-conventions")\n}',
    tags: ['convention-plugins', 'applying', 'id'],
  },
  {
    id: 'kotlin-gradle-l3-060',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Gradle Properties',
    question: 'What is the effect of kotlin.compiler.execution.strategy?',
    options: [
      'Controls how the Kotlin compiler is executed: in-process, out-of-process (daemon), or both',
      'Controls optimization level',
      'Controls compilation order',
      'Controls Kotlin code style',
    ],
    correctAnswer: 0,
    explanation: 'kotlin.compiler.execution.strategy controls the Kotlin compiler execution strategy. "daemon" (default) runs the compiler in a separate daemon process. "in-process" runs it within the Gradle daemon.',
    codeSnippet: '# gradle.properties\nkotlin.compiler.execution.strategy=daemon',
    tags: ['properties', 'compiler', 'execution-strategy'],
  },
  {
    id: 'kotlin-gradle-l3-061',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is the @NormalizeLineEndings annotation used for?',
    options: [
      'Setting the default line ending style',
      'Validating line endings',
      'Converting line endings in source files',
      'Normalizing line endings in input files for cache key calculation, ignoring OS differences',
    ],
    correctAnswer: 3,
    explanation: '@NormalizeLineEndings ensures that different line endings (CRLF vs LF) do not invalidate the build cache when the same content is checked out on different operating systems.',
    tags: ['tasks', 'annotations', 'line-endings', 'caching'],
  },
  {
    id: 'kotlin-gradle-l3-062',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you configure repository content filtering?',
    options: [
      'Configure a proxy server',
      'Use exclude blocks in dependencies',
      'Use content {} block within repository declarations to restrict which dependencies come from which repositories',
      'Use repository filters in gradle.properties',
    ],
    correctAnswer: 2,
    explanation: 'Repository content filtering restricts which artifacts are resolved from which repositories, improving security and performance by preventing unintended repository fallback.',
    codeSnippet: 'repositories {\n    mavenCentral {\n        content {\n            excludeGroup("com.example.internal")\n        }\n    }\n    maven("https://internal.repo.com") {\n        content {\n            includeGroup("com.example.internal")\n        }\n    }\n}',
    tags: ['repositories', 'content-filtering', 'security'],
  },
  {
    id: 'kotlin-gradle-l3-063',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Publishing',
    question: 'How do you configure publishing to multiple Maven repositories?',
    options: [
      'Create multiple build.gradle.kts files',
      'Define multiple repositories in the publishing { repositories {} } block',
      'Create separate publications for each repository',
      'Use multiple maven-publish plugins',
    ],
    correctAnswer: 1,
    explanation: 'You can define multiple target repositories in the publishing {} block. Gradle generates publish tasks for each combination of publication and repository.',
    codeSnippet: 'publishing {\n    repositories {\n        maven {\n            name = "snapshot"\n            url = uri("https://repo.example.com/snapshots")\n        }\n        maven {\n            name = "release"\n            url = uri("https://repo.example.com/releases")\n        }\n    }\n}\n// Tasks: publishMavenPublicationToSnapshotRepository\n//        publishMavenPublicationToReleaseRepository',
    tags: ['publishing', 'repositories', 'multiple'],
  },
  {
    id: 'kotlin-gradle-l3-064',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Source Sets',
    question: 'How do you set up integration tests as a separate source set with its own task?',
    options: [
      'Both B and C are valid approaches',
      'Create a new source set, configuration, and task that extends the test infrastructure',
      'Use a plugin like test-sets',
      'Just add files to src/test/',
    ],
    correctAnswer: 0,
    explanation: 'You can manually create a source set, configurations, and test task, or use the JVM Test Suite plugin (Gradle 7.3+) or community plugins for a more streamlined setup.',
    codeSnippet: 'testing {\n    suites {\n        register<JvmTestSuite>("integrationTest") {\n            dependencies {\n                implementation(project())\n            }\n            targets {\n                all {\n                    testTask.configure {\n                        shouldRunAfter(tasks.test)\n                    }\n                }\n            }\n        }\n    }\n}\n\ntasks.check {\n    dependsOn(testing.suites.named("integrationTest"))\n}',
    tags: ['source-sets', 'integration-test', 'test-suite'],
  },
  {
    id: 'kotlin-gradle-l3-065',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'How do you use the ObjectFactory to create domain objects in plugins?',
    options: [
      'Use new keyword',
      'Use reflection',
      'Use project.objects to create managed types like Properties, NamedDomainObjectContainer, etc.',
      'Use Kotlin data classes directly',
    ],
    correctAnswer: 2,
    explanation: 'ObjectFactory (project.objects) creates managed objects that integrate with Gradle\'s property and dependency management. It creates Properties, FileCollections, NamedDomainObjectContainers, etc.',
    codeSnippet: 'class MyPlugin : Plugin<Project> {\n    override fun apply(project: Project) {\n        val ext = project.extensions.create<MyExtension>("myPlugin")\n        // Properties are created automatically for abstract getters\n    }\n}\n\n// For NamedDomainObjectContainer:\nval servers = project.objects.domainObjectContainer(ServerConfig::class)',
    tags: ['plugins', 'object-factory', 'managed-types'],
  },
  {
    id: 'kotlin-gradle-l3-066',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin DSL',
    question: 'What are named domain object containers and how are they used in Kotlin DSL?',
    options: [
      'List of project names',
      'Containers for environment variables',
      'Collections of named objects',
      'Type-safe containers that allow creating and configuring named objects with DSL syntax, like publications or source sets',
    ],
    correctAnswer: 3,
    explanation: 'NamedDomainObjectContainer<T> is a collection of uniquely named objects that supports DSL-style configuration. Examples include sourceSets, publications, and custom containers in plugins.',
    codeSnippet: 'interface DeploymentConfig {\n    val name: String\n    val url: Property<String>\n    val token: Property<String>\n}\n\n// In extension:\nval deployments: NamedDomainObjectContainer<DeploymentConfig>\n\n// Usage:\nmyPlugin {\n    deployments {\n        register("staging") {\n            url.set("https://staging.example.com")\n        }\n        register("production") {\n            url.set("https://example.com")\n        }\n    }\n}',
    tags: ['kotlin-dsl', 'named-domain-object', 'container'],
  },
  {
    id: 'kotlin-gradle-l3-067',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Build Cache',
    question: 'How do you debug build cache misses?',
    options: [
      'Use the cache-debug plugin',
      'Check the .gradle directory',
      'Use --info or Gradle build scans to see why task outputs were not reused from cache',
      'Check the remote cache server logs only',
    ],
    correctAnswer: 2,
    explanation: 'Running with --info shows cache miss reasons. Gradle build scans provide detailed cache information including input comparisons. The Gradle Enterprise tool adds even more cache debugging capabilities.',
    codeSnippet: './gradlew build --info 2>&1 | grep -A2 "not cached"\n# Or use build scan:\n./gradlew build --scan',
    tags: ['build-cache', 'debugging', 'cache-miss'],
  },
  {
    id: 'kotlin-gradle-l3-068',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'KAPT',
    question: 'How do you make KAPT incremental?',
    options: [
      'Use the incremental-kapt plugin',
      'KAPT cannot be made incremental',
      'KAPT is always incremental',
      'Set kapt.incremental.apt=true in gradle.properties and ensure the annotation processor supports it',
    ],
    correctAnswer: 3,
    explanation: 'KAPT incremental processing requires both the gradle.properties setting and support from the annotation processor. Many processors like Room and Dagger now support incremental processing.',
    codeSnippet: '# gradle.properties\nkapt.incremental.apt=true\nkapt.use.worker.api=true',
    tags: ['kapt', 'incremental', 'performance'],
  },
  {
    id: 'kotlin-gradle-l3-069',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you implement version alignment for a group of libraries?',
    options: [
      'Use a BOM only',
      'Pin each version individually',
      'Set group version in gradle.properties',
      'Use a virtual BOM platform with belongsTo in a ComponentMetadataRule',
    ],
    correctAnswer: 3,
    explanation: 'When a BOM is not published by the library authors, you can create a ComponentMetadataRule that uses belongsTo() to define a virtual platform, ensuring all modules of the group use the same version.',
    codeSnippet: 'class KotlinAlignment : ComponentMetadataRule {\n    override fun execute(ctx: ComponentMetadataContext) {\n        ctx.details.belongsTo(\n            "org.jetbrains.kotlin:kotlin-bom:${ctx.details.id.version}",\n            false\n        )\n    }\n}\n\ndependencies {\n    components {\n        all<KotlinAlignment>()\n    }\n}',
    tags: ['dependencies', 'alignment', 'component-metadata'],
  },
  {
    id: 'kotlin-gradle-l3-070',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin DSL',
    question: 'How do you handle type-safe access to project extensions from plugins applied at runtime?',
    options: [
      'Cast from extensions.getByName("name")',
      'Use the withType or configure extension methods',
      'Use extensions.getByType<T>()',
      'All of the above are valid, with getByType<T>() being preferred when the type is known',
    ],
    correctAnswer: 3,
    explanation: 'Type-safe access depends on how the plugin is applied. For statically known plugins, generated accessors work. For dynamic plugins, use extensions.getByType<T>() or extensions.configure<T>().',
    codeSnippet: '// When type is known at compile time:\nextensions.configure<KotlinJvmExtension> {\n    jvmToolchain(17)\n}\n\n// Or:\nthe<KotlinJvmExtension>().jvmToolchain(17)',
    tags: ['kotlin-dsl', 'extensions', 'type-safe'],
  },
  {
    id: 'kotlin-gradle-l3-071',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Multi-module Projects',
    question: 'What is the "api" vs "implementation" impact on compilation in multi-module projects?',
    options: [
      '"implementation" compiles faster but uses more memory',
      '"api" compiles faster',
      '"api" dependencies are visible to downstream modules, triggering recompilation when changed; "implementation" dependencies are hidden, limiting recompilation scope',
      'No impact on compilation',
    ],
    correctAnswer: 2,
    explanation: 'In multi-module builds, "implementation" dependencies are hidden from consumers, so changes to them only recompile the declaring module. "api" changes trigger recompilation of all dependent modules.',
    tags: ['multi-module', 'api', 'implementation', 'compilation'],
  },
  {
    id: 'kotlin-gradle-l3-072',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you implement a Gradle plugin that adds a custom dependency configuration?',
    options: [
      'Edit the configurations block in build.gradle.kts',
      'Use project.configurations.create() or register() in the plugin\'s apply() method with appropriate attributes',
      'Modify settings.gradle.kts',
      'Override the Configuration class',
    ],
    correctAnswer: 1,
    explanation: 'Plugins can create custom configurations with specific roles, attributes, and extends-from relationships to integrate with Gradle\'s dependency management system.',
    codeSnippet: 'class MyPlugin : Plugin<Project> {\n    override fun apply(project: Project) {\n        val myConfig = project.configurations.create("myConfiguration") {\n            isCanBeConsumed = false\n            isCanBeResolved = true\n            extendsFrom(project.configurations.getByName("implementation"))\n            attributes {\n                attribute(Usage.USAGE_ATTRIBUTE, project.objects.named(Usage.JAVA_RUNTIME))\n            }\n        }\n    }\n}',
    tags: ['plugins', 'configurations', 'custom'],
  },
  {
    id: 'kotlin-gradle-l3-073',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Kotlin Multiplatform',
    question: 'What is cinterop in Kotlin/Native?',
    options: [
      'A cross-compilation tool',
      'A coroutine interception mechanism',
      'A CI tool',
      'A mechanism for Kotlin/Native to interoperate with C libraries by generating Kotlin bindings from .def files',
    ],
    correctAnswer: 3,
    explanation: 'cinterop generates Kotlin bindings for C libraries. A .def file describes the C library, and the Kotlin/Native compiler generates corresponding Kotlin stubs.',
    codeSnippet: 'kotlin {\n    linuxX64 {\n        compilations.getByName("main") {\n            cinterops {\n                create("mylib") {\n                    defFile(project.file("src/nativeInterop/cinterop/mylib.def"))\n                    packageName("mylib")\n                }\n            }\n        }\n    }\n}',
    tags: ['kotlin-multiplatform', 'cinterop', 'native'],
  },
  {
    id: 'kotlin-gradle-l3-074',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Gradle Properties',
    question: 'What is the difference between -P, -D, and environment variables for passing configuration?',
    options: [
      '-P sets project properties (findProperty()), -D sets JVM system properties (System.getProperty()), env vars are accessed via System.getenv()',
      '-P is for plugins, -D is for dependencies',
      '-P is deprecated in favor of -D',
      'They are all the same',
    ],
    correctAnswer: 0,
    explanation: 'These three mechanisms have different scopes: -P for Gradle project properties, -D for JVM system properties, and environment variables for OS-level configuration. Each is accessed differently in build scripts.',
    tags: ['properties', 'configuration', 'mechanisms'],
  },
  {
    id: 'kotlin-gradle-l3-075',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'How do you implement a task with validation rules?',
    options: [
      'Add a separate validation task',
      'Use the validate {} block',
      'Use @get:Optional for optional inputs and implement validateInputs() or let Gradle validate annotations automatically',
      'Add assertions in the task action',
    ],
    correctAnswer: 2,
    explanation: 'Gradle validates task inputs/outputs based on annotations. @Optional marks non-required inputs. You can also add validation logic in the task action or use @ReplacedBy/@Deprecated for migration.',
    codeSnippet: 'abstract class MyTask : DefaultTask() {\n    @get:Input\n    abstract val requiredParam: Property<String>\n    \n    @get:Input\n    @get:Optional\n    abstract val optionalParam: Property<String>\n    \n    @get:InputFile\n    @get:SkipWhenEmpty\n    abstract val inputFile: RegularFileProperty\n}',
    tags: ['tasks', 'validation', 'annotations'],
  },
  {
    id: 'kotlin-gradle-l3-076',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Publishing',
    question: 'What is Gradle Module Metadata and when is it published?',
    options: [
      'It is published alongside POM files by maven-publish and provides richer dependency information like variants and constraints',
      'It is never published automatically',
      'It replaces POM files entirely',
      'It is only for internal builds',
    ],
    correctAnswer: 0,
    explanation: 'Gradle Module Metadata (.module files) is automatically published by the maven-publish plugin alongside POM files. It provides additional metadata like variants, capabilities, and rich dependency constraints.',
    tags: ['publishing', 'module-metadata', 'maven-publish'],
  },
  {
    id: 'kotlin-gradle-l3-077',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Configuration Cache',
    question: 'What is a ValueSource in the context of configuration cache?',
    options: [
      'A dependency source',
      'A source code generator',
      'A data source for databases',
      'A way to read external values (like file contents, system properties) at configuration time while remaining configuration cache compatible',
    ],
    correctAnswer: 3,
    explanation: 'ValueSource<T, P> computes values at configuration time in a controlled way. Gradle tracks which ValueSources were used and invalidates the configuration cache when their values change.',
    codeSnippet: 'abstract class GitCommitHash : ValueSource<String, ValueSourceParameters.None> {\n    override fun obtain(): String {\n        return Runtime.getRuntime()\n            .exec("git rev-parse --short HEAD")\n            .inputStream.bufferedReader().readText().trim()\n    }\n}\n\nval gitHash = providers.of(GitCommitHash::class) {}\nversion = gitHash.get()',
    tags: ['configuration-cache', 'value-source', 'providers'],
  },
  {
    id: 'kotlin-gradle-l3-078',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'What are dependency attributes and how do they affect resolution?',
    options: [
      'Key-value pairs on configurations that Gradle uses for variant-aware matching during dependency resolution',
      'Version qualifiers',
      'Metadata tags for documentation',
      'Dependency descriptions',
    ],
    correctAnswer: 0,
    explanation: 'Attributes (like org.gradle.usage, org.gradle.jvm.version) on configurations enable variant-aware resolution. Gradle matches consumer attributes against producer variants to select the best match.',
    codeSnippet: 'configurations.create("myConfig") {\n    attributes {\n        attribute(Usage.USAGE_ATTRIBUTE, objects.named(Usage.JAVA_API))\n        attribute(TargetJvmVersion.TARGET_JVM_VERSION_ATTRIBUTE, 17)\n    }\n}',
    tags: ['dependencies', 'attributes', 'variant-resolution'],
  },
  {
    id: 'kotlin-gradle-l3-079',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Plugins',
    question: 'How do you create a Gradle plugin with multiple cooperating tasks?',
    options: [
      'Register tasks in the plugin, wire their inputs/outputs using Property API, and optionally add a lifecycle task that depends on all of them',
      'Create a single mega-task',
      'Register all tasks independently',
      'Use task inheritance',
    ],
    correctAnswer: 0,
    explanation: 'Well-designed plugins create multiple focused tasks wired together. Use Property API to connect outputs to inputs (creating implicit dependencies) and a lifecycle task as an entry point.',
    codeSnippet: 'class CodeGenPlugin : Plugin<Project> {\n    override fun apply(project: Project) {\n        val generate = project.tasks.register<GenerateTask>("generateCode") {\n            outputDir.set(project.layout.buildDirectory.dir("generated"))\n        }\n        project.tasks.register<CompileTask>("compileGenerated") {\n            sourceDir.set(generate.flatMap { it.outputDir })\n        }\n    }\n}',
    tags: ['plugins', 'task-wiring', 'design'],
  },
  {
    id: 'kotlin-gradle-l3-080',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Build Variants',
    question: 'How do you configure source set-specific dependencies in Android Gradle?',
    options: [
      'Use flavored repositories',
      'Create separate build.gradle.kts files for each variant',
      'Use if statements to check build variant',
      'Use configuration names like debugImplementation, releaseImplementation, freeDebugImplementation',
    ],
    correctAnswer: 3,
    explanation: 'Android Gradle Plugin creates dependency configurations for each build type and flavor combination. Use the variant-specific configuration name to add dependencies for specific variants.',
    codeSnippet: 'dependencies {\n    implementation("com.example:core:1.0")\n    debugImplementation("com.example:debug-tools:1.0")\n    releaseImplementation("com.example:analytics:1.0")\n    freeImplementation("com.example:ads:1.0")\n}',
    tags: ['android', 'build-variants', 'dependencies'],
  },
  {
    id: 'kotlin-gradle-l3-081',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Tasks',
    question: 'What is the difference between @SkipWhenEmpty and @Optional on input files?',
    options: [
      '@SkipWhenEmpty skips the task if the file collection is empty (NO-SOURCE); @Optional allows the property to be null without failing validation',
      '@Optional skips the task; @SkipWhenEmpty allows null',
      '@SkipWhenEmpty is deprecated',
      'They are the same',
    ],
    correctAnswer: 0,
    explanation: '@SkipWhenEmpty on an input file collection causes the task to be skipped (marked as NO-SOURCE) when there are no files. @Optional allows a property to have no value without causing a validation error.',
    tags: ['tasks', 'annotations', 'skip-when-empty', 'optional'],
  },
  {
    id: 'kotlin-gradle-l3-082',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Incremental Compilation',
    question: 'What is the Kotlin incremental compilation classpath snapshot?',
    options: [
      'A backup of the classpath',
      'A Gradle build cache entry',
      'A serialized representation of the classpath used to detect ABI changes and determine what needs recompilation',
      'A list of all compiled classes',
    ],
    correctAnswer: 2,
    explanation: 'The Kotlin compiler creates classpath snapshots (ABI fingerprints) to detect changes in dependencies. Only ABI-breaking changes (public API changes) trigger recompilation of dependent files.',
    tags: ['incremental-compilation', 'classpath-snapshot', 'abi'],
  },
  {
    id: 'kotlin-gradle-l3-083',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Dependency Management',
    question: 'How do you use Gradle\'s artifact transforms?',
    options: [
      'Register TransformAction implementations that transform artifacts from one format to another during resolution',
      'Use transform {} blocks in dependencies',
      'Modify artifacts at download time',
      'Manually transform files in a task',
    ],
    correctAnswer: 0,
    explanation: 'Artifact transforms convert dependency artifacts from one type to another (e.g., unzipping AARs, extracting classes). They are registered globally and applied based on matching attributes.',
    codeSnippet: 'abstract class UnzipTransform : TransformAction<TransformParameters.None> {\n    @get:InputArtifact\n    abstract val inputArtifact: Provider<FileSystemLocation>\n    \n    override fun transform(outputs: TransformOutputs) {\n        val input = inputArtifact.get().asFile\n        val outputDir = outputs.dir(input.nameWithoutExtension)\n        input.unzipTo(outputDir)\n    }\n}\n\ndependencies {\n    registerTransform(UnzipTransform::class) {\n        from.attribute(ArtifactTypeDefinition.ARTIFACT_TYPE_ATTRIBUTE, "zip")\n        to.attribute(ArtifactTypeDefinition.ARTIFACT_TYPE_ATTRIBUTE, "directory")\n    }\n}',
    tags: ['dependencies', 'artifact-transforms', 'resolution'],
  },
  {
    id: 'kotlin-gradle-l3-084',
    language: 'kotlin',
    level: 'level3',
    category: 'Gradle & Build',
    subcategory: 'Gradle Properties',
    question: 'How do you use providers to lazily read properties in a configuration cache compatible way?',
    options: [
      'Use findProperty() directly',
      'Use providers.gradleProperty(), providers.systemProperty(), and providers.environmentVariable()',
      'Use lazy {} blocks',
      'Properties are always lazy',
    ],
    correctAnswer: 1,
    explanation: 'The providers API creates Provider<String> instances that lazily read properties. Unlike direct property access, these integrate with the configuration cache and task dependency tracking.',
    codeSnippet: 'val myProp: Provider<String> = providers.gradleProperty("myProp")\nval envVar: Provider<String> = providers.environmentVariable("MY_ENV")\nval sysProp: Provider<String> = providers.systemProperty("my.system.prop")\n\ntasks.register<MyTask>("myTask") {\n    config.set(myProp.orElse("default"))\n}',
    tags: ['providers', 'properties', 'configuration-cache'],
  },
];

export default questions;
