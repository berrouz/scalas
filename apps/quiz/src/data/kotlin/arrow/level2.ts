import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-arrow-l2-001',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the difference between either { } and nullable { } builders in Arrow?',
    options: [
      'either { } is deprecated in favor of nullable { }',
      'either { } returns Either<E, A>, nullable { } returns A?',
      'nullable { } is for coroutines only',
      'They are identical',
    ],
    correctAnswer: 1,
    explanation: 'The either { } builder provides a Raise<E> context and returns Either<E, A>. The nullable { } builder provides a Raise<Null> context and returns A?, bridging to Kotlin\'s nullable type system.',
    codeSnippet: 'val either: Either<String, Int> = either {\n  val a = Right(1).bind()\n  a + 1\n}\n\nval nullable: Int? = nullable {\n  val a = 1.takeIf { it > 0 }.bind()\n  a + 1\n}',
    tags: ['raise-dsl', 'nullable', 'either'],
  },
  {
    id: 'kotlin-arrow-l2-002',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does the Raise DSL handle error accumulation with zipOrAccumulate?',
    options: [
      'It stops at the first error',
      'It silently ignores errors',
      'It runs all validations and collects errors into a NonEmptyList',
      'It throws a CompositeException',
    ],
    correctAnswer: 2,
    explanation: 'zipOrAccumulate runs all provided computations regardless of individual failures and accumulates all errors into a NonEmptyList. This is ideal for form validation where you want all errors at once.',
    codeSnippet: 'val result = either {\n  zipOrAccumulate(\n    { ensure(name.isNotBlank()) { "Name required" } },\n    { ensure(age >= 0) { "Age must be positive" } },\n    { ensure(email.contains("@")) { "Invalid email" } }\n  ) { _, _, _ -> User(name, age, email) }\n}',
    tags: ['raise-dsl', 'zipOrAccumulate', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l2-003',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'What is the advantage of typed errors over exceptions?',
    options: [
      'Typed errors appear in function signatures, making error handling explicit and compiler-checked',
      'Typed errors use less memory',
      'Typed errors are faster at runtime',
      'Typed errors work across process boundaries',
    ],
    correctAnswer: 0,
    explanation: 'Typed errors encode the possible failure modes in the function\'s return type (e.g., Either<MyError, Result>). The compiler ensures callers handle all error cases. Exceptions are invisible in the type system.',
    codeSnippet: '// Typed error - caller MUST handle UserError\nfun Raise<UserError>.findUser(id: String): User\n\n// Exception - nothing forces caller to handle\nfun findUser(id: String): User // may throw!',
    tags: ['typed-errors', 'type-safety', 'error-handling'],
  },
  {
    id: 'kotlin-arrow-l2-004',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you compose functions with different error types in the Raise DSL?',
    options: [
      'You cannot compose different error types',
      'Convert everything to String errors',
      'Use withError to map one error type to another',
      'Use try-catch inside the Raise block',
    ],
    correctAnswer: 2,
    explanation: 'withError transforms the error type of a Raise block. This lets you compose functions that raise different error types by mapping their errors to a common supertype or union type.',
    codeSnippet: 'sealed interface AppError {\n  data class User(val e: UserError) : AppError\n  data class Db(val e: DbError) : AppError\n}\n\nfun Raise<AppError>.process() {\n  val user = withError(AppError::User) { findUser(id) }\n  val saved = withError(AppError::Db) { save(user) }\n}',
    tags: ['typed-errors', 'withError', 'composition'],
  },
  {
    id: 'kotlin-arrow-l2-005',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either.zipOrAccumulate do?',
    options: [
      'Runs multiple Either-producing operations and accumulates all Left errors',
      'Applies a function to each element of a list of Either values',
      'Compresses Either values for network transfer',
      'Zips two Either values into a Pair',
    ],
    correctAnswer: 0,
    explanation: 'zipOrAccumulate runs multiple operations independently. If all succeed, it combines the Right values using the provided function. If any fail, it accumulates all Left errors into a NonEmptyList instead of short-circuiting on the first.',
    codeSnippet: 'val result: Either<NonEmptyList<String>, Triple<Int, Int, Int>> =\n  Either.zipOrAccumulate(\n    validateA(),\n    validateB(),\n    validateC()\n  ) { a, b, c -> Triple(a, b, c) }',
    tags: ['either', 'zipOrAccumulate', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l2-006',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you define optics for a data class using the Arrow compiler plugin?',
    options: [
      'Annotate the data class with @optics and add a companion object',
      'Implement the Optics interface',
      'Use reflection at runtime',
      'Manually implement get and set for each field',
    ],
    correctAnswer: 0,
    explanation: 'The @optics annotation with the Arrow compiler plugin (KSP) generates lenses, prisms, and other optics for the annotated data class. A companion object is required as the optics are generated as extensions on it.',
    codeSnippet: '@optics data class Person(\n  val name: String,\n  val address: Address\n) {\n  companion object\n}\n// Generated: Person.name, Person.address',
    tags: ['optics', 'annotation', 'code-generation'],
  },
  {
    id: 'kotlin-arrow-l2-007',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you compose two lenses to access a nested field?',
    options: [
      'lens1 + lens2',
      'lens1 compose lens2 or lens1.compose(lens2)',
      'lens1.andThen(lens2)',
      'lens1.then(lens2)',
    ],
    correctAnswer: 1,
    explanation: 'Lenses compose with the compose operator or compose() method. Composing Person.address with Address.city gives a lens that directly focuses on the city field inside a Person\'s address.',
    codeSnippet: 'val personCity = Person.address compose Address.city\n\n// Get the city from a person\nval city = personCity.get(person)\n\n// Set the city in a person\nval updated = personCity.set(person, "NYC")',
    tags: ['optics', 'lens', 'composition'],
  },
  {
    id: 'kotlin-arrow-l2-008',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is a Prism in Arrow Optics?',
    options: [
      'A lens for nullable fields',
      'A read-only lens',
      'A lens that works on lists',
      'An optic that focuses on one case of a sum type (sealed class)',
    ],
    correctAnswer: 3,
    explanation: 'A Prism focuses on a specific case of a sum type (sealed class/interface). It can attempt to match (getOrNull returns null if not the right case) and construct values. It is the dual of Lens which works on product types.',
    codeSnippet: '@optics sealed interface Shape {\n  data class Circle(val radius: Double) : Shape\n  data class Rect(val w: Double, val h: Double) : Shape\n  companion object\n}\n// Shape.circle is a Prism<Shape, Circle>',
    tags: ['optics', 'prism', 'sum-type'],
  },
  {
    id: 'kotlin-arrow-l2-009',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is an Optional in Arrow Optics (not to be confused with Java Optional)?',
    options: [
      'A deprecated alternative to Lens',
      'An optic that focuses on a value that may or may not exist within a structure',
      'A nullable wrapper',
      'An optional parameter annotation',
    ],
    correctAnswer: 1,
    explanation: 'An Optional optic targets a value that may not be present. It combines aspects of Lens and Prism. For example, accessing a map value by key, or focusing through a nullable field.',
    codeSnippet: 'val optionalHead: Optional<List<Int>, Int> = listHead()\noptionalHead.getOrNull(listOf(1, 2, 3)) // 1\noptionalHead.getOrNull(emptyList())     // null',
    tags: ['optics', 'optional', 'nullable'],
  },
  {
    id: 'kotlin-arrow-l2-010',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is a Traversal in Arrow Optics?',
    options: [
      'An optic that focuses on multiple values within a structure',
      'A way to iterate over a tree structure',
      'A type of graph search',
      'A sorting algorithm',
    ],
    correctAnswer: 0,
    explanation: 'A Traversal focuses on zero or more values within a data structure. It generalizes map to arbitrary nested structures. For example, you can focus on all elements of a list inside a data class.',
    codeSnippet: '@optics data class Department(\n  val employees: List<Employee>\n) { companion object }\n\n// Modify all employee names\nDepartment.employees.every(Every.list()).name\n  .modify(dept) { it.uppercase() }',
    tags: ['optics', 'traversal', 'collection'],
  },
  {
    id: 'kotlin-arrow-l2-011',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What does parMap do in Arrow Fx?',
    options: [
      'Maps over a map data structure',
      'Creates a partial function application',
      'Maps over a list sequentially',
      'Maps over a list with each transformation running in parallel',
    ],
    correctAnswer: 3,
    explanation: 'parMap runs the mapping function for each element in parallel using coroutines. All transformations execute concurrently, and the results are collected in order. If any fails, all others are cancelled.',
    codeSnippet: 'val results = listOf(1, 2, 3).parMap { id ->\n  fetchUser(id) // runs in parallel for each id\n}',
    tags: ['arrow-fx', 'parMap', 'parallel'],
  },
  {
    id: 'kotlin-arrow-l2-012',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is the difference between parMap and parTraverse?',
    options: [
      'parMap returns a Flow, parTraverse returns a List',
      'parTraverse is for suspend functions, parMap is not',
      'parMap works on lists, parTraverse works on sequences',
      'They are essentially the same in Arrow - parMap replaced parTraverse',
    ],
    correctAnswer: 3,
    explanation: 'In recent Arrow versions, parMap is the primary parallel mapping function for Iterable types. parTraverse was the older name/variant. They both achieve parallel execution of a mapping function over a collection.',
    tags: ['arrow-fx', 'parMap', 'parTraverse'],
  },
  {
    id: 'kotlin-arrow-l2-013',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What happens when one of the parallel operations in parMap fails?',
    options: [
      'All other parallel operations are cancelled (structured concurrency)',
      'The failure is silently ignored',
      'Other operations continue normally',
      'The entire application crashes',
    ],
    correctAnswer: 0,
    explanation: 'parMap uses structured concurrency. If any parallel operation fails, all sibling operations are cancelled, and the exception is propagated. This ensures no leaked coroutines or wasted resources.',
    codeSnippet: 'val results = listOf(1, 2, 3).parMap { id ->\n  if (id == 2) throw RuntimeException("fail")\n  fetchUser(id) // id=1 and id=3 are cancelled\n}',
    tags: ['arrow-fx', 'parMap', 'cancellation'],
  },
  {
    id: 'kotlin-arrow-l2-014',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'How do you compose multiple resources in Arrow?',
    options: [
      'Using a resource pool',
      'Nesting try-finally blocks',
      'Using flatMap or the resource { } DSL builder',
      'Resources cannot be composed',
    ],
    correctAnswer: 2,
    explanation: 'Resources can be composed using flatMap or the resource { } DSL builder. The DSL lets you bind multiple resources, and they are released in reverse acquisition order when the scope ends.',
    codeSnippet: 'val composite = resource {\n  val conn = install({ openConnection() }) { c, _ -> c.close() }\n  val file = install({ openFile() }) { f, _ -> f.close() }\n  Pair(conn, file)\n}\n// file closed first, then connection',
    tags: ['resource', 'composition', 'dsl'],
  },
  {
    id: 'kotlin-arrow-l2-015',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'What guarantees does Arrow\'s Resource provide?',
    options: [
      'Performance optimization only',
      'Thread safety only',
      'Type safety only',
      'The release function is always called, even if the use block throws',
    ],
    correctAnswer: 3,
    explanation: 'Resource guarantees that the release function (finalizer) is always called when the resource scope ends, whether normally or exceptionally. This prevents resource leaks in the face of errors or cancellation.',
    codeSnippet: 'resource {\n  val file = install({ File("data.txt").also { it.createNewFile() } }) {\n    f, _ -> f.delete()\n  }\n  // Even if this throws, file.delete() runs\n  processFile(file)\n}',
    tags: ['resource', 'safety', 'guarantee'],
  },
  {
    id: 'kotlin-arrow-l2-016',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'What is Arrow\'s Schedule used for?',
    options: [
      'Scheduling cron jobs',
      'Defining retry and repeat policies with configurable delays and conditions',
      'Thread scheduling',
      'Scheduling database migrations',
    ],
    correctAnswer: 1,
    explanation: 'Schedule defines policies for retrying failed operations or repeating successful ones. You can configure delays (fixed, exponential), limits (max retries), and conditions (which errors to retry).',
    codeSnippet: 'val policy = Schedule.recurs<Throwable>(3)\n  .and(Schedule.exponential(100.milliseconds))\n\nval result = policy.retry {\n  riskyOperation()\n}',
    tags: ['schedule', 'retry', 'policy'],
  },
  {
    id: 'kotlin-arrow-l2-017',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'How do you create an exponential backoff retry policy in Arrow?',
    options: [
      'Schedule.exponential<Throwable>(250.milliseconds)',
      'Schedule.linear(1.seconds)',
      'Schedule.fixed(1.seconds)',
      'Schedule.retry(3)',
    ],
    correctAnswer: 0,
    explanation: 'Schedule.exponential creates a policy with exponentially increasing delays (250ms, 500ms, 1000ms, ...). You can combine it with recurs to limit the number of retries.',
    codeSnippet: 'val policy = Schedule.exponential<Throwable>(250.milliseconds)\n  .and(Schedule.recurs(5)) // max 5 retries\n\npolicy.retry { callExternalService() }',
    tags: ['schedule', 'exponential-backoff', 'retry'],
  },
  {
    id: 'kotlin-arrow-l2-018',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'What does Schedule.and combine?',
    options: [
      'It runs two schedules in sequence',
      'It creates a logical AND condition for errors',
      'It adds additional logging',
      'It intersects two schedules - both must agree to continue',
    ],
    correctAnswer: 3,
    explanation: 'Schedule.and creates a combined schedule that continues only when BOTH schedules agree to continue. The delay is the maximum of the two delays. Useful for combining recurs (limit) with exponential (delay).',
    codeSnippet: '// Retry up to 5 times with exponential backoff\nval policy = Schedule.recurs<Throwable>(5)\n  .and(Schedule.exponential(100.milliseconds))',
    tags: ['schedule', 'composition', 'and'],
  },
  {
    id: 'kotlin-arrow-l2-019',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the difference between mapLeft and handleErrorWith on Either?',
    options: [
      'handleErrorWith is deprecated',
      'mapLeft works on Right, handleErrorWith works on Left',
      'They are identical',
      'mapLeft transforms the error value; handleErrorWith can recover with a new Either',
    ],
    correctAnswer: 3,
    explanation: 'mapLeft transforms the Left value to a different error type (Left<A> -> Left<B>). handleErrorWith takes the Left value and returns a new Either, potentially recovering to a Right value.',
    codeSnippet: '// mapLeft: change error type\nresult.mapLeft { DomainError(it.message) }\n\n// handleErrorWith: potentially recover\nresult.handleErrorWith { error ->\n  if (error.isRetryable) fetchAgain()\n  else error.left()\n}',
    tags: ['either', 'mapLeft', 'recovery'],
  },
  {
    id: 'kotlin-arrow-l2-020',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Railway-Oriented Programming',
    question: 'How do you implement a validation pipeline using railway-oriented programming in Arrow?',
    options: [
      'Using reflection',
      'Using flatMap chains or the either { } builder with bind()',
      'Using a chain of if-else statements',
      'Using annotation processing',
    ],
    correctAnswer: 1,
    explanation: 'Railway-oriented programming in Arrow uses flatMap chains or the either { } builder. Each step either continues on the success track or diverts to the error track. The either { } builder with bind() is the most idiomatic approach.',
    codeSnippet: 'fun processOrder(raw: RawOrder): Either<OrderError, Order> = either {\n  val parsed = parseOrder(raw).bind()\n  val validated = validateOrder(parsed).bind()\n  val enriched = enrichOrder(validated).bind()\n  enriched\n}',
    tags: ['railway-oriented', 'pipeline', 'either'],
  },
  {
    id: 'kotlin-arrow-l2-021',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you combine Raise with coroutines?',
    options: [
      'You need a special adapter',
      'They are incompatible',
      'Only in Arrow Fx, not standard coroutines',
      'Raise works naturally with suspend functions since both use continuation-based control flow',
    ],
    correctAnswer: 3,
    explanation: 'Raise DSL functions can be suspend functions. The either { } builder works inside coroutine contexts, and you can call suspend functions that use Raise directly. Arrow is designed to integrate seamlessly with Kotlin coroutines.',
    codeSnippet: 'suspend fun Raise<DbError>.fetchUser(id: String): User {\n  return db.query(id) ?: raise(DbError.NotFound(id))\n}\n\nval result = either {\n  val user = fetchUser("123") // suspend + Raise\n  user\n}',
    tags: ['raise-dsl', 'coroutines', 'suspend'],
  },
  {
    id: 'kotlin-arrow-l2-022',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What does the recover function do in the Raise DSL?',
    options: [
      'Provides a fallback block that runs when a raise occurs',
      'Converts the error to a warning',
      'Restarts the entire computation',
      'Logs the error and continues',
    ],
    correctAnswer: 0,
    explanation: 'recover catches errors raised in the first block and runs a recovery block. The recovery block can either provide a fallback value or raise a different error. It is the Raise DSL equivalent of catch.',
    codeSnippet: 'val result = either<String, Int> {\n  recover({\n    fetchFromPrimary().bind()\n  }) { error ->\n    fetchFromBackup().bind() // fallback\n  }\n}',
    tags: ['raise-dsl', 'recover', 'error-handling'],
  },
  {
    id: 'kotlin-arrow-l2-023',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'How do you use NonEmptyList for error accumulation with Either?',
    options: [
      'Wrap each error in a separate Either',
      'Use List<Error> as the Left type',
      'Use NonEmptyList<Error> as the Left type with zipOrAccumulate',
      'NonEmptyList cannot be used with Either',
    ],
    correctAnswer: 2,
    explanation: 'By using NonEmptyList<Error> as the Left type, you guarantee that if there are errors, there is at least one. zipOrAccumulate naturally produces NonEmptyList<E> when accumulating errors.',
    codeSnippet: 'val result: Either<NonEmptyList<ValidationError>, User> =\n  Either.zipOrAccumulate(\n    validateName(name),\n    validateAge(age),\n    validateEmail(email)\n  ) { n, a, e -> User(n, a, e) }',
    tags: ['nel', 'error-accumulation', 'either'],
  },
  {
    id: 'kotlin-arrow-l2-024',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'What does flatMap on NonEmptyList do?',
    options: [
      'Transforms each element and flattens the result into a single NonEmptyList',
      'Converts to a regular list',
      'Filters elements',
      'Returns an Either',
    ],
    correctAnswer: 0,
    explanation: 'flatMap on NonEmptyList applies a function that returns a NonEmptyList for each element, then flattens the result. Since each element produces a non-empty list, the overall result is guaranteed non-empty.',
    codeSnippet: 'val nel = nonEmptyListOf(1, 2)\nval result = nel.flatMap { nonEmptyListOf(it, it * 10) }\n// NonEmptyList(1, 10, 2, 20)',
    tags: ['nel', 'flatMap', 'transformation'],
  },
  {
    id: 'kotlin-arrow-l2-025',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Ior',
    question: 'How does flatMap work on Ior?',
    options: [
      'Same as Either - short-circuits on Left',
      'It converts Ior to Either first',
      'It accumulates Left values using a Semigroup while chaining Right computations',
      'It ignores Left values entirely',
    ],
    correctAnswer: 2,
    explanation: 'Ior.flatMap accumulates Left values when Both values are encountered. If both the current Ior and the result of flatMap have Left values, they are combined using a Semigroup (e.g., string concatenation or list append).',
    codeSnippet: 'val result = Ior.Both("warning1", 1).flatMap(String::plus) { v ->\n  Ior.Both("warning2", v + 10)\n}\n// Ior.Both("warning1warning2", 11)',
    tags: ['ior', 'flatMap', 'semigroup'],
  },
  {
    id: 'kotlin-arrow-l2-026',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Ior',
    question: 'When should you prefer Ior over Either?',
    options: [
      'When you need better performance',
      'When operations can produce warnings alongside successful results',
      'When you have nullable types',
      'When working with collections',
    ],
    correctAnswer: 1,
    explanation: 'Ior is ideal when operations can succeed but with caveats. For example, parsing that succeeds but with deprecation warnings, or a calculation that works but with precision loss. Either cannot express "success with warnings."',
    codeSnippet: 'fun parse(json: String): Ior<NonEmptyList<Warning>, Config> =\n  // Returns Both if deprecated fields are used\n  // Returns Right if parsing is clean\n  // Returns Left if parsing fails completely',
    tags: ['ior', 'use-case', 'warnings'],
  },
  {
    id: 'kotlin-arrow-l2-027',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What does Lens.modify do?',
    options: [
      'Modifies the lens definition itself',
      'Deletes the focused field',
      'Validates the structure',
      'Gets the focused value, applies a transformation, and returns the updated structure',
    ],
    correctAnswer: 3,
    explanation: 'modify takes a data structure and a transformation function. It gets the focused value, applies the function, and returns a new copy of the structure with the modified value. It is equivalent to get then set.',
    codeSnippet: 'val person = Person("Alice", 30)\nval older = Person.age.modify(person) { it + 1 }\n// Person("Alice", 31)',
    tags: ['optics', 'lens', 'modify'],
  },
  {
    id: 'kotlin-arrow-l2-028',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use optics with the copy DSL in Arrow?',
    options: [
      'Person.copy { Person.name set "Bob" } on person',
      'person.copy { Person.name transform { it.uppercase() } }',
      'person.opticsCopy { }',
      'There is no copy DSL in Arrow',
    ],
    correctAnswer: 1,
    explanation: 'Arrow provides a copy DSL that lets you use optics inside a copy block. You can use transform to apply functions to focused fields, making complex nested updates readable.',
    codeSnippet: 'val updated = person.copy {\n  Person.name transform { it.uppercase() }\n  Person.address.city.name set "NYC"\n}',
    tags: ['optics', 'copy-dsl', 'transformation'],
  },
  {
    id: 'kotlin-arrow-l2-029',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between fold and reduce in Kotlin?',
    options: [
      'reduce works on maps, fold works on lists',
      'fold is eager, reduce is lazy',
      'fold takes an initial value; reduce uses the first element as the initial value',
      'They are identical',
    ],
    correctAnswer: 2,
    explanation: 'fold takes an explicit initial accumulator value and processes all elements. reduce uses the first element as the initial value and processes the rest. reduce throws on empty collections; fold returns the initial value.',
    codeSnippet: 'listOf(1, 2, 3).fold(0) { acc, x -> acc + x }  // 6\nlistOf(1, 2, 3).reduce { acc, x -> acc + x }   // 6\n\nemptyList<Int>().fold(0) { acc, x -> acc + x }  // 0\nemptyList<Int>().reduce { acc, x -> acc + x }   // throws!',
    tags: ['functional-patterns', 'fold', 'reduce'],
  },
  {
    id: 'kotlin-arrow-l2-030',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a Semigroup?',
    options: [
      'A data structure for storing key-value pairs',
      'A group of related classes',
      'A type with an associative binary operation (combine)',
      'A concurrency primitive',
    ],
    correctAnswer: 2,
    explanation: 'A Semigroup provides an associative combine operation: combine(combine(a, b), c) == combine(a, combine(b, c)). Examples: Int with addition, String with concatenation, List with append.',
    codeSnippet: '// String semigroup: combine via concatenation\n"hello" + " " + "world" // associative\n\n// NonEmptyList semigroup: combine via append\nnel1 + nel2',
    tags: ['functional-patterns', 'semigroup', 'typeclass'],
  },
  {
    id: 'kotlin-arrow-l2-031',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a Monoid?',
    options: [
      'A type with exactly one instance',
      'A Semigroup with an identity element (empty)',
      'A singleton pattern',
      'A monad with a single value',
    ],
    correctAnswer: 1,
    explanation: 'A Monoid is a Semigroup with an identity element (empty). combine(a, empty) == a and combine(empty, a) == a. Examples: Int with 0 and +, String with "" and +, List with emptyList and +.',
    codeSnippet: '// Int monoid: combine = +, empty = 0\n5 + 0 == 5\n0 + 5 == 5\n\n// String monoid: combine = +, empty = ""\n"hello" + "" == "hello"',
    tags: ['functional-patterns', 'monoid', 'typeclass'],
  },
  {
    id: 'kotlin-arrow-l2-032',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you traverse a list with an Either-returning function in Arrow?',
    options: [
      'list.flatMap { eitherFunction(it) }',
      'list.eitherMap { eitherFunction(it) }',
      'list.map { eitherFunction(it) }',
      'list.traverse { eitherFunction(it) } or list.mapOrAccumulate { eitherFunction(it).bind() }',
    ],
    correctAnswer: 3,
    explanation: 'traverse applies an Either-returning function to each element. If all return Right, you get Right(List). If any returns Left, you get Left. mapOrAccumulate is the accumulating variant that collects all errors.',
    codeSnippet: 'val ids = listOf("1", "2", "abc")\n\n// Short-circuit on first error\nval result = either { ids.map { parseId(it).bind() } }\n\n// Accumulate all errors\nval accumulated = either { ids.mapOrAccumulate { parseId(it).bind() } }',
    tags: ['either', 'traverse', 'collection'],
  },
  {
    id: 'kotlin-arrow-l2-033',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the ior { } builder in Arrow?',
    options: [
      'A builder for indexed operations',
      'A builder that provides a Raise context for Ior, allowing Both accumulation',
      'An input/output resource builder',
      'A builder for creating IO operations',
    ],
    correctAnswer: 1,
    explanation: 'The ior { } builder provides a context where you can bind Ior values. Left values accumulate using a Semigroup while Right values are chained. The result is an Ior that may contain both accumulated warnings and a final value.',
    codeSnippet: 'val result: Ior<NonEmptyList<String>, Int> = ior(NonEmptyList.semigroup()) {\n  val a = Ior.Both(nonEmptyListOf("warn1"), 1).bind()\n  val b = Ior.Both(nonEmptyListOf("warn2"), 2).bind()\n  a + b\n}\n// Ior.Both(NonEmptyList("warn1", "warn2"), 3)',
    tags: ['raise-dsl', 'ior', 'accumulation'],
  },
  {
    id: 'kotlin-arrow-l2-034',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you run two independent computations in parallel and combine their results in Arrow?',
    options: [
      'Using Thread.start()',
      'Using parZip which runs computations in parallel and combines results',
      'Parallel computation is not supported',
      'Using async/await from kotlinx.coroutines only',
    ],
    correctAnswer: 1,
    explanation: 'parZip runs multiple suspend functions in parallel and combines their results with a combining function. It uses structured concurrency - if one fails, others are cancelled.',
    codeSnippet: 'val (user, orders) = parZip(\n  { fetchUser(id) },\n  { fetchOrders(id) }\n) { user, orders -> Pair(user, orders) }',
    tags: ['arrow-fx', 'parZip', 'parallel'],
  },
  {
    id: 'kotlin-arrow-l2-035',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is the purpose of the raceN function in Arrow Fx?',
    options: [
      'Runs multiple computations in parallel and returns the result of the first to complete',
      'Sorts computations by speed',
      'Limits the rate of concurrent operations',
      'Creates a race condition for testing',
    ],
    correctAnswer: 0,
    explanation: 'raceN runs multiple computations concurrently and returns the result of the first one to complete, cancelling the others. This is useful for implementing timeouts or choosing the fastest data source.',
    codeSnippet: 'val result = raceN(\n  { fetchFromCacheWithTimeout() },\n  { fetchFromNetwork() }\n) // returns whichever completes first',
    tags: ['arrow-fx', 'raceN', 'parallel'],
  },
  {
    id: 'kotlin-arrow-l2-036',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Circuit Breaker',
    question: 'What is a circuit breaker pattern?',
    options: [
      'An electrical safety mechanism',
      'A pattern that prevents repeated calls to a failing service by tracking failures and opening a circuit',
      'A way to limit memory usage',
      'A pattern for breaking circular dependencies',
    ],
    correctAnswer: 1,
    explanation: 'A circuit breaker monitors calls to a service. After a threshold of failures, it "opens" and immediately returns errors without calling the service. After a timeout, it tries again (half-open state).',
    tags: ['circuit-breaker', 'resilience', 'pattern'],
  },
  {
    id: 'kotlin-arrow-l2-037',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Circuit Breaker',
    question: 'What are the three states of Arrow\'s CircuitBreaker?',
    options: [
      'Closed (normal), Open (failing, fast-fail), HalfOpen (testing recovery)',
      'Open, Closed, Locked',
      'Running, Paused, Stopped',
      'Active, Inactive, Standby',
    ],
    correctAnswer: 0,
    explanation: 'Closed: normal operation, requests pass through. Open: too many failures, requests are rejected immediately. HalfOpen: after a reset timeout, one test request is allowed through to check if the service has recovered.',
    codeSnippet: 'val cb = CircuitBreaker(\n  maxFailures = 5,\n  resetTimeout = 30.seconds,\n  exponentialBackoffFactor = 2.0\n)',
    tags: ['circuit-breaker', 'states', 'resilience'],
  },
  {
    id: 'kotlin-arrow-l2-038',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does either.bimap(leftFn, rightFn) do?',
    options: [
      'Applies both functions simultaneously',
      'Creates a Pair of the two function results',
      'Applies leftFn if Left, rightFn if Right - transforms both sides in one call',
      'Applies both functions and returns a Tuple2',
    ],
    correctAnswer: 2,
    explanation: 'bimap applies the first function to the Left value or the second function to the Right value, depending on which case the Either is. It is a convenience for transforming both sides in a single operation.',
    codeSnippet: 'val result = either.bimap(\n  { error -> error.uppercase() },\n  { value -> value * 2 }\n)\n// Left("error") -> Left("ERROR")\n// Right(5) -> Right(10)',
    tags: ['either', 'bimap', 'transformation'],
  },
  {
    id: 'kotlin-arrow-l2-039',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between a Monad and an Applicative?',
    options: [
      'Monads are faster',
      'Applicative can combine independent effects; Monad allows sequencing dependent effects with flatMap',
      'There is no practical difference',
      'Applicative is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Applicative (ap/zip) combines independent computations - the second does not depend on the first. Monad (flatMap) allows the second computation to depend on the result of the first. This makes Monad strictly more powerful.',
    codeSnippet: '// Applicative: independent\nzip(fetchUser(), fetchOrders()) { u, o -> Screen(u, o) }\n\n// Monad: dependent\nfetchUser().flatMap { user -> fetchOrders(user.id) }',
    tags: ['functional-patterns', 'monad', 'applicative'],
  },
  {
    id: 'kotlin-arrow-l2-040',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Reader pattern in FP?',
    options: [
      'A way to read user input',
      'A pattern for dependency injection using function composition',
      'A CSV parsing library',
      'A file reader abstraction',
    ],
    correctAnswer: 1,
    explanation: 'The Reader pattern models computations that depend on shared configuration or environment. In Kotlin, this is often modeled using extension functions on a context type rather than a separate Reader monad.',
    codeSnippet: '// Reader pattern via extension functions\nfun AppContext.fetchUser(id: String): User {\n  return database.query("SELECT * FROM users WHERE id = ?", id)\n}\n\n// The dependency (AppContext) is passed implicitly',
    tags: ['functional-patterns', 'reader', 'dependency-injection'],
  },
  {
    id: 'kotlin-arrow-l2-041',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you convert a nullable value to a Raise error?',
    options: [
      'ensureNotNull(value) { MyError.NullValue }',
      'value.toRaise()',
      'Raise.fromNullable(value)',
      'value ?: throw NullPointerException()',
    ],
    correctAnswer: 0,
    explanation: 'ensureNotNull checks if a value is null. If non-null, it returns the value with smart-cast to non-null type. If null, it raises the specified error. This cleanly bridges nullable types into the Raise context.',
    codeSnippet: 'fun Raise<UserError>.getUser(id: String): User {\n  val user = repository.findById(id)\n  return ensureNotNull(user) { UserError.NotFound(id) }\n}',
    tags: ['raise-dsl', 'ensureNotNull', 'nullable'],
  },
  {
    id: 'kotlin-arrow-l2-042',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the result builder in Arrow\'s Raise DSL?',
    options: [
      'A test result aggregator',
      'A build system configuration',
      'A builder that creates Kotlin Result<T> values',
      'A builder that provides Raise<Throwable> context and returns Result<A>',
    ],
    correctAnswer: 3,
    explanation: 'The result { } builder provides a Raise<Throwable> context and returns Kotlin\'s standard Result<A>. It bridges the Raise DSL with Kotlin\'s built-in Result type.',
    codeSnippet: 'val result: Result<Int> = result {\n  val x = Result.success(5).bind()\n  val y = Result.success(3).bind()\n  x + y\n}\n// Result.success(8)',
    tags: ['raise-dsl', 'result', 'builder'],
  },
  {
    id: 'kotlin-arrow-l2-043',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either<E, A>.onRight { } do?',
    options: [
      'Throws if Left',
      'Executes a side effect if Right and returns the original Either',
      'Transforms the Right value',
      'Filters the Right value',
    ],
    correctAnswer: 1,
    explanation: 'onRight (also called tap) executes a side-effect action if the Either is Right, then returns the original Either unchanged. It is useful for logging or other side effects without modifying the value.',
    codeSnippet: 'fetchUser(id)\n  .onRight { user -> logger.info("Found user: ${user.name}") }\n  .onLeft { error -> logger.error("Failed: $error") }',
    tags: ['either', 'onRight', 'side-effects'],
  },
  {
    id: 'kotlin-arrow-l2-044',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a type alias in Kotlin and how is it used in Arrow?',
    options: [
      'A way to create new types from existing ones',
      'A serialization format',
      'A way to hide type information',
      'An alternative name for an existing type, often used to simplify complex generic signatures',
    ],
    correctAnswer: 3,
    explanation: 'A type alias provides an alternative name for an existing type. Arrow uses type aliases like Nel<A> = NonEmptyList<A> and EitherNel<E, A> = Either<NonEmptyList<E>, A> to simplify common type signatures.',
    codeSnippet: 'typealias Nel<A> = NonEmptyList<A>\ntypealias EitherNel<E, A> = Either<NonEmptyList<E>, A>\n\n// Instead of Either<NonEmptyList<Error>, User>\nfun validate(input: Input): EitherNel<Error, User>',
    tags: ['functional-patterns', 'type-alias', 'arrow'],
  },
  {
    id: 'kotlin-arrow-l2-045',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use Every (Traversal) to modify all elements in a list field?',
    options: [
      'Using Every.list() composed with a Lens',
      'It is not possible with optics',
      'Using mapIndexed',
      'Using a for loop',
    ],
    correctAnswer: 0,
    explanation: 'Every.list() creates a Traversal that focuses on every element of a list. Composed with lenses, you can modify deeply nested list elements in an immutable data structure.',
    codeSnippet: '@optics data class Company(\n  val employees: List<Employee>\n) { companion object }\n\n@optics data class Employee(\n  val salary: Int\n) { companion object }\n\nCompany.employees.every(Every.list()).salary\n  .modify(company) { it + 1000 }',
    tags: ['optics', 'every', 'traversal'],
  },
  {
    id: 'kotlin-arrow-l2-046',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What does parMapNotNull do in Arrow Fx?',
    options: [
      'Maps in parallel and throws on null',
      'Maps in parallel and filters out null results',
      'There is no parMapNotNull in Arrow',
      'Maps only non-null elements in parallel',
    ],
    correctAnswer: 1,
    explanation: 'parMapNotNull runs the mapping function in parallel for each element and filters out null results from the output list. It combines the behavior of parMap and mapNotNull.',
    codeSnippet: 'val results = listOf(1, 2, 3).parMapNotNull { id ->\n  fetchUserOrNull(id) // null results are excluded\n}',
    tags: ['arrow-fx', 'parMapNotNull', 'parallel'],
  },
  {
    id: 'kotlin-arrow-l2-047',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is an extension function receiver in Kotlin and how does Arrow use it?',
    options: [
      'A Bluetooth receiver',
      'A network endpoint',
      'A serialization protocol',
      'A mechanism that adds functions to existing types; Arrow uses it for DSL builders and Raise context',
    ],
    correctAnswer: 3,
    explanation: 'Extension function receivers let you call functions as if they were members of a type. Arrow leverages this for the Raise DSL (fun Raise<E>.doSomething()) and optics DSL builders.',
    codeSnippet: '// Extension function on Raise receiver\nfun Raise<String>.validatePositive(n: Int): Int {\n  ensure(n > 0) { "Must be positive" }\n  return n\n}\n\n// Used inside either { } which provides Raise<String>\nval result = either { validatePositive(5) }',
    tags: ['functional-patterns', 'extension-receiver', 'dsl'],
  },
  {
    id: 'kotlin-arrow-l2-048',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you model a union of error types in Kotlin for the Raise DSL?',
    options: [
      'Use Exception hierarchy',
      'Use String error messages',
      'Use error codes as Int',
      'Use a sealed interface with subtypes wrapping each error domain',
    ],
    correctAnswer: 3,
    explanation: 'Sealed interfaces provide a compile-time-safe union of error types. Each subtype wraps errors from a different domain. The when expression ensures exhaustive handling of all error cases.',
    codeSnippet: 'sealed interface AppError {\n  data class Validation(val e: ValidationError) : AppError\n  data class Database(val e: DbError) : AppError\n  data class Network(val e: NetworkError) : AppError\n}\n\nfun Raise<AppError>.process(): Result { ... }',
    tags: ['typed-errors', 'sealed-interface', 'union'],
  },
  {
    id: 'kotlin-arrow-l2-049',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the difference between Either.catch and Either.catchOrThrow?',
    options: [
      'catchOrThrow is deprecated',
      'catch catches all Throwables; catchOrThrow only catches specified exception types and rethrows others',
      'They are identical',
      'catch is for checked exceptions, catchOrThrow for unchecked',
    ],
    correctAnswer: 1,
    explanation: 'Either.catch wraps any Throwable in Left. Either.catchOrThrow (reified inline) only catches the specified exception type, rethrowing any other exceptions. This is safer as you only handle expected errors.',
    codeSnippet: '// Catches all Throwables\nEither.catch { riskyOp() }\n\n// Only catches IOException, rethrows others\nEither.catch { riskyOp() }.mapLeft { \n  when(it) {\n    is IOException -> NetworkError(it)\n    else -> throw it\n  }\n}',
    tags: ['either', 'catch', 'exception-handling'],
  },
  {
    id: 'kotlin-arrow-l2-050',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Writer pattern in FP?',
    options: [
      'A console output manager',
      'A file writing abstraction',
      'A pattern for accumulating a log or output alongside a computation',
      'A design pattern for serialization',
    ],
    correctAnswer: 2,
    explanation: 'The Writer pattern models computations that produce a main value alongside an accumulated auxiliary output (like a log). In Kotlin, this is often modeled with Pair<Log, Value> or a custom type.',
    codeSnippet: '// Simple writer pattern\ndata class Writer<W, A>(val log: W, val value: A)\n\nfun Writer<List<String>, Int>.flatMap(\n  f: (Int) -> Writer<List<String>, Int>\n): Writer<List<String>, Int> {\n  val next = f(value)\n  return Writer(log + next.log, next.value)\n}',
    tags: ['functional-patterns', 'writer', 'logging'],
  },
  {
    id: 'kotlin-arrow-l2-051',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Retry',
    question: 'How do you retry only specific exceptions with Arrow Schedule?',
    options: [
      'Schedule.retryIf<Throwable> { it is IOException }',
      'Use a try-catch inside the retry block',
      'Schedule does not support filtering exceptions',
      'Use doWhile to check the exception type',
    ],
    correctAnswer: 3,
    explanation: 'Schedule.doWhile allows you to add conditions to a retry policy. You can inspect the input (the exception) and decide whether to continue retrying based on its type or properties.',
    codeSnippet: 'val policy = Schedule.recurs<Throwable>(3)\n  .and(Schedule.exponential(100.milliseconds))\n  .doWhile { throwable, _ -> throwable is IOException }',
    tags: ['retry', 'schedule', 'filtering'],
  },
  {
    id: 'kotlin-arrow-l2-052',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a lens law in Arrow Optics?',
    options: [
      'Properties that a valid lens must satisfy (get-set, set-get, set-set)',
      'A performance benchmark',
      'A legal restriction on lens usage',
      'A linting rule for optics code',
    ],
    correctAnswer: 0,
    explanation: 'Lens laws ensure correctness: (1) Get-Set: set(s, get(s)) == s, (2) Set-Get: get(set(s, a)) == a, (3) Set-Set: set(set(s, a), b) == set(s, b). Arrow-generated lenses satisfy these laws.',
    codeSnippet: '// Get-Set: setting what you get changes nothing\nPerson.name.set(person, Person.name.get(person)) == person\n\n// Set-Get: getting what you set returns the set value\nPerson.name.get(Person.name.set(person, "Alice")) == "Alice"',
    tags: ['optics', 'laws', 'correctness'],
  },
  {
    id: 'kotlin-arrow-l2-053',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you handle multiple error types at different layers of an application?',
    options: [
      'Use withError to map domain-specific errors to the layer\'s error type',
      'Use a single global error type',
      'Use String for all errors',
      'Use exceptions instead',
    ],
    correctAnswer: 0,
    explanation: 'Each layer defines its own error type. When calling into a different layer, use withError to map that layer\'s errors to the current layer\'s error type. This keeps each layer\'s error types focused and independent.',
    codeSnippet: 'fun Raise<ApiError>.handleRequest(req: Request): Response {\n  val user = withError({ ApiError.UserErr(it) }) {\n    findUser(req.userId) // Raise<UserError>\n  }\n  val order = withError({ ApiError.OrderErr(it) }) {\n    createOrder(user, req.items) // Raise<OrderError>\n  }\n  return Response(order)\n}',
    tags: ['raise-dsl', 'withError', 'layered-architecture'],
  },
  {
    id: 'kotlin-arrow-l2-054',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either<E, A>.filterOrElse(predicate) { error } do?',
    options: [
      'Removes Left values from a list',
      'Filters a list of Either values',
      'Throws if the predicate fails',
      'If Right, tests the predicate; if false, converts to Left with the error; Left passes through',
    ],
    correctAnswer: 3,
    explanation: 'filterOrElse tests the Right value against a predicate. If the predicate returns true, the Right is unchanged. If false, the Either becomes Left with the provided error. Left values pass through unchanged.',
    codeSnippet: 'val result = 42.right()\n  .filterOrElse({ it > 0 }) { "Must be positive" }\n// Right(42)\n\nval result2 = (-1).right()\n  .filterOrElse({ it > 0 }) { "Must be positive" }\n// Left("Must be positive")',
    tags: ['either', 'filter', 'validation'],
  },
  {
    id: 'kotlin-arrow-l2-055',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the State monad pattern?',
    options: [
      'A database state management pattern',
      'The Android Activity lifecycle states',
      'A UI state management framework',
      'A pattern for threading state through a sequence of computations without mutation',
    ],
    correctAnswer: 3,
    explanation: 'The State monad models computations that read and modify a state value. Each computation takes a state as input and returns a new state along with a result. This avoids mutable state while simulating it functionally.',
    codeSnippet: '// State as function: S -> Pair<S, A>\ntypealias State<S, A> = (S) -> Pair<S, A>\n\nval increment: State<Int, Unit> = { count -> Pair(count + 1, Unit) }\nval getCount: State<Int, Int> = { count -> Pair(count, count) }',
    tags: ['functional-patterns', 'state', 'monad'],
  },
  {
    id: 'kotlin-arrow-l2-056',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you limit concurrency in parMap?',
    options: [
      'Using Thread.sleep()',
      'Using a semaphore',
      'Concurrency cannot be limited in parMap',
      'Passing a concurrency parameter: list.parMap(concurrency = 5) { ... }',
    ],
    correctAnswer: 3,
    explanation: 'parMap accepts a concurrency parameter that limits the number of parallel operations. This is essential when calling rate-limited APIs or managing resource-intensive operations.',
    codeSnippet: 'val results = urls.parMap(concurrency = 10) { url ->\n  fetchContent(url) // at most 10 concurrent fetches\n}',
    tags: ['arrow-fx', 'parMap', 'concurrency'],
  },
  {
    id: 'kotlin-arrow-l2-057',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is an Iso in Arrow Optics?',
    options: [
      'An optic representing a lossless, reversible transformation between two types',
      'An error isolation pattern',
      'An internationalization standard',
      'An isolation mechanism for concurrent access',
    ],
    correctAnswer: 0,
    explanation: 'An Iso (isomorphism) represents a lossless, reversible conversion between two types. get converts A to B, and reverseGet converts B back to A. Example: Celsius <-> Fahrenheit.',
    codeSnippet: 'val celsiusToFahrenheit = Iso<Double, Double>(\n  get = { c -> c * 9.0 / 5.0 + 32.0 },\n  reverseGet = { f -> (f - 32.0) * 5.0 / 9.0 }\n)',
    tags: ['optics', 'iso', 'isomorphism'],
  },
  {
    id: 'kotlin-arrow-l2-058',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'How does Arrow Resource handle nested resource acquisition?',
    options: [
      'Inner resources are leaked if outer resource fails',
      'Resources are released in reverse order of acquisition, even on failure',
      'Only the outermost resource is released',
      'Nested resources are not supported',
    ],
    correctAnswer: 1,
    explanation: 'Arrow Resource maintains a stack of acquired resources. They are always released in reverse acquisition order (LIFO), ensuring inner resources are cleaned up first. This applies even when exceptions occur.',
    codeSnippet: 'resource {\n  val outer = install({ acquireDb() }) { db, _ -> db.close() } // released last\n  val inner = install({ acquireSession(outer) }) { s, _ -> s.close() } // released first\n  useSession(inner)\n}',
    tags: ['resource', 'nested', 'release-order'],
  },
  {
    id: 'kotlin-arrow-l2-059',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'What does Schedule.or combine?',
    options: [
      'It creates a logical OR condition',
      'It creates a union schedule that continues while EITHER schedule agrees to continue',
      'It combines error types',
      'It picks the schedule with fewer retries',
    ],
    correctAnswer: 1,
    explanation: 'Schedule.or creates a combined schedule that continues as long as at least one of the schedules agrees to continue. The delay is the minimum of the two delays. It is the dual of and.',
    codeSnippet: '// Retry if EITHER condition is met:\n// - fewer than 10 retries, OR\n// - less than 60 seconds elapsed\nval policy = Schedule.recurs<Throwable>(10)\n  .or(Schedule.doUntil { _, duration -> duration > 60.seconds })',
    tags: ['schedule', 'composition', 'or'],
  },
  {
    id: 'kotlin-arrow-l2-060',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Kleisli arrow pattern?',
    options: [
      'A UI design pattern',
      'A way to compose functions of type A -> F<B> where F is a monad',
      'A graph traversal technique',
      'A sorting algorithm',
    ],
    correctAnswer: 1,
    explanation: 'Kleisli composition chains functions that return monadic values (A -> Either<E, B>). Instead of normal compose (which requires matching types), Kleisli uses flatMap to chain the monadic results.',
    codeSnippet: '// Kleisli composition: (A -> Either<E, B>) + (B -> Either<E, C>) => (A -> Either<E, C>)\nval parse: (String) -> Either<Error, Int> = { ... }\nval validate: (Int) -> Either<Error, ValidInt> = { ... }\n\nval parseAndValidate: (String) -> Either<Error, ValidInt> = { s ->\n  parse(s).flatMap { validate(it) }\n}',
    tags: ['functional-patterns', 'kleisli', 'composition'],
  },
  {
    id: 'kotlin-arrow-l2-061',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you combine two independent Either values without error accumulation?',
    options: [
      'Either.zip(either1, either2) { a, b -> Pair(a, b) }',
      'either1.merge(either2)',
      'Either.combine(either1, either2)',
      'either1 + either2',
    ],
    correctAnswer: 0,
    explanation: 'Either.zip combines multiple independent Either values. If all are Right, the combining function is applied. If any is Left, the first Left encountered is returned (no accumulation).',
    codeSnippet: 'val name: Either<Error, String> = "Alice".right()\nval age: Either<Error, Int> = 30.right()\n\nval user = Either.zip(name, age) { n, a -> User(n, a) }\n// Right(User("Alice", 30))',
    tags: ['either', 'zip', 'combination'],
  },
  {
    id: 'kotlin-arrow-l2-062',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the purpose of the catch block in the Raise DSL?',
    options: [
      'Catches only Arrow-specific exceptions',
      'Converts exceptions into typed errors within a Raise context',
      'Logs exceptions without handling them',
      'Same as try-catch',
    ],
    correctAnswer: 1,
    explanation: 'The catch function in the Raise DSL runs a block that might throw exceptions and converts any thrown exception into a typed error using raise. This bridges exception-throwing code into the typed error world.',
    codeSnippet: 'fun Raise<AppError>.parseJson(json: String): Config {\n  return catch({\n    jsonParser.parse(json) // might throw\n  }) { e ->\n    raise(AppError.ParseFailed(e.message ?: "unknown"))\n  }\n}',
    tags: ['raise-dsl', 'catch', 'exception-handling'],
  },
  {
    id: 'kotlin-arrow-l2-063',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a natural transformation in FP?',
    options: [
      'A mapping from one type constructor to another that preserves structure',
      'A type of sorting',
      'Converting natural numbers',
      'A biological algorithm',
    ],
    correctAnswer: 0,
    explanation: 'A natural transformation converts values from one wrapper type to another (e.g., Option to List, Either to Result) while preserving the contained values and the compositional structure.',
    codeSnippet: '// Natural transformation from Either to nullable\nfun <E, A> Either<E, A>.toNullable(): A? = orNull()\n\n// Natural transformation from nullable to Either\nfun <A> A?.toEither(error: () -> String): Either<String, A> =\n  Either.fromNullable(this, error)',
    tags: ['functional-patterns', 'natural-transformation', 'theory'],
  },
  {
    id: 'kotlin-arrow-l2-064',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Sealed Class Error Handling',
    question: 'How do you model hierarchical errors with sealed classes?',
    options: [
      'Error hierarchies are not possible with sealed classes',
      'Use strings with prefixes',
      'Use enum classes only',
      'Use nested sealed classes for sub-errors',
    ],
    correctAnswer: 3,
    explanation: 'Sealed classes can be nested to create error hierarchies. A top-level sealed class can have sealed class subtypes that further break down into specific cases, creating a tree of error types.',
    codeSnippet: 'sealed interface AppError {\n  sealed interface ValidationError : AppError {\n    data class InvalidEmail(val email: String) : ValidationError\n    data class InvalidAge(val age: Int) : ValidationError\n  }\n  sealed interface DbError : AppError {\n    data object ConnectionLost : DbError\n    data class QueryFailed(val sql: String) : DbError\n  }\n}',
    tags: ['sealed-class', 'hierarchy', 'error-modeling'],
  },
  {
    id: 'kotlin-arrow-l2-065',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is the difference between parZip and async/await in Kotlin coroutines?',
    options: [
      'parZip is slower',
      'async/await does not support parallelism',
      'They are identical',
      'parZip provides structured concurrency with automatic cancellation on failure; async/await requires manual handling',
    ],
    correctAnswer: 3,
    explanation: 'parZip ensures structured concurrency: if one computation fails, all siblings are cancelled automatically. With raw async/await, you need to manually manage the scope and handle failures to avoid leaked coroutines.',
    codeSnippet: '// parZip: safe, structured\nparZip(\n  { fetchUser(id) },    // auto-cancelled if fetchOrders fails\n  { fetchOrders(id) }   // auto-cancelled if fetchUser fails\n) { user, orders -> Screen(user, orders) }\n\n// async/await: must manage yourself\ncoroutineScope {\n  val user = async { fetchUser(id) }\n  val orders = async { fetchOrders(id) }\n  Screen(user.await(), orders.await())\n}',
    tags: ['arrow-fx', 'parZip', 'structured-concurrency'],
  },
  {
    id: 'kotlin-arrow-l2-066',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is an algebraic data type (ADT)?',
    options: [
      'A mathematical equation type',
      'A type composed from product types (data classes) and sum types (sealed classes)',
      'A type that supports arithmetic operations',
      'A type used in algebra libraries',
    ],
    correctAnswer: 1,
    explanation: 'ADTs combine product types (AND: data classes with multiple fields) and sum types (OR: sealed classes with multiple cases). They model domain data precisely and enable exhaustive pattern matching.',
    codeSnippet: '// Sum type (OR): Shape is Circle OR Rectangle OR Triangle\nsealed interface Shape {\n  // Product types (AND): Circle has radius AND center\n  data class Circle(val radius: Double, val center: Point) : Shape\n  data class Rectangle(val width: Double, val height: Double) : Shape\n}',
    tags: ['functional-patterns', 'adt', 'type-system'],
  },
  {
    id: 'kotlin-arrow-l2-067',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you sequence a List<Either<E, A>> into Either<E, List<A>>?',
    options: [
      'list.toEither()',
      'Either.fromList(list)',
      'list.flatten()',
      'list.sequence() or either { list.map { it.bind() } }',
    ],
    correctAnswer: 3,
    explanation: 'Sequencing (or traversing with identity) flips the containers: List<Either<E, A>> becomes Either<E, List<A>>. If all elements are Right, you get Right(List). If any is Left, you get that Left.',
    codeSnippet: 'val eithers: List<Either<String, Int>> = listOf(1.right(), 2.right(), 3.right())\n\nval result: Either<String, List<Int>> = either {\n  eithers.map { it.bind() }\n}\n// Right([1, 2, 3])',
    tags: ['either', 'sequence', 'traverse'],
  },
  {
    id: 'kotlin-arrow-l2-068',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you create optics for a sealed class hierarchy?',
    options: [
      'Use @optics annotation which generates Prisms for each subtype',
      'Use reflection',
      'Manually write pattern matching code',
      'Optics do not work with sealed classes',
    ],
    correctAnswer: 0,
    explanation: 'The @optics annotation with the Arrow compiler plugin generates Prisms for sealed class hierarchies. Each subtype gets a Prism that can be used to focus on that specific case.',
    codeSnippet: '@optics sealed interface NetworkResult {\n  data class Success(val data: String) : NetworkResult\n  data class Error(val code: Int, val msg: String) : NetworkResult\n  companion object\n}\n// Generated: NetworkResult.success (Prism)\n// Generated: NetworkResult.error (Prism)',
    tags: ['optics', 'sealed-class', 'prism'],
  },
  {
    id: 'kotlin-arrow-l2-069',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you accumulate errors from a list of validations using mapOrAccumulate?',
    options: [
      'Using a mutable list to collect errors',
      'mapOrAccumulate runs each validation and collects all errors into NonEmptyList',
      'Accumulation requires the Validated type',
      'Using flatMap in a loop',
    ],
    correctAnswer: 1,
    explanation: 'mapOrAccumulate applies the block to each element. Instead of short-circuiting on the first error, it runs all elements and accumulates errors. The result is Either<NonEmptyList<E>, List<B>>.',
    codeSnippet: 'val result: Either<NonEmptyList<String>, List<Int>> = either {\n  listOf("1", "abc", "3", "xyz").mapOrAccumulate { str ->\n    str.toIntOrNull() ?: raise("Invalid: $str")\n  }\n}\n// Left(NonEmptyList("Invalid: abc", "Invalid: xyz"))',
    tags: ['raise-dsl', 'mapOrAccumulate', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l2-070',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between a value class and a data class in Kotlin for FP?',
    options: [
      'Data classes are deprecated',
      'Value classes are always nullable',
      'Value classes wrap a single value with zero runtime overhead; data classes can have multiple fields',
      'There is no difference',
    ],
    correctAnswer: 2,
    explanation: 'Value classes (inline classes) wrap a single value without runtime allocation overhead. They are useful for type-safe wrappers (e.g., UserId, Email) that prevent mixing up primitive types.',
    codeSnippet: '@JvmInline\nvalue class UserId(val value: String)\n\n@JvmInline\nvalue class OrderId(val value: String)\n\n// Cannot accidentally pass OrderId where UserId is expected\nfun findUser(id: UserId): User',
    tags: ['functional-patterns', 'value-class', 'type-safety'],
  },
  {
    id: 'kotlin-arrow-l2-071',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either.merge() return?',
    options: [
      'An Either with both values combined',
      'Throws if the Either is Left',
      'A Pair of Left and Right',
      'The contained value (works when Left and Right have the same type)',
    ],
    correctAnswer: 3,
    explanation: 'merge() extracts the value from an Either<A, A> where both sides have the same type. Since both cases hold the same type, the result is just that type without the Either wrapper.',
    codeSnippet: 'val either: Either<String, String> = "hello".right()\nval value: String = either.merge() // "hello"\n\nval either2: Either<String, String> = "error".left()\nval value2: String = either2.merge() // "error"',
    tags: ['either', 'merge', 'extraction'],
  },
  {
    id: 'kotlin-arrow-l2-072',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is guaranteed finalization in Arrow Fx?',
    options: [
      'Closing the application',
      'Ensuring cleanup actions run even if a coroutine is cancelled',
      'Finalizing a database transaction',
      'Garbage collection',
    ],
    correctAnswer: 1,
    explanation: 'Guaranteed finalization ensures that cleanup/release actions execute even when a coroutine is cancelled. Arrow\'s resource DSL and bracket functions provide this guarantee, which is critical for preventing resource leaks.',
    codeSnippet: 'resource {\n  val connection = install(\n    { openConnection() }\n  ) { conn, exitCase ->\n    conn.close() // guaranteed to run even on cancellation\n    when (exitCase) {\n      is ExitCase.Completed -> log("Clean exit")\n      is ExitCase.Cancelled -> log("Cancelled")\n      is ExitCase.Failure -> log("Failed: ${exitCase.failure}")\n    }\n  }\n}',
    tags: ['arrow-fx', 'finalization', 'resource-safety'],
  },
  {
    id: 'kotlin-arrow-l2-073',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between flatMap and flatten?',
    options: [
      'flatMap is deprecated in favor of flatten',
      'flatten works on lists only',
      'They are the same operation',
      'flatMap = map + flatten; flatten removes one layer of nesting',
    ],
    correctAnswer: 3,
    explanation: 'flatten removes one layer of nesting: Either<E, Either<E, A>> becomes Either<E, A>. flatMap(f) is equivalent to map(f).flatten(). Both are available on Either, List, and other monadic types.',
    codeSnippet: '// map + flatten = flatMap\nval nested: Either<String, Either<String, Int>> =\n  5.right().map { validate(it) } // Either<String, Either<String, Int>>\nval flat: Either<String, Int> = nested.flatten()\n\n// Equivalent:\nval result = 5.right().flatMap { validate(it) }',
    tags: ['functional-patterns', 'flatMap', 'flatten'],
  },
  {
    id: 'kotlin-arrow-l2-074',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is the difference between Lens.get and Lens.set?',
    options: [
      'get is mutable; set is immutable',
      'get reads the focused value; set creates a new structure with the focused value replaced',
      'There is no difference',
      'get returns Option; set returns Either',
    ],
    correctAnswer: 1,
    explanation: 'Lens.get extracts the focused field from a structure. Lens.set takes a structure and a new value, returning a new copy of the structure with the field updated. Neither mutates the original.',
    codeSnippet: 'val person = Person("Alice", 30)\n\n// get: read the field\nval name = Person.name.get(person) // "Alice"\n\n// set: return a new Person with name changed\nval updated = Person.name.set(person, "Bob") // Person("Bob", 30)',
    tags: ['optics', 'lens', 'get-set'],
  },
  {
    id: 'kotlin-arrow-l2-075',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'Can you use the Raise DSL with custom effect types beyond Either?',
    options: [
      'Yes, there are builders for nullable, result, ior, and option',
      'Only with third-party extensions',
      'No, it only works with Either',
      'Only with coroutines',
    ],
    correctAnswer: 0,
    explanation: 'Arrow provides multiple builders that use the Raise DSL: either { }, nullable { }, result { }, ior { }, and option { }. Each produces a different output type but uses the same Raise mechanism internally.',
    codeSnippet: 'val either: Either<String, Int> = either { ... }\nval nullable: Int? = nullable { ... }\nval result: Result<Int> = result { ... }\nval ior: Ior<String, Int> = ior(String::plus) { ... }',
    tags: ['raise-dsl', 'builders', 'polymorphism'],
  },
  {
    id: 'kotlin-arrow-l2-076',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between a total function and a partial function?',
    options: [
      'Partial functions are always recursive',
      'Total functions are longer',
      'Total functions always return Boolean',
      'Total functions are defined for all inputs; partial functions may be undefined for some inputs',
    ],
    correctAnswer: 3,
    explanation: 'A total function returns a value for every possible input. A partial function is undefined for some inputs (e.g., head on an empty list, division by zero). In FP, we prefer total functions, using Either or Option for potentially undefined cases.',
    codeSnippet: '// Partial function (unsafe)\nfun head(list: List<Int>): Int = list.first() // throws on empty!\n\n// Total function (safe)\nfun headOrNull(list: List<Int>): Int? = list.firstOrNull()\nfun headEither(list: List<Int>): Either<String, Int> =\n  Either.fromNullable(list.firstOrNull()) { "Empty list" }',
    tags: ['functional-patterns', 'total-function', 'partial-function'],
  },
  {
    id: 'kotlin-arrow-l2-077',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the idiomatic way to convert Either<Throwable, A> to Either<DomainError, A>?',
    options: [
      'either.recover { DomainError() }',
      'either.cast()',
      'either.mapLeft { throwable -> toDomainError(throwable) }',
      'DomainError(either)',
    ],
    correctAnswer: 2,
    explanation: 'mapLeft transforms the Left (error) side of an Either. Use it to convert low-level errors (Throwable) to domain-specific error types, keeping the Right side unchanged.',
    codeSnippet: 'val raw: Either<Throwable, User> = Either.catch { fetchUser(id) }\n\nval typed: Either<AppError, User> = raw.mapLeft { throwable ->\n  when (throwable) {\n    is IOException -> AppError.Network(throwable.message)\n    is TimeoutException -> AppError.Timeout\n    else -> AppError.Unknown(throwable)\n  }\n}',
    tags: ['either', 'mapLeft', 'error-conversion'],
  },
  {
    id: 'kotlin-arrow-l2-078',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What does the term "effect" mean in functional programming?',
    options: [
      'A visual effect on the UI',
      'A sound effect library',
      'A computation that performs side effects (I/O, state changes, errors, async)',
      'The result of a function call',
    ],
    correctAnswer: 2,
    explanation: 'In FP, an "effect" refers to a computation that involves something beyond pure value transformation: I/O, errors, state, non-determinism, async operations. Effects are typically tracked in the type system (e.g., Either for errors, suspend for async).',
    tags: ['functional-patterns', 'effects', 'theory'],
  },
  {
    id: 'kotlin-arrow-l2-079',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'What is the ExitCase parameter in resource release functions?',
    options: [
      'The operating system exit code',
      'A debug log level',
      'The return value of the use block',
      'An enum indicating whether the resource scope completed, was cancelled, or failed',
    ],
    correctAnswer: 3,
    explanation: 'ExitCase is passed to the release function and indicates how the resource scope ended: Completed (normal), Cancelled (coroutine cancellation), or Failure (exception). This lets release logic adapt to the exit scenario.',
    codeSnippet: 'resource {\n  install({ openTransaction() }) { tx, exitCase ->\n    when (exitCase) {\n      is ExitCase.Completed -> tx.commit()\n      is ExitCase.Cancelled -> tx.rollback()\n      is ExitCase.Failure -> tx.rollback()\n    }\n  }\n}',
    tags: ['resource', 'exit-case', 'release'],
  },
  {
    id: 'kotlin-arrow-l2-080',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the purpose of the also scope function in Kotlin for FP?',
    options: [
      'It creates a new object',
      'It transforms the receiver',
      'It is a replacement for let',
      'It executes a side-effect action with the receiver and returns the receiver unchanged',
    ],
    correctAnswer: 3,
    explanation: 'also executes a block with the object as the parameter (it) and returns the object unchanged. It is used for side effects like logging, validation, or debugging without interrupting a fluent chain.',
    codeSnippet: 'val user = createUser()\n  .also { logger.info("Created user: ${it.name}") }\n  .also { analytics.track("user_created", it.id) }\n// user is unchanged, side effects executed',
    tags: ['functional-patterns', 'scope-functions', 'also'],
  },
  {
    id: 'kotlin-arrow-l2-081',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does either.getOrElse { } differ from fold { } { } on Either?',
    options: [
      'getOrElse only handles the Left case; fold handles both Left and Right with transformations',
      'fold is deprecated',
      'getOrElse throws on Left',
      'They are identical',
    ],
    correctAnswer: 0,
    explanation: 'getOrElse returns the Right value or computes a default from the Left. fold takes two functions, one for each case, and can transform both sides. fold is more general; getOrElse is a specialization for extracting values.',
    codeSnippet: '// getOrElse: extract or default\nval value: Int = either.getOrElse { 0 }\n\n// fold: handle both cases with different logic\nval message: String = either.fold(\n  { error -> "Failed: $error" },\n  { value -> "Success: $value" }\n)',
    tags: ['raise-dsl', 'getOrElse', 'fold'],
  },
  {
    id: 'kotlin-arrow-l2-082',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'Can you use optics with mutable data in Kotlin?',
    options: [
      'Only with var properties',
      'Only with MutableList fields',
      'Yes, optics require mutable data',
      'No, optics are designed for immutable data and return new copies',
    ],
    correctAnswer: 3,
    explanation: 'Optics are designed for immutable data structures. Operations like set and modify always return new copies rather than mutating in place. This aligns with the FP principle of immutability.',
    codeSnippet: 'val original = Person("Alice", 30)\nval updated = Person.age.set(original, 31)\n\nprintln(original.age) // 30 (unchanged)\nprintln(updated.age)  // 31 (new copy)',
    tags: ['optics', 'immutability', 'copy'],
  },
  {
    id: 'kotlin-arrow-l2-083',
    language: 'kotlin',
    level: 'level2',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Church encoding principle?',
    options: [
      'A database normalization technique',
      'A naming convention for classes',
      'Representing data structures as functions instead of concrete types',
      'An encryption algorithm',
    ],
    correctAnswer: 2,
    explanation: 'Church encoding represents data as functions. For example, a boolean can be a function that takes two arguments and returns one. In practice, fold is the Church encoding of algebraic data types - it replaces constructors with functions.',
    codeSnippet: '// Either\'s fold is its Church encoding\n// Instead of matching on Left/Right constructors,\n// you provide functions for each case:\nval result: String = either.fold(\n  { left -> handleLeft(left) },   // replaces Left constructor\n  { right -> handleRight(right) }  // replaces Right constructor\n)',
    tags: ['functional-patterns', 'church-encoding', 'theory'],
  },
];

export default questions;
