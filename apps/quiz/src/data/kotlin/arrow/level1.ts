import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-arrow-l1-001',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Arrow\'s Either type represent?',
    options: [
      'A list with at most one element',
      'A value that is either Left (error) or Right (success)',
      'A pair of two values',
      'A nullable value',
    ],
    correctAnswer: 1,
    explanation: 'Either<A, B> is a disjoint union that holds either a Left(A) representing an error/failure, or a Right(B) representing a success value. By convention, Left is the error side and Right is the success side.',
    tags: ['either', 'basics', 'error-handling'],
  },
  {
    id: 'kotlin-arrow-l1-002',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you create a Right value in Arrow?',
    options: [
      '42.right()',
      'All of the above',
      'Right(42)',
      'Either.right(42)',
    ],
    correctAnswer: 0,
    explanation: 'In Arrow, the idiomatic way to create a Right value is using the extension function 42.right(). You can also use Either.Right(42) or the Right(42) constructor, but the extension function is most common.',
    codeSnippet: 'val result: Either<String, Int> = 42.right()',
    tags: ['either', 'construction', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-003',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does the following code produce?\n\nval x: Either<String, Int> = "error".left()\nval result = x.map { it * 2 }',
    options: [
      'Right(0)',
      'Left("errorerror")',
      'Left("error")',
      'A compilation error',
    ],
    correctAnswer: 2,
    explanation: 'The map function on Either only transforms the Right side. Since x is a Left, the map operation is skipped entirely and the original Left("error") is returned unchanged.',
    codeSnippet: 'val x: Either<String, Int> = "error".left()\nval result = x.map { it * 2 }\n// result == Left("error")',
    tags: ['either', 'map', 'left'],
  },
  {
    id: 'kotlin-arrow-l1-004',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'Which function transforms the Left side of an Either?',
    options: [
      'map',
      'flatMap',
      'mapLeft',
      'fold',
    ],
    correctAnswer: 2,
    explanation: 'mapLeft transforms the Left side of an Either while leaving the Right side unchanged. map transforms only the Right side. flatMap chains computations on the Right side. fold handles both sides.',
    codeSnippet: 'val x: Either<String, Int> = "error".left()\nval result = x.mapLeft { it.uppercase() }\n// result == Left("ERROR")',
    tags: ['either', 'mapLeft', 'transformation'],
  },
  {
    id: 'kotlin-arrow-l1-005',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does fold on Either do?',
    options: [
      'Applies one function for Left and another for Right, returning a single value',
      'Flattens nested Either types',
      'Combines two Eithers into one',
      'Converts Either to a list',
    ],
    correctAnswer: 0,
    explanation: 'fold takes two functions: one for the Left case and one for the Right case. It extracts the value from the Either by applying the appropriate function, producing a single result value.',
    codeSnippet: 'val result = either.fold(\n  { error -> "Failed: $error" },\n  { value -> "Success: $value" }\n)',
    tags: ['either', 'fold', 'pattern-matching'],
  },
  {
    id: 'kotlin-arrow-l1-006',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the output of the following code?\n\nval x = Either.Right(5)\nval y = x.flatMap { Either.Right(it + 10) }\nprintln(y)',
    options: [
      'Right(5)',
      'Right(Right(15))',
      'Right(15)',
      'Left(15)',
    ],
    correctAnswer: 2,
    explanation: 'flatMap on Either applies the function to the Right value and returns the resulting Either. Here, 5 + 10 = 15, so the result is Right(15). Unlike map, flatMap does not nest the result in another Right wrapper.',
    codeSnippet: 'val x = Either.Right(5)\nval y = x.flatMap { Either.Right(it + 10) }\nprintln(y) // Either.Right(15)',
    tags: ['either', 'flatMap', 'chaining'],
  },
  {
    id: 'kotlin-arrow-l1-007',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you extract the Right value from an Either or provide a default?',
    options: [
      'either.getOrElse { defaultValue }',
      'either.value',
      'either.get()',
      'either.unwrap()',
    ],
    correctAnswer: 0,
    explanation: 'getOrElse returns the Right value if present, or evaluates the provided lambda to produce a default value if the Either is a Left. This is a safe way to extract values without risking exceptions.',
    codeSnippet: 'val result: Int = either.getOrElse { 0 }',
    tags: ['either', 'getOrElse', 'extraction'],
  },
  {
    id: 'kotlin-arrow-l1-008',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does either.isRight() return when called on Either.Left("error")?',
    options: [
      'false',
      'true',
      'Throws an exception',
      'null',
    ],
    correctAnswer: 0,
    explanation: 'isRight() returns false when called on a Left value. Similarly, isLeft() would return true. These functions are useful for simple conditional checks on Either values.',
    tags: ['either', 'isRight', 'isLeft'],
  },
  {
    id: 'kotlin-arrow-l1-009',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a pure function?',
    options: [
      'A function that never throws exceptions',
      'A function written in a functional language',
      'A function that always returns the same output for the same input and has no side effects',
      'A function that takes no parameters',
    ],
    correctAnswer: 2,
    explanation: 'A pure function has two properties: (1) given the same inputs, it always produces the same output (referential transparency), and (2) it produces no side effects such as I/O, mutation, or state changes.',
    tags: ['functional-patterns', 'pure-function', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-010',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is referential transparency?',
    options: [
      'A reference that is visible from all scopes',
      'A transparent proxy pattern',
      'An expression that can be replaced with its value without changing program behavior',
      'The ability to see through references to the actual object',
    ],
    correctAnswer: 2,
    explanation: 'Referential transparency means an expression can be replaced with its evaluated value without changing the behavior of the program. This is a key property of pure functions and makes reasoning about code much easier.',
    tags: ['functional-patterns', 'referential-transparency', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-011',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is function composition?',
    options: [
      'Creating a class that contains functions',
      'Writing functions inside other functions',
      'Defining multiple overloads of a function',
      'Combining two functions where the output of one becomes the input of another',
    ],
    correctAnswer: 3,
    explanation: 'Function composition combines two functions f and g into a new function h where h(x) = g(f(x)). The output of the first function is passed as input to the second. Arrow provides compose and andThen operators for this.',
    codeSnippet: 'val double = { x: Int -> x * 2 }\nval addOne = { x: Int -> x + 1 }\nval doubleThenAdd = double andThen addOne\n// doubleThenAdd(3) == 7',
    tags: ['functional-patterns', 'composition', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-012',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a higher-order function?',
    options: [
      'A recursive function',
      'A function defined in a superclass',
      'A function with more than 3 parameters',
      'A function that takes functions as parameters or returns a function',
    ],
    correctAnswer: 3,
    explanation: 'A higher-order function either takes one or more functions as arguments, returns a function, or both. Examples include map, filter, fold, and flatMap. Kotlin has first-class support for higher-order functions via lambdas.',
    codeSnippet: 'fun <A, B> transform(value: A, f: (A) -> B): B = f(value)\n// transform(5) { it * 2 } == 10',
    tags: ['functional-patterns', 'higher-order-function', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-013',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Result Type',
    question: 'What is Kotlin\'s built-in Result type used for?',
    options: [
      'Storing key-value pairs',
      'Encapsulating a success value or a failure with an exception',
      'Representing nullable values',
      'Wrapping coroutine return values only',
    ],
    correctAnswer: 1,
    explanation: 'Kotlin\'s Result<T> encapsulates either a successful value of type T or a Throwable representing failure. It provides functional-style error handling similar to Either but specifically tied to exceptions.',
    codeSnippet: 'val result = runCatching { "42".toInt() }\n// Result.success(42)',
    tags: ['result', 'error-handling', 'kotlin-stdlib'],
  },
  {
    id: 'kotlin-arrow-l1-014',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Result Type',
    question: 'How do you create a successful Result in Kotlin?',
    options: [
      'Result.success(42)',
      'Result.ok(42)',
      'Result(42)',
      'Result.of(42)',
    ],
    correctAnswer: 0,
    explanation: 'Result.success(value) creates a Result wrapping a successful value. For failures, use Result.failure(exception). You can also use runCatching { ... } which catches exceptions and wraps them in a Result.',
    codeSnippet: 'val ok = Result.success(42)\nval fail = Result.failure<Int>(RuntimeException("oops"))',
    tags: ['result', 'construction', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-015',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Result Type',
    question: 'What does runCatching do in Kotlin?',
    options: [
      'Catches only checked exceptions',
      'Runs a block and ignores exceptions',
      'Runs a block in a separate thread',
      'Runs a block and wraps the result or exception in a Result',
    ],
    correctAnswer: 3,
    explanation: 'runCatching executes the given block and returns Result.success(value) if it succeeds, or Result.failure(exception) if it throws. It is a convenient way to convert exception-based code to Result-based code.',
    codeSnippet: 'val result = runCatching { "abc".toInt() }\n// Result.failure(NumberFormatException)',
    tags: ['result', 'runCatching', 'error-handling'],
  },
  {
    id: 'kotlin-arrow-l1-016',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Sealed Class Error Handling',
    question: 'Why are sealed classes useful for error handling in Kotlin?',
    options: [
      'The compiler ensures when expressions cover all subtypes',
      'They can only be used with Arrow',
      'They automatically catch exceptions',
      'They are faster than regular classes',
    ],
    correctAnswer: 0,
    explanation: 'Sealed classes restrict the hierarchy to a known set of subtypes. When used in when expressions, the compiler can verify exhaustiveness, ensuring every error case is handled. This makes error handling safer and more maintainable.',
    codeSnippet: 'sealed class AppError {\n  data class NotFound(val id: String) : AppError()\n  data class Unauthorized(val msg: String) : AppError()\n}',
    tags: ['sealed-class', 'error-handling', 'exhaustive'],
  },
  {
    id: 'kotlin-arrow-l1-017',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Sealed Class Error Handling',
    question: 'What happens if you forget a branch in a when expression over a sealed class used as a statement?',
    options: [
      'Nothing, it compiles fine',
      'The missing branch defaults to null',
      'Runtime exception',
      'Compilation error',
    ],
    correctAnswer: 0,
    explanation: 'When a when expression over a sealed class is used as a statement (not as an expression), missing branches do NOT cause a compilation error in Kotlin. However, when used as an expression (assigned to a variable), the compiler requires exhaustiveness.',
    tags: ['sealed-class', 'when', 'exhaustive'],
  },
  {
    id: 'kotlin-arrow-l1-018',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Sealed Class Error Handling',
    question: 'What is the output of this code?\n\nsealed class Result {\n  data class Ok(val v: Int) : Result()\n  data class Err(val e: String) : Result()\n}\nval r: Result = Result.Ok(5)\nval msg = when (r) {\n  is Result.Ok -> "Got ${r.v}"\n  is Result.Err -> "Error: ${r.e}"\n}\nprintln(msg)',
    options: [
      'null',
      'Got 5',
      'Error: 5',
      'Compilation error',
    ],
    correctAnswer: 1,
    explanation: 'The variable r is Result.Ok(5). The when expression matches the is Result.Ok branch, smart-casts r to Result.Ok, and accesses r.v which is 5. The output is "Got 5".',
    codeSnippet: 'sealed class Result {\n  data class Ok(val v: Int) : Result()\n  data class Err(val e: String) : Result()\n}\nval r: Result = Result.Ok(5)\nval msg = when (r) {\n  is Result.Ok -> "Got ${r.v}"\n  is Result.Err -> "Error: ${r.e}"\n}\nprintln(msg) // Got 5',
    tags: ['sealed-class', 'smart-cast', 'when'],
  },
  {
    id: 'kotlin-arrow-l1-019',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the difference between map and flatMap on Either?',
    options: [
      'They are identical',
      'map wraps the result in Either, flatMap expects the function to return Either',
      'flatMap is deprecated in favor of map',
      'map works on Left, flatMap works on Right',
    ],
    correctAnswer: 1,
    explanation: 'map transforms the Right value and wraps the result back in Right. flatMap expects the transformation function to return an Either, allowing you to chain operations that themselves may fail. flatMap "flattens" the nested Either.',
    codeSnippet: 'val x = 5.right()\nx.map { it + 1 }      // Right(6)\nx.flatMap { if (it > 0) it.right() else "neg".left() }',
    tags: ['either', 'map', 'flatMap'],
  },
  {
    id: 'kotlin-arrow-l1-020',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Option',
    question: 'What did Arrow\'s Option type represent before it was deprecated?',
    options: [
      'An asynchronous computation',
      'A command-line argument',
      'A value that may or may not be present (Some or None)',
      'A configuration option',
    ],
    correctAnswer: 2,
    explanation: 'Arrow\'s Option<A> represented an optional value: either Some(value) or None. It was deprecated in Arrow 2.x because Kotlin\'s nullable types (A?) serve the same purpose more idiomatically.',
    tags: ['option', 'deprecated', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-021',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Option',
    question: 'Why was Arrow\'s Option type deprecated in Arrow 2.x?',
    options: [
      'It had performance issues',
      'Kotlin\'s nullable types (A?) already serve the same purpose',
      'It conflicted with Java\'s Optional',
      'It was never widely used',
    ],
    correctAnswer: 1,
    explanation: 'Kotlin has first-class null safety with nullable types (A?). Since A? already represents "a value that may or may not exist," Arrow\'s Option was redundant. The Arrow team deprecated it in favor of Kotlin\'s native nullable types.',
    tags: ['option', 'deprecated', 'nullable'],
  },
  {
    id: 'kotlin-arrow-l1-022',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Option',
    question: 'How would you convert a nullable Kotlin value to an Arrow Either?',
    options: [
      'value.toEither()',
      'Either.fromNullable(value) { "was null" }',
      'value?.right() ?: "null".left()',
      'Both B and C are valid approaches',
    ],
    correctAnswer: 3,
    explanation: 'You can use Either.fromNullable which takes the nullable value and a lambda that produces the Left value if null. You can also manually check with the Elvis operator. Both approaches are valid.',
    codeSnippet: 'val x: Int? = null\nval either = Either.fromNullable(x) { "Value was null" }\n// Left("Value was null")',
    tags: ['option', 'nullable', 'either'],
  },
  {
    id: 'kotlin-arrow-l1-023',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'What is a NonEmptyList (Nel) in Arrow?',
    options: [
      'A list that can hold null values',
      'An immutable list',
      'A list that cannot be modified after creation',
      'A list guaranteed to have at least one element',
    ],
    correctAnswer: 3,
    explanation: 'NonEmptyList (aliased as Nel) is a list type that guarantees at least one element at the type level. This eliminates the need to check for emptiness and makes operations like head always safe.',
    codeSnippet: 'val nel = nonEmptyListOf(1, 2, 3)\nval head: Int = nel.head // always safe, no exception possible',
    tags: ['nel', 'non-empty-list', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-024',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'How do you create a NonEmptyList in Arrow?',
    options: [
      'NonEmptyList(1, 2, 3)',
      'nonEmptyListOf(1, 2, 3)',
      'listOf(1, 2, 3).toNonEmpty()',
      'Nel.of(1, 2, 3)',
    ],
    correctAnswer: 1,
    explanation: 'The idiomatic way to create a NonEmptyList is using the nonEmptyListOf() factory function. It requires at least one argument, enforcing the non-empty guarantee at compile time.',
    codeSnippet: 'val nel = nonEmptyListOf(1, 2, 3)\nprintln(nel) // NonEmptyList(1, 2, 3)',
    tags: ['nel', 'construction', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-025',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'What does calling .head on a NonEmptyList return?',
    options: [
      'An Option<A> since the list might be empty',
      'null if the list is empty',
      'The first element directly, since the list is guaranteed non-empty',
      'A Result wrapping the first element',
    ],
    correctAnswer: 2,
    explanation: 'Since NonEmptyList guarantees at least one element, head returns the element directly (type A, not Option<A>). There is no risk of an empty-list exception, which is the key advantage over regular lists.',
    codeSnippet: 'val nel = nonEmptyListOf(42, 1, 2)\nval first: Int = nel.head // 42, always safe',
    tags: ['nel', 'head', 'safety'],
  },
  {
    id: 'kotlin-arrow-l1-026',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is immutability in functional programming?',
    options: [
      'Data that cannot be changed after creation',
      'Classes that cannot be inherited',
      'Functions that cannot be overridden',
      'Objects that cannot be garbage collected',
    ],
    correctAnswer: 0,
    explanation: 'Immutability means that once data is created, it cannot be modified. Instead of changing existing data, you create new data with the desired changes. This eliminates many concurrency bugs and makes code easier to reason about.',
    tags: ['functional-patterns', 'immutability', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-027',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'Which Kotlin keyword declares an immutable variable?',
    options: [
      'final',
      'var',
      'val',
      'const',
    ],
    correctAnswer: 2,
    explanation: 'val declares a read-only (immutable) reference in Kotlin. Once assigned, it cannot be reassigned. var declares a mutable reference. const is used for compile-time constants. final is a Java keyword.',
    codeSnippet: 'val x = 42    // immutable reference\nvar y = 42    // mutable reference\ny = 43        // OK\n// x = 43     // Compilation error',
    tags: ['functional-patterns', 'immutability', 'kotlin-basics'],
  },
  {
    id: 'kotlin-arrow-l1-028',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either.catch { } do in Arrow?',
    options: [
      'Returns null on exception',
      'Catches only checked exceptions',
      'Ignores all exceptions',
      'Executes a block and wraps the result in Right, or catches exceptions as Left',
    ],
    correctAnswer: 3,
    explanation: 'Either.catch executes the given block. If it succeeds, the result is wrapped in Right. If it throws an exception, the exception is caught and wrapped in Left. It bridges exception-based code into the Either world.',
    codeSnippet: 'val result: Either<Throwable, Int> = Either.catch {\n  "42".toInt()\n}\n// Right(42)',
    tags: ['either', 'catch', 'exception-handling'],
  },
  {
    id: 'kotlin-arrow-l1-029',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What type does Either.catch return for the Left side?',
    options: [
      'Error',
      'Exception',
      'Throwable',
      'String',
    ],
    correctAnswer: 2,
    explanation: 'Either.catch returns Either<Throwable, A>. The Left side is always Throwable, which is the supertype of all exceptions and errors in Kotlin/JVM. You can use mapLeft to convert it to a more specific error type.',
    codeSnippet: 'val result: Either<Throwable, Int> = Either.catch {\n  "abc".toInt() // throws NumberFormatException\n}',
    tags: ['either', 'catch', 'throwable'],
  },
  {
    id: 'kotlin-arrow-l1-030',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a side effect in functional programming?',
    options: [
      'Any observable interaction with the outside world (I/O, mutation, etc.)',
      'A function with multiple return statements',
      'A function that returns a value',
      'An unexpected exception',
    ],
    correctAnswer: 0,
    explanation: 'A side effect is any operation that interacts with the outside world or modifies state beyond computing a return value. Examples include writing to a file, modifying a variable, printing to console, or making a network request.',
    tags: ['functional-patterns', 'side-effects', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-031',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the output?\n\nval e: Either<String, Int> = 10.right()\nval result = e.getOrElse { -1 }\nprintln(result)',
    options: [
      'null',
      '10',
      'Either.Right(10)',
      '-1',
    ],
    correctAnswer: 1,
    explanation: 'Since e is Right(10), getOrElse returns the Right value directly, which is 10. The default lambda { -1 } is not evaluated because the Either is a Right.',
    codeSnippet: 'val e: Either<String, Int> = 10.right()\nval result = e.getOrElse { -1 }\nprintln(result) // 10',
    tags: ['either', 'getOrElse', 'extraction'],
  },
  {
    id: 'kotlin-arrow-l1-032',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What does the term "monad" informally refer to in FP?',
    options: [
      'A type that supports flatMap (or bind) and a way to wrap a value',
      'A parallel computation framework',
      'A Kotlin annotation',
      'A design pattern for singleton objects',
    ],
    correctAnswer: 0,
    explanation: 'Informally, a monad is a type constructor M that supports two operations: a way to put a value into the monad (return/pure) and flatMap (bind) to chain computations. Either, List, and Option are all monads.',
    tags: ['functional-patterns', 'monad', 'theory'],
  },
  {
    id: 'kotlin-arrow-l1-033',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is currying?',
    options: [
      'A way to cache function results',
      'Converting synchronous code to asynchronous',
      'Transforming a function with multiple arguments into a chain of single-argument functions',
      'A debugging technique',
    ],
    correctAnswer: 2,
    explanation: 'Currying transforms a function f(a, b) into f(a)(b), where each function takes a single argument and returns a new function. This enables partial application and more flexible function composition.',
    codeSnippet: 'val add = { a: Int, b: Int -> a + b }\nval curriedAdd = add.curried()\nval addFive = curriedAdd(5)\nprintln(addFive(3)) // 8',
    tags: ['functional-patterns', 'currying', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-034',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is partial application?',
    options: [
      'Applying a function inside a try-catch block',
      'Applying a function to only some of its arguments, returning a new function for the rest',
      'Running only part of a program',
      'A function that sometimes fails',
    ],
    correctAnswer: 1,
    explanation: 'Partial application fixes some arguments of a function and produces a new function that takes the remaining arguments. Unlike currying (which always produces single-argument functions), partial application can fix any number of arguments.',
    codeSnippet: 'val add = { a: Int, b: Int -> a + b }\nval addFive = add.partially1(5)\nprintln(addFive(3)) // 8',
    tags: ['functional-patterns', 'partial-application', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-035',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'Which of these correctly chains two Either-returning operations?',
    options: [
      'parseInput(s).then { validate(it) }',
      'parseInput(s).flatMap { validate(it) }',
      'parseInput(s).map { validate(it) }',
      'parseInput(s).andThen { validate(it) }',
    ],
    correctAnswer: 1,
    explanation: 'When chaining operations that each return Either, use flatMap. If you used map, you would get Either<E, Either<E, B>> (nested). flatMap flattens the result so you get Either<E, B>.',
    codeSnippet: 'fun parseInput(s: String): Either<Error, Int> = ...\nfun validate(n: Int): Either<Error, Int> = ...\n\nval result = parseInput("42").flatMap { validate(it) }',
    tags: ['either', 'flatMap', 'chaining'],
  },
  {
    id: 'kotlin-arrow-l1-036',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Railway-Oriented Programming',
    question: 'What is Railway-oriented programming?',
    options: [
      'A database transaction pattern',
      'A multithreading pattern',
      'A train simulation pattern',
      'A metaphor for chaining operations where each step can succeed (stay on track) or fail (switch to error track)',
    ],
    correctAnswer: 3,
    explanation: 'Railway-oriented programming is a metaphor where computations flow along two tracks: a success track and a failure track. Each operation can either continue on the success track or switch to the failure track. Either and flatMap implement this pattern.',
    tags: ['railway-oriented', 'error-handling', 'design-pattern'],
  },
  {
    id: 'kotlin-arrow-l1-037',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Railway-Oriented Programming',
    question: 'In railway-oriented programming with Either, what happens when a Left is encountered in a flatMap chain?',
    options: [
      'The Left is converted to Right automatically',
      'The chain continues with a default value',
      'An exception is thrown',
      'The remaining operations are skipped and the Left propagates',
    ],
    correctAnswer: 3,
    explanation: 'When a Left is encountered in a flatMap chain, all subsequent flatMap operations are short-circuited. The Left value propagates directly to the final result without executing any more transformations.',
    codeSnippet: 'val result = step1()\n  .flatMap { step2(it) }   // skipped if step1 returns Left\n  .flatMap { step3(it) }   // skipped if step1 or step2 returns Left',
    tags: ['railway-oriented', 'short-circuit', 'either'],
  },
  {
    id: 'kotlin-arrow-l1-038',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the Raise DSL in Arrow?',
    options: [
      'A way to throw exceptions more safely',
      'A context receiver-based approach for typed error handling without Either wrapping',
      'A logging framework',
      'A DSL for raising events in an event bus',
    ],
    correctAnswer: 1,
    explanation: 'The Raise DSL uses Kotlin context receivers to provide typed error handling. Inside a Raise<E> context, you can call raise(error) to short-circuit with a typed error, without explicitly wrapping values in Either.',
    codeSnippet: 'fun Raise<String>.validateAge(age: Int): Int {\n  if (age < 0) raise("Age cannot be negative")\n  return age\n}',
    tags: ['raise-dsl', 'typed-errors', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-039',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you run a computation that uses the Raise DSL and get an Either result?',
    options: [
      'Raise.execute { ... }',
      'raise { ... }',
      'either { ... }',
      'Either.run { ... }',
    ],
    correctAnswer: 2,
    explanation: 'The either { } builder runs a computation in a Raise<E> context and returns Either<E, A>. If raise(error) is called inside, it returns Left(error). Otherwise, the block\'s return value is wrapped in Right.',
    codeSnippet: 'val result: Either<String, Int> = either {\n  val age = validateAge(25) // uses Raise<String>\n  age\n}\n// Right(25)',
    tags: ['raise-dsl', 'either-builder', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-040',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What does calling raise("error") inside an either { } block do?',
    options: [
      'Throws a RuntimeException',
      'Logs the error and continues',
      'Returns null from the block',
      'Short-circuits the block and returns Left("error")',
    ],
    correctAnswer: 3,
    explanation: 'Calling raise(error) inside an either { } block immediately short-circuits the entire block and produces Left(error) as the result. No subsequent code in the block is executed.',
    codeSnippet: 'val result = either<String, Int> {\n  raise("something went wrong")\n  42 // never reached\n}\n// result == Left("something went wrong")',
    tags: ['raise-dsl', 'short-circuit', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-041',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you unwrap an Either inside an either { } block using the Raise DSL?',
    options: [
      'either.value',
      'either.bind()',
      'either.unwrap()',
      'either.get()',
    ],
    correctAnswer: 1,
    explanation: 'Inside an either { } block, calling .bind() on an Either extracts the Right value. If the Either is Left, bind() short-circuits the block with that Left. This is the primary way to compose Either values in the Raise DSL.',
    codeSnippet: 'val result = either {\n  val a = computeA().bind() // unwrap or short-circuit\n  val b = computeB().bind()\n  a + b\n}',
    tags: ['raise-dsl', 'bind', 'composition'],
  },
  {
    id: 'kotlin-arrow-l1-042',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either.Left("error").swap() return?',
    options: [
      'Right("error")',
      'Throws an exception',
      'null',
      'Left("error")',
    ],
    correctAnswer: 0,
    explanation: 'swap() exchanges Left and Right. A Left("error") becomes Right("error"), and vice versa. This is useful when you want to work with the error side using Right-biased operations.',
    codeSnippet: 'val original = "error".left()\nval swapped = original.swap()\n// swapped == Right("error")',
    tags: ['either', 'swap', 'transformation'],
  },
  {
    id: 'kotlin-arrow-l1-043',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a functor in FP terms?',
    options: [
      'A factory for creating objects',
      'A function that takes no arguments',
      'A testing framework component',
      'A type that supports a map operation preserving structure',
    ],
    correctAnswer: 3,
    explanation: 'A functor is a type constructor that supports a map operation. Map applies a function to the value(s) inside the functor while preserving its structure. List, Either, and Option are all functors.',
    codeSnippet: '// List is a functor\nlistOf(1, 2, 3).map { it * 2 } // [2, 4, 6]\n\n// Either is a functor\n5.right().map { it * 2 } // Right(10)',
    tags: ['functional-patterns', 'functor', 'theory'],
  },
  {
    id: 'kotlin-arrow-l1-044',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Result Type',
    question: 'What does result.getOrDefault(0) do?',
    options: [
      'Throws if the result is a failure',
      'Returns 0 always',
      'Returns the success value or 0 if it is a failure',
      'Returns null if the result is a failure',
    ],
    correctAnswer: 2,
    explanation: 'getOrDefault returns the encapsulated value if the Result is a success, or the provided default value if it is a failure. It is a safe way to extract values without handling exceptions.',
    codeSnippet: 'val r = Result.failure<Int>(Exception("oops"))\nval v = r.getOrDefault(0) // 0',
    tags: ['result', 'getOrDefault', 'extraction'],
  },
  {
    id: 'kotlin-arrow-l1-045',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Result Type',
    question: 'How do you chain operations on a Kotlin Result?',
    options: [
      'result.then { ... }',
      'result.chain { ... }',
      'result.flatMap { ... }',
      'result.map { ... }',
    ],
    correctAnswer: 3,
    explanation: 'Kotlin\'s Result supports map for transforming the success value. Note that Kotlin\'s standard library Result does not have a flatMap equivalent; you would need to use mapCatching or fold for more complex chaining.',
    codeSnippet: 'val result = Result.success(5)\nval doubled = result.map { it * 2 }\n// Result.success(10)',
    tags: ['result', 'map', 'chaining'],
  },
  {
    id: 'kotlin-arrow-l1-046',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What does the let scope function do in Kotlin?',
    options: [
      'Creates a new thread',
      'Declares a variable',
      'Defines a lazy property',
      'Calls the given block with the object as argument and returns the block\'s result',
    ],
    correctAnswer: 3,
    explanation: 'let is a scope function that calls the specified block with the object as the argument (it) and returns the result of the block. It is often used with safe calls (?.) for null-safe transformations.',
    codeSnippet: 'val name: String? = "Kotlin"\nval length = name?.let { it.length } // 6',
    tags: ['functional-patterns', 'scope-functions', 'let'],
  },
  {
    id: 'kotlin-arrow-l1-047',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'Can you pattern match on Either using Kotlin\'s when expression?',
    options: [
      'No, Either does not support pattern matching',
      'Yes, using is Either.Left and is Either.Right',
      'Only using fold',
      'Only with a special Arrow plugin',
    ],
    correctAnswer: 1,
    explanation: 'Since Either is a sealed class with Left and Right subtypes, you can use Kotlin\'s when expression with is checks. The compiler ensures exhaustiveness when used as an expression.',
    codeSnippet: 'when (result) {\n  is Either.Left -> println("Error: ${result.value}")\n  is Either.Right -> println("Success: ${result.value}")\n}',
    tags: ['either', 'pattern-matching', 'when'],
  },
  {
    id: 'kotlin-arrow-l1-048',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a lambda expression in Kotlin?',
    options: [
      'A type alias',
      'A macro',
      'A named function declaration',
      'An anonymous function literal enclosed in curly braces',
    ],
    correctAnswer: 3,
    explanation: 'A lambda expression in Kotlin is a function literal (anonymous function) enclosed in curly braces { }. It can be passed as an argument, stored in a variable, and is the foundation of functional programming in Kotlin.',
    codeSnippet: 'val double: (Int) -> Int = { x -> x * 2 }\nval result = double(5) // 10',
    tags: ['functional-patterns', 'lambda', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-049',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What does the it keyword refer to in a Kotlin lambda?',
    options: [
      'The implicit name for a single parameter in a lambda',
      'The iterator in a loop',
      'A null reference',
      'The current class instance',
    ],
    correctAnswer: 0,
    explanation: 'When a lambda has exactly one parameter, you can omit the parameter declaration and use it to refer to it. This is a Kotlin convenience that makes short lambdas more concise.',
    codeSnippet: 'listOf(1, 2, 3).map { it * 2 }\n// equivalent to: .map { x -> x * 2 }',
    tags: ['functional-patterns', 'lambda', 'it-keyword'],
  },
  {
    id: 'kotlin-arrow-l1-050',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'Can you create a NonEmptyList from a regular List?',
    options: [
      'No, there is no conversion',
      'Yes, using toNonEmptyList() which throws if the list is empty',
      'Both B and C are available',
      'Yes, using toNonEmptyListOrNull() which returns null if the list is empty',
    ],
    correctAnswer: 2,
    explanation: 'Arrow provides both toNonEmptyListOrNull() which returns null for empty lists (safe), and toNonEmptyListOrNone() for Option-based handling. You can also use direct construction which requires at least one element.',
    codeSnippet: 'val list = listOf(1, 2, 3)\nval nel = list.toNonEmptyListOrNull() // NonEmptyList(1, 2, 3)\n\nval empty = emptyList<Int>()\nval nelOrNull = empty.toNonEmptyListOrNull() // null',
    tags: ['nel', 'conversion', 'safety'],
  },
  {
    id: 'kotlin-arrow-l1-051',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Ior',
    question: 'What does Arrow\'s Ior type represent?',
    options: [
      'An input/output record',
      'An indexed ordered result',
      'An inclusive OR: Left, Right, or Both',
      'An exclusive OR: only Left or Right',
    ],
    correctAnswer: 2,
    explanation: 'Ior (Inclusive Or) can hold a Left value, a Right value, or Both values simultaneously. This is useful when you want to accumulate warnings alongside a successful result.',
    codeSnippet: 'val left: Ior<String, Int> = Ior.Left("warning")\nval right: Ior<String, Int> = Ior.Right(42)\nval both: Ior<String, Int> = Ior.Both("warning", 42)',
    tags: ['ior', 'inclusive-or', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-052',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Ior',
    question: 'When would you use Ior instead of Either?',
    options: [
      'When you need to handle null values',
      'When you need better performance',
      'When working with coroutines',
      'When you want to accumulate warnings while still producing a result',
    ],
    correctAnswer: 3,
    explanation: 'Ior is useful when an operation can succeed with warnings. Unlike Either which is strictly success OR failure, Ior.Both lets you return a result alongside accumulated warnings or non-fatal errors.',
    tags: ['ior', 'warnings', 'use-case'],
  },
  {
    id: 'kotlin-arrow-l1-053',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Validated',
    question: 'What was the main purpose of Arrow\'s Validated type?',
    options: [
      'Accumulating multiple validation errors instead of failing on the first',
      'Validating JSON schemas',
      'Validating database connections',
      'Checking type safety at compile time',
    ],
    correctAnswer: 0,
    explanation: 'Validated was designed for error accumulation. Unlike Either which short-circuits on the first error, Validated collects all errors. This is ideal for form validation where you want to report all issues at once.',
    tags: ['validated', 'error-accumulation', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-054',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Validated',
    question: 'What replaced Validated in newer versions of Arrow?',
    options: [
      'Result',
      'Either with zipOrAccumulate',
      'Validation from kotlinx',
      'Try',
    ],
    correctAnswer: 1,
    explanation: 'In Arrow 2.x, Validated was deprecated in favor of using Either with zipOrAccumulate or the Raise DSL with accumulation. These provide the same error-accumulation capability without a separate type.',
    codeSnippet: 'val result = either {\n  zipOrAccumulate(\n    { validate1() },\n    { validate2() },\n    { validate3() }\n  ) { a, b, c -> Result(a, b, c) }\n}',
    tags: ['validated', 'deprecated', 'zipOrAccumulate'],
  },
  {
    id: 'kotlin-arrow-l1-055',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What are optics in Arrow?',
    options: [
      'Composable tools for accessing and modifying immutable data structures',
      'A serialization framework',
      'A graphics rendering library',
      'A reflection API',
    ],
    correctAnswer: 0,
    explanation: 'Optics are composable, type-safe accessors for reading and modifying parts of immutable data structures. They solve the problem of deeply nested immutable data updates without boilerplate copy() calls.',
    tags: ['optics', 'basics', 'immutable-data'],
  },
  {
    id: 'kotlin-arrow-l1-056',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What problem do optics solve?',
    options: [
      'Network latency',
      'Thread safety',
      'Memory management',
      'Deeply nested immutable data updates without verbose copy() chains',
    ],
    correctAnswer: 3,
    explanation: 'With deeply nested data classes, updating a leaf property requires copy() at every level. Optics provide composable lenses that handle the nesting, making updates concise and readable.',
    codeSnippet: '// Without optics (verbose):\nperson.copy(address = person.address.copy(\n  city = person.address.city.copy(name = "New York")))\n\n// With optics (concise):\nPerson.address.city.name.modify(person) { "New York" }',
    tags: ['optics', 'nested-updates', 'copy'],
  },
  {
    id: 'kotlin-arrow-l1-057',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is a Lens in Arrow Optics?',
    options: [
      'A way to filter collections',
      'A debugging tool',
      'A camera component for image processing',
      'A focus on a single field of a product type (data class)',
    ],
    correctAnswer: 3,
    explanation: 'A Lens focuses on a single field within a data class (product type). It provides get to read the field and set/modify to create a copy with the field changed. Lenses can be composed to access deeply nested fields.',
    codeSnippet: '@optics data class Person(val name: String, val age: Int) {\n  companion object\n}\nval nameLens = Person.name\nnameLens.get(person)         // reads name\nnameLens.set(person, "Alice") // returns new Person',
    tags: ['optics', 'lens', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-058',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between map and flatMap on a List?',
    options: [
      'map is faster than flatMap',
      'map transforms each element; flatMap transforms and then flattens the results',
      'flatMap only works with nullable types',
      'There is no difference',
    ],
    correctAnswer: 1,
    explanation: 'map applies a function to each element: List<A>.map(f: (A) -> B) -> List<B>. flatMap applies a function that returns a list for each element and then flattens: List<A>.flatMap(f: (A) -> List<B>) -> List<B>.',
    codeSnippet: 'listOf(1, 2, 3).map { listOf(it, it) }\n// [[1,1], [2,2], [3,3]]\n\nlistOf(1, 2, 3).flatMap { listOf(it, it) }\n// [1, 1, 2, 2, 3, 3]',
    tags: ['functional-patterns', 'map', 'flatMap'],
  },
  {
    id: 'kotlin-arrow-l1-059',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What does the fold function do on a list?',
    options: [
      'Filters the list',
      'Splits the list in half',
      'Sorts the list',
      'Reduces the list to a single value by applying an operation cumulatively',
    ],
    correctAnswer: 3,
    explanation: 'fold takes an initial accumulator value and a combining function. It processes each element, updating the accumulator, and returns the final accumulated result. It reduces a collection to a single value.',
    codeSnippet: 'val sum = listOf(1, 2, 3, 4).fold(0) { acc, x -> acc + x }\n// sum == 10',
    tags: ['functional-patterns', 'fold', 'reduction'],
  },
  {
    id: 'kotlin-arrow-l1-060',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is Arrow Fx Coroutines?',
    options: [
      'A reactive streams implementation',
      'Functional extensions for Kotlin coroutines providing typed error handling and resource safety',
      'A threading library',
      'A replacement for Kotlin coroutines',
    ],
    correctAnswer: 1,
    explanation: 'Arrow Fx Coroutines builds on top of Kotlin coroutines to provide functional programming constructs like typed errors, resource safety, parallel operations, and retry policies in a coroutine-friendly way.',
    tags: ['arrow-fx', 'coroutines', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-061',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a sealed interface in Kotlin and how does it relate to FP error handling?',
    options: [
      'An interface that is automatically thread-safe',
      'An interface whose implementations are restricted and known at compile time',
      'An encrypted interface',
      'An interface that cannot have implementations',
    ],
    correctAnswer: 1,
    explanation: 'Sealed interfaces restrict their implementations to the same package/module. Like sealed classes, they enable exhaustive when expressions, making them ideal for defining error hierarchies in functional error handling.',
    codeSnippet: 'sealed interface DomainError {\n  data class NotFound(val id: String) : DomainError\n  data class InvalidInput(val field: String) : DomainError\n}',
    tags: ['sealed-interface', 'error-handling', 'exhaustive'],
  },
  {
    id: 'kotlin-arrow-l1-062',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does the following code return?\n\nval result = either<String, Int> {\n  val a = Right(1).bind()\n  val b = Right(2).bind()\n  a + b\n}',
    options: [
      'Right(3)',
      '3',
      'Either<String, Either<String, Int>>',
      'Left("3")',
    ],
    correctAnswer: 0,
    explanation: 'Inside the either { } block, bind() extracts values from Right. Both bind() calls succeed (1 and 2), so the block returns 1 + 2 = 3, which is wrapped in Right(3) by the either builder.',
    codeSnippet: 'val result = either<String, Int> {\n  val a = Right(1).bind()  // 1\n  val b = Right(2).bind()  // 2\n  a + b                     // 3\n}\n// result == Right(3)',
    tags: ['either', 'bind', 'raise-dsl'],
  },
  {
    id: 'kotlin-arrow-l1-063',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does the following code return?\n\nval result = either<String, Int> {\n  val a = Right(1).bind()\n  val b = Left("error").bind()\n  a + b\n}',
    options: [
      'Right(1)',
      'Left("error")',
      'Throws an exception',
      'Right(0)',
    ],
    correctAnswer: 1,
    explanation: 'The first bind() succeeds with 1. The second bind() encounters Left("error") and short-circuits the entire either block, returning Left("error"). The a + b line is never reached.',
    codeSnippet: 'val result = either<String, Int> {\n  val a = Right(1).bind()        // 1\n  val b = Left("error").bind()   // short-circuits!\n  a + b                           // never reached\n}\n// result == Left("error")',
    tags: ['either', 'bind', 'short-circuit'],
  },
  {
    id: 'kotlin-arrow-l1-064',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What does the term "type constructor" mean?',
    options: [
      'A constructor method in a class',
      'A type that takes a type parameter to produce a concrete type (e.g., List<_>, Either<_, _>)',
      'A factory pattern implementation',
      'The companion object of a class',
    ],
    correctAnswer: 1,
    explanation: 'A type constructor is a generic type that needs type parameters to become a concrete type. List is a type constructor; List<Int> is a concrete type. Either needs two type parameters: Either<String, Int>.',
    tags: ['functional-patterns', 'type-constructor', 'theory'],
  },
  {
    id: 'kotlin-arrow-l1-065',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does either.orNull() return?',
    options: [
      'The Right value or null if it is a Left',
      'The Either itself or null',
      'Always null',
      'The Left value or null',
    ],
    correctAnswer: 0,
    explanation: 'orNull() extracts the Right value from an Either, returning null if the Either is a Left. This bridges Either to Kotlin\'s nullable type system.',
    codeSnippet: 'val right: Either<String, Int> = 42.right()\nright.orNull() // 42\n\nval left: Either<String, Int> = "err".left()\nleft.orNull()  // null',
    tags: ['either', 'orNull', 'nullable'],
  },
  {
    id: 'kotlin-arrow-l1-066',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Result Type',
    question: 'What does result.onSuccess { } do?',
    options: [
      'Throws if the Result is a failure',
      'Transforms the success value',
      'Returns a new Result with the block\'s return value',
      'Executes the block if successful and returns the original Result',
    ],
    correctAnswer: 3,
    explanation: 'onSuccess executes the provided action if the Result is successful, then returns the original Result unchanged. It is used for side effects like logging. To transform the value, use map instead.',
    codeSnippet: 'Result.success(42)\n  .onSuccess { println("Got $it") } // prints "Got 42"\n  .onFailure { println("Failed: $it") }',
    tags: ['result', 'onSuccess', 'side-effects'],
  },
  {
    id: 'kotlin-arrow-l1-067',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between eager and lazy evaluation?',
    options: [
      'Eager evaluates immediately, lazy defers evaluation until the value is needed',
      'Lazy uses more memory',
      'There is no practical difference',
      'Eager is faster, lazy is slower',
    ],
    correctAnswer: 0,
    explanation: 'Eager evaluation computes values immediately when defined. Lazy evaluation defers computation until the value is actually accessed. Kotlin\'s lazy delegate and sequences use lazy evaluation.',
    codeSnippet: 'val eager = listOf(1, 2, 3).map { it * 2 }   // computed now\nval lazy by lazy { expensiveComputation() }    // computed on first access',
    tags: ['functional-patterns', 'lazy', 'evaluation'],
  },
  {
    id: 'kotlin-arrow-l1-068',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is a data class in Kotlin and why is it important for FP?',
    options: [
      'A class that can only hold primitive data',
      'A class with no methods',
      'A class for database access',
      'An immutable value type with auto-generated equals, hashCode, copy, and toString',
    ],
    correctAnswer: 3,
    explanation: 'Data classes in Kotlin auto-generate equals, hashCode, toString, copy, and componentN functions. The copy method is essential for FP as it enables creating modified copies of immutable data.',
    codeSnippet: 'data class User(val name: String, val age: Int)\nval user = User("Alice", 30)\nval older = user.copy(age = 31) // new User("Alice", 31)',
    tags: ['functional-patterns', 'data-class', 'immutability'],
  },
  {
    id: 'kotlin-arrow-l1-069',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is destructuring in Kotlin?',
    options: [
      'Removing class hierarchies',
      'Breaking down an object into its component variables',
      'A garbage collection technique',
      'Deleting an object',
    ],
    correctAnswer: 1,
    explanation: 'Destructuring allows you to unpack an object into multiple variables using componentN functions. Data classes automatically support this. It is useful with Pair, Triple, and custom data classes.',
    codeSnippet: 'data class Point(val x: Int, val y: Int)\nval (x, y) = Point(3, 4)\nprintln("$x, $y") // 3, 4',
    tags: ['functional-patterns', 'destructuring', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-070',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What is the type of the following expression?\n\nval x = 42.right()',
    options: [
      'Right<Int>',
      'Either<Any, Int>',
      'Either<Nothing, Int>',
      'Int',
    ],
    correctAnswer: 2,
    explanation: 'The right() extension function returns Either<Nothing, Int>. The Left type parameter is Nothing because no Left value is specified. Nothing is the bottom type in Kotlin and is a subtype of all types.',
    codeSnippet: 'val x = 42.right() // Either<Nothing, Int>\nval y: Either<String, Int> = x // widening is safe',
    tags: ['either', 'type-inference', 'nothing'],
  },
  {
    id: 'kotlin-arrow-l1-071',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the ensure function in the Raise DSL?',
    options: [
      'A guard that raises an error if a condition is false',
      'A null check',
      'A thread-safety mechanism',
      'A unit testing assertion',
    ],
    correctAnswer: 0,
    explanation: 'ensure is a guard function in the Raise DSL. It checks a boolean condition and calls raise with the provided error if the condition is false. It is similar to require() but for typed errors.',
    codeSnippet: 'fun Raise<String>.validateAge(age: Int): Int {\n  ensure(age >= 0) { "Age cannot be negative" }\n  ensure(age < 150) { "Age is unrealistic" }\n  return age\n}',
    tags: ['raise-dsl', 'ensure', 'validation'],
  },
  {
    id: 'kotlin-arrow-l1-072',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What does ensureNotNull do in the Raise DSL?',
    options: [
      'Converts null to an empty string',
      'Logs a warning if null',
      'Returns a non-null value or raises a typed error if the value is null',
      'Throws a NullPointerException',
    ],
    correctAnswer: 2,
    explanation: 'ensureNotNull checks if a value is null. If non-null, it returns the value with a non-null smart cast. If null, it raises the specified error. This bridges nullable types into the Raise DSL.',
    codeSnippet: 'fun Raise<String>.findUser(id: String): User {\n  val user: User? = repository.find(id)\n  return ensureNotNull(user) { "User $id not found" }\n}',
    tags: ['raise-dsl', 'ensureNotNull', 'null-safety'],
  },
  {
    id: 'kotlin-arrow-l1-073',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the pipe operator concept in FP?',
    options: [
      'A bitwise operation',
      'Passing the result of one function as input to the next',
      'A concurrency primitive',
      'A way to connect network streams',
    ],
    correctAnswer: 1,
    explanation: 'Piping passes the result of one function as the input to the next, creating a readable left-to-right data flow. While Kotlin does not have a built-in pipe operator, the let function and extension functions serve a similar purpose.',
    codeSnippet: '// Piping with let\n5.let { it * 2 }.let { it + 1 }.let { println(it) } // 11\n\n// Extension function chain\n"hello".uppercase().reversed() // "OLLEH"',
    tags: ['functional-patterns', 'pipe', 'composition'],
  },
  {
    id: 'kotlin-arrow-l1-074',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the sequence type in Kotlin and how does it relate to FP?',
    options: [
      'A lazy collection that evaluates elements on demand',
      'A coroutine builder',
      'An ordered set',
      'A database sequence generator',
    ],
    correctAnswer: 0,
    explanation: 'Sequence<T> is Kotlin\'s lazy collection type. Operations like map and filter are evaluated lazily, only when terminal operations (toList, first, etc.) are called. This avoids creating intermediate collections.',
    codeSnippet: 'val result = sequenceOf(1, 2, 3, 4, 5)\n  .map { it * 2 }     // lazy, not computed yet\n  .filter { it > 4 }  // lazy\n  .toList()            // triggers evaluation: [6, 8, 10]',
    tags: ['functional-patterns', 'sequence', 'lazy'],
  },
  {
    id: 'kotlin-arrow-l1-075',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you convert an Either to a Kotlin Result?',
    options: [
      'Result(either)',
      'either.toResult()',
      'either.fold({ Result.failure(it) }, { Result.success(it) })',
      'There is no way to convert them',
    ],
    correctAnswer: 2,
    explanation: 'There is no built-in direct conversion, but you can use fold to handle both cases. For Left you create Result.failure (note: Left must be a Throwable) and for Right you create Result.success.',
    codeSnippet: 'val either: Either<Throwable, Int> = 42.right()\nval result = either.fold(\n  { Result.failure(it) },\n  { Result.success(it) }\n)',
    tags: ['either', 'result', 'conversion'],
  },
  {
    id: 'kotlin-arrow-l1-076',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is tail recursion in Kotlin?',
    options: [
      'Recursion that uses a tail pointer',
      'A recursive function where the recursive call is the last operation, optimized with tailrec',
      'A loop disguised as recursion',
      'Recursion that starts from the end of a list',
    ],
    correctAnswer: 1,
    explanation: 'Tail recursion occurs when the recursive call is the very last operation in the function. Kotlin\'s tailrec modifier tells the compiler to optimize it into a loop, preventing stack overflow.',
    codeSnippet: 'tailrec fun factorial(n: Int, acc: Long = 1): Long =\n  if (n <= 1) acc\n  else factorial(n - 1, n * acc)',
    tags: ['functional-patterns', 'tail-recursion', 'tailrec'],
  },
  {
    id: 'kotlin-arrow-l1-077',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Sealed Class Error Handling',
    question: 'What is the advantage of using sealed classes for errors over exceptions?',
    options: [
      'Sealed classes are faster',
      'Errors become part of the type signature, making them explicit and compiler-checked',
      'Sealed classes use less memory',
      'Exceptions cannot carry data',
    ],
    correctAnswer: 1,
    explanation: 'With sealed class errors and Either, errors are encoded in the return type (Either<MyError, Result>). The compiler ensures all error cases are handled. Exceptions are invisible in the type system and can be missed.',
    codeSnippet: '// Error is in the type signature - explicit!\nfun findUser(id: String): Either<UserError, User>\n\n// Error is hidden - caller might not handle it\n@Throws(UserException::class)\nfun findUser(id: String): User',
    tags: ['sealed-class', 'type-safety', 'error-handling'],
  },
  {
    id: 'kotlin-arrow-l1-078',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'What is Arrow\'s Resource type used for?',
    options: [
      'Managing Android resources (strings, drawables)',
      'Loading configuration files',
      'Safely acquiring and releasing resources (files, connections, etc.)',
      'Managing memory allocation',
    ],
    correctAnswer: 2,
    explanation: 'Arrow\'s Resource ensures that acquired resources (file handles, database connections, etc.) are properly released even if an exception occurs. It is similar to try-with-resources but composable and functional.',
    codeSnippet: 'val resource = Resource(\n  acquire = { openFile("data.txt") },\n  release = { file, _ -> file.close() }\n)',
    tags: ['resource', 'safety', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-079',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the identity function?',
    options: [
      'A function with no parameters',
      'A function that returns its own name',
      'A function that returns its argument unchanged',
      'A function that authenticates users',
    ],
    correctAnswer: 2,
    explanation: 'The identity function simply returns its input unchanged: identity(x) = x. It is a fundamental building block in FP, used as a default transformation, a no-op in function composition, and in fold operations.',
    codeSnippet: 'val identity: (Int) -> Int = { it }\nidentity(42) // 42\n\n// Useful as default: transform.getOrElse(::identity)',
    tags: ['functional-patterns', 'identity', 'basics'],
  },
  {
    id: 'kotlin-arrow-l1-080',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does Either.Right(5) == Either.Right(5) evaluate to?',
    options: [
      'Depends on the JVM version',
      'false (different instances)',
      'true (structural equality)',
      'Compilation error',
    ],
    correctAnswer: 2,
    explanation: 'Either.Right and Either.Left are data classes in Arrow, so they use structural equality (comparing the wrapped values). Two Right(5) instances are equal because their contained values are equal.',
    codeSnippet: 'Either.Right(5) == Either.Right(5)  // true\nEither.Left("a") == Either.Left("a") // true\nEither.Right(5) == Either.Left(5)   // false',
    tags: ['either', 'equality', 'data-class'],
  },
  {
    id: 'kotlin-arrow-l1-081',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is memoization?',
    options: [
      'Caching the results of function calls to avoid redundant computation',
      'Converting functions to strings',
      'Writing documentation for functions',
      'A garbage collection strategy',
    ],
    correctAnswer: 0,
    explanation: 'Memoization caches the results of expensive function calls. When the function is called again with the same arguments, the cached result is returned instead of recomputing. This works best with pure functions.',
    codeSnippet: 'val memoizedFib = ::fib.memoize()\nmemoizedFib(10) // computed\nmemoizedFib(10) // returned from cache',
    tags: ['functional-patterns', 'memoization', 'optimization'],
  },
  {
    id: 'kotlin-arrow-l1-082',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Nel',
    question: 'What does map do on a NonEmptyList?',
    options: [
      'Returns a NonEmptyList with each element transformed',
      'Returns a regular List',
      'Throws if the transformation returns null',
      'Returns an Either',
    ],
    correctAnswer: 0,
    explanation: 'map on NonEmptyList transforms each element and returns a new NonEmptyList. Since the input is guaranteed non-empty and map preserves the structure, the output is also guaranteed non-empty.',
    codeSnippet: 'val nel = nonEmptyListOf(1, 2, 3)\nval doubled = nel.map { it * 2 }\n// NonEmptyList(2, 4, 6)',
    tags: ['nel', 'map', 'transformation'],
  },
  {
    id: 'kotlin-arrow-l1-083',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between List and Sequence in Kotlin regarding intermediate operations?',
    options: [
      'List creates intermediate collections; Sequence processes lazily without intermediates',
      'Sequence creates intermediate collections; List does not',
      'No difference',
      'Both always process lazily',
    ],
    correctAnswer: 0,
    explanation: 'List operations like map and filter are eager: each creates a new intermediate list. Sequence operations are lazy: they are chained and only evaluated when a terminal operation (toList, first, etc.) is called, avoiding intermediate allocations.',
    codeSnippet: '// Creates 2 intermediate lists:\nlistOf(1,2,3).map { it*2 }.filter { it>2 }\n\n// No intermediate collections:\nsequenceOf(1,2,3).map { it*2 }.filter { it>2 }.toList()',
    tags: ['functional-patterns', 'sequence', 'list', 'lazy'],
  },
  {
    id: 'kotlin-arrow-l1-084',
    language: 'kotlin',
    level: 'level1',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'What does the recover function do on an Either in Arrow?',
    options: [
      'Converts Either to a nullable type',
      'Retries a failed computation',
      'Logs the error and rethrows',
      'Transforms a Left into a Right, providing error recovery',
    ],
    correctAnswer: 3,
    explanation: 'recover (or handleErrorWith) transforms a Left value into a Right, allowing you to provide a fallback or alternative value when an error occurs. If the Either is already Right, it is returned unchanged.',
    codeSnippet: 'val result: Either<String, Int> = "error".left()\nval recovered = result.getOrElse { 0 }\n// recovered == 0',
    tags: ['either', 'recovery', 'error-handling'],
  },
];

export default questions;
