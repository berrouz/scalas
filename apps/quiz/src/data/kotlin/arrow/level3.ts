import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-arrow-l3-001',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does Arrow implement the Raise DSL under the hood?',
    options: [
      'Using bytecode manipulation',
      'Using coroutine suspension',
      'Using reflection',
      'Using Kotlin\'s context receivers and a fold-based CPS transformation',
    ],
    correctAnswer: 3,
    explanation: 'Arrow\'s Raise DSL uses context receivers (or scope functions) and internally implements raise() via a non-local return or a special exception mechanism. The either { } builder uses fold to catch the raised value and wrap it in Left.',
    tags: ['raise-dsl', 'implementation', 'context-receivers'],
  },
  {
    id: 'kotlin-arrow-l3-002',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the relationship between Raise and Effect in Arrow 2.x?',
    options: [
      'Effect was replaced by Raise DSL; Raise is the core abstraction for typed errors',
      'They are unrelated',
      'Effect extends Raise',
      'Raise is deprecated in favor of Effect',
    ],
    correctAnswer: 0,
    explanation: 'In Arrow 2.x, the Effect type was replaced by the Raise DSL. Raise is a simpler, more composable abstraction that uses context receivers instead of wrapping computations in an Effect type. This reduces nesting and improves ergonomics.',
    tags: ['raise-dsl', 'effect', 'arrow-2'],
  },
  {
    id: 'kotlin-arrow-l3-003',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you implement typed error handling across suspend boundaries in Arrow?',
    options: [
      'Use thread-local storage for errors',
      'Raise context receivers work naturally with suspend functions',
      'Use a global error handler',
      'Use try-catch at each boundary',
    ],
    correctAnswer: 1,
    explanation: 'Raise context receivers compose naturally with suspend functions. A function can be both suspend and have a Raise receiver. The either { } builder works in suspend contexts, and bind() integrates with coroutine cancellation.',
    codeSnippet: 'suspend fun Raise<ApiError>.fetchAndProcess(id: String): ProcessedData {\n  val raw = withError(ApiError::Network) {\n    httpClient.get(id) // suspend + Raise<NetworkError>\n  }\n  val validated = withError(ApiError::Validation) {\n    validate(raw) // Raise<ValidationError>\n  }\n  return process(validated)\n}',
    tags: ['typed-errors', 'suspend', 'coroutines'],
  },
  {
    id: 'kotlin-arrow-l3-004',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'What is Software Transactional Memory (STM) in Arrow?',
    options: [
      'A serialization mechanism',
      'A memory profiling tool',
      'A concurrency primitive that provides composable atomic transactions on shared memory',
      'A database transaction framework',
    ],
    correctAnswer: 2,
    explanation: 'Arrow\'s STM provides composable atomic transactions for shared mutable state. Multiple TVar reads/writes within an atomically block appear as a single atomic operation. If a conflict is detected, the transaction is retried.',
    codeSnippet: 'val balance = TVar.new(100)\n\natomically {\n  val current = balance.read()\n  if (current >= 50) {\n    balance.write(current - 50)\n  } else {\n    retry() // wait until condition is met\n  }\n}',
    tags: ['stm', 'concurrency', 'transactions'],
  },
  {
    id: 'kotlin-arrow-l3-005',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'What is a TVar in Arrow\'s STM?',
    options: [
      'A temporary value reference',
      'A thread-local variable',
      'A type variable in the type system',
      'A transactional variable that can only be read/written inside an STM transaction',
    ],
    correctAnswer: 3,
    explanation: 'TVar (Transactional Variable) is a mutable reference that can only be safely read and modified inside an STM transaction (atomically block). Reads and writes are tracked for conflict detection and automatic retry.',
    codeSnippet: 'val counter = TVar.new(0)\n\n// Must be inside atomically to read/write\natomically {\n  val current = counter.read()\n  counter.write(current + 1)\n}',
    tags: ['stm', 'tvar', 'transactional'],
  },
  {
    id: 'kotlin-arrow-l3-006',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'What does retry() do inside an STM transaction?',
    options: [
      'Blocks the transaction until one of the read TVars changes, then retries',
      'Throws a RetryException',
      'Retries the transaction immediately',
      'Returns a default value',
    ],
    correctAnswer: 0,
    explanation: 'retry() inside an STM transaction blocks the current transaction and waits for any TVar that was read in this transaction to change. Once a change is detected, the transaction is automatically retried from the beginning.',
    codeSnippet: 'val queue = TVar.new(emptyList<String>())\n\n// Blocks until queue is non-empty\natomically {\n  val items = queue.read()\n  if (items.isEmpty()) retry()\n  val head = items.first()\n  queue.write(items.drop(1))\n  head\n}',
    tags: ['stm', 'retry', 'blocking'],
  },
  {
    id: 'kotlin-arrow-l3-007',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Atomic',
    question: 'What is Arrow\'s Atomic type and how does it differ from AtomicReference?',
    options: [
      'Atomic provides a functional API with update, modify, and lens operations on top of atomic references',
      'They are identical',
      'Atomic is slower but safer',
      'Atomic does not support concurrent access',
    ],
    correctAnswer: 0,
    explanation: 'Arrow\'s Atomic wraps AtomicReference with a functional API. It provides update (CAS loop), modify (returns old and new), and integrates with optics for atomic updates of specific fields in immutable data structures.',
    codeSnippet: 'val state = Atomic(AppState(counter = 0, users = emptyList()))\n\n// Atomically update using a lens\nstate.update { it.copy(counter = it.counter + 1) }\n\n// Atomically modify and get old+new values\nval (old, new) = state.modify { s -> s.copy(counter = s.counter + 1) }',
    tags: ['atomic', 'concurrency', 'functional-api'],
  },
  {
    id: 'kotlin-arrow-l3-008',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement a custom optic for a type that is not a data class?',
    options: [
      'It is impossible without data classes',
      'Convert to a data class first',
      'Manually create a Lens by providing get and set functions',
      'Use reflection',
    ],
    correctAnswer: 2,
    explanation: 'You can manually construct optics by providing the get and set (or modify) functions. This works for any type, including Java classes, third-party types, or computed properties.',
    codeSnippet: 'val stringToInt: Prism<String, Int> = Prism(\n  getOrModify = { s ->\n    s.toIntOrNull()?.right() ?: s.left()\n  },\n  reverseGet = { i -> i.toString() }\n)\n\nstringToInt.getOrNull("42")  // 42\nstringToInt.getOrNull("abc") // null',
    tags: ['optics', 'custom', 'manual-construction'],
  },
  {
    id: 'kotlin-arrow-l3-009',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is the relationship between Lens, Prism, Optional, and Traversal in the optics hierarchy?',
    options: [
      'Lens and Prism are specializations of Optional; Optional is a specialization of Traversal',
      'Prism extends Lens',
      'Traversal extends Lens',
      'They are completely independent',
    ],
    correctAnswer: 0,
    explanation: 'The optics hierarchy: Traversal (0..n targets) > Optional (0..1 target) > Lens (exactly 1 target in product type) and Prism (0..1 target in sum type). Lens and Prism are both Optional specializations.',
    tags: ['optics', 'hierarchy', 'theory'],
  },
  {
    id: 'kotlin-arrow-l3-010',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you compose a Lens with a Prism?',
    options: [
      'The result is an Optional, since the target may not exist',
      'The result is always a Prism',
      'The result is always a Lens',
      'You cannot compose different optic types',
    ],
    correctAnswer: 0,
    explanation: 'Composing a Lens (always succeeds) with a Prism (may fail) produces an Optional (may or may not succeed). The composition follows the weakest optic: if either part may fail, the composition may fail.',
    codeSnippet: '@optics data class Config(val value: JsonValue) { companion object }\n@optics sealed interface JsonValue {\n  data class Str(val s: String) : JsonValue\n  data class Num(val n: Double) : JsonValue\n  companion object\n}\n\n// Lens compose Prism = Optional\nval configString: Optional<Config, String> =\n  Config.value compose JsonValue.str compose Str.s',
    tags: ['optics', 'composition', 'optional'],
  },
  {
    id: 'kotlin-arrow-l3-011',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How does Arrow\'s Resource differ from Kotlin\'s use extension function?',
    options: [
      'Resource is composable (flatMap, zip) and works with coroutine cancellation; use is not composable',
      'Resource does not support Closeable',
      'They are identical',
      'use is more type-safe',
    ],
    correctAnswer: 0,
    explanation: 'Kotlin\'s use works for simple single-resource scenarios. Arrow\'s Resource supports composition (bind multiple resources), works with coroutine cancellation, and handles ExitCase for conditional release logic.',
    codeSnippet: '// Kotlin use: simple, non-composable\nFile("a.txt").bufferedReader().use { reader ->\n  File("b.txt").bufferedWriter().use { writer -> // nested!\n    writer.write(reader.readText())\n  }\n}\n\n// Arrow Resource: composable\nresource {\n  val reader = install({ File("a.txt").bufferedReader() }) { r, _ -> r.close() }\n  val writer = install({ File("b.txt").bufferedWriter() }) { w, _ -> w.close() }\n  writer.write(reader.readText())\n}',
    tags: ['arrow-fx', 'resource', 'composition'],
  },
  {
    id: 'kotlin-arrow-l3-012',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is bracket in Arrow and how does it relate to Resource?',
    options: [
      'They are unrelated',
      'bracket is the primitive for acquire-use-release; Resource is a higher-level abstraction built on it',
      'bracket is a formatting tool',
      'Resource is deprecated in favor of bracket',
    ],
    correctAnswer: 1,
    explanation: 'bracket(acquire, use, release) is the fundamental pattern: acquire a resource, use it, and guarantee release. Resource is built on top of bracket, adding composability via flatMap and the resource { } DSL.',
    codeSnippet: '// Low-level bracket\nval result = bracket(\n  acquire = { openConnection() },\n  use = { conn -> query(conn, sql) },\n  release = { conn, exitCase -> conn.close() }\n)',
    tags: ['arrow-fx', 'bracket', 'resource'],
  },
  {
    id: 'kotlin-arrow-l3-013',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Saga',
    question: 'What is the Saga pattern in Arrow?',
    options: [
      'A testing framework',
      'A pattern for long-running transactions with compensating actions for rollback',
      'A storytelling DSL',
      'A logging pattern',
    ],
    correctAnswer: 1,
    explanation: 'The Saga pattern manages distributed transactions where each step has a compensating action. If any step fails, all previously completed steps are compensated (rolled back) in reverse order.',
    codeSnippet: 'saga {\n  val orderId = saga(\n    { createOrder(items) },\n    { id -> cancelOrder(id) } // compensate if later step fails\n  )\n  val paymentId = saga(\n    { processPayment(orderId, amount) },\n    { id -> refundPayment(id) } // compensate\n  )\n  saga(\n    { shipOrder(orderId) },\n    { trackingId -> cancelShipment(trackingId) }\n  )\n}',
    tags: ['saga', 'distributed-transaction', 'compensation'],
  },
  {
    id: 'kotlin-arrow-l3-014',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Saga',
    question: 'How does a Saga differ from a regular transaction?',
    options: [
      'Regular transactions are for distributed systems',
      'Sagas do not support rollback',
      'Sagas use compensating actions instead of ACID rollback, suitable for distributed systems',
      'Sagas are faster',
    ],
    correctAnswer: 2,
    explanation: 'Database transactions use ACID properties with immediate rollback. Sagas are for distributed systems where a single transaction is impossible. Each step has a compensating action that logically undoes it if a later step fails.',
    tags: ['saga', 'distributed-systems', 'compensation'],
  },
  {
    id: 'kotlin-arrow-l3-015',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Circuit Breaker',
    question: 'How do you configure Arrow\'s CircuitBreaker for a specific use case?',
    options: [
      'Using environment variables',
      'Using annotations',
      'Using XML configuration',
      'Programmatically with maxFailures, resetTimeout, exponentialBackoffFactor, and maxResetTimeout',
    ],
    correctAnswer: 3,
    explanation: 'Arrow\'s CircuitBreaker is configured programmatically. Key parameters: maxFailures (failures before opening), resetTimeout (time before trying half-open), exponentialBackoffFactor (multiplier for successive open periods), maxResetTimeout (cap on backoff).',
    codeSnippet: 'val circuitBreaker = CircuitBreaker(\n  maxFailures = 5,\n  resetTimeout = 10.seconds,\n  exponentialBackoffFactor = 2.0,\n  maxResetTimeout = 60.seconds\n)\n\nval result = circuitBreaker.protectOrThrow {\n  callExternalService()\n}',
    tags: ['circuit-breaker', 'configuration', 'resilience'],
  },
  {
    id: 'kotlin-arrow-l3-016',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Circuit Breaker',
    question: 'What exception does Arrow\'s CircuitBreaker throw when the circuit is open?',
    options: [
      'ExecutionRejectedException',
      'IllegalStateException',
      'CircuitBreaker.OpenException',
      'CircuitBreakerOpenException',
    ],
    correctAnswer: 2,
    explanation: 'When the circuit breaker is in the Open state, it immediately rejects calls by throwing CircuitBreaker.OpenException. You can catch this and provide a fallback response.',
    codeSnippet: 'try {\n  circuitBreaker.protectOrThrow { callService() }\n} catch (e: CircuitBreaker.OpenException) {\n  // Circuit is open, provide fallback\n  cachedResponse()\n}',
    tags: ['circuit-breaker', 'exception', 'open-state'],
  },
  {
    id: 'kotlin-arrow-l3-017',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a timeout with Arrow Fx?',
    options: [
      'Timeouts are not supported',
      'Using Thread.sleep()',
      'Using a Timer class',
      'Using raceN with a delay, or kotlinx.coroutines withTimeout',
    ],
    correctAnswer: 3,
    explanation: 'You can use raceN to race the actual computation against a delay(timeout). Alternatively, kotlinx.coroutines withTimeout works well. Arrow integrates naturally with both approaches.',
    codeSnippet: '// Using raceN for timeout\nval result = raceN(\n  { longRunningOperation() },\n  { delay(5.seconds); null }\n).fold(\n  { it },           // operation completed\n  { throw TimeoutException() } // timeout won\n)\n\n// Or simply\nwithTimeout(5.seconds) { longRunningOperation() }',
    tags: ['arrow-fx', 'timeout', 'raceN'],
  },
  {
    id: 'kotlin-arrow-l3-018',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is the guarantee function in Arrow Fx?',
    options: [
      'A compile-time check',
      'A function that ensures a finalizer runs regardless of success, failure, or cancellation',
      'A testing assertion',
      'A performance guarantee',
    ],
    correctAnswer: 1,
    explanation: 'guarantee(fa, finalizer) ensures the finalizer runs after fa completes, regardless of outcome (success, error, or cancellation). It is the functional equivalent of try-finally with cancellation awareness.',
    codeSnippet: 'guarantee(\n  fa = { processData() },\n  finalizer = { exitCase ->\n    when (exitCase) {\n      is ExitCase.Completed -> log("Done")\n      is ExitCase.Cancelled -> log("Cancelled")\n      is ExitCase.Failure -> log("Failed: ${exitCase.failure}")\n    }\n  }\n)',
    tags: ['arrow-fx', 'guarantee', 'finalization'],
  },
  {
    id: 'kotlin-arrow-l3-019',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'How do you compose Schedule policies using jittered delay?',
    options: [
      'Schedule.jitter(1.seconds)',
      'Schedule.exponential<E>(base).jittered()',
      'Schedule.random()',
      'Jitter is not supported in Arrow Schedule',
    ],
    correctAnswer: 1,
    explanation: 'jittered() adds randomized jitter to a schedule\'s delays. This prevents thundering herd problems where multiple clients retry simultaneously after a failure. The jitter randomizes the delay within a range.',
    codeSnippet: 'val policy = Schedule.exponential<Throwable>(250.milliseconds)\n  .jittered() // adds random variation to delays\n  .and(Schedule.recurs(10))\n\npolicy.retry { callService() }',
    tags: ['schedule', 'jitter', 'thundering-herd'],
  },
  {
    id: 'kotlin-arrow-l3-020',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'How do you implement a Schedule that retries with different strategies based on the error type?',
    options: [
      'Use multiple try-catch blocks',
      'Create separate Schedule instances for each error type',
      'This is not possible with Schedule',
      'Use Schedule.doWhile with error type checking combined with different delay strategies',
    ],
    correctAnswer: 3,
    explanation: 'Schedule.doWhile lets you inspect the error and accumulated state to decide whether to continue. Combined with contramap or custom logic, you can vary behavior based on error type.',
    codeSnippet: 'val policy = Schedule.exponential<Throwable>(100.milliseconds)\n  .doWhile { throwable, _ ->\n    when (throwable) {\n      is IOException -> true    // retry network errors\n      is TimeoutException -> true // retry timeouts\n      else -> false              // do not retry others\n    }\n  }',
    tags: ['schedule', 'conditional-retry', 'error-type'],
  },
  {
    id: 'kotlin-arrow-l3-021',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the Tagless Final pattern in Kotlin?',
    options: [
      'Using bytecode weaving',
      'The pattern is not applicable to Kotlin',
      'Using interfaces with suspend functions and providing different implementations',
      'Using annotation processing',
    ],
    correctAnswer: 2,
    explanation: 'Tagless Final in Kotlin uses interfaces to define operations abstractly. Different implementations provide different effects (real, test, logging). Kotlin\'s suspend functions serve as the effect abstraction.',
    codeSnippet: 'interface UserRepository {\n  suspend fun findById(id: String): User?\n  suspend fun save(user: User): Unit\n}\n\n// Production implementation\nclass PostgresUserRepo(val db: Database) : UserRepository {\n  override suspend fun findById(id: String) = db.query(...)\n  override suspend fun save(user: User) = db.insert(...)\n}\n\n// Test implementation\nclass InMemoryUserRepo : UserRepository {\n  private val store = mutableMapOf<String, User>()\n  override suspend fun findById(id: String) = store[id]\n  override suspend fun save(user: User) { store[user.id] = user }\n}',
    tags: ['functional-patterns', 'tagless-final', 'interfaces'],
  },
  {
    id: 'kotlin-arrow-l3-022',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Free monad and why is it rarely used in Kotlin/Arrow?',
    options: [
      'It is the most commonly used pattern',
      'Free monad is only for Haskell',
      'Free monad separates program description from interpretation; Kotlin\'s interfaces and suspend achieve this more idiomatically',
      'Free monad is a performance optimization',
    ],
    correctAnswer: 2,
    explanation: 'The Free monad builds a data structure representing a program, which is then interpreted. In Kotlin, the same separation is achieved more idiomatically using interfaces, suspend functions, and dependency injection.',
    tags: ['functional-patterns', 'free-monad', 'idiomatic-kotlin'],
  },
  {
    id: 'kotlin-arrow-l3-023',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you implement error recovery with fallback strategies in the Raise DSL?',
    options: [
      'Using recover with multiple fallback levels',
      'Using global error handlers',
      'Error recovery is not supported in Raise',
      'Using nested try-catch',
    ],
    correctAnswer: 0,
    explanation: 'The recover function in the Raise DSL catches errors and provides alternatives. You can nest recover calls for multi-level fallback strategies, trying each option before raising a final error.',
    codeSnippet: 'fun Raise<FinalError>.fetchData(id: String): Data {\n  return recover({\n    fetchFromCache(id) // Raise<CacheError>\n  }) { cacheError ->\n    recover({\n      fetchFromDb(id)  // Raise<DbError>\n    }) { dbError ->\n      fetchFromRemote(id) // Raise<FinalError>\n    }\n  }\n}',
    tags: ['typed-errors', 'recover', 'fallback'],
  },
  {
    id: 'kotlin-arrow-l3-024',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How does Arrow handle the relationship between typed errors and exceptions?',
    options: [
      'Arrow provides catch and withError to bridge between exceptions and typed errors',
      'They are completely separate systems',
      'Typed errors replace exceptions entirely',
      'Exceptions are automatically converted to typed errors',
    ],
    correctAnswer: 0,
    explanation: 'Arrow bridges the two systems: catch converts exceptions to typed errors, Either.catch wraps throwables, and you can use withError to transform between different error types. Exceptions still exist for truly exceptional cases.',
    codeSnippet: 'fun Raise<AppError>.processFile(path: String): Data {\n  // Bridge exceptions to typed errors\n  val content = catch({\n    File(path).readText()\n  }) { e: IOException ->\n    raise(AppError.FileError(path, e.message ?: ""))\n  }\n  return parse(content)\n}',
    tags: ['typed-errors', 'exceptions', 'bridging'],
  },
  {
    id: 'kotlin-arrow-l3-025',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement parallel operations with typed error handling?',
    options: [
      'Combine parMap with either { } or use parZipOrAccumulate for error accumulation',
      'Typed errors and parallelism are incompatible',
      'parMap does not support typed errors',
      'Use async/await with Either',
    ],
    correctAnswer: 0,
    explanation: 'Arrow provides parZipOrAccumulate and parMapOrAccumulate that run operations in parallel and accumulate typed errors. You can also use parMap inside an either { } block where bind() short-circuits on the first error.',
    codeSnippet: 'val result: Either<NonEmptyList<Error>, List<User>> = either {\n  ids.parMapOrAccumulate { id ->\n    fetchUser(id).bind()\n  }\n}\n\n// Or short-circuit on first error:\nval result2: Either<Error, List<User>> = either {\n  ids.parMap { id -> fetchUser(id).bind() }\n}',
    tags: ['arrow-fx', 'parallel', 'typed-errors'],
  },
  {
    id: 'kotlin-arrow-l3-026',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is the difference between parZip and parZipOrAccumulate?',
    options: [
      'parZip does not support error handling',
      'parZipOrAccumulate is sequential',
      'They are identical',
      'parZip short-circuits on the first error; parZipOrAccumulate runs all and accumulates errors',
    ],
    correctAnswer: 3,
    explanation: 'parZip cancels remaining operations on the first failure (fail-fast). parZipOrAccumulate runs all operations to completion and accumulates all errors into a NonEmptyList, useful for parallel validation.',
    codeSnippet: '// Fail-fast: cancels fetchOrders if fetchUser fails\nparZip(\n  { fetchUser(id) },\n  { fetchOrders(id) }\n) { user, orders -> Screen(user, orders) }\n\n// Accumulate: runs both even if one fails\nparZipOrAccumulate(\n  { validateUser(input) },\n  { validateAddress(input) }\n) { user, address -> Registration(user, address) }',
    tags: ['arrow-fx', 'parZip', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l3-027',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use optics to modify elements in a Map?',
    options: [
      'Use index() only',
      'Convert the Map to a List first',
      'Optics do not work with Maps',
      'Use At optic which focuses on a value at a specific key',
    ],
    correctAnswer: 3,
    explanation: 'The At optic provides a Lens from a Map to an Optional value at a specific key. index() provides an Optional that focuses on a value at a key if it exists. Both support immutable Map modifications.',
    codeSnippet: 'val map = mapOf("a" to 1, "b" to 2)\n\n// At: focuses on the value (as nullable/Option)\nval atA = At.map<String, Int>().at("a")\n\n// Index: focuses on existing value only\nval indexA = Index.map<String, Int>().index("a")\nval updated = indexA.modify(map) { it + 10 }\n// {"a" to 11, "b" to 2}',
    tags: ['optics', 'map', 'at-index'],
  },
  {
    id: 'kotlin-arrow-l3-028',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use Prism for safe downcasting?',
    options: [
      'Use instanceOf check',
      'Prisms do not support downcasting',
      'Prism provides getOrNull which returns null if the value is not the target subtype',
      'Use as keyword',
    ],
    correctAnswer: 2,
    explanation: 'Prism is the optic for sum types. getOrNull attempts to match the value against the target case, returning null if it does not match. This is a type-safe, composable alternative to is checks and casts.',
    codeSnippet: '@optics sealed interface Event {\n  data class Click(val x: Int, val y: Int) : Event\n  data class Scroll(val amount: Int) : Event\n  companion object\n}\n\nval click: Click? = Event.click.getOrNull(event)\n// null if event is Scroll',
    tags: ['optics', 'prism', 'downcasting'],
  },
  {
    id: 'kotlin-arrow-l3-029',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What are the prism laws?',
    options: [
      'ReverseGet-GetOrModify and GetOrModify-ReverseGet roundtrip properties',
      'Naming conventions for prism fields',
      'Laws about refraction of light',
      'Performance benchmarks for prisms',
    ],
    correctAnswer: 0,
    explanation: 'Prism laws: (1) If getOrModify succeeds (returns Right), reverseGet on that value reconstructs the original. (2) reverseGet always produces a value that getOrModify succeeds on. These ensure lossless roundtripping.',
    codeSnippet: '// Law 1: successful get then reverseGet = identity\nval a: Event.Click = Event.Click(1, 2)\nEvent.click.reverseGet(Event.click.getOrNull(a)!!) == a // true\n\n// Law 2: reverseGet then getOrModify always succeeds\nval click = Event.Click(1, 2)\nEvent.click.getOrNull(click) != null // always true',
    tags: ['optics', 'prism', 'laws'],
  },
  {
    id: 'kotlin-arrow-l3-030',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Interpreter pattern in FP?',
    options: [
      'A debugging tool',
      'A language translation tool',
      'A pattern where a program is represented as a data structure that is then interpreted/executed',
      'A code generation technique',
    ],
    correctAnswer: 2,
    explanation: 'The Interpreter pattern separates program description from execution. Operations are encoded as data (AST), then an interpreter traverses the data to produce effects. This enables multiple interpretations of the same program.',
    codeSnippet: '// Program description as data\nsealed interface DbOp<A> {\n  data class Query(val sql: String) : DbOp<List<Row>>\n  data class Insert(val table: String, val data: Row) : DbOp<Unit>\n}\n\n// Interpreter for production\nfun <A> interpret(op: DbOp<A>): A = when (op) {\n  is DbOp.Query -> realDb.execute(op.sql)\n  is DbOp.Insert -> realDb.insert(op.table, op.data)\n}',
    tags: ['functional-patterns', 'interpreter', 'separation-of-concerns'],
  },
  {
    id: 'kotlin-arrow-l3-031',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement error accumulation for a dynamic collection of validations?',
    options: [
      'Dynamic accumulation is not supported',
      'Use fold with Either',
      'Use mapOrAccumulate which applies a Raise block to each element and accumulates errors',
      'Use a for loop with mutable error list',
    ],
    correctAnswer: 2,
    explanation: 'mapOrAccumulate inside an either { } block applies a validation to each element of a collection. Instead of short-circuiting, it runs all validations and accumulates errors into NonEmptyList.',
    codeSnippet: 'fun validateUsers(inputs: List<UserInput>): Either<NonEmptyList<ValidationError>, List<User>> =\n  either {\n    inputs.mapOrAccumulate { input ->\n      ensure(input.name.isNotBlank()) { ValidationError.BlankName }\n      ensure(input.age >= 0) { ValidationError.NegativeAge }\n      User(input.name, input.age)\n    }\n  }',
    tags: ['raise-dsl', 'mapOrAccumulate', 'dynamic-validation'],
  },
  {
    id: 'kotlin-arrow-l3-032',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between covariance and contravariance in Kotlin\'s type system, and how does it apply to Arrow?',
    options: [
      'Covariance (out) allows subtypes in output positions; contravariance (in) allows supertypes in input positions',
      'Covariance makes types nullable',
      'Contravariance only applies to Java interop',
      'They are the same thing',
    ],
    correctAnswer: 0,
    explanation: 'Either is covariant in both parameters: Either<out A, out B>. This means Either<String, Int> is a subtype of Either<Any, Any>. Covariance allows using more specific types where general ones are expected.',
    codeSnippet: 'val specific: Either<IOException, String> = "hello".right()\n\n// Covariance allows widening\nval general: Either<Throwable, CharSequence> = specific\n\n// Raise uses contravariance for error type\n// Raise<Any> can handle Raise<String> contexts',
    tags: ['functional-patterns', 'variance', 'type-system'],
  },
  {
    id: 'kotlin-arrow-l3-033',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does Raise<Nothing> work and when is it useful?',
    options: [
      'It raises nothing, so it is useless',
      'Raise<Nothing> means no error can be raised; it represents infallible computations',
      'Nothing is the default error type',
      'It always throws an exception',
    ],
    correctAnswer: 1,
    explanation: 'Since Nothing has no instances, raise(nothing) can never be called. Raise<Nothing> represents a computation that cannot fail. Functions that work for Raise<E> for all E also work for Raise<Nothing>.',
    codeSnippet: '// This function cannot fail - it works in any Raise context\nfun <E> Raise<E>.pureComputation(x: Int): Int = x * 2\n\n// Raise<Nothing> means: no errors possible\nfun Raise<Nothing>.safeDivide(a: Int, b: Int): Double {\n  // Cannot call raise() here - Nothing has no instances\n  return a.toDouble() / b // must handle all cases\n}',
    tags: ['raise-dsl', 'nothing', 'infallible'],
  },
  {
    id: 'kotlin-arrow-l3-034',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a producer-consumer pattern with Arrow Fx?',
    options: [
      'Using Thread and synchronized blocks',
      'Arrow does not support this pattern',
      'Using BlockingQueue',
      'Using Channel from kotlinx.coroutines with Arrow\'s resource management',
    ],
    correctAnswer: 3,
    explanation: 'Arrow Fx integrates with Kotlin coroutines channels. You can use Channel for producer-consumer communication, wrapped in Arrow\'s resource management to ensure channels are closed properly.',
    codeSnippet: 'resource {\n  val channel = install(\n    { Channel<Data>(capacity = 100) }\n  ) { ch, _ -> ch.close() }\n\n  parZip(\n    { // Producer\n      for (item in dataSource) channel.send(process(item))\n    },\n    { // Consumer\n      for (item in channel) save(item)\n    }\n  ) { _, _ -> }\n}',
    tags: ['arrow-fx', 'producer-consumer', 'channel'],
  },
  {
    id: 'kotlin-arrow-l3-035',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'How do you implement a fibonacci backoff schedule?',
    options: [
      'Fibonacci backoff is not available in Arrow',
      'Schedule.fibonacci()',
      'Manually compute fibonacci numbers',
      'Schedule.fibonacci<E>(base) which produces delays following the fibonacci sequence',
    ],
    correctAnswer: 3,
    explanation: 'Schedule.fibonacci produces delays following the Fibonacci sequence: base, base, 2*base, 3*base, 5*base, 8*base, etc. This provides a more gradual backoff than exponential for some use cases.',
    codeSnippet: 'val policy = Schedule.fibonacci<Throwable>(100.milliseconds)\n  .and(Schedule.recurs(8))\n// Delays: 100ms, 100ms, 200ms, 300ms, 500ms, 800ms, 1300ms, 2100ms',
    tags: ['schedule', 'fibonacci', 'backoff'],
  },
  {
    id: 'kotlin-arrow-l3-036',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'How do you collect all intermediate results during a retry with Schedule?',
    options: [
      'Intermediate results cannot be collected',
      'Log each attempt manually',
      'Use a mutable list outside the retry',
      'Use Schedule.collect() which accumulates all outputs',
    ],
    correctAnswer: 3,
    explanation: 'Schedule.collect() (or fold) accumulates all outputs during retries. This is useful for gathering diagnostics about each attempt, such as error messages or response codes.',
    codeSnippet: 'val policy = Schedule.recurs<Throwable>(3)\n  .zipRight(Schedule.identity<Throwable>().collect())\n\nval (result, attempts) = policy.retry {\n  callService() // each failure\'s throwable is collected\n}\n// attempts contains all intermediate Throwables',
    tags: ['schedule', 'collect', 'diagnostics'],
  },
  {
    id: 'kotlin-arrow-l3-037',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between a comonad and a monad?',
    options: [
      'Comonad is faster',
      'Monad wraps values (pure) and chains (flatMap); comonad extracts values (extract) and extends context (coflatMap)',
      'They are identical',
      'Comonad is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Monad: pure wraps a value in context, flatMap chains computations. Comonad is the dual: extract pulls a value out of context, coflatMap extends the context around each element. Examples: NonEmptyList, streams.',
    codeSnippet: '// Monad:\n// pure: A -> M<A>\n// flatMap: M<A> -> (A -> M<B>) -> M<B>\n\n// Comonad (dual):\n// extract: W<A> -> A\n// coflatMap: W<A> -> (W<A> -> B) -> W<B>\n\n// NonEmptyList is a comonad:\nval nel = nonEmptyListOf(1, 2, 3)\nnel.extract() // 1 (head)',
    tags: ['functional-patterns', 'comonad', 'theory'],
  },
  {
    id: 'kotlin-arrow-l3-038',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the Strategy pattern functionally in Kotlin?',
    options: [
      'Using the Visitor pattern',
      'Using higher-order functions or function type parameters',
      'Using reflection',
      'Using abstract classes and inheritance',
    ],
    correctAnswer: 1,
    explanation: 'In FP, the Strategy pattern is simply passing a function. Instead of creating strategy interfaces and implementations, you pass lambdas. This is more concise and flexible than the OOP approach.',
    codeSnippet: '// FP Strategy: just pass a function\nfun <A> List<A>.sortWith(comparator: (A, A) -> Int): List<A> =\n  sortedWith(Comparator(comparator))\n\nval byName: (User, User) -> Int = { a, b -> a.name.compareTo(b.name) }\nval byAge: (User, User) -> Int = { a, b -> a.age.compareTo(b.age) }\n\nusers.sortWith(byName)\nusers.sortWith(byAge)',
    tags: ['functional-patterns', 'strategy', 'higher-order-functions'],
  },
  {
    id: 'kotlin-arrow-l3-039',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you implement retrying an Either-returning function?',
    options: [
      'Using a while loop',
      'Either does not support retrying',
      'Using try-catch',
      'Using Schedule.retry with the function, or recursive flatMap with a counter',
    ],
    correctAnswer: 3,
    explanation: 'Arrow\'s Schedule integrates with Either-returning functions. You can retry operations that return Either, with the schedule deciding whether to retry based on the Left value.',
    codeSnippet: 'suspend fun fetchWithRetry(id: String): Either<ApiError, Data> {\n  val schedule = Schedule.recurs<ApiError>(3)\n    .and(Schedule.exponential(100.milliseconds))\n    .doWhile { error, _ -> error is ApiError.Transient }\n\n  return schedule.retryEither { fetchData(id) }\n}',
    tags: ['either', 'retry', 'schedule'],
  },
  {
    id: 'kotlin-arrow-l3-040',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you use context receivers for combining multiple Raise contexts?',
    options: [
      'Use a global error type',
      'Use multiple either { } blocks nested',
      'Context receivers do not support multiple Raise types',
      'Use union types with sealed interfaces and withError for each context',
    ],
    correctAnswer: 3,
    explanation: 'You cannot have multiple Raise receivers simultaneously (ambiguity). Instead, define a sealed interface union of all error types and use withError to transform each sub-error into the union type.',
    codeSnippet: 'sealed interface ProcessError {\n  data class Parse(val e: ParseError) : ProcessError\n  data class Validate(val e: ValidationError) : ProcessError\n}\n\nfun Raise<ProcessError>.process(raw: String): Result {\n  val parsed = withError(ProcessError::Parse) { parse(raw) }\n  val validated = withError(ProcessError::Validate) { validate(parsed) }\n  return transform(validated)\n}',
    tags: ['raise-dsl', 'context-receivers', 'error-union'],
  },
  {
    id: 'kotlin-arrow-l3-041',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement filtering with optics?',
    options: [
      'Using filterIsInstance()',
      'Using filter() on the lens',
      'Using Every composed with a predicate-based Optional',
      'Optics do not support filtering',
    ],
    correctAnswer: 2,
    explanation: 'You can compose Every (Traversal over all elements) with a filtering Optional that only focuses on elements matching a predicate. This creates a Traversal that targets specific elements.',
    codeSnippet: '@optics data class School(val students: List<Student>) { companion object }\n@optics data class Student(val name: String, val grade: Int) { companion object }\n\n// Modify only students with grade >= 90\nSchool.students\n  .every(Every.list())\n  .filter { it.grade >= 90 }\n  .name\n  .modify(school) { "$it (honor roll)" }',
    tags: ['optics', 'filtering', 'traversal'],
  },
  {
    id: 'kotlin-arrow-l3-042',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the phantom type pattern?',
    options: [
      'A ghost class pattern',
      'A type that disappears at runtime',
      'A type that is always null',
      'A type parameter that is used for compile-time safety but never appears in the actual data',
    ],
    correctAnswer: 3,
    explanation: 'Phantom types are type parameters used only for compile-time type checking, not stored in the data. They encode state or constraints that prevent illegal operations at compile time.',
    codeSnippet: '// Phantom type for state tracking\nsealed interface DoorState\nobject Open : DoorState\nobject Closed : DoorState\n\nclass Door<S : DoorState> private constructor(val name: String) {\n  companion object {\n    fun closed(name: String) = Door<Closed>(name)\n  }\n}\n\nfun Door<Closed>.open(): Door<Open> = Door(name)\nfun Door<Open>.close(): Door<Closed> = Door(name)\n// Door<Closed>().open().open() // compile error!',
    tags: ['functional-patterns', 'phantom-type', 'type-safety'],
  },
  {
    id: 'kotlin-arrow-l3-043',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How does STM handle conflicts between concurrent transactions?',
    options: [
      'Transactions never conflict',
      'Using locks',
      'Optimistic concurrency: transactions run without locks and retry on conflict detection',
      'Using semaphores',
    ],
    correctAnswer: 2,
    explanation: 'STM uses optimistic concurrency control. Transactions execute without locks, recording reads and writes. At commit time, if any read TVar was modified by another transaction, the current transaction is automatically retried.',
    tags: ['stm', 'optimistic-concurrency', 'conflict-detection'],
  },
  {
    id: 'kotlin-arrow-l3-044',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'What does orElse do in Arrow\'s STM?',
    options: [
      'Provides a default value',
      'Tries the first transaction; if it calls retry(), tries the alternative transaction',
      'Combines two transactions into one',
      'Handles exceptions in transactions',
    ],
    correctAnswer: 1,
    explanation: 'orElse composes two STM transactions. If the first calls retry() (blocks), the second is tried instead. This enables composable blocking operations with alternatives.',
    codeSnippet: 'val item = atomically {\n  // Try to dequeue from primary queue\n  orElse(\n    { primaryQueue.dequeue() },   // retries if empty\n    { secondaryQueue.dequeue() }  // fallback to secondary\n  )\n}',
    tags: ['stm', 'orElse', 'composition'],
  },
  {
    id: 'kotlin-arrow-l3-045',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'How do you implement a connection pool using Arrow Resource?',
    options: [
      'Use a static list of connections',
      'Create a Resource that acquires a pool and releases all connections on shutdown',
      'Use Thread.sleep() between connection attempts',
      'Connection pools are not possible with Resource',
    ],
    correctAnswer: 1,
    explanation: 'A connection pool is a Resource that acquires a pool of connections during initialization and releases them all during shutdown. Individual connections can be borrowed within the pool\'s resource scope.',
    codeSnippet: 'fun connectionPool(config: PoolConfig): Resource<ConnectionPool> =\n  resource {\n    val pool = install(\n      { HikariDataSource(config.toHikariConfig()) }\n    ) { pool, _ ->\n      pool.close() // closes all connections\n    }\n    pool\n  }\n\n// Usage\nconnectionPool(config).use { pool ->\n  pool.connection.use { conn ->\n    conn.query(sql)\n  }\n}',
    tags: ['resource', 'connection-pool', 'lifecycle'],
  },
  {
    id: 'kotlin-arrow-l3-046',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Y combinator and how does it relate to recursion in FP?',
    options: [
      'A concurrency pattern',
      'A JavaScript framework',
      'A fixed-point combinator that enables recursion without self-reference',
      'A sorting algorithm',
    ],
    correctAnswer: 2,
    explanation: 'The Y combinator is a fixed-point combinator that allows defining recursive functions without explicit self-reference. While primarily theoretical, it demonstrates that recursion can emerge from pure lambda calculus.',
    codeSnippet: '// Y combinator in Kotlin (for illustration)\ntypealias Rec<A, B> = (Rec<A, B>) -> (A) -> B\n\nfun <A, B> y(f: ((A) -> B) -> (A) -> B): (A) -> B {\n  val rec: Rec<A, B> = { r -> { a -> f(r(r))(a) } }\n  return rec(rec)\n}\n\nval factorial = y<Int, Int> { self -> { n ->\n  if (n <= 1) 1 else n * self(n - 1)\n} }',
    tags: ['functional-patterns', 'y-combinator', 'recursion'],
  },
  {
    id: 'kotlin-arrow-l3-047',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the difference between Effect scope and Raise scope in Arrow\'s evolution?',
    options: [
      'Effect scope was the Arrow 1.x approach with wrapper types; Raise scope is Arrow 2.x with context receivers, simpler and more composable',
      'Raise scope is deprecated',
      'They are the same',
      'Effect scope is for coroutines, Raise scope is for regular functions',
    ],
    correctAnswer: 0,
    explanation: 'In Arrow 1.x, Effect<E, A> was a wrapper type requiring .effect { } to compose. Arrow 2.x replaced it with Raise<E> context receivers, eliminating wrapper overhead and enabling direct style programming.',
    codeSnippet: '// Arrow 1.x (old)\nfun findUser(id: String): Effect<UserError, User> = effect {\n  val user = repo.find(id) ?: shift(UserError.NotFound(id))\n  user\n}\n\n// Arrow 2.x (new) \nfun Raise<UserError>.findUser(id: String): User {\n  return repo.find(id) ?: raise(UserError.NotFound(id))\n}',
    tags: ['raise-dsl', 'evolution', 'arrow-migration'],
  },
  {
    id: 'kotlin-arrow-l3-048',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you implement retry with exponential backoff and error type narrowing?',
    options: [
      'Error narrowing during retry is not possible',
      'Using catch blocks',
      'Using Schedule with doWhile to filter retryable errors and mapLeft to narrow the error type',
      'Using a generic retry loop',
    ],
    correctAnswer: 2,
    explanation: 'Combine Schedule for retry logic with error type inspection. Only retry on transient errors, and narrow the final error type after retries are exhausted to distinguish between retryable failures and permanent errors.',
    codeSnippet: 'sealed interface FetchError {\n  data class Transient(val msg: String) : FetchError\n  data class Permanent(val msg: String) : FetchError\n}\n\nsuspend fun fetchWithRetry(): Either<FetchError.Permanent, Data> {\n  val result = Schedule.recurs<FetchError>(3)\n    .doWhile { e, _ -> e is FetchError.Transient }\n    .retryEither { fetchData() }\n  return result.mapLeft { it as FetchError.Permanent }\n}',
    tags: ['either', 'retry', 'error-narrowing'],
  },
  {
    id: 'kotlin-arrow-l3-049',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is the Snoc optic in Arrow?',
    options: [
      'An optic that focuses on the last element and the initial portion of a list',
      'An optic for snake case strings',
      'An optic for sorting collections',
      'An optic for concatenating lists',
    ],
    correctAnswer: 0,
    explanation: 'Snoc is the opposite of Cons. While Cons decomposes a list into head and tail, Snoc decomposes it into init (all but last) and last element. It is a Prism since empty lists have no decomposition.',
    codeSnippet: 'val list = listOf(1, 2, 3)\nSnoc.list<Int>().getOrNull(list) // Pair([1, 2], 3)\nSnoc.list<Int>().getOrNull(emptyList()) // null',
    tags: ['optics', 'snoc', 'list-decomposition'],
  },
  {
    id: 'kotlin-arrow-l3-050',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the Interpreter pattern with Arrow for a simple DSL?',
    options: [
      'Using eval() at runtime',
      'Model DSL operations as sealed classes and write an interpreter function using fold/when',
      'Using code generation',
      'Using runtime reflection',
    ],
    correctAnswer: 1,
    explanation: 'Define operations as a sealed class hierarchy (the AST). Then write interpreter functions that pattern-match on the operations, executing the appropriate logic. Multiple interpreters can be provided for different contexts.',
    codeSnippet: 'sealed interface Expr {\n  data class Lit(val value: Int) : Expr\n  data class Add(val a: Expr, val b: Expr) : Expr\n  data class Mul(val a: Expr, val b: Expr) : Expr\n}\n\nfun eval(expr: Expr): Int = when (expr) {\n  is Expr.Lit -> expr.value\n  is Expr.Add -> eval(expr.a) + eval(expr.b)\n  is Expr.Mul -> eval(expr.a) * eval(expr.b)\n}\n\nfun prettyPrint(expr: Expr): String = when (expr) {\n  is Expr.Lit -> "${expr.value}"\n  is Expr.Add -> "(${prettyPrint(expr.a)} + ${prettyPrint(expr.b)})"\n  is Expr.Mul -> "(${prettyPrint(expr.a)} * ${prettyPrint(expr.b)})"\n}',
    tags: ['functional-patterns', 'interpreter', 'dsl'],
  },
  {
    id: 'kotlin-arrow-l3-051',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What is the difference between parMap and parTraverseEither?',
    options: [
      'parTraverseEither is sequential',
      'parMap runs in parallel ignoring errors; parTraverseEither combines parallel execution with Either short-circuiting',
      'They are the same',
      'parMap returns Either, parTraverseEither returns List',
    ],
    correctAnswer: 1,
    explanation: 'parMap runs parallel operations that may throw exceptions. When combined with the either { } builder, you get parallel execution with typed error handling. The either builder catches raised errors.',
    codeSnippet: '// Parallel with typed errors - short-circuit\nval result: Either<Error, List<User>> = either {\n  ids.parMap { id ->\n    fetchUser(id).bind() // short-circuits on first Left\n  }\n}\n\n// Parallel with error accumulation\nval accumulated: Either<NonEmptyList<Error>, List<User>> = either {\n  ids.parMapOrAccumulate { id ->\n    fetchUser(id).bind()\n  }\n}',
    tags: ['arrow-fx', 'parallel', 'either'],
  },
  {
    id: 'kotlin-arrow-l3-052',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between early return and continuation-based control flow?',
    options: [
      'Continuations always throw exceptions',
      'Early return is functional; continuations are imperative',
      'Early return exits a function; continuation-based flow (like Raise) captures control flow as a first-class concept',
      'They are identical',
    ],
    correctAnswer: 2,
    explanation: 'Kotlin\'s return exits the enclosing function. Raise uses continuation-based control flow (internally similar to exceptions or coroutine suspension) to short-circuit blocks, which can be caught and handled by builders like either { }.',
    tags: ['functional-patterns', 'continuation', 'control-flow'],
  },
  {
    id: 'kotlin-arrow-l3-053',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement a custom builder for the Raise DSL?',
    options: [
      'Extend the Either class',
      'Use annotation processing',
      'You cannot create custom builders',
      'Implement a function that takes a Raise<E> receiver block, handles the fold, and returns your desired type',
    ],
    correctAnswer: 3,
    explanation: 'Custom builders use fold on a Raise computation to produce any output type. You provide handlers for both the error (raise) and success cases, wrapping the result in your desired type.',
    codeSnippet: 'inline fun <E, A> myResult(\n  block: Raise<E>.() -> A\n): MyResult<E, A> = fold(\n  block = block,\n  recover = { error -> MyResult.Failure(error) },\n  transform = { value -> MyResult.Success(value) }\n)',
    tags: ['raise-dsl', 'custom-builder', 'fold'],
  },
  {
    id: 'kotlin-arrow-l3-054',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use optics with Arrow\'s @optics annotation and KSP?',
    options: [
      'No build configuration needed',
      'Write a Gradle script to generate optics manually',
      'Use kapt instead of KSP',
      'Add the Arrow optics KSP plugin to the build, annotate data classes, and access generated optics via companion',
    ],
    correctAnswer: 3,
    explanation: 'Arrow Optics uses KSP (Kotlin Symbol Processing) for code generation. Add the KSP plugin and arrow-optics-ksp-plugin dependency. Annotate data classes with @optics and ensure they have companion objects.',
    codeSnippet: '// build.gradle.kts\nplugins {\n  id("com.google.devtools.ksp") version "..."\n}\ndependencies {\n  implementation("io.arrow-kt:arrow-optics")\n  ksp("io.arrow-kt:arrow-optics-ksp-plugin")\n}\n\n// Usage\n@optics data class Address(\n  val street: String,\n  val city: String\n) { companion object }',
    tags: ['optics', 'ksp', 'code-generation'],
  },
  {
    id: 'kotlin-arrow-l3-055',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the catamorphism concept and how does fold relate to it?',
    options: [
      'A generalized fold that deconstructs a recursive data type; fold on List/Either are catamorphisms',
      'A design pattern for databases',
      'A type of exception handling',
      'A category theory concept unrelated to programming',
    ],
    correctAnswer: 0,
    explanation: 'A catamorphism is a generalization of fold. It recursively deconstructs a data type by replacing constructors with functions. List.fold, Either.fold, and tree traversals are all catamorphisms.',
    codeSnippet: '// List catamorphism (fold)\n// Replaces Nil with initial and Cons with combining function\nlistOf(1, 2, 3).fold(0) { acc, x -> acc + x }\n\n// Either catamorphism (fold)\n// Replaces Left constructor with leftFn, Right with rightFn\neither.fold(\n  { left -> handleError(left) },\n  { right -> handleSuccess(right) }\n)',
    tags: ['functional-patterns', 'catamorphism', 'fold'],
  },
  {
    id: 'kotlin-arrow-l3-056',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you implement parallel validation with error accumulation using Either?',
    options: [
      'Parallel validation is not possible with Either',
      'Use sequential flatMap chains',
      'Use Either.zipOrAccumulate or parZipOrAccumulate for parallel execution',
      'Use Validated only',
    ],
    correctAnswer: 2,
    explanation: 'Either.zipOrAccumulate runs independent validations and collects all errors into NonEmptyList. For parallel execution, parZipOrAccumulate adds coroutine-based parallelism on top of error accumulation.',
    codeSnippet: 'suspend fun validateForm(\n  name: String, email: String, age: Int\n): Either<NonEmptyList<FormError>, ValidForm> =\n  Either.zipOrAccumulate(\n    validateName(name),\n    validateEmail(email),\n    validateAge(age)\n  ) { n, e, a -> ValidForm(n, e, a) }',
    tags: ['either', 'parallel-validation', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l3-057',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Monad Transformer pattern and why is it less common in Arrow 2.x?',
    options: [
      'A performance optimization',
      'A deprecated pattern',
      'A way to transform monads into other types',
      'Stacking monad behaviors (e.g., EitherT); less needed in Arrow 2.x because Raise DSL handles multiple effects naturally',
    ],
    correctAnswer: 3,
    explanation: 'Monad transformers (like EitherT<IO, E, A>) stack monad effects but create complex types. Arrow 2.x favors Raise context receivers with suspend, which naturally combines effects without the stacking complexity.',
    codeSnippet: '// Monad transformer style (complex, old)\n// EitherT<IO, Error, User>\n\n// Arrow 2.x style (simpler)\nsuspend fun Raise<Error>.getUser(id: String): User {\n  // suspend gives us IO-like effects\n  // Raise gives us typed errors\n  // No transformer stacking needed!\n  val user = httpClient.get(id)\n  ensure(user.isActive) { Error.InactiveUser }\n  return user\n}',
    tags: ['functional-patterns', 'monad-transformer', 'arrow-evolution'],
  },
  {
    id: 'kotlin-arrow-l3-058',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How does Arrow handle cancellation in parMap?',
    options: [
      'Cancellation is not supported',
      'When the parent coroutine is cancelled, all parallel operations are cancelled cooperatively',
      'Operations continue even after cancellation',
      'Only the currently running operation is cancelled',
    ],
    correctAnswer: 1,
    explanation: 'parMap uses structured concurrency. When the parent scope is cancelled, all child coroutines launched by parMap receive cancellation signals. They cancel cooperatively at their next suspension point.',
    codeSnippet: '// All parMap operations cancelled if the parent is cancelled\nwithTimeout(5.seconds) {\n  urls.parMap { url ->\n    // Each operation checks for cancellation at suspension points\n    httpClient.get(url) // cancellable\n  }\n}',
    tags: ['arrow-fx', 'cancellation', 'structured-concurrency'],
  },
  {
    id: 'kotlin-arrow-l3-059',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is a Getter optic and when would you use it?',
    options: [
      'An optic that gets values from a database',
      'Same as a Lens',
      'A read-only optic that can only get a value, not set it. Useful for computed properties.',
      'A network request optic',
    ],
    correctAnswer: 2,
    explanation: 'A Getter is a read-only optic. Unlike a Lens (which has both get and set), a Getter only provides get. It is useful for computed properties or derived values that do not have a meaningful set operation.',
    codeSnippet: '// Getter for a computed property\nval fullName: Getter<Person, String> = Getter { person ->\n  "${person.firstName} ${person.lastName}"\n}\n\nfullName.get(Person("John", "Doe")) // "John Doe"\n// fullName.set(...) // Not available - read-only!',
    tags: ['optics', 'getter', 'read-only'],
  },
  {
    id: 'kotlin-arrow-l3-060',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Church-Rosser theorem and why does it matter for FP?',
    options: [
      'A network routing theorem',
      'A database normalization theorem',
      'A sorting algorithm theorem',
      'It guarantees that the order of evaluation in lambda calculus does not affect the final result (confluence)',
    ],
    correctAnswer: 3,
    explanation: 'The Church-Rosser theorem states that if a lambda expression can be reduced in two different ways, both reductions can be further reduced to the same result. This justifies the safety of refactoring in pure FP.',
    tags: ['functional-patterns', 'church-rosser', 'theory'],
  },
  {
    id: 'kotlin-arrow-l3-061',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Resource',
    question: 'How do you combine Resource with Raise DSL for typed error handling during resource usage?',
    options: [
      'Use resource { } for acquisition and either { } inside the use block for typed errors',
      'They are incompatible',
      'Use try-catch inside resource blocks',
      'Resource automatically handles typed errors',
    ],
    correctAnswer: 0,
    explanation: 'Resource handles acquisition/release lifecycle. The Raise DSL handles typed errors during the usage phase. You can nest either { } inside a resource use block, or use resourceScope with Raise context.',
    codeSnippet: 'suspend fun processFile(path: String): Either<AppError, Result> {\n  return resource {\n    install({ File(path).bufferedReader() }) { r, _ -> r.close() }\n  }.use { reader ->\n    either {\n      val content = catch({ reader.readText() }) { e ->\n        raise(AppError.ReadFailed(e.message ?: ""))\n      }\n      parse(content)\n    }\n  }\n}',
    tags: ['resource', 'raise-dsl', 'composition'],
  },
  {
    id: 'kotlin-arrow-l3-062',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'What is the advantage of using Raise over returning Either directly?',
    options: [
      'Raise supports more error types',
      'Raise enables direct style (no wrapping/unwrapping) and composes naturally with suspend and other contexts',
      'Either is deprecated',
      'Raise is faster',
    ],
    correctAnswer: 1,
    explanation: 'Raise DSL provides direct style: you write code that looks imperative but has typed error handling. No need for .bind(), .flatMap(), or explicit Either wrapping. Functions return plain types, not Either-wrapped types.',
    codeSnippet: '// Direct style with Raise\nfun Raise<Error>.processOrder(raw: RawOrder): ProcessedOrder {\n  val parsed = parseOrder(raw)       // returns Order, not Either\n  val validated = validate(parsed)    // returns ValidOrder, not Either\n  return enrich(validated)            // returns ProcessedOrder, not Either\n}\n\n// vs. Either style\nfun processOrder(raw: RawOrder): Either<Error, ProcessedOrder> =\n  parseOrder(raw)\n    .flatMap { validate(it) }\n    .flatMap { enrich(it) }',
    tags: ['typed-errors', 'raise-dsl', 'direct-style'],
  },
  {
    id: 'kotlin-arrow-l3-063',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Atomic',
    question: 'How do you perform atomic updates on deeply nested immutable data using Arrow?',
    options: [
      'Use synchronized blocks',
      'Deep nesting prevents atomic updates',
      'Combine Atomic with Optics lenses for atomic lens-based updates',
      'Use a database transaction',
    ],
    correctAnswer: 2,
    explanation: 'Arrow\'s Atomic combined with optics lenses allows you to atomically update specific nested fields. The Atomic ensures thread-safe CAS updates, while the lens focuses on the specific field to modify.',
    codeSnippet: '@optics data class AppState(\n  val config: Config,\n  val users: Map<String, User>\n) { companion object }\n\nval state = Atomic(AppState(defaultConfig, emptyMap()))\n\n// Atomic update via lens\nstate.update {\n  AppState.config.maxRetries.modify(it) { retries -> retries + 1 }\n}',
    tags: ['atomic', 'optics', 'nested-update'],
  },
  {
    id: 'kotlin-arrow-l3-064',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the difference between parametric and ad-hoc polymorphism in the context of Arrow?',
    options: [
      'Ad-hoc is slower',
      'They are the same',
      'Parametric only works with primitive types',
      'Parametric uses generics (same behavior for all types); ad-hoc uses type classes/interfaces (different behavior per type)',
    ],
    correctAnswer: 3,
    explanation: 'Parametric polymorphism (generics) works the same for all types: fun <A> identity(a: A): A = a. Ad-hoc polymorphism provides different implementations per type, like Comparator<T> or Arrow\'s type class interfaces.',
    codeSnippet: '// Parametric: same behavior for all A\nfun <A> List<A>.head(): A? = firstOrNull()\n\n// Ad-hoc: different behavior per type\ninterface Show<A> {\n  fun show(a: A): String\n}\nval intShow = object : Show<Int> {\n  override fun show(a: Int) = a.toString()\n}\nval listShow = object : Show<List<*>> {\n  override fun show(a: List<*>) = a.joinToString(", ")\n}',
    tags: ['functional-patterns', 'polymorphism', 'type-classes'],
  },
  {
    id: 'kotlin-arrow-l3-065',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does Arrow optimize the Raise DSL to avoid excessive exception creation?',
    options: [
      'It uses reflection',
      'It uses bytecode rewriting',
      'It reuses a singleton exception with a filled stacktrace suppressed for performance',
      'There is no optimization',
    ],
    correctAnswer: 2,
    explanation: 'Arrow\'s Raise implementation uses an internal exception mechanism for non-local control flow, but optimizes by suppressing stack trace filling (which is the expensive part of exception creation), making it very efficient.',
    tags: ['raise-dsl', 'performance', 'optimization'],
  },
  {
    id: 'kotlin-arrow-l3-066',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Circuit Breaker',
    question: 'How do you combine Circuit Breaker with retry logic?',
    options: [
      'Use a single combined pattern',
      'They cannot be combined',
      'Circuit breaker replaces retry',
      'Apply retry inside the circuit breaker, or circuit breaker around the retry',
    ],
    correctAnswer: 3,
    explanation: 'Typically, retry is applied inside the circuit breaker: the circuit breaker tracks overall failures, and each attempt may retry a few times. The circuit opens after repeated overall failures, preventing resource waste.',
    codeSnippet: 'val cb = CircuitBreaker(maxFailures = 5, resetTimeout = 30.seconds)\nval retryPolicy = Schedule.recurs<Throwable>(2)\n  .and(Schedule.exponential(100.milliseconds))\n\n// Circuit breaker wraps retry\nval result = cb.protectOrThrow {\n  retryPolicy.retry {\n    callExternalService()\n  }\n}',
    tags: ['circuit-breaker', 'retry', 'resilience'],
  },
  {
    id: 'kotlin-arrow-l3-067',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the trampolining technique for stack-safe recursion?',
    options: [
      'A garbage collection optimization',
      'Converting recursive calls into a loop by returning thunks that are evaluated iteratively',
      'A JVM performance tuning technique',
      'A way to parallelize recursion',
    ],
    correctAnswer: 1,
    explanation: 'Trampolining wraps recursive calls in thunks (lazy values). Instead of deep recursive calls, each step returns a thunk. A loop evaluates thunks until a final value is reached, preventing stack overflow.',
    codeSnippet: 'sealed interface Trampoline<A> {\n  data class Done<A>(val value: A) : Trampoline<A>\n  data class More<A>(val thunk: () -> Trampoline<A>) : Trampoline<A>\n}\n\nfun <A> Trampoline<A>.run(): A {\n  var current = this\n  while (current is Trampoline.More) {\n    current = current.thunk()\n  }\n  return (current as Trampoline.Done).value\n}',
    tags: ['functional-patterns', 'trampolining', 'stack-safety'],
  },
  {
    id: 'kotlin-arrow-l3-068',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use optics with nullable fields?',
    options: [
      'Use !! operator with lenses',
      'Arrow generates Optional optics for nullable fields, which focus only when the value is non-null',
      'Optics do not support nullable fields',
      'Nullable fields are automatically converted to non-null',
    ],
    correctAnswer: 1,
    explanation: 'For nullable fields, the @optics annotation generates an Optional optic instead of a Lens. The Optional successfully focuses when the value is non-null and fails gracefully (returns null) when it is null.',
    codeSnippet: '@optics data class User(\n  val name: String,\n  val nickname: String?  // nullable -> generates Optional\n) { companion object }\n\nUser.nickname.getOrNull(User("Alice", "Ali")) // "Ali"\nUser.nickname.getOrNull(User("Bob", null))    // null\nUser.nickname.set(User("Bob", null), "Bobby") // User("Bob", "Bobby")',
    tags: ['optics', 'nullable', 'optional'],
  },
  {
    id: 'kotlin-arrow-l3-069',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the principle of least power in FP and how does it apply to optics?',
    options: [
      'Use the fewest lines of code',
      'Use the weakest abstraction that solves the problem (Getter over Lens, Lens over Traversal)',
      'Minimize CPU usage',
      'Use the cheapest cloud instance',
    ],
    correctAnswer: 1,
    explanation: 'The principle of least power suggests using the weakest abstraction that meets your needs. If you only need to read, use a Getter instead of a Lens. This communicates intent and provides stronger guarantees.',
    codeSnippet: '// Only need to read? Use Getter (most constrained)\nval fullName: Getter<Person, String> = ...\n\n// Need read + write for a single field? Use Lens\nval name: Lens<Person, String> = ...\n\n// Need to target a sum type case? Use Prism\nval circle: Prism<Shape, Circle> = ...\n\n// Need to target multiple elements? Use Traversal (least constrained)\nval allNames: Traversal<Department, String> = ...',
    tags: ['functional-patterns', 'least-power', 'optics-hierarchy'],
  },
  {
    id: 'kotlin-arrow-l3-070',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does forEachAccumulating work in the Raise DSL?',
    options: [
      'Accumulates the return values',
      'Iterates over elements, accumulating all raised errors instead of short-circuiting',
      'Runs iterations in parallel',
      'Same as forEach',
    ],
    correctAnswer: 1,
    explanation: 'forEachAccumulating processes each element in a collection and accumulates all errors into NonEmptyList. Unlike mapOrAccumulate, it does not collect results (used for side effects with error accumulation).',
    codeSnippet: 'either<NonEmptyList<Error>, Unit> {\n  users.forEachAccumulating { user ->\n    ensure(user.isValid()) { Error.InvalidUser(user.id) }\n    notifyUser(user)\n  }\n}',
    tags: ['raise-dsl', 'forEachAccumulating', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l3-071',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of totality in FP and how does Arrow promote it?',
    options: [
      'Using 100% test coverage',
      'Writing very long functions',
      'Writing code that handles all possible inputs',
      'Every function should be defined for all inputs of its type, using types like Either/Option instead of exceptions',
    ],
    correctAnswer: 3,
    explanation: 'A total function is defined for every input of its declared type. Arrow promotes totality through Either (for fallible operations), NonEmptyList (for non-empty guarantees), and the Raise DSL (for typed error handling).',
    codeSnippet: '// Partial function (undefined for empty list)\nfun <A> List<A>.unsafeHead(): A = first()\n\n// Total function (defined for all inputs)\nfun <A> List<A>.headOrNull(): A? = firstOrNull()\nfun <A> NonEmptyList<A>.safeHead(): A = head // always valid',
    tags: ['functional-patterns', 'totality', 'type-safety'],
  },
  {
    id: 'kotlin-arrow-l3-072',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement backpressure with Arrow Fx?',
    options: [
      'Using retry logic',
      'Using Thread.sleep()',
      'Using Flow with buffer operators, or Channel with bounded capacity',
      'Backpressure is not supported',
    ],
    correctAnswer: 2,
    explanation: 'Kotlin Flow and Channel support backpressure natively. Bounded channels block producers when full. Flow operators like buffer, conflate, and collectLatest control how fast producers can emit relative to consumer speed.',
    codeSnippet: 'val channel = Channel<Data>(capacity = 100) // bounded = backpressure\n\n// Producer suspends when channel is full\nlaunch {\n  dataSource.forEach { channel.send(it) }\n}\n\n// Consumer processes at its own pace\nlaunch {\n  for (item in channel) process(item)\n}',
    tags: ['arrow-fx', 'backpressure', 'channel'],
  },
  {
    id: 'kotlin-arrow-l3-073',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is a Fold optic in Arrow?',
    options: [
      'A read-only optic that can focus on zero or more targets, combining them with a combining function',
      'A deprecated optic',
      'Same as Kotlin\'s fold function',
      'An optic that folds two types into one',
    ],
    correctAnswer: 0,
    explanation: 'Fold is the read-only equivalent of Traversal. It focuses on multiple elements and combines them. It provides getAll, fold, and other aggregation operations but cannot modify the targeted values.',
    codeSnippet: 'val allEmployeeNames: Fold<Company, String> = \n  Company.departments.every(Every.list())\n    .employees.every(Every.list())\n    .name\n\nval names: List<String> = allEmployeeNames.getAll(company)\nval count: Int = allEmployeeNames.size(company)',
    tags: ['optics', 'fold', 'read-only'],
  },
  {
    id: 'kotlin-arrow-l3-074',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the Decorator pattern functionally?',
    options: [
      'Using function composition to wrap behavior around existing functions',
      'Using annotations',
      'Using class inheritance',
      'Using the Proxy pattern',
    ],
    correctAnswer: 0,
    explanation: 'In FP, the Decorator pattern is simply function composition. You wrap a function with additional behavior (logging, timing, retry) by composing it with other functions, without modifying the original.',
    codeSnippet: '// Decorating a function with logging\nfun <A, B> withLogging(\n  name: String,\n  f: suspend (A) -> B\n): suspend (A) -> B = { input ->\n  log("$name called with $input")\n  val result = f(input)\n  log("$name returned $result")\n  result\n}\n\nval fetchUser = withLogging("fetchUser") { id: String ->\n  repository.findById(id)\n}',
    tags: ['functional-patterns', 'decorator', 'composition'],
  },
  {
    id: 'kotlin-arrow-l3-075',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What is the relationship between Raise and NonEmptyList in error accumulation?',
    options: [
      'Raise cannot work with NonEmptyList',
      'NonEmptyList is used only for success values',
      'Accumulation functions produce Raise<NonEmptyList<E>> to guarantee at least one error on failure',
      'They are unrelated',
    ],
    correctAnswer: 2,
    explanation: 'Error accumulation functions like mapOrAccumulate and zipOrAccumulate produce NonEmptyList<E> as the error type. This guarantees that if validation fails, there is at least one error message, making empty-error-list bugs impossible.',
    codeSnippet: '// The error type is NonEmptyList<E>, not List<E>\nval result: Either<NonEmptyList<ValidationError>, ValidForm> = either {\n  zipOrAccumulate(\n    { validateName(input.name) },\n    { validateEmail(input.email) }\n  ) { name, email -> ValidForm(name, email) }\n}\n// If Left, guaranteed at least one error',
    tags: ['raise-dsl', 'nel', 'error-accumulation'],
  },
  {
    id: 'kotlin-arrow-l3-076',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the expression problem and how does Kotlin/Arrow address it?',
    options: [
      'The difficulty of adding both new types AND new operations to a type hierarchy; Kotlin uses sealed classes + extension functions',
      'A memory allocation issue',
      'A problem with regular expressions',
      'A compiler optimization problem',
    ],
    correctAnswer: 0,
    explanation: 'The expression problem: adding new cases is easy with OOP (new subclass), adding new operations is easy with FP (new function). Kotlin addresses it with sealed classes (fixed cases) + extension functions (open for new operations).',
    codeSnippet: '// Sealed class: fixed cases, easy to add operations\nsealed interface Expr {\n  data class Lit(val n: Int) : Expr\n  data class Add(val a: Expr, val b: Expr) : Expr\n}\n\n// Easy to add new operation (extension)\nfun Expr.eval(): Int = when (this) {\n  is Expr.Lit -> n\n  is Expr.Add -> a.eval() + b.eval()\n}\n\n// Easy to add another operation\nfun Expr.prettyPrint(): String = ...',
    tags: ['functional-patterns', 'expression-problem', 'design'],
  },
  {
    id: 'kotlin-arrow-l3-077',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you derive optics for generic data classes?',
    options: [
      'You must create manual optics for each type parameter',
      'Generics are not supported with optics',
      'The @optics annotation works with generic data classes; generated optics are parameterized',
      'Use type erasure',
    ],
    correctAnswer: 2,
    explanation: 'The @optics annotation supports generic data classes. The generated optics are parameterized by the same type parameters, maintaining type safety across the generic boundary.',
    codeSnippet: '@optics data class Response<T>(\n  val status: Int,\n  val body: T,\n  val headers: Map<String, String>\n) { companion object }\n\n// Generated: Response.status<T>() : Lens<Response<T>, Int>\n// Generated: Response.body<T>() : Lens<Response<T>, T>\n\nval resp = Response(200, User("Alice"), mapOf())\nResponse.body<User>().get(resp) // User("Alice")',
    tags: ['optics', 'generics', 'parameterized'],
  },
  {
    id: 'kotlin-arrow-l3-078',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of structural sharing in immutable data structures?',
    options: [
      'Reusing unchanged portions of data when creating modified copies to save memory',
      'Sharing database connections',
      'Sharing code between classes via inheritance',
      'Sharing data between threads without locks',
    ],
    correctAnswer: 0,
    explanation: 'When you copy an immutable data structure with modifications, unchanged parts are shared (referenced) rather than duplicated. This makes immutable updates efficient in both time and space.',
    codeSnippet: 'data class Tree(val left: Tree?, val value: Int, val right: Tree?)\n\n// When modifying just the left subtree:\n// - right subtree is shared (same reference)\n// - Only the path to the modified node is new\nval original = Tree(Tree(null, 1, null), 2, Tree(null, 3, null))\nval modified = original.copy(left = original.left?.copy(value = 10))\n// modified.right === original.right // same reference!',
    tags: ['functional-patterns', 'structural-sharing', 'immutability'],
  },
  {
    id: 'kotlin-arrow-l3-079',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How do you implement an STM-based concurrent queue?',
    options: [
      'Using a regular MutableList with synchronization',
      'STM does not support queues',
      'Using TVars to hold the queue state inside atomically blocks',
      'Using ConcurrentLinkedQueue',
    ],
    correctAnswer: 2,
    explanation: 'An STM queue uses TVar to hold the list of elements. Enqueue and dequeue operations run in atomically blocks. Dequeue can use retry() to block when the queue is empty, composing naturally with other STM operations.',
    codeSnippet: 'class STMQueue<A> {\n  private val items = TVar.new(emptyList<A>())\n\n  fun STM.enqueue(a: A) {\n    val current = items.read()\n    items.write(current + a)\n  }\n\n  fun STM.dequeue(): A {\n    val current = items.read()\n    if (current.isEmpty()) retry()\n    items.write(current.drop(1))\n    return current.first()\n  }\n}',
    tags: ['stm', 'queue', 'concurrency'],
  },
  {
    id: 'kotlin-arrow-l3-080',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is defunctionalization and how does it relate to Arrow?',
    options: [
      'Converting functions to strings',
      'A debugging technique',
      'Replacing higher-order functions with data representations of function applications',
      'Removing functions from a program',
    ],
    correctAnswer: 2,
    explanation: 'Defunctionalization replaces function values with data constructors representing each possible function. This is what sealed class DSLs do: instead of passing lambdas, you create data objects that an interpreter evaluates.',
    codeSnippet: '// Instead of higher-order function:\n// fun transform(f: (Int) -> Int, x: Int): Int = f(x)\n\n// Defunctionalized:\nsealed interface IntOp {\n  data object Double : IntOp\n  data class Add(val n: Int) : IntOp\n}\n\nfun apply(op: IntOp, x: Int): Int = when (op) {\n  is IntOp.Double -> x * 2\n  is IntOp.Add -> x + op.n\n}',
    tags: ['functional-patterns', 'defunctionalization', 'theory'],
  },
  {
    id: 'kotlin-arrow-l3-081',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Either',
    question: 'How do you implement EitherNel accumulation without zipOrAccumulate?',
    options: [
      'Use mapOrAccumulate in the Raise DSL, or manually combine with NonEmptyList Semigroup',
      'Use flatMap with error list',
      'It is impossible without zipOrAccumulate',
      'Use fold on a list of Either values',
    ],
    correctAnswer: 0,
    explanation: 'You can manually accumulate errors by collecting Either values, partitioning them, and constructing a NonEmptyList from the errors. However, mapOrAccumulate and zipOrAccumulate are the idiomatic approaches.',
    codeSnippet: '// Manual accumulation\nfun <E, A> List<Either<E, A>>.accumulateErrors(): Either<NonEmptyList<E>, List<A>> {\n  val (lefts, rights) = partition { it.isLeft() }\n  val errors = lefts.mapNotNull { it.leftOrNull() }\n  return errors.toNonEmptyListOrNull()\n    ?.let { it.left() }\n    ?: rights.mapNotNull { it.getOrNull() }.right()\n}',
    tags: ['either', 'nel', 'manual-accumulation'],
  },
  {
    id: 'kotlin-arrow-l3-082',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of lawful abstractions in FP?',
    options: [
      'Abstractions that satisfy mathematical laws (identity, associativity, etc.) ensuring predictable behavior',
      'Following coding standards',
      'Using proper naming conventions',
      'Following legal regulations in code',
    ],
    correctAnswer: 0,
    explanation: 'Lawful abstractions satisfy mathematical properties: Functor laws (identity and composition), Monad laws (left identity, right identity, associativity). These laws ensure that code behaves predictably when refactored.',
    codeSnippet: '// Functor identity law: map(id) == id\neither.map { it } == either\n\n// Functor composition law: map(f . g) == map(f) . map(g)\neither.map { g(f(it)) } == either.map(::f).map(::g)\n\n// Monad left identity: pure(a).flatMap(f) == f(a)\na.right().flatMap(::f) == f(a)',
    tags: ['functional-patterns', 'laws', 'correctness'],
  },
  {
    id: 'kotlin-arrow-l3-083',
    language: 'kotlin',
    level: 'level3',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement cooperative cancellation checking in long-running Arrow computations?',
    options: [
      'Long-running computations cannot be cancelled',
      'Cancellation is always automatic',
      'Use Thread.interrupt()',
      'Use ensureActive() or yield() at regular intervals in CPU-bound loops',
    ],
    correctAnswer: 3,
    explanation: 'Coroutine cancellation is cooperative. CPU-bound loops do not have suspension points, so they need explicit cancellation checks using ensureActive() (throws CancellationException if cancelled) or yield() (also gives other coroutines a chance to run).',
    codeSnippet: 'suspend fun heavyComputation(data: List<Int>): Int {\n  var result = 0\n  for (item in data) {\n    ensureActive() // check for cancellation\n    result += expensiveCalculation(item)\n  }\n  return result\n}\n\n// Now this can be properly cancelled\nwithTimeout(5.seconds) {\n  heavyComputation(largeDataset)\n}',
    tags: ['arrow-fx', 'cancellation', 'cooperative'],
  },
];

export default questions;
