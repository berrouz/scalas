import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-arrow-l4-001',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How does Arrow\'s Raise DSL implement non-local control flow without using Kotlin coroutines?',
    options: [
      'By throwing a special internal exception with suppressed stack trace creation for performance',
      'Using bytecode manipulation at compile time',
      'Using Thread.stop()',
      'Using java.lang.reflect.Proxy',
    ],
    correctAnswer: 0,
    explanation: 'Arrow\'s Raise internally throws a special exception (RaiseCancellationException or similar) when raise() is called. Stack trace creation is suppressed for performance. The either { } builder catches this specific exception and wraps the error in Left.',
    codeSnippet: '// Simplified internal mechanism\ninternal class RaiseException(val error: Any?) : Throwable() {\n  override fun fillInStackTrace(): Throwable = this // suppressed for perf\n}\n\ninline fun <E, A> either(block: Raise<E>.() -> A): Either<E, A> =\n  try {\n    Right(block(RaiseImpl()))\n  } catch (e: RaiseException) {\n    Left(e.error as E)\n  }',
    tags: ['raise-dsl', 'implementation', 'internals'],
  },
  {
    id: 'kotlin-arrow-l4-002',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What happens if you capture a Raise reference and use it outside its intended scope?',
    options: [
      'It works fine',
      'It returns null',
      'It automatically creates a new scope',
      'It causes undefined behavior - the Raise context is only valid within its builder scope',
    ],
    correctAnswer: 3,
    explanation: 'Leaking a Raise reference outside its scope (e.g., storing it in a variable and calling raise later) leads to undefined behavior. The internal exception may not be caught by the builder, causing crashes. Arrow 2.x addresses this with the @RaiseDSL annotation.',
    codeSnippet: '// DANGEROUS: leaking Raise scope\nvar leakedRaise: Raise<String>? = null\nval result = either<String, Int> {\n  leakedRaise = this // DON\'T DO THIS\n  42\n}\n// leakedRaise!!.raise("oops") // UNDEFINED BEHAVIOR\n// The either { } builder has already returned',
    tags: ['raise-dsl', 'scope-safety', 'anti-pattern'],
  },
  {
    id: 'kotlin-arrow-l4-003',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'What is the livelock problem in STM and how does Arrow mitigate it?',
    options: [
      'Livelock only occurs with databases',
      'Transactions constantly retry without making progress; mitigated with bounded retries and exponential backoff',
      'Using thread priorities',
      'Livelock is impossible in STM',
    ],
    correctAnswer: 1,
    explanation: 'Livelock occurs when transactions repeatedly conflict and retry, never completing. Arrow\'s STM implementation uses optimistic reads with validation at commit time. In pathological cases, applications should design transactions to minimize conflict windows.',
    tags: ['stm', 'livelock', 'concurrency-hazard'],
  },
  {
    id: 'kotlin-arrow-l4-004',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How do you implement a transactional bounded buffer using Arrow STM?',
    options: [
      'Bounded buffers are not possible with STM',
      'Using a ConcurrentHashMap',
      'Using a regular array with locks',
      'Using TVars for the buffer array and indices, with retry for full/empty conditions',
    ],
    correctAnswer: 3,
    explanation: 'A bounded buffer uses TVars for the array contents, read index, write index, and count. Enqueue retries when full, dequeue retries when empty. The STM runtime handles all synchronization automatically.',
    codeSnippet: 'class BoundedBuffer<A>(capacity: Int) {\n  private val buffer = TVar.new(Array<Any?>(capacity) { null })\n  private val count = TVar.new(0)\n  private val readIdx = TVar.new(0)\n  private val writeIdx = TVar.new(0)\n  private val cap = capacity\n\n  fun STM.put(a: A) {\n    val c = count.read()\n    if (c >= cap) retry() // block when full\n    val arr = buffer.read()\n    val wi = writeIdx.read()\n    arr[wi] = a\n    buffer.write(arr)\n    writeIdx.write((wi + 1) % cap)\n    count.write(c + 1)\n  }\n\n  fun STM.take(): A {\n    val c = count.read()\n    if (c <= 0) retry() // block when empty\n    val arr = buffer.read()\n    val ri = readIdx.read()\n    val value = arr[ri] as A\n    readIdx.write((ri + 1) % cap)\n    count.write(c - 1)\n    return value\n  }\n}',
    tags: ['stm', 'bounded-buffer', 'concurrent-data-structure'],
  },
  {
    id: 'kotlin-arrow-l4-005',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement a polymorphic lens that can change the type parameter?',
    options: [
      'Polymorphic lenses are not possible',
      'Using PLens<S, T, A, B> which gets A from S and sets B to produce T',
      'Using generic type parameters only',
      'Using unsafe casts',
    ],
    correctAnswer: 1,
    explanation: 'A polymorphic lens PLens<S, T, A, B> allows changing the type. get extracts A from S, and set puts B into S to produce T (a different type). This enables type-changing updates like Response<String> to Response<Int>.',
    codeSnippet: '// PLens<Response<A>, Response<B>, A, B>\n// Can change the body type\nval bodyLens = PLens<Response<String>, Response<Int>, String, Int>(\n  get = { resp -> resp.body },\n  set = { resp, newBody -> resp.copy(body = newBody) }\n)\n\nval stringResp = Response(200, "42")\nval intResp = bodyLens.set(stringResp, 42)\n// Response<Int>(200, 42)',
    tags: ['optics', 'polymorphic-lens', 'type-changing'],
  },
  {
    id: 'kotlin-arrow-l4-006',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement a custom Traversal that focuses on all values matching a predicate in a Map?',
    options: [
      'Using filter then toList',
      'Map traversals are not supported',
      'Use reflection to inspect map entries',
      'Implement Traversal by defining foldMap and modify that operate on matching entries',
    ],
    correctAnswer: 3,
    explanation: 'A custom Traversal requires implementing the core operations: foldMap for reading all targets and modify for updating all targets. For a predicate-filtered Map traversal, these iterate over matching entries.',
    codeSnippet: 'fun <K, V> filterMapTraversal(\n  predicate: (K, V) -> Boolean\n): Traversal<Map<K, V>, V> = object : Traversal<Map<K, V>, V> {\n  override fun <R> foldMap(M: Monoid<R>, source: Map<K, V>, map: (V) -> R): R =\n    source.entries\n      .filter { (k, v) -> predicate(k, v) }\n      .fold(M.empty()) { acc, (_, v) -> M.combine(acc, map(v)) }\n\n  override fun modify(source: Map<K, V>, map: (V) -> V): Map<K, V> =\n    source.mapValues { (k, v) ->\n      if (predicate(k, v)) map(v) else v\n    }\n}',
    tags: ['optics', 'custom-traversal', 'map'],
  },
  {
    id: 'kotlin-arrow-l4-007',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Yoneda lemma\'s practical implication for functional programming?',
    options: [
      'It proves that all monads are functors',
      'It has no practical application',
      'It provides a way to implement garbage collection',
      'Nat(Hom(A, -), F) is isomorphic to F(A); practically it enables fmap fusion (combining consecutive maps into one)',
    ],
    correctAnswer: 3,
    explanation: 'The Yoneda lemma implies that consecutive map operations can be fused into a single traversal. The Yoneda/Coyoneda types accumulate maps without executing them, then apply them all at once. This optimizes chains of transformations.',
    codeSnippet: '// Without Yoneda: three separate traversals\nlist.map(f).map(g).map(h)\n\n// Yoneda fusion: single traversal\n// Equivalent to: list.map { h(g(f(it))) }\ndata class Yoneda<F, A>(val run: <B> ((A) -> B) -> Kind<F, B>) {\n  fun <B> map(f: (A) -> B): Yoneda<F, B> =\n    Yoneda { k -> run { a -> k(f(a)) } }\n  // map is O(1)! No traversal until lowered\n}',
    tags: ['functional-patterns', 'yoneda', 'optimization'],
  },
  {
    id: 'kotlin-arrow-l4-008',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Kan extension and its relationship to adjoint functors in practical FP?',
    options: [
      'A type of dependency injection',
      'A mathematical concept with no programming relevance',
      'Kan extensions generalize many FP concepts; left Kan extension relates to colimits and right to limits; practically they unify map, fold, and traverse',
      'A serialization technique',
    ],
    correctAnswer: 2,
    explanation: 'Kan extensions are a powerful category theory concept. Right Kan extensions give rise to universal computations like fold. Left Kan extensions relate to free constructions. Many common FP operations are special cases of Kan extensions.',
    tags: ['functional-patterns', 'kan-extension', 'category-theory'],
  },
  {
    id: 'kotlin-arrow-l4-009',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is a profunctor optic and how does it generalize traditional optics?',
    options: [
      'A type of camera lens',
      'An optic defined in terms of profunctor transformations, unifying all optic types under a single encoding',
      'A lens that works with network protocols',
      'A deprecated optics approach',
    ],
    correctAnswer: 1,
    explanation: 'Profunctor optics represent all optic types (Lens, Prism, Traversal, etc.) uniformly as transformations on profunctors. This provides better composition, type inference, and eliminates the optic hierarchy complexity.',
    codeSnippet: '// Traditional: different types compose to different result types\n// Lens compose Prism = Optional\n// Prism compose Lens = Optional\n\n// Profunctor optics: uniform representation\n// type Optic p s t a b = p a b -> p s t\n// All optics are the same shape, differing only in\n// the constraint on profunctor p',
    tags: ['optics', 'profunctor', 'advanced-theory'],
  },
  {
    id: 'kotlin-arrow-l4-010',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement a Raise-based computation that supports both error short-circuiting and error accumulation in the same block?',
    options: [
      'Use a global flag',
      'Use two separate either blocks',
      'Use accumulate {} inside an either {} block to switch to accumulation mode for specific sections',
      'This is impossible - you must choose one or the other',
    ],
    correctAnswer: 2,
    explanation: 'Arrow allows mixing short-circuit and accumulation semantics. Inside either { }, specific sections can use zipOrAccumulate or mapOrAccumulate for accumulation, while the outer block remains short-circuiting.',
    codeSnippet: 'val result = either<AppError, Order> {\n  // Short-circuit: stop on first parse error\n  val rawOrder = parseRequest(request).bind()\n\n  // Accumulate: collect ALL validation errors\n  val validOrder = withError({ AppError.ValidationErrors(it) }) {\n    zipOrAccumulate(\n      { validateItems(rawOrder.items) },\n      { validateShipping(rawOrder.address) },\n      { validatePayment(rawOrder.payment) }\n    ) { items, shipping, payment -> ValidOrder(items, shipping, payment) }\n  }\n\n  // Short-circuit again: stop if save fails\n  save(validOrder).bind()\n}',
    tags: ['raise-dsl', 'mixed-mode', 'accumulation'],
  },
  {
    id: 'kotlin-arrow-l4-011',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you design a typed error hierarchy that supports both domain-level and infrastructure-level errors with proper separation?',
    options: [
      'Use exception classes',
      'Use a single flat sealed class',
      'Use string error codes',
      'Use nested sealed interfaces with infrastructure errors as separate hierarchies, mapped via withError at boundaries',
    ],
    correctAnswer: 3,
    explanation: 'Domain errors and infrastructure errors should be separate sealed hierarchies. At service boundaries, use withError to map infrastructure errors into domain-level errors. This keeps each layer focused and testable.',
    codeSnippet: '// Infrastructure layer\nsealed interface DbError {\n  data class ConnectionFailed(val cause: Throwable) : DbError\n  data class QueryFailed(val sql: String) : DbError\n}\n\n// Domain layer\nsealed interface OrderError {\n  data class NotFound(val id: OrderId) : OrderError\n  data class InvalidState(val current: State) : OrderError\n  data class InfraFailure(val msg: String) : OrderError\n}\n\n// Service boundary\nfun Raise<OrderError>.getOrder(id: OrderId): Order {\n  return withError({ OrderError.InfraFailure(it.toString()) }) {\n    queryOrder(id) // Raise<DbError>\n  } ?: raise(OrderError.NotFound(id))\n}',
    tags: ['typed-errors', 'architecture', 'error-hierarchy'],
  },
  {
    id: 'kotlin-arrow-l4-012',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you implement union types for error handling without sealed classes?',
    options: [
      'Using Either<ErrorA, Either<ErrorB, A>> nesting, or intersection of Raise contexts with context receivers',
      'Using reflection',
      'Union types are not possible in Kotlin',
      'Using Any as the error type',
    ],
    correctAnswer: 0,
    explanation: 'While Kotlin does not have native union types, you can simulate them with nested Either, or with multiple Raise context receivers (when supported). Sealed classes remain the most idiomatic approach for error unions.',
    codeSnippet: '// Approach 1: Sealed interface (recommended)\nsealed interface AppError\nclass DbError : AppError\nclass ValidationError : AppError\n\n// Approach 2: Multiple Raise contexts (experimental)\ncontext(Raise<DbError>, Raise<ValidationError>)\nfun process(): Result {\n  // Can raise either error type\n}\n\n// Approach 3: Nested Either (complex)\nfun process(): Either<DbError, Either<ValidationError, Result>>',
    tags: ['typed-errors', 'union-types', 'patterns'],
  },
  {
    id: 'kotlin-arrow-l4-013',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a resource-safe worker pool using Arrow?',
    options: [
      'Using ExecutorService only',
      'Worker pools are anti-functional',
      'Using a ThreadPoolExecutor',
      'Using Resource for lifecycle, Channel for work distribution, and parMap with concurrency limit',
    ],
    correctAnswer: 3,
    explanation: 'Combine Resource for lifecycle management (pool creation/shutdown), Channel for distributing work items, and coroutine concurrency control. Resource ensures cleanup even on failure.',
    codeSnippet: 'fun <A, B> workerPool(\n  workers: Int,\n  process: suspend (A) -> B\n): Resource<Pair<Channel<A>, Channel<B>>> = resource {\n  val input = install({ Channel<A>(100) }) { ch, _ -> ch.close() }\n  val output = install({ Channel<B>(100) }) { ch, _ -> ch.close() }\n\n  repeat(workers) {\n    launch {\n      for (item in input) {\n        output.send(process(item))\n      }\n    }\n  }\n\n  Pair(input, output)\n}',
    tags: ['arrow-fx', 'worker-pool', 'resource'],
  },
  {
    id: 'kotlin-arrow-l4-014',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you handle the "diamond dependency" problem in resource composition?',
    options: [
      'Use a singleton pattern',
      'Structure resource dependencies as a DAG, sharing resources via the resource scope',
      'Use global variables',
      'Duplicate resources',
    ],
    correctAnswer: 1,
    explanation: 'When multiple resources depend on the same sub-resource, acquire it once in the resource scope and pass it to all dependents. The resource { } DSL naturally supports this through sequential binding.',
    codeSnippet: 'val appResources = resource {\n  // Shared dependency\n  val config = install({ loadConfig() }) { _, _ -> }\n\n  // Both depend on config\n  val db = install({ Database(config.dbUrl) }) { db, _ -> db.close() }\n  val cache = install({ Redis(config.redisUrl) }) { r, _ -> r.close() }\n\n  // Both depend on db\n  val userRepo = UserRepo(db)\n  val orderRepo = OrderRepo(db)\n\n  AppContext(userRepo, orderRepo, cache)\n}',
    tags: ['arrow-fx', 'resource', 'diamond-dependency'],
  },
  {
    id: 'kotlin-arrow-l4-015',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of "algebras" in FP and how does Arrow use them?',
    options: [
      'A math library',
      'A type of encryption',
      'Mathematical equations in code',
      'Interfaces defining operations that form an algebra; implementations are interpreters of the algebra',
    ],
    correctAnswer: 3,
    explanation: 'An algebra is an interface that defines operations (the "signature"). Different implementations provide different interpretations. This is the foundation of Tagless Final / MTL style, which Arrow supports through interfaces and context receivers.',
    codeSnippet: '// The algebra (interface)\ninterface UserAlgebra {\n  suspend fun Raise<UserError>.find(id: UserId): User\n  suspend fun Raise<UserError>.save(user: User): UserId\n  suspend fun Raise<UserError>.delete(id: UserId): Unit\n}\n\n// Production interpreter\nclass PostgresUserAlgebra(val db: Database) : UserAlgebra {\n  override suspend fun Raise<UserError>.find(id: UserId) = ...\n  // ...\n}\n\n// Test interpreter\nclass InMemoryUserAlgebra : UserAlgebra { ... }',
    tags: ['functional-patterns', 'algebra', 'tagless-final'],
  },
  {
    id: 'kotlin-arrow-l4-016',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the Effect Handler pattern in Kotlin with Arrow?',
    options: [
      'Using compiler plugins',
      'Using try-catch blocks',
      'Using Raise context receivers as effect capabilities, with fold/either as handlers',
      'Using AOP frameworks',
    ],
    correctAnswer: 2,
    explanation: 'Effect handlers are functions that interpret effects. In Arrow, Raise is an effect (capability to fail). The either/fold builders are handlers that interpret the Raise effect. Custom handlers can be written using fold.',
    codeSnippet: '// Effect declaration: Raise<E> is the effect\nfun Raise<String>.computation(): Int {\n  ensure(condition) { "Failed" }  // uses the Raise effect\n  return 42\n}\n\n// Handler 1: interpret as Either\nval asEither: Either<String, Int> = either { computation() }\n\n// Handler 2: interpret as nullable\nval asNullable: Int? = nullable { computation() }\n\n// Handler 3: custom handler\nval withLogging: Int = fold(\n  { computation() },\n  { error -> log(error); -1 },\n  { value -> value }\n)',
    tags: ['functional-patterns', 'effect-handler', 'raise-dsl'],
  },
  {
    id: 'kotlin-arrow-l4-017',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement bidirectional transformations using Arrow Optics for data migration?',
    options: [
      'Use Iso optics to define lossless bidirectional mappings between schema versions',
      'Write manual conversion functions',
      'Optics are not suitable for data migration',
      'Use database migration tools',
    ],
    correctAnswer: 0,
    explanation: 'Iso optics represent lossless, bidirectional transformations. By composing Isos for each schema change, you can build migration paths that can go forward and backward between data versions.',
    codeSnippet: '// V1 -> V2 migration (lossless)\nval v1ToV2: Iso<UserV1, UserV2> = Iso(\n  get = { v1 -> UserV2(v1.name, v1.email, role = "user") },\n  reverseGet = { v2 -> UserV1(v2.name, v2.email) }\n)\n\n// V2 -> V3 migration\nval v2ToV3: Iso<UserV2, UserV3> = Iso(\n  get = { v2 -> UserV3(Name(v2.name), Email(v2.email), Role(v2.role)) },\n  reverseGet = { v3 -> UserV2(v3.name.value, v3.email.value, v3.role.value) }\n)\n\n// Compose: V1 -> V3\nval v1ToV3 = v1ToV2 compose v2ToV3',
    tags: ['optics', 'iso', 'data-migration'],
  },
  {
    id: 'kotlin-arrow-l4-018',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is the van Laarhoven encoding of optics and why is it significant?',
    options: [
      'A naming convention',
      'A binary encoding for optics data',
      'A compression algorithm for large data structures',
      'Encoding optics as polymorphic functions: Lens s a = forall f. Functor f => (a -> f a) -> s -> f s',
    ],
    correctAnswer: 3,
    explanation: 'Van Laarhoven lenses encode optics as rank-2 polymorphic functions. A Lens is a function that works for any Functor f. This encoding provides automatic composition via function composition and unifies different optic types.',
    codeSnippet: '// Van Laarhoven Lens (conceptual)\n// type Lens s a = forall f. Functor f => (a -> f a) -> (s -> f s)\n\n// In Kotlin (simplified):\ntypealias VLLens<S, A> = <F> (Functor<F>) -> ((A) -> Kind<F, A>) -> (S) -> Kind<F, S>\n\n// Composition is just function composition!\nval composed = lens1.andThen(lens2) // automatic!',
    tags: ['optics', 'van-laarhoven', 'encoding'],
  },
  {
    id: 'kotlin-arrow-l4-019',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the relationship between delimited continuations and the Raise DSL?',
    options: [
      'Delimited continuations are only for Scheme/Haskell',
      'No relationship',
      'Raise DSL is a form of delimited continuation where either { } is the delimiter and raise() captures/aborts to it',
      'Raise DSL uses undelimited continuations',
    ],
    correctAnswer: 2,
    explanation: 'Delimited continuations capture computation up to a delimiter (reset). raise() acts like shift (abort to delimiter), and either { } acts like reset (establishes the delimiter). This connection to delimited continuations is fundamental to the Raise DSL\'s design.',
    codeSnippet: '// either { } = reset (delimiter)\n// raise(e) = shift (abort to delimiter with value)\nval result = either<String, Int> { // reset\n  val x = 1\n  if (x < 0) raise("negative") // shift: abort to either { }\n  x + 1\n}\n// Continuation from raise to either { } boundary is discarded',
    tags: ['functional-patterns', 'delimited-continuations', 'raise-dsl'],
  },
  {
    id: 'kotlin-arrow-l4-020',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement a type-safe state machine using sealed classes and the Raise DSL?',
    options: [
      'Define states as sealed types, transitions as functions on specific states using Raise for invalid transitions',
      'Using coroutines only',
      'Using a state transition table',
      'Using enum states and switch statements',
    ],
    correctAnswer: 0,
    explanation: 'Model states as sealed interface subtypes. Transitions are functions that accept specific state types and return new states. Invalid transitions are prevented at compile time or raise typed errors.',
    codeSnippet: 'sealed interface OrderState {\n  data class Created(val id: OrderId) : OrderState\n  data class Paid(val id: OrderId, val paymentId: PaymentId) : OrderState\n  data class Shipped(val id: OrderId, val tracking: String) : OrderState\n  data class Cancelled(val id: OrderId, val reason: String) : OrderState\n}\n\n// Type-safe transitions\nfun Raise<OrderError>.pay(state: OrderState.Created, payment: Payment): OrderState.Paid {\n  val paymentId = processPayment(payment)\n  return OrderState.Paid(state.id, paymentId)\n}\n\nfun Raise<OrderError>.ship(state: OrderState.Paid, carrier: Carrier): OrderState.Shipped {\n  val tracking = carrier.ship(state.id)\n  return OrderState.Shipped(state.id, tracking)\n}\n// ship(Created(...)) -> compile error! Must be Paid first',
    tags: ['functional-patterns', 'state-machine', 'type-safety'],
  },
  {
    id: 'kotlin-arrow-l4-021',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement graceful shutdown with Arrow Resource and coroutines?',
    options: [
      'Using System.exit()',
      'Using Thread.interrupt()',
      'Using Resource scope cancellation with shutdown hooks and cooperative cancellation',
      'Using a kill -9 signal',
    ],
    correctAnswer: 2,
    explanation: 'Set up a JVM shutdown hook that cancels the resource scope. Resources are released in reverse order. Long-running operations should check for cancellation to enable graceful shutdown.',
    codeSnippet: 'suspend fun main() {\n  val scope = resourceScope {\n    val server = install({ startServer() }) { s, exitCase ->\n      when (exitCase) {\n        is ExitCase.Cancelled -> {\n          s.drainConnections(timeout = 30.seconds)\n          s.stop()\n        }\n        else -> s.stop()\n      }\n    }\n    val db = install({ connectDb() }) { db, _ -> db.close() }\n\n    // Register shutdown hook\n    Runtime.getRuntime().addShutdownHook(Thread {\n      runBlocking { scope.cancel() }\n    })\n\n    server.awaitTermination()\n  }\n}',
    tags: ['arrow-fx', 'graceful-shutdown', 'resource'],
  },
  {
    id: 'kotlin-arrow-l4-022',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'What are the implications of structured concurrency violations when using Arrow Fx?',
    options: [
      'Compilation errors',
      'Resource leaks, uncaught exceptions, zombie coroutines, and inability to cancel computations properly',
      'No implications',
      'Only performance degradation',
    ],
    correctAnswer: 1,
    explanation: 'Violating structured concurrency (e.g., launching coroutines in GlobalScope, leaking coroutine references) leads to: resources not being cleaned up, exceptions being silently swallowed, coroutines running after their parent is cancelled, and memory leaks.',
    codeSnippet: '// VIOLATION: leaking scope\nfun badFunction(scope: CoroutineScope) {\n  GlobalScope.launch { // escapes structured concurrency\n    while (true) {\n      processQueue() // zombie coroutine after parent cancellation\n    }\n  }\n}\n\n// CORRECT: respect the scope\nsuspend fun goodFunction() = coroutineScope {\n  launch { // respects parent scope\n    while (isActive) { // checks for cancellation\n      processQueue()\n    }\n  }\n}',
    tags: ['arrow-fx', 'structured-concurrency', 'violations'],
  },
  {
    id: 'kotlin-arrow-l4-023',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Saga',
    question: 'How do you handle partial compensation failures in a Saga?',
    options: [
      'Throw an exception and crash',
      'Log the failure and continue compensating remaining steps; optionally store failed compensations for manual intervention',
      'Ignore compensation failures',
      'Retry the entire saga',
    ],
    correctAnswer: 1,
    explanation: 'Compensation failures are a critical edge case. Best practice: attempt all compensations even if some fail, log failures for manual intervention, store uncompensated operations in a dead-letter queue for later resolution.',
    codeSnippet: 'suspend fun <A> resilientSaga(\n  action: suspend () -> A,\n  compensate: suspend (A) -> Unit,\n  onCompensationFailed: suspend (A, Throwable) -> Unit\n): A {\n  val result = action()\n  // When compensation needed:\n  try {\n    compensate(result)\n  } catch (e: Throwable) {\n    onCompensationFailed(result, e)\n    // Store in dead-letter queue for manual intervention\n    deadLetterQueue.send(FailedCompensation(result, e))\n  }\n  return result\n}',
    tags: ['saga', 'compensation-failure', 'resilience'],
  },
  {
    id: 'kotlin-arrow-l4-024',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Saga',
    question: 'How do you implement an orchestrator-based saga with parallel steps using Arrow?',
    options: [
      'Sagas must be sequential',
      'Use parZip within saga steps, with compensations that undo all parallel operations',
      'Parallel sagas are not supported',
      'Use threads for parallelism',
    ],
    correctAnswer: 1,
    explanation: 'Saga steps can internally run parallel operations using parZip. The compensation for a parallel step must undo all parallel operations. If one parallel operation fails, the others are cancelled and previous saga steps are compensated.',
    codeSnippet: 'saga {\n  val orderId = saga(\n    { createOrder(items) },\n    { cancelOrder(it) }\n  )\n\n  // Parallel step: reserve inventory AND charge payment\n  val (inventoryId, paymentId) = saga(\n    {\n      parZip(\n        { reserveInventory(orderId) },\n        { chargePayment(orderId, amount) }\n      ) { inv, pay -> Pair(inv, pay) }\n    },\n    { (inv, pay) ->\n      // Compensate both parallel operations\n      parZip(\n        { releaseInventory(inv) },\n        { refundPayment(pay) }\n      ) { _, _ -> }\n    }\n  )\n}',
    tags: ['saga', 'parallel', 'orchestrator'],
  },
  {
    id: 'kotlin-arrow-l4-025',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the final tagless encoding versus initial encoding of effects?',
    options: [
      'Final is runtime, initial is compile-time',
      'They are identical approaches',
      'Initial is deprecated',
      'Initial uses data types (sealed classes/free monads) to represent programs; final uses interfaces/type classes for direct interpretation',
    ],
    correctAnswer: 3,
    explanation: 'Initial encoding (free monads): programs are data structures interpreted later. Final encoding (tagless final/interfaces): programs are written against abstract interfaces, interpreted by concrete implementations. Arrow 2.x favors final encoding.',
    codeSnippet: '// Initial encoding: program as data\nsealed interface Program<A> {\n  data class GetUser(val id: String) : Program<User>\n  data class SaveUser(val user: User) : Program<Unit>\n  data class FlatMap<A, B>(val fa: Program<A>, val f: (A) -> Program<B>) : Program<B>\n}\n\n// Final encoding: program against interface (preferred in Kotlin)\ninterface UserService {\n  suspend fun Raise<Error>.getUser(id: String): User\n  suspend fun Raise<Error>.saveUser(user: User)\n}',
    tags: ['functional-patterns', 'final-tagless', 'initial-encoding'],
  },
  {
    id: 'kotlin-arrow-l4-026',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How do you implement a lock-free concurrent data structure using STM\'s composability?',
    options: [
      'STM uses locks internally',
      'Using CAS operations directly',
      'Lock-free is not possible with STM',
      'Build higher-level abstractions by composing atomic STM operations inside atomically blocks',
    ],
    correctAnswer: 3,
    explanation: 'STM\'s power is composability: you can build complex concurrent data structures by composing simple TVar operations. The runtime handles conflict resolution and retry, making the code appear sequential while being concurrent.',
    codeSnippet: 'class ConcurrentMap<K, V> {\n  private val buckets = Array(16) { TVar.new(emptyList<Pair<K, V>>()) }\n\n  fun STM.get(key: K): V? {\n    val bucket = buckets[key.hashCode() % buckets.size]\n    return bucket.read().find { it.first == key }?.second\n  }\n\n  fun STM.put(key: K, value: V) {\n    val idx = key.hashCode() % buckets.size\n    val bucket = buckets[idx]\n    val current = bucket.read().filter { it.first != key }\n    bucket.write(current + (key to value))\n  }\n\n  // Composable: atomically transfer between two maps\n  fun STM.transfer(other: ConcurrentMap<K, V>, key: K) {\n    val value = get(key) ?: retry()\n    remove(key)\n    other.put(key, value)\n  }\n}',
    tags: ['stm', 'lock-free', 'concurrent-data-structure'],
  },
  {
    id: 'kotlin-arrow-l4-027',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement effectful optics that perform side effects during get/set?',
    options: [
      'Use suspend functions inside optic operations, or combine optics with Raise/either for effectful transformations',
      'Optics cannot have side effects',
      'Use mutable state',
      'Use reflection',
    ],
    correctAnswer: 0,
    explanation: 'While standard optics are pure, you can use them within effectful contexts. Combine optic.get for extraction, perform effects, then use optic.set for updates. This keeps the optics pure while the surrounding code handles effects.',
    codeSnippet: 'suspend fun Raise<AppError>.updateUserEmail(\n  state: Atomic<AppState>,\n  userId: String,\n  newEmail: String\n): AppState {\n  // Pure optic composition\n  val userEmail = AppState.users\n    .index(Index.map(), userId)\n    .email\n\n  // Effectful usage\n  val oldState = state.get()\n  val currentEmail = userEmail.getOrNull(oldState)\n    ?: raise(AppError.UserNotFound(userId))\n\n  ensure(isValidEmail(newEmail)) { AppError.InvalidEmail(newEmail) }\n  sendEmailChangeNotification(currentEmail, newEmail) // suspend side effect\n\n  return state.updateAndGet { userEmail.set(it, newEmail) }\n}',
    tags: ['optics', 'effects', 'suspend'],
  },
  {
    id: 'kotlin-arrow-l4-028',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Circuit Breaker',
    question: 'How do you implement a circuit breaker with different policies for different error types?',
    options: [
      'Wrap the circuit breaker with error type inspection to count only specific errors toward the failure threshold',
      'Circuit breakers do not support error type filtering',
      'Use multiple circuit breakers',
      'Use exception hierarchy filtering only',
    ],
    correctAnswer: 0,
    explanation: 'Wrap the circuit breaker to inspect error types. Only count transient/retryable errors toward the failure threshold. Permanent errors should pass through without affecting the circuit breaker state.',
    codeSnippet: 'class SmartCircuitBreaker(\n  private val cb: CircuitBreaker,\n  private val shouldCount: (Throwable) -> Boolean\n) {\n  suspend fun <A> protect(block: suspend () -> A): Either<Throwable, A> =\n    try {\n      Either.Right(cb.protectOrThrow {\n        try {\n          block()\n        } catch (e: Throwable) {\n          if (shouldCount(e)) throw e // counted by CB\n          else throw PermanentError(e) // bypasses CB\n        }\n      })\n    } catch (e: PermanentError) {\n      Either.Left(e.cause)\n    }\n}\n\nval smartCb = SmartCircuitBreaker(cb) { it is IOException }',
    tags: ['circuit-breaker', 'error-type', 'advanced-configuration'],
  },
  {
    id: 'kotlin-arrow-l4-029',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of "initial algebra" and "final coalgebra" in FP?',
    options: [
      'Database concepts',
      'Compilation phases',
      'Initial algebra defines a type as the least fixed point (data); final coalgebra defines as the greatest fixed point (codata/streams)',
      'They are identical',
    ],
    correctAnswer: 2,
    explanation: 'Initial algebra (data): finite, constructed bottom-up (lists, trees). Final coalgebra (codata): potentially infinite, observed/destructed (streams, processes). List is an initial algebra; Sequence/Flow is a final coalgebra.',
    codeSnippet: '// Initial algebra: List (finite, fully constructed)\nsealed interface ListF<A> {\n  data object Nil : ListF<Nothing>\n  data class Cons<A>(val head: A, val tail: ListF<A>) : ListF<A>\n}\n\n// Final coalgebra: Stream (potentially infinite, observed)\nclass Stream<A>(\n  val head: A,\n  val tail: () -> Stream<A> // lazy, may never terminate\n)\n\n// Sequence is Kotlin\'s final coalgebra for collections\nval naturals = generateSequence(0) { it + 1 } // infinite',
    tags: ['functional-patterns', 'algebra', 'coalgebra'],
  },
  {
    id: 'kotlin-arrow-l4-030',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement type-level programming in Kotlin for safer FP abstractions?',
    options: [
      'Using annotation processing only',
      'Using generic constraints, sealed types, phantom types, and value classes to encode invariants',
      'Kotlin does not support any type-level programming',
      'Using runtime reflection only',
    ],
    correctAnswer: 1,
    explanation: 'While Kotlin lacks Haskell-level type features, you can encode significant invariants using generics with constraints (where T : Comparable<T>), sealed types for ADTs, phantom types for state, and value classes for domain types.',
    codeSnippet: '// Phantom type for units\nsealed interface Unit\nobject Meters : Unit\nobject Seconds : Unit\n\n@JvmInline\nvalue class Quantity<U : Unit>(val value: Double) {\n  operator fun plus(other: Quantity<U>): Quantity<U> =\n    Quantity(value + other.value)\n}\n\nfun Quantity<Meters>.toKilometers(): Quantity<Meters> =\n  Quantity(value / 1000.0)\n\nval dist = Quantity<Meters>(5.0)\nval time = Quantity<Seconds>(2.0)\n// dist + time // COMPILE ERROR: type mismatch',
    tags: ['functional-patterns', 'type-level', 'phantom-types'],
  },
  {
    id: 'kotlin-arrow-l4-031',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement backpressure-aware parallel processing with Arrow?',
    options: [
      'Using Thread.sleep() for rate limiting',
      'Combine Flow with buffer operators, parMap with concurrency limits, and Channel for bounded queuing',
      'Using unlimited parallelism',
      'Backpressure is not needed with coroutines',
    ],
    correctAnswer: 1,
    explanation: 'Implement backpressure by combining: (1) bounded channels that suspend producers when full, (2) parMap with concurrency parameter to limit parallel workers, (3) Flow buffer operators for stream backpressure.',
    codeSnippet: 'suspend fun processWithBackpressure(\n  items: Flow<Input>,\n  concurrency: Int = 10,\n  bufferSize: Int = 100\n): Flow<Output> = items\n  .buffer(bufferSize) // backpressure buffer\n  .chunked(concurrency) // process in batches\n  .map { chunk ->\n    chunk.parMap(concurrency = concurrency) { input ->\n      process(input) // parallel within chunk\n    }\n  }\n  .flatMapConcat { it.asFlow() }',
    tags: ['arrow-fx', 'backpressure', 'flow'],
  },
  {
    id: 'kotlin-arrow-l4-032',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement transactional error handling that can rollback multiple operations atomically?',
    options: [
      'Combine Raise DSL with Resource or Saga pattern for acquisition-with-compensation semantics',
      'Using database transactions only',
      'Using try-finally blocks',
      'Atomic rollback is not possible in application code',
    ],
    correctAnswer: 0,
    explanation: 'For transactional semantics at the application level, combine the Saga pattern (compensating actions) with the Raise DSL (typed error handling). Each step records a compensation, and errors trigger compensations in reverse order.',
    codeSnippet: 'suspend fun Raise<OrderError>.processOrder(order: Order): OrderResult = saga {\n  // Each step has a compensation\n  val reserved = saga(\n    { reserveInventory(order.items) },\n    { ids -> releaseInventory(ids) }\n  )\n\n  val charged = saga(\n    { chargePayment(order.payment) },\n    { txId -> refundPayment(txId) }\n  )\n\n  // If this fails, charged is refunded, then reserved is released\n  val shipped = saga(\n    { shipOrder(order, reserved) },\n    { trackingId -> cancelShipment(trackingId) }\n  )\n\n  OrderResult(reserved, charged, shipped)\n}',
    tags: ['raise-dsl', 'transactional', 'saga'],
  },
  {
    id: 'kotlin-arrow-l4-033',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement optics for recursive data structures like trees?',
    options: [
      'Flatten the tree first',
      'Use reflection',
      'Use recursive Traversal composition to focus on nodes at any depth',
      'Optics do not work with recursive types',
    ],
    correctAnswer: 2,
    explanation: 'For recursive types, define a Traversal that recursively descends into children. Arrow can generate optics for the direct fields; you compose them with a recursive function for deep traversal.',
    codeSnippet: '@optics data class TreeNode(\n  val value: String,\n  val children: List<TreeNode>\n) { companion object }\n\n// Recursive traversal of all nodes\nfun allNodes(): Traversal<TreeNode, TreeNode> = object : Traversal<TreeNode, TreeNode> {\n  override fun modify(source: TreeNode, map: (TreeNode) -> TreeNode): TreeNode {\n    val modified = map(source)\n    return modified.copy(\n      children = modified.children.map { allNodes().modify(it, map) }\n    )\n  }\n  // ... foldMap similarly recursive\n}\n\n// Modify all node values\nallNodes().value.modify(root) { it.uppercase() }',
    tags: ['optics', 'recursive', 'tree'],
  },
  {
    id: 'kotlin-arrow-l4-034',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the Curry-Howard correspondence and its relevance to typed error handling?',
    options: [
      'A performance benchmark',
      'A compression algorithm',
      'A way to curry functions',
      'Types correspond to propositions and programs to proofs; Either<A, B> corresponds to logical disjunction (A OR B)',
    ],
    correctAnswer: 3,
    explanation: 'Curry-Howard says types = propositions, programs = proofs. Either<Error, Value> means "either there is an error OR a value" (disjunction). A function A -> B means "A implies B." This gives logical backing to type-safe error handling.',
    codeSnippet: '// Either<E, A> ~ E ∨ A (disjunction: error OR success)\n// Pair<A, B> ~ A ∧ B (conjunction: A AND B)\n// (A) -> B ~ A → B (implication: A implies B)\n// Nothing ~ ⊥ (false/impossible)\n// Unit ~ ⊤ (true/trivially satisfiable)\n\n// A function returning Either<E, A> proves:\n// "given these inputs, either an error of type E occurs,\n//  or a value of type A is produced"\nfun Raise<UserError>.findUser(id: UserId): User',
    tags: ['functional-patterns', 'curry-howard', 'theory'],
  },
  {
    id: 'kotlin-arrow-l4-035',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a distributed semaphore pattern using Arrow primitives?',
    options: [
      'Using Atomic or STM to implement a cooperative semaphore that works with structured concurrency',
      'Using java.util.concurrent.Semaphore',
      'Using file locks',
      'Distributed semaphores require external systems only',
    ],
    correctAnswer: 0,
    explanation: 'A coroutine-friendly semaphore can be built with Atomic for the permit count and suspend for waiting. Arrow\'s STM provides a natural fit: retry() blocks when no permits are available.',
    codeSnippet: 'class CoroutineSemaphore(permits: Int) {\n  private val available = TVar.new(permits)\n\n  suspend fun acquire() = atomically {\n    val current = available.read()\n    if (current <= 0) retry() // suspend until permits available\n    available.write(current - 1)\n  }\n\n  suspend fun release() = atomically {\n    available.modify { it + 1 }\n  }\n\n  suspend fun <A> withPermit(block: suspend () -> A): A {\n    acquire()\n    return try { block() } finally { release() }\n  }\n}',
    tags: ['arrow-fx', 'semaphore', 'stm'],
  },
  {
    id: 'kotlin-arrow-l4-036',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Schedule',
    question: 'How do you implement adaptive retry policies that change behavior based on error patterns?',
    options: [
      'Use machine learning',
      'Use Schedule.fold to accumulate error statistics and adjust strategy via contramap/doWhile',
      'Use a fixed retry policy',
      'Adaptive policies are not possible',
    ],
    correctAnswer: 1,
    explanation: 'Schedule.fold accumulates state across retries. You can use this state to implement adaptive behavior: increase delays for repeated same-type errors, back off harder after timeout errors, or stop after seeing non-retryable patterns.',
    codeSnippet: 'data class RetryState(\n  val totalErrors: Int = 0,\n  val consecutiveTimeouts: Int = 0,\n  val lastErrorType: String = ""\n)\n\nval adaptivePolicy = Schedule.identity<Throwable>()\n  .fold(RetryState()) { state, error ->\n    state.copy(\n      totalErrors = state.totalErrors + 1,\n      consecutiveTimeouts = if (error is TimeoutException)\n        state.consecutiveTimeouts + 1 else 0,\n      lastErrorType = error::class.simpleName ?: ""\n    )\n  }\n  .doWhile { _, (state, _) ->\n    state.totalErrors < 10 && state.consecutiveTimeouts < 3\n  }',
    tags: ['schedule', 'adaptive', 'error-patterns'],
  },
  {
    id: 'kotlin-arrow-l4-037',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the connection between recursion schemes and Arrow optics?',
    options: [
      'Recursion schemes are a subset of optics',
      'Both decompose complex operations over data structures; recursion schemes (cata/ana/hylo) generalize folds over recursive types as optics generalize access to fields',
      'No connection',
      'Optics replace recursion schemes',
    ],
    correctAnswer: 1,
    explanation: 'Both are structure-aware abstractions. Optics abstract over accessing parts of data structures. Recursion schemes (catamorphism, anamorphism, hylomorphism) abstract over recursive traversal patterns. Together they provide comprehensive data manipulation.',
    codeSnippet: '// Catamorphism (fold): tear down recursive structure\n// F-Algebra: F<A> -> A\nfun <A> cata(tree: Tree<Int>, alg: (TreeF<Int, A>) -> A): A\n\n// Anamorphism (unfold): build up recursive structure\n// F-Coalgebra: A -> F<A>\nfun <A> ana(seed: A, coalg: (A) -> TreeF<Int, A>): Tree<Int>\n\n// Hylomorphism: unfold then fold (no intermediate structure)\nfun <A, B> hylo(seed: A, coalg: (A) -> F<A>, alg: (F<B>) -> B): B',
    tags: ['functional-patterns', 'recursion-schemes', 'category-theory'],
  },
  {
    id: 'kotlin-arrow-l4-038',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement scoped error handling where inner scopes can handle a subset of errors?',
    options: [
      'Use global error handlers',
      'Error scoping is not supported',
      'Use nested either { } blocks with different error types and recover for error subsumption',
      'Use nested try-catch',
    ],
    correctAnswer: 2,
    explanation: 'Nest either { } blocks with different error types. Inner blocks handle specific errors, outer blocks handle broader error types. Use recover to catch inner errors and either re-raise or recover.',
    codeSnippet: 'fun Raise<AppError>.processRequest(req: Request): Response {\n  // Inner scope handles ValidationError specifically\n  val validated = recover({\n    // This scope: Raise<ValidationError>\n    validateRequest(req)\n  }) { validationError ->\n    // Recover some validation errors\n    when (validationError) {\n      is ValidationError.MissingField ->\n        applyDefaults(req) // recover with defaults\n      is ValidationError.InvalidFormat ->\n        raise(AppError.BadRequest(validationError.message)) // re-raise as AppError\n    }\n  }\n\n  // Outer scope: Raise<AppError>\n  return withError(AppError::DatabaseError) {\n    saveAndRespond(validated)\n  }\n}',
    tags: ['raise-dsl', 'scoped-errors', 'nested'],
  },
  {
    id: 'kotlin-arrow-l4-039',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'What is the relationship between van Laarhoven lenses and profunctor optics in terms of composability?',
    options: [
      'Profunctor optics do not compose',
      'Both compose via function composition, but profunctor optics use stronger type constraints to distinguish optic types, providing cleaner type inference',
      'They require different composition operators',
      'Van Laarhoven is better',
    ],
    correctAnswer: 1,
    explanation: 'Both encodings compose via function composition. Van Laarhoven uses Functor/Applicative constraints; profunctor optics use profunctor class constraints (Strong for Lens, Choice for Prism). Profunctor encoding gives better type inference and error messages.',
    codeSnippet: '// Van Laarhoven: different constraints per optic type\n// Lens: forall f. Functor f => (a -> f b) -> s -> f t\n// Prism: forall f. Applicative f => (a -> f b) -> s -> f t\n\n// Profunctor: different profunctor constraints\n// Lens: forall p. Strong p => p a b -> p s t\n// Prism: forall p. Choice p => p a b -> p s t\n\n// Both compose via (.): same mechanism, different constraints',
    tags: ['optics', 'van-laarhoven', 'profunctor'],
  },
  {
    id: 'kotlin-arrow-l4-040',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the Zipper data structure pattern for efficient tree navigation?',
    options: [
      'A cursor that tracks the current focus and the context (parent, siblings), enabling O(1) local navigation and modification',
      'Using a compressed file format',
      'Using zip/unzip operations',
      'Using Iterator pattern',
    ],
    correctAnswer: 0,
    explanation: 'A Zipper decomposes a data structure into a focus (current element) and a context (everything else needed to reconstruct the full structure). This enables efficient local navigation and modification of immutable structures.',
    codeSnippet: 'data class TreeZipper<A>(\n  val focus: Tree<A>,\n  val context: List<TreeContext<A>> // breadcrumb trail\n) {\n  data class TreeContext<A>(\n    val parent: A,\n    val leftSiblings: List<Tree<A>>,\n    val rightSiblings: List<Tree<A>>\n  )\n\n  fun goLeft(): TreeZipper<A>? = ...\n  fun goRight(): TreeZipper<A>? = ...\n  fun goUp(): TreeZipper<A>? = ...\n  fun goDown(): TreeZipper<A>? = ...\n\n  fun modify(f: (A) -> A): TreeZipper<A> =\n    copy(focus = focus.map(f)) // O(1) modification at focus\n\n  fun toTree(): Tree<A> = // reconstruct from context\n    context.fold(focus) { tree, ctx -> ... }\n}',
    tags: ['functional-patterns', 'zipper', 'tree-navigation'],
  },
  {
    id: 'kotlin-arrow-l4-041',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you handle the "thundering herd" problem in Arrow applications?',
    options: [
      'Combine jittered retry, circuit breaker, and request deduplication to prevent simultaneous retries',
      'Thundering herd is not a real problem',
      'Using a single thread',
      'Using sleep between retries',
    ],
    correctAnswer: 0,
    explanation: 'The thundering herd occurs when many clients retry simultaneously after a service recovers. Mitigate with: (1) jittered delays to spread retries, (2) circuit breaker to prevent overwhelming the service, (3) request coalescing/deduplication.',
    codeSnippet: '// 1. Jittered retry\nval retryPolicy = Schedule.exponential<Throwable>(100.milliseconds)\n  .jittered() // random variation\n  .and(Schedule.recurs(5))\n\n// 2. Circuit breaker\nval cb = CircuitBreaker(\n  maxFailures = 10,\n  resetTimeout = 30.seconds\n)\n\n// 3. Request deduplication\nval cache = ConcurrentHashMap<String, Deferred<Result>>()\nsuspend fun deduplicated(key: String, block: suspend () -> Result): Result =\n  coroutineScope {\n    cache.getOrPut(key) {\n      async { block() }\n    }.await()\n  }',
    tags: ['arrow-fx', 'thundering-herd', 'resilience'],
  },
  {
    id: 'kotlin-arrow-l4-042',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you design error types for a hexagonal/ports-and-adapters architecture with Arrow?',
    options: [
      'Use exceptions for infrastructure, Either for domain',
      'Use a single global error type',
      'Use HTTP status codes as error types',
      'Each layer defines its own error type; ports define domain errors; adapters translate infrastructure errors to domain errors',
    ],
    correctAnswer: 3,
    explanation: 'In hexagonal architecture: domain defines domain errors, ports (interfaces) use domain errors in their signatures, adapters catch infrastructure errors and translate them to domain errors using withError or mapLeft.',
    codeSnippet: '// Domain layer: pure error types\nsealed interface OrderError {\n  data class NotFound(val id: OrderId) : OrderError\n  data class InvalidTransition(val from: State, val to: State) : OrderError\n}\n\n// Port: uses domain errors\ninterface OrderRepository {\n  suspend fun Raise<OrderError>.findById(id: OrderId): Order\n}\n\n// Adapter: translates infrastructure errors\nclass PostgresOrderRepo(val db: Database) : OrderRepository {\n  override suspend fun Raise<OrderError>.findById(id: OrderId): Order {\n    val row = catch({ db.query("...") }) { e ->\n      raise(OrderError.InfrastructureFailure(e.message ?: "DB error"))\n    }\n    return row?.toDomain() ?: raise(OrderError.NotFound(id))\n  }\n}',
    tags: ['typed-errors', 'hexagonal-architecture', 'clean-architecture'],
  },
  {
    id: 'kotlin-arrow-l4-043',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of "effect polymorphism" and how does Kotlin/Arrow achieve it?',
    options: [
      'Using polymorphic exception handlers',
      'Functions that are polymorphic over their effect system (sync/async, pure/effectful) via suspend and Raise generics',
      'Runtime effect selection',
      'Using dynamic dispatch',
    ],
    correctAnswer: 1,
    explanation: 'Effect polymorphism means writing functions that work regardless of the specific effect system. In Kotlin/Arrow: suspend abstracts over sync/async, Raise<E> abstracts over error handling, and interfaces abstract over specific implementations.',
    codeSnippet: '// Effect polymorphic function: works with any error handler\nfun <E> Raise<E>.validate(\n  value: Int,\n  error: () -> E\n): Int {\n  ensure(value >= 0, error)\n  return value\n}\n\n// Used in either context\neither<String, Int> { validate(5) { "negative" } }\n\n// Used in nullable context\nnullable<Int> { validate(5) { null } }\n\n// Used in result context\nresult<Int> { validate(5) { Exception("negative") } }',
    tags: ['functional-patterns', 'effect-polymorphism', 'abstraction'],
  },
  {
    id: 'kotlin-arrow-l4-044',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'What are the trade-offs between STM and traditional locking for concurrent state management?',
    options: [
      'Locks are always better',
      'STM is always better',
      'They are equivalent in all respects',
      'STM: composable, no deadlocks, optimistic concurrency but may have livelock and overhead from retries; Locks: lower overhead for simple cases but risk deadlock and are not composable',
    ],
    correctAnswer: 3,
    explanation: 'STM advantages: composability (compose atomic operations into larger atomic operations), deadlock freedom, simpler reasoning. STM disadvantages: retry overhead, potential livelock, overhead for low-contention scenarios. Use STM for complex invariants, locks for simple hot paths.',
    tags: ['stm', 'trade-offs', 'concurrency'],
  },
  {
    id: 'kotlin-arrow-l4-045',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement the "finally tagless" pattern with multiple effects in Kotlin?',
    options: [
      'Using a single monolithic interface',
      'Define each effect as a separate interface; combine them using interface delegation or multiple receivers',
      'Using AOP',
      'Using multiple inheritance',
    ],
    correctAnswer: 1,
    explanation: 'Each effect capability is a separate interface. Functions declare which capabilities they need. Implementations provide all capabilities. This is the Kotlin equivalent of Haskell\'s MTL (Monad Transformer Library) style.',
    codeSnippet: '// Effect interfaces (capabilities)\ninterface Logging {\n  suspend fun log(msg: String)\n}\n\ninterface UserStore {\n  suspend fun Raise<UserError>.find(id: UserId): User\n  suspend fun Raise<UserError>.save(user: User): UserId\n}\n\ninterface EmailSender {\n  suspend fun Raise<EmailError>.send(to: String, body: String)\n}\n\n// Function declares needed capabilities\nsuspend fun Raise<AppError>.registerUser(\n  store: UserStore,\n  emailer: EmailSender,\n  logger: Logging,\n  input: RegistrationInput\n): UserId {\n  logger.log("Registering user")\n  val user = createUser(input)\n  val id = with(store) { save(user) }\n  with(emailer) { send(user.email, "Welcome!") }\n  return id\n}',
    tags: ['functional-patterns', 'tagless-final', 'multi-effect'],
  },
  {
    id: 'kotlin-arrow-l4-046',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement optics for sum types with shared fields across variants?',
    options: [
      'Use runtime type checking',
      'Shared fields are not possible with optics',
      'Use an Optional or Traversal that focuses on the shared field across all variants',
      'Duplicate the lens for each variant',
    ],
    correctAnswer: 2,
    explanation: 'When multiple variants share a field, create an Optional or Traversal that pattern-matches each variant. This provides a single optic that can read/modify the shared field regardless of which variant is present.',
    codeSnippet: '@optics sealed interface Shape {\n  data class Circle(val color: String, val radius: Double) : Shape\n  data class Square(val color: String, val side: Double) : Shape\n  data class Triangle(val color: String, val base: Double) : Shape\n  companion object\n}\n\n// Custom optic for shared \'color\' field\nval shapeColor: Lens<Shape, String> = Lens(\n  get = { shape -> when (shape) {\n    is Shape.Circle -> shape.color\n    is Shape.Square -> shape.color\n    is Shape.Triangle -> shape.color\n  }},\n  set = { shape, color -> when (shape) {\n    is Shape.Circle -> shape.copy(color = color)\n    is Shape.Square -> shape.copy(color = color)\n    is Shape.Triangle -> shape.copy(color = color)\n  }}\n)',
    tags: ['optics', 'sum-type', 'shared-fields'],
  },
  {
    id: 'kotlin-arrow-l4-047',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a "bulkhead" pattern for fault isolation using Arrow?',
    options: [
      'Using database partitioning',
      'Using Docker containers',
      'Using physical server separation',
      'Using separate coroutine scopes with limited concurrency to isolate failures between different subsystems',
    ],
    correctAnswer: 3,
    explanation: 'A bulkhead isolates failure domains. Implement by giving each subsystem its own coroutine scope and semaphore/concurrency limit. If one subsystem exhausts its resources, others remain unaffected.',
    codeSnippet: 'class Bulkhead(\n  val name: String,\n  private val maxConcurrency: Int\n) {\n  private val semaphore = Semaphore(maxConcurrency)\n\n  suspend fun <A> execute(block: suspend () -> A): A =\n    semaphore.withPermit {\n      block()\n    }\n}\n\n// Each subsystem has its own bulkhead\nval dbBulkhead = Bulkhead("database", maxConcurrency = 20)\nval apiBulkhead = Bulkhead("external-api", maxConcurrency = 10)\n\nsuspend fun handleRequest(): Response {\n  val user = dbBulkhead.execute { fetchUser() }\n  val enriched = apiBulkhead.execute { enrichFromApi(user) }\n  return Response(enriched)\n}',
    tags: ['arrow-fx', 'bulkhead', 'fault-isolation'],
  },
  {
    id: 'kotlin-arrow-l4-048',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement a Raise-based middleware pipeline?',
    options: [
      'Using annotation-based interceptors',
      'Middleware is not compatible with FP',
      'Using servlet filters',
      'Chain middleware functions as Raise-context transformations that can short-circuit or modify the context',
    ],
    correctAnswer: 3,
    explanation: 'Each middleware is a function with a Raise context that can inspect/modify the request, short-circuit with an error, or pass through to the next middleware. The pipeline is composed via function chaining.',
    codeSnippet: 'typealias Middleware = suspend Raise<ApiError>.(Request, suspend Raise<ApiError>.(Request) -> Response) -> Response\n\nval authMiddleware: Middleware = { request, next ->\n  val token = request.header("Authorization")\n    ?: raise(ApiError.Unauthorized("Missing token"))\n  val user = validateToken(token)\n  next(request.withUser(user))\n}\n\nval loggingMiddleware: Middleware = { request, next ->\n  val start = System.currentTimeMillis()\n  val response = next(request)\n  log("${request.method} ${request.path} -> ${response.status} in ${System.currentTimeMillis() - start}ms")\n  response\n}\n\n// Compose middlewares\nfun compose(vararg middlewares: Middleware): Middleware = { request, handler ->\n  middlewares.foldRight(handler) { mw, next ->\n    { req -> mw(req, next) }\n  }(request)\n}',
    tags: ['raise-dsl', 'middleware', 'pipeline'],
  },
  {
    id: 'kotlin-arrow-l4-049',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "expression problem" in the context of Arrow optics vs. sealed class hierarchies?',
    options: [
      'Adding new optic operations is easy (new function), adding new data variants requires updating all optics/pattern matches',
      'A memory allocation problem',
      'An operator precedence problem',
      'A parsing problem',
    ],
    correctAnswer: 0,
    explanation: 'With sealed classes: adding operations (new functions with when) is easy. Adding variants requires updating all existing pattern matches. Optics help by abstracting access patterns, but sealed class exhaustiveness still requires updates when adding variants.',
    codeSnippet: '// Adding operation: easy, no existing code changes\nfun Shape.perimeter(): Double = when (this) {\n  is Shape.Circle -> 2 * PI * radius\n  is Shape.Square -> 4 * side\n  // must handle all variants\n}\n\n// Adding variant: must update ALL when expressions\n// Adding Shape.Pentagon requires updating area(), perimeter(), draw(), etc.',
    tags: ['functional-patterns', 'expression-problem', 'sealed-class'],
  },
  {
    id: 'kotlin-arrow-l4-050',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How do you implement a multi-version concurrency control (MVCC) pattern using STM?',
    options: [
      'MVCC requires database support only',
      'MVCC and STM are incompatible',
      'Use TVar to hold versioned snapshots, allowing reads from consistent snapshots while writes create new versions',
      'Use timestamps for conflict resolution',
    ],
    correctAnswer: 2,
    explanation: 'STM naturally provides MVCC-like semantics: each transaction sees a consistent snapshot of TVars. Writes are buffered and only committed if no conflicts occurred. You can build explicit versioning on top for application-level MVCC.',
    codeSnippet: 'data class Versioned<A>(val version: Long, val value: A)\n\nclass VersionedStore<K, V> {\n  private val store = TVar.new(mapOf<K, Versioned<V>>())\n  private val versionCounter = TVar.new(0L)\n\n  fun STM.get(key: K): Versioned<V>? =\n    store.read()[key]\n\n  fun STM.put(key: K, value: V, expectedVersion: Long): Boolean {\n    val current = store.read()\n    val existing = current[key]\n    if (existing != null && existing.version != expectedVersion) return false\n    val newVersion = versionCounter.read() + 1\n    versionCounter.write(newVersion)\n    store.write(current + (key to Versioned(newVersion, value)))\n    return true\n  }\n}',
    tags: ['stm', 'mvcc', 'versioning'],
  },
  {
    id: 'kotlin-arrow-l4-051',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a correct and resource-safe fan-out/fan-in pattern with Arrow?',
    options: [
      'Using ExecutorService',
      'Using multiple threads and shared mutable state',
      'Using CompletableFuture',
      'Using Channel for distribution, Resource for lifecycle, and parMap for parallel processing with proper cancellation',
    ],
    correctAnswer: 3,
    explanation: 'Fan-out distributes work across multiple processors; fan-in collects results. Use Channel for work distribution, parMap with concurrency control for processing, and Resource to ensure channels and processors are properly cleaned up.',
    codeSnippet: 'suspend fun <A, B> fanOutFanIn(\n  inputs: List<A>,\n  workers: Int,\n  process: suspend (A) -> B\n): List<B> = coroutineScope {\n  val inputChannel = Channel<IndexedValue<A>>(Channel.BUFFERED)\n  val outputChannel = Channel<IndexedValue<B>>(Channel.BUFFERED)\n\n  // Fan-out: distribute work\n  launch {\n    inputs.withIndex().forEach { inputChannel.send(it) }\n    inputChannel.close()\n  }\n\n  // Workers: parallel processing\n  repeat(workers) {\n    launch {\n      for ((index, item) in inputChannel) {\n        outputChannel.send(IndexedValue(index, process(item)))\n      }\n    }\n  }\n\n  // Fan-in: collect results (maintain order)\n  val results = Array<Any?>(inputs.size) { null }\n  repeat(inputs.size) {\n    val (index, value) = outputChannel.receive()\n    results[index] = value\n  }\n  results.map { it as B }\n}',
    tags: ['arrow-fx', 'fan-out-fan-in', 'channel'],
  },
  {
    id: 'kotlin-arrow-l4-052',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement compile-time enforcement that all error types are handled?',
    options: [
      'Using sealed error hierarchies with when expressions and fold, which the compiler checks for exhaustiveness',
      'It is not possible in Kotlin',
      'Using runtime assertions',
      'Using lint rules',
    ],
    correctAnswer: 0,
    explanation: 'Sealed classes/interfaces provide compile-time exhaustiveness checking when used in when expressions. Combined with fold on Either, the compiler ensures every error subtype is handled. Adding a new error subtype causes compilation errors everywhere it is not handled.',
    codeSnippet: 'sealed interface AppError {\n  data class NotFound(val id: String) : AppError\n  data class Unauthorized(val msg: String) : AppError\n  data class ServerError(val cause: Throwable) : AppError\n  // Adding a new subtype here causes compile errors\n  // in ALL when expressions that handle AppError\n}\n\nfun handleError(error: AppError): Response = when (error) {\n  is AppError.NotFound -> Response(404, "Not found: ${error.id}")\n  is AppError.Unauthorized -> Response(401, error.msg)\n  is AppError.ServerError -> Response(500, "Internal error")\n  // Compiler error if any case is missing!\n}',
    tags: ['raise-dsl', 'exhaustive', 'compile-time-safety'],
  },
  {
    id: 'kotlin-arrow-l4-053',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "expression problem" resolution using object algebras?',
    options: [
      'The expression problem has no resolution',
      'Using abstract factories',
      'Define operations as methods on a generic factory interface; new operations = new interfaces; new variants = new factories',
      'Using the Visitor pattern',
    ],
    correctAnswer: 2,
    explanation: 'Object algebras resolve the expression problem by representing both operations and data as interface methods. New operations are new interfaces (extends existing). New variants are new factory methods. Both dimensions are extensible.',
    codeSnippet: '// Object algebra: operations as factory interface\ninterface ExprAlg<E> {\n  fun lit(n: Int): E\n  fun add(a: E, b: E): E\n}\n\n// Evaluator implementation\nclass Eval : ExprAlg<Int> {\n  override fun lit(n: Int) = n\n  override fun add(a: Int, b: Int) = a + b\n}\n\n// Extension: new operation (no existing code changes)\nclass PrettyPrint : ExprAlg<String> {\n  override fun lit(n: Int) = "$n"\n  override fun add(a: String, b: String) = "($a + $b)"\n}\n\n// Extension: new variant (new interface)\ninterface MulAlg<E> : ExprAlg<E> {\n  fun mul(a: E, b: E): E\n}',
    tags: ['functional-patterns', 'object-algebra', 'expression-problem'],
  },
  {
    id: 'kotlin-arrow-l4-054',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you use optics to implement undo/redo functionality for immutable state?',
    options: [
      'Optics do not help with undo/redo',
      'Store a complete copy for each state',
      'Use optics for precise state diffs combined with a state history stack',
      'Use mutable state with command pattern',
    ],
    correctAnswer: 2,
    explanation: 'Optics can extract targeted diffs. Combined with a zipper-like history (past states, current, future states), you get efficient undo/redo. Optics focus modifications, so structural sharing minimizes memory usage.',
    codeSnippet: 'data class UndoStack<S>(\n  val past: List<S>,\n  val current: S,\n  val future: List<S>\n) {\n  fun undo(): UndoStack<S>? =\n    past.lastOrNull()?.let {\n      UndoStack(past.dropLast(1), it, listOf(current) + future)\n    }\n\n  fun redo(): UndoStack<S>? =\n    future.firstOrNull()?.let {\n      UndoStack(past + current, it, future.drop(1))\n    }\n\n  // Use optics for targeted modification\n  fun <A> modify(lens: Lens<S, A>, f: (A) -> A): UndoStack<S> =\n    UndoStack(\n      past + current,\n      lens.modify(current, f),\n      emptyList() // clear future on new action\n    )\n}',
    tags: ['optics', 'undo-redo', 'state-management'],
  },
  {
    id: 'kotlin-arrow-l4-055',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of a "higher-kinded type" and why did Arrow move away from emulating it?',
    options: [
      'Kotlin supports HKTs natively',
      'HKTs are only for Haskell',
      'Higher-kinded types are not useful',
      'HKTs abstract over type constructors (List, Either, etc.); Arrow moved away from emulation (Kind<F, A>) to context receivers and interfaces for better ergonomics and performance',
    ],
    correctAnswer: 3,
    explanation: 'Higher-kinded types let you abstract over type constructors (F<_>). Arrow 1.x emulated them with Kind<F, A> wrappers, but this had poor ergonomics and runtime overhead. Arrow 2.x uses interfaces and context receivers instead, achieving similar abstraction more idiomatically.',
    codeSnippet: '// Arrow 1.x: HKT emulation (deprecated)\ninterface Functor<F> {\n  fun <A, B> Kind<F, A>.map(f: (A) -> B): Kind<F, B>\n}\n\n// Arrow 2.x: direct interfaces (current)\n// No HKT emulation needed; use specific types\nfun <A, B> Either<*, A>.map(f: (A) -> B): Either<*, B>\nfun <A, B> List<A>.map(f: (A) -> B): List<B>\n\n// Or use interfaces for abstraction\ninterface Repository<E> {\n  fun Raise<E>.findAll(): List<Entity>\n}',
    tags: ['functional-patterns', 'higher-kinded-types', 'arrow-evolution'],
  },
  {
    id: 'kotlin-arrow-l4-056',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a rate limiter using Arrow primitives?',
    options: [
      'Rate limiting is not possible with Arrow',
      'Using Thread.sleep()',
      'Using a token bucket algorithm with Atomic or STM for thread-safe state, and delay for pacing',
      'Using a Guava RateLimiter only',
    ],
    correctAnswer: 2,
    explanation: 'Implement a token bucket with Atomic for the token count and last refill time. Use coroutine delay for non-blocking waiting when tokens are exhausted. STM can also be used for more complex rate limiting scenarios.',
    codeSnippet: 'class TokenBucket(\n  private val maxTokens: Int,\n  private val refillRate: Duration // one token per refillRate\n) {\n  private val state = Atomic(BucketState(maxTokens, Clock.System.now()))\n\n  data class BucketState(val tokens: Int, val lastRefill: Instant)\n\n  suspend fun acquire() {\n    while (true) {\n      val (oldState, newState) = state.modify { s ->\n        val now = Clock.System.now()\n        val elapsed = now - s.lastRefill\n        val newTokens = (elapsed / refillRate).toInt()\n        val refilled = s.copy(\n          tokens = minOf(maxTokens, s.tokens + newTokens),\n          lastRefill = if (newTokens > 0) now else s.lastRefill\n        )\n        if (refilled.tokens > 0)\n          refilled.copy(tokens = refilled.tokens - 1)\n        else refilled\n      }\n      if (newState.tokens < oldState.tokens) return // got a token\n      delay(refillRate) // wait for refill\n    }\n  }\n}',
    tags: ['arrow-fx', 'rate-limiter', 'token-bucket'],
  },
  {
    id: 'kotlin-arrow-l4-057',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'What are the performance characteristics of the Raise DSL compared to returning Either directly?',
    options: [
      'Raise has near-zero overhead on the happy path; raise() has cost similar to a lightweight exception (suppressed stack trace); comparable to Either for most use cases',
      'Raise is always slower due to exception overhead',
      'They have identical performance',
      'Either is always slower due to wrapping',
    ],
    correctAnswer: 0,
    explanation: 'On the happy path, Raise has virtually zero overhead - values are returned directly without wrapping. When raise() is called, it uses an exception with suppressed stack trace, which is cheap. Either wraps every value in Right. For most applications, the difference is negligible.',
    codeSnippet: '// Raise: zero wrapping on happy path\nfun Raise<Error>.compute(): Int {\n  return 42 // no wrapping, returned as-is\n}\n\n// Either: always wraps\nfun compute(): Either<Error, Int> {\n  return Right(42) // allocation for Right wrapper\n}\n\n// In practice, JVM JIT often optimizes away Either allocations too',
    tags: ['raise-dsl', 'performance', 'benchmarking'],
  },
  {
    id: 'kotlin-arrow-l4-058',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement property-based testing for Arrow optics laws?',
    options: [
      'Optics laws do not need testing',
      'Manual unit tests only',
      'Use integration tests',
      'Use a property testing library (Kotest) with generators for your data types to verify lens/prism laws hold for all inputs',
    ],
    correctAnswer: 3,
    explanation: 'Property-based testing with libraries like Kotest generates random inputs to verify that optic laws (get-set, set-get, set-set for Lens; roundtrip laws for Prism) hold universally, not just for hand-picked examples.',
    codeSnippet: 'class PersonOpticsSpec : StringSpec({\n  val personArb = arbitrary {\n    Person(Arb.string().bind(), Arb.int(0..120).bind())\n  }\n\n  "Lens get-set law" {\n    checkAll(personArb) { person ->\n      Person.name.set(person, Person.name.get(person)) shouldBe person\n    }\n  }\n\n  "Lens set-get law" {\n    checkAll(personArb, Arb.string()) { person, name ->\n      Person.name.get(Person.name.set(person, name)) shouldBe name\n    }\n  }\n\n  "Lens set-set law" {\n    checkAll(personArb, Arb.string(), Arb.string()) { person, a, b ->\n      Person.name.set(Person.name.set(person, a), b) shouldBe\n        Person.name.set(person, b)\n    }\n  }\n})',
    tags: ['functional-patterns', 'property-testing', 'optics-laws'],
  },
  {
    id: 'kotlin-arrow-l4-059',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "classy optics" pattern for error handling?',
    options: [
      'Using Prisms to focus on specific error subtypes within a larger error hierarchy, enabling modular error handling',
      'A deprecated error pattern',
      'Using CSS classes for error display',
      'Using optics for elegant code',
    ],
    correctAnswer: 0,
    explanation: 'Classy optics use Prisms to create modular error handlers. Instead of depending on the full error hierarchy, functions depend only on a Prism for the specific error they can produce. This decouples error producers from the overall error type.',
    codeSnippet: '// Instead of depending on full error type:\n// fun Raise<AppError>.userLogic(): ...\n\n// Depend only on a Prism for your error subtype:\nclass UserService<E>(\n  private val asUserError: Prism<E, UserError>\n) {\n  fun Raise<E>.findUser(id: String): User {\n    val user = repo.find(id)\n      ?: raise(asUserError.reverseGet(UserError.NotFound(id)))\n    return user\n  }\n}\n\n// Wiring: provide the Prism\nval service = UserService(AppError.userError) // generated Prism',
    tags: ['functional-patterns', 'classy-optics', 'modular-errors'],
  },
  {
    id: 'kotlin-arrow-l4-060',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a deadline-aware computation pipeline with Arrow?',
    options: [
      'Deadlines are not supported',
      'Using alarm signals',
      'Thread a deadline through the computation using coroutine context, checking remaining time at each step',
      'Using System.currentTimeMillis() checks',
    ],
    correctAnswer: 2,
    explanation: 'Store the deadline in the coroutine context. Each step checks remaining time and can adapt behavior (skip non-essential work, reduce quality, or fail fast). withTimeout provides automatic cancellation at the deadline.',
    codeSnippet: 'data class Deadline(val instant: Instant) : AbstractCoroutineContextElement(Key) {\n  companion object Key : CoroutineContext.Key<Deadline>\n  val remaining: Duration get() = instant - Clock.System.now()\n  val isExpired: Boolean get() = remaining <= Duration.ZERO\n}\n\nsuspend fun Raise<ApiError>.processWithDeadline(request: Request): Response {\n  val deadline = coroutineContext[Deadline]\n    ?: Deadline(Clock.System.now() + 30.seconds)\n\n  val user = withTimeout(deadline.remaining) {\n    fetchUser(request.userId)\n  }\n\n  // Skip enrichment if less than 5s remaining\n  val enriched = if (deadline.remaining > 5.seconds) {\n    withTimeout(deadline.remaining - 2.seconds) {\n      enrichUser(user)\n    }\n  } else user\n\n  return buildResponse(enriched)\n}',
    tags: ['arrow-fx', 'deadline', 'timeout'],
  },
  {
    id: 'kotlin-arrow-l4-061',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the connection between lenses and derivations/automatic differentiation?',
    options: [
      'They use the same algorithm',
      'No connection',
      'Lenses perform numerical differentiation',
      'Lenses track how changes propagate through data structures, similar to how derivatives track how changes propagate through functions',
    ],
    correctAnswer: 3,
    explanation: 'Both lenses and derivatives describe how local changes propagate through a structure. A lens\'s modify is analogous to the chain rule: composed lenses propagate modifications through nested structures, just as the chain rule composes derivatives through composed functions.',
    tags: ['functional-patterns', 'lenses', 'differentiation'],
  },
  {
    id: 'kotlin-arrow-l4-062',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How do you implement a lock-free skip list using Arrow STM?',
    options: [
      'Use TVars for node links at each level, with STM transactions ensuring consistent updates across levels',
      'STM does not support complex data structures',
      'Use ConcurrentSkipListMap instead',
      'Skip lists require traditional locks',
    ],
    correctAnswer: 0,
    explanation: 'An STM-based skip list uses TVars for the forward pointers at each level. Insert/delete operations atomically update pointers across multiple levels within a single STM transaction, maintaining consistency without locks.',
    codeSnippet: 'class STMSkipList<K : Comparable<K>, V>(private val maxLevel: Int) {\n  class Node<K, V>(\n    val key: K,\n    val value: TVar<V>,\n    val forwards: Array<TVar<Node<K, V>?>> // one per level\n  )\n\n  private val head = Array(maxLevel) { TVar.new<Node<K, V>?>(null) }\n\n  fun STM.find(key: K): V? {\n    var current: Node<K, V>? = null\n    for (level in (maxLevel - 1) downTo 0) {\n      var next = (current?.forwards?.get(level) ?: head[level]).read()\n      while (next != null && next.key < key) {\n        current = next\n        next = next.forwards[level].read()\n      }\n      if (next?.key == key) return next.value.read()\n    }\n    return null\n  }\n}',
    tags: ['stm', 'skip-list', 'data-structure'],
  },
  {
    id: 'kotlin-arrow-l4-063',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement domain event sourcing with the Raise DSL?',
    options: [
      'Using database triggers',
      'Using a message queue directly',
      'Event sourcing requires OOP patterns',
      'Model commands and events as sealed types; command handlers use Raise for validation and emit events on success',
    ],
    correctAnswer: 3,
    explanation: 'Commands are validated in a Raise context that can reject invalid commands with typed errors. Successful commands produce domain events. Events are applied to reconstruct state. This cleanly separates validation from state changes.',
    codeSnippet: 'sealed interface AccountCommand {\n  data class Deposit(val amount: Money) : AccountCommand\n  data class Withdraw(val amount: Money) : AccountCommand\n}\n\nsealed interface AccountEvent {\n  data class Deposited(val amount: Money) : AccountEvent\n  data class Withdrawn(val amount: Money) : AccountEvent\n}\n\nfun Raise<AccountError>.handle(\n  state: AccountState,\n  command: AccountCommand\n): List<AccountEvent> = when (command) {\n  is AccountCommand.Deposit -> {\n    ensure(command.amount > Money.ZERO) { AccountError.InvalidAmount }\n    listOf(AccountEvent.Deposited(command.amount))\n  }\n  is AccountCommand.Withdraw -> {\n    ensure(command.amount <= state.balance) {\n      AccountError.InsufficientFunds(state.balance, command.amount)\n    }\n    listOf(AccountEvent.Withdrawn(command.amount))\n  }\n}\n\nfun apply(state: AccountState, event: AccountEvent): AccountState = when (event) {\n  is AccountEvent.Deposited -> state.copy(balance = state.balance + event.amount)\n  is AccountEvent.Withdrawn -> state.copy(balance = state.balance - event.amount)\n}',
    tags: ['raise-dsl', 'event-sourcing', 'domain-modeling'],
  },
  {
    id: 'kotlin-arrow-l4-064',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the relationship between Arrow\'s approach and algebraic effects systems like OCaml 5 or Koka?',
    options: [
      'Arrow\'s Raise DSL provides a limited form of algebraic effects; full algebraic effects support resumable handlers which Kotlin does not natively support',
      'Algebraic effects are the same as exceptions',
      'No relationship',
      'Arrow fully implements algebraic effects',
    ],
    correctAnswer: 0,
    explanation: 'Algebraic effects separate effect declaration from handling. Arrow\'s Raise is a one-shot effect (raise aborts). Full algebraic effects support resumable handlers (the handler can continue the computation). Kotlin lacks native support for multi-shot continuations needed for full algebraic effects.',
    codeSnippet: '// Arrow\'s Raise: one-shot effect (abort)\nfun Raise<E>.compute(): A {\n  raise(error) // computation aborted, no resumption\n}\n\n// Full algebraic effects (not in Kotlin): resumable\n// effect Ask { fun ask(): Int }\n// handler {\n//   resume(42) // can resume the computation with a value!\n// }',
    tags: ['functional-patterns', 'algebraic-effects', 'comparison'],
  },
  {
    id: 'kotlin-arrow-l4-065',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement a type-safe JSON path using optics?',
    options: [
      'Parse JSON into data classes first',
      'Using string-based path expressions',
      'Model JSON as a sealed type hierarchy and compose optics for type-safe navigation',
      'Using JSONPath library only',
    ],
    correctAnswer: 2,
    explanation: 'Model JSON values as a sealed class (JsObject, JsArray, JsString, etc.). Create optics for each type. Compose them to build type-safe paths that verify at compile time that navigation is valid.',
    codeSnippet: '@optics sealed interface Json {\n  data class JObj(val fields: Map<String, Json>) : Json\n  data class JArr(val elements: List<Json>) : Json\n  data class JStr(val value: String) : Json\n  data class JNum(val value: Double) : Json\n  data object JNull : Json\n  companion object\n}\n\n// Type-safe path: root.users[0].name\nval firstUserName: Optional<Json, String> =\n  Json.jObj compose\n  JObj.fields compose\n  Index.map<String, Json>().index("users") compose\n  Json.jArr compose\n  JArr.elements compose\n  Index.list<Json>().index(0) compose\n  Json.jStr compose\n  JStr.value\n\nval name = firstUserName.getOrNull(json) // type-safe!',
    tags: ['optics', 'json', 'type-safe-navigation'],
  },
  {
    id: 'kotlin-arrow-l4-066',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "free applicative" and when is it preferable to the free monad?',
    options: [
      'Free applicative is faster',
      'Free applicative is deprecated',
      'They are identical',
      'Free applicative allows static analysis of the program structure (inspection without execution) since effects are independent; free monad requires execution to determine next steps',
    ],
    correctAnswer: 3,
    explanation: 'Free applicative represents programs where effects are independent. This allows analyzing the program statically (e.g., listing all queries before executing). Free monad represents sequential dependencies where the next step depends on the previous result.',
    codeSnippet: '// Free Applicative: can inspect without executing\nsealed interface Query<A> {\n  data class GetUser(val id: String) : Query<User>\n  data class GetOrder(val id: String) : Query<Order>\n}\n\n// Can statically extract all queries before execution:\n// [GetUser("123"), GetOrder("456")]\n// Then batch-execute them!\n\n// Free Monad: cannot inspect - must execute to know next query\n// getUser("123").flatMap { user -> getOrder(user.orderId) }\n// Cannot know orderId without executing first query',
    tags: ['functional-patterns', 'free-applicative', 'static-analysis'],
  },
  {
    id: 'kotlin-arrow-l4-067',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a correct double-checked locking pattern functionally with Arrow?',
    options: [
      'Double-checked locking is always an anti-pattern',
      'Use volatile variables',
      'Use synchronized blocks',
      'Use Atomic with compareAndSet for lock-free initialization, or lazy delegates for thread-safe single initialization',
    ],
    correctAnswer: 3,
    explanation: 'Arrow\'s Atomic provides CAS operations for lock-free concurrent initialization. For simple lazy initialization, Kotlin\'s lazy with LazyThreadSafetyMode.PUBLICATION provides double-checked locking semantics. For async, use Mutex or Atomic.',
    codeSnippet: 'class AsyncSingleton<A> {\n  private val value = Atomic<Deferred<A>?>(null)\n\n  suspend fun getOrInit(init: suspend () -> A): A = coroutineScope {\n    val existing = value.get()\n    if (existing != null) return@coroutineScope existing.await()\n\n    val deferred = async { init() }\n    val winner = value.compareAndSet(null, deferred)\n    if (winner) {\n      deferred.await()\n    } else {\n      deferred.cancel()\n      value.get()!!.await()\n    }\n  }\n}',
    tags: ['arrow-fx', 'atomic', 'initialization'],
  },
  {
    id: 'kotlin-arrow-l4-068',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you implement a type-safe error reporting system that preserves error context through multiple layers?',
    options: [
      'Use nested sealed types with cause chains, carrying structured context at each layer',
      'Use exception chaining only',
      'Use string concatenation for error messages',
      'Use logging frameworks',
    ],
    correctAnswer: 0,
    explanation: 'Design error types that carry structured context: the current layer\'s error information plus the original cause. This creates a typed cause chain that can be inspected programmatically, unlike string-based error messages.',
    codeSnippet: '// Each error carries structured context + cause\nsealed interface ApiError {\n  val context: ErrorContext\n\n  data class ValidationFailed(\n    override val context: ErrorContext,\n    val violations: NonEmptyList<Violation>\n  ) : ApiError\n\n  data class ServiceUnavailable(\n    override val context: ErrorContext,\n    val service: String,\n    val cause: InfraError // typed cause chain\n  ) : ApiError\n}\n\ndata class ErrorContext(\n  val requestId: String,\n  val timestamp: Instant,\n  val path: String,\n  val layer: String\n)\n\nfun Raise<ApiError>.handleRequest(req: Request): Response {\n  val ctx = ErrorContext(req.id, now(), req.path, "api")\n  withError({ ApiError.ServiceUnavailable(ctx, "user-service", it) }) {\n    userService.fetch(req.userId) // Raise<InfraError>\n  }\n}',
    tags: ['typed-errors', 'error-context', 'cause-chain'],
  },
  {
    id: 'kotlin-arrow-l4-069',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the relationship between profunctors and Arrow\'s optics implementation?',
    options: [
      'Profunctors replace all other optics',
      'Arrow\'s optics use a concrete encoding but the profunctor perspective influences the composition rules and hierarchy',
      'Profunctors are used for serialization',
      'Arrow does not use profunctors',
    ],
    correctAnswer: 1,
    explanation: 'While Arrow uses concrete classes for optics (Lens, Prism, etc.), the profunctor perspective informs the design: what composes with what and what the result type is. The optics hierarchy mirrors profunctor class constraints.',
    tags: ['functional-patterns', 'profunctor', 'optics-design'],
  },
  {
    id: 'kotlin-arrow-l4-070',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement a type-safe builder pattern using the Raise DSL?',
    options: [
      'Use ensure/ensureNotNull in a Raise context to validate each field, short-circuiting or accumulating on invalid configurations',
      'Using mutable builder classes',
      'Using the Builder annotation',
      'Using optional parameters',
    ],
    correctAnswer: 0,
    explanation: 'Combine data class construction with Raise-based validation. Each field is validated using ensure/ensureNotNull. Use zipOrAccumulate to collect all validation errors. The result is a guaranteed-valid instance or a comprehensive error list.',
    codeSnippet: 'data class Config(\n  val host: String,\n  val port: Int,\n  val maxConnections: Int,\n  val timeout: Duration\n)\n\nfun Raise<NonEmptyList<ConfigError>>.buildConfig(\n  raw: RawConfig\n): Config = zipOrAccumulate(\n  {\n    ensureNotNull(raw.host) { ConfigError.Missing("host") }\n    ensure(raw.host.isNotBlank()) { ConfigError.Invalid("host", "blank") }\n    raw.host\n  },\n  {\n    val port = ensureNotNull(raw.port?.toIntOrNull()) { ConfigError.Invalid("port", "not a number") }\n    ensure(port in 1..65535) { ConfigError.Invalid("port", "out of range") }\n    port\n  },\n  {\n    val max = raw.maxConnections ?: 10\n    ensure(max > 0) { ConfigError.Invalid("maxConnections", "must be positive") }\n    max\n  },\n  {\n    val timeout = raw.timeoutMs?.milliseconds ?: 30.seconds\n    ensure(timeout > Duration.ZERO) { ConfigError.Invalid("timeout", "must be positive") }\n    timeout\n  }\n) { host, port, maxConn, timeout ->\n  Config(host, port, maxConn, timeout)\n}',
    tags: ['raise-dsl', 'builder', 'validation'],
  },
  {
    id: 'kotlin-arrow-l4-071',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement cooperative task scheduling with priorities using Arrow?',
    options: [
      'Using real-time OS features',
      'Using Thread.setPriority()',
      'Use a priority Channel or PriorityQueue backed by Atomic, with coroutine suspension for consumers',
      'Priority scheduling is not possible with coroutines',
    ],
    correctAnswer: 2,
    explanation: 'Implement with a concurrent priority queue using Atomic for thread-safe state. Producers insert with priority. Consumers suspend (using CompletableDeferred or STM retry) until work is available, always taking the highest-priority item.',
    codeSnippet: 'class PriorityScheduler<A : Comparable<A>> {\n  private val queue = Atomic(sortedSetOf<Prioritized<A>>())\n  private val waiting = Channel<Unit>(Channel.CONFLATED)\n\n  data class Prioritized<A>(val priority: Int, val value: A, val id: Long)\n    : Comparable<Prioritized<A>> {\n    override fun compareTo(other: Prioritized<A>) =\n      compareValuesBy(this, other, { -it.priority }, { it.id })\n  }\n\n  suspend fun submit(priority: Int, value: A) {\n    queue.update { it + Prioritized(priority, value, idGen.incrementAndGet()) }\n    waiting.trySend(Unit) // notify consumers\n  }\n\n  suspend fun take(): A {\n    while (true) {\n      val item = queue.modify { set ->\n        if (set.isEmpty()) Pair(set, null)\n        else {\n          val first = set.first()\n          Pair(set - first, first)\n        }\n      }.second\n      if (item != null) return item.value\n      waiting.receive() // suspend until new items\n    }\n  }\n}',
    tags: ['arrow-fx', 'priority-scheduling', 'coroutines'],
  },
  {
    id: 'kotlin-arrow-l4-072',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "Scrap Your Boilerplate" (SYB) pattern and how do optics relate to it?',
    options: [
      'A code generation technique',
      'A generic programming pattern for traversing/transforming data structures without boilerplate; optics provide a type-safe version of the same idea',
      'A testing pattern',
      'A refactoring tool',
    ],
    correctAnswer: 1,
    explanation: 'SYB enables generic traversals over data structures using runtime type information. Arrow optics achieve similar goals with compile-time type safety: Traversal visits many elements, and composed optics navigate complex structures without hand-written boilerplate.',
    tags: ['functional-patterns', 'syb', 'generic-programming'],
  },
  {
    id: 'kotlin-arrow-l4-073',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'STM',
    question: 'How do you handle the "write skew" anomaly in STM?',
    options: [
      'Use locks to prevent write skew',
      'STM naturally prevents write skew because transactions validate ALL reads at commit time, not just written variables',
      'Write skew cannot occur in STM',
      'Write skew is automatically detected and an exception is thrown',
    ],
    correctAnswer: 1,
    explanation: 'Write skew occurs when two transactions read overlapping data and write non-overlapping data, creating an inconsistent state. STM prevents this because ALL TVars read during a transaction are validated at commit time, catching the conflict.',
    codeSnippet: '// Write skew scenario (prevented by STM):\n// Account A: 100, Account B: 100\n// Constraint: A + B >= 100\n\n// Transaction 1: withdraw 100 from A\n// Transaction 2: withdraw 100 from B\n// Both read A + B = 200 >= 100, both proceed\n// Without STM: A=0, B=0 (violation!)\n\n// With STM: one transaction retries because\n// the other committed first, changing the read set\natomically {\n  val a = accountA.read()\n  val b = accountB.read()\n  check(a + b >= 100 + amount) // uses both reads\n  accountA.write(a - amount)\n}',
    tags: ['stm', 'write-skew', 'anomaly-prevention'],
  },
  {
    id: 'kotlin-arrow-l4-074',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement a diffing algorithm using optics?',
    options: [
      'Use reflection-based comparison',
      'Use standard library equals',
      'Optics cannot be used for diffing',
      'Use Traversal to compare focused elements between two versions and generate minimal change sets',
    ],
    correctAnswer: 3,
    explanation: 'Optics can focus on specific parts of complex data structures. By getting values through the same optic from two versions and comparing them, you can generate targeted, minimal diff operations for exactly the changed portions.',
    codeSnippet: 'data class Diff<A>(\n  val path: String,\n  val oldValue: A?,\n  val newValue: A?\n)\n\nfun <S, A> diff(\n  old: S,\n  new: S,\n  optic: Getter<S, A>,\n  path: String\n): Diff<A>? {\n  val oldVal = optic.get(old)\n  val newVal = optic.get(new)\n  return if (oldVal != newVal) Diff(path, oldVal, newVal) else null\n}\n\n// Generate diffs for all fields\nfun diffPerson(old: Person, new: Person): List<Diff<*>> = listOfNotNull(\n  diff(old, new, Person.name, "name"),\n  diff(old, new, Person.age, "age"),\n  diff(old, new, Person.address.city, "address.city")\n)',
    tags: ['optics', 'diffing', 'change-detection'],
  },
  {
    id: 'kotlin-arrow-l4-075',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "Final Encoding" of a GADT (Generalized Algebraic Data Type) in Kotlin?',
    options: [
      'Using runtime type tokens',
      'Using regular sealed classes',
      'Using generic sealed classes where the type parameter constrains the return type, enforcing type-safe expression evaluation',
      'GADTs are not expressible in Kotlin',
    ],
    correctAnswer: 2,
    explanation: 'While Kotlin does not have native GADTs, you can approximate them using sealed classes with type parameters that constrain the output type. This enables type-safe expression evaluation where each variant specifies its result type.',
    codeSnippet: '// GADT-style typed expressions\nsealed interface Expr<A> {\n  data class IntLit(val value: Int) : Expr<Int>\n  data class BoolLit(val value: Boolean) : Expr<Boolean>\n  data class Add(val a: Expr<Int>, val b: Expr<Int>) : Expr<Int>\n  data class IfThenElse<A>(\n    val cond: Expr<Boolean>,\n    val then: Expr<A>,\n    val otherwise: Expr<A>\n  ) : Expr<A>\n}\n\n// Type-safe evaluator (requires unchecked casts in Kotlin)\n@Suppress("UNCHECKED_CAST")\nfun <A> eval(expr: Expr<A>): A = when (expr) {\n  is Expr.IntLit -> expr.value as A\n  is Expr.BoolLit -> expr.value as A\n  is Expr.Add -> (eval(expr.a) + eval(expr.b)) as A\n  is Expr.IfThenElse<*> ->\n    if (eval(expr.cond)) eval(expr.then) else eval(expr.otherwise)\n}',
    tags: ['functional-patterns', 'gadt', 'type-safety'],
  },
  {
    id: 'kotlin-arrow-l4-076',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement a supervision strategy where child failures do not cancel siblings?',
    options: [
      'Using supervisorScope or SupervisorJob combined with Arrow\'s error handling to isolate failures',
      'Using SupervisorJob from kotlinx.coroutines',
      'Catching all exceptions silently',
      'Supervision is not possible with coroutines',
    ],
    correctAnswer: 0,
    explanation: 'supervisorScope or SupervisorJob prevents child failures from propagating to siblings. Combine with Arrow\'s Either for typed error handling: each child can fail independently, and results are collected as Either values.',
    codeSnippet: 'suspend fun fetchAllWithSupervision(\n  ids: List<String>\n): List<Either<Throwable, User>> = supervisorScope {\n  ids.map { id ->\n    async {\n      Either.catch { fetchUser(id) }\n    }\n  }.awaitAll()\n}\n\n// Or using Arrow\'s parMap with supervision:\nsuspend fun fetchAllSupervised(\n  ids: List<String>\n): List<Either<Throwable, User>> =\n  ids.parMap { id ->\n    Either.catch { fetchUser(id) }\n  }',
    tags: ['arrow-fx', 'supervision', 'fault-tolerance'],
  },
  {
    id: 'kotlin-arrow-l4-077',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Raise DSL',
    question: 'How do you implement context-dependent error handling where the same function raises different errors based on calling context?',
    options: [
      'Context-dependent errors are an anti-pattern',
      'Use runtime type checking',
      'Use a single error type for all contexts',
      'Make the function generic in the error type and accept a transformation function',
    ],
    correctAnswer: 3,
    explanation: 'Parameterize the function over the error type. The caller provides a mapping from the internal error to their error type. This allows the same core logic to produce different error types in different contexts.',
    codeSnippet: '// Core logic parameterized over error type\nfun <E> Raise<E>.validateAge(\n  age: Int,\n  toError: (String) -> E\n): Int {\n  ensure(age >= 0) { toError("Age cannot be negative: $age") }\n  ensure(age < 150) { toError("Age is unrealistic: $age") }\n  return age\n}\n\n// Used in API context\nfun Raise<ApiError>.handleRequest(request: Request) {\n  val age = validateAge(request.age) { ApiError.BadRequest(it) }\n}\n\n// Used in batch context\nfun Raise<BatchError>.processBatch(record: Record) {\n  val age = validateAge(record.age) { BatchError.InvalidRecord(record.id, it) }\n}',
    tags: ['raise-dsl', 'context-dependent', 'generic-errors'],
  },
  {
    id: 'kotlin-arrow-l4-078',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the concept of "coherent instances" and why does it matter for type class-based FP?',
    options: [
      'Instances that are thread-safe',
      'A performance optimization',
      'Instances that are serializable',
      'For each type, there should be at most one canonical instance of each type class, ensuring consistent behavior',
    ],
    correctAnswer: 3,
    explanation: 'Coherence means there is a unique, canonical instance of each type class for each type. Without coherence, different code paths might use different Monoid or Eq instances for the same type, leading to subtle bugs.',
    codeSnippet: '// Coherent: one canonical Monoid for Int (addition)\n// Incoherent: multiple Monoid instances for Int\nval addMonoid = object : Monoid<Int> {\n  override fun empty() = 0\n  override fun combine(a: Int, b: Int) = a + b\n}\n\nval mulMonoid = object : Monoid<Int> {\n  override fun empty() = 1\n  override fun combine(a: Int, b: Int) = a * b\n}\n\n// Which one should sum(list) use? Coherence says: pick one canonical\n// Solution: use newtype wrappers for alternatives\n@JvmInline value class Sum(val value: Int)\n@JvmInline value class Product(val value: Int)',
    tags: ['functional-patterns', 'coherence', 'type-classes'],
  },
  {
    id: 'kotlin-arrow-l4-079',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Optics',
    question: 'How do you implement a reactive state management system using Arrow optics and atomic state?',
    options: [
      'Using mutable global variables',
      'Using RxJava',
      'Using LiveData/StateFlow directly',
      'Combine Atomic<AppState> with optics for targeted updates and Flow for reactive observation of specific state slices',
    ],
    correctAnswer: 3,
    explanation: 'Use Atomic for thread-safe state, optics for targeted immutable updates, and StateFlow/Flow for reactive observation. Optics allow subscribing to specific state slices, reducing unnecessary recomposition.',
    codeSnippet: 'class Store<S>(initial: S) {\n  private val _state = MutableStateFlow(initial)\n  val state: StateFlow<S> = _state.asStateFlow()\n\n  fun update(f: (S) -> S) {\n    _state.update(f)\n  }\n\n  // Observe a specific lens-focused slice\n  fun <A> observe(lens: Getter<S, A>): Flow<A> =\n    state.map { lens.get(it) }.distinctUntilChanged()\n\n  // Update via lens\n  fun <A> modify(lens: Lens<S, A>, f: (A) -> A) {\n    update { lens.modify(it, f) }\n  }\n}\n\n// Usage\nval store = Store(AppState(...))\nstore.observe(AppState.user.name).collect { name ->\n  updateUI(name) // only triggers when name changes\n}\nstore.modify(AppState.user.name) { it.uppercase() }',
    tags: ['optics', 'reactive', 'state-management'],
  },
  {
    id: 'kotlin-arrow-l4-080',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'What is the "Dependency Rejection" principle and how does it contrast with Dependency Injection in FP?',
    options: [
      'Using global singletons',
      'Instead of injecting dependencies, push them to the boundary; core logic is pure functions that take values, not services',
      'Rejecting all external dependencies',
      'Removing all interfaces',
    ],
    correctAnswer: 1,
    explanation: 'Dependency Rejection pushes impure dependencies to the application boundary. Core domain logic is pure functions taking plain values. The boundary layer (adapter) handles I/O and maps between domain and infrastructure. This makes the core testable without mocks.',
    codeSnippet: '// Dependency Injection: core depends on service interface\nclass OrderService(val repo: OrderRepository) {\n  suspend fun process(id: OrderId): Either<Error, Order> {\n    val order = repo.findById(id) // depends on repo\n    return validate(order)\n  }\n}\n\n// Dependency Rejection: core is pure, boundary does I/O\nfun Raise<OrderError>.processOrder(order: Order): ProcessedOrder {\n  // Pure logic: no dependencies, no I/O\n  ensure(order.items.isNotEmpty()) { OrderError.EmptyOrder }\n  ensure(order.total > Money.ZERO) { OrderError.InvalidTotal }\n  return ProcessedOrder(order, calculateTax(order))\n}\n\n// Boundary: fetches data and calls pure core\nsuspend fun handleRequest(id: OrderId): Either<Error, ProcessedOrder> = either {\n  val order = repo.findById(id) ?: raise(OrderError.NotFound(id))\n  processOrder(order) // pure function, no mock needed in tests\n}',
    tags: ['functional-patterns', 'dependency-rejection', 'pure-core'],
  },
  {
    id: 'kotlin-arrow-l4-081',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Arrow Fx Coroutines',
    question: 'How do you implement an event-driven architecture with backpressure using Arrow and Flow?',
    options: [
      'Event-driven requires reactive frameworks only',
      'Using a message broker only',
      'Combine Flow for event streams, buffer/conflate for backpressure, parMap for parallel processing, and Resource for lifecycle',
      'Using polling loops',
    ],
    correctAnswer: 2,
    explanation: 'Flow provides the reactive stream. Buffer controls backpressure. parMap enables parallel event processing. Resource manages the lifecycle of event sources and processors. Raise DSL handles typed errors in event handling.',
    codeSnippet: 'fun eventProcessor(): Resource<Unit> = resource {\n  val eventSource = install({ createEventSource() }) { s, _ -> s.close() }\n  val db = install({ connectDb() }) { d, _ -> d.close() }\n\n  eventSource.events()\n    .buffer(1000) // backpressure buffer\n    .mapNotNull { event ->\n      either<ProcessingError, DomainEvent> {\n        validate(event)\n      }.getOrElse { error ->\n        logger.warn("Invalid event: $error")\n        null\n      }\n    }\n    .chunked(100, 1.seconds) // batch for efficiency\n    .collect { batch ->\n      batch.parMap(concurrency = 10) { event ->\n        either { process(db, event) }\n      }\n    }\n}',
    tags: ['arrow-fx', 'event-driven', 'flow'],
  },
  {
    id: 'kotlin-arrow-l4-082',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Typed Errors',
    question: 'How do you design an error system that supports both client-facing errors and internal debugging information?',
    options: [
      'Separate public error types (for clients) from internal error types (with debug context); map at the boundary',
      'Use HTTP status codes only',
      'Log everything and return generic errors',
      'Include all debug info in client errors',
    ],
    correctAnswer: 0,
    explanation: 'Design two error layers: internal errors carry full debug context (stack traces, SQL queries, internal IDs). At the API boundary, map to client-safe errors that only expose user-friendly messages and stable error codes.',
    codeSnippet: '// Internal errors: full context for debugging\nsealed interface InternalError {\n  val debugContext: DebugContext\n  data class DbQueryFailed(\n    override val debugContext: DebugContext,\n    val sql: String,\n    val params: Map<String, Any>,\n    val cause: Throwable\n  ) : InternalError\n}\n\n// Client-safe errors: no internal details\nsealed interface ClientError {\n  val code: String\n  val message: String\n  data class ServiceUnavailable(\n    override val code: String = "SERVICE_UNAVAILABLE",\n    override val message: String = "Please try again later"\n  ) : ClientError\n}\n\n// Boundary mapping\nfun InternalError.toClientError(): ClientError = when (this) {\n  is InternalError.DbQueryFailed -> {\n    logger.error("DB failed", debugContext, sql, cause)\n    ClientError.ServiceUnavailable()\n  }\n}',
    tags: ['typed-errors', 'error-design', 'boundary'],
  },
  {
    id: 'kotlin-arrow-l4-083',
    language: 'kotlin',
    level: 'level4',
    category: 'Arrow & FP',
    subcategory: 'Functional Patterns',
    question: 'How do you implement a compile-time validated configuration system using Arrow and value classes?',
    options: [
      'Use value classes for type-safe config values, validated at construction time using Raise, composed with optics for nested config',
      'Using runtime validation only',
      'Using property files only',
      'Using String properties for all configuration',
    ],
    correctAnswer: 0,
    explanation: 'Value classes provide zero-overhead type safety. Construction via validated smart constructors (using Raise) ensures only valid values exist. Optics enable clean access to nested configuration. The type system prevents misconfiguration at compile time.',
    codeSnippet: '@JvmInline value class Port private constructor(val value: Int) {\n  companion object {\n    fun Raise<ConfigError>.create(raw: Int): Port {\n      ensure(raw in 1..65535) { ConfigError.InvalidPort(raw) }\n      return Port(raw)\n    }\n  }\n}\n\n@JvmInline value class Host private constructor(val value: String) {\n  companion object {\n    fun Raise<ConfigError>.create(raw: String): Host {\n      ensure(raw.isNotBlank()) { ConfigError.BlankHost }\n      ensure(!raw.contains(" ")) { ConfigError.InvalidHost(raw) }\n      return Host(raw)\n    }\n  }\n}\n\n@optics data class ServerConfig(\n  val host: Host,  // impossible to create invalid Host\n  val port: Port,  // impossible to create invalid Port\n  val maxConnections: PositiveInt\n) { companion object }\n\n// Construction validates everything\nfun Raise<NonEmptyList<ConfigError>>.loadConfig(env: Map<String, String>): ServerConfig =\n  zipOrAccumulate(\n    { Host.create(env["HOST"] ?: raise(ConfigError.Missing("HOST"))) },\n    { Port.create(env["PORT"]?.toIntOrNull() ?: raise(ConfigError.Missing("PORT"))) },\n    { PositiveInt.create(env["MAX_CONN"]?.toIntOrNull() ?: 100) }\n  ) { host, port, maxConn -> ServerConfig(host, port, maxConn) }',
    tags: ['functional-patterns', 'value-class', 'validated-config'],
  },
];

export default questions;
