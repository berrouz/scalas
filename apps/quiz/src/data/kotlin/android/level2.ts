import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-android-l2-001',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Activity Lifecycle',
    question: 'What method is called after onStop() when the user navigates back to the Activity?',
    options: ['onStart()', 'onRestart()', 'onCreate()', 'onResume()'],
    correctAnswer: 1,
    explanation: 'onRestart() is called after onStop() when the activity is coming back to interact with the user. After onRestart(), the system calls onStart() and then onResume(). This is distinct from the initial creation flow which goes through onCreate().',
    tags: ['activity', 'lifecycle', 'onrestart'],
  },
  {
    id: 'kotlin-android-l2-002',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Activity Lifecycle',
    question: 'When is onSaveInstanceState() called relative to other lifecycle methods?',
    options: ['After onStop()', 'After onPause() but before onStop()', 'Before onPause()', 'After onStop() on API 28+; before onStop() on earlier versions'],
    correctAnswer: 3,
    explanation: 'On API 28 (Pie) and higher, onSaveInstanceState() is called after onStop(). On earlier API levels, it is called before onStop() with no guarantee whether it occurs before or after onPause(). This change was made to allow Fragment transactions in onStop().',
    tags: ['activity', 'lifecycle', 'saveinstancestate', 'api-level'],
  },
  {
    id: 'kotlin-android-l2-003',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'ViewModel',
    question: 'What happens to a ViewModel when the associated Activity is finished (not due to configuration change)?',
    options: ['onCleared() is called and the ViewModel is destroyed', 'It throws an exception', 'It persists in memory', 'It is cached for later use'],
    correctAnswer: 0,
    explanation: 'When the Activity is finished (user presses back or finish() is called), the ViewModel\'s onCleared() method is called, giving you a chance to clean up resources. The ViewModel is then garbage collected.',
    codeSnippet: 'class MyViewModel : ViewModel() {\n    override fun onCleared() {\n        super.onCleared()\n        // Clean up resources, cancel jobs, etc.\n    }\n}',
    tags: ['viewmodel', 'lifecycle', 'oncleared'],
  },
  {
    id: 'kotlin-android-l2-004',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'ViewModel',
    question: 'How do you pass arguments to a ViewModel constructor?',
    options: ['By calling the constructor directly', 'Using ViewModelProvider.Factory', 'Using Intent extras', 'ViewModels cannot have constructor arguments'],
    correctAnswer: 1,
    explanation: 'To pass arguments to a ViewModel constructor, you need to create a ViewModelProvider.Factory. With Hilt, you can use @HiltViewModel and @Inject constructor. The SavedStateHandle can also be injected for saved state.',
    codeSnippet: 'class MyViewModelFactory(private val repo: Repository) : ViewModelProvider.Factory {\n    override fun <T : ViewModel> create(modelClass: Class<T>): T {\n        return MyViewModel(repo) as T\n    }\n}\n\n// Usage\nval vm: MyViewModel by viewModels { MyViewModelFactory(repo) }',
    tags: ['viewmodel', 'factory', 'constructor'],
  },
  {
    id: 'kotlin-android-l2-005',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'LiveData',
    question: 'What is the difference between setValue() and postValue() on MutableLiveData?',
    options: ['postValue() is deprecated', 'setValue() must be called from the main thread; postValue() can be called from any thread', 'There is no difference', 'setValue() is for primitives; postValue() is for objects'],
    correctAnswer: 1,
    explanation: 'setValue() must be called from the main thread and updates observers synchronously. postValue() can be called from any thread and posts a task to the main thread to set the value. If postValue() is called multiple times before the main thread processes them, only the last value is dispatched.',
    tags: ['livedata', 'setvalue', 'postvalue', 'threading'],
  },
  {
    id: 'kotlin-android-l2-006',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'LiveData',
    question: 'What is Transformations.map() used for with LiveData?',
    options: ['To convert LiveData to a list', 'To combine two LiveData sources', 'To filter LiveData values', 'To apply a function to each value emitted by the source LiveData and return a new LiveData'],
    correctAnswer: 3,
    explanation: 'Transformations.map() creates a new LiveData that applies a given function to each value emitted by the source LiveData. The transformation happens on the main thread and the result is set on the new LiveData.',
    codeSnippet: 'val userName: LiveData<String> = Transformations.map(userLiveData) { user ->\n    "${user.firstName} ${user.lastName}"\n}',
    tags: ['livedata', 'transformations', 'map'],
  },
  {
    id: 'kotlin-android-l2-007',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'LiveData',
    question: 'What is Transformations.switchMap() used for?',
    options: ['To apply a function that returns a new LiveData for each value of the source LiveData', 'To switch between two LiveData sources', 'To map LiveData values to a different type', 'To toggle LiveData on and off'],
    correctAnswer: 0,
    explanation: 'switchMap() is similar to map() but the function returns a LiveData instead of a direct value. When the source LiveData changes, the function is called, and the observer is switched to the newly returned LiveData. This is useful for performing lookups based on an ID.',
    codeSnippet: 'private val userId = MutableLiveData<String>()\nval user: LiveData<User> = userId.switchMap { id ->\n    repository.getUserById(id)\n}',
    tags: ['livedata', 'transformations', 'switchmap'],
  },
  {
    id: 'kotlin-android-l2-008',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'StateFlow in Android',
    question: 'How do you safely collect a StateFlow in an Activity or Fragment?',
    options: ['Using collect() in onCreate()', 'Using a simple coroutine launch', 'Using repeatOnLifecycle() with lifecycleScope to collect only when in a certain lifecycle state', 'Using LiveData.observe()'],
    correctAnswer: 2,
    explanation: 'The safe way to collect flows in UI is using lifecycleScope.launch with repeatOnLifecycle(). This ensures collection only happens when the lifecycle is at least in the specified state, preventing wasted resources when the UI is not visible.',
    codeSnippet: 'lifecycleScope.launch {\n    repeatOnLifecycle(Lifecycle.State.STARTED) {\n        viewModel.uiState.collect { state ->\n            updateUI(state)\n        }\n    }\n}',
    tags: ['stateflow', 'lifecycle', 'collection'],
  },
  {
    id: 'kotlin-android-l2-009',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'StateFlow in Android',
    question: 'What is the difference between StateFlow and SharedFlow?',
    options: ['StateFlow is for UI; SharedFlow is for services', 'StateFlow always has a current value and emits only distinct values; SharedFlow is more configurable with replay and buffer', 'They are identical', 'SharedFlow is deprecated'],
    correctAnswer: 1,
    explanation: 'StateFlow always holds a value, requires an initial value, and only emits when the value changes (conflated). SharedFlow is more flexible: it has no initial value requirement, can replay multiple values, configure buffer sizes, and emit duplicate values.',
    tags: ['stateflow', 'sharedflow', 'comparison'],
  },
  {
    id: 'kotlin-android-l2-010',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'StateFlow in Android',
    question: 'How do you collect StateFlow in Jetpack Compose?',
    options: ['Using remember { flow.value }', 'Using observeAsState()', 'Using collectAsState() or collectAsStateWithLifecycle()', 'Using LaunchedEffect with collect'],
    correctAnswer: 2,
    explanation: 'In Compose, use collectAsState() to convert a StateFlow to Compose State. collectAsStateWithLifecycle() (from lifecycle-runtime-compose) is preferred as it stops collection when the app is in the background.',
    codeSnippet: '@Composable\nfun MyScreen(viewModel: MyViewModel) {\n    val uiState by viewModel.uiState.collectAsStateWithLifecycle()\n    Text(text = uiState.title)\n}',
    tags: ['stateflow', 'compose', 'collectasstate'],
  },
  {
    id: 'kotlin-android-l2-011',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'What is the purpose of the key parameter in LazyColumn items?',
    options: ['It provides a stable identity for items to enable efficient recomposition and animations', 'It sets the item ID for click handling', 'It sorts the items', 'It filters duplicate items'],
    correctAnswer: 0,
    explanation: 'The key parameter provides a stable identity for each item. This allows Compose to efficiently reorder and recompose items when the list changes, and enables proper item animations. Without keys, Compose uses position as identity.',
    codeSnippet: 'LazyColumn {\n    items(\n        items = users,\n        key = { user -> user.id }\n    ) { user ->\n        UserItem(user)\n    }\n}',
    tags: ['compose', 'lazycolumn', 'key', 'performance'],
  },
  {
    id: 'kotlin-android-l2-012',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'remember/mutableStateOf',
    question: 'What is the difference between remember and rememberSaveable?',
    options: ['remember survives recomposition only; rememberSaveable also survives configuration changes and process death', 'There is no difference', 'remember is for Compose; rememberSaveable is for Views', 'rememberSaveable is slower'],
    correctAnswer: 0,
    explanation: 'remember stores values across recompositions but not across configuration changes. rememberSaveable stores values across both recompositions and configuration changes (like rotation) by saving them in the Bundle, similar to onSaveInstanceState.',
    codeSnippet: '// Lost on rotation\nvar count by remember { mutableStateOf(0) }\n\n// Survives rotation\nvar count by rememberSaveable { mutableStateOf(0) }',
    tags: ['compose', 'remember', 'remembersaveable', 'state'],
  },
  {
    id: 'kotlin-android-l2-013',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'State management',
    question: 'What is derivedStateOf in Jetpack Compose?',
    options: ['A state that is computed from other state objects and only recomposes when the derived value changes', 'A way to derive a new composable', 'A debugging tool for state', 'A method to copy state'],
    correctAnswer: 0,
    explanation: 'derivedStateOf creates a State that is derived from a calculation based on other observable states. It caches the result and only triggers recomposition when the derived value actually changes, even if the input states change frequently.',
    codeSnippet: 'val listState = rememberLazyListState()\nval showButton by remember {\n    derivedStateOf {\n        listState.firstVisibleItemIndex > 0\n    }\n}',
    tags: ['compose', 'derivedstateof', 'optimization'],
  },
  {
    id: 'kotlin-android-l2-014',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What is LaunchedEffect used for in Jetpack Compose?',
    options: ['To run a suspend function safely when a composable enters the composition', 'To launch an Activity', 'To launch a new screen', 'To animate composables'],
    correctAnswer: 0,
    explanation: 'LaunchedEffect is a side-effect API that launches a coroutine scoped to the composable. The coroutine is cancelled when the composable leaves the composition. It takes keys that restart the coroutine when they change.',
    codeSnippet: '@Composable\nfun MyScreen(userId: String) {\n    LaunchedEffect(userId) {\n        // This runs when userId changes\n        viewModel.loadUser(userId)\n    }\n}',
    tags: ['compose', 'launchedeffect', 'side-effect'],
  },
  {
    id: 'kotlin-android-l2-015',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What is DisposableEffect in Jetpack Compose?',
    options: ['A method to remove items from a list', 'A way to dispose of composables', 'A side-effect that provides an onDispose callback for cleanup when the composable leaves composition', 'A garbage collection helper'],
    correctAnswer: 2,
    explanation: 'DisposableEffect is for side effects that need cleanup. It provides an onDispose block that runs when the composable leaves the composition or when the key changes, similar to onDestroy or removing a listener.',
    codeSnippet: '@Composable\nfun LifecycleObserverComposable(lifecycle: Lifecycle) {\n    DisposableEffect(lifecycle) {\n        val observer = LifecycleEventObserver { _, event -> /* handle */ }\n        lifecycle.addObserver(observer)\n        onDispose {\n            lifecycle.removeObserver(observer)\n        }\n    }\n}',
    tags: ['compose', 'disposableeffect', 'cleanup'],
  },
  {
    id: 'kotlin-android-l2-016',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Navigation',
    question: 'How do you pass arguments to a destination in Compose Navigation?',
    options: ['Using a shared ViewModel only', 'By defining arguments in the route and using NavBackStackEntry to retrieve them', 'Using Intent extras', 'Using global variables'],
    correctAnswer: 1,
    explanation: 'In Compose Navigation, you define arguments in the route string (like "detail/{id}") and specify them using the arguments parameter. The receiving composable retrieves them from the NavBackStackEntry.',
    codeSnippet: 'NavHost(navController, startDestination = "list") {\n    composable("list") { ListScreen(navController) }\n    composable(\n        "detail/{itemId}",\n        arguments = listOf(navArgument("itemId") { type = NavType.IntType })\n    ) { backStackEntry ->\n        val itemId = backStackEntry.arguments?.getInt("itemId")\n        DetailScreen(itemId)\n    }\n}\n\n// Navigate\nnavController.navigate("detail/42")',
    tags: ['navigation', 'compose', 'arguments'],
  },
  {
    id: 'kotlin-android-l2-017',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Navigation',
    question: 'What does popBackStack() do in the Navigation component?',
    options: ['Pushes a new destination onto the stack', 'Saves the back stack to disk', 'Pops the current destination off the back stack, navigating back', 'Clears the entire back stack'],
    correctAnswer: 2,
    explanation: 'popBackStack() removes the current destination from the navigation back stack, effectively navigating the user back to the previous destination. You can also pop to a specific destination using the overloaded version.',
    codeSnippet: '// Pop current destination\nnavController.popBackStack()\n\n// Pop back to a specific destination\nnavController.popBackStack("home", inclusive = false)',
    tags: ['navigation', 'backstack', 'pop'],
  },
  {
    id: 'kotlin-android-l2-018',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Room Database',
    question: 'How do you handle database migrations in Room?',
    options: ['Migrations are not supported', 'By adding a Migration object that defines how to transform the schema from one version to the next', 'By deleting and recreating the database', 'Room handles it automatically'],
    correctAnswer: 1,
    explanation: 'Room requires you to provide Migration objects that define how to transform the database schema from one version to the next. Each Migration specifies a start version, end version, and the SQL commands to execute.',
    codeSnippet: 'val MIGRATION_1_2 = object : Migration(1, 2) {\n    override fun migrate(database: SupportSQLiteDatabase) {\n        database.execSQL("ALTER TABLE users ADD COLUMN age INTEGER NOT NULL DEFAULT 0")\n    }\n}\n\nRoom.databaseBuilder(context, AppDatabase::class.java, "app.db")\n    .addMigrations(MIGRATION_1_2)\n    .build()',
    tags: ['room', 'migration', 'database'],
  },
  {
    id: 'kotlin-android-l2-019',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Room Database',
    question: 'How can Room return LiveData or Flow from a DAO query?',
    options: ['By using LiveData<> or Flow<> as the return type of the @Query method', 'By wrapping the result manually', 'By using a special annotation', 'It cannot'],
    correctAnswer: 0,
    explanation: 'Room natively supports returning LiveData<List<T>> or Flow<List<T>> from @Query methods. When the underlying data changes, Room automatically notifies the observers/collectors with the updated data.',
    codeSnippet: '@Dao\ninterface UserDao {\n    @Query("SELECT * FROM users")\n    fun getAllUsersLiveData(): LiveData<List<User>>\n\n    @Query("SELECT * FROM users")\n    fun getAllUsersFlow(): Flow<List<User>>\n}',
    tags: ['room', 'livedata', 'flow', 'reactive'],
  },
  {
    id: 'kotlin-android-l2-020',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Room Database',
    question: 'What is a TypeConverter in Room?',
    options: ['A class that tells Room how to convert custom types to and from types that Room can persist', 'A class that converts between Kotlin and Java types', 'A layout inflater', 'A JSON converter'],
    correctAnswer: 0,
    explanation: 'TypeConverters tell Room how to convert custom types (like Date, List, or enums) to primitive types that SQLite can store (like Long, String). You annotate methods with @TypeConverter and register the class with @TypeConverters.',
    codeSnippet: 'class Converters {\n    @TypeConverter\n    fun fromTimestamp(value: Long?): Date? = value?.let { Date(it) }\n\n    @TypeConverter\n    fun dateToTimestamp(date: Date?): Long? = date?.time\n}\n\n@Database(entities = [User::class], version = 1)\n@TypeConverters(Converters::class)\nabstract class AppDatabase : RoomDatabase() { }',
    tags: ['room', 'typeconverter', 'custom-types'],
  },
  {
    id: 'kotlin-android-l2-021',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Retrofit',
    question: 'How do you add query parameters to a Retrofit request?',
    options: ['Using a config file', 'Append them to the URL string', 'Using the @Query annotation on method parameters', 'Using SharedPreferences'],
    correctAnswer: 2,
    explanation: '@Query annotation adds a query parameter to the URL. The parameter name and value are URL-encoded automatically. For optional parameters, use nullable types.',
    codeSnippet: '@GET("users")\nsuspend fun searchUsers(\n    @Query("name") name: String,\n    @Query("page") page: Int,\n    @Query("limit") limit: Int = 20\n): List<User>\n\n// Produces: users?name=John&page=1&limit=20',
    tags: ['retrofit', 'query', 'parameters'],
  },
  {
    id: 'kotlin-android-l2-022',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Retrofit',
    question: 'How do you add headers to a Retrofit request?',
    options: ['Using the manifest file', 'Using SharedPreferences', 'Headers cannot be added', 'Using @Header or @Headers annotations, or an OkHttp Interceptor'],
    correctAnswer: 3,
    explanation: 'You can add headers using @Headers (static headers on the method), @Header (dynamic headers as parameters), or by adding an OkHttp Interceptor for headers that apply to all requests (like auth tokens).',
    codeSnippet: '// Static headers\n@Headers("Accept: application/json")\n@GET("users")\nsuspend fun getUsers(): List<User>\n\n// Dynamic header\n@GET("users")\nsuspend fun getUsers(@Header("Authorization") token: String): List<User>\n\n// Interceptor for all requests\nclient.addInterceptor { chain ->\n    val request = chain.request().newBuilder()\n        .addHeader("Authorization", "Bearer $token")\n        .build()\n    chain.proceed(request)\n}',
    tags: ['retrofit', 'headers', 'interceptor'],
  },
  {
    id: 'kotlin-android-l2-023',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Retrofit',
    question: 'What is the role of a Converter.Factory in Retrofit?',
    options: ['It converts views to JSON', 'It converts layouts to code', 'It converts between API versions', 'It converts HTTP response bodies to Kotlin objects and vice versa'],
    correctAnswer: 3,
    explanation: 'A Converter.Factory (like GsonConverterFactory or MoshiConverterFactory) tells Retrofit how to serialize request bodies to JSON and deserialize JSON response bodies into Kotlin/Java objects.',
    codeSnippet: 'val retrofit = Retrofit.Builder()\n    .baseUrl("https://api.example.com/")\n    .addConverterFactory(GsonConverterFactory.create())\n    .client(okHttpClient)\n    .build()',
    tags: ['retrofit', 'converter', 'gson', 'serialization'],
  },
  {
    id: 'kotlin-android-l2-024',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Hilt/Dagger DI',
    question: 'What annotation is used to mark an Android Application class for Hilt?',
    options: ['@HiltAndroidApp', '@HiltApplication', '@DaggerApplication', '@InjectApp'],
    correctAnswer: 0,
    explanation: '@HiltAndroidApp triggers Hilt\'s code generation and must be placed on your Application class. It serves as the parent component for dependency injection and is required for Hilt to work.',
    codeSnippet: '@HiltAndroidApp\nclass MyApplication : Application() {\n    // Hilt is now set up\n}',
    tags: ['hilt', 'application', 'setup'],
  },
  {
    id: 'kotlin-android-l2-025',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Hilt/Dagger DI',
    question: 'How do you inject dependencies into an Activity using Hilt?',
    options: ['By calling Hilt.inject(this)', 'Using @Component annotation', 'By annotating the Activity with @AndroidEntryPoint and using @Inject on fields', 'Using @Inject on the Activity constructor'],
    correctAnswer: 2,
    explanation: '@AndroidEntryPoint annotation on the Activity (or Fragment) enables Hilt to inject dependencies. You then use @Inject lateinit var on fields. Hilt generates the necessary code to provide the dependencies.',
    codeSnippet: '@AndroidEntryPoint\nclass MainActivity : AppCompatActivity() {\n    @Inject lateinit var analytics: AnalyticsService\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        analytics.logScreenView("main")\n    }\n}',
    tags: ['hilt', 'inject', 'activity'],
  },
  {
    id: 'kotlin-android-l2-026',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Hilt/Dagger DI',
    question: 'What is a Hilt Module and when do you need one?',
    options: ['A class annotated with @Module that provides bindings for types that cannot be constructor-injected', 'An Android library module', 'A feature module', 'A Gradle module'],
    correctAnswer: 0,
    explanation: 'A Hilt Module (annotated with @Module and @InstallIn) is needed when you cannot use constructor injection, such as for interfaces, third-party library classes, or when you need to configure how an instance is created.',
    codeSnippet: '@Module\n@InstallIn(SingletonComponent::class)\nobject NetworkModule {\n    @Provides\n    @Singleton\n    fun provideRetrofit(): Retrofit {\n        return Retrofit.Builder()\n            .baseUrl("https://api.example.com/")\n            .addConverterFactory(GsonConverterFactory.create())\n            .build()\n    }\n}',
    tags: ['hilt', 'module', 'provides'],
  },
  {
    id: 'kotlin-android-l2-027',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'WorkManager',
    question: 'What is the difference between OneTimeWorkRequest and PeriodicWorkRequest?',
    options: ['OneTimeWorkRequest runs once; PeriodicWorkRequest runs repeatedly at specified intervals', 'OneTimeWorkRequest is synchronous; PeriodicWorkRequest is asynchronous', 'There is no difference', 'PeriodicWorkRequest is deprecated'],
    correctAnswer: 0,
    explanation: 'OneTimeWorkRequest executes the work exactly once. PeriodicWorkRequest runs the work repeatedly at specified intervals (minimum 15 minutes). Both support constraints, retry policies, and chaining.',
    codeSnippet: '// One-time work\nval uploadWork = OneTimeWorkRequestBuilder<UploadWorker>()\n    .setConstraints(constraints)\n    .build()\n\n// Periodic work (minimum 15 min interval)\nval syncWork = PeriodicWorkRequestBuilder<SyncWorker>(\n    15, TimeUnit.MINUTES\n).build()\n\nWorkManager.getInstance(context).enqueue(uploadWork)',
    tags: ['workmanager', 'onetime', 'periodic'],
  },
  {
    id: 'kotlin-android-l2-028',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'WorkManager',
    question: 'How do you add constraints to a WorkManager request?',
    options: ['Constraints are not supported', 'Using a Constraints.Builder to define conditions like network availability', 'Using if-else in the Worker class', 'Using manifest permissions'],
    correctAnswer: 1,
    explanation: 'You use Constraints.Builder to specify conditions that must be met before the work runs. Constraints include network type, battery status, storage space, charging state, and device idle state.',
    codeSnippet: 'val constraints = Constraints.Builder()\n    .setRequiredNetworkType(NetworkType.CONNECTED)\n    .setRequiresBatteryNotLow(true)\n    .setRequiresCharging(false)\n    .build()\n\nval workRequest = OneTimeWorkRequestBuilder<UploadWorker>()\n    .setConstraints(constraints)\n    .build()',
    tags: ['workmanager', 'constraints', 'conditions'],
  },
  {
    id: 'kotlin-android-l2-029',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'What is the difference between Dispatchers.IO and Dispatchers.Default?',
    options: ['Dispatchers.Default is for I/O; Dispatchers.IO is for CPU work', 'Dispatchers.IO is for I/O-bound tasks (network, disk); Dispatchers.Default is for CPU-intensive computations', 'They are the same', 'Dispatchers.IO runs on the main thread'],
    correctAnswer: 1,
    explanation: 'Dispatchers.IO is optimized for I/O-bound work (network calls, file operations, database queries) and uses a larger thread pool. Dispatchers.Default is optimized for CPU-intensive work (sorting, parsing, complex calculations) and uses a thread pool sized to the number of CPU cores.',
    tags: ['coroutines', 'dispatchers', 'io', 'default'],
  },
  {
    id: 'kotlin-android-l2-030',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'What does withContext() do in Kotlin coroutines?',
    options: ['Creates a new thread', 'Switches the coroutine to a different dispatcher and suspends until the block completes', 'Creates a new coroutine', 'Cancels the current context'],
    correctAnswer: 1,
    explanation: 'withContext() switches the coroutine execution to a different dispatcher without creating a new coroutine. It suspends the current coroutine, executes the block on the specified dispatcher, and returns the result.',
    codeSnippet: 'viewModelScope.launch {\n    // Main thread\n    val data = withContext(Dispatchers.IO) {\n        // IO thread - fetch data\n        repository.fetchData()\n    }\n    // Back on Main thread\n    _uiState.value = data\n}',
    tags: ['coroutines', 'withcontext', 'dispatcher-switching'],
  },
  {
    id: 'kotlin-android-l2-031',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'What happens if you launch a coroutine in viewModelScope and the ViewModel is cleared?',
    options: ['The coroutine is automatically cancelled', 'The coroutine is paused', 'An exception is thrown', 'The coroutine continues running'],
    correctAnswer: 0,
    explanation: 'viewModelScope is automatically cancelled when the ViewModel\'s onCleared() is called. All coroutines launched in this scope are cancelled, preventing memory leaks and unnecessary work after the ViewModel is no longer needed.',
    tags: ['coroutines', 'viewmodelscope', 'cancellation'],
  },
  {
    id: 'kotlin-android-l2-032',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'RecyclerView',
    question: 'What is DiffUtil in RecyclerView?',
    options: ['A class for comparing view types', 'A layout comparison tool', 'A utility that calculates the difference between two lists and dispatches minimal update operations', 'A debugging utility'],
    correctAnswer: 2,
    explanation: 'DiffUtil calculates the minimal set of updates needed to transform one list into another. It dispatches specific change notifications (insert, remove, move, change) to the adapter, resulting in smooth animations and better performance than notifyDataSetChanged().',
    codeSnippet: 'class UserDiffCallback : DiffUtil.ItemCallback<User>() {\n    override fun areItemsTheSame(oldItem: User, newItem: User): Boolean {\n        return oldItem.id == newItem.id\n    }\n    override fun areContentsTheSame(oldItem: User, newItem: User): Boolean {\n        return oldItem == newItem\n    }\n}',
    tags: ['recyclerview', 'diffutil', 'performance'],
  },
  {
    id: 'kotlin-android-l2-033',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'RecyclerView',
    question: 'What is ListAdapter in RecyclerView?',
    options: ['A RecyclerView.Adapter subclass that uses DiffUtil on a background thread for efficient list updates', 'An adapter for Spinner', 'A deprecated adapter class', 'An adapter for ListView'],
    correctAnswer: 0,
    explanation: 'ListAdapter is a convenience wrapper around RecyclerView.Adapter that uses AsyncListDiffer to compute list differences on a background thread. You just call submitList() with the new list, and it handles diffing and animations automatically.',
    codeSnippet: 'class UserAdapter : ListAdapter<User, UserViewHolder>(UserDiffCallback()) {\n    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): UserViewHolder {\n        val view = LayoutInflater.from(parent.context).inflate(R.layout.item_user, parent, false)\n        return UserViewHolder(view)\n    }\n    override fun onBindViewHolder(holder: UserViewHolder, position: Int) {\n        holder.bind(getItem(position))\n    }\n}\n\n// Update list\nadapter.submitList(newList)',
    tags: ['recyclerview', 'listadapter', 'diffutil'],
  },
  {
    id: 'kotlin-android-l2-034',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Notifications',
    question: 'How do you create an expandable notification in Android?',
    options: ['Using a custom layout only', 'Using NotificationCompat.Builder with a Style like BigTextStyle, BigPictureStyle, or InboxStyle', 'Using a DialogFragment', 'Expandable notifications are not supported'],
    correctAnswer: 1,
    explanation: 'Expandable notifications use NotificationCompat styles. BigTextStyle shows a large block of text, BigPictureStyle shows a large image, InboxStyle shows multiple lines like an email inbox, and MessagingStyle shows conversation-style messages.',
    codeSnippet: 'val notification = NotificationCompat.Builder(this, "channel_id")\n    .setSmallIcon(R.drawable.ic_notification)\n    .setContentTitle("Long message")\n    .setContentText("Summary text")\n    .setStyle(NotificationCompat.BigTextStyle()\n        .bigText("This is a very long text that will be shown when the notification is expanded..."))\n    .build()',
    tags: ['notifications', 'style', 'expandable'],
  },
  {
    id: 'kotlin-android-l2-035',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Notifications',
    question: 'What is a PendingIntent and how is it used with notifications?',
    options: ['A delayed Intent that runs after a timeout', 'An Intent that has not been sent yet', 'An Intent for pending installations', 'A token that grants permission for another app (or the system) to execute a predefined Intent on your behalf'],
    correctAnswer: 3,
    explanation: 'A PendingIntent is a reference to an Intent that will be executed later by another application (like the system notification service). In notifications, it defines what happens when the user taps the notification.',
    codeSnippet: 'val intent = Intent(this, MainActivity::class.java)\nval pendingIntent = PendingIntent.getActivity(\n    this, 0, intent,\n    PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE\n)\n\nval notification = NotificationCompat.Builder(this, "channel_id")\n    .setContentTitle("Title")\n    .setContentIntent(pendingIntent)\n    .setAutoCancel(true)\n    .build()',
    tags: ['notifications', 'pendingintent', 'interaction'],
  },
  {
    id: 'kotlin-android-l2-036',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Permissions',
    question: 'What should you do if shouldShowRequestPermissionRationale() returns true?',
    options: ['Open app settings', 'Ignore the permission', 'Show an explanation UI to the user about why the permission is needed, then request it', 'Request the permission immediately'],
    correctAnswer: 2,
    explanation: 'When shouldShowRequestPermissionRationale() returns true, it means the user has previously denied the permission. You should display a UI explaining why the permission is necessary before requesting it again.',
    codeSnippet: 'when {\n    ContextCompat.checkSelfPermission(this, permission) == PackageManager.PERMISSION_GRANTED -> {\n        // Permission already granted\n    }\n    shouldShowRequestPermissionRationale(permission) -> {\n        // Show explanation, then request\n        showRationaleDialog()\n    }\n    else -> {\n        // Request permission directly\n        requestPermissionLauncher.launch(permission)\n    }\n}',
    tags: ['permissions', 'rationale', 'ux'],
  },
  {
    id: 'kotlin-android-l2-037',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Fragment Lifecycle',
    question: 'What is the correct order of Fragment lifecycle callbacks when a Fragment is added?',
    options: ['onAttach -> onCreate -> onCreateView -> onViewCreated -> onStart -> onResume', 'onCreateView -> onCreate -> onAttach -> onStart -> onResume', 'onCreate -> onCreateView -> onStart -> onResume -> onAttach', 'onStart -> onCreate -> onCreateView -> onResume -> onAttach'],
    correctAnswer: 0,
    explanation: 'The Fragment lifecycle when added: onAttach() (attached to host Activity) -> onCreate() (initialize non-UI) -> onCreateView() (inflate layout) -> onViewCreated() (set up views) -> onStart() (visible) -> onResume() (interactive).',
    tags: ['fragment', 'lifecycle', 'order'],
  },
  {
    id: 'kotlin-android-l2-038',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Fragment Lifecycle',
    question: 'What is a DialogFragment and when should you use it?',
    options: ['A Fragment for full-screen views', 'A deprecated dialog class', 'A Fragment subclass for displaying a dialog window, managing its own lifecycle', 'A Fragment for login screens'],
    correctAnswer: 2,
    explanation: 'DialogFragment is a specialized Fragment that displays a dialog window. It manages the dialog lifecycle properly, handles configuration changes, and is the recommended way to show dialogs instead of using Dialog directly.',
    codeSnippet: 'class ConfirmDialogFragment : DialogFragment() {\n    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog {\n        return MaterialAlertDialogBuilder(requireContext())\n            .setTitle("Confirm")\n            .setMessage("Are you sure?")\n            .setPositiveButton("Yes") { _, _ -> /* confirm */ }\n            .setNegativeButton("No") { _, _ -> dismiss() }\n            .create()\n    }\n}',
    tags: ['fragment', 'dialogfragment', 'dialog'],
  },
  {
    id: 'kotlin-android-l2-039',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'What is the difference between Column and LazyColumn?',
    options: ['Column renders all children immediately; LazyColumn only renders visible items (lazy composition)', 'LazyColumn is slower', 'Column is for text; LazyColumn is for images', 'They behave identically'],
    correctAnswer: 0,
    explanation: 'Column composes and lays out all of its children immediately, which is fine for small lists. LazyColumn only composes the items that are currently visible on screen, making it efficient for large lists. LazyColumn also supports scrolling by default.',
    tags: ['compose', 'column', 'lazycolumn', 'performance'],
  },
  {
    id: 'kotlin-android-l2-040',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'How do you apply a theme in Jetpack Compose?',
    options: ['Using styles.xml', 'Using CSS files', 'Using a MaterialTheme composable that wraps the content and provides colors, typography, and shapes', 'Themes are not supported in Compose'],
    correctAnswer: 2,
    explanation: 'In Compose, theming is done with MaterialTheme (or Material3 MaterialTheme) which provides color schemes, typography, and shapes to all descendant composables through CompositionLocal.',
    codeSnippet: '@Composable\nfun MyAppTheme(content: @Composable () -> Unit) {\n    MaterialTheme(\n        colorScheme = lightColorScheme(\n            primary = Color(0xFF6200EE),\n            secondary = Color(0xFF03DAC5)\n        ),\n        typography = Typography,\n        content = content\n    )\n}',
    tags: ['compose', 'theme', 'materialtheme'],
  },
  {
    id: 'kotlin-android-l2-041',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'State management',
    question: 'What is a UiState pattern commonly used with ViewModel?',
    options: ['A testing pattern', 'A database pattern', 'A sealed class/data class that represents all possible states of the UI (loading, success, error)', 'A navigation pattern'],
    correctAnswer: 2,
    explanation: 'The UiState pattern uses a data class or sealed class to represent all possible UI states in a single object. The ViewModel exposes this as a StateFlow, and the UI observes it. This makes state management predictable and testable.',
    codeSnippet: 'data class HomeUiState(\n    val isLoading: Boolean = false,\n    val items: List<Item> = emptyList(),\n    val error: String? = null\n)\n\nclass HomeViewModel : ViewModel() {\n    private val _uiState = MutableStateFlow(HomeUiState())\n    val uiState: StateFlow<HomeUiState> = _uiState.asStateFlow()\n\n    fun loadItems() {\n        viewModelScope.launch {\n            _uiState.update { it.copy(isLoading = true) }\n            try {\n                val items = repository.getItems()\n                _uiState.update { it.copy(isLoading = false, items = items) }\n            } catch (e: Exception) {\n                _uiState.update { it.copy(isLoading = false, error = e.message) }\n            }\n        }\n    }\n}',
    tags: ['viewmodel', 'uistate', 'pattern', 'stateflow'],
  },
  {
    id: 'kotlin-android-l2-042',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'DataBinding',
    question: 'What is two-way data binding in Android?',
    options: ['Binding data and network calls', 'Binding two activities together', 'A mechanism where changes in the UI automatically update the data model and vice versa', 'Binding data to two views'],
    correctAnswer: 2,
    explanation: 'Two-way data binding allows automatic synchronization between the UI and the data model. Using @={} syntax, changes in an EditText update the underlying variable, and changes to the variable update the EditText.',
    codeSnippet: '<!-- layout.xml -->\n<EditText\n    android:text="@={viewModel.userName}" />\n\n// ViewModel\nval userName = MutableLiveData<String>()',
    tags: ['databinding', 'two-way', 'binding'],
  },
  {
    id: 'kotlin-android-l2-043',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Material Design',
    question: 'What is a BottomSheet in Material Design?',
    options: ['A dialog at the bottom', 'A footer layout', 'A surface that slides up from the bottom of the screen to show additional content', 'A bottom navigation bar'],
    correctAnswer: 2,
    explanation: 'A BottomSheet is a Material Design component that slides up from the bottom edge of the screen. There are standard (persistent) and modal (overlay) variants. In Compose, you can use ModalBottomSheet or BottomSheetScaffold.',
    codeSnippet: 'val sheetState = rememberModalBottomSheetState()\nvar showSheet by remember { mutableStateOf(false) }\n\nif (showSheet) {\n    ModalBottomSheet(\n        onDismissRequest = { showSheet = false },\n        sheetState = sheetState\n    ) {\n        Text("Bottom Sheet Content")\n    }\n}',
    tags: ['material-design', 'bottomsheet', 'compose'],
  },
  {
    id: 'kotlin-android-l2-044',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Material Design',
    question: 'What is a Snackbar and how does it differ from a Toast?',
    options: ['Snackbar is attached to a view and can have actions; Toast is a simple floating message with no interaction', 'Toast supports actions; Snackbar does not', 'Snackbar is deprecated', 'They are identical'],
    correctAnswer: 0,
    explanation: 'Snackbar is a Material Design component that appears at the bottom of the screen, attached to a CoordinatorLayout/Scaffold. It can include an action button and is dismissible. Toast is system-level, cannot have actions, and appears as a floating overlay.',
    codeSnippet: '// Compose Snackbar with Scaffold\nval snackbarHostState = remember { SnackbarHostState() }\n\nScaffold(snackbarHost = { SnackbarHost(snackbarHostState) }) {\n    // Content\n}\n\nscope.launch {\n    snackbarHostState.showSnackbar(\n        message = "Item deleted",\n        actionLabel = "Undo"\n    )\n}',
    tags: ['material-design', 'snackbar', 'toast'],
  },
  {
    id: 'kotlin-android-l2-045',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What is SideEffect in Jetpack Compose?',
    options: ['An animation helper', 'A composable that runs a block of code on every successful recomposition to publish compose state to non-compose code', 'A memory leak', 'A bug in the code'],
    correctAnswer: 1,
    explanation: 'SideEffect is invoked on every successful recomposition. It is used to publish Compose state to objects not managed by Compose, ensuring non-Compose code always has the latest state.',
    codeSnippet: '@Composable\nfun MyScreen(analytics: Analytics) {\n    SideEffect {\n        // Called on every successful recomposition\n        analytics.setUserProperty("screen", "home")\n    }\n}',
    tags: ['compose', 'sideeffect', 'side-effect'],
  },
  {
    id: 'kotlin-android-l2-046',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What is the purpose of produceState in Jetpack Compose?',
    options: ['To produce new composables', 'To convert non-Compose state (like Flow, LiveData, or callback-based APIs) into Compose State', 'To produce animations', 'To generate test data'],
    correctAnswer: 1,
    explanation: 'produceState launches a coroutine scoped to the composition and converts non-Compose state into Compose State. It is useful for loading data from a suspend function and exposing it as State.',
    codeSnippet: '@Composable\nfun loadUser(userId: String): State<User?> {\n    return produceState<User?>(initialValue = null, userId) {\n        value = repository.getUser(userId)\n    }\n}',
    tags: ['compose', 'producestate', 'state-conversion'],
  },
  {
    id: 'kotlin-android-l2-047',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Navigation',
    question: 'What is a deep link in the Navigation component?',
    options: ['A URI that navigates directly to a specific destination within the app', 'A link between fragments', 'A link to deep storage', 'A database reference'],
    correctAnswer: 0,
    explanation: 'A deep link is a URI that navigates directly to a specific screen in your app. The Navigation component supports both implicit deep links (from external URIs) and explicit deep links (from PendingIntents like notifications).',
    codeSnippet: 'composable(\n    "profile/{userId}",\n    deepLinks = listOf(\n        navDeepLink { uriPattern = "https://myapp.com/profile/{userId}" }\n    )\n) { backStackEntry ->\n    ProfileScreen(backStackEntry.arguments?.getString("userId"))\n}',
    tags: ['navigation', 'deeplink', 'uri'],
  },
  {
    id: 'kotlin-android-l2-048',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Intents',
    question: 'What are common implicit Intent actions used in Android?',
    options: ['ACTION_CREATE and ACTION_DELETE', 'ACTION_VIEW, ACTION_SEND, ACTION_DIAL, ACTION_PICK, and ACTION_SENDTO', 'ACTION_MAIN and ACTION_DEFAULT only', 'There are no standard actions'],
    correctAnswer: 1,
    explanation: 'Common implicit Intent actions include: ACTION_VIEW (open a URL or content), ACTION_SEND (share content), ACTION_DIAL (open dialer), ACTION_PICK (pick content like a photo), ACTION_SENDTO (send to a specific address like email).',
    codeSnippet: '// Open a URL\nval viewIntent = Intent(Intent.ACTION_VIEW, Uri.parse("https://example.com"))\n\n// Share text\nval shareIntent = Intent(Intent.ACTION_SEND).apply {\n    type = "text/plain"\n    putExtra(Intent.EXTRA_TEXT, "Check this out!")\n}\nstartActivity(Intent.createChooser(shareIntent, "Share via"))',
    tags: ['intent', 'implicit', 'actions'],
  },
  {
    id: 'kotlin-android-l2-049',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Bundle',
    question: 'What types can be stored in a Bundle?',
    options: ['Only Strings', 'Any Kotlin object', 'Primitives, Strings, Parcelable, Serializable, and arrays/ArrayLists of these types', 'Only Int and String'],
    correctAnswer: 2,
    explanation: 'Bundle supports primitive types (int, float, boolean, etc.), String, CharSequence, Parcelable objects, Serializable objects, and arrays/ArrayLists of these types. Complex objects must implement Parcelable or Serializable.',
    tags: ['bundle', 'types', 'parcelable'],
  },
  {
    id: 'kotlin-android-l2-050',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Bundle',
    question: 'What is Parcelable and why is it preferred over Serializable on Android?',
    options: ['Parcelable is the same as Serializable', 'Serializable is always faster', 'Parcelable is an Android-specific interface that is faster than Serializable because it avoids reflection', 'Parcelable is deprecated'],
    correctAnswer: 2,
    explanation: 'Parcelable is an Android-specific serialization mechanism that is much faster than Java Serializable because it does not use reflection. Kotlin provides the @Parcelize annotation to auto-generate the implementation.',
    codeSnippet: '@Parcelize\ndata class User(\n    val id: Int,\n    val name: String,\n    val email: String\n) : Parcelable\n\n// Pass via Intent\nintent.putExtra("user", user)',
    tags: ['parcelable', 'serialization', 'performance'],
  },
  {
    id: 'kotlin-android-l2-051',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'What is structured concurrency in Kotlin coroutines?',
    options: ['A way to structure UI code', 'Using threads in a structured way', 'A design pattern for databases', 'A principle that coroutines form parent-child hierarchies where parent cancellation cancels children and failures propagate'],
    correctAnswer: 3,
    explanation: 'Structured concurrency means coroutines are organized in a hierarchy. When a parent coroutine/scope is cancelled, all children are cancelled too. If a child fails, the failure propagates to the parent. This prevents leaked coroutines and makes error handling predictable.',
    tags: ['coroutines', 'structured-concurrency', 'scope'],
  },
  {
    id: 'kotlin-android-l2-052',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'How do you run two network calls in parallel using coroutines?',
    options: ['Using runBlocking', 'Using two sequential suspend calls', 'Using async { } for each call and await() on both', 'Parallel calls are not supported'],
    correctAnswer: 2,
    explanation: 'Using async { } launches coroutines concurrently. Each async returns a Deferred that you can await(). By launching both async blocks first and then awaiting both, the calls run in parallel.',
    codeSnippet: 'viewModelScope.launch {\n    val usersDeferred = async { api.getUsers() }\n    val postsDeferred = async { api.getPosts() }\n    \n    val users = usersDeferred.await()\n    val posts = postsDeferred.await()\n    \n    _uiState.value = UiState(users, posts)\n}',
    tags: ['coroutines', 'async', 'parallel'],
  },
  {
    id: 'kotlin-android-l2-053',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Hilt/Dagger DI',
    question: 'What are the standard Hilt component scopes and their lifetimes?',
    options: ['Hilt does not have predefined scopes', 'SingletonComponent, ActivityComponent, FragmentComponent, ViewModelComponent, and others', 'Only Singleton', 'ApplicationScope and ActivityScope only'],
    correctAnswer: 1,
    explanation: 'Hilt provides standard components: SingletonComponent (app lifetime), ActivityRetainedComponent (survives config changes), ViewModelComponent (ViewModel lifetime), ActivityComponent (Activity lifetime), FragmentComponent (Fragment lifetime), ViewComponent, and ServiceComponent.',
    codeSnippet: '// Scoped to the entire application\n@Module\n@InstallIn(SingletonComponent::class)\nobject AppModule {\n    @Provides\n    @Singleton\n    fun provideDatabase(app: Application): AppDatabase { }\n}\n\n// Scoped to ViewModel\n@Module\n@InstallIn(ViewModelComponent::class)\nobject ViewModelModule {\n    @Provides\n    @ViewModelScoped\n    fun provideUseCase(repo: Repository): MyUseCase { }\n}',
    tags: ['hilt', 'scopes', 'components'],
  },
  {
    id: 'kotlin-android-l2-054',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Hilt/Dagger DI',
    question: 'How do you inject a ViewModel using Hilt?',
    options: ['Using @Inject on the ViewModel constructor only', 'Using ViewModelProvider.Factory manually', 'Annotate the ViewModel with @HiltViewModel and use @Inject constructor, then use hiltViewModel() in Compose', 'Hilt does not support ViewModels'],
    correctAnswer: 2,
    explanation: '@HiltViewModel annotation combined with @Inject constructor allows Hilt to provide dependencies to the ViewModel. In Compose, use hiltViewModel() to obtain the instance. In Activities/Fragments, use by viewModels().',
    codeSnippet: '@HiltViewModel\nclass HomeViewModel @Inject constructor(\n    private val repository: UserRepository,\n    private val savedStateHandle: SavedStateHandle\n) : ViewModel() { }\n\n// In Compose\n@Composable\nfun HomeScreen(viewModel: HomeViewModel = hiltViewModel()) { }',
    tags: ['hilt', 'viewmodel', 'injection'],
  },
  {
    id: 'kotlin-android-l2-055',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Room Database',
    question: 'How do you define a one-to-many relationship in Room?',
    options: ['Room does not support relationships', 'Using foreign keys only', 'Using JOIN queries only', 'Using @Relation annotation in a data class that combines the parent and children'],
    correctAnswer: 3,
    explanation: 'Room uses @Relation annotation to define relationships. You create a data class that embeds the parent entity and uses @Relation to specify the related child entities. You also define @ForeignKey on the child entity.',
    codeSnippet: 'data class UserWithPosts(\n    @Embedded val user: User,\n    @Relation(\n        parentColumn = "userId",\n        entityColumn = "authorId"\n    )\n    val posts: List<Post>\n)\n\n@Dao\ninterface UserDao {\n    @Transaction\n    @Query("SELECT * FROM users")\n    fun getUsersWithPosts(): List<UserWithPosts>\n}',
    tags: ['room', 'relation', 'one-to-many'],
  },
  {
    id: 'kotlin-android-l2-056',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'What is CompositionLocal in Jetpack Compose?',
    options: ['A local variable scope', 'A local database', 'A localization library', 'A tool for implicitly passing data down the composition tree without passing it through every composable'],
    correctAnswer: 3,
    explanation: 'CompositionLocal allows data to be passed implicitly through the composition tree. MaterialTheme uses this to provide colors, typography, etc. Custom CompositionLocals can be created with compositionLocalOf or staticCompositionLocalOf.',
    codeSnippet: 'val LocalUserName = compositionLocalOf { "Guest" }\n\n@Composable\nfun App() {\n    CompositionLocalProvider(LocalUserName provides "Alice") {\n        Greeting() // Can access LocalUserName\n    }\n}\n\n@Composable\nfun Greeting() {\n    val name = LocalUserName.current\n    Text("Hello, $name")\n}',
    tags: ['compose', 'compositionlocal', 'implicit-data'],
  },
  {
    id: 'kotlin-android-l2-057',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'What is the difference between Modifier.weight() and Modifier.fillMaxWidth()?',
    options: ['weight() distributes space proportionally among siblings; fillMaxWidth() takes the full available width', 'fillMaxWidth() is deprecated', 'They are identical', 'weight() only works in Column'],
    correctAnswer: 0,
    explanation: 'Modifier.weight() distributes remaining space among siblings proportionally (only works in Row/Column). Modifier.fillMaxWidth() makes the element take up the entire available width. weight() is relative to siblings; fillMaxWidth() is absolute.',
    codeSnippet: 'Row {\n    Text("Left", modifier = Modifier.weight(1f))   // Takes 1/3\n    Text("Center", modifier = Modifier.weight(1f)) // Takes 1/3\n    Text("Right", modifier = Modifier.weight(1f))  // Takes 1/3\n}\n\n// vs\nText("Full width", modifier = Modifier.fillMaxWidth()) // Takes all width',
    tags: ['compose', 'modifier', 'weight', 'layout'],
  },
  {
    id: 'kotlin-android-l2-058',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What is AnimatedVisibility in Jetpack Compose?',
    options: ['A debug tool for animations', 'A composable that animates the appearance and disappearance of its content', 'A view visibility manager', 'A visibility modifier'],
    correctAnswer: 1,
    explanation: 'AnimatedVisibility is a composable that animates the entrance and exit of its content. You can customize the enter and exit transitions using parameters like fadeIn, slideIn, expandIn, and their exit counterparts.',
    codeSnippet: 'var visible by remember { mutableStateOf(true) }\n\nAnimatedVisibility(\n    visible = visible,\n    enter = fadeIn() + slideInVertically(),\n    exit = fadeOut() + slideOutVertically()\n) {\n    Text("Animated Content")\n}',
    tags: ['compose', 'animation', 'visibility'],
  },
  {
    id: 'kotlin-android-l2-059',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'RecyclerView',
    question: 'How do you implement swipe-to-delete in RecyclerView?',
    options: ['Using ItemTouchHelper with a SimpleCallback', 'Using a GestureDetector', 'Swipe actions are not supported', 'Using OnClickListener'],
    correctAnswer: 0,
    explanation: 'ItemTouchHelper is a utility class that adds swipe-to-dismiss and drag-and-drop support to RecyclerView. You create a SimpleCallback specifying the directions and override onSwiped() to handle the action.',
    codeSnippet: 'val itemTouchHelper = ItemTouchHelper(\n    object : ItemTouchHelper.SimpleCallback(0, ItemTouchHelper.LEFT or ItemTouchHelper.RIGHT) {\n        override fun onMove(rv: RecyclerView, vh: RecyclerView.ViewHolder, target: RecyclerView.ViewHolder) = false\n        override fun onSwiped(viewHolder: RecyclerView.ViewHolder, direction: Int) {\n            val position = viewHolder.adapterPosition\n            adapter.removeItem(position)\n        }\n    }\n)\nitemTouchHelper.attachToRecyclerView(recyclerView)',
    tags: ['recyclerview', 'swipe', 'itemtouchhelper'],
  },
  {
    id: 'kotlin-android-l2-060',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'ViewBinding',
    question: 'How do you use ViewBinding in a Fragment?',
    options: ['ViewBinding does not work in Fragments', 'The same as in Activity', 'Inflate in onCreateView and null out the reference in onDestroyView to avoid memory leaks', 'Using FragmentBinding.create()'],
    correctAnswer: 2,
    explanation: 'In Fragments, you inflate the binding in onCreateView() and must set the binding reference to null in onDestroyView() because the Fragment can outlive its view. This prevents memory leaks from holding references to destroyed views.',
    codeSnippet: 'class MyFragment : Fragment() {\n    private var _binding: FragmentMyBinding? = null\n    private val binding get() = _binding!!\n\n    override fun onCreateView(inflater: LayoutInflater, container: ViewGroup?, savedInstanceState: Bundle?): View {\n        _binding = FragmentMyBinding.inflate(inflater, container, false)\n        return binding.root\n    }\n\n    override fun onDestroyView() {\n        super.onDestroyView()\n        _binding = null // Prevent memory leak\n    }\n}',
    tags: ['viewbinding', 'fragment', 'memory-leak'],
  },
  {
    id: 'kotlin-android-l2-061',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'ViewModel',
    question: 'What is SavedStateHandle in ViewModel?',
    options: ['A key-value map that enables ViewModel to access and contribute to the saved state, surviving process death', 'A handle to the saved instance state of the Activity', 'A database handle', 'A file handle'],
    correctAnswer: 0,
    explanation: 'SavedStateHandle is a key-value map that allows ViewModel to write to and retrieve data from the saved state. Unlike regular ViewModel data, SavedStateHandle data survives process death, similar to onSaveInstanceState.',
    codeSnippet: 'class SearchViewModel(\n    private val savedStateHandle: SavedStateHandle\n) : ViewModel() {\n    val query = savedStateHandle.getStateFlow("query", "")\n\n    fun setQuery(newQuery: String) {\n        savedStateHandle["query"] = newQuery\n    }\n}',
    tags: ['viewmodel', 'savedstatehandle', 'process-death'],
  },
  {
    id: 'kotlin-android-l2-062',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'What is a CoroutineExceptionHandler and when should you use it?',
    options: ['A crash reporting tool', 'A lint checker for coroutines', 'A handler that catches unhandled exceptions in coroutines launched with launch (not async)', 'A try-catch replacement'],
    correctAnswer: 2,
    explanation: 'CoroutineExceptionHandler is a context element that handles uncaught exceptions in coroutines. It only works with launch (not async, which stores exceptions in the Deferred). It is useful as a last-resort handler.',
    codeSnippet: 'val handler = CoroutineExceptionHandler { _, exception ->\n    Log.e("TAG", "Caught: ${exception.message}")\n}\n\nviewModelScope.launch(handler) {\n    throw RuntimeException("Something went wrong")\n    // Exception caught by handler instead of crashing\n}',
    tags: ['coroutines', 'exception-handler', 'error-handling'],
  },
  {
    id: 'kotlin-android-l2-063',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Retrofit',
    question: 'How do you handle API errors in Retrofit with coroutines?',
    options: ['Errors cannot be caught', 'Using a callback interface', 'Using try-catch around the suspend function call and checking Response.isSuccessful for non-2xx codes', 'Using onFailure() listener'],
    correctAnswer: 2,
    explanation: 'With suspend functions, network errors throw exceptions that can be caught with try-catch. For non-2xx HTTP responses, you can return Response<T> and check isSuccessful. This gives you access to the error body and status code.',
    codeSnippet: 'interface ApiService {\n    @GET("users")\n    suspend fun getUsers(): Response<List<User>>\n}\n\ntry {\n    val response = apiService.getUsers()\n    if (response.isSuccessful) {\n        val users = response.body()\n    } else {\n        val errorCode = response.code()\n        val errorBody = response.errorBody()?.string()\n    }\n} catch (e: IOException) {\n    // Network error\n} catch (e: HttpException) {\n    // HTTP error\n}',
    tags: ['retrofit', 'error-handling', 'coroutines'],
  },
  {
    id: 'kotlin-android-l2-064',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'WorkManager',
    question: 'How do you chain work requests in WorkManager?',
    options: ['Using beginWith() and then() to create sequential or parallel chains', 'Using callbacks', 'Using a linked list', 'Chaining is not supported'],
    correctAnswer: 0,
    explanation: 'WorkManager supports chaining with beginWith() (starts the chain) and then() (adds sequential work). You can also run work in parallel by passing a list to beginWith(). Chains ensure execution order and pass data between workers.',
    codeSnippet: 'WorkManager.getInstance(context)\n    .beginWith(listOf(downloadWork1, downloadWork2)) // Parallel\n    .then(processWork)  // After both downloads complete\n    .then(uploadWork)   // After processing\n    .enqueue()',
    tags: ['workmanager', 'chaining', 'sequential'],
  },
  {
    id: 'kotlin-android-l2-065',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What is snapshotFlow in Jetpack Compose?',
    options: ['A function that converts Compose State reads into a Flow, emitting when the state changes', 'A screenshot API', 'A state snapshot tool', 'A flow of UI screenshots'],
    correctAnswer: 0,
    explanation: 'snapshotFlow converts Compose State into a Kotlin Flow. The lambda reads Compose state, and whenever those state values change, a new value is emitted to the flow. This is useful for reacting to state changes in coroutines.',
    codeSnippet: 'val listState = rememberLazyListState()\n\nLaunchedEffect(listState) {\n    snapshotFlow { listState.firstVisibleItemIndex }\n        .distinctUntilChanged()\n        .collect { index ->\n            analytics.logScroll(index)\n        }\n}',
    tags: ['compose', 'snapshotflow', 'state-to-flow'],
  },
  {
    id: 'kotlin-android-l2-066',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Navigation',
    question: 'How do you implement a bottom navigation bar with the Navigation component in Compose?',
    options: ['Using NavigationBar with NavigationBarItem composables, each linked to a NavController route', 'Bottom navigation is not supported in Compose', 'Using a BottomAppBar only', 'Using a TabRow'],
    correctAnswer: 0,
    explanation: 'In Compose, you use NavigationBar (Material3) with NavigationBarItem composables. Each item navigates to a route using navController.navigate() with options to restore state and avoid duplicate destinations.',
    codeSnippet: 'NavigationBar {\n    val navBackStackEntry by navController.currentBackStackEntryAsState()\n    val currentRoute = navBackStackEntry?.destination?.route\n\n    items.forEach { screen ->\n        NavigationBarItem(\n            icon = { Icon(screen.icon, contentDescription = screen.title) },\n            label = { Text(screen.title) },\n            selected = currentRoute == screen.route,\n            onClick = {\n                navController.navigate(screen.route) {\n                    popUpTo(navController.graph.findStartDestination().id) { saveState = true }\n                    launchSingleTop = true\n                    restoreState = true\n                }\n            }\n        )\n    }\n}',
    tags: ['navigation', 'bottomnav', 'compose'],
  },
  {
    id: 'kotlin-android-l2-067',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'State management',
    question: 'What is the difference between mutableStateListOf and toMutableStateList()?',
    options: ['mutableStateListOf creates a new observable list; toMutableStateList() converts an existing collection to an observable one', 'mutableStateListOf only works with strings', 'toMutableStateList() is deprecated', 'They are identical'],
    correctAnswer: 0,
    explanation: 'mutableStateListOf() creates a new SnapshotStateList that Compose can observe for changes. toMutableStateList() converts an existing collection into a SnapshotStateList. Both trigger recomposition when items are added, removed, or changed.',
    codeSnippet: '// Create new observable list\nval items = remember { mutableStateListOf("A", "B", "C") }\n\n// Convert existing list\nval items = remember { existingList.toMutableStateList() }\n\n// Modifications trigger recomposition\nitems.add("D")\nitems.removeAt(0)',
    tags: ['compose', 'state', 'list', 'observable'],
  },
  {
    id: 'kotlin-android-l2-068',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'What is the ConstraintLayout equivalent in Jetpack Compose?',
    options: ['ConstraintLayout for Compose, using createRefs() and constrainAs()', 'RelativeLayout composable', 'There is no equivalent', 'AbsoluteLayout composable'],
    correctAnswer: 0,
    explanation: 'Compose has its own ConstraintLayout implementation. You use createRefs() to create references for composables and constrainAs() modifier to define constraints. It requires the constraintlayout-compose dependency.',
    codeSnippet: 'ConstraintLayout(modifier = Modifier.fillMaxSize()) {\n    val (button, text) = createRefs()\n    Button(\n        onClick = { },\n        modifier = Modifier.constrainAs(button) {\n            top.linkTo(parent.top, margin = 16.dp)\n            start.linkTo(parent.start)\n        }\n    ) { Text("Button") }\n    Text("Hello",\n        modifier = Modifier.constrainAs(text) {\n            top.linkTo(button.bottom, margin = 16.dp)\n            start.linkTo(button.start)\n        }\n    )\n}',
    tags: ['compose', 'constraintlayout', 'layout'],
  },
  {
    id: 'kotlin-android-l2-069',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Material Design',
    question: 'How do you implement a navigation drawer in Jetpack Compose?',
    options: ['Using a Side Sheet', 'Using DrawerLayout', 'Drawers are not available in Compose', 'Using ModalNavigationDrawer composable with a drawerContent parameter'],
    correctAnswer: 3,
    explanation: 'In Compose Material3, ModalNavigationDrawer provides a sliding navigation drawer. You define the drawer content and main content separately, and control the drawer state with DrawerState.',
    codeSnippet: 'val drawerState = rememberDrawerState(DrawerValue.Closed)\nval scope = rememberCoroutineScope()\n\nModalNavigationDrawer(\n    drawerState = drawerState,\n    drawerContent = {\n        ModalDrawerSheet {\n            Text("Menu", modifier = Modifier.padding(16.dp))\n            NavigationDrawerItem(\n                label = { Text("Home") },\n                selected = true,\n                onClick = { scope.launch { drawerState.close() } }\n            )\n        }\n    }\n) {\n    Scaffold { /* content */ }\n}',
    tags: ['compose', 'drawer', 'material-design', 'navigation'],
  },
  {
    id: 'kotlin-android-l2-070',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What are Compose stability and skippable composables?',
    options: ['Thread safety features', 'Features for app stability testing', 'Performance metrics', 'Compose can skip recomposition of a composable if all its parameters are stable and unchanged'],
    correctAnswer: 3,
    explanation: 'Compose can skip recomposing a composable if all its parameters are "stable" (immutable or observable) and have not changed since the last composition. Unstable parameters force recomposition every time. Using data classes with val properties and immutable collections helps stability.',
    tags: ['compose', 'stability', 'performance', 'skippable'],
  },
  {
    id: 'kotlin-android-l2-071',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'LazyColumn/LazyRow',
    question: 'How do you add sticky headers to a LazyColumn?',
    options: ['Using a TopAppBar instead', 'Using a regular item with a fixed position', 'Using the stickyHeader {} function in LazyColumn scope', 'Sticky headers are not supported'],
    correctAnswer: 2,
    explanation: 'LazyColumn supports sticky headers through the stickyHeader {} DSL function. Headers remain pinned at the top of the list as the user scrolls past them. Note: this API is experimental.',
    codeSnippet: 'LazyColumn {\n    groupedItems.forEach { (category, items) ->\n        stickyHeader {\n            Text(\n                text = category,\n                modifier = Modifier\n                    .fillMaxWidth()\n                    .background(MaterialTheme.colorScheme.surface)\n                    .padding(8.dp)\n            )\n        }\n        items(items) { item ->\n            ItemRow(item)\n        }\n    }\n}',
    tags: ['compose', 'lazycolumn', 'stickyheader'],
  },
  {
    id: 'kotlin-android-l2-072',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Permissions',
    question: 'How do you handle the case where the user selects "Don\'t ask again" for a permission?',
    options: ['The app should crash gracefully', 'Keep requesting the permission', 'Check if shouldShowRequestPermissionRationale returns false after denial, and guide the user to app settings', 'You cannot handle this case'],
    correctAnswer: 2,
    explanation: 'If shouldShowRequestPermissionRationale() returns false after a denial, it means the user selected "Don\'t ask again". You should explain why the permission is needed and provide a button to open the app settings where the user can manually grant it.',
    codeSnippet: 'val launcher = registerForActivityResult(\n    ActivityResultContracts.RequestPermission()\n) { isGranted ->\n    if (!isGranted && !shouldShowRequestPermissionRationale(permission)) {\n        // User selected "Don\'t ask again"\n        showSettingsDialog() // Guide to Settings > App > Permissions\n    }\n}\n\nfun openAppSettings() {\n    startActivity(Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {\n        data = Uri.fromParts("package", packageName, null)\n    })\n}',
    tags: ['permissions', 'dont-ask-again', 'settings'],
  },
  {
    id: 'kotlin-android-l2-073',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Retrofit',
    question: 'What is an OkHttp Interceptor and how is it used with Retrofit?',
    options: ['A security feature', 'A caching library', 'A logging framework', 'A mechanism to intercept, modify, and monitor HTTP requests and responses'],
    correctAnswer: 3,
    explanation: 'OkHttp Interceptors can observe, modify, and short-circuit requests and responses. Application interceptors run once per call; network interceptors access the actual network request. Common uses include logging, authentication, caching headers, and retry logic.',
    codeSnippet: 'val loggingInterceptor = HttpLoggingInterceptor().apply {\n    level = HttpLoggingInterceptor.Level.BODY\n}\n\nval client = OkHttpClient.Builder()\n    .addInterceptor(loggingInterceptor)\n    .addInterceptor(authInterceptor)\n    .build()\n\nval retrofit = Retrofit.Builder()\n    .baseUrl(BASE_URL)\n    .client(client)\n    .build()',
    tags: ['retrofit', 'okhttp', 'interceptor'],
  },
  {
    id: 'kotlin-android-l2-074',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Room Database',
    question: 'What does @Transaction annotation do on a Room DAO method?',
    options: ['It encrypts the data', 'It caches the result', 'It logs the query', 'It ensures all database operations in the method run in a single atomic transaction'],
    correctAnswer: 3,
    explanation: '@Transaction ensures all database operations within the annotated method execute in a single transaction. If any operation fails, all changes are rolled back. It is required for methods returning objects with @Relation annotations.',
    codeSnippet: '@Dao\ninterface OrderDao {\n    @Transaction\n    suspend fun placeOrder(order: Order, items: List<OrderItem>) {\n        insertOrder(order)\n        insertItems(items)\n        // If insertItems fails, insertOrder is also rolled back\n    }\n\n    @Transaction\n    @Query("SELECT * FROM orders WHERE id = :orderId")\n    suspend fun getOrderWithItems(orderId: Int): OrderWithItems\n}',
    tags: ['room', 'transaction', 'atomic'],
  },
  {
    id: 'kotlin-android-l2-075',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'How do you handle back press in Jetpack Compose?',
    options: ['Using a back button in the UI only', 'Overriding onBackPressed()', 'Back press handling is not supported in Compose', 'Using BackHandler composable from the activity-compose library'],
    correctAnswer: 3,
    explanation: 'BackHandler is a composable that intercepts the system back button press. When enabled is true, it invokes the provided callback instead of the default back behavior. It is part of the activity-compose library.',
    codeSnippet: '@Composable\nfun MyScreen() {\n    var showDialog by remember { mutableStateOf(false) }\n\n    BackHandler(enabled = true) {\n        showDialog = true // Show confirmation dialog instead of navigating back\n    }\n\n    if (showDialog) {\n        AlertDialog(\n            onDismissRequest = { showDialog = false },\n            title = { Text("Exit?") },\n            confirmButton = {\n                TextButton(onClick = { /* actually navigate back */ }) {\n                    Text("Yes")\n                }\n            }\n        )\n    }\n}',
    tags: ['compose', 'backhandler', 'back-press'],
  },
  {
    id: 'kotlin-android-l2-076',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'What is a LazyGrid in Jetpack Compose?',
    options: ['A CSS Grid equivalent', 'A lazy composable that displays items in a grid layout, similar to RecyclerView with GridLayoutManager', 'A grid layout that loads lazily from the network', 'A lazy-loaded image grid'],
    correctAnswer: 1,
    explanation: 'LazyVerticalGrid and LazyHorizontalGrid are composables that display items in a grid, only composing visible items. You can use GridCells.Fixed for a fixed number of columns or GridCells.Adaptive for responsive sizing.',
    codeSnippet: 'LazyVerticalGrid(\n    columns = GridCells.Adaptive(minSize = 128.dp),\n    contentPadding = PaddingValues(8.dp)\n) {\n    items(photos) { photo ->\n        PhotoCard(photo)\n    }\n}',
    tags: ['compose', 'lazygrid', 'grid', 'layout'],
  },
  {
    id: 'kotlin-android-l2-077',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'ViewModel',
    question: 'How do you share a ViewModel between a Fragment and its parent Activity?',
    options: ['Use activityViewModels() delegate in the Fragment to scope the ViewModel to the Activity', 'This is not possible', 'Using static variables', 'Create two separate instances'],
    correctAnswer: 0,
    explanation: 'The activityViewModels() delegate scopes the ViewModel to the Activity, allowing all Fragments within that Activity to share the same ViewModel instance. This is a common pattern for communication between fragments.',
    codeSnippet: '// In Fragment A\nclass FragmentA : Fragment() {\n    private val sharedViewModel: SharedViewModel by activityViewModels()\n}\n\n// In Fragment B - gets the SAME instance\nclass FragmentB : Fragment() {\n    private val sharedViewModel: SharedViewModel by activityViewModels()\n}',
    tags: ['viewmodel', 'shared', 'fragment', 'activity'],
  },
  {
    id: 'kotlin-android-l2-078',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'WorkManager',
    question: 'How do you observe the status of a WorkManager request?',
    options: ['Using a callback interface', 'Using getWorkInfoByIdLiveData() or getWorkInfoByIdFlow() to observe the WorkInfo', 'Work status cannot be observed', 'By checking a status file'],
    correctAnswer: 1,
    explanation: 'WorkManager provides getWorkInfoByIdLiveData() and getWorkInfoByIdFlow() to observe the state of work. WorkInfo contains the state (ENQUEUED, RUNNING, SUCCEEDED, FAILED, etc.) and any output data.',
    codeSnippet: 'val workRequest = OneTimeWorkRequestBuilder<MyWorker>().build()\nWorkManager.getInstance(context).enqueue(workRequest)\n\n// Observe with LiveData\nWorkManager.getInstance(context)\n    .getWorkInfoByIdLiveData(workRequest.id)\n    .observe(this) { workInfo ->\n        when (workInfo?.state) {\n            WorkInfo.State.SUCCEEDED -> showSuccess()\n            WorkInfo.State.FAILED -> showError()\n            WorkInfo.State.RUNNING -> showProgress()\n            else -> { }\n        }\n    }',
    tags: ['workmanager', 'observation', 'workinfo'],
  },
  {
    id: 'kotlin-android-l2-079',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Coroutines in Android',
    question: 'What is the difference between launch and async in coroutines?',
    options: ['They are identical', 'async is deprecated', 'launch is for IO; async is for UI', 'launch returns Job and is fire-and-forget; async returns Deferred and allows retrieving a result with await()'],
    correctAnswer: 3,
    explanation: 'launch starts a coroutine and returns a Job. It is used for fire-and-forget operations. async starts a coroutine and returns a Deferred<T> which is a Job that holds a future result. You call await() to get the result.',
    codeSnippet: '// launch - fire and forget\nval job = scope.launch {\n    saveToDatabase(data) // No result needed\n}\n\n// async - returns a result\nval deferred = scope.async {\n    fetchFromNetwork() // Returns a result\n}\nval result = deferred.await()',
    tags: ['coroutines', 'launch', 'async', 'comparison'],
  },
  {
    id: 'kotlin-android-l2-080',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Hilt/Dagger DI',
    question: 'What is the difference between @Binds and @Provides in a Hilt Module?',
    options: ['They are identical', '@Binds maps an interface to its implementation (abstract); @Provides creates an instance with custom logic', '@Provides is deprecated in favor of @Binds', '@Binds is for Activities; @Provides is for ViewModels'],
    correctAnswer: 1,
    explanation: '@Binds is used in abstract modules to tell Hilt which implementation to use for an interface. It is more efficient than @Provides as it does not create a wrapper. @Provides is used when you need custom creation logic or for third-party classes.',
    codeSnippet: '// @Binds - maps interface to implementation\n@Module\n@InstallIn(SingletonComponent::class)\nabstract class RepositoryModule {\n    @Binds\n    abstract fun bindUserRepo(impl: UserRepositoryImpl): UserRepository\n}\n\n// @Provides - custom creation logic\n@Module\n@InstallIn(SingletonComponent::class)\nobject NetworkModule {\n    @Provides\n    fun provideOkHttp(): OkHttpClient = OkHttpClient.Builder().build()\n}',
    tags: ['hilt', 'binds', 'provides', 'comparison'],
  },
  {
    id: 'kotlin-android-l2-081',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Jetpack Compose basics',
    question: 'How do you handle text input with validation in Jetpack Compose?',
    options: ['Using XML-based validation rules', 'Using form validation libraries only', 'Validation is not supported in Compose', 'Managing state with remember, validating on value change, and showing error state on TextField'],
    correctAnswer: 3,
    explanation: 'In Compose, you manage text state and validation logic using remember and mutableStateOf. The TextField supports isError parameter to show error state, and you can show error messages conditionally.',
    codeSnippet: 'var email by remember { mutableStateOf("") }\nvar isError by remember { mutableStateOf(false) }\n\nOutlinedTextField(\n    value = email,\n    onValueChange = {\n        email = it\n        isError = !Patterns.EMAIL_ADDRESS.matcher(it).matches()\n    },\n    label = { Text("Email") },\n    isError = isError,\n    supportingText = {\n        if (isError) Text("Invalid email address")\n    }\n)',
    tags: ['compose', 'textfield', 'validation', 'input'],
  },
  {
    id: 'kotlin-android-l2-082',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Composable functions',
    question: 'What does rememberCoroutineScope() do in Compose?',
    options: ['Creates a global coroutine scope', 'Creates a viewModelScope', 'Creates a lifecycleScope', 'Returns a CoroutineScope bound to the composition that can be used to launch coroutines from event handlers'],
    correctAnswer: 3,
    explanation: 'rememberCoroutineScope() returns a CoroutineScope that is bound to the point of the composition where it is called. The scope is cancelled when the composable leaves the composition. It is used to launch coroutines from callbacks.',
    codeSnippet: '@Composable\nfun MyScreen() {\n    val scope = rememberCoroutineScope()\n    val snackbarHostState = remember { SnackbarHostState() }\n\n    Button(onClick = {\n        scope.launch {\n            snackbarHostState.showSnackbar("Hello!")\n        }\n    }) {\n        Text("Show Snackbar")\n    }\n}',
    tags: ['compose', 'coroutinescope', 'event-handling'],
  },
  {
    id: 'kotlin-android-l2-083',
    language: 'kotlin',
    level: 'level2',
    category: 'Android Basics',
    subcategory: 'Navigation',
    question: 'What is the launchSingleTop flag in Navigation?',
    options: ['It launches the activity in single-task mode', 'It ensures only one instance of the destination exists on top of the back stack, preventing duplicate screens', 'It pins the destination to the top', 'It launches the app on top of other apps'],
    correctAnswer: 1,
    explanation: 'launchSingleTop prevents creating a new instance of the destination if it is already on top of the back stack. This avoids duplicate screens when the user taps the same navigation item multiple times.',
    codeSnippet: 'navController.navigate("home") {\n    launchSingleTop = true\n    // Also commonly used with:\n    popUpTo(navController.graph.findStartDestination().id) {\n        saveState = true\n    }\n    restoreState = true\n}',
    tags: ['navigation', 'launchsingletop', 'backstack'],
  },
];

export default questions;
