import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-core-l3-001',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Null Safety',
    question: 'What is the output of this code?',
    options: ['NullPointerException', 'null', 'Compilation error', '0'],
    correctAnswer: 3,
    explanation: 'The `?.let` returns null when `value` is null, then the `?.let` on the outer null also returns null, and the Elvis operator provides 0. Chaining `?.let` on nullable results composes safely.',
    codeSnippet: 'val value: String? = null\nval result = value?.let { it.toIntOrNull() }?.let { it * 2 } ?: 0\nprintln(result)',
    tags: ['null-safety', 'let', 'chaining', 'elvis-operator'],
  },
  {
    id: 'kotlin-core-l3-002',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Null Safety',
    question: 'What is the output of this code?',
    options: ['IllegalStateException', '0', 'Compilation error', 'null'],
    correctAnswer: 0,
    explanation: 'The Elvis operator can have throw or return on its right side. Here, `checkNotNull(null)` throws `IllegalStateException` because the value is null.',
    codeSnippet: 'val value: String? = null\ntry {\n    val result = checkNotNull(value) { "Must not be null" }\n    println(result)\n} catch (e: IllegalStateException) {\n    println(e::class.simpleName)\n}',
    tags: ['null-safety', 'checkNotNull', 'exceptions'],
  },
  {
    id: 'kotlin-core-l3-003',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Null Safety',
    question: 'What is the output of this code?',
    options: ['Compilation error', 'default', 'null', ''],
    correctAnswer: 1,
    explanation: 'An empty string is not null, so `?.let` executes. But `""` is falsy-like in `takeIf { it.isNotBlank() }`, so `takeIf` returns null. The Elvis operator then provides "default".',
    codeSnippet: 'val input: String? = ""\nval result = input?.takeIf { it.isNotBlank() } ?: "default"\nprintln(result)',
    tags: ['null-safety', 'takeIf', 'elvis-operator'],
  },
  {
    id: 'kotlin-core-l3-005',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Data Classes',
    question: 'What is the output of this code?',
    options: ['ConcurrentModificationException', 'Compilation error', '[A, B, C]', '[A, B]'],
    correctAnswer: 2,
    explanation: 'Because `copy()` is shallow, `p1.items` and `p2.items` point to the same list. Adding "C" through `p2` is visible through `p1`.',
    codeSnippet: 'data class Person(val name: String, val items: MutableList<String>)\nval p1 = Person("Alice", mutableListOf("A", "B"))\nval p2 = p1.copy(name = "Bob")\np2.items.add("C")\nprintln(p1.items)',
    tags: ['data-classes', 'shallow-copy', 'mutable-collections'],
  },
  {
    id: 'kotlin-core-l3-006',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Sealed Classes',
    question: 'What is the output of this code?',
    options: ['Matched: 42', 'Compilation error', 'No match', 'Matched: null'],
    correctAnswer: 0,
    explanation: 'Nested sealed class hierarchies work with nested `when` expressions. The pattern matching first checks for `Result.Success`, then smart-casts to access the `data` property.',
    codeSnippet: 'sealed class Result<out T> {\n    data class Success<T>(val data: T) : Result<T>()\n    data class Failure(val error: String) : Result<Nothing>()\n}\nval r: Result<Int> = Result.Success(42)\nwhen (r) {\n    is Result.Success -> println("Matched: ${r.data}")\n    is Result.Failure -> println("Error: ${r.error}")\n}',
    tags: ['sealed-classes', 'generics', 'pattern-matching'],
  },
  {
    id: 'kotlin-core-l3-007',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Sealed Classes',
    question: 'What is the advantage of using `Nothing` as a type parameter in sealed class hierarchies?',
    options: [
      'It prevents the class from being instantiated',
      'It allows the subclass to be a subtype of Result for any T due to covariance',
      'It makes the class thread-safe',
      'It makes the class immutable',
    ],
    correctAnswer: 1,
    explanation: '`Nothing` is a subtype of every type in Kotlin. Combined with `out` (covariance), `Result<Nothing>` is a subtype of `Result<T>` for any `T`, so `Failure` can be used regardless of the success type.',
    codeSnippet: 'sealed class Result<out T> {\n    data class Success<T>(val data: T) : Result<T>()\n    data class Failure(val msg: String) : Result<Nothing>()\n}\n// Failure works as Result<Int>, Result<String>, etc.',
    tags: ['sealed-classes', 'Nothing', 'covariance', 'generics'],
  },
  {
    id: 'kotlin-core-l3-008',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Sealed Interfaces',
    question: 'What is the output of this code?',
    options: ['Runtime error', 'Error', 'Compilation error', 'Recoverable network error: 500'],
    correctAnswer: 3,
    explanation: 'A class can implement multiple sealed interfaces. The `when` expression can check for combinations using `&&`. `ServerError` implements both `Error` and `Recoverable`.',
    codeSnippet: 'sealed interface Error\nsealed interface Recoverable\ndata class ServerError(val code: Int) : Error, Recoverable\ndata class FatalError(val msg: String) : Error\n\nfun handle(e: Error): String = when (e) {\n    is ServerError -> when (e) {\n        is Recoverable -> "Recoverable network error: ${e.code}"\n    }\n    is FatalError -> "Fatal: ${e.msg}"\n}\nprintln(handle(ServerError(500)))',
    tags: ['sealed-interfaces', 'multiple-inheritance', 'when-expression'],
  },
  {
    id: 'kotlin-core-l3-010',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Extension Functions',
    question: 'What is the output of this code?',
    options: ['Ext(Any)', 'Compilation error', 'Runtime error', 'Ext(String)'],
    correctAnswer: 3,
    explanation: 'Extension functions are dispatched statically based on the declared type of the receiver. Additionally, the most specific overload is chosen at compile time. Since `s` is declared as `String`, `greet(String)` is called.',
    codeSnippet: 'fun Any.greet() = "Ext(Any)"\nfun String.greet() = "Ext(String)"\n\nval s: String = "hello"\nprintln(s.greet())',
    tags: ['extension-functions', 'overload-resolution'],
  },
  {
    id: 'kotlin-core-l3-012',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Extension Properties',
    question: 'What is the output of this code?',
    options: ['0', 'Compilation error', '3', 'null'],
    correctAnswer: 2,
    explanation: 'The generic extension property `secondOrNull` is defined on `List<T>`. It returns the element at index 1 if the list has at least 2 elements. `listOf(1, 3, 5)` has 3 elements, so index 1 is 3.',
    codeSnippet: 'val <T> List<T>.secondOrNull: T?\n    get() = if (size >= 2) this[1] else null\n\nprintln(listOf(1, 3, 5).secondOrNull)',
    tags: ['extension-properties', 'generics', 'nullable'],
  },
  {
    id: 'kotlin-core-l3-013',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Scope Functions',
    question: 'What is the output of this code?',
    options: ['NonNull: 5', 'null', 'NullPointerException', 'Compilation error'],
    correctAnswer: 1,
    explanation: 'The `run` block returns the lambda result. Since `text` is null, `text?.run { length }` evaluates to null because the safe call short-circuits. Then `println(null)` prints "null".',
    codeSnippet: 'val text: String? = null\nval length = text?.run { length }\nprintln(length)',
    tags: ['scope-functions', 'run', 'null-safety'],
  },
  {
    id: 'kotlin-core-l3-014',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Scope Functions',
    question: 'What is the output of this code?',
    options: ['Alice', 'Compilation error', 'Alice (30)', '30'],
    correctAnswer: 2,
    explanation: '`with` takes the object as argument and the lambda uses `this`. The lambda returns the last expression which is the formatted string. `name` and `age` are accessed directly through `this`.',
    codeSnippet: 'data class Person(val name: String, val age: Int)\nval person = Person("Alice", 30)\nval desc = with(person) {\n    "$name ($age)"\n}\nprintln(desc)',
    tags: ['scope-functions', 'with', 'this-context'],
  },
  {
    id: 'kotlin-core-l3-015',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Delegation',
    question: 'What is the output of this code?',
    options: ['null -> new', 'initial -> new', 'old -> new', 'Compilation error'],
    correctAnswer: 1,
    explanation: '`Delegates.observable` is initialized with "initial" and the callback is invoked when `name` is reassigned. The old value is "initial" and the new value is "new".',
    codeSnippet: 'import kotlin.properties.Delegates\n\nvar name: String by Delegates.observable("initial") { _, old, new ->\n    println("$old -> $new")\n}\nname = "new"',
    tags: ['delegation', 'observable', 'property-delegates'],
  },
  {
    id: 'kotlin-core-l3-016',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Delegation',
    question: 'What is the output of this code?',
    options: ['25', 'Compilation error', '-5', '0'],
    correctAnswer: 0,
    explanation: '`Delegates.vetoable` rejects values where the callback returns false. Setting age to 25 is accepted (25 >= 0 is true). Setting to -5 is rejected (-5 >= 0 is false), so age remains 25.',
    codeSnippet: 'import kotlin.properties.Delegates\n\nvar age: Int by Delegates.vetoable(0) { _, _, new -> new >= 0 }\nage = 25\nage = -5\nprintln(age)',
    tags: ['delegation', 'vetoable', 'property-delegates'],
  },
  {
    id: 'kotlin-core-l3-017',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Delegation',
    question: 'How do you create a custom property delegate in Kotlin?',
    options: [
      'Implement the `Delegate` interface',
      'Use the `@Delegate` annotation',
      'Implement `getValue()` and optionally `setValue()` operator functions',
      'Extend the `PropertyDelegate` class',
    ],
    correctAnswer: 2,
    explanation: 'A custom delegate needs `operator fun getValue()` for read access and `operator fun setValue()` for write access. These can implement `ReadOnlyProperty` or `ReadWriteProperty` interfaces for convenience.',
    codeSnippet: 'class Trimmed : ReadWriteProperty<Any?, String> {\n    private var value = ""\n    override fun getValue(thisRef: Any?, property: KProperty<*>) = value\n    override fun setValue(thisRef: Any?, property: KProperty<*>, value: String) {\n        this.value = value.trim()\n    }\n}',
    tags: ['delegation', 'custom-delegate', 'property-delegates'],
  },
  {
    id: 'kotlin-core-l3-018',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Inline Classes',
    question: 'When does boxing occur with value classes?',
    options: [
      'Only when passed to Java code',
      'Never; value classes are always inlined',
      'Only when stored in collections',
      'When used as a generic type argument, nullable type, or interface type',
    ],
    correctAnswer: 3,
    explanation: 'Value classes are boxed when used as nullable types (`Email?`), generic type parameters (`List<Email>`), or when the value class implements an interface and is used as that interface type.',
    codeSnippet: '@JvmInline\nvalue class Email(val s: String)\n\nval e: Email = Email("a@b.com")  // No boxing\nval n: Email? = Email("a@b.com") // Boxing!\nval list: List<Email> = listOf(e) // Boxing!',
    tags: ['inline-classes', 'value-classes', 'boxing', 'performance'],
  },
  {
    id: 'kotlin-core-l3-019',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Inline Classes',
    question: 'Can value classes have `init` blocks?',
    options: [
      'Yes, and they are useful for validation',
      'Only with the @JvmInline annotation',
      'No, value classes cannot have init blocks',
      'Only if they implement an interface',
    ],
    correctAnswer: 0,
    explanation: 'Value classes can have `init` blocks which are useful for validating the wrapped value. The `init` block runs when the value class is instantiated.',
    codeSnippet: '@JvmInline\nvalue class PositiveInt(val value: Int) {\n    init {\n        require(value > 0) { "Must be positive" }\n    }\n}\nPositiveInt(5)  // OK\nPositiveInt(-1) // IllegalArgumentException',
    tags: ['inline-classes', 'value-classes', 'validation', 'init'],
  },
  {
    id: 'kotlin-core-l3-021',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'When Expression',
    question: 'What is the output of this code?',
    options: ['B', 'Compilation error', 'Warning only', 'A'],
    correctAnswer: 1,
    explanation: 'When `when` is used as an expression with a sealed class or enum, it must be exhaustive. If a new subclass `C` is added but not handled, the code fails to compile (assuming no `else` branch).',
    codeSnippet: 'sealed class Letter\nobject A : Letter()\nobject B : Letter()\nobject C : Letter()\n\nfun describe(l: Letter): String = when (l) {\n    is A -> "A"\n    is B -> "B"\n    // Missing: is C\n}',
    tags: ['when-expression', 'exhaustive', 'sealed-classes'],
  },
  {
    id: 'kotlin-core-l3-022',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Smart Casts',
    question: 'What is the output of this code?',
    options: ['5', 'Compilation error', '-1', 'ClassCastException'],
    correctAnswer: 0,
    explanation: 'Smart cast works with `||` and `&&` conditions. After `input is String && input.length > 3`, if true, the compiler knows `input` is a String in the then-branch.',
    codeSnippet: 'fun getLength(input: Any): Int {\n    return if (input is String && input.length > 3) {\n        input.length\n    } else {\n        -1\n    }\n}\nprintln(getLength("Hello"))',
    tags: ['smart-casts', 'boolean-operators', 'type-checks'],
  },
  {
    id: 'kotlin-core-l3-025',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Properties',
    question: 'What is the output of this code?',
    options: ['3', 'Compilation error', 'UninitializedPropertyAccessException', '0'],
    correctAnswer: 0,
    explanation: 'The `lateinit` property is initialized before access, so it works fine. The `isInitialized` check confirms initialization. Accessing `name.length` returns 3 for "Bob".',
    codeSnippet: 'class Service {\n    lateinit var name: String\n}\nval s = Service()\ns.name = "Bob"\nprintln(s.name.length)',
    tags: ['properties', 'lateinit'],
  },
  {
    id: 'kotlin-core-l3-027',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Operator Overloading',
    question: 'What is the output of this code?',
    options: ['Counter(5)', 'Counter(4)', 'Compilation error', 'Counter(6)'],
    correctAnswer: 3,
    explanation: 'The `inc()` operator corresponds to `++`. The postfix `counter++` returns the old value but the variable is updated. The `println` after `++counter` would show the incremented value. Here we print after `++`.',
    codeSnippet: 'data class Counter(val value: Int) {\n    operator fun inc() = Counter(value + 1)\n}\nvar counter = Counter(5)\n++counter\nprintln(counter)',
    tags: ['operator-overloading', 'inc', 'increment'],
  },
  {
    id: 'kotlin-core-l3-028',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Operator Overloading',
    question: 'Which operator function enables the `()` invoke syntax on an object?',
    options: ['apply', 'call', 'execute', 'invoke'],
    correctAnswer: 3,
    explanation: 'The `invoke` operator allows calling an object as if it were a function. `obj()` translates to `obj.invoke()`. This is commonly used in DSLs.',
    codeSnippet: 'class Greeter(val greeting: String) {\n    operator fun invoke(name: String) = "$greeting, $name!"\n}\nval hello = Greeter("Hello")\nprintln(hello("World")) // Hello, World!',
    tags: ['operator-overloading', 'invoke'],
  },
  {
    id: 'kotlin-core-l3-031',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Destructuring',
    question: 'What is the output of this code?',
    options: ['first first', 'second first', 'first second', 'Compilation error'],
    correctAnswer: 2,
    explanation: 'The `to` infix function creates a `Pair`. Destructuring a `Pair` gives `component1()` and `component2()`. The pair `("first" to "second")` destructures to `a="first"`, `b="second"`.',
    codeSnippet: 'val (a, b) = "first" to "second"\nprintln("$a $b")',
    tags: ['destructuring', 'pair', 'infix'],
  },
  {
    id: 'kotlin-core-l3-032',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Lazy',
    question: 'What is the output of this code?',
    options: ['Computed\n42\n42', 'Computed\nComputed\n42\n42', '42\n42', 'Computed\n42\nComputed\n42'],
    correctAnswer: 0,
    explanation: 'The `lazy` block runs exactly once on first access. The first `println(value)` triggers computation, printing "Computed" then 42. The second `println(value)` returns the cached 42.',
    codeSnippet: 'val value: Int by lazy {\n    println("Computed")\n    42\n}\nprintln(value)\nprintln(value)',
    tags: ['lazy', 'caching', 'initialization'],
  },
  {
    id: 'kotlin-core-l3-033',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Lazy',
    question: 'What is the behavior of `lazy(LazyThreadSafetyMode.PUBLICATION)`?',
    options: [
      'No synchronization is used',
      'Multiple threads may compute the value, but only the first to complete is stored',
      'Only one thread can initialize the value',
      'The value is computed at class loading time',
    ],
    correctAnswer: 1,
    explanation: 'With `PUBLICATION` mode, multiple threads may invoke the initializer concurrently, but only the first result to complete is stored and returned. Subsequent threads see the stored result.',
    tags: ['lazy', 'thread-safety', 'PUBLICATION'],
  },
  {
    id: 'kotlin-core-l3-035',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Type Aliases',
    question: 'Can type aliases have type parameters?',
    options: [
      'No, type aliases are always concrete types',
      'Yes, they can be generic',
      'Only for function types',
      'Only with bounded type parameters',
    ],
    correctAnswer: 1,
    explanation: 'Type aliases can be generic. This is especially useful for abbreviating complex generic types.',
    codeSnippet: 'typealias Mapper<T, R> = (T) -> R\ntypealias StringMapper<R> = Mapper<String, R>\ntypealias NodeSet = Set<Network.Node>\ntypealias FileTable<K> = MutableMap<K, MutableList<File>>',
    tags: ['type-aliases', 'generics'],
  },
  {
    id: 'kotlin-core-l3-037',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Enums',
    question: 'Can enum classes have companion objects?',
    options: [
      'Only if the enum has no constructor parameters',
      'No, enums cannot have companion objects',
      'Only for Java interoperability',
      'Yes, and they can contain factory functions and shared state',
    ],
    correctAnswer: 3,
    explanation: 'Enum classes can have companion objects, which is useful for factory methods, parsing functions, or shared constants.',
    codeSnippet: 'enum class Direction {\n    NORTH, SOUTH, EAST, WEST;\n    companion object {\n        fun fromAngle(angle: Int): Direction = when (angle) {\n            in 315..360, in 0..45 -> NORTH\n            in 46..135 -> EAST\n            in 136..225 -> SOUTH\n            else -> WEST\n        }\n    }\n}',
    tags: ['enums', 'companion-objects', 'factory'],
  },
  {
    id: 'kotlin-core-l3-038',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Object Declarations',
    question: 'What is the output of this code?',
    options: ['Init', 'Hello\nInit', 'Init\nHello', 'Hello'],
    correctAnswer: 2,
    explanation: 'Object declarations are lazily initialized. When `Registry.greet()` is called, the object is initialized first (init block runs), then the method executes.',
    codeSnippet: 'object Registry {\n    init { println("Init") }\n    fun greet() = println("Hello")\n}\nRegistry.greet()',
    tags: ['object-declarations', 'lazy-initialization', 'init'],
  },
  {
    id: 'kotlin-core-l3-039',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Companion Objects',
    question: 'What is the output of this code?',
    options: ['42', 'null', 'Compilation error', 'Runtime error'],
    correctAnswer: 0,
    explanation: 'Extension functions on companion objects are called on the class name. `MyClass.factory()` calls the extension on `MyClass.Companion`, returning 42.',
    codeSnippet: 'class MyClass {\n    companion object\n}\nfun MyClass.Companion.factory(): Int = 42\n\nprintln(MyClass.factory())',
    tags: ['companion-objects', 'extension-functions'],
  },
  {
    id: 'kotlin-core-l3-041',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Null Safety',
    question: 'What is the output of this code?',
    options: ['[Alice, Charlie, null]', 'Compilation error', '[Alice, Charlie]', '[Alice, null, Charlie]'],
    correctAnswer: 2,
    explanation: 'The `mapNotNull` function applies the transformation and filters out null results in a single step. Names that are null after the `takeIf` check are excluded.',
    codeSnippet: 'val names = listOf("Alice", "", "Charlie", " ")\nval result = names.mapNotNull { it.takeIf { n -> n.isNotBlank() } }\nprintln(result)',
    tags: ['null-safety', 'mapNotNull', 'takeIf', 'collections'],
  },
  {
    id: 'kotlin-core-l3-042',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Scope Functions',
    question: 'What is the output of this code?',
    options: ['[1, 2, 3, 2, 4, 6]', '[2, 4, 6]', 'Compilation error', '[1, 2, 3]'],
    correctAnswer: 1,
    explanation: '`let` transforms the value and returns the lambda result. The list `[1, 2, 3]` is passed as `it` to `let`, and `it.map { n -> n * 2 }` creates a new list `[2, 4, 6]`.',
    codeSnippet: 'val result = listOf(1, 2, 3).let { list ->\n    list.map { it * 2 }\n}\nprintln(result)',
    tags: ['scope-functions', 'let', 'transformation'],
  },
  {
    id: 'kotlin-core-l3-043',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Extension Functions',
    question: 'What is the output of this code?',
    options: ['Nothing is printed', 'Compilation error', 'NullPointerException', 'Nullable receiver: null'],
    correctAnswer: 3,
    explanation: 'Extension functions can be defined on nullable receiver types. Inside the function, `this` can be null and must be handled. This allows calling the function on null values.',
    codeSnippet: 'fun String?.describe(): String = "Nullable receiver: $this"\n\nval s: String? = null\nprintln(s.describe())',
    tags: ['extension-functions', 'nullable-receiver'],
  },
  {
    id: 'kotlin-core-l3-045',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Delegation',
    question: 'What is the key limitation of class delegation with `by`?',
    options: [
      'The delegate cannot access the delegating class members; self-calls in the delegate go to the delegate, not the wrapper',
      'Delegated methods cannot be overridden',
      'Only one interface can be delegated',
      'Delegation only works with abstract classes',
    ],
    correctAnswer: 0,
    explanation: 'The delegate object has no reference to the outer class. If the delegate calls one of its own methods internally, it calls its own implementation, not any override in the delegating class. This differs from inheritance.',
    tags: ['delegation', 'limitations', 'self-calls'],
  },
  {
    id: 'kotlin-core-l3-046',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Properties',
    question: 'What is the difference between `lateinit var` and `by lazy` for deferred initialization?',
    options: [
      '`lateinit` is for `var` and non-nullable non-primitives; `lazy` is for `val` and works with any type including primitives',
      'They are identical in behavior',
      '`lateinit` is for `val`; `lazy` is for `var`',
      '`lateinit` is thread-safe; `lazy` is not',
    ],
    correctAnswer: 0,
    explanation: '`lateinit` works with `var`, cannot be used with primitive types or nullable types, and allows re-assignment. `lazy` works with `val`, supports any type including primitives, and is computed once (default thread-safe).',
    tags: ['properties', 'lateinit', 'lazy', 'comparison'],
  },
  {
    id: 'kotlin-core-l3-047',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Properties',
    question: 'What is the output of this code?',
    options: ['Getter\nGetter\n5\n5', '5\n5', 'Getter\n5\nGetter\n5', 'Compilation error'],
    correctAnswer: 2,
    explanation: 'The custom getter runs every time the property is accessed. Each `println(obj.value)` triggers the getter, which first prints "Getter" and then returns 5.',
    codeSnippet: 'class MyClass {\n    val value: Int\n        get() {\n            println("Getter")\n            return 5\n        }\n}\nval obj = MyClass()\nprintln(obj.value)\nprintln(obj.value)',
    tags: ['properties', 'custom-getter', 'side-effects'],
  },
  {
    id: 'kotlin-core-l3-048',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Sealed Classes',
    question: 'Can sealed classes have multiple levels of inheritance?',
    options: [
      'Only two levels of inheritance',
      'No, only direct subclasses are allowed',
      'Yes, indirect subclasses are allowed but only direct subclasses must be in the same module',
      'Only if all levels are sealed',
    ],
    correctAnswer: 2,
    explanation: 'Sealed classes restrict only their direct subclasses (must be in the same module/package). Indirect subclasses (subclasses of non-sealed direct subclasses) can be defined anywhere.',
    codeSnippet: 'sealed class Error\nopen class IOError : Error()\nclass FileNotFound : IOError() // Can be anywhere\nclass NetworkError : IOError() // Can be anywhere',
    tags: ['sealed-classes', 'inheritance', 'hierarchy'],
  },
  {
    id: 'kotlin-core-l3-049',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Type Checks',
    question: 'What is the output of this code?',
    options: ['false', 'Compilation error (cannot check erased type)', 'Runtime error', 'true'],
    correctAnswer: 3,
    explanation: '`is List<*>` uses a star projection which is valid at runtime. It checks that `obj` is some kind of `List` without checking the element type (which is erased).',
    codeSnippet: 'val obj: Any = listOf(1, 2, 3)\nprintln(obj is List<*>)',
    tags: ['type-checks', 'star-projection', 'type-erasure'],
  },
  {
    id: 'kotlin-core-l3-050',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Type Checks',
    question: 'What is the `as` operator used for in Kotlin?',
    options: [
      'Type checking like `is`',
      'Creating extension functions',
      'Creating type aliases',
      'Unsafe type casting that throws ClassCastException on failure',
    ],
    correctAnswer: 3,
    explanation: 'The `as` operator performs an unsafe (unchecked) cast. If the cast fails, it throws a `ClassCastException`. Use `as?` for safe casting that returns null on failure.',
    codeSnippet: 'val obj: Any = "Hello"\nval str: String = obj as String    // OK\nval num: Int = obj as Int          // ClassCastException!',
    tags: ['type-checks', 'as-operator', 'unsafe-cast'],
  },
  {
    id: 'kotlin-core-l3-052',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Scope Functions',
    question: 'What is the difference between `takeIf` and `takeUnless`?',
    options: [
      '`takeIf` throws on false; `takeUnless` returns null',
      '`takeIf` is for nullable types; `takeUnless` is for non-nullable',
      'They are identical',
      '`takeIf` returns the object if predicate is true; `takeUnless` returns it if predicate is false',
    ],
    correctAnswer: 3,
    explanation: '`takeIf` returns the receiver when the predicate is `true`, null otherwise. `takeUnless` is the opposite: it returns the receiver when the predicate is `false`, null when `true`.',
    codeSnippet: 'val x = -5\nprintln(x.takeIf { it > 0 })     // null\nprintln(x.takeUnless { it > 0 }) // -5',
    tags: ['scope-functions', 'takeIf', 'takeUnless'],
  },
  {
    id: 'kotlin-core-l3-053',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Data Classes',
    question: 'What is the output of this code?',
    options: ['2', '3', 'Compilation error', '1'],
    correctAnswer: 3,
    explanation: 'Data class destructuring uses `componentN()` functions based on constructor parameter order. `component1()` corresponds to the first constructor parameter `a`, which is 1.',
    codeSnippet: 'data class Triple(val a: Int, val b: Int, val c: Int)\nval t = Triple(1, 2, 3)\nprintln(t.component1())',
    tags: ['data-classes', 'componentN', 'destructuring'],
  },
  {
    id: 'kotlin-core-l3-054',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Inline Classes',
    question: 'What is the output of this code?',
    options: ['user@example.com', 'Compilation error', 'example.com', 'null'],
    correctAnswer: 2,
    explanation: 'Value classes can have computed properties (without backing fields) and methods. The `domain` property computes the domain part of the email on each access.',
    codeSnippet: '@JvmInline\nvalue class Email(val address: String) {\n    val domain: String\n        get() = address.substringAfter("@")\n}\nprintln(Email("user@example.com").domain)',
    tags: ['inline-classes', 'value-classes', 'computed-properties'],
  },
  {
    id: 'kotlin-core-l3-055',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'When Expression',
    question: 'What is the output of this code?',
    options: ['Odd', 'Compilation error', 'Even positive', 'Even'],
    correctAnswer: 2,
    explanation: 'When blocks can be nested and can use arbitrary expressions. The outer `when` checks if `x` is positive, and the inner `when` checks parity.',
    codeSnippet: 'val x = 4\nval result = when {\n    x > 0 -> when {\n        x % 2 == 0 -> "Even positive"\n        else -> "Odd positive"\n    }\n    x < 0 -> "Negative"\n    else -> "Zero"\n}\nprintln(result)',
    tags: ['when-expression', 'nested', 'complex-conditions'],
  },
  {
    id: 'kotlin-core-l3-056',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Extension Functions',
    question: 'What is the output of this code?',
    options: ['[1, 2, 3, 4, 5]', '[1, 2, 3]', 'Compilation error', '[5, 4, 3, 2, 1]'],
    correctAnswer: 0,
    explanation: 'The extension function `merge` is defined as an infix function on `List<T>`. It concatenates two lists. Since it is an extension, it does not modify the original lists.',
    codeSnippet: 'infix fun <T> List<T>.merge(other: List<T>): List<T> = this + other\n\nval result = listOf(1, 2, 3) merge listOf(4, 5)\nprintln(result)',
    tags: ['extension-functions', 'infix', 'generics'],
  },
  {
    id: 'kotlin-core-l3-058',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Operator Overloading',
    question: 'What is the output of this code?',
    options: ['ClassCastException', 'Compilation error', 'false', 'true'],
    correctAnswer: 3,
    explanation: 'The `compareTo` operator function enables comparison operators. `a > b` translates to `a.compareTo(b) > 0`. Since Temperature(100) > Temperature(50), the result is true.',
    codeSnippet: 'data class Temperature(val degrees: Double) : Comparable<Temperature> {\n    override fun compareTo(other: Temperature) =\n        degrees.compareTo(other.degrees)\n}\nprintln(Temperature(100.0) > Temperature(50.0))',
    tags: ['operator-overloading', 'compareTo', 'comparable'],
  },
  {
    id: 'kotlin-core-l3-059',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Properties',
    question: 'What is the output of this code?',
    options: ['hello', 'Compilation error', 'StackOverflowError', 'HELLO'],
    correctAnswer: 3,
    explanation: 'The custom setter uses the `field` keyword correctly. The setter uppercases the value and stores it in the backing field. Accessing the property returns the stored uppercased value.',
    codeSnippet: 'class Config {\n    var name: String = ""\n        set(value) {\n            field = value.uppercase()\n        }\n}\nval c = Config()\nc.name = "hello"\nprintln(c.name)',
    tags: ['properties', 'backing-fields', 'custom-setter'],
  },
  {
    id: 'kotlin-core-l3-060',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Delegation',
    question: 'What is the output of this code?',
    options: ['Count: 3', 'Compilation error', 'Count: 1', 'Count: 0'],
    correctAnswer: 0,
    explanation: 'The `notNull` delegate throws an `IllegalStateException` if accessed before being set. Here it is set to 3 before access, so it works. It is similar to `lateinit` but works with primitives.',
    codeSnippet: 'import kotlin.properties.Delegates\n\nvar count: Int by Delegates.notNull<Int>()\ncount = 3\nprintln("Count: $count")',
    tags: ['delegation', 'notNull', 'property-delegates'],
  },
  {
    id: 'kotlin-core-l3-061',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Sealed Interfaces',
    question: 'Can a data class implement a sealed interface from a different module?',
    options: [
      'Yes, sealed interfaces have no location restrictions',
      'Only if the data class is also sealed',
      'No, all implementations must be in the same module and package',
      'Only in test modules',
    ],
    correctAnswer: 2,
    explanation: 'Like sealed classes, sealed interfaces require all direct implementations to be in the same package and module. This restriction enables exhaustive `when` expressions at compile time.',
    tags: ['sealed-interfaces', 'module-restrictions'],
  },
  {
    id: 'kotlin-core-l3-062',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'String Templates',
    question: 'What is the output of this code?',
    options: ['Line 1\nLine 2', 'Line 1\n    Line 2', 'Line 1\\nLine 2', 'Compilation error'],
    correctAnswer: 0,
    explanation: '`trimMargin()` removes leading whitespace up to and including the margin prefix (default `|`). Each line is trimmed from the start to `|`, producing clean multi-line output.',
    codeSnippet: 'val text = """\n    |Line 1\n    |Line 2\n""".trimMargin()\nprintln(text)',
    tags: ['string-templates', 'raw-strings', 'trimMargin'],
  },
  {
    id: 'kotlin-core-l3-063',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Extension Functions',
    question: 'Can extension functions be virtual (dispatched dynamically)?',
    options: [
      'Only inside abstract classes',
      'Only if declared as open',
      'Yes, they are always virtual',
      'No, extension functions are always resolved statically at compile time',
    ],
    correctAnswer: 3,
    explanation: 'Extension functions are resolved statically based on the declared type of the expression, not the runtime type. They cannot be overridden in subclasses. This is a fundamental characteristic.',
    tags: ['extension-functions', 'static-dispatch', 'virtual'],
  },
  {
    id: 'kotlin-core-l3-065',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Null Safety',
    question: 'What is the output of this code?',
    options: ['Error', 'Compilation error', 'Success', 'null'],
    correctAnswer: 2,
    explanation: '`runCatching` wraps the block in a try-catch and returns a `Result`. `getOrNull()` returns the value if successful, null if failed. Here the block succeeds, so "Success" is returned.',
    codeSnippet: 'val result = runCatching {\n    "Success"\n}.getOrNull()\nprintln(result)',
    tags: ['null-safety', 'runCatching', 'result'],
  },
  {
    id: 'kotlin-core-l3-066',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Operator Overloading',
    question: 'What is the output of this code?',
    options: ['Matrix(4, 4, 4, 4)', 'Compilation error', 'Matrix(2, 2, 2, 2)', 'Matrix(3, 3, 3, 3)'],
    correctAnswer: 3,
    explanation: 'The `plus` operator adds corresponding elements of two matrices. 1+2=3 for all elements, producing Matrix(3, 3, 3, 3).',
    codeSnippet: 'data class Matrix(val a: Int, val b: Int, val c: Int, val d: Int) {\n    operator fun plus(other: Matrix) =\n        Matrix(a + other.a, b + other.b, c + other.c, d + other.d)\n}\nval m1 = Matrix(1, 1, 1, 1)\nval m2 = Matrix(2, 2, 2, 2)\nprintln(m1 + m2)',
    tags: ['operator-overloading', 'plus', 'data-classes'],
  },
  {
    id: 'kotlin-core-l3-067',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Operator Overloading',
    question: 'Can operators be defined as extension functions?',
    options: [
      'No, operators must be member functions',
      'Only for built-in types',
      'Only the plus operator',
      'Yes, operators can be defined as extension functions with the operator modifier',
    ],
    correctAnswer: 3,
    explanation: 'Operator functions can be defined as extension functions. This is useful for adding operator support to classes you do not own.',
    codeSnippet: 'operator fun String.times(n: Int): String = repeat(n)\n\nprintln("ha" * 3) // hahaha',
    tags: ['operator-overloading', 'extension-functions'],
  },
  {
    id: 'kotlin-core-l3-069',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Smart Casts',
    question: 'What is the output of this code?',
    options: ['null', 'Compilation error', 'Hello', 'ClassCastException'],
    correctAnswer: 2,
    explanation: 'Smart cast works with `&&` in conditions. After `obj != null && obj is String`, the compiler knows `obj` is a non-null `String` in the then-branch, allowing `obj.uppercase()` to be called, but here we just pass it through.',
    codeSnippet: 'fun safe(obj: Any?): String {\n    if (obj is String) return obj\n    return "Not a string"\n}\nprintln(safe("Hello"))',
    tags: ['smart-casts', 'nullable', 'return'],
  },
  {
    id: 'kotlin-core-l3-071',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Delegation',
    question: 'What is the output of this code?',
    options: ['NoSuchElementException', 'null', 'Alice (age 30)', 'Compilation error'],
    correctAnswer: 2,
    explanation: 'Properties can be delegated to a `Map`. The property name is used as the map key to look up the value. `name` looks up "name" and `age` looks up "age" in the map.',
    codeSnippet: 'class Person(map: Map<String, Any?>) {\n    val name: String by map\n    val age: Int by map\n}\nval p = Person(mapOf("name" to "Alice", "age" to 30))\nprintln("${p.name} (age ${p.age})")',
    tags: ['delegation', 'map-delegation'],
  },
  {
    id: 'kotlin-core-l3-072',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Properties',
    question: 'Can `const val` be used with non-primitive types other than String?',
    options: [
      'No, `const val` only works with primitives and String',
      'Only with data classes',
      'Yes, with any type',
      'Only with enums',
    ],
    correctAnswer: 0,
    explanation: '`const val` can only be used with primitive types (Int, Long, Double, etc.) and String. The value must be known at compile time. It cannot be used with arbitrary objects.',
    tags: ['properties', 'const', 'restrictions'],
  },
  {
    id: 'kotlin-core-l3-074',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Object Declarations',
    question: 'Can an object declaration inherit from a class?',
    options: [
      'No, objects can only implement interfaces',
      'Only abstract classes',
      'Yes, objects can extend classes and implement interfaces',
      'Only sealed classes',
    ],
    correctAnswer: 2,
    explanation: 'Object declarations can extend classes (including abstract ones) and implement interfaces. This is useful for creating singleton implementations of abstract types.',
    codeSnippet: 'abstract class Animal(val name: String) {\n    abstract fun sound(): String\n}\nobject Cat : Animal("Cat") {\n    override fun sound() = "Meow"\n}',
    tags: ['object-declarations', 'inheritance', 'abstract-classes'],
  },
  {
    id: 'kotlin-core-l3-075',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Inline Classes',
    question: 'Can value classes have secondary constructors?',
    options: [
      'Only if marked with @JvmOverloads',
      'No, value classes cannot have secondary constructors',
      'Yes, they can have multiple constructors',
      'Only if the primary constructor has a default value',
    ],
    correctAnswer: 1,
    explanation: 'Value classes cannot have secondary constructors. They must have exactly one primary constructor parameter. For alternative construction, use companion object factory functions.',
    codeSnippet: '@JvmInline\nvalue class Percentage(val value: Double) {\n    companion object {\n        fun fromFraction(f: Double) = Percentage(f * 100)\n    }\n}',
    tags: ['inline-classes', 'value-classes', 'constructors'],
  },
  {
    id: 'kotlin-core-l3-078',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Extension Properties',
    question: 'Can extension properties have initializers?',
    options: [
      'Yes, just like regular properties',
      'Only in companion objects',
      'Only with const val',
      'No, because they cannot have backing fields',
    ],
    correctAnswer: 3,
    explanation: 'Extension properties cannot have initializers because they do not have backing fields. The property is not actually added to the class. They must always define a custom getter.',
    tags: ['extension-properties', 'no-initializer', 'backing-fields'],
  },
  {
    id: 'kotlin-core-l3-080',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Lazy',
    question: 'Can `lazy` delegates be used for local variables?',
    options: [
      'Only in main functions',
      'Only with top-level variables',
      'Yes, lazy can be used with local variables in functions',
      'No, lazy only works with class properties',
    ],
    correctAnswer: 2,
    explanation: 'Since Kotlin 1.1, `by lazy` can be used with local variables inside functions. The lazy initialization occurs on first access within the function execution.',
    codeSnippet: 'fun process() {\n    val expensiveResult by lazy {\n        println("Computing...")\n        42\n    }\n    println("Before access")\n    println(expensiveResult) // "Computing..." then 42\n}',
    tags: ['lazy', 'local-variables'],
  },
  {
    id: 'kotlin-core-l3-082',
    language: 'kotlin',
    level: 'level3',
    category: 'Core Kotlin',
    subcategory: 'Enums',
    question: 'How do you iterate over all enum values and filter them?',
    options: [
      'Using entries property with collection operations',
      'Using a for loop with values()',
      'Only with when expressions',
      'Enums cannot be iterated',
    ],
    correctAnswer: 0,
    explanation: 'The `entries` property (Kotlin 1.9+) returns a `List` of all enum constants that supports standard collection operations like `filter`, `map`, `forEach`, etc.',
    codeSnippet: 'enum class Priority(val level: Int) {\n    LOW(1), MEDIUM(2), HIGH(3), CRITICAL(4);\n}\nval urgent = Priority.entries.filter { it.level >= 3 }\nprintln(urgent) // [HIGH, CRITICAL]',
    tags: ['enums', 'entries', 'collections', 'filter'],
  }
];

export default questions;
