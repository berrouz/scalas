import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'kotlin-ktor-l3-001',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you create a custom Ktor plugin using the createApplicationPlugin API?',
    options: [
      'val MyPlugin = createApplicationPlugin("MyPlugin") { onCall { } }',
      'object MyPlugin : ApplicationPlugin { override fun install() { } }',
      'class MyPlugin : Plugin { }',
      'fun createPlugin(): ApplicationPlugin { }',
    ],
    correctAnswer: 0,
    explanation: 'The createApplicationPlugin() function is the modern way to create custom plugins in Ktor 2.x. It provides hooks like onCall, onCallReceive, onCallRespond for intercepting the request pipeline.',
    codeSnippet: 'val MyPlugin = createApplicationPlugin(\"MyPlugin\") {\n    onCall { call ->\n        call.response.headers.append(\"X-Custom\", \"value\")\n    }\n}',
    tags: ['ktor', 'plugins', 'custom-plugin', 'createApplicationPlugin'],
  },
  {
    id: 'kotlin-ktor-l3-002',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'What hooks are available in createApplicationPlugin?',
    options: [
      'beforeRequest, afterResponse only',
      'onCall, onCallReceive, onCallRespond, and pipeline phase hooks',
      'onRequest, onResponse only',
      'intercept, transform only',
    ],
    correctAnswer: 1,
    explanation: 'createApplicationPlugin provides onCall (intercept the call), onCallReceive (intercept receiving body), onCallRespond (intercept sending response), and hooks for specific pipeline phases.',
    tags: ['ktor', 'plugins', 'hooks'],
  },
  {
    id: 'kotlin-ktor-l3-003',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you create a custom plugin with configuration in Ktor?',
    options: [
      'Use @ConfigProperty annotations',
      'createApplicationPlugin with a PluginConfig class',
      'Pass a Map to the plugin constructor',
      'Define a companion object with defaults',
    ],
    correctAnswer: 1,
    explanation: 'You define a configuration class and pass it as a type parameter to createApplicationPlugin. The configuration is populated in the install block.',
    codeSnippet: 'class RateLimitConfig {\n    var requestsPerSecond: Int = 10\n}\n\nval RateLimitPlugin = createApplicationPlugin(\n    \"RateLimit\",\n    createConfiguration = ::RateLimitConfig\n) {\n    val limit = pluginConfig.requestsPerSecond\n    onCall { call ->\n        // rate limit logic\n    }\n}',
    tags: ['ktor', 'plugins', 'configuration', 'custom-plugin'],
  },
  {
    id: 'kotlin-ktor-l3-004',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'What is the difference between createApplicationPlugin and createRouteScopedPlugin?',
    options: [
      'createApplicationPlugin applies globally; createRouteScopedPlugin applies only to specific routes',
      'createRouteScopedPlugin is deprecated',
      'No difference',
      'createApplicationPlugin is for client only',
    ],
    correctAnswer: 0,
    explanation: 'createApplicationPlugin creates a plugin that applies to the entire application, while createRouteScopedPlugin creates a plugin that can be installed on specific routes only.',
    codeSnippet: 'val RoutePlugin = createRouteScopedPlugin(\"RoutePlugin\") {\n    onCall { call ->\n        // only applies to routes where installed\n    }\n}\n\nrouting {\n    route(\"/api\") {\n        install(RoutePlugin)\n        get { /* protected by RoutePlugin */ }\n    }\n}',
    tags: ['ktor', 'plugins', 'route-scoped'],
  },
  {
    id: 'kotlin-ktor-l3-005',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'What is the Ktor application pipeline and what are its phases?',
    options: [
      'A CI/CD pipeline',
      'A build pipeline with compile, test, deploy phases',
      'A data transformation pipeline',
      'A request processing pipeline with phases like Setup, Monitoring, Plugins, Call, Fallback',
    ],
    correctAnswer: 3,
    explanation: 'The Ktor application pipeline processes requests through ordered phases: Setup, Monitoring, Plugins (formerly Features), Call, and Fallback. Plugins intercept specific phases to add functionality.',
    tags: ['ktor', 'pipeline', 'phases'],
  },
  {
    id: 'kotlin-ktor-l3-006',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you intercept a specific pipeline phase in Ktor?',
    options: [
      'hook(Plugins) { }',
      'on(Phase.Plugins) { }',
      'addPhaseInterceptor(Plugins) { }',
      'intercept(ApplicationCallPipeline.Plugins) { }',
    ],
    correctAnswer: 3,
    explanation: 'You use intercept() on the application call pipeline, specifying the phase to intercept. The interceptor lambda receives PipelineContext and ApplicationCall.',
    codeSnippet: 'intercept(ApplicationCallPipeline.Plugins) {\n    val startTime = System.currentTimeMillis()\n    proceed()\n    val duration = System.currentTimeMillis() - startTime\n    call.response.headers.append(\"X-Duration\", \"$duration ms\")\n}',
    tags: ['ktor', 'pipeline', 'intercept'],
  },
  {
    id: 'kotlin-ktor-l3-007',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Authentication',
    question: 'How do you implement OAuth 2.0 authentication in Ktor?',
    options: [
      'install(OAuthPlugin) { }',
      'oauth("provider") { } inside Authentication install block',
      'install(OAuth2) { }',
      'install(Authentication) { oAuth2 { } }',
    ],
    correctAnswer: 1,
    explanation: 'OAuth is configured using oauth() inside the Authentication install block. You provide the URL provider, client, and a callback URL for the OAuth flow.',
    codeSnippet: 'install(Authentication) {\n    oauth(\"google\") {\n        urlProvider = { \"http://localhost:8080/callback\" }\n        providerLookup = {\n            OAuthServerSettings.OAuth2ServerSettings(\n                name = \"google\",\n                authorizeUrl = \"https://accounts.google.com/o/oauth2/auth\",\n                accessTokenUrl = \"https://oauth2.googleapis.com/token\",\n                clientId = \"client-id\",\n                clientSecret = \"client-secret\",\n                requestMethod = HttpMethod.Post,\n                defaultScopes = listOf(\"profile\", \"email\")\n            )\n        }\n        client = httpClient\n    }\n}',
    tags: ['ktor', 'authentication', 'oauth', 'oauth2'],
  },
  {
    id: 'kotlin-ktor-l3-008',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Authentication',
    question: 'How do you handle the OAuth callback in Ktor?',
    options: [
      'Register an OAuthCallback handler',
      'Use @OAuthCallback annotation',
      'Handle it in the oauth configuration directly',
      'Define a route at the callback URL inside authenticate block',
    ],
    correctAnswer: 3,
    explanation: 'You define a route at the callback URL inside an authenticate() block. After successful authentication, the OAuthAccessTokenResponse principal is available via call.principal().',
    codeSnippet: 'routing {\n    authenticate(\"google\") {\n        get(\"/login\") {\n            // Redirects to Google\n        }\n        get(\"/callback\") {\n            val principal = call.principal<OAuthAccessTokenResponse.OAuth2>()\n            val accessToken = principal?.accessToken\n            // Use token to fetch user info\n        }\n    }\n}',
    tags: ['ktor', 'authentication', 'oauth', 'callback'],
  },
  {
    id: 'kotlin-ktor-l3-009',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Authentication',
    question: 'How do you create and sign a JWT token in a Ktor application?',
    options: [
      'JWTToken.generate(claims)',
      'call.createJWT(claims)',
      'JWT.create().withIssuer("app").sign(algorithm)',
      'Authentication.generateToken(claims)',
    ],
    correctAnswer: 2,
    explanation: 'JWT tokens are created using the java-jwt library (com.auth0:java-jwt). You use JWT.create() builder to set claims, issuer, expiration, and then sign with an algorithm.',
    codeSnippet: 'val token = JWT.create()\n    .withIssuer(\"ktor-app\")\n    .withClaim(\"username\", user.name)\n    .withExpiresAt(Date(System.currentTimeMillis() + 3600000))\n    .sign(Algorithm.HMAC256(\"secret\"))',
    tags: ['ktor', 'jwt', 'token-creation'],
  },
  {
    id: 'kotlin-ktor-l3-010',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Authentication',
    question: 'How do you access JWT claims in a Ktor route handler?',
    options: [
      'call.jwtClaims["username"]',
      'call.principal<JWTPrincipal>()?.payload?.getClaim("username")',
      'call.jwt.claim("username")',
      'call.authentication.claims["username"]',
    ],
    correctAnswer: 1,
    explanation: 'After JWT authentication, you access claims via the JWTPrincipal. The payload property gives access to all JWT claims through getClaim().',
    codeSnippet: 'authenticate(\"auth-jwt\") {\n    get(\"/me\") {\n        val principal = call.principal<JWTPrincipal>()\n        val username = principal?.payload?.getClaim(\"username\")?.asString()\n        val expiresAt = principal?.expiresAt\n        call.respondText(\"Hello, $username\")\n    }\n}',
    tags: ['ktor', 'jwt', 'claims', 'principal'],
  },
  {
    id: 'kotlin-ktor-l3-011',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'WebSockets',
    question: 'How do you implement a WebSocket broadcast to all connected clients in Ktor?',
    options: [
      'call.respondToAll("message")',
      'Maintain a collection of WebSocketSession objects and iterate to send',
      'WebSocket.broadcastAll("message")',
      'ws.broadcast("message")',
    ],
    correctAnswer: 1,
    explanation: 'Ktor does not have built-in broadcast. You maintain a thread-safe collection of active WebSocketSession objects and iterate over them to send messages to all clients.',
    codeSnippet: 'val sessions = Collections.synchronizedSet<WebSocketSession>(LinkedHashSet())\n\nwebSocket(\"/chat\") {\n    sessions += this\n    try {\n        for (frame in incoming) {\n            if (frame is Frame.Text) {\n                val text = frame.readText()\n                sessions.forEach { it.send(text) }\n            }\n        }\n    } finally {\n        sessions -= this\n    }\n}',
    tags: ['ktor', 'websockets', 'broadcast'],
  },
  {
    id: 'kotlin-ktor-l3-012',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'WebSockets',
    question: 'How do you handle WebSocket close events in Ktor?',
    options: [
      'onClose { reason -> }',
      'The closeReason property is available after the incoming channel completes',
      'ws.addEventListener("close") { }',
      'addCloseHandler { }',
    ],
    correctAnswer: 1,
    explanation: 'When the WebSocket session ends, the incoming channel completes. You can then check closeReason.await() to get the close reason (code and message).',
    codeSnippet: 'webSocket(\"/ws\") {\n    try {\n        for (frame in incoming) { /* handle frames */ }\n    } finally {\n        val reason = closeReason.await()\n        println(\"Closed: ${reason?.code} ${reason?.message}\")\n    }\n}',
    tags: ['ktor', 'websockets', 'close'],
  },
  {
    id: 'kotlin-ktor-l3-013',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'WebSockets',
    question: 'How do you implement WebSocket serialization with kotlinx.serialization in Ktor?',
    options: [
      'Use sendSerialized() and receiveDeserialized() with ContentNegotiation configured for WebSockets',
      'install(WebSockets) { json() }',
      'webSocket { serialize = true }',
      'install(WebSocketSerialization) { }',
    ],
    correctAnswer: 0,
    explanation: 'With ContentNegotiation configured in the WebSockets plugin, you can use sendSerialized<T>() and receiveDeserialized<T>() to automatically serialize/deserialize WebSocket messages.',
    codeSnippet: 'install(WebSockets) {\n    contentConverter = KotlinxWebsocketSerializationConverter(Json)\n}\n\nwebSocket(\"/ws\") {\n    val message = receiveDeserialized<ChatMessage>()\n    sendSerialized(ChatResponse(\"received\"))\n}',
    tags: ['ktor', 'websockets', 'serialization'],
  },
  {
    id: 'kotlin-ktor-l3-014',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you implement connection pooling in Ktor HttpClient?',
    options: [
      'install(ConnectionPool) { maxConnections = 100 }',
      'HttpClient(CIO) { pool { size = 100 } }',
      'client.pool = ConnectionPool(100)',
      'Configure maxConnectionsCount in the engine block',
    ],
    correctAnswer: 3,
    explanation: 'Connection pooling is configured in the engine { } block. Different engines have different configuration options for pool size, keep-alive, and connection limits.',
    codeSnippet: 'val client = HttpClient(CIO) {\n    engine {\n        maxConnectionsCount = 1000\n        endpoint {\n            maxConnectionsPerRoute = 100\n            pipelineMaxSize = 20\n            keepAliveTime = 5000\n            connectTimeout = 5000\n        }\n    }\n}',
    tags: ['ktor', 'client', 'connection-pool'],
  },
  {
    id: 'kotlin-ktor-l3-015',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you handle streaming responses in Ktor HttpClient?',
    options: [
      'Use response.bodyAsChannel() to get a ByteReadChannel',
      'client.getStream("url")',
      'response.asStream()',
      'client.stream("url") { }',
    ],
    correctAnswer: 0,
    explanation: 'For streaming responses, use response.bodyAsChannel() which returns a ByteReadChannel. You can read from it incrementally without loading the entire response into memory.',
    codeSnippet: 'client.prepareGet(\"http://example.com/large-file\").execute { response ->\n    val channel = response.bodyAsChannel()\n    val file = File(\"output.dat\")\n    file.outputStream().use { output ->\n        val buffer = ByteArray(8192)\n        while (!channel.isClosedForRead) {\n            val read = channel.readAvailable(buffer)\n            if (read > 0) output.write(buffer, 0, read)\n        }\n    }\n}',
    tags: ['ktor', 'client', 'streaming', 'ByteReadChannel'],
  },
  {
    id: 'kotlin-ktor-l3-016',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'What is the difference between client.get() and client.prepareGet() in Ktor?',
    options: [
      'get() is synchronous, prepareGet() is asynchronous',
      'get() executes immediately and reads the body; prepareGet() returns a statement for deferred execution',
      'No difference',
      'prepareGet() is for testing only',
    ],
    correctAnswer: 1,
    explanation: 'client.get() executes the request and fully reads the response. client.prepareGet() creates an HttpStatement that can be executed later with execute { }, useful for streaming where you want to process the body incrementally.',
    tags: ['ktor', 'client', 'prepareGet', 'HttpStatement'],
  },
  {
    id: 'kotlin-ktor-l3-017',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you upload a file using multipart form data with Ktor HttpClient?',
    options: [
      'client.post(url) { file(file) }',
      'client.submitFormWithBinaryData(url, formData { append("file", file.readBytes(), headers) })',
      'client.upload(file)',
      'client.postMultipart(url, file)',
    ],
    correctAnswer: 1,
    explanation: 'Use client.submitFormWithBinaryData() with formData { } to upload files. The formData builder lets you append binary data with headers specifying filename and content type.',
    codeSnippet: 'val response = client.submitFormWithBinaryData(\n    url = \"http://localhost:8080/upload\",\n    formData = formData {\n        append(\"file\", File(\"photo.jpg\").readBytes(), Headers.build {\n            append(HttpHeaders.ContentType, \"image/jpeg\")\n            append(HttpHeaders.ContentDisposition, \"filename=\\\"photo.jpg\\\"\")\n        })\n    }\n)',
    tags: ['ktor', 'client', 'multipart', 'file-upload'],
  },
  {
    id: 'kotlin-ktor-l3-018',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Testing',
    question: 'How do you test WebSocket routes in Ktor?',
    options: [
      'testWebSocket { }',
      'testApplication { ws("/path") { } }',
      'Use createClient with WebSockets plugin and call webSocket() in testApplication',
      'mockWebSocket { }',
    ],
    correctAnswer: 2,
    explanation: 'In testApplication, create a client with the WebSockets plugin installed, then use client.webSocket() to connect and test WebSocket routes.',
    codeSnippet: 'testApplication {\n    application { configureWebSockets() }\n    val client = createClient {\n        install(WebSockets)\n    }\n    client.webSocket(\"/ws\") {\n        send(\"Hello\")\n        val response = (incoming.receive() as Frame.Text).readText()\n        assertEquals(\"Echo: Hello\", response)\n    }\n}',
    tags: ['ktor', 'testing', 'websockets'],
  },
  {
    id: 'kotlin-ktor-l3-019',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Testing',
    question: 'How do you mock external HTTP calls in Ktor tests?',
    options: [
      'Mock the URL class',
      'Use WireMock only',
      'Use MockServer library',
      'Use Ktor MockEngine for the HttpClient',
    ],
    correctAnswer: 3,
    explanation: 'Ktor provides a MockEngine for the HttpClient that lets you define programmatic responses for requests. This avoids making real HTTP calls in tests.',
    codeSnippet: 'val mockEngine = MockEngine { request ->\n    when (request.url.encodedPath) {\n        \"/api/users\" -> respond(\n            content = \"\"\"{\"name\":\"Alice\"}\"\"\",\n            status = HttpStatusCode.OK,\n            headers = headersOf(HttpHeaders.ContentType, \"application/json\")\n        )\n        else -> respondError(HttpStatusCode.NotFound)\n    }\n}\nval client = HttpClient(mockEngine) {\n    install(ContentNegotiation) { json() }\n}',
    tags: ['ktor', 'testing', 'MockEngine', 'client'],
  },
  {
    id: 'kotlin-ktor-l3-020',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Testing',
    question: 'How do you verify that a MockEngine received specific requests?',
    options: [
      'mockEngine.assertCalled("/path")',
      'mockEngine.verify { }',
      'Access mockEngine.requestHistory to inspect recorded requests',
      'MockEngine automatically asserts',
    ],
    correctAnswer: 2,
    explanation: 'MockEngine keeps a requestHistory list of all requests it received. You can inspect this list in your test assertions to verify request URLs, headers, bodies, etc.',
    codeSnippet: 'val mockEngine = MockEngine { respond(\"OK\") }\nval client = HttpClient(mockEngine)\nclient.get(\"http://example.com/api\")\n\nassertEquals(1, mockEngine.requestHistory.size)\nassertEquals(\"/api\", mockEngine.requestHistory[0].url.encodedPath)',
    tags: ['ktor', 'testing', 'MockEngine', 'verification'],
  },
  {
    id: 'kotlin-ktor-l3-021',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement dependency injection in a Ktor application?',
    options: [
      'Use a DI framework like Koin, Kodein, or Dagger and integrate it with Ktor modules',
      'Use @Inject annotations',
      'Use ApplicationContext from Spring',
      'Ktor has built-in DI',
    ],
    correctAnswer: 0,
    explanation: 'Ktor does not have built-in dependency injection. Koin is the most popular choice and integrates cleanly with Ktor through the koin-ktor extension module.',
    codeSnippet: 'fun Application.module() {\n    install(Koin) {\n        modules(module {\n            single<UserRepository> { UserRepositoryImpl() }\n            single { UserService(get()) }\n        })\n    }\n    routing {\n        val service by inject<UserService>()\n        get(\"/users\") { call.respond(service.getAll()) }\n    }\n}',
    tags: ['ktor', 'dependency-injection', 'koin'],
  },
  {
    id: 'kotlin-ktor-l3-022',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement graceful shutdown in a Ktor application?',
    options: [
      'server.shutdown()',
      'server.stop(gracePeriodMillis, timeoutMillis)',
      'server.close()',
      'server.stop()',
    ],
    correctAnswer: 1,
    explanation: 'server.stop(gracePeriodMillis, timeoutMillis) performs a graceful shutdown. During the grace period, the server stops accepting new connections but finishes processing existing ones.',
    codeSnippet: 'val server = embeddedServer(Netty, port = 8080) { module() }\nserver.start(wait = false)\n\nRuntime.getRuntime().addShutdownHook(Thread {\n    server.stop(1000, 5000) // 1s grace, 5s timeout\n})',
    tags: ['ktor', 'server', 'graceful-shutdown'],
  },
  {
    id: 'kotlin-ktor-l3-023',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Routing',
    question: 'How do you implement route-level middleware (interceptors) in Ktor?',
    options: [
      'Define middleware classes',
      'Use @Middleware annotation',
      'Install a route-scoped plugin or use intercept on the route',
      'Use before { } and after { } blocks',
    ],
    correctAnswer: 2,
    explanation: 'Ktor does not have Express-style middleware. Instead, you can install route-scoped plugins (createRouteScopedPlugin) or intercept specific phases on the Route object.',
    codeSnippet: 'routing {\n    route(\"/api\") {\n        install(MyRoutePlugin)\n        // Or use intercept:\n        intercept(ApplicationCallPipeline.Plugins) {\n            val apiKey = call.request.headers[\"X-API-Key\"]\n            if (apiKey == null) {\n                call.respond(HttpStatusCode.Unauthorized)\n                finish()\n            }\n        }\n        get(\"/data\") { call.respond(\"protected data\") }\n    }\n}',
    tags: ['ktor', 'routing', 'interceptor', 'middleware'],
  },
  {
    id: 'kotlin-ktor-l3-024',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'What does the `proceed()` function do inside a pipeline interceptor?',
    options: [
      'Restarts the pipeline',
      'Continues execution of the pipeline, calling the next interceptor',
      'Commits the response',
      'Skips the current phase',
    ],
    correctAnswer: 1,
    explanation: 'proceed() continues the pipeline execution to the next interceptor. Without calling proceed(), the pipeline is effectively short-circuited at the current interceptor.',
    tags: ['ktor', 'pipeline', 'proceed', 'interceptor'],
  },
  {
    id: 'kotlin-ktor-l3-025',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'What does the `finish()` function do inside a pipeline interceptor?',
    options: [
      'Closes the connection',
      'Throws an exception',
      'Sends the response immediately',
      'Completes the pipeline execution without calling subsequent interceptors in the current phase',
    ],
    correctAnswer: 3,
    explanation: 'finish() stops execution of subsequent interceptors in the current pipeline phase. It is used to short-circuit processing, for example when sending an error response.',
    tags: ['ktor', 'pipeline', 'finish', 'interceptor'],
  },
  {
    id: 'kotlin-ktor-l3-026',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Content Negotiation',
    question: 'How do you implement custom content transformation in Ktor?',
    options: [
      'Use @Converter annotation',
      'Override the ContentConverter class',
      'Implement the ContentConverter interface with serialize() and deserialize() methods',
      'Register a lambda with contentType',
    ],
    correctAnswer: 2,
    explanation: 'To support custom content types, implement the ContentConverter interface with serialize() and deserialize() methods, then register it in ContentNegotiation with register().',
    codeSnippet: 'class CsvConverter : ContentConverter {\n    override suspend fun serialize(\n        contentType: ContentType,\n        charset: Charset,\n        typeInfo: TypeInfo,\n        value: Any?\n    ): OutgoingContent? { /* convert to CSV */ }\n\n    override suspend fun deserialize(\n        charset: Charset,\n        typeInfo: TypeInfo,\n        content: ByteReadChannel\n    ): Any? { /* parse CSV */ }\n}',
    tags: ['ktor', 'content-negotiation', 'ContentConverter', 'custom'],
  },
  {
    id: 'kotlin-ktor-l3-027',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Sessions',
    question: 'How do you encrypt session data in Ktor?',
    options: [
      'Use transform(SessionTransportTransformerEncrypt(encryptKey, signKey))',
      'install(EncryptedSessions) { }',
      'cookie<Session>("name") { encrypt = true }',
      'cookie<Session>("name", encrypted = true)',
    ],
    correctAnswer: 0,
    explanation: 'Session encryption is applied using SessionTransportTransformerEncrypt. You provide encryption and signing keys. The transformer encrypts session data before sending and decrypts on receipt.',
    codeSnippet: 'install(Sessions) {\n    cookie<UserSession>(\"session\") {\n        cookie.path = \"/\"\n        transform(\n            SessionTransportTransformerEncrypt(\n                hex(\"0123456789abcdef0123456789abcdef\"),\n                hex(\"abcdef0123456789\")\n            )\n        )\n    }\n}',
    tags: ['ktor', 'sessions', 'encryption', 'security'],
  },
  {
    id: 'kotlin-ktor-l3-028',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Sessions',
    question: 'How do you sign (but not encrypt) session cookies in Ktor?',
    options: [
      'sign(key)',
      'cookie.secure = true',
      'cookie.signed = true',
      'transform(SessionTransportTransformerMessageAuthentication(key))',
    ],
    correctAnswer: 3,
    explanation: 'SessionTransportTransformerMessageAuthentication signs the session data with HMAC to prevent tampering, without encrypting the content. The session data is visible but cannot be modified.',
    codeSnippet: 'install(Sessions) {\n    cookie<UserSession>(\"session\") {\n        transform(\n            SessionTransportTransformerMessageAuthentication(\n                hex(\"6819b57a326945c1968f45236589\")\n            )\n        )\n    }\n}',
    tags: ['ktor', 'sessions', 'signing', 'hmac'],
  },
  {
    id: 'kotlin-ktor-l3-029',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Sessions',
    question: 'How do you transport sessions via HTTP headers instead of cookies in Ktor?',
    options: [
      'install(Sessions) { headerSession("name") }',
      'session(transport = SessionTransport.Header)',
      'cookie<Session>("name", transport = Header)',
      'header<UserSession>("X-Session")',
    ],
    correctAnswer: 3,
    explanation: 'Use header<SessionType>("headerName") instead of cookie<SessionType>() to transport sessions via HTTP headers. This is useful for API-only applications.',
    codeSnippet: 'install(Sessions) {\n    header<UserSession>(\"X-Session-Id\", SessionStorageMemory()) {\n        transform(SessionTransportTransformerMessageAuthentication(secretKey))\n    }\n}',
    tags: ['ktor', 'sessions', 'header-transport'],
  },
  {
    id: 'kotlin-ktor-l3-030',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Deployment',
    question: 'How do you configure Ktor to run behind a reverse proxy (Nginx)?',
    options: [
      'Install ForwardedHeaders or XForwardedHeaders plugin',
      'Set ktor.proxy = true in application.conf',
      'Configure the server engine with proxyMode = true',
      'No configuration needed',
    ],
    correctAnswer: 0,
    explanation: 'When behind a reverse proxy, install ForwardedHeaders (for standard Forwarded header) or XForwardedHeaders (for X-Forwarded-* headers) to correctly determine client IP and protocol.',
    codeSnippet: 'install(XForwardedHeaders)\n// or\ninstall(ForwardedHeaders)',
    tags: ['ktor', 'deployment', 'reverse-proxy', 'nginx'],
  },
  {
    id: 'kotlin-ktor-l3-031',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Deployment',
    question: 'How do you configure Ktor for GraalVM native image compilation?',
    options: [
      'Use the ktor-server-cio engine and configure reflection/serialization for GraalVM',
      'Add the graalvm plugin',
      'Just run native-image on the fat JAR',
      'GraalVM native images are not supported',
    ],
    correctAnswer: 0,
    explanation: 'GraalVM native images work best with CIO engine (no external JNI dependencies). You need to configure reflection metadata for kotlinx.serialization and any reflective operations.',
    tags: ['ktor', 'deployment', 'graalvm', 'native-image'],
  },
  {
    id: 'kotlin-ktor-l3-032',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Deployment',
    question: 'How do you deploy a Ktor application as a WAR file?',
    options: [
      'Use ktor-server-servlet dependency and configure web.xml or use the war plugin',
      'WAR deployment is not supported',
      'Convert the JAR to WAR manually',
      'Use ktor-server-war only',
    ],
    correctAnswer: 0,
    explanation: 'Ktor supports WAR deployment via the ktor-server-servlet dependency. You configure web.xml to point to the ServletApplicationEngine and specify your application modules.',
    tags: ['ktor', 'deployment', 'war', 'servlet'],
  },
  {
    id: 'kotlin-ktor-l3-033',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Metrics',
    question: 'How do you expose a Prometheus metrics endpoint in Ktor?',
    options: [
      'install(Prometheus) { endpoint = "/metrics" }',
      'Configure MicrometerMetrics with PrometheusMeterRegistry and create a route that calls registry.scrape()',
      'metrics.expose("/prometheus")',
      'install(PrometheusEndpoint) { }',
    ],
    correctAnswer: 1,
    explanation: 'After installing MicrometerMetrics with a PrometheusMeterRegistry, create a GET route (usually /metrics) that responds with registry.scrape() to expose Prometheus-format metrics.',
    codeSnippet: 'val prometheusRegistry = PrometheusMeterRegistry(PrometheusConfig.DEFAULT)\ninstall(MicrometerMetrics) {\n    registry = prometheusRegistry\n}\nrouting {\n    get(\"/metrics\") {\n        call.respondText(prometheusRegistry.scrape())\n    }\n}',
    tags: ['ktor', 'metrics', 'prometheus', 'micrometer'],
  },
  {
    id: 'kotlin-ktor-l3-034',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Metrics',
    question: 'How do you add custom metrics in Ktor with Micrometer?',
    options: [
      'Ktor only supports built-in metrics',
      'call.metrics.increment("counter")',
      'install(CustomMetrics) { }',
      'Use the MeterRegistry directly to create counters, timers, and gauges',
    ],
    correctAnswer: 3,
    explanation: 'Access the MeterRegistry injected through MicrometerMetrics and use it to create custom counters, timers, gauges, and distribution summaries.',
    codeSnippet: 'val registry: PrometheusMeterRegistry = /* ... */\nval requestCounter = registry.counter(\"custom_requests\", \"endpoint\", \"/api\")\n\nget(\"/api\") {\n    requestCounter.increment()\n    call.respond(\"data\")\n}',
    tags: ['ktor', 'metrics', 'custom', 'micrometer'],
  },
  {
    id: 'kotlin-ktor-l3-035',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you implement request/response interceptors in Ktor HttpClient?',
    options: [
      'install(Interceptor) { }',
      'client.interceptors.add { }',
      'client.addInterceptor(interceptor)',
      'Create a custom client plugin using createClientPlugin with hooks like onRequest, onResponse',
    ],
    correctAnswer: 3,
    explanation: 'Client plugins created with createClientPlugin() provide hooks like onRequest and onResponse for intercepting client requests and responses.',
    codeSnippet: 'val AuthPlugin = createClientPlugin(\"Auth\") {\n    onRequest { request, _ ->\n        request.headers.append(\"Authorization\", \"Bearer $token\")\n    }\n    onResponse { response ->\n        if (response.status == HttpStatusCode.Unauthorized) {\n            // handle token refresh\n        }\n    }\n}',
    tags: ['ktor', 'client', 'plugin', 'interceptor'],
  },
  {
    id: 'kotlin-ktor-l3-036',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you implement automatic token refresh in Ktor HttpClient?',
    options: [
      'client.autoRefresh = true',
      'install(OAuth) { refresh = true }',
      'install(TokenRefresh) { }',
      'Configure the Auth plugin with bearer { refreshTokens { } }',
    ],
    correctAnswer: 3,
    explanation: 'The Auth plugin for Ktor client supports bearer authentication with automatic token refresh. The refreshTokens block is called when a 401 response is received.',
    codeSnippet: 'val client = HttpClient(CIO) {\n    install(Auth) {\n        bearer {\n            loadTokens {\n                BearerTokens(accessToken, refreshToken)\n            }\n            refreshTokens {\n                val response = client.post(\"http://auth/refresh\") {\n                    setBody(mapOf(\"refresh_token\" to oldTokens?.refreshToken))\n                }\n                val tokens = response.body<TokenResponse>()\n                BearerTokens(tokens.access, tokens.refresh)\n            }\n        }\n    }\n}',
    tags: ['ktor', 'client', 'auth', 'token-refresh'],
  },
  {
    id: 'kotlin-ktor-l3-037',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you handle Server-Sent Events (SSE) with Ktor HttpClient?',
    options: [
      'client.sse("url") { }',
      'client.eventSource("url") { }',
      'install(SSE) { }',
      'Use client.prepareGet() with streaming and parse the text/event-stream format',
    ],
    correctAnswer: 3,
    explanation: 'Ktor client handles SSE by streaming the response using prepareGet().execute { } and reading the text/event-stream formatted data from the body channel incrementally.',
    codeSnippet: 'client.prepareGet(\"http://example.com/events\") {\n    headers { append(HttpHeaders.Accept, \"text/event-stream\") }\n}.execute { response ->\n    val channel = response.bodyAsChannel()\n    while (!channel.isClosedForRead) {\n        val line = channel.readUTF8Line()\n        if (line?.startsWith(\"data:\") == true) {\n            val data = line.removePrefix(\"data: \")\n            println(\"Event: $data\")\n        }\n    }\n}',
    tags: ['ktor', 'client', 'sse', 'streaming'],
  },
  {
    id: 'kotlin-ktor-l3-038',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Routing',
    question: 'How do you implement typed routing (Resources plugin) in Ktor?',
    options: [
      'Use typed<Route>("/path") { }',
      'Install TypedRouting plugin',
      'Define RouteType classes',
      'Define routes as data classes and use @Resource annotation',
    ],
    correctAnswer: 3,
    explanation: 'The Resources plugin enables type-safe routing. You define route classes annotated with @Resource and use get<ResourceClass> { } in routing to handle them type-safely.',
    codeSnippet: '@Serializable\n@Resource(\"/users\")\nclass Users {\n    @Serializable\n    @Resource(\"{id}\")\n    class Id(val parent: Users = Users(), val id: Long)\n}\n\nrouting {\n    get<Users> { call.respond(userService.getAll()) }\n    get<Users.Id> { user ->\n        call.respond(userService.getById(user.id))\n    }\n}',
    tags: ['ktor', 'routing', 'resources', 'type-safe'],
  },
  {
    id: 'kotlin-ktor-l3-039',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Routing',
    question: 'What plugin is needed for type-safe routing in Ktor?',
    options: ['TypedRoutes', 'Locations (deprecated)', 'Resources', 'TypeSafeRouting'],
    correctAnswer: 2,
    explanation: 'The Resources plugin (ktor-server-resources) provides type-safe routing in Ktor 2.x. It replaces the deprecated Locations plugin from Ktor 1.x.',
    tags: ['ktor', 'routing', 'resources', 'type-safe'],
  },
  {
    id: 'kotlin-ktor-l3-040',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you implement request rate limiting per route in Ktor?',
    options: [
      'install(RateLimit) on each route',
      'Use rateLimit(\"limiter-name\") { } to wrap routes with specific rate limit configurations',
      'Use middleware("rateLimit") { }',
      'Add @RateLimit annotation',
    ],
    correctAnswer: 1,
    explanation: 'After installing RateLimit with named configurations, wrap specific routes with rateLimit("name") { } to apply different rate limits to different route groups.',
    codeSnippet: 'install(RateLimit) {\n    register(RateLimitName(\"public\")) {\n        rateLimiter(limit = 100, refillPeriod = 60.seconds)\n    }\n    register(RateLimitName(\"api\")) {\n        rateLimiter(limit = 10, refillPeriod = 60.seconds)\n    }\n}\nrouting {\n    rateLimit(RateLimitName(\"public\")) {\n        get(\"/public\") { /* ... */ }\n    }\n    rateLimit(RateLimitName(\"api\")) {\n        get(\"/api\") { /* ... */ }\n    }\n}',
    tags: ['ktor', 'rate-limit', 'per-route'],
  },
  {
    id: 'kotlin-ktor-l3-041',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement Server-Sent Events (SSE) on the Ktor server?',
    options: [
      'sse("/events") { }',
      'Use call.respondTextWriter() or call.respondBytesWriter() with text/event-stream content type',
      'route.eventStream("/events") { }',
      'install(SSE) { }',
    ],
    correctAnswer: 1,
    explanation: 'Ktor does not have a built-in SSE plugin, but you can implement SSE using call.respondTextWriter() with ContentType.Text.EventStream and writing events in the SSE format.',
    codeSnippet: 'get(\"/events\") {\n    call.respondTextWriter(contentType = ContentType.Text.EventStream) {\n        repeat(10) {\n            write(\"data: Event $it\\n\\n\")\n            flush()\n            delay(1000)\n        }\n    }\n}',
    tags: ['ktor', 'sse', 'server-sent-events', 'streaming'],
  },
  {
    id: 'kotlin-ktor-l3-042',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you serve Single Page Application (SPA) routes in Ktor?',
    options: [
      'spa("/") { indexFile = "index.html" }',
      'install(StaticContent) { spa = true }',
      'install(SPA) { }',
      'Use singlePageApplication { } inside routing or configure static files with a fallback to index.html',
    ],
    correctAnswer: 3,
    explanation: 'Ktor provides singlePageApplication { } for serving SPAs. It serves static files and falls back to index.html for unmatched routes, allowing client-side routing to work.',
    codeSnippet: 'routing {\n    singlePageApplication {\n        useResources(\"static\")\n        defaultPage = \"index.html\"\n    }\n}',
    tags: ['ktor', 'spa', 'single-page-application', 'static'],
  },
  {
    id: 'kotlin-ktor-l3-043',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you create a custom authentication provider in Ktor?',
    options: [
      'Implement the Authenticator interface',
      'Create an AuthenticationProvider subclass with authenticate() method and register via config.register()',
      'Use @AuthProvider annotation',
      'Extend AuthenticationProvider and register it',
    ],
    correctAnswer: 1,
    explanation: 'Custom authentication providers extend AuthenticationProvider and implement the authenticate() method. The provider is registered in the Authentication configuration.',
    codeSnippet: 'class ApiKeyAuthProvider(config: Config) : AuthenticationProvider(config) {\n    class Config(name: String?) : AuthenticationProvider.Config(name) {\n        var apiKeyHeader: String = \"X-API-Key\"\n        var validate: suspend (String) -> Principal? = { null }\n    }\n    override suspend fun onAuthenticate(context: AuthenticationContext) {\n        val apiKey = context.call.request.headers[config.apiKeyHeader]\n        if (apiKey != null) {\n            val principal = config.validate(apiKey)\n            if (principal != null) {\n                context.principal(principal)\n            }\n        }\n    }\n}',
    tags: ['ktor', 'authentication', 'custom-provider'],
  },
  {
    id: 'kotlin-ktor-l3-044',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'What is the purpose of the Webjars plugin in Ktor?',
    options: [
      'Manages JavaScript dependencies',
      'Packages the application as a JAR',
      'Creates web archives',
      'Serves static assets from WebJar dependencies (like Bootstrap, jQuery)',
    ],
    correctAnswer: 3,
    explanation: 'The Webjars plugin serves static assets from WebJar dependencies (Maven packages containing web libraries). This lets you include frontend libraries like Bootstrap via Gradle dependencies.',
    codeSnippet: 'install(Webjars) {\n    path = \"/webjars\"\n}\n// Access: /webjars/bootstrap/5.3.0/css/bootstrap.min.css',
    tags: ['ktor', 'plugins', 'webjars', 'static-assets'],
  },
  {
    id: 'kotlin-ktor-l3-045',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement health check endpoints in Ktor?',
    options: [
      'Define regular routes (e.g., GET /health) that check application health and return appropriate responses',
      'install(HealthCheck) { }',
      'Use the Actuator plugin',
      'Ktor has built-in health checks',
    ],
    correctAnswer: 0,
    explanation: 'Ktor does not have a built-in health check plugin. You create regular route handlers that check database connectivity, external services, etc., and return health status.',
    codeSnippet: 'routing {\n    get(\"/health\") {\n        call.respond(mapOf(\"status\" to \"UP\"))\n    }\n    get(\"/health/ready\") {\n        val dbOk = checkDatabase()\n        if (dbOk) {\n            call.respond(mapOf(\"status\" to \"READY\"))\n        } else {\n            call.respond(\n                HttpStatusCode.ServiceUnavailable,\n                mapOf(\"status\" to \"NOT_READY\")\n            )\n        }\n    }\n}',
    tags: ['ktor', 'health-check', 'monitoring'],
  },
  {
    id: 'kotlin-ktor-l3-046',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement request/response logging with body content in Ktor?',
    options: [
      'install(CallLogging) { logBody = true }',
      'Create a custom plugin that reads the body using DoubleReceive and logs it',
      'CallLogging automatically logs bodies',
      'install(BodyLogging) { }',
    ],
    correctAnswer: 1,
    explanation: 'CallLogging does not log body content by default. To log request/response bodies, create a custom plugin that uses DoubleReceive to read the body and onCallRespond to capture the response.',
    tags: ['ktor', 'logging', 'body', 'custom-plugin'],
  },
  {
    id: 'kotlin-ktor-l3-047',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you configure SSL/TLS settings for Ktor HttpClient?',
    options: [
      'client.ssl = SSLConfig()',
      'install(SSL) { }',
      'client.trustAll()',
      'Configure it in the engine block with https { } or ssl { }',
    ],
    correctAnswer: 3,
    explanation: 'SSL/TLS configuration is engine-specific. For example, with CIO, you use engine { https { } } to configure trust stores, key stores, and custom trust managers.',
    codeSnippet: 'val client = HttpClient(CIO) {\n    engine {\n        https {\n            trustManager = object : X509TrustManager {\n                override fun checkClientTrusted(chain: Array<out X509Certificate>?, authType: String?) {}\n                override fun checkServerTrusted(chain: Array<out X509Certificate>?, authType: String?) {}\n                override fun getAcceptedIssuers(): Array<X509Certificate> = emptyArray()\n            }\n        }\n    }\n}',
    tags: ['ktor', 'client', 'ssl', 'tls'],
  },
  {
    id: 'kotlin-ktor-l3-048',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Testing',
    question: 'How do you test authentication-protected routes in Ktor?',
    options: [
      'Include authentication in the test application config and send valid credentials/tokens in requests',
      'testApplication does not support authentication',
      'Use @SkipAuth annotation',
      'Mock the Authentication plugin',
    ],
    correctAnswer: 0,
    explanation: 'Configure authentication in the test application just like in production. Send appropriate credentials (e.g., Authorization header with Bearer token) in the test client requests.',
    codeSnippet: 'testApplication {\n    application {\n        install(Authentication) {\n            bearer(\"test\") {\n                authenticate { BearerTokenCredential(it.token)\n                    if (it.token == \"valid-token\") UserIdPrincipal(\"test\")\n                    else null\n                }\n            }\n        }\n        routing {\n            authenticate(\"test\") {\n                get(\"/protected\") { call.respondText(\"OK\") }\n            }\n        }\n    }\n    val response = client.get(\"/protected\") {\n        header(HttpHeaders.Authorization, \"Bearer valid-token\")\n    }\n    assertEquals(HttpStatusCode.OK, response.status)\n}',
    tags: ['ktor', 'testing', 'authentication'],
  },
  {
    id: 'kotlin-ktor-l3-049',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Testing',
    question: 'How do you test a Ktor application that uses external configuration from application.conf?',
    options: [
      'Test applications cannot use configuration files',
      'Copy the config file to test resources',
      'Use environment { config = MapApplicationConfig(...) } in testApplication',
      'Mock the configuration',
    ],
    correctAnswer: 2,
    explanation: 'In testApplication, you can override configuration using environment { config = ... } to provide test-specific configuration values without relying on the actual file.',
    codeSnippet: 'testApplication {\n    environment {\n        config = MapApplicationConfig(\n            \"ktor.deployment.port\" to \"8080\",\n            \"app.database.url\" to \"jdbc:h2:mem:test\"\n        )\n    }\n    application { module() }\n}',
    tags: ['ktor', 'testing', 'configuration'],
  },
  {
    id: 'kotlin-ktor-l3-050',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement structured concurrency for background tasks in a Ktor application?',
    options: [
      'GlobalScope.launch { }',
      'Thread.start { }',
      'BackgroundWorker.start { }',
      'Use the Application coroutine scope via launch { } in the application context',
    ],
    correctAnswer: 3,
    explanation: 'The Application object implements CoroutineScope. You should use application.launch { } for background tasks to ensure structured concurrency and proper cancellation on shutdown.',
    codeSnippet: 'fun Application.schedulePeriodicTask() {\n    launch {\n        while (isActive) {\n            delay(60_000)\n            log.info(\"Running periodic task\")\n            performCleanup()\n        }\n    }\n}',
    tags: ['ktor', 'coroutines', 'background-tasks', 'structured-concurrency'],
  },
  {
    id: 'kotlin-ktor-l3-051',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'What happens if an unhandled exception occurs in a Ktor route handler?',
    options: [
      'The request hangs indefinitely',
      'The server crashes',
      'A 500 Internal Server Error is returned; with StatusPages, you can customize the response',
      'The exception is silently ignored',
    ],
    correctAnswer: 2,
    explanation: 'Unhandled exceptions in route handlers result in a 500 Internal Server Error. The StatusPages plugin allows you to catch these exceptions and provide custom error responses.',
    tags: ['ktor', 'error-handling', 'exception', 'StatusPages'],
  },
  {
    id: 'kotlin-ktor-l3-052',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'CORS',
    question: 'How do you configure CORS to allow specific headers in Ktor?',
    options: [
      'headers = listOf("Authorization")',
      'allowHeader(HttpHeaders.Authorization); allowNonSimpleContentTypes = true',
      'allowHeaders { it.contains("X-Custom") }',
      'setAllowedHeaders("Authorization")',
    ],
    correctAnswer: 1,
    explanation: 'Use allowHeader() for specific headers and allowNonSimpleContentTypes for non-simple content types. You can also use allowHeaders { predicate } for pattern-based matching.',
    codeSnippet: 'install(CORS) {\n    allowHeader(HttpHeaders.Authorization)\n    allowHeader(HttpHeaders.ContentType)\n    allowNonSimpleContentTypes = true\n    allowMethod(HttpMethod.Put)\n    allowMethod(HttpMethod.Delete)\n    anyHost()\n}',
    tags: ['ktor', 'cors', 'headers', 'configuration'],
  },
  {
    id: 'kotlin-ktor-l3-053',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'CORS',
    question: 'What is a preflight request in CORS and how does Ktor handle it?',
    options: [
      'A request sent before the actual request; Ktor ignores it',
      'A GET request to check server health',
      'An OPTIONS request sent by the browser; Ktor CORS plugin handles it automatically',
      'A HEAD request to check headers',
    ],
    correctAnswer: 2,
    explanation: 'A preflight request is an OPTIONS request sent by the browser before non-simple cross-origin requests. The Ktor CORS plugin automatically handles preflight requests based on the configuration.',
    tags: ['ktor', 'cors', 'preflight', 'OPTIONS'],
  },
  {
    id: 'kotlin-ktor-l3-054',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you configure multiple connectors (HTTP and HTTPS) in Ktor?',
    options: [
      'Use applicationEngineEnvironment with multiple connectors',
      'install(MultiConnector) { }',
      'embeddedServer(Netty, port = 80, sslPort = 443)',
      'Run two server instances',
    ],
    correctAnswer: 0,
    explanation: 'Use applicationEngineEnvironment to configure multiple connectors (HTTP and HTTPS) on different ports. Each connector can have its own configuration.',
    codeSnippet: 'val env = applicationEngineEnvironment {\n    connector {\n        port = 8080\n    }\n    sslConnector(\n        keyStore = keyStore,\n        keyAlias = \"myAlias\",\n        keyStorePassword = { \"changeit\".toCharArray() },\n        privateKeyPassword = { \"changeit\".toCharArray() }\n    ) {\n        port = 8443\n    }\n    module { configureRouting() }\n}\nembeddedServer(Netty, env).start(wait = true)',
    tags: ['ktor', 'server', 'connectors', 'ssl'],
  },
  {
    id: 'kotlin-ktor-l3-055',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you implement request body size limiting in Ktor?',
    options: [
      'call.request.maxSize = 1024',
      'install(BodySize) { maxSize = 1024 }',
      'Set maxRequestSize in engine configuration or check content-length in an interceptor',
      'install(RequestLimit) { bytes = 1024 }',
    ],
    correctAnswer: 2,
    explanation: 'You can limit request body size at the engine level or create a custom plugin that checks the Content-Length header and rejects oversized requests.',
    codeSnippet: 'val RequestSizeLimit = createApplicationPlugin(\"RequestSizeLimit\") {\n    val maxSize = 1_048_576L // 1 MB\n    onCall { call ->\n        val contentLength = call.request.headers[HttpHeaders.ContentLength]?.toLongOrNull()\n        if (contentLength != null && contentLength > maxSize) {\n            call.respond(HttpStatusCode.PayloadTooLarge)\n        }\n    }\n}',
    tags: ['ktor', 'plugins', 'request-size', 'security'],
  },
  {
    id: 'kotlin-ktor-l3-056',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Routing',
    question: 'How do you implement route-specific error handling in Ktor?',
    options: [
      'route.onError { }',
      'try-catch in each route',
      'Install StatusPages at the route level or use try-catch with custom error responses',
      'errorHandler { } inside route block',
    ],
    correctAnswer: 2,
    explanation: 'You can install StatusPages at different route levels for route-specific error handling. Alternatively, wrap route handler code in try-catch blocks for fine-grained control.',
    codeSnippet: 'routing {\n    route(\"/api\") {\n        install(StatusPages) {\n            exception<IllegalArgumentException> { call, cause ->\n                call.respond(\n                    HttpStatusCode.BadRequest,\n                    ErrorResponse(cause.message ?: \"Invalid input\")\n                )\n            }\n        }\n        get(\"/users/{id}\") {\n            val id = call.parameters[\"id\"]?.toLongOrNull()\n                ?: throw IllegalArgumentException(\"Invalid ID\")\n            call.respond(userService.get(id))\n        }\n    }\n}',
    tags: ['ktor', 'error-handling', 'route-specific'],
  },
  {
    id: 'kotlin-ktor-l3-057',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement database integration in a Ktor application?',
    options: [
      'Ktor has a built-in ORM',
      'install(Database) { }',
      'Use external libraries like Exposed, Ktorm, or jOOQ; Ktor does not include database support',
      'Use the ktor-database plugin',
    ],
    correctAnswer: 2,
    explanation: 'Ktor does not include built-in database support. You integrate external libraries such as Exposed (JetBrains ORM), Ktorm, Hibernate, jOOQ, or plain JDBC.',
    tags: ['ktor', 'database', 'exposed', 'integration'],
  },
  {
    id: 'kotlin-ktor-l3-058',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you manage database connection pools in a Ktor application?',
    options: [
      'Use ktor-hikari plugin',
      'Use HikariCP or similar connection pool library configured at application startup',
      'install(ConnectionPool) { }',
      'Ktor manages connection pools automatically',
    ],
    correctAnswer: 1,
    explanation: 'HikariCP is the most popular connection pool library used with Ktor. You configure it at application startup and share the DataSource across your application.',
    codeSnippet: 'fun Application.configureDatabases() {\n    val hikariConfig = HikariConfig().apply {\n        driverClassName = \"org.postgresql.Driver\"\n        jdbcUrl = \"jdbc:postgresql://localhost:5432/mydb\"\n        username = \"user\"\n        password = \"pass\"\n        maximumPoolSize = 10\n    }\n    val dataSource = HikariDataSource(hikariConfig)\n    Database.connect(dataSource)\n}',
    tags: ['ktor', 'database', 'connection-pool', 'hikari'],
  },
  {
    id: 'kotlin-ktor-l3-059',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Deployment',
    question: 'How do you override application.conf settings at runtime?',
    options: [
      'Runtime overrides are not supported',
      'Use environment variables with HOCON ${?ENV_VAR} or pass -P:ktor.deployment.port=9090 JVM arguments',
      'Edit the file at runtime',
      'Call application.config.set()',
    ],
    correctAnswer: 1,
    explanation: 'HOCON supports environment variable substitution with ${?ENV_VAR}. Additionally, you can override settings with -P:path=value JVM arguments or by setting Java system properties.',
    tags: ['ktor', 'configuration', 'runtime', 'override'],
  },
  {
    id: 'kotlin-ktor-l3-060',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you use YAML configuration instead of HOCON in Ktor?',
    options: [
      'Use the ktor-server-config-yaml dependency and provide application.yaml',
      'Rename application.conf to application.yaml',
      'YAML is not supported',
      'install(YamlConfig) { }',
    ],
    correctAnswer: 0,
    explanation: 'Ktor supports YAML configuration via the ktor-server-config-yaml dependency. Place an application.yaml file in the resources directory instead of application.conf.',
    codeSnippet: '# application.yaml\nktor:\n  deployment:\n    port: 8080\n  application:\n    modules:\n      - com.example.ApplicationKt.module',
    tags: ['ktor', 'configuration', 'yaml'],
  },
  {
    id: 'kotlin-ktor-l3-061',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How does the onCallRespond hook differ from onCall in a custom plugin?',
    options: [
      'No difference',
      'onCall fires before route handling; onCallRespond fires when the response is being sent',
      'onCallRespond is for error responses only',
      'onCall is for GET, onCallRespond is for POST',
    ],
    correctAnswer: 1,
    explanation: 'onCall fires before the route handler processes the request. onCallRespond fires when call.respond() is invoked, allowing you to transform or inspect the response being sent.',
    codeSnippet: 'val ResponseTransform = createApplicationPlugin(\"ResponseTransform\") {\n    onCallRespond { call, body ->\n        // Transform the response body before sending\n        if (body is String) {\n            transformBody { ApiResponse(data = body) }\n        }\n    }\n}',
    tags: ['ktor', 'plugins', 'hooks', 'onCallRespond'],
  },
  {
    id: 'kotlin-ktor-l3-062',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you handle client response validation in Ktor HttpClient?',
    options: [
      'client.validate { }',
      'client.expectSuccess = true only',
      'response.validate()',
      'Install the HttpCallValidator plugin with validateResponse { } and handleResponseExceptionWithRequest { }',
    ],
    correctAnswer: 3,
    explanation: 'The HttpCallValidator plugin provides validateResponse for checking response status and handleResponseExceptionWithRequest for custom exception handling.',
    codeSnippet: 'val client = HttpClient(CIO) {\n    HttpResponseValidator {\n        validateResponse { response ->\n            if (response.status.value >= 500) {\n                throw ServerException(\"Server error: ${response.status}\")\n            }\n        }\n        handleResponseExceptionWithRequest { exception, request ->\n            throw ApiException(\"Request to ${request.url} failed\", exception)\n        }\n    }\n}',
    tags: ['ktor', 'client', 'validation', 'error-handling'],
  },
  {
    id: 'kotlin-ktor-l3-063',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement CORS with exposed headers in Ktor?',
    options: [
      'cors.expose("header")',
      'allowHeader("X-Custom", exposed = true)',
      'exposeHeader("X-Custom-Header") inside CORS configuration',
      'headers.expose("X-Custom")',
    ],
    correctAnswer: 2,
    explanation: 'exposeHeader() in the CORS configuration specifies which response headers the browser is allowed to access from JavaScript. By default, only simple response headers are exposed.',
    codeSnippet: 'install(CORS) {\n    anyHost()\n    exposeHeader(\"X-Total-Count\")\n    exposeHeader(\"X-Request-Id\")\n}',
    tags: ['ktor', 'cors', 'exposed-headers'],
  },
  {
    id: 'kotlin-ktor-l3-064',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement environment-specific configuration in Ktor?',
    options: [
      'Ktor does not support environment-specific config',
      'Use HOCON includes and environment variables to switch between configuration files',
      'Use @Profile annotation',
      'install(Environment) { }',
    ],
    correctAnswer: 1,
    explanation: 'Use HOCON include statements and environment variable substitution to handle different environments. You can include environment-specific config files or override values via environment variables.',
    codeSnippet: '# application.conf\ninclude \"base.conf\"\ninclude file(\"/etc/app/production.conf\")\n\nktor {\n    deployment {\n        port = 8080\n        port = ${?PORT}\n    }\n}',
    tags: ['ktor', 'configuration', 'environment', 'hocon'],
  },
  {
    id: 'kotlin-ktor-l3-065',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement request tracing / distributed tracing in Ktor?',
    options: [
      'install(Tracing) { }',
      'Use the Zipkin plugin',
      'Use CallId plugin for request IDs and integrate with OpenTelemetry or similar libraries',
      'Ktor has built-in distributed tracing',
    ],
    correctAnswer: 2,
    explanation: 'Ktor does not have built-in distributed tracing. Combine the CallId plugin for request ID propagation with OpenTelemetry, Zipkin, or Jaeger for full distributed tracing.',
    tags: ['ktor', 'tracing', 'distributed-tracing', 'opentelemetry'],
  },
  {
    id: 'kotlin-ktor-l3-066',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you implement content transformation (e.g., wrapping all responses) using plugins?',
    options: [
      'Override the respond function',
      'Use onCallRespond hook with transformBody in a custom plugin',
      'install(ResponseWrapper) { }',
      'Use call.respond() wrapper',
    ],
    correctAnswer: 1,
    explanation: 'The onCallRespond hook in a custom plugin lets you intercept and transform response bodies. The transformBody function replaces the original response body with a new value.',
    codeSnippet: 'val ResponseWrapper = createApplicationPlugin(\"ResponseWrapper\") {\n    onCallRespond { call, body ->\n        if (body !is ApiResponse<*>) {\n            transformBody {\n                ApiResponse(data = body, status = \"success\")\n            }\n        }\n    }\n}',
    tags: ['ktor', 'plugins', 'response-transformation'],
  },
  {
    id: 'kotlin-ktor-l3-067',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'kotlinx.serialization',
    question: 'How do you handle polymorphic serialization with kotlinx.serialization in Ktor?',
    options: [
      'Use @Polymorphic annotation only',
      'Configure SerializersModule with polymorphic { } and pass it to the Json builder',
      'Polymorphic serialization is not supported',
      'Use @JsonTypeInfo annotation',
    ],
    correctAnswer: 1,
    explanation: 'Polymorphic serialization requires a SerializersModule that maps the type hierarchy. You pass this module to the Json configuration in ContentNegotiation.',
    codeSnippet: 'val module = SerializersModule {\n    polymorphic(Animal::class) {\n        subclass(Cat::class, Cat.serializer())\n        subclass(Dog::class, Dog.serializer())\n    }\n}\n\ninstall(ContentNegotiation) {\n    json(Json {\n        serializersModule = module\n        classDiscriminator = \"type\"\n    })\n}',
    tags: ['ktor', 'kotlinx-serialization', 'polymorphic'],
  },
  {
    id: 'kotlin-ktor-l3-068',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'kotlinx.serialization',
    question: 'How do you customize the JSON field name in kotlinx.serialization?',
    options: [
      '@SerialName("name")',
      '@Name("name")',
      '@Field("name")',
      '@JsonProperty("name")',
    ],
    correctAnswer: 0,
    explanation: '@SerialName("name") changes the serialized name of a property. This is the kotlinx.serialization equivalent of Jackson\'s @JsonProperty.',
    codeSnippet: '@Serializable\ndata class User(\n    @SerialName(\"user_name\")\n    val userName: String,\n    @SerialName(\"created_at\")\n    val createdAt: String\n)',
    tags: ['ktor', 'kotlinx-serialization', 'SerialName'],
  },
  {
    id: 'kotlin-ktor-l3-069',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement API versioning in Ktor?',
    options: [
      '@Version("v1") annotation',
      'install(ApiVersion) { }',
      'Ktor does not support API versioning',
      'Use route grouping with version prefix, header-based routing, or custom plugins',
    ],
    correctAnswer: 3,
    explanation: 'Ktor does not have built-in API versioning. Common approaches include URL path prefixes (route("/v1") { }), custom header inspection, or Accept header versioning via plugins.',
    codeSnippet: 'routing {\n    route(\"/api/v1\") {\n        get(\"/users\") { call.respond(usersV1()) }\n    }\n    route(\"/api/v2\") {\n        get(\"/users\") { call.respond(usersV2()) }\n    }\n}',
    tags: ['ktor', 'api-versioning', 'routing'],
  },
  {
    id: 'kotlin-ktor-l3-070',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you implement a custom session storage backed by a database?',
    options: [
      'Use SessionStorageDatabase()',
      'Extend CookieStorage class',
      'install(DatabaseSessions) { }',
      'Implement the SessionStorage interface with read(), write(), and invalidate() methods',
    ],
    correctAnswer: 3,
    explanation: 'Create a class that implements the SessionStorage interface. You need to implement read(), write(), and invalidate() methods that interact with your database.',
    codeSnippet: 'class DatabaseSessionStorage(private val db: Database) : SessionStorage {\n    override suspend fun read(id: String): String {\n        return db.query(\"SELECT data FROM sessions WHERE id = ?\", id)\n            ?: throw NoSuchElementException(\"Session $id not found\")\n    }\n    override suspend fun write(id: String, value: String) {\n        db.execute(\"INSERT INTO sessions (id, data) VALUES (?, ?) ON CONFLICT (id) DO UPDATE SET data = ?\", id, value, value)\n    }\n    override suspend fun invalidate(id: String) {\n        db.execute(\"DELETE FROM sessions WHERE id = ?\", id)\n    }\n}',
    tags: ['ktor', 'sessions', 'storage', 'database'],
  },
  {
    id: 'kotlin-ktor-l3-071',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement OpenAPI/Swagger documentation for a Ktor application?',
    options: [
      'install(Swagger) { }',
      'Use community plugins like ktor-swagger-ui or kompendium to generate OpenAPI specs',
      'Use @Api annotations',
      'Ktor has built-in OpenAPI support',
    ],
    correctAnswer: 1,
    explanation: 'Ktor does not have built-in OpenAPI support. Community libraries like ktor-swagger-ui and kompendium provide integration for generating and serving OpenAPI documentation.',
    tags: ['ktor', 'openapi', 'swagger', 'documentation'],
  },
  {
    id: 'kotlin-ktor-l3-072',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Deployment',
    question: 'How do you configure Ktor application for Docker deployment?',
    options: [
      'Use the built-in Docker support',
      'Ktor does not support Docker',
      'Create a Dockerfile or use the Ktor Gradle plugin buildImage task',
      'install(Docker) { }',
    ],
    correctAnswer: 2,
    explanation: 'You can create a manual Dockerfile that copies the fat JAR, or use the Ktor Gradle plugin which provides buildImage tasks for generating Docker images.',
    codeSnippet: '# Dockerfile\nFROM eclipse-temurin:17-jre-alpine\nCOPY build/libs/app-all.jar /app/app.jar\nEXPOSE 8080\nENTRYPOINT [\"java\", \"-jar\", \"/app/app.jar\"]',
    tags: ['ktor', 'deployment', 'docker'],
  },
  {
    id: 'kotlin-ktor-l3-073',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement CSRF protection in Ktor?',
    options: [
      'Implement a custom plugin that generates and validates CSRF tokens, or use session-based double submit cookies',
      'install(CSRF) { }',
      'Use @CSRF annotation',
      'CORS handles CSRF automatically',
    ],
    correctAnswer: 0,
    explanation: 'Ktor does not have a built-in CSRF plugin. You implement CSRF protection via a custom plugin that generates tokens, stores them in sessions, and validates them on form submissions.',
    tags: ['ktor', 'csrf', 'security'],
  },
  {
    id: 'kotlin-ktor-l3-074',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you implement response caching with ETags in Ktor?',
    options: [
      'install(ETag) { }',
      'install(Caching) { }',
      'call.response.etag("value")',
      'Use ConditionalHeaders plugin to add ETag and Last-Modified headers based on content',
    ],
    correctAnswer: 3,
    explanation: 'The ConditionalHeaders plugin adds ETag and Last-Modified headers. You configure a lambda that generates version information for responses, enabling conditional requests.',
    codeSnippet: 'install(ConditionalHeaders) {\n    version { call, outgoingContent ->\n        when (outgoingContent.contentType?.withoutParameters()) {\n            ContentType.Application.Json -> listOf(\n                EntityTagVersion(\"etag-${outgoingContent.hashCode()}\")\n            )\n            else -> emptyList()\n        }\n    }\n}',
    tags: ['ktor', 'caching', 'etag', 'ConditionalHeaders'],
  },
  {
    id: 'kotlin-ktor-l3-075',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'What is the difference between application.conf and application.yaml in Ktor?',
    options: [
      'application.yaml is the default, application.conf is deprecated',
      'application.conf uses HOCON format (default); application.yaml uses YAML and requires ktor-server-config-yaml dependency',
      'No difference, they are interchangeable without any extra setup',
      'application.conf is for production, application.yaml is for development',
    ],
    correctAnswer: 1,
    explanation: 'application.conf uses HOCON format and is supported by default. application.yaml uses YAML format and requires the ktor-server-config-yaml dependency to be added.',
    tags: ['ktor', 'configuration', 'hocon', 'yaml'],
  },
  {
    id: 'kotlin-ktor-l3-076',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you configure the Ktor client to follow redirects?',
    options: [
      'client.followRedirects = true',
      'client.autoRedirect = true',
      'install(HttpRedirect) { } in the client configuration',
      'Ktor client follows redirects by default',
    ],
    correctAnswer: 3,
    explanation: 'The Ktor HttpClient follows redirects by default via the HttpRedirect plugin. You can disable it with followRedirects = false in the engine configuration or configure redirect limits.',
    tags: ['ktor', 'client', 'redirects'],
  },
  {
    id: 'kotlin-ktor-l3-077',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Ktor Client',
    question: 'How do you implement request caching in Ktor HttpClient?',
    options: [
      'Client-side caching is not supported',
      'client.cache = true',
      'install(CachePlugin) { }',
      'install(HttpCache) { } in the client configuration',
    ],
    correctAnswer: 3,
    explanation: 'The HttpCache plugin for the Ktor client provides HTTP caching that respects Cache-Control headers, ETags, and other caching directives.',
    codeSnippet: 'val client = HttpClient(CIO) {\n    install(HttpCache)\n}',
    tags: ['ktor', 'client', 'caching', 'HttpCache'],
  },
  {
    id: 'kotlin-ktor-l3-078',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you implement a custom Ktor server engine?',
    options: [
      'Extend Server class',
      'Implement ApplicationEngine and ApplicationEngine.Configuration interfaces',
      'Override HttpServer',
      'Custom engines are not supported',
    ],
    correctAnswer: 1,
    explanation: 'Custom server engines implement the ApplicationEngine interface and its Configuration class. The engine handles low-level networking while Ktor provides the application pipeline.',
    tags: ['ktor', 'server-engine', 'custom'],
  },
  {
    id: 'kotlin-ktor-l3-079',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Testing',
    question: 'How do you load test application configuration from a custom file in testApplication?',
    options: [
      'Use environment { config = ApplicationConfig("test.conf") }',
      'testConfig("test.conf") { }',
      'testApplication(configFile = "test.conf") { }',
      'Pass the file path to testApplication',
    ],
    correctAnswer: 0,
    explanation: 'Inside testApplication, use the environment block to load configuration from a custom file or provide test-specific configuration.',
    codeSnippet: 'testApplication {\n    environment {\n        config = ApplicationConfig(\"test.conf\")\n    }\n    application { module() }\n}',
    tags: ['ktor', 'testing', 'configuration'],
  },
  {
    id: 'kotlin-ktor-l3-080',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Plugins',
    question: 'How do you share state between plugin hooks using attributes in Ktor?',
    options: [
      'Use ThreadLocal',
      'Use a shared map',
      'Use call.attributes to store and retrieve per-request data',
      'Use global variables',
    ],
    correctAnswer: 2,
    explanation: 'call.attributes provides a type-safe attribute map scoped to the current request. You create AttributeKey instances and use them to put/get values on the call.',
    codeSnippet: 'val StartTimeKey = AttributeKey<Long>(\"startTime\")\n\nval TimingPlugin = createApplicationPlugin(\"Timing\") {\n    onCall { call ->\n        call.attributes.put(StartTimeKey, System.currentTimeMillis())\n    }\n    onCallRespond { call, _ ->\n        val startTime = call.attributes[StartTimeKey]\n        val duration = System.currentTimeMillis() - startTime\n        call.response.headers.append(\"X-Response-Time\", \"${duration}ms\")\n    }\n}',
    tags: ['ktor', 'plugins', 'attributes', 'state'],
  },
  {
    id: 'kotlin-ktor-l3-081',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How does Ktor handle concurrent requests?',
    options: [
      'Using the reactor pattern exclusively',
      'Using an event loop like Node.js',
      'One thread per request',
      'Using Kotlin coroutines; requests are handled as suspend functions on shared thread pools',
    ],
    correctAnswer: 3,
    explanation: 'Ktor handles requests using Kotlin coroutines. Each request is processed as a suspend function, allowing thousands of concurrent requests on a small thread pool without blocking.',
    tags: ['ktor', 'concurrency', 'coroutines'],
  },
  {
    id: 'kotlin-ktor-l3-082',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'What dispatcher does Ktor use for request handling by default?',
    options: [
      'Dispatchers.Default',
      'Dispatchers.IO',
      'Dispatchers.Main',
      'The engine-specific dispatcher (e.g., Netty event loop)',
    ],
    correctAnswer: 3,
    explanation: 'Ktor route handlers run on the engine-specific dispatcher. For Netty, this is based on Netty worker threads. You can switch to Dispatchers.IO for blocking operations using withContext().',
    tags: ['ktor', 'dispatchers', 'coroutines', 'threading'],
  },
  {
    id: 'kotlin-ktor-l3-083',
    language: 'kotlin',
    level: 'level3',
    category: 'Ktor',
    subcategory: 'Application',
    question: 'How do you handle blocking operations (like JDBC calls) in Ktor route handlers?',
    options: [
      'Use withContext(Dispatchers.IO) { } to switch to the IO dispatcher',
      'Use async { } without dispatchers',
      'Call them directly in the route handler',
      'Blocking operations are handled automatically',
    ],
    correctAnswer: 0,
    explanation: 'Since route handlers run on the engine dispatcher, blocking calls (JDBC, file I/O) should be wrapped in withContext(Dispatchers.IO) { } to avoid blocking the request processing threads.',
    codeSnippet: 'get(\"/users\") {\n    val users = withContext(Dispatchers.IO) {\n        // Blocking JDBC call\n        database.query(\"SELECT * FROM users\")\n    }\n    call.respond(users)\n}',
    tags: ['ktor', 'blocking', 'dispatchers', 'IO'],
  },
];

export default questions;
