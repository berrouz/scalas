import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-batch-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'A social network wants to compute "people you may know" recommendations. Each MapReduce job produces a candidate list, but the pipeline requires three chained jobs. What is the main operational concern?',
    options: ['The social graph does not fit in HDFS', 'Intermediate results are materialized to HDFS between every job, tripling I/O and wallclock time', 'MapReduce cannot express graph algorithms', 'Each job uses a different programming language'],
    correctAnswer: 1,
    explanation: 'Chained MapReduce jobs must materialize intermediate state to HDFS between each stage. With three jobs, the intermediate I/O overhead is substantial, increasing total runtime significantly compared to a dataflow engine that could pipeline these operations.',
    tags: ['mapreduce', 'chaining', 'intermediate-state', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Sort-Merge Joins',
    question: 'An e-commerce company joins a 500GB user activity log with a 2TB product catalog in MapReduce. Both datasets are too large for memory. Which join strategy is most appropriate?',
    options: ['Broadcast hash join', 'Partitioned hash join with the activity log in memory', 'Reducer-side sort-merge join', 'Nested loop join'],
    correctAnswer: 2,
    explanation: 'Since both datasets are very large and neither fits in memory, a reducer-side sort-merge join is appropriate. Both datasets are shuffled by the join key, sorted, and merged in the reducer, processing records sequentially without requiring either side to fit in memory.',
    tags: ['sort-merge-join', 'large-dataset', 'scenario', 'join-strategy'],
  },
  {
    id: 'ddia-batch-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Broadcast Joins',
    question: 'A batch job joins a 10TB click event log with a 200MB country code lookup table. Which join approach minimizes shuffle overhead?',
    options: ['Broadcast hash join with the country table loaded into memory on each mapper', 'Writing both datasets to a relational database and doing a SQL join', 'Reducer-side sort-merge join', 'Re-partitioning both datasets by country code'],
    correctAnswer: 0,
    explanation: 'The country code table at 200MB easily fits in memory. Using a broadcast hash join, each mapper loads the small table into a hash map and joins locally, completely avoiding the need to shuffle the massive 10TB click log over the network.',
    tags: ['broadcast-join', 'scenario', 'optimization', 'network'],
  },
  {
    id: 'ddia-batch-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'A team migrates a pipeline of 5 chained MapReduce jobs to Spark. After migration, they observe faster execution but occasional long recovery times when a node fails. What explains this behavior?',
    options: ['The Spark driver is a single point of failure with no recovery mechanism', 'Spark uses lineage-based recovery which avoids I/O in the normal case, but a failure may trigger recomputation of multiple stages from the beginning', 'Spark silently drops records during recovery', 'Spark does not support fault tolerance'],
    correctAnswer: 1,
    explanation: 'Spark is faster because it avoids materializing intermediate results. However, without intermediate checkpoints, a node failure may require recomputing several stages of the DAG from the input data. The team could mitigate this by adding strategic checkpoints.',
    tags: ['spark', 'fault-tolerance', 'lineage', 'scenario', 'checkpoint'],
  },
  {
    id: 'ddia-batch-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'A search engine rebuilds its entire index nightly via a batch job. The old index serves queries while the new one is being built. What is this deployment pattern called?',
    options: ['Canary release', 'Blue-green deployment', 'Shadow deployment', 'Rolling update'],
    correctAnswer: 1,
    explanation: 'This is a blue-green deployment pattern: the batch job builds a complete new index while the old one continues serving traffic. Once the new index is ready and validated, traffic is switched over atomically. If the new index has problems, you can switch back.',
    tags: ['blue-green', 'deployment', 'search-index', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'A data engineer notices that one MapReduce job in a 10-job pipeline always takes 10x longer than the others because a single celebrity user ID accounts for 40% of the activity data. How should they address this skew?',
    options: ['Use a two-stage approach: first spread the hot key across multiple reducers with a random prefix, then aggregate the partial results in a second stage', 'Add more mappers', 'Switch to a broadcast join', 'Remove the celebrity data from the dataset'],
    correctAnswer: 0,
    explanation: 'The standard technique for handling hot keys is to distribute the skewed key across multiple reducers by appending a random prefix, then run a second stage to combine the partial results. This parallelizes the work for the hot key.',
    tags: ['skew', 'hot-key', 'two-stage', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'A Spark job processes 50TB of log data with 10 transformations. After profiling, the engineer finds that a failure at stage 8 recomputes from stage 1. What optimization should they apply?',
    options: ['Rewrite the pipeline in MapReduce', 'Remove fault tolerance entirely', 'Increase the replication factor of HDFS', 'Add an explicit checkpoint or cache/persist call at a strategic intermediate stage to limit recomputation scope'],
    correctAnswer: 3,
    explanation: 'By calling persist() or checkpoint() at a strategic point in the DAG, the engineer limits the recomputation needed on failure. Instead of recomputing from stage 1, the system only needs to recompute from the last checkpoint.',
    tags: ['spark', 'checkpoint', 'persist', 'optimization', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Broadcast Joins',
    question: 'A batch pipeline joins user events with a 15GB dimension table. The mapper machines have 16GB of heap memory. The broadcast join keeps failing with out-of-memory errors. What is the best alternative?',
    options: ['Compress the dimension table to fit in 16GB', 'Switch to a partitioned hash join where both inputs are co-partitioned, so each mapper loads only its partition of the dimension table', 'Increase heap to 64GB on every machine', 'Fall back to a nested loop join'],
    correctAnswer: 1,
    explanation: 'A partitioned hash join is the ideal solution. If both datasets are partitioned by the same join key with the same number of partitions, each mapper only loads the corresponding partition of the dimension table, which will be much smaller than 15GB.',
    tags: ['partitioned-hash-join', 'memory', 'scenario', 'co-partitioning'],
  },
  {
    id: 'ddia-batch-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'A batch job builds a recommendation model and writes results into a serving key-value store. During the batch write, the store becomes overloaded and latencies spike for online queries. How should the engineer fix this?',
    options: ['Have the batch job produce immutable data files (e.g., SSTables) that the store bulk-loads atomically, avoiding write amplification on the serving path', 'Run the batch job less frequently', 'Throttle the batch writes', 'Use a faster network'],
    correctAnswer: 0,
    explanation: 'The recommended pattern from DDIA is to have batch jobs produce output files that the serving system can bulk-load or swap in atomically. This avoids overwhelming the serving database with random writes during the batch run.',
    tags: ['batch-output', 'bulk-load', 'serving', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Sort-Merge Joins',
    question: 'A MapReduce join between orders and customers uses customer_id as the join key. The reducer sees interleaved records from both datasets. What technique ensures the customer record is always processed first?',
    options: ['Load all customer records into memory first', 'Use a broadcast join instead', 'Use secondary sort with a composite key that places the customer record before order records for the same customer_id', 'Filter customer records to a separate job'],
    correctAnswer: 2,
    explanation: 'Secondary sort uses a composite key (customer_id + record_type) where the sort order ensures the customer dimension record comes before the order fact records. The reducer then reads the customer record first and can apply it to all subsequent order records.',
    tags: ['secondary-sort', 'composite-key', 'scenario', 'ordering'],
  },
  {
    id: 'ddia-batch-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'A team uses Flink in batch mode. They observe that for a sort-merge join, Flink spills data to disk when memory is insufficient. How does this compare to MapReduce\'s approach?',
    options: ['Flink never uses disk for joins', 'Both can spill to disk, but Flink only does so when needed and can pipeline other operators, whereas MapReduce always materializes full intermediate results to HDFS', 'MapReduce never spills to disk', 'They handle it identically'],
    correctAnswer: 1,
    explanation: 'Dataflow engines like Flink can spill to local disk when memory is tight, similar to a database sort spill. However, they only materialize what is necessary and can pipeline other parts of the computation, unlike MapReduce which always writes full intermediate results to HDFS.',
    tags: ['flink', 'spill-to-disk', 'dataflow', 'comparison'],
  },
  {
    id: 'ddia-batch-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'A analytics team runs daily MapReduce jobs over web server logs. On some days the input is 100GB, on others it is 5TB. They notice the small days waste cluster resources. What is a more efficient alternative?',
    options: ['Migrate to a dataflow engine like Spark that can dynamically allocate resources and avoid the overhead of HDFS materialization for smaller datasets', 'Concatenate multiple days into single runs', 'Run more mappers on small days', 'Always provision for 5TB'],
    correctAnswer: 0,
    explanation: 'Dataflow engines can dynamically adjust resource usage and avoid the fixed overhead of reading/writing HDFS between stages. They handle varying input sizes more gracefully through features like dynamic partition coalescing and in-memory processing.',
    tags: ['resource-efficiency', 'spark', 'dynamic-allocation', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'An application maintains a denormalized user profile view built by a nightly batch job. During the day, users update their profiles in the source database. What is the fundamental consistency challenge?',
    options: ['The materialized view becomes stale immediately after the batch job finishes, as new writes to the source accumulate throughout the day', 'Users cannot read the materialized view during the batch run', 'The batch job has bugs', 'The materialized view takes too much storage'],
    correctAnswer: 0,
    explanation: 'Because the batch job runs periodically (e.g., nightly), the derived view is only as fresh as the last batch run. Any changes made after the batch completes will not be reflected until the next run, creating a staleness window of up to a day.',
    tags: ['staleness', 'consistency', 'materialized-view', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Sort-Merge Joins',
    question: 'A MapReduce job computes session analytics by grouping events by (user_id, session_id). The reducer must output events within each session in timestamp order. How should this be achieved?',
    options: ['Sort events in the reducer code', 'Use a composite sort key of (user_id, session_id, timestamp) with appropriate partitioning and grouping comparators so the framework delivers events pre-sorted', 'Post-process the output with a separate sorting job', 'Use a broadcast join'],
    correctAnswer: 1,
    explanation: 'By using a composite sort key, the MapReduce framework handles the sorting. The partitioner ensures all records for the same (user_id, session_id) go to the same reducer, and the sort comparator includes the timestamp, so events arrive in chronological order.',
    tags: ['composite-key', 'secondary-sort', 'session-analytics', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Broadcast Joins',
    question: 'A data engineer wants to enrich a 2TB clickstream dataset with IP-to-geolocation data (50MB). The pipeline uses Spark. What is the most efficient approach?',
    options: ['Use a reducer-side sort-merge join', 'Write both to a database and use JDBC', 'Shuffle both datasets by IP address', 'Use a broadcast variable to distribute the geo lookup table to all executors and perform a map-side join'],
    correctAnswer: 3,
    explanation: 'Spark\'s broadcast variable mechanism distributes the small 50MB geolocation table to every executor once. Each task then performs a local hash lookup, avoiding any shuffle of the 2TB clickstream data. This is the classic broadcast hash join pattern.',
    tags: ['broadcast-variable', 'spark', 'geolocation', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'A Spark ETL pipeline reads from S3, applies 15 transformations, and writes to a data warehouse. The team discovers that adding a .cache() call at transformation 7 both speeds up recovery and slows down normal execution slightly. Why?',
    options: ['cache() forces a shuffle', 'cache() disables pipelining entirely', 'cache() compresses data, adding CPU overhead', 'Caching materializes data in memory (or disk) at that point, adding overhead to normal runs but limiting recomputation to transformations 8-15 on failure'],
    correctAnswer: 3,
    explanation: 'The .cache() call materializes the RDD at that point, consuming memory and adding a small time cost. However, on failure, only stages 8-15 need recomputation instead of all 15. This is the classic trade-off between normal-case speed and recovery cost.',
    tags: ['spark', 'cache', 'materialization', 'trade-off', 'scenario'],
  },
  {
    id: 'ddia-batch-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'A company uses batch processing to build a full-text search index from 100 million documents nightly. They want to reduce the delay between document ingestion and searchability. What architectural evolution does DDIA suggest?',
    options: ['Run the batch job more frequently (e.g., every hour)', 'Pre-index documents at write time in the primary database', 'Combine batch processing for the full rebuild with stream processing for incremental near-real-time updates (lambda architecture or similar)', 'Use a faster batch framework'],
    correctAnswer: 2,
    explanation: 'DDIA discusses combining batch and stream processing: the batch layer periodically rebuilds the complete index for correctness, while a stream processing layer applies incremental updates for low-latency searchability. This reduces the staleness window from a day to seconds.',
    tags: ['lambda-architecture', 'stream-batch', 'search-index', 'near-real-time'],
  },
  {
    id: 'ddia-batch-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'Your MapReduce job processes 10TB of data but one reducer receives 80% of the records because of a skewed key distribution. Based on DDIA, what techniques can mitigate this?',
    options: [
      'Use more mappers to compensate',
      'Use a skew-handling technique: split the hot key across multiple reducers by adding a random prefix, then run a second stage to merge the partial results',
      'Increase the reducer memory',
      'Hot keys cannot be handled in MapReduce',
    ],
    correctAnswer: 1,
    explanation: 'DDIA discusses techniques for handling skewed joins in MapReduce: Pig\'s "skewed join" and Hive\'s "map join for skewed keys" split hot keys across multiple reducers. Each reducer processes a portion, and a second pass merges the results. Crunch\'s "sharded join" follows a similar approach.',
    tags: ['mapreduce', 'skew', 'hot-key', 'skewed-join', 'applied'],
  },
  {
    id: 'ddia-batch-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'Your Spark job fails partway through a complex 10-stage pipeline. Based on DDIA, how does Spark recover differently from MapReduce?',
    options: [
      'Spark restarts the entire job from scratch',
      'Spark recomputes only the lost partitions by tracing back through the DAG lineage to find the last materialized checkpoint or input data, then recomputing from there',
      'Spark has no fault tolerance mechanism',
      'Spark uses HDFS replication for all intermediate data',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that Spark tracks the lineage of each RDD partition — the sequence of transformations from input data. On failure, Spark recomputes only the lost partitions by following the lineage graph. If checkpoints are placed strategically, recomputation starts from the checkpoint rather than the beginning.',
    tags: ['spark', 'lineage', 'fault-tolerance', 'recomputation', 'applied'],
  },
  {
    id: 'ddia-batch-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'Joins',
    question: 'Your batch job needs to join a 1TB user activity log with a 500MB user profile table. Based on DDIA, what join strategy is most efficient?',
    options: [
      'Reducer-side sort-merge join',
      'Broadcast hash join — load the 500MB user profiles into memory on each mapper and join locally, avoiding the shuffle entirely',
      'Nested loop join',
      'Load both datasets into a database and use SQL',
    ],
    correctAnswer: 1,
    explanation: 'DDIA recommends broadcast hash joins when one side fits in memory. The 500MB user profiles can be loaded into a hash map on each mapper. Each mapper then scans its partition of the 1TB activity log and probes the hash map, avoiding the expensive shuffle phase.',
    tags: ['broadcast-join', 'hash-join', 'optimization', 'applied'],
  },
  {
    id: 'ddia-batch-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'Your batch job produces a search index as output. The index must be loaded into production search servers. Based on DDIA, what is the recommended approach?',
    options: [
      'Write the index directly to the production search servers during the batch job',
      'Build the index as immutable files in the batch job, then atomically swap the files into production search servers, ensuring the index is complete and consistent before serving queries',
      'Index documents one at a time from the batch job output',
      'Load the batch output into a relational database and query from there',
    ],
    correctAnswer: 1,
    explanation: 'DDIA recommends building indexes as immutable files during the batch job, then bulk-loading them into production. This ensures the index is complete and consistent (no partial state visible to users). It also follows the principle of separation between batch computation and serving infrastructure.',
    tags: ['search-index', 'immutable-output', 'atomic-swap', 'applied'],
  },
];

export default questions;
