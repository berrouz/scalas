import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-batch-l2-001',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'Why is writing intermediate results to HDFS between MapReduce stages considered a disadvantage?',
    options: ['It introduces significant I/O overhead due to replication and disk writes', 'It makes the code harder to read', 'It requires manual garbage collection of files', 'It prevents parallel execution of mappers'],
    correctAnswer: 0,
    explanation: 'Materializing intermediate state to HDFS means every intermediate result is written to disk and replicated across multiple nodes. This adds substantial I/O overhead compared to pipelining data directly between operators.',
    tags: ['mapreduce', 'intermediate-state', 'io-overhead'],
  },
  {
    id: 'ddia-batch-l2-002',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'How do dataflow engines like Spark handle fault tolerance differently from MapReduce?',
    options: ['They recompute lost partitions by tracking lineage of transformations rather than writing all intermediate data to disk', 'They checkpoint every record to a message queue', 'They replicate every operator across three nodes', 'They use database transactions'],
    correctAnswer: 0,
    explanation: 'Spark tracks the lineage (sequence of transformations) that produced each partition. If a partition is lost due to a node failure, it can recompute just that partition from the original input rather than re-running the entire job.',
    tags: ['spark', 'fault-tolerance', 'lineage', 'recomputation'],
  },
  {
    id: 'ddia-batch-l2-003',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'What is the main advantage of pipelining in dataflow engines compared to MapReduce?',
    options: ['Operators can pass data directly to downstream operators without waiting for the entire stage to finish', 'Better support for SQL queries', 'Simpler programming model', 'Automatic schema evolution'],
    correctAnswer: 0,
    explanation: 'Pipelining allows the output of one operator to be immediately consumed by the next operator without materializing the entire intermediate dataset. This reduces latency and avoids unnecessary disk I/O.',
    tags: ['pipelining', 'dataflow', 'performance'],
  },
  {
    id: 'ddia-batch-l2-004',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Sort-Merge Joins',
    question: 'What is a key advantage of sort-merge joins over hash joins when processing large datasets?',
    options: ['They do not require either dataset to fit in memory since both sides are sorted and merged sequentially', 'They avoid network transfer entirely', 'They produce sorted output which is useless downstream', 'They are always faster'],
    correctAnswer: 0,
    explanation: 'Sort-merge joins work by sorting both sides by the join key and then merging them. Neither dataset needs to fit in memory because records are processed sequentially. This makes sort-merge joins suitable when both datasets are very large.',
    tags: ['sort-merge-join', 'memory', 'large-datasets'],
  },
  {
    id: 'ddia-batch-l2-005',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Broadcast Joins',
    question: 'When is a broadcast hash join preferable to a reducer-side sort-merge join?',
    options: ['When the data is already sorted', 'When the join key has high cardinality', 'When both datasets are very large', 'When one dataset is small enough to fit in memory on each mapper'],
    correctAnswer: 3,
    explanation: 'A broadcast hash join is ideal when one side of the join is small enough to load into a hash table in memory on each mapper. This avoids the cost of shuffling and sorting the large dataset entirely.',
    tags: ['broadcast-join', 'hash-join', 'trade-off'],
  },
  {
    id: 'ddia-batch-l2-006',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Broadcast Joins',
    question: 'What is a partitioned hash join, and when can it be used?',
    options: ['A join that hashes the output for deduplication', 'A join that partitions results across consumers', 'A join where both inputs are partitioned the same way, so each mapper only loads the matching partition of the small table', 'A join that randomly assigns records to partitions'],
    correctAnswer: 2,
    explanation: 'If both inputs are partitioned in the same way (e.g., by the same key with the same number of partitions), each mapper only needs to load the corresponding partition of the small table into its hash table, reducing memory requirements.',
    tags: ['partitioned-hash-join', 'co-partitioning', 'memory'],
  },
  {
    id: 'ddia-batch-l2-007',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'How does MapReduce relate to the Unix philosophy of composing tools via pipes?',
    options: ['It has no relation to Unix', 'It uses actual Unix pipes between mappers and reducers', 'It replaces Unix pipes with TCP connections', 'Mappers and reducers are like Unix programs reading stdin and writing stdout, but the "pipe" is the distributed sort-merge with materialization to HDFS'],
    correctAnswer: 3,
    explanation: 'MapReduce follows a similar philosophy to Unix pipelines: each mapper/reducer is a self-contained program. However, instead of Unix pipes, the framework provides a distributed shuffle with sorting, and intermediate data is materialized to HDFS rather than streamed.',
    tags: ['unix-philosophy', 'pipes', 'mapreduce', 'analogy'],
  },
  {
    id: 'ddia-batch-l2-008',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'Why does DDIA advocate writing batch job output to a new location rather than updating an existing database in place?',
    options: ['Writing to a new location preserves immutability, enables easy rollback, and avoids partial-update inconsistencies', 'The batch framework forbids database connections', 'Databases cannot handle batch writes', 'It is always faster to write to a new location'],
    correctAnswer: 0,
    explanation: 'By writing output to a new file or new database, the batch job preserves the input data, enables easy rollback (just switch back to the old output), and avoids leaving the database in an inconsistent state if the job fails partway through.',
    tags: ['immutability', 'rollback', 'batch-output', 'consistency'],
  },
  {
    id: 'ddia-batch-l2-009',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'Why can dataflow engines often run faster than a chain of MapReduce jobs for the same computation?',
    options: ['They avoid unnecessary materialization of intermediate state, sort only when needed, and better utilize locality', 'They always use more machines', 'They skip the map phase entirely', 'They use a faster programming language'],
    correctAnswer: 0,
    explanation: 'Dataflow engines eliminate the need to write and read intermediate results to/from HDFS between stages, only sort data when a specific operator requires it, and can schedule operators to exploit data locality, all of which reduce overhead.',
    tags: ['dataflow', 'performance', 'optimization', 'materialization'],
  },
  {
    id: 'ddia-batch-l2-010',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'What problem does the "skew" or "hot key" issue cause in MapReduce, and at which phase?',
    options: ['A few reducers get disproportionately more data, causing the job to wait for those slow reducers', 'Map phase runs out of memory', 'The HDFS namenode becomes a bottleneck', 'The shuffle phase drops records'],
    correctAnswer: 0,
    explanation: 'When certain keys have far more records than others, the reducers responsible for those hot keys receive disproportionately large amounts of data, becoming stragglers that delay the entire job since it cannot complete until all reducers finish.',
    tags: ['skew', 'hot-key', 'straggler', 'reduce-phase'],
  },
  {
    id: 'ddia-batch-l2-011',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Sort-Merge Joins',
    question: 'How does a GROUP BY operation in MapReduce relate to sort-merge joins?',
    options: ['GROUP BY always uses a broadcast join', 'GROUP BY does not require a reduce phase', 'GROUP BY uses the same shuffle-and-sort mechanism, grouping records by the grouping key just like a join groups by the join key', 'They are completely unrelated operations'],
    correctAnswer: 2,
    explanation: 'A GROUP BY operation in MapReduce works similarly to a join: the mapper extracts the grouping key, the framework sorts and groups by that key, and the reducer aggregates all records in each group. The underlying mechanism is the same shuffle-and-sort.',
    tags: ['group-by', 'sort-merge', 'shuffle', 'aggregation'],
  },
  {
    id: 'ddia-batch-l2-012',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'What is the relationship between batch processing output and derived data?',
    options: ['Batch processing creates derived datasets that can be reconstructed from the original input at any time', 'Derived data is always stored in a relational database', 'Derived data cannot be rebuilt once created', 'Batch output replaces the source of truth'],
    correctAnswer: 0,
    explanation: 'Batch processing output is a form of derived data. It is computed from the input dataset, and if lost or corrupted, it can be recreated by re-running the batch job on the original input. The input remains the source of truth.',
    tags: ['derived-data', 'source-of-truth', 'recomputation'],
  },
  {
    id: 'ddia-batch-l2-013',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'What trade-off does Spark\'s lineage-based fault recovery introduce compared to MapReduce\'s full materialization?',
    options: ['It is always strictly better with no drawbacks', 'It uses more disk but less network', 'It uses less disk I/O during normal operation but may need to recompute substantial work if a failure occurs', 'It requires stronger consistency guarantees from the filesystem'],
    correctAnswer: 2,
    explanation: 'Spark avoids writing intermediate state to disk, making normal (no-failure) execution faster. However, if a node fails, it must recompute the lost partitions from their lineage, which can be expensive for long chains of transformations.',
    tags: ['spark', 'lineage', 'fault-tolerance', 'trade-off'],
  },
  {
    id: 'ddia-batch-l2-014',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'What is the purpose of a combiner in MapReduce?',
    options: ['To combine the map and reduce phases into a single step', 'To perform a local pre-aggregation on mapper output before the shuffle, reducing network transfer', 'To merge the outputs of multiple reducers', 'To combine multiple MapReduce jobs into one'],
    correctAnswer: 1,
    explanation: 'A combiner runs on the mapper node and performs a partial aggregation of the mapper output before it is sent over the network to reducers. This reduces the volume of data transferred during the shuffle for associative and commutative operations.',
    tags: ['combiner', 'optimization', 'network', 'pre-aggregation'],
  },
  {
    id: 'ddia-batch-l2-015',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'How can batch processing be used to build key-value stores?',
    options: ['By using the key-value store as the shuffle mechanism', 'By writing to the key-value store row by row during the reduce phase', 'By producing immutable sorted files (like SSTables) that the key-value store can bulk-load or swap in atomically', 'By caching the batch results in memory'],
    correctAnswer: 2,
    explanation: 'Batch jobs can produce sorted, immutable data files (such as SSTables or HFiles) that a key-value store can bulk-load atomically. This is more efficient than individual writes and avoids overloading the database during the batch run.',
    tags: ['key-value-store', 'sstable', 'bulk-load', 'batch-output'],
  },
  {
    id: 'ddia-batch-l2-016',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Broadcast Joins',
    question: 'What is the key disadvantage of a broadcast hash join?',
    options: ['It cannot handle duplicate keys', 'It always requires a reduce phase', 'It requires sorted input data', 'The small table must fit entirely in memory on every mapper task, limiting the size of the small side'],
    correctAnswer: 3,
    explanation: 'The fundamental limitation of a broadcast hash join is that the small dataset must be loaded into memory on every mapper. If the "small" table grows too large, it will exceed available memory and the approach fails.',
    tags: ['broadcast-join', 'memory-limitation', 'trade-off'],
  },
  {
    id: 'ddia-batch-l2-017',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'What advantage do dataflow engines have over MapReduce when expressing a join followed by a group-by aggregation?',
    options: ['They skip the join and go directly to aggregation', 'They can chain join and aggregation operators in a single job rather than requiring two separate MapReduce jobs with intermediate materialization', 'Dataflow engines cannot express this pattern', 'They automatically denormalize the data'],
    correctAnswer: 1,
    explanation: 'In MapReduce, a join followed by an aggregation requires two separate jobs: one for the join and one for the aggregation, with intermediate results materialized to HDFS. Dataflow engines express this as operators in a single DAG, avoiding the intermediate I/O.',
    tags: ['dataflow', 'dag', 'join', 'aggregation', 'efficiency'],
  },
  {
    id: 'ddia-batch-l2-018',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'MapReduce',
    question: 'How does MapReduce handle failures of individual map or reduce tasks?',
    options: [
      'The entire job must be restarted from scratch',
      'Failed tasks are retried on another machine; since mappers read from HDFS and outputs are written to HDFS, work can be safely repeated',
      'Failed tasks are skipped and their data is lost',
      'A backup process runs in parallel and takes over on failure',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that MapReduce fault tolerance is based on retrying failed tasks. Since input is from HDFS (immutable) and output is written atomically to HDFS, a task can be safely retried on another machine. This deterministic, retry-based approach is a key advantage of batch processing.',
    tags: ['mapreduce', 'fault-tolerance', 'retry', 'deterministic'],
  },
  {
    id: 'ddia-batch-l2-019',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Dataflow Engines',
    question: 'Why is materializing intermediate results to HDFS between every MapReduce stage inefficient?',
    options: [
      'HDFS cannot handle intermediate data',
      'Writing to HDFS involves network replication (typically 3 copies), disk I/O, and scheduling overhead for the next job — all of which add significant latency for data that is only used temporarily',
      'HDFS has a file size limit that intermediate data exceeds',
      'Intermediate results are too small for HDFS',
    ],
    correctAnswer: 1,
    explanation: 'DDIA identifies this as a major inefficiency of MapReduce: intermediate data between stages is written to HDFS with full replication (3x), requiring network transfers and disk writes. Dataflow engines like Spark pipeline data between operators or materialize to local disk, avoiding the full HDFS overhead.',
    tags: ['mapreduce', 'intermediate-materialization', 'hdfs', 'inefficiency'],
  },
  {
    id: 'ddia-batch-l2-020',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Joins',
    question: 'What is a broadcast hash join?',
    options: [
      'A join that broadcasts the result to all nodes',
      'When one side of the join is small enough to fit in memory, it is loaded into a hash table on every mapper, and each mapper joins its partition of the large dataset against the in-memory hash table',
      'A join that uses a hash function to match keys',
      'A join that broadcasts messages over the network',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes the broadcast hash join as loading the smaller dataset into an in-memory hash table on each mapper node. Each mapper then scans its partition of the larger dataset and probes the hash table for matches. This avoids the expensive shuffle phase entirely.',
    tags: ['broadcast-join', 'hash-join', 'map-side-join', 'optimization'],
  },
  {
    id: 'ddia-batch-l2-021',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Unix Philosophy',
    question: 'DDIA compares Unix pipes to MapReduce\'s use of HDFS. What is the key difference?',
    options: [
      'Unix pipes are faster than HDFS',
      'Unix pipes stream data between processes (in-memory buffer); MapReduce materializes data to HDFS between stages, enabling fault tolerance but adding latency',
      'HDFS uses pipes internally',
      'There is no meaningful difference',
    ],
    correctAnswer: 1,
    explanation: 'Unix pipes stream data incrementally between processes through an in-memory buffer. MapReduce fully materializes each stage\'s output to HDFS before the next stage begins. This materialization enables retry on failure (fault tolerance) but adds significant I/O overhead.',
    tags: ['unix-pipes', 'hdfs', 'materialization', 'fault-tolerance'],
  },
  {
    id: 'ddia-batch-l2-022',
    language: 'ddia',
    level: 'level2',
    category: 'Batch Processing',
    subcategory: 'Materialized Views',
    question: 'How does DDIA relate batch processing to materialized views?',
    options: [
      'Batch processing cannot create materialized views',
      'Batch processing is like maintaining materialized views: the batch job derives new datasets (views) from input data, and when input changes, the job can be re-run to update the derived data',
      'Materialized views are only for OLAP databases',
      'Batch processing replaces materialized views entirely',
    ],
    correctAnswer: 1,
    explanation: 'DDIA draws an analogy: a batch job that reads input data and produces derived output (like search indexes, analytics reports, or recommendation caches) is conceptually similar to maintaining a materialized view. Re-running the batch job is like refreshing the materialized view.',
    tags: ['batch-processing', 'materialized-views', 'derived-data'],
  },
];

export default questions;
