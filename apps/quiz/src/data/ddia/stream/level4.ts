import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-stream-l4-001',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'In an event-sourced system, how should you handle GDPR "right to erasure" requests when events are supposed to be immutable?',
    options: [
      'Replace all personal data in events with placeholders, rewriting the entire log',
      'Delete all events for that user from the log',
      'Refuse the request because event logs are immutable by design',
      'Use crypto-shredding: encrypt personal data in events with a per-user key, and destroy the key when erasure is requested, rendering the personal data unrecoverable',
    ],
    correctAnswer: 3,
    explanation: 'Crypto-shredding stores personal data encrypted with a per-user key. When erasure is requested, the encryption key is deleted, making the personal data in all events unreadable. The event log structure remains intact, and non-personal fields remain accessible. This balances immutability with privacy requirements.',
    tags: ['event-sourcing', 'gdpr', 'crypto-shredding', 'privacy'],
  },
  {
    id: 'ddia-stream-l4-002',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'What is the "closing the books" analogy that DDIA draws between accounting and event sourcing?',
    options: [
      'Old events should be deleted at the end of each fiscal year',
      'Just as accountants never modify past ledger entries but add correcting entries, event-sourced systems never modify past events but append compensating events; periodic snapshots serve as "closing" the books',
      'Event sourcing requires double-entry bookkeeping where each event has a debit and credit side',
      'The event log must be signed by an auditor before it can be compacted',
    ],
    correctAnswer: 1,
    explanation: 'DDIA draws a direct parallel to accounting: accountants have used append-only immutable ledgers for centuries. Errors are corrected by adding new compensating transactions, not by erasing entries. Snapshots (like balance sheets) summarize the state at a point in time, similar to "closing the books."',
    tags: ['event-sourcing', 'accounting-analogy', 'compensating-events'],
  },
  {
    id: 'ddia-stream-l4-003',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'When using log-based CDC with Kafka, why is log compaction essential for the CDC topic?',
    options: [
      'Log compaction is required to maintain message ordering within partitions',
      'Compaction converts the CDC format from JSON to Avro for efficiency',
      'Without compaction, Kafka would reject new messages once the topic reaches its size limit',
      'Log compaction ensures that the topic retains at least the latest value for every key, allowing a new consumer to reconstruct the full current state of the database from the topic alone',
    ],
    correctAnswer: 3,
    explanation: 'Without log compaction, old segments would be deleted by time-based retention, and a new consumer could miss records that were written and then never updated again. With compaction, the topic acts as a durable, key-value snapshot of the database: every key has at least its latest value, enabling full state reconstruction.',
    tags: ['cdc', 'kafka', 'log-compaction', 'bootstrap'],
  },
  {
    id: 'ddia-stream-l4-004',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'A system uses CDC to replicate data from an OLTP database to a data warehouse. During a schema migration, the OLTP database adds a NOT NULL column with a default value in a backwards-compatible way. However, the CDC pipeline breaks. What is the most likely cause?',
    options: [
      'The database stops emitting CDC events during schema migrations',
      'Kafka cannot handle events with different numbers of fields in the same topic',
      'The schema registry rejects the new schema because the downstream Avro schema has no default for the new field, violating backward compatibility rules',
      'The CDC connector cannot read NOT NULL constraints from the replication log',
    ],
    correctAnswer: 2,
    explanation: 'Even though the database migration is backwards-compatible, the Avro schema registered for the CDC topic must also be compatible. If the new field lacks a default value in the Avro schema, the schema registry (configured with backward compatibility) rejects it because old consumers cannot read the new schema. The Avro schema must include a default.',
    tags: ['cdc', 'schema-registry', 'avro', 'schema-evolution'],
  },
  {
    id: 'ddia-stream-l4-005',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'In a stream-stream join with a 1-hour window, the processor maintains state for all events within the window. If the input rate is 100,000 events/second across both streams, approximately how much state must be maintained?',
    options: [
      'State for the current second only (~100K events)',
      'No state is needed because the join is computed on-the-fly',
      'State for only matched events, which is typically small',
      'State for both streams over the entire 1-hour window (~360 million events), requiring significant memory or disk-backed state',
    ],
    correctAnswer: 3,
    explanation: 'A stream-stream join must buffer all events within the window from both streams. At 100K events/second over 3600 seconds, that is approximately 360 million events. This state is often too large for memory alone and must be backed by a local disk-based state store (like RocksDB in Kafka Streams or Flink).',
    tags: ['stream-joins', 'stream-stream-join', 'state-size', 'rocksdb'],
  },
  {
    id: 'ddia-stream-l4-006',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'A stream processor performs a stream-table join where the table is loaded locally via CDC. During a consumer rebalance, the processor instance loses its partitions and receives new ones. What must happen to the local table state?',
    options: [
      'The local state is automatically migrated between instances by the coordinator',
      'Nothing — the local table state is global and shared across all instances',
      'The join switches to querying the remote database directly until the local state catches up',
      'The instance must rebuild the local state for its new partitions, either from a changelog topic (compacted) or by re-reading the relevant CDC partitions from the beginning',
    ],
    correctAnswer: 3,
    explanation: 'When partitions are reassigned during a rebalance, the new owner must have the corresponding local state. In Kafka Streams, this is handled by restoring state from changelog topics (which are compacted). In other frameworks, the CDC stream for the relevant partitions must be replayed. Standby replicas can reduce restoration time.',
    tags: ['stream-joins', 'stream-table-join', 'rebalancing', 'state-restoration'],
  },
  {
    id: 'ddia-stream-l4-007',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A stream processor uses event-time windowing. One partition stops producing events (its producer is down), while other partitions continue. What problem does this cause for watermark computation?',
    options: [
      'No problem — watermarks are computed per partition independently',
      'The global watermark gets stuck at the timestamp of the last event from the idle partition, preventing all windows from closing and causing unbounded buffering',
      'The idle partition is automatically removed from watermark computation',
      'Events from active partitions are dropped because they are ahead of the watermark',
    ],
    correctAnswer: 1,
    explanation: 'The global watermark is typically the minimum of per-partition watermarks. An idle partition\'s watermark does not advance, so the global watermark gets stuck. This prevents downstream windows from firing, causing unbounded state growth. Solutions include idle-source detection (advancing the idle partition\'s watermark based on processing time) as implemented in Flink.',
    tags: ['time-windows', 'watermarks', 'idle-partitions', 'flink'],
  },
  {
    id: 'ddia-stream-l4-008',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A sliding window is defined as "all events within the last 10 minutes of each event." How does this differ from a hopping window, and what is the computational implication?',
    options: [
      'A sliding window is triggered by every event and conceptually creates a unique window per event, which is more expensive than a hopping window that triggers at fixed intervals',
      'A sliding window is cheaper because it reuses computation from the previous window',
      'A hopping window processes more events per window than a sliding window',
      'They are the same thing with different names',
    ],
    correctAnswer: 0,
    explanation: 'A sliding window places each event at the end of its own window (covering the preceding 10 minutes). In the worst case, every event triggers window computation. A hopping window only triggers at fixed hop intervals. Sliding windows can be implemented efficiently by maintaining a sorted buffer and incrementally adding/removing events.',
    tags: ['time-windows', 'sliding-window', 'hopping-window', 'performance'],
  },
  {
    id: 'ddia-stream-l4-009',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'A Flink job checkpoints state to a distributed filesystem every 30 seconds. If the job fails and restarts from the last checkpoint, what happens to events that were processed between the checkpoint and the failure?',
    options: [
      'The job resumes from the exact point of failure without any reprocessing',
      'Those events are recovered from Flink\'s internal memory without reprocessing',
      'Those events are lost and never reprocessed',
      'Those events are reprocessed from the source (e.g., by resetting Kafka consumer offsets to the checkpoint position), and exactly-once semantics is maintained because output from the failed run is not committed',
    ],
    correctAnswer: 3,
    explanation: 'Flink\'s checkpointing mechanism stores both operator state and source offsets atomically. On recovery, it restores state to the checkpoint and replays input from the checkpointed offsets. Output sinks use two-phase commit or idempotent writes to ensure that the partial output from the failed attempt is not visible, achieving exactly-once end-to-end.',
    tags: ['exactly-once', 'flink', 'checkpointing', 'recovery'],
  },
  {
    id: 'ddia-stream-l4-010',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'What is the fundamental difference between Spark Streaming\'s microbatch approach and Flink\'s continuous operator approach to exactly-once semantics?',
    options: [
      'Spark Streaming provides stronger guarantees than Flink',
      'Flink uses microbatching internally but hides it from the user',
      'Spark Streaming divides the stream into discrete mini-batches and reprocesses entire batches on failure (adding latency equal to batch interval); Flink processes events continuously and uses periodic asynchronous checkpoints for recovery (lower latency)',
      'Spark Streaming does not support exactly-once; only Flink does',
    ],
    correctAnswer: 2,
    explanation: 'Spark Streaming\'s microbatch model provides natural exactly-once semantics at the cost of latency (typically seconds). Flink processes events one at a time with low latency and achieves exactly-once through asynchronous barrier-based checkpointing (Chandy-Lamport algorithm), allowing sub-second latency with the same guarantees.',
    tags: ['exactly-once', 'microbatching', 'flink', 'spark-streaming', 'comparison'],
  },
  {
    id: 'ddia-stream-l4-011',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'In a CQRS (Command Query Responsibility Segregation) system paired with event sourcing, what is the inherent consistency trade-off between the write side and the read side?',
    options: [
      'The write side is eventually consistent with the read side',
      'Both sides are always strongly consistent because they share the same event log',
      'There is no consistency trade-off — CQRS eliminates consistency concerns',
      'The read side is eventually consistent with the write side because there is an asynchronous step between appending an event and updating the read-side projection',
    ],
    correctAnswer: 3,
    explanation: 'In CQRS with event sourcing, the write side appends events to the log and the read side asynchronously consumes those events to update its projections. There is an inherent propagation delay, making the read side eventually consistent. This is acceptable for many use cases but must be considered in the system design.',
    tags: ['event-sourcing', 'cqrs', 'eventual-consistency'],
  },
  {
    id: 'ddia-stream-l4-012',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'A team uses Debezium CDC with Kafka Connect. They discover that during a database failover (primary to replica promotion), some events are duplicated and others are missing in the CDC stream. What is causing this?',
    options: [
      'The new primary\'s binlog position differs from the old primary\'s, and Debezium\'s stored offset references the old binlog, causing it to either re-read events (duplicates) or skip events (gaps) depending on how the offsets map',
      'Kafka Connect does not support database failovers',
      'The replica database does not have CDC-compatible binlog format',
      'Debezium pauses indefinitely during failovers',
    ],
    correctAnswer: 0,
    explanation: 'During a database failover, the binlog file names and positions on the new primary may not match the old primary\'s. Debezium\'s stored offset (file + position) from the old primary may map to a different position in the new primary\'s binlog. Solutions include using GTIDs (Global Transaction IDs) which are consistent across primary and replicas.',
    tags: ['cdc', 'debezium', 'database-failover', 'gtid'],
  },
  {
    id: 'ddia-stream-l4-013',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'A stream processor writes results to an external database (not Kafka). The processor uses Kafka transactions for exactly-once within Kafka, but the external database write is outside the transaction boundary. How can end-to-end exactly-once be achieved?',
    options: [
      'Buffer all database writes in memory and flush only after the Kafka transaction commits',
      'Make the database write idempotent (e.g., using a unique event ID as the primary key with upsert semantics) so that reprocessing the same event on failure produces the same result',
      'Use at-most-once delivery for the database write to avoid duplicates',
      'Extend the Kafka transaction to include the database write using XA/2PC',
    ],
    correctAnswer: 1,
    explanation: 'Kafka transactions only cover Kafka-to-Kafka processing. For external sinks, the recommended approach is idempotent writes. By including a deterministic deduplication key (derived from the input event) in the database write, reprocessing the same event on failure is safe because the second write has no additional effect.',
    tags: ['exactly-once', 'idempotency', 'external-sink', 'end-to-end'],
  },
  {
    id: 'ddia-stream-l4-014',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'In a table-table join maintained by a stream processor (e.g., joining orders with customers via their CDC streams), what happens when a customer\'s address is updated?',
    options: [
      'The customer table is locked until all affected orders are reprocessed',
      'The processor detects the customer update, re-evaluates the join for all existing orders for that customer, and emits updated join results downstream',
      'The update is ignored until the next batch window triggers',
      'Only future orders are affected — past join results remain unchanged',
    ],
    correctAnswer: 1,
    explanation: 'A table-table join maintains both sides in state. When a customer record changes, the processor finds all orders matching that customer key, recomputes the join result, and emits the updated rows downstream. This is essentially incremental materialized view maintenance performed by the stream processor.',
    tags: ['stream-joins', 'table-table-join', 'materialized-view', 'incremental-update'],
  },
  {
    id: 'ddia-stream-l4-015',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'An event-sourced system needs to evolve an event schema (e.g., splitting a "name" field into "firstName" and "lastName"). Old events use the old schema and new events use the new schema. What is the recommended approach?',
    options: [
      'Rewrite all historical events with the new schema',
      'Stop the system, migrate all events, and restart',
      'Maintain two separate event stores, one for each schema version',
      'Use upcasting: when reading old events, apply a transformation (upcaster) that converts them to the latest schema version on the fly',
    ],
    correctAnswer: 3,
    explanation: 'Upcasting transforms old event formats to the latest version at read time. When an old event is loaded, the upcaster splits "name" into "firstName" and "lastName" before the event handler processes it. This avoids rewriting the immutable event log while allowing the application to work with a single current schema.',
    tags: ['event-sourcing', 'schema-evolution', 'upcasting'],
  },
  {
    id: 'ddia-stream-l4-016',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A Flink job performs event-time windowed aggregation. The team observes that during backfill (replaying historical data), windows fire almost instantly, but during live processing, windows take their full duration to fire. Why?',
    options: [
      'The historical data has fewer events, so windows fill up faster',
      'Flink uses a different windowing algorithm for backfill versus live processing',
      'Flink detects backfill mode and disables watermark delays',
      'During backfill, events arrive rapidly with advancing event timestamps, so watermarks advance quickly and windows complete in seconds of processing time even though they span hours of event time',
    ],
    correctAnswer: 3,
    explanation: 'Watermarks are driven by event timestamps, not wall-clock time. During backfill, events for hours or days of event time may arrive in seconds of processing time, causing watermarks to advance rapidly. Windows covering those event-time ranges fire quickly. During live processing, event time advances roughly at the rate of wall-clock time, so windows take their natural duration.',
    tags: ['time-windows', 'watermarks', 'backfill', 'event-time-vs-processing-time'],
  },
  {
    id: 'ddia-stream-l4-017',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'Flink uses the Chandy-Lamport algorithm for distributed snapshots. How do checkpoint barriers work in this algorithm?',
    options: [
      'Barriers are injected only at the sink operators and propagate backward to the sources',
      'Each operator independently decides when to checkpoint based on its local clock',
      'The coordinator injects barrier markers into input streams; when an operator receives barriers from all inputs, it snapshots its state and forwards the barriers, creating a consistent cut without stopping processing',
      'All processing stops while the checkpoint is taken, creating a pause in the pipeline',
    ],
    correctAnswer: 2,
    explanation: 'Flink injects checkpoint barriers into the source streams. These barriers flow through the dataflow graph like regular events. When an operator receives a barrier on all its input channels (barrier alignment), it snapshots its state and forwards the barrier. This creates a consistent global snapshot without pausing processing, implementing the Chandy-Lamport distributed snapshot algorithm.',
    tags: ['exactly-once', 'flink', 'chandy-lamport', 'checkpoint-barriers'],
  },
  {
    id: 'ddia-stream-l4-018',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'DDIA discusses why "exactly-once" delivery is technically impossible in a distributed system. What does "effectively exactly-once" actually mean?',
    options: [
      'Messages are truly delivered exactly once at the network layer',
      'The system uses at-least-once delivery combined with idempotent processing or transactional state updates, so the observable effect is the same as if each message were processed exactly once, even though messages may be delivered multiple times',
      'The system prevents all network duplicates',
      'Messages are delivered at most once with guaranteed success',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that true exactly-once delivery is impossible in distributed systems (messages may be retransmitted). "Effectively exactly-once" means the system ensures that even if a message is processed multiple times (due to retries), the observable outcome is identical to processing it once. This is achieved through idempotent operations or atomic state updates.',
    tags: ['exactly-once', 'effectively-exactly-once', 'idempotency', 'expert'],
  },
  {
    id: 'ddia-stream-l4-019',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'DDIA discusses the duality between a log (stream of events) and a table (current state). What is this duality?',
    options: [
      'Logs and tables are stored differently on disk but otherwise identical',
      'A table is the result of applying all events in a log (fold/reduce); a log is the record of all changes made to a table (changelog). Each can be derived from the other.',
      'Logs are for batch processing; tables are for stream processing',
      'Tables are immutable; logs are mutable',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes the table-log duality: a change log (stream of events) can be replayed to reconstruct a table (current state), and a table\'s changes over time produce a log. This fundamental duality underlies CDC, event sourcing, and stream-table joins. It means tables and streams are two sides of the same coin.',
    tags: ['log-table-duality', 'event-sourcing', 'cdc', 'fundamental', 'expert'],
  },
  {
    id: 'ddia-stream-l4-020',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'DDIA discusses the challenge of determining when a window is "complete" in event-time processing. Why is this fundamentally harder than in batch processing?',
    options: [
      'Event-time windows require more memory than batch windows',
      'In batch processing, the input is finite and complete — you can see all events. In stream processing, you never know for certain that all events for a time window have arrived, because stragglers may arrive arbitrarily late. Watermarks are a heuristic, not a guarantee.',
      'Stream processing frameworks lack window support',
      'Event timestamps are unreliable in stream processing',
    ],
    correctAnswer: 1,
    explanation: 'DDIA emphasizes that in batch processing, the dataset is bounded — you can definitively determine all events for a time window. In stream processing, events may arrive out of order and arbitrarily late. Watermarks provide a best-guess declaration of completeness, but there is always a trade-off between waiting longer (more complete) and emitting results sooner (lower latency).',
    tags: ['time-windows', 'completeness', 'watermarks', 'batch-vs-stream', 'expert'],
  },
  {
    id: 'ddia-stream-l4-021',
    language: 'ddia',
    level: 'level4',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'DDIA describes CDC as a way to turn the database "inside out." What does this mean architecturally?',
    options: [
      'The database schema is exposed publicly',
      'Instead of the database being the center with all reads going through it, CDC publishes the database\'s internal state changes as a stream, allowing derived systems (caches, indexes, analytics) to be materialized independently — the write path and read paths are decoupled',
      'The database is replaced with a message broker',
      'CDC inverts the database\'s B-tree structure',
    ],
    correctAnswer: 1,
    explanation: 'DDIA uses the "turning the database inside out" metaphor to describe how CDC decouples the write path (primary database) from read paths (derived views, caches, search indexes). The database\'s internal replication log becomes an external API, enabling each derived system to maintain its own optimized representation of the data independently.',
    tags: ['cdc', 'turning-database-inside-out', 'derived-data', 'architecture', 'expert'],
  },
];

export default questions;
