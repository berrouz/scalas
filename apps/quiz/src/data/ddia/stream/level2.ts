import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-stream-l2-001',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'What is a key advantage of event sourcing over traditional CRUD-based state storage?',
    options: [
      'Event sourcing requires less disk space than CRUD databases',
      'Event sourcing eliminates the need for a message broker',
      'Event sourcing preserves the full history of changes, enabling audit trails and the ability to reconstruct past states',
      'Event sourcing guarantees faster read performance for all queries',
    ],
    correctAnswer: 2,
    explanation: 'Event sourcing retains the complete history of every change. This enables audit logging, debugging by replaying events, building new views retroactively, and understanding exactly how the system arrived at its current state — none of which are possible when state is overwritten in place.',
    tags: ['event-sourcing', 'advantages', 'audit-trail'],
  },
  {
    id: 'ddia-stream-l2-002',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'What is a significant trade-off of using event sourcing?',
    options: [
      'Deriving current state from events requires replaying the log, which can be slow without snapshots, and the event schema must evolve carefully',
      'Event sourcing cannot work with more than one consumer',
      'Event sourcing prevents the use of any relational databases',
      'Events cannot be partitioned across nodes',
    ],
    correctAnswer: 0,
    explanation: 'Event sourcing trades off simplicity of reads for richness of the history. Reconstructing current state requires replaying events (mitigated by snapshots), event schemas must be evolved carefully since events are immutable, and some queries are harder to answer from an event log than from a denormalized state store.',
    tags: ['event-sourcing', 'trade-offs', 'schema-evolution'],
  },
  {
    id: 'ddia-stream-l2-003',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'How does log-based CDC compare to trigger-based CDC?',
    options: [
      'Log-based CDC requires modifying application code, while trigger-based CDC does not',
      'Trigger-based CDC captures more information than log-based CDC',
      'Trigger-based CDC is always preferred because it is more reliable',
      'Log-based CDC reads the internal replication log with low overhead, while trigger-based CDC uses application-level triggers that add write overhead and are fragile',
    ],
    correctAnswer: 3,
    explanation: 'Log-based CDC reads the database\'s existing replication log and imposes minimal overhead on the database. Trigger-based CDC installs triggers on tables that fire on every write, adding significant overhead and complexity. Log-based CDC is generally preferred for production use.',
    tags: ['cdc', 'log-based', 'trigger-based', 'comparison'],
  },
  {
    id: 'ddia-stream-l2-004',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'Why is CDC often preferable to dual writes (writing to both a database and a message queue from the application)?',
    options: [
      'Dual writes always require two-phase commit, which CDC avoids',
      'Dual writes can lead to race conditions and inconsistency because the two writes are not atomic, while CDC derives the stream from a single source of truth',
      'CDC is faster because it writes to fewer systems',
      'CDC supports more message formats than dual writes',
    ],
    correctAnswer: 1,
    explanation: 'With dual writes, if one write succeeds and the other fails (or they arrive in different orders due to concurrency), the two systems become permanently inconsistent. CDC avoids this by deriving the event stream from the database\'s own log, making the database the single source of truth.',
    tags: ['cdc', 'dual-writes', 'consistency', 'single-source-of-truth'],
  },
  {
    id: 'ddia-stream-l2-005',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'What is the key difference between a stream-table join and a stream-stream join?',
    options: [
      'Stream-table joins require windowing; stream-stream joins do not',
      'Stream-stream joins are deterministic; stream-table joins are not',
      'A stream-table join enriches each event from the current state of a table, while a stream-stream join correlates events from two streams within a time window',
      'A stream-table join produces output once; a stream-stream join produces continuous output',
    ],
    correctAnswer: 2,
    explanation: 'In a stream-table join, each stream event is enriched by looking up data in a (possibly changing) table. In a stream-stream join, events from two streams must be buffered and matched within a time window because both sides are unbounded and events may arrive at different times.',
    tags: ['stream-joins', 'stream-table-join', 'stream-stream-join', 'comparison'],
  },
  {
    id: 'ddia-stream-l2-006',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'What is a table-table join (also called a materialized view maintenance join)?',
    options: [
      'A batch join of two database tables run on a schedule',
      'A join where both inputs are CDC streams representing database tables, and the result is kept up to date as either table changes',
      'A join that only triggers when both tables receive an update in the same transaction',
      'A join that merges two tables into one table by deleting duplicates',
    ],
    correctAnswer: 1,
    explanation: 'A table-table join takes two CDC streams (each representing a changelog of a database table) and maintains a materialized view of the join result. Whenever either input table changes, the join output is updated accordingly, similar to how a database would maintain an index.',
    tags: ['stream-joins', 'table-table-join', 'materialized-view'],
  },
  {
    id: 'ddia-stream-l2-007',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'What is the difference between event time and processing time, and why does it matter?',
    options: [
      'Event time is when the event occurred; processing time is when the system processes it — they can differ due to delays, causing events to arrive out of order',
      'Event time is set by the consumer; processing time is set by the producer',
      'Processing time is always earlier than event time due to network latency',
      'They are always the same in a well-designed system',
    ],
    correctAnswer: 0,
    explanation: 'Event time is the timestamp at which the event actually occurred (set by the producer). Processing time is when the stream processor processes the event. Due to network delays, queueing, and retries, these can differ significantly, and events may arrive out of order with respect to their event time.',
    tags: ['time-windows', 'event-time', 'processing-time'],
  },
  {
    id: 'ddia-stream-l2-008',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'What is a watermark in stream processing?',
    options: [
      'A limit on the number of events that can be buffered in memory',
      'A threshold that declares the system will not expect any more events with a timestamp older than the watermark, used to trigger window completion',
      'A unique identifier embedded in each event for deduplication',
      'A special marker event that signals the end of a stream',
    ],
    correctAnswer: 1,
    explanation: 'A watermark is a declaration that no more events with a timestamp earlier than the watermark are expected. It allows the stream processor to decide when a time window is complete and can be finalized, balancing completeness of results against latency.',
    tags: ['time-windows', 'watermarks', 'late-events'],
  },
  {
    id: 'ddia-stream-l2-009',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'How do tumbling windows and hopping windows compare?',
    options: [
      'Tumbling windows overlap; hopping windows do not',
      'Tumbling windows use event time; hopping windows use processing time',
      'Tumbling windows are a special case of hopping windows where the hop size equals the window size, so there is no overlap',
      'Hopping windows are a special case of tumbling windows with variable-size hops',
    ],
    correctAnswer: 2,
    explanation: 'A tumbling window is effectively a hopping window where the hop interval equals the window length, meaning windows are contiguous and non-overlapping. When the hop is smaller than the window length, windows overlap and each event may belong to multiple windows.',
    tags: ['time-windows', 'tumbling-window', 'hopping-window', 'comparison'],
  },
  {
    id: 'ddia-stream-l2-010',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'Why is true "exactly-once" delivery generally considered impossible in a distributed system?',
    options: [
      'Because CPUs cannot count messages exactly',
      'Because message brokers always drop a small percentage of messages',
      'Because in the presence of network faults, you cannot distinguish a lost message from a slow one, so the sender must retry (risking duplicates) or give up (risking loss)',
      'Because network protocols do not support it',
    ],
    correctAnswer: 2,
    explanation: 'In a distributed system, if a sender does not receive an acknowledgment, it cannot tell whether the message was lost or the acknowledgment was lost. Retrying risks duplicate delivery (at-least-once), and not retrying risks message loss (at-most-once). Exactly-once delivery requires additional mechanisms like idempotency.',
    tags: ['exactly-once', 'distributed-systems', 'impossibility'],
  },
  {
    id: 'ddia-stream-l2-011',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'How does idempotency help achieve effectively-exactly-once processing?',
    options: [
      'Idempotent operations are faster, reducing the chance of timeouts',
      'Idempotent operations automatically deduplicate messages at the network level',
      'Idempotency prevents messages from being sent more than once',
      'If an operation is idempotent, processing a message twice has the same effect as processing it once, so at-least-once delivery effectively becomes exactly-once',
    ],
    correctAnswer: 3,
    explanation: 'With at-least-once delivery, a message may be processed multiple times. If the processing operation is idempotent, these duplicate processings produce the same result as a single processing, effectively achieving exactly-once semantics from the application\'s perspective.',
    tags: ['exactly-once', 'idempotency', 'at-least-once'],
  },
  {
    id: 'ddia-stream-l2-012',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'What is microbatching, and how does it relate to stream processing guarantees?',
    options: [
      'A technique for compressing events before processing',
      'Breaking the stream into small batches (e.g., 1 second), processing each batch atomically, which simplifies exactly-once semantics within each batch',
      'Sending events in tiny network packets for lower latency',
      'Processing events one at a time with a small buffer',
    ],
    correctAnswer: 1,
    explanation: 'Microbatching (used by Spark Streaming) divides the stream into small time-based batches and processes each batch as a mini batch job. This approach provides exactly-once semantics within each batch because the batch can be retried atomically, at the cost of increased latency equal to the batch interval.',
    tags: ['exactly-once', 'microbatching', 'spark-streaming'],
  },
  {
    id: 'ddia-stream-l2-013',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'How does event sourcing differ from change data capture (CDC)?',
    options: [
      'They are identical concepts with different names',
      'CDC stores events immutably; event sourcing does not',
      'Event sourcing only works with NoSQL databases; CDC only works with relational databases',
      'In event sourcing, events are designed by the application as the primary write model; in CDC, events are extracted from a database that was written to using a traditional CRUD model',
    ],
    correctAnswer: 3,
    explanation: 'In event sourcing, the application is explicitly designed to write events as the primary data model. In CDC, the application writes to a traditional mutable database, and the change events are extracted after the fact from the database\'s replication log. The level of intent in the event design differs significantly.',
    tags: ['event-sourcing', 'cdc', 'comparison'],
  },
  {
    id: 'ddia-stream-l2-014',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'What problem does log compaction solve in the context of CDC?',
    options: [
      'It speeds up write throughput by compressing events',
      'It reduces the size of the log by retaining only the most recent value for each key, allowing new consumers to bootstrap without replaying the entire history',
      'It merges multiple CDC streams into a single stream',
      'It encrypts old events to comply with data retention policies',
    ],
    correctAnswer: 1,
    explanation: 'Log compaction (as in Kafka) discards older records for a key when a newer record with the same key exists. For CDC, this means the compacted log contains the latest state of every row, allowing a new consumer to read the compacted log to get the current state without replaying the full change history.',
    tags: ['cdc', 'log-compaction', 'kafka'],
  },
  {
    id: 'ddia-stream-l2-015',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'What are late events and how are they typically handled?',
    options: [
      'Late events arrive after the watermark has passed their event-time window — they can be dropped, processed with an updated result, or routed to a side output',
      'Late events are events with missing timestamps — they are assigned the current processing time',
      'Late events are events that arrive after processing has started — they are always discarded',
      'Late events are events that fail validation — they are sent to a dead-letter queue',
    ],
    correctAnswer: 0,
    explanation: 'Late events are those whose event time falls into a window that has already been declared complete (the watermark has passed). Strategies include dropping them, emitting a correction/update to the previous result, or routing them to a side output for separate handling.',
    tags: ['time-windows', 'late-events', 'watermarks'],
  },
  {
    id: 'ddia-stream-l2-016',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'Why do stream-stream joins require maintaining state?',
    options: [
      'Because the join result must be stored in a database',
      'Because stream processors cannot access event data without state',
      'Because events from one stream may arrive before their matching event on the other stream, so events must be buffered until a match arrives or the window expires',
      'Because both streams must be sorted before joining',
    ],
    correctAnswer: 2,
    explanation: 'In a stream-stream join, matching events from two streams may arrive at different times. The processor must buffer events from both streams within the join window. When a new event arrives, it is checked against buffered events from the other stream for matches.',
    tags: ['stream-joins', 'stream-stream-join', 'state-management'],
  },
  {
    id: 'ddia-stream-l2-017',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'How do Kafka transactions support exactly-once semantics in stream processing?',
    options: [
      'By preventing any message from being sent more than once at the network level',
      'By limiting each consumer to a single partition',
      'By atomically committing the consumption of input messages and the production of output messages together, so either all succeed or none do',
      'By encrypting messages so duplicates can be detected',
    ],
    correctAnswer: 2,
    explanation: 'Kafka\'s transactional API allows a stream processor to atomically read from input topics, process events, write to output topics, and commit consumer offsets as a single atomic operation. If the processor fails, the entire transaction is rolled back and can be retried without producing duplicates.',
    tags: ['exactly-once', 'kafka', 'transactions'],
  },
  {
    id: 'ddia-stream-l2-018',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Messaging',
    question: 'What is backpressure in stream processing?',
    options: [
      'Compressing data to reduce message size',
      'A mechanism where a slow consumer signals upstream to slow down, preventing the consumer from being overwhelmed',
      'Replaying messages backward through the pipeline',
      'A technique for prioritizing certain messages over others',
    ],
    correctAnswer: 1,
    explanation: 'Backpressure is a flow control mechanism: when a consumer cannot keep up with the rate of incoming messages, it signals the producer to slow down. This prevents buffer overflow and out-of-memory errors. Unix pipes naturally provide backpressure; some message brokers support it explicitly.',
    tags: ['backpressure', 'flow-control', 'messaging'],
  },
  {
    id: 'ddia-stream-l2-019',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'Why does DDIA describe the database as a stream of changes?',
    options: [
      'Because databases store data as streams internally',
      'The replication log of a database is essentially a stream of write events — CDC captures this stream and makes it available to external consumers',
      'Because all database queries are processed as streams',
      'Because databases use streaming protocols for all network communication',
    ],
    correctAnswer: 1,
    explanation: 'DDIA observes that the replication log of a database is, fundamentally, a stream of state-changing events. CDC tools like Debezium tap into this stream, making the database\'s internal change log available to external systems. This enables real-time data integration and derived data systems.',
    tags: ['cdc', 'replication-log', 'change-stream'],
  },
  {
    id: 'ddia-stream-l2-020',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'What is a watermark in stream processing?',
    options: [
      'A security marker to detect message tampering',
      'A declaration by the system that all events with event time before the watermark have been seen — it is used to trigger window computations',
      'A limit on the number of events per window',
      'A marker in the log that separates different partitions',
    ],
    correctAnswer: 1,
    explanation: 'A watermark is a heuristic assertion that no more events with a timestamp earlier than the watermark will arrive. When the watermark passes the end of a window, the window can be finalized and results emitted. Watermarks allow event-time processing despite out-of-order arrival.',
    tags: ['watermarks', 'event-time', 'window-trigger'],
  },
  {
    id: 'ddia-stream-l2-021',
    language: 'ddia',
    level: 'level2',
    category: 'Stream Processing',
    subcategory: 'Messaging',
    question: 'What is the difference between fan-out delivery and load-balanced delivery in messaging systems?',
    options: [
      'Fan-out uses TCP; load-balanced uses UDP',
      'Fan-out delivers each message to all consumers (pub/sub); load-balanced delivers each message to one of the consumers (work queue)',
      'Fan-out is faster but less reliable than load-balanced',
      'They are different names for the same delivery pattern',
    ],
    correctAnswer: 1,
    explanation: 'DDIA distinguishes two messaging patterns: fan-out (each message is delivered to all subscribers, like a pub/sub topic) and load-balanced (each message is delivered to one of several consumers for parallel processing, like a work queue). Kafka supports both via consumer groups.',
    tags: ['messaging', 'fan-out', 'load-balanced', 'pub-sub'],
  },
];

export default questions;
