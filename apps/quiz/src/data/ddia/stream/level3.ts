import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-stream-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'A team is building an e-commerce system using event sourcing. They need to enforce a business rule that an item cannot be sold if it is out of stock. What is the recommended approach?',
    options: [
      'Store the stock count in a separate relational database and skip event sourcing for inventory',
      'Check the stock in a read-side projection before appending the event and hope no concurrent update occurs',
      'Use a synchronous command handler that validates the command against the current state derived from the event log before emitting the event, serializing writes per aggregate',
      'Write the "item sold" event first and roll it back later if stock was insufficient',
    ],
    correctAnswer: 2,
    explanation: 'In event sourcing, business invariants are enforced in the command handler. The handler loads the current state of the aggregate (e.g., from a snapshot plus recent events), validates the command against that state, and only then appends the event. Serializing writes per aggregate ensures no concurrent command can violate the invariant.',
    tags: ['event-sourcing', 'commands', 'invariants', 'applied'],
  },
  {
    id: 'ddia-stream-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'An event-sourced system has accumulated millions of events per aggregate over several years. Replaying all events to rebuild state takes minutes. What is the best mitigation?',
    options: [
      'Store periodic snapshots of the aggregate state and only replay events after the latest snapshot',
      'Delete old events to keep the log short',
      'Switch to a CRUD database and abandon event sourcing',
      'Increase CPU and memory to speed up replay',
    ],
    correctAnswer: 0,
    explanation: 'Snapshots capture the aggregate state at a point in time. On subsequent loads, the system restores the snapshot and only replays events that occurred after it. This reduces reconstruction time from proportional to the full history to proportional to events since the last snapshot.',
    tags: ['event-sourcing', 'snapshots', 'performance', 'applied'],
  },
  {
    id: 'ddia-stream-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'A microservices team uses dual writes to keep a PostgreSQL database and an Elasticsearch index in sync. They notice the two stores sometimes diverge. What approach would DDIA recommend?',
    options: [
      'Use a distributed two-phase commit protocol between PostgreSQL and Elasticsearch',
      'Add retry logic to the dual write to make it more reliable',
      'Replace dual writes with CDC: let PostgreSQL be the source of truth and use log-based CDC to feed changes into Elasticsearch',
      'Periodically run a batch reconciliation job to fix inconsistencies',
    ],
    correctAnswer: 2,
    explanation: 'DDIA recommends CDC over dual writes because dual writes are not atomic — one can succeed while the other fails, or race conditions can cause them to be applied in different orders. CDC derives the Elasticsearch updates from the PostgreSQL replication log, ensuring a single source of truth.',
    tags: ['cdc', 'dual-writes', 'consistency', 'applied'],
  },
  {
    id: 'ddia-stream-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'A team needs to publish domain events reliably whenever a database record changes, but they cannot use log-based CDC because their database does not expose its replication log. What pattern should they use?',
    options: [
      'Use the outbox pattern: write events to an outbox table in the same transaction as the data change, then poll or use triggers to publish them',
      'Publish events from the application code before committing the database transaction',
      'Poll the database for changes using a last-modified timestamp column',
      'Use a two-phase commit between the database and the message broker',
    ],
    correctAnswer: 0,
    explanation: 'The outbox pattern writes events to a dedicated outbox table within the same database transaction that makes the data change. A separate process reads the outbox table and publishes events to the message broker. Because the event and data change are in the same transaction, they are guaranteed to be consistent.',
    tags: ['cdc', 'outbox-pattern', 'reliability', 'applied'],
  },
  {
    id: 'ddia-stream-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'An ad-tech company needs to join a stream of ad clicks with a stream of ad impressions to calculate click-through rates. Clicks may arrive up to 1 hour after the impression. What type of join is needed?',
    options: [
      'A table-table join on the impression and click databases',
      'A stream-stream join with a 1-hour time window, buffering impressions and matching clicks when they arrive',
      'A batch join run hourly on accumulated data',
      'A stream-table join using the impressions table',
    ],
    correctAnswer: 1,
    explanation: 'This is a classic stream-stream join scenario. Both impressions and clicks are unbounded event streams. The system buffers impressions within a 1-hour window and matches arriving clicks to their corresponding impressions. Unmatched impressions are emitted as "no click" after the window expires.',
    tags: ['stream-joins', 'stream-stream-join', 'window-join', 'applied'],
  },
  {
    id: 'ddia-stream-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'A stream processing pipeline enriches order events with the latest customer information (name, address, loyalty tier). Customer data changes infrequently. What join type and data loading strategy is most appropriate?',
    options: [
      'A stream-stream join between orders and customer update events',
      'A stream-table join, keeping a local copy of the customer table (loaded via CDC) and looking up each order\'s customer',
      'A batch join that runs after all orders are collected',
      'A table-table join between the orders table and the customer table',
    ],
    correctAnswer: 1,
    explanation: 'This is a stream-table join. The order stream is enriched with customer data. The customer table can be loaded into the processor\'s local state via CDC, so the processor has a fresh local copy. Each order event triggers a lookup in the local customer state for enrichment.',
    tags: ['stream-joins', 'stream-table-join', 'enrichment', 'applied'],
  },
  {
    id: 'ddia-stream-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A ride-sharing service needs to compute the number of active rides per city in the last 5 minutes, updated every minute. Which window type should they use?',
    options: [
      'Sliding windows triggered on every event',
      'Tumbling windows of 5 minutes',
      'Session windows with a 5-minute gap timeout',
      'Hopping windows with a 5-minute window size and a 1-minute hop interval',
    ],
    correctAnswer: 3,
    explanation: 'A hopping window with a 5-minute length and 1-minute hop produces a new window every minute, each covering the last 5 minutes. This gives a smoothly updating count of active rides rather than the discontinuous jumps that a tumbling window would produce.',
    tags: ['time-windows', 'hopping-window', 'applied'],
  },
  {
    id: 'ddia-stream-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A web analytics system needs to group user clickstream events into activity sessions that end after 30 minutes of inactivity. Which window type is most appropriate?',
    options: [
      'Sliding windows of 30 minutes',
      'Session windows with a 30-minute inactivity gap timeout',
      'Tumbling windows of 30 minutes',
      'Hopping windows with a 30-minute size and 5-minute hop',
    ],
    correctAnswer: 1,
    explanation: 'Session windows are specifically designed for this use case. They group events by key (user) and create a window that extends as long as events keep arriving within the gap timeout. A session ends when no event arrives for 30 minutes. Unlike fixed windows, session windows have variable length.',
    tags: ['time-windows', 'session-window', 'applied'],
  },
  {
    id: 'ddia-stream-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A stream processor uses event-time tumbling windows of 1 minute. Some events arrive 5 minutes late due to mobile network delays. The watermark is configured with a 2-minute allowed lateness. What happens to the events that arrive 5 minutes late?',
    options: [
      'They are placed into the correct 1-minute window retroactively',
      'They are placed into the current window regardless of their event time',
      'The watermark is moved backward to accommodate them',
      'They are dropped (or sent to a side output) because they arrive after the watermark plus the allowed lateness has passed',
    ],
    correctAnswer: 3,
    explanation: 'Events arriving 5 minutes late exceed the 2-minute allowed lateness. By the time they arrive, their window has been finalized and its result emitted. These late events are typically dropped or routed to a side output for separate handling. The watermark never moves backward.',
    tags: ['time-windows', 'watermarks', 'late-events', 'applied'],
  },
  {
    id: 'ddia-stream-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'A payment processing pipeline must ensure that each payment is applied exactly once, even if the stream processor restarts. The downstream system is a traditional SQL database. What approach should the team use?',
    options: [
      'Use at-most-once delivery to avoid duplicate payments',
      'Make the write operation idempotent by including a unique payment ID and using an upsert (INSERT ON CONFLICT DO NOTHING)',
      'Process payments in large batches to reduce the probability of duplicates',
      'Rely on the message broker\'s exactly-once delivery guarantee alone',
    ],
    correctAnswer: 1,
    explanation: 'By including a unique payment ID (deduplication key) and using an idempotent write operation (e.g., INSERT ON CONFLICT DO NOTHING), reprocessing the same payment event produces the same result. Combined with at-least-once delivery, this achieves effectively-exactly-once semantics.',
    tags: ['exactly-once', 'idempotency', 'deduplication', 'applied'],
  },
  {
    id: 'ddia-stream-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'A Kafka Streams application reads from an input topic, transforms events, and writes to an output topic. The team wants exactly-once processing. What Kafka feature should they enable?',
    options: [
      'Using log compaction on the input topic',
      'Increasing the replication factor of the output topic to 3',
      'Kafka transactions (enable.idempotence=true and transactional.id) so that reading input, processing, and writing output are committed atomically',
      'Consumer group rebalancing with sticky assignment',
    ],
    correctAnswer: 2,
    explanation: 'Kafka\'s exactly-once semantics for stream processing uses transactions. The processor begins a transaction, reads from input topics, writes to output topics, and commits consumer offsets all atomically. If processing fails, the entire transaction is aborted, and nothing is visible to downstream consumers.',
    tags: ['exactly-once', 'kafka', 'transactions', 'applied'],
  },
  {
    id: 'ddia-stream-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'A company sets up Debezium CDC on their MySQL database but discovers that initial data already in the tables is not captured. Only new changes appear in the stream. What feature do they need?',
    options: [
      'Switch from MySQL to PostgreSQL for better CDC support',
      'Enable binlog retention to store the entire database history',
      'Manually export all tables to CSV and import them into the message broker',
      'Perform an initial snapshot: Debezium reads the current state of each table before starting to stream changes from the binlog',
    ],
    correctAnswer: 3,
    explanation: 'Debezium supports an initial snapshot mode where it first reads the entire current state of each table (a consistent snapshot) and produces events for all existing rows. After the snapshot completes, it switches to streaming changes from the binlog. This ensures no data is missed.',
    tags: ['cdc', 'debezium', 'initial-snapshot', 'applied'],
  },
  {
    id: 'ddia-stream-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'A team using event sourcing wants to add a new read-side projection (e.g., a leaderboard) that requires data from events going back to the beginning of the system. How can they build this?',
    options: [
      'Copy the production database and transform the data into the new projection format',
      'Query the existing projections and aggregate the data',
      'They cannot — only projections defined at system launch can use historical events',
      'Replay the entire event log from the beginning through the new projection\'s event handler to build up the projection\'s state',
    ],
    correctAnswer: 3,
    explanation: 'One of the key benefits of event sourcing is that the event log retains the full history. A new projection can be built at any time by replaying all events from the beginning through the new projection\'s event handler. This is sometimes called "temporal querying" or building a new read model.',
    tags: ['event-sourcing', 'projections', 'replay', 'applied'],
  },
  {
    id: 'ddia-stream-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'A fraud detection system needs to join a stream of login events with a stream of transaction events. A login followed by a transaction from a different country within 10 minutes should trigger an alert. If a transaction arrives but no login event was seen, what should happen?',
    options: [
      'The system waits indefinitely until a matching login arrives',
      'The system queries the login database for the missing event',
      'The system uses a left join: if no matching login exists in the window, it still emits the transaction (possibly with a null login), and the absence of a login itself may be a fraud signal',
      'The transaction is discarded because it cannot be joined',
    ],
    correctAnswer: 2,
    explanation: 'A left stream-stream join ensures all transactions are emitted regardless of whether a matching login is found within the window. The absence of a corresponding login event is itself valuable fraud-detection information. An inner join would lose these potentially suspicious transactions.',
    tags: ['stream-joins', 'stream-stream-join', 'left-join', 'applied'],
  },
  {
    id: 'ddia-stream-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'A team implements idempotent processing by storing processed event IDs in a database. Over time, the set of stored IDs grows unboundedly. What practical solution does DDIA suggest?',
    options: [
      'Store all event IDs forever since storage is cheap',
      'Use a Bloom filter that never expires',
      'Use a combination of event ID storage with expiration (TTL) and consumer offset tracking — once the offset advances past a point, IDs older than that point can be garbage collected',
      'Switch to at-most-once delivery to avoid needing deduplication',
    ],
    correctAnswer: 2,
    explanation: 'Event IDs can be garbage collected once the consumer offset has advanced past them and the window for potential reprocessing has closed. A TTL-based expiration or periodic cleanup of IDs older than a safe threshold keeps the deduplication store bounded.',
    tags: ['exactly-once', 'idempotency', 'deduplication', 'garbage-collection'],
  },
  {
    id: 'ddia-stream-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'A database schema migration adds a new column to a table that is being captured by CDC. What challenge does this create for downstream consumers?',
    options: [
      'All historical events are retroactively updated with a default value for the new column',
      'The new column is automatically ignored by CDC until explicitly registered',
      'CDC stops working entirely until the consumer is redeployed',
      'Downstream consumers receive events with the new field and must handle schema evolution — older events lack the field, newer events include it',
    ],
    correctAnswer: 3,
    explanation: 'Schema evolution in CDC means downstream consumers must handle events with different schemas. Using a schema registry and a format like Avro (which supports default values for new fields) allows consumers to process both old and new events. Historical events are not retroactively changed.',
    tags: ['cdc', 'schema-evolution', 'avro', 'applied'],
  },
  {
    id: 'ddia-stream-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Time Windows',
    question: 'A real-time dashboard shows the number of errors per microservice in tumbling 1-minute windows. The team notices that the count sometimes changes after the window was supposedly closed. What is likely happening?',
    options: [
      'Late-arriving events are being included in already-closed windows because the system uses allowed lateness, triggering window re-evaluation and updated results',
      'The tumbling windows are misconfigured and overlapping',
      'The stream processor is double-counting events due to rebalancing',
      'The dashboard is displaying cached stale data',
    ],
    correctAnswer: 0,
    explanation: 'When a system is configured with allowed lateness, late events that arrive after the watermark but within the allowed lateness threshold can trigger re-evaluation of a previously emitted window result. The dashboard receives an updated (refined) count, causing the number to change.',
    tags: ['time-windows', 'late-events', 'allowed-lateness', 'applied'],
  },
  {
    id: 'ddia-stream-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Exactly-Once Semantics',
    question: 'Your stream processor crashes and restarts, reprocessing the last 100 events. Some of those events triggered emails to users. Based on DDIA, how do you prevent duplicate emails?',
    options: [
      'Exactly-once processing guarantees prevent duplicates automatically',
      'Since the email-sending side effect is outside the stream processor\'s transactional boundary, you need external deduplication — store sent email IDs in a database and check before sending, or make the email service idempotent using event IDs',
      'Configure the stream processor to skip events it has already processed',
      'Use at-most-once delivery to prevent duplicates at the cost of lost emails',
    ],
    correctAnswer: 1,
    explanation: 'DDIA emphasizes that exactly-once semantics within a stream processor do not extend to external side effects like sending emails. External actions need their own deduplication mechanism. Storing sent event IDs and checking before sending, or using idempotent operations, ensures emails are not duplicated on reprocessing.',
    tags: ['exactly-once', 'side-effects', 'idempotency', 'deduplication', 'applied'],
  },
  {
    id: 'ddia-stream-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Stream Joins',
    question: 'Your e-commerce system needs to enrich order events with the latest product information. The product catalog changes frequently. Based on DDIA, what join type and data strategy should you use?',
    options: [
      'Query the product database for each order event in real time',
      'Use a stream-table join: maintain a local copy of the product catalog (updated via CDC) and join order events against this local table, avoiding remote database calls for each event',
      'Embed product information in each order event at write time',
      'Run a batch join every hour to add product data to orders',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes the stream-table join pattern: consume the product catalog\'s CDC stream to maintain a local (possibly in-memory) copy. When an order event arrives, enrich it by looking up the product locally. This avoids the latency and load of querying a remote database for every event.',
    tags: ['stream-joins', 'stream-table-join', 'cdc', 'enrichment', 'applied'],
  },
  {
    id: 'ddia-stream-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'CDC',
    question: 'Your team uses Kafka Connect with Debezium for CDC from PostgreSQL. The database has 500GB of data. A new consumer needs the complete current state plus ongoing changes. How does this work without disrupting the existing CDC pipeline?',
    options: [
      'The new consumer must wait for the database to be exported manually',
      'Kafka log compaction ensures the compacted topic contains the latest state for every key; the new consumer reads the compacted topic from the beginning to get the current state, then continues consuming new changes',
      'The new consumer queries the database directly for the initial state',
      'CDC cannot support new consumers after initial setup',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes how log compaction in Kafka retains the latest value for each key. A new consumer can read the compacted CDC topic from the beginning to get a snapshot of the current state of every row, then continue consuming from that point for ongoing changes. This is self-contained within Kafka.',
    tags: ['cdc', 'log-compaction', 'kafka', 'new-consumer', 'applied'],
  },
  {
    id: 'ddia-stream-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Stream Processing',
    subcategory: 'Event Sourcing',
    question: 'Your event-sourced system stores user events. A GDPR deletion request requires removing all data about a specific user. But the event log is supposed to be immutable. How do you handle this?',
    options: [
      'GDPR does not apply to event-sourced systems',
      'Delete only the read-side projections and keep the event log intact',
      'Use crypto-shredding: encrypt each user\'s events with a user-specific key, and delete the key when a GDPR request comes in, making the events unreadable',
      'Rewrite the entire event log excluding the user\'s events',
    ],
    correctAnswer: 2,
    explanation: 'DDIA acknowledges the tension between immutable event logs and data deletion requirements. Crypto-shredding is a practical solution: encrypt events with per-user keys. When deletion is required, destroy the key. The encrypted events remain in the log but are effectively deleted since they cannot be decrypted.',
    tags: ['event-sourcing', 'gdpr', 'crypto-shredding', 'immutability', 'applied'],
  },
];

export default questions;
