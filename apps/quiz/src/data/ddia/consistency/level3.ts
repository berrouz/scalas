import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-consistency-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Linearizability',
    question: 'A social media app shows a user their own post immediately after writing it, but other users see it with a delay. Which consistency model is this?',
    options: [
      'Eventual consistency with no guarantees',
      'Read-your-writes consistency (a form of causal consistency), not linearizability',
      'Strict serializability',
      'Linearizability',
    ],
    correctAnswer: 1,
    explanation: 'This is read-your-writes consistency (also called read-after-write consistency): the writing user always sees their own write, but other users may see it later. This is a causal guarantee (the user caused the write and should see its effect) but not linearizability, which would require all users to see the write at the same time.',
    tags: ['linearizability', 'read-your-writes', 'causal-consistency', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Linearizability',
    question: 'Two clients concurrently try to acquire a distributed lock. Client A writes "locked" and client B reads "unlocked" slightly later. Under linearizability, is this valid?',
    options: [
      'It depends on the wall-clock time of the operations',
      'No, distributed locks cannot be linearizable',
      'It is only valid if client B\'s read started before client A\'s write completed; if B\'s read started after A\'s write completed, it must return "locked"',
      'Yes, because client B read before client A\'s write completed',
    ],
    correctAnswer: 2,
    explanation: 'Under linearizability, if client B\'s read operation begins after client A\'s write operation completes, B must see "locked." But if B\'s read overlaps with A\'s write (they are concurrent), B may return either value. Linearizability constrains the ordering of non-overlapping operations.',
    tags: ['linearizability', 'distributed-lock', 'concurrent-operations', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Causal Consistency',
    question: 'A chat application shows messages out of order: an answer appears before the question it is responding to. What consistency guarantee is being violated?',
    options: [
      'Linearizability — the system is not behaving as a single copy',
      'Causal consistency — the reply causally depends on the question but is being displayed first',
      'Eventual consistency — the messages have not converged yet',
      'Serializability — the transactions are not isolated',
    ],
    correctAnswer: 1,
    explanation: 'This violates causal consistency. The reply causally depends on the question (it was written in response to it), so causal ordering requires the question to be displayed before the reply. Linearizability would also prevent this, but causal consistency is the minimal guarantee needed.',
    tags: ['causal-consistency', 'ordering', 'scenario', 'chat'],
  },
  {
    id: 'ddia-consistency-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Total Order Broadcast',
    question: 'A system uses total order broadcast to replicate a log of database writes. How does this help with consistency across replicas?',
    options: [
      'It only works if replicas are in the same data center',
      'Total order broadcast prevents write conflicts but does not help with replica consistency',
      'Every replica processes the same writes in the same order, so they all reach the same state — this is the basis of state machine replication',
      'Each replica can apply writes in any order as long as they eventually converge',
    ],
    correctAnswer: 2,
    explanation: 'This is the principle of state machine replication: if every replica processes the same sequence of writes in the same order, and the processing is deterministic, all replicas will end up in the same state. Total order broadcast guarantees the same ordering across all replicas.',
    tags: ['total-order-broadcast', 'state-machine-replication', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Raft',
    question: 'A 5-node Raft cluster has 2 nodes in datacenter A and 3 nodes in datacenter B. A network partition separates the two datacenters. What happens?',
    options: [
      'The original leader continues regardless of which datacenter it is in',
      'The entire cluster stops to prevent split-brain',
      'Datacenter B (with 3 nodes) can elect a leader and continue; datacenter A (with 2 nodes) cannot form a majority and stops accepting writes',
      'Both sides elect a leader and continue serving writes independently',
    ],
    correctAnswer: 2,
    explanation: 'With a 5-node cluster, a majority requires 3 nodes. Datacenter B has 3 nodes and can form a majority quorum, so it can elect a leader and continue serving reads and writes. Datacenter A has only 2 nodes and cannot form a majority, so it cannot elect a leader and must stop accepting writes.',
    tags: ['raft', 'partition', 'quorum', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Raft',
    question: 'In Raft, a leader crashes after replicating a log entry to 2 out of 5 followers but before committing it. What happens to that entry?',
    options: [
      'The entry is guaranteed to be committed by the new leader',
      'The followers commit the entry automatically after a timeout',
      'The entry may or may not survive — the new leader may overwrite it if a candidate with a longer or more up-to-date log wins the election',
      'The entry is immediately discarded by all followers',
    ],
    correctAnswer: 2,
    explanation: 'Since the entry was not committed (not replicated to a majority), it may or may not survive. If a follower with the entry becomes the new leader, it will be preserved. If a follower without it wins (because it has a higher term or equally up-to-date log), the entry may be overwritten. Raft\'s safety guarantees only apply to committed entries.',
    tags: ['raft', 'log-replication', 'crash', 'uncommitted', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Paxos',
    question: 'In Paxos, a proposer sends a prepare request with proposal number n=5. An acceptor has already accepted a value with n=3. What does the acceptor respond?',
    options: [
      'It forwards the request to the designated leader',
      'It promises not to accept proposals numbered less than 5 and returns the previously accepted value (with n=3) so the proposer knows what was already accepted',
      'It overwrites the old value immediately',
      'It rejects the prepare because it already accepted a value',
    ],
    correctAnswer: 1,
    explanation: 'In the prepare phase, if the acceptor has not already promised to ignore proposals numbered less than n=5, it promises to do so and returns any value it has already accepted (the value accepted at n=3). The proposer must then propose the previously accepted value if one exists, ensuring safety.',
    tags: ['paxos', 'prepare-phase', 'acceptor', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'ZooKeeper',
    question: 'A microservices architecture uses ZooKeeper for service discovery. Each service instance creates an ephemeral node on startup. What happens when a service instance crashes?',
    options: [
      'The ephemeral node remains until an administrator manually deletes it',
      'The node becomes a persistent node after a timeout',
      'ZooKeeper detects the session timeout, deletes the ephemeral node, and notifies watchers — other services learn the instance is gone',
      'ZooKeeper immediately restarts the crashed service',
    ],
    correctAnswer: 2,
    explanation: 'When a service instance crashes, its ZooKeeper session eventually times out. ZooKeeper then automatically deletes the ephemeral node associated with that session. Any other services watching that node are notified, allowing them to update their view of available service instances.',
    tags: ['zookeeper', 'service-discovery', 'ephemeral-nodes', 'failure-detection', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Linearizability',
    question: 'A system uses leaderless replication with quorum reads (r + w > n). Is this system linearizable?',
    options: [
      'Only if strict quorums are used with n=3, w=2, r=2',
      'Only if the system uses last-write-wins conflict resolution',
      'Not necessarily — due to issues like sloppy quorums, read repair timing, and concurrent write conflicts, quorum systems can return stale values',
      'Yes, quorum reads and writes always guarantee linearizability',
    ],
    correctAnswer: 2,
    explanation: 'Even with strict quorums (r + w > n), linearizability is not guaranteed in a leaderless system. Concurrent writes may arrive in different orders at different replicas, sloppy quorums may route reads to non-overlapping nodes, and read repair is asynchronous. Dynamo-style databases are generally not linearizable.',
    tags: ['linearizability', 'quorum', 'leaderless', 'dynamo', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Total Order Broadcast',
    question: 'You need to implement a unique username constraint across multiple database partitions. Why is total order broadcast (or consensus) needed?',
    options: [
      'Because you need all nodes to agree on the order of registrations — if two users try to claim the same name concurrently, total ordering ensures a deterministic winner',
      'Because you need to broadcast the username to all partitions for caching',
      'Because you need to encrypt the usernames consistently',
      'Because username validation requires complex regex that must run on all nodes',
    ],
    correctAnswer: 0,
    explanation: 'A uniqueness constraint requires all nodes to agree on which registration "came first." Total order broadcast ensures that if two users concurrently try to register the same username, all nodes see these requests in the same order and agree on which one succeeded. Without this, different nodes could accept different users for the same name.',
    tags: ['total-order-broadcast', 'uniqueness', 'constraint', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Raft',
    question: 'In a Raft cluster, the leader becomes isolated from the other nodes due to a network issue but can still receive client requests. What problem does this create?',
    options: [
      'No problem — the leader continues to serve requests normally',
      'The clients automatically switch to a new leader',
      'The leader accepts writes that it cannot replicate to a majority, so they will never be committed; meanwhile, the other nodes elect a new leader, causing a temporary split-brain where the old leader may mislead clients',
      'The followers immediately shut down to prevent data loss',
    ],
    correctAnswer: 2,
    explanation: 'The isolated leader may continue receiving client requests but cannot replicate them to a majority, so they cannot be committed. The other nodes will elect a new leader (since they stop receiving heartbeats). The old leader will eventually discover (via a higher term number) that it is no longer the leader and step down, but during this window clients may receive misleading responses.',
    tags: ['raft', 'network-partition', 'split-brain', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Paxos',
    question: 'Two Paxos proposers keep outbidding each other with higher proposal numbers, preventing any value from being accepted. What is this problem called, and how is it typically solved?',
    options: [
      'Split-brain — solved by increasing the number of acceptors',
      'Deadlock — solved by adding timeouts',
      'Byzantine failure — solved by adding digital signatures',
      'Livelock (or dueling proposers) — solved by electing a distinguished proposer (leader) so that typically only one proposer is active at a time',
    ],
    correctAnswer: 3,
    explanation: 'This is the livelock (or dueling proposers) problem: two proposers alternate sending prepare messages with increasing proposal numbers, each invalidating the other\'s accept messages. The standard solution is to elect a leader (distinguished proposer) so only one proposer is active, which is what Multi-Paxos does.',
    tags: ['paxos', 'livelock', 'dueling-proposers', 'leader', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'ZooKeeper',
    question: 'A distributed system uses ZooKeeper to implement a distributed lock. A client acquires the lock, then experiences a long GC pause. What risk does this create?',
    options: [
      'Other clients will wait indefinitely until the GC pause ends',
      'No risk — the lock is safe as long as ZooKeeper is running',
      'The GC pause causes ZooKeeper itself to crash',
      'ZooKeeper may consider the session expired due to the GC pause, delete the ephemeral lock node, and grant the lock to another client — the original client may still think it holds the lock after GC completes',
    ],
    correctAnswer: 3,
    explanation: 'If the GC pause exceeds the ZooKeeper session timeout, ZooKeeper will consider the client dead, delete its ephemeral node (the lock), and allow another client to acquire it. When the original client resumes after GC, it may not realize the lock was revoked. This is why fencing tokens are recommended.',
    tags: ['zookeeper', 'distributed-lock', 'gc-pause', 'fencing-token', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Causal Consistency',
    question: 'In a system with causal consistency, Client A writes x=1 then y=2. Client B reads y=2. Can Client B then read x=null (i.e., not yet see x=1)?',
    options: [
      'Yes, causal consistency only guarantees ordering within a single key',
      'Yes, because x and y are different keys',
      'No — since y=2 causally depends on x=1 (A wrote x before y in the same session), if B sees y=2 it must also see x=1',
      'It depends on the replication lag',
    ],
    correctAnswer: 2,
    explanation: 'Under causal consistency, if Client A\'s write of x=1 happened before its write of y=2 (and y=2 was a causal consequence of the session state that includes x=1), then any node that has seen y=2 must also have seen x=1. Causal consistency preserves cross-key ordering when there is a causal relationship.',
    tags: ['causal-consistency', 'cross-key', 'ordering', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Linearizability',
    question: 'An application stores account balances and uses a linearizable database. Two ATMs concurrently try to withdraw from the same account with a balance of $100 — each withdrawing $80. What does linearizability guarantee here?',
    options: [
      'Linearizability alone does not prevent both from succeeding — you also need an atomic compare-and-set or transaction to enforce the balance constraint',
      'Both withdrawals will succeed because linearizability does not prevent overdrafts',
      'Exactly one withdrawal will succeed and the other will be automatically rejected',
      'Linearizability ensures the balance never goes negative',
    ],
    correctAnswer: 0,
    explanation: 'Linearizability ensures that reads see the latest write, but it does not by itself enforce application-level constraints like "balance >= 0." You need an atomic read-modify-write operation (like compare-and-set) or a transaction with proper isolation. Linearizability provides the recency guarantee, but the constraint check requires atomicity.',
    tags: ['linearizability', 'atomic-operations', 'compare-and-set', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Raft',
    question: 'A Raft leader receives a write, replicates it to a majority, and commits it. Before responding to the client, the leader crashes. What happens?',
    options: [
      'The write is durable — the new leader will have the committed entry and the client can retry safely (with idempotent operations)',
      'The followers roll back the entry because the leader did not confirm it',
      'The write is lost because the client never received confirmation',
      'The system is in an inconsistent state that requires manual resolution',
    ],
    correctAnswer: 0,
    explanation: 'Once an entry is committed (replicated to a majority), it is durable and will survive leader crashes. The new leader is guaranteed to have all committed entries (because of the election restriction that requires up-to-date logs). The client may not know the write succeeded, so it might retry — idempotency is important.',
    tags: ['raft', 'commit', 'durability', 'crash-recovery', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'ZooKeeper',
    question: 'An application uses ZooKeeper to store cluster configuration. When the configuration changes, all nodes must update their local state. How should this be implemented?',
    options: [
      'ZooKeeper automatically pushes configuration to all registered clients via RPC',
      'Each node polls ZooKeeper every second for changes',
      'Each node sets a watch on the configuration znode and reacts to change notifications from ZooKeeper',
      'The administrator manually restarts each node after changing the configuration',
    ],
    correctAnswer: 2,
    explanation: 'ZooKeeper watches are the idiomatic mechanism: each node sets a watch on the configuration znode. When the configuration is updated, ZooKeeper notifies all watching clients, who can then read the new value and update their local state. This is more efficient than polling.',
    tags: ['zookeeper', 'watches', 'configuration', 'scenario'],
  },
  {
    id: 'ddia-consistency-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Two-Phase Commit',
    question: 'Your microservices architecture uses 2PC for distributed transactions across a payment service and an inventory service. The coordinator crashes after the prepare phase. Both services hold database locks. Based on DDIA, what is the operational impact?',
    options: [
      'The services automatically roll back after a timeout',
      'Both services are stuck holding locks indefinitely until the coordinator recovers and completes the transaction, potentially blocking all other transactions that need those rows',
      'The services can independently decide to commit',
      'The locks are automatically released by the database after 30 seconds',
    ],
    correctAnswer: 1,
    explanation: 'DDIA warns that 2PC participants that voted "yes" in the prepare phase cannot unilaterally abort or commit — they must wait for the coordinator\'s decision. The locks they hold block other transactions. If the coordinator\'s recovery takes hours, those rows are locked for hours. This is why 2PC is often avoided in microservices architectures.',
    tags: ['2pc', 'coordinator-failure', 'lock-blocking', 'microservices', 'applied'],
  },
  {
    id: 'ddia-consistency-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Linearizability',
    question: 'Your system uses multi-leader replication across two datacenters. A developer asks whether they can get linearizable reads. Based on DDIA, what is the answer?',
    options: [
      'Yes — multi-leader replication is linearizable if both leaders are synchronized',
      'No — multi-leader replication is inherently not linearizable because concurrent writes to different leaders can be applied in different orders, and there is no single source of truth for real-time ordering',
      'Only if the replication lag is under 1ms',
      'Yes, if you use synchronous replication between leaders',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explicitly states that multi-leader replication does not provide linearizability. Both leaders can independently accept writes, and asynchronous replication means there is no point in time where all nodes agree on the current state. Even with synchronous replication, the independent acceptance of writes by multiple leaders prevents linearizability.',
    tags: ['linearizability', 'multi-leader', 'not-linearizable', 'applied'],
  },
  {
    id: 'ddia-consistency-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Raft',
    question: 'Your 5-node Raft cluster has lost 3 nodes. Can the remaining 2 nodes elect a new leader and continue serving requests?',
    options: [
      'Yes — 2 nodes are sufficient to form a cluster',
      'No — a majority of 5 is 3, so 2 nodes cannot form a quorum, and the cluster is unavailable for writes until at least one more node recovers',
      'Yes, but only for read operations',
      'The cluster automatically reconfigures to a 2-node cluster',
    ],
    correctAnswer: 1,
    explanation: 'Raft requires a majority (3 out of 5) for both leader election and committing entries. With only 2 nodes remaining, no majority can be formed, so the cluster cannot elect a leader or commit new entries. It remains unavailable until at least one more node recovers. This is the availability trade-off of consensus.',
    tags: ['raft', 'majority', 'quorum', 'availability', 'applied'],
  },
  {
    id: 'ddia-consistency-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Consistency & Consensus',
    subcategory: 'Causal Consistency',
    question: 'Your event processing system needs to ensure that a "user created" event is always processed before a "user updated profile" event. Based on DDIA, what ordering guarantee do you need?',
    options: [
      'Total order of all events in the system',
      'Causal ordering — ensure events with a causal relationship (create before update) are delivered in order, while allowing concurrent unrelated events to be processed in any order',
      'FIFO ordering per partition',
      'No ordering is needed since events are idempotent',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that causal ordering is often sufficient and more scalable than total ordering. "User created" causally precedes "user updated profile," so causal consistency ensures correct ordering. Unrelated events (different users) can be processed concurrently, improving throughput.',
    tags: ['causal-consistency', 'event-processing', 'ordering', 'applied'],
  },
];

export default questions;
