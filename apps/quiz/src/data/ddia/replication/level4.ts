import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-replication-l4-001',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Conflict Resolution',
    question: 'CRDTs (Conflict-free Replicated Data Types) guarantee convergence without coordination. What is the key mathematical property that enables this?',
    options: ['Commutativity only', 'All operations must be idempotent', 'Operations must be totally ordered by timestamps', 'The merge function must form a semilattice: it must be commutative, associative, and idempotent'],
    correctAnswer: 3,
    explanation: 'State-based CRDTs require a merge function that forms a join-semilattice: commutative (order of merging does not matter), associative (grouping of merges does not matter), and idempotent (merging the same state twice has no additional effect). This ensures all replicas converge regardless of message ordering or duplication.',
    tags: ['replication', 'crdt', 'semilattice', 'convergence'],
  },
  {
    id: 'ddia-replication-l4-002',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Conflict Resolution',
    question: 'A G-Counter CRDT uses a map of {nodeId: count}. Why can\'t a simple integer counter work as a CRDT?',
    options: ['A simple increment operation on a shared integer is not idempotent; replaying a message would double-count. The per-node map allows each node\'s increments to be tracked independently and merged by taking the max per node.', 'Integers cannot be serialized across the network', 'Integers are too small to represent distributed counts', 'CRDTs do not support numeric values'],
    correctAnswer: 0,
    explanation: 'With a simple integer, if node A increments and sends the new value to B, and B also incremented, merging the two values is ambiguous (add them? take the max?). A G-Counter tracks increments per node. The merge takes the max of each node\'s counter, making it commutative, associative, and idempotent.',
    tags: ['replication', 'crdt', 'g-counter', 'design'],
  },
  {
    id: 'ddia-replication-l4-003',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Quorums',
    question: 'In a system with n=5, w=3, r=3, a write succeeds on nodes {A, B, C} and a concurrent read contacts {C, D, E}. Node C has the new value, but D and E have the old value. What does the client see?',
    options: ['Always the new value since w+r > n', 'The read fails because D and E are stale', 'The new value, but only because node C is in the overlap', 'Potentially the old value if the client does not compare versions from all contacted nodes'],
    correctAnswer: 2,
    explanation: 'The quorum condition guarantees at least one overlap node (C in this case). The client reads from C, D, and E, gets the new value from C and old from D and E. By comparing version numbers across all responses, the client can identify and return the newest value. The quorum works because of this overlap.',
    tags: ['replication', 'quorum', 'overlap', 'deep-dive'],
  },
  {
    id: 'ddia-replication-l4-004',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Single-Leader',
    question: 'Statement-based replication sends SQL statements to followers. Why does this approach break with nondeterministic functions like NOW() or RAND()?',
    options: ['The replication log cannot store SQL strings', 'The SQL syntax may differ between leader and follower', 'Nondeterministic functions are not valid SQL', 'Each node evaluates NOW() or RAND() independently, producing different values on leader and follower, causing data divergence'],
    correctAnswer: 3,
    explanation: 'If the leader executes INSERT INTO t VALUES(NOW()) and sends this statement to a follower, the follower evaluates NOW() at a different time, getting a different value. Similarly, RAND() produces different results. This causes leader and follower data to diverge. WAL-based or row-based replication avoids this by shipping the actual data values.',
    tags: ['replication', 'statement-based', 'nondeterminism'],
  },
  {
    id: 'ddia-replication-l4-005',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Single-Leader',
    question: 'WAL (Write-Ahead Log) shipping sends the storage engine\'s physical log to followers. What is the major drawback compared to logical (row-based) replication?',
    options: ['WAL shipping does not support transactions', 'Logical replication requires less network bandwidth', 'The WAL describes data at the storage engine level (disk blocks, byte offsets), making it coupled to the storage engine version and preventing zero-downtime upgrades where leader and followers run different versions', 'WAL shipping is slower due to larger log sizes'],
    correctAnswer: 2,
    explanation: 'WAL shipping is tightly coupled to the storage engine internals. If the leader and follower run different versions of the database software (e.g., during a rolling upgrade), the WAL format may be incompatible. Logical (row-based) replication operates at the row level, making it version-independent and enabling zero-downtime upgrades.',
    tags: ['replication', 'wal', 'logical-replication', 'upgrades'],
  },
  {
    id: 'ddia-replication-l4-006',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Multi-Leader',
    question: 'In an all-to-all multi-leader topology with three leaders (A, B, C), leader A sends an INSERT to B and C, then sends an UPDATE (depending on that INSERT) to B and C. Due to network delays, C receives the UPDATE before the INSERT. What technique from DDIA can fix this?',
    options: ['Switch to circular topology', 'Use version vectors to track causal dependencies and buffer writes until their causal dependencies are satisfied', 'Retry the UPDATE until the INSERT arrives', 'Use wall-clock timestamps to order the operations'],
    correctAnswer: 1,
    explanation: 'Version vectors (or similar causal tracking mechanisms) allow each node to know which prior writes a given write depends on. If a node receives a write whose dependencies have not yet arrived, it can buffer that write until the dependencies are satisfied, preserving causal ordering.',
    tags: ['replication', 'causality', 'version-vector', 'topology'],
  },
  {
    id: 'ddia-replication-l4-007',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leaderless',
    question: 'Amazon\'s Dynamo paper introduced the concept of "vector clocks" for conflict detection. DDIA notes that Riak uses "dotted version vectors" instead. What problem do dotted version vectors solve that plain vector clocks do not?',
    options: ['They are more compact and never grow unboundedly', 'Plain vector clocks can create false concurrency (sibling explosion) when a client reads siblings and writes back a resolved value, because the client is assigned a new actor entry each time. Dotted version vectors associate updates with the server node, not the client, preventing this growth.', 'They provide total ordering instead of partial ordering', 'They support multi-datacenter replication'],
    correctAnswer: 1,
    explanation: 'With plain vector clocks using client IDs as actor entries, each client write creates a new entry, causing the vector to grow without bound ("sibling explosion"). Dotted version vectors use server node IDs as actors and attach a single "dot" to each update, keeping the vector bounded by the number of nodes.',
    tags: ['replication', 'dotted-version-vector', 'vector-clock', 'dynamo'],
  },
  {
    id: 'ddia-replication-l4-008',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Quorums',
    question: 'A Dynamo-style system uses n=3, w=2, r=2. Two clients concurrently write different values to the same key. Client X writes "A" to nodes {1, 2}. Client Y writes "B" to nodes {2, 3}. Node 2 receives both writes. What is the state after both writes complete?',
    options: ['The system uses consensus to pick one value', 'The system rejects one write due to conflict', 'All nodes have the same value', 'Node 1 has "A", node 3 has "B", and node 2 has either "A" or "B" (whichever it processed last), creating a state where a quorum read may return either value'],
    correctAnswer: 3,
    explanation: 'Without consensus, concurrent writes to overlapping quorums create ambiguity. Node 2\'s final state depends on arrival order. A subsequent quorum read of {1, 2} or {2, 3} may return different results depending on which value node 2 holds. This demonstrates why Dynamo-style quorums provide only eventual consistency, not strong consistency.',
    tags: ['replication', 'quorum', 'concurrent-writes', 'deep-dive'],
  },
  {
    id: 'ddia-replication-l4-009',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Conflict Resolution',
    question: 'An OR-Set (Observed-Remove Set) CRDT solves the "add wins over concurrent remove" problem. How does it achieve this?',
    options: ['By preventing concurrent adds and removes through locking', 'By making removes permanent and irreversible', 'Each added element is tagged with a unique identifier. A remove only removes specific tags it has seen, so a concurrent add with a new tag survives the remove.', 'By using timestamps to order adds and removes'],
    correctAnswer: 2,
    explanation: 'In an OR-Set, each add operation tags the element with a unique identifier (e.g., a UUID). A remove operation removes only the specific tags it has observed. If another replica concurrently adds the same element with a new tag, that tag is not removed by the concurrent remove, so the add "wins." This provides intuitive semantics without coordination.',
    tags: ['replication', 'crdt', 'or-set', 'design'],
  },
  {
    id: 'ddia-replication-l4-010',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Single-Leader',
    question: 'Logical (row-based) replication logs record changes at the row level (e.g., "row with PK=5 was inserted with values..."). Why does this make it easier to build change data capture (CDC) pipelines compared to WAL shipping?',
    options: ['WAL entries cannot represent row-level changes', 'Logical logs are decoupled from the storage engine internals, can be parsed by external systems, and remain compatible across database versions, making them ideal for feeding data to warehouses, search indexes, and caches', 'Logical logs are smaller than WAL entries', 'Logical logs are encrypted'],
    correctAnswer: 1,
    explanation: 'Logical replication logs describe changes in a format that is meaningful to external consumers (row inserts, updates, deletes with actual values). They are not tied to a specific storage engine or database version, so external systems like data warehouses, search indexes, and cache invalidation systems can reliably consume them.',
    tags: ['replication', 'logical-replication', 'cdc', 'architecture'],
  },
  {
    id: 'ddia-replication-l4-011',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leaderless',
    question: 'In a leaderless system, a node that was down comes back and serves a read before read repair or anti-entropy can update it. With n=3, w=2, r=1, why does the client get a stale value, and why might this configuration still be chosen?',
    options: ['The anti-entropy process prevents this scenario', 'This configuration is never used in practice', 'This configuration guarantees linearizability', 'w + r = 3, which equals n, so there is no quorum overlap. This is chosen when low read latency and high read availability are prioritized over consistency, accepting that some reads may be stale.'],
    correctAnswer: 3,
    explanation: 'With w=2 and r=1, w+r=3=n, so there is no guaranteed overlap between write and read quorums. A read may go to the one node that missed the write. This is accepted in systems where read latency and availability are more important than reading the latest value (e.g., serving cached product recommendations).',
    tags: ['replication', 'quorum', 'stale-reads', 'trade-offs'],
  },
  {
    id: 'ddia-replication-l4-012',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Multi-Leader',
    question: 'Why does DDIA describe collaborative editing (like Google Docs) as a database replication problem rather than a simple application-level problem?',
    options: ['Because Google Docs uses MySQL under the hood', 'Because collaborative editing requires exactly-once delivery', 'Because each user\'s device holds a local replica of the document and accepts writes (edits) independently, creating the same concurrent write and conflict resolution challenges as multi-leader database replication', 'Because it requires SQL databases'],
    correctAnswer: 2,
    explanation: 'Each user editing a shared document has a local copy (replica) that immediately reflects their changes. These changes must be replicated to all other users\' replicas, and concurrent edits must be resolved. This is structurally identical to multi-leader replication where each client is a "leader" that accepts writes.',
    tags: ['replication', 'collaborative-editing', 'multi-leader', 'analogy'],
  },
  {
    id: 'ddia-replication-l4-013',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Conflict Resolution',
    question: 'The "happens-before" relationship defines a partial order on events. Given three events where A happens-before B, and C is concurrent with both A and B, what can we conclude?',
    options: ['There exists a total order: A, C, B', 'C is not causally related to either A or B; any ordering of C relative to A and B is valid', 'C must be discarded as it cannot be ordered', 'C must have happened between A and B in wall-clock time'],
    correctAnswer: 1,
    explanation: 'The happens-before relation is a partial order. If C is concurrent with both A and B, it means there is no causal relationship between C and either of them. The system can order C before A, between A and B, or after B, as long as A is before B. This is the fundamental insight behind detecting concurrency with version vectors.',
    tags: ['replication', 'happens-before', 'partial-order', 'causality'],
  },
  {
    id: 'ddia-replication-l4-014',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Single-Leader',
    question: 'When setting up a new follower in single-leader replication, why can\'t we simply copy the leader\'s data files while the leader is running?',
    options: ['The follower needs a different data format', 'The leader\'s data files are constantly being modified; copying them would yield an inconsistent snapshot with some parts reflecting earlier writes and some reflecting later writes', 'Copying files is slower than streaming the replication log', 'The file system prevents concurrent access'],
    correctAnswer: 1,
    explanation: 'Copying data files from a running database produces an inconsistent snapshot: some files may reflect writes that happened during the copy while others do not. Instead, a consistent snapshot must be taken (e.g., using the database\'s snapshot facility), and then the follower catches up by replaying the replication log from the snapshot\'s position.',
    tags: ['replication', 'snapshot', 'follower-setup', 'consistency'],
  },
  {
    id: 'ddia-replication-l4-015',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Quorums',
    question: 'DDIA mentions that even with strict quorums (w + r > n), linearizability is not guaranteed in Dynamo-style systems. What additional mechanism would be required to achieve linearizability?',
    options: ['A consensus protocol (like Paxos or Raft) to agree on the order of reads and writes', 'Using CRDTs for all data types', 'Anti-entropy running more frequently', 'Larger quorum sizes'],
    correctAnswer: 0,
    explanation: 'Dynamo-style quorums do not enforce a total order on operations. Concurrent writes can be processed in different orders on different nodes. Linearizability requires that all operations appear to execute atomically in a single total order. Achieving this requires a consensus protocol to agree on operation ordering.',
    tags: ['replication', 'quorum', 'linearizability', 'consensus'],
  },
  {
    id: 'ddia-replication-l4-016',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leaderless',
    question: 'In Cassandra, a read at consistency level QUORUM returns a value, but a subsequent read at QUORUM returns a different (older) value. Assuming no new writes occurred, how is this possible?',
    options: ['If a write was only partially completed (reached w nodes but some later failed and rolled back), or if a concurrent read repair from another client changed the state, the quorum can return different results between reads', 'Cassandra does not support QUORUM reads', 'This is impossible with QUORUM reads', 'The client cached the first result incorrectly'],
    correctAnswer: 0,
    explanation: 'This can happen due to several edge cases: a partially failed write that reached enough nodes for one quorum read but not another; read repair from a concurrent client updating some nodes between the two reads; or a node restart losing an un-flushed write. Dynamo-style quorums do not prevent all anomalies.',
    tags: ['replication', 'quorum', 'anomalies', 'cassandra'],
  },
  {
    id: 'ddia-replication-l4-017',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Conflict Resolution',
    question: 'A multi-leader system must ensure that all replicas converge to the same final state for conflicting writes. Beyond LWW, DDIA describes several approaches. Which of the following is NOT one of them?',
    options: ['Merge the values together (e.g., concatenate strings, union sets)', 'Assign each write a unique ID and pick the highest (a variation of LWW)', 'Record the conflict in a data structure and let application code resolve it on read', 'Use a two-phase commit protocol to prevent conflicts entirely'],
    correctAnswer: 3,
    explanation: 'Two-phase commit (2PC) is a consensus mechanism for distributed transactions, not a conflict resolution strategy for multi-leader replication. DDIA describes LWW (highest ID wins), merging values, and recording conflicts for application-level resolution as convergence strategies. 2PC would defeat the purpose of multi-leader replication by requiring synchronous coordination.',
    tags: ['replication', 'conflict-resolution', 'convergence', '2pc'],
  },
  {
    id: 'ddia-replication-l4-018',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Conflict Resolution',
    question: 'DDIA discusses how CRDTs (Conflict-free Replicated Data Types) address multi-leader conflicts. What is the key property that makes CRDTs work?',
    options: [
      'CRDTs use timestamps to order all operations globally',
      'CRDTs are data structures designed so that concurrent modifications can always be merged automatically and deterministically without conflicts, because their merge function is mathematically guaranteed to be commutative, associative, and idempotent',
      'CRDTs prevent concurrent writes entirely',
      'CRDTs require a central coordinator to resolve conflicts',
    ],
    correctAnswer: 1,
    explanation: 'DDIA mentions CRDTs as a family of data structures that can be concurrently modified by different nodes and always merged into a consistent state. The mathematical properties (commutativity, associativity, idempotency) of the merge function ensure that all replicas converge regardless of the order in which operations are applied.',
    tags: ['replication', 'crdt', 'conflict-resolution', 'convergence', 'expert'],
  },
  {
    id: 'ddia-replication-l4-019',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leaderless Replication',
    question: 'DDIA explains that Dynamo-style quorum replication provides eventual consistency but not linearizability. What is the fundamental reason?',
    options: [
      'Dynamo-style databases use LWW which is inherently inconsistent',
      'Even with w + r > n, concurrent operations and network delays mean a read can return a value that a concurrent write is in the process of overwriting — there is no single point of serialization for all operations',
      'Leaderless databases cannot process reads and writes simultaneously',
      'Quorum parameters are always configured incorrectly',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that without a single leader to serialize all writes, concurrent operations on different nodes can observe different orderings. A quorum read might contact nodes that have not yet received a concurrent write, returning stale data even though w + r > n. True linearizability requires a stronger coordination mechanism.',
    tags: ['replication', 'leaderless', 'linearizability', 'quorum-limitations', 'expert'],
  },
  {
    id: 'ddia-replication-l4-020',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leader-Based Replication',
    question: 'DDIA describes the dangers of "split brain" during failover. Why is having two nodes that both believe they are the leader so dangerous?',
    options: [
      'It wastes computing resources',
      'Both leaders accept writes independently, causing data divergence that may be impossible to automatically reconcile; this can lead to data loss, corruption, or violation of application invariants',
      'Split brain only affects read performance, not data integrity',
      'The database automatically detects and resolves split brain',
    ],
    correctAnswer: 1,
    explanation: 'DDIA identifies split brain as one of the most dangerous situations in distributed databases. With two leaders accepting writes, the data diverges with no clear way to reconcile. Some databases use a "shoot the other node in the head" (STONITH/fencing) mechanism to force-shutdown one of the competing leaders.',
    tags: ['replication', 'split-brain', 'failover', 'fencing', 'expert'],
  },
  {
    id: 'ddia-replication-l4-021',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Multi-Leader Replication',
    question: 'DDIA discusses the "write topology" problem in multi-leader replication (circular, star, all-to-all). What is the key weakness of circular and star topologies vs all-to-all?',
    options: [
      'Circular and star topologies use more network bandwidth',
      'In circular and star topologies, a single node failure can break the replication path, preventing writes from reaching all replicas; all-to-all is more fault-tolerant but can suffer from causality violations due to different propagation speeds',
      'All-to-all topologies are simpler to implement',
      'Circular topologies provide stronger consistency guarantees',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that circular and star topologies have a single point of failure: if one intermediate node fails, replication between some nodes is interrupted. All-to-all avoids this but introduces the risk of messages arriving out of causal order via different paths, potentially violating consistent prefix reads.',
    tags: ['replication', 'multi-leader', 'topology', 'fault-tolerance', 'expert'],
  },
  {
    id: 'ddia-replication-l4-022',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leaderless Replication',
    question: 'DDIA describes hinted handoff as a mechanism used with sloppy quorums. What is it and what is its limitation?',
    options: [
      'Hinted handoff is a conflict resolution strategy',
      'When a write is sent to a temporary node (not the key\'s home node), the temporary node stores the data with a "hint" of which home node it was intended for, and forwards it when the home node comes back; the limitation is that there is no guarantee of durability or read consistency from the home nodes until handoff completes',
      'Hinted handoff provides the same consistency as regular quorums',
      'Hinted handoff is only used during leader elections',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that hinted handoff maintains write availability during node failures by accepting writes on behalf of unavailable nodes. The temporary node holds the data and forwards it when the home node recovers. The limitation is that reads during this window may not find the data because the home nodes have not received it yet.',
    tags: ['replication', 'hinted-handoff', 'sloppy-quorum', 'availability', 'expert'],
  },
  {
    id: 'ddia-replication-l4-023',
    language: 'ddia',
    level: 'level4',
    category: 'Replication',
    subcategory: 'Leader-Based Replication',
    question: 'DDIA discusses WAL-based replication (used by PostgreSQL) vs logical (row-based) replication. What is the key architectural advantage of logical replication?',
    options: [
      'Logical replication is faster than WAL replication',
      'Logical replication decouples the replication format from the storage engine internals, allowing different storage engine versions (or even different databases) on leader and follower, enabling zero-downtime upgrades',
      'Logical replication provides stronger consistency guarantees',
      'WAL replication is deprecated in modern databases',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that WAL-based replication is tightly coupled to the storage engine — leader and follower must run the same version. Logical replication uses a higher-level format (insert row X, delete row Y) that is independent of the storage engine, allowing the follower to run different software versions. This enables rolling upgrades and cross-system replication.',
    tags: ['replication', 'wal', 'logical-replication', 'version-independence', 'expert'],
  },
];

export default questions;
