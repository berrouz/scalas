import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-encoding-l2-001',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'Compared to plain JSON, what advantage does MessagePack (a binary JSON encoding) offer?',
    options: [
      'It converts all data to fixed-width 64-bit values',
      'It adds schema enforcement that JSON lacks',
      'It supports streaming while JSON does not',
      'It is more compact but still does not provide a schema, so savings are modest',
    ],
    correctAnswer: 3,
    explanation: 'MessagePack is a binary encoding for JSON-like data. It is somewhat more compact than JSON because it encodes types and lengths in binary rather than text. However, since it still includes field name strings in the encoded data (no schema), the space savings compared to JSON are modest — far less than what schema-driven binary formats like Protocol Buffers achieve.',
    tags: ['messagepack', 'json', 'binary-encoding', 'compactness'],
  },
  {
    id: 'ddia-encoding-l2-002',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'Thrift has two binary encoding formats: BinaryProtocol and CompactProtocol. What is the key trade-off?',
    options: [
      'CompactProtocol uses compression; BinaryProtocol does not',
      'CompactProtocol packs field tags and type into fewer bytes, producing smaller output at slightly more encoding complexity',
      'BinaryProtocol supports more data types; CompactProtocol does not support floats',
      'BinaryProtocol is text-based; CompactProtocol is binary',
    ],
    correctAnswer: 1,
    explanation: 'Thrift BinaryProtocol uses a straightforward encoding where each field has a type byte, a 2-byte field tag, and then the value. CompactProtocol packs the field tag delta and type into a single byte when possible and uses varint encoding for integers, resulting in smaller output at the cost of slightly more complex encoding/decoding logic.',
    tags: ['thrift', 'binary-protocol', 'compact-protocol', 'trade-offs'],
  },
  {
    id: 'ddia-encoding-l2-003',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'Why can you freely add a new optional field with a new tag number in Protocol Buffers without breaking compatibility?',
    options: [
      'Because Protocol Buffers automatically migrates old data to include the new field',
      'Because Protocol Buffers re-encodes all existing data when the schema changes',
      'Because old code ignores unknown tags (forward compatible) and new code uses a default for missing fields (backward compatible)',
      'Because the new field is automatically removed during serialization',
    ],
    correctAnswer: 2,
    explanation: 'Adding an optional field with a new tag preserves both directions of compatibility: old code encountering the new tag simply skips it (forward compatibility), and new code reading old data without the field uses a default value (backward compatibility). The key requirement is that the new field must not be required.',
    tags: ['protobuf', 'schema-evolution', 'compatibility', 'optional-fields'],
  },
  {
    id: 'ddia-encoding-l2-004',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'How does Avro handle schema evolution without field tags?',
    options: [
      'It stores a version number and looks up the schema at runtime from a central server',
      'It uses positional matching only — the nth field in the writer maps to the nth field in the reader',
      'It compares the writer\'s and reader\'s schemas side by side, matching fields by name',
      'It requires all data to be re-encoded when the schema changes',
    ],
    correctAnswer: 2,
    explanation: 'Avro resolves differences between the writer\'s schema and reader\'s schema by matching fields by name. If a field exists in the writer\'s schema but not the reader\'s, it is ignored. If a field exists in the reader\'s schema but not the writer\'s, a default value is used. This name-based matching allows schema evolution without embedding field tags in the data.',
    tags: ['avro', 'schema-resolution', 'writer-schema', 'reader-schema'],
  },
  {
    id: 'ddia-encoding-l2-005',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'What is the difference between full compatibility and just backward or forward compatibility?',
    options: [
      'Full compatibility means data is replicated across all nodes',
      'Full compatibility means the schema never changes',
      'Full compatibility means the encoding format supports all data types',
      'Full compatibility means both backward and forward compatibility — old code can read new data and new code can read old data',
    ],
    correctAnswer: 3,
    explanation: 'Full compatibility is the combination of backward compatibility (new code can read old data) and forward compatibility (old code can read new data). This is the strongest guarantee and is needed in systems where different versions of code coexist simultaneously, such as during rolling upgrades.',
    tags: ['full-compatibility', 'backward-compatibility', 'forward-compatibility'],
  },
  {
    id: 'ddia-encoding-l2-006',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'Why is removing a required field in Protocol Buffers (proto2) a breaking change?',
    options: [
      'Because the field tag number can never be reused',
      'Because required fields use special compression that cannot be undone',
      'Because required fields are stored in a separate data segment',
      'Because old code that still expects the field will fail when reading data written by new code without it',
    ],
    correctAnswer: 3,
    explanation: 'If you remove a required field, old code reading data written by new code will not find the field and will raise a validation error. Required fields must always be present, so removing one breaks forward compatibility. This is one reason proto3 dropped the required keyword entirely.',
    tags: ['protobuf', 'required', 'breaking-changes', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l2-007',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'In Avro, what happens if a field is in the reader\'s schema but not in the writer\'s schema?',
    options: [
      'The entire record is skipped',
      'The field is filled with the default value defined in the reader\'s schema',
      'The decoder raises an error',
      'The field is set to null regardless of type',
    ],
    correctAnswer: 1,
    explanation: 'When the reader\'s schema contains a field that the writer\'s schema does not have, Avro fills it with the default value specified in the reader\'s schema. If no default is defined for that field, an error is raised. This is how Avro supports backward compatibility when new fields are added.',
    tags: ['avro', 'schema-resolution', 'default-values', 'backward-compatibility'],
  },
  {
    id: 'ddia-encoding-l2-008',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'What problem arises when JSON is used to represent binary data such as images or encrypted blobs?',
    options: [
      'Binary data cannot be included in JSON at all',
      'JSON parsers convert binary data to integers',
      'Binary data must be Base64-encoded, increasing size by about 33%',
      'JSON truncates binary data at 1 MB',
    ],
    correctAnswer: 2,
    explanation: 'JSON has no native binary string type. To include binary data in JSON, it must be encoded as a text string, typically using Base64. Base64 encoding represents every 3 bytes of binary data as 4 ASCII characters, increasing the data size by approximately 33%.',
    tags: ['json', 'binary-data', 'base64', 'overhead'],
  },
  {
    id: 'ddia-encoding-l2-009',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'Why must you never reuse a field tag number after removing a field in Protocol Buffers?',
    options: [
      'Protocol Buffers keeps a hash table indexed by tag and would get a collision',
      'Old data still contains the old tag, and reusing it would cause the old field\'s data to be misinterpreted as the new field',
      'The tag number is stored in a global registry and cannot be released',
      'The compiler rejects duplicate tag numbers across all schema versions',
    ],
    correctAnswer: 1,
    explanation: 'If a field tag number is reused for a new field after the old field is removed, any old data that still contains the original field will be decoded as if it were the new field, corrupting the data. The removed tag should be marked as reserved to prevent accidental reuse.',
    tags: ['protobuf', 'field-tags', 'reserved', 'data-corruption'],
  },
  {
    id: 'ddia-encoding-l2-010',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'What is the safest way to change the data type of a field in Protocol Buffers?',
    options: [
      'Use a cast annotation in the .proto file',
      'Modify the type in the schema and re-deploy all services simultaneously',
      'Add a new field with a new tag number and the desired type, deprecate the old field',
      'Change the type and rely on Protocol Buffers auto-conversion',
    ],
    correctAnswer: 2,
    explanation: 'Changing the type of an existing field risks data loss or misinterpretation (e.g., changing int32 to int64 may truncate in old code). The safest approach is to add a new field with a new tag number and the new type, then deprecate the old field. This preserves both forward and backward compatibility.',
    tags: ['protobuf', 'schema-evolution', 'type-changes', 'deprecation'],
  },
  {
    id: 'ddia-encoding-l2-011',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'How does an Avro file (object container file) store the writer\'s schema?',
    options: [
      'In a schema registry that the reader queries by file hash',
      'In a separate sidecar file that must always accompany the data file',
      'In the file header, so the reader always has access to the schema that was used to write the data',
      'It does not store the schema — the reader must independently know it',
    ],
    correctAnswer: 2,
    explanation: 'An Avro object container file embeds the writer\'s schema in its file header. This means anyone reading the file always has the schema used to write it, which is necessary for Avro\'s schema resolution process. For RPC or database use cases, other mechanisms like schema registries are used.',
    tags: ['avro', 'container-file', 'writer-schema', 'file-header'],
  },
  {
    id: 'ddia-encoding-l2-012',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'In a request/response RPC system, which direction of compatibility does the server need when the client is updated first?',
    options: [
      'The client is never updated before the server',
      'The server needs backward compatibility to read requests from the new client',
      'The server does not need any compatibility — it can reject unknown requests',
      'The server needs forward compatibility to read requests from the new client',
    ],
    correctAnswer: 3,
    explanation: 'If the client is updated first and sends requests using a newer schema, the server (still running old code) must be able to read data written by newer code. This is forward compatibility. The server needs to ignore or tolerate unknown fields in requests from the newer client.',
    tags: ['rpc', 'forward-compatibility', 'client-server', 'deployment'],
  },
  {
    id: 'ddia-encoding-l2-013',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'What advantage does a schema-based binary format have over a self-describing format like JSON for documentation?',
    options: [
      'Binary formats auto-generate API documentation websites',
      'Binary schemas include comments that are embedded in the encoded data',
      'The schema serves as documentation that is guaranteed to be up-to-date because it is required for encoding/decoding',
      'JSON cannot be documented at all',
    ],
    correctAnswer: 2,
    explanation: 'With schema-based formats like Protocol Buffers, Thrift, and Avro, the schema is a machine-readable description of the data structure that must be kept up-to-date because it is needed to encode and decode data. This makes it a form of documentation that cannot fall out of sync with reality, unlike manually maintained API docs.',
    tags: ['schema', 'documentation', 'binary-encoding', 'json'],
  },
  {
    id: 'ddia-encoding-l2-014',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'Why does Avro have union types, and how are they typically used for evolution?',
    options: [
      'Union types like ["null", "string"] allow a field to be nullable, which is needed when adding new fields that may not exist in old data',
      'Union types let Avro support multiple programming languages simultaneously',
      'Union types combine multiple Avro files into one',
      'Union types replace the need for schema registries',
    ],
    correctAnswer: 0,
    explanation: 'Avro union types allow a field to accept values of multiple types, most commonly ["null", "SomeType"]. This is important for schema evolution because when you add a new field, old data won\'t have it, so the field needs either a default value or the ability to be null. A union with null provides this flexibility.',
    tags: ['avro', 'union-types', 'nullable', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l2-015',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'What is the challenge of schema evolution in databases compared to RPC or messaging?',
    options: [
      'Databases always require downtime for schema changes',
      'Databases may store data written by many different schema versions over a long time, so all versions must remain readable',
      'Databases do not support binary encoding',
      'Database schemas cannot be versioned',
    ],
    correctAnswer: 1,
    explanation: 'In a database, data written years ago with an old schema may sit alongside data written yesterday with the latest schema. The application must be able to read all of it. This is unlike RPC where you typically only need to handle the current and previous versions. Databases effectively require backward compatibility across many schema versions.',
    tags: ['database', 'schema-evolution', 'long-lived-data', 'backward-compatibility'],
  },
  {
    id: 'ddia-encoding-l2-016',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'How does Protocol Buffers encode a string or bytes field differently from an integer field?',
    options: [
      'Strings are encoded with a length prefix followed by the raw bytes; integers use varint encoding',
      'Strings are null-terminated; integers use big-endian format',
      'Strings use fixed 256-byte blocks; integers use 8 bytes',
      'Strings and integers are both encoded as varints',
    ],
    correctAnswer: 0,
    explanation: 'In Protocol Buffers, string and bytes fields use a length-delimited encoding: a varint indicating the byte length, followed by that many bytes of data. Integer fields use varint encoding where smaller values use fewer bytes. This distinction is encoded in the wire type, which is part of the field tag.',
    tags: ['protobuf', 'wire-types', 'string-encoding', 'varint'],
  },
  {
    id: 'ddia-encoding-l2-017',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'Which data flow scenario requires only backward compatibility (not forward compatibility)?',
    options: [
      'A rolling upgrade of a distributed service',
      'A message queue where producers and consumers upgrade independently',
      'All data flow scenarios require both forward and backward compatibility',
      'A single-writer database where only the application writing data is ever upgraded, and no old code reads new data',
    ],
    correctAnswer: 3,
    explanation: 'If only one application writes to the database and it is upgraded atomically (not a rolling upgrade), then only backward compatibility is needed: the new code must read old data. Forward compatibility is not needed because old code will never encounter data written by new code. In practice, most real-world scenarios require both.',
    tags: ['backward-compatibility', 'data-flow', 'single-writer', 'database'],
  },
  {
    id: 'ddia-encoding-l2-018',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'DDIA describes a "data outlives code" problem in databases. What does this mean?',
    options: [
      'Database schemas are harder to change than application code',
      'Data written with an old schema version may remain in the database for years, long after the code that wrote it has been replaced',
      'Databases are more reliable than application code',
      'Data cannot be deleted from databases',
    ],
    correctAnswer: 1,
    explanation: 'DDIA emphasizes that data in a database can be very long-lived. A row written 5 years ago with an old schema must still be readable by today\'s code. This is unlike service calls where you only interact with the current version. Schema evolution in databases must handle this long time span.',
    tags: ['database', 'data-outlives-code', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l2-019',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'What is the "lost field" problem when updating a database record through application code?',
    options: [
      'Database fields are accidentally deleted during migration',
      'When an application reads a record (decoding), modifies a field, and writes it back (encoding), any fields that the application\'s schema does not know about may be silently dropped',
      'Fields lose their data type during encoding',
      'Network failures cause fields to be lost in transit',
    ],
    correctAnswer: 1,
    explanation: 'DDIA warns about this subtle issue: if application code reads a record, the decoder fills in the known fields but drops unknown ones. When the application writes the record back, those unknown fields are lost. This is a problem during rolling upgrades where old code may not know about new fields.',
    tags: ['database', 'schema-evolution', 'lost-fields', 'rolling-upgrades'],
  },
  {
    id: 'ddia-encoding-l2-020',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'What problems does DDIA identify with the RPC model\'s attempt to make remote calls look like local function calls?',
    options: [
      'RPC is slower than REST in all scenarios',
      'Network requests are fundamentally different from local calls: they can fail, timeout, have unpredictable latency, and require different error handling strategies',
      'RPC cannot use binary encoding',
      'RPC does not support schema evolution',
    ],
    correctAnswer: 1,
    explanation: 'DDIA argues that trying to make remote calls look identical to local calls is flawed because network requests can fail due to network problems, may be slow due to congestion, may time out with ambiguous results (was it processed or not?), and require serialization of parameters. These fundamental differences should not be hidden.',
    tags: ['rpc', 'network-fallacies', 'local-vs-remote'],
  },
  {
    id: 'ddia-encoding-l2-021',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'What is the actor model for message passing?',
    options: [
      'A design pattern for user interfaces',
      'A programming model where each actor is an entity with local state that communicates with other actors by sending and receiving asynchronous messages',
      'A protocol for encoding messages as JSON',
      'A framework for building REST APIs',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes the actor model (used by Akka, Orleans, Erlang OTP) where logic is encapsulated in actors. Each actor has private state and communicates only by sending asynchronous messages to other actors. This naturally integrates with message encoding concerns since messages must be serialized.',
    tags: ['actor-model', 'async-messaging', 'akka', 'erlang'],
  },
  {
    id: 'ddia-encoding-l2-022',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'How does Avro handle schema evolution when field names are changed?',
    options: [
      'Field names can be freely renamed without any mechanism',
      'Avro supports aliases — the writer\'s schema can include aliases that map old field names to new ones during schema resolution',
      'Field name changes are never allowed in Avro',
      'Avro uses field positions instead of names, so renaming has no effect',
    ],
    correctAnswer: 1,
    explanation: 'Avro supports aliases in the reader\'s schema that allow mapping from the writer\'s field names. If a field is renamed from "user_name" to "username", the reader\'s schema can include an alias for the old name so that data written with the old name is correctly decoded.',
    tags: ['avro', 'aliases', 'field-renaming', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l2-023',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'Why can you not remove a required field in Protocol Buffers (proto2) in a backward-compatible way?',
    options: [
      'Required fields use special encoding that cannot be removed',
      'If you remove a required field, new code (without the field) reading old data (with the field) will ignore it, but old code (expecting the field) reading new data (without it) will fail validation',
      'Required fields are protected by database constraints',
      'Required field removal is always safe in Protocol Buffers',
    ],
    correctAnswer: 1,
    explanation: 'Removing a required field breaks forward compatibility: old code reading data from new code will not find the required field and will raise a validation error. This is why proto3 removed the required keyword entirely — it made schema evolution too risky.',
    tags: ['protobuf', 'required-fields', 'schema-evolution', 'compatibility'],
  },
  {
    id: 'ddia-encoding-l2-024',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'What is the "rolling upgrade" deployment strategy and why does it require careful schema evolution?',
    options: [
      'Deploying all servers simultaneously so the upgrade is instantaneous',
      'Upgrading one server at a time so that both old and new code versions run simultaneously, requiring both forward and backward compatibility',
      'Rolling back to the previous version after every upgrade',
      'Upgrading the database schema without upgrading the application code',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes rolling upgrades as deploying new code to a few nodes at a time. During the rollout, some nodes run old code and some run new code. New code must read data written by old code (backward compatibility), and old code must read data written by new code (forward compatibility). This dual requirement makes schema evolution critical.',
    tags: ['rolling-upgrade', 'compatibility', 'deployment', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l2-025',
    language: 'ddia',
    level: 'level2',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'In Protocol Buffers, what is the difference between changing a field type (e.g., int32 to int64) and changing a field tag number?',
    options: [
      'Both changes are equally safe',
      'Changing a field type may be safe (with truncation risk); changing a tag number is equivalent to deleting the old field and adding a new one, breaking compatibility',
      'Changing tag numbers is safe; changing types is not',
      'Neither change is allowed in Protocol Buffers',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that changing a compatible field type (like int32 to int64) may work with potential truncation when old code reads the larger value. But changing a tag number means old code looking for the old tag will not find data, and new code looking for the new tag will not find old data — it is effectively deleting one field and adding another.',
    tags: ['protobuf', 'field-type', 'tag-number', 'schema-evolution'],
  },
];

export default questions;
