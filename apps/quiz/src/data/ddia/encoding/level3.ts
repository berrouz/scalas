import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-encoding-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'Your team adds a new required field "email" to a Protocol Buffers message that is stored in a database. Old records lack this field. What happens when new code reads old records?',
    options: [
      'The parser raises an error because the required field is missing',
      'The field is set to null',
      'The old records are automatically migrated to include the field',
      'The field is silently filled with an empty string',
    ],
    correctAnswer: 0,
    explanation: 'In proto2, a required field must be present in every encoded message. When the parser encounters an encoded message that lacks a required field, it raises an error (e.g., InvalidProtocolBufferException in Java, DecodeError in Python) rather than returning a default value. Old records in the database were written without the "email" field, so the parser will reject them. This is why adding required fields is a backward-incompatible change — the field should be added as optional with a default value instead. Note: proto3 removed required fields entirely for this reason.',
    tags: ['protobuf', 'required', 'backward-compatibility', 'database'],
  },
  {
    id: 'ddia-encoding-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'During a rolling deployment, the new version of a service adds an optional "priority" field (tag 7) to its request message. Some clients still run the old version. What should the service do to ensure correctness?',
    options: [
      'Store the raw request bytes without decoding until all clients are upgraded',
      'Force all clients to upgrade before deploying the service',
      'Provide a sensible default for the priority field when it is absent from requests sent by old clients',
      'Reject requests that do not include the priority field',
    ],
    correctAnswer: 2,
    explanation: 'During a rolling upgrade, old clients send requests without the new field. The service must handle missing optional fields gracefully by using a default value. This is backward compatibility in action — new code reads data from old code. Rejecting requests or forcing synchronized upgrades defeats the purpose of rolling deployments.',
    tags: ['rolling-upgrade', 'backward-compatibility', 'default-values', 'deployment'],
  },
  {
    id: 'ddia-encoding-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'A data pipeline produces Avro files daily. The schema evolves over months. How should the system make the writer\'s schema available so consumers can always decode old files?',
    options: [
      'Encode a schema version number in each record and hard-code the schemas in the consumer application',
      'Rely on consumers to maintain a copy of every historical schema version',
      'Embed the schema in every individual record for maximum portability',
      'Use Avro object container files which embed the writer\'s schema in the file header, or use a schema registry',
    ],
    correctAnswer: 3,
    explanation: 'Avro object container files include the writer\'s schema in the file header, so the file is self-contained. For other transport mechanisms (RPC, messaging), a schema registry stores schemas by ID so that each message only needs to include a small schema identifier. Both approaches ensure the writer\'s schema is always available for resolution against the reader\'s schema.',
    tags: ['avro', 'schema-registry', 'container-file', 'data-pipeline'],
  },
  {
    id: 'ddia-encoding-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'A developer renames a field from "userName" to "displayName" in a Protocol Buffers schema but keeps the same tag number. What is the impact?',
    options: [
      'No impact on binary compatibility — field names are not part of the binary encoding, only the tag number matters',
      'All stored data becomes unreadable and must be re-encoded',
      'The tag number must also change when a field is renamed',
      'Forward compatibility is broken because old code references the old name',
    ],
    correctAnswer: 0,
    explanation: 'In Protocol Buffers, field names are only used in generated code; the binary encoding uses only tag numbers. Renaming a field while keeping the same tag is a source-level change (code referencing the old name must be updated), but it has zero impact on the binary wire format or compatibility with stored data.',
    tags: ['protobuf', 'field-names', 'field-tags', 'binary-encoding'],
  },
  {
    id: 'ddia-encoding-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'Your API returns a JSON response containing a 64-bit order ID (e.g., 9007199254740999). A JavaScript client reports receiving incorrect IDs. What is the root cause and fix?',
    options: [
      'The HTTP transport is truncating the response body; increase max response size',
      'The server is encoding the ID incorrectly; switch to XML',
      'JavaScript parses JSON numbers as 64-bit doubles, which cannot represent integers above 2^53 precisely; send the ID as a string',
      'JavaScript does not support numbers larger than 32 bits; use two 32-bit fields',
    ],
    correctAnswer: 2,
    explanation: 'JavaScript\'s JSON.parse() represents numbers as IEEE 754 double-precision floats, which have only 53 bits of mantissa. Integers larger than 2^53 lose precision. The standard fix is to encode large IDs as JSON strings (e.g., "9007199254740999") so they are preserved exactly. Twitter\'s API famously does this with "id_str".',
    tags: ['json', 'javascript', 'precision', 'ieee754', 'large-numbers'],
  },
  {
    id: 'ddia-encoding-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'A microservice writes events to a message queue using Avro. A new version adds a field "region" with no default value. Consumers running old code start failing. Why?',
    options: [
      'The message queue rejects messages with unknown fields',
      'Avro requires a default value for new fields to maintain backward compatibility; without a default, old reader schemas cannot resolve the field',
      'Avro does not support adding fields after the initial schema is defined',
      'Old consumers cannot connect to the queue because the schema changed',
    ],
    correctAnswer: 1,
    explanation: 'This is a forward compatibility issue — old consumers (with the old reader schema) are trying to read new data (written with the new writer schema that includes "region"). In Avro schema resolution, when the writer\'s schema contains a field absent from the reader\'s schema, the reader normally ignores it. However, if the new field lacks a default value, schema resolution fails because Avro cannot determine how to handle the mismatch. A default value is required for new fields to maintain both forward compatibility (old code reading new data) and backward compatibility (new code reading old data).',
    tags: ['avro', 'schema-evolution', 'default-values', 'messaging'],
  },
  {
    id: 'ddia-encoding-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'A database-backed application reads a record, modifies one field, and writes it back. The application was recently upgraded and the schema has a new field. What subtle data loss can occur?',
    options: [
      'If the application does not preserve unknown fields, writing the record back may silently drop the new field from records written by even newer code',
      'The database index becomes corrupted',
      'The new field value is duplicated across all records',
      'The record is locked and cannot be written back',
    ],
    correctAnswer: 0,
    explanation: 'This is the "read-modify-write" problem with schema evolution. If code reads a record containing fields it does not know about (written by a newer version), modifies one known field, and writes it back, it may strip out the unknown fields. The application must preserve unknown fields during round-trips to avoid silent data loss.',
    tags: ['read-modify-write', 'unknown-fields', 'data-loss', 'forward-compatibility'],
  },
  {
    id: 'ddia-encoding-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'A Hadoop-based analytics system processes Avro files from hundreds of producers. Embedding the full schema in each message would be wasteful. What is the recommended approach?',
    options: [
      'Negotiate the schema at connection time and never change it',
      'Compress each message with gzip to offset the schema overhead',
      'Use JSON instead to avoid schema overhead',
      'Store schemas in a schema registry; each message includes only a small schema version ID',
    ],
    correctAnswer: 3,
    explanation: 'A schema registry (such as Confluent Schema Registry) stores Avro schemas by a unique ID. Each encoded message includes only the schema ID (a few bytes), and the consumer looks up the full writer\'s schema from the registry. This avoids embedding the full schema in every message while still enabling schema resolution.',
    tags: ['avro', 'schema-registry', 'hadoop', 'efficiency'],
  },
  {
    id: 'ddia-encoding-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'A team changes a Protocol Buffers field from int32 (tag 5) to int64 (tag 5) to support larger values. What risk does this pose?',
    options: [
      'The field tag must change because int32 and int64 use different wire types',
      'Old code reading the int64 value will truncate it to 32 bits, potentially losing data for large values',
      'No risk — Protocol Buffers automatically converts between integer sizes',
      'The encoding becomes incompatible and all old data is unreadable',
    ],
    correctAnswer: 1,
    explanation: 'int32 and int64 use the same varint wire type in Protocol Buffers, so old code can parse the bytes. However, old code that expects int32 will truncate the value to 32 bits. For small values this works fine, but large values that exceed the 32-bit range will be silently truncated. This makes the change safe only if values are guaranteed to stay within 32-bit range during the transition.',
    tags: ['protobuf', 'type-change', 'truncation', 'int32', 'int64'],
  },
  {
    id: 'ddia-encoding-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'An engineering team is choosing between JSON and Protocol Buffers for internal service-to-service communication. What argument most strongly favors Protocol Buffers?',
    options: [
      'JSON cannot represent nested structures',
      'Protocol Buffers enforce a schema at compile time, catch type errors early, and produce significantly smaller payloads',
      'Protocol Buffers support human-readable debugging out of the box',
      'JSON is not supported in most programming languages',
    ],
    correctAnswer: 1,
    explanation: 'For internal service-to-service communication, Protocol Buffers offer compile-time type checking via generated code, much smaller binary payloads (especially important at scale), and built-in schema evolution rules. JSON\'s advantages — human readability and ubiquity — matter more for external/public APIs. Protocol Buffers are not human-readable by default, which is a trade-off.',
    tags: ['protobuf', 'json', 'service-communication', 'trade-offs'],
  },
  {
    id: 'ddia-encoding-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'You need to rename a field in Avro from "timestamp" to "event_time". Unlike Protocol Buffers, Avro matches fields by name. How can you do this without breaking compatibility?',
    options: [
      'Field renames are impossible in Avro; you must create a new schema from scratch',
      'Add a new field "event_time" and keep "timestamp" forever, duplicating the data',
      'Use Avro\'s aliases feature to declare that "event_time" was previously known as "timestamp"',
      'Simply rename the field — Avro handles renames automatically',
    ],
    correctAnswer: 2,
    explanation: 'Avro supports aliases, which let you declare alternative names for a field. By adding "timestamp" as an alias for the "event_time" field, old data written with the "timestamp" name can be matched to the new "event_time" field during schema resolution. This is how Avro handles field renames without breaking compatibility.',
    tags: ['avro', 'aliases', 'field-rename', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'A Kafka-based system uses a schema registry with compatibility checks. The team sets compatibility mode to BACKWARD. Which schema change will be rejected by the registry?',
    options: [
      'Adding a new optional field with a default value',
      'Adding a new field alias',
      'Removing a field that has a default value',
      'Adding a new required field without a default value',
    ],
    correctAnswer: 3,
    explanation: 'BACKWARD compatibility means new code must be able to read old data. Adding a required field without a default means new code reading old data (which lacks this field) would fail. The schema registry rejects this change. Adding optional fields with defaults (new code uses the default for old data) and removing fields with defaults are both backward-compatible.',
    tags: ['schema-registry', 'backward-compatibility', 'kafka', 'validation'],
  },
  {
    id: 'ddia-encoding-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'In a MapReduce job, input files use three different Avro writer schemas from different time periods. The job uses a single reader schema. Will this work?',
    options: [
      'Only if the files are merged and re-encoded with the current schema first',
      'Yes — Avro resolves each file\'s writer schema against the reader schema independently, as long as they are all compatible',
      'Only if the schemas differ by at most one field',
      'No — all input files must use the same schema',
    ],
    correctAnswer: 1,
    explanation: 'Avro\'s schema resolution works per-file (or per-record). Each file embeds its own writer\'s schema, and the Avro reader resolves each writer\'s schema independently against the reader\'s schema. As long as each writer\'s schema is compatible with the reader\'s schema, this works seamlessly — making Avro well-suited for Hadoop/MapReduce workloads.',
    tags: ['avro', 'mapreduce', 'schema-resolution', 'multiple-schemas'],
  },
  {
    id: 'ddia-encoding-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'A gRPC service (using Protocol Buffers) needs to add a "repeated string tags" field. The developer picks tag number 3, which was previously used for a removed "string description" field. What happens?',
    options: [
      'The Protocol Buffers compiler prevents reuse of tag numbers',
      'The gRPC framework automatically migrates old data',
      'Nothing — tag numbers can be safely reused after deletion',
      'Old data that has a string at tag 3 will be decoded as a repeated string field, causing incorrect parsing or errors',
    ],
    correctAnswer: 3,
    explanation: 'Reusing a tag number means old data encoded with the original field type at that tag will be decoded as the new field type. A single "string description" value at tag 3 might be parsed as a repeated field with one element, or cause errors if wire types differ. The correct practice is to mark removed tag numbers as "reserved" to prevent accidental reuse.',
    tags: ['protobuf', 'grpc', 'tag-reuse', 'reserved', 'data-corruption'],
  },
  {
    id: 'ddia-encoding-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'A legacy system sends XML with a namespace-prefixed element <order:total>. A new JSON-based consumer needs to process this data. What encoding challenge arises?',
    options: [
      'JSON can embed XML directly as a string value, avoiding the problem',
      'JSON supports namespaces natively, so there is no challenge',
      'XML namespaces have no direct equivalent in JSON, so the conversion must establish a convention for representing namespaced elements',
      'XML namespaces are deprecated and can be safely ignored',
    ],
    correctAnswer: 2,
    explanation: 'JSON has no concept of namespaces. When converting XML to JSON, namespace-prefixed elements must be handled by some convention (e.g., flattening into a combined key like "order_total", or using a nested object). This is one of the reasons XML-to-JSON conversion is not always straightforward and can lose semantic information.',
    tags: ['xml', 'json', 'namespaces', 'conversion'],
  },
  {
    id: 'ddia-encoding-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'A team discovers that during a rolling deployment, some responses contain a new field "metadata" that the load balancer\'s health check parser cannot understand, causing false health check failures. What principle was violated?',
    options: [
      'The health checker should not parse response bodies at all',
      'Backward compatibility — the health checker is old code that cannot read new data',
      'The schema was not registered in the schema registry',
      'Forward compatibility — the health checker is old code that cannot handle unknown fields in new responses',
    ],
    correctAnswer: 3,
    explanation: 'The health checker is old code encountering data written by new code — this is a forward compatibility problem. The old parser should be tolerant of unknown fields (ignoring them). If the health check parser fails on unknown fields, it is not forward compatible, and adding any new field becomes a risky deployment operation.',
    tags: ['forward-compatibility', 'rolling-deployment', 'health-check', 'unknown-fields'],
  },
  {
    id: 'ddia-encoding-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'Your company stores Protocol Buffers-encoded events in a long-term archive (years of data). The schema has evolved through 15 versions. A new analytics tool needs to read all historical data. What property ensures this works?',
    options: [
      'All 15 schema versions must be forward and backward compatible with each other',
      'Forward compatibility of all 15 schema versions',
      'The analytics tool using the latest schema must be backward compatible with all 15 historical schema versions',
      'The archive must re-encode all data to the latest schema before analytics can read it',
    ],
    correctAnswer: 2,
    explanation: 'The analytics tool runs the latest code and reads old data — this requires backward compatibility. As long as each schema evolution step maintained backward compatibility (e.g., only added optional fields, never reused tags), the latest schema can read data from any historical version. This is the key benefit of careful backward-compatible evolution.',
    tags: ['backward-compatibility', 'long-term-storage', 'schema-evolution', 'analytics'],
  },
  {
    id: 'ddia-encoding-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'Your microservice architecture uses REST/JSON for inter-service communication. Message sizes average 5KB per request, with 100k requests/sec. A colleague suggests switching to gRPC/Protobuf. Based on DDIA\'s encoding discussion, what is the primary benefit?',
    options: [
      'gRPC supports more programming languages',
      'Protocol Buffers binary encoding is typically 3-10x more compact than JSON, reducing bandwidth and parsing overhead significantly at this scale',
      'gRPC eliminates the need for schema evolution',
      'JSON parsing is always faster than Protobuf parsing',
    ],
    correctAnswer: 1,
    explanation: 'At 100k req/sec with 5KB JSON messages, that is ~500MB/s of encoded data. DDIA shows that Protocol Buffers typically produce 3-10x smaller encodings than JSON. Switching could reduce bandwidth to 50-150MB/s and significantly reduce parsing CPU overhead, as binary parsing avoids string-to-number conversion.',
    tags: ['encoding', 'protobuf', 'json', 'performance', 'applied'],
  },
  {
    id: 'ddia-encoding-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'You use Avro to store events in a data lake. A new team wants to read this data but their reader schema adds a new field "priority" with default "normal". What happens when they read historical data that does not have this field?',
    options: [
      'The read fails because the field is missing',
      'Avro schema resolution fills in the default value "normal" for the missing field, so historical records seamlessly appear to have priority = "normal"',
      'The field is silently dropped from the reader output',
      'Avro requires all historical data to be re-encoded first',
    ],
    correctAnswer: 1,
    explanation: 'Avro schema resolution handles this gracefully: when a field exists in the reader\'s schema but not in the writer\'s schema, and the reader\'s schema provides a default value, that default is used. This enables the new team to read old data as if the field always existed with the default value.',
    tags: ['avro', 'schema-resolution', 'defaults', 'data-lake', 'applied'],
  },
  {
    id: 'ddia-encoding-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'During a rolling deployment, you discover that the new service version writes a field "amount" as int64, while the old version wrote it as int32. Some consumers are still running old code. What compatibility issue arises?',
    options: [
      'No issue — int32 and int64 are wire-compatible in all encodings',
      'Old consumers reading int64 values may silently truncate large values that exceed int32 range, causing data corruption without errors',
      'The old consumers will crash when encountering int64 data',
      'Only the producer needs to handle this difference',
    ],
    correctAnswer: 1,
    explanation: 'DDIA warns about this exact scenario: changing int32 to int64 is safe for new code reading old data (values always fit), but old code reading new 64-bit values may silently truncate to 32 bits if the value exceeds the int32 range. This is a subtle data corruption risk that does not produce errors.',
    tags: ['schema-evolution', 'type-change', 'truncation', 'rolling-deployment', 'applied'],
  },
  {
    id: 'ddia-encoding-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'Your system stores user records in a database. A new service adds a "preferences" field to the user record. An older service reads a user, modifies the "email" field, and writes it back. Based on DDIA\'s discussion, what happens to the preferences field?',
    options: [
      'The preferences field is preserved because databases handle this automatically',
      'The preferences field may be silently dropped if the older service\'s encoding does not preserve unknown fields during the read-modify-write cycle',
      'The preferences field is moved to a separate archive table',
      'The older service will crash when encountering the unknown field',
    ],
    correctAnswer: 1,
    explanation: 'DDIA specifically warns about this pattern: during a read-modify-write cycle, if the application code does not preserve unknown fields, they are lost when the record is written back. The old service\'s decoder does not know about "preferences," so it drops it, and the re-encoded record no longer contains it.',
    tags: ['database', 'read-modify-write', 'lost-fields', 'schema-evolution', 'applied'],
  },
  {
    id: 'ddia-encoding-l3-022',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'Your team debates using REST vs RPC for a new internal service. Based on DDIA\'s discussion, what are the key trade-offs?',
    options: [
      'REST is always superior for all use cases',
      'REST is better for public APIs (discoverable, well-understood conventions), while RPC with schema-based encoding (gRPC/Protobuf) excels for internal services (compact encoding, code generation, streaming support)',
      'RPC is legacy technology that should never be used',
      'The choice does not matter because they are functionally identical',
    ],
    correctAnswer: 1,
    explanation: 'DDIA discusses how REST has broad tooling support and is well-suited for public APIs, while RPC frameworks like gRPC provide benefits for internal services: compact binary encoding, automatic code generation, bidirectional streaming, and well-defined schema evolution rules. The choice depends on the context.',
    tags: ['rest', 'rpc', 'grpc', 'trade-offs', 'applied'],
  },
  {
    id: 'ddia-encoding-l3-023',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'Your Kafka pipeline has producers on schema v5 and consumers on a mix of v3, v4, and v5. A new v6 schema removes an optional field that v3 consumers still read. Using Kafka with a schema registry, how do you handle this?',
    options: [
      'Remove the field — consumers will just get null',
      'The schema registry will reject v6 if it breaks forward compatibility with v3 consumers — you must first upgrade all consumers past v3 before removing the field',
      'The schema registry does not check cross-version compatibility',
      'Kafka automatically translates between schema versions',
    ],
    correctAnswer: 1,
    explanation: 'A schema registry enforces compatibility rules. Removing a field that older consumers expect breaks forward compatibility: v3 consumers expect the field to be present. The registry rejects the v6 schema change, forcing the team to upgrade all consumers past v3 first. This is a key benefit of compatibility checking.',
    tags: ['kafka', 'schema-registry', 'forward-compatibility', 'consumer-upgrade', 'applied'],
  },
  {
    id: 'ddia-encoding-l3-024',
    language: 'ddia',
    level: 'level3',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'Your JavaScript frontend receives a JSON response containing a 64-bit integer ID (e.g., 9007199254740993). Users report that the ID occasionally appears incorrect. Based on DDIA\'s discussion, what is the root cause?',
    options: [
      'The server is sending incorrect IDs',
      'JavaScript parses JSON numbers as IEEE 754 doubles, which cannot precisely represent integers larger than 2^53, causing silent precision loss',
      'The network is corrupting the data',
      'JSON does not support numbers this large',
    ],
    correctAnswer: 1,
    explanation: 'DDIA specifically mentions this problem: JSON does not distinguish integers from floats, and JavaScript (like many languages) parses all numbers as IEEE 754 doubles. Doubles can only exactly represent integers up to 2^53. Larger integers lose precision silently. The common workaround is to encode large IDs as strings in JSON.',
    tags: ['json', 'ieee754', 'precision', 'javascript', 'applied'],
  },
];

export default questions;
