import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-encoding-l4-001',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'Avro does not tag fields in its binary encoding, relying entirely on schema resolution. What specific advantage does this give Avro for dynamically generated schemas compared to Protocol Buffers?',
    options: [
      'Avro schemas are smaller in memory',
      'Avro supports more data types than Protocol Buffers',
      'Avro schemas can be generated directly from a database schema (e.g., column names become field names) without manually assigning tag numbers',
      'Avro can encode data without any schema at all',
    ],
    correctAnswer: 2,
    explanation: 'Because Avro matches fields by name rather than by numeric tags, you can automatically generate an Avro schema from a relational database schema — each column becomes a field with its name. With Protocol Buffers or Thrift, you would need to manually assign a tag number to each field, making automatic schema generation impractical when the database schema changes frequently.',
    tags: ['avro', 'dynamic-schema', 'database', 'code-generation'],
  },
  {
    id: 'ddia-encoding-l4-002',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'In a system using Protocol Buffers, a developer changes a field from "optional int32 status = 4" to "optional enum Status status = 4". What are the precise compatibility implications?',
    options: [
      'This is fully compatible because enums are encoded as integers in Protocol Buffers',
      'This is completely incompatible — the wire type changes',
      'This is backward compatible (new code reads old int32 values as enum values) but old code reading new enum values will see them as integers, which may cause logic errors if the code does range checks',
      'Enums cannot reuse a tag previously used by an integer',
    ],
    correctAnswer: 2,
    explanation: 'In Protocol Buffers, enums are encoded as varints on the wire, just like int32. So the binary format is compatible. New code can read old int32 values and map them to enum values (as long as the values match). Old code reads the enum value as a plain integer. However, if old code has range checks (e.g., "if status > 5") the semantics may not match the new enum definition, which is a logic-level (not encoding-level) concern.',
    tags: ['protobuf', 'enum', 'int32', 'wire-type', 'semantic-compatibility'],
  },
  {
    id: 'ddia-encoding-l4-003',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'An Avro schema has a field defined as a union type ["null", "string", "int"]. What constraint does Avro impose on union evolution?',
    options: [
      'You may add types to a union for backward compatibility but the reader must have all the writer\'s union types; removing a type from the writer breaks backward compatibility for readers with the old schema',
      'Avro does not allow unions with more than two types',
      'Union types are resolved purely by position, so reordering types is a breaking change',
      'Union types cannot be changed once defined',
    ],
    correctAnswer: 0,
    explanation: 'Avro schema resolution for unions requires that each type in the writer\'s union can be matched to a type in the reader\'s union. Adding a type to the union is safe if readers are updated to include it (backward compatible if readers have the new type). Removing a type from the writer is backward compatible but not forward compatible. The matching is by type name, not position, so reordering is fine.',
    tags: ['avro', 'union-types', 'schema-evolution', 'compatibility-rules'],
  },
  {
    id: 'ddia-encoding-l4-004',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'Protocol Buffers uses ZigZag encoding for signed integers (sint32/sint64) but not for regular int32/int64. What problem does ZigZag solve?',
    options: [
      'ZigZag encoding compresses large positive integers more efficiently',
      'ZigZag maps signed integers to unsigned integers so that small-magnitude negative numbers produce small varints, whereas standard int32 encodes -1 as a 10-byte varint',
      'ZigZag allows mixing 32-bit and 64-bit integers in the same field',
      'ZigZag provides error detection for corrupted integer values',
    ],
    correctAnswer: 1,
    explanation: 'Standard varint encoding treats the number as unsigned, so -1 in two\'s complement is a very large unsigned number requiring 10 bytes. ZigZag encoding maps signed integers to unsigned: 0→0, -1→1, 1→2, -2→3, etc. This ensures that small-magnitude numbers (positive or negative) produce small varints. The sint32/sint64 types use this encoding for fields where negative values are common.',
    tags: ['protobuf', 'zigzag', 'varint', 'signed-integers', 'encoding-efficiency'],
  },
  {
    id: 'ddia-encoding-l4-005',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'In a Confluent Schema Registry, what is the difference between BACKWARD, FORWARD, and TRANSITIVE compatibility modes?',
    options: [
      'BACKWARD/FORWARD check only against the latest schema; TRANSITIVE variants check against all previously registered schemas',
      'TRANSITIVE is a compression mode; BACKWARD and FORWARD are compatibility modes',
      'TRANSITIVE means the schema applies to all topics; BACKWARD/FORWARD are per-topic',
      'BACKWARD means schemas can only add fields; FORWARD means they can only remove fields; TRANSITIVE allows both',
    ],
    correctAnswer: 0,
    explanation: 'In Confluent Schema Registry, BACKWARD and FORWARD compatibility check only against the immediately previous schema version. BACKWARD_TRANSITIVE and FORWARD_TRANSITIVE check against ALL previously registered schema versions. Transitive compatibility is more restrictive but guarantees that consumers at any schema version can read data from any other version, not just adjacent versions.',
    tags: ['schema-registry', 'confluent', 'transitive', 'compatibility-modes'],
  },
  {
    id: 'ddia-encoding-l4-006',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'BSON (Binary JSON, used by MongoDB) includes type information for each value. How does this compare to MessagePack and Protocol Buffers in terms of self-description vs. schema-dependence?',
    options: [
      'BSON and MessagePack are self-describing (include field names and types in the data); Protocol Buffers is schema-dependent (field names exist only in the schema)',
      'Protocol Buffers is more self-describing than BSON because it includes the full schema in each message',
      'BSON, MessagePack, and Protocol Buffers are all equally self-describing',
      'All three require an external schema to decode',
    ],
    correctAnswer: 0,
    explanation: 'BSON and MessagePack are self-describing binary formats: each encoded document includes field names and type tags, so it can be decoded without a separate schema (like JSON). Protocol Buffers encodes only tag numbers and wire types — you need the .proto schema to know field names and semantic types. Self-describing formats are larger but more flexible; schema-dependent formats are more compact.',
    tags: ['bson', 'messagepack', 'protobuf', 'self-describing', 'schema-dependent'],
  },
  {
    id: 'ddia-encoding-l4-007',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'A Protocol Buffers schema changes a field from "optional" to "repeated" (same tag and type). What is the precise encoding-level behavior?',
    options: [
      'Old code reading new data sees only the last value of the repeated field; new code reading old data sees a single-element list. Wire format is compatible for scalar types.',
      'Protocol Buffers rejects this change at compile time',
      'The repeated values are concatenated into a single value by old code',
      'This is a breaking change — the wire types are different',
    ],
    correctAnswer: 0,
    explanation: 'For scalar types in proto2, an optional field and each element of a repeated field use the same wire format. A repeated field simply has the same tag appearing multiple times. Old code that expects optional sees the last occurrence (standard protobuf behavior for duplicate tags). New code reading old data with a single value sees a list with one element. So this change is wire-format compatible, though semantics differ.',
    tags: ['protobuf', 'optional-to-repeated', 'wire-format', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l4-008',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'Avro supports schema evolution for map types (maps with string keys). What constraint applies when evolving the value type of a map?',
    options: [
      'The writer\'s map value type must be compatible with (promotable to) the reader\'s map value type according to Avro\'s type promotion rules',
      'Maps in Avro are always encoded as JSON regardless of the container format',
      'The new value type must be a union that includes the old value type',
      'Map value types cannot be changed once defined',
    ],
    correctAnswer: 0,
    explanation: 'Avro schema resolution for maps requires that the writer\'s value type is compatible with the reader\'s value type. Avro supports type promotion (e.g., int can be promoted to long, float to double). The value type can evolve as long as the promotion/resolution rules are satisfied. The map key type is always string in Avro and cannot be changed.',
    tags: ['avro', 'map-types', 'type-promotion', 'schema-resolution'],
  },
  {
    id: 'ddia-encoding-l4-009',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'In a microservices architecture using asynchronous messaging, Service A publishes events that are consumed by Services B, C, and D. Service A wants to remove a field. What compatibility considerations apply?',
    options: [
      'Removing fields from published events is never allowed',
      'Only backward compatibility matters since A is the only writer',
      'Service A must ensure forward compatibility: B, C, D (potentially running older code) must not break when the field disappears. All consumers must stop relying on the field before it is removed.',
      'The field can be removed immediately since message queues buffer the old messages',
    ],
    correctAnswer: 2,
    explanation: 'Removing a field from a published event means new data (from updated A) will lack that field. Consumers still running old code expect the field to be present. This is a forward compatibility issue: old consumer code must tolerate the missing field. In practice, A must coordinate with all consumers to ensure they no longer depend on the field before removing it. This is why event schema evolution in pub/sub is harder than in point-to-point RPC.',
    tags: ['forward-compatibility', 'microservices', 'event-driven', 'field-removal'],
  },
  {
    id: 'ddia-encoding-l4-010',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'Thrift supports multiple serialization protocols (Binary, Compact, JSON). What architectural advantage does this separation of transport protocol from serialization provide?',
    options: [
      'It allows Thrift to work without any network connectivity',
      'It allows different services to use different wire formats while sharing the same IDL and generated code',
      'It eliminates the need for schema evolution',
      'It makes Thrift faster than Protocol Buffers in all cases',
    ],
    correctAnswer: 1,
    explanation: 'By separating the serialization protocol from the IDL and code generation, Thrift allows different services to choose the most appropriate wire format for their use case (e.g., CompactProtocol for high-throughput internal services, JSON protocol for debugging or external APIs) while sharing the same schema definitions and generated code. This flexibility is a key architectural advantage.',
    tags: ['thrift', 'serialization-protocols', 'architecture', 'wire-format'],
  },
  {
    id: 'ddia-encoding-l4-011',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'Consider a scenario where data flows through the system as: API (JSON) → Service (Protobuf) → Database (Avro) → Analytics (Parquet). What challenge does this multi-format pipeline create for schema evolution?',
    options: [
      'Multi-format pipelines are prohibited by data governance standards',
      'Schema changes must be coordinated across all format boundaries, and type mappings between formats may have lossy conversions that constrain what evolutions are safe',
      'Only the first format (JSON) needs to support evolution; downstream formats are derived',
      'No challenge — each format handles evolution independently',
    ],
    correctAnswer: 1,
    explanation: 'When data crosses format boundaries, each translation must preserve semantics. A field added in Protobuf must also be added in the Avro schema and Parquet schema. Type differences between formats (e.g., JSON has no integer type, Protobuf has no union type, Avro has no map with non-string keys) may limit which evolutions are possible without data loss. Schema evolution becomes a cross-system coordination problem.',
    tags: ['multi-format', 'schema-evolution', 'type-mapping', 'data-pipeline'],
  },
  {
    id: 'ddia-encoding-l4-012',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'Avro\'s specification states that the order of fields in the binary encoding is determined by the writer\'s schema. If the reader\'s schema has a different field order, how is this handled?',
    options: [
      'Field order has no significance in Avro because fields are tagged like Protocol Buffers',
      'The Avro decoder reads fields in writer\'s schema order and rearranges them into the reader\'s schema order during resolution',
      'The reader re-sorts the binary data before parsing',
      'The reader rejects the data because field orders must match',
    ],
    correctAnswer: 1,
    explanation: 'Avro binary encoding serializes fields in the order defined by the writer\'s schema. During schema resolution, the decoder reads the binary data according to the writer\'s schema order (which it knows from the embedded or registered writer\'s schema), then maps each field to the corresponding field in the reader\'s schema by name. The reader\'s field order does not need to match the writer\'s.',
    tags: ['avro', 'field-order', 'schema-resolution', 'binary-encoding'],
  },
  {
    id: 'ddia-encoding-l4-013',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'A system uses Protocol Buffers with the "oneof" feature (a field that can be one of several types). What are the schema evolution rules for oneof?',
    options: [
      'Oneof fields cannot be evolved at all — they are frozen at definition time',
      'You can freely add and remove fields from a oneof without any constraints',
      'You can add new fields to a oneof, but moving an existing field into or out of a oneof is a breaking change because it changes the mutual exclusivity semantics',
      'Oneof fields are syntactic sugar with no impact on wire format or evolution',
    ],
    correctAnswer: 2,
    explanation: 'Adding new fields to a oneof is safe — old code treats them as unknown fields. However, moving an existing regular optional field into a oneof changes its semantics: in a oneof, setting one field clears others. Old code would not enforce this mutual exclusivity, leading to data inconsistencies. Similarly, moving a field out of a oneof is dangerous. The wire format is the same, but the semantic contract changes.',
    tags: ['protobuf', 'oneof', 'schema-evolution', 'mutual-exclusivity'],
  },
  {
    id: 'ddia-encoding-l4-014',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'JSON/XML/Binary',
    question: 'When comparing the encoding overhead for a record with 10 fields, roughly how do JSON, MessagePack, Thrift CompactProtocol, and Avro compare in encoded size?',
    options: [
      'All produce roughly the same size because the values dominate',
      'MessagePack is always the smallest because it was designed for minimal size',
      'JSON is largest (field names as strings + text values), MessagePack is slightly smaller (binary types but still field names), Thrift Compact is much smaller (numeric tags + varints), Avro is smallest (no field identifiers at all)',
      'Avro is largest because it includes the schema; JSON is smallest',
    ],
    correctAnswer: 2,
    explanation: 'JSON repeats full field name strings and uses text for values. MessagePack uses binary type tags but still includes field name strings. Thrift CompactProtocol replaces field names with small numeric tags and uses varint encoding. Avro encodes no field identifiers at all — just concatenated values in schema order. For a 10-field record, the differences can be dramatic: a record that is 81 bytes in JSON might be 66 in MessagePack, 34 in Thrift Compact, and 32 in Avro.',
    tags: ['encoding-size', 'json', 'messagepack', 'thrift', 'avro', 'comparison'],
  },
  {
    id: 'ddia-encoding-l4-015',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'In Protocol Buffers proto3, all fields are implicitly optional and have default values (0 for integers, empty string for strings). What subtle problem does this create for schema evolution compared to proto2?',
    options: [
      'Proto3 requires all fields to be present, unlike proto2',
      'Proto3 cannot distinguish between a field that was explicitly set to its default value and a field that was absent, making it ambiguous whether a field was intentionally set or simply missing',
      'Proto3 default values consume more storage space than proto2',
      'Proto3 does not support schema evolution at all',
    ],
    correctAnswer: 1,
    explanation: 'In proto3, there is no "has_field" check for scalar fields — a zero value is indistinguishable from an absent field. If you add a new integer field, you cannot tell whether old data has 0 because the sender explicitly set it to 0, or because the sender used an old schema that did not have the field. Proto2\'s explicit optional/required semantics and "has" accessors do not have this ambiguity. Proto3 added optional keyword back in later versions to address this.',
    tags: ['proto3', 'default-values', 'field-presence', 'ambiguity', 'schema-evolution'],
  },
  {
    id: 'ddia-encoding-l4-016',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Thrift/Protocol Buffers',
    question: 'Protocol Buffers defines exactly 6 wire types (varint, 64-bit, length-delimited, start group, end group, 32-bit). Why is the wire type important for forward compatibility?',
    options: [
      'Wire types are used only for debugging and have no runtime impact',
      'Wire types allow the parser to determine how many bytes an unknown field occupies, enabling it to skip unknown fields without understanding their schema',
      'Wire types enable encryption of unknown fields',
      'Wire types enforce type safety across schema versions',
    ],
    correctAnswer: 1,
    explanation: 'When old code encounters an unknown field tag, the wire type (encoded in the tag) tells the parser exactly how to determine the field\'s size: varint reads until no continuation bit, 64-bit reads 8 bytes, length-delimited reads a varint length then that many bytes, 32-bit reads 4 bytes. This allows the parser to correctly skip unknown fields without corrupting the remaining data. This is the foundation of forward compatibility.',
    tags: ['protobuf', 'wire-types', 'forward-compatibility', 'parser', 'field-skipping'],
  },
  {
    id: 'ddia-encoding-l4-017',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'A company migrating from JSON REST APIs to gRPC (Protocol Buffers) needs to maintain backward compatibility with existing JSON clients during the transition. What approach does the gRPC ecosystem provide?',
    options: [
      'gRPC cannot interoperate with JSON — all clients must be converted simultaneously',
      'Protocol Buffers has a built-in JSON encoding mode, but it is incompatible with standard JSON parsers',
      'gRPC-Gateway or similar transcoding proxies can translate between JSON/HTTP and gRPC/Protobuf, allowing JSON clients to work while services migrate to gRPC',
      'The clients must implement Protocol Buffers parsing alongside JSON parsing',
    ],
    correctAnswer: 2,
    explanation: 'The gRPC ecosystem provides tools like gRPC-Gateway, which is a reverse proxy that translates RESTful JSON HTTP requests into gRPC calls. Additionally, Protocol Buffers has a canonical JSON mapping (proto3 JSON), allowing the same message definitions to be serialized as either binary protobuf or JSON. These approaches enable a gradual migration where existing JSON clients continue to work while services adopt gRPC internally.',
    tags: ['grpc', 'json', 'transcoding', 'migration', 'grpc-gateway'],
  },
  {
    id: 'ddia-encoding-l4-018',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'Avro\'s schema resolution algorithm matches fields by name between writer and reader schemas. What happens when the writer has a field the reader does not have, and vice versa?',
    options: [
      'Both cases cause an error',
      'Fields in the writer but not the reader are silently ignored; fields in the reader but not the writer are filled with the reader\'s default value (error if no default)',
      'Fields are always preserved regardless of schema mismatch',
      'Avro requires writer and reader schemas to be identical',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes Avro\'s resolution rules precisely: writer-only fields are ignored (enables forward compatibility), reader-only fields use their default values (enables backward compatibility). If a reader-only field has no default, schema resolution fails. This bidirectional handling is what makes Avro\'s evolution model elegant.',
    tags: ['avro', 'schema-resolution', 'compatibility-rules', 'expert'],
  },
  {
    id: 'ddia-encoding-l4-019',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Schema Evolution',
    question: 'DDIA discusses how databases handle schema evolution differently from message-based systems. What is the key architectural difference that makes database schema evolution harder?',
    options: [
      'Databases use text encoding while messages use binary',
      'In message-based systems, producers and consumers can be upgraded independently with limited version overlap; databases contain data from ALL past schema versions simultaneously, requiring backward compatibility across potentially hundreds of versions over years',
      'Database schemas are more complex than message schemas',
      'Message-based systems do not need schema evolution',
    ],
    correctAnswer: 1,
    explanation: 'DDIA emphasizes that databases are a fundamentally harder case: they contain data spanning years of schema evolution, all of which must remain readable by the current code. Message systems typically only need compatibility between a few adjacent versions during rolling upgrades. This temporal dimension makes database schema evolution uniquely challenging.',
    tags: ['database', 'schema-evolution', 'temporal-dimension', 'expert'],
  },
  {
    id: 'ddia-encoding-l4-020',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'DDIA argues that "location transparency" in RPC frameworks (like Akka remote actors) is fundamentally flawed. What is the deeper computer science argument?',
    options: [
      'Location transparency was never implemented in any real system',
      'Network calls have fundamentally different failure modes (partial failure, unbounded latency, message duplication) that cannot be meaningfully abstracted away without leaking — making the "transparent" abstraction misleading and dangerous',
      'Location transparency adds too much overhead',
      'Location transparency is only problematic in languages without garbage collection',
    ],
    correctAnswer: 1,
    explanation: 'DDIA, echoing the "fallacies of distributed computing," argues that network communication differs from local function calls in ways that cannot be hidden: requests can be lost, may arrive out of order, responses can timeout ambiguously (processed but response lost), and latency varies by orders of magnitude. Pretending these differences do not exist leads to brittle systems.',
    tags: ['rpc', 'location-transparency', 'distributed-fallacies', 'expert'],
  },
  {
    id: 'ddia-encoding-l4-021',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Avro',
    question: 'Avro\'s design choice of not including field identifiers in the binary encoding has a profound implication for code generation. What is it?',
    options: [
      'Avro cannot generate code at all',
      'Avro-generated code must always have the schema available at both encode and decode time, making it unsuitable for dynamic environments',
      'Avro can be used with dynamically typed languages without code generation — since the schema is always available at read time, a generic Avro decoder can produce records as key-value maps without needing compiled classes',
      'Avro code generation is identical to Protocol Buffers code generation',
    ],
    correctAnswer: 2,
    explanation: 'DDIA highlights that Avro\'s design enables dynamic use: since the writer\'s schema is always available (in the file or registry), a generic decoder can parse any record into a key-value map without pre-generated classes. This makes Avro particularly well-suited for dynamically typed data processing pipelines (Hadoop, Spark) where schemas are discovered at runtime.',
    tags: ['avro', 'code-generation', 'dynamic-typing', 'generic-decoder', 'expert'],
  },
  {
    id: 'ddia-encoding-l4-022',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Compatibility',
    question: 'DDIA implies that maintaining both forward and backward compatibility simultaneously constrains the kinds of schema changes you can make. What operations are safe in this dual-compatibility requirement?',
    options: [
      'Any change is safe as long as you increment the version number',
      'Adding optional fields with defaults (safe in both directions), removing optional fields (old code ignores the missing field if it uses defaults). Renaming fields, changing types, adding required fields, and removing required fields are generally unsafe.',
      'Only adding fields is ever safe; removing is always unsafe',
      'Forward and backward compatibility cannot be maintained simultaneously',
    ],
    correctAnswer: 1,
    explanation: 'DDIA shows that the intersection of forward-compatible changes (old code tolerates new data) and backward-compatible changes (new code handles old data) is narrow: essentially adding optional fields with defaults and removing optional fields. This constraint is why schema design requires careful upfront planning in systems requiring both compatibility directions.',
    tags: ['compatibility', 'dual-compatibility', 'safe-changes', 'schema-design', 'expert'],
  },
  {
    id: 'ddia-encoding-l4-023',
    language: 'ddia',
    level: 'level4',
    category: 'Encoding & Evolution',
    subcategory: 'Data Flow',
    question: 'DDIA discusses how Avro was specifically designed for Hadoop\'s needs. What specific Hadoop requirements drove Avro\'s design decisions (no field tags, writer schema in file header)?',
    options: [
      'Hadoop needed encryption support',
      'Hadoop processes large files with millions of records and the schema is the same for all records in a file — including the schema once in the header and omitting field identifiers per record minimizes per-record overhead, and enables schema evolution across different files without coordination',
      'Hadoop required XML-based schemas',
      'Hadoop could not use binary encoding',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that Hadoop processes large files where all records share the same schema. Avro\'s design of embedding the schema once in the file header and using no per-field identifiers minimizes per-record overhead. Different files can use different schema versions, and the reader resolves them independently. This is ideal for batch processing of evolving data.',
    tags: ['avro', 'hadoop', 'design-rationale', 'batch-processing', 'expert'],
  },
];

export default questions;
