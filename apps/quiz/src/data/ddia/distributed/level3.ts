import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-distributed-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Unreliable Networks',
    question: 'A distributed key-value store receives no response from a replica after sending a write request. The coordinator retries and eventually gets a success. What risk does this introduce?',
    options: [
      'The write was applied twice, which is harmless for idempotent operations but dangerous otherwise',
      'The coordinator should never retry network requests',
      'The retry is guaranteed to have no side effects',
      'The replica will always deduplicate retried writes',
    ],
    correctAnswer: 0,
    explanation: 'If the original request was received and processed but the response was lost, a retry causes the write to be applied twice. For idempotent operations (like setting a value) this is safe, but for non-idempotent operations (like incrementing a counter) it leads to incorrect results. This is why idempotency or deduplication mechanisms are critical.',
    tags: ['retry', 'idempotency', 'at-least-once'],
  },
  {
    id: 'ddia-distributed-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Clocks',
    question: 'A last-write-wins (LWW) database uses wall-clock timestamps to resolve conflicts. Node A writes key X at its local time 100 and Node B writes key X at its local time 99. Node B\'s clock is actually 5ms ahead of true time. What happens?',
    options: [
      'The database detects the clock skew and resolves correctly',
      'Node B\'s write wins because it happened later in real time',
      'Node A\'s write wins because 100 > 99',
      'Node A\'s write wins, but this is incorrect — Node B\'s write actually happened later in real time',
    ],
    correctAnswer: 3,
    explanation: 'With LWW, the write with the higher timestamp wins. Node A\'s timestamp (100) is higher than Node B\'s (99), so Node A\'s write is kept. However, if Node B\'s clock is ahead by 5ms, Node B\'s write actually occurred at true time 94, meaning Node A\'s write at true time 100 was indeed later. But in general, clock skew can cause the wrong write to win, silently dropping valid updates.',
    tags: ['lww', 'clock-skew', 'conflict-resolution'],
  },
  {
    id: 'ddia-distributed-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Process Pauses',
    question: 'A leader node acquires a lease valid for 10 seconds. After 8 seconds, a GC pause starts and lasts 5 seconds. When the pause ends, the node checks its local clock and sees that only 8 seconds have passed (the clock did not advance during the pause). What problem occurs?',
    options: [
      'The node correctly detects the lease has expired',
      'The GC pause automatically extends the lease',
      'The lease duration is paused during GC',
      'The node believes the lease is still valid for 2 more seconds, but it actually expired 3 seconds ago',
    ],
    correctAnswer: 3,
    explanation: 'During a stop-the-world GC pause, application code does not execute, so the local clock check reflects the time before the pause. The node thinks only 8 seconds have passed and believes it has 2 seconds remaining. In reality, 13 seconds have elapsed (8 before + 5 during pause), and the lease expired 3 seconds ago. Another node may already hold the lease.',
    tags: ['lease', 'gc-pause', 'time-check'],
  },
  {
    id: 'ddia-distributed-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Failure Detection',
    question: 'A 3-node cluster uses majority-based failure detection. Node C becomes network-partitioned from A and B but can still serve local reads. What should node C do?',
    options: [
      'Increase its timeout and keep retrying connections to A and B indefinitely',
      'Promote itself to leader since it has the most recent data',
      'Continue serving reads and writes as normal',
      'Step down from any leadership role since it cannot communicate with a majority',
    ],
    correctAnswer: 3,
    explanation: 'In a majority-based system, a node that cannot communicate with a majority of nodes must step down from leadership because it cannot confirm that it is not on the minority side of a partition. If it continues serving writes, it risks split brain. Serving stale reads may be acceptable depending on consistency requirements.',
    tags: ['partition', 'majority', 'step-down'],
  },
  {
    id: 'ddia-distributed-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Unreliable Networks',
    question: 'A service sets request timeouts to the 99th percentile network latency (e.g., 500ms). During a network congestion event, latency spikes to the 99.9th percentile. What cascading effect might occur?',
    options: [
      'Many requests timeout, triggering retries that further increase congestion and cause more timeouts',
      'Timeouts have no effect on network congestion',
      'All requests succeed but with higher latency',
      'The network automatically reduces congestion',
    ],
    correctAnswer: 0,
    explanation: 'When latency spikes beyond the timeout, many requests fail and are retried. These retries add additional load to an already congested network, increasing latency further and causing even more timeouts. This positive feedback loop can lead to a cascading failure where the system becomes completely overwhelmed.',
    tags: ['timeout', 'cascading-failure', 'retry-storm'],
  },
  {
    id: 'ddia-distributed-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Clocks',
    question: 'An application uses Lamport timestamps to order events. Event A on Node 1 has timestamp 5, and event B on Node 2 has timestamp 3. Can you conclude that A happened after B?',
    options: [
      'Yes, because 5 > 3 means A happened after B',
      'No — Lamport timestamps guarantee that if A causally precedes B then timestamp(A) < timestamp(B), but the converse is not true',
      'No, because Lamport timestamps are always random',
      'Yes, Lamport timestamps provide perfect causal ordering in both directions',
    ],
    correctAnswer: 1,
    explanation: 'Lamport timestamps provide a one-way guarantee: if A causally happened before B, then timestamp(A) < timestamp(B). However, timestamp(A) > timestamp(B) does not imply A happened after B — the events could be concurrent. A higher timestamp only means it comes later in the total order, not that there is a causal relationship.',
    tags: ['lamport-timestamp', 'causality', 'total-order'],
  },
  {
    id: 'ddia-distributed-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Process Pauses',
    question: 'A distributed lock service issues lock grants with fencing tokens. Client A receives token 33, then pauses. Client B receives token 34 and writes to storage. Client A resumes and tries to write with token 33. What happens?',
    options: [
      'Both writes are accepted and merged automatically',
      'Client A\'s write overwrites Client B\'s because locks are first-come-first-served',
      'The storage server rejects Client A\'s write because 33 < 34, the highest token it has seen',
      'Client A\'s write succeeds because it acquired the lock first',
    ],
    correctAnswer: 2,
    explanation: 'The storage server tracks the highest fencing token it has processed (34 from Client B). When Client A attempts a write with token 33, the server rejects it because 33 < 34. This prevents the stale client from corrupting data, which is the entire purpose of fencing tokens.',
    tags: ['fencing-token', 'stale-client', 'safety'],
  },
  {
    id: 'ddia-distributed-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Byzantine Faults',
    question: 'A peer-to-peer cryptocurrency network has 100 nodes, 20 of which are controlled by a malicious actor. Can a BFT consensus protocol still ensure safety?',
    options: [
      'Yes, but only if the malicious nodes are all in the same data center',
      'No, because BFT only works with exactly 4 nodes',
      'Yes, because 20 < 100/3 (approximately 33), which is within the 3f+1 threshold',
      'No, any number of Byzantine nodes makes consensus impossible',
    ],
    correctAnswer: 2,
    explanation: 'A BFT protocol with 100 nodes can tolerate up to floor((100-1)/3) = 33 Byzantine faults. Since only 20 nodes are malicious, this is within the tolerance threshold. The 80 honest nodes form a supermajority sufficient for the protocol to reach correct consensus despite the malicious nodes.',
    tags: ['bft', 'quorum', 'byzantine-threshold'],
  },
  {
    id: 'ddia-distributed-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Failure Detection',
    question: 'A distributed database uses a phi accrual failure detector with a threshold of 8. Node X\'s heartbeat interval suddenly increases from 100ms to 2 seconds due to a network issue. How does the phi detector respond compared to a fixed 500ms timeout?',
    options: [
      'The phi detector immediately declares X dead while the fixed timeout waits',
      'The fixed timeout handles this better because it detects the failure faster',
      'Both detectors behave identically',
      'The phi detector adapts to the new interval and avoids a false positive; the fixed timeout falsely declares X dead',
    ],
    correctAnswer: 3,
    explanation: 'The phi accrual detector maintains a model of heartbeat arrival times and adjusts dynamically. It can incorporate the new, longer intervals into its model and produce a lower suspicion level. The fixed 500ms timeout would immediately declare X dead after 500ms, despite X still being alive. The phi detector trades off speed of detection for fewer false positives.',
    tags: ['phi-accrual', 'adaptive', 'false-positive'],
  },
  {
    id: 'ddia-distributed-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Unreliable Networks',
    question: 'A microservice architecture has service A calling service B with a 200ms timeout. Service B calls service C with a 200ms timeout. If C is slow, what problem emerges?',
    options: [
      'Timeouts are independent and do not affect each other',
      'A\'s timeout will be extended automatically',
      'A may timeout waiting for B, even though B is healthy — because B is blocked waiting for C',
      'Service B will cancel the call to C when A times out',
    ],
    correctAnswer: 2,
    explanation: 'If C is slow, B blocks waiting for C\'s response. Meanwhile, A\'s 200ms timeout for B may expire because B cannot respond until C does. This creates a cascading timeout problem where A falsely perceives B as unhealthy. This is why timeouts in service chains must be carefully coordinated, with inner timeouts shorter than outer ones.',
    tags: ['cascading-timeout', 'microservices', 'latency'],
  },
  {
    id: 'ddia-distributed-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Clocks',
    question: 'A distributed system uses vector clocks with 3 nodes (A, B, C). Node A has vector [3, 1, 0] and Node B has vector [2, 3, 1]. Are these events causally related or concurrent?',
    options: [
      'B\'s event happened before A\'s event',
      'The comparison is invalid with 3 nodes',
      'The events are concurrent because neither vector dominates the other',
      'A\'s event happened before B\'s event',
    ],
    correctAnswer: 2,
    explanation: 'To determine ordering, each component of one vector must be less than or equal to the corresponding component of the other. A[0]=3 > B[0]=2, but A[1]=1 < B[1]=3. Since neither vector is element-wise less than or equal to the other, the events are concurrent — neither causally preceded the other.',
    tags: ['vector-clock', 'concurrent', 'causal-ordering'],
  },
  {
    id: 'ddia-distributed-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Process Pauses',
    question: 'A real-time trading system runs on the JVM. To minimize the impact of GC pauses, which approach is most appropriate?',
    options: [
      'Use a concurrent collector and treat GC pauses as planned node outages, redistributing traffic before collection',
      'Increase heap size to delay GC indefinitely',
      'Disable garbage collection entirely',
      'Run the GC only during market closing hours',
    ],
    correctAnswer: 0,
    explanation: 'One practical approach is to use a low-latency GC collector and proactively redirect traffic away from a node before triggering a full GC cycle, treating it like a brief planned restart. Some systems also limit heap usage to reduce GC pressure. Disabling GC entirely would cause memory leaks, and increasing heap size only delays longer pauses.',
    tags: ['gc', 'real-time', 'latency-sensitive'],
  },
  {
    id: 'ddia-distributed-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Failure Detection',
    question: 'A 5-node cluster experiences a network partition: nodes {A, B, C} on one side and {D, E} on the other. Both sides try to elect a leader. Which side can successfully elect one?',
    options: [
      'Neither side can elect a leader during a partition',
      'Only the {A, B, C} side can, because it has a majority (3 out of 5)',
      'The side with node A always wins because it has the lowest ID',
      'Both sides can elect a leader',
    ],
    correctAnswer: 1,
    explanation: 'Majority-based leader election requires votes from more than half the nodes (at least 3 out of 5). The {A, B, C} partition has 3 nodes and can form a majority. The {D, E} partition has only 2 nodes and cannot. This ensures at most one leader exists at any time, preventing split brain.',
    tags: ['leader-election', 'majority', 'partition'],
  },
  {
    id: 'ddia-distributed-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Byzantine Faults',
    question: 'A web application receives data from user-submitted forms. Should this be modeled as a Byzantine fault tolerance problem?',
    options: [
      'No, because Byzantine faults only occur in hardware',
      'No — input validation and sanitization are sufficient; BFT addresses node-to-node trust, not user input',
      'Yes, user input is untrusted and requires full BFT consensus',
      'Yes, all external input requires Byzantine fault tolerance protocols',
    ],
    correctAnswer: 1,
    explanation: 'Malicious user input is a security concern handled by input validation, authentication, and authorization — not BFT protocols. Byzantine fault tolerance addresses the problem of nodes within a distributed system behaving arbitrarily. These are different threat models requiring different solutions.',
    tags: ['byzantine', 'input-validation', 'threat-model'],
  },
  {
    id: 'ddia-distributed-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Unreliable Networks',
    question: 'An application experiences occasional TCP connection resets between two datacenters. The operations team suspects a firewall is dropping idle connections. What is the appropriate mitigation?',
    options: [
      'Increase the TCP window size',
      'Implement application-level keepalive messages to prevent connections from appearing idle',
      'Disable the firewall entirely',
      'Switch from TCP to UDP',
    ],
    correctAnswer: 1,
    explanation: 'Many firewalls and NAT devices drop TCP connections that have been idle for a certain period. Application-level keepalive messages (distinct from TCP keepalives, which may be too infrequent) ensure regular traffic flows on the connection, preventing the firewall from classifying it as idle and dropping it.',
    tags: ['tcp', 'keepalive', 'firewall'],
  },
  {
    id: 'ddia-distributed-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Clocks',
    question: 'A system using Spanner-style commit wait needs to commit a transaction. TrueTime returns the interval [T, T+7ms]. How long must the system wait before the commit timestamp is guaranteed to be in the past?',
    options: [
      'T ms — the absolute timestamp value',
      '0 ms — the commit is immediate',
      '7 ms — the width of the uncertainty interval',
      'It depends on the network round-trip time',
    ],
    correctAnswer: 2,
    explanation: 'Spanner\'s commit wait requires waiting for the uncertainty interval to elapse. If TrueTime says the true time is between T and T+7ms, then after waiting 7ms, we can be certain that the timestamp T is in the past. Any subsequent transaction will get a timestamp strictly greater than T, preserving linearizability.',
    tags: ['spanner', 'commit-wait', 'truetime'],
  },
  {
    id: 'ddia-distributed-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Failure Detection',
    question: 'A system uses exponential backoff for retries after a timeout. The initial retry delay is 100ms and doubles each time, up to a cap of 10 seconds. After 5 consecutive failures, the retry delay is 3.2 seconds. What is the primary benefit of this approach over fixed-interval retries?',
    options: [
      'It uses less memory than fixed-interval retries',
      'It guarantees the node will recover within the backoff period',
      'It detects failures faster',
      'It reduces load on a potentially overloaded or recovering node, giving it time to recover',
    ],
    correctAnswer: 3,
    explanation: 'Exponential backoff reduces the rate of retries over time, preventing a thundering herd of retry requests from overwhelming a node that may be struggling or recovering. Fixed-interval retries maintain constant pressure on the failing node, potentially preventing its recovery. The backoff cap prevents the retry interval from growing too large.',
    tags: ['exponential-backoff', 'retry', 'overload'],
  },
  {
    id: 'ddia-distributed-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Process Pauses',
    question: 'Your distributed lock service grants a lock to node A with a fencing token of 42. Node A pauses for GC. The lock expires and is granted to node B with fencing token 43. Node A resumes and tries to write with token 42. Based on DDIA, what should happen?',
    options: [
      'The write succeeds because node A had the lock when it started',
      'The storage service rejects the write because it has already seen fencing token 43, which is higher than 42',
      'The storage service cannot tell which token is newer',
      'Both writes succeed and are merged',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes fencing tokens as the solution to this exact problem. The storage service tracks the highest fencing token it has seen. When node A tries to write with token 42 after the service has seen token 43, the write is rejected. This prevents stale writes from a node that lost its lock during a pause.',
    tags: ['fencing-token', 'distributed-lock', 'gc-pause', 'applied'],
  },
  {
    id: 'ddia-distributed-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Clocks',
    question: 'Your distributed database uses timestamps from NTP-synchronized clocks for LWW conflict resolution. Two writes to the same key arrive with timestamps 1ms apart. NTP documentation says accuracy is "within 100ms." Based on DDIA, can you rely on this ordering?',
    options: [
      'Yes — 1ms difference is within NTP accuracy bounds',
      'No — the NTP accuracy of 100ms means the clock error could be larger than the 1ms timestamp difference, making the ordering unreliable and potentially causing the "wrong" write to win',
      'NTP accuracy is always sub-millisecond in datacenters',
      'LWW does not depend on clock accuracy',
    ],
    correctAnswer: 1,
    explanation: 'DDIA warns that LWW with physical timestamps is unreliable when the timestamp difference is smaller than the clock synchronization error. With 100ms NTP accuracy, a 1ms difference in timestamps is meaningless — the write with the "later" timestamp might have actually happened first. This can cause silent data loss.',
    tags: ['clocks', 'ntp', 'lww', 'clock-accuracy', 'applied'],
  },
  {
    id: 'ddia-distributed-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Unreliable Networks',
    question: 'Your service has a 5-second timeout for requests to a downstream service. During a network blip, 1000 requests timeout simultaneously and are all retried at the 5-second mark. Based on DDIA, what problem does this create?',
    options: [
      'No problem — retries are the standard approach to timeouts',
      'A "retry storm" that hits the downstream service with 1000 simultaneous requests, potentially overwhelming it; this should be mitigated with jittered (randomized) retry delays and exponential backoff',
      'The downstream service will handle the retries efficiently',
      'TCP will automatically space out the retries',
    ],
    correctAnswer: 1,
    explanation: 'DDIA discusses how correlated retries can amplify failures. If all clients timeout at the same time and retry simultaneously, the burst of retries can overwhelm the recovering service. Adding random jitter to retry timing spreads the load, and exponential backoff reduces retry frequency for persistent failures.',
    tags: ['retry-storm', 'jitter', 'exponential-backoff', 'applied'],
  },
  {
    id: 'ddia-distributed-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Distributed Problems',
    subcategory: 'Failure Detection',
    question: 'Your monitoring system uses a fixed 10-second heartbeat timeout. During normal operation, heartbeat round-trip time is 1-2ms. However, you experience false failovers when a node has a 12-second GC pause. Based on DDIA, what should you do?',
    options: [
      'Increase the timeout to 30 seconds',
      'Disable garbage collection',
      'Use an adaptive timeout that adjusts based on observed network round-trip times and their variability, like a Phi Accrual failure detector',
      'Use a 1-second timeout for faster detection',
    ],
    correctAnswer: 2,
    explanation: 'DDIA discusses the Phi Accrual failure detector (used by Akka and Cassandra) which continuously measures response times and adjusts its suspicion level based on observed variability. Rather than a binary timeout, it provides a probability that a node has failed, allowing the system to make more nuanced decisions.',
    tags: ['failure-detection', 'phi-accrual', 'adaptive-timeout', 'applied'],
  },
];

export default questions;
