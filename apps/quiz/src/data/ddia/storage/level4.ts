import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-storage-l4-001',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'In leveled compaction (as used by LevelDB/RocksDB), what invariant is maintained at each level beyond level 0?',
    options: ['All SSTables across all levels are sorted by creation time', 'SSTables at each level have non-overlapping key ranges, and each level is 10x larger than the previous', 'Each level contains at most one SSTable', 'Each level uses a different compression algorithm'],
    correctAnswer: 1,
    explanation: 'In leveled compaction, level 0 may have overlapping SSTables (flushed directly from memtable), but from level 1 onward, SSTables have non-overlapping key ranges, and each level has a size limit roughly 10x the previous level. When a level exceeds its limit, one SSTable is picked and merged into the next level.',
    tags: ['lsm-tree', 'leveled-compaction', 'leveldb'],
  },
  {
    id: 'ddia-storage-l4-002',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'What is the theoretical write amplification of leveled compaction with a size ratio of 10 between levels?',
    options: ['O(N) where N is the dataset size', 'O(1)', 'O(N log N)', 'O(log N) with base 10, roughly 10 * number of levels'],
    correctAnswer: 3,
    explanation: 'In leveled compaction, when an SSTable is compacted from level L to level L+1, it may need to rewrite up to 10 SSTables at level L+1 (due to the 10x size ratio). Across all levels, write amplification is approximately 10 * L, where L is the number of levels (proportional to log base 10 of the dataset size).',
    tags: ['lsm-tree', 'write-amplification', 'leveled-compaction'],
  },
  {
    id: 'ddia-storage-l4-003',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'A B+ tree with 4 KB pages, 8-byte keys, 8-byte page pointers, and 8-byte value pointers has what approximate branching factor and maximum leaf entries?',
    options: ['Branching factor ~128, leaf entries ~512', 'Branching factor ~256, leaf entries ~256', 'Branching factor ~250, leaf entries ~250', 'Branching factor ~500, leaf entries ~500'],
    correctAnswer: 2,
    explanation: 'Internal (branch) pages store keys and child page pointers. Each entry is ~16 bytes (8-byte key + 8-byte page pointer). With a 4 KB (4096-byte) page: 4096 / 16 = 256 entries maximum. Accounting for page headers, sibling pointers, and metadata overhead, the effective branching factor is roughly ~250. Leaf pages store keys and value pointers (8 + 8 = 16 bytes per entry), yielding a similar ~250 entries per leaf. With a branching factor of 250, a 3-level tree can index ~250^3 ≈ 15.6 million keys, and 4 levels can handle ~3.9 billion keys.',
    tags: ['b-tree', 'branching-factor', 'page-layout'],
  },
  {
    id: 'ddia-storage-l4-004',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'In a column store using bitmap encoding with run-length encoding, a country column with 5 billion rows and 200 distinct values would require approximately how much space?',
    options: ['200 * 8 bytes = 1600 bytes total', '5 billion * 200 bits = ~125 GB uncompressed, but run-length encoding on sorted data can reduce this to megabytes', '5 billion * 1 byte = 5 GB regardless of encoding', 'Exactly 5 billion bytes'],
    correctAnswer: 1,
    explanation: 'Each of the 200 distinct values gets a bitmap of 5 billion bits (~625 MB each, ~125 GB total uncompressed). However, if the data is sorted by country, each bitmap is a long run of 1s followed by 0s (or vice versa), and run-length encoding compresses this dramatically, often to just a few megabytes.',
    tags: ['column-storage', 'bitmap-encoding', 'run-length-encoding'],
  },
  {
    id: 'ddia-storage-l4-005',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'Why does size-tiered compaction in LSM-trees tend to cause temporary spikes in disk space usage?',
    options: ['Because merging similarly-sized SSTables requires space for both the old and new SSTables until the merge completes and old SSTables are deleted', 'Because it writes data to multiple replicas simultaneously', 'Because it keeps all historical versions of every key', 'Because the memtable grows unbounded between flushes'],
    correctAnswer: 0,
    explanation: 'In size-tiered compaction, when multiple SSTables of similar size are merged, the engine must write the merged output while keeping the input SSTables intact (they may still be serving reads). Disk usage temporarily doubles for the affected SSTables until the old ones are safely deleted after the merge.',
    tags: ['lsm-tree', 'size-tiered', 'disk-usage'],
  },
  {
    id: 'ddia-storage-l4-006',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'What is the "write amplification" of a B-tree update that causes a leaf page split propagating up to the root?',
    options: ['At least 2h+1 page writes where h is the tree height, since each level requires writing the modified page plus the WAL entry', '2 page writes (leaf + parent)', 'The number of pages equals the number of keys in the tree', '1 page write (only the leaf)'],
    correctAnswer: 0,
    explanation: 'A page split creates a new leaf page, modifies the original leaf, and updates the parent to add a pointer. If the parent is also full, it splits too, propagating up. In the worst case, splits propagate to the root, requiring writes at every level. Each page write also goes to the WAL, roughly doubling the I/O.',
    tags: ['b-tree', 'write-amplification', 'page-split'],
  },
  {
    id: 'ddia-storage-l4-007',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'A Bloom filter with 10 bits per key and 7 hash functions has approximately what false positive rate?',
    options: ['~10%', '~1%', '~0.1%', '~0.01%'],
    correctAnswer: 1,
    explanation: 'The false positive rate of a Bloom filter is approximately (1 - e^(-kn/m))^k, where k is the number of hash functions, n is the number of elements, and m is the number of bits. With 10 bits per key (m/n = 10) and k = 7, this evaluates to approximately 0.82%, or roughly 1%.',
    tags: ['bloom-filter', 'false-positive-rate', 'lsm-tree'],
  },
  {
    id: 'ddia-storage-l4-008',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Hash Indexes',
    question: 'Bitcask requires that all keys fit in RAM, but values can be much larger and reside on disk. For a dataset with 100 million keys averaging 32 bytes each, approximately how much RAM is needed just for the hash map?',
    options: ['~32 GB', '~320 MB', '~3.2 GB for keys alone, plus overhead for hash buckets, pointers, and byte offsets, totaling roughly 5-10 GB', '~100 MB'],
    correctAnswer: 2,
    explanation: 'Each key occupies ~32 bytes, and the hash map must also store a byte offset (8 bytes), file ID, and hash bucket overhead. With 100 million entries, keys alone are ~3.2 GB. Hash map overhead (load factor, pointers, metadata) typically adds 50-200%, bringing the total to roughly 5-10 GB.',
    tags: ['hash-index', 'bitcask', 'memory-sizing'],
  },
  {
    id: 'ddia-storage-l4-009',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'In a column store with vectorized processing, what is the performance advantage of operating on compressed column data (e.g., bitmaps) directly without decompression?',
    options: ['It reduces network bandwidth between storage nodes', 'It avoids CPU cache misses by keeping data compressed in L1/L2 cache and using bitwise operations that process many rows per CPU cycle', 'It allows the use of GPU acceleration', 'It eliminates the need for query optimization'],
    correctAnswer: 1,
    explanation: 'Compressed column data (like bitmaps) can fit in CPU cache more effectively. Bitwise AND/OR operations on compressed bitmaps evaluate predicates for many rows simultaneously in a single CPU cycle. This tight loop over compressed data in cache is orders of magnitude faster than processing individual decompressed rows.',
    tags: ['column-storage', 'vectorized-processing', 'cpu-cache'],
  },
  {
    id: 'ddia-storage-l4-010',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Log-Structured Storage',
    question: 'When merging SSTables, if the same key appears in multiple segments, which value is kept and why does this guarantee correctness?',
    options: ['All values are kept and versioned', 'The value from the most recent (newest) segment is kept, because newer segments always contain more recent writes due to the append-only nature of the log', 'The value from the oldest segment is kept as the original truth', 'The smallest value is kept to minimize storage'],
    correctAnswer: 1,
    explanation: 'Since writes are always appended to the newest segment, a key appearing in a newer segment must have been written more recently than the same key in an older segment. During merging, keeping the value from the newest segment ensures the most recent write wins, maintaining the correct state of the database.',
    tags: ['sstable', 'merging', 'correctness'],
  },
  {
    id: 'ddia-storage-l4-011',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'Some databases (like LMDB) implement B-trees using copy-on-write with MVCC. What subtle advantage does this provide for long-running read transactions?',
    options: ['Read transactions complete faster because they skip unchanged pages', 'Long-running reads automatically cancel if a write conflict is detected', 'Read transactions can use a consistent snapshot of the old tree root without being affected by concurrent writes, requiring no read locks', 'Read transactions are redirected to a replica'],
    correctAnswer: 2,
    explanation: 'With copy-on-write, writers create a new version of modified pages and a new root. A long-running reader that started with the old root continues to see a consistent snapshot of the database at the point in time when it started, even as writers modify the tree. No locks are needed for readers, enabling true MVCC.',
    tags: ['b-tree', 'copy-on-write', 'mvcc'],
  },
  {
    id: 'ddia-storage-l4-012',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'OLAP',
    question: 'A data cube precomputing aggregates over 5 dimensions, each with cardinality C, would have how many cells in the fully materialized cube?',
    options: ['C * log(5)', '5^C', 'C^5', '5 * C'],
    correctAnswer: 2,
    explanation: 'A fully materialized data cube is the Cartesian product of all dimension values. With 5 dimensions each having C distinct values, the total number of cells is C^5. For even modest cardinalities (e.g., C=100), this becomes 10 billion cells, which is why fully materialized cubes are impractical for high-cardinality dimensions.',
    tags: ['olap', 'data-cube', 'cardinality'],
  },
  {
    id: 'ddia-storage-l4-013',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'RocksDB uses "prefix Bloom filters" as an optimization. What do these provide that standard Bloom filters do not?',
    options: ['They allow Bloom filters to be shared across multiple SSTables', 'They allow efficient existence checks for all keys sharing a common prefix, enabling fast prefix-based range scans', 'They reduce false positive rates to exactly zero', 'They compress keys using prefix elimination'],
    correctAnswer: 1,
    explanation: 'Standard Bloom filters only support exact key lookups. Prefix Bloom filters index key prefixes, so a query like "find all keys starting with user:123:" can first check the prefix Bloom filter to determine if any keys with that prefix exist in the SSTable, avoiding unnecessary disk reads for prefix-based scans.',
    tags: ['lsm-tree', 'bloom-filter', 'rocksdb'],
  },
  {
    id: 'ddia-storage-l4-014',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'In a column store, writes are challenging because inserting a row means adding a value to every column file. How do modern column stores like Vertica handle this?',
    options: ['They lock all column files during every write', 'They buffer writes in a row-oriented in-memory store (like an LSM-tree memtable) and periodically merge into the column-oriented storage', 'They convert to row storage during write-heavy periods', 'They only allow batch inserts at scheduled intervals'],
    correctAnswer: 1,
    explanation: 'Column stores like Vertica use an LSM-tree-like approach: writes go into an in-memory, row-oriented write-optimized store (WOS). Periodically, the WOS is bulk-converted into column-oriented, compressed, sorted storage (read-optimized store). Queries merge results from both stores transparently.',
    tags: ['column-storage', 'write-path', 'vertica'],
  },
  {
    id: 'ddia-storage-l4-015',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Log-Structured Storage',
    question: 'What is the "space amplification" of an LSM-tree, and how does it differ between size-tiered and leveled compaction?',
    options: ['Leveled compaction always uses more space than size-tiered', 'Space amplification only applies to B-trees', 'Space amplification is the ratio of actual disk usage to logical data size; size-tiered has higher space amplification because it can have many overlapping copies of the same key', 'Space amplification measures CPU overhead, not disk usage'],
    correctAnswer: 2,
    explanation: 'Space amplification is the ratio of physical storage used to the logical size of the data. Size-tiered compaction can have high space amplification because multiple SSTables may contain the same key at different tiers. Leveled compaction maintains non-overlapping key ranges per level, keeping space amplification close to 1 at the cost of higher write amplification.',
    tags: ['lsm-tree', 'space-amplification', 'compaction'],
  },
  {
    id: 'ddia-storage-l4-016',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'A B-tree with a branching factor of 500 stores 500 billion key-value pairs. What is the minimum height of this tree?',
    options: ['6 levels', '5 levels', '3 levels', '4 levels'],
    correctAnswer: 1,
    explanation: 'A B-tree of height h can store up to ~500^h entries. 500^4 = 62.5 billion (not enough), while 500^5 = 31.25 trillion (more than enough). So the minimum height to store 500 billion entries is 5 levels. This means any key can be found with at most 5 disk seeks.',
    tags: ['b-tree', 'height', 'branching-factor'],
  },
  {
    id: 'ddia-storage-l4-017',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'The "RUM conjecture" states that a storage engine can optimize at most two of three metrics: Read, Update, and Memory overhead. How do LSM-trees and B-trees position themselves on this trade-off?',
    options: ['LSM-trees optimize reads and memory; B-trees optimize updates and memory', 'LSM-trees optimize updates and memory (at the cost of reads); B-trees optimize reads and updates (at the cost of memory/space)', 'Both optimize the same two metrics', 'The RUM conjecture does not apply to these storage structures'],
    correctAnswer: 1,
    explanation: 'LSM-trees sacrifice read performance (checking multiple levels) to achieve excellent update performance (sequential writes) and good space efficiency (compaction). B-trees sacrifice space efficiency (fragmentation, page overhead) to provide good read performance (single tree traversal) and reasonable update performance.',
    tags: ['lsm-tree', 'b-tree', 'rum-conjecture'],
  },
  {
    id: 'ddia-storage-l4-018',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'DDIA warns that compaction in LSM-trees cannot always keep up with the incoming write rate. What is the catastrophic consequence and how should it be handled?',
    options: [
      'The database runs out of memory',
      'Uncompacted SSTables accumulate on disk, increasing the number of files reads must check, causing read latency to degrade progressively; monitoring and back-pressure mechanisms are needed to detect and respond before the system becomes unusable',
      'The WAL grows unbounded and crashes the system',
      'Compaction lag only affects write performance, not reads',
    ],
    correctAnswer: 1,
    explanation: 'DDIA highlights this critical operational issue: if writes arrive faster than compaction can process them, uncompacted SSTables accumulate. Each read must check more files, degrading read performance. In extreme cases, the system can effectively become unusable. Monitoring compaction lag and applying write back-pressure is essential.',
    tags: ['lsm-tree', 'compaction-lag', 'back-pressure', 'operations', 'expert'],
  },
  {
    id: 'ddia-storage-l4-019',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'DDIA discusses how B-tree page fragmentation occurs over time. What is the impact and how does it compare to LSM-tree space usage?',
    options: [
      'B-trees never fragment because pages are fixed size',
      'B-tree pages can become partially empty after splits and deletions, leaving fragmented unused space; LSM-trees rewrite data during compaction, achieving better disk utilization, but at the cost of write amplification',
      'LSM-trees fragment more than B-trees',
      'Fragmentation only affects SSDs, not spinning disks',
    ],
    correctAnswer: 1,
    explanation: 'DDIA notes that B-tree pages may have wasted space due to splits leaving pages partially empty and deletions leaving gaps. LSM-trees periodically rewrite data during compaction, which eliminates fragmentation but causes write amplification. This is a fundamental space-efficiency trade-off between the two approaches.',
    tags: ['b-tree', 'fragmentation', 'lsm-tree', 'space-efficiency', 'expert'],
  },
  {
    id: 'ddia-storage-l4-020',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'DDIA describes how column-oriented storage enables vectorized processing. What is vectorized processing and why does column storage enable it?',
    options: [
      'Processing data using GPU vector units',
      'A technique where the query engine operates on compressed column data in batches using SIMD CPU instructions, exploiting the fact that column data is homogeneous and contiguous in memory',
      'Distributing query processing across multiple servers',
      'Storing data in vector format instead of scalar format',
    ],
    correctAnswer: 1,
    explanation: 'DDIA discusses how column data (all values of the same type stored contiguously) is ideally suited for vectorized processing: the CPU can load a chunk of column data into its L1 cache and apply operations using SIMD instructions that process multiple values in a single CPU cycle. This is not possible with row-oriented storage where different column types are interleaved.',
    tags: ['column-storage', 'vectorized-processing', 'simd', 'cpu-cache', 'expert'],
  },
  {
    id: 'ddia-storage-l4-021',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'DDIA discusses the bandwidth trade-off in LSM-trees: the disk bandwidth must be shared between incoming writes and compaction. What is the formal relationship?',
    options: [
      'Compaction uses exactly 50% of disk bandwidth',
      'The total disk write bandwidth available is split between initial writes and compaction rewrites; the write amplification factor determines the ratio — if write amplification is 10x, compaction uses roughly 10x the bandwidth of incoming writes',
      'Compaction bandwidth is independent of write rate',
      'Only SSD bandwidth is affected, not HDD bandwidth',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that if write amplification is W, then for every byte the application writes, the storage engine writes W bytes total (initial write plus compaction rewrites). The finite disk bandwidth must accommodate both, so higher write amplification reduces the effective write throughput available to the application.',
    tags: ['lsm-tree', 'write-amplification', 'bandwidth', 'compaction', 'expert'],
  },
  {
    id: 'ddia-storage-l4-022',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'DDIA mentions that writes to column-oriented storage are inherently more difficult than in row-oriented storage. Why is this, and how do systems like Vertica handle it?',
    options: [
      'Column stores cannot accept writes at all',
      'Inserting a row requires updating many separate column files simultaneously; systems use an LSM-tree-like approach — writing to an in-memory row-oriented buffer first, then merging into column storage in bulk during background compaction',
      'Column stores require twice the storage for write buffering',
      'Writes are slower because each column must be compressed individually in real-time',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that column storage makes writes complex because a single row insertion touches many separate column files. The practical solution (used by Vertica and others) is an LSM-tree-like approach: buffer writes in memory in row format, then bulk-merge them into compressed column files. This mirrors the memtable-to-SSTable pattern.',
    tags: ['column-storage', 'writes', 'lsm-tree-hybrid', 'vertica', 'expert'],
  },
  {
    id: 'ddia-storage-l4-023',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'Indexes',
    question: 'DDIA discusses multi-dimensional indexes for geospatial queries (like finding restaurants within a geographic rectangle). Why are standard B-tree or LSM-tree indexes insufficient for this?',
    options: [
      'B-trees cannot store geographic data',
      'Standard indexes sort data along a single dimension; a geographic query like "latitude BETWEEN x1 AND x2 AND longitude BETWEEN y1 AND y2" requires simultaneously filtering on two dimensions, which a single-dimensional index cannot efficiently do',
      'Geographic data requires column storage',
      'LSM-trees do not support range queries',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that a standard B-tree or LSM-tree index can efficiently filter on one dimension (e.g., latitude) but must then scan all matching entries for the second dimension. Multi-dimensional indexes (R-trees, space-filling curves like Z-order) map multi-dimensional data into a single dimension that preserves locality.',
    tags: ['indexes', 'multi-dimensional', 'geospatial', 'r-tree', 'expert'],
  },
  {
    id: 'ddia-storage-l4-024',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'In-Memory Databases',
    question: 'DDIA notes that in-memory databases can still provide durability. What mechanisms do they use, and why does this not negate their performance advantage?',
    options: [
      'In-memory databases sacrifice durability for performance',
      'They use WAL on disk, periodic snapshots, and/or replication for durability; the performance advantage persists because the write path is append-only (fast sequential writes) and reads never touch disk',
      'They rely solely on battery-backed RAM for durability',
      'They write every transaction synchronously to disk, just like disk-based databases',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that in-memory databases achieve durability through append-only WAL, periodic snapshots, and/or replication to other machines. These mechanisms use fast sequential disk writes (not random I/O) and do not affect the read path, which remains entirely in-memory. The performance advantage comes from read optimization and flexible data structures.',
    tags: ['in-memory', 'durability', 'wal', 'snapshots', 'expert'],
  },
  {
    id: 'ddia-storage-l4-025',
    language: 'ddia',
    level: 'level4',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'DDIA mentions that some B-tree implementations use abbreviated keys in internal nodes. What is the purpose and trade-off?',
    options: [
      'Abbreviated keys reduce security risks',
      'Using abbreviated keys (just enough to act as boundaries) increases the branching factor by fitting more keys per page, making the tree shallower and reducing disk seeks; the trade-off is slightly more complex key comparison logic',
      'Abbreviated keys make writes faster but reads slower',
      'Abbreviated keys are required by the B+ tree specification',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes that B+ tree variants pack more keys into each page by abbreviating keys (only storing enough information to serve as boundaries). This increases the branching factor, reducing tree depth and the number of disk seeks needed. The pages can hold more child pointers, making the tree more efficient.',
    tags: ['b-tree', 'abbreviated-keys', 'branching-factor', 'optimization', 'expert'],
  },
];

export default questions;
