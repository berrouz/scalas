import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-storage-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'You are designing a time-series database that ingests millions of sensor readings per second but rarely reads old data. Which storage engine type would be most appropriate?',
    options: ['B-tree with in-place updates', 'Hash index with in-memory storage', 'Heap file with no index', 'LSM-tree with size-tiered compaction'],
    correctAnswer: 3,
    explanation: 'LSM-trees excel at write-heavy workloads because they batch writes in memory and flush them sequentially. Size-tiered compaction is well-suited here because it minimizes write amplification, which is critical at extremely high write throughput. Reads are infrequent, so the read penalty is acceptable.',
    tags: ['lsm-tree', 'time-series', 'size-tiered'],
  },
  {
    id: 'ddia-storage-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'A banking application requires strong transactional guarantees with predictable read latency. Why might a B-tree engine be preferred over an LSM-tree engine?',
    options: ['B-trees provide more predictable read latency since each key exists in exactly one place, and locks can be applied directly to key ranges', 'B-trees have higher write throughput', 'B-trees use less disk space', 'B-trees do not need crash recovery mechanisms'],
    correctAnswer: 0,
    explanation: 'In a B-tree, each key exists in exactly one place in the index, making it straightforward to attach locks to key ranges for transactional isolation. Read latency is also more predictable because lookups traverse a single tree path. LSM-trees may have keys in multiple SSTables and compaction can interfere with read latency.',
    tags: ['b-tree', 'transactions', 'read-latency'],
  },
  {
    id: 'ddia-storage-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'Your LSM-tree database is experiencing high read latency at the 99th percentile. Compaction is falling behind incoming writes. What is the most likely consequence?',
    options: ['The database will run out of memory', 'The WAL becomes corrupted', 'Reads slow down because they must check an ever-growing number of unmerged SSTable segments', 'Write throughput increases as more segments are available'],
    correctAnswer: 2,
    explanation: 'When compaction cannot keep up with the write rate, the number of unmerged SSTables grows. Reads become progressively slower because they must check more segment files to find a key. Disk space usage also increases because duplicate and deleted keys are not reclaimed promptly.',
    tags: ['lsm-tree', 'compaction', 'read-latency'],
  },
  {
    id: 'ddia-storage-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'An analytics team runs queries that aggregate sales by date and product category. The data warehouse has 200 columns per row. How does column storage help?',
    options: ['The query engine reads only the 3 relevant columns from disk, ignoring the other 197 columns entirely', 'It stores all 200 columns in a single compressed file', 'It automatically creates materialized views for common queries', 'It removes the need for indexes'],
    correctAnswer: 0,
    explanation: 'In column-oriented storage, each column is stored separately. A query that only needs date, product_category, and sales_amount reads only those 3 column files, completely skipping the other 197 columns. In a row-oriented store, all 200 columns would be loaded from disk even though most are unused.',
    tags: ['column-storage', 'query-optimization', 'analytics'],
  },
  {
    id: 'ddia-storage-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Hash Indexes',
    question: 'You are building a caching layer where each key is a URL and the value is the cached page content. Keys are updated frequently but never deleted, and range queries are not needed. Which storage approach from DDIA is well-suited?',
    options: ['B-tree with WAL', 'Bitcask-style hash index with append-only log and in-memory hash map', 'LSM-tree with leveled compaction', 'Column-oriented storage'],
    correctAnswer: 1,
    explanation: 'Bitcask is ideal for workloads with many writes per key but where all keys fit in memory. The in-memory hash map provides O(1) key lookups, and the append-only log ensures high write throughput. Since range queries are not needed, the lack of key ordering is not a limitation.',
    tags: ['hash-index', 'bitcask', 'caching'],
  },
  {
    id: 'ddia-storage-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'A Cassandra cluster uses size-tiered compaction and is running low on disk space. Switching to leveled compaction is proposed. What trade-off should be expected?',
    options: ['Leveled compaction reduces both disk usage and write amplification', 'Leveled compaction eliminates the need for Bloom filters', 'Leveled compaction uses less disk space but increases write amplification', 'Leveled compaction uses more disk space but improves read performance'],
    correctAnswer: 2,
    explanation: 'Leveled compaction maintains data in non-overlapping key ranges at each level, so each key exists in at most one SSTable per level. This uses less disk space and provides better read performance. However, it rewrites data more frequently as it moves between levels, increasing write amplification.',
    tags: ['lsm-tree', 'leveled-compaction', 'cassandra'],
  },
  {
    id: 'ddia-storage-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'A B-tree database crashes midway through a page split operation. Without a WAL, what is the most dangerous potential outcome?',
    options: ['The entire database is automatically rebuilt', 'An orphan page is created, leading to data loss because no parent points to it', 'Some keys become duplicated', 'Only the most recent transaction is lost'],
    correctAnswer: 1,
    explanation: 'During a page split, a B-tree must write two new child pages and update the parent page. If a crash occurs after writing the child pages but before updating the parent, the new pages become orphans with no references. The WAL ensures that the operation can be replayed to completion after recovery.',
    tags: ['b-tree', 'wal', 'crash-recovery'],
  },
  {
    id: 'ddia-storage-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'A column store needs to reconstruct a complete row (e.g., to display a customer record). What is the main challenge?',
    options: ['Each column file must be read and the values at the same row position must be assembled, requiring all columns to use the same row ordering', 'The database must convert column data to row format permanently', 'Column stores do not support row reconstruction', 'Columns cannot store all data types'],
    correctAnswer: 0,
    explanation: 'In a column store, the nth entry in each column file must belong to the same row. To reconstruct a row, the engine reads the relevant entry position from every column file. This requires all column files to maintain a consistent row ordering, which complicates operations like sorting.',
    tags: ['column-storage', 'row-reconstruction', 'ordering'],
  },
  {
    id: 'ddia-storage-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'OLAP',
    question: 'A data warehouse star schema has a fact table with 10 billion rows and a date dimension with 3,650 rows (10 years of days). A query joins them to aggregate monthly revenue. What makes this practical?',
    options: ['The database automatically parallelizes the join across all CPU cores', 'The join is eliminated by denormalization', 'The date dimension is tiny and fits in memory, so the join is effectively a lookup into a small cached table', 'Column storage prevents the need for joins'],
    correctAnswer: 2,
    explanation: 'Dimension tables in star schemas are typically much smaller than fact tables. The date dimension easily fits in memory, making the join effectively a hash lookup for each fact row. The actual computational cost is dominated by scanning the fact table columns, which column storage handles efficiently.',
    tags: ['olap', 'star-schema', 'join-performance'],
  },
  {
    id: 'ddia-storage-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'An LSM-tree storage engine shows that 30% of read requests are for keys that do not exist. What optimization would have the greatest impact?',
    options: ['Increasing the memtable size', 'Adding a secondary B-tree index', 'Switching from leveled to size-tiered compaction', 'Adding Bloom filters to SSTable files'],
    correctAnswer: 3,
    explanation: 'Without Bloom filters, reading a non-existent key requires checking the memtable and every SSTable at every level before confirming the key does not exist. Bloom filters can definitively say a key is not in an SSTable, avoiding expensive disk reads for most non-existent key lookups.',
    tags: ['lsm-tree', 'bloom-filter', 'non-existent-keys'],
  },
  {
    id: 'ddia-storage-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Log-Structured Storage',
    question: 'A team is considering using a single large segment file versus breaking the log into multiple smaller segment files. What is the key operational benefit of multiple segments?',
    options: ['Multiple segments provide stronger durability guarantees', 'Multiple segments require less total disk space', 'Multiple segments eliminate the need for an index', 'Compaction can run on old segments while new writes go to the current segment, enabling continuous operation'],
    correctAnswer: 3,
    explanation: 'With multiple segments, compaction and merging can run as background processes on older, frozen segments while new writes are appended to the current segment. A single file would require stopping writes during compaction or dealing with complex concurrent modifications.',
    tags: ['log-structured', 'segments', 'compaction'],
  },
  {
    id: 'ddia-storage-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'A column-oriented data warehouse chooses to sort its data by date as the primary sort key and customer_id as the secondary. Which query pattern benefits most from this choice?',
    options: ['Queries that perform full table scans without any filter', 'Queries that update individual customer records', 'Queries filtering by product category', 'Queries filtering by date range and then grouping by customer'],
    correctAnswer: 3,
    explanation: 'Sorting by date first allows date range queries to skip large portions of the data. The secondary sort by customer_id means that within each date, customer records are grouped together, making queries that filter by date and group by customer very efficient with minimal I/O.',
    tags: ['column-storage', 'sort-order', 'query-optimization'],
  },
  {
    id: 'ddia-storage-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'An application performs many range scans over sequential keys (e.g., scanning all orders from order-10000 to order-10500). How does a B-tree layout support this?',
    options: ['B-trees store all data in a single sorted array', 'B-trees convert range scans into hash lookups', 'Leaf pages are often laid out sequentially on disk and linked together via sibling pointers, enabling efficient sequential scans', 'B-trees do not support range scans efficiently'],
    correctAnswer: 2,
    explanation: 'In many B-tree implementations, leaf pages store keys in sorted order and contain pointers to their sibling pages. Once the starting key is found, the engine can scan sequentially through sibling leaf pages without traversing the tree again, making range scans efficient.',
    tags: ['b-tree', 'range-scan', 'leaf-nodes'],
  },
  {
    id: 'ddia-storage-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Hash Indexes',
    question: 'A Bitcask-based system restarts after a crash. How does it rebuild the in-memory hash map?',
    options: ['It reads the hash map from a replicated backup', 'It uses the B-tree index stored alongside the segments', 'It scans all segment files from start to end, rebuilding the hash map entry for each key from the most recent value', 'The hash map is persisted as part of the WAL'],
    correctAnswer: 2,
    explanation: 'After a restart, Bitcask must re-read all segment files to reconstruct the in-memory hash map. This can be slow for large datasets. To speed up recovery, Bitcask stores periodic snapshots (hint files) of the hash map on disk, though the canonical rebuild process scans the segments.',
    tags: ['hash-index', 'bitcask', 'crash-recovery'],
  },
  {
    id: 'ddia-storage-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'OLAP',
    question: 'A data warehouse materialized view precomputes total revenue by country and month. A new query asks for revenue by country, month, and product category. Can the existing materialized view answer it?',
    options: ['Yes, if you apply an additional GROUP BY', 'Yes, because it already contains country and month', 'No, but a simple filter on the materialized view will work', 'No, because the precomputed aggregates have already collapsed the product category dimension'],
    correctAnswer: 3,
    explanation: 'A materialized view or data cube that aggregates by country and month has already summed across all product categories. You cannot disaggregate the totals to recover per-category breakdowns. The query must go back to the raw fact table or a different materialized view that includes the product category dimension.',
    tags: ['olap', 'materialized-view', 'data-cube'],
  },
  {
    id: 'ddia-storage-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'An LSM-tree database loses power while the memtable contains uncommitted data. How is data loss typically prevented?',
    options: ['The memtable is periodically replicated to another server', 'LSM-trees accept this data loss as a trade-off for write speed', 'Every write is also appended to a WAL on disk before being added to the memtable, so the memtable can be restored from the WAL', 'The operating system automatically flushes the memtable on power loss'],
    correctAnswer: 2,
    explanation: 'To avoid losing memtable data on crash, LSM-tree engines write each incoming operation to a write-ahead log (WAL) on disk before inserting it into the memtable. On recovery, the WAL is replayed to restore the memtable to its pre-crash state.',
    tags: ['lsm-tree', 'wal', 'crash-recovery'],
  },
  {
    id: 'ddia-storage-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'Vertica allows storing the same column data sorted in multiple different orders across different replicas. What is the benefit of this approach?',
    options: ['It makes writes faster because each replica writes a smaller portion of the data', 'It allows each replica to independently compact its data', 'It provides redundancy in case of hardware failure only', 'Different queries can use the replica whose sort order best matches the query filter, acting like having multiple sort indexes without extra storage cost'],
    correctAnswer: 3,
    explanation: 'By storing redundant copies sorted differently (e.g., one by date, another by product_id), different query patterns can be routed to the replica with the most efficient sort order. Since replicas are needed for fault tolerance anyway, this provides multiple sort orders at no extra storage overhead.',
    tags: ['column-storage', 'vertica', 'sort-order'],
  },
  {
    id: 'ddia-storage-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'Your write-heavy application uses an LSM-tree storage engine but you notice read latency spikes during compaction. Based on DDIA, what is happening and how can you mitigate it?',
    options: [
      'Compaction corrupts the data files causing re-reads',
      'Compaction consumes disk I/O bandwidth, competing with read operations; rate-limiting compaction or using separate disks for compaction can help',
      'Reads are always slow in LSM-tree engines',
      'Compaction deletes files that reads are trying to access',
    ],
    correctAnswer: 1,
    explanation: 'DDIA warns that compaction is a significant source of I/O contention. When compaction runs, it reads and writes large amounts of data, consuming disk bandwidth that reads also need. Strategies include rate-limiting compaction throughput and scheduling compaction during low-traffic periods.',
    tags: ['lsm-tree', 'compaction', 'io-contention', 'applied'],
  },
  {
    id: 'ddia-storage-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'Your B-tree based database experiences a crash mid-way through a page split operation. Based on DDIA, what mechanism prevents data corruption?',
    options: [
      'B-trees are immune to crash corruption',
      'The WAL (write-ahead log) records the intended changes before they are applied; on recovery, the database replays the WAL to complete or undo the interrupted operation',
      'The operating system prevents crashes during page splits',
      'B-trees use checksums to auto-repair corrupted pages',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that a page split requires updating multiple pages (the split page, the new page, and the parent). The WAL records all changes before they are applied. On crash recovery, the WAL is replayed to bring the tree to a consistent state, completing any interrupted multi-page operations.',
    tags: ['b-tree', 'wal', 'crash-recovery', 'page-split', 'applied'],
  },
  {
    id: 'ddia-storage-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'Your data warehouse query scans 10 billion rows but only needs 3 out of 200 columns. Why would column-oriented storage outperform row-oriented storage by orders of magnitude here?',
    options: [
      'Column storage uses more CPU cores',
      'Column storage only reads the 3 needed columns from disk (1.5% of the data), whereas row storage must read all 200 columns for each of the 10 billion rows; combined with column compression, the I/O savings are massive',
      'Row storage cannot handle 10 billion rows',
      'Column storage has better network performance',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that column storage reads only the columns needed by the query. For 3 out of 200 columns, that is 1.5% of the total data. Additionally, each column compresses well because it contains values of a single type with repetition. The combination of selective column reading and compression provides orders-of-magnitude I/O reduction.',
    tags: ['column-storage', 'analytics', 'io-efficiency', 'applied'],
  },
  {
    id: 'ddia-storage-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'LSM-Trees',
    question: 'Your LSM-tree based database shows high read latency for point lookups of keys that do not exist. Based on DDIA, what optimization addresses this?',
    options: [
      'Adding a B-tree index on top of the LSM-tree',
      'Bloom filters — a probabilistic data structure that quickly determines a key definitely does not exist, avoiding the need to check multiple SSTable files',
      'Caching all non-existent keys in memory',
      'Using larger memtables to reduce SSTable count',
    ],
    correctAnswer: 1,
    explanation: 'DDIA specifically recommends Bloom filters for this problem. Without them, looking up a non-existent key requires checking the memtable and potentially all SSTables on disk. A Bloom filter can definitively say a key is not present, avoiding unnecessary disk I/O.',
    tags: ['lsm-tree', 'bloom-filter', 'non-existent-keys', 'applied'],
  },
  {
    id: 'ddia-storage-l3-022',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'OLAP',
    question: 'Your company runs analytics queries on its OLTP production database, causing performance degradation for user-facing transactions. Based on DDIA, what is the recommended architecture?',
    options: [
      'Run analytics queries during off-peak hours only',
      'Use read replicas for analytics queries',
      'Create a separate data warehouse with ETL from the OLTP system, so analytics queries run on an optimized copy without affecting production',
      'Upgrade the OLTP database hardware',
    ],
    correctAnswer: 2,
    explanation: 'DDIA recommends separate data warehouses for analytics. OLTP and OLAP have fundamentally different access patterns: OLTP reads/writes small amounts of data with low latency, while OLAP scans massive amounts of data. A data warehouse is optimized for OLAP with column storage, while the OLTP system remains unaffected.',
    tags: ['olap', 'oltp', 'data-warehouse', 'separation', 'applied'],
  },
  {
    id: 'ddia-storage-l3-023',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Indexes',
    question: 'Your application queries users by both (city, last_name) and (last_name, city) frequently. You have a concatenated index on (city, last_name). Why does this index not help the second query pattern?',
    options: [
      'Concatenated indexes work for both orderings equally',
      'The concatenated index (city, last_name) can efficiently filter by city first, but a query filtering by last_name first cannot use this index because the index is sorted by city first — you would need a separate index on (last_name, city)',
      'The second query does not need an index',
      'Concatenated indexes only support exact matches, not ranges',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that concatenated indexes sort by the first column, then within each first-column value by the second column. Queries that filter on the first column (or both) benefit from the index, but queries filtering only on the second column cannot efficiently use it because that column is not the primary sort key.',
    tags: ['indexes', 'concatenated-index', 'query-patterns', 'applied'],
  },
  {
    id: 'ddia-storage-l3-024',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'In-Memory Databases',
    question: 'Your application needs to support data structures like priority queues and sets with efficient operations. Based on DDIA\'s discussion, why might an in-memory database like Redis be a better fit than a disk-based database?',
    options: [
      'Redis uses faster disk drives',
      'In-memory databases can implement data structures (like sets, sorted sets, queues) that would be difficult to maintain efficiently in a disk-based B-tree or LSM-tree format',
      'Disk-based databases cannot store sets at all',
      'Redis does not need any persistence mechanism',
    ],
    correctAnswer: 1,
    explanation: 'DDIA notes that a key advantage of in-memory databases is the ability to provide data structures (priority queues, sets, sorted sets) that are hard to implement efficiently with disk-based indexes. These structures can be maintained in memory using algorithms that would not map well to B-tree or LSM-tree formats.',
    tags: ['in-memory', 'redis', 'data-structures', 'applied'],
  },
  {
    id: 'ddia-storage-l3-025',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'B-Trees',
    question: 'Your database uses B-trees and you notice that concurrent readers block while a writer holds a latch on a page being modified. Based on DDIA, what optimization technique addresses this?',
    options: [
      'Switching to an LSM-tree engine that has no locking',
      'Using latches (lightweight locks) with careful latch ordering, or using a copy-on-write scheme where readers always see a consistent snapshot without blocking on writers',
      'Disabling concurrent access entirely',
      'Using only append operations on the B-tree',
    ],
    correctAnswer: 1,
    explanation: 'DDIA discusses that B-trees require careful concurrency control. The copy-on-write approach (used by LMDB) is particularly elegant: modified pages are written to new locations, so readers always see a consistent old version until the new version is atomically installed, eliminating reader-writer blocking.',
    tags: ['b-tree', 'concurrency', 'latching', 'copy-on-write', 'applied'],
  },
  {
    id: 'ddia-storage-l3-026',
    language: 'ddia',
    level: 'level3',
    category: 'Storage & Retrieval',
    subcategory: 'Column Storage',
    question: 'You are designing a column store and need to choose a sort key. Your most common query filters by date_key and aggregates by product_sk. Based on DDIA, what sort order would you choose?',
    options: [
      'Sort by product_sk since aggregation is the main operation',
      'Sort by date_key first, then product_sk — this enables efficient date range scans (most common filter) and groups identical product_sk values together for compression within each date range',
      'Random order for even distribution',
      'Sort by row ID for simple sequential access',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that sorting by the most common query filter (date_key) enables efficient range scans. The secondary sort key (product_sk) groups similar values together within each date range, improving compression. This dual benefit of query efficiency and compression makes sort order choice critical.',
    tags: ['column-storage', 'sort-order', 'query-optimization', 'applied'],
  },
];

export default questions;
