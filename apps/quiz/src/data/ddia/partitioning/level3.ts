import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-partitioning-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'You are designing an IoT system where sensors write temperature readings every second, keyed by timestamp. Using key-range partitioning, what will happen and what is the best fix?',
    options: [
      'The hash function will produce collisions; use SHA-256 instead',
      'All writes go to the partition for the current time range (hot spot); prefix keys with sensor ID so writes spread across partitions',
      'Partitions will become corrupted; switch to a relational database',
      'Reads will be slow; add more replicas per partition',
    ],
    correctAnswer: 1,
    explanation: 'With pure timestamp keys and range partitioning, all current writes hit one partition. Prefixing with sensor_id (e.g., "sensor42_2024-01-15T10:30:00") distributes writes across partitions. To query a time range, you must issue a separate range query per sensor, but the write load is balanced.',
    tags: ['key-range', 'hot-spot', 'iot', 'sensor-data'],
  },
  {
    id: 'ddia-partitioning-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'An e-commerce platform uses key-range partitioning on product IDs (alphabetical). Category "Electronics" (product IDs E0001-E9999) gets 80% of traffic. What strategy best addresses this skew?',
    options: [
      'Add more replicas for the Electronics partition only',
      'Delete low-traffic product categories to rebalance',
      'Increase the RAM on the Electronics partition node',
      'Switch to hash partitioning on the product ID to distribute Electronics products across all partitions',
    ],
    correctAnswer: 3,
    explanation: 'Hash partitioning on the product ID would distribute Electronics products evenly across all partitions, eliminating the hot spot. This sacrifices range scans by product ID but solves the skew problem. If range scans are needed, a compound key approach (hash on category, range on product ID) could be used.',
    tags: ['key-range', 'hot-spot', 'hash-partitioning', 'skew'],
  },
  {
    id: 'ddia-partitioning-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'A social media app hash-partitions user data by user_id. A celebrity with millions of followers posts an update that triggers notifications. What is the core problem and DDIA\'s insight?',
    options: [
      'The celebrity\'s partition becomes a hot spot; DDIA notes most systems require the application to handle such skew, e.g., by appending random digits to the key',
      'Increasing the number of partitions will eliminate this hot spot',
      'The notification system should use a message queue, solving the problem entirely',
      'The hash function is broken; use a different hash algorithm',
    ],
    correctAnswer: 0,
    explanation: 'Even with hash partitioning, all reads/writes for one key go to one partition. DDIA suggests the application can mitigate this by appending a random number (e.g., two digits) to the key, splitting writes across 100 keys. However, reads then need to fan out to all 100 keys and merge results. Most systems today leave this to the application layer.',
    tags: ['hash-partitioning', 'celebrity-problem', 'hot-spot', 'application-level'],
  },
  {
    id: 'ddia-partitioning-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'A team uses Cassandra with a compound primary key (user_id, timestamp) for a messaging app. How does Cassandra handle a query for "all messages from user_id=42 in the last hour"?',
    options: [
      'It performs scatter/gather across all partitions since hash partitioning cannot do range queries',
      'It requires a full table scan because secondary indexes are not supported',
      'It uses a global index to look up the timestamps first, then fetches from the partition',
      'It hashes user_id to find the single partition, then performs a range scan on timestamp within that partition',
    ],
    correctAnswer: 3,
    explanation: 'Cassandra\'s compound primary key hashes the first component (user_id) to determine the partition. Within that partition, data is sorted by the remaining columns (timestamp). So a range query on timestamp for a specific user_id goes to exactly one partition and does an efficient range scan.',
    tags: ['cassandra', 'compound-key', 'range-scan', 'partition-lookup'],
  },
  {
    id: 'ddia-partitioning-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'An online marketplace uses document-partitioned (local) secondary indexes. A user searches for "red cars under $10,000." What happens at query time?',
    options: [
      'The query is sent to all partitions, each checks its local index, and results are merged (scatter/gather)',
      'The query is routed to the single partition that holds the "red cars" index',
      'The query fails because local indexes cannot handle compound filters',
      'The query is handled by the routing tier without touching any partition',
    ],
    correctAnswer: 0,
    explanation: 'With document-partitioned indexes, each partition indexes only its own data. A search for "red cars under $10,000" could match documents on any partition, so the query must be scattered to all partitions. Each partition searches its local index and returns matching results, which are then gathered and merged.',
    tags: ['secondary-index', 'local-index', 'scatter-gather', 'marketplace'],
  },
  {
    id: 'ddia-partitioning-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'A system uses term-partitioned (global) secondary indexes. A document with fields color=red and make=Honda is inserted. What happens to the indexes?',
    options: [
      'The global index is updated synchronously on a single coordinator node',
      'The index partitions responsible for the terms "red" and "Honda" must both be updated, possibly on different nodes',
      'Only the local partition\'s index is updated',
      'A full index rebuild is triggered across all partitions',
    ],
    correctAnswer: 1,
    explanation: 'With a global (term-partitioned) index, the index for "color:red" might live on one node and "make:Honda" on another. Inserting a document matching both terms requires updating both index partitions, which may be on different nodes. This is why writes are more complex and often done asynchronously.',
    tags: ['global-index', 'term-partitioned', 'cross-partition-write'],
  },
  {
    id: 'ddia-partitioning-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'You observe that secondary index queries on your 50-partition database occasionally have very high p99 latency, even though median latency is fine. Which index strategy is most likely in use and why?',
    options: [
      'Term-partitioned index; the global index is stored on a slow disk',
      'Document-partitioned index; scatter/gather across 50 partitions amplifies tail latency because the response waits for the slowest partition',
      'No secondary index; primary key lookups are causing the issue',
      'Term-partitioned index; it contacts too many partitions for writes',
    ],
    correctAnswer: 1,
    explanation: 'Document-partitioned (local) indexes require scatter/gather, querying all 50 partitions. The response time equals the slowest partition. With 50 partitions, the probability that at least one has a slow response (GC pause, disk I/O, etc.) is high, significantly amplifying tail latency.',
    tags: ['scatter-gather', 'tail-latency', 'local-index', 'p99'],
  },
  {
    id: 'ddia-partitioning-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'A Riak cluster uses a fixed number of 256 partitions across 4 nodes. A 5th node is added. What happens?',
    options: [
      '256 new partitions are created on the 5th node',
      'All 256 partitions are redistributed: roughly 51 partitions are moved to the new node, leaving about 51 per node',
      'Each partition is split in half, creating 512 total partitions',
      'The new node remains empty until a manual rebalance is triggered',
    ],
    correctAnswer: 1,
    explanation: 'With a fixed number of partitions, adding a 5th node means some partitions are reassigned from existing nodes. Ideally, each node ends up with about 256/5 ~ 51 partitions. Approximately 51 partitions are moved from the 4 existing nodes to the new one, with no new partitions created.',
    tags: ['fixed-partitions', 'rebalancing', 'riak', 'node-addition'],
  },
  {
    id: 'ddia-partitioning-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'Your HBase cluster uses dynamic partitioning. The dataset doubles in size over 6 months. What do you expect to observe regarding partitions?',
    options: [
      'The cluster will refuse writes once partition sizes exceed the configured limit',
      'The number of partitions roughly doubles as large partitions are automatically split',
      'HBase will require a manual partition split operation',
      'The number of partitions stays the same but each partition doubles in size',
    ],
    correctAnswer: 1,
    explanation: 'HBase uses dynamic partitioning: when a partition exceeds a size threshold, it is split into two. As data doubles, many partitions will exceed the threshold and split. The number of partitions grows roughly proportional to data size, keeping individual partitions manageable.',
    tags: ['dynamic-partitioning', 'hbase', 'partition-splitting', 'data-growth'],
  },
  {
    id: 'ddia-partitioning-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'A team enables fully automatic rebalancing. During a deployment, one node experiences increased GC pauses. What cascading problem might occur?',
    options: [
      'The system mistakes the slow node for a dead node, triggers rebalancing which increases network load, potentially overloading other nodes and causing further rebalancing',
      'Automatic rebalancing immediately shuts down the slow node',
      'The system waits for GC to finish and then resumes normally with no issues',
      'The GC pauses corrupt data on that node',
    ],
    correctAnswer: 0,
    explanation: 'Automatic rebalancing may interpret a slow node as failed and start moving its partitions away. This adds network and disk I/O pressure to other nodes, which may then become slow themselves, triggering more rebalancing. This cascading effect is why DDIA recommends human-in-the-loop rebalancing.',
    tags: ['automatic-rebalancing', 'cascading-failure', 'gc-pause', 'human-in-the-loop'],
  },
  {
    id: 'ddia-partitioning-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'A MongoDB cluster uses a config server (similar to ZooKeeper) for routing. The config server goes down. What happens to client queries?',
    options: [
      'The cluster automatically elects a new config server and continues without interruption',
      'All data is automatically copied to the client to enable direct access',
      'Mongos routers can continue routing using their cached partition maps, but partition changes will not be reflected until the config server recovers',
      'All queries fail immediately because the routing information is lost',
    ],
    correctAnswer: 2,
    explanation: 'Routing components (like MongoDB\'s mongos) cache the partition-to-node mapping. If the coordination service goes down, cached routing information is still usable. However, any partition reassignments during the outage will not be seen until the config server recovers, potentially leading to some stale routing.',
    tags: ['request-routing', 'config-server', 'caching', 'mongodb'],
  },
  {
    id: 'ddia-partitioning-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'A Cassandra cluster uses gossip-based routing. A network partition splits the cluster into two halves. How does routing behave?',
    options: [
      'Each side may have a different view of the cluster state, potentially routing requests to nodes that are unreachable from the client\'s network segment',
      'Cassandra automatically falls back to ZooKeeper when gossip fails',
      'All routing stops immediately on both sides',
      'Gossip protocol is immune to network partitions and continues to work perfectly',
    ],
    correctAnswer: 0,
    explanation: 'With gossip-based routing, each node has its own view of the cluster. During a network partition, the two halves may diverge in their views. Nodes on one side may believe a node on the other side is still alive (or vice versa), leading to incorrect routing attempts until gossip converges on the updated state.',
    tags: ['gossip-protocol', 'network-partition', 'cassandra', 'split-brain'],
  },
  {
    id: 'ddia-partitioning-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'You need to support both "get user by ID" (point lookup) and "get all users in a zip code" (range query). What partitioning approach should you consider?',
    options: [
      'Store two complete copies of the data, one hash-partitioned and one range-partitioned',
      'Key-range partition by zip code, ignoring point lookup performance',
      'Use no partitioning and keep all data on a single node',
      'Hash partition by user ID and use a document-partitioned secondary index on zip code',
    ],
    correctAnswer: 3,
    explanation: 'Hash partitioning on user_id efficiently serves point lookups. For range queries on zip code, a document-partitioned secondary index allows searching by zip code (with scatter/gather). Alternatively, a term-partitioned secondary index on zip code could avoid scatter/gather for that query pattern.',
    tags: ['hash-partitioning', 'secondary-index', 'point-lookup', 'range-query'],
  },
  {
    id: 'ddia-partitioning-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'You are using key-range partitioning in a multi-tenant SaaS application, keyed by (tenant_id, entity_id). One tenant has 100x more data than others. What is the impact?',
    options: [
      'No impact because key-range partitioning handles this automatically',
      'All tenants experience equal performance regardless of data volume',
      'The large tenant\'s data is rejected once a partition reaches capacity',
      'The large tenant\'s data spans many partitions while small tenants share partitions; the range containing the large tenant\'s boundary may become a hot spot during sequential writes',
    ],
    correctAnswer: 3,
    explanation: 'With key-range partitioning on (tenant_id, entity_id), the large tenant\'s data is in a contiguous range that may span multiple partitions. However, if entity_ids are sequential, new writes still concentrate on the latest partition boundary. Small tenants may share partitions, and the large tenant dominates certain partitions, creating potential skew.',
    tags: ['key-range', 'multi-tenant', 'skew', 'hot-spot'],
  },
  {
    id: 'ddia-partitioning-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'Your team chose a fixed number of 10 partitions for a database that is now 100x its initial size. What operational problem do you face?',
    options: [
      'No problem; fixed partitioning handles any data size equally well',
      'The database refuses to accept data once partitions exceed their initial size',
      'Each partition is now very large, making rebalancing slow and recovery from node failure expensive due to the large amount of data per partition',
      'Fixed partitioning automatically creates new partitions when the data grows',
    ],
    correctAnswer: 2,
    explanation: 'With a fixed number of partitions, each partition grows proportionally with the dataset. If too few partitions were chosen initially, each becomes very large. Large partitions take longer to transfer during rebalancing and node recovery, and may cause memory or disk pressure. This is hard to fix without re-partitioning the entire dataset.',
    tags: ['fixed-partitions', 'sizing', 'operational-issues', 'rebalancing'],
  },
  {
    id: 'ddia-partitioning-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'An analytics platform queries a term-partitioned global secondary index for "all orders with status=pending." What happens if the index is updated asynchronously?',
    options: [
      'The query blocks until all pending index updates are complete',
      'Asynchronous updates are only used for writes, not for index updates',
      'The query may miss recently created orders whose index entries have not yet propagated to the index partition',
      'The query will always return perfectly up-to-date results',
    ],
    correctAnswer: 2,
    explanation: 'With asynchronous global index updates, there is a lag between a document being written and its index entry appearing in the term-partitioned index. A query for "status=pending" may miss orders that were very recently created or updated because their index entries have not yet been applied.',
    tags: ['global-index', 'async-update', 'staleness', 'eventual-consistency'],
  },
  {
    id: 'ddia-partitioning-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'You deploy a routing tier for your partitioned database. During a rebalancing operation, the routing tier briefly has stale routing information. What is the likely outcome for client requests?',
    options: [
      'Requests may be sent to the wrong node, which can either forward the request to the correct node or return an error, causing a brief latency increase',
      'Data corruption occurs on the wrongly contacted node',
      'The routing tier automatically detects the stale information and halts all requests',
      'All requests fail with a timeout error',
    ],
    correctAnswer: 0,
    explanation: 'During routing table staleness, some requests go to the old partition owner. That node either forwards the request to the new owner (adding latency) or returns a "not responsible" error so the client retries. Well-designed systems handle this gracefully with brief latency increases rather than failures.',
    tags: ['routing-tier', 'stale-routing', 'rebalancing', 'latency'],
  },
  {
    id: 'ddia-partitioning-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'Your social media platform has a celebrity user whose posts generate 10x the write traffic of other users. All their data is hash-partitioned by user_id, landing on one partition. Based on DDIA, what can you do?',
    options: [
      'Hash partitioning guarantees uniform distribution, so this cannot happen',
      'Add a random number suffix to the celebrity\'s key to split their writes across multiple partitions, at the cost of needing to merge reads from all those partitions',
      'Move the celebrity to a dedicated database',
      'Increase the hash function strength',
    ],
    correctAnswer: 1,
    explanation: 'DDIA suggests appending a random number (e.g., 00-99) to hot keys, distributing their writes across 100 partitions. The trade-off is that reads must now query all 100 partitions and merge results. This technique requires the application to know which keys are hot.',
    tags: ['hash-partitioning', 'hot-key', 'key-splitting', 'applied'],
  },
  {
    id: 'ddia-partitioning-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'Your e-commerce platform uses document-partitioned secondary indexes. A search for "red shoes" triggers scatter/gather across 200 partitions. The p99 latency is 2 seconds because of one slow partition. Based on DDIA, how would you improve this?',
    options: [
      'Switch to a term-partitioned global secondary index for product attributes so "red shoes" queries hit only the partitions that index "red" and "shoes" terms, not all 200',
      'Add more replicas to each partition',
      'Cache all search results permanently',
      'Remove the secondary index and scan the data directly',
    ],
    correctAnswer: 0,
    explanation: 'DDIA explains that term-partitioned (global) indexes avoid scatter/gather by co-locating all index entries for a term on one partition. A search for "red shoes" would hit at most the partition(s) responsible for those terms. The trade-off is more complex writes (updating the global index) and potential staleness from async updates.',
    tags: ['secondary-index', 'global-index', 'scatter-gather', 'applied'],
  },
  {
    id: 'ddia-partitioning-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'Your cluster uses fixed-number partitioning with 1000 partitions across 10 nodes. You need to add 5 new nodes. How does rebalancing work, and how much data moves?',
    options: [
      'All data is redistributed evenly — 100% of data moves',
      'Approximately 1/3 of partitions (333) are moved to the new nodes so each of the 15 nodes has ~67 partitions; only the data in those 333 partitions moves',
      'New empty partitions are created on the new nodes',
      'The existing 1000 partitions are split into 1500 partitions',
    ],
    correctAnswer: 1,
    explanation: 'With fixed partitioning, adding 5 nodes to 10 means going from 100 partitions/node to ~67 partitions/node. About 333 partitions are moved from existing nodes to new ones, moving roughly 1/3 of the data. No partitions are created or split — they are simply reassigned. This is the efficiency advantage of fixed partitioning.',
    tags: ['rebalancing', 'fixed-partitions', 'data-movement', 'applied'],
  },
  {
    id: 'ddia-partitioning-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'You are building an IoT platform that ingests sensor data with timestamps. Using key-range partitioning with timestamp as the key causes write hot spots. Based on DDIA, how do you fix this?',
    options: [
      'Switch to hash partitioning by timestamp',
      'Use a compound key like (sensor_id, timestamp) so writes from different sensors go to different partitions while maintaining time-ordered data within each sensor\'s partition',
      'Buffer all writes and flush them in batch every hour',
      'Use a single partition for all sensor data',
    ],
    correctAnswer: 1,
    explanation: 'DDIA recommends prefixing the timestamp with something that distributes writes, like the sensor name. With key (sensor_id, timestamp), writes from different sensors go to different partitions (avoiding hot spots) while data for each sensor remains sorted by time (enabling efficient time-range queries per sensor).',
    tags: ['key-range', 'compound-key', 'iot', 'hot-spot', 'applied'],
  },
];

export default questions;
