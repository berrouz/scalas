import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-partitioning-l4-001',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'In a key-range partitioned time-series database with the key format (sensor_id, timestamp), a single sensor generates 10x the data of others. The partition for that sensor\'s key range is a hot spot. What is the most effective DDIA-aligned approach?',
    options: [
      'Increase the replication factor for that partition only',
      'Add a random suffix to the sensor_id to split its data across multiple partitions, accepting the read fan-out trade-off',
      'Move that sensor\'s data to a completely separate database',
      'Switch the entire database to hash partitioning',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes appending a random number to hot keys to spread load across multiple partitions. For this sensor, using keys like (sensor_42_00, timestamp) through (sensor_42_99, timestamp) distributes writes to ~100 different partitions. Reads must fan out to all 100 keys, but this trade-off is worthwhile for write-heavy hot keys.',
    tags: ['key-range', 'hot-key', 'random-suffix', 'write-amplification'],
  },
  {
    id: 'ddia-partitioning-l4-002',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'A distributed database uses key-range partitioning. An operator notices that partition boundaries have drifted such that 3 out of 20 partitions hold 70% of the data. What does this indicate about the boundary selection algorithm?',
    options: [
      'The system likely uses static boundaries that do not adapt to changing data distribution, or the split/merge thresholds are misconfigured',
      'The replication factor is too high',
      'This is expected behavior and requires no action',
      'The hash function has too many collisions',
    ],
    correctAnswer: 0,
    explanation: 'Severe data skew across key-range partitions indicates that partition boundaries are not adapting to the actual data distribution. Either the boundaries were set statically and the data distribution changed, or the dynamic splitting mechanism has thresholds that are too high, allowing partitions to grow very large before splitting.',
    tags: ['key-range', 'boundary-selection', 'data-skew', 'split-threshold'],
  },
  {
    id: 'ddia-partitioning-l4-003',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'A system uses hash partitioning with consistent hashing (virtual nodes). Each physical node has 256 virtual nodes (tokens). What is the primary benefit of using many virtual nodes per physical node?',
    options: [
      'It reduces the total number of partitions in the system',
      'It makes the data distribution more uniform and ensures that when a node is added or removed, load is transferred evenly to/from many other nodes',
      'It eliminates the need for a coordination service',
      'It allows range queries to be processed locally',
    ],
    correctAnswer: 1,
    explanation: 'More virtual nodes (tokens) per physical node means smaller, more numerous partition ranges. This produces a more uniform distribution of data. When a node joins or leaves, its virtual nodes are scattered across the hash ring, so the data transfer is spread across many other nodes rather than burdening a single neighbor.',
    tags: ['consistent-hashing', 'virtual-nodes', 'tokens', 'uniform-distribution'],
  },
  {
    id: 'ddia-partitioning-l4-004',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'A team is deciding between MD5, Murmur3, and a cryptographic hash (SHA-256) for hash partitioning. What does DDIA suggest about hash function selection?',
    options: [
      'Always use a cryptographic hash for security',
      'Any hash function works identically; the choice does not matter',
      'Only MD5 is suitable because it produces 128-bit hashes',
      'The hash function need not be cryptographically strong; it just needs to produce a sufficiently uniform distribution. Non-cryptographic hashes like Murmur3 are preferred for performance',
    ],
    correctAnswer: 3,
    explanation: 'DDIA states that the hash function used for partitioning does not need to be cryptographically strong. The key requirement is uniform distribution. Non-cryptographic hash functions like Murmur3 are faster and produce sufficiently uniform distributions for partitioning. Cassandra and MongoDB use Murmur3 and MD5 respectively.',
    tags: ['hash-function', 'murmur3', 'md5', 'performance'],
  },
  {
    id: 'ddia-partitioning-l4-005',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'A search engine uses a term-partitioned global secondary index partitioned by hash of the indexed term. An analyst runs a range query: "find all products priced between $50 and $100." How does the index handle this?',
    options: [
      'It requires scatter/gather across all index partitions because hash-partitioned terms do not support range scans on the indexed values',
      'It performs a single-partition range scan since prices are contiguous',
      'It returns an error because global indexes do not support range queries',
      'It uses the primary key index instead of the secondary index',
    ],
    correctAnswer: 0,
    explanation: 'If the global secondary index is partitioned by hash of the term (e.g., hash of price), range queries on price cannot be served by a single index partition because adjacent price values hash to different partitions. The query degrades to scatter/gather. If the index were range-partitioned by price, a single partition could serve this query.',
    tags: ['global-index', 'hash-of-term', 'range-query', 'scatter-gather'],
  },
  {
    id: 'ddia-partitioning-l4-006',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'A database uses document-partitioned secondary indexes. An engineer proposes switching to a term-partitioned global index to speed up reads. What is the hidden cost during index construction?',
    options: [
      'There is no additional cost; index type is just a configuration change',
      'Building a global index requires reading all data across all partitions and repartitioning the index entries by term, which is a large cross-cluster data shuffle',
      'Global indexes require twice the storage of local indexes',
      'Global indexes cannot be built incrementally; the database must go offline',
    ],
    correctAnswer: 1,
    explanation: 'Constructing a term-partitioned global index means every document on every partition must have its indexed terms extracted and routed to the correct index partition (based on the term). This is a distributed shuffle operation similar to a MapReduce job, requiring significant network transfer and coordination.',
    tags: ['global-index', 'index-construction', 'data-shuffle', 'migration'],
  },
  {
    id: 'ddia-partitioning-l4-007',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'A cluster initially provisioned 1000 partitions for 10 nodes (100 per node). After 3 years, the cluster has grown to 100 nodes. What is the operational concern with the fixed partition count?',
    options: [
      'Each node now has only 10 partitions, and each partition is very large, making individual partition operations (backup, restore, rebalance) slow and resource-intensive',
      'The system runs out of partition IDs',
      'There is no concern; 10 partitions per node is the recommended configuration',
      'Having fewer partitions per node improves performance because of less metadata overhead',
    ],
    correctAnswer: 0,
    explanation: 'With 1000 fixed partitions across 100 nodes, each node holds 10 very large partitions. Individual partition operations (transfer during rebalancing, recovery after failure) are slow because each partition is massive. DDIA notes that choosing the right number of fixed partitions is difficult: it must account for future growth.',
    tags: ['fixed-partitions', 'partition-sizing', 'scalability', 'long-term'],
  },
  {
    id: 'ddia-partitioning-l4-008',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'Cassandra uses partitioning proportional to nodes with 256 tokens per node. When a new node joins, it picks random split points in existing partitions. What is a known downside of random split point selection?',
    options: [
      'Random splits may produce unevenly sized partitions, and the new node needs to transfer data by reading existing nodes\' SSTables via a compaction-like process',
      'Random splits always produce perfectly equal partitions',
      'It can only split partitions that contain more than 1 million records',
      'It requires ZooKeeper to coordinate the splits',
    ],
    correctAnswer: 0,
    explanation: 'Random split points do not account for actual data distribution within the partition being split, so the two resulting halves may be unequal in size. Additionally, the split requires the new node to fetch data from the existing owner by streaming SSTables, which is a resource-intensive compaction-like operation.',
    tags: ['cassandra', 'proportional-partitioning', 'random-splits', 'sstable-streaming'],
  },
  {
    id: 'ddia-partitioning-l4-009',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'In Cassandra\'s later versions, the "virtual nodes" approach was enhanced with a token allocation algorithm instead of random token assignment. What problem does this solve?',
    options: [
      'It achieves more even data distribution by analyzing existing token ranges and placing new tokens to minimize imbalance, rather than relying on randomness',
      'It reduces the amount of memory used by the token metadata',
      'It eliminates the need for anti-entropy repair',
      'It allows Cassandra to use key-range partitioning instead of hash partitioning',
    ],
    correctAnswer: 0,
    explanation: 'Random token assignment relies on the law of large numbers for uniformity, but in practice (especially with few nodes), it can produce significant imbalance. Token allocation algorithms deterministically place new tokens to equalize the load across nodes, resulting in better distribution than random placement.',
    tags: ['cassandra', 'token-allocation', 'virtual-nodes', 'load-balancing'],
  },
  {
    id: 'ddia-partitioning-l4-010',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'A system uses ZooKeeper for partition routing. The ZooKeeper ensemble has 3 nodes. What happens if 2 ZooKeeper nodes fail simultaneously?',
    options: [
      'ZooKeeper elects the remaining node as the new leader and continues normally',
      'ZooKeeper loses its quorum and becomes read-only or unavailable; partition reassignment stops but existing cached routing may still work in the database layer',
      'ZooKeeper continues with reduced performance on the remaining node',
      'The database automatically switches to gossip-based routing',
    ],
    correctAnswer: 1,
    explanation: 'ZooKeeper requires a majority quorum (2 of 3) to operate. With 2 nodes down, the remaining node cannot form a quorum, so ZooKeeper stops accepting writes (partition reassignments). The database nodes and routing tiers can continue operating with their cached partition maps, but no changes can be made until ZooKeeper recovers.',
    tags: ['zookeeper', 'quorum', 'availability', 'failure-scenario'],
  },
  {
    id: 'ddia-partitioning-l4-011',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'LinkedIn\'s Espresso uses Helix (built on ZooKeeper) for partition management. What is the advantage of a dedicated partition management layer like Helix over embedding routing logic in the database itself?',
    options: [
      'Helix eliminates the need for any network communication between database nodes',
      'Helix reduces storage costs by compressing partition metadata',
      'It separates concerns: Helix handles partition assignment, rebalancing, and failure detection as a reusable component, while the database focuses on data storage and queries',
      'Helix provides stronger consistency than database-embedded routing',
    ],
    correctAnswer: 2,
    explanation: 'Dedicated partition management systems like Helix abstract the complexity of partition assignment, node failure detection, and rebalancing into a reusable component. This separation of concerns means the database does not need to implement its own consensus and coordination logic, and the partition management layer can be used across multiple systems.',
    tags: ['helix', 'espresso', 'partition-management', 'separation-of-concerns'],
  },
  {
    id: 'ddia-partitioning-l4-012',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'A system uses hash partitioning and wants to support multi-dimensional range queries (e.g., latitude AND longitude). Why is standard hash partitioning insufficient, and what does DDIA suggest as alternatives?',
    options: [
      'Hash partitioning destroys ordering on all dimensions; DDIA mentions multi-dimensional indexes like R-trees or space-filling curves (e.g., Z-order/Hilbert curves) as approaches to convert multi-dimensional data to a single key',
      'Hash partitioning works fine for multi-dimensional queries as long as you hash both dimensions together',
      'Use separate hash partitions for each dimension and join the results',
      'Multi-dimensional queries are impossible in any distributed database',
    ],
    correctAnswer: 0,
    explanation: 'Standard hash partitioning destroys the ordering needed for range queries on any dimension. DDIA discusses the challenge of multi-dimensional queries and mentions space-filling curves (like Z-order curves) that map multi-dimensional data to a single dimension while preserving some locality, enabling range queries on a single partitioned index.',
    tags: ['multi-dimensional', 'space-filling-curves', 'r-tree', 'geospatial'],
  },
  {
    id: 'ddia-partitioning-l4-013',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'A DynamoDB table uses local secondary indexes (document-partitioned). An engineer discovers that scatter/gather queries across 500 partitions are causing timeouts. What architectural change would DDIA suggest to improve read performance for a specific query pattern?',
    options: [
      'Create a term-partitioned (global) secondary index for that specific query pattern, trading write complexity for efficient single-partition reads',
      'Increase the timeout and accept slow queries',
      'Add more read replicas for each partition',
      'Reduce the number of partitions to 50 to reduce scatter/gather overhead',
    ],
    correctAnswer: 0,
    explanation: 'For a specific query pattern that suffers from scatter/gather across many partitions, a term-partitioned (global) secondary index on the queried field can serve reads from a single index partition. The trade-off is that writes become more complex (cross-partition index updates) and the index may be slightly stale due to async updates.',
    tags: ['global-index', 'dynamodb', 'scatter-gather', 'query-optimization'],
  },
  {
    id: 'ddia-partitioning-l4-014',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'An operator must rebalance a 10 TB partition to a new node. During the transfer, the source node continues to accept writes. What consistency challenge arises during partition handoff?',
    options: [
      'No challenge; writes are simply forwarded to the new node immediately',
      'The old and new nodes permanently share responsibility for the partition',
      'Writes are blocked for the entire duration of the transfer',
      'Writes to the partition during transfer must be tracked and applied to the new node after the bulk transfer completes, similar to how database replication catches up from a snapshot',
    ],
    correctAnswer: 3,
    explanation: 'During partition transfer, the source continues serving writes. The new node receives a snapshot (bulk copy), but writes that arrive during this copy are not in the snapshot. These concurrent writes must be captured (e.g., via a change log) and replayed on the new node after the bulk transfer, similar to replication log catchup from a consistent snapshot.',
    tags: ['partition-handoff', 'rebalancing', 'write-during-transfer', 'change-log'],
  },
  {
    id: 'ddia-partitioning-l4-015',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Key-Range',
    question: 'A database stores data with composite keys (country, city, timestamp). It uses key-range partitioning. An analyst wants to query "all entries for Germany in 2024." Another wants "all entries worldwide for January 2024." How do these queries differ in partition access?',
    options: [
      'The Germany query hits only partitions in the (Germany, ...) key range (few partitions); the worldwide-January query must scan partitions for every country (effectively all partitions)',
      'Both queries access the same set of partitions',
      'The worldwide query is faster because it can skip country-based boundaries',
      'Neither query can be served without a full table scan',
    ],
    correctAnswer: 0,
    explanation: 'With key-range partitioning on (country, city, timestamp), keys for Germany are contiguous and likely in a few partitions. A query for Germany in 2024 is a targeted range scan. But a query for January 2024 across all countries must access every country\'s key range separately, touching most or all partitions. The key hierarchy determines which query patterns are efficient.',
    tags: ['composite-key', 'key-range', 'query-pattern', 'key-hierarchy'],
  },
  {
    id: 'ddia-partitioning-l4-016',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'A distributed database initially uses ZooKeeper for routing but is considering migrating to a gossip protocol. What are the implications of this architectural change?',
    options: [
      'Gossip is strictly superior to ZooKeeper in all scenarios',
      'Gossip removes the external dependency on ZooKeeper but convergence is eventually consistent, meaning routing information may temporarily lag behind actual partition state during changes',
      'Gossip requires more nodes than ZooKeeper and is always slower',
      'The change has no practical impact because both approaches provide identical guarantees',
    ],
    correctAnswer: 1,
    explanation: 'Moving from ZooKeeper to gossip eliminates a critical external dependency and single point of coordination. However, gossip-based routing is eventually consistent: after a partition reassignment, it takes time for all nodes to learn the new mapping. ZooKeeper provides stronger consistency for metadata changes but introduces an external dependency.',
    tags: ['zookeeper-vs-gossip', 'eventual-consistency', 'routing-convergence', 'architecture'],
  },
  {
    id: 'ddia-partitioning-l4-017',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'A team runs a fixed-partition cluster with 10,000 partitions. They observe that partition metadata management itself is causing significant overhead (memory usage on every node, ZooKeeper watch pressure). What is the root cause and how should they address it?',
    options: [
      'Too many partitions relative to data volume creates metadata overhead; each partition has metadata that must be tracked. They should reduce the partition count, which requires a full re-partitioning migration',
      'The problem is caused by too few partitions; they should increase to 100,000',
      'The hash function is too slow; switch to a faster one',
      'Metadata overhead is unavoidable and cannot be reduced',
    ],
    correctAnswer: 0,
    explanation: 'Each partition carries metadata overhead: routing entries, ZooKeeper znodes/watches, leader election state, replication tracking, etc. With 10,000 partitions, this overhead becomes significant. DDIA warns that choosing the right number of fixed partitions is hard: too few means large partitions, too many means excessive metadata overhead. The only fix is re-partitioning with fewer partitions.',
    tags: ['fixed-partitions', 'metadata-overhead', 'zookeeper', 'partition-count'],
  },
  {
    id: 'ddia-partitioning-l4-018',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Secondary Indexes',
    question: 'DDIA describes the fundamental tension between document-partitioned and term-partitioned secondary indexes. What is the formal read/write trade-off?',
    options: [
      'Both types have identical performance characteristics',
      'Document-partitioned indexes are efficient for writes (index update is local) but expensive for reads (scatter/gather); term-partitioned indexes are expensive for writes (cross-partition update) but efficient for reads (single partition)',
      'Document-partitioned indexes are always superior',
      'The trade-off only applies to graph databases',
    ],
    correctAnswer: 1,
    explanation: 'DDIA frames this as a fundamental trade-off: document-partitioned (local) indexes co-locate the index with the data, making writes simple but reads expensive (query all partitions). Term-partitioned (global) indexes make reads efficient (one partition per term) but writes must update potentially remote partitions, adding latency and complexity.',
    tags: ['secondary-index', 'read-write-tradeoff', 'local-vs-global', 'expert'],
  },
  {
    id: 'ddia-partitioning-l4-019',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Rebalancing',
    question: 'DDIA discusses consistent hashing but notes that in practice, databases rarely use it in its original form. Why?',
    options: [
      'Consistent hashing is patented and cannot be used freely',
      'The original consistent hashing algorithm produces uneven load distribution (some nodes get much more data than others); databases instead use hash partitioning with fixed-size virtual partitions assigned to nodes',
      'Consistent hashing does not work with non-string keys',
      'Consistent hashing requires too much memory',
    ],
    correctAnswer: 1,
    explanation: 'DDIA clarifies that the original consistent hashing algorithm (hash the key and walk clockwise to the next node on the ring) produces uneven partition sizes. Real systems use a fixed number of "virtual nodes" (partitions) that are assigned to physical nodes, giving more control over load distribution while maintaining the property that adding/removing nodes moves minimal data.',
    tags: ['consistent-hashing', 'virtual-nodes', 'load-distribution', 'expert'],
  },
  {
    id: 'ddia-partitioning-l4-020',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Hash Partitioning',
    question: 'DDIA notes that handling hot keys (like celebrity user IDs) requires application-level workarounds. Why can databases not solve this automatically?',
    options: [
      'Databases are not designed to handle popular keys',
      'The database cannot distinguish between a legitimately popular key and a temporarily hot key without application-level knowledge about access patterns; splitting a key arbitrarily could break application semantics',
      'It is technically impossible to detect hot keys',
      'Databases always distribute keys perfectly with hash functions',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that hot key handling requires application knowledge: the database does not know which keys are hot, whether hotness is temporary, or how to split/merge reads for a hot key without breaking application logic. Techniques like adding random suffixes require the application to track which keys are hot and handle the read-time merging.',
    tags: ['hot-keys', 'application-level', 'database-limitations', 'expert'],
  },
  {
    id: 'ddia-partitioning-l4-021',
    language: 'ddia',
    level: 'level4',
    category: 'Partitioning',
    subcategory: 'Request Routing',
    question: 'DDIA describes how partition-aware clients (like the Cassandra driver) maintain a local copy of the partition map. What is the consistency challenge of this approach?',
    options: [
      'There is no consistency challenge since the partition map never changes',
      'Every client must have the exact same partition map at all times, but gossip propagation and client cache invalidation introduce a window where different clients have different views of partition ownership, potentially causing temporary routing errors or stale reads',
      'Partition-aware clients always route correctly',
      'The challenge is only about memory usage on the client',
    ],
    correctAnswer: 1,
    explanation: 'DDIA acknowledges that partition-aware routing distributes the routing state to every client, creating a distributed consensus problem. During partition reassignment, different clients may have different versions of the partition map, leading to requests going to the wrong node. Well-designed systems handle this with version numbers and automatic retry/redirect.',
    tags: ['request-routing', 'partition-aware', 'consistency', 'gossip', 'expert'],
  },
];

export default questions;
