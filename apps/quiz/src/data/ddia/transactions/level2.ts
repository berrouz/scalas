import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-transactions-l2-001',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'Why does DDIA argue that "Consistency" is the odd one out in ACID?',
    options: ['Because consistency is only relevant to distributed systems', 'Because consistency is a property of the application, not the database itself', 'Because consistency conflicts with durability', 'Because consistency is already implied by atomicity'],
    correctAnswer: 1,
    explanation: 'Kleppmann argues that consistency (in the ACID sense of preserving invariants) is a property of the application. The application defines what invariants must hold, and the database merely provides tools like atomicity and isolation to help achieve that. The "C" doesn\'t really belong in ACID.',
    tags: ['acid', 'consistency', 'application-invariants'],
  },
  {
    id: 'ddia-transactions-l2-002',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'How does read committed typically prevent dirty reads without holding long-lived read locks?',
    options: ['By using write-ahead logging', 'By returning the old committed value for any object that is currently being written by an uncommitted transaction', 'By aborting transactions that attempt to read uncommitted data', 'By buffering all reads until no writes are in progress'],
    correctAnswer: 1,
    explanation: 'For every object that is being written, the database remembers both the old committed value and the new value set by the currently holding write lock. While the transaction is ongoing, any other transactions that read the object are given the old value. Only after the new value is committed do transactions start reading the new value.',
    tags: ['read-committed', 'dirty-reads', 'implementation'],
  },
  {
    id: 'ddia-transactions-l2-003',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'In MVCC-based snapshot isolation, how does a transaction determine which version of an object to read?',
    options: ['It always reads the latest version', 'It reads the version created by the most recent transaction that committed before the reading transaction began', 'It reads the version with the highest transaction ID', 'It reads the version with the lowest transaction ID'],
    correctAnswer: 1,
    explanation: 'Under snapshot isolation with MVCC, a transaction reads the version of each object that was created by the most recent committed transaction at the time the reading transaction started. Versions created by transactions that started after the reader, or that have not yet committed, are invisible.',
    tags: ['mvcc', 'snapshot-isolation', 'visibility-rules'],
  },
  {
    id: 'ddia-transactions-l2-004',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'What is "write skew" and why doesn\'t snapshot isolation prevent it?',
    options: ['When a write operation is slower than expected due to disk latency', 'When two transactions write to the same row simultaneously, causing data corruption', 'When data is written to the wrong partition', 'When two transactions read the same data, make decisions based on it, then write to different objects, violating an invariant'],
    correctAnswer: 3,
    explanation: 'Write skew occurs when two transactions read the same set of rows, check some condition, and then each updates different rows in a way that violates the condition. Snapshot isolation doesn\'t prevent this because neither transaction writes to a row the other writes to, so there is no write-write conflict to detect.',
    tags: ['write-skew', 'snapshot-isolation', 'anomaly'],
  },
  {
    id: 'ddia-transactions-l2-005',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'What is a "phantom" in the context of transaction isolation?',
    options: ['A deleted row that still takes up storage space', 'A lock that is held but never released', 'A transaction that appears committed but was actually aborted', 'A situation where a search query returns different results because another transaction inserted or deleted matching rows'],
    correctAnswer: 3,
    explanation: 'A phantom occurs when a transaction\'s search query matches a different set of rows at different points in time because another transaction inserted, deleted, or updated rows that affect the search condition. Phantoms are a more general form of write skew where the conflict is on a set of rows matching a condition.',
    tags: ['phantom', 'snapshot-isolation', 'anomaly'],
  },
  {
    id: 'ddia-transactions-l2-006',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'Why does two-phase locking have significantly worse performance than snapshot isolation?',
    options: ['Because 2PL requires more memory for storing multiple versions', 'Because 2PL requires network round-trips between nodes', 'Because readers block writers and writers block readers, greatly reducing concurrency', 'Because 2PL requires more disk I/O'],
    correctAnswer: 2,
    explanation: 'The big downside of 2PL is performance. Unlike snapshot isolation where readers never block writers and writers never block readers, in 2PL any transaction that wants to read must wait for writers, and any transaction that wants to write must wait for readers. This drastically reduces the amount of concurrency and throughput.',
    tags: ['2pl', 'performance', 'concurrency'],
  },
  {
    id: 'ddia-transactions-l2-007',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'What is a predicate lock, and what problem does it solve?',
    options: ['A lock that matches all objects satisfying a search condition; it prevents phantoms', 'A lock on a database schema to prevent DDL changes; it solves schema conflicts', 'A lock on a specific row to prevent updates; it solves dirty writes', 'A lock on an index entry to prevent duplicate inserts; it solves uniqueness violations'],
    correctAnswer: 0,
    explanation: 'A predicate lock locks all objects (even those that do not yet exist) matching a search condition. This prevents phantoms: if transaction A reads objects matching some condition and transaction B wants to insert/update/delete an object matching that condition, B must wait. However, predicate locks are expensive to check.',
    tags: ['predicate-lock', '2pl', 'phantoms'],
  },
  {
    id: 'ddia-transactions-l2-008',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'How do index-range locks (next-key locks) approximate predicate locks?',
    options: ['They lock all indexes in the database during a transaction', 'They attach a lock to an index entry or a range of index entries that cover the predicate', 'They only lock the primary key index, ignoring secondary indexes', 'They lock the entire table instead of individual rows'],
    correctAnswer: 1,
    explanation: 'Index-range locks simplify predicate locks by locking a broader range in the index. For example, if a predicate searches for bookings of room 123 between noon and 1pm, an index-range lock might lock all bookings of room 123 (or all bookings at noon). This is less precise but much cheaper to implement than full predicate locks.',
    tags: ['index-range-lock', 'next-key-lock', '2pl'],
  },
  {
    id: 'ddia-transactions-l2-009',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'How does SSI differ from 2PL in its approach to concurrency control?',
    options: ['SSI only works on single-node databases; 2PL works on distributed databases', 'SSI uses pessimistic concurrency control; 2PL uses optimistic', 'SSI is optimistic (allows transactions to proceed and checks at commit); 2PL is pessimistic (blocks transactions upfront)', 'SSI requires manual conflict resolution; 2PL resolves conflicts automatically'],
    correctAnswer: 2,
    explanation: 'SSI is based on optimistic concurrency control. Instead of blocking when something potentially dangerous happens, transactions continue executing in the hope that everything will turn out all right. When a transaction wants to commit, the database checks whether anything bad happened; if so, the transaction is aborted and retried. 2PL is pessimistic: it blocks upfront.',
    tags: ['ssi', '2pl', 'optimistic-vs-pessimistic'],
  },
  {
    id: 'ddia-transactions-l2-010',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'What are the two main situations SSI must detect to prevent serialization anomalies?',
    options: ['Detecting reads of stale data (MVCC visibility) and detecting writes that affect prior reads', 'Deadlocks and livelocks', 'Read timeouts and write timeouts', 'Lock escalation and lock contention'],
    correctAnswer: 0,
    explanation: 'SSI detects two situations: (1) detecting reads of a stale MVCC object version (another transaction has written a new version since the reader\'s snapshot) and (2) detecting writes that affect prior reads (another transaction modifies data after it was read by the current transaction). If either is detected at commit time, the transaction is aborted.',
    tags: ['ssi', 'conflict-detection', 'stale-reads'],
  },
  {
    id: 'ddia-transactions-l2-011',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'What made actual serial execution feasible as a practical approach for serializability?',
    options: ['Networks became fast enough to handle distributed locks', 'CPUs became fast enough to execute any workload single-threaded', 'The invention of solid-state drives', 'RAM became cheap enough to keep entire datasets in memory, and OLTP transactions are short'],
    correctAnswer: 3,
    explanation: 'Two developments made serial execution practical: (1) RAM became cheap enough to keep the active dataset entirely in memory, making transactions fast since they avoid disk I/O, and (2) database designers recognized that OLTP transactions are usually short and make a small number of reads and writes, unlike OLAP queries.',
    tags: ['serial-execution', 'in-memory', 'oltp'],
  },
  {
    id: 'ddia-transactions-l2-012',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'Why are stored procedures important for actual serial execution?',
    options: ['They encrypt transaction data for security', 'They allow the entire transaction to be submitted as a single unit, avoiding interactive multi-statement round trips', 'They automatically parallelize transactions across CPU cores', 'They provide automatic retry logic for failed transactions'],
    correctAnswer: 1,
    explanation: 'In serial execution, interactive multi-statement transactions are problematic because the database would be idle while waiting for the application to issue the next query. Stored procedures allow the entire transaction logic to be submitted to the database upfront, so it can be executed without waiting for network round trips.',
    tags: ['stored-procedures', 'serial-execution', 'latency'],
  },
  {
    id: 'ddia-transactions-l2-013',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'Why is "repeatable read" considered a confusing isolation level name?',
    options: ['Because it has nothing to do with reads', 'Because different databases use the name to mean very different things — some actually provide snapshot isolation under this name', 'Because it was deprecated in the SQL standard', 'Because it only applies to NoSQL databases'],
    correctAnswer: 1,
    explanation: 'The SQL standard\'s definition of repeatable read is ambiguous. Several databases (notably PostgreSQL and MySQL) call their implementation "repeatable read" but actually provide snapshot isolation, which is stronger in some ways. There is no standard definition that everyone agrees on, making the terminology confusing.',
    tags: ['repeatable-read', 'snapshot-isolation', 'sql-standard'],
  },
  {
    id: 'ddia-transactions-l2-014',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'Which MVCC visibility rule ensures a transaction never sees data from concurrent or future transactions?',
    options: ['A version is visible only if it was created by a transaction that had committed before the reader\'s transaction started', 'A version is visible if it was created within the last 5 seconds', 'A version is visible only if no other transaction is currently reading it', 'A version is visible if it was created by a transaction with a lower ID'],
    correctAnswer: 0,
    explanation: 'The MVCC visibility rule states that an object version is visible to a transaction if: (1) the transaction that created it had already committed before the reader started, (2) the object has not been marked for deletion, or if it has, the deleting transaction had not committed when the reader started.',
    tags: ['mvcc', 'visibility-rules', 'snapshot-isolation'],
  },
  {
    id: 'ddia-transactions-l2-015',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'How does atomicity in ACID relate to concurrency?',
    options: ['Atomicity is not about concurrency — it is about ensuring all-or-nothing on abort; isolation handles concurrency', 'Atomicity guarantees that concurrent transactions always succeed', 'Atomicity prevents two transactions from running at the same time', 'Atomicity ensures transactions see each other\'s intermediate states'],
    correctAnswer: 0,
    explanation: 'Despite the name suggesting "indivisible," ACID atomicity is not about concurrency (that is isolation). Atomicity describes what happens if a client wants to make several writes but a fault occurs partway through: the transaction is aborted and all writes are rolled back so the application can safely retry.',
    tags: ['atomicity', 'abort', 'concurrency'],
  },
  {
    id: 'ddia-transactions-l2-016',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'In which scenario is read committed isolation insufficient, making snapshot isolation necessary?',
    options: ['When running backups or analytic queries that need to see a consistent state across many rows', 'When transactions need to read only a single row', 'When the database has only a single table', 'When all transactions are write-only'],
    correctAnswer: 0,
    explanation: 'Read committed allows non-repeatable reads, meaning a long-running query like a backup or analytical report may see some parts of the database at an earlier point in time and other parts at a later point. Snapshot isolation provides a consistent snapshot, ensuring the entire query sees data from one point in time.',
    tags: ['read-committed', 'snapshot-isolation', 'backups', 'analytics'],
  },
  {
    id: 'ddia-transactions-l2-017',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'How are deadlocks handled under two-phase locking?',
    options: ['They cannot occur because locks are always acquired in a fixed order', 'The database automatically detects deadlocks and aborts one of the transactions', 'Deadlocks are prevented by using timeouts only', 'Deadlocks are resolved by the application, not the database'],
    correctAnswer: 1,
    explanation: 'Deadlocks can easily occur in 2PL when transaction A waits for transaction B\'s lock and B waits for A\'s lock. The database automatically detects these cycles in the waits-for graph and aborts one of the transactions to break the deadlock. The aborted transaction is then retried by the application.',
    tags: ['deadlock', '2pl', 'abort-retry'],
  },
  {
    id: 'ddia-transactions-l2-018',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'What are the three approaches to implementing serializable isolation that DDIA describes?',
    options: [
      'Locking, timestamping, and validation',
      'Actual serial execution, two-phase locking (2PL), and serializable snapshot isolation (SSI)',
      'Optimistic, pessimistic, and semi-optimistic concurrency control',
      'Row-level, table-level, and database-level serialization',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes three approaches: (1) Actual serial execution — literally running transactions one at a time on a single thread. (2) Two-phase locking (2PL) — using shared and exclusive locks. (3) Serializable snapshot isolation (SSI) — an optimistic approach that runs transactions concurrently and checks for conflicts at commit time.',
    tags: ['serializability', 'serial-execution', '2pl', 'ssi'],
  },
  {
    id: 'ddia-transactions-l2-019',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'How does actual serial execution (single-threaded) achieve serializable isolation?',
    options: [
      'By using sophisticated locking protocols',
      'By literally executing transactions one at a time on a single CPU core, eliminating all concurrency and therefore all concurrency bugs',
      'By running transactions in parallel but serializing the results',
      'By using optimistic concurrency control',
    ],
    correctAnswer: 1,
    explanation: 'Actual serial execution simply runs one transaction at a time on a single thread. With no concurrency, there are no concurrency issues. This became viable with in-memory databases (transactions complete in microseconds) and the realization that many OLTP transactions are short. Used by VoltDB, Redis, and Datomic.',
    tags: ['serial-execution', 'single-threaded', 'voltdb'],
  },
  {
    id: 'ddia-transactions-l2-020',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Write Skew',
    question: 'What is write skew and how does it differ from a lost update?',
    options: [
      'Write skew and lost update are the same thing',
      'Write skew occurs when two transactions read overlapping data, make decisions based on what they read, and then write to different objects — the combination of writes violates an invariant that neither write alone would violate',
      'Write skew only happens with dirty writes',
      'Write skew is exclusive to NoSQL databases',
    ],
    correctAnswer: 1,
    explanation: 'Write skew is a generalization of lost update: instead of two transactions writing to the same object, they write to different objects based on a shared read condition. The classic example is two doctors both checking if they are the last one on call, both deciding they can go off duty, and both going off — leaving nobody on call.',
    tags: ['write-skew', 'lost-update', 'invariant-violation'],
  },
  {
    id: 'ddia-transactions-l2-021',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'What is the key difference between two-phase locking (2PL) and simple lock-based concurrency?',
    options: [
      'There is no difference',
      'In 2PL, writers block both other writers AND readers, and readers block writers — providing serializable isolation at the cost of reduced concurrency',
      '2PL only locks during the write phase',
      '2PL uses optimistic locking without blocking',
    ],
    correctAnswer: 1,
    explanation: 'In 2PL, the critical difference from simpler schemes is that readers block writers (not just the reverse). If transaction A has a shared read lock on an object, transaction B cannot write to it until A releases the lock. This provides serializability but significantly reduces concurrency and throughput.',
    tags: ['2pl', 'readers-block-writers', 'serializability'],
  },
  {
    id: 'ddia-transactions-l2-022',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'Is SSI (Serializable Snapshot Isolation) optimistic or pessimistic?',
    options: [
      'Pessimistic — it blocks transactions that might conflict',
      'Neither — it is a hybrid approach',
      'Optimistic — it allows transactions to proceed without blocking, then checks for serialization conflicts at commit time and aborts if necessary',
      'It depends on the workload',
    ],
    correctAnswer: 2,
    explanation: 'SSI is an optimistic concurrency control scheme. Transactions run without blocking on each other (using snapshot isolation). At commit time, the database checks whether the transaction\'s reads are still valid. If another committed transaction has modified the data, the transaction is aborted and must retry.',
    tags: ['ssi', 'optimistic', 'concurrency-control'],
  },
  {
    id: 'ddia-transactions-l2-023',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'What is a phantom read?',
    options: [
      'Reading data that was deleted',
      'A transaction searches for rows matching a condition, another transaction inserts a new row matching that condition, and the first transaction\'s result is now stale',
      'Reading data from a non-existent table',
      'A read that returns no results',
    ],
    correctAnswer: 1,
    explanation: 'A phantom read occurs when a transaction queries rows matching a condition (e.g., SELECT WHERE ...), another transaction inserts or deletes rows that match that condition, and the first transaction\'s query result becomes stale. Phantoms are especially problematic because you cannot lock rows that do not yet exist.',
    tags: ['phantom', 'isolation', 'range-query'],
  },
  {
    id: 'ddia-transactions-l2-024',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'MVCC',
    question: 'How does MVCC implement read committed vs snapshot isolation?',
    options: [
      'They use completely different implementations',
      'Read committed uses a fresh snapshot for each query within a transaction; snapshot isolation uses a single snapshot for the entire transaction',
      'Read committed does not use MVCC',
      'Snapshot isolation uses multiple snapshots per transaction',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that both use MVCC but differently: read committed creates a new snapshot for each individual query (so each query sees the latest committed data), while snapshot isolation uses a single snapshot taken at the start of the transaction (providing a consistent view throughout).',
    tags: ['mvcc', 'read-committed', 'snapshot-isolation', 'implementation'],
  },
  {
    id: 'ddia-transactions-l2-025',
    language: 'ddia',
    level: 'level2',
    category: 'Transactions',
    subcategory: 'Write Skew',
    question: 'What is the "materializing conflicts" technique for preventing write skew?',
    options: [
      'Converting all writes to reads',
      'Creating explicit lock objects in the database that represent the abstract condition being checked, so transactions can lock these objects to prevent concurrent conflicting writes',
      'Converting the database to a materialized view',
      'Using materialized snapshots for each transaction',
    ],
    correctAnswer: 1,
    explanation: 'DDIA describes materializing conflicts as creating rows in the database that represent abstract concepts (like time slots, meeting rooms) so transactions can lock them. For example, creating a row for each (doctor, shift) combination allows transactions to lock the relevant row before changing on-call status.',
    tags: ['write-skew', 'materializing-conflicts', 'locking'],
  },
];

export default questions;
