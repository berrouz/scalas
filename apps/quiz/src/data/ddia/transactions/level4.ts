import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-transactions-l4-001',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'In SSI, when a transaction T1 reads a set of rows and transaction T2 subsequently writes a row affecting that set, SSI records this dependency. If T2 commits first and T1 then tries to commit, T1 is aborted. But what happens if T1 commits first?',
    options: ['Both are allowed to commit since T1 committed before T2\'s write', 'T2 is allowed to commit because its write did not exist when T1 committed — the conflict is detected only if T1 commits after T2', 'The database rolls back T1 retroactively', 'T2 is aborted because T1 committed with data T2 is about to invalidate'],
    correctAnswer: 1,
    explanation: 'SSI checks for conflicts at commit time. If T1 commits before T2 writes, T1\'s read was valid at commit time. When T2 later commits, it does not need to abort because T1 has already committed with a consistent view. The conflict only matters if the transaction committing later was the one that read the now-stale data.',
    tags: ['ssi', 'commit-ordering', 'conflict-resolution'],
  },
  {
    id: 'ddia-transactions-l4-002',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'DDIA describes a pattern where write skew can be prevented under snapshot isolation by using a technique called "materializing conflicts." What does this involve?',
    options: ['Converting snapshot isolation to serializable isolation dynamically', 'Using database triggers to detect and reject conflicting writes', 'Logging all write conflicts to a separate audit table for later resolution', 'Creating a physical table of lock entries so that phantom-like conditions can be attached to concrete rows for SELECT FOR UPDATE'],
    correctAnswer: 3,
    explanation: 'Materializing conflicts is a technique where you create a table that explicitly represents the resource being contested (e.g., a row for every time slot and room combination). Transactions can then SELECT FOR UPDATE on these rows to prevent concurrent conflicting operations. This turns a phantom problem into a lock-on-existing-rows problem.',
    tags: ['materializing-conflicts', 'write-skew', 'phantom', 'workaround'],
  },
  {
    id: 'ddia-transactions-l4-003',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'In strict 2PL (as used in practice), when exactly are locks released, and how does this differ from the theoretical two-phase locking definition?',
    options: ['Locks can be released as soon as no more locks need to be acquired; strict 2PL is identical to basic 2PL', 'There is no practical difference; strict 2PL is just a marketing term', 'In strict 2PL, all exclusive locks are held until the transaction commits or aborts, whereas basic 2PL allows locks to be released in a shrinking phase before commit', 'Strict 2PL releases read locks at commit but holds write locks indefinitely'],
    correctAnswer: 2,
    explanation: 'In basic 2PL, locks can be released during the shrinking phase before the transaction commits. In strict 2PL (used in practice), all locks — especially exclusive locks — are held until the transaction ends (commit or abort). This prevents cascading aborts: if a transaction releases a lock early and another transaction reads the written value, an abort would cascade.',
    tags: ['strict-2pl', 'lock-release', 'cascading-abort'],
  },
  {
    id: 'ddia-transactions-l4-004',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'When using actual serial execution with data partitioning, cross-partition transactions require coordination. According to DDIA, approximately how much slower are cross-partition transactions compared to single-partition ones?',
    options: ['About 10x slower because they must be executed in lock-step across all partitions', 'Cross-partition transactions are not supported at all in serial execution systems', 'They are roughly the same speed if the network is fast', 'About 2x slower due to network overhead'],
    correctAnswer: 0,
    explanation: 'DDIA notes that cross-partition transactions are vastly slower than single-partition ones in serial execution systems. They require coordination across partition threads, effectively serializing across multiple partitions. VoltDB reports roughly 1,000 cross-partition writes per second versus far more for single-partition transactions, making the overhead approximately an order of magnitude.',
    tags: ['serial-execution', 'cross-partition', 'performance', 'voltdb'],
  },
  {
    id: 'ddia-transactions-l4-005',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'In PostgreSQL\'s MVCC implementation, each row version has a created_by and deleted_by transaction ID. A garbage collection process removes old versions. What determines when a version can be safely garbage-collected?',
    options: ['When the version is older than 24 hours', 'When no currently running transaction could possibly need to see that version — i.e., all transactions that started before the version was superseded have completed', 'When the row has been read at least once after being superseded', 'When the disk usage exceeds a configured threshold'],
    correctAnswer: 1,
    explanation: 'A version can be garbage-collected only when no active transaction has a snapshot old enough to need it. The database tracks the oldest active transaction ID and removes versions that are invisible to all current and future transactions. This is why long-running transactions can prevent garbage collection and cause table bloat in PostgreSQL (the "VACUUM" problem).',
    tags: ['mvcc', 'garbage-collection', 'postgresql', 'vacuum'],
  },
  {
    id: 'ddia-transactions-l4-006',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'The SQL standard defines four isolation levels based on which phenomena they prevent: dirty reads, non-repeatable reads, and phantoms. DDIA criticizes this approach. What is the main criticism?',
    options: ['The standard does not define enough phenomena — it misses lost updates, write skew, and other anomalies', 'The standard only applies to relational databases', 'The standard is too strict and prevents practical implementations', 'The phenomena are too precisely defined, leaving no room for interpretation'],
    correctAnswer: 0,
    explanation: 'DDIA points out that the SQL standard\'s definition of isolation levels is flawed because it was defined based on the old System R implementation and only considers a narrow set of anomalies. It does not account for lost updates, write skew, or phantoms caused by inserts (as opposed to updates). This has led to confusion and incompatible implementations across databases.',
    tags: ['sql-standard', 'isolation-levels', 'critique', 'anomalies'],
  },
  {
    id: 'ddia-transactions-l4-007',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'SSI was first described in a 2008 paper by Cahill et al. and later implemented in PostgreSQL 9.1. Compared to 2PL, what is the key advantage of SSI regarding read-write contention?',
    options: ['SSI allows readers to never block writers and writers to never block readers, while still providing serializable guarantees', 'SSI can detect conflicts without tracking which rows were read', 'SSI eliminates the need for any locking mechanism', 'SSI has lower memory usage because it does not store multiple versions'],
    correctAnswer: 0,
    explanation: 'Like snapshot isolation, SSI allows readers and writers to proceed without blocking each other. It achieves serializability not by blocking but by detecting conflicts at commit time and aborting conflicting transactions. This gives it much better read-write performance than 2PL, where readers and writers block each other.',
    tags: ['ssi', 'non-blocking', 'readers-writers', 'postgresql'],
  },
  {
    id: 'ddia-transactions-l4-008',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'If no suitable index exists for a predicate lock or index-range lock to attach to, what does the database fall back to under 2PL?',
    options: ['It switches to optimistic concurrency control for that transaction', 'It aborts the transaction and asks the application to create an index first', 'It falls back to a shared lock on the entire table, which is safe but kills concurrency', 'It creates a temporary index for the duration of the transaction'],
    correctAnswer: 2,
    explanation: 'If there is no suitable index for an index-range lock, the database must fall back to locking the entire table with a shared lock. This is safe because it prevents any concurrent writes to the table, but it destroys concurrency for all transactions touching that table. This is why proper indexing is critical for 2PL performance.',
    tags: ['index-range-lock', 'table-lock', '2pl', 'fallback'],
  },
  {
    id: 'ddia-transactions-l4-009',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'DDIA lists several examples of write skew. Which of the following is NOT an example of write skew as described in the book?',
    options: ['Two transactions each incrementing a counter by reading the old value and writing old+1', 'Two transactions each booking a meeting room after checking it is free', 'Two users claiming the same username when usernames must be unique', 'Two doctors going off call simultaneously when at least one must remain'],
    correctAnswer: 0,
    explanation: 'The counter increment example is a lost update, not write skew. In a lost update, both transactions write to the same object. In write skew, transactions read the same data but write to different objects. The doctors, username, and meeting room examples are all write skew or phantom variants discussed in DDIA Chapter 7.',
    tags: ['write-skew', 'lost-update', 'distinction', 'anomaly-classification'],
  },
  {
    id: 'ddia-transactions-l4-010',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'DDIA notes that the meaning of "durability" changes in different contexts. In a single-node database, durability means writing to disk. In a replicated database, what does durability typically mean?',
    options: ['Encrypting the data before storing it', 'Flushing the operating system\'s page cache to physical disk on the primary node', 'Successfully replicating the data to some number of nodes so it can survive node failures', 'Writing to at least one disk on any node'],
    correctAnswer: 2,
    explanation: 'In a replicated database, durability means that the data has been successfully copied to some number of other nodes. There is no absolute guarantee — if all disks and replicas fail simultaneously, data is lost. DDIA emphasizes that absolute durability does not exist; it is always about reducing risk through redundancy (replication, disk writes, backups).',
    tags: ['durability', 'replication', 'risk', 'no-absolute-guarantee'],
  },
  {
    id: 'ddia-transactions-l4-011',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'DDIA discusses three approaches to implementing serializable transactions: actual serial execution, 2PL, and SSI. If a workload is highly write-contended (many transactions modifying the same hot rows), which approach will suffer the most and why?',
    options: ['SSI — because high contention means almost every transaction will be aborted at commit time, leading to excessive retries', 'All three suffer equally under high contention', '2PL — because many transactions will be blocked waiting for exclusive locks on the same rows, but at least each transaction eventually completes without retrying', 'Actual serial execution — because all transactions must queue on one thread regardless of contention'],
    correctAnswer: 0,
    explanation: 'Under high write contention, SSI suffers the most because it optimistically allows transactions to execute, only to abort most of them at commit time. This leads to wasted work and high retry rates. 2PL makes transactions wait but they eventually proceed. Serial execution queues transactions but each one completes. SSI\'s optimistic approach backfires when contention is high.',
    tags: ['serializability', 'contention', 'ssi', '2pl', 'serial-execution', 'trade-offs'],
  },
  {
    id: 'ddia-transactions-l4-012',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'Some databases (like Oracle) call their snapshot isolation level "serializable" even though it does not prevent write skew. What is the practical consequence for application developers?',
    options: ['None — write skew is extremely rare in practice', 'Developers must disable snapshot isolation and use 2PL instead', 'The database automatically upgrades to true serializability when write skew is detected', 'Developers may believe they have full serializability and not implement application-level safeguards against write skew anomalies'],
    correctAnswer: 3,
    explanation: 'When a database labels snapshot isolation as "serializable," developers may trust that all concurrency anomalies are handled by the database. In reality, write skew and phantom anomalies can still occur. This mislabeling means developers fail to add application-level protections (like explicit locking or materializing conflicts) and discover bugs only under specific concurrency conditions.',
    tags: ['snapshot-isolation', 'serializable-mislabel', 'oracle', 'developer-impact'],
  },
  {
    id: 'ddia-transactions-l4-013',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'In MVCC, each transaction gets a monotonically increasing transaction ID. DDIA describes specific visibility rules using these IDs. Which combination of conditions makes an object version visible to transaction T?',
    options: ['The creating transaction started before T, regardless of whether it committed', 'The object was created in the same session as T', 'The creating transaction\'s ID is less than T\'s ID', 'The creating transaction had committed when T started, AND the object was not deleted (or was deleted by a transaction that had not yet committed when T started)'],
    correctAnswer: 3,
    explanation: 'An object version is visible to transaction T if: (1) the transaction that created the version had already committed at the time T started, (2) the version is not marked for deletion, or if it is, the transaction that requested the deletion had not yet committed at the time T started. Uncommitted or later-started transactions are always invisible.',
    tags: ['mvcc', 'visibility-rules', 'transaction-id', 'formal-definition'],
  },
  {
    id: 'ddia-transactions-l4-014',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'SSI can be implemented on top of snapshot isolation with relatively little overhead. DDIA mentions two data structures SSI maintains beyond what snapshot isolation requires. What are they?',
    options: ['A commit log and an undo log', 'A record of which transactions read which data (read dependencies) and which transactions wrote data that other transactions had read (write notifications)', 'A conflict graph and a priority queue of transactions', 'A lock table and a deadlock detector'],
    correctAnswer: 1,
    explanation: 'SSI extends snapshot isolation by tracking (1) for each object version, which transactions have read it, and (2) when a transaction writes to an object, notifying transactions that previously read older versions. At commit time, SSI checks these records to determine if a transaction\'s reads are still valid. These lightweight bookkeeping structures are what enable SSI\'s serializable guarantees.',
    tags: ['ssi', 'implementation', 'read-tracking', 'write-notification'],
  },
  {
    id: 'ddia-transactions-l4-015',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'In actual serial execution, DDIA notes that the throughput is limited to a single CPU core. Given that modern servers have many cores, why is this approach still viable for some workloads?',
    options: ['Because the operating system automatically distributes the single thread across multiple cores', 'Because serial execution uses SIMD instructions to process multiple transactions simultaneously', 'Because if the dataset fits in memory and transactions are short stored procedures, a single core can handle tens of thousands of OLTP transactions per second, which is sufficient for many applications', 'Because most databases are I/O-bound, not CPU-bound'],
    correctAnswer: 2,
    explanation: 'With the dataset in memory and transactions expressed as short stored procedures (no disk I/O, no network waits), a single CPU core can execute a surprisingly high number of transactions per second. For many OLTP workloads, this throughput is more than sufficient. The key constraint is that every transaction must be fast and self-contained.',
    tags: ['serial-execution', 'throughput', 'in-memory', 'practical-limits'],
  },
  {
    id: 'ddia-transactions-l4-016',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'DDIA notes that 2PL has been used for implementing serializability for approximately 30 years but has practical problems beyond lock contention. What additional operational risk does 2PL introduce?',
    options: ['Inability to perform backup operations while transactions are running', 'Data corruption due to lock table overflow', 'Unstable latencies due to deadlocks — a single deadlock can cause a cascade of transaction aborts and retries, creating unpredictable response times', 'Permanent blocking of all transactions if the lock manager process crashes'],
    correctAnswer: 2,
    explanation: 'Beyond basic lock contention, 2PL introduces unpredictable latency spikes. When deadlocks occur, the database aborts one transaction, which releases its locks and may cause waiting transactions to suddenly acquire locks and conflict with each other. At the application level, retrying aborted transactions adds load, potentially causing further contention. This makes 2PL latencies very hard to predict under load.',
    tags: ['2pl', 'deadlock', 'latency-spikes', 'operational-risk'],
  },
  {
    id: 'ddia-transactions-l4-017',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'DDIA discusses how retrying aborted transactions is not always safe. Which of the following is NOT a problem with automatic transaction retries that DDIA identifies?',
    options: ['If the transaction succeeded but the network failed to acknowledge it, retrying creates a duplicate transaction', 'If the transaction had side effects outside the database (e.g., sending an email), those side effects may be duplicated on retry', 'If the error is due to overload, retrying makes the problem worse', 'If the transaction reads data from a cache instead of the database, the retry will read stale data'],
    correctAnswer: 3,
    explanation: 'DDIA identifies several retry pitfalls: duplicate side effects (emails, API calls), overload amplification from retries, duplicate commits when the network loses the acknowledgment, and transient vs. permanent error distinction. Reading from cache is not discussed as a retry-specific problem — it is a general caching concern unrelated to transaction abort-and-retry semantics.',
    tags: ['retry', 'abort', 'side-effects', 'idempotency', 'overload'],
  },
  {
    id: 'ddia-transactions-l4-018',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'DDIA describes two situations that SSI must detect to prevent serialization anomalies. What are they?',
    options: [
      'Read-write conflicts and write-write conflicts',
      'Detecting reads of stale MVCC object versions (another transaction committed a write after our read) and detecting writes that affect prior reads (our write may affect the premise of another transaction\'s read)',
      'Detecting phantom reads and dirty reads',
      'Detecting deadlocks and lock timeouts',
    ],
    correctAnswer: 1,
    explanation: 'DDIA identifies two key detection mechanisms for SSI: (1) detecting reads of stale data — if another transaction updated and committed data we read, our premise is outdated. (2) Detecting that our writes may invalidate another transaction\'s reads — our commit might make another running transaction\'s reads stale. Both require abort of one transaction.',
    tags: ['ssi', 'stale-reads', 'write-affects-reads', 'detection', 'expert'],
  },
  {
    id: 'ddia-transactions-l4-019',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'DDIA describes "predicate locks" as a solution to phantom reads in 2PL. Why are they rarely used in practice, and what is the common alternative?',
    options: [
      'Predicate locks are used in all modern databases',
      'Predicate locks apply to all rows matching a condition (even those not yet existing), but checking whether new rows match arbitrary predicates is computationally expensive; index-range locks approximate predicate locks by locking a broader range of an index, which is more practical',
      'Predicate locks are faster than index-range locks',
      'There is no alternative to predicate locks',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that predicate locks are theoretically ideal but impractical because evaluating whether each new row satisfies an arbitrary predicate is too expensive. Index-range (next-key) locks are the practical alternative: they lock a range of an index, which is a superset of the rows that would match the predicate. This is less precise (may block non-conflicting transactions) but much more efficient.',
    tags: ['2pl', 'predicate-locks', 'index-range-locks', 'phantoms', 'expert'],
  },
  {
    id: 'ddia-transactions-l4-020',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'DDIA describes how VoltDB and Datomic achieve serial execution across partitions. What is their approach?',
    options: [
      'They use distributed 2PL across partitions',
      'They route each transaction to a single partition by its partition key; cross-partition transactions use a coordinator that acquires serial execution slots on each involved partition, effectively serializing the multi-partition transaction across partitions',
      'They do not support cross-partition transactions',
      'They use optimistic concurrency control across partitions',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that for single-partition transactions, serial execution is straightforward (one thread per partition). For cross-partition transactions, the system must coordinate across partitions, which adds significant overhead and partially negates the performance benefits of serial execution. VoltDB supports this but it is much slower.',
    tags: ['serial-execution', 'cross-partition', 'voltdb', 'coordination', 'expert'],
  },
  {
    id: 'ddia-transactions-l4-021',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'DDIA argues that the "C" (consistency) in ACID is fundamentally different from the other three properties. Why?',
    options: [
      'Consistency is the most important property',
      'Consistency is a property of the application (application invariants must be maintained), not the database. The database provides A, I, and D as tools, but the application must define and enforce what "consistent" means. The C does not really belong in ACID.',
      'Consistency only applies to distributed databases',
      'Consistency is automatically guaranteed by the other three properties',
    ],
    correctAnswer: 1,
    explanation: 'DDIA makes a philosophical distinction: atomicity, isolation, and durability are properties of the database itself (guaranteed by the system). Consistency, however, is a property of the application — the application defines what invariants must hold, and uses the database\'s A, I, and D guarantees to maintain them. The "C" was arguably included in ACID to make a nice acronym.',
    tags: ['acid', 'consistency', 'application-invariants', 'philosophy', 'expert'],
  },
  {
    id: 'ddia-transactions-l4-022',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'DDIA compares the performance characteristics of SSI, 2PL, and serial execution. What is the key performance trade-off between SSI and 2PL?',
    options: [
      'SSI and 2PL have identical performance',
      'SSI has higher throughput under low contention (no blocking) but wastes work under high contention (aborted transactions); 2PL has lower throughput overall (blocking) but wastes less work since conflicts are detected eagerly',
      'SSI is always faster than 2PL',
      '2PL never wastes any work',
    ],
    correctAnswer: 1,
    explanation: 'DDIA presents SSI as optimistic (good for low contention: transactions run without blocking, rarely abort) and 2PL as pessimistic (always blocks, even when no conflict would occur, but at least transactions do not waste work doing processing that will be aborted). The choice depends on the expected contention level.',
    tags: ['ssi', '2pl', 'performance-comparison', 'contention', 'expert'],
  },
  {
    id: 'ddia-transactions-l4-023',
    language: 'ddia',
    level: 'level4',
    category: 'Transactions',
    subcategory: 'Write Skew',
    question: 'DDIA presents write skew as a general pattern that encompasses several specific anomalies. What is the general structure of a write skew anomaly?',
    options: [
      'Two transactions write to the same row',
      'A transaction reads a set of rows matching a condition, makes a decision based on that read, and writes an update; another transaction concurrently does the same, and the combination of both writes violates an invariant that each individually preserves',
      'Two transactions read different rows and write to different rows with no shared data',
      'Write skew only occurs with INSERT operations, not UPDATEs',
    ],
    correctAnswer: 1,
    explanation: 'DDIA identifies the general pattern: (1) read a condition, (2) make a decision based on the read, (3) write an update. When two transactions execute this pattern concurrently on overlapping read sets, the condition checked in step 1 may no longer hold when step 3 executes. Examples include the doctor on-call, booking, and username registration scenarios.',
    tags: ['write-skew', 'general-pattern', 'read-decide-write', 'expert'],
  },
];

export default questions;
