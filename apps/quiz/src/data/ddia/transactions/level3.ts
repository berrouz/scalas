import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-transactions-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'A hospital system requires that at least one doctor is on call at all times. Two on-call doctors simultaneously request leave and each checks that the other is still on call. Both transactions commit. What anomaly occurred?',
    options: ['Lost update', 'Dirty read', 'Phantom read', 'Write skew'],
    correctAnswer: 3,
    explanation: 'This is a classic write skew example from DDIA. Both transactions read the same set of rows (on-call doctors), check the same condition (at least one on call), and then each updates a different row (their own on-call status). Snapshot isolation cannot detect this because neither transaction overwrites the other\'s write.',
    tags: ['write-skew', 'snapshot-isolation', 'scenario'],
  },
  {
    id: 'ddia-transactions-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'A meeting room booking system uses snapshot isolation. Two transactions simultaneously check that room 42 is free between 12:00-13:00 and both insert a booking. What anomaly is this, and why?',
    options: ['Dirty write — both transactions wrote to the same booking row', 'Lost update — one booking overwrote the other', 'Phantom — both transactions checked for the absence of conflicting rows, and each inserted a row that invalidates the other\'s check', 'Write skew — both transactions checked the same condition and wrote to the same row'],
    correctAnswer: 2,
    explanation: 'This is a phantom. The initial check is a search query for conflicting bookings, which returns empty. Each transaction then inserts a new row. The conflict is not on an existing row but on the appearance of new rows that match the search condition. SELECT FOR UPDATE cannot help because there is no existing row to lock.',
    tags: ['phantom', 'write-skew', 'booking-scenario'],
  },
  {
    id: 'ddia-transactions-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'A system uses actual serial execution on a single CPU core. An OLAP query that takes 30 seconds is submitted. What is the impact?',
    options: ['The OLAP query is automatically aborted to protect OLTP throughput', 'Only the OLAP query is slowed down; OLTP transactions continue normally', 'All OLTP transactions are blocked for 30 seconds because only one transaction can run at a time', 'The system switches to snapshot isolation for the OLAP query automatically'],
    correctAnswer: 2,
    explanation: 'In actual serial execution, there is only a single thread processing transactions. A long-running OLAP query would block all other transactions for its entire duration. This is why serial execution is only practical for short OLTP transactions and OLAP workloads must be handled separately, typically using a consistent snapshot.',
    tags: ['serial-execution', 'oltp', 'olap', 'throughput'],
  },
  {
    id: 'ddia-transactions-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'Under 2PL, transaction A holds a shared lock on row X and wants to upgrade it to an exclusive lock. Transaction B also holds a shared lock on row X. What happens?',
    options: ['A\'s lock is upgraded immediately since it already holds a lock', 'The database converts both shared locks to exclusive locks', 'A must wait for B to release its shared lock, and if B also tries to upgrade, a deadlock occurs', 'Both locks are automatically released and reacquired'],
    correctAnswer: 2,
    explanation: 'To upgrade from shared to exclusive, A must wait until no other transaction holds a shared lock on the same row. If B also tries to upgrade, both are waiting for each other to release their shared locks, creating a deadlock. The database must detect this and abort one of the transactions.',
    tags: ['2pl', 'lock-upgrade', 'deadlock'],
  },
  {
    id: 'ddia-transactions-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'Under SSI, transaction T1 reads rows matching a condition, then transaction T2 writes a new row that matches that condition and commits. When T1 tries to commit, what happens?',
    options: ['T1 is aborted because T2\'s write affected the result of T1\'s prior read', 'T1 commits successfully because it started before T2', 'T2 is retroactively aborted in favor of T1', 'Both transactions are aborted and must be retried'],
    correctAnswer: 0,
    explanation: 'SSI detects that T2\'s committed write affects the result set that T1 previously read. At T1\'s commit time, SSI identifies that T1\'s decisions were based on a premise (the read) that is no longer true. T1 is aborted and must be retried. This is how SSI prevents phantoms and write skew without blocking.',
    tags: ['ssi', 'conflict-detection', 'phantom', 'abort'],
  },
  {
    id: 'ddia-transactions-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'An application transfers $100 from account A to account B. Under read committed isolation, a user queries both balances during the transfer and sees the money deducted from A but not yet added to B. What anomaly is this?',
    options: ['Lost update', 'Non-repeatable read (read skew)', 'Phantom', 'Dirty read'],
    correctAnswer: 1,
    explanation: 'This is a non-repeatable read (read skew). The user sees account A after the debit has been committed but account B before the credit has been committed. The data is not inconsistent in a permanent way — it is a temporary inconsistency visible because of the timing of reads. Snapshot isolation would prevent this.',
    tags: ['read-skew', 'read-committed', 'bank-transfer'],
  },
  {
    id: 'ddia-transactions-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'A database uses actual serial execution and needs to scale to multiple CPU cores. What approach does DDIA describe for partitioning transactions across cores?',
    options: ['Data is partitioned and each partition has its own thread; cross-partition transactions require coordination', 'All reads go to one core and all writes go to another', 'Each core runs a separate database instance with shared disk', 'Transactions are randomly assigned to cores using a load balancer'],
    correctAnswer: 0,
    explanation: 'To scale serial execution, each CPU core is assigned its own partition of the data and processes transactions for that partition serially. However, transactions that touch data in multiple partitions require coordination across the partition threads, which is much slower. The system works best when transactions can be confined to a single partition.',
    tags: ['serial-execution', 'partitioning', 'multi-core'],
  },
  {
    id: 'ddia-transactions-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'Two transactions each read a counter value of 42 and write back 43. Under snapshot isolation without explicit locking, what is the final value?',
    options: ['43, because one update is lost — this is the lost update problem', 'It depends on which transaction commits first', '42, because both transactions are aborted', '44, because both increments are applied'],
    correctAnswer: 0,
    explanation: 'This is the lost update problem. Both transactions read the same snapshot value (42), increment it to 43, and write 43. The second write overwrites the first. The counter should be 44 but ends up at 43. Solutions include atomic operations, explicit locking (SELECT FOR UPDATE), or automatic detection by the database.',
    tags: ['lost-update', 'snapshot-isolation', 'counter'],
  },
  {
    id: 'ddia-transactions-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'A system uses a leaderless replication setup with no transaction support. A write succeeds on some replicas but fails on others. The application retries. What ACID property is hardest to guarantee in this setup?',
    options: ['Isolation — reads during the retry may see inconsistent states', 'Atomicity — there is no mechanism to roll back the partial writes across replicas', 'All of the above are equally difficult', 'Durability — data may be lost on the failed replicas'],
    correctAnswer: 1,
    explanation: 'Without multi-object transactions, there is no way to atomically ensure that a write either succeeds on all replicas or is rolled back from all of them. The partial write leaves the system in an inconsistent state. Leaderless systems typically rely on read-repair and anti-entropy to eventually fix such inconsistencies.',
    tags: ['atomicity', 'leaderless', 'replication', 'partial-failure'],
  },
  {
    id: 'ddia-transactions-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'A web application using 2PL experiences frequent timeouts during peak hours. Queries that normally take 5ms are taking 2 seconds. What is the most likely cause?',
    options: ['Disk I/O bottleneck due to write-ahead logging', 'Lock contention — transactions are waiting for locks held by other transactions, causing cascading delays', 'Index corruption requiring full table scans', 'Network latency between the application and the database'],
    correctAnswer: 1,
    explanation: 'Under 2PL, a single slow transaction that holds locks can cause all other transactions accessing the same data to queue up. During peak hours with high concurrency, this cascading effect leads to dramatically increased latencies. This lock contention is one of the primary practical drawbacks of 2PL.',
    tags: ['2pl', 'lock-contention', 'performance', 'peak-load'],
  },
  {
    id: 'ddia-transactions-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'An SSI-based system has a high abort rate (40% of transactions are retried). What is the most likely cause and the recommended action?',
    options: ['The database has insufficient memory; add more RAM', 'SSI is incorrectly configured; switch to read committed', 'The network is too slow; reduce packet size', 'Many transactions have long read-write spans with overlapping data, causing frequent conflict detection; redesign access patterns or shorten transactions'],
    correctAnswer: 3,
    explanation: 'SSI\'s optimistic approach works well when contention is low. If many transactions read and write overlapping data with long durations, SSI detects many conflicts and aborts frequently. The solution is to shorten transaction durations, reduce contention by restructuring data access, or consider if the workload is better suited to 2PL.',
    tags: ['ssi', 'abort-rate', 'contention', 'optimization'],
  },
  {
    id: 'ddia-transactions-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'An e-commerce application has a product listing page and a checkout flow. Users report seeing items as "in stock" on the listing page but getting "out of stock" errors at checkout. Which isolation level is likely in use?',
    options: ['Read uncommitted — the listing page sees uncommitted inventory changes', 'Serializable — this scenario cannot happen', 'Snapshot isolation — the listing snapshot is stale by the time checkout runs', 'Read committed — the listing and checkout are separate transactions seeing different committed states'],
    correctAnswer: 3,
    explanation: 'Under read committed, each statement sees the most recently committed data. The listing page query sees inventory at one moment, and by the time the user checks out (a separate transaction), another user may have purchased the last item. This is a non-repeatable read across transactions. Even snapshot isolation within a single transaction would not help since these are separate transactions.',
    tags: ['read-committed', 'e-commerce', 'practical-scenario'],
  },
  {
    id: 'ddia-transactions-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Snapshot Isolation',
    question: 'A database backup is running using snapshot isolation while the application continues processing transactions. Is the backup guaranteed to be consistent?',
    options: ['Only if the application pauses writes during the backup', 'Yes, because the backup transaction sees a consistent snapshot from the point it started, regardless of concurrent writes', 'Only if the backup is taken from a read replica', 'No, because writes during the backup will corrupt it'],
    correctAnswer: 1,
    explanation: 'This is one of the primary use cases for snapshot isolation. The backup transaction reads from a frozen-in-time snapshot of the database. All concurrent writes by other transactions are invisible to the backup. The resulting backup is consistent with the state of the database at the time the backup transaction started.',
    tags: ['snapshot-isolation', 'backup', 'consistent-snapshot'],
  },
  {
    id: 'ddia-transactions-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'A VoltDB cluster uses actual serial execution. An application developer writes a transaction that calls an external HTTP API mid-transaction. What happens?',
    options: ['The HTTP call is skipped and logged for later processing', 'The single-threaded executor is blocked waiting for the HTTP response, stalling all other transactions on that partition', 'The HTTP call is batched and executed after the transaction commits', 'VoltDB automatically wraps the HTTP call in a separate transaction'],
    correctAnswer: 1,
    explanation: 'In actual serial execution, any I/O operation (like an HTTP call) during a transaction blocks the single execution thread. Since all transactions must go through that thread, the entire partition is stalled. This is why serial execution systems require transactions to be fast stored procedures without any external I/O.',
    tags: ['serial-execution', 'stored-procedure', 'voltdb', 'blocking'],
  },
  {
    id: 'ddia-transactions-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'A database using index-range locks has a query: SELECT * FROM bookings WHERE room_id = 123 AND start_time > "2024-01-01 12:00" AND end_time < "2024-01-01 13:00". If there is an index on room_id but not on time range, what gets locked?',
    options: ['Only rows matching the exact time range', 'All bookings for room 123, regardless of time', 'The entire bookings table', 'No lock is needed since no rows may match'],
    correctAnswer: 1,
    explanation: 'Index-range locks approximate the predicate by attaching to an existing index. With an index on room_id, the lock covers all bookings for room 123 — a broader range than the original predicate. This is safe (no false negatives) but may block transactions for non-overlapping time slots in room 123.',
    tags: ['index-range-lock', '2pl', 'predicate-approximation'],
  },
  {
    id: 'ddia-transactions-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'An application inserts a row into a table and updates a secondary index in the same transaction. The system crashes after the row insert but before the index update. After recovery, what should be true?',
    options: ['Both the row and the index update are either present or absent — atomicity ensures all-or-nothing', 'The system cannot recover and requires manual intervention', 'The row is present and the index will be updated lazily on next access', 'The row exists but the index is missing the entry — the database is in an inconsistent state'],
    correctAnswer: 0,
    explanation: 'Atomicity ensures that multi-object operations (here, the row insert and index update) are treated as one unit. If the transaction cannot complete, all changes are rolled back. After crash recovery using the write-ahead log, either both the row and index entry are present (if committed) or neither is.',
    tags: ['atomicity', 'crash-recovery', 'secondary-index'],
  },
  {
    id: 'ddia-transactions-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'Under SSI, transaction T1 reads row R at version v1. Transaction T2 updates R to v2 and commits. Transaction T1 then writes to a different row S and attempts to commit. Is T1 aborted?',
    options: ['Yes, always — T1 read stale data', 'It depends on whether T1\'s write to S was based on the value it read from R', 'It depends on whether T2 read row S', 'No, never — T1 did not write to R so there is no conflict'],
    correctAnswer: 1,
    explanation: 'SSI tracks which data each transaction read and checks at commit time whether the premises still hold. If T1\'s write to S is causally dependent on the value it read from R (which is now stale), SSI aborts T1. In practice, SSI may be conservative and abort T1 even if the dependency is not certain, since proving independence is difficult.',
    tags: ['ssi', 'causal-dependency', 'stale-read', 'commit-validation'],
  },
  {
    id: 'ddia-transactions-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Write Skew',
    question: 'Your hospital scheduling system has a rule: at least one doctor must be on call at all times. Two doctors simultaneously check the on-call count (2 doctors), each decides they can go off-duty, and both update their status. Now zero doctors are on call. What DDIA concept is this?',
    options: [
      'Lost update',
      'Dirty read',
      'Write skew — both transactions read the same condition (count >= 2), make individual decisions, and write to different rows, violating the invariant that neither write alone would break',
      'Phantom read',
    ],
    correctAnswer: 2,
    explanation: 'DDIA uses this exact hospital example to illustrate write skew: the invariant (at least one doctor on call) is violated because each transaction read the same snapshot showing 2 doctors on call, each decided their individual withdrawal was safe, and each wrote to their own row. Snapshot isolation allows this because no single object is write-conflicted.',
    tags: ['write-skew', 'hospital-example', 'invariant-violation', 'applied'],
  },
  {
    id: 'ddia-transactions-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Serializability',
    question: 'Your application uses actual serial execution (single-threaded transactions) for serializability. A developer adds a transaction that makes a network request to an external API. Based on DDIA, why is this a problem?',
    options: [
      'Network requests are not supported in serial execution mode',
      'The single-threaded executor stalls during the network request, blocking all other transactions; serial execution requires transactions to be very fast (no I/O waits)',
      'The network request will be executed multiple times',
      'External APIs cannot participate in transactions',
    ],
    correctAnswer: 1,
    explanation: 'DDIA emphasizes that serial execution only works if transactions are fast enough to execute on a single thread without blocking others. A network round-trip (milliseconds to seconds) would stall the entire transaction pipeline. Serial execution requires all data to be in memory and transactions to be short stored procedures without external I/O.',
    tags: ['serial-execution', 'network-io', 'stored-procedures', 'applied'],
  },
  {
    id: 'ddia-transactions-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: '2PL',
    question: 'Your database uses 2PL and you notice that transaction throughput drops significantly as concurrency increases. Based on DDIA, what is the fundamental cause?',
    options: [
      'The database has insufficient memory',
      '2PL causes transactions to queue behind each other — readers block writers and writers block readers, creating contention; combined with potential deadlock aborts and retries, throughput degrades at high concurrency',
      '2PL uses too much CPU for lock management',
      'The database needs more disk I/O bandwidth',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that 2PL\'s performance bottleneck is lock contention: the mutual blocking between readers and writers creates queuing delays. At high concurrency, transactions spend more time waiting for locks than doing useful work. Deadlock detection and abort/retry cycles further reduce effective throughput.',
    tags: ['2pl', 'lock-contention', 'throughput', 'applied'],
  },
  {
    id: 'ddia-transactions-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'SSI',
    question: 'Your system uses SSI and you observe a high abort rate during peak traffic. Based on DDIA, what should you consider?',
    options: [
      'Switch to 2PL since it never aborts transactions',
      'SSI aborts transactions when it detects that their reads may be stale due to concurrent writes; high abort rates during high contention are a trade-off of optimistic concurrency — consider whether the workload has too many conflicting transactions for SSI to be effective',
      'SSI abort rates are always zero in correctly designed systems',
      'Increase the database memory to reduce aborts',
    ],
    correctAnswer: 1,
    explanation: 'DDIA explains that SSI is optimistic: it works well when contention is low (most transactions do not conflict). Under high contention, many transactions will be aborted at commit time because their reads are stale. If the abort rate is too high, the overhead of aborted work may exceed the benefits of SSI over 2PL.',
    tags: ['ssi', 'abort-rate', 'contention', 'trade-offs', 'applied'],
  },
  {
    id: 'ddia-transactions-l3-022',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Isolation Levels',
    question: 'A financial application transfers $100 from account A to account B. The transaction reads both balances, checks A has sufficient funds, then writes the new balances. Under read committed isolation, why might this go wrong?',
    options: [
      'Read committed is sufficient for this use case',
      'Under read committed, between reading A\'s balance and writing the new balance, another transaction might also withdraw from A, causing the balance to go negative because the check was based on a stale read',
      'Read committed prevents all concurrency issues',
      'The transfer will always succeed regardless of isolation level',
    ],
    correctAnswer: 1,
    explanation: 'Read committed allows another transaction to modify A\'s balance between the read and write operations of the first transaction. The balance check (A >= 100) may pass on a stale value. To prevent this, you need at least snapshot isolation (to see a consistent view) or serializable isolation (to prevent the concurrent modification entirely).',
    tags: ['read-committed', 'transfer', 'race-condition', 'applied'],
  },
  {
    id: 'ddia-transactions-l3-023',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'Write Skew',
    question: 'Your booking system has a constraint: each meeting room can only have one booking per time slot. Two users simultaneously book the same room for the same time. Both transactions check "is this slot available?" (yes), then both insert a booking. What happened and how do you prevent it?',
    options: [
      'This is a lost update — use SELECT FOR UPDATE',
      'This is a phantom: both transactions checked for the absence of a row that did not exist yet, so there was nothing to lock; prevent it with predicate locks, index-range locks, or serializable isolation',
      'This is a dirty read — use read committed isolation',
      'This cannot happen with snapshot isolation',
    ],
    correctAnswer: 1,
    explanation: 'DDIA identifies this as a phantom problem: the transactions checked for the absence of rows matching a condition. Since the conflicting rows did not exist yet, there was nothing to lock with ordinary row locks. Predicate locks or index-range locks (which lock the concept of "bookings in this time slot") are needed to prevent this.',
    tags: ['phantom', 'write-skew', 'booking-example', 'predicate-locks', 'applied'],
  },
  {
    id: 'ddia-transactions-l3-024',
    language: 'ddia',
    level: 'level3',
    category: 'Transactions',
    subcategory: 'ACID',
    question: 'A user reports that after a database crash during a batch import, half the records were imported and half were not. Based on DDIA, which ACID property was violated?',
    options: [
      'Consistency — the data is inconsistent',
      'Atomicity — the batch import was not all-or-nothing; some records committed while others did not because they were not wrapped in a single atomic transaction',
      'Isolation — other transactions interfered',
      'Durability — the committed records were lost',
    ],
    correctAnswer: 1,
    explanation: 'DDIA defines atomicity as the ability to abort a transaction on error and have all writes discarded. If the batch import was not in a single atomic transaction (or broken into atomic sub-batches), a crash leaves the database in a partially-applied state. Proper atomicity ensures either all records are imported or none.',
    tags: ['atomicity', 'crash', 'batch-import', 'applied'],
  },
];

export default questions;
