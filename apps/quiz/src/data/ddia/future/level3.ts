import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'ddia-future-l3-001',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Data Integration',
    question: 'Your e-commerce platform writes orders to PostgreSQL. You need the same data available in Elasticsearch for search and in a data warehouse for analytics. What approach does DDIA recommend?',
    options: [
      'Copy the PostgreSQL database files directly to the other systems nightly',
      'Have the application write to all three systems in the same HTTP request handler',
      'Replace PostgreSQL with a single system that handles OLTP, search, and analytics',
      'Use change data capture from PostgreSQL to an event log, then consume that log to update Elasticsearch and the warehouse',
    ],
    correctAnswer: 3,
    explanation: 'DDIA recommends using change data capture (CDC) to stream changes from the system of record (PostgreSQL) into an event log (e.g., Kafka), which derived systems consume independently. This avoids dual-write inconsistencies, provides total ordering, and allows each derived system to process changes at its own pace.',
    tags: ['data-integration', 'cdc', 'scenario', 'derived-data'],
  },
  {
    id: 'ddia-future-l3-002',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Data Integration',
    question: 'Your team discovers that a bug in a stream processor has been corrupting derived search indexes for two weeks. How does DDIA suggest you recover?',
    options: [
      'Replay the event log through the corrected processor to rebuild the derived index from scratch',
      'Restore the search index from a two-week-old backup and accept the data loss',
      'Manually fix each corrupted record in the search index',
      'Switch to a completely different search engine',
    ],
    correctAnswer: 0,
    explanation: 'Because the event log retains the raw source events, you can fix the processing logic and replay the entire log to rebuild the derived index from scratch. This is a key advantage of the log-based data integration approach — derived data is expendable and can always be reconstructed from the source of truth.',
    tags: ['data-integration', 'reprocessing', 'event-log', 'recovery'],
  },
  {
    id: 'ddia-future-l3-003',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Lambda Architecture',
    question: 'Your company runs a Lambda Architecture for real-time dashboards. The team frequently finds discrepancies between the batch and speed layer results. What is the most likely root cause and the recommended solution?',
    options: [
      'The two separate codebases have diverged in logic; consider migrating to Kappa Architecture with a single stream processing codebase',
      'The batch layer is too slow; upgrade the hardware',
      'The speed layer should be removed entirely',
      'The serving layer has a bug; rewrite it from scratch',
    ],
    correctAnswer: 0,
    explanation: 'Maintaining two separate code paths (batch and speed) that must produce equivalent results is the fundamental weakness of Lambda Architecture. Logic divergence is common. DDIA suggests that Kappa Architecture, which uses a single stream processing codebase and replays the log for reprocessing, avoids this problem by eliminating the dual-codebase requirement.',
    tags: ['lambda-architecture', 'kappa-architecture', 'scenario', 'migration'],
  },
  {
    id: 'ddia-future-l3-004',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Lambda Architecture',
    question: 'You need to reprocess 6 months of clickstream data to build a new recommendation model. In a Kappa Architecture, how would you handle this?',
    options: [
      'Export the data to CSV and process it in a spreadsheet',
      'Replay the last 6 months of events from the log through a new version of the stream processor, building the output alongside the old one, then switch over',
      'Delete the old data and start collecting fresh events',
      'Write a new batch processing job in a separate framework',
    ],
    correctAnswer: 1,
    explanation: 'In Kappa Architecture, reprocessing is done by deploying a new version of the stream processor that reads from the beginning (or a specific offset) of the retained event log. It writes output to a new derived store. Once it catches up, you switch consumers to the new output and decommission the old one — all using the same stream processing code.',
    tags: ['kappa-architecture', 'reprocessing', 'scenario'],
  },
  {
    id: 'ddia-future-l3-005',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Unbundling Databases',
    question: 'Your microservices architecture uses separate databases for orders (PostgreSQL), product search (Elasticsearch), and user sessions (Redis). How does the unbundling analogy from DDIA help reason about this setup?',
    options: [
      'The event log connecting them is analogous to the WAL inside a single database, and each system is like a specialized index or view maintained by that log',
      'This setup is exactly how a monolithic database works internally',
      'The setup is fundamentally flawed and all data should be in one database',
      'Each microservice is like a separate company with no relationship to the others',
    ],
    correctAnswer: 0,
    explanation: 'DDIA\'s unbundling analogy says these separate systems are like the components inside a single database — PostgreSQL is the primary storage, Elasticsearch is like a full-text index, Redis is like a cache. The event log connecting them plays the role of the internal write-ahead log that keeps all components consistent within a traditional database.',
    tags: ['unbundling', 'microservices', 'analogy', 'scenario'],
  },
  {
    id: 'ddia-future-l3-006',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Unbundling Databases',
    question: 'You want to add a new analytics dashboard that requires a materialized view joining data from three different services. Using the unbundled approach, how should you build this?',
    options: [
      'Copy all three databases into a single relational database and create a SQL view',
      'Query all three services synchronously at dashboard load time',
      'Consume event streams from all three services, join them in a stream processor, and maintain the materialized view as derived data',
      'Have each service push data directly into the dashboard\'s database on every write',
    ],
    correctAnswer: 2,
    explanation: 'The unbundled approach treats the materialized view as derived data maintained by consuming event streams. A stream processor subscribes to change events from all three services, performs the join, and writes the result to a dedicated store for the dashboard. This is analogous to how a database maintains a materialized view from its own tables.',
    tags: ['unbundling', 'materialized-view', 'stream-processing', 'scenario'],
  },
  {
    id: 'ddia-future-l3-007',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'End-to-End Arguments',
    question: 'A user submits a payment, but the response times out. The client retries, and the user is charged twice. How would you apply the end-to-end argument to prevent this?',
    options: [
      'Use UDP instead of TCP to avoid duplicate packets',
      'Disable client-side retries entirely',
      'Increase the network timeout to avoid retries',
      'Assign a unique operation identifier to each payment request, and have the server check for and suppress duplicates before processing',
    ],
    correctAnswer: 3,
    explanation: 'The end-to-end argument says that duplicate suppression cannot be fully solved by the network layer alone — the application must handle it. By assigning a unique idempotency key (operation identifier) to each payment request, the server can detect retries of the same logical operation and return the original result rather than processing the payment again.',
    tags: ['end-to-end', 'idempotency', 'deduplication', 'scenario'],
  },
  {
    id: 'ddia-future-l3-008',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'End-to-End Arguments',
    question: 'Your message broker guarantees exactly-once delivery to consumers, yet your downstream database still shows duplicate records. What does the end-to-end argument say about this situation?',
    options: [
      'This scenario is impossible if the broker guarantees exactly-once delivery',
      'The broker\'s guarantee only covers delivery to the consumer; the application must also ensure exactly-once processing through its own idempotency or deduplication mechanism',
      'The message broker is malfunctioning and should be replaced',
      'The database is ignoring the broker\'s deduplication headers',
    ],
    correctAnswer: 1,
    explanation: 'The end-to-end argument teaches that a guarantee at one layer (the broker) does not automatically extend to the entire end-to-end flow. The consumer might crash after receiving the message but before committing to the database, leading to a retry that creates duplicates. The application itself must implement idempotent writes or deduplication to achieve true end-to-end exactly-once semantics.',
    tags: ['end-to-end', 'exactly-once', 'scenario', 'message-broker'],
  },
  {
    id: 'ddia-future-l3-009',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'End-to-End Arguments',
    question: 'You are designing an API that transfers money between accounts. The operation "transfer $100 from A to B" is not naturally idempotent. How can you make it safe to retry?',
    options: [
      'Include a client-generated unique transfer ID; the server records completed transfer IDs and rejects duplicates',
      'Make the transfer amount random so duplicates are unlikely',
      'Log every request to a file for manual review',
      'Use a database transaction so it cannot fail',
    ],
    correctAnswer: 0,
    explanation: 'By having the client generate a unique transfer ID (idempotency key) and including it with each request, the server can check whether that transfer has already been processed. If a retry arrives with the same ID, the server returns the previous result instead of executing the transfer again. This converts a non-idempotent operation into an effectively idempotent one.',
    tags: ['idempotency', 'operation-identifier', 'scenario', 'deduplication'],
  },
  {
    id: 'ddia-future-l3-010',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Ethics',
    question: 'Your company trains a credit-scoring model on historical loan data. The model performs well on accuracy metrics but disproportionately denies loans to certain ethnic groups. What ethical concern from DDIA does this illustrate?',
    options: [
      'Credit scoring should not use machine learning',
      'The model needs more training data to be accurate',
      'The model has a software bug in its scoring function',
      'Historical data encodes past discriminatory practices, and the model amplifies this bias at scale with a veneer of objectivity',
    ],
    correctAnswer: 3,
    explanation: 'DDIA warns that machine learning models trained on historical data can perpetuate and amplify past discrimination. If historical lending was biased against certain groups, the model learns those patterns and applies them systematically at scale. The appearance of being "data-driven" and "objective" makes it harder to challenge than individual human decisions.',
    tags: ['ethics', 'bias', 'discrimination', 'scenario'],
  },
  {
    id: 'ddia-future-l3-011',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Ethics',
    question: 'A social media platform collects location data, browsing history, and message metadata from its users. Users agreed to the terms of service. A government agency requests this data for mass surveillance. What ethical concerns does DDIA raise?',
    options: [
      'The platform should simply comply since government requests are always legal',
      'Users did not meaningfully consent to surveillance; data collected for one purpose should not be repurposed, and there is an inherent power asymmetry',
      'None — the users consented to the terms of service',
      'The platform should delete all data immediately',
    ],
    correctAnswer: 1,
    explanation: 'DDIA raises multiple concerns: informed consent is largely fictional (users had no real choice), data collected for service improvement is being repurposed for surveillance, and there is a fundamental power asymmetry between data collectors and individuals. Kleppmann argues that data engineers have an ethical responsibility to consider these implications.',
    tags: ['ethics', 'surveillance', 'consent', 'privacy', 'scenario'],
  },
  {
    id: 'ddia-future-l3-012',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Data Integration',
    question: 'Your system uses dual writes to keep PostgreSQL and Elasticsearch in sync. Occasionally, a write succeeds in PostgreSQL but fails in Elasticsearch, leaving them inconsistent. How does DDIA suggest fixing this architectural issue?',
    options: [
      'Replace dual writes with CDC from PostgreSQL to an event log that Elasticsearch consumes',
      'Switch from Elasticsearch to a PostgreSQL full-text search extension',
      'Add retry logic with exponential backoff for Elasticsearch writes',
      'Wrap both writes in a distributed transaction using two-phase commit',
    ],
    correctAnswer: 0,
    explanation: 'DDIA identifies dual writes as fundamentally problematic because they lack atomicity and ordering guarantees. The recommended fix is to make PostgreSQL the single system of record and use CDC to stream changes to a log (like Kafka), which Elasticsearch consumes. This ensures total ordering and allows Elasticsearch to catch up even after temporary failures.',
    tags: ['dual-writes', 'cdc', 'data-integration', 'consistency', 'scenario'],
  },
  {
    id: 'ddia-future-l3-013',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Unbundling Databases',
    question: 'Your team is debating whether to use a single all-in-one database (like CockroachDB with built-in CDC and indexing) or to compose specialized systems (Kafka + PostgreSQL + Elasticsearch). What factor does DDIA identify as most important for this decision?',
    options: [
      'Always choose the unbundled approach because it is newer',
      'Always choose the monolithic approach because it is simpler',
      'Consider whether the integration burden of composing multiple systems is justified by the need for specialized capabilities that no single system provides',
      'The vendor with the best marketing should be chosen',
    ],
    correctAnswer: 2,
    explanation: 'DDIA frames this as a trade-off: monolithic systems provide integrated guarantees with less operational overhead, while unbundled systems allow specialization at the cost of integration complexity. The right choice depends on whether your use case truly needs capabilities that a single system cannot adequately provide.',
    tags: ['unbundling', 'monolithic', 'decision-making', 'scenario'],
  },
  {
    id: 'ddia-future-l3-014',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Lambda Architecture',
    question: 'Your batch pipeline runs daily to rebuild recommendation models, but users complain about 24-hour delays before their preferences are reflected. You also have a real-time stream of user clicks. What architecture would DDIA suggest?',
    options: [
      'Cache the old recommendations for longer to hide the delay',
      'Eliminate the batch pipeline entirely and use only the click stream',
      'Run the batch pipeline more frequently, every hour',
      'Use a unified batch-plus-stream approach where the stream layer handles recent events with low latency while the batch layer periodically reprocesses for correctness',
    ],
    correctAnswer: 3,
    explanation: 'This is the core motivation for combining batch and stream processing. The batch layer provides completeness and correctness by periodically reprocessing all data, while the stream layer supplements it with low-latency updates from recent events. This way, user preferences are reflected quickly while the overall model maintains accuracy.',
    tags: ['lambda-architecture', 'batch-stream', 'scenario', 'latency'],
  },
  {
    id: 'ddia-future-l3-015',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Ethics',
    question: 'Your company stores detailed behavioral profiles of users and uses them for ad targeting. A data breach exposes this information. What DDIA ethical principle does this scenario highlight?',
    options: [
      'Data should always be stored in the cloud for better security',
      'Ad targeting is always unethical regardless of data handling',
      'Data collected is a liability — detailed personal data creates risks that persist long after collection, and data minimization should be practiced',
      'Breaches are inevitable and there is nothing to learn from them',
    ],
    correctAnswer: 2,
    explanation: 'DDIA emphasizes that accumulated personal data is a liability, not just an asset. Detailed behavioral profiles can cause lasting harm if breached. This supports the principle of data minimization — collecting only what is necessary and deleting what is no longer needed, rather than hoarding data on the assumption it might be useful later.',
    tags: ['ethics', 'privacy', 'data-minimization', 'breach', 'scenario'],
  },
  {
    id: 'ddia-future-l3-016',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Unbundling Databases',
    question: 'You are building a system where writes go to a PostgreSQL database, and a CDC pipeline feeds events to Kafka. Three teams independently consume from Kafka to build a search index, a cache, and an analytics cube. One consumer falls behind by several hours. How does the unbundled approach handle this?',
    options: [
      'Each consumer reads from the log independently at its own pace, so the slow consumer catches up without affecting others',
      'The slow consumer blocks all other consumers until it catches up',
      'The CDC pipeline must be paused until all consumers are synchronized',
      'Kafka automatically drops messages for the slow consumer',
    ],
    correctAnswer: 0,
    explanation: 'A key advantage of log-based data integration is that each consumer maintains its own offset and reads independently. A slow consumer does not block others — it simply reads from an earlier position in the log and catches up at its own pace. The log acts as a buffer that decouples producers from consumers.',
    tags: ['unbundling', 'event-log', 'consumer-independence', 'scenario'],
  },
  {
    id: 'ddia-future-l3-017',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'End-to-End Arguments',
    question: 'Your distributed system uses a message broker with at-least-once delivery. A consumer processes messages and writes results to a database. The consumer crashes after writing to the database but before acknowledging the message. What happens and how should it be handled?',
    options: [
      'The database transaction automatically rolls back when the consumer crashes',
      'The broker redelivers the message, causing the consumer to write a duplicate; the consumer should use an idempotent write or deduplication to handle this',
      'The broker automatically detects the duplicate and suppresses it',
      'The message is lost since the consumer crashed',
    ],
    correctAnswer: 1,
    explanation: 'With at-least-once delivery, the broker redelivers unacknowledged messages. Since the consumer already wrote to the database before crashing, the redelivered message will cause a duplicate write. The end-to-end argument says the consumer must handle this — for example, by using an idempotent write (UPSERT with the same key) or by checking an operation ID before writing.',
    tags: ['end-to-end', 'at-least-once', 'idempotency', 'scenario'],
  },
  {
    id: 'ddia-future-l3-018',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'End-to-End Arguments',
    question: 'Your payment system occasionally processes the same payment twice during retries. Users are charged twice. Based on DDIA\'s end-to-end argument, where should deduplication be implemented?',
    options: [
      'At the network layer using TCP guarantees',
      'At the database layer using unique constraints',
      'At the application level using an idempotency key generated by the client and checked end-to-end, because no lower-level mechanism can fully prevent duplicates from the user\'s perspective',
      'At the message broker level using exactly-once delivery',
    ],
    correctAnswer: 2,
    explanation: 'DDIA applies the end-to-end argument: TCP, message brokers, and databases can each prevent duplicates at their layer, but none can guarantee end-to-end deduplication. The client must generate a unique operation ID (idempotency key) that is checked at the final point of effect (the payment processor) to ensure true end-to-end exactly-once semantics.',
    tags: ['end-to-end', 'idempotency-key', 'payment', 'deduplication', 'applied'],
  },
  {
    id: 'ddia-future-l3-019',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Data Integration',
    question: 'Your company has data spread across PostgreSQL, MongoDB, Elasticsearch, and Redis. Updates to PostgreSQL are not consistently reflected in the other systems. Based on DDIA, what architectural change is needed?',
    options: [
      'Replace all systems with a single database',
      'Implement dual writes from the application to all four systems',
      'Make PostgreSQL the single system of record and use CDC to propagate changes through a log (Kafka) to all derived systems, ensuring ordered, consistent updates',
      'Periodically dump PostgreSQL and reload into the other systems',
    ],
    correctAnswer: 2,
    explanation: 'DDIA recommends the unbundled approach with a single system of record (PostgreSQL) and CDC feeding a log. Dual writes are fragile (partial failures cause inconsistency). Periodic dumps add latency and waste resources. CDC provides near-real-time, ordered, reliable propagation of changes to all derived systems.',
    tags: ['data-integration', 'cdc', 'system-of-record', 'unbundling', 'applied'],
  },
  {
    id: 'ddia-future-l3-020',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Lambda Architecture',
    question: 'Your team uses a Kappa Architecture with Flink for all processing. A bug in the Flink job corrupted the output for 3 days. Based on DDIA, how do you recover?',
    options: [
      'Restore from a database backup',
      'Fix the bug, then replay the last 3+ days of events from Kafka through the corrected Flink job to rebuild the output — the immutable event log enables reprocessing with corrected logic',
      'Manually correct the corrupted output records',
      'Switch to Lambda Architecture to prevent this in the future',
    ],
    correctAnswer: 1,
    explanation: 'DDIA emphasizes that retaining the immutable event log is what makes recovery possible. By fixing the bug and replaying the relevant portion of the log through the corrected processing logic, you can rebuild correct output. This is one of the key advantages of maintaining an immutable, replayable event log.',
    tags: ['kappa-architecture', 'reprocessing', 'event-log', 'recovery', 'applied'],
  },
  {
    id: 'ddia-future-l3-021',
    language: 'ddia',
    level: 'level3',
    category: 'Future of Data Systems',
    subcategory: 'Ethics',
    question: 'Your company builds a machine learning model that predicts loan default risk. It unintentionally discriminates against certain demographics because the training data reflects historical biases. Based on DDIA, what is the ethical responsibility?',
    options: [
      'No responsibility since the algorithm is objective and data-driven',
      'The engineers and organization bear responsibility for deploying a system that causes harm, regardless of whether the discrimination was intentional — they should audit for bias, ensure transparency, and provide recourse for affected individuals',
      'Only the data scientists are responsible, not the engineers who built the infrastructure',
      'Historical bias in data is acceptable because it reflects reality',
    ],
    correctAnswer: 1,
    explanation: 'DDIA argues that algorithmic bias is not excused by being unintentional. The organization deploying the system is responsible for its outcomes. Engineers should audit models for bias, make decisions transparent and explainable, and ensure that affected individuals have recourse to challenge decisions.',
    tags: ['ethics', 'algorithmic-bias', 'responsibility', 'fairness', 'applied'],
  },
];

export default questions;
