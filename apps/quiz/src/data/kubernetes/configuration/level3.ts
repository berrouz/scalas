import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-configuration-l3-001',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'A pod mounts a ConfigMap as a volume with subPath. After updating the ConfigMap, the pod still sees old data. Why?',
    options: [
      'The ConfigMap was not saved correctly',
      'Volume mounts using subPath do not receive automatic updates from ConfigMap changes',
      'The kubelet cache has not expired',
      'The pod needs to be in a Ready state to receive updates',
    ],
    correctAnswer: 1,
    explanation: 'When using subPath to mount individual files from a ConfigMap, the mounted file is not updated when the ConfigMap changes. This is a known limitation. You must restart the pod or use a full volume mount.',
    tags: ['kubernetes', 'configmaps', 'subpath', 'troubleshooting'],
  },
  {
    id: 'k8s-configuration-l3-002',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How can you trigger a rolling update of a Deployment when a ConfigMap it uses is updated?',
    options: [
      'ConfigMap changes automatically trigger rolling updates',
      'Use a hash of the ConfigMap data as an annotation on the pod template',
      'Set the ConfigMap version in the Deployment labels',
      'Enable the ConfigMap auto-reload feature in the Deployment spec',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes does not automatically restart pods when ConfigMaps change. A common pattern is to compute a hash of the ConfigMap data and store it as a pod template annotation. When the ConfigMap changes, the hash changes, triggering a rolling update.',
    codeSnippet: 'spec:\n  template:\n    metadata:\n      annotations:\n        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}',
    tags: ['kubernetes', 'configmaps', 'rolling-update', 'helm'],
  },
  {
    id: 'k8s-configuration-l3-003',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'You need to rotate a TLS Secret used by multiple Ingress resources. What is the safest approach?',
    options: [
      'Delete the old Secret and create a new one with the same name',
      'Update the Secret in-place using kubectl apply; Ingress controllers will pick up the change',
      'Create a new Secret with a different name and update all Ingress resources simultaneously',
      'You must restart the Ingress controller after any Secret change',
    ],
    correctAnswer: 1,
    explanation: 'Most Ingress controllers watch for Secret changes and reload certificates automatically. Updating the Secret in-place with kubectl apply is the safest approach as it avoids downtime from missing Secrets.',
    tags: ['kubernetes', 'secrets', 'tls', 'rotation', 'ingress'],
  },
  {
    id: 'k8s-configuration-l3-004',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the recommended approach for managing Secrets in a GitOps workflow?',
    options: [
      'Commit base64-encoded Secrets directly to Git',
      'Use tools like Sealed Secrets, SOPS, or External Secrets Operator to encrypt or externalize Secrets',
      'Store Secrets in a separate private Git repository',
      'Use ConfigMaps instead of Secrets for GitOps',
    ],
    correctAnswer: 1,
    explanation: 'Storing raw or base64-encoded Secrets in Git is insecure. Tools like Bitnami Sealed Secrets encrypt Secrets client-side, SOPS encrypts specific fields, and External Secrets Operator fetches from external stores like Vault or AWS Secrets Manager.',
    tags: ['kubernetes', 'secrets', 'gitops', 'sealed-secrets', 'security'],
  },
  {
    id: 'k8s-configuration-l3-005',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'A pod is experiencing frequent CPU throttling despite low average CPU usage. What is the likely cause?',
    options: [
      'The node does not have enough CPU cores',
      'The CPU limit is too low, causing CFS quota throttling during burst periods',
      'The pod has the wrong QoS class',
      'The kubelet is misconfigured',
    ],
    correctAnswer: 1,
    explanation: 'CFS (Completely Fair Scheduler) enforces CPU limits over short periods (100ms by default). Even if average usage is low, a container can burst and hit the CFS quota within a single period, causing throttling. Consider removing CPU limits or increasing them.',
    tags: ['kubernetes', 'resource-limits', 'cpu', 'throttling', 'cfs'],
  },
  {
    id: 'k8s-configuration-l3-006',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the recommended practice regarding CPU limits in production workloads?',
    options: [
      'Always set CPU limits equal to requests',
      'Never set CPU limits',
      'Set CPU requests for scheduling but consider omitting CPU limits to avoid CFS throttling',
      'Set CPU limits to 10x the requests',
    ],
    correctAnswer: 2,
    explanation: 'Many production environments set CPU requests for scheduling but omit CPU limits to avoid CFS throttling. This is recommended by some experts (e.g., Google SRE) because CPU is compressible and throttling can cause latency spikes.',
    tags: ['kubernetes', 'resource-limits', 'cpu', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-007',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the minimal SecurityContext configuration recommended for production containers?',
    options: [
      'Just setting runAsNonRoot: true',
      'runAsNonRoot: true, readOnlyRootFilesystem: true, allowPrivilegeEscalation: false, drop ALL capabilities',
      'Setting privileged: false',
      'Using the default SecurityContext',
    ],
    correctAnswer: 1,
    explanation: 'A hardened SecurityContext should include: runAsNonRoot (prevent root), readOnlyRootFilesystem (prevent filesystem writes), allowPrivilegeEscalation: false (prevent escalation), and dropping all capabilities. Add back only specific capabilities needed.',
    codeSnippet: 'securityContext:\n  runAsNonRoot: true\n  readOnlyRootFilesystem: true\n  allowPrivilegeEscalation: false\n  capabilities:\n    drop: ["ALL"]\n  seccompProfile:\n    type: RuntimeDefault',
    tags: ['kubernetes', 'security-context', 'hardening', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-008',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'A developer reports that environment variables from a ConfigMap are not updating after the ConfigMap was changed. What is the root cause?',
    options: [
      'The ConfigMap was updated in the wrong namespace',
      'Environment variables from ConfigMaps are set at container start and never update without a pod restart',
      'The kubelet needs to be restarted to pick up ConfigMap changes',
      'The container runtime cache must be cleared',
    ],
    correctAnswer: 1,
    explanation: 'Environment variables sourced from ConfigMaps (via env or envFrom) are set once when the container starts. They are never automatically updated. You must restart the pod to pick up changes.',
    tags: ['kubernetes', 'env-vars', 'configmaps', 'troubleshooting'],
  },
  {
    id: 'k8s-configuration-l3-009',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'A pod needs to write temporary files but has readOnlyRootFilesystem: true. What is the best solution?',
    options: [
      'Remove readOnlyRootFilesystem',
      'Mount an emptyDir volume at the path where temp files are needed',
      'Use a hostPath volume for temp files',
      'Use a PersistentVolumeClaim for temporary storage',
    ],
    correctAnswer: 1,
    explanation: 'The best practice is to keep readOnlyRootFilesystem: true and mount emptyDir volumes at paths that need to be writable (e.g., /tmp, /var/cache). This maintains security while allowing necessary writes.',
    codeSnippet: 'containers:\n  - name: app\n    securityContext:\n      readOnlyRootFilesystem: true\n    volumeMounts:\n      - name: tmp\n        mountPath: /tmp\n      - name: cache\n        mountPath: /var/cache\nvolumes:\n  - name: tmp\n    emptyDir: {}\n  - name: cache\n    emptyDir: {}',
    tags: ['kubernetes', 'volume-mounts', 'security-context', 'emptydir'],
  },
  {
    id: 'k8s-configuration-l3-010',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'A team deploys a pod without resource requests in a namespace with a ResourceQuota requiring requests. The pod succeeds. Why?',
    options: [
      'ResourceQuotas do not actually require requests',
      'A LimitRange in the namespace provides default requests',
      'The quota was temporarily disabled',
      'The pod bypassed the quota check',
    ],
    correctAnswer: 1,
    explanation: 'When a LimitRange with default/defaultRequest values exists in the same namespace as a ResourceQuota, pods without explicit resource specs get the LimitRange defaults applied, satisfying the quota requirement.',
    tags: ['kubernetes', 'limit-ranges', 'resource-quotas', 'defaults'],
  },
  {
    id: 'k8s-configuration-l3-011',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How do you enforce that all pods in a namespace must have resource requests?',
    options: [
      'Create a NetworkPolicy that blocks pods without requests',
      'Create a ResourceQuota that includes requests.cpu and requests.memory',
      'Create a LimitRange with min values only',
      'Use a PodDisruptionBudget',
    ],
    correctAnswer: 1,
    explanation: 'When a ResourceQuota includes requests.cpu or requests.memory, every pod in the namespace must specify those fields. If they do not (and no LimitRange provides defaults), the pod is rejected.',
    tags: ['kubernetes', 'resource-quotas', 'enforcement', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-012',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'How can you use the Downward API to help a container discover its own resource limits for JVM heap sizing?',
    options: [
      'Read the /proc/meminfo file inside the container',
      'Use resourceFieldRef to expose limits.memory as an env var and configure -Xmx based on it',
      'Query the Kubernetes API from the container',
      'Use a sidecar container to inject the information',
    ],
    correctAnswer: 1,
    explanation: 'Exposing the memory limit via resourceFieldRef allows the application to dynamically set its heap size. For JVMs, you can derive -Xmx from the exposed memory limit (e.g., 75% of the limit).',
    codeSnippet: 'env:\n  - name: MEM_LIMIT\n    valueFrom:\n      resourceFieldRef:\n        resource: limits.memory\n        divisor: "1Mi"\n  - name: JAVA_OPTS\n    value: "-Xmx$(MEM_LIMIT)m"',
    tags: ['kubernetes', 'downward-api', 'resource-limits', 'jvm'],
  },
  {
    id: 'k8s-configuration-l3-013',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the impact of having many pods watching the same ConfigMap?',
    options: [
      'No impact; ConfigMap watches are free',
      'Increased API server load from watch connections and ConfigMap distribution',
      'The ConfigMap becomes read-only after a certain number of watchers',
      'Network bandwidth is the only concern',
    ],
    correctAnswer: 1,
    explanation: 'Each kubelet watching a ConfigMap volume creates load on the API server. For large clusters, using immutable ConfigMaps eliminates the need for watches and significantly reduces API server load.',
    tags: ['kubernetes', 'configmaps', 'performance', 'scalability'],
  },
  {
    id: 'k8s-configuration-l3-014',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What are the encryption providers available for Secret encryption at rest, ordered from most to least recommended?',
    options: [
      'aescbc, aesgcm, secretbox, identity',
      'kms v2, kms v1, secretbox, aescbc, aesgcm, identity',
      'identity, aescbc, kms',
      'tls, aescbc, identity',
    ],
    correctAnswer: 1,
    explanation: 'KMS v2 is the most recommended (integrates with external KMS like AWS KMS, Azure Key Vault). Then KMS v1, secretbox, aescbc, aesgcm. Identity means no encryption (stored as-is).',
    tags: ['kubernetes', 'secrets', 'encryption', 'kms'],
  },
  {
    id: 'k8s-configuration-l3-015',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'A Burstable pod is getting OOMKilled even though the node has free memory. What could be the cause?',
    options: [
      'The kernel is defective',
      'The container is exceeding its own memory limit, not the node memory',
      'The scheduler miscalculated available memory',
      'OOMKill only happens when the node runs out of memory',
    ],
    correctAnswer: 1,
    explanation: 'A container is OOMKilled when it exceeds its own memory limit, regardless of free node memory. The cgroup memory limit enforces per-container constraints. Check the container memory limit vs. actual usage.',
    tags: ['kubernetes', 'resource-limits', 'memory', 'oom', 'troubleshooting'],
  },
  {
    id: 'k8s-configuration-l3-016',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does the container-level SecurityContext interact with the pod-level SecurityContext?',
    options: [
      'Pod-level settings always override container-level settings',
      'Container-level settings override pod-level settings for overlapping fields',
      'They are merged with no precedence; conflicts cause errors',
      'Only one level can be specified',
    ],
    correctAnswer: 1,
    explanation: 'When both pod-level and container-level SecurityContexts are defined, container-level settings override pod-level settings for fields that exist at both levels (like runAsUser, capabilities). Some fields like fsGroup exist only at the pod level.',
    tags: ['kubernetes', 'security-context', 'precedence', 'pods'],
  },
  {
    id: 'k8s-configuration-l3-017',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'A container needs to run a startup script that sources environment from a file and then starts the main process. What is the best approach?',
    options: [
      'Use an init container to set environment variables',
      'Use command to run a shell that sources the file and exec the main process',
      'Put the logic in the image ENTRYPOINT only',
      'Use a postStart lifecycle hook',
    ],
    correctAnswer: 1,
    explanation: 'Using command: ["/bin/sh", "-c"] with args that source the env file and exec the main process is a common pattern. The exec replaces the shell with the main process for proper signal handling.',
    codeSnippet: 'command: ["/bin/sh", "-c"]\nargs:\n  - |\n    source /etc/config/env.sh\n    exec /app/server',
    tags: ['kubernetes', 'command-args', 'shell', 'exec'],
  },
  {
    id: 'k8s-configuration-l3-018',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'What happens when a high-priority pod cannot be scheduled even after preempting lower-priority pods?',
    options: [
      'The preempted pods are restored',
      'The high-priority pod remains Pending; preempted pods are still terminated',
      'The scheduler retries with different preemption targets',
      'The high-priority pod is downgraded to a lower priority',
    ],
    correctAnswer: 1,
    explanation: 'If a high-priority pod still cannot be scheduled after preemption (e.g., due to affinity constraints or other non-preemptable requirements), it remains Pending. The already-preempted pods are not restored.',
    tags: ['kubernetes', 'priority-classes', 'preemption', 'scheduling'],
  },
  {
    id: 'k8s-configuration-l3-019',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How do you use Kustomize to generate ConfigMaps from files?',
    options: [
      'By listing ConfigMaps in the resources section',
      'By using the configMapGenerator field in kustomization.yaml',
      'By creating a separate ConfigMap manifest',
      'Kustomize does not support ConfigMap generation',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize has a configMapGenerator that creates ConfigMaps from files, literals, or env files. It automatically appends a hash suffix to the name for immutable-like behavior, triggering rolling updates.',
    codeSnippet: '# kustomization.yaml\nconfigMapGenerator:\n  - name: app-config\n    files:\n      - application.properties\n    literals:\n      - LOG_LEVEL=info',
    tags: ['kubernetes', 'configmaps', 'kustomize'],
  },
  {
    id: 'k8s-configuration-l3-020',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the External Secrets Operator work?',
    options: [
      'It encrypts Secrets in etcd using an external KMS',
      'It syncs secrets from external secret management systems (like AWS Secrets Manager, Vault) into Kubernetes Secrets',
      'It stores Kubernetes Secrets in an external database',
      'It rotates Secrets on a scheduled basis',
    ],
    correctAnswer: 1,
    explanation: 'The External Secrets Operator reads information from external APIs (AWS Secrets Manager, HashiCorp Vault, Azure Key Vault, etc.) and creates Kubernetes Secrets. It uses ExternalSecret CRDs to define the mapping.',
    codeSnippet: 'apiVersion: external-secrets.io/v1beta1\nkind: ExternalSecret\nmetadata:\n  name: db-secret\nspec:\n  refreshInterval: 1h\n  secretStoreRef:\n    name: aws-secrets-manager\n    kind: ClusterSecretStore\n  target:\n    name: db-credentials\n  data:\n    - secretKey: password\n      remoteRef:\n        key: prod/db/password',
    tags: ['kubernetes', 'secrets', 'external-secrets', 'vault'],
  },
  {
    id: 'k8s-configuration-l3-021',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How do you use the Vertical Pod Autoscaler (VPA) to right-size resource requests?',
    options: [
      'VPA automatically changes running pod resources without restart',
      'VPA provides recommendations and can update requests on pod recreation',
      'VPA only scales pods horizontally',
      'VPA adjusts node resources, not pod resources',
    ],
    correctAnswer: 1,
    explanation: 'VPA monitors pod resource usage and provides recommendations. In "Auto" mode, it updates resource requests when pods are recreated. In "Off" mode, it only provides recommendations without making changes.',
    codeSnippet: 'apiVersion: autoscaling.k8s.io/v1\nkind: VerticalPodAutoscaler\nmetadata:\n  name: my-app-vpa\nspec:\n  targetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: my-app\n  updatePolicy:\n    updateMode: "Auto"',
    tags: ['kubernetes', 'resource-limits', 'vpa', 'autoscaling'],
  },
  {
    id: 'k8s-configuration-l3-022',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'A container needs to bind to port 80 but runs as non-root. How do you solve this?',
    options: [
      'Run the container as root',
      'Add the NET_BIND_SERVICE capability and set the listening port to 80',
      'Use an Ingress to route traffic from port 80',
      'Change the container to listen on a high port and use a Service to expose port 80',
    ],
    correctAnswer: 3,
    explanation: 'The best practice is to have the container listen on a high port (e.g., 8080) and use a Kubernetes Service to expose it on port 80. This avoids requiring special capabilities. Adding NET_BIND_SERVICE works but is not preferred.',
    tags: ['kubernetes', 'security-context', 'non-root', 'ports', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-023',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'How do you handle configuration that needs different values across environments (dev, staging, prod)?',
    options: [
      'Use different Docker images for each environment',
      'Hardcode values in the application code',
      'Use Kustomize overlays or Helm values to inject environment-specific ConfigMaps',
      'Use the same ConfigMap with a namespace prefix',
    ],
    correctAnswer: 2,
    explanation: 'Kustomize overlays allow patching base manifests per environment. Helm values files (values-dev.yaml, values-prod.yaml) parametrize templates. Both produce environment-specific ConfigMaps from a shared base.',
    tags: ['kubernetes', 'env-vars', 'configmaps', 'kustomize', 'helm'],
  },
  {
    id: 'k8s-configuration-l3-024',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is the risk of using hostPath volumes in production?',
    options: [
      'There is no risk; hostPath is production-ready',
      'They create a security risk, are not portable, and can allow container escape if misconfigured',
      'They are slower than other volume types',
      'They only work on single-node clusters',
    ],
    correctAnswer: 1,
    explanation: 'hostPath volumes pose security risks (containers can access host filesystem), are not portable (node-specific paths), and can enable container escape. They should be avoided in production except for system-level components.',
    tags: ['kubernetes', 'volume-mounts', 'hostpath', 'security', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-025',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'How do you set different LimitRange defaults for init containers vs. regular containers?',
    options: [
      'Use separate LimitRange objects with different types',
      'Use the initContainer type in the LimitRange spec',
      'LimitRanges apply equally to both; you cannot differentiate between init and regular containers',
      'Use annotations on the LimitRange',
    ],
    correctAnswer: 2,
    explanation: 'LimitRanges with type: Container apply to all containers including init containers. There is no separate type for init containers. If init containers need different limits, they must specify them explicitly.',
    tags: ['kubernetes', 'limit-ranges', 'init-containers'],
  },
  {
    id: 'k8s-configuration-l3-026',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How do you set up ResourceQuotas for multiple teams sharing a cluster?',
    options: [
      'Use a single global quota for the cluster',
      'Create separate namespaces per team with individual ResourceQuotas',
      'Use node affinity to separate teams',
      'Use PodSecurityPolicies per team',
    ],
    correctAnswer: 1,
    explanation: 'The standard approach is namespace-per-team with ResourceQuotas. Each team gets a namespace with quotas limiting their total CPU, memory, storage, and object counts. This provides fair resource sharing.',
    tags: ['kubernetes', 'resource-quotas', 'multi-tenancy', 'namespaces'],
  },
  {
    id: 'k8s-configuration-l3-027',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'Can the Downward API expose custom resource usage (e.g., GPU count)?',
    options: [
      'Yes, using resourceFieldRef with the extended resource name',
      'No, resourceFieldRef only supports cpu, memory, and ephemeral-storage',
      'Yes, but only as environment variables',
      'Only with a custom admission webhook',
    ],
    correctAnswer: 1,
    explanation: 'The Downward API resourceFieldRef only supports standard resources: limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory, and requests.ephemeral-storage. Extended resources are not supported.',
    tags: ['kubernetes', 'downward-api', 'extended-resources', 'limitations'],
  },
  {
    id: 'k8s-configuration-l3-028',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the difference between "Localhost" and "RuntimeDefault" seccomp profile types?',
    options: [
      'They are identical',
      'RuntimeDefault uses the container runtime built-in profile; Localhost loads a custom profile from the node',
      'Localhost is more restrictive than RuntimeDefault',
      'RuntimeDefault only works with Docker, not containerd',
    ],
    correctAnswer: 1,
    explanation: 'RuntimeDefault applies the container runtime default seccomp profile (e.g., Docker or containerd default). Localhost loads a custom profile from a file on the node at the kubelet seccomp profile root.',
    codeSnippet: '# Localhost example\nsecurityContext:\n  seccompProfile:\n    type: Localhost\n    localhostProfile: profiles/my-profile.json',
    tags: ['kubernetes', 'security-context', 'seccomp', 'profiles'],
  },
  {
    id: 'k8s-configuration-l3-029',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How do you handle ConfigMap updates for StatefulSet pods that rely on stable configuration?',
    options: [
      'Use immutable ConfigMaps with version suffixes and update the StatefulSet pod template',
      'Update the ConfigMap and wait for automatic propagation',
      'Delete and recreate all pods',
      'Use a DaemonSet instead of a StatefulSet',
    ],
    correctAnswer: 0,
    explanation: 'For StatefulSets requiring controlled rollouts, use immutable ConfigMaps with versioned names (e.g., app-config-v2). Update the StatefulSet pod template to reference the new ConfigMap, triggering a controlled rolling update.',
    tags: ['kubernetes', 'configmaps', 'statefulset', 'immutable', 'versioning'],
  },
  {
    id: 'k8s-configuration-l3-030',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How do you configure automatic Secret rotation for Secrets managed by the External Secrets Operator?',
    options: [
      'Use the CronJob API to periodically delete and recreate Secrets',
      'Set the refreshInterval field on the ExternalSecret resource',
      'Enable the auto-rotate annotation on the Secret',
      'Configure a webhook to trigger on secret changes',
    ],
    correctAnswer: 1,
    explanation: 'The refreshInterval field on an ExternalSecret resource defines how often the operator syncs the external secret value into the Kubernetes Secret. Setting it to "1h" checks every hour for updates.',
    tags: ['kubernetes', 'secrets', 'external-secrets', 'rotation'],
  },
  {
    id: 'k8s-configuration-l3-031',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What are the implications of setting memory requests much lower than limits (e.g., 128Mi request, 2Gi limit)?',
    options: [
      'No implications; this is the recommended configuration',
      'The scheduler may place many pods on a node based on low requests, leading to node-level OOM when pods burst',
      'The pod automatically gets Guaranteed QoS',
      'The kernel prevents the pod from using more than the request',
    ],
    correctAnswer: 1,
    explanation: 'When requests are much lower than limits, the scheduler overcommits the node. If many pods burst simultaneously, the node can run out of memory, causing OOM kills at the node level. This is known as memory overcommitment risk.',
    tags: ['kubernetes', 'resource-limits', 'memory', 'overcommitment', 'scheduling'],
  },
  {
    id: 'k8s-configuration-l3-032',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'How can you inject a JSON configuration as a single environment variable from a ConfigMap?',
    options: [
      'It is not possible to store JSON in an environment variable',
      'Store the JSON as a value in the ConfigMap and reference it via configMapKeyRef',
      'Use a JSON-type ConfigMap',
      'Use a Secret instead because ConfigMaps do not support JSON',
    ],
    correctAnswer: 1,
    explanation: 'ConfigMap values can contain any string, including JSON. Store the JSON as a single key value and inject it via configMapKeyRef. The application can parse the JSON from the environment variable.',
    codeSnippet: '# ConfigMap\ndata:\n  config.json: |\n    {"database": {"host": "db", "port": 5432}}\n\n# Pod env\nenv:\n  - name: APP_CONFIG\n    valueFrom:\n      configMapKeyRef:\n        name: app-config\n        key: config.json',
    tags: ['kubernetes', 'env-vars', 'configmaps', 'json'],
  },
  {
    id: 'k8s-configuration-l3-033',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is the sizeLimit field on an emptyDir volume and how is it enforced?',
    options: [
      'It preallocates the specified storage amount',
      'It sets a soft limit; the kubelet evicts the pod if usage exceeds the limit',
      'It is purely informational and not enforced',
      'It limits the number of files, not the total size',
    ],
    correctAnswer: 1,
    explanation: 'The sizeLimit field on emptyDir sets a maximum usage. The kubelet periodically checks and evicts the pod if the emptyDir exceeds this limit. For memory-backed emptyDir, the limit also counts against the container memory limit.',
    tags: ['kubernetes', 'volume-mounts', 'emptydir', 'sizelimit', 'eviction'],
  },
  {
    id: 'k8s-configuration-l3-034',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'A PVC creation is rejected even though the StorageClass exists. What could be the cause?',
    options: [
      'The StorageClass is not in the same namespace',
      'A LimitRange with a PVC max storage constraint is limiting the PVC size',
      'The PVC name is too long',
      'StorageClasses are deprecated',
    ],
    correctAnswer: 1,
    explanation: 'A LimitRange can constrain PVC sizes using type: PersistentVolumeClaim with min and max storage values. If the requested PVC size exceeds the max, creation is rejected.',
    codeSnippet: 'spec:\n  limits:\n    - type: PersistentVolumeClaim\n      max:\n        storage: 10Gi\n      min:\n        storage: 1Gi',
    tags: ['kubernetes', 'limit-ranges', 'pvc', 'storage'],
  },
  {
    id: 'k8s-configuration-l3-035',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How do you implement a "burst" quota that allows temporary resource spikes above the base quota?',
    options: [
      'Use a ResourceQuota with burst: true',
      'Kubernetes does not support burst quotas; use separate namespaces with different quotas and namespace migration',
      'Set limits higher than requests in the ResourceQuota',
      'Use a custom scheduler plugin',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes ResourceQuotas are hard limits with no built-in burst capability. To allow temporary spikes, you can use separate namespaces, implement a custom admission webhook, or use request/limit separation at the pod level.',
    tags: ['kubernetes', 'resource-quotas', 'burst', 'multi-tenancy'],
  },
  {
    id: 'k8s-configuration-l3-036',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'How does preemption interact with PodDisruptionBudgets (PDBs)?',
    options: [
      'Preemption always respects PDBs',
      'Preemption may violate PDBs if no other victims are available, but the scheduler attempts to respect them',
      'PDBs are ignored during preemption',
      'Preemption and PDBs are independent and never interact',
    ],
    correctAnswer: 1,
    explanation: 'The scheduler tries to find preemption victims that do not violate PDBs. However, if no valid set of victims exists that respects all PDBs, the scheduler may still preempt while violating PDBs to schedule the high-priority pod.',
    tags: ['kubernetes', 'priority-classes', 'preemption', 'pdb'],
  },
  {
    id: 'k8s-configuration-l3-037',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the Reloader controller and why is it useful?',
    options: [
      'A built-in Kubernetes controller that restarts pods on config changes',
      'A third-party controller that watches ConfigMaps/Secrets and triggers rolling updates on associated Deployments when data changes',
      'A tool for reloading cluster certificates',
      'A kubelet feature for reloading container images',
    ],
    correctAnswer: 1,
    explanation: 'Reloader (stakater/Reloader) is a third-party controller that watches ConfigMaps and Secrets. When they change, it triggers rolling updates on Deployments/StatefulSets that reference them, solving the update propagation problem.',
    tags: ['kubernetes', 'configmaps', 'secrets', 'reloader', 'third-party'],
  },
  {
    id: 'k8s-configuration-l3-038',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is a Sealed Secret and how does it work?',
    options: [
      'A Secret that is sealed in a specific namespace and cannot be moved',
      'A CRD that contains an encrypted Secret that can only be decrypted by the Sealed Secrets controller in the cluster',
      'A Secret that is write-only and cannot be read',
      'A Secret stored in a sealed hardware module',
    ],
    correctAnswer: 1,
    explanation: 'Bitnami Sealed Secrets uses asymmetric encryption. The kubeseal CLI encrypts Secrets using the cluster public key. Only the Sealed Secrets controller (with the private key) can decrypt them. The encrypted form is safe to store in Git.',
    tags: ['kubernetes', 'secrets', 'sealed-secrets', 'encryption', 'gitops'],
  },
  {
    id: 'k8s-configuration-l3-039',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is Topology-Aware Resource Allocation and how does it affect CPU requests?',
    options: [
      'It distributes pods evenly across topology domains',
      'It ensures CPU and memory allocations are from the same NUMA node for performance-sensitive workloads',
      'It limits resources based on geographic topology',
      'It allocates resources based on network topology',
    ],
    correctAnswer: 1,
    explanation: 'Topology Manager in the kubelet aligns CPU, memory, and device allocations to the same NUMA node. For Guaranteed QoS pods with integer CPU requests, the CPU Manager assigns dedicated cores for optimal performance.',
    tags: ['kubernetes', 'resource-limits', 'topology', 'numa', 'cpu-manager'],
  },
  {
    id: 'k8s-configuration-l3-040',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the impact of setting "shareProcessNamespace: true" on a pod?',
    options: [
      'Containers share the same network namespace',
      'All containers in the pod can see each other processes and the pause container PID 1',
      'Containers share the same user namespace',
      'Only init containers can see other processes',
    ],
    correctAnswer: 1,
    explanation: 'When shareProcessNamespace is true, all containers share the same PID namespace. They can see each other processes, signal them, and /proc is shared. PID 1 is the infrastructure (pause) container.',
    tags: ['kubernetes', 'security-context', 'pid-namespace', 'multi-container'],
  },
  {
    id: 'k8s-configuration-l3-041',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'Why is it important to use "exec" in shell commands used in container command/args?',
    options: [
      'It speeds up command execution',
      'It replaces the shell process with the application, ensuring proper signal propagation (e.g., SIGTERM)',
      'It prevents the shell from consuming memory',
      'It is required syntax for Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Without exec, the application runs as a child of the shell. Signals like SIGTERM are sent to PID 1 (the shell), not the application. Using exec replaces the shell so the application receives signals directly for graceful shutdown.',
    codeSnippet: 'command: ["/bin/sh", "-c"]\nargs:\n  - |\n    source /config/env.sh\n    exec java -jar /app/app.jar',
    tags: ['kubernetes', 'command-args', 'exec', 'signals', 'graceful-shutdown'],
  },
  {
    id: 'k8s-configuration-l3-042',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'How do you safely inject a multi-line certificate PEM file as an environment variable?',
    options: [
      'It is not possible; use volume mounts instead',
      'Store the PEM in a Secret and reference it via secretKeyRef; the application reads the multi-line string',
      'Base64 encode the PEM twice',
      'Use a ConfigMap with the "multiline" annotation',
    ],
    correctAnswer: 1,
    explanation: 'While technically possible via secretKeyRef, multi-line values in environment variables can cause issues with some shells and applications. Volume mounting the certificate file is generally preferred for PEM files.',
    tags: ['kubernetes', 'env-vars', 'secrets', 'certificates', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-043',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'How do you configure a volume mount that is propagated from the host to the container but not vice versa?',
    options: [
      'mountPropagation: None',
      'mountPropagation: HostToContainer',
      'mountPropagation: Bidirectional',
      'mountPropagation: OneWay',
    ],
    correctAnswer: 1,
    explanation: 'HostToContainer propagation means new mounts created on the host within the volume path are visible inside the container, but mounts created inside the container are not visible on the host.',
    tags: ['kubernetes', 'volume-mounts', 'mount-propagation'],
  },
  {
    id: 'k8s-configuration-l3-044',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How can you validate ConfigMap data before deploying it to a cluster?',
    options: [
      'ConfigMaps have built-in schema validation',
      'Use a validating admission webhook or OPA/Gatekeeper policy',
      'Use kubectl validate configmap',
      'The API server automatically validates ConfigMap formats',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes does not validate ConfigMap values. To enforce data format or structure, use a validating admission webhook or OPA/Gatekeeper policies that check ConfigMap data against expected schemas.',
    tags: ['kubernetes', 'configmaps', 'validation', 'admission-webhook', 'opa'],
  },
  {
    id: 'k8s-configuration-l3-045',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How do you audit which pods or service accounts have accessed a particular Secret?',
    options: [
      'Check the Secret annotations',
      'Enable Kubernetes audit logging and filter for Secret access events',
      'Use kubectl describe secret to see access history',
      'Secrets do not support access auditing',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes audit logging records API requests including Secret access. Configure audit policy to log get/list/watch on Secrets at the Request or RequestResponse level to track who accessed which Secrets.',
    codeSnippet: '# Audit policy snippet\nrules:\n  - level: Metadata\n    resources:\n      - group: ""\n        resources: ["secrets"]\n    verbs: ["get", "list", "watch"]',
    tags: ['kubernetes', 'secrets', 'audit', 'security'],
  },
  {
    id: 'k8s-configuration-l3-046',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the CPU Manager static policy affect pod scheduling?',
    options: [
      'It has no effect on scheduling',
      'Only Guaranteed QoS pods with integer CPU requests get dedicated (exclusive) CPU cores',
      'All pods get dedicated CPU cores',
      'CPU Manager replaces the default scheduler',
    ],
    correctAnswer: 1,
    explanation: 'With the static CPU Manager policy, Guaranteed QoS pods with integer CPU requests (e.g., 2, not 2500m) are assigned dedicated CPU cores. Other pods share the remaining CPUs. This provides isolation for latency-sensitive workloads.',
    tags: ['kubernetes', 'resource-limits', 'cpu-manager', 'static-policy'],
  },
  {
    id: 'k8s-configuration-l3-047',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is a user namespace in Kubernetes and what security benefit does it provide?',
    options: [
      'A Kubernetes namespace owned by a specific user',
      'A Linux user namespace that remaps container UIDs, so root inside the container is a non-root user on the host',
      'A namespace for storing user credentials',
      'A RBAC namespace for user permissions',
    ],
    correctAnswer: 1,
    explanation: 'Linux user namespaces remap UIDs/GIDs so that root (UID 0) inside the container maps to a non-root UID on the host. Kubernetes supports this via hostUsers: false, providing defense-in-depth against container escapes.',
    codeSnippet: 'spec:\n  hostUsers: false',
    tags: ['kubernetes', 'security-context', 'user-namespaces', 'container-security'],
  },
  {
    id: 'k8s-configuration-l3-048',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'What is the effect of a LimitRange when a container specifies requests but not limits?',
    options: [
      'The pod is rejected',
      'The LimitRange default limits are applied if set; otherwise only the max constraint is enforced',
      'Limits are set equal to requests',
      'The container gets unlimited resources',
    ],
    correctAnswer: 1,
    explanation: 'If a container specifies requests but not limits, the LimitRange default limits are applied (if configured). If no default is set, the max value acts as the upper bound that would reject pods exceeding it.',
    tags: ['kubernetes', 'limit-ranges', 'defaults', 'requests'],
  },
  {
    id: 'k8s-configuration-l3-049',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How can you allow certain pods to bypass a ResourceQuota?',
    options: [
      'Use the "quota-exempt" annotation on the pod',
      'ResourceQuotas apply to all pods; use a separate namespace without a quota for exempt workloads',
      'Set the pod priority above the quota threshold',
      'Use the "exempt: true" field in the ResourceQuota',
    ],
    correctAnswer: 1,
    explanation: 'ResourceQuotas cannot be bypassed for individual pods within a namespace. The only way to exempt workloads is to run them in a namespace without a quota, or use a quota with sufficient headroom.',
    tags: ['kubernetes', 'resource-quotas', 'exemptions', 'namespaces'],
  },
  {
    id: 'k8s-configuration-l3-050',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'How can you determine from inside a container which node the pod is running on?',
    options: [
      'Read the /etc/nodename file',
      'Use the Downward API to expose spec.nodeName as an environment variable',
      'Query the kubelet API',
      'Parse the hostname',
    ],
    correctAnswer: 1,
    explanation: 'The Downward API field spec.nodeName exposes the name of the node where the pod is scheduled. This is available via fieldRef and can be set as an environment variable.',
    codeSnippet: 'env:\n  - name: NODE_NAME\n    valueFrom:\n      fieldRef:\n        fieldPath: spec.nodeName',
    tags: ['kubernetes', 'downward-api', 'node-name'],
  },
  {
    id: 'k8s-configuration-l3-051',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'A microservice reads configuration at startup only. What is the best ConfigMap update strategy?',
    options: [
      'Use volume-mounted ConfigMaps for live updates',
      'Use immutable ConfigMaps and rolling deployment updates to ensure consistent config across pod replicas',
      'Use envFrom and rely on eventual consistency',
      'Use a sidecar that polls for ConfigMap changes',
    ],
    correctAnswer: 1,
    explanation: 'For applications that read config only at startup, immutable ConfigMaps with versioned names and rolling updates ensure all pods consistently use the same config version during and after the rollout.',
    tags: ['kubernetes', 'configmaps', 'immutable', 'rolling-update', 'strategy'],
  },
  {
    id: 'k8s-configuration-l3-052',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the CSI Secret Store driver and how does it differ from the External Secrets Operator?',
    options: [
      'They are the same tool',
      'CSI Secret Store mounts secrets as volumes from external stores; External Secrets Operator creates Kubernetes Secret objects',
      'CSI Secret Store is for ConfigMaps; External Secrets Operator is for Secrets',
      'CSI Secret Store is deprecated in favor of External Secrets Operator',
    ],
    correctAnswer: 1,
    explanation: 'CSI Secret Store driver mounts secrets directly from external stores (Vault, AWS) as volumes without creating Kubernetes Secrets. External Secrets Operator creates actual Kubernetes Secret objects synced from external stores.',
    tags: ['kubernetes', 'secrets', 'csi', 'external-secrets', 'vault'],
  },
  {
    id: 'k8s-configuration-l3-053',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the impact of not setting resource requests on scheduling efficiency?',
    options: [
      'No impact; the scheduler handles it automatically',
      'Pods with no requests are treated as BestEffort and can be placed on any node, potentially leading to unbalanced clusters',
      'The scheduler rejects pods without requests',
      'Pods without requests always land on the same node',
    ],
    correctAnswer: 1,
    explanation: 'Without requests, pods are BestEffort and do not reserve resources on nodes. The scheduler may stack many on one node since they report zero resource needs, leading to unbalanced resource usage and potential node-level OOM.',
    tags: ['kubernetes', 'resource-limits', 'scheduling', 'best-effort'],
  },
  {
    id: 'k8s-configuration-l3-054',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the AppArmor annotation used for in a pod spec?',
    options: [
      'Configuring application-level firewall rules',
      'Loading an AppArmor profile that restricts the container system calls and file access',
      'Armoring the container image against tampering',
      'Enabling application-level encryption',
    ],
    correctAnswer: 1,
    explanation: 'AppArmor profiles restrict what a container can do at the kernel level, including file access, network access, and capabilities. They are specified as annotations on the pod.',
    codeSnippet: 'metadata:\n  annotations:\n    container.apparmor.security.beta.kubernetes.io/app: localhost/my-profile',
    tags: ['kubernetes', 'security-context', 'apparmor', 'profiles'],
  },
  {
    id: 'k8s-configuration-l3-055',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'How do you run a one-time database migration as part of a Kubernetes deployment?',
    options: [
      'Add the migration command to the main container args',
      'Use an init container with the migration command that runs before the application starts',
      'Use a postStart lifecycle hook',
      'Run the migration on the host using SSH',
    ],
    correctAnswer: 1,
    explanation: 'Init containers run to completion before the main container starts. This is ideal for database migrations, ensuring the migration finishes successfully before the application starts serving traffic.',
    codeSnippet: 'initContainers:\n  - name: migrate\n    image: myapp:latest\n    command: ["./migrate", "--target", "latest"]\n    env:\n      - name: DATABASE_URL\n        valueFrom:\n          secretKeyRef:\n            name: db-credentials\n            key: url',
    tags: ['kubernetes', 'command-args', 'init-containers', 'migration'],
  },
  {
    id: 'k8s-configuration-l3-056',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What is the risk of using envFrom to import all keys from a large ConfigMap?',
    options: [
      'No risk; envFrom is always safe',
      'Keys with invalid environment variable names are silently skipped, potentially causing missing configuration',
      'The container starts slower with more environment variables',
      'envFrom has a limit of 100 keys',
    ],
    correctAnswer: 1,
    explanation: 'envFrom silently skips keys that are not valid Linux environment variable names (e.g., containing dots, dashes, or starting with numbers). This can lead to unexpected missing configuration without clear error messages.',
    tags: ['kubernetes', 'env-vars', 'envfrom', 'gotchas'],
  },
  {
    id: 'k8s-configuration-l3-057',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is a CSI ephemeral volume and when would you use it?',
    options: [
      'A volume backed by Container Storage Interface that is tied to the pod lifecycle',
      'A temporary CSI driver installation',
      'An encrypted ephemeral volume',
      'A volume for CSI driver logs',
    ],
    correctAnswer: 0,
    explanation: 'CSI ephemeral volumes are created and deleted with the pod, like emptyDir, but use a CSI driver for provisioning. They are useful for injecting secrets from external stores or providing specialized temp storage.',
    codeSnippet: 'volumes:\n  - name: secrets\n    csi:\n      driver: secrets-store.csi.k8s.io\n      readOnly: true\n      volumeAttributes:\n        secretProviderClass: "vault-db-creds"',
    tags: ['kubernetes', 'volume-mounts', 'csi', 'ephemeral'],
  },
  {
    id: 'k8s-configuration-l3-058',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'What is the impact of a LimitRange on existing pods when it is created or updated?',
    options: [
      'Existing pods are updated to comply with the new LimitRange',
      'Existing pods are evicted and recreated',
      'The LimitRange only affects newly created pods; existing pods are not affected',
      'Existing pods that violate the LimitRange are terminated',
    ],
    correctAnswer: 2,
    explanation: 'LimitRanges are applied by the admission controller at pod creation time only. Existing pods that were valid when created continue to run even if a new LimitRange would reject them.',
    tags: ['kubernetes', 'limit-ranges', 'admission', 'existing-pods'],
  },
  {
    id: 'k8s-configuration-l3-059',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How do you track ResourceQuota usage trends over time?',
    options: [
      'kubectl describe resourcequota shows historical data',
      'Use Prometheus with kube-state-metrics to collect and graph quota usage metrics',
      'Enable quota logging in the API server',
      'Use kubectl top quota',
    ],
    correctAnswer: 1,
    explanation: 'kube-state-metrics exposes ResourceQuota metrics (kube_resourcequota) that Prometheus can scrape. You can then create Grafana dashboards to visualize quota usage trends and alert on approaching limits.',
    tags: ['kubernetes', 'resource-quotas', 'monitoring', 'prometheus'],
  },
  {
    id: 'k8s-configuration-l3-060',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How do you ensure all pods in a namespace have a minimum security standard?',
    options: [
      'Set SecurityContext defaults at the namespace level',
      'Use Pod Security Admission (PSA) with the "restricted" or "baseline" standard on the namespace',
      'Create a LimitRange for security settings',
      'Use a ConfigMap with security defaults',
    ],
    correctAnswer: 1,
    explanation: 'Pod Security Admission (PSA) enforces Pod Security Standards at the namespace level. Label the namespace to enforce/audit/warn against baseline or restricted profiles.',
    codeSnippet: 'apiVersion: v1\nkind: Namespace\nmetadata:\n  name: production\n  labels:\n    pod-security.kubernetes.io/enforce: restricted\n    pod-security.kubernetes.io/audit: restricted\n    pod-security.kubernetes.io/warn: restricted',
    tags: ['kubernetes', 'security-context', 'psa', 'pod-security-standards'],
  },
  {
    id: 'k8s-configuration-l3-061',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How does Kustomize handle ConfigMap updates differently from plain kubectl apply?',
    options: [
      'There is no difference',
      'Kustomize appends a content hash to the ConfigMap name, so updates create new ConfigMaps and trigger rolling updates',
      'Kustomize encrypts ConfigMap data',
      'Kustomize applies ConfigMaps first before other resources',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize configMapGenerator appends a hash of the ConfigMap contents to the name. When data changes, a new name is generated, causing Deployments to update their pod templates, which triggers a rolling update.',
    tags: ['kubernetes', 'configmaps', 'kustomize', 'hash', 'rolling-update'],
  },
  {
    id: 'k8s-configuration-l3-062',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the best practice for least-privilege Secret access via RBAC?',
    options: [
      'Grant get access on all Secrets in the namespace',
      'Use resourceNames to restrict RBAC to only the specific Secrets a service account needs',
      'Use ClusterRoles for all Secret access',
      'Grant list/watch permissions instead of get',
    ],
    correctAnswer: 1,
    explanation: 'For least-privilege Secret access, create Roles with get permission limited to specific Secret names using the resourceNames field. This prevents access to other Secrets in the namespace.',
    codeSnippet: 'apiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: secret-reader\nrules:\n  - apiGroups: [""]\n    resources: ["secrets"]\n    resourceNames: ["db-credentials"]\n    verbs: ["get"]',
    tags: ['kubernetes', 'secrets', 'rbac', 'least-privilege'],
  },
  {
    id: 'k8s-configuration-l3-063',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the relationship between cgroup v2 and Kubernetes resource management?',
    options: [
      'cgroup v2 is not supported by Kubernetes',
      'cgroup v2 provides unified hierarchy, better memory management, and MemoryQoS features for Kubernetes pods',
      'cgroup v2 only affects CPU scheduling',
      'cgroup v2 replaces the need for resource limits',
    ],
    correctAnswer: 1,
    explanation: 'cgroup v2 provides a unified hierarchy for CPU, memory, and I/O. It enables MemoryQoS features like memory.min/memory.high for Kubernetes pods, offering more granular memory management than cgroup v1.',
    tags: ['kubernetes', 'resource-limits', 'cgroup-v2', 'memory-qos'],
  },
  {
    id: 'k8s-configuration-l3-064',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'What is the recommended approach for PriorityClass management in a multi-tenant cluster?',
    options: [
      'Let each team create their own PriorityClasses',
      'Use a small set of well-defined PriorityClasses managed centrally, with RBAC preventing tenants from creating their own',
      'Disable preemption entirely',
      'Use the same PriorityClass for all workloads',
    ],
    correctAnswer: 1,
    explanation: 'Centrally managed PriorityClasses prevent priority inflation. Define tiers (system-critical, high, default, low) and use RBAC to restrict PriorityClass creation to cluster admins. Tenants select from predefined classes.',
    tags: ['kubernetes', 'priority-classes', 'multi-tenancy', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l3-065',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'How do you conditionally include environment variables based on a feature flag?',
    options: [
      'Use if-else syntax in the pod spec',
      'Kubernetes does not support conditional env vars; use Helm/Kustomize templating or application-level feature flags',
      'Use the "conditional" field in env',
      'Use annotations to toggle env vars',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes manifests do not support conditionals. Use Helm templates ({{ if }}) or Kustomize patches to conditionally include/exclude env vars. Alternatively, always include the env var and handle the flag in application code.',
    tags: ['kubernetes', 'env-vars', 'helm', 'kustomize', 'feature-flags'],
  },
  {
    id: 'k8s-configuration-l3-066',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What happens when a ConfigMap mounted via subPath is updated?',
    options: [
      'The file is updated after the kubelet sync period',
      'The file is never updated; subPath mounts do not receive live updates',
      'The pod is restarted automatically',
      'Only the metadata is updated, not the content',
    ],
    correctAnswer: 1,
    explanation: 'A known limitation of subPath is that files mounted this way are not updated when the underlying ConfigMap or Secret changes. This is because subPath creates a bind mount of the individual file rather than using the symlink mechanism.',
    tags: ['kubernetes', 'volume-mounts', 'subpath', 'configmaps', 'limitation'],
  },
  {
    id: 'k8s-configuration-l3-067',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'What is a cross-namespace ResourceQuota and how is it achieved?',
    options: [
      'A ResourceQuota can directly span multiple namespaces',
      'Cross-namespace quotas do not exist natively; use a custom controller or hierarchical namespaces (HNC) with quota inheritance',
      'Use a ClusterResourceQuota CRD',
      'Prefix the quota name with the namespaces',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes ResourceQuotas are namespace-scoped. For cross-namespace or hierarchical quotas, use tools like Hierarchical Namespace Controller (HNC) or OpenShift ClusterResourceQuotas, or implement a custom controller.',
    tags: ['kubernetes', 'resource-quotas', 'cross-namespace', 'hnc'],
  },
  {
    id: 'k8s-configuration-l3-068',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What Linux capability is needed for a container to use raw sockets (e.g., for ping)?',
    options: [
      'SYS_ADMIN',
      'NET_RAW',
      'NET_ADMIN',
      'SYS_RAWIO',
    ],
    correctAnswer: 1,
    explanation: 'NET_RAW capability is needed for raw socket operations like ping (ICMP). The restricted Pod Security Standard drops this capability. If you need ping in a container, you must add NET_RAW explicitly.',
    tags: ['kubernetes', 'security-context', 'capabilities', 'net-raw'],
  },
  {
    id: 'k8s-configuration-l3-069',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How do you handle large configuration files that exceed the ConfigMap 1MiB limit?',
    options: [
      'Split the configuration across multiple ConfigMaps and merge in the application',
      'Increase the ConfigMap size limit',
      'Use a PersistentVolume to store the file and an init container to download it',
      'Both A and C are valid approaches depending on the use case',
    ],
    correctAnswer: 3,
    explanation: 'For large configs, you can split across multiple ConfigMaps or store the file externally (PV, S3, etc.) and fetch it with an init container. The ConfigMap size limit (1MiB) cannot be increased.',
    tags: ['kubernetes', 'configmaps', 'large-files', 'workarounds'],
  },
  {
    id: 'k8s-configuration-l3-070',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How do you implement short-lived credentials using projected service account tokens?',
    options: [
      'Use the default service account token',
      'Use a projected volume with a serviceAccountToken source and set an expirationSeconds',
      'Create a CronJob that rotates Secrets',
      'Use a sidecar that refreshes tokens',
    ],
    correctAnswer: 1,
    explanation: 'Projected service account tokens are bound, time-limited tokens that can specify audience and expiration. They are more secure than long-lived auto-mounted tokens.',
    codeSnippet: 'volumes:\n  - name: token\n    projected:\n      sources:\n        - serviceAccountToken:\n            path: token\n            expirationSeconds: 3600\n            audience: "vault"',
    tags: ['kubernetes', 'secrets', 'service-accounts', 'projected-tokens'],
  },
  {
    id: 'k8s-configuration-l3-071',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is memory.high in the context of cgroup v2 and MemoryQoS?',
    options: [
      'The absolute maximum memory a container can use',
      'A memory throttling threshold that triggers reclaim pressure before the hard limit (memory.max)',
      'The highest priority memory allocation',
      'A memory tier for high-performance workloads',
    ],
    correctAnswer: 1,
    explanation: 'memory.high is a soft memory limit in cgroup v2. When a container exceeds memory.high, it faces memory reclaim pressure (throttling) but is not killed. It is killed only when exceeding memory.max (the hard limit).',
    tags: ['kubernetes', 'resource-limits', 'cgroup-v2', 'memory-qos', 'memory-high'],
  },
  {
    id: 'k8s-configuration-l3-072',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'What is the relationship between LimitRange type: Pod min/max and type: Container min/max?',
    options: [
      'They are the same and cannot coexist',
      'Pod constraints apply to the sum of all containers; Container constraints apply individually to each container',
      'Pod constraints override Container constraints',
      'Container constraints must be within Pod constraints',
    ],
    correctAnswer: 1,
    explanation: 'A Pod-type LimitRange limits the sum of all container resources in a pod. A Container-type LimitRange limits each individual container. Both can coexist; a pod must satisfy both constraints.',
    tags: ['kubernetes', 'limit-ranges', 'pod-type', 'container-type'],
  },
  {
    id: 'k8s-configuration-l3-073',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'How do you make pod labels available to a sidecar container for Prometheus metrics labeling?',
    options: [
      'Labels are automatically available as environment variables',
      'Mount a downwardAPI volume with labels and have the sidecar read the labels file',
      'Use a ConfigMap to mirror labels',
      'Query the Kubernetes API for pod labels',
    ],
    correctAnswer: 1,
    explanation: 'Mount a downwardAPI volume exposing metadata.labels as a file. The sidecar container reads this file to extract pod labels for metric labeling. This avoids needing API access.',
    tags: ['kubernetes', 'downward-api', 'labels', 'sidecar', 'prometheus'],
  },
  {
    id: 'k8s-configuration-l3-074',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the difference between the "baseline" and "restricted" Pod Security Standards?',
    options: [
      'They are identical; restricted is just an alias',
      'Baseline prevents known privilege escalations; restricted adds further requirements like non-root, drop ALL capabilities, and seccomp',
      'Restricted only applies to system namespaces',
      'Baseline is more restrictive than restricted',
    ],
    correctAnswer: 1,
    explanation: 'Baseline prevents known privilege escalations (no hostNetwork, privileged, etc.). Restricted adds: must run as non-root, drop ALL capabilities, read-only root filesystem recommendation, and seccomp RuntimeDefault or Localhost.',
    tags: ['kubernetes', 'security-context', 'pod-security-standards', 'baseline', 'restricted'],
  },
  {
    id: 'k8s-configuration-l3-075',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'How do you debug a CrashLoopBackOff by overriding the container command?',
    options: [
      'Use kubectl logs to see the crash reason',
      'Override the command to sleep infinity, then exec into the container to debug',
      'Delete and recreate the pod',
      'Increase the restart backoff timer',
    ],
    correctAnswer: 1,
    explanation: 'A common debugging technique is to override the command with sleep infinity to prevent the crash. Then use kubectl exec to enter the container and manually run the original command to see the error.',
    codeSnippet: 'kubectl run debug --image=myapp:v1 --overrides=\'{\n  "spec": {\n    "containers": [{\n      "name": "debug",\n      "image": "myapp:v1",\n      "command": ["sleep", "infinity"]\n    }]\n  }\n}\'',
    tags: ['kubernetes', 'command-args', 'debugging', 'crashloopbackoff'],
  },
  {
    id: 'k8s-configuration-l3-076',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What is the maximum total size of all environment variables in a container?',
    options: [
      '1 MiB (same as ConfigMap limit)',
      '32 KiB (Linux kernel limit for the environment block)',
      'There is no limit; it depends on available memory',
      '128 bytes per variable',
    ],
    correctAnswer: 2,
    explanation: 'Kubernetes itself does not impose a limit on total environment variable size. However, the Linux kernel has a per-process limit (MAX_ARG_STRLEN, typically 128KB per variable, and total stack size limit). In practice, very large env blocks can cause startup failures.',
    tags: ['kubernetes', 'env-vars', 'limits', 'linux'],
  },
  {
    id: 'k8s-configuration-l3-077',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'How do you use ephemeral volumes for per-pod unique storage in a Deployment?',
    options: [
      'Use a StatefulSet with PVCs instead',
      'Use a generic ephemeral volume with a volumeClaimTemplate in the pod spec',
      'Use emptyDir with unique names',
      'Use hostPath with the pod UID',
    ],
    correctAnswer: 1,
    explanation: 'Generic ephemeral volumes use a volumeClaimTemplate directly in the pod spec (not StatefulSet). Each pod gets its own PVC that is automatically deleted when the pod is removed.',
    codeSnippet: 'volumes:\n  - name: data\n    ephemeral:\n      volumeClaimTemplate:\n        spec:\n          accessModes: ["ReadWriteOnce"]\n          storageClassName: fast\n          resources:\n            requests:\n              storage: 10Gi',
    tags: ['kubernetes', 'volume-mounts', 'ephemeral', 'pvc'],
  },
  {
    id: 'k8s-configuration-l3-078',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How do you implement a tenant cost allocation model using ResourceQuotas and labels?',
    options: [
      'ResourceQuotas automatically track costs',
      'Use ResourceQuota usage metrics combined with cost-per-resource pricing and namespace labels for team attribution',
      'Use the cost annotation on ResourceQuotas',
      'Kubernetes has built-in cost management',
    ],
    correctAnswer: 1,
    explanation: 'Combine ResourceQuota usage metrics (from kube-state-metrics) with per-resource pricing. Namespace labels identify the team/project. Tools like Kubecost automate this by correlating resource consumption with cloud pricing.',
    tags: ['kubernetes', 'resource-quotas', 'cost-allocation', 'multi-tenancy'],
  },
  {
    id: 'k8s-configuration-l3-079',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What happens if a container with runAsNonRoot: true uses an image with a USER directive set to root?',
    options: [
      'The container runs as root anyway',
      'The container fails to start with a "must not run as root" error',
      'The UID is automatically changed to 65534 (nobody)',
      'The runAsNonRoot setting is ignored',
    ],
    correctAnswer: 1,
    explanation: 'If runAsNonRoot is true and the image specifies USER root (or UID 0) without a runAsUser override in the pod spec, the container fails to start. You must either fix the image or set runAsUser to a non-root UID.',
    tags: ['kubernetes', 'security-context', 'non-root', 'image', 'troubleshooting'],
  },
  {
    id: 'k8s-configuration-l3-080',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How do you implement a blue-green configuration switch using ConfigMaps?',
    options: [
      'Update the ConfigMap and wait for propagation',
      'Maintain two ConfigMaps (blue and green) and switch the Deployment pod template to reference the target ConfigMap',
      'Use a ConfigMap with a "color" key',
      'Create two namespaces, one for blue and one for green',
    ],
    correctAnswer: 1,
    explanation: 'Maintain separate ConfigMaps for each configuration version. Switch the Deployment pod template to reference the desired ConfigMap. This provides instant rollback by switching back to the previous ConfigMap.',
    tags: ['kubernetes', 'configmaps', 'blue-green', 'deployment-strategy'],
  },
  {
    id: 'k8s-configuration-l3-081',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How do you monitor if pods are being throttled due to CPU limits?',
    options: [
      'Check kubectl top pods',
      'Monitor container_cpu_cfs_throttled_seconds_total metric from cAdvisor/Prometheus',
      'Check the pod events',
      'CPU throttling is not observable',
    ],
    correctAnswer: 1,
    explanation: 'The container_cpu_cfs_throttled_seconds_total and container_cpu_cfs_throttled_periods_total metrics from cAdvisor (exposed via Prometheus) show CFS throttling. High throttling indicates CPU limits may be too low.',
    tags: ['kubernetes', 'resource-limits', 'cpu', 'throttling', 'monitoring'],
  },
  {
    id: 'k8s-configuration-l3-082',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the SOPS approach to managing Secrets in Git?',
    options: [
      'SOPS replaces Kubernetes Secrets with ConfigMaps',
      'SOPS encrypts specific values in YAML/JSON files while leaving keys and structure visible for code review',
      'SOPS stores Secrets in a separate encrypted repository',
      'SOPS converts Secrets to base64 format for Git storage',
    ],
    correctAnswer: 1,
    explanation: 'SOPS (Secrets OPerationS) encrypts only the values in YAML/JSON files, keeping keys and structure in plain text. This allows meaningful Git diffs and code review while keeping sensitive data encrypted.',
    tags: ['kubernetes', 'secrets', 'sops', 'encryption', 'gitops'],
  },
  {
    id: 'k8s-configuration-l3-083',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'How do you audit which pods in a namespace are running without explicit resource requests?',
    options: [
      'kubectl get pods --show-resources',
      'Use kubectl with jsonpath or jq to filter pods where containers lack resource requests',
      'Check the LimitRange events',
      'Use kubectl describe namespace',
    ],
    correctAnswer: 1,
    explanation: 'Use kubectl get pods with JSON output and jq to filter containers without resource specs. This helps identify pods relying on LimitRange defaults that might need explicit configuration.',
    codeSnippet: 'kubectl get pods -o json | jq \'.items[] | select(.spec.containers[] | .resources.requests == null) | .metadata.name\'',
    tags: ['kubernetes', 'limit-ranges', 'resource-limits', 'auditing', 'kubectl'],
  },
  {
    id: 'k8s-configuration-l3-084',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'What is the limitation of the Downward API for multi-container pods when exposing resource information?',
    options: [
      'There are no limitations',
      'Resource fields default to the current container; cross-container references require explicit containerName and are available only in env vars, not volume files',
      'Only the first container can use the Downward API',
      'Multi-container pods cannot use the Downward API at all',
    ],
    correctAnswer: 1,
    explanation: 'In env vars, resourceFieldRef defaults to the enclosing container but can reference another via containerName. In downwardAPI volumes, only the current container resources can be referenced; cross-container resource references are not supported in volumes.',
    tags: ['kubernetes', 'downward-api', 'multi-container', 'limitations'],
  },
  {
    id: 'k8s-configuration-l3-085',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'How do you prevent non-admin users from using high-priority PriorityClasses?',
    options: [
      'PriorityClasses are always available to all users',
      'Use a validating admission webhook or policy engine to restrict PriorityClass usage based on the requesting user or namespace',
      'Set the "restricted" field on the PriorityClass',
      'Remove the PriorityClass from the API',
    ],
    correctAnswer: 1,
    explanation: 'RBAC cannot directly control which PriorityClass a pod references. Use a validating admission webhook, OPA/Gatekeeper, or Kyverno to enforce policies on which namespaces or service accounts can use specific PriorityClasses.',
    tags: ['kubernetes', 'priority-classes', 'admission-webhook', 'policy'],
  },
  {
    id: 'k8s-configuration-l3-086',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the "Unconfined" seccomp profile type and when might it be appropriate?',
    options: [
      'It applies the most restrictive profile',
      'It disables seccomp filtering entirely, allowing all system calls; appropriate only for debugging or specific system-level workloads',
      'It uses the container runtime default',
      'It is not a valid profile type',
    ],
    correctAnswer: 1,
    explanation: 'Unconfined disables seccomp for the container, allowing all system calls. It should only be used for debugging or system-level containers that need unrestricted syscall access. Production workloads should use RuntimeDefault or Localhost.',
    tags: ['kubernetes', 'security-context', 'seccomp', 'unconfined'],
  },
  {
    id: 'k8s-configuration-l3-087',
    language: 'kubernetes',
    level: 'level3',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'How do you implement a graceful shutdown sequence in a container that uses command/args?',
    options: [
      'Set terminationGracePeriodSeconds to a high value',
      'Use exec to ensure the app process is PID 1, combined with proper SIGTERM handling in the application and a preStop hook if needed',
      'Use the "graceful" annotation',
      'Kubernetes automatically handles graceful shutdown',
    ],
    correctAnswer: 1,
    explanation: 'For graceful shutdown: use exec so the app is PID 1 and receives SIGTERM directly. Implement SIGTERM handling in the app. Use preStop hooks for additional cleanup. Set terminationGracePeriodSeconds to allow enough time.',
    tags: ['kubernetes', 'command-args', 'graceful-shutdown', 'signals', 'lifecycle'],
  },
];

export default questions;
