import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-configuration-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How does the kubelet watch-based ConfigMap propagation work internally, and what determines the maximum propagation delay?',
    options: [
      'The kubelet receives push notifications from the API server',
      'The delay is bounded by the kubelet sync frequency + watch cache propagation delay + TTL of the ConfigMap cache in the kubelet',
      'ConfigMap updates are instant through shared memory',
      'The delay is fixed at 60 seconds',
    ],
    correctAnswer: 1,
    explanation: 'The total delay is: kubelet sync period (default 1m) + API server watch cache propagation delay + kubelet ConfigMap/Secret cache TTL (configurable via --sync-frequency). With watch-based caching, the kubelet watches for changes rather than polling.',
    tags: ['kubernetes', 'configmaps', 'kubelet', 'internals', 'propagation'],
  },
  {
    id: 'k8s-configuration-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the atomic update mechanism used by the kubelet for ConfigMap volume mounts?',
    options: [
      'The kubelet uses file locking to update files atomically',
      'The kubelet creates a new timestamped directory, writes updated files, then atomically swaps the "..data" symlink',
      'Files are updated in-place with write barriers',
      'The kubelet unmounts and remounts the volume',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet creates a new directory (e.g., ..2024_01_01_12_00_00.123456), writes all files, then atomically renames the ..data symlink to point to the new directory. This ensures applications see either all old or all new data, never a partial update.',
    tags: ['kubernetes', 'configmaps', 'kubelet', 'atomic-update', 'symlinks'],
  },
  {
    id: 'k8s-configuration-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the KMS v2 encryption provider differ from KMS v1 for Secret encryption at rest?',
    options: [
      'KMS v2 uses a different encryption algorithm',
      'KMS v2 uses a key hierarchy with a local DEK seed, reducing KMS calls to once per API server restart instead of per-Secret encryption',
      'KMS v2 does not use external KMS providers',
      'KMS v2 only encrypts ConfigMaps, not Secrets',
    ],
    correctAnswer: 1,
    explanation: 'KMS v2 introduces a key hierarchy: the external KMS encrypts a seed (DEK seed), and the API server derives per-object DEKs from this seed locally. This dramatically reduces external KMS API calls from per-encryption to per-restart.',
    tags: ['kubernetes', 'secrets', 'encryption', 'kms-v2', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the EncryptionConfiguration "write" resource definition and how does it affect existing Secrets?',
    options: [
      'It encrypts all existing Secrets automatically',
      'The first provider in the list is used for writing new Secrets; existing Secrets remain encrypted with their original provider until re-written',
      'It configures write-ahead logging for Secrets',
      'It determines which users can write Secrets',
    ],
    correctAnswer: 1,
    explanation: 'In EncryptionConfiguration, the first provider is the write provider. Existing Secrets encrypted with older providers are read using any listed provider but re-encrypted with the first provider only when updated. To re-encrypt all, run: kubectl get secrets --all-namespaces -o json | kubectl replace -f -',
    codeSnippet: 'apiVersion: apiserver.config.k8s.io/v1\nkind: EncryptionConfiguration\nresources:\n  - resources:\n      - secrets\n    providers:\n      - aescbc:\n          keys:\n            - name: key2\n              secret: <new-base64-key>\n      - aescbc:\n          keys:\n            - name: key1\n              secret: <old-base64-key>\n      - identity: {}',
    tags: ['kubernetes', 'secrets', 'encryption', 'key-rotation', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the CFS bandwidth control mechanism enforce CPU limits, and what is the CFS quota period?',
    options: [
      'CFS uses nice values to limit CPU',
      'CFS enforces limits using a quota/period mechanism: default 100ms period, with quota proportional to the limit. A 500m limit gets 50ms quota per 100ms period',
      'CFS limits are enforced per-second',
      'CFS uses CPU pinning for limit enforcement',
    ],
    correctAnswer: 1,
    explanation: 'CFS bandwidth control uses a period (default 100ms, configurable via --cpu-cfs-quota-period kubelet flag) and a quota. For a 500m limit: quota = 50ms per 100ms period. If the process uses 50ms of CPU in a period, it is throttled for the remainder.',
    tags: ['kubernetes', 'resource-limits', 'cpu', 'cfs', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the impact of CFS quota period length on latency-sensitive applications?',
    options: [
      'Period length has no impact on latency',
      'A shorter period (e.g., 1ms) reduces burstiness but increases scheduling overhead; a longer period allows larger bursts but may cause longer throttling pauses',
      'Longer periods always improve performance',
      'The period only affects throughput, not latency',
    ],
    correctAnswer: 1,
    explanation: 'With a 100ms default period, a throttled container might pause for up to ~100ms. Reducing the period (e.g., 5ms) reduces max pause duration but increases context switch overhead. Google recommended reducing it for latency-sensitive workloads.',
    tags: ['kubernetes', 'resource-limits', 'cpu', 'cfs', 'latency'],
  },
  {
    id: 'k8s-configuration-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does the Linux seccomp filter work at the kernel level for Kubernetes containers?',
    options: [
      'Seccomp intercepts syscalls at the application level',
      'Seccomp uses BPF (Berkeley Packet Filter) programs loaded into the kernel to filter system calls before they are executed, returning ALLOW, KILL, ERRNO, or TRACE',
      'Seccomp modifies system call arguments',
      'Seccomp runs as a userspace daemon',
    ],
    correctAnswer: 1,
    explanation: 'Seccomp uses BPF (specifically seccomp-BPF) programs that the kernel evaluates for each system call. The filter inspects the syscall number and arguments, then returns an action: ALLOW, KILL_PROCESS, ERRNO (return error), LOG, or TRACE.',
    tags: ['kubernetes', 'security-context', 'seccomp', 'bpf', 'kernel'],
  },
  {
    id: 'k8s-configuration-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How do you create a custom seccomp profile that allows only the system calls your application needs?',
    options: [
      'Manually list all needed syscalls in a JSON profile',
      'Use tools like strace or oci-seccomp-bpf-hook to trace application syscalls, generate a profile, then deploy it to nodes and reference it as a Localhost profile',
      'The RuntimeDefault profile is always sufficient',
      'Modify the kernel to disable unneeded syscalls',
    ],
    correctAnswer: 1,
    explanation: 'Use strace, oci-seccomp-bpf-hook, or the Security Profiles Operator to record which syscalls your application makes. Generate a custom JSON profile, deploy it to nodes (or use Security Profiles Operator), and reference it as Localhost type.',
    codeSnippet: '# Custom seccomp profile example\n{\n  "defaultAction": "SCMP_ACT_ERRNO",\n  "architectures": ["SCMP_ARCH_X86_64"],\n  "syscalls": [\n    {\n      "names": ["read", "write", "close", "fstat",\n                 "mmap", "mprotect", "munmap",\n                 "brk", "rt_sigaction", "exit_group"],\n      "action": "SCMP_ACT_ALLOW"\n    }\n  ]\n}',
    tags: ['kubernetes', 'security-context', 'seccomp', 'custom-profile'],
  },
  {
    id: 'k8s-configuration-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What is the internal mechanism by which the kubelet resolves $(VAR_NAME) references in container env/args?',
    options: [
      'The container runtime resolves them at process start',
      'The kubelet performs string substitution during pod spec processing, replacing $(VAR_NAME) with resolved values before passing to the container runtime',
      'The shell inside the container resolves them',
      'The API server resolves them during admission',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet resolves $(VAR_NAME) references in env values and args fields by performing string substitution during pod spec processing. Variables are resolved in definition order. If a variable cannot be resolved, the literal $(VAR_NAME) string is kept.',
    tags: ['kubernetes', 'env-vars', 'kubelet', 'internals', 'resolution'],
  },
  {
    id: 'k8s-configuration-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is the Bidirectional mount propagation and what are its security implications?',
    options: [
      'It allows volumes to be shared between pods',
      'Mounts created inside the container are visible on the host and vice versa; it requires the container to be privileged and can affect the host filesystem',
      'It synchronizes volume data bidirectionally',
      'It enables two-way replication of volume contents',
    ],
    correctAnswer: 1,
    explanation: 'Bidirectional mount propagation means new mounts in the container appear on the host and vice versa. It requires CAP_SYS_ADMIN (typically privileged). This is used by CSI node plugins and FlexVolume but poses a security risk if misused.',
    tags: ['kubernetes', 'volume-mounts', 'mount-propagation', 'bidirectional', 'security'],
  },
  {
    id: 'k8s-configuration-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the memory manager in the kubelet interact with NUMA topology for Guaranteed pods?',
    options: [
      'Memory manager does not consider NUMA topology',
      'The memory manager allocates memory from the same NUMA node as assigned CPUs for Guaranteed QoS pods with static CPU manager, ensuring data locality',
      'NUMA is only relevant for GPU workloads',
      'The memory manager spreads memory across all NUMA nodes',
    ],
    correctAnswer: 1,
    explanation: 'With the Topology Manager single-numa-node policy and Memory Manager, Guaranteed pods get memory and CPUs from the same NUMA node. This provides data locality and predictable performance for latency-sensitive workloads.',
    tags: ['kubernetes', 'resource-limits', 'memory-manager', 'numa', 'topology'],
  },
  {
    id: 'k8s-configuration-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What happens to ConfigMap volume mounts during a kubelet restart?',
    options: [
      'All ConfigMap volumes are lost',
      'ConfigMap volumes survive the kubelet restart because they are managed by the container runtime; the kubelet reconciles on restart',
      'Pods using ConfigMap volumes are killed',
      'ConfigMap volumes are remounted with stale data',
    ],
    correctAnswer: 1,
    explanation: 'Container runtime manages the actual mounts. When kubelet restarts, it discovers existing containers and volumes during reconciliation. ConfigMap volumes remain intact. The kubelet resumes syncing ConfigMap updates after restart.',
    tags: ['kubernetes', 'configmaps', 'kubelet', 'restart', 'reconciliation'],
  },
  {
    id: 'k8s-configuration-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the kubelet handle Secret distribution to pods, and what caching strategies are available?',
    options: [
      'Secrets are pulled directly from etcd by each pod',
      'The kubelet fetches Secrets from the API server and caches them; caching strategies include watch-based (default), TTL-based, or uncached',
      'Secrets are pushed to kubelets by the controller manager',
      'Each container runtime independently fetches Secrets',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet fetches Secrets via the API server. Caching strategies (configured via --configmap-and-secret-cache): watch-based (efficient, default), TTL-based (less API connections), or always-fetch (no cache, high API load).',
    tags: ['kubernetes', 'secrets', 'kubelet', 'caching', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the MemoryQoS feature in Kubernetes with cgroup v2, and how does it map memory requests to cgroup settings?',
    options: [
      'MemoryQoS is identical to standard memory limits',
      'MemoryQoS maps memory requests to memory.min (guaranteed minimum) and computes memory.high as a throttling threshold between requests and limits',
      'MemoryQoS only affects swap usage',
      'MemoryQoS replaces the OOM killer with throttling',
    ],
    correctAnswer: 1,
    explanation: 'With MemoryQoS and cgroup v2: memory.min = pod memory request (protected from reclaim), memory.high = throttling threshold (reduces reclaim pressure before OOM), memory.max = memory limit (OOM kill). This provides smoother memory management.',
    tags: ['kubernetes', 'resource-limits', 'memory-qos', 'cgroup-v2', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How do Linux user namespaces (hostUsers: false) remap UIDs in Kubernetes?',
    options: [
      'UIDs are mapped 1:1 with a different prefix',
      'The container UID range (0-65535) is mapped to a high, non-overlapping host UID range (e.g., 65536-131071), so container root (0) maps to host UID 65536',
      'All container UIDs map to nobody on the host',
      'UIDs are randomized on each pod start',
    ],
    correctAnswer: 1,
    explanation: 'With user namespaces, Kubernetes allocates a non-overlapping UID/GID range per pod on the host (e.g., 65536-131071). UID 0 in the container maps to UID 65536 on the host. This means even if an attacker escapes the container as root, they are unprivileged on the host.',
    tags: ['kubernetes', 'security-context', 'user-namespaces', 'uid-mapping', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'How does the LimitRanger admission controller interact with the mutating and validating admission phases?',
    options: [
      'LimitRanger only validates',
      'LimitRanger acts as both mutating (applies defaults) and validating (enforces min/max); it mutates first to apply defaults, then validates the final spec',
      'LimitRanger only mutates',
      'LimitRanger runs after all other admission controllers',
    ],
    correctAnswer: 1,
    explanation: 'The LimitRanger admission plugin runs in both phases: during mutation, it applies default requests/limits to containers without them. During validation, it checks that all resource values fall within the min/max constraints.',
    tags: ['kubernetes', 'limit-ranges', 'admission-controller', 'mutating', 'validating'],
  },
  {
    id: 'k8s-configuration-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How does the ResourceQuota admission controller handle race conditions when multiple pod creations happen simultaneously?',
    options: [
      'It uses distributed locks on etcd',
      'It uses optimistic concurrency: each quota update includes a resourceVersion, and conflicting updates are retried with the latest quota state',
      'It serializes all pod creation requests',
      'It uses eventual consistency and may temporarily exceed quotas',
    ],
    correctAnswer: 1,
    explanation: 'The ResourceQuota controller uses optimistic concurrency with resourceVersion in etcd. When updating usage, it reads the current quota, computes new usage, and writes with the resourceVersion. If another update changed the quota, it retries.',
    tags: ['kubernetes', 'resource-quotas', 'admission-controller', 'concurrency', 'etcd'],
  },
  {
    id: 'k8s-configuration-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'What are the internal implementation differences between Downward API env vars and volume files?',
    options: [
      'They use the same mechanism internally',
      'Env vars are resolved once by the kubelet at container creation; volume files use a projected volume plugin that can receive updates via the kubelet sync loop',
      'Volume files are resolved at pod scheduling time',
      'Env vars use the watch API; volumes use polling',
    ],
    correctAnswer: 1,
    explanation: 'For env vars, the kubelet resolves fieldRef/resourceFieldRef values once during container creation and passes them to the runtime. For volumes, the downwardAPI volume plugin runs during kubelet sync, re-reading label/annotation changes from the API server.',
    tags: ['kubernetes', 'downward-api', 'kubelet', 'internals', 'implementation'],
  },
  {
    id: 'k8s-configuration-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'How does the kubelet construct the OCI runtime spec for container command and args?',
    options: [
      'It passes them directly to the shell',
      'The kubelet maps command to OCI Process.Args[0:] (overriding image Entrypoint+Cmd), with args appended, and passes the merged array to the container runtime via CRI',
      'The container runtime determines the command independently',
      'The kubelet creates a wrapper script',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet merges command and args into a single Process.Args array in the OCI spec. If command is set, it replaces the image Entrypoint. Args replace Cmd. The CRI runtime (containerd/CRI-O) uses this array directly without shell interpretation.',
    tags: ['kubernetes', 'command-args', 'kubelet', 'oci', 'cri', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'How does the scheduler preemption algorithm work internally?',
    options: [
      'It randomly selects pods to evict',
      'It evaluates each node, finds the minimum set of lower-priority victims that would free enough resources, preferring victims that minimize PDB violations and priority impact',
      'It evicts all lower-priority pods on the selected node',
      'It delegates preemption decisions to the kubelet',
    ],
    correctAnswer: 1,
    explanation: 'The scheduler preemption algorithm: 1) filters nodes where preemption could help, 2) for each node, finds the minimum set of lower-priority victims using a greedy algorithm, 3) ranks candidates by: fewer PDB violations, lowest total victim priority, fewest victims, latest start time.',
    tags: ['kubernetes', 'priority-classes', 'preemption', 'scheduler', 'algorithm'],
  },
  {
    id: 'k8s-configuration-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How does the API server watch mechanism for ConfigMaps scale, and what are the implications for large clusters?',
    options: [
      'Each watch creates a goroutine; clusters with millions of ConfigMaps have no issues',
      'The API server caches watch events in a watch cache (ring buffer); many concurrent watches on the same resource type share the cache, but high churn can cause watch bookmark events and reconnections',
      'ConfigMap watches do not use server resources',
      'The API server delegates watches to etcd directly',
    ],
    correctAnswer: 1,
    explanation: 'The API server watch cache uses a ring buffer per resource type. Multiple watchers share it. Under high churn, old events may be evicted, causing watches to receive bookmark events or need re-lists. Immutable ConfigMaps reduce watch load significantly.',
    tags: ['kubernetes', 'configmaps', 'api-server', 'watch-cache', 'scalability'],
  },
  {
    id: 'k8s-configuration-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What are the attack vectors for Kubernetes Secrets and how do you mitigate each?',
    options: [
      'Secrets are fully secure by default',
      'Vectors include: etcd access (mitigate with encryption at rest), API access (mitigate with RBAC), node access (mitigate with node restriction admission), log exposure (mitigate with audit policy); defense in depth is needed',
      'Only RBAC is needed to secure Secrets',
      'Secrets are only vulnerable in transit',
    ],
    correctAnswer: 1,
    explanation: 'Attack vectors: 1) etcd direct access → encrypt at rest, restrict etcd access. 2) API server → RBAC, audit logging. 3) Node compromise → NodeRestriction admission, limit Secret access per node. 4) Log/env exposure → avoid logging Secrets, use volumes. 5) Git → Sealed Secrets/SOPS.',
    tags: ['kubernetes', 'secrets', 'security', 'attack-vectors', 'defense-in-depth'],
  },
  {
    id: 'k8s-configuration-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the kubelet eviction manager use resource signals and thresholds?',
    options: [
      'It monitors only memory usage',
      'It monitors multiple signals (memory.available, nodefs.available, imagefs.available, pid.available) and triggers soft/hard evictions based on configurable thresholds and grace periods',
      'It relies on the OOM killer exclusively',
      'The scheduler handles all eviction decisions',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet eviction manager monitors signals: memory.available, nodefs.available, imagefs.available, pid.available. Hard thresholds trigger immediate eviction. Soft thresholds have a grace period. BestEffort pods are evicted first, then Burstable pods exceeding requests.',
    codeSnippet: '# Kubelet eviction flags\n--eviction-hard=memory.available<100Mi,nodefs.available<10%\n--eviction-soft=memory.available<200Mi\n--eviction-soft-grace-period=memory.available=30s',
    tags: ['kubernetes', 'resource-limits', 'eviction', 'kubelet', 'thresholds'],
  },
  {
    id: 'k8s-configuration-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does the Security Profiles Operator (SPO) simplify seccomp and AppArmor management?',
    options: [
      'It replaces the kubelet security features',
      'SPO manages custom profiles as CRDs, automatically distributes them to nodes, supports profile recording from running workloads, and integrates with the webhook for validation',
      'SPO only works with SELinux',
      'SPO is a replacement for Pod Security Admission',
    ],
    correctAnswer: 1,
    explanation: 'The Security Profiles Operator: 1) manages SeccompProfile and AppArmorProfile CRDs, 2) syncs profiles to nodes automatically, 3) supports ProfileRecording to capture syscalls from running containers, 4) provides a webhook for profile validation.',
    codeSnippet: 'apiVersion: security-profiles-operator.x-k8s.io/v1beta1\nkind: SeccompProfile\nmetadata:\n  name: my-app-profile\nspec:\n  defaultAction: SCMP_ACT_ERRNO\n  syscalls:\n    - action: SCMP_ACT_ALLOW\n      names: ["read", "write", "exit_group"]',
    tags: ['kubernetes', 'security-context', 'seccomp', 'security-profiles-operator'],
  },
  {
    id: 'k8s-configuration-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What is the ordering guarantee for environment variable resolution in a multi-container pod?',
    options: [
      'All containers resolve env vars simultaneously',
      'Env vars are resolved per-container in the order defined within each container spec; there is no cross-container dependency resolution',
      'Init container env vars are resolved first, then regular containers',
      'The API server resolves all env vars before the kubelet',
    ],
    correctAnswer: 1,
    explanation: 'Each container env list is resolved independently and in order. $(VAR) references can only refer to variables defined earlier in the SAME container env list. There is no cross-container env var dependency. Init container env resolution is also independent.',
    tags: ['kubernetes', 'env-vars', 'ordering', 'resolution', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'How does the kubelet handle volume reconstruction after a node restart?',
    options: [
      'Volumes are lost and recreated from scratch',
      'The kubelet uses volume reconstruction from the node disk state and API server records to reattach and remount volumes to running containers',
      'The container runtime handles all volume management',
      'Volumes are always ephemeral and cannot survive node restarts',
    ],
    correctAnswer: 1,
    explanation: 'After a node restart, the kubelet reconciles volumes by: 1) scanning /var/lib/kubelet/pods for existing volume mount evidence, 2) querying the API server for expected pod/volume state, 3) reconstructing the volume manager desired state, 4) re-mounting volumes as needed.',
    tags: ['kubernetes', 'volume-mounts', 'kubelet', 'reconstruction', 'node-restart'],
  },
  {
    id: 'k8s-configuration-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the static CPU Manager policy and how does it allocate CPUs for Guaranteed pods?',
    options: [
      'It distributes CPUs evenly across all pods',
      'It assigns exclusive CPUs from the reserved pool for Guaranteed QoS pods with integer CPU requests, using cpuset cgroup to pin processes to specific cores',
      'It increases CPU frequency for guaranteed pods',
      'It is identical to the default none policy',
    ],
    correctAnswer: 1,
    explanation: 'Static policy: kubelet maintains a shared pool (all CPUs minus reserved). Guaranteed pods with integer CPU requests get exclusive CPUs removed from the shared pool. Uses cpuset cgroup to pin container processes. Other pods share remaining CPUs.',
    codeSnippet: '# kubelet configuration\ncpuManagerPolicy: static\ncpuManagerPolicyOptions:\n  full-pcpus-only: "true"\nreservedSystemCPUs: "0-1"',
    tags: ['kubernetes', 'resource-limits', 'cpu-manager', 'static-policy', 'cpuset'],
  },
  {
    id: 'k8s-configuration-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'What is the internal implementation of the LimitRanger admission plugin and how does it handle edge cases?',
    options: [
      'It is a simple comparison function',
      'It processes Container, InitContainer, and Pod type limits separately; for Pod limits, it sums all container values and validates against Pod max; it handles multiple LimitRanges by applying the most restrictive constraints',
      'It only validates at the namespace level',
      'It delegates validation to etcd',
    ],
    correctAnswer: 1,
    explanation: 'The LimitRanger plugin: 1) processes each LimitRange in the namespace, 2) for Container type: applies per-container defaults, validates per-container min/max/ratio, 3) for Pod type: sums container resources and validates against Pod limits. Multiple LimitRanges are all enforced (most restrictive wins).',
    tags: ['kubernetes', 'limit-ranges', 'admission-controller', 'internals', 'implementation'],
  },
  {
    id: 'k8s-configuration-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How does the ResourceQuota controller maintain accurate usage counts in a distributed system?',
    options: [
      'It counts objects on every API call',
      'It uses an informer-based controller that watches all resources, maintains a local usage cache, and periodically reconciles with the actual cluster state to correct drift',
      'It queries etcd directly for counts',
      'It relies on the API server cache exclusively',
    ],
    correctAnswer: 1,
    explanation: 'The quota controller uses informers to watch resource changes and update usage incrementally. A periodic full replenishment scan reconciles usage with actual state to handle missed events. The admission controller also performs real-time checks against the latest usage.',
    tags: ['kubernetes', 'resource-quotas', 'controller', 'informers', 'reconciliation'],
  },
  {
    id: 'k8s-configuration-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the ProcMount Unmasked type and what security paths does the Default type mask?',
    options: [
      'Default masks nothing; Unmasked exposes everything',
      'Default masks /proc/kcore, /proc/keys, /proc/timer_list and others to prevent information disclosure; Unmasked removes these masks for containers needing full proc access',
      'Default and Unmasked are identical',
      'Default only masks /proc/self',
    ],
    correctAnswer: 1,
    explanation: 'The Default procMount masks sensitive /proc paths: /proc/acpi, /proc/kcore, /proc/keys, /proc/latency_stats, /proc/timer_list, /proc/timer_stats, /proc/sched_debug, /proc/scsi, and /sys/firmware. Unmasked bypasses these for special use cases like nested containers.',
    tags: ['kubernetes', 'security-context', 'proc-mount', 'masking', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the etcd storage overhead for ConfigMaps and how does it affect cluster performance?',
    options: [
      'ConfigMaps are stored as compressed blobs',
      'Each ConfigMap is stored as a key-value pair in etcd with the full protobuf-encoded object; many large ConfigMaps increase etcd database size, compaction time, and snapshot duration',
      'ConfigMaps are stored in a separate database',
      'etcd has no overhead for ConfigMaps',
    ],
    correctAnswer: 1,
    explanation: 'ConfigMaps are stored as protobuf-encoded objects in etcd. Each revision is kept until compacted. Many large ConfigMaps increase: database size, compaction time, snapshot size, restore time, and leader election stability. Monitor etcd database size and compaction lag.',
    tags: ['kubernetes', 'configmaps', 'etcd', 'performance', 'storage'],
  },
  {
    id: 'k8s-configuration-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the NodeRestriction admission plugin protect Secrets?',
    options: [
      'It encrypts Secrets on the node',
      'It prevents kubelets from accessing Secrets for pods not scheduled to their node, limiting the blast radius of a node compromise',
      'It restricts Secret creation to the control plane',
      'It limits the number of Secrets per node',
    ],
    correctAnswer: 1,
    explanation: 'NodeRestriction limits kubelet API access: a kubelet can only read Secrets referenced by pods bound to its node. If a node is compromised, the attacker can only access Secrets for pods on that specific node, not all cluster Secrets.',
    tags: ['kubernetes', 'secrets', 'node-restriction', 'admission', 'security'],
  },
  {
    id: 'k8s-configuration-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How do in-place pod resource resizing (InPlacePodVerticalScaling) work?',
    options: [
      'The kubelet adjusts cgroup limits directly without restarting the container',
      'The kubelet updates the container cgroup resource limits and may optionally restart the container based on the resizePolicy; the pod spec has separate spec.containers[].resources and status.containerStatuses[].resources',
      'The API server restarts the pod with new resources',
      'In-place resizing is not supported in Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'InPlacePodVerticalScaling allows changing CPU/memory requests and limits without pod restart. The kubelet updates cgroup settings. resizePolicy per container specifies NotRequired (no restart) or RestartContainer. status.resize shows Proposed/InProgress/Infeasible.',
    codeSnippet: 'spec:\n  containers:\n    - name: app\n      resizePolicy:\n        - resourceName: cpu\n          restartPolicy: NotRequired\n        - resourceName: memory\n          restartPolicy: RestartContainer',
    tags: ['kubernetes', 'resource-limits', 'in-place-resize', 'vertical-scaling'],
  },
  {
    id: 'k8s-configuration-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does the Topology Manager "single-numa-node" policy interact with SecurityContext and device allocation?',
    options: [
      'Topology Manager does not affect SecurityContext',
      'The topology manager ensures CPUs, memory, and devices (e.g., GPU) are all from the same NUMA node; pods that cannot be satisfied are rejected with TopologyAffinityError',
      'It only applies to network topology',
      'It assigns pods to nodes based on geographic location',
    ],
    correctAnswer: 1,
    explanation: 'The single-numa-node policy requires all resource hints (CPU Manager, Memory Manager, Device Manager) to agree on a single NUMA node. If no single NUMA node can satisfy all resources, the pod admission fails with TopologyAffinityError.',
    tags: ['kubernetes', 'security-context', 'topology-manager', 'numa', 'device-allocation'],
  },
  {
    id: 'k8s-configuration-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What happens at the container runtime level when environment variables exceed system limits?',
    options: [
      'The runtime truncates the variables',
      'The container creation fails via CRI with an error; the total arg+env size is bounded by the Linux kernel MAX_ARG_PAGES limit (typically ~2MB combined for args and env)',
      'The variables are paginated',
      'Excess variables are stored in /proc/environ',
    ],
    correctAnswer: 1,
    explanation: 'Linux limits the combined size of arguments and environment via MAX_ARG_PAGES (typically 32 pages = ~128KB, or ~2MB on newer kernels with RLIMIT_STACK). If exceeded, execve() fails with E2BIG, and the container fails to start with a CRI error.',
    tags: ['kubernetes', 'env-vars', 'linux-kernel', 'limits', 'cri'],
  },
  {
    id: 'k8s-configuration-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'How does the kubelet CSI node plugin handle volume staging and publishing?',
    options: [
      'CSI volumes are mounted in a single step',
      'CSI uses a two-stage process: NodeStageVolume (mount to a staging directory, shared across pods) then NodePublishVolume (bind mount from staging to pod directory)',
      'The API server directly manages CSI mounts',
      'CSI volumes use NFS under the hood',
    ],
    correctAnswer: 1,
    explanation: 'CSI uses two stages: NodeStageVolume mounts the volume to a global staging path (once per volume per node). NodePublishVolume bind-mounts from the staging path to each pod target path. This allows sharing a single device mount across multiple pods.',
    tags: ['kubernetes', 'volume-mounts', 'csi', 'staging', 'publishing'],
  },
  {
    id: 'k8s-configuration-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the OOM score adjustment work for different QoS classes?',
    options: [
      'All containers have the same OOM score',
      'Guaranteed pods get oom_score_adj=-997, BestEffort gets 1000, Burstable gets a value between 2 and 999 based on memory request ratio',
      'OOM scores are random',
      'Only BestEffort pods have OOM scores',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet sets oom_score_adj: Guaranteed=-997 (almost never killed), BestEffort=1000 (killed first), Burstable=min(max(2, 1000-1000*memoryRequest/memoryCapacity), 999). Lower scores mean less likely to be killed by the kernel OOM killer.',
    tags: ['kubernetes', 'resource-limits', 'oom-score', 'qos', 'kernel'],
  },
  {
    id: 'k8s-configuration-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'How do LimitRanges handle ephemeral containers added via kubectl debug?',
    options: [
      'Ephemeral containers are exempt from LimitRanges',
      'LimitRanges apply to ephemeral containers the same as regular containers; defaults are applied and constraints are enforced',
      'Ephemeral containers bypass admission controllers',
      'LimitRanges only apply to ephemeral containers if explicitly configured',
    ],
    correctAnswer: 1,
    explanation: 'Ephemeral containers are subject to the same LimitRange constraints as regular containers. Default requests/limits are applied if not specified, and min/max constraints are enforced. This can cause issues with debug containers if LimitRange maximums are very restrictive.',
    tags: ['kubernetes', 'limit-ranges', 'ephemeral-containers', 'kubectl-debug'],
  },
  {
    id: 'k8s-configuration-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the watch cache mechanism in the API server for ConfigMap list/watch operations?',
    options: [
      'There is no cache; all requests go to etcd',
      'The API server maintains an in-memory ring buffer cache that serves watch events and consistent list operations, reducing etcd load',
      'The cache is stored in a sidecar container',
      'etcd provides its own caching layer',
    ],
    correctAnswer: 1,
    explanation: 'The API server watch cache (cacher.go) maintains a ring buffer of recent events per resource type. LIST requests can be served from cache (resourceVersion="0" or consistent reads). WATCH connections receive events from the cache. This significantly reduces etcd read load.',
    tags: ['kubernetes', 'configmaps', 'api-server', 'watch-cache', 'performance'],
  },
  {
    id: 'k8s-configuration-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the bound service account token volume projection work internally?',
    options: [
      'It creates a long-lived token stored as a Secret',
      'The kubelet requests a time-bound, audience-bound token from the TokenRequest API, writes it as a projected volume file, and rotates it at 80% of its expiration time',
      'The API server mounts tokens directly',
      'Tokens are generated by the service account controller and stored in etcd',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet uses the TokenRequest API to get a time-bound token (default 1h) with a specific audience. It writes the token to the projected volume and rotates it when 80% of the TTL has elapsed or 24h before expiry, whichever is sooner.',
    codeSnippet: '# Default projected token volume (auto-mounted)\nvolumes:\n  - name: kube-api-access-xxxxx\n    projected:\n      sources:\n        - serviceAccountToken:\n            expirationSeconds: 3607\n            path: token\n        - configMap:\n            name: kube-root-ca.crt\n            items:\n              - key: ca.crt\n                path: ca.crt\n        - downwardAPI:\n            items:\n              - fieldRef:\n                  fieldPath: metadata.namespace\n                path: namespace',
    tags: ['kubernetes', 'secrets', 'service-account-token', 'projected', 'rotation'],
  },
  {
    id: 'k8s-configuration-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the "burstable" memory behavior in cgroup v1 vs cgroup v2?',
    options: [
      'Memory bursting behavior is identical in v1 and v2',
      'In cgroup v1, exceeding memory.limit_in_bytes triggers OOM kill immediately; in cgroup v2, memory.high provides a throttling boundary before the hard memory.max OOM kill',
      'cgroup v1 supports memory throttling; cgroup v2 does not',
      'Neither supports memory bursting',
    ],
    correctAnswer: 1,
    explanation: 'cgroup v1 only has memory.limit_in_bytes (hard limit, OOM kill). cgroup v2 introduces memory.high (soft limit, reclaim/throttle) before memory.max (hard limit, OOM kill). Kubernetes MemoryQoS uses memory.high = (request + (limit-request)*throttleFactor) for smoother behavior.',
    tags: ['kubernetes', 'resource-limits', 'cgroup-v1', 'cgroup-v2', 'memory', 'comparison'],
  },
  {
    id: 'k8s-configuration-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does hostPID: true affect the container security posture and what attacks does it enable?',
    options: [
      'hostPID only shows host process names',
      'hostPID shares the host PID namespace, enabling: process inspection via /proc, signal injection to host processes, environment variable reading, potential ptrace-based attacks, and breaking out of other namespaces',
      'hostPID is safe because processes are still sandboxed',
      'hostPID only affects networking',
    ],
    correctAnswer: 1,
    explanation: 'hostPID: true shares the host PID namespace. Attackers can: read /proc/<pid>/environ of host processes (potentially containing secrets), send signals, read /proc/<pid>/root for filesystem access, and use ptrace if CAP_SYS_PTRACE is available. This is why restricted PSS disallows it.',
    tags: ['kubernetes', 'security-context', 'hostpid', 'attacks', 'container-escape'],
  },
  {
    id: 'k8s-configuration-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'How does the scheduler handle preemption across multiple scheduling queues and plugins?',
    options: [
      'Preemption uses a simple FIFO queue',
      'The scheduler uses a two-queue system: activeQ (ready to schedule) and unschedulableQ (waiting); preemption is attempted for unschedulable pods, and PostFilter plugins implement the preemption logic',
      'Preemption bypasses all scheduler plugins',
      'Each PriorityClass has its own scheduler instance',
    ],
    correctAnswer: 1,
    explanation: 'The scheduler uses activeQ (heap by priority) and unschedulableQ. When a pod fails scheduling, PostFilter plugins (DefaultPreemption) attempt preemption. It evaluates all nodes, simulates removing victims, and nominates a node. The pod is re-queued and scheduled after victims terminate.',
    tags: ['kubernetes', 'priority-classes', 'scheduler', 'queues', 'postfilter'],
  },
  {
    id: 'k8s-configuration-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'What is the implementation of the downwardAPI volume plugin in the kubelet?',
    options: [
      'It reads from a database',
      'It implements the volume.VolumePlugin interface, fetching pod metadata from the kubelet pod cache and writing files to an emptyDir-like tmpfs; it re-evaluates during each kubelet sync period',
      'It proxies requests to the API server',
      'It uses inotify to watch for changes',
    ],
    correctAnswer: 1,
    explanation: 'The downwardAPI volume plugin: 1) allocates a tmpfs-backed directory, 2) reads pod metadata from the kubelet internal pod cache, 3) writes files using atomic writer (same mechanism as ConfigMaps), 4) re-evaluates on each sync period to detect label/annotation changes.',
    tags: ['kubernetes', 'downward-api', 'kubelet', 'volume-plugin', 'implementation'],
  },
  {
    id: 'k8s-configuration-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the "full-pcpus-only" CPU Manager policy option and when is it needed?',
    options: [
      'It ensures CPUs are fully utilized before allocating new ones',
      'It ensures Guaranteed pods only receive full physical cores (not partial/hyperthreaded), preventing noisy-neighbor effects from SMT siblings',
      'It reserves full cores for system processes',
      'It distributes workloads evenly across physical CPUs',
    ],
    correctAnswer: 1,
    explanation: 'The full-pcpus-only option allocates whole physical CPU cores (both hyperthreads) to Guaranteed pods. This prevents another pod from sharing the same physical core via SMT, eliminating cross-hyperthread interference for latency-sensitive workloads.',
    tags: ['kubernetes', 'resource-limits', 'cpu-manager', 'smt', 'physical-cores'],
  },
  {
    id: 'k8s-configuration-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How do you implement zero-downtime ConfigMap rollouts for applications that watch config file changes?',
    options: [
      'Just update the ConfigMap; the application will see changes eventually',
      'Use a volume-mounted ConfigMap with the application watching the ..data symlink target; the atomic symlink swap ensures the app sees a consistent update without partial reads',
      'Use a sidecar that polls the API server',
      'Restart all pods after every ConfigMap update',
    ],
    correctAnswer: 1,
    explanation: 'Applications can watch the ..data symlink target (or use inotify on the mount directory). The kubelet atomic symlink swap ensures the application never sees partial data. Libraries like viper (Go) or watchdog (Python) can auto-reload on file changes.',
    tags: ['kubernetes', 'configmaps', 'zero-downtime', 'file-watch', 'symlinks'],
  },
  {
    id: 'k8s-configuration-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the secretbox encryption provider and how does it compare to aescbc?',
    options: [
      'They are the same algorithm with different names',
      'secretbox uses XSalsa20-Poly1305 (NaCl/libsodium) which provides authenticated encryption with nonce-misuse resistance; aescbc uses AES-CBC with HMAC-SHA256 which is vulnerable to padding oracle attacks without proper implementation',
      'aescbc is always more secure than secretbox',
      'secretbox is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'secretbox uses XSalsa20-Poly1305 from NaCl (libsodium), providing authenticated encryption that is simpler to use correctly. aescbc uses AES-CBC with HMAC-SHA256 which requires careful implementation to avoid padding oracle attacks. secretbox is recommended over aescbc.',
    tags: ['kubernetes', 'secrets', 'encryption', 'secretbox', 'aescbc', 'cryptography'],
  },
  {
    id: 'k8s-configuration-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How do you implement defense-in-depth for container security using multiple overlapping mechanisms?',
    options: [
      'Use only one security mechanism to avoid conflicts',
      'Layer: SecurityContext (capabilities, non-root, read-only fs) + seccomp (syscall filtering) + AppArmor/SELinux (MAC) + user namespaces (UID remapping) + PSA enforcement + network policies',
      'Use privileged containers with network policies',
      'Rely solely on RBAC for container security',
    ],
    correctAnswer: 1,
    explanation: 'Defense-in-depth layers: 1) SecurityContext: drop capabilities, non-root, read-only fs. 2) seccomp: filter syscalls. 3) AppArmor/SELinux: restrict file/network access. 4) User namespaces: remap UIDs. 5) PSA: enforce standards. 6) Network policies: restrict traffic. Each layer mitigates different attack vectors.',
    tags: ['kubernetes', 'security-context', 'defense-in-depth', 'hardening', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the kubelet determine the allocatable resources on a node?',
    options: [
      'It uses the total node resources',
      'Allocatable = NodeCapacity - KubeReserved - SystemReserved - EvictionThreshold; this is what is available for pod scheduling',
      'Allocatable equals the node capacity',
      'The scheduler determines allocatable resources',
    ],
    correctAnswer: 1,
    explanation: 'Node allocatable = Capacity - kube-reserved (kubelet, runtime) - system-reserved (OS daemons) - eviction-threshold (hard eviction buffer). Only the allocatable portion is available for pod scheduling. This prevents system instability from pod resource consumption.',
    codeSnippet: '# Kubelet configuration\nkubeReserved:\n  cpu: "200m"\n  memory: "256Mi"\nsystemReserved:\n  cpu: "200m"\n  memory: "256Mi"\nevictionHard:\n  memory.available: "100Mi"\n# Allocatable = Capacity - 200m - 200m CPU\n#             = Capacity - 256Mi - 256Mi - 100Mi memory',
    tags: ['kubernetes', 'resource-limits', 'allocatable', 'kubelet', 'node-capacity'],
  },
  {
    id: 'k8s-configuration-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'How does the CRI (Container Runtime Interface) handle process signal propagation for different command configurations?',
    options: [
      'CRI always wraps processes in a signal handler',
      'CRI passes the command array directly to the runtime; if the first element is a shell, SIGTERM goes to the shell (PID 1), not the child process unless exec is used or the shell traps/propagates signals',
      'CRI sends SIGTERM to all processes in the container simultaneously',
      'Signal propagation depends on the container image',
    ],
    correctAnswer: 1,
    explanation: 'CRI sets Process.Args in the OCI spec. The first process (PID 1) receives SIGTERM on termination. If PID 1 is /bin/sh running a child process without exec, the shell receives SIGTERM but may not propagate it. Solutions: use exec, trap signals in the shell, or use tini/dumb-init as PID 1.',
    tags: ['kubernetes', 'command-args', 'cri', 'signals', 'pid1', 'oci'],
  },
  {
    id: 'k8s-configuration-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is the volume attach/mount lifecycle in the kubelet and how do attach/detach errors affect pods?',
    options: [
      'Volumes are mounted instantaneously',
      'Lifecycle: WaitForAttach → Attach (AD controller) → WaitForMount → MountDevice (NodeStageVolume) → SetUp (NodePublishVolume); failures cause pod to stay in ContainerCreating with volume-related events',
      'The scheduler handles all volume operations',
      'Volumes are mounted after the container starts',
    ],
    correctAnswer: 1,
    explanation: 'Volume lifecycle: 1) Attach/Detach controller attaches the volume to the node (cloud API). 2) Kubelet volume manager waits for attach, then calls MountDevice (format if needed, mount to staging). 3) SetUp bind-mounts to pod directory. Timeouts and errors surface as pod events.',
    tags: ['kubernetes', 'volume-mounts', 'lifecycle', 'attach', 'kubelet', 'csi'],
  },
  {
    id: 'k8s-configuration-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'How do LimitRanges interact with the in-place pod resize feature?',
    options: [
      'LimitRanges do not affect in-place resizing',
      'When a pod is resized in-place, the new resource values are validated against the LimitRange; if the new values violate the LimitRange, the resize is rejected',
      'In-place resizing bypasses LimitRange checks',
      'LimitRanges automatically trigger resizing',
    ],
    correctAnswer: 1,
    explanation: 'In-place pod resize goes through the API server, which invokes admission controllers including LimitRanger. The new resource values must comply with the LimitRange. This ensures resized pods remain within namespace constraints.',
    tags: ['kubernetes', 'limit-ranges', 'in-place-resize', 'admission'],
  },
  {
    id: 'k8s-configuration-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How does ResourceQuota interact with the priority-based preemption and what are the gotchas?',
    options: [
      'ResourceQuota prevents preemption',
      'Preempted pod resources are not immediately freed in the quota; there is a reconciliation delay where the quota may temporarily show higher usage than actual, potentially blocking new pod creation',
      'Preemption always frees quota immediately',
      'ResourceQuota and preemption are completely independent',
    ],
    correctAnswer: 1,
    explanation: 'When a pod is preempted, its quota is freed after the pod is fully terminated and the quota controller reconciles. During the grace period, the quota may still count the terminating pod, potentially causing a brief period where new pods cannot be created due to quota exhaustion.',
    tags: ['kubernetes', 'resource-quotas', 'preemption', 'reconciliation', 'gotcha'],
  },
  {
    id: 'k8s-configuration-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the AppArmor migration from annotations to native fields in Kubernetes, and how does it affect existing workloads?',
    options: [
      'AppArmor has always used native fields',
      'Kubernetes 1.30+ added securityContext.appArmorProfile as a native field; the annotation is deprecated but still supported with automatic conversion; both cannot be set simultaneously',
      'AppArmor annotations are removed without replacement',
      'AppArmor is no longer supported in Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes 1.30 GA promoted AppArmor to a native SecurityContext field (appArmorProfile with type: RuntimeDefault|Localhost|Unconfined). Annotations are auto-converted but deprecated. Setting both annotation and field simultaneously for the same container is an error.',
    codeSnippet: '# New native field (preferred)\nsecurityContext:\n  appArmorProfile:\n    type: Localhost\n    localhostProfile: my-profile\n\n# Old annotation (deprecated)\n# container.apparmor.security.beta.kubernetes.io/app: localhost/my-profile',
    tags: ['kubernetes', 'security-context', 'apparmor', 'migration', 'native-field'],
  },
  {
    id: 'k8s-configuration-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the impact of Kubernetes API priority and fairness (APF) on ConfigMap operations?',
    options: [
      'APF does not affect ConfigMap operations',
      'APF classifies ConfigMap API requests into flow schemas and priority levels; high volume ConfigMap watches can be throttled if they exceed their fair share, affecting propagation latency',
      'APF prioritizes ConfigMap operations above all others',
      'APF only affects authentication requests',
    ],
    correctAnswer: 1,
    explanation: 'API Priority and Fairness classifies requests into priority levels with concurrency limits. Heavy ConfigMap watch/list traffic can be throttled under load. Immutable ConfigMaps reduce this by eliminating watches. Monitor the apiserver_flowcontrol_* metrics for throttling.',
    tags: ['kubernetes', 'configmaps', 'api-priority-fairness', 'throttling', 'scalability'],
  },
  {
    id: 'k8s-configuration-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the Kubernetes audit system handle Secret data in audit logs?',
    options: [
      'Secret data is always included in audit logs',
      'The audit policy level controls this: Metadata logs only request metadata (no body), Request logs include the request body (including Secret data), RequestResponse includes both; use Metadata level for Secrets to avoid data exposure',
      'Secret data is automatically redacted in audit logs',
      'Audit logs do not capture Secret operations',
    ],
    correctAnswer: 1,
    explanation: 'At Request or RequestResponse audit levels, Secret data (base64-encoded) can appear in logs. Best practice: use Metadata level for Secrets (captures who/when/what but not the data). Some audit backends support selective field redaction.',
    codeSnippet: '# Recommended audit policy for Secrets\nrules:\n  - level: Metadata\n    resources:\n      - group: ""\n        resources: ["secrets"]\n  - level: RequestResponse\n    resources:\n      - group: ""\n        resources: ["configmaps"]',
    tags: ['kubernetes', 'secrets', 'audit', 'data-exposure', 'best-practices'],
  },
  {
    id: 'k8s-configuration-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the swap support in Kubernetes and how does it interact with resource limits?',
    options: [
      'Kubernetes fully supports swap for all QoS classes',
      'Kubernetes 1.28+ supports limited swap for Burstable QoS pods with cgroup v2; Guaranteed and BestEffort pods cannot use swap; the kubelet NodeSwap feature gate controls this',
      'Swap is completely unsupported and must be disabled',
      'Swap is only available for init containers',
    ],
    correctAnswer: 1,
    explanation: 'With NodeSwap feature gate and cgroup v2: LimitedSwap behavior allows only Burstable pods to use swap (proportional to the difference between limit and request). Guaranteed pods have memory.swap.max=0. BestEffort pods cannot use swap. This prevents performance degradation for latency-sensitive workloads.',
    tags: ['kubernetes', 'resource-limits', 'swap', 'cgroup-v2', 'feature-gate'],
  },
  {
    id: 'k8s-configuration-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'What is the fieldRef validation mechanism in the API server for Downward API?',
    options: [
      'Any field path is accepted',
      'The API server validates fieldRef.fieldPath against a whitelist of allowed paths per API version; invalid paths are rejected during admission with a clear error message',
      'Validation happens at the kubelet level only',
      'Field paths are validated by the container runtime',
    ],
    correctAnswer: 1,
    explanation: 'The API server validates fieldRef.fieldPath against a strict whitelist: metadata.name, metadata.namespace, metadata.uid, metadata.labels, metadata.annotations, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs. Invalid paths cause API rejection.',
    tags: ['kubernetes', 'downward-api', 'validation', 'api-server', 'fieldref'],
  },
  {
    id: 'k8s-configuration-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'What is the interaction between Pod Priority and the kube-scheduler framework scoring plugins?',
    options: [
      'Priority replaces all scoring plugins',
      'Pod priority only affects queue ordering and preemption; scoring plugins (NodeResourcesFit, InterPodAffinity, etc.) still determine which node to use independently of priority',
      'Higher priority pods always get the best nodes',
      'Scoring plugins are disabled for high-priority pods',
    ],
    correctAnswer: 1,
    explanation: 'Priority affects: 1) Queue ordering: higher priority pods are dequeued first. 2) Preemption: triggered as a PostFilter when scheduling fails. Scoring plugins (NodeResourcesFit, ImageLocality, InterPodAffinity, etc.) still determine node selection independently of priority.',
    tags: ['kubernetes', 'priority-classes', 'scheduler', 'scoring-plugins', 'framework'],
  },
  {
    id: 'k8s-configuration-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does the Linux capability bounding set affect container security in Kubernetes?',
    options: [
      'Containers inherit all host capabilities',
      'The container runtime sets a default capability bounding set; capabilities.add can only add capabilities within this set, not arbitrary kernel capabilities; the bounding set limits the maximum possible capabilities',
      'Capabilities are unlimited in containers',
      'The bounding set only affects privileged containers',
    ],
    correctAnswer: 1,
    explanation: 'The OCI runtime sets a default bounding set for containers. capabilities.add can only add capabilities within this bounding set. Even with add: [ALL], the container gets the runtime default set, not all kernel capabilities (unlike privileged: true which truly grants all).',
    tags: ['kubernetes', 'security-context', 'capabilities', 'bounding-set', 'linux-kernel'],
  },
  {
    id: 'k8s-configuration-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How does the kubelet ConfigMap/Secret manager implement the "Watch" caching strategy?',
    options: [
      'It polls the API server at a fixed interval',
      'It establishes a single watch per referenced ConfigMap/Secret, caches the object, and updates the cache on watch events; broken watches are re-established with a resync period',
      'It shares a single watch for all ConfigMaps',
      'It delegates caching to etcd',
    ],
    correctAnswer: 1,
    explanation: 'The Watch strategy creates individual reflector/informer per ConfigMap/Secret referenced by pods on the node. Each maintains a watch connection and local cache. On watch errors, it reconnects with a re-list. This is the most efficient strategy for clusters with many ConfigMaps.',
    tags: ['kubernetes', 'configmaps', 'kubelet', 'watch-strategy', 'caching'],
  },
  {
    id: 'k8s-configuration-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the kubelet pod admission relate to resource requests and allocatable resources?',
    options: [
      'The kubelet accepts all pods assigned by the scheduler',
      'The kubelet performs its own admission check: it verifies the sum of pod resource requests plus existing pod requests does not exceed node allocatable; pods failing this check are rejected with OutOfCPU/OutOfMemory',
      'Only the scheduler checks resource availability',
      'The kubelet admission only checks disk space',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet independently validates resources during admission (after the scheduler assigns the pod). If the node is overcommitted (e.g., due to static pods or race conditions), the kubelet rejects the pod. It also checks PID limits and disk space.',
    tags: ['kubernetes', 'resource-limits', 'kubelet', 'admission', 'allocatable'],
  },
  {
    id: 'k8s-configuration-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the etcd encryption key rotation procedure and what are the risks?',
    options: [
      'Replace the key and restart the API server',
      'Add the new key as first provider (write), keep the old key as second (read), restart API servers, re-encrypt all Secrets, then optionally remove the old key; risk: if the old key is removed before re-encryption, old Secrets become unreadable',
      'Keys cannot be rotated',
      'etcd handles key rotation automatically',
    ],
    correctAnswer: 1,
    explanation: 'Key rotation: 1) Add new key as first provider (used for writes), old key as second (for reading existing). 2) Restart ALL API servers. 3) Re-encrypt all secrets: kubectl get secrets --all-namespaces -o json | kubectl replace -f -. 4) Remove old key from config. Risk: premature old key removal causes data loss.',
    tags: ['kubernetes', 'secrets', 'encryption', 'key-rotation', 'etcd', 'procedure'],
  },
  {
    id: 'k8s-configuration-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What is the internal kubelet mechanism for resolving environment variables from ConfigMaps/Secrets during container creation?',
    options: [
      'The API server resolves them before sending to the kubelet',
      'The kubelet fetches the ConfigMap/Secret via the API server (or cache), extracts the referenced key, and substitutes it into the container env spec before calling the CRI CreateContainer RPC',
      'The container runtime resolves them from mounted volumes',
      'A sidecar process resolves them',
    ],
    correctAnswer: 1,
    explanation: 'During container creation, the kubelet: 1) resolves all valueFrom references by fetching ConfigMaps/Secrets (from cache or API). 2) resolves $(VAR_NAME) dependent variables. 3) constructs the final env array. 4) passes it to the CRI CreateContainer RPC. Failures cause CreateContainerConfigError.',
    tags: ['kubernetes', 'env-vars', 'kubelet', 'resolution', 'cri', 'internals'],
  },
  {
    id: 'k8s-configuration-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'How does the VolumeSubpathEnvExpansion feature work internally and what are its edge cases?',
    options: [
      'The shell expands variables at mount time',
      'The kubelet resolves $(VAR_NAME) in subPathExpr before calling the runtime; edge cases include: variables not yet resolved (race with env order), empty variable values creating root-level mounts, and path traversal prevention',
      'The container runtime handles expansion',
      'Environment variables in subPath are not supported',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet resolves subPathExpr $(VAR_NAME) references using the same env resolution mechanism. Edge cases: 1) Empty var creates a blank subpath (mounts volume root). 2) ".." components are rejected to prevent path traversal. 3) Variables are resolved in env definition order.',
    tags: ['kubernetes', 'volume-mounts', 'subpath-expr', 'env-expansion', 'edge-cases'],
  },
  {
    id: 'k8s-configuration-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'What is the ResourceQuota scopeSelector with CrossNamespacePodAffinity scope used for?',
    options: [
      'Limiting pods across namespaces',
      'Limiting the number of pods with cross-namespace pod affinity/anti-affinity terms, which are expensive for the scheduler to evaluate at scale',
      'Enabling pod affinity across namespaces',
      'Restricting network access across namespaces',
    ],
    correctAnswer: 1,
    explanation: 'Cross-namespace pod affinity is expensive for the scheduler (must evaluate pods in all namespaces). The CrossNamespacePodAffinity scope in ResourceQuota limits how many pods can use this feature, protecting scheduler performance.',
    codeSnippet: 'apiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: cross-ns-affinity-quota\nspec:\n  hard:\n    pods: "5"\n  scopeSelector:\n    matchExpressions:\n      - scopeName: CrossNamespacePodAffinity\n        operator: Exists',
    tags: ['kubernetes', 'resource-quotas', 'cross-namespace', 'pod-affinity', 'scheduler'],
  },
  {
    id: 'k8s-configuration-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How do you implement fine-grained SELinux context assignment for multi-tenant pod workloads?',
    options: [
      'SELinux is automatically configured by Kubernetes',
      'Use seLinuxOptions in pod/container SecurityContext with MCS (Multi-Category Security) labels to isolate tenants; different category pairs (e.g., s0:c1,c2 vs s0:c3,c4) prevent cross-tenant volume access',
      'SELinux only works with single-tenant clusters',
      'Use AppArmor instead of SELinux for multi-tenancy',
    ],
    correctAnswer: 1,
    explanation: 'SELinux MCS (Multi-Category Security) assigns unique category pairs to each tenant. Volumes and processes with different categories cannot access each other. The SELinuxMount feature relabels volumes automatically. This provides kernel-enforced tenant isolation.',
    codeSnippet: '# Tenant A pod\nsecurityContext:\n  seLinuxOptions:\n    level: "s0:c100,c200"\n# Tenant B pod\nsecurityContext:\n  seLinuxOptions:\n    level: "s0:c300,c400"',
    tags: ['kubernetes', 'security-context', 'selinux', 'multi-tenancy', 'mcs'],
  },
  {
    id: 'k8s-configuration-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'What are the ordering guarantees when multiple LimitRanges exist in the same namespace?',
    options: [
      'Only the first LimitRange is applied',
      'All LimitRanges are applied; the most restrictive constraint across all LimitRanges wins for validation; for defaults, the last applied LimitRange default takes effect',
      'LimitRanges are merged alphabetically',
      'Multiple LimitRanges cause an error',
    ],
    correctAnswer: 1,
    explanation: 'When multiple LimitRanges exist: all min/max constraints are validated (most restrictive wins). For default values, the behavior depends on the admission controller implementation; generally the default from the first matching LimitRange is applied. Best practice: use a single LimitRange per namespace.',
    tags: ['kubernetes', 'limit-ranges', 'multiple', 'ordering', 'constraints'],
  },
  {
    id: 'k8s-configuration-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'What happens to the NominatedNode field during the preemption lifecycle?',
    options: [
      'NominatedNode is set permanently after scheduling',
      'The scheduler sets status.nominatedNodeName when a preemption victim is selected; it is cleared after scheduling succeeds or if conditions change; other pods see this and may avoid the node',
      'NominatedNode is only used for affinity',
      'NominatedNode determines the final scheduling decision',
    ],
    correctAnswer: 1,
    explanation: 'When preemption identifies a target node: 1) status.nominatedNodeName is set. 2) The scheduler considers this during scheduling other pods (avoids over-nominating one node). 3) After the pod is scheduled, it is cleared. If the pod cannot be scheduled there, it may be re-evaluated.',
    tags: ['kubernetes', 'priority-classes', 'preemption', 'nominated-node', 'scheduler'],
  },
  {
    id: 'k8s-configuration-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'How does the API server handle ConfigMap validation and what are the undocumented limits?',
    options: [
      'ConfigMaps have no validation beyond the 1MiB size limit',
      'The API server validates: total size <= 1MiB, key names must be valid DNS subdomain segments (alphanumeric, dashes, dots, underscores), and the total number of ConfigMaps per namespace can be limited by ResourceQuota',
      'ConfigMaps are validated by etcd directly',
      'Only the key format is validated',
    ],
    correctAnswer: 1,
    explanation: 'ConfigMap validation: 1) Total size (data + binaryData) <= 1MiB. 2) Keys must match [-._a-zA-Z0-9]+ and be valid path segments (no ".." or leading slashes). 3) Maximum key length: 253 characters. 4) etcd has a default max request size of 1.5MiB (affects total object size including metadata).',
    tags: ['kubernetes', 'configmaps', 'validation', 'api-server', 'limits'],
  },
  {
    id: 'k8s-configuration-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the distribute-cpus-across-numa CPU Manager policy option work?',
    options: [
      'It pins all CPUs to a single NUMA node',
      'It distributes allocated CPUs evenly across NUMA nodes for workloads that benefit from aggregate memory bandwidth rather than single-node locality',
      'It is the default CPU allocation strategy',
      'It only affects non-Guaranteed pods',
    ],
    correctAnswer: 1,
    explanation: 'The distribute-cpus-across-numa option (used with static policy) spreads CPU allocations across NUMA nodes. This is useful for memory-bandwidth-bound workloads (e.g., HPC, ML inference) that benefit from aggregate memory bandwidth rather than single-NUMA-node memory latency.',
    tags: ['kubernetes', 'resource-limits', 'cpu-manager', 'numa', 'distribute-cpus'],
  },
  {
    id: 'k8s-configuration-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'What is the lifecycle of a Secret referenced by a pod during pod termination and deletion?',
    options: [
      'Secrets are deleted when the pod is deleted',
      'During graceful termination: Secret volumes remain mounted, env vars remain set; after pod deletion: kubelet unmounts Secret volumes; the Secret object itself persists independently of any pod lifecycle',
      'Secrets are locked during pod termination',
      'The kubelet re-reads Secrets during termination',
    ],
    correctAnswer: 1,
    explanation: 'During termination: Secret volumes stay mounted (for graceful shutdown access). After the pod is deleted: kubelet cleans up volume mounts. The Secret API object is independent and persists. tmpfs-backed Secret volumes (in kubelet memory) are freed when the pod is fully deleted.',
    tags: ['kubernetes', 'secrets', 'lifecycle', 'pod-termination', 'volumes'],
  },
  {
    id: 'k8s-configuration-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'How does the SELinuxMount feature improve volume mount performance for SELinux-enabled clusters?',
    options: [
      'It disables SELinux for volume mounts',
      'It uses mount option context= to set SELinux labels at mount time instead of recursively relabeling all files with chcon, dramatically reducing mount time for large volumes',
      'It caches SELinux labels for reuse',
      'It only affects NFS volumes',
    ],
    correctAnswer: 1,
    explanation: 'Without SELinuxMount, the kubelet recursively relabels all files (chcon) in a volume when mounting, which is extremely slow for large volumes. SELinuxMount uses the mount -o context= option to set labels at mount time, avoiding the recursive walk. This reduces mount time from minutes to seconds.',
    tags: ['kubernetes', 'security-context', 'selinux', 'selinux-mount', 'performance'],
  },
  {
    id: 'k8s-configuration-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'env-vars',
    question: 'What is the interaction between environment variables and the OCI runtime spec in the CRI?',
    options: [
      'Environment variables are passed as command arguments',
      'The kubelet constructs a ContainerConfig with Envs field containing key-value pairs; the CRI runtime (containerd/CRI-O) maps these to the OCI Process.Env array in the runtime spec as VAR=VALUE strings',
      'The container runtime reads env vars from ConfigMaps directly',
      'Environment variables are stored in a file and sourced by the shell',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet resolves all env vars and creates a ContainerConfig protobuf with Envs as KeyValue pairs. The CRI runtime converts these to OCI Process.Env (string array of VAR=VALUE). The runtime passes them to runc/crun which sets them in the container process via clone/exec.',
    tags: ['kubernetes', 'env-vars', 'cri', 'oci', 'runtime-spec'],
  },
  {
    id: 'k8s-configuration-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is the volume plugin registration mechanism and how do out-of-tree CSI plugins integrate?',
    options: [
      'CSI plugins are compiled into the kubelet',
      'CSI plugins register via the kubelet plugin registration mechanism: a gRPC socket in /var/lib/kubelet/plugins_registry/; the kubelet discovers the socket, calls GetInfo to get driver capabilities, then uses CSI NodeService RPCs for volume operations',
      'CSI plugins communicate via HTTP REST APIs',
      'The API server manages CSI plugin registration',
    ],
    correctAnswer: 1,
    explanation: 'CSI plugin registration: 1) Driver creates a gRPC socket at /var/lib/kubelet/plugins/<driver>/csi.sock. 2) Registration socket in /var/lib/kubelet/plugins_registry/. 3) Kubelet plugin watcher discovers it. 4) Calls CSI Identity.GetPluginInfo. 5) Uses Node.NodeStageVolume/NodePublishVolume for mounts.',
    tags: ['kubernetes', 'volume-mounts', 'csi', 'plugin-registration', 'grpc'],
  },
  {
    id: 'k8s-configuration-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What is the interaction between container restart policy, OOMKill, and resource limits?',
    options: [
      'OOMKilled containers are always restarted immediately',
      'When a container is OOMKilled: the kubelet records the OOMKilled reason, applies restartPolicy (Always/OnFailure restarts with exponential backoff starting at 10s, up to 5m), and cgroup limits are re-applied on restart',
      'OOMKilled containers bypass the restart policy',
      'The scheduler reschedules OOMKilled pods to a different node',
    ],
    correctAnswer: 1,
    explanation: 'OOMKill flow: 1) kernel OOM killer sends SIGKILL. 2) Container runtime reports exit code 137 (128+9). 3) Kubelet sets containerStatus.reason=OOMKilled. 4) RestartPolicy is evaluated. 5) Backoff: 10s, 20s, 40s, 80s, 160s, 300s max. 6) New container gets same cgroup limits. Monitor for repeated OOMKills.',
    tags: ['kubernetes', 'resource-limits', 'oomkill', 'restart-policy', 'backoff'],
  },
  {
    id: 'k8s-configuration-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'limit-ranges',
    question: 'How does the LimitRange admission plugin handle sidecar containers (restartable init containers)?',
    options: [
      'Sidecar containers are exempt from LimitRanges',
      'Sidecar containers (init containers with restartPolicy: Always) are validated as init containers for LimitRange purposes but their resources contribute to the effective pod resource calculation differently than regular init containers',
      'Sidecar containers use the Pod-type LimitRange only',
      'LimitRanges are not aware of sidecar containers',
    ],
    correctAnswer: 1,
    explanation: 'Sidecar containers (restartable init containers) are subject to Container-type LimitRange constraints. For pod resource calculations: their resources are added to the sum of regular containers (unlike regular init containers where only the max is used). This affects scheduling and Pod-type LimitRange validation.',
    tags: ['kubernetes', 'limit-ranges', 'sidecar-containers', 'resource-calculation'],
  },
  {
    id: 'k8s-configuration-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-quotas',
    question: 'How does ResourceQuota handle init container resources vs. sidecar container resources?',
    options: [
      'All containers are summed equally',
      'For quota, effective resources = max(max(initContainers), sum(regularContainers + sidecarContainers)); sidecar resources are added to the regular sum because they run concurrently',
      'Init containers are excluded from quota calculations',
      'Quota only counts regular containers',
    ],
    correctAnswer: 1,
    explanation: 'ResourceQuota uses the effective pod resource: max of (highest init container resource) vs (sum of regular + sidecar containers). Sidecar containers run alongside regular containers so their resources are summed. This ensures quotas accurately reflect peak resource consumption.',
    tags: ['kubernetes', 'resource-quotas', 'init-containers', 'sidecar-containers', 'calculation'],
  },
  {
    id: 'k8s-configuration-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'downward-api',
    question: 'What are the concurrency and consistency guarantees of the Downward API volume updates?',
    options: [
      'Updates are always consistent and immediate',
      'Downward API volume updates are eventually consistent: the kubelet syncs periodically, writes atomically via symlink swap, but there is no guaranteed ordering between label/annotation changes and file updates',
      'Updates are transactionally consistent with the API server',
      'The Downward API does not support updates',
    ],
    correctAnswer: 1,
    explanation: 'Downward API volume files are eventually consistent. The kubelet reads current pod state during sync (configurable period), writes files atomically. There is no guaranteed time bound between an API server label update and the file reflecting it. Applications must handle stale data.',
    tags: ['kubernetes', 'downward-api', 'consistency', 'concurrency', 'eventual'],
  },
  {
    id: 'k8s-configuration-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'security-context',
    question: 'What is the complete set of kernel namespaces available to Kubernetes containers and how are they controlled?',
    options: [
      'Kubernetes only uses network and PID namespaces',
      'Containers use: mount (always), UTS (always), IPC (hostIPC), PID (hostPID/shareProcessNamespace), network (hostNetwork), user (hostUsers), cgroup (always); each is controlled by specific pod spec fields',
      'All namespaces are shared with the host by default',
      'Namespace configuration is handled by the CNI plugin',
    ],
    correctAnswer: 1,
    explanation: 'Linux namespaces in containers: mount (always isolated), UTS (isolated, hostname from pod spec), IPC (hostIPC: true shares host), PID (hostPID/shareProcessNamespace), network (hostNetwork), user (hostUsers: false enables), cgroup (always isolated in cgroup v2). Each provides a different isolation boundary.',
    tags: ['kubernetes', 'security-context', 'linux-namespaces', 'isolation', 'kernel'],
  },
  {
    id: 'k8s-configuration-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'configmaps',
    question: 'What is the watch bookmark mechanism and how does it affect ConfigMap watch efficiency?',
    options: [
      'Bookmarks mark favorite ConfigMaps',
      'Watch bookmarks are synthetic BOOKMARK events sent by the API server to inform watchers of the current resourceVersion without actual changes, allowing efficient watch reconnection without re-listing',
      'Bookmarks are used for pagination',
      'Bookmarks compress watch events',
    ],
    correctAnswer: 1,
    explanation: 'Watch bookmarks (WatchBookmark feature): the API server periodically sends BOOKMARK events with the current resourceVersion. On reconnect, the watcher can resume from the bookmark resourceVersion instead of re-listing all objects. This reduces API server load and reconnection latency.',
    tags: ['kubernetes', 'configmaps', 'watch-bookmark', 'api-server', 'efficiency'],
  },
  {
    id: 'k8s-configuration-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'What are the implications of running Kubernetes on cgroup v1 vs. cgroup v2 for resource management?',
    options: [
      'There is no difference for Kubernetes workloads',
      'cgroup v2 provides: unified hierarchy (simpler management), memory.high for soft limits, PSI (Pressure Stall Information) for monitoring, MemoryQoS support, proper swap control, and io.latency for I/O QoS; cgroup v1 has fragmented controllers and fewer features',
      'cgroup v1 provides better performance',
      'cgroup v2 is not production-ready',
    ],
    correctAnswer: 1,
    explanation: 'cgroup v2 advantages: unified hierarchy (all controllers in one tree), memory.min/memory.high/memory.max (protection + throttling + hard limit), PSI metrics (cpu.pressure, memory.pressure, io.pressure), swap control per cgroup, io.latency for I/O QoS. Kubernetes 1.25+ defaults to cgroup v2.',
    tags: ['kubernetes', 'resource-limits', 'cgroup-v1', 'cgroup-v2', 'comparison'],
  },
  {
    id: 'k8s-configuration-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'secrets',
    question: 'How does the TokenRequest API work internally for bound service account tokens?',
    options: [
      'It generates random tokens stored in etcd',
      'The API server signs a JWT with the service account private key, embedding: audience, expiration, pod binding (name, UID), and claims; tokens are validated by the TokenReview API without etcd lookup',
      'Tokens are generated by the kubelet',
      'Tokens are fetched from an external identity provider',
    ],
    correctAnswer: 1,
    explanation: 'TokenRequest: API server generates a JWT signed with its private key. Claims include: sub (SA), aud (audience), exp (expiration), pod (name+UID), secret (name+UID if bound to Secret). Validation via TokenReview verifies signature, expiry, and binding constraints without etcd access.',
    tags: ['kubernetes', 'secrets', 'token-request', 'jwt', 'service-accounts'],
  },
  {
    id: 'k8s-configuration-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'priority-classes',
    question: 'How does the scheduler handle preemption victims that are in the middle of graceful termination?',
    options: [
      'The scheduler waits for all victims to fully terminate before scheduling',
      'The scheduler sets a nominatedNodeName and waits; victims terminating count toward the grace period; if the node does not have enough resources after grace period expiry, the scheduler may force-delete victims or re-evaluate',
      'Victims are immediately killed without grace period',
      'The scheduler ignores terminating pods',
    ],
    correctAnswer: 1,
    explanation: 'After nominating a node: 1) Victims start graceful termination. 2) The scheduler periodically checks if enough resources are freed. 3) The waiting pod stays Pending with nominatedNodeName. 4) When enough resources are available (victims terminated), the pod is scheduled. 5) If the pod becomes unschedulable, nomination is cleared.',
    tags: ['kubernetes', 'priority-classes', 'preemption', 'graceful-termination', 'scheduler'],
  },
  {
    id: 'k8s-configuration-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'command-args',
    question: 'How does the kubelet handle process lifecycle management for containers with PID 1 zombie reaping?',
    options: [
      'The kubelet handles zombie reaping for all containers',
      'If the container process is PID 1, it must handle SIGCHLD and waitpid() for child processes; alternatively, use tini/dumb-init as PID 1, or set shareProcessNamespace: true which makes the pause container PID 1 (it handles zombie reaping)',
      'Zombie processes are automatically cleaned up by the kernel',
      'Containers cannot have zombie processes',
    ],
    correctAnswer: 1,
    explanation: 'In Linux, PID 1 is responsible for reaping zombie children (wait/waitpid). Most application processes do not handle this. Solutions: 1) Use tini/dumb-init as PID 1. 2) Use shareProcessNamespace: true (pause container becomes PID 1 and reaps zombies). 3) Implement signal handling in the app.',
    tags: ['kubernetes', 'command-args', 'pid1', 'zombie-reaping', 'tini'],
  },
  {
    id: 'k8s-configuration-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'volume-mounts',
    question: 'What is the volume expansion mechanism for CSI volumes and how does it interact with pod restarts?',
    options: [
      'CSI volumes cannot be expanded',
      'CSI expansion has two phases: controller-side (ControllerExpandVolume to resize the backing storage) and node-side (NodeExpandVolume to resize the filesystem); node expansion may require a pod restart depending on the CSI driver capabilities',
      'Only the API server needs to be updated',
      'Volume expansion is handled by the storage class automatically',
    ],
    correctAnswer: 1,
    explanation: 'CSI expansion: 1) PVC spec.resources is updated. 2) external-resizer calls ControllerExpandVolume (resize underlying storage). 3) On next pod mount, kubelet calls NodeExpandVolume (resize filesystem). Some drivers support online expansion (no restart). Others require the pod to be restarted for node expansion.',
    tags: ['kubernetes', 'volume-mounts', 'csi', 'expansion', 'resize'],
  },
  {
    id: 'k8s-configuration-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'Configuration',
    subcategory: 'resource-limits',
    question: 'How does the kubelet handle the transition from cgroup v1 to cgroup v2 for existing pods during a node upgrade?',
    options: [
      'All pods must be recreated',
      'The kubelet detects the cgroup version at startup and creates new cgroup paths in the appropriate hierarchy; running pods may need to be drained and recreated since the cgroup structure differs between v1 and v2',
      'cgroup migration is transparent and automatic',
      'The container runtime handles the migration',
    ],
    correctAnswer: 1,
    explanation: 'cgroup v1→v2 migration requires draining nodes. The kubelet detects the cgroup version at startup (via /sys/fs/cgroup/cgroup.controllers presence). v2 uses unified hierarchy vs v1 separate controllers. Running pods have v1 cgroup paths that do not exist in v2. Node drain and pod recreation is necessary.',
    tags: ['kubernetes', 'resource-limits', 'cgroup-migration', 'upgrade', 'kubelet'],
  },
];

export default questions;
