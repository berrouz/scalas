import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-security-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the RBAC authorization module in the API server evaluate a request internally?',
    options: [
      'It checks a single role for the user',
      'It iterates through all RoleBindings and ClusterRoleBindings, collecting rules from all referenced Roles/ClusterRoles for the requesting subject, then checks if any rule allows the requested verb on the resource',
      'It delegates to etcd for authorization decisions',
      'It uses a fixed priority order of roles',
    ],
    correctAnswer: 1,
    explanation: 'The RBAC authorizer: 1) identifies the user/groups/SA from the authentication info. 2) Collects all RoleBindings (in the request namespace) and ClusterRoleBindings where the subject matches. 3) Aggregates rules from all referenced roles. 4) Checks if any rule permits the action. RBAC is allow-only; no matching rule means deny.',
    tags: ['kubernetes', 'rbac', 'roles', 'authorization', 'internals'],
  },
  {
    id: 'k8s-security-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How does the RBAC escalation prevention mechanism work internally in the API server?',
    options: [
      'It compares the new role to the user current role',
      'When creating/updating a Role, the API server checks if the requesting user has all permissions in the new Role rules (covers check), or has the "escalate" verb on the roles resource',
      'It uses a hash comparison of permissions',
      'Escalation prevention is handled by admission webhooks only',
    ],
    correctAnswer: 1,
    explanation: 'The API server performs a "covers" check: for each rule in the Role being created/updated, it verifies the requesting user has those permissions. If any rule grants permissions the user lacks, the request is denied unless the user has the "escalate" verb on roles/clusterroles.',
    tags: ['kubernetes', 'rbac-best-practices', 'escalation-prevention', 'api-server', 'internals'],
  },
  {
    id: 'k8s-security-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server admission chain work internally for a pod creation request?',
    options: [
      'Authentication, then admission, then authorization',
      'Authentication → Authorization → Mutating Admission (webhooks + built-in) → Object Schema Validation → Validating Admission (webhooks + built-in) → etcd Persistence',
      'All steps run in parallel',
      'Admission runs before authentication',
    ],
    correctAnswer: 1,
    explanation: 'The full chain: 1) Authentication (who). 2) Authorization/RBAC (allowed?). 3) Mutating admission plugins in order (ServiceAccount, LimitRanger, MutatingWebhooks). 4) Object validation (schema). 5) Validating admission (PodSecurity, ResourceQuota, ValidatingWebhooks, VAP). 6) etcd write.',
    tags: ['kubernetes', 'admission-controllers', 'api-server', 'chain', 'internals'],
  },
  {
    id: 'k8s-security-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the TokenRequest API generate and validate bound service account tokens internally?',
    options: [
      'It generates random tokens stored in etcd',
      'The API server signs a JWT using its private key with claims (sub, aud, exp, pod binding); validation uses the public key to verify signature and checks expiry, audience, and pod/secret binding existence',
      'Tokens are generated by the kubelet',
      'Tokens are fetched from an OIDC provider',
    ],
    correctAnswer: 1,
    explanation: 'TokenRequest: API server creates a JWT with header (alg, kid), payload (iss, sub=SA, aud, exp, kubernetes.io claims for pod name/UID, namespace). Signed with the SA key pair. TokenReview validates: signature, expiry, audience, and that the bound pod/secret still exists.',
    tags: ['kubernetes', 'service-accounts', 'token-request', 'jwt', 'internals'],
  },
  {
    id: 'k8s-security-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How does the kubelet translate SecurityContext settings into OCI runtime spec and cgroup configuration?',
    options: [
      'SecurityContext is passed directly to the container',
      'The kubelet maps SecurityContext to OCI spec fields: runAsUser→Process.User.UID, capabilities→Process.Capabilities, seccomp→Linux.Seccomp, and creates cgroup settings for resource limits',
      'The container runtime interprets SecurityContext directly',
      'SecurityContext is stored as environment variables',
    ],
    correctAnswer: 1,
    explanation: 'Kubelet → CRI → OCI mapping: runAsUser→Process.User.UID, runAsGroup→Process.User.GID, capabilities→Process.Capabilities.{Bounding,Effective,Permitted}, seccomp→Linux.Seccomp, readOnlyRootFilesystem→Root.Readonly, seLinux→Process.SelinuxLabel, appArmor→Process.ApparmorProfile.',
    tags: ['kubernetes', 'security-context', 'oci', 'kubelet', 'cri', 'internals'],
  },
  {
    id: 'k8s-security-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'pod-security-standards',
    question: 'How does the PodSecurity admission controller evaluate pod specs against the standards internally?',
    options: [
      'It uses a simple field comparison',
      'It implements checks as a set of Check functions per standard level; each check validates specific fields against allowed values; pod-level and container-level checks are evaluated separately with version-specific policies',
      'It delegates to an external webhook',
      'It only checks annotations',
    ],
    correctAnswer: 1,
    explanation: 'PSA implementation: each standard level (baseline/restricted) has a set of Check functions organized by version. Checks validate: hostProcess, hostNamespaces, privileged, capabilities, volumes, hostPorts, appArmor, seLinux, procMount, seccomp, sysctls, runAsNonRoot, runAsUser, seccompProfile. Version pinning selects which checks apply.',
    tags: ['kubernetes', 'pod-security-standards', 'admission-controller', 'internals'],
  },
  {
    id: 'k8s-security-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How does the kubelet configure seccomp profiles at the container runtime level?',
    options: [
      'It modifies the kernel configuration',
      'The kubelet sets the seccomp profile in the CRI ContainerConfig SecurityProfile field; the CRI runtime maps this to the OCI Linux.Seccomp spec which is loaded as a BPF program by the kernel at container start',
      'Seccomp is applied by the API server',
      'The container runtime downloads profiles from the API server',
    ],
    correctAnswer: 1,
    explanation: 'Flow: 1) kubelet reads SecurityContext.seccompProfile. 2) For Localhost: reads JSON profile from node disk. 3) Sets CRI SecurityProfile with the profile data. 4) CRI runtime writes OCI Linux.Seccomp config. 5) runc/crun loads the seccomp BPF filter via seccomp() syscall before exec.',
    tags: ['kubernetes', 'seccomp', 'kubelet', 'cri', 'oci', 'bpf'],
  },
  {
    id: 'k8s-security-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How does Gatekeeper handle high-availability and consistency during policy evaluation?',
    options: [
      'Gatekeeper uses a single replica only',
      'Gatekeeper deploys multiple webhook replicas; OPA evaluates policies using its in-memory data store replicated via Kubernetes API sync; each replica independently evaluates requests against the same policy/data set',
      'Gatekeeper uses distributed consensus',
      'Only one replica handles requests at a time',
    ],
    correctAnswer: 1,
    explanation: 'Gatekeeper HA: multiple replicas behind a Service. Each replica runs OPA with synced data from the Kubernetes API (via audit controller informers). Evaluation is stateless (same policies + data = same result). Data sync has eventual consistency lag; audit reconciles.',
    tags: ['kubernetes', 'opa-gatekeeper', 'high-availability', 'consistency', 'internals'],
  },
  {
    id: 'k8s-security-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How does the API server audit system handle high throughput without impacting API latency?',
    options: [
      'Audit logging is synchronous and always impacts latency',
      'The audit system uses an async backend with configurable buffering; events are queued in memory and batched for writing; truncation and sampling can be configured for very high-throughput clusters',
      'Audit events are written directly to etcd',
      'The audit system drops events under load',
    ],
    correctAnswer: 1,
    explanation: 'Audit performance: log backend is synchronous (can impact latency). Webhook backend supports async mode with configurable buffer size, batch size, and throttling. --audit-webhook-batch-max-wait, --audit-webhook-batch-throttle-qps, and --audit-webhook-batch-buffer-size control behavior.',
    tags: ['kubernetes', 'audit-logging', 'performance', 'async', 'buffering'],
  },
  {
    id: 'k8s-security-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does container image verification work at the admission level with Sigstore/cosign?',
    options: [
      'Images are verified at pull time by the runtime',
      'A validating webhook (policy-controller/Kyverno) receives the pod spec, extracts image references, fetches signatures from the registry OCI manifest, verifies them against configured public keys/keyless identity, and allows/denies admission',
      'The API server verifies images internally',
      'Images are verified by the kubelet',
    ],
    correctAnswer: 1,
    explanation: 'Verification flow: 1) Webhook receives AdmissionReview with pod spec. 2) Extracts image references. 3) Queries registry for cosign signatures (stored as OCI artifacts). 4) Verifies signature cryptographically against public key or keyless identity (Fulcio cert + Rekor transparency log). 5) Returns allow/deny.',
    tags: ['kubernetes', 'image-security', 'sigstore', 'cosign', 'verification', 'internals'],
  },
  {
    id: 'k8s-security-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the RBAC rule matching algorithm handle wildcard resources and subresources?',
    options: [
      'Wildcards match everything including subresources',
      'Resource "*" matches all resources but NOT subresources; to match subresources, you need "*/status" or "*/*" for all subresources; resourceNames cannot use wildcards',
      'Subresources inherit parent resource permissions',
      'Wildcards are not supported in RBAC',
    ],
    correctAnswer: 1,
    explanation: 'RBAC matching: resources: ["*"] matches all top-level resources but not subresources. "pods/*" matches all pod subresources. "*/*" matches everything. resourceNames does not support wildcards; each name must be explicit. apiGroups: ["*"] matches all API groups.',
    tags: ['kubernetes', 'rbac', 'roles', 'wildcards', 'subresources', 'matching'],
  },
  {
    id: 'k8s-security-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the OIDC discovery endpoint for service account token validation work?',
    options: [
      'It is a standard OIDC endpoint on the API server',
      'The API server serves .well-known/openid-configuration with the issuer URL, JWKS endpoint, and supported claims; external services can validate SA tokens without calling the TokenReview API',
      'Each service account has its own OIDC endpoint',
      'OIDC discovery is only for human users',
    ],
    correctAnswer: 1,
    explanation: 'The API server exposes OIDC discovery at /.well-known/openid-configuration with the issuer URL (--service-account-issuer) and JWKS at /openid/v1/jwks. External services (AWS IAM, Vault) can validate SA JWTs by fetching the JWKS and verifying signatures locally, without K8s API access.',
    tags: ['kubernetes', 'service-accounts', 'oidc', 'discovery', 'token-validation'],
  },
  {
    id: 'k8s-security-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'What is the complete lifecycle of Linux capability handling from SecurityContext to process execution?',
    options: [
      'Capabilities are set once and never change',
      'Kubelet maps SecurityContext capabilities to OCI spec; the runtime sets the capability bounding set via prctl, then execve applies the new set; the effective/permitted/inheritable sets are computed based on the bounding set and the file capabilities of the executed binary',
      'Capabilities are handled by the kernel only',
      'The container runtime ignores SecurityContext capabilities',
    ],
    correctAnswer: 1,
    explanation: 'Capability lifecycle: 1) SecurityContext.capabilities → OCI Process.Capabilities (Bounding, Effective, Permitted, Inheritable, Ambient). 2) runc sets bounding set via prctl(PR_CAPBSET_DROP). 3) On exec, kernel computes new caps from: bounding set ∩ (file inheritable ∪ file permitted). 4) Ambient caps add to inherited for non-setuid.',
    tags: ['kubernetes', 'security-context', 'capabilities', 'lifecycle', 'kernel'],
  },
  {
    id: 'k8s-security-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the ValidatingAdmissionPolicy CEL evaluation engine work?',
    options: [
      'CEL expressions are compiled at runtime for each request',
      'CEL expressions are compiled and type-checked at policy creation time; at admission, the compiled program evaluates against the request object with cost budgets to prevent resource exhaustion',
      'CEL is interpreted like JavaScript',
      'CEL delegates to OPA for evaluation',
    ],
    correctAnswer: 1,
    explanation: 'VAP CEL: 1) At policy creation: expressions are parsed, type-checked against the resource schema, and compiled to an AST. 2) At admission: compiled program evaluates with the request object bound to "object" and "oldObject". 3) Cost budget (runtime cost limit) prevents expensive evaluations. 4) Evaluation is sandboxed (no I/O).',
    tags: ['kubernetes', 'admission-controllers', 'vap', 'cel', 'compilation', 'internals'],
  },
  {
    id: 'k8s-security-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'pod-security-standards',
    question: 'How does the PSA admission controller handle Windows containers differently from Linux?',
    options: [
      'PSA applies identically to both',
      'PSA has Windows-specific exemptions: hostProcess containers are evaluated under windowsHostProcess checks, Linux-specific fields (capabilities, seccomp) are not required for Windows containers',
      'Windows containers are exempt from PSA',
      'PSA does not support Windows',
    ],
    correctAnswer: 1,
    explanation: 'PSA Windows handling: 1) Linux-only fields (capabilities, seccomp, appArmor, seLinux) are not evaluated for Windows containers. 2) Windows hostProcess is evaluated separately (allowed only in Privileged standard). 3) runAsUserName uses the Windows-specific field. 4) Both baseline and restricted have Windows-adapted checks.',
    tags: ['kubernetes', 'pod-security-standards', 'windows', 'linux', 'differences'],
  },
  {
    id: 'k8s-security-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How does the notify_on_release cgroup mechanism interact with seccomp in container escape scenarios?',
    options: [
      'They are unrelated',
      'The notify_on_release cgroup feature can be abused for container escape if a container has write access to cgroup filesystem; seccomp profiles that block mount, open_by_handle_at, and unshare syscalls help prevent cgroup-based escapes',
      'Seccomp prevents all cgroup interactions',
      'notify_on_release is not available in containers',
    ],
    correctAnswer: 1,
    explanation: 'cgroup escape technique: write to notify_on_release + release_agent in cgroup v1 to execute commands on the host. Defense: 1) seccomp blocking mount/umount/pivot_root. 2) Drop CAP_SYS_ADMIN. 3) Read-only /sys/fs/cgroup. 4) Use cgroup v2 (different structure). 5) User namespaces.',
    tags: ['kubernetes', 'seccomp', 'cgroup', 'container-escape', 'defense'],
  },
  {
    id: 'k8s-security-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How does Gatekeeper optimize Rego evaluation performance for complex policies?',
    options: [
      'Rego is always fast regardless of complexity',
      'OPA uses partial evaluation, indexing on rule heads, and the topdown evaluator with memoization; Gatekeeper adds caching of admission review data and constraint evaluation results',
      'Gatekeeper uses a C++ evaluator',
      'Policies are compiled to machine code',
    ],
    correctAnswer: 1,
    explanation: 'Performance optimizations: 1) OPA indexes rule heads for O(1) lookup. 2) Partial evaluation precomputes known parts. 3) Topdown evaluator with memoization avoids redundant computation. 4) Gatekeeper caches constraint evaluation. 5) Data replication uses efficient informer caching.',
    tags: ['kubernetes', 'opa-gatekeeper', 'rego', 'performance', 'optimization'],
  },
  {
    id: 'k8s-security-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How does the API server audit event lifecycle work from request to persistence?',
    options: [
      'Events are generated after the response',
      'The audit system generates events at multiple stages (RequestReceived, ResponseStarted, ResponseComplete); each event is evaluated against the audit policy, filtered by rules, then dispatched to configured backends (log/webhook) with stage-specific processing',
      'Only one event per request is generated',
      'Audit events bypass the API server',
    ],
    correctAnswer: 1,
    explanation: 'Audit lifecycle: 1) RequestReceived event generated before handler. 2) Policy evaluation determines level. 3) ResponseStarted event for long-running (watch/exec). 4) ResponseComplete with final status. 5) Each event filtered by omitStages. 6) Dispatched to log/webhook backends. 7) Backends handle buffering/batching independently.',
    tags: ['kubernetes', 'audit-logging', 'lifecycle', 'stages', 'internals'],
  },
  {
    id: 'k8s-security-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'clusterroles',
    question: 'How does ClusterRole aggregation work internally and what are the consistency implications?',
    options: [
      'Aggregation merges rules at creation time only',
      'The clusterrole-aggregation controller watches ClusterRoles with aggregationRule, continuously reconciles by matching label selectors, and overwrites the rules field; the rules field is controller-managed and should not be manually edited',
      'Aggregation uses etcd transactions',
      'Aggregation happens in the API server memory',
    ],
    correctAnswer: 1,
    explanation: 'The clusterrole-aggregation controller: 1) Watches all ClusterRoles via informer. 2) For ClusterRoles with aggregationRule, matches clusterRoleSelectors against all ClusterRole labels. 3) Collects rules from matching ClusterRoles. 4) Updates the aggregated ClusterRole rules field. 5) Runs continuously with resync.',
    tags: ['kubernetes', 'rbac', 'clusterroles', 'aggregation', 'controller', 'internals'],
  },
  {
    id: 'k8s-security-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the kubelet bound token rotation mechanism work to ensure continuous availability?',
    options: [
      'Tokens are rotated when they expire',
      'The kubelet requests a new token when 80% of the expiration time has elapsed or 24h before expiry (whichever is sooner); it writes the new token atomically using a temp file and rename; the old token remains valid until its expiry',
      'The API server pushes new tokens',
      'Applications must request new tokens themselves',
    ],
    correctAnswer: 1,
    explanation: 'Token rotation: kubelet monitors token expiry via a goroutine. At max(80% of TTL, TTL-24h), it calls TokenRequest API for a new token. Writes to a temp file, then atomic rename to the token path. Applications using the InClusterConfig re-read the file periodically. Old token remains valid until its own expiry.',
    tags: ['kubernetes', 'service-accounts', 'token-rotation', 'kubelet', 'internals'],
  },
  {
    id: 'k8s-security-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How does the Linux kernel enforce the no_new_privs flag set by allowPrivilegeEscalation: false?',
    options: [
      'It removes all capabilities at process start',
      'The kernel sets PR_SET_NO_NEW_PRIVS via prctl, which prevents execve from granting new privileges (no setuid/setgid bits honored, no new capabilities from file capabilities) for the process and all its descendants',
      'It prevents fork() calls',
      'It disables the exec syscall',
    ],
    correctAnswer: 1,
    explanation: 'no_new_privs: kernel flag set via prctl(PR_SET_NO_NEW_PRIVS, 1). Effects: 1) execve ignores setuid/setgid bits on binaries. 2) File capabilities are not granted during exec. 3) Flag is inherited by all child processes. 4) Cannot be unset. This is how allowPrivilegeEscalation: false is implemented.',
    tags: ['kubernetes', 'security-context', 'no-new-privs', 'kernel', 'privilege-escalation'],
  },
  {
    id: 'k8s-security-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server handle webhook certificate rotation without downtime?',
    options: [
      'The API server caches certificates permanently',
      'The API server re-reads the caBundle from the webhook configuration on each request (with caching); cert-manager can automatically update the caBundle in the webhook configuration when certificates rotate',
      'Certificates cannot be rotated without API server restart',
      'The webhook service handles certificate rotation independently',
    ],
    correctAnswer: 1,
    explanation: 'Certificate rotation: 1) cert-manager issues new TLS cert for webhook service. 2) cert-manager updates the caBundle in the WebhookConfiguration via cainjector. 3) API server TLS client re-reads the caBundle from the config (cached with TTL). 4) Webhook pod mounts the new cert from Secret. No downtime with proper overlap.',
    tags: ['kubernetes', 'admission-controllers', 'certificate-rotation', 'cert-manager'],
  },
  {
    id: 'k8s-security-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'What is the performance impact of RBAC with many RoleBindings and how does the API server optimize it?',
    options: [
      'RBAC performance is constant regardless of bindings',
      'The RBAC authorizer uses an in-memory cache (informer-based) of all Roles, ClusterRoles, RoleBindings, and ClusterRoleBindings; authorization checks iterate through cached bindings for the subject; performance degrades with many bindings per subject',
      'RBAC queries etcd for each request',
      'RBAC is O(1) regardless of the number of bindings',
    ],
    correctAnswer: 1,
    explanation: 'RBAC optimization: 1) Informer-based cache of all RBAC objects in API server memory. 2) On each request: filter bindings by subject (user/groups/SA). 3) Collect rules from matched roles. 4) Check if any rule permits the action. Complexity: O(bindings * rules). For large clusters, minimize broad ClusterRoleBindings.',
    tags: ['kubernetes', 'rbac', 'roles', 'performance', 'caching', 'internals'],
  },
  {
    id: 'k8s-security-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'pod-security-standards',
    question: 'How does the PSA controller handle spec changes to Namespace labels atomically?',
    options: [
      'Label changes are applied instantly',
      'PSA labels are evaluated on each pod creation/update; changing enforcement level affects only new pods, not existing ones; the dry-run option tests the impact on existing pods before committing',
      'All existing pods are re-evaluated immediately',
      'Label changes require namespace recreation',
    ],
    correctAnswer: 1,
    explanation: 'PSA enforcement: 1) Labels on namespace are evaluated at pod admission time only. 2) Changing from baseline→restricted only affects new/updated pods. 3) Existing non-compliant pods continue running. 4) kubectl label --dry-run=server tests existing pod compliance. 5) No automatic remediation of existing pods.',
    tags: ['kubernetes', 'pod-security-standards', 'labels', 'enforcement', 'atomicity'],
  },
  {
    id: 'k8s-security-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How do seccomp filters handle multi-threaded applications and what are the implications?',
    options: [
      'Seccomp applies per-process only',
      'Seccomp filters are inherited by all threads (via clone/fork); the SECCOMP_FILTER_FLAG_TSYNC flag synchronizes filters across existing threads; in containers, the filter is applied before the entrypoint exec, covering all subsequent threads',
      'Each thread has its own seccomp filter',
      'Seccomp does not work with threads',
    ],
    correctAnswer: 1,
    explanation: 'Seccomp and threads: 1) Filter set before exec via seccomp() syscall with SECCOMP_FILTER_FLAG_TSYNC. 2) All existing threads get the same filter. 3) New threads (clone) inherit the filter. 4) Filters are additive (cannot be relaxed, only made stricter). 5) For containers, the runtime sets filters before exec, so all application threads are covered.',
    tags: ['kubernetes', 'seccomp', 'threads', 'filter-inheritance', 'kernel'],
  },
  {
    id: 'k8s-security-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How does Gatekeeper handle referential constraints that need to evaluate data from other Kubernetes objects?',
    options: [
      'Gatekeeper can only evaluate the admitted object',
      'Gatekeeper uses the sync config to cache specified resource types; Rego policies access cached data via data.inventory; consistency is eventual as cache updates are async from the Kubernetes API',
      'Gatekeeper queries the API server in real-time',
      'Referential constraints are not supported',
    ],
    correctAnswer: 1,
    explanation: 'Referential constraints: 1) Configure Config.sync to replicate resource types (e.g., Namespaces, Services). 2) Gatekeeper watches these resources and caches them. 3) Rego accesses via data.inventory.namespace[ns][apiGroup][kind][name]. 4) Cache is eventually consistent; new resources may not be visible for a few seconds.',
    codeSnippet: '# Gatekeeper Config for data replication\napiVersion: config.gatekeeper.sh/v1alpha1\nkind: Config\nmetadata:\n  name: config\nspec:\n  sync:\n    syncOnly:\n      - group: ""\n        version: "v1"\n        kind: "Namespace"\n      - group: "networking.k8s.io"\n        version: "v1"\n        kind: "Ingress"',
    tags: ['kubernetes', 'opa-gatekeeper', 'referential', 'data-replication', 'consistency'],
  },
  {
    id: 'k8s-security-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does the Sigstore keyless signing flow work with Fulcio and Rekor?',
    options: [
      'It uses traditional PKI with long-lived keys',
      'Fulcio issues short-lived certificates based on OIDC identity; the signature + cert are stored in Rekor transparency log; verification checks the Rekor entry, Fulcio cert chain, and that the OIDC identity matches the policy',
      'It uses PGP keys stored in Kubernetes Secrets',
      'Sigstore requires a hardware security module',
    ],
    correctAnswer: 1,
    explanation: 'Keyless signing: 1) User authenticates via OIDC (Google, GitHub, etc.). 2) Fulcio issues ephemeral X.509 cert binding OIDC identity to a public key. 3) Image is signed with the private key. 4) Signature + cert recorded in Rekor (transparency log). 5) Verification: check Rekor entry exists, verify cert chain to Fulcio root, verify OIDC identity claim matches policy.',
    tags: ['kubernetes', 'image-security', 'sigstore', 'fulcio', 'rekor', 'keyless'],
  },
  {
    id: 'k8s-security-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How do you detect and prevent RBAC-based lateral movement in a compromised cluster?',
    options: [
      'RBAC prevents all lateral movement',
      'Monitor for: SA token usage from unexpected IPs, cross-namespace bindings creation, new pods referencing privileged SAs, escalate/bind verb usage; implement network-level SA token validation and runtime anomaly detection',
      'Lateral movement is not possible with RBAC',
      'Use firewalls only',
    ],
    correctAnswer: 1,
    explanation: 'Detection: 1) Audit log analysis for SA token misuse (unexpected source IP). 2) Alert on new bindings to privileged roles. 3) Monitor pods/exec in sensitive namespaces. 4) Track SA token creation. Prevention: 1) Minimal RBAC. 2) No auto-mount tokens. 3) Network policies. 4) Runtime monitoring (Falco).',
    tags: ['kubernetes', 'rbac-best-practices', 'lateral-movement', 'detection', 'prevention'],
  },
  {
    id: 'k8s-security-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How does the API server handle audit log integrity and what mechanisms prevent log tampering?',
    options: [
      'Audit logs have built-in integrity checks',
      'The API server does not natively provide log integrity; defense requires: streaming to immutable external storage, TLS for webhook transport, short buffer TTL, and separate access controls for audit infrastructure',
      'Audit logs are signed by the API server',
      'etcd stores audit logs with integrity checks',
    ],
    correctAnswer: 1,
    explanation: 'No native integrity: API server writes plain text/JSON. Defense: 1) Webhook backend streams off-cluster via TLS. 2) Immutable/WORM storage backend. 3) Separate IAM for audit infra (an attacker with cluster access cannot delete audit logs). 4) Short buffer TTL minimizes data loss window. 5) Log ingestion monitoring for gaps.',
    tags: ['kubernetes', 'audit-logging', 'integrity', 'tamper-prevention', 'defense'],
  },
  {
    id: 'k8s-security-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the ServiceAccount admission controller work internally?',
    options: [
      'It only sets the serviceAccountName field',
      'It validates the SA exists, sets the default SA if none specified, auto-mounts the projected token volume, adds imagePullSecrets from the SA, and validates automountServiceAccountToken settings',
      'It creates new ServiceAccounts on demand',
      'It only handles authentication',
    ],
    correctAnswer: 1,
    explanation: 'SA admission controller: 1) Validates referenced SA exists (rejects if not). 2) Sets "default" SA if unspecified. 3) Adds projected token volume (unless automount=false on SA or pod). 4) Merges imagePullSecrets from SA into pod spec. 5) Handles deprecated SA token Secret creation. Runs as a mutating admission plugin.',
    tags: ['kubernetes', 'service-accounts', 'admission-controller', 'internals'],
  },
  {
    id: 'k8s-security-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How does the container runtime implement user namespace mapping for pods with hostUsers: false?',
    options: [
      'The kernel handles it automatically',
      'The kubelet allocates a unique UID/GID range per pod, writes /etc/subuid and /etc/subgid mappings, sets OCI Linux.UIDMappings/GIDMappings, and the runtime configures the user namespace via clone(CLONE_NEWUSER)',
      'User namespaces only remap UID 0',
      'The API server manages UID ranges',
    ],
    correctAnswer: 1,
    explanation: 'User namespace flow: 1) kubelet allocates a range from configured UID space (e.g., 65536 UIDs per pod). 2) Writes OCI Linux.UIDMappings: [{containerID:0, hostID:65536, size:65536}]. 3) Runtime creates user namespace via clone(CLONE_NEWUSER). 4) Writes uid_map/gid_map in /proc/PID/. 5) Container UID 0 → host UID 65536.',
    tags: ['kubernetes', 'security-context', 'user-namespaces', 'uid-mapping', 'runtime'],
  },
  {
    id: 'k8s-security-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'What are the conversion webhook implications for security when CRD versions change?',
    options: [
      'CRD version changes have no security impact',
      'Conversion webhooks can modify CRD objects during version conversion; a compromised conversion webhook can alter any field including security-relevant ones; the webhook runs with the API server trust level',
      'Conversion webhooks are validated by RBAC',
      'CRD versions are always backwards compatible',
    ],
    correctAnswer: 1,
    explanation: 'Security implications: 1) Conversion webhooks see ALL objects being converted (including during list/get). 2) They can modify any field. 3) A compromised webhook is a supply chain attack vector. 4) Runs with high privilege (API server calls it for every CRD operation). Mitigation: audit webhook code, use TLS, limit access to webhook deployment.',
    tags: ['kubernetes', 'admission-controllers', 'conversion-webhook', 'crd', 'security'],
  },
  {
    id: 'k8s-security-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the API server handle authorization for aggregated API servers?',
    options: [
      'Aggregated APIs use the same RBAC as the main API server',
      'The main API server proxies requests to aggregated APIs after its own authn/authz; the aggregated server can perform additional authorization; the SubjectAccessReview API enables delegated authorization back to the main API server',
      'Aggregated APIs bypass RBAC',
      'Each aggregated API has its own RBAC database',
    ],
    correctAnswer: 1,
    explanation: 'Aggregated API auth: 1) Main API server authenticates (authn). 2) Main API server authorizes (RBAC for the aggregated API group). 3) Proxies to aggregated server with user info in headers. 4) Aggregated server can perform additional authz via SubjectAccessReview (delegated to main API server) or its own logic.',
    tags: ['kubernetes', 'rbac', 'aggregated-api', 'authorization', 'proxy'],
  },
  {
    id: 'k8s-security-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How does the Security Profiles Operator handle profile recording and what are the security considerations?',
    options: [
      'It reads /proc/syscalls directly',
      'SPO uses either the log enricher (reads audit/syslog for seccomp log entries) or eBPF-based recording to capture syscalls from target pods; security concern: recording pods must have elevated privileges to monitor syscalls',
      'SPO intercepts syscalls via ptrace',
      'SPO records syscalls from the API server',
    ],
    correctAnswer: 1,
    explanation: 'SPO recording methods: 1) Log enricher: parses /var/log/audit/audit.log or journalctl for seccomp LOG events. Requires seccomp profile with SCMP_ACT_LOG. 2) eBPF recorder: attaches BPF programs to trace syscalls (requires privileged DaemonSet). Security: recording infra itself needs elevated privileges; limit recording to test environments.',
    tags: ['kubernetes', 'seccomp', 'security-profiles-operator', 'recording', 'ebpf'],
  },
  {
    id: 'k8s-security-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'pod-security-standards',
    question: 'How does the PodSecurity admission controller handle pod template mutations (e.g., by other mutating webhooks) that occur after PSA evaluation?',
    options: [
      'PSA re-evaluates after all mutations',
      'PSA is a validating admission controller (runs after mutating webhooks); however, it also runs on workload resources (Deployments, etc.) where it evaluates the pod template, not actual pods, which may miss runtime mutations',
      'PSA ignores mutating webhook changes',
      'PSA runs before any mutations',
    ],
    correctAnswer: 1,
    explanation: 'PSA timing: 1) As a validating admission, PSA evaluates after all mutations. 2) For Deployments/StatefulSets/etc., PSA evaluates the pod template (warn/audit only, not enforce) to give early feedback. 3) Actual enforcement happens on pod creation. 4) If a mutating webhook adds a non-compliant field to a pod, PSA catches it at pod creation time.',
    tags: ['kubernetes', 'pod-security-standards', 'admission', 'mutations', 'ordering'],
  },
  {
    id: 'k8s-security-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'What is the Gatekeeper Expansion mechanism and how does it handle workload resources?',
    options: [
      'Gatekeeper only evaluates pods',
      'Gatekeeper ExpansionTemplate CRD defines how to extract pod specs from workload resources (Deployments, Jobs, etc.) and evaluate them against constraints before the actual pods are created',
      'Gatekeeper delegates workload evaluation to PSA',
      'Workload resources are never evaluated',
    ],
    correctAnswer: 1,
    explanation: 'Expansion: 1) ExpansionTemplate defines how to extract pod spec from a workload (e.g., Deployment spec.template). 2) When a Deployment is created, Gatekeeper extracts the pod template. 3) Evaluates the expanded pod against all constraints. 4) Rejects the Deployment if the pod template violates policies. This provides early feedback.',
    tags: ['kubernetes', 'opa-gatekeeper', 'expansion', 'workload-resources', 'early-validation'],
  },
  {
    id: 'k8s-security-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How does Kubernetes structured audit logging work and what are the implications for SIEM integration?',
    options: [
      'Audit logs are always unstructured text',
      'Kubernetes produces JSON-structured audit events with standardized fields (verb, user, resource, response code, requestURI, objectRef); SIEM systems can parse these for automated correlation, anomaly detection, and compliance reporting',
      'Audit logs are binary encoded',
      'Each Kubernetes version uses a different format',
    ],
    correctAnswer: 1,
    explanation: 'Structured audit: JSON events with fields: apiVersion, kind, level, auditID, stage, requestURI, verb, user (username, groups, uid), sourceIPs, userAgent, objectRef (resource, namespace, name), responseStatus, requestObject, responseObject. SIEM correlation: join by auditID across stages, track user sessions across requests.',
    tags: ['kubernetes', 'audit-logging', 'structured', 'json', 'siem'],
  },
  {
    id: 'k8s-security-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does the OCI distribution spec handle signature artifacts and what are the security implications?',
    options: [
      'Signatures are stored outside the registry',
      'Signatures are stored as OCI artifacts (referrers API) linked to the image manifest via digest; the registry serves them via the referrers endpoint; supply chain attacks can target the signature verification process itself',
      'Each registry has a custom signature format',
      'Signatures are embedded in the image layers',
    ],
    correctAnswer: 1,
    explanation: 'OCI signatures: 1) Stored as separate OCI manifests in the registry. 2) Linked to the image via the Referrers API (tag or API-based). 3) Cosign stores sigs at sha256-<digest>.sig tag. 4) Supply chain attacks: compromised registry could serve wrong sigs, man-in-the-middle on verification, or Rekor transparency log manipulation. Defense: pin trust anchors, verify Rekor inclusion proofs.',
    tags: ['kubernetes', 'image-security', 'oci', 'signatures', 'supply-chain-attack'],
  },
  {
    id: 'k8s-security-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the API server handle multiple authentication methods simultaneously and what is the priority?',
    options: [
      'Only one auth method can be configured',
      'The API server tries authenticators in order: client cert → bearer token (SA, OIDC, webhook) → anonymous; the first successful authenticator determines the identity; multiple can be configured simultaneously',
      'All authenticators must agree',
      'Authentication is handled by etcd',
    ],
    correctAnswer: 1,
    explanation: 'Authentication chain: 1) Client certificate (X.509, CN=user, O=group). 2) Bearer token (checked against: SA token validator, OIDC, token webhook, bootstrap tokens). 3) HTTP basic auth (deprecated). 4) Anonymous (if enabled, returns system:anonymous). First successful match wins. Failed attempts continue to next authenticator.',
    tags: ['kubernetes', 'service-accounts', 'authentication', 'chain', 'priority'],
  },
  {
    id: 'k8s-security-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How does the container runtime handle the interaction between privileged mode and all other SecurityContext fields?',
    options: [
      'Privileged mode respects all SecurityContext fields',
      'Privileged mode overrides most SecurityContext restrictions: grants all capabilities, disables seccomp, enables full device access, shares host namespaces (if configured), and mounts /dev; other fields like runAsUser are still respected',
      'Privileged mode ignores all SecurityContext fields',
      'Privileged mode is identical to running as root',
    ],
    correctAnswer: 1,
    explanation: 'Privileged container: 1) All capabilities granted (overrides drop ALL). 2) Seccomp set to Unconfined. 3) AppArmor set to unconfined. 4) /dev devices mounted. 5) Full /proc and /sys access. 6) However: runAsUser IS respected (can be privileged non-root). 7) readOnlyRootFilesystem IS respected. 8) resource limits ARE enforced.',
    tags: ['kubernetes', 'security-context', 'privileged', 'override', 'interactions'],
  },
  {
    id: 'k8s-security-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server handle webhook conversion from v1beta1 to v1 AdmissionReview?',
    options: [
      'All webhooks must use v1',
      'The API server checks admissionReviewVersions in the webhook config, selects the first mutually supported version, and converts the AdmissionReview to that version; this affects the available fields and response format',
      'Version conversion is handled by the webhook',
      'Only v1beta1 is supported',
    ],
    correctAnswer: 1,
    explanation: 'Version negotiation: 1) Webhook config lists admissionReviewVersions: ["v1", "v1beta1"]. 2) API server selects first version it supports. 3) Creates AdmissionReview in that version. 4) Sends to webhook. 5) Webhook responds with same version. 6) v1 added: warnings, subResource in objectRef, dryRun handling.',
    tags: ['kubernetes', 'admission-controllers', 'webhook', 'version-negotiation', 'compatibility'],
  },
  {
    id: 'k8s-security-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How do you implement a comprehensive Kubernetes security posture management (KSPM) system?',
    options: [
      'Run periodic CIS benchmark scans',
      'Combine: continuous RBAC analysis (detect overprivileged subjects), PSA enforcement across namespaces, admission policy coverage measurement, audit log anomaly detection, runtime threat detection, and image vulnerability management',
      'Use a single security tool',
      'Manual security reviews quarterly',
    ],
    correctAnswer: 1,
    explanation: 'KSPM components: 1) RBAC analyzer (detect overprivilege, unused bindings). 2) PSA/policy coverage dashboard. 3) CIS benchmark continuous scanning (kube-bench). 4) Audit log SIEM integration. 5) Runtime: Falco/Tetragon for threat detection. 6) Image: Trivy Operator for CVE tracking. 7) Network: policy coverage analysis. 8) Secrets: rotation monitoring.',
    tags: ['kubernetes', 'rbac-best-practices', 'kspm', 'security-posture', 'comprehensive'],
  },
  {
    id: 'k8s-security-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the API server SubjectAccessReview API work internally for authorization delegation?',
    options: [
      'It queries etcd directly',
      'SubjectAccessReview sends a request to the authorization chain (RBAC, Node, etc.) for a specified subject and action; the authorizer evaluates it as if the subject made the request directly; this enables delegation and can-i checks',
      'It uses a separate RBAC database',
      'It only checks the requesting user permissions',
    ],
    correctAnswer: 1,
    explanation: 'SubjectAccessReview: 1) API receives SAR with spec (user, groups, verb, resource, namespace). 2) Constructs an authorization.Attributes object. 3) Passes to the authorization chain (RBAC, Node, Webhook, ABAC). 4) Returns allowed/denied. Used by: kubectl auth can-i, webhook authorization delegation, and operator can-i checks.',
    tags: ['kubernetes', 'rbac', 'subject-access-review', 'authorization', 'delegation'],
  },
  {
    id: 'k8s-security-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'What is the performance overhead of seccomp filtering and how does it affect latency-sensitive workloads?',
    options: [
      'Seccomp has zero overhead',
      'Each syscall is evaluated against the BPF filter adding nanoseconds per call; for syscall-heavy workloads this can add measurable overhead; optimize by placing common syscalls first in the filter for faster BPF evaluation',
      'Seccomp adds milliseconds per syscall',
      'Seccomp only has overhead on blocked syscalls',
    ],
    correctAnswer: 1,
    explanation: 'Seccomp overhead: BPF evaluation per syscall (~50-100ns). For syscall-heavy workloads (100K+ syscalls/sec), this can be measurable. Optimization: 1) Order rules so common syscalls match early (BPF is sequential). 2) Use SECCOMP_RET_ALLOW for hot paths. 3) cBPF vs eBPF: classic BPF (used by seccomp) is simpler but less optimizable.',
    tags: ['kubernetes', 'seccomp', 'performance', 'overhead', 'bpf'],
  },
  {
    id: 'k8s-security-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How does Gatekeeper handle constraint template conflicts and dependency ordering?',
    options: [
      'Templates cannot conflict',
      'Each ConstraintTemplate creates a unique CRD; multiple Constraints of the same template are evaluated independently; all constraint violations are collected and returned as a list; there is no ordering between constraints',
      'Templates are evaluated in alphabetical order',
      'Dependencies are resolved automatically',
    ],
    correctAnswer: 1,
    explanation: 'Gatekeeper constraint evaluation: 1) All matching constraints are evaluated independently (no ordering). 2) Each constraint runs its template Rego. 3) Violations from all constraints are collected. 4) If any constraint in "deny" mode has violations, the request is rejected with all violation messages. 5) There is no dependency mechanism between constraints.',
    tags: ['kubernetes', 'opa-gatekeeper', 'constraints', 'evaluation', 'ordering'],
  },
  {
    id: 'k8s-security-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does the Kubernetes image pull process interact with security at each stage?',
    options: [
      'Images are pulled without any security checks',
      'Pull process: resolve tag→digest (registry API), check ImagePullPolicy, authenticate to registry (imagePullSecrets), verify TLS, download layers (content-addressable by digest), verify layer checksums; admission policies can validate before pull',
      'The kubelet handles all security',
      'Image pulls are always unauthenticated',
    ],
    correctAnswer: 1,
    explanation: 'Image pull security: 1) Admission: image policy webhooks validate image references. 2) Resolution: tag→digest via registry API (MITM risk if not TLS). 3) Auth: imagePullSecrets or node-level credentials. 4) TLS verification: registry cert validation. 5) Pull: layers downloaded by digest (content-addressable, tamper-evident). 6) Each layer checksum verified against manifest.',
    tags: ['kubernetes', 'image-security', 'pull-process', 'security-stages', 'verification'],
  },
  {
    id: 'k8s-security-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How do you implement dynamic audit policy changes without API server restart?',
    options: [
      'Audit policy changes always require restart',
      'Use the --audit-policy-file with a dynamic file that the API server periodically re-reads; or use the audit webhook backend with dynamic backends that can be reconfigured without API server changes',
      'Hot-reload is automatic',
      'Use a ConfigMap for the audit policy',
    ],
    correctAnswer: 0,
    explanation: 'Currently, the audit policy file is read at API server startup and cannot be hot-reloaded. Changes require API server restart (rolling update in HA setups). Dynamic audit (alpha feature) was proposed but not widely implemented. Workaround: use webhook backend where the backend service can be reconfigured independently.',
    tags: ['kubernetes', 'audit-logging', 'dynamic-policy', 'restart', 'limitation'],
  },
  {
    id: 'k8s-security-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'clusterroles',
    question: 'How does the API server handle the system:authenticated and system:unauthenticated groups?',
    options: [
      'They are regular groups created by administrators',
      'system:authenticated is automatically added to all authenticated requests; system:unauthenticated is added when anonymous auth succeeds; ClusterRoleBindings to these groups affect ALL users/anonymous users respectively',
      'They only exist in RBAC documentation',
      'They are used for pod-to-pod authentication',
    ],
    correctAnswer: 1,
    explanation: 'Virtual groups: 1) system:authenticated: auto-added to every authenticated user identity (dangerous for ClusterRoleBindings). 2) system:unauthenticated: added for anonymous requests. 3) Binding cluster-admin to system:authenticated gives EVERY user full access. 4) Default installations have minimal bindings to these groups. 5) Audit these regularly.',
    tags: ['kubernetes', 'rbac', 'clusterroles', 'virtual-groups', 'security'],
  },
  {
    id: 'k8s-security-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the Kubernetes Token Review API validate tokens and what are the performance implications?',
    options: [
      'It checks tokens against a database',
      'For SA tokens: the API server verifies the JWT signature locally (fast, no etcd access); for webhook tokens: it calls the token webhook (external dependency, latency); for OIDC tokens: it verifies against cached JWKS from the IdP',
      'All tokens are validated against etcd',
      'Token validation is delegated to the kubelet',
    ],
    correctAnswer: 1,
    explanation: 'Token validation paths: 1) SA JWT: local signature verification with cached public key (microseconds). 2) Bound token: also checks pod/secret existence via API (adds lookup). 3) OIDC: validates against cached JWKS (cache TTL ~10min). 4) Webhook: calls external endpoint (network latency). 5) Bootstrap tokens: checked against etcd Secret. Performance: SA tokens are fastest.',
    tags: ['kubernetes', 'service-accounts', 'token-review', 'validation', 'performance'],
  },
  {
    id: 'k8s-security-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'What are the known container escape techniques and how does each SecurityContext field mitigate them?',
    options: [
      'Container escapes are not possible with SecurityContext',
      'Escape vectors include: kernel exploits (mitigated by seccomp, capabilities drop), /proc manipulation (mitigated by procMount masking), cgroup write (mitigated by read-only fs), setuid (mitigated by no_new_privs), and namespace breakout (mitigated by user namespaces)',
      'SecurityContext prevents all escapes',
      'Only network isolation prevents escapes',
    ],
    correctAnswer: 1,
    explanation: 'Escape → Mitigation mapping: 1) Kernel CVE → seccomp (reduce syscall surface), user namespaces (unprivileged on host). 2) /proc/sys writes → read-only root fs, procMount:Default masking. 3) cgroup notify_on_release → drop CAP_SYS_ADMIN, seccomp block mount. 4) setuid → no_new_privs. 5) Device access → drop ALL capabilities. 6) Host namespace → deny hostPID/hostNetwork/hostIPC.',
    tags: ['kubernetes', 'security-context', 'container-escape', 'mitigation', 'defense-in-depth'],
  },
  {
    id: 'k8s-security-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server implement the CEL cost budget for ValidatingAdmissionPolicy?',
    options: [
      'CEL has no cost limits',
      'CEL expressions are assigned a cost estimate at compilation time based on expression complexity; at runtime, actual cost is tracked per operation; if total cost exceeds the budget (currently 10^7 cost units), evaluation is terminated and the request is denied',
      'The cost is determined by wall-clock time',
      'CEL expressions have a fixed timeout of 1 second',
    ],
    correctAnswer: 1,
    explanation: 'CEL cost model: 1) Compile-time: estimated cost based on expression complexity (list comprehensions, string operations). 2) Runtime: actual cost tracked per CEL operation. 3) Budget: per-expression limit prevents DoS. 4) List operations: cost proportional to list size. 5) Nested iterations: multiplicative cost. 6) String regex: estimated based on pattern complexity.',
    tags: ['kubernetes', 'admission-controllers', 'cel', 'cost-budget', 'dos-prevention'],
  },
  {
    id: 'k8s-security-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How do you implement a complete Kubernetes hardening strategy based on the NSA/CISA Kubernetes Hardening Guide?',
    options: [
      'Follow the CIS benchmark only',
      'Implement all five pillars: supply chain security (image scanning/signing), strong authentication (OIDC, disable anonymous), network separation (network policies, encrypt traffic), audit logging and monitoring, and pod security (PSA restricted, seccomp, capabilities)',
      'Use a managed Kubernetes service for automatic hardening',
      'Hardening is only about network policies',
    ],
    correctAnswer: 1,
    explanation: 'NSA/CISA five pillars: 1) Supply chain: scan images, use digests, sign and verify. 2) Authentication: OIDC, disable anonymous, rotate certs. 3) Network: default-deny policies, encrypt in transit, restrict API access. 4) Audit: comprehensive policy, SIEM integration, alerting. 5) Pod security: restricted PSA, seccomp, least-privilege RBAC, non-root, read-only fs.',
    tags: ['kubernetes', 'rbac-best-practices', 'nsa-cisa', 'hardening', 'comprehensive'],
  },
  {
    id: 'k8s-security-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'pod-security-standards',
    question: 'How does PSA handle ephemeral containers (kubectl debug) differently from regular containers?',
    options: [
      'Ephemeral containers are exempt from PSA',
      'Ephemeral containers are evaluated against the same PSA standards as regular containers; a debug container that violates the namespace PSA level is rejected; this can complicate debugging in restricted namespaces',
      'Ephemeral containers use the privileged standard always',
      'PSA does not know about ephemeral containers',
    ],
    correctAnswer: 1,
    explanation: 'PSA evaluates ephemeral containers the same as regular containers. In a restricted namespace, kubectl debug with an unrestricted profile is rejected. Workaround: use PSA exemptions for specific SAs, or debug in a less restrictive namespace. The kubectl debug --profile flag (1.28+) helps create PSA-compliant debug containers.',
    codeSnippet: '# PSA-compliant debug container\nkubectl debug -it pod/my-app --image=busybox --profile=restricted',
    tags: ['kubernetes', 'pod-security-standards', 'ephemeral-containers', 'debugging'],
  },
  {
    id: 'k8s-security-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How does the interaction between seccomp and ptrace affect container debugging and security?',
    options: [
      'ptrace is always allowed in containers',
      'Seccomp filters apply to all processes including debuggers; a seccomp profile blocking ptrace prevents strace/gdb inside containers; the SECCOMP_RET_TRACE action allows a tracer process to inspect and modify syscalls before they execute',
      'Seccomp does not affect ptrace',
      'ptrace bypasses seccomp filters',
    ],
    correctAnswer: 1,
    explanation: 'Seccomp + ptrace interaction: 1) If seccomp blocks ptrace syscall, gdb/strace cannot attach. 2) SECCOMP_RET_TRACE allows a ptrace-attached process to intercept syscalls. 3) CAP_SYS_PTRACE is needed to ptrace other processes. 4) The restricted PSS drops ALL capabilities (including SYS_PTRACE). 5) For debugging: use ephemeral containers with explicit SYS_PTRACE and a profile allowing ptrace.',
    tags: ['kubernetes', 'seccomp', 'ptrace', 'debugging', 'interaction'],
  },
  {
    id: 'k8s-security-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How do you implement a Gatekeeper policy that validates CRD instances against a JSON schema stored externally?',
    options: [
      'Gatekeeper cannot validate against external schemas',
      'Use Gatekeeper external data provider to fetch the schema, then validate the CRD spec against it in Rego using json.is_valid or custom validation functions',
      'Use the built-in CRD validation only',
      'Store the schema in a ConfigMap and reference it',
    ],
    correctAnswer: 1,
    explanation: 'Approach: 1) Deploy an external data provider that serves JSON schemas. 2) ConstraintTemplate references the provider. 3) Rego fetches schema via external_data[]. 4) Validates CRD spec fields against schema rules. Alternative: sync ConfigMaps containing schemas via Gatekeeper config, access via data.inventory.',
    tags: ['kubernetes', 'opa-gatekeeper', 'external-data', 'json-schema', 'crd-validation'],
  },
  {
    id: 'k8s-security-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How do you implement cross-cluster audit log correlation for a multi-cluster environment?',
    options: [
      'Each cluster audit log is independent',
      'Forward all cluster audit logs to a central SIEM with cluster-identifying metadata; correlate by user identity, timestamps, and cross-cluster actions; use structured audit events for consistent parsing',
      'Use a federated audit system',
      'Cross-cluster correlation is not possible',
    ],
    correctAnswer: 1,
    explanation: 'Cross-cluster correlation: 1) Add cluster name/ID as metadata to all audit events (webhook enrichment or log processing). 2) Central SIEM ingests all clusters. 3) Correlate: same user across clusters (OIDC identity), cross-cluster actions (federated deployments), timing analysis. 4) Alert on: user accessing multiple clusters rapidly, privilege escalation patterns.',
    tags: ['kubernetes', 'audit-logging', 'multi-cluster', 'correlation', 'siem'],
  },
  {
    id: 'k8s-security-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does content trust work with OCI registries and what are the limitations?',
    options: [
      'Content trust is automatic for all registries',
      'Content trust relies on image digest verification (content-addressable storage), optional cosign signatures, and transparency logs; limitations include: tag-to-digest resolution can be spoofed without signature verification, registry compromise can serve wrong manifests for tags',
      'Content trust uses blockchain',
      'OCI registries have built-in content trust',
    ],
    correctAnswer: 1,
    explanation: 'Content trust: 1) Digests ensure layer integrity (SHA-256). 2) But tags are mutable pointers (vulnerable to registry compromise). 3) Cosign signatures bind identity to image digest. 4) Rekor provides non-repudiation (cannot deny signing). Limitations: trust-on-first-use for new images, Fulcio CA compromise, Rekor availability for verification.',
    tags: ['kubernetes', 'image-security', 'content-trust', 'oci', 'limitations'],
  },
  {
    id: 'k8s-security-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the API server Node authorizer work and how does it interact with RBAC?',
    options: [
      'Node authorizer replaces RBAC for kubelets',
      'Node authorizer runs alongside RBAC in the authorization chain; it specifically handles kubelet requests by restricting access to only pods, secrets, configmaps, and PVCs bound to the kubelet node; RBAC handles all other authorization',
      'Node authorizer is deprecated',
      'RBAC handles node authorization completely',
    ],
    correctAnswer: 1,
    explanation: 'Node authorizer: 1) Evaluates requests from users in system:nodes group. 2) Restricts: Secrets/ConfigMaps only for pods on the node. 3) PVCs only for pods on the node. 4) Nodes: own node object only. 5) Works with NodeRestriction admission controller. 6) Authorization chain: Node → RBAC → (Webhook). First "allow" wins; all "no opinion" = deny.',
    tags: ['kubernetes', 'rbac', 'node-authorizer', 'authorization-chain', 'kubelet'],
  },
  {
    id: 'k8s-security-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How do you implement a comprehensive container breakout detection system?',
    options: [
      'Monitor container logs only',
      'Use eBPF-based tools (Tetragon/Falco) to monitor: syscalls indicating escape attempts, unexpected file access to host paths, process namespace changes, capability usage, network connections from container to host services, and kernel exploit indicators',
      'Container breakouts cannot be detected',
      'Use network monitoring only',
    ],
    correctAnswer: 1,
    explanation: 'Detection layers: 1) eBPF/Tetragon: monitor syscalls (mount, pivot_root, unshare, clone with new namespaces). 2) File monitoring: /etc/shadow, /etc/kubernetes, device files access. 3) Process monitoring: shells spawned in restricted containers. 4) Network: unexpected connections to metadata services, kubelet API. 5) Kernel: exploit indicators like kernel module loading.',
    tags: ['kubernetes', 'security-context', 'container-breakout', 'detection', 'ebpf'],
  },
  {
    id: 'k8s-security-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server ensure webhook ordering and consistency when multiple mutating webhooks modify the same object?',
    options: [
      'The API server prevents conflicts',
      'Mutating webhooks run sequentially in defined order; each sees the previous result; however, there is no transactional guarantee; reinvocationPolicy: IfNeeded re-runs webhooks if later mutations change their input; validating webhooks see the final result',
      'All mutations are applied atomically',
      'Conflicts are resolved by the last writer wins',
    ],
    correctAnswer: 1,
    explanation: 'Mutation ordering: 1) Sequential by webhook name/order. 2) Each webhook receives the current (mutated) state. 3) JSON patch from each webhook applied in sequence. 4) reinvocationPolicy: IfNeeded triggers re-evaluation if object changed since last call. 5) After all mutations: validation webhooks see final state. 6) No rollback if validation fails (entire request rejected). 7) Potential for infinite reinvocation (mitigated by max reinvocation count).',
    tags: ['kubernetes', 'admission-controllers', 'webhook', 'ordering', 'consistency'],
  },
  {
    id: 'k8s-security-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How do you implement Kubernetes cluster security for compliance with SOC 2 Type II requirements?',
    options: [
      'SOC 2 does not apply to Kubernetes',
      'Implement: RBAC with documented access policies, comprehensive audit logging with retention, automated vulnerability scanning, incident response procedures, change management via GitOps, encryption in transit and at rest, and regular access reviews',
      'Use a compliant cloud provider only',
      'Run Kubernetes in a SOC 2 certified data center',
    ],
    correctAnswer: 1,
    explanation: 'SOC 2 for Kubernetes: 1) Access control: RBAC with documented policies, JIT access, regular reviews. 2) Audit: comprehensive logging, SIEM alerts, 1-year retention. 3) Change management: GitOps, admission policies. 4) Encryption: TLS for all traffic, secrets encryption at rest. 5) Vulnerability management: continuous scanning, SLA for patching. 6) Incident response: documented procedures, regular drills.',
    tags: ['kubernetes', 'rbac-best-practices', 'soc2', 'compliance', 'governance'],
  },
  {
    id: 'k8s-security-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the structured authentication configuration (AuthenticationConfiguration) replace individual API server flags?',
    options: [
      'It uses the same flags in a different format',
      'AuthenticationConfiguration (1.30+ GA) provides a single file defining all authenticators: multiple OIDC providers, JWT validation rules, CEL claim mappings, and username/group prefixes; replacing scattered --oidc-* flags',
      'It only supports OIDC authentication',
      'It is an alpha feature',
    ],
    correctAnswer: 1,
    explanation: 'AuthenticationConfiguration: 1) Single config file (--authentication-config). 2) Multiple OIDC issuers (not possible with flags). 3) CEL expressions for claim validation and mapping. 4) User validation rules. 5) Structured format (auditable). 6) Supports audience-bound tokens per issuer. 7) Replaces: --oidc-issuer-url, --oidc-client-id, --oidc-username-claim, etc.',
    codeSnippet: 'apiVersion: apiserver.config.k8s.io/v1beta1\nkind: AuthenticationConfiguration\njwt:\n  - issuer:\n      url: https://accounts.google.com\n      audiences: ["my-cluster"]\n    claimMappings:\n      username:\n        expression: "claims.email"\n      groups:\n        expression: "claims.groups"\n    claimValidationRules:\n      - expression: "claims.email_verified == true"',
    tags: ['kubernetes', 'service-accounts', 'authentication-config', 'oidc', 'structured'],
  },
  {
    id: 'k8s-security-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How do eBPF-based security tools (Tetragon, Falco with modern_bpf) compare to seccomp for runtime security?',
    options: [
      'eBPF replaces seccomp entirely',
      'Seccomp provides syscall filtering (prevention); eBPF tools provide deep observability and policy enforcement with context (process tree, file paths, network connections); they are complementary, not replacements',
      'eBPF is less capable than seccomp',
      'They serve identical purposes',
    ],
    correctAnswer: 1,
    explanation: 'Comparison: seccomp: kernel-level syscall filter, low overhead, no context (just syscall number/args), prevention only. eBPF (Tetragon): rich context (process tree, file paths, network 5-tuple), programmable policies, detection + prevention, higher overhead. Best practice: use both. Seccomp for baseline prevention. eBPF/Tetragon for contextual detection and advanced enforcement.',
    tags: ['kubernetes', 'seccomp', 'ebpf', 'tetragon', 'comparison', 'runtime-security'],
  },
  {
    id: 'k8s-security-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How does the Gatekeeper webhook certificate and availability interact with cluster bootstrapping?',
    options: [
      'Gatekeeper is always available during bootstrapping',
      'During cluster bootstrap, Gatekeeper may not be ready, causing webhook failures; use failurePolicy: Ignore initially, or exclude critical namespaces; self-hosting chicken-and-egg: Gatekeeper validates its own namespace resources',
      'Gatekeeper bootstraps before all other components',
      'Certificate issues do not affect Gatekeeper',
    ],
    correctAnswer: 1,
    explanation: 'Bootstrap challenges: 1) Chicken-and-egg: Gatekeeper webhook validates resources in its own namespace. 2) If webhook fails during bootstrap, critical resources may be blocked. 3) Solutions: failurePolicy: Ignore initially, namespace exclusions, startup probes. 4) cert-manager dependency: if cert-manager is not ready, webhook certs may not exist. 5) Order: install CRDs → deploy Gatekeeper → wait for webhook ready → apply constraints.',
    tags: ['kubernetes', 'opa-gatekeeper', 'bootstrapping', 'availability', 'chicken-and-egg'],
  },
  {
    id: 'k8s-security-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How do you implement audit log analysis to detect a sophisticated attack chain (reconnaissance → credential theft → lateral movement → data exfiltration)?',
    options: [
      'Look for individual suspicious events',
      'Correlate multiple audit events over time: list/watch of pods/services (recon) → Secret access or SA token creation (credential theft) → pod creation in new namespace (lateral movement) → volume access or exec commands (exfiltration); use UEBA and timeline analysis',
      'Single events are sufficient for detection',
      'Use rate limiting instead',
    ],
    correctAnswer: 1,
    explanation: 'Attack chain detection: 1) Baseline normal behavior per SA/user (UEBA). 2) Detect recon: unusual list/get patterns. 3) Detect theft: Secret access from unexpected SAs, token creation. 4) Detect lateral: pod creation with different SAs, cross-namespace bindings. 5) Detect exfiltration: pods/exec, volume mounts to new PVCs, DNS exfil. 6) Timeline analysis: correlate by user/IP across events. 7) Machine learning for anomaly scoring.',
    tags: ['kubernetes', 'audit-logging', 'attack-chain', 'detection', 'ueba', 'correlation'],
  },
  {
    id: 'k8s-security-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does the Kubernetes ValidatingAdmissionPolicy (CEL) handle image verification compared to webhook-based approaches?',
    options: [
      'VAP can fully replace image verification webhooks',
      'VAP CEL can validate image format (require digests, check registry prefix) but cannot perform cryptographic signature verification; webhook-based approaches (Sigstore policy controller, Kyverno) are still needed for actual signature verification',
      'VAP is faster and more capable than webhooks',
      'VAP does not support image-related checks',
    ],
    correctAnswer: 1,
    explanation: 'VAP limitations for image security: 1) CEL can check string patterns (registry prefix, digest format). 2) Cannot make external calls (no registry API for signature fetching). 3) Cannot perform cryptographic verification. 4) Use VAP for: registry allowlists, digest requirements, tag patterns. 5) Use webhooks for: signature verification, SBOM attestation checks, vulnerability threshold enforcement.',
    tags: ['kubernetes', 'image-security', 'vap', 'cel', 'limitations', 'comparison'],
  },
  {
    id: 'k8s-security-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How does the Kubernetes API server structured authorization configuration work?',
    options: [
      'Authorization uses only command-line flags',
      'AuthorizationConfiguration (1.30+ beta) defines an ordered chain of authorizers (RBAC, Node, Webhook) in a file; supports multiple webhooks with match conditions (CEL), failure policies, and connection settings per authorizer',
      'Only RBAC can be configured via file',
      'Authorization configuration is managed by etcd',
    ],
    correctAnswer: 1,
    explanation: 'AuthorizationConfiguration: 1) Ordered list of authorizers. 2) Each can have CEL matchConditions (evaluate which requests to check). 3) Webhook authorizer: connection config, timeout, failure policy per instance. 4) Multiple webhooks with different policies. 5) Replaces: --authorization-mode, --authorization-webhook-config-file flags.',
    codeSnippet: 'apiVersion: apiserver.config.k8s.io/v1beta1\nkind: AuthorizationConfiguration\nauthorizers:\n  - type: Node\n  - type: RBAC\n  - type: Webhook\n    name: custom-authz\n    webhook:\n      timeout: 5s\n      failurePolicy: Deny\n      connectionInfo:\n        type: KubeConfigFile\n        kubeConfigFile: /etc/webhook-authz.kubeconfig\n    matchConditions:\n      - expression: "request.resourceAttributes.namespace == \'sensitive\'"',
    tags: ['kubernetes', 'security-context', 'authorization-config', 'structured', 'webhook'],
  },
  {
    id: 'k8s-security-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the self-subject access review differ from subject access review, and what are the security implications?',
    options: [
      'They are identical',
      'SelfSubjectAccessReview checks the current user permissions (always allowed, no RBAC needed); SubjectAccessReview checks any specified user (requires create permission on SAR); SAR can be used for privilege discovery if not properly restricted',
      'SelfSubjectAccessReview requires cluster-admin',
      'SubjectAccessReview only works for ServiceAccounts',
    ],
    correctAnswer: 1,
    explanation: 'Differences: 1) SelfSubjectAccessReview: always allowed, checks own permissions. 2) SubjectAccessReview: requires RBAC permission (create on subjectaccessreviews), can check ANY user. 3) Security: SAR permission allows discovering permissions of other users (reconnaissance). 4) SelfSubjectRulesReview: lists all own permissions (used by kubectl auth can-i --list). 5) Restrict SAR access to admins.',
    tags: ['kubernetes', 'rbac', 'subject-access-review', 'self-subject', 'security'],
  },
  {
    id: 'k8s-security-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server implement dry-run mode for admission webhooks and what are the security considerations?',
    options: [
      'Dry-run skips all admission webhooks',
      'In dry-run mode, the API server still calls webhooks with dryRun:true in the AdmissionReview; webhooks must handle this correctly (no side effects); sideEffects field in the webhook config determines whether the webhook is called during dry-run',
      'Dry-run only applies to built-in admission controllers',
      'Webhooks are never called during dry-run',
    ],
    correctAnswer: 1,
    explanation: 'Dry-run + webhooks: 1) sideEffects: None → webhook called during dry-run (safe, no external changes). 2) sideEffects: NoneOnDryRun → webhook called, knows to skip side effects. 3) sideEffects: Unknown/Some → webhook skipped during dry-run. 4) Security: if a security webhook is skipped during dry-run, users can test policy violations without being blocked.',
    tags: ['kubernetes', 'admission-controllers', 'dry-run', 'side-effects', 'security'],
  },
  {
    id: 'k8s-security-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How do you implement a complete incident response procedure for a compromised Kubernetes cluster?',
    options: [
      'Rebuild the entire cluster',
      'Isolate: contain compromised nodes/pods. Investigate: analyze audit logs, identify attack vector and scope. Remediate: rotate credentials, patch vulnerabilities, revoke compromised tokens. Recover: redeploy clean workloads. Improve: update policies and monitoring',
      'Only rotate credentials',
      'Report to the cloud provider',
    ],
    correctAnswer: 1,
    explanation: 'IR procedure: 1) Contain: cordon compromised nodes, apply deny-all network policies, disable compromised SAs. 2) Investigate: audit log analysis, identify entry point, determine data exposure. 3) Collect evidence: pod specs, RBAC state, network logs. 4) Remediate: rotate ALL tokens/certs, patch CVEs, remove backdoors. 5) Recover: redeploy from clean images. 6) Post-mortem: update policies, improve detection.',
    tags: ['kubernetes', 'rbac-best-practices', 'incident-response', 'security-operations'],
  },
  {
    id: 'k8s-security-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'pod-security-standards',
    question: 'How does the PodSecurity admission controller handle mutating webhooks that modify pod spec security fields after PSA evaluation?',
    options: [
      'PSA always runs last',
      'PSA is a validating admission controller that runs after all mutating webhooks; if a mutating webhook adds a non-compliant field (e.g., privileged: true), PSA will catch and reject it; however, if the mutation removes a security field, PSA validates the final state',
      'PSA runs before mutating webhooks',
      'PSA ignores mutations',
    ],
    correctAnswer: 1,
    explanation: 'PSA execution order: 1) Mutating webhooks run first. 2) PSA (validating) evaluates the FINAL pod spec after all mutations. 3) This is correct behavior: a mutating webhook that removes seccomp profile would be caught. 4) Edge case: a webhook that sets privileged:true on an otherwise compliant pod → PSA rejects. 5) Important: ensure mutating webhooks do not accidentally violate PSA.',
    tags: ['kubernetes', 'pod-security-standards', 'mutating-webhooks', 'ordering', 'validation'],
  },
  {
    id: 'k8s-security-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'What is the relationship between seccomp, LSM (Linux Security Modules), and the security model of a Kubernetes container?',
    options: [
      'They are alternative approaches that cannot coexist',
      'Seccomp filters syscalls at the kernel entry; LSMs (AppArmor, SELinux) provide MAC after syscall dispatch; they are complementary layers: seccomp reduces syscall surface, LSM enforces fine-grained access control on allowed syscalls',
      'LSMs replace seccomp entirely',
      'Seccomp is an LSM',
    ],
    correctAnswer: 1,
    explanation: 'Security layer interaction: 1) DAC (file permissions): traditional access control. 2) LSM hooks: AppArmor/SELinux MAC checks on file/network/capability operations. 3) Seccomp: BPF filter on syscall entry (checked before the syscall executes). Order: seccomp → security_* LSM hooks → actual syscall. Each provides independent protection. All should be used together.',
    tags: ['kubernetes', 'seccomp', 'lsm', 'apparmor', 'selinux', 'security-model'],
  },
  {
    id: 'k8s-security-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the API server handle concurrent token validation requests and what are the scaling implications?',
    options: [
      'Each request creates a new validation goroutine',
      'SA token validation uses cached public keys (in-memory, fast); OIDC uses cached JWKS; webhook authentication is the bottleneck as it requires external calls; the API server uses a token cache (--authentication-token-webhook-cache-ttl) to reduce webhook calls',
      'Token validation is single-threaded',
      'All validation goes through etcd',
    ],
    correctAnswer: 1,
    explanation: 'Token validation scaling: 1) SA JWT: in-memory key verification (microseconds, unlimited concurrency). 2) OIDC: JWKS cached with TTL (~10min), verification is in-memory after cache. 3) Webhook: external network call, cached with --authentication-token-webhook-cache-ttl (default 2m). 4) Bound token: JWT verification + API call to check pod existence (cached). At scale, webhook tokens are the bottleneck.',
    tags: ['kubernetes', 'service-accounts', 'token-validation', 'scaling', 'caching'],
  },
  {
    id: 'k8s-security-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How do you implement a forensic-ready Kubernetes environment for post-incident analysis?',
    options: [
      'Keep default logging settings',
      'Configure: comprehensive audit policy at RequestResponse level for sensitive resources, immutable log storage with long retention, network flow logs, container runtime events, node-level system logs, and pre-built incident response playbooks',
      'Only need audit logs',
      'Use cloud provider forensic tools only',
    ],
    correctAnswer: 1,
    explanation: 'Forensic readiness: 1) Audit logs at RequestResponse for RBAC, Secrets, pods. 2) Immutable storage (WORM) with 1+ year retention. 3) Network flow logs (Cilium/Calico). 4) Container runtime events (Falco/Tetragon). 5) Node syslog/journald. 6) Image SBOMs for vulnerability timeline. 7) etcd backups for state reconstruction. 8) Documented evidence collection procedures.',
    tags: ['kubernetes', 'audit-logging', 'forensics', 'incident-readiness', 'evidence'],
  },
  {
    id: 'k8s-security-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How does the in-toto framework integrate with Kubernetes for end-to-end supply chain security?',
    options: [
      'in-toto is a Kubernetes-native tool',
      'in-toto defines supply chain layouts (expected steps and materials/products); each CI/CD step produces signed link metadata; at admission, the layout and links are verified to ensure the complete supply chain was followed; can be verified by policy engines',
      'in-toto replaces container registries',
      'in-toto is only for physical supply chains',
    ],
    correctAnswer: 1,
    explanation: 'in-toto for Kubernetes: 1) Define supply chain layout (steps: build, test, scan, sign). 2) Each step produces signed link metadata (who did what, with what materials and products). 3) Layout verification checks: all required steps completed, by authorized functionaries, with correct material/product hashes. 4) Kubernetes admission: verify in-toto attestations on images. 5) Integrates with SLSA via attestation format.',
    tags: ['kubernetes', 'image-security', 'in-toto', 'supply-chain', 'attestations'],
  },
  {
    id: 'k8s-security-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How do you implement a comprehensive multi-tenant isolation strategy using SecurityContext, network policies, RBAC, and resource quotas?',
    options: [
      'Use namespaces only',
      'Layer: namespace per tenant with RBAC isolation, ResourceQuotas for fair sharing, NetworkPolicies for network isolation, PSA restricted for pod security, unique SELinux/user namespace labels for kernel-level isolation, and audit logging per tenant',
      'Use separate clusters per tenant',
      'Multi-tenancy requires specialized software only',
    ],
    correctAnswer: 1,
    explanation: 'Multi-tenant isolation layers: 1) RBAC: tenant SA with namespace-only access. 2) Network: deny-all + explicit tenant policies. 3) Resources: quota per namespace. 4) PSA: restricted enforcement. 5) SecurityContext: non-root, drop ALL caps, read-only fs. 6) SELinux MCS: unique categories per tenant. 7) User namespaces: UID remapping. 8) RuntimeClass: gVisor/Kata for stronger isolation. 9) Audit: per-tenant logging for accountability.',
    tags: ['kubernetes', 'security-context', 'multi-tenancy', 'isolation', 'comprehensive'],
  },
  {
    id: 'k8s-security-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server handle the interaction between ValidatingAdmissionPolicy (CEL) and ValidatingAdmissionWebhook?',
    options: [
      'VAP replaces all webhooks',
      'Both run during the validating admission phase; VAP CEL expressions evaluate first (in-process, fast), then validating webhooks are called; both must pass for the request to be admitted; they complement each other for different use cases',
      'Only one can be active at a time',
      'Webhooks run before VAP',
    ],
    correctAnswer: 1,
    explanation: 'Coexistence: 1) VAP (CEL) runs in-process within the API server (no network call). 2) Validating webhooks make external calls. 3) Both are in the validating admission phase. 4) All must pass (any rejection denies the request). 5) Strategy: use VAP for simple, fast checks (label requirements, format validation). Use webhooks for complex logic (image verification, external data checks).',
    tags: ['kubernetes', 'admission-controllers', 'vap', 'webhook', 'coexistence'],
  },
  {
    id: 'k8s-security-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the API server authorization decision caching work and what are the implications for RBAC changes?',
    options: [
      'Authorization decisions are never cached',
      'The API server caches authorization decisions with a short TTL; RBAC changes (new roles/bindings) take effect after the cache expires; the SubjectAccessReview cache is separate from the request authorization cache',
      'RBAC changes are instant',
      'Cache is only used for webhook authorization',
    ],
    correctAnswer: 1,
    explanation: 'RBAC caching: 1) RBAC evaluator uses informer-cached role/binding data (eventual consistency with etcd). 2) Informer watch delay: typically <1 second. 3) SubjectAccessReview uses the same evaluator (same cache). 4) Webhook authorizer has separate cache with --authorization-webhook-cache-authorized-ttl and --authorization-webhook-cache-unauthorized-ttl. 5) After RBAC change: may take up to informer resync period for full effect.',
    tags: ['kubernetes', 'rbac', 'authorization', 'caching', 'consistency'],
  },
  {
    id: 'k8s-security-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'opa-gatekeeper',
    question: 'How do you implement Gatekeeper constraints that validate cross-resource relationships (e.g., an Ingress must reference an existing Service)?',
    options: [
      'Gatekeeper cannot check cross-resource relationships',
      'Configure data replication for the dependent resource type in Gatekeeper Config sync; write Rego that checks data.inventory for the existence of the referenced resource; handle eventual consistency limitations',
      'Use a separate webhook for cross-resource validation',
      'This requires a custom controller',
    ],
    correctAnswer: 1,
    explanation: 'Cross-resource validation: 1) Config.sync: replicate Services into Gatekeeper cache. 2) Rego: on Ingress admission, check data.inventory.namespace[ns][""][\"Service\"][serviceName] exists. 3) Limitation: cache is eventually consistent (a just-created Service may not be visible). 4) Mitigation: use warning mode for tight-coupling policies. 5) Alternative: validating webhook with direct API access.',
    codeSnippet: '# Rego checking Service exists for Ingress\nviolation[{"msg": msg}] {\n  input.review.object.kind == "Ingress"\n  backend := input.review.object.spec.rules[_].http.paths[_].backend\n  service_name := backend.service.name\n  ns := input.review.object.metadata.namespace\n  not data.inventory.namespace[ns][""]["Service"][service_name]\n  msg := sprintf("Service %v not found in namespace %v", [service_name, ns])\n}',
    tags: ['kubernetes', 'opa-gatekeeper', 'cross-resource', 'referential', 'rego'],
  },
  {
    id: 'k8s-security-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'seccomp',
    question: 'How does the kernel seccomp BPF filter interact with container exec (kubectl exec) and what are the implications?',
    options: [
      'Exec processes are not affected by seccomp',
      'The seccomp filter is inherited by all new processes in the container including those spawned via exec; the exec process runs under the same filter as PID 1; this means debug commands (strace, gdb) may be blocked by restrictive profiles',
      'Exec creates a new seccomp context',
      'Only the original entrypoint is filtered',
    ],
    correctAnswer: 1,
    explanation: 'Seccomp + exec: 1) seccomp filter applied to container init process (PID 1 in container). 2) Filter inherited by all child processes (via fork/clone). 3) kubectl exec spawns a process inside the container namespace → inherits the filter. 4) Restrictive profiles may block strace (ptrace), gdb, or other debug tools. 5) Mitigation: use ephemeral containers with less restrictive seccomp for debugging.',
    tags: ['kubernetes', 'seccomp', 'exec', 'debugging', 'filter-inheritance'],
  },
  {
    id: 'k8s-security-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'audit-logging',
    question: 'How does the API server handle audit event annotation enrichment and what custom annotations can be added?',
    options: [
      'Audit events cannot be enriched',
      'Admission webhooks can add audit annotations to events via the response.auditAnnotations field; the API server merges these into the audit event; this enables: policy decision logging, request classification, and traceability',
      'Annotations are added by the audit backend',
      'Only built-in controllers can annotate events',
    ],
    correctAnswer: 1,
    explanation: 'Audit annotation enrichment: 1) Admission webhook response includes auditAnnotations map. 2) API server adds these to the audit event metadata.annotations. 3) Use cases: policy engine (Gatekeeper) logs which constraints matched, mutation webhook logs what was changed, custom webhook adds business context. 4) Annotations survive to the audit backend for SIEM analysis.',
    tags: ['kubernetes', 'audit-logging', 'annotations', 'enrichment', 'webhooks'],
  },
  {
    id: 'k8s-security-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'image-security',
    question: 'How do you implement runtime image drift detection in a Kubernetes cluster?',
    options: [
      'Compare running images to a whitelist periodically',
      'Use admission policies to record deployed images, runtime monitoring (Falco/Tetragon) to detect unexpected binary execution within containers, and continuous scanning to detect new CVEs in running images',
      'Image drift cannot be detected',
      'Use immutable images only',
    ],
    correctAnswer: 1,
    explanation: 'Drift detection layers: 1) Admission: record expected image digests per deployment. 2) Runtime: eBPF/Falco monitors execve() for unexpected binaries (not from the original image). 3) File integrity: monitor writes to sensitive paths. 4) Image re-scan: Trivy Operator continuously scans running image digests for new CVEs. 5) Comparison: alert when running image differs from expected (e.g., manual kubectl set image).',
    tags: ['kubernetes', 'image-security', 'drift-detection', 'runtime', 'monitoring'],
  },
  {
    id: 'k8s-security-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'service-accounts',
    question: 'How does the KubeAPIServer handle token invalidation for bound service account tokens when a pod is deleted?',
    options: [
      'Tokens remain valid until they expire',
      'Bound tokens include the pod UID in claims; the TokenReview API checks if the bound pod still exists; if the pod is deleted, the token fails validation even before its expiry time; the kubelet also removes the projected token volume',
      'Tokens are immediately revoked in etcd',
      'The kubelet sends a revocation request',
    ],
    correctAnswer: 1,
    explanation: 'Bound token invalidation: 1) Token JWT contains kubernetes.io/pod claim with name and UID. 2) TokenReview validation checks: a) signature valid, b) not expired, c) audience matches, d) referenced pod exists (API lookup/cache). 3) Pod deleted → next TokenReview check fails. 4) No active revocation; relies on validation-time check. 5) Cached validation may briefly accept deleted pod tokens.',
    tags: ['kubernetes', 'service-accounts', 'token-invalidation', 'bound-tokens', 'pod-deletion'],
  },
  {
    id: 'k8s-security-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'security-context',
    question: 'How does the kernel cgroup device controller interact with privileged containers and device access?',
    options: [
      'All containers have full device access',
      'Non-privileged containers get an allowlist (null, zero, full, random, urandom, tty, console, ptmx); privileged containers get all devices via "a *:* rwm" cgroup rule; the devices cgroup controller mediates all device access even for root',
      'Device access is controlled by SELinux only',
      'The kubelet manages device access directly',
    ],
    correctAnswer: 1,
    explanation: 'Device access: 1) cgroup devices controller: whitelist/blacklist of allowed devices (major:minor, type). 2) Non-privileged default: /dev/null, /dev/zero, /dev/full, /dev/random, /dev/urandom, /dev/tty*, /dev/pts/*, /dev/console. 3) Privileged: "a *:* rwm" (all devices, all access). 4) Extended resources (GPU): device plugin adds specific device to the cgroup. 5) Even root in a non-privileged container cannot access /dev/sda.',
    tags: ['kubernetes', 'security-context', 'cgroup', 'devices', 'privileged'],
  },
  {
    id: 'k8s-security-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'admission-controllers',
    question: 'How does the API server implement the admission webhook conversion between v1 AdmissionReview request/response formats?',
    options: [
      'No conversion is needed',
      'The API server marshals the request object into the matched API version, constructs the AdmissionReview with the requested version, sends to webhook, then deserializes the response; object versions in the review may differ from the stored version',
      'The webhook handles version conversion',
      'All objects are sent as v1',
    ],
    correctAnswer: 1,
    explanation: 'AdmissionReview conversion: 1) API server receives request (e.g., apps/v1 Deployment). 2) For each webhook: check rules match. 3) If matchPolicy: Equivalent, may send a different API version. 4) Marshal object to the version the webhook expects. 5) Construct AdmissionReview proto. 6) Send via HTTPS. 7) Deserialize response. 8) Apply JSON patches to the original version object. Version skew between webhook expectation and actual version must be handled.',
    tags: ['kubernetes', 'admission-controllers', 'webhook', 'version-conversion', 'api'],
  },
  {
    id: 'k8s-security-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'rbac-best-practices',
    question: 'How do you implement the principle of defense in depth across all Kubernetes security layers?',
    options: [
      'Use a single comprehensive security tool',
      'Layer independent security mechanisms at each level: authentication (OIDC+MFA), authorization (RBAC least privilege), admission (PSA+policies), runtime (seccomp+AppArmor+capabilities), network (policies+mTLS), data (encryption at rest+in transit), monitoring (audit+runtime detection)',
      'Focus on network security only',
      'Rely on cloud provider security',
    ],
    correctAnswer: 1,
    explanation: 'Defense in depth layers: 1) Identity: OIDC+MFA, short-lived tokens, workload identity. 2) Authorization: RBAC least privilege, no wildcards. 3) Admission: PSA restricted, VAP/webhooks, image verification. 4) Pod security: non-root, drop ALL, seccomp, AppArmor, user namespaces, read-only fs. 5) Network: default-deny policies, mTLS, egress filtering. 6) Data: encryption at rest (KMS v2), in transit (TLS). 7) Monitoring: audit logging, Falco/Tetragon, vulnerability scanning. 8) Each layer independently prevents different attack vectors.',
    tags: ['kubernetes', 'rbac-best-practices', 'defense-in-depth', 'comprehensive-security'],
  },
  {
    id: 'k8s-security-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'Security & RBAC',
    subcategory: 'roles',
    question: 'How does the API server handle authorization for watch requests and what are the security implications of long-lived watches?',
    options: [
      'Watch authorization is checked once at connection start',
      'The "watch" verb is authorized at connection establishment; the watch remains open until timeout/disconnect; RBAC changes during a watch do NOT terminate existing watches; this means a revoked permission may still receive events until the watch reconnects',
      'Watch requests are re-authorized on every event',
      'Watches bypass RBAC entirely',
    ],
    correctAnswer: 1,
    explanation: 'Watch authorization: 1) Checked once at watch creation (list+watch verbs). 2) Events streamed without re-authorization. 3) RBAC revocation does NOT close existing watches. 4) Watch reconnects (timeout, network issue) trigger re-authorization. 5) Implication: revoking Secret read access does not immediately stop a running watch. 6) Mitigation: set short watch timeouts, force reconnections, or restart the API server for critical revocations.',
    tags: ['kubernetes', 'rbac', 'watch', 'authorization', 'long-lived', 'security'],
  },
];

export default questions;
