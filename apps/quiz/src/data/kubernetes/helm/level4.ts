import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-helm-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How does Helm internally handle the three-way strategic merge during upgrades?',
    options: [
      'It compares only old and new manifests',
      'It compares old chart manifest, new chart manifest, and live cluster state, using Kubernetes strategic merge patch logic',
      'It uses a simple diff algorithm on YAML text',
      'It delegates all merge logic to the Kubernetes API server',
    ],
    correctAnswer: 1,
    explanation: 'Helm v3 performs a three-way merge comparing the previous release manifest, the new rendered manifest, and the current live state in the cluster. This prevents overwriting manual changes while applying intended chart changes.',
    tags: ['kubernetes', 'helm', 'internals', 'three-way-merge'],
  },
  {
    id: 'k8s-helm-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is stored inside a Helm v3 release Secret and how is it encoded?',
    options: [
      'Plain YAML manifests in the data field',
      'Base64-encoded, gzip-compressed protobuf-serialized release object containing chart, values, and manifest',
      'JSON-encoded release metadata only',
      'A reference to an external storage location',
    ],
    correctAnswer: 1,
    explanation: 'Helm v3 release Secrets contain a release.v1 key with a base64-encoded, gzip-compressed, base64-encoded (double-encoded) protobuf release object that includes the chart, config values, manifest, and metadata.',
    codeSnippet: '# Decode a Helm release secret:\nkubectl get secret sh.helm.release.v1.my-release.v1 \\\n  -o jsonpath=\'{.data.release}\' | base64 -d | base64 -d | gunzip',
    tags: ['kubernetes', 'helm', 'internals', 'release', 'storage'],
  },
  {
    id: 'k8s-helm-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What happens when the Helm template engine encounters an error in a subchart template but not in the parent chart?',
    options: [
      'Only the subchart fails; parent resources are still deployed',
      'The entire release fails because all templates are compiled and rendered as a single unit',
      'The subchart is skipped with a warning',
      'The error is silently ignored',
    ],
    correctAnswer: 1,
    explanation: 'All chart and subchart templates are compiled into a single Go template set. An error anywhere causes the entire rendering to fail, preventing partial deployments.',
    tags: ['kubernetes', 'helm', 'templates', 'error-handling', 'subcharts'],
  },
  {
    id: 'k8s-helm-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What is the exact execution order when a Helm install has both pre-install hooks and CRDs in the crds/ directory?',
    options: [
      'Hooks run first, then CRDs, then resources',
      'CRDs from crds/ are installed first, then pre-install hooks run, then chart resources are applied',
      'CRDs and hooks run simultaneously',
      'Resources are applied first, then hooks, then CRDs',
    ],
    correctAnswer: 1,
    explanation: 'The order is: 1) CRDs from crds/ directory are installed, 2) pre-install hooks run (in weight order), 3) chart resources are applied, 4) post-install hooks run.',
    tags: ['kubernetes', 'helm', 'hooks', 'crds', 'ordering'],
  },
  {
    id: 'k8s-helm-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How does the Go template execution model affect Helm template performance with large value sets?',
    options: [
      'Large values have no performance impact',
      'Each template function call copies the entire scope, so deeply nested values with large data structures can cause significant memory and CPU overhead',
      'Go templates are compiled to machine code',
      'Template execution is parallelized across CPU cores',
    ],
    correctAnswer: 1,
    explanation: 'Go templates pass scope by value in many operations. Large values (especially with range/with) create copies, which can significantly impact rendering time and memory usage for charts with large data sets.',
    tags: ['kubernetes', 'helm', 'templates', 'performance'],
  },
  {
    id: 'k8s-helm-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How does Helm handle concurrent upgrades to the same release?',
    options: [
      'Both upgrades run simultaneously',
      'Helm uses optimistic locking via Kubernetes resource version on the release Secret',
      'Helm uses a distributed lock in etcd',
      'The second upgrade silently overwrites the first',
    ],
    correctAnswer: 1,
    explanation: 'Helm uses Kubernetes resource versioning on the release Secret for optimistic locking. If two upgrades race, one will fail with a conflict error because the Secret version changed.',
    tags: ['kubernetes', 'helm', 'concurrency', 'locking'],
  },
  {
    id: 'k8s-helm-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the security implication of using `tpl` with user-supplied values?',
    options: [
      'There are no security implications',
      'The tpl function renders values as Go templates, enabling template injection if values contain {{ }} expressions',
      'It only affects performance',
      'It can only read local files',
    ],
    correctAnswer: 1,
    explanation: 'The `tpl` function evaluates strings as Go templates. If user-supplied values contain template directives like {{ .Release.Namespace }}, they will be executed. Malicious input could access .Values secrets or use Sprig functions like env to read environment variables.',
    codeSnippet: '# Dangerous if .Values.userInput is untrusted:\n{{ tpl .Values.userInput . }}\n\n# If userInput = "{{ .Values.secret.password }}"\n# It would render the actual password!',
    tags: ['kubernetes', 'helm', 'security', 'template-injection', 'tpl'],
  },
  {
    id: 'k8s-helm-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What happens to orphaned resources when Helm upgrade removes a resource from the template but the resource was manually modified in the cluster?',
    options: [
      'The resource is always deleted',
      'The three-way merge may fail or behave unexpectedly depending on which fields were modified',
      'The resource is preserved and becomes unmanaged',
      'Helm reverts the resource to its original state first',
    ],
    correctAnswer: 1,
    explanation: 'In the three-way merge, if a resource was in the old manifest but not in the new one, Helm deletes it. However, if the live state differs significantly from the old manifest (manual edits), the delete may encounter unexpected issues.',
    tags: ['kubernetes', 'helm', 'three-way-merge', 'orphaned-resources'],
  },
  {
    id: 'k8s-helm-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a Helm library chart that supports multiple rendering strategies?',
    options: [
      'Create multiple library charts',
      'Use define blocks with dict parameters that control rendering behavior via conditional logic within the named template',
      'Library charts only support one strategy',
      'Use external rendering plugins',
    ],
    correctAnswer: 1,
    explanation: 'Library charts can implement flexible named templates that accept dict parameters with strategy flags, using conditional logic to render differently based on the parameters.',
    codeSnippet: '{{- define "common.deployment" -}}\n{{- $ctx := . -}}\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: {{ $ctx.name }}\nspec:\n  {{- if eq (default "rolling" $ctx.strategy) "recreate" }}\n  strategy:\n    type: Recreate\n  {{- else }}\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: {{ default 1 $ctx.maxSurge }}\n      maxUnavailable: {{ default 0 $ctx.maxUnavailable }}\n  {{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'library-chart', 'patterns'],
  },
  {
    id: 'k8s-helm-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'What are the limitations of Kustomize strategic merge patches compared to server-side apply?',
    options: [
      'They are identical in capability',
      'Client-side strategic merge has limited knowledge of custom resource schemas and may not merge arrays correctly for CRDs',
      'Strategic merge is always superior',
      'Server-side apply does not support patches',
    ],
    correctAnswer: 1,
    explanation: 'Client-side strategic merge patches rely on built-in Kubernetes schema knowledge. For CRDs without strategic merge annotations, arrays are replaced instead of merged. Server-side apply (SSA) uses the server schema for correct merging.',
    tags: ['kubernetes', 'kustomize', 'patches', 'strategic-merge', 'ssa'],
  },
  {
    id: 'k8s-helm-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How does Helm handle the case where a release Secret is accidentally deleted?',
    options: [
      'Helm automatically recreates it from the cluster state',
      'The release is orphaned; Helm cannot manage it anymore without manual Secret reconstruction',
      'Helm uses a backup in etcd',
      'The release continues to work normally',
    ],
    correctAnswer: 1,
    explanation: 'If a release Secret is deleted, Helm loses track of the release. The deployed resources remain but Helm cannot upgrade or rollback. Manual reconstruction of the Secret or force-reinstall is required.',
    tags: ['kubernetes', 'helm', 'release', 'disaster-recovery'],
  },
  {
    id: 'k8s-helm-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the impact of using `set` (Sprig) on the .Values object inside a range loop?',
    options: [
      'It safely modifies a copy',
      'It mutates the original .Values dict, which can cause unexpected side effects across templates and range iterations',
      'It has no effect inside range',
      'It creates a new scope automatically',
    ],
    correctAnswer: 1,
    explanation: 'The Sprig `set` function mutates dictionaries in place. Using it on .Values inside a range loop modifies the original .Values, potentially causing unexpected behavior in subsequent templates.',
    codeSnippet: '# DANGEROUS: mutates .Values\n{{- range .Values.services }}\n{{- $_ := set . "fullName" (printf "%s-%s" $.Release.Name .name) }}\n{{- end }}\n# All iterations share the same dict; last write wins!\n\n# SAFE: use deepCopy\n{{- range .Values.services }}\n{{- $svc := deepCopy . }}\n{{- $_ := set $svc "fullName" (printf "%s-%s" $.Release.Name .name) }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'mutation', 'pitfalls'],
  },
  {
    id: 'k8s-helm-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'How do you implement idempotent Helm hooks that can safely run multiple times?',
    options: [
      'Hooks are always idempotent',
      'Use before-hook-creation delete policy and design the hook Job with idempotent operations (e.g., IF NOT EXISTS for DB migrations)',
      'Set restartPolicy: Always',
      'Use a StatefulSet instead of a Job',
    ],
    correctAnswer: 1,
    explanation: 'The before-hook-creation policy deletes previous hook resources. Combined with idempotent logic in the hook (like SQL IF NOT EXISTS), hooks can safely run on every upgrade.',
    codeSnippet: 'annotations:\n  "helm.sh/hook": pre-upgrade\n  "helm.sh/hook-delete-policy": before-hook-creation\nspec:\n  backoffLimit: 3\n  template:\n    spec:\n      restartPolicy: OnFailure\n      containers:\n        - name: migrate\n          command:\n            - sh\n            - -c\n            - |\n              # Idempotent migration\n              alembic upgrade head',
    tags: ['kubernetes', 'helm', 'hooks', 'idempotency'],
  },
  {
    id: 'k8s-helm-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How does Kustomize handle the ordering of transformers internally?',
    options: [
      'Transformers run in alphabetical order',
      'Transformers follow a built-in ordering: generators, then built-in transformers (namePrefix, commonLabels, etc.), then patches, then validators',
      'Transformers run in the order listed in kustomization.yaml',
      'All transformers run simultaneously',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize has a defined transformer ordering: resource generation first, then built-in transformers (namespace, namePrefix, nameSuffix, commonLabels, commonAnnotations), then patches. This ensures predictable results.',
    tags: ['kubernetes', 'kustomize', 'transformers', 'ordering', 'internals'],
  },
  {
    id: 'k8s-helm-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle the edge case where .Values.someMap is null versus an empty map in Helm templates?',
    options: [
      'They behave identically',
      'null causes nil pointer errors; use `default dict` to safely handle both cases',
      'Empty maps cause errors; null is safe',
      'Use try/catch for both cases',
    ],
    correctAnswer: 1,
    explanation: 'A null value and an empty map behave differently. Calling functions like `range` on nil causes errors. Using `default (dict)` provides a safe fallback for nil values.',
    codeSnippet: '# UNSAFE: fails if .Values.annotations is null\n{{- range $k, $v := .Values.annotations }}\n\n# SAFE: default to empty dict\n{{- range $k, $v := (.Values.annotations | default dict) }}\n{{ $k }}: {{ $v | quote }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'null-safety'],
  },
  {
    id: 'k8s-helm-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you recover a Helm release when the release Secret data is corrupted?',
    options: [
      'Helm has built-in corruption recovery',
      'Manually reconstruct the Secret from the last known good state, or delete the corrupted Secret and force reinstall',
      'Restart the Kubernetes API server',
      'Use helm repair command',
    ],
    correctAnswer: 1,
    explanation: 'If a release Secret is corrupted, you must either reconstruct it from backup/history or delete the corrupted revision Secret and use helm upgrade --install to create a new release.',
    codeSnippet: '# List release secrets:\nkubectl get secrets -l owner=helm,name=my-release\n\n# Delete corrupted revision:\nkubectl delete secret sh.helm.release.v1.my-release.v5\n\n# Reinstall:\nhelm upgrade --install my-release my-chart',
    tags: ['kubernetes', 'helm', 'release', 'corruption', 'recovery'],
  },
  {
    id: 'k8s-helm-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the behavior of Helm when a template produces a YAML document with an unknown field for the target Kubernetes version?',
    options: [
      'The field is silently ignored',
      'With client-side dry-run, unknown fields pass; with server-side validation, strict unknown field validation depends on the API server configuration',
      'Helm always rejects unknown fields',
      'Unknown fields cause warnings but always succeed',
    ],
    correctAnswer: 1,
    explanation: 'Client-side rendering does not validate against the Kubernetes schema. Server-side dry-run or install validates fields based on the API server OpenAPI schema. Unknown fields may be rejected depending on the field validation setting.',
    tags: ['kubernetes', 'helm', 'templates', 'validation', 'unknown-fields'],
  },
  {
    id: 'k8s-helm-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do OCI Helm chart artifacts differ from container image artifacts at the registry level?',
    options: [
      'They use the same media types',
      'Helm charts use application/vnd.cncf.helm.chart.content.v1.tar+gzip as the media type, with a config blob of application/vnd.cncf.helm.config.v1+json',
      'Helm charts are stored as image layers',
      'There is no difference; OCI treats them identically',
    ],
    correctAnswer: 1,
    explanation: 'OCI Helm artifacts use specific media types to distinguish them from container images: a custom config type and a chart content layer type, allowing registries to handle them appropriately.',
    tags: ['kubernetes', 'helm', 'oci', 'media-types', 'internals'],
  },
  {
    id: 'k8s-helm-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How does Helm resolve diamond dependencies where two subcharts depend on the same library chart?',
    options: [
      'Both versions are installed independently',
      'Helm flattens dependencies; the last resolved version wins, and named templates from the library may conflict',
      'An error is thrown for conflicting dependencies',
      'Helm automatically uses the latest version',
    ],
    correctAnswer: 1,
    explanation: 'Helm flattens the dependency tree. If multiple subcharts depend on the same library, the resolved version depends on which version is present in charts/. Named templates from the library exist in the global scope, so conflicts can arise.',
    tags: ['kubernetes', 'helm', 'dependencies', 'diamond', 'conflicts'],
  },
  {
    id: 'k8s-helm-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How does Helm determine the order in which resources are applied to the cluster?',
    options: [
      'Alphabetically by file name',
      'In the order they appear in template files',
      'By a predefined install order based on resource kind (Namespace, CRD, ServiceAccount, etc.)',
      'Randomly',
    ],
    correctAnswer: 2,
    explanation: 'Helm applies resources in a predefined kind ordering: Namespace > NetworkPolicy > ResourceQuota > ServiceAccount > Role > RoleBinding > ConfigMap > Secret > Service > Deployment > etc. This ensures dependencies are created first.',
    tags: ['kubernetes', 'helm', 'install-order', 'internals'],
  },
  {
    id: 'k8s-helm-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How does Kustomize handle field ownership conflicts when used with server-side apply?',
    options: [
      'Kustomize always wins',
      'kubectl apply -k with --server-side tracks field ownership per manager; conflicts arise when multiple managers modify the same field',
      'Field ownership is not tracked with Kustomize',
      'Server-side apply is incompatible with Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'Server-side apply with Kustomize tracks which field manager owns each field. If Kustomize and another controller both try to set the same field, a conflict error occurs unless --force-conflicts is used.',
    codeSnippet: '# Apply with SSA:\nkubectl apply -k overlays/prod --server-side\n\n# Force to take ownership of conflicting fields:\nkubectl apply -k overlays/prod --server-side --force-conflicts',
    tags: ['kubernetes', 'kustomize', 'ssa', 'field-ownership'],
  },
  {
    id: 'k8s-helm-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the maximum size of a Helm release Secret and why does it matter?',
    options: [
      'There is no limit',
      'Kubernetes Secrets are limited to 1MB; large charts with many resources or large values can exceed this limit',
      'The limit is 10MB',
      'The limit depends on the chart version',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes Secrets have a 1MB size limit. Since Helm stores the entire release (chart, values, rendered manifests) in a Secret, very large charts can exceed this limit, causing release failures.',
    tags: ['kubernetes', 'helm', 'release', 'size-limit'],
  },
  {
    id: 'k8s-helm-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the subtle issue with generating random values in Helm templates used by multiple resources?',
    options: [
      'Random values are only generated once',
      'Each reference to randAlphaNum generates a different value, causing inconsistency between resources referencing the same "random" value',
      'Random values are deterministic in Helm',
      'The random seed is shared across all templates',
    ],
    correctAnswer: 1,
    explanation: 'Each call to randAlphaNum (or similar) generates a new random value. If a Secret and a Deployment both reference a random value in separate template expressions, they get different values.',
    codeSnippet: '# WRONG: generates different values each call\nSecret: {{ randAlphaNum 16 }}\nDeployment env: {{ randAlphaNum 16 }}  # different!\n\n# CORRECT: generate once and reference\n{{- $password := randAlphaNum 16 -}}\nSecret: {{ $password }}\nDeployment env: {{ $password }}',
    tags: ['kubernetes', 'helm', 'templates', 'random', 'pitfalls'],
  },
  {
    id: 'k8s-helm-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What is the interaction between Helm hooks and Kubernetes admission controllers?',
    options: [
      'Hooks bypass admission controllers',
      'Hook resources go through the same admission control pipeline as regular resources, which can cause hook failures from policy engines like OPA/Gatekeeper',
      'Only pre-install hooks are subject to admission control',
      'Admission controllers are paused during hook execution',
    ],
    correctAnswer: 1,
    explanation: 'Hook resources are regular Kubernetes resources and go through all admission controllers. Policy engines like OPA Gatekeeper or Kyverno can reject hook resources, causing the entire release to fail.',
    tags: ['kubernetes', 'helm', 'hooks', 'admission-control', 'security'],
  },
  {
    id: 'k8s-helm-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How does Kustomize determine the merge key for list items in strategic merge patches?',
    options: [
      'It always uses the first field as the merge key',
      'It uses the patchMergeKey defined in the Kubernetes API schema (e.g., "name" for containers, "containerPort" for ports)',
      'It uses the array index',
      'All list items are always replaced, never merged',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes strategic merge patches use patchMergeKey annotations in the API schema to identify list items. For containers, the merge key is "name"; for ports, it is "containerPort". Kustomize uses these keys for client-side merging.',
    tags: ['kubernetes', 'kustomize', 'patches', 'merge-key', 'internals'],
  },
  {
    id: 'k8s-helm-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement recursive named templates in Helm for rendering nested data structures?',
    options: [
      'Recursion is not supported in Go templates',
      'A named template can call itself by name via include, enabling recursive rendering of tree structures',
      'Use a for loop instead',
      'Use the recurse function from Sprig',
    ],
    correctAnswer: 1,
    explanation: 'Go templates support recursion since a named template can call itself via `include`. This is useful for rendering deeply nested structures like hierarchical configurations.',
    codeSnippet: '{{- define "renderMap" -}}\n{{- range $key, $value := . }}\n{{- if kindIs "map" $value }}\n{{ $key }}:\n{{ include "renderMap" $value | indent 2 }}\n{{- else }}\n{{ $key }}: {{ $value }}\n{{- end }}\n{{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'recursion'],
  },
  {
    id: 'k8s-helm-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you implement a safe multi-cluster Helm deployment strategy that prevents configuration drift?',
    options: [
      'Deploy manually to each cluster',
      'Use GitOps with a single source of truth (Git), cluster-specific value overlays, and automated reconciliation via ArgoCD/Flux',
      'Use a shared Helm repository',
      'Run helm install on each cluster sequentially',
    ],
    correctAnswer: 1,
    explanation: 'GitOps with a single Git repo as source of truth, environment-specific value overlays, and continuous reconciliation via ArgoCD or Flux prevents drift by automatically correcting deviations.',
    tags: ['kubernetes', 'helm', 'multi-cluster', 'gitops', 'drift'],
  },
  {
    id: 'k8s-helm-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the difference between Kustomize "resources" and "generators" at the plugin level?',
    options: [
      'They are the same thing',
      'Resources are static YAML files loaded from disk; generators are plugins that create resources dynamically based on configuration',
      'Resources are for Deployments; generators are for ConfigMaps',
      'Generators only create random data',
    ],
    correctAnswer: 1,
    explanation: 'Resources are static YAML files included directly. Generators are Kustomize plugins (built-in or custom) that dynamically create resources based on input configuration, like configMapGenerator or secretGenerator.',
    tags: ['kubernetes', 'kustomize', 'generators', 'plugins', 'internals'],
  },
  {
    id: 'k8s-helm-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the pitfall of using the `env` Sprig function in Helm templates?',
    options: [
      'It has no pitfalls',
      'The env function reads host environment variables, leaking sensitive data if templates are rendered on CI/CD systems or shared machines',
      'It only reads Kubernetes env vars',
      'It does not exist in Helm',
    ],
    correctAnswer: 1,
    explanation: 'The Sprig `env` function reads environment variables from the machine running Helm. In CI/CD environments with sensitive vars (tokens, passwords), this can leak secrets into rendered manifests.',
    codeSnippet: '# Potentially dangerous in CI/CD:\napi-key: {{ env "API_KEY" }}\n# Renders the actual API_KEY from the CI environment\n\n# Better: use values or external secret management\napi-key: {{ .Values.apiKey }}',
    tags: ['kubernetes', 'helm', 'security', 'env', 'sprig'],
  },
  {
    id: 'k8s-helm-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How does Helmfile handle inter-release dependencies with DAG-based execution?',
    options: [
      'Helmfile always deploys sequentially',
      'The needs field creates a DAG; releases without dependencies deploy in parallel, while dependent releases wait for their prerequisites',
      'Helmfile uses Kubernetes resource ordering',
      'Dependencies must be in separate helmfiles',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile builds a Directed Acyclic Graph from the needs declarations. Independent releases deploy in parallel for speed, while dependent releases wait for their prerequisites to complete.',
    codeSnippet: 'releases:\n  - name: crds\n    chart: ./charts/crds\n  - name: database\n    chart: bitnami/postgresql\n    needs:\n      - crds\n  - name: cache\n    chart: bitnami/redis\n    needs:\n      - crds\n  - name: app  # waits for both db and cache\n    chart: ./charts/app\n    needs:\n      - database\n      - cache',
    tags: ['kubernetes', 'helmfile', 'dag', 'dependencies'],
  },
  {
    id: 'k8s-helm-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a Helm chart that supports both OpenShift and standard Kubernetes?',
    options: [
      'Create separate charts for each platform',
      'Use .Capabilities to detect the platform and conditionally render OpenShift-specific resources (Routes, SCCs) or Kubernetes equivalents (Ingress, PSPs)',
      'OpenShift always uses Helm the same way',
      'Use a Helm plugin for OpenShift',
    ],
    correctAnswer: 1,
    explanation: 'Using .Capabilities.APIVersions.Has to detect OpenShift-specific APIs (route.openshift.io) allows conditional rendering of platform-specific resources alongside standard Kubernetes resources.',
    codeSnippet: '{{- if .Capabilities.APIVersions.Has "route.openshift.io/v1" }}\napiVersion: route.openshift.io/v1\nkind: Route\nmetadata:\n  name: {{ .Release.Name }}\nspec:\n  to:\n    kind: Service\n    name: {{ .Release.Name }}\n{{- else }}\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: {{ .Release.Name }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'openshift', 'multi-platform'],
  },
  {
    id: 'k8s-helm-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the "release drift" problem in Helm and how do you detect it?',
    options: [
      'Release drift does not exist in Helm v3',
      'Drift occurs when cluster state diverges from the Helm release manifest (e.g., manual kubectl edits); detect with helm-diff or ArgoCD sync status',
      'Drift only affects Helm v2',
      'Drift is caused by network latency',
    ],
    correctAnswer: 1,
    explanation: 'Release drift happens when someone manually modifies resources that Helm manages. The helm-diff plugin or GitOps tools like ArgoCD detect drift by comparing desired state with actual cluster state.',
    codeSnippet: '# Detect drift using helm-diff:\nhelm diff revision my-release 0\n# Shows differences between Helm manifest and live state\n\n# Or with ArgoCD:\n# argocd app diff my-app',
    tags: ['kubernetes', 'helm', 'drift', 'detection'],
  },
  {
    id: 'k8s-helm-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you write a custom Kustomize transformer plugin?',
    options: [
      'Custom transformers are not possible',
      'Create an executable or KRM function container that reads ResourceList from stdin and writes modified ResourceList to stdout',
      'Write a Go library and compile it into Kustomize',
      'Use JavaScript plugins',
    ],
    correctAnswer: 1,
    explanation: 'Custom Kustomize plugins follow the KRM Functions specification: they receive a ResourceList on stdin and emit modified resources on stdout. They can be exec plugins or containerized functions.',
    codeSnippet: '# transformer config (in kustomization.yaml):\ntransformers:\n  - |-\n    apiVersion: my.org/v1\n    kind: MyTransformer\n    metadata:\n      name: my-transform\n      annotations:\n        config.kubernetes.io/function: |\n          container:\n            image: myregistry/my-transformer:v1',
    tags: ['kubernetes', 'kustomize', 'plugins', 'transformers', 'krm'],
  },
  {
    id: 'k8s-helm-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What happens when a Helm chart uses both server-side apply and a post-renderer?',
    options: [
      'They are incompatible',
      'The post-renderer modifies manifests before they are sent via SSA, and field ownership is based on the post-rendered output',
      'SSA is skipped when a post-renderer is used',
      'The post-renderer runs after SSA',
    ],
    correctAnswer: 1,
    explanation: 'The post-renderer runs before the manifests are sent to the API server. When combined with SSA, the post-rendered output determines field ownership, which can be useful for adding fields that Helm should not manage.',
    tags: ['kubernetes', 'helm', 'ssa', 'post-renderer'],
  },
  {
    id: 'k8s-helm-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How does Helm handle the edge case of upgrading a release where the target namespace has been deleted and recreated?',
    options: [
      'The upgrade works normally',
      'The release Secrets are gone with the namespace, so Helm sees no existing release and install must be used instead',
      'Helm automatically restores the namespace',
      'The upgrade creates the namespace',
    ],
    correctAnswer: 1,
    explanation: 'Since release Secrets are stored in the release namespace, deleting the namespace removes all release history. Helm no longer recognizes the release and treats it as a new install.',
    tags: ['kubernetes', 'helm', 'namespace', 'edge-case'],
  },
  {
    id: 'k8s-helm-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a Helm chart with cross-resource dependency validation at template time?',
    options: [
      'Cross-resource validation is not possible',
      'Use template variables and conditional fail functions to validate consistency between resources before rendering',
      'Use a Kubernetes validating webhook',
      'Rely on the API server to catch inconsistencies',
    ],
    correctAnswer: 1,
    explanation: 'By computing shared values (like ports, names) as template variables and validating consistency with fail functions, you can catch misconfigurations before resources reach the cluster.',
    codeSnippet: '{{- $svcPort := .Values.service.port -}}\n{{- $containerPort := .Values.container.port -}}\n{{- if ne (int $svcPort) (int $containerPort) }}\n  {{- if not .Values.service.targetPort }}\n    {{- fail (printf "service.port (%d) != container.port (%d) and no targetPort set" (int $svcPort) (int $containerPort)) }}\n  {{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'validation', 'cross-resource'],
  },
  {
    id: 'k8s-helm-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you implement a Kustomize plugin that validates all resources meet security policies before applying?',
    options: [
      'Kustomize does not support validation plugins',
      'Write a KRM validator function that reads resources and returns errors if policies are violated',
      'Use kubectl validate after kustomize build',
      'Integrate with OPA as a Kustomize extension',
    ],
    correctAnswer: 1,
    explanation: 'KRM validator functions inspect the resource list and can emit error results that prevent kustomize build from succeeding if policies are violated.',
    codeSnippet: '# kustomization.yaml\nvalidators:\n  - |-\n    apiVersion: my.org/v1\n    kind: SecurityValidator\n    metadata:\n      name: security-check\n      annotations:\n        config.kubernetes.io/function: |\n          exec:\n            path: ./validate-security.sh\n    spec:\n      requireReadOnlyRoot: true\n      requireNonRoot: true',
    tags: ['kubernetes', 'kustomize', 'validators', 'security', 'krm'],
  },
  {
    id: 'k8s-helm-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What is the edge case with Helm test hooks and long-running test pods?',
    options: [
      'Test pods are always cleaned up',
      'If a test pod hangs, helm test blocks indefinitely unless --timeout is set; the pod remains even after timeout and must be cleaned manually',
      'Test pods have a default 5-minute timeout',
      'Helm kills test pods automatically after 30 seconds',
    ],
    correctAnswer: 1,
    explanation: 'helm test waits for test pods to complete. Without --timeout, a hanging pod blocks forever. After timeout, the command fails but the pod remains running in the cluster.',
    codeSnippet: '# Set a timeout for tests:\nhelm test my-release --timeout 5m\n\n# Clean up test pods:\nhelm test my-release --logs\nkubectl delete pod my-release-test',
    tags: ['kubernetes', 'helm', 'hooks', 'testing', 'edge-case'],
  },
  {
    id: 'k8s-helm-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the interaction between Helm templates and Kubernetes admission webhook timeouts?',
    options: [
      'Admission webhooks do not affect Helm',
      'Slow admission webhooks can cause Helm install/upgrade to timeout even though templates rendered correctly, leading to "pending" release states',
      'Helm bypasses admission webhooks',
      'Admission webhooks run before Helm sends resources',
    ],
    correctAnswer: 1,
    explanation: 'Each resource creation goes through admission webhooks. If webhooks are slow or failing, resources take longer to create. This can cause Helm timeout before all resources are admitted, leaving the release in a pending state.',
    tags: ['kubernetes', 'helm', 'admission-webhooks', 'timeout'],
  },
  {
    id: 'k8s-helm-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you implement a Helm chart CI/CD pipeline with proper validation stages?',
    options: [
      'Just run helm install in CI',
      'Lint -> template render -> schema validation -> unit tests -> dry-run against cluster -> deploy to staging -> integration tests -> promote to production',
      'Run helm upgrade with --atomic',
      'Use only helm test for validation',
    ],
    correctAnswer: 1,
    explanation: 'A comprehensive pipeline includes: helm lint, helm template (render), kubeconform/kubeval (schema validation), helm-unittest (unit tests), dry-run against cluster, staging deployment, integration tests, and production promotion.',
    codeSnippet: '# CI pipeline stages:\nhelm lint my-chart/\nhelm template my-release my-chart/ -f values-prod.yaml | kubeconform -strict\nhelm unittest my-chart/\nhelm upgrade --install my-release my-chart/ \\\n  --dry-run --debug -f values-staging.yaml\nhelm upgrade --install my-release my-chart/ \\\n  -f values-staging.yaml --atomic --wait\nhelm test my-release --timeout 5m',
    tags: ['kubernetes', 'helm', 'ci-cd', 'pipeline', 'validation'],
  },
  {
    id: 'k8s-helm-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you handle the Kustomize limitation of not supporting conditional resource inclusion natively?',
    options: [
      'Kustomize supports conditionals via if statements',
      'Use components for optional features, or generate kustomization.yaml dynamically via a script or CI pipeline before applying',
      'Use patches to remove unwanted resources',
      'This is not a real limitation',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize has no if/else. Components provide feature toggles, or you can generate kustomization.yaml dynamically in CI/CD. Another approach is to include all resources and use patches to scale unwanted ones to zero.',
    tags: ['kubernetes', 'kustomize', 'conditionals', 'workarounds'],
  },
  {
    id: 'k8s-helm-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the "subchart scope leaking" problem in Helm?',
    options: [
      'Subcharts can read parent chart templates',
      'Named templates defined in subcharts are globally accessible and can accidentally override parent chart templates with the same name',
      'Subchart values leak into the parent scope',
      'Subcharts can access the parent Release object',
    ],
    correctAnswer: 1,
    explanation: 'Since all named templates exist in a global scope, a subchart defining a template with the same name as the parent chart (e.g., both define "app.labels") causes one to silently override the other.',
    tags: ['kubernetes', 'helm', 'templates', 'subcharts', 'scope-leaking'],
  },
  {
    id: 'k8s-helm-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How does Helm interact with Kubernetes garbage collection and owner references?',
    options: [
      'Helm sets owner references on all resources',
      'Helm does NOT set owner references; it tracks resources via labels and annotations, not Kubernetes GC. This means orphaned resources require manual cleanup or helm uninstall',
      'Kubernetes GC automatically cleans up Helm resources',
      'Helm uses finalizers for cleanup',
    ],
    correctAnswer: 1,
    explanation: 'Unlike controllers that set owner references, Helm tracks resources via app.kubernetes.io/managed-by labels and meta.helm.sh annotations. It does not use Kubernetes garbage collection via owner references.',
    tags: ['kubernetes', 'helm', 'garbage-collection', 'owner-references'],
  },
  {
    id: 'k8s-helm-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'What are the implications of using OCI registry garbage collection on Helm charts?',
    options: [
      'GC does not affect Helm charts',
      'Untagged chart manifests can be garbage collected; if a chart is referenced only by digest and the tag is removed, GC may delete the chart layers',
      'OCI registries never garbage collect Helm charts',
      'GC only affects container images',
    ],
    correctAnswer: 1,
    explanation: 'Registry garbage collection removes unreferenced layers. If Helm chart manifests lose their tags (during overwrites), the old versions may be collected. Using immutable tags or digest references prevents unexpected deletion.',
    tags: ['kubernetes', 'helm', 'oci', 'garbage-collection'],
  },
  {
    id: 'k8s-helm-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What is the "helmfile fetch" pattern and when is it useful?',
    options: [
      'It fetches remote helmfile configurations',
      'helmfile fetch downloads all chart dependencies locally, enabling offline or air-gapped deployments',
      'It fetches cluster state',
      'It downloads the helmfile binary',
    ],
    correctAnswer: 1,
    explanation: 'helmfile fetch downloads all charts referenced in the helmfile locally. This is essential for air-gapped environments where the deployment machine has no internet access.',
    codeSnippet: '# Download all charts for air-gapped use:\nhelmfile fetch\n\n# Charts are stored in a local cache\n# Then deploy offline:\nhelmfile apply',
    tags: ['kubernetes', 'helmfile', 'air-gapped', 'offline'],
  },
  {
    id: 'k8s-helm-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle Helm chart upgrades that change the Service type from ClusterIP to LoadBalancer?',
    options: [
      'Helm handles this automatically',
      'The Service type field is immutable once set; use --force to delete/recreate, or use a separate Service name for each type',
      'Change the type in values.yaml and upgrade normally',
      'Use a patch to modify the Service type',
    ],
    correctAnswer: 1,
    explanation: 'Some Service fields (including certain transitions of spec.type and related fields like clusterIP) are immutable. Changing from ClusterIP to LoadBalancer may require --force (delete/recreate) or strategic handling in templates.',
    tags: ['kubernetes', 'helm', 'service', 'immutable', 'upgrade'],
  },
  {
    id: 'k8s-helm-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How does Kustomize handle resource ID conflicts when multiple bases include resources with the same GVK and name?',
    options: [
      'The last base wins',
      'Kustomize throws an error for duplicate resource IDs (same group/version/kind/namespace/name) unless buildMetadata nameReference or merge annotations are used',
      'Resources are merged automatically',
      'Both resources are included as-is',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize identifies resources by GVK + namespace + name. Duplicates cause an error. To handle this, use namePrefix to differentiate, or restructure bases to avoid conflicts.',
    tags: ['kubernetes', 'kustomize', 'resource-conflicts', 'edge-case'],
  },
  {
    id: 'k8s-helm-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you implement a Helm release promotion strategy across environments?',
    options: [
      'Manually copy charts between environments',
      'Use the same chart version with environment-specific values, promoting via Git merges or CD pipeline stages with approval gates',
      'Create different chart versions for each environment',
      'Use different release names in each environment',
    ],
    correctAnswer: 1,
    explanation: 'Best practice is one chart version promoted through environments using environment-specific values files, controlled by CD pipeline stages with approval gates, and tracked in Git.',
    tags: ['kubernetes', 'helm', 'promotion', 'environments', 'cd'],
  },
  {
    id: 'k8s-helm-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the behavior when a Helm chart defines a resource with an unknown apiVersion?',
    options: [
      'Helm rejects it during template rendering',
      'helm template succeeds (client-side only validates syntax), but helm install fails when the API server rejects the unknown apiVersion',
      'The resource is created but non-functional',
      'Helm converts it to the closest known API version',
    ],
    correctAnswer: 1,
    explanation: 'helm template only validates Go template syntax, not Kubernetes API compatibility. The Kubernetes API server validates apiVersion during install and rejects unknown ones.',
    tags: ['kubernetes', 'helm', 'templates', 'apiversion', 'validation'],
  },
  {
    id: 'k8s-helm-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you handle the Kustomize issue where commonLabels breaks HPA target references?',
    options: [
      'HPA is not affected by commonLabels',
      'commonLabels adds labels to HPA selectors, which may not match the target Deployment if the Deployment also has commonLabels in its template labels, causing a reference mismatch',
      'Use the labels transformer with includeSelectors: false to avoid modifying selectors',
      'Both B and C describe valid concerns/solutions',
    ],
    correctAnswer: 3,
    explanation: 'commonLabels can cause issues with HPA by modifying labels inconsistently. The newer `labels` transformer with includeSelectors: false avoids modifying selector fields while still adding metadata labels.',
    codeSnippet: '# Safer approach using labels transformer:\napiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\nlabels:\n  - pairs:\n      app: my-app\n      env: production\n    includeSelectors: false  # don\'t touch selectors',
    tags: ['kubernetes', 'kustomize', 'labels', 'hpa', 'edge-case'],
  },
  {
    id: 'k8s-helm-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How does Helmfile handle partial failures during a multi-release apply?',
    options: [
      'All releases are rolled back on any failure',
      'Failed releases are reported; successful releases remain; Helmfile returns a non-zero exit code listing failed releases',
      'Helmfile stops at the first failure',
      'Partial failures are silently ignored',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile processes releases according to the dependency DAG. If a release fails, dependent releases are skipped, but independent releases continue. The overall command returns non-zero with a summary of failures.',
    tags: ['kubernetes', 'helmfile', 'failure-handling', 'partial'],
  },
  {
    id: 'k8s-helm-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a Helm chart that supports both stringData and data fields for Secrets based on user preference?',
    options: [
      'Always use stringData',
      'Use a conditional that checks if values are already base64-encoded and renders either data or stringData accordingly',
      'Always use data with b64enc',
      'Let users edit the Secret directly',
    ],
    correctAnswer: 1,
    explanation: 'By checking a flag (e.g., .Values.secretsBase64Encoded), templates can render either the data field (for pre-encoded values) or stringData field (for plain text) based on user configuration.',
    codeSnippet: 'apiVersion: v1\nkind: Secret\nmetadata:\n  name: {{ .Release.Name }}-secret\ntype: Opaque\n{{- if .Values.secretsBase64Encoded }}\ndata:\n  {{- range $key, $value := .Values.secrets }}\n  {{ $key }}: {{ $value }}\n  {{- end }}\n{{- else }}\nstringData:\n  {{- range $key, $value := .Values.secrets }}\n  {{ $key }}: {{ $value | quote }}\n  {{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'secrets', 'flexibility'],
  },
  {
    id: 'k8s-helm-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the impact of etcd compaction on Helm release history?',
    options: [
      'etcd compaction does not affect Helm',
      'etcd compaction can remove old revisions of Helm release Secrets if they were deleted but not yet compacted, affecting rollback capabilities',
      'etcd compaction improves Helm performance',
      'Helm manages its own compaction',
    ],
    correctAnswer: 1,
    explanation: 'While Helm Secrets persist as regular Kubernetes resources, etcd compaction affects the version history of those resources. If release Secrets are deleted (e.g., by --max-history), compaction reclaims that space permanently.',
    tags: ['kubernetes', 'helm', 'etcd', 'compaction', 'internals'],
  },
  {
    id: 'k8s-helm-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you implement Kustomize configurations that work correctly with both kubectl apply and ArgoCD?',
    options: [
      'They always work the same way',
      'Ensure all remote references are accessible, avoid exec plugins (use container-based functions), and be aware that ArgoCD Kustomize version may differ from local',
      'Use only inline resources',
      'ArgoCD does not support Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'ArgoCD runs kustomize build server-side. Remote bases must be accessible from the ArgoCD server. Exec plugins are restricted by default; use container-based KRM functions. ArgoCD may use a different Kustomize version.',
    tags: ['kubernetes', 'kustomize', 'argocd', 'compatibility'],
  },
  {
    id: 'k8s-helm-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What are the limitations of the Helm values.schema.json validation?',
    options: [
      'There are no limitations',
      'Schema validation only runs on values (not templates), does not support conditional schemas well, and does not validate cross-field dependencies easily',
      'It only works with string values',
      'Schema validation is always bypassed by --set',
    ],
    correctAnswer: 1,
    explanation: 'JSON Schema validation only validates the values structure, not the rendered templates. Complex conditional validations (e.g., "if field A is set, field B is required") are difficult to express in JSON Schema and are better handled with template-level validation.',
    tags: ['kubernetes', 'helm', 'schema', 'validation', 'limitations'],
  },
  {
    id: 'k8s-helm-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you implement Helmfile configurations that support multiple Kubernetes clusters in a single file?',
    options: [
      'Helmfile only supports one cluster',
      'Use the kubeContext field per release to target different clusters, with environment-specific kubeconfig settings',
      'Create separate helmfile.yaml per cluster',
      'Use namespace isolation only',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports per-release kubeContext and kubeConfig settings, allowing a single helmfile to manage releases across multiple clusters.',
    codeSnippet: 'environments:\n  multi-cluster:\n    values:\n      - clusters:\n          us-east: us-east-context\n          eu-west: eu-west-context\n\nreleases:\n  - name: app-us\n    chart: ./charts/app\n    kubeContext: us-east-context\n    values:\n      - region: us-east-1\n  - name: app-eu\n    chart: ./charts/app\n    kubeContext: eu-west-context\n    values:\n      - region: eu-west-1',
    tags: ['kubernetes', 'helmfile', 'multi-cluster'],
  },
  {
    id: 'k8s-helm-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the maximum recursion depth for Go templates in Helm and how do you work around it?',
    options: [
      'There is no recursion limit',
      'Go templates have a default max nesting depth (around 100000 but practical limits from stack size); deeply recursive structures should be flattened or handled iteratively',
      'The limit is 10 levels',
      'Recursion is not supported',
    ],
    correctAnswer: 1,
    explanation: 'Go templates technically support deep recursion but are limited by the goroutine stack size. Practical recursive template depth depends on the complexity of each iteration. Very deep structures should be pre-processed or handled differently.',
    tags: ['kubernetes', 'helm', 'templates', 'recursion', 'limits'],
  },
  {
    id: 'k8s-helm-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you implement a complete disaster recovery plan for Helm releases?',
    options: [
      'Rely on etcd backups only',
      'Combine GitOps source of truth, regular backup of release Secrets, documented values files in VCS, and automated redeployment playbooks',
      'Use helm plugin backup',
      'Helm releases do not need disaster recovery',
    ],
    correctAnswer: 1,
    explanation: 'A complete DR plan includes: charts and values in version control (GitOps), backup of Helm release Secrets and cluster state, documentation of dependency order, and automated redeployment procedures.',
    tags: ['kubernetes', 'helm', 'disaster-recovery', 'backup'],
  },
  {
    id: 'k8s-helm-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'What is the behavior of Kustomize when applying patches to resources that reference other resources by name, and namePrefix is also used?',
    options: [
      'namePrefix does not affect cross-resource references',
      'Kustomize updates name references in known fields (like Service selectors, Ingress backends) when namePrefix is applied, but may miss custom or CRD fields',
      'All references are updated automatically',
      'namePrefix only affects the resource itself',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize has built-in knowledge of common reference fields (like Ingress backend serviceName) and updates them when namePrefix is applied. However, custom fields in CRDs are not automatically updated unless configured via the nameReference transformer.',
    codeSnippet: '# Configure name reference updates for CRDs:\nconfigurations:\n  - name-reference.yaml\n\n# name-reference.yaml\nnameReference:\n  - kind: MyCustomResource\n    fieldSpecs:\n      - path: spec/targetRef/name\n        kind: MyOtherResource',
    tags: ['kubernetes', 'kustomize', 'namePrefix', 'name-references'],
  },
  {
    id: 'k8s-helm-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you design a Helm chart that correctly handles Kubernetes API deprecations across versions?',
    options: [
      'Pin to the oldest supported API version',
      'Use a named template that checks .Capabilities.APIVersions and .Capabilities.KubeVersion to select the correct API version and fields for each resource',
      'Create separate charts per Kubernetes version',
      'API deprecations do not affect Helm charts',
    ],
    correctAnswer: 1,
    explanation: 'A well-designed chart uses .Capabilities to dynamically select API versions (e.g., networking.k8s.io/v1 vs v1beta1 for Ingress) and adjust field names that changed between versions.',
    codeSnippet: '{{- define "my-chart.ingress.apiVersion" -}}\n{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}\nnetworking.k8s.io/v1\n{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}\nnetworking.k8s.io/v1beta1\n{{- else -}}\nextensions/v1beta1\n{{- end }}\n{{- end }}\n\n{{- define "my-chart.ingress.backend" -}}\n{{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}\nservice:\n  name: {{ .serviceName }}\n  port:\n    number: {{ .servicePort }}\n{{- else }}\nserviceName: {{ .serviceName }}\nservicePort: {{ .servicePort }}\n{{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'api-deprecation', 'compatibility'],
  },
  {
    id: 'k8s-helm-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the "helm mapkubeapis" plugin and when do you need it?',
    options: [
      'It maps Kubernetes APIs to Helm charts',
      'It updates deprecated API versions in Helm release metadata when Kubernetes removes old APIs, preventing upgrade failures',
      'It generates API documentation',
      'It maps between different cluster versions',
    ],
    correctAnswer: 1,
    explanation: 'When Kubernetes removes a deprecated API, existing Helm release Secrets contain manifests with the old API version. helm-mapkubeapis updates these stored manifests to use the new API versions, preventing "resource not found" errors during upgrade.',
    codeSnippet: '# Install the plugin:\nhelm plugin install https://github.com/helm/helm-mapkubeapis\n\n# Update stored APIs in a release:\nhelm mapkubeapis my-release --namespace default',
    tags: ['kubernetes', 'helm', 'api-deprecation', 'mapkubeapis'],
  },
  {
    id: 'k8s-helm-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize with GitOps controllers that require all resources to be in a single repository?',
    options: [
      'Git submodules for shared bases',
      'Use the resources field with Git URL references, or vendor remote bases into the repo using a build pipeline',
      'Copy-paste shared resources',
      'GitOps controllers do not work with Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'While Kustomize supports remote references, some GitOps controllers prefer all resources in a single repo. Vendoring (copying remote bases locally via CI) or using Git URL references in resources (if supported by the controller) are common approaches.',
    tags: ['kubernetes', 'kustomize', 'gitops', 'remote-bases'],
  },
  {
    id: 'k8s-helm-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What are the implications of using Helm to manage resources that are also managed by a Kubernetes operator?',
    options: [
      'They work together seamlessly',
      'Both Helm and the operator may try to reconcile the same fields, causing a fight loop; careful field ownership partitioning or using SSA with field managers is required',
      'Operators always take priority',
      'Helm always takes priority',
    ],
    correctAnswer: 1,
    explanation: 'When both Helm and an operator manage the same resource, they can fight over field values. Server-side apply with distinct field managers helps partition ownership. Alternatively, Helm should only manage fields the operator does not touch.',
    tags: ['kubernetes', 'helm', 'operators', 'field-ownership', 'conflicts'],
  },
  {
    id: 'k8s-helm-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you implement Helmfile configurations that support both helm and kustomize in the same deployment?',
    options: [
      'Helmfile only supports Helm charts',
      'Use the kustomize field in releases to apply Kustomize overlays on top of Helm-rendered output',
      'Run helmfile and kubectl apply -k separately',
      'Use a wrapper script',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports a strategic merge with Kustomize where you can specify Kustomize overlays to apply on top of Helm-rendered charts using the strategicMergePatches or jsonPatches in the release configuration.',
    tags: ['kubernetes', 'helmfile', 'kustomize', 'integration'],
  },
  {
    id: 'k8s-helm-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle the scenario where a Helm upgrade needs to rename a Kubernetes resource (which is an immutable field)?',
    options: [
      'Rename directly in the template',
      'Use a two-phase approach: first upgrade to create the new-named resource alongside the old, then a second upgrade to remove the old resource',
      'Resource names can be changed in place',
      'Delete the namespace and redeploy',
    ],
    correctAnswer: 1,
    explanation: 'Resource names are immutable. Renaming requires creating the new resource first (keeping both in one release), migrating any state, then removing the old resource in a subsequent release.',
    tags: ['kubernetes', 'helm', 'rename', 'migration', 'immutable'],
  },
  {
    id: 'k8s-helm-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the impact of running kustomize build vs kubectl kustomize on large kustomization trees?',
    options: [
      'They produce identical output in all cases',
      'kubectl kustomize may use a different (often older) bundled Kustomize version than the standalone kustomize binary, leading to feature and behavior differences',
      'kustomize build is always faster',
      'kubectl kustomize supports more features',
    ],
    correctAnswer: 1,
    explanation: 'kubectl bundles a specific Kustomize version that may lag behind the standalone binary. Features like helmCharts, components, or newer transformer syntax may work differently or not at all in the bundled version.',
    tags: ['kubernetes', 'kustomize', 'version-differences', 'kubectl'],
  },
  {
    id: 'k8s-helm-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement multi-tenancy in a single Helm chart where each tenant gets isolated resources?',
    options: [
      'Create separate releases per tenant',
      'Use range to iterate over a tenants list in values.yaml, generating namespaced resources for each tenant with proper isolation',
      'Multi-tenancy is not possible with Helm',
      'Use namespace selectors only',
    ],
    correctAnswer: 1,
    explanation: 'A single chart can generate per-tenant resources by iterating over a tenants list, creating Namespaces, NetworkPolicies, ResourceQuotas, and application resources for each tenant.',
    codeSnippet: '{{- range .Values.tenants }}\n---\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: tenant-{{ .name }}\n  labels:\n    tenant: {{ .name }}\n---\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: isolate-tenant\n  namespace: tenant-{{ .name }}\nspec:\n  podSelector: {}\n  policyTypes: [Ingress, Egress]\n  ingress:\n    - from:\n        - namespaceSelector:\n            matchLabels:\n              tenant: {{ .name }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'multi-tenancy', 'isolation'],
  },
  {
    id: 'k8s-helm-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the Helm "pending-rollback" state and how do you resolve it?',
    options: [
      'It means a rollback is queued',
      'It means a previous rollback was interrupted; resolve by attempting another rollback or deleting the pending revision Secret',
      'It is a normal transient state',
      'It means the rollback succeeded but resources are still updating',
    ],
    correctAnswer: 1,
    explanation: 'The "pending-rollback" state occurs when a rollback operation was interrupted. To resolve, try another rollback to a known good revision, or manually delete the pending revision Secret and retry.',
    codeSnippet: '# Check the state:\nhelm history my-release\n\n# Try another rollback:\nhelm rollback my-release 2\n\n# If that fails, delete pending revision:\nkubectl delete secret sh.helm.release.v1.my-release.v5',
    tags: ['kubernetes', 'helm', 'pending-rollback', 'troubleshooting'],
  },
  {
    id: 'k8s-helm-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use Kustomize to implement a configuration where all Pods must have a specific security context?',
    options: [
      'Use PodSecurityPolicies instead',
      'Write a wildcard patch targeting all resource kinds that have pod specs, adding the security context to every container',
      'Use Pod Security Standards instead',
      'Kustomize cannot enforce security contexts',
    ],
    correctAnswer: 1,
    explanation: 'A wildcard patch can target multiple resource kinds (Deployment, StatefulSet, DaemonSet, Job) to add security context settings to all containers.',
    codeSnippet: 'patches:\n  - target:\n      kind: Deployment|StatefulSet|DaemonSet\n    patch: |-\n      apiVersion: apps/v1\n      kind: placeholder\n      metadata:\n        name: placeholder\n      spec:\n        template:\n          spec:\n            securityContext:\n              runAsNonRoot: true\n              fsGroup: 1000\n            containers:\n              - name: \"*\"\n                securityContext:\n                  allowPrivilegeEscalation: false\n                  readOnlyRootFilesystem: true\n                  capabilities:\n                    drop: [ALL]',
    tags: ['kubernetes', 'kustomize', 'security-context', 'patches'],
  },
  {
    id: 'k8s-helm-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the performance impact of using lookup extensively in Helm charts with many resources?',
    options: [
      'lookup has no performance impact',
      'Each lookup call makes a Kubernetes API request; many lookups in a chart with hundreds of resources can significantly slow down rendering and overload the API server',
      'lookup results are cached automatically',
      'lookup runs in parallel for better performance',
    ],
    correctAnswer: 1,
    explanation: 'Each lookup function call makes a real Kubernetes API request. Charts with many resources that each use lookup can generate hundreds of API calls during rendering, significantly slowing installation and potentially rate-limiting.',
    tags: ['kubernetes', 'helm', 'lookup', 'performance', 'api-calls'],
  },
  {
    id: 'k8s-helm-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you implement a Helmfile-based platform that enforces organizational policies on all releases?',
    options: [
      'Review each release manually',
      'Use Helmfile base values (defaults), hooks for policy validation, and shared library charts with enforced configurations',
      'Rely on Kubernetes admission controllers only',
      'Helmfile cannot enforce policies',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile can enforce policies through: default values applied to all releases, pre-sync hooks running validation tools (kubeconform, OPA), and shared library charts that bake in security and compliance requirements.',
    codeSnippet: 'helmfile.yaml with shared defaults, helmDefaults (wait/timeout/atomic), and presync hooks running conftest policy validation',
    tags: ['kubernetes', 'helmfile', 'policies', 'governance'],
  },
  {
    id: 'k8s-helm-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How does Helm interact with Kubernetes finalizers on resources it manages?',
    options: [
      'Helm removes all finalizers before deleting resources',
      'Helm sends delete requests but does not manage finalizers; resources with finalizers block deletion until the finalizer controller processes them, which can cause helm uninstall to hang',
      'Helm ignores finalizers completely',
      'Helm adds its own finalizers',
    ],
    correctAnswer: 1,
    explanation: 'Helm issues delete requests but does not handle finalizers. If a resource has a finalizer and no controller is running to handle it, the deletion blocks. With --wait, helm uninstall hangs waiting for deletion to complete.',
    tags: ['kubernetes', 'helm', 'finalizers', 'deletion', 'edge-case'],
  },
  {
    id: 'k8s-helm-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do you implement a multi-architecture Helm chart distribution strategy using OCI registries?',
    options: [
      'Helm charts are architecture-independent',
      'While charts themselves are arch-independent, you can use OCI manifest lists to reference different chart variants, or embed arch-specific image references and use platform-aware values',
      'Create separate charts per architecture',
      'Use Docker buildx for Helm charts',
    ],
    correctAnswer: 1,
    explanation: 'Helm charts are typically architecture-independent, but they reference container images that are not. Best practice is to use multi-arch images and document image tags, or use values that default to multi-arch image manifests.',
    tags: ['kubernetes', 'helm', 'oci', 'multi-architecture'],
  },
  {
    id: 'k8s-helm-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the interaction between Helm server-side dry-run and cluster-scoped resources?',
    options: [
      'Server-side dry-run works identically for all resources',
      'Server-side dry-run for cluster-scoped resources requires cluster-admin permissions even though no resources are created, which can fail in restricted RBAC environments',
      'Cluster-scoped resources are skipped during dry-run',
      'Dry-run does not validate cluster-scoped resources',
    ],
    correctAnswer: 1,
    explanation: 'Server-side dry-run performs admission and validation on the API server. Cluster-scoped resources (like ClusterRole, Namespace) require appropriate RBAC permissions even for dry-run, which may not be available to all users.',
    tags: ['kubernetes', 'helm', 'dry-run', 'rbac', 'cluster-scoped'],
  },
  {
    id: 'k8s-helm-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you design a Kustomize structure for a microservices platform with hundreds of services?',
    options: [
      'Put all services in one kustomization',
      'Use a layered approach: common base, service-type bases (web, worker, cron), per-service overlays, and environment overlays composed via components',
      'Create separate repositories per service',
      'Use Helm instead; Kustomize cannot scale',
    ],
    correctAnswer: 1,
    explanation: 'A scalable Kustomize structure uses layers: shared common base (labels, monitoring), service-type bases (web apps, workers), per-service customization, and environment overlays. Components enable reuse of cross-cutting concerns.',
    codeSnippet: '# Structure:\n# bases/\n#   common/          - shared labels, monitoring\n#   web-service/      - web app template\n#   worker-service/   - worker template\n# components/\n#   istio-sidecar/    - optional Istio injection\n#   monitoring/       - Prometheus annotations\n# services/\n#   user-api/\n#     base/           - service-specific config\n#     overlays/\n#       dev/\n#       staging/\n#       prod/',
    tags: ['kubernetes', 'kustomize', 'architecture', 'microservices', 'scale'],
  },
  {
    id: 'k8s-helm-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the "Helm release name collision" problem in multi-tenant clusters and how do you prevent it?',
    options: [
      'Release names are namespace-scoped, so collisions are impossible',
      'While release names are namespace-scoped in v3, resources may have cluster-scoped names (ClusterRole, CRDs) that collide; use unique name prefixes incorporating tenant/namespace info',
      'Use different clusters for each tenant',
      'Helm prevents all name collisions automatically',
    ],
    correctAnswer: 1,
    explanation: 'Although release names are namespace-scoped, cluster-scoped resources (ClusterRole, ClusterRoleBinding, CRDs) can still collide. Templates must include unique prefixes (release name + namespace) for cluster-scoped resources.',
    codeSnippet: '# Safe cluster-scoped naming:\napiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRole\nmetadata:\n  # Include release name AND namespace to prevent collision\n  name: {{ .Release.Namespace }}-{{ .Release.Name }}-reader',
    tags: ['kubernetes', 'helm', 'multi-tenant', 'name-collision'],
  },
  {
    id: 'k8s-helm-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle Helm chart upgrades that change the StatefulSet volumeClaimTemplate (which is immutable)?',
    options: [
      'StatefulSet PVC templates can be changed freely',
      'You must delete the StatefulSet (--cascade=orphan to keep pods), apply the new StatefulSet, and manually handle PVC migration',
      'Use --force and it works automatically',
      'Create a new StatefulSet with a different name',
    ],
    correctAnswer: 1,
    explanation: 'StatefulSet volumeClaimTemplates are immutable. Changing them requires deleting the StatefulSet without cascading (keeping pods and PVCs), creating the new StatefulSet, and potentially migrating PVC data.',
    codeSnippet: '# 1. Delete StatefulSet keeping pods:\nkubectl delete statefulset my-sts --cascade=orphan\n\n# 2. Upgrade helm chart with new PVC template:\nhelm upgrade my-release my-chart/ --set storage.size=20Gi\n\n# 3. Manually resize PVCs if needed:\nkubectl patch pvc data-my-sts-0 -p \'{\"spec\":{\"resources\":{\"requests\":{\"storage\":\"20Gi\"}}}}\'',
    tags: ['kubernetes', 'helm', 'statefulset', 'pvc', 'immutable'],
  },
  {
    id: 'k8s-helm-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you implement a Kustomize configuration that handles the migration from PodSecurityPolicies to Pod Security Standards?',
    options: [
      'Replace PSP resources with namespace labels in the overlay',
      'Use components: one that includes PSPs for old clusters and another that applies PSS namespace labels for new clusters',
      'PSPs and PSS are identical in Kustomize',
      'Remove PSPs and rely on defaults',
    ],
    correctAnswer: 1,
    explanation: 'Using Kustomize components, you can create a PSP component (for older clusters) and a PSS component (with namespace labels for newer clusters), including the appropriate one in each environment overlay.',
    codeSnippet: '# components/pss/kustomization.yaml\napiVersion: kustomize.config.k8s.io/v1alpha1\nkind: Component\npatches:\n  - target:\n      kind: Namespace\n    patch: |-\n      apiVersion: v1\n      kind: Namespace\n      metadata:\n        name: placeholder\n        labels:\n          pod-security.kubernetes.io/enforce: restricted\n          pod-security.kubernetes.io/audit: restricted\n          pod-security.kubernetes.io/warn: restricted',
    tags: ['kubernetes', 'kustomize', 'psp', 'pss', 'migration'],
  },
  {
    id: 'k8s-helm-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you implement blue-green deployments at the Helmfile level for a set of related microservices?',
    options: [
      'Helmfile does not support blue-green',
      'Use environment-based release naming (blue/green suffix), dual service definitions, and a traffic switch mechanism managed by a separate release',
      'Use Kubernetes service mesh only',
      'Deploy to different namespaces manually',
    ],
    correctAnswer: 1,
    explanation: 'At the Helmfile level, blue-green can be implemented with parameterized release names (app-blue/app-green), deploying to alternate sets, and switching traffic via a gateway/service release that is updated last.',
    tags: ['kubernetes', 'helmfile', 'blue-green', 'deployment-strategy'],
  },
  {
    id: 'k8s-helm-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the Helm "resource ordering within kind" behavior and when does it matter?',
    options: [
      'Resources of the same kind are always ordered alphabetically',
      'Within the same kind, resources are applied in the order they appear in the rendered manifests; this matters when one ConfigMap is referenced by another resource of the same kind',
      'Ordering within a kind is random',
      'Helm applies all resources of the same kind simultaneously',
    ],
    correctAnswer: 1,
    explanation: 'After Helm sorts by kind priority, resources of the same kind are applied in the order they appear in the concatenated template output. This order can matter for resources with creation-time dependencies on same-kind resources.',
    tags: ['kubernetes', 'helm', 'resource-ordering', 'internals'],
  },
  {
    id: 'k8s-helm-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you implement Helm chart signing with GPG for supply chain security?',
    options: [
      'Helm charts cannot be signed',
      'Use helm package --sign to create a .prov file, then helm verify to validate the signature before installation',
      'Use Docker Content Trust',
      'Sign the Git tag instead',
    ],
    correctAnswer: 1,
    explanation: 'Helm supports GPG signing via helm package --sign, which creates a .prov provenance file. helm verify checks the signature. For OCI, cosign/Notary provide signing capabilities.',
    codeSnippet: '# Sign a chart:\nhelm package --sign --key \'John Doe\' --keyring ~/.gnupg/pubring.gpg my-chart/\n\n# Verify before installing:\nhelm verify my-chart-0.1.0.tgz --keyring ~/.gnupg/pubring.gpg\n\n# Install with verification:\nhelm install my-release my-chart-0.1.0.tgz --verify --keyring ~/.gnupg/pubring.gpg',
    tags: ['kubernetes', 'helm', 'signing', 'security', 'supply-chain'],
  },
  {
    id: 'k8s-helm-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the overhead of deeply nested Kustomize overlay chains and how do you optimize them?',
    options: [
      'Nesting depth has no impact',
      'Each overlay level adds processing time and can accumulate unexpected transformer interactions; flatten where possible and prefer components over deep nesting',
      'Kustomize caches intermediate results',
      'Deep nesting improves reusability without cost',
    ],
    correctAnswer: 1,
    explanation: 'Each overlay level reruns all transformers. Deep nesting (overlay referencing overlay referencing overlay) can accumulate unexpected label/annotation additions and slow build times. Flatter structures with components are more maintainable.',
    tags: ['kubernetes', 'kustomize', 'nesting', 'optimization', 'performance'],
  },
  {
    id: 'k8s-helm-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle Helm chart rendering that produces resources larger than 1MB (Kubernetes object size limit)?',
    options: [
      'Kubernetes has no object size limit',
      'Split the large resource (often a ConfigMap) into multiple smaller resources, or use external storage references instead of inline data',
      'Increase the Kubernetes size limit',
      'Use compression in the template',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes objects (stored in etcd) have a ~1.5MB limit. Large ConfigMaps or Secrets must be split into smaller ones, or external storage (S3, Git) should be referenced instead of inline data.',
    tags: ['kubernetes', 'helm', 'size-limit', 'large-resources'],
  },
  {
    id: 'k8s-helm-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What is the difference between helmfile init and helmfile repos in terms of plugin management?',
    options: [
      'They are identical',
      'helmfile init installs required helm plugins (diff, secrets); helmfile repos only adds/updates chart repositories defined in helmfile.yaml',
      'init creates a new helmfile; repos lists repositories',
      'init is deprecated in favor of repos',
    ],
    correctAnswer: 1,
    explanation: 'helmfile init ensures required Helm plugins (like helm-diff, helm-secrets) are installed. helmfile repos manages chart repository configurations. They serve different purposes in the Helmfile workflow.',
    tags: ['kubernetes', 'helmfile', 'init', 'plugins'],
  },
  {
    id: 'k8s-helm-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle the edge case where a Helm rollback target revision used a now-removed Kubernetes API?',
    options: [
      'Rollback always works regardless of API changes',
      'The rollback fails because the stored manifest contains removed APIs; use helm-mapkubeapis to update the stored manifest before rolling back',
      'Helm automatically converts old APIs',
      'Use kubectl rollback instead',
    ],
    correctAnswer: 1,
    explanation: 'If the target rollback revision contains manifests with removed APIs (e.g., extensions/v1beta1 Ingress), the rollback fails. Use helm-mapkubeapis to update the API versions in stored release data first.',
    codeSnippet: '# This fails if revision 3 uses removed APIs:\nhelm rollback my-release 3\n# Error: unable to build kubernetes objects: no matches for kind "Ingress" in version "extensions/v1beta1"\n\n# Fix with mapkubeapis:\nhelm mapkubeapis my-release\nhelm rollback my-release 3',
    tags: ['kubernetes', 'helm', 'rollback', 'api-deprecation', 'edge-case'],
  },
  {
    id: 'k8s-helm-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you create a Kustomize configuration that supports progressive rollouts using Argo Rollouts?',
    options: [
      'Kustomize is incompatible with Argo Rollouts',
      'Replace Deployment resources with Argo Rollout resources in an overlay, using strategic merge patches to transform the base Deployment spec into a Rollout spec',
      'Use a special Argo Rollouts Kustomize plugin',
      'Only Helm supports Argo Rollouts',
    ],
    correctAnswer: 1,
    explanation: 'An overlay can replace Deployments with Argo Rollouts CRs by removing the Deployment and adding a Rollout resource that reuses the same pod spec, with canary or blue-green strategy configuration.',
    codeSnippet: '# overlay/kustomization.yaml\nresources:\n  - ../../base\n  - rollout.yaml\npatches:\n  - target:\n      kind: Deployment\n      name: my-app\n    patch: |-\n      $patch: delete\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: my-app',
    tags: ['kubernetes', 'kustomize', 'argo-rollouts', 'progressive-delivery'],
  },
  {
    id: 'k8s-helm-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the "template rendering race condition" in Helm and when does it manifest?',
    options: [
      'Templates are always rendered sequentially',
      'Since templates are rendered in a single pass, a template cannot reference the output of another template file; all cross-template data must flow through values or named templates, not computed resource fields',
      'Race conditions only occur with parallel installs',
      'Go templates handle race conditions automatically',
    ],
    correctAnswer: 1,
    explanation: 'Helm renders all templates in a single pass. You cannot reference a computed value from one template file in another (e.g., reading a generated Secret name from a deployment template). All shared data must be computed in named templates or values.',
    tags: ['kubernetes', 'helm', 'templates', 'rendering', 'limitations'],
  },
];

export default questions;
