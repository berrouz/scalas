import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-helm-l2-001',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the correct way to define a named template in Helm?',
    options: [
      '{{ template "my-chart.labels" }}',
      '{{ define "my-chart.labels" }} ... {{ end }}',
      '{{ named "my-chart.labels" }} ... {{ end }}',
      '{{ block "my-chart.labels" }} ... {{ end }}',
    ],
    correctAnswer: 1,
    explanation: 'Named templates are defined using the `define` action, which assigns a name to a block of template code.',
    codeSnippet: '{{- define "my-chart.labels" -}}\napp.kubernetes.io/name: {{ .Chart.Name }}\napp.kubernetes.io/instance: {{ .Release.Name }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'named-templates'],
  },
  {
    id: 'k8s-helm-l2-002',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'named-templates',
    question: 'What is the difference between `template` and `include` in Helm?',
    options: [
      'They are identical in functionality',
      '`template` is faster than `include`',
      '`include` returns a string that can be piped; `template` cannot',
      '`template` works with subcharts; `include` does not',
    ],
    correctAnswer: 2,
    explanation: 'The key difference is that `include` returns a string that can be piped to other functions like `nindent`, while `template` inserts the result directly and cannot be piped.',
    codeSnippet: '# include can be piped:\n{{ include "my-chart.labels" . | nindent 4 }}\n\n# template cannot be piped:\n{{ template "my-chart.labels" . }}',
    tags: ['kubernetes', 'helm', 'named-templates', 'include'],
  },
  {
    id: 'k8s-helm-l2-003',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you iterate over a list in Helm templates?',
    options: [
      '{{ for item in .Values.items }}',
      '{{ each .Values.items }}',
      '{{ range .Values.items }}',
      '{{ loop .Values.items }}',
    ],
    correctAnswer: 2,
    explanation: 'The `range` action in Go templates iterates over lists and maps, setting the dot (.) to the current element.',
    codeSnippet: '# values.yaml:\n# ports:\n#   - 80\n#   - 443\n\nports:\n{{- range .Values.ports }}\n  - containerPort: {{ . }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'range'],
  },
  {
    id: 'k8s-helm-l2-004',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you access the parent scope inside a `range` block?',
    options: [
      'Use .parent',
      'Use $',
      'Use .Root',
      'Use .Global',
    ],
    correctAnswer: 1,
    explanation: 'Inside a range block, the dot (.) changes scope. Use $ to refer to the root scope, which includes .Values, .Release, etc.',
    codeSnippet: '{{- range .Values.ingress.hosts }}\n- host: {{ . }}\n  http:\n    paths:\n      - path: /\n        backend:\n          serviceName: {{ $.Values.service.name }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'scope'],
  },
  {
    id: 'k8s-helm-l2-005',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `with` action do in Helm templates?',
    options: [
      'Imports an external file into the template',
      'Sets the scope (dot) to the specified object, and skips the block if the object is empty',
      'Defines a variable with a specific value',
      'Creates a conditional block that always executes',
    ],
    correctAnswer: 1,
    explanation: 'The `with` action sets the current scope (.) to the specified object. If the object is empty or false, the block is skipped entirely.',
    codeSnippet: '{{- with .Values.nodeSelector }}\nnodeSelector:\n  {{- toYaml . | nindent 2 }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'with'],
  },
  {
    id: 'k8s-helm-l2-006',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'values',
    question: 'How do you set a list value using the --set flag?',
    options: [
      '--set "tags=[a,b,c]"',
      '--set tags={a,b,c}',
      '--set tags[0]=a,tags[1]=b,tags[2]=c',
      '--set tags=a --set tags=b --set tags=c',
    ],
    correctAnswer: 1,
    explanation: 'Lists are specified using curly brace notation with the --set flag.',
    codeSnippet: 'helm install my-release my-chart \\\n  --set "tags={web,frontend,production}"',
    tags: ['kubernetes', 'helm', 'values', 'set'],
  },
  {
    id: 'k8s-helm-l2-007',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'Which annotation marks a Kubernetes resource as a Helm hook?',
    options: [
      'helm.sh/hook-type',
      'helm.sh/hook',
      'hooks.helm.sh/type',
      'kubernetes.io/helm-hook',
    ],
    correctAnswer: 1,
    explanation: 'The annotation `helm.sh/hook` is used to designate a resource as a Helm hook and specify when it should run.',
    codeSnippet: 'apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: {{ .Release.Name }}-db-migrate\n  annotations:\n    "helm.sh/hook": pre-install,pre-upgrade\n    "helm.sh/hook-weight": "0"\n    "helm.sh/hook-delete-policy": hook-succeeded',
    tags: ['kubernetes', 'helm', 'hooks', 'annotations'],
  },
  {
    id: 'k8s-helm-l2-008',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'Which of the following is NOT a valid Helm hook event?',
    options: [
      'pre-install',
      'post-upgrade',
      'pre-deploy',
      'pre-delete',
    ],
    correctAnswer: 2,
    explanation: 'There is no "pre-deploy" hook in Helm. Valid hooks include pre-install, post-install, pre-delete, post-delete, pre-upgrade, post-upgrade, pre-rollback, post-rollback, and test.',
    tags: ['kubernetes', 'helm', 'hooks'],
  },
  {
    id: 'k8s-helm-l2-009',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What does the `helm.sh/hook-weight` annotation control?',
    options: [
      'The CPU resources allocated to the hook pod',
      'The execution order of hooks with the same event',
      'The priority of the hook relative to normal resources',
      'How long the hook runs before timeout',
    ],
    correctAnswer: 1,
    explanation: 'Hook weight determines the execution order among hooks for the same event. Hooks are sorted by weight in ascending order (lower weights run first).',
    codeSnippet: 'annotations:\n  "helm.sh/hook": pre-install\n  "helm.sh/hook-weight": "-5"  # runs before weight "0"',
    tags: ['kubernetes', 'helm', 'hooks', 'weight'],
  },
  {
    id: 'k8s-helm-l2-010',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What does the hook-delete-policy "hook-succeeded" mean?',
    options: [
      'Delete the hook resource when the release is deleted',
      'Delete the hook resource after it completes successfully',
      'Delete the hook only if the next hook succeeds',
      'Never delete the hook resource',
    ],
    correctAnswer: 1,
    explanation: 'The "hook-succeeded" delete policy tells Helm to delete the hook resource after it runs successfully.',
    codeSnippet: 'annotations:\n  "helm.sh/hook": post-install\n  "helm.sh/hook-delete-policy": hook-succeeded',
    tags: ['kubernetes', 'helm', 'hooks', 'delete-policy'],
  },
  {
    id: 'k8s-helm-l2-011',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How do you pass values to a subchart from the parent chart?',
    options: [
      'Using the --subchart flag during install',
      'By nesting values under the subchart name key in values.yaml',
      'By creating a separate values file for each subchart',
      'Subcharts cannot receive values from parent charts',
    ],
    correctAnswer: 1,
    explanation: 'Values for subcharts are passed by nesting them under a key matching the subchart name in the parent values.yaml.',
    codeSnippet: '# parent values.yaml\nmysql:\n  auth:\n    rootPassword: secret\n  primary:\n    persistence:\n      size: 10Gi',
    tags: ['kubernetes', 'helm', 'dependencies', 'values'],
  },
  {
    id: 'k8s-helm-l2-012',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'What does the `condition` field do in a chart dependency?',
    options: [
      'Specifies the Kubernetes version required',
      'Defines a values key that enables or disables the dependency',
      'Sets a condition for chart version compatibility',
      'Determines if the dependency is optional or required',
    ],
    correctAnswer: 1,
    explanation: 'The condition field references a values path (e.g., mysql.enabled) that, when set to false, disables the dependency.',
    codeSnippet: '# Chart.yaml\ndependencies:\n  - name: mysql\n    version: 9.0.0\n    repository: https://charts.bitnami.com/bitnami\n    condition: mysql.enabled',
    tags: ['kubernetes', 'helm', 'dependencies', 'condition'],
  },
  {
    id: 'k8s-helm-l2-013',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'What is the purpose of the `alias` field in a chart dependency?',
    options: [
      'It renames the chart binary',
      'It provides an alternative name for the dependency allowing multiple instances',
      'It creates a shortcut command for helm install',
      'It aliases the repository URL',
    ],
    correctAnswer: 1,
    explanation: 'The alias field allows you to reference the dependency by a different name, enabling multiple instances of the same chart as dependencies.',
    codeSnippet: 'dependencies:\n  - name: mysql\n    version: 9.0.0\n    repository: https://charts.bitnami.com/bitnami\n    alias: primary-db\n  - name: mysql\n    version: 9.0.0\n    repository: https://charts.bitnami.com/bitnami\n    alias: replica-db',
    tags: ['kubernetes', 'helm', 'dependencies', 'alias'],
  },
  {
    id: 'k8s-helm-l2-014',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you conditionally include a resource in a Helm template?',
    options: [
      '{{ when .Values.ingress.enabled }}',
      '{{ if .Values.ingress.enabled }} ... {{ end }}',
      '{{ condition .Values.ingress.enabled }}',
      '{{ check .Values.ingress.enabled }}',
    ],
    correctAnswer: 1,
    explanation: 'The `if` action conditionally includes or excludes blocks of template content based on the truthiness of the expression.',
    codeSnippet: '{{- if .Values.ingress.enabled }}\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: {{ .Release.Name }}-ingress\nspec:\n  rules:\n    {{- range .Values.ingress.hosts }}\n    - host: {{ .host }}\n    {{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'conditionals'],
  },
  {
    id: 'k8s-helm-l2-015',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `required` function do in Helm templates?',
    options: [
      'Marks a Kubernetes field as required in the manifest',
      'Fails template rendering with an error message if the value is empty',
      'Sets a required minimum Helm version',
      'Makes a subchart dependency required',
    ],
    correctAnswer: 1,
    explanation: 'The `required` function causes template rendering to fail with a custom error message if the specified value is empty or missing.',
    codeSnippet: 'image: {{ required "image.repository is required" .Values.image.repository }}',
    tags: ['kubernetes', 'helm', 'templates', 'required'],
  },
  {
    id: 'k8s-helm-l2-016',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you define a local variable in a Helm template?',
    options: [
      '{{ let myVar := "value" }}',
      '{{ var myVar = "value" }}',
      '{{ $myVar := "value" }}',
      '{{ set myVar "value" }}',
    ],
    correctAnswer: 2,
    explanation: 'Variables in Go templates are defined with the $ prefix and := assignment operator.',
    codeSnippet: '{{- $fullName := printf "%s-%s" .Release.Name .Chart.Name -}}\nmetadata:\n  name: {{ $fullName }}',
    tags: ['kubernetes', 'helm', 'templates', 'variables'],
  },
  {
    id: 'k8s-helm-l2-017',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `lookup` function do in Helm templates?',
    options: [
      'Searches for a value in multiple values files',
      'Queries the Kubernetes API to retrieve existing resources',
      'Looks up a chart in the repository index',
      'Finds template files in the chart directory',
    ],
    correctAnswer: 1,
    explanation: 'The `lookup` function queries the Kubernetes API server to get information about existing cluster resources during template rendering.',
    codeSnippet: '{{- $secret := lookup "v1" "Secret" .Release.Namespace "my-secret" -}}\n{{- if $secret }}\n# Secret exists, use it\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'lookup'],
  },
  {
    id: 'k8s-helm-l2-018',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'values',
    question: 'How do you set a string value containing a comma using --set?',
    options: [
      '--set key="value,with,commas"',
      '--set key=value\\,with\\,commas',
      '--set-string key=value,with,commas',
      '--set key=value%2Cwith%2Ccommas',
    ],
    correctAnswer: 1,
    explanation: 'Commas in --set values must be escaped with a backslash because commas normally separate multiple key=value pairs.',
    codeSnippet: 'helm install my-release my-chart \\\n  --set annotation=value\\,with\\,commas',
    tags: ['kubernetes', 'helm', 'values', 'escaping'],
  },
  {
    id: 'k8s-helm-l2-019',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'values',
    question: 'What does --set-string do compared to --set?',
    options: [
      'It validates that the value is a valid string',
      'It forces the value to be treated as a string, not converted to a number or boolean',
      'It sets environment variable strings in containers',
      'It reads the value from a string file',
    ],
    correctAnswer: 1,
    explanation: 'The --set-string flag ensures the value is always treated as a string, preventing automatic type conversion that --set performs.',
    codeSnippet: '# Without --set-string, "true" becomes boolean true\nhelm install r c --set-string enabled=true\n# In values: enabled: "true" (string)',
    tags: ['kubernetes', 'helm', 'values', 'set-string'],
  },
  {
    id: 'k8s-helm-l2-020',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What does --atomic do during helm install or upgrade?',
    options: [
      'Ensures only one helm command runs at a time',
      'Automatically rolls back to the previous state if the operation fails',
      'Makes all resources immutable after deployment',
      'Ensures all resources are created in a single API call',
    ],
    correctAnswer: 1,
    explanation: 'The --atomic flag causes Helm to automatically rollback the release if the install or upgrade fails, ensuring no partial deployments.',
    codeSnippet: 'helm upgrade --install my-release my-chart \\\n  --atomic \\\n  --timeout 10m',
    tags: ['kubernetes', 'helm', 'atomic', 'rollback'],
  },
  {
    id: 'k8s-helm-l2-021',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `quote` function do in Helm templates?',
    options: [
      'Adds YAML block scalar markers',
      'Wraps the value in double quotes',
      'Escapes special YAML characters',
      'Converts the value to a heredoc format',
    ],
    correctAnswer: 1,
    explanation: 'The `quote` function wraps a string value in double quotes, which is important for ensuring YAML values are treated as strings.',
    codeSnippet: 'metadata:\n  annotations:\n    checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum | quote }}',
    tags: ['kubernetes', 'helm', 'templates', 'quote'],
  },
  {
    id: 'k8s-helm-l2-022',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the difference between patchesStrategicMerge and patchesJson6902 in Kustomize?',
    options: [
      'There is no difference; they are aliases',
      'patchesStrategicMerge uses partial YAML; patchesJson6902 uses JSON Patch operations',
      'patchesStrategicMerge works only with Deployments; patchesJson6902 works with all resources',
      'patchesJson6902 is deprecated in favor of patchesStrategicMerge',
    ],
    correctAnswer: 1,
    explanation: 'Strategic merge patches use partial YAML documents that merge with the original. JSON 6902 patches use explicit operations (add, remove, replace) with JSON Pointer paths.',
    codeSnippet: '# Strategic Merge Patch:\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  replicas: 5\n\n# JSON 6902 Patch:\n- op: replace\n  path: /spec/replicas\n  value: 5',
    tags: ['kubernetes', 'kustomize', 'patches'],
  },
  {
    id: 'k8s-helm-l2-023',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you specify the target resource for a JSON 6902 patch in Kustomize?',
    options: [
      'By including the resource metadata in the patch file',
      'Using the target field with group, version, kind, and name',
      'By placing the patch in the same directory as the resource',
      'Using a selector label',
    ],
    correctAnswer: 1,
    explanation: 'JSON 6902 patches require a target specification that identifies the resource by group, version, kind, name, and optionally namespace.',
    codeSnippet: 'patches:\n  - target:\n      group: apps\n      version: v1\n      kind: Deployment\n      name: my-app\n    path: replica-patch.yaml',
    tags: ['kubernetes', 'kustomize', 'patches', 'json6902'],
  },
  {
    id: 'k8s-helm-l2-024',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How does Kustomize handle ConfigMap and Secret name hashing?',
    options: [
      'It does not modify names',
      'It appends a content hash suffix to ensure updates trigger pod restarts',
      'It uses UUIDs for uniqueness',
      'It adds a timestamp suffix',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize appends a hash suffix to generated ConfigMaps and Secrets based on their content, ensuring that changes to data trigger new rollouts.',
    codeSnippet: '# Generated name: app-config-7h2g8m\nconfigMapGenerator:\n  - name: app-config\n    literals:\n      - KEY=value',
    tags: ['kubernetes', 'kustomize', 'configmap', 'hashing'],
  },
  {
    id: 'k8s-helm-l2-025',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you generate a Secret from a file using Kustomize?',
    options: [
      'secretGenerator with files field',
      'secretFactory with source field',
      'secrets with fromFile field',
      'generateSecrets with data field',
    ],
    correctAnswer: 0,
    explanation: 'The secretGenerator in kustomization.yaml creates Secrets from files, literals, or env files, similar to configMapGenerator.',
    codeSnippet: 'secretGenerator:\n  - name: db-credentials\n    files:\n      - password.txt\n    literals:\n      - username=admin\n    type: Opaque',
    tags: ['kubernetes', 'kustomize', 'secrets'],
  },
  {
    id: 'k8s-helm-l2-026',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the correct way to handle multiline strings in Helm templates for ConfigMaps?',
    options: [
      'Use the concat function',
      'Use YAML block scalars with the | or > indicator',
      'Use \\n for newlines',
      'Use the multiline function',
    ],
    correctAnswer: 1,
    explanation: 'YAML block scalar indicators (| for literal, > for folded) are used for multiline strings in ConfigMap data fields.',
    codeSnippet: 'apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-config\ndata:\n  application.properties: |\n    server.port={{ .Values.server.port }}\n    spring.datasource.url={{ .Values.db.url }}',
    tags: ['kubernetes', 'helm', 'templates', 'configmap'],
  },
  {
    id: 'k8s-helm-l2-027',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you include the contents of an external file in a Helm template?',
    options: [
      '{{ readFile "config.json" }}',
      '{{ .Files.Get "config.json" }}',
      '{{ import "config.json" }}',
      '{{ load "config.json" }}',
    ],
    correctAnswer: 1,
    explanation: 'The .Files object provides access to non-template files in the chart. The .Files.Get method reads a file and returns its contents as a string.',
    codeSnippet: 'apiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-files\ndata:\n  config.json: |\n{{ .Files.Get "files/config.json" | indent 4 }}',
    tags: ['kubernetes', 'helm', 'templates', 'files'],
  },
  {
    id: 'k8s-helm-l2-028',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How can you trigger a Deployment rollout when a ConfigMap changes?',
    options: [
      'Add a restart annotation to the ConfigMap',
      'Use a checksum annotation in the Pod template with the ConfigMap content hash',
      'Set the ConfigMap immutable field to false',
      'Add a rolling update strategy to the ConfigMap',
    ],
    correctAnswer: 1,
    explanation: 'Adding a checksum annotation to the Pod template that references the ConfigMap content ensures the Deployment detects changes and triggers a rollout.',
    codeSnippet: 'spec:\n  template:\n    metadata:\n      annotations:\n        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}',
    tags: ['kubernetes', 'helm', 'templates', 'rollout'],
  },
  {
    id: 'k8s-helm-l2-029',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'What does the `tags` field do in a chart dependency?',
    options: [
      'Adds Kubernetes labels to the dependency resources',
      'Tags the chart version in the repository',
      'Groups dependencies so they can be enabled/disabled together by tag name',
      'Adds metadata tags for chart search',
    ],
    correctAnswer: 2,
    explanation: 'Tags allow grouping dependencies so they can be collectively enabled or disabled via corresponding boolean values in the parent values.',
    codeSnippet: '# Chart.yaml\ndependencies:\n  - name: mysql\n    version: 9.0.0\n    repository: https://charts.bitnami.com/bitnami\n    tags:\n      - database\n\n# values.yaml\ntags:\n  database: true',
    tags: ['kubernetes', 'helm', 'dependencies', 'tags'],
  },
  {
    id: 'k8s-helm-l2-030',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How do you share values between a parent chart and all subcharts?',
    options: [
      'Use the shared: key in values.yaml',
      'Use the global: key in values.yaml',
      'Use environment variables',
      'Duplicate the values in each subchart section',
    ],
    correctAnswer: 1,
    explanation: 'Values under the `global` key in values.yaml are available to all subcharts via .Values.global.',
    codeSnippet: '# parent values.yaml\nglobal:\n  imageRegistry: myregistry.io\n  storageClass: fast-ssd\n\n# accessible in subchart as:\n# {{ .Values.global.imageRegistry }}',
    tags: ['kubernetes', 'helm', 'dependencies', 'global'],
  },
  {
    id: 'k8s-helm-l2-031',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What does the `--force` flag do during helm upgrade?',
    options: [
      'Skips all validation checks',
      'Forces resource updates through delete and recreate instead of patching',
      'Overrides namespace restrictions',
      'Forces the chart to be pulled from remote even if cached',
    ],
    correctAnswer: 1,
    explanation: 'The --force flag forces resource updates through delete/recreate rather than patching, which can resolve conflicts with immutable fields.',
    tags: ['kubernetes', 'helm', 'upgrade', 'force'],
  },
  {
    id: 'k8s-helm-l2-032',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What does `--cleanup-on-fail` do during helm upgrade?',
    options: [
      'Removes the entire release on failure',
      'Deletes newly created resources on a failed upgrade',
      'Cleans up old revision history',
      'Removes temporary files from the upgrade process',
    ],
    correctAnswer: 1,
    explanation: 'The --cleanup-on-fail flag removes any new resources created during a failed upgrade attempt, preventing orphaned resources.',
    codeSnippet: 'helm upgrade my-release my-chart \\\n  --cleanup-on-fail \\\n  --timeout 5m',
    tags: ['kubernetes', 'helm', 'upgrade', 'cleanup'],
  },
  {
    id: 'k8s-helm-l2-033',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the purpose of the `fail` function in Helm templates?',
    options: [
      'Marks a test as failed',
      'Immediately stops template rendering with a custom error message',
      'Logs a warning but continues rendering',
      'Triggers a rollback',
    ],
    correctAnswer: 1,
    explanation: 'The `fail` function unconditionally stops template rendering and outputs the specified error message, useful for validation.',
    codeSnippet: '{{- if not (or (eq .Values.env "dev") (eq .Values.env "prod")) }}\n  {{ fail "env must be either dev or prod" }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'fail'],
  },
  {
    id: 'k8s-helm-l2-034',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'named-templates',
    question: 'What is the recommended naming convention for named templates?',
    options: [
      'template_name',
      'chart-name.template-name',
      'Template.Name',
      'Any name without dots',
    ],
    correctAnswer: 1,
    explanation: 'The convention is to prefix named templates with the chart name followed by a dot to avoid naming collisions with subcharts.',
    codeSnippet: '{{- define "my-chart.fullname" -}}\n{{- printf "%s-%s" .Release.Name .Chart.Name | trunc 63 | trimSuffix "-" }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'named-templates', 'conventions'],
  },
  {
    id: 'k8s-helm-l2-035',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'named-templates',
    question: 'Why are named template names global across a chart and its subcharts?',
    options: [
      'They are stored in a shared ConfigMap',
      'Go templates share a single namespace, so all defined templates are accessible from any template file',
      'Helm merges all templates into one file before rendering',
      'Kubernetes requires globally unique template names',
    ],
    correctAnswer: 1,
    explanation: 'Go template definitions exist in a flat global namespace. When charts and subcharts are compiled together, all named templates are accessible, which is why the chart name prefix convention prevents collisions.',
    tags: ['kubernetes', 'helm', 'named-templates', 'scope'],
  },
  {
    id: 'k8s-helm-l2-036',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you iterate over a map (dictionary) in Helm templates?',
    options: [
      '{{ range $key, $value := .Values.labels }}',
      '{{ for key, value in .Values.labels }}',
      '{{ each $key $value .Values.labels }}',
      '{{ map .Values.labels as $key $value }}',
    ],
    correctAnswer: 0,
    explanation: 'Using range with two variables iterates over a map, assigning each key-value pair to the specified variables.',
    codeSnippet: '{{- range $key, $value := .Values.podAnnotations }}\n{{ $key }}: {{ $value | quote }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'range', 'maps'],
  },
  {
    id: 'k8s-helm-l2-037',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What Sprig function generates a random password in Helm templates?',
    options: [
      'password',
      'randAlphaNum',
      'generatePassword',
      'randomString',
    ],
    correctAnswer: 1,
    explanation: 'The `randAlphaNum` function from the Sprig library generates a random alphanumeric string of the specified length.',
    codeSnippet: 'apiVersion: v1\nkind: Secret\nmetadata:\n  name: {{ .Release.Name }}-secret\ndata:\n  password: {{ randAlphaNum 16 | b64enc | quote }}',
    tags: ['kubernetes', 'helm', 'templates', 'sprig'],
  },
  {
    id: 'k8s-helm-l2-038',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you add an element to an array using a JSON 6902 patch in Kustomize?',
    options: [
      'Using op: append',
      'Using op: add with path ending in /-',
      'Using op: insert',
      'Using op: push',
    ],
    correctAnswer: 1,
    explanation: 'The JSON Patch "add" operation with a path ending in /- appends a new element to the end of an array.',
    codeSnippet: '- op: add\n  path: /spec/template/spec/containers/0/env/-\n  value:\n    name: NEW_VAR\n    value: "hello"',
    tags: ['kubernetes', 'kustomize', 'patches', 'json6902'],
  },
  {
    id: 'k8s-helm-l2-039',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What does the `resources` field in kustomization.yaml specify?',
    options: [
      'CPU and memory limits for pods',
      'List of Kubernetes resource files or directories to include',
      'Remote URLs for downloading charts',
      'Storage resources to provision',
    ],
    correctAnswer: 1,
    explanation: 'The resources field lists the Kubernetes manifest files, directories, or remote URLs that Kustomize should include and process.',
    codeSnippet: 'apiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\nresources:\n  - deployment.yaml\n  - service.yaml\n  - ../base',
    tags: ['kubernetes', 'kustomize', 'resources'],
  },
  {
    id: 'k8s-helm-l2-040',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How does Helmfile handle environment-specific values?',
    options: [
      'Using Git branches for each environment',
      'Using the environments section and per-environment values files',
      'Using Kubernetes namespaces only',
      'Using Docker environment variables',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports an environments section where you can define per-environment values files and settings.',
    codeSnippet: 'environments:\n  production:\n    values:\n      - production.yaml\n  staging:\n    values:\n      - staging.yaml\n\nreleases:\n  - name: my-app\n    chart: ./charts/my-app\n    values:\n      - values/{{ .Environment.Name }}.yaml',
    tags: ['kubernetes', 'helmfile', 'environments'],
  },
  {
    id: 'k8s-helm-l2-041',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What command applies all releases defined in a Helmfile?',
    options: [
      'helmfile install',
      'helmfile deploy',
      'helmfile apply',
      'helmfile sync',
    ],
    correctAnswer: 2,
    explanation: 'The `helmfile apply` command shows a diff and applies changes. It combines diff and sync to only update releases that have changed.',
    codeSnippet: 'helmfile -e production apply',
    tags: ['kubernetes', 'helmfile', 'apply'],
  },
  {
    id: 'k8s-helm-l2-042',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do you install a chart from an OCI registry?',
    options: [
      'helm install my-release oci://registry.example.com/charts/my-chart --version 1.0.0',
      'helm install my-release registry.example.com/charts/my-chart:1.0.0',
      'helm oci install registry.example.com/charts/my-chart',
      'helm pull --install oci://registry.example.com/charts/my-chart',
    ],
    correctAnswer: 0,
    explanation: 'Charts from OCI registries are installed using the oci:// protocol prefix with the registry path.',
    codeSnippet: 'helm install my-release \\\n  oci://registry.example.com/charts/my-chart \\\n  --version 1.0.0',
    tags: ['kubernetes', 'helm', 'oci', 'install'],
  },
  {
    id: 'k8s-helm-l2-043',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do you authenticate to an OCI registry for Helm charts?',
    options: [
      'helm repo login',
      'helm registry login',
      'helm auth login',
      'helm oci authenticate',
    ],
    correctAnswer: 1,
    explanation: 'The `helm registry login` command authenticates with an OCI-compliant registry using username and password or token.',
    codeSnippet: 'helm registry login registry.example.com \\\n  --username myuser \\\n  --password mypass',
    tags: ['kubernetes', 'helm', 'oci', 'authentication'],
  },
  {
    id: 'k8s-helm-l2-044',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'chart-structure',
    question: 'What is the purpose of the crds/ directory in a Helm chart?',
    options: [
      'Stores credential files for registry access',
      'Contains CRD manifests that are installed before other chart resources',
      'Holds chart rendering definitions',
      'Stores custom resource documentation',
    ],
    correctAnswer: 1,
    explanation: 'The crds/ directory contains Custom Resource Definition manifests that Helm installs before any other chart resources on first install.',
    codeSnippet: 'my-chart/\n  crds/\n    my-crd.yaml\n  templates/\n    my-custom-resource.yaml',
    tags: ['kubernetes', 'helm', 'crds'],
  },
  {
    id: 'k8s-helm-l2-045',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'chart-structure',
    question: 'How does Helm handle CRDs in the crds/ directory during upgrades?',
    options: [
      'CRDs are upgraded automatically with each helm upgrade',
      'CRDs are only installed on first install and never updated or deleted',
      'CRDs are deleted and recreated on each upgrade',
      'CRDs are patched using strategic merge',
    ],
    correctAnswer: 1,
    explanation: 'Helm only installs CRDs on the first install. CRDs in the crds/ directory are never updated or deleted by Helm to prevent accidental data loss.',
    tags: ['kubernetes', 'helm', 'crds', 'upgrade'],
  },
  {
    id: 'k8s-helm-l2-046',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you use a conditional with multiple branches in Helm templates?',
    options: [
      '{{ if }} {{ elseif }} {{ else }} {{ end }}',
      '{{ if }} {{ else if }} {{ else }} {{ end }}',
      '{{ switch }} {{ case }} {{ default }} {{ end }}',
      '{{ cond }} {{ when }} {{ otherwise }} {{ end }}',
    ],
    correctAnswer: 1,
    explanation: 'Helm uses Go template syntax with `if`, `else if`, and `else` for multi-branch conditionals.',
    codeSnippet: '{{- if eq .Values.env "production" }}\nreplicas: 3\n{{- else if eq .Values.env "staging" }}\nreplicas: 2\n{{- else }}\nreplicas: 1\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'conditionals'],
  },
  {
    id: 'k8s-helm-l2-047',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `b64enc` function do in Helm templates?',
    options: [
      'Decodes a base64 string',
      'Encodes a string to base64',
      'Encrypts a string using AES',
      'Hashes a string using SHA256',
    ],
    correctAnswer: 1,
    explanation: 'The `b64enc` function base64-encodes a string, commonly used when creating Secret resources that require base64-encoded data.',
    codeSnippet: 'apiVersion: v1\nkind: Secret\nmetadata:\n  name: {{ .Release.Name }}-secret\ntype: Opaque\ndata:\n  password: {{ .Values.password | b64enc | quote }}',
    tags: ['kubernetes', 'helm', 'templates', 'base64'],
  },
  {
    id: 'k8s-helm-l2-048',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the difference between `.Files.Get` and `.Files.GetBytes` in Helm?',
    options: [
      'Get returns JSON; GetBytes returns YAML',
      'Get returns a string; GetBytes returns a byte array',
      'Get reads from local disk; GetBytes reads from remote URLs',
      'They are identical',
    ],
    correctAnswer: 1,
    explanation: '.Files.Get returns the file contents as a string, while .Files.GetBytes returns raw bytes, useful for binary data.',
    tags: ['kubernetes', 'helm', 'templates', 'files'],
  },
  {
    id: 'k8s-helm-l2-049',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What does --max-history control in Helm?',
    options: [
      'Maximum number of charts cached locally',
      'Maximum number of release revisions retained',
      'Maximum number of concurrent deployments',
      'Maximum number of rollback attempts',
    ],
    correctAnswer: 1,
    explanation: 'The --max-history flag limits the number of revisions saved per release. Older revisions beyond this limit are garbage collected.',
    codeSnippet: 'helm upgrade my-release my-chart --max-history 10',
    tags: ['kubernetes', 'helm', 'release', 'history'],
  },
  {
    id: 'k8s-helm-l2-050',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'What is a strategic merge patch in Kustomize?',
    options: [
      'A patch that completely replaces the original resource',
      'A partial YAML document that is merged with the original using Kubernetes merge strategies',
      'A patch applied only during specific deployment strategies',
      'A patch that merges multiple resources into one',
    ],
    correctAnswer: 1,
    explanation: 'A strategic merge patch is a partial YAML document merged with the original resource. It uses Kubernetes-aware merge strategies that handle arrays differently based on the field type.',
    codeSnippet: '# patch-replicas.yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  replicas: 5',
    tags: ['kubernetes', 'kustomize', 'patches', 'strategic-merge'],
  },
  {
    id: 'k8s-helm-l2-051',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you add annotations to all resources using Kustomize?',
    options: [
      'Using the annotations field',
      'Using the commonAnnotations field',
      'Using a patch for each resource',
      'Using the metadata.annotations field',
    ],
    correctAnswer: 1,
    explanation: 'The commonAnnotations field in kustomization.yaml adds the specified annotations to all resources.',
    codeSnippet: 'apiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\ncommonAnnotations:\n  team: backend\n  managed-by: kustomize',
    tags: ['kubernetes', 'kustomize', 'annotations'],
  },
  {
    id: 'k8s-helm-l2-052',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'values',
    question: 'What happens when you use --set-file in Helm?',
    options: [
      'It sets a values file as the main values source',
      'It sets a value to the contents of the specified file',
      'It creates a file in the chart from the value',
      'It sets a file path as a volume mount',
    ],
    correctAnswer: 1,
    explanation: 'The --set-file flag reads the contents of a file and uses it as the value for the specified key.',
    codeSnippet: 'helm install my-release my-chart \\\n  --set-file sslCert=./server.crt',
    tags: ['kubernetes', 'helm', 'values', 'set-file'],
  },
  {
    id: 'k8s-helm-l2-053',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `trunc` function do in Helm?',
    options: [
      'Truncates a list to the specified number of elements',
      'Truncates a string to the specified number of characters',
      'Removes trailing whitespace',
      'Rounds a number down to an integer',
    ],
    correctAnswer: 1,
    explanation: 'The `trunc` function truncates a string to the specified length, commonly used to ensure Kubernetes name length limits (63 characters) are not exceeded.',
    codeSnippet: '{{- define "my-chart.fullname" -}}\n{{- printf "%s-%s" .Release.Name .Chart.Name | trunc 63 | trimSuffix "-" }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'trunc'],
  },
  {
    id: 'k8s-helm-l2-054',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'Which resource types are commonly used as Helm hooks?',
    options: [
      'Deployments and StatefulSets',
      'Services and Ingresses',
      'Jobs and Pods',
      'ConfigMaps and Secrets',
    ],
    correctAnswer: 2,
    explanation: 'Jobs are the most commonly used hook resources because they run to completion. Pods can also be used but Jobs provide better retry and cleanup semantics.',
    tags: ['kubernetes', 'helm', 'hooks', 'jobs'],
  },
  {
    id: 'k8s-helm-l2-055',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What are the valid values for helm.sh/hook-delete-policy?',
    options: [
      'always, never, on-success',
      'hook-succeeded, hook-failed, before-hook-creation',
      'delete-on-success, delete-on-fail, delete-always',
      'auto, manual, timed',
    ],
    correctAnswer: 1,
    explanation: 'The three valid delete policies are: hook-succeeded (delete after success), hook-failed (delete after failure), and before-hook-creation (delete previous hook before creating new one).',
    codeSnippet: 'annotations:\n  "helm.sh/hook": pre-upgrade\n  "helm.sh/hook-delete-policy": before-hook-creation',
    tags: ['kubernetes', 'helm', 'hooks', 'delete-policy'],
  },
  {
    id: 'k8s-helm-l2-056',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you keep the old release history when uninstalling a Helm release?',
    options: [
      'helm uninstall my-release --preserve',
      'helm uninstall my-release --keep-history',
      'helm uninstall my-release --no-delete-history',
      'helm uninstall my-release --retain',
    ],
    correctAnswer: 1,
    explanation: 'The --keep-history flag preserves the release history after uninstall, allowing `helm rollback` to restore the release later.',
    codeSnippet: 'helm uninstall my-release --keep-history\n# Later, you can rollback:\nhelm rollback my-release 1',
    tags: ['kubernetes', 'helm', 'uninstall', 'history'],
  },
  {
    id: 'k8s-helm-l2-057',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle optional values that may not exist in the values hierarchy?',
    options: [
      'Use try/catch blocks',
      'Use the `hasKey` function or the `dig` function for deep nested access',
      'Use the `optional` keyword',
      'All values must exist or the template fails',
    ],
    correctAnswer: 1,
    explanation: 'The `hasKey` function checks if a key exists in a map, and `dig` safely navigates deeply nested values with a default fallback.',
    codeSnippet: '{{- if hasKey .Values "monitoring" }}\n  {{- if .Values.monitoring.enabled }}\n# enable monitoring\n  {{- end }}\n{{- end }}\n\n# Or using dig:\nmonitoring: {{ dig "monitoring" "enabled" false .Values.AsMap }}',
    tags: ['kubernetes', 'helm', 'templates', 'hasKey'],
  },
  {
    id: 'k8s-helm-l2-058',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize with a remote base?',
    options: [
      'Download the base manually first',
      'Reference the Git repository URL in the resources field',
      'Use the remote field in kustomization.yaml',
      'Import it using helm dependency',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize supports referencing remote bases via Git URLs in the resources field.',
    codeSnippet: 'apiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\nresources:\n  - github.com/org/repo/base?ref=v1.0.0',
    tags: ['kubernetes', 'kustomize', 'remote'],
  },
  {
    id: 'k8s-helm-l2-059',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you remove a field from a resource using a strategic merge patch in Kustomize?',
    options: [
      'Set the field to null',
      'Use the $patch: delete directive',
      'Set the field to an empty string',
      'Use op: remove in the patch',
    ],
    correctAnswer: 1,
    explanation: 'In strategic merge patches, the special $patch: delete directive removes entries from lists, while setting a field to null removes scalar fields.',
    codeSnippet: '# Remove a specific container:\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  template:\n    spec:\n      containers:\n        - name: sidecar\n          $patch: delete',
    tags: ['kubernetes', 'kustomize', 'patches', 'delete'],
  },
  {
    id: 'k8s-helm-l2-060',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What does `helmfile diff` do?',
    options: [
      'Shows the difference between two helmfile.yaml versions',
      'Shows the diff between the current cluster state and the desired state',
      'Compares two different Helm charts',
      'Shows the difference between two namespaces',
    ],
    correctAnswer: 1,
    explanation: 'The `helmfile diff` command uses the helm-diff plugin to show changes between the currently deployed releases and what would be deployed.',
    codeSnippet: 'helmfile -e production diff',
    tags: ['kubernetes', 'helmfile', 'diff'],
  },
  {
    id: 'k8s-helm-l2-061',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How does Helmfile handle release ordering?',
    options: [
      'Releases are always deployed alphabetically',
      'Releases are deployed in the order listed, and needs/dependencies can specify explicit ordering',
      'All releases are deployed simultaneously',
      'Ordering is random and cannot be controlled',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile deploys releases in the order they appear in the file. The `needs` field can specify explicit dependency ordering between releases.',
    codeSnippet: 'releases:\n  - name: database\n    chart: bitnami/postgresql\n  - name: backend\n    chart: ./charts/backend\n    needs:\n      - database',
    tags: ['kubernetes', 'helmfile', 'ordering'],
  },
  {
    id: 'k8s-helm-l2-062',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the purpose of the `sha256sum` function in Helm?',
    options: [
      'Verifies chart integrity',
      'Generates a SHA256 hash of a string, useful for change detection annotations',
      'Encrypts sensitive values',
      'Creates a unique identifier for releases',
    ],
    correctAnswer: 1,
    explanation: 'The sha256sum function generates a SHA256 hash of the input string, commonly used in annotations to detect ConfigMap/Secret changes.',
    codeSnippet: 'annotations:\n  checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}',
    tags: ['kubernetes', 'helm', 'templates', 'sha256'],
  },
  {
    id: 'k8s-helm-l2-063',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'What does `helm dependency build` do differently from `helm dependency update`?',
    options: [
      'They are identical commands',
      '`build` uses Chart.lock for exact versions; `update` regenerates Chart.lock from Chart.yaml',
      '`build` compiles templates; `update` downloads charts',
      '`build` creates the charts/ directory; `update` only adds new charts',
    ],
    correctAnswer: 1,
    explanation: '`helm dep build` downloads dependencies based on the exact versions in Chart.lock. `helm dep update` resolves versions from Chart.yaml and regenerates Chart.lock.',
    tags: ['kubernetes', 'helm', 'dependencies', 'build'],
  },
  {
    id: 'k8s-helm-l2-064',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'values',
    question: 'How do you set a JSON value using the --set-json flag?',
    options: [
      '--set-json allows setting complex values using JSON syntax',
      '--set-json reads a JSON file path',
      '--set-json converts YAML values to JSON',
      '--set-json only works with string values',
    ],
    correctAnswer: 0,
    explanation: 'The --set-json flag allows setting values using JSON syntax, which is easier for complex structures like arrays of objects.',
    codeSnippet: 'helm install my-release my-chart \\\n  --set-json \'ingress.hosts=[{"host":"example.com","paths":[{"path":"/"}]}]\'',
    tags: ['kubernetes', 'helm', 'values', 'set-json'],
  },
  {
    id: 'k8s-helm-l2-065',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the state of a release when `helm install` fails midway?',
    options: [
      'The release does not exist',
      'The release is in "failed" state',
      'The release is in "pending-install" state',
      'The release is automatically rolled back',
    ],
    correctAnswer: 1,
    explanation: 'When a helm install fails, the release is left in a "failed" state. You can then upgrade it or uninstall it.',
    tags: ['kubernetes', 'helm', 'release', 'failed'],
  },
  {
    id: 'k8s-helm-l2-066',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you use the ternary operator pattern in Helm templates?',
    options: [
      '{{ .Values.enabled ? "yes" : "no" }}',
      '{{ ternary "yes" "no" .Values.enabled }}',
      '{{ if .Values.enabled then "yes" else "no" }}',
      '{{ cond .Values.enabled "yes" "no" }}',
    ],
    correctAnswer: 1,
    explanation: 'The `ternary` function from the Sprig library provides ternary operator functionality: ternary trueValue falseValue condition.',
    codeSnippet: 'readOnlyRootFilesystem: {{ ternary true false .Values.security.readOnlyRoot }}',
    tags: ['kubernetes', 'helm', 'templates', 'ternary'],
  },
  {
    id: 'k8s-helm-l2-067',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you create a helper that generates standard Kubernetes labels?',
    options: [
      'Use a pre-built Kubernetes label function',
      'Define a named template that outputs label key-value pairs',
      'Import a labels library from Helm Hub',
      'Use the .Labels built-in object',
    ],
    correctAnswer: 1,
    explanation: 'Standard Kubernetes labels are typically generated using a named template in _helpers.tpl that outputs the recommended label set.',
    codeSnippet: '{{- define "my-chart.labels" -}}\nhelm.sh/chart: {{ include "my-chart.chart" . }}\n{{ include "my-chart.selectorLabels" . }}\napp.kubernetes.io/managed-by: {{ .Release.Service }}\n{{- end }}\n\n{{- define "my-chart.selectorLabels" -}}\napp.kubernetes.io/name: {{ include "my-chart.name" . }}\napp.kubernetes.io/instance: {{ .Release.Name }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'labels'],
  },
  {
    id: 'k8s-helm-l2-068',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How does Kustomize handle resource ordering when applying?',
    options: [
      'Resources are applied in alphabetical order',
      'Resources are applied in the order listed in kustomization.yaml',
      'Kustomize applies resources in a Kubernetes-aware order (namespaces before deployments, etc.)',
      'Resources are applied randomly',
    ],
    correctAnswer: 2,
    explanation: 'When using kubectl apply -k, resources are sorted based on Kubernetes resource type ordering, ensuring dependencies like Namespaces and CRDs are created first.',
    tags: ['kubernetes', 'kustomize', 'ordering'],
  },
  {
    id: 'k8s-helm-l2-069',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you apply a patch to multiple resources in Kustomize?',
    options: [
      'List each resource separately with the same patch',
      'Use a label selector or annotation selector in the target field',
      'Kustomize patches can only target one resource',
      'Use wildcard * in the resource name',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize patches can target multiple resources using label selectors or annotation selectors, applying the same patch to all matching resources.',
    codeSnippet: 'patches:\n  - target:\n      kind: Deployment\n      labelSelector: "app=my-app"\n    patch: |-\n      - op: add\n        path: /metadata/annotations/updated\n        value: "true"',
    tags: ['kubernetes', 'kustomize', 'patches', 'selectors'],
  },
  {
    id: 'k8s-helm-l2-070',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the `print` function family (print, printf, println) used for in Helm?',
    options: [
      'Outputting debug logs to the console',
      'Formatting strings similar to Go fmt functions',
      'Printing chart information',
      'Generating documentation',
    ],
    correctAnswer: 1,
    explanation: 'The printf function formats strings using Go-style format verbs (like %s, %d), similar to fmt.Sprintf in Go.',
    codeSnippet: '{{- $fullName := printf "%s-%s" .Release.Name "api" -}}\nname: {{ $fullName }}',
    tags: ['kubernetes', 'helm', 'templates', 'printf'],
  },
  {
    id: 'k8s-helm-l2-071',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'repositories',
    question: 'How do you remove a Helm chart repository?',
    options: [
      'helm repo delete stable',
      'helm repo remove stable',
      'helm repo rm stable',
      'Both helm repo remove and helm repo rm work',
    ],
    correctAnswer: 1,
    explanation: 'The `helm repo remove` command removes a previously added chart repository from the local configuration.',
    codeSnippet: 'helm repo remove bitnami',
    tags: ['kubernetes', 'helm', 'repository'],
  },
  {
    id: 'k8s-helm-l2-072',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How can you embed binary files (like certificates) in a Helm ConfigMap or Secret?',
    options: [
      'Use the .Files.Glob and .Files.AsSecrets functions',
      'Binary files cannot be embedded in Helm charts',
      'Convert them to JSON first',
      'Use base64 encoding manually',
    ],
    correctAnswer: 0,
    explanation: 'The .Files.Glob method finds files by pattern and .Files.AsSecrets returns them base64-encoded, ready for use in Secrets.',
    codeSnippet: 'apiVersion: v1\nkind: Secret\nmetadata:\n  name: {{ .Release.Name }}-certs\ntype: Opaque\ndata:\n{{ (.Files.Glob "certs/*").AsSecrets | indent 2 }}',
    tags: ['kubernetes', 'helm', 'templates', 'files', 'secrets'],
  },
  {
    id: 'k8s-helm-l2-073',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What does the `--reset-values` flag do during helm upgrade?',
    options: [
      'Resets all Kubernetes resources to their initial state',
      'Uses only the default values from the chart, ignoring previously set values',
      'Clears the release history',
      'Resets the chart version to the initial release',
    ],
    correctAnswer: 1,
    explanation: 'The --reset-values flag tells Helm to use only the chart default values during upgrade, discarding any values from previous releases.',
    tags: ['kubernetes', 'helm', 'upgrade', 'reset-values'],
  },
  {
    id: 'k8s-helm-l2-074',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the difference between --reuse-values and --reset-values in helm upgrade?',
    options: [
      'They are identical',
      '--reuse-values merges old values with new; --reset-values uses only chart defaults',
      '--reuse-values is for v2; --reset-values is for v3',
      '--reset-values resets the release name; --reuse-values keeps it',
    ],
    correctAnswer: 1,
    explanation: '--reuse-values uses values from the previous release and merges any newly specified values. --reset-values discards previous values and starts from chart defaults.',
    tags: ['kubernetes', 'helm', 'upgrade', 'values'],
  },
  {
    id: 'k8s-helm-l2-075',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the purpose of the `components` feature in Kustomize?',
    options: [
      'It replaces the concept of overlays',
      'It allows reusable groups of patches and resources that can be included in multiple overlays',
      'It defines individual container components',
      'It creates Kubernetes custom resources',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize components are reusable groups of Kustomize configuration (patches, resources, etc.) that can be included in different overlays to avoid duplication.',
    codeSnippet: '# components/monitoring/kustomization.yaml\napiVersion: kustomize.config.k8s.io/v1alpha1\nkind: Component\npatches:\n  - patch-with-sidecar.yaml\n\n# overlays/prod/kustomization.yaml\ncomponents:\n  - ../../components/monitoring',
    tags: ['kubernetes', 'kustomize', 'components'],
  },
  {
    id: 'k8s-helm-l2-076',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `indent` function do differently from `nindent`?',
    options: [
      '`indent` adds a newline; `nindent` does not',
      '`indent` only adds spaces; `nindent` prepends a newline before indenting',
      'They are identical',
      '`indent` uses tabs; `nindent` uses spaces',
    ],
    correctAnswer: 1,
    explanation: '`indent` adds the specified number of spaces to the beginning of each line. `nindent` does the same but also prepends a newline character first.',
    codeSnippet: '# indent - no leading newline:\ndata: {{ toYaml .Values.data | indent 4 }}\n\n# nindent - adds leading newline:\ndata:\n  {{- toYaml .Values.data | nindent 4 }}',
    tags: ['kubernetes', 'helm', 'templates', 'indent'],
  },
  {
    id: 'k8s-helm-l2-077',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you selectively deploy specific releases in Helmfile?',
    options: [
      'Comment out unwanted releases',
      'Use the --selector or -l flag with label matching',
      'Pass release names as positional arguments',
      'Use the --only flag',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports label-based selection with -l to deploy only releases matching specific labels.',
    codeSnippet: '# helmfile.yaml\nreleases:\n  - name: frontend\n    labels:\n      tier: frontend\n    chart: ./charts/frontend\n  - name: backend\n    labels:\n      tier: backend\n    chart: ./charts/backend\n\n# Deploy only backend:\nhelmfile -l tier=backend apply',
    tags: ['kubernetes', 'helmfile', 'selectors'],
  },
  {
    id: 'k8s-helm-l2-078',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'chart-structure',
    question: 'Where should you put test templates in a Helm chart?',
    options: [
      'In a tests/ directory at the chart root',
      'In the templates/tests/ directory with test hook annotations',
      'In a separate test chart',
      'Tests are defined in values.yaml',
    ],
    correctAnswer: 1,
    explanation: 'Test templates are placed in templates/tests/ and annotated with helm.sh/hook: test. They run when `helm test` is executed.',
    codeSnippet: '# templates/tests/test-connection.yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: "{{ .Release.Name }}-test"\n  annotations:\n    "helm.sh/hook": test\nspec:\n  containers:\n    - name: wget\n      image: busybox\n      command: [\'wget\']\n      args: [\'{{ .Release.Name }}:{{ .Values.service.port }}\']',
    tags: ['kubernetes', 'helm', 'testing'],
  },
  {
    id: 'k8s-helm-l2-079',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How do you import specific values from a subchart into the parent chart?',
    options: [
      'Use the import-values field in the dependency declaration',
      'Use shared variables',
      'Values are always fully isolated between charts',
      'Use environment variables',
    ],
    correctAnswer: 0,
    explanation: 'The import-values field in the dependency declaration allows importing specific values from the subchart into the parent chart scope.',
    codeSnippet: 'dependencies:\n  - name: mysql\n    version: 9.0.0\n    repository: https://charts.bitnami.com/bitnami\n    import-values:\n      - child: primary.service\n        parent: database.service',
    tags: ['kubernetes', 'helm', 'dependencies', 'import-values'],
  },
  {
    id: 'k8s-helm-l2-080',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do you pull a chart from an OCI registry without installing it?',
    options: [
      'helm fetch oci://registry.example.com/charts/my-chart',
      'helm pull oci://registry.example.com/charts/my-chart --version 1.0.0',
      'helm download oci://registry.example.com/charts/my-chart',
      'helm get oci://registry.example.com/charts/my-chart',
    ],
    correctAnswer: 1,
    explanation: 'The `helm pull` command with oci:// prefix downloads the chart archive from an OCI registry without installing it.',
    codeSnippet: 'helm pull oci://registry.example.com/charts/my-chart \\\n  --version 1.0.0 \\\n  --untar',
    tags: ['kubernetes', 'helm', 'oci', 'pull'],
  },
  {
    id: 'k8s-helm-l2-081',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `regexMatch` function do in Helm?',
    options: [
      'Extracts groups from a regex match',
      'Returns true if the input string matches the regular expression',
      'Replaces regex matches with a new string',
      'Validates that a regex pattern is correct',
    ],
    correctAnswer: 1,
    explanation: 'The `regexMatch` function tests if a string matches a regular expression pattern, returning a boolean.',
    codeSnippet: '{{- if not (regexMatch "^[a-z][a-z0-9-]*$" .Values.appName) }}\n  {{ fail "appName must be lowercase alphanumeric with hyphens" }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'regex'],
  },
  {
    id: 'k8s-helm-l2-082',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the `replacements` transformer in Kustomize?',
    options: [
      'A way to find and replace strings in all files',
      'A field-level value propagation mechanism that copies values between resources',
      'A tool to replace entire resources',
      'A deprecated feature replaced by patches',
    ],
    correctAnswer: 1,
    explanation: 'The replacements transformer copies values from one resource field to another, enabling cross-resource value propagation without patches.',
    codeSnippet: 'replacements:\n  - source:\n      kind: Service\n      name: my-svc\n      fieldPath: metadata.name\n    targets:\n      - select:\n          kind: Deployment\n        fieldPaths:\n          - spec.template.spec.containers.0.env.0.value',
    tags: ['kubernetes', 'kustomize', 'replacements'],
  },
  {
    id: 'k8s-helm-l2-083',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you disable the hash suffix on generated ConfigMaps in Kustomize?',
    options: [
      'Set hashSuffix: false on the generator',
      'Set generatorOptions.disableNameSuffixHash to true',
      'Use the --no-hash flag with kubectl apply -k',
      'Hash suffixes cannot be disabled',
    ],
    correctAnswer: 1,
    explanation: 'Setting disableNameSuffixHash in generatorOptions prevents Kustomize from appending content hashes to generated ConfigMap and Secret names.',
    codeSnippet: 'generatorOptions:\n  disableNameSuffixHash: true\nconfigMapGenerator:\n  - name: app-config\n    literals:\n      - KEY=value',
    tags: ['kubernetes', 'kustomize', 'configmap', 'hashing'],
  },
  {
    id: 'k8s-helm-l2-084',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle YAML type coercion issues with numeric strings in Helm?',
    options: [
      'Use the int function to force integer type',
      'Wrap the value in quotes using the quote function',
      'Use the type function to specify the type',
      'YAML type coercion cannot be controlled',
    ],
    correctAnswer: 1,
    explanation: 'YAML may interpret values like "1234" as integers. Using the `quote` function wraps the value in double quotes to preserve it as a string.',
    codeSnippet: 'metadata:\n  annotations:\n    port: {{ .Values.port | quote }}\n    # Without quote: port: 8080 (number)\n    # With quote: port: "8080" (string)',
    tags: ['kubernetes', 'helm', 'templates', 'quote'],
  },
  {
    id: 'k8s-helm-l2-085',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you adopt existing Kubernetes resources into a Helm release?',
    options: [
      'Use helm import',
      'Add Helm labels and annotations to existing resources, then helm upgrade',
      'Run helm install --adopt',
      'Existing resources cannot be managed by Helm',
    ],
    correctAnswer: 1,
    explanation: 'You can adopt existing resources by adding the required Helm labels (app.kubernetes.io/managed-by: Helm) and annotations (meta.helm.sh/release-name, meta.helm.sh/release-namespace) to them.',
    codeSnippet: '# Add to existing resource:\nmetadata:\n  labels:\n    app.kubernetes.io/managed-by: Helm\n  annotations:\n    meta.helm.sh/release-name: my-release\n    meta.helm.sh/release-namespace: default',
    tags: ['kubernetes', 'helm', 'adopt', 'release'],
  },
  {
    id: 'k8s-helm-l2-086',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What does the `fromYaml` function do in Helm?',
    options: [
      'Reads a YAML file from disk',
      'Parses a YAML string into a Go data structure (dict)',
      'Converts a Go struct to YAML',
      'Validates YAML syntax',
    ],
    correctAnswer: 1,
    explanation: 'The `fromYaml` function parses a YAML-formatted string and returns a dict/map, enabling further programmatic access to its fields.',
    codeSnippet: '{{- $config := .Files.Get "config.yaml" | fromYaml -}}\nserverPort: {{ $config.server.port }}',
    tags: ['kubernetes', 'helm', 'templates', 'fromYaml'],
  },
  {
    id: 'k8s-helm-l2-087',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What happens if you specify both commonLabels and a patch that modifies labels in Kustomize?',
    options: [
      'The patch overrides commonLabels',
      'commonLabels is applied first, then the patch is applied on top',
      'An error is thrown due to conflict',
      'commonLabels takes precedence and the patch is ignored',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize applies transformers in a defined order: generators first, then transformations like commonLabels, then patches. So patches can override commonLabels.',
    tags: ['kubernetes', 'kustomize', 'labels', 'patches'],
  },
  {
    id: 'k8s-helm-l2-088',
    language: 'kubernetes',
    level: 'level2',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What templating does Helmfile support in helmfile.yaml?',
    options: [
      'Only simple variable substitution',
      'Go templates with Sprig functions, similar to Helm',
      'Jinja2 templates',
      'No templating; helmfile.yaml must be static',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports Go templates with Sprig functions, allowing dynamic configuration similar to Helm chart templates.',
    codeSnippet: 'releases:\n  - name: my-app\n    chart: ./charts/my-app\n    values:\n      - image:\n          tag: {{ requiredEnv "IMAGE_TAG" }}',
    tags: ['kubernetes', 'helmfile', 'templates'],
  },
];

export default questions;
