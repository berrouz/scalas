import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-helm-l3-001',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you create a reusable template that renders differently based on the calling context?',
    options: [
      'Pass different values files for each call',
      'Pass the appropriate scope as the second argument to include/template',
      'Create separate templates for each context',
      'Use environment variables to switch behavior',
    ],
    correctAnswer: 1,
    explanation: 'Named templates accept a scope argument (the second parameter to include/template). By passing different scopes, the same template can render differently based on context.',
    codeSnippet: '{{- define "my-chart.container" -}}\n- name: {{ .name }}\n  image: {{ .image }}\n  ports:\n    - containerPort: {{ .port }}\n{{- end }}\n\ncontainers:\n  {{- include "my-chart.container" (dict "name" "web" "image" "nginx" "port" 80) | nindent 2 }}',
    tags: ['kubernetes', 'helm', 'templates', 'advanced'],
  },
  {
    id: 'k8s-helm-l3-002',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the purpose of the `dict` function in Helm templates?',
    options: [
      'It converts a dictionary to YAML',
      'It creates a new dictionary (map) from key-value pairs',
      'It looks up a value in a dictionary',
      'It merges two dictionaries',
    ],
    correctAnswer: 1,
    explanation: 'The `dict` function creates a new map from alternating key-value arguments, useful for constructing custom scopes for named templates.',
    codeSnippet: '{{- $ctx := dict "Release" .Release "Values" .Values "customKey" "customValue" -}}\n{{ include "my-chart.helper" $ctx }}',
    tags: ['kubernetes', 'helm', 'templates', 'dict'],
  },
  {
    id: 'k8s-helm-l3-003',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you merge multiple dictionaries in Helm templates?',
    options: [
      'Use the concat function',
      'Use the merge or mustMergeOverwrite function',
      'Use the combine function',
      'Dictionaries cannot be merged in Helm',
    ],
    correctAnswer: 1,
    explanation: 'The `merge` function performs a deep merge of dictionaries (first argument wins on conflict). `mustMergeOverwrite` gives later arguments priority.',
    codeSnippet: '{{- $defaults := dict "replicas" 1 "image" "nginx:latest" -}}\n{{- $overrides := dict "replicas" 3 -}}\n{{- $merged := mustMergeOverwrite $defaults $overrides -}}\nreplicas: {{ $merged.replicas }}  # outputs 3',
    tags: ['kubernetes', 'helm', 'templates', 'merge'],
  },
  {
    id: 'k8s-helm-l3-004',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'named-templates',
    question: 'Why is it problematic to use `template` instead of `include` when you need to indent the output?',
    options: [
      '`template` is deprecated',
      '`template` inserts content directly into the output pipeline and cannot be piped to indent/nindent',
      '`template` does not support named templates',
      '`template` only works in _helpers.tpl',
    ],
    correctAnswer: 1,
    explanation: 'The `template` action inserts the rendered output directly into the pipeline, so it cannot be piped to functions like nindent. `include` returns a string that can be piped.',
    codeSnippet: '# WRONG - cannot pipe template:\n{{ template "my-chart.labels" . | nindent 4 }}\n\n# CORRECT - include returns a string:\n{{ include "my-chart.labels" . | nindent 4 }}',
    tags: ['kubernetes', 'helm', 'named-templates', 'include'],
  },
  {
    id: 'k8s-helm-l3-005',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'How do you ensure a pre-upgrade hook runs a database migration before any pods are updated?',
    options: [
      'Use a Deployment with a higher priority',
      'Create a Job with helm.sh/hook: pre-upgrade and appropriate weight',
      'Use an init container in the main Deployment',
      'Add a preStop lifecycle hook to the existing pods',
    ],
    correctAnswer: 1,
    explanation: 'A pre-upgrade hook Job runs before any release resources are updated, ensuring the database migration completes first.',
    codeSnippet: 'apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: {{ .Release.Name }}-migrate\n  annotations:\n    "helm.sh/hook": pre-upgrade\n    "helm.sh/hook-weight": "-1"\n    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded\nspec:\n  template:\n    spec:\n      restartPolicy: Never\n      containers:\n        - name: migrate\n          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}\n          command: ["./migrate.sh"]',
    tags: ['kubernetes', 'helm', 'hooks', 'migration'],
  },
  {
    id: 'k8s-helm-l3-006',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'What happens if a Helm hook Job fails?',
    options: [
      'The hook is retried automatically',
      'The release proceeds regardless',
      'The release operation (install/upgrade) fails and does not proceed',
      'Only a warning is logged',
    ],
    correctAnswer: 2,
    explanation: 'If a hook resource fails (e.g., a Job does not complete successfully), the entire release operation is marked as failed and does not continue.',
    tags: ['kubernetes', 'helm', 'hooks', 'failure'],
  },
  {
    id: 'k8s-helm-l3-007',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'A Helm release is stuck in "pending-upgrade" state. How do you recover?',
    options: [
      'Wait for the timeout to expire',
      'Run helm rollback to the last successful revision',
      'Delete the namespace and reinstall',
      'Restart the Kubernetes API server',
    ],
    correctAnswer: 1,
    explanation: 'A "pending-upgrade" state means a previous upgrade was interrupted. Rolling back to the last successful revision recovers the release.',
    codeSnippet: '# Check release history\nhelm history my-release\n\n# Rollback to last good revision\nhelm rollback my-release 3',
    tags: ['kubernetes', 'helm', 'release', 'troubleshooting'],
  },
  {
    id: 'k8s-helm-l3-008',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you debug template rendering issues in Helm?',
    options: [
      'Add print statements to templates',
      'Use helm template with --debug flag, or helm install --dry-run --debug',
      'Check the Kubernetes API server logs',
      'Use kubectl describe on the release',
    ],
    correctAnswer: 1,
    explanation: 'The --debug flag with helm template or helm install --dry-run shows detailed rendering output including the computed values and rendered manifests.',
    codeSnippet: '# Debug locally without cluster:\nhelm template my-release my-chart/ --debug\n\n# Debug with server-side validation:\nhelm install my-release my-chart/ --dry-run --debug',
    tags: ['kubernetes', 'helm', 'debug', 'troubleshooting'],
  },
  {
    id: 'k8s-helm-l3-009',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you prevent a randomly generated Secret value from changing on every helm upgrade?',
    options: [
      'Store the secret in values.yaml',
      'Use the lookup function to check if the Secret already exists and reuse its value',
      'Generate the secret outside of Helm',
      'Use a fixed seed for the random generator',
    ],
    correctAnswer: 1,
    explanation: 'The lookup function queries the cluster for an existing Secret. If found, the existing value is reused; otherwise, a new random value is generated.',
    codeSnippet: '{{- $secret := lookup "v1" "Secret" .Release.Namespace (printf "%s-secret" .Release.Name) -}}\napiVersion: v1\nkind: Secret\nmetadata:\n  name: {{ .Release.Name }}-secret\ntype: Opaque\ndata:\n  password: {{ if $secret }}{{ index $secret.data "password" }}{{ else }}{{ randAlphaNum 32 | b64enc }}{{ end }}',
    tags: ['kubernetes', 'helm', 'templates', 'secrets', 'lookup'],
  },
  {
    id: 'k8s-helm-l3-010',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the limitation of the `lookup` function during `helm template`?',
    options: [
      'It only works with Secrets',
      'It always returns an empty dict because there is no cluster connection',
      'It requires admin permissions',
      'It is slower than other functions',
    ],
    correctAnswer: 1,
    explanation: 'During `helm template` (client-side rendering), the lookup function always returns an empty dict/map because there is no connection to a Kubernetes cluster.',
    tags: ['kubernetes', 'helm', 'templates', 'lookup', 'limitations'],
  },
  {
    id: 'k8s-helm-l3-011',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How do you override the values of a subchart that uses an alias?',
    options: [
      'Use the original chart name as the key',
      'Use the alias name as the key in the parent values.yaml',
      'Pass values directly with --set subchart.value',
      'Create a separate values file for the alias',
    ],
    correctAnswer: 1,
    explanation: 'When a dependency uses an alias, values for that dependency must be nested under the alias name, not the original chart name.',
    codeSnippet: '# Chart.yaml\ndependencies:\n  - name: redis\n    alias: cache\n    version: 17.0.0\n    repository: https://charts.bitnami.com/bitnami\n\n# values.yaml - use alias name:\ncache:\n  architecture: standalone\n  auth:\n    enabled: false',
    tags: ['kubernetes', 'helm', 'dependencies', 'alias'],
  },
  {
    id: 'k8s-helm-l3-012',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you generate multiple similar Kubernetes resources from a single template file?',
    options: [
      'Create separate template files for each resource',
      'Use range to iterate over a list in values.yaml and use the YAML document separator (---)',
      'Kubernetes does not support multiple resources per file',
      'Use the replicate function',
    ],
    correctAnswer: 1,
    explanation: 'You can use range to loop over a list from values and emit multiple YAML documents separated by --- in a single template file.',
    codeSnippet: '{{- range .Values.configMaps }}\n---\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ $.Release.Name }}-{{ .name }}\n  labels:\n    {{- include "my-chart.labels" $ | nindent 4 }}\ndata:\n  {{- toYaml .data | nindent 2 }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'range', 'multi-resource'],
  },
  {
    id: 'k8s-helm-l3-013',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use Kustomize to add a sidecar container to all Deployments?',
    options: [
      'Modify each Deployment file manually',
      'Use a strategic merge patch targeting all Deployments',
      'It is impossible without modifying the base',
      'Use an init container instead',
    ],
    correctAnswer: 1,
    explanation: 'A strategic merge patch can target all Deployments to add a sidecar container to their pod specs.',
    codeSnippet: '# kustomization.yaml\npatches:\n  - target:\n      kind: Deployment\n    patch: |-\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: not-used\n      spec:\n        template:\n          spec:\n            containers:\n              - name: log-shipper\n                image: fluent/fluent-bit:latest',
    tags: ['kubernetes', 'kustomize', 'patches', 'sidecar'],
  },
  {
    id: 'k8s-helm-l3-014',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you replace an entire array (instead of merging) in a strategic merge patch?',
    options: [
      'Use $patch: replace directive on the array',
      'Use op: replace in JSON 6902 patch',
      'Both A and B are valid approaches',
      'Arrays are always merged and cannot be replaced',
    ],
    correctAnswer: 2,
    explanation: 'Both approaches work: the $patch: replace directive in strategic merge patches replaces the array, and JSON 6902 op: replace can replace the entire array at its path.',
    codeSnippet: '# Strategic merge with $patch: replace:\nspec:\n  template:\n    spec:\n      containers:\n        $patch: replace\n        - name: only-container\n          image: myapp:latest',
    tags: ['kubernetes', 'kustomize', 'patches', 'replace'],
  },
  {
    id: 'k8s-helm-l3-015',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you handle secrets in Helmfile?',
    options: [
      'Store them in plain text in helmfile.yaml',
      'Use helm-secrets plugin with encrypted values files (SOPS/age)',
      'Pass them as command line arguments only',
      'Secrets are not supported in Helmfile',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile integrates with the helm-secrets plugin to decrypt values files encrypted with SOPS (supporting age, PGP, AWS KMS, etc.) at deploy time.',
    codeSnippet: 'releases:\n  - name: my-app\n    chart: ./charts/my-app\n    values:\n      - values.yaml\n    secrets:\n      - secrets.yaml  # encrypted with SOPS',
    tags: ['kubernetes', 'helmfile', 'secrets'],
  },
  {
    id: 'k8s-helm-l3-016',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What is the purpose of helmfile sync vs helmfile apply?',
    options: [
      'They are identical commands',
      'sync always runs install/upgrade; apply first shows a diff and only updates changed releases',
      'sync is for local charts; apply is for remote charts',
      'apply is deprecated in favor of sync',
    ],
    correctAnswer: 1,
    explanation: '`helmfile sync` unconditionally runs helm upgrade --install for all releases. `helmfile apply` first runs a diff and only upgrades releases that have changes.',
    tags: ['kubernetes', 'helmfile', 'sync', 'apply'],
  },
  {
    id: 'k8s-helm-l3-017',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle Helm releases that manage resources across multiple namespaces?',
    options: [
      'Helm inherently supports multi-namespace releases',
      'Hardcode the namespace in each template metadata instead of relying on the release namespace',
      'Create separate releases for each namespace',
      'Use namespace federation',
    ],
    correctAnswer: 1,
    explanation: 'While a Helm release lives in one namespace, individual resources can specify their own namespace in metadata. However, this requires careful management.',
    codeSnippet: 'apiVersion: v1\nkind: Namespace\nmetadata:\n  name: monitoring\n---\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-config\n  namespace: monitoring  # explicit namespace',
    tags: ['kubernetes', 'helm', 'namespaces', 'multi-namespace'],
  },
  {
    id: 'k8s-helm-l3-018',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the `list` function used for in Helm templates?',
    options: [
      'Lists all resources in the chart',
      'Creates a new list (slice) from the provided arguments',
      'Lists all available Sprig functions',
      'Lists all values keys',
    ],
    correctAnswer: 1,
    explanation: 'The `list` function creates a new list from its arguments, useful for building dynamic data structures within templates.',
    codeSnippet: '{{- $envs := list "dev" "staging" "prod" -}}\n{{- if has .Values.env $envs }}\n# Valid environment\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'list'],
  },
  {
    id: 'k8s-helm-l3-019',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a complex validation schema for Helm chart values?',
    options: [
      'Use Go template conditionals only',
      'Use a JSON Schema file (values.schema.json) at the chart root',
      'Use an admission webhook',
      'Write a Helm plugin',
    ],
    correctAnswer: 1,
    explanation: 'Helm supports JSON Schema validation via a values.schema.json file at the chart root, which validates values before template rendering.',
    codeSnippet: '{\n  "$schema": "https://json-schema.org/draft-07/schema#",\n  "type": "object",\n  "required": ["image"],\n  "properties": {\n    "image": {\n      "type": "object",\n      "required": ["repository", "tag"],\n      "properties": {\n        "repository": {"type": "string"},\n        "tag": {"type": "string", "pattern": "^v[0-9]+\\\\.[0-9]+\\\\.[0-9]+$"}\n      }\n    },\n    "replicaCount": {"type": "integer", "minimum": 1}\n  }\n}',
    tags: ['kubernetes', 'helm', 'schema', 'validation'],
  },
  {
    id: 'k8s-helm-l3-020',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How do you create a chart that installs the same subchart multiple times with different configs?',
    options: [
      'List the same dependency multiple times without aliases',
      'Use aliases to install the same dependency chart under different names',
      'Use the replicas field in Chart.yaml',
      'This is not possible in Helm',
    ],
    correctAnswer: 1,
    explanation: 'By using the alias field, you can declare the same chart as a dependency multiple times, each with a different alias and configuration.',
    codeSnippet: '# Chart.yaml\ndependencies:\n  - name: redis\n    alias: session-cache\n    version: 17.0.0\n    repository: https://charts.bitnami.com/bitnami\n  - name: redis\n    alias: data-cache\n    version: 17.0.0\n    repository: https://charts.bitnami.com/bitnami\n\n# values.yaml\nsession-cache:\n  master:\n    persistence:\n      size: 1Gi\ndata-cache:\n  master:\n    persistence:\n      size: 10Gi',
    tags: ['kubernetes', 'helm', 'dependencies', 'alias', 'multiple'],
  },
  {
    id: 'k8s-helm-l3-021',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What Helm plugin provides a diff of changes before upgrading?',
    options: [
      'helm-compare',
      'helm-diff',
      'helm-preview',
      'helm-delta',
    ],
    correctAnswer: 1,
    explanation: 'The helm-diff plugin shows a colored diff of what would change in the cluster when running an upgrade, without actually applying changes.',
    codeSnippet: '# Install the plugin:\nhelm plugin install https://github.com/databus23/helm-diff\n\n# Show diff:\nhelm diff upgrade my-release my-chart/ -f values.yaml',
    tags: ['kubernetes', 'helm', 'diff', 'plugin'],
  },
  {
    id: 'k8s-helm-l3-022',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle optional CRDs that may not exist in the cluster when rendering templates?',
    options: [
      'Always install CRDs first',
      'Use .Capabilities.APIVersions.Has to check if the CRD API exists before including the resource',
      'Use try/catch blocks',
      'CRDs are always available',
    ],
    correctAnswer: 1,
    explanation: 'The .Capabilities.APIVersions.Has function checks if a specific API version is available in the cluster, allowing conditional rendering of CRD instances.',
    codeSnippet: '{{- if .Capabilities.APIVersions.Has "monitoring.coreos.com/v1" }}\napiVersion: monitoring.coreos.com/v1\nkind: ServiceMonitor\nmetadata:\n  name: {{ .Release.Name }}-monitor\nspec:\n  selector:\n    matchLabels:\n      app: {{ .Release.Name }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'capabilities', 'crds'],
  },
  {
    id: 'k8s-helm-l3-023',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize with Helm charts?',
    options: [
      'They are mutually exclusive tools',
      'Use the helmCharts or helmGlobals field in kustomization.yaml to render Helm charts as a base',
      'Only by running helm template first and using the output as Kustomize input',
      'Import Helm templates directly into Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'Modern Kustomize supports helmCharts in kustomization.yaml, which renders Helm charts and uses the output as Kustomize input for further customization.',
    codeSnippet: 'apiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\nhelmCharts:\n  - name: nginx\n    repo: https://charts.bitnami.com/bitnami\n    version: 15.0.0\n    releaseName: my-nginx\n    valuesFile: nginx-values.yaml',
    tags: ['kubernetes', 'kustomize', 'helm', 'integration'],
  },
  {
    id: 'k8s-helm-l3-024',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use inline patches in Kustomize instead of separate patch files?',
    options: [
      'Inline patches are not supported',
      'Use the patch field with a multi-line YAML string under the patches array',
      'Embed patches in the resources field',
      'Use the inline: true directive',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize supports inline patches using the `patch` field with a multi-line YAML string, avoiding the need for separate patch files.',
    codeSnippet: 'patches:\n  - target:\n      kind: Deployment\n      name: my-app\n    patch: |-\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: my-app\n      spec:\n        replicas: 5',
    tags: ['kubernetes', 'kustomize', 'patches', 'inline'],
  },
  {
    id: 'k8s-helm-l3-025',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you migrate a Helm v2 release to Helm v3?',
    options: [
      'Reinstall the chart with Helm v3',
      'Use the helm-2to3 plugin to convert release metadata',
      'Export the manifests and reimport them',
      'Helm v3 automatically reads v2 releases',
    ],
    correctAnswer: 1,
    explanation: 'The helm-2to3 plugin migrates Helm v2 configuration and releases to v3 format, converting release metadata from ConfigMaps to Secrets.',
    codeSnippet: '# Install the migration plugin:\nhelm3 plugin install https://github.com/helm/helm-2to3\n\n# Migrate a release:\nhelm3 2to3 convert my-release',
    tags: ['kubernetes', 'helm', 'migration', 'v2', 'v3'],
  },
  {
    id: 'k8s-helm-l3-026',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What problem does the "Helm upgrade --reuse-values" approach create with chart upgrades?',
    options: [
      'It doubles the resource consumption',
      'New default values added to the chart are not applied because old values are reused',
      'It prevents rollbacks',
      'It causes namespace conflicts',
    ],
    correctAnswer: 1,
    explanation: 'When --reuse-values is used, values from the previous release are merged, but any new default values added in the updated chart are not included. This can cause issues if the chart requires new values.',
    tags: ['kubernetes', 'helm', 'upgrade', 'reuse-values', 'pitfalls'],
  },
  {
    id: 'k8s-helm-l3-027',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do you reference a chart dependency from an OCI registry?',
    options: [
      'repository: https://registry.example.com/charts',
      'repository: oci://registry.example.com/charts',
      'ociRepository: registry.example.com/charts',
      'registry: oci://registry.example.com/charts',
    ],
    correctAnswer: 1,
    explanation: 'Chart dependencies from OCI registries use the oci:// protocol prefix in the repository field of Chart.yaml.',
    codeSnippet: '# Chart.yaml\ndependencies:\n  - name: common\n    version: 2.0.0\n    repository: oci://registry.example.com/charts',
    tags: ['kubernetes', 'helm', 'oci', 'dependencies'],
  },
  {
    id: 'k8s-helm-l3-028',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle Helm chart backward compatibility when removing a values key?',
    options: [
      'Just remove the key; users will notice the error',
      'Use the `coalesce` or `default` function to fall back to the new key while supporting the old one',
      'Create a separate chart version',
      'Document the change and force users to update',
    ],
    correctAnswer: 1,
    explanation: 'Using coalesce allows checking multiple keys, enabling a deprecation period where both old and new keys work.',
    codeSnippet: '# Support both old .Values.imageTag and new .Values.image.tag:\nimage: {{ .Values.image.repository }}:{{ coalesce .Values.image.tag .Values.imageTag "latest" }}',
    tags: ['kubernetes', 'helm', 'templates', 'backward-compatibility'],
  },
  {
    id: 'k8s-helm-l3-029',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'hooks',
    question: 'How can you run a hook that executes after all resources are ready (not just created)?',
    options: [
      'Use post-install hook; it waits for all resources',
      'Combine post-install/post-upgrade hooks with --wait flag on helm install/upgrade',
      'Use a readiness-hook annotation',
      'Hooks always wait for readiness',
    ],
    correctAnswer: 1,
    explanation: 'Post-install hooks run after resources are sent to Kubernetes but not necessarily ready. Using --wait makes Helm wait for resources to be ready before running post hooks.',
    tags: ['kubernetes', 'helm', 'hooks', 'wait'],
  },
  {
    id: 'k8s-helm-l3-030',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle secrets management in Helm charts securely?',
    options: [
      'Store secrets in values.yaml encrypted with base64',
      'Use helm-secrets plugin with SOPS encryption, or reference external secret managers via External Secrets Operator',
      'Pass all secrets via --set flags',
      'Store secrets in the charts/ directory',
    ],
    correctAnswer: 1,
    explanation: 'Best practices include using helm-secrets with SOPS for encrypting values files, or using the External Secrets Operator to pull secrets from external vaults at runtime.',
    tags: ['kubernetes', 'helm', 'secrets', 'security'],
  },
  {
    id: 'k8s-helm-l3-031',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you make Helm manage resources with server-side apply (SSA)?',
    options: [
      'Use the --server-side flag with helm install/upgrade',
      'Set serverSideApply: true in values.yaml',
      'SSA is not supported by Helm',
      'Enable it in Chart.yaml',
    ],
    correctAnswer: 0,
    explanation: 'Helm supports server-side apply via the --server-side flag, which uses the Kubernetes SSA mechanism for managing field ownership and conflicts.',
    codeSnippet: 'helm upgrade --install my-release my-chart \\\n  --server-side \\\n  --force-conflicts',
    tags: ['kubernetes', 'helm', 'server-side-apply'],
  },
  {
    id: 'k8s-helm-l3-032',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you manage multiple environments (dev, staging, prod) with Kustomize?',
    options: [
      'Use different kustomization.yaml files in the same directory',
      'Use a base directory with separate overlay directories for each environment',
      'Use branches in Git for each environment',
      'Use environment variables in kustomization.yaml',
    ],
    correctAnswer: 1,
    explanation: 'The standard Kustomize pattern is a base directory with common resources and separate overlay directories for each environment that modify the base.',
    codeSnippet: '# Directory structure:\n# base/\n#   kustomization.yaml\n#   deployment.yaml\n#   service.yaml\n# overlays/\n#   dev/\n#     kustomization.yaml\n#     replica-patch.yaml\n#   prod/\n#     kustomization.yaml\n#     replica-patch.yaml\n#     hpa.yaml\n\n# overlays/prod/kustomization.yaml\napiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\nresources:\n  - ../../base\npatches:\n  - replica-patch.yaml',
    tags: ['kubernetes', 'kustomize', 'environments'],
  },
  {
    id: 'k8s-helm-l3-033',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you use Helm with GitOps tools like ArgoCD?',
    options: [
      'ArgoCD cannot work with Helm',
      'ArgoCD natively supports rendering Helm charts as an Application source',
      'You must use helm template output committed to Git',
      'ArgoCD only supports Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'ArgoCD natively supports Helm charts as an Application source. It renders charts using helm template and manages the resulting resources.',
    codeSnippet: 'apiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: my-app\nspec:\n  source:\n    repoURL: https://charts.bitnami.com/bitnami\n    chart: nginx\n    targetRevision: 15.0.0\n    helm:\n      releaseName: my-nginx\n      values: |\n        replicaCount: 3',
    tags: ['kubernetes', 'helm', 'argocd', 'gitops'],
  },
  {
    id: 'k8s-helm-l3-034',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What happens when you try to install a chart whose CRDs conflict with existing CRDs in the cluster?',
    options: [
      'Helm automatically upgrades the CRDs',
      'The installation fails because Helm will not overwrite existing CRDs from the crds/ directory',
      'Helm merges the CRD definitions',
      'The existing CRDs are deleted and replaced',
    ],
    correctAnswer: 1,
    explanation: 'Helm refuses to modify or delete existing CRDs. If a CRD from the crds/ directory already exists, the install may fail with a conflict error.',
    tags: ['kubernetes', 'helm', 'crds', 'conflicts'],
  },
  {
    id: 'k8s-helm-l3-035',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you write a Helm chart that supports both Ingress API v1beta1 and v1?',
    options: [
      'Use two separate template files',
      'Check .Capabilities.APIVersions to conditionally render the correct API version',
      'Always use v1beta1 for backward compatibility',
      'Use a separate chart for each API version',
    ],
    correctAnswer: 1,
    explanation: 'Using .Capabilities.APIVersions.Has or .Capabilities.KubeVersion, you can conditionally render the appropriate Ingress API version based on the cluster capabilities.',
    codeSnippet: '{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}\napiVersion: networking.k8s.io/v1\n{{- else -}}\napiVersion: networking.k8s.io/v1beta1\n{{- end }}\nkind: Ingress',
    tags: ['kubernetes', 'helm', 'templates', 'capabilities', 'ingress'],
  },
  {
    id: 'k8s-helm-l3-036',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use Kustomize to remove an environment variable from a container?',
    options: [
      'Set the env var value to null',
      'Use a JSON 6902 patch with op: remove',
      'Use $patch: delete in strategic merge',
      'Both B and C can work, depending on the patch type used',
    ],
    correctAnswer: 3,
    explanation: 'JSON 6902 patches can use op: remove with the specific path. Strategic merge patches can use $patch: delete. Both approaches work.',
    codeSnippet: '# JSON 6902 approach:\n- op: remove\n  path: /spec/template/spec/containers/0/env/1\n\n# Strategic merge approach:\nspec:\n  template:\n    spec:\n      containers:\n        - name: app\n          env:\n            - name: OLD_VAR\n              $patch: delete',
    tags: ['kubernetes', 'kustomize', 'patches', 'remove'],
  },
  {
    id: 'k8s-helm-l3-037',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you post-render Helm output for further processing?',
    options: [
      'Use --post-renderer to pass rendered manifests through an external command',
      'Use a post-install hook',
      'Pipe helm template output through kubectl',
      'Post-rendering is not supported',
    ],
    correctAnswer: 0,
    explanation: 'The --post-renderer flag passes rendered manifests through an external executable (like kustomize) for further processing before they are sent to the cluster.',
    codeSnippet: '# Use kustomize as a post-renderer:\nhelm upgrade --install my-release my-chart \\\n  --post-renderer ./kustomize-post-render.sh\n\n# kustomize-post-render.sh:\n#!/bin/bash\ncat > base/all.yaml\nkustomize build base/',
    tags: ['kubernetes', 'helm', 'post-renderer'],
  },
  {
    id: 'k8s-helm-l3-038',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the `semverCompare` function used for in Helm?',
    options: [
      'Compares two chart versions',
      'Compares a semver constraint against a version string, returning a boolean',
      'Validates that a version string follows semver format',
      'Compares Helm binary versions',
    ],
    correctAnswer: 1,
    explanation: 'semverCompare checks if a version satisfies a semver constraint, commonly used to check Kubernetes version for API compatibility.',
    codeSnippet: '{{- if semverCompare ">=1.21-0" .Capabilities.KubeVersion.GitVersion }}\n# Use features available in K8s 1.21+\npolicy/v1\n{{- else }}\npolicy/v1beta1\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'semver'],
  },
  {
    id: 'k8s-helm-l3-039',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you use Helmfile to manage chart repositories without adding them globally?',
    options: [
      'Repositories must always be added globally',
      'Declare repositories in the repositories section of helmfile.yaml',
      'Use inline chart URLs',
      'Use OCI registries only',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile manages its own repository definitions in the repositories section, adding them automatically when needed.',
    codeSnippet: 'repositories:\n  - name: bitnami\n    url: https://charts.bitnami.com/bitnami\n  - name: prometheus-community\n    url: https://prometheus-community.github.io/helm-charts\n\nreleases:\n  - name: nginx\n    chart: bitnami/nginx\n    version: 15.0.0',
    tags: ['kubernetes', 'helmfile', 'repositories'],
  },
  {
    id: 'k8s-helm-l3-040',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a "dry-run" validation at the template level to catch misconfigurations early?',
    options: [
      'Use helm lint only',
      'Combine required, fail, and regexMatch functions for comprehensive value validation',
      'Use Kubernetes admission webhooks',
      'Template-level validation is not possible',
    ],
    correctAnswer: 1,
    explanation: 'Combining template functions like required, fail, regexMatch, and conditional logic creates a comprehensive validation layer that catches issues during rendering.',
    codeSnippet: '{{- $repo := required ".image.repository is required" .Values.image.repository -}}\n{{- if not (regexMatch "^[a-z0-9./-]+$" $repo) }}\n  {{- fail (printf "Invalid image repository: %s" $repo) }}\n{{- end }}\n{{- if and (eq .Values.env "production") (lt (int .Values.replicaCount) 2) }}\n  {{- fail "Production requires at least 2 replicas" }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'validation'],
  },
  {
    id: 'k8s-helm-l3-041',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How does the Chart.lock file work in Helm?',
    options: [
      'It prevents concurrent chart installations',
      'It records the exact versions of resolved dependencies for reproducible builds',
      'It locks the chart from being modified',
      'It locks the release to prevent upgrades',
    ],
    correctAnswer: 1,
    explanation: 'Chart.lock records the exact resolved versions and hashes of dependencies, ensuring helm dependency build produces reproducible results.',
    codeSnippet: '# Chart.lock (auto-generated)\ndependencies:\n- name: mysql\n  repository: https://charts.bitnami.com/bitnami\n  version: 9.4.1\ndigest: sha256:abc123...\ngenerated: "2024-01-15T10:30:00Z"',
    tags: ['kubernetes', 'helm', 'dependencies', 'lock'],
  },
  {
    id: 'k8s-helm-l3-042',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the proper way to handle Kubernetes resource naming constraints in Helm?',
    options: [
      'Let users set any name they want',
      'Truncate names to 63 characters, trim trailing hyphens, and use lowercase',
      'Always use the release name directly',
      'Use UUIDs for resource names',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes DNS-based naming requires names to be max 63 characters, lowercase alphanumeric with hyphens. The fullname helper should enforce these constraints.',
    codeSnippet: '{{- define "my-chart.fullname" -}}\n{{- if .Values.fullnameOverride }}\n{{- .Values.fullnameOverride | trunc 63 | trimSuffix "-" }}\n{{- else }}\n{{- $name := default .Chart.Name .Values.nameOverride }}\n{{- printf "%s-%s" .Release.Name $name | trunc 63 | trimSuffix "-" }}\n{{- end }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'naming'],
  },
  {
    id: 'k8s-helm-l3-043',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the effect of setting HELM_DRIVER=sql?',
    options: [
      'Helm stores releases in a SQL database instead of Kubernetes Secrets',
      'Helm uses SQL queries to filter releases',
      'Helm validates SQL connections in chart templates',
      'This environment variable does not exist',
    ],
    correctAnswer: 0,
    explanation: 'Setting HELM_DRIVER=sql stores release information in a SQL database (PostgreSQL) instead of the default Kubernetes Secrets, useful for large-scale environments.',
    tags: ['kubernetes', 'helm', 'release', 'storage', 'sql'],
  },
  {
    id: 'k8s-helm-l3-044',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize transformers to modify all resources matching certain criteria?',
    options: [
      'Use shell scripts to process YAML files',
      'Write custom transformer configurations with specific GVK targeting',
      'Kustomize cannot target specific resources',
      'Use annotations to opt resources in or out',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize transformers can target specific resources by GVK (Group, Version, Kind), name, and namespace, allowing selective modifications across all matching resources.',
    tags: ['kubernetes', 'kustomize', 'transformers'],
  },
  {
    id: 'k8s-helm-l3-045',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What is the helmfile destroy command used for?',
    options: [
      'Destroys the Kubernetes cluster',
      'Uninstalls all releases defined in the helmfile',
      'Removes the helmfile binary',
      'Deletes the helmfile.yaml configuration',
    ],
    correctAnswer: 1,
    explanation: 'The `helmfile destroy` command runs helm uninstall for every release defined in the helmfile, tearing down the entire stack.',
    codeSnippet: '# Destroy all releases in staging:\nhelmfile -e staging destroy\n\n# Destroy specific releases:\nhelmfile -l tier=frontend destroy',
    tags: ['kubernetes', 'helmfile', 'destroy'],
  },
  {
    id: 'k8s-helm-l3-046',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the best practice for organizing complex Helm templates?',
    options: [
      'Put everything in one large template file',
      'Use one template file per Kubernetes resource kind, with shared logic in _helpers.tpl',
      'Create nested directories inside templates/',
      'Use external template libraries via HTTP',
    ],
    correctAnswer: 1,
    explanation: 'Best practice is one file per resource type (deployment.yaml, service.yaml, etc.) with reusable named templates in _helpers.tpl. Files prefixed with _ are not rendered as manifests.',
    tags: ['kubernetes', 'helm', 'templates', 'best-practices'],
  },
  {
    id: 'k8s-helm-l3-047',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'named-templates',
    question: 'How do you create a library chart that other charts can consume?',
    options: [
      'Set type: library in Chart.yaml and define named templates only',
      'Create a regular chart and publish it to a repository',
      'Use a Git submodule',
      'Share _helpers.tpl files manually',
    ],
    correctAnswer: 0,
    explanation: 'A library chart is created by setting type: library in Chart.yaml. It only contains named templates (define blocks) with no renderable manifests, and is consumed as a dependency.',
    codeSnippet: '# library Chart.yaml\napiVersion: v2\nname: common-lib\ntype: library\nversion: 1.0.0\n\n# library templates/_helpers.tpl\n{{- define "common-lib.labels" -}}\napp.kubernetes.io/managed-by: {{ .Release.Service }}\nhelm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'library-chart', 'named-templates'],
  },
  {
    id: 'k8s-helm-l3-048',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How can you perform blue-green or canary deployments using Helm?',
    options: [
      'Helm natively supports blue-green deployments',
      'Use separate releases with different names and a service switch, or integrate with tools like Flagger or Argo Rollouts',
      'Use the --strategy=canary flag',
      'This is not possible with Helm',
    ],
    correctAnswer: 1,
    explanation: 'Helm does not natively support advanced deployment strategies. You can manage separate releases for blue/green or integrate with progressive delivery tools like Flagger or Argo Rollouts.',
    tags: ['kubernetes', 'helm', 'deployment-strategy', 'canary'],
  },
  {
    id: 'k8s-helm-l3-049',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle the problem of Helm template whitespace producing invalid YAML?',
    options: [
      'Always use YAML validators after rendering',
      'Use {{- and -}} trim markers strategically, and use nindent for controlled formatting',
      'Add yaml-lint comments',
      'Whitespace issues cannot be resolved',
    ],
    correctAnswer: 1,
    explanation: 'Careful use of trim markers ({{- and -}}) combined with nindent/indent functions controls whitespace precisely to produce valid YAML output.',
    codeSnippet: '# Without trim markers (extra blank lines):\n{{ if .Values.enabled }}\nresource: here\n{{ end }}\n\n# With trim markers (clean output):\n{{- if .Values.enabled }}\nresource: here\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'whitespace'],
  },
  {
    id: 'k8s-helm-l3-050',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize to apply different resource quotas per environment?',
    options: [
      'Kustomize cannot manage ResourceQuotas',
      'Include the ResourceQuota in the base and use patches in overlays to modify limits',
      'Use a Kustomize plugin',
      'Create separate ResourceQuota files outside of Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'Define a ResourceQuota in the base and use environment-specific overlays with patches to adjust the limits for each environment.',
    codeSnippet: '# base/resource-quota.yaml\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: env-quota\nspec:\n  hard:\n    cpu: "2"\n    memory: 4Gi\n\n# overlays/prod/quota-patch.yaml\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: env-quota\nspec:\n  hard:\n    cpu: "16"\n    memory: 32Gi',
    tags: ['kubernetes', 'kustomize', 'resource-quota', 'environments'],
  },
  {
    id: 'k8s-helm-l3-051',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'How do you sign and verify Helm charts stored in OCI registries?',
    options: [
      'OCI registries automatically sign charts',
      'Use cosign or Notary to sign chart artifacts and verify signatures before installation',
      'Helm built-in signing works with OCI registries',
      'OCI charts cannot be signed',
    ],
    correctAnswer: 1,
    explanation: 'Sigstore cosign or Notary can sign OCI artifacts including Helm charts. Verification can be done before installing to ensure chart integrity.',
    codeSnippet: '# Sign a chart with cosign:\ncosign sign oci://registry.example.com/charts/my-chart:1.0.0\n\n# Verify before installing:\ncosign verify oci://registry.example.com/charts/my-chart:1.0.0',
    tags: ['kubernetes', 'helm', 'oci', 'signing', 'security'],
  },
  {
    id: 'k8s-helm-l3-052',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you use Helm to generate a random value once and preserve it across upgrades without lookup?',
    options: [
      'Use a pre-install hook that creates the secret',
      'Generate it in values.yaml with a comment',
      'This is not possible without lookup',
      'Use a ConfigMap with immutable: true',
    ],
    correctAnswer: 0,
    explanation: 'A pre-install hook Job can generate and store random values in a Secret. Since hooks run only at the specified lifecycle point, the value persists across upgrades.',
    codeSnippet: 'apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: {{ .Release.Name }}-gen-secret\n  annotations:\n    "helm.sh/hook": pre-install\n    "helm.sh/hook-delete-policy": hook-succeeded\nspec:\n  template:\n    spec:\n      restartPolicy: Never\n      containers:\n        - name: gen\n          image: bitnami/kubectl\n          command:\n            - sh\n            - -c\n            - |\n              kubectl create secret generic my-secret \\\n                --from-literal=key=$(openssl rand -hex 16) \\\n                -n {{ .Release.Namespace }} || true',
    tags: ['kubernetes', 'helm', 'hooks', 'secrets'],
  },
  {
    id: 'k8s-helm-l3-053',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'Why might helm upgrade fail with "another operation is in progress"?',
    options: [
      'Two users are running helm simultaneously on different charts',
      'A previous operation (install/upgrade) did not complete and left the release in a pending state',
      'The Kubernetes API server is overloaded',
      'The Helm repository is being updated',
    ],
    correctAnswer: 1,
    explanation: 'This error occurs when a previous Helm operation was interrupted, leaving the release in a pending state (pending-install, pending-upgrade, etc.).',
    codeSnippet: '# Check the release status:\nhelm status my-release\n\n# If stuck, rollback to recover:\nhelm rollback my-release 0',
    tags: ['kubernetes', 'helm', 'troubleshooting', 'pending'],
  },
  {
    id: 'k8s-helm-l3-054',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you use Helmfile with Terraform for infrastructure provisioning?',
    options: [
      'Helmfile replaces Terraform',
      'Helmfile has a built-in Terraform provider',
      'Use Terraform to provision the cluster, then Helmfile to deploy applications, triggered by CI/CD',
      'They cannot be used together',
    ],
    correctAnswer: 2,
    explanation: 'The common pattern is using Terraform for infrastructure provisioning (cluster, VPC, etc.) and Helmfile for application deployment on top, typically orchestrated by CI/CD pipelines.',
    tags: ['kubernetes', 'helmfile', 'terraform', 'infrastructure'],
  },
  {
    id: 'k8s-helm-l3-055',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you dynamically generate RBAC rules based on values in a Helm chart?',
    options: [
      'RBAC rules cannot be templated',
      'Use range to iterate over a list of rules defined in values.yaml',
      'Use a Kubernetes webhook to inject rules',
      'Always hardcode RBAC rules for security',
    ],
    correctAnswer: 1,
    explanation: 'RBAC rules can be dynamically generated using range to iterate over a configurable list of rules in values.yaml.',
    codeSnippet: 'apiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: {{ .Release.Name }}-role\nrules:\n{{- range .Values.rbac.rules }}\n  - apiGroups: {{ .apiGroups | toYaml | nindent 6 }}\n    resources: {{ .resources | toYaml | nindent 6 }}\n    verbs: {{ .verbs | toYaml | nindent 6 }}\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'rbac'],
  },
  {
    id: 'k8s-helm-l3-056',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize to inject a sidecar only in specific environments?',
    options: [
      'Use a component that adds the sidecar and include it only in the overlays that need it',
      'Add conditional logic in kustomization.yaml',
      'Use a Kustomize if statement',
      'Kustomize does not support conditional resource inclusion',
    ],
    correctAnswer: 0,
    explanation: 'Kustomize components allow defining optional additions like sidecars that can be selectively included in specific overlays.',
    codeSnippet: '# components/istio-sidecar/kustomization.yaml\napiVersion: kustomize.config.k8s.io/v1alpha1\nkind: Component\npatches:\n  - path: inject-sidecar.yaml\n\n# overlays/prod/kustomization.yaml\nresources:\n  - ../../base\ncomponents:\n  - ../../components/istio-sidecar',
    tags: ['kubernetes', 'kustomize', 'components', 'sidecar'],
  },
  {
    id: 'k8s-helm-l3-057',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you configure Helm to use a specific kubeconfig or context?',
    options: [
      'Helm always uses the current kubectl context',
      'Use --kubeconfig flag or KUBECONFIG environment variable, and --kube-context for specific context',
      'Modify the Chart.yaml to include context information',
      'Use a Helm plugin for multi-cluster support',
    ],
    correctAnswer: 1,
    explanation: 'Helm supports --kubeconfig to specify a kubeconfig file and --kube-context to select a specific context within that file.',
    codeSnippet: 'helm install my-release my-chart \\\n  --kubeconfig ~/.kube/prod-config \\\n  --kube-context prod-cluster',
    tags: ['kubernetes', 'helm', 'kubeconfig', 'context'],
  },
  {
    id: 'k8s-helm-l3-058',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you handle Helm chart upgrades that require changing immutable fields (like a Job selector)?',
    options: [
      'Use --force to delete and recreate the resource',
      'Immutable fields cannot be changed; redesign to avoid them',
      'Use helm.sh/resource-policy: keep to preserve the old resource',
      'Helm automatically handles immutable field changes',
    ],
    correctAnswer: 0,
    explanation: 'The --force flag during upgrade deletes and recreates resources rather than patching them, which is the only way to change immutable fields like Job selectors.',
    tags: ['kubernetes', 'helm', 'upgrade', 'immutable'],
  },
  {
    id: 'k8s-helm-l3-059',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What does the annotation `helm.sh/resource-policy: keep` do?',
    options: [
      'Keeps the resource cached in Helm storage',
      'Prevents the resource from being deleted when the release is uninstalled',
      'Keeps the resource in pending state until approved',
      'Preserves the resource YAML in a backup',
    ],
    correctAnswer: 1,
    explanation: 'The `helm.sh/resource-policy: keep` annotation prevents Helm from deleting the resource when the release is uninstalled or upgraded, useful for PVCs and databases.',
    codeSnippet: 'apiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: {{ .Release.Name }}-data\n  annotations:\n    "helm.sh/resource-policy": keep',
    tags: ['kubernetes', 'helm', 'resource-policy', 'keep'],
  },
  {
    id: 'k8s-helm-l3-060',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use a Kustomize transformer to set resource limits on all containers?',
    options: [
      'Use a LimitRange instead',
      'Write a patch that targets all Deployments and sets resource limits',
      'This requires a custom Kustomize plugin',
      'Use commonResources field in kustomization.yaml',
    ],
    correctAnswer: 1,
    explanation: 'A patch targeting all Deployments (or Pods) by kind can add or modify resource limits. Alternatively, a custom transformer or plugin can handle this.',
    codeSnippet: 'patches:\n  - target:\n      kind: Deployment\n    patch: |-\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: not-important\n      spec:\n        template:\n          spec:\n            containers:\n              - name: "*"\n                resources:\n                  limits:\n                    memory: 512Mi\n                    cpu: 500m',
    tags: ['kubernetes', 'kustomize', 'patches', 'resources'],
  },
  {
    id: 'k8s-helm-l3-061',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement a Helm chart that manages external resources (like DNS records) alongside Kubernetes resources?',
    options: [
      'Helm can only manage Kubernetes resources',
      'Use Kubernetes operators (like ExternalDNS) deployed via the chart, with Kubernetes annotations to drive external changes',
      'Use Helm hooks to call external APIs',
      'Create custom resource definitions for external resources',
    ],
    correctAnswer: 1,
    explanation: 'The best practice is to use Kubernetes operators (deployed via the chart or separately) that watch Kubernetes resources and manage external resources based on annotations.',
    tags: ['kubernetes', 'helm', 'external-resources', 'operators'],
  },
  {
    id: 'k8s-helm-l3-062',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you handle helmfile.yaml that grows too large?',
    options: [
      'Large helmfiles are unavoidable',
      'Split into multiple helmfile.yaml files and use the helmfiles: field to include them',
      'Use Git branches for different sections',
      'Convert to plain Helm commands',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports splitting configuration across multiple files using the helmfiles: field, which includes other helmfile.yaml files.',
    codeSnippet: '# helmfile.yaml\nhelmfiles:\n  - path: helmfile.d/infrastructure.yaml\n  - path: helmfile.d/applications.yaml\n  - path: helmfile.d/monitoring.yaml',
    tags: ['kubernetes', 'helmfile', 'organization'],
  },
  {
    id: 'k8s-helm-l3-063',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle a "UPGRADE FAILED: has no deployed releases" error?',
    options: [
      'The release must be uninstalled and reinstalled',
      'Use helm upgrade --install with --force, or uninstall with --keep-history then rollback',
      'Restart the Kubernetes cluster',
      'Delete the namespace and start over',
    ],
    correctAnswer: 1,
    explanation: 'This error occurs when all revisions are in failed state. You can use --force to force a new upgrade, or uninstall with --keep-history and reinstall.',
    codeSnippet: '# Option 1: Force upgrade\nhelm upgrade my-release my-chart --force\n\n# Option 2: Uninstall and reinstall\nhelm uninstall my-release\nhelm install my-release my-chart',
    tags: ['kubernetes', 'helm', 'troubleshooting'],
  },
  {
    id: 'k8s-helm-l3-064',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What are the security considerations when using Helm chart values from untrusted sources?',
    options: [
      'Helm automatically sanitizes all values',
      'Values injected into templates can cause YAML injection; use quote/squote and validate inputs',
      'Only --set values are dangerous',
      'Security is only a concern with Helm v2',
    ],
    correctAnswer: 1,
    explanation: 'Untrusted values can inject unexpected YAML structures. Always quote string values, validate input patterns, and use the `required` function for mandatory fields.',
    codeSnippet: '# Vulnerable:\nannotations:\n  description: {{ .Values.description }}\n\n# Safer:\nannotations:\n  description: {{ .Values.description | quote }}',
    tags: ['kubernetes', 'helm', 'security', 'injection'],
  },
  {
    id: 'k8s-helm-l3-065',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How does Kustomize handle cross-cutting concerns like adding a label to all resources including their selectors?',
    options: [
      'commonLabels adds labels to metadata only',
      'commonLabels adds labels to metadata, selectors, and template metadata automatically',
      'You must manually patch each selector',
      'Labels and selectors are independent in Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'commonLabels in Kustomize adds the specified labels not only to metadata.labels but also to spec.selector.matchLabels and spec.template.metadata.labels.',
    tags: ['kubernetes', 'kustomize', 'labels', 'selectors'],
  },
  {
    id: 'k8s-helm-l3-066',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'What is the danger of using commonLabels with existing Deployments in Kustomize?',
    options: [
      'There is no danger',
      'It can change immutable selector labels, causing Deployment update failures',
      'It only affects new resources',
      'It removes existing labels',
    ],
    correctAnswer: 1,
    explanation: 'Adding commonLabels modifies selector.matchLabels, which is immutable on existing Deployments. This causes the apply to fail because the selector cannot be changed after creation.',
    tags: ['kubernetes', 'kustomize', 'labels', 'pitfalls'],
  },
  {
    id: 'k8s-helm-l3-067',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'oci-registries',
    question: 'What are the advantages of using OCI registries for Helm charts over traditional chart repositories?',
    options: [
      'OCI registries are faster',
      'OCI registries provide unified artifact storage, support authentication, content-addressable storage, and eliminate the need for a separate index.yaml',
      'OCI registries are free while chart repos are not',
      'OCI registries support more chart formats',
    ],
    correctAnswer: 1,
    explanation: 'OCI registries unify chart and image storage, leverage existing registry auth, use content-addressable storage for integrity, and do not need the index.yaml file.',
    tags: ['kubernetes', 'helm', 'oci', 'advantages'],
  },
  {
    id: 'k8s-helm-l3-068',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement feature flags in a Helm chart?',
    options: [
      'Use Kubernetes feature gates',
      'Use boolean values in values.yaml with conditional template blocks for each feature',
      'Use environment variables only',
      'Feature flags require a separate service',
    ],
    correctAnswer: 1,
    explanation: 'Feature flags are implemented as boolean values in values.yaml, with if/else blocks in templates that conditionally include resources or configuration based on these flags.',
    codeSnippet: '# values.yaml\nfeatures:\n  monitoring: true\n  autoscaling: false\n  ingress: true\n\n# template:\n{{- if .Values.features.autoscaling }}\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\n...\n{{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'feature-flags'],
  },
  {
    id: 'k8s-helm-l3-069',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you perform a Helm upgrade with zero downtime?',
    options: [
      'Use helm upgrade; it is always zero-downtime',
      'Ensure proper rolling update strategy, readiness probes, PDBs, and use --wait with --timeout',
      'Use --atomic flag only',
      'Zero downtime is not possible with Helm',
    ],
    correctAnswer: 1,
    explanation: 'Zero-downtime upgrades require proper Deployment strategy (RollingUpdate), readiness probes, PodDisruptionBudgets, and using --wait to ensure new pods are ready before proceeding.',
    tags: ['kubernetes', 'helm', 'upgrade', 'zero-downtime'],
  },
  {
    id: 'k8s-helm-l3-070',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the purpose of using `deepCopy` in Helm templates?',
    options: [
      'Creates a backup of the values file',
      'Creates a deep copy of a dict to prevent modifying the original when using set or merge',
      'Copies files from the chart to the cluster',
      'Duplicates a Kubernetes resource',
    ],
    correctAnswer: 1,
    explanation: 'The `deepCopy` function creates an independent copy of a dictionary, preventing accidental mutations of the original data when modifying the copy.',
    codeSnippet: '{{- $copy := deepCopy .Values.defaults }}\n{{- $_ := set $copy "replicas" 5 }}\n# .Values.defaults.replicas is unchanged',
    tags: ['kubernetes', 'helm', 'templates', 'deepCopy'],
  },
  {
    id: 'k8s-helm-l3-071',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use Kustomize to add a toleration to all Deployments?',
    options: [
      'Use commonTolerations in kustomization.yaml',
      'Write a patch targeting all Deployments that adds the toleration',
      'Tolerations can only be added per-resource',
      'Use a PodPreset instead',
    ],
    correctAnswer: 1,
    explanation: 'There is no commonTolerations field. You must use a patch (strategic merge or JSON 6902) targeting Deployments to add tolerations.',
    codeSnippet: 'patches:\n  - target:\n      kind: Deployment\n    patch: |-\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: not-important\n      spec:\n        template:\n          spec:\n            tolerations:\n              - key: "dedicated"\n                operator: "Equal"\n                value: "app"\n                effect: "NoSchedule"',
    tags: ['kubernetes', 'kustomize', 'patches', 'tolerations'],
  },
  {
    id: 'k8s-helm-l3-072',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How does Helmfile handle hooks that need to run before or after all releases?',
    options: [
      'Helmfile does not support hooks',
      'Use helmfile hooks with events like presync, postsync, prepare, and cleanup',
      'Use Helm hooks for each release individually',
      'Use shell scripts outside of Helmfile',
    ],
    correctAnswer: 1,
    explanation: 'Helmfile supports its own hook system with events like presync, postsync, prepare, and cleanup that can run commands before or after sync operations.',
    codeSnippet: 'releases:\n  - name: my-app\n    chart: ./charts/my-app\n    hooks:\n      - events: [\"presync\"]\n        showlogs: true\n        command: \"./scripts/check-prerequisites.sh\"\n      - events: [\"postsync\"]\n        command: \"./scripts/run-smoke-tests.sh\"',
    tags: ['kubernetes', 'helmfile', 'hooks'],
  },
  {
    id: 'k8s-helm-l3-073',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you prevent Helm from managing certain fields of a resource during upgrades?',
    options: [
      'Use kubectl annotate to mark fields as unmanaged',
      'Fields managed by other controllers (like HPA managing replicas) should be excluded from templates or controlled via values',
      'Use the --skip-fields flag',
      'All fields in templates are always managed by Helm',
    ],
    correctAnswer: 1,
    explanation: 'When other controllers manage certain fields (e.g., HPA managing replicas), those fields should be conditionally excluded from templates to avoid conflicts.',
    codeSnippet: 'spec:\n  {{- if not .Values.autoscaling.enabled }}\n  replicas: {{ .Values.replicaCount }}\n  {{- end }}',
    tags: ['kubernetes', 'helm', 'templates', 'field-management'],
  },
  {
    id: 'k8s-helm-l3-074',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'What is the significance of Helm release namespacing in v3?',
    options: [
      'Release names are unique globally across all namespaces',
      'Release names are scoped to their namespace, allowing the same name in different namespaces',
      'Namespaces are ignored in Helm v3',
      'Each release creates its own namespace automatically',
    ],
    correctAnswer: 1,
    explanation: 'In Helm v3, releases are namespace-scoped. The same release name can exist in different namespaces, unlike v2 where names were globally unique.',
    tags: ['kubernetes', 'helm', 'release', 'namespaces'],
  },
  {
    id: 'k8s-helm-l3-075',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize vars (now deprecated) and what replaced them?',
    options: [
      'Vars are still the recommended approach',
      'Vars allowed referencing field values across resources; they are replaced by the replacements transformer',
      'Vars were replaced by patches',
      'Vars were replaced by configMapGenerator',
    ],
    correctAnswer: 1,
    explanation: 'Kustomize vars allowed cross-resource value references but are deprecated. The replacements transformer provides a more powerful and flexible alternative.',
    codeSnippet: '# New approach using replacements:\nreplacements:\n  - source:\n      kind: ConfigMap\n      name: my-config\n      fieldPath: data.DATABASE_HOST\n    targets:\n      - select:\n          kind: Deployment\n          name: my-app\n        fieldPaths:\n          - spec.template.spec.containers.[name=app].env.[name=DB_HOST].value',
    tags: ['kubernetes', 'kustomize', 'replacements', 'vars'],
  },
  {
    id: 'k8s-helm-l3-076',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you test Helm chart templates using unit tests?',
    options: [
      'Helm charts cannot be unit tested',
      'Use helm-unittest plugin to write YAML-based test cases for template output',
      'Use kubectl dry-run as the test framework',
      'Write Go tests that import the chart package',
    ],
    correctAnswer: 1,
    explanation: 'The helm-unittest plugin allows writing YAML-based test suites that assert on the rendered template output for different value combinations.',
    codeSnippet: '# tests/deployment_test.yaml\nsuite: test deployment\ntemplates:\n  - templates/deployment.yaml\ntests:\n  - it: should set replicas from values\n    set:\n      replicaCount: 5\n    asserts:\n      - equal:\n          path: spec.replicas\n          value: 5\n  - it: should fail without image\n    set:\n      image.repository: null\n    asserts:\n      - failedTemplate: {}',
    tags: ['kubernetes', 'helm', 'testing', 'unittest'],
  },
  {
    id: 'k8s-helm-l3-077',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'What does helmfile template do?',
    options: [
      'Creates a new helmfile.yaml template',
      'Renders all Helm charts locally and outputs the combined YAML manifests',
      'Generates Go templates from helmfile.yaml',
      'Creates template files in each chart directory',
    ],
    correctAnswer: 1,
    explanation: 'The `helmfile template` command renders all Helm charts defined in the helmfile locally and outputs the combined YAML, useful for review and GitOps.',
    codeSnippet: '# Render all templates:\nhelmfile -e production template\n\n# Render specific releases:\nhelmfile -l app=nginx template > rendered.yaml',
    tags: ['kubernetes', 'helmfile', 'template'],
  },
  {
    id: 'k8s-helm-l3-078',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'dependencies',
    question: 'How do you override a subchart template from the parent chart?',
    options: [
      'Copy the template to the parent chart templates directory with the same define name',
      'Use a template-override field in Chart.yaml',
      'Override templates via values.yaml',
      'Subchart templates cannot be overridden',
    ],
    correctAnswer: 0,
    explanation: 'Since named templates are global, defining a template with the same name in the parent chart overrides the subchart definition because the parent templates are loaded last.',
    tags: ['kubernetes', 'helm', 'dependencies', 'templates', 'override'],
  },
  {
    id: 'k8s-helm-l3-079',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle Helm release secrets that have grown too large?',
    options: [
      'Increase the etcd storage limit',
      'Use --max-history to limit revisions, or switch to SQL storage driver',
      'Compress the release data',
      'Split into multiple releases',
    ],
    correctAnswer: 1,
    explanation: 'Helm release Secrets can grow large with many revisions. Limiting --max-history reduces stored revisions. For very large environments, the SQL storage driver avoids etcd size limits.',
    tags: ['kubernetes', 'helm', 'release', 'storage', 'limits'],
  },
  {
    id: 'k8s-helm-l3-080',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you validate Kustomize output before applying?',
    options: [
      'Kustomize automatically validates output',
      'Use kubectl kustomize to render, then pipe to kubeval, kubeconform, or kubectl apply --dry-run=server',
      'Use the --validate flag with kubectl apply -k',
      'Validation is not possible with Kustomize',
    ],
    correctAnswer: 1,
    explanation: 'Best practice is to render with `kubectl kustomize` and validate the output using tools like kubeconform or server-side dry-run.',
    codeSnippet: '# Render and validate:\nkubectl kustomize overlays/prod | kubeconform -strict\n\n# Or server-side dry-run:\nkubectl apply -k overlays/prod --dry-run=server',
    tags: ['kubernetes', 'kustomize', 'validation'],
  },
  {
    id: 'k8s-helm-l3-081',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'What is the Helm post-renderer pattern with Kustomize?',
    options: [
      'Rendering Kustomize after Helm, allowing patches on Helm-rendered output',
      'Using Kustomize to pre-process values before Helm',
      'Converting Helm charts to Kustomize format',
      'Running Helm after Kustomize for validation',
    ],
    correctAnswer: 0,
    explanation: 'The post-renderer pattern runs Kustomize on Helm-rendered output, allowing you to apply Kustomize patches to any Helm chart without modifying it.',
    codeSnippet: '#!/bin/bash\n# kustomize-post-renderer.sh\ncat <&0 > kustomize/all.yaml\nkustomize build kustomize/\n\n# Usage:\nhelm upgrade --install my-release my-chart \\\n  --post-renderer ./kustomize-post-renderer.sh',
    tags: ['kubernetes', 'helm', 'kustomize', 'post-renderer'],
  },
  {
    id: 'k8s-helm-l3-082',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you handle a Helm chart that needs to delete a previously created resource on upgrade?',
    options: [
      'Helm automatically deletes resources removed from templates',
      'Helm uses a three-way merge; resources removed from templates are deleted if they were in the previous release',
      'Removed resources must be deleted manually',
      'Use a pre-upgrade hook to delete the resource',
    ],
    correctAnswer: 1,
    explanation: 'Helm v3 uses a three-way strategic merge: it compares old manifest, new manifest, and live state. Resources present in the old manifest but not in the new one are deleted.',
    tags: ['kubernetes', 'helm', 'upgrade', 'three-way-merge'],
  },
  {
    id: 'k8s-helm-l3-083',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize-patches',
    question: 'How do you use Kustomize to add init containers to all Deployments?',
    options: [
      'Use commonInitContainers in kustomization.yaml',
      'Write a strategic merge patch or component that adds the init container to all Deployments',
      'Init containers cannot be added via Kustomize',
      'Use a mutating webhook instead',
    ],
    correctAnswer: 1,
    explanation: 'A strategic merge patch targeting all Deployments can add init containers to the pod spec.',
    codeSnippet: 'patches:\n  - target:\n      kind: Deployment\n    patch: |-\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: irrelevant\n      spec:\n        template:\n          spec:\n            initContainers:\n              - name: wait-for-db\n                image: busybox\n                command: [\'sh\', \'-c\', \'until nc -z db 5432; do sleep 1; done\']',
    tags: ['kubernetes', 'kustomize', 'patches', 'init-containers'],
  },
  {
    id: 'k8s-helm-l3-084',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'helmfile',
    question: 'How do you use Helmfile with ArgoCD?',
    options: [
      'ArgoCD natively supports Helmfile',
      'Use the argocd-helmfile plugin or render helmfile output and commit to Git',
      'Helmfile and ArgoCD are incompatible',
      'Use helmfile sync inside ArgoCD hooks',
    ],
    correctAnswer: 1,
    explanation: 'ArgoCD does not natively support Helmfile, but you can use a config management plugin (argocd-helmfile) or render helmfile output to plain manifests committed to Git.',
    tags: ['kubernetes', 'helmfile', 'argocd', 'gitops'],
  },
  {
    id: 'k8s-helm-l3-085',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'templates',
    question: 'How do you implement conditional dependency installation in a Helm chart?',
    options: [
      'Dependencies are always installed',
      'Use the condition or tags field in Chart.yaml dependencies with corresponding values toggles',
      'Remove dependencies from Chart.yaml at build time',
      'Use if blocks in Chart.yaml',
    ],
    correctAnswer: 1,
    explanation: 'The condition field references a boolean value path, and tags allow grouping dependencies. Both can be toggled via values to enable or disable dependencies.',
    codeSnippet: '# Chart.yaml\ndependencies:\n  - name: redis\n    version: 17.0.0\n    repository: https://charts.bitnami.com/bitnami\n    condition: redis.enabled\n    tags:\n      - caching\n\n# values.yaml\nredis:\n  enabled: false  # disable redis dependency\ntags:\n  caching: false   # alternative via tags',
    tags: ['kubernetes', 'helm', 'dependencies', 'conditional'],
  },
  {
    id: 'k8s-helm-l3-086',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'release-management',
    question: 'How do you track which Helm chart version is deployed in each environment?',
    options: [
      'Check helm list in each cluster manually',
      'Use helm get metadata, GitOps state tracking, or a deployment dashboard that queries release metadata',
      'Helm does not track chart versions',
      'Check the Docker image tags',
    ],
    correctAnswer: 1,
    explanation: 'Release metadata includes the chart version. Combining helm list across clusters, GitOps state (ArgoCD dashboard), or custom tooling provides environment tracking.',
    codeSnippet: '# Check deployed chart version:\nhelm list -A -o json | jq \'.[] | {name, namespace, chart, app_version}\'',
    tags: ['kubernetes', 'helm', 'release', 'tracking'],
  },
  {
    id: 'k8s-helm-l3-087',
    language: 'kubernetes',
    level: 'level3',
    category: 'Helm & Packaging',
    subcategory: 'kustomize',
    question: 'How do you use Kustomize to manage CRDs alongside their instances?',
    options: [
      'Put CRDs and instances in the same base',
      'Use a two-phase approach: separate bases for CRDs and instances, apply CRDs first',
      'Kustomize cannot manage CRDs',
      'Use Helm for CRDs and Kustomize for instances',
    ],
    correctAnswer: 1,
    explanation: 'Best practice is to separate CRDs into their own kustomization and apply them first, then apply instances in a second phase to avoid ordering issues.',
    codeSnippet: '# Apply CRDs first:\nkubectl apply -k crds/\n\n# Then apply instances:\nkubectl apply -k overlays/prod/\n\n# crds/kustomization.yaml\nresources:\n  - my-crd.yaml\n\n# overlays/prod/kustomization.yaml\nresources:\n  - ../../base  # contains CR instances',
    tags: ['kubernetes', 'kustomize', 'crds', 'ordering'],
  },
];

export default questions;
