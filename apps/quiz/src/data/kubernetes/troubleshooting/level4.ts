import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-troubleshooting-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container using seccomp profile RuntimeDefault crashes with SIGSYS (exit code 159). What is happening?',
    options: [
      'The container ran out of memory',
      'The application is making a system call that is blocked by the seccomp profile, and the default action is to send SIGSYS',
      'The kernel version is incompatible',
      'The container runtime is misconfigured',
    ],
    correctAnswer: 1,
    explanation: 'SIGSYS (exit code 159 = 128 + 31) is sent when a blocked syscall is attempted under a seccomp filter. The RuntimeDefault profile blocks dangerous syscalls; custom profiles may need adjustments.',
    codeSnippet: '# Identify the blocked syscall using strace\nkubectl debug -it my-pod --image=ubuntu --target=my-app\nstrace -f -p 1 2>&1 | grep SIGSYS\n\n# Or create a permissive audit profile first\nsecurityContext:\n  seccompProfile:\n    type: Localhost\n    localhostProfile: profiles/audit.json\n# audit.json logs blocked syscalls without killing',
    tags: ['kubernetes', 'seccomp', 'sigsys', 'syscalls', 'security'],
  },
  {
    id: 'k8s-troubleshooting-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'In an IPVS-based kube-proxy setup, you notice that traffic to a Service is not being load balanced. All connections go to one backend pod. What is the likely cause?',
    options: [
      'IPVS does not support load balancing',
      'The IPVS scheduler algorithm is set to source hashing (sh), which pins connections from the same source IP to the same backend; or the conntrack table has stale entries',
      'The Service selector is wrong',
      'DNS caching is causing the issue',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check IPVS scheduler algorithm\nipvsadm -Ln\n# TCP  10.96.0.100:80 rr   <- should be round-robin (rr)\n#   -> 10.244.1.5:8080  Masq  1  0  0\n#   -> 10.244.2.3:8080  Masq  1  0  0\n\n# If using "sh" (source hashing), connections from same IP\n# always go to same backend\n\n# Change kube-proxy config:\n# scheduler: "rr"  (round-robin, default)\n\n# Also check for stale conntrack entries:\nconntrack -L -d <service-ip> -p tcp --dport 80',
    explanation: 'IPVS supports multiple scheduling algorithms. Source hashing (sh) causes all connections from one source to hit the same backend. Stale conntrack entries can also pin connections.',
    tags: ['kubernetes', 'ipvs', 'load-balancing', 'scheduler', 'conntrack'],
  },
  {
    id: 'k8s-troubleshooting-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'CoreDNS pods are in a CrashLoopBackOff with "Loop detected" in logs on a newly provisioned cluster. What causes this and how do you fix it?',
    options: [
      'CoreDNS has too many plugins',
      'The node /etc/resolv.conf points to the CoreDNS Service IP (127.0.0.1 or kube-dns IP), creating a DNS forwarding loop; fix by pointing forward to real upstream DNS',
      'The cluster CIDR overlaps with DNS IP range',
      'CoreDNS needs more memory',
    ],
    correctAnswer: 1,
    codeSnippet: '# The loop plugin detects when CoreDNS forwards to itself\n# Node /etc/resolv.conf has:\n# nameserver 127.0.0.53  # systemd-resolved -> loops back\n\n# Fix Option 1: Point forward to real DNS\n.:53 {\n    forward . 8.8.8.8 8.8.4.4  # Instead of /etc/resolv.conf\n}\n\n# Fix Option 2: Disable systemd-resolved loop\n# On the node: /etc/systemd/resolved.conf\n# DNS=8.8.8.8\n# Then: systemctl restart systemd-resolved\n\n# Fix Option 3: Remove loop plugin (not recommended)',
    explanation: 'On systems using systemd-resolved, /etc/resolv.conf may point to 127.0.0.53 which resolves via the local DNS stub. CoreDNS forwards there, which routes back to CoreDNS, creating a loop.',
    tags: ['kubernetes', 'coredns', 'dns-loop', 'systemd-resolved', 'forward'],
  },
  {
    id: 'k8s-troubleshooting-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container with memory limit 2Gi is OOMKilled, but the application profiler shows only 800Mi heap usage. The kernel log shows "Memory cgroup out of memory: Killed process" and "oom-kill:constraint=CONSTRAINT_MEMCG". What specific mechanism triggered this?',
    options: [
      'The node ran out of memory',
      'The container cgroup memory limit was reached, including kernel memory (kmem) charged to the cgroup such as slab caches, page tables, and socket buffers',
      'The swap was disabled',
      'The profiler is inaccurate',
    ],
    correctAnswer: 1,
    codeSnippet: '# CONSTRAINT_MEMCG = cgroup memory constraint\n# Check kernel memory charged to cgroup:\ncat /sys/fs/cgroup/memory/kubepods/pod<uid>/memory.kmem.usage_in_bytes\n\n# Check memory breakdown:\ncat /sys/fs/cgroup/memory/kubepods/pod<uid>/memory.stat\n# cache: file-backed pages\n# rss: anonymous memory\n# kernel_stack: kernel stack usage\n# slab: slab allocator usage\n# sock: socket buffer memory\n\n# Total = rss + cache + kernel memory\n# This total is what triggers OOM',
    explanation: 'CONSTRAINT_MEMCG confirms the OOM was triggered by the cgroup limit, not system memory. Kernel memory (slab, page tables, socket buffers) is charged to the cgroup and contributes to the total.',
    tags: ['kubernetes', 'oomkilled', 'cgroup', 'kernel-memory', 'constraint-memcg'],
  },
  {
    id: 'k8s-troubleshooting-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'The scheduler extender returns a filter error for all nodes when scheduling a specific pod. The extender is not reachable. How does the scheduler handle this?',
    options: [
      'The scheduler ignores the extender and schedules the pod normally',
      'Behavior depends on the extender configuration: if ignorable is true, the scheduler proceeds without it; if false (default), the pod remains unschedulable',
      'The scheduler crashes and restarts',
      'The pod is immediately rejected',
    ],
    correctAnswer: 1,
    codeSnippet: '# Scheduler extender configuration\napiVersion: kubescheduler.config.k8s.io/v1\nkind: KubeSchedulerConfiguration\nextenders:\n- urlPrefix: "http://my-extender:8080"\n  filterVerb: "filter"\n  weight: 5\n  ignorable: false  # If true, scheduler proceeds when extender fails\n  # Default: false -> pod stays Pending if extender unreachable\n  managedResources:\n  - name: example.com/gpu\n    ignoredByScheduler: false',
    explanation: 'Scheduler extenders have an ignorable field. When false (default), extender failures prevent scheduling. When true, the scheduler skips the extender and proceeds with built-in logic.',
    tags: ['kubernetes', 'scheduler-extender', 'pending', 'ignorable'],
  },
  {
    id: 'k8s-troubleshooting-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A mutating webhook fails open (failurePolicy: Ignore) but requests are still being rejected. RBAC is correct. What could cause this?',
    options: [
      'The failurePolicy is not working',
      'A validating webhook (running after mutating webhooks) is rejecting the modified resource, or the API server admission controller chain has other rejecting plugins',
      'Mutating webhooks cannot fail open',
      'The service account token expired',
    ],
    correctAnswer: 1,
    codeSnippet: '# Admission flow:\n# 1. Authentication\n# 2. Authorization (RBAC)\n# 3. Mutating Admission Webhooks\n# 4. Object Schema Validation\n# 5. Validating Admission Webhooks\n# 6. Persist to etcd\n\n# Check all webhooks:\nkubectl get mutatingwebhookconfigurations\nkubectl get validatingwebhookconfigurations\n\n# Check API server audit logs for rejection source\n# The audit log shows which admission plugin rejected',
    explanation: 'The Kubernetes admission chain has multiple stages. Even if a mutating webhook fails open, validating webhooks, built-in admission plugins (LimitRanger, PodSecurity), or schema validation can still reject.',
    tags: ['kubernetes', 'webhook', 'admission', 'failurepolicy', 'validating'],
  },
  {
    id: 'k8s-troubleshooting-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service running in IPVS mode with externalTrafficPolicy: Local shows correct IPVS rules but external clients get connection timeouts on some nodes. kube-proxy logs show no errors. What is the issue?',
    options: [
      'IPVS does not support externalTrafficPolicy: Local',
      'IPVS requires proper handling of the "kube-ipvs0" dummy interface and the node may be missing ARP/NDP entries or the load balancer health checks are not detecting nodes without local pods',
      'The Service port is blocked by a firewall',
      'The IPVS scheduler is wrong',
    ],
    correctAnswer: 1,
    codeSnippet: '# IPVS mode creates a dummy interface kube-ipvs0\n# with all Service ClusterIPs bound to it\nip addr show kube-ipvs0\n\n# With externalTrafficPolicy: Local:\n# - Traffic must be health-checked per node\n# - Nodes without pods should NOT receive traffic\n# - Check cloud LB health check path:\n#   /healthz on NodePort for the Service\n\n# Verify health check endpoint\ncurl http://<node-ip>:<health-check-nodeport>/healthz\n# Returns 200 if local endpoints exist, 503 otherwise',
    explanation: 'With IPVS and externalTrafficPolicy: Local, the cloud load balancer must use health checks to route only to nodes with local endpoints. Missing health checks cause timeouts on nodes without pods.',
    tags: ['kubernetes', 'ipvs', 'externaltrafficpolicy', 'health-check', 'kube-ipvs0'],
  },
  {
    id: 'k8s-troubleshooting-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'A node spontaneously reboots and pods are rescheduled, but the kubelet journal shows "OOM" before the reboot. Container memory limits were respected. What happened?',
    options: [
      'The pod memory limits were too high',
      'The system reserved and kube reserved memory was insufficient, causing kubelet/system processes to OOM, which triggered the kernel OOM killer at the system level',
      'The container runtime crashed',
      'etcd ran out of memory',
    ],
    correctAnswer: 1,
    codeSnippet: '# System-level OOM (not cgroup-constrained)\n# Kernel log: "Out of memory: Kill process <kubelet-pid>"\n\n# Prevention: Configure kubelet resource reservations\n--system-reserved=memory=1Gi,cpu=500m\n--kube-reserved=memory=1Gi,cpu=500m\n--eviction-hard=memory.available<500Mi\n\n# Allocatable = Capacity - system-reserved - kube-reserved - eviction-threshold\n# This ensures the kubelet and OS always have enough memory\n\n# Also protect kubelet from OOM:\n# /etc/systemd/system/kubelet.service.d/override.conf\n# [Service]\n# OOMScoreAdjust=-999',
    explanation: 'If system-reserved and kube-reserved are not configured, pods can consume all node memory, starving the kubelet and OS. The system OOM killer then kills critical processes, potentially causing a node reboot.',
    tags: ['kubernetes', 'node-oom', 'system-reserved', 'kube-reserved', 'reboot'],
  },
  {
    id: 'k8s-troubleshooting-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A CSI driver-provisioned volume shows as "attached" in VolumeAttachment but the pod sees an empty directory at the mount point. What is happening at the CSI level?',
    options: [
      'The volume is corrupted',
      'The CSI NodePublishVolume (mount) call failed after ControllerPublishVolume (attach) succeeded; check kubelet logs for mount errors at the node stage',
      'The PV size is wrong',
      'The container runtime cannot see the mount',
    ],
    correctAnswer: 1,
    codeSnippet: '# CSI volume lifecycle:\n# 1. CreateVolume (Provisioner)\n# 2. ControllerPublishVolume (Attach to node)  <- succeeded\n# 3. NodeStageVolume (Mount to staging dir)     <- may fail\n# 4. NodePublishVolume (Mount to pod dir)       <- may fail\n\n# Check kubelet logs for CSI errors:\njournalctl -u kubelet | grep -i "NodeStageVolume\\|NodePublishVolume"\n\n# Check CSI driver pod logs:\nkubectl logs -n kube-system <csi-node-pod> -c <driver-container>\n\n# Verify mount on node:\nmount | grep <pv-name>\nls /var/lib/kubelet/pods/<pod-uid>/volumes/',
    explanation: 'CSI volume operations happen in stages. ControllerPublishVolume (attach) can succeed while NodeStageVolume or NodePublishVolume (mount) fails. Check kubelet and CSI driver logs for mount-stage errors.',
    tags: ['kubernetes', 'csi', 'volume-mount', 'node-publish', 'stages'],
  },
  {
    id: 'k8s-troubleshooting-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'Liveness probes are failing for all pods on a specific node, but the applications are healthy. Other nodes are fine. What node-level issue could cause this?',
    options: [
      'The node has a NetworkPolicy blocking probes',
      'The kubelet on that node is under heavy load or experiencing PLEG latency, causing it to miss probe deadlines even though the containers respond correctly',
      'The container runtime on that node uses a different probe protocol',
      'The node has a different DNS configuration',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check kubelet metrics on the affected node\ncurl -s http://localhost:10255/metrics | grep -E \\\n  "kubelet_pleg_relist_duration|kubelet_runtime_operations_duration"\n\n# Check probe execution latency\ncurl -s http://localhost:10255/metrics | grep prober\n\n# Common causes:\n# - Container runtime (containerd) overloaded\n# - High disk I/O causing PLEG delays\n# - Too many containers on the node\n# - Kubelet garbage collection consuming CPU',
    explanation: 'The kubelet executes probes. If the kubelet is overloaded (PLEG latency, high container count, I/O issues), it may not execute probes within their timeout, causing false failures.',
    tags: ['kubernetes', 'probes', 'kubelet-overload', 'pleg', 'node-specific'],
  },
  {
    id: 'k8s-troubleshooting-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container is crashing with SIGBUS (exit code 135). It uses memory-mapped files from a projected volume. What is the root cause?',
    options: [
      'The container has insufficient memory',
      'SIGBUS with memory-mapped files indicates the underlying file was truncated or removed while still mapped; projected volumes (ConfigMap/Secret) can be atomically swapped, invalidating mapped regions',
      'The kernel does not support memory mapping',
      'The volume permissions are wrong',
    ],
    correctAnswer: 1,
    codeSnippet: '# SIGBUS (exit code 135 = 128 + 7) with mmap files:\n# The file backing the mmap was removed/changed\n\n# Projected volumes use atomic symlink swap:\n# /projected-volume/ -> ..data -> ..2024_01_01/\n# When ConfigMap updates:\n# /projected-volume/ -> ..data -> ..2024_01_02/\n# Old directory is deleted -> mmap references invalid pages\n\n# Fix: Don\'t mmap projected volume files\n# Or copy files to emptyDir before mmapping\ninitContainers:\n- name: copy-config\n  command: [\'cp\', \'/projected/config\', \'/data/config\']\n  volumeMounts:\n  - name: projected\n    mountPath: /projected\n  - name: data\n    mountPath: /data',
    explanation: 'Projected volumes (ConfigMap, Secret) use atomic symlink swaps on update. If a process has memory-mapped a file and the volume updates, the backing file is removed, causing SIGBUS when accessing mapped pages.',
    tags: ['kubernetes', 'sigbus', 'mmap', 'projected-volume', 'configmap'],
  },
  {
    id: 'k8s-troubleshooting-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'After enabling the eBPF dataplane in Cilium CNI (replacing kube-proxy), pods report intermittent "connection refused" to Services. Standard kube-proxy iptables rules are removed. What should you investigate?',
    options: [
      'Re-enable kube-proxy alongside Cilium',
      'Check Cilium eBPF maps for correct Service entries, verify Cilium agent health on all nodes, and inspect BPF program attachment points with cilium bpf commands',
      'The CNI does not support Services',
      'IPVS is conflicting with eBPF',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check Cilium service map\ncilium service list\n\n# Check BPF maps for the service\ncilium bpf lb list\n\n# Verify Cilium agent status on all nodes\nkubectl exec -n kube-system <cilium-pod> -- cilium status\n\n# Check for packet drops\ncilium monitor --type drop\n\n# Verify BPF programs are attached\ncilium bpf nat list\ntc filter show dev eth0 ingress',
    explanation: 'With eBPF dataplane, Cilium replaces kube-proxy iptables with BPF programs for Service routing. If BPF maps are inconsistent or the Cilium agent is unhealthy, Service routing fails.',
    tags: ['kubernetes', 'cilium', 'ebpf', 'service-routing', 'bpf-maps'],
  },
  {
    id: 'k8s-troubleshooting-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'CoreDNS returns SERVFAIL for a specific internal domain. The Corefile has a forward entry for that domain to an internal DNS server. CoreDNS logs show "i/o timeout". The internal DNS server is reachable from the node. What is happening?',
    options: [
      'CoreDNS does not support forwarding',
      'CoreDNS pods are in a different network namespace than the node; the NetworkPolicy or pod network routing may not reach the internal DNS server, even though the node can reach it',
      'The domain name is too long',
      'CoreDNS needs a restart',
    ],
    correctAnswer: 1,
    codeSnippet: '# Test from CoreDNS pod vs from node\n# From node (works):\ndig @10.0.0.53 internal.example.com\n\n# From CoreDNS pod (fails):\nkubectl exec -n kube-system <coredns-pod> -- \\\n  dig @10.0.0.53 internal.example.com\n\n# Possible causes:\n# 1. NetworkPolicy blocking CoreDNS egress to 10.0.0.53\n# 2. Pod network cannot route to 10.0.0.53\n# 3. The internal DNS is on a VPN/VLAN not routable from pod CIDR\n\n# Fix: Ensure pod network can reach the DNS server\n# Or use hostNetwork: true for CoreDNS (not recommended)',
    explanation: 'Pods have a different network namespace than the host. Internal DNS servers reachable from the node may not be reachable from the pod network due to routing, firewall, or NetworkPolicy restrictions.',
    tags: ['kubernetes', 'coredns', 'forwarding', 'network-namespace', 'routing'],
  },
  {
    id: 'k8s-troubleshooting-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container using cgroup v2 has memory.max set to 1Gi and memory.high set to 800Mi. The application slows down significantly when approaching 800Mi but is not killed. Why?',
    options: [
      'memory.high is a soft limit that triggers throttling',
      'memory.high in cgroup v2 triggers memory pressure throttling (reclaim) when exceeded, slowing the process; the OOM kill only happens at memory.max; Kubernetes sets memory.high when using memory QoS (alpha feature)',
      'The application has a memory leak',
      'The cgroup v2 controller is broken',
    ],
    correctAnswer: 1,
    codeSnippet: '# cgroup v2 memory controls:\n# memory.max  = hard limit (OOM kill at this point)\n# memory.high = high watermark (throttle/reclaim above this)\n# memory.low  = low watermark (protected from reclaim below this)\n# memory.min  = absolute minimum (guaranteed)\n\n# Kubernetes MemoryQoS (alpha in 1.22+):\n# When enabled, Kubernetes sets:\n# memory.max = limits.memory\n# memory.high = requests.memory (for Burstable)\n# This causes throttling between request and limit\n\n# Check cgroup v2 settings:\ncat /sys/fs/cgroup/kubepods.slice/kubepods-pod<uid>.slice/memory.max\ncat /sys/fs/cgroup/kubepods.slice/kubepods-pod<uid>.slice/memory.high',
    explanation: 'In cgroup v2, memory.high is a throttling point. When memory usage exceeds it, the kernel aggressively reclaims memory, slowing the process. This is different from the hard OOM kill at memory.max.',
    tags: ['kubernetes', 'cgroup-v2', 'memory-high', 'throttling', 'memory-qos'],
  },
  {
    id: 'k8s-troubleshooting-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A node periodically evicts pods even though resources appear available. The kubelet logs show "eviction manager: node conditions: [DiskPressure]" but df shows 30% disk free. What is the discrepancy?',
    options: [
      'The kubelet is using stale data',
      'The kubelet considers imagefs (container runtime storage) separately from nodefs (root filesystem); the imagefs partition may be under pressure even if the root partition has space',
      'The eviction threshold is set too high',
      'The df command is incorrect',
    ],
    correctAnswer: 1,
    codeSnippet: '# Kubelet tracks two filesystems:\n# nodefs: root filesystem (/)\n# imagefs: container runtime storage (/var/lib/containerd)\n\n# Check both:\ndf -h /                    # nodefs\ndf -h /var/lib/containerd  # imagefs (may be separate)\n\n# Kubelet eviction thresholds apply to both:\n--eviction-hard=\\\n  nodefs.available<10%,\\\n  nodefs.inodesFree<5%,\\\n  imagefs.available<15%,\\\n  imagefs.inodesFree<10%\n\n# Check which filesystem triggered eviction:\njournalctl -u kubelet | grep "eviction manager"',
    explanation: 'The kubelet tracks nodefs and imagefs independently. If container runtime storage is on a separate partition, it may run low on space while the root filesystem appears healthy.',
    tags: ['kubernetes', 'eviction', 'imagefs', 'nodefs', 'disk-pressure'],
  },
  {
    id: 'k8s-troubleshooting-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'A pod with a PriorityClass value of 1000000 is still being evicted. Another pod with PriorityClass value of 900000 survives. What could explain this?',
    options: [
      'Higher priority values do not guarantee survival',
      'The evicted pod has Burstable QoS while the surviving pod has Guaranteed QoS; under memory pressure, QoS class is considered alongside priority for eviction ordering',
      'PriorityClass values are random',
      'The eviction controller ignores priority',
    ],
    correctAnswer: 1,
    codeSnippet: '# Kubelet eviction ordering:\n# 1. BestEffort/Burstable pods exceeding requests (by how much)\n# 2. Priority is a tiebreaker within the same QoS class\n# 3. Pods using more resources relative to requests\n#    are evicted before pods using less\n\n# So a Burstable pod (priority 1000000) using 2x its request\n# CAN be evicted before a Guaranteed pod (priority 900000)\n\n# Best practice for critical pods:\n# - Use high PriorityClass AND Guaranteed QoS\n# - Set requests = limits for both CPU and memory',
    explanation: 'The kubelet eviction algorithm considers QoS class, resource usage relative to requests, AND priority. A high-priority Burstable pod exceeding requests can be evicted before a lower-priority Guaranteed pod.',
    tags: ['kubernetes', 'eviction', 'priority', 'qos', 'ordering'],
  },
  {
    id: 'k8s-troubleshooting-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You need to trace kernel-level syscalls from a container in production without modifying the pod spec. What approach uses Kubernetes-native capabilities?',
    options: [
      'SSH into the node and use strace on the host',
      'Use kubectl debug with a privileged ephemeral container that has SYS_PTRACE capability and share the PID namespace to strace the target process',
      'Deploy a DaemonSet with eBPF tracing',
      'Use kubectl exec to install strace',
    ],
    correctAnswer: 1,
    codeSnippet: '# Create debug container with ptrace capability\nkubectl debug -it my-pod \\\n  --image=ubuntu \\\n  --target=my-container \\\n  --profile=general\n\n# Or with custom profile for elevated access:\n# kubectl debug uses profiles:\n# - general: default\n# - baseline: same security as pod\n# - restricted: minimal permissions\n# - netadmin: NET_ADMIN capability\n# - sysadmin: privileged (SYS_PTRACE, etc.)\n\nkubectl debug -it my-pod \\\n  --image=nicolaka/netshoot \\\n  --target=my-container \\\n  --profile=sysadmin\n\n# Inside: strace the target process\nstrace -f -p 1 -e trace=network',
    explanation: 'kubectl debug with --profile=sysadmin creates a privileged ephemeral container with capabilities like SYS_PTRACE. Combined with --target for PID namespace sharing, you can strace any process.',
    tags: ['kubernetes', 'debug', 'strace', 'syscalls', 'privileged', 'profile'],
  },
  {
    id: 'k8s-troubleshooting-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'imagepullbackoff',
    question: 'Image pulls are extremely slow (taking 10+ minutes) on all nodes. The registry is fast when tested from outside the cluster. Containerd logs show normal pull progress. What could cause this?',
    options: [
      'The images are too large',
      'The container runtime is decompressing and unpacking layers through content-addressable storage, which may be slow due to overlay filesystem performance, high iowait, or image layer fan-out on ephemeral storage',
      'The registry is rate limiting',
      'The kubelet timeout is too short',
    ],
    correctAnswer: 1,
    codeSnippet: '# Containerd pull consists of:\n# 1. Download (fast) - pulling compressed layers\n# 2. Unpack (slow) - decompressing + creating filesystem\n# 3. Snapshot (slow) - preparing overlay/snapshotters\n\n# Check containerd snapshotter performance:\nctr --namespace k8s.io snapshots info <snapshot-id>\n\n# Check I/O during pulls:\niostat -x 1\n\n# Solutions:\n# - Use faster storage (local NVMe vs network disk)\n# - Enable stargz/lazy pulling\n# - Use image streaming (GKE)\n# - Pre-pull images using DaemonSets\n# - Use smaller base images (alpine, distroless)',
    explanation: 'Image pull speed includes download AND unpack. Decompressing layers and creating the filesystem snapshot can be very slow on nodes with slow disks or overlay filesystem overhead.',
    tags: ['kubernetes', 'image-pull', 'performance', 'containerd', 'unpack'],
  },
  {
    id: 'k8s-troubleshooting-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Kubernetes Service receives traffic bursts that cause endpoint connection failures. The issue correlates with endpoint changes. What mechanism causes this and how do you mitigate it?',
    options: [
      'DNS caching causes stale records',
      'kube-proxy iptables rule updates are not atomic; during updates, there is a brief window where traffic can be routed to terminated pods or miss new endpoints',
      'The Service port is overloaded',
      'The pods are too slow to start',
    ],
    correctAnswer: 1,
    codeSnippet: '# iptables update race condition:\n# 1. Pod terminates\n# 2. Endpoint controller removes endpoint\n# 3. kube-proxy receives watch event\n# 4. kube-proxy rebuilds ALL iptables rules\n# 5. During rebuild, old rules still in effect\n# 6. Traffic may hit the terminated pod\n\n# Mitigations:\n# 1. Use IPVS mode (incremental updates, faster)\n# 2. Use preStop hooks to delay shutdown\nlifecycle:\n  preStop:\n    exec:\n      command: ["/bin/sh", "-c", "sleep 5"]\n# 3. Implement connection draining in the app\n# 4. Consider eBPF-based CNI (Cilium) for atomic updates',
    explanation: 'kube-proxy in iptables mode rebuilds the entire rule set on every endpoint change. This is not atomic and can cause brief routing errors. IPVS and eBPF modes provide faster, incremental updates.',
    tags: ['kubernetes', 'kube-proxy', 'iptables', 'race-condition', 'endpoint-updates'],
  },
  {
    id: 'k8s-troubleshooting-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'The API server audit logs show many 409 Conflict errors on pod status updates. What does this indicate and how does it affect cluster behavior?',
    options: [
      'The API server is rejecting pod creation',
      'Multiple controllers or kubelets are racing to update the same pod status using optimistic concurrency (resourceVersion); this is normal but excessive 409s indicate controller contention',
      'RBAC is preventing status updates',
      'The pod status subresource is disabled',
    ],
    correctAnswer: 1,
    codeSnippet: '# 409 Conflict = optimistic concurrency conflict\n# Kubernetes uses resourceVersion for optimistic locking\n\n# Common causes of excessive 409s:\n# - Kubelet status updates competing with controllers\n# - Custom controllers with tight reconcile loops\n# - Webhook responses modifying the same object\n# - High pod churn rate\n\n# Check in audit logs:\n# "responseStatus": {"code": 409}\n# "verb": "update"\n# "resource": "pods"\n# "subresource": "status"\n\n# Impact: retries consume API server resources\n# Fix: Implement proper retry with backoff in controllers',
    explanation: 'Kubernetes uses optimistic concurrency with resourceVersion. 409 Conflicts occur when two updates race. This is normal but excessive conflicts indicate hot objects or tight reconcile loops.',
    tags: ['kubernetes', 'audit-log', '409-conflict', 'optimistic-concurrency', 'resourceversion'],
  },
  {
    id: 'k8s-troubleshooting-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'After upgrading the CNI plugin, some pods retain old network configuration (IP addresses from the old CIDR). New pods get correct IPs. What is the issue?',
    options: [
      'The CNI upgrade was incomplete',
      'Running pods are not restarted during CNI upgrades; they keep their existing network namespace and IP. The CNI plugin only configures networking at pod creation time (ADD) and cleanup at pod deletion (DEL)',
      'The kubelet cached the old CNI configuration',
      'The node needs a reboot',
    ],
    correctAnswer: 1,
    codeSnippet: '# CNI plugin lifecycle:\n# ADD: called when pod sandbox is created\n# DEL: called when pod sandbox is deleted\n# CHECK: health check (optional)\n\n# Running pods are NOT re-networked on CNI upgrade\n# They keep their existing network namespace\n\n# To apply new CNI config to all pods:\n# 1. Rolling restart workloads\nkubectl rollout restart deployment --all -n <ns>\n\n# 2. Or drain and uncordon nodes one by one\nkubectl drain <node> --ignore-daemonsets\nkubectl uncordon <node>\n\n# Verify old vs new pod IPs\nkubectl get pods -o wide',
    explanation: 'CNI plugins only configure pod networking at creation time (CNI ADD). Running pods retain their network configuration through CNI upgrades. Pods must be recreated to get the new CNI configuration.',
    tags: ['kubernetes', 'cni', 'upgrade', 'network-namespace', 'pod-ip'],
  },
  {
    id: 'k8s-troubleshooting-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'init-container-failures',
    question: 'A pod with multiple init containers uses restartPolicy: Always. Init container #2 fails. What happens to the state of init container #1 which had already completed?',
    options: [
      'Init container #1 state is preserved',
      'With restartPolicy: Always, the kubelet restarts from the failed init container; however, if the pod is recreated (not just the container), all init containers run again from the beginning',
      'All init containers are skipped',
      'Only the main containers are restarted',
    ],
    correctAnswer: 1,
    codeSnippet: '# Init container restart behavior:\n# With restartPolicy: Always:\n# - Only the FAILED init container is restarted\n# - Already completed init containers are NOT re-run\n# - The sequence continues from the failed one\n\n# BUT if the POD is recreated (e.g., node failure):\n# - ALL init containers run from scratch\n\n# Important: init containers should be idempotent\n# because they may run multiple times across pod restarts\n\n# For native sidecars (1.28+):\ninitContainers:\n- name: setup      # Runs once, must complete\n  image: setup\n- name: sidecar    # Runs alongside main containers\n  image: sidecar\n  restartPolicy: Always',
    explanation: 'When an init container fails, the kubelet restarts from that specific init container, not from the beginning. But if the entire pod is recreated, all init containers run again.',
    tags: ['kubernetes', 'init-containers', 'restart-behavior', 'sequence'],
  },
  {
    id: 'k8s-troubleshooting-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A custom controller running as a Deployment keeps losing permissions intermittently. The Role and RoleBinding exist. Checking reveals the controller uses token projection with a 1-hour expiry. What is happening?',
    options: [
      'The RBAC rules are being deleted',
      'The projected service account token is expiring and the controller is not refreshing it from the mounted file; it is using a cached stale token',
      'The API server is rejecting valid tokens',
      'The RoleBinding has a TTL',
    ],
    correctAnswer: 1,
    codeSnippet: '# Projected SA token with 1h expiry\nvolumes:\n- name: token\n  projected:\n    sources:\n    - serviceAccountToken:\n        expirationSeconds: 3600\n        audience: api\n        path: token\n\n# The token file is automatically rotated by kubelet\n# But the application must RE-READ the file periodically!\n\n# Bad: Read token once at startup\ntoken = open("/var/run/secrets/.../token").read()\n\n# Good: Read token before each API call\n# Or use a client that watches the file for changes\n\n# Kubernetes client libraries handle this automatically\n# when using InClusterConfig()',
    explanation: 'Projected service account tokens have short expiry and are rotated by the kubelet. If the application caches the token instead of re-reading the file, it will use an expired token after rotation.',
    tags: ['kubernetes', 'service-account-token', 'projected', 'expiry', 'rotation'],
  },
  {
    id: 'k8s-troubleshooting-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container using AppArmor with a custom profile crashes with "permission denied" on specific file operations. The AppArmor profile was loaded. How do you debug which operation is being denied?',
    options: [
      'Disable AppArmor entirely',
      'Check the audit log (/var/log/audit/audit.log or dmesg) for AppArmor DENIED messages, then update the profile to allow the specific operation',
      'The AppArmor profile is corrupt',
      'Container runtimes do not support AppArmor',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check AppArmor denials\ndmesg | grep apparmor\n# Or\ncat /var/log/audit/audit.log | grep DENIED\n\n# Example denial:\n# apparmor="DENIED" operation="mknod" profile="my-profile"\n# name="/dev/null" pid=1234\n\n# Set profile to complain mode for testing:\naa-complain /etc/apparmor.d/my-profile\n# Logs denials but doesn\'t block\n\n# Pod annotation for AppArmor:\nmetadata:\n  annotations:\n    container.apparmor.security.beta.kubernetes.io/my-container: \\\n      localhost/my-profile',
    explanation: 'AppArmor logs denied operations to the audit log or dmesg. Use complain mode to log without enforcing, identify all needed permissions, then switch back to enforce mode.',
    tags: ['kubernetes', 'apparmor', 'security', 'audit-log', 'debugging'],
  },
  {
    id: 'k8s-troubleshooting-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'The scheduler is not placing pods optimally, leading to hotspots on some nodes while others are nearly empty. The scheduler uses default configuration. What scheduling features can address this?',
    options: [
      'Only cluster autoscaler can fix this',
      'Use pod topology spread constraints, the Descheduler for rebalancing existing pods, and configure scheduler scoring plugins (NodeResourcesFit with MostAllocated strategy) for better bin-packing',
      'Manually move pods using kubectl move',
      'Increase maxSurge on all deployments',
    ],
    correctAnswer: 1,
    codeSnippet: '# 1. Topology Spread Constraints\ntopologySpreadConstraints:\n- maxSkew: 1\n  topologyKey: kubernetes.io/hostname\n  whenUnsatisfiable: ScheduleAnyway  # Soft constraint\n\n# 2. Descheduler policy (rebalances existing pods)\napiVersion: descheduler/v1alpha2\nkind: DeschedulerPolicy\nprofiles:\n- name: default\n  pluginConfig:\n  - name: LowNodeUtilization\n    args:\n      thresholds:\n        cpu: 20\n        memory: 20\n      targetThresholds:\n        cpu: 50\n        memory: 50\n\n# 3. Custom scorer for bin-packing\napiVersion: kubescheduler.config.k8s.io/v1\nkind: KubeSchedulerConfiguration\nprofiles:\n- schedulerName: default-scheduler\n  pluginConfig:\n  - name: NodeResourcesFit\n    args:\n      scoringStrategy:\n        type: MostAllocated  # Pack nodes tightly',
    explanation: 'The default scheduler balances load but may not pack optimally. Topology spread constraints, Descheduler (rebalances existing pods), and custom scoring strategies address hotspots.',
    tags: ['kubernetes', 'scheduler', 'bin-packing', 'descheduler', 'topology-spread'],
  },
  {
    id: 'k8s-troubleshooting-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'A node shows "NetworkUnavailable: True" after upgrading the CNI plugin. The kubelet is running and the node is Ready. What is the specific mechanism behind this condition?',
    options: [
      'The kubelet sets this condition',
      'The CNI plugin must set the NetworkUnavailable condition to False via the Node Status API; if the new CNI version or its node agent is not running or fails to update this condition, it remains True',
      'kube-proxy sets this condition',
      'The cloud controller sets this condition',
    ],
    correctAnswer: 1,
    codeSnippet: '# NetworkUnavailable is set by:\n# 1. Cloud controller manager (initially)\n# 2. CNI plugin node agent (to clear it)\n\n# Check if CNI plugin agent is running\nkubectl get pods -n kube-system -l app=<cni-agent> -o wide\n\n# Manually clear the condition if needed:\nkubectl patch node <node> --type=merge -p \'\n{\n  "status": {\n    "conditions": [{\n      "type": "NetworkUnavailable",\n      "status": "False",\n      "reason": "CiliumIsUp",\n      "message": "Cilium is running on this node"\n    }]\n  }\n}\'\n\n# Note: this uses the /status subresource',
    explanation: 'The NetworkUnavailable condition is typically set by the cloud controller and cleared by the CNI plugin agent. If the CNI agent fails to start after an upgrade, this condition stays True.',
    tags: ['kubernetes', 'network-unavailable', 'cni', 'node-condition', 'upgrade'],
  },
  {
    id: 'k8s-troubleshooting-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A CSI driver reports "NodePublishVolume: mount failed: mount point busy". The pod was previously terminated without proper cleanup. How do you resolve this without node restart?',
    options: [
      'Delete the PV and PVC',
      'Identify and unmount the stale mount point from the previous pod, then clean up the kubelet volume directory for that pod',
      'Restart the kubelet',
      'Delete the CSI driver pods',
    ],
    correctAnswer: 1,
    codeSnippet: '# Find stale mounts\nmount | grep <pv-name>\n# or\nfindmnt -r | grep <pod-uid>\n\n# Check kubelet volume directories\nls /var/lib/kubelet/pods/<old-pod-uid>/volumes/\n\n# Unmount stale mount\numount /var/lib/kubelet/pods/<old-pod-uid>/volumes/kubernetes.io~csi/<pv-name>/mount\n\n# Clean up the directory\nrm -rf /var/lib/kubelet/pods/<old-pod-uid>/volumes/\n\n# Force kubelet to reconcile\n# (usually happens automatically after cleanup)\n\n# Prevention: ensure proper CSI NodeUnpublishVolume\n# implementation handles cleanup',
    explanation: 'Stale mount points from improperly terminated pods can block new mounts. Manual unmounting and cleanup of the kubelet volume directory resolves this without a node restart.',
    tags: ['kubernetes', 'csi', 'stale-mount', 'node-publish', 'cleanup'],
  },
  {
    id: 'k8s-troubleshooting-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'DNS queries from pods are slow because of excessive search domain lookups (ndots:5). An application makes thousands of external API calls per second. What is the most efficient fix that does not require application changes?',
    options: [
      'Increase CoreDNS replicas',
      'Deploy NodeLocal DNSCache as a DaemonSet and configure pods to use the node-local cache, which reduces upstream DNS queries and eliminates conntrack issues',
      'Set ndots to 1',
      'Use a DNS sidecar in every pod',
    ],
    correctAnswer: 1,
    codeSnippet: '# NodeLocal DNSCache architecture:\n# Pod -> 169.254.20.10 (node-local cache) -> CoreDNS -> upstream\n\n# Deploy NodeLocal DNSCache DaemonSet\n# It creates a local DNS cache per node\n# using a link-local IP (169.254.20.10)\n\n# Benefits:\n# 1. Caching reduces upstream queries\n# 2. Uses TCP to CoreDNS (avoids conntrack for UDP)\n# 3. Reduces latency for cached entries\n# 4. Reduces CoreDNS load\n\n# Pod dnsConfig to use node-local cache:\ndnsConfig:\n  nameservers:\n  - 169.254.20.10\n  searches:\n  - default.svc.cluster.local\n  - svc.cluster.local\n  - cluster.local\n  options:\n  - name: ndots\n    value: "5"',
    explanation: 'NodeLocal DNSCache runs on every node, caching DNS responses locally. This eliminates the search domain expansion overhead for frequently queried names and avoids UDP conntrack issues.',
    tags: ['kubernetes', 'dns', 'nodelocal-dnscache', 'performance', 'conntrack'],
  },
  {
    id: 'k8s-troubleshooting-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container shows memory growing steadily over hours but never triggers OOM. Memory is at 95% of the limit. Heap dumps show no leak. /proc/meminfo inside the container shows high "Slab" and "SReclaimable". What is this memory?',
    options: [
      'Application heap memory',
      'Kernel slab memory (page cache metadata, dentry cache, inode cache) charged to the container cgroup; this is reclaimable under pressure but counts toward the cgroup limit',
      'Container runtime overhead',
      'Shared library memory',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check slab memory in cgroup\ncat /sys/fs/cgroup/memory/kubepods/pod<uid>/memory.stat\n# slab_reclaimable: N bytes  <- kernel cache memory\n# slab_unreclaimable: N bytes\n\n# This is kernel cache (dentry, inode) charged to cgroup\n# It IS counted toward the memory limit\n# But it CAN be reclaimed under pressure\n\n# To force reclamation:\necho 2 > /proc/sys/vm/drop_caches  # (from host)\n\n# Better: monitor memory.stat breakdown\n# and understand that near-limit is OK if mostly reclaimable\n\n# Alert on: memory.usage_in_bytes - slab_reclaimable\n# This is the "true" non-reclaimable usage',
    explanation: 'Kernel slab memory (dentry cache, inode cache) is charged to container cgroups. It grows with file operations and is reclaimable under pressure, but counts toward the memory limit.',
    tags: ['kubernetes', 'memory', 'slab', 'kernel-cache', 'cgroup'],
  },
  {
    id: 'k8s-troubleshooting-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service mesh (Istio) is injecting sidecar proxies. After injection, services experience increased latency and intermittent 503 errors. Standard Kubernetes Service endpoints are correct. How do you troubleshoot this?',
    options: [
      'Remove the service mesh',
      'Check Envoy sidecar proxy configuration, circuit breaker settings, upstream health checks, and mTLS handshake errors using istioctl proxy-config and Envoy admin interface',
      'The sidecar proxy is consuming too much CPU',
      'The Service needs to be recreated',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check Envoy proxy configuration\nistioctl proxy-config cluster my-pod -n default\nistioctl proxy-config endpoint my-pod -n default\nistioctl proxy-config route my-pod -n default\n\n# Check for upstream connection errors\nistioctl proxy-config log my-pod --level debug\n\n# Access Envoy admin interface\nkubectl port-forward my-pod 15000:15000\ncurl localhost:15000/stats | grep upstream_cx_connect_fail\n\n# Common issues:\n# - Circuit breaker tripping (too many pending requests)\n# - mTLS misconfiguration between services\n# - Envoy outlier detection removing healthy endpoints\n# - Resource limits on sidecar too low',
    explanation: 'Service mesh issues require inspecting the sidecar proxy configuration, not just Kubernetes Service objects. Envoy stats, circuit breakers, mTLS, and outlier detection all affect traffic routing.',
    tags: ['kubernetes', 'istio', 'envoy', 'service-mesh', 'sidecar-debugging'],
  },
  {
    id: 'k8s-troubleshooting-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'Intermittent packet drops are occurring between pods on the same node. tcpdump on the pod interfaces shows sent packets that never arrive. What kernel-level mechanisms could cause this?',
    options: [
      'The pods are using different protocols',
      'The Linux bridge (cbr0) or veth pair may be dropping packets due to iptables/nftables rules, conntrack table overflow, or the net.bridge.bridge-nf-call-iptables sysctl affecting bridge traffic',
      'The pods are in different cgroups',
      'Container runtime is dropping packets',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check for drops on the bridge\nip -s link show cbr0\n\n# Check conntrack table\nsysctl net.netfilter.nf_conntrack_count\nsysctl net.netfilter.nf_conntrack_max\nconntrack -S  # Look for "drop" and "insert_failed"\n\n# Critical sysctl for bridge traffic\nsysctl net.bridge.bridge-nf-call-iptables\n# Must be 1 for NetworkPolicy to work on bridged traffic\n# But can cause drops if iptables rules are wrong\n\n# Check for veth pair drops\nethtool -S <veth-interface>\n# Look for tx_dropped, rx_dropped\n\n# Check nftables/iptables for DROP rules\niptables -L -v -n | grep DROP\nnft list ruleset | grep drop',
    explanation: 'Same-node packet drops between pods often involve the Linux bridge, veth pairs, or iptables rules applied to bridged traffic. conntrack table overflow and bridge-nf-call-iptables are common culprits.',
    tags: ['kubernetes', 'packet-drops', 'bridge', 'conntrack', 'veth', 'iptables'],
  },
  {
    id: 'k8s-troubleshooting-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A cluster uses cgroup v2 and memory.swap.max is set. Pods under memory pressure are not being OOMKilled but are extremely slow. Why?',
    options: [
      'cgroup v2 does not support OOM',
      'With swap enabled and memory.swap.max configured, containers spill into swap instead of being OOMKilled, causing severe performance degradation due to disk I/O',
      'The kernel is defective',
      'Pods cannot use swap in Kubernetes',
    ],
    correctAnswer: 1,
    codeSnippet: '# cgroup v2 swap configuration:\n# memory.swap.max = max swap usage for the cgroup\n# memory.swap.current = current swap usage\n\ncat /sys/fs/cgroup/kubepods.slice/pod<uid>/memory.swap.max\ncat /sys/fs/cgroup/kubepods.slice/pod<uid>/memory.swap.current\n\n# Kubernetes swap support (beta in 1.28):\n# kubelet config:\n# memorySwap:\n#   swapBehavior: LimitedSwap  # Limits swap to pod request\n# OR\n#   swapBehavior: NoSwap       # Disable swap (default)\n\n# If swap is causing issues, disable it:\n# swapoff -a\n# And set kubelet: failSwapOn: true',
    explanation: 'With swap enabled in cgroup v2, containers can use swap space instead of being OOMKilled. This prevents crashes but causes severe slowness as memory pages are swapped to disk.',
    tags: ['kubernetes', 'cgroup-v2', 'swap', 'performance', 'memory-swap'],
  },
  {
    id: 'k8s-troubleshooting-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A pod using a user namespace (userns) crashes because the application writes to a file owned by UID 0 inside the container. With user namespaces enabled, UID 0 in the container maps to a high UID on the host. What is the fix?',
    options: [
      'Disable user namespaces',
      'Ensure the file permissions account for UID remapping, or use fsGroup/runAsUser that maps correctly within the user namespace mapping range',
      'Run the container as UID 0 on the host',
      'User namespaces do not affect file permissions',
    ],
    correctAnswer: 1,
    codeSnippet: '# User namespace UID mapping:\n# Container UID 0 -> Host UID 65536 (example)\n# Container UID 1000 -> Host UID 66536\n\n# Pod spec with user namespaces (1.28+ beta):\nspec:\n  hostUsers: false  # Enable user namespace\n\n# File permissions in image:\n# /app/data owned by root (UID 0 in container)\n# On host: file is owned by UID 65536\n# If volume is pre-existing with root (UID 0) ownership,\n# the remapped user cannot write to it\n\n# Fix: Use fsGroup to adjust volume permissions\nsecurityContext:\n  fsGroup: 0  # Adjusts within the user namespace\n\n# Or adjust volume permissions with initContainer',
    explanation: 'User namespaces remap UIDs between the container and host. Files owned by UID 0 on the host are not accessible by UID 0 in the container (which maps to a different host UID).',
    tags: ['kubernetes', 'user-namespace', 'uid-mapping', 'permissions', 'security'],
  },
  {
    id: 'k8s-troubleshooting-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'A node has 200 pods, each with HTTP liveness and readiness probes. The kubelet is consuming high CPU and some probes are timing out. What is the underlying issue?',
    options: [
      'HTTP probes are not efficient',
      'The kubelet probe worker pool has a finite size; with 200 pods having 2 probes each (400 total), probe execution is queued and delays cause timeouts that trigger unnecessary restarts',
      'The node does not have enough network bandwidth',
      'The container runtime is overloaded',
    ],
    correctAnswer: 1,
    codeSnippet: '# Kubelet has a limited number of probe workers\n# Default probe workers: configured per probe type\n\n# Calculate probe load:\n# 200 pods x 2 probes x (1/periodSeconds) = probes/sec\n# With periodSeconds: 10 -> 40 probes/sec\n\n# Mitigations:\n# 1. Increase periodSeconds for non-critical probes\n# 2. Reduce pod count per node (smaller nodes, more of them)\n# 3. Use gRPC/TCP probes instead of HTTP (less overhead)\n# 4. Monitor kubelet probe metrics:\ncurl http://localhost:10255/metrics | grep prober_probe_duration\n\n# 5. Consider if all pods need both probes\n# Some pods may only need readiness, not liveness',
    explanation: 'The kubelet executes probes synchronously per pod. With many pods, probe execution queues up, causing delays. Late probe execution can exceed timeouts, falsely indicating failures.',
    tags: ['kubernetes', 'probes', 'kubelet', 'scalability', 'probe-workers'],
  },
  {
    id: 'k8s-troubleshooting-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'The API server is experiencing high latency. Investigation shows etcd "took too long" warnings in API server logs correlated with large LIST operations. How do you diagnose and fix this?',
    options: [
      'Increase API server replicas',
      'Enable API server request tracing, use etcd metrics to identify slow range queries, implement resource quotas and limit list operations, and enable API Priority and Fairness',
      'Restart etcd',
      'Upgrade the Kubernetes version',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check etcd performance\netcdctl --endpoints=https://127.0.0.1:2379 \\\n  endpoint health --write-out=table\n\n# Check etcd slow operations\netcdctl --endpoints=https://127.0.0.1:2379 \\\n  debug perf-stats\n\n# API server metrics for request latency\ncurl -s https://localhost:6443/metrics | \\\n  grep apiserver_request_duration_seconds\n\n# Enable API Priority and Fairness\n# to prevent LIST storms from affecting other operations\napiVersion: flowcontrol.apiserver.k8s.io/v1beta3\nkind: FlowSchema\nmetadata:\n  name: restrict-list\nspec:\n  matchingPrecedence: 1000\n  rules:\n  - resourceRules:\n    - verbs: ["list"]',
    explanation: 'Large LIST operations can overwhelm etcd with expensive range queries. API Priority and Fairness (APF) can throttle expensive requests, and resource quotas limit the number of objects to reduce list sizes.',
    tags: ['kubernetes', 'etcd', 'api-server', 'performance', 'apf'],
  },
  {
    id: 'k8s-troubleshooting-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'A namespace has a ResourceQuota and a pod is evicted. The evicted pod controller tries to create a replacement but fails with "exceeded quota". What is the deadlock scenario and how do you resolve it?',
    options: [
      'Increase the ResourceQuota',
      'The evicted pod still counts against the quota (Terminating phase) while the new pod needs quota too; resolve by setting a terminationGracePeriodSeconds limit and ensuring timely pod cleanup',
      'ResourceQuotas do not count evicted pods',
      'Delete the namespace quota',
    ],
    correctAnswer: 1,
    codeSnippet: '# Quota deadlock scenario:\n# 1. Quota: pods=5, Pod count=5\n# 2. Pod A is evicted -> enters Terminating\n# 3. Controller creates Pod B -> REJECTED (5 pods exist)\n# 4. Pod A stuck in Terminating (finalizer, slow shutdown)\n# 5. Deadlock until Pod A fully terminates\n\n# Mitigations:\n# 1. Set appropriate terminationGracePeriodSeconds\n# 2. Use ResourceQuota with scope NotTerminating\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: compute-quota\nspec:\n  hard:\n    pods: "10"\n  scopeSelector:\n    matchExpressions:\n    - operator: NotIn\n      scopeName: Terminating\n      values: []',
    explanation: 'Terminating pods still count against ResourceQuota. If quota is tight, new pods cannot be created until terminated pods are cleaned up. Use scopeSelector with NotTerminating to exclude them.',
    tags: ['kubernetes', 'resourcequota', 'eviction', 'deadlock', 'terminating'],
  },
  {
    id: 'k8s-troubleshooting-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You need to perform live binary patching of a running process in a container for an emergency fix. The container has no writable filesystem (readOnlyRootFilesystem: true) and no package manager. How do you accomplish this?',
    options: [
      'This is impossible in Kubernetes',
      'Use kubectl debug to add an ephemeral container with the patched binary, then use nsenter/ptrace to replace the running process in the target PID namespace',
      'Rolling update is the only option',
      'Mount a ConfigMap with the new binary',
    ],
    correctAnswer: 1,
    codeSnippet: '# Emergency live patching approach:\n# 1. Add debug container with patched binary\nkubectl debug -it my-pod \\\n  --image=my-debug-image \\  # Contains patched binary\n  --target=my-container \\\n  --profile=sysadmin\n\n# 2. Inside debug container:\n# Copy patched binary to shared PID namespace\ncp /patched-binary /proc/1/root/tmp/patched-binary\n\n# 3. Or use ptrace to modify running process memory\n# (requires SYS_PTRACE capability)\n\n# IMPORTANT: This is an emergency procedure only\n# Proper fix: build new image, rolling update\n# Document: what was patched, why, when, by whom',
    explanation: 'In extreme emergencies, ephemeral containers with privileged access can modify running processes via the shared PID namespace. This is a last resort - proper deployments should be used normally.',
    tags: ['kubernetes', 'debug', 'live-patching', 'emergency', 'ptrace'],
  },
  {
    id: 'k8s-troubleshooting-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'imagepullbackoff',
    question: 'A cluster uses a containerd mirror configuration with a pull-through cache. Some images pull from cache while others fail with "failed to resolve reference" despite existing in the upstream registry. What is the issue?',
    options: [
      'The cache is full',
      'The containerd mirror configuration only matches specific registry hostnames; images from unmatched registries bypass the mirror, and the mirror may not support the OCI distribution spec for certain media types',
      'The pull-through cache does not support multi-arch images',
      'Containerd does not support mirrors',
    ],
    correctAnswer: 1,
    codeSnippet: '# containerd mirror configuration\n# /etc/containerd/certs.d/docker.io/hosts.toml\nserver = "https://registry-1.docker.io"\n\n[host."https://mirror.example.com"]\n  capabilities = ["pull", "resolve"]\n  # "resolve" is critical for tag -> manifest resolution\n\n[host."https://registry-1.docker.io"]\n  capabilities = ["pull", "resolve"]\n  # Fallback to original registry\n\n# Common issues:\n# 1. Missing "resolve" capability on mirror\n# 2. Mirror doesn\'t support certain manifest types\n# 3. Mirror registry path doesn\'t match expected format\n# 4. Different registries need separate host configs',
    explanation: 'Containerd mirror configs are per-registry and require the "resolve" capability for tag resolution. Mirrors must support the OCI distribution spec, and configuration must match the exact registry hostname.',
    tags: ['kubernetes', 'containerd', 'mirror', 'pull-through-cache', 'registry'],
  },
  {
    id: 'k8s-troubleshooting-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'An operator needs to watch a CRD that has been registered with a conversion webhook. The operator has permissions for the CRD apiGroup but still gets errors. What RBAC-adjacent issue could cause this?',
    options: [
      'CRDs do not support RBAC',
      'The conversion webhook serving certificates may be expired, the webhook service may be unreachable, or the webhook is rejecting conversions between stored and requested API versions',
      'The operator needs cluster-admin',
      'CRD conversion does not require webhooks',
    ],
    correctAnswer: 1,
    codeSnippet: '# CRD with conversion webhook\napiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nspec:\n  conversion:\n    strategy: Webhook\n    webhook:\n      clientConfig:\n        service:\n          name: my-conversion-svc\n          namespace: default\n          path: /convert\n        caBundle: <base64-ca>\n\n# Troubleshooting:\n# 1. Check webhook service is running\nkubectl get svc my-conversion-svc\n\n# 2. Check webhook certificates\nkubectl get secret webhook-cert -o jsonpath=\'{.data.tls\\.crt}\' | \\\n  base64 -d | openssl x509 -noout -dates\n\n# 3. Check API server logs for conversion errors\n# "failed calling webhook: Post ... connection refused"',
    explanation: 'CRD conversion webhooks are called when the API server needs to convert between stored and requested versions. Webhook failures (certs, connectivity) prevent all operations on the CRD, even reads.',
    tags: ['kubernetes', 'crd', 'conversion-webhook', 'certificates', 'api-versions'],
  },
  {
    id: 'k8s-troubleshooting-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'A node is experiencing "PLEG is not healthy" and pods take minutes to reach Running state. The container runtime (containerd) is responsive. What is the root cause?',
    options: [
      'The kubelet needs more CPU',
      'The PLEG relist operation calls the container runtime to list all sandboxes and containers; if a container is in a stuck state (e.g., hanging on a mount operation), the relist blocks, causing PLEG health failure',
      'The API server is slow',
      'The CNI plugin is slow',
    ],
    correctAnswer: 1,
    codeSnippet: '# PLEG relist process:\n# 1. Call runtime.ListPodSandboxes()\n# 2. Call runtime.ListContainers()\n# 3. Compare with previous state\n# 4. Generate events for state changes\n\n# If ANY container operation hangs:\n# - Mount operation waiting for storage\n# - Container removal waiting for process exit\n# - CRI call blocked on containerd\n\n# Debug:\n# 1. Check for stuck container operations\ncrictl ps -a | grep -i removing\n\n# 2. Check containerd task status\nctr -n k8s.io tasks list\n\n# 3. Check for stuck mount operations\ncat /proc/mounts | grep <pv-name>\n\n# 4. Monitor PLEG metrics\ncurl http://localhost:10255/metrics | grep pleg_relist',
    explanation: 'PLEG health depends on completing a full container relist within 3 minutes. A single stuck container operation (hanging mount, removal) blocks the entire relist, causing PLEG to become unhealthy.',
    tags: ['kubernetes', 'pleg', 'containerd', 'stuck-container', 'relist'],
  },
  {
    id: 'k8s-troubleshooting-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'After deploying NodeLocal DNSCache, pods intermittently fail DNS resolution with "no servers could be reached". The node-local cache pods are running. What is the subtle failure mode?',
    options: [
      'NodeLocal DNSCache does not support all DNS record types',
      'If the node-local DNS cache pod restarts, the iptables/IPVS rules routing DNS to the local IP (169.254.20.10) may briefly be missing, causing DNS failures for pods during the restart window',
      'CoreDNS is down',
      'The pods are using the wrong DNS server',
    ],
    correctAnswer: 1,
    codeSnippet: '# NodeLocal DNSCache uses iptables rules to intercept\n# DNS traffic to the kube-dns ClusterIP and redirect\n# to the local cache (169.254.20.10)\n\n# During cache pod restart:\n# 1. iptables rules are removed (cache teardown)\n# 2. DNS to 169.254.20.10 fails (no listener)\n# 3. Until new cache pod starts and adds rules\n\n# Mitigation: Use iptables-based setup (default)\n# which falls back to kube-dns ClusterIP on cache failure\n\n# Or configure pods with fallback:\ndnsConfig:\n  nameservers:\n  - 169.254.20.10   # Node-local cache\n  - 10.96.0.10      # kube-dns ClusterIP (fallback)',
    explanation: 'During NodeLocal DNSCache pod restarts, there is a brief window where DNS interception rules are missing. Pods configured only with the node-local IP have no fallback during this window.',
    tags: ['kubernetes', 'nodelocal-dnscache', 'restart', 'dns-failure', 'fallback'],
  },
  {
    id: 'k8s-troubleshooting-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container running in cgroup v2 shows memory.peak > memory.max in the cgroup filesystem. How is this possible?',
    options: [
      'It is a bug in cgroup v2',
      'memory.peak tracks the highest memory.current value which can briefly exceed memory.max before the OOM killer activates; the kernel reclaim and OOM kill are not instantaneous',
      'memory.max was changed after the peak was recorded',
      'memory.peak includes swap',
    ],
    correctAnswer: 1,
    codeSnippet: '# cgroup v2 memory files:\n# memory.current  - current usage\n# memory.max      - hard limit (triggers OOM)\n# memory.peak     - highest memory.current ever recorded\n# memory.high     - throttling point\n\n# Why peak > max:\n# The kernel allocates memory in page-sized chunks\n# Between allocation and the OOM killer activating,\n# memory can briefly exceed max\n\n# This is a known behavior:\n# 1. Process requests memory (page fault)\n# 2. Kernel charges to cgroup -> exceeds max\n# 3. memory.current > memory.max momentarily\n# 4. Kernel invokes OOM killer\n# 5. memory.peak records the peak before OOM\n\n# Check: cat /sys/fs/cgroup/.../memory.peak\n# This helps understand true peak memory needs',
    explanation: 'Memory allocation is not atomic with OOM killing. The kernel can charge memory to a cgroup exceeding memory.max before the OOM killer activates, and memory.peak captures this transient peak.',
    tags: ['kubernetes', 'cgroup-v2', 'memory-peak', 'oom-internals', 'kernel'],
  },
  {
    id: 'k8s-troubleshooting-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service with topology-aware routing (TopologyAwareHints) is enabled but traffic is not being routed to zone-local endpoints. What conditions must be met for topology-aware routing to work?',
    options: [
      'It only works with headless Services',
      'EndpointSlices must have zone hints populated, endpoints must be sufficiently distributed across zones, and the kube-proxy must have the feature gate enabled',
      'Topology-aware routing is always active',
      'It requires a service mesh',
    ],
    correctAnswer: 1,
    codeSnippet: '# Enable topology-aware routing\napiVersion: v1\nkind: Service\nmetadata:\n  name: my-service\n  annotations:\n    service.kubernetes.io/topology-mode: Auto\nspec:\n  selector:\n    app: my-app\n\n# Requirements for it to work:\n# 1. Nodes must have topology.kubernetes.io/zone label\n# 2. EndpointSlice controller must allocate hints\n# 3. Sufficient endpoints in each zone\n# 4. If allocation is "overloaded" (not enough endpoints\n#    in a zone), hints are removed and traffic is global\n\n# Check hints:\nkubectl get endpointslice -l kubernetes.io/service-name=my-service -o yaml\n# Look for: hints.forZones in endpoint entries',
    explanation: 'Topology-aware routing requires zone labels on nodes, sufficient endpoint distribution across zones, and proper hint allocation. If any zone is underrepresented, the controller removes all hints and routing becomes global.',
    tags: ['kubernetes', 'topology-aware-routing', 'endpointslice', 'zone-affinity'],
  },
  {
    id: 'k8s-troubleshooting-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A PV using block mode (volumeMode: Block) causes the pod to crash because the application expects a filesystem. How does block mode differ from filesystem mode at the kernel level?',
    options: [
      'There is no difference',
      'Block mode exposes the raw block device to the container as a device file; no filesystem is created. Filesystem mode formats the device with a filesystem (ext4/xfs) and mounts it at a path',
      'Block mode is faster than filesystem mode',
      'Block mode only works with local volumes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Filesystem mode (default):\n# 1. CSI driver formats device (mkfs.ext4)\n# 2. Kubelet mounts at container path\n# 3. App reads/writes files normally\n\n# Block mode:\n# 1. Raw device is exposed to container\n# 2. Available as /dev/xvdf (device file)\n# 3. App must handle raw I/O (databases, etc.)\n\n# Pod spec for block mode:\nspec:\n  containers:\n  - name: my-app\n    volumeDevices:         # NOT volumeMounts!\n    - name: data\n      devicePath: /dev/xvdf\n  volumes:\n  - name: data\n    persistentVolumeClaim:\n      claimName: block-pvc\n---\n# PVC for block mode:\napiVersion: v1\nkind: PersistentVolumeClaim\nspec:\n  volumeMode: Block       # Not Filesystem\n  accessModes: [ReadWriteOnce]',
    explanation: 'Block mode skips filesystem creation and exposes raw devices. It uses volumeDevices (not volumeMounts) in the pod spec. Only applications designed for raw block I/O (like certain databases) should use it.',
    tags: ['kubernetes', 'block-volume', 'raw-device', 'volume-mode', 'csi'],
  },
  {
    id: 'k8s-troubleshooting-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'A pod using dual-stack networking (IPv4 + IPv6) can reach IPv4 services but not IPv6 services. The cluster has dual-stack enabled. What should you investigate?',
    options: [
      'IPv6 is not supported in Kubernetes',
      'Check that the CNI plugin supports dual-stack, IPv6 forwarding is enabled on nodes, the kube-proxy has dual-stack CIDR configured, and firewall rules allow IPv6 traffic',
      'The pod needs a special annotation for IPv6',
      'IPv6 services need a different Service type',
    ],
    correctAnswer: 1,
    codeSnippet: '# Verify dual-stack configuration\n# 1. Check service CIDRs\nkubectl cluster-info dump | grep service-cluster-ip-range\n# Should show both IPv4 and IPv6 ranges\n\n# 2. Check pod CIDRs\nkubectl get nodes -o jsonpath=\'{.items[*].spec.podCIDRs}\'\n# Should show both IPv4 and IPv6 ranges per node\n\n# 3. Check node IPv6 forwarding\nsysctl net.ipv6.conf.all.forwarding  # Must be 1\n\n# 4. Check kube-proxy config\nkubectl get configmap kube-proxy -n kube-system -o yaml\n# clusterCIDR should include IPv6 range\n\n# 5. Check Service has both IPs\nkubectl get svc my-svc -o jsonpath=\'{.spec.clusterIPs}\'\n# ["10.96.0.100", "fd00::100"]\n\n# 6. Check ip6tables rules\nip6tables -t nat -L -n | grep <svc-ipv6>',
    explanation: 'Dual-stack requires end-to-end configuration: API server, controller manager, kubelet, kube-proxy, and CNI plugin must all support and be configured for dual-stack. IPv6 forwarding must be enabled on nodes.',
    tags: ['kubernetes', 'dual-stack', 'ipv6', 'networking', 'troubleshooting'],
  },
  {
    id: 'k8s-troubleshooting-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container crashes with SIGSEGV (exit code 139) only when running in Kubernetes, not locally. The image is identical. The cluster enforces securityContext.capabilities.drop: [ALL]. What is the connection?',
    options: [
      'SIGSEGV is caused by insufficient memory',
      'Dropping all capabilities removes CAP_SYS_PTRACE which some applications need for internal debugging/profiling, and can affect glibc pthread behavior; also, seccomp profiles may block syscalls that cause SIGSEGV when blocked instead of EPERM',
      'Capabilities do not affect application behavior',
      'The container runtime is different',
    ],
    correctAnswer: 1,
    codeSnippet: '# SIGSEGV = Segmentation Fault (exit 139 = 128 + 11)\n# Dropping ALL capabilities can affect:\n# - CAP_SYS_PTRACE: needed by some profilers/debuggers\n# - Some syscalls return different errors without caps\n\n# Seccomp can also cause SIGSEGV:\n# Some profiles return SIGSEGV instead of EPERM for\n# blocked syscalls (configurable action)\n\n# Debug approach:\n# 1. Test with capabilities added back one at a time\nsecurityContext:\n  capabilities:\n    drop: ["ALL"]\n    add: ["SYS_PTRACE"]  # Add suspected capability\n\n# 2. Check seccomp profile\n# 3. Run with strace to identify the failing syscall\nkubectl debug -it my-pod --image=ubuntu --profile=sysadmin\nstrace -f -p 1 2>&1 | tail -20  # See last syscalls before crash',
    explanation: 'Dropping capabilities and seccomp filtering can cause unexpected SIGSEGV in applications that depend on specific capabilities or syscalls. Debug by selectively re-adding capabilities or using strace.',
    tags: ['kubernetes', 'sigsegv', 'capabilities', 'seccomp', 'security'],
  },
  {
    id: 'k8s-troubleshooting-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A custom scheduler (schedulerName: my-scheduler) is running but some pods assigned to it remain Pending forever. Other pods scheduled by my-scheduler work fine. What should you investigate in the custom scheduler?',
    options: [
      'The pods have the wrong schedulerName',
      'The custom scheduler filter/score plugins may be rejecting the pod due to specific constraints, or the scheduler lease has expired and another instance is not picking up the work',
      'Custom schedulers do not support all pod types',
      'The API server is blocking the scheduler',
    ],
    correctAnswer: 1,
    codeSnippet: '# Verify scheduler is running and healthy\nkubectl get pods -n kube-system -l component=my-scheduler\nkubectl logs -n kube-system <my-scheduler-pod>\n\n# Check scheduler lease\nkubectl get lease my-scheduler -n kube-system -o yaml\n# holderIdentity: should match running pod\n# renewTime: should be recent\n\n# Check for scheduling failures in scheduler logs\n# Common issues in custom schedulers:\n# - Filter plugins rejecting all nodes\n# - Score plugins returning errors\n# - Permit plugins timing out\n# - Reserve plugins failing\n\n# Test with verbose scheduler logging:\n# --v=10 shows detailed scheduling decisions',
    explanation: 'Custom schedulers can fail silently for specific pods if filter/score plugins reject all nodes. Check scheduler logs with high verbosity to see the scheduling decision chain for the affected pod.',
    tags: ['kubernetes', 'custom-scheduler', 'pending', 'scheduler-plugins', 'lease'],
  },
  {
    id: 'k8s-troubleshooting-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'init-container-failures',
    question: 'A native sidecar container (restartPolicy: Always in initContainers) is not stopping after the main container completes in Kubernetes 1.29. This prevents the Job from completing. What is happening?',
    options: [
      'Native sidecars always run forever',
      'Native sidecar containers receive SIGTERM when all main containers have terminated, but if the sidecar ignores SIGTERM and terminationGracePeriodSeconds is long, the job completion is delayed',
      'Jobs do not support native sidecars',
      'The sidecar needs a special annotation to stop',
    ],
    correctAnswer: 1,
    codeSnippet: '# Native sidecar lifecycle:\n# 1. Sidecar starts (as init container with restartPolicy: Always)\n# 2. Main containers start\n# 3. Main containers complete\n# 4. Sidecar receives SIGTERM\n# 5. Sidecar has terminationGracePeriodSeconds to shut down\n# 6. After grace period: SIGKILL\n\n# Problem: Sidecar ignores SIGTERM\n# Solution 1: Handle SIGTERM in sidecar\n# Solution 2: Reduce terminationGracePeriodSeconds\nspec:\n  terminationGracePeriodSeconds: 10\n  initContainers:\n  - name: log-sidecar\n    restartPolicy: Always\n    image: log-collector\n    # Must handle SIGTERM for clean shutdown',
    explanation: 'Native sidecar containers receive SIGTERM when main containers finish. If the sidecar does not handle SIGTERM gracefully, it runs until terminationGracePeriodSeconds expires and SIGKILL is sent.',
    tags: ['kubernetes', 'native-sidecar', 'job', 'sigterm', 'completion'],
  },
  {
    id: 'k8s-troubleshooting-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'After enabling memory cgroup v2 on cluster nodes, pods that previously ran fine are now being OOMKilled. Memory usage reported by cgroup v2 is higher than cgroup v1 for the same workload. Why?',
    options: [
      'cgroup v2 has a bug in memory accounting',
      'cgroup v2 accounts kernel stack memory, socket buffers, and more kernel objects to the pod cgroup by default, whereas cgroup v1 had some of these as global or separate counters',
      'cgroup v2 uses different memory units',
      'The container runtime reports memory differently',
    ],
    correctAnswer: 1,
    codeSnippet: '# cgroup v1 vs v2 memory accounting differences:\n# cgroup v1:\n# memory.usage_in_bytes = RSS + cache\n# kernel memory partially tracked separately\n\n# cgroup v2:\n# memory.current = RSS + cache + kernel stack +\n#   page tables + socket buffers + slab (charged to cgroup)\n# More comprehensive accounting = higher reported usage\n\n# Check the breakdown:\ncat /sys/fs/cgroup/kubepods.slice/pod<uid>/memory.stat\n# anon: anonymous memory (heap, stack)\n# file: file-backed memory (page cache)\n# kernel: kernel memory charged to cgroup\n# sock: socket buffer memory\n# slab: slab allocator memory\n\n# Fix: Increase memory limits to account for\n# the additional kernel memory now being tracked',
    explanation: 'cgroup v2 has more comprehensive memory accounting than v1, including kernel stack, page tables, and socket buffers. This means the same workload appears to use more memory under v2.',
    tags: ['kubernetes', 'cgroup-v2', 'memory-accounting', 'migration', 'oomkilled'],
  },
  {
    id: 'k8s-troubleshooting-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'A Kubernetes cluster uses Calico in eBPF mode. Pod-to-Service traffic works, but pod-to-external traffic is being dropped. tcpdump on the node physical interface shows no packets leaving. What is the issue?',
    options: [
      'eBPF does not support external traffic',
      'Calico eBPF dataplane handles NAT differently from iptables; the BPF program may not be properly attached to the host interface for SNAT of pod traffic to external destinations',
      'The default gateway is wrong',
      'CoreDNS cannot resolve external names',
    ],
    correctAnswer: 1,
    codeSnippet: '# Calico eBPF dataplane attachment points:\n# - Pod veth interfaces: ingress/egress TC programs\n# - Host interfaces: TC programs for SNAT/routing\n\n# Check BPF program attachment:\ntc filter show dev eth0 ingress\ntc filter show dev eth0 egress\n# Should show calico BPF programs attached\n\n# Check if SNAT is working:\ncalico-node bpf nat dump\ncalico-node bpf conntrack dump\n\n# Common issues:\n# 1. BPF programs not attached to correct host interface\n# 2. Host interface name doesn\'t match Calico config\n# 3. MTU mismatch causing silent drops\n# 4. Missing route for return traffic\n\n# Fix: Check Calico node config\nkubectl get felixconfiguration default -o yaml\n# bpfExternalServiceMode, bpfDataIfacePattern',
    explanation: 'Calico eBPF mode attaches BPF programs to specific interfaces for NAT and routing. If the host interface is not correctly identified or BPF programs are not attached, SNAT for external traffic fails.',
    tags: ['kubernetes', 'calico', 'ebpf', 'snat', 'external-traffic'],
  },
  {
    id: 'k8s-troubleshooting-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'CoreDNS autopath plugin is enabled but causes failures for some pods. DNS queries return NXDOMAIN for valid external domains. What is the autopath edge case?',
    options: [
      'autopath does not support external domains',
      'autopath optimizes search domain lookups by following the CNAME chain; but if a search domain prefix happens to match a real external domain, autopath returns that result instead of continuing the search',
      'autopath conflicts with the cache plugin',
      'autopath requires additional RBAC permissions',
    ],
    correctAnswer: 1,
    codeSnippet: '# Autopath plugin optimizes DNS search path:\n# Instead of trying: api.default.svc.cluster.local,\n#   api.svc.cluster.local, api.cluster.local, api\n# It uses CNAME to shortcut to the actual answer\n\n# Edge case:\n# Query: "analytics" (short name)\n# Search domain: "default.svc.cluster.local"\n# If "analytics.default.svc.cluster.local" exists -> returned\n# But user wanted "analytics.google.com" (external)\n\n# Or: Wildcard DNS entries in a search domain\n# can capture queries meant for external resolution\n\n# Corefile:\n.:53 {\n    autopath @kubernetes  # Can cause issues\n    kubernetes cluster.local {\n      pods insecure\n    }\n    forward . /etc/resolv.conf\n}',
    explanation: 'autopath follows the DNS search path using CNAMEs. If a search domain prefix accidentally matches a valid record, autopath returns early with the wrong result instead of continuing the search.',
    tags: ['kubernetes', 'coredns', 'autopath', 'dns-search', 'edge-case'],
  },
  {
    id: 'k8s-troubleshooting-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'The kube-controller-manager is creating excessive events, causing etcd to be overwhelmed. What mechanism limits event creation and how can you tune it?',
    options: [
      'Events cannot be rate-limited',
      'The event recorder uses event aggregation and rate limiting; configure --event-burst and --event-qps on the controller manager, and events with the same source/reason are aggregated into count-based events',
      'etcd automatically limits events',
      'Events are stored in a separate database',
    ],
    correctAnswer: 1,
    codeSnippet: '# Event recording chain:\n# 1. Component creates event\n# 2. Event recorder aggregates similar events\n# 3. Rate limiter controls event API calls\n# 4. Events stored in etcd\n\n# Controller manager flags:\n# --event-burst=100     (burst size for event creation)\n# --event-qps=50        (sustained events per second)\n\n# Events are aggregated:\n# Same source + same reason + same object + within 10min\n# -> incremented "count" field instead of new event\n\n# Check current event rate:\nkubectl get events -A --no-headers | wc -l\n\n# If etcd is overwhelmed:\n# 1. Reduce event TTL\n# 2. Tune rate limits on controllers\n# 3. Use separate etcd for events\n# kube-apiserver flag:\n# --etcd-servers-overrides=/events#https://etcd-events:2379',
    explanation: 'The Kubernetes event system uses aggregation and rate limiting. For high-churn clusters, use --etcd-servers-overrides to direct events to a separate etcd cluster, reducing load on the main etcd.',
    tags: ['kubernetes', 'events', 'rate-limiting', 'etcd', 'aggregation'],
  },
  {
    id: 'k8s-troubleshooting-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'A pod marked as critical (priorityClassName: system-cluster-critical) was unexpectedly evicted. The node had no resource pressure. What could cause this?',
    options: [
      'Critical pods can never be evicted',
      'An API-initiated eviction (e.g., from kubectl drain, cluster autoscaler scale-down, or a pod disruption controller) respects priority differently than kubelet eviction; API evictions can evict critical pods if no PDB protects them',
      'The priority value was wrong',
      'The node was deleted',
    ],
    correctAnswer: 1,
    codeSnippet: '# Two types of eviction:\n# 1. Kubelet-initiated (resource pressure)\n#    - Respects QoS class and priority\n#    - Critical pods evicted LAST\n#\n# 2. API-initiated (drain, autoscaler)\n#    - Respects PDB (PodDisruptionBudget) only\n#    - Does NOT consider pod priority for whether to evict\n#    - Priority only affects scheduling order after eviction\n\n# Protection: Create a PDB for critical pods\napiVersion: policy/v1\nkind: PodDisruptionBudget\nmetadata:\n  name: critical-pdb\nspec:\n  minAvailable: 1\n  selector:\n    matchLabels:\n      app: critical-app',
    explanation: 'API-initiated evictions (drain, autoscaler) do not consider pod priority; they only respect PDBs. Without a PDB, even system-critical pods can be evicted by drain or autoscaler operations.',
    tags: ['kubernetes', 'eviction', 'critical-pods', 'api-eviction', 'pdb'],
  },
  {
    id: 'k8s-troubleshooting-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A CSI driver supports volume snapshots but VolumeSnapshot creation hangs with no events. The VolumeSnapshotClass exists. What is the component that manages snapshot lifecycle?',
    options: [
      'The kubelet manages snapshots',
      'The external-snapshotter controller must be deployed separately from the CSI driver; it watches VolumeSnapshot objects and calls the CSI CreateSnapshot RPC',
      'The kube-controller-manager handles snapshots',
      'Snapshots are managed by etcd',
    ],
    correctAnswer: 1,
    codeSnippet: '# Volume snapshot components:\n# 1. VolumeSnapshot CRDs (must be installed)\n# 2. snapshot-controller (manages VolumeSnapshot objects)\n# 3. CSI external-snapshotter sidecar (calls CSI driver)\n\n# Check if components are running:\nkubectl get pods -n kube-system | grep snapshot\n\n# Check CRDs exist:\nkubectl get crd | grep volumesnapshot\n\n# Check VolumeSnapshotClass:\nkubectl get volumesnapshotclass\n\n# Create snapshot:\napiVersion: snapshot.storage.k8s.io/v1\nkind: VolumeSnapshot\nmetadata:\n  name: my-snapshot\nspec:\n  volumeSnapshotClassName: csi-snapclass\n  source:\n    persistentVolumeClaimName: my-pvc',
    explanation: 'Volume snapshots require three separate components: CRDs, snapshot-controller (cluster-level), and the CSI external-snapshotter sidecar (per CSI driver). Missing any component causes silent failures.',
    tags: ['kubernetes', 'csi', 'volume-snapshot', 'external-snapshotter', 'lifecycle'],
  },
  {
    id: 'k8s-troubleshooting-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A controller with proper RBAC can list resources but receives stale data from the API server. Subsequent GET calls return up-to-date data. What is happening at the API server caching layer?',
    options: [
      'The API server has a bug',
      'The controller is reading from the API server watch cache, which may be slightly stale; LIST calls with resourceVersion="" get data from the cache, while resourceVersion="0" gets the latest from etcd',
      'RBAC affects data freshness',
      'The controller client is caching locally',
    ],
    correctAnswer: 1,
    codeSnippet: '# API server caching behavior:\n# LIST with no resourceVersion:\n#   -> Served from watch cache (may be slightly stale)\n\n# LIST with resourceVersion="0":\n#   -> Served from watch cache (most recent cached version)\n\n# LIST with resourceVersion="":\n#   -> Served from etcd (fully consistent)\n\n# GET with resourceVersion="":\n#   -> Served from etcd (always fresh)\n\n# In Go client:\n# client.CoreV1().Pods("ns").List(ctx, metav1.ListOptions{\n#   ResourceVersion: "",  // Get from etcd\n# })\n\n# Watch cache staleness is typically milliseconds\n# but can be longer under API server load',
    explanation: 'The API server watch cache can serve slightly stale LIST responses. For strongly consistent reads, specify resourceVersion="" explicitly. GET requests without a resourceVersion always read from etcd.',
    tags: ['kubernetes', 'api-server', 'watch-cache', 'consistency', 'resourceversion'],
  },
  {
    id: 'k8s-troubleshooting-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A multi-cluster service mesh routes traffic between clusters. Services in Cluster A cannot reach services in Cluster B. The mesh control plane shows both clusters as connected. What layer should you investigate?',
    options: [
      'DNS configuration in each cluster',
      'The mesh data plane: cross-cluster gateway connectivity, mTLS certificate trust between clusters, and gateway load balancer health; also verify that the service export/import CRDs are correctly configured',
      'The Kubernetes version must match',
      'The CNI plugin must be the same',
    ],
    correctAnswer: 1,
    codeSnippet: '# Multi-cluster service mesh troubleshooting:\n# 1. Check gateway connectivity\nkubectl get svc -n istio-system istio-eastwestgateway\n\n# 2. Verify cross-cluster trust\nistioctl proxy-config secret <gateway-pod> | grep ROOTCA\n\n# 3. Check service export/import\nkubectl get serviceexport -A  # Cluster A\nkubectl get serviceimport -A  # Cluster B\n\n# 4. Verify gateway can reach remote cluster\nkubectl exec <gateway-pod> -- curl <remote-gateway-ip>:15443\n\n# 5. Check Envoy cluster configuration\nistioctl proxy-config cluster <pod> | grep <remote-service>\n\n# 6. Verify mTLS between clusters\nistioctl x authz check <pod>',
    explanation: 'Multi-cluster mesh issues involve multiple layers: gateway connectivity (L4), certificate trust (mTLS), service discovery (export/import CRDs), and data plane configuration (Envoy routing).',
    tags: ['kubernetes', 'multi-cluster', 'service-mesh', 'gateway', 'mtls'],
  },
  {
    id: 'k8s-troubleshooting-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'After upgrading containerd, the kubelet reports "node not found" errors and cannot register. The /etc/containerd/config.toml was regenerated. What containerd configuration is critical for kubelet integration?',
    options: [
      'The containerd logging level',
      'The CRI plugin configuration: sandbox image (pause container), cgroup driver must match kubelet (systemd vs cgroupfs), and the runtime handler must be configured correctly',
      'The containerd storage path',
      'The containerd network configuration',
    ],
    correctAnswer: 1,
    codeSnippet: '# Critical containerd config for Kubernetes:\n# /etc/containerd/config.toml\n\nversion = 2\n\n[plugins."io.containerd.grpc.v1.cri"]\n  # Sandbox (pause) image - must be pullable\n  sandbox_image = "registry.k8s.io/pause:3.9"\n\n  [plugins."io.containerd.grpc.v1.cri".containerd]\n    default_runtime_name = "runc"\n\n    [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]\n      runtime_type = "io.containerd.runc.v2"\n\n      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]\n        # MUST match kubelet cgroup driver\n        SystemdCgroup = true  # For systemd cgroup driver\n\n# If SystemdCgroup doesn\'t match kubelet:\n# kubelet uses systemd but containerd uses cgroupfs\n# -> pods fail to start, node goes NotReady',
    explanation: 'The containerd CRI plugin config must match the kubelet: cgroup driver (systemd/cgroupfs), sandbox image, and runtime handler. Mismatches after upgrades cause node registration failures.',
    tags: ['kubernetes', 'containerd', 'cri', 'cgroup-driver', 'configuration'],
  },
  {
    id: 'k8s-troubleshooting-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A Rust application container crashes with exit code 134 (SIGABRT) only under high concurrency. The application uses async I/O with tokio runtime. No panics are logged. What is the likely cause?',
    options: [
      'Rust applications cannot handle concurrency',
      'The tokio runtime thread count exceeds the container CPU limit, causing CFS throttling that triggers thread starvation; the runtime then aborts due to an unrecoverable state or the glibc allocator detects heap corruption from throttling-induced race conditions',
      'The container image is corrupt',
      'Async I/O is not supported in containers',
    ],
    correctAnswer: 1,
    codeSnippet: '# Tokio default thread count = num_cpus::get()\n# In a container, this may read host CPU count\n# e.g., 64 threads on a 64-core host with 500m CPU limit\n\n# Fix 1: Set explicit thread count\nenv:\n- name: TOKIO_WORKER_THREADS\n  value: "2"  # Match CPU request/limit\n\n# Fix 2: Use Go-style GOMAXPROCS approach\n# Read /sys/fs/cgroup/cpu/cpu.cfs_quota_us\n# and /sys/fs/cgroup/cpu/cpu.cfs_period_us\n# to determine actual CPU allocation\n\n# Fix 3: Ensure Rust/tokio reads container limits\n# Use num_cpus crate with cgroup awareness\n# or set --cpus explicitly in the runtime',
    explanation: 'Tokio (and many runtimes) default thread count to host CPU count, not container CPU limit. Excessive threads with CPU throttling causes thread starvation and can trigger aborts or heap corruption.',
    tags: ['kubernetes', 'rust', 'tokio', 'cpu-throttling', 'thread-count'],
  },
  {
    id: 'k8s-troubleshooting-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'After enabling Pod Security Standards (restricted profile), all startup probes using exec commands fail immediately. The containers are healthy when probes are removed. What is the PSS restriction causing this?',
    options: [
      'The restricted profile does not allow probes',
      'The restricted profile requires runAsNonRoot and may enforce seccomp profile that restricts the exec probe from forking processes or accessing specific paths needed by the probe command',
      'The restricted profile requires HTTPS probes',
      'Startup probes are not supported under PSS',
    ],
    correctAnswer: 1,
    codeSnippet: '# Restricted PSS requirements:\n# - runAsNonRoot: true\n# - allowPrivilegeEscalation: false\n# - seccompProfile: RuntimeDefault or Localhost\n# - capabilities.drop: ["ALL"]\n\n# Exec probe runs a process in the container\n# If the probe command requires:\n# - Writing to a directory (readOnlyRootFilesystem)\n# - Accessing a restricted syscall (seccomp)\n# - Running as root (runAsNonRoot)\n# The probe fails\n\n# Example failing probe:\nstartupProbe:\n  exec:\n    command: ["sh", "-c", "curl localhost:8080/health"]\n# \'sh\' may not exist in distroless images\n# \'curl\' requires network syscalls that may be blocked\n\n# Fix: Use HTTP/TCP probes instead\nstartupProbe:\n  httpGet:\n    path: /health\n    port: 8080',
    explanation: 'Pod Security Standards restrict container behavior. Exec probes run within these restrictions, so if the probe command requires capabilities, syscalls, or filesystem access that is restricted, the probe fails.',
    tags: ['kubernetes', 'pod-security-standards', 'probes', 'restricted', 'seccomp'],
  },
  {
    id: 'k8s-troubleshooting-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You need to debug a containerd-level issue where a container is stuck in "Created" state and never transitions to "Running". kubectl debug cannot help because the container never starts. What is the approach?',
    options: [
      'Restart the kubelet',
      'Use crictl on the node to inspect the container state, check containerd shim logs, and examine the OCI runtime (runc) output for the specific container create operation',
      'Delete and recreate the pod',
      'Upgrade containerd',
    ],
    correctAnswer: 1,
    codeSnippet: '# Debug at the container runtime level\n# 1. Find the container ID\ncrictl ps -a | grep <pod-name>\n\n# 2. Inspect container details\ncrictl inspect <container-id>\n# Look for: state, created_at, started_at, exit_code\n\n# 3. Check containerd events\nctr --namespace k8s.io events\n\n# 4. Check the OCI runtime (runc) directly\n# Find the runc state\nrunc --root /run/containerd/runc/k8s.io state <container-id>\n\n# 5. Check containerd shim logs\njournalctl -u containerd | grep <container-id>\n\n# 6. Check for resource issues preventing container start\n# - cgroup creation failures\n# - seccomp/AppArmor profile loading errors\n# - Mount point issues',
    explanation: 'When containers are stuck at the runtime level, kubectl cannot help. Use crictl (CRI interface) and runc (OCI runtime) directly on the node to inspect the container lifecycle and identify the failure point.',
    tags: ['kubernetes', 'crictl', 'containerd', 'runc', 'runtime-debugging'],
  },
  {
    id: 'k8s-troubleshooting-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container using tmpfs (emptyDir with medium: Memory) is OOMKilled despite the application using minimal heap memory. The tmpfs mount has grown to 500Mi. How does tmpfs memory accounting work?',
    options: [
      'tmpfs does not count toward container memory',
      'tmpfs backed by memory (emptyDir medium: Memory) counts against the container cgroup memory limit; data written to the tmpfs mount consumes memory from the container memory quota',
      'tmpfs uses disk space, not memory',
      'tmpfs memory is counted separately',
    ],
    correctAnswer: 1,
    codeSnippet: '# emptyDir with medium: Memory creates tmpfs\nvolumes:\n- name: cache\n  emptyDir:\n    medium: Memory\n    sizeLimit: 1Gi  # Limits tmpfs size, but memory\n                     # still counts against container limit!\n\n# Container memory limit: 1Gi\n# Application heap: 300Mi\n# tmpfs data: 500Mi\n# Total charged to cgroup: 800Mi+ (approaching limit!)\n\n# tmpfs pages are charged to:\n# memory.stat -> shmem (shared memory)\n# This is part of the cgroup memory total\n\n# Fix: Account for tmpfs in memory limits\nresources:\n  limits:\n    memory: "2Gi"  # App (500Mi) + tmpfs (1Gi) + overhead\n\n# Or use disk-backed emptyDir instead:\nvolumes:\n- name: cache\n  emptyDir: {}  # Uses node disk, not memory',
    explanation: 'tmpfs (emptyDir medium: Memory) data is backed by RAM and charged to the container cgroup. This memory counts toward the container memory limit, often causing unexpected OOMKills.',
    tags: ['kubernetes', 'tmpfs', 'emptydir', 'memory', 'oomkilled'],
  },
  {
    id: 'k8s-troubleshooting-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'TCP connections between pods are being reset after exactly 120 seconds of idle time. The application sets keepalive to 300 seconds. Neither kube-proxy nor any NetworkPolicy has a 120-second timeout. What is causing this?',
    options: [
      'The application keepalive is not working',
      'The cloud load balancer or NAT gateway has a 120-second idle connection timeout; conntrack entries for NATed connections expire, and when the keepalive probe arrives, the NAT mapping is gone, causing a RST',
      'The CNI plugin has a connection limit',
      'TCP keepalive does not work in containers',
    ],
    correctAnswer: 1,
    codeSnippet: '# Connection timeout chain:\n# 1. Application keepalive: 300s\n# 2. kube-proxy conntrack: default 432000s (5 days)\n# 3. Cloud NAT/LB idle timeout: 120s  <- culprit!\n\n# The cloud LB/NAT drops the connection mapping\n# at 120s. When keepalive at 300s tries to use it,\n# the NAT entry is gone -> RST\n\n# Fix: Set TCP keepalive LOWER than NAT timeout\n# In pod:\nenv:\n- name: TCP_KEEPALIVE_TIME\n  value: "60"  # Start keepalive probes at 60s\n\n# Or via sysctls:\nsecurityContext:\n  sysctls:\n  - name: net.ipv4.tcp_keepalive_time\n    value: "60"\n  - name: net.ipv4.tcp_keepalive_intvl\n    value: "10"\n  - name: net.ipv4.tcp_keepalive_probes\n    value: "3"',
    explanation: 'Cloud NAT gateways and load balancers have their own idle connection timeouts. TCP keepalive must be set lower than the NAT timeout to keep connections alive through the NAT mapping.',
    tags: ['kubernetes', 'tcp-keepalive', 'nat-timeout', 'connection-reset', 'cloud'],
  },
  {
    id: 'k8s-troubleshooting-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'The kube-scheduler performance is degraded. Scheduling latency is 500ms+ per pod. The cluster has 5000 nodes and 100,000 pods. What scheduler configuration tuning helps?',
    options: [
      'Add more scheduler replicas',
      'Tune percentageOfNodesToScore to limit the number of feasible nodes evaluated, enable scheduler caching, and use scheduler profiles with appropriate plugin configurations',
      'Move to a custom scheduler',
      'Reduce the number of nodes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Scheduler performance tuning\napiVersion: kubescheduler.config.k8s.io/v1\nkind: KubeSchedulerConfiguration\npercentageOfNodesToScore: 10  # Only score 10% of nodes\n# Default: 50% (scales down with cluster size)\n# With 5000 nodes: scores ~500 nodes instead of 2500\n\n# Parallelism for scheduling\nparallelism: 16  # Default: 16, can be increased\n\n# Profile optimization: disable unused plugins\nprofiles:\n- schedulerName: default-scheduler\n  plugins:\n    score:\n      disabled:\n      - name: PodTopologySpread  # If not used\n      - name: InterPodAffinity   # If not used\n\n# Also: ensure informer cache is warming properly\n# Check scheduler metrics:\n# scheduler_scheduling_attempt_duration_seconds\n# scheduler_pending_pods',
    explanation: 'percentageOfNodesToScore limits how many nodes the scheduler evaluates, dramatically reducing scheduling latency in large clusters. Disabling unused plugins and tuning parallelism also helps.',
    tags: ['kubernetes', 'scheduler', 'performance', 'large-cluster', 'tuning'],
  },
  {
    id: 'k8s-troubleshooting-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service with 500 endpoints in iptables-based kube-proxy mode causes high CPU on all nodes during endpoint updates. What is the architectural issue?',
    options: [
      'iptables cannot handle 500 endpoints',
      'kube-proxy in iptables mode rewrites ALL iptables rules for ALL services on every endpoint change, and with large numbers of services/endpoints, this full-sync becomes extremely expensive',
      'The Service needs to be split',
      'Endpoints are limited to 100 per Service',
    ],
    correctAnswer: 1,
    codeSnippet: '# iptables mode: O(n) rule count, O(n^2) update cost\n# With 500 endpoints per service and 100 services:\n# Total rules: 50,000+ iptables entries\n# Each endpoint change -> full iptables reload\n\n# Metrics to watch:\n# kubeproxy_sync_proxy_rules_duration_seconds\n# kubeproxy_sync_proxy_rules_iptables_total\n\n# Solutions:\n# 1. Switch to IPVS mode (O(1) updates)\napiVersion: kubeproxy.config.k8s.io/v1alpha1\nkind: KubeProxyConfiguration\nmode: "ipvs"\n\n# 2. Switch to eBPF-based CNI (Cilium)\n# 3. Use EndpointSlices (default since 1.21)\n#    to reduce the size of endpoint updates\n\n# IPVS advantages:\n# - Hash-based lookups: O(1)\n# - Incremental updates: only changed backends\n# - Multiple LB algorithms: rr, lc, wlc, sh',
    explanation: 'kube-proxy iptables mode uses linear chain matching and full-table rewrites. With many endpoints, every change triggers a complete iptables reload, consuming significant CPU. IPVS provides O(1) updates.',
    tags: ['kubernetes', 'kube-proxy', 'iptables', 'scalability', 'ipvs'],
  },
  {
    id: 'k8s-troubleshooting-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A cluster experiences intermittent scheduling failures with "too many open files" errors from the kube-scheduler. The scheduler runs as a static pod on control plane nodes. What is the root cause?',
    options: [
      'The scheduler has a memory leak',
      'The scheduler opens watch connections to the API server for each informer; the container or process file descriptor limit (ulimit -n) is too low for the number of watch connections needed in a large cluster',
      'etcd is running out of file descriptors',
      'The container runtime limits are too low',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check scheduler file descriptor usage\n# On control plane node:\nls /proc/$(pgrep kube-scheduler)/fd | wc -l\n\n# Check limits\ncat /proc/$(pgrep kube-scheduler)/limits | grep "Max open files"\n\n# Fix: Increase limits for the scheduler\n# In static pod manifest or systemd unit:\n# /etc/kubernetes/manifests/kube-scheduler.yaml\nspec:\n  containers:\n  - name: kube-scheduler\n    # Cannot set ulimits in pod spec directly\n    # Must configure via container runtime\n\n# For containerd:\n# /etc/containerd/config.toml\n# [plugins."io.containerd.grpc.v1.cri"]\n#   [plugins."io.containerd.grpc.v1.cri".containerd]\n#     [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]\n#       [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options.Rlimits]\n#         RLIMIT_NOFILE = 1048576',
    explanation: 'The kube-scheduler maintains many watch connections (informers) to the API server. In large clusters, the number of file descriptors for these connections can exceed the container/process ulimit.',
    tags: ['kubernetes', 'scheduler', 'file-descriptors', 'ulimits', 'informers'],
  },
  {
    id: 'k8s-troubleshooting-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'A Kubernetes controller is producing "too old resource version" (410 Gone) errors in its logs, causing it to miss events and enter a broken state. What is happening?',
    options: [
      'The controller API permissions expired',
      'The watch connection resourceVersion is older than what etcd has retained (etcd compacted the revision); the controller must handle 410 Gone by re-listing from the current state',
      'The events are being deleted too quickly',
      'The controller Kubernetes client is outdated',
    ],
    correctAnswer: 1,
    codeSnippet: '# Watch lifecycle:\n# 1. List all resources -> get resourceVersion (rv=100)\n# 2. Watch from rv=100 -> stream changes\n# 3. etcd compacts old revisions (e.g., rv < 95)\n# 4. Watch connection breaks\n# 5. Controller reconnects with rv=100\n# 6. etcd returns 410 Gone (rv=100 is compacted!)\n\n# Proper handling:\n# 1. On 410 Gone: re-list from current state\n# 2. Use informer framework (handles this automatically)\n# 3. Don\'t cache resourceVersion for too long\n\n# etcd compaction settings:\n# --auto-compaction-mode=periodic\n# --auto-compaction-retention=5m  # Retain 5 min of history\n\n# If 410s are frequent:\n# - Increase etcd compaction retention\n# - Fix controller to handle 410 properly\n# - Use reflector/informer pattern',
    explanation: 'etcd compacts old revisions periodically. If a watch/list uses a resourceVersion older than the earliest retained revision, etcd returns 410 Gone. Controllers must handle this by re-listing.',
    tags: ['kubernetes', 'watch', '410-gone', 'etcd-compaction', 'resourceversion'],
  },
  {
    id: 'k8s-troubleshooting-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'CoreDNS crashes under high query load with "fatal error: runtime: out of memory". The pod has a 256Mi memory limit and serves 50,000 queries/second. What is the fix beyond increasing memory?',
    options: [
      'Use a different DNS server',
      'Optimize the CoreDNS configuration: reduce cache size, disable unnecessary plugins, deploy more replicas with HPA, and implement NodeLocal DNSCache to reduce load',
      'Increase ndots to reduce queries',
      'Deploy CoreDNS outside the cluster',
    ],
    correctAnswer: 1,
    codeSnippet: '# CoreDNS optimization for high load:\n\n# 1. Horizontal scaling with HPA\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: coredns\n  namespace: kube-system\nspec:\n  scaleTargetRef:\n    kind: Deployment\n    name: coredns\n  minReplicas: 3\n  maxReplicas: 20\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target:\n        type: Utilization\n        averageUtilization: 70\n\n# 2. Optimize Corefile\n.:53 {\n    errors\n    health\n    ready\n    kubernetes cluster.local in-addr.arpa ip6.arpa {\n        pods insecure\n        fallthrough in-addr.arpa ip6.arpa\n    }\n    cache 60  # Increase cache TTL\n    forward . /etc/resolv.conf {\n        max_concurrent 5000  # Increase concurrent forwards\n    }\n    bufsize 1232  # Optimal for avoiding EDNS fallback\n}\n\n# 3. Deploy NodeLocal DNSCache\n# Reduces CoreDNS load by ~90% for cached entries',
    explanation: 'Under extreme load, CoreDNS needs horizontal scaling (HPA), configuration optimization (cache tuning, concurrent forwards), and NodeLocal DNSCache to offload queries from the central CoreDNS.',
    tags: ['kubernetes', 'coredns', 'performance', 'hpa', 'scaling'],
  },
  {
    id: 'k8s-troubleshooting-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container crash generates a core dump that is larger than the emptyDir volume. The pod enters CrashLoopBackOff and filling the emptyDir triggers disk pressure on the node. How do you safely handle core dumps in Kubernetes?',
    options: [
      'Disable core dumps entirely',
      'Configure the kernel core_pattern to pipe to a handler that limits size and uploads to external storage, and set emptyDir sizeLimit to prevent node disk pressure',
      'Increase the node disk',
      'Use PersistentVolumes for core dumps',
    ],
    correctAnswer: 1,
    codeSnippet: '# Configure core dump handling:\n# 1. Limit emptyDir to prevent node impact\nvolumes:\n- name: coredumps\n  emptyDir:\n    sizeLimit: 500Mi  # Prevents filling node disk\n\n# 2. Configure kernel core_pattern on nodes\n# /proc/sys/kernel/core_pattern\n# |/usr/bin/core_handler %e %p %t\n# Pipes core dumps to a handler that:\n# - Truncates large dumps\n# - Uploads to S3/GCS\n# - Removes after upload\n\n# 3. Use ulimit to limit core size per container\n# In containerd config:\n# RLIMIT_CORE = 500000000  # 500MB max\n\n# 4. Or use securityContext to limit\nsecurityContext:\n  sysctls:\n  - name: kernel.core_pipe_limit\n    value: "1"',
    explanation: 'Core dumps from crashing containers can fill node storage. Use emptyDir sizeLimit, kernel core_pattern piping to a size-limiting handler, and ulimit RLIMIT_CORE to control core dump behavior.',
    tags: ['kubernetes', 'core-dump', 'disk-pressure', 'emptydir', 'crash-handling'],
  },
  {
    id: 'k8s-troubleshooting-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'An in-tree volume plugin (e.g., kubernetes.io/aws-ebs) is deprecated and migration to CSI is required. During the migration, some volumes fail to attach. What is the CSI migration mechanism and what can go wrong?',
    options: [
      'In-tree and CSI plugins cannot coexist',
      'The CSI migration feature translates in-tree volume operations to CSI calls at the API level; failures occur if the CSI driver is not installed, the feature gate is mismatched across components, or the CSI driver version does not support all in-tree features',
      'You must manually convert all PV specs',
      'CSI migration requires cluster recreation',
    ],
    correctAnswer: 1,
    codeSnippet: '# CSI Migration mechanism:\n# 1. Feature gate: CSIMigration, CSIMigrationAWS (etc.)\n# 2. When enabled, the API server translates:\n#    kubernetes.io/aws-ebs -> ebs.csi.aws.com\n# 3. All operations go through the CSI driver\n\n# Common migration failures:\n# 1. CSI driver not installed\nkubectl get csidrivers\n# Must show: ebs.csi.aws.com\n\n# 2. Feature gate mismatch\n# Must be enabled on: API server, controller-manager,\n# kubelet, AND kube-scheduler\n\n# 3. CSI driver missing capabilities\n# Some in-tree features may not be in CSI driver yet\n\n# 4. RBAC: CSI driver needs permissions\n# to manage VolumeAttachments\n\n# Check migration status:\nkubectl get pv <pv-name> -o yaml\n# Should show CSI spec, not in-tree spec\n# annotations:\n#   pv.kubernetes.io/migrated-to: ebs.csi.aws.com',
    explanation: 'CSI migration transparently translates in-tree volume operations to CSI calls. Failures occur when the CSI driver is missing, feature gates are inconsistent across components, or the CSI driver lacks feature parity.',
    tags: ['kubernetes', 'csi-migration', 'in-tree', 'volume-plugin', 'feature-gate'],
  },
  {
    id: 'k8s-troubleshooting-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A ValidatingAdmissionPolicy (CEL-based, Kubernetes 1.28+) is rejecting requests but the error message does not indicate which policy is responsible. How do you identify the rejecting policy?',
    options: [
      'Check kube-proxy logs',
      'Enable API server audit logging at RequestResponse level and inspect the audit annotations for admission.k8s.io/validation_result which identifies the specific policy and expression that rejected the request',
      'ValidatingAdmissionPolicies do not produce audit data',
      'Check the etcd logs',
    ],
    correctAnswer: 1,
    codeSnippet: '# Enable audit logging for admission decisions\napiVersion: audit.k8s.io/v1\nkind: Policy\nrules:\n- level: RequestResponse\n  resources:\n  - group: ""\n    resources: ["pods"]\n  omitStages: ["RequestReceived"]\n\n# Check audit log for rejection details:\n# "annotations": {\n#   "validation.policy.admission.k8s.io/validation_failure":\n#     "[{\"message\":\"denied by policy: require-labels\",\n#        \"policy\":\"require-labels.k8s.io\",\n#        \"binding\":\"require-labels-binding\",\n#        \"expressionIndex\":0}]"\n# }\n\n# List all ValidatingAdmissionPolicies\nkubectl get validatingadmissionpolicies\n\n# Check specific policy\nkubectl get validatingadmissionpolicy require-labels -o yaml',
    explanation: 'ValidatingAdmissionPolicies include their identity in API server audit annotations. Enable RequestResponse audit level to see which specific policy and CEL expression rejected the request.',
    tags: ['kubernetes', 'validating-admission-policy', 'cel', 'audit', 'debugging'],
  },
  {
    id: 'k8s-troubleshooting-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'After adding a GPU node to the cluster, pods requesting nvidia.com/gpu resources stay Pending even though the GPU node is Ready. What device plugin mechanism must be working?',
    options: [
      'GPUs are automatically detected by the kubelet',
      'The NVIDIA device plugin DaemonSet must be running on the GPU node, registering GPU resources with the kubelet via the device plugin API (unix socket at /var/lib/kubelet/device-plugins/)',
      'GPU support requires a custom scheduler',
      'GPUs are configured via node labels only',
    ],
    correctAnswer: 1,
    codeSnippet: '# GPU device plugin chain:\n# 1. Install NVIDIA drivers on the node\n# 2. Install nvidia-container-toolkit\n# 3. Deploy NVIDIA device plugin DaemonSet\nkubectl apply -f https://...nvidia-device-plugin.yml\n\n# Verify device plugin is running\nkubectl get pods -n kube-system -l name=nvidia-device-plugin-ds\n\n# Check node allocatable resources\nkubectl describe node <gpu-node> | grep nvidia\n# Capacity:\n#   nvidia.com/gpu: 2\n# Allocatable:\n#   nvidia.com/gpu: 2\n\n# If nvidia.com/gpu is not shown:\n# 1. Check device plugin logs\nkubectl logs -n kube-system <nvidia-device-plugin-pod>\n\n# 2. Check kubelet device plugin socket\nls /var/lib/kubelet/device-plugins/\n# Should show nvidia.com-gpu.sock\n\n# 3. Check NVIDIA driver on node\nnvidia-smi',
    explanation: 'GPU resources are managed by the device plugin framework. The NVIDIA device plugin runs as a DaemonSet, discovers GPUs via nvidia-smi, and registers them as extended resources with the kubelet.',
    tags: ['kubernetes', 'gpu', 'device-plugin', 'nvidia', 'extended-resources'],
  },
  {
    id: 'k8s-troubleshooting-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'init-container-failures',
    question: 'A pod with a native sidecar (restartPolicy: Always init container) fails to start because the sidecar depends on a volume that is populated by a preceding regular init container. The regular init container has not run yet. Why?',
    options: [
      'Native sidecars run after all init containers',
      'Init containers run in the order defined in the spec; if the native sidecar is defined before the regular init container, it starts first and the volume is empty, causing failure',
      'Volumes are not available to init containers',
      'Native sidecars cannot use volumes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Problematic ordering:\ninitContainers:\n- name: sidecar-proxy         # Starts first (index 0)\n  restartPolicy: Always       # Native sidecar\n  image: proxy-image\n  volumeMounts:\n  - name: config\n    mountPath: /config        # Volume empty!\n\n- name: config-init           # Runs second (index 1)\n  image: config-fetcher\n  command: ["fetch-config"]\n  volumeMounts:\n  - name: config\n    mountPath: /config        # Populates volume\n\n# Fix: Reorder init containers\ninitContainers:\n- name: config-init           # Runs first (index 0)\n  image: config-fetcher\n  command: ["fetch-config"]   # Populates volume\n  volumeMounts:\n  - name: config\n    mountPath: /config\n\n- name: sidecar-proxy         # Starts second (index 1)\n  restartPolicy: Always       # Native sidecar\n  image: proxy-image\n  volumeMounts:\n  - name: config\n    mountPath: /config        # Volume populated!',
    explanation: 'Init containers (including native sidecars) run in spec order. If a sidecar depends on data from a preceding init container, it must come after that init container in the spec.',
    tags: ['kubernetes', 'native-sidecar', 'ordering', 'init-containers', 'volumes'],
  },
  {
    id: 'k8s-troubleshooting-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'A cluster uses WireGuard encryption for pod-to-pod traffic via the CNI plugin. Some pods experience high latency. What WireGuard-specific issues should you investigate?',
    options: [
      'WireGuard does not support Kubernetes',
      'Check MTU configuration (WireGuard overhead reduces effective MTU), CPU usage for encryption/decryption on the node, key rotation failures between nodes, and whether the WireGuard kernel module is loaded',
      'WireGuard only encrypts external traffic',
      'The container runtime does not support encryption',
    ],
    correctAnswer: 1,
    codeSnippet: '# WireGuard troubleshooting:\n# 1. Check WireGuard interface\nwg show\n# Look for: latest handshake, transfer stats, endpoints\n\n# 2. Check MTU (WireGuard overhead is ~60 bytes)\nip link show wg0\n# MTU should be: node_MTU - 60 (e.g., 1440 for 1500 MTU)\n# If MTU is wrong: fragmentation causes latency\n\n# 3. Check CPU impact of encryption\ntop  # Look for ksoftirqd or wg processes using CPU\n\n# 4. Check for key rotation issues\nwg show all latest-handshakes\n# If handshake is old -> connection may be stale\n\n# 5. Verify kernel module\nlsmod | grep wireguard\n\n# Calico WireGuard:\nkubectl get felixconfiguration default -o yaml\n# wireguardEnabled: true\n# wireguardMTU: 1440',
    explanation: 'WireGuard adds encryption overhead (~60 bytes MTU reduction) and CPU cost. MTU misconfiguration causes fragmentation and retransmission, and key rotation issues can disrupt connections.',
    tags: ['kubernetes', 'wireguard', 'encryption', 'mtu', 'latency'],
  },
  {
    id: 'k8s-troubleshooting-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'After migrating from cgroup v1 to cgroup v2, a container that previously ran stably now intermittently gets OOMKilled. Memory limits are unchanged. What cgroup v2 behavior change causes this?',
    options: [
      'cgroup v2 uses different memory units',
      'cgroup v2 accounts file page cache more aggressively to the cgroup that opened the files; in v1, page cache was more easily reclaimed globally; in v2, the cgroup must reclaim its own cached pages, hitting the limit sooner',
      'cgroup v2 does not support memory limits',
      'The container runtime changed memory accounting',
    ],
    correctAnswer: 1,
    codeSnippet: '# cgroup v1 page cache behavior:\n# File page cache could be reclaimed globally\n# by the kernel regardless of which cgroup created it\n\n# cgroup v2 page cache behavior:\n# Page cache is charged to the cgroup that opened the file\n# Reclaim is cgroup-local first, then global\n\n# This means:\n# - I/O-heavy workloads see higher memory usage in v2\n# - The cgroup must reclaim its OWN cache when under pressure\n# - Global reclaim is still possible but cgroup limit\n#   may be hit before global reclaim kicks in\n\n# Mitigation:\n# 1. Increase memory limits to account for cached I/O\n# 2. Monitor memory.stat for \'file\' (page cache) vs \'anon\'\ncat /sys/fs/cgroup/kubepods.slice/.../memory.stat\n# file: shows file-backed cache charged to cgroup\n\n# 3. Consider memory.high for soft throttling instead\n# of hard OOM at memory.max',
    explanation: 'cgroup v2 more strictly associates page cache with the owning cgroup. Workloads with heavy file I/O may see higher effective memory usage under v2 because cache is not as freely reclaimable globally.',
    tags: ['kubernetes', 'cgroup-v2', 'page-cache', 'memory-accounting', 'migration'],
  },
  {
    id: 'k8s-troubleshooting-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A pod requesting "example.com/special-resource: 1" (extended resource) stays Pending. The node reports the resource as available. The pod spec also has a ResourceClaim referencing a DynamicResourceAllocation (DRA) resource. What is the scheduling flow?',
    options: [
      'Extended resources and DRA cannot be used together',
      'DRA introduces a new scheduling flow: the scheduler must find a node where both the extended resource is available AND the DRA resource driver can allocate the claim; the claim allocation may fail even if the extended resource is available',
      'The ResourceClaim is automatically fulfilled',
      'DRA resources do not affect scheduling',
    ],
    correctAnswer: 1,
    codeSnippet: '# DRA scheduling flow (alpha in 1.26+, beta in 1.31):\n# 1. Pod has ResourceClaim\n# 2. Scheduler filters: find nodes where claim can be allocated\n# 3. DRA driver\'s scheduler plugin reports feasible nodes\n# 4. Scheduler also checks traditional resources (CPU, mem, etc.)\n# 5. DRA driver allocates the claim on the chosen node\n# 6. Kubelet calls DRA driver NodePrepareResource\n\n# Debugging:\nkubectl get resourceclaim <claim-name> -o yaml\n# Check: allocation, reservedFor, status\n\nkubectl get resourceclass <class-name> -o yaml\n# Check: driver name, parameters\n\n# Check DRA driver pods\nkubectl get pods -n kube-system -l app=<dra-driver>\n\n# Check scheduler for DRA-related errors\nkubectl logs -n kube-system kube-scheduler | grep DRA',
    explanation: 'Dynamic Resource Allocation adds a new scheduling dimension. The DRA driver must report node feasibility, and the claim must be successfully allocated on the chosen node, adding potential failure points.',
    tags: ['kubernetes', 'dra', 'dynamic-resource-allocation', 'scheduling', 'extended-resources'],
  },
  {
    id: 'k8s-troubleshooting-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'A cluster uses a hardened container runtime (gVisor/runsc). kubectl exec fails with "exec not supported" or unusual behavior. How do you debug pods in a gVisor environment?',
    options: [
      'gVisor pods cannot be debugged',
      'gVisor intercepts syscalls through its kernel; kubectl exec works but with limitations. Use gVisor-specific tools (runsc debug) or create a debug pod copy with the standard runc runtime for full debugging capabilities',
      'Switch to containerd for debugging',
      'gVisor has its own kubectl plugin',
    ],
    correctAnswer: 1,
    codeSnippet: '# gVisor (runsc) debugging:\n# 1. kubectl exec works but is limited by gVisor\'s\n#    syscall compatibility\n\n# 2. Use runsc debug commands on the node\nrunsc --root /run/containerd/runsc/k8s.io \\\n  debug --stacks <container-id>\n\n# 3. Create debug pod copy with standard runtime\nkubectl debug my-pod --copy-to=debug-pod \\\n  --set-image=*=ubuntu\n\n# Override the RuntimeClass to use runc:\n# Edit the debug pod to use standard runtime\n# (remove runtimeClassName or set to runc)\n\n# 4. gVisor application logs\nrunsc --root /run/containerd/runsc/k8s.io \\\n  debug --log-dir=/tmp/gvisor-logs <container-id>\n\n# 5. Check gVisor-specific errors\njournalctl | grep runsc',
    explanation: 'gVisor provides syscall-level isolation, which can limit kubectl exec behavior. Use runsc debug tools directly or create pod copies with the standard runc runtime for unrestricted debugging.',
    tags: ['kubernetes', 'gvisor', 'runsc', 'sandbox-runtime', 'debugging'],
  },
  {
    id: 'k8s-troubleshooting-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service with internal traffic policy (internalTrafficPolicy: Local) causes connection failures when the local pod is not Ready but exists on the node. What is the exact behavior?',
    options: [
      'Traffic is routed to pods on other nodes as fallback',
      'With internalTrafficPolicy: Local, traffic is only routed to Ready endpoints on the same node; if no local pod is Ready, the connection fails even if Ready pods exist on other nodes',
      'Non-ready pods still receive traffic',
      'The policy is ignored for ClusterIP services',
    ],
    correctAnswer: 1,
    codeSnippet: 'apiVersion: v1\nkind: Service\nmetadata:\n  name: my-service\nspec:\n  internalTrafficPolicy: Local  # Only local endpoints\n  selector:\n    app: my-app\n  ports:\n  - port: 80\n\n# Behavior:\n# Node A: pod Running but NOT Ready -> no endpoint\n# Node A: client pod tries to connect -> FAILS\n# Node B: pod Running and Ready -> has endpoint\n# But Node A client cannot reach Node B pod!\n\n# Mitigation:\n# - Ensure sufficient replicas on each node (DaemonSet)\n# - Use preferLocalEndpoints via topology hints instead\n# - Monitor local endpoint availability per node',
    explanation: 'internalTrafficPolicy: Local strictly limits traffic to Ready endpoints on the same node. There is no fallback to remote endpoints, making it critical to ensure local pod availability.',
    tags: ['kubernetes', 'internal-traffic-policy', 'local', 'service', 'endpoint'],
  },
  {
    id: 'k8s-troubleshooting-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container using a custom OCI runtime hook fails on some nodes but works on others. The pod spec is identical. What infrastructure difference would cause this?',
    options: [
      'The pod labels are different per node',
      'OCI runtime hooks are configured at the container runtime level on the node; some nodes may have different containerd configurations, missing hook binaries, or different hook paths',
      'OCI hooks are not supported in Kubernetes',
      'The CNI plugin differs between nodes',
    ],
    correctAnswer: 1,
    codeSnippet: '# OCI hooks are node-level configuration\n# /etc/containers/oci/hooks.d/ or\n# /usr/share/containers/oci/hooks.d/\n\n# Example hook:\n# {\n#   "version": "1.0.0",\n#   "hook": {\n#     "path": "/usr/bin/nvidia-container-toolkit",\n#     "args": ["hook", "--config", "/etc/nvidia-container-runtime/config.toml"]\n#   },\n#   "when": {\n#     "always": true\n#   },\n#   "stages": ["prestart"]\n# }\n\n# Troubleshooting:\n# 1. Check hook configuration on failing node\nls /etc/containers/oci/hooks.d/\n# 2. Verify hook binary exists and is executable\nwhich nvidia-container-toolkit\n# 3. Check containerd config for hook directories\ncat /etc/containerd/config.toml | grep hook\n# 4. Check runc logs for hook execution errors\njournalctl -u containerd | grep hook',
    explanation: 'OCI runtime hooks are configured at the node level, not in the pod spec. Inconsistent hook configuration across nodes (missing binaries, different paths, permission issues) causes node-specific failures.',
    tags: ['kubernetes', 'oci-hooks', 'runtime', 'node-configuration', 'inconsistency'],
  },
  {
    id: 'k8s-troubleshooting-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'A cluster has both IPv4 and IPv6 pods (dual-stack). A DNS query for a Service returns both A and AAAA records. An IPv4-only application pod fails to connect because it receives the AAAA record first. How do you fix this?',
    options: [
      'Disable IPv6 on the cluster',
      'Set the Service ipFamilyPolicy to SingleStack with ipFamilies: [IPv4], or configure the application to prefer IPv4 (AI_ADDRCONFIG) or use the dns config to control resolution order',
      'The application must support IPv6',
      'Change the Service type to NodePort',
    ],
    correctAnswer: 1,
    codeSnippet: '# Fix 1: Make Service IPv4 only\napiVersion: v1\nkind: Service\nspec:\n  ipFamilyPolicy: SingleStack\n  ipFamilies:\n  - IPv4  # Only allocate IPv4 ClusterIP\n\n# Fix 2: Control DNS response ordering\n# Some resolvers try AAAA first, then A\n# Application should use AI_ADDRCONFIG flag\n# which only returns addresses for families\n# the system has configured\n\n# Fix 3: Pod dnsConfig\ndnsConfig:\n  options:\n  - name: single-request-reopen\n    # Prevents parallel A + AAAA queries\n    # Reduces chance of wrong-family response',
    explanation: 'In dual-stack clusters, DNS returns both A (IPv4) and AAAA (IPv6) records. Applications must handle both or the Service should be configured as SingleStack for the desired IP family.',
    tags: ['kubernetes', 'dual-stack', 'dns', 'ipv4', 'ipv6', 'service'],
  },
  {
    id: 'k8s-troubleshooting-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A Kubernetes cluster shows increasing API server latency over time. The etcd database size is growing despite no significant increase in workloads. What is the likely cause and how do you investigate?',
    options: [
      'The API server needs more memory',
      'Stale objects (orphaned events, completed Jobs, old ReplicaSets) are accumulating in etcd; investigate with etcdctl endpoint status for DB size, check for disabled garbage collection controllers, and verify TTL-based cleanup is working',
      'etcd disks are too slow',
      'The cluster needs more nodes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check etcd database size\netcdctl --endpoints=https://127.0.0.1:2379 \\\n  endpoint status --write-out=table\n# DB SIZE should be < 8GB (max recommended)\n\n# Common causes of etcd growth:\n# 1. Events not being garbage collected\nkubectl get events -A --no-headers | wc -l\n\n# 2. Completed Jobs not cleaned up\nkubectl get jobs -A --field-selector status.successful=1 | wc -l\n\n# 3. Old ReplicaSets from Deployments\nkubectl get rs -A --no-headers | wc -l\n\n# Fix:\n# 1. Set TTL for Jobs\nspec:\n  ttlSecondsAfterFinished: 3600\n\n# 2. Limit Deployment revision history\nspec:\n  revisionHistoryLimit: 3\n\n# 3. Compact and defrag etcd\netcdctl compact <revision>\netcdctl defrag',
    explanation: 'etcd grows with accumulated objects like events, completed Jobs, and old ReplicaSets. Without proper garbage collection (TTL, revisionHistoryLimit), etcd size increases, degrading API server performance.',
    tags: ['kubernetes', 'etcd', 'database-growth', 'garbage-collection', 'performance'],
  },
  {
    id: 'k8s-troubleshooting-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'After implementing a gRPC health check probe, the kubelet reports "rpc error: code = Unimplemented desc = unknown service grpc.health.v1.Health". The application uses a custom gRPC health service. What is wrong?',
    options: [
      'The kubelet does not support gRPC probes',
      'Kubernetes gRPC probes require the standard grpc.health.v1.Health service; custom health RPC methods are not supported. The application must register the standard health service from grpc-health-probe',
      'The port number is wrong',
      'gRPC probes need HTTPS',
    ],
    correctAnswer: 1,
    codeSnippet: '# Kubernetes gRPC probe calls:\n# grpc.health.v1.Health/Check\n# This is a STANDARD protocol\n\n# Application must register this service:\n# Go example:\nimport "google.golang.org/grpc/health/grpc_health_v1"\n\nhealthServer := health.NewServer()\ngrpc_health_v1.RegisterHealthServer(s, healthServer)\nhealthServer.SetServingStatus("", grpc_health_v1.HealthCheckResponse_SERVING)\nhealthServer.SetServingStatus("my-service", grpc_health_v1.HealthCheckResponse_SERVING)\n\n# Pod probe:\nlivenessProbe:\n  grpc:\n    port: 50051\n    service: "my-service"  # Must match SetServingStatus name',
    explanation: 'Kubernetes gRPC probes use the standard grpc.health.v1.Health/Check RPC. Applications must register this specific service, not a custom health endpoint. The service parameter must match a registered service name.',
    tags: ['kubernetes', 'grpc', 'health-check', 'standard-protocol', 'probe'],
  },
  {
    id: 'k8s-troubleshooting-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'A cluster has thousands of namespaces and listing events across all namespaces takes over 30 seconds. The API server returns partial results with a "continue" token. What is happening and how do you handle large event queries?',
    options: [
      'Events cannot be paginated',
      'The API server implements list pagination; large lists are chunked with continue tokens. Use --chunk-size to control page size, or use the watch API for streaming instead of listing',
      'The API server has a timeout bug',
      'Events should be queried one namespace at a time',
    ],
    correctAnswer: 1,
    codeSnippet: '# API server list pagination:\n# Default chunk size: 500 items per response\n# Large lists require multiple round-trips\n\n# Control chunk size\nkubectl get events -A --chunk-size=250\n\n# For programmatic access, use continue tokens:\n# 1. List with limit=100\n# 2. Response includes metadata.continue token\n# 3. Next request includes continue=<token>\n# 4. Repeat until no continue token\n\n# Better for monitoring: use WATCH API\nkubectl get events -A -w\n\n# For bulk analysis, export to external system:\n# - Event exporter -> Elasticsearch\n# - Fluentd/Fluent Bit -> log aggregation\n# - Prometheus metrics from kube-state-metrics\n\n# API server tuning:\n# --max-requests-inflight=400\n# --max-mutating-requests-inflight=200',
    explanation: 'The API server paginates large list responses using continue tokens. For clusters with many namespaces/events, use the watch API, event exporters, or efficient pagination instead of full listing.',
    tags: ['kubernetes', 'api-pagination', 'continue-token', 'events', 'large-cluster'],
  },
  {
    id: 'k8s-troubleshooting-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'A graceful node shutdown (GracefulNodeShutdown feature) is configured but pods are not being evicted in priority order during shutdown. Some critical pods are killed before lower-priority pods. Why?',
    options: [
      'GracefulNodeShutdown does not support priority',
      'The shutdownGracePeriodByPodPriority configuration in the kubelet must be explicitly set with appropriate grace periods per priority class range; without proper configuration, the default behavior may not respect priorities',
      'Pod priority does not work during shutdown',
      'The node does not have systemd',
    ],
    correctAnswer: 1,
    codeSnippet: '# Kubelet GracefulNodeShutdown config:\napiVersion: kubelet.config.k8s.io/v1beta1\nkind: KubeletConfiguration\nshutdownGracePeriod: 60s\nshutdownGracePeriodCriticalPods: 20s\n\n# Priority-based shutdown (finer control):\nshutdownGracePeriodByPodPriority:\n- priority: 0         # BestEffort/low priority\n  shutdownGracePeriodSeconds: 0   # Kill immediately\n- priority: 1000000   # Normal apps\n  shutdownGracePeriodSeconds: 20  # 20s to shutdown\n- priority: 2000000000  # Critical pods\n  shutdownGracePeriodSeconds: 40  # 40s to shutdown\n\n# The kubelet hooks into systemd inhibitor locks\n# to delay node shutdown until pods are terminated\n\n# Verify systemd integration:\nsystemd-inhibit --list | grep kubelet',
    explanation: 'GracefulNodeShutdown uses shutdownGracePeriodByPodPriority to define per-priority-class shutdown periods. Without explicit configuration, all pods may get the same grace period, ignoring priority ordering.',
    tags: ['kubernetes', 'graceful-shutdown', 'priority', 'kubelet', 'systemd'],
  },
  {
    id: 'k8s-troubleshooting-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'CoreDNS is experiencing cache poisoning concerns because upstream DNS responses are not validated. What CoreDNS feature prevents this and what are its prerequisites?',
    options: [
      'CoreDNS cannot validate DNS responses',
      'Enable DNSSEC validation in CoreDNS using the dnssec plugin; this requires the upstream DNS server to support DNSSEC and the domain to have DNSSEC records (RRSIG, DNSKEY) properly configured',
      'TLS encryption prevents cache poisoning',
      'CoreDNS automatically validates all responses',
    ],
    correctAnswer: 1,
    codeSnippet: '# CoreDNS DNSSEC validation:\n.:53 {\n    errors\n    health\n    kubernetes cluster.local {\n        pods insecure\n    }\n    # Enable DNSSEC validation for forwarded queries\n    dnssec {\n      # Validates DNSSEC signatures on upstream responses\n    }\n    forward . tls://8.8.8.8 tls://8.8.4.4 {\n      tls_servername dns.google\n      # DoT (DNS over TLS) for transport encryption\n      # DNSSEC for response validation\n    }\n    cache 30\n}\n\n# Alternative: Use DNS over HTTPS (DoH)\nforward . https://dns.google/dns-query {\n  except cluster.local\n}\n\n# Note: DNSSEC validation adds latency\n# Not all domains have DNSSEC configured',
    explanation: 'DNSSEC validates the authenticity and integrity of DNS responses using cryptographic signatures. It requires domain DNSSEC records and validation-capable resolvers. DNS-over-TLS/HTTPS provides transport encryption.',
    tags: ['kubernetes', 'coredns', 'dnssec', 'dns-security', 'cache-poisoning'],
  },
  {
    id: 'k8s-troubleshooting-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A stateful application using a CSI driver with volume snapshots experiences data inconsistency after restoring from a snapshot. The snapshot was taken while the application was actively writing. What went wrong?',
    options: [
      'CSI snapshots are always consistent',
      'The snapshot captured a crash-consistent state, not an application-consistent state; the application was mid-transaction during snapshot, requiring application-level quiesce (freeze I/O) before snapshot for consistency',
      'The CSI driver is buggy',
      'Snapshots do not capture data',
    ],
    correctAnswer: 1,
    codeSnippet: '# Crash-consistent vs Application-consistent snapshots:\n# Crash-consistent: disk state at a point in time\n#   - Like pulling the power plug\n#   - Database may need recovery on restore\n# Application-consistent: application has flushed buffers\n#   - All writes are committed\n#   - Clean state on restore\n\n# For application consistency:\n# 1. Freeze application I/O before snapshot\n# 2. Use filesystem freeze (fsfreeze)\n# 3. Take snapshot\n# 4. Thaw application I/O\n\n# Example with a database:\n# 1. kubectl exec db-pod -- pg_backup_start(\'snapshot\')\n# 2. Create VolumeSnapshot\n# 3. kubectl exec db-pod -- pg_backup_stop()\n\n# Or use a backup tool that handles this:\n# - Velero with pre/post hooks\n# - Kasten K10\n# - Database-native backup tools',
    explanation: 'CSI snapshots are crash-consistent by default. For application consistency, the application must flush buffers and freeze I/O before snapshot. Database-specific backup procedures are essential.',
    tags: ['kubernetes', 'snapshot', 'consistency', 'crash-consistent', 'application-consistent'],
  },
  {
    id: 'k8s-troubleshooting-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A controller watching multiple CRDs using a shared informer factory stops receiving watch events for one CRD but continues working for others. The RBAC permissions are correct. What is the failure mode?',
    options: [
      'Shared informers cannot watch multiple CRDs',
      'The individual informer for that CRD may have hit a watch error (410 Gone, network timeout) and its reflector is in a backoff retry loop; the shared informer factory does not fail atomically for all informers',
      'The CRD was deleted',
      'The API server has a connection limit per CRD',
    ],
    correctAnswer: 1,
    codeSnippet: '# Shared informer factory internals:\n# - One informer per resource type\n# - Each informer has its own reflector (list + watch)\n# - Reflectors are independent -> can fail independently\n\n# Debugging:\n# 1. Check controller metrics\n# cache_list_total{resource="mycrds"}\n# cache_watch_total{resource="mycrds"}\n\n# 2. Check for 410 Gone errors in controller logs\n# "reflector: watch of *v1.MyCRD ended with:\n#  too old resource version"\n\n# 3. Check informer sync status\n# factory.WaitForCacheSync(stopCh)\n# Returns false for informers that haven\'t synced\n\n# Fix:\n# 1. Implement proper error handling in the reflector\n# 2. Monitor informer health metrics\n# 3. Use controller-runtime which handles this automatically\n# 4. Implement a watchdog that restarts on informer failure',
    explanation: 'Shared informer factories run independent reflectors per resource. One reflector can fail (410 Gone, network error) while others continue, causing silent partial failure in the controller.',
    tags: ['kubernetes', 'informer', 'reflector', 'watch-failure', 'controller'],
  },
  {
    id: 'k8s-troubleshooting-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'A node running many short-lived pods (from CronJobs) experiences increasing container startup latency over time. Restarting containerd temporarily fixes it. What is the progressive degradation?',
    options: [
      'The node is running out of IP addresses',
      'Containerd snapshot metadata and dead container state accumulates over time; the overlayfs snapshotter slows down with many layers, and containerd garbage collection may not keep up with pod churn',
      'The kubelet lease is not being renewed',
      'The node needs more CPU for scheduling',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check containerd state\n# 1. Count snapshots\nctr --namespace k8s.io snapshots list | wc -l\n\n# 2. Check content store size\ndu -sh /var/lib/containerd/io.containerd.content.v1.content/\n\n# 3. Check for dead containers\ncrictl ps -a --no-trunc | grep -c Exited\n\n# 4. Force garbage collection\ncrictl rmi --prune  # Remove unused images\n\n# 5. Check overlayfs mount count\nmount | grep overlay | wc -l\n\n# Mitigation:\n# 1. Set container log max size\n# kubelet: containerLogMaxSize: 10Mi\n# kubelet: containerLogMaxFiles: 3\n\n# 2. Configure image garbage collection\n# kubelet: imageGCHighThresholdPercent: 85\n# kubelet: imageGCLowThresholdPercent: 80\n\n# 3. Set eviction thresholds for imagefs\n# --eviction-hard=imagefs.available<15%',
    explanation: 'Containerd accumulates snapshot metadata, dead container state, and overlay mounts over time. Without adequate garbage collection, the overlayfs performance degrades and container startups slow down.',
    tags: ['kubernetes', 'containerd', 'snapshot', 'garbage-collection', 'degradation'],
  },
];

export default questions;
