import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-troubleshooting-l3-001',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A pod runs fine locally in Docker but enters CrashLoopBackOff in Kubernetes. The container runs as root locally. What is the most likely Kubernetes-specific cause?',
    options: [
      'The cluster DNS is not configured',
      'A SecurityContext or PodSecurityStandard is preventing the container from running as root',
      'The image tag is different between local and cluster',
      'The service mesh is injecting a sidecar',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes can enforce security contexts (runAsNonRoot: true) or PodSecurity Standards (restricted profile) that prevent containers from running as root, which Docker allows by default.',
    codeSnippet: '# Pod Security Context preventing root\nsecurityContext:\n  runAsNonRoot: true\n  # Container trying to run as root will fail with:\n  # "Error: container has runAsNonRoot and image will run as root"',
    tags: ['kubernetes', 'crashloopbackoff', 'security-context', 'root'],
  },
  {
    id: 'k8s-troubleshooting-l3-002',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'Pods in namespace A can reach pods in namespace B, but not vice versa. Both namespaces have NetworkPolicies. What is the most systematic approach to debug this?',
    options: [
      'Delete all NetworkPolicies and start over',
      'Check ingress policies on namespace A pods and egress policies on namespace B pods, as both directions need to be explicitly allowed',
      'Restart kube-proxy on all nodes',
      'Recreate the CNI plugin',
    ],
    correctAnswer: 1,
    explanation: 'NetworkPolicies are additive but default-deny when any policy applies. You need ingress rules on the destination pods (namespace A) allowing traffic from namespace B, AND egress rules on the source pods (namespace B) if egress policies exist.',
    codeSnippet: '# Allow ingress from namespace B to namespace A\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-from-ns-b\n  namespace: namespace-a\nspec:\n  podSelector: {}\n  ingress:\n  - from:\n    - namespaceSelector:\n        matchLabels:\n          kubernetes.io/metadata.name: namespace-b',
    tags: ['kubernetes', 'networkpolicy', 'cross-namespace', 'debugging'],
  },
  {
    id: 'k8s-troubleshooting-l3-003',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'DNS queries in the cluster are slow, with some timing out. CoreDNS pods are running. What should you investigate?',
    options: [
      'Increase the number of CoreDNS replicas only',
      'Check CoreDNS logs for errors, verify the loop detection plugin is enabled, check for conntrack table exhaustion on nodes, and test upstream DNS latency',
      'Restart all application pods',
      'Change dnsPolicy to None for all pods',
    ],
    correctAnswer: 1,
    explanation: 'DNS slowness can be caused by CoreDNS errors, DNS loop detection issues, conntrack table exhaustion (dropping UDP packets), or slow upstream DNS servers. A multi-pronged investigation is needed.',
    codeSnippet: '# Check CoreDNS logs\nkubectl logs -n kube-system -l k8s-app=kube-dns --tail=100\n\n# Check conntrack table on node\nsysctl net.netfilter.nf_conntrack_count\nsysctl net.netfilter.nf_conntrack_max\n\n# Test upstream DNS\nkubectl exec -n kube-system coredns-xxx -- dig @8.8.8.8 google.com',
    tags: ['kubernetes', 'dns', 'performance', 'conntrack'],
  },
  {
    id: 'k8s-troubleshooting-l3-004',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A Go application container is OOMKilled despite the process memory usage appearing well below the limit. What is happening?',
    options: [
      'Go applications cannot run in containers',
      'The Go runtime GOMAXPROCS is too high',
      'The kernel counts all RSS memory including memory-mapped files and the Go runtime reserves virtual memory that contributes to OOM decisions',
      'The container has a CPU limit causing memory issues',
    ],
    correctAnswer: 2,
    explanation: 'The Linux OOM killer considers RSS (Resident Set Size) which includes shared libraries and memory-mapped files. Go runtime can also hold onto freed memory (MADV_FREE) that still counts toward RSS until the kernel reclaims it.',
    codeSnippet: '# Set GOMEMLIMIT to help Go runtime stay within limits\nenv:\n- name: GOMEMLIMIT\n  value: "400MiB"  # Slightly below container limit\n- name: GOGC\n  value: "50"      # More aggressive GC',
    tags: ['kubernetes', 'oomkilled', 'golang', 'memory-management'],
  },
  {
    id: 'k8s-troubleshooting-l3-005',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A StatefulSet with 5 replicas is stuck with only 3 pods running. Pods 3 and 4 are Pending with "pod has unbound immediate PersistentVolumeClaims". What is the issue?',
    options: [
      'StatefulSets can only have 3 replicas',
      'The StorageClass used by the volumeClaimTemplates cannot provision more volumes (e.g., max volumes per node reached or quota exceeded)',
      'The pod names conflict with existing pods',
      'StatefulSets do not support PVCs',
    ],
    correctAnswer: 1,
    explanation: 'StatefulSet pods each get their own PVC. If the StorageClass cannot provision more PVs (node volume limits, storage quota, or provisioner errors), remaining pods stay Pending.',
    codeSnippet: '# Check PVC status\nkubectl get pvc -l app=my-statefulset\n\n# Check StorageClass provisioner events\nkubectl get events --field-selector reason=ProvisioningFailed\n\n# Check node volume limits\nkubectl describe node | grep -A5 "Allocated resources"',
    tags: ['kubernetes', 'statefulset', 'pvc', 'pending', 'storage'],
  },
  {
    id: 'k8s-troubleshooting-l3-006',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'An operator running in the cluster cannot create Custom Resource instances but can create standard resources. What is the likely RBAC issue?',
    options: [
      'The operator needs cluster-admin',
      'The Role/ClusterRole is missing the CRD apiGroup and resource in its rules',
      'Custom Resources do not support RBAC',
      'The operator needs to run in kube-system namespace',
    ],
    correctAnswer: 1,
    codeSnippet: '# Missing CRD permissions example:\n# The operator can manage pods (apiGroup: "") but not CRDs\nrules:\n- apiGroups: [""]\n  resources: ["pods", "services"]\n  verbs: ["*"]\n# Need to add:\n- apiGroups: ["myapp.example.com"]  # CRD group\n  resources: ["myresources"]         # CRD plural name\n  verbs: ["get", "list", "watch", "create", "update", "delete"]',
    explanation: 'CRDs have their own apiGroup (e.g., myapp.example.com). The RBAC rules must explicitly include this apiGroup and the CRD resource name for the operator to manage them.',
    tags: ['kubernetes', 'rbac', 'crd', 'operator', 'apigroup'],
  },
  {
    id: 'k8s-troubleshooting-l3-007',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'An application experiences intermittent 502 errors during rolling updates. Pods pass readiness probes. What is the most likely cause?',
    options: [
      'The image is corrupt',
      'The application does not handle SIGTERM gracefully and closes connections before being removed from Service endpoints',
      'The Service type is wrong',
      'CoreDNS is caching stale records',
    ],
    correctAnswer: 1,
    explanation: 'During pod termination, there is a race condition: SIGTERM is sent at the same time endpoints are updated. If the app closes connections before the endpoint is removed, in-flight requests get 502 errors.',
    codeSnippet: '# Solution: Add a preStop hook to delay shutdown\nlifecycle:\n  preStop:\n    exec:\n      command: ["/bin/sh", "-c", "sleep 5"]\n# This gives time for endpoints to be updated\n# before the container starts shutting down',
    tags: ['kubernetes', 'rolling-update', '502', 'graceful-shutdown', 'prestop'],
  },
  {
    id: 'k8s-troubleshooting-l3-008',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'All pods on a specific node are reporting high latency but the node shows no CPU or memory pressure. What should you investigate?',
    options: [
      'Application code bugs',
      'Disk I/O saturation, network bandwidth saturation, or kernel parameters (e.g., conntrack table, socket buffers)',
      'Kubernetes version mismatch',
      'The scheduler algorithm',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check disk I/O on the node\niostat -x 1 5\n\n# Check network saturation\nsar -n DEV 1 5\n\n# Check conntrack table\nsysctl net.netfilter.nf_conntrack_count\ncat /proc/sys/net/netfilter/nf_conntrack_max\n\n# Check for throttled CPU (CFS throttling)\ncat /sys/fs/cgroup/cpu/kubepods/cpu.stat',
    explanation: 'High latency without CPU/memory pressure often points to I/O bottlenecks, network saturation, or kernel-level issues like conntrack exhaustion or CPU CFS throttling.',
    tags: ['kubernetes', 'node', 'latency', 'io', 'network'],
  },
  {
    id: 'k8s-troubleshooting-l3-009',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A PV using NFS shows "permission denied" when the pod tries to write. The NFS export allows the IP range. What is the issue?',
    options: [
      'The PV access mode is wrong',
      'The container runs as a UID that does not have write permissions on the NFS directory, and root_squash on the NFS server maps root to nobody',
      'The PVC storage size is too small',
      'NFS does not support write operations in Kubernetes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check container UID\nkubectl exec my-pod -- id\n# uid=1000(app) gid=1000(app)\n\n# NFS server export (root_squash is default):\n# /data  *(rw,sync,root_squash)\n# root_squash maps UID 0 to nobody\n\n# Solution: Set fsGroup in securityContext\nsecurityContext:\n  fsGroup: 1000  # Changes group ownership on mount',
    explanation: 'NFS root_squash maps root access to nobody. If the container runs as a non-root UID without matching NFS directory permissions, writes fail. Use securityContext.fsGroup or fix NFS permissions.',
    tags: ['kubernetes', 'nfs', 'permissions', 'root-squash', 'securitycontext'],
  },
  {
    id: 'k8s-troubleshooting-l3-010',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'A pod has a readiness probe on /ready that depends on a downstream service. The downstream service goes down. What is the impact?',
    options: [
      'Only the downstream service pods are affected',
      'The pod fails readiness, is removed from endpoints, and causes cascading failures if other services depend on it',
      'The pod is restarted',
      'The pod continues receiving traffic normally',
    ],
    correctAnswer: 1,
    explanation: 'Readiness probes that depend on external services create cascading failure risks. If a downstream dependency fails, all upstream pods may become unready, amplifying the outage.',
    codeSnippet: '# Anti-pattern: readiness depends on external service\nreadinessProbe:\n  httpGet:\n    path: /ready  # Internally checks DB, cache, etc.\n\n# Better: Only check local application health\nreadinessProbe:\n  httpGet:\n    path: /healthz  # Only checks if this app can serve',
    tags: ['kubernetes', 'readiness-probe', 'cascading-failure', 'best-practices'],
  },
  {
    id: 'k8s-troubleshooting-l3-011',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'Events are being lost because they only persist for ~1 hour. How do you implement persistent event collection?',
    options: [
      'Increase the API server event TTL',
      'Deploy an event exporter (like kubernetes-event-exporter or Eventrouter) that forwards events to a logging backend',
      'Configure etcd to retain events longer',
      'Use kubectl get events with --persist flag',
    ],
    correctAnswer: 1,
    codeSnippet: '# Example: kubernetes-event-exporter config\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: event-exporter-cfg\ndata:\n  config.yaml: |\n    route:\n      routes:\n        - match:\n            - receiver: "elasticsearch"\n    receivers:\n      - name: "elasticsearch"\n        elasticsearch:\n          hosts: ["http://elasticsearch:9200"]\n          index: kube-events',
    explanation: 'Event exporters watch the Kubernetes event stream and forward events to external systems like Elasticsearch, Loki, or cloud logging services for persistent storage and analysis.',
    tags: ['kubernetes', 'events', 'event-exporter', 'logging'],
  },
  {
    id: 'k8s-troubleshooting-l3-012',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'During a node drain, a pod is stuck in Terminating state for over 10 minutes. What could cause this?',
    options: [
      'The pod has too many containers',
      'The pod has a finalizer that is not being removed, or a process is ignoring SIGTERM and the terminationGracePeriodSeconds is very long',
      'The pod image is still being pulled',
      'The pod namespace is being deleted',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check for finalizers\nkubectl get pod stuck-pod -o jsonpath=\'{.metadata.finalizers}\'\n\n# Check terminationGracePeriodSeconds\nkubectl get pod stuck-pod -o jsonpath=\'{.spec.terminationGracePeriodSeconds}\'\n\n# Force delete if needed (last resort)\nkubectl delete pod stuck-pod --grace-period=0 --force',
    explanation: 'Pods stuck in Terminating can be caused by: finalizers that a controller is not clearing, processes ignoring SIGTERM (requiring SIGKILL after grace period), or a very long terminationGracePeriodSeconds.',
    tags: ['kubernetes', 'terminating', 'finalizer', 'drain', 'stuck-pod'],
  },
  {
    id: 'k8s-troubleshooting-l3-013',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'Nodes show CPU throttling symptoms (high latency, slow response) but kubectl top shows CPU usage below 50%. What is happening?',
    options: [
      'The metrics server is inaccurate',
      'CFS bandwidth throttling is occurring because CPU limits are set too low relative to burst requirements',
      'The nodes need more memory',
      'kube-proxy is overloaded',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check CFS throttling on the node\ncat /sys/fs/cgroup/cpu/kubepods/burstable/pod<uid>/cpu.stat\n# nr_periods 10000\n# nr_throttled 4500  # <-- 45% of periods are throttled!\n# throttled_time 123456789\n\n# Solution: increase CPU limits or remove them\n# Some teams use requests only (no limits) for CPU',
    explanation: 'CFS (Completely Fair Scheduler) throttling occurs when a container hits its CPU limit within a CFS period (100ms). Even at low average CPU, burst patterns can trigger severe throttling.',
    tags: ['kubernetes', 'cpu-throttling', 'cfs', 'performance', 'limits'],
  },
  {
    id: 'k8s-troubleshooting-l3-014',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You need to capture a network trace from a running pod without restarting it. How can you accomplish this?',
    options: [
      'kubectl logs with --network flag',
      'Use kubectl debug to attach an ephemeral container with tcpdump and share the network namespace',
      'Use kube-proxy to capture traffic',
      'Configure the Service to log all traffic',
    ],
    correctAnswer: 1,
    codeSnippet: '# Attach a debug container with network tools\nkubectl debug -it my-pod --image=nicolaka/netshoot --target=my-container\n\n# Inside the debug container:\ntcpdump -i eth0 -w /tmp/capture.pcap\n\n# Or capture specific traffic\ntcpdump -i eth0 port 8080 -c 100',
    explanation: 'kubectl debug with --target shares the network namespace with the specified container. Using an image like netshoot (which includes tcpdump, nmap, etc.) enables network debugging.',
    tags: ['kubernetes', 'debug', 'tcpdump', 'network-trace', 'ephemeral'],
  },
  {
    id: 'k8s-troubleshooting-l3-015',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'imagepullbackoff',
    question: 'Image pulls are succeeding on some nodes but failing on others with "x509: certificate signed by unknown authority". What is the issue?',
    options: [
      'The image registry TLS certificate is self-signed and some nodes do not have the CA certificate installed in their container runtime trust store',
      'The registry is down for some nodes',
      'The imagePullSecret is namespace-specific',
      'Some nodes have an older Kubernetes version',
    ],
    correctAnswer: 0,
    codeSnippet: '# On failing nodes, add CA cert to containerd\n# /etc/containerd/certs.d/registry.example.com/hosts.toml\n[host."https://registry.example.com"]\n  ca = "/etc/containerd/certs.d/registry.example.com/ca.crt"\n\n# Or for Docker\n# /etc/docker/certs.d/registry.example.com/ca.crt\n\n# Then restart the container runtime\nsystemctl restart containerd',
    explanation: 'When using a private registry with a self-signed or internal CA certificate, each node must have the CA certificate installed in the container runtime TLS trust store.',
    tags: ['kubernetes', 'imagepullbackoff', 'tls', 'certificates', 'x509'],
  },
  {
    id: 'k8s-troubleshooting-l3-016',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A pod with a sidecar container enters CrashLoopBackOff. The main container exits successfully (code 0) but the sidecar keeps crashing. What is the root cause?',
    options: [
      'Sidecar containers are not supported',
      'The main container exiting causes the pod to terminate, but restartPolicy: Always restarts the sidecar which then crashes because the main app is gone',
      'The sidecar image is incompatible',
      'Sidecars must always exit before the main container',
    ],
    correctAnswer: 1,
    explanation: 'With restartPolicy: Always, when any container exits, Kubernetes restarts it. If the main container completes but the sidecar depends on it, the sidecar crashes repeatedly. Native sidecar containers (KEP-753) address this.',
    codeSnippet: '# Native sidecar container (Kubernetes 1.28+)\ninitContainers:\n- name: log-shipper\n  image: fluent-bit\n  restartPolicy: Always  # Makes this a sidecar\n  # Sidecar starts before main, stops after main',
    tags: ['kubernetes', 'sidecar', 'crashloopbackoff', 'container-lifecycle'],
  },
  {
    id: 'k8s-troubleshooting-l3-017',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A pod is Pending with "didn\'t match pod anti-affinity rules". The deployment has 3 replicas across 3 nodes. You try to scale to 4. What happens?',
    options: [
      'The 4th pod is placed on a random node',
      'The 4th pod stays Pending because requiredDuringSchedulingIgnoredDuringExecution anti-affinity prevents two pods on the same node',
      'Kubernetes automatically adds a new node',
      'One of the existing pods is evicted',
    ],
    correctAnswer: 1,
    codeSnippet: 'affinity:\n  podAntiAffinity:\n    requiredDuringSchedulingIgnoredDuringExecution:\n    - labelSelector:\n        matchExpressions:\n        - key: app\n          operator: In\n          values: ["my-app"]\n      topologyKey: kubernetes.io/hostname\n# With 3 nodes and this rule, max 3 replicas can run',
    explanation: 'Required pod anti-affinity with topologyKey: kubernetes.io/hostname ensures one pod per node. With 3 nodes, the 4th pod cannot be scheduled and stays Pending.',
    tags: ['kubernetes', 'anti-affinity', 'pending', 'scheduling'],
  },
  {
    id: 'k8s-troubleshooting-l3-018',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'After adding a custom stubDomain to CoreDNS for an internal domain, all DNS queries are failing. What is the likely misconfiguration?',
    options: [
      'The custom domain name is too long',
      'The Corefile syntax is incorrect and CoreDNS failed to reload, causing all DNS to fail',
      'Custom domains are not supported',
      'You need to restart all pods after CoreDNS changes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check CoreDNS pod status after config change\nkubectl get pods -n kube-system -l k8s-app=kube-dns\n\n# Check CoreDNS logs for config errors\nkubectl logs -n kube-system -l k8s-app=kube-dns | grep -i error\n\n# Correct Corefile with custom domain\n.:53 {\n    errors\n    health\n    kubernetes cluster.local in-addr.arpa ip6.arpa {\n       pods insecure\n       fallthrough in-addr.arpa ip6.arpa\n    }\n    forward . /etc/resolv.conf\n    cache 30\n}\ninternal.company.com:53 {\n    forward . 10.0.0.53\n}',
    explanation: 'CoreDNS validates its Corefile on reload. A syntax error causes the reload to fail, and depending on the reload plugin behavior, may cause CoreDNS to crash, breaking all DNS.',
    tags: ['kubernetes', 'coredns', 'corefile', 'stubdomain', 'configuration'],
  },
  {
    id: 'k8s-troubleshooting-l3-019',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A Node.js application is OOMKilled with a 1Gi limit. The process uses 500Mi but the container RSS shows 900Mi. What is consuming the extra memory?',
    options: [
      'The operating system in the container',
      'V8 garbage collection overhead, buffer pools, native addons (C++ bindings), and memory fragmentation in the heap',
      'The container image layers',
      'Kubernetes overhead per container',
    ],
    correctAnswer: 1,
    codeSnippet: '# Set Node.js max old space size\nenv:\n- name: NODE_OPTIONS\n  value: "--max-old-space-size=700"\n# Leave ~300Mi for native addons, buffers, etc.\n\n# Monitor Node.js memory from inside the container\n# process.memoryUsage() vs container RSS',
    explanation: 'Node.js RSS includes V8 heap, external memory (Buffers, TypedArrays), native addon memory, and libuv thread pool memory. Set --max-old-space-size well below the container limit to account for non-heap memory.',
    tags: ['kubernetes', 'oomkilled', 'nodejs', 'memory', 'v8'],
  },
  {
    id: 'k8s-troubleshooting-l3-020',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A gRPC service behind a Kubernetes Service experiences load imbalancing where one pod gets most traffic. What is the cause?',
    options: [
      'The pods have different resource limits',
      'gRPC uses HTTP/2 with long-lived connections, and kube-proxy L4 load balancing only balances at connection establishment, not per-request',
      'The pod readiness probes are misconfigured',
      'The Service type needs to be LoadBalancer',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes Services use L4 (TCP) load balancing. gRPC multiplexes multiple requests over a single HTTP/2 connection, so all requests on one connection go to the same pod. Use a service mesh or client-side load balancing.',
    codeSnippet: '# Solutions for gRPC load balancing:\n# 1. Use a headless Service + client-side LB\napiVersion: v1\nkind: Service\nmetadata:\n  name: my-grpc-svc\nspec:\n  clusterIP: None  # Headless\n\n# 2. Use an L7 load balancer (e.g., Istio, Linkerd)\n# 3. Use gRPC client-side load balancing with DNS resolver',
    tags: ['kubernetes', 'grpc', 'load-balancing', 'http2', 'service'],
  },
  {
    id: 'k8s-troubleshooting-l3-021',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'Nodes intermittently go NotReady for 1-2 minutes then recover. The kubelet and container runtime are healthy. What should you investigate?',
    options: [
      'The pod count on the node',
      'Network connectivity between the node and API server (heartbeat failures due to network partitions or high API server load)',
      'The node disk space',
      'The Kubernetes version',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check kubelet heartbeat timing\nkubectl get node <node-name> -o jsonpath=\'{.status.conditions[?(@.type=="Ready")]}\'\n\n# Check node lease objects (used for heartbeats)\nkubectl get lease -n kube-node-lease <node-name> -o yaml\n\n# Relevant kube-controller-manager flags:\n# --node-monitor-period=5s    (how often to check)\n# --node-monitor-grace-period=40s  (NotReady after this)\n# --pod-eviction-timeout=5m   (evict pods after this)',
    explanation: 'Intermittent NotReady states are often caused by network partitions between nodes and the API server, or API server being overloaded and not processing heartbeats in time.',
    tags: ['kubernetes', 'notready', 'heartbeat', 'network-partition'],
  },
  {
    id: 'k8s-troubleshooting-l3-022',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'An EBS volume fails to attach to a new node after the old node was terminated. The error says "volume is already attached to instance i-xxx". How do you resolve this?',
    options: [
      'Wait indefinitely for auto-resolution',
      'Force-detach the volume using the cloud provider CLI/API and delete the stale VolumeAttachment object in Kubernetes',
      'Delete and recreate the PV',
      'Restart the kubelet on all nodes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check VolumeAttachment objects\nkubectl get volumeattachments\n\n# Delete stale attachment\nkubectl delete volumeattachment <name>\n\n# Force detach via AWS CLI\naws ec2 detach-volume --volume-id vol-xxx --force\n\n# The CSI driver should then re-attach to new node',
    explanation: 'When a node is terminated abruptly, the EBS volume may not be properly detached. The stale VolumeAttachment object and AWS attachment must be cleaned up for the volume to attach to a new node.',
    tags: ['kubernetes', 'ebs', 'volume-attachment', 'cloud', 'stale'],
  },
  {
    id: 'k8s-troubleshooting-l3-023',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A user can list pods but cannot view pod logs. What additional RBAC permission is needed?',
    options: [
      'The "logs" verb on pods',
      'The "get" verb on the "pods/log" subresource',
      'The "read" verb on pods',
      'Access to the container runtime API',
    ],
    correctAnswer: 1,
    codeSnippet: '# Pod logs require subresource permission\nrules:\n- apiGroups: [""]\n  resources: ["pods"]\n  verbs: ["get", "list"]\n- apiGroups: [""]\n  resources: ["pods/log"]    # Subresource\n  verbs: ["get"]\n# Similarly: pods/exec, pods/portforward, pods/attach',
    explanation: 'Pod logs are accessed through the pods/log subresource. You need an explicit RBAC rule for pods/log with the "get" verb, separate from the pods resource permission.',
    tags: ['kubernetes', 'rbac', 'subresource', 'pods-log'],
  },
  {
    id: 'k8s-troubleshooting-l3-024',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'After implementing a NetworkPolicy with default deny, all pods lose connectivity including to CoreDNS. How do you fix DNS while maintaining the default deny?',
    options: [
      'Change dnsPolicy to None on all pods',
      'Add an explicit egress rule allowing traffic to kube-dns on port 53 (TCP and UDP)',
      'Disable NetworkPolicies for the kube-system namespace',
      'Use IP addresses instead of DNS names',
    ],
    correctAnswer: 1,
    codeSnippet: '# Default deny all egress\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: default-deny-egress\nspec:\n  podSelector: {}\n  policyTypes: ["Egress"]\n---\n# Allow DNS egress\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-dns\nspec:\n  podSelector: {}\n  egress:\n  - to:\n    - namespaceSelector:\n        matchLabels:\n          kubernetes.io/metadata.name: kube-system\n      podSelector:\n        matchLabels:\n          k8s-app: kube-dns\n    ports:\n    - port: 53\n      protocol: UDP\n    - port: 53\n      protocol: TCP',
    explanation: 'When implementing default-deny egress, you must explicitly allow DNS traffic to CoreDNS on port 53 (both UDP and TCP) or all service name resolution will break.',
    tags: ['kubernetes', 'networkpolicy', 'dns', 'default-deny', 'egress'],
  },
  {
    id: 'k8s-troubleshooting-l3-025',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'init-container-failures',
    question: 'An init container is supposed to run a database migration but keeps failing because another pod replica runs the same migration simultaneously. How do you prevent this?',
    options: [
      'Use a readiness probe on the init container',
      'Implement a Kubernetes Job for migrations instead, or use distributed locking (e.g., leader election) in the init container',
      'Set the deployment replicas to 1',
      'Use a DaemonSet instead',
    ],
    correctAnswer: 1,
    codeSnippet: '# Option 1: Separate Job for migrations\napiVersion: batch/v1\nkind: Job\nmetadata:\n  name: db-migration\nspec:\n  template:\n    spec:\n      containers:\n      - name: migrate\n        image: my-app\n        command: ["./migrate"]\n      restartPolicy: OnFailure\n\n# Run before deployment with a CI/CD pipeline\n# or use Helm hooks / Argo CD sync waves',
    explanation: 'Database migrations in init containers run on every replica simultaneously. Use a dedicated Job (run once) or implement distributed locking to prevent concurrent migrations.',
    tags: ['kubernetes', 'init-containers', 'migrations', 'job', 'concurrency'],
  },
  {
    id: 'k8s-troubleshooting-l3-026',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container crashes with "too many open files" (EMFILE). The application only uses 50 file descriptors. What is the Kubernetes-level fix?',
    options: [
      'Increase the container memory limit',
      'The container inherits the default ulimits; use a securityContext or init container to raise the file descriptor limit',
      'Upgrade the container runtime',
      'Add more CPU resources',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check current limits inside the container\nkubectl exec my-pod -- cat /proc/1/limits | grep "Max open files"\n\n# Solution: Use an init container with privileged access\n# or configure containerd default ulimits\n# /etc/containerd/config.toml:\n# [plugins."io.containerd.grpc.v1.cri"]\n#   [plugins."io.containerd.grpc.v1.cri".containerd]\n#     default_runtime_name = "runc"\n#     [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]\n#       [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options.Rlimits]\n#         RLIMIT_NOFILE = 65536',
    explanation: 'Container file descriptor limits are inherited from the container runtime defaults. Even with low app usage, libraries and connections can consume FDs. Adjust runtime config or use sysctls.',
    tags: ['kubernetes', 'ulimits', 'file-descriptors', 'emfile', 'crashloopbackoff'],
  },
  {
    id: 'k8s-troubleshooting-l3-027',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'Critical system pods keep getting evicted during node memory pressure. How do you protect them?',
    options: [
      'Run them on dedicated nodes with taints',
      'Assign a high PriorityClass (like system-cluster-critical) and ensure they have Guaranteed QoS',
      'Increase the node memory',
      'Disable evictions on the node',
    ],
    correctAnswer: 1,
    codeSnippet: 'apiVersion: v1\nkind: Pod\nmetadata:\n  name: critical-pod\nspec:\n  priorityClassName: system-cluster-critical\n  containers:\n  - name: app\n    resources:\n      requests:\n        memory: "256Mi"\n        cpu: "250m"\n      limits:\n        memory: "256Mi"   # Equal to request = Guaranteed QoS\n        cpu: "250m"',
    explanation: 'system-cluster-critical and system-node-critical PriorityClasses protect pods from eviction. Combined with Guaranteed QoS (requests = limits), these pods are evicted last.',
    tags: ['kubernetes', 'eviction', 'priority', 'guaranteed-qos', 'critical'],
  },
  {
    id: 'k8s-troubleshooting-l3-028',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'You need to find all warning events cluster-wide that occurred in the last hour. What is the most efficient approach?',
    options: [
      'kubectl get events -A --field-selector type=Warning',
      'kubectl get events -A --field-selector type=Warning --sort-by=.metadata.creationTimestamp',
      'kubectl describe all -A | grep Warning',
      'kubectl logs --all-namespaces | grep warning',
    ],
    correctAnswer: 1,
    codeSnippet: '# All warning events, sorted by time\nkubectl get events -A --field-selector type=Warning \\\n  --sort-by=.metadata.creationTimestamp\n\n# Alternative with custom columns\nkubectl get events -A --field-selector type=Warning \\\n  -o custom-columns=\\\nNAMESPACE:.metadata.namespace,\\\nREASON:.reason,\\\nMESSAGE:.message,\\\nOBJECT:.involvedObject.name,\\\nTIME:.metadata.creationTimestamp',
    explanation: 'Field selector type=Warning filters to only warning events, -A includes all namespaces, and --sort-by orders chronologically for easy review.',
    tags: ['kubernetes', 'events', 'warning', 'cluster-wide'],
  },
  {
    id: 'k8s-troubleshooting-l3-029',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A cluster has 10 nodes with 16Gi each, but pods requesting a total of only 80Gi cannot all be scheduled. Some are Pending. Why?',
    options: [
      'The cluster has a ResourceQuota limiting total memory',
      'Allocatable memory is less than capacity (system-reserved + kube-reserved + eviction-threshold reduce it), and fragmentation across nodes prevents optimal bin-packing',
      'The scheduler is single-threaded and too slow',
      'Memory limits and requests must be the same',
    ],
    correctAnswer: 1,
    codeSnippet: '# Per-node allocatable calculation:\n# Allocatable = Capacity - kube-reserved - system-reserved - eviction-threshold\n# Example: 16Gi - 1Gi - 1Gi - 100Mi = ~13.9Gi allocatable\n# 10 nodes * 13.9Gi = ~139Gi total allocatable\n\n# But fragmentation matters:\n# Node 1: 5Gi free, Pod needs 8Gi -> doesn\'t fit\n# Node 2: 3Gi free\n# Combined free: 8Gi, but pod still can\'t be scheduled',
    explanation: 'Node allocatable memory is less than capacity due to system reservations. Additionally, memory fragmentation across nodes means available memory may not be concentrated enough for large pod requests.',
    tags: ['kubernetes', 'scheduling', 'allocatable', 'fragmentation', 'resources'],
  },
  {
    id: 'k8s-troubleshooting-l3-030',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'An exec-based liveness probe is intermittently failing with timeout. The command normally runs in 200ms. What is happening?',
    options: [
      'Exec probes are inherently unreliable',
      'The container is under CPU throttling (CFS), causing the exec command to take longer than the probe timeout',
      'The kubelet is overloaded',
      'Exec probes only work with HTTP containers',
    ],
    correctAnswer: 1,
    codeSnippet: '# Probe with 1s timeout\nlivenessProbe:\n  exec:\n    command: ["cat", "/tmp/healthy"]\n  timeoutSeconds: 1\n  periodSeconds: 5\n\n# CPU throttling can cause a simple cat to take >1s!\n# Check throttling:\n# cat /sys/fs/cgroup/cpu/kubepods/.../cpu.stat\n# nr_throttled: high number indicates throttling',
    explanation: 'CPU throttling can delay even simple exec probes beyond their timeout. If the container CPU limit is low and the node is busy, CFS throttling causes probe timeouts.',
    tags: ['kubernetes', 'exec-probe', 'cpu-throttling', 'timeout'],
  },
  {
    id: 'k8s-troubleshooting-l3-031',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'DNS resolution works for <service>.<namespace>.svc.cluster.local but fails for just <service> from a pod in the same namespace. What is wrong?',
    options: [
      'Short names are not supported',
      'The pod dnsPolicy is set to None or Default instead of ClusterFirst, so search domains are not configured',
      'The service does not exist',
      'CoreDNS does not support short names',
    ],
    correctAnswer: 1,
    codeSnippet: '# Pod with correct dnsPolicy\nspec:\n  dnsPolicy: ClusterFirst  # Default, adds search domains\n\n# Expected /etc/resolv.conf:\n# nameserver 10.96.0.10\n# search default.svc.cluster.local svc.cluster.local cluster.local\n# options ndots:5\n\n# With dnsPolicy: None, search domains are missing\n# unless explicitly configured in dnsConfig',
    explanation: 'Short name resolution relies on the search domains in /etc/resolv.conf. If dnsPolicy is None or Default, these search domains are missing and only FQDNs work.',
    tags: ['kubernetes', 'dns', 'search-domains', 'dnspolicy'],
  },
  {
    id: 'k8s-troubleshooting-l3-032',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'imagepullbackoff',
    question: 'Images pull successfully during the day but fail at night with "429 Too Many Requests" from Docker Hub. What is happening?',
    options: [
      'Docker Hub is performing maintenance at night',
      'Docker Hub rate limits unauthenticated pulls (100/6h) and authenticated free-tier pulls (200/6h); overnight batch jobs exceed the limit',
      'The cluster time zone is misconfigured',
      'The container runtime needs to be updated',
    ],
    correctAnswer: 1,
    codeSnippet: '# Solutions:\n# 1. Authenticate with Docker Hub (increases limit)\nkubectl create secret docker-registry dockerhub-cred \\\n  --docker-server=https://index.docker.io/v1/ \\\n  --docker-username=<user> --docker-password=<token>\n\n# 2. Use a pull-through registry cache\n# 3. Mirror frequently used images to a private registry\n# 4. Upgrade to Docker Hub paid plan',
    explanation: 'Docker Hub imposes rate limits: 100 pulls/6h for unauthenticated and 200 pulls/6h for free authenticated users. Batch jobs or cluster operations can easily exceed these limits.',
    tags: ['kubernetes', 'docker-hub', 'rate-limit', 'imagepullbackoff'],
  },
  {
    id: 'k8s-troubleshooting-l3-033',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service with externalTrafficPolicy: Local returns connection refused from some nodes. Why?',
    options: [
      'The Service port is wrong',
      'Nodes without local pods matching the Service selector do not have endpoints to route to, so connections are refused',
      'The CNI plugin does not support Local policy',
      'The Service needs an Ingress',
    ],
    correctAnswer: 1,
    explanation: 'externalTrafficPolicy: Local means traffic arriving at a node is only routed to pods on that same node. Nodes without matching pods cannot serve the traffic, resulting in connection refused.',
    codeSnippet: '# externalTrafficPolicy: Local\n# Preserves client source IP but only works on nodes with pods\napiVersion: v1\nkind: Service\nspec:\n  type: NodePort\n  externalTrafficPolicy: Local\n  # Traffic to node without pods = connection refused\n  # Use with DaemonSet or ensure pods on all nodes',
    tags: ['kubernetes', 'service', 'externaltrafficpolicy', 'local'],
  },
  {
    id: 'k8s-troubleshooting-l3-034',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A StatefulSet pod is recreated but the PVC data appears empty even though the PV shows Bound. What could be wrong?',
    options: [
      'StatefulSets do not persist data',
      'The PV reclaimPolicy is Delete and was recreated empty, or the PV was backed by emptyDir which is ephemeral',
      'The container image was updated',
      'The namespace was recreated',
    ],
    correctAnswer: 1,
    explanation: 'If the PV was backed by ephemeral storage or the reclaim policy caused data deletion and recreation, the volume will appear empty. Also check that the correct PV is bound and the mount path is correct.',
    tags: ['kubernetes', 'statefulset', 'pv', 'data-loss', 'reclaim'],
  },
  {
    id: 'k8s-troubleshooting-l3-035',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'After upgrading Kubernetes, some RBAC policies stop working. API calls that previously succeeded now return 403. What is the likely cause?',
    options: [
      'RBAC was disabled during the upgrade',
      'API group versions changed (e.g., beta APIs removed) and RBAC rules reference the old apiGroup/version',
      'The upgrade reset all RBAC rules',
      'The cluster certificates expired',
    ],
    correctAnswer: 1,
    codeSnippet: '# Before upgrade: policy/v1beta1 was valid\nrules:\n- apiGroups: ["policy"]\n  resources: ["podsecuritypolicies"]  # Removed in 1.25\n  verbs: ["use"]\n\n# After upgrade: need to use Pod Security Standards\n# or update to the new API versions',
    explanation: 'Kubernetes deprecates and removes API versions across upgrades. RBAC rules referencing removed apiGroups (like PodSecurityPolicies in 1.25) stop working.',
    tags: ['kubernetes', 'rbac', 'api-deprecation', 'upgrade'],
  },
  {
    id: 'k8s-troubleshooting-l3-036',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'Intermittent connection resets are occurring between pods. tcpdump shows RST packets. What should you investigate?',
    options: [
      'The application logs only',
      'Conntrack table overflow, idle connection timeouts in kube-proxy/iptables, and network policy rule changes',
      'DNS configuration',
      'The container image version',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check conntrack table utilization\nsysctl net.netfilter.nf_conntrack_count\nsysctl net.netfilter.nf_conntrack_max\n\n# Check for conntrack drops\nconntrack -S | grep drop\n\n# Check kube-proxy idle timeout\n# Default iptables idle timeout:\n# TCP established: 432000s (5 days)\n# TCP close-wait: 120s\n\n# If using cloud LB with shorter idle timeout,\n# connections may be reset',
    explanation: 'Connection resets can be caused by conntrack table overflow (dropping packets), mismatched idle timeouts between kube-proxy and load balancers, or NetworkPolicy changes dropping established connections.',
    tags: ['kubernetes', 'connection-reset', 'conntrack', 'iptables'],
  },
  {
    id: 'k8s-troubleshooting-l3-037',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'Memory usage metrics from kubectl top show 400Mi but the pod is OOMKilled at a 512Mi limit. The container cgroup shows 510Mi. What accounts for the difference?',
    options: [
      'kubectl top is delayed by 30 seconds',
      'kubectl top shows working_set memory which excludes inactive file cache; the cgroup includes all RSS + cache memory',
      'The metrics server has a bug',
      'Memory limits are applied differently than shown',
    ],
    correctAnswer: 1,
    codeSnippet: '# Metrics from different sources:\n# kubectl top: uses metrics-server (working_set)\n# cgroup: memory.usage_in_bytes (includes inactive cache)\n# OOM killer: uses memory.usage_in_bytes\n\n# Check cgroup memory directly on the node:\ncat /sys/fs/cgroup/memory/kubepods/pod<uid>/memory.usage_in_bytes\ncat /sys/fs/cgroup/memory/kubepods/pod<uid>/memory.stat',
    explanation: 'kubectl top shows working_set_bytes (RSS + active cache), not total cgroup usage. The OOM killer triggers on cgroup memory.usage_in_bytes which includes inactive file cache.',
    tags: ['kubernetes', 'oomkilled', 'memory-metrics', 'cgroup', 'working-set'],
  },
  {
    id: 'k8s-troubleshooting-l3-038',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You need to debug a CrashLoopBackOff pod where the container exits too quickly to exec into it. What is the best approach?',
    options: [
      'Keep running kubectl exec hoping to catch it',
      'Use kubectl debug to create a copy of the pod with the entrypoint changed to sleep infinity',
      'Delete the pod and recreate with more memory',
      'Increase the restart backoff time',
    ],
    correctAnswer: 1,
    codeSnippet: '# Create a copy with sleep command to keep it alive\nkubectl debug my-crashing-pod -it \\\n  --copy-to=debug-pod \\\n  --container=my-app \\\n  -- sleep infinity\n\n# Now you can exec into the debug copy\nkubectl exec -it debug-pod -- sh\n\n# Inspect the filesystem, environment, config\nenv\ncat /app/config.yaml\nls -la /app/',
    explanation: 'kubectl debug --copy-to creates a pod copy with a different command (sleep infinity), keeping the same volumes, env vars, and config so you can investigate the crash cause.',
    tags: ['kubernetes', 'debug', 'crashloopbackoff', 'sleep-infinity'],
  },
  {
    id: 'k8s-troubleshooting-l3-039',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A pod is Pending due to "Insufficient pods" in the scheduler event. What does this mean?',
    options: [
      'The namespace has too few pods',
      'The node has reached its maximum pod count (--max-pods kubelet configuration)',
      'The cluster needs more nodes',
      'The pod spec is invalid',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check node max pods\nkubectl describe node <name> | grep -A5 "Capacity"\n# Capacity:\n#   pods: 110  # Default max pods per node\n\n# Check current pod count\nkubectl get pods --field-selector spec.nodeName=<name> | wc -l\n\n# kubelet config: --max-pods=110 (default)\n# Can also be limited by pod CIDR size',
    explanation: 'Each node has a maximum pod count (default 110, configurable via --max-pods). "Insufficient pods" means the node cannot accept more pods, not that there are too few pods.',
    tags: ['kubernetes', 'max-pods', 'pending', 'node-capacity'],
  },
  {
    id: 'k8s-troubleshooting-l3-040',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'A node shows high "PLEG relist latency" in kubelet metrics. What does this indicate?',
    options: [
      'The node network is slow',
      'The Pod Lifecycle Event Generator is slow, usually due to container runtime issues or too many containers on the node',
      'The API server is slow',
      'The node clock is out of sync',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check kubelet logs for PLEG issues\njournalctl -u kubelet | grep -i PLEG\n\n# Common messages:\n# "PLEG is not healthy: pleg was last seen active Xm ago"\n# "skipping pod synchronization - PLEG is not healthy"\n\n# Causes:\n# - Too many containers on the node\n# - Container runtime (containerd) performance issues\n# - Disk I/O bottleneck affecting container operations',
    explanation: 'PLEG (Pod Lifecycle Event Generator) periodically lists all containers to detect state changes. High latency means the container runtime is slow, often due to too many containers or I/O issues.',
    tags: ['kubernetes', 'pleg', 'kubelet', 'performance', 'node'],
  },
  {
    id: 'k8s-troubleshooting-l3-041',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A pod with securityContext.readOnlyRootFilesystem: true crashes because the application tries to write to /tmp. How do you fix this without removing the security restriction?',
    options: [
      'Remove the readOnlyRootFilesystem setting',
      'Mount an emptyDir volume at /tmp',
      'Change the application to not use /tmp',
      'Use a privileged container',
    ],
    correctAnswer: 1,
    codeSnippet: 'securityContext:\n  readOnlyRootFilesystem: true\ncontainers:\n- name: my-app\n  volumeMounts:\n  - name: tmp\n    mountPath: /tmp\n  - name: cache\n    mountPath: /app/cache\nvolumes:\n- name: tmp\n  emptyDir: {}\n- name: cache\n  emptyDir:\n    sizeLimit: 100Mi',
    explanation: 'Mount emptyDir volumes at paths where the application needs to write (like /tmp, /var/cache). This maintains the security of a read-only root filesystem while allowing necessary writes.',
    tags: ['kubernetes', 'readonly-filesystem', 'emptydir', 'security'],
  },
  {
    id: 'k8s-troubleshooting-l3-042',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service shows endpoints but curl from within the cluster hangs and eventually times out. The pods are running and healthy. What is the issue?',
    options: [
      'The curl command is wrong',
      'A NetworkPolicy is blocking ingress to the pods, or the container is not actually listening on the Service targetPort',
      'The DNS TTL is too high',
      'The Service needs to be recreated',
    ],
    correctAnswer: 1,
    codeSnippet: '# Systematically debug:\n# 1. Check if pod is listening on the port\nkubectl exec <pod> -- netstat -tlnp  # or ss -tlnp\n\n# 2. Try connecting to pod IP directly\nkubectl exec debug-pod -- curl -v <pod-ip>:<container-port>\n\n# 3. Check NetworkPolicies in the namespace\nkubectl get networkpolicy -n <namespace>\n\n# 4. If pod IP works but service doesn\'t, check kube-proxy',
    explanation: 'If Service endpoints exist but connections hang, either a NetworkPolicy is dropping packets or the container is not listening on the expected port. Verify by testing the pod IP directly.',
    tags: ['kubernetes', 'service', 'timeout', 'networkpolicy', 'debugging'],
  },
  {
    id: 'k8s-troubleshooting-l3-043',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'Pods on a node are experiencing high latency. kubectl top node shows normal CPU/memory, but iowait is high. What is happening?',
    options: [
      'The CPU is overloaded',
      'The node disk I/O is saturated, causing processes to wait for disk operations, which manifests as high latency',
      'The network is congested',
      'The kubelet is using too much memory',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check I/O wait on the node\ntop  # Check %wa column\n\n# Check disk utilization\niostat -x 1\n# Look for: %util close to 100%, high await times\n\n# Identify which pods/containers are causing I/O\niotop -o\n\n# Solutions:\n# - Move I/O-heavy pods to nodes with faster storage\n# - Use local SSDs instead of network-attached storage\n# - Set I/O resource limits (blkio cgroup)',
    explanation: 'High iowait means processes are blocked waiting for disk I/O. Even with available CPU and memory, disk saturation causes severe latency. Use iostat and iotop to identify the source.',
    tags: ['kubernetes', 'iowait', 'disk-io', 'performance', 'latency'],
  },
  {
    id: 'k8s-troubleshooting-l3-044',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'CoreDNS logs show "HINFO: read udp ... i/o timeout" for every query to an upstream nameserver. What is the likely cause?',
    options: [
      'CoreDNS is misconfigured',
      'A firewall or NetworkPolicy is blocking UDP port 53 egress from CoreDNS pods to the upstream DNS server',
      'The upstream DNS server is overloaded',
      'CoreDNS needs more replicas',
    ],
    correctAnswer: 1,
    codeSnippet: '# Test from CoreDNS pod directly\nkubectl exec -n kube-system <coredns-pod> -- \\\n  nslookup google.com <upstream-dns-ip>\n\n# Check if NetworkPolicy blocks CoreDNS egress\nkubectl get networkpolicy -n kube-system\n\n# Check node firewall rules\niptables -L -n | grep 53',
    explanation: 'UDP i/o timeout for upstream DNS indicates the packets are being dropped. This is commonly caused by NetworkPolicies in kube-system or node-level firewall rules blocking CoreDNS egress.',
    tags: ['kubernetes', 'coredns', 'upstream-dns', 'firewall', 'timeout'],
  },
  {
    id: 'k8s-troubleshooting-l3-045',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'During a rolling update, the PodDisruptionBudget prevents all old pods from being evicted, causing the update to stall. How do you resolve this without violating the PDB?',
    options: [
      'Delete the PDB temporarily',
      'Check that the Deployment strategy maxUnavailable and PDB minAvailable are compatible, and ensure new pods are becoming Ready',
      'Scale the deployment to 0 and back up',
      'Force delete all old pods',
    ],
    correctAnswer: 1,
    codeSnippet: '# Problem: PDB minAvailable = 3, replicas = 3\n# Rolling update can\'t evict any pod!\n\n# Fix: Ensure maxSurge allows new pods first\napiVersion: apps/v1\nkind: Deployment\nspec:\n  replicas: 3\n  strategy:\n    rollingUpdate:\n      maxSurge: 1        # Create 1 new pod first\n      maxUnavailable: 0  # Then drain 1 old pod\n---\napiVersion: policy/v1\nkind: PodDisruptionBudget\nspec:\n  minAvailable: 2  # Allow 1 to be unavailable',
    explanation: 'If minAvailable equals replicas and maxSurge is 0, no pod can be evicted. Ensure maxSurge allows new pods to come up before old ones are removed, and PDB allows at least 1 unavailable.',
    tags: ['kubernetes', 'pdb', 'rolling-update', 'stalled', 'deployment'],
  },
  {
    id: 'k8s-troubleshooting-l3-046',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'init-container-failures',
    question: 'An init container succeeds on initial deployment but fails after pod restart with "resource already exists" error during a migration script. What is the issue?',
    options: [
      'Init containers are not supposed to be idempotent',
      'The migration script is not idempotent; it fails when run a second time because resources already exist from the first run',
      'The init container image is cached',
      'The PV data is corrupted',
    ],
    correctAnswer: 1,
    explanation: 'Init containers run every time a pod starts, including restarts. Migration scripts and initialization logic must be idempotent to handle repeated execution safely.',
    codeSnippet: '# Non-idempotent (fails on restart):\ncommand: ["sql", "CREATE TABLE users (...)"]\n\n# Idempotent (safe on restart):\ncommand: ["sql", "CREATE TABLE IF NOT EXISTS users (...)"]\n\n# Or use migration tools that track state:\ncommand: ["flyway", "migrate"]',
    tags: ['kubernetes', 'init-containers', 'idempotent', 'migrations'],
  },
  {
    id: 'k8s-troubleshooting-l3-047',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'After switching from iptables to IPVS mode in kube-proxy, some Services stop working. What should you check?',
    options: [
      'IPVS does not support Kubernetes Services',
      'IPVS kernel modules must be loaded (ip_vs, ip_vs_rr, etc.) and the IPVS proxy rules must be correctly created',
      'IPVS only supports ClusterIP services',
      'The CNI plugin must be changed',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check required IPVS kernel modules\nlsmod | grep ip_vs\n# Required: ip_vs, ip_vs_rr, ip_vs_wrr, ip_vs_sh, nf_conntrack\n\n# Check IPVS rules\nipvsadm -Ln\n\n# Verify kube-proxy is running in IPVS mode\nkubectl logs -n kube-system -l k8s-app=kube-proxy | grep "Using ipvs"',
    explanation: 'IPVS mode requires specific kernel modules (ip_vs, ip_vs_rr, ip_vs_wrr, ip_vs_sh). If modules are missing, kube-proxy may fall back to iptables silently or fail.',
    tags: ['kubernetes', 'kube-proxy', 'ipvs', 'kernel-modules'],
  },
  {
    id: 'k8s-troubleshooting-l3-048',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A Python application pod is in CrashLoopBackOff. Logs show "PermissionError: [Errno 13] Permission denied: \'/app/data\'". The directory exists in the image. What is the issue?',
    options: [
      'The Python version is incompatible',
      'The container is running as a non-root user (runAsUser) that does not have write permissions to the directory created by root during image build',
      'The directory is on a read-only volume',
      'Python cannot access container directories',
    ],
    correctAnswer: 1,
    codeSnippet: '# Dockerfile creates dir as root\nFROM python:3.11\nRUN mkdir /app/data  # Owned by root\n\n# But pod runs as non-root\nsecurityContext:\n  runAsUser: 1000\n\n# Fix in Dockerfile:\nRUN mkdir /app/data && chown 1000:1000 /app/data\n# Or use fsGroup:\nsecurityContext:\n  runAsUser: 1000\n  fsGroup: 1000',
    explanation: 'Directories created during image build are owned by root. When runAsUser forces a non-root UID, the container cannot write to those directories. Fix ownership in Dockerfile or use fsGroup.',
    tags: ['kubernetes', 'permissions', 'runasuser', 'security-context'],
  },
  {
    id: 'k8s-troubleshooting-l3-049',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container is OOMKilled despite having no memory limit set. How is this possible?',
    options: [
      'All containers must have memory limits',
      'A LimitRange in the namespace is applying a default memory limit, or the node itself is under memory pressure',
      'The container runtime enforces its own limits',
      'OOMKill only happens with explicit limits',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check if LimitRange applies defaults\nkubectl get limitrange -n <namespace> -o yaml\n\n# Example LimitRange applying default limits:\napiVersion: v1\nkind: LimitRange\nmetadata:\n  name: mem-limit\nspec:\n  limits:\n  - default:\n      memory: 256Mi    # Applied if not specified\n    defaultRequest:\n      memory: 128Mi\n    type: Container',
    explanation: 'Even without explicit limits, a LimitRange can inject default memory limits. Additionally, the node Linux kernel OOM killer can kill processes when the node runs out of memory, regardless of container limits.',
    tags: ['kubernetes', 'oomkilled', 'limitrange', 'default-limits'],
  },
  {
    id: 'k8s-troubleshooting-l3-050',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A CSI driver-based PVC shows "volume published but error expanding volume". What is happening and how do you fix it?',
    options: [
      'The CSI driver does not support volumes',
      'The StorageClass needs allowVolumeExpansion: true, and the CSI driver must support the ControllerExpandVolume and NodeExpandVolume capabilities',
      'Volume expansion is not a Kubernetes feature',
      'You need to delete and recreate the PVC',
    ],
    correctAnswer: 1,
    codeSnippet: '# StorageClass must allow expansion\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: expandable-sc\nprovisioner: ebs.csi.aws.com\nallowVolumeExpansion: true\n\n# Expand PVC by editing the spec\nkubectl edit pvc my-pvc\n# Change spec.resources.requests.storage to new size\n\n# CSI driver capabilities needed:\n# - ControllerExpandVolume (resize in cloud)\n# - NodeExpandVolume (resize filesystem on node)',
    explanation: 'Volume expansion requires both the StorageClass setting (allowVolumeExpansion: true) and the CSI driver to implement the expand capabilities. The filesystem is resized on the next pod mount.',
    tags: ['kubernetes', 'csi', 'volume-expansion', 'storageclass'],
  },
  {
    id: 'k8s-troubleshooting-l3-051',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'An aggregated ClusterRole is not picking up new rules from a sub-ClusterRole. What should you check?',
    options: [
      'Aggregated ClusterRoles are not supported',
      'The sub-ClusterRole must have labels matching the aggregationRule.clusterRoleSelectors in the parent ClusterRole',
      'You must restart the API server',
      'The sub-ClusterRole must be in the same namespace',
    ],
    correctAnswer: 1,
    codeSnippet: '# Parent aggregated ClusterRole\napiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRole\nmetadata:\n  name: monitoring\naggregationRule:\n  clusterRoleSelectors:\n  - matchLabels:\n      rbac.example.com/aggregate-to-monitoring: "true"\n\n# Sub-ClusterRole (must have matching label)\napiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRole\nmetadata:\n  name: monitoring-endpoints\n  labels:\n    rbac.example.com/aggregate-to-monitoring: "true"  # Must match!\nrules:\n- apiGroups: [""]\n  resources: ["endpoints"]\n  verbs: ["get", "list"]',
    explanation: 'Aggregated ClusterRoles automatically combine rules from ClusterRoles matching the label selector. If the labels do not match, the rules are not aggregated.',
    tags: ['kubernetes', 'rbac', 'aggregation', 'clusterrole'],
  },
  {
    id: 'k8s-troubleshooting-l3-052',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'After a deployment rollout, new pods are Running and Ready but the Service returns mixed responses from old and new versions. What is happening?',
    options: [
      'The Service is caching old responses',
      'The Service selector matches both old and new ReplicaSet pods because labels have not changed between versions',
      'CoreDNS needs to be restarted',
      'The Service type needs to change',
    ],
    correctAnswer: 1,
    explanation: 'During a rolling update, both old and new ReplicaSet pods match the Service selector. This is expected behavior. Ensure maxUnavailable and maxSurge are configured correctly for your deployment strategy.',
    tags: ['kubernetes', 'service', 'rolling-update', 'selector', 'mixed-traffic'],
  },
  {
    id: 'k8s-troubleshooting-l3-053',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'The event "Warning FailedKillPod" appears after scaling down a deployment. What does this indicate?',
    options: [
      'The pod image is corrupted',
      'The kubelet failed to terminate the pod, possibly because the container runtime could not stop the container or a preStop hook is hanging',
      'The pod does not exist',
      'RBAC prevents pod deletion',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check if preStop hook is hanging\nkubectl get pod <pod> -o jsonpath=\'{.spec.containers[*].lifecycle}\'\n\n# Check container runtime status on the node\ncrictl ps -a | grep <container-id>\ncrictl stop <container-id>\n\n# Check kubelet logs\njournalctl -u kubelet | grep "kill" | grep <pod-name>',
    explanation: 'FailedKillPod means the kubelet could not terminate the pod. Causes include hanging preStop hooks, unresponsive container runtime, or processes ignoring SIGTERM and SIGKILL.',
    tags: ['kubernetes', 'failedkillpod', 'termination', 'prestop'],
  },
  {
    id: 'k8s-troubleshooting-l3-054',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'A gRPC service health check probe is failing even though the gRPC health service is implemented. What might be wrong?',
    options: [
      'gRPC probes are not supported in Kubernetes',
      'The grpc probe feature requires Kubernetes 1.24+ and the health check must use the standard grpc.health.v1.Health service on the correct port',
      'gRPC services cannot have probes',
      'Only HTTP probes work for gRPC',
    ],
    correctAnswer: 1,
    codeSnippet: '# gRPC probe (Kubernetes 1.24+ GA)\nlivenessProbe:\n  grpc:\n    port: 50051\n    service: "my-service"  # Optional, defaults to ""\n  initialDelaySeconds: 10\n\n# The container must implement:\n# grpc.health.v1.Health/Check RPC\n# Responding with SERVING status',
    explanation: 'gRPC native probes require the container to implement the standard gRPC health checking protocol (grpc.health.v1.Health). The service name in the probe must match what the server registers.',
    tags: ['kubernetes', 'grpc', 'probe', 'health-check'],
  },
  {
    id: 'k8s-troubleshooting-l3-055',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A cluster autoscaler is not adding nodes despite Pending pods. What could prevent scale-up?',
    options: [
      'The autoscaler only scales down',
      'The autoscaler respects max node limits in the node group, pod resource requests may not match any available instance type, or the cloud provider quota is exceeded',
      'Pending pods do not trigger autoscaling',
      'The autoscaler only works with HPA',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check cluster autoscaler status\nkubectl get configmap cluster-autoscaler-status -n kube-system -o yaml\n\n# Common scale-up blockers:\n# - Node group at maxSize\n# - Pod requests too large for any instance type\n# - Cloud provider quota/limits exceeded\n# - Pod has nodeSelector/affinity not matching any node group\n# - Scale-up event recently failed (backoff)',
    explanation: 'The cluster autoscaler can be blocked by: max node group size, no instance type matching pod requirements, cloud quotas, or pod affinity rules that no scalable node group satisfies.',
    tags: ['kubernetes', 'cluster-autoscaler', 'scale-up', 'pending'],
  },
  {
    id: 'k8s-troubleshooting-l3-056',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You need to inspect the filesystem of a running container that uses a distroless image with no shell. Without creating a pod copy, how do you access its files?',
    options: [
      'kubectl cp always works without a shell',
      'Use kubectl debug with --target and an image that has a shell; the shared PID namespace gives access to the target container filesystem via /proc/<pid>/root',
      'Mount the container volume to your local machine',
      'Use kubectl exec with the --no-shell flag',
    ],
    correctAnswer: 1,
    codeSnippet: '# Attach debug container with shared PID namespace\nkubectl debug -it my-pod --image=busybox --target=my-container\n\n# Inside the debug container, access target filesystem:\n# Find PID of the target container\'s main process\nps aux  # Look for the target process\n\n# Access its filesystem via /proc\nls /proc/1/root/app/\ncat /proc/1/root/app/config.yaml',
    explanation: 'With --target, the debug container shares the PID namespace. You can access the target container filesystem through /proc/<pid>/root, bypassing the need for a shell in the target container.',
    tags: ['kubernetes', 'debug', 'distroless', 'pid-namespace', 'proc'],
  },
  {
    id: 'k8s-troubleshooting-l3-057',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'imagepullbackoff',
    question: 'A multi-arch cluster (AMD64 + ARM64) has image pull failures only on ARM64 nodes. What is the issue?',
    options: [
      'ARM64 nodes cannot pull images',
      'The container image only has an AMD64 manifest; it needs a multi-arch manifest that includes ARM64',
      'The container runtime is different on ARM64',
      'ARM64 nodes need special imagePullSecrets',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check image manifest architectures\ndocker manifest inspect myregistry.io/myapp:v1\n\n# Build multi-arch image:\ndocker buildx build --platform linux/amd64,linux/arm64 \\\n  -t myregistry.io/myapp:v1 --push .\n\n# Or use crane to check:\ncrane manifest myregistry.io/myapp:v1 | jq \'.manifests[].platform\'',
    explanation: 'Multi-arch clusters require container images with manifests for each architecture. If the image only has an AMD64 manifest, ARM64 nodes will fail to pull or get exec format errors.',
    tags: ['kubernetes', 'multi-arch', 'arm64', 'image-manifest'],
  },
  {
    id: 'k8s-troubleshooting-l3-058',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'The kubelet is reporting "eviction manager: attempting to reclaim ephemeral-storage" but no pods are using significant disk. What is consuming the ephemeral storage?',
    options: [
      'The container images cached on the node',
      'Container image layers, container writable layers, container log files, and dead container filesystem data',
      'etcd data',
      'Kubernetes certificates',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check disk usage on the node\ndu -sh /var/lib/containerd/  # Container runtime data\ndu -sh /var/log/pods/        # Pod logs\ndu -sh /var/log/containers/  # Container logs\n\n# Garbage collect dead containers\ncrictl rmi --prune\n\n# Check for large log files\nfind /var/log/pods -name "*.log" -size +100M',
    explanation: 'Ephemeral storage includes container images, writable layers, and log files. Stale container data and large log files from pods without log rotation can consume significant disk space.',
    tags: ['kubernetes', 'ephemeral-storage', 'disk', 'garbage-collection'],
  },
  {
    id: 'k8s-troubleshooting-l3-059',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'Pods using hostNetwork: true can communicate with external services but not with cluster Services by ClusterIP. Why?',
    options: [
      'hostNetwork pods use a different DNS server',
      'hostNetwork pods use the node network stack and kube-proxy iptables rules may not apply correctly, or the pod is trying to reach a ClusterIP that is not routable from the host network',
      'ClusterIP services do not support hostNetwork pods',
      'The CNI plugin blocks hostNetwork pods',
    ],
    correctAnswer: 1,
    codeSnippet: '# hostNetwork pods should set dnsPolicy correctly\nspec:\n  hostNetwork: true\n  dnsPolicy: ClusterFirstWithHostNet  # Important!\n  # Default dnsPolicy: ClusterFirst doesn\'t work with hostNetwork\n\n# If ClusterIPs are unreachable, check kube-proxy\n# mode and iptables rules on the host',
    explanation: 'hostNetwork pods bypass the pod network. DNS must use ClusterFirstWithHostNet to reach cluster DNS. ClusterIP routing depends on kube-proxy rules on the host network stack.',
    tags: ['kubernetes', 'hostnetwork', 'clusterip', 'dnspolicy'],
  },
  {
    id: 'k8s-troubleshooting-l3-060',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A pod with topology spread constraints is Pending. Events show "doesn\'t satisfy spread constraint". How do you diagnose this?',
    options: [
      'Topology spread constraints are not supported',
      'Check if the topologyKey labels exist on nodes and if the maxSkew allows placement given current pod distribution across topology domains',
      'Delete the topology constraints',
      'Add more replicas',
    ],
    correctAnswer: 1,
    codeSnippet: 'topologySpreadConstraints:\n- maxSkew: 1\n  topologyKey: topology.kubernetes.io/zone\n  whenUnsatisfiable: DoNotSchedule\n  labelSelector:\n    matchLabels:\n      app: my-app\n\n# Check current distribution:\nkubectl get pods -l app=my-app -o wide\n# If zone-a has 3 pods, zone-b has 2,\n# maxSkew:1 means zone-a can\'t have more than zone-b + 1',
    explanation: 'Topology spread constraints limit the skew of pod distribution across topology domains. If placing a new pod would exceed maxSkew, and whenUnsatisfiable is DoNotSchedule, the pod stays Pending.',
    tags: ['kubernetes', 'topology-spread', 'scheduling', 'pending'],
  },
  {
    id: 'k8s-troubleshooting-l3-061',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A container works with Docker but crashes in Kubernetes with "standard_init_linux.go: exec user process caused: no such file or directory". The binary exists in the image. What is the cause?',
    options: [
      'The Kubernetes version is incompatible',
      'The binary is dynamically linked against libraries not present in the image (e.g., libc), or the shebang line references a missing interpreter',
      'The binary needs execute permissions',
      'The container runtime is different',
    ],
    correctAnswer: 1,
    codeSnippet: '# Common cause: Go binary built with CGO_ENABLED=1\n# linked against glibc, but image is alpine (musl)\n\n# Fix: Build static binary\nCGO_ENABLED=0 go build -o myapp .\n\n# Or use matching base image\nFROM golang:1.21 AS builder\nRUN CGO_ENABLED=0 go build -o /myapp .\nFROM gcr.io/distroless/static\nCOPY --from=builder /myapp /myapp',
    explanation: 'This error occurs when the dynamic linker cannot find required shared libraries. Common with Go binaries built with CGO on glibc but run in alpine/scratch/distroless images. Build with CGO_ENABLED=0.',
    tags: ['kubernetes', 'dynamic-linking', 'cgo', 'distroless', 'crash'],
  },
  {
    id: 'k8s-troubleshooting-l3-062',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'DNS resolution is slow for external domains with the default ndots:5 setting. How do you optimize this without breaking internal resolution?',
    options: [
      'Disable DNS entirely',
      'Lower ndots to 2 or use FQDNs with trailing dots for external domains to skip search domain lookups',
      'Increase CoreDNS cache to 1 hour',
      'Use a custom DNS server outside the cluster',
    ],
    correctAnswer: 1,
    codeSnippet: '# Option 1: Use FQDN with trailing dot (no search domains tried)\ncurl http://api.example.com.  # Note trailing dot\n\n# Option 2: Lower ndots per pod\nspec:\n  dnsConfig:\n    options:\n    - name: ndots\n      value: "2"\n  # Internal names like "my-svc.my-ns" have 1 dot (< 2)\n  # so search domains are still tried for internal names\n  # External names like "api.example.com" have 2 dots\n  # so they\'re queried directly without search domains',
    explanation: 'With ndots:5, a name like "api.example.com" (2 dots < 5) triggers 4-5 search domain lookups before the absolute query. Lowering ndots or using trailing dots reduces unnecessary DNS queries.',
    tags: ['kubernetes', 'dns', 'ndots', 'performance', 'optimization'],
  },
  {
    id: 'k8s-troubleshooting-l3-063',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A container memory limit is 1Gi but kubectl describe shows lastState.terminated.reason: OOMKilled with exit code 137, and the container memory.max_usage_in_bytes in cgroup shows exactly 1073741824. What does this confirm?',
    options: [
      'The OOM was caused by the node running out of memory',
      'The container hit its exact cgroup memory limit and the kernel OOM killer terminated it',
      'The OOM was caused by swap exhaustion',
      'The limit was calculated incorrectly',
    ],
    correctAnswer: 1,
    explanation: '1073741824 bytes = exactly 1Gi. The cgroup memory.max_usage_in_bytes matching the limit confirms the container reached its cgroup limit, triggering the kernel OOM killer (exit code 137 = SIGKILL).',
    tags: ['kubernetes', 'oomkilled', 'cgroup', 'memory-limit', 'confirmation'],
  },
  {
    id: 'k8s-troubleshooting-l3-064',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service with sessionAffinity: ClientIP works correctly for external traffic but not for pod-to-pod traffic within the cluster. Why?',
    options: [
      'Session affinity is not supported for internal traffic',
      'SNAT on internal traffic can change the source IP, breaking client IP-based session affinity; some CNI plugins preserve source IP while others do not',
      'The sessionAffinityConfig timeout is too short',
      'ClusterIP services do not support session affinity',
    ],
    correctAnswer: 1,
    explanation: 'Some CNI plugins or kube-proxy configurations perform SNAT on pod-to-service traffic, changing the source IP. Since session affinity is based on client IP, SNAT can break it for internal traffic.',
    tags: ['kubernetes', 'session-affinity', 'snat', 'source-ip'],
  },
  {
    id: 'k8s-troubleshooting-l3-065',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'Monitoring shows nodes have plenty of available resources, but new pods remain Pending. kubectl describe reports "0/5 nodes are available: 5 node(s) had taint {node.kubernetes.io/unreachable: }". What happened?',
    options: [
      'The nodes have PID pressure',
      'The node controller added unreachable taints because nodes lost connectivity to the API server; this is often a control plane or network issue',
      'The pods have the wrong image',
      'The scheduler is not running',
    ],
    correctAnswer: 1,
    explanation: 'The node controller automatically adds node.kubernetes.io/unreachable taint when it cannot reach nodes. This prevents scheduling. Investigate network connectivity between nodes and the control plane.',
    tags: ['kubernetes', 'unreachable', 'taint', 'control-plane', 'network'],
  },
  {
    id: 'k8s-troubleshooting-l3-066',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A PV with reclaimPolicy: Retain is Released after deleting the PVC, but a new PVC cannot bind to it. How do you make the PV available again?',
    options: [
      'Delete and recreate the PV',
      'Remove the spec.claimRef from the PV to clear the binding',
      'Change the reclaim policy to Delete',
      'Create a new StorageClass',
    ],
    correctAnswer: 1,
    codeSnippet: '# PV in Released state\nkubectl get pv my-pv\n# NAME    CAPACITY  STATUS     CLAIM\n# my-pv   10Gi      Released   default/old-pvc\n\n# Clear the claimRef to make it Available\nkubectl patch pv my-pv --type json -p \\\n  \'[{"op": "remove", "path": "/spec/claimRef"}]\'\n\n# Now it shows Available and can be bound to new PVC\nkubectl get pv my-pv\n# NAME    CAPACITY  STATUS      CLAIM\n# my-pv   10Gi      Available',
    explanation: 'A Released PV retains its claimRef from the deleted PVC. Removing the claimRef makes the PV Available for binding to a new PVC. The data on the volume is preserved.',
    tags: ['kubernetes', 'pv', 'released', 'claimref', 'retain'],
  },
  {
    id: 'k8s-troubleshooting-l3-067',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A pod running a kubectl command inside the container gets "error: the server doesn\'t have a resource type pods". The SA has correct RBAC. What else could be wrong?',
    options: [
      'kubectl is not installed in the container',
      'The KUBERNETES_SERVICE_HOST or KUBERNETES_SERVICE_PORT environment variables are wrong or the service account token is expired/invalid',
      'The pod has no labels',
      'The cluster does not have the pods API',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check env vars inside the pod\nenv | grep KUBERNETES\n\n# Check service account token\ncat /var/run/secrets/kubernetes.io/serviceaccount/token\n\n# Check if API server is reachable\ncurl -k https://$KUBERNETES_SERVICE_HOST:$KUBERNETES_SERVICE_PORT/api/v1\n\n# Common issues:\n# - automountServiceAccountToken: false\n# - Projected token volume misconfigured\n# - API server TLS CA not in the SA mount',
    explanation: 'Even with correct RBAC, API access can fail if the environment variables are wrong, the service account token is not mounted, or the API server CA certificate is missing from the projected volume.',
    tags: ['kubernetes', 'api-access', 'serviceaccount', 'token', 'in-cluster'],
  },
  {
    id: 'k8s-troubleshooting-l3-068',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pod-eviction',
    question: 'A node is being drained but kubectl drain hangs for a specific pod. The pod belongs to a ReplicaSet with a PDB that minAvailable equals the current replica count. What is the correct resolution?',
    options: [
      'Force delete the pod',
      'Temporarily scale up the ReplicaSet so that a new pod starts on another node, then the PDB allows evicting the old pod',
      'Delete the PDB',
      'Reboot the node',
    ],
    correctAnswer: 1,
    explanation: 'When PDB minAvailable equals total replicas, drain cannot evict any pod. Scaling up adds a new pod on another node, making total replicas > minAvailable, allowing drain to evict the original pod.',
    tags: ['kubernetes', 'drain', 'pdb', 'scale-up', 'resolution'],
  },
  {
    id: 'k8s-troubleshooting-l3-069',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'A pod with multiple containers has containers that need to communicate. One container listens on localhost:8080 but another container cannot reach it. Why?',
    options: [
      'Containers in the same pod use different network namespaces',
      'This should work because containers in the same pod share the network namespace; check if the container is actually binding to 127.0.0.1 vs 0.0.0.0',
      'Containers must use the pod IP, not localhost',
      'A NetworkPolicy blocks localhost traffic',
    ],
    correctAnswer: 1,
    codeSnippet: '# All containers in a pod share localhost\n# Container A binds to 0.0.0.0:8080 -> accessible by Container B\n# Container A binds to 127.0.0.1:8080 -> accessible by Container B\n\n# Debug: check what each container is listening on\nkubectl exec my-pod -c container-a -- ss -tlnp\nkubectl exec my-pod -c container-b -- curl localhost:8080',
    explanation: 'Containers in the same pod share the network namespace, so localhost should work. If it does not, verify the application is actually binding to the expected port and interface.',
    tags: ['kubernetes', 'multi-container', 'localhost', 'networking'],
  },
  {
    id: 'k8s-troubleshooting-l3-070',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'init-container-failures',
    question: 'An init container needs to set a kernel parameter (sysctl) for the application. It fails with "read-only file system" when writing to /proc/sys. How do you solve this?',
    options: [
      'Run the init container as root',
      'Use the pod-level securityContext.sysctls field for safe sysctls, or annotate the node for unsafe sysctls',
      'Mount /proc as a volume',
      'Use a DaemonSet to set sysctls on the host',
    ],
    correctAnswer: 1,
    codeSnippet: '# Safe sysctls can be set in pod spec\nspec:\n  securityContext:\n    sysctls:\n    - name: net.core.somaxconn\n      value: "1024"\n\n# Unsafe sysctls require kubelet configuration\n# --allowed-unsafe-sysctls=net.ipv4.tcp_keepalive_time\nspec:\n  securityContext:\n    sysctls:\n    - name: net.ipv4.tcp_keepalive_time\n      value: "60"',
    explanation: 'Sysctls should be set via the pod securityContext.sysctls field, not by writing to /proc/sys. Safe sysctls work by default; unsafe sysctls require kubelet-level allowlisting.',
    tags: ['kubernetes', 'sysctls', 'init-containers', 'kernel-parameters'],
  },
  {
    id: 'k8s-troubleshooting-l3-071',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'Events show "Warning EvictionThresholdMet" on a node but no pods are actually evicted. What does this mean?',
    options: [
      'The event is a false positive',
      'The kubelet detected resource pressure exceeding the soft eviction threshold but the condition recovered within the grace period, so no eviction occurred',
      'Eviction is disabled on this node',
      'The pod priority prevented eviction',
    ],
    correctAnswer: 1,
    explanation: 'Soft eviction thresholds trigger a warning event when crossed but allow a grace period for recovery. If the resource pressure drops back below the threshold within the grace period, no eviction occurs.',
    tags: ['kubernetes', 'eviction', 'soft-threshold', 'events'],
  },
  {
    id: 'k8s-troubleshooting-l3-072',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A deployment using RollingUpdate strategy has maxUnavailable: 0 and maxSurge: 1. New pods enter CrashLoopBackOff. The old pods continue to run. What is the state of the rollout?',
    options: [
      'The old pods are immediately replaced',
      'The rollout is stuck: new pods keep crashing, old pods remain because maxUnavailable: 0 prevents removing healthy old pods; use kubectl rollout undo to roll back',
      'The rollout succeeds with the old pods',
      'The deployment is deleted',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check rollout status\nkubectl rollout status deployment/my-app\n# Waiting for deployment "my-app" rollout to finish...\n\n# Check replica sets\nkubectl get rs -l app=my-app\n# NAME           DESIRED  CURRENT  READY\n# my-app-abc123  3        3        3    # Old (healthy)\n# my-app-def456  1        1        0    # New (crashing)\n\n# Rollback\nkubectl rollout undo deployment/my-app',
    explanation: 'With maxUnavailable: 0, Kubernetes will not remove old pods until new ones are Ready. Since new pods crash, the rollout stalls. Use kubectl rollout undo to revert to the working version.',
    tags: ['kubernetes', 'rolling-update', 'stuck-rollout', 'rollback'],
  },
  {
    id: 'k8s-troubleshooting-l3-073',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'node-issues',
    question: 'A node has a condition "Ready: True" but also "MemoryPressure: True". What does this combination mean?',
    options: [
      'This combination is impossible',
      'The node is functional (kubelet is healthy) but is experiencing memory pressure and may start evicting pods',
      'The node is shutting down',
      'The memory pressure condition is stale',
    ],
    correctAnswer: 1,
    explanation: 'Ready: True means the kubelet is healthy and responsive. MemoryPressure: True means available memory is below the eviction threshold. The node is functional but actively managing memory by potentially evicting pods.',
    tags: ['kubernetes', 'node-conditions', 'memory-pressure', 'ready'],
  },
  {
    id: 'k8s-troubleshooting-l3-074',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'probe-failures',
    question: 'A health check endpoint returns 200 for the readiness probe but the pod shows 0/1 Ready. The Events show no probe failures. What else could prevent Ready status?',
    options: [
      'The container image is wrong',
      'A readinessGate condition is not met, or the container has not yet passed its startup probe',
      'The Service selector does not match',
      'The namespace has a quota issue',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check pod conditions\nkubectl get pod my-pod -o jsonpath=\'{.status.conditions}\' | jq .\n\n# Look for:\n# - ContainersReady: True/False\n# - Ready: True/False (includes readinessGates)\n# - Custom conditions from readinessGates\n\n# Check if startup probe is still running\nkubectl describe pod my-pod | grep -A10 "Startup"',
    explanation: 'Ready status depends on multiple factors: container readiness probes, startup probes (which block readiness), and readinessGates (custom conditions). All must be satisfied for Ready: True.',
    tags: ['kubernetes', 'readiness', 'conditions', 'readiness-gates', 'startup-probe'],
  },
  {
    id: 'k8s-troubleshooting-l3-075',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'An ephemeral container cannot see the target container processes even with --target flag. What is wrong?',
    options: [
      'Ephemeral containers never share PID namespaces',
      'The container runtime or Kubernetes version does not support the EphemeralContainerTargetPID feature, or shareProcessNamespace is disabled',
      'The target container has security restrictions',
      'Only privileged containers can share PID namespaces',
    ],
    correctAnswer: 1,
    codeSnippet: '# Ensure process namespace sharing works\n# Kubernetes 1.25+ supports this by default\n\n# Alternative: use shareProcessNamespace on pod copy\nkubectl debug my-pod -it --copy-to=debug-copy \\\n  --image=busybox \\\n  --share-processes\n\n# Inside: ps aux should show all container processes',
    explanation: 'PID namespace sharing for ephemeral containers requires sufficient Kubernetes version and container runtime support. If --target does not work, use --copy-to with --share-processes instead.',
    tags: ['kubernetes', 'debug', 'pid-namespace', 'ephemeral-containers'],
  },
  {
    id: 'k8s-troubleshooting-l3-076',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'service-debugging',
    question: 'A Service intermittently returns 503 errors. Readiness probes are passing for all pods. What could cause this?',
    options: [
      'The Service type is wrong',
      'The application has a concurrency limit or connection pool exhaustion; the readiness probe may use a lightweight endpoint that succeeds even when the app is overloaded',
      'DNS is intermittent',
      'The Service port is wrong',
    ],
    correctAnswer: 1,
    codeSnippet: '# Readiness probe may not reflect actual capacity\nreadinessProbe:\n  httpGet:\n    path: /health  # Returns 200 even when overloaded\n    port: 8080\n\n# Better: readiness endpoint that checks load\nreadinessProbe:\n  httpGet:\n    path: /ready  # Returns 503 when at capacity\n    port: 8080\n# Implementation should check:\n# - Active connections vs max\n# - Thread pool utilization\n# - Queue depth',
    explanation: 'Simple health endpoints may return 200 even when the app is at capacity. The readiness endpoint should reflect actual ability to handle requests, including checking connection pools and queue depth.',
    tags: ['kubernetes', 'service', '503', 'readiness', 'capacity'],
  },
  {
    id: 'k8s-troubleshooting-l3-077',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'resource-pressure',
    question: 'A pod has requests.cpu: 100m and limits.cpu: 100m (Guaranteed QoS for CPU). During peak traffic, response times increase dramatically. Why?',
    options: [
      'The memory limit is too low',
      'The CPU limit of 100m (0.1 core) is being hit; CFS throttling limits the container to 10ms of CPU time per 100ms period, causing latency spikes',
      'The network is congested',
      'The pod is being evicted',
    ],
    correctAnswer: 1,
    codeSnippet: '# 100m CPU = 10ms of CPU per 100ms CFS period\n# If request processing needs 50ms of CPU,\n# it takes 500ms wall-clock time due to throttling!\n\n# Consider removing CPU limits (keep requests only)\nresources:\n  requests:\n    cpu: "100m"\n    memory: "256Mi"\n  limits:\n    # No CPU limit - pod can burst beyond request\n    memory: "256Mi"',
    explanation: 'CPU limits cause CFS throttling. 100m means the container gets only 10ms of CPU per 100ms period. Under load, this causes significant latency. Many teams use CPU requests without limits.',
    tags: ['kubernetes', 'cpu-limit', 'cfs-throttling', 'latency', 'performance'],
  },
  {
    id: 'k8s-troubleshooting-l3-078',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'imagepullbackoff',
    question: 'After migrating to a new container registry, some pods pull images successfully while others get ImagePullBackOff. All pods reference the same imagePullSecret. What could be different?',
    options: [
      'The images are different sizes',
      'Pods in different namespaces may be using a different ServiceAccount that does not have the imagePullSecret, or the Secret was not created in all namespaces',
      'The pod CPU limit is too low for image pulling',
      'The container runtime differs across nodes',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check if the secret exists in all namespaces\nkubectl get secret regcred -n namespace-a  # Found\nkubectl get secret regcred -n namespace-b  # Not found!\n\n# Copy secret to another namespace\nkubectl get secret regcred -n namespace-a -o yaml | \\\n  sed \'s/namespace: namespace-a/namespace: namespace-b/\' | \\\n  kubectl apply -f -\n\n# Or use a tool like Replicator to sync secrets',
    explanation: 'Secrets are namespace-scoped. If the imagePullSecret is not created in every namespace, pods in namespaces without the secret will fail to pull from the private registry.',
    tags: ['kubernetes', 'imagepullbackoff', 'secrets', 'namespace-scope'],
  },
  {
    id: 'k8s-troubleshooting-l3-079',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'volume-errors',
    question: 'A PV using a cloud disk (e.g., EBS) is stuck in "Released" state and the corresponding cloud volume shows "in-use". Attempts to delete the PV hang. What is happening?',
    options: [
      'The PV has no reclaim policy',
      'The PV has a finalizer (kubernetes.io/pv-protection) and the CSI driver cannot detach the volume from the terminated node',
      'The PV namespace was deleted',
      'The storage class was deleted',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check PV finalizers\nkubectl get pv my-pv -o jsonpath=\'{.metadata.finalizers}\'\n# ["kubernetes.io/pv-protection"]\n\n# Check VolumeAttachment\nkubectl get volumeattachment | grep my-pv\n\n# If node is gone, clean up stale attachment\nkubectl delete volumeattachment <name>\n\n# If PV delete still hangs, the CSI driver\n# cannot complete the DeleteVolume call',
    explanation: 'PV finalizers prevent deletion until the storage is properly cleaned up. If the CSI driver cannot detach/delete the cloud volume (e.g., stale attachment to terminated node), the PV deletion hangs.',
    tags: ['kubernetes', 'pv', 'finalizer', 'csi', 'stuck-delete'],
  },
  {
    id: 'k8s-troubleshooting-l3-080',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'dns-issues',
    question: 'A pod needs to resolve both cluster DNS and a private corporate DNS for internal.company.com. How do you configure this?',
    options: [
      'Run two CoreDNS instances',
      'Configure CoreDNS with a forward plugin for the corporate domain pointing to the corporate DNS server',
      'Set dnsPolicy to None and configure all DNS manually',
      'Add the corporate DNS to each pod dnsConfig',
    ],
    correctAnswer: 1,
    codeSnippet: '# CoreDNS ConfigMap with conditional forwarding\n.:53 {\n    errors\n    health\n    kubernetes cluster.local in-addr.arpa ip6.arpa {\n       pods insecure\n       fallthrough in-addr.arpa ip6.arpa\n    }\n    forward . /etc/resolv.conf\n    cache 30\n}\ninternal.company.com:53 {\n    forward . 10.0.0.53 10.0.0.54  # Corporate DNS servers\n    cache 30\n}',
    explanation: 'CoreDNS supports conditional forwarding by defining separate server blocks for different domains. Queries for internal.company.com go to corporate DNS; everything else uses the default forward.',
    tags: ['kubernetes', 'coredns', 'conditional-forwarding', 'corporate-dns'],
  },
  {
    id: 'k8s-troubleshooting-l3-081',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'rbac-errors',
    question: 'A webhook admission controller is rejecting pod creation with "user system:serviceaccount:default:default is not allowed". The namespace has correct roles. What is the issue?',
    options: [
      'The webhook is buggy',
      'The pod is using the default service account instead of a dedicated one, and the webhook requires a specific service account or RBAC identity',
      'Webhooks do not use RBAC',
      'The namespace needs a NetworkPolicy',
    ],
    correctAnswer: 1,
    codeSnippet: '# Pods default to "default" service account\n# Create a dedicated SA with proper permissions\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: my-app-sa\n---\n# Assign to pods\nspec:\n  serviceAccountName: my-app-sa  # Not "default"',
    explanation: 'Many security policies and webhooks reject the default service account. Use dedicated service accounts for workloads with specific identity and permission requirements.',
    tags: ['kubernetes', 'rbac', 'webhook', 'default-sa', 'admission'],
  },
  {
    id: 'k8s-troubleshooting-l3-082',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'network-debugging',
    question: 'After implementing Calico NetworkPolicies using GlobalNetworkPolicy resources, standard Kubernetes NetworkPolicies stop working. Why?',
    options: [
      'Calico does not support Kubernetes NetworkPolicies',
      'Calico GlobalNetworkPolicies are evaluated before Kubernetes NetworkPolicies, and a deny-all GlobalNetworkPolicy may override the Kubernetes policies',
      'You cannot use both types simultaneously',
      'Kubernetes NetworkPolicies are deprecated with Calico',
    ],
    correctAnswer: 1,
    codeSnippet: '# Calico policy ordering:\n# 1. GlobalNetworkPolicy (lowest order first)\n# 2. NetworkPolicy (Kubernetes)\n# 3. Calico NetworkPolicy\n# 4. Profile\n\n# If a GlobalNetworkPolicy has a deny action\n# with a lower order, it takes precedence\napiVersion: projectcalico.org/v3\nkind: GlobalNetworkPolicy\nmetadata:\n  name: deny-all\nspec:\n  order: 100  # Lower = higher priority\n  ingress:\n  - action: Deny',
    explanation: 'Calico has its own policy ordering. GlobalNetworkPolicies with lower order values take precedence over standard Kubernetes NetworkPolicies, potentially blocking traffic that K8s policies allow.',
    tags: ['kubernetes', 'calico', 'networkpolicy', 'ordering', 'global'],
  },
  {
    id: 'k8s-troubleshooting-l3-083',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'oomkilled',
    question: 'A Java application with -Xmx768m and a container limit of 1Gi still gets OOMKilled. What is consuming the remaining ~256Mi?',
    options: [
      'The container overhead',
      'JVM off-heap memory: metaspace, thread stacks, direct byte buffers, code cache, GC overhead, and native memory allocations',
      'Kubernetes control plane overhead',
      'The container filesystem',
    ],
    correctAnswer: 1,
    codeSnippet: '# JVM memory components beyond -Xmx:\n# -Xmx768m         = Max heap\n# -XX:MaxMetaspaceSize=128m  # Class metadata\n# Thread stacks    = ~1Mi per thread * N threads\n# Direct buffers   = NIO allocations\n# Code cache       = JIT compiled code (~48-240Mi)\n# GC overhead      = GC data structures\n\n# Better JVM settings for containers:\nenv:\n- name: JAVA_OPTS\n  value: >\n    -XX:MaxRAMPercentage=75.0\n    -XX:MaxMetaspaceSize=128m\n    -XX:+UseContainerSupport',
    explanation: 'The JVM uses significant memory beyond the heap: metaspace, thread stacks (1MB each), direct byte buffers, code cache, and GC overhead. Use MaxRAMPercentage for container-aware sizing.',
    tags: ['kubernetes', 'oomkilled', 'java', 'off-heap', 'jvm-memory'],
  },
  {
    id: 'k8s-troubleshooting-l3-084',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'pending-pods',
    question: 'A pod is Pending and events show "didn\'t match pod topology spread constraints (missing required label)". What does this mean?',
    options: [
      'The pod labels are wrong',
      'The nodes are missing the topology label specified in topologyKey (e.g., topology.kubernetes.io/zone is not set on some nodes)',
      'The pod needs more replicas',
      'The namespace labels are wrong',
    ],
    correctAnswer: 1,
    codeSnippet: '# Check if nodes have the required topology label\nkubectl get nodes -L topology.kubernetes.io/zone\n\n# If nodes don\'t have the label, add it:\nkubectl label node node-1 topology.kubernetes.io/zone=us-east-1a\n\n# Or change the topologyKey to a label that exists:\ntopologySpreadConstraints:\n- topologyKey: kubernetes.io/hostname  # Always exists',
    explanation: 'Topology spread constraints require nodes to have the label specified in topologyKey. If nodes lack this label, the scheduler cannot compute the topology spread and the pod stays Pending.',
    tags: ['kubernetes', 'topology-spread', 'labels', 'pending'],
  },
  {
    id: 'k8s-troubleshooting-l3-085',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'crashloopbackoff',
    question: 'A pod repeatedly crashes with SIGABRT (exit code 134). Application logs show no errors before the crash. What investigation steps should you take?',
    options: [
      'Increase memory limits',
      'Check for core dumps, enable debug logging, investigate potential thread safety issues or assertion failures in native libraries',
      'Restart the node',
      'Change the base image',
    ],
    correctAnswer: 1,
    codeSnippet: '# Exit code 134 = 128 + 6 (SIGABRT)\n# SIGABRT is sent by abort() - usually from:\n# - C/C++ assertion failures\n# - Memory corruption detected by glibc\n# - Uncaught C++ exception with no handler\n\n# Enable core dumps in the pod:\nsecurityContext:\n  sysctls:\n  - name: kernel.core_pattern\n    value: "/tmp/core.%p"\n\n# Or debug with ephemeral container:\nkubectl debug -it my-pod --image=ubuntu --target=my-app\n# Install gdb, analyze core dump or attach to process',
    explanation: 'SIGABRT (exit code 134) indicates the process called abort(), usually from assertion failures, memory corruption detection (glibc), or unhandled C++ exceptions in native code.',
    tags: ['kubernetes', 'sigabrt', 'core-dump', 'native-crash', 'debugging'],
  },
  {
    id: 'k8s-troubleshooting-l3-086',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'events-analysis',
    question: 'How can you use kubectl to quickly identify all pods with non-zero restart counts across the cluster?',
    options: [
      'kubectl get pods -A --sort-by=.status.containerStatuses[0].restartCount',
      'kubectl get pods -A -o custom-columns=NAME:.metadata.name,NAMESPACE:.metadata.namespace,RESTARTS:.status.containerStatuses[0].restartCount --sort-by=.status.containerStatuses[0].restartCount',
      'kubectl top pods -A --sort-by=restarts',
      'kubectl get events -A --field-selector reason=BackOff',
    ],
    correctAnswer: 1,
    codeSnippet: '# Find pods with restarts across all namespaces\nkubectl get pods -A -o custom-columns=\\\nNAME:.metadata.name,\\\nNAMESPACE:.metadata.namespace,\\\nRESTARTS:.status.containerStatuses[0].restartCount \\\n--sort-by=\'.status.containerStatuses[0].restartCount\'\n\n# Or using jsonpath for more detail:\nkubectl get pods -A -o jsonpath=\'{range .items[?(@.status.containerStatuses[0].restartCount>0)]}{.metadata.namespace}/{.metadata.name}: {.status.containerStatuses[0].restartCount}{\"\\n\"}{end}\'',
    explanation: 'Using custom-columns with --sort-by provides a quick overview of pod restarts across the cluster, helping identify the most problematic workloads.',
    tags: ['kubernetes', 'restarts', 'custom-columns', 'cluster-health'],
  },
  {
    id: 'k8s-troubleshooting-l3-087',
    language: 'kubernetes',
    level: 'level3',
    category: 'Troubleshooting',
    subcategory: 'debug-containers',
    question: 'You want to profile a Java application running in a pod without restarting it. Which approach uses Kubernetes debugging features effectively?',
    options: [
      'Restart the pod with JAVA_OPTS for profiling',
      'Use kubectl debug to add an ephemeral container with async-profiler and share the PID namespace to attach to the JVM process',
      'Redeploy with a profiling agent',
      'Use kubectl port-forward to connect a remote profiler',
    ],
    correctAnswer: 1,
    codeSnippet: '# Add debug container with profiling tools\nkubectl debug -it my-java-pod \\\n  --image=my-profiler-image \\\n  --target=java-container\n\n# Inside debug container:\n# Find JVM PID\nps aux | grep java  # e.g., PID 1\n\n# Run async-profiler against the JVM\n./async-profiler/asprof -d 30 -f /tmp/flamegraph.html 1\n\n# Copy results out\nkubectl cp my-java-pod:/tmp/flamegraph.html ./flamegraph.html',
    explanation: 'Ephemeral containers with shared PID namespace can attach profiling tools (like async-profiler) to a running JVM without restart, enabling production debugging.',
    tags: ['kubernetes', 'debug', 'profiling', 'java', 'async-profiler'],
  },
];

export default questions;
