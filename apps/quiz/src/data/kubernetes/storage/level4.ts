import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-storage-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI gRPC protocol handle the two-phase volume lifecycle (Controller + Node operations)?',
    options: [
      'All operations happen on the controller',
      'Controller service handles CreateVolume/DeleteVolume/ControllerPublishVolume at the cluster level; Node service handles NodeStageVolume/NodePublishVolume at the node level, with separate gRPC endpoints',
      'Node operations always come before controller operations',
      'The kubelet handles both phases directly',
    ],
    correctAnswer: 1,
    explanation: 'CSI defines two gRPC services: Controller (runs centrally, handles provisioning and cloud-level attach) and Node (runs on each node, handles filesystem format/mount). The kubelet calls Node RPCs via a Unix socket, while the CSI sidecars call Controller RPCs.',
    tags: ['kubernetes', 'csi', 'grpc', 'architecture', 'internals'],
  },
  {
    id: 'k8s-storage-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the exact sequence of operations when a Pod starts and needs a PVC-backed volume?',
    options: [
      'Mount -> Attach -> Schedule',
      'Schedule -> PVC bind -> ControllerPublishVolume (attach) -> NodeStageVolume (format/global mount) -> NodePublishVolume (bind mount to Pod) -> Container starts',
      'PVC bind -> Container starts -> Mount',
      'Schedule -> Container starts -> Volume mounts asynchronously',
    ],
    correctAnswer: 1,
    explanation: 'The full sequence: scheduler places Pod, PV controller binds PVC, attach-detach controller creates VolumeAttachment, CSI ControllerPublishVolume attaches to node, kubelet calls NodeStageVolume (format+global mount), then NodePublishVolume (bind mount), and finally the container starts.',
    tags: ['kubernetes', 'persistent-volumes', 'volume-lifecycle', 'internals'],
  },
  {
    id: 'k8s-storage-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the PV controller handle the "delayed binding" with WaitForFirstConsumer, and what role does the scheduler play?',
    options: [
      'The scheduler provisions the volume',
      'The scheduler adds a node annotation to the PVC; the PV controller sees this annotation and provisions the PV in the correct topology matching the selected node',
      'The PV controller polls the scheduler',
      'Binding happens after the container starts',
    ],
    correctAnswer: 1,
    explanation: 'With WaitForFirstConsumer: the scheduler selects a node for the Pod and adds the volume.kubernetes.io/selected-node annotation to the PVC. The external-provisioner watches for this annotation and provisions the PV in the matching topology.',
    codeSnippet: '# PVC annotation added by scheduler\nmetadata:\n  annotations:\n    volume.kubernetes.io/selected-node: "worker-1"\n# This triggers the CSI provisioner to create the volume\n# in the same topology as worker-1',
    tags: ['kubernetes', 'storage-classes', 'wait-for-first-consumer', 'scheduler', 'internals'],
  },
  {
    id: 'k8s-storage-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "volume condition" feature and how does it enable proactive volume management?',
    options: [
      'It checks volume capacity',
      'CSI drivers can report volume health conditions (abnormal: true/false, message) via NodeGetVolumeStats and ListVolumes; Kubernetes surfaces these as events on PVCs and Pods for monitoring and automated remediation',
      'It validates CSI driver configuration',
      'It checks network connectivity to storage',
    ],
    correctAnswer: 1,
    explanation: 'CSI volume condition monitoring (alpha) allows drivers to report volume abnormalities (degraded performance, impending failure, corruption). Kubernetes exposes these as events, enabling monitoring systems to trigger alerts or automated remediation.',
    tags: ['kubernetes', 'csi', 'volume-condition', 'health-monitoring'],
  },
  {
    id: 'k8s-storage-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the Kubernetes attach-detach controller handle "uncertain" volume attachment states?',
    options: [
      'It ignores uncertain states',
      'It tracks "desired" vs "actual" attachment state; if uncertain (API call succeeded but status unclear), it retries with exponential backoff and relies on the CSI driver ControllerPublishVolume idempotency',
      'It force-detaches the volume',
      'It marks the node as unschedulable',
    ],
    correctAnswer: 1,
    explanation: 'The AD controller maintains desired/actual state maps. When a ControllerPublish call times out or returns ambiguous errors, it retries relying on CSI idempotency guarantees. It uses exponential backoff and node status to make decisions.',
    tags: ['kubernetes', 'persistent-volumes', 'attach-detach', 'controller', 'internals'],
  },
  {
    id: 'k8s-storage-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the internal architecture of the VolumeSnapshot controller and how does it interact with the CSI driver?',
    options: [
      'The snapshot controller directly calls the CSI driver',
      'The external-snapshotter has two components: a snapshot-controller (watches VolumeSnapshot CRs) and a CSI snapshotter sidecar (calls CreateSnapshot/DeleteSnapshot on the CSI driver via gRPC)',
      'The kubelet handles snapshot creation',
      'The API server creates snapshots directly',
    ],
    correctAnswer: 1,
    explanation: 'The snapshot system has two components: (1) snapshot-controller (cluster singleton) manages VolumeSnapshot/VolumeSnapshotContent lifecycle, (2) csi-snapshotter sidecar (per CSI driver) watches VolumeSnapshotContent and makes CSI CreateSnapshot/DeleteSnapshot gRPC calls.',
    tags: ['kubernetes', 'snapshots', 'architecture', 'internals'],
  },
  {
    id: 'k8s-storage-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI staging path differ from the publishing path, and why is this two-level mount important?',
    options: [
      'They are the same path',
      'Staging mounts the volume once per node at a global path; publishing creates bind mounts from staging to each Pod path. This enables efficient multi-Pod access without re-formatting or re-mounting the block device.',
      'Staging is for read operations, publishing for write',
      'Staging is for block devices, publishing for filesystems',
    ],
    correctAnswer: 1,
    explanation: 'NodeStageVolume mounts to a global staging path (/var/lib/kubelet/plugins/kubernetes.io/csi/...) once per node. NodePublishVolume creates bind mounts from staging to each Pod path (/var/lib/kubelet/pods/<uid>/volumes/...). This avoids re-formatting the device for each Pod.',
    codeSnippet: '# Staging path (global, once per node per volume)\n/var/lib/kubelet/plugins/kubernetes.io/csi/<driver>/globalmount/<vol-id>\n\n# Publishing path (per Pod)\n/var/lib/kubelet/pods/<pod-uid>/volumes/kubernetes.io~csi/<vol-name>/mount',
    tags: ['kubernetes', 'csi', 'staging', 'publishing', 'mount-paths'],
  },
  {
    id: 'k8s-storage-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "force detach" mechanism and what are its risks?',
    options: [
      'It safely detaches volumes from crashed nodes',
      'The AD controller can force-detach a volume from a node that is unreachable by deleting the VolumeAttachment; the risk is data corruption if the node is still alive but partitioned',
      'Force detach is always safe',
      'Force detach requires admin approval',
    ],
    correctAnswer: 1,
    explanation: 'When a node becomes unreachable, the AD controller can force-detach by deleting VolumeAttachment. Risk: if the node is actually alive but network-partitioned, both the old and new node may write to the volume simultaneously, causing corruption.',
    tags: ['kubernetes', 'persistent-volumes', 'force-detach', 'split-brain', 'risk'],
  },
  {
    id: 'k8s-storage-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the PV controller handle PVC/PV matching with multiple criteria simultaneously?',
    options: [
      'It matches only by size',
      'The controller finds PVs matching ALL criteria: access modes (superset), capacity (>=), StorageClass (exact), selector labels (all match), volume mode (exact), and then selects the smallest matching PV',
      'It uses a scoring algorithm',
      'It matches in order of creation time',
    ],
    correctAnswer: 1,
    explanation: 'The binding algorithm checks ALL criteria conjunctively: PV access modes must be a superset of PVC request, capacity >= request, StorageClass must match exactly, selector labels must all match, and volume mode must match. The smallest eligible PV is selected to minimize waste.',
    tags: ['kubernetes', 'storage-classes', 'pv-matching', 'algorithm', 'internals'],
  },
  {
    id: 'k8s-storage-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "volume cloning" implementation at the protocol level?',
    options: [
      'The CSI driver copies data byte-by-byte',
      'CreateVolume RPC receives the source volume ID in the VolumeContentSource field; the storage backend implements the clone (typically copy-on-write), and the new volume is immediately usable',
      'Kubernetes copies data between volumes',
      'A separate clone API endpoint is used',
    ],
    correctAnswer: 1,
    explanation: 'CSI cloning uses the standard CreateVolume RPC with VolumeContentSource.Volume set to the source volume ID. The storage backend implements the actual clone (usually COW). The CSI external-provisioner orchestrates this when it sees a PVC with dataSource.',
    tags: ['kubernetes', 'csi', 'cloning', 'protocol', 'internals'],
  },
  {
    id: 'k8s-storage-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the exact flow of a volume expansion through the Kubernetes and CSI layers?',
    options: [
      'User edits PVC -> Volume is immediately resized',
      'User edits PVC -> API server validates -> external-resizer calls ControllerExpandVolume -> PVC status updated -> kubelet detects size mismatch -> calls NodeExpandVolume -> filesystem resize -> PVC capacity updated',
      'kubelet resizes the volume directly',
      'The StorageClass provisioner handles all resize steps',
    ],
    correctAnswer: 1,
    explanation: 'Full flow: (1) User patches PVC.spec.resources, (2) external-resizer detects change, calls CSI ControllerExpandVolume, (3) backend storage is expanded, (4) PVC condition set to FileSystemResizePending, (5) kubelet detects mismatch and calls NodeExpandVolume, (6) filesystem is resized, (7) PVC.status.capacity updated.',
    tags: ['kubernetes', 'volume-expansion', 'flow', 'internals'],
  },
  {
    id: 'k8s-storage-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the kubelet volume manager reconstruct volume state after a restart?',
    options: [
      'It queries the API server for all volumes',
      'It reads the "actual state of the world" by scanning /var/lib/kubelet/pods/*/volumes/* directories and reconciles with the "desired state" from the API server',
      'It relies on the CSI driver to report mounted volumes',
      'It remounts all volumes from scratch',
    ],
    correctAnswer: 1,
    explanation: 'After restart, the kubelet volume manager reconstructs actual state by scanning mount points on disk. It compares this with desired state (from running Pods) and reconciles: unmounting volumes for deleted Pods and mounting volumes for new Pods.',
    tags: ['kubernetes', 'persistent-volumes', 'kubelet', 'volume-manager', 'reconstruction'],
  },
  {
    id: 'k8s-storage-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "volume migration" process from in-tree plugins to CSI, and how does it work at the API level?',
    options: [
      'PVs are recreated with CSI spec',
      'CSIMigration translates in-tree volume specs to CSI equivalents at the API admission level; existing PV/PVC specs are unchanged but the kubelet and controllers route operations to the CSI driver instead of the in-tree plugin',
      'A migration job converts all PVs',
      'Users must update all PVC specs manually',
    ],
    correctAnswer: 1,
    explanation: 'CSIMigration works at the translation layer: when enabled, the API server and kubelet translate in-tree volume operations to CSI equivalents transparently. The in-tree PV spec is preserved but operations go to the corresponding CSI driver.',
    codeSnippet: '# In-tree PV spec (unchanged)\nspec:\n  awsElasticBlockStore:\n    volumeID: vol-abc123\n    fsType: ext4\n# CSIMigration translates this to CSI calls:\n# - ControllerPublishVolume with vol-abc123\n# - NodeStageVolume with fsType ext4\n# All routed to ebs.csi.aws.com instead of in-tree plugin',
    tags: ['kubernetes', 'csi', 'migration', 'in-tree', 'translation'],
  },
  {
    id: 'k8s-storage-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'What is the "immediate topology" problem with Immediate binding mode and how does it lead to unschedulable Pods?',
    options: [
      'Immediate binding has no problems',
      'With Immediate binding, a PV may be provisioned in zone-A before the scheduler runs; if the Pod has other constraints forcing it to zone-B, it cannot mount the zone-A PV, creating an unschedulable state',
      'Immediate binding causes data loss',
      'Immediate binding is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Immediate binding provisions PVs before Pod scheduling. If the PV lands in zone-A but the Pod has affinity/other constraints requiring zone-B, the Pod cannot start. WaitForFirstConsumer solves this by coordinating provisioning with scheduling.',
    tags: ['kubernetes', 'storage-classes', 'immediate-binding', 'topology-mismatch'],
  },
  {
    id: 'k8s-storage-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How does the snapshot controller handle the "bound/unbound" state machine for VolumeSnapshots?',
    options: [
      'Snapshots are always bound immediately',
      'The controller manages ReadyToUse state: pending -> creating (CSI call in progress) -> ready (snapshot exists) or error. Deletion triggers finalizer processing to clean up VolumeSnapshotContent.',
      'The CSI driver manages snapshot states',
      'Snapshot state is managed by the API server',
    ],
    correctAnswer: 1,
    explanation: 'The snapshot controller manages a state machine: VolumeSnapshot starts as "not ready", transitions to "creating" during CSI call, becomes "ReadyToUse: true" when complete. Deletion is coordinated through finalizers between VolumeSnapshot and VolumeSnapshotContent.',
    tags: ['kubernetes', 'snapshots', 'state-machine', 'controller', 'internals'],
  },
  {
    id: 'k8s-storage-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "controller capabilities" negotiation and how does the external-provisioner use it?',
    options: [
      'Capabilities are hardcoded',
      'During startup, sidecars call GetPluginCapabilities and ControllerGetCapabilities to discover driver features; the external-provisioner only watches PVCs if CREATE_DELETE_VOLUME is reported',
      'Kubernetes configures capabilities via the CSIDriver resource',
      'Capabilities are negotiated with the API server',
    ],
    correctAnswer: 1,
    explanation: 'CSI sidecars probe the driver at startup using GetPluginCapabilities and ControllerGetCapabilities RPCs. Based on reported capabilities (CREATE_DELETE_VOLUME, PUBLISH_UNPUBLISH, etc.), sidecars decide which operations to perform.',
    codeSnippet: '# CSI capability probing flow:\n# 1. Sidecar starts\n# 2. Calls GetPluginInfo -> driver name, version\n# 3. Calls GetPluginCapabilities -> SERVICE, VOLUME_ACCESSIBILITY\n# 4. Calls ControllerGetCapabilities ->\n#    - CREATE_DELETE_VOLUME\n#    - PUBLISH_UNPUBLISH_VOLUME\n#    - CREATE_DELETE_SNAPSHOT\n#    - EXPAND_VOLUME\n#    - CLONE_VOLUME\n# 5. Sidecar enables features based on capabilities',
    tags: ['kubernetes', 'csi', 'capabilities', 'negotiation', 'internals'],
  },
  {
    id: 'k8s-storage-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "maximum volume count" scheduler plugin and how does it determine per-node limits?',
    options: [
      'It uses a fixed limit for all nodes',
      'The CSIMaxVolumeLimitChecker reads limits from CSINode objects (populated by the CSI driver) and node allocatable resources to prevent scheduling Pods that would exceed the volume attachment limit',
      'The kubelet enforces volume limits',
      'Volume limits are set in the StorageClass',
    ],
    correctAnswer: 1,
    explanation: 'The scheduler CSIMaxVolumeLimitChecker plugin reads volume limits from CSINode objects. CSI node plugins report their per-node attachment limits via NodeGetInfo. The scheduler counts existing attachments and prevents exceeding the limit.',
    codeSnippet: '# CSINode object (auto-populated by CSI driver)\napiVersion: storage.k8s.io/v1\nkind: CSINode\nmetadata:\n  name: worker-1\nspec:\n  drivers:\n    - name: ebs.csi.aws.com\n      nodeID: i-0abc123\n      allocatable:\n        count: 39  # Max EBS volumes on this instance type\n      topologyKeys:\n        - topology.ebs.csi.aws.com/zone',
    tags: ['kubernetes', 'persistent-volumes', 'scheduler', 'volume-limits', 'internals'],
  },
  {
    id: 'k8s-storage-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the "recovery from volume expansion failure" feature and how does it handle partial expansion?',
    options: [
      'Failed expansions are automatically reverted',
      'The feature allows users to retry with a smaller size or revert to the original by editing PVC.spec; the allocatedResourceStatuses field tracks per-resource expansion state to handle partial failures (controller expanded but node failed)',
      'The CSI driver handles recovery automatically',
      'Failed expansions require PVC recreation',
    ],
    correctAnswer: 1,
    explanation: 'Recovery from expansion failure (GA in 1.30) tracks expansion state per-resource. If controller expansion succeeded but node expansion failed, the PVC shows this mixed state. Users can retry with the same or different size without requiring PVC recreation.',
    codeSnippet: '# PVC status showing expansion state\nstatus:\n  capacity:\n    storage: 100Gi  # Current actual size\n  allocatedResources:\n    storage: 200Gi  # Target size\n  allocatedResourceStatuses:\n    storage: ControllerResizeInProgress  # Or NodeResizeInProgress/NodeResizeFailed',
    tags: ['kubernetes', 'volume-expansion', 'recovery', 'partial-failure', 'internals'],
  },
  {
    id: 'k8s-storage-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI secrets mechanism work for passing sensitive information to CSI drivers?',
    options: [
      'Secrets are passed as environment variables',
      'CSI secrets are referenced in StorageClass/VolumeSnapshotClass parameters and passed in the CreateVolume/NodePublishVolume gRPC calls secrets field; Kubernetes resolves Secret references to values before the call',
      'Secrets are stored in the CSI driver configuration',
      'Secrets are encrypted by the CSI protocol',
    ],
    correctAnswer: 1,
    explanation: 'StorageClass can reference Kubernetes Secrets via csi.storage.k8s.io/* annotations. The external-provisioner resolves these to actual Secret values and passes them in the CSI gRPC secrets field. This enables per-volume credentials for storage backends.',
    codeSnippet: 'apiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: ceph-sc\nprovisioner: rook-ceph.ceph.rook.io\nparameters:\n  clusterID: rook-ceph\n  pool: replicapool\n  csi.storage.k8s.io/provisioner-secret-name: rook-csi-rbd-provisioner\n  csi.storage.k8s.io/provisioner-secret-namespace: rook-ceph\n  csi.storage.k8s.io/node-stage-secret-name: rook-csi-rbd-node\n  csi.storage.k8s.io/node-stage-secret-namespace: rook-ceph',
    tags: ['kubernetes', 'csi', 'secrets', 'authentication', 'internals'],
  },
  {
    id: 'k8s-storage-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "PV protection" finalizer mechanism and how does it prevent data loss?',
    options: [
      'It prevents PV deletion',
      'kubernetes.io/pv-protection finalizer blocks PV deletion while bound; the controller only removes the finalizer when the PVC is deleted and reclaim policy processing is complete',
      'It creates backups before deletion',
      'It encrypts PV data',
    ],
    correctAnswer: 1,
    explanation: 'The pv-protection finalizer is added to all PVs. When a PV deletion is attempted while bound, the deletion is blocked (object stays with DeletionTimestamp set). The PV controller removes the finalizer only after the bound PVC is deleted and the reclaim process completes.',
    tags: ['kubernetes', 'persistent-volumes', 'finalizer', 'protection', 'internals'],
  },
  {
    id: 'k8s-storage-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the "allowedTopologies" field interact with the scheduler and provisioner for topology-constrained provisioning?',
    options: [
      'allowedTopologies restricts scheduling',
      'allowedTopologies is passed to CSI CreateVolume as accessible_topology; the provisioner intersects it with the selected-node topology to determine where to provision the volume',
      'allowedTopologies only affects the default StorageClass',
      'allowedTopologies is enforced by the kubelet',
    ],
    correctAnswer: 1,
    explanation: 'The external-provisioner uses allowedTopologies to determine valid provisioning zones. With WaitForFirstConsumer, it intersects allowedTopologies with the selected-node topology. The result is passed as accessible_topology in the CreateVolume call.',
    tags: ['kubernetes', 'storage-classes', 'allowed-topologies', 'provisioner', 'internals'],
  },
  {
    id: 'k8s-storage-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the VolumeGroupSnapshot CRD architecture and how does it ensure cross-volume consistency?',
    options: [
      'It takes snapshots sequentially',
      'VolumeGroupSnapshot references multiple PVCs; the CSI GroupCreateSnapshot RPC instructs the storage backend to take an atomic snapshot of all volumes simultaneously, ensuring crash consistency across volumes',
      'It pauses all I/O before snapshotting',
      'It coordinates with the application for consistency',
    ],
    correctAnswer: 1,
    explanation: 'VolumeGroupSnapshot uses the CSI CreateVolumeGroupSnapshot RPC. The storage backend takes an atomic, crash-consistent snapshot of all specified volumes at the same point in time. Individual VolumeSnapshotContent objects are created for each volume in the group.',
    tags: ['kubernetes', 'csi', 'group-snapshots', 'architecture', 'consistency'],
  },
  {
    id: 'k8s-storage-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the Kubernetes "lease-based" volume detach work for unreachable nodes?',
    options: [
      'Volumes are detached immediately when a node goes down',
      'The node controller marks nodes as unreachable after node-monitor-grace-period; the AD controller waits for pod-eviction-timeout before force-detaching volumes, creating a configurable window to prevent split-brain',
      'The CSI driver detects node failure',
      'etcd handles volume lease management',
    ],
    correctAnswer: 1,
    explanation: 'When a node stops updating its lease: (1) node controller marks it NotReady after node-monitor-grace-period, (2) taint manager adds unreachable taint, (3) Pods are evicted after pod-eviction-timeout, (4) AD controller force-detaches volumes. The timing is configurable to balance availability vs split-brain risk.',
    tags: ['kubernetes', 'persistent-volumes', 'node-failure', 'lease', 'force-detach'],
  },
  {
    id: 'k8s-storage-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the VolumeAttributesClass "ModifyVolume" CSI operation and how does it differ from expansion?',
    options: [
      'It is the same as expansion',
      'ModifyVolume changes volume attributes (IOPS, throughput, tier) without changing size; the CSI ControllerModifyVolume RPC allows changing performance characteristics independently from capacity changes',
      'It modifies the volume filesystem',
      'It changes the volume access mode',
    ],
    correctAnswer: 1,
    explanation: 'CSI ControllerModifyVolume (separate from ExpandVolume) changes non-capacity attributes. This enables changing IOPS, throughput, or storage tier without resizing. The external-resizer handles both expand and modify operations.',
    codeSnippet: '# VolumeAttributesClass for IOPS change\napiVersion: storage.k8s.io/v1beta1\nkind: VolumeAttributesClass\nmetadata:\n  name: premium-iops\ndriverName: ebs.csi.aws.com\nparameters:\n  iops: "16000"\n  throughput: "1000"\n# The external-resizer calls CSI ControllerModifyVolume\n# with the new parameters when PVC.volumeAttributesClassName changes',
    tags: ['kubernetes', 'volume-expansion', 'modify-volume', 'csi', 'attributes'],
  },
  {
    id: 'k8s-storage-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI secrets-store driver implement automatic rotation of mounted secrets?',
    options: [
      'It recreates the Pod when secrets change',
      'A rotation reconciler periodically calls the provider plugin to fetch updated secrets, compares with mounted versions, and atomically updates the mounted files and synced Kubernetes Secrets',
      'The external secret manager pushes updates',
      'kubelet handles secret rotation',
    ],
    correctAnswer: 1,
    explanation: 'The secrets-store CSI driver runs a rotation reconciler that: (1) periodically calls the provider (Vault, AWS SM) via gRPC, (2) compares fetched secrets with mounted versions, (3) atomically updates mounted files using tmpfs, (4) updates synced K8s Secrets if configured.',
    codeSnippet: '# Enable rotation in Secrets Store CSI Driver\nhelm install secrets-store-csi-driver \\\n  secrets-store-csi-driver/secrets-store-csi-driver \\\n  --set enableSecretRotation=true \\\n  --set rotationPollInterval=2m  # Check every 2 minutes',
    tags: ['kubernetes', 'csi', 'secrets-store', 'rotation', 'internals'],
  },
  {
    id: 'k8s-storage-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "bidirectional mount propagation" feature and when is it needed for CSI?',
    options: [
      'It propagates mounts between Pods',
      'Bidirectional mount propagation allows mounts created inside a container to be visible on the host and vice versa; CSI node plugins need this to make volume mounts available to kubelet and other Pods',
      'It replicates mount configurations',
      'It synchronizes mounts between nodes',
    ],
    correctAnswer: 1,
    explanation: 'CSI node plugins must use Bidirectional mount propagation for the kubelet plugins directory. Without it, volumes mounted by the CSI driver inside its container would not be visible to kubelet or other Pods. This is set via mountPropagation: Bidirectional.',
    codeSnippet: '# CSI node plugin DaemonSet volume mount\ncontainers:\n  - name: csi-node-driver\n    volumeMounts:\n      - name: kubelet-dir\n        mountPath: /var/lib/kubelet\n        mountPropagation: Bidirectional  # Required for CSI\n    securityContext:\n      privileged: true  # Required for Bidirectional propagation',
    tags: ['kubernetes', 'persistent-volumes', 'mount-propagation', 'csi', 'internals'],
  },
  {
    id: 'k8s-storage-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'What is the "topology manager" interaction with storage scheduling?',
    options: [
      'Topology manager only handles CPU/memory',
      'When topology manager policy is "single-numa-node" or "restricted", it coordinates with the volume scheduler plugin to ensure volumes are accessible from the same NUMA domain as the allocated CPU/memory, critical for high-performance storage',
      'Topology manager replaces the storage scheduler',
      'Topology manager is only for GPUs',
    ],
    correctAnswer: 1,
    explanation: 'For high-performance workloads, the topology manager ensures CPU, memory, devices (GPUs), and storage are co-located in the same NUMA domain. This prevents cross-NUMA access penalties for local NVMe storage.',
    tags: ['kubernetes', 'storage-classes', 'topology-manager', 'numa', 'performance'],
  },
  {
    id: 'k8s-storage-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI "data populator" mechanism work for custom data sources?',
    options: [
      'The CSI driver populates data',
      'A custom populator controller watches PVCs with dataSourceRef pointing to a custom resource, creates a temporary PVC, populates it using a Job/Pod, then rebinds the PV to the original PVC',
      'Kubernetes copies data from the data source',
      'The API server handles data population',
    ],
    correctAnswer: 1,
    explanation: 'Custom populators: (1) PVC has dataSourceRef pointing to a custom CR, (2) populator controller creates a temporary "prime" PVC, (3) runs a Pod to populate it (from S3, DB export, etc.), (4) rebinds the PV from prime PVC to the original PVC.',
    tags: ['kubernetes', 'csi', 'data-populator', 'custom-resource', 'internals'],
  },
  {
    id: 'k8s-storage-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "storage capacity scheduling" implementation at the scheduler level?',
    options: [
      'The scheduler checks cloud provider APIs',
      'The scheduler reads CSIStorageCapacity objects created by CSI drivers, calculates available capacity per topology segment, and filters out nodes/zones where provisioning would likely fail due to insufficient capacity',
      'The provisioner reports capacity to the scheduler',
      'Capacity is checked after scheduling',
    ],
    correctAnswer: 1,
    explanation: 'The VolumeBinding scheduler plugin reads CSIStorageCapacity objects (created by external-provisioner with --enable-capacity flag). It filters nodes where matching capacity < PVC request. This is best-effort — the actual provisioning may still fail.',
    codeSnippet: '# CSIStorageCapacity flow:\n# 1. CSI external-provisioner creates CSIStorageCapacity objects\n#    per node/zone based on GetCapacity RPC results\n# 2. Scheduler VolumeBinding plugin reads these\n# 3. During scheduling, it filters nodes where capacity < PVC request\n# 4. Pod is scheduled to a node with sufficient reported capacity\n# 5. Provisioning is attempted (may still fail if capacity changed)',
    tags: ['kubernetes', 'persistent-volumes', 'storage-capacity', 'scheduler', 'internals'],
  },
  {
    id: 'k8s-storage-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the interaction between PVC expansion and the "quota" system?',
    options: [
      'Quotas do not apply to expansion',
      'PVC expansion must respect ResourceQuota; the increase counts against the namespace storage quota. If the new size exceeds the quota, the API server rejects the edit even if the underlying storage supports it.',
      'Quota is only checked during initial creation',
      'The CSI driver enforces quotas',
    ],
    correctAnswer: 1,
    explanation: 'PVC expansion is validated against ResourceQuota. The delta (new size - old size) is checked against remaining quota. If insufficient quota remains, the API server rejects the expansion with a "exceeded quota" error.',
    tags: ['kubernetes', 'volume-expansion', 'resource-quota', 'validation'],
  },
  {
    id: 'k8s-storage-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the Rook-Ceph CSI driver implement multi-tenant isolation for RBD volumes?',
    options: [
      'Using namespace isolation',
      'Rook creates separate Ceph pools per StorageClass, each with its own crush rule and RBAC; CSI credentials are scoped per pool, preventing cross-tenant access at the storage level',
      'Using NetworkPolicies for storage',
      'Using Kubernetes RBAC only',
    ],
    correctAnswer: 1,
    explanation: 'Rook-Ceph provides multi-tenant isolation through: separate Ceph pools (with distinct CRUSH rules), per-pool CSI credentials (stored as Kubernetes Secrets), pool-specific quotas, and namespace-scoped StorageClasses with references to tenant-specific pools.',
    tags: ['kubernetes', 'csi', 'rook', 'ceph', 'multi-tenancy', 'isolation'],
  },
  {
    id: 'k8s-storage-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "volume scheduling" algorithm in the scheduler and how does it handle multiple volumes per Pod?',
    options: [
      'It schedules each volume independently',
      'The VolumeBinding plugin evaluates all PVCs for a Pod together, finding a node where ALL volumes can be satisfied (matching topology for bound PVs + provisioning capability for unbound PVCs), using a two-pass filter-and-score approach',
      'Volumes are scheduled after the Pod',
      'Only the first volume affects scheduling',
    ],
    correctAnswer: 1,
    explanation: 'The VolumeBinding plugin evaluates ALL Pod PVCs in a single pass per node: bound PVCs must match node topology, unbound PVCs must have matching StorageClass with capacity. Only nodes satisfying ALL volume requirements pass the filter.',
    tags: ['kubernetes', 'persistent-volumes', 'scheduler', 'volume-binding', 'algorithm'],
  },
  {
    id: 'k8s-storage-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the snapshot "finalizer" chain and how does it prevent orphaned resources?',
    options: [
      'Finalizers are not used for snapshots',
      'VolumeSnapshot has a finalizer preventing deletion until VolumeSnapshotContent is updated; VolumeSnapshotContent has a finalizer preventing deletion until the CSI DeleteSnapshot call succeeds, ensuring no orphaned storage snapshots',
      'The API server manages snapshot lifecycle',
      'Only VolumeSnapshotContent has a finalizer',
    ],
    correctAnswer: 1,
    explanation: 'The chain: VolumeSnapshot has a snapshot-bound-protection finalizer (removed after VSC is updated). VolumeSnapshotContent has a csi-snapshotter finalizer (removed after CSI DeleteSnapshot succeeds). This ensures the storage snapshot is deleted before the K8s objects.',
    tags: ['kubernetes', 'snapshots', 'finalizers', 'lifecycle', 'internals'],
  },
  {
    id: 'k8s-storage-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the impact of CSI driver gRPC timeout configuration on volume operations?',
    options: [
      'Timeouts have no impact',
      'Sidecar timeout settings (--timeout) affect how long Kubernetes waits for CSI operations; too short causes premature failures for slow storage; too long delays error detection. Different operations may need different timeouts.',
      'The CSI spec defines fixed timeouts',
      'Timeouts are handled by the API server',
    ],
    correctAnswer: 1,
    explanation: 'Each CSI sidecar has configurable timeouts. CreateVolume for large volumes may need longer timeouts. ControllerPublishVolume depends on cloud API speed. NodeStageVolume with formatting depends on volume size. Misconfigured timeouts cause false failures or delayed detection.',
    codeSnippet: '# CSI sidecar timeout configuration\ncontainers:\n  - name: csi-provisioner\n    args:\n      - --timeout=300s        # 5 min for provisioning\n      - --retry-interval-start=1s\n      - --retry-interval-max=5m\n  - name: csi-attacher\n    args:\n      - --timeout=120s        # 2 min for attach\n  - name: csi-resizer\n    args:\n      - --timeout=300s        # 5 min for resize',
    tags: ['kubernetes', 'csi', 'timeout', 'configuration', 'reliability'],
  },
  {
    id: 'k8s-storage-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the "volume topology" aware scheduling handle cross-zone StatefulSets?',
    options: [
      'All PVs are created in the same zone',
      'Each StatefulSet Pod is scheduled independently; with WaitForFirstConsumer, each Pod PVC is provisioned in the zone where that specific Pod is scheduled, potentially spreading PVs across zones with pod topology spread constraints',
      'The StatefulSet controller manages zone placement',
      'Cross-zone StatefulSets are not supported',
    ],
    correctAnswer: 1,
    explanation: 'With WaitForFirstConsumer and topologySpreadConstraints, StatefulSet Pods spread across zones. Each Pod PVC is provisioned in its Pod zone. This creates a zone-aware data distribution. The trade-off is that Pods are tied to their PV zone for recovery.',
    tags: ['kubernetes', 'storage-classes', 'topology', 'statefulset', 'cross-zone'],
  },
  {
    id: 'k8s-storage-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "reservation" mechanism in the PV controller for preventing PV stealing?',
    options: [
      'PVs cannot be stolen',
      'When a PV is selected for a PVC, the controller sets spec.claimRef on the PV to "reserve" it before the PVC is fully bound, preventing other PVCs from claiming the same PV in a race condition',
      'RBAC prevents PV stealing',
      'PVs are locked in etcd during binding',
    ],
    correctAnswer: 1,
    explanation: 'The PV controller uses a two-phase binding: (1) set claimRef on PV to reserve it (optimistic locking via resourceVersion), (2) set volumeName on PVC to complete binding. The claimRef prevents other PVCs from claiming the PV during the binding window.',
    tags: ['kubernetes', 'persistent-volumes', 'binding', 'reservation', 'race-condition'],
  },
  {
    id: 'k8s-storage-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the SELinuxMount optimization work with CSI drivers at the mount level?',
    options: [
      'SELinux is disabled for CSI volumes',
      'The kubelet passes the required SELinux context in NodeStageVolume/NodePublishVolume calls; the CSI driver uses mount -o context= to set the SELinux label at mount time, avoiding the expensive recursive relabel by the container runtime',
      'The container runtime applies SELinux labels',
      'SELinux labels are stored in the PV spec',
    ],
    correctAnswer: 1,
    explanation: 'Without SELinuxMount: container runtime relabels all files recursively (O(n) where n=number of files). With SELinuxMount: kubelet sends SELinux context to CSI driver, which passes it as a mount option. All files inherit the context from the mount, requiring zero relabeling.',
    tags: ['kubernetes', 'csi', 'selinux', 'mount-optimization', 'performance'],
  },
  {
    id: 'k8s-storage-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the "allocated resources" tracking mechanism in PVC status for expansion?',
    options: [
      'It tracks current disk usage',
      'PVC.status.allocatedResources tracks the target expansion size while status.capacity shows the actual current size; this split allows detecting stuck expansions and implementing recovery logic',
      'It tracks the allocated IOPS',
      'It tracks the resources used by the PVC controller',
    ],
    correctAnswer: 1,
    explanation: 'allocatedResources represents what the user requested (spec) and the controller accepted. When different from capacity (what is actually provisioned), an expansion is in progress. allocatedResourceStatuses tracks per-resource state (ControllerResizeInProgress, NodeResizeInProgress, NodeResizeFailed).',
    tags: ['kubernetes', 'volume-expansion', 'allocated-resources', 'status', 'internals'],
  },
  {
    id: 'k8s-storage-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How does the snapshot controller handle "pre-provisioned" vs "dynamically provisioned" VolumeSnapshotContent differently?',
    options: [
      'No difference in handling',
      'Pre-provisioned VSC has no deletionPolicy enforcement (admin manages lifecycle); dynamic VSC follows the VolumeSnapshotClass deletionPolicy. The controller uses the source field to distinguish: snapshotHandle = pre-provisioned, volumeHandle = dynamic.',
      'Pre-provisioned VSC cannot be deleted',
      'Dynamic VSC requires manual creation',
    ],
    correctAnswer: 1,
    explanation: 'The snapshot controller distinguishes by VolumeSnapshotContent.spec.source: if snapshotHandle is set, it is pre-provisioned (existing snapshot); if volumeHandle is set, it is dynamically provisioned (from PVC). Pre-provisioned VSC skips the CreateSnapshot call.',
    tags: ['kubernetes', 'snapshots', 'pre-provisioned', 'dynamic', 'internals'],
  },
  {
    id: 'k8s-storage-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the impact of the kubelet "volume reconciler" frequency on volume operations?',
    options: [
      'It has no impact',
      'The volume reconciler runs periodically (default every ~100ms) comparing desired vs actual mount state; its frequency affects how quickly new mounts/unmounts are processed and stale mounts are cleaned up',
      'It only runs at kubelet startup',
      'It runs once per Pod lifecycle event',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet volume reconciler loop (~100ms) compares desired state (from Pod specs) vs actual state (from mount scanning). It triggers mount/unmount/attach/detach operations. The frequency affects responsiveness to Pod changes and cleanup of stale mounts after crashes.',
    tags: ['kubernetes', 'csi', 'kubelet', 'volume-reconciler', 'internals'],
  },
  {
    id: 'k8s-storage-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "node-specific volume limits" reporting mechanism and how do different instance types affect it?',
    options: [
      'All nodes have the same volume limit',
      'CSI NodeGetInfo reports per-node max_volumes_per_node based on instance type; this is stored in CSINode.spec.drivers[].allocatable.count and used by the scheduler to prevent over-commitment',
      'Volume limits are set in the CSI driver configuration',
      'The cloud provider API reports limits',
    ],
    correctAnswer: 1,
    explanation: 'Different instance types have different attachment limits (e.g., AWS m5.large: 25 EBS, m5.24xlarge: 39). The CSI node plugin calls NodeGetInfo which queries instance metadata to determine the limit. This value is written to CSINode and used by the scheduler.',
    tags: ['kubernetes', 'persistent-volumes', 'volume-limits', 'instance-types', 'scheduling'],
  },
  {
    id: 'k8s-storage-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the Ceph RBD CSI driver implement "thick provisioning" vs "thin provisioning"?',
    options: [
      'All RBD volumes are thin-provisioned',
      'Thin provisioning (default) creates sparse images that grow on write. Thick provisioning allocates full space upfront by writing zeros, preventing oversubscription but taking longer to create.',
      'Thick provisioning is not supported in CSI',
      'The provisioning type is determined by the pool',
    ],
    correctAnswer: 1,
    explanation: 'Ceph RBD defaults to thin provisioning (sparse images). For thick provisioning, the CSI driver writes zeros to allocate full space during CreateVolume. This prevents ENOSPC errors from oversubscription but significantly increases provisioning time.',
    codeSnippet: '# Rook-Ceph StorageClass with thick provisioning\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: rbd-thick\nprovisioner: rook-ceph.rbd.csi.ceph.com\nparameters:\n  clusterID: rook-ceph\n  pool: replicapool\n  thickProvision: "true"  # Allocate full space upfront',
    tags: ['kubernetes', 'storage-classes', 'ceph', 'rbd', 'provisioning'],
  },
  {
    id: 'k8s-storage-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "ReadOnly" flag handling path through the CSI protocol?',
    options: [
      'ReadOnly is enforced by the container runtime',
      'The volumeMount.readOnly flag is passed through: kubelet sets readonly=true in NodePublishVolume request, CSI driver mounts with ro flag, AND kubelet adds an additional read-only bind mount for defense-in-depth',
      'ReadOnly is only enforced at the filesystem level',
      'ReadOnly prevents only write operations',
    ],
    correctAnswer: 1,
    explanation: 'ReadOnly enforcement is layered: (1) kubelet passes readonly=true in CSI NodePublishVolume, (2) CSI driver mounts with -o ro, (3) kubelet creates the final bind mount with MS_RDONLY flag. This defense-in-depth ensures read-only even if the CSI driver does not properly handle it.',
    tags: ['kubernetes', 'csi', 'readonly', 'mount-flags', 'security'],
  },
  {
    id: 'k8s-storage-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the "subPath" implementation work at the mount level and what are its security implications?',
    options: [
      'subPath creates a symbolic link',
      'subPath uses bind mounts from the volume subdirectory to the container mount point; historically it was vulnerable to symlink attacks where a malicious container could escape to the host filesystem',
      'subPath copies files to the container',
      'subPath has no security implications',
    ],
    correctAnswer: 1,
    explanation: 'subPath creates a bind mount from a specific subdirectory. The historical CVE-2017-1002101 exploited race conditions in symlink resolution to escape the volume boundary. subPathExpr was added for variable expansion but shares similar risks. Kubelet now validates the path after resolution.',
    tags: ['kubernetes', 'persistent-volumes', 'subpath', 'security', 'cve'],
  },
  {
    id: 'k8s-storage-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the interaction between volume expansion and "guaranteed" vs "burstable" QoS classes?',
    options: [
      'QoS class does not affect expansion',
      'During node-level filesystem resize, CPU/memory is consumed; for Guaranteed QoS Pods with tight limits, the resize operation may fail or cause OOM if the filesystem resize tool exceeds available resources within the cgroup',
      'Only Guaranteed Pods can expand volumes',
      'Burstable Pods cannot expand volumes',
    ],
    correctAnswer: 1,
    explanation: 'Filesystem resize operations (resize2fs, xfs_growfs) consume CPU and memory. In Guaranteed QoS Pods with strict limits, these operations run within the Pod cgroup. For very large filesystems, the resize may need more resources than the cgroup allows.',
    tags: ['kubernetes', 'volume-expansion', 'qos', 'resources', 'edge-cases'],
  },
  {
    id: 'k8s-storage-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI external-provisioner implement "leader election" and why is it important?',
    options: [
      'Leader election is not needed',
      'The external-provisioner uses Kubernetes Lease objects for leader election; only the leader processes PVC events to prevent duplicate volume creation. If the leader dies, another instance acquires the lease and takes over.',
      'The API server selects the leader',
      'All instances process events independently',
    ],
    correctAnswer: 1,
    explanation: 'CSI sidecars are deployed as Deployments (multiple replicas for HA). Leader election via Lease objects ensures only one instance processes PVC/VolumeSnapshot events. Without it, concurrent processing could create duplicate volumes or cause race conditions.',
    codeSnippet: '# External-provisioner with leader election\ncontainers:\n  - name: csi-provisioner\n    args:\n      - --leader-election=true\n      - --leader-election-namespace=kube-system\n      - --leader-election-lease-duration=15s\n      - --leader-election-renew-deadline=10s\n      - --leader-election-retry-period=5s',
    tags: ['kubernetes', 'csi', 'leader-election', 'ha', 'internals'],
  },
  {
    id: 'k8s-storage-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "generic ephemeral volume" ownership mechanism and how does it prevent PVC reuse attacks?',
    options: [
      'PVC reuse is not possible',
      'Generic ephemeral volume PVCs have an ownerReference to the Pod; the garbage collector deletes them when the Pod is deleted. The admission plugin validates that PVC names match the Pod-volume naming convention to prevent hijacking.',
      'The CSI driver prevents reuse',
      'PVCs are encrypted to prevent reuse',
    ],
    correctAnswer: 1,
    explanation: 'Security measures: (1) PVC name is derived from Pod name + volume name (predictable but validated), (2) ownerReference links PVC to Pod for GC, (3) admission plugin validates naming convention and ownership, (4) existing PVCs with same name but different owner are rejected.',
    tags: ['kubernetes', 'persistent-volumes', 'ephemeral-volumes', 'security', 'ownership'],
  },
  {
    id: 'k8s-storage-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the Longhorn distributed storage engine replicate data across nodes?',
    options: [
      'Using NFS replication',
      'Longhorn creates a dedicated engine controller and replica containers per volume; replicas are placed on different nodes using scheduling rules, and data is synchronously replicated via iSCSI/NVMe-oF to maintain strong consistency',
      'Using DRBD kernel module',
      'Using etcd for data replication',
    ],
    correctAnswer: 1,
    explanation: 'Each Longhorn volume has a dedicated engine (running as a container on the volume node) that replicates writes to replica containers on different nodes. Replicas are essentially consistent copies. The engine uses synchronous replication for write acknowledgment.',
    tags: ['kubernetes', 'storage-classes', 'longhorn', 'replication', 'architecture'],
  },
  {
    id: 'k8s-storage-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "CSI migration" translation layer implementation for AWS EBS?',
    options: [
      'PVs are converted to CSI format',
      'The migration shim translates in-tree awsElasticBlockStore volume specs to CSI calls: volumeID maps to CreateVolume handle, fsType to NodeStageVolume parameter, and partition to stage device mapping',
      'A separate migration controller converts volumes',
      'Users must update PV specs manually',
    ],
    correctAnswer: 1,
    explanation: 'The translation layer maps: in-tree spec.awsElasticBlockStore.volumeID -> CSI volume handle, fsType -> NodeStageVolume VolumeCapability, partition -> NodeStageVolume publish_context. The in-tree PV spec is unchanged; translation happens at runtime in kubelet and controllers.',
    codeSnippet: '# In-tree PV (unchanged by migration)\nspec:\n  awsElasticBlockStore:\n    volumeID: aws://us-east-1a/vol-abc123\n    fsType: ext4\n# Translated at runtime to CSI calls:\n# ControllerPublishVolume(volume_id="vol-abc123", node_id="i-xyz")\n# NodeStageVolume(volume_id="vol-abc123", staging_target_path=...,\n#   volume_capability={mount: {fs_type: "ext4"}})',
    tags: ['kubernetes', 'csi', 'migration', 'aws-ebs', 'translation'],
  },
  {
    id: 'k8s-storage-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does Kubernetes handle the "split-brain" scenario when a node partition causes the same RWO volume to be mounted on two nodes?',
    options: [
      'Kubernetes prevents split-brain automatically',
      'Kubernetes relies on storage-level fencing; cloud providers use SCSI reservations or API-level single-attach enforcement. If both fail, data corruption can occur. The user must implement application-level fencing or use RWOP.',
      'The CSI driver locks the volume',
      'etcd prevents concurrent access',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes best-effort prevention: AD controller force-detach, 6-minute timeout. But during network partition, both nodes may believe they own the volume. True protection requires: storage-level fencing (SCSI-3 PR), cloud API single-attach, or RWOP mode. Without fencing, data corruption is possible.',
    tags: ['kubernetes', 'persistent-volumes', 'split-brain', 'fencing', 'data-corruption'],
  },
  {
    id: 'k8s-storage-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'How does online filesystem expansion work without unmounting, and what are the kernel requirements?',
    options: [
      'Online expansion is not possible',
      'resize2fs (ext4) and xfs_growfs (xfs) support online resize on mounted filesystems; the kernel ext4/xfs implementation supports growing while I/O continues. Requires kernel >= 3.6 for ext4 online resize.',
      'The filesystem must be unmounted for resize',
      'Online expansion requires a specific CSI version',
    ],
    correctAnswer: 1,
    explanation: 'ext4: resize2fs can grow a mounted filesystem (kernel 3.6+). xfs: xfs_growfs only works on mounted filesystems. The CSI driver calls these during NodeExpandVolume while the filesystem is mounted, with the block device already extended by ControllerExpandVolume.',
    tags: ['kubernetes', 'volume-expansion', 'online-resize', 'kernel', 'filesystem'],
  },
  {
    id: 'k8s-storage-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "volume lifecycle" state tracking in the kubelet actual state of world?',
    options: [
      'The kubelet does not track volume states',
      'The kubelet ASW (actual state of world) tracks each volume through states: attached -> globally mounted (staged) -> per-Pod mounted (published). Unmount follows reverse: unpublish -> unstage -> detach.',
      'Volume states are tracked in etcd',
      'The CSI driver tracks volume states',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet maintains ASW with volume states: attached (VolumeAttachment exists), globally_mounted (NodeStageVolume succeeded), per-Pod mounted (NodePublishVolume succeeded). The reconciler compares ASW with desired state (DSW from Pod specs) to determine needed operations.',
    tags: ['kubernetes', 'csi', 'kubelet', 'volume-states', 'actual-state-of-world'],
  },
  {
    id: 'k8s-storage-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the impact of VolumeSnapshot "readyToUse" timing on restore operations?',
    options: [
      'Snapshots are always ready immediately',
      'readyToUse: false means the snapshot is not yet usable for restore; some storage backends need time to finalize the snapshot. Creating a PVC from a non-ready snapshot may hang or fail depending on the CSI driver implementation.',
      'readyToUse only affects deletion',
      'readyToUse is informational only',
    ],
    correctAnswer: 1,
    explanation: 'Some storage backends take time to finalize snapshots (especially for large volumes). readyToUse: false means the snapshot data is not yet consistent. The CSI driver CreateVolume (from snapshot) may wait for readiness or fail. Applications should check readyToUse before initiating restore.',
    tags: ['kubernetes', 'snapshots', 'ready-to-use', 'timing', 'restore'],
  },
  {
    id: 'k8s-storage-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "volume reconstruction" process when a kubelet restarts on a node with running Pods?',
    options: [
      'All volumes are remounted',
      'Kubelet scans existing mount points, reconstructs volume state from filesystem paths and Device Mapper entries, validates against API Pod specs, and only remounts/fixes volumes where actual state does not match desired state',
      'Volumes are unmounted and remounted',
      'The CSI driver handles reconstruction',
    ],
    correctAnswer: 1,
    explanation: 'After kubelet restart: (1) scan /var/lib/kubelet/pods/*/volumes/* to find mounted volumes, (2) reconstruct ASW from mount info, (3) compare with DSW from API, (4) fix inconsistencies (mount missing volumes, unmount orphaned mounts). Running containers continue accessing already-mounted volumes without interruption.',
    tags: ['kubernetes', 'persistent-volumes', 'kubelet', 'reconstruction', 'restart'],
  },
  {
    id: 'k8s-storage-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "volume populator" architecture and how does the "prime PVC" mechanism ensure atomicity?',
    options: [
      'Populators write directly to the target PVC',
      'The populator controller creates a temporary "prime" PVC, runs a population Pod against it, and when complete, rebinds the underlying PV from the prime PVC to the original PVC using claimRef manipulation, ensuring atomic delivery of populated data',
      'The CSI driver handles population',
      'Data is streamed to the PVC in real-time',
    ],
    correctAnswer: 1,
    explanation: 'Architecture: (1) Controller creates prime PVC (temporary), (2) populator Pod writes data to prime PVC, (3) on completion, controller patches PV claimRef from prime PVC to original PVC, (4) original PVC transitions from Pending to Bound with fully populated data. The prime PVC is then deleted.',
    tags: ['kubernetes', 'csi', 'volume-populator', 'prime-pvc', 'architecture'],
  },
  {
    id: 'k8s-storage-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the Ceph CSI driver handle "journal-based" provisioning for crash recovery?',
    options: [
      'It does not handle crash recovery',
      'The Ceph CSI driver writes provisioning operations to an OMAP journal in Ceph before executing them; if the driver crashes mid-operation, it reads the journal on restart to determine if cleanup or completion is needed',
      'The Kubernetes API handles crash recovery',
      'Ceph handles all recovery internally',
    ],
    correctAnswer: 1,
    explanation: 'The Ceph CSI driver uses a journal (stored as Ceph OMAP objects) to track in-progress operations. Before creating a volume, it writes intent to the journal. On crash recovery, it checks the journal: if the volume exists, it completes the PV creation; if not, it cleans up the journal entry.',
    tags: ['kubernetes', 'storage-classes', 'ceph', 'journal', 'crash-recovery'],
  },
  {
    id: 'k8s-storage-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the edge case when a PVC is expanded while the Pod using it is being deleted?',
    options: [
      'Expansion completes normally',
      'The controller expansion may succeed but node expansion cannot complete because the volume is being unmounted; the PVC stays in FileSystemResizePending until a new Pod mounts it and triggers the node-level resize',
      'The expansion is cancelled',
      'The PVC enters an error state',
    ],
    correctAnswer: 1,
    explanation: 'If a Pod is deleted during expansion: ControllerExpandVolume may succeed (backend expanded), but NodeExpandVolume cannot run (no mounted filesystem). The PVC shows FileSystemResizePending. The next Pod to mount this volume triggers the filesystem resize.',
    tags: ['kubernetes', 'volume-expansion', 'edge-case', 'pod-deletion'],
  },
  {
    id: 'k8s-storage-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the "pod scheduling readiness" feature interact with volume provisioning?',
    options: [
      'It does not interact with volumes',
      'schedulingGates can hold a Pod in SchedulingGated state until external conditions are met (e.g., custom volume provisioner ready, compliance check); the Pod does not consume volume resources until gates are cleared',
      'It gates volume creation',
      'It pauses volume attachment',
    ],
    correctAnswer: 1,
    explanation: 'SchedulingGates (GA in 1.30) can delay Pod scheduling until custom conditions are met. This prevents premature volume provisioning (WaitForFirstConsumer) and resource reservation. Gates must be removed by an external controller before the scheduler processes the Pod.',
    tags: ['kubernetes', 'persistent-volumes', 'scheduling-gates', 'pod-readiness'],
  },
  {
    id: 'k8s-storage-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the impact of "Pod startup latency" from volume operations and how can it be optimized?',
    options: [
      'Volume operations add no latency',
      'Volume operations add significant latency: provision (seconds-minutes), attach (seconds), stage/format (seconds-minutes for large volumes), publish (milliseconds). Optimize with pre-provisioned PVCs, faster storage tiers, and parallel operations.',
      'Only network volumes add latency',
      'Latency is fixed regardless of volume type',
    ],
    correctAnswer: 1,
    explanation: 'Volume operation latency: CreateVolume (1s-5min depending on storage), ControllerPublish (1-30s for cloud attach), NodeStage (1s-10min for format), NodePublish (<100ms bind mount). Optimizations: pre-provisioned PVCs, WaitForFirstConsumer (parallel with scheduling), fast storage APIs.',
    tags: ['kubernetes', 'csi', 'latency', 'startup', 'optimization'],
  },
  {
    id: 'k8s-storage-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the "pre-provisioned snapshot" workflow and how does it enable importing external snapshots into Kubernetes?',
    options: [
      'External snapshots cannot be imported',
      'Admin creates VolumeSnapshotContent with snapshotHandle pointing to the external snapshot ID, then creates a VolumeSnapshot referencing the VSC; the snapshot controller binds them without calling CSI CreateSnapshot',
      'The CSI driver auto-discovers external snapshots',
      'Users must recreate volumes from external snapshots manually',
    ],
    correctAnswer: 1,
    explanation: 'Pre-provisioned workflow: (1) Admin creates VSC with spec.source.snapshotHandle=<external-snap-id> and spec.volumeSnapshotRef pointing to the desired VS, (2) Admin creates VS with spec.source.volumeSnapshotContentName=<vsc-name>, (3) Controller binds them. No CSI call is made — the snapshot already exists.',
    codeSnippet: '# Import an existing AWS EBS snapshot\napiVersion: snapshot.storage.k8s.io/v1\nkind: VolumeSnapshotContent\nmetadata:\n  name: imported-content\nspec:\n  driver: ebs.csi.aws.com\n  deletionPolicy: Retain\n  source:\n    snapshotHandle: "snap-0123456789abcdef0"  # Existing snapshot\n  sourceVolumeMode: Filesystem\n  volumeSnapshotRef:\n    name: imported-snapshot\n    namespace: default',
    tags: ['kubernetes', 'snapshots', 'pre-provisioned', 'import', 'workflow'],
  },
  {
    id: 'k8s-storage-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "in-tree to CSI migration" graceful rollback mechanism?',
    options: [
      'Migration cannot be rolled back',
      'The CSIMigration feature gate can be disabled, reverting to in-tree plugins. PVs created by CSI during migration are still valid because the in-tree to CSI translation is bidirectional. However, PVs using CSI-only features will not work with in-tree plugins.',
      'Rolling back requires recreating all PVs',
      'Rolling back requires cluster rebuild',
    ],
    correctAnswer: 1,
    explanation: 'Migration is designed to be reversible: disabling the feature gate reverts to in-tree plugins. PV specs remain in in-tree format (the translation is transparent). However, if CSI-only features (snapshots, expansion) were used during migration, those PVs may not be fully compatible with in-tree plugins.',
    tags: ['kubernetes', 'persistent-volumes', 'csi-migration', 'rollback'],
  },
  {
    id: 'k8s-storage-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI "volume stats" mechanism work for kubelet volume metrics?',
    options: [
      'kubelet queries the filesystem directly',
      'kubelet periodically calls CSI NodeGetVolumeStats RPC which returns available/used/total bytes and inodes; these are exposed as kubelet_volume_stats_* Prometheus metrics for monitoring',
      'The CSI controller tracks stats centrally',
      'Stats are read from the PV status field',
    ],
    correctAnswer: 1,
    explanation: 'kubelet calls NodeGetVolumeStats on the CSI driver node plugin for each mounted volume. The driver returns usage (bytes and inodes). kubelet exposes these as Prometheus metrics: kubelet_volume_stats_capacity_bytes, kubelet_volume_stats_used_bytes, kubelet_volume_stats_inodes_used, etc.',
    tags: ['kubernetes', 'csi', 'volume-stats', 'metrics', 'monitoring'],
  },
  {
    id: 'k8s-storage-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'What is the "storage capacity aware scheduling" race condition and how is it mitigated?',
    options: [
      'There is no race condition',
      'Between the scheduler checking CSIStorageCapacity and the provisioner creating the volume, capacity may be consumed by other workloads; mitigation is through retry with backoff, and the scheduler uses capacity as a hint rather than a guarantee',
      'The provisioner locks capacity during scheduling',
      'The scheduler reserves capacity in advance',
    ],
    correctAnswer: 1,
    explanation: 'The race: scheduler reads capacity, decides to schedule, but by the time provisioner runs, another volume consumed the capacity. Mitigation: (1) capacity is a scheduling hint, not reservation, (2) provisioner retries with backoff on failure, (3) scheduler can reschedule the Pod to a different node.',
    tags: ['kubernetes', 'storage-classes', 'storage-capacity', 'race-condition', 'scheduling'],
  },
  {
    id: 'k8s-storage-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the "node expansion secret" feature for CSI volume expansion?',
    options: [
      'It encrypts expansion operations',
      'CSI NodeExpandVolume can receive secrets (credentials) from a Kubernetes Secret referenced in the StorageClass, enabling authentication with the storage backend during node-level filesystem resize',
      'Secrets are used to authorize expansion',
      'Node expansion does not support secrets',
    ],
    correctAnswer: 1,
    explanation: 'Some CSI drivers need credentials during NodeExpandVolume (e.g., Ceph needs authentication). The csi.storage.k8s.io/node-expand-secret-name and namespace annotations in StorageClass reference a Secret that kubelet passes to NodeExpandVolume.',
    codeSnippet: '# StorageClass with node expansion secret\nparameters:\n  csi.storage.k8s.io/node-expand-secret-name: ceph-expand-secret\n  csi.storage.k8s.io/node-expand-secret-namespace: rook-ceph',
    tags: ['kubernetes', 'volume-expansion', 'csi', 'secrets', 'node-expand'],
  },
  {
    id: 'k8s-storage-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What are the implications of the "ClusterIP None" service on StatefulSet volume management?',
    options: [
      'No relationship between headless services and volumes',
      'The headless Service enables stable DNS names for StatefulSet Pods; combined with volumeClaimTemplates, each Pod gets a persistent identity (stable DNS + persistent volume) that survives rescheduling to any node in the same zone',
      'Headless services manage volume lifecycle',
      'ClusterIP None disables volume mounts',
    ],
    correctAnswer: 1,
    explanation: 'StatefulSet combines: headless Service (stable DNS: pod-0.svc.ns.svc.cluster.local) + volumeClaimTemplates (persistent storage: data-pod-0). Together, they provide stable identity. PVCs persist across Pod reschedule, and DNS follows the Pod. This is the foundation for stateful workloads.',
    tags: ['kubernetes', 'persistent-volumes', 'statefulset', 'headless-service', 'identity'],
  },
  {
    id: 'k8s-storage-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI "volume lifecycle" differ for ephemeral inline volumes vs persistent volumes in the kubelet?',
    options: [
      'No difference in kubelet handling',
      'Ephemeral inline volumes skip the attach-detach controller and VolumeAttachment creation; kubelet calls NodePublishVolume directly (no stage step) with the volume_context from the Pod spec, and manages cleanup on Pod deletion',
      'Ephemeral volumes use a different CSI protocol',
      'kubelet does not handle ephemeral volumes',
    ],
    correctAnswer: 1,
    explanation: 'Ephemeral inline volumes: (1) no PV/PVC objects, (2) no ControllerPublishVolume (no attach), (3) kubelet calls NodePublishVolume directly with volume_context from CSIVolumeSource in the Pod spec, (4) kubelet calls NodeUnpublishVolume on Pod deletion. This simplified path is faster but less feature-rich.',
    tags: ['kubernetes', 'csi', 'ephemeral-volumes', 'kubelet', 'lifecycle'],
  },
  {
    id: 'k8s-storage-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the "volume snapshot group" cross-resource consistency guarantee?',
    options: [
      'Snapshots in a group are eventually consistent',
      'The storage backend must guarantee that all volumes in the group are snapshotted at the same I/O barrier point; Kubernetes delegates this to the CSI GroupCreateSnapshot RPC which must be atomic from the storage perspective',
      'Kubernetes ensures consistency through Pod pause',
      'Consistency is guaranteed by the snapshot controller',
    ],
    correctAnswer: 1,
    explanation: 'The consistency guarantee is entirely delegated to the storage backend via CSI. The GroupCreateSnapshot RPC must be implemented atomically by the driver/storage. Kubernetes does not freeze I/O or coordinate with applications — that responsibility falls on the storage layer.',
    tags: ['kubernetes', 'snapshots', 'group-consistency', 'storage-guarantee'],
  },
  {
    id: 'k8s-storage-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the "NodeUnpublishVolume" and "NodeUnstageVolume" cleanup sequence handle failures?',
    options: [
      'Failures are ignored',
      'The kubelet volume reconciler retries unpublish/unstage with exponential backoff; if the volume cannot be cleaned up (e.g., busy mount), the Pod deletion may be blocked by the finalizer until cleanup succeeds',
      'Failed cleanup is logged but ignored',
      'The node is marked as unschedulable',
    ],
    correctAnswer: 1,
    explanation: 'Cleanup sequence: NodeUnpublishVolume (remove Pod bind mount) -> NodeUnstageVolume (unmount global staging) -> ControllerUnpublishVolume (detach). If any step fails, the reconciler retries. Persistent failures can block Pod deletion and prevent node drain.',
    tags: ['kubernetes', 'persistent-volumes', 'cleanup', 'failure-handling', 'internals'],
  },
  {
    id: 'k8s-storage-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "node service capability" VOLUME_CONDITION and how does it enable proactive maintenance?',
    options: [
      'It reports node health',
      'The NODE_GET_VOLUME_STATS capability with VOLUME_CONDITION enables the CSI driver to report volume abnormalities (degraded, failing hardware) through the NodeGetVolumeStats response; kubelet exposes these as Pod events',
      'It checks CSI driver node connectivity',
      'It validates volume mount configuration',
    ],
    correctAnswer: 1,
    explanation: 'With VOLUME_CONDITION capability, NodeGetVolumeStats returns a VolumeCondition message with abnormal (bool) and message fields. The kubelet creates PodEvents (VolumeConditionAbnormal) that monitoring systems can alert on, enabling proactive maintenance before data loss.',
    codeSnippet: '# CSI NodeGetVolumeStats response with condition\n{\n  "usage": [...],\n  "volumeCondition": {\n    "abnormal": true,\n    "message": "Volume backing disk showing increased error rate"\n  }\n}\n# kubelet creates event:\n# Warning VolumeConditionAbnormal Volume xyz: abnormal condition detected',
    tags: ['kubernetes', 'csi', 'volume-condition', 'proactive-maintenance'],
  },
  {
    id: 'k8s-storage-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does the Kubernetes scheduler volume binding plugin handle preemption with volumes?',
    options: [
      'Preemption ignores volume constraints',
      'During preemption evaluation, the scheduler must verify that evicting lower-priority Pods frees enough volume capacity/attachments for the higher-priority Pod; it checks volume limits and topology feasibility post-preemption',
      'Volumes prevent preemption entirely',
      'Preempted Pod volumes are transferred',
    ],
    correctAnswer: 1,
    explanation: 'The VolumeBinding plugin participates in preemption evaluation: it checks if evicting lower-priority Pods would make volume attachment (within limits) and provisioning (within capacity) feasible for the preemptor. If not, the node is not considered for preemption.',
    tags: ['kubernetes', 'storage-classes', 'scheduler', 'preemption', 'volume-binding'],
  },
  {
    id: 'k8s-storage-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the exact mechanism by which the PV controller handles "stale" claimRef after a namespace deletion?',
    options: [
      'claimRef is automatically cleaned up',
      'When a namespace is deleted, all PVCs in it are deleted; PVs with Retain policy keep stale claimRef pointing to the deleted PVC. The PV stays in Released state and cannot be rebound until an admin manually removes the claimRef.',
      'The garbage collector handles stale refs',
      'PVs are deleted with the namespace',
    ],
    correctAnswer: 1,
    explanation: 'Namespace deletion triggers PVC deletion (cascade). PVs with Retain policy keep the stale claimRef (pointing to a non-existent PVC/namespace). The PV controller sees the PVC is gone and sets status to Released. Admin must patch to remove claimRef to make it Available again.',
    tags: ['kubernetes', 'persistent-volumes', 'claim-ref', 'namespace-deletion', 'stale'],
  },
  {
    id: 'k8s-storage-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "read-write once pod" enforcement path through the kubelet volume manager?',
    options: [
      'RWOP is only enforced by the API server',
      'Kubelet checks mountedPods counter in the actual state of world during NodePublishVolume; if another Pod already has the volume published, it rejects the mount with a clear error, regardless of what the storage backend supports',
      'The CSI driver enforces RWOP',
      'RWOP is enforced by etcd locks',
    ],
    correctAnswer: 1,
    explanation: 'RWOP enforcement in kubelet: during the reconcile loop, before calling NodePublishVolume, kubelet checks if the volume is already published to another Pod (via ASW tracking). If so, it sets an error condition on the new Pod event. This is a kubelet-level enforcement independent of the storage backend.',
    tags: ['kubernetes', 'csi', 'rwop', 'kubelet', 'enforcement-path'],
  },
  {
    id: 'k8s-storage-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the recovery mechanism when a node-level expansion fails due to filesystem corruption?',
    options: [
      'Automatic recovery',
      'The PVC shows NodeResizeFailed condition; admin must run fsck on the volume (possibly requiring detach/reattach to a maintenance Pod), then retry expansion or restore from snapshot',
      'The CSI driver auto-repairs the filesystem',
      'The volume is automatically replaced',
    ],
    correctAnswer: 1,
    explanation: 'If NodeExpandVolume fails due to corruption: (1) PVC shows allocatedResourceStatuses: NodeResizeFailed, (2) admin must diagnose (check kubelet + CSI logs), (3) possibly attach volume to maintenance Pod for fsck, (4) retry expansion or restore from snapshot. This is a manual recovery process.',
    tags: ['kubernetes', 'volume-expansion', 'corruption', 'recovery', 'fsck'],
  },
  {
    id: 'k8s-storage-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the "PV deletion protection" interact with the finalizer system during force deletion?',
    options: [
      'Force deletion bypasses finalizers',
      'Even with kubectl delete --force, the PV is not actually removed from etcd until all finalizers are cleared; the deletion timestamp is set immediately but the PV object persists. The PV controller must remove pv-protection finalizer first.',
      'Force deletion removes the PV immediately',
      'Finalizers are ignored after a timeout',
    ],
    correctAnswer: 1,
    explanation: 'Force deletion (--grace-period=0 --force) sets DeletionTimestamp and grace period to 0, but does NOT bypass finalizers. The PV object remains in etcd with DeletionTimestamp until the PV controller removes the kubernetes.io/pv-protection finalizer. Only then is the object garbage collected from etcd.',
    tags: ['kubernetes', 'persistent-volumes', 'finalizer', 'force-deletion', 'etcd'],
  },
  {
    id: 'k8s-storage-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "volume health informer" architecture in the kubelet for CSI volume condition monitoring?',
    options: [
      'The kubelet polls the CSI driver once at startup',
      'The kubelet volumeHealthChecker runs a periodic loop calling NodeGetVolumeStats for each mounted CSI volume; if volumeCondition.abnormal is true, it generates a PodEvent and optionally a PVC event for monitoring systems',
      'Volume health is monitored by the API server',
      'The CSI driver pushes health updates to kubelet',
    ],
    correctAnswer: 1,
    explanation: 'The kubelet runs a volumeHealthChecker goroutine that periodically (configurable interval) calls NodeGetVolumeStats for each CSI volume. It checks the VolumeCondition in the response and creates Events on affected Pods and PVCs. This enables Prometheus alerts for volume degradation.',
    tags: ['kubernetes', 'csi', 'volume-health', 'kubelet', 'monitoring', 'architecture'],
  },
  {
    id: 'k8s-storage-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How does the snapshot controller handle concurrent snapshot operations on the same source PVC?',
    options: [
      'Concurrent snapshots are queued',
      'Multiple VolumeSnapshots can reference the same PVC; the controller processes them independently, calling CreateSnapshot for each. The storage backend must handle concurrent snapshot creation for the same source volume correctly.',
      'Only one snapshot per PVC is allowed',
      'The controller serializes all snapshot operations',
    ],
    correctAnswer: 1,
    explanation: 'The controller does not serialize per-PVC. Multiple snapshots of the same PVC are processed concurrently. The CSI driver and storage backend must handle concurrent CreateSnapshot calls. Some backends serialize internally (e.g., AWS EBS allows one snapshot creation at a time per volume).',
    tags: ['kubernetes', 'snapshots', 'concurrency', 'controller'],
  },
  {
    id: 'k8s-storage-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'What is the "immediate topology" vs "late binding topology" flow difference at the CSI protocol level?',
    options: [
      'No protocol-level difference',
      'Immediate: CreateVolume receives all allowed topologies. Late binding: CreateVolume receives a single preferred topology (from the scheduled node). The driver uses accessible_topology in CreateVolume to decide where to place the volume.',
      'Immediate uses more gRPC calls',
      'Late binding skips CreateVolume',
    ],
    correctAnswer: 1,
    explanation: 'In the CreateVolume RPC: Immediate binding passes AllowedTopologies from StorageClass as accessible_topology (driver picks one). WaitForFirstConsumer passes the specific node topology as the preferred accessible_topology (scheduler already chose the node). The driver creates the volume in the specified topology.',
    tags: ['kubernetes', 'storage-classes', 'topology', 'csi-protocol', 'internals'],
  },
  {
    id: 'k8s-storage-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the impact of etcd compaction on PV/PVC history and volume operations?',
    options: [
      'etcd compaction has no impact on volumes',
      'etcd compaction removes old revisions; the PV controller and CSI sidecars use resourceVersion for optimistic concurrency. After compaction, stale watches may receive "resource version too old" errors requiring re-list, potentially causing temporary delays in volume operations.',
      'Compaction deletes PV data',
      'Compaction requires PV recreation',
    ],
    correctAnswer: 1,
    explanation: 'etcd compaction can cause watch channels to close with "410 Gone" (resource version too old) errors. CSI sidecars and controllers using informers handle this by re-listing from etcd. During re-list, volume operations may be briefly delayed. This is generally transparent but can cause spikes in API server load.',
    tags: ['kubernetes', 'persistent-volumes', 'etcd', 'compaction', 'resource-version'],
  },
  {
    id: 'k8s-storage-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI volume lifecycle handle "node unstage" failure during Pod deletion?',
    options: [
      'The volume remains staged forever',
      'If NodeUnstageVolume fails (e.g., device busy), kubelet retries with backoff. The volume remains globally mounted, blocking VolumeAttachment deletion and potentially preventing the node from being drained. Admin intervention may be needed.',
      'The node is automatically rebooted',
      'The CSI driver force-unmounts',
    ],
    correctAnswer: 1,
    explanation: 'Failed unstage: kubelet retries NodeUnstageVolume. If persistently failing (device busy due to leaked mount), the volume stays staged. This blocks: ControllerUnpublishVolume (still staged), VolumeAttachment deletion (still published), and potentially node drain. Admin may need to manually unmount.',
    tags: ['kubernetes', 'csi', 'unstage-failure', 'volume-lifecycle', 'troubleshooting'],
  },
  {
    id: 'k8s-storage-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the interaction between "volume expansion" and "pod disruption budget" during rolling updates?',
    options: [
      'PDB does not affect expansion',
      'If expansion requires Pod restart (offline expansion) and PDB limits disruptions, the expansion may be delayed until the disruption budget allows the restart; StatefulSet ordered updates further serialize this',
      'PDB prevents expansion entirely',
      'Expansion bypasses PDB',
    ],
    correctAnswer: 1,
    explanation: 'For offline expansion requiring Pod restart: the PDB may prevent simultaneous Pod deletions needed for volume unmount. StatefulSet OrderedReady policy further serializes (one Pod at a time). This means expanding all PVCs in a 5-replica StatefulSet with PDB maxUnavailable=1 takes 5x the per-volume expansion time.',
    tags: ['kubernetes', 'volume-expansion', 'pdb', 'statefulset', 'rolling-update'],
  },
  {
    id: 'k8s-storage-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How does the "fsGroupChangePolicy: OnRootMismatch" optimization determine if a chown is needed?',
    options: [
      'It checks all files in the volume',
      'Kubelet only checks the root directory ownership/permissions; if the root dir group matches fsGroup, it skips the recursive chown. This reduces mount time from O(n-files) to O(1) for volumes with matching ownership.',
      'It uses filesystem metadata to check',
      'It skips chown entirely',
    ],
    correctAnswer: 1,
    explanation: 'OnRootMismatch: kubelet stats the volume root directory. If root gid matches fsGroup, recursive chown is skipped entirely. For volumes with millions of files (ML datasets, log archives), this reduces mount time from minutes to milliseconds. The assumption is that if root matches, all contents match.',
    tags: ['kubernetes', 'persistent-volumes', 'fsgroup', 'on-root-mismatch', 'optimization'],
  },
  {
    id: 'k8s-storage-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the "node-specific" vs "controller-specific" CSI identity negotiation and why do some drivers have both?',
    options: [
      'Drivers only have one identity',
      'CSI drivers can run separate controller and node plugins with different identities; the controller plugin handles provisioning/attach while the node plugin handles stage/mount. Some storage backends require different authentication for each role.',
      'Identity is only for logging purposes',
      'Both plugins always share the same identity',
    ],
    correctAnswer: 1,
    explanation: 'A CSI driver can deploy as: (1) controller plugin (Deployment, handles CreateVolume/ControllerPublish), (2) node plugin (DaemonSet, handles NodeStage/NodePublish). They can have different names/versions via GetPluginInfo. The controller may need admin credentials while nodes need mount-only credentials.',
    tags: ['kubernetes', 'csi', 'identity', 'controller-node', 'architecture'],
  },
  {
    id: 'k8s-storage-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the "transfer" operation for VolumeSnapshotContent between VolumeSnapshots?',
    options: [
      'Snapshots cannot be transferred',
      'An admin can change the volumeSnapshotRef in a VolumeSnapshotContent to point to a different VolumeSnapshot, effectively transferring ownership of the underlying snapshot resource between namespaces or use cases',
      'Transfer requires recreating the snapshot',
      'Transfer is handled by the CSI driver',
    ],
    correctAnswer: 1,
    explanation: 'Since VolumeSnapshotContent is cluster-scoped, an admin can rebind it to a different VolumeSnapshot by updating spec.volumeSnapshotRef. This enables cross-namespace snapshot sharing and ownership transfer without duplicating the underlying storage snapshot.',
    tags: ['kubernetes', 'snapshots', 'transfer', 'ownership', 'cross-namespace'],
  },
  {
    id: 'k8s-storage-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "mount utility" detection mechanism in CSI node plugins?',
    options: [
      'Mount utilities are always available',
      'CSI node plugins must detect and use the correct mount utilities (mount, umount, mkfs.ext4, xfs_growfs, resize2fs) from the host; they typically mount the host /usr/sbin or /sbin into the container for access to the host mount tools',
      'The kubelet provides mount utilities',
      'CSI drivers bundle their own mount tools',
    ],
    correctAnswer: 1,
    explanation: 'CSI node plugins run in containers but need host-level mount utilities. They typically: (1) mount host /sbin and /usr/sbin into the container, (2) use nsenter to execute in the host mount namespace, or (3) use privileged mode to access host tools. This is necessary for format, mount, resize operations.',
    codeSnippet: '# CSI node plugin accessing host mount utilities\ncontainers:\n  - name: csi-node-driver\n    securityContext:\n      privileged: true\n    volumeMounts:\n      - name: host-dev\n        mountPath: /dev\n      - name: host-sys\n        mountPath: /sys\n      - name: host-mount\n        mountPath: /host/mount\n        mountPropagation: Bidirectional\nvolumes:\n  - name: host-dev\n    hostPath:\n      path: /dev\n  - name: host-sys\n    hostPath:\n      path: /sys',
    tags: ['kubernetes', 'persistent-volumes', 'csi', 'mount-utilities', 'node-plugin'],
  },
  {
    id: 'k8s-storage-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'dynamic-provisioning',
    question: 'What is the "provision timeout" behavior in the CSI external-provisioner for slow storage backends?',
    options: [
      'Provisioning never times out',
      'The external-provisioner has a configurable timeout (--timeout); if CreateVolume does not respond within this period, it retries with exponential backoff. For very slow backends, the timeout may need to be increased to prevent premature failure.',
      'Timeout is handled by the API server',
      'The kubelet manages provisioning timeouts',
    ],
    correctAnswer: 1,
    explanation: 'The external-provisioner gRPC timeout defaults to 10s. For slow storage (e.g., thick-provisioned large volumes, cross-region replication setup), CreateVolume may take minutes. If timeout < actual time, the provisioner retries (idempotent), potentially creating the volume on the second attempt while the first is still running.',
    tags: ['kubernetes', 'dynamic-provisioning', 'timeout', 'external-provisioner', 'slow-storage'],
  },
  {
    id: 'k8s-storage-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI "token requests" feature enable per-Pod identity-based volume access?',
    options: [
      'It uses a global service account',
      'The CSIDriver.spec.tokenRequests field specifies audiences; kubelet obtains a bound service account token for the Pod and passes it to NodePublishVolume. The CSI driver uses this token to authenticate the Pod identity with external systems.',
      'Tokens are passed via environment variables',
      'The API server handles token exchange',
    ],
    correctAnswer: 1,
    explanation: 'Flow: (1) CSIDriver spec declares tokenRequests with audiences, (2) kubelet requests bound SA token from TokenRequest API for the Pod SA with specified audience, (3) kubelet passes token in NodePublishVolume.volume_context, (4) CSI driver uses token to authenticate with Vault/cloud IAM and fetch Pod-specific secrets.',
    codeSnippet: '# CSIDriver with token requests\napiVersion: storage.k8s.io/v1\nkind: CSIDriver\nmetadata:\n  name: secrets-store.csi.k8s.io\nspec:\n  podInfoOnMount: true\n  tokenRequests:\n    - audience: "vault"          # Token audience for Vault\n      expirationSeconds: 600\n    - audience: ""               # Default API server audience\n  volumeLifecycleModes:\n    - Ephemeral',
    tags: ['kubernetes', 'csi', 'token-requests', 'per-pod-identity', 'internals'],
  },
  {
    id: 'k8s-storage-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "generic ephemeral volume" security model for preventing PVC name collision attacks?',
    options: [
      'Name collisions are not possible',
      'The ephemeral volume admission plugin validates that the PVC name follows the <pod-name>-<volume-name> convention and rejects Pods that try to use pre-existing PVCs with matching names, preventing data theft from orphaned PVCs',
      'PVC names are randomized',
      'RBAC prevents name collisions',
    ],
    correctAnswer: 1,
    explanation: 'Security: (1) PVC name = <pod-name>-<volume-name> (deterministic), (2) admission plugin validates naming convention, (3) if a PVC with that name already exists but is NOT owned by the Pod (wrong ownerRef), the Pod creation is rejected. This prevents a malicious Pod from claiming an existing PVC to access its data.',
    tags: ['kubernetes', 'persistent-volumes', 'ephemeral-volumes', 'security', 'name-collision'],
  },
];

export default questions;
