import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-storage-l3-001',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'A Pod is stuck in ContainerCreating state with "Multi-Attach error" for its PVC. What is the most likely cause?',
    options: [
      'The PVC does not exist',
      'The RWO volume is still attached to a node where the previous Pod was running',
      'The StorageClass is misconfigured',
      'The Pod has incorrect volume mounts',
    ],
    correctAnswer: 1,
    explanation: 'Multi-Attach errors occur when an RWO volume is still attached to a previous node (often due to ungraceful Pod termination). The volume must be detached from the old node before it can be mounted on the new one.',
    codeSnippet: '# Check VolumeAttachment status\nkubectl get volumeattachment\n\n# Force detach if stuck (use with caution)\nkubectl delete volumeattachment <attachment-name>\n\n# Or delete the stuck Pod on the old node\nkubectl delete pod <old-pod> --grace-period=0 --force',
    tags: ['kubernetes', 'pvcs', 'multi-attach', 'troubleshooting'],
  },
  {
    id: 'k8s-storage-l3-002',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'dynamic-provisioning',
    question: 'A PVC is stuck in Pending state. The StorageClass exists and has a valid provisioner. What should you investigate?',
    options: [
      'Check if the Pod referencing the PVC exists',
      'Check CSI driver logs, StorageClass parameters, available capacity, and if WaitForFirstConsumer is blocking binding',
      'Delete and recreate the PVC',
      'Check CoreDNS logs',
    ],
    correctAnswer: 1,
    explanation: 'Check: (1) CSI driver controller Pod logs for provisioning errors, (2) StorageClass parameters are valid, (3) sufficient capacity in the target zone, (4) if WaitForFirstConsumer requires a Pod to be scheduled first.',
    codeSnippet: '# Check PVC events\nkubectl describe pvc <pvc-name>\n\n# Check CSI controller logs\nkubectl -n kube-system logs -l app=ebs-csi-controller\n\n# Check StorageClass\nkubectl get sc <class-name> -o yaml\n\n# Check if WaitForFirstConsumer is waiting for Pod\nkubectl get pvc <pvc-name> -o jsonpath=\'{.spec.storageClassName}\'',
    tags: ['kubernetes', 'dynamic-provisioning', 'troubleshooting', 'pending'],
  },
  {
    id: 'k8s-storage-l3-003',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'After expanding a PVC, the Pod filesystem still shows the old size. What is happening?',
    options: [
      'The expansion failed',
      'The storage backend expanded but the filesystem has not been resized yet; the PVC condition shows FileSystemResizePending',
      'The kubelet cache needs to be cleared',
      'The PV needs to be manually resized',
    ],
    correctAnswer: 1,
    explanation: 'Volume expansion is a two-phase process: (1) backend storage expansion (controller), (2) filesystem expansion on the node. The filesystem resize may require remounting the volume, which happens when a Pod starts using it.',
    codeSnippet: '# Check PVC conditions\nkubectl get pvc <name> -o jsonpath=\'{.status.conditions}\'\n\n# The condition will show:\n# type: FileSystemResizePending\n# message: "Waiting for user to (re-)start a pod to finish filesystem resize"\n\n# Solution: restart the Pod or wait for the kubelet to resize online\nkubectl delete pod <pod-using-pvc>',
    tags: ['kubernetes', 'volume-expansion', 'filesystem-resize', 'troubleshooting'],
  },
  {
    id: 'k8s-storage-l3-004',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'The CSI driver node plugin DaemonSet is in CrashLoopBackOff. How do you troubleshoot?',
    options: [
      'Reinstall the CSI driver',
      'Check the node plugin logs, verify the CSI socket path, check node plugin registrar, and verify required host paths are accessible',
      'Scale the DaemonSet to 0 and back',
      'Check the API server logs',
    ],
    correctAnswer: 1,
    explanation: 'Check node plugin container and registrar sidecar logs. Common issues include: missing CSI socket, kubelet plugin directory permissions, required kernel modules, and host path mount issues.',
    codeSnippet: '# Check CSI node plugin logs\nkubectl -n kube-system logs <csi-node-pod> -c <driver-container>\nkubectl -n kube-system logs <csi-node-pod> -c node-driver-registrar\n\n# Verify registration\nls -la /var/lib/kubelet/plugins_registry/\nls -la /var/lib/kubelet/plugins/<driver-name>/\n\n# Check kubelet logs for registration issues\njournalctl -u kubelet | grep -i csi',
    tags: ['kubernetes', 'csi', 'troubleshooting', 'daemonset'],
  },
  {
    id: 'k8s-storage-l3-005',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you safely migrate data from one PV to another (e.g., changing StorageClass)?',
    options: [
      'Edit the PV to change the StorageClass',
      'Create a new PVC, run a data migration job/Pod that mounts both PVCs and copies data, then switch the application to the new PVC',
      'Delete the PV and let dynamic provisioning recreate it',
      'Use kubectl cp to transfer data',
    ],
    correctAnswer: 1,
    explanation: 'Create the new PVC with the desired StorageClass, run a job that mounts both old and new PVCs, copy the data (using rsync, cp, or backup/restore), then update the application to use the new PVC.',
    codeSnippet: '# Migration Pod\napiVersion: v1\nkind: Pod\nmetadata:\n  name: pv-migrator\nspec:\n  containers:\n    - name: migrator\n      image: alpine:latest\n      command: ["sh", "-c", "cp -av /old-data/* /new-data/ && echo \'Done\'"]\n      volumeMounts:\n        - name: old\n          mountPath: /old-data\n          readOnly: true\n        - name: new\n          mountPath: /new-data\n  volumes:\n    - name: old\n      persistentVolumeClaim:\n        claimName: old-pvc\n    - name: new\n      persistentVolumeClaim:\n        claimName: new-pvc\n  restartPolicy: Never',
    tags: ['kubernetes', 'persistent-volumes', 'migration', 'best-practices'],
  },
  {
    id: 'k8s-storage-l3-006',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you implement storage tiering with different performance levels in a Kubernetes cluster?',
    options: [
      'Use a single StorageClass with variable parameters',
      'Create multiple StorageClasses (fast-ssd, standard-hdd, archive) with different provisioner parameters and let teams select the appropriate tier via PVC storageClassName',
      'Use node labels to route storage',
      'Configure the kubelet with storage tiers',
    ],
    correctAnswer: 1,
    explanation: 'Create distinct StorageClasses for each tier with appropriate provisioner parameters (disk type, IOPS, throughput). Teams select the tier by specifying storageClassName in their PVCs.',
    codeSnippet: '# High-performance tier\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: high-perf\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: io2\n  iops: "10000"\n---\n# Standard tier\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: standard\n  annotations:\n    storageclass.kubernetes.io/is-default-class: "true"\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n---\n# Archive tier\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: archive\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: sc1',
    tags: ['kubernetes', 'storage-classes', 'tiering', 'best-practices'],
  },
  {
    id: 'k8s-storage-l3-007',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How do you implement a backup strategy using VolumeSnapshots?',
    options: [
      'Enable automatic backups in the StorageClass',
      'Use CronJobs or Velero to create VolumeSnapshots on a schedule, with retention policies to clean up old snapshots',
      'Snapshots are automatically created by the CSI driver',
      'Use the snapshot controller scheduled mode',
    ],
    correctAnswer: 1,
    explanation: 'Create a CronJob that runs kubectl/client to create VolumeSnapshots on a schedule. Implement retention by cleaning up old snapshots. Velero provides a more comprehensive solution with cross-cluster backup/restore.',
    codeSnippet: '# CronJob for scheduled snapshots\napiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: db-snapshot\nspec:\n  schedule: "0 2 * * *"  # Daily at 2 AM\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          serviceAccountName: snapshot-creator\n          containers:\n            - name: snapshot\n              image: bitnami/kubectl:latest\n              command:\n                - /bin/sh\n                - -c\n                - |\n                  SNAP_NAME="db-snap-$(date +%Y%m%d-%H%M%S)"\n                  kubectl apply -f - <<SNAP\n                  apiVersion: snapshot.storage.k8s.io/v1\n                  kind: VolumeSnapshot\n                  metadata:\n                    name: $SNAP_NAME\n                  spec:\n                    volumeSnapshotClassName: csi-snap-class\n                    source:\n                      persistentVolumeClaimName: db-data\n                  SNAP\n          restartPolicy: OnFailure',
    tags: ['kubernetes', 'snapshots', 'backup', 'cronjob'],
  },
  {
    id: 'k8s-storage-l3-008',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'access-modes',
    question: 'You need to share a volume across multiple Pods on different nodes, but your cloud provider only supports RWO block storage. What are your options?',
    options: [
      'Use RWO and schedule all Pods on the same node',
      'Use an NFS server (or EFS/Azure Files), deploy an NFS provisioner on top of block storage, or use a distributed filesystem like CephFS',
      'Use hostPath on all nodes',
      'Create multiple PVCs with the same data',
    ],
    correctAnswer: 1,
    explanation: 'Options include: managed file storage (EFS, Azure Files), deploying an NFS server (nfs-subdir-external-provisioner) backed by a block PV, or deploying a distributed storage solution (Rook/Ceph, Longhorn, OpenEBS).',
    tags: ['kubernetes', 'access-modes', 'rwx', 'shared-storage'],
  },
  {
    id: 'k8s-storage-l3-009',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI volume health monitoring feature?',
    options: [
      'Monitoring disk space usage',
      'CSI drivers can report volume health conditions to Kubernetes, enabling Pods to be notified about volume issues like corruption or detachment',
      'Health checks for the CSI driver pods',
      'SMART monitoring for disks',
    ],
    correctAnswer: 1,
    explanation: 'CSI volume health monitoring (alpha) allows drivers to report abnormal volume conditions. The kubelet exposes these as events on Pods, enabling applications to take corrective action.',
    tags: ['kubernetes', 'csi', 'health-monitoring'],
  },
  {
    id: 'k8s-storage-l3-010',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you handle PV reclamation in a production environment to prevent data loss?',
    options: [
      'Always use Delete reclaim policy',
      'Use Retain policy for critical data, implement PV lifecycle management with labeling, backup before release, and automated cleanup procedures',
      'Never delete PVCs',
      'Use the Recycle policy',
    ],
    correctAnswer: 1,
    explanation: 'For production: use Retain policy for critical PVs, label PVs with ownership info, ensure backups before PVC deletion, implement a process for reviewing Released PVs, and automate cleanup of verified PVs.',
    tags: ['kubernetes', 'persistent-volumes', 'reclaim-policy', 'production'],
  },
  {
    id: 'k8s-storage-l3-011',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you prevent users from creating PVCs with expensive storage classes?',
    options: [
      'Delete the expensive StorageClasses',
      'Use ResourceQuota per StorageClass to limit usage, and RBAC to restrict StorageClass access',
      'Set a cluster-wide storage limit',
      'Make the expensive classes non-default',
    ],
    correctAnswer: 1,
    explanation: 'Use ResourceQuota to limit storage per StorageClass per namespace. Use RBAC or admission controllers (OPA Gatekeeper, Kyverno) to restrict which namespaces/teams can use specific StorageClasses.',
    codeSnippet: '# ResourceQuota limiting high-perf storage\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: storage-quota\n  namespace: team-a\nspec:\n  hard:\n    high-perf.storageclass.storage.k8s.io/requests.storage: "100Gi"\n    high-perf.storageclass.storage.k8s.io/persistentvolumeclaims: "5"',
    tags: ['kubernetes', 'storage-classes', 'resource-quota', 'governance'],
  },
  {
    id: 'k8s-storage-l3-012',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'emptydir',
    question: 'A Pod is being evicted due to ephemeral storage pressure. What is happening and how do you fix it?',
    options: [
      'The node is out of memory',
      'The Pod emptyDir, container logs, or writable layers exceeded the ephemeral-storage limit; set proper ephemeral-storage limits and sizeLimit on emptyDir',
      'The PVC is full',
      'The node disk is full due to images',
    ],
    correctAnswer: 1,
    explanation: 'Kubelet evicts Pods when ephemeral-storage (emptyDir + logs + writable layers) exceeds the limit. Set appropriate requests/limits for ephemeral-storage and sizeLimit on emptyDir volumes.',
    codeSnippet: 'spec:\n  containers:\n    - name: app\n      resources:\n        requests:\n          ephemeral-storage: "2Gi"\n        limits:\n          ephemeral-storage: "5Gi"\n  volumes:\n    - name: cache\n      emptyDir:\n        sizeLimit: "2Gi"  # Explicit limit on emptyDir',
    tags: ['kubernetes', 'emptydir', 'ephemeral-storage', 'eviction'],
  },
  {
    id: 'k8s-storage-l3-013',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'How do you implement a disaster recovery strategy for StatefulSet PVCs across availability zones?',
    options: [
      'Use RWX volumes across zones',
      'Use VolumeSnapshots for periodic backups, replicate snapshots cross-region, and use restore procedures to recreate PVCs from snapshots in a different zone',
      'Run StatefulSets in every zone',
      'Use hostPath on each node',
    ],
    correctAnswer: 1,
    explanation: 'DR strategy: (1) Periodic VolumeSnapshots, (2) Cross-region/zone snapshot replication (cloud-provider specific or Velero), (3) Documented restore procedure to create PVCs from snapshots in the DR zone.',
    tags: ['kubernetes', 'pvcs', 'disaster-recovery', 'statefulset'],
  },
  {
    id: 'k8s-storage-l3-014',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the Secrets Store CSI Driver sync-as-kubernetes-secret feature?',
    options: [
      'It syncs Kubernetes Secrets to external vaults',
      'It creates Kubernetes Secrets from externally stored secrets, making them available as environment variables in addition to volume mounts',
      'It encrypts all CSI volumes with secrets',
      'It syncs CSI driver configuration as secrets',
    ],
    correctAnswer: 1,
    explanation: 'The sync feature creates mirror Kubernetes Secrets from externally stored secrets. This allows using env vars (envFrom) in addition to volume mounts, which is needed when applications read config from environment variables.',
    codeSnippet: 'apiVersion: secrets-store.csi.x-k8s.io/v1\nkind: SecretProviderClass\nmetadata:\n  name: vault-db-creds\nspec:\n  provider: vault\n  secretObjects:    # Sync to K8s Secret\n    - secretName: db-creds\n      type: Opaque\n      data:\n        - objectName: db-password\n          key: password\n  parameters:\n    vaultAddress: "https://vault.example.com"\n    roleName: "db-role"\n    objects: |\n      - objectName: "db-password"\n        secretPath: "secret/data/db"\n        secretKey: "password"',
    tags: ['kubernetes', 'csi', 'secrets-store', 'sync'],
  },
  {
    id: 'k8s-storage-l3-015',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you implement storage cost optimization in a multi-tenant Kubernetes cluster?',
    options: [
      'Use a single expensive StorageClass for everyone',
      'Implement tiered StorageClasses, per-namespace ResourceQuotas, monitoring of unused PVCs, and automated cleanup of Released PVs',
      'Let each team manage their own storage',
      'Use hostPath to avoid cloud storage costs',
    ],
    correctAnswer: 1,
    explanation: 'Cost optimization: tiered classes (teams must justify high-perf), per-namespace quotas, monitoring tools to find unused/over-provisioned PVCs, automated cleanup of Released PVs, and right-sizing recommendations.',
    tags: ['kubernetes', 'storage-classes', 'cost-optimization', 'multi-tenancy'],
  },
  {
    id: 'k8s-storage-l3-016',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'How do you handle volume expansion for a database that cannot tolerate downtime?',
    options: [
      'Expand during maintenance window only',
      'Use a CSI driver that supports online expansion (no Pod restart needed) and verify the driver supports ONLINE expansion capability',
      'Use volume replication and failover',
      'Migrate to a larger volume with zero downtime',
    ],
    correctAnswer: 1,
    explanation: 'Ensure your CSI driver supports online volume expansion (both CONTROLLER_EXPAND and NODE_EXPAND with ONLINE capability). EBS CSI, GCE PD CSI, and Azure Disk CSI all support online expansion.',
    codeSnippet: '# Check if online expansion is supported\nkubectl get csidriver <driver-name> -o yaml\n\n# Expand PVC (online - no Pod restart needed with capable drivers)\nkubectl patch pvc db-data -p \'{"spec":{"resources":{"requests":{"storage":"200Gi"}}}}\'\n\n# Monitor expansion\nkubectl get pvc db-data -o jsonpath=\'{.status.conditions}\' -w',
    tags: ['kubernetes', 'volume-expansion', 'online', 'database'],
  },
  {
    id: 'k8s-storage-l3-017',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is Velero and how does it provide Kubernetes backup and restore?',
    options: [
      'A CSI driver for backups',
      'A backup tool that snapshots PVs and backs up Kubernetes resources (YAML) to object storage, supporting scheduled backups and cross-cluster migration',
      'A PV replication tool',
      'A built-in Kubernetes backup feature',
    ],
    correctAnswer: 1,
    explanation: 'Velero backs up Kubernetes resources to object storage (S3, GCS) and uses CSI VolumeSnapshots or Velero plugins for PV data. It supports scheduled backups, restore, and cluster migration.',
    codeSnippet: '# Create a backup\nvelero backup create daily-backup \\\n  --include-namespaces production \\\n  --snapshot-volumes=true\n\n# Schedule regular backups\nvelero schedule create daily \\\n  --schedule="0 2 * * *" \\\n  --include-namespaces production \\\n  --ttl 168h  # 7 day retention\n\n# Restore from backup\nvelero restore create --from-backup daily-backup',
    tags: ['kubernetes', 'persistent-volumes', 'velero', 'backup'],
  },
  {
    id: 'k8s-storage-l3-018',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'access-modes',
    question: 'What is the RWOP (ReadWriteOncePod) access mode useful for in a database context?',
    options: [
      'Allowing multiple database replicas to share storage',
      'Ensuring only one Pod can mount the volume, preventing data corruption from concurrent write access even on the same node',
      'Providing read-only access to database backups',
      'Optimizing write performance for single Pods',
    ],
    correctAnswer: 1,
    explanation: 'RWOP prevents data corruption by ensuring only a single Pod across the entire cluster can mount the volume. Unlike RWO (which allows multiple Pods on the same node), RWOP provides true exclusive access.',
    tags: ['kubernetes', 'access-modes', 'rwop', 'database'],
  },
  {
    id: 'k8s-storage-l3-019',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How do you troubleshoot a volume that fails to mount with "permission denied" errors?',
    options: [
      'Restart the kubelet',
      'Check fsGroup/securityContext settings, volume file permissions, SELinux labels, and whether the CSI driver supports fsGroup delegation',
      'Reinstall the CSI driver',
      'Change the volume to RWX',
    ],
    correctAnswer: 1,
    explanation: 'Permission issues are often caused by: incorrect fsGroup in securityContext, SELinux labels (use seLinuxMount), or the container running as a non-root user without matching file ownership.',
    codeSnippet: '# Fix with securityContext\nspec:\n  securityContext:\n    fsGroup: 1000        # Changes group ownership of mounted files\n    fsGroupChangePolicy: OnRootMismatch  # Only change if needed\n  containers:\n    - name: app\n      securityContext:\n        runAsUser: 1000\n        runAsGroup: 1000',
    tags: ['kubernetes', 'csi', 'permissions', 'troubleshooting'],
  },
  {
    id: 'k8s-storage-l3-020',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'What is Longhorn and how does it provide distributed storage in Kubernetes?',
    options: [
      'A cloud storage service',
      'A lightweight distributed block storage system built using containers, providing replicated storage with snapshot and backup capabilities',
      'A CNI plugin with storage features',
      'A Kubernetes scheduler for storage',
    ],
    correctAnswer: 1,
    explanation: 'Longhorn is a CNCF incubating project that provides distributed block storage. It replicates data across nodes using containers, supports snapshots, backups to S3, and disaster recovery. Ideal for clusters without cloud-provider storage.',
    tags: ['kubernetes', 'storage-classes', 'longhorn', 'distributed-storage'],
  },
  {
    id: 'k8s-storage-l3-021',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the difference between a VolumeSnapshot sourced from a PVC vs a pre-provisioned VolumeSnapshotContent?',
    options: [
      'There is no difference',
      'PVC-sourced snapshots are dynamically created by the CSI driver; pre-provisioned snapshots reference existing storage snapshots created outside Kubernetes',
      'Pre-provisioned snapshots are faster',
      'PVC-sourced snapshots are limited in size',
    ],
    correctAnswer: 1,
    explanation: 'Dynamic snapshots are created on-demand from PVCs. Pre-provisioned snapshots reference storage snapshots created outside Kubernetes (e.g., via cloud console), using a pre-existing VolumeSnapshotContent.',
    codeSnippet: '# Pre-provisioned VolumeSnapshotContent\napiVersion: snapshot.storage.k8s.io/v1\nkind: VolumeSnapshotContent\nmetadata:\n  name: imported-snap-content\nspec:\n  driver: ebs.csi.aws.com\n  deletionPolicy: Retain\n  source:\n    snapshotHandle: snap-0abc123def456  # Existing AWS snapshot ID\n  volumeSnapshotRef:\n    name: imported-snap\n    namespace: default\n---\napiVersion: snapshot.storage.k8s.io/v1\nkind: VolumeSnapshot\nmetadata:\n  name: imported-snap\nspec:\n  source:\n    volumeSnapshotContentName: imported-snap-content',
    tags: ['kubernetes', 'snapshots', 'pre-provisioned'],
  },
  {
    id: 'k8s-storage-l3-022',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'emptydir',
    question: 'How do you use emptyDir as a build cache in CI/CD pipelines running on Kubernetes?',
    options: [
      'Use persistent storage instead',
      'Use emptyDir to share build artifacts between init containers and the main container, and memory-backed emptyDir for compilation caches',
      'Use ConfigMap volumes for cache',
      'Use hostPath for the build cache',
    ],
    correctAnswer: 1,
    explanation: 'In CI/CD Pods: use emptyDir to share git checkout and build artifacts between pipeline steps (init containers + main container). Memory-backed emptyDir speeds up compilation with fast tmpfs storage.',
    codeSnippet: 'apiVersion: v1\nkind: Pod\nmetadata:\n  name: build-job\nspec:\n  initContainers:\n    - name: checkout\n      image: git:latest\n      command: ["git", "clone", "repo-url", "/workspace"]\n      volumeMounts:\n        - name: workspace\n          mountPath: /workspace\n  containers:\n    - name: build\n      image: maven:latest\n      command: ["mvn", "package", "-f", "/workspace/pom.xml"]\n      volumeMounts:\n        - name: workspace\n          mountPath: /workspace\n        - name: maven-cache\n          mountPath: /root/.m2\n  volumes:\n    - name: workspace\n      emptyDir: {}\n    - name: maven-cache\n      emptyDir:\n        medium: Memory\n        sizeLimit: 512Mi',
    tags: ['kubernetes', 'emptydir', 'ci-cd', 'build-cache'],
  },
  {
    id: 'k8s-storage-l3-023',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'How do you handle PVC deletion protection for critical databases?',
    options: [
      'Set the PVC as immutable',
      'Use a combination of PVC protection finalizers, RBAC restrictions on PVC deletion, and Retain reclaim policy on the PV',
      'Disable PVC deletion in the API server',
      'Use read-only PVCs',
    ],
    correctAnswer: 1,
    explanation: 'Protect critical PVCs by: keeping the built-in pvc-protection finalizer (prevents deletion while in use), restricting PVC delete via RBAC, using Retain reclaim policy, and admission webhooks to prevent deletion of labeled PVCs.',
    tags: ['kubernetes', 'pvcs', 'protection', 'best-practices'],
  },
  {
    id: 'k8s-storage-l3-024',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is CSI topology and how does it affect volume scheduling?',
    options: [
      'The physical topology of the storage array',
      'CSI topology information (zone, region) reported by the driver to ensure volumes are provisioned where they can be accessed by the scheduled Pod',
      'The network topology between CSI components',
      'The replication topology of CSI volumes',
    ],
    correctAnswer: 1,
    explanation: 'CSI drivers report topology information (e.g., topology.ebs.csi.aws.com/zone: us-east-1a) that the scheduler uses to ensure Pods are placed on nodes that can access the provisioned volume.',
    tags: ['kubernetes', 'csi', 'topology', 'scheduling'],
  },
  {
    id: 'k8s-storage-l3-025',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "fsGroupChangePolicy" field and why is it important for large volumes?',
    options: [
      'It changes the filesystem type',
      'It controls whether Kubernetes recursively changes file ownership on mount; "OnRootMismatch" avoids slow recursive chown for large volumes',
      'It changes the group policy for NFS',
      'It modifies the StorageClass group settings',
    ],
    correctAnswer: 1,
    explanation: 'By default, Kubernetes recursively changes ownership of all files in a volume to match fsGroup ("Always" policy). For large volumes with millions of files, this is extremely slow. "OnRootMismatch" only changes ownership if the root directory permissions do not match.',
    codeSnippet: 'spec:\n  securityContext:\n    fsGroup: 1000\n    fsGroupChangePolicy: OnRootMismatch  # Only change if root dir doesn\'t match',
    tags: ['kubernetes', 'persistent-volumes', 'fsgroup', 'performance'],
  },
  {
    id: 'k8s-storage-l3-026',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How does OpenEBS provide storage in Kubernetes?',
    options: [
      'As a cloud-only storage service',
      'As a Container Attached Storage (CAS) solution that runs storage engines as Pods, providing local PV, replicated, and distributed storage options',
      'As a replacement for CSI',
      'As a storage monitoring tool',
    ],
    correctAnswer: 1,
    explanation: 'OpenEBS is a CAS solution where storage controllers and replicas run as Pods. It offers multiple engines: LocalPV (for local storage), Mayastor (NVMe-based replicated), and provides storage on any node using local disks.',
    tags: ['kubernetes', 'storage-classes', 'openebs', 'cas'],
  },
  {
    id: 'k8s-storage-l3-027',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'How do you implement automated volume expansion when a PVC reaches a certain usage threshold?',
    options: [
      'Enable auto-expansion in the StorageClass',
      'Use a custom controller or tools like prometheus-pvc-autoresizer that monitor PVC usage and automatically expand when thresholds are exceeded',
      'Configure kubelet to auto-expand volumes',
      'Use dynamic provisioning with auto-resize',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes does not have built-in auto-expansion. Use tools like prometheus-pvc-autoresizer or write a custom controller that monitors PVC usage (via kubelet metrics) and patches PVCs to increase size when thresholds are reached.',
    tags: ['kubernetes', 'volume-expansion', 'automation', 'monitoring'],
  },
  {
    id: 'k8s-storage-l3-028',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'projected-volumes',
    question: 'How do you detect and handle ConfigMap/Secret updates in mounted volumes without Pod restart?',
    options: [
      'Use a sidecar container that watches for file changes (inotify) and signals the main application',
      'ConfigMap updates always require Pod restart',
      'Use the kubectl rollout restart command',
      'The application automatically detects changes',
    ],
    correctAnswer: 0,
    explanation: 'Use a sidecar with inotify-tools to watch the mounted directory. When files change (kubelet updates ConfigMap volumes), the sidecar can send SIGHUP to the main process or call a reload endpoint.',
    codeSnippet: '# Sidecar that watches for config changes\n- name: config-reloader\n  image: jimmidyson/configmap-reload:latest\n  args:\n    - --volume-dir=/config\n    - --webhook-url=http://localhost:8080/-/reload\n  volumeMounts:\n    - name: config\n      mountPath: /config',
    tags: ['kubernetes', 'projected-volumes', 'configmap', 'reload', 'sidecar'],
  },
  {
    id: 'k8s-storage-l3-029',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is volume populators and how do they extend the PVC data source mechanism?',
    options: [
      'Pods that fill volumes with initial data',
      'A mechanism using dataSourceRef and custom controllers to populate PVCs from any source (databases, URLs, backups) beyond just snapshots and clones',
      'Pre-populating volumes with default data',
      'A CSI driver feature for seeding data',
    ],
    correctAnswer: 1,
    explanation: 'Volume populators (GA in 1.24) extend dataSourceRef to support custom resources. Custom populator controllers can populate PVCs from any source like a database export, S3 bucket, or git repository.',
    tags: ['kubernetes', 'csi', 'volume-populators', 'data-source'],
  },
  {
    id: 'k8s-storage-l3-030',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you manage local PVs at scale across many nodes?',
    options: [
      'Manually create PVs for each node',
      'Use the local-static-provisioner to auto-discover disks, combined with a DaemonSet for disk preparation and topology-aware StorageClasses',
      'Use hostPath instead',
      'Use a single shared NFS volume',
    ],
    correctAnswer: 1,
    explanation: 'At scale: use the sig-storage local-static-provisioner DaemonSet to auto-discover and create PVs for local disks. Combine with device preparation (formatting, mounting) DaemonSets and WaitForFirstConsumer StorageClasses.',
    tags: ['kubernetes', 'persistent-volumes', 'local-pv', 'scale'],
  },
  {
    id: 'k8s-storage-l3-031',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'access-modes',
    question: 'What is the ReadWriteOncePod enforcement mechanism and which CSI features does it require?',
    options: [
      'It is enforced by the API server only',
      'RWOP is enforced by the CSI driver implementing SINGLE_NODE_SINGLE_WRITER volume capability; the API server validates it and kubelet enforces at mount time',
      'It requires a special NetworkPolicy',
      'RWOP is enforced by kube-proxy',
    ],
    correctAnswer: 1,
    explanation: 'RWOP requires: the CSI driver to report SINGLE_NODE_SINGLE_WRITER capability, the API server to validate the access mode, and kubelet to enforce single-Pod access during mount operations.',
    tags: ['kubernetes', 'access-modes', 'rwop', 'csi', 'enforcement'],
  },
  {
    id: 'k8s-storage-l3-032',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How do you perform cross-namespace restore from a VolumeSnapshot?',
    options: [
      'Reference the snapshot from another namespace directly',
      'VolumeSnapshots are namespaced; use VolumeSnapshotContent (cluster-scoped) to create a new VolumeSnapshot in the target namespace referencing the same snapshot content',
      'Copy the snapshot to another namespace',
      'Export and import the snapshot',
    ],
    correctAnswer: 1,
    explanation: 'Since VolumeSnapshots are namespaced, create a new VolumeSnapshot in the target namespace that references the same VolumeSnapshotContent (which is cluster-scoped) from the original snapshot.',
    tags: ['kubernetes', 'snapshots', 'cross-namespace', 'restore'],
  },
  {
    id: 'k8s-storage-l3-033',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "fsGroupPolicy" in the CSIDriver resource?',
    options: [
      'The filesystem group ID to use',
      'Controls whether the CSI driver supports fsGroup-based ownership changes and whether kubelet or the driver handles them',
      'The policy for file group permissions',
      'The group of files to include in snapshots',
    ],
    correctAnswer: 1,
    explanation: 'fsGroupPolicy controls fsGroup behavior: "ReadWriteOnceWithFSType" (default, kubelet applies fsGroup for RWO+filesystem), "File" (always apply), or "None" (never apply, driver handles it). This affects mount performance for large volumes.',
    tags: ['kubernetes', 'csi', 'fsgroup-policy'],
  },
  {
    id: 'k8s-storage-l3-034',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you implement a Kubernetes storage solution for a bare-metal cluster without cloud storage?',
    options: [
      'Use hostPath for everything',
      'Deploy a distributed storage solution like Rook/Ceph, Longhorn, or OpenEBS that uses local node disks to provide replicated block and file storage',
      'Use NFS only',
      'Attach external SAN storage to each node',
    ],
    correctAnswer: 1,
    explanation: 'For bare-metal: deploy Rook/Ceph (enterprise-grade, complex), Longhorn (simpler, CNCF), or OpenEBS (CAS). These aggregate local disks across nodes into replicated, highly available storage pools with CSI integration.',
    tags: ['kubernetes', 'storage-classes', 'bare-metal', 'distributed-storage'],
  },
  {
    id: 'k8s-storage-l3-035',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you handle the "zone mismatch" problem where a PV is in a different zone than the scheduled Pod?',
    options: [
      'Manually move the PV to the correct zone',
      'Use WaitForFirstConsumer binding mode to ensure PVs are provisioned in the same zone as the Pod, and use pod topology spread constraints',
      'Always use multi-zone volumes',
      'Ignore the zone constraint',
    ],
    correctAnswer: 1,
    explanation: 'Use WaitForFirstConsumer to delay PV provisioning until Pod scheduling determines the zone. For existing PVs, use nodeAffinity to match PV zones. Use pod topology spread constraints to control Pod placement.',
    tags: ['kubernetes', 'persistent-volumes', 'zone-mismatch', 'topology'],
  },
  {
    id: 'k8s-storage-l3-036',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'ephemeral-volumes',
    question: 'When would you use a generic ephemeral volume instead of emptyDir?',
    options: [
      'They serve the same purpose',
      'When you need dynamic provisioning features (specific StorageClass, guaranteed IOPS) with ephemeral lifecycle, or when you need more storage than the node local disk can provide',
      'When you need memory-backed storage',
      'When you need the fastest possible performance',
    ],
    correctAnswer: 1,
    explanation: 'Generic ephemeral volumes offer StorageClass-backed storage with Pod lifecycle. Use when you need: specific storage type (SSD), guaranteed performance, more capacity than node disk, or CSI features like encryption.',
    tags: ['kubernetes', 'ephemeral-volumes', 'generic', 'use-cases'],
  },
  {
    id: 'k8s-storage-l3-037',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'A PVC expansion has been stuck in "Resizing" state for a long time. How do you troubleshoot?',
    options: [
      'Delete and recreate the PVC',
      'Check the CSI controller logs for expansion errors, verify cloud provider quotas, check if the volume is in a valid state for expansion, and check events on the PVC',
      'Restart the kubelet',
      'Wait longer',
    ],
    correctAnswer: 1,
    explanation: 'Check: CSI controller logs for errors, cloud provider limits (max IOPS changes per hour for EBS), volume state (must not be in error/modifying state), PVC events for detailed error messages.',
    codeSnippet: '# Check PVC events\nkubectl describe pvc <name>\n\n# Check CSI controller logs\nkubectl -n kube-system logs -l app=ebs-csi-controller -c csi-resizer\n\n# Check cloud-level volume status (AWS example)\naws ec2 describe-volumes-modifications --volume-id <vol-id>',
    tags: ['kubernetes', 'volume-expansion', 'troubleshooting', 'stuck'],
  },
  {
    id: 'k8s-storage-l3-038',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'How does PVC data population work when creating a PVC from a VolumeSnapshot vs cloning?',
    options: [
      'Both create identical copies instantly',
      'Snapshot restore creates a new volume from the snapshot data (may involve copy); cloning uses CSI clone which can use copy-on-write for instant copies without full data copy',
      'Cloning is always slower',
      'Snapshot restore requires more storage',
    ],
    correctAnswer: 1,
    explanation: 'CSI cloning can use storage-level copy-on-write (if supported) for near-instant clones with no data copy overhead. Snapshot restore creates a new volume from snapshot data, which may involve a full copy depending on the storage backend.',
    tags: ['kubernetes', 'pvcs', 'cloning', 'snapshots', 'comparison'],
  },
  {
    id: 'k8s-storage-l3-039',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the difference between CSI driver "Persistent" and "Ephemeral" volumeLifecycleModes?',
    options: [
      'Persistent is for SSDs, Ephemeral is for HDDs',
      'Persistent volumes go through PV/PVC binding; Ephemeral volumes are specified inline in Pod spec and have Pod lifecycle',
      'Persistent is for production; Ephemeral is for testing',
      'They differ in performance characteristics',
    ],
    correctAnswer: 1,
    explanation: 'Persistent mode uses the standard PV/PVC workflow. Ephemeral mode allows inline volume specification in Pod spec (CSI ephemeral volumes), where the volume lifecycle matches the Pod. A driver can support both modes.',
    tags: ['kubernetes', 'csi', 'volume-lifecycle-modes'],
  },
  {
    id: 'k8s-storage-l3-040',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you configure storage class parameters for different IOPS tiers on AWS EBS?',
    options: [
      'Set IOPS on the PVC',
      'Create separate StorageClasses with different iops and throughput parameters for each tier',
      'Configure IOPS in the kubelet',
      'Set IOPS on the PV after creation',
    ],
    correctAnswer: 1,
    explanation: 'Create dedicated StorageClasses for each IOPS tier. For EBS CSI, parameters include type (gp3, io2), iops, and throughput to define different performance characteristics.',
    codeSnippet: '# Standard: gp3 default IOPS\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: standard\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n---\n# High IOPS: io2 with 10000 IOPS\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: high-iops\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: io2\n  iops: "10000"\n---\n# High throughput: gp3 with max throughput\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: high-throughput\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n  iops: "16000"\n  throughput: "1000"',
    tags: ['kubernetes', 'storage-classes', 'aws', 'iops', 'performance'],
  },
  {
    id: 'k8s-storage-l3-041',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'hostpath',
    question: 'What is the security implication of using hostPath with PodSecurityAdmission and how do you enforce restrictions?',
    options: [
      'hostPath has no security implications',
      'hostPath allows container escape to the host filesystem; enforce restrictions using the "restricted" Pod Security Standard which disallows hostPath volumes',
      'hostPath is safe when used with read-only mounts',
      'Only privileged containers can use hostPath',
    ],
    correctAnswer: 1,
    explanation: 'hostPath can expose sensitive host files to containers, enabling privilege escalation. The "restricted" Pod Security Standard (PodSecurityAdmission) disallows hostPath. Use admission controllers to enforce this.',
    codeSnippet: '# Enforce restricted PSS on a namespace\nkubectl label namespace production \\\n  pod-security.kubernetes.io/enforce=restricted \\\n  pod-security.kubernetes.io/audit=restricted \\\n  pod-security.kubernetes.io/warn=restricted',
    tags: ['kubernetes', 'hostpath', 'security', 'pod-security-admission'],
  },
  {
    id: 'k8s-storage-l3-042',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you implement read-only shared storage for application deployments (e.g., shared static content)?',
    options: [
      'Use ConfigMaps for all static content',
      'Use a PVC with ROX access mode, populate it via a Job, then mount it read-only across multiple Deployment Pods',
      'Use emptyDir with init containers',
      'Bake content into container images',
    ],
    correctAnswer: 1,
    explanation: 'Create a PVC with ROX (or RWX initially), populate it using a Job or init container, then mount it as readOnly across Pods. This is efficient for shared static content like ML models or reference data.',
    tags: ['kubernetes', 'persistent-volumes', 'rox', 'shared-content'],
  },
  {
    id: 'k8s-storage-l3-043',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What are the consistency considerations when taking a VolumeSnapshot of a running database?',
    options: [
      'Snapshots are always consistent',
      'Application-level consistency (flushing buffers, WAL) must be ensured before taking the snapshot; otherwise the snapshot may contain a crash-consistent but not application-consistent state',
      'The CSI driver ensures application consistency',
      'Consistency is not relevant for snapshots',
    ],
    correctAnswer: 1,
    explanation: 'VolumeSnapshots are crash-consistent by default. For application consistency, you must flush database buffers, create a checkpoint, or use application hooks (preSnapshot) before taking the snapshot. Some CSI drivers support app-aware snapshots.',
    tags: ['kubernetes', 'snapshots', 'consistency', 'database'],
  },
  {
    id: 'k8s-storage-l3-044',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the SELinuxMount feature in CSI and why does it improve mount performance?',
    options: [
      'It disables SELinux for CSI volumes',
      'It allows the CSI driver to mount volumes with the correct SELinux label, avoiding the expensive recursive relabeling of all files by the container runtime',
      'It encrypts volumes using SELinux',
      'It applies SELinux policies to the CSI driver',
    ],
    correctAnswer: 1,
    explanation: 'Without SELinuxMount, the container runtime must recursively relabel all files in the volume to match the container SELinux context. With this feature, the CSI driver mounts with the correct context directly, dramatically improving mount speed for large volumes.',
    tags: ['kubernetes', 'csi', 'selinux', 'performance'],
  },
  {
    id: 'k8s-storage-l3-045',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'dynamic-provisioning',
    question: 'How do you implement storage quotas per team in a multi-tenant cluster?',
    options: [
      'Set limits in the StorageClass',
      'Use ResourceQuota per namespace to limit total storage, PVC count, and per-StorageClass storage',
      'Use LimitRange for storage',
      'Create separate clusters for each team',
    ],
    correctAnswer: 1,
    explanation: 'Use ResourceQuota to limit: total PVC storage (requests.storage), PVC count (persistentvolumeclaims), and per-StorageClass limits (<classname>.storageclass.storage.k8s.io/requests.storage).',
    tags: ['kubernetes', 'dynamic-provisioning', 'resource-quota', 'multi-tenancy'],
  },
  {
    id: 'k8s-storage-l3-046',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'What is the impact of choosing ext4 vs xfs filesystem type for Kubernetes volumes?',
    options: [
      'No practical difference',
      'ext4 is more stable and widely supported; xfs handles large files and volumes better, supports online growing, and has better parallel I/O but uses more memory',
      'ext4 is newer than xfs',
      'xfs does not support Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'ext4: more mature, better for small files, lower memory overhead. xfs: better for large files and high I/O, supports online grow but not shrink, better parallel I/O, default on RHEL. Choose based on workload characteristics.',
    tags: ['kubernetes', 'storage-classes', 'filesystem', 'ext4', 'xfs'],
  },
  {
    id: 'k8s-storage-l3-047',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you monitor PersistentVolume usage and health in a cluster?',
    options: [
      'Check each volume manually',
      'Use kubelet metrics (kubelet_volume_stats_*), Prometheus monitoring, and CSI health monitoring to track usage, latency, and errors',
      'Use kubectl describe pv',
      'Check cloud provider console',
    ],
    correctAnswer: 1,
    explanation: 'Kubelet exposes volume metrics: kubelet_volume_stats_used_bytes, kubelet_volume_stats_capacity_bytes, kubelet_volume_stats_available_bytes. Set up Prometheus alerts for high usage and CSI health monitoring for error detection.',
    codeSnippet: '# Prometheus alert for PVC nearly full\ngroups:\n  - name: storage\n    rules:\n      - alert: PVCAlmostFull\n        expr: |\n          kubelet_volume_stats_available_bytes /\n          kubelet_volume_stats_capacity_bytes < 0.1\n        for: 5m\n        labels:\n          severity: warning\n        annotations:\n          summary: "PVC {{ $labels.persistentvolumeclaim }} is >90% full"',
    tags: ['kubernetes', 'persistent-volumes', 'monitoring', 'prometheus'],
  },
  {
    id: 'k8s-storage-l3-048',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What are the limitations of volume expansion for different storage backends?',
    options: [
      'All backends support unlimited expansion',
      'Limitations include: AWS EBS max one modify per 6 hours, some backends require Pod restart, block mode volumes may need application-level resize, shrinking is never supported',
      'Only size limit matters',
      'Expansion is instant for all backends',
    ],
    correctAnswer: 1,
    explanation: 'Key limitations: AWS EBS allows only one modification per 6 hours, some CSI drivers require offline expansion (Pod delete), block-mode volumes need application-level resize, and shrinking is never supported by Kubernetes.',
    tags: ['kubernetes', 'volume-expansion', 'limitations'],
  },
  {
    id: 'k8s-storage-l3-049',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "token request" feature and when would you use it?',
    options: [
      'Requesting API tokens for the CSI driver',
      'The CSI driver receives a service account token scoped to the Pod during NodePublishVolume, enabling per-Pod authentication with external secret stores',
      'Requesting access tokens for volume encryption',
      'Token-based authentication between CSI components',
    ],
    correctAnswer: 1,
    explanation: 'CSI token requests (tokenRequests in CSIDriver spec) provide the Pod service account token to the CSI driver during mount. This enables the Secrets Store CSI Driver to authenticate to Vault/AWS using the Pod identity.',
    tags: ['kubernetes', 'csi', 'token-requests', 'authentication'],
  },
  {
    id: 'k8s-storage-l3-050',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'emptydir',
    question: 'What is the relationship between emptyDir sizeLimit and node ephemeral-storage eviction?',
    options: [
      'They are unrelated',
      'If an emptyDir exceeds sizeLimit or total ephemeral-storage (all emptyDirs + logs + writable layers) exceeds the node threshold, the Pod is evicted',
      'sizeLimit only applies to memory-backed emptyDir',
      'Node eviction only considers PVC storage',
    ],
    correctAnswer: 1,
    explanation: 'Kubelet monitors both per-volume sizeLimit and total node ephemeral-storage. Pods exceeding either limit are evicted. The node-level threshold protects the node from disk pressure.',
    tags: ['kubernetes', 'emptydir', 'eviction', 'ephemeral-storage'],
  },
  {
    id: 'k8s-storage-l3-051',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'How does the PVC binding algorithm match PVCs to PVs?',
    options: [
      'First-come, first-served',
      'The controller finds the smallest PV that satisfies access modes, capacity, StorageClass, and label selector requirements',
      'Random selection',
      'Always selects the largest available PV',
    ],
    correctAnswer: 1,
    explanation: 'The PV controller finds PVs matching: access modes (exact match), capacity (>= request), StorageClass (exact match), and selector (if specified). Among matches, it selects the smallest PV to minimize waste.',
    tags: ['kubernetes', 'pvcs', 'binding', 'algorithm'],
  },
  {
    id: 'k8s-storage-l3-052',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you configure a StorageClass for GCP Filestore (NFS)?',
    options: [
      'Use the pd.csi.storage.gke.io provisioner with NFS parameters',
      'Use the filestore.csi.storage.gke.io provisioner with tier and network parameters',
      'Use the standard NFS provisioner',
      'Filestore does not support StorageClasses',
    ],
    correctAnswer: 1,
    explanation: 'GCP Filestore uses its own CSI driver (filestore.csi.storage.gke.io) with parameters for tier (BASIC_HDD, BASIC_SSD, ENTERPRISE), network, and reserved IP ranges.',
    codeSnippet: 'apiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: filestore-sc\nprovisioner: filestore.csi.storage.gke.io\nparameters:\n  tier: enterprise\n  network: default\nvolumeBindingMode: WaitForFirstConsumer\nallowVolumeExpansion: true',
    tags: ['kubernetes', 'storage-classes', 'gcp', 'filestore'],
  },
  {
    id: 'k8s-storage-l3-053',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "volume attachment limit" and how does it affect Pod scheduling?',
    options: [
      'The maximum number of PVCs per Pod',
      'Each node has a limit on attached volumes (e.g., AWS EBS: 39 per node); the scheduler respects this to avoid scheduling failures',
      'The maximum PV size',
      'The limit on PV creation rate',
    ],
    correctAnswer: 1,
    explanation: 'Cloud providers limit attached volumes per node (AWS: 25-39 depending on instance type, GCP: 128). The kube-scheduler CSIMaxVolumeLimitChecker plugin checks this to avoid scheduling Pods to nodes that have reached the limit.',
    tags: ['kubernetes', 'persistent-volumes', 'volume-limits', 'scheduling'],
  },
  {
    id: 'k8s-storage-l3-054',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How do you implement a data pipeline that uses VolumeSnapshots for consistent data processing?',
    options: [
      'Process data directly on the production volume',
      'Snapshot the source PVC, create a new PVC from the snapshot, run the processing job on the cloned data, and clean up afterwards',
      'Copy data using kubectl cp',
      'Use emptyDir for the pipeline',
    ],
    correctAnswer: 1,
    explanation: 'Snapshot the source PVC for point-in-time consistency, restore to a new PVC, run analytics/ETL jobs against the restored data (without impacting production), and clean up the temporary PVC when done.',
    tags: ['kubernetes', 'snapshots', 'data-pipeline', 'processing'],
  },
  {
    id: 'k8s-storage-l3-055',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI driver handle volume staging and publishing for Pods with multiple containers?',
    options: [
      'Each container gets its own volume',
      'NodeStageVolume is called once per volume per node; NodePublishVolume is called once per Pod, and all containers in the Pod share the same mount',
      'Staging happens per container',
      'Publishing happens once per node',
    ],
    correctAnswer: 1,
    explanation: 'NodeStageVolume prepares the volume once per node (global mount). NodePublishVolume creates a bind mount to the Pod directory. All containers in the Pod access the same mount point (same filesystem).',
    tags: ['kubernetes', 'csi', 'staging', 'publishing', 'multi-container'],
  },
  {
    id: 'k8s-storage-l3-056',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'dynamic-provisioning',
    question: 'What is CSIStorageCapacity and how does it improve scheduling?',
    options: [
      'It reports the total cluster storage',
      'It publishes available storage capacity per node/zone, allowing the scheduler to avoid placing Pods where there is insufficient storage capacity',
      'It limits the maximum capacity of CSI volumes',
      'It tracks storage usage per namespace',
    ],
    correctAnswer: 1,
    explanation: 'CSIStorageCapacity objects are created by CSI drivers to report available storage per topology segment. The scheduler uses this to avoid scheduling Pods to nodes/zones where provisioning would fail due to insufficient capacity.',
    codeSnippet: '# Example CSIStorageCapacity (auto-created by CSI driver)\napiVersion: storage.k8s.io/v1\nkind: CSIStorageCapacity\nmetadata:\n  name: csisc-xyz\n  namespace: kube-system\nstorageClassName: fast-ssd\nnodeTopology:\n  matchLabels:\n    topology.ebs.csi.aws.com/zone: us-east-1a\ncapacity: 500Gi',
    tags: ['kubernetes', 'dynamic-provisioning', 'csi-storage-capacity', 'scheduling'],
  },
  {
    id: 'k8s-storage-l3-057',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'projected-volumes',
    question: 'How do you implement secret rotation for mounted Secrets without Pod restart?',
    options: [
      'Use the Secrets Store CSI Driver with rotation enabled',
      'Both approaches work: (1) Use the Secrets Store CSI Driver with enableSecretRotation, or (2) use standard Secret volumes (without subPath) which auto-update, combined with an application reload mechanism',
      'Secrets cannot be rotated without Pod restart',
      'Use environment variables instead of volume mounts',
    ],
    correctAnswer: 1,
    explanation: 'Two approaches: (1) Secrets Store CSI Driver supports rotation from external vaults. (2) Standard Secret volumes auto-update (except subPath mounts). Both require the application to detect and reload the updated files.',
    tags: ['kubernetes', 'projected-volumes', 'secrets', 'rotation'],
  },
  {
    id: 'k8s-storage-l3-058',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you migrate from in-tree volume plugins to CSI drivers?',
    options: [
      'Replace all PVCs and recreate volumes',
      'Use the CSIMigration feature that transparently routes in-tree volume operations to the corresponding CSI driver without requiring PV/PVC changes',
      'Manually move data to new CSI-backed volumes',
      'In-tree plugins cannot be migrated',
    ],
    correctAnswer: 1,
    explanation: 'CSIMigration is a Kubernetes feature that translates in-tree volume plugin operations to CSI driver calls. It is transparent to existing PVs and PVCs. Most major cloud provider migrations are now GA.',
    tags: ['kubernetes', 'persistent-volumes', 'csi-migration'],
  },
  {
    id: 'k8s-storage-l3-059',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you implement storage encryption at rest for Kubernetes volumes?',
    options: [
      'Use Kubernetes Secrets for encryption keys',
      'Configure encryption in the StorageClass parameters (e.g., encrypted: true for EBS), use KMS key references, and verify the CSI driver supports encryption',
      'Encrypt data at the application level only',
      'Enable etcd encryption',
    ],
    correctAnswer: 1,
    explanation: 'Most cloud CSI drivers support encryption via StorageClass parameters. Specify encrypted: true and optionally a KMS key. For on-prem, use storage backend encryption or dm-crypt/LUKS with appropriate CSI configuration.',
    tags: ['kubernetes', 'storage-classes', 'encryption', 'security'],
  },
  {
    id: 'k8s-storage-l3-060',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the VolumeAttributesClass resource and how does it enable dynamic volume modification?',
    options: [
      'A class for defining volume labels',
      'A resource that defines modifiable volume attributes (IOPS, throughput) that can be applied to existing PVCs, enabling performance changes without recreation',
      'A class for volume encryption attributes',
      'A classification of volume health attributes',
    ],
    correctAnswer: 1,
    explanation: 'VolumeAttributesClass (beta in 1.31) defines volume performance attributes (IOPS, throughput). By changing the volumeAttributesClassName on a PVC, you can dynamically modify volume performance without recreating the volume.',
    codeSnippet: 'apiVersion: storage.k8s.io/v1beta1\nkind: VolumeAttributesClass\nmetadata:\n  name: high-perf\ndriverName: ebs.csi.aws.com\nparameters:\n  iops: "10000"\n  throughput: "500"\n---\n# Modify PVC to use new performance tier\nkubectl patch pvc my-data \\\n  -p \'{"spec":{"volumeAttributesClassName":"high-perf"}}\'',
    tags: ['kubernetes', 'volume-expansion', 'volume-attributes-class', 'performance'],
  },
  {
    id: 'k8s-storage-l3-061',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What are CSI volume limits and how do they affect cluster capacity planning?',
    options: [
      'The maximum volume size',
      'Per-node limits on the number of volumes a CSI driver can attach; these limits must be factored into cluster sizing and scheduling decisions',
      'The maximum number of CSI drivers per cluster',
      'Rate limits on volume operations',
    ],
    correctAnswer: 1,
    explanation: 'CSI drivers report per-node volume limits (e.g., EBS: 39 per instance type). The scheduler uses these to prevent over-scheduling. Capacity planning must account for these limits when sizing nodes and planning Pod density.',
    tags: ['kubernetes', 'csi', 'volume-limits', 'capacity-planning'],
  },
  {
    id: 'k8s-storage-l3-062',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'How do you implement cross-region backup using VolumeSnapshots?',
    options: [
      'VolumeSnapshots support cross-region natively',
      'Take a VolumeSnapshot, then use cloud-provider tools to copy the underlying snapshot to another region (e.g., AWS copy-snapshot), or use Velero for cross-region backup',
      'Create PVCs in multiple regions simultaneously',
      'Use NFS replication',
    ],
    correctAnswer: 1,
    explanation: 'VolumeSnapshots are region-local. For cross-region: take a snapshot, use cloud APIs to copy the underlying snapshot (e.g., aws ec2 copy-snapshot), or use Velero with a cross-region backup location (S3 with cross-region replication).',
    tags: ['kubernetes', 'snapshots', 'cross-region', 'backup'],
  },
  {
    id: 'k8s-storage-l3-063',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What are the considerations for running databases on Kubernetes with persistent storage?',
    options: [
      'Databases should not run on Kubernetes',
      'Use StatefulSets with dedicated StorageClasses, RWOP access mode, appropriate fsGroup, Pod disruption budgets, and robust backup/restore procedures',
      'Use Deployments with PVCs',
      'Use emptyDir for database storage',
    ],
    correctAnswer: 1,
    explanation: 'Database considerations: StatefulSets for stable identity/storage, dedicated high-IOPS StorageClasses, RWOP for exclusive access, PodDisruptionBudgets for controlled disruption, regular snapshots, and tested restore procedures.',
    tags: ['kubernetes', 'persistent-volumes', 'database', 'best-practices'],
  },
  {
    id: 'k8s-storage-l3-064',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'access-modes',
    question: 'How do you implement a shared cache layer using RWX volumes across a Deployment?',
    options: [
      'Use individual PVCs per Pod',
      'Create a single PVC with RWX access mode backed by NFS/EFS/Azure Files, and mount it in all Pods of the Deployment',
      'Use emptyDir on each Pod',
      'Use a Redis cache instead',
    ],
    correctAnswer: 1,
    explanation: 'Create a single RWX PVC (backed by NFS, EFS, or similar) and reference it in the Deployment Pod template. All replicas mount the same volume, sharing a common cache directory.',
    codeSnippet: 'apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web-app\nspec:\n  replicas: 5\n  template:\n    spec:\n      containers:\n        - name: app\n          volumeMounts:\n            - name: shared-cache\n              mountPath: /cache\n      volumes:\n        - name: shared-cache\n          persistentVolumeClaim:\n            claimName: shared-cache-pvc  # RWX PVC',
    tags: ['kubernetes', 'access-modes', 'rwx', 'shared-cache'],
  },
  {
    id: 'k8s-storage-l3-065',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'ephemeral-volumes',
    question: 'What is the impact of generic ephemeral volumes on the PVC count in a namespace with ResourceQuota?',
    options: [
      'Ephemeral PVCs are not counted',
      'Each Pod with a generic ephemeral volume creates a PVC that counts toward the namespace ResourceQuota for PVCs and storage',
      'Ephemeral PVCs have a separate quota',
      'Only manually created PVCs are counted',
    ],
    correctAnswer: 1,
    explanation: 'Generic ephemeral volumes create real PVCs that count against ResourceQuota. In a Deployment with 10 replicas, 10 PVCs are created. This must be factored into quota planning.',
    tags: ['kubernetes', 'ephemeral-volumes', 'resource-quota', 'planning'],
  },
  {
    id: 'k8s-storage-l3-066',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How do you implement volume encryption with customer-managed keys (CMK)?',
    options: [
      'Encrypt the data in the application',
      'Reference the KMS key ID in the StorageClass parameters, and ensure the CSI driver service account has permissions to use the key',
      'Store encryption keys in Kubernetes Secrets',
      'Use etcd encryption for volume data',
    ],
    correctAnswer: 1,
    explanation: 'Specify the KMS key (ARN for AWS, resource ID for GCP/Azure) in StorageClass parameters. Ensure the node IAM role or CSI driver service account has permissions to use the KMS key for encrypt/decrypt operations.',
    codeSnippet: '# AWS EBS with customer-managed KMS key\napiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: encrypted-cmk\nprovisioner: ebs.csi.aws.com\nparameters:\n  type: gp3\n  encrypted: "true"\n  kmsKeyId: "arn:aws:kms:us-east-1:123456789:key/mrk-abc"',
    tags: ['kubernetes', 'csi', 'encryption', 'kms', 'cmk'],
  },
  {
    id: 'k8s-storage-l3-067',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you implement storage class selection policies using admission controllers?',
    options: [
      'Set the StorageClass in each PVC manually',
      'Use OPA Gatekeeper or Kyverno to enforce policies like: only specific namespaces can use expensive StorageClasses, PVCs must have a specific label, or default StorageClass mutation',
      'Restrict StorageClasses using RBAC',
      'Use LimitRange for StorageClass restrictions',
    ],
    correctAnswer: 1,
    explanation: 'Use policy engines (Gatekeeper, Kyverno) to: validate PVC StorageClass against allowed classes per namespace, mutate PVCs to set default classes based on namespace labels, and enforce storage governance.',
    codeSnippet: '# Kyverno policy: restrict StorageClass per namespace\napiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n  name: restrict-storage-class\nspec:\n  rules:\n    - name: check-storage-class\n      match:\n        resources:\n          kinds: [PersistentVolumeClaim]\n          namespaces: ["team-*"]\n      validate:\n        message: "Only \'standard\' StorageClass is allowed"\n        pattern:\n          spec:\n            storageClassName: "standard"',
    tags: ['kubernetes', 'storage-classes', 'admission-controllers', 'policy'],
  },
  {
    id: 'k8s-storage-l3-068',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'How do you handle orphaned PVCs in a cluster (PVCs not used by any Pod)?',
    options: [
      'Kubernetes auto-deletes orphaned PVCs',
      'Use monitoring tools to identify PVCs not mounted by any Pod, review them, and implement cleanup procedures with appropriate grace periods',
      'Delete all PVCs older than 30 days',
      'Orphaned PVCs are automatically reclaimed',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes does not auto-clean orphaned PVCs. Use tools like kubectl-unused-volumes or custom scripts that check PVC usage against running Pods. Implement alerts for long-unused PVCs and review before deletion.',
    codeSnippet: '# Find PVCs not mounted by any Pod\nkubectl get pvc --all-namespaces -o json | jq -r \'\n  .items[] |\n  select(.metadata.name as $name |\n    [env.pods[] | select(.spec.volumes[]?.persistentVolumeClaim?.claimName == $name)] | length == 0\n  ) | "\\(.metadata.namespace)/\\(.metadata.name)"\'',
    tags: ['kubernetes', 'pvcs', 'cleanup', 'orphaned'],
  },
  {
    id: 'k8s-storage-l3-069',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'How do you automate PVC expansion based on usage metrics?',
    options: [
      'Enable auto-expansion in Kubernetes',
      'Deploy a custom controller like pvc-autoresizer that watches kubelet volume metrics and automatically patches PVCs when usage exceeds a threshold',
      'Use HPA for storage',
      'Configure kubelet to auto-expand',
    ],
    correctAnswer: 1,
    explanation: 'Tools like prometheus-pvc-autoresizer watch kubelet_volume_stats metrics. When usage exceeds a threshold (e.g., 80%), they patch the PVC to increase storage by a configurable amount. This prevents storage-related outages.',
    tags: ['kubernetes', 'volume-expansion', 'automation', 'pvc-autoresizer'],
  },
  {
    id: 'k8s-storage-l3-070',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the "ReadWriteOncePod" scheduling implication for StatefulSets?',
    options: [
      'No scheduling impact',
      'With RWOP, if a Pod fails and the volume is still attached, the replacement Pod must wait for volume detachment before it can mount, potentially increasing failover time',
      'RWOP prevents StatefulSet scaling',
      'RWOP requires manual volume detachment',
    ],
    correctAnswer: 1,
    explanation: 'RWOP ensures only one Pod can mount the volume. During failover, the new Pod must wait for the old Pod to fully terminate and the volume to be detached/unmounted. This can increase failover time compared to RWO.',
    tags: ['kubernetes', 'persistent-volumes', 'rwop', 'statefulset', 'failover'],
  },
  {
    id: 'k8s-storage-l3-071',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How does the CSI driver handle volume group snapshots (Kubernetes 1.27+ alpha)?',
    options: [
      'By taking individual snapshots sequentially',
      'VolumeGroupSnapshot creates a consistent snapshot of multiple volumes simultaneously, ensuring cross-volume consistency for applications using multiple PVCs',
      'By pausing all Pods using the volumes',
      'Group snapshots are not supported in CSI',
    ],
    correctAnswer: 1,
    explanation: 'VolumeGroupSnapshot (alpha) enables taking a crash-consistent snapshot of multiple volumes atomically. This is critical for applications like databases with data and log volumes that must be consistent with each other.',
    tags: ['kubernetes', 'csi', 'group-snapshots', 'consistency'],
  },
  {
    id: 'k8s-storage-l3-072',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What is the storage overhead of VolumeSnapshots and how do you manage it?',
    options: [
      'Snapshots use no additional storage',
      'Snapshots use copy-on-write storage that grows as the source data changes; manage overhead with retention policies, monitoring snapshot sizes, and cleaning up old snapshots',
      'Each snapshot uses the same amount of storage as the source',
      'Snapshots are compressed and use minimal storage',
    ],
    correctAnswer: 1,
    explanation: 'Most storage backends use copy-on-write for snapshots. Initial overhead is minimal, but it grows as the source volume data diverges. Monitor snapshot storage, set retention policies, and regularly clean up unneeded snapshots.',
    tags: ['kubernetes', 'snapshots', 'storage-overhead', 'management'],
  },
  {
    id: 'k8s-storage-l3-073',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'dynamic-provisioning',
    question: 'What is the cross-namespace data source feature for PVCs?',
    options: [
      'Sharing PVCs across namespaces',
      'The ability to create PVCs with dataSourceRef referencing snapshots or PVCs from a different namespace, with ReferenceGrant for authorization',
      'Cross-namespace quota sharing',
      'Replicating data across namespaces',
    ],
    correctAnswer: 1,
    explanation: 'Cross-namespace data sources (alpha) allow PVCs to reference VolumeSnapshots or PVCs from other namespaces using dataSourceRef. ReferenceGrant controls which namespaces can reference what, ensuring security.',
    tags: ['kubernetes', 'dynamic-provisioning', 'cross-namespace', 'data-source'],
  },
  {
    id: 'k8s-storage-l3-074',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you implement multi-cloud storage abstraction in Kubernetes?',
    options: [
      'Use the same CSI driver across all clouds',
      'Create StorageClasses with the same name but different provisioners per cloud, use infrastructure-as-code to deploy cloud-specific classes, and reference abstract class names in PVCs',
      'Use NFS for all clouds',
      'Use cloud-agnostic storage only',
    ],
    correctAnswer: 1,
    explanation: 'Create identically named StorageClasses across clouds (e.g., "fast-ssd") with cloud-specific provisioners. Application PVCs reference the abstract name. Use Terraform/Crossplane to deploy the correct StorageClass per environment.',
    tags: ['kubernetes', 'storage-classes', 'multi-cloud', 'abstraction'],
  },
  {
    id: 'k8s-storage-l3-075',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What happens when a node with a local PV is permanently lost?',
    options: [
      'The PV automatically moves to another node',
      'The PV and its data are lost; you must delete the PV, the corresponding PVC remains bound to a non-existent PV, and a new volume must be provisioned from backup',
      'Kubernetes migrates the data automatically',
      'The PV enters Released state',
    ],
    correctAnswer: 1,
    explanation: 'Local PVs are tied to specific nodes. If a node is permanently lost, the data is unrecoverable. You must: delete the PV (and optionally PVC), provision new storage, and restore from backup. This is why backups are critical for local PVs.',
    tags: ['kubernetes', 'persistent-volumes', 'local-pv', 'node-failure'],
  },
  {
    id: 'k8s-storage-l3-076',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the CSI "read-write once pod" volume capability and how is it enforced?',
    options: [
      'A software lock in the CSI driver',
      'The SINGLE_NODE_SINGLE_WRITER access mode in CSI, enforced at both API level (admission) and node level (kubelet rejects mount if another Pod is using it)',
      'A Pod-level mutex',
      'A file lock on the volume',
    ],
    correctAnswer: 1,
    explanation: 'RWOP maps to CSI SINGLE_NODE_SINGLE_WRITER. The API server validates the access mode on PVC creation. The kubelet enforces it during mount by checking if another Pod already has the volume mounted.',
    tags: ['kubernetes', 'csi', 'rwop', 'enforcement'],
  },
  {
    id: 'k8s-storage-l3-077',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'emptydir',
    question: 'How do you use emptyDir for a sidecar pattern where a proxy and the main app share a Unix socket?',
    options: [
      'Use a network connection instead',
      'Mount an emptyDir volume to both containers and use it to share a Unix domain socket file',
      'Use hostPath for the socket',
      'Use a ConfigMap with the socket configuration',
    ],
    correctAnswer: 1,
    explanation: 'Mount an emptyDir to the same path in both containers. One container creates the Unix socket file in the shared volume, and the other container connects to it. Common for database proxies and authentication sidecars.',
    codeSnippet: 'spec:\n  containers:\n    - name: app\n      image: myapp:latest\n      volumeMounts:\n        - name: socket\n          mountPath: /var/run/proxy\n    - name: proxy\n      image: cloud-sql-proxy:latest\n      volumeMounts:\n        - name: socket\n          mountPath: /var/run/proxy\n  volumes:\n    - name: socket\n      emptyDir: {}',
    tags: ['kubernetes', 'emptydir', 'sidecar', 'unix-socket'],
  },
  {
    id: 'k8s-storage-l3-078',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'pvcs',
    question: 'What is the StatefulSet volumeClaimTemplates immutability rule and its workaround?',
    options: [
      'volumeClaimTemplates can be freely modified',
      'volumeClaimTemplates are immutable after creation; to change storage size or class, you must delete the StatefulSet (with --cascade=orphan), update the template, recreate it, and manually resize existing PVCs',
      'Only the storage size can be changed',
      'You must delete all PVCs first',
    ],
    correctAnswer: 1,
    explanation: 'volumeClaimTemplates cannot be modified on an existing StatefulSet. Workaround: delete with --cascade=orphan (keeps Pods/PVCs), recreate StatefulSet with updated template. Existing PVCs must be manually expanded if needed.',
    codeSnippet: '# Delete StatefulSet without deleting Pods\nkubectl delete sts db --cascade=orphan\n\n# Update the volumeClaimTemplate in the YAML\n# Then recreate\nkubectl apply -f updated-statefulset.yaml\n\n# Manually expand existing PVCs\nkubectl patch pvc data-db-0 -p \'{"spec":{"resources":{"requests":{"storage":"100Gi"}}}}\'',
    tags: ['kubernetes', 'pvcs', 'statefulset', 'immutability', 'workaround'],
  },
  {
    id: 'k8s-storage-l3-079',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'volume-expansion',
    question: 'What is the node-level filesystem resize process for CSI volumes?',
    options: [
      'The filesystem is resized automatically by the cloud provider',
      'Kubelet calls CSI NodeExpandVolume, which runs filesystem resize tools (resize2fs for ext4, xfs_growfs for xfs) on the node to expand the filesystem to fill the larger block device',
      'The application must resize the filesystem',
      'A reboot is required for filesystem resize',
    ],
    correctAnswer: 1,
    explanation: 'After the controller expands the backend storage, kubelet calls CSI NodeExpandVolume on the node. The CSI driver runs resize2fs (ext4) or xfs_growfs (xfs) to grow the filesystem to match the new block device size.',
    tags: ['kubernetes', 'volume-expansion', 'filesystem-resize', 'node-level'],
  },
  {
    id: 'k8s-storage-l3-080',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'storage-classes',
    question: 'How do you test storage failover and recovery in a Kubernetes cluster?',
    options: [
      'Wait for a real failure to occur',
      'Use chaos engineering tools (Litmus, Chaos Mesh) to simulate volume failures, node failures, and CSI driver failures, and verify recovery procedures',
      'Test in a non-production environment only',
      'Check the CSI driver documentation',
    ],
    correctAnswer: 1,
    explanation: 'Use chaos engineering: simulate CSI driver failures, node cordoning/draining, volume detachment, and storage backend issues. Verify Pod rescheduling, volume reattachment, data integrity, and backup/restore procedures.',
    tags: ['kubernetes', 'storage-classes', 'chaos-engineering', 'testing'],
  },
  {
    id: 'k8s-storage-l3-081',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'How do you handle the "volume is already exclusively attached to one node" error during Pod migration?',
    options: [
      'Force delete the old Pod immediately',
      'Wait for the old Pod to fully terminate and the volume to be detached; if stuck, check VolumeAttachment status and consider force-deleting the stuck Pod with --grace-period=0',
      'Delete the PV and PVC',
      'Restart the kubelet on the old node',
    ],
    correctAnswer: 1,
    explanation: 'This error occurs when the volume is still attached to the old node. Wait for graceful termination. If stuck: check VolumeAttachment objects, verify old Pod status, and as a last resort, force-delete the stuck Pod.',
    codeSnippet: '# Check VolumeAttachment\nkubectl get volumeattachment | grep <pv-name>\n\n# If Pod is stuck terminating on old node\nkubectl get pod <pod> -o jsonpath=\'{.metadata.deletionTimestamp}\'\n\n# Force delete stuck Pod (last resort)\nkubectl delete pod <pod> --grace-period=0 --force\n\n# If VolumeAttachment is stuck\nkubectl delete volumeattachment <name>',
    tags: ['kubernetes', 'persistent-volumes', 'volume-attachment', 'troubleshooting'],
  },
  {
    id: 'k8s-storage-l3-082',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'What is the volume snapshot validation webhook and why is it important?',
    options: [
      'It validates snapshot data integrity',
      'It validates VolumeSnapshot, VolumeSnapshotContent, and VolumeSnapshotClass objects to prevent misconfigured snapshots and invalid cross-references',
      'It validates CSI driver health',
      'It sends webhooks when snapshots complete',
    ],
    correctAnswer: 1,
    explanation: 'The snapshot validation webhook prevents creation of invalid snapshot resources. It validates required fields, cross-references between VolumeSnapshot and VolumeSnapshotContent, and prevents conflicting configurations.',
    tags: ['kubernetes', 'csi', 'snapshots', 'validation-webhook'],
  },
  {
    id: 'k8s-storage-l3-083',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'projected-volumes',
    question: 'How do you implement environment-specific configuration using projected volumes?',
    options: [
      'Use different container images per environment',
      'Use projected volumes combining a base ConfigMap with environment-specific overrides from another ConfigMap or Secret',
      'Use Helm values only',
      'Hard-code configuration in the image',
    ],
    correctAnswer: 1,
    explanation: 'Use projected volumes to layer configuration: a base ConfigMap with common settings, plus an environment-specific ConfigMap/Secret for overrides. Files from later sources override earlier ones in the projected mount.',
    codeSnippet: 'volumes:\n  - name: config\n    projected:\n      sources:\n        - configMap:\n            name: app-config-base    # Common config\n        - configMap:\n            name: app-config-prod    # Production overrides\n        - secret:\n            name: app-secrets-prod   # Production secrets',
    tags: ['kubernetes', 'projected-volumes', 'configuration', 'environments'],
  },
  {
    id: 'k8s-storage-l3-084',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'snapshots',
    question: 'What are VolumeGroupSnapshots and what problem do they solve?',
    options: [
      'Snapshots of volume groups in LVM',
      'Atomic snapshots of multiple related PVCs to ensure cross-volume data consistency for applications using multiple volumes',
      'Grouping snapshots by label for management',
      'Taking snapshots of all volumes in a namespace',
    ],
    correctAnswer: 1,
    explanation: 'VolumeGroupSnapshots (alpha in 1.27) enable taking a consistent snapshot of multiple PVCs simultaneously. This is essential for applications like databases with separate data and WAL volumes that must be snapshotted atomically.',
    tags: ['kubernetes', 'snapshots', 'group-snapshots', 'consistency'],
  },
  {
    id: 'k8s-storage-l3-085',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'dynamic-provisioning',
    question: 'How do you implement storage provisioning quotas that vary by team/project in a shared cluster?',
    options: [
      'Use a single global quota',
      'Create per-namespace ResourceQuotas with team-specific storage limits, including per-StorageClass quotas to prevent teams from using expensive tiers without approval',
      'Use RBAC to restrict storage creation',
      'Set limits in the StorageClass',
    ],
    correctAnswer: 1,
    explanation: 'Use per-namespace ResourceQuotas with different limits per team. Include per-StorageClass quotas (e.g., team-a gets 100Gi of "high-perf" but 1Ti of "standard"). Combine with admission policies for enforcement.',
    tags: ['kubernetes', 'dynamic-provisioning', 'quotas', 'multi-tenancy'],
  },
  {
    id: 'k8s-storage-l3-086',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'persistent-volumes',
    question: 'What is the impact of the PV reclaim policy on cloud storage costs?',
    options: [
      'No cost impact',
      'With Delete policy, storage is removed on PVC deletion (no ongoing cost). With Retain, orphaned storage continues to incur costs until manually deleted.',
      'Retain is always cheaper',
      'Delete causes double billing',
    ],
    correctAnswer: 1,
    explanation: 'Retain policy preserves storage after PVC deletion, which means continued cloud storage charges for potentially unused volumes. Implement monitoring for Released PVs and a review process to control costs.',
    tags: ['kubernetes', 'persistent-volumes', 'reclaim-policy', 'cost'],
  },
  {
    id: 'k8s-storage-l3-087',
    language: 'kubernetes',
    level: 'level3',
    category: 'Storage',
    subcategory: 'csi',
    question: 'How do you implement a CSI driver health check to detect and recover from driver failures?',
    options: [
      'CSI drivers are always reliable',
      'Monitor CSI driver Pod health with liveness probes, use the CSI liveness-probe sidecar that exposes a health endpoint, and set up alerts for driver failures',
      'Restart the kubelet periodically',
      'Use a watchdog service',
    ],
    correctAnswer: 1,
    explanation: 'The CSI liveness-probe sidecar exposes a health endpoint by probing the CSI driver gRPC endpoint. Configure Kubernetes liveness probes to detect unhealthy CSI drivers. Set up Prometheus alerts for CSI operation failures.',
    codeSnippet: '# CSI liveness probe sidecar\n- name: liveness-probe\n  image: registry.k8s.io/sig-storage/livenessprobe:v2.12.0\n  args:\n    - --csi-address=/csi/csi.sock\n  volumeMounts:\n    - name: socket-dir\n      mountPath: /csi\n# Liveness probe configuration\nlivenessProbe:\n  httpGet:\n    path: /healthz\n    port: 9808\n  initialDelaySeconds: 10\n  periodSeconds: 10',
    tags: ['kubernetes', 'csi', 'health-check', 'monitoring'],
  },
];

export default questions;
