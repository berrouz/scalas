import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-crds-l3-001',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'What is a conversion webhook and when is it needed?',
    options: [
      'A webhook that converts container image formats',
      'A webhook that converts custom resources between different API versions stored in etcd',
      'A webhook that converts YAML to JSON',
      'A webhook that converts Helm charts to operators',
    ],
    correctAnswer: 1,
    explanation: 'A conversion webhook converts custom resources between different served versions of the same CRD, enabling multi-version support when versions have different schemas.',
    codeSnippet: 'spec:\n  conversion:\n    strategy: Webhook\n    webhook:\n      clientConfig:\n        service:\n          name: my-conversion-webhook\n          namespace: system\n          path: /convert\n        caBundle: <base64-cert>\n      conversionReviewVersions: ["v1"]',
    tags: ['kubernetes', 'webhooks', 'conversion'],
  },
  {
    id: 'k8s-crds-l3-002',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How should an operator handle a "hub and spoke" conversion model for multi-version CRDs?',
    options: [
      'Convert directly between any two versions',
      'Designate one version as the hub and convert all other versions through it',
      'Store all versions simultaneously in etcd',
      'Use Kubernetes built-in type conversion',
    ],
    correctAnswer: 1,
    explanation: 'The hub-and-spoke model designates one API version as the hub. All other versions (spokes) convert to/from the hub, reducing the number of conversion paths from O(n^2) to O(n).',
    codeSnippet: '// Hub version - the canonical internal representation\nfunc (src *MemcachedV1) ConvertTo(dstRaw conversion.Hub) error {\n    dst := dstRaw.(*MemcachedV2)\n    dst.Spec.Size = src.Spec.Replicas // field renamed\n    return nil\n}\n\nfunc (src *MemcachedV1) ConvertFrom(srcRaw conversion.Hub) error {\n    hub := srcRaw.(*MemcachedV2)\n    src.Spec.Replicas = hub.Spec.Size\n    return nil\n}',
    tags: ['kubernetes', 'reconciliation', 'conversion'],
  },
  {
    id: 'k8s-crds-l3-003',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the problem with "hot loop" reconciliation and how do you prevent it?',
    options: [
      'A hot loop is when the controller uses too much CPU; prevent it with resource limits',
      'It is when reconciliation triggers itself by updating the resource, causing infinite loops; prevent it with status subresource and generation-based filtering',
      'It is a network issue; prevent it with circuit breakers',
      'It is when too many events queue up; prevent it with rate limiting only',
    ],
    correctAnswer: 1,
    explanation: 'If a controller updates the resource during reconciliation, it triggers another reconciliation. Using status subresource (separate endpoint) and generation-based predicates prevents this.',
    codeSnippet: '// Use status subresource so status updates don\'t trigger reconciliation\n// Use GenerationChangedPredicate to skip metadata-only changes\nctrl.NewControllerManagedBy(mgr).\n    For(&cachev1alpha1.Memcached{}, builder.WithPredicates(\n        predicate.GenerationChangedPredicate{},\n    )).\n    Complete(r)',
    tags: ['kubernetes', 'controllers', 'hot-loop'],
  },
  {
    id: 'k8s-crds-l3-004',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement multi-namespace watching in an operator?',
    options: [
      'Deploy one operator per namespace',
      'Use the MultiNamespacedCacheBuilder or configure namespaced caches per namespace',
      'Use cluster-scoped CRDs only',
      'Set the WATCH_NAMESPACE environment variable to a comma-separated list',
    ],
    correctAnswer: 1,
    explanation: 'controller-runtime supports watching multiple specific namespaces using cache.MultiNamespacedCacheBuilder, creating separate caches per namespace.',
    codeSnippet: 'namespaces := []string{"ns-a", "ns-b", "ns-c"}\n\nmgr, err := ctrl.NewManager(cfg, ctrl.Options{\n    NewCache: cache.MultiNamespacedCacheBuilder(namespaces),\n})\n\n// Or for all namespaces (empty string means cluster-wide):\nmgr, err := ctrl.NewManager(cfg, ctrl.Options{\n    Namespace: "", // watch all namespaces\n})',
    tags: ['kubernetes', 'operator-sdk', 'multi-namespace'],
  },
  {
    id: 'k8s-crds-l3-005',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'What are CEL (Common Expression Language) validation rules in CRDs?',
    options: [
      'A replacement for OpenAPI validation',
      'Custom validation expressions that can reference multiple fields for cross-field validation',
      'A new programming language for operators',
      'Container Expression Language for runtime validation',
    ],
    correctAnswer: 1,
    explanation: 'CEL validation rules (x-kubernetes-validations) allow complex cross-field validation expressions that cannot be expressed with standard OpenAPI schema constraints.',
    codeSnippet: 'openAPIV3Schema:\n  type: object\n  properties:\n    spec:\n      type: object\n      x-kubernetes-validations:\n        - rule: "self.minReplicas <= self.maxReplicas"\n          message: "minReplicas must be <= maxReplicas"\n        - rule: "self.replicas >= self.minReplicas && self.replicas <= self.maxReplicas"\n          message: "replicas must be between minReplicas and maxReplicas"\n      properties:\n        minReplicas:\n          type: integer\n        maxReplicas:\n          type: integer\n        replicas:\n          type: integer',
    tags: ['kubernetes', 'validation', 'cel'],
  },
  {
    id: 'k8s-crds-l3-006',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'finalizers',
    question: 'A namespace is stuck in Terminating state due to a finalizer on a CRD that no longer has a running controller. How do you resolve this?',
    options: [
      'Restart the API server',
      'Force delete the namespace by removing the finalizer via the API, bypassing normal deletion',
      'Reinstall the CRD and controller',
      'Delete the etcd data directly',
    ],
    correctAnswer: 1,
    explanation: 'You can remove stuck finalizers by patching the resource directly via the Kubernetes API (kubectl edit or kubectl patch) to remove the finalizer, allowing deletion to proceed.',
    codeSnippet: '# Remove a stuck finalizer from a custom resource\nkubectl patch crontab my-cron -p \'{"metadata":{"finalizers":null}}\' --type=merge\n\n# Or for a stuck namespace\nkubectl get namespace stuck-ns -o json | \\\n  jq \'.spec.finalizers = []\' | \\\n  kubectl replace --raw "/api/v1/namespaces/stuck-ns/finalize" -f -',
    tags: ['kubernetes', 'finalizers', 'troubleshooting'],
  },
  {
    id: 'k8s-crds-l3-007',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you ensure webhooks do not block cluster operations if the operator is down?',
    options: [
      'Set failurePolicy: Ignore and use objectSelector/namespaceSelector to limit scope',
      'Remove the webhook configuration entirely',
      'Run the webhook on every node',
      'Webhooks never block cluster operations',
    ],
    correctAnswer: 0,
    explanation: 'Using failurePolicy: Ignore allows requests through when the webhook is unavailable. Combine with selectors to minimize scope and add timeoutSeconds to limit wait time.',
    codeSnippet: 'webhooks:\n  - name: validate.example.com\n    failurePolicy: Ignore\n    timeoutSeconds: 5\n    objectSelector:\n      matchLabels:\n        managed-by: my-operator\n    namespaceSelector:\n      matchExpressions:\n        - key: kubernetes.io/metadata.name\n          operator: NotIn\n          values: ["kube-system", "kube-public"]',
    tags: ['kubernetes', 'webhooks', 'high-availability'],
  },
  {
    id: 'k8s-crds-l3-008',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "level-triggered" vs "edge-triggered" reconciliation approach?',
    options: [
      'Level-triggered uses logging levels; edge-triggered uses event edges',
      'Level-triggered reconciles based on current state; edge-triggered reacts to specific events',
      'They refer to network trigger types',
      'They are Kubernetes scheduling modes',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes controllers use level-triggered (state-based) reconciliation. The Reconcile function receives the current state and computes actions, rather than reacting to specific change events.',
    tags: ['kubernetes', 'reconciliation', 'design-patterns'],
  },
  {
    id: 'k8s-crds-l3-009',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How should an operator handle external resource lifecycle management (e.g., cloud resources)?',
    options: [
      'Create cloud resources in init containers',
      'Use finalizers for cleanup, status conditions for state tracking, and exponential backoff for provisioning',
      'Store cloud credentials in the CRD spec',
      'Use Helm hooks for cloud resource management',
    ],
    correctAnswer: 1,
    explanation: 'For external resources: use finalizers to ensure cleanup on deletion, conditions to track provisioning state, and backoff for eventual consistency with external APIs.',
    codeSnippet: 'func (r *DatabaseReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    db := &v1alpha1.Database{}\n    if err := r.Get(ctx, req.NamespacedName, db); err != nil {\n        return ctrl.Result{}, client.IgnoreNotFound(err)\n    }\n\n    // Handle deletion with finalizer\n    if !db.DeletionTimestamp.IsZero() {\n        if err := r.deleteExternalDB(ctx, db); err != nil {\n            return ctrl.Result{RequeueAfter: 30 * time.Second}, nil\n        }\n        controllerutil.RemoveFinalizer(db, finalizerName)\n        return ctrl.Result{}, r.Update(ctx, db)\n    }\n\n    // Create/update external resource\n    status, err := r.ensureExternalDB(ctx, db)\n    if err != nil {\n        meta.SetStatusCondition(&db.Status.Conditions, metav1.Condition{\n            Type:    "Ready",\n            Status:  metav1.ConditionFalse,\n            Reason:  "ProvisioningFailed",\n            Message: err.Error(),\n        })\n        r.Status().Update(ctx, db)\n        return ctrl.Result{RequeueAfter: 1 * time.Minute}, nil\n    }\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'operator-pattern', 'external-resources'],
  },
  {
    id: 'k8s-crds-l3-010',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement custom indexers for efficient lookups in controller-runtime?',
    options: [
      'Use SQL queries on etcd',
      'Use mgr.GetFieldIndexer().IndexField() to create custom indexes on resource fields',
      'Create a separate database for indexing',
      'Use label selectors for all queries',
    ],
    correctAnswer: 1,
    explanation: 'Custom indexers allow efficient lookups by arbitrary fields. You register them with the manager, then use them in List operations with MatchingFields.',
    codeSnippet: '// Register index during setup\nif err := mgr.GetFieldIndexer().IndexField(ctx, &corev1.Pod{}, "spec.nodeName",\n    func(obj client.Object) []string {\n        pod := obj.(*corev1.Pod)\n        return []string{pod.Spec.NodeName}\n    }); err != nil {\n    return err\n}\n\n// Use index in reconciliation\npodList := &corev1.PodList{}\nerr := r.List(ctx, podList, client.MatchingFields{\n    "spec.nodeName": nodeName,\n})',
    tags: ['kubernetes', 'controllers', 'indexing'],
  },
  {
    id: 'k8s-crds-l3-011',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'How do you implement a seamless upgrade path for an operator managed by OLM?',
    options: [
      'Delete and reinstall the operator',
      'Define spec.replaces or spec.skips in the CSV to create an upgrade graph',
      'Use kubectl rollout to upgrade',
      'OLM does not support upgrades',
    ],
    correctAnswer: 1,
    explanation: 'CSVs use spec.replaces (pointing to the previous version CSV) and optionally spec.skips to define an upgrade graph that OLM follows for rolling updates.',
    codeSnippet: 'apiVersion: operators.coreos.com/v1alpha1\nkind: ClusterServiceVersion\nmetadata:\n  name: my-operator.v1.2.0\nspec:\n  replaces: my-operator.v1.1.0\n  # Or skip intermediate versions:\n  # skips:\n  #   - my-operator.v1.1.0\n  #   - my-operator.v1.0.1\n  version: 1.2.0\n  # ...',
    tags: ['kubernetes', 'olm', 'upgrades'],
  },
  {
    id: 'k8s-crds-l3-012',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you implement immutable fields in a CRD (fields that cannot change after creation)?',
    options: [
      'Set immutable: true in the schema',
      'Use a validating webhook or CEL transition rule that compares old and new values on update',
      'Use the readOnly keyword',
      'Immutable fields are not possible in CRDs',
    ],
    correctAnswer: 1,
    explanation: 'Immutable fields require a validating webhook comparing old and new objects on UPDATE, or CEL transition rules using oldSelf to detect changes.',
    codeSnippet: '# Using CEL validation rules for immutability\nproperties:\n  spec:\n    type: object\n    properties:\n      storageClass:\n        type: string\n        x-kubernetes-validations:\n          - rule: "self == oldSelf"\n            message: "storageClass is immutable after creation"\n\n# Note: oldSelf is only available with transition rules\n# Requires x-kubernetes-validations at the field level',
    tags: ['kubernetes', 'validation', 'immutability'],
  },
  {
    id: 'k8s-crds-l3-013',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that watches resources across multiple clusters?',
    options: [
      'Set the KUBECONFIG to multiple files',
      'Create separate managers or clients per cluster, each with their own kubeconfig',
      'Use a single manager with cluster-wide scope',
      'Multi-cluster watching is not supported',
    ],
    correctAnswer: 1,
    explanation: 'Multi-cluster operators create separate clients/managers for each target cluster using their respective kubeconfigs, coordinating through a central control plane.',
    codeSnippet: '// Create a client for the remote cluster\nremoteConfig, err := clientcmd.BuildConfigFromFlags("", "/path/to/remote-kubeconfig")\nremoteClient, err := client.New(remoteConfig, client.Options{Scheme: scheme})\n\n// Use in reconciliation\nremotePodList := &corev1.PodList{}\nerr = remoteClient.List(ctx, remotePodList, client.InNamespace("default"))',
    tags: ['kubernetes', 'controllers', 'multi-cluster'],
  },
  {
    id: 'k8s-crds-l3-014',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you debug a webhook that is rejecting requests but not providing clear error messages?',
    options: [
      'Check the webhook server logs, the API server audit logs, and test with dry-run',
      'Restart the API server',
      'Delete and recreate the webhook configuration',
      'There is no way to debug webhooks',
    ],
    correctAnswer: 0,
    explanation: 'Debug webhooks by: checking webhook pod logs, reviewing API server audit logs, using kubectl --dry-run=server, and inspecting the AdmissionReview response.',
    codeSnippet: '# Test with dry-run to see webhook responses\nkubectl apply -f resource.yaml --dry-run=server -v=8\n\n# Check webhook pod logs\nkubectl logs -n system deployment/operator-controller-manager\n\n# Check API server logs for webhook calls\n# Look for entries containing "admission" or "webhook"',
    tags: ['kubernetes', 'webhooks', 'debugging'],
  },
  {
    id: 'k8s-crds-l3-015',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "drift detection and correction" pattern in operator design?',
    options: [
      'Detecting when the operator binary has changed',
      'Comparing actual state of managed resources against desired state and correcting differences',
      'Detecting network latency issues',
      'Monitoring CRD schema changes',
    ],
    correctAnswer: 1,
    explanation: 'Drift detection compares the actual state of managed child resources against the desired state derived from the custom resource spec, correcting any differences.',
    codeSnippet: '// Check if deployment matches desired state\nexistingDep := &appsv1.Deployment{}\nerr := r.Get(ctx, types.NamespacedName{Name: depName, Namespace: ns}, existingDep)\nif err == nil {\n    // Compare and correct drift\n    desired := r.desiredDeployment(cr)\n    if !equality.Semantic.DeepEqual(existingDep.Spec, desired.Spec) {\n        existingDep.Spec = desired.Spec\n        if err := r.Update(ctx, existingDep); err != nil {\n            return ctrl.Result{}, err\n        }\n        r.Recorder.Event(cr, corev1.EventTypeNormal, "DriftCorrected",\n            "Deployment spec corrected to match desired state")\n    }\n}',
    tags: ['kubernetes', 'reconciliation', 'drift-detection'],
  },
  {
    id: 'k8s-crds-l3-016',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement integration tests for an operator using envtest?',
    options: [
      'Deploy to a real cluster and run kubectl commands',
      'Use envtest.Environment which provides a local API server and etcd for testing',
      'Mock all Kubernetes API calls',
      'Use Docker Compose to simulate a cluster',
    ],
    correctAnswer: 1,
    explanation: 'envtest starts a local API server and etcd (from controller-runtime test binaries) to run integration tests without a full cluster.',
    codeSnippet: 'var testEnv *envtest.Environment\nvar k8sClient client.Client\n\nfunc TestAPIs(t *testing.T) {\n    testEnv = &envtest.Environment{\n        CRDDirectoryPaths: []string{\n            filepath.Join("..", "config", "crd", "bases"),\n        },\n        WebhookInstallOptions: envtest.WebhookInstallOptions{\n            Paths: []string{filepath.Join("..", "config", "webhook")},\n        },\n    }\n    cfg, err := testEnv.Start()\n    // ... setup client, manager, start controller\n\n    // Test: create CR and verify reconciliation\n    cr := &v1alpha1.Memcached{\n        ObjectMeta: metav1.ObjectMeta{Name: "test", Namespace: "default"},\n        Spec: v1alpha1.MemcachedSpec{Size: 3},\n    }\n    Expect(k8sClient.Create(ctx, cr)).To(Succeed())\n\n    // Verify deployment was created\n    Eventually(func() bool {\n        dep := &appsv1.Deployment{}\n        err := k8sClient.Get(ctx, types.NamespacedName{Name: "test", Namespace: "default"}, dep)\n        return err == nil\n    }).Should(BeTrue())\n}',
    tags: ['kubernetes', 'operator-sdk', 'testing'],
  },
  {
    id: 'k8s-crds-l3-017',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'owner-references',
    question: 'What are the different garbage collection policies and how do they interact with owner references?',
    options: [
      'There is only one garbage collection policy',
      'Foreground (delete dependents first), Background (delete owner first then GC), and Orphan (leave dependents)',
      'Delete and Keep',
      'Cascade and NoCascade',
    ],
    correctAnswer: 1,
    explanation: 'Foreground deletion blocks until dependents are deleted. Background deletion deletes the owner immediately and GC collects dependents asynchronously. Orphan leaves dependents.',
    codeSnippet: '# Delete with foreground cascading (blocks until dependents are gone)\nkubectl delete memcached my-cache --cascade=foreground\n\n# Delete with background cascading (default)\nkubectl delete memcached my-cache --cascade=background\n\n# Orphan dependents (remove owner reference without deleting them)\nkubectl delete memcached my-cache --cascade=orphan',
    tags: ['kubernetes', 'owner-references', 'garbage-collection'],
  },
  {
    id: 'k8s-crds-l3-018',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'How do you implement selective field-level access control in a CRD?',
    options: [
      'Use field-level RBAC rules',
      'CRDs do not support field-level access; use subresources and webhooks to restrict access patterns',
      'Use annotations to mark restricted fields',
      'Use OpenAPI readOnly property',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes RBAC works at the resource level, not field level. To control field access, use the status subresource for spec/status separation and admission webhooks for field restrictions.',
    tags: ['kubernetes', 'crd-definition', 'access-control'],
  },
  {
    id: 'k8s-crds-l3-019',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you handle rate limiting for external API calls in an operator?',
    options: [
      'Use Kubernetes built-in rate limiting',
      'Implement custom rate limiters (e.g., token bucket) and use RequeueAfter for backoff',
      'External API calls should not be rate limited',
      'Use network policies to limit throughput',
    ],
    correctAnswer: 1,
    explanation: 'Use Go rate limiters (e.g., golang.org/x/time/rate) for external API calls and return RequeueAfter when rate limited to retry later without overwhelming the external service.',
    codeSnippet: 'type Reconciler struct {\n    client.Client\n    rateLimiter *rate.Limiter\n}\n\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    // Wait for rate limiter before calling external API\n    if err := r.rateLimiter.Wait(ctx); err != nil {\n        return ctrl.Result{RequeueAfter: 10 * time.Second}, nil\n    }\n\n    // Make external API call\n    result, err := r.externalClient.CreateResource(ctx, spec)\n    if err != nil {\n        // Exponential backoff\n        return ctrl.Result{RequeueAfter: calculateBackoff(attempts)}, nil\n    }\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'controllers', 'rate-limiting'],
  },
  {
    id: 'k8s-crds-l3-020',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'api-versioning',
    question: 'What is the proper strategy for migrating CRD storage version?',
    options: [
      'Change the storage flag and restart the API server',
      'Add new version with storage: true, migrate existing objects by reading and writing them, then remove old version',
      'Delete all objects and recreate them',
      'Storage version cannot be changed',
    ],
    correctAnswer: 1,
    explanation: 'To migrate storage versions: add the new version as storage, run a migration job that reads and writes all existing objects (converting them to the new storage version), then remove the old version.',
    codeSnippet: '# Step 1: Add new version as storage\nversions:\n  - name: v1alpha1\n    served: true\n    storage: false  # Changed from true\n  - name: v1\n    served: true\n    storage: true   # New storage version\n\n# Step 2: Migrate existing objects\nkubectl get crontabs -A -o json | kubectl apply -f -\n\n# Step 3: Check storedVersions\nkubectl get crd crontabs.example.com -o jsonpath=\'{.status.storedVersions}\'\n\n# Step 4: Remove old version after confirming migration',
    tags: ['kubernetes', 'api-versioning', 'migration'],
  },
  {
    id: 'k8s-crds-l3-021',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you implement graceful shutdown handling in an operator?',
    options: [
      'Use os.Exit(0) immediately',
      'Handle SIGTERM signal and use context cancellation to stop reconciliation loops cleanly',
      'Kill all managed pods before shutting down',
      'Operators do not need graceful shutdown',
    ],
    correctAnswer: 1,
    explanation: 'controller-runtime Manager handles SIGTERM by cancelling the context passed to reconcilers. Reconcilers should check ctx.Done() in long operations.',
    codeSnippet: 'func (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    // Check if context is cancelled (shutdown signal)\n    select {\n    case <-ctx.Done():\n        return ctrl.Result{}, nil\n    default:\n    }\n\n    // For long operations, pass context\n    if err := r.longRunningOperation(ctx); err != nil {\n        if ctx.Err() != nil {\n            // Graceful shutdown in progress\n            return ctrl.Result{}, nil\n        }\n        return ctrl.Result{}, err\n    }\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'reconciliation', 'graceful-shutdown'],
  },
  {
    id: 'k8s-crds-l3-022',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement a backup and restore mechanism in an operator?',
    options: [
      'Use kubectl cp to copy data',
      'Define Backup/Restore CRDs, use a controller to manage backup jobs, and store data in external storage',
      'Use PersistentVolume snapshots only',
      'Operators should not handle backup/restore',
    ],
    correctAnswer: 1,
    explanation: 'Best practice is to create Backup/Restore custom resources that trigger the operator to run backup jobs, store data externally, and manage the backup lifecycle.',
    codeSnippet: 'apiVersion: database.example.com/v1alpha1\nkind: DatabaseBackup\nmetadata:\n  name: daily-backup\nspec:\n  databaseRef:\n    name: my-production-db\n  schedule: "0 2 * * *"\n  storage:\n    s3:\n      bucket: my-backups\n      prefix: production/\n  retention:\n    keepLast: 7\n    keepDaily: 30\nstatus:\n  lastBackupTime: "2024-01-15T02:00:00Z"\n  lastBackupStatus: Succeeded\n  backupCount: 45',
    tags: ['kubernetes', 'operator-pattern', 'backup-restore'],
  },
  {
    id: 'k8s-crds-l3-023',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you implement a mutating webhook that injects sidecar containers?',
    options: [
      'Modify the Pod spec in the controller',
      'Use a mutating admission webhook that intercepts Pod CREATE operations and patches the spec',
      'Use init containers instead',
      'Use a DaemonSet for sidecar functionality',
    ],
    correctAnswer: 1,
    explanation: 'A mutating webhook intercepts Pod creation and uses JSON patch to inject sidecar containers, volumes, and environment variables before the Pod is created.',
    codeSnippet: 'func (h *SidecarInjector) Handle(ctx context.Context, req admission.Request) admission.Response {\n    pod := &corev1.Pod{}\n    if err := h.decoder.Decode(req, pod); err != nil {\n        return admission.Errored(http.StatusBadRequest, err)\n    }\n\n    // Check annotation to determine if injection is needed\n    if pod.Annotations["inject-sidecar"] != "true" {\n        return admission.Allowed("no injection needed")\n    }\n\n    // Inject sidecar container\n    sidecar := corev1.Container{\n        Name:  "sidecar",\n        Image: "sidecar:latest",\n        Ports: []corev1.ContainerPort{{ContainerPort: 9090}},\n    }\n    pod.Spec.Containers = append(pod.Spec.Containers, sidecar)\n\n    marshaledPod, _ := json.Marshal(pod)\n    return admission.PatchResponseFromRaw(req.Object.Raw, marshaledPod)\n}',
    tags: ['kubernetes', 'webhooks', 'sidecar-injection'],
  },
  {
    id: 'k8s-crds-l3-024',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'status-subresource',
    question: 'How do you implement the standard conditions pattern with metav1.Condition?',
    options: [
      'Use custom condition structs',
      'Use metav1.Condition with meta.SetStatusCondition for proper last transition time handling',
      'Use annotations to store conditions',
      'Store conditions in a ConfigMap',
    ],
    correctAnswer: 1,
    explanation: 'Using meta.SetStatusCondition correctly manages condition transitions, preserving lastTransitionTime when the status does not change.',
    codeSnippet: 'import "k8s.io/apimachinery/pkg/api/meta"\n\n// Set a condition\nmeta.SetStatusCondition(&cr.Status.Conditions, metav1.Condition{\n    Type:               "Available",\n    Status:             metav1.ConditionTrue,\n    ObservedGeneration: cr.Generation,\n    Reason:             "ReconcileSuccess",\n    Message:            fmt.Sprintf("Deployment has %d ready replicas", readyReplicas),\n})\n\n// Check a condition\nif meta.IsStatusConditionTrue(cr.Status.Conditions, "Available") {\n    // resource is available\n}\n\n// Find a specific condition\ncond := meta.FindStatusCondition(cr.Status.Conditions, "Degraded")',
    tags: ['kubernetes', 'status-subresource', 'conditions'],
  },
  {
    id: 'k8s-crds-l3-025',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the purpose of the work queue rate limiter in a controller?',
    options: [
      'To limit the number of custom resources',
      'To prevent reconciliation storms by rate-limiting how quickly items are re-queued after failures',
      'To limit API server request throughput',
      'To throttle container creation rate',
    ],
    correctAnswer: 1,
    explanation: 'The work queue rate limiter prevents thundering herd problems by using exponential backoff when items fail repeatedly, protecting both the controller and API server.',
    codeSnippet: 'import "k8s.io/client-go/util/workqueue"\n\nctrl.NewControllerManagedBy(mgr).\n    For(&v1alpha1.MyResource{}).\n    WithOptions(controller.Options{\n        RateLimiter: workqueue.NewMaxOfRateLimiter(\n            workqueue.NewItemExponentialFailureRateLimiter(5*time.Millisecond, 1000*time.Second),\n            &workqueue.BucketRateLimiter{Limiter: rate.NewLimiter(rate.Limit(10), 100)},\n        ),\n    }).\n    Complete(r)',
    tags: ['kubernetes', 'controllers', 'rate-limiting'],
  },
  {
    id: 'k8s-crds-l3-026',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'How do you implement operator-level RBAC scoping with OLM?',
    options: [
      'RBAC is always cluster-wide with OLM',
      'OLM creates RBAC based on CSV permissions and the OperatorGroup target namespaces',
      'You manually create all RBAC resources',
      'OLM does not manage RBAC',
    ],
    correctAnswer: 1,
    explanation: 'OLM generates RBAC from the CSV spec.install.permissions (namespace-scoped) and spec.install.clusterPermissions (cluster-scoped), scoped to OperatorGroup targets.',
    codeSnippet: 'spec:\n  install:\n    strategy: deployment\n    spec:\n      permissions:\n        - serviceAccountName: my-operator\n          rules:\n            - apiGroups: [""]\n              resources: ["configmaps"]\n              verbs: ["get", "list", "watch"]\n      clusterPermissions:\n        - serviceAccountName: my-operator\n          rules:\n            - apiGroups: ["cache.example.com"]\n              resources: ["memcacheds"]\n              verbs: ["*"]',
    tags: ['kubernetes', 'olm', 'rbac'],
  },
  {
    id: 'k8s-crds-l3-027',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you use CEL to validate that an array has unique values for a specific field?',
    options: [
      'Use x-kubernetes-list-type: set',
      'Use a CEL rule with self.all() and size() to check uniqueness',
      'Use the unique keyword',
      'Array uniqueness validation is not possible',
    ],
    correctAnswer: 1,
    explanation: 'CEL expressions can validate array uniqueness by comparing the size of the array against the size of a mapped unique set.',
    codeSnippet: 'properties:\n  containers:\n    type: array\n    x-kubernetes-validations:\n      - rule: "self.all(c, self.exists_one(d, c.name == d.name))"\n        message: "container names must be unique"\n      # Or using map and size:\n      - rule: "self.map(c, c.name).size() == self.size()"\n        message: "container names must be unique"\n    items:\n      type: object\n      properties:\n        name:\n          type: string\n        image:\n          type: string',
    tags: ['kubernetes', 'validation', 'cel-advanced'],
  },
  {
    id: 'k8s-crds-l3-028',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement the Ansible operator proxy functionality for managing Kubernetes resources?',
    options: [
      'Use kubectl commands in Ansible tasks',
      'Use the k8s Ansible module provided by the operator SDK proxy to interact with the Kubernetes API',
      'Write a custom Ansible module for each resource type',
      'Use shell commands with curl to call the API server',
    ],
    correctAnswer: 1,
    explanation: 'The Ansible operator provides the k8s Ansible module through its proxy, allowing Ansible playbooks to create, update, and delete Kubernetes resources natively.',
    codeSnippet: '# roles/memcached/tasks/main.yml\n- name: Create Memcached Deployment\n  k8s:\n    definition:\n      apiVersion: apps/v1\n      kind: Deployment\n      metadata:\n        name: "{{ ansible_operator_meta.name }}-deployment"\n        namespace: "{{ ansible_operator_meta.namespace }}"\n      spec:\n        replicas: "{{ size | int }}"\n        selector:\n          matchLabels:\n            app: memcached\n        template:\n          spec:\n            containers:\n              - name: memcached\n                image: "memcached:{{ version }}"',
    tags: ['kubernetes', 'operator-sdk', 'ansible'],
  },
  {
    id: 'k8s-crds-l3-029',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'finalizers',
    question: 'How do you implement a pre-delete hook pattern using finalizers for complex cleanup?',
    options: [
      'Use Kubernetes Job hooks',
      'Add a finalizer, create cleanup Jobs during deletion, wait for completion, then remove the finalizer',
      'Use preStop lifecycle hooks',
      'Implement a deletion webhook',
    ],
    correctAnswer: 1,
    explanation: 'For complex cleanup (like data migration before deletion), create a Job during the deletion phase, monitor its completion, then remove the finalizer.',
    codeSnippet: 'func (r *Reconciler) handleDeletion(ctx context.Context, cr *v1alpha1.Database) (ctrl.Result, error) {\n    // Check if cleanup job exists\n    job := &batchv1.Job{}\n    err := r.Get(ctx, types.NamespacedName{\n        Name: cr.Name + "-cleanup", Namespace: cr.Namespace}, job)\n\n    if apierrors.IsNotFound(err) {\n        // Create cleanup job\n        cleanupJob := r.buildCleanupJob(cr)\n        ctrl.SetControllerReference(cr, cleanupJob, r.Scheme)\n        return ctrl.Result{}, r.Create(ctx, cleanupJob)\n    }\n\n    // Check if job completed\n    if job.Status.Succeeded > 0 {\n        controllerutil.RemoveFinalizer(cr, finalizerName)\n        return ctrl.Result{}, r.Update(ctx, cr)\n    }\n\n    // Still running, requeue\n    return ctrl.Result{RequeueAfter: 10 * time.Second}, nil\n}',
    tags: ['kubernetes', 'finalizers', 'cleanup-jobs'],
  },
  {
    id: 'k8s-crds-l3-030',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement cross-namespace owner references when the CRD manages resources in other namespaces?',
    options: [
      'Use standard owner references across namespaces',
      'Cross-namespace owner references are not supported; use labels/annotations and custom cleanup logic with finalizers',
      'Use cluster-scoped CRDs only',
      'Set the namespace field in the owner reference',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes owner references do not work across namespaces. For cross-namespace management, use labels/annotations to track relationships and finalizers for cleanup.',
    codeSnippet: '// Mark cross-namespace resources with labels instead of owner refs\nmanagedResource.Labels = map[string]string{\n    "managed-by":     "my-operator",\n    "owner-name":     cr.Name,\n    "owner-namespace": cr.Namespace,\n}\n\n// In finalizer cleanup, find and delete cross-namespace resources\nresourceList := &corev1.ServiceList{}\nr.List(ctx, resourceList, client.MatchingLabels{\n    "managed-by":     "my-operator",\n    "owner-name":     cr.Name,\n    "owner-namespace": cr.Namespace,\n})',
    tags: ['kubernetes', 'controllers', 'cross-namespace'],
  },
  {
    id: 'k8s-crds-l3-031',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How should an operator handle CRD schema migration when upgrading from v1beta1 to v1?',
    options: [
      'Kubernetes handles it automatically',
      'Create a new v1 CRD with structural schema, update operator code, and use kubectl replace to swap CRDs',
      'Delete all CRDs and start over',
      'v1beta1 and v1 are identical',
    ],
    correctAnswer: 1,
    explanation: 'Migration from v1beta1 to v1 requires adding structural schemas (required in v1), updating the apiVersion, and carefully replacing the CRD to avoid data loss.',
    tags: ['kubernetes', 'reconciliation', 'migration'],
  },
  {
    id: 'k8s-crds-l3-032',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'What is the "operator of operators" pattern (meta-operator)?',
    options: [
      'An operator that manages other operator installations and configurations',
      'An operator that runs on multiple clusters',
      'An operator with multiple CRDs',
      'An operator that manages the Kubernetes control plane',
    ],
    correctAnswer: 0,
    explanation: 'A meta-operator manages other operators, handling their lifecycle, configuration, and dependencies. OLM itself is an example of a meta-operator.',
    tags: ['kubernetes', 'operator-pattern', 'meta-operator'],
  },
  {
    id: 'k8s-crds-l3-033',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you prevent webhook-related deadlocks during cluster bootstrapping?',
    options: [
      'Disable webhooks during bootstrapping',
      'Exclude critical namespaces using namespaceSelector and use reinvocationPolicy: IfNeeded',
      'Run webhooks as DaemonSets',
      'Deadlocks cannot happen with webhooks',
    ],
    correctAnswer: 1,
    explanation: 'Exclude kube-system and the operator namespace from webhooks to prevent circular dependencies where the webhook cannot start because it intercepts its own creation.',
    codeSnippet: 'webhooks:\n  - name: validate.example.com\n    namespaceSelector:\n      matchExpressions:\n        - key: kubernetes.io/metadata.name\n          operator: NotIn\n          values:\n            - kube-system\n            - kube-public\n            - operator-system  # Exclude operator namespace\n    reinvocationPolicy: IfNeeded',
    tags: ['kubernetes', 'webhooks', 'bootstrapping'],
  },
  {
    id: 'k8s-crds-l3-034',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the source.Channel event source used for in controller-runtime?',
    options: [
      'To create Kubernetes channels',
      'To inject external events into the controller reconciliation queue from non-Kubernetes sources',
      'To configure OLM channels',
      'To set up inter-controller communication',
    ],
    correctAnswer: 1,
    explanation: 'source.Channel allows external event sources (e.g., webhooks from external systems, timers) to trigger reconciliation by sending events through a Go channel.',
    codeSnippet: 'externalEvents := make(chan event.GenericEvent)\n\nctrl.NewControllerManagedBy(mgr).\n    For(&v1alpha1.MyResource{}).\n    WatchesRawSource(&source.Channel{\n        Source: externalEvents,\n    }, &handler.EnqueueRequestForObject{}).\n    Complete(r)\n\n// From external webhook handler:\nexternalEvents <- event.GenericEvent{\n    Object: &v1alpha1.MyResource{\n        ObjectMeta: metav1.ObjectMeta{\n            Name: "affected-resource",\n            Namespace: "default",\n        },\n    },\n}',
    tags: ['kubernetes', 'controllers', 'external-events'],
  },
  {
    id: 'k8s-crds-l3-035',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you implement cross-resource validation (validating a CR against the state of other resources)?',
    options: [
      'Use OpenAPI schema references',
      'Use a validating webhook that queries the API server for related resources during validation',
      'Use CEL rules with external references',
      'Cross-resource validation is not possible',
    ],
    correctAnswer: 1,
    explanation: 'Cross-resource validation requires a validating webhook that can query the Kubernetes API to check related resources before accepting or rejecting the request.',
    codeSnippet: 'func (v *Validator) Handle(ctx context.Context, req admission.Request) admission.Response {\n    db := &v1alpha1.Database{}\n    if err := v.decoder.Decode(req, db); err != nil {\n        return admission.Errored(http.StatusBadRequest, err)\n    }\n\n    // Check if referenced StorageClass exists\n    sc := &storagev1.StorageClass{}\n    if err := v.client.Get(ctx, types.NamespacedName{\n        Name: db.Spec.StorageClassName}, sc); err != nil {\n        return admission.Denied(fmt.Sprintf(\n            "referenced StorageClass %q not found", db.Spec.StorageClassName))\n    }\n\n    // Check quota\n    dbList := &v1alpha1.DatabaseList{}\n    v.client.List(ctx, dbList, client.InNamespace(db.Namespace))\n    if len(dbList.Items) >= 5 {\n        return admission.Denied("maximum 5 databases per namespace")\n    }\n\n    return admission.Allowed("")\n}',
    tags: ['kubernetes', 'validation', 'cross-resource'],
  },
  {
    id: 'k8s-crds-l3-036',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'What is an operator bundle index and how is it used?',
    options: [
      'A search index for finding operators',
      'A container image containing a database of operator bundles used as a CatalogSource',
      'An index of all CRDs in the cluster',
      'A metric index for operator performance',
    ],
    correctAnswer: 1,
    explanation: 'A bundle index (catalog image) contains a database of operator bundles. OLM uses it as a CatalogSource to discover and install operators.',
    codeSnippet: '# Build catalog index image\nopm index add \\\n  --bundles example.com/my-operator-bundle:v0.1.0,example.com/my-operator-bundle:v0.2.0 \\\n  --tag example.com/my-operator-index:latest\n\n# Use as CatalogSource\napiVersion: operators.coreos.com/v1alpha1\nkind: CatalogSource\nmetadata:\n  name: my-operator-catalog\n  namespace: olm\nspec:\n  sourceType: grpc\n  image: example.com/my-operator-index:latest\n  displayName: My Operator Catalog',
    tags: ['kubernetes', 'olm', 'catalog-index'],
  },
  {
    id: 'k8s-crds-l3-037',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement metrics collection in an operator?',
    options: [
      'Write logs and parse them',
      'Use the controller-runtime metrics server and register custom Prometheus metrics',
      'Deploy a separate monitoring agent',
      'Use Kubernetes built-in metrics only',
    ],
    correctAnswer: 1,
    explanation: 'controller-runtime exposes a Prometheus metrics endpoint. Register custom metrics using the prometheus/client_golang library to expose operator-specific measurements.',
    codeSnippet: 'import "github.com/prometheus/client_golang/prometheus"\nimport "sigs.k8s.io/controller-runtime/pkg/metrics"\n\nvar (\n    reconcileCount = prometheus.NewCounterVec(\n        prometheus.CounterOpts{\n            Name: "operator_reconcile_total",\n            Help: "Total number of reconciliations",\n        },\n        []string{"resource", "result"},\n    )\n    reconcileDuration = prometheus.NewHistogramVec(\n        prometheus.HistogramOpts{\n            Name: "operator_reconcile_duration_seconds",\n            Help: "Duration of reconciliation in seconds",\n            Buckets: prometheus.DefBuckets,\n        },\n        []string{"resource"},\n    )\n)\n\nfunc init() {\n    metrics.Registry.MustRegister(reconcileCount, reconcileDuration)\n}',
    tags: ['kubernetes', 'operator-sdk', 'metrics'],
  },
  {
    id: 'k8s-crds-l3-038',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "adopt and release" pattern for managing pre-existing resources?',
    options: [
      'Creating resources and then abandoning them',
      'Taking ownership of existing resources by adding owner references, and releasing them by removing owner references',
      'Importing resources from another cluster',
      'Converting ConfigMaps to custom resources',
    ],
    correctAnswer: 1,
    explanation: 'The adopt pattern takes ownership of existing resources by adding owner references. Release removes them. This is useful when migrating to an operator without recreating resources.',
    tags: ['kubernetes', 'reconciliation', 'adopt-release'],
  },
  {
    id: 'k8s-crds-l3-039',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'How do you handle large CRD objects that exceed the etcd size limit?',
    options: [
      'Increase the etcd size limit',
      'Split the resource into multiple smaller CRDs, use references between them, and consider using configmaps for large data',
      'Compress the YAML',
      'Use a different storage backend',
    ],
    correctAnswer: 1,
    explanation: 'etcd has a default 1.5MB per-object limit. Split large resources into related smaller CRDs or store large data externally with references in the CR.',
    tags: ['kubernetes', 'crd-definition', 'size-limits'],
  },
  {
    id: 'k8s-crds-l3-040',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that uses unstructured objects instead of typed Go structs?',
    options: [
      'You cannot use unstructured objects with controllers',
      'Use unstructured.Unstructured with dynamic client for CRDs without Go type definitions',
      'Use map[string]interface{} directly',
      'Use YAML parsing in the controller',
    ],
    correctAnswer: 1,
    explanation: 'Unstructured objects allow working with any Kubernetes resource without Go type definitions, useful for generic controllers or dynamic CRD handling.',
    codeSnippet: 'u := &unstructured.Unstructured{}\nu.SetGroupVersionKind(schema.GroupVersionKind{\n    Group:   "cache.example.com",\n    Version: "v1alpha1",\n    Kind:    "Memcached",\n})\n\nerr := r.Get(ctx, req.NamespacedName, u)\nif err != nil {\n    return ctrl.Result{}, err\n}\n\n// Access fields dynamically\nspec, found, err := unstructured.NestedMap(u.Object, "spec")\nreplicas, found, err := unstructured.NestedInt64(u.Object, "spec", "replicas")',
    tags: ['kubernetes', 'controllers', 'unstructured'],
  },
  {
    id: 'k8s-crds-l3-041',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'What is the reinvocationPolicy in webhook configuration?',
    options: [
      'How often to retry failed webhooks',
      'Whether the webhook should be called again after other mutating webhooks modify the object',
      'The policy for restarting the webhook server',
      'How often to refresh the webhook certificate',
    ],
    correctAnswer: 1,
    explanation: 'reinvocationPolicy: IfNeeded causes the webhook to be called again if other mutating webhooks modify the object after this webhook ran, ensuring consistency.',
    tags: ['kubernetes', 'webhooks', 'reinvocation'],
  },
  {
    id: 'k8s-crds-l3-042',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How should an operator implement health checking for its managed application?',
    options: [
      'Rely on Kubernetes readiness probes only',
      'Implement application-aware health checks in the reconciliation loop and report via status conditions',
      'Use external monitoring tools only',
      'Check container restart counts only',
    ],
    correctAnswer: 1,
    explanation: 'Beyond pod readiness, operators should perform application-level health checks (e.g., database connectivity, replication lag) and report findings through status conditions.',
    tags: ['kubernetes', 'operator-pattern', 'health-checking'],
  },
  {
    id: 'k8s-crds-l3-043',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'api-versioning',
    question: 'What is the storedVersions field in CRD status and why is it important?',
    options: [
      'It lists all versions ever defined',
      'It lists versions of objects currently stored in etcd; important for safe version removal',
      'It shows the current storage version',
      'It tracks API server versions',
    ],
    correctAnswer: 1,
    explanation: 'storedVersions in CRD status tracks which versions have objects stored in etcd. You cannot remove a version from the CRD spec while it appears in storedVersions.',
    codeSnippet: '# Check stored versions\nkubectl get crd crontabs.example.com -o jsonpath=\'{.status.storedVersions}\'\n# Output: ["v1alpha1","v1"]\n\n# After migrating all objects to v1, update storedVersions\n# This happens automatically when all objects are re-saved in the new version',
    tags: ['kubernetes', 'api-versioning', 'stored-versions'],
  },
  {
    id: 'k8s-crds-l3-044',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that processes items in a specific order (e.g., by priority)?',
    options: [
      'Kubernetes controllers always process in FIFO order and this cannot be changed',
      'Use custom work queue with priority queue implementation and custom rate limiter',
      'Sort the resources by label',
      'Use multiple controllers with different priorities',
    ],
    correctAnswer: 1,
    explanation: 'You can implement a custom work queue that orders items by priority, though care must be taken not to starve lower-priority items.',
    tags: ['kubernetes', 'controllers', 'ordering'],
  },
  {
    id: 'k8s-crds-l3-045',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is "server-side apply" and how does it benefit operator development?',
    options: [
      'It is a feature for server deployment',
      'It tracks field ownership per manager, enabling conflict detection and safe multi-actor resource management',
      'It applies YAML on the server instead of the client',
      'It is the default kubectl apply behavior',
    ],
    correctAnswer: 1,
    explanation: 'Server-side apply tracks field ownership per field manager. This prevents different controllers from accidentally overwriting each other\'s fields on the same resource.',
    codeSnippet: '// Apply with field ownership tracking\nopts := []client.PatchOption{\n    client.ForceOwnership,\n    client.FieldOwner("my-operator"),\n}\nerr := r.Patch(ctx, desired, client.Apply, opts...)\n\n// Now only "my-operator" owns the fields it set\n// Other managers can set different fields without conflict',
    tags: ['kubernetes', 'reconciliation', 'server-side-apply'],
  },
  {
    id: 'k8s-crds-l3-046',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement end-to-end tests for an operator?',
    options: [
      'envtest is sufficient for all testing',
      'Use a real or ephemeral cluster (kind/k3d), deploy the operator, create CRs, and validate behavior',
      'Use unit tests with mocks only',
      'Manual testing in production',
    ],
    correctAnswer: 1,
    explanation: 'E2E tests deploy the operator to a real cluster (often using kind or k3d), create custom resources, and verify the operator correctly manages the application.',
    codeSnippet: '# Create a kind cluster for e2e testing\nkind create cluster --name operator-e2e\n\n# Deploy the operator\nmake deploy IMG=my-operator:test\n\n# Run e2e tests\ngo test ./test/e2e/ -v -count=1 -timeout 300s\n\n# Cleanup\nkind delete cluster --name operator-e2e',
    tags: ['kubernetes', 'operator-sdk', 'e2e-testing'],
  },
  {
    id: 'k8s-crds-l3-047',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'custom-resources',
    question: 'How do you implement pagination when listing large numbers of custom resources?',
    options: [
      'Custom resources do not support pagination',
      'Use the limit and continue parameters in List requests',
      'Use offset and limit like SQL',
      'Fetch all and paginate client-side',
    ],
    correctAnswer: 1,
    explanation: 'The Kubernetes API supports pagination using limit (number of items per page) and continue (token for the next page) parameters.',
    codeSnippet: '// In controller code\nopts := []client.ListOption{\n    client.InNamespace("default"),\n    client.Limit(100),\n}\nlist := &v1alpha1.MyResourceList{}\nfor {\n    if err := r.List(ctx, list, opts...); err != nil {\n        return err\n    }\n    for _, item := range list.Items {\n        // process item\n    }\n    if list.Continue == "" {\n        break\n    }\n    opts = append(opts, client.Continue(list.Continue))\n}',
    tags: ['kubernetes', 'custom-resources', 'pagination'],
  },
  {
    id: 'k8s-crds-l3-048',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you use annotation-based webhook injection with cert-manager?',
    options: [
      'Manually inject certificates',
      'Add cert-manager.io/inject-ca-from annotation to the webhook configuration',
      'Use a separate certificate injector',
      'Cert-manager does not support webhook injection',
    ],
    correctAnswer: 1,
    explanation: 'cert-manager CA injector watches for the annotation and automatically injects the CA bundle from the referenced Certificate into the webhook configuration.',
    codeSnippet: 'apiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingWebhookConfiguration\nmetadata:\n  name: my-webhook\n  annotations:\n    cert-manager.io/inject-ca-from: system/webhook-certificate\nwebhooks:\n  - name: validate.example.com\n    clientConfig:\n      service:\n        name: webhook-service\n        namespace: system\n        path: /validate\n      # caBundle is automatically injected by cert-manager',
    tags: ['kubernetes', 'webhooks', 'cert-manager'],
  },
  {
    id: 'k8s-crds-l3-049',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'status-subresource',
    question: 'How do you implement a phased reconciliation status (e.g., Pending -> Provisioning -> Running)?',
    options: [
      'Use labels to track phase',
      'Set a phase field in status and use conditions for detailed sub-states',
      'Use annotations for phase tracking',
      'Use separate CRDs for each phase',
    ],
    correctAnswer: 1,
    explanation: 'Use status.phase for the high-level lifecycle state and status.conditions for detailed sub-states. This follows the Kubernetes convention used by Pods and PVs.',
    codeSnippet: 'type DatabaseStatus struct {\n    Phase      DatabasePhase    `json:"phase,omitempty"`\n    Conditions []metav1.Condition `json:"conditions,omitempty"`\n}\n\ntype DatabasePhase string\nconst (\n    PhasePending      DatabasePhase = "Pending"\n    PhaseProvisioning DatabasePhase = "Provisioning"\n    PhaseRunning      DatabasePhase = "Running"\n    PhaseFailed       DatabasePhase = "Failed"\n    PhaseTerminating  DatabasePhase = "Terminating"\n)\n\n// In reconciliation\nswitch cr.Status.Phase {\ncase PhasePending:\n    // Start provisioning\ncase PhaseProvisioning:\n    // Check if provisioning is complete\ncase PhaseRunning:\n    // Monitor and maintain\n}',
    tags: ['kubernetes', 'status-subresource', 'phases'],
  },
  {
    id: 'k8s-crds-l3-050',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the mapper pattern for watching unowned resources in controller-runtime?',
    options: [
      'A map data structure for caching resources',
      'A handler that maps events on secondary resources to reconcile requests for the primary resource',
      'A mapping between CRD versions',
      'A network address mapping for services',
    ],
    correctAnswer: 1,
    explanation: 'The mapper (EnqueueRequestsFromMapFunc) allows watching resources not owned by the primary resource and mapping their events to reconcile the correct primary resource.',
    codeSnippet: 'ctrl.NewControllerManagedBy(mgr).\n    For(&v1alpha1.Database{}).\n    Watches(\n        &corev1.Secret{},\n        handler.EnqueueRequestsFromMapFunc(func(ctx context.Context, obj client.Object) []reconcile.Request {\n            // Find databases that reference this secret\n            dbList := &v1alpha1.DatabaseList{}\n            mgr.GetClient().List(ctx, dbList)\n            var requests []reconcile.Request\n            for _, db := range dbList.Items {\n                if db.Spec.CredentialSecret == obj.GetName() {\n                    requests = append(requests, reconcile.Request{\n                        NamespacedName: types.NamespacedName{\n                            Name: db.Name, Namespace: db.Namespace,\n                        },\n                    })\n                }\n            }\n            return requests\n        }),\n    ).\n    Complete(r)',
    tags: ['kubernetes', 'controllers', 'mapper'],
  },
  {
    id: 'k8s-crds-l3-051',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'What is the "cluster singleton" pattern for operators?',
    options: [
      'Running only one pod of any kind in the cluster',
      'Ensuring exactly one instance of an operator-managed application exists cluster-wide using a cluster-scoped CR',
      'A design pattern for single-node clusters',
      'Running the operator on a single node',
    ],
    correctAnswer: 1,
    explanation: 'The cluster singleton pattern uses a cluster-scoped CR (often with a fixed name) to ensure only one instance of a managed application exists cluster-wide.',
    tags: ['kubernetes', 'operator-pattern', 'singleton'],
  },
  {
    id: 'k8s-crds-l3-052',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you handle CRD validation for fields that depend on feature flags or cluster capabilities?',
    options: [
      'Create different CRDs for each configuration',
      'Use a validating webhook that checks cluster capabilities at runtime',
      'Use optional fields only',
      'Skip validation entirely',
    ],
    correctAnswer: 1,
    explanation: 'A validating webhook can check cluster capabilities (API versions, features) at runtime to conditionally validate fields that depend on the cluster environment.',
    tags: ['kubernetes', 'validation', 'dynamic'],
  },
  {
    id: 'k8s-crds-l3-053',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you implement a two-phase commit pattern in an operator managing distributed resources?',
    options: [
      'Use database transactions',
      'Use a prepare phase (create resources in a paused state), verify all succeed, then activate (unpause) all',
      'Create all resources simultaneously',
      'Two-phase commit is not applicable to Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Create resources in a preliminary state (e.g., with 0 replicas or suspended), verify all creation succeeds, then update them to active state in a second phase.',
    tags: ['kubernetes', 'reconciliation', 'distributed'],
  },
  {
    id: 'k8s-crds-l3-054',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'How does OLM handle API conflict resolution when multiple operators provide the same CRD?',
    options: [
      'It installs both operators',
      'OLM prevents installation of operators that conflict on provided APIs/CRDs',
      'It merges the CRDs',
      'The last installed operator wins',
    ],
    correctAnswer: 1,
    explanation: 'OLM tracks which operators provide which APIs/CRDs and prevents installing conflicting operators that would both try to manage the same CRD.',
    tags: ['kubernetes', 'olm', 'conflict-resolution'],
  },
  {
    id: 'k8s-crds-l3-055',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What are the tradeoffs of using cached vs. uncached reads in controller-runtime?',
    options: [
      'There are no tradeoffs; always use cache',
      'Cached reads are faster but may be stale; uncached reads are always current but add API server load',
      'Uncached reads are faster',
      'Cache only works for built-in resources',
    ],
    correctAnswer: 1,
    explanation: 'Cached reads (default) are fast but may be slightly stale. For operations requiring the latest data (e.g., before status updates), use direct API reads.',
    codeSnippet: '// Default: reads from cache (may be stale)\nerr := r.Get(ctx, req.NamespacedName, obj)\n\n// Direct API read (always current, slower)\nerr := r.Client.Get(ctx, req.NamespacedName, obj, &client.GetOptions{})\n\n// Configure uncached objects in manager\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    Client: client.Options{\n        Cache: &client.CacheOptions{\n            DisableFor: []client.Object{&corev1.Secret{}},\n        },\n    },\n})',
    tags: ['kubernetes', 'controllers', 'cache-tradeoffs'],
  },
  {
    id: 'k8s-crds-l3-056',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'finalizers',
    question: 'How do you implement a timeout for finalizer cleanup operations?',
    options: [
      'Set a timeout in the finalizer string',
      'Use context with deadline in the cleanup logic and provide manual override escape hatch',
      'Kubernetes automatically times out finalizers',
      'Use terminationGracePeriodSeconds',
    ],
    correctAnswer: 1,
    explanation: 'Implement timeouts using context deadlines. Track attempts in status or annotations. Provide an annotation-based escape hatch to skip cleanup if it cannot complete.',
    codeSnippet: 'func (r *Reconciler) handleDeletion(ctx context.Context, cr *v1alpha1.Resource) (ctrl.Result, error) {\n    // Check for force-delete annotation\n    if cr.Annotations["force-delete"] == "true" {\n        controllerutil.RemoveFinalizer(cr, finalizerName)\n        return ctrl.Result{}, r.Update(ctx, cr)\n    }\n\n    // Track cleanup attempt time\n    cleanupStart := cr.DeletionTimestamp.Time\n    if time.Since(cleanupStart) > 10*time.Minute {\n        // Cleanup timed out - log error but remove finalizer\n        log.Error(nil, "cleanup timed out, removing finalizer", "resource", cr.Name)\n        controllerutil.RemoveFinalizer(cr, finalizerName)\n        return ctrl.Result{}, r.Update(ctx, cr)\n    }\n\n    // Attempt cleanup\n    if err := r.cleanup(ctx, cr); err != nil {\n        return ctrl.Result{RequeueAfter: 30 * time.Second}, nil\n    }\n    controllerutil.RemoveFinalizer(cr, finalizerName)\n    return ctrl.Result{}, r.Update(ctx, cr)\n}',
    tags: ['kubernetes', 'finalizers', 'timeout'],
  },
  {
    id: 'k8s-crds-l3-057',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'What is the "canary deployment" pattern for operator-managed applications?',
    options: [
      'A deployment with yellow containers',
      'Gradually rolling out changes by deploying new version alongside old, routing partial traffic, and validating before full rollout',
      'Deploying to canary islands data center',
      'A deployment that alerts on failures',
    ],
    correctAnswer: 1,
    explanation: 'Operators can implement canary deployments by managing traffic splitting, monitoring error rates, and automatically rolling back if the canary shows degradation.',
    tags: ['kubernetes', 'operator-pattern', 'canary'],
  },
  {
    id: 'k8s-crds-l3-058',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'What is x-kubernetes-embedded-resource and when should you use it?',
    options: [
      'To embed binary files in CRDs',
      'To indicate a field contains a full Kubernetes object (with apiVersion, kind, metadata) as an embedded resource',
      'To reference external resources',
      'To embed CRDs within CRDs',
    ],
    correctAnswer: 1,
    explanation: 'x-kubernetes-embedded-resource: true marks a field that contains a complete Kubernetes object. It automatically preserves apiVersion, kind, and metadata without explicit schema.',
    codeSnippet: 'properties:\n  template:\n    type: object\n    x-kubernetes-embedded-resource: true\n    x-kubernetes-preserve-unknown-fields: true\n    description: "A PodTemplate to use for the managed pods"',
    tags: ['kubernetes', 'crd-definition', 'embedded-resource'],
  },
  {
    id: 'k8s-crds-l3-059',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you implement ordered reconciliation for resources with dependencies?',
    options: [
      'Reconcile all resources at once',
      'Use a DAG-based reconciliation that processes resources in dependency order and returns early if prerequisites are not met',
      'Create separate controllers for each dependency level',
      'Use finalizers to order reconciliation',
    ],
    correctAnswer: 1,
    explanation: 'Implement dependency tracking in the reconciler. Check prerequisites before proceeding. Return Requeue if dependencies are not ready yet.',
    tags: ['kubernetes', 'reconciliation', 'dependency-ordering'],
  },
  {
    id: 'k8s-crds-l3-060',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you test admission webhooks in a CI/CD pipeline?',
    options: [
      'Skip webhook testing in CI/CD',
      'Use envtest with WebhookInstallOptions to start a local webhook server',
      'Deploy to production and test manually',
      'Mock the webhook entirely',
    ],
    correctAnswer: 1,
    explanation: 'envtest supports webhook testing by starting a local webhook server. Configure WebhookInstallOptions with the webhook configuration path.',
    codeSnippet: 'testEnv = &envtest.Environment{\n    CRDDirectoryPaths: []string{filepath.Join("..", "config", "crd", "bases")},\n    WebhookInstallOptions: envtest.WebhookInstallOptions{\n        Paths: []string{filepath.Join("..", "config", "webhook")},\n    },\n}\ncfg, err := testEnv.Start()\n\n// Setup webhook server\nwebhookServer := mgr.GetWebhookServer()\nwebhookServer.Register("/validate-v1alpha1-memcached",\n    &webhook.Admission{Handler: &MemcachedValidator{}})\n\n// Test: attempt to create invalid resource\ninvalidCR := &v1alpha1.Memcached{Spec: v1alpha1.MemcachedSpec{Size: -1}}\nerr = k8sClient.Create(ctx, invalidCR)\nExpect(err).To(HaveOccurred()) // Should be rejected by webhook',
    tags: ['kubernetes', 'webhooks', 'testing'],
  },
  {
    id: 'k8s-crds-l3-061',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement a Helm-based operator that supports custom values?',
    options: [
      'Pass values through environment variables',
      'The custom resource spec is automatically mapped to Helm values for the chart',
      'Create a separate ConfigMap for values',
      'Edit the chart templates directly',
    ],
    correctAnswer: 1,
    explanation: 'In Helm-based operators, the custom resource spec fields are automatically passed as values to the Helm chart during reconciliation.',
    codeSnippet: '# Custom Resource\napiVersion: example.com/v1alpha1\nkind: Nginx\nmetadata:\n  name: my-nginx\nspec:\n  replicaCount: 3\n  image:\n    repository: nginx\n    tag: "1.25"\n  service:\n    type: LoadBalancer\n    port: 80\n\n# These spec fields are passed as values to the Helm chart\n# equivalent to: helm install --set replicaCount=3,image.repository=nginx,...',
    tags: ['kubernetes', 'operator-sdk', 'helm-values'],
  },
  {
    id: 'k8s-crds-l3-062',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the "eventual consistency" challenge in operator design and how do you address it?',
    options: [
      'Kubernetes is always strongly consistent',
      'Due to caching and async operations, the operator may see stale data; address with retry, status tracking, and optimistic concurrency',
      'Use distributed transactions',
      'Run reconciliation synchronously',
    ],
    correctAnswer: 1,
    explanation: 'The informer cache may be stale, and external operations are async. Handle this with retries, tracking progress in status, and using resourceVersion for concurrency control.',
    tags: ['kubernetes', 'controllers', 'eventual-consistency'],
  },
  {
    id: 'k8s-crds-l3-063',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'What is the cost budget for CEL validation rules and how does it affect complex validations?',
    options: [
      'CEL rules have no execution limits',
      'Each CEL rule has a cost budget based on estimated execution time; complex rules may exceed the budget and be rejected',
      'Cost is only measured in memory usage',
      'Cost budget only applies to webhook validations',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes estimates CEL rule cost during CRD creation. Rules exceeding the cost budget (based on data type sizes and operations) are rejected to prevent API server DoS.',
    tags: ['kubernetes', 'validation', 'cel-cost'],
  },
  {
    id: 'k8s-crds-l3-064',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'How do you implement a custom update strategy with OLM beyond simple replacement?',
    options: [
      'OLM only supports replacement updates',
      'Use CSV install strategy with custom deployment specs, and implement update logic in the operator itself',
      'Use Helm hooks with OLM',
      'Override OLM behavior with annotations',
    ],
    correctAnswer: 1,
    explanation: 'While OLM handles the operator deployment update, complex application upgrade logic (like database migrations) should be implemented within the operator reconciliation loop.',
    tags: ['kubernetes', 'olm', 'update-strategy'],
  },
  {
    id: 'k8s-crds-l3-065',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'owner-references',
    question: 'How do you handle owner references when the owner is in a different API group than the dependent?',
    options: [
      'Cross-group owner references are not allowed',
      'Set the apiVersion field in the ownerReference to the owner GroupVersion and it works across API groups',
      'Use a common API group',
      'Use labels instead of owner references',
    ],
    correctAnswer: 1,
    explanation: 'Owner references work across API groups. The ownerReference includes the apiVersion and kind of the owner, which can be from any API group.',
    codeSnippet: '// Owner is a custom resource from cache.example.com/v1alpha1\n// Dependent is a core/v1 Service\nownerRef := metav1.OwnerReference{\n    APIVersion:         "cache.example.com/v1alpha1",\n    Kind:               "Memcached",\n    Name:               owner.Name,\n    UID:                owner.UID,\n    Controller:         pointer.Bool(true),\n    BlockOwnerDeletion: pointer.Bool(true),\n}\nservice.OwnerReferences = append(service.OwnerReferences, ownerRef)',
    tags: ['kubernetes', 'owner-references', 'cross-group'],
  },
  {
    id: 'k8s-crds-l3-066',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement rolling upgrade support for a stateful application managed by an operator?',
    options: [
      'Use Deployment rolling update strategy',
      'Implement custom rolling update logic that respects application quorum, data consistency, and node ordering',
      'Delete all pods and recreate them',
      'Use StatefulSet and Kubernetes handles everything',
    ],
    correctAnswer: 1,
    explanation: 'For stateful apps, the operator should manage rolling updates with application-aware logic: checking quorum, replication state, node ordering, and readiness before proceeding.',
    tags: ['kubernetes', 'operator-pattern', 'rolling-upgrade'],
  },
  {
    id: 'k8s-crds-l3-067',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'How do you implement a CRD that supports both spec and data sections (like ConfigMap)?',
    options: [
      'Use two separate CRDs',
      'Define both spec and data as top-level fields in the schema, using spec for configuration and data for content',
      'Use annotations for data',
      'Only spec is supported in CRDs',
    ],
    correctAnswer: 1,
    explanation: 'CRDs can have custom top-level fields beyond spec and status. Define them in the schema, though following the spec/status convention is recommended.',
    codeSnippet: 'openAPIV3Schema:\n  type: object\n  properties:\n    spec:\n      type: object\n      properties:\n        retention:\n          type: string\n    data:\n      type: object\n      additionalProperties:\n        type: string\n      description: "Key-value data stored by this resource"',
    tags: ['kubernetes', 'crd-definition', 'custom-fields'],
  },
  {
    id: 'k8s-crds-l3-068',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "status patch" pattern and when should you use it over status update?',
    options: [
      'Status patching and updating are identical',
      'Status patch sends only changed fields, reducing conflict risk when multiple controllers update different status fields',
      'Status patch is for spec changes',
      'Status patch is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Status patch (r.Status().Patch) sends only the changed fields, reducing the chance of conflicts when multiple controllers or reconciliation loops update different status fields.',
    codeSnippet: '// Status Patch - safer for concurrent updates\npatch := client.MergeFrom(cr.DeepCopy())\ncr.Status.ReadyReplicas = readyCount\nmeta.SetStatusCondition(&cr.Status.Conditions, newCondition)\nerr := r.Status().Patch(ctx, cr, patch)\n\n// vs Status Update - replaces entire status\ncr.Status.ReadyReplicas = readyCount\nerr := r.Status().Update(ctx, cr)',
    tags: ['kubernetes', 'reconciliation', 'status-patch'],
  },
  {
    id: 'k8s-crds-l3-069',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you implement dry-run support in a mutating webhook?',
    options: [
      'Webhooks automatically support dry-run',
      'Check the dryRun field in the AdmissionRequest and skip side effects when true',
      'Dry-run is not supported for webhooks',
      'Return a special dry-run response',
    ],
    correctAnswer: 1,
    explanation: 'When dryRun is true in the admission request, the webhook should still return mutations but must not perform any side effects (external API calls, resource creation, etc.).',
    codeSnippet: 'func (h *Handler) Handle(ctx context.Context, req admission.Request) admission.Response {\n    if req.DryRun != nil && *req.DryRun {\n        // Still return mutations, but skip side effects\n        return h.mutateWithoutSideEffects(ctx, req)\n    }\n\n    // Normal path with side effects\n    return h.mutateWithSideEffects(ctx, req)\n}',
    tags: ['kubernetes', 'webhooks', 'dry-run'],
  },
  {
    id: 'k8s-crds-l3-070',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you configure operator resource limits and requests best practices?',
    options: [
      'Resource limits are not needed for operators',
      'Set appropriate CPU/memory requests and limits based on profiling, and consider the number of watched resources',
      'Use the default limits',
      'Set limits as high as possible',
    ],
    correctAnswer: 1,
    explanation: 'Operator resource usage scales with the number of watched resources (informer cache). Profile under realistic loads and set limits accordingly.',
    codeSnippet: '# In the operator deployment\nresources:\n  requests:\n    cpu: 100m\n    memory: 128Mi\n  limits:\n    cpu: 500m\n    memory: 512Mi\n# Note: memory should account for informer cache size\n# Cache memory ≈ number_of_watched_resources × average_resource_size',
    tags: ['kubernetes', 'operator-sdk', 'resource-management'],
  },
  {
    id: 'k8s-crds-l3-071',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that reacts to Kubernetes events (Event objects) rather than resource changes?',
    options: [
      'Watch core/v1 Events directly and filter by reason',
      'Events cannot be watched by controllers',
      'Use a separate event processor',
      'Parse kubelet logs',
    ],
    correctAnswer: 0,
    explanation: 'Controllers can watch Event objects like any other resource. Filter events by reason, involved object, or type to trigger specific reconciliation actions.',
    codeSnippet: 'ctrl.NewControllerManagedBy(mgr).\n    For(&corev1.Event{}, builder.WithPredicates(\n        predicate.NewPredicateFuncs(func(obj client.Object) bool {\n            event := obj.(*corev1.Event)\n            return event.Reason == "BackOff" &&\n                   event.InvolvedObject.Kind == "Pod"\n        }),\n    )).\n    Complete(r)',
    tags: ['kubernetes', 'controllers', 'event-watching'],
  },
  {
    id: 'k8s-crds-l3-072',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'api-versioning',
    question: 'How do you handle data loss during CRD version conversion when a field exists in v2 but not v1?',
    options: [
      'Data loss is unavoidable and acceptable',
      'Store extra data in annotations during conversion to preserve fields that do not map to the target version',
      'Reject the conversion',
      'Always use the latest version only',
    ],
    correctAnswer: 1,
    explanation: 'When converting from a version with more fields to one with fewer, store unmapped data in annotations to preserve it for round-trip conversion.',
    codeSnippet: 'func (src *MemcachedV2) ConvertTo(dstRaw conversion.Hub) error {\n    dst := dstRaw.(*MemcachedV1)\n    dst.Spec.Replicas = src.Spec.Size\n\n    // Store v2-only fields in annotations to prevent data loss\n    if src.Spec.AdvancedConfig != "" {\n        if dst.Annotations == nil {\n            dst.Annotations = map[string]string{}\n        }\n        dst.Annotations["cache.example.com/advanced-config"] = src.Spec.AdvancedConfig\n    }\n    return nil\n}',
    tags: ['kubernetes', 'api-versioning', 'data-preservation'],
  },
  {
    id: 'k8s-crds-l3-073',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement tenant isolation in a multi-tenant operator?',
    options: [
      'Deploy separate operators per tenant',
      'Use namespace isolation, RBAC per tenant, and webhook-enforced policies for resource quotas and access',
      'Use a single namespace for all tenants',
      'Multi-tenancy is not possible with operators',
    ],
    correctAnswer: 1,
    explanation: 'Multi-tenant operators use namespace-per-tenant isolation, RBAC to restrict CR access, webhooks to enforce quotas, and label-based resource segregation.',
    tags: ['kubernetes', 'operator-pattern', 'multi-tenancy'],
  },
  {
    id: 'k8s-crds-l3-074',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "generation skew" problem and how do controllers handle it?',
    options: [
      'When different nodes run different Kubernetes versions',
      'When the controller processes an older generation of a resource because a newer update has already been made',
      'When CRD versions are out of sync',
      'When container images have different versions',
    ],
    correctAnswer: 1,
    explanation: 'Generation skew occurs when the cache is behind the latest version. Controllers handle this by being idempotent and re-reconciling when the cache catches up.',
    tags: ['kubernetes', 'reconciliation', 'generation-skew'],
  },
  {
    id: 'k8s-crds-l3-075',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'custom-resources',
    question: 'How do you implement table-based output for custom resources (kubectl get with custom columns)?',
    options: [
      'Use kubectl with -o custom-columns flag',
      'Define additionalPrinterColumns in the CRD and optionally implement the Table API endpoint',
      'Create a kubectl plugin',
      'Use labels for column data',
    ],
    correctAnswer: 1,
    explanation: 'CRD additionalPrinterColumns define the default table output. For advanced formatting, implement the /apis/<group>/<version>/namespaces/<ns>/<resource> endpoint with Table format.',
    tags: ['kubernetes', 'custom-resources', 'table-output'],
  },
  {
    id: 'k8s-crds-l3-076',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you handle the "thundering herd" problem when an operator restarts?',
    options: [
      'It is not a problem for operators',
      'Use resync period with jitter, rate limiting on the work queue, and phased startup',
      'Restart one pod at a time',
      'Increase API server capacity',
    ],
    correctAnswer: 1,
    explanation: 'When an operator restarts, all watched resources are re-listed and may flood the work queue. Rate limiting, jitter on resync, and phased processing prevent overwhelming the API server.',
    tags: ['kubernetes', 'controllers', 'thundering-herd'],
  },
  {
    id: 'k8s-crds-l3-077',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'status-subresource',
    question: 'How do you implement a progress indicator in the status of a long-running operation?',
    options: [
      'Use percentage in an annotation',
      'Update status conditions with progress details and use a progress field in status',
      'Send progress events to a message queue',
      'Log progress to stdout',
    ],
    correctAnswer: 1,
    explanation: 'Use status fields for progress tracking (percentage, current step) and conditions for phase changes. Periodically update status during long-running reconciliation.',
    codeSnippet: 'type MigrationStatus struct {\n    Phase       string             `json:"phase"`\n    Progress    int                `json:"progress"`       // 0-100\n    CurrentStep string             `json:"currentStep"`\n    TotalSteps  int                `json:"totalSteps"`\n    CompletedAt *metav1.Time       `json:"completedAt,omitempty"`\n    Conditions  []metav1.Condition `json:"conditions,omitempty"`\n}',
    tags: ['kubernetes', 'status-subresource', 'progress'],
  },
  {
    id: 'k8s-crds-l3-078',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you use CEL transition rules to validate update operations differently from create?',
    options: [
      'CEL rules apply equally to all operations',
      'Use oldSelf variable in transition rules which is only available during UPDATE operations',
      'Use a separate webhook for updates',
      'Transition rules are defined in a different schema section',
    ],
    correctAnswer: 1,
    explanation: 'CEL transition rules use the oldSelf variable (previous value) which is only populated during UPDATE operations, allowing update-specific validation.',
    codeSnippet: 'properties:\n  spec:\n    type: object\n    properties:\n      size:\n        type: integer\n        x-kubernetes-validations:\n          - rule: "self >= oldSelf"\n            message: "size can only be increased, not decreased"\n            # oldSelf is only available during UPDATE\n          - rule: "self <= 100"\n            message: "size must not exceed 100"\n            # This applies to both CREATE and UPDATE',
    tags: ['kubernetes', 'validation', 'cel-transitions'],
  },
  {
    id: 'k8s-crds-l3-079',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement a hybrid operator that uses both Go controllers and Helm charts?',
    options: [
      'This is not possible with a single operator',
      'Use the Go-based operator and invoke Helm libraries programmatically for chart rendering',
      'Use two separate operators',
      'Convert the Helm chart to Go code',
    ],
    correctAnswer: 1,
    explanation: 'A Go-based operator can use the Helm Go SDK to render and apply chart templates programmatically, combining custom Go logic with Helm chart management.',
    tags: ['kubernetes', 'operator-sdk', 'hybrid'],
  },
  {
    id: 'k8s-crds-l3-080',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'What is the purpose of the olm.skipRange annotation in a CSV?',
    options: [
      'To skip certain Kubernetes versions',
      'To allow OLM to upgrade from any version in the specified semver range, skipping intermediate versions',
      'To skip health checks during upgrade',
      'To define a range of namespaces to skip',
    ],
    correctAnswer: 1,
    explanation: 'olm.skipRange allows upgrading from any CSV version matching the semver range, providing more flexible upgrade paths than explicit replaces chains.',
    codeSnippet: 'apiVersion: operators.coreos.com/v1alpha1\nkind: ClusterServiceVersion\nmetadata:\n  name: my-operator.v2.0.0\n  annotations:\n    olm.skipRange: ">=1.0.0 <2.0.0"\nspec:\n  version: 2.0.0\n  # This CSV can replace any version from 1.0.0 to 1.x.x',
    tags: ['kubernetes', 'olm', 'skip-range'],
  },
  {
    id: 'k8s-crds-l3-081',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that needs to perform batch operations efficiently?',
    options: [
      'Process each resource individually in sequence',
      'Collect multiple items from the work queue, batch API calls, and use bulk operations where possible',
      'Use goroutines for each resource',
      'Increase MaxConcurrentReconciles to a very high number',
    ],
    correctAnswer: 1,
    explanation: 'For batch operations, debounce reconciliation requests, collect items, and perform bulk API operations (batch deletes, list-and-update) to reduce API server load.',
    tags: ['kubernetes', 'controllers', 'batch-operations'],
  },
  {
    id: 'k8s-crds-l3-082',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "slow start" pattern for operator reconciliation?',
    options: [
      'Adding a sleep at the start of reconciliation',
      'Gradually increasing the scope of reconciliation after startup to avoid overwhelming the system',
      'Starting with low CPU limits',
      'A pattern for slow network connections',
    ],
    correctAnswer: 1,
    explanation: 'After restart, gradually ramp up reconciliation activity (e.g., process a few resources first, then increase) to avoid thundering herd effects on the API server and managed systems.',
    tags: ['kubernetes', 'reconciliation', 'slow-start'],
  },
  {
    id: 'k8s-crds-l3-083',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you handle webhook certificate rotation without downtime?',
    options: [
      'Manually update certificates and restart the webhook',
      'Use cert-manager with automatic rotation, or implement a certificate controller that updates the secret and webhook configuration atomically',
      'Certificates do not need rotation',
      'Use long-lived certificates (10+ years)',
    ],
    correctAnswer: 1,
    explanation: 'cert-manager automatically rotates certificates before expiry and updates the webhook caBundle through CA injection. The webhook server detects new certificates in the mounted secret.',
    tags: ['kubernetes', 'webhooks', 'certificate-rotation'],
  },
  {
    id: 'k8s-crds-l3-084',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement disaster recovery for an operator and its managed resources?',
    options: [
      'Rely on etcd backups only',
      'Implement CRD backup/restore, document recovery procedures, and support cross-cluster resource recreation from CR specs',
      'Deploy in multiple clusters simultaneously',
      'Use PersistentVolumes for all data',
    ],
    correctAnswer: 1,
    explanation: 'DR strategy includes: backup CRs and CRDs, document restore procedures, implement reconciliation that can recreate infrastructure from CR specs, and test recovery regularly.',
    tags: ['kubernetes', 'operator-pattern', 'disaster-recovery'],
  },
  {
    id: 'k8s-crds-l3-085',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'What is the impact of large CRD schemas on API server performance?',
    options: [
      'Schema size has no impact on performance',
      'Large schemas increase API server memory usage, validation time, and CRD registration time',
      'Large schemas only affect etcd',
      'Schemas are not stored in memory',
    ],
    correctAnswer: 1,
    explanation: 'Large OpenAPI schemas consume API server memory (stored in the OpenAPI spec), increase validation time for each request, and can slow CRD registration.',
    tags: ['kubernetes', 'crd-definition', 'performance'],
  },
  {
    id: 'k8s-crds-l3-086',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'finalizers',
    question: 'How do you implement cascading deletion for custom resources that manage other custom resources?',
    options: [
      'Kubernetes handles it automatically',
      'Use owner references between CRs for automatic GC, or finalizers for custom cleanup logic across CR hierarchies',
      'Delete all CRs manually',
      'Use a batch delete API',
    ],
    correctAnswer: 1,
    explanation: 'Set owner references between parent and child CRs for automatic garbage collection. Use finalizers when custom cleanup logic is needed beyond simple deletion.',
    tags: ['kubernetes', 'finalizers', 'cascading-deletion'],
  },
  {
    id: 'k8s-crds-l3-087',
    language: 'kubernetes',
    level: 'level3',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you implement webhook-based admission with dry-run and server-side apply awareness?',
    options: [
      'Webhooks are not affected by dry-run or SSA',
      'Check request.DryRun and request.Options for SSA field manager, adapting behavior accordingly',
      'Use a separate webhook for each mode',
      'Disable SSA when using webhooks',
    ],
    correctAnswer: 1,
    explanation: 'Webhooks receive the full AdmissionRequest including dryRun flag and SubResource info. They should avoid side effects during dry-run and handle SSA field ownership correctly.',
    tags: ['kubernetes', 'validation', 'webhook-ssa'],
  },
];

export default questions;
