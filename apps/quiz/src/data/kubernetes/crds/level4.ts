import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-crds-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How does the Kubernetes API server handle the watch cache for CRDs internally?',
    options: [
      'CRDs share the same watch cache as built-in resources',
      'Each CRD resource type gets its own watch cache backed by a cacher that multiplexes etcd watches',
      'CRDs do not use watch caches',
      'The watch cache is stored in a separate etcd instance',
    ],
    correctAnswer: 1,
    explanation: 'The API server creates a separate cacher (watch cache) per resource type, including CRDs. The cacher multiplexes a single etcd watch into multiple client watches.',
    tags: ['kubernetes', 'controllers', 'api-server-internals'],
  },
  {
    id: 'k8s-crds-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "bookmark" event type in Kubernetes watches and how does it affect operator performance?',
    options: [
      'It marks the beginning of a watch stream',
      'It is a synthetic event that communicates the current resourceVersion without sending full objects, reducing re-list costs',
      'It bookmarks resources for later processing',
      'It is used to save the position in a paginated list',
    ],
    correctAnswer: 1,
    explanation: 'BOOKMARK events send only the resourceVersion, allowing informers to resume watches from a recent point without a full re-list after disconnection, significantly reducing API server load.',
    codeSnippet: '// Bookmark events are handled internally by informers\n// When the informer reconnects, it uses the last bookmark resourceVersion\n// instead of re-listing all objects\n\n// In the watch stream:\n// {"type":"BOOKMARK","object":{"metadata":{"resourceVersion":"12345"}}}\n\n// This is enabled by default in controller-runtime informers\n// Reduces re-list data transfer after network interruptions',
    tags: ['kubernetes', 'reconciliation', 'watch-bookmark'],
  },
  {
    id: 'k8s-crds-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How does the API server handle webhook timeout and what are the implications for conversion webhooks specifically?',
    options: [
      'All webhooks have a fixed 30-second timeout',
      'timeoutSeconds is configurable (1-30s, default 10); for conversion webhooks, timeout causes the entire API request to fail, potentially breaking list operations',
      'Conversion webhooks have unlimited timeout',
      'Timeout only affects mutating webhooks',
    ],
    correctAnswer: 1,
    explanation: 'Conversion webhook timeouts are critical because they affect any API operation that requires version conversion, including list operations that may need to convert stored objects.',
    codeSnippet: '# Conversion webhook timeout affects ALL operations on the CRD\n# If stored as v1alpha1 but requested as v1, conversion happens for:\n# - GET (single object conversion)\n# - LIST (batch conversion of all matching objects)\n# - WATCH (conversion of each event)\n#\n# A slow conversion webhook can make the CRD unusable\nspec:\n  conversion:\n    strategy: Webhook\n    webhook:\n      clientConfig:\n        service:\n          name: conversion-service\n          namespace: system\n          path: /convert\n      conversionReviewVersions: ["v1"]\n      # No explicit timeout - uses API server default (10s)',
    tags: ['kubernetes', 'webhooks', 'conversion-timeout'],
  },
  {
    id: 'k8s-crds-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'What is the internal implementation of CEL validation in the API server and what are its limitations?',
    options: [
      'CEL runs in a sandboxed VM',
      'CEL programs are compiled once per CRD and evaluated per request with cost budgets based on estimated worst-case complexity',
      'CEL is interpreted at runtime with no caching',
      'CEL validation is delegated to a separate service',
    ],
    correctAnswer: 1,
    explanation: 'CEL programs are compiled when the CRD is registered and cached. Each evaluation has a cost budget estimated from schema types. exceeding the budget fails the CRD registration.',
    codeSnippet: '# CEL cost estimation rules:\n# - String operations: cost proportional to maxLength\n# - Array operations: cost proportional to maxItems\n# - Nested iterations (self.all(x, self.all(y, ...))): multiplicative cost\n#\n# Without maxLength/maxItems, worst-case size is assumed, often exceeding budget\nproperties:\n  items:\n    type: array\n    maxItems: 100  # REQUIRED for reasonable CEL cost estimation\n    items:\n      type: object\n      properties:\n        name:\n          type: string\n          maxLength: 253  # Helps CEL cost estimation\n    x-kubernetes-validations:\n      - rule: "self.map(i, i.name).size() == self.size()"\n        message: "names must be unique"',
    tags: ['kubernetes', 'validation', 'cel-internals'],
  },
  {
    id: 'k8s-crds-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What happens internally when a controller-runtime informer loses its watch connection to the API server?',
    options: [
      'The controller crashes and restarts',
      'The informer attempts to re-watch from the last known resourceVersion; if too old (410 Gone), it performs a full re-list',
      'It creates a new informer instance',
      'It switches to polling mode',
    ],
    correctAnswer: 1,
    explanation: 'On watch disconnect, the informer tries to resume from the last resourceVersion. If etcd has compacted past that version (410 Gone), it triggers a full re-list and resynchronizes the cache.',
    tags: ['kubernetes', 'controllers', 'informer-internals'],
  },
  {
    id: 'k8s-crds-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'What is the significance of the "established" condition in CRD status and how does it affect API availability?',
    options: [
      'It indicates the CRD has been running for at least 24 hours',
      'It indicates the API server has accepted and registered the CRD; the API endpoints are available only after this condition is True',
      'It means the controller is running',
      'It means all custom resources have been validated',
    ],
    correctAnswer: 1,
    explanation: 'The Established condition becomes True when the CRD discovery information is available and the API endpoint is registered. NamesAccepted must also be True for proper functioning.',
    codeSnippet: '# Check CRD conditions\nkubectl get crd crontabs.example.com -o jsonpath=\'{.status.conditions}\'\n# [\n#   {"type":"NamesAccepted","status":"True","reason":"NoConflicts"},\n#   {"type":"Established","status":"True","reason":"InitialNamesAccepted"}\n# ]\n\n# Wait for CRD to be established\nkubectl wait --for=condition=Established crd/crontabs.example.com --timeout=60s\n\n# In Go:\n// Wait for CRD using a poller\nerr := wait.PollImmediate(time.Second, 30*time.Second, func() (bool, error) {\n    crd := &apiextensionsv1.CustomResourceDefinition{}\n    err := client.Get(ctx, types.NamespacedName{Name: crdName}, crd)\n    if err != nil { return false, err }\n    for _, c := range crd.Status.Conditions {\n        if c.Type == "Established" && c.Status == "True" { return true, nil }\n    }\n    return false, nil\n})',
    tags: ['kubernetes', 'crd-definition', 'establishment'],
  },
  {
    id: 'k8s-crds-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How does the API server handle strategic merge patch for custom resources vs built-in resources?',
    options: [
      'Strategic merge patch works identically for CRDs and built-in resources',
      'CRDs do not support strategic merge patch natively; they fall back to JSON merge patch unless x-kubernetes-list-type and related directives are set',
      'CRDs always use replace semantics',
      'Strategic merge patch is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Built-in resources have patch strategy tags in Go structs. CRDs must use x-kubernetes-list-type, x-kubernetes-list-map-keys, and x-kubernetes-map-type for strategic merge behavior.',
    codeSnippet: '# Without list-type directives, arrays are replaced atomically:\n# PATCH: {"spec":{"ports":[{"port":8080}]}} replaces entire ports array\n\n# With map-type list, items are merged by key:\nproperties:\n  ports:\n    type: array\n    x-kubernetes-list-type: map\n    x-kubernetes-list-map-keys: ["port"]\n    items:\n      type: object\n      required: ["port"]\n      properties:\n        port:\n          type: integer\n        protocol:\n          type: string\n\n# Now PATCH merges by port key instead of replacing the array',
    tags: ['kubernetes', 'reconciliation', 'strategic-merge'],
  },
  {
    id: 'k8s-crds-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'What are the risks and mitigations of running an operator with cluster-admin privileges?',
    options: [
      'Cluster-admin is required and has no risks',
      'Excessive privileges increase blast radius; mitigate with least-privilege RBAC, namespace scoping, Pod Security Standards, and network policies',
      'Operators always need cluster-admin',
      'There are no security concerns with operator RBAC',
    ],
    correctAnswer: 1,
    explanation: 'Follow least-privilege: only request needed RBAC verbs/resources. Use namespace-scoped roles where possible. Apply PSS restrictions and network policies to operator pods.',
    codeSnippet: '# Bad: Overly permissive\nrules:\n  - apiGroups: ["*"]\n    resources: ["*"]\n    verbs: ["*"]\n\n# Good: Least privilege\nrules:\n  - apiGroups: ["cache.example.com"]\n    resources: ["memcacheds"]\n    verbs: ["get", "list", "watch", "update", "patch"]\n  - apiGroups: ["cache.example.com"]\n    resources: ["memcacheds/status"]\n    verbs: ["get", "update", "patch"]\n  - apiGroups: ["apps"]\n    resources: ["deployments"]\n    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]\n  - apiGroups: [""]\n    resources: ["events"]\n    verbs: ["create", "patch"]',
    tags: ['kubernetes', 'operator-pattern', 'security'],
  },
  {
    id: 'k8s-crds-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How does the controller-runtime DelegatingClient work internally?',
    options: [
      'It delegates all operations to a single client',
      'It routes reads to the cache and writes to the API server, with configurable per-type overrides for uncached reads',
      'It delegates to different controllers',
      'It uses round-robin across multiple API servers',
    ],
    correctAnswer: 1,
    explanation: 'The DelegatingClient uses a split reader/writer: reads go to the informer cache by default, writes go directly to the API server. Specific types can be configured to bypass cache.',
    codeSnippet: '// Internal structure of DelegatingClient\ntype DelegatingClient struct {\n    Reader       client.Reader  // Cache-backed reader\n    Writer       client.Writer  // Direct API writer\n    StatusClient client.StatusClient\n    // Types configured to bypass cache:\n    CacheUnstructured bool\n    UncachedObjects   []client.Object\n}\n\n// Configure specific types to always read from API server\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    Client: client.Options{\n        Cache: &client.CacheOptions{\n            // These types always read from API server, not cache\n            DisableFor: []client.Object{\n                &corev1.Secret{},\n                &corev1.ConfigMap{},\n            },\n        },\n    },\n})',
    tags: ['kubernetes', 'controllers', 'delegating-client'],
  },
  {
    id: 'k8s-crds-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'What is the internal flow of an AdmissionReview request through the API server and what are the failure modes?',
    options: [
      'The API server sends the request directly to the webhook',
      'Request goes through authentication, authorization, mutating webhooks (in order), object schema validation, then validating webhooks (in parallel per webhook, serial across webhooks)',
      'Webhooks run before authentication',
      'All webhooks run in parallel regardless of type',
    ],
    correctAnswer: 1,
    explanation: 'The flow is: Authentication -> Authorization -> Mutating admission (serial, may run multiple rounds) -> Schema validation -> Validating admission (parallel per webhook). Failure at any stage rejects the request.',
    tags: ['kubernetes', 'webhooks', 'admission-flow'],
  },
  {
    id: 'k8s-crds-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How does the API server handle validation when both CRD schema validation and a validating webhook are configured?',
    options: [
      'Only one validation method is used',
      'Schema validation runs first during admission; if it passes, validating webhooks are called. Both must pass for the request to succeed.',
      'Webhooks override schema validation',
      'They run in parallel and the first to respond wins',
    ],
    correctAnswer: 1,
    explanation: 'Schema validation (including CEL rules) is part of the admission chain. It runs before validating webhooks. A request must pass both schema validation and all validating webhooks.',
    tags: ['kubernetes', 'validation', 'admission-chain'],
  },
  {
    id: 'k8s-crds-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "resyncPeriod" in controller-runtime and what are the implications of setting it too low or too high?',
    options: [
      'It is the interval between API server requests',
      'It triggers periodic re-queuing of all watched objects; too low increases API/controller load, too high delays drift detection',
      'It controls how often the cache is cleared',
      'It sets the leader election renewal interval',
    ],
    correctAnswer: 1,
    explanation: 'ResyncPeriod causes all cached objects to be re-queued for reconciliation periodically. It acts as a safety net for missed events but adds load proportional to the number of watched objects.',
    codeSnippet: '// Set resync period on the manager\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    // Resync all objects every 10 minutes\n    // This means every 10 minutes, ALL watched objects will be re-queued\n    // For 10,000 objects, this means 10,000 reconciliations every 10 minutes\n    Cache: cache.Options{\n        SyncPeriod: &[]time.Duration{10 * time.Minute}[0],\n    },\n})\n\n// Set 0 to disable periodic resync (rely only on watches)\n// This is more efficient but requires trusting watch reliability',
    tags: ['kubernetes', 'reconciliation', 'resync-period'],
  },
  {
    id: 'k8s-crds-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'What is the impact of CRD registration on API server OpenAPI spec generation?',
    options: [
      'CRDs do not affect the OpenAPI spec',
      'Each CRD with a structural schema adds to the API server OpenAPI spec, increasing its size and affecting discovery clients',
      'CRDs have a separate OpenAPI endpoint',
      'OpenAPI spec is only generated for built-in resources',
    ],
    correctAnswer: 1,
    explanation: 'Each CRD adds its schema to the aggregated OpenAPI spec. Many CRDs with large schemas can significantly increase the spec size, slowing down kubectl and other discovery clients.',
    codeSnippet: '# Check the size of your cluster\'s OpenAPI spec\nkubectl get --raw /openapi/v2 | wc -c\n# A cluster with many CRDs can have an OpenAPI spec of 50MB+\n\n# OpenAPI v3 (Kubernetes 1.27+) uses a more efficient per-group structure\nkubectl get --raw /openapi/v3\n# Returns an index of per-group API specs instead of one massive document\n\n# Impact on clients:\n# - kubectl caches the OpenAPI spec for validation and completion\n# - Large specs slow down kubectl startup and memory usage\n# - Consider using x-kubernetes-validation instead of deep nested schemas',
    tags: ['kubernetes', 'crd-definition', 'openapi-impact'],
  },
  {
    id: 'k8s-crds-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement a "leader-for-life" pattern vs standard leader election for an operator?',
    options: [
      'They are the same pattern',
      'Leader-for-life uses an ownerReference on the lease tied to the pod; when the pod dies, the lease is GC\'d, unlike standard leader election which uses renewals and timeouts',
      'Leader-for-life means no leader election',
      'Standard leader election uses pod names',
    ],
    correctAnswer: 1,
    explanation: 'Leader-for-life sets an ownerReference from the Lease to the pod. When the pod is deleted, the Lease is garbage collected, allowing a new leader. This avoids split-brain from clock skew.',
    codeSnippet: '// Standard leader election: uses lease duration and renewals\n// Risk: If the leader pod is partitioned but alive, split-brain possible\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    LeaderElection:          true,\n    LeaderElectionID:        "my-operator-lock",\n    LeaseDuration:           &[]time.Duration{15 * time.Second}[0],\n    RenewDeadline:           &[]time.Duration{10 * time.Second}[0],\n    RetryPeriod:             &[]time.Duration{2 * time.Second}[0],\n})\n\n// Leader-for-life: ties lease to pod lifecycle\n// The operator SDK previously supported this via:\n// leader.Become(ctx, "my-operator-lock")\n// This creates a ConfigMap owned by the pod\n// When the pod is deleted, the ConfigMap is garbage collected',
    tags: ['kubernetes', 'operator-pattern', 'leader-election'],
  },
  {
    id: 'k8s-crds-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the "stale informer cache" problem during rapid updates and how does it cause reconciliation issues?',
    options: [
      'The cache is always consistent',
      'After writing an object, the next Get from cache may return the old version because the watch event has not arrived yet, causing incorrect diff calculations',
      'The cache becomes stale only after hours',
      'Stale cache only affects list operations',
    ],
    correctAnswer: 1,
    explanation: 'After a controller writes an update, the cache may still have the old version. If reconciliation reads from cache immediately, it sees stale data and may redo the update, causing thrashing.',
    codeSnippet: '// Problematic pattern:\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    obj := &v1alpha1.MyResource{}\n    r.Get(ctx, req.NamespacedName, obj) // reads from cache\n\n    // Update the object\n    obj.Spec.Replicas = 5\n    r.Update(ctx, obj) // writes to API server\n\n    // Problem: next reconciliation triggered by the watch event\n    // may still read the OLD object from cache\n    // causing it to update again unnecessarily\n\n    // Solutions:\n    // 1. Use status.observedGeneration to track processed versions\n    // 2. Compare only meaningful fields, not entire objects\n    // 3. Use Patch instead of Update to avoid resourceVersion conflicts\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'controllers', 'stale-cache'],
  },
  {
    id: 'k8s-crds-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you implement a conversion webhook that handles batch conversions efficiently?',
    options: [
      'Each object is converted in a separate HTTP request',
      'The ConversionReview can contain multiple objects; the webhook must convert all of them in a single request',
      'Batch conversion is not supported',
      'Use parallel HTTP requests for each object',
    ],
    correctAnswer: 1,
    explanation: 'A ConversionReview request can contain multiple objects (e.g., during a list operation). The webhook must convert all objects in the request and return them in the same order.',
    codeSnippet: 'func handleConversion(w http.ResponseWriter, r *http.Request) {\n    review := &apiextensionsv1.ConversionReview{}\n    json.NewDecoder(r.Body).Decode(review)\n\n    convertedObjects := make([]runtime.RawExtension, len(review.Request.Objects))\n    for i, obj := range review.Request.Objects {\n        // Decode, convert, and re-encode each object\n        src := &unstructured.Unstructured{}\n        json.Unmarshal(obj.Raw, &src.Object)\n\n        converted, err := convert(src, review.Request.DesiredAPIVersion)\n        if err != nil {\n            review.Response = &apiextensionsv1.ConversionResponse{\n                Result: metav1.Status{\n                    Status:  "Failure",\n                    Message: err.Error(),\n                },\n            }\n            json.NewEncoder(w).Encode(review)\n            return\n        }\n        raw, _ := json.Marshal(converted)\n        convertedObjects[i] = runtime.RawExtension{Raw: raw}\n    }\n\n    review.Response = &apiextensionsv1.ConversionResponse{\n        UID:              review.Request.UID,\n        ConvertedObjects: convertedObjects,\n        Result:           metav1.Status{Status: "Success"},\n    }\n    json.NewEncoder(w).Encode(review)\n}',
    tags: ['kubernetes', 'webhooks', 'batch-conversion'],
  },
  {
    id: 'k8s-crds-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'api-versioning',
    question: 'What are the internal mechanics of the API server\'s storage version migration and the StorageVersionMigration API?',
    options: [
      'Storage migration happens automatically on CRD update',
      'The kube-storage-version-migrator reads all objects of a resource type and writes them back to trigger conversion to the current storage version',
      'Migration requires deleting and recreating all objects',
      'Storage version is fixed and cannot be changed',
    ],
    correctAnswer: 1,
    explanation: 'The storage version migrator controller reads all objects via LIST and writes each back with no changes (PUT), causing the API server to re-store them using the current storage version.',
    codeSnippet: '# StorageVersionMigration API (alpha in 1.30)\napiVersion: storagemigration.k8s.io/v1alpha1\nkind: StorageVersionMigration\nmetadata:\n  name: migrate-crontabs\nspec:\n  resource:\n    group: stable.example.com\n    version: v1\n    resource: crontabs\nstatus:\n  conditions:\n    - type: Running\n      status: "True"\n    - type: Succeeded\n      status: "False"\n\n# Manual migration approach:\n# Read all objects and write them back\nkubectl get crontabs -A -o json | \\\n  jq \'.items[] | del(.metadata.managedFields)\' | \\\n  while read -r item; do echo "$item" | kubectl apply -f -; done',
    tags: ['kubernetes', 'api-versioning', 'storage-migration'],
  },
  {
    id: 'k8s-crds-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How does the controller-runtime work queue internally handle deduplication and ordering?',
    options: [
      'It uses a simple FIFO queue with no deduplication',
      'It uses a combination of a queue, a dirty set, and a processing set to ensure each key is processed only once concurrently while allowing re-queuing',
      'It uses a priority queue sorted by creation time',
      'It deduplicates based on resource UID only',
    ],
    correctAnswer: 1,
    explanation: 'The workqueue has three sets: queue (ordered items to process), dirty (items that need processing), and processing (currently being processed). This ensures at-most-once concurrent processing per key.',
    codeSnippet: '// Work queue internal structure:\n// queue:      [item-a, item-b, item-c]  // ordered items to process\n// dirty:      {item-a, item-b, item-c}  // items needing processing\n// processing: {item-d}                   // currently being processed\n\n// When item-d is added while being processed:\n// - It\'s added to dirty but NOT to queue\n// - When item-d processing finishes and calls Done():\n//   - It\'s removed from processing\n//   - Since it\'s in dirty, it\'s re-added to queue\n// - This ensures item-d is processed again with latest state\n\n// This prevents:\n// 1. Processing the same item concurrently\n// 2. Missing updates that arrive during processing\n// 3. Duplicate entries in the queue',
    tags: ['kubernetes', 'operator-sdk', 'workqueue-internals'],
  },
  {
    id: 'k8s-crds-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How does server-side field validation (strict mode) interact with CRD schemas and what are the edge cases?',
    options: [
      'Strict validation is identical to standard validation',
      'fieldValidation=Strict rejects unknown fields even if x-kubernetes-preserve-unknown-fields is set on parent objects; this can break clients sending extra fields',
      'Strict mode only works with built-in resources',
      'Strict mode is always enabled',
    ],
    correctAnswer: 1,
    explanation: 'With fieldValidation=Strict (kubectl --validate=strict), unknown fields are rejected. This interacts with preserve-unknown-fields and can catch schema mismatches that are silently pruned normally.',
    codeSnippet: '# Standard apply - unknown fields are pruned silently\nkubectl apply -f resource.yaml\n# No error even if resource.yaml has fields not in schema\n\n# Strict validation - unknown fields cause errors\nkubectl apply -f resource.yaml --validate=strict\n# Error: strict decoding error: unknown field "spec.unknownField"\n\n# Server-side validation header:\n# fieldValidation=Strict - reject unknown fields\n# fieldValidation=Warn - return warnings for unknown fields (default in 1.27+)\n# fieldValidation=Ignore - silently drop unknown fields\n\n# Edge case: fields defined in v1alpha1 but not in v1\n# Strict validation catches version mismatch issues',
    tags: ['kubernetes', 'validation', 'strict-mode'],
  },
  {
    id: 'k8s-crds-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that needs to aggregate data from thousands of custom resources without loading them all into memory?',
    options: [
      'Load all resources into memory using List',
      'Use paginated List with server-side filtering, shared informers with transform functions to reduce cached object size',
      'Use etcd queries directly',
      'Deploy multiple controller replicas to split the load',
    ],
    correctAnswer: 1,
    explanation: 'Use transform functions on informers to strip unnecessary fields before caching, reducing memory. Paginated list for initial load. Index for efficient lookups.',
    codeSnippet: '// Transform function to strip unnecessary fields before caching\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    Cache: cache.Options{\n        ByObject: map[client.Object]cache.ByObject{\n            &v1alpha1.MyResource{}: {\n                Transform: func(obj interface{}) (interface{}, error) {\n                    resource := obj.(*v1alpha1.MyResource)\n                    // Strip large fields not needed for reconciliation\n                    resource.Spec.LargeConfig = ""\n                    resource.ManagedFields = nil\n                    return resource, nil\n                },\n            },\n        },\n    },\n})\n\n// Use label selectors to limit what\'s cached\ncache.Options{\n    ByObject: map[client.Object]cache.ByObject{\n        &corev1.Pod{}: {\n            Label: labels.SelectorFromSet(labels.Set{\n                "managed-by": "my-operator",\n            }),\n        },\n    },\n}',
    tags: ['kubernetes', 'controllers', 'memory-optimization'],
  },
  {
    id: 'k8s-crds-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "consistent read" problem with controller-runtime and how do you solve it for critical operations?',
    options: [
      'All reads are consistent by default',
      'Reads from cache may be stale; for critical operations use direct API reads with specific resourceVersion or quorum reads',
      'Use etcd directly for consistent reads',
      'Consistent reads require a special client',
    ],
    correctAnswer: 1,
    explanation: 'Cache reads can be stale. For critical operations (e.g., before updating status with important metrics), use a direct uncached read to ensure the latest data.',
    codeSnippet: '// Cached read (may be stale, fast)\nobj := &v1alpha1.MyResource{}\nr.Get(ctx, req.NamespacedName, obj)\n\n// Direct API read (always current, slower)\n// Method 1: Use an uncached client\nuncachedClient, _ := client.New(cfg, client.Options{Scheme: scheme})\nuncachedClient.Get(ctx, req.NamespacedName, obj)\n\n// Method 2: Configure specific types as uncached\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    Client: client.Options{\n        Cache: &client.CacheOptions{\n            DisableFor: []client.Object{&corev1.Secret{}},\n        },\n    },\n})\n\n// Method 3: Use APIReader for specific reads\napiReader := mgr.GetAPIReader()\napiReader.Get(ctx, req.NamespacedName, obj)',
    tags: ['kubernetes', 'reconciliation', 'consistent-reads'],
  },
  {
    id: 'k8s-crds-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'What are the security implications of admission webhooks and how can they be exploited?',
    options: [
      'Webhooks have no security implications',
      'Compromised webhooks can exfiltrate secrets, modify workloads silently, deny legitimate requests, and act as a persistent backdoor',
      'Webhooks only read data and cannot modify anything',
      'Webhooks run with no privileges',
    ],
    correctAnswer: 1,
    explanation: 'A compromised mutating webhook sees all resources matching its rules (including Secrets). It can silently inject sidecars, modify images, or block cluster operations.',
    codeSnippet: '# Security mitigations for webhooks:\n\n# 1. Restrict webhook scope to minimum necessary resources\nwebhooks:\n  - name: validate.example.com\n    rules:\n      - apiGroups: ["cache.example.com"]  # Only your CRD\n        resources: ["memcacheds"]\n        operations: ["CREATE", "UPDATE"]  # Not DELETE\n\n# 2. Use namespaceSelector to exclude system namespaces\n    namespaceSelector:\n      matchExpressions:\n        - key: kubernetes.io/metadata.name\n          operator: NotIn\n          values: ["kube-system"]\n\n# 3. Apply network policies to webhook pods\n# 4. Use read-only filesystem for webhook pods\n# 5. Scan webhook images for vulnerabilities\n# 6. Monitor webhook audit logs for suspicious modifications',
    tags: ['kubernetes', 'webhooks', 'security'],
  },
  {
    id: 'k8s-crds-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'How does the API server handle CRD field selector indexing internally and what are the performance implications?',
    options: [
      'CRDs support full field selector indexing',
      'CRDs only support metadata.name and metadata.namespace field selectors; the API server filters other fields post-list, meaning no server-side optimization',
      'Field selectors are fully indexed for all CRD fields',
      'CRDs use a dedicated index server',
    ],
    correctAnswer: 1,
    explanation: 'Unlike built-in resources, CRDs have no server-side field indexes beyond metadata.name/namespace. Any other field filtering happens client-side after fetching all matching objects.',
    codeSnippet: '# These work with server-side optimization:\nkubectl get crontabs --field-selector metadata.name=my-cron\nkubectl get crontabs --field-selector metadata.namespace=default\n\n# These do NOT have server-side indexes for CRDs:\n# The API server returns ALL objects and filters client-side\nkubectl get crontabs --field-selector spec.cronSpec="* * * * *"\n# This is equivalent to getting all and filtering locally!\n\n# For efficient filtering of CRDs, use:\n# 1. Label selectors (indexed): kubectl get crontabs -l app=frontend\n# 2. Informer-based indexing in the controller (local cache)\n# 3. CEL-based admission to enforce constraints at write time',
    tags: ['kubernetes', 'crd-definition', 'field-selectors'],
  },
  {
    id: 'k8s-crds-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you design an operator for safe zero-downtime upgrades across its own version changes?',
    options: [
      'Always delete and recreate managed resources during operator upgrade',
      'Ensure backward-compatible CRDs, use status to persist reconciliation state, handle mid-reconciliation restarts, and test upgrade scenarios',
      'Use blue-green deployment for the operator',
      'Freeze all operations during upgrade',
    ],
    correctAnswer: 1,
    explanation: 'Safe operator upgrades require: backward-compatible CRD changes, storing reconciliation progress in status, idempotent reconciliation, and testing upgrade paths.',
    codeSnippet: '// Safe upgrade patterns:\n\n// 1. Store reconciliation state in status (survives operator restart)\ntype DatabaseStatus struct {\n    Phase           string `json:"phase"`\n    MigrationStep   int    `json:"migrationStep"`\n    LastOperatorVer string `json:"lastOperatorVersion"`\n}\n\n// 2. Handle version skew in reconciliation\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    cr := &v1alpha1.Database{}\n    r.Get(ctx, req.NamespacedName, cr)\n\n    // Detect operator version change\n    if cr.Status.LastOperatorVersion != operatorVersion {\n        // Run migration logic for operator upgrade\n        if err := r.handleOperatorUpgrade(ctx, cr); err != nil {\n            return ctrl.Result{}, err\n        }\n        cr.Status.LastOperatorVersion = operatorVersion\n        r.Status().Update(ctx, cr)\n    }\n\n    // Continue normal reconciliation...\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'operator-pattern', 'zero-downtime-upgrade'],
  },
  {
    id: 'k8s-crds-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How does etcd compaction affect long-running controllers and what error handling is required?',
    options: [
      'Etcd compaction has no effect on controllers',
      'After compaction, old resourceVersions become invalid, causing watches to receive a 410 Gone error, triggering a full re-list',
      'Compaction only affects etcd performance',
      'Controllers use a separate etcd instance',
    ],
    correctAnswer: 1,
    explanation: 'etcd compaction removes old revisions. If a controller\'s watch or list uses a resourceVersion older than the compacted version, it gets a 410 Gone and must re-list.',
    codeSnippet: '// When a watch receives 410 Gone:\n// 1. The informer detects the error\n// 2. It performs a full re-LIST of all objects\n// 3. It replaces the entire cache with fresh data\n// 4. It re-establishes the watch from the new resourceVersion\n\n// Impact: Full re-list can be expensive for resources with many objects\n// The API server returns ALL objects, which temporarily spikes:\n// - API server CPU and memory\n// - Network bandwidth\n// - Controller memory (processing the full list)\n\n// Mitigation strategies:\n// - Use WatchBookmarks to keep resourceVersion recent\n// - Configure etcd compaction interval appropriately\n// - Use label selectors to reduce watched object count\n// - Monitor for frequent re-lists (metric: reflector_last_resource_version)',
    tags: ['kubernetes', 'controllers', 'etcd-compaction'],
  },
  {
    id: 'k8s-crds-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'What are the limitations of CEL validation regarding access to external state and how does this affect CRD design?',
    options: [
      'CEL can access any Kubernetes resource',
      'CEL can only access the object being validated (self) and its old value (oldSelf); for cross-resource validation, webhooks are required',
      'CEL can access the full API server state',
      'CEL runs with cluster-admin privileges',
    ],
    correctAnswer: 1,
    explanation: 'CEL validation rules can only reference the current object (self) and its previous version (oldSelf). They cannot query other resources, make HTTP calls, or access external state.',
    codeSnippet: '# What CEL CAN do:\nx-kubernetes-validations:\n  # Cross-field validation within the same object\n  - rule: "self.spec.minReplicas <= self.spec.maxReplicas"\n  # Transition rules (comparing old and new values)\n  - rule: "self.spec.storageSize >= oldSelf.spec.storageSize"\n    message: "storage can only be increased"\n  # String, numeric, list, map operations\n  - rule: "self.spec.name.matches(\'^[a-z][a-z0-9-]*$\')"\n  # Duration and timestamp comparisons\n  - rule: "duration(self.spec.timeout) <= duration(\'1h\')"\n\n# What CEL CANNOT do:\n# - Check if a referenced Secret exists\n# - Validate against other CRs\n# - Call external APIs\n# - Access node information\n# For these, use a ValidatingAdmissionWebhook or ValidatingAdmissionPolicy',
    tags: ['kubernetes', 'validation', 'cel-limitations'],
  },
  {
    id: 'k8s-crds-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you implement conflict-free replicated data type (CRDT) patterns in operator status for multi-controller scenarios?',
    options: [
      'Use standard Go maps',
      'Use append-only status fields, last-writer-wins timestamps, or merge-friendly data structures to avoid conflicts between multiple status writers',
      'CRDT patterns are not applicable to Kubernetes',
      'Use etcd transactions',
    ],
    correctAnswer: 1,
    explanation: 'When multiple controllers update different status fields, use merge-friendly patterns: separate status sections per controller, timestamp-based LWW, or status patch to minimize conflicts.',
    codeSnippet: '// Pattern: Separate status sections per controller\ntype ClusterStatus struct {\n    // Each controller updates its own section\n    ControllerA ControllerAStatus `json:"controllerA,omitempty"`\n    ControllerB ControllerBStatus `json:"controllerB,omitempty"`\n}\n\n// Pattern: Using status patch to update only owned fields\npatch := client.MergeFrom(cr.DeepCopy())\ncr.Status.ControllerA = ControllerAStatus{\n    LastSeen:    metav1.Now(),\n    ReplicaCount: 3,\n}\n// Only patches controllerA section, no conflict with controllerB updates\nr.Status().Patch(ctx, cr, patch)\n\n// Pattern: Append-only with deduplication\ntype EventLog struct {\n    Events []StatusEvent `json:"events,omitempty"`\n}\n// Each controller appends events; reconciler periodically compacts',
    tags: ['kubernetes', 'reconciliation', 'crdt'],
  },
  {
    id: 'k8s-crds-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'How does OLM handle operator upgrades internally and what are the failure modes?',
    options: [
      'OLM does a simple deployment rollout',
      'OLM creates a new CSV, waits for it to succeed, then deletes the old one; if the new CSV fails, the old one remains active',
      'OLM deletes the old operator before installing the new one',
      'OLM handles upgrades outside the cluster',
    ],
    correctAnswer: 1,
    explanation: 'OLM uses a staged upgrade: creates new CSV (Pending), installs new resources (Installing), waits for the deployment to be ready (Succeeded). The old CSV is only replaced on success.',
    tags: ['kubernetes', 'olm', 'upgrade-internals'],
  },
  {
    id: 'k8s-crds-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the "watch fan-out" problem and how does it affect clusters with many operators?',
    options: [
      'Watch fan-out only affects networking',
      'Each controller creates watches on the API server; many operators watching the same resources multiply API server load and memory usage',
      'Fan-out is handled by etcd',
      'Kubernetes limits the number of watches',
    ],
    correctAnswer: 1,
    explanation: 'Each informer creates a watch on the API server. If 20 operators watch Pods, that is 20 watches. The API server must serialize and send every Pod event to each watch, multiplying load.',
    codeSnippet: '# Impact calculation:\n# If you have:\n# - 10,000 Pods\n# - 5 operators watching Pods\n# Each Pod update generates:\n# - 1 etcd watch event\n# - 5 API server watch serializations (one per informer)\n# - 5 network transmissions\n# - 5 cache updates in operator memory\n\n# Mitigations:\n# 1. Use label selectors to watch only relevant resources\ncache.Options{\n    ByObject: map[client.Object]cache.ByObject{\n        &corev1.Pod{}: {\n            Label: labels.SelectorFromSet(labels.Set{\n                "app.kubernetes.io/managed-by": "my-operator",\n            }),\n        },\n    },\n}\n# 2. Use field selectors where possible\n# 3. Consider shared informer factories across controllers in same process\n# 4. Use transforms to reduce cached object size',
    tags: ['kubernetes', 'controllers', 'watch-fan-out'],
  },
  {
    id: 'k8s-crds-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How does the API server handle webhook ordering, and what happens when mutating webhooks conflict with each other?',
    options: [
      'Webhooks run in alphabetical order',
      'Mutating webhooks run in the order defined by their configuration names; later webhooks see mutations from earlier ones; reinvocationPolicy handles re-runs',
      'Webhook order is random',
      'Conflicting webhooks are automatically resolved',
    ],
    correctAnswer: 1,
    explanation: 'Mutating webhooks run alphabetically by configuration name. Each sees the result of previous mutations. With reinvocationPolicy: IfNeeded, webhooks may run again if the object changed.',
    codeSnippet: '# Webhook execution order for a Pod CREATE:\n# 1. a-istio-sidecar-injector.MutatingWebhookConfiguration\n#    -> Injects sidecar container\n# 2. b-my-operator.MutatingWebhookConfiguration\n#    -> Sets resource limits (sees sidecar from step 1)\n# 3. z-vault-injector.MutatingWebhookConfiguration\n#    -> Injects Vault sidecar (sees both previous mutations)\n#\n# If z-vault-injector modifies pod and a-istio has reinvocationPolicy: IfNeeded,\n# a-istio runs AGAIN to check if its injection is still valid\n#\n# Warning: This can cause infinite loops if webhooks keep modifying\n# each other\'s changes. The API server limits reinvocation rounds.',
    tags: ['kubernetes', 'webhooks', 'ordering'],
  },
  {
    id: 'k8s-crds-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'What is the SelectableField feature (Kubernetes 1.30+) for CRDs and how does it change field selector behavior?',
    options: [
      'It allows CSS-style field selection in YAML',
      'It enables server-side field selector indexing for specified CRD fields, making field-based filtering efficient',
      'It allows selecting fields for kubectl output',
      'It restricts which fields users can modify',
    ],
    correctAnswer: 1,
    explanation: 'SelectableFields (alpha in 1.30) allows CRD authors to declare fields that should be indexed for efficient server-side field selector queries, previously only available for built-in resources.',
    codeSnippet: '# With SelectableFields (Kubernetes 1.30+, requires feature gate)\napiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nspec:\n  versions:\n    - name: v1\n      served: true\n      storage: true\n      selectableFields:\n        - jsonPath: .spec.phase\n        - jsonPath: .spec.region\n      schema:\n        openAPIV3Schema:\n          type: object\n          properties:\n            spec:\n              type: object\n              properties:\n                phase:\n                  type: string\n                region:\n                  type: string\n\n# Now this is efficient (server-side indexed):\nkubectl get mycrd --field-selector spec.phase=Running\n# Previously this was NOT indexed and required fetching all objects',
    tags: ['kubernetes', 'crd-definition', 'selectable-fields'],
  },
  {
    id: 'k8s-crds-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you implement a "lease-based" distributed lock in an operator for coordinating access to external resources?',
    options: [
      'Use Go mutexes',
      'Use Kubernetes Lease objects as distributed locks with TTL-based automatic release',
      'Use etcd locks directly',
      'Use file-based locking on shared volumes',
    ],
    correctAnswer: 1,
    explanation: 'Kubernetes Lease objects can serve as distributed locks. The controller acquires the lease, performs operations, then releases it. TTL ensures automatic release if the holder crashes.',
    codeSnippet: 'import "k8s.io/client-go/tools/leaderelection/resourcelock"\n\nfunc (r *Reconciler) acquireLock(ctx context.Context, name, namespace string) (bool, error) {\n    lease := &coordinationv1.Lease{}\n    err := r.Get(ctx, types.NamespacedName{Name: name, Namespace: namespace}, lease)\n\n    if apierrors.IsNotFound(err) {\n        // Create new lease\n        lease = &coordinationv1.Lease{\n            ObjectMeta: metav1.ObjectMeta{Name: name, Namespace: namespace},\n            Spec: coordinationv1.LeaseSpec{\n                HolderIdentity:       ptr.To(r.podName),\n                LeaseDurationSeconds: ptr.To(int32(60)),\n                AcquireTime:          &metav1.MicroTime{Time: time.Now()},\n                RenewTime:            &metav1.MicroTime{Time: time.Now()},\n            },\n        }\n        return true, r.Create(ctx, lease)\n    }\n\n    // Check if existing lease has expired\n    if lease.Spec.RenewTime != nil {\n        expiry := lease.Spec.RenewTime.Add(\n            time.Duration(*lease.Spec.LeaseDurationSeconds) * time.Second)\n        if time.Now().After(expiry) {\n            // Lease expired, take over\n            lease.Spec.HolderIdentity = ptr.To(r.podName)\n            lease.Spec.RenewTime = &metav1.MicroTime{Time: time.Now()}\n            return true, r.Update(ctx, lease)\n        }\n    }\n    return false, nil // Lock held by another\n}',
    tags: ['kubernetes', 'reconciliation', 'distributed-lock'],
  },
  {
    id: 'k8s-crds-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'What is the "aggregated API server" alternative to CRDs and when should it be preferred?',
    options: [
      'They are the same thing with different names',
      'An aggregated API server is a custom API server that handles requests for specific API groups, offering more control over storage, validation, and subresources than CRDs',
      'Aggregated API servers are deprecated',
      'Aggregated API servers are only for metrics',
    ],
    correctAnswer: 1,
    explanation: 'Aggregated API servers offer custom storage backends, subresources, full admission control, protobuf support, and long-running endpoints. Use when CRDs hit limitations.',
    codeSnippet: '# API service registration\napiVersion: apiregistration.k8s.io/v1\nkind: APIService\nmetadata:\n  name: v1.custom.example.com\nspec:\n  group: custom.example.com\n  version: v1\n  service:\n    namespace: custom-system\n    name: custom-api-server\n  groupPriorityMinimum: 100\n  versionPriority: 100\n  caBundle: <base64-ca>\n\n# When to prefer aggregated API server over CRDs:\n# - Need custom storage backend (not etcd)\n# - Need protocol buffer serialization\n# - Need long-running subresources (like exec, logs)\n# - Need custom field selector indexing (pre 1.30)\n# - Need virtual resources computed on-the-fly\n# - Hit CRD size/performance limitations',
    tags: ['kubernetes', 'operator-pattern', 'aggregated-api'],
  },
  {
    id: 'k8s-crds-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a "shared informer" that multiple controllers can use without creating duplicate watches?',
    options: [
      'Each controller must create its own informer',
      'Use the controller-runtime Manager which provides a shared cache that all controllers registered with it share',
      'Use a singleton pattern in Go',
      'Share informers through environment variables',
    ],
    correctAnswer: 1,
    explanation: 'The controller-runtime Manager creates one shared cache (informer set) for all controllers. When multiple controllers watch the same resource type, they share one informer and one watch.',
    codeSnippet: '// The Manager shares a single cache across all controllers\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{})\n\n// Controller A watches Pods\nctrl.NewControllerManagedBy(mgr).\n    For(&v1alpha1.ResourceA{}).\n    Owns(&corev1.Pod{}).        // Shares Pod informer\n    Complete(&ReconcilerA{})\n\n// Controller B also watches Pods\nctrl.NewControllerManagedBy(mgr).\n    For(&v1alpha1.ResourceB{}).\n    Owns(&corev1.Pod{}).        // Same Pod informer as Controller A!\n    Complete(&ReconcilerB{})\n\n// Internally, the Manager\'s cache creates one SharedInformer per GVK\n// Both controllers register event handlers on the same Pod informer\n// This means one etcd watch, one cache, shared memory',
    tags: ['kubernetes', 'controllers', 'shared-informers'],
  },
  {
    id: 'k8s-crds-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'What is ValidatingAdmissionPolicy (Kubernetes 1.30 GA) and how does it differ from webhooks for CRD validation?',
    options: [
      'It is identical to a validating webhook',
      'It runs CEL expressions in-process in the API server without HTTP calls, providing better performance, reliability, and no external webhook dependency',
      'It replaces CRD schema validation',
      'It only works with built-in resources',
    ],
    correctAnswer: 1,
    explanation: 'ValidatingAdmissionPolicy runs CEL in the API server process, eliminating webhook latency, network failure risks, and infrastructure management. It can reference parameter resources for dynamic policies.',
    codeSnippet: '# ValidatingAdmissionPolicy (in-process, no webhook needed)\napiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingAdmissionPolicy\nmetadata:\n  name: deny-large-memcached\nspec:\n  failurePolicy: Fail\n  matchConstraints:\n    resourceRules:\n      - apiGroups: ["cache.example.com"]\n        apiVersions: ["v1alpha1"]\n        operations: ["CREATE", "UPDATE"]\n        resources: ["memcacheds"]\n  validations:\n    - expression: "object.spec.size <= 100"\n      message: "Memcached size must be <= 100"\n    - expression: "object.spec.size >= oldObject.spec.size"\n      message: "Memcached size cannot be decreased"\n      reason: Invalid\n---\n# Bind the policy to specific namespaces\napiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingAdmissionPolicyBinding\nmetadata:\n  name: deny-large-memcached-binding\nspec:\n  policyName: deny-large-memcached\n  matchResources:\n    namespaceSelector:\n      matchLabels:\n        environment: production',
    tags: ['kubernetes', 'validation', 'admission-policy'],
  },
  {
    id: 'k8s-crds-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How does the "apply configuration" pattern from the Go typed client differ from unstructured apply?',
    options: [
      'They produce identical results',
      'Typed apply configurations provide compile-time type safety and explicit field ownership, while unstructured apply requires careful manual field management',
      'Unstructured apply is always preferred',
      'Typed apply only works with built-in resources',
    ],
    correctAnswer: 1,
    explanation: 'Typed apply configurations (applyconfigurations) generate type-safe builders. Fields not set are not owned, preventing accidental field ownership. Unstructured apply risks owning zero-value fields.',
    codeSnippet: '// Typed apply configuration (recommended for SSA)\nimport appsac "k8s.io/client-go/applyconfigurations/apps/v1"\nimport coreac "k8s.io/client-go/applyconfigurations/core/v1"\n\ndeployApply := appsac.Deployment("my-deploy", "default").\n    WithSpec(appsac.DeploymentSpec().\n        WithReplicas(3).\n        WithSelector(metav1ac.LabelSelector().\n            WithMatchLabels(map[string]string{"app": "test"})).\n        WithTemplate(coreac.PodTemplateSpec().\n            WithLabels(map[string]string{"app": "test"}).\n            WithSpec(coreac.PodSpec().\n                WithContainers(coreac.Container().\n                    WithName("app").\n                    WithImage("nginx:1.25")))))\n\n// Only fields explicitly set are owned by this field manager\n// Other fields (annotations, etc.) remain unowned and can be set by others\nerr := r.Patch(ctx, deployApply, client.Apply, client.FieldOwner("my-operator"))',
    tags: ['kubernetes', 'reconciliation', 'typed-apply'],
  },
  {
    id: 'k8s-crds-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement a multi-group operator that manages CRDs across different API groups?',
    options: [
      'Create separate operators for each API group',
      'Use the multigroup layout in Kubebuilder/Operator SDK where each API group has its own directory and types',
      'Register all types in a single API group',
      'Use API aggregation for multiple groups',
    ],
    correctAnswer: 1,
    explanation: 'Kubebuilder supports multi-group layout with separate directories per API group. Each group has its own types, but they share one controller manager and binary.',
    codeSnippet: '# Initialize multi-group project\noperator-sdk init --domain example.com --repo github.com/example/multi-operator\n\n# Enable multi-group layout\nkubebuilder edit --multigroup=true\n\n# Create APIs in different groups\noperator-sdk create api --group cache --version v1 --kind Memcached\noperator-sdk create api --group database --version v1 --kind PostgreSQL\noperator-sdk create api --group messaging --version v1beta1 --kind Kafka\n\n# Directory structure:\n# apis/\n#   cache/v1/memcached_types.go\n#   database/v1/postgresql_types.go\n#   messaging/v1beta1/kafka_types.go\n# controllers/\n#   cache/memcached_controller.go\n#   database/postgresql_controller.go\n#   messaging/kafka_controller.go',
    tags: ['kubernetes', 'operator-sdk', 'multi-group'],
  },
  {
    id: 'k8s-crds-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the memory impact of storing managed fields in CRD objects and how do you mitigate it?',
    options: [
      'Managed fields have negligible memory impact',
      'Server-side apply managed fields can be larger than the object itself; strip them in informer transforms or when not needed',
      'Managed fields are stored separately from the object',
      'Managed fields are compressed automatically',
    ],
    correctAnswer: 1,
    explanation: 'managedFields tracks field ownership per manager and can be very large for frequently-patched objects. Strip them in informer transforms to reduce cache memory usage.',
    codeSnippet: '// managedFields can be 5-10x larger than the actual spec\n// For a simple Deployment, managedFields might be 50KB+\n// With 10,000 Deployments in cache: 500MB just for managedFields!\n\n// Strip managedFields in informer transform\ncache.Options{\n    ByObject: map[client.Object]cache.ByObject{\n        &appsv1.Deployment{}: {\n            Transform: func(obj interface{}) (interface{}, error) {\n                accessor, _ := meta.Accessor(obj)\n                accessor.SetManagedFields(nil)\n                return obj, nil\n            },\n        },\n    },\n}\n\n// Note: If you use server-side apply in the controller,\n// you may need managedFields for conflict detection\n// Only strip them if you don\'t need SSA conflict resolution',
    tags: ['kubernetes', 'controllers', 'managed-fields'],
  },
  {
    id: 'k8s-crds-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you implement a conversion webhook that preserves round-trip fidelity for lossless conversion?',
    options: [
      'Simply map matching fields between versions',
      'Use the hub-spoke model with annotation-based preservation of version-specific fields that do not exist in the hub',
      'Store all versions in parallel',
      'Reject conversion requests that would lose data',
    ],
    correctAnswer: 1,
    explanation: 'For lossless round-trip conversion, store version-specific fields in annotations when converting to the hub. Restore them from annotations when converting back to that version.',
    codeSnippet: '// v2 has a field "advancedScheduling" not present in v1 (hub)\nfunc (src *MyResourceV2) ConvertTo(hub conversion.Hub) error {\n    dst := hub.(*MyResourceV1)\n    dst.Spec.Name = src.Spec.Name\n    dst.Spec.Replicas = src.Spec.Size\n\n    // Preserve v2-specific fields in annotations\n    data, _ := json.Marshal(struct {\n        AdvancedScheduling *AdvancedScheduling `json:"advancedScheduling"`\n    }{src.Spec.AdvancedScheduling})\n\n    if dst.Annotations == nil {\n        dst.Annotations = map[string]string{}\n    }\n    dst.Annotations["myresource.example.com/v2-fields"] = string(data)\n    return nil\n}\n\nfunc (dst *MyResourceV2) ConvertFrom(hub conversion.Hub) error {\n    src := hub.(*MyResourceV1)\n    dst.Spec.Name = src.Spec.Name\n    dst.Spec.Size = src.Spec.Replicas\n\n    // Restore v2-specific fields from annotations\n    if saved, ok := src.Annotations["myresource.example.com/v2-fields"]; ok {\n        var extra struct {\n            AdvancedScheduling *AdvancedScheduling `json:"advancedScheduling"`\n        }\n        json.Unmarshal([]byte(saved), &extra)\n        dst.Spec.AdvancedScheduling = extra.AdvancedScheduling\n    }\n    return nil\n}',
    tags: ['kubernetes', 'webhooks', 'round-trip-conversion'],
  },
  {
    id: 'k8s-crds-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement an operator that safely handles the "split-brain" scenario with leader election?',
    options: [
      'Split-brain cannot happen with Kubernetes leader election',
      'Use fencing tokens (resourceVersion) on all writes, implement leader check before critical operations, and design for at-least-once delivery',
      'Run only one replica',
      'Use a distributed consensus algorithm outside Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Split-brain can occur briefly during leader transitions. Use resourceVersion as a fencing token, re-check leadership before critical writes, and ensure idempotent operations.',
    codeSnippet: 'func (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    // The manager handles leader election, but during transition:\n    // - Old leader may still be processing (hasn\'t received cancellation yet)\n    // - New leader starts processing the same resource\n\n    // Protection: Use resourceVersion for optimistic locking\n    obj := &v1alpha1.MyResource{}\n    if err := r.Get(ctx, req.NamespacedName, obj); err != nil {\n        return ctrl.Result{}, client.IgnoreNotFound(err)\n    }\n\n    // The Update will fail with Conflict if another writer\n    // (old leader) modified the object since our read\n    obj.Status.Phase = "Processing"\n    if err := r.Status().Update(ctx, obj); err != nil {\n        if apierrors.IsConflict(err) {\n            // Another writer modified the object, re-read and retry\n            return ctrl.Result{Requeue: true}, nil\n        }\n        return ctrl.Result{}, err\n    }\n\n    // For external resources: check if action was already performed\n    // before making changes (idempotent operations)\n    if !r.externalResourceExists(ctx, obj) {\n        r.createExternalResource(ctx, obj)\n    }\n\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'operator-pattern', 'split-brain'],
  },
  {
    id: 'k8s-crds-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'What is the maximum etcd value size for a CRD object and how does encoding overhead affect it?',
    options: [
      'There is no size limit',
      'The default etcd limit is 1.5MB; with JSON encoding overhead and metadata, the usable payload is significantly less, typically around 1MB',
      'The limit is exactly 1MB',
      'The limit depends on the CRD schema size',
    ],
    correctAnswer: 1,
    explanation: 'etcd has a 1.5MB default request size limit. After JSON encoding, base64 for binary data, metadata, and managed fields, the actual usable spec+status is typically under 1MB.',
    codeSnippet: '# etcd configuration:\n# --max-request-bytes=1572864  (1.5MB default)\n\n# Overhead breakdown for a CRD object:\n# - metadata.managedFields: can be 10-100KB\n# - metadata (name, namespace, labels, annotations, resourceVersion, etc.): 1-10KB\n# - JSON encoding overhead: ~30% for nested structures\n# - Base64 encoding for binary data: 33% increase\n#\n# Example: A CRD with a 1MB spec.config field\n# After JSON encoding: ~1.3MB\n# Plus metadata and managedFields: ~1.5MB\n# This will FAIL to store!\n#\n# Strategies for large data:\n# 1. Store large blobs in ConfigMaps/Secrets and reference them\n# 2. Use external storage (S3, etc.) with references in the CR\n# 3. Split into multiple related CRs\n# 4. Consider an aggregated API server with custom storage',
    tags: ['kubernetes', 'crd-definition', 'size-limits'],
  },
  {
    id: 'k8s-crds-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'finalizers',
    question: 'What is the interaction between finalizers and Kubernetes namespace deletion, and how can finalizers cause namespace deletion to hang?',
    options: [
      'Namespace deletion ignores finalizers on resources within',
      'Namespace deletion waits for all resources with finalizers to be cleaned up; orphaned CRD finalizers can prevent namespace deletion indefinitely',
      'Finalizers are automatically removed during namespace deletion',
      'Namespace deletion deletes the finalizers first',
    ],
    correctAnswer: 1,
    explanation: 'Namespace deletion sets deletionTimestamp on all resources. Resources with finalizers stay in Terminating state. If the controller is not running, the namespace hangs in Terminating.',
    codeSnippet: '# Scenario: CRD has finalizer, but operator is already deleted\n# 1. User deletes namespace\n# 2. Kubernetes sets deletionTimestamp on all resources\n# 3. CRD objects have finalizers -> stuck in Terminating\n# 4. Namespace stuck in Terminating\n\n# Fix: Remove the finalizer from stuck resources\n# For each CRD in the namespace:\nkubectl get <crd-resource> -n stuck-namespace -o name | \\\n  xargs -I {} kubectl patch {} -n stuck-namespace \\\n  -p \'{"metadata":{"finalizers":null}}\' --type=merge\n\n# Prevention: Ensure operator handles deletion BEFORE it is uninstalled\n# Use OLM lifecycle hooks or document uninstall order\n# Consider adding a cleanup job as a pre-uninstall hook\n\n# Nuclear option for stuck namespace (bypasses all finalizers):\nkubectl get namespace stuck-namespace -o json | \\\n  jq \'.spec.finalizers = []\' | \\\n  kubectl replace --raw "/api/v1/namespaces/stuck-namespace/finalize" -f -',
    tags: ['kubernetes', 'finalizers', 'namespace-termination'],
  },
  {
    id: 'k8s-crds-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How does the Kubernetes API server apply admission on subresource requests and what does this mean for operator CRD design?',
    options: [
      'Admission applies identically to all subresources',
      'Subresource requests (status, scale) have their own admission path; webhooks must explicitly match subresources or they will not be called',
      'Subresources bypass admission entirely',
      'Only the main resource admission applies',
    ],
    correctAnswer: 1,
    explanation: 'Webhook rules must explicitly include subresources (e.g., "memcacheds/status") to match subresource requests. Without this, status updates bypass the webhook.',
    codeSnippet: '# Webhook that validates BOTH main resource and status updates\nwebhooks:\n  - name: validate.example.com\n    rules:\n      - operations: ["CREATE", "UPDATE"]\n        apiGroups: ["cache.example.com"]\n        apiVersions: ["v1alpha1"]\n        resources: ["memcacheds", "memcacheds/status"]  # Explicitly include status\n        # Without "memcacheds/status", the webhook won\'t be called\n        # for r.Status().Update() calls from the controller\n\n# Similarly for scale subresource:\n      - resources: ["memcacheds", "memcacheds/scale"]\n        # Validates kubectl scale and HPA operations',
    tags: ['kubernetes', 'controllers', 'subresource-admission'],
  },
  {
    id: 'k8s-crds-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "watch cache consistency" guarantee provided by the API server and its implications for controllers?',
    options: [
      'The watch cache provides strong consistency',
      'The watch cache provides "consistency within a watch session"; after reconnection, events may be replayed but no events are lost within a session',
      'The watch cache has no consistency guarantees',
      'Events can be lost silently',
    ],
    correctAnswer: 1,
    explanation: 'Within a watch session, events are delivered in order and none are lost. On reconnection, the informer re-lists. The watch cache may delay events slightly but guarantees delivery.',
    tags: ['kubernetes', 'reconciliation', 'watch-consistency'],
  },
  {
    id: 'k8s-crds-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'api-versioning',
    question: 'What are the implications of CRD version removal and the required migration procedure?',
    options: [
      'You can remove any version at any time',
      'You must first ensure no objects are stored in the old version (storedVersions), then mark it as not served, then remove it from the spec',
      'Version removal is automatic after deprecation',
      'Versions cannot be removed from a CRD',
    ],
    correctAnswer: 1,
    explanation: 'Removing a version requires: 1) Migrate all objects to new storage version, 2) Remove old version from status.storedVersions, 3) Set served: false, 4) Remove from spec.versions.',
    codeSnippet: '# Step 1: Ensure new version is storage\nspec:\n  versions:\n    - name: v1alpha1\n      served: true\n      storage: false  # No longer storage\n    - name: v1\n      served: true\n      storage: true   # Now storage\n\n# Step 2: Migrate all stored objects (re-save them)\nkubectl get myresource -A -o json | kubectl apply -f -\n\n# Step 3: Check storedVersions - old version must be gone\nkubectl get crd myresources.example.com -o jsonpath=\'{.status.storedVersions}\'\n# Should show: ["v1"] not ["v1alpha1","v1"]\n\n# Step 4: If v1alpha1 still in storedVersions, manually patch\n# (only after confirming all objects migrated)\nkubectl patch crd myresources.example.com --type=json \\\n  -p=\'[{"op":"replace","path":"/status/storedVersions","value":["v1"]}]\' \\\n  --subresource=status\n\n# Step 5: Stop serving the old version\n# spec.versions[0].served = false\n\n# Step 6: Remove the old version from spec entirely',
    tags: ['kubernetes', 'api-versioning', 'version-removal'],
  },
  {
    id: 'k8s-crds-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you design an operator that can manage resources across different Kubernetes API versions (e.g., working with both 1.24 and 1.30 clusters)?',
    options: [
      'Target only one Kubernetes version',
      'Use discovery API to check available API versions, feature gates, and capabilities at runtime, falling back to older APIs when newer ones are unavailable',
      'Use the oldest common API version',
      'Build separate binaries per version',
    ],
    correctAnswer: 1,
    explanation: 'Use the discovery client to check cluster capabilities at startup. Implement version-aware code paths that use newer APIs when available and fall back to older ones.',
    codeSnippet: 'func (r *Reconciler) setupClusterCapabilities(ctx context.Context) error {\n    disc := discovery.NewDiscoveryClientForConfigOrDie(r.Config)\n\n    // Check API version support\n    resources, err := disc.ServerResourcesForGroupVersion("policy/v1")\n    if err != nil {\n        // Fall back to policy/v1beta1 for PDB\n        r.pdbVersion = "v1beta1"\n    } else {\n        r.pdbVersion = "v1"\n    }\n\n    // Check for specific feature\n    version, _ := disc.ServerVersion()\n    r.serverVersion = version\n\n    // Check if a specific CRD exists\n    _, err = disc.ServerResourcesForGroupVersion("monitoring.coreos.com/v1")\n    r.hasPrometheusOperator = err == nil\n\n    return nil\n}\n\n// In reconciliation, use the appropriate API version\nfunc (r *Reconciler) createPDB(ctx context.Context, cr *v1.MyResource) error {\n    if r.pdbVersion == "v1" {\n        return r.createPDBV1(ctx, cr)\n    }\n    return r.createPDBV1Beta1(ctx, cr)\n}',
    tags: ['kubernetes', 'operator-pattern', 'version-compatibility'],
  },
  {
    id: 'k8s-crds-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'What are the performance characteristics of admission webhooks and how do you minimize their latency impact?',
    options: [
      'Webhooks add negligible latency',
      'Each webhook adds an HTTP round-trip (typically 5-50ms); optimize with connection pooling, local caching, and keep-alive; use namespaceSelector to skip irrelevant requests',
      'Webhook latency is fixed at 1ms',
      'Webhooks run asynchronously and add no latency',
    ],
    correctAnswer: 1,
    explanation: 'Each webhook is a synchronous HTTP call adding latency to API requests. Multiple webhooks compound. Optimize by narrowing scope, caching decisions, and using fast response paths.',
    codeSnippet: '# Performance optimization strategies:\n\n# 1. Narrow webhook scope to minimize invocations\nrules:\n  - operations: ["CREATE"]  # Not UPDATE if not needed\n    apiGroups: ["cache.example.com"]\n    resources: ["memcacheds"]  # Not "*"\n\n# 2. Use objectSelector to skip unmanaged resources\nobjectSelector:\n  matchLabels:\n    needs-validation: "true"\n\n# 3. Set aggressive timeout\ntimeoutSeconds: 3  # Fail fast, don\'t block API for 30s\n\n# 4. In webhook code:\nfunc (h *Handler) Handle(ctx context.Context, req admission.Request) admission.Response {\n    // Cache validation results for immutable checks\n    if cached, ok := h.cache.Get(req.UID); ok {\n        return cached\n    }\n    // Use a response pool to reduce GC pressure\n    // Keep HTTP connections alive (default in Go)\n    // Avoid reading external state for every request\n}\n\n# 5. Monitor webhook latency\n# metric: apiserver_admission_webhook_admission_duration_seconds',
    tags: ['kubernetes', 'webhooks', 'performance'],
  },
  {
    id: 'k8s-crds-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'status-subresource',
    question: 'What is the internal API server behavior when updating the status subresource and how does it handle conflicts differently from main resource updates?',
    options: [
      'Status updates and main updates use the same conflict resolution',
      'Status subresource updates increment resourceVersion but NOT metadata.generation; they cannot conflict with spec updates since they use a separate endpoint',
      'Status updates never cause conflicts',
      'Status updates do not change resourceVersion',
    ],
    correctAnswer: 1,
    explanation: 'Status updates go through /status endpoint, incrementing resourceVersion but not generation. They can still conflict with other status updates but not with spec updates, reducing contention.',
    codeSnippet: '# Without status subresource:\n# Both spec and status are updated via the same endpoint\n# spec update and status update WILL conflict on resourceVersion\n# generation increments on every update (spec or status)\n\n# With status subresource:\n# PUT /apis/cache.example.com/v1/memcacheds/my-cache\n#   -> Updates only spec, increments generation\n#   -> Rejects if resourceVersion is stale\n\n# PUT /apis/cache.example.com/v1/memcacheds/my-cache/status\n#   -> Updates only status, does NOT increment generation\n#   -> Uses same resourceVersion for optimistic locking\n#   -> BUT: a spec update does NOT invalidate a pending status update\n#   -> because they operate on different paths\n\n# Important: resourceVersion still increments on status update\n# So two rapid status updates WILL conflict with each other\n# Use Status().Patch() instead of Status().Update() to minimize this',
    tags: ['kubernetes', 'status-subresource', 'conflict-behavior'],
  },
  {
    id: 'k8s-crds-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you profile and optimize a controller-runtime operator for high-scale environments (10,000+ custom resources)?',
    options: [
      'Increase CPU and memory limits',
      'Use Go pprof profiling, optimize informer transforms, tune work queue settings, batch status updates, and use efficient data structures',
      'Deploy more replicas',
      'Use a faster programming language',
    ],
    correctAnswer: 1,
    explanation: 'Profile with pprof, optimize cache with transforms, use label selectors to limit watched scope, batch operations, tune MaxConcurrentReconciles, and minimize unnecessary reconciliations.',
    codeSnippet: '// 1. Enable pprof endpoint\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    PprofBindAddress: ":8082",\n})\n// Access at: http://localhost:8082/debug/pprof/\n\n// 2. Optimize cache with transforms\ncache.Options{\n    ByObject: map[client.Object]cache.ByObject{\n        &v1alpha1.MyResource{}: {\n            Transform: stripManagedFieldsAndAnnotations,\n            Label: relevantResourceSelector,\n        },\n    },\n}\n\n// 3. Tune concurrent reconciliations\nWithOptions(controller.Options{\n    MaxConcurrentReconciles: 10,  // Based on profiling\n    RateLimiter: workqueue.NewMaxOfRateLimiter(\n        workqueue.NewItemExponentialFailureRateLimiter(100*time.Millisecond, 5*time.Minute),\n        &workqueue.BucketRateLimiter{Limiter: rate.NewLimiter(rate.Limit(50), 300)},\n    ),\n})\n\n// 4. Batch status updates using debounce\n// Instead of updating status on every minor change,\n// collect changes and update once per reconciliation cycle\n\n// 5. Key metrics to monitor:\n// - controller_runtime_reconcile_time_seconds\n// - workqueue_queue_duration_seconds\n// - workqueue_depth\n// - go_memstats_alloc_bytes',
    tags: ['kubernetes', 'controllers', 'performance-optimization'],
  },
  {
    id: 'k8s-crds-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you implement admission control using parameterized ValidatingAdmissionPolicy with CRD-based parameter resources?',
    options: [
      'Parameters are hardcoded in the policy',
      'Use spec.paramKind to reference a CRD type, then bindings specify parameter resource instances that provide dynamic configuration to CEL expressions',
      'Parameters are passed as environment variables',
      'ValidatingAdmissionPolicy does not support parameters',
    ],
    correctAnswer: 1,
    explanation: 'ValidatingAdmissionPolicy can reference parameter CRDs via spec.paramKind. Bindings specify which parameter object to use, making CEL rules configurable without modifying the policy.',
    codeSnippet: '# Parameter CRD\napiVersion: example.com/v1\nkind: SizeLimits\nmetadata:\n  name: production-limits\nspec:\n  maxReplicas: 50\n  maxMemory: "8Gi"\n---\n# Policy referencing the parameter\napiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingAdmissionPolicy\nmetadata:\n  name: check-size-limits\nspec:\n  paramKind:\n    apiVersion: example.com/v1\n    kind: SizeLimits\n  matchConstraints:\n    resourceRules:\n      - apiGroups: ["cache.example.com"]\n        resources: ["memcacheds"]\n        operations: ["CREATE", "UPDATE"]\n  validations:\n    - expression: "object.spec.size <= params.spec.maxReplicas"\n      messageExpression: "\'size \' + string(object.spec.size) + \' exceeds limit \' + string(params.spec.maxReplicas)"\n---\n# Binding with specific parameter instance\napiVersion: admissionregistration.k8s.io/v1\nkind: ValidatingAdmissionPolicyBinding\nmetadata:\n  name: check-size-limits-production\nspec:\n  policyName: check-size-limits\n  paramRef:\n    name: production-limits\n    namespace: default\n  matchResources:\n    namespaceSelector:\n      matchLabels:\n        environment: production',
    tags: ['kubernetes', 'validation', 'parameterized-policy'],
  },
  {
    id: 'k8s-crds-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the impact of using unstructured objects vs typed objects in controller-runtime on performance and memory?',
    options: [
      'They have identical performance characteristics',
      'Typed objects use less memory due to Go struct efficiency but require code generation; unstructured objects use map[string]interface{} with higher memory overhead and GC pressure',
      'Unstructured objects are always faster',
      'Typed objects require more memory',
    ],
    correctAnswer: 1,
    explanation: 'Typed objects benefit from Go struct memory layout and avoid map overhead. Unstructured objects use nested maps with interface{} values, increasing memory use and GC pressure at scale.',
    tags: ['kubernetes', 'reconciliation', 'typed-vs-unstructured'],
  },
  {
    id: 'k8s-crds-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement a "gitops-friendly" operator that works well with tools like ArgoCD and Flux?',
    options: [
      'GitOps tools are incompatible with operators',
      'Avoid mutating defaulting webhooks that cause diff, use server-side apply compatible field ownership, and document all generated/computed fields in status',
      'Disable all webhooks when using GitOps',
      'Use Helm instead of an operator',
    ],
    correctAnswer: 1,
    explanation: 'GitOps tools compare desired vs actual state. Mutating webhooks cause perpetual diffs. Use SSA, minimize defaulting mutations, keep computed data in status, and document expected drift.',
    codeSnippet: '# Problem: Mutating webhook adds defaulted fields\n# GitOps tool sees: desired (no defaults) vs actual (with defaults) = DIFF\n# This causes constant "out of sync" warnings\n\n# Solution 1: Use CRD schema defaults instead of mutating webhooks\nproperties:\n  replicas:\n    type: integer\n    default: 3  # Set in schema, not webhook\n\n# Solution 2: Use ignoreDifferences in ArgoCD\nspec:\n  ignoreDifferences:\n    - group: cache.example.com\n      kind: Memcached\n      jsonPointers:\n        - /status\n        - /metadata/annotations/last-reconciled\n\n# Solution 3: Use server-side apply with proper field ownership\n# Operator owns: status, computed annotations\n# GitOps tool owns: spec, user annotations\n# No conflict because different field managers',
    tags: ['kubernetes', 'operator-pattern', 'gitops'],
  },
  {
    id: 'k8s-crds-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How does the API server handle concurrent writes to the same CRD object from multiple controllers?',
    options: [
      'The API server queues writes',
      'It uses optimistic concurrency via resourceVersion; conflicting writes receive a 409 Conflict error, requiring the client to re-read and retry',
      'Last write always wins',
      'The API server uses distributed locks',
    ],
    correctAnswer: 1,
    explanation: 'Each update must include the current resourceVersion. If another write happened since the read, the resourceVersion is stale and the API returns 409 Conflict.',
    codeSnippet: '// Handling conflicts with retry\nimport "k8s.io/client-go/util/retry"\n\nerr := retry.RetryOnConflict(retry.DefaultRetry, func() error {\n    // Re-read the latest version\n    obj := &v1alpha1.MyResource{}\n    if err := r.Get(ctx, req.NamespacedName, obj); err != nil {\n        return err\n    }\n\n    // Make changes\n    obj.Status.Phase = "Running"\n\n    // Update - will fail with Conflict if resourceVersion changed\n    return r.Status().Update(ctx, obj)\n})\n\n// Alternative: Use Patch to minimize conflict surface\npatch := client.MergeFrom(obj.DeepCopy())\nobj.Status.Phase = "Running"\nerr := r.Status().Patch(ctx, obj, patch)\n// Patch is less likely to conflict because it sends only changed fields',
    tags: ['kubernetes', 'controllers', 'optimistic-concurrency'],
  },
  {
    id: 'k8s-crds-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you implement webhook high availability and what happens during rolling updates of the webhook server?',
    options: [
      'Webhook HA is automatic',
      'Run multiple webhook pod replicas behind a Service; during rolling updates, readiness probes prevent traffic to terminating pods, but in-flight requests may fail',
      'Webhooks do not support HA',
      'Use an external load balancer',
    ],
    correctAnswer: 1,
    explanation: 'Webhook HA requires multiple replicas behind a Service. During rolling updates, configure proper readiness probes, PDB, and graceful shutdown to avoid request failures.',
    codeSnippet: '# Webhook deployment with HA\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: webhook-server\nspec:\n  replicas: 3\n  strategy:\n    rollingUpdate:\n      maxSurge: 1\n      maxUnavailable: 0  # Never reduce below 3 during update\n  template:\n    spec:\n      containers:\n        - name: webhook\n          readinessProbe:\n            httpGet:\n              path: /readyz\n              port: 9443\n              scheme: HTTPS\n            initialDelaySeconds: 5\n          # Handle SIGTERM gracefully\n          lifecycle:\n            preStop:\n              exec:\n                command: ["sh", "-c", "sleep 5"]\n          # Give time for in-flight requests\n          terminationGracePeriodSeconds: 30\n---\n# PodDisruptionBudget\napiVersion: policy/v1\nkind: PodDisruptionBudget\nmetadata:\n  name: webhook-pdb\nspec:\n  minAvailable: 2\n  selector:\n    matchLabels:\n      app: webhook-server',
    tags: ['kubernetes', 'webhooks', 'high-availability'],
  },
  {
    id: 'k8s-crds-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'How do you implement selective pruning where some fields allow unknown data while maintaining a structural schema?',
    options: [
      'Use x-kubernetes-preserve-unknown-fields at the root level',
      'Set x-kubernetes-preserve-unknown-fields: true on specific nested objects while maintaining structural schema for the rest of the CRD',
      'Disable pruning entirely',
      'Use additionalProperties: true everywhere',
    ],
    correctAnswer: 1,
    explanation: 'x-kubernetes-preserve-unknown-fields can be set per-field. Apply it only to specific objects that need flexible schemas while keeping strict structural schemas elsewhere.',
    codeSnippet: 'openAPIV3Schema:\n  type: object\n  properties:\n    spec:\n      type: object\n      properties:\n        # Strictly validated fields\n        replicas:\n          type: integer\n          minimum: 1\n        # Flexible field that allows arbitrary nested data\n        customConfig:\n          type: object\n          x-kubernetes-preserve-unknown-fields: true\n          # Can still have SOME validated fields within\n          properties:\n            version:\n              type: string  # This field IS validated\n            # Other fields under customConfig are preserved but not validated\n        # Another flexible pattern: typed map with free-form values\n        annotations:\n          type: object\n          additionalProperties:\n            type: string\n    status:\n      type: object\n      # Status is always strictly validated\n      properties:\n        phase:\n          type: string',
    tags: ['kubernetes', 'crd-definition', 'selective-pruning'],
  },
  {
    id: 'k8s-crds-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'What are the security considerations for operators that need to create RBAC resources dynamically?',
    options: [
      'There are no special considerations',
      'The operator needs escalation privileges to grant roles it holds; use RBAC aggregation, limit scope to specific API groups, and audit dynamic role creation',
      'Operators cannot create RBAC resources',
      'RBAC creation is automatically safe',
    ],
    correctAnswer: 1,
    explanation: 'An operator can only grant RBAC permissions it already has (escalation prevention). Use ClusterRole aggregation for extensibility and audit all dynamic RBAC changes.',
    codeSnippet: '# The operator service account needs:\n# 1. Permission to manage Roles/RoleBindings\n# 2. The actual permissions it wants to delegate\n\n# Example: Operator creates per-tenant RoleBindings\nrules:\n  - apiGroups: ["rbac.authorization.k8s.io"]\n    resources: ["roles", "rolebindings"]\n    verbs: ["create", "get", "list", "update", "delete"]\n  # Must have the permissions it wants to grant\n  - apiGroups: ["cache.example.com"]\n    resources: ["memcacheds"]\n    verbs: ["get", "list", "watch", "create", "update"]\n\n# The operator CANNOT grant permissions it doesn\'t have\n# This will fail at runtime:\n# Creating a Role with verbs: ["delete"] on memcacheds\n# if the operator SA only has ["get", "list"]\n\n# Best practice: Use ClusterRole aggregation\napiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRole\nmetadata:\n  name: memcached-viewer\n  labels:\n    rbac.authorization.k8s.io/aggregate-to-view: "true"\nrules:\n  - apiGroups: ["cache.example.com"]\n    resources: ["memcacheds"]\n    verbs: ["get", "list", "watch"]',
    tags: ['kubernetes', 'operator-pattern', 'rbac-security'],
  },
  {
    id: 'k8s-crds-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "dry-run reconciliation" pattern and how do you implement it for previewing changes?',
    options: [
      'Use kubectl --dry-run',
      'Implement a spec.dryRun field that makes the controller compute and report planned changes in status without actually applying them',
      'Dry-run reconciliation is the same as dry-run apply',
      'Use a separate controller for dry-run',
    ],
    correctAnswer: 1,
    explanation: 'A dry-run field in the CR spec tells the controller to compute the desired state, diff it against actual state, report planned changes in status, but not apply them.',
    codeSnippet: 'type MyResourceSpec struct {\n    DryRun   bool   `json:"dryRun,omitempty"`\n    Replicas int32  `json:"replicas"`\n}\n\ntype MyResourceStatus struct {\n    PlannedChanges []PlannedChange `json:"plannedChanges,omitempty"`\n    DryRunResult   string          `json:"dryRunResult,omitempty"`\n}\n\ntype PlannedChange struct {\n    Resource string `json:"resource"`\n    Action   string `json:"action"`  // create, update, delete\n    Diff     string `json:"diff,omitempty"`\n}\n\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    cr := &v1.MyResource{}\n    r.Get(ctx, req.NamespacedName, cr)\n\n    desired := r.computeDesiredState(cr)\n    actual := r.getCurrentState(ctx, cr)\n    changes := r.diff(desired, actual)\n\n    if cr.Spec.DryRun {\n        cr.Status.PlannedChanges = changes\n        cr.Status.DryRunResult = "computed"\n        return ctrl.Result{}, r.Status().Update(ctx, cr)\n    }\n\n    // Actually apply changes\n    return r.applyChanges(ctx, cr, changes)\n}',
    tags: ['kubernetes', 'reconciliation', 'dry-run'],
  },
  {
    id: 'k8s-crds-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement graceful operator migration from one operator to another for the same CRD?',
    options: [
      'Delete the old operator and install the new one',
      'Use field ownership transfer via server-side apply, coordinated leader election, and status handoff between old and new operators',
      'Run both operators simultaneously forever',
      'CRD migration between operators is not possible',
    ],
    correctAnswer: 1,
    explanation: 'Graceful migration: run both operators, use SSA field managers to transfer ownership, coordinate via leader election or feature flags, verify new operator behavior, then remove old.',
    tags: ['kubernetes', 'controllers', 'operator-migration'],
  },
  {
    id: 'k8s-crds-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'olm',
    question: 'How does OLM resolve dependency conflicts when two operators require different versions of the same CRD?',
    options: [
      'OLM installs both CRD versions',
      'OLM uses a SAT solver to find a compatible set of operator versions where all CRD dependencies are satisfied',
      'The first installed operator wins',
      'OLM cannot handle dependency conflicts',
    ],
    correctAnswer: 1,
    explanation: 'OLM uses a resolution algorithm (SAT solver) to find a compatible set of operators where all required CRDs and APIs are provided without conflicts.',
    tags: ['kubernetes', 'olm', 'dependency-resolution'],
  },
  {
    id: 'k8s-crds-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'What is the interaction between CRD validation, defaulting, and server-side apply managed fields?',
    options: [
      'They are independent features',
      'SSA tracks which manager set each field; CRD defaults are applied by the API server (manager: kube-apiserver); validation runs after defaulting; conflicts arise when SSA and defaults interact',
      'Validation runs before defaulting',
      'SSA disables CRD validation',
    ],
    correctAnswer: 1,
    explanation: 'With SSA: the user\'s field manager owns explicitly set fields. Defaults set by the API server are owned by it. This can cause unexpected conflicts if a user tries to change a defaulted field.',
    codeSnippet: '# Interaction chain:\n# 1. Client sends object via SSA (fieldManager: "my-controller")\n# 2. Mutating webhooks run (may set additional fields)\n# 3. CRD defaults are applied (for missing fields)\n#    - Defaults are "owned" by the API server\n# 4. Validation runs (schema + CEL + validating webhooks)\n# 5. Object is stored with managed fields tracking\n\n# Edge case:\n# - CRD has default: replicas = 3\n# - User creates object without setting replicas\n# - API server applies default, owns the "replicas" field\n# - User later tries to set replicas = 5 via SSA\n# - CONFLICT: API server owns replicas, user doesn\'t\n# - Fix: User must use forceOwnership or set the field on creation',
    tags: ['kubernetes', 'validation', 'ssa-defaults-interaction'],
  },
  {
    id: 'k8s-crds-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement an operator that uses leader-elected webhooks to avoid duplicate processing?',
    options: [
      'Webhooks do not need leader election',
      'All webhook replicas serve requests (webhooks are stateless), but ensure any side effects in webhooks check leader status or are idempotent',
      'Only the leader pod runs the webhook server',
      'Use a webhook-specific leader election',
    ],
    correctAnswer: 1,
    explanation: 'Webhooks must be served by all replicas (for HA). But if webhooks have side effects (external API calls), they must be idempotent or check leader status before performing them.',
    codeSnippet: '// controller-runtime setup:\n// - Webhooks run on ALL replicas (not just leader)\n// - Controllers run only on the leader\n// - This is the correct default behavior\n\nmgr, _ := ctrl.NewManager(cfg, ctrl.Options{\n    LeaderElection: true,\n    // Webhooks are NOT gated by leader election\n    // They must handle requests from any replica\n})\n\n// If webhook has side effects:\nfunc (h *Handler) Handle(ctx context.Context, req admission.Request) admission.Response {\n    // Pure validation/mutation: safe on all replicas\n    if h.isValidationOnly(req) {\n        return h.validate(req)\n    }\n\n    // Side effect (e.g., create external resource):\n    // Either make it idempotent or check leadership\n    if !h.isLeader() {\n        // Skip side effects on non-leader\n        // Still return the mutation/validation result\n    }\n    return h.handleWithSideEffects(req)\n}',
    tags: ['kubernetes', 'operator-sdk', 'webhook-leader-election'],
  },
  {
    id: 'k8s-crds-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the "informer transformation" feature and how does it enable running operators at massive scale?',
    options: [
      'It transforms CRD schemas',
      'Transform functions process objects before caching, allowing stripping of unnecessary data to dramatically reduce memory footprint at scale',
      'It transforms events before processing',
      'It is a data format conversion feature',
    ],
    correctAnswer: 1,
    explanation: 'Informer transforms run on each object before it enters the cache. By stripping managed fields, annotations, and unused spec fields, memory usage can be reduced by 50-80% at scale.',
    codeSnippet: '// At scale example: Watching 50,000 Pods\n// Without transform: ~50KB per Pod × 50,000 = ~2.5GB cache\n// With transform: ~5KB per Pod × 50,000 = ~250MB cache\n\ncache.Options{\n    ByObject: map[client.Object]cache.ByObject{\n        &corev1.Pod{}: {\n            Transform: func(i interface{}) (interface{}, error) {\n                pod := i.(*corev1.Pod)\n                // Keep only what the controller needs\n                stripped := &corev1.Pod{\n                    ObjectMeta: metav1.ObjectMeta{\n                        Name:            pod.Name,\n                        Namespace:       pod.Namespace,\n                        Labels:          pod.Labels,\n                        OwnerReferences: pod.OwnerReferences,\n                        ResourceVersion: pod.ResourceVersion,\n                    },\n                    Spec: corev1.PodSpec{\n                        NodeName: pod.Spec.NodeName,\n                    },\n                    Status: corev1.PodStatus{\n                        Phase: pod.Status.Phase,\n                    },\n                }\n                return stripped, nil\n            },\n        },\n    },\n}\n\n// WARNING: Stripped fields are gone from cache!\n// If reconciliation needs a stripped field, use APIReader for that specific Get',
    tags: ['kubernetes', 'controllers', 'informer-transform'],
  },
  {
    id: 'k8s-crds-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you implement a "safe rollback" mechanism in an operator for failed upgrades of managed applications?',
    options: [
      'Use Deployment rollback built into Kubernetes',
      'Track the previous desired state in an annotation or dedicated field, detect failure conditions, and automatically restore the previous configuration',
      'Manual rollback only',
      'Rollback is not an operator concern',
    ],
    correctAnswer: 1,
    explanation: 'Store the previous successful state (spec hash, configmap snapshot). On detecting failure (conditions, health checks), automatically restore the previous known-good state.',
    codeSnippet: 'type DatabaseStatus struct {\n    LastSuccessfulSpec    string         `json:"lastSuccessfulSpec,omitempty"`\n    LastSuccessfulSpecHash string        `json:"lastSuccessfulSpecHash,omitempty"`\n    UpgradeAttempts       int            `json:"upgradeAttempts,omitempty"`\n    RollbackPerformed     bool           `json:"rollbackPerformed,omitempty"`\n}\n\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    db := &v1.Database{}\n    r.Get(ctx, req.NamespacedName, db)\n\n    currentHash := hashSpec(db.Spec)\n    if currentHash != db.Status.LastSuccessfulSpecHash {\n        // Spec changed - attempting upgrade\n        if err := r.applyUpgrade(ctx, db); err != nil {\n            db.Status.UpgradeAttempts++\n            if db.Status.UpgradeAttempts >= 3 {\n                // Auto-rollback after 3 failures\n                var lastGoodSpec v1.DatabaseSpec\n                json.Unmarshal([]byte(db.Status.LastSuccessfulSpec), &lastGoodSpec)\n                db.Spec = lastGoodSpec\n                db.Status.RollbackPerformed = true\n                r.Update(ctx, db)\n                r.Recorder.Event(db, corev1.EventTypeWarning, "Rollback",\n                    "Auto-rollback after 3 failed upgrade attempts")\n            }\n            return ctrl.Result{RequeueAfter: 30 * time.Second}, nil\n        }\n        // Success!\n        specJSON, _ := json.Marshal(db.Spec)\n        db.Status.LastSuccessfulSpec = string(specJSON)\n        db.Status.LastSuccessfulSpecHash = currentHash\n        db.Status.UpgradeAttempts = 0\n    }\n    return ctrl.Result{}, r.Status().Update(ctx, db)\n}',
    tags: ['kubernetes', 'reconciliation', 'rollback'],
  },
  {
    id: 'k8s-crds-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'What is the impact of webhook failure on cluster control plane operations and how do you implement circuit breaker patterns?',
    options: [
      'Webhook failures have no impact on the control plane',
      'A failing webhook with failurePolicy: Fail can block ALL operations on matched resources including system operations; implement health-based circuit breakers that auto-switch to Ignore',
      'The API server automatically handles webhook failures',
      'Circuit breakers are not applicable to webhooks',
    ],
    correctAnswer: 1,
    explanation: 'A webhook with Fail policy that is down blocks all matching API operations. This can prevent node operations, pod scheduling, etc. Implement monitoring that patches the webhook to Ignore mode.',
    codeSnippet: '// Circuit breaker pattern for webhooks:\n\n// 1. Monitor webhook health\nfunc (r *WebhookMonitor) checkWebhookHealth(ctx context.Context) {\n    // Check if webhook service endpoints are healthy\n    endpoints := &corev1.Endpoints{}\n    err := r.Get(ctx, types.NamespacedName{\n        Name: "webhook-service", Namespace: "system"}, endpoints)\n\n    healthyEndpoints := countHealthyEndpoints(endpoints)\n\n    if healthyEndpoints == 0 {\n        // Circuit breaker: Switch to Ignore to unblock cluster\n        webhook := &admissionv1.ValidatingWebhookConfiguration{}\n        r.Get(ctx, types.NamespacedName{Name: "my-webhook"}, webhook)\n\n        for i := range webhook.Webhooks {\n            ignore := admissionv1.Ignore\n            webhook.Webhooks[i].FailurePolicy = &ignore\n        }\n        r.Update(ctx, webhook)\n\n        // Alert operators\n        r.Recorder.Event(webhook, corev1.EventTypeWarning, "CircuitBreakerOpen",\n            "Webhook failure policy changed to Ignore due to unhealthy endpoints")\n    }\n}\n\n// 2. Restore when healthy\n// When webhook pods recover, switch back to Fail policy',
    tags: ['kubernetes', 'webhooks', 'circuit-breaker'],
  },
  {
    id: 'k8s-crds-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement operator observability with the OpenTelemetry standard for distributed tracing across reconciliation?',
    options: [
      'Use log-based tracing only',
      'Instrument the reconciliation loop with OpenTelemetry spans, propagate trace context through annotations on managed resources',
      'Operators do not need tracing',
      'Use Prometheus metrics only',
    ],
    correctAnswer: 1,
    explanation: 'Instrument reconciliation with OTEL spans for each phase. Store trace IDs in resource annotations for correlation. Export to Jaeger/Tempo for end-to-end visibility.',
    codeSnippet: 'import "go.opentelemetry.io/otel"\n\nvar tracer = otel.Tracer("my-operator")\n\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    ctx, span := tracer.Start(ctx, "reconcile",\n        trace.WithAttributes(\n            attribute.String("resource.name", req.Name),\n            attribute.String("resource.namespace", req.Namespace),\n        ))\n    defer span.End()\n\n    // Phase 1: Read\n    ctx, readSpan := tracer.Start(ctx, "read-resource")\n    cr := &v1.MyResource{}\n    r.Get(ctx, req.NamespacedName, cr)\n    readSpan.End()\n\n    // Phase 2: Compute desired state\n    ctx, computeSpan := tracer.Start(ctx, "compute-desired-state")\n    desired := r.computeDesired(cr)\n    computeSpan.End()\n\n    // Phase 3: Apply changes\n    ctx, applySpan := tracer.Start(ctx, "apply-changes")\n    err := r.apply(ctx, desired)\n    if err != nil {\n        span.SetStatus(codes.Error, err.Error())\n        span.RecordError(err)\n    }\n    applySpan.End()\n\n    // Store trace ID for correlation\n    cr.Annotations["last-reconcile-trace"] = span.SpanContext().TraceID().String()\n\n    return ctrl.Result{}, err\n}',
    tags: ['kubernetes', 'operator-pattern', 'observability'],
  },
  {
    id: 'k8s-crds-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you implement CRD validation that prevents privilege escalation through custom resources?',
    options: [
      'Trust all user input',
      'Validate that referenced resources (ServiceAccounts, Secrets, namespaces) exist and the user has permissions to use them via SubjectAccessReview in a webhook',
      'RBAC on the CRD is sufficient',
      'Privilege escalation is not possible through CRDs',
    ],
    correctAnswer: 1,
    explanation: 'A user might reference a privileged ServiceAccount or Secret they do not have direct access to. Validate permissions via SubjectAccessReview in the admission webhook.',
    codeSnippet: 'func (v *Validator) Handle(ctx context.Context, req admission.Request) admission.Response {\n    db := &v1.Database{}\n    v.decoder.Decode(req, db)\n\n    // Check if the requesting user can use the referenced ServiceAccount\n    sar := &authorizationv1.SubjectAccessReview{\n        Spec: authorizationv1.SubjectAccessReviewSpec{\n            User:   req.UserInfo.Username,\n            Groups: req.UserInfo.Groups,\n            ResourceAttributes: &authorizationv1.ResourceAttributes{\n                Verb:      "use",\n                Group:     "",\n                Resource:  "serviceaccounts",\n                Name:      db.Spec.ServiceAccountName,\n                Namespace: db.Namespace,\n            },\n        },\n    }\n    v.client.Create(ctx, sar)\n\n    if !sar.Status.Allowed {\n        return admission.Denied(fmt.Sprintf(\n            "user %s is not allowed to use ServiceAccount %s",\n            req.UserInfo.Username, db.Spec.ServiceAccountName))\n    }\n\n    // Similarly check referenced Secrets, PVCs, etc.\n    return admission.Allowed("")\n}',
    tags: ['kubernetes', 'validation', 'privilege-escalation'],
  },
  {
    id: 'k8s-crds-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How does the Kubernetes API server guarantee watch ordering and what are the implications for eventual consistency in operators?',
    options: [
      'Watch events are unordered',
      'Events for a single object are delivered in order of their resourceVersion; events for different objects may interleave; operators must handle partial views during convergence',
      'All events across all objects are globally ordered',
      'Events are delivered in random order',
    ],
    correctAnswer: 1,
    explanation: 'Per-object events are ordered by resourceVersion. Cross-object ordering is not guaranteed. An operator may see a child resource update before the parent update that caused it.',
    tags: ['kubernetes', 'controllers', 'event-ordering'],
  },
  {
    id: 'k8s-crds-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "apply-and-wait" anti-pattern and how should you design reconciliation to avoid it?',
    options: [
      'Apply-and-wait is the recommended pattern',
      'Waiting synchronously in reconciliation for resources to become ready blocks the worker; instead, set conditions, return, and let the next reconciliation check status',
      'Apply-and-wait is only a problem with webhooks',
      'This pattern does not exist',
    ],
    correctAnswer: 1,
    explanation: 'Blocking in Reconcile (waiting for a Deployment to roll out) holds a worker thread. Instead, check the status, set appropriate conditions, and return with RequeueAfter.',
    codeSnippet: '// ANTI-PATTERN: blocking in reconciliation\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    r.Create(ctx, deployment)\n    // DON\'T DO THIS: blocks the worker for up to 5 minutes\n    wait.PollImmediate(5*time.Second, 5*time.Minute, func() (bool, error) {\n        dep := &appsv1.Deployment{}\n        r.Get(ctx, depKey, dep)\n        return dep.Status.ReadyReplicas == *dep.Spec.Replicas, nil\n    })\n    return ctrl.Result{}, nil\n}\n\n// CORRECT: non-blocking reconciliation\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    // Ensure deployment exists\n    dep := &appsv1.Deployment{}\n    err := r.Get(ctx, depKey, dep)\n    if apierrors.IsNotFound(err) {\n        r.Create(ctx, desiredDep)\n        // Return and wait for the Owns() watch to trigger next reconciliation\n        return ctrl.Result{}, nil\n    }\n\n    // Check if ready\n    if dep.Status.ReadyReplicas < *dep.Spec.Replicas {\n        meta.SetStatusCondition(&cr.Status.Conditions, metav1.Condition{\n            Type: "Available", Status: metav1.ConditionFalse,\n            Reason: "DeploymentNotReady",\n        })\n        r.Status().Update(ctx, cr)\n        // Reconciliation will be triggered by the Owns() watch\n        // when the Deployment status changes\n        return ctrl.Result{}, nil\n    }\n\n    // Deployment is ready, proceed\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'reconciliation', 'anti-patterns'],
  },
  {
    id: 'k8s-crds-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement a chaos engineering strategy for testing operator resilience?',
    options: [
      'Operators do not need chaos testing',
      'Inject failures at multiple levels: API server latency, etcd errors, watch disconnects, webhook timeouts, and managed resource failures to verify operator recovery',
      'Only test happy paths',
      'Use standard unit tests',
    ],
    correctAnswer: 1,
    explanation: 'Chaos testing for operators includes: killing the operator pod, partitioning from API server, corrupting CRD status, deleting managed resources, and inducing webhook failures.',
    codeSnippet: '// Chaos test scenarios for operators:\n\n// 1. Kill operator pod during mid-reconciliation\n// Verify: reconciliation resumes correctly after restart\n// Check: no orphaned resources, status is eventually correct\n\n// 2. Simulate API server latency (using toxiproxy)\n// Verify: operator handles context timeouts gracefully\n// Check: no goroutine leaks, proper backoff\n\n// 3. Delete managed resources behind operator\'s back\n// Verify: operator detects drift and recreates resources\n// Check: no duplicate resources, correct owner references\n\n// 4. Corrupt status (external tool sets wrong status)\n// Verify: next reconciliation corrects the status\n// Check: observedGeneration is updated correctly\n\n// 5. Webhook server becomes unavailable\n// Verify: cluster operations continue (with Ignore policy)\n// Check: operator logs webhook errors\n\n// 6. etcd compaction causes watch disconnect (410 Gone)\n// Verify: informer re-lists and recovers\n// Check: no events are lost, reconciliation continues\n\n// Implementation: Use Litmus, Chaos Mesh, or custom test harness\n// Run these tests in CI/CD on every operator release',
    tags: ['kubernetes', 'operator-pattern', 'chaos-testing'],
  },
  {
    id: 'k8s-crds-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement a controller that needs to maintain a global ordering or sequence number across all custom resources?',
    options: [
      'Use a counter in memory',
      'Use a dedicated ConfigMap or Lease as a distributed counter with optimistic concurrency, or leverage the monotonically increasing resourceVersion',
      'Use a database',
      'Global ordering is not possible in Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Use a ConfigMap/Lease as a distributed sequence counter with optimistic concurrency (resourceVersion). Or use resourceVersion for approximate ordering (not guaranteed across resource types).',
    codeSnippet: 'func (r *Reconciler) getNextSequence(ctx context.Context) (int64, error) {\n    return retry.RetryOnConflict(retry.DefaultRetry, func() error {\n        counter := &corev1.ConfigMap{}\n        err := r.Get(ctx, types.NamespacedName{\n            Name: "sequence-counter", Namespace: "system"}, counter)\n\n        if apierrors.IsNotFound(err) {\n            counter = &corev1.ConfigMap{\n                ObjectMeta: metav1.ObjectMeta{\n                    Name: "sequence-counter", Namespace: "system"},\n                Data: map[string]string{"value": "1"},\n            }\n            return r.Create(ctx, counter)\n        }\n\n        current, _ := strconv.ParseInt(counter.Data["value"], 10, 64)\n        next := current + 1\n        counter.Data["value"] = strconv.FormatInt(next, 10)\n\n        // This fails with Conflict if another controller instance\n        // incremented the counter since our read\n        return r.Update(ctx, counter)\n    })\n}',
    tags: ['kubernetes', 'controllers', 'distributed-counter'],
  },
  {
    id: 'k8s-crds-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'status-subresource',
    question: 'What is the "generation gap" problem with status updates and how do observedGeneration and conditions work together to solve it?',
    options: [
      'There is no generation gap problem',
      'Between a spec change and status update, there is a period where status does not reflect the current spec; observedGeneration bridges this gap by indicating which generation the status reflects',
      'Generation and status are always in sync',
      'Use timestamps instead of generation',
    ],
    correctAnswer: 1,
    explanation: 'After a spec change (generation increments), the controller needs time to reconcile. observedGeneration in status indicates which generation was last successfully processed.',
    codeSnippet: '// The generation gap problem:\n// t0: User updates spec (generation: 5 -> 6)\n// t1: Status still shows observedGeneration: 5 (stale!)\n// t2: Controller reconciles, updates observedGeneration: 6\n\n// Client checking if update has been processed:\nfunc isReconciled(cr *v1.MyResource) bool {\n    return cr.Status.ObservedGeneration >= cr.Generation\n}\n\n// Combined with conditions for full picture:\nfunc getResourceState(cr *v1.MyResource) string {\n    if cr.Status.ObservedGeneration < cr.Generation {\n        return "Reconciling" // Spec changed, not yet processed\n    }\n    if meta.IsStatusConditionTrue(cr.Status.Conditions, "Available") {\n        return "Ready" // Processed and healthy\n    }\n    if meta.IsStatusConditionTrue(cr.Status.Conditions, "Degraded") {\n        return "Degraded" // Processed but unhealthy\n    }\n    return "Unknown"\n}\n\n// In kubectl wait:\n// kubectl wait myresource/test --for=jsonpath=\'{.status.observedGeneration}\'=6',
    tags: ['kubernetes', 'status-subresource', 'generation-gap'],
  },
  {
    id: 'k8s-crds-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'crd-definition',
    question: 'How do you implement a CRD that supports the Kubernetes "conditions ready gate" for controlling pod readiness?',
    options: [
      'Conditions ready gates only work with built-in resources',
      'Use the scale subresource with custom status fields that pod readiness gates can reference through a custom controller',
      'CRDs cannot influence pod readiness',
      'Use readinessProbes directly on the CRD',
    ],
    correctAnswer: 1,
    explanation: 'While CRDs cannot directly define pod readiness gates, an operator can manage pod readinessGates and set custom conditions on pods based on CRD state.',
    tags: ['kubernetes', 'crd-definition', 'readiness-gates'],
  },
  {
    id: 'k8s-crds-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How do you handle the "tombstone" problem in controller-runtime informers?',
    options: [
      'Tombstones do not exist in Kubernetes',
      'When the informer cache misses a DELETE event (e.g., due to watch disconnect), it creates a DeletedFinalStateUnknown tombstone that the controller must handle',
      'Tombstones are etcd-specific',
      'The informer automatically handles tombstones',
    ],
    correctAnswer: 1,
    explanation: 'If the informer misses a delete during a watch gap, it creates a tombstone (DeletedFinalStateUnknown). Event handlers must check for this type and handle it gracefully.',
    codeSnippet: '// In controller-runtime, tombstones are handled internally\n// But if using client-go directly:\nhandler := cache.ResourceEventHandlerFuncs{\n    DeleteFunc: func(obj interface{}) {\n        // Handle tombstones from missed deletes\n        if tombstone, ok := obj.(cache.DeletedFinalStateUnknown); ok {\n            obj = tombstone.Obj\n            if obj == nil {\n                // Object was deleted and we don\'t have its final state\n                // Must handle gracefully\n                return\n            }\n        }\n        resource := obj.(*v1alpha1.MyResource)\n        // Process deletion...\n    },\n}\n\n// controller-runtime predicates also need to handle tombstones:\nWithEventFilter(predicate.Funcs{\n    DeleteFunc: func(e event.DeleteEvent) bool {\n        // e.DeleteStateUnknown is true for tombstones\n        return !e.DeleteStateUnknown\n    },\n})',
    tags: ['kubernetes', 'reconciliation', 'tombstones'],
  },
  {
    id: 'k8s-crds-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement an operator that supports canary-style rollout of its own CRD schema changes?',
    options: [
      'CRD schema changes are always atomic',
      'Serve both old and new versions simultaneously, use conversion webhooks for compatibility, migrate users gradually, and monitor for conversion errors',
      'Deploy a new CRD with a different name',
      'Schema canary is not possible',
    ],
    correctAnswer: 1,
    explanation: 'Use multi-version CRDs to serve both schemas. Conversion webhooks handle translation. Monitor conversion error rates. Gradually migrate users to the new version before removing the old one.',
    tags: ['kubernetes', 'operator-pattern', 'schema-canary'],
  },
  {
    id: 'k8s-crds-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you implement efficient bulk reconciliation for operators managing thousands of related resources?',
    options: [
      'Reconcile each resource individually',
      'Use a "parent reconciliation" pattern: reconcile the parent CR once, compute desired state for all children in one pass, diff against actual state, and batch API calls',
      'Use goroutines for each resource',
      'Let Kubernetes handle it automatically',
    ],
    correctAnswer: 1,
    explanation: 'Instead of reconciling each child resource individually, compute all desired children from the parent CR in one reconciliation, diff against listed children, and batch creates/updates/deletes.',
    codeSnippet: 'func (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    cr := &v1.Fleet{}\n    r.Get(ctx, req.NamespacedName, cr)\n\n    // Compute all desired children at once\n    desiredPods := r.computeDesiredPods(cr)  // e.g., 1000 pods\n\n    // List all existing children with a single API call\n    existingPods := &corev1.PodList{}\n    r.List(ctx, existingPods, client.InNamespace(cr.Namespace),\n        client.MatchingLabels{"fleet": cr.Name})\n\n    // Diff efficiently\n    toCreate, toUpdate, toDelete := r.diff(desiredPods, existingPods.Items)\n\n    // Batch creates (with rate limiting)\n    for _, pod := range toCreate {\n        if err := r.Create(ctx, &pod); err != nil {\n            // Record error, continue with others\n        }\n    }\n\n    // Batch updates\n    for _, pod := range toUpdate {\n        if err := r.Update(ctx, &pod); err != nil {\n            // Use Patch for better conflict handling\n        }\n    }\n\n    // Batch deletes\n    for _, pod := range toDelete {\n        r.Delete(ctx, &pod)\n    }\n\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'controllers', 'bulk-reconciliation'],
  },
  {
    id: 'k8s-crds-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'finalizers',
    question: 'What is the "finalizer deadlock" scenario and how do you prevent it in complex operator hierarchies?',
    options: [
      'Finalizer deadlocks cannot happen',
      'Circular finalizer dependencies between resources can cause deletion to hang; prevent with directed acyclic deletion order and timeout-based escape hatches',
      'Deadlocks are resolved automatically by the GC',
      'Only one finalizer per namespace is allowed',
    ],
    correctAnswer: 1,
    explanation: 'If Resource A has a finalizer waiting for Resource B to be deleted, and B has a finalizer waiting for A, neither can be deleted. Prevent with DAG-based deletion ordering and timeouts.',
    codeSnippet: '// Deadlock scenario:\n// ResourceA finalizer: "wait for ResourceB to be deleted"\n// ResourceB finalizer: "wait for ResourceA to be deleted"\n// Both are stuck in Terminating forever!\n\n// Prevention strategies:\n// 1. Establish a clear deletion hierarchy (DAG)\n//    Delete children BEFORE parents\n//    Never wait for parent deletion in child finalizer\n\n// 2. Implement timeout-based escape\nfunc (r *Reconciler) handleFinalizer(ctx context.Context, cr *v1.Resource) error {\n    cleanupStart := cr.DeletionTimestamp.Time\n    if time.Since(cleanupStart) > 5*time.Minute {\n        // Timeout: force remove finalizer to break potential deadlock\n        log.Error(nil, "finalizer cleanup timed out, forcing removal",\n            "resource", cr.Name,\n            "elapsed", time.Since(cleanupStart))\n        controllerutil.RemoveFinalizer(cr, finalizerName)\n        return r.Update(ctx, cr)\n    }\n    // Normal cleanup logic...\n    return nil\n}\n\n// 3. Use owner references for automatic GC instead of finalizers\n//    wherever possible (no deadlock risk with GC)',
    tags: ['kubernetes', 'finalizers', 'deadlock'],
  },
  {
    id: 'k8s-crds-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'api-versioning',
    question: 'What are the implications of changing a CRD from Namespaced to Cluster scope or vice versa?',
    options: [
      'Scope can be changed freely',
      'Scope changes require deleting and recreating the CRD because the API endpoints change; existing data is lost unless manually migrated',
      'Scope changes are automatic',
      'Scope cannot be changed after creation',
    ],
    correctAnswer: 1,
    explanation: 'Changing scope changes the API path (from namespaced to non-namespaced endpoints). This requires deleting the CRD (losing all data), recreating with new scope, and re-creating resources.',
    codeSnippet: '# Namespaced: /apis/cache.example.com/v1/namespaces/{ns}/memcacheds\n# Cluster:    /apis/cache.example.com/v1/memcacheds\n\n# Migration procedure:\n# 1. Export all existing custom resources\nkubectl get memcacheds -A -o yaml > backup.yaml\n\n# 2. Delete the CRD (WARNING: deletes ALL instances)\nkubectl delete crd memcacheds.cache.example.com\n\n# 3. Recreate CRD with new scope\n# spec:\n#   scope: Cluster  # Changed from Namespaced\n\n# 4. Recreate resources (adjust manifests - remove/add namespace)\n# For namespaced->cluster: remove namespace from metadata\n# For cluster->namespaced: add namespace to metadata\n\n# IMPORTANT: This is a breaking change for all users\n# Plan this as a major version bump (v1 -> v2)',
    tags: ['kubernetes', 'api-versioning', 'scope-change'],
  },
  {
    id: 'k8s-crds-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-sdk',
    question: 'How do you implement operator-level feature gates that can be toggled without restarting the operator?',
    options: [
      'Use compile-time flags',
      'Watch a ConfigMap or CRD for feature flag changes and dynamically enable/disable controller behavior',
      'Feature gates require operator restart',
      'Use environment variables only',
    ],
    correctAnswer: 1,
    explanation: 'Watch a ConfigMap or dedicated CRD for feature flags. Update in-memory feature state on changes. Controllers check feature flags during reconciliation.',
    codeSnippet: 'type FeatureGates struct {\n    mu       sync.RWMutex\n    features map[string]bool\n}\n\nfunc (f *FeatureGates) Enabled(name string) bool {\n    f.mu.RLock()\n    defer f.mu.RUnlock()\n    return f.features[name]\n}\n\n// Watch ConfigMap for dynamic feature flags\nctrl.NewControllerManagedBy(mgr).\n    For(&corev1.ConfigMap{}, builder.WithPredicates(\n        predicate.NewPredicateFuncs(func(obj client.Object) bool {\n            return obj.GetName() == "operator-features" &&\n                   obj.GetNamespace() == "system"\n        }),\n    )).\n    Complete(&FeatureGateReconciler{gates: featureGates})\n\n// In main reconciler\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    if r.featureGates.Enabled("advanced-scheduling") {\n        // Use new scheduling logic\n    } else {\n        // Use legacy logic\n    }\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'operator-sdk', 'feature-gates'],
  },
  {
    id: 'k8s-crds-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'webhooks',
    question: 'How do you implement a "policy engine" using admission webhooks that supports pluggable policy rules?',
    options: [
      'Hardcode all policies in the webhook',
      'Load policy rules from CRDs or ConfigMaps at runtime, evaluate them dynamically against incoming objects using a rule engine',
      'Use one webhook per policy',
      'Policies cannot be dynamic',
    ],
    correctAnswer: 1,
    explanation: 'Define policies as CRDs (or use OPA/Gatekeeper). The webhook loads policies from the cluster, evaluates incoming objects against all matching policies, and aggregates results.',
    codeSnippet: '// Policy CRD\napiVersion: policy.example.com/v1\nkind: ValidationPolicy\nmetadata:\n  name: require-labels\nspec:\n  match:\n    kinds: ["Memcached"]\n  rules:\n    - field: metadata.labels.team\n      operator: Required\n      message: "team label is required"\n    - field: spec.replicas\n      operator: LessThanOrEqual\n      value: "50"\n      message: "replicas must be <= 50"\n---\n// Webhook evaluates all matching policies\nfunc (h *PolicyHandler) Handle(ctx context.Context, req admission.Request) admission.Response {\n    policies := &v1.ValidationPolicyList{}\n    h.client.List(ctx, policies)\n\n    var violations []string\n    for _, policy := range policies.Items {\n        if !policy.Matches(req.Resource) {\n            continue\n        }\n        for _, rule := range policy.Spec.Rules {\n            if !rule.Evaluate(req.Object) {\n                violations = append(violations, rule.Message)\n            }\n        }\n    }\n\n    if len(violations) > 0 {\n        return admission.Denied(strings.Join(violations, "; "))\n    }\n    return admission.Allowed("")\n}',
    tags: ['kubernetes', 'webhooks', 'policy-engine'],
  },
  {
    id: 'k8s-crds-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'What is the "state machine" pattern for complex operator reconciliation and how does it prevent state corruption?',
    options: [
      'State machines are too complex for operators',
      'Model the resource lifecycle as a finite state machine with explicit transitions, guards, and recovery states; persist state in status to survive restarts',
      'Use if-else chains instead',
      'Kubernetes resources do not have state',
    ],
    correctAnswer: 1,
    explanation: 'A formal state machine ensures only valid transitions occur, preventing state corruption from concurrent operations or partial failures. The current state is always persisted in status.',
    codeSnippet: 'type State string\nconst (\n    StatePending     State = "Pending"\n    StateCreating    State = "Creating"\n    StateRunning     State = "Running"\n    StateUpgrading   State = "Upgrading"\n    StateFailed      State = "Failed"\n    StateTerminating State = "Terminating"\n)\n\nvar validTransitions = map[State][]State{\n    StatePending:     {StateCreating, StateTerminating},\n    StateCreating:    {StateRunning, StateFailed, StateTerminating},\n    StateRunning:     {StateUpgrading, StateTerminating},\n    StateUpgrading:   {StateRunning, StateFailed, StateTerminating},\n    StateFailed:      {StateCreating, StateTerminating},  // Retry\n    StateTerminating: {},  // Terminal state\n}\n\nfunc (r *Reconciler) transition(cr *v1.DB, to State) error {\n    from := State(cr.Status.Phase)\n    valid := false\n    for _, allowed := range validTransitions[from] {\n        if allowed == to { valid = true; break }\n    }\n    if !valid {\n        return fmt.Errorf("invalid transition %s -> %s", from, to)\n    }\n    cr.Status.Phase = string(to)\n    cr.Status.LastTransition = metav1.Now()\n    return nil\n}',
    tags: ['kubernetes', 'reconciliation', 'state-machine'],
  },
  {
    id: 'k8s-crds-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'How do you handle the "watch bookmark" feature gate and its impact on controller resilience during API server upgrades?',
    options: [
      'Bookmarks are always available',
      'WatchBookmarks are GA since 1.17 but require AllowWatchBookmarks in ListOptions; without them, controllers must re-list from scratch after watch disconnection',
      'Bookmarks are deprecated',
      'Bookmarks are only for etcd',
    ],
    correctAnswer: 1,
    explanation: 'WatchBookmarks (GA since 1.17) send periodic resourceVersion updates. controller-runtime enables them by default. Without bookmarks, watch reconnection requires a full re-list.',
    tags: ['kubernetes', 'controllers', 'watch-bookmarks'],
  },
  {
    id: 'k8s-crds-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you design an operator to be safely uninstalled without leaving orphaned resources or broken cluster state?',
    options: [
      'Just delete the operator deployment',
      'Implement a pre-uninstall lifecycle: remove finalizers from all CRs, delete managed resources, remove webhooks, then delete CRDs in the correct order',
      'Kubernetes handles cleanup automatically',
      'Orphaned resources are acceptable',
    ],
    correctAnswer: 1,
    explanation: 'Safe uninstall requires: remove webhook configs first (to unblock API), clean up finalizers, delete managed non-CR resources, delete CRs, then delete CRDs.',
    codeSnippet: '# Safe operator uninstall procedure:\n\n# 1. FIRST: Remove webhook configurations to prevent blocking\nkubectl delete validatingwebhookconfiguration my-operator-webhook\nkubectl delete mutatingwebhookconfiguration my-operator-webhook\n\n# 2. Scale down to prevent new reconciliation\nkubectl scale deployment operator -n system --replicas=0\n\n# 3. Clean up finalizers on all custom resources\nfor ns in $(kubectl get namespaces -o name); do\n  kubectl get memcacheds -n $ns -o name | xargs -I {} \\\n    kubectl patch {} -n $ns --type=merge \\\n    -p \'{"metadata":{"finalizers":null}}\'\ndone\n\n# 4. Delete all custom resources (triggers GC of owned resources)\nkubectl delete memcacheds --all -A\n\n# 5. Wait for GC to clean up owned resources\nkubectl wait --for=delete deployment -l app.kubernetes.io/managed-by=my-operator -A\n\n# 6. Delete the CRD (any remaining CRs are deleted)\nkubectl delete crd memcacheds.cache.example.com\n\n# 7. Delete operator resources (deployment, RBAC, etc.)\nkubectl delete -f operator-install.yaml',
    tags: ['kubernetes', 'operator-pattern', 'uninstall'],
  },
  {
    id: 'k8s-crds-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'validation',
    question: 'How do you implement a CRD that supports "spec.forProvider" and "spec.initProvider" patterns used in Crossplane-style providers?',
    options: [
      'These patterns are not relevant to CRDs',
      'Use nested object schemas with management policies: forProvider for desired state, initProvider for create-only defaults, and separate status.atProvider for observed state',
      'Use two separate CRDs',
      'Use annotations for provider configuration',
    ],
    correctAnswer: 1,
    explanation: 'Crossplane providers use spec.forProvider (reconciled continuously), spec.initProvider (applied only on creation), and status.atProvider (observed external state). This enables declarative external resource management.',
    codeSnippet: 'openAPIV3Schema:\n  type: object\n  properties:\n    spec:\n      type: object\n      properties:\n        managementPolicies:\n          type: array\n          items:\n            type: string\n            enum: ["Observe", "Create", "Update", "Delete", "*"]\n          default: ["*"]\n        forProvider:\n          type: object\n          description: "Desired state - continuously reconciled"\n          properties:\n            region:\n              type: string\n            instanceType:\n              type: string\n        initProvider:\n          type: object\n          description: "Init-only defaults - applied at creation, overridden by forProvider"\n          properties:\n            instanceType:\n              type: string\n    status:\n      type: object\n      properties:\n        atProvider:\n          type: object\n          description: "Observed state of the external resource"\n          properties:\n            id:\n              type: string\n            arn:\n              type: string\n            state:\n              type: string',
    tags: ['kubernetes', 'validation', 'crossplane-patterns'],
  },
  {
    id: 'k8s-crds-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'controllers',
    question: 'What is the performance impact of watch cache size on API server and how does it relate to CRD controller scaling?',
    options: [
      'Watch cache has unlimited capacity',
      'The default watch cache size is 100 events per resource type; heavily-watched CRDs may need tuning to prevent cache thrashing and unnecessary re-lists',
      'Watch cache is fixed and cannot be configured',
      'Each controller has its own API server cache',
    ],
    correctAnswer: 1,
    explanation: 'The API server watch cache has configurable capacity per resource type (--watch-cache-sizes). If the cache overflows, watches receive stale data and may need re-lists.',
    codeSnippet: '# API server watch cache configuration:\n# --default-watch-cache-size=100    (events per resource type)\n# --watch-cache-sizes=resource.group=size,...\n\n# For CRDs with many watchers and frequent updates:\nkube-apiserver \\\n  --watch-cache-sizes=memcacheds.cache.example.com=1000\n\n# Impact of insufficient cache:\n# 1. Watch events are evicted from cache\n# 2. Slow watchers get 410 Gone (missed events)\n# 3. They must re-list (expensive!)\n# 4. Re-list amplifies API server load\n# 5. Cycle repeats under load\n\n# Monitoring:\n# - apiserver_cache_list_fetched_objects_total\n# - apiserver_cache_list_returned_objects_total\n# - watch_cache_capacity\n# - watch_cache_capacity_increase_total',
    tags: ['kubernetes', 'controllers', 'watch-cache'],
  },
  {
    id: 'k8s-crds-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement a "pause reconciliation" feature for operators during maintenance windows?',
    options: [
      'Scale the operator to 0',
      'Check for a pause annotation or spec field during reconciliation; skip all changes but continue status reporting when paused',
      'Use PodDisruptionBudget to pause',
      'Disable the CRD',
    ],
    correctAnswer: 1,
    explanation: 'Implement a pause mechanism via annotation or spec field. When paused, the controller skips mutations but continues to read and report status, allowing manual intervention.',
    codeSnippet: 'const pauseAnnotation = "operator.example.com/paused"\n\nfunc (r *Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    cr := &v1.MyResource{}\n    r.Get(ctx, req.NamespacedName, cr)\n\n    // Check for pause\n    if cr.Annotations[pauseAnnotation] == "true" {\n        // Update status to indicate paused\n        meta.SetStatusCondition(&cr.Status.Conditions, metav1.Condition{\n            Type:    "Paused",\n            Status:  metav1.ConditionTrue,\n            Reason:  "UserRequested",\n            Message: "Reconciliation paused via annotation",\n        })\n        r.Status().Update(ctx, cr)\n\n        // Still watch for unpause (requeue periodically)\n        return ctrl.Result{RequeueAfter: 30 * time.Second}, nil\n    }\n\n    // Remove paused condition when unpaused\n    meta.RemoveStatusCondition(&cr.Status.Conditions, "Paused")\n\n    // Normal reconciliation...\n    return ctrl.Result{}, nil\n}\n\n// Usage:\n// kubectl annotate myresource test operator.example.com/paused=true\n// kubectl annotate myresource test operator.example.com/paused-',
    tags: ['kubernetes', 'operator-pattern', 'pause-reconciliation'],
  },
  {
    id: 'k8s-crds-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'reconciliation',
    question: 'How does the Kubernetes API server handle request priority and fairness (APF) for CRD requests from operators?',
    options: [
      'All requests have equal priority',
      'APF classifies requests into priority levels and queues; operator service account requests may be throttled under load if not in a high-priority flow schema',
      'CRD requests bypass APF',
      'Operators always get highest priority',
    ],
    correctAnswer: 1,
    explanation: 'APF classifies requests by FlowSchema. Operator requests may land in the default workload priority level. Configure a dedicated FlowSchema for critical operators.',
    codeSnippet: '# Give operator requests higher priority\napiVersion: flowcontrol.apiserver.k8s.io/v1beta3\nkind: FlowSchema\nmetadata:\n  name: my-operator-high-priority\nspec:\n  priorityLevelConfiguration:\n    name: workload-high\n  matchingPrecedence: 100\n  rules:\n    - subjects:\n        - kind: ServiceAccount\n          serviceAccount:\n            name: my-operator\n            namespace: system\n      resourceRules:\n        - apiGroups: ["cache.example.com"]\n          resources: ["*"]\n          namespaces: ["*"]\n          verbs: ["*"]\n\n# Monitor APF throttling:\n# apiserver_flowcontrol_request_queue_length_after_enqueue\n# apiserver_flowcontrol_request_concurrency_limit\n# apiserver_flowcontrol_rejected_requests_total',
    tags: ['kubernetes', 'reconciliation', 'api-priority-fairness'],
  },
  {
    id: 'k8s-crds-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'CRDs & Operators',
    subcategory: 'operator-pattern',
    question: 'How do you implement an operator that uses the "composition" pattern to build complex resources from simpler components?',
    options: [
      'Use Helm templates',
      'Define a high-level CRD that composes multiple lower-level CRDs and built-in resources, using a reconciler that orchestrates the creation and lifecycle of all components',
      'Use kustomize overlays',
      'Composition is not an operator pattern',
    ],
    correctAnswer: 1,
    explanation: 'The composition pattern uses a top-level CRD that decomposes into multiple resources. The reconciler manages the full component graph, handling ordering, dependencies, and health aggregation.',
    codeSnippet: '// High-level CRD: Application\n// Composes: Deployment, Service, Ingress, PDB, HPA, ConfigMap, Secret\n\nfunc (r *AppReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {\n    app := &v1.Application{}\n    r.Get(ctx, req.NamespacedName, app)\n\n    // Build component graph\n    components := []Component{\n        r.buildConfigMap(app),     // Order 1: ConfigMap first\n        r.buildSecret(app),        // Order 1: Secret in parallel\n        r.buildDeployment(app),    // Order 2: Depends on ConfigMap, Secret\n        r.buildService(app),       // Order 2: Can be parallel with Deployment\n        r.buildHPA(app),           // Order 3: Depends on Deployment\n        r.buildPDB(app),           // Order 3: Depends on Deployment\n        r.buildIngress(app),       // Order 3: Depends on Service\n    }\n\n    // Apply in dependency order\n    for _, component := range components {\n        if err := r.applyComponent(ctx, app, component); err != nil {\n            app.Status.Phase = "Degraded"\n            app.Status.Message = fmt.Sprintf("Failed to apply %s: %v",\n                component.Name, err)\n            r.Status().Update(ctx, app)\n            return ctrl.Result{RequeueAfter: 30 * time.Second}, nil\n        }\n    }\n\n    // Aggregate health from all components\n    app.Status.Phase = r.aggregateHealth(ctx, app, components)\n    r.Status().Update(ctx, app)\n    return ctrl.Result{}, nil\n}',
    tags: ['kubernetes', 'operator-pattern', 'composition'],
  },
];

export default questions;
