import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'k8s-networking-l4-001',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'In Cilium, what is the purpose of the "identity" concept and how does it relate to network policy enforcement?',
    options: [
      'Identity is the Pod name used for DNS resolution',
      'Each unique set of labels gets a numeric identity; eBPF programs use identities instead of IP addresses for policy enforcement, making it faster and more resilient to IP changes',
      'Identity is the TLS certificate assigned to each Pod',
      'Identity is the unique ID of the CNI configuration',
    ],
    correctAnswer: 1,
    explanation: 'Cilium assigns a numeric security identity to each unique set of labels. eBPF datapath programs use these identities (not IPs) for policy decisions, which is faster and handles IP churn better than IP-based rules.',
    tags: ['kubernetes', 'cni', 'cilium', 'identity', 'ebpf'],
  },
  {
    id: 'k8s-networking-l4-002',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'How does the kube-proxy iptables mode implement random load balancing across N endpoints?',
    options: [
      'Uses round-robin iptables rules',
      'Uses probability-based DNAT rules where each rule has probability 1/remaining_endpoints',
      'Uses connection tracking to distribute evenly',
      'Uses a hash of the source IP',
    ],
    correctAnswer: 1,
    explanation: 'kube-proxy creates a chain of DNAT rules with calculated probabilities. For N endpoints: first rule has probability 1/N, second 1/(N-1), etc. This achieves uniform random distribution.',
    codeSnippet: '# Example: 3 endpoints\n# Rule 1: probability 0.333 -> DNAT to endpoint 1\n# Rule 2: probability 0.500 -> DNAT to endpoint 2 (1/2 of remaining)\n# Rule 3: probability 1.000 -> DNAT to endpoint 3 (last one)\n-A KUBE-SVC-XXX -m statistic --mode random --probability 0.33333 -j KUBE-SEP-AAA\n-A KUBE-SVC-XXX -m statistic --mode random --probability 0.50000 -j KUBE-SEP-BBB\n-A KUBE-SVC-XXX -j KUBE-SEP-CCC',
    tags: ['kubernetes', 'kube-proxy', 'iptables', 'load-balancing', 'internals'],
  },
  {
    id: 'k8s-networking-l4-003',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is a subtle difference between having an empty "ingress: []" array and omitting the "ingress" field entirely in a NetworkPolicy with policyTypes: [Ingress]?',
    options: [
      'Empty array allows all traffic; omitting denies all traffic',
      'They are identical — both deny all ingress traffic',
      'Empty array denies all traffic; omitting means the policy does not affect ingress',
      'Empty array denies all; omitting allows all',
    ],
    correctAnswer: 1,
    explanation: 'When policyTypes includes Ingress, both an empty ingress array and an omitted ingress field result in denying all ingress traffic. The key is the policyTypes field, not the presence of the ingress field.',
    tags: ['kubernetes', 'network-policies', 'edge-cases', 'semantics'],
  },
  {
    id: 'k8s-networking-l4-004',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What is the conntrack race condition in Linux that affects DNS resolution in Kubernetes, and how does NodeLocal DNSCache mitigate it?',
    options: [
      'A race between CoreDNS replicas responding to the same query',
      'When two DNS packets (A and AAAA) for the same query race to create conntrack entries, one gets dropped; NodeLocal DNSCache uses TCP instead of UDP, avoiding the issue',
      'A race between kube-proxy and CoreDNS for DNS port allocation',
      'CoreDNS and kubelet racing to read /etc/resolv.conf',
    ],
    correctAnswer: 1,
    explanation: 'The Linux kernel conntrack subsystem can drop one of two simultaneous UDP packets (A and AAAA queries) competing for the same conntrack entry. NodeLocal DNSCache uses TCP for upstream queries to CoreDNS, avoiding the UDP conntrack race.',
    tags: ['kubernetes', 'dns', 'conntrack', 'race-condition', 'nodelocal-dnscache'],
  },
  {
    id: 'k8s-networking-l4-005',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'How does Cilium XDP (eXpress Data Path) acceleration work for Kubernetes Services?',
    options: [
      'It caches DNS lookups for faster resolution',
      'XDP programs process packets at the NIC driver level before they enter the kernel network stack, enabling wire-speed load balancing',
      'It pre-allocates network buffers for Pods',
      'It bypasses the kernel entirely using DPDK',
    ],
    correctAnswer: 1,
    explanation: 'Cilium can attach eBPF programs to the XDP hook, which runs at the earliest possible point in the network stack (NIC driver). This enables Service load balancing at near wire speed with minimal CPU overhead.',
    codeSnippet: '# Enable XDP acceleration in Cilium\nhelm upgrade cilium cilium/cilium \\\n  --set loadBalancer.acceleration=native \\\n  --set loadBalancer.mode=dsr \\\n  --set kubeProxyReplacement=true',
    tags: ['kubernetes', 'cni', 'cilium', 'xdp', 'performance'],
  },
  {
    id: 'k8s-networking-l4-006',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is Direct Server Return (DSR) in the context of Kubernetes Service load balancing, and which CNI supports it?',
    options: [
      'A mode where responses go directly from the server to the client without passing through the load balancer; supported by Cilium',
      'A mode where the DNS server returns results directly; supported by CoreDNS',
      'A mode where the kube-proxy returns traffic directly; supported by all CNIs',
      'A failover mode where traffic is rerouted; supported by Calico',
    ],
    correctAnswer: 0,
    explanation: 'DSR allows response packets to go directly from the backend Pod to the client, bypassing the load balancer node. This halves the load on the LB node. Cilium supports DSR mode with eBPF.',
    tags: ['kubernetes', 'services', 'dsr', 'cilium', 'performance'],
  },
  {
    id: 'k8s-networking-l4-007',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'Why can NetworkPolicies with ipBlock not effectively restrict traffic to ExternalName Services?',
    options: [
      'ExternalName Services do not use IP addresses that can be matched by ipBlock',
      'ExternalName Services bypass NetworkPolicies entirely',
      'ipBlock does not work with CNAME records because the resolved IP may change and is resolved by CoreDNS, not the Pod',
      'ExternalName Services are always in the kube-system namespace',
    ],
    correctAnswer: 2,
    explanation: 'ExternalName Services use CNAME records where the resolved IP can change dynamically. Since standard NetworkPolicies operate on IP addresses (not DNS), you cannot reliably restrict access using ipBlock when the destination IP changes.',
    tags: ['kubernetes', 'network-policies', 'externalname', 'limitations'],
  },
  {
    id: 'k8s-networking-l4-008',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'How does the NGINX Ingress Controller handle the "configuration snippets" vulnerability, and what is the security risk?',
    options: [
      'Configuration snippets are always safe',
      'The server-snippet and configuration-snippet annotations can inject arbitrary NGINX config, potentially exposing secrets or creating backdoors; they should be restricted via --enable-annotation-validation',
      'Configuration snippets only allow read-only NGINX directives',
      'They are disabled by default',
    ],
    correctAnswer: 1,
    explanation: 'Configuration snippet annotations allow injecting raw NGINX configuration. A malicious user could inject directives to read secrets, proxy to unauthorized backends, or bypass security. Use --enable-annotation-validation or restrict annotations.',
    codeSnippet: '# Example of dangerous snippet injection\nmetadata:\n  annotations:\n    nginx.ingress.kubernetes.io/server-snippet: |\n      # Could expose secrets or create backdoors\n      location /evil {\n        alias /etc/nginx/;\n      }\n\n# Mitigation: Disable snippet annotations\n# --enable-annotation-validation=true\n# Or restrict via --annotations-prefix',
    tags: ['kubernetes', 'ingress', 'nginx', 'security', 'cve'],
  },
  {
    id: 'k8s-networking-l4-009',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'How does Istio implement transparent traffic interception for the sidecar proxy?',
    options: [
      'By modifying the application to send traffic to Envoy',
      'Using iptables rules in an init container (istio-init) that redirect all traffic through the Envoy sidecar',
      'By replacing the CNI plugin with Istio networking',
      'By modifying kube-proxy rules',
    ],
    correctAnswer: 1,
    explanation: 'Istio uses an init container (istio-init) or the Istio CNI plugin to set up iptables rules in the Pod network namespace that redirect all inbound and outbound traffic through the Envoy sidecar proxy.',
    codeSnippet: '# iptables rules set by istio-init\n# Redirect all outbound traffic to Envoy (port 15001)\niptables -t nat -A OUTPUT -p tcp -j ISTIO_OUTPUT\niptables -t nat -A ISTIO_OUTPUT -j ISTIO_REDIRECT\niptables -t nat -A ISTIO_REDIRECT -p tcp -j REDIRECT --to-port 15001\n\n# Redirect all inbound traffic to Envoy (port 15006)\niptables -t nat -A PREROUTING -p tcp -j ISTIO_INBOUND\niptables -t nat -A ISTIO_INBOUND -p tcp -j ISTIO_IN_REDIRECT\niptables -t nat -A ISTIO_IN_REDIRECT -p tcp -j REDIRECT --to-port 15006',
    tags: ['kubernetes', 'service-mesh', 'istio', 'iptables', 'traffic-interception'],
  },
  {
    id: 'k8s-networking-l4-010',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the Calico eBPF dataplane and how does it differ from the standard dataplane?',
    options: [
      'It is identical but with a different name',
      'eBPF dataplane uses eBPF programs instead of iptables for routing, NAT, and policy enforcement, providing better performance and replacing kube-proxy',
      'eBPF dataplane only handles network policies, not routing',
      'eBPF dataplane requires a specific Linux distribution',
    ],
    correctAnswer: 1,
    explanation: 'Calico eBPF dataplane replaces iptables with eBPF programs for all data plane operations including routing, SNAT, Service load balancing, and policy enforcement. It also replaces kube-proxy, providing native source IP preservation.',
    tags: ['kubernetes', 'cni', 'calico', 'ebpf', 'dataplane'],
  },
  {
    id: 'k8s-networking-l4-011',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What happens during a SNAT (Source NAT) when a Pod accesses a Service that has an endpoint on the same node?',
    options: [
      'SNAT always occurs regardless of endpoint location',
      'With default externalTrafficPolicy, kube-proxy performs SNAT to the node IP to ensure the response returns through the same node. With eBPF, this can be avoided.',
      'No SNAT ever occurs for same-node traffic',
      'kube-proxy skips the Pod entirely',
    ],
    correctAnswer: 1,
    explanation: 'In iptables mode, kube-proxy performs MASQUERADE (SNAT) on Service traffic to ensure symmetric routing. This hides the original Pod source IP. eBPF-based implementations can avoid SNAT for same-node or even cross-node traffic.',
    tags: ['kubernetes', 'kube-proxy', 'snat', 'masquerade', 'internals'],
  },
  {
    id: 'k8s-networking-l4-012',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the order of evaluation when both standard NetworkPolicies and Calico GlobalNetworkPolicies apply to the same Pod?',
    options: [
      'Standard policies always take precedence',
      'Calico GlobalNetworkPolicies with order < 1000 are evaluated first, then standard NetworkPolicies, then GlobalNetworkPolicies with order >= 1000',
      'They are merged into a single policy',
      'The most recently created policy takes precedence',
    ],
    correctAnswer: 1,
    explanation: 'Calico evaluates policies by order: GlobalNetworkPolicies with lower order values first, then standard NetworkPolicies (treated as order 1000), then higher-order GlobalNetworkPolicies. This enables "guardrail" policies.',
    tags: ['kubernetes', 'network-policies', 'calico', 'policy-ordering'],
  },
  {
    id: 'k8s-networking-l4-013',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'How does CoreDNS handle the "thundering herd" problem when a popular DNS cache entry expires?',
    options: [
      'It locks the cache during renewal',
      'CoreDNS does not handle this natively; the "cache" plugin configuration with "prefetch" option proactively refreshes entries before TTL expiry',
      'It uses a distributed cache across replicas',
      'It returns stale records with a flag',
    ],
    correctAnswer: 1,
    explanation: 'The CoreDNS cache plugin supports a "prefetch" option that proactively refreshes frequently queried entries before TTL expiry, preventing a flood of upstream queries when cache entries expire simultaneously.',
    codeSnippet: '# Corefile with prefetch enabled\n.:53 {\n    cache 30 {\n        prefetch 10 60s 10%\n        # prefetch AMOUNT DURATION PERCENTAGE\n        # Prefetch if >10 queries in 60s and >10% of TTL remaining\n    }\n    forward . /etc/resolv.conf\n}',
    tags: ['kubernetes', 'dns', 'coredns', 'cache', 'prefetch'],
  },
  {
    id: 'k8s-networking-l4-014',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'What is the difference between the "Prefix" and "ImplementationSpecific" pathType when the NGINX Ingress Controller processes a path like "/api"?',
    options: [
      'No practical difference',
      'Prefix matches "/api", "/api/", and "/api/anything". ImplementationSpecific treats the path as an NGINX location block which may use regex or exact matching depending on annotations.',
      'Prefix is case-sensitive, ImplementationSpecific is case-insensitive',
      'Prefix uses regex, ImplementationSpecific does not',
    ],
    correctAnswer: 1,
    explanation: 'With Prefix, the Ingress controller normalizes to prefix matching. With ImplementationSpecific, NGINX uses its native location matching which can include regex (when use-regex annotation is set), allowing more complex patterns.',
    tags: ['kubernetes', 'ingress', 'pathtype', 'nginx', 'edge-cases'],
  },
  {
    id: 'k8s-networking-l4-015',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the BPF map in Cilium and how is it used for Service load balancing?',
    options: [
      'A map of all nodes in the cluster',
      'An in-kernel hash table that maps Service VIP:port to backend Pod IPs, enabling O(1) lookup for packet forwarding',
      'A topology map of Pod placement',
      'A mapping between CNI plugins and namespaces',
    ],
    correctAnswer: 1,
    explanation: 'Cilium uses BPF maps (in-kernel hash tables) to store Service-to-backend mappings. eBPF programs look up the destination in these maps with O(1) complexity, performing DNAT to the selected backend without traversing iptables chains.',
    tags: ['kubernetes', 'cni', 'cilium', 'bpf-maps', 'internals'],
  },
  {
    id: 'k8s-networking-l4-016',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the "hairpin" or "loopback" problem in Kubernetes networking and how is it solved?',
    options: [
      'A problem with DNS circular references',
      'When a Pod sends traffic to a Service that routes back to itself; solved by kube-proxy masquerading the source IP so the return path goes through the Service',
      'When two Services reference each other creating a loop',
      'When a Pod network namespace conflicts with the host',
    ],
    correctAnswer: 1,
    explanation: 'When a Pod accesses a Service that selects itself as a backend, the packet needs to return to the same Pod. Without SNAT, the Pod would see its own IP as source and drop the response. MASQUERADE solves this by using the node IP as source.',
    tags: ['kubernetes', 'services', 'hairpin', 'masquerade', 'internals'],
  },
  {
    id: 'k8s-networking-l4-017',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'How does Istio Ambient Mesh ztunnel differ from traditional sidecar architecture in terms of resource overhead?',
    options: [
      'ztunnel uses more resources per Pod',
      'ztunnel runs one proxy per node (DaemonSet) handling L4 for all Pods, eliminating per-Pod sidecar overhead; L7 processing uses shared waypoint proxies only where needed',
      'ztunnel eliminates all proxy overhead',
      'ztunnel uses the same resources as sidecars but in a different location',
    ],
    correctAnswer: 1,
    explanation: 'ztunnel runs as a per-node proxy handling L4 (mTLS, TCP routing) for all Pods on that node. L7 features are handled by shared waypoint proxies, deployed only for services that need them. This dramatically reduces total proxy resource consumption.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'ambient-mesh', 'ztunnel'],
  },
  {
    id: 'k8s-networking-l4-018',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the impact of having a very large number of NetworkPolicy rules on the datapath in iptables-based CNI implementations?',
    options: [
      'No impact as rules are compiled to bytecode',
      'Each packet must traverse all matching iptables rules linearly, increasing latency and CPU usage proportionally to the number of rules',
      'Rules are cached after first evaluation so there is no ongoing impact',
      'The kernel optimizes rules automatically',
    ],
    correctAnswer: 1,
    explanation: 'In iptables mode, the kernel evaluates rules linearly per packet. With thousands of rules (from many NetworkPolicies), this creates measurable latency and CPU overhead. eBPF-based implementations (Cilium) compile policies into more efficient programs.',
    tags: ['kubernetes', 'network-policies', 'iptables', 'performance', 'internals'],
  },
  {
    id: 'k8s-networking-l4-019',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'How does the CoreDNS "kubernetes" plugin interact with the Kubernetes API server for service discovery?',
    options: [
      'It polls the API server every 30 seconds',
      'It uses a watch/list pattern with informers to maintain an in-memory cache of Services, Endpoints, and Pods, responding to DNS queries from cache',
      'It queries etcd directly for DNS records',
      'It reads from a shared volume mounted from the API server',
    ],
    correctAnswer: 1,
    explanation: 'The CoreDNS kubernetes plugin uses the Kubernetes client-go informer mechanism to watch for changes to Services, EndpointSlices, and Pods. It maintains an in-memory cache and serves DNS queries from it without additional API calls.',
    tags: ['kubernetes', 'dns', 'coredns', 'informers', 'internals'],
  },
  {
    id: 'k8s-networking-l4-020',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the difference between Cilium host-routing and tunneling mode, and when would you use each?',
    options: [
      'They provide identical functionality',
      'Host-routing uses the kernel routing table for native performance but requires compatible infrastructure; tunneling (VXLAN/Geneve) works anywhere but adds encapsulation overhead',
      'Host-routing is for IPv4, tunneling is for IPv6',
      'Host-routing requires IPVS, tunneling requires iptables',
    ],
    correctAnswer: 1,
    explanation: 'Native host-routing leverages the existing network infrastructure for direct routing, offering the best performance. Tunnel mode (VXLAN/Geneve) encapsulates packets, adding ~50 bytes overhead but working in any network environment.',
    codeSnippet: '# Cilium with native routing\nhelm install cilium cilium/cilium \\\n  --set routingMode=native \\\n  --set ipv4NativeRoutingCIDR=10.0.0.0/8\n\n# Cilium with tunnel mode\nhelm install cilium cilium/cilium \\\n  --set tunnelProtocol=vxlan  # or geneve',
    tags: ['kubernetes', 'cni', 'cilium', 'routing', 'tunneling'],
  },
  {
    id: 'k8s-networking-l4-021',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'How does the IPVS mode handle Service connection affinity differently from iptables mode?',
    options: [
      'They handle it identically',
      'IPVS uses native connection table persistence for session affinity, while iptables uses the "recent" module with less reliability',
      'IPVS does not support session affinity',
      'iptables is more reliable for session affinity',
    ],
    correctAnswer: 1,
    explanation: 'IPVS has native persistence support in its connection table, making session affinity more reliable and configurable. iptables uses the "recent" module which can be less reliable under high concurrency.',
    tags: ['kubernetes', 'kube-proxy', 'ipvs', 'session-affinity', 'internals'],
  },
  {
    id: 'k8s-networking-l4-022',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'What is the Gateway API "reference grant" mechanism and what security problem does it solve?',
    options: [
      'It grants admin access to manage Gateways',
      'It allows resources in one namespace to reference resources in another namespace with explicit permission, preventing unauthorized cross-namespace route hijacking',
      'It grants TLS certificates across namespaces',
      'It provides RBAC for Ingress resources',
    ],
    correctAnswer: 1,
    explanation: 'ReferenceGrant explicitly authorizes cross-namespace references in the Gateway API. Without it, a HTTPRoute in namespace A cannot reference a Service in namespace B, preventing route hijacking attacks that were possible with Ingress.',
    codeSnippet: '# Allow HTTPRoutes in "frontend" namespace to reference\n# Services in "backend" namespace\napiVersion: gateway.networking.k8s.io/v1beta1\nkind: ReferenceGrant\nmetadata:\n  name: allow-frontend-routes\n  namespace: backend\nspec:\n  from:\n    - group: gateway.networking.k8s.io\n      kind: HTTPRoute\n      namespace: frontend\n  to:\n    - group: ""\n      kind: Service',
    tags: ['kubernetes', 'gateway-api', 'reference-grant', 'security'],
  },
  {
    id: 'k8s-networking-l4-023',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the SCTP protocol support limitation in NetworkPolicies?',
    options: [
      'SCTP is fully supported in all CNI plugins',
      'SCTP protocol in NetworkPolicy port rules requires CNI plugin support and is not universally implemented; some CNIs silently ignore SCTP rules',
      'SCTP is not supported at all in NetworkPolicies',
      'SCTP only works with headless Services',
    ],
    correctAnswer: 1,
    explanation: 'While the NetworkPolicy API supports specifying SCTP protocol, not all CNI plugins implement SCTP filtering. Some may silently ignore SCTP rules, creating a false sense of security.',
    tags: ['kubernetes', 'network-policies', 'sctp', 'limitations'],
  },
  {
    id: 'k8s-networking-l4-024',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is SPIFFE in the context of service mesh identity and how does Istio use it?',
    options: [
      'A DNS protocol for service naming',
      'SPIFFE (Secure Production Identity Framework For Everyone) provides cryptographic identities; Istio uses SPIFFE IDs as x509 certificate URIs for mTLS identity',
      'A monitoring standard for service mesh metrics',
      'A service mesh data plane protocol',
    ],
    correctAnswer: 1,
    explanation: 'SPIFFE defines a standard for service identity. Istio assigns each service a SPIFFE ID (spiffe://cluster.local/ns/<namespace>/sa/<service-account>) embedded in X.509 certificates used for mTLS.',
    codeSnippet: '# Istio SPIFFE ID format:\n# spiffe://<trust-domain>/ns/<namespace>/sa/<service-account>\n#\n# Example certificate SAN:\n# URI: spiffe://cluster.local/ns/production/sa/api-service',
    tags: ['kubernetes', 'service-mesh', 'istio', 'spiffe', 'identity'],
  },
  {
    id: 'k8s-networking-l4-025',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'How does Cilium implement transparent encryption using WireGuard or IPsec?',
    options: [
      'It encrypts at the application level',
      'Cilium uses node-level encryption tunnels between nodes; each node pair establishes a WireGuard/IPsec tunnel and all Pod traffic between those nodes is transparently encrypted',
      'It encrypts individual Pod-to-Pod connections',
      'It uses TLS for all container traffic',
    ],
    correctAnswer: 1,
    explanation: 'Cilium establishes node-to-node encryption tunnels (WireGuard or IPsec). All Pod traffic traversing between nodes is automatically encrypted without application changes. WireGuard is preferred for its performance.',
    codeSnippet: '# Enable WireGuard encryption in Cilium\nhelm install cilium cilium/cilium \\\n  --set encryption.enabled=true \\\n  --set encryption.type=wireguard\n\n# Verify encryption is active\ncilium status | grep Encryption\n# Encryption: Wireguard [cilium_wg0 (Pubkey: ..., Port: 51871)]',
    tags: ['kubernetes', 'cni', 'cilium', 'wireguard', 'encryption'],
  },
  {
    id: 'k8s-networking-l4-026',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the "stale endpoint" problem during rapid Pod churn and how does it manifest?',
    options: [
      'Old DNS records persist forever',
      'kube-proxy iptables rules may temporarily point to terminated Pod IPs during rapid scaling, causing connection resets until rules are updated',
      'Endpoints in etcd become corrupted',
      'Services stop accepting new connections',
    ],
    correctAnswer: 1,
    explanation: 'During rapid Pod churn, there is a window between Pod termination and kube-proxy updating iptables rules. During this window, traffic may be routed to terminated Pods, causing TCP RST or connection timeouts.',
    tags: ['kubernetes', 'services', 'stale-endpoints', 'pod-churn', 'race-condition'],
  },
  {
    id: 'k8s-networking-l4-027',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What is the impact of NXDOMAIN response caching in CoreDNS and how can it cause issues during Service creation?',
    options: [
      'NXDOMAIN responses are never cached',
      'CoreDNS caches NXDOMAIN responses for the configured TTL; if a Pod queries a Service name before the Service is created, the negative cache may cause resolution failures until TTL expires',
      'NXDOMAIN caching only affects external domains',
      'CoreDNS retries immediately on NXDOMAIN',
    ],
    correctAnswer: 1,
    explanation: 'CoreDNS caches NXDOMAIN (negative) responses. If an application queries a Service name before the Service exists, the cached negative response may cause continued failures even after the Service is created, until the cache TTL expires.',
    codeSnippet: '# CoreDNS cache configuration with denial TTL\ncache 30 {\n    denial 5  # Cache negative responses for only 5 seconds\n    success 30  # Cache positive responses for 30 seconds\n}',
    tags: ['kubernetes', 'dns', 'coredns', 'nxdomain', 'negative-cache'],
  },
  {
    id: 'k8s-networking-l4-028',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'How do Cilium CiliumClusterwideNetworkPolicies differ from Kubernetes AdminNetworkPolicies?',
    options: [
      'They are the same resource with different names',
      'CiliumClusterwideNetworkPolicies support L7 filtering, DNS-based rules, and CIDR-based identity matching; AdminNetworkPolicies are standard Kubernetes with L3/L4 only but wider CNI support',
      'AdminNetworkPolicies are more feature-rich',
      'CiliumClusterwideNetworkPolicies only work in tunnel mode',
    ],
    correctAnswer: 1,
    explanation: 'CiliumClusterwideNetworkPolicies are Cilium-specific and support L7 filtering, DNS-based policies, and identity-aware matching. AdminNetworkPolicies are a standard K8s API (alpha) with broader CNI support but limited to L3/L4.',
    tags: ['kubernetes', 'network-policies', 'cilium', 'admin-network-policy', 'comparison'],
  },
  {
    id: 'k8s-networking-l4-029',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'What are the security implications of the Ingress "pathType: Prefix" with path "/" in a multi-tenant cluster?',
    options: [
      'No security implications',
      'A malicious tenant could create an Ingress with path "/" that catches all traffic for a shared domain, intercepting other tenants requests if not properly isolated by IngressClass or namespace restrictions',
      'Path "/" is automatically restricted to cluster admins',
      'Only one Ingress per namespace can use path "/"',
    ],
    correctAnswer: 1,
    explanation: 'Without proper isolation (separate IngressClasses, namespace restrictions, or Gateway API ReferenceGrants), a tenant could create a catch-all Ingress route, intercepting traffic meant for other services on the same host.',
    tags: ['kubernetes', 'ingress', 'security', 'multi-tenancy'],
  },
  {
    id: 'k8s-networking-l4-030',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the impact of MTU misconfiguration on Kubernetes networking with overlay CNI plugins?',
    options: [
      'No impact, MTU is auto-detected',
      'Packets exceeding the tunnel MTU are fragmented or dropped (if DF bit is set), causing mysterious connectivity issues for large payloads while small packets work fine',
      'MTU misconfiguration only affects external traffic',
      'The CNI plugin automatically adjusts packet sizes',
    ],
    correctAnswer: 1,
    explanation: 'Overlay networks add encapsulation headers (e.g., 50 bytes for VXLAN). If the Pod MTU is not reduced accordingly, large packets (close to 1500 bytes) will exceed the tunnel MTU, causing fragmentation or drops. This creates hard-to-debug issues where small requests work but large payloads fail.',
    codeSnippet: '# Typical MTU values:\n# Physical network: 1500\n# VXLAN overlay:    1450 (1500 - 50 byte VXLAN header)\n# WireGuard:        1420 (1500 - 80 byte WG header)\n# IPIP:             1480 (1500 - 20 byte IP header)\n\n# Check Pod MTU\nkubectl exec <pod> -- ip link show eth0 | grep mtu\n\n# Calico MTU configuration\napiVersion: crd.projectcalico.org/v1\nkind: FelixConfiguration\nmetadata:\n  name: default\nspec:\n  mtu: 1450',
    tags: ['kubernetes', 'cni', 'mtu', 'troubleshooting', 'overlay'],
  },
  {
    id: 'k8s-networking-l4-031',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'How does the Kubernetes API server allocate ClusterIPs and what happens when the range is exhausted?',
    options: [
      'IPs are allocated randomly and the range auto-expands',
      'The API server uses a bitmap allocator backed by etcd; when exhausted, new Service creation fails with an allocation error. The range is split into static (lower) and dynamic (upper) sub-ranges.',
      'kube-proxy allocates IPs on demand',
      'ClusterIPs are allocated by CoreDNS',
    ],
    correctAnswer: 1,
    explanation: 'The API server maintains a bitmap in etcd tracking allocated ClusterIPs. Since K8s 1.24, the range is split: lower portion for static allocation, upper for dynamic. Exhaustion causes "failed to allocate a serviceIP" errors.',
    tags: ['kubernetes', 'services', 'clusterip', 'allocation', 'internals'],
  },
  {
    id: 'k8s-networking-l4-032',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the Envoy HTTP Connection Manager (HCM) and why is its configuration critical for Istio performance?',
    options: [
      'HCM manages TLS connections between nodes',
      'HCM is the core L7 filter in Envoy that handles HTTP/1.1, HTTP/2, and HTTP/3 processing; its settings for connection pooling, circuit breaking, and buffer sizes directly impact latency and resource usage',
      'HCM is the component that manages etcd connections',
      'HCM handles DNS resolution for Envoy',
    ],
    correctAnswer: 1,
    explanation: 'The Envoy HCM handles all HTTP protocol processing. Tuning its settings (idle timeout, max concurrent streams, buffer limits, header size limits) is critical for performance. Misconfiguration can cause 503 errors, memory issues, or high latency.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'envoy', 'hcm', 'performance'],
  },
  {
    id: 'k8s-networking-l4-033',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'How do you implement egress traffic control to specific FQDNs using Cilium?',
    options: [
      'Use standard NetworkPolicy with ipBlock',
      'Use CiliumNetworkPolicy with toFQDNs that leverages DNS-aware policy enforcement via intercepted DNS responses',
      'Use ExternalName Services with NetworkPolicies',
      'Use Istio ServiceEntry with NetworkPolicies',
    ],
    correctAnswer: 1,
    explanation: 'Cilium CiliumNetworkPolicy supports toFQDNs rules. Cilium intercepts DNS responses to learn IP-to-FQDN mappings and dynamically updates eBPF maps to enforce policies based on domain names.',
    codeSnippet: 'apiVersion: cilium.io/v2\nkind: CiliumNetworkPolicy\nmetadata:\n  name: allow-specific-external\nspec:\n  endpointSelector:\n    matchLabels:\n      app: api\n  egress:\n    - toFQDNs:\n        - matchName: "api.github.com"\n        - matchPattern: "*.amazonaws.com"\n      toPorts:\n        - ports:\n            - port: "443"\n              protocol: TCP\n    - toEndpoints:\n        - matchLabels:\n            k8s:io.kubernetes.pod.namespace: kube-system\n            k8s-app: kube-dns\n      toPorts:\n        - ports:\n            - port: "53"\n              protocol: UDP',
    tags: ['kubernetes', 'network-policies', 'cilium', 'fqdn', 'dns-aware'],
  },
  {
    id: 'k8s-networking-l4-034',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What is the "pods verified" mode in the CoreDNS kubernetes plugin and how does it differ from "pods insecure"?',
    options: [
      'Both modes are identical',
      '"pods verified" validates that a Pod A record query matches an existing Pod IP in the same namespace, preventing IP spoofing; "pods insecure" returns A records without verification',
      '"pods verified" uses DNSSEC, "pods insecure" does not',
      '"pods verified" checks Pod readiness, "pods insecure" does not',
    ],
    correctAnswer: 1,
    explanation: '"pods insecure" returns A records for any Pod IP format query without validation. "pods verified" verifies the queried IP actually belongs to an existing Pod in the same namespace, preventing DNS-based IP spoofing attacks.',
    tags: ['kubernetes', 'dns', 'coredns', 'pod-verification', 'security'],
  },
  {
    id: 'k8s-networking-l4-035',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'How does the NGINX Ingress Controller handle multiple Ingress resources with conflicting rules for the same host/path?',
    options: [
      'It merges all rules and serves them all',
      'The controller uses creation timestamp for priority — earlier Ingress wins. For same-timestamp, lexicographic order of namespace/name decides. Conflicting rules are logged as warnings.',
      'It rejects the conflicting Ingress resource',
      'The last applied Ingress always wins',
    ],
    correctAnswer: 1,
    explanation: 'NGINX Ingress resolves conflicts using creation timestamp (oldest wins) and then lexicographic namespace/name ordering. Conflicting configurations generate warnings in the controller logs.',
    tags: ['kubernetes', 'ingress', 'nginx', 'conflict-resolution'],
  },
  {
    id: 'k8s-networking-l4-036',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the Geneve tunnel protocol and why is it preferred over VXLAN in some CNI implementations?',
    options: [
      'Geneve is faster than VXLAN',
      'Geneve supports variable-length metadata/options in the tunnel header, enabling extensible features like carrying security identities; VXLAN has a fixed header format',
      'Geneve is a non-standard protocol',
      'Geneve requires less overhead than VXLAN',
    ],
    correctAnswer: 1,
    explanation: 'Geneve (Generic Network Virtualization Encapsulation) supports TLV (type-length-value) options in the header, allowing extensible metadata like security identities. VXLAN has a fixed 8-byte header with limited extensibility. Cilium uses Geneve for carrying identity information.',
    tags: ['kubernetes', 'cni', 'geneve', 'vxlan', 'encapsulation'],
  },
  {
    id: 'k8s-networking-l4-037',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the "terminating endpoint" behavior in kube-proxy and how does it affect zero-downtime deployments?',
    options: [
      'Terminating endpoints are immediately removed',
      'In newer Kubernetes versions, kube-proxy can keep routing to "serving+terminating" endpoints when no ready endpoints exist, preventing total service disruption during rolling updates',
      'Terminating endpoints receive double the traffic',
      'kube-proxy ignores endpoint state changes',
    ],
    correctAnswer: 1,
    explanation: 'Since Kubernetes 1.26 (GA), kube-proxy uses the "serving" and "terminating" conditions from EndpointSlices. If all endpoints are terminating, traffic is still routed to serving+terminating Pods rather than returning errors.',
    tags: ['kubernetes', 'kube-proxy', 'terminating-endpoints', 'zero-downtime'],
  },
  {
    id: 'k8s-networking-l4-038',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the Envoy "original destination" listener filter and how does Istio use it?',
    options: [
      'It logs the original destination for debugging',
      'It recovers the original destination IP/port from redirected connections (via iptables REDIRECT), allowing Envoy to connect to the intended backend',
      'It sets the original destination in HTTP headers',
      'It caches the original destination for retries',
    ],
    correctAnswer: 1,
    explanation: 'When iptables REDIRECT sends all traffic to Envoy, the original destination is stored in the socket metadata (SO_ORIGINAL_DST). The original_dst listener filter reads this to determine where the connection was intended to go.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'envoy', 'original-dst'],
  },
  {
    id: 'k8s-networking-l4-039',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'endpoint-slices',
    question: 'What is the maximum number of EndpointSlices per Service and what happens when a Service has thousands of endpoints?',
    options: [
      'Limited to 10 EndpointSlices per Service',
      'No hard limit; the controller creates as many EndpointSlices as needed (each holding ~100 endpoints), but very large numbers can impact API server performance and controller reconciliation time',
      'Limited to 100 EndpointSlices per Service',
      'There is a hard limit of 1000 endpoints per Service',
    ],
    correctAnswer: 1,
    explanation: 'There is no hard limit on EndpointSlices per Service. For a Service with 5000 endpoints, approximately 50 EndpointSlices are created. Very large Services can strain the API server and increase kube-proxy rule update time.',
    tags: ['kubernetes', 'endpoint-slices', 'scalability', 'limits'],
  },
  {
    id: 'k8s-networking-l4-040',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the "baseline" AdminNetworkPolicy and how does it differ from a regular AdminNetworkPolicy?',
    options: [
      'Baseline ANP has higher priority than regular ANP',
      'Baseline ANP provides default rules that can be overridden by namespace-scoped NetworkPolicies, while regular ANP takes precedence over namespace policies',
      'Baseline ANP only applies to the default namespace',
      'They are identical in behavior',
    ],
    correctAnswer: 1,
    explanation: 'BaselineAdminNetworkPolicy provides cluster-wide defaults that namespace-scoped NetworkPolicies CAN override. Regular AdminNetworkPolicy rules take precedence over namespace policies and CANNOT be overridden.',
    tags: ['kubernetes', 'network-policies', 'admin-network-policy', 'baseline'],
  },
  {
    id: 'k8s-networking-l4-041',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'How do you implement split-horizon DNS in Kubernetes where internal and external clients resolve different IPs for the same domain?',
    options: [
      'Use two CoreDNS deployments with different configurations',
      'Configure CoreDNS with the "view" plugin or use separate DNS zones for internal vs external resolution, combined with different resolution paths',
      'This is not possible in Kubernetes',
      'Use two Services with the same name in different namespaces',
    ],
    correctAnswer: 1,
    explanation: 'Split-horizon DNS can be implemented by: configuring CoreDNS to return internal IPs for cluster clients, while external-dns manages public DNS records. Alternatively, use CoreDNS server blocks with different upstream servers based on source IP.',
    tags: ['kubernetes', 'dns', 'split-horizon', 'advanced'],
  },
  {
    id: 'k8s-networking-l4-042',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the Calico "felix" component and what role does it play in the networking dataplane?',
    options: [
      'Felix is the Calico API server',
      'Felix is the per-node agent that programs routes, ACLs, and dataplane rules (iptables/eBPF/nftables) based on policy and endpoint information from the Calico datastore',
      'Felix is the BGP route reflector',
      'Felix is the IPAM controller',
    ],
    correctAnswer: 1,
    explanation: 'Felix runs on each node and is responsible for programming the Linux kernel dataplane. It reads policy and endpoint data, then writes iptables rules (or eBPF programs), routes, and kernel interface configuration.',
    tags: ['kubernetes', 'cni', 'calico', 'felix', 'architecture'],
  },
  {
    id: 'k8s-networking-l4-043',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'loadbalancer',
    question: 'What is MetalLB speaker and how does it operate in Layer 2 mode vs BGP mode?',
    options: [
      'Speaker handles DNS for load-balanced Services',
      'In L2 mode, one speaker answers ARP/NDP for the Service IP (single-node failover); in BGP mode, all speakers advertise the IP via BGP (true load balancing across nodes)',
      'Speaker creates iptables rules for load balancing',
      'Speaker is only used in BGP mode',
    ],
    correctAnswer: 1,
    explanation: 'In L2 mode, one MetalLB speaker wins leader election and responds to ARP requests for the Service VIP, directing all traffic to that node (failover, not load balancing). In BGP mode, multiple speakers advertise the VIP via BGP, achieving true ECMP load balancing.',
    tags: ['kubernetes', 'loadbalancer', 'metallb', 'l2', 'bgp'],
  },
  {
    id: 'k8s-networking-l4-044',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the Envoy "cluster" concept and how does Istio map Kubernetes Services to Envoy clusters?',
    options: [
      'Envoy cluster maps 1:1 with Kubernetes cluster',
      'An Envoy cluster is a group of upstream hosts; Istio generates an Envoy cluster for each Service:port combination, with endpoints from EndpointSlices as the cluster members',
      'Envoy clusters represent node groups',
      'Envoy clusters are the same as Kubernetes namespaces',
    ],
    correctAnswer: 1,
    explanation: 'In Envoy, a "cluster" represents a set of upstream endpoints. Istio pilot (istiod) translates each Kubernetes Service:port into an Envoy cluster, populating it with endpoint addresses from EndpointSlices via EDS (Endpoint Discovery Service).',
    tags: ['kubernetes', 'service-mesh', 'istio', 'envoy', 'clusters'],
  },
  {
    id: 'k8s-networking-l4-045',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the impact of large iptables rule sets on node CPU usage and how can you mitigate it?',
    options: [
      'iptables rules have no CPU impact',
      'Each Service adds ~8 iptables rules and each endpoint adds ~4; with thousands of Services, rule updates can lock the iptables mutex for seconds, causing CPU spikes. Mitigate by switching to IPVS or eBPF.',
      'CPU impact is only during initial rule creation',
      'iptables rules are processed by dedicated hardware',
    ],
    correctAnswer: 1,
    explanation: 'iptables rule updates are not atomic and require holding a global lock. With thousands of Services/endpoints, a single update can involve rewriting tens of thousands of rules, causing seconds-long CPU spikes. IPVS and eBPF handle updates more efficiently.',
    tags: ['kubernetes', 'kube-proxy', 'iptables', 'cpu', 'scalability'],
  },
  {
    id: 'k8s-networking-l4-046',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'Why can a Pod with a default-deny NetworkPolicy still communicate with the Kubernetes API server?',
    options: [
      'The API server is exempt from all NetworkPolicies',
      'The API server IP is in the host network; depending on the CNI implementation, host-to-Pod and Pod-to-host traffic may bypass NetworkPolicy enforcement',
      'kube-proxy creates exception rules for the API server',
      'The API server uses a special RBAC bypass',
    ],
    correctAnswer: 1,
    explanation: 'Many CNI implementations do not enforce NetworkPolicies on traffic to/from the host network (where the API server runs). This means Pods can often reach the API server even with deny-all policies. Calico and Cilium have options to control host traffic.',
    tags: ['kubernetes', 'network-policies', 'api-server', 'host-network', 'edge-cases'],
  },
  {
    id: 'k8s-networking-l4-047',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'What is the Kubernetes Gateway API "BackendTLSPolicy" resource used for?',
    options: [
      'Configuring TLS on the Gateway frontend',
      'Configuring TLS settings for connections between the Gateway and backend services, including specifying the CA for backend certificate validation',
      'Creating TLS certificates for backends',
      'Monitoring TLS connections to backends',
    ],
    correctAnswer: 1,
    explanation: 'BackendTLSPolicy configures how the Gateway validates backend TLS certificates when connecting to HTTPS backends. It specifies the CA certificate and hostname for verification, enabling proper end-to-end TLS.',
    tags: ['kubernetes', 'gateway-api', 'tls', 'backend-tls-policy'],
  },
  {
    id: 'k8s-networking-l4-048',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What is the "lameduck" plugin in CoreDNS and why is it important for zero-downtime CoreDNS upgrades?',
    options: [
      'It detects unhealthy DNS servers',
      'It delays CoreDNS shutdown by a configured duration, allowing in-flight DNS requests to complete and clients to failover to other CoreDNS replicas',
      'It marks CoreDNS as not ready before shutdown',
      'It caches responses during shutdown',
    ],
    correctAnswer: 1,
    explanation: 'The lameduck plugin adds a delay before CoreDNS shuts down. During this period, the health endpoint returns unhealthy (triggering load balancer failover) while still serving existing queries, preventing DNS resolution failures during upgrades.',
    codeSnippet: '.:53 {\n    lameduck 5s  # Delay shutdown by 5 seconds\n    health\n    ready\n    kubernetes cluster.local\n    forward . /etc/resolv.conf\n    cache 30\n}',
    tags: ['kubernetes', 'dns', 'coredns', 'lameduck', 'zero-downtime'],
  },
  {
    id: 'k8s-networking-l4-049',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'How does the Cilium "per-endpoint routing" feature optimize Pod networking compared to traditional per-node routing?',
    options: [
      'It creates more routes for redundancy',
      'Instead of routing all Pod traffic through a single veth pair, Cilium can use per-endpoint routing tables to avoid unnecessary hops through the host network stack',
      'It routes traffic directly between containers without the kernel',
      'It creates individual VPN tunnels for each endpoint',
    ],
    correctAnswer: 1,
    explanation: 'Per-endpoint routing in Cilium sets up independent routing tables for each Pod endpoint, allowing the eBPF datapath to make forwarding decisions without traversing the full host network stack, reducing latency for Pod-to-Pod communication.',
    tags: ['kubernetes', 'cni', 'cilium', 'per-endpoint-routing', 'optimization'],
  },
  {
    id: 'k8s-networking-l4-050',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the Envoy Wasm (WebAssembly) extension mechanism in Istio and when would you use it?',
    options: [
      'A way to run Envoy in a WebAssembly sandbox',
      'Wasm allows deploying custom filters to Envoy sidecars without recompiling; used for custom authentication, header manipulation, or protocol handling beyond Envoy built-in capabilities',
      'A mechanism for running application code inside Envoy',
      'A way to compile Istio to WebAssembly for edge deployment',
    ],
    correctAnswer: 1,
    explanation: 'Envoy Wasm extensions allow loading custom filter logic (compiled to Wasm) into the proxy at runtime. This enables custom request/response transformation, authentication schemes, or telemetry without modifying or rebuilding Envoy.',
    codeSnippet: '# Istio WasmPlugin resource\napiVersion: extensions.istio.io/v1alpha1\nkind: WasmPlugin\nmetadata:\n  name: custom-auth\nspec:\n  selector:\n    matchLabels:\n      app: my-service\n  url: oci://registry.example.com/custom-auth:v1\n  phase: AUTHN\n  pluginConfig:\n    auth_endpoint: "https://auth.example.com"',
    tags: ['kubernetes', 'service-mesh', 'istio', 'envoy', 'wasm'],
  },
  {
    id: 'k8s-networking-l4-051',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'How does the Kubernetes Service controller handle the "ClusterIP None" to "ClusterIP allocated" migration and vice versa?',
    options: [
      'You can freely change between None and a specific ClusterIP',
      'The clusterIP field is immutable after creation; you cannot change a headless Service to a ClusterIP Service or vice versa without deleting and recreating the Service',
      'You can change it with a kubectl patch command',
      'Only cluster admins can change the clusterIP field',
    ],
    correctAnswer: 1,
    explanation: 'The clusterIP field is immutable. To switch between headless (None) and regular ClusterIP, you must delete and recreate the Service. This is a deliberate design choice to prevent accidental changes that could disrupt traffic.',
    tags: ['kubernetes', 'services', 'clusterip', 'immutable', 'migration'],
  },
  {
    id: 'k8s-networking-l4-052',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the "Kubernetes Network Policy Logging" feature in Calico and how does it help with security auditing?',
    options: [
      'It logs all DNS queries',
      'It logs allowed and denied connections per NetworkPolicy rule with metadata (source, destination, action, policy name), enabling security audit trails',
      'It logs NetworkPolicy creation and deletion events',
      'It logs kube-proxy rule changes',
    ],
    correctAnswer: 1,
    explanation: 'Calico can log every connection that is allowed or denied by a NetworkPolicy, including source/dest IPs, ports, and which policy/rule was matched. This is essential for security auditing and troubleshooting.',
    codeSnippet: '# Calico NetworkPolicy with logging\napiVersion: crd.projectcalico.org/v1\nkind: NetworkPolicy\nmetadata:\n  name: log-denied\n  namespace: production\nspec:\n  selector: app == \'sensitive\'\n  types: [Ingress]\n  ingress:\n    - action: Log\n      metadata:\n        annotations:\n          description: "Logged denied connection"\n    - action: Deny',
    tags: ['kubernetes', 'network-policies', 'calico', 'logging', 'audit'],
  },
  {
    id: 'k8s-networking-l4-053',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'How does CoreDNS handle zone transfers (AXFR) and when might this be needed?',
    options: [
      'CoreDNS always supports zone transfers',
      'CoreDNS supports zone transfers via the "transfer" plugin for secondary DNS servers to replicate zone data; useful for hybrid environments with traditional DNS infrastructure',
      'Zone transfers are not possible with CoreDNS',
      'Zone transfers happen automatically to CoreDNS replicas',
    ],
    correctAnswer: 1,
    explanation: 'The CoreDNS transfer plugin enables AXFR/IXFR zone transfers. This is useful when external DNS servers need to replicate cluster DNS data, such as in hybrid environments with traditional DNS infrastructure.',
    tags: ['kubernetes', 'dns', 'coredns', 'zone-transfer', 'hybrid'],
  },
  {
    id: 'k8s-networking-l4-054',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the Big TCP feature in Cilium and how does it improve performance?',
    options: [
      'It increases the TCP window size',
      'Big TCP enables GRO/GSO to create packets larger than 64KB (up to 192KB) for node-to-node Pod traffic, significantly reducing per-packet overhead for high-throughput workloads',
      'It enables TCP BBR congestion control',
      'It creates larger TCP connection pools',
    ],
    correctAnswer: 1,
    explanation: 'Big TCP allows the kernel to handle super-sized TCP segments (up to 192KB vs standard 64KB GSO limit) between nodes. This reduces per-packet CPU overhead and dramatically improves throughput for bulk data transfers.',
    codeSnippet: '# Enable Big TCP in Cilium\nhelm install cilium cilium/cilium \\\n  --set enableIPv4BigTCP=true \\\n  --set enableIPv6BigTCP=true \\\n  --set routingMode=native',
    tags: ['kubernetes', 'cni', 'cilium', 'big-tcp', 'performance'],
  },
  {
    id: 'k8s-networking-l4-055',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'loadbalancer',
    question: 'What is the interaction between externalTrafficPolicy: Local and topology-aware routing?',
    options: [
      'They cannot be used together',
      'externalTrafficPolicy: Local restricts external traffic to local-node Pods; topology-aware routing prefers same-zone endpoints for internal traffic. Together they minimize cross-zone traffic for all sources.',
      'Topology-aware routing overrides externalTrafficPolicy',
      'They conflict and cause unpredictable routing',
    ],
    correctAnswer: 1,
    explanation: 'These features complement each other: externalTrafficPolicy: Local ensures external traffic stays on the receiving node, while topology-aware routing prefers same-zone endpoints for internal cluster traffic. Together they minimize cross-zone data transfer costs.',
    tags: ['kubernetes', 'loadbalancer', 'topology', 'external-traffic-policy'],
  },
  {
    id: 'k8s-networking-l4-056',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the "protocol sniffing" behavior in Istio and what are its pitfalls?',
    options: [
      'Istio scans for protocol vulnerabilities',
      'Istio auto-detects whether traffic is HTTP or TCP by inspecting initial bytes; this can misidentify protocols, causing unexpected behavior for non-HTTP TCP traffic or delayed connection establishment',
      'Istio detects and blocks non-standard protocols',
      'Protocol sniffing determines the TLS version',
    ],
    correctAnswer: 1,
    explanation: 'When port protocol is not explicitly declared, Istio Envoy inspects initial bytes to detect HTTP. This can misidentify binary TCP protocols as HTTP (causing corruption) or add latency waiting for enough bytes. Always explicitly declare port protocols.',
    codeSnippet: '# Best practice: explicitly name ports with protocol prefix\napiVersion: v1\nkind: Service\nmetadata:\n  name: my-svc\nspec:\n  ports:\n    - name: http-api      # Istio treats as HTTP\n      port: 8080\n    - name: tcp-database   # Istio treats as TCP\n      port: 5432\n    - name: grpc-service   # Istio treats as gRPC\n      port: 50051',
    tags: ['kubernetes', 'service-mesh', 'istio', 'protocol-sniffing', 'pitfalls'],
  },
  {
    id: 'k8s-networking-l4-057',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the behavior of kube-proxy when it detects stale UDP conntrack entries after an endpoint change?',
    options: [
      'UDP conntrack entries are automatically cleaned up',
      'kube-proxy proactively cleans stale UDP conntrack entries for terminated endpoints; without this, UDP traffic may be black-holed until the conntrack entry expires (default 30s)',
      'kube-proxy does not manage conntrack entries',
      'UDP does not use conntrack',
    ],
    correctAnswer: 1,
    explanation: 'kube-proxy includes logic to delete stale UDP conntrack entries when endpoints change. Without this cleanup, UDP packets would continue being routed to terminated Pods via the stale conntrack entry until it expires.',
    tags: ['kubernetes', 'kube-proxy', 'udp', 'conntrack', 'stale-entries'],
  },
  {
    id: 'k8s-networking-l4-058',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'How does Cilium implement "identity-aware" network policies at scale?',
    options: [
      'By assigning IP ranges per identity',
      'By embedding numeric identity in packet headers (via Geneve options) and checking identity in eBPF programs at the receiver, avoiding IP-to-identity lookups',
      'By using SPIFFE certificates for identity',
      'By creating unique NetworkPolicies per Pod',
    ],
    correctAnswer: 1,
    explanation: 'Cilium embeds the sender security identity in the Geneve tunnel option header. The receiving node eBPF program extracts this identity and performs policy checks against it. This is more efficient and resilient than IP-based matching.',
    tags: ['kubernetes', 'network-policies', 'cilium', 'identity', 'ebpf', 'internals'],
  },
  {
    id: 'k8s-networking-l4-059',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'What is the PROXY protocol v2 in the context of Kubernetes Ingress and LoadBalancer Services?',
    options: [
      'A custom protocol for proxying HTTP requests',
      'A binary protocol prepended to TCP connections that carries original client connection metadata (IP, port, TLS info); more efficient than v1 text format and supports TLS extension data',
      'Version 2 of the kube-proxy protocol',
      'A protocol for multi-proxy chaining',
    ],
    correctAnswer: 1,
    explanation: 'PROXY protocol v2 uses a binary header format to convey original client connection info through proxies. It is more efficient than v1 (text) and can carry additional TLV data like TLS version and cipher info.',
    tags: ['kubernetes', 'ingress', 'proxy-protocol', 'loadbalancer'],
  },
  {
    id: 'k8s-networking-l4-060',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the impact of running a CNI plugin upgrade in a live cluster?',
    options: [
      'CNI upgrades have no impact on running Pods',
      'Running Pods keep existing network configuration but new Pods use updated CNI; a CNI bug during upgrade can prevent new Pod scheduling. DaemonSet rollout strategy matters for minimizing disruption.',
      'All Pods are restarted during CNI upgrade',
      'CNI upgrades require cluster recreation',
    ],
    correctAnswer: 1,
    explanation: 'Existing Pods retain their network config (veth pairs, IPs). New Pods use the updated CNI binary. The DaemonSet update strategy (RollingUpdate) ensures nodes are updated one at a time. If the new CNI is broken, new Pods on updated nodes fail to get networking.',
    tags: ['kubernetes', 'cni', 'upgrade', 'operational', 'risk'],
  },
  {
    id: 'k8s-networking-l4-061',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the Service Proxy Name field and how does it enable custom Service implementations?',
    options: [
      'It sets the name of the kube-proxy Pod',
      'It allows multiple kube-proxy implementations; Services with a specific proxyName are only handled by the matching proxy, enabling custom load balancing implementations',
      'It renames the Service in DNS',
      'It sets the proxy container name in a service mesh',
    ],
    correctAnswer: 1,
    explanation: 'The spec.internalTrafficPolicy combined with the kube-proxy --proxy-name flag allows multiple proxy implementations. Only the proxy matching a Service proxyName handles that Service, enabling custom L4 load balancers.',
    tags: ['kubernetes', 'services', 'proxy-name', 'custom-proxy'],
  },
  {
    id: 'k8s-networking-l4-062',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What are the implications of enabling DNSSEC validation in CoreDNS for a Kubernetes cluster?',
    options: [
      'All internal DNS is automatically signed',
      'DNSSEC validation for upstream queries works but internal cluster DNS is NOT signed; enabling it can break resolution for domains with misconfigured DNSSEC records or cause latency from validation overhead',
      'DNSSEC is fully supported for all records',
      'DNSSEC replaces mTLS for DNS security',
    ],
    correctAnswer: 1,
    explanation: 'CoreDNS can validate DNSSEC for upstream queries, but Kubernetes internal DNS (Services, Pods) is not DNSSEC-signed. Enabling validation may break resolution for external domains with misconfigured DNSSEC and adds CPU overhead.',
    tags: ['kubernetes', 'dns', 'coredns', 'dnssec', 'security'],
  },
  {
    id: 'k8s-networking-l4-063',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'How does Istio handle the interaction between Kubernetes NetworkPolicies and Istio AuthorizationPolicies?',
    options: [
      'They are mutually exclusive',
      'NetworkPolicies are enforced at L3/L4 by the CNI, AuthorizationPolicies at L7 by Envoy; both apply independently, creating defense-in-depth. A packet must pass both to reach the application.',
      'Istio AuthorizationPolicies override NetworkPolicies',
      'NetworkPolicies are disabled when Istio is installed',
    ],
    correctAnswer: 1,
    explanation: 'NetworkPolicies (CNI level, L3/L4) and Istio AuthorizationPolicies (Envoy level, L7) are enforced independently. Traffic must pass both layers. This creates defense-in-depth with network-level and application-level access control.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'network-policies', 'defense-in-depth'],
  },
  {
    id: 'k8s-networking-l4-064',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'headless-services',
    question: 'What is the DNS behavior when a headless Service has both ready and not-ready Pods with publishNotReadyAddresses: true?',
    options: [
      'Only ready Pod IPs are returned',
      'All Pod IPs are returned in DNS regardless of readiness state, including terminating Pods',
      'Not-ready Pods are listed in a separate DNS record',
      'DNS returns an error when not-ready Pods exist',
    ],
    correctAnswer: 1,
    explanation: 'With publishNotReadyAddresses: true, CoreDNS returns A records for ALL Pods backing the Service, regardless of their readiness state. This includes Pods that are initializing, failing probes, or terminating.',
    tags: ['kubernetes', 'headless-services', 'dns', 'publish-not-ready'],
  },
  {
    id: 'k8s-networking-l4-065',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the Cilium "Egress Gateway" feature and how does it solve the problem of stable egress IPs?',
    options: [
      'It routes all egress through the API server',
      'It designates specific nodes as egress gateways that SNAT Pod traffic to a predictable IP, solving the problem of external services needing to whitelist cluster egress IPs',
      'It creates NAT gateways in the cloud',
      'It blocks all egress traffic by default',
    ],
    correctAnswer: 1,
    explanation: 'Cilium Egress Gateway routes selected Pod egress traffic through designated gateway nodes, SNATing to a stable IP. External services can whitelist this IP instead of all node IPs, simplifying firewall rules.',
    codeSnippet: 'apiVersion: cilium.io/v2\nkind: CiliumEgressGatewayPolicy\nmetadata:\n  name: stable-egress\nspec:\n  selectors:\n    - podSelector:\n        matchLabels:\n          app: external-api-client\n  destinationCIDRs:\n    - "203.0.113.0/24"\n  egressGateway:\n    nodeSelector:\n      matchLabels:\n        egress-gateway: "true"\n    egressIP: 10.0.0.100',
    tags: ['kubernetes', 'cni', 'cilium', 'egress-gateway', 'stable-ip'],
  },
  {
    id: 'k8s-networking-l4-066',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the "deny-all" vs "isolate" semantic difference in NetworkPolicies?',
    options: [
      'They are the same thing',
      'Selecting a Pod with any NetworkPolicy "isolates" it (default deny for selected policyTypes); empty rules in that policyType means deny-all. But the Pod is NOT isolated for policyTypes not listed.',
      'Deny-all blocks all traffic; isolate only blocks external traffic',
      'Deny-all requires explicit rules; isolate uses default rules',
    ],
    correctAnswer: 1,
    explanation: 'A Pod becomes "isolated" for a traffic direction when ANY NetworkPolicy selects it AND lists that direction in policyTypes. If a policy has "policyTypes: [Ingress]" but no ingress rules, ingress is denied. But egress is NOT affected (still open) because Egress is not in policyTypes.',
    tags: ['kubernetes', 'network-policies', 'semantics', 'isolation'],
  },
  {
    id: 'k8s-networking-l4-067',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'loadbalancer',
    question: 'How does the AWS Load Balancer Controller implement "IP mode" targeting and what advantages does it provide?',
    options: [
      'IP mode assigns public IPs to Pods',
      'IP mode registers Pod IPs directly as targets in the NLB/ALB target group, bypassing NodePorts; this reduces hops, preserves source IP, and eliminates the NodePort bottleneck',
      'IP mode uses static IPs for the load balancer',
      'IP mode creates one target group per IP',
    ],
    correctAnswer: 1,
    explanation: 'In IP mode, the AWS LB Controller registers Pod IPs (not node IPs) as targets. This eliminates the NodePort hop, preserves client source IP, enables pod-level health checks, and allows the LB to route directly to the correct Pod.',
    codeSnippet: 'apiVersion: v1\nkind: Service\nmetadata:\n  name: web\n  annotations:\n    service.beta.kubernetes.io/aws-load-balancer-type: "external"\n    service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: "ip"\n    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"\nspec:\n  type: LoadBalancer\n  loadBalancerClass: service.k8s.aws/nlb',
    tags: ['kubernetes', 'loadbalancer', 'aws', 'ip-mode', 'alb-controller'],
  },
  {
    id: 'k8s-networking-l4-068',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What happens at the kernel level when kube-proxy IPVS mode processes a new Service connection?',
    options: [
      'IPVS creates a new iptables rule',
      'The IPVS kernel module matches the VIP:port to a virtual server, selects a backend using the configured algorithm, creates a connection entry in the IPVS connection table, and performs DNAT',
      'IPVS sends the packet to userspace kube-proxy',
      'IPVS performs DNS resolution for the Service name',
    ],
    correctAnswer: 1,
    explanation: 'IPVS operates in the kernel: it matches incoming packets against virtual servers (VIP:port), applies the configured scheduling algorithm (rr, lc, sh, etc.), creates a persistent connection entry in its hash table, and performs inline DNAT to the selected backend.',
    tags: ['kubernetes', 'kube-proxy', 'ipvs', 'kernel', 'internals'],
  },
  {
    id: 'k8s-networking-l4-069',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the "sidecar resource consumption" problem at scale and how does ambient mesh address it?',
    options: [
      'Sidecars use minimal resources at any scale',
      'Each sidecar consumes 50-100MB RAM and CPU overhead; in a cluster with thousands of Pods, sidecar resources can exceed application resources. Ambient mesh shares L4 proxy (ztunnel) per node, reducing total overhead by 90%+',
      'Sidecars are only a problem with gRPC',
      'Resource consumption scales linearly with traffic, not Pods',
    ],
    correctAnswer: 1,
    explanation: 'At scale, sidecar memory (50-100MB+ each) and CPU overhead is significant. A 1000-Pod cluster could use 50-100GB just for sidecars. Ambient mesh uses per-node ztunnel (one proxy per node, not per Pod), dramatically reducing total resource overhead.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'ambient-mesh', 'resources', 'scale'],
  },
  {
    id: 'k8s-networking-l4-070',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What is the CoreDNS "bufsize" directive and why does it matter for EDNS0 compliance?',
    options: [
      'It sets the maximum DNS record size stored in cache',
      'It sets the EDNS0 UDP payload size advertised by CoreDNS; the default was reduced from 4096 to 1232 to prevent DNS amplification attacks and fragmentation issues',
      'It configures the buffer for TCP DNS connections',
      'It limits the number of DNS records returned',
    ],
    correctAnswer: 1,
    explanation: 'The bufsize directive sets the EDNS0 UDP buffer size. The recommendation (RFC 8020/DNS Flag Day 2020) is 1232 bytes to avoid IP fragmentation issues. Larger values can enable DNS amplification attacks or cause packet loss.',
    codeSnippet: '.:53 {\n    bufsize 1232  # Recommended EDNS0 buffer size\n    cache 30\n    forward . /etc/resolv.conf\n}',
    tags: ['kubernetes', 'dns', 'coredns', 'edns0', 'bufsize'],
  },
  {
    id: 'k8s-networking-l4-071',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'How does Calico BGP route reflector configuration optimize large cluster networking?',
    options: [
      'Every node peers with every other node in a full mesh',
      'Route reflectors aggregate and redistribute BGP routes, changing the topology from O(n^2) full mesh to O(n) hub-and-spoke, dramatically reducing BGP session count in large clusters',
      'Route reflectors cache routes for faster convergence',
      'Route reflectors are only needed for multi-cluster setups',
    ],
    correctAnswer: 1,
    explanation: 'In a full-mesh BGP topology, each node peers with every other node (n*(n-1)/2 sessions). Route reflectors act as central hubs — nodes peer only with reflectors, reducing sessions from O(n^2) to O(n). Critical for clusters with 100+ nodes.',
    codeSnippet: '# Calico BGP route reflector configuration\napiVersion: crd.projectcalico.org/v1\nkind: BGPPeer\nmetadata:\n  name: peer-with-rr\nspec:\n  peerIP: 10.0.0.100\n  asNumber: 64512\n  nodeSelector: "!route-reflector"\n---\napiVersion: crd.projectcalico.org/v1\nkind: Node\nmetadata:\n  name: rr-node\n  labels:\n    route-reflector: "true"\nspec:\n  bgp:\n    routeReflectorClusterID: 224.0.0.1',
    tags: ['kubernetes', 'cni', 'calico', 'bgp', 'route-reflector', 'scalability'],
  },
  {
    id: 'k8s-networking-l4-072',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What are the implications of NetworkPolicy evaluation order and why cannot you create "deny" rules with standard NetworkPolicies?',
    options: [
      'Deny rules are supported but with lower priority',
      'Standard NetworkPolicies are purely additive (whitelist model); once a Pod is selected, all non-explicitly-allowed traffic is denied. There is no concept of a deny rule because there is nothing to deny — everything is denied by default when isolated.',
      'Deny rules were removed in Kubernetes 1.20',
      'Deny rules require a special API flag to enable',
    ],
    correctAnswer: 1,
    explanation: 'The standard NetworkPolicy API is a pure whitelist model. When a Pod is selected by any policy, all unmatched traffic for that policyType is implicitly denied. Multiple policies are additive (union). For explicit deny rules, use Calico or Cilium extended policies.',
    tags: ['kubernetes', 'network-policies', 'whitelist', 'deny', 'design'],
  },
  {
    id: 'k8s-networking-l4-073',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'How does the Gateway API "HTTPRoute" handle traffic splitting compared to Ingress canary annotations?',
    options: [
      'They work identically',
      'HTTPRoute natively supports backendRefs with weight fields for traffic splitting as a first-class API feature, while Ingress requires controller-specific annotations, making Gateway API more portable',
      'Ingress canary is more feature-rich',
      'HTTPRoute does not support traffic splitting',
    ],
    correctAnswer: 1,
    explanation: 'Gateway API HTTPRoute has native weight-based routing in the spec (backendRefs with weight). This is portable across all Gateway API implementations, unlike Ingress where canary/splitting relies on controller-specific annotations.',
    codeSnippet: 'apiVersion: gateway.networking.k8s.io/v1\nkind: HTTPRoute\nmetadata:\n  name: canary\nspec:\n  parentRefs:\n    - name: my-gateway\n  rules:\n    - backendRefs:\n        - name: app-v1\n          port: 80\n          weight: 90\n        - name: app-v2\n          port: 80\n          weight: 10',
    tags: ['kubernetes', 'gateway-api', 'traffic-splitting', 'httproute'],
  },
  {
    id: 'k8s-networking-l4-074',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the Istio "ServiceEntry" resource and how does it extend the mesh to external services?',
    options: [
      'ServiceEntry creates external Kubernetes Services',
      'ServiceEntry adds external services to the Istio service registry, enabling mesh features (mTLS, retries, circuit breaking, observability) for traffic to external endpoints',
      'ServiceEntry replaces ExternalName Services',
      'ServiceEntry blocks traffic to unregistered external services',
    ],
    correctAnswer: 1,
    explanation: 'ServiceEntry adds external hosts to Istio internal service registry. This allows applying mesh features like traffic management, mTLS, retries, and observability to outbound traffic to external APIs, databases, or legacy services.',
    codeSnippet: 'apiVersion: networking.istio.io/v1alpha3\nkind: ServiceEntry\nmetadata:\n  name: external-api\nspec:\n  hosts:\n    - api.external.com\n  ports:\n    - number: 443\n      name: https\n      protocol: TLS\n  location: MESH_EXTERNAL\n  resolution: DNS\n---\n# Now you can apply VirtualService rules to this external service\napiVersion: networking.istio.io/v1alpha3\nkind: VirtualService\nmetadata:\n  name: external-api\nspec:\n  hosts: [api.external.com]\n  http:\n    - timeout: 5s\n      retries:\n        attempts: 3',
    tags: ['kubernetes', 'service-mesh', 'istio', 'service-entry', 'external'],
  },
  {
    id: 'k8s-networking-l4-075',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the "nf_conntrack_tcp_be_liberal" sysctl and why is it relevant to Kubernetes Service networking?',
    options: [
      'It controls TCP keepalive behavior',
      'When set to 1, conntrack allows TCP packets with out-of-window sequence numbers instead of dropping them; this prevents false connection resets during asymmetric routing in SNAT scenarios',
      'It enables liberal TCP window scaling',
      'It controls the TCP connection timeout',
    ],
    correctAnswer: 1,
    explanation: 'In SNAT scenarios common with Kubernetes Services, return traffic may take a different path, causing out-of-window packets. nf_conntrack_tcp_be_liberal=1 prevents conntrack from dropping these legitimate packets, avoiding spurious connection resets.',
    tags: ['kubernetes', 'services', 'conntrack', 'sysctl', 'internals'],
  },
  {
    id: 'k8s-networking-l4-076',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'How does CoreDNS "ready" plugin differ from the "health" plugin, and why are both needed?',
    options: [
      'They serve the same purpose',
      '"health" reports CoreDNS process liveness on port 8080 (for liveness probes); "ready" reports if all plugins are loaded on port 8181 (for readiness probes). Using both ensures correct Pod lifecycle management.',
      '"ready" is for external checks, "health" is internal',
      '"health" checks DNS resolution, "ready" checks HTTP',
    ],
    correctAnswer: 1,
    explanation: 'The health plugin (port 8080) indicates CoreDNS is alive (liveness probe). The ready plugin (port 8181) indicates all plugins have loaded and CoreDNS can serve queries (readiness probe). Both are needed for proper Kubernetes probe configuration.',
    codeSnippet: '.:53 {\n    health :8080    # Liveness: is CoreDNS process alive?\n    ready :8181     # Readiness: are all plugins loaded?\n    kubernetes cluster.local\n    forward . /etc/resolv.conf\n}\n---\n# CoreDNS Pod probes\nlivenessProbe:\n  httpGet:\n    path: /health\n    port: 8080\nreadinessProbe:\n  httpGet:\n    path: /ready\n    port: 8181',
    tags: ['kubernetes', 'dns', 'coredns', 'health', 'ready', 'probes'],
  },
  {
    id: 'k8s-networking-l4-077',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the "local redirect" policy in Cilium and how is it used for node-local DNS caching?',
    options: [
      'It redirects all traffic to localhost',
      'It intercepts traffic destined for a specific Service VIP and redirects it to a local backend Pod on the same node, enabling transparent node-local DNS caching without modifying Pod DNS config',
      'It creates local iptables redirect rules',
      'It redirects failed connections to a local fallback',
    ],
    correctAnswer: 1,
    explanation: 'Cilium Local Redirect Policy can transparently redirect traffic to CoreDNS ClusterIP to a local node-local-dns Pod instead, without changing Pod resolv.conf. This provides DNS caching benefits without any application or kubelet changes.',
    codeSnippet: 'apiVersion: cilium.io/v2\nkind: CiliumLocalRedirectPolicy\nmetadata:\n  name: node-local-dns\n  namespace: kube-system\nspec:\n  redirectFrontend:\n    serviceMatcher:\n      serviceName: kube-dns\n      namespace: kube-system\n  redirectBackend:\n    localEndpointSelector:\n      matchLabels:\n        k8s-app: node-local-dns\n    toPorts:\n      - port: "53"\n        protocol: UDP',
    tags: ['kubernetes', 'cni', 'cilium', 'local-redirect', 'dns-caching'],
  },
  {
    id: 'k8s-networking-l4-078',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'endpoint-slices',
    question: 'What is the "traffic distribution" field in EndpointSlices (Kubernetes 1.30+) and how does it improve topology-aware routing?',
    options: [
      'It sets bandwidth limits per endpoint',
      'It provides hints about where traffic should be routed to (e.g., "PreferClose"), replacing the older topology hints mechanism with a simpler, more flexible API',
      'It distributes DNS queries across endpoints',
      'It determines how many connections each endpoint receives',
    ],
    correctAnswer: 1,
    explanation: 'The traffic distribution field (service.kubernetes.io/topology-mode) in Kubernetes 1.31+ provides simplified topology-aware routing. "PreferClose" replaces the complex hints system with a straightforward preference for nearby endpoints.',
    tags: ['kubernetes', 'endpoint-slices', 'traffic-distribution', 'topology'],
  },
  {
    id: 'k8s-networking-l4-079',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'What is the impact of Envoy "draining" state on graceful Service mesh upgrades?',
    options: [
      'Draining has no impact on upgrades',
      'During drain, Envoy stops accepting new connections but continues processing existing ones; the drain duration must be coordinated with terminationGracePeriodSeconds and preStop hooks for zero-downtime upgrades',
      'Draining only affects the control plane',
      'Draining disconnects all connections immediately',
    ],
    correctAnswer: 1,
    explanation: 'Envoy drain duration, Pod terminationGracePeriodSeconds, and preStop hooks must be coordinated. If drain completes before in-flight requests finish, connections drop. If terminationGracePeriodSeconds is too short, SIGKILL kills the proxy before drain completes.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'envoy', 'draining', 'zero-downtime'],
  },
  {
    id: 'k8s-networking-l4-080',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the Kubernetes Service "IPMode" field for LoadBalancer Services and what problem does it solve?',
    options: [
      'It switches between IPv4 and IPv6',
      'IPMode indicates whether the LoadBalancer status IP is a VIP that is load balanced by the LB ("VIP") or directly assigned to a node ("Proxy"); this tells kube-proxy whether to create local routing rules',
      'It sets the IP allocation mode for the cluster',
      'It configures IPAM for the cloud provider',
    ],
    correctAnswer: 1,
    explanation: 'IPMode (beta in 1.29) tells kube-proxy whether the LB IP is a VIP (LB handles balancing, kube-proxy creates DNAT rules) or Proxy (LB sends to specific node, kube-proxy skips rules). This prevents double-hop routing for proxy-mode LBs.',
    tags: ['kubernetes', 'services', 'loadbalancer', 'ipmode', 'internals'],
  },
  {
    id: 'k8s-networking-l4-081',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the known limitation of NetworkPolicies with NodePort Services?',
    options: [
      'NetworkPolicies fully protect NodePort traffic',
      'NodePort traffic arrives via kube-proxy SNAT, so the original client IP is lost; ingress NetworkPolicy ipBlock rules cannot match the real client IP, only the node IP after SNAT',
      'NetworkPolicies block NodePort traffic by default',
      'NodePort Services ignore NetworkPolicies entirely',
    ],
    correctAnswer: 1,
    explanation: 'With externalTrafficPolicy: Cluster, kube-proxy performs SNAT on NodePort traffic, replacing the client IP with the node IP. This means NetworkPolicy ipBlock rules cannot match the real client IP. Use externalTrafficPolicy: Local to preserve client IP.',
    tags: ['kubernetes', 'network-policies', 'nodeport', 'snat', 'limitation'],
  },
  {
    id: 'k8s-networking-l4-082',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'ingress',
    question: 'What is the "mirror" traffic feature in the Gateway API and how does it differ from Istio traffic mirroring?',
    options: [
      'They are identical features',
      'Gateway API HTTPRoute supports requestMirror filter natively in the spec; Istio uses VirtualService mirror field. Gateway API version is portable across implementations while Istio version supports more options like mirror percentage.',
      'Only Istio supports traffic mirroring',
      'Gateway API mirroring replaces the original request',
    ],
    correctAnswer: 1,
    explanation: 'Both support traffic mirroring (sending a copy of traffic to a secondary service). Gateway API uses requestMirror in HTTPRoute filters (portable). Istio uses VirtualService mirror field with additional controls like mirror percentage.',
    codeSnippet: '# Gateway API traffic mirroring\napiVersion: gateway.networking.k8s.io/v1\nkind: HTTPRoute\nspec:\n  rules:\n    - filters:\n        - type: RequestMirror\n          requestMirror:\n            backendRef:\n              name: mirror-svc\n              port: 80\n      backendRefs:\n        - name: primary-svc\n          port: 80',
    tags: ['kubernetes', 'gateway-api', 'traffic-mirroring', 'comparison'],
  },
  {
    id: 'k8s-networking-l4-083',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'cni',
    question: 'What is the "socket-level load balancing" in Cilium and how does it differ from packet-level DNAT?',
    options: [
      'It load balances at the TCP socket level instead of the application level',
      'Cilium can intercept connect() syscalls and translate the Service VIP to a backend IP at socket creation time, avoiding per-packet DNAT overhead and eliminating the need for conntrack entries',
      'It creates separate sockets for each backend',
      'It only works with UDP sockets',
    ],
    correctAnswer: 1,
    explanation: 'Socket-level load balancing hooks into the connect() syscall via eBPF, replacing the Service VIP with a backend IP before the first packet is sent. This eliminates per-packet DNAT, avoids conntrack entries, and is faster than packet-level processing.',
    tags: ['kubernetes', 'cni', 'cilium', 'socket-lb', 'ebpf', 'performance'],
  },
  {
    id: 'k8s-networking-l4-084',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'dns',
    question: 'What is the impact of CoreDNS "loop" plugin detection on cluster stability?',
    options: [
      'It detects infinite DNS query loops and restarts CoreDNS',
      'The loop plugin detects forwarding loops (CoreDNS querying itself) and causes a crash to prevent resource exhaustion; improper resolv.conf configuration on the node can trigger false positives',
      'It detects circular CNAME records',
      'It limits the number of DNS queries per second',
    ],
    correctAnswer: 1,
    explanation: 'The loop plugin sends a special query and checks if it comes back, detecting forwarding loops. If the node /etc/resolv.conf points to the CoreDNS ClusterIP (e.g., in systemd-resolved configurations), CoreDNS detects a loop and crashes, causing DNS outages.',
    tags: ['kubernetes', 'dns', 'coredns', 'loop-detection', 'stability'],
  },
  {
    id: 'k8s-networking-l4-085',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'service-mesh',
    question: 'How does Istio handle the "thundering herd" problem during mTLS certificate rotation?',
    options: [
      'All certificates are rotated simultaneously',
      'Istio uses graceful certificate rotation with overlap periods; istiod issues new certificates before old ones expire, and Envoy performs hot-restart of TLS contexts without dropping connections',
      'Certificate rotation requires service downtime',
      'Only the control plane certificate is rotated',
    ],
    correctAnswer: 1,
    explanation: 'Istio staggers certificate rotation by issuing new certificates before expiry (default lifetime 24h, rotation at ~12h). Envoy performs in-place TLS context updates using its SDS (Secret Discovery Service) API without connection interruption.',
    tags: ['kubernetes', 'service-mesh', 'istio', 'certificate-rotation', 'mtls'],
  },
  {
    id: 'k8s-networking-l4-086',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'clusterip',
    question: 'What is the "min-sync-period" and "sync-period" in kube-proxy and how do they affect Service update latency?',
    options: [
      'They control how often kube-proxy checks for DNS changes',
      'min-sync-period is the minimum time between iptables/IPVS syncs; sync-period is the maximum time between full syncs. Lowering min-sync-period reduces update latency but increases CPU usage.',
      'They control kube-proxy restart intervals',
      'They set the Service endpoint health check interval',
    ],
    correctAnswer: 1,
    explanation: 'min-sync-period (default 1s) prevents too-frequent rule updates. sync-period (default 30s) triggers periodic full reconciliation. In high-churn environments, tuning these balances update latency against CPU usage from rule regeneration.',
    tags: ['kubernetes', 'kube-proxy', 'sync-period', 'tuning'],
  },
  {
    id: 'k8s-networking-l4-087',
    language: 'kubernetes',
    level: 'level4',
    category: 'Services & Networking',
    subcategory: 'network-policies',
    question: 'What is the Cilium "policy audit mode" and how does it help with NetworkPolicy migration?',
    options: [
      'It audits who created each policy',
      'Audit mode logs what traffic would be allowed or denied by policies without actually enforcing them, enabling safe validation of new policies before switching to enforcement mode',
      'It creates audit logs of all network traffic',
      'It validates policy YAML syntax',
    ],
    correctAnswer: 1,
    explanation: 'Cilium policy audit mode (policyAuditMode: true) applies policies in monitor-only mode. All traffic is allowed, but Hubble logs show what would be allowed/denied. This enables safe policy development and migration from no-policy to policy-enforced clusters.',
    codeSnippet: '# Enable policy audit mode\nhelm upgrade cilium cilium/cilium \\\n  --set policyAuditMode=true\n\n# Monitor what would be denied\nhubble observe --verdict AUDIT\n\n# After validating, disable audit mode to enforce\nhelm upgrade cilium cilium/cilium \\\n  --set policyAuditMode=false',
    tags: ['kubernetes', 'network-policies', 'cilium', 'audit-mode', 'migration'],
  },
];

export default questions;
