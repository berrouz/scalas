import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'docker-cicd-l2-001',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you cache Docker layers between GitHub Actions workflow runs?',
    options: [
      'Docker layers are automatically cached between runs',
      'Use the cache-from and cache-to options with the GitHub Actions cache backend (type=gha)',
      'Use the actions/cache action to cache the Docker daemon',
      'You cannot cache Docker layers in GitHub Actions',
    ],
    correctAnswer: 1,
    explanation: 'The docker/build-push-action supports BuildKit cache backends. The GHA cache type stores layers in GitHub Actions cache, persisting across workflow runs.',
    codeSnippet: '- uses: docker/build-push-action@v5\n  with:\n    context: .\n    push: true\n    tags: myapp:latest\n    cache-from: type=gha\n    cache-to: type=gha,mode=max',
    tags: ['github-actions', 'caching', 'gha-cache'],
  },
  {
    id: 'docker-cicd-l2-002',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'What are the different cache export types supported by Docker Buildx?',
    options: [
      'Only local cache is supported',
      'inline, local, registry, gha (GitHub Actions), and s3',
      'Only registry cache is supported',
      'inline and local only',
    ],
    correctAnswer: 1,
    explanation: 'Buildx supports multiple cache backends: inline (embedded in image), local (directory), registry (OCI image), gha (GitHub Actions cache), and s3 (AWS S3).',
    codeSnippet: '# Registry cache\ndocker buildx build \\\n  --cache-from type=registry,ref=registry.example.com/myapp:cache \\\n  --cache-to type=registry,ref=registry.example.com/myapp:cache,mode=max \\\n  -t myapp:latest .',
    tags: ['buildx', 'cache-types', 'export'],
  },
  {
    id: 'docker-cicd-l2-003',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you use the Docker socket mounting approach instead of DinD in GitLab CI?',
    options: [
      'Mount /var/run/docker.sock from the host into the job container',
      'Use the --privileged flag on the runner',
      'Install Docker inside the job container',
      'Docker socket mounting is not supported',
    ],
    correctAnswer: 0,
    explanation: 'Mounting the Docker socket from the host allows the CI job to use the host\'s Docker daemon directly, avoiding the overhead of DinD.',
    codeSnippet: '# GitLab runner config.toml\n[[runners]]\n  [runners.docker]\n    volumes = ["/var/run/docker.sock:/var/run/docker.sock"]\n\n# .gitlab-ci.yml\nbuild:\n  image: docker:24\n  script:\n    - docker build -t myapp .',
    tags: ['gitlab-ci', 'docker-socket', 'runner'],
  },
  {
    id: 'docker-cicd-l2-004',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you build multi-platform images in a GitHub Actions workflow?',
    options: [
      'Run separate build jobs for each platform',
      'Use docker/setup-qemu-action for emulation and docker/build-push-action with the platforms input',
      'Build only on ARM runners',
      'Multi-platform builds are not supported in GitHub Actions',
    ],
    correctAnswer: 1,
    explanation: 'Set up QEMU for cross-platform emulation, configure Buildx, then use the platforms input to specify target architectures in a single build step.',
    codeSnippet: 'steps:\n  - uses: docker/setup-qemu-action@v3\n  - uses: docker/setup-buildx-action@v3\n  - uses: docker/build-push-action@v5\n    with:\n      platforms: linux/amd64,linux/arm64\n      push: true\n      tags: myapp:latest',
    tags: ['multi-platform', 'github-actions', 'qemu'],
  },
  {
    id: 'docker-cicd-l2-005',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'What is the difference between mode=min and mode=max for BuildKit cache export?',
    options: [
      'min uses less disk space; max uses more',
      'min caches only the final build stage layers; max caches all intermediate layers from all stages',
      'min is faster; max is slower',
      'They are the same; mode is just a hint',
    ],
    correctAnswer: 1,
    explanation: 'mode=min exports only the layers in the final build stage. mode=max exports layers from all intermediate build stages, providing better cache hits for multi-stage builds.',
    codeSnippet: '# Max mode - caches all stages\ndocker buildx build \\\n  --cache-to type=registry,ref=myapp:cache,mode=max \\\n  -t myapp:latest .\n\n# Min mode - caches only final stage\ndocker buildx build \\\n  --cache-to type=registry,ref=myapp:cache,mode=min \\\n  -t myapp:latest .',
    tags: ['caching', 'mode', 'buildkit'],
  },
  {
    id: 'docker-cicd-l2-006',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'How do you enable caching in Kaniko builds?',
    options: [
      'Kaniko does not support caching',
      'Using --cache=true flag with --cache-repo to specify a registry for cache layers',
      'By mounting a local cache directory',
      'Caching is always enabled by default',
    ],
    correctAnswer: 1,
    explanation: 'Kaniko supports registry-based layer caching. The --cache=true flag enables caching, and --cache-repo specifies the registry where cached layers are stored.',
    codeSnippet: '/kaniko/executor \\\n  --dockerfile=Dockerfile \\\n  --context=/workspace \\\n  --destination=registry.example.com/myapp:latest \\\n  --cache=true \\\n  --cache-repo=registry.example.com/myapp/cache',
    tags: ['kaniko', 'caching', 'registry'],
  },
  {
    id: 'docker-cicd-l2-007',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you set up a private Docker registry using the official registry image?',
    options: [
      'docker create registry',
      'docker run -d -p 5000:5000 --name registry registry:2',
      'docker registry start',
      'docker service create --name registry nginx',
    ],
    correctAnswer: 1,
    explanation: 'The official Docker registry image (registry:2) can be run as a container, providing a basic private registry for development and small deployments.',
    codeSnippet: '# Start a local registry\ndocker run -d -p 5000:5000 \\\n  --name registry \\\n  -v registry-data:/var/lib/registry \\\n  registry:2\n\n# Push to local registry\ndocker tag myapp:latest localhost:5000/myapp:latest\ndocker push localhost:5000/myapp:latest',
    tags: ['registry', 'private', 'self-hosted'],
  },
  {
    id: 'docker-cicd-l2-008',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you use the docker/metadata-action to generate semantic version tags?',
    options: [
      'It reads version from package.json',
      'It parses Git tags matching semver patterns and generates corresponding Docker tags',
      'It auto-increments version numbers',
      'It reads version from the Dockerfile',
    ],
    correctAnswer: 1,
    explanation: 'The metadata-action can parse semver Git tags and generate multiple Docker tags (e.g., v1.2.3, v1.2, v1, latest) from a single semantic version tag.',
    codeSnippet: '- uses: docker/metadata-action@v5\n  id: meta\n  with:\n    images: ghcr.io/owner/myapp\n    tags: |\n      type=semver,pattern={{version}}\n      type=semver,pattern={{major}}.{{minor}}\n      type=semver,pattern={{major}}\n      type=sha',
    tags: ['github-actions', 'semver', 'metadata'],
  },
  {
    id: 'docker-cicd-l2-009',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you use BuildKit secret mounts to pass sensitive data during builds?',
    options: [
      'Using ARG instructions with environment variables',
      'Using --secret flag to mount secrets that are available only during build and not stored in the image',
      'By encoding secrets in the Dockerfile',
      'Secrets cannot be used during builds',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit secret mounts make sensitive data available during build without persisting them in image layers, solving the problem of needing private keys or tokens during dependency installation.',
    codeSnippet: '# Dockerfile\nRUN --mount=type=secret,id=npm_token \\\n    NPM_TOKEN=$(cat /run/secrets/npm_token) \\\n    npm install\n\n# Build command\ndocker buildx build \\\n  --secret id=npm_token,src=$HOME/.npmrc \\\n  -t myapp:latest .',
    tags: ['buildx', 'secrets', 'security'],
  },
  {
    id: 'docker-cicd-l2-010',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you use GitLab CI dependency proxy for Docker images?',
    options: [
      'It is not available in GitLab',
      'Configure the CI to pull images through ${CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX} to cache Docker Hub images',
      'Use a special Docker daemon configuration',
      'Install a proxy server on the runner',
    ],
    correctAnswer: 1,
    explanation: 'GitLab Dependency Proxy caches Docker Hub images to avoid rate limits and improve pull performance for CI jobs.',
    codeSnippet: 'build:\n  image: $CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX/docker:24\n  services:\n    - name: $CI_DEPENDENCY_PROXY_GROUP_IMAGE_PREFIX/docker:24-dind\n      alias: docker\n  script:\n    - docker build -t myapp .',
    tags: ['gitlab-ci', 'dependency-proxy', 'caching'],
  },
  {
    id: 'docker-cicd-l2-011',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'What is the performance impact of using QEMU for multi-platform builds?',
    options: [
      'No performance impact',
      'Emulated builds can be 5-20x slower than native builds due to instruction translation overhead',
      'QEMU speeds up builds',
      'There is only a 10% overhead',
    ],
    correctAnswer: 1,
    explanation: 'QEMU emulates a different CPU architecture, which means every instruction must be translated, resulting in significant build time increase (typically 5-20x for compute-heavy operations).',
    tags: ['multi-platform', 'qemu', 'performance'],
  },
  {
    id: 'docker-cicd-l2-012',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you use BuildKit cache mounts to speed up package manager installations?',
    options: [
      'Caching package managers is not possible',
      'Using RUN --mount=type=cache to persist package manager caches across builds',
      'By committing the cache directory as a separate layer',
      'By using VOLUME instructions for cache directories',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit cache mounts persist directories across builds. Package manager caches (npm, pip, apt) are kept between builds without being included in the final image.',
    codeSnippet: '# Cache npm modules\nRUN --mount=type=cache,target=/root/.npm \\\n    npm install\n\n# Cache pip packages\nRUN --mount=type=cache,target=/root/.cache/pip \\\n    pip install -r requirements.txt\n\n# Cache apt packages\nRUN --mount=type=cache,target=/var/cache/apt \\\n    apt-get update && apt-get install -y curl',
    tags: ['caching', 'cache-mounts', 'buildkit'],
  },
  {
    id: 'docker-cicd-l2-013',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you configure Docker Hub automated builds?',
    options: [
      'Docker Hub automatically builds all public repositories',
      'Connect your GitHub/Bitbucket repository and configure build rules to trigger builds on push events',
      'Upload Dockerfiles to Docker Hub directly',
      'Docker Hub does not support automated builds',
    ],
    correctAnswer: 1,
    explanation: 'Docker Hub can be linked to source code repositories (GitHub/Bitbucket) and configured with build rules that trigger image builds when code is pushed.',
    tags: ['registry', 'docker-hub', 'automated-builds'],
  },
  {
    id: 'docker-cicd-l2-014',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildah',
    question: 'How do you build images rootlessly with Buildah?',
    options: [
      'Rootless builds are not supported',
      'Buildah supports rootless builds by default using user namespaces, without requiring root privileges',
      'Use the --rootless flag',
      'Configure the kernel for rootless operation first',
    ],
    correctAnswer: 1,
    explanation: 'Buildah can build images as a non-root user using user namespaces (userns), improving security by not requiring root privileges on the host.',
    codeSnippet: '# Run as non-root user\nbuildah bud --isolation chroot -t myapp .\n\n# Or with user namespace remapping\nbuildah unshare buildah bud -t myapp .',
    tags: ['buildah', 'rootless', 'security'],
  },
  {
    id: 'docker-cicd-l2-015',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you conditionally build and push Docker images only on specific events?',
    options: [
      'You cannot control push behavior per event',
      'Use conditional expressions with the push input based on GitHub context variables',
      'Create separate workflows for each event',
      'Use the only keyword',
    ],
    correctAnswer: 1,
    explanation: 'Use GitHub Actions expressions to control when images are pushed, such as only pushing on main branch pushes or tag events while building on all events.',
    codeSnippet: '- uses: docker/build-push-action@v5\n  with:\n    context: .\n    push: \${{ github.event_name != \'pull_request\' }}\n    tags: \${{ steps.meta.outputs.tags }}\n    labels: \${{ steps.meta.outputs.labels }}',
    tags: ['github-actions', 'conditional', 'push'],
  },
  {
    id: 'docker-cicd-l2-016',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you target a specific build stage with docker buildx build?',
    options: [
      'Use --stage flag',
      'Use --target flag to build up to and including a specific named stage',
      'Use --from flag',
      'Specify the stage in the Dockerfile only',
    ],
    correctAnswer: 1,
    explanation: 'The --target flag builds only up to the specified stage, useful for building test or development images from intermediate stages in a multi-stage Dockerfile.',
    codeSnippet: '# Dockerfile\nFROM node:20 AS deps\nCOPY package*.json ./\nRUN npm install\n\nFROM deps AS test\nCOPY . .\nRUN npm test\n\nFROM deps AS production\nCOPY . .\nRUN npm run build\n\n# Build only the test stage\ndocker buildx build --target test -t myapp:test .',
    tags: ['buildx', 'target', 'multi-stage'],
  },
  {
    id: 'docker-cicd-l2-017',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you implement a Docker build cache in GitLab CI?',
    options: [
      'GitLab CI automatically caches Docker layers',
      'Pull the previous image and use --cache-from, or use BuildKit registry cache',
      'Use the cache keyword with Docker layers path',
      'Docker caching is not possible in GitLab CI',
    ],
    correctAnswer: 1,
    explanation: 'In GitLab CI, pull the previously built image and use it as a cache source with --cache-from, or use BuildKit cache export to the registry.',
    codeSnippet: 'build:\n  image: docker:24\n  services:\n    - docker:24-dind\n  variables:\n    DOCKER_BUILDKIT: "1"\n  script:\n    - docker pull $CI_REGISTRY_IMAGE:latest || true\n    - docker build\n        --cache-from $CI_REGISTRY_IMAGE:latest\n        --tag $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n        --tag $CI_REGISTRY_IMAGE:latest\n        .\n    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n    - docker push $CI_REGISTRY_IMAGE:latest',
    tags: ['gitlab-ci', 'caching', 'cache-from'],
  },
  {
    id: 'docker-cicd-l2-018',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'How do you use Kaniko with GitLab CI?',
    options: [
      'Kaniko is not compatible with GitLab CI',
      'Use the Kaniko executor image directly in a GitLab CI job with proper registry authentication',
      'Install Kaniko on the GitLab runner',
      'Use the kaniko keyword in .gitlab-ci.yml',
    ],
    correctAnswer: 1,
    explanation: 'Run the Kaniko executor as the job image in GitLab CI, mounting registry credentials from GitLab CI variables for authentication.',
    codeSnippet: 'build:\n  stage: build\n  image:\n    name: gcr.io/kaniko-project/executor:debug\n    entrypoint: [""]\n  script:\n    - mkdir -p /kaniko/.docker\n    - echo "{\\\"auths\\\":{\\\"\${CI_REGISTRY}\\\":{\\\"auth\\\":\\\"$(printf \"%s:%s\" \"\${CI_REGISTRY_USER}\" \"\${CI_REGISTRY_PASSWORD}\" | base64 | tr -d \'\\n\')\\\"}}}" > /kaniko/.docker/config.json\n    - /kaniko/executor\n        --context=\"\${CI_PROJECT_DIR}\"\n        --dockerfile=\"\${CI_PROJECT_DIR}/Dockerfile\"\n        --destination=\"\${CI_REGISTRY_IMAGE}:\${CI_COMMIT_TAG}\"',
    tags: ['kaniko', 'gitlab-ci', 'integration'],
  },
  {
    id: 'docker-cicd-l2-019',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'What is the difference between Docker Hub and GitHub Container Registry for image storage?',
    options: [
      'They are identical services',
      'Docker Hub is the default public registry with rate limits; GHCR is GitHub-integrated with GITHUB_TOKEN authentication and repository-level permissions',
      'GHCR only supports private images',
      'Docker Hub is faster than GHCR',
    ],
    correctAnswer: 1,
    explanation: 'Docker Hub is the default public registry but has pull rate limits. GHCR integrates with GitHub permissions, uses GITHUB_TOKEN for auth, and links images to repositories.',
    tags: ['registry', 'comparison', 'ghcr'],
  },
  {
    id: 'docker-cicd-l2-020',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you handle platform-specific dependencies in a multi-platform Dockerfile?',
    options: [
      'Create separate Dockerfiles per platform',
      'Use TARGETPLATFORM or TARGETARCH build arguments to conditionally install platform-specific packages',
      'Platform-specific dependencies are not possible',
      'Use separate base images per platform',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit automatically sets TARGETPLATFORM, TARGETOS, and TARGETARCH build arguments during multi-platform builds, which can be used for conditional logic.',
    codeSnippet: 'FROM --platform=$BUILDPLATFORM golang:1.21 AS builder\nARG TARGETARCH\nARG TARGETOS\nRUN CGO_ENABLED=0 GOOS=$TARGETOS GOARCH=$TARGETARCH \\\n    go build -o /app .\n\nFROM alpine:3.18\nCOPY --from=builder /app /app\nCMD ["/app"]',
    tags: ['multi-platform', 'targetarch', 'conditional'],
  },
  {
    id: 'docker-cicd-l2-021',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How does the registry cache type work in BuildKit?',
    options: [
      'It caches images in the local daemon',
      'It exports cache metadata as a separate OCI image to a registry, allowing cache sharing across machines',
      'It uses the registry as a proxy',
      'It compresses cached layers for storage',
    ],
    correctAnswer: 1,
    explanation: 'Registry cache stores build cache as an OCI image in a container registry. Any builder that can pull from the registry can reuse the cache, enabling cache sharing across CI runners.',
    codeSnippet: 'docker buildx build \\\n  --cache-from type=registry,ref=myregistry.io/myapp:buildcache \\\n  --cache-to type=registry,ref=myregistry.io/myapp:buildcache,mode=max \\\n  --push -t myregistry.io/myapp:latest .',
    tags: ['caching', 'registry-cache', 'sharing'],
  },
  {
    id: 'docker-cicd-l2-022',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you create a reusable workflow for Docker builds in GitHub Actions?',
    options: [
      'Copy the workflow file to each repository',
      'Define a reusable workflow with workflow_call trigger and use it from other workflows',
      'Create a GitHub Action from the workflow',
      'Use workflow templates only',
    ],
    correctAnswer: 1,
    explanation: 'Reusable workflows use the workflow_call trigger, allowing other workflows to call them with inputs and secrets, centralizing Docker build logic.',
    codeSnippet: '# .github/workflows/docker-build.yml (reusable)\non:\n  workflow_call:\n    inputs:\n      image-name:\n        required: true\n        type: string\n    secrets:\n      registry-password:\n        required: true\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: docker/build-push-action@v5\n        with:\n          tags: \${{ inputs.image-name }}',
    tags: ['github-actions', 'reusable', 'workflows'],
  },
  {
    id: 'docker-cicd-l2-023',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'What is the kubernetes driver in Docker Buildx?',
    options: [
      'A Buildx plugin for managing Kubernetes',
      'A driver that creates builder Pods in a Kubernetes cluster for distributed builds',
      'A replacement for kubectl',
      'A driver for building Kubernetes-specific images',
    ],
    correctAnswer: 1,
    explanation: 'The kubernetes driver creates BuildKit builder Pods in a Kubernetes cluster, allowing builds to be distributed across cluster nodes with configurable resources.',
    codeSnippet: 'docker buildx create --name k8s-builder \\\n  --driver kubernetes \\\n  --driver-opt replicas=3 \\\n  --driver-opt namespace=buildkit',
    tags: ['buildx', 'kubernetes', 'distributed'],
  },
  {
    id: 'docker-cicd-l2-024',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you implement a multi-stage CI/CD pipeline for Docker in GitLab?',
    options: [
      'Put everything in one stage',
      'Define separate stages for build, test, scan, and deploy with appropriate Docker commands in each',
      'Use multiple .gitlab-ci.yml files',
      'GitLab CI does not support multi-stage pipelines',
    ],
    correctAnswer: 1,
    explanation: 'Organize the pipeline into stages: build the image, run tests against it, scan for vulnerabilities, and deploy. Each stage depends on the previous one.',
    codeSnippet: 'stages:\n  - build\n  - test\n  - scan\n  - deploy\n\nbuild:\n  stage: build\n  script:\n    - docker build -t $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA .\n    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n\ntest:\n  stage: test\n  script:\n    - docker run $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA npm test\n\nscan:\n  stage: scan\n  script:\n    - trivy image $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n\ndeploy:\n  stage: deploy\n  script:\n    - docker tag $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA $CI_REGISTRY_IMAGE:latest\n    - docker push $CI_REGISTRY_IMAGE:latest\n  only:\n    - main',
    tags: ['gitlab-ci', 'multi-stage', 'pipeline'],
  },
  {
    id: 'docker-cicd-l2-025',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you configure image retention policies in a container registry?',
    options: [
      'Images are automatically cleaned up',
      'Set lifecycle policies that automatically delete images based on age, tag count, or matching rules',
      'Manually delete old images',
      'Registries do not support retention policies',
    ],
    correctAnswer: 1,
    explanation: 'Most registries support lifecycle policies that automatically delete images based on criteria like age, number of tags to retain, or tag patterns.',
    codeSnippet: '# AWS ECR lifecycle policy\n{\n  "rules": [{\n    "rulePriority": 1,\n    "description": "Keep last 10 images",\n    "selection": {\n      "tagStatus": "any",\n      "countType": "imageCountMoreThan",\n      "countNumber": 10\n    },\n    "action": {\n      "type": "expire"\n    }\n  }]\n}',
    tags: ['registry', 'retention', 'lifecycle'],
  },
  {
    id: 'docker-cicd-l2-026',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildah',
    question: 'How does Buildah handle image layers differently from Docker?',
    options: [
      'Buildah creates more layers',
      'Buildah can create images with a single layer using the --squash flag or interactive building',
      'Buildah does not use layers',
      'There is no difference in layer handling',
    ],
    correctAnswer: 1,
    explanation: 'Buildah can squash all layers into one, and when building interactively (using buildah from/run/commit), you control exactly how many layers are created.',
    codeSnippet: '# Squash all layers\nbuildah bud --squash -t myapp:latest .\n\n# Interactive: creates a single layer\ncontainer=$(buildah from ubuntu:22.04)\nbuildah run $container -- apt-get update\nbuildah run $container -- apt-get install -y nginx\nbuildah commit --squash $container nginx:custom',
    tags: ['buildah', 'layers', 'squash'],
  },
  {
    id: 'docker-cicd-l2-027',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you pass build arguments to docker/build-push-action?',
    options: [
      'Using the args input',
      'Using the build-args input with key=value pairs',
      'Using environment variables',
      'Build arguments are not supported',
    ],
    correctAnswer: 1,
    explanation: 'The build-args input accepts key=value pairs (one per line) that are passed as --build-arg flags to the Docker build.',
    codeSnippet: '- uses: docker/build-push-action@v5\n  with:\n    context: .\n    push: true\n    tags: myapp:latest\n    build-args: |\n      APP_VERSION=\${{ github.sha }}\n      BUILD_DATE=\${{ github.event.head_commit.timestamp }}\n      NODE_ENV=production',
    tags: ['github-actions', 'build-args', 'configuration'],
  },
  {
    id: 'docker-cicd-l2-028',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you inspect a multi-platform image manifest?',
    options: [
      'docker inspect',
      'docker manifest inspect to view the manifest list with platform-specific entries',
      'docker image ls --platforms',
      'docker buildx inspect',
    ],
    correctAnswer: 1,
    explanation: 'docker manifest inspect shows the manifest list with all platform-specific image references, digests, and platform information.',
    codeSnippet: 'docker manifest inspect myapp:latest\n# Shows:\n# {\n#   "manifests": [\n#     {"platform": {"architecture": "amd64", "os": "linux"}, ...},\n#     {"platform": {"architecture": "arm64", "os": "linux"}, ...}\n#   ]\n# }',
    tags: ['multi-platform', 'manifest', 'inspect'],
  },
  {
    id: 'docker-cicd-l2-029',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'What is the difference between Kaniko executor and Kaniko warmer?',
    options: [
      'They are the same tool',
      'Executor builds images; Warmer pre-caches base images to speed up subsequent builds',
      'Executor is for production; Warmer is for development',
      'Warmer converts Docker images to Kaniko format',
    ],
    correctAnswer: 1,
    explanation: 'The Kaniko warmer pre-pulls and caches base images to a persistent volume. When the executor runs, it uses these cached images instead of pulling from the registry.',
    codeSnippet: '# Warmer: pre-cache base images\n/kaniko/warmer \\\n  --cache-dir=/cache \\\n  --image=node:20 \\\n  --image=nginx:1.25\n\n# Executor: use cached images\n/kaniko/executor \\\n  --cache=true \\\n  --cache-dir=/cache \\\n  --destination=registry.example.com/myapp:latest',
    tags: ['kaniko', 'warmer', 'pre-cache'],
  },
  {
    id: 'docker-cicd-l2-030',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'What is the impact of the build context size on Docker build performance?',
    options: [
      'Build context size does not affect performance',
      'A large build context increases the time to send files to the builder and can slow down COPY operations',
      'Larger build context produces larger images',
      'Build context size only affects network usage',
    ],
    correctAnswer: 1,
    explanation: 'The build context is sent to the builder before the build starts. A large context (e.g., including node_modules or .git) significantly slows down the build initiation.',
    codeSnippet: '# Check build context size\ndocker build . 2>&1 | head -1\n# "Sending build context to Docker daemon  2.5GB"\n\n# Fix with .dockerignore\necho "node_modules\n.git\n*.log" > .dockerignore',
    tags: ['caching', 'build-context', 'performance'],
  },
  {
    id: 'docker-cicd-l2-031',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you use SSH agent forwarding during a Docker build with BuildKit?',
    options: [
      'SSH is not available during builds',
      'Using RUN --mount=type=ssh to access the host SSH agent for private repository access',
      'By copying SSH keys into the image',
      'Using the --ssh-agent flag',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit SSH mounts forward the host SSH agent into the build, allowing operations like cloning private Git repositories without exposing SSH keys in the image.',
    codeSnippet: '# Dockerfile\nRUN --mount=type=ssh \\\n    git clone git@github.com:org/private-repo.git\n\n# Build with SSH forwarding\ndocker buildx build --ssh default=$SSH_AUTH_SOCK \\\n  -t myapp:latest .',
    tags: ['buildx', 'ssh', 'private-repos'],
  },
  {
    id: 'docker-cicd-l2-032',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you use GitLab CI environments for Docker deployments?',
    options: [
      'Environments are only for Kubernetes',
      'Define environments in jobs to track deployments, enable rollbacks, and manage deployment history',
      'Environments are the same as stages',
      'Use separate .gitlab-ci.yml files per environment',
    ],
    correctAnswer: 1,
    explanation: 'GitLab environments track deployment history, provide rollback capability, and can be configured with protection rules and review apps.',
    codeSnippet: 'deploy_production:\n  stage: deploy\n  environment:\n    name: production\n    url: https://myapp.example.com\n  script:\n    - docker pull $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n    - docker service update --image $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA web\n  when: manual\n  only:\n    - main',
    tags: ['gitlab-ci', 'environments', 'deployments'],
  },
  {
    id: 'docker-cicd-l2-033',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you mirror images between registries?',
    options: [
      'Docker automatically mirrors images',
      'Use tools like skopeo, crane, or regctl to copy images between registries without pulling locally',
      'Download and re-upload images manually',
      'Registries handle mirroring internally',
    ],
    correctAnswer: 1,
    explanation: 'Tools like skopeo and crane copy images between registries server-to-server without pulling locally, preserving multi-platform manifests and signatures.',
    codeSnippet: '# Copy with skopeo (server-to-server)\nskopeo copy \\\n  docker://docker.io/library/nginx:latest \\\n  docker://myregistry.io/nginx:latest\n\n# Copy with crane\ncrane copy docker.io/library/nginx:latest myregistry.io/nginx:latest',
    tags: ['registry', 'mirroring', 'skopeo'],
  },
  {
    id: 'docker-cicd-l2-034',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you run Docker container tests in a GitHub Actions workflow?',
    options: [
      'Tests can only run outside Docker',
      'Build the image, then use docker run or docker compose to execute tests against it',
      'Use the testing-action from Docker',
      'Tests are automatically run during docker build',
    ],
    correctAnswer: 1,
    explanation: 'Build the image in an earlier step, then run test commands inside containers based on that image, or use Docker Compose for integration tests with dependencies.',
    codeSnippet: 'steps:\n  - uses: docker/build-push-action@v5\n    with:\n      load: true\n      tags: myapp:test\n  - name: Run tests\n    run: docker run --rm myapp:test npm test\n  - name: Integration tests\n    run: |\n      docker compose -f docker-compose.test.yml up -d\n      docker compose -f docker-compose.test.yml run tests\n      docker compose -f docker-compose.test.yml down',
    tags: ['github-actions', 'testing', 'containers'],
  },
  {
    id: 'docker-cicd-l2-035',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'What is the purpose of the --output flag in docker buildx build?',
    options: [
      'To set the log output format',
      'To specify how build results are exported (image, local filesystem, tar archive, or OCI layout)',
      'To define the output image name',
      'To redirect build logs to a file',
    ],
    correctAnswer: 1,
    explanation: 'The --output flag controls the build result format: type=image (push to registry), type=local (export to directory), type=tar (export as tarball), or type=oci (OCI layout).',
    codeSnippet: '# Export to local directory\ndocker buildx build --output type=local,dest=./output .\n\n# Export as tar\ndocker buildx build --output type=tar,dest=./image.tar .\n\n# Export as OCI layout\ndocker buildx build --output type=oci,dest=./oci-layout .',
    tags: ['buildx', 'output', 'export'],
  },
  {
    id: 'docker-cicd-l2-036',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you use the --mount=type=bind flag in RUN instructions for better caching?',
    options: [
      'Bind mounts are not available in Dockerfiles',
      'Mount files read-only during RUN without creating a COPY layer, avoiding cache invalidation from source changes',
      'Bind mounts replace COPY instructions',
      'They are used for persisting data between builds',
    ],
    correctAnswer: 1,
    explanation: 'Bind mounts in RUN instructions make files available during the command execution without creating a separate layer, useful for build scripts that should not trigger cache invalidation.',
    codeSnippet: '# Mount source for compilation without COPY layer\nFROM golang:1.21 AS builder\nRUN --mount=type=bind,target=/src,source=. \\\n    --mount=type=cache,target=/root/.cache/go-build \\\n    cd /src && go build -o /app .',
    tags: ['caching', 'bind-mount', 'buildkit'],
  },
  {
    id: 'docker-cicd-l2-037',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you pass Docker build secrets in GitLab CI?',
    options: [
      'Use CI variables as build arguments',
      'Use BuildKit secret mounts with CI variables piped as secrets during the build',
      'Store secrets in the Dockerfile',
      'GitLab CI does not support build secrets',
    ],
    correctAnswer: 1,
    explanation: 'Use BuildKit secret mounts to pass sensitive CI variables during the build without exposing them in image layers.',
    codeSnippet: 'build:\n  image: docker:24\n  services:\n    - docker:24-dind\n  variables:\n    DOCKER_BUILDKIT: "1"\n  script:\n    - echo "$NPM_TOKEN" > /tmp/npm_token\n    - docker build\n        --secret id=npm_token,src=/tmp/npm_token\n        -t $CI_REGISTRY_IMAGE:latest .\n    - rm /tmp/npm_token',
    tags: ['gitlab-ci', 'secrets', 'buildkit'],
  },
  {
    id: 'docker-cicd-l2-038',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you use native builders for faster multi-platform builds?',
    options: [
      'Native builders are not available in Buildx',
      'Append remote builders for different architectures to a single Buildx builder using --append',
      'Always use QEMU for all platforms',
      'Create separate builders for each platform',
    ],
    correctAnswer: 1,
    explanation: 'You can attach native builders for different architectures to a single Buildx builder. Each platform is built natively on the appropriate architecture, avoiding QEMU overhead.',
    codeSnippet: '# Create builder with local node\ndocker buildx create --name multi --use\n\n# Append remote ARM64 node\ndocker buildx create --name multi --append \\\n  --node arm-builder \\\n  ssh://user@arm-host',
    tags: ['multi-platform', 'native', 'remote-builders'],
  },
  {
    id: 'docker-cicd-l2-039',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'What is an OCI image manifest and how does it differ from a Docker manifest?',
    options: [
      'They are identical formats',
      'OCI manifest is a standardized format based on the Docker manifest; both describe image layers but OCI is the open standard',
      'OCI format is incompatible with Docker',
      'Docker manifest is newer than OCI',
    ],
    correctAnswer: 1,
    explanation: 'The OCI Image Specification standardizes the image format based on Docker\'s v2 manifest. Most registries support both, and they are largely compatible.',
    tags: ['registry', 'oci', 'manifest-format'],
  },
  {
    id: 'docker-cicd-l2-040',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildah',
    question: 'How do you push images from Buildah to a Docker registry?',
    options: [
      'buildah upload',
      'buildah push with a transport prefix specifying the destination type',
      'buildah deploy',
      'Use docker push after buildah build',
    ],
    correctAnswer: 1,
    explanation: 'Buildah push supports multiple transport formats: docker:// for registries, oci: for OCI layout, dir: for directories, and docker-archive: for tar archives.',
    codeSnippet: '# Push to registry\nbuildah push myapp:latest docker://registry.example.com/myapp:latest\n\n# Save as OCI layout\nbuildah push myapp:latest oci:/tmp/myapp-oci\n\n# Save as Docker archive\nbuildah push myapp:latest docker-archive:/tmp/myapp.tar',
    tags: ['buildah', 'push', 'transports'],
  },
  {
    id: 'docker-cicd-l2-041',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you scan Docker images for vulnerabilities in GitHub Actions?',
    options: [
      'GitHub automatically scans all Docker images',
      'Use scanning actions like aquasecurity/trivy-action or anchore/scan-action in your workflow',
      'Scanning is only available in Docker Hub',
      'Install a scanner manually in each workflow',
    ],
    correctAnswer: 1,
    explanation: 'Integrate vulnerability scanning tools (Trivy, Grype, Snyk) as GitHub Actions steps to scan built images before pushing or deploying.',
    codeSnippet: '- uses: docker/build-push-action@v5\n  with:\n    load: true\n    tags: myapp:test\n\n- uses: aquasecurity/trivy-action@master\n  with:\n    image-ref: myapp:test\n    format: table\n    exit-code: 1\n    severity: CRITICAL,HIGH',
    tags: ['github-actions', 'scanning', 'trivy'],
  },
  {
    id: 'docker-cicd-l2-042',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you configure BuildKit to use a custom registry mirror?',
    options: [
      'Only through daemon.json',
      'Using a buildkitd.toml configuration file with the builder instance',
      'Registry mirrors are not supported in BuildKit',
      'Through environment variables',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit uses its own configuration file (buildkitd.toml) for registry mirrors, separate from the Docker daemon configuration.',
    codeSnippet: '# buildkitd.toml\n[registry."docker.io"]\n  mirrors = ["mirror.example.com"]\n\n[registry."mirror.example.com"]\n  http = true\n  insecure = true\n\n# Create builder with config\ndocker buildx create --name mybuilder \\\n  --config buildkitd.toml',
    tags: ['buildx', 'registry-mirror', 'configuration'],
  },
  {
    id: 'docker-cicd-l2-043',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'What is the difference between --cache-from type=registry and --cache-from type=local?',
    options: [
      'They are the same but stored differently',
      'Registry type pulls cache from a registry image; local type reads from a filesystem directory on the build machine',
      'Local is faster but registry is more portable',
      'Registry type only works with Docker Hub',
    ],
    correctAnswer: 1,
    explanation: 'Registry cache is stored as an OCI image in a container registry, shareable across machines. Local cache is stored in a filesystem directory, only available on the same machine.',
    codeSnippet: '# Local cache\ndocker buildx build \\\n  --cache-from type=local,src=/tmp/buildcache \\\n  --cache-to type=local,dest=/tmp/buildcache \\\n  -t myapp:latest .\n\n# Registry cache\ndocker buildx build \\\n  --cache-from type=registry,ref=myapp:cache \\\n  --cache-to type=registry,ref=myapp:cache \\\n  -t myapp:latest .',
    tags: ['caching', 'registry-vs-local', 'comparison'],
  },
  {
    id: 'docker-cicd-l2-044',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'What is the purpose of the rules keyword in GitLab CI?',
    options: [
      'To define coding standards',
      'To control when jobs are added to a pipeline using conditional expressions',
      'To set build rules for Docker',
      'To define merge request rules',
    ],
    correctAnswer: 1,
    explanation: 'The rules keyword replaces only/except with more powerful conditional expressions for determining when jobs run, supporting variables, file changes, and complex logic.',
    codeSnippet: 'build_docker:\n  stage: build\n  script:\n    - docker build -t myapp .\n  rules:\n    - if: $CI_COMMIT_BRANCH == "main"\n      when: always\n    - if: $CI_PIPELINE_SOURCE == "merge_request_event"\n      when: always\n    - changes:\n        - Dockerfile\n        - src/**/*\n      when: always\n    - when: never',
    tags: ['gitlab-ci', 'rules', 'conditional'],
  },
  {
    id: 'docker-cicd-l2-045',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'How do you use Kaniko with multi-stage Dockerfiles?',
    options: [
      'Kaniko does not support multi-stage builds',
      'Kaniko fully supports multi-stage builds; use --target to build a specific stage',
      'Each stage must be built separately',
      'Multi-stage requires Docker-in-Docker',
    ],
    correctAnswer: 1,
    explanation: 'Kaniko supports all standard Dockerfile features including multi-stage builds. The --target flag can build up to a specific named stage.',
    codeSnippet: '/kaniko/executor \\\n  --dockerfile=Dockerfile \\\n  --context=/workspace \\\n  --destination=registry.example.com/myapp:latest \\\n  --target=production \\\n  --cache=true',
    tags: ['kaniko', 'multi-stage', 'target'],
  },
  {
    id: 'docker-cicd-l2-046',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you implement image signing with Docker Content Trust?',
    options: [
      'Images are automatically signed',
      'Enable DOCKER_CONTENT_TRUST=1 to sign images on push and verify on pull using Notary',
      'Use GPG signatures manually',
      'Image signing requires a third-party tool',
    ],
    correctAnswer: 1,
    explanation: 'Docker Content Trust uses Notary to sign images. When enabled, docker push signs the image and docker pull verifies signatures, ensuring image integrity.',
    codeSnippet: '# Enable content trust\nexport DOCKER_CONTENT_TRUST=1\n\n# Push signs the image automatically\ndocker push myregistry.io/myapp:v1.0.0\n\n# Pull verifies the signature\ndocker pull myregistry.io/myapp:v1.0.0\n\n# Verify trust information\ndocker trust inspect myregistry.io/myapp:v1.0.0',
    tags: ['registry', 'signing', 'dct'],
  },
  {
    id: 'docker-cicd-l2-047',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you use outputs from one GitHub Actions step in another?',
    options: [
      'Outputs are shared automatically',
      'Set outputs using $GITHUB_OUTPUT and reference them using steps.<step-id>.outputs.<name>',
      'Use environment variables only',
      'Outputs cannot be shared between steps',
    ],
    correctAnswer: 1,
    explanation: 'Steps can set outputs by writing to $GITHUB_OUTPUT, which subsequent steps reference using the steps context.',
    codeSnippet: '- id: meta\n  uses: docker/metadata-action@v5\n  with:\n    images: myapp\n\n- uses: docker/build-push-action@v5\n  with:\n    tags: \${{ steps.meta.outputs.tags }}\n    labels: \${{ steps.meta.outputs.labels }}',
    tags: ['github-actions', 'outputs', 'steps'],
  },
  {
    id: 'docker-cicd-l2-048',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'What is the BUILDPLATFORM variable in multi-platform builds?',
    options: [
      'The target platform for the final image',
      'The platform of the build host, used to run build tools natively during cross-compilation',
      'A user-defined platform variable',
      'The platform of the base image',
    ],
    correctAnswer: 1,
    explanation: 'BUILDPLATFORM represents the host platform. Using FROM --platform=$BUILDPLATFORM ensures build tools run natively on the host while cross-compiling for the target architecture.',
    codeSnippet: '# Cross-compile Go application\nFROM --platform=$BUILDPLATFORM golang:1.21 AS builder\nARG TARGETARCH\nARG TARGETOS\nWORKDIR /app\nCOPY . .\n# Build tools run natively, output targets TARGETARCH\nRUN GOOS=$TARGETOS GOARCH=$TARGETARCH go build -o /app/server\n\nFROM alpine:3.18\nCOPY --from=builder /app/server /server\nCMD ["/server"]',
    tags: ['multi-platform', 'buildplatform', 'cross-compile'],
  },
  {
    id: 'docker-cicd-l2-049',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you export build provenance and SBOM with Buildx?',
    options: [
      'Provenance and SBOM are not supported',
      'Use --provenance and --sbom flags to generate and attach build attestations to the image',
      'Use a separate scanning tool',
      'Export manually after the build',
    ],
    correctAnswer: 1,
    explanation: 'Buildx can generate SLSA provenance and SBOM attestations during the build and attach them to the image, enhancing supply chain security.',
    codeSnippet: 'docker buildx build \\\n  --provenance=true \\\n  --sbom=true \\\n  --push \\\n  -t myapp:latest .',
    tags: ['buildx', 'provenance', 'sbom'],
  },
  {
    id: 'docker-cicd-l2-050',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you optimize a Python application Dockerfile for caching?',
    options: [
      'COPY everything first then install dependencies',
      'Copy requirements.txt first, install dependencies, then copy the rest of the application',
      'Install dependencies in the base image',
      'Use a single COPY instruction for everything',
    ],
    correctAnswer: 1,
    explanation: 'Separating dependency installation from application code ensures that the pip install layer is only rebuilt when requirements.txt changes, not on every code change.',
    codeSnippet: 'FROM python:3.11-slim\nWORKDIR /app\n\n# Copy only requirements first\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\n# Then copy application code\nCOPY . .\n\nCMD ["python", "app.py"]',
    tags: ['caching', 'python', 'optimization'],
  },
  {
    id: 'docker-cicd-l2-051',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you use GitLab CI to build and push to multiple registries?',
    options: [
      'GitLab CI can only push to the GitLab registry',
      'Tag the image with multiple registry references and push each one in the job script',
      'Use multiple .gitlab-ci.yml files',
      'Create separate pipelines for each registry',
    ],
    correctAnswer: 1,
    explanation: 'Tag the built image with references for each target registry and push to each, or use parallel jobs for different registry targets.',
    codeSnippet: 'build_and_push:\n  script:\n    - docker build -t myapp:$CI_COMMIT_SHA .\n    # Push to GitLab Registry\n    - docker tag myapp:$CI_COMMIT_SHA $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n    # Push to Docker Hub\n    - docker login -u $DOCKER_USER -p $DOCKER_PASS\n    - docker tag myapp:$CI_COMMIT_SHA docker.io/myorg/myapp:$CI_COMMIT_SHA\n    - docker push docker.io/myorg/myapp:$CI_COMMIT_SHA',
    tags: ['gitlab-ci', 'multi-registry', 'push'],
  },
  {
    id: 'docker-cicd-l2-052',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'What is immutable tags and why are they important?',
    options: [
      'Tags that cannot be viewed',
      'Tags that cannot be overwritten once pushed, ensuring a specific tag always points to the same image',
      'Tags that are automatically encrypted',
      'Tags that expire after a set time',
    ],
    correctAnswer: 1,
    explanation: 'Immutable tags prevent overwriting an existing tag with a different image, ensuring deployment consistency and preventing accidental or malicious image replacement.',
    tags: ['registry', 'immutable-tags', 'security'],
  },
  {
    id: 'docker-cicd-l2-053',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildah',
    question: 'How do you use Buildah in a CI/CD pipeline without Docker?',
    options: [
      'Buildah requires Docker to be installed',
      'Install Buildah directly and use buildah bud and buildah push commands in the CI script',
      'Buildah is only for local development',
      'Use Buildah through Docker',
    ],
    correctAnswer: 1,
    explanation: 'Buildah runs independently without Docker. Install it on the CI runner and use buildah bud for building and buildah push for publishing, without any Docker daemon.',
    codeSnippet: '# CI script without Docker\n- apt-get install -y buildah\n- buildah bud -t myapp:$CI_COMMIT_SHA .\n- buildah login -u $REGISTRY_USER -p $REGISTRY_PASS registry.example.com\n- buildah push myapp:$CI_COMMIT_SHA docker://registry.example.com/myapp:$CI_COMMIT_SHA',
    tags: ['buildah', 'cicd', 'dockerless'],
  },
  {
    id: 'docker-cicd-l2-054',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you set up a self-hosted GitHub Actions runner for Docker builds?',
    options: [
      'Self-hosted runners cannot build Docker images',
      'Install Docker on the host, register it as a GitHub Actions runner, and use runs-on: self-hosted in workflows',
      'Use a special Docker runner image',
      'Self-hosted runners only work with Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Install Docker and the GitHub Actions runner on your own infrastructure. Workflows can target it with runs-on: self-hosted or custom labels for persistent caching and faster builds.',
    codeSnippet: 'jobs:\n  build:\n    runs-on: [self-hosted, linux, docker]\n    steps:\n      - uses: actions/checkout@v4\n      - uses: docker/build-push-action@v5\n        with:\n          context: .\n          push: true\n          tags: myapp:latest\n          # Local cache persists between runs\n          cache-from: type=local,src=/tmp/.buildx-cache\n          cache-to: type=local,dest=/tmp/.buildx-cache-new',
    tags: ['github-actions', 'self-hosted', 'runners'],
  },
  {
    id: 'docker-cicd-l2-055',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you test multi-platform images locally before pushing?',
    options: [
      'You cannot test multi-platform images locally',
      'Use --load for the native platform to test locally, and docker run --platform for emulated testing',
      'Push to a staging registry and pull back',
      'Multi-platform images can only be tested in CI',
    ],
    correctAnswer: 1,
    explanation: 'Build with --load for the native platform to test locally. For cross-platform testing, use docker run --platform to emulate the target architecture (requires QEMU setup).',
    codeSnippet: '# Build and load native platform\ndocker buildx build --load -t myapp:test .\ndocker run myapp:test\n\n# Test ARM image on x86 host (requires QEMU)\ndocker run --platform linux/arm64 myapp:test',
    tags: ['multi-platform', 'testing', 'local'],
  },
  {
    id: 'docker-cicd-l2-056',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you use build contexts from different sources in docker buildx?',
    options: [
      'Only local directories can be build contexts',
      'Buildx supports local directories, Git repositories, HTTP URLs, OCI images, and stdin as build contexts',
      'Only Git repositories can be build contexts',
      'Build contexts must be created before building',
    ],
    correctAnswer: 1,
    explanation: 'Buildx accepts multiple build context types: local paths, Git URLs, HTTP URLs for remote Dockerfiles, OCI image references, and piped input via stdin.',
    codeSnippet: '# Git repository context\ndocker buildx build https://github.com/org/repo.git#main\n\n# Named contexts from images\ndocker buildx build \\\n  --build-context deps=docker-image://deps:latest \\\n  -t myapp:latest .\n\n# In Dockerfile:\n# FROM deps AS external-deps',
    tags: ['buildx', 'contexts', 'sources'],
  },
  {
    id: 'docker-cicd-l2-057',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you optimize a Go application Dockerfile for caching?',
    options: [
      'COPY the entire source code first',
      'Copy go.mod and go.sum first, download dependencies, then copy source code',
      'Compile outside Docker and copy the binary',
      'Use a single stage only',
    ],
    correctAnswer: 1,
    explanation: 'Copy dependency files first to cache the go mod download step. Source code changes only invalidate the compile step, not the dependency download.',
    codeSnippet: 'FROM golang:1.21 AS builder\nWORKDIR /app\n\n# Cache dependencies\nCOPY go.mod go.sum ./\nRUN go mod download\n\n# Build application\nCOPY . .\nRUN CGO_ENABLED=0 go build -o /server .\n\n# Minimal runtime\nFROM scratch\nCOPY --from=builder /server /server\nENTRYPOINT ["/server"]',
    tags: ['caching', 'golang', 'optimization'],
  },
  {
    id: 'docker-cicd-l2-058',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you use GitLab CI to deploy Docker images to a Kubernetes cluster?',
    options: [
      'GitLab CI cannot deploy to Kubernetes',
      'Use kubectl in the CI job to update deployments with the new image tag',
      'Push to the registry and Kubernetes auto-detects changes',
      'Use docker service update in GitLab CI',
    ],
    correctAnswer: 1,
    explanation: 'After building and pushing the image, use kubectl commands in the deploy job to update the Kubernetes deployment with the new image reference.',
    codeSnippet: 'deploy_k8s:\n  stage: deploy\n  image: bitnami/kubectl:latest\n  script:\n    - kubectl config set-cluster k8s --server=$KUBE_URL --certificate-authority=$KUBE_CA\n    - kubectl config set-credentials admin --token=$KUBE_TOKEN\n    - kubectl config set-context default --cluster=k8s --user=admin\n    - kubectl config use-context default\n    - kubectl set image deployment/web web=$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA\n  only:\n    - main',
    tags: ['gitlab-ci', 'kubernetes', 'deployment'],
  },
  {
    id: 'docker-cicd-l2-059',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'What are the limitations of Kaniko compared to Docker build?',
    options: [
      'Kaniko has no limitations',
      'Kaniko does not support all Dockerfile instructions identically, has limited caching options, and cannot use Docker socket mounts',
      'Kaniko is slower than Docker build',
      'Kaniko only supports Linux images',
    ],
    correctAnswer: 1,
    explanation: 'Kaniko limitations include: some Dockerfile instructions may behave slightly differently, no Docker socket access, limited caching to registry-based only, and some build args may not work identically.',
    tags: ['kaniko', 'limitations', 'comparison'],
  },
  {
    id: 'docker-cicd-l2-060',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you configure a Docker registry to use TLS?',
    options: [
      'TLS is automatically enabled',
      'Place TLS certificate and key files and configure the registry with REGISTRY_HTTP_TLS_CERTIFICATE and REGISTRY_HTTP_TLS_KEY',
      'TLS is not supported for self-hosted registries',
      'Use a reverse proxy for TLS only',
    ],
    correctAnswer: 1,
    explanation: 'Configure the registry with TLS certificate and key using environment variables or a config.yml file to enable HTTPS for secure image transfers.',
    codeSnippet: 'docker run -d -p 5000:5000 \\\n  -v /certs:/certs \\\n  -e REGISTRY_HTTP_TLS_CERTIFICATE=/certs/domain.crt \\\n  -e REGISTRY_HTTP_TLS_KEY=/certs/domain.key \\\n  --name registry \\\n  registry:2',
    tags: ['registry', 'tls', 'security'],
  },
  {
    id: 'docker-cicd-l2-061',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you use GitHub Actions concurrency to prevent duplicate builds?',
    options: [
      'GitHub automatically prevents duplicate builds',
      'Use the concurrency keyword with a group name to cancel in-progress runs when new ones start',
      'Use locks in the workflow file',
      'Set max-parallel to 1',
    ],
    correctAnswer: 1,
    explanation: 'The concurrency keyword groups workflow runs. With cancel-in-progress: true, a new run cancels any ongoing run in the same group, preventing wasted compute.',
    codeSnippet: 'concurrency:\n  group: docker-build-\${{ github.ref }}\n  cancel-in-progress: true\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: docker/build-push-action@v5\n        with:\n          push: true\n          tags: myapp:latest',
    tags: ['github-actions', 'concurrency', 'optimization'],
  },
  {
    id: 'docker-cicd-l2-062',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'What is the purpose of the --progress flag in docker buildx build?',
    options: [
      'To show a progress bar',
      'To control the build output format: auto, plain, or tty for different levels of detail',
      'To limit build speed',
      'To enable incremental builds',
    ],
    correctAnswer: 1,
    explanation: 'The --progress flag controls build output verbosity: plain shows full output, tty shows interactive progress (default in terminals), auto selects based on the terminal.',
    codeSnippet: '# Full output (useful in CI)\ndocker buildx build --progress=plain -t myapp .\n\n# Interactive progress (default in terminal)\ndocker buildx build --progress=tty -t myapp .',
    tags: ['buildx', 'progress', 'output'],
  },
  {
    id: 'docker-cicd-l2-063',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you handle caching for monorepo Docker builds?',
    options: [
      'Use a single Dockerfile for the entire monorepo',
      'Use .dockerignore per service, copy only relevant dependency files first, and use registry cache for sharing between services',
      'Build everything together in one image',
      'Monorepos cannot use Docker caching effectively',
    ],
    correctAnswer: 1,
    explanation: 'In monorepos, use targeted COPY instructions and .dockerignore to limit build context. Copy shared dependency files first for caching, and use registry cache for cross-service sharing.',
    codeSnippet: '# packages/api/Dockerfile\nFROM node:20\nWORKDIR /app\n\n# Copy root-level dependency files\nCOPY package.json yarn.lock ./\nCOPY packages/api/package.json ./packages/api/\nCOPY packages/shared/package.json ./packages/shared/\nRUN yarn install --frozen-lockfile\n\n# Copy source\nCOPY packages/api ./packages/api\nCOPY packages/shared ./packages/shared\nRUN yarn workspace api build',
    tags: ['caching', 'monorepo', 'strategy'],
  },
  {
    id: 'docker-cicd-l2-064',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'What is the purpose of the needs keyword in GitLab CI?',
    options: [
      'To define required CI variables',
      'To create a directed acyclic graph (DAG) of job dependencies, allowing jobs to run as soon as their dependencies complete',
      'To specify runner requirements',
      'To define stage ordering',
    ],
    correctAnswer: 1,
    explanation: 'The needs keyword creates a DAG pipeline where jobs start as soon as their specific dependencies finish, rather than waiting for the entire previous stage to complete.',
    codeSnippet: 'stages:\n  - build\n  - test\n  - deploy\n\nbuild_api:\n  stage: build\n  script: docker build -t api .\n\nbuild_web:\n  stage: build\n  script: docker build -t web .\n\ntest_api:\n  stage: test\n  needs: [build_api]\n  script: docker run api npm test\n\ntest_web:\n  stage: test\n  needs: [build_web]\n  script: docker run web npm test',
    tags: ['gitlab-ci', 'needs', 'dag'],
  },
  {
    id: 'docker-cicd-l2-065',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you implement registry garbage collection?',
    options: [
      'Registries automatically garbage collect',
      'Run the garbage-collect command on the registry to remove unreferenced blobs and reclaim disk space',
      'Delete images manually',
      'Restart the registry to trigger cleanup',
    ],
    correctAnswer: 1,
    explanation: 'The registry garbage-collect command removes unreferenced image layers (blobs) from the storage backend, reclaiming disk space after tag deletions.',
    codeSnippet: '# Run garbage collection on the registry\ndocker exec registry bin/registry garbage-collect \\\n  /etc/docker/registry/config.yml\n\n# Dry run to see what would be deleted\ndocker exec registry bin/registry garbage-collect \\\n  --dry-run /etc/docker/registry/config.yml',
    tags: ['registry', 'garbage-collection', 'maintenance'],
  },
  {
    id: 'docker-cicd-l2-066',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you handle architecture-specific base images in multi-platform builds?',
    options: [
      'Always use the same base image',
      'Use official multi-platform base images that automatically resolve to the correct architecture',
      'Maintain separate Dockerfiles per architecture',
      'Manually specify the base image digest for each platform',
    ],
    correctAnswer: 1,
    explanation: 'Official Docker images are multi-platform. When building with --platform, FROM automatically resolves to the correct architecture variant of the base image.',
    codeSnippet: '# This works for all platforms automatically\nFROM node:20-slim\n# Node.js official image has variants for:\n# linux/amd64, linux/arm64, linux/arm/v7, etc.\n\nWORKDIR /app\nCOPY . .\nRUN npm install\nCMD ["node", "app.js"]',
    tags: ['multi-platform', 'base-images', 'resolution'],
  },
  {
    id: 'docker-cicd-l2-067',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildah',
    question: 'How do you use Buildah with Podman for a complete Docker-free workflow?',
    options: [
      'Buildah and Podman are incompatible',
      'Buildah builds images and Podman runs them, sharing the same image store without requiring Docker',
      'Podman replaces Buildah',
      'They require Docker to communicate',
    ],
    correctAnswer: 1,
    explanation: 'Buildah and Podman share the same image store. Buildah handles building, Podman handles running. Together they provide a complete container workflow without Docker.',
    codeSnippet: '# Build with Buildah\nbuildah bud -t myapp:latest .\n\n# Run with Podman (shares image store)\npodman run --rm myapp:latest\n\n# Push with Buildah or Podman\npodman push myapp:latest docker://registry.example.com/myapp:latest',
    tags: ['buildah', 'podman', 'docker-free'],
  },
  {
    id: 'docker-cicd-l2-068',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you use Docker Compose in GitHub Actions for integration testing?',
    options: [
      'Docker Compose is not available in GitHub Actions',
      'Use docker compose commands directly in run steps since Compose is pre-installed on runners',
      'Install Docker Compose manually',
      'Use a special Compose action',
    ],
    correctAnswer: 1,
    explanation: 'GitHub-hosted runners come with Docker Compose pre-installed, so you can use docker compose commands directly in workflow steps for integration testing.',
    codeSnippet: 'steps:\n  - uses: actions/checkout@v4\n  - name: Start services\n    run: docker compose -f docker-compose.test.yml up -d\n  - name: Wait for services\n    run: docker compose -f docker-compose.test.yml exec -T api ./wait-for-it.sh db:5432\n  - name: Run integration tests\n    run: docker compose -f docker-compose.test.yml exec -T api npm run test:integration\n  - name: Cleanup\n    if: always()\n    run: docker compose -f docker-compose.test.yml down -v',
    tags: ['github-actions', 'compose', 'integration-testing'],
  },
  {
    id: 'docker-cicd-l2-069',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you use Buildx bake for building multiple images?',
    options: [
      'Run docker buildx build multiple times',
      'Define multiple build targets in a docker-bake.hcl or docker-bake.json file and build them in parallel',
      'Use a shell script to loop over builds',
      'Bake is not a real Buildx feature',
    ],
    correctAnswer: 1,
    explanation: 'docker buildx bake reads build definitions from HCL or JSON files, enabling parallel building of multiple images with shared configurations and variables.',
    codeSnippet: '# docker-bake.hcl\ngroup "default" {\n  targets = ["api", "web"]\n}\n\ntarget "api" {\n  dockerfile = "Dockerfile.api"\n  tags = ["myapp/api:latest"]\n  cache-from = ["type=registry,ref=myapp/api:cache"]\n  cache-to = ["type=registry,ref=myapp/api:cache,mode=max"]\n}\n\ntarget "web" {\n  dockerfile = "Dockerfile.web"\n  tags = ["myapp/web:latest"]\n}\n\n# Build all\n# docker buildx bake',
    tags: ['buildx', 'bake', 'multi-image'],
  },
  {
    id: 'docker-cicd-l2-070',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'What is the GHA cache type in BuildKit and what are its limitations?',
    options: [
      'A cache type with no limitations',
      'A cache backend using GitHub Actions cache with a 10GB size limit per repository',
      'A cache type specific to Google Cloud',
      'A cache type for Git-based builds only',
    ],
    correctAnswer: 1,
    explanation: 'The GHA cache type stores BuildKit cache in GitHub Actions cache. It is limited to 10GB per repository and uses a FIFO eviction policy when the limit is reached.',
    codeSnippet: '- uses: docker/build-push-action@v5\n  with:\n    cache-from: type=gha\n    cache-to: type=gha,mode=max\n    # Note: 10GB limit per repository\n    # Oldest cache entries are evicted first',
    tags: ['caching', 'gha', 'limitations'],
  },
  {
    id: 'docker-cicd-l2-071',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you implement parallel matrix builds in GitLab CI for Docker?',
    options: [
      'GitLab CI does not support matrix builds',
      'Use the parallel:matrix keyword to generate multiple jobs from a single definition with different variables',
      'Create separate jobs manually',
      'Use external scripts for parallelization',
    ],
    correctAnswer: 1,
    explanation: 'The parallel:matrix keyword creates multiple job instances with different variable combinations, enabling parallel builds for multiple platforms or configurations.',
    codeSnippet: 'build:\n  stage: build\n  parallel:\n    matrix:\n      - PLATFORM: [linux/amd64, linux/arm64]\n        VARIANT: [slim, full]\n  script:\n    - docker buildx build\n        --platform $PLATFORM\n        -t myapp:$VARIANT-$CI_COMMIT_SHA\n        -f Dockerfile.$VARIANT\n        .',
    tags: ['gitlab-ci', 'matrix', 'parallel'],
  },
  {
    id: 'docker-cicd-l2-072',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'What is the Harbor registry and what features does it provide?',
    options: [
      'A Docker Hub alternative with identical features',
      'An open-source enterprise registry with vulnerability scanning, content signing, RBAC, and image replication',
      'A lightweight registry for development only',
      'A commercial registry from Docker Inc',
    ],
    correctAnswer: 1,
    explanation: 'Harbor is an open-source enterprise registry that adds vulnerability scanning (via Trivy), content trust, RBAC, audit logging, and multi-registry replication on top of the Docker registry.',
    tags: ['registry', 'harbor', 'enterprise'],
  },
  {
    id: 'docker-cicd-l2-073',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'How do you handle build arguments in Kaniko?',
    options: [
      'Kaniko does not support build arguments',
      'Use the --build-arg flag, same as Docker build',
      'Define them in a separate configuration file',
      'Use environment variables only',
    ],
    correctAnswer: 1,
    explanation: 'Kaniko supports the --build-arg flag with the same syntax as docker build for passing build-time variables.',
    codeSnippet: '/kaniko/executor \\\n  --dockerfile=Dockerfile \\\n  --context=/workspace \\\n  --destination=registry.example.com/myapp:latest \\\n  --build-arg APP_VERSION=1.2.3 \\\n  --build-arg NODE_ENV=production',
    tags: ['kaniko', 'build-args', 'configuration'],
  },
  {
    id: 'docker-cicd-l2-074',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you use GitHub Actions environments for deployment protection?',
    options: [
      'Environments are only for display purposes',
      'Configure environments with required reviewers, wait timers, and branch restrictions for controlled deployments',
      'Environments automatically protect all branches',
      'Use manual approval actions instead',
    ],
    correctAnswer: 1,
    explanation: 'GitHub environments support required reviewers, wait timers before deployment, and branch/tag restrictions, providing controlled deployment gates.',
    codeSnippet: 'jobs:\n  deploy:\n    runs-on: ubuntu-latest\n    environment:\n      name: production\n      url: https://myapp.example.com\n    steps:\n      - uses: docker/login-action@v3\n        with:\n          registry: \${{ secrets.REGISTRY }}\n      - run: |\n          docker pull \${{ secrets.REGISTRY }}/myapp:\${{ github.sha }}\n          # Deploy to production...',
    tags: ['github-actions', 'environments', 'deployment'],
  },
  {
    id: 'docker-cicd-l2-075',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'What is the difference between docker build and docker buildx build?',
    options: [
      'They are identical commands',
      'docker buildx build uses BuildKit engine with support for multi-platform builds, advanced caching, and additional output formats',
      'docker build is newer and replaces buildx',
      'docker buildx build is only for ARM builds',
    ],
    correctAnswer: 1,
    explanation: 'docker buildx build uses the BuildKit backend with features like multi-platform builds, cache export/import, secret mounts, and multiple output formats that docker build does not support.',
    tags: ['buildx', 'comparison', 'features'],
  },
  {
    id: 'docker-cicd-l2-076',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you handle cache invalidation for packages installed via git clone in a Dockerfile?',
    options: [
      'Git clones are always cached',
      'Use a build argument with a cache-busting value (like date or commit SHA) to invalidate the git clone layer when needed',
      'Git clone always invalidates cache',
      'Use --no-cache for git clone layers only',
    ],
    correctAnswer: 1,
    explanation: 'Since Docker caches RUN by command string only, a git clone of the same URL is always cached. Use a build ARG (like CACHEBUST) before the RUN to force cache invalidation when needed.',
    codeSnippet: 'ARG CACHEBUST=1\nRUN git clone https://github.com/org/repo.git /app\n\n# Build with cache bust\n# docker build --build-arg CACHEBUST=$(date +%s) .',
    tags: ['caching', 'git-clone', 'invalidation'],
  },
  {
    id: 'docker-cicd-l2-077',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'What is the purpose of the extends keyword in GitLab CI?',
    options: [
      'To extend the pipeline timeout',
      'To inherit and override configuration from a template job',
      'To add extra stages to the pipeline',
      'To extend the artifact retention period',
    ],
    correctAnswer: 1,
    explanation: 'The extends keyword allows jobs to inherit configuration from template jobs (prefixed with .), reducing duplication in .gitlab-ci.yml files.',
    codeSnippet: '.docker_build:\n  image: docker:24\n  services:\n    - docker:24-dind\n  before_script:\n    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY\n\nbuild_api:\n  extends: .docker_build\n  script:\n    - docker build -t $CI_REGISTRY_IMAGE/api:$CI_COMMIT_SHA -f Dockerfile.api .\n    - docker push $CI_REGISTRY_IMAGE/api:$CI_COMMIT_SHA\n\nbuild_web:\n  extends: .docker_build\n  script:\n    - docker build -t $CI_REGISTRY_IMAGE/web:$CI_COMMIT_SHA -f Dockerfile.web .\n    - docker push $CI_REGISTRY_IMAGE/web:$CI_COMMIT_SHA',
    tags: ['gitlab-ci', 'extends', 'templates'],
  },
  {
    id: 'docker-cicd-l2-078',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'What is the docker buildx imagetools command used for?',
    options: [
      'Inspecting image layers',
      'Creating and manipulating multi-platform manifest lists and inspecting images in registries',
      'Editing image metadata',
      'Converting between image formats',
    ],
    correctAnswer: 1,
    explanation: 'docker buildx imagetools creates manifest lists from existing platform-specific images and inspects remote images without pulling them.',
    codeSnippet: '# Create a multi-platform manifest from existing images\ndocker buildx imagetools create \\\n  -t myapp:latest \\\n  myapp:amd64 myapp:arm64\n\n# Inspect remote image\ndocker buildx imagetools inspect myapp:latest',
    tags: ['multi-platform', 'imagetools', 'manifest'],
  },
  {
    id: 'docker-cicd-l2-079',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'How do you implement webhook notifications from a Docker registry?',
    options: [
      'Registries do not support webhooks',
      'Configure the registry with notification endpoints that receive push, pull, and delete events',
      'Use polling to check for new images',
      'Webhooks are only available on Docker Hub',
    ],
    correctAnswer: 1,
    explanation: 'Docker registry supports webhook notifications for events like push, pull, and delete. Configure endpoints in the registry config.yml to receive event payloads.',
    codeSnippet: '# Registry config.yml\nnotifications:\n  endpoints:\n    - name: deployment-trigger\n      url: https://deploy.example.com/webhook\n      headers:\n        Authorization: [Bearer token123]\n      timeout: 5s\n      threshold: 5\n      backoff: 10s\n      events:\n        includedActions:\n          - push',
    tags: ['registry', 'webhooks', 'notifications'],
  },
  {
    id: 'docker-cicd-l2-080',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildah',
    question: 'How does Buildah handle user namespaces for improved security?',
    options: [
      'Buildah does not use user namespaces',
      'Buildah uses user namespaces to map container root to a non-root host user, preventing privilege escalation',
      'User namespaces are only for running containers',
      'Buildah requires root for all operations',
    ],
    correctAnswer: 1,
    explanation: 'Buildah leverages Linux user namespaces (userns) to run builds as non-root on the host while appearing as root inside the build context, enhancing security.',
    codeSnippet: '# Run Buildah rootlessly\nbuildah unshare bash -c \'\n  ctr=$(buildah from alpine)\n  buildah run $ctr -- apk add --no-cache nginx\n  buildah commit $ctr nginx:custom\n\'',
    tags: ['buildah', 'user-namespaces', 'security'],
  },
  {
    id: 'docker-cicd-l2-081',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitHub Actions',
    question: 'How do you handle Docker build failures gracefully in GitHub Actions?',
    options: [
      'Build failures always stop the entire workflow',
      'Use continue-on-error, if conditions, and status check functions to handle failures and run cleanup steps',
      'Failures are automatically retried',
      'Use try-catch blocks in the workflow',
    ],
    correctAnswer: 1,
    explanation: 'Use continue-on-error to prevent job failure, if: always() to run cleanup regardless of failure, and if: failure() to run recovery steps only on failure.',
    codeSnippet: 'steps:\n  - id: build\n    uses: docker/build-push-action@v5\n    with:\n      push: true\n      tags: myapp:latest\n  - name: Notify on failure\n    if: failure()\n    run: curl -X POST $SLACK_WEBHOOK -d \'{"text":"Build failed!"}\'\n  - name: Cleanup\n    if: always()\n    run: docker buildx prune -f',
    tags: ['github-actions', 'error-handling', 'cleanup'],
  },
  {
    id: 'docker-cicd-l2-082',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you inspect the BuildKit build cache?',
    options: [
      'docker cache inspect',
      'docker buildx du to show disk usage of the build cache',
      'docker system df',
      'Build cache cannot be inspected',
    ],
    correctAnswer: 1,
    explanation: 'docker buildx du shows the disk usage of the BuildKit build cache, including the size of cached layers and the total cache size.',
    codeSnippet: '# Show cache disk usage\ndocker buildx du\n\n# Verbose output\ndocker buildx du --verbose',
    tags: ['buildx', 'cache', 'inspection'],
  },
  {
    id: 'docker-cicd-l2-083',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Layer Caching',
    question: 'How do you use BuildKit heredoc syntax for better Dockerfile readability?',
    options: [
      'Heredoc syntax is not supported in Dockerfiles',
      'Use <<EOF syntax in RUN instructions to write multi-line scripts without backslash continuation',
      'Use HEREDOC instruction',
      'Write scripts in separate files and COPY them',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit supports heredoc syntax in Dockerfiles, allowing multi-line scripts and inline file creation without backslash line continuation.',
    codeSnippet: '# syntax=docker/dockerfile:1\nFROM python:3.11\n\n# Multi-line script with heredoc\nRUN <<EOF\n  apt-get update\n  apt-get install -y curl git\n  rm -rf /var/lib/apt/lists/*\nEOF\n\n# Inline file creation\nCOPY <<EOF /app/config.yaml\nserver:\n  host: 0.0.0.0\n  port: 8080\nEOF',
    tags: ['caching', 'heredoc', 'buildkit'],
  },
  {
    id: 'docker-cicd-l2-084',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'GitLab CI',
    question: 'How do you tag Docker images with both the commit SHA and branch name in GitLab CI?',
    options: [
      'You can only use one tag per image',
      'Build once and apply multiple tags using docker tag, then push each tag',
      'Use multiple Dockerfiles for different tags',
      'Tags are automatically applied by GitLab CI',
    ],
    correctAnswer: 1,
    explanation: 'Build the image once, then tag it with multiple references (commit SHA, branch name, latest) and push each tag to the registry.',
    codeSnippet: 'build:\n  script:\n    - docker build -t $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA .\n    - docker tag $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG\n    - |\n      if [ "$CI_COMMIT_BRANCH" == "main" ]; then\n        docker tag $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA $CI_REGISTRY_IMAGE:latest\n      fi\n    - docker push $CI_REGISTRY_IMAGE --all-tags',
    tags: ['gitlab-ci', 'tagging', 'multi-tag'],
  },
  {
    id: 'docker-cicd-l2-085',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Kaniko',
    question: 'How do you use Kaniko with a custom registry certificate?',
    options: [
      'Kaniko does not support custom certificates',
      'Mount the CA certificate to /kaniko/ssl/certs/ or use the --skip-tls-verify flag',
      'Add the certificate to the Dockerfile',
      'Configure TLS in the Kaniko config file',
    ],
    correctAnswer: 1,
    explanation: 'Place custom CA certificates in /kaniko/ssl/certs/ for Kaniko to trust them. For testing, --skip-tls-verify can bypass certificate validation (not for production).',
    codeSnippet: '# Mount CA cert in Kubernetes\nvolumeMounts:\n- name: ca-cert\n  mountPath: /kaniko/ssl/certs/\nvolumes:\n- name: ca-cert\n  secret:\n    secretName: registry-ca-cert\n    items:\n    - key: ca.crt\n      path: ca-certificates.crt',
    tags: ['kaniko', 'tls', 'certificates'],
  },
  {
    id: 'docker-cicd-l2-086',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Registry',
    question: 'What is Azure Container Registry (ACR)?',
    options: [
      'An Azure VM service',
      'A managed Docker registry service from Microsoft Azure with build tasks, geo-replication, and integration with Azure services',
      'A container orchestration service',
      'A CI/CD pipeline service',
    ],
    correctAnswer: 1,
    explanation: 'ACR is Microsoft Azure\'s managed container registry providing image storage, ACR Tasks for builds, geo-replication, and integration with AKS and other Azure services.',
    codeSnippet: '# Login to ACR\naz acr login --name myregistry\n\n# Push image\ndocker push myregistry.azurecr.io/myapp:latest\n\n# Build with ACR Tasks (cloud build)\naz acr build --registry myregistry --image myapp:latest .',
    tags: ['registry', 'acr', 'azure'],
  },
  {
    id: 'docker-cicd-l2-087',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Multi-platform',
    question: 'How do you verify that a multi-platform image contains all expected platforms?',
    options: [
      'Pull and run on each platform',
      'Use docker manifest inspect or docker buildx imagetools inspect to verify the platform list',
      'Check the Dockerfile',
      'Platforms cannot be verified after building',
    ],
    correctAnswer: 1,
    explanation: 'Use docker manifest inspect or docker buildx imagetools inspect to view the manifest list and verify all expected platform-specific images are present.',
    codeSnippet: '# Verify platforms\ndocker buildx imagetools inspect myapp:latest\n# Digest: sha256:abc...\n# MediaType: ...\n# Platform: linux/amd64\n# Platform: linux/arm64\n\n# Or with manifest inspect\ndocker manifest inspect myapp:latest | jq \'.manifests[].platform\'',
    tags: ['multi-platform', 'verification', 'inspect'],
  },
  {
    id: 'docker-cicd-l2-088',
    language: 'docker',
    level: 'level2',
    category: 'CI/CD',
    subcategory: 'Buildx',
    question: 'How do you use named build contexts in Buildx?',
    options: [
      'Named contexts are not supported',
      'Use --build-context flag to define additional named contexts that can be referenced in FROM instructions',
      'Name the build directory',
      'Use Docker Compose for named contexts',
    ],
    correctAnswer: 1,
    explanation: 'Named build contexts allow referencing additional directories, images, or Git repos in Dockerfile FROM instructions, enabling modular builds.',
    codeSnippet: '# Dockerfile\nFROM baseapp AS builder\n# \'baseapp\' is resolved via named context\n\n# Build with named context\ndocker buildx build \\\n  --build-context baseapp=docker-image://mybase:latest \\\n  --build-context shared=../shared-lib/ \\\n  -t myapp:latest .',
    tags: ['buildx', 'named-contexts', 'modular'],
  },
];

export default questions;
