import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'docker-dockerfile-l2-001',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Caching',
    question: 'Why is the order of COPY instructions important for build caching?',
    options: [
      'Docker processes COPY in alphabetical order',
      'Changing a file invalidates the cache for that COPY and all subsequent instructions',
      'COPY order determines file priority',
      'Only the first COPY is cached',
    ],
    correctAnswer: 1,
    explanation: 'Docker builds sequentially. If a file referenced by a COPY instruction changes, that layer and all layers after it are rebuilt. Placing frequently-changing files later maximizes cache usage.',
    codeSnippet: '# Dependency files change rarely\nCOPY package.json package-lock.json ./\nRUN npm ci\n# Source code changes often\nCOPY . .',
    tags: ['caching', 'copy', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-002',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Caching',
    question: 'How does Docker determine if a RUN instruction cache is valid?',
    options: [
      'By checking if the command output would be the same',
      'By comparing the instruction string exactly; the same string reuses the cache',
      'By running the command and comparing results',
      'By checking the timestamp of the previous build',
    ],
    correctAnswer: 1,
    explanation: 'For RUN instructions, Docker only compares the command string. If the string is identical and all previous layers are cached, the cache is used. Docker does not check if external resources changed.',
    tags: ['caching', 'run', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-003',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Caching',
    question: 'How does Docker determine if a COPY instruction cache is valid?',
    options: [
      'By checking file names only',
      'By checking the checksum of each file in the context being copied',
      'By checking file timestamps',
      'By comparing file sizes only',
    ],
    correctAnswer: 1,
    explanation: 'For COPY and ADD, Docker computes a checksum of the file contents being copied. If the checksums match the previous build, the cached layer is reused.',
    tags: ['caching', 'copy', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-004',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: '.dockerignore',
    question: 'What is the syntax for the `.dockerignore` file?',
    options: [
      'Regular expression patterns',
      'Glob patterns similar to .gitignore with # for comments',
      'JSON array of file paths',
      'YAML list of exclusions',
    ],
    correctAnswer: 1,
    explanation: 'The `.dockerignore` file uses glob patterns similar to `.gitignore`. Lines starting with `#` are comments, `*` matches any sequence of characters, and `!` creates exceptions.',
    codeSnippet: '# .dockerignore\n*.md\n!README.md\n.git\nnode_modules\n**/*.test.js\n.env*',
    tags: ['dockerignore', 'patterns', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-005',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: '.dockerignore',
    question: 'Where should the `.dockerignore` file be placed?',
    options: [
      'In the home directory',
      'In the root of the build context (same directory as the Dockerfile)',
      'In the /etc/docker/ directory',
      'In any subdirectory of the project',
    ],
    correctAnswer: 1,
    explanation: 'The `.dockerignore` file should be in the root of the build context. Docker reads it before sending the context to the daemon, excluding matching files.',
    tags: ['dockerignore', 'location', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-006',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Environment Variables',
    question: 'What is the difference between `ENV` and `ARG` visibility?',
    options: [
      'Both are visible at build and run time',
      'ENV persists in the image and container; ARG is only available during build',
      'ARG persists in the image; ENV is only available during build',
      'Neither is visible after the build',
    ],
    correctAnswer: 1,
    explanation: 'ENV values are baked into the image and available as environment variables in running containers. ARG values exist only during the build process and are not present in the final image.',
    codeSnippet: 'ARG BUILD_DATE  # only during build\nENV APP_ENV=production  # persists in container',
    tags: ['env', 'arg', 'visibility'],
  },
  {
    id: 'docker-dockerfile-l2-007',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Environment Variables',
    question: 'How do you set environment variables from a file during the build?',
    options: [
      'ENV --file .env',
      'You cannot; ENV values must be inline. Use ARG + --build-arg or copy and source the file',
      'LOAD .env',
      'IMPORT .env',
    ],
    correctAnswer: 1,
    explanation: 'There is no direct way to load an .env file during build. You can use ARG with --build-arg, or COPY the file and source it in a RUN instruction.',
    tags: ['env', 'env-file', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-008',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'COPY Advanced',
    question: 'What does `COPY --chmod=755 script.sh /app/` do?',
    options: [
      'Changes permissions of the source file on the host',
      'Copies the file and sets its permissions to 755 in the image',
      'Creates a read-only copy',
      'Sets directory permissions for /app/',
    ],
    correctAnswer: 1,
    explanation: 'The `--chmod` flag on COPY sets the file permissions in the image without needing a separate `RUN chmod` command. This requires BuildKit.',
    codeSnippet: 'COPY --chmod=755 entrypoint.sh /app/entrypoint.sh',
    tags: ['copy', 'chmod', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-009',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'COPY Advanced',
    question: 'How do you copy files from a specific directory while preserving the directory structure?',
    options: [
      'COPY src/app /dest/app',
      'COPY src/ /dest/',
      'COPY --recursive src /dest',
      'COPY --preserve src /dest',
    ],
    correctAnswer: 0,
    explanation: 'To preserve directory structure, specify the exact source and destination paths. `COPY src/app /dest/app` copies the app directory maintaining its structure.',
    codeSnippet: 'COPY src/app /dest/app',
    tags: ['copy', 'directories', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-010',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Basics',
    question: 'What is the main benefit of multi-stage builds?',
    options: [
      'Faster container startup time',
      'Smaller final images by excluding build tools and intermediate files',
      'Better networking between containers',
      'Improved container security policies',
    ],
    correctAnswer: 1,
    explanation: 'Multi-stage builds let you use large build environments (compilers, tools) in early stages and copy only the necessary artifacts to a minimal final image.',
    codeSnippet: 'FROM node:20 AS builder\nRUN npm run build\n\nFROM nginx:alpine\nCOPY --from=builder /app/dist /usr/share/nginx/html',
    tags: ['multi-stage', 'optimization', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-011',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Basics',
    question: 'How many stages can a multi-stage Dockerfile have?',
    options: [
      'Maximum 2 stages',
      'Maximum 5 stages',
      'No specific limit on the number of stages',
      'Maximum 10 stages',
    ],
    correctAnswer: 2,
    explanation: 'There is no specific limit on the number of stages in a multi-stage build. You can have as many stages as needed, each starting with a FROM instruction.',
    tags: ['multi-stage', 'stages', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-012',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Best Practices',
    question: 'Why should `apt-get update` and `apt-get install` be in the same RUN instruction?',
    options: [
      'They are faster when combined',
      'If separated, the cached update layer may become stale while the install layer expects fresh package lists',
      'Docker requires them together',
      'To save memory during build',
    ],
    correctAnswer: 1,
    explanation: 'If `apt-get update` is in a separate RUN, Docker may use a cached (stale) package list layer when the install command changes. Combining them ensures the package list is always fresh.',
    codeSnippet: '# Bad: apt-get update may be cached and stale\nRUN apt-get update\nRUN apt-get install -y curl\n\n# Good: always fresh\nRUN apt-get update && apt-get install -y curl',
    tags: ['run', 'apt-get', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-013',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Best Practices',
    question: 'Why should packages be sorted alphabetically in multi-line `apt-get install` commands?',
    options: [
      'apt-get requires alphabetical order',
      'It makes it easier to spot duplicates and manage additions in code review',
      'Alphabetical order installs packages faster',
      'It prevents dependency conflicts',
    ],
    correctAnswer: 1,
    explanation: 'Sorting packages alphabetically makes the list easier to maintain, helps avoid duplicate entries, and makes changes clearer in version control diffs.',
    codeSnippet: 'RUN apt-get update && apt-get install -y \\\n    curl \\\n    git \\\n    vim \\\n    wget \\\n && rm -rf /var/lib/apt/lists/*',
    tags: ['run', 'apt-get', 'readability'],
  },
  {
    id: 'docker-dockerfile-l2-014',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT Patterns',
    question: 'What is the entrypoint wrapper script pattern?',
    options: [
      'A Python script that replaces ENTRYPOINT',
      'A shell script as ENTRYPOINT that performs setup before exec-ing the main process',
      'A Dockerfile template system',
      'A Docker Compose override pattern',
    ],
    correctAnswer: 1,
    explanation: 'An entrypoint wrapper script handles initialization (environment setup, config generation, waiting for dependencies) before using `exec` to replace itself with the main application process.',
    codeSnippet: '#!/bin/sh\n# entrypoint.sh\nset -e\n\n# Initialization\nenvsubst < /etc/nginx/template.conf > /etc/nginx/nginx.conf\n\n# Exec the main process\nexec "$@"',
    tags: ['entrypoint', 'wrapper', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l2-015',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT Patterns',
    question: 'Why is `exec "$@"` important in an entrypoint wrapper script?',
    options: [
      'It exits the script',
      'It replaces the shell with the main process so it becomes PID 1 and receives signals correctly',
      'It executes additional scripts',
      'It enables error handling',
    ],
    correctAnswer: 1,
    explanation: '`exec` replaces the current shell process with the specified command. This makes the main application PID 1, ensuring it receives SIGTERM for graceful shutdown instead of the shell.',
    codeSnippet: 'ENTRYPOINT ["/entrypoint.sh"]\nCMD ["nginx", "-g", "daemon off;"]',
    tags: ['entrypoint', 'exec', 'signals'],
  },
  {
    id: 'docker-dockerfile-l2-016',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK',
    question: 'What are the HEALTHCHECK configuration options?',
    options: [
      'Only the test command',
      '--interval, --timeout, --start-period, --retries, and the test command',
      '--interval and --retries only',
      '--timeout and --start-period only',
    ],
    correctAnswer: 1,
    explanation: 'HEALTHCHECK supports --interval (check frequency), --timeout (max check duration), --start-period (grace period for startup), --retries (consecutive failures before unhealthy), and the CMD.',
    codeSnippet: 'HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \\\n  CMD curl -f http://localhost:8080/health || exit 1',
    tags: ['healthcheck', 'options', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-017',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK',
    question: 'What exit codes does a HEALTHCHECK command use?',
    options: [
      '0 for unhealthy, 1 for healthy',
      '0 for healthy, 1 for unhealthy',
      'Any non-zero for healthy',
      'HTTP status codes (200 for healthy)',
    ],
    correctAnswer: 1,
    explanation: 'A HEALTHCHECK command should exit with 0 for healthy and 1 for unhealthy. Exit code 2 is reserved for future use.',
    codeSnippet: 'HEALTHCHECK CMD curl -f http://localhost/ || exit 1',
    tags: ['healthcheck', 'exit-codes', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-018',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Image Size',
    question: 'Which approach produces the smallest Node.js image?',
    options: [
      'FROM node:20',
      'FROM node:20-slim',
      'FROM node:20-alpine',
      'FROM node:20-buster',
    ],
    correctAnswer: 2,
    explanation: 'Alpine-based images are the smallest: `node:20-alpine` (~50MB) vs `node:20-slim` (~200MB) vs `node:20` (~350MB). Alpine uses musl libc instead of glibc.',
    tags: ['image-size', 'alpine', 'node'],
  },
  {
    id: 'docker-dockerfile-l2-019',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Image Size',
    question: 'What is the trade-off of using Alpine-based images?',
    options: [
      'They are slower at runtime',
      'They use musl libc which may cause compatibility issues with some native packages',
      'They do not support networking',
      'They cannot run as non-root',
    ],
    correctAnswer: 1,
    explanation: 'Alpine uses musl libc instead of glibc. Some applications and native dependencies may not work correctly or may need recompilation. Python packages with C extensions are a common issue.',
    tags: ['alpine', 'musl', 'compatibility'],
  },
  {
    id: 'docker-dockerfile-l2-020',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Best Practices',
    question: 'How do you install Python packages efficiently in a Dockerfile?',
    options: [
      'RUN pip install flask django requests',
      'COPY requirements.txt . then RUN pip install --no-cache-dir -r requirements.txt',
      'RUN python -m install requirements',
      'ADD pip-install.sh && RUN sh pip-install.sh',
    ],
    correctAnswer: 1,
    explanation: 'Copy requirements.txt first (for caching), then install with `--no-cache-dir` to avoid caching pip downloads in the image layer.',
    codeSnippet: 'COPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt',
    tags: ['python', 'pip', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-021',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'USER Best Practices',
    question: 'What is the recommended pattern for creating a non-root user on Alpine?',
    options: [
      'USER nobody',
      'RUN addgroup -S app && adduser -S -G app app then USER app',
      'RUN useradd app',
      'ENV USER=app',
    ],
    correctAnswer: 1,
    explanation: 'On Alpine, use `addgroup -S` (system group) and `adduser -S -G` (system user in group) to create a non-root user. Then switch with the USER instruction.',
    codeSnippet: 'RUN addgroup -S app && adduser -S -G app app\nUSER app',
    tags: ['user', 'alpine', 'security'],
  },
  {
    id: 'docker-dockerfile-l2-022',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ENV Best Practices',
    question: 'Why might you use ENV PATH to extend the PATH variable?',
    options: [
      'To make the Dockerfile shorter',
      'To make installed binaries available without specifying their full path',
      'To speed up container startup',
      'To enable networking',
    ],
    correctAnswer: 1,
    explanation: 'Extending PATH with ENV ensures that binaries installed in non-standard locations (like `/app/bin` or virtual environments) are accessible without the full path.',
    codeSnippet: 'ENV PATH="/app/venv/bin:$PATH"',
    tags: ['env', 'path', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-023',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'LABEL Best Practices',
    question: 'What is the OCI image annotation standard for labels?',
    options: [
      'Using docker.* prefix',
      'Using org.opencontainers.image.* prefix',
      'Using com.docker.* prefix',
      'Using oci.image.* prefix',
    ],
    correctAnswer: 1,
    explanation: 'The OCI image specification defines standard annotation keys with the `org.opencontainers.image.` prefix for common metadata like title, description, version, and authors.',
    codeSnippet: 'LABEL org.opencontainers.image.title="My App" \\\n      org.opencontainers.image.version="1.0" \\\n      org.opencontainers.image.authors="team@example.com"',
    tags: ['label', 'oci', 'standards'],
  },
  {
    id: 'docker-dockerfile-l2-024',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'What is the builder pattern in multi-stage builds?',
    options: [
      'Using a builder tool instead of Docker',
      'Using a separate stage to compile/build and copying only the output to the final stage',
      'Building the Dockerfile with a builder script',
      'Using docker compose build',
    ],
    correctAnswer: 1,
    explanation: 'The builder pattern uses one stage (the "builder") with all build tools to compile the application, then copies only the compiled artifacts to a minimal final stage.',
    codeSnippet: 'FROM golang:1.22 AS builder\nWORKDIR /app\nCOPY . .\nRUN CGO_ENABLED=0 go build -o app\n\nFROM alpine:3.19\nCOPY --from=builder /app/app /usr/local/bin/\nCMD ["app"]',
    tags: ['multi-stage', 'builder-pattern', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-025',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'How do you create a testing stage in a multi-stage build?',
    options: [
      'Add tests to the final stage',
      'Create a separate stage that runs tests and only proceed to the final stage if tests pass',
      'Use docker compose test',
      'Add a TEST instruction',
    ],
    correctAnswer: 1,
    explanation: 'A test stage builds on the builder stage, runs tests, and if they fail, the build fails. The final production stage does not include test code or dependencies.',
    codeSnippet: 'FROM node:20 AS builder\nCOPY . .\nRUN npm ci && npm run build\n\nFROM builder AS test\nRUN npm test\n\nFROM node:20-alpine AS production\nCOPY --from=builder /app/dist ./dist\nCMD ["node", "dist/server.js"]',
    tags: ['multi-stage', 'testing', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-026',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ARG Advanced',
    question: 'Which predefined ARG values are automatically available without declaration?',
    options: [
      'HOSTNAME and IP_ADDRESS',
      'HTTP_PROXY, HTTPS_PROXY, FTP_PROXY, NO_PROXY and their lowercase variants',
      'BUILD_DATE and BUILD_VERSION',
      'USER and GROUP',
    ],
    correctAnswer: 1,
    explanation: 'Docker has predefined proxy ARGs (HTTP_PROXY, HTTPS_PROXY, etc.) that are automatically available in the build without needing an ARG instruction. They are excluded from image history.',
    codeSnippet: '# No ARG declaration needed for proxies\n# docker build --build-arg HTTP_PROXY=http://proxy:3128 .',
    tags: ['arg', 'proxy', 'predefined'],
  },
  {
    id: 'docker-dockerfile-l2-027',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'VOLUME Best Practices',
    question: 'Why should you be careful with VOLUME in Dockerfiles?',
    options: [
      'Volumes slow down the container',
      'VOLUME creates anonymous volumes that may cause confusion, and changes after VOLUME to that path are discarded during build',
      'Volumes are not supported on all platforms',
      'VOLUME instruction is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'After a VOLUME instruction, any changes to that directory in subsequent Dockerfile instructions are discarded. Also, VOLUME creates anonymous volumes that are hard to manage.',
    codeSnippet: 'VOLUME /data\n# WARNING: This RUN change to /data will be LOST\nRUN echo "test" > /data/file.txt',
    tags: ['volume', 'gotchas', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-028',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'WORKDIR Best Practices',
    question: 'Why should you use absolute paths in WORKDIR?',
    options: [
      'Relative paths are not supported',
      'Absolute paths are clearer and avoid confusion about the current directory',
      'Relative paths are slower',
      'Docker Desktop requires absolute paths',
    ],
    correctAnswer: 1,
    explanation: 'While relative WORKDIR paths are supported (relative to the previous WORKDIR), using absolute paths makes the Dockerfile more readable and less error-prone.',
    codeSnippet: '# Clear and explicit\nWORKDIR /app\n\n# Confusing: relative to what?\n# WORKDIR app',
    tags: ['workdir', 'absolute-paths', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-029',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Signals',
    question: 'Why does shell form prevent proper signal handling?',
    options: [
      'Shell form does not support signals',
      'Shell form wraps the command in /bin/sh -c, making sh PID 1 instead of the application',
      'Shell form runs in the background',
      'Shell form disables the init system',
    ],
    correctAnswer: 1,
    explanation: 'Shell form runs `CMD command` as `/bin/sh -c command`. The shell becomes PID 1 and may not forward SIGTERM to the child process, preventing graceful shutdown.',
    codeSnippet: '# Shell form: sh is PID 1, node won\'t get SIGTERM\nCMD node server.js\n\n# Exec form: node is PID 1, gets SIGTERM\nCMD ["node", "server.js"]',
    tags: ['signals', 'shell-form', 'pid1'],
  },
  {
    id: 'docker-dockerfile-l2-030',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Optimization',
    question: 'How do you check the size of each layer in a Docker image?',
    options: [
      'docker size <image>',
      'docker history <image>',
      'docker layers <image>',
      'docker info <image>',
    ],
    correctAnswer: 1,
    explanation: '`docker history` shows each layer with its size, creation command, and timestamp. This helps identify which instructions contribute most to image size.',
    codeSnippet: 'docker history myapp:latest',
    tags: ['layers', 'size', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-031',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ADD vs COPY',
    question: 'When is ADD preferable over COPY?',
    options: [
      'Always, ADD is the modern replacement for COPY',
      'When you need automatic tar extraction or URL download',
      'When copying large files',
      'When copying from a different build stage',
    ],
    correctAnswer: 1,
    explanation: 'ADD is preferable only when you need its special features: automatic extraction of local tar archives or downloading files from URLs. For all other cases, COPY is recommended.',
    codeSnippet: '# ADD for tar extraction\nADD rootfs.tar.gz /\n\n# COPY for everything else\nCOPY config.json /app/',
    tags: ['add', 'copy', 'comparison'],
  },
  {
    id: 'docker-dockerfile-l2-032',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Advanced',
    question: 'What is the purpose of `set -e` in a RUN shell script?',
    options: [
      'Enables environment variables',
      'Causes the script to exit immediately if any command fails',
      'Enables echo mode for debugging',
      'Sets the encoding to UTF-8',
    ],
    correctAnswer: 1,
    explanation: '`set -e` (errexit) causes the shell to exit immediately if any command returns a non-zero exit code, ensuring build failures are caught early.',
    codeSnippet: 'RUN set -e; \\\n    apt-get update; \\\n    apt-get install -y curl; \\\n    curl -o app https://example.com/app; \\\n    chmod +x app',
    tags: ['run', 'shell', 'error-handling'],
  },
  {
    id: 'docker-dockerfile-l2-033',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Advanced',
    question: 'What does `set -x` do in a RUN instruction?',
    options: [
      'Enables execute permissions',
      'Prints each command before execution for debugging',
      'Exits on undefined variables',
      'Enables XML output',
    ],
    correctAnswer: 1,
    explanation: '`set -x` (xtrace) prints each command to stderr before executing it. This is useful for debugging build failures as you can see exactly which command ran.',
    codeSnippet: 'RUN set -ex; \\\n    echo "Building..."; \\\n    make install',
    tags: ['run', 'debugging', 'shell'],
  },
  {
    id: 'docker-dockerfile-l2-034',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'COPY Advanced',
    question: 'How do you exclude certain files when using COPY with a wildcard?',
    options: [
      'COPY --exclude=*.test.js . /app/',
      'Use a .dockerignore file to exclude files from the build context',
      'COPY --filter=!*.test.js . /app/',
      'You cannot; COPY always copies all matching files',
    ],
    correctAnswer: 1,
    explanation: 'COPY does not have an exclude flag. Use `.dockerignore` to exclude files from the build context, or use specific COPY instructions instead of wildcards.',
    codeSnippet: '# .dockerignore\n**/*.test.js\n**/*.spec.js\n__tests__/',
    tags: ['copy', 'exclude', 'dockerignore'],
  },
  {
    id: 'docker-dockerfile-l2-035',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ENV Best Practices',
    question: 'Why should you avoid using ENV for secrets?',
    options: [
      'ENV does not support special characters in values',
      'ENV values are visible in the image metadata and can be read by anyone with access to the image',
      'ENV values are logged to Docker Hub',
      'ENV values expire after 24 hours',
    ],
    correctAnswer: 1,
    explanation: 'ENV values are stored in the image metadata and visible through `docker inspect`. Anyone with access to the image can extract them. Use runtime secrets or build secrets instead.',
    codeSnippet: '# BAD: visible in image metadata\nENV DATABASE_PASSWORD=secret123\n\n# GOOD: passed at runtime\n# docker run -e DATABASE_PASSWORD=secret123 myapp',
    tags: ['env', 'security', 'secrets'],
  },
  {
    id: 'docker-dockerfile-l2-036',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ONBUILD',
    question: 'When do ONBUILD triggers execute?',
    options: [
      'When the image is pulled from a registry',
      'When a downstream image uses this image as a base in its FROM instruction',
      'When the container starts',
      'Immediately after the ONBUILD instruction in the build',
    ],
    correctAnswer: 1,
    explanation: 'ONBUILD instructions are stored in the image metadata and executed when another Dockerfile uses this image as its base (in FROM). They run after FROM and before the next instruction.',
    codeSnippet: '# Base image Dockerfile\nFROM node:20\nONBUILD COPY package*.json ./\nONBUILD RUN npm ci\nONBUILD COPY . .',
    tags: ['onbuild', 'triggers', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-037',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Best Practices',
    question: 'How do you install packages efficiently on Alpine Linux?',
    options: [
      'RUN apt-get install curl',
      'RUN apk add --no-cache curl',
      'RUN yum install curl',
      'RUN dnf install curl',
    ],
    correctAnswer: 1,
    explanation: 'Alpine uses `apk` package manager. The `--no-cache` flag avoids storing the package index locally, keeping the image smaller.',
    codeSnippet: 'RUN apk add --no-cache \\\n    curl \\\n    git \\\n    openssh',
    tags: ['alpine', 'apk', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-038',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you use `npm ci` instead of `npm install` in Dockerfiles?',
    options: [
      'npm ci is newer',
      'npm ci installs from the lockfile exactly, is faster, and deletes node_modules first for clean installs',
      'npm ci supports Docker caching',
      'npm install is deprecated',
    ],
    correctAnswer: 1,
    explanation: '`npm ci` is designed for CI environments: it installs exact versions from package-lock.json, removes existing node_modules first, and fails if the lockfile is out of sync.',
    codeSnippet: 'COPY package.json package-lock.json ./\nRUN npm ci --production',
    tags: ['npm', 'ci', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-039',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'EXPOSE Best Practices',
    question: 'Should you use EXPOSE 443 if your app handles TLS termination?',
    options: [
      'Always expose both 80 and 443',
      'Yes, EXPOSE 443 documents that the container expects HTTPS traffic on that port',
      'No, TLS should always be handled by a reverse proxy',
      'Use EXPOSE 443/ssl',
    ],
    correctAnswer: 1,
    explanation: 'EXPOSE 443 documents that the container handles HTTPS traffic. Whether TLS is terminated at the container or a reverse proxy depends on your architecture.',
    codeSnippet: 'EXPOSE 80\nEXPOSE 443',
    tags: ['expose', 'tls', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-040',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Optimization',
    question: 'What tool can you use to analyze Docker image layers and find waste?',
    options: [
      'docker analyze',
      'dive (third-party tool)',
      'docker optimize',
      'docker layer-check',
    ],
    correctAnswer: 1,
    explanation: '`dive` is a popular open-source tool that provides a visual interface to explore each layer of a Docker image, showing file changes and wasted space.',
    codeSnippet: 'dive myapp:latest',
    tags: ['dive', 'optimization', 'tools'],
  },
  {
    id: 'docker-dockerfile-l2-041',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'How do you use multi-stage builds for a React application?',
    options: [
      'Build and serve in the same stage',
      'Build stage with Node.js to create static files, then serve stage with nginx',
      'Use a single FROM with both Node.js and nginx',
      'Build on the host and copy to the image',
    ],
    correctAnswer: 1,
    explanation: 'Use Node.js in a build stage to install dependencies and build the React app. Then copy the static build output to an nginx image for serving.',
    codeSnippet: 'FROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM nginx:alpine\nCOPY --from=builder /app/build /usr/share/nginx/html',
    tags: ['multi-stage', 'react', 'nginx'],
  },
  {
    id: 'docker-dockerfile-l2-042',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ARG Scope',
    question: 'What is the scope of an ARG declared before FROM?',
    options: [
      'Available throughout the entire Dockerfile',
      'Only available in the FROM instruction itself',
      'Available in all stages',
      'Not available anywhere',
    ],
    correctAnswer: 1,
    explanation: 'An ARG declared before FROM is only available in the FROM instruction. To use it in subsequent instructions, redeclare it after FROM (the value is inherited from the pre-FROM declaration).',
    codeSnippet: 'ARG BASE_TAG=3.19\nFROM alpine:$BASE_TAG\n# ARG BASE_TAG is NOT available here\nARG BASE_TAG\n# Now it is available again',
    tags: ['arg', 'scope', 'from'],
  },
  {
    id: 'docker-dockerfile-l2-043',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'Why should you not run applications as root in containers?',
    options: [
      'Root processes use more memory',
      'If the container is compromised, the attacker has root access which could lead to host compromise',
      'Root cannot access certain container features',
      'Docker does not support root users',
    ],
    correctAnswer: 1,
    explanation: 'Running as root inside a container means a container escape vulnerability could give the attacker root access on the host. Running as non-root significantly limits the blast radius.',
    codeSnippet: 'FROM node:20-alpine\nRUN addgroup -S app && adduser -S -G app app\nWORKDIR /app\nCOPY --chown=app:app . .\nUSER app\nCMD ["node", "server.js"]',
    tags: ['security', 'root', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-044',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'COPY Advanced',
    question: 'What does `COPY --link` do?',
    options: [
      'Creates a symbolic link instead of copying',
      'Creates independent layers that do not depend on previous layers, improving cache performance',
      'Links the COPY to a specific build stage',
      'Copies using hard links for speed',
    ],
    correctAnswer: 1,
    explanation: 'COPY --link creates layers independently of previous layers, allowing Docker to reuse these layers even when previous layers change. This can significantly improve rebuild times.',
    codeSnippet: 'COPY --link package.json /app/\nCOPY --link . /app/',
    tags: ['copy', 'link', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-045',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the best way to handle timezone configuration in a Dockerfile?',
    options: [
      'RUN date --set-timezone',
      'Set the TZ environment variable and install timezone data',
      'Use a timezone-specific base image',
      'Configure it at runtime only',
    ],
    correctAnswer: 1,
    explanation: 'Set the TZ environment variable and ensure timezone data is installed. On Alpine, install `tzdata`. On Debian, configure with `ln -sf`.',
    codeSnippet: 'ENV TZ=Europe/London\nRUN apk add --no-cache tzdata',
    tags: ['timezone', 'env', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-046',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Advanced',
    question: 'What does `--no-install-recommends` do with apt-get?',
    options: [
      'Installs only packages from recommended repositories',
      'Prevents installation of recommended but not required dependency packages',
      'Disables installation recommendations output',
      'Skips recommended version updates',
    ],
    correctAnswer: 1,
    explanation: 'By default, apt-get installs recommended packages that are not strictly required. `--no-install-recommends` skips these, reducing image size.',
    codeSnippet: 'RUN apt-get update && \\\n    apt-get install -y --no-install-recommends curl ca-certificates && \\\n    rm -rf /var/lib/apt/lists/*',
    tags: ['apt-get', 'optimization', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-047',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK Patterns',
    question: 'How do you write a HEALTHCHECK for a web application?',
    options: [
      'HEALTHCHECK ping localhost',
      'HEALTHCHECK CMD curl -f http://localhost:8080/health || exit 1',
      'HEALTHCHECK HTTP GET /health',
      'HEALTHCHECK RUN wget localhost',
    ],
    correctAnswer: 1,
    explanation: 'A common pattern is using curl or wget to hit a health endpoint. The `|| exit 1` ensures a non-zero exit code if the request fails.',
    codeSnippet: 'HEALTHCHECK --interval=30s --timeout=5s --retries=3 \\\n  CMD curl -f http://localhost:8080/health || exit 1',
    tags: ['healthcheck', 'web', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l2-048',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Build Context',
    question: 'Why is a large build context a problem?',
    options: [
      'Docker rejects contexts over 1GB',
      'The entire context is sent to the Docker daemon, slowing down builds',
      'Large contexts cause permission errors',
      'Large contexts prevent caching',
    ],
    correctAnswer: 1,
    explanation: 'Before building, Docker sends the entire build context to the daemon. A large context (e.g., including node_modules, .git) significantly slows down the build start.',
    tags: ['build-context', 'performance', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-049',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Advanced',
    question: 'How do you verify downloaded files in a Dockerfile?',
    options: [
      'Docker automatically verifies downloads',
      'Download the file and verify its checksum with sha256sum',
      'Use COPY instead of downloading',
      'Check the file size after download',
    ],
    correctAnswer: 1,
    explanation: 'Always verify downloaded files by checking their SHA256 checksum against a known good value. This prevents supply chain attacks with tampered files.',
    codeSnippet: 'RUN curl -fsSL https://example.com/app.tar.gz -o app.tar.gz && \\\n    echo "abc123...expected_sha256  app.tar.gz" | sha256sum -c - && \\\n    tar xzf app.tar.gz && \\\n    rm app.tar.gz',
    tags: ['security', 'verification', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-050',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'How do you share files between non-adjacent stages?',
    options: [
      'Files automatically flow between all stages',
      'Use COPY --from=<stage_name> to copy files from any named stage',
      'Use shared volumes between stages',
      'Use environment variables to pass file paths',
    ],
    correctAnswer: 1,
    explanation: 'Use `COPY --from=<stage_name>` to copy files from any named stage. Stages do not share filesystems; you must explicitly copy what you need.',
    codeSnippet: 'FROM node:20 AS deps\nCOPY package*.json ./\nRUN npm ci\n\nFROM node:20 AS builder\nCOPY --from=deps /node_modules ./node_modules\nCOPY . .\nRUN npm run build\n\nFROM nginx:alpine\nCOPY --from=builder /app/dist /usr/share/nginx/html',
    tags: ['multi-stage', 'copy-from', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-051',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: '.dockerignore',
    question: 'What does `**/node_modules` exclude in .dockerignore?',
    options: [
      'Only the root node_modules directory',
      'node_modules directories at any depth in the build context',
      'All JavaScript files',
      'The node binary',
    ],
    correctAnswer: 1,
    explanation: 'The `**` pattern matches any number of directories. `**/node_modules` excludes all `node_modules` directories regardless of their depth in the directory tree.',
    codeSnippet: '# .dockerignore\n**/node_modules\n**/.git\n**/dist',
    tags: ['dockerignore', 'glob', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-052',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Debugging',
    question: 'How do you debug a failing RUN instruction in a Dockerfile?',
    options: [
      'Use docker debug command',
      'Build up to the failing layer using --target and inspect with an interactive shell',
      'Docker automatically shows the error',
      'Use docker logs on the build',
    ],
    correctAnswer: 1,
    explanation: 'You can add a named stage before the failing instruction, build with `--target` to stop there, then run an interactive shell to debug.',
    codeSnippet: '# Add temporary debug stage\nFROM node:20 AS debug\nCOPY . .\n# Build to this stage:\n# docker build --target debug -t debug .\n# docker run -it debug sh',
    tags: ['debugging', 'build', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-053',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the recommended way to handle application configuration in Docker?',
    options: [
      'Hardcode all configuration in the Dockerfile',
      'Use environment variables for configuration with sensible defaults in ENV',
      'Use configuration files committed to the image',
      'Prompt the user for configuration at runtime',
    ],
    correctAnswer: 1,
    explanation: 'Environment variables provide flexible configuration that can be set at runtime. Set sensible defaults with ENV and allow overrides via `docker run -e` or `.env` files.',
    codeSnippet: 'ENV PORT=3000\nENV LOG_LEVEL=info\nENV DB_HOST=localhost\n\nCMD ["node", "server.js"]',
    tags: ['configuration', 'env', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-054',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'How do you create development and production images from the same Dockerfile?',
    options: [
      'Use two separate Dockerfiles',
      'Use multi-stage builds with --target to build either a development or production stage',
      'Use docker compose to switch between images',
      'Use ARG to conditionally install packages',
    ],
    correctAnswer: 1,
    explanation: 'Define separate stages for development (with dev tools, hot reload) and production (minimal). Use `--target` to build the appropriate stage.',
    codeSnippet: 'FROM node:20 AS base\nWORKDIR /app\nCOPY package*.json ./\n\nFROM base AS development\nRUN npm install\nCOPY . .\nCMD ["npm", "run", "dev"]\n\nFROM base AS production\nRUN npm ci --production\nCOPY . .\nCMD ["node", "server.js"]',
    tags: ['multi-stage', 'dev-prod', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-055',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the recommended way to handle logging in Docker containers?',
    options: [
      'Write logs to /var/log inside the container',
      'Write logs to stdout and stderr so Docker logging drivers can capture them',
      'Use a sidecar logging container',
      'Mount a log volume',
    ],
    correctAnswer: 1,
    explanation: 'Docker best practice is to write logs to stdout/stderr. Docker captures these streams and makes them available via `docker logs` and logging drivers.',
    codeSnippet: '# nginx: redirect logs to stdout/stderr\nRUN ln -sf /dev/stdout /var/log/nginx/access.log && \\\n    ln -sf /dev/stderr /var/log/nginx/error.log',
    tags: ['logging', 'stdout', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-056',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ENV Advanced',
    question: 'How do you set a multi-line environment variable?',
    options: [
      'ENV cannot hold multi-line values',
      'Use quotes and \\n for newlines, or set it at runtime',
      'Use multiple ENV instructions',
      'Use a HEREDOC',
    ],
    correctAnswer: 1,
    explanation: 'Multi-line ENV values can be set using escape characters within quotes. For complex multi-line configs, consider using COPY to include a configuration file instead.',
    codeSnippet: 'ENV CONFIG="line1\\nline2\\nline3"',
    tags: ['env', 'multiline', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-057',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'COPY Advanced',
    question: 'How do you preserve file permissions when copying in a Dockerfile?',
    options: [
      'COPY --preserve-permissions',
      'File permissions from the build context are preserved by default in COPY',
      'Use RUN chmod after COPY',
      'COPY --mode=preserve',
    ],
    correctAnswer: 1,
    explanation: 'COPY preserves file permissions from the source by default. However, ownership is set to root:root unless `--chown` is used. Use `--chmod` to explicitly set permissions.',
    tags: ['copy', 'permissions', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-058',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you use `pip install --no-cache-dir` in Dockerfiles?',
    options: [
      'To install packages faster',
      'To avoid storing pip download cache in the image layer, reducing image size',
      'To skip version checks',
      'To install from local cache only',
    ],
    correctAnswer: 1,
    explanation: 'The `--no-cache-dir` flag tells pip not to save downloaded packages in its cache. Since the cache is useless inside a Docker image, this reduces image size.',
    codeSnippet: 'RUN pip install --no-cache-dir -r requirements.txt',
    tags: ['pip', 'cache', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-059',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'What is a "vendored" dependencies pattern in multi-stage builds?',
    options: [
      'Buying commercial dependencies',
      'A stage that downloads all dependencies which are then copied to subsequent stages',
      'Using vendor-specific base images',
      'Installing packages from a vendor repository',
    ],
    correctAnswer: 1,
    explanation: 'A vendored dependencies stage downloads and caches all dependencies separately. Other stages copy from this stage, allowing dependency installation to be cached independently of source code.',
    codeSnippet: 'FROM golang:1.22 AS deps\nWORKDIR /app\nCOPY go.mod go.sum ./\nRUN go mod download\n\nFROM deps AS builder\nCOPY . .\nRUN go build -o app',
    tags: ['multi-stage', 'dependencies', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l2-060',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Optimization',
    question: 'What is the impact of file deletions across layers?',
    options: [
      'Files are removed from all layers',
      'Deleted files still consume space in their original layer; only whiteout markers are added in the new layer',
      'File deletions are free and reduce image size',
      'Deleted files are moved to a recycle bin layer',
    ],
    correctAnswer: 1,
    explanation: 'Deleting a file in a layer does not remove it from the previous layer. It creates a whiteout marker hiding the file. The original data still occupies space in the image.',
    codeSnippet: '# BAD: file.tar still in layer 1\nRUN curl -o file.tar https://example.com/file.tar\nRUN tar xf file.tar && rm file.tar\n\n# GOOD: file.tar cleaned in same layer\nRUN curl -o file.tar https://example.com/file.tar && \\\n    tar xf file.tar && \\\n    rm file.tar',
    tags: ['layers', 'whiteout', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-061',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How do you make a Java application Docker-friendly?',
    options: [
      'Use the largest JDK image available',
      'Use a JRE (not JDK) for runtime, set memory flags, and use exec form for proper signal handling',
      'Install Java inside an Ubuntu container',
      'Always use openjdk:latest',
    ],
    correctAnswer: 1,
    explanation: 'Use JRE instead of JDK for smaller images, set JVM memory flags to respect container limits, and use exec form CMD for proper SIGTERM handling.',
    codeSnippet: 'FROM eclipse-temurin:21-jre-alpine\nWORKDIR /app\nCOPY target/app.jar .\nENV JAVA_OPTS="-XX:MaxRAMPercentage=75.0"\nCMD ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]',
    tags: ['java', 'jre', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-062',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK Patterns',
    question: 'How do you write a HEALTHCHECK for a container without curl or wget?',
    options: [
      'You cannot have a HEALTHCHECK without curl',
      'Use built-in tools like the application health command or write a custom health check script',
      'Install curl in the HEALTHCHECK instruction',
      'Use docker inspect instead',
    ],
    correctAnswer: 1,
    explanation: 'For minimal images, use native tools: database clients for DB containers, custom scripts, or built-in health commands. Many languages have simple HTTP check scripts.',
    codeSnippet: '# PostgreSQL\nHEALTHCHECK CMD pg_isready -U postgres\n\n# Node.js without curl\nHEALTHCHECK CMD node -e "require(\'http\').get(\'http://localhost:3000/health\', (r) => { process.exit(r.statusCode === 200 ? 0 : 1) })"',
    tags: ['healthcheck', 'minimal', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l2-063',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the purpose of creating a `.dockerignore` entry for `.git`?',
    options: [
      'To prevent Git from tracking the Docker image',
      'To exclude the large .git directory from the build context, reducing transfer time and image size',
      'To prevent Git hooks from running during build',
      'To avoid version conflicts',
    ],
    correctAnswer: 1,
    explanation: 'The `.git` directory can be very large and is rarely needed in the image. Excluding it reduces the build context size and prevents it from invalidating the Docker cache.',
    codeSnippet: '# .dockerignore\n.git\n.gitignore',
    tags: ['dockerignore', 'git', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-064',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you use `COPY --chown` instead of `RUN chown`?',
    options: [
      'COPY --chown is newer',
      'COPY --chown sets ownership during copy without creating an additional layer from a RUN command',
      'RUN chown does not work in Docker',
      'COPY --chown is faster',
    ],
    correctAnswer: 1,
    explanation: 'Using `COPY --chown` sets ownership during the copy operation in a single layer. A separate `RUN chown` creates an additional layer that duplicates all the files with new ownership.',
    codeSnippet: '# Good: single layer\nCOPY --chown=app:app . /app/\n\n# Bad: extra layer duplicating files\nCOPY . /app/\nRUN chown -R app:app /app/',
    tags: ['copy', 'chown', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-065',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Debugging',
    question: 'How do you see what a Dockerfile build produces at each step?',
    options: [
      'docker build --verbose',
      'docker build --progress=plain to see full output of each instruction',
      'docker build --debug',
      'docker build --show-steps',
    ],
    correctAnswer: 1,
    explanation: 'The `--progress=plain` flag shows the full output of each build step instead of the default tty progress display. This is essential for debugging build issues.',
    codeSnippet: 'docker build --progress=plain -t myapp .',
    tags: ['debugging', 'build', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-066',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How should you handle the `.env` file in Docker builds?',
    options: [
      'COPY .env into the image',
      'Add .env to .dockerignore and pass values at runtime via -e or --env-file',
      'Use ENV to set all .env values in the Dockerfile',
      'Store .env in a Docker volume',
    ],
    correctAnswer: 1,
    explanation: 'Never include `.env` files in Docker images as they may contain secrets. Add `.env` to `.dockerignore` and pass environment values at runtime.',
    codeSnippet: '# .dockerignore\n.env\n.env.*\n\n# At runtime:\n# docker run --env-file .env myapp',
    tags: ['env-file', 'security', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-067',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'ARG Advanced',
    question: 'Can ARG values affect the build cache?',
    options: [
      'No, ARG values never affect caching',
      'Yes, changing an ARG value used in an instruction invalidates the cache from that point',
      'Only ENV values affect caching',
      'ARG values disable caching entirely',
    ],
    correctAnswer: 1,
    explanation: 'If an ARG value is used in a RUN or other instruction, changing the ARG value (via --build-arg) invalidates the cache for that instruction and all subsequent instructions.',
    tags: ['arg', 'cache', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-068',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Layer Optimization',
    question: 'Why should you combine related RUN commands but separate unrelated ones?',
    options: [
      'Docker requires a specific number of layers',
      'Combining related commands reduces layers; separate unrelated ones allows better cache reuse',
      'It makes the Dockerfile more readable',
      'There is no benefit to separating commands',
    ],
    correctAnswer: 1,
    explanation: 'Combine related commands (install + cleanup) to reduce image size. Keep unrelated commands separate (e.g., OS packages vs app setup) so changes only invalidate relevant layers.',
    codeSnippet: '# OS packages (changes rarely)\nRUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*\n\n# App setup (changes occasionally)\nRUN mkdir -p /app && chown -R appuser:appuser /app',
    tags: ['layers', 'cache', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-069',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How do you ensure a container runs with a read-only filesystem?',
    options: [
      'Add READONLY instruction in the Dockerfile',
      'Design the Dockerfile so the app writes only to volumes or tmpfs, then run with --read-only',
      'Use FROM scratch',
      'Set ENV READONLY=true',
    ],
    correctAnswer: 1,
    explanation: 'Design the application to write only to specific directories (volumes/tmpfs). Use VOLUME for data directories and run with `--read-only` for security.',
    codeSnippet: 'FROM node:20-alpine\nWORKDIR /app\nCOPY . .\nVOLUME /app/data\n# Run with: docker run --read-only --tmpfs /tmp myapp',
    tags: ['read-only', 'security', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-070',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Hadolint',
    question: 'What is Hadolint?',
    options: [
      'A Docker daemon optimizer',
      'A Dockerfile linter that checks for best practices and common mistakes',
      'A container health monitoring tool',
      'A Docker image compression tool',
    ],
    correctAnswer: 1,
    explanation: 'Hadolint is a popular open-source Dockerfile linter. It parses Dockerfiles and checks against best practice rules, including ShellCheck analysis of RUN instructions.',
    codeSnippet: 'hadolint Dockerfile',
    tags: ['hadolint', 'linting', 'tools'],
  },
  {
    id: 'docker-dockerfile-l2-071',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you use specific version tags instead of `latest` in FROM?',
    options: [
      'latest is not supported',
      'Specific versions ensure reproducible builds; latest can change unexpectedly',
      'latest images are larger',
      'latest requires authentication',
    ],
    correctAnswer: 1,
    explanation: 'Using `latest` means different builds may use different base image versions, leading to inconsistent and potentially broken builds. Pin to specific versions for reproducibility.',
    codeSnippet: '# Bad: unpredictable\nFROM node:latest\n\n# Good: reproducible\nFROM node:20.11-alpine3.19',
    tags: ['versioning', 'reproducible', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-072',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'How do you create a static binary with Go for use with FROM scratch?',
    options: [
      'Just run go build',
      'Use CGO_ENABLED=0 to disable cgo and create a statically linked binary',
      'Use go build --static',
      'Compile with gcc instead of go',
    ],
    correctAnswer: 1,
    explanation: 'Setting `CGO_ENABLED=0` disables cgo, producing a statically linked binary with no external dependencies. This binary can run in a FROM scratch image.',
    codeSnippet: 'FROM golang:1.22 AS builder\nWORKDIR /app\nCOPY . .\nRUN CGO_ENABLED=0 GOOS=linux go build -o app\n\nFROM scratch\nCOPY --from=builder /app/app /\nENTRYPOINT ["/app"]',
    tags: ['go', 'static-binary', 'scratch'],
  },
  {
    id: 'docker-dockerfile-l2-073',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Advanced',
    question: 'What does `set -o pipefail` do in a RUN instruction?',
    options: [
      'Enables piping between commands',
      'Causes the pipe to return the exit code of the last failing command, not just the last command',
      'Pipes output to a log file',
      'Enables parallel pipe processing',
    ],
    correctAnswer: 1,
    explanation: 'Without pipefail, a pipe returns the exit code of only the last command. With pipefail, if any command in the pipe fails, the entire pipe returns a failure.',
    codeSnippet: 'SHELL ["/bin/bash", "-o", "pipefail", "-c"]\nRUN curl -fsSL https://example.com/install.sh | bash',
    tags: ['run', 'pipefail', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-074',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the purpose of `--production` or `--omit=dev` in npm ci?',
    options: [
      'Enables production mode for the application',
      'Skips installing devDependencies to reduce image size',
      'Generates a production build',
      'Enables production-level logging',
    ],
    correctAnswer: 1,
    explanation: 'The `--production` flag (or `--omit=dev`) skips installing devDependencies listed in package.json, reducing the final image size by excluding test frameworks, linters, etc.',
    codeSnippet: 'RUN npm ci --omit=dev',
    tags: ['npm', 'production', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-075',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Build Context',
    question: 'Can you use a URL as the build context?',
    options: [
      'No, only local directories',
      'Yes, docker build accepts Git repository URLs and tar archive URLs',
      'Only with BuildKit',
      'Only tar archives, not Git URLs',
    ],
    correctAnswer: 1,
    explanation: 'Docker build supports Git repository URLs and tar archive URLs as the build context. Docker clones the repo or downloads the archive before building.',
    codeSnippet: 'docker build -t myapp https://github.com/user/repo.git#main',
    tags: ['build-context', 'git', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-076',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you use `COPY` at the end rather than at the beginning of a Dockerfile?',
    options: [
      'Docker requires COPY to be after RUN',
      'Placing COPY of source code at the end maximizes cache reuse since earlier layers with dependencies remain cached',
      'COPY is slower at the beginning',
      'It prevents file conflicts',
    ],
    correctAnswer: 1,
    explanation: 'Source code changes frequently. Placing `COPY . .` after dependency installation ensures that code changes only invalidate the final layers, not the expensive dependency layers.',
    tags: ['copy', 'cache', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-077',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Debugging',
    question: 'How do you list all Hadolint rules for a specific category?',
    options: [
      'hadolint --list-rules',
      'hadolint --list-all',
      'Visit the Hadolint documentation or use hadolint --ignore to suppress specific rules',
      'hadolint --categories',
    ],
    correctAnswer: 2,
    explanation: 'Hadolint rules are documented on its GitHub page. Use `--ignore DLXXXX` to suppress specific rules, or configure them in a `.hadolint.yaml` file.',
    codeSnippet: 'hadolint --ignore DL3008 --ignore DL3009 Dockerfile',
    tags: ['hadolint', 'rules', 'linting'],
  },
  {
    id: 'docker-dockerfile-l2-078',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How do you set proper locale/encoding in a Docker image?',
    options: [
      'Docker handles encoding automatically',
      'Install locales and set LANG or LC_ALL environment variable',
      'Use a locale-specific base image',
      'Set ENCODING in the Dockerfile',
    ],
    correctAnswer: 1,
    explanation: 'Set locale environment variables and optionally install locale packages to ensure proper character encoding for applications that depend on it.',
    codeSnippet: 'ENV LANG=C.UTF-8\nENV LC_ALL=C.UTF-8',
    tags: ['locale', 'encoding', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-079',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'What is the advantage of using `FROM image AS base` with multiple stages inheriting from it?',
    options: [
      'It makes the Dockerfile shorter',
      'A shared base stage avoids duplicating common setup across stages',
      'It is required for multi-stage builds',
      'It enables parallel builds',
    ],
    correctAnswer: 1,
    explanation: 'A shared base stage with common dependencies (OS packages, configuration) prevents duplicating setup code. Other stages can build FROM this base, keeping the Dockerfile DRY.',
    codeSnippet: 'FROM node:20-alpine AS base\nWORKDIR /app\nRUN apk add --no-cache tini\n\nFROM base AS development\nRUN npm install\nCMD ["npm", "run", "dev"]\n\nFROM base AS production\nRUN npm ci --production\nCMD ["tini", "--", "node", "server.js"]',
    tags: ['multi-stage', 'base-stage', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l2-080',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you use `tini` or `--init` for Node.js containers?',
    options: [
      'For faster startup',
      'Node.js does not handle signals well as PID 1; tini properly forwards signals and reaps zombie processes',
      'tini reduces memory usage',
      'It is required by the Node.js runtime',
    ],
    correctAnswer: 1,
    explanation: 'Node.js does not handle SIGTERM properly when running as PID 1 and does not reap zombie child processes. tini (or --init) runs as PID 1, properly handling signals and zombies.',
    codeSnippet: 'FROM node:20-alpine\nRUN apk add --no-cache tini\nENTRYPOINT ["tini", "--"]\nCMD ["node", "server.js"]',
    tags: ['tini', 'node', 'signals'],
  },
  {
    id: 'docker-dockerfile-l2-081',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'COPY Advanced',
    question: 'How do you use COPY with multiple source files going to one destination?',
    options: [
      'COPY file1 file2 /dest (without trailing slash) - they will be concatenated',
      'COPY file1 file2 /dest/ (with trailing slash) - both files copied to the directory',
      'You cannot copy multiple files to one destination',
      'Use ADD for multiple files',
    ],
    correctAnswer: 1,
    explanation: 'When copying multiple files, the destination must end with a trailing slash to indicate it is a directory. All source files are copied into that directory.',
    codeSnippet: 'COPY package.json tsconfig.json /app/',
    tags: ['copy', 'multiple', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-082',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How do you handle SSL/TLS certificates in a Docker image?',
    options: [
      'Certificates are always available in Docker images',
      'Install the ca-certificates package to enable HTTPS connections',
      'Docker provides certificates automatically',
      'Mount certificates at runtime only',
    ],
    correctAnswer: 1,
    explanation: 'Minimal base images may not include CA certificates. Install `ca-certificates` to enable HTTPS connections from the container.',
    codeSnippet: 'RUN apt-get update && \\\n    apt-get install -y --no-install-recommends ca-certificates && \\\n    rm -rf /var/lib/apt/lists/*',
    tags: ['certificates', 'ssl', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-083',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Patterns',
    question: 'How do you include SSL certificates in a scratch-based image?',
    options: [
      'Certificates are built into scratch',
      'Copy the ca-certificates.crt file from a builder stage',
      'Use ADD to download certificates',
      'scratch images cannot make HTTPS connections',
    ],
    correctAnswer: 1,
    explanation: 'Since scratch has no filesystem, you must copy the CA certificates from a builder stage that has them installed.',
    codeSnippet: 'FROM alpine:3.19 AS certs\nRUN apk add --no-cache ca-certificates\n\nFROM scratch\nCOPY --from=certs /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/\nCOPY --from=builder /app/myapp /myapp',
    tags: ['scratch', 'certificates', 'multi-stage'],
  },
  {
    id: 'docker-dockerfile-l2-084',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How do you handle file permissions for volumes in a Dockerfile?',
    options: [
      'Docker handles permissions automatically',
      'Create the mount directory with proper ownership before the VOLUME instruction',
      'Use chmod at runtime',
      'Volumes ignore permissions',
    ],
    correctAnswer: 1,
    explanation: 'Create the mount point directory with the correct user ownership before defining it as a VOLUME. Docker initializes new volumes with the directory content and permissions from the image.',
    codeSnippet: 'RUN mkdir -p /data && chown -R appuser:appuser /data\nVOLUME /data\nUSER appuser',
    tags: ['volumes', 'permissions', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-085',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'RUN Advanced',
    question: 'How do you run a specific shell script in a RUN instruction?',
    options: [
      'RUN script.sh',
      'RUN /bin/bash script.sh or COPY the script first and RUN it',
      'RUN EXEC script.sh',
      'SCRIPT script.sh',
    ],
    correctAnswer: 1,
    explanation: 'First COPY the script into the image, then RUN it. Use the exec form for explicit shell selection or ensure the script has a shebang line and execute permissions.',
    codeSnippet: 'COPY build-script.sh /tmp/\nRUN chmod +x /tmp/build-script.sh && /tmp/build-script.sh && rm /tmp/build-script.sh',
    tags: ['run', 'scripts', 'intermediate'],
  },
  {
    id: 'docker-dockerfile-l2-086',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the benefit of using `.dockerignore` for test files?',
    options: [
      'Tests run faster when ignored',
      'Excluding test files reduces build context size and prevents test changes from invalidating the Docker cache',
      'Docker cannot process test files',
      'Test files are automatically excluded',
    ],
    correctAnswer: 1,
    explanation: 'Test files, test fixtures, and coverage reports are not needed in production images. Excluding them speeds up context transfer and prevents unnecessary cache invalidation.',
    codeSnippet: '# .dockerignore\n**/__tests__\n**/*.test.*\n**/*.spec.*\ncoverage/\n.nyc_output/',
    tags: ['dockerignore', 'tests', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l2-087',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'How do you set the correct file ownership when using a non-root user?',
    options: [
      'Docker automatically adjusts ownership',
      'Use COPY --chown=user:group and ensure directories are owned by the user before switching with USER',
      'RUN chown after every COPY',
      'Use root user and setuid',
    ],
    correctAnswer: 1,
    explanation: 'Set up the user, create directories with correct ownership, use `COPY --chown` for files, and switch to the non-root user with USER as one of the last instructions.',
    codeSnippet: 'FROM node:20-alpine\nRUN addgroup -S app && adduser -S -G app app\nWORKDIR /app\nCOPY --chown=app:app package*.json ./\nRUN npm ci\nCOPY --chown=app:app . .\nUSER app\nCMD ["node", "server.js"]',
    tags: ['user', 'ownership', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l2-088',
    language: 'docker',
    level: 'level2',
    category: 'Dockerfile',
    subcategory: 'EXPOSE Best Practices',
    question: 'Can you expose a range of ports in a Dockerfile?',
    options: [
      'No, only individual ports',
      'Yes, using EXPOSE with a range like EXPOSE 8000-8010',
      'Only with Docker Compose',
      'Only TCP ranges, not UDP',
    ],
    correctAnswer: 1,
    explanation: 'EXPOSE supports port ranges. `EXPOSE 8000-8010` documents that the container may listen on ports 8000 through 8010.',
    codeSnippet: 'EXPOSE 8000-8010',
    tags: ['expose', 'port-range', 'intermediate'],
  },
];

export default questions;
