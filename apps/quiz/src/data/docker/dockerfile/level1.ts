import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'docker-dockerfile-l1-001',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Introduction',
    question: 'What is a Dockerfile?',
    options: [
      'A running container configuration',
      'A text file with instructions to build a Docker image',
      'A Docker Compose configuration file',
      'A container log file',
    ],
    correctAnswer: 1,
    explanation: 'A Dockerfile is a text document containing a series of instructions that Docker reads to automatically build an image. Each instruction creates a layer in the image.',
    tags: ['dockerfile', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-002',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'FROM',
    question: 'What does the FROM instruction do?',
    options: [
      'Copies files from the host',
      'Sets the base image for the build',
      'Defines the output format',
      'Specifies the Docker registry',
    ],
    correctAnswer: 1,
    explanation: 'FROM initializes a new build stage and sets the base image. Every valid Dockerfile must start with a FROM instruction (except for ARG before FROM).',
    codeSnippet: 'FROM ubuntu:22.04',
    tags: ['from', 'base-image', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-003',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'What does the RUN instruction do?',
    options: [
      'Starts the container',
      'Executes a command during the image build and creates a new layer',
      'Runs a health check',
      'Runs the container in the background',
    ],
    correctAnswer: 1,
    explanation: 'RUN executes commands in a new layer on top of the current image and commits the result. It is commonly used to install packages and set up the image.',
    codeSnippet: 'RUN apt-get update && apt-get install -y curl',
    tags: ['run', 'build', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-004',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'CMD',
    question: 'What does the CMD instruction do?',
    options: [
      'Executes a command during image build',
      'Provides the default command to run when a container starts',
      'Comments out a line in the Dockerfile',
      'Creates a new image layer',
    ],
    correctAnswer: 1,
    explanation: 'CMD sets the default command to execute when a container is started from the image. There can only be one CMD instruction; if multiple are listed, only the last one takes effect.',
    codeSnippet: 'CMD ["node", "server.js"]',
    tags: ['cmd', 'runtime', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-005',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What does the COPY instruction do?',
    options: [
      'Copies files from a URL to the image',
      'Copies files from the build context into the image',
      'Copies files between containers',
      'Copies environment variables between stages',
    ],
    correctAnswer: 1,
    explanation: 'COPY copies files and directories from the build context (the local filesystem) into the image filesystem at the specified destination.',
    codeSnippet: 'COPY ./src /app/src',
    tags: ['copy', 'files', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-006',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'WORKDIR',
    question: 'What does the WORKDIR instruction do?',
    options: [
      'Sets the working directory on the host machine',
      'Sets the working directory for subsequent instructions in the Dockerfile',
      'Creates a workspace for Docker Compose',
      'Defines the build context directory',
    ],
    correctAnswer: 1,
    explanation: 'WORKDIR sets the working directory for any subsequent RUN, CMD, ENTRYPOINT, COPY, and ADD instructions. If the directory does not exist, it is created automatically.',
    codeSnippet: 'WORKDIR /app',
    tags: ['workdir', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-007',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'EXPOSE',
    question: 'What does the EXPOSE instruction do?',
    options: [
      'Publishes the port to the host automatically',
      'Documents which ports the container listens on at runtime',
      'Opens a firewall port',
      'Connects the container to a network',
    ],
    correctAnswer: 1,
    explanation: 'EXPOSE documents the port(s) the container listens on at runtime. It does not actually publish the port; you still need `-p` when running the container.',
    codeSnippet: 'EXPOSE 8080',
    tags: ['expose', 'networking', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-008',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENV',
    question: 'What does the ENV instruction do?',
    options: [
      'Sets environment variables only during build',
      'Sets environment variables that persist in the image and running containers',
      'Sets the container environment (development/production)',
      'Configures the Docker environment',
    ],
    correctAnswer: 1,
    explanation: 'ENV sets environment variables that are available during the build and persist when a container is run from the resulting image.',
    codeSnippet: 'ENV NODE_ENV=production\nENV PORT=3000',
    tags: ['env', 'environment', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-009',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ADD',
    question: 'What does the ADD instruction do?',
    options: [
      'Adds a new user to the image',
      'Copies files to the image and can also extract tar archives and fetch URLs',
      'Adds a new layer to the image',
      'Adds a new package to the image',
    ],
    correctAnswer: 1,
    explanation: 'ADD copies files from the build context to the image, similar to COPY, but with additional features: it can extract compressed tar archives and download files from URLs.',
    codeSnippet: 'ADD archive.tar.gz /app/',
    tags: ['add', 'files', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-010',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT',
    question: 'What does the ENTRYPOINT instruction do?',
    options: [
      'Specifies the entry directory for the container',
      'Configures the container to run as an executable with a fixed command',
      'Sets the first instruction in the Dockerfile',
      'Defines the Docker registry entry point',
    ],
    correctAnswer: 1,
    explanation: 'ENTRYPOINT configures the container to run as an executable. Unlike CMD, the ENTRYPOINT command is not overridden by arguments passed to `docker run`.',
    codeSnippet: 'ENTRYPOINT ["python", "app.py"]',
    tags: ['entrypoint', 'runtime', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-011',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'VOLUME',
    question: 'What does the VOLUME instruction do?',
    options: [
      'Mounts a host directory into the container',
      'Creates a mount point and marks it as holding externally mounted volumes',
      'Sets the disk size limit for the container',
      'Creates a named volume in Docker',
    ],
    correctAnswer: 1,
    explanation: 'VOLUME creates a mount point with the specified path and marks it as holding externally mounted volumes from the host or other containers.',
    codeSnippet: 'VOLUME /data',
    tags: ['volume', 'storage', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-012',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'USER',
    question: 'What does the USER instruction do?',
    options: [
      'Adds a new user to the Docker host',
      'Sets the user and optionally the group for subsequent instructions and the container runtime',
      'Authenticates with Docker Hub',
      'Lists users in the image',
    ],
    correctAnswer: 1,
    explanation: 'USER sets the user name (or UID) and optionally the group (or GID) for any subsequent RUN, CMD, ENTRYPOINT instructions and the running container.',
    codeSnippet: 'RUN addgroup --system app && adduser --system --ingroup app app\nUSER app',
    tags: ['user', 'security', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-013',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'LABEL',
    question: 'What does the LABEL instruction do?',
    options: [
      'Names the Docker image',
      'Adds metadata as key-value pairs to the image',
      'Labels the container for network routing',
      'Tags the image in a registry',
    ],
    correctAnswer: 1,
    explanation: 'LABEL adds metadata to an image as key-value pairs. Labels are used for organizing images, providing version info, descriptions, maintainer details, and more.',
    codeSnippet: 'LABEL maintainer="team@example.com"\nLABEL version="1.0"\nLABEL description="My application image"',
    tags: ['label', 'metadata', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-014',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'CMD',
    question: 'What are the two forms of the CMD instruction?',
    options: [
      'text form and binary form',
      'exec form (JSON array) and shell form (string)',
      'inline form and external form',
      'build form and runtime form',
    ],
    correctAnswer: 1,
    explanation: 'CMD has two forms: exec form `CMD ["executable", "param1"]` which runs the command directly, and shell form `CMD command param1` which runs via `/bin/sh -c`.',
    codeSnippet: '# Exec form (preferred)\nCMD ["node", "server.js"]\n\n# Shell form\nCMD node server.js',
    tags: ['cmd', 'exec-form', 'shell-form'],
  },
  {
    id: 'docker-dockerfile-l1-015',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'CMD',
    question: 'What happens when you specify a command in `docker run` with a CMD-based Dockerfile?',
    options: [
      'Both CMD and the docker run command execute',
      'The docker run command overrides the CMD instruction',
      'The CMD instruction takes priority',
      'An error occurs',
    ],
    correctAnswer: 1,
    explanation: 'When you pass a command to `docker run`, it completely overrides the CMD instruction in the Dockerfile.',
    codeSnippet: '# Dockerfile: CMD ["echo", "hello"]\n# Running: docker run myimage echo "world"\n# Output: world (CMD is overridden)',
    tags: ['cmd', 'override', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-016',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY vs ADD',
    question: 'When should you use COPY instead of ADD?',
    options: [
      'Never, ADD is always better',
      'When you only need to copy local files without tar extraction or URL download',
      'When copying from a remote URL',
      'When the files are larger than 1MB',
    ],
    correctAnswer: 1,
    explanation: 'COPY is preferred over ADD for simple file copying because it is more transparent and predictable. Use ADD only when you need tar auto-extraction or URL downloading.',
    codeSnippet: '# Preferred for simple copies\nCOPY package.json .\n\n# Use ADD only for tar extraction\nADD archive.tar.gz /app/',
    tags: ['copy', 'add', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-017',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'FROM',
    question: 'What does `FROM node:20-alpine` mean?',
    options: [
      'Install Node.js version 20 on Alpine Linux during build',
      'Use the Node.js 20 image based on Alpine Linux as the base image',
      'Download Alpine Linux and then install Node.js 20',
      'Create a new image called node:20-alpine',
    ],
    correctAnswer: 1,
    explanation: 'This instruction uses the official Node.js version 20 image built on Alpine Linux as the base. Alpine-based images are significantly smaller than Debian-based ones.',
    codeSnippet: 'FROM node:20-alpine',
    tags: ['from', 'alpine', 'node', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-018',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'What are the two forms of the RUN instruction?',
    options: [
      'foreground and background forms',
      'exec form (JSON array) and shell form (string)',
      'sync and async forms',
      'local and remote forms',
    ],
    correctAnswer: 1,
    explanation: 'RUN has two forms: shell form `RUN <command>` which executes via `/bin/sh -c`, and exec form `RUN ["executable", "param1"]` which executes directly without a shell.',
    codeSnippet: '# Shell form\nRUN echo "hello world"\n\n# Exec form\nRUN ["apt-get", "install", "-y", "curl"]',
    tags: ['run', 'exec-form', 'shell-form'],
  },
  {
    id: 'docker-dockerfile-l1-019',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Naming',
    question: 'What is the default filename Docker looks for when building?',
    options: [
      'docker.file',
      'Dockerfile',
      'dockerfile.yml',
      'Docker.conf',
    ],
    correctAnswer: 1,
    explanation: 'By default, Docker looks for a file named `Dockerfile` (capital D, no extension) in the build context directory. You can specify a different file with the `-f` flag.',
    codeSnippet: 'docker build -t myapp .\n# Or with custom filename:\ndocker build -f Dockerfile.prod -t myapp .',
    tags: ['dockerfile', 'naming', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-020',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Comments',
    question: 'How do you add a comment in a Dockerfile?',
    options: [
      '// This is a comment',
      '# This is a comment',
      '/* This is a comment */',
      '-- This is a comment',
    ],
    correctAnswer: 1,
    explanation: 'Comments in a Dockerfile start with `#`. They are ignored by the Docker builder (except for parser directives like `# syntax=`).',
    codeSnippet: '# This is a comment\nFROM alpine:3.19\n# Install dependencies\nRUN apk add --no-cache curl',
    tags: ['comments', 'syntax', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-021',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'WORKDIR',
    question: 'What happens if the WORKDIR directory does not exist?',
    options: [
      'The build fails with an error',
      'Docker creates the directory automatically',
      'Docker uses the root directory instead',
      'A warning is displayed but the build continues',
    ],
    correctAnswer: 1,
    explanation: 'If the directory specified in WORKDIR does not exist, Docker creates it automatically. This applies to all subsequent WORKDIR instructions as well.',
    codeSnippet: 'WORKDIR /app\n# /app is created if it does not exist',
    tags: ['workdir', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-022',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'WORKDIR',
    question: 'Can you use multiple WORKDIR instructions in a Dockerfile?',
    options: [
      'No, only one WORKDIR is allowed',
      'Yes, each subsequent WORKDIR is relative to the previous one if not absolute',
      'Yes, but only in multi-stage builds',
      'No, WORKDIR can only be set once per stage',
    ],
    correctAnswer: 1,
    explanation: 'Multiple WORKDIR instructions are allowed. If a relative path is used, it is relative to the previous WORKDIR. For example, `WORKDIR /app` then `WORKDIR src` results in `/app/src`.',
    codeSnippet: 'WORKDIR /app\nWORKDIR src\n# Current directory is now /app/src',
    tags: ['workdir', 'paths', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-023',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT vs CMD',
    question: 'What is the key difference between ENTRYPOINT and CMD?',
    options: [
      'ENTRYPOINT runs during build; CMD runs at runtime',
      'ENTRYPOINT defines the fixed executable; CMD provides default arguments that can be overridden',
      'CMD is newer and replaces ENTRYPOINT',
      'There is no difference',
    ],
    correctAnswer: 1,
    explanation: 'ENTRYPOINT sets the main executable that always runs. CMD provides default arguments to ENTRYPOINT or a default command that can be overridden at runtime.',
    codeSnippet: 'ENTRYPOINT ["python"]\nCMD ["app.py"]\n# docker run myimage => python app.py\n# docker run myimage test.py => python test.py',
    tags: ['entrypoint', 'cmd', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-024',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'EXPOSE',
    question: 'Does EXPOSE actually publish the port?',
    options: [
      'Yes, it makes the port accessible from the host',
      'No, it is documentation only; you must use -p when running the container',
      'Yes, but only for TCP ports',
      'It depends on the Docker network mode',
    ],
    correctAnswer: 1,
    explanation: 'EXPOSE is purely informational/documentation. It tells users which ports the application uses but does not publish them. Use `-p` or `-P` with `docker run` to actually publish ports.',
    codeSnippet: 'EXPOSE 3000\n# Still need: docker run -p 3000:3000 myapp',
    tags: ['expose', 'ports', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-025',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Simple Dockerfile',
    question: 'Which Dockerfile correctly creates a Node.js application image?',
    options: [
      'FROM node:20\nRUN npm install\nCOPY . .\nCMD node server.js',
      'FROM node:20\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nCMD ["node", "server.js"]',
      'INSTALL node:20\nADD . /app\nRUN npm start',
      'USE node:20\nCOPY /app\nSTART npm install',
    ],
    correctAnswer: 1,
    explanation: 'The correct Dockerfile sets a working directory, copies package files first for caching, installs dependencies, then copies the rest of the source code, and defines the startup command.',
    codeSnippet: 'FROM node:20\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nCMD ["node", "server.js"]',
    tags: ['dockerfile', 'node', 'example'],
  },
  {
    id: 'docker-dockerfile-l1-026',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Simple Dockerfile',
    question: 'Which Dockerfile creates a simple Python application image?',
    options: [
      'FROM python:3.12\nCOPY . /app\nRUN python setup.py',
      'FROM python:3.12\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\nCOPY . .\nCMD ["python", "app.py"]',
      'INSTALL python\nADD requirements.txt\nRUN pip install',
      'FROM python\nRUN pip install all\nSTART app.py',
    ],
    correctAnswer: 1,
    explanation: 'A proper Python Dockerfile sets a working directory, copies and installs requirements first for caching, then copies the application code, and defines the start command.',
    codeSnippet: 'FROM python:3.12\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\nCOPY . .\nCMD ["python", "app.py"]',
    tags: ['dockerfile', 'python', 'example'],
  },
  {
    id: 'docker-dockerfile-l1-027',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Layers',
    question: 'Which Dockerfile instructions create new layers?',
    options: [
      'Every instruction creates a new layer',
      'RUN, COPY, and ADD create new layers; other instructions add metadata',
      'Only RUN creates layers',
      'Only FROM creates a layer',
    ],
    correctAnswer: 1,
    explanation: 'RUN, COPY, and ADD are the main instructions that create new filesystem layers. Other instructions like ENV, LABEL, CMD, and EXPOSE only add metadata to the image configuration.',
    tags: ['layers', 'instructions', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-028',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENV',
    question: 'How do you use an environment variable in a subsequent Dockerfile instruction?',
    options: [
      'Using %{VAR_NAME}%',
      'Using $VAR_NAME or ${VAR_NAME}',
      'Using @VAR_NAME',
      'Using #{VAR_NAME}',
    ],
    correctAnswer: 1,
    explanation: 'Environment variables set with ENV can be referenced in subsequent instructions using `$variable_name` or `\${variable_name}` syntax.',
    codeSnippet: 'ENV APP_HOME=/app\nWORKDIR $APP_HOME\nCOPY . $APP_HOME',
    tags: ['env', 'variables', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-029',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What does `COPY . .` do in a Dockerfile?',
    options: [
      'Copies nothing',
      'Copies all files from the build context to the current WORKDIR in the image',
      'Copies the Dockerfile itself',
      'Creates an empty directory',
    ],
    correctAnswer: 1,
    explanation: 'The first `.` refers to the build context (the directory passed to `docker build`). The second `.` refers to the current WORKDIR inside the image. It copies all files from the context to the working directory.',
    codeSnippet: 'WORKDIR /app\nCOPY . .  # copies build context contents to /app/',
    tags: ['copy', 'context', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-030',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'Why should you combine commands with `&&` in a single RUN instruction?',
    options: [
      'It is required by Docker syntax',
      'It reduces the number of layers and prevents intermediate files from persisting',
      'It makes the build faster by using parallel execution',
      'It enables error handling',
    ],
    correctAnswer: 1,
    explanation: 'Combining commands in a single RUN reduces layers. If you install packages and clean up in separate RUN instructions, the installed files still exist in the first layer even after cleanup.',
    codeSnippet: '# Good: single layer, cleanup included\nRUN apt-get update && \\\n    apt-get install -y curl && \\\n    rm -rf /var/lib/apt/lists/*',
    tags: ['run', 'layers', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-031',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What does `COPY --chown=user:group files /app/` do?',
    options: [
      'Changes the owner of files on the host',
      'Copies files and sets their ownership in the image',
      'Creates a new user and group',
      'Changes ownership of the /app directory only',
    ],
    correctAnswer: 1,
    explanation: 'The `--chown` flag on COPY sets the ownership of the copied files in the image to the specified user and group, avoiding a separate `RUN chown` command.',
    codeSnippet: 'COPY --chown=node:node package.json /app/',
    tags: ['copy', 'chown', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-032',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'EXPOSE',
    question: 'How do you expose both TCP and UDP ports?',
    options: [
      'EXPOSE 80/both',
      'EXPOSE 80/tcp 80/udp',
      'EXPOSE 80 protocol=tcp,udp',
      'EXPOSE 80:tcp 80:udp',
    ],
    correctAnswer: 1,
    explanation: 'You can specify the protocol after the port number with a slash. By default, EXPOSE assumes TCP. To expose UDP, explicitly add `/udp`.',
    codeSnippet: 'EXPOSE 80/tcp\nEXPOSE 53/udp',
    tags: ['expose', 'tcp', 'udp', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-033',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ARG',
    question: 'What does the ARG instruction do?',
    options: [
      'Sets runtime environment variables',
      'Defines a build-time variable that can be passed with --build-arg',
      'Sets command-line arguments for the container',
      'Configures Docker daemon arguments',
    ],
    correctAnswer: 1,
    explanation: 'ARG defines a variable that users can pass at build time using `--build-arg`. Unlike ENV, ARG values are not available in the running container.',
    codeSnippet: 'ARG VERSION=1.0\nRUN echo "Building version $VERSION"',
    tags: ['arg', 'build-time', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-034',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'FROM',
    question: 'Can a Dockerfile have multiple FROM instructions?',
    options: [
      'No, only one FROM is allowed',
      'Yes, for multi-stage builds where each FROM starts a new build stage',
      'Only if they use the same base image',
      'Only with Docker Compose',
    ],
    correctAnswer: 1,
    explanation: 'A Dockerfile can have multiple FROM instructions for multi-stage builds. Each FROM starts a new build stage. You can selectively copy artifacts from one stage to another.',
    codeSnippet: 'FROM node:20 AS builder\nRUN npm run build\n\nFROM nginx:alpine\nCOPY --from=builder /app/dist /usr/share/nginx/html',
    tags: ['from', 'multi-stage', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-035',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Line Continuation',
    question: 'How do you split a long instruction across multiple lines in a Dockerfile?',
    options: [
      'Using a semicolon (;)',
      'Using a backslash (\\) at the end of the line',
      'Using three dots (...)',
      'Using a pipe (|)',
    ],
    correctAnswer: 1,
    explanation: 'A backslash (\\) at the end of a line continues the instruction on the next line, making long commands more readable.',
    codeSnippet: 'RUN apt-get update && \\\n    apt-get install -y \\\n    curl \\\n    vim \\\n    git',
    tags: ['syntax', 'line-continuation', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-036',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT',
    question: 'What are the two forms of ENTRYPOINT?',
    options: [
      'start form and stop form',
      'exec form (JSON array) and shell form (string)',
      'foreground form and background form',
      'static form and dynamic form',
    ],
    correctAnswer: 1,
    explanation: 'Like CMD and RUN, ENTRYPOINT supports exec form `ENTRYPOINT ["executable", "param"]` and shell form `ENTRYPOINT command param`. Exec form is preferred.',
    codeSnippet: '# Exec form (preferred)\nENTRYPOINT ["java", "-jar", "app.jar"]\n\n# Shell form\nENTRYPOINT java -jar app.jar',
    tags: ['entrypoint', 'exec-form', 'shell-form'],
  },
  {
    id: 'docker-dockerfile-l1-037',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'USER',
    question: 'Why is it important to use the USER instruction?',
    options: [
      'To speed up the build process',
      'To run the container as a non-root user for security',
      'To set the Docker Hub username',
      'To enable multi-user access to the container',
    ],
    correctAnswer: 1,
    explanation: 'Running containers as root is a security risk. The USER instruction ensures the container process runs as a non-privileged user, limiting the impact of potential security breaches.',
    codeSnippet: 'RUN useradd --create-home appuser\nUSER appuser',
    tags: ['user', 'security', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-038',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Build Context',
    question: 'What does the `.` at the end of `docker build -t myapp .` mean?',
    options: [
      'Build the current file',
      'The build context is the current directory',
      'Use the default Dockerfile',
      'Build all files',
    ],
    correctAnswer: 1,
    explanation: 'The `.` specifies the build context, which is the current directory. Docker sends this directory (minus .dockerignore exclusions) to the daemon for the build.',
    codeSnippet: 'docker build -t myapp .',
    tags: ['build-context', 'docker-build', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-039',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Instruction Order',
    question: 'Which instruction must appear first in a Dockerfile (ignoring ARG)?',
    options: [
      'RUN',
      'FROM',
      'LABEL',
      'WORKDIR',
    ],
    correctAnswer: 1,
    explanation: 'FROM must be the first instruction in a Dockerfile (the only exception is ARG, which can appear before FROM to be used in the FROM instruction).',
    codeSnippet: 'ARG BASE_TAG=3.19\nFROM alpine:$BASE_TAG',
    tags: ['from', 'order', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-040',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'Can COPY copy multiple files at once?',
    options: [
      'No, only one file per COPY instruction',
      'Yes, you can list multiple sources or use wildcards',
      'Only with the --multi flag',
      'Only if they are in the same directory',
    ],
    correctAnswer: 1,
    explanation: 'COPY can copy multiple files using wildcards or by listing multiple source paths. When copying multiple files, the destination must be a directory (ending with /).',
    codeSnippet: 'COPY package.json package-lock.json ./\nCOPY *.py /app/',
    tags: ['copy', 'wildcards', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-041',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENV',
    question: 'What is the difference between `ENV VAR value` and `ENV VAR=value`?',
    options: [
      'The first sets a string; the second sets a number',
      'Both work but the = form is preferred and can set multiple variables on one line',
      'The first is for build time; the second is for runtime',
      'The = form is deprecated',
    ],
    correctAnswer: 1,
    explanation: 'Both forms work identically for single variables. The `=` form is preferred because it can set multiple variables on one line: `ENV VAR1=value1 VAR2=value2`.',
    codeSnippet: 'ENV NODE_ENV=production PORT=3000',
    tags: ['env', 'syntax', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-042',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'CMD',
    question: 'How many CMD instructions can be effective in a Dockerfile?',
    options: [
      'Unlimited',
      'Only the last CMD instruction takes effect',
      'One per stage',
      'Exactly three',
    ],
    correctAnswer: 1,
    explanation: 'Only the last CMD instruction in a Dockerfile takes effect. If you have multiple CMD instructions, all but the last one are ignored.',
    tags: ['cmd', 'rules', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-043',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'FROM',
    question: 'What does `FROM scratch` mean?',
    options: [
      'Start from a random base image',
      'Start from a brand new empty image with no base layer',
      'Start from the default Ubuntu image',
      'Rebuild the base image from source',
    ],
    correctAnswer: 1,
    explanation: '`FROM scratch` starts from a completely empty image. It is used for statically compiled binaries that do not need any OS libraries or tools.',
    codeSnippet: 'FROM scratch\nCOPY myapp /\nENTRYPOINT ["/myapp"]',
    tags: ['from', 'scratch', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-044',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'LABEL',
    question: 'How do you set multiple labels in a single instruction?',
    options: [
      'LABEL key1 value1 key2 value2',
      'LABEL key1=value1 key2=value2',
      'LABEL [key1=value1, key2=value2]',
      'LABEL {key1: value1, key2: value2}',
    ],
    correctAnswer: 1,
    explanation: 'Multiple labels can be set in a single LABEL instruction using the key=value format separated by spaces, reducing the number of layers.',
    codeSnippet: 'LABEL version="1.0" \\\n      maintainer="team@example.com" \\\n      description="My app"',
    tags: ['label', 'syntax', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-045',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'VOLUME',
    question: 'What path format does VOLUME accept?',
    options: [
      'Only absolute paths',
      'Both JSON array format and plain string format with absolute paths',
      'Only relative paths',
      'Both relative and absolute paths',
    ],
    correctAnswer: 1,
    explanation: 'VOLUME accepts paths in JSON array format (`VOLUME ["/data", "/logs"]`) or plain string format (`VOLUME /data /logs`). Paths must be absolute.',
    codeSnippet: 'VOLUME ["/data", "/logs"]\n# or\nVOLUME /data /logs',
    tags: ['volume', 'syntax', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-046',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'What does `RUN apt-get update && apt-get install -y curl` do?',
    options: [
      'Updates the Docker image and installs Docker',
      'Updates the package list and installs curl inside the image layer',
      'Updates the host system and installs curl locally',
      'Downloads curl from the internet at runtime',
    ],
    correctAnswer: 1,
    explanation: 'This RUN instruction updates the package repository list and installs the curl package inside the image during the build process, all in a single layer.',
    codeSnippet: 'RUN apt-get update && apt-get install -y curl',
    tags: ['run', 'apt-get', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-047',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT + CMD',
    question: 'What happens when both ENTRYPOINT and CMD are defined?',
    options: [
      'CMD overrides ENTRYPOINT',
      'CMD provides default arguments to ENTRYPOINT',
      'Both commands run sequentially',
      'An error occurs',
    ],
    correctAnswer: 1,
    explanation: 'When both are defined in exec form, ENTRYPOINT is the executable and CMD provides the default arguments. Arguments passed to `docker run` override CMD but not ENTRYPOINT.',
    codeSnippet: 'ENTRYPOINT ["python"]\nCMD ["app.py"]\n# Container runs: python app.py\n# docker run myimage test.py => python test.py',
    tags: ['entrypoint', 'cmd', 'combination'],
  },
  {
    id: 'docker-dockerfile-l1-048',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Exec vs Shell Form',
    question: 'Why is exec form preferred over shell form for CMD and ENTRYPOINT?',
    options: [
      'Exec form is easier to write',
      'Exec form runs the process directly as PID 1, receiving Unix signals properly',
      'Shell form is deprecated',
      'Exec form supports more options',
    ],
    correctAnswer: 1,
    explanation: 'Exec form runs the process directly as PID 1, which properly receives signals like SIGTERM for graceful shutdown. Shell form wraps the command in `/bin/sh -c`, making the shell PID 1 instead.',
    codeSnippet: '# Exec form: node is PID 1, receives SIGTERM\nCMD ["node", "server.js"]\n\n# Shell form: sh is PID 1, node may not get SIGTERM\nCMD node server.js',
    tags: ['exec-form', 'shell-form', 'pid1'],
  },
  {
    id: 'docker-dockerfile-l1-049',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What does `COPY --from=0` mean in a multi-stage build?',
    options: [
      'Copy from the root directory',
      'Copy from the first build stage (stage index 0)',
      'Copy zero files',
      'Copy with no permissions',
    ],
    correctAnswer: 1,
    explanation: 'In a multi-stage build, stages can be referenced by their index number (starting from 0). `COPY --from=0` copies from the first stage.',
    codeSnippet: 'FROM golang:1.22\nRUN go build -o app\n\nFROM alpine\nCOPY --from=0 /go/app /usr/local/bin/',
    tags: ['copy', 'multi-stage', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-050',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'FROM',
    question: 'What does `FROM python:3.12-slim` mean?',
    options: [
      'Installs a slim version of Python without standard libraries',
      'Uses a smaller Debian-based Python image with fewer pre-installed packages',
      'Trims the Python image after building',
      'Uses Python with memory limits',
    ],
    correctAnswer: 1,
    explanation: 'The `-slim` variant is a Debian-based image with only the essential packages needed to run Python. It is smaller than the full image but larger than Alpine variants.',
    codeSnippet: 'FROM python:3.12-slim',
    tags: ['from', 'slim', 'images'],
  },
  {
    id: 'docker-dockerfile-l1-051',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'Can COPY copy files from outside the build context?',
    options: [
      'Yes, using absolute host paths',
      'No, COPY can only access files within the build context',
      'Yes, using ../parent syntax',
      'Yes, using URL paths',
    ],
    correctAnswer: 1,
    explanation: 'COPY can only access files that are within the build context (the directory specified in `docker build`). Files outside the context are not sent to the Docker daemon.',
    tags: ['copy', 'build-context', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-052',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ADD',
    question: 'What happens when ADD is used with a tar archive?',
    options: [
      'It copies the archive as-is',
      'It automatically extracts the archive contents into the destination',
      'It compresses the archive further',
      'It validates the archive integrity',
    ],
    correctAnswer: 1,
    explanation: 'ADD automatically extracts recognized tar archives (gzip, bzip2, xz) into the destination directory. This is one of the key differences from COPY.',
    codeSnippet: '# Archive is extracted into /app/\nADD app-files.tar.gz /app/',
    tags: ['add', 'tar', 'extraction'],
  },
  {
    id: 'docker-dockerfile-l1-053',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'STOPSIGNAL',
    question: 'What does the STOPSIGNAL instruction do?',
    options: [
      'Stops the build process',
      'Sets the system call signal that will be sent to the container to stop it',
      'Defines a stop timeout',
      'Prevents the container from being stopped',
    ],
    correctAnswer: 1,
    explanation: 'STOPSIGNAL sets the signal that Docker sends to stop the container. The default is SIGTERM. Some applications need a different signal for graceful shutdown.',
    codeSnippet: 'STOPSIGNAL SIGQUIT',
    tags: ['stopsignal', 'signals', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-054',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you copy dependency files before the rest of the source code?',
    options: [
      'Docker requires dependencies to be installed first',
      'To leverage Docker build cache — dependency files change less often than source code',
      'To ensure dependencies are available for the COPY command',
      'To reduce the image size',
    ],
    correctAnswer: 1,
    explanation: 'By copying dependency files (package.json, requirements.txt) first and installing dependencies before copying source code, the dependency installation layer is cached and reused when only source code changes.',
    codeSnippet: 'COPY package.json package-lock.json ./\nRUN npm ci\n# Source code changes don\'t invalidate npm install cache\nCOPY . .',
    tags: ['cache', 'optimization', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-055',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Which base image strategy results in the smallest image?',
    options: [
      'Using ubuntu:latest as the base',
      'Using Alpine-based or distroless images',
      'Using the full Debian image',
      'Using Windows Server Core',
    ],
    correctAnswer: 1,
    explanation: 'Alpine-based images (~5MB) and distroless images are much smaller than full Debian/Ubuntu images (~100-200MB), resulting in smaller final images and faster deployments.',
    tags: ['base-image', 'alpine', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l1-056',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'Why should you clean up package manager caches in the same RUN instruction?',
    options: [
      'To speed up the build',
      'Because files deleted in a later layer still exist in the previous layer, wasting space',
      'To prevent security vulnerabilities',
      'Docker requires it',
    ],
    correctAnswer: 1,
    explanation: 'Each RUN creates a layer. If you install packages in one RUN and clean up in another, the package files still exist in the first layer. Clean up in the same RUN to save space.',
    codeSnippet: '# Good: cleanup in same layer\nRUN apt-get update && \\\n    apt-get install -y curl && \\\n    rm -rf /var/lib/apt/lists/*',
    tags: ['run', 'cleanup', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l1-057',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Should you pin specific versions for base images and packages?',
    options: [
      'No, always use latest for the newest features',
      'Yes, pinning versions ensures reproducible builds',
      'Only for production images',
      'Only for base images, not packages',
    ],
    correctAnswer: 1,
    explanation: 'Pinning specific versions for base images and packages ensures reproducible builds. Using `latest` or unpinned versions can lead to unexpected changes when images are rebuilt.',
    codeSnippet: 'FROM node:20.11-alpine3.19\nRUN apk add --no-cache curl=8.5.0-r0',
    tags: ['versioning', 'reproducible', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-058',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'WORKDIR',
    question: 'Why should you use WORKDIR instead of `RUN cd /app`?',
    options: [
      'RUN cd is not valid in a Dockerfile',
      'WORKDIR persists for subsequent instructions; RUN cd only affects that single RUN instruction',
      'WORKDIR is faster than cd',
      'There is no difference',
    ],
    correctAnswer: 1,
    explanation: 'Each RUN instruction starts in a new shell, so `RUN cd /app` has no effect on subsequent instructions. WORKDIR changes the directory for all following instructions.',
    codeSnippet: '# Bad: cd has no lasting effect\nRUN cd /app\nRUN pwd  # still in /\n\n# Good: WORKDIR persists\nWORKDIR /app\nRUN pwd  # in /app',
    tags: ['workdir', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-059',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Simple Dockerfile',
    question: 'Which Dockerfile creates a simple static website with nginx?',
    options: [
      'FROM nginx\nRUN start nginx',
      'FROM nginx:alpine\nCOPY ./html /usr/share/nginx/html\nEXPOSE 80',
      'INSTALL nginx\nADD html\nPORT 80',
      'FROM alpine\nRUN nginx\nCOPY html',
    ],
    correctAnswer: 1,
    explanation: 'A simple nginx Dockerfile uses the official nginx image, copies static files to the default web root, and documents the exposed port.',
    codeSnippet: 'FROM nginx:alpine\nCOPY ./html /usr/share/nginx/html\nEXPOSE 80',
    tags: ['dockerfile', 'nginx', 'example'],
  },
  {
    id: 'docker-dockerfile-l1-060',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Instructions',
    question: 'Are Dockerfile instructions case-sensitive?',
    options: [
      'Yes, they must be lowercase',
      'No, but convention is to use uppercase for instructions',
      'Yes, they must be uppercase',
      'They alternate between upper and lower case',
    ],
    correctAnswer: 1,
    explanation: 'Dockerfile instructions are not case-sensitive, but the convention is to write them in uppercase (FROM, RUN, COPY, etc.) to distinguish them from arguments.',
    tags: ['syntax', 'convention', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-061',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What does the trailing slash mean in `COPY src/ /app/`?',
    options: [
      'It has no meaning',
      'The destination is treated as a directory; the source copies directory contents',
      'It enables recursive copying',
      'It creates a symbolic link',
    ],
    correctAnswer: 1,
    explanation: 'A trailing slash on the destination ensures it is treated as a directory. A trailing slash on the source copies the directory contents (not the directory itself) into the destination.',
    codeSnippet: 'COPY src/ /app/src/',
    tags: ['copy', 'paths', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-062',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ARG',
    question: 'Can ARG have a default value?',
    options: [
      'No, ARG always requires --build-arg',
      'Yes, using ARG NAME=default_value syntax',
      'Only if it is before FROM',
      'Only numeric defaults are allowed',
    ],
    correctAnswer: 1,
    explanation: 'ARG can have a default value that is used when no `--build-arg` is provided. If no default is set and no value is passed, the variable is empty.',
    codeSnippet: 'ARG VERSION=1.0\nARG PORT=8080',
    tags: ['arg', 'defaults', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-063',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENV',
    question: 'Can ENV values be overridden at runtime?',
    options: [
      'No, ENV values are baked into the image',
      'Yes, using the -e flag or --env-file when running the container',
      'Only if the ENV has a default value',
      'Only with Docker Compose',
    ],
    correctAnswer: 1,
    explanation: 'ENV values set in the Dockerfile serve as defaults. They can be overridden at runtime using `docker run -e VAR=value` or `--env-file`.',
    codeSnippet: '# Dockerfile\nENV PORT=3000\n\n# Override at runtime\n# docker run -e PORT=8080 myapp',
    tags: ['env', 'override', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-064',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'USER',
    question: 'Which instruction creates a non-root user in a Dockerfile?',
    options: [
      'USER create appuser',
      'RUN useradd or RUN adduser followed by USER',
      'ADDUSER appuser',
      'CREATEUSER appuser',
    ],
    correctAnswer: 1,
    explanation: 'You first create the user with a RUN instruction (using `useradd`, `adduser`, or `addgroup`/`adduser` on Alpine), then switch to it with the USER instruction.',
    codeSnippet: '# Debian/Ubuntu\nRUN useradd --create-home --shell /bin/bash appuser\nUSER appuser\n\n# Alpine\nRUN adduser -D appuser\nUSER appuser',
    tags: ['user', 'non-root', 'security'],
  },
  {
    id: 'docker-dockerfile-l1-065',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the recommended order of instructions in a Dockerfile?',
    options: [
      'CMD first, then FROM, then RUN',
      'FROM, then least-changing instructions, then most-changing instructions, then CMD',
      'Any order works; Docker sorts them automatically',
      'RUN first, then COPY, then FROM',
    ],
    correctAnswer: 1,
    explanation: 'Place FROM first, then instructions that change infrequently (install OS packages, set up users), then dependency installation, then frequently changing source code, and finally CMD/ENTRYPOINT.',
    tags: ['instruction-order', 'cache', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-066',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'FROM',
    question: 'How do you name a build stage in a Dockerfile?',
    options: [
      'FROM image NAME builder',
      'FROM image AS builder',
      'FROM image --name builder',
      'STAGE builder FROM image',
    ],
    correctAnswer: 1,
    explanation: 'The `AS` keyword after FROM names the build stage. This name can be used in COPY --from to reference the stage.',
    codeSnippet: 'FROM golang:1.22 AS builder\nRUN go build -o app\n\nFROM alpine\nCOPY --from=builder /app /usr/local/bin/',
    tags: ['from', 'stages', 'naming'],
  },
  {
    id: 'docker-dockerfile-l1-067',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'CMD',
    question: 'What does `CMD ["npm", "start"]` do?',
    options: [
      'Installs npm and runs start during build',
      'Runs `npm start` when the container starts, using exec form',
      'Downloads npm packages at runtime',
      'Starts the npm registry server',
    ],
    correctAnswer: 1,
    explanation: 'This exec form CMD runs `npm start` as the default command when a container is created from the image. The npm process becomes PID 1 in the container.',
    codeSnippet: 'CMD ["npm", "start"]',
    tags: ['cmd', 'npm', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-068',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What does `COPY package*.json ./` copy?',
    options: [
      'Only package.json',
      'All files matching the pattern package*.json (like package.json and package-lock.json)',
      'All JSON files',
      'The package directory',
    ],
    correctAnswer: 1,
    explanation: 'The wildcard `*` matches any characters. `package*.json` matches `package.json`, `package-lock.json`, and any other file matching that pattern.',
    codeSnippet: 'COPY package*.json ./',
    tags: ['copy', 'wildcards', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-069',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'What is `RUN npm ci` used for in a Dockerfile?',
    options: [
      'Runs continuous integration tests',
      'Installs dependencies from package-lock.json in a clean, reproducible way',
      'Initializes a new npm project',
      'Compiles the application',
    ],
    correctAnswer: 1,
    explanation: '`npm ci` (clean install) installs dependencies exactly as specified in package-lock.json. It is faster and more reliable than `npm install` for CI and Docker builds.',
    codeSnippet: 'COPY package.json package-lock.json ./\nRUN npm ci --production',
    tags: ['run', 'npm', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-070',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Should you use `apt-get upgrade` in a Dockerfile?',
    options: [
      'Yes, always upgrade for security',
      'Generally no; update the base image tag instead for more predictable builds',
      'Only on Fridays',
      'Only with the --force flag',
    ],
    correctAnswer: 1,
    explanation: 'Using `apt-get upgrade` can lead to unpredictable builds since different package versions may be installed at different times. Instead, use a newer base image tag for security updates.',
    tags: ['apt-get', 'upgrade', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-071',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENTRYPOINT',
    question: 'How do you override ENTRYPOINT at runtime?',
    options: [
      'You cannot override ENTRYPOINT',
      'Using the --entrypoint flag in docker run',
      'By passing arguments after the image name',
      'By setting an environment variable',
    ],
    correctAnswer: 1,
    explanation: 'While ENTRYPOINT is normally fixed, you can override it using `docker run --entrypoint <command>`. Arguments after the image name in `docker run` override CMD, not ENTRYPOINT.',
    codeSnippet: 'docker run --entrypoint /bin/sh myimage',
    tags: ['entrypoint', 'override', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-072',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'RUN',
    question: 'What does `rm -rf /var/lib/apt/lists/*` do in a Dockerfile RUN instruction?',
    options: [
      'Removes all installed packages',
      'Removes the apt package cache to reduce image size',
      'Removes the apt configuration',
      'Removes the apt repository sources',
    ],
    correctAnswer: 1,
    explanation: 'Removing `/var/lib/apt/lists/*` clears the package index cache created by `apt-get update`, reducing the image layer size.',
    codeSnippet: 'RUN apt-get update && \\\n    apt-get install -y curl && \\\n    rm -rf /var/lib/apt/lists/*',
    tags: ['run', 'cleanup', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l1-073',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What is the purpose of using `-y` flag with `apt-get install` in Dockerfiles?',
    options: [
      'Enables YAML output',
      'Automatically answers yes to prompts, allowing non-interactive installation',
      'Installs yearly updates',
      'Enables verbose output',
    ],
    correctAnswer: 1,
    explanation: 'The `-y` flag automatically answers "yes" to all prompts during package installation. Without it, the build would hang waiting for user input.',
    codeSnippet: 'RUN apt-get install -y curl vim git',
    tags: ['apt-get', 'non-interactive', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-074',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ONBUILD',
    question: 'What does the ONBUILD instruction do?',
    options: [
      'Runs a command when the image is built',
      'Adds a trigger instruction that executes when this image is used as a base for another build',
      'Builds the image automatically on push',
      'Schedules a build for later execution',
    ],
    correctAnswer: 1,
    explanation: 'ONBUILD adds a trigger instruction to the image that is executed when the image is used as the base image in another Dockerfile FROM instruction.',
    codeSnippet: '# Base image Dockerfile\nONBUILD COPY . /app\nONBUILD RUN npm install',
    tags: ['onbuild', 'triggers', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-075',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Simple Dockerfile',
    question: 'Which Dockerfile creates a Go application image?',
    options: [
      'FROM golang\nRUN go run main.go',
      'FROM golang:1.22\nWORKDIR /app\nCOPY go.mod go.sum ./\nRUN go mod download\nCOPY . .\nRUN go build -o main .\nCMD ["./main"]',
      'INSTALL golang\nBUILD main.go',
      'FROM go\nMAKE main.go',
    ],
    correctAnswer: 1,
    explanation: 'A proper Go Dockerfile sets WORKDIR, copies module files first for dependency caching, downloads modules, copies source, builds the binary, and sets the CMD.',
    codeSnippet: 'FROM golang:1.22\nWORKDIR /app\nCOPY go.mod go.sum ./\nRUN go mod download\nCOPY . .\nRUN go build -o main .\nCMD ["./main"]',
    tags: ['dockerfile', 'golang', 'example'],
  },
  {
    id: 'docker-dockerfile-l1-076',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why is `COPY . .` often the last COPY instruction?',
    options: [
      'Docker requires it to be last',
      'Source code changes frequently, so placing it last maximizes cache utilization for earlier layers',
      'It must follow all RUN instructions',
      'It is a convention with no practical benefit',
    ],
    correctAnswer: 1,
    explanation: 'Since source code changes on almost every build, placing `COPY . .` last ensures that dependency installation and other setup layers above it remain cached.',
    tags: ['copy', 'cache', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-077',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Build',
    question: 'How do you specify a different Dockerfile name when building?',
    options: [
      'docker build --name Dockerfile.prod .',
      'docker build -f Dockerfile.prod .',
      'docker build --dockerfile Dockerfile.prod .',
      'docker build Dockerfile.prod .',
    ],
    correctAnswer: 1,
    explanation: 'The `-f` (or `--file`) flag specifies the Dockerfile to use when it is not named `Dockerfile` or is in a different location.',
    codeSnippet: 'docker build -f Dockerfile.prod -t myapp:prod .',
    tags: ['docker-build', 'filename', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-078',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ADD',
    question: 'Can ADD download files from a URL?',
    options: [
      'No, only COPY can access URLs',
      'Yes, ADD can fetch files from a remote URL',
      'Only with BuildKit enabled',
      'Only HTTPS URLs',
    ],
    correctAnswer: 1,
    explanation: 'ADD can download files from a URL and place them in the image. However, using `RUN curl` or `RUN wget` is often preferred as it allows you to clean up in the same layer.',
    codeSnippet: 'ADD https://example.com/config.json /app/config.json',
    tags: ['add', 'url', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-079',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ENV',
    question: 'What is the scope of ENV variables in a Dockerfile?',
    options: [
      'Only the next instruction',
      'From the point of declaration through all subsequent instructions and the running container',
      'Only during the build phase',
      'Only in RUN instructions',
    ],
    correctAnswer: 1,
    explanation: 'ENV variables are available from the line they are declared through all subsequent build instructions AND persist as environment variables in the running container.',
    codeSnippet: 'ENV APP_HOME=/app\n# Available in all following instructions\nWORKDIR $APP_HOME\nRUN echo $APP_HOME\n# Also available in the running container',
    tags: ['env', 'scope', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-080',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Instructions',
    question: 'What instruction is used to set the default shell in a Dockerfile?',
    options: [
      'DEFAULT_SHELL',
      'SHELL',
      'SET SHELL',
      'USE SHELL',
    ],
    correctAnswer: 1,
    explanation: 'The SHELL instruction changes the default shell used for the shell form of RUN, CMD, and ENTRYPOINT instructions.',
    codeSnippet: 'SHELL ["/bin/bash", "-c"]',
    tags: ['shell', 'instruction', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-081',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK',
    question: 'What does the HEALTHCHECK instruction do?',
    options: [
      'Checks if the Dockerfile is valid',
      'Tells Docker how to test if the container is still working',
      'Monitors host health',
      'Checks image integrity',
    ],
    correctAnswer: 1,
    explanation: 'HEALTHCHECK defines a command that Docker runs periodically to check if the container application is still healthy and responding correctly.',
    codeSnippet: 'HEALTHCHECK CMD curl -f http://localhost/ || exit 1',
    tags: ['healthcheck', 'monitoring', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-082',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should you avoid installing unnecessary packages in a Dockerfile?',
    options: [
      'Docker limits the number of packages',
      'Extra packages increase image size, build time, and attack surface',
      'It causes permission issues',
      'Docker Compose does not support extra packages',
    ],
    correctAnswer: 1,
    explanation: 'Every additional package increases the image size, extends build time, and enlarges the attack surface by introducing more potential vulnerabilities.',
    tags: ['optimization', 'security', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-083',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'COPY',
    question: 'What happens if the COPY destination directory does not exist?',
    options: [
      'The build fails',
      'Docker creates the directory automatically',
      'Files are copied to the root directory',
      'A warning is shown',
    ],
    correctAnswer: 1,
    explanation: 'If the destination path does not exist, Docker creates it along with any missing parent directories, similar to `mkdir -p`.',
    codeSnippet: 'COPY app.py /opt/myapp/  # /opt/myapp/ is created if it does not exist',
    tags: ['copy', 'directories', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-084',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'ARG vs ENV',
    question: 'How do you convert an ARG value to an ENV value?',
    options: [
      'ARG values are automatically available as ENV',
      'Use ENV to assign the ARG value: ENV MY_VAR=$MY_ARG',
      'Use CONVERT ARG TO ENV',
      'ARG and ENV cannot be combined',
    ],
    correctAnswer: 1,
    explanation: 'To persist a build-time ARG value into the running container, assign it to an ENV variable: `ARG VERSION` followed by `ENV APP_VERSION=$VERSION`.',
    codeSnippet: 'ARG VERSION=1.0\nENV APP_VERSION=$VERSION',
    tags: ['arg', 'env', 'conversion'],
  },
  {
    id: 'docker-dockerfile-l1-085',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'What should you avoid storing in a Docker image?',
    options: [
      'Application code',
      'Secrets, credentials, and sensitive configuration',
      'Dependency files',
      'Static assets',
    ],
    correctAnswer: 1,
    explanation: 'Never store secrets, passwords, API keys, or credentials in Docker images. They can be extracted from image layers. Use runtime secrets, environment variables, or mounted secret files instead.',
    tags: ['security', 'secrets', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-086',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Build',
    question: 'What does `docker build -t myapp:v1 .` produce?',
    options: [
      'A running container named myapp:v1',
      'An image tagged myapp:v1',
      'A Dockerfile named myapp',
      'A Docker Compose file',
    ],
    correctAnswer: 1,
    explanation: 'This command builds an image from the Dockerfile in the current directory and tags it as `myapp:v1`. The image is stored locally and can be used to create containers.',
    codeSnippet: 'docker build -t myapp:v1 .',
    tags: ['docker-build', 'tags', 'basics'],
  },
  {
    id: 'docker-dockerfile-l1-087',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Best Practices',
    question: 'Why should each container run only one process?',
    options: [
      'Docker cannot handle multiple processes',
      'Single-process containers are easier to scale, monitor, and manage independently',
      'Docker licensing limits one process per container',
      'Multiple processes cause networking conflicts',
    ],
    correctAnswer: 1,
    explanation: 'Running one process per container follows the separation of concerns principle. It makes containers easier to scale horizontally, monitor, restart, and update independently.',
    tags: ['single-process', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l1-088',
    language: 'docker',
    level: 'level1',
    category: 'Dockerfile',
    subcategory: 'Simple Dockerfile',
    question: 'Which Dockerfile creates a Java application image?',
    options: [
      'FROM java:latest\nRUN java app.jar',
      'FROM eclipse-temurin:21-jre\nWORKDIR /app\nCOPY target/app.jar app.jar\nEXPOSE 8080\nCMD ["java", "-jar", "app.jar"]',
      'INSTALL java\nBUILD app.jar',
      'FROM jdk\nSTART app.jar',
    ],
    correctAnswer: 1,
    explanation: 'A Java runtime Dockerfile uses a JRE base image (not JDK for smaller size), copies the built JAR file, exposes the port, and runs it with the java command.',
    codeSnippet: 'FROM eclipse-temurin:21-jre\nWORKDIR /app\nCOPY target/app.jar app.jar\nEXPOSE 8080\nCMD ["java", "-jar", "app.jar"]',
    tags: ['dockerfile', 'java', 'example'],
  },
];

export default questions;
