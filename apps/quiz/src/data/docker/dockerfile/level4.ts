import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'docker-dockerfile-l4-001',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Internals',
    question: 'What is the Dockerfile frontend in BuildKit architecture?',
    options: [
      'A web-based Dockerfile editor',
      'A component that parses Dockerfiles into LLB (Low-Level Build) graph representation',
      'The Docker CLI',
      'A syntax highlighter for Dockerfiles',
    ],
    correctAnswer: 1,
    explanation: 'The Dockerfile frontend (gateway) is a BuildKit component that converts Dockerfile instructions into an LLB DAG. It runs as a container itself, allowing version updates via the # syntax directive.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\n# This line tells BuildKit which frontend version to use',
    tags: ['buildkit', 'frontend', 'llb'],
  },
  {
    id: 'docker-dockerfile-l4-002',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Internals',
    question: 'How does BuildKit optimize multi-stage build execution?',
    options: [
      'It runs all stages sequentially',
      'It analyzes the LLB dependency graph and executes independent stages concurrently',
      'It skips unused stages automatically',
      'Both concurrent execution of independent stages and skipping of unreferenced stages',
    ],
    correctAnswer: 3,
    explanation: 'BuildKit performs two key optimizations: (1) concurrent execution of independent stages that do not depend on each other, and (2) skipping stages that are not referenced by the target stage.',
    tags: ['buildkit', 'optimization', 'parallel'],
  },
  {
    id: 'docker-dockerfile-l4-003',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Build Secrets',
    question: 'What is the security model of BuildKit secrets?',
    options: [
      'Secrets are encrypted in image layers',
      'Secrets are mounted via tmpfs, never written to disk, not included in build cache, and not visible in image history',
      'Secrets are stored in a Docker vault',
      'Secrets are passed through encrypted environment variables',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit secrets are mounted as tmpfs, written only to memory, excluded from the build cache, never committed to any image layer, and not visible in docker history.',
    codeSnippet: 'RUN --mount=type=secret,id=aws,target=/root/.aws/credentials \\\n    aws s3 cp s3://bucket/data /app/data',
    tags: ['secrets', 'security', 'tmpfs'],
  },
  {
    id: 'docker-dockerfile-l4-004',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Build Secrets',
    question: 'How do you use Docker build secrets with multiple secret files?',
    options: [
      'Only one secret per build is allowed',
      'Pass multiple --secret flags, each with a unique id, and mount them separately in RUN instructions',
      'Combine all secrets into one file',
      'Use --secrets-dir flag',
    ],
    correctAnswer: 1,
    explanation: 'Multiple secrets can be passed by using multiple `--secret` flags with unique IDs. Each secret is mounted independently in the RUN instructions that need it.',
    codeSnippet: 'RUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n    --mount=type=secret,id=ssh_key,target=/root/.ssh/id_rsa \\\n    npm ci && git clone git@github.com:private/repo.git\n\n# docker build \\\n#   --secret id=npmrc,src=.npmrc \\\n#   --secret id=ssh_key,src=~/.ssh/id_rsa .',
    tags: ['secrets', 'multiple', 'buildkit'],
  },
  {
    id: 'docker-dockerfile-l4-005',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Cross-compilation',
    question: 'How do you implement efficient Go cross-compilation in Docker?',
    options: [
      'Use QEMU emulation for each platform',
      'Use FROM --platform=$BUILDPLATFORM to run the compiler natively and set GOOS/GOARCH for the target',
      'Build on separate machines for each architecture',
      'Use a pre-compiled binary',
    ],
    correctAnswer: 1,
    explanation: 'Go supports cross-compilation natively. By running the builder on the host platform ($BUILDPLATFORM), the compiler executes at native speed while producing binaries for the target architecture.',
    codeSnippet: 'FROM --platform=$BUILDPLATFORM golang:1.22 AS builder\nARG TARGETOS TARGETARCH\nWORKDIR /app\nCOPY . .\nRUN CGO_ENABLED=0 GOOS=$TARGETOS GOARCH=$TARGETARCH \\\n    go build -ldflags="-w -s" -o /app/server\n\nFROM --platform=$TARGETPLATFORM alpine:3.19\nCOPY --from=builder /app/server /usr/local/bin/\nCMD ["server"]',
    tags: ['cross-compilation', 'go', 'multi-platform'],
  },
  {
    id: 'docker-dockerfile-l4-006',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Cross-compilation',
    question: 'How do you cross-compile Rust applications in Docker?',
    options: [
      'Use QEMU for all builds',
      'Add the target architecture toolchain and use cargo build --target with the cross-compilation target',
      'Build on native ARM hardware',
      'Use a Rust cross Docker image',
    ],
    correctAnswer: 1,
    explanation: 'Rust supports cross-compilation via rustup targets. Install the target triple, configure the linker, and build with `--target`. This is much faster than QEMU emulation.',
    codeSnippet: 'FROM --platform=$BUILDPLATFORM rust:1.77 AS builder\nARG TARGETARCH\nRUN case $TARGETARCH in \\\n    amd64) TARGET=x86_64-unknown-linux-musl ;; \\\n    arm64) TARGET=aarch64-unknown-linux-musl ;; \\\n    esac && \\\n    rustup target add $TARGET && \\\n    cargo build --release --target $TARGET',
    tags: ['cross-compilation', 'rust', 'multi-platform'],
  },
  {
    id: 'docker-dockerfile-l4-007',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Cross-compilation',
    question: 'What is the role of `FROM --platform=$BUILDPLATFORM` in cross-compilation?',
    options: [
      'Sets the target platform for the final image',
      'Ensures the build stage runs natively on the host, avoiding QEMU emulation for compilation steps',
      'Pulls the latest platform-specific image',
      'Configures the container runtime platform',
    ],
    correctAnswer: 1,
    explanation: '`FROM --platform=$BUILDPLATFORM` pins the build stage to the native host platform. The compiler runs at native speed and cross-compiles for the target platform, avoiding slow QEMU emulation.',
    codeSnippet: '# Builder runs natively on the build host\nFROM --platform=$BUILDPLATFORM golang:1.22 AS builder\n\n# Final image targets the destination platform\nFROM --platform=$TARGETPLATFORM alpine:3.19',
    tags: ['cross-compilation', 'buildplatform', 'performance'],
  },
  {
    id: 'docker-dockerfile-l4-008',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'OCI Compliance',
    question: 'How do you build OCI-compliant images with Docker?',
    options: [
      'Docker images are always OCI-compliant',
      'Use docker buildx build with --output type=oci or type=image with oci-mediatypes=true',
      'Use a special OCI Dockerfile',
      'Install an OCI plugin',
    ],
    correctAnswer: 1,
    explanation: 'Docker can output images in OCI format using buildx. The `oci-mediatypes=true` option ensures the image uses OCI media types instead of Docker-specific ones.',
    codeSnippet: '# Export as OCI tar\ndocker buildx build --output type=oci,dest=image.tar .\n\n# Push with OCI media types\ndocker buildx build --output type=image,oci-mediatypes=true --push -t myapp .',
    tags: ['oci', 'buildx', 'compliance'],
  },
  {
    id: 'docker-dockerfile-l4-009',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'OCI Compliance',
    question: 'What is the OCI image config JSON?',
    options: [
      'A Docker Compose configuration',
      'A JSON document describing the image runtime configuration including env vars, entrypoint, volumes, and layer history',
      'A registry authentication config',
      'A build configuration file',
    ],
    correctAnswer: 1,
    explanation: 'The OCI image config is a JSON document containing runtime configuration (environment, entrypoint, cmd, working directory, user), layer diff IDs, and build history.',
    tags: ['oci', 'config', 'specification'],
  },
  {
    id: 'docker-dockerfile-l4-010',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you use BuildKit with a custom Dockerfile frontend?',
    options: [
      'Install a plugin',
      'Set the # syntax directive to a custom frontend image that implements the BuildKit gateway API',
      'Use a different Dockerfile format',
      'Configure in daemon.json',
    ],
    correctAnswer: 1,
    explanation: 'Custom frontends are container images implementing the BuildKit gateway API. The `# syntax=` directive tells BuildKit to use that image for parsing, enabling custom build definition formats.',
    codeSnippet: '# syntax=my-custom-frontend:latest\n# This Dockerfile uses a custom syntax/parser',
    tags: ['buildkit', 'frontend', 'custom'],
  },
  {
    id: 'docker-dockerfile-l4-011',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is BuildKit garbage collection?',
    options: [
      'Removing unused containers',
      'Automatic cleanup of build cache based on configurable size limits and age policies',
      'Deleting old Dockerfiles',
      'Removing unused images',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit has a built-in garbage collector that automatically removes build cache entries based on configured maximum size, minimum free space, and keep duration policies.',
    codeSnippet: '# buildkitd.toml\n[worker.oci]\n  gc = true\n  gckeepstorage = 10000 # MB\n  [[worker.oci.gcpolicy]]\n    keepBytes = 5000000000\n    keepDuration = 604800 # 7 days',
    tags: ['buildkit', 'gc', 'cache'],
  },
  {
    id: 'docker-dockerfile-l4-012',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you debug BuildKit cache behavior?',
    options: [
      'Use docker build --verbose',
      'Use --progress=plain output and docker buildx du to inspect cache usage',
      'Check /var/lib/docker/cache',
      'Use docker cache inspect',
    ],
    correctAnswer: 1,
    explanation: '`--progress=plain` shows whether each step was cached. `docker buildx du` shows cache disk usage by type. `docker buildx prune --verbose` shows what would be cleaned.',
    codeSnippet: 'docker buildx build --progress=plain -t myapp .\ndocker buildx du\ndocker buildx prune --verbose --dry-run',
    tags: ['buildkit', 'cache', 'debugging'],
  },
  {
    id: 'docker-dockerfile-l4-013',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Supply Chain',
    question: 'What is SLSA level 3 provenance for Docker builds?',
    options: [
      'A logging level',
      'A supply chain security level requiring a non-falsifiable build environment with verifiable provenance attestation',
      'A Docker subscription tier',
      'A caching strategy level',
    ],
    correctAnswer: 1,
    explanation: 'SLSA L3 requires builds to run on a hardened, non-falsifiable build platform that generates signed provenance automatically. GitHub Actions with BuildKit can achieve this level.',
    codeSnippet: '# GitHub Actions with SLSA L3 provenance\n- uses: docker/build-push-action@v5\n  with:\n    provenance: true\n    sbom: true',
    tags: ['slsa', 'provenance', 'supply-chain'],
  },
  {
    id: 'docker-dockerfile-l4-014',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Supply Chain',
    question: 'How do you verify the provenance of a Docker image?',
    options: [
      'Check the Dockerfile',
      'Use docker buildx imagetools inspect to view attestations and cosign/notation to verify signatures',
      'Read the image labels',
      'Check Docker Hub download count',
    ],
    correctAnswer: 1,
    explanation: 'Use `docker buildx imagetools inspect` to view embedded attestations (provenance, SBOM). Verification tools like cosign and notation can cryptographically verify the signatures.',
    codeSnippet: '# View attestations\ndocker buildx imagetools inspect myapp:latest --format \'{{json .Provenance}}\'\n\n# Verify with cosign\ncosign verify-attestation --type slsaprovenance myapp:latest',
    tags: ['provenance', 'verification', 'cosign'],
  },
  {
    id: 'docker-dockerfile-l4-015',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Supply Chain',
    question: 'What are in-toto attestations in Docker images?',
    options: [
      'Italian language labels',
      'Cryptographically signed statements about the software supply chain conforming to the in-toto specification',
      'Internal Docker metadata',
      'Image integrity checksums',
    ],
    correctAnswer: 1,
    explanation: 'in-toto is a framework for securing software supply chains. Docker attestations (provenance, SBOM) use the in-toto format with cryptographic signatures for verifiable supply chain metadata.',
    tags: ['in-toto', 'attestations', 'supply-chain'],
  },
  {
    id: 'docker-dockerfile-l4-016',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Reproducible Builds',
    question: 'What are the key requirements for reproducible Docker builds?',
    options: [
      'Using the same Docker version',
      'Pinning base image by digest, locking dependency versions, setting SOURCE_DATE_EPOCH, and disabling non-deterministic operations',
      'Building on the same machine',
      'Using the same network',
    ],
    correctAnswer: 1,
    explanation: 'Reproducible builds require: (1) base image pinned by digest, (2) all dependencies version-locked, (3) SOURCE_DATE_EPOCH for timestamps, (4) deterministic build steps.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nFROM node:20-alpine@sha256:abc123... AS builder\nENV SOURCE_DATE_EPOCH=0\nCOPY package.json package-lock.json ./\nRUN npm ci --ignore-scripts\nCOPY . .\nRUN npm run build',
    tags: ['reproducible', 'deterministic', 'expert'],
  },
  {
    id: 'docker-dockerfile-l4-017',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Reproducible Builds',
    question: 'How does SOURCE_DATE_EPOCH affect Docker builds?',
    options: [
      'Sets the build start time',
      'Sets file modification timestamps and image creation timestamp to a fixed value for reproducibility',
      'Sets the expiration date of the image',
      'Controls when the build cache expires',
    ],
    correctAnswer: 1,
    explanation: 'SOURCE_DATE_EPOCH causes BuildKit to set all file modification times and the image creation timestamp to the specified epoch value, eliminating timestamp-based differences between builds.',
    codeSnippet: '# Use the git commit timestamp for reproducibility\ndocker buildx build \\\n  --build-arg SOURCE_DATE_EPOCH=$(git log -1 --format=%ct) \\\n  -t myapp:latest .',
    tags: ['reproducible', 'timestamp', 'expert'],
  },
  {
    id: 'docker-dockerfile-l4-018',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the `docker-container` driver in Buildx?',
    options: [
      'The default Docker driver',
      'A driver that runs BuildKit inside a dedicated container with full BuildKit features',
      'A container management driver',
      'A Docker Compose driver',
    ],
    correctAnswer: 1,
    explanation: 'The docker-container driver runs a dedicated BuildKit daemon in a container. It supports all BuildKit features including multi-platform builds, cache export/import, and custom configurations.',
    codeSnippet: 'docker buildx create --driver docker-container \\\n  --name container-builder \\\n  --buildkitd-flags \'--oci-worker-no-process-sandbox\' \\\n  --use',
    tags: ['buildx', 'docker-container', 'driver'],
  },
  {
    id: 'docker-dockerfile-l4-019',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you configure BuildKit with a custom buildkitd.toml?',
    options: [
      'Edit daemon.json',
      'Create a buildkitd.toml config file and pass it when creating a builder instance',
      'Set BUILDKIT_CONFIG environment variable',
      'Place it in /etc/buildkit/',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit daemon configuration (workers, GC policies, registry mirrors) is set in buildkitd.toml and can be passed via `--config` when creating a builder or `--buildkitd-config` with docker-container driver.',
    codeSnippet: '# buildkitd.toml\n[registry."docker.io"]\n  mirrors = ["mirror.example.com"]\n[worker.oci]\n  gc = true\n  gckeepstorage = 20000\n\ndocker buildx create --config ./buildkitd.toml --name mybuilder',
    tags: ['buildkit', 'configuration', 'buildkitd'],
  },
  {
    id: 'docker-dockerfile-l4-020',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is BuildKit trace/telemetry support?',
    options: [
      'Docker build logging',
      'OpenTelemetry integration that exports build traces to observability backends like Jaeger',
      'Container tracing',
      'Network packet tracing',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit supports OpenTelemetry for exporting build traces. This provides detailed timing information for each build step, useful for optimizing complex builds.',
    codeSnippet: 'export OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317\ndocker buildx build -t myapp .',
    tags: ['buildkit', 'opentelemetry', 'tracing'],
  },
  {
    id: 'docker-dockerfile-l4-021',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a Dockerfile that supports both development with hot-reload and production?',
    options: [
      'Use two separate Dockerfiles',
      'Use a multi-stage Dockerfile with compose.yaml develop.watch for dev and a minimal production stage',
      'Use Docker volumes for everything',
      'Use environment variables to switch modes',
    ],
    correctAnswer: 1,
    explanation: 'A single Dockerfile with dev and prod stages, combined with Compose `develop.watch` for file synchronization, provides both hot-reload development and optimized production builds.',
    codeSnippet: 'FROM node:20-alpine AS base\nWORKDIR /app\n\nFROM base AS development\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nCMD ["npm", "run", "dev"]\n\nFROM base AS production\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY --from=builder /app/dist ./dist\nCMD ["node", "dist/server.js"]',
    tags: ['dev-prod', 'hot-reload', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-022',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a "builder image" pattern for CI/CD?',
    options: [
      'Build a new image for each CI run',
      'Create a pre-built builder image with all tools/dependencies cached, then use it as the base for CI builds',
      'Use GitHub Actions cache',
      'Build on the CI runner directly',
    ],
    correctAnswer: 1,
    explanation: 'A pre-built builder image contains all build tools, compilers, and cached dependencies. CI builds inherit from this image, significantly reducing build time.',
    codeSnippet: '# builder-base.Dockerfile (built weekly)\nFROM node:20\nRUN npm install -g typescript eslint\nCOPY package*.json /cache/\nRUN cd /cache && npm ci\n\n# app.Dockerfile (built on each commit)\nFROM builder-base:latest\nCOPY --from=builder-base /cache/node_modules ./node_modules\nCOPY . .\nRUN npm run build',
    tags: ['ci-cd', 'builder-image', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-023',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is `docker buildx bake` and how does it improve monorepo builds?',
    options: [
      'A recipe for building images',
      'A command that reads HCL/JSON build definitions for building multiple targets with shared variables and matrix strategies',
      'A caching strategy',
      'A multi-stage build tool',
    ],
    correctAnswer: 1,
    explanation: 'docker buildx bake reads HCL/JSON build definitions that support variables, functions, target groups, and matrix builds. It can build multiple targets in parallel with shared configuration.',
    codeSnippet: '# docker-bake.hcl\nvariable "TAG" { default = "latest" }\n\ngroup "all" {\n  targets = ["api", "web", "worker"]\n}\n\ntarget "api" {\n  context = "./services/api"\n  tags = ["myorg/api:\\${TAG}"]\n  platforms = ["linux/amd64", "linux/arm64"]\n}\n\ntarget "web" {\n  context = "./services/web"\n  tags = ["myorg/web:\\${TAG}"]\n}',
    tags: ['buildx', 'bake', 'hcl'],
  },
  {
    id: 'docker-dockerfile-l4-024',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What are matrix builds in docker buildx bake?',
    options: [
      'Building images in a grid pattern',
      'Building multiple variants of a target using a matrix of variable values',
      'Matrix multiplication of layers',
      'Building on multiple hosts simultaneously',
    ],
    correctAnswer: 1,
    explanation: 'Matrix builds in bake allow generating multiple target variants from a single definition by varying parameters like platform, version, or base image.',
    codeSnippet: '# docker-bake.hcl\ntarget "app" {\n  name = "app-\\${item.version}"\n  matrix = {\n    item = [\n      { version = "20", variant = "alpine" },\n      { version = "22", variant = "slim" },\n    ]\n  }\n  args = {\n    NODE_VERSION = item.version\n    VARIANT = item.variant\n  }\n  tags = ["myapp:\\${item.version}"]\n}',
    tags: ['buildx', 'bake', 'matrix'],
  },
  {
    id: 'docker-dockerfile-l4-025',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement the "golden image" pattern?',
    options: [
      'Use the latest base image',
      'Create a hardened base image with security configs, monitoring agents, and standard tooling that all applications inherit from',
      'Use official Docker images only',
      'Build images with gold-level optimization',
    ],
    correctAnswer: 1,
    explanation: 'A golden image is a standardized, security-hardened base image maintained by the platform team. All application Dockerfiles build FROM this image, ensuring consistent security and compliance.',
    codeSnippet: '# Golden base image\nFROM ubuntu:22.04\nRUN apt-get update && apt-get install -y ca-certificates && \\\n    rm -rf /var/lib/apt/lists/*\nRUN useradd --system --no-create-home app\nCOPY security-configs /etc/security/\nLABEL org.company.golden-image.version="2.0"\n\n# Application uses golden image\nFROM myorg/golden-base:2.0\nCOPY app /app\nUSER app\nCMD ["/app"]',
    tags: ['golden-image', 'security', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-026',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement Dockerfile templating across multiple services?',
    options: [
      'Copy the Dockerfile between services',
      'Use ONBUILD triggers, shared base stages, or external templating tools (envsubst, jsonnet, dhall)',
      'Use Docker Compose for templating',
      'Write a custom Dockerfile parser',
    ],
    correctAnswer: 1,
    explanation: 'For shared Dockerfile patterns across services, use shared base images with ONBUILD, buildx bake with HCL variables, or external templating tools to generate per-service Dockerfiles.',
    codeSnippet: '# Shared base with ONBUILD\nFROM node:20-alpine\nWORKDIR /app\nONBUILD COPY package*.json ./\nONBUILD RUN npm ci\nONBUILD COPY . .\n\n# Per-service Dockerfile\nFROM myorg/node-base:latest\nCMD ["node", "server.js"]',
    tags: ['templating', 'onbuild', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-027',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you implement image signing in a CI/CD pipeline?',
    options: [
      'Docker signs images automatically',
      'Use cosign or notation to sign images after building, with keys stored in a KMS or using keyless signing',
      'Add a signature label to the image',
      'Use Docker Content Trust only',
    ],
    correctAnswer: 1,
    explanation: 'Sign images in CI after building and pushing using cosign (supports keyless with OIDC) or notation. Store keys in cloud KMS (AWS KMS, Azure Key Vault) for security.',
    codeSnippet: '# Keyless signing with cosign in GitHub Actions\n- name: Sign image\n  run: cosign sign --yes myregistry.com/myapp@$DIGEST\n  env:\n    COSIGN_EXPERIMENTAL: "1"',
    tags: ['signing', 'ci-cd', 'cosign'],
  },
  {
    id: 'docker-dockerfile-l4-028',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is image admission control with signed images?',
    options: [
      'Rate limiting image pulls',
      'Policies that verify image signatures before allowing containers to run in a cluster',
      'Access control for Docker Hub',
      'Image size limits',
    ],
    correctAnswer: 1,
    explanation: 'Image admission controllers (Kyverno, OPA Gatekeeper, Connaisseur) intercept container creation requests and verify that images have valid signatures before allowing deployment.',
    tags: ['signing', 'admission-control', 'policy'],
  },
  {
    id: 'docker-dockerfile-l4-029',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you use BuildKit with Kubernetes as a builder?',
    options: [
      'Install Docker on Kubernetes nodes',
      'Create a Buildx builder with the kubernetes driver that runs BuildKit pods in the cluster',
      'Use docker-in-docker pods',
      'Build on the control plane',
    ],
    correctAnswer: 1,
    explanation: 'The kubernetes driver creates BuildKit pods in the cluster for building. This enables scalable, distributed builds without Docker-in-Docker, using the cluster resources.',
    codeSnippet: 'docker buildx create --driver kubernetes \\\n  --driver-opt namespace=buildkit \\\n  --driver-opt replicas=3 \\\n  --name k8s-builder --use',
    tags: ['buildx', 'kubernetes', 'driver'],
  },
  {
    id: 'docker-dockerfile-l4-030',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'How do you profile Docker build performance?',
    options: [
      'Use docker build --profile',
      'Use --progress=plain for timing, OpenTelemetry traces, and docker buildx du for cache analysis',
      'Check build logs for timestamps',
      'Use docker stats during build',
    ],
    correctAnswer: 1,
    explanation: 'Combine `--progress=plain` (shows step durations), OpenTelemetry traces (detailed timing), `docker buildx du` (cache usage), and `--metadata-file` (build metadata) for complete build profiling.',
    codeSnippet: '# Detailed timing output\ndocker buildx build --progress=plain \\\n  --metadata-file metadata.json \\\n  -t myapp:latest .\n\n# Cache analysis\ndocker buildx du --verbose',
    tags: ['performance', 'profiling', 'buildkit'],
  },
  {
    id: 'docker-dockerfile-l4-031',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'How do you optimize Docker build performance in CI/CD?',
    options: [
      'Use the largest CI runner available',
      'Combine registry cache, GitHub Actions cache, multi-stage parallelism, and COPY --link for optimal caching',
      'Build locally and push',
      'Use a single-stage Dockerfile',
    ],
    correctAnswer: 1,
    explanation: 'Optimal CI performance requires: registry or GHA cache for layer persistence, multi-stage builds for parallelism, COPY --link for base-image-independent layers, and efficient .dockerignore.',
    codeSnippet: 'docker buildx build \\\n  --cache-from type=gha \\\n  --cache-to type=gha,mode=max \\\n  --build-arg BUILDKIT_INLINE_CACHE=1 \\\n  -t myapp:latest --push .',
    tags: ['ci-cd', 'performance', 'caching'],
  },
  {
    id: 'docker-dockerfile-l4-032',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you handle database migrations in Docker?',
    options: [
      'Run migrations in the application container',
      'Create a separate migration stage/image and run it as an init container or one-off command',
      'Apply migrations during image build',
      'Use a migration sidecar',
    ],
    correctAnswer: 1,
    explanation: 'A separate migration image ensures migrations run once before the application starts. In Kubernetes, use init containers. In Compose, use a separate one-off service.',
    codeSnippet: 'FROM base AS app\nCMD ["node", "server.js"]\n\nFROM base AS migrate\nCOPY migrations ./migrations\nCMD ["npx", "db-migrate", "up"]\n\n# compose.yaml\n# services:\n#   migrate:\n#     build: { target: migrate }\n#   app:\n#     build: { target: app }\n#     depends_on: { migrate: { condition: service_completed_successfully } }',
    tags: ['migrations', 'pattern', 'multi-stage'],
  },
  {
    id: 'docker-dockerfile-l4-033',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is `RUN --security=insecure` used for?',
    options: [
      'Disabling all security in the container',
      'Running build steps that require elevated privileges, like mounting filesystems or loading kernel modules',
      'Allowing insecure network connections',
      'Disabling image signature verification',
    ],
    correctAnswer: 1,
    explanation: '`RUN --security=insecure` grants elevated privileges during build. It requires `--allow security.insecure` at build time and is used for operations like nested containers or kernel operations.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nRUN --security=insecure cat /proc/self/status | grep CapEff\n\n# Build with explicit permission:\n# docker buildx build --allow security.insecure .',
    tags: ['buildkit', 'security', 'privileged'],
  },
  {
    id: 'docker-dockerfile-l4-034',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you implement a custom Dockerfile linter?',
    options: [
      'Write a regex parser',
      'Build a custom BuildKit frontend that validates instructions before generating LLB, or use the BuildKit check API',
      'Use shell script analysis',
      'Create a Docker plugin',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit supports inline linting via the check directive and custom frontend validators. For external linting, build a tool that parses Dockerfiles using the Dockerfile AST parser library.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\n# check=error=true\n# This enables BuildKit inline linting\n# Build errors on lint warnings\nFROM alpine',
    tags: ['linting', 'buildkit', 'custom'],
  },
  {
    id: 'docker-dockerfile-l4-035',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement conditional instructions in a Dockerfile without if/else?',
    options: [
      'Docker supports IF/ELSE natively',
      'Use RUN with shell conditionals, ARG-based variable substitution, or multi-stage builds with --target',
      'Use docker-compose conditions',
      'Use WHEN instruction',
    ],
    correctAnswer: 1,
    explanation: 'Dockerfiles have no native conditionals. Use shell conditionals in RUN, ARG default values with substitution, or separate stages for different variants selected via --target.',
    codeSnippet: 'ARG INSTALL_DEV_TOOLS=false\nRUN if [ "$INSTALL_DEV_TOOLS" = "true" ]; then \\\n      apk add --no-cache vim curl; \\\n    fi\n\n# Or use multi-stage with --target\nFROM base AS with-tools\nRUN apk add vim curl\n\nFROM base AS without-tools\n# Nothing extra',
    tags: ['conditional', 'patterns', 'expert'],
  },
  {
    id: 'docker-dockerfile-l4-036',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you handle GPU support in Docker images?',
    options: [
      'GPUs are automatically available',
      'Use NVIDIA base images, install CUDA toolkit in the build, and run with --gpus flag',
      'Install GPU drivers at runtime',
      'Mount /dev/gpu',
    ],
    correctAnswer: 1,
    explanation: 'For GPU workloads, use NVIDIA CUDA base images, install application-specific GPU libraries, and run containers with `--gpus` flag or NVIDIA Container Toolkit.',
    codeSnippet: 'FROM nvidia/cuda:12.3.2-runtime-ubuntu22.04\nRUN pip install torch torchvision\nCOPY model.py /app/\nCMD ["python", "/app/model.py"]\n\n# Run: docker run --gpus all myapp',
    tags: ['gpu', 'cuda', 'nvidia'],
  },
  {
    id: 'docker-dockerfile-l4-037',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'OCI Compliance',
    question: 'What are OCI annotations and how do you add them?',
    options: [
      'Comments in the Dockerfile',
      'Standardized metadata in the image manifest/index set via LABEL or --annotation flags',
      'Docker Hub tags',
      'Registry configuration',
    ],
    correctAnswer: 1,
    explanation: 'OCI annotations are key-value metadata in image manifests following the org.opencontainers.image.* convention. Set via LABEL in Dockerfiles or --annotation in buildx.',
    codeSnippet: '# Via Dockerfile\nLABEL org.opencontainers.image.source="https://github.com/org/repo"\n\n# Via buildx\ndocker buildx build \\\n  --annotation "index:org.opencontainers.image.description=My App" \\\n  -t myapp:latest .',
    tags: ['oci', 'annotations', 'metadata'],
  },
  {
    id: 'docker-dockerfile-l4-038',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you create a Docker image that runs WebAssembly?',
    options: [
      'Use FROM wasm:latest',
      'Compile to WASM, use a WASM-compatible base, and configure the runtime via annotations or --platform wasi/wasm',
      'Install a WASM runtime in a Linux container',
      'WASM does not work in Docker',
    ],
    correctAnswer: 1,
    explanation: 'Docker supports WASM workloads via containerd shims. Build the WASM module, use FROM scratch with COPY, and run with the appropriate WASM runtime.',
    codeSnippet: 'FROM rust:1.77 AS builder\nRUN rustup target add wasm32-wasi\nCOPY . .\nRUN cargo build --target wasm32-wasi --release\n\nFROM scratch\nCOPY --from=builder /app/target/wasm32-wasi/release/app.wasm /app.wasm\nENTRYPOINT ["/app.wasm"]',
    tags: ['wasm', 'webassembly', 'expert'],
  },
  {
    id: 'docker-dockerfile-l4-039',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you export build results as a local directory?',
    options: [
      'docker build --output ./output',
      'docker buildx build --output type=local,dest=./output',
      'docker cp from the built image',
      'docker export the build container',
    ],
    correctAnswer: 1,
    explanation: 'The `--output type=local` exports the build result as files to a local directory. This is useful for extracting build artifacts without creating an image.',
    codeSnippet: '# Extract compiled binary to local filesystem\ndocker buildx build --output type=local,dest=./bin --target=binary .\n\n# Extract test results\ndocker buildx build --output type=local,dest=./coverage --target=test-results .',
    tags: ['buildx', 'output', 'local'],
  },
  {
    id: 'docker-dockerfile-l4-040',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement Docker image signing verification in Dockerfiles?',
    options: [
      'Docker verifies signatures automatically',
      'Verify base image signatures in the first build stage using cosign/notation before proceeding with the build',
      'Add a VERIFY instruction',
      'Use only signed base images',
    ],
    correctAnswer: 1,
    explanation: 'You can add a verification step in the first build stage that checks base image signatures using cosign or notation, failing the build if verification fails.',
    codeSnippet: 'FROM alpine AS verifier\nRUN apk add --no-cache cosign\nRUN cosign verify --key cosign.pub mybase:latest\n\nFROM mybase:latest AS builder\n# Only reached if verification passed\nCOPY . /app\nRUN build',
    tags: ['signing', 'verification', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-041',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What are BuildKit workers?',
    options: [
      'Docker Swarm worker nodes',
      'Execution environments (OCI or containerd) that BuildKit uses to run build steps',
      'Background build processes',
      'CI/CD build agents',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit workers are the execution backends that run build operations. BuildKit supports OCI (runc) and containerd workers. Workers handle execution isolation, snapshots, and caching.',
    codeSnippet: '# buildkitd.toml worker configuration\n[worker.oci]\n  enabled = true\n  platforms = ["linux/amd64", "linux/arm64"]\n  snapshotter = "overlayfs"\n  gc = true',
    tags: ['buildkit', 'workers', 'internals'],
  },
  {
    id: 'docker-dockerfile-l4-042',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a Docker build that outputs multiple artifacts?',
    options: [
      'Docker builds can only produce one image',
      'Use buildx bake with multiple targets or --output type=local to extract different artifacts from different stages',
      'Build multiple times',
      'Use Docker Compose build',
    ],
    correctAnswer: 1,
    explanation: 'Use bake to build multiple targets (images) in parallel, or use --output type=local with --target to extract specific artifacts from different stages.',
    codeSnippet: '# docker-bake.hcl\ngroup "all" {\n  targets = ["api-image", "worker-image", "docs"]\n}\n\ntarget "docs" {\n  target = "docs-output"\n  output = ["type=local,dest=./docs"]\n}',
    tags: ['buildx', 'bake', 'multi-output'],
  },
  {
    id: 'docker-dockerfile-l4-043',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you implement policy enforcement for Dockerfiles?',
    options: [
      'Use code review only',
      'Use OPA (Open Policy Agent) or Conftest to evaluate Dockerfiles against custom Rego policies',
      'Docker enforces policies automatically',
      'Use file permissions',
    ],
    correctAnswer: 1,
    explanation: 'Conftest (using OPA/Rego) can evaluate Dockerfiles against custom policies in CI, enforcing rules like requiring specific base images, non-root users, or health checks.',
    codeSnippet: '# policy/dockerfile.rego\npackage main\n\ndeny[msg] {\n  input[i].Cmd == "from"\n  val := input[i].Value[0]\n  not startswith(val, "myorg/")\n  msg := sprintf("base image must be from myorg/ registry, got %s", [val])\n}\n\n# conftest test Dockerfile',
    tags: ['policy', 'opa', 'security'],
  },
  {
    id: 'docker-dockerfile-l4-044',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'What is the impact of image layer compression on pull performance?',
    options: [
      'Compression has no impact',
      'Higher compression reduces transfer size but increases decompression time; zstd offers better speed/ratio than gzip',
      'Always use maximum compression',
      'Docker does not compress layers',
    ],
    correctAnswer: 1,
    explanation: 'Layer compression involves a trade-off: higher compression reduces download size but increases CPU time for decompression. zstd provides 3-5x faster decompression than gzip at similar ratios.',
    codeSnippet: 'docker buildx build \\\n  --output type=image,compression=zstd,compression-level=3 \\\n  -t myapp:latest .',
    tags: ['compression', 'performance', 'zstd'],
  },
  {
    id: 'docker-dockerfile-l4-045',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a Docker build that generates and embeds Git metadata?',
    options: [
      'Include the .git directory',
      'Use ARGs to pass Git info at build time and embed as LABELs or compile into the application',
      'Docker detects Git automatically',
      'Use Git hooks',
    ],
    correctAnswer: 1,
    explanation: 'Pass Git metadata (commit hash, branch, tag) as build arguments and embed them as image labels for traceability or compile them into the application binary for version reporting.',
    codeSnippet: 'ARG GIT_COMMIT\nARG GIT_BRANCH\nARG BUILD_DATE\nLABEL org.opencontainers.image.revision=$GIT_COMMIT \\\n      org.opencontainers.image.created=$BUILD_DATE\nRUN go build -ldflags "-X main.version=$GIT_COMMIT" -o /app\n\n# docker build \\\n#   --build-arg GIT_COMMIT=$(git rev-parse HEAD) \\\n#   --build-arg BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ) .',
    tags: ['git', 'metadata', 'traceability'],
  },
  {
    id: 'docker-dockerfile-l4-046',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the difference between buildx `--load` and `--push` for multi-platform builds?',
    options: [
      'They work the same for multi-platform',
      '--load only works for single-platform (loads to local daemon); --push is required for multi-platform images',
      '--load is faster; --push is for production',
      '--load creates a tar; --push sends to a registry',
    ],
    correctAnswer: 1,
    explanation: 'The local Docker daemon can only store single-platform images, so --load fails for multi-platform builds. --push sends multi-platform manifests directly to a registry.',
    codeSnippet: '# Single platform: --load works\ndocker buildx build --platform linux/amd64 --load -t myapp .\n\n# Multi-platform: must use --push\ndocker buildx build --platform linux/amd64,linux/arm64 --push -t myapp .',
    tags: ['buildx', 'multi-platform', 'load-push'],
  },
  {
    id: 'docker-dockerfile-l4-047',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is image hardening and what are the key practices?',
    options: [
      'Compiling images with optimization flags',
      'Reducing attack surface through minimal base, non-root user, read-only fs, dropped capabilities, and no unnecessary packages',
      'Encrypting the image layers',
      'Adding a firewall to the image',
    ],
    correctAnswer: 1,
    explanation: 'Image hardening involves: minimal base image, running as non-root, read-only filesystem, dropping all capabilities except needed ones, no shell if possible, and regular vulnerability scanning.',
    codeSnippet: 'FROM gcr.io/distroless/static-debian12:nonroot\nCOPY --from=builder /app/server /server\nUSER 65532:65532\nENTRYPOINT ["/server"]\n# Run with: docker run --read-only --cap-drop ALL myapp',
    tags: ['hardening', 'security', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l4-048',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a sidecar pattern using multi-stage builds?',
    options: [
      'Run multiple processes in one container',
      'Build multiple images from the same Dockerfile with different targets for the main app and sidecar',
      'Use Docker Compose only',
      'Use Docker networking',
    ],
    correctAnswer: 1,
    explanation: 'Build both the main application and sidecar container from the same Dockerfile using different target stages. This keeps related code together while producing separate images.',
    codeSnippet: 'FROM base AS app\nCOPY app.py /app/\nCMD ["python", "/app/app.py"]\n\nFROM base AS log-collector\nCOPY log-collector.py /app/\nCMD ["python", "/app/log-collector.py"]\n\n# docker build --target app -t myapp:latest .\n# docker build --target log-collector -t myapp-logs:latest .',
    tags: ['sidecar', 'multi-stage', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-049',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you use `--metadata-file` in docker buildx build?',
    options: [
      'Adds metadata to the Dockerfile',
      'Writes build result metadata (image digest, build ref) to a JSON file for CI/CD integration',
      'Reads metadata from a file',
      'Exports image labels to a file',
    ],
    correctAnswer: 1,
    explanation: 'The `--metadata-file` flag writes build result information to a JSON file, including the image digest. This is essential for CI/CD pipelines that need to reference the exact built image.',
    codeSnippet: 'docker buildx build --metadata-file metadata.json \\\n  -t myapp:latest --push .\n\n# metadata.json contains:\n# {"containerimage.digest": "sha256:abc123..."}',
    tags: ['buildx', 'metadata', 'ci-cd'],
  },
  {
    id: 'docker-dockerfile-l4-050',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a hermetic Docker build?',
    options: [
      'Build in a sealed room',
      'Pre-download all dependencies, disable network in build steps, and use only vendored/local resources',
      'Use FROM scratch only',
      'Build without internet connection',
    ],
    correctAnswer: 1,
    explanation: 'Hermetic builds ensure complete isolation from external resources. Pre-vendor all dependencies, use `RUN --network=none` for build steps, and pin all inputs by digest for full reproducibility.',
    codeSnippet: '# Pre-download deps in a networked stage\nFROM golang:1.22 AS deps\nCOPY go.mod go.sum ./\nRUN go mod download\n\n# Build without network access\nFROM deps AS builder\nCOPY . .\nRUN --network=none go build -mod=vendor -o /app',
    tags: ['hermetic', 'reproducible', 'expert'],
  },
  {
    id: 'docker-dockerfile-l4-051',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the `--annotation` flag in docker buildx build?',
    options: [
      'Adds comments to the Dockerfile',
      'Attaches OCI annotations to specific parts of the image (manifest, manifest-descriptor, index)',
      'Adds build notes',
      'Labels the build cache',
    ],
    correctAnswer: 1,
    explanation: 'The `--annotation` flag adds OCI annotations to specific parts of the image output. You can target the index, manifest, or manifest-descriptor with prefix syntax.',
    codeSnippet: 'docker buildx build \\\n  --annotation "manifest:org.opencontainers.image.description=My App" \\\n  --annotation "manifest-descriptor[linux/amd64]:com.example.arch=amd64" \\\n  -t myapp:latest --push .',
    tags: ['buildx', 'annotations', 'oci'],
  },
  {
    id: 'docker-dockerfile-l4-052',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'How do you implement parallel test execution in Docker builds?',
    options: [
      'Tests always run sequentially',
      'Use multiple independent test stages that BuildKit executes concurrently, then COPY results to a final stage',
      'Use docker compose for parallel tests',
      'Run tests outside Docker',
    ],
    correctAnswer: 1,
    explanation: 'Create independent test stages (unit tests, integration tests, lint) that BuildKit runs concurrently. A final stage can COPY from all test stages to ensure they all passed.',
    codeSnippet: 'FROM builder AS unit-tests\nRUN npm run test:unit\n\nFROM builder AS lint\nRUN npm run lint\n\nFROM builder AS type-check\nRUN npm run type-check\n\nFROM alpine AS verify\nCOPY --from=unit-tests /tmp/.tests-passed /tmp/\nCOPY --from=lint /tmp/.lint-passed /tmp/\nCOPY --from=type-check /tmp/.types-passed /tmp/',
    tags: ['parallel', 'testing', 'performance'],
  },
  {
    id: 'docker-dockerfile-l4-053',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is `docker buildx imagetools create`?',
    options: [
      'Creates a new image from scratch',
      'Creates a manifest list combining multiple platform-specific images into a single multi-arch tag',
      'Creates build tools',
      'Creates image metadata',
    ],
    correctAnswer: 1,
    explanation: '`docker buildx imagetools create` combines multiple platform-specific images (already pushed to a registry) into a single multi-architecture manifest list under one tag.',
    codeSnippet: '# Combine separately built images into one multi-arch tag\ndocker buildx imagetools create \\\n  -t myapp:latest \\\n  myapp:latest-amd64 \\\n  myapp:latest-arm64',
    tags: ['buildx', 'imagetools', 'multi-arch'],
  },
  {
    id: 'docker-dockerfile-l4-054',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement zero-downtime Docker image updates?',
    options: [
      'Stop the old container and start the new one',
      'Design images to support graceful shutdown (SIGTERM handling, health checks) and use orchestration for rolling updates',
      'Use the --restart flag',
      'Keep both versions running permanently',
    ],
    correctAnswer: 1,
    explanation: 'Design images with proper SIGTERM handling (exec form ENTRYPOINT), health checks, and readiness probes. Use orchestrators for rolling updates with configurable rollout parameters.',
    codeSnippet: 'FROM node:20-alpine\nRUN apk add --no-cache tini\nENTRYPOINT ["tini", "--"]\nCMD ["node", "server.js"]\nHEALTHCHECK --interval=5s --timeout=3s --start-period=30s \\\n  CMD curl -f http://localhost:3000/health || exit 1\nSTOPSIGNAL SIGTERM',
    tags: ['zero-downtime', 'graceful-shutdown', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-055',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is `--call=check` in docker buildx build?',
    options: [
      'Checks if the build will succeed',
      'Runs BuildKit inline Dockerfile linting without building an image',
      'Validates the Dockerfile syntax',
      'Checks cache status',
    ],
    correctAnswer: 1,
    explanation: '`docker buildx build --call=check` runs the BuildKit Dockerfile linter without actually building the image. It reports warnings and errors based on best practice rules.',
    codeSnippet: 'docker buildx build --call=check .\n# or\ndocker buildx build --check .',
    tags: ['buildkit', 'linting', 'check'],
  },
  {
    id: 'docker-dockerfile-l4-056',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you implement image vulnerability management at scale?',
    options: [
      'Scan images manually',
      'Integrate scanning in CI, use admission controllers to block vulnerable images, and automate base image updates',
      'Use only official images',
      'Rebuild all images weekly',
    ],
    correctAnswer: 1,
    explanation: 'A comprehensive approach includes: CI-integrated scanning (Trivy/Snyk), admission controllers blocking high-severity CVEs, automated base image updates (Renovate/Dependabot), and SBOM tracking.',
    tags: ['vulnerability', 'management', 'security'],
  },
  {
    id: 'docker-dockerfile-l4-057',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement ephemeral build environments with Docker?',
    options: [
      'Use the same build environment for all builds',
      'Create disposable builder instances that are destroyed after each build, ensuring clean isolated environments',
      'Build on developer machines',
      'Use shared CI runners',
    ],
    correctAnswer: 1,
    explanation: 'Ephemeral builders ensure clean, reproducible environments. Use `docker buildx create` with per-build instances or Kubernetes-based builders that spin up pods per build.',
    codeSnippet: '# Create ephemeral builder\nBUILDER=$(docker buildx create --driver docker-container --name build-$BUILD_ID)\ndocker buildx build --builder $BUILDER -t myapp:latest --push .\ndocker buildx rm $BUILDER',
    tags: ['ephemeral', 'ci-cd', 'isolation'],
  },
  {
    id: 'docker-dockerfile-l4-058',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you implement remote caching with authentication?',
    options: [
      'Cache does not support authentication',
      'Use registry cache type with authenticated registry or S3 cache with IAM credentials',
      'Pass credentials in the cache URL',
      'Use a public cache',
    ],
    correctAnswer: 1,
    explanation: 'Registry cache uses the same authentication as docker push/pull (credential helpers, docker login). S3 cache uses standard AWS credentials (env vars, instance profiles, etc.).',
    codeSnippet: '# Registry cache (uses docker login credentials)\ndocker buildx build \\\n  --cache-to type=registry,ref=myregistry.com/cache:latest \\\n  --cache-from type=registry,ref=myregistry.com/cache:latest \\\n  -t myapp:latest .\n\n# S3 cache (uses AWS credentials)\ndocker buildx build \\\n  --cache-to type=s3,bucket=mybucket,region=us-east-1 \\\n  --cache-from type=s3,bucket=mybucket,region=us-east-1 .',
    tags: ['cache', 'authentication', 'remote'],
  },
  {
    id: 'docker-dockerfile-l4-059',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'OCI Compliance',
    question: 'What is an OCI referrer and how does it relate to image attestations?',
    options: [
      'A user who referred to the image',
      'An OCI artifact (signature, SBOM, attestation) that references another artifact by its digest',
      'A redirect in the registry',
      'A cross-reference between tags',
    ],
    correctAnswer: 1,
    explanation: 'OCI referrers are artifacts linked to a specific image digest via the subject field. Attestations (SBOM, provenance, signatures) are stored as referrers, discoverable via the referrers API.',
    codeSnippet: '# View referrers (attestations) for an image\ndocker buildx imagetools inspect myapp:latest --format \'{{json .Manifest}}\'',
    tags: ['oci', 'referrers', 'attestations'],
  },
  {
    id: 'docker-dockerfile-l4-060',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'How do you minimize container startup time through Dockerfile optimization?',
    options: [
      'Use the smallest possible image only',
      'Combine small images, lazy-loading (eStargz/SOCI), efficient compression (zstd), and minimal init process',
      'Use the --quick flag',
      'Disable health checks',
    ],
    correctAnswer: 1,
    explanation: 'Minimize startup by: using small base images, enabling lazy loading (eStargz/SOCI), zstd compression for faster decompression, minimal init process, and pre-warming caches.',
    codeSnippet: '# Optimized for fast startup\nFROM gcr.io/distroless/static-debian12:nonroot\nCOPY --from=builder /app/server /server\nENTRYPOINT ["/server"]\n\n# Build with zstd for faster pull\n# docker buildx build --output type=image,compression=zstd .',
    tags: ['startup', 'performance', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l4-061',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement blue-green deployments with Docker images?',
    options: [
      'Tag images with colors',
      'Build identical images tagged for blue/green slots and use routing/orchestration to switch traffic',
      'Use Docker Compose profiles',
      'Run both versions in the same container',
    ],
    correctAnswer: 1,
    explanation: 'Blue-green deployment uses two identical environments. Build the image, deploy to the inactive slot, verify with health checks, then switch routing. The Dockerfile should support configuration via environment variables.',
    tags: ['blue-green', 'deployment', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-062',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the `--call=targets` option in docker buildx bake?',
    options: [
      'Lists all build targets',
      'Prints the resolved build graph showing targets, their dependencies, and configuration without building',
      'Calls external targets',
      'Triggers target builds',
    ],
    correctAnswer: 1,
    explanation: '`docker buildx bake --print` (or `--call=targets`) outputs the resolved build definition as JSON, showing all targets with their fully resolved configuration including variable substitutions.',
    codeSnippet: 'docker buildx bake --print',
    tags: ['buildx', 'bake', 'inspection'],
  },
  {
    id: 'docker-dockerfile-l4-063',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement canary deployments with Docker?',
    options: [
      'Name the image "canary"',
      'Deploy the new image alongside the old one, gradually shifting traffic based on health metrics and error rates',
      'Build a smaller version of the image',
      'Use Docker health checks only',
    ],
    correctAnswer: 1,
    explanation: 'Canary deployment runs a small percentage of traffic on the new image version. The Dockerfile should include comprehensive health checks and metrics endpoints for monitoring.',
    codeSnippet: 'HEALTHCHECK --interval=5s --timeout=3s --retries=3 \\\n  CMD curl -f http://localhost:3000/health || exit 1\n\n# Expose metrics for canary analysis\nEXPOSE 9090\nENV METRICS_PORT=9090',
    tags: ['canary', 'deployment', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-064',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you use BuildKit with a custom registry mirror for builds?',
    options: [
      'Set DOCKER_REGISTRY_MIRROR',
      'Configure registry mirrors in the buildkitd.toml configuration file',
      'Use --registry-mirror flag',
      'Modify the Dockerfile FROM instructions',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit supports registry mirrors configured in buildkitd.toml. This redirects image pulls to local mirrors, reducing external network dependency and improving build speed.',
    codeSnippet: '# buildkitd.toml\n[registry."docker.io"]\n  mirrors = ["registry-mirror.example.com"]\n\n[registry."registry-mirror.example.com"]\n  http = true\n\ndocker buildx create --config ./buildkitd.toml --use',
    tags: ['buildkit', 'registry-mirror', 'configuration'],
  },
  {
    id: 'docker-dockerfile-l4-065',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement image tagging strategies for production?',
    options: [
      'Use latest for everything',
      'Use semantic versioning, Git SHA, and environment tags with immutable digests for production references',
      'Use date-based tags only',
      'Use random tags',
    ],
    correctAnswer: 1,
    explanation: 'Use a combination: semantic version tags (v1.2.3), Git commit SHA for traceability, environment tags (staging, production), and always reference by digest in production deployments.',
    codeSnippet: '# Tag with multiple meaningful tags\ndocker buildx build \\\n  -t myapp:v1.2.3 \\\n  -t myapp:v1.2 \\\n  -t myapp:v1 \\\n  -t myapp:$(git rev-parse --short HEAD) \\\n  -t myapp:latest \\\n  --push .',
    tags: ['tagging', 'versioning', 'strategy'],
  },
  {
    id: 'docker-dockerfile-l4-066',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the `--attest` flag in docker buildx build?',
    options: [
      'Attests that the build is complete',
      'Generates and attaches attestation documents (SBOM, provenance) to the built image',
      'Attests image integrity',
      'Generates build certificates',
    ],
    correctAnswer: 1,
    explanation: 'The `--attest` flag generates specific attestation types (SBOM, provenance) and attaches them to the image as OCI referrers. This is the explicit form of --sbom and --provenance flags.',
    codeSnippet: 'docker buildx build \\\n  --attest type=sbom,generator=docker/scout-sbom-indexer \\\n  --attest type=provenance,mode=max \\\n  -t myapp:latest --push .',
    tags: ['buildx', 'attestations', 'supply-chain'],
  },
  {
    id: 'docker-dockerfile-l4-067',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a Docker-based development environment that matches production?',
    options: [
      'Use the production Dockerfile for development',
      'Use multi-stage builds sharing a base, with dev stage adding tools/debug utilities and Compose watch for live reload',
      'Use a VM instead',
      'Install all dev tools in the production image',
    ],
    correctAnswer: 1,
    explanation: 'Share a common base between dev and prod stages. Dev adds debug tools, dev dependencies, and hot-reload capabilities. Use Compose develop.watch for file synchronization.',
    codeSnippet: 'FROM node:20-alpine AS base\nWORKDIR /app\n\nFROM base AS development\nRUN apk add --no-cache git vim\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nCMD ["npm", "run", "dev"]\n\nFROM base AS production\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY --from=builder /app/dist ./dist\nUSER node\nCMD ["node", "dist/server.js"]',
    tags: ['dev-environment', 'parity', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-068',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'What is the maximum theoretical speedup from BuildKit parallel stage execution?',
    options: [
      'Always 2x faster',
      'It depends on the dependency graph; independent stages run concurrently but total time is bounded by the critical path',
      '10x faster always',
      'No measurable improvement',
    ],
    correctAnswer: 1,
    explanation: 'The speedup depends on the build DAG. The minimum build time is the longest path (critical path) through the dependency graph. Independent branches execute concurrently.',
    tags: ['buildkit', 'parallel', 'theory'],
  },
  {
    id: 'docker-dockerfile-l4-069',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you implement runtime vulnerability monitoring for Docker containers?',
    options: [
      'Scan images at build time only',
      'Combine build-time scanning, runtime SCA agents, and continuous monitoring of deployed images against new CVE disclosures',
      'Use Docker security settings',
      'Monitor container logs',
    ],
    correctAnswer: 1,
    explanation: 'Complete security requires: build-time scanning in CI, runtime agents (Falco, Sysdig) for behavior monitoring, and continuous re-scanning of deployed image SBOMs against new CVE databases.',
    tags: ['runtime', 'monitoring', 'security'],
  },
  {
    id: 'docker-dockerfile-l4-070',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you use docker buildx bake with HCL functions?',
    options: [
      'HCL does not support functions',
      'Use built-in functions like timestamp(), formatdate(), regex_replace() for dynamic values in bake files',
      'Write custom shell functions',
      'Use JavaScript functions',
    ],
    correctAnswer: 1,
    explanation: 'Docker bake HCL files support built-in functions including string manipulation, date formatting, regex, and environment variable access for dynamic build configurations.',
    codeSnippet: '# docker-bake.hcl\nvariable "TAG" {\n  default = "dev"\n}\n\nfunction "tag" {\n  params = [name]\n  result = ["myorg/\\${name}:\\${TAG}", "myorg/\\${name}:latest"]\n}\n\ntarget "api" {\n  tags = tag("api")\n  labels = {\n    "org.opencontainers.image.created" = timestamp()\n  }\n}',
    tags: ['buildx', 'bake', 'hcl-functions'],
  },
  {
    id: 'docker-dockerfile-l4-071',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement image promotion across environments?',
    options: [
      'Rebuild the image for each environment',
      'Build once, tag for dev, promote to staging/prod by retagging the same digest without rebuilding',
      'Use different Dockerfiles per environment',
      'Use environment variables in the image',
    ],
    correctAnswer: 1,
    explanation: 'Build the image once with a unique tag (Git SHA), test in dev, then promote to staging and production by adding new tags to the same image digest. Never rebuild for promotion.',
    codeSnippet: '# Build once\ndocker buildx build -t myapp:sha-abc123 --push .\n\n# Promote to staging (retag same digest)\ndocker buildx imagetools create -t myapp:staging myapp:sha-abc123\n\n# Promote to production\ndocker buildx imagetools create -t myapp:production myapp:sha-abc123',
    tags: ['promotion', 'environments', 'ci-cd'],
  },
  {
    id: 'docker-dockerfile-l4-072',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the `--invoke` flag in docker buildx build?',
    options: [
      'Invokes a webhook after building',
      'Launches an interactive debugging session in the build container after the build completes or fails',
      'Invokes a custom build step',
      'Calls an external API',
    ],
    correctAnswer: 1,
    explanation: '`docker buildx build --invoke` (experimental) starts an interactive shell session in the build result or at the point of failure, enabling direct debugging of the build environment.',
    codeSnippet: 'docker buildx debug --invoke /bin/sh build -t myapp .',
    tags: ['buildx', 'debugging', 'interactive'],
  },
  {
    id: 'docker-dockerfile-l4-073',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement feature flags in Docker images?',
    options: [
      'Build separate images for each feature',
      'Use build arguments to conditionally include features, or use runtime feature flags via environment variables',
      'Use Docker labels for features',
      'Use different branches',
    ],
    correctAnswer: 1,
    explanation: 'Build-time flags (ARG) control what is included in the image. Runtime flags (ENV) enable/disable features without rebuilding. Combine both for a flexible feature flag system.',
    codeSnippet: 'ARG ENABLE_METRICS=true\nRUN if [ "$ENABLE_METRICS" = "true" ]; then \\\n      npm install prom-client; \\\n    fi\n\nENV FEATURE_NEW_UI=false\nENV FEATURE_DARK_MODE=true',
    tags: ['feature-flags', 'configuration', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-074',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'How do you benchmark Docker image pull and startup performance?',
    options: [
      'Use docker stats',
      'Measure time for pull, create, and start operations; compare compressed/uncompressed sizes and decompression times',
      'Use docker info',
      'Check Docker Hub statistics',
    ],
    correctAnswer: 1,
    explanation: 'Benchmark pull performance by measuring download time and decompression time separately. Measure startup time from container creation to health check passing. Compare across compression methods.',
    codeSnippet: '# Benchmark pull time\ntime docker pull myapp:latest\n\n# Benchmark startup time\ntime docker run --rm myapp:latest echo "started"\n\n# Compare image sizes\ndocker image ls myapp --format "{{.Tag}}\\t{{.Size}}"',
    tags: ['benchmarking', 'performance', 'metrics'],
  },
  {
    id: 'docker-dockerfile-l4-075',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the relationship between BuildKit and Buildpacks?',
    options: [
      'They are the same technology',
      'BuildKit builds from Dockerfiles; Buildpacks auto-detect language and generate images without Dockerfiles; both can produce OCI images',
      'Buildpacks replaced BuildKit',
      'BuildKit uses Buildpacks internally',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit processes Dockerfiles into images. Cloud Native Buildpacks (CNB) auto-detect application type and build OCI images without Dockerfiles. Both produce standard OCI images but serve different workflows.',
    codeSnippet: '# BuildKit (from Dockerfile)\ndocker buildx build -t myapp .\n\n# Buildpacks (no Dockerfile needed)\npack build myapp --builder paketobuildpacks/builder-jammy-base',
    tags: ['buildkit', 'buildpacks', 'comparison'],
  },
  {
    id: 'docker-dockerfile-l4-076',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement configuration injection without rebuilding?',
    options: [
      'Hardcode all configuration',
      'Use environment variables, mounted config files, and runtime configuration services to separate config from the image',
      'Rebuild for each configuration change',
      'Use Docker labels for config',
    ],
    correctAnswer: 1,
    explanation: 'Follow the twelve-factor app methodology: externalize all configuration via environment variables, mounted ConfigMaps/secrets, or runtime config services. The image should be environment-agnostic.',
    codeSnippet: 'FROM node:20-alpine\nWORKDIR /app\nCOPY . .\n\n# Configurable via env vars and mounted files\nENV CONFIG_PATH=/etc/app/config.json\nENV LOG_LEVEL=info\nENV PORT=3000\n\n# Runtime: docker run -e LOG_LEVEL=debug \\\n#   -v ./config.json:/etc/app/config.json myapp',
    tags: ['configuration', 'twelve-factor', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l4-077',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you create a custom BuildKit frontend?',
    options: [
      'Modify the Docker source code',
      'Implement the BuildKit gateway.LLBBridgeForwarder interface as a gRPC server packaged as a container image',
      'Write a Dockerfile plugin',
      'Use the BuildKit SDK directly',
    ],
    correctAnswer: 1,
    explanation: 'Custom frontends implement the BuildKit gateway gRPC interface. They receive the build context and options, generate an LLB graph, and return it to BuildKit for execution.',
    codeSnippet: '# Custom frontend image referenced via # syntax=\n# syntax=myorg/custom-frontend:latest\n\n# The frontend image implements:\n# - BuildKit gateway.LLBBridgeForwarder interface\n# - Reads build context and generates LLB',
    tags: ['buildkit', 'frontend', 'custom-implementation'],
  },
  {
    id: 'docker-dockerfile-l4-078',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement a Docker image that supports both amd64 and arm64 with native performance?',
    options: [
      'Use QEMU emulation for everything',
      'Use cross-compilation for compiled languages and platform-specific binary downloads for tools, with FROM --platform for each stage',
      'Build separately on each architecture',
      'Use a universal binary format',
    ],
    correctAnswer: 1,
    explanation: 'For best performance: cross-compile compiled code on the native host, download pre-built platform-specific binaries for tools, and use the correct platform for runtime base images.',
    codeSnippet: 'FROM --platform=$BUILDPLATFORM golang:1.22 AS builder\nARG TARGETOS TARGETARCH\nRUN CGO_ENABLED=0 GOOS=$TARGETOS GOARCH=$TARGETARCH go build -o /app\n\nFROM --platform=$TARGETPLATFORM alpine:3.19\nARG TARGETARCH\nRUN apk add --no-cache curl && \\\n    curl -fsSL https://tool.io/releases/tool-$TARGETARCH -o /usr/local/bin/tool\nCOPY --from=builder /app /usr/local/bin/',
    tags: ['multi-platform', 'native', 'performance'],
  },
  {
    id: 'docker-dockerfile-l4-079',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you share BuildKit cache across a team?',
    options: [
      'Share the Docker daemon',
      'Use a shared registry cache or S3 bucket as the cache backend so all team members and CI can read/write',
      'Copy cache directories between machines',
      'Use a shared NFS mount',
    ],
    correctAnswer: 1,
    explanation: 'A shared cache backend (registry or S3) allows all developers and CI systems to contribute to and benefit from the same build cache, dramatically reducing build times across the team.',
    codeSnippet: '# All team members and CI use the same cache\ndocker buildx build \\\n  --cache-from type=registry,ref=myorg/cache:main \\\n  --cache-to type=registry,ref=myorg/cache:main,mode=max \\\n  -t myapp:latest .',
    tags: ['cache', 'team', 'shared'],
  },
  {
    id: 'docker-dockerfile-l4-080',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is the Docker image supply chain and how do you secure it end-to-end?',
    options: [
      'Use Docker Hub for everything',
      'Secure source code, build environment, artifacts, registry, and deployment with signatures, attestations, and policy enforcement at each stage',
      'Use private registries only',
      'Scan images once before deployment',
    ],
    correctAnswer: 1,
    explanation: 'End-to-end supply chain security includes: signed source code, hermetic builds, SBOM/provenance attestations, signed images, secure registry, admission controllers, and runtime monitoring.',
    tags: ['supply-chain', 'end-to-end', 'security'],
  },
  {
    id: 'docker-dockerfile-l4-081',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is `--opt filename` in buildx bake?',
    options: [
      'Sets the output filename',
      'Overrides variable values in the bake file from the command line',
      'Specifies the bake file name',
      'Sets optimization level',
    ],
    correctAnswer: 1,
    explanation: 'The `--set` flag in buildx bake overrides target properties from the command line, allowing you to dynamically change tags, platforms, or args without modifying the bake file.',
    codeSnippet: 'docker buildx bake \\\n  --set "*.platform=linux/amd64" \\\n  --set "api.tags=myorg/api:v2.0" \\\n  --set "api.args.VERSION=2.0" \\\n  all',
    tags: ['buildx', 'bake', 'overrides'],
  },
  {
    id: 'docker-dockerfile-l4-082',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you implement Docker image garbage collection in a registry?',
    options: [
      'Registries handle this automatically',
      'Use registry garbage collection with retention policies based on age, tag count, or semantic versioning rules',
      'Delete images manually',
      'Use docker system prune on the registry',
    ],
    correctAnswer: 1,
    explanation: 'Registry GC removes unreferenced blobs. Implement retention policies: keep N most recent tags, keep tagged releases, delete untagged images older than N days. Tools like crane can help.',
    codeSnippet: '# Docker registry garbage collection\ndocker exec registry registry garbage-collect /etc/docker/registry/config.yml\n\n# Using crane for tag management\ncrane ls myregistry.com/myapp\ncrane delete myregistry.com/myapp:old-tag',
    tags: ['registry', 'garbage-collection', 'retention'],
  },
  {
    id: 'docker-dockerfile-l4-083',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the `--sbom-dir` option for SBOM attestations?',
    options: [
      'Directory for SBOM reports',
      'An option to customize the SBOM scanner output directory within the attestation generation process',
      'Source directory for SBOMs',
      'SBOM database directory',
    ],
    correctAnswer: 0,
    explanation: 'The `--sbom-dir` option in buildx allows directing SBOM output to a specific directory when using `--output type=local`, useful for integrating SBOMs into CI artifact storage.',
    codeSnippet: 'docker buildx build \\\n  --attest type=sbom \\\n  --output type=local,dest=./output \\\n  -t myapp:latest .',
    tags: ['sbom', 'buildx', 'attestations'],
  },
  {
    id: 'docker-dockerfile-l4-084',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Performance',
    question: 'How do you optimize Docker layer sharing across multiple images?',
    options: [
      'Layers are never shared',
      'Use common base images, identical early instructions, and layer-aware ordering to maximize deduplication on the registry and host',
      'Combine all images into one',
      'Use different tags for each service',
    ],
    correctAnswer: 1,
    explanation: 'Images sharing the same base and early layers only store/transfer unique layers. Use a common base image, order instructions consistently, and keep shared setup in early layers.',
    codeSnippet: '# Shared base for all services\nFROM node:20-alpine AS base\nRUN apk add --no-cache tini\nWORKDIR /app\n\n# Service A and B share the base layers\n# Only their unique COPY/RUN layers are additional',
    tags: ['layer-sharing', 'deduplication', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l4-085',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'How do you implement a Docker build that outputs test results as artifacts?',
    options: [
      'Read test output from docker logs',
      'Use --output type=local on a test stage to extract test results, coverage reports, and artifacts to the local filesystem',
      'Save results in the image',
      'Use docker cp after build',
    ],
    correctAnswer: 1,
    explanation: 'Create a test stage that runs tests and saves results. Use `--output type=local --target=test-results` to extract artifacts to the local filesystem without creating an image.',
    codeSnippet: 'FROM builder AS tests\nRUN npm test -- --coverage --outputFile=/results/test-report.xml\n\nFROM scratch AS test-results\nCOPY --from=tests /results /\n\n# Extract results\n# docker buildx build --target test-results \\\n#   --output type=local,dest=./test-results .',
    tags: ['testing', 'artifacts', 'output'],
  },
  {
    id: 'docker-dockerfile-l4-086',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'Advanced Patterns',
    question: 'How do you handle Docker image versioning in a microservices architecture?',
    options: [
      'Use the same version for all services',
      'Independent semantic versioning per service, shared base image versions, and dependency tracking via SBOMs',
      'Use Git commit hashes only',
      'Use date-based versions',
    ],
    correctAnswer: 1,
    explanation: 'Each microservice has independent semantic versioning. Shared base images have their own version. SBOMs track dependencies. A service catalog tracks which versions are deployed together.',
    tags: ['versioning', 'microservices', 'strategy'],
  },
  {
    id: 'docker-dockerfile-l4-087',
    language: 'docker',
    level: 'level4',
    category: 'Dockerfile',
    subcategory: 'BuildKit Advanced',
    question: 'What is the future of Docker builds with BuildKit v2?',
    options: [
      'BuildKit will be replaced by a new tool',
      'BuildKit v2 focuses on improved caching, faster builds, better multi-platform support, and deeper CI/CD integration',
      'BuildKit v2 removes Dockerfile support',
      'BuildKit v2 only supports Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit continues to evolve with improved cache management, faster concurrent builds, better multi-platform support, enhanced attestation features, and deeper integration with CI/CD platforms.',
    tags: ['buildkit', 'future', 'evolution'],
  },
];

export default questions;
