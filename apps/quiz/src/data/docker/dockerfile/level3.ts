import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'docker-dockerfile-l3-001',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you create a multi-stage Dockerfile that builds for multiple environments?',
    options: [
      'Use ARG to conditionally run commands',
      'Create named stages for each environment and use --target to select one',
      'Use separate Dockerfiles for each environment',
      'Use environment-specific base images',
    ],
    correctAnswer: 1,
    explanation: 'Named stages for different environments (dev, staging, prod) allow building any target from a single Dockerfile using `--target`, keeping all configurations in one place.',
    codeSnippet: 'FROM node:20-alpine AS base\nWORKDIR /app\nCOPY package*.json ./\n\nFROM base AS development\nRUN npm install\nCOPY . .\nCMD ["npm", "run", "dev"]\n\nFROM base AS production\nRUN npm ci --production\nCOPY . .\nCMD ["node", "server.js"]\n\n# docker build --target production -t myapp:prod .',
    tags: ['multi-stage', 'environments', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-002',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'What is the diamond dependency pattern in multi-stage builds?',
    options: [
      'A pattern where stages form a diamond shape dependency graph',
      'A pattern where two parallel stages (e.g., frontend and backend) both feed into a final stage',
      'A circular dependency between stages',
      'A pattern for building gem dependencies',
    ],
    correctAnswer: 1,
    explanation: 'The diamond pattern has parallel independent build stages that all contribute artifacts to a final stage. BuildKit builds the parallel stages concurrently for faster builds.',
    codeSnippet: 'FROM node:20 AS frontend\nRUN npm run build\n\nFROM golang:1.22 AS backend\nRUN go build -o api\n\nFROM alpine AS final\nCOPY --from=frontend /app/dist /srv/static\nCOPY --from=backend /app/api /usr/bin/',
    tags: ['multi-stage', 'diamond', 'pattern'],
  },
  {
    id: 'docker-dockerfile-l3-003',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Build Args Advanced',
    question: 'How do you use ARG to parameterize the base image?',
    options: [
      'FROM ${ARG_NAME}',
      'Declare ARG before FROM and reference it in the FROM instruction',
      'FROM image --arg=value',
      'BASE_IMAGE=node:20 FROM $BASE_IMAGE',
    ],
    correctAnswer: 1,
    explanation: 'ARG declared before FROM can be used in the FROM instruction itself. This allows parameterizing the base image at build time.',
    codeSnippet: 'ARG NODE_VERSION=20\nARG VARIANT=alpine\nFROM node:\\${NODE_VERSION}-\\${VARIANT}\nWORKDIR /app',
    tags: ['arg', 'from', 'parameterization'],
  },
  {
    id: 'docker-dockerfile-l3-004',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Build Args Advanced',
    question: 'How do ARG values interact with build cache?',
    options: [
      'ARG values never affect the cache',
      'An ARG value change invalidates the cache from the first instruction that uses it',
      'ARG always invalidates the entire cache',
      'Only ENV values affect the cache',
    ],
    correctAnswer: 1,
    explanation: 'Docker tracks which instructions reference each ARG. When an ARG value changes, cache invalidation starts at the first instruction that uses that ARG, not at the ARG declaration itself.',
    codeSnippet: 'ARG VERSION=1.0\nRUN echo "static step"  # cached even if VERSION changes\nRUN echo "version: $VERSION"  # cache invalidated if VERSION changes',
    tags: ['arg', 'cache', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-005',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK Advanced',
    question: 'How do you implement a HEALTHCHECK with `--start-period` for slow-starting applications?',
    options: [
      'Set a long --interval',
      'Use --start-period to give the application time to initialize before health checks count as failures',
      'Use --timeout with a large value',
      'Skip HEALTHCHECK for slow applications',
    ],
    correctAnswer: 1,
    explanation: '`--start-period` defines a grace period during which health check failures do not count toward the retry limit. The container starts as "starting" during this period.',
    codeSnippet: 'HEALTHCHECK --interval=10s --timeout=5s --start-period=60s --retries=3 \\\n  CMD curl -f http://localhost:8080/ready || exit 1',
    tags: ['healthcheck', 'start-period', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-006',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'HEALTHCHECK Advanced',
    question: 'What is the difference between a liveness check and a readiness check in Docker?',
    options: [
      'Docker supports both natively',
      'Docker HEALTHCHECK acts as a liveness check; readiness is typically handled by orchestrators like Kubernetes',
      'They are the same thing',
      'Readiness is checked at build time; liveness at runtime',
    ],
    correctAnswer: 1,
    explanation: 'Docker HEALTHCHECK only provides liveness checking (is the app alive?). Readiness checking (is the app ready to serve traffic?) requires orchestration tools like Kubernetes or health-aware load balancers.',
    tags: ['healthcheck', 'liveness', 'readiness'],
  },
  {
    id: 'docker-dockerfile-l3-007',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'SHELL Advanced',
    question: 'How do you use PowerShell as the default shell in Windows Dockerfiles?',
    options: [
      'RUN powershell command',
      'SHELL ["powershell", "-Command", "$ErrorActionPreference = \'Stop\';"]',
      'USE POWERSHELL',
      'DEFAULT_SHELL powershell',
    ],
    correctAnswer: 1,
    explanation: 'For Windows containers, SHELL can change the default to PowerShell. Setting $ErrorActionPreference ensures errors are caught in subsequent RUN instructions.',
    codeSnippet: 'FROM mcr.microsoft.com/windows/servercore:ltsc2022\nSHELL ["powershell", "-Command", "$ErrorActionPreference = \'Stop\';"]\ \nRUN Invoke-WebRequest -Uri https://example.com/app.zip -OutFile app.zip',
    tags: ['shell', 'powershell', 'windows'],
  },
  {
    id: 'docker-dockerfile-l3-008',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'SHELL Advanced',
    question: 'When should you change the default SHELL in a Dockerfile?',
    options: [
      'Always use the default /bin/sh',
      'When you need bash features (pipefail, arrays, process substitution) or Windows PowerShell',
      'Only for Windows containers',
      'Never, it causes compatibility issues',
    ],
    correctAnswer: 1,
    explanation: 'Change the SHELL when you need features unavailable in /bin/sh: bash pipefail for proper error handling in pipes, bash arrays, or PowerShell on Windows.',
    codeSnippet: '# Enable pipefail for pipe error handling\nSHELL ["/bin/bash", "-o", "pipefail", "-c"]\nRUN curl -fsSL https://example.com/script | bash',
    tags: ['shell', 'bash', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-009',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you reduce Python Docker image size with virtual environments?',
    options: [
      'Do not use virtual environments in Docker',
      'Install in a venv in the builder stage and copy only the venv to the final stage',
      'Use pip install --global',
      'Use Python slim image only',
    ],
    correctAnswer: 1,
    explanation: 'Using a virtual environment in a multi-stage build lets you install all dependencies (including build tools) in the builder stage and copy only the compact venv to the final stage.',
    codeSnippet: 'FROM python:3.12 AS builder\nRUN python -m venv /opt/venv\nENV PATH="/opt/venv/bin:$PATH"\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\nFROM python:3.12-slim\nCOPY --from=builder /opt/venv /opt/venv\nENV PATH="/opt/venv/bin:$PATH"\nCOPY . /app\nCMD ["python", "/app/main.py"]',
    tags: ['python', 'venv', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-010',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you create a minimal Java Docker image?',
    options: [
      'Use the full JDK image',
      'Use jlink to create a custom minimal JRE in a multi-stage build',
      'Use Java 8 for smaller size',
      'Compress the JRE files',
    ],
    correctAnswer: 1,
    explanation: 'jlink (JDK 9+) creates a custom JRE with only the modules your application needs. Combined with multi-stage builds, this dramatically reduces Java image size.',
    codeSnippet: 'FROM eclipse-temurin:21-jdk AS builder\nRUN jlink --add-modules java.base,java.logging,java.sql \\\n    --strip-debug --compress=2 --no-header-files \\\n    --no-man-pages --output /custom-jre\n\nFROM debian:bookworm-slim\nCOPY --from=builder /custom-jre /opt/java\nENV PATH="/opt/java/bin:$PATH"\nCOPY app.jar /app.jar\nCMD ["java", "-jar", "/app.jar"]',
    tags: ['java', 'jlink', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-011',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you reduce Rust Docker image size?',
    options: [
      'Use the Rust nightly compiler',
      'Build a statically linked binary with musl and use FROM scratch',
      'Use the largest Rust image for best optimization',
      'Disable compiler optimizations',
    ],
    correctAnswer: 1,
    explanation: 'Building Rust with musl target creates a fully static binary that can run in a FROM scratch image with no dependencies, resulting in very small final images.',
    codeSnippet: 'FROM rust:1.77 AS builder\nRUN rustup target add x86_64-unknown-linux-musl\nWORKDIR /app\nCOPY . .\nRUN cargo build --release --target x86_64-unknown-linux-musl\n\nFROM scratch\nCOPY --from=builder /app/target/x86_64-unknown-linux-musl/release/myapp /\nENTRYPOINT ["/myapp"]',
    tags: ['rust', 'musl', 'scratch'],
  },
  {
    id: 'docker-dockerfile-l3-012',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is the recommended way to handle secrets during Docker builds?',
    options: [
      'Use ARG to pass secrets',
      'Use BuildKit secret mounts with RUN --mount=type=secret',
      'Use environment variables',
      'Copy secret files and delete them later',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit secret mounts temporarily make secrets available during a RUN instruction without storing them in any image layer. They are the only secure way to use secrets during builds.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nRUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n  npm ci\n\n# docker build --secret id=npmrc,src=.npmrc .',
    tags: ['secrets', 'buildkit', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-013',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you access private Git repos during Docker build securely?',
    options: [
      'COPY the SSH key into the image',
      'Use BuildKit SSH mount to forward the host SSH agent',
      'Use ARG to pass the SSH key',
      'Make the repo public temporarily',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit SSH mount forwards the host SSH agent into a RUN instruction without copying keys into the image. The keys are never stored in any layer.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nRUN --mount=type=ssh \\\n  git clone git@github.com:private/repo.git /app\n\n# docker build --ssh default .',
    tags: ['ssh', 'buildkit', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-014',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Mounts',
    question: 'What is a BuildKit cache mount?',
    options: [
      'A mount for the Docker build cache',
      'A persistent cache directory that survives between builds for package manager caches',
      'A read-only copy of the build context',
      'A temporary directory for build artifacts',
    ],
    correctAnswer: 1,
    explanation: 'Cache mounts create persistent directories for package manager caches (pip, npm, apt, maven). They survive between builds, avoiding redundant downloads.',
    codeSnippet: 'RUN --mount=type=cache,target=/root/.cache/pip \\\n  pip install -r requirements.txt\n\nRUN --mount=type=cache,target=/root/.npm \\\n  npm ci',
    tags: ['buildkit', 'cache-mount', 'performance'],
  },
  {
    id: 'docker-dockerfile-l3-015',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Mounts',
    question: 'How do you use a cache mount for apt-get?',
    options: [
      'RUN apt-get update --cache',
      'Mount /var/cache/apt and /var/lib/apt as cache types',
      'Cache is built into apt-get',
      'Use a local apt mirror',
    ],
    correctAnswer: 1,
    explanation: 'Mounting apt cache directories as BuildKit cache mounts preserves downloaded packages between builds, avoiding re-downloads while keeping the image layer clean.',
    codeSnippet: 'RUN --mount=type=cache,target=/var/cache/apt \\\n    --mount=type=cache,target=/var/lib/apt \\\n    apt-get update && apt-get install -y curl git',
    tags: ['buildkit', 'apt', 'cache-mount'],
  },
  {
    id: 'docker-dockerfile-l3-016',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Mounts',
    question: 'What is the `sharing` option in cache mounts?',
    options: [
      'Controls file sharing between containers',
      'Controls concurrent access: shared (parallel reads), locked (exclusive), or private (per-build copy)',
      'Enables sharing with other Docker hosts',
      'Shares the cache with registries',
    ],
    correctAnswer: 1,
    explanation: 'The `sharing` option controls how cache mounts handle concurrent builds: `shared` (default, parallel access), `locked` (exclusive lock), or `private` (copy for each build).',
    codeSnippet: 'RUN --mount=type=cache,target=/root/.cache,sharing=locked \\\n  build-command',
    tags: ['buildkit', 'cache-mount', 'sharing'],
  },
  {
    id: 'docker-dockerfile-l3-017',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the purpose of `CGO_ENABLED=0` in Go Dockerfiles?',
    options: [
      'Enables Go compiler optimizations',
      'Disables cgo to produce a statically linked binary without C library dependencies',
      'Compiles Go code',
      'Enables CGO for performance',
    ],
    correctAnswer: 1,
    explanation: 'Setting CGO_ENABLED=0 disables cgo (C interop), producing a binary statically linked with Go standard library. This allows using FROM scratch or minimal base images.',
    codeSnippet: 'RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \\\n  go build -ldflags="-w -s" -o /app',
    tags: ['go', 'cgo', 'static-binary'],
  },
  {
    id: 'docker-dockerfile-l3-018',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What do `-ldflags="-w -s"` do in Go builds?',
    options: [
      'Enable debug logging',
      'Strip debug information (-w) and symbol table (-s) to reduce binary size',
      'Set linker flags for dynamic linking',
      'Enable warnings and strict mode',
    ],
    correctAnswer: 1,
    explanation: '`-w` disables DWARF debugging information and `-s` disables the symbol table. Together they significantly reduce the binary size, sometimes by 20-30%.',
    codeSnippet: 'RUN go build -ldflags="-w -s" -o /app',
    tags: ['go', 'ldflags', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-019',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you use numbered stages instead of named stages?',
    options: [
      'COPY --stage=0 /app .',
      'COPY --from=0 /app . where 0 is the first FROM stage',
      'COPY --index=0 /app .',
      'COPY --position=0 /app .',
    ],
    correctAnswer: 1,
    explanation: 'Stages are numbered starting from 0. `COPY --from=0` copies from the first stage, `--from=1` from the second, etc. Named stages are preferred for readability.',
    tags: ['multi-stage', 'numbering', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-020',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Heredoc Syntax',
    question: 'What is the heredoc syntax in BuildKit Dockerfiles?',
    options: [
      'A documentation system for Dockerfiles',
      'A syntax for inline multi-line strings using <<EOF...EOF in RUN and COPY instructions',
      'A template inheritance system',
      'A YAML-like syntax for Dockerfiles',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit heredoc syntax allows writing multi-line content directly in the Dockerfile using <<EOF delimiters, reducing the need for external files.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nRUN <<EOF\napt-get update\napt-get install -y curl git\nrm -rf /var/lib/apt/lists/*\nEOF',
    tags: ['heredoc', 'buildkit', 'syntax'],
  },
  {
    id: 'docker-dockerfile-l3-021',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Heredoc Syntax',
    question: 'How do you create a file inline using heredoc in a Dockerfile?',
    options: [
      'RUN echo "content" > file',
      'Use COPY with heredoc syntax to write file contents directly in the Dockerfile',
      'FILE /path/to/file "content"',
      'WRITE /path/to/file content',
    ],
    correctAnswer: 1,
    explanation: 'COPY with heredoc allows creating files with inline content directly in the Dockerfile, without needing an external file in the build context.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nCOPY <<EOF /etc/nginx/conf.d/default.conf\nserver {\n    listen 80;\n    location / {\n        proxy_pass http://app:3000;\n    }\n}\nEOF',
    tags: ['heredoc', 'copy', 'inline-files'],
  },
  {
    id: 'docker-dockerfile-l3-022',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Heredoc Syntax',
    question: 'How do you specify a different interpreter for a heredoc RUN instruction?',
    options: [
      'RUN LANGUAGE python',
      'Use a shebang-like syntax: RUN <<EOF python3',
      'SHELL ["python3"] before RUN',
      'RUN --interpreter=python3',
    ],
    correctAnswer: 1,
    explanation: 'You can specify the interpreter directly after the heredoc delimiter, similar to a script shebang. This allows running inline scripts in Python, bash, or any interpreter.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nRUN <<EOF python3\nimport json\nconfig = {"key": "value"}\nwith open("/etc/app/config.json", "w") as f:\n    json.dump(config, f)\nEOF',
    tags: ['heredoc', 'interpreter', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-023',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you scan a Docker image for vulnerabilities?',
    options: [
      'docker audit <image>',
      'Use docker scout cves, Trivy, Snyk, or Grype to scan image layers',
      'docker security-check <image>',
      'Docker automatically scans all images',
    ],
    correctAnswer: 1,
    explanation: 'Several tools scan Docker images: `docker scout cves` (Docker), Trivy (Aqua Security), Snyk Container, and Grype (Anchore). They check packages against CVE databases.',
    codeSnippet: '# Docker Scout\ndocker scout cves myapp:latest\n\n# Trivy\ntrivy image myapp:latest',
    tags: ['security', 'scanning', 'vulnerabilities'],
  },
  {
    id: 'docker-dockerfile-l3-024',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you pin base images by digest for security?',
    options: [
      'Use FROM image:tag --verified',
      'Use FROM image@sha256:<digest> to reference an immutable image version',
      'Use FROM image:latest --secure',
      'Set DOCKER_CONTENT_TRUST=1',
    ],
    correctAnswer: 1,
    explanation: 'Pinning by digest (SHA256 hash) ensures you always use the exact same image. Tags can be moved to different images, but digests are immutable.',
    codeSnippet: 'FROM node:20-alpine@sha256:1a526b97c...',
    tags: ['security', 'digest', 'pinning'],
  },
  {
    id: 'docker-dockerfile-l3-025',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you optimize a .NET Docker image?',
    options: [
      'Use the full SDK image for runtime',
      'Use multi-stage: SDK for build/publish, then runtime-only image for the final stage',
      'Install .NET manually in an Ubuntu image',
      'Use a Windows container',
    ],
    correctAnswer: 1,
    explanation: 'Use the .NET SDK image for building and publishing, then copy the output to the smaller ASP.NET runtime image. This significantly reduces the final image size.',
    codeSnippet: 'FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build\nWORKDIR /src\nCOPY *.csproj .\nRUN dotnet restore\nCOPY . .\nRUN dotnet publish -c Release -o /app\n\nFROM mcr.microsoft.com/dotnet/aspnet:8.0\nCOPY --from=build /app .\nENTRYPOINT ["dotnet", "MyApp.dll"]',
    tags: ['dotnet', 'multi-stage', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-026',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the benefit of using `COPY --link` in Dockerfiles?',
    options: [
      'Creates symbolic links instead of copies',
      'Creates layers independent of previous layers, enabling better cache reuse when base image changes',
      'Links files between stages without copying',
      'Creates hard links for space efficiency',
    ],
    correctAnswer: 1,
    explanation: '`COPY --link` creates layers that do not depend on the content of previous layers. This means layers can be reused even when the base image changes, significantly improving cache efficiency.',
    codeSnippet: 'FROM nginx:alpine\nCOPY --link dist/ /usr/share/nginx/html/\n# This layer can be reused even if nginx:alpine updates',
    tags: ['copy', 'link', 'cache'],
  },
  {
    id: 'docker-dockerfile-l3-027',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you optimize Maven builds in Docker?',
    options: [
      'Copy the entire project and run mvn package',
      'Copy pom.xml first, resolve dependencies with mvn dependency:resolve, then copy source code',
      'Use gradle instead',
      'Build outside Docker and copy the JAR',
    ],
    correctAnswer: 1,
    explanation: 'Copy the POM file and download dependencies first to leverage Docker cache. Source code changes will not trigger dependency re-downloads.',
    codeSnippet: 'FROM maven:3.9-eclipse-temurin-21 AS builder\nWORKDIR /app\nCOPY pom.xml .\nRUN mvn dependency:resolve\nCOPY src ./src\nRUN mvn package -DskipTests\n\nFROM eclipse-temurin:21-jre-alpine\nCOPY --from=builder /app/target/*.jar app.jar\nCMD ["java", "-jar", "app.jar"]',
    tags: ['maven', 'java', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-028',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'How do you use BuildKit bind mounts in a RUN instruction?',
    options: [
      'RUN --bind /source:/target command',
      'RUN --mount=type=bind,source=file,target=/path command',
      'BIND /source /target',
      'RUN --volume file:/path command',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit bind mounts make files from the build context or another stage available during a RUN instruction without copying them into the image layer.',
    codeSnippet: 'RUN --mount=type=bind,source=requirements.txt,target=/tmp/requirements.txt \\\n  pip install -r /tmp/requirements.txt',
    tags: ['buildkit', 'bind-mount', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-029',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What is the `# syntax=` parser directive?',
    options: [
      'A comment with no effect',
      'A directive that specifies the BuildKit frontend used to parse the Dockerfile',
      'A syntax highlighting directive for editors',
      'A version declaration for the Dockerfile format',
    ],
    correctAnswer: 1,
    explanation: 'The `# syntax=` directive must be the first line and tells BuildKit which frontend to use. This enables using newer Dockerfile features without upgrading Docker.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\n# Now you can use the latest features\nFROM alpine\nRUN --mount=type=cache,target=/var/cache/apk apk add curl',
    tags: ['buildkit', 'syntax', 'parser'],
  },
  {
    id: 'docker-dockerfile-l3-030',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you use UPX to compress binaries in Docker images?',
    options: [
      'Docker compresses binaries automatically',
      'Install UPX in the builder stage, compress the binary, then copy it to the final stage',
      'Use the --compress flag in docker build',
      'UPX only works outside Docker',
    ],
    correctAnswer: 1,
    explanation: 'UPX (Ultimate Packer for eXecutables) compresses executables, typically reducing binary size by 50-70%. Use it in the builder stage before copying to the final image.',
    codeSnippet: 'FROM golang:1.22 AS builder\nRUN go build -ldflags="-w -s" -o /app\nRUN apt-get update && apt-get install -y upx && \\\n    upx --best --lzma /app\n\nFROM scratch\nCOPY --from=builder /app /app',
    tags: ['upx', 'compression', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-031',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is the benefit of using distroless images?',
    options: [
      'They are faster to build',
      'They contain no shell, package manager, or unnecessary OS tools, minimizing attack surface',
      'They support all Linux packages',
      'They are smaller than scratch images',
    ],
    correctAnswer: 1,
    explanation: 'Distroless images from Google contain only the application runtime (e.g., JRE, Python interpreter). With no shell or package manager, there are fewer potential vulnerabilities and no way to exec into the container.',
    codeSnippet: 'FROM gcr.io/distroless/java21-debian12\nCOPY --from=builder /app/target/app.jar /app.jar\nCMD ["app.jar"]',
    tags: ['distroless', 'security', 'minimal'],
  },
  {
    id: 'docker-dockerfile-l3-032',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you create a non-root user in a distroless image?',
    options: [
      'RUN adduser in the distroless stage',
      'Use the USER instruction with the numeric UID since distroless has predefined users',
      'You cannot run as non-root in distroless',
      'Install user management tools first',
    ],
    correctAnswer: 1,
    explanation: 'Distroless images have no shell or user management tools. They include a predefined `nonroot` user (UID 65532). Use `USER nonroot` or `USER 65532`.',
    codeSnippet: 'FROM gcr.io/distroless/static-debian12:nonroot\n# or\nFROM gcr.io/distroless/static-debian12\nUSER 65532',
    tags: ['distroless', 'user', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-033',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you selectively build only changed services in a monorepo?',
    options: [
      'Rebuild everything every time',
      'Use targeted COPY instructions and build stages per service with shared dependency stages',
      'Use separate repos for each service',
      'Docker handles monorepo optimization automatically',
    ],
    correctAnswer: 1,
    explanation: 'Structure the Dockerfile with a shared base for common dependencies, then per-service stages that only COPY their specific source code. Changes to one service do not rebuild others.',
    codeSnippet: 'FROM node:20 AS deps\nCOPY package*.json ./\nRUN npm ci\n\nFROM deps AS api\nCOPY packages/api ./packages/api\nRUN npm run build:api\n\nFROM deps AS web\nCOPY packages/web ./packages/web\nRUN npm run build:web',
    tags: ['monorepo', 'multi-stage', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-034',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle native dependencies (C extensions) in Python Alpine images?',
    options: [
      'Alpine handles native dependencies automatically',
      'Install build dependencies, compile wheels, then remove build deps; or use multi-stage builds',
      'Use pip install --no-build',
      'Native dependencies are not supported on Alpine',
    ],
    correctAnswer: 1,
    explanation: 'Alpine uses musl libc and lacks pre-built wheels for many Python packages. You need to install build tools (gcc, musl-dev), compile the packages, and then clean up or use multi-stage builds.',
    codeSnippet: 'FROM python:3.12-alpine AS builder\nRUN apk add --no-cache gcc musl-dev libffi-dev\nCOPY requirements.txt .\nRUN pip wheel --no-cache-dir --wheel-dir=/wheels -r requirements.txt\n\nFROM python:3.12-alpine\nCOPY --from=builder /wheels /wheels\nRUN pip install --no-cache-dir /wheels/*',
    tags: ['python', 'alpine', 'native-deps'],
  },
  {
    id: 'docker-dockerfile-l3-035',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What are the security implications of ADD with a URL?',
    options: [
      'No security concerns',
      'ADD URL does not verify checksums, may download over HTTP, and creates an extra layer',
      'ADD URLs are always verified',
      'ADD URLs are blocked by default',
    ],
    correctAnswer: 1,
    explanation: 'ADD with URLs does not verify file integrity (no checksum check), may download over insecure HTTP, and creates a non-removable layer. Use RUN curl with checksum verification instead.',
    codeSnippet: '# Bad: no verification\nADD https://example.com/binary /usr/local/bin/\n\n# Good: download, verify, cleanup in one layer\nRUN curl -fsSL https://example.com/binary -o /usr/local/bin/binary && \\\n    echo "expected_sha256  /usr/local/bin/binary" | sha256sum -c - && \\\n    chmod +x /usr/local/bin/binary',
    tags: ['add', 'url', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-036',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'How do you conditionally copy files based on platform in a Dockerfile?',
    options: [
      'Use IF/ELSE in the Dockerfile',
      'Use TARGETPLATFORM, TARGETOS, or TARGETARCH ARG variables in a RUN instruction',
      'Use separate Dockerfiles per platform',
      'Docker does not support conditional logic',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit provides automatic platform ARGs (TARGETPLATFORM, TARGETOS, TARGETARCH) that can be used in RUN instructions to conditionally download or build platform-specific artifacts.',
    codeSnippet: 'ARG TARGETARCH\nRUN case $TARGETARCH in \\\n    amd64) ARCH=x86_64 ;; \\\n    arm64) ARCH=aarch64 ;; \\\n    esac && \\\n    curl -fsSL https://example.com/app-$ARCH -o /usr/local/bin/app',
    tags: ['multi-platform', 'conditional', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-037',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the `--mount=type=cache` pattern for Go module downloads?',
    options: [
      'Cache the Go binary',
      'Mount the Go module cache as a persistent BuildKit cache to avoid re-downloading modules',
      'Cache the final binary',
      'Cache the build context',
    ],
    correctAnswer: 1,
    explanation: 'Mounting the Go module cache (`GOPATH/pkg/mod`) and build cache as BuildKit cache mounts allows modules to persist between builds, dramatically speeding up dependency resolution.',
    codeSnippet: 'RUN --mount=type=cache,target=/go/pkg/mod \\\n    --mount=type=cache,target=/root/.cache/go-build \\\n    go build -o /app',
    tags: ['go', 'cache-mount', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-038',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you use `.dockerignore` with multiple Dockerfiles?',
    options: [
      'Only one .dockerignore per project',
      'Create Dockerfile-specific ignore files like Dockerfile.prod.dockerignore',
      'Use multiple .dockerignore files in subdirectories',
      'Dockerignore does not work with multiple Dockerfiles',
    ],
    correctAnswer: 1,
    explanation: 'Docker supports Dockerfile-specific ignore files. For `Dockerfile.prod`, Docker looks for `Dockerfile.prod.dockerignore` first, then falls back to `.dockerignore`.',
    codeSnippet: '# Dockerfile.prod.dockerignore\ntest/\n*.test.js\n.env.development\n\n# Dockerfile.dev.dockerignore\n# Less restrictive for development',
    tags: ['dockerignore', 'multiple', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-039',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you audit installed packages in a Docker image?',
    options: [
      'Docker keeps an automatic audit log',
      'Generate an SBOM using docker scout, Syft, or Trivy to list all packages and versions',
      'Inspect the Dockerfile manually',
      'Use docker inspect for package list',
    ],
    correctAnswer: 1,
    explanation: 'SBOM (Software Bill of Materials) tools scan image contents and list all packages, libraries, and their versions. This enables vulnerability tracking and compliance auditing.',
    codeSnippet: '# Generate SBOM with Syft\nsyft myapp:latest\n\n# With Docker Scout\ndocker scout sbom myapp:latest',
    tags: ['sbom', 'audit', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-040',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you run tests as a build gate in a multi-stage Dockerfile?',
    options: [
      'Tests should be run outside Docker',
      'Create a test stage that runs after build; the build fails if tests fail, preventing the production image from being created',
      'Add tests to the CMD instruction',
      'Use docker compose for testing',
    ],
    correctAnswer: 1,
    explanation: 'A test stage that depends on the build stage runs tests as part of the build process. If tests fail, the build fails and no production image is created.',
    codeSnippet: 'FROM golang:1.22 AS builder\nCOPY . .\nRUN go build -o app\n\nFROM builder AS tester\nRUN go test ./...\n\nFROM alpine\nCOPY --from=builder /app/app /usr/local/bin/\n# Only reached if tests pass',
    tags: ['multi-stage', 'testing', 'ci'],
  },
  {
    id: 'docker-dockerfile-l3-041',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the difference between `npm install` and `npm ci` for Docker builds?',
    options: [
      'They are identical',
      'npm ci is faster, deletes node_modules, installs exact lockfile versions, and fails if lockfile is out of sync',
      'npm install is for production; npm ci is for development',
      'npm ci only installs devDependencies',
    ],
    correctAnswer: 1,
    explanation: 'npm ci: (1) deletes node_modules, (2) installs exact versions from lockfile, (3) fails if lockfile does not match package.json, (4) never modifies lockfile. Ideal for Docker builds.',
    tags: ['npm', 'ci', 'comparison'],
  },
  {
    id: 'docker-dockerfile-l3-042',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle .NET restore caching in Docker?',
    options: [
      'Restore and build in the same step',
      'Copy only the .csproj files first, run dotnet restore, then copy the rest and build',
      'Use the --no-restore flag',
      'Cache is not possible with .NET in Docker',
    ],
    correctAnswer: 1,
    explanation: 'Copy .csproj (and .sln if applicable) files first and run `dotnet restore` to leverage Docker cache for NuGet package restoration. Source code changes will not trigger re-restore.',
    codeSnippet: 'FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build\nWORKDIR /src\nCOPY *.csproj .\nRUN dotnet restore\nCOPY . .\nRUN dotnet publish -c Release -o /app --no-restore',
    tags: ['dotnet', 'restore', 'caching'],
  },
  {
    id: 'docker-dockerfile-l3-043',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is the principle of minimal base images?',
    options: [
      'Using the cheapest base image',
      'Using the smallest image that includes only what your application needs, reducing attack surface',
      'Using images with minimal documentation',
      'Using images with fewer layers',
    ],
    correctAnswer: 1,
    explanation: 'Minimal base images (Alpine, distroless, scratch) contain fewer packages, meaning fewer potential vulnerabilities. A smaller attack surface makes containers more secure.',
    tags: ['security', 'minimal', 'base-image'],
  },
  {
    id: 'docker-dockerfile-l3-044',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-platform',
    question: 'How do you write a Dockerfile that works for both AMD64 and ARM64?',
    options: [
      'Create separate Dockerfiles for each architecture',
      'Use platform-agnostic instructions or ARG TARGETARCH for architecture-specific steps',
      'Docker automatically handles all architectures',
      'Only use FROM scratch',
    ],
    correctAnswer: 1,
    explanation: 'Most Dockerfile instructions are platform-agnostic. For architecture-specific steps (downloading binaries), use the TARGETARCH ARG with conditional logic.',
    codeSnippet: 'FROM node:20-alpine\nARG TARGETARCH\nRUN if [ "$TARGETARCH" = "arm64" ]; then \\\n      apk add --no-cache some-arm-package; \\\n    fi\nCOPY . .\nCMD ["node", "server.js"]',
    tags: ['multi-platform', 'arm64', 'amd64'],
  },
  {
    id: 'docker-dockerfile-l3-045',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you reduce Node.js image size by pruning devDependencies?',
    options: [
      'Manually delete packages from node_modules',
      'Use npm ci --omit=dev or a multi-stage build that installs all deps for build, then only production deps for runtime',
      'Use a smaller node image',
      'Set NODE_ENV=production in the Dockerfile',
    ],
    correctAnswer: 1,
    explanation: 'A multi-stage approach: install all dependencies for building, then in the final stage install only production dependencies. Or use `npm ci --omit=dev` if no build step is needed.',
    codeSnippet: 'FROM node:20-alpine AS builder\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY --from=builder /app/dist ./dist\nCMD ["node", "dist/server.js"]',
    tags: ['node', 'pruning', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-046',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What is `RUN --network=none` in a Dockerfile?',
    options: [
      'Runs the container without networking at runtime',
      'Disables network access for that specific RUN instruction during build',
      'Creates a network called none',
      'Tests the application without network',
    ],
    correctAnswer: 1,
    explanation: '`RUN --network=none` disables all network access for that build step. This ensures the step cannot download anything, improving reproducibility and security.',
    codeSnippet: '# This step can only use local files\nRUN --network=none gcc -o app main.c',
    tags: ['buildkit', 'network', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-047',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle timezone data efficiently in Alpine-based images?',
    options: [
      'Alpine includes all timezone data by default',
      'Install tzdata, copy the needed timezone, then remove tzdata to save space',
      'Use ENV TZ only',
      'Timezone is not supported on Alpine',
    ],
    correctAnswer: 1,
    explanation: 'Alpine does not include timezone data by default. Install tzdata, copy your timezone to /etc/localtime, and remove tzdata to keep the image small.',
    codeSnippet: 'RUN apk add --no-cache --virtual .tz tzdata && \\\n    cp /usr/share/zoneinfo/UTC /etc/localtime && \\\n    echo "UTC" > /etc/timezone && \\\n    apk del .tz',
    tags: ['alpine', 'timezone', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-048',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you prevent shell injection in Dockerfiles?',
    options: [
      'Docker prevents injection automatically',
      'Use exec form for CMD/ENTRYPOINT and validate/sanitize ARG values used in RUN instructions',
      'Use single quotes around all values',
      'Disable shell access',
    ],
    correctAnswer: 1,
    explanation: 'Exec form avoids shell interpretation. For RUN instructions with ARG values, validate inputs and use exec form where possible to prevent injection through build arguments.',
    codeSnippet: '# Vulnerable to injection\nARG USER_INPUT\nRUN echo $USER_INPUT\n\n# Safer: exec form\nRUN ["echo", "fixed-string"]',
    tags: ['security', 'injection', 'best-practices'],
  },
  {
    id: 'docker-dockerfile-l3-049',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'What is a ci-specific stage in multi-stage builds?',
    options: [
      'A stage for configuring CI/CD',
      'A dedicated stage for CI tasks like linting, testing, and coverage that is not included in the production image',
      'A stage that runs only in CI pipelines',
      'A stage for continuous integration tools',
    ],
    correctAnswer: 1,
    explanation: 'CI-specific stages run linters, tests, security scans, and generate coverage reports. They use `--target ci` in CI pipelines while production builds use `--target production`.',
    codeSnippet: 'FROM node:20 AS base\nCOPY . .\n\nFROM base AS ci\nRUN npm ci\nRUN npm run lint\nRUN npm test\nRUN npm audit\n\nFROM base AS production\nRUN npm ci --omit=dev\nCMD ["node", "server.js"]',
    tags: ['multi-stage', 'ci', 'testing'],
  },
  {
    id: 'docker-dockerfile-l3-050',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you optimize Gradle builds in Docker?',
    options: [
      'Copy everything and run gradle build',
      'Use BuildKit cache mounts for the Gradle cache directory and copy build files before source',
      'Use Maven instead',
      'Build outside Docker',
    ],
    correctAnswer: 1,
    explanation: 'Mount the Gradle cache as a BuildKit cache mount and copy build configuration files before source code to maximize cache usage.',
    codeSnippet: 'FROM gradle:8-jdk21 AS builder\nWORKDIR /app\nCOPY build.gradle.kts settings.gradle.kts ./\nRUN --mount=type=cache,target=/home/gradle/.gradle \\\n    gradle dependencies --no-daemon\nCOPY src ./src\nRUN --mount=type=cache,target=/home/gradle/.gradle \\\n    gradle build --no-daemon',
    tags: ['gradle', 'java', 'cache-mount'],
  },
  {
    id: 'docker-dockerfile-l3-051',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'How do you use `RUN --mount=type=secret` with environment variables?',
    options: [
      'Secrets are automatically available as env vars',
      'Mount the secret as a file and read it into an env var within the RUN instruction',
      'Use --secret-env flag',
      'Secrets cannot be used as env vars',
    ],
    correctAnswer: 1,
    explanation: 'Secrets are mounted as files. To use them as environment variables, read the file content into a variable within the same RUN instruction.',
    codeSnippet: 'RUN --mount=type=secret,id=api_key \\\n  export API_KEY=$(cat /run/secrets/api_key) && \\\n  curl -H "Authorization: Bearer $API_KEY" https://api.example.com/data',
    tags: ['buildkit', 'secrets', 'env'],
  },
  {
    id: 'docker-dockerfile-l3-052',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you minimize layer size when installing packages from a private repo?',
    options: [
      'Add the private key to the image',
      'Use BuildKit secret mount for authentication and cache mount for packages, all in one layer',
      'Download packages before building',
      'Use a public mirror',
    ],
    correctAnswer: 1,
    explanation: 'Combine secret mounts (for authentication) with cache mounts (for package cache) in a single RUN instruction. The secret never enters the image and packages are cached between builds.',
    codeSnippet: 'RUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n    --mount=type=cache,target=/root/.npm \\\n    npm ci',
    tags: ['secrets', 'cache-mount', 'private-repo'],
  },
  {
    id: 'docker-dockerfile-l3-053',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is the recommended way to keep base images updated?',
    options: [
      'Always use :latest tag',
      'Use automation tools like Renovate or Dependabot to automatically update pinned base image versions',
      'Manually check Docker Hub weekly',
      'Never update base images for stability',
    ],
    correctAnswer: 1,
    explanation: 'Tools like Renovate and Dependabot automatically create PRs when new base image versions are available, keeping images secure while maintaining version control.',
    tags: ['security', 'updates', 'automation'],
  },
  {
    id: 'docker-dockerfile-l3-054',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you build and test a monorepo with shared libraries efficiently?',
    options: [
      'Build everything in one stage',
      'Use a shared dependencies stage, then parallel per-service stages that COPY only their code',
      'Build each service in a separate Dockerfile',
      'Use submodules',
    ],
    correctAnswer: 1,
    explanation: 'A shared deps stage installs common dependencies. Per-service stages copy only their specific source code plus shared libraries, minimizing rebuild scope.',
    codeSnippet: 'FROM node:20 AS deps\nCOPY package.json yarn.lock ./\nCOPY packages/shared/package.json packages/shared/\nCOPY packages/api/package.json packages/api/\nRUN yarn install --frozen-lockfile\n\nFROM deps AS api-build\nCOPY packages/shared ./packages/shared\nCOPY packages/api ./packages/api\nRUN yarn workspace api build',
    tags: ['monorepo', 'multi-stage', 'workspaces'],
  },
  {
    id: 'docker-dockerfile-l3-055',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you use `COPY --parents` in BuildKit?',
    options: [
      'Copies parent directories only',
      'Preserves the directory structure of the source path in the destination',
      'Copies from parent stages',
      'Inherits COPY from parent image',
    ],
    correctAnswer: 1,
    explanation: '`COPY --parents` (BuildKit) preserves the directory structure of copied files. For example, copying `src/app/main.go` preserves the `src/app/` directory hierarchy.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nCOPY --parents src/app/main.go src/lib/ /build/',
    tags: ['copy', 'parents', 'buildkit'],
  },
  {
    id: 'docker-dockerfile-l3-056',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the impact of ENTRYPOINT shell form on container memory usage?',
    options: [
      'No impact on memory',
      'Shell form spawns an extra /bin/sh process that uses memory and prevents proper signal handling',
      'Shell form uses less memory',
      'Memory impact depends on the shell type',
    ],
    correctAnswer: 1,
    explanation: 'Shell form spawns `/bin/sh -c` as an extra parent process. This wastes memory and, more critically, prevents SIGTERM from reaching the application for graceful shutdown.',
    codeSnippet: '# Shell form: 2 processes (sh + app)\nENTRYPOINT java -jar app.jar\n\n# Exec form: 1 process (app only)\nENTRYPOINT ["java", "-jar", "app.jar"]',
    tags: ['entrypoint', 'shell-form', 'memory'],
  },
  {
    id: 'docker-dockerfile-l3-057',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What is the `id` parameter in cache mounts?',
    options: [
      'A unique identifier for the build',
      'An identifier that allows different RUN instructions to share the same persistent cache',
      'The container ID',
      'The image layer ID',
    ],
    correctAnswer: 1,
    explanation: 'The `id` parameter in cache mounts creates named caches that can be shared across different RUN instructions and even different Dockerfiles. Without `id`, the target path is used as the identifier.',
    codeSnippet: 'RUN --mount=type=cache,id=npm-cache,target=/root/.npm \\\n    npm ci\n\n# Another stage can share the same cache\nRUN --mount=type=cache,id=npm-cache,target=/root/.npm \\\n    npm ci --production',
    tags: ['buildkit', 'cache-mount', 'sharing'],
  },
  {
    id: 'docker-dockerfile-l3-058',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you create a TypeScript application Docker image efficiently?',
    options: [
      'Include TypeScript compiler in the runtime image',
      'Compile TypeScript in a builder stage, copy only the compiled JavaScript to the runtime stage',
      'Use ts-node in production',
      'Compile TypeScript on the host before Docker build',
    ],
    correctAnswer: 1,
    explanation: 'Use a builder stage with TypeScript and dev dependencies to compile. Copy only the compiled JavaScript and production dependencies to the minimal runtime stage.',
    codeSnippet: 'FROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package*.json tsconfig.json ./\nRUN npm ci\nCOPY src ./src\nRUN npm run build\n\nFROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --omit=dev\nCOPY --from=builder /app/dist ./dist\nCMD ["node", "dist/index.js"]',
    tags: ['typescript', 'multi-stage', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-059',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you handle CA certificates in minimal Docker images?',
    options: [
      'Certificates are included in all base images',
      'Copy ca-certificates from a builder stage for scratch/distroless images or install the ca-certificates package',
      'Mount certificates at runtime',
      'Disable TLS verification',
    ],
    correctAnswer: 1,
    explanation: 'Minimal images may lack CA certificates needed for HTTPS. Either install the `ca-certificates` package or copy them from a stage that has them.',
    codeSnippet: 'FROM alpine:3.19 AS certs\nRUN apk add --no-cache ca-certificates\n\nFROM scratch\nCOPY --from=certs /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/\nCOPY app /app\nENTRYPOINT ["/app"]',
    tags: ['certificates', 'scratch', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-060',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What does `# check=skip=all` do in a Dockerfile?',
    options: [
      'Skips all HEALTHCHECK instructions',
      'Skips all Dockerfile lint checks when using BuildKit check feature',
      'Skips all build stages',
      'Skips all cache checks',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit supports inline lint checking. The `# check=` directive controls which lint rules to skip or enable. `skip=all` disables all lint checks.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\n# check=skip=SecretsUsedInArgOrEnv\nARG BUILD_TOKEN\n# This suppresses the lint warning about secrets in ARG',
    tags: ['buildkit', 'linting', 'check'],
  },
  {
    id: 'docker-dockerfile-l3-061',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-platform',
    question: 'How do you handle platform-specific binary downloads in Dockerfiles?',
    options: [
      'Download both architectures and select at runtime',
      'Use TARGETARCH and TARGETOS ARGs to construct the correct download URL',
      'Use separate Dockerfiles per platform',
      'Always download the x86_64 version',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit provides TARGETARCH (amd64, arm64), TARGETOS (linux), and TARGETPLATFORM (linux/amd64) variables for constructing architecture-specific download URLs.',
    codeSnippet: 'ARG TARGETARCH\nRUN curl -fsSL "https://github.com/tool/releases/download/v1.0/tool-linux-\${TARGETARCH}" \\\n    -o /usr/local/bin/tool && \\\n    chmod +x /usr/local/bin/tool',
    tags: ['multi-platform', 'downloads', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-062',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the benefit of using `apk add --virtual` in Alpine Dockerfiles?',
    options: [
      'Creates virtual packages for simulation',
      'Groups packages under a virtual name for easy removal after build',
      'Installs virtual machine packages',
      'Creates package aliases',
    ],
    correctAnswer: 1,
    explanation: '`--virtual` creates a named group of packages. After using them for building, `apk del <virtual-name>` removes all packages in the group, keeping the image clean.',
    codeSnippet: 'RUN apk add --no-cache --virtual .build-deps \\\n      gcc musl-dev python3-dev && \\\n    pip install --no-cache-dir cryptography && \\\n    apk del .build-deps',
    tags: ['alpine', 'apk', 'virtual'],
  },
  {
    id: 'docker-dockerfile-l3-063',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you extract only specific files from a builder stage?',
    options: [
      'All files are copied automatically',
      'Use COPY --from with specific paths to copy only needed files',
      'Use EXTRACT instruction',
      'Delete unwanted files in the final stage',
    ],
    correctAnswer: 1,
    explanation: 'Use multiple targeted COPY --from instructions to cherry-pick only the files needed from the builder stage, leaving behind build tools, source code, and intermediate files.',
    codeSnippet: 'FROM builder AS final\nCOPY --from=builder /app/build/main /usr/local/bin/\nCOPY --from=builder /app/config /etc/app/\nCOPY --from=builder /app/migrations /migrations/',
    tags: ['multi-stage', 'copy-from', 'selective'],
  },
  {
    id: 'docker-dockerfile-l3-064',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you optimize Docker image size for a Next.js application?',
    options: [
      'Use the default Next.js build output',
      'Enable standalone output mode in Next.js, which creates a minimal server with only necessary files',
      'Use a CDN instead of Docker',
      'Compile Next.js to static HTML',
    ],
    correctAnswer: 1,
    explanation: 'Next.js standalone output mode creates a self-contained deployment with only the files needed for production, dramatically reducing Docker image size compared to including all of node_modules.',
    codeSnippet: '# next.config.js: output: \'standalone\'\nFROM node:20-alpine AS builder\nCOPY . .\nRUN npm ci && npm run build\n\nFROM node:20-alpine\nCOPY --from=builder /app/.next/standalone ./\nCOPY --from=builder /app/.next/static ./.next/static\nCOPY --from=builder /app/public ./public\nCMD ["node", "server.js"]',
    tags: ['nextjs', 'standalone', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-065',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'How do you pass build secrets from environment variables?',
    options: [
      'docker build --secret id=mysecret,env=MY_SECRET',
      'docker build --secret id=mysecret,src=<(echo $MY_SECRET)',
      'docker build --build-arg SECRET=$MY_SECRET',
      'Both the env= option and process substitution approaches work',
    ],
    correctAnswer: 0,
    explanation: 'BuildKit supports `env=` in the --secret flag to pass secrets directly from environment variables, avoiding the need to write them to temporary files.',
    codeSnippet: 'export MY_TOKEN=abc123\ndocker build --secret id=token,env=MY_TOKEN .',
    tags: ['buildkit', 'secrets', 'env'],
  },
  {
    id: 'docker-dockerfile-l3-066',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What are `.whl` files and how do they speed up Python Docker builds?',
    options: [
      'Wheel configuration files',
      'Pre-built Python package distributions that install without compilation',
      'Watchdog health libraries',
      'Worker host libraries',
    ],
    correctAnswer: 1,
    explanation: 'Wheel (.whl) files are pre-compiled Python packages. Building wheels in a builder stage avoids needing C compilers and development libraries in the final image.',
    codeSnippet: 'FROM python:3.12 AS builder\nRUN pip wheel --no-cache-dir --wheel-dir=/wheels -r requirements.txt\n\nFROM python:3.12-slim\nCOPY --from=builder /wheels /wheels\nRUN pip install --no-cache-dir /wheels/* && rm -rf /wheels',
    tags: ['python', 'wheels', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-067',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you verify GPG signatures of downloaded packages in Dockerfiles?',
    options: [
      'Docker verifies signatures automatically',
      'Import the GPG key, download the signature file, and verify before using the package',
      'Use ADD instead of curl',
      'Signatures cannot be verified in Docker',
    ],
    correctAnswer: 1,
    explanation: 'For security, import the maintainer GPG key, download the detached signature, and verify the package integrity before extracting or installing.',
    codeSnippet: 'RUN gpg --keyserver keyserver.ubuntu.com --recv-keys KEY_ID && \\\n    curl -fsSL https://example.com/app.tar.gz -o app.tar.gz && \\\n    curl -fsSL https://example.com/app.tar.gz.asc -o app.tar.gz.asc && \\\n    gpg --verify app.tar.gz.asc app.tar.gz && \\\n    tar xzf app.tar.gz',
    tags: ['gpg', 'verification', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-068',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle font and locale requirements in minimal Docker images?',
    options: [
      'Fonts are always available',
      'Install only the specific font packages needed and configure locale with minimal packages',
      'Use a full desktop Linux image',
      'Mount fonts from the host',
    ],
    correctAnswer: 1,
    explanation: 'Install only the specific fonts needed (e.g., fontconfig, ttf-dejavu for CJK support) rather than full font packages. Configure locale with minimal locale-gen.',
    codeSnippet: '# Alpine with basic fonts\nRUN apk add --no-cache fontconfig ttf-dejavu\n\n# Debian with specific locale\nRUN apt-get update && \\\n    apt-get install -y --no-install-recommends locales && \\\n    sed -i \'/en_US.UTF-8/s/^# //\' /etc/locale.gen && \\\n    locale-gen',
    tags: ['fonts', 'locale', 'minimal'],
  },
  {
    id: 'docker-dockerfile-l3-069',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What is `COPY --exclude` in BuildKit?',
    options: [
      'Not a valid flag',
      'A flag that excludes specific patterns from the COPY source, available in newer BuildKit versions',
      'Excludes files from the image',
      'Excludes layers from the build',
    ],
    correctAnswer: 1,
    explanation: '`COPY --exclude` (BuildKit 1.7+) allows excluding files matching patterns from a COPY instruction, similar to .dockerignore but per-instruction.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nCOPY --exclude=*.test.js --exclude=__tests__ . /app/',
    tags: ['copy', 'exclude', 'buildkit'],
  },
  {
    id: 'docker-dockerfile-l3-070',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you use multi-stage builds with Docker Compose?',
    options: [
      'Docker Compose does not support multi-stage',
      'Specify the target stage in the Compose build configuration',
      'Use separate Dockerfiles referenced in Compose',
      'Multi-stage only works with docker build',
    ],
    correctAnswer: 1,
    explanation: 'Docker Compose supports the `target` key in the build configuration to specify which stage to build, enabling different targets for different environments.',
    codeSnippet: 'services:\n  web:\n    build:\n      context: .\n      target: development\n  web-prod:\n    build:\n      context: .\n      target: production',
    tags: ['multi-stage', 'docker-compose', 'target'],
  },
  {
    id: 'docker-dockerfile-l3-071',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you create a container that cannot write to the filesystem?',
    options: [
      'Use chmod 000 on all directories',
      'Design the Dockerfile for read-only root: use volumes for writable data and tmpfs for temp files',
      'Set ENV READONLY=true',
      'Use FROM scratch only',
    ],
    correctAnswer: 1,
    explanation: 'Design the image so all writable paths use volumes or tmpfs. Run with `--read-only` to enforce an immutable root filesystem, enhancing security.',
    codeSnippet: 'FROM node:20-alpine\nWORKDIR /app\nCOPY . .\n\n# Mark writable directories\nVOLUME /app/data\nVOLUME /app/logs\n\n# Run: docker run --read-only --tmpfs /tmp myapp',
    tags: ['read-only', 'security', 'immutable'],
  },
  {
    id: 'docker-dockerfile-l3-072',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you minimize the number of layers in a Dockerfile while maintaining readability?',
    options: [
      'Put everything in one RUN instruction',
      'Combine logically related commands, use heredoc syntax for multi-line scripts, and leverage COPY --link for independent layers',
      'Use only RUN instructions',
      'Layers do not affect image size',
    ],
    correctAnswer: 1,
    explanation: 'Group related commands in single RUN instructions, use heredoc for cleaner multi-line scripts, and use COPY --link for layers that do not depend on previous layers.',
    codeSnippet: '# syntax=docker/dockerfile:1.7\nRUN <<EOF\napt-get update\napt-get install -y --no-install-recommends curl git\nrm -rf /var/lib/apt/lists/*\nEOF\n\nCOPY --link package.json /app/',
    tags: ['layers', 'readability', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-073',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle yarn workspaces in Docker?',
    options: [
      'Copy the entire monorepo',
      'Copy root package.json and yarn.lock first, then per-workspace package.json files, then install',
      'Install each workspace separately',
      'Use npm instead of yarn in Docker',
    ],
    correctAnswer: 1,
    explanation: 'Copy all package.json files matching the workspace structure first, then run yarn install. This leverages Docker cache for dependency installation.',
    codeSnippet: 'FROM node:20-alpine\nWORKDIR /app\nCOPY package.json yarn.lock .yarnrc.yml ./\nCOPY .yarn .yarn\nCOPY packages/api/package.json packages/api/\nCOPY packages/shared/package.json packages/shared/\nRUN yarn install --immutable\nCOPY . .\nRUN yarn workspace api build',
    tags: ['yarn', 'workspaces', 'monorepo'],
  },
  {
    id: 'docker-dockerfile-l3-074',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is the `--security-opt` flag for in docker run?',
    options: [
      'Sets the Docker Hub security token',
      'Configures security options like AppArmor, SELinux, seccomp profiles, and no-new-privileges',
      'Enables encrypted communication',
      'Sets password for the container',
    ],
    correctAnswer: 1,
    explanation: 'The `--security-opt` flag configures various Linux security modules for the container including AppArmor profiles, SELinux labels, seccomp profiles, and privilege restrictions.',
    codeSnippet: 'docker run --security-opt no-new-privileges \\\n  --security-opt seccomp=custom.json \\\n  --security-opt apparmor=docker-default \\\n  myapp',
    tags: ['security', 'security-opt', 'hardening'],
  },
  {
    id: 'docker-dockerfile-l3-075',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'How do you use a secret in a multi-stage build across stages?',
    options: [
      'Secrets are automatically available in all stages',
      'Mount the secret in each RUN instruction that needs it; secrets are not shared between stages',
      'Pass secrets via ARG between stages',
      'Copy secrets from one stage to another',
    ],
    correctAnswer: 1,
    explanation: 'Secrets must be mounted in each RUN instruction that needs them. They are not inherited between stages or instructions, ensuring they are only available when explicitly requested.',
    codeSnippet: 'FROM node:20 AS deps\nRUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n    npm ci\n\nFROM node:20 AS builder\nCOPY --from=deps /app/node_modules ./node_modules\nRUN --mount=type=secret,id=api_key \\\n    API_KEY=$(cat /run/secrets/api_key) npm run build',
    tags: ['buildkit', 'secrets', 'multi-stage'],
  },
  {
    id: 'docker-dockerfile-l3-076',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you create a Rust Docker image with cargo chef for faster builds?',
    options: [
      'Use cargo build directly',
      'Use cargo-chef to pre-compute a dependency plan, cache dependency compilation, then build the application',
      'Build outside Docker',
      'Use a pre-built binary',
    ],
    correctAnswer: 1,
    explanation: 'cargo-chef separates dependency compilation from application compilation. Dependencies are built from a recipe file, enabling caching even when source code changes.',
    codeSnippet: 'FROM rust:1.77 AS chef\nRUN cargo install cargo-chef\nWORKDIR /app\n\nFROM chef AS planner\nCOPY . .\nRUN cargo chef prepare --recipe-path recipe.json\n\nFROM chef AS builder\nCOPY --from=planner /app/recipe.json recipe.json\nRUN cargo chef cook --release --recipe-path recipe.json\nCOPY . .\nRUN cargo build --release',
    tags: ['rust', 'cargo-chef', 'caching'],
  },
  {
    id: 'docker-dockerfile-l3-077',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you efficiently handle pnpm in Docker builds?',
    options: [
      'Use npm instead',
      'Enable pnpm corepack, copy lockfile first, use fetch and install commands for optimal caching',
      'Install pnpm globally in each stage',
      'Use the pnpm Docker image',
    ],
    correctAnswer: 1,
    explanation: 'pnpm in Docker works best with: corepack enable, copy lockfile first, `pnpm fetch` for downloading packages, then `pnpm install --offline` for installation.',
    codeSnippet: 'FROM node:20-alpine\nRUN corepack enable\nWORKDIR /app\nCOPY pnpm-lock.yaml .\nRUN pnpm fetch\nCOPY package.json .\nRUN pnpm install --offline\nCOPY . .\nRUN pnpm build',
    tags: ['pnpm', 'caching', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-078',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What does `RUN --mount=type=bind,from=stage` do?',
    options: [
      'Binds a network port',
      'Makes files from another build stage available during RUN without copying them into the layer',
      'Binds a volume to the container',
      'Creates a bind mount at runtime',
    ],
    correctAnswer: 1,
    explanation: 'A bind mount from another stage makes that stage files temporarily available during the RUN instruction without adding them to the current layer.',
    codeSnippet: 'FROM golang:1.22 AS tools\nRUN go install github.com/tool/cmd@latest\n\nFROM alpine\nRUN --mount=type=bind,from=tools,source=/go/bin/cmd,target=/usr/local/bin/cmd \\\n    cmd generate --output /app/generated',
    tags: ['buildkit', 'bind-mount', 'stages'],
  },
  {
    id: 'docker-dockerfile-l3-079',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle private NPM packages in Docker builds?',
    options: [
      'Hardcode the NPM token in the Dockerfile',
      'Use BuildKit secret mounts with .npmrc file',
      'Set the token in ENV',
      'Use ARG for the NPM token',
    ],
    correctAnswer: 1,
    explanation: 'Mount .npmrc as a BuildKit secret during npm install. The file is available only for that instruction and is never stored in any image layer.',
    codeSnippet: '# .npmrc contains: //registry.npmjs.org/:_authToken=\\${NPM_TOKEN}\nRUN --mount=type=secret,id=npmrc,target=/root/.npmrc \\\n    npm ci\n\n# Build: docker build --secret id=npmrc,src=.npmrc .',
    tags: ['npm', 'private-packages', 'secrets'],
  },
  {
    id: 'docker-dockerfile-l3-080',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'What is image provenance and why does it matter?',
    options: [
      'The image download location',
      'Verifiable metadata about how, where, and from what source an image was built',
      'The image creation date',
      'The number of times an image was pulled',
    ],
    correctAnswer: 1,
    explanation: 'Image provenance is verifiable metadata documenting the build process: source code, builder, build parameters, and environment. It enables supply chain verification and security auditing.',
    codeSnippet: 'docker buildx build --provenance=true -t myapp:latest --push .',
    tags: ['provenance', 'supply-chain', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-081',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you use turborepo with Docker for monorepo builds?',
    options: [
      'Build everything in one stage',
      'Use turbo prune to generate a minimal subset of the monorepo, then build only what is needed',
      'Copy the entire monorepo',
      'Build each package separately',
    ],
    correctAnswer: 1,
    explanation: '`turbo prune` generates a subset of the monorepo containing only the workspace and its dependencies, minimizing the Docker build context and enabling efficient caching.',
    codeSnippet: 'FROM node:20-alpine AS pruner\nRUN npx turbo prune --scope=api --docker\n\nFROM node:20-alpine AS installer\nCOPY --from=pruner /app/out/json/ .\nRUN npm ci\n\nFROM installer AS builder\nCOPY --from=pruner /app/out/full/ .\nRUN npx turbo build --filter=api',
    tags: ['turborepo', 'monorepo', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-082',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'Can you use a build stage as a source in --build-context?',
    options: [
      'No, --build-context only supports directories',
      'Yes, --build-context can reference images, directories, Git URLs, and build stages',
      'Only with Docker Compose',
      'Only local directories and URLs',
    ],
    correctAnswer: 1,
    explanation: '`--build-context` supports multiple source types including local directories, Docker images, Git URLs, HTTP archives, and even other build stages.',
    codeSnippet: 'docker buildx build \\\n  --build-context shared=docker-image://myregistry/shared-libs:latest \\\n  --build-context config=./configs \\\n  -t myapp:latest .',
    tags: ['buildx', 'build-context', 'advanced'],
  },
  {
    id: 'docker-dockerfile-l3-083',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'How do you handle Playwright or Puppeteer browsers in Docker?',
    options: [
      'Install Chrome manually',
      'Install browser dependencies in the builder, or use official Playwright Docker image as base',
      'Browsers cannot run in Docker',
      'Use a full desktop Linux image',
    ],
    correctAnswer: 1,
    explanation: 'Use the official Playwright Docker image (mcr.microsoft.com/playwright) as a base, or install browser dependencies explicitly. For production, use headless browsers only.',
    codeSnippet: 'FROM mcr.microsoft.com/playwright:v1.42.0-jammy\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nCMD ["npx", "playwright", "test"]',
    tags: ['playwright', 'browsers', 'testing'],
  },
  {
    id: 'docker-dockerfile-l3-084',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'BuildKit Features',
    question: 'What is `COPY --from` with a Git URL?',
    options: [
      'Clones a Git repo during COPY',
      'BuildKit supports Git URLs as build contexts that can be referenced in COPY --from',
      'Downloads a single file from GitHub',
      'Copies Git history into the image',
    ],
    correctAnswer: 1,
    explanation: 'With BuildKit and `--build-context`, you can specify a Git URL as a named context and then use COPY --from with that name to include files from the repo.',
    codeSnippet: '# docker build --build-context shared=https://github.com/org/shared.git .\n# In Dockerfile:\nCOPY --from=shared /utils.sh /app/',
    tags: ['buildkit', 'git', 'build-context'],
  },
  {
    id: 'docker-dockerfile-l3-085',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Optimization',
    question: 'What is the advantage of using `npm prune --production` in a separate layer?',
    options: [
      'It is faster than npm ci --production',
      'It removes devDependencies after building, useful when the build step needs devDependencies',
      'It cleans the npm cache',
      'It upgrades production packages',
    ],
    correctAnswer: 1,
    explanation: 'When the build step requires devDependencies (TypeScript, webpack), use `npm prune --production` after building to remove them, keeping only production dependencies in the final image.',
    codeSnippet: 'RUN npm ci\nRUN npm run build\nRUN npm prune --production\n# node_modules now only has production deps',
    tags: ['npm', 'prune', 'optimization'],
  },
  {
    id: 'docker-dockerfile-l3-086',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Security',
    question: 'How do you enforce that a Docker image was built from a specific Dockerfile?',
    options: [
      'Compare the image to the Dockerfile manually',
      'Use build provenance attestations that record the Dockerfile content used during the build',
      'Add a hash of the Dockerfile as a label',
      'It cannot be enforced',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit provenance attestations (especially mode=max) include the complete Dockerfile used to build the image. This can be verified to ensure the image matches the expected build definition.',
    codeSnippet: 'docker buildx build --provenance=mode=max --sbom=true \\\n  -t myapp:latest --push .',
    tags: ['provenance', 'verification', 'security'],
  },
  {
    id: 'docker-dockerfile-l3-087',
    language: 'docker',
    level: 'level3',
    category: 'Dockerfile',
    subcategory: 'Multi-stage Advanced',
    question: 'How do you share a build cache between CI and local development?',
    options: [
      'Cache is always local only',
      'Export cache to a registry or S3 and import it in subsequent builds using --cache-from/--cache-to',
      'Use a shared Docker daemon',
      'Copy the /var/lib/docker directory',
    ],
    correctAnswer: 1,
    explanation: 'BuildKit supports exporting cache to registries, S3, or local directories. CI can export cache after building, and subsequent builds (CI or local) can import it for faster builds.',
    codeSnippet: '# CI exports cache\ndocker buildx build --cache-to type=registry,ref=myapp:cache,mode=max --push .\n\n# Local imports cache\ndocker buildx build --cache-from type=registry,ref=myapp:cache -t myapp:dev .',
    tags: ['cache', 'ci', 'sharing'],
  },
];

export default questions;
