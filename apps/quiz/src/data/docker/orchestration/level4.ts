import { Question } from '@/types/question';

const questions: Question[] = [
  {
    id: 'docker-orchestration-l4-001',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How does Docker Swarm implement the Raft consensus protocol for log replication, and what are the performance implications of large state sizes?',
    options: [
      'Raft uses async replication with eventual consistency',
      'Raft replicates log entries to a quorum of managers synchronously; large state sizes increase snapshot time, memory usage, and leader election latency',
      'Raft uses a single leader that handles all reads and writes without replication',
      'Raft replicates the entire state on every write operation',
    ],
    correctAnswer: 1,
    explanation: 'Swarm\'s Raft implementation replicates log entries synchronously to a quorum. Large state (many services/tasks) increases snapshot size, recovery time, and memory footprint on managers.',
    tags: ['swarm', 'raft', 'internals'],
  },
  {
    id: 'docker-orchestration-l4-002',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a sophisticated deployment pipeline with automated canary analysis in Docker Swarm?',
    options: [
      'Use Docker Swarm built-in canary analysis',
      'Deploy canary service with separate metrics collection, compare error rates and latencies against baseline using Prometheus queries, and automate promotion/rollback',
      'Simply use docker service update with --canary flag',
      'Canary analysis requires Kubernetes with Flagger',
    ],
    correctAnswer: 1,
    explanation: 'Build a custom pipeline: deploy canary alongside stable, collect separate metrics via Prometheus labels, run statistical comparison scripts, and automate the decision to promote or rollback.',
    codeSnippet: '#!/bin/bash\n# Automated canary analysis\nCANARY_ERROR_RATE=$(curl -s "prometheus:9090/api/v1/query?query=\\\n  rate(http_errors{service=\'web-canary\'}[5m])" | jq \'.data.result[0].value[1]\')\nBASELINE_ERROR_RATE=$(curl -s "prometheus:9090/api/v1/query?query=\\\n  rate(http_errors{service=\'web-stable\'}[5m])" | jq \'.data.result[0].value[1]\')\n\nif (( $(echo "$CANARY_ERROR_RATE > $BASELINE_ERROR_RATE * 1.1" | bc -l) )); then\n  echo "Canary failed - rolling back"\n  docker service rm web-canary\nelse\n  echo "Canary passed - promoting"\n  docker service update --image myapp:v2 web-stable\n  docker service rm web-canary\nfi',
    tags: ['services', 'canary-analysis', 'automation'],
  },
  {
    id: 'docker-orchestration-l4-003',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement GitOps for Kubernetes deployments using Flux or ArgoCD?',
    options: [
      'By manually applying YAML files from a git repository',
      'A GitOps controller continuously reconciles the cluster state with the desired state defined in a Git repository, automatically applying changes on commits',
      'By using CI/CD pipelines to run kubectl apply on each commit',
      'GitOps is just a rebranding of traditional CI/CD',
    ],
    correctAnswer: 1,
    explanation: 'GitOps controllers (Flux, ArgoCD) watch Git repositories and automatically reconcile cluster state. Any drift from the Git-defined state is automatically corrected.',
    codeSnippet: '# ArgoCD Application\napiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: myapp\nspec:\n  project: default\n  source:\n    repoURL: https://github.com/org/k8s-manifests.git\n    targetRevision: main\n    path: production\n  destination:\n    server: https://kubernetes.default.svc\n    namespace: production\n  syncPolicy:\n    automated:\n      prune: true\n      selfHeal: true\n    syncOptions:\n    - CreateNamespace=true',
    tags: ['kubernetes', 'gitops', 'argocd'],
  },
  {
    id: 'docker-orchestration-l4-004',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'What are the internal data structures and communication patterns of the SwarmKit orchestrator?',
    options: [
      'A simple in-memory key-value store',
      'A store backed by Raft with separate allocator, scheduler, dispatcher, and orchestrator components communicating via the internal store',
      'A relational database with standard SQL queries',
      'A file-based configuration system',
    ],
    correctAnswer: 1,
    explanation: 'SwarmKit consists of the Raft store for state, allocator (IPs, ports), scheduler (task placement), dispatcher (worker communication), and orchestrator (desired state reconciliation).',
    tags: ['swarm', 'swarmkit', 'architecture'],
  },
  {
    id: 'docker-orchestration-l4-005',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement zero-downtime TLS certificate rotation for a service using Docker Swarm secrets?',
    options: [
      'Docker handles TLS rotation automatically for all services',
      'Create versioned secret pairs, update services with new secrets while maintaining old ones during transition, then remove old secrets after all tasks reload',
      'Replace the secrets in-place and restart all containers',
      'Use environment variables for certificates instead of secrets',
    ],
    correctAnswer: 1,
    explanation: 'Store cert/key as versioned secrets. During rotation, add new secrets alongside old ones, trigger rolling update so each task picks up new certs, then clean up old secrets.',
    codeSnippet: '# Create new cert secrets\ndocker secret create tls-cert-v2 new-cert.pem\ndocker secret create tls-key-v2 new-key.pem\n\n# Update service (rolling) with both old and new\ndocker service update \\\n  --secret-rm tls-cert-v1 \\\n  --secret-add source=tls-cert-v2,target=tls-cert \\\n  --secret-rm tls-key-v1 \\\n  --secret-add source=tls-key-v2,target=tls-key \\\n  --update-order start-first \\\n  web\n\n# Cleanup after all tasks updated\ndocker secret rm tls-cert-v1 tls-key-v1',
    tags: ['services', 'tls-rotation', 'zero-downtime'],
  },
  {
    id: 'docker-orchestration-l4-006',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement multi-cluster Kubernetes federation for global deployments?',
    options: [
      'Run a single large Kubernetes cluster across regions',
      'Use Kubernetes Federation (KubeFed) or tools like Admiralty/Liqo to synchronize resources across multiple clusters with location-aware routing',
      'Deploy the same manifests manually to each cluster',
      'Multi-cluster requires a proprietary cloud solution',
    ],
    correctAnswer: 1,
    explanation: 'Federation tools propagate resource definitions across clusters. Combined with global load balancers and DNS-based routing, they enable geo-distributed deployments with local failover.',
    codeSnippet: '# KubeFed FederatedDeployment\napiVersion: types.kubefed.io/v1beta1\nkind: FederatedDeployment\nmetadata:\n  name: web\n  namespace: production\nspec:\n  template:\n    spec:\n      replicas: 3\n      selector:\n        matchLabels:\n          app: web\n      template:\n        spec:\n          containers:\n          - name: web\n            image: myapp:v1\n  placement:\n    clusters:\n    - name: us-east\n    - name: eu-west\n  overrides:\n  - clusterName: us-east\n    clusterOverrides:\n    - path: "/spec/replicas"\n      value: 5',
    tags: ['kubernetes', 'federation', 'multi-cluster'],
  },
  {
    id: 'docker-orchestration-l4-007',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How does the Docker Swarm task state machine work internally?',
    options: [
      'Tasks only have running and stopped states',
      'Tasks transition through new, pending, assigned, accepted, preparing, ready, starting, running, complete/failed/shutdown states with specific triggers',
      'Task states are managed by the container runtime',
      'Tasks use a simple running/paused/stopped model',
    ],
    correctAnswer: 1,
    explanation: 'The task state machine tracks the full lifecycle: new -> pending (awaiting resources) -> assigned (to a node) -> accepted -> preparing (pulling image) -> ready -> starting -> running -> terminal state.',
    tags: ['swarm', 'task-states', 'internals'],
  },
  {
    id: 'docker-orchestration-l4-008',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you design a multi-tenancy architecture using Docker Swarm?',
    options: [
      'Docker Swarm has built-in multi-tenancy support',
      'Use separate overlay networks per tenant, namespace services with tenant prefixes, use constraints and labels for isolation, and implement RBAC at the application level',
      'Create a separate Swarm for each tenant',
      'Multi-tenancy requires Kubernetes namespaces',
    ],
    correctAnswer: 1,
    explanation: 'Implement tenant isolation through dedicated overlay networks, service naming conventions, node label-based constraints, resource limits per tenant, and application-level access control.',
    codeSnippet: '# Per-tenant network isolation\ndocker network create --driver overlay tenant-a-net\ndocker network create --driver overlay tenant-b-net\n\n# Per-tenant services with constraints\ndocker service create --name tenant-a-web \\\n  --network tenant-a-net \\\n  --constraint \'node.labels.tenant == a\' \\\n  --limit-cpu 2 --limit-memory 2G \\\n  myapp --config tenant-a\n\ndocker service create --name tenant-b-web \\\n  --network tenant-b-net \\\n  --constraint \'node.labels.tenant == b\' \\\n  --limit-cpu 2 --limit-memory 2G \\\n  myapp --config tenant-b',
    tags: ['services', 'multi-tenancy', 'isolation'],
  },
  {
    id: 'docker-orchestration-l4-009',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Health Checks',
    question: 'How do you implement distributed health checking with consensus across replicas?',
    options: [
      'Each container runs its own independent health check',
      'Implement a gossip-based health protocol where replicas share health state, and only report unhealthy when a quorum of peers agrees',
      'Use a centralized health monitoring service',
      'Docker Swarm handles distributed health consensus automatically',
    ],
    correctAnswer: 1,
    explanation: 'For complex distributed systems, implement application-level health consensus where replicas communicate to determine cluster health, preventing false positives from individual node issues.',
    codeSnippet: '// Distributed health check with peer validation\nasync function healthCheck() {\n  const localHealth = await checkLocalState();\n  const peerHealths = await Promise.allSettled(\n    peers.map(peer => fetch(`http://\${peer}/internal-health`))\n  );\n  const healthyPeers = peerHealths.filter(\n    r => r.status === \'fulfilled\' && r.value.ok\n  ).length;\n  const quorum = Math.ceil(peers.length / 2);\n  return localHealth && healthyPeers >= quorum;\n}',
    tags: ['health-checks', 'distributed', 'consensus'],
  },
  {
    id: 'docker-orchestration-l4-010',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How does the Kubernetes API server implement watch semantics and how does this affect controller performance?',
    options: [
      'Controllers poll the API server at regular intervals',
      'The API server uses HTTP long-polling with resourceVersion tracking, allowing controllers to receive streaming updates without full re-list',
      'Controllers use WebSocket connections for real-time updates',
      'The API server pushes updates to controllers via gRPC streams',
    ],
    correctAnswer: 1,
    explanation: 'The API server implements watches using HTTP streaming with resourceVersion. Controllers maintain a local cache via informers. If the watch is broken, they re-list from the last known resourceVersion.',
    codeSnippet: '# Watch API example\nGET /api/v1/namespaces/default/pods?watch=true&resourceVersion=12345\n\n# Returns streaming JSON events:\n# {"type":"ADDED","object":{...}}\n# {"type":"MODIFIED","object":{...}}\n# {"type":"DELETED","object":{...}}',
    tags: ['kubernetes', 'api-server', 'watch'],
  },
  {
    id: 'docker-orchestration-l4-011',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you optimize the Docker Swarm network data path for high-throughput workloads?',
    options: [
      'The Swarm network data path cannot be optimized',
      'Use host mode publishing to bypass routing mesh, tune MTU for VXLAN overhead, disable encryption when not needed, and use dedicated network interfaces',
      'Always use the routing mesh for maximum throughput',
      'Increase the number of overlay networks',
    ],
    correctAnswer: 1,
    explanation: 'For performance: use host-mode ports to bypass IPVS/routing mesh overhead, set MTU to account for VXLAN encapsulation (typically 1450), and avoid encrypted overlays unless required.',
    codeSnippet: '# Create overlay with optimized MTU\ndocker network create --driver overlay \\\n  --opt com.docker.network.driver.mtu=1450 \\\n  fast-net\n\n# Use host mode for high-throughput service\ndocker service create --name fast-api \\\n  --mode global \\\n  --publish mode=host,target=8080,published=8080 \\\n  --network fast-net \\\n  high-perf-app',
    tags: ['swarm', 'networking', 'performance'],
  },
  {
    id: 'docker-orchestration-l4-012',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a complete observability stack for Docker Swarm services?',
    options: [
      'Use docker stats and docker logs only',
      'Deploy metrics (Prometheus/Grafana), logging (EFK stack), and tracing (Jaeger) as Swarm services with service-level instrumentation',
      'Use cloud provider monitoring',
      'Monitoring requires Kubernetes-specific tools',
    ],
    correctAnswer: 1,
    explanation: 'A full observability stack includes: Prometheus + Grafana for metrics, Fluentd + Elasticsearch + Kibana for logs, Jaeger/Zipkin for distributed tracing, deployed as Swarm services.',
    codeSnippet: '# observability-stack.yml\nservices:\n  prometheus:\n    image: prom/prometheus\n    deploy:\n      placement:\n        constraints: [node.role == manager]\n    configs:\n      - source: prom-config\n        target: /etc/prometheus/prometheus.yml\n  grafana:\n    image: grafana/grafana\n  jaeger:\n    image: jaegertracing/all-in-one\n    environment:\n      COLLECTOR_OTLP_ENABLED: "true"\n  fluentd:\n    image: custom-fluentd\n    deploy:\n      mode: global\n    volumes:\n      - /var/lib/docker/containers:/fluentd/log:ro',
    tags: ['services', 'observability', 'monitoring'],
  },
  {
    id: 'docker-orchestration-l4-013',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a zero-trust security model in Kubernetes?',
    options: [
      'Enable RBAC and the job is done',
      'Combine service mesh mTLS, NetworkPolicies, Pod Security Standards, OPA/Gatekeeper for policy enforcement, and workload identity',
      'Use a VPN to connect all cluster nodes',
      'Rely on cloud provider firewalls',
    ],
    correctAnswer: 1,
    explanation: 'Zero-trust requires: mTLS between all services (Istio/Linkerd), strict NetworkPolicies, Pod Security Standards, admission controllers for policy (OPA/Kyverno), and identity-based access with SPIFFE.',
    codeSnippet: '# OPA Constraint: require resource limits\napiVersion: constraints.gatekeeper.sh/v1beta1\nkind: K8sRequiredResources\nmetadata:\n  name: require-limits\nspec:\n  match:\n    kinds:\n    - apiGroups: [""]\n      kinds: ["Pod"]\n    namespaces: ["production"]\n  parameters:\n    limits:\n    - cpu\n    - memory\n    requests:\n    - cpu\n    - memory',
    tags: ['kubernetes', 'zero-trust', 'security'],
  },
  {
    id: 'docker-orchestration-l4-014',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How does the Docker Swarm allocator work for IP address and port assignment?',
    options: [
      'IPs and ports are assigned randomly',
      'The allocator reserves IP addresses from network pools and published ports, tracking allocations in the Raft store for consistency',
      'Each node manages its own IP allocations',
      'The allocator uses DHCP for all assignments',
    ],
    correctAnswer: 1,
    explanation: 'The Swarm allocator is a centralized component on the leader that manages IP address pools for overlay networks and port allocations for published services, persisting all allocations in the Raft store.',
    tags: ['swarm', 'allocator', 'ip-management'],
  },
  {
    id: 'docker-orchestration-l4-015',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a robust service dependency graph with health-aware traffic routing in Docker Swarm?',
    options: [
      'Docker Swarm handles service dependencies automatically',
      'Deploy a reverse proxy (Traefik/Envoy) that health-checks backends, combined with application circuit breakers and retry policies for each dependency',
      'Use depends_on in Compose files',
      'Service dependencies require Kubernetes service mesh',
    ],
    correctAnswer: 1,
    explanation: 'Build a dependency graph with health-aware routing using reverse proxies that monitor backend health, application-level circuit breakers, retry policies with backoff, and fallback handlers.',
    codeSnippet: '# Envoy configuration for health-aware routing\nclusters:\n- name: api_cluster\n  health_checks:\n  - timeout: 5s\n    interval: 10s\n    unhealthy_threshold: 3\n    healthy_threshold: 2\n    http_health_check:\n      path: /health\n  outlier_detection:\n    consecutive_5xx: 3\n    interval: 10s\n    base_ejection_time: 30s\n    max_ejection_percent: 50\n  circuit_breakers:\n    thresholds:\n    - max_connections: 1000\n      max_pending_requests: 100\n      max_retries: 3',
    tags: ['services', 'dependency-graph', 'traffic-routing'],
  },
  {
    id: 'docker-orchestration-l4-016',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Rolling Updates',
    question: 'How do you implement feature-flag-driven progressive rollouts in Docker Swarm?',
    options: [
      'Feature flags are handled by the application, not orchestration',
      'Combine Docker configs for feature flags, percentage-based routing via reverse proxy, and metrics-driven automation to progressively enable features',
      'Use --feature-flag with docker service update',
      'Feature-driven rollouts require Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Deploy both versions behind a proxy. Use feature flag configs to control which percentage of traffic gets the new feature. Monitor metrics and automate promotion based on success criteria.',
    codeSnippet: '# Feature flag config\n{\n  "new_checkout_flow": {\n    "enabled": true,\n    "percentage": 10,\n    "allowed_users": ["beta-testers"],\n    "metrics_threshold": {\n      "error_rate": 0.01,\n      "p99_latency_ms": 500\n    }\n  }\n}\n\n# Progressive rollout script\nfor pct in 10 25 50 75 100; do\n  update_feature_flag "new_checkout_flow" $pct\n  sleep 300  # 5 min observation\n  if ! check_metrics; then\n    rollback_feature_flag "new_checkout_flow"\n    exit 1\n  fi\ndone',
    tags: ['rolling-updates', 'feature-flags', 'progressive'],
  },
  {
    id: 'docker-orchestration-l4-017',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes cluster hardening according to CIS benchmarks?',
    options: [
      'Enable RBAC and the cluster is secure',
      'Apply CIS controls: disable anonymous auth, enable audit logging, restrict etcd access, configure pod security standards, encrypt secrets at rest, and use network policies',
      'Use the default cluster configuration',
      'Security is handled entirely by the cloud provider',
    ],
    correctAnswer: 1,
    explanation: 'CIS benchmarks cover: API server configuration (auth, admission), etcd encryption and access control, kubelet security, network policies, Pod Security Standards, audit logging, and secret encryption.',
    codeSnippet: '# API server hardening flags\n--anonymous-auth=false\n--authorization-mode=Node,RBAC\n--enable-admission-plugins=NodeRestriction,PodSecurity\n--audit-log-path=/var/log/audit.log\n--audit-policy-file=/etc/kubernetes/audit-policy.yaml\n--encryption-provider-config=/etc/kubernetes/enc-config.yaml\n\n# Encryption config for secrets at rest\napiVersion: apiserver.config.k8s.io/v1\nkind: EncryptionConfiguration\nresources:\n- resources: ["secrets"]\n  providers:\n  - aescbc:\n      keys:\n      - name: key1\n        secret: <base64-encoded-key>\n  - identity: {}',
    tags: ['kubernetes', 'hardening', 'cis-benchmarks'],
  },
  {
    id: 'docker-orchestration-l4-018',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a multi-region Docker Swarm architecture?',
    options: [
      'Docker Swarm natively supports multi-region clusters',
      'Use separate Swarms per region with DNS-based failover, synchronized deployments, and a global load balancer for cross-region traffic',
      'Simply add nodes from different regions to one Swarm',
      'Multi-region requires Kubernetes federation',
    ],
    correctAnswer: 1,
    explanation: 'Multi-region Swarm requires separate clusters per region (Raft latency prevents cross-region Swarm), global DNS for traffic routing, and CI/CD that deploys to all regions.',
    codeSnippet: '# Deploy to multiple regions\n#!/bin/bash\nREGIONS=("us-east" "eu-west" "ap-south")\n\nfor region in "${REGIONS[@]}"; do\n  echo "Deploying to $region"\n  DOCKER_HOST="tcp://manager-${region}:2376" \\\n  DOCKER_TLS_VERIFY=1 \\\n  DOCKER_CERT_PATH="/certs/${region}" \\\n  docker stack deploy -c docker-compose.yml \\\n    -c "docker-compose.${region}.yml" myapp\ndone\n\n# Update Route53 health checks\naws route53 change-resource-record-sets ...',
    tags: ['swarm', 'multi-region', 'architecture'],
  },
  {
    id: 'docker-orchestration-l4-019',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a service mesh with traffic mirroring for testing in Docker Swarm?',
    options: [
      'Traffic mirroring is built into Docker Swarm',
      'Deploy Envoy or HAProxy as a sidecar service that mirrors production traffic to a shadow service for testing without affecting users',
      'Use docker service create --mirror flag',
      'Traffic mirroring requires Istio on Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Deploy a proxy that copies incoming requests to a shadow service asynchronously. The shadow service processes the request but responses are discarded, allowing testing with real traffic.',
    codeSnippet: '# Envoy config for traffic mirroring\nroute_config:\n  virtual_hosts:\n  - name: backend\n    routes:\n    - match:\n        prefix: "/"\n      route:\n        cluster: production\n      request_mirror_policies:\n      - cluster: shadow\n        runtime_fraction:\n          default_value:\n            numerator: 100\n            denominator: HUNDRED\n\nclusters:\n- name: production\n  load_assignment:\n    endpoints:\n    - lb_endpoints:\n      - endpoint: {address: {socket_address: {address: web, port_value: 8080}}}\n- name: shadow\n  load_assignment:\n    endpoints:\n    - lb_endpoints:\n      - endpoint: {address: {socket_address: {address: web-shadow, port_value: 8080}}}',
    tags: ['services', 'traffic-mirroring', 'testing'],
  },
  {
    id: 'docker-orchestration-l4-020',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How does etcd compaction and defragmentation work, and when should you perform maintenance?',
    options: [
      'etcd handles all maintenance automatically',
      'Compaction removes superseded key revisions; defragmentation reclaims disk space; both should be performed regularly to prevent performance degradation',
      'etcd data is automatically garbage collected',
      'Maintenance is only needed when etcd runs out of space',
    ],
    correctAnswer: 1,
    explanation: 'etcd keeps a history of all key revisions. Compaction removes old revisions to bound history. Defragmentation reclaims the freed disk space. Without maintenance, etcd can exceed its quota.',
    codeSnippet: '# Check etcd status\netcdctl endpoint status --write-out=table\n\n# Compact to revision\nREV=$(etcdctl endpoint status --write-out="json" | \\\n  jq \'.[0].Status.header.revision\')\netcdctl compact $REV\n\n# Defragment each member\netcdctl defrag --endpoints=https://etcd-0:2379\netcdctl defrag --endpoints=https://etcd-1:2379\netcdctl defrag --endpoints=https://etcd-2:2379\n\n# Set auto-compaction\n# etcd flag: --auto-compaction-retention=1h',
    tags: ['kubernetes', 'etcd', 'maintenance'],
  },
  {
    id: 'docker-orchestration-l4-021',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement custom scheduling strategies in Docker Swarm?',
    options: [
      'Docker Swarm supports pluggable scheduler backends',
      'Use placement constraints, preferences, and resource reservations together; for complex logic, build an external orchestration layer using the Docker API',
      'Modify the SwarmKit source code',
      'Custom scheduling is not possible in Docker Swarm',
    ],
    correctAnswer: 1,
    explanation: 'Swarm\'s scheduler is not pluggable, but complex placement can be achieved by combining constraints, preferences, labels, and resource reservations. For advanced needs, build an API-driven orchestration layer.',
    codeSnippet: '# Complex placement strategy\ndocker service create --name ml-job \\\n  --constraint \'node.labels.gpu == true\' \\\n  --constraint \'node.labels.gpu-mem >= 16\' \\\n  --placement-pref \'spread=node.labels.rack\' \\\n  --reserve-cpu 4 \\\n  --reserve-memory 32G \\\n  --generic-resource "NVIDIA-GPU=2" \\\n  ml-training:latest',
    tags: ['swarm', 'scheduling', 'custom'],
  },
  {
    id: 'docker-orchestration-l4-022',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement chaos engineering practices in a Docker Swarm environment?',
    options: [
      'Docker Swarm is immune to chaos engineering',
      'Use tools like Pumba or custom scripts to inject failures (kill containers, introduce latency, partition networks) and verify service resilience',
      'Simply stop random containers manually',
      'Chaos engineering requires Kubernetes with LitmusChaos',
    ],
    correctAnswer: 1,
    explanation: 'Deploy chaos tools as Swarm services. Inject failures like container kills, network delays, CPU stress. Verify health checks, rollbacks, and self-healing work correctly.',
    codeSnippet: '# Pumba - kill random containers every 5 minutes\ndocker run -d --name pumba \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  gaiaadm/pumba --random \\\n  kill --signal SIGKILL --interval 5m \\\n  "re2:.*web.*"\n\n# Network chaos - add latency\ndocker run -d --name pumba-netem \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  gaiaadm/pumba netem --duration 60s \\\n  delay --time 200 --jitter 50 \\\n  "re2:.*api.*"\n\n# Custom script - simulate node failure\n#!/bin/bash\nNODE=$(docker node ls -q | shuf -n 1)\ndocker node update --availability drain $NODE\nsleep 120\ndocker node update --availability active $NODE',
    tags: ['services', 'chaos-engineering', 'resilience'],
  },
  {
    id: 'docker-orchestration-l4-023',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a multi-tenant Kubernetes platform with resource isolation and cost allocation?',
    options: [
      'Use separate namespaces per tenant',
      'Combine namespaces with ResourceQuotas, LimitRanges, NetworkPolicies, RBAC, and cost monitoring tools for comprehensive tenant isolation',
      'Use separate clusters for each tenant',
      'Multi-tenancy is not supported in Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Multi-tenancy requires: namespaces for logical isolation, ResourceQuotas for fair sharing, LimitRanges for defaults, NetworkPolicies for network isolation, RBAC for access control, and tools like kubecost for cost allocation.',
    codeSnippet: '# Complete tenant setup\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: tenant-acme\n  labels:\n    pod-security.kubernetes.io/enforce: baseline\n---\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: tenant-quota\n  namespace: tenant-acme\nspec:\n  hard:\n    requests.cpu: "10"\n    requests.memory: 20Gi\n    limits.cpu: "20"\n    limits.memory: 40Gi\n    pods: "50"\n    services: "10"\n    persistentvolumeclaims: "20"\n---\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: deny-cross-tenant\n  namespace: tenant-acme\nspec:\n  podSelector: {}\n  policyTypes: ["Ingress","Egress"]\n  ingress:\n  - from:\n    - namespaceSelector:\n        matchLabels:\n          name: tenant-acme',
    tags: ['kubernetes', 'multi-tenancy', 'cost-allocation'],
  },
  {
    id: 'docker-orchestration-l4-024',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'What are the internal mechanisms of the Swarm dispatcher and how do they affect worker node communication?',
    options: [
      'Workers connect via HTTP REST API',
      'Workers maintain gRPC streams to managers for task assignment, health reporting, and state updates; the dispatcher manages these sessions with heartbeat tracking',
      'Workers use UDP for all communication',
      'Communication is handled by the overlay network',
    ],
    correctAnswer: 1,
    explanation: 'Workers maintain persistent gRPC connections to the leader manager. The dispatcher assigns tasks via these streams, monitors heartbeats, and handles session management. Lost connections trigger task rescheduling.',
    tags: ['swarm', 'dispatcher', 'internals'],
  },
  {
    id: 'docker-orchestration-l4-025',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a custom service discovery mechanism for non-HTTP protocols in Docker Swarm?',
    options: [
      'Use the built-in DNS for all protocols',
      'Implement a custom service registry (Consul/etcd) running as a Swarm service, with registration sidecars for each application service',
      'Non-HTTP service discovery is not possible',
      'Use DNSRR endpoint mode for everything',
    ],
    correctAnswer: 1,
    explanation: 'For non-HTTP protocols or advanced discovery needs, deploy a service registry (Consul, etcd) in Swarm. Application services register themselves, and clients query the registry for endpoints.',
    codeSnippet: '# Consul as service registry in Swarm\ndocker service create --name consul \\\n  --replicas 3 \\\n  --network discovery \\\n  -p 8500:8500 \\\n  consul agent -server -bootstrap-expect=3\n\n# Registration sidecar for gRPC service\ndocker service create --name grpc-app \\\n  --network discovery \\\n  --env SERVICE_NAME=grpc-app \\\n  --env CONSUL_ADDR=consul:8500 \\\n  myapp-with-consul-registration\n\n# Client discovery\n# curl consul:8500/v1/health/service/grpc-app?passing',
    tags: ['services', 'service-discovery', 'consul'],
  },
  {
    id: 'docker-orchestration-l4-026',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes Pod Security Standards (PSS) and migrate from PodSecurityPolicy?',
    options: [
      'PSS is automatically enabled and requires no configuration',
      'Label namespaces with enforcement levels (privileged/baseline/restricted) and gradually migrate workloads from PSP to PSS',
      'PSS and PSP are the same thing',
      'PSS is only available in managed Kubernetes services',
    ],
    correctAnswer: 1,
    explanation: 'PSS replaces PSP with namespace-level labels for enforcement. Migration involves auditing existing workloads, labeling namespaces in warn/audit mode first, then switching to enforce.',
    codeSnippet: '# Step 1: Audit existing workloads\nkubectl label --dry-run=server --overwrite ns production \\\n  pod-security.kubernetes.io/enforce=restricted\n\n# Step 2: Apply in warn/audit mode first\nkubectl label ns production \\\n  pod-security.kubernetes.io/warn=restricted \\\n  pod-security.kubernetes.io/audit=restricted\n\n# Step 3: Fix workloads that violate restricted policy\n# Add securityContext to all Pods:\nspec:\n  securityContext:\n    runAsNonRoot: true\n    seccompProfile:\n      type: RuntimeDefault\n  containers:\n  - securityContext:\n      allowPrivilegeEscalation: false\n      capabilities:\n        drop: ["ALL"]\n\n# Step 4: Enforce\nkubectl label ns production \\\n  pod-security.kubernetes.io/enforce=restricted',
    tags: ['kubernetes', 'pod-security', 'migration'],
  },
  {
    id: 'docker-orchestration-l4-027',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a high-performance ingress architecture for Docker Swarm?',
    options: [
      'Use the default routing mesh for all traffic',
      'Deploy global-mode reverse proxies with host-mode ports, bypassing the routing mesh, combined with external load balancers and SSL termination',
      'Add more manager nodes for better ingress performance',
      'Use multiple overlay networks',
    ],
    correctAnswer: 1,
    explanation: 'For high performance, bypass the routing mesh: deploy Nginx/HAProxy/Traefik as global services with host-mode ports. Use external LB for health checking and SSL termination.',
    codeSnippet: '# High-performance ingress\ndocker service create --name ingress \\\n  --mode global \\\n  --publish mode=host,target=80,published=80 \\\n  --publish mode=host,target=443,published=443 \\\n  --mount type=bind,src=/etc/ssl,dst=/etc/ssl,ro \\\n  --constraint \'node.labels.role == ingress\' \\\n  --network frontend \\\n  custom-nginx\n\n# External LB (AWS ALB/NLB) health checks ingress nodes\n# -> Nginx routes to backend services via overlay\n# -> Backend services on internal overlay only',
    tags: ['swarm', 'ingress', 'performance'],
  },
  {
    id: 'docker-orchestration-l4-028',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a blue-green deployment with database migration coordination in Docker Swarm?',
    options: [
      'Simply update the service image',
      'Run database migration as a one-shot service, deploy green stack in parallel, verify with health checks, switch traffic atomically, then remove blue stack',
      'Stop the service, migrate, restart',
      'Blue-green with database migration is not possible',
    ],
    correctAnswer: 1,
    explanation: 'The pattern involves: 1) backward-compatible DB migration 2) deploy green alongside blue 3) health verification 4) atomic traffic switch via proxy update 5) blue teardown 6) cleanup migration.',
    codeSnippet: '#!/bin/bash\nset -e\n\n# 1. Run backward-compatible migration\ndocker service create --name migrate --restart-condition none \\\n  --network backend myapp:v2 python manage.py migrate\n\n# Wait for migration to complete\nwhile docker service ps migrate --format "{{.CurrentState}}" | grep -q Running; do\n  sleep 5\ndone\n\n# 2. Deploy green\ndocker service create --name web-green --replicas 3 \\\n  --network backend --network frontend myapp:v2\n\n# 3. Wait for healthy\nuntil [ "$(docker service inspect web-green --format \'{{.UpdateStatus.State}}\')" != "updating" ]; do\n  sleep 5\ndone\n\n# 4. Switch traffic (update Traefik routing)\ndocker service update --label-add traefik.enable=true web-green\ndocker service update --label-add traefik.enable=false web-blue\n\n# 5. Remove blue\ndocker service rm web-blue\ndocker service update --name web web-green',
    tags: ['services', 'blue-green', 'database-migration'],
  },
  {
    id: 'docker-orchestration-l4-029',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Health Checks',
    question: 'How do you implement adaptive health checks that adjust behavior based on system load?',
    options: [
      'Health checks have fixed parameters that cannot change',
      'Build application-level health endpoints that consider system load, connection pool utilization, and queue depth when determining health status',
      'Use Docker daemon configuration for adaptive checks',
      'Let the orchestrator handle adaptive health checking',
    ],
    correctAnswer: 1,
    explanation: 'Implement smart health endpoints that consider current system state. Under high load, the endpoint might still report healthy but indicate reduced capacity, while catastrophic failures report unhealthy.',
    codeSnippet: '// Adaptive health check endpoint\nfunc healthHandler(w http.ResponseWriter, r *http.Request) {\n    cpuLoad := getCPULoad()\n    memUsage := getMemUsage()\n    dbConns := getDBPoolUtilization()\n    queueDepth := getMessageQueueDepth()\n\n    health := HealthStatus{\n        Status: "healthy",\n        Load:   cpuLoad,\n        Memory: memUsage,\n    }\n\n    // Only unhealthy for catastrophic failures\n    if dbConns > 0.95 || !dbPingSuccessful() {\n        health.Status = "unhealthy"\n        w.WriteHeader(503)\n    } else if cpuLoad > 0.9 || queueDepth > 10000 {\n        health.Status = "degraded"\n        w.WriteHeader(200) // Still healthy but signal pressure\n    }\n\n    json.NewEncoder(w).Encode(health)\n}',
    tags: ['health-checks', 'adaptive', 'load-aware'],
  },
  {
    id: 'docker-orchestration-l4-030',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How does the Kubernetes scheduler framework work for custom scheduler plugins?',
    options: [
      'You must replace the entire scheduler binary',
      'The scheduler framework provides extension points (PreFilter, Filter, Score, Reserve, Permit, PreBind, Bind, PostBind) for custom plugins',
      'Custom schedulers can only be run as separate processes',
      'The scheduler framework is only available in managed Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'The scheduler framework defines extension points in the scheduling cycle. Custom plugins implement these interfaces to add scoring, filtering, or binding logic without forking the scheduler.',
    codeSnippet: '// Custom scheduler plugin\ntype MyPlugin struct{}\n\nfunc (p *MyPlugin) Name() string { return "MyPlugin" }\n\nfunc (p *MyPlugin) Score(\n    ctx context.Context,\n    state *framework.CycleState,\n    pod *v1.Pod,\n    nodeName string,\n) (int64, *framework.Status) {\n    // Custom scoring logic\n    nodeInfo, _ := state.Read(framework.GetNodeInfoKey(nodeName))\n    gpuCount := getGPUCount(nodeInfo)\n    if needsGPU(pod) && gpuCount > 0 {\n        return 100, framework.NewStatus(framework.Success)\n    }\n    return 0, framework.NewStatus(framework.Success)\n}\n\n// KubeSchedulerConfiguration\napiVersion: kubescheduler.config.k8s.io/v1\nkind: KubeSchedulerConfiguration\nprofiles:\n- plugins:\n    score:\n      enabled:\n      - name: MyPlugin',
    tags: ['kubernetes', 'scheduler', 'plugins'],
  },
  {
    id: 'docker-orchestration-l4-031',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a complete CI/CD pipeline that deploys to Docker Swarm with rollback capabilities?',
    options: [
      'Use docker service update in a shell script',
      'Build immutable images with SHA tags, deploy via docker stack deploy with health verification, automated smoke tests, and scripted rollback to previous version on failure',
      'Push to latest tag and restart services',
      'CI/CD for Swarm requires third-party orchestration tools only',
    ],
    correctAnswer: 1,
    explanation: 'A robust pipeline includes: build with commit SHA tags, push to registry, deploy stack with new tags, health verification, integration tests, and automated rollback on failure.',
    codeSnippet: '# GitLab CI pipeline for Swarm deployment\ndeploy:\n  script:\n    - export TAG=$CI_COMMIT_SHORT_SHA\n    - docker build -t $REGISTRY/myapp:$TAG .\n    - docker push $REGISTRY/myapp:$TAG\n    # Save current version for rollback\n    - PREV=$(docker service inspect web --format \'{{.Spec.TaskTemplate.ContainerSpec.Image}}\')\n    - echo $PREV > /tmp/prev_version\n    # Deploy\n    - TAG=$TAG docker stack deploy -c docker-compose.yml myapp\n    # Health check\n    - for i in $(seq 1 30); do\n        curl -sf http://app/health && break;\n        [ $i -eq 30 ] && { echo "Deploy failed"; docker service update --image $PREV web; exit 1; };\n        sleep 10;\n      done\n    # Run smoke tests\n    - ./run_smoke_tests.sh || { docker service update --image $PREV web; exit 1; }',
    tags: ['swarm', 'cicd', 'rollback'],
  },
  {
    id: 'docker-orchestration-l4-032',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement distributed tracing across Docker Swarm services?',
    options: [
      'Docker Swarm automatically traces requests across services',
      'Instrument applications with OpenTelemetry SDKs, propagate trace context headers between services, and collect traces in a backend like Jaeger deployed as a Swarm service',
      'Use docker service logs for tracing',
      'Tracing requires a service mesh',
    ],
    correctAnswer: 1,
    explanation: 'Instrument each service with OpenTelemetry. Propagate W3C Trace Context headers across service boundaries. Deploy Jaeger/Tempo as a Swarm service to collect and visualize traces.',
    codeSnippet: '# OpenTelemetry instrumentation (Python)\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.exporter.jaeger.thrift import JaegerExporter\nfrom opentelemetry.instrumentation.flask import FlaskInstrumentor\nfrom opentelemetry.instrumentation.requests import RequestsInstrumentor\n\ntrace.set_tracer_provider(TracerProvider())\njaeger_exporter = JaegerExporter(\n    agent_host_name="jaeger",\n    agent_port=6831,\n)\ntrace.get_tracer_provider().add_span_processor(\n    BatchSpanProcessor(jaeger_exporter)\n)\n\n# Auto-instrument Flask and outgoing requests\nFlaskInstrumentor().instrument_app(app)\nRequestsInstrumentor().instrument()',
    tags: ['services', 'tracing', 'opentelemetry'],
  },
  {
    id: 'docker-orchestration-l4-033',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a Kubernetes operator for a stateful application like a database?',
    options: [
      'Use a StatefulSet directly',
      'Create a CRD for the database, implement a controller that manages the full lifecycle including provisioning, scaling, backups, and failover',
      'Use Helm charts for stateful applications',
      'Operators are only for simple applications',
    ],
    correctAnswer: 1,
    explanation: 'A database operator encodes DBA knowledge: it watches a CRD (e.g., PostgresCluster), manages StatefulSets, handles replication, automated backups, failover, and version upgrades.',
    codeSnippet: '// Operator reconciliation logic (pseudo-code)\nfunc (r *PostgresReconciler) Reconcile(ctx context.Context,\n    req ctrl.Request) (ctrl.Result, error) {\n\n    pg := &v1.PostgresCluster{}\n    r.Get(ctx, req.NamespacedName, pg)\n\n    // Ensure StatefulSet exists with right replicas\n    sts := buildStatefulSet(pg)\n    r.CreateOrUpdate(ctx, sts)\n\n    // Configure replication\n    if pg.Spec.Replicas > 1 {\n        r.configureStreaming(ctx, pg)\n    }\n\n    // Handle backup schedule\n    if pg.Spec.Backup.Schedule != "" {\n        r.ensureCronJob(ctx, pg)\n    }\n\n    // Monitor and handle failover\n    if !r.isPrimaryHealthy(ctx, pg) {\n        r.promoteReplica(ctx, pg)\n    }\n\n    return ctrl.Result{RequeueAfter: 30 * time.Second}, nil\n}',
    tags: ['kubernetes', 'operators', 'stateful'],
  },
  {
    id: 'docker-orchestration-l4-034',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a secure supply chain for Docker Swarm deployments?',
    options: [
      'Pull images from Docker Hub directly',
      'Use Docker Content Trust for image signing, private registries with vulnerability scanning, image policies enforced at deployment, and SBOM generation',
      'Only use official images',
      'Security scanning is sufficient',
    ],
    correctAnswer: 1,
    explanation: 'A secure supply chain includes: DCT/Notary for image signing, private registry with vulnerability scanning (Trivy/Clair), deployment policies that only allow signed/scanned images, and SBOM tracking.',
    codeSnippet: '# Enable Docker Content Trust\nexport DOCKER_CONTENT_TRUST=1\n\n# Sign and push image\ndocker trust sign myregistry.io/myapp:v1.2.3\n\n# Scan image for vulnerabilities\ntrivy image --severity HIGH,CRITICAL myregistry.io/myapp:v1.2.3\n\n# Generate SBOM\nsynft myregistry.io/myapp:v1.2.3 -o spdx-json > sbom.json\n\n# Deploy only if signed and scanned\n#!/bin/bash\nIMAGE="myregistry.io/myapp:v1.2.3"\nif docker trust inspect $IMAGE > /dev/null 2>&1 && \\\n   trivy image --exit-code 1 --severity CRITICAL $IMAGE; then\n  docker service update --image $IMAGE web\nelse\n  echo "Image failed security checks"\n  exit 1\nfi',
    tags: ['swarm', 'supply-chain', 'security'],
  },
  {
    id: 'docker-orchestration-l4-035',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement request-level load balancing with circuit breakers in Docker Swarm?',
    options: [
      'The Swarm routing mesh provides circuit breakers',
      'Deploy Envoy or Linkerd as a per-service proxy with circuit breaker, retry, and timeout configuration for fine-grained traffic control',
      'Use --circuit-breaker flag on services',
      'Circuit breakers can only be implemented in application code',
    ],
    correctAnswer: 1,
    explanation: 'Deploy L7 proxies (Envoy/Linkerd) alongside each service. Configure per-route circuit breakers, retry budgets, timeouts, and outlier detection for resilient service communication.',
    codeSnippet: '# Envoy sidecar with circuit breakers\nclusters:\n- name: api_backend\n  connect_timeout: 5s\n  circuit_breakers:\n    thresholds:\n    - priority: DEFAULT\n      max_connections: 1024\n      max_pending_requests: 1024\n      max_requests: 1024\n      max_retries: 3\n      retry_budget:\n        budget_percent:\n          value: 20.0\n        min_retry_concurrency: 3\n  outlier_detection:\n    consecutive_5xx: 5\n    interval: 10s\n    base_ejection_time: 30s\n    max_ejection_percent: 50\n    success_rate_minimum_hosts: 3\n    success_rate_request_volume: 100\n    success_rate_stdev_factor: 1900',
    tags: ['services', 'circuit-breakers', 'envoy'],
  },
  {
    id: 'docker-orchestration-l4-036',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement cost-effective Kubernetes autoscaling with mixed instance types?',
    options: [
      'Use a single instance type for all workloads',
      'Combine HPA for Pod scaling, Cluster Autoscaler with mixed instance node pools, spot/preemptible instances for fault-tolerant workloads, and Karpenter for just-in-time provisioning',
      'Manually add and remove nodes based on cost reports',
      'Cost optimization only works with managed Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Use HPA for Pod-level scaling, Cluster Autoscaler or Karpenter for node-level. Mix on-demand for critical and spot instances for batch workloads. Use PDBs to ensure resilience.',
    codeSnippet: '# Karpenter provisioner for cost-effective scaling\napiVersion: karpenter.sh/v1alpha5\nkind: Provisioner\nmetadata:\n  name: default\nspec:\n  requirements:\n  - key: karpenter.sh/capacity-type\n    operator: In\n    values: ["spot", "on-demand"]\n  - key: node.kubernetes.io/instance-type\n    operator: In\n    values: ["m5.large", "m5.xlarge", "c5.large", "c5.xlarge"]\n  limits:\n    resources:\n      cpu: "100"\n      memory: 400Gi\n  consolidation:\n    enabled: true\n  ttlSecondsAfterEmpty: 30\n---\n# Spot-tolerant workload\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: batch-worker\nspec:\n  replicas: 10\n  template:\n    spec:\n      nodeSelector:\n        karpenter.sh/capacity-type: spot\n      tolerations:\n      - key: "karpenter.sh/capacity-type"\n        value: "spot"',
    tags: ['kubernetes', 'autoscaling', 'cost-optimization'],
  },
  {
    id: 'docker-orchestration-l4-037',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you troubleshoot and resolve Swarm networking issues at the kernel level?',
    options: [
      'Use docker network inspect only',
      'Inspect network namespaces, iptables rules, IPVS tables, VXLAN interfaces, and ARP/FDB entries on the host',
      'Restart the Docker daemon',
      'Recreate all overlay networks',
    ],
    correctAnswer: 1,
    explanation: 'Deep debugging involves inspecting network namespaces (ip netns), iptables NAT/filter rules, IPVS backends (ipvsadm), VXLAN tunnel state (bridge fdb), and ARP tables within container network namespaces.',
    codeSnippet: '# List network namespaces\nls /var/run/docker/netns/\n\n# Inspect ingress network namespace\nnsenter --net=/var/run/docker/netns/ingress_sbox\niptables -t nat -L -n\nipvsadm -Ln\n\n# Check VXLAN FDB entries\nbridge fdb show dev vxlan0\n\n# Inspect overlay network\nnsenter --net=/var/run/docker/netns/1-abc123\nip addr show\nip route show\narp -a\n\n# Trace packet flow\ntcpdump -i any -n port 4789\n\n# Check iptables DNAT rules\niptables -t nat -L DOCKER-INGRESS -n -v',
    tags: ['swarm', 'networking', 'kernel-debugging'],
  },
  {
    id: 'docker-orchestration-l4-038',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a sophisticated rolling migration from Docker Swarm to Kubernetes?',
    options: [
      'Run both simultaneously from day one',
      'Run services in parallel on both platforms, gradually shift traffic using DNS weights or a global LB, validate each migrated service, then decommission Swarm',
      'Stop Swarm and start Kubernetes on the same hosts',
      'Migration is not possible; you must start fresh',
    ],
    correctAnswer: 1,
    explanation: 'Migration strategy: convert Compose files to K8s manifests (using Kompose as starting point), deploy on K8s alongside Swarm, shift traffic gradually via DNS/LB, validate, then decommission.',
    codeSnippet: '# Phase 1: Convert and deploy to K8s\nkompose convert -f docker-compose.yml\nkubectl apply -f .\n\n# Phase 2: DNS-based traffic shifting\n# Route53 weighted routing\naws route53 change-resource-record-sets --hosted-zone-id Z123 \\\n  --change-batch \'{\n  "Changes": [{\n    "Action": "UPSERT",\n    "ResourceRecordSet": {\n      "Name": "api.example.com",\n      "Type": "A",\n      "SetIdentifier": "swarm",\n      "Weight": 50,\n      "ResourceRecords": [{"Value": "10.0.1.1"}]\n    }\n  },{\n    "Action": "UPSERT",\n    "ResourceRecordSet": {\n      "Name": "api.example.com",\n      "Type": "A",\n      "SetIdentifier": "k8s",\n      "Weight": 50,\n      "ResourceRecords": [{"Value": "10.0.2.1"}]\n    }\n  }]\n}\'',
    tags: ['services', 'migration', 'swarm-to-k8s'],
  },
  {
    id: 'docker-orchestration-l4-039',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a Kubernetes-native CI/CD system using Tekton Pipelines?',
    options: [
      'Deploy Jenkins on Kubernetes',
      'Define Tasks and Pipelines as Kubernetes custom resources that run build, test, and deploy steps as Pods within the cluster',
      'Use kubectl scripts in a CI system',
      'Tekton is only for managing pipelines UI',
    ],
    correctAnswer: 1,
    explanation: 'Tekton provides Kubernetes-native CI/CD with Tasks, Pipelines, PipelineRuns as CRDs. Each step runs as a container in a Pod, leveraging Kubernetes scheduling and resource management.',
    codeSnippet: 'apiVersion: tekton.dev/v1\nkind: Pipeline\nmetadata:\n  name: build-deploy\nspec:\n  params:\n  - name: git-url\n  - name: image-name\n  workspaces:\n  - name: source\n  tasks:\n  - name: clone\n    taskRef:\n      name: git-clone\n    params:\n    - name: url\n      value: $(params.git-url)\n    workspaces:\n    - name: output\n      workspace: source\n  - name: build\n    taskRef:\n      name: kaniko\n    runAfter: ["clone"]\n    params:\n    - name: IMAGE\n      value: $(params.image-name)\n    workspaces:\n    - name: source\n      workspace: source\n  - name: deploy\n    taskRef:\n      name: kubectl-deploy\n    runAfter: ["build"]\n    params:\n    - name: image\n      value: $(params.image-name)',
    tags: ['kubernetes', 'tekton', 'cicd'],
  },
  {
    id: 'docker-orchestration-l4-040',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement resource overcommitment strategies in Docker Swarm for maximizing cluster utilization?',
    options: [
      'Docker Swarm does not support overcommitment',
      'Set resource reservations lower than limits, allowing more tasks to be scheduled; monitor actual usage and adjust ratios based on workload patterns',
      'Always set reservations equal to limits',
      'Overcommitment requires custom scheduler plugins',
    ],
    correctAnswer: 1,
    explanation: 'Overcommitment means setting reservations (guaranteed resources) lower than limits (maximum resources). The scheduler places tasks based on reservations, allowing more tasks per node than limits would permit.',
    codeSnippet: '# Overcommitment strategy: reserve 25% of limit\ndocker service create --name api \\\n  --reserve-cpu 0.25 --limit-cpu 1.0 \\\n  --reserve-memory 256M --limit-memory 1G \\\n  --replicas 20 \\\n  myapp\n\n# Monitor actual usage vs reservations\ndocker stats --no-stream --format \\\n  "table {{.Name}}\\t{{.CPUPerc}}\\t{{.MemUsage}}"\n\n# Calculate overcommitment ratio\n# Total reserved CPU: 20 * 0.25 = 5 cores\n# Total limit CPU: 20 * 1.0 = 20 cores\n# Actual cluster capacity: 8 cores\n# Overcommit ratio: 20/8 = 2.5x',
    tags: ['swarm', 'overcommitment', 'resources'],
  },
  {
    id: 'docker-orchestration-l4-041',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement sophisticated log routing with multi-destination output in Docker Swarm?',
    options: [
      'Docker services can only log to one destination',
      'Use Fluentd/Fluent Bit as a global log collector with multiple output plugins routing logs by service labels, severity, and content patterns',
      'Configure multiple --log-driver flags per service',
      'Use syslog driver for all destinations',
    ],
    correctAnswer: 1,
    explanation: 'Deploy Fluentd/Fluent Bit as a global service. Configure routing rules to send logs to different destinations (Elasticsearch for search, S3 for archive, PagerDuty for alerts) based on metadata.',
    codeSnippet: '# Fluent Bit configuration for multi-destination\n[INPUT]\n    Name forward\n    Listen 0.0.0.0\n    Port 24224\n\n[FILTER]\n    Name record_modifier\n    Match *\n    Record hostname ${HOSTNAME}\n\n[OUTPUT]\n    Name es\n    Match *\n    Host elasticsearch\n    Index docker-logs\n    Type _doc\n\n[OUTPUT]\n    Name s3\n    Match *\n    bucket my-log-archive\n    region us-east-1\n    total_file_size 50M\n    upload_timeout 10m\n\n[OUTPUT]\n    Name slack\n    Match *.error\n    webhook https://hooks.slack.com/...',
    tags: ['services', 'logging', 'multi-destination'],
  },
  {
    id: 'docker-orchestration-l4-042',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes cluster disaster recovery with cross-region backup and restore?',
    options: [
      'etcd replication handles disaster recovery automatically',
      'Use Velero for cluster state backup, etcd snapshots for control plane recovery, and replicated storage for persistent data with documented RTO/RPO targets',
      'Simply recreate the cluster from manifests',
      'DR requires multi-cluster federation only',
    ],
    correctAnswer: 1,
    explanation: 'DR strategy includes: Velero for backing up namespaced resources to S3, etcd snapshots for control plane, replicated storage backends, and regular DR drills with documented RTO/RPO.',
    codeSnippet: '# Velero backup schedule\nvelero schedule create daily-backup \\\n  --schedule="0 2 * * *" \\\n  --include-namespaces production,staging \\\n  --storage-location s3-backup \\\n  --ttl 720h\n\n# etcd snapshot\nETCDCTL_API=3 etcdctl snapshot save /backup/etcd-$(date +%Y%m%d).db \\\n  --endpoints=https://127.0.0.1:2379 \\\n  --cacert=/etc/kubernetes/pki/etcd/ca.crt \\\n  --cert=/etc/kubernetes/pki/etcd/server.crt \\\n  --key=/etc/kubernetes/pki/etcd/server.key\n\n# Restore in DR region\nvelero restore create --from-backup daily-backup-20240115\n\n# Verify restoration\nkubectl get pods -A\nkubectl get pv\nkubectl get svc',
    tags: ['kubernetes', 'disaster-recovery', 'velero'],
  },
  {
    id: 'docker-orchestration-l4-043',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you optimize Docker Swarm for running latency-sensitive workloads?',
    options: [
      'Swarm is not suitable for latency-sensitive workloads',
      'Use host-mode networking, CPU pinning via cpuset constraints, NUMA-aware scheduling, and disable overlay encryption for minimum overhead',
      'Add more replicas to reduce latency',
      'Use the fastest available network driver',
    ],
    correctAnswer: 1,
    explanation: 'For latency: bypass routing mesh with host-mode ports, use cpuset to pin tasks to specific CPUs, consider NUMA topology, minimize GC pauses, and avoid encrypted overlays.',
    codeSnippet: '# Low-latency service configuration\ndocker service create --name fast-api \\\n  --mode global \\\n  --publish mode=host,target=8080,published=8080 \\\n  --limit-cpu 4 \\\n  --reserve-cpu 4 \\\n  --limit-memory 8G \\\n  --reserve-memory 8G \\\n  --constraint \'node.labels.latency == low\' \\\n  --env GOMAXPROCS=4 \\\n  --env MALLOC_ARENA_MAX=2 \\\n  --ulimit nofile=65536:65536 \\\n  --network host \\\n  fast-api:optimized',
    tags: ['swarm', 'latency', 'performance'],
  },
  {
    id: 'docker-orchestration-l4-044',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Rolling Updates',
    question: 'How do you implement a comprehensive deployment verification pipeline with automated rollback?',
    options: [
      'Use health checks alone for verification',
      'Chain pre-deploy validation, progressive rollout, synthetic monitoring, real-user metrics analysis, and automated rollback triggers in a deployment controller',
      'Run manual smoke tests after each deploy',
      'Verification requires external deployment tools only',
    ],
    correctAnswer: 1,
    explanation: 'A complete pipeline: pre-deploy checks (image scan, config validation), progressive rollout (1% -> 10% -> 50% -> 100%), synthetic tests, real-user metric comparison, automated rollback on anomaly.',
    codeSnippet: '#!/bin/bash\n# Deployment controller\nset -e\n\n# Pre-deploy validation\ntrivy image --exit-code 1 --severity CRITICAL $IMAGE\n./validate-config.sh $IMAGE\n\n# Progressive rollout\nfor stage in "canary:1" "partial:25" "majority:50" "full:100"; do\n  STAGE_NAME=$(echo $stage | cut -d: -f1)\n  PCT=$(echo $stage | cut -d: -f2)\n\n  echo "Stage: $STAGE_NAME ($PCT%%)"\n  deploy_percentage $IMAGE $PCT\n\n  # Wait and observe\n  sleep 300\n\n  # Check metrics\n  ERROR_RATE=$(query_prometheus "rate(http_5xx[5m])")\n  P99_LATENCY=$(query_prometheus "histogram_quantile(0.99, rate(http_duration_bucket[5m]))")\n\n  if (( $(echo "$ERROR_RATE > 0.01" | bc -l) )); then\n    echo "Error rate too high at $STAGE_NAME - rolling back"\n    rollback_to_previous\n    alert_team "Rollback triggered at $STAGE_NAME"\n    exit 1\n  fi\ndone\n\necho "Deployment successful"',
    tags: ['rolling-updates', 'verification', 'automation'],
  },
  {
    id: 'docker-orchestration-l4-045',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How does Kubernetes handle Pod scheduling at scale (10,000+ nodes) and what are the bottlenecks?',
    options: [
      'Kubernetes scales linearly with no bottlenecks',
      'At scale, etcd write throughput, API server caching, scheduler throughput, and watch notification volume become bottlenecks requiring tuning and sharding',
      'Each node runs its own scheduler at scale',
      'Kubernetes supports up to 1,000 nodes only',
    ],
    correctAnswer: 1,
    explanation: 'At scale: etcd needs to be on fast SSDs with tuned snapshot intervals, API server needs caching layer, scheduler needs parallelism tuning, and watch notifications need pagination/partitioning.',
    codeSnippet: '# Scheduler performance tuning\napiVersion: kubescheduler.config.k8s.io/v1\nkind: KubeSchedulerConfiguration\npercentageOfNodesToScore: 50  # Score only 50% of nodes\nparallelism: 16  # Parallel scheduling goroutines\n\n# API server tuning\n--max-requests-inflight=800\n--max-mutating-requests-inflight=400\n--watch-cache-sizes=pods#5000,nodes#1000\n\n# etcd performance\n--quota-backend-bytes=8589934592  # 8GB\n--snapshot-count=50000\n# Place etcd on NVMe SSDs',
    tags: ['kubernetes', 'scale', 'performance'],
  },
  {
    id: 'docker-orchestration-l4-046',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a service-level SLA monitoring and enforcement system in Docker Swarm?',
    options: [
      'Docker Swarm has built-in SLA monitoring',
      'Deploy SLI collectors (Prometheus) that track request latency, error rate, and throughput; define SLOs and error budgets; alert on SLO burn rates',
      'Use docker service logs to calculate SLA',
      'SLA monitoring requires specialized cloud tools only',
    ],
    correctAnswer: 1,
    explanation: 'Implement SRE practices: define SLIs (latency, availability), set SLOs (99.9% availability), calculate error budgets, and use multi-window burn-rate alerting.',
    codeSnippet: '# Prometheus recording rules for SLI tracking\ngroups:\n- name: sli_rules\n  rules:\n  - record: sli:request_availability:ratio\n    expr: |\n      1 - (\n        sum(rate(http_requests_total{code=~"5.."}[5m]))\n        /\n        sum(rate(http_requests_total[5m]))\n      )\n  - record: sli:request_latency:p99\n    expr: |\n      histogram_quantile(0.99,\n        sum(rate(http_request_duration_seconds_bucket[5m])) by (le)\n      )\n\n# Alert on error budget burn rate\n- alert: HighErrorBudgetBurn\n  expr: |\n    sli:request_availability:ratio < 0.999\n    and\n    sli:request_availability:ratio < 0.997  # 3x burn rate\n  for: 5m\n  labels:\n    severity: critical\n  annotations:\n    summary: "Error budget burning at 3x rate"',
    tags: ['swarm', 'sla', 'sre'],
  },
  {
    id: 'docker-orchestration-l4-047',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a data pipeline orchestration system using Docker Swarm?',
    options: [
      'Docker Swarm is not suitable for data pipelines',
      'Deploy workflow engines (Airflow, Prefect) as Swarm services that spawn task containers via the Docker API, with shared storage for intermediate results',
      'Use Kubernetes CronJobs instead',
      'Run everything as one large service',
    ],
    correctAnswer: 1,
    explanation: 'Deploy Apache Airflow or Prefect as Swarm services. They orchestrate data tasks by creating/monitoring Docker containers via the Docker API. Use shared NFS volumes for data exchange.',
    codeSnippet: '# Airflow with Docker executor on Swarm\nservices:\n  airflow-webserver:\n    image: apache/airflow:2.7\n    command: webserver\n    deploy:\n      replicas: 1\n  airflow-scheduler:\n    image: apache/airflow:2.7\n    command: scheduler\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock\n    environment:\n      AIRFLOW__CORE__EXECUTOR: DockerExecutor\n      AIRFLOW__DOCKER__DOCKER_URL: unix:///var/run/docker.sock\n      AIRFLOW__DOCKER__AUTO_REMOVE: "true"\n    deploy:\n      placement:\n        constraints: [node.role == manager]',
    tags: ['services', 'data-pipeline', 'airflow'],
  },
  {
    id: 'docker-orchestration-l4-048',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement eBPF-based observability and security in Kubernetes?',
    options: [
      'eBPF requires kernel modifications',
      'Deploy Cilium for eBPF-based networking and security, Pixie for auto-instrumented observability, and Falco with eBPF probes for runtime security',
      'eBPF is only for network monitoring',
      'eBPF tools are not compatible with Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'eBPF enables deep kernel-level visibility without instrumentation: Cilium for network policies and observability, Pixie for automatic protocol tracing, Falco for runtime security monitoring.',
    codeSnippet: '# Cilium Network Policy with L7 visibility\napiVersion: cilium.io/v2\nkind: CiliumNetworkPolicy\nmetadata:\n  name: api-policy\nspec:\n  endpointSelector:\n    matchLabels:\n      app: api\n  ingress:\n  - fromEndpoints:\n    - matchLabels:\n        app: frontend\n    toPorts:\n    - ports:\n      - port: "8080"\n        protocol: TCP\n      rules:\n        http:\n        - method: "GET"\n          path: "/api/v1/.*"\n\n# Hubble (Cilium) for network observability\n# hubble observe --pod api --protocol http\n# Shows: source, destination, L7 protocol, latency',
    tags: ['kubernetes', 'ebpf', 'observability'],
  },
  {
    id: 'docker-orchestration-l4-049',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement deterministic task placement in Docker Swarm for compliance requirements?',
    options: [
      'Docker Swarm always places tasks deterministically',
      'Combine strict placement constraints, node labels for compliance zones, encrypted networks, and audit logging to ensure data residency and regulatory compliance',
      'Use random placement and hope for compliance',
      'Compliance requires Kubernetes-only features',
    ],
    correctAnswer: 1,
    explanation: 'For compliance: label nodes by jurisdiction/classification, use constraints to enforce data residency, encrypt overlay networks for data in transit, and maintain audit trails.',
    codeSnippet: '# Compliance-aware deployment\n# Label nodes by jurisdiction\ndocker node update --label-add jurisdiction=eu worker-eu-1\ndocker node update --label-add jurisdiction=eu worker-eu-2\ndocker node update --label-add pci-compliant=true worker-pci-1\n\n# Deploy with compliance constraints\ndocker service create --name eu-data-processor \\\n  --constraint \'node.labels.jurisdiction == eu\' \\\n  --constraint \'node.labels.pci-compliant == true\' \\\n  --network encrypted-overlay \\\n  --secret db-credentials \\\n  --read-only \\\n  --no-healthcheck \\\n  --log-driver syslog \\\n  --log-opt syslog-address=tcp://audit-log:514 \\\n  --log-opt tag="{{.Name}}/{{.ID}}" \\\n  eu-compliant-app',
    tags: ['swarm', 'compliance', 'data-residency'],
  },
  {
    id: 'docker-orchestration-l4-050',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a self-healing architecture with automated incident response in Docker Swarm?',
    options: [
      'Docker Swarm self-healing only restarts failed containers',
      'Combine Swarm self-healing with external monitoring that detects anomalies, triggers automated remediation (scaling, restarts, failovers), and escalates to humans when automation fails',
      'Use health checks alone',
      'Self-healing requires AI-powered tools',
    ],
    correctAnswer: 1,
    explanation: 'Layer automated responses: Swarm restarts failed tasks (L1), monitoring triggers scaling/remediation scripts (L2), complex issues escalate to on-call with runbooks (L3).',
    codeSnippet: '# Automated incident response system\n# Level 1: Swarm auto-restart (built-in)\n# Level 2: Metric-based automation\n\n# Prometheus Alertmanager -> webhook handler\nalertmanager:\n  route:\n    receiver: auto-remediation\n  receivers:\n  - name: auto-remediation\n    webhook_configs:\n    - url: http://remediation-service:8080/alert\n\n# Remediation service\n@app.post("/alert")\ndef handle_alert(alert):\n    if alert.name == "HighMemoryUsage":\n        service = alert.labels["service"]\n        # Scale up\n        docker_client.services.get(service).scale(\n            current_replicas + 2\n        )\n    elif alert.name == "ServiceDown":\n        # Restart service\n        docker_client.services.get(service).force_update()\n    elif alert.name == "DiskPressure":\n        # Drain affected node\n        node_id = alert.labels["node"]\n        docker_client.nodes.get(node_id).update(\n            availability="drain"\n        )\n        # Page on-call\n        pagerduty.trigger(alert)',
    tags: ['services', 'self-healing', 'incident-response'],
  },
  {
    id: 'docker-orchestration-l4-051',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a platform engineering approach with internal developer platforms on Kubernetes?',
    options: [
      'Give developers direct kubectl access',
      'Build abstractions using Backstage for catalog, Crossplane for infrastructure, and custom CRDs that hide Kubernetes complexity behind golden paths',
      'Use managed Kubernetes dashboards',
      'Internal platforms require proprietary tools',
    ],
    correctAnswer: 1,
    explanation: 'Platform engineering abstracts Kubernetes complexity: Backstage provides a developer portal, Crossplane manages infrastructure as K8s resources, and custom CRDs define simplified application specs.',
    codeSnippet: '# Custom Application CRD (simplified interface)\napiVersion: platform.company.com/v1\nkind: Application\nmetadata:\n  name: user-service\nspec:\n  team: backend\n  language: go\n  replicas: 3\n  port: 8080\n  resources:\n    size: medium  # Maps to predefined CPU/memory\n  database:\n    type: postgres\n    size: small\n  monitoring:\n    enabled: true\n    alerting:\n      errorRateThreshold: 1%\n      latencyP99Threshold: 500ms\n  ingress:\n    host: user-service.internal.company.com\n    tls: true\n\n# Platform controller converts this to:\n# Deployment, Service, Ingress, HPA, PDB,\n# Postgres CRD, ServiceMonitor, PrometheusRule',
    tags: ['kubernetes', 'platform-engineering', 'developer-experience'],
  },
  {
    id: 'docker-orchestration-l4-052',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement capacity planning and right-sizing for Docker Swarm clusters?',
    options: [
      'Over-provision resources generously',
      'Analyze historical resource usage patterns, project growth, simulate failure scenarios, and size clusters with headroom for both growth and fault tolerance',
      'Use the smallest instances possible',
      'Capacity planning requires cloud-specific tools',
    ],
    correctAnswer: 1,
    explanation: 'Capacity planning involves: collecting resource usage metrics, analyzing trends, accounting for burst capacity, sizing for N+1 redundancy, and regularly reviewing against actual usage.',
    codeSnippet: '# Capacity planning calculations\n#!/bin/bash\n\n# Current resource usage\nTOTAL_CPU=$(docker node ls -q | xargs -I{} docker node inspect {} \\\n  --format \'{{.Description.Resources.NanoCPUs}}\' | \\\n  awk \'{s+=$1} END {print s/1e9}\')\n\nUSED_CPU=$(docker service ls -q | xargs -I{} docker service inspect {} \\\n  --format \'{{.Spec.TaskTemplate.Resources.Reservations.NanoCPUs}}\' | \\\n  awk \'{s+=$1} END {print s/1e9}\')\n\nUTILIZATION=$(echo "$USED_CPU / $TOTAL_CPU * 100" | bc -l)\n\n# Sizing formula\n# Required = (current_usage * growth_factor) / target_utilization\n# + fault_tolerance_headroom\nGROWTH_FACTOR=1.3  # 30% growth\nTARGET_UTIL=0.7    # 70% target\nFAULT_HEADROOM=2   # 2 node failures\n\nREQUIRED=$(echo "$USED_CPU * $GROWTH_FACTOR / $TARGET_UTIL" | bc -l)\necho "Required CPU capacity: $REQUIRED cores (+$FAULT_HEADROOM nodes headroom)"',
    tags: ['swarm', 'capacity-planning', 'sizing'],
  },
  {
    id: 'docker-orchestration-l4-053',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Health Checks',
    question: 'How do you implement a health check aggregation system across an entire Docker Swarm microservices architecture?',
    options: [
      'Check each service individually',
      'Build a health dashboard service that queries all service health endpoints, computes dependency graphs, identifies cascade risks, and provides a unified health API',
      'Use docker service ps to check all services',
      'Health aggregation requires a service mesh',
    ],
    correctAnswer: 1,
    explanation: 'Build a centralized health aggregation service that: discovers all services via Docker API, polls their health endpoints, builds dependency graphs, computes cascade risks, and exposes an aggregate health API.',
    codeSnippet: '// Health aggregation service\nclass HealthAggregator {\n  async getClusterHealth() {\n    const services = await docker.listServices();\n    const healthMap = new Map();\n\n    for (const svc of services) {\n      const tasks = await docker.listTasks({service: svc.ID});\n      const health = {\n        name: svc.Spec.Name,\n        replicas: tasks.length,\n        healthy: tasks.filter(t => t.Status.State === \'running\').length,\n        dependencies: await this.checkDependencies(svc),\n        cascadeRisk: \'low\'\n      };\n\n      if (health.healthy / health.replicas < 0.5) {\n        health.cascadeRisk = \'critical\';\n        // Check downstream dependencies\n        const affected = this.dependencyGraph\n          .getDownstream(svc.Spec.Name);\n        health.affectedServices = affected;\n      }\n      healthMap.set(svc.Spec.Name, health);\n    }\n    return { overall: this.computeOverall(healthMap), services: healthMap };\n  }\n}',
    tags: ['health-checks', 'aggregation', 'dashboard'],
  },
  {
    id: 'docker-orchestration-l4-054',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes cost optimization with spot instances and workload classification?',
    options: [
      'Run everything on on-demand instances',
      'Classify workloads by criticality and fault tolerance, assign cost-appropriate instance types, use spot for interruptible workloads with proper draining and PDBs',
      'Use the cheapest instances for everything',
      'Cost optimization only works with specific cloud providers',
    ],
    correctAnswer: 1,
    explanation: 'Classify workloads: critical (on-demand, reserved), standard (mixed), batch/dev (spot). Use node affinity, taints/tolerations, and PDBs to safely leverage spot instances.',
    codeSnippet: '# Workload classification with Karpenter\napiVersion: karpenter.sh/v1alpha5\nkind: Provisioner\nmetadata:\n  name: batch-spot\nspec:\n  labels:\n    workload-type: batch\n  taints:\n  - key: workload-type\n    value: batch\n    effect: NoSchedule\n  requirements:\n  - key: karpenter.sh/capacity-type\n    operator: In\n    values: ["spot"]\n  - key: kubernetes.io/arch\n    operator: In\n    values: ["amd64"]\n  limits:\n    resources:\n      cpu: "200"\n  ttlSecondsAfterEmpty: 60\n  consolidation:\n    enabled: true\n---\n# Batch workload tolerating spot\napiVersion: batch/v1\nkind: Job\nspec:\n  template:\n    spec:\n      tolerations:\n      - key: workload-type\n        value: batch\n      - key: karpenter.sh/capacity-type\n        value: spot\n      nodeSelector:\n        workload-type: batch\n      terminationGracePeriodSeconds: 120\n      containers:\n      - name: batch\n        image: batch-processor\n        lifecycle:\n          preStop:\n            exec:\n              command: ["/bin/sh","-c","save-checkpoint.sh"]',
    tags: ['kubernetes', 'cost-optimization', 'spot-instances'],
  },
  {
    id: 'docker-orchestration-l4-055',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement network segmentation and microsegmentation in Docker Swarm?',
    options: [
      'Use a single overlay network for all services',
      'Create dedicated overlay networks per security zone, use encrypted overlays for sensitive traffic, and implement application-level firewalling via reverse proxies',
      'Network segmentation requires SDN',
      'Docker Swarm networks are always isolated',
    ],
    correctAnswer: 1,
    explanation: 'Segment services into dedicated overlay networks (DMZ, application, database tiers). Only connect services to networks they need. Use encrypted overlays for sensitive data.',
    codeSnippet: '# Network segmentation\ndocker network create --driver overlay --opt encrypted dmz-net\ndocker network create --driver overlay --opt encrypted app-net\ndocker network create --driver overlay --opt encrypted db-net\n\n# DMZ: public-facing services\ndocker service create --name nginx \\\n  --network dmz-net --network app-net \\\n  -p 443:443 nginx-proxy\n\n# Application tier\ndocker service create --name api \\\n  --network app-net --network db-net \\\n  myapp\n\n# Database tier (no external access)\ndocker service create --name postgres \\\n  --network db-net \\\n  postgres\n\n# api can reach postgres (shared db-net)\n# nginx can reach api (shared app-net)\n# nginx CANNOT reach postgres (no shared network)',
    tags: ['swarm', 'microsegmentation', 'security'],
  },
  {
    id: 'docker-orchestration-l4-056',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Scaling',
    question: 'How do you implement predictive autoscaling for Docker Swarm services?',
    options: [
      'Docker Swarm has built-in predictive scaling',
      'Use historical metrics with time-series forecasting to predict load patterns and pre-scale services before demand increases',
      'React to metrics in real-time only',
      'Predictive scaling is impossible without ML',
    ],
    correctAnswer: 1,
    explanation: 'Analyze historical patterns (time of day, day of week, seasonal trends) using tools like Prophet or simple heuristics. Schedule pre-scaling actions ahead of predicted demand peaks.',
    codeSnippet: '# Predictive autoscaler using historical patterns\nimport pandas as pd\nfrom prophet import Prophet\nimport docker\n\ndef predict_and_scale(service_name, metric_history):\n    # Fit model on historical request rates\n    model = Prophet()\n    model.fit(metric_history)  # df with ds, y columns\n\n    # Predict next hour\n    future = model.make_future_dataframe(periods=60, freq=\'min\')\n    forecast = model.predict(future)\n\n    # Calculate required replicas\n    predicted_rps = forecast.tail(60)[\'yhat\'].max()\n    rps_per_replica = 100  # Known capacity per replica\n    required = max(2, int(predicted_rps / rps_per_replica) + 1)\n\n    # Pre-scale\n    client = docker.from_env()\n    service = client.services.get(service_name)\n    current = service.attrs[\'Spec\'][\'Mode\'][\'Replicated\'][\'Replicas\']\n\n    if required != current:\n        service.scale(required)\n        print(f"Scaled {service_name}: {current} -> {required}")',
    tags: ['scaling', 'predictive', 'forecasting'],
  },
  {
    id: 'docker-orchestration-l4-057',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a production-grade Kubernetes cluster upgrade strategy?',
    options: [
      'Upgrade all components simultaneously',
      'Use a sequential approach: upgrade control plane first (API server, controller, scheduler), then kubelets node by node with cordoning and draining, while maintaining PDB compliance',
      'Create a new cluster and migrate workloads',
      'Let the cloud provider handle all upgrades',
    ],
    correctAnswer: 1,
    explanation: 'Upgrade sequence: etcd first, then control plane components (skew policy: kubelet can be at most one minor version behind API server), then node-by-node with cordon/drain. Test in staging first.',
    codeSnippet: '#!/bin/bash\n# Production K8s upgrade runbook\n\n# 1. Pre-flight checks\nkubectl get nodes -o wide\nkubectl get pods -A | grep -v Running\n\n# 2. Upgrade control plane (kubeadm)\nsudo kubeadm upgrade plan\nsudo kubeadm upgrade apply v1.29.0\n\n# 3. Upgrade each node\nfor node in $(kubectl get nodes -o name); do\n  echo "Upgrading $node"\n\n  # Cordon (prevent new scheduling)\n  kubectl cordon $node\n\n  # Drain (evict pods, respecting PDBs)\n  kubectl drain $node --ignore-daemonsets \\\n    --delete-emptydir-data --timeout=300s\n\n  # SSH and upgrade kubelet\n  ssh $node "sudo apt-get update && \\\n    sudo apt-get install -y kubelet=1.29.0-00 kubectl=1.29.0-00 && \\\n    sudo systemctl daemon-reload && \\\n    sudo systemctl restart kubelet"\n\n  # Uncordon\n  kubectl uncordon $node\n\n  # Verify node ready\n  kubectl wait --for=condition=Ready node/$node --timeout=120s\n\n  echo "$node upgraded successfully"\ndone',
    tags: ['kubernetes', 'upgrades', 'production'],
  },
  {
    id: 'docker-orchestration-l4-058',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a complete GitOps workflow for Docker Swarm deployments?',
    options: [
      'GitOps only works with Kubernetes',
      'Store Compose files and configs in Git, use webhooks to trigger a pull-based deployment controller that reconciles Swarm state with the Git-defined desired state',
      'Push to Git and manually deploy',
      'Use docker stack deploy from CI/CD only',
    ],
    correctAnswer: 1,
    explanation: 'Implement GitOps for Swarm: store all configs in Git, deploy a reconciliation controller that watches the repo and applies changes, ensuring the cluster always matches the Git state.',
    codeSnippet: '# GitOps controller for Docker Swarm\nimport docker, git, time, hashlib\n\ndef reconcile_loop():\n    client = docker.from_env()\n    repo = git.Repo(\'/repo\')\n\n    while True:\n        # Pull latest state from Git\n        repo.remotes.origin.pull()\n\n        # Read desired state\n        desired = parse_compose(\'/repo/docker-compose.yml\')\n\n        # Get current state\n        current = get_current_services(client)\n\n        # Reconcile\n        for svc_name, svc_spec in desired.items():\n            if svc_name not in current:\n                deploy_service(client, svc_name, svc_spec)\n                log(f"Created {svc_name}")\n            elif spec_changed(current[svc_name], svc_spec):\n                update_service(client, svc_name, svc_spec)\n                log(f"Updated {svc_name}")\n\n        # Remove services not in desired state\n        for svc_name in current:\n            if svc_name not in desired:\n                remove_service(client, svc_name)\n                log(f"Removed {svc_name}")\n\n        time.sleep(30)  # Reconcile every 30 seconds',
    tags: ['swarm', 'gitops', 'reconciliation'],
  },
  {
    id: 'docker-orchestration-l4-059',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a comprehensive service dependency testing strategy in Docker Swarm?',
    options: [
      'Test services independently only',
      'Use contract testing between services, deploy isolated test environments with docker stack, inject failures to verify resilience, and run end-to-end tests against the full service graph',
      'Run integration tests on a single node',
      'Dependency testing requires a dedicated test cluster',
    ],
    correctAnswer: 1,
    explanation: 'Comprehensive testing includes: unit tests per service, consumer-driven contract tests (Pact), integration tests with Swarm stacks, chaos tests for failure scenarios, and full e2e tests.',
    codeSnippet: '# Test infrastructure as code\n#!/bin/bash\nTEST_STACK="test-$(date +%s)"\n\n# Deploy isolated test stack\nTAG=$CI_COMMIT_SHA docker stack deploy \\\n  -c docker-compose.yml \\\n  -c docker-compose.test.yml \\\n  $TEST_STACK\n\n# Wait for all services healthy\nfor svc in $(docker stack services $TEST_STACK --format \'{{.Name}}\'); do\n  timeout 120 bash -c "until docker service inspect $svc \\\n    --format \'{{.UpdateStatus.State}}\' | grep -q completed; do sleep 5; done"\ndone\n\n# Run contract tests\ndocker run --rm --network ${TEST_STACK}_default \\\n  pact-verifier --provider-base-url http://api:8080\n\n# Run e2e tests\ndocker run --rm --network ${TEST_STACK}_default \\\n  e2e-tests --base-url http://web:80\n\n# Chaos test - kill random containers\ndocker run --rm -v /var/run/docker.sock:/var/run/docker.sock \\\n  pumba kill --interval 30s "re2:${TEST_STACK}.*" &\nCHAOS_PID=$!\nsleep 120\nkill $CHAOS_PID\n\n# Verify recovery\n./verify-services-healthy.sh $TEST_STACK\n\n# Cleanup\ndocker stack rm $TEST_STACK',
    tags: ['services', 'testing', 'dependency-testing'],
  },
  {
    id: 'docker-orchestration-l4-060',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a Kubernetes-native secrets management solution with external secret stores?',
    options: [
      'Store secrets directly in Kubernetes',
      'Use External Secrets Operator to sync secrets from Vault, AWS Secrets Manager, or Azure Key Vault into Kubernetes Secrets automatically',
      'Mount secret stores as volumes',
      'External secret stores cannot integrate with Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'The External Secrets Operator watches ExternalSecret CRDs and syncs data from external providers (HashiCorp Vault, AWS SM, GCP SM) into native Kubernetes Secrets.',
    codeSnippet: '# External Secrets Operator with Vault\napiVersion: external-secrets.io/v1beta1\nkind: SecretStore\nmetadata:\n  name: vault-backend\nspec:\n  provider:\n    vault:\n      server: "https://vault.example.com"\n      path: "secret"\n      auth:\n        kubernetes:\n          mountPath: "kubernetes"\n          role: "my-role"\n---\napiVersion: external-secrets.io/v1beta1\nkind: ExternalSecret\nmetadata:\n  name: db-credentials\nspec:\n  refreshInterval: 1h\n  secretStoreRef:\n    name: vault-backend\n    kind: SecretStore\n  target:\n    name: db-credentials\n    creationPolicy: Owner\n  data:\n  - secretKey: username\n    remoteRef:\n      key: database/creds\n      property: username\n  - secretKey: password\n    remoteRef:\n      key: database/creds\n      property: password',
    tags: ['kubernetes', 'secrets-management', 'vault'],
  },
  {
    id: 'docker-orchestration-l4-061',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'What are the internal mechanisms of Docker Swarm\'s gossip protocol for node discovery and health?',
    options: [
      'Swarm uses a simple heartbeat mechanism',
      'Swarm uses SWIM-based gossip over TCP/UDP 7946 for membership management, node failure detection, and metadata dissemination across the cluster',
      'Gossip is only used for overlay network management',
      'Swarm does not use a gossip protocol',
    ],
    correctAnswer: 1,
    explanation: 'Docker Swarm uses a SWIM (Scalable Weakly-consistent Infection-style Process Group Membership) gossip protocol on port 7946 for efficient failure detection and membership updates.',
    tags: ['swarm', 'gossip', 'swim-protocol'],
  },
  {
    id: 'docker-orchestration-l4-062',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a comprehensive audit logging system for Docker Swarm operations?',
    options: [
      'Docker Swarm logs all operations by default',
      'Intercept Docker API calls via an audit proxy, log all service/node/secret operations to a tamper-proof log store with timestamps and user identity',
      'Use docker events for complete auditing',
      'Audit logging requires Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Deploy a Docker API proxy that logs all requests with caller identity, action, and parameters. Store logs in an append-only store (e.g., Elasticsearch with ILM) for compliance.',
    codeSnippet: '# Docker API audit proxy\nfrom flask import Flask, request\nimport docker, logging, json, datetime\n\naudit_logger = logging.getLogger(\'audit\')\n\n@app.before_request\ndef audit_log():\n    audit_entry = {\n        \'timestamp\': datetime.utcnow().isoformat(),\n        \'method\': request.method,\n        \'path\': request.path,\n        \'user\': request.headers.get(\'X-User\', \'unknown\'),\n        \'source_ip\': request.remote_addr,\n        \'body_preview\': request.get_json(silent=True)\n    }\n    audit_logger.info(json.dumps(audit_entry))\n\n@app.route(\'/<path:path>\', methods=[\'GET\',\'POST\',\'PUT\',\'DELETE\'])\ndef proxy(path):\n    # Forward to actual Docker socket\n    resp = docker_api_forward(request.method, path, request.data)\n    audit_logger.info(json.dumps({\n        \'response_code\': resp.status_code,\n        \'path\': path\n    }))\n    return resp',
    tags: ['services', 'audit-logging', 'compliance'],
  },
  {
    id: 'docker-orchestration-l4-063',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes cluster authentication with OIDC for enterprise SSO integration?',
    options: [
      'Use static token files for all users',
      'Configure the API server with OIDC flags pointing to the identity provider, map OIDC claims to Kubernetes groups, and bind groups to RBAC roles',
      'Use basic auth with username/password',
      'OIDC integration requires a commercial Kubernetes distribution',
    ],
    correctAnswer: 1,
    explanation: 'Configure the API server to validate JWT tokens from an OIDC provider (Okta, Azure AD, Dex). Map OIDC claims (groups, email) to Kubernetes RBAC for enterprise access control.',
    codeSnippet: '# API server OIDC configuration\n--oidc-issuer-url=https://accounts.google.com\n--oidc-client-id=kubernetes\n--oidc-username-claim=email\n--oidc-groups-claim=groups\n--oidc-groups-prefix=oidc:\n\n# RBAC binding for OIDC group\napiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRoleBinding\nmetadata:\n  name: platform-admins\nsubjects:\n- kind: Group\n  name: "oidc:platform-team"\n  apiGroup: rbac.authorization.k8s.io\nroleRef:\n  kind: ClusterRole\n  name: admin\n  apiGroup: rbac.authorization.k8s.io\n\n# kubeconfig with OIDC\nusers:\n- name: developer\n  user:\n    exec:\n      apiVersion: client.authentication.k8s.io/v1beta1\n      command: kubelogin\n      args:\n      - get-token\n      - --oidc-issuer-url=https://accounts.google.com\n      - --oidc-client-id=kubernetes',
    tags: ['kubernetes', 'oidc', 'authentication'],
  },
  {
    id: 'docker-orchestration-l4-064',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you handle the transition from Docker Swarm to alternative orchestrators while maintaining service continuity?',
    options: [
      'Swarm is the only viable orchestrator',
      'Run parallel infrastructure during migration, use feature-equivalent abstractions, migrate services incrementally with traffic shifting, and maintain rollback capability',
      'Stop Swarm and start the new orchestrator',
      'Convert all Compose files at once',
    ],
    correctAnswer: 1,
    explanation: 'Plan: inventory all services and dependencies, set up the target platform alongside Swarm, migrate non-critical services first, use DNS/LB for traffic shifting, and keep Swarm as fallback until validation.',
    tags: ['swarm', 'migration', 'planning'],
  },
  {
    id: 'docker-orchestration-l4-065',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a multi-stage deployment pipeline with environment promotion in Docker Swarm?',
    options: [
      'Deploy directly to production',
      'Use separate Swarms per environment, promote immutable artifacts through stages (dev -> staging -> prod) with gates, and track promotions in a deployment manifest',
      'Use branches for different environments',
      'Environment promotion requires Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Build once, deploy everywhere: same image SHA goes through dev, staging, prod Swarms. Each stage has gates (tests, approvals). Track state in a deployment manifest or database.',
    codeSnippet: '# Multi-stage promotion pipeline\nstages:\n  - stage: dev\n    swarm: dev-swarm\n    auto_deploy: true\n    tests: [unit, integration]\n\n  - stage: staging\n    swarm: staging-swarm\n    auto_deploy: true\n    tests: [e2e, performance, security]\n    gate: auto  # All tests must pass\n\n  - stage: production\n    swarm: prod-swarm\n    auto_deploy: false\n    gate: manual  # Requires approval\n    canary:\n      initial_percent: 5\n      increment: 10\n      interval: 15m\n      metrics:\n        error_rate: "< 0.1%"\n        p99_latency: "< 500ms"\n\n# Promotion command\n./promote.sh --image myapp:abc123 --from staging --to production',
    tags: ['services', 'promotion', 'multi-stage'],
  },
  {
    id: 'docker-orchestration-l4-066',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes-native application configuration management at enterprise scale?',
    options: [
      'Use ConfigMaps and Secrets directly',
      'Use Kustomize for environment-specific overlays, Sealed Secrets for GitOps-safe encryption, and external-config-operator for dynamic config from external sources',
      'Store all config in environment variables',
      'Use a single Helm chart for everything',
    ],
    correctAnswer: 1,
    explanation: 'At scale: Kustomize provides base + overlay structure, Sealed Secrets encrypts secrets for Git storage, external-config-data syncs from Consul/Vault, and Helm for complex templating.',
    codeSnippet: '# Kustomize directory structure\n# base/\n#   deployment.yaml\n#   kustomization.yaml\n# overlays/\n#   dev/kustomization.yaml\n#   staging/kustomization.yaml\n#   prod/kustomization.yaml\n\n# overlays/prod/kustomization.yaml\napiVersion: kustomize.config.k8s.io/v1beta1\nkind: Kustomization\nbases:\n- ../../base\nnamePrefix: prod-\ncommonLabels:\n  environment: production\npatchesStrategicMerge:\n- deployment-patch.yaml\nconfigMapGenerator:\n- name: app-config\n  envs:\n  - config.env\nsecretGenerator:\n- name: sealed-db-creds\n  type: kubernetes.io/tls\n  files:\n  - tls.crt=sealed-cert.pem\n  - tls.key=sealed-key.pem',
    tags: ['kubernetes', 'configuration', 'kustomize'],
  },
  {
    id: 'docker-orchestration-l4-067',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you benchmark and optimize Docker Swarm scheduling performance?',
    options: [
      'Scheduling performance cannot be measured',
      'Measure task placement latency across the scheduling pipeline (allocation, scheduling, dispatch, preparation), identify bottlenecks, and tune parameters',
      'Use more powerful hardware for managers',
      'Reduce the number of services',
    ],
    correctAnswer: 1,
    explanation: 'Benchmark by measuring time from service create to task running. Profile each stage: IP/port allocation, node selection, task dispatch, image pull. Optimize by pre-pulling images and tuning batch sizes.',
    codeSnippet: '# Benchmark scheduling latency\n#!/bin/bash\nfor i in $(seq 1 100); do\n  START=$(date +%s%N)\n\n  docker service create --name bench-$i --replicas 1 \\\n    --restart-condition none alpine sleep 10\n\n  # Wait for task to be running\n  while ! docker service ps bench-$i --format \'{{.CurrentState}}\' | grep -q Running; do\n    sleep 0.1\n  done\n\n  END=$(date +%s%N)\n  LATENCY=$(( (END - START) / 1000000 ))  # ms\n  echo "Service $i scheduled in ${LATENCY}ms"\n\n  docker service rm bench-$i\ndone\n\n# Optimization: pre-pull images on all nodes\ndocker service create --mode global --name prepull \\\n  --restart-condition none myapp:v2 true',
    tags: ['swarm', 'benchmarking', 'scheduling'],
  },
  {
    id: 'docker-orchestration-l4-068',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Rolling Updates',
    question: 'How do you implement a comprehensive rollback strategy that handles both application and infrastructure state?',
    options: [
      'Use docker service rollback only',
      'Maintain versioned snapshots of application images, configuration state, database schemas, and infrastructure configs; coordinate rollback across all layers simultaneously',
      'Rollback application code only',
      'Infrastructure rollback is not possible',
    ],
    correctAnswer: 1,
    explanation: 'Full rollback requires coordinated reversal of: application images, Docker configs/secrets, environment variables, database schema (backward-compatible migrations), and infrastructure changes.',
    codeSnippet: '#!/bin/bash\n# Coordinated rollback system\nROLLBACK_VERSION=$1\n\n# Load rollback manifest\nMANIFEST=$(cat "deployments/${ROLLBACK_VERSION}.json")\nIMAGE=$(echo $MANIFEST | jq -r \'.image\')\nCONFIG_VER=$(echo $MANIFEST | jq -r \'.config_version\')\nDB_MIGRATION=$(echo $MANIFEST | jq -r \'.db_migration\')\n\necho "Rolling back to version $ROLLBACK_VERSION"\n\n# 1. Verify database backward compatibility\nif ! ./check-db-compat.sh $DB_MIGRATION; then\n  echo "WARNING: DB rollback may cause data issues"\n  read -p "Continue? [y/N] " confirm\n  [[ $confirm != "y" ]] && exit 1\nfi\n\n# 2. Rollback database (if needed)\n./db-rollback.sh $DB_MIGRATION\n\n# 3. Rollback configs\ndocker config create app-config-rollback config-${CONFIG_VER}.json\n\n# 4. Rollback application\ndocker service update \\\n  --image $IMAGE \\\n  --config-rm current-config \\\n  --config-add source=app-config-rollback,target=/app/config.json \\\n  --update-order stop-first \\\n  api\n\n# 5. Verify rollback\n./health-check.sh api || {\n  echo "CRITICAL: Rollback failed!"\n  alert_oncall "Rollback to $ROLLBACK_VERSION failed"\n  exit 1\n}\n\necho "Rollback to $ROLLBACK_VERSION successful"',
    tags: ['rolling-updates', 'rollback', 'coordinated'],
  },
  {
    id: 'docker-orchestration-l4-069',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a Kubernetes cluster monitoring and alerting stack at enterprise scale?',
    options: [
      'Use kubectl top for monitoring',
      'Deploy Prometheus with Thanos/Cortex for long-term storage, Grafana for dashboards, Alertmanager with routing trees, and custom ServiceMonitors for each application',
      'Use cloud provider monitoring only',
      'Install a single Prometheus instance',
    ],
    correctAnswer: 1,
    explanation: 'Enterprise monitoring: federated Prometheus with Thanos for long-term storage and global view, Grafana for visualization, Alertmanager with smart routing, and per-team ServiceMonitors.',
    codeSnippet: '# Thanos sidecar for Prometheus\napiVersion: monitoring.coreos.com/v1\nkind: Prometheus\nmetadata:\n  name: prometheus\nspec:\n  replicas: 2\n  retention: 24h\n  thanos:\n    image: thanosio/thanos:v0.32\n    objectStorageConfig:\n      key: thanos.yaml\n      name: thanos-objstore-config\n---\n# ServiceMonitor per application\napiVersion: monitoring.coreos.com/v1\nkind: ServiceMonitor\nmetadata:\n  name: api-monitor\n  labels:\n    team: backend\nspec:\n  selector:\n    matchLabels:\n      app: api\n  endpoints:\n  - port: metrics\n    interval: 15s\n    path: /metrics\n---\n# PrometheusRule for SLO alerts\napiVersion: monitoring.coreos.com/v1\nkind: PrometheusRule\nmetadata:\n  name: api-slo\nspec:\n  groups:\n  - name: api-slo\n    rules:\n    - alert: APIHighLatency\n      expr: |\n        histogram_quantile(0.99,\n          sum(rate(http_request_duration_seconds_bucket[5m]))\n          by (le)\n        ) > 0.5\n      for: 5m\n      labels:\n        severity: warning\n        team: backend',
    tags: ['kubernetes', 'monitoring', 'enterprise'],
  },
  {
    id: 'docker-orchestration-l4-070',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a zero-trust network architecture within Docker Swarm?',
    options: [
      'Overlay networks provide sufficient isolation',
      'Implement encrypted overlays, per-service mTLS via application-level certificates stored as secrets, and deploy a policy enforcement proxy for request-level authorization',
      'Use VPN tunnels between all containers',
      'Zero-trust networking requires a service mesh only available in Kubernetes',
    ],
    correctAnswer: 1,
    explanation: 'Zero-trust in Swarm: encrypt all overlay traffic, use mTLS between services with cert rotation via secrets, deploy policy enforcement proxies (OPA/Envoy) for per-request authorization.',
    codeSnippet: '# Zero-trust service deployment\n# 1. Encrypted overlay\ndocker network create --driver overlay --opt encrypted zt-net\n\n# 2. mTLS certs as secrets\ndocker secret create svc-a-cert svc-a.crt\ndocker secret create svc-a-key svc-a.key\ndocker secret create ca-cert ca.crt\n\n# 3. Deploy with mTLS and policy sidecar\ndocker service create --name svc-a \\\n  --network zt-net \\\n  --secret svc-a-cert --secret svc-a-key --secret ca-cert \\\n  myapp-with-mtls\n\n# 4. OPA policy sidecar for authorization\ndocker service create --name opa-svc-a \\\n  --network zt-net \\\n  --config source=opa-policy,target=/policy/auth.rego \\\n  openpolicyagent/opa run --server /policy',
    tags: ['swarm', 'zero-trust', 'mtls'],
  },
  {
    id: 'docker-orchestration-l4-071',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a service catalog with automated dependency management in Docker Swarm?',
    options: [
      'Maintain a spreadsheet of services',
      'Build an automated service catalog that discovers services via Docker API, tracks dependencies through network topology and health checks, and provides a dependency graph API',
      'Use Docker Hub as a service catalog',
      'Service catalogs require Kubernetes with Backstage',
    ],
    correctAnswer: 1,
    explanation: 'Build a catalog service that queries Docker API for services, analyzes overlay network connections for dependencies, monitors health propagation, and exposes a graph for visualization.',
    tags: ['services', 'catalog', 'dependency-management'],
  },
  {
    id: 'docker-orchestration-l4-072',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes-based edge computing with lightweight distributions?',
    options: [
      'Deploy standard Kubernetes on edge nodes',
      'Use K3s or MicroK8s for lightweight edge clusters, sync workloads from a central control plane, and handle intermittent connectivity gracefully',
      'Edge computing is incompatible with Kubernetes',
      'Use Docker Swarm for edge instead',
    ],
    correctAnswer: 1,
    explanation: 'K3s provides a lightweight Kubernetes distribution (~70MB) suitable for edge. Combined with fleet management tools (Rancher, KubeEdge), it enables centrally managed edge deployments.',
    codeSnippet: '# Install K3s on edge node (single binary)\ncurl -sfL https://get.k3s.io | sh -s - \\\n  --node-label "location=edge-store-42" \\\n  --node-label "type=retail"\n\n# Fleet management with Rancher\napiVersion: fleet.cattle.io/v1alpha1\nkind: GitRepo\nmetadata:\n  name: edge-apps\n  namespace: fleet-default\nspec:\n  repo: https://github.com/org/edge-manifests\n  paths:\n  - retail-app\n  targets:\n  - name: retail-stores\n    clusterSelector:\n      matchLabels:\n        type: retail\n  - name: warehouse\n    clusterSelector:\n      matchLabels:\n        type: warehouse',
    tags: ['kubernetes', 'edge-computing', 'k3s'],
  },
  {
    id: 'docker-orchestration-l4-073',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement automated compliance scanning and remediation in Docker Swarm?',
    options: [
      'Run manual security audits periodically',
      'Deploy automated scanners that continuously verify service configurations, node security, secret usage, and network policies against compliance baselines, with auto-remediation',
      'Use Docker Bench Security once',
      'Compliance scanning requires specialized enterprise tools',
    ],
    correctAnswer: 1,
    explanation: 'Build a compliance controller that continuously audits: image vulnerabilities, secret hygiene, network encryption, resource limits, and node hardening. Auto-remediate where safe, alert otherwise.',
    codeSnippet: '# Compliance scanner\nimport docker\n\ndef audit_swarm(client):\n    findings = []\n\n    for svc in client.services.list():\n        spec = svc.attrs[\'Spec\']\n\n        # Check: no latest tag\n        image = spec[\'TaskTemplate\'][\'ContainerSpec\'][\'Image\']\n        if \':latest\' in image or \':\' not in image:\n            findings.append({\n                \'service\': spec[\'Name\'],\n                \'severity\': \'HIGH\',\n                \'finding\': \'Using latest/untagged image\',\n                \'remediation\': \'Pin to specific version\'\n            })\n\n        # Check: resource limits set\n        resources = spec[\'TaskTemplate\'].get(\'Resources\', {})\n        if not resources.get(\'Limits\'):\n            findings.append({\n                \'service\': spec[\'Name\'],\n                \'severity\': \'MEDIUM\',\n                \'finding\': \'No resource limits\',\n                \'auto_remediate\': True\n            })\n\n        # Check: health check defined\n        healthcheck = spec[\'TaskTemplate\'][\'ContainerSpec\'].get(\'Healthcheck\')\n        if not healthcheck:\n            findings.append({\n                \'service\': spec[\'Name\'],\n                \'severity\': \'MEDIUM\',\n                \'finding\': \'No health check defined\'\n            })\n\n    return findings',
    tags: ['swarm', 'compliance', 'automation'],
  },
  {
    id: 'docker-orchestration-l4-074',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Health Checks',
    question: 'How do you implement a canary health check system that compares new version metrics against the baseline?',
    options: [
      'Use standard health checks for canary deployments',
      'Deploy a canary controller that collects real-time metrics from both canary and baseline, performs statistical comparison, and makes automated promotion/rollback decisions',
      'Manually compare dashboards during canary',
      'Canary health checks require Kubernetes with Flagger',
    ],
    correctAnswer: 1,
    explanation: 'Build a canary analysis system that compares key metrics (error rate, latency percentiles, resource usage) between canary and baseline using statistical tests (Mann-Whitney, Kolmogorov-Smirnov).',
    codeSnippet: '# Canary analysis engine\nimport numpy as np\nfrom scipy import stats\n\ndef analyze_canary(baseline_metrics, canary_metrics):\n    results = {}\n\n    for metric_name in [\'error_rate\', \'p99_latency\', \'p50_latency\']:\n        baseline = np.array(baseline_metrics[metric_name])\n        canary = np.array(canary_metrics[metric_name])\n\n        # Mann-Whitney U test\n        stat, p_value = stats.mannwhitneyu(\n            baseline, canary, alternative=\'less\'\n        )\n\n        # Effect size (Cohen\'s d)\n        effect_size = (canary.mean() - baseline.mean()) / \\\n            np.sqrt((baseline.std()**2 + canary.std()**2) / 2)\n\n        results[metric_name] = {\n            \'baseline_mean\': baseline.mean(),\n            \'canary_mean\': canary.mean(),\n            \'p_value\': p_value,\n            \'effect_size\': effect_size,\n            \'verdict\': \'pass\' if p_value > 0.05 else \'fail\'\n        }\n\n    overall = \'pass\' if all(\n        r[\'verdict\'] == \'pass\' for r in results.values()\n    ) else \'fail\'\n\n    return {\'overall\': overall, \'metrics\': results}',
    tags: ['health-checks', 'canary', 'statistical-analysis'],
  },
  {
    id: 'docker-orchestration-l4-075',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a Kubernetes-native progressive delivery system with Argo Rollouts?',
    options: [
      'Use standard Kubernetes Deployments',
      'Replace Deployments with Argo Rollouts that support canary with traffic shaping, blue-green, and automated analysis with metrics integration',
      'Argo Rollouts replaces the Kubernetes scheduler',
      'Progressive delivery is only possible with Istio',
    ],
    correctAnswer: 1,
    explanation: 'Argo Rollouts provides a Rollout CRD replacing Deployment with native support for canary (with traffic shaping via Istio/Nginx/ALB), blue-green, automated analysis, and rollback.',
    codeSnippet: 'apiVersion: argoproj.io/v1alpha1\nkind: Rollout\nmetadata:\n  name: web\nspec:\n  replicas: 10\n  strategy:\n    canary:\n      canaryService: web-canary\n      stableService: web-stable\n      trafficRouting:\n        istio:\n          virtualServices:\n          - name: web-vsvc\n            routes:\n            - primary\n      analysis:\n        templates:\n        - templateName: success-rate\n        startingStep: 2\n        args:\n        - name: service-name\n          value: web-canary\n      steps:\n      - setWeight: 5\n      - pause: {duration: 5m}\n      - setWeight: 20\n      - pause: {duration: 5m}\n      - setWeight: 50\n      - pause: {duration: 10m}\n      - setWeight: 80\n      - pause: {duration: 10m}\n---\napiVersion: argoproj.io/v1alpha1\nkind: AnalysisTemplate\nmetadata:\n  name: success-rate\nspec:\n  args:\n  - name: service-name\n  metrics:\n  - name: success-rate\n    interval: 60s\n    successCondition: result[0] > 0.99\n    provider:\n      prometheus:\n        address: http://prometheus:9090\n        query: |\n          sum(rate(http_requests_total{service="{{args.service-name}}",\n            code!~"5.."}[5m])) /\n          sum(rate(http_requests_total{service="{{args.service-name}}"}[5m]))',
    tags: ['kubernetes', 'argo-rollouts', 'progressive-delivery'],
  },
  {
    id: 'docker-orchestration-l4-076',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a Docker Swarm upgrade strategy that maintains cluster availability during the Docker Engine upgrade process?',
    options: [
      'Upgrade all nodes simultaneously during a maintenance window',
      'Implement a rolling Docker Engine upgrade with manager-first strategy, maintaining Raft quorum at each step, with pre-upgrade validation and rollback checkpoints',
      'Only upgrade when the cluster is idle',
      'In-place upgrades are not safe; create a new cluster',
    ],
    correctAnswer: 1,
    explanation: 'Upgrade managers one at a time keeping quorum. For each: backup Raft data, drain, upgrade Docker, verify Raft health. Then upgrade workers in batches. Maintain rollback capability at each step.',
    codeSnippet: '#!/bin/bash\n# Safe Docker Engine upgrade for Swarm\nset -e\n\n# Pre-flight\ndocker node ls\nTOTAL_MANAGERS=$(docker node ls --filter role=manager -q | wc -l)\necho "Total managers: $TOTAL_MANAGERS"\n\n# Backup before upgrade\nssh manager-1 "systemctl stop docker && \\\n  cp -a /var/lib/docker/swarm /backup/pre-upgrade-swarm && \\\n  systemctl start docker"\n\n# Upgrade managers one at a time\nfor mgr in manager-{1..3}; do\n  echo "=== Upgrading $mgr ==="\n\n  # Check quorum\n  HEALTHY=$(docker node ls --filter role=manager \\\n    --format \'{{.Status}}\' | grep -c Ready)\n  if [ $HEALTHY -lt $((TOTAL_MANAGERS/2 + 1)) ]; then\n    echo "ABORT: Quorum at risk"\n    exit 1\n  fi\n\n  docker node update --availability drain $mgr\n  ssh $mgr "apt-get update && apt-get install -y docker-ce=5:25.0.0-1"\n  docker node update --availability active $mgr\n\n  # Verify Raft health\n  sleep 10\n  docker node ls\n  echo "$mgr upgraded successfully"\ndone',
    tags: ['swarm', 'engine-upgrade', 'rolling'],
  },
  {
    id: 'docker-orchestration-l4-077',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Scaling',
    question: 'How do you implement queue-based autoscaling for Docker Swarm worker services?',
    options: [
      'Scale based on CPU metrics only',
      'Monitor message queue depth and consumer lag, calculate required workers based on processing rate, and scale the service to match demand with dampening to prevent oscillation',
      'Use fixed replica counts',
      'Queue-based scaling requires Kubernetes KEDA',
    ],
    correctAnswer: 1,
    explanation: 'Monitor queue metrics (depth, consumer lag, message age). Calculate required workers: workers = queue_depth / (processing_rate * target_processing_time). Add dampening to prevent thrashing.',
    codeSnippet: '# Queue-based autoscaler for Swarm\nimport docker, redis, time\n\nclient = docker.from_env()\nredis_client = redis.Redis(host=\'redis\')\n\ndef autoscale_workers():\n    while True:\n        # Get queue metrics\n        queue_depth = redis_client.llen(\'task_queue\')\n        processing_rate = float(redis_client.get(\'avg_rate\') or 10)  # msgs/sec/worker\n\n        # Calculate desired workers\n        target_drain_time = 300  # 5 minutes\n        desired = max(1, int(queue_depth / (processing_rate * target_drain_time)))\n        desired = min(desired, 50)  # Max cap\n\n        # Dampening: only scale if difference is significant\n        service = client.services.get(\'worker\')\n        current = service.attrs[\'Spec\'][\'Mode\'][\'Replicated\'][\'Replicas\']\n\n        if abs(desired - current) >= max(1, current * 0.2):  # 20% threshold\n            # Smooth scaling: don\'t jump more than 50%\n            if desired > current:\n                target = min(desired, int(current * 1.5))\n            else:\n                target = max(desired, int(current * 0.7))\n\n            service.scale(target)\n            print(f"Scaled workers: {current} -> {target} (queue: {queue_depth})")\n\n        time.sleep(30)',
    tags: ['scaling', 'queue-based', 'autoscaling'],
  },
  {
    id: 'docker-orchestration-l4-078',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes workload identity for secure cloud service access without static credentials?',
    options: [
      'Store cloud credentials as Kubernetes Secrets',
      'Use workload identity federation (GKE WI, EKS IRSA, AKS Pod Identity) to bind Kubernetes ServiceAccounts to cloud IAM roles',
      'Use instance profiles for all Pods',
      'Mount cloud credentials from a vault',
    ],
    correctAnswer: 1,
    explanation: 'Workload identity binds K8s ServiceAccounts to cloud IAM roles. Pods automatically receive short-lived cloud credentials without any static secrets, following the principle of least privilege.',
    codeSnippet: '# AWS EKS: IAM Roles for Service Accounts (IRSA)\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: s3-reader\n  annotations:\n    eks.amazonaws.com/role-arn: arn:aws:iam::123456789:role/s3-reader\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: data-processor\nspec:\n  template:\n    spec:\n      serviceAccountName: s3-reader\n      containers:\n      - name: app\n        image: data-processor\n        # AWS SDK automatically uses IRSA token\n        # No AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY needed!\n\n# IAM Trust Policy\n{\n  "Version": "2012-10-17",\n  "Statement": [{\n    "Effect": "Allow",\n    "Principal": {\n      "Federated": "arn:aws:iam::123456789:oidc-provider/oidc.eks.region.amazonaws.com/id/CLUSTER_ID"\n    },\n    "Action": "sts:AssumeRoleWithWebIdentity",\n    "Condition": {\n      "StringEquals": {\n        "oidc.../:sub": "system:serviceaccount:default:s3-reader"\n      }\n    }\n  }]\n}',
    tags: ['kubernetes', 'workload-identity', 'cloud-security'],
  },
  {
    id: 'docker-orchestration-l4-079',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you optimize Docker Swarm for running machine learning inference workloads?',
    options: [
      'Docker Swarm is not suitable for ML workloads',
      'Use generic resources for GPU allocation, dedicated nodes with GPU labels, optimized container images with CUDA, and model versioning via Docker configs',
      'Run ML inference on separate infrastructure',
      'Use Kubernetes with GPU operator',
    ],
    correctAnswer: 1,
    explanation: 'Configure GPU nodes with generic resources, create optimized inference images with CUDA/TensorRT, serve models via Docker configs for hot-swapping, and scale based on request queues.',
    codeSnippet: '# GPU node daemon.json\n{\n  "runtimes": {\n    "nvidia": {\n      "path": "/usr/bin/nvidia-container-runtime"\n    }\n  },\n  "default-runtime": "nvidia",\n  "node-generic-resources": [\n    "NVIDIA-GPU=GPU-uuid-1",\n    "NVIDIA-GPU=GPU-uuid-2"\n  ]\n}\n\n# ML inference service\ndocker service create --name ml-inference \\\n  --generic-resource "NVIDIA-GPU=1" \\\n  --constraint \'node.labels.gpu == true\' \\\n  --replicas 4 \\\n  --limit-memory 16G \\\n  --config source=model-v3,target=/models/current \\\n  --env MODEL_PATH=/models/current \\\n  --env BATCH_SIZE=32 \\\n  --publish 8501:8501 \\\n  ml-serving:cuda12\n\n# Hot-swap model version\ndocker config create model-v4 new-model.pb\ndocker service update \\\n  --config-rm model-v3 \\\n  --config-add source=model-v4,target=/models/current \\\n  ml-inference',
    tags: ['swarm', 'ml-inference', 'gpu'],
  },
  {
    id: 'docker-orchestration-l4-080',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Services',
    question: 'How do you implement a complete API gateway pattern in Docker Swarm?',
    options: [
      'Expose each service directly to the internet',
      'Deploy Kong, Traefik, or a custom gateway as a Swarm service handling authentication, rate limiting, request routing, SSL termination, and API versioning',
      'Use the Swarm routing mesh as an API gateway',
      'API gateways require Kubernetes Ingress controllers',
    ],
    correctAnswer: 1,
    explanation: 'Deploy a full API gateway (Kong/Traefik) as a Swarm service that provides centralized authentication, rate limiting, request transformation, SSL termination, and API versioning for all backend services.',
    codeSnippet: '# Kong API Gateway on Swarm\nservices:\n  kong-db:\n    image: postgres:15\n    environment:\n      POSTGRES_DB: kong\n      POSTGRES_USER: kong\n      POSTGRES_PASSWORD_FILE: /run/secrets/kong_db_pass\n    secrets:\n      - kong_db_pass\n    networks:\n      - kong-internal\n\n  kong:\n    image: kong:3.4\n    environment:\n      KONG_DATABASE: postgres\n      KONG_PG_HOST: kong-db\n    deploy:\n      replicas: 3\n      update_config:\n        order: start-first\n    ports:\n      - "8000:8000"  # Proxy\n      - "8443:8443"  # Proxy SSL\n    networks:\n      - kong-internal\n      - frontend\n      - backend\n\n# Kong route configuration\n# POST :8001/services -d name=user-api -d url=http://user-service:8080\n# POST :8001/services/user-api/routes -d paths[]=/api/v1/users\n# POST :8001/services/user-api/plugins -d name=rate-limiting -d config.minute=100\n# POST :8001/services/user-api/plugins -d name=jwt',
    tags: ['services', 'api-gateway', 'kong'],
  },
  {
    id: 'docker-orchestration-l4-081',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes-native event-driven autoscaling with KEDA?',
    options: [
      'Use HPA with custom metrics only',
      'Deploy KEDA to scale workloads based on external event sources like Kafka topics, SQS queues, HTTP requests, and cron schedules',
      'Event-driven scaling requires custom controllers',
      'KEDA only works with Azure services',
    ],
    correctAnswer: 1,
    explanation: 'KEDA extends Kubernetes HPA to scale based on external event sources. It can scale to/from zero and supports 60+ scalers for queues, databases, metrics systems, and more.',
    codeSnippet: 'apiVersion: keda.sh/v1alpha1\nkind: ScaledObject\nmetadata:\n  name: kafka-consumer-scaler\nspec:\n  scaleTargetRef:\n    name: kafka-consumer\n  pollingInterval: 15\n  cooldownPeriod: 60\n  minReplicaCount: 0\n  maxReplicaCount: 50\n  triggers:\n  - type: kafka\n    metadata:\n      bootstrapServers: kafka:9092\n      consumerGroup: my-group\n      topic: events\n      lagThreshold: "100"\n  - type: prometheus\n    metadata:\n      serverAddress: http://prometheus:9090\n      metricName: http_requests_per_second\n      threshold: "100"\n      query: |\n        sum(rate(http_requests_total{service="api"}[2m]))',
    tags: ['kubernetes', 'keda', 'event-driven'],
  },
  {
    id: 'docker-orchestration-l4-082',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a Docker Swarm-based platform with tenant isolation using Linux security features?',
    options: [
      'Docker provides sufficient isolation by default',
      'Combine Docker user namespaces, seccomp profiles, AppArmor/SELinux policies, read-only filesystems, and dropped capabilities for defense-in-depth',
      'Use separate Docker daemons per tenant',
      'Linux security features are not applicable to Swarm',
    ],
    correctAnswer: 1,
    explanation: 'Layer security: user namespaces map container root to unprivileged host user, seccomp restricts syscalls, AppArmor/SELinux provides MAC, read-only root prevents tampering, dropped capabilities reduce attack surface.',
    codeSnippet: '# Hardened service deployment\ndocker service create --name secure-app \\\n  --read-only \\\n  --mount type=tmpfs,dst=/tmp \\\n  --cap-drop ALL \\\n  --cap-add NET_BIND_SERVICE \\\n  --security-opt no-new-privileges:true \\\n  --security-opt seccomp=/path/to/seccomp-profile.json \\\n  --security-opt apparmor=docker-restricted \\\n  --user 1000:1000 \\\n  --pids-limit 100 \\\n  --limit-cpu 1 \\\n  --limit-memory 512M \\\n  --network isolated-overlay \\\n  myapp:hardened',
    tags: ['swarm', 'linux-security', 'hardening'],
  },
  {
    id: 'docker-orchestration-l4-083',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Rolling Updates',
    question: 'How do you implement a deployment strategy that ensures backward compatibility of API contracts during rolling updates?',
    options: [
      'Deploy new API versions on separate endpoints',
      'Use semantic versioning with API version negotiation, deploy with start-first order, and run consumer-driven contract tests during the rolling update window',
      'Break compatibility and update all clients simultaneously',
      'API compatibility is the application team\'s problem',
    ],
    correctAnswer: 1,
    explanation: 'Ensure backward compatibility by: supporting N and N-1 API versions simultaneously, using consumer-driven contracts (Pact), deploying with start-first order, and verifying contracts during rollout.',
    codeSnippet: '# API versioning middleware\napp.use(\'/api\', (req, res, next) => {\n  const version = req.headers[\'accept-version\'] || \'v2\';\n  req.apiVersion = version;\n  next();\n});\n\n// Support both v1 and v2\napp.get(\'/api/users/:id\', (req, res) => {\n  const user = getUser(req.params.id);\n  if (req.apiVersion === \'v1\') {\n    // V1 response (backward compatible)\n    res.json({ id: user.id, name: user.name });\n  } else {\n    // V2 response (new fields)\n    res.json({\n      id: user.id,\n      name: user.name,\n      email: user.email,\n      roles: user.roles\n    });\n  }\n});\n\n# Pact contract verification in CI\n# Run during rolling update to verify both versions satisfy contracts\npact-verifier --provider-base-url http://api:8080 \\\n  --pact-broker-base-url https://pact-broker \\\n  --provider api --consumer-version-selectors \'{"deployed": true}\'',
    tags: ['rolling-updates', 'api-compatibility', 'contract-testing'],
  },
  {
    id: 'docker-orchestration-l4-084',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement a multi-cluster service mesh with Istio for cross-cluster communication?',
    options: [
      'Istio only works within a single cluster',
      'Configure Istio multi-cluster with shared control plane or replicated control planes, using east-west gateways for cross-cluster traffic and unified observability',
      'Use VPN tunnels between clusters',
      'Multi-cluster mesh requires proprietary solutions',
    ],
    correctAnswer: 1,
    explanation: 'Istio multi-cluster supports primary-remote (shared control plane) or multi-primary (replicated) topologies. East-west gateways handle cross-cluster traffic with mTLS.',
    codeSnippet: '# Multi-primary Istio installation\n# Cluster 1\nistioctl install -f cluster1.yaml --set values.global.meshID=mesh1 \\\n  --set values.global.multiCluster.clusterName=cluster1 \\\n  --set values.global.network=network1\n\n# Create cross-cluster secret\nistioctl x create-remote-secret --name=cluster2 | \\\n  kubectl apply -f - --context=cluster1\n\n# East-west gateway for cross-cluster traffic\napiVersion: install.istio.io/v1alpha1\nkind: IstioOperator\nmetadata:\n  name: eastwest\nspec:\n  components:\n    ingressGateways:\n    - name: istio-eastwestgateway\n      label:\n        istio: eastwestgateway\n        topology.istio.io/network: network1\n      enabled: true\n      k8s:\n        env:\n        - name: ISTIO_META_REQUESTED_NETWORK_VIEW\n          value: network1\n\n# Service accessible across clusters\napiVersion: v1\nkind: Service\nmetadata:\n  name: api\n  labels:\n    app: api\n  annotations:\n    # Automatic cross-cluster discovery\n    networking.istio.io/exportTo: "*"',
    tags: ['kubernetes', 'istio', 'multi-cluster'],
  },
  {
    id: 'docker-orchestration-l4-085',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you implement a Docker Swarm cluster that spans on-premises and cloud infrastructure?',
    options: [
      'Swarm cannot span on-premises and cloud',
      'Use VPN/WireGuard tunnels between sites, place managers strategically for latency-optimal Raft consensus, and handle split-brain scenarios with proper quorum distribution',
      'Run separate Swarms and sync manually',
      'Hybrid deployments require Kubernetes only',
    ],
    correctAnswer: 1,
    explanation: 'Hybrid Swarm: establish secure tunnels (WireGuard/IPSec), distribute managers across sites with majority in the most reliable location, account for latency in heartbeat/election timeouts.',
    codeSnippet: '# Hybrid Swarm architecture\n# On-premises: 2 managers + N workers\n# Cloud: 1 manager + N workers\n# VPN: WireGuard tunnel between sites\n\n# WireGuard on each node\n[Interface]\nAddress = 10.200.0.1/24\nPrivateKey = <private-key>\nListenPort = 51820\n\n[Peer]\nPublicKey = <cloud-node-public-key>\nEndpoint = cloud-node.example.com:51820\nAllowedIPs = 10.200.0.0/24\nPersistentKeepalive = 25\n\n# Initialize Swarm with WireGuard IPs\ndocker swarm init --advertise-addr 10.200.0.1\n\n# Tune for higher latency\ndocker swarm update \\\n  --dispatcher-heartbeat 10s  # Longer for cross-site latency\n\n# Placement: keep latency-sensitive services local\ndocker service create --name api \\\n  --constraint \'node.labels.site == on-premises\' \\\n  --placement-pref \'spread=node.labels.rack\' \\\n  myapp',
    tags: ['swarm', 'hybrid', 'multi-site'],
  },
  {
    id: 'docker-orchestration-l4-086',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Kubernetes',
    question: 'How do you implement Kubernetes Pod sandboxing with gVisor or Kata Containers for enhanced isolation?',
    options: [
      'Standard containers provide sufficient isolation',
      'Configure RuntimeClasses to use gVisor (user-space kernel) or Kata Containers (lightweight VMs) for Pods requiring stronger isolation boundaries',
      'Sandboxing requires host-level virtualization only',
      'Pod sandboxing is only for development environments',
    ],
    correctAnswer: 1,
    explanation: 'gVisor provides a user-space kernel that intercepts syscalls for isolation. Kata Containers uses lightweight VMs. RuntimeClasses select the appropriate runtime per-workload.',
    codeSnippet: '# RuntimeClass for gVisor\napiVersion: node.k8s.io/v1\nkind: RuntimeClass\nmetadata:\n  name: gvisor\nhandler: runsc\nscheduling:\n  nodeSelector:\n    sandbox-runtime: gvisor\n---\n# RuntimeClass for Kata Containers\napiVersion: node.k8s.io/v1\nkind: RuntimeClass\nmetadata:\n  name: kata\nhandler: kata-runtime\noverhead:\n  podFixed:\n    memory: "160Mi"\n    cpu: "250m"\n---\n# Pod using gVisor sandbox\napiVersion: v1\nkind: Pod\nmetadata:\n  name: untrusted-workload\nspec:\n  runtimeClassName: gvisor\n  containers:\n  - name: app\n    image: untrusted-app\n    securityContext:\n      runAsNonRoot: true\n      readOnlyRootFilesystem: true',
    tags: ['kubernetes', 'sandboxing', 'gvisor'],
  },
  {
    id: 'docker-orchestration-l4-087',
    language: 'docker',
    level: 'level4',
    category: 'Orchestration',
    subcategory: 'Swarm',
    question: 'How do you design a Docker Swarm production architecture that maximizes availability while minimizing operational complexity?',
    options: [
      'Use a single manager node for simplicity',
      'Deploy 3-5 managers across failure domains, dedicated ingress nodes, monitoring as global services, automated backup/recovery, and IaC for reproducibility',
      'Maximize the number of managers for best availability',
      'Use a managed orchestration service instead',
    ],
    correctAnswer: 1,
    explanation: 'Production architecture: 3-5 managers across AZs (drain mode), dedicated ingress nodes with host-mode LB, global monitoring stack, automated Raft backups, and infrastructure as code for the entire cluster.',
    codeSnippet: '# Production Swarm Architecture (IaC)\n# Managers: 3 nodes across 3 AZs (management only)\n# Ingress: 2+ nodes with host-mode Traefik (global)\n# Workers: N nodes across AZs (application workloads)\n# Monitoring: Prometheus + Grafana + EFK (dedicated nodes)\n\n# Terraform/Ansible setup\nmanagers:\n  count: 3\n  availability: drain  # No application workloads\n  distribution: [az-a, az-b, az-c]\n  backup: daily at 02:00\n\ningress_nodes:\n  count: 2\n  labels: {role: ingress}\n  external_lb: aws_nlb\n\nworkers:\n  min: 6\n  max: 50\n  distribution: [az-a, az-b, az-c]\n  labels_by_capability:\n    gpu: {gpu: \'true\'}\n    memory: {high-memory: \'true\'}\n\nmonitoring:\n  stack: prometheus + grafana + alertmanager\n  log_stack: fluentd + elasticsearch + kibana\n  retention: 30d\n  alerting:\n    - slack: \'#ops-alerts\'\n    - pagerduty: team-infra',
    tags: ['swarm', 'production', 'architecture'],
  },
];

export default questions;
